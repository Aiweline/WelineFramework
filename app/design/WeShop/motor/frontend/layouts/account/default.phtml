<?php
$this->setLayout('base');
?>
<theme:css>Weline_Theme::theme/frontend/assets/css/motor-account-dashboard.css</theme:css>
<section class="motor-account-page">
    <div class="motor-account-shell <?= htmlspecialchars(($meta['class'] ?? '')) ?>" data-layout="motor_account_shell">
        <w:hook>Weline_Theme::frontend::layouts::base::content-before</w:hook>
        <if condition="meta.sidebar">
            <aside class="motor-account-page__aside">
                {{meta.sidebar}}
            </aside>
        </if>

        <div class="motor-account-page__main">
            <w:hook>Weline_Theme::frontend::layouts::account::content-before</w:hook>
            <div class="motor-account-page__surface" id="account-main-content" data-layout="motor_account_main">
                <section data-wslot="account-main"
                         data-wslot-name="Account Main"
                         data-wslot-accept="content,product,banner"
                         data-wslot-multiple="true">
                    <if condition="meta.content">
                        {{meta.content}}
                        <elseif condition="content"/>
                        {{content}}
                        <elseif condition="meta.contentTemplate"/>
                        <?php echo $this->fetch($this->getData("meta.contentTemplate")); ?>
                        <elseif condition="contentTemplate"/>
                        <?php echo $this->fetch($this->getData("contentTemplate")); ?>
                        <else>
                        <?= $this->getChildHtml('content') ?>
                    </if>
                </section>
            </div>
            <w:hook>Weline_Theme::frontend::layouts::account::content-after</w:hook>
        </div>
        <w:hook>Weline_Theme::frontend::layouts::base::content-after</w:hook>
    </div>
</section>
