<?php
/**
 * WeShop Motor Theme - Base Layout
 * 摩托车主题基础布局 - 继承 WeShop Default，覆盖字体、配色、Tailwind 扩展
 */
?>
<!DOCTYPE html>
<html class="light" lang="{{htmlLang}}" data-local="{{lang_local}}" data-lang="{{lang}}" data-theme="motor">
<head>
    <meta charset="utf-8"/>
    <meta content="width=device-width, initial-scale=1.0" name="viewport"/>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>

    <!-- Google Fonts: Oswald（标题/运动感）+ Rajdhani（正文/科技感）+ Material Symbols -->
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700&family=Rajdhani:wght@400;500;600;700&family=Inter:wght@400;500;700;900&display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet"/>

    <!-- Tailwind Config: Motor 品牌色扩展 -->
    <script id="tailwind-config">
      tailwind.config = {
        darkMode: "class",
        theme: {
          extend: {
            colors: {
              "primary": "#e31837",
              "primary-light": "#ff4d5a",
              "primary-dark": "#b8122a",
              "secondary": "#2d2d2d",
              "background-light": "#F2F2F2",
              "background-dark": "#141414",
              "surface-light": "#FFFFFF",
              "surface-dark": "#1f1f1f",
              "text-primary-light": "#1a1a1a",
              "text-primary-dark": "#e5e5e5",
              "text-secondary-light": "#3d3d3d",
              "text-secondary-dark": "#b0b0b0",
              "border-light": "#d0d0d0",
              "border-dark": "#3d3d3d",
              "motor-red": "#e31837",
              "motor-black": "#1a1a1a",
              "motor-steel": "#8c8c8c",
            },
            fontFamily: {
              "display": ["Rajdhani", "Inter", "sans-serif"],
              "heading": ["Oswald", "sans-serif"],
            },
            borderRadius: {"DEFAULT": "0.125rem", "lg": "0.25rem", "xl": "0.375rem", "full": "9999px"},
          },
        },
      }
    </script>

    <style>
      .material-symbols-outlined {
        font-variation-settings:
        'FILL' 0,
        'wght' 400,
        'GRAD' 0,
        'opsz' 24
      }
    </style>

    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::base::head-after</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::default::head-after</w:hook>
    <theme:css>Weline_Theme::theme/frontend/assets/css/motor.css</theme:css>

    <style>
    /* ========================================
       Motor Theme - 布局样式
       ======================================== */

    /* 基础重置和全局样式 */
    :root {
        --motor-layout-content-max-width: var(--weline-layout-content-max-width, var(--weshop-container-max-width, 1440px));
    }

    * {
        box-sizing: border-box;
    }

    html[data-theme="motor"] .max-w-7xl {
        max-width: var(--motor-layout-content-max-width);
    }

    html {
        height: 100%;
    }

    body {
        margin: 0;
        padding: 0;
        font-family: var(--weline-layout-font-family, var(--weshop-font-family, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif));
        font-size: var(--weline-layout-font-size-base, var(--font-size-base, var(--weshop-font-size-base, 1rem)));
        line-height: var(--weline-layout-line-height-normal, var(--line-height-normal, var(--weshop-line-height-normal, 1.5)));
        color: var(--weline-layout-text-primary, var(--color-text-primary, #111));
        background-color: var(--weline-layout-surface-primary, var(--color-bg-primary, #fff));
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
    }

    /* 主内容区域 */
    #main-content[data-layout="main_content"] {
        width: 100%;
        padding: 0;
        flex: 1 0 auto;
        min-height: 0;
    }

    /* 响应式容器 */
    @media (max-width: 1400px) {
        #main-content[data-layout="main_content"] {
            padding: 0;
        }
    }

    @media (max-width: 1024px) {
        #main-content[data-layout="main_content"] {
            padding: 0;
        }
    }

    @media (max-width: 768px) {
        #main-content[data-layout="main_content"] {
            padding: 0;
        }
    }

    /* 内容区域样式 */
    #main-content[data-layout="main_content"] > * {
        width: 100%;
    }

    /* 平滑滚动 */
    html {
        scroll-behavior: smooth;
    }

    /* 链接样式 */
    a {
        color: var(--weline-layout-link-color, var(--color-link, #007185));
        text-decoration: none;
        transition: color 0.2s ease, text-decoration 0.2s ease;
    }

    a:hover {
        color: var(--weline-layout-link-hover-color, var(--color-link-hover, #c45500));
        text-decoration: underline;
    }

    /* 按钮基础样式 */
    button {
        cursor: pointer;
        transition: all 0.2s ease;
    }

    /* 输入框基础样式 */
    input, textarea, select {
        font-family: inherit;
        font-size: inherit;
    }

    /* 清除默认样式 */
    ul, ol {
        margin: 0;
        padding: 0;
        list-style: none;
    }

    /* 图片响应式 */
    img {
        max-width: 100%;
        height: auto;
    }

    /* 工具类 */
    .clearfix::after {
        content: "";
        display: table;
        clear: both;
    }

    .hidden {
        display: none !important;
    }

    .sr-only {
        position: absolute;
        width: 1px;
        height: 1px;
        padding: 0;
        margin: -1px;
        overflow: hidden;
        clip: rect(0, 0, 0, 0);
        white-space: nowrap;
        border-width: 0;
    }
    </style>
</head>
<body class="font-display bg-background-light dark:bg-background-dark text-text-primary-light dark:text-text-primary-dark">
    <!-- Hook: body-start -->
    <w:hook>Weline_Theme::frontend::layouts::base::body-start</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::default::body-start</w:hook>

    <div class="relative flex min-h-screen w-full flex-col group/design-root overflow-x-hidden">
        <div class="layout-container flex h-full grow flex-col">

            <if condition="meta.showHeader">
                <!-- Hook: header-before -->
                <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>

                <!-- Header -->
                <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>

                <!-- Hook: header-after -->
                <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>
            </if>

            <w:hook>Weline_Theme::frontend::layouts::base::breadcrumb-before</w:hook>
            <if condition="meta.showBreadcrumb !== false">
              <w:block class="Weline\Theme\Block\Partials" area="frontend" type="breadcrumb" default-option="default"/>
            </if>
            <w:hook>Weline_Theme::frontend::layouts::base::breadcrumb-after</w:hook>

            <!-- Main Content -->
            <main class="flex-1" id="main-content" data-layout="main_content">
                <!-- Hook: content-before -->
                <w:hook>Weline_Theme::frontend::layouts::base::content-before</w:hook>
                <w:hook>Weline_Theme::frontend::layouts::default::content-before</w:hook>

                <?= $this->getChildHtml('content') ?>

                <!-- Hook: content-after -->
                <w:hook>Weline_Theme::frontend::layouts::default::content-after</w:hook>
                <w:hook>Weline_Theme::frontend::layouts::base::content-after</w:hook>
            </main>

            <if condition="meta.showFooter">
                <!-- Hook: footer-before -->
                <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>

                <!-- Footer -->
                <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default" vars="copyright,links,showBackToTop"/>

                <!-- Hook: footer-after -->
                <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
            </if>

        </div>
    </div>

    <!-- Hook: body-end -->
    <w:hook>Weline_Theme::frontend::layouts::default::body-end</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::body-end</w:hook>

    <!-- Weline.Api 同步回退（确保 request 立即可用） -->
    <theme:js>Weline_Theme::theme/frontend/assets/js/motor.js</theme:js>

<script>
(function() {
    'use strict';

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }

    function init() {
        initSmoothScroll();
        initExternalLinks();
    }

    function initSmoothScroll() {
        document.querySelectorAll('a[href^="#"]').forEach(function(anchor) {
            anchor.addEventListener('click', function(e) {
                const href = this.getAttribute('href');
                if (href === '#' || href === '#!') {
                    e.preventDefault();
                    return;
                }

                const target = document.querySelector(href);
                if (target) {
                    e.preventDefault();
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    }

    function initExternalLinks() {
        document.querySelectorAll('a[href^="http"]').forEach(function(link) {
            const hostname = window.location.hostname;
            const linkHostname = new URL(link.href).hostname;

            if (linkHostname !== hostname) {
                link.setAttribute('target', '_blank');
                link.setAttribute('rel', 'noopener noreferrer');
            }
        });
    }
})();
</script>
</body>
</html>
