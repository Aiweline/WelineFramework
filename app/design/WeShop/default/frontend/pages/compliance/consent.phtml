<?php
$items = array_values((array) ($this->getData('consent_items') ?? []));
$saveUrl = (string) ($this->getData('save_url') ?? $this->getUrl('compliance/consent/save'));
$privacyUrl = (string) ($this->getData('privacy_url') ?? $this->getUrl('compliance/privacy'));
?>
<main class="mx-auto flex w-full max-w-6xl flex-1 flex-col gap-8 px-4 py-8 sm:px-6 lg:px-8">
    <section class="rounded-[2rem] border border-border-light bg-surface-light p-8 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary"><?= __('Consent Workspace') ?></p>
        <h1 class="mt-4 text-3xl font-black tracking-tight text-text-primary-light dark:text-text-primary-dark"><?= __('Update Consent Preferences') ?></h1>
        <p class="mt-3 max-w-2xl text-base leading-7 text-text-secondary-light dark:text-text-secondary-dark">
            <?= __('Use this page to update optional communications and acknowledge required policy items for your account.') ?>
        </p>
        <a href="<?= htmlspecialchars($privacyUrl, ENT_QUOTES) ?>" class="mt-5 inline-flex items-center justify-center rounded-2xl border border-border-light px-5 py-3 text-sm font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark">
            <?= __('Review Privacy Details') ?>
        </a>
    </section>

    <w:hook name="WeShop_Compliance::frontend::layouts::compliance-page::before"/>

    <section class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="grid gap-4">
            <?php foreach ($items as $item): ?>
                <article class="rounded-2xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                    <h2 class="text-lg font-bold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars((string) ($item['title'] ?? ''), ENT_QUOTES) ?></h2>
                    <p class="mt-2 text-sm leading-6 text-text-secondary-light dark:text-text-secondary-dark"><?= htmlspecialchars((string) ($item['description'] ?? ''), ENT_QUOTES) ?></p>
                    <div class="mt-4 flex flex-wrap items-center gap-3">
                        <span class="inline-flex rounded-full px-3 py-1 text-xs font-semibold <?= !empty($item['accepted']) ? 'bg-emerald-500/15 text-emerald-700' : 'bg-rose-500/15 text-rose-700' ?>">
                            <?= !empty($item['accepted']) ? __('Accepted') : __('Not Accepted') ?>
                        </span>
                        <form method="post" action="<?= htmlspecialchars($saveUrl, ENT_QUOTES) ?>">
                            <?= $this->getFormKey($saveUrl) ?>
                            <input type="hidden" name="consent_type" value="<?= htmlspecialchars((string) ($item['code'] ?? ''), ENT_QUOTES) ?>">
                            <input type="hidden" name="is_accepted" value="<?= !empty($item['accepted']) ? '0' : '1' ?>">
                            <button type="submit" class="inline-flex items-center justify-center rounded-xl bg-primary px-4 py-2 text-sm font-semibold text-white transition hover:bg-primary/90">
                                <?= !empty($item['accepted']) ? __('Disable') : __('Enable') ?>
                            </button>
                        </form>
                    </div>
                    <w:hook name="WeShop_Compliance::frontend::partials::consent-item::after"/>
                </article>
            <?php endforeach; ?>
        </div>
    </section>
</main>

