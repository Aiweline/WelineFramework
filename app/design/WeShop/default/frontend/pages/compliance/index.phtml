<?php
$items = array_values((array) ($this->getData('consent_items') ?? []));
$saveUrl = (string) ($this->getData('save_url') ?? $this->getUrl('compliance/consent/save'));
$privacyUrl = (string) ($this->getData('privacy_url') ?? $this->getUrl('compliance/privacy'));
$consentUrl = (string) ($this->getData('consent_url') ?? $this->getUrl('compliance/consent'));
$acceptedCount = (int) ($this->getData('consent_accepted_count') ?? 0);
$consentCount = (int) ($this->getData('consent_count') ?? count($items));
?>
<main class="mx-auto flex w-full max-w-7xl flex-1 flex-col gap-8 px-4 py-8 sm:px-6 lg:px-8">
    <section class="rounded-[2rem] border border-border-light bg-surface-light shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="grid gap-6 p-8 lg:grid-cols-[1.25fr_0.75fr] lg:p-10">
            <div>
                <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary"><?= __('Compliance Center') ?></p>
                <h1 class="mt-4 text-3xl font-black tracking-tight text-text-primary-light dark:text-text-primary-dark sm:text-4xl"><?= __('Consent Management') ?></h1>
                <p class="mt-3 max-w-2xl text-base leading-7 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('Manage privacy-related preferences, track required acknowledgements, and keep account permissions aligned with your needs.') ?>
                </p>
                <div class="mt-6 flex flex-wrap gap-3">
                    <a href="<?= htmlspecialchars($consentUrl, ENT_QUOTES) ?>" class="inline-flex items-center justify-center rounded-2xl bg-primary px-5 py-3 text-sm font-semibold text-white transition hover:bg-primary/90">
                        <?= __('Open Full Consent View') ?>
                    </a>
                    <a href="<?= htmlspecialchars($privacyUrl, ENT_QUOTES) ?>" class="inline-flex items-center justify-center rounded-2xl border border-border-light px-5 py-3 text-sm font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark">
                        <?= __('Read Privacy Policy') ?>
                    </a>
                </div>
            </div>
            <div class="rounded-3xl border border-border-light bg-background-light p-6 dark:border-border-dark dark:bg-background-dark">
                <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Preferences Accepted') ?></p>
                <p class="mt-3 text-5xl font-black text-primary"><?= $acceptedCount ?>/<?= $consentCount ?></p>
                <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Keep this score current to avoid compliance interruptions during account operations.') ?></p>
            </div>
        </div>
    </section>

    <w:hook name="WeShop_Compliance::frontend::layouts::compliance-page::before"/>

    <section class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <h2 class="text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Consent Preferences') ?></h2>
        <div class="mt-6 grid gap-4">
            <?php foreach ($items as $item): ?>
                <article class="rounded-2xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                    <div class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between">
                        <div>
                            <h3 class="text-lg font-bold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars((string) ($item['title'] ?? ''), ENT_QUOTES) ?></h3>
                            <p class="mt-2 text-sm leading-6 text-text-secondary-light dark:text-text-secondary-dark"><?= htmlspecialchars((string) ($item['description'] ?? ''), ENT_QUOTES) ?></p>
                            <?php if (!empty($item['required'])): ?>
                                <p class="mt-3 inline-flex rounded-full bg-amber-500/15 px-3 py-1 text-xs font-semibold text-amber-700"><?= __('Required') ?></p>
                            <?php endif; ?>
                        </div>
                        <div class="flex flex-col items-start gap-3 sm:items-end">
                            <span class="inline-flex rounded-full px-3 py-1 text-xs font-semibold <?= !empty($item['accepted']) ? 'bg-emerald-500/15 text-emerald-700' : 'bg-rose-500/15 text-rose-700' ?>">
                                <?= !empty($item['accepted']) ? __('Accepted') : __('Not Accepted') ?>
                            </span>
                            <form method="post" action="<?= htmlspecialchars($saveUrl, ENT_QUOTES) ?>">
                                <?= $this->getFormKey($saveUrl) ?>
                                <input type="hidden" name="consent_type" value="<?= htmlspecialchars((string) ($item['code'] ?? ''), ENT_QUOTES) ?>">
                                <input type="hidden" name="is_accepted" value="<?= !empty($item['accepted']) ? '0' : '1' ?>">
                                <button type="submit" class="inline-flex items-center justify-center rounded-xl border border-border-light px-4 py-2 text-sm font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark">
                                    <?= !empty($item['accepted']) ? __('Revoke') : __('Accept') ?>
                                </button>
                            </form>
                        </div>
                    </div>
                    <w:hook name="WeShop_Compliance::frontend::partials::consent-item::after"/>
                </article>
            <?php endforeach; ?>
        </div>
    </section>
</main>

