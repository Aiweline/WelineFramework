<?php
$sections = array_values((array) ($this->getData('sections') ?? []));
$consentUrl = (string) ($this->getData('consent_url') ?? $this->getUrl('compliance/consent'));
$complianceUrl = (string) ($this->getData('compliance_url') ?? $this->getUrl('compliance'));
?>
<main class="mx-auto flex w-full max-w-5xl flex-1 flex-col gap-8 px-4 py-8 sm:px-6 lg:px-8">
    <section class="rounded-[2rem] border border-border-light bg-surface-light p-8 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary"><?= __('Privacy') ?></p>
        <h1 class="mt-4 text-3xl font-black tracking-tight text-text-primary-light dark:text-text-primary-dark"><?= __('Privacy Policy Summary') ?></h1>
        <p class="mt-3 max-w-3xl text-base leading-7 text-text-secondary-light dark:text-text-secondary-dark">
            <?= __('This summary highlights how storefront data is collected, processed, retained, and controlled. It complements the full legal policy text.') ?>
        </p>
        <div class="mt-6 flex flex-wrap gap-3">
            <a href="<?= htmlspecialchars($consentUrl, ENT_QUOTES) ?>" class="inline-flex items-center justify-center rounded-2xl bg-primary px-5 py-3 text-sm font-semibold text-white transition hover:bg-primary/90">
                <?= __('Manage Consent') ?>
            </a>
            <a href="<?= htmlspecialchars($complianceUrl, ENT_QUOTES) ?>" class="inline-flex items-center justify-center rounded-2xl border border-border-light px-5 py-3 text-sm font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark">
                <?= __('Back to Compliance Center') ?>
            </a>
        </div>
    </section>

    <w:hook name="WeShop_Compliance::frontend::layouts::compliance-page::before"/>

    <section class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="space-y-5">
            <?php foreach ($sections as $section): ?>
                <article class="rounded-2xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                    <h2 class="text-lg font-bold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars((string) ($section['title'] ?? ''), ENT_QUOTES) ?></h2>
                    <p class="mt-3 text-sm leading-7 text-text-secondary-light dark:text-text-secondary-dark"><?= htmlspecialchars((string) ($section['body'] ?? ''), ENT_QUOTES) ?></p>
                </article>
            <?php endforeach; ?>
        </div>
    </section>
</main>

