<?php
$items = (array) ($this->getData('items') ?? []);
$statusOptions = (array) ($this->getData('status_options') ?? []);
$currentStatus = (string) ($this->getData('current_status') ?? '');
$total = (int) ($this->getData('total') ?? 0);
$listUrl = (string) ($this->getData('list_url') ?? $this->getUrl('subscription'));
?>
<main class="mx-auto flex w-full max-w-7xl flex-1 flex-col gap-8 px-4 py-8 sm:px-6 lg:px-8">
    <section class="rounded-[2rem] border border-border-light bg-surface-light shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="grid gap-6 p-8 lg:grid-cols-[1.2fr_0.8fr] lg:p-10">
            <div>
                <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary"><?= __('Recurring Orders') ?></p>
                <h1 class="mt-4 text-3xl font-black tracking-tight text-text-primary-light dark:text-text-primary-dark sm:text-4xl"><?= __('My Subscriptions') ?></h1>
                <p class="mt-3 max-w-2xl text-base leading-7 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('Manage recurring purchases, adjust plan status, and check your next billing windows from one place.') ?>
                </p>
            </div>
            <div class="rounded-3xl border border-border-light bg-background-light p-6 text-center dark:border-border-dark dark:bg-background-dark">
                <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Total Subscriptions') ?></p>
                <p class="mt-3 text-5xl font-black text-primary"><?= $total ?></p>
                <p class="mt-1 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Includes active, paused, and historical plans for this account.') ?></p>
            </div>
        </div>
    </section>

    <section class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
            <h2 class="text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Subscription List') ?></h2>
            <form method="get" action="<?= htmlspecialchars($listUrl, ENT_QUOTES) ?>" class="flex items-center gap-3">
                <label for="status" class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Status') ?></label>
                <select id="status" name="status" class="rounded-xl border border-border-light bg-background-light px-3 py-2 text-sm text-text-primary-light dark:border-border-dark dark:bg-background-dark dark:text-text-primary-dark">
                    <option value=""><?= __('All') ?></option>
                    <?php foreach ($statusOptions as $status => $label): ?>
                        <option value="<?= htmlspecialchars((string) $status, ENT_QUOTES) ?>"<?= $currentStatus === (string) $status ? ' selected' : '' ?>>
                            <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                        </option>
                    <?php endforeach; ?>
                </select>
                <button type="submit" class="inline-flex items-center justify-center rounded-xl border border-border-light px-4 py-2 text-sm font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark">
                    <?= __('Apply') ?>
                </button>
            </form>
        </div>

        <w:hook name="WeShop_Subscription::frontend::layouts::subscription::page-before"/>

        <?php if ($items): ?>
            <div class="mt-6 space-y-4">
                <?php foreach ($items as $item): ?>
                    <article class="rounded-2xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                        <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
                            <div>
                                <p class="text-xs font-semibold uppercase tracking-[0.2em] text-text-secondary-light dark:text-text-secondary-dark">
                                    <?= __('Subscription') ?> #<?= (int) ($item['subscription_id'] ?? 0) ?>
                                </p>
                                <h3 class="mt-2 text-lg font-bold text-text-primary-light dark:text-text-primary-dark">
                                    <?= htmlspecialchars((string) ($item['billing_label'] ?? ''), ENT_QUOTES) ?>
                                </h3>
                                <p class="mt-1 text-sm text-text-secondary-light dark:text-text-secondary-dark">
                                    <?= htmlspecialchars((string) ($item['currency'] ?? 'USD'), ENT_QUOTES) ?>
                                    <?= number_format((float) ($item['price'] ?? 0), 2) ?>
                                </p>
                            </div>
                            <span class="inline-flex items-center rounded-full border border-primary/30 bg-primary/10 px-3 py-1 text-sm font-semibold text-primary">
                                <?= htmlspecialchars((string) ($item['status_label'] ?? ''), ENT_QUOTES) ?>
                            </span>
                        </div>

                        <div class="mt-4 grid gap-3 text-sm text-text-secondary-light dark:text-text-secondary-dark sm:grid-cols-3">
                            <p><?= __('Current Period') ?>: <?= htmlspecialchars((string) ($item['current_period_start'] ?? ''), ENT_QUOTES) ?> - <?= htmlspecialchars((string) ($item['current_period_end'] ?? ''), ENT_QUOTES) ?></p>
                            <p><?= __('Next Billing') ?>: <?= htmlspecialchars((string) ($item['next_billing_at'] ?? ''), ENT_QUOTES) ?></p>
                            <p><?= __('Renewal Count') ?>: <?= (int) ($item['renewal_count'] ?? 0) ?></p>
                        </div>

                        <div class="mt-5 flex flex-wrap gap-2">
                            <a href="<?= $this->getUrl('subscription/view', ['id' => (int) ($item['subscription_id'] ?? 0)]) ?>" class="inline-flex items-center justify-center rounded-xl border border-border-light px-4 py-2 text-sm font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark">
                                <?= __('View Details') ?>
                            </a>
                            <?php if (!empty($item['can_pause'])): ?>
                                <form method="post" action="<?= $this->getUrl('subscription/pause') ?>">
                                    <?= $this->getFormKey($this->getUrl('subscription/pause')) ?>
                                    <input type="hidden" name="id" value="<?= (int) ($item['subscription_id'] ?? 0) ?>">
                                    <button type="submit" class="inline-flex items-center justify-center rounded-xl border border-amber-500/40 px-4 py-2 text-sm font-semibold text-amber-600 transition hover:bg-amber-500 hover:text-white">
                                        <?= __('Pause') ?>
                                    </button>
                                </form>
                            <?php endif; ?>
                            <?php if (!empty($item['can_resume'])): ?>
                                <form method="post" action="<?= $this->getUrl('subscription/pause/resume') ?>">
                                    <?= $this->getFormKey($this->getUrl('subscription/pause/resume')) ?>
                                    <input type="hidden" name="id" value="<?= (int) ($item['subscription_id'] ?? 0) ?>">
                                    <button type="submit" class="inline-flex items-center justify-center rounded-xl border border-emerald-500/40 px-4 py-2 text-sm font-semibold text-emerald-600 transition hover:bg-emerald-500 hover:text-white">
                                        <?= __('Resume') ?>
                                    </button>
                                </form>
                            <?php endif; ?>
                            <?php if (!empty($item['can_cancel'])): ?>
                                <form method="post" action="<?= $this->getUrl('subscription/cancel') ?>" class="flex items-center gap-2">
                                    <?= $this->getFormKey($this->getUrl('subscription/cancel')) ?>
                                    <input type="hidden" name="id" value="<?= (int) ($item['subscription_id'] ?? 0) ?>">
                                    <input type="text" name="reason" class="w-52 rounded-xl border border-border-light bg-surface-light px-3 py-2 text-sm dark:border-border-dark dark:bg-surface-dark" placeholder="<?= __('Cancellation reason (optional)') ?>">
                                    <button type="submit" class="inline-flex items-center justify-center rounded-xl border border-rose-500/40 px-4 py-2 text-sm font-semibold text-rose-600 transition hover:bg-rose-500 hover:text-white">
                                        <?= __('Cancel Subscription') ?>
                                    </button>
                                </form>
                            <?php endif; ?>
                        </div>

                        <w:hook name="WeShop_Subscription::frontend::layouts::subscription::item-after"/>
                    </article>
                <?php endforeach; ?>
            </div>
        <?php else: ?>
            <div class="mt-6 rounded-3xl border border-dashed border-border-light bg-background-light px-6 py-10 text-center dark:border-border-dark dark:bg-background-dark">
                <span class="material-symbols-outlined text-5xl text-primary">event_repeat</span>
                <h3 class="mt-4 text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('No subscriptions yet') ?></h3>
                <p class="mx-auto mt-2 max-w-xl text-sm leading-6 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('Recurring plans will appear here once you place products with subscription-enabled checkout options.') ?>
                </p>
            </div>
        <?php endif; ?>
    </section>
</main>
