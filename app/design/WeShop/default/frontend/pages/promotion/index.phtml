<?php
$items = (array) ($this->getData('items') ?? []);
$promotions = (array) ($this->getData('promotions') ?? []);
$pageType = (string) ($this->getData('page_type') ?? 'index');
$total = (int) ($this->getData('total') ?? count($items));
$listUrl = (string) ($this->getData('list_url') ?? $this->getUrl('promotion'));
$dealsUrl = (string) ($this->getData('deals_url') ?? $this->getUrl('promotion/deals'));
$saleUrl = (string) ($this->getData('sale_url') ?? $this->getUrl('promotion/sale'));

$pageTitle = match ($pageType) {
    'deals' => (string) __('Today Deals'),
    'sale' => (string) __('Seasonal Sale'),
    default => (string) __('Promotion Hub'),
};
?>
<main class="mx-auto flex w-full max-w-7xl flex-1 flex-col gap-8 px-4 py-8 sm:px-6 lg:px-8">
    <section class="rounded-[2rem] border border-border-light bg-surface-light shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="grid gap-6 p-8 lg:grid-cols-[1.2fr_0.8fr] lg:p-10">
            <div>
                <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary"><?= __('Promotion Center') ?></p>
                <h1 class="mt-4 text-3xl font-black tracking-tight text-text-primary-light dark:text-text-primary-dark sm:text-4xl"><?= htmlspecialchars($pageTitle, ENT_QUOTES) ?></h1>
                <p class="mt-3 max-w-2xl text-base leading-7 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('Explore campaign products, compare discounts, and move directly into checkout-ready offers.') ?>
                </p>
                <div class="mt-8 flex flex-wrap gap-2">
                    <a href="<?= htmlspecialchars($listUrl, ENT_QUOTES) ?>" class="inline-flex items-center justify-center rounded-2xl px-4 py-2 text-sm font-semibold <?= $pageType === 'index' ? 'bg-primary text-white' : 'border border-border-light text-text-primary-light hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark' ?>">
                        <?= __('Hub') ?>
                    </a>
                    <a href="<?= htmlspecialchars($dealsUrl, ENT_QUOTES) ?>" class="inline-flex items-center justify-center rounded-2xl px-4 py-2 text-sm font-semibold <?= $pageType === 'deals' ? 'bg-primary text-white' : 'border border-border-light text-text-primary-light hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark' ?>">
                        <?= __('Deals') ?>
                    </a>
                    <a href="<?= htmlspecialchars($saleUrl, ENT_QUOTES) ?>" class="inline-flex items-center justify-center rounded-2xl px-4 py-2 text-sm font-semibold <?= $pageType === 'sale' ? 'bg-primary text-white' : 'border border-border-light text-text-primary-light hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark' ?>">
                        <?= __('Sale') ?>
                    </a>
                </div>
            </div>
            <div class="rounded-3xl border border-border-light bg-background-light p-6 text-center dark:border-border-dark dark:bg-background-dark">
                <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Matched Products') ?></p>
                <p class="mt-3 text-5xl font-black text-primary"><?= $total ?></p>
                <p class="mt-1 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Campaign candidates currently available for this storefront view.') ?></p>
            </div>
        </div>
    </section>

    <?php if ($promotions): ?>
        <section class="grid gap-4 md:grid-cols-2">
            <?php foreach ($promotions as $promotion): ?>
                <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
                    <h2 class="text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars((string) ($promotion['title'] ?? ''), ENT_QUOTES) ?></h2>
                    <p class="mt-2 text-sm leading-6 text-text-secondary-light dark:text-text-secondary-dark"><?= htmlspecialchars((string) ($promotion['subtitle'] ?? ''), ENT_QUOTES) ?></p>
                    <a href="<?= htmlspecialchars((string) ($promotion['action_url'] ?? $listUrl), ENT_QUOTES) ?>" class="mt-5 inline-flex items-center justify-center rounded-2xl bg-primary px-4 py-3 text-sm font-semibold text-white transition hover:bg-primary/90">
                        <?= htmlspecialchars((string) ($promotion['action_label'] ?? __('Open')), ENT_QUOTES) ?>
                    </a>
                </article>
            <?php endforeach; ?>
        </section>
    <?php endif; ?>

    <section class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <h2 class="text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Promotion Products') ?></h2>
        <w:hook name="WeShop_Promotion::frontend::layouts::promotion::page-before"/>
        <?php if ($items): ?>
            <div class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
                <?php foreach ($items as $item): ?>
                    <article class="group overflow-hidden rounded-2xl border border-border-light bg-background-light transition hover:-translate-y-1 hover:border-primary dark:border-border-dark dark:bg-background-dark">
                        <a href="<?= htmlspecialchars((string) ($item['url'] ?? '#'), ENT_QUOTES) ?>" class="block">
                            <div class="aspect-square overflow-hidden bg-surface-light dark:bg-surface-dark">
                                <img src="<?= htmlspecialchars((string) ($item['image'] ?? ''), ENT_QUOTES) ?>" alt="<?= htmlspecialchars((string) ($item['name'] ?? ''), ENT_QUOTES) ?>" class="h-full w-full object-cover transition duration-500 group-hover:scale-105">
                            </div>
                            <div class="p-4">
                                <h3 class="line-clamp-2 text-sm font-semibold text-text-primary-light group-hover:text-primary dark:text-text-primary-dark"><?= htmlspecialchars((string) ($item['name'] ?? ''), ENT_QUOTES) ?></h3>
                                <p class="mt-1 line-clamp-2 text-xs text-text-secondary-light dark:text-text-secondary-dark"><?= htmlspecialchars((string) ($item['short_description'] ?? ''), ENT_QUOTES) ?></p>
                                <div class="mt-3 flex items-center gap-2">
                                    <span class="text-lg font-black text-primary"><?= $this->formatPrice((float) ($item['price'] ?? 0)) ?></span>
                                    <?php if ((float) ($item['original_price'] ?? 0) > (float) ($item['price'] ?? 0)): ?>
                                        <span class="text-xs text-text-secondary-light line-through dark:text-text-secondary-dark"><?= $this->formatPrice((float) $item['original_price']) ?></span>
                                        <span class="rounded-full bg-primary/10 px-2 py-0.5 text-xs font-semibold text-primary">-<?= (int) ($item['discount_percent'] ?? 0) ?>%</span>
                                    <?php endif; ?>
                                </div>
                            </div>
                        </a>
                    </article>
                <?php endforeach; ?>
            </div>
        <?php else: ?>
            <div class="mt-6 rounded-3xl border border-dashed border-border-light bg-background-light px-6 py-10 text-center dark:border-border-dark dark:bg-background-dark">
                <span class="material-symbols-outlined text-5xl text-primary">sell</span>
                <h3 class="mt-4 text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('No promotion products yet') ?></h3>
                <p class="mx-auto mt-2 max-w-xl text-sm leading-6 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('This campaign view has no eligible products right now. Check again soon for refreshed deals.') ?>
                </p>
            </div>
        <?php endif; ?>
        <w:hook name="WeShop_Promotion::frontend::layouts::promotion::page-after"/>
    </section>
</main>
