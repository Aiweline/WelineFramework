<?php
$membership = (array) ($this->getData('membership') ?? []);
$benefits = array_values((array) ($this->getData('benefits') ?? []));
$tiers = array_values((array) ($this->getData('tiers') ?? []));
?>
<main class="mx-auto flex w-full max-w-7xl flex-1 flex-col gap-8 px-4 py-8 sm:px-6 lg:px-8">
    <section class="rounded-[2rem] border border-border-light bg-surface-light shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="grid gap-6 p-8 lg:grid-cols-[1.2fr_0.8fr] lg:p-10">
            <div>
                <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary"><?= __('Member Center') ?></p>
                <h1 class="mt-4 text-3xl font-black tracking-tight text-text-primary-light dark:text-text-primary-dark sm:text-4xl"><?= __('My Membership') ?></h1>
                <p class="mt-3 max-w-2xl text-base leading-7 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('Track your points, compare tier benefits, and unlock stronger storefront privileges as you grow.') ?>
                </p>
            </div>
            <div class="rounded-3xl border border-border-light bg-background-light p-6 dark:border-border-dark dark:bg-background-dark">
                <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Current Tier') ?></p>
                <p class="mt-3 text-4xl font-black text-primary"><?= htmlspecialchars((string) ($membership['level_label'] ?? ''), ENT_QUOTES) ?></p>
                <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Points: %1', (int) ($membership['points'] ?? 0)) ?></p>
                <?php if (!empty($membership['is_top_tier'])): ?>
                    <p class="mt-2 text-sm font-semibold text-emerald-600"><?= __('You are already at the highest tier.') ?></p>
                <?php else: ?>
                    <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark">
                        <?= __('%1 points to %2', (int) ($membership['points_to_next'] ?? 0), (string) ($membership['next_level_label'] ?? '')) ?>
                    </p>
                    <div class="mt-3 h-2 rounded-full bg-border-light dark:bg-border-dark">
                        <div class="h-2 rounded-full bg-primary" style="width: <?= (int) ($membership['progress_percent'] ?? 0) ?>%;"></div>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </section>

    <w:hook name="WeShop_Membership::frontend::layouts::membership::page-before"/>

    <section class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <h2 class="text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Current Tier Benefits') ?></h2>
        <?php if ($benefits): ?>
            <ul class="mt-5 grid gap-3 sm:grid-cols-2">
                <?php foreach ($benefits as $benefit): ?>
                    <li class="rounded-2xl border border-border-light bg-background-light px-4 py-3 text-sm text-text-primary-light dark:border-border-dark dark:bg-background-dark dark:text-text-primary-dark">
                        <?= htmlspecialchars((string) $benefit, ENT_QUOTES) ?>
                    </li>
                <?php endforeach; ?>
            </ul>
        <?php else: ?>
            <p class="mt-4 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Tier benefits will appear here once your membership profile is available.') ?></p>
        <?php endif; ?>
        <w:hook name="WeShop_Membership::frontend::partials::membership-tier::benefits-after"/>
    </section>

    <section class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <h2 class="text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Tier Roadmap') ?></h2>
        <div class="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">
            <?php foreach ($tiers as $tier): ?>
                <?php $isCurrent = !empty($tier['is_current']); ?>
                <?php $isUnlocked = !empty($tier['is_unlocked']); ?>
                <article class="rounded-2xl border px-4 py-4 <?= $isCurrent ? 'border-primary bg-primary/5' : 'border-border-light bg-background-light dark:border-border-dark dark:bg-background-dark' ?>">
                    <div class="flex items-center justify-between gap-3">
                        <h3 class="text-lg font-bold <?= $isCurrent ? 'text-primary' : 'text-text-primary-light dark:text-text-primary-dark' ?>">
                            <?= htmlspecialchars((string) ($tier['label'] ?? ''), ENT_QUOTES) ?>
                        </h3>
                        <?php if ($isCurrent): ?>
                            <span class="rounded-full bg-primary px-2 py-1 text-xs font-semibold text-white"><?= __('Current') ?></span>
                        <?php elseif ($isUnlocked): ?>
                            <span class="rounded-full bg-emerald-500 px-2 py-1 text-xs font-semibold text-white"><?= __('Unlocked') ?></span>
                        <?php endif; ?>
                    </div>
                    <p class="mt-3 text-sm text-text-secondary-light dark:text-text-secondary-dark">
                        <?= __('Unlock at %1 points', (int) ($tier['threshold'] ?? 0)) ?>
                    </p>
                </article>
            <?php endforeach; ?>
        </div>
    </section>
</main>

