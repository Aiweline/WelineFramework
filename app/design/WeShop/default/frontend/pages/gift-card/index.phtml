<?php
$giftCards = (array) ($this->getData('gift_cards') ?? []);
$giftCardCount = (int) ($this->getData('gift_card_count') ?? 0);
$activeCount = (int) ($this->getData('gift_card_active_count') ?? 0);
$totalBalance = (float) ($this->getData('gift_card_total_balance') ?? 0);
$totalAmount = (float) ($this->getData('gift_card_total_amount') ?? 0);
?>
<main class="mx-auto flex w-full max-w-7xl flex-1 flex-col gap-8 px-4 py-8 sm:px-6 lg:px-8">
    <w:hook name="WeShop_GiftCard::frontend::layouts::gift-card-page::before"/>

    <section class="overflow-hidden rounded-[2rem] border border-border-light bg-surface-light shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="grid gap-6 p-8 lg:grid-cols-[1.2fr_0.8fr] lg:p-10">
            <div>
                <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary"><?= __('Store Credit') ?></p>
                <h1 class="mt-4 text-3xl font-black tracking-tight text-text-primary-light dark:text-text-primary-dark sm:text-4xl"><?= __('Gift Card Wallet') ?></h1>
                <p class="mt-3 max-w-2xl text-base leading-7 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('Manage your gift card balances, check expiration dates, and prepare value for future checkout sessions.') ?>
                </p>

                <div class="mt-8 flex flex-wrap gap-3 text-sm">
                    <a href="<?= $this->getUrl('weshop/customer/account/index') ?>" class="inline-flex items-center justify-center rounded-2xl bg-primary px-5 py-3 font-semibold text-white transition hover:bg-primary/90">
                        <?= __('Back to Account') ?>
                    </a>
                    <a href="<?= $this->getUrl('checkout') ?>" class="inline-flex items-center justify-center rounded-2xl border border-border-light px-5 py-3 font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark">
                        <?= __('Go to Checkout') ?>
                    </a>
                </div>
            </div>

            <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-1">
                <article class="rounded-3xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                    <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Total Cards') ?></p>
                    <p class="mt-2 text-3xl font-black text-text-primary-light dark:text-text-primary-dark"><?= $giftCardCount ?></p>
                    <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Gift cards currently attached to this account.') ?></p>
                </article>
                <article class="rounded-3xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                    <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Active Cards') ?></p>
                    <p class="mt-2 text-3xl font-black text-text-primary-light dark:text-text-primary-dark"><?= $activeCount ?></p>
                    <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Cards available for immediate use at checkout.') ?></p>
                </article>
            </div>
        </div>
    </section>

    <section class="grid gap-4 sm:grid-cols-2">
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Available Balance') ?></p>
            <p class="mt-2 text-3xl font-black text-text-primary-light dark:text-text-primary-dark"><?= $this->formatPrice($totalBalance) ?></p>
            <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Current balance that can be applied to future orders.') ?></p>
        </article>
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Issued Value') ?></p>
            <p class="mt-2 text-3xl font-black text-text-primary-light dark:text-text-primary-dark"><?= $this->formatPrice($totalAmount) ?></p>
            <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Total face value of cards assigned to this account.') ?></p>
        </article>
    </section>

    <section class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="flex items-center justify-between gap-4">
            <div>
                <p class="text-sm font-semibold uppercase tracking-[0.2em] text-primary"><?= __('Card List') ?></p>
                <h2 class="mt-2 text-2xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Your Gift Cards') ?></h2>
            </div>
            <span class="material-symbols-outlined text-4xl text-primary">card_giftcard</span>
        </div>

        <?php if ($giftCards): ?>
            <div class="mt-6 space-y-4">
                <?php foreach ($giftCards as $card): ?>
                    <?php
                    $status = strtolower((string) ($card['status'] ?? ''));
                    $badgeClass = 'bg-primary/10 text-primary';
                    if ($status === 'redeemed') {
                        $badgeClass = 'bg-emerald-100 text-emerald-700';
                    } elseif ($status === 'expired') {
                        $badgeClass = 'bg-rose-100 text-rose-700';
                    } elseif ($status !== 'active') {
                        $badgeClass = 'bg-slate-100 text-slate-700';
                    }
                    ?>
                    <article class="rounded-3xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                        <div class="flex flex-wrap items-start justify-between gap-3">
                            <div>
                                <div class="inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-semibold uppercase tracking-[0.15em] <?= $badgeClass ?>">
                                    <?= htmlspecialchars((string) ($card['status'] ?? 'unknown'), ENT_QUOTES) ?>
                                </div>
                                <h3 class="mt-3 text-lg font-bold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars((string) ($card['card_number'] ?? ''), ENT_QUOTES) ?></h3>
                                <p class="mt-1 text-sm text-text-secondary-light dark:text-text-secondary-dark">
                                    <?= __('Face value: %1', $this->formatPrice((float) ($card['amount'] ?? 0))) ?>
                                </p>
                                <?php if ((string) ($card['expires_at'] ?? '') !== ''): ?>
                                    <p class="mt-1 text-sm text-text-secondary-light dark:text-text-secondary-dark">
                                        <?= __('Expires: %1', htmlspecialchars((string) ($card['expires_at'] ?? ''), ENT_QUOTES)) ?>
                                    </p>
                                <?php endif; ?>
                            </div>

                            <div class="rounded-2xl border border-border-light bg-surface-light px-4 py-3 text-right dark:border-border-dark dark:bg-surface-dark">
                                <p class="text-xs font-semibold uppercase tracking-[0.15em] text-text-secondary-light dark:text-text-secondary-dark"><?= __('Balance') ?></p>
                                <p class="mt-2 text-2xl font-black text-text-primary-light dark:text-text-primary-dark"><?= $this->formatPrice((float) ($card['balance'] ?? 0)) ?></p>
                            </div>
                        </div>
                        <w:hook name="WeShop_GiftCard::frontend::partials::gift-card-list::item-after"/>
                    </article>
                <?php endforeach; ?>
            </div>
        <?php else: ?>
            <div class="mt-6 rounded-3xl border border-dashed border-border-light bg-background-light px-6 py-10 text-center dark:border-border-dark dark:bg-background-dark">
                <span class="material-symbols-outlined text-5xl text-primary">cards</span>
                <h3 class="mt-4 text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('No gift cards yet') ?></h3>
                <p class="mx-auto mt-2 max-w-xl text-sm leading-6 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('When a gift card is issued to this account, it will appear here with balance and status details.') ?>
                </p>
            </div>
        <?php endif; ?>
    </section>
</main>
