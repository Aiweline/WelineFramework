<?php
/**
 * WeShop Default Theme - Customer Account Center
 */
$customer = (array) ($this->getData('customer') ?? []);
$recentOrders = (array) ($this->getData('recent_orders') ?? []);
$orderCount = (int) ($this->getData('order_count') ?? 0);
$unpaidCount = (int) ($this->getData('unpaid_count') ?? 0);
$quickLinks = (array) ($this->getData('quick_links') ?? []);
$wishlistCount = (int) ($this->getData('wishlist_count') ?? 0);
$wishlistPreview = (array) ($this->getData('wishlist_preview') ?? []);
$recentlyViewedCount = (int) ($this->getData('recently_viewed_count') ?? 0);
$recentlyViewed = (array) ($this->getData('recently_viewed') ?? []);
$recommendations = (array) ($this->getData('recommendations') ?? []);

$fullName = trim(((string) ($customer['firstname'] ?? '')) . ' ' . ((string) ($customer['lastname'] ?? '')));
if ($fullName === '') {
    $fullName = (string) ($customer['username'] ?? $customer['email'] ?? __('Customer'));
}

$memberSince = trim((string) ($customer['created_at'] ?? ''));
?>
<main class="mx-auto flex w-full max-w-7xl flex-1 flex-col gap-8 px-4 py-8 sm:px-6 lg:px-8">
    <section class="overflow-hidden rounded-[2rem] border border-border-light bg-surface-light shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="grid gap-0 lg:grid-cols-[1.35fr_0.65fr]">
            <div class="bg-gradient-to-br from-primary/10 via-primary/5 to-transparent p-8 sm:p-10">
                <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary"><?= __('My Account') ?></p>
                <h1 class="mt-4 text-3xl font-black tracking-tight text-text-primary-light dark:text-text-primary-dark sm:text-4xl"><?= htmlspecialchars($fullName, ENT_QUOTES) ?></h1>
                <p class="mt-3 max-w-2xl text-base leading-7 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('Manage your profile, review recent orders, and keep your sign-in methods ready for a faster checkout experience across the storefront.') ?>
                </p>

                <div class="mt-8 flex flex-wrap gap-3 text-sm">
                    <a href="<?= $this->getUrl('weshop/order/list') ?>" class="inline-flex items-center justify-center rounded-2xl bg-primary px-5 py-3 font-semibold text-white transition hover:bg-primary/90">
                        <?= __('View Orders') ?>
                    </a>
                    <a href="<?= $this->getUrl('weshop/customer/account/logout') ?>" class="inline-flex items-center justify-center rounded-2xl border border-border-light px-5 py-3 font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark">
                        <?= __('Sign Out') ?>
                    </a>
                </div>
            </div>

            <div class="grid gap-4 p-6 sm:grid-cols-2 lg:grid-cols-1 lg:border-l lg:border-border-light dark:lg:border-border-dark">
                <div class="rounded-3xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                    <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Email') ?></p>
                    <p class="mt-2 break-all text-lg font-bold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars((string) ($customer['email'] ?? __('Unknown')), ENT_QUOTES) ?></p>
                </div>
                <div class="rounded-3xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                    <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Member Since') ?></p>
                    <p class="mt-2 text-lg font-bold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars($memberSince !== '' ? $memberSince : (string) __('Just now'), ENT_QUOTES) ?></p>
                </div>
            </div>
        </div>
    </section>

    <section class="grid gap-4 sm:grid-cols-2 xl:grid-cols-6">
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Total Orders') ?></p>
            <p class="mt-3 text-3xl font-black text-text-primary-light dark:text-text-primary-dark"><?= $orderCount ?></p>
            <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('All orders placed with this customer account.') ?></p>
        </article>
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Awaiting Payment') ?></p>
            <p class="mt-3 text-3xl font-black text-text-primary-light dark:text-text-primary-dark"><?= $unpaidCount ?></p>
            <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Open orders that still need payment confirmation.') ?></p>
        </article>
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Recent Orders') ?></p>
            <p class="mt-3 text-3xl font-black text-text-primary-light dark:text-text-primary-dark"><?= count($recentOrders) ?></p>
            <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Latest orders shown in your dashboard summary.') ?></p>
        </article>
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Saved Shortcuts') ?></p>
            <p class="mt-3 text-3xl font-black text-text-primary-light dark:text-text-primary-dark"><?= count($quickLinks) ?></p>
            <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Quick navigation into the most-used customer workflows.') ?></p>
        </article>
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Wishlist Items') ?></p>
            <p class="mt-3 text-3xl font-black text-text-primary-light dark:text-text-primary-dark"><?= $wishlistCount ?></p>
            <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Saved products ready for your next checkout session.') ?></p>
        </article>
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Recently Viewed') ?></p>
            <p class="mt-3 text-3xl font-black text-text-primary-light dark:text-text-primary-dark"><?= $recentlyViewedCount ?></p>
            <p class="mt-2 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Products you inspected most recently across the storefront.') ?></p>
        </article>
    </section>

    <section id="account-security" class="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]">
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <div class="flex items-center justify-between gap-4">
                <div>
                    <p class="text-sm font-semibold uppercase tracking-[0.2em] text-primary"><?= __('Quick Actions') ?></p>
                    <h2 class="mt-2 text-2xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Jump Back In') ?></h2>
                </div>
                <span class="material-symbols-outlined text-4xl text-primary">dashboard</span>
            </div>

            <div class="mt-6 grid gap-3 sm:grid-cols-2">
                <?php foreach ($quickLinks as $link): ?>
                    <a href="<?= $this->getUrl((string) ($link['url'] ?? '')) ?>" class="group rounded-2xl border border-border-light bg-background-light p-4 transition hover:border-primary hover:shadow-sm dark:border-border-dark dark:bg-background-dark">
                        <div class="flex items-center gap-3">
                            <div class="flex h-11 w-11 items-center justify-center rounded-2xl bg-primary/10 text-primary">
                                <span class="material-symbols-outlined text-2xl"><?= htmlspecialchars((string) ($link['icon'] ?? 'arrow_forward'), ENT_QUOTES) ?></span>
                            </div>
                            <div>
                                <p class="font-semibold text-text-primary-light group-hover:text-primary dark:text-text-primary-dark"><?= htmlspecialchars((string) ($link['title'] ?? __('Open')), ENT_QUOTES) ?></p>
                                <p class="text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Open this customer workspace.') ?></p>
                            </div>
                        </div>
                    </a>
                <?php endforeach; ?>
            </div>
        </article>

        <div class="grid gap-6 lg:grid-cols-2">
            <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
                <div class="flex items-start justify-between gap-4">
                    <div>
                        <p class="text-sm font-semibold uppercase tracking-[0.2em] text-primary"><?= __('Password Access') ?></p>
                        <h2 class="mt-2 text-2xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Primary Sign-In') ?></h2>
                    </div>
                    <span class="material-symbols-outlined text-4xl text-primary">lock</span>
                </div>

                <p class="mt-4 text-sm leading-6 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('Your email and password remain the canonical storefront login. If you ever lose access, the password recovery flow can send a reset link to your registered email address.') ?>
                </p>

                <div class="mt-6 flex flex-wrap gap-3">
                    <a href="<?= $this->getUrl('weshop/customer/account/forgot-password') ?>" class="inline-flex items-center justify-center rounded-2xl border border-border-light px-4 py-3 text-sm font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark">
                        <?= __('Reset Password') ?>
                    </a>
                    <a href="<?= $this->getUrl('weshop/address') ?>" class="inline-flex items-center justify-center rounded-2xl bg-primary px-4 py-3 text-sm font-semibold text-white transition hover:bg-primary/90">
                        <?= __('Manage Addresses') ?>
                    </a>
                </div>
            </article>

            <w:hook name="WeShop_Customer::frontend::account::security::cards"/>
        </div>
    </section>

    <section class="grid gap-6 xl:grid-cols-3">
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <div class="flex items-center justify-between gap-4">
                <div>
                    <p class="text-sm font-semibold uppercase tracking-[0.2em] text-primary"><?= __('Saved For Later') ?></p>
                    <h2 class="mt-2 text-2xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Wishlist') ?></h2>
                </div>
                <span class="material-symbols-outlined text-4xl text-primary">favorite</span>
            </div>

            <?php if ($wishlistPreview): ?>
                <div class="mt-6 space-y-4">
                    <?php foreach ($wishlistPreview as $product): ?>
                        <a href="<?= $this->getUrl('product/view', ['id' => $product['product_id']]) ?>" class="group flex items-center gap-4 rounded-2xl border border-border-light bg-background-light p-3 transition hover:border-primary dark:border-border-dark dark:bg-background-dark">
                            <div class="h-16 w-16 overflow-hidden rounded-2xl bg-surface-light dark:bg-surface-dark">
                                <img src="<?= $product['image'] ?? '' ?>" alt="<?= htmlspecialchars((string) ($product['name'] ?? ''), ENT_QUOTES) ?>" class="h-full w-full object-cover">
                            </div>
                            <div class="min-w-0 flex-1">
                                <p class="truncate font-semibold text-text-primary-light group-hover:text-primary dark:text-text-primary-dark"><?= htmlspecialchars((string) ($product['name'] ?? ''), ENT_QUOTES) ?></p>
                                <p class="mt-1 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= $this->formatPrice($product['price'] ?? 0) ?></p>
                            </div>
                        </a>
                    <?php endforeach; ?>
                </div>
            <?php else: ?>
                <div class="mt-6 rounded-3xl border border-dashed border-border-light bg-background-light px-5 py-8 text-sm leading-6 text-text-secondary-light dark:border-border-dark dark:bg-background-dark dark:text-text-secondary-dark">
                    <?= __('Save products to your wishlist so they are easy to revisit from your account center.') ?>
                </div>
            <?php endif; ?>
        </article>

        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <div class="flex items-center justify-between gap-4">
                <div>
                    <p class="text-sm font-semibold uppercase tracking-[0.2em] text-primary"><?= __('Keep Exploring') ?></p>
                    <h2 class="mt-2 text-2xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Recently Viewed') ?></h2>
                </div>
                <span class="material-symbols-outlined text-4xl text-primary">history</span>
            </div>

            <?php if ($recentlyViewed): ?>
                <div class="mt-6 space-y-4">
                    <?php foreach ($recentlyViewed as $product): ?>
                        <a href="<?= $this->getUrl('product/view', ['id' => $product['product_id']]) ?>" class="group flex items-center gap-4 rounded-2xl border border-border-light bg-background-light p-3 transition hover:border-primary dark:border-border-dark dark:bg-background-dark">
                            <div class="h-16 w-16 overflow-hidden rounded-2xl bg-surface-light dark:bg-surface-dark">
                                <img src="<?= $product['image'] ?? '' ?>" alt="<?= htmlspecialchars((string) ($product['name'] ?? ''), ENT_QUOTES) ?>" class="h-full w-full object-cover">
                            </div>
                            <div class="min-w-0 flex-1">
                                <p class="truncate font-semibold text-text-primary-light group-hover:text-primary dark:text-text-primary-dark"><?= htmlspecialchars((string) ($product['name'] ?? ''), ENT_QUOTES) ?></p>
                                <p class="mt-1 text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= $this->formatPrice($product['price'] ?? 0) ?></p>
                            </div>
                        </a>
                    <?php endforeach; ?>
                </div>
            <?php else: ?>
                <div class="mt-6 rounded-3xl border border-dashed border-border-light bg-background-light px-5 py-8 text-sm leading-6 text-text-secondary-light dark:border-border-dark dark:bg-background-dark dark:text-text-secondary-dark">
                    <?= __('Browse more products and your recently viewed picks will appear here for quick return visits.') ?>
                </div>
            <?php endif; ?>
        </article>

        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <div class="flex items-center justify-between gap-4">
                <div>
                    <p class="text-sm font-semibold uppercase tracking-[0.2em] text-primary"><?= __('Guess You May Like') ?></p>
                    <h2 class="mt-2 text-2xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Recommended For You') ?></h2>
                </div>
                <span class="material-symbols-outlined text-4xl text-primary">auto_awesome</span>
            </div>

            <?php if ($recommendations): ?>
                <div class="mt-6 grid grid-cols-2 gap-3">
                    <?php foreach ($recommendations as $product): ?>
                        <a href="<?= $this->getUrl('product/view', ['id' => $product['product_id']]) ?>" class="group rounded-2xl border border-border-light bg-background-light p-3 transition hover:border-primary dark:border-border-dark dark:bg-background-dark">
                            <div class="aspect-square overflow-hidden rounded-2xl bg-surface-light dark:bg-surface-dark">
                                <img src="<?= $product['image'] ?? '' ?>" alt="<?= htmlspecialchars((string) ($product['name'] ?? ''), ENT_QUOTES) ?>" class="h-full w-full object-cover">
                            </div>
                            <p class="mt-3 line-clamp-2 text-sm font-semibold text-text-primary-light group-hover:text-primary dark:text-text-primary-dark"><?= htmlspecialchars((string) ($product['name'] ?? ''), ENT_QUOTES) ?></p>
                            <p class="mt-1 text-sm font-bold text-primary"><?= $this->formatPrice($product['price'] ?? 0) ?></p>
                        </a>
                    <?php endforeach; ?>
                </div>
            <?php else: ?>
                <div class="mt-6 rounded-3xl border border-dashed border-border-light bg-background-light px-5 py-8 text-sm leading-6 text-text-secondary-light dark:border-border-dark dark:bg-background-dark dark:text-text-secondary-dark">
                    <?= __('As you save and browse products, personalized recommendations will show up here.') ?>
                </div>
            <?php endif; ?>
        </article>

        <w:hook name="WeShop_Customer::frontend::account::discovery::cards"/>
    </section>

    <section class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
            <div>
                <p class="text-sm font-semibold uppercase tracking-[0.2em] text-primary"><?= __('Order Snapshot') ?></p>
                <h2 class="text-2xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Recent Orders') ?></h2>
            </div>
            <a href="<?= $this->getUrl('weshop/order/list') ?>" class="text-sm font-semibold text-primary hover:underline"><?= __('View All Orders') ?></a>
        </div>

        <?php if ($recentOrders): ?>
            <div class="mt-6 overflow-hidden rounded-3xl border border-border-light dark:border-border-dark">
                <div class="grid grid-cols-[1.1fr_0.8fr_0.8fr_1fr] gap-4 bg-background-light px-5 py-4 text-sm font-semibold text-text-secondary-light dark:bg-background-dark dark:text-text-secondary-dark">
                    <div><?= __('Order') ?></div>
                    <div><?= __('Status') ?></div>
                    <div><?= __('Total') ?></div>
                    <div><?= __('Created At') ?></div>
                </div>
                <?php foreach ($recentOrders as $order): ?>
                    <div class="grid grid-cols-[1.1fr_0.8fr_0.8fr_1fr] gap-4 border-t border-border-light px-5 py-4 text-sm text-text-primary-light dark:border-border-dark dark:text-text-primary-dark">
                        <div class="font-semibold">#<?= htmlspecialchars((string) ($order['increment_id'] ?? $order['order_id'] ?? ''), ENT_QUOTES) ?></div>
                        <div><?= htmlspecialchars((string) ($order['status'] ?? __('Unknown')), ENT_QUOTES) ?></div>
                        <div><?= htmlspecialchars((string) ($order['total'] ?? '0.00'), ENT_QUOTES) ?></div>
                        <div><?= htmlspecialchars((string) ($order['created_at'] ?? ''), ENT_QUOTES) ?></div>
                    </div>
                <?php endforeach; ?>
            </div>
        <?php else: ?>
            <div class="mt-6 rounded-3xl border border-dashed border-border-light bg-background-light px-6 py-10 text-center dark:border-border-dark dark:bg-background-dark">
                <span class="material-symbols-outlined text-5xl text-primary">package_2</span>
                <h3 class="mt-4 text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('No orders yet') ?></h3>
                <p class="mx-auto mt-2 max-w-xl text-sm leading-6 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('Once this customer places an order, the latest activity will appear here for quick follow-up and repeat purchases.') ?>
                </p>
                <a href="<?= $this->getUrl('catalog/category') ?>" class="mt-6 inline-flex items-center justify-center rounded-2xl bg-primary px-5 py-3 text-sm font-semibold text-white transition hover:bg-primary/90">
                    <?= __('Start Shopping') ?>
                </a>
            </div>
        <?php endif; ?>
    </section>
</main>
