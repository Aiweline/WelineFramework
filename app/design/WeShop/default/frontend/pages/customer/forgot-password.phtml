<?php
$isResetMode = (bool) ($this->getData('is_reset_mode') ?? false);
$resetToken = (string) ($this->getData('reset_token') ?? '');
?>
<main class="flex-1 flex items-center justify-center py-12 px-4">
    <div class="w-full max-w-md">
        <div class="bg-surface-light dark:bg-surface-dark rounded-xl shadow-lg border border-border-light dark:border-border-dark p-8">
            <div class="text-center mb-8">
                <div class="flex justify-center mb-4">
                    <div class="p-3 bg-primary/10 rounded-full">
                        <span class="material-symbols-outlined text-4xl text-primary"><?= $isResetMode ? 'password' : 'mail' ?></span>
                    </div>
                </div>
                <h1 class="text-2xl font-bold text-text-primary-light dark:text-text-primary-dark">
                    <?= $isResetMode ? __('Reset Password') : __('Forgot Password') ?>
                </h1>
                <p class="text-text-secondary-light dark:text-text-secondary-dark mt-2">
                    <?= $isResetMode ? __('Enter your new password below.') : __('Enter your email and we will send you a reset link.') ?>
                </p>
            </div>

            <?php if ($isResetMode): ?>
                <form action="<?= $this->getUrl('weshop/customer/account/forgot-password/reset-password') ?>" method="post" class="space-y-5">
                    <?= $this->getFormKey() ?>
                    <input type="hidden" name="token" value="<?= htmlspecialchars($resetToken, ENT_QUOTES) ?>">

                    <div>
                        <label for="password" class="block text-sm font-medium text-text-primary-light dark:text-text-primary-dark mb-2"><?= __('New Password') ?></label>
                        <input type="password" id="password" name="password" required minlength="8"
                               class="form-input w-full rounded-lg border-border-light dark:border-border-dark bg-background-light dark:bg-background-dark h-12 px-4 focus:border-primary focus:ring-primary"
                               placeholder="<?= __('At least 8 characters') ?>">
                    </div>

                    <div>
                        <label for="password_confirm" class="block text-sm font-medium text-text-primary-light dark:text-text-primary-dark mb-2"><?= __('Confirm Password') ?></label>
                        <input type="password" id="password_confirm" name="password_confirm" required
                               class="form-input w-full rounded-lg border-border-light dark:border-border-dark bg-background-light dark:bg-background-dark h-12 px-4 focus:border-primary focus:ring-primary"
                               placeholder="<?= __('Re-enter your password') ?>">
                    </div>

                    <button type="submit" class="w-full bg-primary hover:bg-primary/90 text-white font-bold py-3 rounded-lg transition-colors">
                        <?= __('Reset Password') ?>
                    </button>
                </form>
            <?php else: ?>
                <form action="<?= $this->getUrl('weshop/customer/account/forgot-password') ?>" method="post" class="space-y-5">
                    <?= $this->getFormKey() ?>

                    <div>
                        <label for="email" class="block text-sm font-medium text-text-primary-light dark:text-text-primary-dark mb-2"><?= __('Email Address') ?></label>
                        <input type="email" id="email" name="email" required
                               class="form-input w-full rounded-lg border-border-light dark:border-border-dark bg-background-light dark:bg-background-dark h-12 px-4 focus:border-primary focus:ring-primary"
                               placeholder="<?= __('you@example.com') ?>">
                    </div>

                    <button type="submit" class="w-full bg-primary hover:bg-primary/90 text-white font-bold py-3 rounded-lg transition-colors">
                        <?= __('Send Reset Link') ?>
                    </button>
                </form>
            <?php endif; ?>

            <p class="text-center mt-8 text-text-secondary-light dark:text-text-secondary-dark">
                <a href="<?= $this->getUrl('weshop/customer/account/login') ?>" class="text-primary hover:underline font-medium"><?= __('Back to sign in') ?></a>
            </p>
        </div>
    </div>
</main>
