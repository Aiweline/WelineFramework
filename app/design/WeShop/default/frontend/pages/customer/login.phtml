<?php
/**
 * WeShop Default Theme - Customer Login Page
 */
$captchaHtml = $this->getData('captcha_html') ?? '';
$redirectUrl = (string) ($this->getData('redirect_url') ?? '');
?>
<main class="flex-1 flex items-center justify-center py-12 px-4">
    <div class="w-full max-w-md">
        <w:hook name="WeShop_Customer::login::before"/>

        <div class="bg-surface-light dark:bg-surface-dark rounded-xl shadow-lg border border-border-light dark:border-border-dark p-8">
            <div class="text-center mb-8">
                <div class="flex justify-center mb-4">
                    <div class="p-3 bg-primary/10 rounded-full">
                        <span class="material-symbols-outlined text-4xl text-primary">person</span>
                    </div>
                </div>
                <h1 class="text-2xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Welcome Back') ?></h1>
                <p class="text-text-secondary-light dark:text-text-secondary-dark mt-2"><?= __('Sign in to your account') ?></p>
            </div>

            <form action="<?= $this->getUrl('weshop/customer/account/login') ?>" method="post" class="space-y-6">
                <?= $this->getFormKey() ?>
                <input type="hidden" name="redirect_url" value="<?= htmlspecialchars($redirectUrl, ENT_QUOTES) ?>">

                <div>
                    <label for="email" class="block text-sm font-medium text-text-primary-light dark:text-text-primary-dark mb-2"><?= __('Email Address') ?></label>
                    <div class="relative">
                        <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-text-secondary-light dark:text-text-secondary-dark">mail</span>
                        <input type="email" id="email" name="email" required
                               class="form-input w-full rounded-lg border-border-light dark:border-border-dark bg-background-light dark:bg-background-dark h-12 pl-10 pr-4 focus:border-primary focus:ring-primary"
                               placeholder="<?= __('you@example.com') ?>">
                    </div>
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium text-text-primary-light dark:text-text-primary-dark mb-2"><?= __('Password') ?></label>
                    <div class="relative">
                        <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-text-secondary-light dark:text-text-secondary-dark">lock</span>
                        <input type="password" id="password" name="password" required
                               class="form-input w-full rounded-lg border-border-light dark:border-border-dark bg-background-light dark:bg-background-dark h-12 pl-10 pr-12 focus:border-primary focus:ring-primary"
                               placeholder="<?= __('Enter your password') ?>">
                        <button type="button" class="toggle-password absolute right-3 top-1/2 -translate-y-1/2 text-text-secondary-light dark:text-text-secondary-dark hover:text-primary">
                            <span class="material-symbols-outlined">visibility</span>
                        </button>
                    </div>
                </div>

                <div class="flex items-center justify-between">
                    <label class="flex items-center gap-2">
                        <input type="checkbox" name="remember_me" class="form-checkbox rounded text-primary focus:ring-primary">
                        <span class="text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Remember me') ?></span>
                    </label>
                    <a href="<?= $this->getUrl('weshop/customer/account/forgot-password') ?>" class="text-sm text-primary hover:underline"><?= __('Forgot password?') ?></a>
                </div>

                <?php if ($captchaHtml): ?>
                    <div class="captcha-container">
                        <?= $captchaHtml ?>
                    </div>
                <?php endif; ?>

                <button type="submit" class="w-full bg-primary hover:bg-primary/90 text-white font-bold py-3 rounded-lg transition-colors flex items-center justify-center gap-2">
                    <span class="material-symbols-outlined">login</span>
                    <?= __('Sign In') ?>
                </button>
            </form>

            <div class="relative my-8">
                <div class="absolute inset-0 flex items-center">
                    <div class="w-full border-t border-border-light dark:border-border-dark"></div>
                </div>
                <div class="relative flex justify-center text-sm">
                    <span class="px-4 bg-surface-light dark:bg-surface-dark text-text-secondary-light dark:text-text-secondary-dark"><?= __('Or continue with') ?></span>
                </div>
            </div>

            <w:hook name="WeShop_Social::frontend::partials::login::buttons"/>

            <p class="text-center mt-8 text-text-secondary-light dark:text-text-secondary-dark">
                <?= __("Don't have an account?") ?>
                <a href="<?= $this->getUrl('weshop/customer/account/register') ?>" class="text-primary hover:underline font-medium"><?= __('Sign up') ?></a>
            </p>
        </div>

        <w:hook name="WeShop_Customer::login::after"/>
    </div>
</main>

<script>
document.querySelectorAll('.toggle-password').forEach(function(btn) {
    btn.addEventListener('click', function() {
        var input = this.parentElement.querySelector('input');
        var icon = this.querySelector('.material-symbols-outlined');
        if (input.type === 'password') {
            input.type = 'text';
            icon.textContent = 'visibility_off';
        } else {
            input.type = 'password';
            icon.textContent = 'visibility';
        }
    });
});
</script>
