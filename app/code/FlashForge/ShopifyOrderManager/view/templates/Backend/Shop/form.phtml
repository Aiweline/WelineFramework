<?php
/**
 * Shopify 店铺表单 - 内容模板
 * @var \Weline\Framework\View\Template $this
 */
?>
<style>
/* Shopify店铺表单样式 */
.shopify-shop-form .modern-form-card {
    background: var(--backend-color-card-bg, rgba(255,255,255,0.95));
    border: none;
    border-radius: 20px;
    box-shadow: 0 20px 40px var(--backend-color-shadow, rgba(0,0,0,0.1));
    backdrop-filter: blur(10px);
    overflow: hidden;
}

.shopify-shop-form .form-header {
    background: linear-gradient(135deg, var(--backend-color-primary, #667eea) 0%, var(--backend-color-primary-dark, #764ba2) 100%);
    color: var(--backend-color-text-light, white);
    padding: 25px 30px;
    margin: -20px -20px 30px -20px;
}

.shopify-shop-form .form-header h4 {
    margin: 0;
    font-weight: 600;
    font-size: 24px;
}

.shopify-shop-form .form-group {
    margin-bottom: 25px;
}

.shopify-shop-form .form-label {
    font-weight: 600;
    color: var(--backend-color-text-secondary, #495057);
    margin-bottom: 8px;
    display: flex;
    align-items: center;
}

.shopify-shop-form .form-label i {
    margin-right: 8px;
    color: var(--backend-color-primary, #667eea);
}

.shopify-shop-form .form-control {
    border: 2px solid var(--backend-color-border, #e9ecef);
    border-radius: 12px;
    padding: 12px 16px;
    font-size: 14px;
    transition: all 0.3s ease;
    background: var(--backend-color-input-bg, rgba(255,255,255,0.9));
    color: var(--backend-color-text-primary, #333);
}

.shopify-shop-form .form-control:focus {
    border-color: var(--backend-color-primary, #667eea);
    box-shadow: 0 0 0 0.2rem var(--backend-color-primary-alpha, rgba(102, 126, 234, 0.25));
    background: var(--backend-color-input-bg-focus, white);
}

.shopify-shop-form .form-control::placeholder {
    color: var(--backend-color-text-muted, #adb5bd);
    font-style: italic;
}

.shopify-shop-form .form-check-input {
    width: 20px;
    height: 20px;
    border-radius: 6px;
    border: 2px solid var(--backend-color-primary, #667eea);
}

.shopify-shop-form .form-check-input:checked {
    background-color: var(--backend-color-primary, #667eea);
    border-color: var(--backend-color-primary, #667eea);
}

.shopify-shop-form .form-check-label {
    font-weight: 500;
    color: var(--backend-color-text-secondary, #495057);
    margin-left: 8px;
}

.shopify-shop-form .btn-submit {
    background: linear-gradient(135deg, var(--backend-color-primary, #667eea) 0%, var(--backend-color-primary-dark, #764ba2) 100%);
    border: none;
    border-radius: 12px;
    padding: 12px 30px;
    color: var(--backend-color-text-light, white);
    font-weight: 600;
    font-size: 16px;
    transition: all 0.3s ease;
    box-shadow: 0 5px 15px var(--backend-color-primary-alpha, rgba(102, 126, 234, 0.3));
}

.shopify-shop-form .btn-submit:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px var(--backend-color-primary-alpha, rgba(102, 126, 234, 0.4));
    color: var(--backend-color-text-light, white);
}

.shopify-shop-form .required-star {
    color: #dc3545;
    margin-left: 4px;
}

.shopify-shop-form .form-section {
    background: var(--backend-color-bg-secondary, rgba(255,255,255,0.5));
    border-radius: 15px;
    padding: 20px;
    margin-bottom: 20px;
    border: 1px solid var(--backend-color-border, rgba(255,255,255,0.3));
}

.shopify-shop-form .section-title {
    color: var(--backend-color-primary, #667eea);
    font-weight: 600;
    font-size: 18px;
    margin-bottom: 15px;
    display: flex;
    align-items: center;
}

.shopify-shop-form .section-title i {
    margin-right: 10px;
}

.shopify-shop-form .loading-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.5);
    display: none;
    justify-content: center;
    align-items: center;
    z-index: 9999;
}

.shopify-shop-form .loading-spinner {
    background: var(--backend-color-card-bg, white);
    padding: 30px;
    border-radius: 15px;
    text-align: center;
    box-shadow: 0 10px 30px var(--backend-color-shadow, rgba(0,0,0,0.3));
}

.shopify-shop-form .spinner-border {
    color: var(--backend-color-primary, #667eea);
}
</style>

<!-- 页面标题 -->
<div class="row mb-4">
    <div class="col-12">
        <h4 class="mb-0"><?= $this->getData('shop.shop_id') ? __('编辑店铺') : __('添加新店铺') ?></h4>
    </div>
</div>

<div class='row'>
    <template>Weline_Component::message.phtml</template>
</div>

<div class="shopify-shop-form">
    <div class="row">
        <div class="col-12">
            <form id="shopForm" method="post" action="<?= $this->getBackendUrl('shopify/backend/shop/save') ?>">
                <div class="card modern-form-card">
                    <div class="form-header">
                        <h4><i class="mdi mdi-store me-2"></i><?= $this->getData('shop.shop_id') ? __('编辑店铺') : __('添加新店铺') ?></h4>
                    </div>
                    <div class="card-body">
                        <div class="form-section">
                            <div class="section-title">
                                <i class="mdi mdi-information"></i><lang>基本信息</lang>
                            </div>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="shop_name" class="form-label">
                                            <i class="mdi mdi-store"></i><lang>店铺名称</lang><span class="required-star">*</span>
                                        </label>
                                        <input type="text" class="form-control" id="shop_name" name="shop_name" required 
                                               placeholder="<?= __('请输入店铺名称') ?>" value="<?= htmlspecialchars($this->getData('shop.shop_name') ?? '') ?>">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="shop_url" class="form-label">
                                            <i class="mdi mdi-link"></i><lang>店铺URL</lang><span class="required-star">*</span>
                                        </label>
                                        <input type="url" class="form-control" id="shop_url" name="shop_url" required 
                                               placeholder="https://your-shop.myshopify.com" value="<?= htmlspecialchars($this->getData('shop.shop_url') ?? '') ?>">
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="form-section">
                            <div class="section-title">
                                <i class="mdi mdi-key"></i><lang>API配置</lang>
                            </div>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="api_key" class="form-label">
                                            <i class="mdi mdi-key"></i>API Key<span class="required-star">*</span>
                                        </label>
                                        <input type="text" class="form-control" id="api_key" name="api_key" required 
                                               placeholder="<?= __('请输入API Key') ?>" value="<?= htmlspecialchars($this->getData('shop.api_key') ?? '') ?>">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="api_secret" class="form-label">
                                            <i class="mdi mdi-key-variant"></i>API Secret<span class="required-star">*</span>
                                        </label>
                                        <input type="password" class="form-control" id="api_secret" name="api_secret" required 
                                               placeholder="<?= __('请输入API Secret') ?>" value="<?= htmlspecialchars($this->getData('shop.api_secret') ?? '') ?>">
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="form-section">
                            <div class="section-title">
                                <i class="mdi mdi-shield-key"></i><lang>访问令牌</lang>
                            </div>
                            <div class="row">
                                <div class="col-12">
                                    <div class="form-group">
                                        <label for="access_token" class="form-label">
                                            <i class="mdi mdi-shield-key"></i>Access Token<span class="required-star">*</span>
                                        </label>
                                        <textarea class="form-control" id="access_token" name="access_token" rows="3" required 
                                                  placeholder="<?= __('请输入Access Token') ?>"><?= htmlspecialchars($this->getData('shop.access_token') ?? '') ?></textarea>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="form-section">
                            <div class="section-title">
                                <i class="mdi mdi-toggle-switch"></i><lang>状态设置</lang>
                            </div>
                            <div class="row">
                                <div class="col-12">
                                    <div class="form-group">
                                        <div class="form-check">
                                            <input type="checkbox" class="form-check-input" id="status" name="status" value="1" 
                                                   <?= $this->getData('shop.status') ? 'checked' : '' ?>>
                                            <label class="form-check-label" for="status">
                                                <i class="mdi mdi-toggle-switch me-1"></i><lang>启用店铺</lang>
                                            </label>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <?php if ($this->getData('shop.id')): ?>
                        <input type="hidden" name="shop_id" value="<?= htmlspecialchars($this->getData('shop.id')) ?>">
                        <?php endif; ?>
                        
                        <div class="text-center mt-4">
                            <button type="submit" class="btn btn-submit">
                                <i class="mdi mdi-content-save me-2"></i><lang>保存店铺</lang>
                            </button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- 加载动画 -->
    <div class="loading-overlay" id="loadingOverlay">
        <div class="loading-spinner">
            <div class="spinner-border" role="status">
                <span class="visually-hidden"><lang>保存中...</lang></span>
            </div>
            <p class="mt-3 mb-0"><lang>正在保存店铺信息...</lang></p>
        </div>
    </div>
</div>

<script>
(function() {
    'use strict';
    
    function init() {
        var form = document.getElementById('shopForm');
        var loadingOverlay = document.getElementById('loadingOverlay');
        
        form.addEventListener('submit', function(e) {
            e.preventDefault();
            loadingOverlay.style.display = 'flex';
            
            var formData = new FormData(form);
            
            fetch(form.action, { method: 'POST', body: formData })
                .then(function(response) { return response.json(); })
                .then(function(data) {
                    loadingOverlay.style.display = 'none';
                    if (data.code === 0) {
                        showMessage('success', data.msg);
                        setTimeout(function() {
                            if (window.parent && window.parent.closeOffCanvas) {
                                window.parent.closeOffCanvas();
                            }
                            if (window.parent && window.parent.location) {
                                window.parent.location.reload();
                            }
                        }, 1500);
                    } else {
                        showMessage('error', data.msg);
                    }
                })
                .catch(function(error) {
                    loadingOverlay.style.display = 'none';
                    showMessage('error', '保存失败，请重试');
                });
        });
        
        var inputs = form.querySelectorAll('input[required], textarea[required]');
        inputs.forEach(function(input) {
            input.addEventListener('blur', function() {
                if (!this.value.trim()) {
                    this.classList.add('is-invalid');
                } else {
                    this.classList.remove('is-invalid');
                }
            });
            
            input.addEventListener('input', function() {
                if (this.classList.contains('is-invalid') && this.value.trim()) {
                    this.classList.remove('is-invalid');
                }
            });
        });
    }
    
    function showMessage(type, message) {
        var alertDiv = document.createElement('div');
        alertDiv.className = 'alert alert-' + (type === 'success' ? 'success' : 'danger') + ' alert-dismissible fade show';
        alertDiv.style.position = 'fixed';
        alertDiv.style.top = '20px';
        alertDiv.style.right = '20px';
        alertDiv.style.zIndex = '10000';
        alertDiv.style.minWidth = '300px';
        alertDiv.innerHTML = '<i class="mdi mdi-' + (type === 'success' ? 'check-circle' : 'alert-circle') + ' me-2"></i>' + message + '<button type="button" class="btn-close" data-bs-dismiss="alert"></button>';
        
        document.body.appendChild(alertDiv);
        
        setTimeout(function() {
            if (alertDiv.parentNode) alertDiv.parentNode.removeChild(alertDiv);
        }, 5000);
    }
    
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }
})();
</script>
