<?php
/**@var \Weline\Framework\View\Template $this */
?>
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= $this->getData('title') ?></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/@mdi/font@6.9.96/css/materialdesignicons.min.css" rel="stylesheet">
    <style>
        body { 
            padding: 20px; 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .modern-form-card {
            background: rgba(255,255,255,0.95);
            border: none;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            backdrop-filter: blur(10px);
            overflow: hidden;
        }
        
        .form-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 25px 30px;
            margin: -20px -20px 30px -20px;
        }
        
        .form-header h4 {
            margin: 0;
            font-weight: 600;
            font-size: 24px;
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        .form-label {
            font-weight: 600;
            color: #495057;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
        }
        
        .form-label i {
            margin-right: 8px;
            color: #667eea;
        }
        
        .form-control {
            border: 2px solid #e9ecef;
            border-radius: 12px;
            padding: 12px 16px;
            font-size: 14px;
            transition: all 0.3s ease;
            background: rgba(255,255,255,0.9);
        }
        
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
            background: white;
        }
        
        .form-control::placeholder {
            color: #adb5bd;
            font-style: italic;
        }
        
        .form-check {
            margin-bottom: 20px;
        }
        
        .form-check-input {
            width: 20px;
            height: 20px;
            border-radius: 6px;
            border: 2px solid #667eea;
        }
        
        .form-check-input:checked {
            background-color: #667eea;
            border-color: #667eea;
        }
        
        .form-check-label {
            font-weight: 500;
            color: #495057;
            margin-left: 8px;
        }
        
        .btn-submit {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 12px;
            padding: 12px 30px;
            color: white;
            font-weight: 600;
            font-size: 16px;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
        }
        
        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
            color: white;
        }
        
        .btn-submit:active {
            transform: translateY(0);
        }
        
        .text-danger {
            color: #dc3545 !important;
            font-weight: 500;
        }
        
        .required-star {
            color: #dc3545;
            margin-left: 4px;
        }
        
        .form-section {
            background: rgba(255,255,255,0.5);
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
            border: 1px solid rgba(255,255,255,0.3);
        }
        
        .section-title {
            color: #667eea;
            font-weight: 600;
            font-size: 18px;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
        }
        
        .section-title i {
            margin-right: 10px;
        }
        
        .loading-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 9999;
        }
        
        .loading-spinner {
            background: white;
            padding: 30px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }
        
        .spinner-border {
            color: #667eea;
        }
        
        @media (max-width: 768px) {
            body {
                padding: 10px;
            }
            
            .modern-form-card {
                margin: 0;
                border-radius: 15px;
            }
            
            .form-header {
                padding: 20px;
                margin: -15px -15px 20px -15px;
            }
            
            .form-header h4 {
                font-size: 20px;
            }
        }
    </style>
</head>
<body>
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <form id="shopForm" method="post" action="<?= $this->getBackendUrl('shopify/backend/shop/save') ?>">
                <div class="card modern-form-card">
                    <div class="form-header">
                        <h4><i class="mdi mdi-store me-2"></i><?= $this->getData('shop.shop_id') ? '编辑店铺' : '添加新店铺' ?></h4>
                    </div>
                    <div class="card-body">
                        <div class="form-section">
                            <div class="section-title">
                                <i class="mdi mdi-information"></i>基本信息
                            </div>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="shop_name" class="form-label">
                                            <i class="mdi mdi-store"></i>店铺名称<span class="required-star">*</span>
                                        </label>
                                        <input type="text" class="form-control" id="shop_name" name="shop_name" required 
                                               placeholder="请输入店铺名称" value="<?= $this->getData('shop.shop_name') ?>">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="shop_url" class="form-label">
                                            <i class="mdi mdi-link"></i>店铺URL<span class="required-star">*</span>
                                        </label>
                                        <input type="url" class="form-control" id="shop_url" name="shop_url" required 
                                               placeholder="https://your-shop.myshopify.com" value="<?= $this->getData('shop.shop_url') ?>">
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="form-section">
                            <div class="section-title">
                                <i class="mdi mdi-key"></i>API配置
                            </div>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="api_key" class="form-label">
                                            <i class="mdi mdi-key"></i>API Key<span class="required-star">*</span>
                                        </label>
                                        <input type="text" class="form-control" id="api_key" name="api_key" required 
                                               placeholder="请输入API Key" value="<?= $this->getData('shop.api_key') ?>">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="api_secret" class="form-label">
                                            <i class="mdi mdi-key-variant"></i>API Secret<span class="required-star">*</span>
                                        </label>
                                        <input type="password" class="form-control" id="api_secret" name="api_secret" required 
                                               placeholder="请输入API Secret" value="<?= $this->getData('shop.api_secret') ?>">
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="form-section">
                            <div class="section-title">
                                <i class="mdi mdi-shield-key"></i>访问令牌
                            </div>
                            <div class="row">
                                <div class="col-12">
                                    <div class="form-group">
                                        <label for="access_token" class="form-label">
                                            <i class="mdi mdi-shield-key"></i>Access Token<span class="required-star">*</span>
                                        </label>
                                        <textarea class="form-control" id="access_token" name="access_token" rows="3" required 
                                                  placeholder="请输入Access Token"><?= $this->getData('shop.access_token') ?></textarea>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="form-section">
                            <div class="section-title">
                                <i class="mdi mdi-toggle-switch"></i>状态设置
                            </div>
                            <div class="row">
                                <div class="col-12">
                                    <div class="form-group">
                                        <div class="form-check">
                                            <input type="checkbox" class="form-check-input" id="status" name="status" value="1" 
                                                   <?= $this->getData('shop.status') ? 'checked' : '' ?>>
                                            <label class="form-check-label" for="status">
                                                <i class="mdi mdi-toggle-switch me-1"></i>启用店铺
                                            </label>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <?php if ($this->getData('shop.id')): ?>
                        <input type="hidden" name="shop_id" value="<?= $this->getData('shop.id') ?>">
                        <?php endif; ?>
                        
                        <div class="text-center mt-4">
                            <button type="submit" class="btn btn-submit">
                                <i class="mdi mdi-content-save me-2"></i>保存店铺
                            </button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- 加载动画 -->
<div class="loading-overlay" id="loadingOverlay">
    <div class="loading-spinner">
        <div class="spinner-border" role="status">
            <span class="visually-hidden">保存中...</span>
        </div>
        <p class="mt-3 mb-0">正在保存店铺信息...</p>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('shopForm');
    const loadingOverlay = document.getElementById('loadingOverlay');
    
    // 表单提交处理
    form.addEventListener('submit', function(e) {
        e.preventDefault();
        
        // 显示加载动画
        loadingOverlay.style.display = 'flex';
        
        // 收集表单数据
        const formData = new FormData(form);
        
        // 发送AJAX请求
        fetch(form.action, {
            method: 'POST',
            body: formData
        })
        .then(response => response.json())
        .then(data => {
            // 隐藏加载动画
            loadingOverlay.style.display = 'none';
            
            if (data.code === 0) {
                // 成功提示
                showMessage('success', data.msg);
                // 延迟关闭窗口
                setTimeout(() => {
                    if (window.parent && window.parent.closeOffCanvas) {
                        window.parent.closeOffCanvas();
                    }
                    if (window.parent && window.parent.location) {
                        window.parent.location.reload();
                    }
                }, 1500);
            } else {
                // 错误提示
                showMessage('error', data.msg);
            }
        })
        .catch(error => {
            // 隐藏加载动画
            loadingOverlay.style.display = 'none';
            console.error('Error:', error);
            showMessage('error', '保存失败，请重试');
        });
    });
    
    // 显示消息提示
    function showMessage(type, message) {
        // 创建提示元素
        const alertDiv = document.createElement('div');
        alertDiv.className = `alert alert-${type === 'success' ? 'success' : 'danger'} alert-dismissible fade show`;
        alertDiv.style.position = 'fixed';
        alertDiv.style.top = '20px';
        alertDiv.style.right = '20px';
        alertDiv.style.zIndex = '10000';
        alertDiv.style.minWidth = '300px';
        alertDiv.innerHTML = `
            <i class="mdi mdi-${type === 'success' ? 'check-circle' : 'alert-circle'} me-2"></i>
            ${message}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        `;
        
        document.body.appendChild(alertDiv);
        
        // 自动移除提示
        setTimeout(() => {
            if (alertDiv.parentNode) {
                alertDiv.parentNode.removeChild(alertDiv);
            }
        }, 5000);
    }
    
    // 表单验证增强
    const inputs = form.querySelectorAll('input[required], textarea[required]');
    inputs.forEach(input => {
        input.addEventListener('blur', function() {
            if (!this.value.trim()) {
                this.classList.add('is-invalid');
            } else {
                this.classList.remove('is-invalid');
            }
        });
        
        input.addEventListener('input', function() {
            if (this.classList.contains('is-invalid') && this.value.trim()) {
                this.classList.remove('is-invalid');
            }
        });
    });
});
</script>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
