<?php

$title = $this->getData('title') ?? '';
$description = $this->getData('description') ?? '';
$monitoringHint = $this->getData('monitoring_hint') ?? '';

?>
<div class="container-fluid py-3">
    <h1 class="h4 mb-2"><?= htmlspecialchars((string) $title) ?></h1>
    <p class="mb-0 text-muted"><?= htmlspecialchars((string) $description) ?></p>
    <?php if ($monitoringHint !== ''): ?>
        <p class="mb-0 mt-2 text-muted" data-testid="cursor-base-monitoring-hint"><?= htmlspecialchars((string) $monitoringHint) ?></p>
    <?php endif; ?>
</div>

