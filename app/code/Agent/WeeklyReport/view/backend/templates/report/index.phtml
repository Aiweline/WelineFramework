<?php

$title = $this->getData('title') ?? '';
$heading = $title !== '' ? (string) $title : __('Weekly report');

?>
<div class="container-fluid py-3">
    <h1 class="h4 mb-0 text-body"><?= htmlspecialchars($heading, ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?></h1>
    <?php if ($title === ''): ?>
        <p class="text-muted small mb-0 mt-2"><?= __('Weekly report hub description') ?></p>
    <?php endif; ?>
</div>

