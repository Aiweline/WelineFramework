<?php
$memberships = $this->getData('memberships') ?? [];
$summary = $this->getData('summary') ?? [];
$filters = $this->getData('filters') ?? [];
$levelOptionsRaw = $this->getData('levelOptions') ?? [];
if (!is_array($levelOptionsRaw)) {
    $levelOptionsRaw = [];
}
$membershipLevelCodes = ['bronze', 'silver', 'gold', 'platinum'];
$levelOptions = [];
foreach ($membershipLevelCodes as $code) {
    $label = '';
    if (array_key_exists($code, $levelOptionsRaw) && $levelOptionsRaw[$code] !== null && $levelOptionsRaw[$code] !== '') {
        $label = (string) $levelOptionsRaw[$code];
    }
    if ($label === '') {
        $label = match ($code) {
            'bronze' => (string) __('Bronze'),
            'silver' => (string) __('Silver'),
            'gold' => (string) __('Gold'),
            'platinum' => (string) __('Platinum'),
            default => $code,
        };
    }
    $levelOptions[$code] = $label;
}
$editingRecord = $this->getData('editingRecord') ?? [];
$pagination = $this->getData('pagination') ?? [];
$membershipIndexUrl = (string) ($this->getData('membershipIndexUrl') ?? $this->getBackendUrl('*/backend/membership'));
$membershipSaveUrl = (string) ($this->getData('membershipSaveUrl') ?? $this->getBackendUrl('*/backend/membership/save'));
$isEditing = !empty($editingRecord['membership_id']);
?>

<div class="container-fluid py-3">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <?php if ($isEditing): ?>
                <a href="<?= $membershipIndexUrl ?>" class="text-decoration-none small text-muted">
                    <i class="mdi mdi-arrow-left me-1"></i><?= __('Back to Memberships') ?>
                </a>
                <h1 class="h3 mb-1"><?= __('Edit Membership') ?></h1>
            <?php else: ?>
                <h1 class="h3 mb-1"><?= __('Membership Management') ?></h1>
            <?php endif; ?>
            <p class="text-muted mb-0">
                <?= __('Operate customer tiers, points, and upgrade visibility without leaving the backend.') ?>
            </p>
        </div>
        <a href="<?= $this->getBackendUrl('*/backend/membership/view') ?>" class="btn btn-primary">
            <i class="mdi mdi-account-plus me-1"></i><?= __('Create Membership') ?>
        </a>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Total Members') ?></div>
                    <div class="display-6"><?= number_format((int) ($summary['total'] ?? 0)) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Gold Members') ?></div>
                    <div class="display-6 text-warning"><?= number_format((int) ($summary['gold'] ?? 0)) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Platinum Members') ?></div>
                    <div class="display-6 text-primary"><?= number_format((int) ($summary['platinum'] ?? 0)) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Total Points') ?></div>
                    <div class="display-6"><?= number_format((int) ($summary['total_points'] ?? 0)) ?></div>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <?php if ($isEditing): ?>
        <div class="col-lg-4">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= __('Edit Membership') ?></h2>
                    <form method="post" action="<?= $membershipSaveUrl ?>" class="d-grid gap-3">
                        <input type="hidden" name="membership_id" value="<?= (int) ($editingRecord['membership_id'] ?? 0) ?>">
                        <input type="hidden" name="back_url" value="<?= $this->escapeHtmlAttr($membershipIndexUrl) ?>">
                        <div>
                            <label class="form-label"><?= __('Customer ID') ?></label>
                            <input type="number" name="customer_id" class="form-control" value="<?= htmlspecialchars((string) (int) ($editingRecord['customer_id'] ?? 0), ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?>" required min="1" readonly>
                            <div class="form-text"><?= __('Customer ID cannot be changed after creation.') ?></div>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Level') ?></label>
                            <select name="level" class="form-select">
                                <?php foreach ($membershipLevelCodes as $code): ?>
                                    <option value="<?= htmlspecialchars($code, ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?>" <?= ($editingRecord['level'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= $this->escapeHtml($levelOptions[$code]) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Points') ?></label>
                            <input type="number" min="0" name="points" class="form-control" value="<?= htmlspecialchars((string) (int) ($editingRecord['points'] ?? 0), ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?>" required>
                        </div>
                        <button type="submit" class="btn btn-primary">
                            <i class="mdi mdi-content-save me-1"></i><?= __('Save Membership') ?>
                        </button>
                        <a href="<?= $membershipIndexUrl ?>" class="btn btn-outline-secondary">
                            <?= __('Cancel') ?>
                        </a>
                    </form>
                </div>
            </div>
        </div>
        <?php endif; ?>

        <div class="<?= $isEditing ? 'col-lg-8' : 'col-lg-12' ?>">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <form method="get" action="<?= $membershipIndexUrl ?>" class="row g-3 align-items-end">
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Customer ID') ?></label>
                            <input type="number" name="customer_id" class="form-control" value="<?= $this->escapeHtmlAttr((string) ($filters['customer_id'] ?? '')) ?>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Level') ?></label>
                            <select name="level" class="form-select">
                                <option value=""><?= __('All levels') ?></option>
                                <?php foreach ($membershipLevelCodes as $code): ?>
                                    <option value="<?= htmlspecialchars($code, ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?>" <?= ($filters['level'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= $this->escapeHtml($levelOptions[$code]) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="col-12 d-flex gap-2">
                            <button type="submit" class="btn btn-outline-primary">
                                <i class="mdi mdi-magnify me-1"></i><?= __('Apply Filters') ?>
                            </button>
                            <a href="<?= $membershipIndexUrl ?>" class="btn btn-outline-secondary">
                                <i class="mdi mdi-filter-off me-1"></i><?= __('Reset') ?>
                            </a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <?php if ($memberships): ?>
                        <div class="table-responsive">
                            <table class="table align-middle">
                                <thead>
                                <tr>
                                    <th><?= __('Membership ID') ?></th>
                                    <th><?= __('Customer ID') ?></th>
                                    <th><?= __('Level') ?></th>
                                    <th><?= __('Points') ?></th>
                                    <th><?= __('Updated At') ?></th>
                                    <th><?= __('Created At') ?></th>
                                    <th class="text-end"><?= __('Actions') ?></th>
                                </tr>
                                </thead>
                                <tbody>
                                <?php foreach ($memberships as $membership): ?>
                                    <tr>
                                        <td>#<?= (int) $membership['membership_id'] ?></td>
                                        <td><?= (int) $membership['customer_id'] ?></td>
                                        <td>
                                            <span class="badge bg-<?= $this->escapeHtmlAttr($membership['level']) ?>">
                                                <?= $this->escapeHtml($membership['level_label']) ?>
                                            </span>
                                        </td>
                                        <td><?= number_format((int) $membership['points']) ?></td>
                                        <td><?= $this->escapeHtml($membership['updated_at']) ?></td>
                                        <td><?= $this->escapeHtml($membership['created_at']) ?></td>
                                        <td class="text-end">
                                            <a href="<?= $this->getBackendUrl('*/backend/membership/view', ['id' => $membership['membership_id']]) ?>" class="btn btn-sm btn-outline-primary">
                                                <i class="mdi mdi-pencil me-1"></i><?= __('Edit') ?>
                                            </a>
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>

                        <?php if (!empty($pagination['total_pages']) && $pagination['total_pages'] > 1): ?>
                            <nav aria-label="<?= __('Membership pagination') ?>" class="mt-4">
                                <ul class="pagination justify-content-center mb-0">
                                    <?php if (!empty($pagination['current_page']) && $pagination['current_page'] > 1): ?>
                                        <li class="page-item">
                                            <a class="page-link" href="<?= $this->getBackendUrl('*/backend/membership', array_merge($filters, ['page' => $pagination['current_page'] - 1])) ?>">
                                                <?= __('Previous') ?>
                                            </a>
                                        </li>
                                    <?php endif; ?>

                                    <?php for ($i = max(1, ($pagination['current_page'] ?? 1) - 2); $i <= min($pagination['total_pages'], ($pagination['current_page'] ?? 1) + 2); $i++): ?>
                                        <li class="page-item <?= $i === ($pagination['current_page'] ?? 1) ? 'active' : '' ?>">
                                            <a class="page-link" href="<?= $this->getBackendUrl('*/backend/membership', array_merge($filters, ['page' => $i])) ?>">
                                                <?= $i ?>
                                            </a>
                                        </li>
                                    <?php endfor; ?>

                                    <?php if (($pagination['current_page'] ?? 1) < $pagination['total_pages']): ?>
                                        <li class="page-item">
                                            <a class="page-link" href="<?= $this->getBackendUrl('*/backend/membership', array_merge($filters, ['page' => $pagination['current_page'] + 1])) ?>">
                                                <?= __('Next') ?>
                                            </a>
                                        </li>
                                    <?php endif; ?>
                                </ul>
                            </nav>
                        <?php endif; ?>
                    <?php else: ?>
                        <div class="text-center py-5">
                            <i class="mdi mdi-account-circle-outline display-1 text-muted mb-3"></i>
                            <h2 class="h5 mb-2"><?= __('No memberships found.') ?></h2>
                            <p class="text-muted mb-0"><?= __('Create the first membership record to begin customer tier operations.') ?></p>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>
