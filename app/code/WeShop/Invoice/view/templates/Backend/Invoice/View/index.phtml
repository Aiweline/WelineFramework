<?php
/**
 * Invoice View - 发票详情
 *
 * @var \Weline\Framework\View\Template $this
 * @var array $invoice 发票信息
 * @var array $invoiceItems 发票明细
 * @var array $statusOptions 状态选项
 */
$title = $title ?? __('Invoice Detail');
$invoice = $invoice ?? [];
$invoiceItems = $invoiceItems ?? [];
$statusOptions = $statusOptions ?? [];
$invoiceIndexUrl = $this->getBackendUrl('*/backend/invoice');
$invoiceIssueUrl = $this->getData('invoiceIssueUrl') ?? $this->getBackendUrl('*/backend/invoice/issue');

$statusBadgeMap = [
    'pending' => 'bg-warning',
    'processing' => 'bg-info',
    'issued' => 'bg-success',
    'cancelled' => 'bg-secondary',
    'refunded' => 'bg-danger',
];

$invoiceTypeMap = [
    'invoice' => __('Standard Invoice'),
    'vat' => __('VAT Invoice'),
    'receipt' => __('Receipt'),
];
?>
<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><?= $title ?>: #<?= htmlspecialchars((string) ($invoice['invoice_number'] ?? $invoice['invoice_id'] ?? '')) ?></h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="javascript: void(0);"><?= __('WeShop') ?></a></li>
                    <li class="breadcrumb-item"><a href="javascript: void(0);"><?= __('Invoice') ?></a></li>
                    <li class="breadcrumb-item"><a href="<?= htmlspecialchars($invoiceIndexUrl) ?>"><?= __('Invoice List') ?></a></li>
                    <li class="breadcrumb-item active"><?= __('Detail') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- Action Buttons -->
<div class="row mb-4">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <?php
                $status = (string) ($invoice['status'] ?? 'pending');
                $badgeClass = $statusBadgeMap[$status] ?? 'bg-secondary';
                $statusLabel = $statusOptions[$status] ?? ucfirst($status);
                ?>
                <span class="badge <?= $badgeClass ?> fs-6">
                    <?= htmlspecialchars($statusLabel) ?>
                </span>
            </div>
            <div class="d-flex gap-2">
                <a href="<?= htmlspecialchars($invoiceIndexUrl) ?>" class="btn btn-secondary">
                    <i class="mdi mdi-arrow-left me-1"></i><?= __('Back to List') ?>
                </a>
                <?php if ($status === 'issued'): ?>
                    <a href="<?= $this->getBackendUrl('*/backend/invoice/download', ['id' => $invoice['invoice_id'] ?? $invoice['id'] ?? 0]) ?>"
                       class="btn btn-success">
                        <i class="mdi mdi-download me-1"></i><?= __('Download PDF') ?>
                    </a>
                <?php endif; ?>
                <?php if ($status === 'pending'): ?>
                    <a href="<?= $this->getBackendUrl('*/backend/invoice/issue', ['id' => $invoice['invoice_id'] ?? $invoice['id'] ?? 0]) ?>"
                       class="btn btn-primary">
                        <i class="mdi mdi-check me-1"></i><?= __('Issue Invoice') ?>
                    </a>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <!-- Invoice Information -->
    <div class="col-lg-8">
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="card-title mb-0"><i class="mdi mdi-information me-2"></i><?= __('Invoice Information') ?></h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Invoice Number') ?></label>
                            <div class="fw-semibold">
                                <code><?= htmlspecialchars((string) ($invoice['invoice_number'] ?? '-')) ?></code>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Order Number') ?></label>
                            <div class="fw-semibold">
                                <a href="<?= $this->getBackendUrl('*/backend/order/view', ['id' => $invoice['order_id'] ?? 0]) ?>">
                                    #<?= htmlspecialchars((string) ($invoice['order_increment_id'] ?? $invoice['order_id'] ?? '')) ?>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Invoice Type') ?></label>
                            <div class="fw-semibold">
                                <?= $invoiceTypeMap[$invoice['type'] ?? 'invoice'] ?? htmlspecialchars((string) ($invoice['type'] ?? 'Standard Invoice')) ?>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Billing Period') ?></label>
                            <div class="fw-semibold">
                                <?= htmlspecialchars((string) ($invoice['billing_period'] ?? '-')) ?>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Issue Date') ?></label>
                            <div class="fw-semibold">
                                <?= htmlspecialchars((string) ($invoice['issued_at'] ?? $invoice['created_at'] ?? '-')) ?>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Due Date') ?></label>
                            <div class="fw-semibold">
                                <?= htmlspecialchars((string) ($invoice['due_date'] ?? '-')) ?>
                            </div>
                        </div>
                    </div>
                </div>

                <?php if (!empty($invoice['notes'])): ?>
                <div class="row">
                    <div class="col-12">
                        <div class="mb-0">
                            <label class="form-label text-muted"><?= __('Notes') ?></label>
                            <div class="fw-semibold">
                                <?= nl2br(htmlspecialchars((string) $invoice['notes'])) ?>
                            </div>
                        </div>
                    </div>
                </div>
                <?php endif; ?>
            </div>
        </div>

        <!-- Invoice Items -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="card-title mb-0"><i class="mdi mdi-format-list-bulleted me-2"></i><?= __('Invoice Items') ?></h5>
            </div>
            <div class="card-body p-0">
                <?php if (empty($invoiceItems)): ?>
                    <div class="text-center text-muted py-5">
                        <i class="mdi mdi-format-list-bulleted font-size-48 d-block mb-3"></i>
                        <p><?= __('No invoice items available') ?></p>
                    </div>
                <?php else: ?>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th class="text-center" style="width: 50px;">#</th>
                                    <th><?= __('Description') ?></th>
                                    <th class="text-end"><?= __('Quantity') ?></th>
                                    <th class="text-end"><?= __('Unit Price') ?></th>
                                    <th class="text-end"><?= __('Tax Rate') ?></th>
                                    <th class="text-end"><?= __('Tax Amount') ?></th>
                                    <th class="text-end"><?= __('Total') ?></th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($invoiceItems as $index => $item): ?>
                                    <tr>
                                        <td class="text-center text-muted"><?= $index + 1 ?></td>
                                        <td>
                                            <div class="fw-semibold"><?= htmlspecialchars((string) ($item['description'] ?? $item['name'] ?? '-')) ?></div>
                                            <?php if (!empty($item['sku'])): ?>
                                                <div class="small text-muted">SKU: <?= htmlspecialchars((string) $item['sku']) ?></div>
                                            <?php endif; ?>
                                        </td>
                                        <td class="text-end"><?= number_format((float) ($item['quantity'] ?? 1), 2) ?></td>
                                        <td class="text-end"><?= number_format((float) ($item['unit_price'] ?? 0), 2) ?></td>
                                        <td class="text-end"><?= number_format((float) ($item['tax_rate'] ?? 0) * 100, 1) ?>%</td>
                                        <td class="text-end"><?= number_format((float) ($item['tax_amount'] ?? 0), 2) ?></td>
                                        <td class="text-end fw-semibold"><?= number_format((float) ($item['total'] ?? 0), 2) ?></td>
                                    </tr>
                                <?php endforeach; ?>
                            </tbody>
                            <tfoot class="table-light">
                                <tr>
                                    <td colspan="5" class="text-end"><strong><?= __('Subtotal') ?>:</strong></td>
                                    <td class="text-end"><?= number_format((float) ($invoice['tax_amount'] ?? 0), 2) ?></td>
                                    <td class="text-end"><?= number_format((float) ($invoice['subtotal'] ?? 0), 2) ?></td>
                                </tr>
                                <tr>
                                    <td colspan="5" class="text-end"><strong><?= __('Tax') ?>:</strong></td>
                                    <td class="text-end">-</td>
                                    <td class="text-end"><?= number_format((float) ($invoice['tax_amount'] ?? 0), 2) ?></td>
                                </tr>
                                <tr class="table-primary">
                                    <td colspan="5" class="text-end"><strong><?= __('Total') ?>:</strong></td>
                                    <td class="text-end">-</td>
                                    <td class="text-end fw-bold fs-5"><?= number_format((float) ($invoice['amount'] ?? 0), 2) ?></td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>

    <!-- Sidebar - Customer & Payment Info -->
    <div class="col-lg-4">
        <!-- Customer Information -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="card-title mb-0"><i class="mdi mdi-account me-2"></i><?= __('Customer Information') ?></h5>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <label class="form-label text-muted"><?= __('Customer Name') ?></label>
                    <div class="fw-semibold">
                        <?= htmlspecialchars((string) ($invoice['customer_name'] ?? '-')) ?>
                    </div>
                </div>
                <div class="mb-3">
                    <label class="form-label text-muted"><?= __('Email') ?></label>
                    <div class="fw-semibold">
                        <?= htmlspecialchars((string) ($invoice['customer_email'] ?? '-')) ?>
                    </div>
                </div>
                <div class="mb-3">
                    <label class="form-label text-muted"><?= __('Tax ID / VAT Number') ?></label>
                    <div class="fw-semibold">
                        <?= htmlspecialchars((string) ($invoice['tax_id'] ?? '-')) ?>
                    </div>
                </div>
                <div class="mb-0">
                    <label class="form-label text-muted"><?= __('Billing Address') ?></label>
                    <div class="fw-semibold">
                        <?= nl2br(htmlspecialchars((string) ($invoice['billing_address'] ?? '-'))) ?>
                    </div>
                </div>
            </div>
        </div>

        <!-- Payment Summary -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="card-title mb-0"><i class="mdi mdi-currency-cny me-2"></i><?= __('Payment Summary') ?></h5>
            </div>
            <div class="card-body">
                <div class="d-flex justify-content-between mb-2">
                    <span class="text-muted"><?= __('Subtotal') ?></span>
                    <span class="fw-semibold"><?= number_format((float) ($invoice['subtotal'] ?? 0), 2) ?></span>
                </div>
                <div class="d-flex justify-content-between mb-2">
                    <span class="text-muted"><?= __('Tax') ?></span>
                    <span class="fw-semibold"><?= number_format((float) ($invoice['tax_amount'] ?? 0), 2) ?></span>
                </div>
                <hr>
                <div class="d-flex justify-content-between">
                    <span class="fw-bold"><?= __('Total Amount') ?></span>
                    <span class="fw-bold fs-5 text-primary"><?= number_format((float) ($invoice['amount'] ?? 0), 2) ?></span>
                </div>

                <?php if (!empty($invoice['paid_amount'])): ?>
                <hr>
                <div class="d-flex justify-content-between mb-2">
                    <span class="text-muted"><?= __('Paid Amount') ?></span>
                    <span class="fw-semibold text-success"><?= number_format((float) ($invoice['paid_amount'] ?? 0), 2) ?></span>
                </div>
                <div class="d-flex justify-content-between">
                    <span class="text-muted"><?= __('Outstanding') ?></span>
                    <span class="fw-semibold <?= ($invoice['amount'] - $invoice['paid_amount']) > 0 ? 'text-danger' : 'text-success' ?>">
                        <?= number_format((float) ($invoice['amount'] - $invoice['paid_amount']), 2) ?>
                    </span>
                </div>
                <?php endif; ?>
            </div>
        </div>

        <!-- Timeline / Audit Trail -->
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0"><i class="mdi mdi-history me-2"></i><?= __('Audit Trail') ?></h5>
            </div>
            <div class="card-body">
                <ul class="list-unstyled mb-0">
                    <li class="mb-3">
                        <div class="d-flex align-items-start">
                            <div class="flex-shrink-0 me-2">
                                <span class="badge bg-success rounded-circle" style="width: 8px; height: 8px; margin-top: 6px;"></span>
                            </div>
                            <div>
                                <div class="fw-semibold"><?= __('Invoice Created') ?></div>
                                <div class="small text-muted"><?= htmlspecialchars((string) ($invoice['created_at'] ?? '-')) ?></div>
                            </div>
                        </div>
                    </li>
                    <?php if (!empty($invoice['issued_at'])): ?>
                    <li class="mb-3">
                        <div class="d-flex align-items-start">
                            <div class="flex-shrink-0 me-2">
                                <span class="badge bg-primary rounded-circle" style="width: 8px; height: 8px; margin-top: 6px;"></span>
                            </div>
                            <div>
                                <div class="fw-semibold"><?= __('Invoice Issued') ?></div>
                                <div class="small text-muted"><?= htmlspecialchars((string) $invoice['issued_at']) ?></div>
                            </div>
                        </div>
                    </li>
                    <?php endif; ?>
                    <?php if (!empty($invoice['paid_at'])): ?>
                    <li class="mb-0">
                        <div class="d-flex align-items-start">
                            <div class="flex-shrink-0 me-2">
                                <span class="badge bg-info rounded-circle" style="width: 8px; height: 8px; margin-top: 6px;"></span>
                            </div>
                            <div>
                                <div class="fw-semibold"><?= __('Payment Received') ?></div>
                                <div class="small text-muted"><?= htmlspecialchars((string) $invoice['paid_at']) ?></div>
                            </div>
                        </div>
                    </li>
                    <?php endif; ?>
                </ul>
            </div>
        </div>
    </div>
</div>
