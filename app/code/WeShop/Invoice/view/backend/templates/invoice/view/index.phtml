<?php
$invoice = $this->getData('invoice') ?? [];
$canIssue = (bool) ($this->getData('canIssue') ?? false);
$invoiceIndexUrl = (string) ($this->getData('invoiceIndexUrl') ?? $this->getBackendUrl('*/backend/invoice'));
$invoiceIssueUrl = (string) ($this->getData('invoiceIssueUrl') ?? $this->getBackendUrl('*/backend/invoice/issue'));
?>

<div class="container-fluid py-3">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <h1 class="h3 mb-1"><?= __('Invoice Detail') ?></h1>
            <p class="text-muted mb-0"><?= __('Review invoice references and issue status without leaving the backend workflow.') ?></p>
        </div>
        <a href="<?= $invoiceIndexUrl ?>" class="btn btn-outline-secondary"><?= __('Back to Invoices') ?></a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <dl class="row g-3 mb-0">
                <div class="col-md-6">
                    <dt class="text-muted small"><?= __('Invoice Number') ?></dt>
                    <dd class="fw-semibold"><?= htmlspecialchars((string) ($invoice['invoice_number'] ?? ''), ENT_QUOTES) ?></dd>
                </div>
                <div class="col-md-6">
                    <dt class="text-muted small"><?= __('Order Number') ?></dt>
                    <dd class="fw-semibold"><?= htmlspecialchars((string) ($invoice['order_increment_id'] ?? ''), ENT_QUOTES) ?></dd>
                </div>
                <div class="col-md-4">
                    <dt class="text-muted small"><?= __('Amount') ?></dt>
                    <dd class="fw-semibold"><?= number_format((float) ($invoice['amount'] ?? 0), 2) ?></dd>
                </div>
                <div class="col-md-4">
                    <dt class="text-muted small"><?= __('Status') ?></dt>
                    <dd class="fw-semibold"><?= htmlspecialchars((string) ($invoice['status_label'] ?? $invoice['status'] ?? ''), ENT_QUOTES) ?></dd>
                </div>
                <div class="col-md-4">
                    <dt class="text-muted small"><?= __('Created At') ?></dt>
                    <dd class="fw-semibold"><?= htmlspecialchars((string) ($invoice['created_at'] ?? ''), ENT_QUOTES) ?></dd>
                </div>
            </dl>

            <?php if ($canIssue): ?>
                <form method="post" action="<?= $invoiceIssueUrl ?>" class="mt-4">
                    <input type="hidden" name="invoice_id" value="<?= (int) ($invoice['invoice_id'] ?? 0) ?>">
                    <input type="hidden" name="back_url" value="<?= htmlspecialchars($invoiceIndexUrl, ENT_QUOTES) ?>">
                    <button type="submit" class="btn btn-success"><?= __('Issue Invoice') ?></button>
                </form>
            <?php endif; ?>
        </div>
    </div>
</div>
