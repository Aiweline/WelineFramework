<?php
/**
 * 评价项配置。
 *
 * @var \Weline\Framework\View\Template $this
 */
$title = $title ?? __('评价项配置');
$options = is_array($options ?? null) ? $options : [];
$reviewMode = (string) ($reviewMode ?? 'order');
$reviewModeOptions = is_array($reviewModeOptions ?? null) ? $reviewModeOptions : [];
$reviewIndexUrl = (string) ($reviewIndexUrl ?? $this->getBackendUrl('*/backend/review'));
$reviewConfigSaveUrl = (string) ($reviewConfigSaveUrl ?? $this->getBackendUrl('*/backend/review/config/save'));
?>
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><?= htmlspecialchars((string) $title, ENT_QUOTES, 'UTF-8') ?></h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="<?= htmlspecialchars($reviewIndexUrl, ENT_QUOTES, 'UTF-8') ?>"><?= __('评论管理') ?></a></li>
                    <li class="breadcrumb-item active"><?= __('评价项配置') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="row mb-3">
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
    </div>
</div>

<form method="post" action="<?= htmlspecialchars($reviewConfigSaveUrl, ENT_QUOTES, 'UTF-8') ?>">
    <div class="row">
        <div class="col-xl-8">
            <div class="card">
                <div class="card-header d-flex align-items-center justify-content-between">
                    <h5 class="card-title mb-0"><i class="mdi mdi-tune-variant me-2"></i><?= __('已配置评价项') ?></h5>
                    <a href="<?= htmlspecialchars($reviewIndexUrl, ENT_QUOTES, 'UTF-8') ?>" class="btn btn-sm btn-outline-secondary">
                        <i class="mdi mdi-arrow-left me-1"></i><?= __('返回评论列表') ?>
                    </a>
                </div>
                <div class="card-body">
                    <?php if ($options === []): ?>
                        <div class="text-center text-muted py-5">
                            <i class="mdi mdi-tune-variant font-size-48 d-block mb-3"></i>
                            <p class="mb-0"><?= __('暂无评价项，请运行安装升级或新增自定义评价项。') ?></p>
                        </div>
                    <?php else: ?>
                        <div class="table-responsive">
                            <table class="table table-hover align-middle mb-0">
                                <thead class="table-light">
                                <tr>
                                    <th><?= __('启用') ?></th>
                                    <th><?= __('编码') ?></th>
                                    <th><?= __('名称') ?></th>
                                    <th><?= __('排序') ?></th>
                                    <th><?= __('类型') ?></th>
                                </tr>
                                </thead>
                                <tbody>
                                <?php foreach ($options as $option): ?>
                                    <?php
                                    $optionId = (int) ($option['option_id'] ?? 0);
                                    $code = (string) ($option['code'] ?? '');
                                    $label = (string) ($option['label'] ?? '');
                                    $sortOrder = (int) ($option['sort_order'] ?? 0);
                                    $isEnabled = !empty($option['is_enabled']);
                                    $isSystem = !empty($option['is_system']);
                                    ?>
                                    <tr>
                                        <td style="width: 90px;">
                                            <input type="hidden" name="options[<?= $optionId ?>][option_id]" value="<?= $optionId ?>">
                                            <div class="form-check form-switch">
                                                <input class="form-check-input" type="checkbox"
                                                       name="options[<?= $optionId ?>][is_enabled]"
                                                       value="1" <?= $isEnabled ? 'checked' : '' ?>>
                                            </div>
                                        </td>
                                        <td>
                                            <code><?= htmlspecialchars($code, ENT_QUOTES, 'UTF-8') ?></code>
                                        </td>
                                        <td>
                                            <input type="text" class="form-control"
                                                   name="options[<?= $optionId ?>][label]"
                                                   value="<?= htmlspecialchars($label, ENT_QUOTES, 'UTF-8') ?>"
                                                   required>
                                        </td>
                                        <td style="width: 140px;">
                                            <input type="number" class="form-control"
                                                   name="options[<?= $optionId ?>][sort_order]"
                                                   value="<?= $sortOrder ?>">
                                        </td>
                                        <td>
                                            <span class="badge <?= $isSystem ? 'bg-primary' : 'bg-secondary' ?>">
                                                <?= $isSystem ? __('系统项') : __('自定义') ?>
                                            </span>
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>

        <div class="col-xl-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0"><i class="mdi mdi-account-check-outline me-2"></i><?= __('评价模式') ?></h5>
                </div>
                <div class="card-body">
                    <?php foreach ($reviewModeOptions as $mode => $label): ?>
                        <div class="form-check mb-3">
                            <input
                                class="form-check-input"
                                type="radio"
                                name="review_mode"
                                id="review-mode-<?= htmlspecialchars((string) $mode, ENT_QUOTES, 'UTF-8') ?>"
                                value="<?= htmlspecialchars((string) $mode, ENT_QUOTES, 'UTF-8') ?>"
                                <?= (string) $mode === $reviewMode ? 'checked' : '' ?>
                            >
                            <label class="form-check-label fw-semibold" for="review-mode-<?= htmlspecialchars((string) $mode, ENT_QUOTES, 'UTF-8') ?>">
                                <?= htmlspecialchars((string) $label, ENT_QUOTES, 'UTF-8') ?>
                            </label>
                            <div class="form-text">
                                <?php if ((string) $mode === 'anonymous'): ?>
                                    <?= __('游客可直接提交评价，后台审核通过后才会公开展示；提交后仅当前浏览器会立即看到待审核评价。') ?>
                                <?php else: ?>
                                    <?= __('客户需要登录，并且购买过当前商品后才能提交评价。') ?>
                                <?php endif; ?>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0"><i class="mdi mdi-plus-circle-outline me-2"></i><?= __('新增自定义评价项') ?></h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label"><?= __('编码') ?></label>
                        <input type="text" class="form-control" name="new_option[code]" placeholder="packaging">
                        <div class="form-text"><?= __('仅支持小写字母、数字和下划线，保存时会自动规范化。') ?></div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('名称') ?></label>
                        <input type="text" class="form-control" name="new_option[label]" placeholder="<?= htmlspecialchars((string) __('包装体验'), ENT_QUOTES, 'UTF-8') ?>">
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('排序') ?></label>
                        <input type="number" class="form-control" name="new_option[sort_order]" value="100">
                    </div>
                    <div class="form-check form-switch mb-0">
                        <input class="form-check-input" type="checkbox" name="new_option[is_enabled]" value="1" checked>
                        <label class="form-check-label"><?= __('保存后立即启用') ?></label>
                    </div>
                </div>
            </div>

            <div class="card">
                <div class="card-body">
                    <p class="text-muted mb-0">
                        <?= __('启用的评价项会展示在前台评价表单中，买家提交后会随评价一起保存并展示在后台详情和产品评价区。') ?>
                    </p>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body d-flex justify-content-end gap-2">
                    <a href="<?= htmlspecialchars($reviewIndexUrl, ENT_QUOTES, 'UTF-8') ?>" class="btn btn-outline-secondary">
                        <?= __('取消') ?>
                    </a>
                    <button type="submit" class="btn btn-primary">
                        <i class="mdi mdi-content-save me-1"></i><?= __('保存配置') ?>
                    </button>
                </div>
            </div>
        </div>
    </div>
</form>
