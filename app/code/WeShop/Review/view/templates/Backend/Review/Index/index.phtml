<?php
/**
 * Review List - 评论列表
 *
 * @var \Weline\Framework\View\Template $this
 * @var array $reviews 评论列表
 * @var array $filters 筛选条件
 * @var array $pagination 分页信息
 * @var array $statusOptions 状态选项
 * @var array $summary 统计信息
 */
$title = $title ?? __('Review Management');
$reviews = $reviews ?? [];
$filters = $filters ?? [];
$pagination = $pagination ?? [];
$statusOptions = $statusOptions ?? [];
$summary = $summary ?? [];
$reviewIndexUrl = $this->getBackendUrl('*/backend/review');
$reviewViewUrl = $this->getData('reviewViewUrl') ?? $this->getBackendUrl('*/backend/review/view');
$reviewApproveUrl = $this->getData('reviewApproveUrl') ?? $this->getBackendUrl('*/backend/review/approve');
$reviewRejectUrl = $this->getData('reviewRejectUrl') ?? $this->getBackendUrl('*/backend/review/reject');
$reviewDeleteUrl = $this->getData('reviewDeleteUrl') ?? $this->getBackendUrl('*/backend/review/delete');
$reviewConfigUrl = $this->getData('reviewConfigUrl') ?? $this->getBackendUrl('*/backend/review/config');

$statusBadgeMap = [
    'pending' => 'bg-warning',
    'approved' => 'bg-success',
    'rejected' => 'bg-danger',
];

$ratingStarsMap = [
    1 => '⭐',
    2 => '⭐⭐',
    3 => '⭐⭐⭐',
    4 => '⭐⭐⭐⭐',
    5 => '⭐⭐⭐⭐⭐',
];
?>
<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><?= $title ?></h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="javascript: void(0);"><?= __('WeShop') ?></a></li>
                    <li class="breadcrumb-item"><a href="javascript: void(0);"><?= __('Review') ?></a></li>
                    <li class="breadcrumb-item active"><?= __('Review List') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- start page message -->
<div class="row mb-3">
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
        <div id="page-message-container"></div>
    </div>
</div>
<!-- end message -->

<!-- Statistics Cards -->
<div class="row mb-4">
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-primary bg-soft">
                            <span class="avatar-title bg-primary rounded-circle">
                                <i class="mdi mdi-star font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('Total Reviews') ?></p>
                        <h4 class="mb-0"><?= number_format($summary['total'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-warning bg-soft">
                            <span class="avatar-title bg-warning rounded-circle">
                                <i class="mdi mdi-clock-outline font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('Pending') ?></p>
                        <h4 class="mb-0"><?= number_format($summary['pending'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-success bg-soft">
                            <span class="avatar-title bg-success rounded-circle">
                                <i class="mdi mdi-check-circle font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('Approved') ?></p>
                        <h4 class="mb-0"><?= number_format($summary['approved'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-danger bg-soft">
                            <span class="avatar-title bg-danger rounded-circle">
                                <i class="mdi mdi-close-circle font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('Rejected') ?></p>
                        <h4 class="mb-0"><?= number_format($summary['rejected'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Filter Form -->
<div class="row mb-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0"><i class="mdi mdi-filter me-2"></i><?= __('Filter Reviews') ?></h5>
            </div>
            <div class="card-body">
                <form method="get" action="<?= htmlspecialchars($reviewIndexUrl) ?>">
                    <div class="row g-3">
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Product ID') ?></label>
                            <input type="number" class="form-control" name="product_id"
                                   value="<?= htmlspecialchars((string) ($filters['product_id'] ?? '')) ?>"
                                   placeholder="<?= __('Enter product ID') ?>">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Customer ID') ?></label>
                            <input type="number" class="form-control" name="customer_id"
                                   value="<?= htmlspecialchars((string) ($filters['customer_id'] ?? '')) ?>"
                                   placeholder="<?= __('Enter customer ID') ?>">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Status') ?></label>
                            <select class="form-select" name="status">
                                <option value=""><?= __('All Statuses') ?></option>
                                <?php foreach ($statusOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars($code) ?>" <?= ($filters['status'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars($label) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Rating') ?></label>
                            <select class="form-select" name="rating">
                                <option value=""><?= __('All Ratings') ?></option>
                                <?php for ($i = 5; $i >= 1; $i--): ?>
                                    <option value="<?= $i ?>" <?= ($filters['rating'] ?? '') == $i ? 'selected' : '' ?>>
                                        <?= $ratingStarsMap[$i] ?? $i ?> <?= __('Star(s)') ?>
                                    </option>
                                <?php endfor; ?>
                            </select>
                        </div>
                    </div>
                    <div class="row mt-3">
                        <div class="col-12">
                            <button type="submit" class="btn btn-primary">
                                <i class="mdi mdi-filter"></i> <?= __('Apply Filters') ?>
                            </button>
                            <a href="<?= htmlspecialchars($reviewIndexUrl) ?>" class="btn btn-secondary">
                                <i class="mdi mdi-refresh"></i> <?= __('Reset') ?>
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Review List -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="card-title mb-0"><?= __('Review List') ?></h5>
                <div>
                    <a href="<?= htmlspecialchars((string) $reviewConfigUrl) ?>" class="btn btn-sm btn-outline-primary">
                        <i class="mdi mdi-tune-variant"></i> <?= __('评价项配置') ?>
                    </a>
                </div>
            </div>
            <div class="card-body">
                <?php if (empty($reviews)): ?>
                    <div class="text-center text-muted py-5">
                        <i class="mdi mdi-star-outline font-size-48 d-block mb-3"></i>
                        <p><?= __('No reviews found') ?></p>
                    </div>
                <?php else: ?>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th><?= __('Review ID') ?></th>
                                    <th><?= __('Product ID') ?></th>
                                    <th><?= __('Customer ID') ?></th>
                                    <th><?= __('Rating') ?></th>
                                    <th><?= __('细分评分') ?></th>
                                    <th><?= __('媒体') ?></th>
                                    <th><?= __('Title') ?></th>
                                    <th><?= __('Content') ?></th>
                                    <th><?= __('Status') ?></th>
                                    <th><?= __('Created At') ?></th>
                                    <th><?= __('Actions') ?></th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($reviews as $review): ?>
                                    <?php
                                    $reviewId = (int) ($review['review_id'] ?? $review['id'] ?? 0);
                                    $status = (string) ($review['status'] ?? 'pending');
                                    $badgeClass = $statusBadgeMap[$status] ?? 'bg-secondary';
                                    $statusLabel = $statusOptions[$status] ?? ucfirst($status);
                                    $rating = (int) ($review['rating'] ?? 5);
                                    $ratingStars = $ratingStarsMap[$rating] ?? str_repeat('⭐', $rating);
                                    ?>
                                    <tr>
                                        <td><strong>#<?= $reviewId ?></strong></td>
                                        <td>
                                            <a href="<?= $this->getBackendUrl('*/backend/product/edit', ['product_id' => $review['product_id'] ?? 0]) ?>">
                                                #<?= htmlspecialchars((string) ($review['product_id'] ?? '')) ?>
                                            </a>
                                        </td>
                                        <td>
                                            <a href="<?= $this->getBackendUrl('*/backend/customer/edit', ['id' => $review['customer_id'] ?? 0]) ?>">
                                                #<?= htmlspecialchars((string) ($review['customer_id'] ?? '')) ?>
                                            </a>
                                        </td>
                                        <td>
                                            <span class="text-warning"><?= $ratingStars ?></span>
                                            <span class="text-muted ms-1">(<?= $rating ?>)</span>
                                        </td>
                                        <td>
                                            <?php $scoreLabels = is_array($review['rating_score_labels'] ?? null) ? $review['rating_score_labels'] : []; ?>
                                            <?php if ($scoreLabels): ?>
                                                <div class="d-flex flex-column gap-1">
                                                    <?php foreach ($scoreLabels as $scoreItem): ?>
                                                        <span class="small">
                                                            <?= htmlspecialchars((string) __((string) ($scoreItem['label'] ?? '')), ENT_QUOTES, 'UTF-8') ?>:
                                                            <strong><?= (int) ($scoreItem['score'] ?? 0) ?></strong>
                                                        </span>
                                                    <?php endforeach; ?>
                                                </div>
                                            <?php else: ?>
                                                <span class="text-muted">-</span>
                                            <?php endif; ?>
                                        </td>
                                        <td>
                                            <?php
                                            $imageCount = (int) ($review['image_count'] ?? 0);
                                            $videoCount = (int) ($review['video_count'] ?? 0);
                                            ?>
                                            <?php if ($imageCount > 0 || $videoCount > 0): ?>
                                                <div class="d-flex gap-1 flex-wrap">
                                                    <?php if ($imageCount > 0): ?>
                                                        <span class="badge bg-info"><?= __('图片') ?> <?= $imageCount ?></span>
                                                    <?php endif; ?>
                                                    <?php if ($videoCount > 0): ?>
                                                        <span class="badge bg-primary"><?= __('视频') ?> <?= $videoCount ?></span>
                                                    <?php endif; ?>
                                                </div>
                                            <?php else: ?>
                                                <span class="text-muted">-</span>
                                            <?php endif; ?>
                                        </td>
                                        <td>
                                            <?php if (!empty($review['title'])): ?>
                                                <span title="<?= htmlspecialchars((string) ($review['title'] ?? '')) ?>">
                                                    <?= htmlspecialchars(mb_substr((string) ($review['title'] ?? ''), 0, 30)) ?><?= mb_strlen((string) ($review['title'] ?? '')) > 30 ? '...' : '' ?>
                                                </span>
                                            <?php else: ?>
                                                <span class="text-muted">-</span>
                                            <?php endif; ?>
                                        </td>
                                        <td>
                                            <?php if (!empty($review['content'])): ?>
                                                <span title="<?= htmlspecialchars((string) ($review['content'] ?? '')) ?>">
                                                    <?= htmlspecialchars(mb_substr((string) ($review['content'] ?? ''), 0, 50)) ?><?= mb_strlen((string) ($review['content'] ?? '')) > 50 ? '...' : '' ?>
                                                </span>
                                            <?php else: ?>
                                                <span class="text-muted">-</span>
                                            <?php endif; ?>
                                        </td>
                                        <td>
                                            <span class="badge <?= $badgeClass ?>">
                                                <?= htmlspecialchars($statusLabel) ?>
                                            </span>
                                        </td>
                                        <td><?= htmlspecialchars((string) ($review['created_at'] ?? '-')) ?></td>
                                        <td>
                                            <div class="btn-group btn-group-sm">
                                                <a href="<?= $this->getBackendUrl('*/backend/review/view', ['id' => $reviewId]) ?>"
                                                   class="btn btn-outline-info"
                                                   title="<?= __('View') ?>">
                                                    <i class="mdi mdi-eye"></i>
                                                </a>
                                                <?php if ($status === 'pending'): ?>
                                                    <button type="button"
                                                            class="btn btn-outline-success"
                                                            title="<?= __('Approve') ?>"
                                                            onclick="approveReview(<?= $reviewId ?>)">
                                                        <i class="mdi mdi-check"></i>
                                                    </button>
                                                    <button type="button"
                                                            class="btn btn-outline-danger"
                                                            title="<?= __('Reject') ?>"
                                                            onclick="rejectReview(<?= $reviewId ?>)">
                                                        <i class="mdi mdi-close"></i>
                                                    </button>
                                                <?php endif; ?>
                                                <button type="button"
                                                        class="btn btn-outline-danger"
                                                        title="<?= __('Delete') ?>"
                                                        onclick="deleteReview(<?= $reviewId ?>)">
                                                    <i class="mdi mdi-trash-can-outline"></i>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>

                    <!-- Pagination -->
                    <?php if (!empty($pagination) && ($pagination['total_pages'] ?? 1) > 1): ?>
                        <nav aria-label="Page navigation" class="mt-4">
                            <ul class="pagination justify-content-center">
                                <?php if ($pagination['has_previous'] ?? false): ?>
                                    <li class="page-item">
                                        <a class="page-link" href="<?= $this->getBackendUrl('*/backend/review', array_merge($filters, ['page' => $pagination['previous_page'] ?? 1])) ?>">
                                            <?= __('Previous') ?>
                                        </a>
                                    </li>
                                <?php endif; ?>

                                <?php
                                $currentPage = (int) ($pagination['current_page'] ?? 1);
                                $totalPages = (int) ($pagination['total_pages'] ?? 1);
                                $startPage = max(1, $currentPage - 2);
                                $endPage = min($totalPages, $currentPage + 2);
                                ?>
                                <?php for ($i = $startPage; $i <= $endPage; $i++): ?>
                                    <li class="page-item <?= $i === $currentPage ? 'active' : '' ?>">
                                        <a class="page-link" href="<?= $this->getBackendUrl('*/backend/review', array_merge($filters, ['page' => $i])) ?>">
                                            <?= $i ?>
                                        </a>
                                    </li>
                                <?php endfor; ?>

                                <?php if ($pagination['has_next'] ?? false): ?>
                                    <li class="page-item">
                                        <a class="page-link" href="<?= $this->getBackendUrl('*/backend/review', array_merge($filters, ['page' => $pagination['next_page'] ?? 2])) ?>">
                                            <?= __('Next') ?>
                                        </a>
                                    </li>
                                <?php endif; ?>
                            </ul>
                        </nav>
                        <div class="text-center text-muted mt-2">
                            <?= __('Page %{1} of %{2}', [$currentPage, $totalPages]) ?>
                        </div>
                    <?php endif; ?>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<script>
function approveReview(reviewId) {
    BackendConfirm({
        title: '<?= __('Approve Review') ?>',
        message: '<?= __('Are you sure you want to approve this review?') ?>',
        confirmText: '<?= __('Approve') ?>',
        onConfirm: function() {
            fetch('<?= htmlspecialchars($reviewApproveUrl) ?>?id=' + reviewId, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    BackendToast.success(data.message || '<?= __('Review approved successfully') ?>');
                    setTimeout(function() {
                        window.location.reload();
                    }, 1000);
                } else {
                    BackendToast.error(data.message || '<?= __('Failed to approve review') ?>');
                }
            })
            .catch(error => {
                BackendToast.error('<?= __('Request failed') ?>: ' + error.message);
            });
        }
    });
}

function rejectReview(reviewId) {
    BackendConfirm({
        title: '<?= __('Reject Review') ?>',
        message: '<?= __('Are you sure you want to reject this review?') ?>',
        confirmText: '<?= __('Reject') ?>',
        onConfirm: function() {
            fetch('<?= htmlspecialchars($reviewRejectUrl) ?>?id=' + reviewId, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    BackendToast.success(data.message || '<?= __('Review rejected successfully') ?>');
                    setTimeout(function() {
                        window.location.reload();
                    }, 1000);
                } else {
                    BackendToast.error(data.message || '<?= __('Failed to reject review') ?>');
                }
            })
            .catch(error => {
                BackendToast.error('<?= __('Request failed') ?>: ' + error.message);
            });
        }
    });
}

function deleteReview(reviewId) {
    BackendConfirm({
        title: '<?= __('Delete Review') ?>',
        message: '<?= __('Are you sure you want to delete this review? This action cannot be undone.') ?>',
        confirmText: '<?= __('Delete') ?>',
        onConfirm: function() {
            fetch('<?= htmlspecialchars($reviewDeleteUrl) ?>?id=' + reviewId, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    BackendToast.success(data.message || '<?= __('Review deleted successfully') ?>');
                    setTimeout(function() {
                        window.location.href = '<?= htmlspecialchars($reviewIndexUrl) ?>';
                    }, 1000);
                } else {
                    BackendToast.error(data.message || '<?= __('Failed to delete review') ?>');
                }
            })
            .catch(error => {
                BackendToast.error('<?= __('Request failed') ?>: ' + error.message);
            });
        }
    });
}

</script>
