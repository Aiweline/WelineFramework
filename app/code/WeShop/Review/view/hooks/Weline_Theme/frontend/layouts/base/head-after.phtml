<?php
/**
 * Product review frontend assets.
 *
 * @hook-priority 180
 * @hook-sort-order 20
 * @hook-solo false
 */
try {
    $productReviewCssUrl = (string) $this->fetchTagSource('statics', 'WeShop_Review::css/product-reviews.css', false);
} catch (\Throwable) {
    $productReviewCssUrl = '';
}

if ($productReviewCssUrl === '') {
    return;
}

$productReviewCssUrl .= str_contains($productReviewCssUrl, '?') ? '&v=20260528-product-review-thread-polish-3' : '?v=20260528-product-review-thread-polish-3';
?>
<link rel="stylesheet" href="<?= htmlspecialchars($productReviewCssUrl, ENT_QUOTES, 'UTF-8') ?>">
