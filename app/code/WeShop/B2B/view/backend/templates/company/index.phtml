<?php
$companySummary = $this->getData('companySummary') ?? [];
$companyRecords = $this->getData('companyRecords') ?? [];
$filters = $this->getData('filters') ?? [];
$statusOptions = $this->getData('statusOptions') ?? [];
$editingRecord = $this->getData('editingRecord') ?? [];
$companyIndexUrl = (string) ($this->getData('companyIndexUrl') ?? $this->getBackendUrl('*/backend/company'));
$companySaveUrl = (string) ($this->getData('companySaveUrl') ?? $this->getBackendUrl('*/backend/company/save'));
$pagination = $this->getData('pagination') ?? [];
$currentPage = max(1, (int) ($pagination['page'] ?? 1));
$pageCount = max(1, (int) ($pagination['page_count'] ?? 1));
$totalCompanies = max(0, (int) ($pagination['total'] ?? 0));
$editingId = max(0, (int) ($editingRecord['company_id'] ?? 0));
$companyBackUrl = $editingId > 0
    ? (string) $this->getBackendUrl('*/backend/company', ['id' => $editingId])
    : $companyIndexUrl;
?>

<div class="container-fluid py-3">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <h1 class="h3 mb-1"><?= __('B2B Company Management') ?></h1>
            <p class="text-muted mb-0"><?= __('Manage corporate account records, contact emails, and approval statuses from one operational workspace.') ?></p>
        </div>
        <div class="text-muted small">
            <?= htmlspecialchars(sprintf(__('Page %d of %d, %d companies found'), $currentPage, $pageCount, $totalCompanies), ENT_QUOTES) ?>
        </div>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-sm-6 col-xl">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-uppercase small text-muted mb-2"><?= __('Total') ?></div>
                    <div class="display-6 fw-semibold"><?= (int) ($companySummary['total'] ?? 0) ?></div>
                </div>
            </div>
        </div>
        <?php foreach ($statusOptions as $statusCode => $statusLabel): ?>
            <div class="col-sm-6 col-xl">
                <div class="card shadow-sm h-100">
                    <div class="card-body">
                        <div class="text-uppercase small text-muted mb-2"><?= htmlspecialchars((string) $statusLabel, ENT_QUOTES) ?></div>
                        <div class="display-6 fw-semibold"><?= (int) ($companySummary[$statusCode] ?? 0) ?></div>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>

    <div class="row g-4">
        <div class="col-lg-4">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= $editingId ? __('Edit B2B Company') : __('Create B2B Company') ?></h2>
                    <form method="post" action="<?= $companySaveUrl ?>" class="d-grid gap-3" data-e2e="weshop-b2b-company-edit-form">
                        <input type="hidden" name="company_id" value="<?= $editingId ?>">
                        <input type="hidden" name="back_url" value="<?= htmlspecialchars($companyBackUrl, ENT_QUOTES) ?>">
                        <div>
                            <label class="form-label"><?= __('Company Name') ?></label>
                            <input type="text"
                                   name="name"
                                   class="form-control"
                                   value="<?= htmlspecialchars((string) ($editingRecord['name'] ?? ''), ENT_QUOTES) ?>"
                                   required>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Contact Email') ?></label>
                            <input type="email"
                                   name="email"
                                   class="form-control"
                                   value="<?= htmlspecialchars((string) ($editingRecord['email'] ?? ''), ENT_QUOTES) ?>"
                                   required>
                        </div>
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label"><?= __('Tax ID') ?></label>
                                <input type="text"
                                       name="tax_id"
                                       class="form-control"
                                       value="<?= htmlspecialchars((string) ($editingRecord['tax_id'] ?? ''), ENT_QUOTES) ?>">
                            </div>
                            <div class="col-md-6">
                                <label class="form-label"><?= __('Phone') ?></label>
                                <input type="text"
                                       name="phone"
                                       class="form-control"
                                       value="<?= htmlspecialchars((string) ($editingRecord['phone'] ?? ''), ENT_QUOTES) ?>">
                            </div>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Address') ?></label>
                            <textarea name="address" class="form-control" rows="3"><?= htmlspecialchars((string) ($editingRecord['address'] ?? ''), ENT_QUOTES) ?></textarea>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Status') ?></label>
                            <select name="status" class="form-select">
                                <?php foreach ($statusOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>" <?= ($editingRecord['status'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <button type="submit" class="btn btn-primary"><?= __('Save Company') ?></button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-8">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <form method="get" action="<?= $companyIndexUrl ?>" class="row g-3 align-items-end">
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Company Name') ?></label>
                            <input type="text" name="name" class="form-control" value="<?= htmlspecialchars((string) ($filters['name'] ?? ''), ENT_QUOTES) ?>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Contact Email') ?></label>
                            <input type="text" name="email" class="form-control" value="<?= htmlspecialchars((string) ($filters['email'] ?? ''), ENT_QUOTES) ?>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Status') ?></label>
                            <select name="status" class="form-select">
                                <option value=""><?= __('All statuses') ?></option>
                                <?php foreach ($statusOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>" <?= ($filters['status'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="col-12 d-flex gap-2">
                            <button type="submit" class="btn btn-outline-primary"><?= __('Apply Filters') ?></button>
                            <a href="<?= $companyIndexUrl ?>" class="btn btn-outline-secondary"><?= __('Reset') ?></a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <?php if ($companyRecords): ?>
                        <div class="table-responsive">
                            <table class="table align-middle">
                                <thead>
                                <tr>
                                    <th><?= __('Company ID') ?></th>
                                    <th><?= __('Company Name') ?></th>
                                    <th><?= __('Contact Email') ?></th>
                                    <th><?= __('Tax ID') ?></th>
                                    <th><?= __('Phone') ?></th>
                                    <th><?= __('Status') ?></th>
                                    <th><?= __('Created At') ?></th>
                                    <th class="text-end"><?= __('Actions') ?></th>
                                </tr>
                                </thead>
                                <tbody>
                                <?php foreach ($companyRecords as $record): ?>
                                    <tr>
                                        <td><?= (int) $record['company_id'] ?></td>
                                        <td><?= htmlspecialchars((string) $record['name'], ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) $record['email'], ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) ($record['tax_id'] ?: __('Not provided')), ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) ($record['phone'] ?: __('Not provided')), ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) $record['status_label'], ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) $record['created_at'], ENT_QUOTES) ?></td>
                                        <td class="text-end">
                                            <a href="<?= $this->getBackendUrl('*/backend/company', ['id' => $record['company_id']]) ?>" class="btn btn-sm btn-outline-primary">
                                                <?= __('Edit') ?>
                                            </a>
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>
                    <?php else: ?>
                        <div class="text-center py-5">
                            <h2 class="h5 mb-2"><?= __('No B2B companies found.') ?></h2>
                            <p class="text-muted mb-0"><?= __('Create the first linked company profile or adjust the filters to find an existing account.') ?></p>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>
