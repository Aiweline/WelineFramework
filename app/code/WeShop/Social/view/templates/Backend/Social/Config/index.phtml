<?php
$pageTitle = (string) ($this->getData('page_title') ?? __('Social Configuration'));
$saveUrl = (string) ($this->getData('save_url') ?? '');
$footerLinks = (array) ($this->getData('footer_links') ?? []);
$platforms = (array) ($this->getData('platforms') ?? []);

$platformIcons = [
    'facebook' => 'mdi-facebook',
    'instagram' => 'mdi-instagram',
    'x' => 'mdi-twitter-x',
    'youtube' => 'mdi-youtube',
    'tiktok' => 'mdi-music-note',
    'linkedin' => 'mdi-linkedin',
];
?>
<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                <h4 class="mb-sm-0"><?= htmlspecialchars($pageTitle) ?></h4>
                <div class="page-title-right">
                    <ol class="breadcrumb m-0">
                        <li class="breadcrumb-item"><?= __('WeShop') ?></li>
                        <li class="breadcrumb-item"><?= __('Social') ?></li>
                        <li class="breadcrumb-item active"><?= __('Configuration') ?></li>
                    </ol>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-md-4">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small"><?= __('Total Platforms') ?></div>
                    <div class="display-6 fw-semibold"><?= \count($platforms) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small"><?= __('Configured Links') ?></div>
                    <div class="display-6 fw-semibold text-success"><?= \count($footerLinks) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small"><?= __('Pending Configuration') ?></div>
                    <div class="display-6 fw-semibold text-warning"><?= \count($platforms) - \count($footerLinks) ?></div>
                </div>
            </div>
        </div>
    </div>

    <form action="<?= htmlspecialchars($saveUrl) ?>" method="post" class="d-grid gap-4">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-transparent">
                <h5 class="mb-0"><?= __('Footer Social Links') ?></h5>
                <p class="text-muted small mb-0"><?= __('Configure the social media links displayed in the store footer.') ?></p>
            </div>
            <div class="card-body p-4">
                <div class="row g-4">
                    <?php foreach ($platforms as $platform => $config): ?>
                        <?php
                        $label = (string) ($config['label'] ?? $platform);
                        $icon = (string) ($platformIcons[$platform] ?? 'mdi-link');
                        $existingUrl = '';
                        foreach ($footerLinks as $link) {
                            if (\is_array($link) && ($link['platform'] ?? '') === $platform) {
                                $existingUrl = (string) ($link['url'] ?? '');
                                break;
                            }
                        }
                        ?>
                        <div class="col-md-6">
                            <div class="d-flex align-items-start gap-3 p-3 bg-light rounded-3">
                                <div class="social-link__icon-wrapper">
                                    <i class="<?= htmlspecialchars($icon) ?> fs-4 text-primary"></i>
                                </div>
                                <div class="flex-grow-1">
                                    <label class="form-label fw-semibold" for="social_<?= htmlspecialchars($platform) ?>">
                                        <?= htmlspecialchars($label) ?>
                                    </label>
                                    <input
                                        class="form-control"
                                        type="url"
                                        id="social_<?= htmlspecialchars($platform) ?>"
                                        name="links[<?= htmlspecialchars($platform) ?>]"
                                        value="<?= htmlspecialchars($existingUrl) ?>"
                                        placeholder="https://"
                                    >
                                    <div class="form-text"><?= __('Leave empty to hide from footer.') ?></div>
                                </div>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            </div>
        </div>

        <div class="card border-0 shadow-sm">
            <div class="card-header bg-transparent">
                <h5 class="mb-0"><?= __('Share Settings') ?></h5>
                <p class="text-muted small mb-0"><?= __('Configure product and page sharing behavior.') ?></p>
            </div>
            <div class="card-body p-4">
                <div class="row g-4">
                    <div class="col-md-6">
                        <div class="form-check form-switch">
                            <input
                                class="form-check-input"
                                type="checkbox"
                                id="share_tracking_enabled"
                                name="share_tracking_enabled"
                                value="1"
                                checked
                            >
                            <label class="form-check-label" for="share_tracking_enabled">
                                <?= __('Enable Share Tracking') ?>
                            </label>
                        </div>
                        <div class="form-text"><?= __('Record when users share products on social media.') ?></div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-check form-switch">
                            <input
                                class="form-check-input"
                                type="checkbox"
                                id="share_count_enabled"
                                name="share_count_enabled"
                                value="1"
                                checked
                            >
                            <label class="form-check-label" for="share_count_enabled">
                                <?= __('Show Share Counts') ?>
                            </label>
                        </div>
                        <div class="form-text"><?= __('Display the number of times a product has been shared.') ?></div>
                    </div>
                </div>
            </div>
        </div>

        <div class="d-flex justify-content-end">
            <button class="btn btn-primary btn-lg" type="submit">
                <i class="mdi mdi-content-save-outline me-1"></i><?= __('Save Social Settings') ?>
            </button>
        </div>
    </form>
</div>

<style>
.social-link__icon-wrapper {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bs-light-bg-subtle, #f8f9fa);
    border-radius: var(--bs-border-radius, 0.25rem);
    flex-shrink: 0;
}
</style>
