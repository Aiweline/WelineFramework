<?php
/**
 * 注意：勿在此文件使用 declare(strict_types=1)。框架运行时会在本文件内容前追加带哈希的 PHP 开闭标签，
 * 这会导致 strict_types 声明不是脚本的第一条语句，从而触发 E_COMPILE_ERROR。
 */

$report = $this->getData('report') ?? [];
$startDate = $this->getData('start_date') ?? '';
$endDate = $this->getData('end_date') ?? '';
?>

<div class="weline-backend-page">
    <div class="page-head">
        <h1><?= __('Sales Report') ?></h1>
        <p>
            <?= __('Reporting window:') ?>
            <strong><?= htmlspecialchars($startDate) ?></strong>
            —
            <strong><?= htmlspecialchars($endDate) ?></strong>
        </p>
    </div>

    <div class="report-grid">
        <div class="report-card">
            <p class="report-label"><?= __('Total Sales') ?></p>
            <p class="report-value"><?= number_format((float)($report['total_sales'] ?? 0), 2) ?></p>
            <p class="report-sub"><?= __('Completed orders value in the window') ?></p>
        </div>
        <div class="report-card">
            <p class="report-label"><?= __('Order Count') ?></p>
            <p class="report-value"><?= (int)($report['order_count'] ?? 0) ?></p>
            <p class="report-sub"><?= __('Number of completed orders counted') ?></p>
        </div>
        <div class="report-card">
            <p class="report-label"><?= __('Average Order') ?></p>
            <p class="report-value"><?= number_format((float)($report['average_order'] ?? 0), 2) ?></p>
            <p class="report-sub"><?= __('Average completed order value') ?></p>
        </div>
    </div>
</div>
