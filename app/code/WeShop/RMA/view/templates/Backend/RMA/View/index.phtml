<?php
/**
 * WeShop RMA Backend - RMA View Detail Template
 * @var \Weline\Framework\View\Template $this
 */
use WeShop\RMA\Service\RmaService;

$rma = (array) ($this->getData('rma') ?? []);
$rmaIndexUrl = (string) ($this->getData('rmaIndexUrl') ?? '');
$rmaApproveUrl = (string) ($this->getData('rmaApproveUrl') ?? '');
$rmaRejectUrl = (string) ($this->getData('rmaRejectUrl') ?? '');

$statusBadgeClassMap = [
    RmaService::STATUS_PENDING => 'bg-warning',
    RmaService::STATUS_APPROVED => 'bg-success',
    RmaService::STATUS_REJECTED => 'bg-danger',
];

$statusLabelMap = [
    RmaService::STATUS_PENDING => __('待处理'),
    RmaService::STATUS_APPROVED => __('已同意'),
    RmaService::STATUS_REJECTED => __('已拒绝'),
];

$rmaId = (int) ($rma['rma_id'] ?? 0);
$orderId = (int) ($rma['order_id'] ?? 0);
$customerId = (int) ($rma['customer_id'] ?? 0);
$reason = htmlspecialchars((string) ($rma['reason'] ?? ''));
$description = htmlspecialchars((string) ($rma['description'] ?? ''));
$status = (string) ($rma['status'] ?? RmaService::STATUS_PENDING);
$statusLabel = (string) ($rma['status_label'] ?? ($statusLabelMap[$status] ?? ucfirst($status)));
$badgeClass = $statusBadgeClassMap[$status] ?? 'bg-secondary';
$createdAt = htmlspecialchars((string) ($rma['created_at'] ?? ''));
$updatedAt = htmlspecialchars((string) ($rma['updated_at'] ?? ''));
?>
<style>
    .weshop-rma-detail-card {
        border-left: 4px solid var(--backend-color-primary);
    }
    .rma-detail-label {
        color: var(--backend-color-text-secondary);
        font-size: 0.875rem;
    }
    .rma-detail-value {
        color: var(--backend-color-text-primary);
        font-weight: 500;
    }
    .rma-status-badge {
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 0.875rem;
        font-weight: 600;
    }
    .rma-reason-box {
        background-color: var(--backend-color-bg-secondary);
        border-radius: 8px;
        padding: 12px 16px;
    }
    .rma-description-box {
        background-color: var(--backend-color-bg-secondary);
        border-radius: 8px;
        padding: 12px 16px;
        white-space: pre-wrap;
    }
</style>

<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0">
                <lang>售后详情</lang>
            </h4>

            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="javascript: void(0);">
                            <lang>WeShop</lang>
                        </a></li>
                    <li class="breadcrumb-item"><a href="<?= htmlspecialchars($rmaIndexUrl) ?>">
                            <lang>售后管理</lang>
                        </a></li>
                    <li class="breadcrumb-item active">
                        <lang>详情</lang>
                    </li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- start page message -->
<div class="row">
    <template>Weline_Component::message.phtml</template>
</div>
<!-- end message -->

<!-- Action Buttons -->
<div class="row mb-4">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <span class="badge <?= $badgeClass ?> fs-6">
                    <?= $statusLabel ?>
                </span>
                <span class="ms-2 text-muted">
                    <lang>售后单 ID</lang>: #<?= $rmaId ?>
                </span>
            </div>
            <div class="d-flex gap-2">
                <a href="<?= htmlspecialchars($rmaIndexUrl) ?>" class="btn btn-secondary">
                    <i class="mdi mdi-arrow-left me-1"></i><?= __('返回列表') ?>
                </a>
                <?php if ($status === RmaService::STATUS_PENDING): ?>
                    <button class="btn btn-success"
                            w-confirm="<?= __('确认批准此售后申请？') ?>"
                            w-delete="<?= htmlspecialchars($rmaApproveUrl) ?>"
                            w-params='{"rma_id": <?= $rmaId ?>}'
                            w-method="post">
                        <i class="mdi mdi-check me-1"></i><?= __('批准') ?>
                    </button>
                    <button class="btn btn-danger"
                            w-confirm="<?= __('确认拒绝此售后申请？') ?>"
                            w-delete="<?= htmlspecialchars($rmaRejectUrl) ?>"
                            w-params='{"rma_id": <?= $rmaId ?>}'
                            w-method="post">
                        <i class="mdi mdi-close me-1"></i><?= __('拒绝') ?>
                    </button>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <!-- Main Content -->
    <div class="col-lg-8">
        <!-- RMA Information -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="mdi mdi-information me-2"></i><?= __('售后信息') ?>
                </h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label rma-detail-label"><?= __('售后单 ID') ?></label>
                            <div class="rma-detail-value">#<?= $rmaId ?></div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label rma-detail-label"><?= __('订单号') ?></label>
                            <div class="rma-detail-value">
                                <a href="<?= $this->getBackendUrl('*/backend/order/view') ?>?id=<?= $orderId ?>">
                                    #<?= $orderId ?>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label rma-detail-label"><?= __('客户 ID') ?></label>
                            <div class="rma-detail-value">
                                <a href="<?= $this->getBackendUrl('*/backend/customer/edit') ?>?id=<?= $customerId ?>">
                                    #<?= $customerId ?>
                                </a>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label rma-detail-label"><?= __('状态') ?></label>
                            <div class="rma-detail-value">
                                <span class="rma-status-badge <?= $badgeClass ?>">
                                    <?= $statusLabel ?>
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label rma-detail-label"><?= __('创建时间') ?></label>
                            <div class="rma-detail-value"><?= $createdAt ?></div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label rma-detail-label"><?= __('更新时间') ?></label>
                            <div class="rma-detail-value"><?= $updatedAt ?></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Reason & Description -->
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="mdi mdi-text-box me-2"></i><?= __('原因与说明') ?>
                </h5>
            </div>
            <div class="card-body">
                <div class="mb-4">
                    <label class="form-label rma-detail-label"><?= __('原因') ?></label>
                    <div class="rma-reason-box">
                        <?= $reason ?>
                    </div>
                </div>
                <?php if (!empty($description)): ?>
                    <div>
                        <label class="form-label rma-detail-label"><?= __('说明') ?></label>
                        <div class="rma-description-box">
                            <?= $description ?>
                        </div>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>

    <!-- Sidebar - Timeline -->
    <div class="col-lg-4">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="mdi mdi-history me-2"></i><?= __('操作记录') ?>
                </h5>
            </div>
            <div class="card-body">
                <ul class="list-unstyled mb-0">
                    <li class="mb-3">
                        <div class="d-flex align-items-start">
                            <div class="flex-shrink-0 me-2">
                                <span class="badge bg-primary rounded-circle" style="width: 8px; height: 8px; margin-top: 6px;"></span>
                            </div>
                            <div>
                                <div class="fw-semibold"><?= __('售后申请已提交') ?></div>
                                <div class="small text-muted"><?= $createdAt ?></div>
                            </div>
                        </div>
                    </li>
                    <?php if ($status !== RmaService::STATUS_PENDING): ?>
                        <li class="mb-3">
                            <div class="d-flex align-items-start">
                                <div class="flex-shrink-0 me-2">
                                    <span class="badge <?= $badgeClass ?> rounded-circle" style="width: 8px; height: 8px; margin-top: 6px;"></span>
                                </div>
                                <div>
                                    <div class="fw-semibold">
                                        <?= $status === RmaService::STATUS_APPROVED ? __('售后已批准') : __('售后已拒绝') ?>
                                    </div>
                                    <div class="small text-muted"><?= $updatedAt ?></div>
                                </div>
                            </div>
                        </li>
                    <?php endif; ?>
                </ul>
            </div>
        </div>

        <!-- Quick Actions -->
        <?php if ($status === RmaService::STATUS_PENDING): ?>
            <div class="card mt-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="mdi mdi-lightning-bolt me-2"></i><?= __('快捷操作') ?>
                    </h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <button class="btn btn-success"
                                w-confirm="<?= __('确认批准此售后申请？') ?>"
                                w-delete="<?= htmlspecialchars($rmaApproveUrl) ?>"
                                w-params='{"rma_id": <?= $rmaId ?>}'
                                w-method="post">
                            <i class="mdi mdi-check me-1"></i><?= __('批准售后') ?>
                        </button>
                        <button class="btn btn-danger"
                                w-confirm="<?= __('确认拒绝此售后申请？') ?>"
                                w-delete="<?= htmlspecialchars($rmaRejectUrl) ?>"
                                w-params='{"rma_id": <?= $rmaId ?>}'
                                w-method="post">
                            <i class="mdi mdi-close me-1"></i><?= __('拒绝售后') ?>
                        </button>
                    </div>
                </div>
            </div>
        <?php endif; ?>
    </div>
</div>

<js:part name="w-delete"/>
