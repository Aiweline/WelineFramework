<?php
/**
 * WeShop Default Theme - Order Success Page
 * 订单确认成功页模板
 */
$order = $this->getData('order') ?? [];
$orderItems = $this->getData('order_items') ?? [];
$shippingAddress = $this->getData('shipping_address') ?? [];
$paymentMethod = $this->getData('payment_method') ?? [];
$recommendations = $this->getData('recommendations') ?? [];
?>
<main class="flex-1 w-full max-w-[1000px] mx-auto p-4 md:p-6 lg:p-8">
    <w:hook name="WeShop_Checkout::success::before"/>
    
    <!-- Success Header -->
    <div class="text-center mb-12">
        <div class="flex justify-center mb-6">
            <div class="relative">
                <div class="w-24 h-24 bg-green-100 dark:bg-green-900/30 rounded-full flex items-center justify-center animate-scale-up">
                    <span class="material-symbols-outlined text-5xl text-green-600 dark:text-green-400" style="font-variation-settings: 'FILL' 1;">check_circle</span>
                </div>
                <div class="absolute -bottom-2 -right-2 bg-primary text-white rounded-full p-2">
                    <span class="material-symbols-outlined text-xl">local_shipping</span>
                </div>
            </div>
        </div>
        <h1 class="text-3xl font-bold text-text-primary-light dark:text-text-primary-dark mb-2"><?= __('Order Confirmed!') ?></h1>
        <p class="text-lg text-text-secondary-light dark:text-text-secondary-dark mb-4"><?= __('Thank you for your purchase') ?></p>
        <p class="text-text-secondary-light dark:text-text-secondary-dark">
            <?= __('Order Number:') ?> <span class="font-bold text-text-primary-light dark:text-text-primary-dark">#<?= htmlspecialchars($order['increment_id'] ?? '') ?></span>
        </p>
    </div>
    
    <!-- Order Details Card -->
    <div class="bg-surface-light dark:bg-surface-dark rounded-xl shadow-lg border border-border-light dark:border-border-dark overflow-hidden mb-8">
        <!-- Order Summary Header -->
        <div class="bg-primary/5 border-b border-border-light dark:border-border-dark p-6">
            <div class="flex flex-col md:flex-row md:items-center justify-between gap-4">
                <div>
                    <p class="text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Order placed on') ?></p>
                    <p class="font-bold text-text-primary-light dark:text-text-primary-dark"><?= date('F j, Y', strtotime($order['created_at'] ?? 'now')) ?></p>
                </div>
                <div class="text-right">
                    <p class="text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Total') ?></p>
                    <p class="text-2xl font-bold text-primary"><?= $this->formatPrice($order['grand_total'] ?? 0) ?></p>
                </div>
            </div>
        </div>
        
        <!-- Order Items -->
        <div class="p-6 border-b border-border-light dark:border-border-dark">
            <h2 class="text-lg font-bold text-text-primary-light dark:text-text-primary-dark mb-4"><?= __('Order Items') ?></h2>
            <div class="space-y-4">
                <?php foreach ($orderItems as $item): ?>
                    <div class="flex gap-4">
                        <div class="w-16 h-16 rounded-lg bg-background-light dark:bg-background-dark overflow-hidden">
                            <img src="<?= $item['image'] ?? '' ?>" alt="<?= htmlspecialchars($item['name']) ?>" class="w-full h-full object-cover">
                        </div>
                        <div class="flex-1">
                            <p class="font-medium text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars($item['name']) ?></p>
                            <p class="text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Qty: %1', $item['qty']) ?></p>
                        </div>
                        <div class="text-right">
                            <p class="font-bold text-text-primary-light dark:text-text-primary-dark"><?= $this->formatPrice($item['row_total']) ?></p>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        </div>
        
        <!-- Shipping & Payment Info -->
        <div class="p-6 grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Shipping Address -->
            <div>
                <h3 class="text-sm font-bold text-text-primary-light dark:text-text-primary-dark mb-3 flex items-center gap-2">
                    <span class="material-symbols-outlined text-lg">location_on</span>
                    <?= __('Shipping Address') ?>
                </h3>
                <div class="text-sm text-text-secondary-light dark:text-text-secondary-dark space-y-1">
                    <p class="font-medium text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars($shippingAddress['name'] ?? '') ?></p>
                    <p><?= htmlspecialchars($shippingAddress['street'] ?? '') ?></p>
                    <p><?= htmlspecialchars($shippingAddress['city'] ?? '') ?>, <?= htmlspecialchars($shippingAddress['region'] ?? '') ?> <?= htmlspecialchars($shippingAddress['postcode'] ?? '') ?></p>
                    <p><?= htmlspecialchars($shippingAddress['country'] ?? '') ?></p>
                </div>
            </div>
            
            <!-- Payment Method -->
            <div>
                <h3 class="text-sm font-bold text-text-primary-light dark:text-text-primary-dark mb-3 flex items-center gap-2">
                    <span class="material-symbols-outlined text-lg">payment</span>
                    <?= __('Payment Method') ?>
                </h3>
                <div class="text-sm text-text-secondary-light dark:text-text-secondary-dark">
                    <p class="font-medium text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars($paymentMethod['name'] ?? '') ?></p>
                    <?php if ($paymentMethod['card_last4'] ?? null): ?>
                        <p><?= __('Card ending in %1', $paymentMethod['card_last4']) ?></p>
                    <?php endif; ?>
                </div>
            </div>
        </div>
        
        <!-- Order Summary -->
        <div class="p-6 bg-background-light dark:bg-background-dark">
            <div class="space-y-2 text-sm">
                <div class="flex justify-between">
                    <span class="text-text-secondary-light dark:text-text-secondary-dark"><?= __('Subtotal') ?></span>
                    <span class="font-medium text-text-primary-light dark:text-text-primary-dark"><?= $this->formatPrice($order['subtotal'] ?? 0) ?></span>
                </div>
                <div class="flex justify-between">
                    <span class="text-text-secondary-light dark:text-text-secondary-dark"><?= __('Shipping') ?></span>
                    <span class="font-medium text-text-primary-light dark:text-text-primary-dark"><?= $this->formatPrice($order['shipping_amount'] ?? 0) ?></span>
                </div>
                <?php if (($order['discount_amount'] ?? 0) > 0): ?>
                    <div class="flex justify-between text-green-600">
                        <span><?= __('Discount') ?></span>
                        <span class="font-medium">-<?= $this->formatPrice($order['discount_amount']) ?></span>
                    </div>
                <?php endif; ?>
                <div class="flex justify-between">
                    <span class="text-text-secondary-light dark:text-text-secondary-dark"><?= __('Tax') ?></span>
                    <span class="font-medium text-text-primary-light dark:text-text-primary-dark"><?= $this->formatPrice($order['tax_amount'] ?? 0) ?></span>
                </div>
                <div class="flex justify-between pt-2 border-t border-border-light dark:border-border-dark text-lg">
                    <span class="font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Total') ?></span>
                    <span class="font-bold text-primary"><?= $this->formatPrice($order['grand_total'] ?? 0) ?></span>
                </div>
            </div>
        </div>
    </div>
    
    <!-- What's Next -->
    <div class="bg-surface-light dark:bg-surface-dark rounded-xl shadow-lg border border-border-light dark:border-border-dark p-6 mb-8">
        <h2 class="text-lg font-bold text-text-primary-light dark:text-text-primary-dark mb-4"><?= __("What's Next?") ?></h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <div class="flex gap-4">
                <div class="w-12 h-12 bg-primary/10 rounded-lg flex items-center justify-center shrink-0">
                    <span class="material-symbols-outlined text-2xl text-primary">mail</span>
                </div>
                <div>
                    <h3 class="font-medium text-text-primary-light dark:text-text-primary-dark"><?= __('Confirmation Email') ?></h3>
                    <p class="text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('A confirmation email has been sent to your address.') ?></p>
                </div>
            </div>
            <div class="flex gap-4">
                <div class="w-12 h-12 bg-primary/10 rounded-lg flex items-center justify-center shrink-0">
                    <span class="material-symbols-outlined text-2xl text-primary">inventory_2</span>
                </div>
                <div>
                    <h3 class="font-medium text-text-primary-light dark:text-text-primary-dark"><?= __('Processing') ?></h3>
                    <p class="text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __('Your order is being processed and will ship soon.') ?></p>
                </div>
            </div>
            <div class="flex gap-4">
                <div class="w-12 h-12 bg-primary/10 rounded-lg flex items-center justify-center shrink-0">
                    <span class="material-symbols-outlined text-2xl text-primary">local_shipping</span>
                </div>
                <div>
                    <h3 class="font-medium text-text-primary-light dark:text-text-primary-dark"><?= __('Tracking') ?></h3>
                    <p class="text-sm text-text-secondary-light dark:text-text-secondary-dark"><?= __("You'll receive tracking info when your order ships.") ?></p>
                </div>
            </div>
        </div>
    </div>

    <w:hook name="WeShop_Checkout::success::recommendations_before"/>
    <?php if (!empty($recommendations)): ?>
        <div class="bg-surface-light dark:bg-surface-dark rounded-xl shadow-lg border border-border-light dark:border-border-dark p-6 mb-8">
            <h2 class="text-lg font-bold text-text-primary-light dark:text-text-primary-dark mb-6"><?= __('Customers who bought this also bought') ?></h2>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                <?php foreach ($recommendations as $product): ?>
                    <a href="<?= $this->getUrl('product/view', ['id' => $product['product_id']]) ?>" class="group flex flex-col gap-3 rounded-xl border border-border-light dark:border-border-dark bg-background-light dark:bg-background-dark p-3 hover:border-primary/40 hover:shadow-md transition-all">
                        <div class="aspect-square rounded-lg overflow-hidden bg-surface-light dark:bg-surface-dark">
                            <img src="<?= $product['image'] ?? '' ?>" alt="<?= htmlspecialchars($product['name'] ?? '') ?>" class="w-full h-full object-cover group-hover:scale-[1.02] transition-transform">
                        </div>
                        <div class="space-y-1">
                            <p class="font-medium text-sm text-text-primary-light dark:text-text-primary-dark line-clamp-2"><?= htmlspecialchars($product['name'] ?? '') ?></p>
                            <p class="text-primary font-bold"><?= $this->formatPrice($product['price'] ?? 0) ?></p>
                        </div>
                    </a>
                <?php endforeach; ?>
            </div>
        </div>
    <?php endif; ?>
    <w:hook name="WeShop_Checkout::success::recommendations_after"/>
    
    <!-- Actions -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="<?= $this->getUrl('weshop/order/list') ?>" class="flex items-center justify-center gap-2 px-6 py-3 bg-primary text-white font-bold rounded-lg hover:bg-primary/90 transition-colors">
            <span class="material-symbols-outlined">receipt_long</span>
            <?= __('View Order Details') ?>
        </a>
        <a href="<?= $this->getUrl('weshop') ?>" class="flex items-center justify-center gap-2 px-6 py-3 border border-border-light dark:border-border-dark text-text-primary-light dark:text-text-primary-dark font-bold rounded-lg hover:bg-background-light dark:hover:bg-background-dark transition-colors">
            <span class="material-symbols-outlined">shopping_bag</span>
            <?= __('Continue Shopping') ?>
        </a>
    </div>
    
    <w:hook name="WeShop_Checkout::success::after"/>
</main>

<style>
@keyframes scale-up {
    0% {
        transform: scale(0);
        opacity: 0;
    }
    50% {
        transform: scale(1.1);
    }
    100% {
        transform: scale(1);
        opacity: 1;
    }
}
.animate-scale-up {
    animation: scale-up 0.5s ease-out;
}
</style>
