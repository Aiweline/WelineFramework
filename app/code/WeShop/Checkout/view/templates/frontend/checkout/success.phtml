<?php
/**
 * WeShop default checkout success page.
 */
$order = $this->getData('order') ?? [];
$orderItems = $this->getData('order_items') ?? [];
$shippingAddress = $this->getData('shipping_address') ?? [];
$paymentMethod = $this->getData('payment_method') ?? [];
$recommendations = $this->getData('recommendations') ?? [];

$formatSuccessPrice = static function (float $amount): string {
    return \Weline\Currency\Helper\CurrencyFormatter::formatConverted($amount);
};

$createdAt = (string) ($order['created_at'] ?? '');
$placedAtTs = $createdAt !== '' ? strtotime($createdAt) : time();
$placedAt = $placedAtTs !== false
    ? __('%{1}年%{2}月%{3}日', [(int) date('Y', $placedAtTs), (int) date('n', $placedAtTs), (int) date('j', $placedAtTs)])
    : '';
$orderNumber = (string) ($order['increment_id'] ?? '');
?>
<main class="weshop-success-page">
    <w:hook name="WeShop_Checkout::frontend::layouts::checkout-success::before"/>

    <section class="weshop-success-hero">
        <div class="weshop-success-mark" aria-hidden="true">
            <span class="material-symbols-outlined">check_circle</span>
        </div>
        <p class="weshop-success-eyebrow"><?= __('订单提交成功！') ?></p>
        <h1><?= __('订单已确认！') ?></h1>
        <p class="weshop-success-lead"><?= __('感谢购买，我们已收到你的订单，并会持续更新处理进度。') ?></p>
        <div class="weshop-success-meta">
            <span><?= __('订单号：') ?> <strong>#<?= htmlspecialchars($orderNumber) ?></strong></span>
            <span><?= __('下单时间') ?> <strong><?= htmlspecialchars($placedAt) ?></strong></span>
            <span><?= __('合计') ?> <strong><?= $formatSuccessPrice($order['grand_total'] ?? 0) ?></strong></span>
        </div>
    </section>

    <div class="weshop-success-layout">
        <div class="weshop-success-main">
            <section class="weshop-success-card">
                <div class="weshop-success-card-head">
                    <div>
                        <p class="weshop-success-kicker"><?= __('订单详情') ?></p>
                        <h2><?= __('商品明细') ?></h2>
                    </div>
                    <strong><?= $formatSuccessPrice($order['grand_total'] ?? 0) ?></strong>
                </div>

                <div class="weshop-success-items">
                    <?php foreach ($orderItems as $item): ?>
                        <?php
                        $itemName = (string) ($item['name'] ?? '');
                        $itemImage = (string) ($item['image'] ?? '');
                        $itemOptions = is_array($item['options'] ?? null) ? $item['options'] : [];
                        ?>
                        <article class="weshop-success-item">
                            <div class="weshop-success-item-media">
                                <?php if ($itemImage !== ''): ?>
                                    <img src="<?= htmlspecialchars($itemImage) ?>" alt="<?= htmlspecialchars($itemName) ?>">
                                <?php else: ?>
                                    <span><?= htmlspecialchars(mb_substr($itemName, 0, 1) ?: 'W') ?></span>
                                <?php endif; ?>
                            </div>
                            <div>
                                <h3><?= htmlspecialchars($itemName) ?></h3>
                                <?php if ($itemOptions !== []): ?>
                                    <div class="weshop-success-item-options">
                                        <?= \WeShop\Product\Helper\ProductOptionRenderer::renderItems($itemOptions) ?>
                                    </div>
                                <?php endif; ?>
                                <p><?= __('数量：%{1}', $item['qty'] ?? 1) ?></p>
                            </div>
                            <strong><?= $formatSuccessPrice($item['row_total'] ?? 0) ?></strong>
                        </article>
                    <?php endforeach; ?>
                </div>
            </section>

            <section class="weshop-success-info-grid">
                <article class="weshop-success-card weshop-success-info">
                    <span class="material-symbols-outlined">location_on</span>
                    <div>
                        <h2><?= __('收货地址') ?></h2>
                        <p><strong><?= htmlspecialchars($shippingAddress['name'] ?? '') ?></strong></p>
                        <p><?= htmlspecialchars($shippingAddress['street'] ?? '') ?></p>
                        <p><?= htmlspecialchars($shippingAddress['city'] ?? '') ?>, <?= htmlspecialchars($shippingAddress['region'] ?? '') ?> <?= htmlspecialchars($shippingAddress['postcode'] ?? '') ?></p>
                        <p><?= htmlspecialchars($shippingAddress['country'] ?? '') ?></p>
                    </div>
                </article>

                <article class="weshop-success-card weshop-success-info">
                    <span class="material-symbols-outlined">payments</span>
                    <div>
                        <h2><?= __('支付方式') ?></h2>
                        <p><strong><?= htmlspecialchars($paymentMethod['name'] ?? '') ?></strong></p>
                        <?php if (!empty($paymentMethod['description'])): ?>
                            <p><?= htmlspecialchars($paymentMethod['description']) ?></p>
                        <?php endif; ?>
                        <?php if ($paymentMethod['card_last4'] ?? null): ?>
                            <p><?= __('尾号 %{1} 的银行卡', $paymentMethod['card_last4']) ?></p>
                        <?php endif; ?>
                    </div>
                </article>
            </section>

            <section class="weshop-success-card">
                <div class="weshop-success-card-head">
                    <div>
                        <p class="weshop-success-kicker"><?= __('后续流程') ?></p>
                        <h2><?= __('接下来会发生什么？') ?></h2>
                    </div>
                </div>
                <div class="weshop-success-steps">
                    <article>
                        <span class="material-symbols-outlined">mail</span>
                        <h3><?= __('确认邮件') ?></h3>
                        <p><?= __('确认邮件已发送到你的邮箱。') ?></p>
                    </article>
                    <article>
                        <span class="material-symbols-outlined">inventory_2</span>
                        <h3><?= __('处理中') ?></h3>
                        <p><?= __('订单正在处理，很快会安排发货。') ?></p>
                    </article>
                    <article>
                        <span class="material-symbols-outlined">local_shipping</span>
                        <h3><?= __('物流追踪') ?></h3>
                        <p><?= __('订单发货后你会收到物流信息。') ?></p>
                    </article>
                </div>
            </section>

            <w:hook name="WeShop_Checkout::frontend::layouts::checkout-success::recommendations-before"/>
            <?php if (!empty($recommendations)): ?>
                <section class="weshop-success-card">
                    <div class="weshop-success-card-head">
                        <div>
                            <p class="weshop-success-kicker"><?= __('猜你喜欢') ?></p>
                            <h2><?= __('购买该商品的顾客还购买了') ?></h2>
                        </div>
                    </div>
                    <div class="weshop-success-recommendations">
                        <?php foreach ($recommendations as $product): ?>
                            <?php
                            $productName = (string) ($product['name'] ?? '');
                            $productImage = (string) ($product['image'] ?? '');
                            ?>
                            <a href="<?= $this->getUrl('product/view', ['id' => $product['product_id']]) ?>" class="weshop-success-product">
                                <span class="weshop-success-product-media">
                                    <?php if ($productImage !== ''): ?>
                                        <img src="<?= htmlspecialchars($productImage) ?>" alt="<?= htmlspecialchars($productName) ?>">
                                    <?php else: ?>
                                        <span><?= htmlspecialchars(mb_substr($productName, 0, 1) ?: 'W') ?></span>
                                    <?php endif; ?>
                                </span>
                                <strong><?= htmlspecialchars($productName) ?></strong>
                                <em><?= $formatSuccessPrice($product['price'] ?? 0) ?></em>
                            </a>
                        <?php endforeach; ?>
                    </div>
                </section>
            <?php endif; ?>
            <w:hook name="WeShop_Checkout::frontend::layouts::checkout-success::recommendations-after"/>
        </div>

        <aside class="weshop-success-summary" aria-label="<?= __('订单摘要') ?>">
            <h2><?= __('订单摘要') ?></h2>
            <div class="weshop-success-summary-row">
                <span><?= __('小计') ?></span>
                <strong><?= $formatSuccessPrice($order['subtotal'] ?? 0) ?></strong>
            </div>
            <div class="weshop-success-summary-row">
                <span><?= __('运费') ?></span>
                <strong><?= $formatSuccessPrice($order['shipping_amount'] ?? 0) ?></strong>
            </div>
            <?php if (($order['discount_amount'] ?? 0) > 0): ?>
                <div class="weshop-success-summary-row weshop-success-summary-discount">
                    <span><?= __('优惠') ?></span>
                    <strong>-<?= $formatSuccessPrice($order['discount_amount']) ?></strong>
                </div>
            <?php endif; ?>
            <div class="weshop-success-summary-row">
                <span><?= __('税费') ?></span>
                <strong><?= $formatSuccessPrice($order['tax_amount'] ?? 0) ?></strong>
            </div>
            <div class="weshop-success-summary-total">
                <span><?= __('合计') ?></span>
                <strong><?= $formatSuccessPrice($order['grand_total'] ?? 0) ?></strong>
            </div>
            <div class="weshop-success-actions">
                <a href="<?= $this->getUrl('weshop/order/list') ?>"><?= __('查看订单详情') ?></a>
                <a href="<?= $this->getUrl('weshop') ?>"><?= __('继续购物') ?></a>
            </div>
        </aside>
    </div>

    <w:hook name="WeShop_Checkout::frontend::layouts::checkout-success::after"/>
</main>

<style>
.weshop-success-page {
    --ws-primary: var(--weshop-color-primary, var(--color-primary, #f7c441));
    --ws-primary-strong: var(--weshop-color-primary-strong, #d99d00);
    --ws-text: var(--weshop-color-text, var(--color-text, #111827));
    --ws-muted: var(--weshop-color-muted, #6b7280);
    --ws-border: var(--weshop-color-border, #e5e7eb);
    --ws-surface: var(--weshop-color-surface, #ffffff);
    --ws-soft: var(--weshop-color-soft, #f6f7f9);
    width: min(100% - 2rem, var(--layout-max-width, 1200px));
    margin: 1.5rem auto 4rem;
    color: var(--ws-text);
}

.checkout-success-content > .success-title {
    display: none !important;
}

.weshop-success-page * {
    box-sizing: border-box;
}

.weshop-success-hero,
.weshop-success-card,
.weshop-success-summary {
    background: var(--ws-surface);
    border: 1px solid var(--ws-border);
    border-radius: 24px;
    box-shadow: 0 20px 50px rgba(17, 24, 39, .08);
}

.weshop-success-hero {
    display: grid;
    gap: .75rem;
    justify-items: center;
    padding: 2.5rem;
    text-align: center;
    background:
        radial-gradient(circle at 50% 0%, rgba(247, 196, 65, .24), transparent 34rem),
        var(--ws-surface);
}

.weshop-success-mark {
    display: grid;
    width: 76px;
    height: 76px;
    place-items: center;
    color: #0f7b4f;
    background: #dff8ea;
    border-radius: 50%;
    box-shadow: inset 0 0 0 8px #f2fff7;
}

.weshop-success-mark .material-symbols-outlined {
    display: grid;
    place-items: center;
    font-size: 0;
    font-variation-settings: 'FILL' 1;
}

.weshop-success-mark .material-symbols-outlined::before {
    content: "";
    display: block;
    width: 18px;
    height: 34px;
    border-right: 5px solid currentColor;
    border-bottom: 5px solid currentColor;
    transform: rotate(45deg) translate(-2px, -4px);
}

.weshop-success-eyebrow,
.weshop-success-kicker {
    margin: 0;
    color: var(--ws-primary-strong);
    font-size: .8rem;
    font-weight: 800;
    letter-spacing: .08em;
    text-transform: uppercase;
}

.weshop-success-hero h1,
.weshop-success-card h2,
.weshop-success-summary h2 {
    margin: 0;
    color: var(--ws-text);
    line-height: 1.15;
}

.weshop-success-hero h1 {
    font-size: clamp(2rem, 4vw, 3.5rem);
}

.weshop-success-lead {
    max-width: 660px;
    margin: 0;
    color: var(--ws-muted);
    font-size: 1rem;
}

.weshop-success-meta {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: .75rem;
    margin-top: .75rem;
}

.weshop-success-meta span {
    display: inline-flex;
    gap: .35rem;
    align-items: center;
    padding: .7rem 1rem;
    color: var(--ws-muted);
    background: rgba(255, 255, 255, .76);
    border: 1px solid var(--ws-border);
    border-radius: 999px;
}

.weshop-success-meta strong {
    color: var(--ws-text);
}

.weshop-success-layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 360px;
    gap: 1.5rem;
    margin-top: 1.5rem;
    align-items: start;
}

.weshop-success-main {
    display: grid;
    gap: 1.5rem;
}

.weshop-success-card,
.weshop-success-summary {
    padding: 1.5rem;
}

.weshop-success-card-head {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    align-items: flex-start;
    margin-bottom: 1rem;
}

.weshop-success-card-head > strong {
    color: var(--ws-text);
    font-size: 1.35rem;
}

.weshop-success-items {
    display: grid;
    gap: .85rem;
}

.weshop-success-item {
    display: grid;
    grid-template-columns: 76px minmax(0, 1fr) auto;
    gap: 1rem;
    align-items: center;
    padding: .9rem;
    background: var(--ws-soft);
    border-radius: 18px;
}

.weshop-success-item-media,
.weshop-success-product-media {
    display: grid;
    place-items: center;
    overflow: hidden;
    background: #fff;
    border: 1px solid var(--ws-border);
}

.weshop-success-item-media {
    width: 76px;
    height: 76px;
    border-radius: 16px;
}

.weshop-success-item-media img,
.weshop-success-product-media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.weshop-success-item h3,
.weshop-success-steps h3,
.weshop-success-product strong {
    margin: 0;
    color: var(--ws-text);
    font-size: .98rem;
}

.weshop-success-item p,
.weshop-success-info p,
.weshop-success-steps p {
    margin: .25rem 0 0;
    color: var(--ws-muted);
}

.weshop-success-item-options {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: .25rem .6rem;
    margin-top: .3rem;
    color: var(--ws-muted);
    font-size: .82rem;
    line-height: 1.35;
}

.weshop-success-item-options .weshop-product-option {
    display: inline-flex;
    align-items: center;
    gap: .25rem;
    min-width: 0;
}

.weshop-success-item-options .weshop-product-option__label {
    font-weight: 700;
    color: var(--ws-text);
}

.weshop-success-item-options .weshop-product-option__swatch {
    width: .95rem;
    height: .95rem;
    flex: 0 0 .95rem;
    border: 1px solid rgba(17, 24, 39, .18);
    background: #fff;
}

.weshop-success-item-options .weshop-product-option__swatch--color {
    border-radius: 999px;
    box-shadow: inset 0 0 0 2px rgba(255, 255, 255, .62);
}

.weshop-success-item-options .weshop-product-option__swatch--image {
    border-radius: .25rem;
    overflow: hidden;
}

.weshop-success-item-options .weshop-product-option__swatch img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
}

.weshop-success-info-grid,
.weshop-success-steps {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
}

.weshop-success-info {
    display: flex;
    gap: 1rem;
    box-shadow: none;
}

.weshop-success-info > .material-symbols-outlined,
.weshop-success-steps .material-symbols-outlined {
    display: grid;
    flex: 0 0 auto;
    width: 44px;
    height: 44px;
    place-items: center;
    color: var(--ws-primary-strong);
    background: rgba(247, 196, 65, .15);
    border-radius: 14px;
    font-size: 0;
}

.weshop-success-info > .material-symbols-outlined::before,
.weshop-success-steps .material-symbols-outlined::before {
    content: "";
    display: block;
    width: 12px;
    height: 12px;
    background: currentColor;
    border-radius: 999px;
}

.weshop-success-steps {
    grid-template-columns: repeat(3, minmax(0, 1fr));
}

.weshop-success-steps article {
    padding: 1rem;
    background: var(--ws-soft);
    border-radius: 18px;
}

.weshop-success-recommendations {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1rem;
}

.weshop-success-product {
    display: grid;
    gap: .7rem;
    color: inherit;
    text-decoration: none;
}

.weshop-success-product-media {
    aspect-ratio: 1;
    border-radius: 18px;
}

.weshop-success-product em {
    color: var(--ws-primary-strong);
    font-style: normal;
    font-weight: 800;
}

.weshop-success-summary {
    position: sticky;
    top: 1rem;
}

.weshop-success-summary-row,
.weshop-success-summary-total {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    padding: .7rem 0;
    color: var(--ws-muted);
    border-bottom: 1px solid var(--ws-border);
}

.weshop-success-summary-row strong {
    color: var(--ws-text);
}

.weshop-success-summary-discount strong {
    color: #0f7b4f;
}

.weshop-success-summary-total {
    margin-top: .25rem;
    color: var(--ws-text);
    font-size: 1.2rem;
    font-weight: 900;
    border-bottom: 0;
}

.weshop-success-actions {
    display: grid;
    gap: .75rem;
    margin-top: 1rem;
}

.weshop-success-actions a {
    display: inline-flex;
    justify-content: center;
    padding: .9rem 1rem;
    color: var(--ws-text);
    font-weight: 800;
    text-decoration: none;
    background: var(--ws-primary);
    border: 1px solid var(--ws-primary);
    border-radius: 999px;
}

.weshop-success-actions a + a {
    color: var(--ws-text);
    background: #fff;
    border-color: var(--ws-border);
}

@media (max-width: 980px) {
    .weshop-success-layout,
    .weshop-success-info-grid,
    .weshop-success-steps {
        grid-template-columns: 1fr;
    }

    .weshop-success-summary {
        position: static;
    }
}

@media (max-width: 640px) {
    .weshop-success-page {
        width: min(100% - 1rem, var(--layout-max-width, 1200px));
        margin-top: .75rem;
    }

    .weshop-success-hero,
    .weshop-success-card,
    .weshop-success-summary {
        border-radius: 18px;
        padding: 1rem;
    }

    .weshop-success-item {
        grid-template-columns: 60px minmax(0, 1fr);
    }

    .weshop-success-item > strong {
        grid-column: 2;
    }

    .weshop-success-recommendations {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }
}
</style>
