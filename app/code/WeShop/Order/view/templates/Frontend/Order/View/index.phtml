<?php
/**
 * WeShop Order storefront detail template.
 */
$order = (array) ($this->getData('order') ?? []);
$items = (array) ($this->getData('items') ?? []);
$backUrl = (string) ($this->getData('back_url') ?? 'weshop/order/list');
$orderId = (int) ($order['order_id'] ?? 0);
$incrementId = trim((string) ($order['increment_id'] ?? ''));
$statusLabel = trim((string) ($order['status_label'] ?? ucfirst((string) ($order['status'] ?? 'pending'))));
$paymentStatusLabel = trim((string) ($order['payment_status_label'] ?? ucfirst((string) ($order['payment_status'] ?? 'pending'))));
$createdAt = trim((string) ($order['created_at'] ?? ''));
$updatedAt = trim((string) ($order['updated_at'] ?? ''));
$customerId = (int) ($order['customer_id'] ?? 0);
$canRetry = !empty($order['can_retry_payment']);
$canCancel = !empty($order['can_cancel']);
$requireReturn = !empty($order['require_return']);
$cancelReason = trim((string) ($order['cancel_reason'] ?? ''));
?>
<main class="mx-auto flex w-full max-w-7xl flex-1 flex-col gap-8 px-4 py-8 sm:px-6 lg:px-8">
    <w:hook name="WeShop_Order::frontend::pages::order::view-before"/>

    <section class="overflow-hidden rounded-[2rem] border border-border-light bg-surface-light shadow-sm dark:border-border-dark dark:bg-surface-dark">
        <div class="grid gap-0 lg:grid-cols-[1.2fr_0.8fr]">
            <div class="bg-gradient-to-br from-primary/10 via-primary/5 to-transparent p-8 sm:p-10">
                <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary"><?= __('Order Detail') ?></p>
                <h1 class="mt-4 text-3xl font-black tracking-tight text-text-primary-light dark:text-text-primary-dark sm:text-4xl">
                    #<?= htmlspecialchars($incrementId !== '' ? $incrementId : (string) $orderId, ENT_QUOTES) ?>
                </h1>
                <p class="mt-3 max-w-2xl text-base leading-7 text-text-secondary-light dark:text-text-secondary-dark">
                    <?= __('Review the latest status, payment state, and ordered products for this order.') ?>
                </p>
                <div class="mt-8 flex flex-wrap gap-3 text-sm">
                    <a href="<?= $this->getUrl($backUrl) ?>" class="inline-flex items-center justify-center rounded-2xl border border-border-light px-5 py-3 font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark">
                        <?= __('Back to Orders') ?>
                    </a>
                    <?php if ($canRetry): ?>
                        <a href="<?= $this->getUrl('weshop/order/retry-payment', ['order_id' => $orderId]) ?>" class="inline-flex items-center justify-center rounded-2xl bg-primary px-5 py-3 font-semibold text-white transition hover:bg-primary/90">
                            <?= __('Retry Payment') ?>
                        </a>
                    <?php endif; ?>
                </div>
            </div>

            <div class="grid gap-4 p-6 sm:grid-cols-2 lg:grid-cols-1 lg:border-l lg:border-border-light dark:lg:border-border-dark">
                <article class="rounded-3xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                    <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Status') ?></p>
                    <p class="mt-2 text-lg font-bold text-text-primary-light dark:text-text-primary-dark">
                        <?= htmlspecialchars($statusLabel, ENT_QUOTES) ?>
                    </p>
                </article>
                <article class="rounded-3xl border border-border-light bg-background-light p-5 dark:border-border-dark dark:bg-background-dark">
                    <p class="text-sm font-medium text-text-secondary-light dark:text-text-secondary-dark"><?= __('Grand Total') ?></p>
                    <p class="mt-2 text-2xl font-black text-text-primary-light dark:text-text-primary-dark"><?= $this->formatPrice((float) ($order['total'] ?? 0)) ?></p>
                </article>
            </div>
        </div>
    </section>

    <section class="grid gap-6 lg:grid-cols-2">
        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <h2 class="text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Order Information') ?></h2>
            <dl class="mt-5 grid gap-3 text-sm">
                <div class="flex items-center justify-between gap-4">
                    <dt class="text-text-secondary-light dark:text-text-secondary-dark"><?= __('Order ID') ?></dt>
                    <dd class="font-semibold text-text-primary-light dark:text-text-primary-dark"><?= $orderId ?></dd>
                </div>
                <div class="flex items-center justify-between gap-4">
                    <dt class="text-text-secondary-light dark:text-text-secondary-dark"><?= __('Increment ID') ?></dt>
                    <dd class="font-semibold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars($incrementId, ENT_QUOTES) ?></dd>
                </div>
                <div class="flex items-center justify-between gap-4">
                    <dt class="text-text-secondary-light dark:text-text-secondary-dark"><?= __('Customer ID') ?></dt>
                    <dd class="font-semibold text-text-primary-light dark:text-text-primary-dark"><?= $customerId ?></dd>
                </div>
                <div class="flex items-center justify-between gap-4">
                    <dt class="text-text-secondary-light dark:text-text-secondary-dark"><?= __('Payment Status') ?></dt>
                    <dd class="font-semibold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars($paymentStatusLabel, ENT_QUOTES) ?></dd>
                </div>
            </dl>
        </article>

        <article class="rounded-3xl border border-border-light bg-surface-light p-6 shadow-sm dark:border-border-dark dark:bg-surface-dark">
            <h2 class="text-xl font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Timeline') ?></h2>
            <dl class="mt-5 grid gap-3 text-sm">
                <div class="flex items-center justify-between gap-4">
                    <dt class="text-text-secondary-light dark:text-text-secondary-dark"><?= __('Created At') ?></dt>
                    <dd class="font-semibold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars($createdAt !== '' ? $createdAt : (string) __('N/A'), ENT_QUOTES) ?></dd>
                </div>
                <div class="flex items-center justify-between gap-4">
                    <dt class="text-text-secondary-light dark:text-text-secondary-dark"><?= __('Updated At') ?></dt>
                    <dd class="font-semibold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars($updatedAt !== '' ? $updatedAt : (string) __('N/A'), ENT_QUOTES) ?></dd>
                </div>
            </dl>

            <div class="mt-6 flex flex-wrap gap-2">
                <?php if ($canCancel): ?>
                    <form method="post" action="<?= $this->getUrl('weshop/order/cancel') ?>" onsubmit="return confirm('<?= htmlspecialchars((string) __('Cancel this order?'), ENT_QUOTES) ?>');">
                        <?= $this->getFormKey($this->getUrl('weshop/order/cancel')) ?>
                        <input type="hidden" name="order_id" value="<?= $orderId ?>">
                        <button type="submit" class="inline-flex items-center justify-center rounded-2xl border border-rose-300 px-4 py-2 text-sm font-semibold text-rose-700 transition hover:border-rose-500 hover:text-rose-800">
                            <?= __('Cancel Order') ?>
                        </button>
                    </form>
                <?php elseif ($requireReturn): ?>
                    <a href="<?= $this->getUrl('rma/create', ['order_id' => $orderId]) ?>" class="inline-flex items-center justify-center rounded-2xl border border-border-light px-4 py-2 text-sm font-semibold text-text-primary-light transition hover:border-primary hover:text-primary dark:border-border-dark dark:text-text-primary-dark" title="<?= htmlspecialchars($cancelReason, ENT_QUOTES) ?>">
                        <?= __('Request Return') ?>
                    </a>
                <?php elseif ($cancelReason !== ''): ?>
                    <span class="inline-flex items-center justify-center rounded-2xl bg-background-light px-4 py-2 text-xs font-semibold text-text-secondary-light dark:bg-background-dark dark:text-text-secondary-dark" title="<?= htmlspecialchars($cancelReason, ENT_QUOTES) ?>">
                        <?= __('Not Cancellable') ?>
                    </span>
                <?php endif; ?>
            </div>
        </article>
    </section>

    <section class="rounded-3xl border border-dashed border-border-light bg-background-light px-6 py-8 dark:border-border-dark dark:bg-background-dark">
        <h2 class="text-lg font-bold text-text-primary-light dark:text-text-primary-dark"><?= __('Items') ?></h2>

        <?php if ($items === []): ?>
            <p class="mt-2 text-sm leading-6 text-text-secondary-light dark:text-text-secondary-dark">
                <?= __('No order items are available for this order yet.') ?>
            </p>
        <?php else: ?>
            <div class="mt-5 grid gap-4">
                <?php foreach ($items as $item): ?>
                    <article class="rounded-2xl border border-border-light bg-surface-light p-5 dark:border-border-dark dark:bg-surface-dark">
                        <div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between">
                            <div>
                                <h3 class="text-lg font-bold text-text-primary-light dark:text-text-primary-dark"><?= htmlspecialchars((string) ($item['product_name'] ?? ''), ENT_QUOTES) ?></h3>
                                <p class="mt-1 text-sm text-text-secondary-light dark:text-text-secondary-dark">
                                    <?= __('SKU') ?>: <?= htmlspecialchars((string) ($item['product_sku'] ?? ''), ENT_QUOTES) ?>
                                </p>
                            </div>
                            <div class="text-sm text-text-secondary-light dark:text-text-secondary-dark sm:text-right">
                                <p><?= __('Qty') ?>: <span class="font-semibold text-text-primary-light dark:text-text-primary-dark"><?= (int) ($item['quantity'] ?? 0) ?></span></p>
                                <p class="mt-1"><?= __('Price') ?>: <span class="font-semibold text-text-primary-light dark:text-text-primary-dark"><?= $this->formatPrice((float) ($item['price'] ?? 0)) ?></span></p>
                                <p class="mt-1"><?= __('Total') ?>: <span class="font-semibold text-text-primary-light dark:text-text-primary-dark"><?= $this->formatPrice((float) ($item['total'] ?? 0)) ?></span></p>
                            </div>
                        </div>
                    </article>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>

        <w:hook name="WeShop_Order::frontend::pages::order::view-items"/>
    </section>

    <w:hook name="WeShop_Order::frontend::pages::order::view-after"/>
</main>
