<?php
/**
 * @hook-priority 160
 * @hook-sort-order 10
 * @hook-solo false
 */

use Weline\Framework\Session\SessionFactory;

$session = SessionFactory::getInstance()->createFrontendSession();
$isLoggedIn = $session->isLoggedIn();
?>

<?php if ($isLoggedIn): ?>
    <li role="none">
        <a href="@url{'customer/account/index'}#orders" role="menuitem" class="header-account-menu-link-with-badge">
            <span><?= __('我的订单') ?></span>
            <span class="header-account-menu-badge" data-order-unpaid-badge data-order-unpaid-count style="display: none;">0</span>
        </a>
    </li>
    <style>
        .header-account-menu-link-with-badge {
            align-items: center;
            display: flex;
            gap: 0.75rem;
            justify-content: space-between;
        }

        .header-account-menu-badge {
            align-items: center;
            background: #ef4444;
            border-radius: 999px;
            color: #fff;
            display: inline-flex;
            flex-shrink: 0;
            font-size: 0.75rem;
            font-weight: 700;
            height: 1.35rem;
            justify-content: center;
            line-height: 1;
            min-width: 1.35rem;
            padding: 0 0.35rem;
        }
    </style>
<?php endif; ?>
