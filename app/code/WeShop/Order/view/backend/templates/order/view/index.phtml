<?php
/** @var array $order */
$order = $this->getData('order') ?? [];
$items = $this->getData('items') ?? [];
$statusOptions = $this->getData('statusOptions') ?? [];
$orderIndexUrl = (string) ($this->getData('orderIndexUrl') ?? $this->getBackendUrl('*/backend/order'));
$updateStatusUrl = (string) ($this->getData('updateStatusUrl') ?? $this->getBackendUrl('*/backend/order/update-status'));
?>

<div class="container-fluid py-3">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <a href="<?= $orderIndexUrl ?>" class="text-decoration-none small text-muted"><?= __('返回订单列表') ?></a>
            <h1 class="h3 mb-1"><?= __('订单详情') ?> #<?= htmlspecialchars((string) ($order['increment_id'] ?? ''), ENT_QUOTES) ?></h1>
            <p class="text-muted mb-0"><?= __('查看订单数据、商品行与当前状态。') ?></p>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-lg-4">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= __('订单摘要') ?></h2>
                    <dl class="row mb-0">
                        <dt class="col-sm-5"><?= __('订单号') ?></dt>
                        <dd class="col-sm-7">#<?= htmlspecialchars((string) ($order['increment_id'] ?? ''), ENT_QUOTES) ?></dd>

                        <dt class="col-sm-5"><?= __('客户 ID') ?></dt>
                        <dd class="col-sm-7"><?= (int) ($order['customer_id'] ?? 0) ?></dd>

                        <dt class="col-sm-5"><?= __('状态') ?></dt>
                        <dd class="col-sm-7">
                            <span class="badge text-bg-<?= htmlspecialchars((string) ($order['status_badge_class'] ?? 'secondary'), ENT_QUOTES) ?>">
                                <?= htmlspecialchars((string) ($order['status_label'] ?? ''), ENT_QUOTES) ?>
                            </span>
                        </dd>

                        <dt class="col-sm-5"><?= __('支付状态') ?></dt>
                        <dd class="col-sm-7"><?= htmlspecialchars((string) ($order['payment_status'] ?? ''), ENT_QUOTES) ?></dd>

                        <dt class="col-sm-5"><?= __('订单总额') ?></dt>
                        <dd class="col-sm-7"><?= number_format((float) ($order['total'] ?? 0), 2) ?></dd>

                        <dt class="col-sm-5"><?= __('创建时间') ?></dt>
                        <dd class="col-sm-7"><?= htmlspecialchars((string) ($order['created_at'] ?? ''), ENT_QUOTES) ?></dd>
                    </dl>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= __('更新状态') ?></h2>
                    <form method="post" action="<?= $updateStatusUrl ?>" class="d-grid gap-3">
                        <input type="hidden" name="id" value="<?= (int) ($order['order_id'] ?? 0) ?>">
                        <input type="hidden" name="back_url" value="<?= htmlspecialchars((string) $this->getBackendUrl('*/backend/order/view', ['id' => $order['order_id'] ?? 0]), ENT_QUOTES) ?>">
                        <div>
                            <label class="form-label"><?= __('状态') ?></label>
                            <select name="status" class="form-select">
                                <?php foreach ($statusOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>" <?= ($order['status'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <button type="submit" class="btn btn-primary"><?= __('保存状态') ?></button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= __('订单商品') ?></h2>
                    <?php if ($items): ?>
                        <div class="table-responsive">
                            <table class="table align-middle">
                                <thead>
                                <tr>
                                    <th><?= __('商品') ?></th>
                                    <th><?= __('SKU') ?></th>
                                    <th><?= __('数量') ?></th>
                                    <th><?= __('单价') ?></th>
                                    <th><?= __('小计') ?></th>
                                </tr>
                                </thead>
                                <tbody>
                                <?php foreach ($items as $item): ?>
                                    <tr>
                                        <td><?= htmlspecialchars((string) $item['product_name'], ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) $item['product_sku'], ENT_QUOTES) ?></td>
                                        <td><?= (int) $item['quantity'] ?></td>
                                        <td><?= number_format((float) $item['price'], 2) ?></td>
                                        <td><?= number_format((float) $item['total'], 2) ?></td>
                                    </tr>
                                <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>
                    <?php else: ?>
                        <div class="text-center py-4 text-muted"><?= __('暂无订单商品。') ?></div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>
