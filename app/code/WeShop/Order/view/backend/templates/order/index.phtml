<?php
/** @var array $orders */
$orders = $this->getData('orders') ?? [];
$summary = $this->getData('summary') ?? [];
$filters = $this->getData('filters') ?? [];
$statusOptions = $this->getData('statusOptions') ?? [];
$orderIndexUrl = (string) ($this->getData('orderIndexUrl') ?? $this->getBackendUrl('*/backend/order'));
?>

<div class="container-fluid py-3">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <h1 class="h3 mb-1"><?= __('Order Management') ?></h1>
            <p class="text-muted mb-0"><?= __('Manage order search, review, and status transitions from one place.') ?></p>
        </div>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-md-3">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Total Orders') ?></div>
                    <div class="display-6"><?= (int) ($summary['total'] ?? 0) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Pending') ?></div>
                    <div class="display-6"><?= (int) ($summary['pending'] ?? 0) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Processing') ?></div>
                    <div class="display-6"><?= (int) ($summary['processing'] ?? 0) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Completed / Cancelled') ?></div>
                    <div class="display-6"><?= (int) (($summary['completed'] ?? 0) + ($summary['cancelled'] ?? 0)) ?></div>
                </div>
            </div>
        </div>
    </div>

    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <form method="get" action="<?= $orderIndexUrl ?>" class="row g-3 align-items-end">
                <div class="col-md-3">
                    <label class="form-label"><?= __('Order Number') ?></label>
                    <input type="text"
                           name="increment_id"
                           value="<?= htmlspecialchars((string) ($filters['increment_id'] ?? ''), ENT_QUOTES) ?>"
                           class="form-control"
                           placeholder="<?= __('Search by order number') ?>">
                </div>
                <div class="col-md-3">
                    <label class="form-label"><?= __('Customer ID') ?></label>
                    <input type="number"
                           name="customer_id"
                           value="<?= htmlspecialchars((string) ($filters['customer_id'] ?? ''), ENT_QUOTES) ?>"
                           class="form-control"
                           placeholder="<?= __('Search by customer ID') ?>">
                </div>
                <div class="col-md-3">
                    <label class="form-label"><?= __('Status') ?></label>
                    <select name="status" class="form-select">
                        <option value=""><?= __('All statuses') ?></option>
                        <?php foreach ($statusOptions as $code => $label): ?>
                            <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>" <?= ($filters['status'] ?? '') === $code ? 'selected' : '' ?>>
                                <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </div>
                <div class="col-md-3 d-flex gap-2">
                    <button type="submit" class="btn btn-primary w-100"><?= __('Apply Filters') ?></button>
                    <a href="<?= $orderIndexUrl ?>" class="btn btn-outline-secondary"><?= __('Reset') ?></a>
                </div>
            </form>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <?php if ($orders): ?>
                <div class="table-responsive">
                    <table class="table align-middle">
                        <thead>
                        <tr>
                            <th><?= __('Order Number') ?></th>
                            <th><?= __('Customer ID') ?></th>
                            <th><?= __('Status') ?></th>
                            <th><?= __('Payment Status') ?></th>
                            <th><?= __('Total') ?></th>
                            <th><?= __('Created At') ?></th>
                            <th class="text-end"><?= __('Actions') ?></th>
                        </tr>
                        </thead>
                        <tbody>
                        <?php foreach ($orders as $order): ?>
                            <tr>
                                <td>
                                    <a href="<?= $this->getBackendUrl('*/backend/order/view', ['id' => $order['order_id']]) ?>" class="fw-semibold">
                                        #<?= htmlspecialchars((string) $order['increment_id'], ENT_QUOTES) ?>
                                    </a>
                                </td>
                                <td><?= (int) $order['customer_id'] ?></td>
                                <td>
                                    <span class="badge text-bg-<?= htmlspecialchars((string) $order['status_badge_class'], ENT_QUOTES) ?>">
                                        <?= htmlspecialchars((string) $order['status_label'], ENT_QUOTES) ?>
                                    </span>
                                </td>
                                <td><?= htmlspecialchars((string) $order['payment_status'], ENT_QUOTES) ?></td>
                                <td><?= number_format((float) $order['total'], 2) ?></td>
                                <td><?= htmlspecialchars((string) $order['created_at'], ENT_QUOTES) ?></td>
                                <td class="text-end">
                                    <a href="<?= $this->getBackendUrl('*/backend/order/view', ['id' => $order['order_id']]) ?>" class="btn btn-sm btn-outline-primary">
                                        <?= __('View') ?>
                                    </a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            <?php else: ?>
                <div class="text-center py-5">
                    <h2 class="h5 mb-2"><?= __('No orders found.') ?></h2>
                    <p class="text-muted mb-0"><?= __('Try adjusting the filters or create a new order flow from the storefront.') ?></p>
                </div>
            <?php endif; ?>
        </div>
    </div>
</div>
