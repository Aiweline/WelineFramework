<?php
/** @var \Weline\Framework\View\Template $this */
$affiliate = $this->getData('affiliate') ?? [];
$statusOptions = $this->getData('statusOptions') ?? [];
$affiliateIndexUrl = (string) ($this->getData('affiliateIndexUrl') ?? $this->getBackendUrl('*/backend/affiliate'));
$affiliateSaveUrl = (string) ($this->getData('affiliateSaveUrl') ?? $this->getBackendUrl('*/backend/affiliate/save'));
$affiliateId = (int) ($affiliate['affiliate_id'] ?? 0);
$isEditMode = $affiliateId > 0;
$backUrl = $affiliateId > 0
    ? (string) $this->getBackendUrl('*/backend/affiliate/view', ['id' => $affiliateId])
    : $affiliateIndexUrl;
?>

<div class="container-fluid py-3">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <h1 class="h3 mb-1"><?= __('Affiliate Details') ?></h1>
            <p class="text-muted mb-0"><?= __('View and manage affiliate account information.') ?></p>
        </div>
        <div class="d-flex gap-2">
            <a href="<?= $affiliateIndexUrl ?>" class="btn btn-outline-secondary">
                <?= __('Back to List') ?>
            </a>
            <?php if ($affiliateId > 0): ?>
                <a href="<?= $this->getBackendUrl('*/backend/affiliate', ['id' => $affiliateId]) ?>" class="btn btn-outline-primary">
                    <?= __('Edit') ?>
                </a>
            <?php endif; ?>
        </div>
    </div>

    <?php if ($affiliateId <= 0): ?>
        <div class="alert alert-warning">
            <?= __('Affiliate not found.') ?>
        </div>
        <?php return;
    endif; ?>

    <div class="row g-4">
        <div class="col-lg-8">
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-light">
                    <h2 class="h5 mb-0"><?= __('Affiliate Information') ?></h2>
                </div>
                <div class="card-body">
                    <form method="post" action="<?= $affiliateSaveUrl ?>" class="d-grid gap-3">
                        <input type="hidden" name="affiliate_id" value="<?= $affiliateId ?>">
                        <input type="hidden" name="back_url" value="<?= htmlspecialchars($backUrl, ENT_QUOTES) ?>">

                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label"><?= __('Affiliate ID') ?></label>
                                <input type="text" class="form-control" value="<?= $affiliateId ?>" readonly>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label"><?= __('Customer ID') ?></label>
                                <input type="number" name="customer_id" class="form-control"
                                       value="<?= (int) ($affiliate['customer_id'] ?? 0) ?>" readonly>
                            </div>
                        </div>

                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label"><?= __('Referral Code') ?></label>
                                <div class="input-group">
                                    <input type="text" class="form-control"
                                           value="<?= htmlspecialchars((string) ($affiliate['referral_code'] ?? ''), ENT_QUOTES) ?>"
                                           readonly>
                                    <button type="button" class="btn btn-outline-secondary" onclick="copyReferralCode()">
                                        <?= __('Copy') ?>
                                    </button>
                                </div>
                                <small class="text-muted"><?= __('Share this code with partners to track referrals.') ?></small>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label"><?= __('Status') ?></label>
                                <select name="status" class="form-select" <?= $isEditMode ? '' : 'disabled' ?>>
                                    <?php foreach ($statusOptions as $code => $label): ?>
                                        <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>"
                                            <?= ($affiliate['status'] ?? '') === $code ? 'selected' : '' ?>>
                                            <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                                        </option>
                                    <?php endforeach; ?>
                                </select>
                            </div>
                        </div>

                        <div class="row g-3">
                            <div class="col-md-4">
                                <label class="form-label"><?= __('Commission Rate') ?></label>
                                <div class="input-group">
                                    <input type="number" name="commission_rate" class="form-control"
                                           step="0.01" min="0" max="1"
                                           value="<?= htmlspecialchars(number_format((float) ($affiliate['commission_rate'] ?? 0.10), 2), ENT_QUOTES) ?>"
                                        <?= $isEditMode ? 'required' : 'readonly' ?>>
                                    <span class="input-group-text">%</span>
                                </div>
                            </div>
                        </div>

                        <?php if ($isEditMode): ?>
                            <div class="d-flex gap-2">
                                <button type="submit" class="btn btn-primary"><?= __('Save Changes') ?></button>
                            </div>
                        <?php endif; ?>
                    </form>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-header bg-light">
                    <h2 class="h5 mb-0"><?= __('Referral Link') ?></h2>
                </div>
                <div class="card-body">
                    <div class="input-group">
                        <input type="text" id="referralLink" class="form-control" readonly
                               value="<?= htmlspecialchars((string) ($affiliate['referral_link'] ?? ''), ENT_QUOTES) ?>">
                        <button type="button" class="btn btn-outline-secondary" onclick="copyReferralLink()">
                            <?= __('Copy Link') ?>
                        </button>
                    </div>
                    <small class="text-muted"><?= __('Partners can use this link to sign up and automatically receive referral tracking.') ?></small>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-light">
                    <h2 class="h5 mb-0"><?= __('Account Status') ?></h2>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <span class="badge bg-<?= ($affiliate['status'] ?? '') === 'active' ? 'success' : 'secondary' ?> fs-6">
                            <?= htmlspecialchars((string) ($affiliate['status_label'] ?? ($affiliate['status'] ?? '')), ENT_QUOTES) ?>
                        </span>
                    </div>
                    <dl class="mb-0">
                        <dt class="text-muted small"><?= __('Affiliate ID') ?></dt>
                        <dd class="mb-2"><?= $affiliateId ?></dd>
                        <dt class="text-muted small"><?= __('Customer ID') ?></dt>
                        <dd class="mb-2"><?= (int) ($affiliate['customer_id'] ?? 0) ?></dd>
                        <dt class="text-muted small"><?= __('Created At') ?></dt>
                        <dd class="mb-2"><?= htmlspecialchars((string) ($affiliate['created_at'] ?? '-'), ENT_QUOTES) ?></dd>
                        <dt class="text-muted small"><?= __('Updated At') ?></dt>
                        <dd class="mb-0"><?= htmlspecialchars((string) ($affiliate['updated_at'] ?? '-'), ENT_QUOTES) ?></dd>
                    </dl>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-header bg-light">
                    <h2 class="h5 mb-0"><?= __('Commission Summary') ?></h2>
                </div>
                <div class="card-body">
                    <div class="row g-3 text-center mb-3">
                        <div class="col-6">
                            <div class="text-muted small"><?= __('Total Commission') ?></div>
                            <div class="fs-5 fw-bold"><?= number_format((float) ($affiliate['total_commission'] ?? 0), 2) ?></div>
                        </div>
                        <div class="col-6">
                            <div class="text-muted small"><?= __('Commission Rate') ?></div>
                            <div class="fs-5 fw-bold"><?= number_format((float) ($affiliate['commission_rate'] ?? 0) * 100, 2) ?>%</div>
                        </div>
                    </div>
                    <hr>
                    <div class="row g-3 text-center">
                        <div class="col-6">
                            <div class="text-muted small"><?= __('Paid') ?></div>
                            <div class="fs-5 fw-bold text-success"><?= number_format((float) ($affiliate['paid_commission'] ?? 0), 2) ?></div>
                        </div>
                        <div class="col-6">
                            <div class="text-muted small"><?= __('Pending') ?></div>
                            <div class="fs-5 fw-bold text-primary"><?= number_format((float) ($affiliate['pending_commission'] ?? 0), 2) ?></div>
                        </div>
                    </div>
                    <?php if (($affiliate['total_commission'] ?? 0) > 0): ?>
                        <hr>
                        <div class="text-center">
                            <div class="text-muted small mb-2"><?= __('Payment Progress') ?></div>
                            <div class="progress" style="height: 8px;">
                                <div class="progress-bar bg-success" role="progressbar"
                                     style="width: <?= min(100, round((float) ($affiliate['paid_commission'] ?? 0) / (float) ($affiliate['total_commission'] ?? 1) * 100, 2)) ?>%">
                                </div>
                            </div>
                            <small class="text-muted">
                                <?= number_format((float) ($affiliate['paid_commission'] ?? 0), 2) ?>
                                / <?= number_format((float) ($affiliate['total_commission'] ?? 0), 2) ?>
                            </small>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function copyReferralCode() {
        const code = <?= json_encode((string) ($affiliate['referral_code'] ?? '')) ?>;
        navigator.clipboard.writeText(code).then(function () {
            BackendToast.show(<?= json_encode((string) __('Referral code copied!')) ?>);
        });
    }

    function copyReferralLink() {
        const link = document.getElementById('referralLink').value;
        navigator.clipboard.writeText(link).then(function () {
            BackendToast.show(<?= json_encode((string) __('Referral link copied!')) ?>);
        });
    }
</script>
