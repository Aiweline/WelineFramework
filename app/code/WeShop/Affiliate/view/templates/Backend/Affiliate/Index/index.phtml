<?php
/** @var \Weline\Framework\View\Template $this */
$affiliateRecords = $this->getData('affiliateRecords') ?? [];
$summary = $this->getData('summary') ?? [];
$filters = $this->getData('filters') ?? [];
$statusOptions = $this->getData('statusOptions') ?? [];
$editingRecord = $this->getData('editingRecord') ?? [];
$affiliateIndexUrl = (string) ($this->getData('affiliateIndexUrl') ?? $this->getBackendUrl('*/backend/affiliate'));
$affiliateSaveUrl = (string) ($this->getData('affiliateSaveUrl') ?? $this->getBackendUrl('*/backend/affiliate/save'));
$pagination = $this->getData('pagination') ?? [];
$currentPage = max(1, (int) ($pagination['page'] ?? 1));
$pageCount = max(1, (int) ($pagination['page_count'] ?? 1));
$totalAffiliates = max(0, (int) ($pagination['total'] ?? 0));
$editingId = max(0, (int) ($editingRecord['affiliate_id'] ?? 0));
$customerIdReadonly = $editingId ? 'readonly' : '';
$customerIdRequired = $editingId ? '' : 'required';
$isEditMode = $editingId > 0;
$editBackUrl = $isEditMode
    ? (string) $this->getBackendUrl('*/backend/affiliate/view') . '?id={id}'
    : $affiliateIndexUrl;
?>

<style>
    .ws-affiliate-admin {
        max-width: 1440px;
        margin: 0 auto;
        padding: 24px;
        color: #1f2937;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Arial, sans-serif;
        line-height: 1.45;
    }

    .ws-affiliate-admin .d-flex {
        display: flex;
    }

    .ws-affiliate-admin .flex-wrap {
        flex-wrap: wrap;
    }

    .ws-affiliate-admin .justify-content-between {
        justify-content: space-between;
    }

    .ws-affiliate-admin .align-items-center {
        align-items: center;
    }

    .ws-affiliate-admin .align-items-end {
        align-items: end;
    }

    .ws-affiliate-admin .gap-2 {
        gap: 8px;
    }

    .ws-affiliate-admin .h3,
    .ws-affiliate-admin h1 {
        margin: 0;
        font-size: 28px;
        font-weight: 700;
        letter-spacing: 0;
    }

    .ws-affiliate-admin .h5,
    .ws-affiliate-admin h2 {
        margin: 0 0 16px;
        font-size: 18px;
        font-weight: 700;
        letter-spacing: 0;
    }

    .ws-affiliate-admin .text-muted {
        color: #64748b;
    }

    .ws-affiliate-admin .small {
        font-size: 12px;
    }

    .ws-affiliate-admin .mb-0 {
        margin-bottom: 0;
    }

    .ws-affiliate-admin .mb-1 {
        margin-bottom: 4px;
    }

    .ws-affiliate-admin .mb-2 {
        margin-bottom: 8px;
    }

    .ws-affiliate-admin .mb-3 {
        margin-bottom: 12px;
    }

    .ws-affiliate-admin .mb-4 {
        margin-bottom: 20px;
    }

    .ws-affiliate-admin .mt-4 {
        margin-top: 20px;
    }

    .ws-affiliate-admin .row {
        display: grid;
        gap: 16px;
    }

    .ws-affiliate-admin .row.g-3.mb-4 {
        grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .ws-affiliate-admin .row.g-4 {
        grid-template-columns: minmax(300px, 380px) minmax(0, 1fr);
        align-items: start;
    }

    .ws-affiliate-admin .card {
        border: 1px solid #e5e7eb;
        border-radius: 8px;
        background: #fff;
        box-shadow: 0 1px 2px rgba(15, 23, 42, 0.06);
    }

    .ws-affiliate-admin .card-body {
        padding: 18px;
    }

    .ws-affiliate-admin .display-6 {
        font-size: 26px;
        font-weight: 700;
        line-height: 1.1;
    }

    .ws-affiliate-admin .text-success {
        color: #047857;
    }

    .ws-affiliate-admin .text-secondary {
        color: #64748b;
    }

    .ws-affiliate-admin .d-grid {
        display: grid;
    }

    .ws-affiliate-admin .gap-3 {
        gap: 14px;
    }

    .ws-affiliate-admin .form-label {
        display: block;
        margin-bottom: 6px;
        color: #334155;
        font-size: 13px;
        font-weight: 600;
    }

    .ws-affiliate-admin .form-control,
    .ws-affiliate-admin .form-select {
        width: 100%;
        min-height: 38px;
        box-sizing: border-box;
        border: 1px solid #cbd5e1;
        border-radius: 6px;
        background: #fff;
        color: #111827;
        font: inherit;
        padding: 8px 10px;
    }

    .ws-affiliate-admin .form-control:focus,
    .ws-affiliate-admin .form-select:focus {
        border-color: #2563eb;
        box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.14);
        outline: none;
    }

    .ws-affiliate-admin .input-group {
        display: flex;
        align-items: stretch;
    }

    .ws-affiliate-admin .input-group .form-control {
        border-radius: 6px 0 0 6px;
    }

    .ws-affiliate-admin .input-group-text {
        display: inline-flex;
        align-items: center;
        border: 1px solid #cbd5e1;
        border-left: 0;
        border-radius: 0 6px 6px 0;
        background: #f8fafc;
        color: #64748b;
        padding: 0 10px;
        white-space: nowrap;
    }

    .ws-affiliate-admin .border.rounded {
        border: 1px solid #e2e8f0;
        border-radius: 8px;
    }

    .ws-affiliate-admin .p-3 {
        padding: 14px;
    }

    .ws-affiliate-admin .bg-light {
        background: #f8fafc;
    }

    .ws-affiliate-admin .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: 36px;
        border: 1px solid transparent;
        border-radius: 6px;
        cursor: pointer;
        font: inherit;
        font-weight: 600;
        padding: 7px 12px;
        text-decoration: none;
        transition: background-color 0.15s ease, border-color 0.15s ease, color 0.15s ease;
    }

    .ws-affiliate-admin .btn-primary {
        background: #2563eb;
        color: #fff;
    }

    .ws-affiliate-admin .btn-primary:hover {
        background: #1d4ed8;
    }

    .ws-affiliate-admin .btn-outline-primary {
        border-color: #bfdbfe;
        color: #1d4ed8;
        background: #eff6ff;
    }

    .ws-affiliate-admin .btn-outline-info {
        border-color: #bae6fd;
        color: #0369a1;
        background: #f0f9ff;
    }

    .ws-affiliate-admin .btn-outline-secondary {
        border-color: #cbd5e1;
        color: #475569;
        background: #fff;
    }

    .ws-affiliate-admin .btn-sm {
        min-height: 30px;
        padding: 5px 9px;
        font-size: 12px;
    }

    .ws-affiliate-admin .col-12.d-flex {
        display: flex;
        flex-wrap: wrap;
    }

    .ws-affiliate-admin .table-responsive {
        overflow-x: auto;
    }

    .ws-affiliate-admin table {
        width: 100%;
        border-collapse: collapse;
        min-width: 860px;
    }

    .ws-affiliate-admin th,
    .ws-affiliate-admin td {
        border-bottom: 1px solid #e5e7eb;
        padding: 11px 10px;
        text-align: left;
        vertical-align: middle;
        white-space: nowrap;
    }

    .ws-affiliate-admin th {
        color: #475569;
        font-size: 12px;
        font-weight: 700;
        text-transform: uppercase;
    }

    .ws-affiliate-admin tbody tr:hover {
        background: #f8fafc;
    }

    .ws-affiliate-admin .text-end {
        text-align: right;
    }

    .ws-affiliate-admin .badge {
        display: inline-flex;
        align-items: center;
        min-height: 22px;
        border-radius: 999px;
        font-size: 12px;
        font-weight: 700;
        padding: 2px 8px;
    }

    .ws-affiliate-admin .bg-success {
        background: #dcfce7;
        color: #166534;
    }

    .ws-affiliate-admin .bg-secondary {
        background: #e2e8f0;
        color: #475569;
    }

    .ws-affiliate-admin code {
        border-radius: 4px;
        background: #f1f5f9;
        color: #334155;
        padding: 2px 5px;
    }

    .ws-affiliate-admin .pagination {
        display: flex;
        gap: 6px;
        list-style: none;
        padding: 0;
    }

    .ws-affiliate-admin .page-link {
        display: inline-flex;
        border: 1px solid #cbd5e1;
        border-radius: 6px;
        color: #2563eb;
        padding: 6px 10px;
        text-decoration: none;
    }

    .ws-affiliate-admin .page-item.disabled .page-link {
        color: #94a3b8;
        pointer-events: none;
    }

    .ws-affiliate-admin .page-item.active .page-link {
        background: #2563eb;
        color: #fff;
    }

    @media (max-width: 1080px) {
        .ws-affiliate-admin .row.g-3.mb-4 {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .ws-affiliate-admin .row.g-4 {
            grid-template-columns: 1fr;
        }
    }

    @media (max-width: 640px) {
        .ws-affiliate-admin {
            padding: 16px;
        }

        .ws-affiliate-admin .row.g-3.mb-4 {
            grid-template-columns: 1fr;
        }

        .ws-affiliate-admin .h3,
        .ws-affiliate-admin h1 {
            font-size: 24px;
        }
    }
</style>

<div class="container-fluid py-3 ws-affiliate-admin">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <h1 class="h3 mb-1"><?= __('Affiliate Management') ?></h1>
            <p class="text-muted mb-0"><?= __('Review, filter, and adjust affiliate accounts created by marketing partners.') ?></p>
        </div>
        <div class="text-muted small">
            <?= htmlspecialchars(sprintf(
                __('Page %d of %d, %d affiliates found'),
                $currentPage,
                $pageCount,
                $totalAffiliates
            ), ENT_QUOTES) ?>
        </div>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Total Affiliates') ?></div>
                    <div class="display-6"><?= (int) ($summary['total'] ?? $totalAffiliates) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Active') ?></div>
                    <div class="display-6 text-success"><?= (int) ($summary['active'] ?? 0) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Disabled') ?></div>
                    <div class="display-6 text-secondary"><?= (int) ($summary['disabled'] ?? 0) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Total Commission') ?></div>
                    <div class="display-6"><?= number_format((float) ($summary['total_commission'] ?? 0), 2) ?></div>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-lg-4">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= $isEditMode ? __('Edit Affiliate Account') : __('Create Affiliate Account') ?></h2>
                    <form method="post" action="<?= $affiliateSaveUrl ?>" class="d-grid gap-3">
                        <input type="hidden" name="affiliate_id" value="<?= $editingId ?>">
                        <input type="hidden" name="back_url" value="<?= htmlspecialchars($editBackUrl, ENT_QUOTES) ?>">
                        <div>
                            <label class="form-label"><?= __('Customer ID') ?></label>
                            <input type="number"
                                   name="customer_id"
                                   class="form-control"
                                   min="1"
                                   value="<?= htmlspecialchars((string) ($editingRecord['customer_id'] ?? ''), ENT_QUOTES) ?>"
                                <?= $customerIdRequired ?> <?= $customerIdReadonly ?>>
                            <?php if ($isEditMode): ?>
                                <small class="text-muted"><?= __('Customer ID cannot be changed after creation.') ?></small>
                            <?php endif; ?>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Referral Code') ?></label>
                            <input type="text"
                                   class="form-control"
                                   value="<?= htmlspecialchars((string) ($editingRecord['referral_code'] ?? ''), ENT_QUOTES) ?>"
                                   readonly>
                            <small class="text-muted"><?= __('Auto-generated referral code.') ?></small>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Commission Rate') ?></label>
                            <div class="input-group">
                                <input type="number"
                                       name="commission_rate"
                                       class="form-control"
                                       step="0.01"
                                       min="0"
                                       max="1"
                                       value="<?= htmlspecialchars(number_format((float) ($editingRecord['commission_rate'] ?? 0.10), 2), ENT_QUOTES) ?>"
                                       required>
                                <span class="input-group-text"><?= __('decimal') ?></span>
                            </div>
                            <small class="text-muted"><?= __('Enter a decimal between 0 and 1 to represent the commission percentage.') ?></small>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Status') ?></label>
                            <select name="status" class="form-select">
                                <?php foreach ($statusOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>" <?= ($editingRecord['status'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="border rounded p-3 bg-light">
                            <div class="d-flex justify-content-between text-muted small mb-2">
                                <span><?= __('Total Commission') ?>:</span>
                                <span class="fw-semibold"><?= htmlspecialchars(number_format((float) ($editingRecord['total_commission'] ?? 0.0), 2), ENT_QUOTES) ?></span>
                            </div>
                            <div class="d-flex justify-content-between text-muted small mb-2">
                                <span><?= __('Paid Commission') ?>:</span>
                                <span class="fw-semibold"><?= htmlspecialchars(number_format((float) ($editingRecord['paid_commission'] ?? 0.0), 2), ENT_QUOTES) ?></span>
                            </div>
                            <div class="d-flex justify-content-between text-muted small">
                                <span><?= __('Pending') ?>:</span>
                                <span class="fw-semibold text-primary"><?= htmlspecialchars(number_format((float) ($editingRecord['pending_commission'] ?? 0.0), 2), ENT_QUOTES) ?></span>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-primary"><?= $isEditMode ? __('Update Affiliate') : __('Create Affiliate') ?></button>
                        <?php if ($isEditMode): ?>
                            <a href="<?= $affiliateIndexUrl ?>" class="btn btn-outline-secondary"><?= __('Cancel Edit') ?></a>
                        <?php endif; ?>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-8">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <form method="get" action="<?= $affiliateIndexUrl ?>" class="row g-3 align-items-end">
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Customer ID') ?></label>
                            <input type="number" name="customer_id" class="form-control" value="<?= htmlspecialchars((string) ($filters['customer_id'] ?? ''), ENT_QUOTES) ?>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Referral Code') ?></label>
                            <input type="text" name="referral_code" class="form-control" value="<?= htmlspecialchars((string) ($filters['referral_code'] ?? ''), ENT_QUOTES) ?>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Status') ?></label>
                            <select name="status" class="form-select">
                                <option value=""><?= __('All statuses') ?></option>
                                <?php foreach ($statusOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>" <?= ($filters['status'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="col-12 d-flex gap-2">
                            <button type="submit" class="btn btn-outline-primary"><?= __('Apply Filters') ?></button>
                            <a href="<?= $affiliateIndexUrl ?>" class="btn btn-outline-secondary"><?= __('Reset') ?></a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <?php if ($affiliateRecords): ?>
                        <div class="table-responsive">
                            <table class="table align-middle">
                                <thead>
                                <tr>
                                    <th><?= __('Affiliate ID') ?></th>
                                    <th><?= __('Customer ID') ?></th>
                                    <th><?= __('Referral Code') ?></th>
                                    <th><?= __('Commission Rate') ?></th>
                                    <th><?= __('Status') ?></th>
                                    <th><?= __('Total') ?></th>
                                    <th><?= __('Paid') ?></th>
                                    <th><?= __('Pending') ?></th>
                                    <th><?= __('Created At') ?></th>
                                    <th class="text-end"><?= __('Actions') ?></th>
                                </tr>
                                </thead>
                                <tbody>
                                <?php foreach ($affiliateRecords as $record): ?>
                                    <tr class="<?= $record['affiliate_id'] === $editingId ? 'table-primary' : '' ?>">
                                        <td><?= (int) $record['affiliate_id'] ?></td>
                                        <td><?= (int) $record['customer_id'] ?></td>
                                        <td>
                                            <code class="small"><?= htmlspecialchars((string) $record['referral_code'], ENT_QUOTES) ?></code>
                                        </td>
                                        <td><?= htmlspecialchars(number_format((float) ($record['commission_rate'] ?? 0.0) * 100, 2), ENT_QUOTES) ?>%</td>
                                        <td>
                                            <span class="badge bg-<?= ($record['status'] ?? '') === 'active' ? 'success' : 'secondary' ?>">
                                                <?= htmlspecialchars((string) ($record['status_label'] ?? ''), ENT_QUOTES) ?>
                                            </span>
                                        </td>
                                        <td><?= htmlspecialchars(number_format((float) ($record['total_commission'] ?? 0.0), 2), ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars(number_format((float) ($record['paid_commission'] ?? 0.0), 2), ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars(number_format((float) ($record['pending_commission'] ?? 0.0), 2), ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) $record['created_at'], ENT_QUOTES) ?></td>
                                        <td class="text-end">
                                            <a href="<?= $this->getBackendUrl('*/backend/affiliate/view', ['id' => $record['affiliate_id']]) ?>" class="btn btn-sm btn-outline-info">
                                                <?= __('View') ?>
                                            </a>
                                            <a href="<?= $this->getBackendUrl('*/backend/affiliate', ['id' => $record['affiliate_id']]) ?>" class="btn btn-sm btn-outline-primary">
                                                <?= __('Edit') ?>
                                            </a>
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>

                        <?php if ($pageCount > 1): ?>
                            <nav aria-label="<?= __('Affiliate pagination') ?>" class="mt-4">
                                <ul class="pagination justify-content-center mb-0">
                                    <li class="page-item <?= $currentPage <= 1 ? 'disabled' : '' ?>">
                                        <a class="page-link" href="<?= $affiliateIndexUrl ?>?page=<?= $currentPage - 1 ?><?= !empty($filters['customer_id']) ? '&customer_id=' . urlencode((string) $filters['customer_id']) : '' ?><?= !empty($filters['referral_code']) ? '&referral_code=' . urlencode((string) $filters['referral_code']) : '' ?><?= !empty($filters['status']) ? '&status=' . urlencode((string) $filters['status']) : '' ?>">
                                            <?= __('Previous') ?>
                                        </a>
                                    </li>
                                    <?php for ($i = max(1, $currentPage - 2); $i <= min($pageCount, $currentPage + 2); $i++): ?>
                                        <li class="page-item <?= $i === $currentPage ? 'active' : '' ?>">
                                            <a class="page-link" href="<?= $affiliateIndexUrl ?>?page=<?= $i ?><?= !empty($filters['customer_id']) ? '&customer_id=' . urlencode((string) $filters['customer_id']) : '' ?><?= !empty($filters['referral_code']) ? '&referral_code=' . urlencode((string) $filters['referral_code']) : '' ?><?= !empty($filters['status']) ? '&status=' . urlencode((string) $filters['status']) : '' ?>">
                                                <?= $i ?>
                                            </a>
                                        </li>
                                    <?php endfor; ?>
                                    <li class="page-item <?= $currentPage >= $pageCount ? 'disabled' : '' ?>">
                                        <a class="page-link" href="<?= $affiliateIndexUrl ?>?page=<?= $currentPage + 1 ?><?= !empty($filters['customer_id']) ? '&customer_id=' . urlencode((string) $filters['customer_id']) : '' ?><?= !empty($filters['referral_code']) ? '&referral_code=' . urlencode((string) $filters['referral_code']) : '' ?><?= !empty($filters['status']) ? '&status=' . urlencode((string) $filters['status']) : '' ?>">
                                            <?= __('Next') ?>
                                        </a>
                                    </li>
                                </ul>
                            </nav>
                        <?php endif; ?>

                    <?php else: ?>
                        <div class="text-center py-5">
                            <h2 class="h5 mb-2"><?= __('No affiliate accounts found.') ?></h2>
                            <p class="text-muted mb-0"><?= __('Use the form to create the first affiliate or adjust filters to find existing accounts.') ?></p>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>
