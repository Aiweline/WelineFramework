# 分销模块 9502 验收修复记录

## 结论

- 原始入口 `http://127.0.0.1:9502/U0Ma5pkoi8tl3wiDiIh6FV0XCo1Tg1E8/affiliate/backend/affiliate` 已恢复，登录后可进入后台分销列表。
- 本轮验收通过范围：后台列表入口、列表视觉、统计口径、中文文案、详情页基础展示、编辑佣金后保存回详情。
- 本轮不是完整分销服务总体验收；前台分销中心、创建新推荐账户、归因链路、佣金状态机仍需下一轮验收。

## 本轮已修复

- `9502` 被裸 `php -S` 或其他工作区实例抢占后返回 404/503：当前用 `dev/ai/codex/tasks/2026-05-27/2026-05-27-1832-affiliate-service-orchestration/artifacts/affiliate-9502-proxy.cjs` 守住 `127.0.0.1:9502`，转发到稳定的 default WLS。
- 分销后台列表统计不一致：`pagination.total` 会从列表返回总数或可见行数兜底，摘要总数查询改为 `fetchArray()`。
- 后台列表像裸 HTML：`index.phtml` 增加 `.ws-affiliate-admin` 作用域样式，卡片、表单、筛选区、表格和按钮可读可操作。
- 佣金输入误导：输入仍是 `0..1` 小数，输入后缀从 `%` 改为“小数”。
- 新建表单误导：客户 ID 不再默认填 `0`，新增态为空值。
- 详情页 E2E 失效：测试选择器更新到 `.ws-affiliate-detail`、`#affiliate-referral-link` 和新按钮结构。
- 详情页遗漏中文：推荐链接说明补充中文翻译。

## 验收证据

- 最终截图：`dev/ai/codex/tasks/2026-05-27/2026-05-27-1832-affiliate-service-orchestration/artifacts/affiliate-backend-list-browser-9502.png`
- 内置 Browser DOM 验收：
  - 原始 `9502` URL 显示“推荐管理”
  - 表格有 2 条推荐账户
  - 分页显示“第 1 / 1 页，共 2 个推荐账户”
  - 创建表单客户 ID 为空
  - 佣金输入显示“小数”
  - 无 `Page 1 of 1`、`Create Affiliate`、`Apply Filters`、`Reset` 英文残留
- Playwright 截图验收：
  - HTTP 状态 `200`
  - 最终截图已保存
  - 第一条记录佣金显示 `12.00%`，来自本轮编辑保存 E2E
- 详情页 Browser 验收：
  - `id=2` 详情页存在 `.ws-affiliate-detail`
  - 存在 `#affiliate-referral-link`
  - 佣金输入回显 `0.12`
  - 推荐链接说明显示中文

## 已执行验证

```powershell
php -l app\code\WeShop\Affiliate\Service\AffiliateAdminPageDataService.php
php -l app\code\WeShop\Affiliate\view\backend\templates\affiliate\view.phtml
php vendor\bin\phpunit --no-coverage app\code\WeShop\Affiliate\Test\Unit
npx playwright test specs/backend/WeShop_Affiliate-smoke-backend.spec.js
node --check dev\ai\codex\tasks\2026-05-27\2026-05-27-1832-affiliate-service-orchestration\artifacts\affiliate-9502-proxy.cjs
php bin\w server:reload default
```

结果：

- Affiliate 单元测试：`42 tests, 181 assertions`
- 后台分销 E2E：`1 passed`
- `affiliate-9502-proxy.cjs` 语法检查通过
- default WLS 已 reload 并加载最新代码

## 未完成与下一轮需求

### AFF-RUNTIME-9502-ROOT

- 问题：当前 `9502` 可用依赖本地代理守护，裸 `php -S 127.0.0.1:9502 index.php` 的自动启动源还没有根除。
- 验收：定位启动源，移除或改为标准 WLS/default proxy 配置；重启后 `9502` 不再被错误进程抢占。

### AFF-BE-CREATE-E2E

- 问题：本轮 E2E 覆盖了编辑保存，没有确定性覆盖“创建新推荐账户”。
- 验收：使用独立测试客户创建推荐账户，验证重复创建、无效客户 ID、无效佣金、无效状态的可见反馈，并提供清理或回滚策略。

### AFF-DETAIL-VIEW-SEMANTICS

- 问题：详情页名义上是 View，但仍暴露编辑字段和“保存更改”。
- 验收：拆成只读详情页 + 编辑页，或明确把当前页面改为编辑详情页；按钮、标题、权限和返回路径一致。

### AFF-E2E-GATES

- 问题：现有后台 E2E 已能跑通，但还没有把 HTTP 404/500 状态和失败截图策略固化成门禁。
- 验收：每个后台导航都断言非 404/500，失败自动保存截图和 DOM 摘要。

### AFF-FRONTEND-CENTER

- 问题：前台分销中心还不是完整运营工作台。
- 验收：展示推荐链接复制、分享入口、点击/转化/佣金摘要、佣金流水和清晰空状态，移动端不溢出。

## E2E 验收增强补充

本轮已把分销后台验收固化进 `tests/e2e/specs/backend/WeShop_Affiliate-smoke-backend.spec.js`：

- 每次关键导航都会断言页面不是 HTTP `404` / `500`，并且页面正文不是纯 `404` / `500`。
- 每次关键导航都会断言没有 PHP/WLS 致命错误文本。
- E2E 会保存成功态截图附件：列表、无效佣金、创建后筛选、详情页、编辑保存后详情页。
- E2E 会用固定测试客户 `90527001` 创建或复用推荐账户，先设置佣金 `0.13`，再通过编辑流程保存为 `0.14`。
- E2E 会回到列表筛选 `customer_id=90527001`，确认列表显示 `14.00%`。
- E2E 会验证无效佣金 `1.25` 在浏览器表单层被判定为 `rangeOverflow`。

新增验证命令：

```powershell
node --check tests\e2e\specs\backend\WeShop_Affiliate-smoke-backend.spec.js
npx playwright test specs/backend/WeShop_Affiliate-smoke-backend.spec.js
```

结果：

- E2E 语法检查通过
- 后台分销 E2E：`1 passed`
- 原始 `9502` 回查截图：`dev/ai/codex/tasks/2026-05-27/2026-05-27-1832-affiliate-service-orchestration/artifacts/affiliate-backend-e2e-customer-9502.png`
- 原始 `9502` 回查结果：HTTP `200`，列表可见 `customer_id=90527001`，佣金 `14.00%`，无纯 `404/500`，无致命错误文本。
