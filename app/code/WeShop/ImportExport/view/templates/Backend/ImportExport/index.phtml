<?php
$title = $this->getData('title') ?: 'Import / Export';
$productExportUrl = (string) ($this->getData('productExportUrl') ?? '');
$orderExportUrl = (string) ($this->getData('orderExportUrl') ?? '');
$productImportUrl = (string) ($this->getData('productImportUrl') ?? '');
?>
<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                <h4 class="mb-sm-0"><?= htmlspecialchars($title) ?></h4>
                <div class="page-title-right">
                    <ol class="breadcrumb m-0">
                        <li class="breadcrumb-item"><?= __('数据工具') ?></li>
                        <li class="breadcrumb-item active"><?= __('导入导出') ?></li>
                    </ol>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-lg-6">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-body p-4">
                    <div class="d-flex align-items-center mb-3">
                        <div class="rounded-circle bg-success-subtle text-success d-inline-flex align-items-center justify-content-center me-3" style="width:48px;height:48px;">
                            <i class="mdi mdi-download fs-3"></i>
                        </div>
                        <div>
                            <h5 class="mb-1"><?= __('CSV 导出') ?></h5>
                            <p class="text-muted mb-0"><?= __('生成最新产品与订单快照，方便运营归档或外部处理。') ?></p>
                        </div>
                    </div>

                    <div class="d-grid gap-3">
                        <a class="btn btn-success btn-lg" href="<?= htmlspecialchars($productExportUrl) ?>">
                            <i class="mdi mdi-package-variant-closed me-1"></i><?= __('导出产品 CSV') ?>
                        </a>
                        <a class="btn btn-outline-success btn-lg" href="<?= htmlspecialchars($orderExportUrl) ?>">
                            <i class="mdi mdi-receipt-text-outline me-1"></i><?= __('导出订单 CSV') ?>
                        </a>
                    </div>

                    <div class="mt-4 small text-muted">
                        <div><?= __('当前切片支持产品与订单导出。') ?></div>
                        <div><?= __('导出文件会实时生成，返回浏览器下载。') ?></div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-6">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-body p-4">
                    <div class="d-flex align-items-center mb-3">
                        <div class="rounded-circle bg-primary-subtle text-primary d-inline-flex align-items-center justify-content-center me-3" style="width:48px;height:48px;">
                            <i class="mdi mdi-upload fs-3"></i>
                        </div>
                        <div>
                            <h5 class="mb-1"><?= __('产品导入') ?></h5>
                            <p class="text-muted mb-0"><?= __('上传 CSV 批量创建或更新商品，按 SKU 合并。') ?></p>
                        </div>
                    </div>

                    <form action="<?= htmlspecialchars($productImportUrl) ?>" method="post" enctype="multipart/form-data" class="d-grid gap-3">
                        <div>
                            <label class="form-label fw-semibold" for="csv_file"><?= __('选择 CSV 文件') ?></label>
                            <input class="form-control" id="csv_file" name="csv_file" type="file" accept=".csv,text/csv" required>
                        </div>
                        <button class="btn btn-primary btn-lg" type="submit">
                            <i class="mdi mdi-file-upload-outline me-1"></i><?= __('导入产品 CSV') ?>
                        </button>
                    </form>

                    <div class="mt-4 small text-muted">
                        <div><?= __('最低推荐列：sku,name,price,cost,stock,set_id') ?></div>
                        <div><?= __('缺失描述、SPU、Meta 等字段时会按默认规则自动补齐。') ?></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
