<?php
/**
 * Shipment List - 发货列表
 *
 * @var \Weline\Framework\View\Template $this
 * @var array $shipments 发货列表
 * @var array $filters 筛选条件
 * @var array $pagination 分页信息
 * @var array $statusOptions 状态选项
 * @var array $summary 统计信息
 */
$title = $title ?? __('Shipment Management');
$shipments = $shipments ?? [];
$filters = $filters ?? [];
$pagination = $pagination ?? [];
$statusOptions = $statusOptions ?? [];
$summary = $summary ?? [];
$shipmentIndexUrl = $this->getBackendUrl('*/backend/shipment');

$statusBadgeMap = [
    'pending' => 'bg-warning',
    'processing' => 'bg-info',
    'shipped' => 'bg-primary',
    'delivered' => 'bg-success',
    'cancelled' => 'bg-secondary',
    'returned' => 'bg-danger',
];
?>
<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><?= $title ?></h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="javascript: void(0);"><?= __('WeShop') ?></a></li>
                    <li class="breadcrumb-item"><a href="javascript: void(0);"><?= __('Logistics') ?></a></li>
                    <li class="breadcrumb-item active"><?= __('Shipments') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- start page message -->
<div class="row mb-3">
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
        <div id="page-message-container"></div>
    </div>
</div>
<!-- end message -->

<!-- Statistics Cards -->
<div class="row mb-4">
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-primary bg-soft">
                            <span class="avatar-title bg-primary rounded-circle">
                                <i class="mdi mdi-truck-delivery font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('Total Shipments') ?></p>
                        <h4 class="mb-0"><?= number_format($summary['total'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-info bg-soft">
                            <span class="avatar-title bg-info rounded-circle">
                                <i class="mdi mdi-clock-outline font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('Pending') ?></p>
                        <h4 class="mb-0"><?= number_format($summary['pending'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-success bg-soft">
                            <span class="avatar-title bg-success rounded-circle">
                                <i class="mdi mdi-truck-check font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('Shipped') ?></p>
                        <h4 class="mb-0"><?= number_format($summary['shipped'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-warning bg-soft">
                            <span class="avatar-title bg-warning rounded-circle">
                                <i class="mdi mdi-home-analytics font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('Delivered') ?></p>
                        <h4 class="mb-0"><?= number_format($summary['delivered'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Filter Form -->
<div class="row mb-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0"><i class="mdi mdi-filter me-2"></i><?= __('Filter Shipments') ?></h5>
            </div>
            <div class="card-body">
                <form method="get" action="<?= htmlspecialchars($shipmentIndexUrl) ?>">
                    <div class="row g-3">
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Order ID') ?></label>
                            <input type="text" class="form-control" name="order_id"
                                   value="<?= htmlspecialchars($filters['order_id'] ?? '') ?>"
                                   placeholder="<?= __('Enter order ID') ?>">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Tracking Number') ?></label>
                            <input type="text" class="form-control" name="tracking_number"
                                   value="<?= htmlspecialchars($filters['tracking_number'] ?? '') ?>"
                                   placeholder="<?= __('Enter tracking number') ?>">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Carrier') ?></label>
                            <select class="form-select" name="carrier">
                                <option value=""><?= __('All Carriers') ?></option>
                                <?php foreach ($carrierOptions ?? [] as $code => $name): ?>
                                    <option value="<?= htmlspecialchars($code) ?>" <?= ($filters['carrier'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars($name) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Status') ?></label>
                            <select class="form-select" name="status">
                                <option value=""><?= __('All Statuses') ?></option>
                                <?php foreach ($statusOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars($code) ?>" <?= ($filters['status'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars($label) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                    </div>
                    <div class="row mt-3">
                        <div class="col-12">
                            <button type="submit" class="btn btn-primary">
                                <i class="mdi mdi-filter"></i> <?= __('Apply Filters') ?>
                            </button>
                            <a href="<?= htmlspecialchars($shipmentIndexUrl) ?>" class="btn btn-secondary">
                                <i class="mdi mdi-refresh"></i> <?= __('Reset') ?>
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Shipment List -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="card-title mb-0"><?= __('Shipment List') ?></h5>
                <div>
                    <button type="button" class="btn btn-sm btn-success" onclick="exportShipments()">
                        <i class="mdi mdi-download"></i> <?= __('Export') ?>
                    </button>
                </div>
            </div>
            <div class="card-body">
                <?php if (empty($shipments)): ?>
                    <div class="text-center text-muted py-5">
                        <i class="mdi mdi-truck-delivery font-size-48 d-block mb-3"></i>
                        <p><?= __('No shipments found') ?></p>
                    </div>
                <?php else: ?>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th><?= __('Shipment ID') ?></th>
                                    <th><?= __('Order ID') ?></th>
                                    <th><?= __('Tracking Number') ?></th>
                                    <th><?= __('Carrier') ?></th>
                                    <th><?= __('Items Count') ?></th>
                                    <th><?= __('Status') ?></th>
                                    <th><?= __('Shipped At') ?></th>
                                    <th><?= __('Delivered At') ?></th>
                                    <th><?= __('Actions') ?></th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($shipments as $shipment): ?>
                                    <tr>
                                        <td>
                                            <strong><?= (int) ($shipment['shipment_id'] ?? $shipment['id'] ?? 0) ?></strong>
                                        </td>
                                        <td>
                                            <a href="<?= $this->getBackendUrl('*/backend/order/view', ['id' => $shipment['order_id'] ?? 0]) ?>">
                                                #<?= (int) ($shipment['order_id'] ?? 0) ?>
                                            </a>
                                        </td>
                                        <td>
                                            <code><?= htmlspecialchars((string) ($shipment['tracking_number'] ?? '')) ?></code>
                                        </td>
                                        <td><?= htmlspecialchars((string) ($shipment['carrier'] ?? '')) ?></td>
                                        <td><?= (int) ($shipment['items_count'] ?? 0) ?></td>
                                        <td>
                                            <?php
                                            $status = (string) ($shipment['status'] ?? 'pending');
                                            $badgeClass = $statusBadgeMap[$status] ?? 'bg-secondary';
                                            $statusLabel = $statusOptions[$status] ?? ucfirst($status);
                                            ?>
                                            <span class="badge <?= $badgeClass ?>">
                                                <?= htmlspecialchars($statusLabel) ?>
                                            </span>
                                        </td>
                                        <td><?= htmlspecialchars((string) ($shipment['shipped_at'] ?? '-')) ?></td>
                                        <td><?= htmlspecialchars((string) ($shipment['delivered_at'] ?? '-')) ?></td>
                                        <td>
                                            <div class="btn-group btn-group-sm">
                                                <a href="<?= $this->getBackendUrl('*/backend/shipment/track', ['id' => $shipment['shipment_id'] ?? $shipment['id'] ?? 0]) ?>"
                                                   class="btn btn-outline-primary"
                                                   title="<?= __('Track') ?>">
                                                    <i class="mdi mdi-map-marker-path"></i>
                                                </a>
                                                <a href="<?= $this->getBackendUrl('*/backend/shipment/view', ['id' => $shipment['shipment_id'] ?? $shipment['id'] ?? 0]) ?>"
                                                   class="btn btn-outline-info"
                                                   title="<?= __('View') ?>">
                                                    <i class="mdi mdi-eye"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>

                    <!-- Pagination -->
                    <?php if (!empty($pagination) && ($pagination['total_pages'] ?? 1) > 1): ?>
                        <nav aria-label="Page navigation" class="mt-4">
                            <ul class="pagination justify-content-center">
                                <?php if ($pagination['has_previous'] ?? false): ?>
                                    <li class="page-item">
                                        <a class="page-link" href="<?= $this->getBackendUrl('*/backend/shipment', array_merge($filters, ['page' => $pagination['previous_page'] ?? 1])) ?>">
                                            <?= __('Previous') ?>
                                        </a>
                                    </li>
                                <?php endif; ?>

                                <?php
                                $currentPage = (int) ($pagination['current_page'] ?? 1);
                                $totalPages = (int) ($pagination['total_pages'] ?? 1);
                                $startPage = max(1, $currentPage - 2);
                                $endPage = min($totalPages, $currentPage + 2);
                                ?>
                                <?php for ($i = $startPage; $i <= $endPage; $i++): ?>
                                    <li class="page-item <?= $i === $currentPage ? 'active' : '' ?>">
                                        <a class="page-link" href="<?= $this->getBackendUrl('*/backend/shipment', array_merge($filters, ['page' => $i])) ?>">
                                            <?= $i ?>
                                        </a>
                                    </li>
                                <?php endfor; ?>

                                <?php if ($pagination['has_next'] ?? false): ?>
                                    <li class="page-item">
                                        <a class="page-link" href="<?= $this->getBackendUrl('*/backend/shipment', array_merge($filters, ['page' => $pagination['next_page'] ?? 2])) ?>">
                                            <?= __('Next') ?>
                                        </a>
                                    </li>
                                <?php endif; ?>
                            </ul>
                        </nav>
                        <div class="text-center text-muted mt-2">
                            <?= __('Page %{1} of %{2}', [$currentPage, $totalPages]) ?>
                        </div>
                    <?php endif; ?>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<script>
function exportShipments() {
    window.location.href = '<?= $this->getBackendUrl('*/backend/shipment/export') ?>';
}
</script>
