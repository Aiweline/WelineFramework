<?php
/**
 * Shipment Tracking - 物流追踪
 *
 * @var \Weline\Framework\View\Template $this
 * @var array $shipment 发货信息
 * @var array $trackingRecords 追踪记录
 * @var array $timeline 物流时间线
 * @var array $statusOptions 状态选项
 */
$title = $title ?? __('Shipment Tracking');
$shipment = $shipment ?? [];
$trackingRecords = $trackingRecords ?? [];
$timeline = $timeline ?? [];
$statusOptions = $statusOptions ?? [];
$shipmentIndexUrl = $this->getBackendUrl('*/backend/shipment');

$statusColorMap = [
    'pending' => 'text-warning',
    'processing' => 'text-info',
    'shipped' => 'text-primary',
    'in_transit' => 'text-primary',
    'out_for_delivery' => 'text-info',
    'delivered' => 'text-success',
    'cancelled' => 'text-secondary',
    'returned' => 'text-danger',
];
?>
<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><?= $title ?></h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="javascript: void(0);"><?= __('WeShop') ?></a></li>
                    <li class="breadcrumb-item"><a href="javascript: void(0);"><?= __('Logistics') ?></a></li>
                    <li class="breadcrumb-item"><a href="<?= htmlspecialchars($shipmentIndexUrl) ?>"><?= __('Shipments') ?></a></li>
                    <li class="breadcrumb-item active"><?= __('Tracking') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- Shipment Info Card -->
<div class="row mb-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0"><i class="mdi mdi-information me-2"></i><?= __('Shipment Information') ?></h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Shipment ID') ?></label>
                            <div class="fw-semibold">#<?= (int) ($shipment['shipment_id'] ?? $shipment['id'] ?? 0) ?></div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Order ID') ?></label>
                            <div class="fw-semibold">
                                <a href="<?= $this->getBackendUrl('*/backend/order/view', ['id' => $shipment['order_id'] ?? 0]) ?>">
                                    #<?= (int) ($shipment['order_id'] ?? 0) ?>
                                </a>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Tracking Number') ?></label>
                            <div class="fw-semibold">
                                <code><?= htmlspecialchars((string) ($shipment['tracking_number'] ?? '')) ?></code>
                                <button class="btn btn-sm btn-link" onclick="copyTrackingNumber()" title="<?= __('Copy') ?>">
                                    <i class="mdi mdi-content-copy"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Carrier') ?></label>
                            <div class="fw-semibold"><?= htmlspecialchars((string) ($shipment['carrier'] ?? '')) ?></div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Current Status') ?></label>
                            <?php
                            $currentStatus = (string) ($shipment['status'] ?? 'pending');
                            $statusColor = $statusColorMap[$currentStatus] ?? 'text-secondary';
                            $statusLabel = $statusOptions[$currentStatus] ?? ucfirst($currentStatus);
                            ?>
                            <div class="fw-semibold <?= $statusColor ?>">
                                <i class="mdi mdi-circle me-1" style="font-size: 10px;"></i>
                                <?= htmlspecialchars($statusLabel) ?>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Shipped At') ?></label>
                            <div class="fw-semibold"><?= htmlspecialchars((string) ($shipment['shipped_at'] ?? '-')) ?></div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Estimated Delivery') ?></label>
                            <div class="fw-semibold"><?= htmlspecialchars((string) ($shipment['estimated_delivery'] ?? '-')) ?></div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label text-muted"><?= __('Delivered At') ?></label>
                            <div class="fw-semibold"><?= htmlspecialchars((string) ($shipment['delivered_at'] ?? '-')) ?></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Tracking Timeline -->
<div class="row mb-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0"><i class="mdi mdi-timeline-clock me-2"></i><?= __('Tracking Timeline') ?></h5>
            </div>
            <div class="card-body">
                <?php if (empty($timeline)): ?>
                    <div class="text-center text-muted py-5">
                        <i class="mdi mdi-map-marker-off font-size-48 d-block mb-3"></i>
                        <p><?= __('No tracking information available') ?></p>
                    </div>
                <?php else: ?>
                    <div class="tracking-timeline">
                        <?php foreach ($timeline as $index => $event): ?>
                            <?php
                            $isFirst = $index === 0;
                            $isLast = $index === count($timeline) - 1;
                            $eventStatus = (string) ($event['status'] ?? 'pending');
                            $eventColor = $statusColorMap[$eventStatus] ?? 'text-secondary';
                            ?>
                            <div class="timeline-item <?= $isFirst ? 'timeline-item-first' : '' ?> <?= $isLast ? 'timeline-item-last' : '' ?>">
                                <div class="timeline-marker <?= $eventColor ?>">
                                    <i class="mdi mdi-circle"></i>
                                </div>
                                <div class="timeline-content">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div>
                                            <h6 class="fw-semibold mb-1 <?= $eventColor ?>">
                                                <?= htmlspecialchars((string) ($event['title'] ?? __('Status Update'))) ?>
                                            </h6>
                                            <p class="text-muted mb-1">
                                                <?= htmlspecialchars((string) ($event['description'] ?? '')) ?>
                                            </p>
                                            <?php if (!empty($event['location'])): ?>
                                                <p class="mb-0 small">
                                                    <i class="mdi mdi-map-marker me-1"></i>
                                                    <?= htmlspecialchars((string) $event['location']) ?>
                                                </p>
                                            <?php endif; ?>
                                        </div>
                                        <div class="text-end">
                                            <div class="small text-muted">
                                                <?= htmlspecialchars((string) ($event['timestamp'] ?? '')) ?>
                                            </div>
                                            <?php if (!empty($event['status'])): ?>
                                                <?php
                                                $eventStatusLabel = $statusOptions[$eventStatus] ?? ucfirst($eventStatus);
                                                ?>
                                                <span class="badge bg-light text-dark mt-1">
                                                    <?= htmlspecialchars($eventStatusLabel) ?>
                                                </span>
                                            <?php endif; ?>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <?php endforeach; ?>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<!-- Tracking Records Table (Detailed) -->
<?php if (!empty($trackingRecords)): ?>
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0"><i class="mdi mdi-format-list-bulleted me-2"></i><?= __('Detailed Records') ?></h5>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th><?= __('Time') ?></th>
                                <th><?= __('Status') ?></th>
                                <th><?= __('Location') ?></th>
                                <th><?= __('Description') ?></th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($trackingRecords as $record): ?>
                                <tr>
                                    <td class="text-nowrap">
                                        <i class="mdi mdi-clock-outline me-1 text-muted"></i>
                                        <?= htmlspecialchars((string) ($record['tracked_at'] ?? $record['timestamp'] ?? '')) ?>
                                    </td>
                                    <td>
                                        <?php
                                        $recStatus = (string) ($record['status'] ?? 'pending');
                                        $recColor = $statusColorMap[$recStatus] ?? 'text-secondary';
                                        $recLabel = $statusOptions[$recStatus] ?? ucfirst($recStatus);
                                        ?>
                                        <span class="<?= $recColor ?>">
                                            <i class="mdi mdi-circle me-1" style="font-size: 8px;"></i>
                                            <?= htmlspecialchars($recLabel) ?>
                                        </span>
                                    </td>
                                    <td>
                                        <?php if (!empty($record['location'])): ?>
                                            <i class="mdi mdi-map-marker me-1 text-muted"></i>
                                            <?= htmlspecialchars((string) $record['location']) ?>
                                        <?php else: ?>
                                            <span class="text-muted">-</span>
                                        <?php endif; ?>
                                    </td>
                                    <td><?= htmlspecialchars((string) ($record['description'] ?? '')) ?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<?php endif; ?>

<style>
.tracking-timeline {
    position: relative;
    padding-left: 30px;
}
.tracking-timeline::before {
    content: '';
    position: absolute;
    left: 7px;
    top: 0;
    bottom: 0;
    width: 2px;
    background: #dee2e6;
}
.timeline-item {
    position: relative;
    padding-bottom: 24px;
}
.timeline-item:last-child {
    padding-bottom: 0;
}
.timeline-marker {
    position: absolute;
    left: -30px;
    top: 0;
    width: 16px;
    height: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #fff;
    z-index: 1;
}
.timeline-marker i {
    font-size: 16px;
}
.timeline-item-first .timeline-marker {
    left: -33px;
    width: 22px;
    height: 22px;
    background: #fff;
    border-radius: 50%;
    box-shadow: 0 0 0 4px rgba(0,0,0,0.1);
}
.timeline-item-first .timeline-marker i {
    font-size: 22px;
}
.timeline-content {
    background: #f8f9fa;
    border-radius: 8px;
    padding: 16px;
}
.timeline-item-first .timeline-content {
    background: #e7f1ff;
    border: 1px solid #c3d7ff;
}
</style>

<script>
function copyTrackingNumber() {
    const trackingNumber = '<?= htmlspecialchars(addslashes((string) ($shipment['tracking_number'] ?? ''))) ?>';
    if (navigator.clipboard && navigator.clipboard.writeText) {
        navigator.clipboard.writeText(trackingNumber).then(function() {
            BackendToast.show('<?= __('Tracking number copied!') ?>');
        }).catch(function() {
            fallbackCopy(trackingNumber);
        });
    } else {
        fallbackCopy(trackingNumber);
    }
}

function fallbackCopy(text) {
    const textarea = document.createElement('textarea');
    textarea.value = text;
    textarea.style.position = 'fixed';
    textarea.style.opacity = '0';
    document.body.appendChild(textarea);
    textarea.select();
    try {
        document.execCommand('copy');
        BackendToast.show('<?= __('Tracking number copied!') ?>');
    } catch (err) {
        console.error('Copy failed:', err);
    }
    document.body.removeChild(textarea);
}
</script>
