<?php
$trackingRecords = $this->getData('trackingRecords') ?? [];
$filters = $this->getData('filters') ?? [];
$carrierOptions = $this->getData('carrierOptions') ?? [];
$statusOptions = $this->getData('statusOptions') ?? [];
$editingRecord = $this->getData('editingRecord') ?? [];
$trackingIndexUrl = (string) ($this->getData('trackingIndexUrl') ?? $this->getBackendUrl('*/backend/tracking'));
$trackingSaveUrl = (string) ($this->getData('trackingSaveUrl') ?? $this->getBackendUrl('*/backend/tracking/save'));
?>

<div class="container-fluid py-3">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <h1 class="h3 mb-1"><?= __('Tracking Management') ?></h1>
            <p class="text-muted mb-0"><?= __('Maintain shipment events and let customer-facing tracking stay up to date.') ?></p>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-lg-4">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= (int) ($editingRecord['tracking_id'] ?? 0) > 0 ? __('Edit Tracking Event') : __('Create Tracking Event') ?></h2>
                    <form method="post" action="<?= $trackingSaveUrl ?>" class="d-grid gap-3">
                        <input type="hidden" name="tracking_id" value="<?= (int) ($editingRecord['tracking_id'] ?? 0) ?>">
                        <input type="hidden" name="back_url" value="<?= htmlspecialchars((string) $trackingIndexUrl, ENT_QUOTES) ?>">
                        <div>
                            <label class="form-label"><?= __('Order ID') ?></label>
                            <input type="number" name="order_id" class="form-control" value="<?= htmlspecialchars((string) ($editingRecord['order_id'] ?? 0), ENT_QUOTES) ?>" required>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Tracking Number') ?></label>
                            <input type="text" name="tracking_number" class="form-control" value="<?= htmlspecialchars((string) ($editingRecord['tracking_number'] ?? ''), ENT_QUOTES) ?>" required>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Carrier') ?></label>
                            <select name="carrier" class="form-select">
                                <?php foreach ($carrierOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>" <?= ($editingRecord['carrier'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Status') ?></label>
                            <select name="status" class="form-select">
                                <?php foreach ($statusOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>" <?= ($editingRecord['status'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Location') ?></label>
                            <input type="text" name="location" class="form-control" value="<?= htmlspecialchars((string) ($editingRecord['location'] ?? ''), ENT_QUOTES) ?>">
                        </div>
                        <div>
                            <label class="form-label"><?= __('Description') ?></label>
                            <textarea name="description" class="form-control" rows="4"><?= htmlspecialchars((string) ($editingRecord['description'] ?? ''), ENT_QUOTES) ?></textarea>
                        </div>
                        <button type="submit" class="btn btn-primary"><?= __('Save Tracking Event') ?></button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-8">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <form method="get" action="<?= $trackingIndexUrl ?>" class="row g-3 align-items-end">
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Order ID') ?></label>
                            <input type="number" name="order_id" class="form-control" value="<?= htmlspecialchars((string) ($filters['order_id'] ?? ''), ENT_QUOTES) ?>">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Tracking Number') ?></label>
                            <input type="text" name="tracking_number" class="form-control" value="<?= htmlspecialchars((string) ($filters['tracking_number'] ?? ''), ENT_QUOTES) ?>">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Carrier') ?></label>
                            <select name="carrier" class="form-select">
                                <option value=""><?= __('All carriers') ?></option>
                                <?php foreach ($carrierOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>" <?= ($filters['carrier'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label"><?= __('Status') ?></label>
                            <select name="status" class="form-select">
                                <option value=""><?= __('All statuses') ?></option>
                                <?php foreach ($statusOptions as $code => $label): ?>
                                    <option value="<?= htmlspecialchars((string) $code, ENT_QUOTES) ?>" <?= ($filters['status'] ?? '') === $code ? 'selected' : '' ?>>
                                        <?= htmlspecialchars((string) $label, ENT_QUOTES) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="col-12 d-flex gap-2">
                            <button type="submit" class="btn btn-outline-primary"><?= __('Apply Filters') ?></button>
                            <a href="<?= $trackingIndexUrl ?>" class="btn btn-outline-secondary"><?= __('Reset') ?></a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <?php if ($trackingRecords): ?>
                        <div class="table-responsive">
                            <table class="table align-middle">
                                <thead>
                                <tr>
                                    <th><?= __('Order ID') ?></th>
                                    <th><?= __('Tracking Number') ?></th>
                                    <th><?= __('Carrier') ?></th>
                                    <th><?= __('Status') ?></th>
                                    <th><?= __('Tracked At') ?></th>
                                    <th><?= __('Location') ?></th>
                                    <th class="text-end"><?= __('Actions') ?></th>
                                </tr>
                                </thead>
                                <tbody>
                                <?php foreach ($trackingRecords as $record): ?>
                                    <tr>
                                        <td><?= (int) $record['order_id'] ?></td>
                                        <td><?= htmlspecialchars((string) $record['tracking_number'], ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) $record['carrier'], ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) $record['status_label'], ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) $record['tracked_at'], ENT_QUOTES) ?></td>
                                        <td><?= htmlspecialchars((string) $record['location'], ENT_QUOTES) ?></td>
                                        <td class="text-end">
                                            <a href="<?= $this->getBackendUrl('*/backend/tracking', ['id' => $record['tracking_id']]) ?>" class="btn btn-sm btn-outline-primary">
                                                <?= __('Edit') ?>
                                            </a>
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>
                    <?php else: ?>
                        <div class="text-center py-5">
                            <h2 class="h5 mb-2"><?= __('No tracking events found.') ?></h2>
                            <p class="text-muted mb-0"><?= __('Create the first tracking event to start the logistics timeline.') ?></p>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>
