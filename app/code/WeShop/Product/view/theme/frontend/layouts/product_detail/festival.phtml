<!--
布局：产品详情节日布局

产品详情页节日主题布局模板（示例：圣诞节、春节等）
字段
@meta.name {default="产品详情节日布局",name="产品详情节日布局",description="产品详情页的节日主题布局模板"}
@meta.description {default="产品详情页节日主题布局，适用于节日促销活动",name="产品详情页节日主题布局",description="产品详情页节日主题布局，适用于节日促销活动"}
@preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="是否需要登录，0不需要登录，1需要登录"}
@param.title {default="",name="页面标题",description="页面标题，用于设置页面标题"}
@param.showHeader {default=true,name="是否显示header",description="是否显示header，用于控制header的显示"}
@param.showFooter {default=true,name="是否显示footer",description="是否显示footer，用于控制footer的显示"}
@param.class {default="",name="额外CSS类",description="额外CSS类，用于设置页面额外CSS类"}

参数配置由 ControllerFetchFileBefore Observer 自动加载到 meta 数组中
模板中使用 meta.参数 输出内容，使用 meta.参数 在条件判断中
产品数据通过 Observer 自动传递到 product 变量中
-->
<!DOCTYPE html>
<html lang="{{htmlLang}}" data-local="{{lang_local}}" data-lang="{{lang}}">
<head>
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::product::head-before</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::product_detail::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::product_detail::head-after</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::product::head-after</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::head-after</w:hook>
</head>
<body>
    <w:hook>Weline_Theme::frontend::layouts::base::body-start</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::product::body-start</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::product_detail::body-start</w:hook>
    
    <if condition="meta.showHeader">
        <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
        <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>
        <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>
    </if>
    
    <w:hook>Weline_Theme::frontend::layouts::base::content-before</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::product::content-before</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::product_detail::content-before</w:hook>
    <main class="main-content product-detail festival-layout <?= htmlspecialchars(($meta['class'] ?? '')) ?>" id="main-content" data-layout="product_detail_festival">
        <?php
        $product = $this->getData('product');
        if ($product && $product->getId()):
        ?>
        <!-- 节日横幅 -->
        <div class="festival-banner">
            <h2>🎉 <?= __('节日特惠进行中！') ?></h2>
        </div>
        
        <div class="product-detail-container festival">
            <div class="product-images">
                <img src="<?= htmlspecialchars($product->getImage()) ?>" alt="<?= htmlspecialchars($product->getName()) ?>" />
                <div class="festival-badge"><?= __('限时特价') ?></div>
            </div>
            <div class="product-info">
                <h1 class="product-name"><?= htmlspecialchars($product->getName()) ?></h1>
                <div class="product-price-container">
                    <span class="original-price"><?= __('原价：') ?><?= \Weline\Currency\Helper\CurrencyFormatter::formatConverted($product->getPrice() * 1.2) ?></span>
                    <span class="product-price"><?= __('现价：') ?><?= \Weline\Currency\Helper\CurrencyFormatter::formatConverted($product->getPrice()) ?></span>
                </div>
                <div class="festival-countdown">
                    <p><?= __('活动倒计时：') ?><span id="countdown">23:59:59</span></p>
                </div>
                <div class="product-description">
                    <?= $product->getDescription() ?>
                </div>
            </div>
        </div>
        <?php else: ?>
        <div class="product-not-found">
            <p><?= __('产品不存在或已下架') ?></p>
        </div>
        <?php endif; ?>
    </main>
    <w:hook>Weline_Theme::frontend::layouts::product_detail::content-after</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::product::content-after</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::content-after</w:hook>
    
    <if condition="meta.showFooter">
        <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
        <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default" vars="copyright,links,showBackToTop"/>
        <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
    </if>
    
    <w:hook>Weline_Theme::frontend::layouts::product_detail::body-end</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::product::body-end</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::body-end</w:hook>
</body>
</html>

<style>
/* 产品详情节日布局样式 */
.festival-banner {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    text-align: center;
    padding: 1rem;
    margin-bottom: 2rem;
}

.festival-banner h2 {
    margin: 0;
    font-size: 1.5rem;
}

.product-detail-container.festival {
    display: flex;
    gap: 2rem;
    padding: 2rem;
    background: #f8f9fa;
    border-radius: 12px;
    position: relative;
}

.product-images {
    flex: 1;
    position: relative;
}

.product-images img {
    width: 100%;
    height: auto;
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.festival-badge {
    position: absolute;
    top: 10px;
    right: 10px;
    background: #e74c3c;
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-weight: bold;
    font-size: 0.9rem;
}

.product-info {
    flex: 1;
}

.product-name {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: #2c3e50;
}

.product-price-container {
    margin-bottom: 1rem;
}

.original-price {
    text-decoration: line-through;
    color: #95a5a6;
    margin-right: 1rem;
    font-size: 1rem;
}

.product-price {
    font-size: 1.8rem;
    color: #e74c3c;
    font-weight: bold;
}

.festival-countdown {
    background: #fff3cd;
    padding: 1rem;
    border-radius: 8px;
    margin-bottom: 1rem;
    border-left: 4px solid #ffc107;
}

.festival-countdown p {
    margin: 0;
    font-weight: bold;
}

#countdown {
    color: #e74c3c;
    font-size: 1.2rem;
}

.product-description {
    line-height: 1.6;
    color: #34495e;
}

.product-not-found {
    text-align: center;
    padding: 4rem 2rem;
}
</style>

<script>
// 简单的倒计时示例（实际应用中应该从服务器获取结束时间）
(function() {
    let seconds = 86399; // 23:59:59
    const countdownEl = document.getElementById('countdown');
    if (!countdownEl) return;
    
    setInterval(function() {
        const hours = Math.floor(seconds / 3600);
        const minutes = Math.floor((seconds % 3600) / 60);
        const secs = seconds % 60;
        countdownEl.textContent = 
            String(hours).padStart(2, '0') + ':' +
            String(minutes).padStart(2, '0') + ':' +
            String(secs).padStart(2, '0');
        seconds--;
        if (seconds < 0) seconds = 86399;
    }, 1000);
})();
</script>

