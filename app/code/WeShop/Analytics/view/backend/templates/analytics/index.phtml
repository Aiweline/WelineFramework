<?php
$providers = $this->getData('providers') ?? [];
$summary = $this->getData('summary') ?? [];
$editingProvider = $this->getData('editingProvider') ?? [];
$trackedEvents = $this->getData('trackedEvents') ?? [];
$snippetPreview = $this->getData('snippetPreview') ?? [];
$analyticsIndexUrl = (string) ($this->getData('analyticsIndexUrl') ?? $this->getBackendUrl('*/backend/analytics'));
$analyticsSaveUrl = (string) ($this->getData('analyticsSaveUrl') ?? $this->getBackendUrl('*/backend/analytics/save'));
?>

<div class="container-fluid py-3">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <h1 class="h3 mb-1"><?= __('Analytics Management') ?></h1>
            <p class="text-muted mb-0"><?= __('Manage storefront analytics providers, verify readiness, and keep the default theme injection chain healthy.') ?></p>
        </div>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Providers') ?></div>
                    <div class="display-6"><?= (int) ($summary['total_providers'] ?? 0) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Enabled') ?></div>
                    <div class="display-6 text-success"><?= (int) ($summary['enabled_providers'] ?? 0) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Ready') ?></div>
                    <div class="display-6 text-primary"><?= (int) ($summary['ready_providers'] ?? 0) ?></div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <div class="text-muted small mb-1"><?= __('Rendered Snippets') ?></div>
                    <div class="display-6"><?= (int) ($summary['snippet_count'] ?? 0) ?></div>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-lg-5">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= __('Providers') ?></h2>
                    <div class="d-grid gap-3">
                        <?php foreach ($providers as $provider): ?>
                            <a href="<?= $this->getBackendUrl('*/backend/analytics', ['provider' => $provider['code'] ?? '']) ?>" class="text-decoration-none text-reset">
                                <div class="border rounded p-3">
                                    <div class="d-flex justify-content-between align-items-start gap-2">
                                        <div>
                                            <div class="fw-semibold"><?= htmlspecialchars((string) ($provider['label'] ?? ''), ENT_QUOTES) ?></div>
                                            <div class="text-muted small"><?= htmlspecialchars((string) ($provider['description'] ?? ''), ENT_QUOTES) ?></div>
                                        </div>
                                        <div class="text-end small">
                                            <div class="badge <?= !empty($provider['enabled']) ? 'bg-success' : 'bg-secondary' ?>"><?= !empty($provider['enabled']) ? __('Enabled') : __('Disabled') ?></div>
                                            <div class="mt-2 badge <?= !empty($provider['ready']) ? 'bg-primary' : 'bg-warning text-dark' ?>"><?= !empty($provider['ready']) ? __('Ready') : __('Needs Config') ?></div>
                                        </div>
                                    </div>
                                </div>
                            </a>
                        <?php endforeach; ?>
                    </div>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= __('Tracked Events') ?></h2>
                    <div class="d-flex flex-wrap gap-2">
                        <?php foreach ($trackedEvents as $event): ?>
                            <span class="badge bg-light text-dark border"><?= htmlspecialchars((string) ($event['label'] ?? $event['code'] ?? ''), ENT_QUOTES) ?></span>
                        <?php endforeach; ?>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-7">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= htmlspecialchars((string) ($editingProvider['label'] ?? __('Provider Config')), ENT_QUOTES) ?></h2>
                    <p class="text-muted"><?= htmlspecialchars((string) ($editingProvider['description'] ?? ''), ENT_QUOTES) ?></p>

                    <form method="post" action="<?= $analyticsSaveUrl ?>" class="d-grid gap-3">
                        <input type="hidden" name="provider" value="<?= htmlspecialchars((string) ($editingProvider['code'] ?? ''), ENT_QUOTES) ?>">
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" id="analytics-enabled" name="enabled" value="1" <?= !empty($editingProvider['config']['enabled']) ? 'checked' : '' ?>>
                            <label class="form-check-label" for="analytics-enabled"><?= __('Enable provider') ?></label>
                        </div>

                        <?php foreach (($editingProvider['fields'] ?? []) as $field): ?>
                            <?php
                            $fieldName = (string) ($field['name'] ?? '');
                            if ($fieldName === '') {
                                continue;
                            }
                            $fieldValue = (string) ($editingProvider['config'][$fieldName] ?? '');
                            ?>
                            <div>
                                <label class="form-label"><?= htmlspecialchars((string) ($field['label'] ?? $fieldName), ENT_QUOTES) ?></label>
                                <input
                                    type="text"
                                    name="<?= htmlspecialchars($fieldName, ENT_QUOTES) ?>"
                                    class="form-control"
                                    value="<?= htmlspecialchars($fieldValue, ENT_QUOTES) ?>"
                                    <?= !empty($field['required']) ? 'required' : '' ?>
                                >
                            </div>
                        <?php endforeach; ?>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary"><?= __('Save Provider Config') ?></button>
                            <a href="<?= $analyticsIndexUrl ?>" class="btn btn-outline-secondary"><?= __('Reset') ?></a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= __('Snippet Preview') ?></h2>
                    <?php if ($snippetPreview): ?>
                        <div class="d-grid gap-3">
                            <?php foreach ($snippetPreview as $snippet): ?>
                                <div class="border rounded p-3">
                                    <div class="fw-semibold mb-2"><?= htmlspecialchars((string) ($snippet['provider'] ?? ''), ENT_QUOTES) ?></div>
                                    <pre class="mb-0 small" style="white-space: pre-wrap;"><?= htmlspecialchars((string) ($snippet['snippet'] ?? ''), ENT_QUOTES) ?></pre>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    <?php else: ?>
                        <div class="text-muted"><?= __('Enable and configure a provider to preview the storefront snippet.') ?></div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>
