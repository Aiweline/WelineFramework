<?php
/**@var \Weline\Framework\View\Template $this */
?>
<w:template>Weline_Admin::Backend/page-layout/main-content-before.phtml</w:template>

<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0">
                <?= $this->getData('page_title') ?: __('CMS页面管理') ?>
            </h4>

            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item">
                        <a href="javascript: void(0);">
                            <?= $this->getData('breadcrumb_parent') ?: __('内容管理') ?>
                        </a>
                    </li>
                    <li class="breadcrumb-item active">
                        <?= $this->getData('breadcrumb_current') ?: __('CMS页面管理') ?>
                    </li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- start page message -->
<div class='row'>
    <template>Weline_Component::message.phtml</template>
</div>
<!-- end message -->

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">
                    <lang>页面管理</lang>
                </h4>
                
                <div class="card-title-desc">
                    <div class='row'>
                        <div class='col-6'>
                            <div class="col-md-8">
                                <form action="<?= $this->getBackendUrl('*/backend/page/index') ?>" method="get">
                                    <div class="input-group">
                                        <input type="text" class="form-control" name="search" 
                                               value="<?= htmlspecialchars($this->request->getGet('search', '')) ?>"
                                               placeholder="<?= __('搜索页面名称、句柄...') ?>">
                                        <button class="btn btn-primary" type="submit">
                                            <i class="mdi mdi-magnify"></i> <?= __('搜索') ?>
                                        </button>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class='col-6'>
                            <acl source="WeShop_Cms::cms_page_create">
                                <a class='btn btn-primary pull-right mt-1 mb-1'
                                   href="<?= $this->getBackendUrl('*/backend/page/create') ?>">
                                    <i class="mdi mdi-plus"></i> <lang>新建页面</lang>
                                </a>
                            </acl>
                        </div>
                    </div>
                </div>

                <div class='table-responsive'>
                    <table class="table table-nowrap align-middle">
                        <thead>
                        <tr>
                            <th>ID</th>
                            <th><lang>页面标题</lang></th>
                            <th><lang>页面路径</lang></th>
                            <th><lang>状态</lang></th>
                            <th><lang>创建时间</lang></th>
                            <th><lang>更新时间</lang></th>
                            <th><lang>操作</lang></th>
                        </tr>
                        </thead>
                        <tbody>
                        <?php if (empty($this->getData('pages'))): ?>
                            <tr>
                                <td colspan="7" class="text-center text-muted">
                                    <div class="py-4">
                                        <i class="mdi mdi-file-document-outline" style="font-size: 48px;"></i>
                                        <p class="mt-2"><lang>暂无页面，点击右上角新建页面开始使用</lang></p>
                                    </div>
                                </td>
                            </tr>
                        <?php else: ?>
                            <?php foreach ($this->getData('pages') as $page): ?>
                                <tr data-id='<?= $page->getId() ?>'>
                                    <td><?= $page->getId() ?></td>
                                    <td><?= htmlspecialchars($page->getData('name')) ?></td>
                                    <td><?= htmlspecialchars($page->getData('handle')) ?></td>
                                    <td>
                                        <?php if ($page->getData('status') == 1): ?>
                                            <span class="badge bg-success"><?= __('已发布') ?></span>
                                        <?php else: ?>
                                            <span class="badge bg-secondary"><?= __('草稿') ?></span>
                                        <?php endif; ?>
                                    </td>
                                    <td><?= $page->getData('create_time') ?></td>
                                    <td><?= $page->getData('update_time') ?></td>
                                    <td>
                                        <div class="d-flex gap-2">
                                            <acl source="WeShop_Cms::cms_page_edit">
                                                <a class='btn btn-info btn-sm'
                                                   href="<?= $this->getBackendUrl('*/backend/page/edit') ?>?id=<?= $page->getId() ?>">
                                                    <i class="mdi mdi-pencil"></i> <?= __('编辑') ?>
                                                </a>
                                            </acl>
                                            <acl source="WeShop_Cms::cms_page_delete">
                                                <form action="<?= $this->getBackendUrl('*/backend/page/delete') ?>"
                                                      method='post' style="display:inline;">
                                                    <input type='hidden' name='id' value='<?= $page->getId() ?>'>
                                                    <button type='submit'
                                                            class='btn btn-danger btn-sm ajax-confirm'>
                                                        <i class="mdi mdi-delete"></i> <?= __('删除') ?>
                                                    </button>
                                                </form>
                                            </acl>
                                        </div>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        <?php endif; ?>
                        </tbody>
                    </table>
                </div>
                
                <?php if ($this->getData('pagination')): ?>
                    <div class="mt-3">
                        <?= $this->getData('pagination') ?>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<w:template>Weline_Admin::Backend/page-layout/main-content-after.phtml</w:template>
