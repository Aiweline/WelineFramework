<?php
/** @var \Weline\Framework\View\Template $this */
$items = $this->getData('items') ?? [];
$total = (int) ($this->getData('total') ?? 0);
$currentPage = (int) ($this->getData('current_page') ?? 1);
$pageSize = (int) ($this->getData('page_size') ?? 20);
$filters = $this->getData('filters') ?? [];
$editingPage = $this->getData('editing_page') ?? null;
$pageIndexUrl = (string) ($this->getData('pageIndexUrl') ?? $this->getBackendUrl('*/backend/cms/page'));
$pageSaveUrl = (string) ($this->getData('pageSaveUrl') ?? $this->getBackendUrl('*/backend/cms/page/save'));
$pageCount = max(1, (int) ceil($total / $pageSize));
$editingId = $editingPage ? (int) $editingPage->getId() : 0;
$isEditMode = $editingId > 0;
?>
<div class="container-fluid py-3">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <h1 class="h3 mb-1"><?= __('CMS Page Management') ?></h1>
            <p class="text-muted mb-0"><?= __('Create and manage static content pages for your storefront.') ?></p>
        </div>
        <div class="text-muted small">
            <?= htmlspecialchars(sprintf(__('Total %d pages'), $total), ENT_QUOTES) ?>
        </div>
    </div>

    <div class="row g-4">
        <!-- Edit Form -->
        <div class="col-lg-5">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5 mb-3"><?= $isEditMode ? __('Edit Page') : __('Create New Page') ?></h2>
                    <form method="post" action="<?= $pageSaveUrl ?>" class="d-grid gap-3">
                        <input type="hidden" name="page_id" value="<?= $editingId ?>">
                        <input type="hidden" name="back_url" value="<?= htmlspecialchars($pageIndexUrl, ENT_QUOTES) ?>">
                        <div>
                            <label class="form-label"><?= __('Title') ?> <span class="text-danger">*</span></label>
                            <input type="text" name="title" class="form-control"
                                   value="<?= htmlspecialchars($editingPage ? $editingPage->getTitle() : '', ENT_QUOTES) ?>" required>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Identifier') ?> <span class="text-danger">*</span></label>
                            <input type="text" name="identifier" class="form-control"
                                   value="<?= htmlspecialchars($editingPage ? $editingPage->getIdentifier() : '', ENT_QUOTES) ?>" required>
                            <small class="text-muted"><?= __('URL key used to access this page (e.g., "about-us", "privacy-policy").') ?></small>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Content Heading') ?></label>
                            <input type="text" name="content_heading" class="form-control"
                                   value="<?= htmlspecialchars($editingPage ? $editingPage->getContentHeading() : '', ENT_QUOTES) ?>">
                        </div>
                        <div>
                            <label class="form-label"><?= __('Content') ?></label>
                            <textarea name="content" class="form-control" rows="8"><?= htmlspecialchars($editingPage ? $editingPage->getContent() : '', ENT_QUOTES) ?></textarea>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Meta Title') ?></label>
                            <input type="text" name="meta_title" class="form-control"
                                   value="<?= htmlspecialchars($editingPage ? $editingPage->getMetaTitle() : '', ENT_QUOTES) ?>">
                        </div>
                        <div>
                            <label class="form-label"><?= __('Meta Description') ?></label>
                            <textarea name="meta_description" class="form-control" rows="2"><?= htmlspecialchars($editingPage ? $editingPage->getMetaDescription() : '', ENT_QUOTES) ?></textarea>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Meta Keywords') ?></label>
                            <input type="text" name="meta_keywords" class="form-control"
                                   value="<?= htmlspecialchars($editingPage ? $editingPage->getMetaKeywords() : '', ENT_QUOTES) ?>">
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                                <label class="form-label"><?= __('Status') ?></label>
                                <select name="status" class="form-select">
                                    <option value="1" <?= $editingPage && $editingPage->getStatus() === 1 ? 'selected' : '' ?>><?= __('Enabled') ?></option>
                                    <option value="0" <?= $editingPage !== null && $editingPage->getStatus() === 0 ? 'selected' : '' ?>><?= __('Disabled') ?></option>
                                </select>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label"><?= __('Sort Order') ?></label>
                                <input type="number" name="sort_order" class="form-control" min="0"
                                       value="<?= (int) ($editingPage ? $editingPage->getSortOrder() : 0) ?>">
                            </div>
                        </div>
                        <div>
                            <label class="form-label"><?= __('Page Layout') ?></label>
                            <select name="page_layout" class="form-select">
                                <option value=""><?= __('Default') ?></option>
                                <option value="1column" <?= $editingPage && $editingPage->getPageLayout() === '1column' ? 'selected' : '' ?>><?= __('1 Column') ?></option>
                                <option value="2columns-left" <?= $editingPage && $editingPage->getPageLayout() === '2columns-left' ? 'selected' : '' ?>><?= __('2 Columns Left') ?></option>
                                <option value="2columns-right" <?= $editingPage && $editingPage->getPageLayout() === '2columns-right' ? 'selected' : '' ?>><?= __('2 Columns Right') ?></option>
                                <option value="full-width" <?= $editingPage && $editingPage->getPageLayout() === 'full-width' ? 'selected' : '' ?>><?= __('Full Width') ?></option>
                            </select>
                        </div>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary"><?= __('Save Page') ?></button>
                            <?php if ($isEditMode): ?>
                                <a href="<?= $pageIndexUrl ?>" class="btn btn-outline-secondary"><?= __('Cancel') ?></a>
                            <?php endif; ?>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <!-- Page List -->
        <div class="col-lg-7">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <form method="get" action="<?= $pageIndexUrl ?>" class="row g-3 align-items-end">
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Title') ?></label>
                            <input type="text" name="title" class="form-control"
                                   value="<?= htmlspecialchars((string) ($filters['title'] ?? ''), ENT_QUOTES) ?>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Identifier') ?></label>
                            <input type="text" name="identifier" class="form-control"
                                   value="<?= htmlspecialchars((string) ($filters['identifier'] ?? ''), ENT_QUOTES) ?>">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label"><?= __('Status') ?></label>
                            <select name="status" class="form-select">
                                <option value=""><?= __('All') ?></option>
                                <option value="1" <?= ($filters['status'] ?? '') === '1' ? 'selected' : '' ?>><?= __('Enabled') ?></option>
                                <option value="0" <?= ($filters['status'] ?? '') === '0' ? 'selected' : '' ?>><?= __('Disabled') ?></option>
                            </select>
                        </div>
                        <div class="col-12 d-flex gap-2">
                            <button type="submit" class="btn btn-outline-primary"><?= __('Search') ?></button>
                            <a href="<?= $pageIndexUrl ?>" class="btn btn-outline-secondary"><?= __('Reset') ?></a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <?php if (!empty($items)): ?>
                        <div class="table-responsive">
                            <table class="table align-middle">
                                <thead>
                                <tr>
                                    <th style="width:50px">ID</th>
                                    <th><?= __('Title') ?></th>
                                    <th><?= __('Identifier') ?></th>
                                    <th><?= __('Status') ?></th>
                                    <th><?= __('Sort Order') ?></th>
                                    <th class="text-end"><?= __('Actions') ?></th>
                                </tr>
                                </thead>
                                <tbody>
                                <?php foreach ($items as $item): ?>
                                    <?php $itemId = (int) ($item['page_id'] ?? 0); ?>
                                    <tr class="<?= $itemId === $editingId ? 'table-primary' : '' ?>">
                                        <td><?= $itemId ?></td>
                                        <td><?= htmlspecialchars((string) ($item['title'] ?? ''), ENT_QUOTES) ?></td>
                                        <td><code class="small"><?= htmlspecialchars((string) ($item['identifier'] ?? ''), ENT_QUOTES) ?></code></td>
                                        <td>
                                            <span class="badge bg-<?= ((int) ($item['status'] ?? 0)) === 1 ? 'success' : 'secondary' ?>">
                                                <?= ((int) ($item['status'] ?? 0)) === 1 ? __('Enabled') : __('Disabled') ?>
                                            </span>
                                        </td>
                                        <td><?= (int) ($item['sort_order'] ?? 0) ?></td>
                                        <td class="text-end">
                                            <a href="<?= $this->getBackendUrl('*/backend/cms/page', ['id' => $itemId]) ?>"
                                               class="btn btn-sm btn-outline-primary"><?= __('Edit') ?></a>
                                            <form method="post" action="<?= $this->getBackendUrl('*/backend/cms/page/save/delete') ?>"
                                                  class="d-inline" onsubmit="return confirm('<?= __('Are you sure you want to delete this page?') ?>')">
                                                <input type="hidden" name="page_id" value="<?= $itemId ?>">
                                                <button type="submit" class="btn btn-sm btn-outline-danger"><?= __('Delete') ?></button>
                                            </form>
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>

                        <?php if ($pageCount > 1): ?>
                            <nav aria-label="Page pagination" class="mt-4">
                                <ul class="pagination justify-content-center mb-0">
                                    <li class="page-item <?= $currentPage <= 1 ? 'disabled' : '' ?>">
                                        <a class="page-link" href="<?= $pageIndexUrl ?>?page=<?= $currentPage - 1 ?><?= !empty($filters['title']) ? '&title=' . urlencode((string) $filters['title']) : '' ?><?= !empty($filters['identifier']) ? '&identifier=' . urlencode((string) $filters['identifier']) : '' ?><?= isset($filters['status']) && $filters['status'] !== '' ? '&status=' . urlencode((string) $filters['status']) : '' ?>">
                                            <?= __('Previous') ?>
                                        </a>
                                    </li>
                                    <?php for ($i = max(1, $currentPage - 2); $i <= min($pageCount, $currentPage + 2); $i++): ?>
                                        <li class="page-item <?= $i === $currentPage ? 'active' : '' ?>">
                                            <a class="page-link" href="<?= $pageIndexUrl ?>?page=<?= $i ?><?= !empty($filters['title']) ? '&title=' . urlencode((string) $filters['title']) : '' ?><?= !empty($filters['identifier']) ? '&identifier=' . urlencode((string) $filters['identifier']) : '' ?><?= isset($filters['status']) && $filters['status'] !== '' ? '&status=' . urlencode((string) $filters['status']) : '' ?>">
                                                <?= $i ?>
                                            </a>
                                        </li>
                                    <?php endfor; ?>
                                    <li class="page-item <?= $currentPage >= $pageCount ? 'disabled' : '' ?>">
                                        <a class="page-link" href="<?= $pageIndexUrl ?>?page=<?= $currentPage + 1 ?><?= !empty($filters['title']) ? '&title=' . urlencode((string) $filters['title']) : '' ?><?= !empty($filters['identifier']) ? '&identifier=' . urlencode((string) $filters['identifier']) : '' ?><?= isset($filters['status']) && $filters['status'] !== '' ? '&status=' . urlencode((string) $filters['status']) : '' ?>">
                                            <?= __('Next') ?>
                                        </a>
                                    </li>
                                </ul>
                            </nav>
                        <?php endif; ?>

                    <?php else: ?>
                        <div class="text-center py-5">
                            <h2 class="h5 mb-2"><?= __('No CMS pages found.') ?></h2>
                            <p class="text-muted mb-0"><?= __('Use the form to create your first CMS page or adjust filters.') ?></p>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>
