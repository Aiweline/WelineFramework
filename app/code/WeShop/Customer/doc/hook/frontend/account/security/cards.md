# Storefront Account Security Cards

Use `WeShop_Customer::frontend::account::security::cards` to inject additional security-management cards into the storefront customer account center.

Typical examples:

- Google account binding
- two-factor authentication settings
- device/session management

Hook implementations should render one or more card-sized blocks that fit inside the account center security grid.
