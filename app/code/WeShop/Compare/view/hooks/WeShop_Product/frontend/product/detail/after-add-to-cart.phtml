<?php
/**
 * Compare CTA for storefront product detail pages.
 *
 * @hook-priority 200
 * @hook-sort-order 0
 * @hook-solo false
 */
$product = (array) ($this->getData('product') ?? []);
$productId = (int) ($product['product_id'] ?? $this->getData('product_id') ?? 0);
if ($productId <= 0) {
    return;
}
?>
<button type="button" class="w-full flex cursor-pointer items-center justify-center overflow-hidden rounded-lg h-12 border border-border-light dark:border-border-dark bg-background-light dark:bg-background-dark text-text-primary-light dark:text-text-primary-dark gap-2 text-base font-bold leading-normal tracking-wide hover:border-primary hover:text-primary transition-colors add-to-compare" data-product-id="<?= $productId ?>">
    <span class="material-symbols-outlined">compare_arrows</span>
    <?= __('Add to Compare') ?>
</button>
