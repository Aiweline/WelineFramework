<?php
/**
 * 订阅管理 - 内容模板
 * @var \Weline\Framework\View\Template $this
 * @var array $items
 * @var array $statistics
 * @var array $status_options
 * @var string $current_status
 */

$title = $title ?? __('订阅管理');
$items = $items ?? [];
$statistics = $statistics ?? [];
$status_options = $status_options ?? [];
$current_status = $current_status ?? '';

$statusBadgeMap = [
    'trialing'  => 'bg-info',
    'active'    => 'bg-success',
    'paused'    => 'bg-warning',
    'past_due'  => 'bg-danger',
    'cancelled' => 'bg-secondary',
    'expired'   => 'bg-dark',
];
?>

<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0 font-size-18"><?= $title ?></h4>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- start page message -->
<div class="row mb-3">
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
        <div id="page-message-container"></div>
    </div>
</div>
<!-- end message -->

<!-- 统计卡片 -->
<div class="row">
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-primary bg-soft">
                            <span class="avatar-title bg-primary rounded-circle">
                                <i class="mdi mdi-refresh-auto font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('总计') ?></p>
                        <h4 class="mb-0"><?= number_format($statistics['total'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-success bg-soft">
                            <span class="avatar-title bg-success rounded-circle">
                                <i class="mdi mdi-check-circle font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('活跃订阅') ?></p>
                        <h4 class="mb-0"><?= number_format($statistics['active'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-info bg-soft">
                            <span class="avatar-title bg-info rounded-circle">
                                <i class="mdi mdi-clock-outline font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('试用订阅') ?></p>
                        <h4 class="mb-0"><?= number_format($statistics['trialing'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm rounded-circle bg-danger bg-soft">
                            <span class="avatar-title bg-danger rounded-circle">
                                <i class="mdi mdi-alert-circle font-size-24"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <p class="text-uppercase fw-medium text-muted mb-0"><?= __('逾期订阅') ?></p>
                        <h4 class="mb-0"><?= number_format($statistics['past_due'] ?? 0) ?></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- 订阅列表 -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h5 class="card-title mb-0"><?= __('订阅列表') ?></h5>
                    <div class="d-flex gap-2">
                        <select class="form-select form-select-sm" id="status-filter" style="width: auto;">
                            <option value=""><?= __('全部') ?></option>
                            <?php foreach ($status_options as $key => $label): ?>
                                <option value="<?= $key ?>" <?= $current_status === $key ? 'selected' : '' ?>><?= $label ?></option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                </div>

                <?php if (empty($items)): ?>
                    <div class="text-center text-muted py-5">
                        <i class="mdi mdi-refresh-auto font-size-48 d-block mb-3"></i>
                        <p><?= __('暂无订阅') ?></p>
                    </div>
                <?php else: ?>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="table-light">
                            <tr>
                                <th>ID</th>
                                <th><?= __('客户ID') ?></th>
                                <th><?= __('订阅计划') ?></th>
                                <th><?= __('订阅价格') ?></th>
                                <th><?= __('计费周期') ?></th>
                                <th><?= __('状态') ?></th>
                                <th><?= __('下次计费') ?></th>
                                <th><?= __('续费次数') ?></th>
                                <th><?= __('创建时间') ?></th>
                                <th><?= __('操作') ?></th>
                            </tr>
                            </thead>
                            <tbody>
                            <?php foreach ($items as $item): ?>
                                <tr>
                                    <td><?= $item['subscription_id'] ?></td>
                                    <td><?= $item['customer_id'] ?></td>
                                    <td><?= $item['plan_id'] ?></td>
                                    <td><?= $item['currency'] ?? 'USD' ?> <?= number_format((float)($item['price'] ?? 0), 2) ?></td>
                                    <td><?= $item['billing_cycle'] ?>/<?= $item['billing_interval'] ?></td>
                                    <td>
                                        <span class="badge <?= $statusBadgeMap[$item['status']] ?? 'bg-secondary' ?>">
                                            <?= $status_options[$item['status']] ?? $item['status'] ?>
                                        </span>
                                    </td>
                                    <td><?= $item['next_billing_at'] ?? '-' ?></td>
                                    <td><?= $item['renewal_count'] ?? 0 ?></td>
                                    <td><?= $item['created_at'] ?></td>
                                    <td>
                                        <a href="<?= $this->getUrl('*/subscription/view', ['id' => $item['subscription_id']]) ?>" class="btn btn-sm btn-outline-primary">
                                            <i class="mdi mdi-eye"></i> <?= __('查看') ?>
                                        </a>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>

                    <!-- 分页 -->
                    <?php if (!empty($pagination)): ?>
                        <div class="mt-3">
                            <template>Weline_Component::pagination.phtml</template>
                        </div>
                    <?php endif; ?>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<script>
(function() {
    'use strict';
    
    document.getElementById('status-filter').addEventListener('change', function() {
        var status = this.value;
        var url = new URL(window.location.href);
        if (status) {
            url.searchParams.set('status', status);
        } else {
            url.searchParams.delete('status');
        }
        url.searchParams.delete('page');
        window.location.href = url.toString();
    });
})();
</script>
