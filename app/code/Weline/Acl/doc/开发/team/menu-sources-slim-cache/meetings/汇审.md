# 汇审 — 菜单源缓存瘦身

日期：2026-09-20  
模块：Weline_Acl `1.1.9`  
结论：通过

## 根因

`enabledMenuSourcesCache` 缓存完整 ACL 行；`resource_metadata` 合计约 15.5MB（393 菜单），诊断里「count=2」实为 `expires`+`data` 两键，不是两条菜单。

## 修复

只缓存建树字段；读写投影并回写旧胖共享条目。实测进程缓存 serialize：15.1MB → **约 138KB**。

## 证据

`ResourceTreeServiceCacheInvalidationTest`：5 tests / 18 assertions PASS。
