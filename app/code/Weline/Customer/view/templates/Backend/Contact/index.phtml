<?php
/** @var \Weline\Framework\View\Template $this */
$inquiries = $inquiries ?? [];
$statusOptions = $status_options ?? [];
$categoryOptions = $category_options ?? [];
$filters = $filters ?? [];
$total = (int)($total ?? 0);
$page = (int)($page ?? 1);
$limit = (int)($limit ?? 20);
$totalPages = (int)($total_pages ?? 0);
$escape = static fn($value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
?>

<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0 font-size-18"><?= __('客户联系表单') ?></h4>
            <div class="text-muted"><?= __('共') ?> <?= $total ?> <?= __('条记录') ?></div>
        </div>
    </div>
</div>

<div class="row mb-3">
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
    </div>
</div>

<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <form method="get" action="<?= $this->getBackendUrl('customer/backend/contact/index') ?>" class="row g-3 align-items-end">
                    <div class="col-md-3">
                        <label class="form-label"><?= __('联系人姓名') ?></label>
                        <input type="text" class="form-control" name="name" value="<?= $escape($filters['name'] ?? '') ?>" placeholder="<?= __('按联系人姓名筛选') ?>">
                    </div>
                    <div class="col-md-3">
                        <label class="form-label"><?= __('联系邮箱') ?></label>
                        <input type="text" class="form-control" name="email" value="<?= $escape($filters['email'] ?? '') ?>" placeholder="<?= __('按联系邮箱筛选') ?>">
                    </div>
                    <div class="col-md-3">
                        <label class="form-label"><?= __('订单号') ?></label>
                        <input type="text" class="form-control" name="order_number" value="<?= $escape($filters['order_number'] ?? '') ?>" placeholder="<?= __('按订单号筛选') ?>">
                    </div>
                    <div class="col-md-2">
                        <label class="form-label"><?= __('处理状态') ?></label>
                        <select class="form-select" name="status">
                            <option value=""><?= __('全部状态') ?></option>
                            <?php foreach ($statusOptions as $statusCode => $statusLabel): ?>
                                <option value="<?= $escape($statusCode) ?>"<?= ($filters['status'] ?? '') === $statusCode ? ' selected' : '' ?>>
                                    <?= $escape($statusLabel) ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                    <div class="col-md-1">
                        <button type="submit" class="btn btn-primary w-100"><?= __('搜索') ?></button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-body">
                <?php if ($inquiries !== []): ?>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle table-nowrap">
                            <thead class="table-light">
                            <tr>
                                <th>ID</th>
                                <th><?= __('联系人') ?></th>
                                <th><?= __('主题 / 分类') ?></th>
                                <th><?= __('订单号') ?></th>
                                <th><?= __('留言') ?></th>
                                <th><?= __('状态') ?></th>
                                <th><?= __('提交时间') ?></th>
                                <th><?= __('操作') ?></th>
                            </tr>
                            </thead>
                            <tbody>
                            <?php foreach ($inquiries as $inquiry): ?>
                                <tr>
                                    <td><?= (int)$inquiry['inquiry_id'] ?></td>
                                    <td>
                                        <div class="fw-semibold"><?= $escape($inquiry['name']) ?></div>
                                        <div class="text-muted small"><?= $escape($inquiry['email']) ?></div>
                                        <?php if ($inquiry['phone'] !== ''): ?>
                                            <div class="text-muted small"><?= $escape($inquiry['phone']) ?></div>
                                        <?php endif; ?>
                                        <?php if ((int)$inquiry['customer_id'] > 0): ?>
                                            <div class="text-muted small"><?= __('客户ID') ?>: <?= (int)$inquiry['customer_id'] ?></div>
                                        <?php endif; ?>
                                    </td>
                                    <td>
                                        <div class="fw-semibold"><?= $escape($inquiry['subject']) ?></div>
                                        <div class="text-muted small"><?= $escape($categoryOptions[$inquiry['category']] ?? $inquiry['category']) ?></div>
                                    </td>
                                    <td><?= $escape($inquiry['order_number'] ?: '-') ?></td>
                                    <td style="min-width: 320px;">
                                        <details>
                                            <summary><?= __('查看留言详情') ?></summary>
                                            <div class="mt-2 text-muted small">
                                                <div><?= nl2br($escape($inquiry['message'])) ?></div>
                                                <?php if ($inquiry['source_url'] !== ''): ?>
                                                    <div class="mt-2"><?= __('来源页面') ?>: <?= $escape($inquiry['source_url']) ?></div>
                                                <?php endif; ?>
                                                <?php if ($inquiry['ip_address'] !== ''): ?>
                                                    <div><?= __('提交IP') ?>: <?= $escape($inquiry['ip_address']) ?></div>
                                                <?php endif; ?>
                                            </div>
                                        </details>
                                    </td>
                                    <td style="min-width: 180px;">
                                        <form method="post" action="<?= $this->getBackendUrl('customer/backend/contact/updateStatus') ?>" class="d-flex gap-2">
                                            <input type="hidden" name="inquiry_id" value="<?= (int)$inquiry['inquiry_id'] ?>">
                                            <select class="form-select form-select-sm" name="status">
                                                <?php foreach ($statusOptions as $statusCode => $statusLabel): ?>
                                                    <option value="<?= $escape($statusCode) ?>"<?= $inquiry['status'] === $statusCode ? ' selected' : '' ?>>
                                                        <?= $escape($statusLabel) ?>
                                                    </option>
                                                <?php endforeach; ?>
                                            </select>
                                            <button type="submit" class="btn btn-sm btn-primary"><?= __('保存') ?></button>
                                        </form>
                                    </td>
                                    <td>
                                        <div><?= $escape($inquiry['created_at']) ?></div>
                                        <?php if ($inquiry['updated_at'] !== '' && $inquiry['updated_at'] !== $inquiry['created_at']): ?>
                                            <div class="text-muted small"><?= __('更新于') ?> <?= $escape($inquiry['updated_at']) ?></div>
                                        <?php endif; ?>
                                    </td>
                                    <td>
                                        <form method="post" action="<?= $this->getBackendUrl('customer/backend/contact/delete') ?>" onsubmit="return confirm('<?= $escape(__('确定要删除该联系单吗？此操作不可撤销。')) ?>')">
                                            <input type="hidden" name="inquiry_id" value="<?= (int)$inquiry['inquiry_id'] ?>">
                                            <button type="submit" class="btn btn-sm btn-danger"><?= __('删除') ?></button>
                                        </form>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>

                    <?php if ($totalPages > 1): ?>
                        <div class="row mt-3">
                            <div class="col-md-6">
                                <div class="text-muted">
                                    <?= __('显示') ?>
                                    <?= (($page - 1) * $limit + 1) ?>
                                    -
                                    <?= min($page * $limit, $total) ?>
                                    <?= __('条') ?>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <nav aria-label="pagination" class="float-end">
                                    <ul class="pagination pagination-rounded mb-0">
                                        <?php if ($page > 1): ?>
                                            <li class="page-item">
                                                <a class="page-link" href="?page=<?= $page - 1 ?>&name=<?= urlencode((string)($filters['name'] ?? '')) ?>&email=<?= urlencode((string)($filters['email'] ?? '')) ?>&order_number=<?= urlencode((string)($filters['order_number'] ?? '')) ?>&status=<?= urlencode((string)($filters['status'] ?? '')) ?>"><?= __('上一页') ?></a>
                                            </li>
                                        <?php endif; ?>
                                        <?php for ($i = max(1, $page - 2); $i <= min($totalPages, $page + 2); $i++): ?>
                                            <li class="page-item <?= $i === $page ? 'active' : '' ?>">
                                                <a class="page-link" href="?page=<?= $i ?>&name=<?= urlencode((string)($filters['name'] ?? '')) ?>&email=<?= urlencode((string)($filters['email'] ?? '')) ?>&order_number=<?= urlencode((string)($filters['order_number'] ?? '')) ?>&status=<?= urlencode((string)($filters['status'] ?? '')) ?>"><?= $i ?></a>
                                            </li>
                                        <?php endfor; ?>
                                        <?php if ($page < $totalPages): ?>
                                            <li class="page-item">
                                                <a class="page-link" href="?page=<?= $page + 1 ?>&name=<?= urlencode((string)($filters['name'] ?? '')) ?>&email=<?= urlencode((string)($filters['email'] ?? '')) ?>&order_number=<?= urlencode((string)($filters['order_number'] ?? '')) ?>&status=<?= urlencode((string)($filters['status'] ?? '')) ?>"><?= __('下一页') ?></a>
                                            </li>
                                        <?php endif; ?>
                                    </ul>
                                </nav>
                            </div>
                        </div>
                    <?php endif; ?>
                <?php else: ?>
                    <div class="text-center text-muted py-5">
                        <i class="mdi mdi-card-account-mail-outline" style="font-size: 48px;"></i>
                        <p class="mt-3"><?= __('暂无客户联系单') ?></p>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
