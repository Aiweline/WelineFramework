<?php
$loginUrl = (string) ($this->getData('login_url') ?? '/customer/account/login');
?>
<div class="login-form-container">
    <div class="login-header">
        <div class="login-icon">
            <i class="fas fa-user-plus"></i>
        </div>
        <h1 class="login-title"><?= __('Create Account') ?></h1>
        <p class="login-subtitle"><?= __('Create your WeShop account with your email address.') ?></p>
    </div>

    <form action="/customer/account/register" method="post" class="login-form">
        <?= $this->getFormKey('/customer/account/register') ?>

        <div class="row g-3">
            <div class="col-6">
                <div class="form-group-enhanced">
                    <label for="firstname"><i class="fas fa-id-card"></i><?= __('First Name') ?></label>
                    <div class="form-input-wrapper">
                        <i class="fas fa-user"></i>
                        <input type="text" id="firstname" name="firstname" required autocomplete="given-name" placeholder="<?= __('Ada') ?>" />
                    </div>
                </div>
            </div>
            <div class="col-6">
                <div class="form-group-enhanced">
                    <label for="lastname"><i class="fas fa-id-card"></i><?= __('Last Name') ?></label>
                    <div class="form-input-wrapper">
                        <i class="fas fa-user"></i>
                        <input type="text" id="lastname" name="lastname" required autocomplete="family-name" placeholder="<?= __('Lovelace') ?>" />
                    </div>
                </div>
            </div>
        </div>

        <div class="form-group-enhanced">
            <label for="email"><i class="fas fa-envelope"></i><?= __('Email Address') ?></label>
            <div class="form-input-wrapper">
                <i class="fas fa-envelope"></i>
                <input type="email" id="email" name="email" required autocomplete="email" placeholder="<?= __('you@example.com') ?>" />
            </div>
        </div>

        <div class="form-group-enhanced">
            <label for="password"><i class="fas fa-lock"></i><?= __('Password') ?></label>
            <div class="form-input-wrapper">
                <i class="fas fa-key"></i>
                <input type="password" id="password" name="password" required minlength="8" autocomplete="new-password" placeholder="<?= __('At least 8 characters with letters and numbers') ?>" />
            </div>
        </div>

        <div class="form-group-enhanced">
            <label for="confirm_password"><i class="fas fa-check-double"></i><?= __('Confirm Password') ?></label>
            <div class="form-input-wrapper">
                <i class="fas fa-check-double"></i>
                <input type="password" id="confirm_password" name="confirm_password" required autocomplete="new-password" placeholder="<?= __('Re-enter your password') ?>" />
            </div>
        </div>

        <div class="remember-section">
            <label class="d-flex align-items-start gap-2" for="agree_terms">
                <input type="checkbox" id="agree_terms" name="agree_terms" value="1" required style="margin-top: 0.2rem;" />
                <span><?= __('I agree to the terms of service and privacy policy.') ?></span>
            </label>
        </div>

        <button type="submit" class="btn-login">
            <?= __('Create Account') ?>
        </button>
    </form>

    <div class="login-footer">
        <p>
            <?= __('Already have an account?') ?>
            <a href="<?= htmlspecialchars($loginUrl, ENT_QUOTES) ?>">
                <i class="fas fa-sign-in-alt me-1"></i><?= __('Sign in') ?>
            </a>
        </p>
    </div>
</div>
