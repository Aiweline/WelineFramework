<?php
/** @var \Weline\Framework\View\Template $this */

$entries = is_array($this->getData('social_login_guide_entries') ?? null) ? $this->getData('social_login_guide_entries') : [];
$activeEntry = is_array($this->getData('social_login_guide_entry') ?? null) ? $this->getData('social_login_guide_entry') : [];
$activeCode = strtolower(trim((string) ($activeEntry['code'] ?? '')));
$pageType = (string) ($this->getData('social_login_guide_page_type') ?? '');
$escape = static fn(string $value): string => htmlspecialchars($value, ENT_QUOTES, 'UTF-8');
?>
<aside class="amazon-payment-guide-layout__sidebar" aria-label="@lang(社媒登录导航)" weline-code="customer.social_login.guide.sidebar">
    <h2 class="amazon-payment-guide-layout__sidebar-title"><lang>社媒登录</lang></h2>
    <ul class="amazon-payment-guide-layout__nav">
        <li>
            <a class="amazon-payment-guide-layout__nav-link<?= $activeCode === '' ? ' is-active' : '' ?>"
               href="@url{'guide/social-login'}"
               <?= $activeCode === '' ? 'aria-current="page"' : '' ?>>
                <lang>全部提供方</lang>
            </a>
        </li>
        <?php foreach ($entries as $entry): ?>
            <?php
            if (!is_array($entry)) {
                continue;
            }
            $code = strtolower(trim((string) ($entry['code'] ?? '')));
            $providerTitle = (string) ($entry['title'] ?? '');
            $guideTitle = (string) ($entry['guide_title'] ?? '');
            $policyTitle = (string) ($entry['policy_title'] ?? '');
            $guideRoute = (string) ($entry['guide_route'] ?? '');
            $policyRoute = (string) ($entry['policy_route'] ?? '');
            $isExpanded = $code !== '' && $code === $activeCode;
            ?>
            <li class="amazon-payment-guide-layout__nav-group<?= $isExpanded ? ' is-expanded' : '' ?>">
                <?php if ($isExpanded): ?>
                    <span class="amazon-payment-guide-layout__nav-group-label"><?= $escape($providerTitle) ?></span>
                    <ul class="amazon-payment-guide-layout__nav-group-children">
                        <li>
                            <a class="amazon-payment-guide-layout__nav-article<?= $pageType === 'guide' ? ' is-active' : '' ?>"
                               href="@url{$guideRoute}"
                               <?= $pageType === 'guide' ? 'aria-current="page"' : '' ?>>
                                <?= $escape($guideTitle) ?>
                            </a>
                        </li>
                        <li>
                            <a class="amazon-payment-guide-layout__nav-article<?= $pageType === 'policy' ? ' is-active' : '' ?>"
                               href="@url{$policyRoute}"
                               <?= $pageType === 'policy' ? 'aria-current="page"' : '' ?>>
                                <?= $escape($policyTitle) ?>
                            </a>
                        </li>
                    </ul>
                <?php else: ?>
                    <a class="amazon-payment-guide-layout__nav-link" href="@url{$guideRoute}">
                        <?= $escape($providerTitle) ?>
                    </a>
                <?php endif; ?>
            </li>
        <?php endforeach; ?>
    </ul>
</aside>
