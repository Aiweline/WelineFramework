# Weline_Customer 需求

> 本文是 Customer 当前已确认需求与业务逻辑的 owner；存量未验证行为列为待确认，不补造历史。

## 文档信息

- 模块：`Weline_Customer`
- 当前基线版本：`1.0.0`
- 下一目标版本：`1.0.1`
- 最后确认人：用户
- 最后确认日期：2026-08-11

## 需求索引

| 需求 ID | 标题 | 状态 | 引入版本 | 最近变更版本 | 验收状态 | 关联功能文档 |
|---|---|---|---|---|---|---|
| `REQ-CUSTOMER-0001` | 前台每设备记住登录接入 | confirmed | `1.0.1` | `1.0.1` | passed | `Weline_SessionManager/doc/设备管理架构.md` |
| `REQ-CUSTOMER-0002` | 找回密码页区块可被稳定定位 | confirmed | `1.0.1` | `1.0.1` | passed | `doc/README.md` |

## 当前有效需求

### `REQ-CUSTOMER-0002` 找回密码页区块可被稳定定位

- 来源与确认：2026-08-22 完整前端规范检查发现找回密码页 `<section>` 缺少 `weline-code`；用户要求缺少即修复。
- 业务逻辑：找回密码主区块使用稳定语义码 `customer.account.password_recovery`。
- 范围：仅源模板 section 属性；不改表单、认证、验证码、样式或交互。
- 验收标准：`frontend:check-section-code` 不再报告该模板，页面经独立 WLS 可访问。

### `REQ-CUSTOMER-0001` 前台每设备记住登录接入

- 来源与确认：用户于 2026-08-11 确认多端登录与设备管理实施计划。
- 目标版本：`1.0.1`。
- 用户与运营场景：Customer 在多浏览器登录时，每个设备拥有独立的记住登录凭证；下线其中一个设备不影响其他设备。
- 业务逻辑：设备 Provider 可用时，密码登录沿用正常 Customer 登录并为当前设备签发 `w_frontend_ut`；自动登录先解析新凭证并恢复原设备，成功后轮换凭证；已登录用户可把经确认归属的旧 `w_ut` 惰性迁移。
- 范围：Customer 记住登录签发、自动恢复、退出清理和重复签发入口收口。
- 非目标：Customer 保存设备 ORM、直接渲染设备管理 UI、管理后台设备或查看其他客户设备。
- 数据/权限/安全：跨模块只依赖 Framework 发布的中立契约；原始 Token 仅进入 HttpOnly Cookie；Provider 声明故障时不得回退旧 Token。
- 兼容/迁移：Provider 未配置时保留现有 `CustomerToken`/`w_ut` 行为；Provider 可用后，仅在确认旧 Token 属于当前 Customer 时迁移并删除旧行。
- 验收标准：两台前台设备可独立记住登录；撤销 B 后 B 不能自动恢复，A 保持登录；frontend/backend Cookie 不串域。
- WebUI/E2E：两个隔离浏览器上下文完成登录、设备列表、远端下线和被下线后的登录跳转。
- 运营与支持影响：迁移后回滚旧代码可能要求用户重新输入密码，不恢复已删除旧凭证。
- 依赖与风险：现有多个记住登录签发入口必须统一使用同一服务，避免部分路径继续全用户覆盖 Token。

## 版本变更记录

### 1.0.1 — 2026-08-11

| 需求 ID | 变更前 | 变更后 | 业务逻辑/原因 | 用户决定 | 影响范围 |
|---|---|---|---|---|---|
| `REQ-CUSTOMER-0001` | 每用户单个共享 `w_ut` | Provider 可用时每设备独立凭证并兼容惰性迁移 | 支持多端并行和单设备下线 | confirmed | 登录、自动登录、退出、remember 签发 |

## 待确认

| 项目 | 现有证据 | 不确定点 | 需要谁确认 | 阻断范围 |
|---|---|---|---|---|
| 历史 `w_ut` 实际存量 | `CustomerToken` 当前每用户删除旧行再签发 | 线上有效旧凭证规模 | 运营 | 不阻断惰性兼容 |
