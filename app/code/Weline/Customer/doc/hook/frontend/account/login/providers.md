# Storefront Customer Login Providers

Use `Weline_Customer::frontend::account::login::providers` to inject third-party sign-in providers into the storefront customer login page without coupling `Weline_Customer` to optional provider modules.

Implementation path example:

- `view/hooks/Weline_Customer/frontend/account/login/providers.phtml`
