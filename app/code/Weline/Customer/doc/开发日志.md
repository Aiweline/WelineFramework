# Weline_Customer 开发日志

## Fix — 2026-09-03：个人中心版心与顶栏左右不对等

- 问题：版心左右间距相对上方 header 容器不对齐，观感不舒服。
- 根因：账户布局未套 `.w-container` 公式，主区另加水平 padding。
- 修复：见 Theme `account/dashboard` / `default` 版心对齐。
- 验收：`/customer/account/index#profile` 侧栏左缘与主区右缘对齐顶栏内容缘。

## Fix — 2026-09-03：个人中心主区白卡套红欢迎条

- 问题：去掉内层 card 后仍见套层——`.account-main` 白底壳内再放红色欢迎条。
- 修复：Theme `account/dashboard`（及 `default`）主区改为透明无圆角阴影；`account-index.css` 恢复 `.account-card` 为页面底色上的单层内容白卡。
- 验收：`/customer/account/index#profile` 红欢迎条直接落在灰底上，其下资料卡单层；侧栏白卡保留。

## Fix — 2026-09-03：个人中心主内容白卡套白卡

- 问题：个人中心主区 `.account-main` 白底内再套 `.account-card`（白底+米色头条），观感像套了两层。
- 修复：曾压平 `.account-card`；后续确认真正套层是主区白壳，已由上条修正。
- 验收：见上条。

## Fix — 2026-09-03：前台登录后不久掉线（共享 Session + session_rebound）

- 现象：前台已登录，同浏览器再登后台或 Session regenerate 后，前台很快被踢。
- 根因：前后台共用 `WELINE_SESSID`；后台 `login` regenerate 换 sess id，前台设备行 `session_digest` 未跟随 → `auth_device_rejected` / `session_rebound`。
- 修复：见 `Weline_SessionManager` `1.2.1`（validate 同设备同 install_key 时 rebind）。
- 验收：SessionManager 单测轮换用例；同浏览器先后台再访前台应保持客户登录。

## Fix — 2026-09-02：header-account-links 访客默认可见性

- 问题：钩子按 Session `isLoggedIn` SSR 已登录菜单项，登录页 header 混出「退出登录」。
- 修复：双态项固定访客可见、已登录 `hidden`；由 `applyHeaderMenuAuth` 升迁。
- 验收：`HeaderAccountLinksTemplateTest`；登录页 SSR 不含 Session 驱动已登录壳。

## Fix — 2026-09-02：个人中心半登录态

- 问题：能进个人中心并显示「已登录」，点「我的订阅」却 toast「请先登录」；账户标识可为 #0。
- 根因：Session `isLoggedIn` 接受 `login_id=0`；`getUser` 可缓存空 Customer；侧栏仅 `instanceof Customer` 即标已登录；账户页只查 `isLoggedIn`。
- 修复：`loginCustomer` 强制 reload 有效主键；账户 Index 无有效用户则 logout+跳登录；侧栏「已登录」要求 `customer_id>0`。
- 验收：半登录会话不得停留在个人中心；有效登录后侧栏分区不再「请先登录」。

## Fix — 2026-09-02：checkout-success-guest-convert 空 HTML 健康误报

- 问题：结账成功页 DEV toast「checkout-success-guest-convert: 部件 HTML 为空」。
- 根因：订单 `already_bound` / 已登录 / 缺参等跳过路径 `return` 空串；`WidgetHtmlHealthInspector` 报 `empty_html`。
- 修复：对齐 `featured-products`——输出 `hidden` 空态壳（`checkout-success-guest-convert-empty` + `data-skip-reason`），视觉仍隐藏。
- 验收：`CheckoutSuccessGuestConvertWidgetContractTest`；成功页不再 empty_html toast。

## Fix — 2026-09-02：转化点击「不满足操作授权要求」

- 根因：`convertGuestCheckout` 描述符 `auth=guest`，网关要求未登录；成功页会话/缓存不一致时已登录仍见按钮 → 403。
- 变更：与 `login/register` 对齐改为 `auth=any`；业务层继续拒绝已登录；`must_set_password` 时返回设密页 redirect。
- 交付：结账成功页「登录并保存订单」。

## Fix — 2026-09-02：设密页 404（转化跳转失败）

- 根因：新增 `Controller/Account/SetPassword` 后 `generated/routers` 未含路由；转化成功跳转 `/customer/account/set-password` → 404。
- 处理：`RouteUpdateService` 更新 `Weline_Customer` 路由并重启 WLS；未登录访问该路径应 302 登录页，登录且 `must_set_password` 时展示设密表单。
- 交付：结账成功页「登录并保存订单」→ 设密页。

## Feature — 2026-09-02：结账成功页访客转化部件

- 结账成功 CTA 上方由 Checkout 释放 `checkout-success-guest-account` 空槽；本模块应用部件默认注入。
- 新邮箱：`checkout_token` 建户 + Session + `must_set_password` + `/customer/account/set-password`；认领本单与同 group。
- 已有邮箱：同槽「去登录」；订单已有 `customer_id` 时部件不渲染（客户模块识别）。
- 版本：`1.0.14` → `1.0.15`。

## Hotfix — 2026-09-01：已登录顶栏下拉仍显示登录/注册

- 当前状态：`implemented`
- 问题：FPC/游客 SSR 后 JS soft-reconcile 切到「您好, 名字」，下拉仍是登录/注册。
- 变更：`header-account-links` 双态输出 `data-account-menu-auth`；Frontend `applyHeaderMenuAuth` 随 `data-auth-state` 切换。
- 版本：`1.0.13` → `1.0.14`。

## Hotfix — 2026-09-01：页头问候用显示名而非邮箱

- 当前状态：`implemented`
- 问题：登录后顶栏「您好, email」过长（`session->getUsername()` 为鉴权邮箱）。
- 变更：`Customer::getDisplayName()` 优先非邮箱形 `username`，否则取邮箱 `@` 前本地名；`account.current` payload 增 `display_name`。
- 版本：`1.0.12` → `1.0.13`。
- 协同：Theme Header SSR、Frontend `resolveHeaderDisplayName`。

## Hotfix — 2026-08-31：页脚帮助中心注入账户/订单

- 当前状态：`implemented`
- 变更：新增 `footer-my-account-link` / `footer-my-orders-link`，经 `default_injections` 注入 Theme `footer-help-links`；分别指向 `customer/account/index` 与 `#orders`。
- 版本：`1.0.11` → `1.0.12`。

## 2026-08-30 — 退出登录改用主题确认弹窗

- 当前状态：`fixed`
- 问题：上次为防取消退出回退了 `window.confirm`；用户要求必须用主题弹窗。
- 变更：`account-logout.js` 优先 `Theme.Notice.confirm`，否则用已加载的 `Weline.UI.dialog.confirm`（主题 UI）；短暂等待 UI 就绪；**禁止**原生 `window.confirm`。
- 版本：`1.0.10` → `1.0.11`。
- 验收：`AccountLogoutConfirmFallbackContractTest`；账户页退出弹出 `w-dialog` 主题框。

## 2026-08-30 — 退出登录 Notice 不可用时取消退出

- 当前状态：`fixed`
- 问题：账户侧栏/页头点「退出登录」控制台报 `Notice confirmation is unavailable; logout cancelled.`，无法退出。账户布局常未挂载 `theme.js` 的 `Weline.Theme.Notice`。
- 变更：`account-logout.js` 依次尝试 Theme.Notice / Weline.Notice / Theme / Toast / `Weline.UI.confirm`，最终回退 `window.confirm`；禁止因确认 UI 缺失而取消退出。
- 版本：`1.0.8` → `1.0.9`。
- 验收：`AccountLogoutConfirmFallbackContractTest`；账户页确认后可跳转 logout。

## 2026-08-30 — 首页登录态：FPC 候选 Cookie + account guest 软对账

- 当前状态：`implemented`
- 问题：登录后首页头栏仍「登录/注册」；控制台已立即加载 `account`。
- 协同修复：Framework FPC `cookieHeaderHasLoggedInFrontendSession` 对齐 `requestCookieCandidates`；Frontend `weline-api-account.js` 对 guest SSR 软对账 `account.current`。
- 验收：已登录打开首页应 SSR 登录壳或由 Account 模块切壳；`FullPageCacheCoordinatorTest` + Account 模块契约。

## 2026-08-30 — 两步验证页有码仍提示请输入验证码

- 当前状态：`implemented`
- 问题：部件渲染时 `challenge_token` 常为空；服务端把空令牌与空验证码都报成「请输入验证码。」；Flash 提示输入后不消失。
- 变更：表单从 Request/URL 回填令牌；JS 提交强制写入 `challenge_token`+纯数字 `code` 并清除 flash；控制器区分令牌/验证码缺失。
- 版本：`1.0.7` → `1.0.8`。

## 2026-08-30 — 两步验证独立布局 + Theme 部件 + document POST

- 当前状态：`implemented`
- 问题：challenge 页走 account.auth 但无登录舞台部件，手写粉橙 CSS、背景顶死；JS 走 QueryBin `completeChallenge`，Session challenge token 不可见导致卡在「验证中…」。
- 变更：`layoutType=account.challenge` + `challenge-shell`；表单改 `w-auth-login`；提交改 document `fetch(form.action)`；舞台由 Theme `form/account-challenge` 承载。
- 版本：`1.0.6` → `1.0.7`。

## 2026-08-30 — 侧栏 AJAX 注入后加载 data-weline-load 并支持分区软刷新

- 当前状态：`implemented`
- 问题：`account.sidebar.content` 经 AJAX 注入后，首屏 `data-weline-load` 扫描不会重跑，两步验证等模块脚本未挂上，表单原生 POST 整页出 JSON。
- 变更：`account-index.js` 注入后调用 `loadDeclaredSidebarModules`；提供 `weline:account-sidebar-section-reload` 软刷新分区。
- 版本：`1.0.5` → `1.0.6`。

## 2026-08-30 — 个人中心侧栏分组 Hook

- 当前状态：`implemented`
- 变更：Customer 宿主输出 security/commerce/addresses/connections/developer 分组壳；业务模块改挂 `account.sidebar.group.*`；空组前端剪枝；`account.sidebar` 保留为兼容「更多」。
- 版本：`1.0.4` → `1.0.5`。

## 2026-08-30 — 页头账户部件补 data-weline-load（Theme）

- 当前状态：`implemented`
- 问题：登录态刷新依赖 `account`，但 Theme `widgets/header/account` 根节点未声明，部件扫描列表无 account。
- 变更：Theme 部件根 `data-weline-load="api,account"`；Frontend `weline.js` 补 `Account.handleAuthRefreshSignal` 代理；`weline-api-account.js` 在模块加载且存在页头账户根时自触发一次 `w_auth` 刷新。
- 验收：首页「立即加载」含 `api`/`account`；带 `w_auth=1` 回跳后页头切已登录壳。

## 2026-08-30 — 账户页 JS 改 modules + data-weline-load

- 当前状态：`implemented`
- 变更：`view/statics/frontend/weline.modules.js` 注册 `customerAccount` / `customerLogout`；登录/注册等 `data-weline-load="api,account"`；账户首页/侧栏去掉裸 `<js>`。
- 注意：模块名须裸标识符（编译器不合并 `"hyphen-keys"`）。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。

## 2026-08-30 — 登录页误报 Response terminate with status 302

- 当前状态：`implemented`
- 问题：`ForgotPassword` / `Register` 在 `try` 内调用 `$this->redirect()`，`RedirectException`（文案即 `Response terminate with status 302`）被 `catch (\Throwable)` 当成业务失败写入 MessageManager flash（标题「错误！」），登录/认证页渲染出来。
- 变更：业务 `try` 只包服务调用；`ResponseTerminateException` 原样抛出；成功/失败 redirect 移到 `catch` 之外。
- 验收：`AuthRedirectTerminateRethrowContractTest`；成功重置应 302 到登录且无 terminate flash。

## 2026-08-30 — w_auth 刷新改由 Weline.Account 处理

- 当前状态：`implemented`
- 问题：独立 `header-account.js` 挂在 body-end，违背「账户操作由 Account 对象统一处理、theme.js 加载」约定。
- 变更：逻辑迁入 `weline-api-account.js::handleAuthRefreshSignal`；theme.js / Frontend weline.js 代理；移除 body-end 独立脚本。
- 验收：无 `header-account.js` 全局挂载；`Weline.Account.handleAuthRefreshSignal` 可处理 `w_auth=0|1`。

## 2026-08-30 — 登出/登录失效补 w_auth=0

- 当前状态：`implemented`
- 问题：上次只处理登录成功 `w_auth=1`；退出后若落地页仍是已登录 chrome（或后续改跳首页 FPC），页头不会回游客壳。
- 变更：`formatAuthInvalidRedirect()` → `w_auth=0`；Logout 控制器与 `account.logout` 使用；页头 JS 对 `0/1` 均查一次 `account.current`。
- 验收：退出跳转含 `w_auth=0`；`CustomerAuthReturnUrlServiceTest` 覆盖登录路由上的失效信号。

## 2026-08-30 — 登录成功跳转打 w_auth，页头刷新登录态（FPC）

- 当前状态：`implemented`
- 问题：首页等公共 FPC 命中游客 HTML；登录后跳回仍显示「登录/注册」。
- 变更：`CustomerAuthReturnUrlService::formatAuthSuccessRedirect()` 追加 `w_auth=1`；登录/注册/2FA 成功 redirect 统一走该 API；契约见 `storefront-auth-return.md`。
- 验收：成功 redirect 含 `w_auth=1`；`CustomerAuthReturnUrlServiceTest` 覆盖信号与 query/fragment 保留。

## 2026-08-29 — API 登录成功整页仍踢回 login（Session Cookie 名分裂）

- 当前状态：`implemented`
- 问题：`account.login` 经 QueryBin Worker 写 Cookie 在内嵌浏览器不可靠；另有 `WELINE_SESSID_{port}` 与 `_w0` 分裂。
- 变更：登录改为文档级 `fetch` POST 到 `Login::postIndex`（JSON）；Framework Session 候选 Cookie 回读/重写/登出清理见同日 Framework 条目。
- 验收：按钮登录有 loading；成功后 `/en_US/customer/account` 停留 Personal center。

## 2026-08-29 — 登录按钮无 loading / 登不上（双挂载消费验证码）

- 当前状态：`implemented`
- 问题：Theme `account-login` 部件 dock 与 `login.phtml` 卡片同时 `data-w-component="account-login"`，一次提交触发两次 `account.login`，验证码 token 单次失效；另有 API 不可用时静默整页 POST，失败跳转可叠成 `/en_US/en_US/...`。
- 变更：dock 去掉组件挂载（注册页同理）；`account-login.js` 增加 in-flight 守卫且始终 `preventDefault`；同步 Theme UI 包与缓存串；`CustomerAuthReturnUrlService` 折叠重复 locale 前缀；契约/单测更新。
- 验收：登录页仅一处 `account-login` 挂载；点击登录仅一次 Query；按钮出现「登录中...」/`aria-busy`；错误回跳无双 locale。

## 2026-08-29 — 前台登录几分钟掉线：会话滑动续期

- 当前状态：`implemented`
- 问题：登录后短暂活动即掉线；根因含 WLS Session Server 默认 `session_ttl=3600` 未对齐 `session.lifetime`、干净请求不 touch、文件镜像 mtime 不滑动。
- 变更：Session flush 活动 touch；FileStorage 读后 touch；Session Server TTL 回落对齐 lifetime；Remember 续签见同日 SessionManager/Framework 条目。
- 验收：登录后持续浏览个人中心 >10 分钟仍保持登录；Session Server 日志 `session_ttl` 与 `session.lifetime` 一致（未显式覆盖时）。

## 2026-08-29 — 账户侧栏 w:slot position 非法导致整页 Fatal

- 当前状态：`implemented`
- 问题：`side.phtml` 使用 `position="status|nav-before|nav-after"`，Taglib 仅允许 `header/content/footer/sidebar/dashboard-*`，登录后 `/customer/account` 抛 `TemplateException`。
- 变更：status→`header`，nav 前后→`sidebar`，footer 保持；版本串 `sidebar-slots-2`。
- 验收：登录后账户页 200，无 w:slot position 报错。

## 2026-08-29 — 个人中心侧栏 Slot / 登录状态 / 菜单修复

- 当前状态：`implemented`
- 问题：
  1. 多模块 `account.sidebar` hook 的 `href=\"@url...\"` 把 hash 渲成 `#devices\"`，分区无法切换；
  2. 装饰圆点箭头 + CSS 只隐藏 `i` 不隐藏 `svg.w-icon`，子菜单左右双圆点、订阅图标重叠；
  3. 侧栏缺少 Theme Slot，其他应用难以插入部件。
- 变更：修正各模块 hook href；去掉 circle 箭头；CSS 隐藏 `.w-icon`（父级子项仍显示）；`side.phtml` 增加 `account-sidebar-status/nav-before/nav-after/footer` Slot 与「已登录」状态。
- 验收：`AccountSidebarHookHostTest`；登录后 `/en_US/customer/account#devices` 无尾部引号并切换设备分区；侧栏无 stray 圆点。

## 2026-08-29 — 登录验证码一直不变

- 当前状态：`implemented`
- 问题：登录表单 `captcha="required"` SSR 挑战被缓存复用，多次刷新同一图码。
- 修复：登录改 `captcha="lazy"`；`account-login.js` 拉取 `weline_captcha/frontend/challenge` 并支持刷新。
- 验收：`/en_US/customer/account/login` 无固化 `captcha_token`，挑战图可刷新变化。

## 2026-08-29 — 注册页 @url 管道参数 TypeError

- 当前状态：`implemented`
- 问题：带 `redirect_url`（含 referer 捕获）时 `@url{'…'|['redirect_url'=>$x]}` 编译成按位或，500。
- 根因：Framework `Taglib` `@url{}` 内联未解析 `|`；已在 Framework 修复。
- 验收：`/en_US/customer/account/register`（含 redirect/referer）HTTP 200。

## 2026-08-29 — 登录/注册/忘记密码地址改用 url 标签

- 当前状态：`implemented`
- 问题：Theme 部件渲染登录表单时拿不到控制器 `register_url` 等赋值，模板回退硬编码 `/customer/account/*`，英文站丢失 `/en_US` 前缀。
- 修复：`login`/`register`/`forgot-password` 的 `href`/`action`/`data-*-url`/`reset_url` 一律 `@url{'customer/account/...'}`；契约测试更新。
- 验收：英文登录页注册/忘记密码/表单 action 含 `/en_US/`。

## 2026-08-29 — 登录/注册页英文仍显示中文标题副标题

- 当前状态：`implemented`
- 问题：Theme `account-login` 把中文默认 `title`/`subtitle` 注入表单后原样输出，绕过 `<lang>` 编译期烘焙；英文站 header 已英文化，表单标题仍为「登录」「使用您的账户继续购物」。
- 修复：默认文案不再注入，回退 `login.phtml`/`register.phtml` 的 `<lang>`；自定义 Theme Editor 文案仍 `__()`；契约测试 `AccountAuthCopyI18nContractTest`。清 `view/tpl` 后英文页标题/副标题/验证码标签英文化。
- 验收：`https://p05113ef3.weline.test:9555/en_US/customer/account/login` → title `Log in`、subtitle `Use your account to continue shopping`。

## 2026-08-28 — 登录改为有边界部件卡片（非整页布局）

- 当前状态：`implemented`
- 范围：`account-login` 外层为居中部件卡片（圆角/描边/阴影），分栏仅发生在卡片内部；页面灰底留白；表单去掉嵌套卡片边框。
- 验收：登录区是独立部件块，非整页左右劈开；PC 卡片内左右栏；窄屏上下堆叠。

## 2026-08-28 — 登录左右分栏（左营销 / 右表单）

- 当前状态：`implemented`
- 范围：Theme `account-login` 改为左右布局；左侧 `background_image` 营销图，无图用主色+点缀色渐变；右侧登录卡片；平板以下上下堆叠。
- 验收：PC 可见左右两栏；无图时左侧非纯白色块渐变；主题编辑器可换营销图。

## 2026-08-28 — 登录卡片顶对齐（亚马逊布局）

- 当前状态：`implemented`
- 范围：舞台由 `align-items:center` 改为 `flex-start`；去掉大 `min-height` 垂直居中；上边距用 `--weline-layout-spacing-xl`，卡片靠头栏下方。
- 验收：PC/平板登录卡贴近内容区上方，不再悬在视口正中。

## 2026-08-28 — 登录舞台间距与无图渐变

- 当前状态：`implemented`
- 范围：舞台上下 padding 用 `--weline-layout-spacing-3xl`；取消 `.weline-main-content` 白底；无图默认 `#f3f4f4 → #eaeded → #e3e6e6` 亚马逊浅灰渐变；卡片标题区内边距改主题 spacing。
- 验收：PC(1280) 头栏→卡片约 84px；平板(768) 约 72px；computed `background-image` 为浅灰三段渐变。

## 2026-08-28 — 登录舞台改为 Theme 模板内嵌部件

- 当前状态：`implemented`
- 范围：背景图配置改为 Theme `form/account-login` 在 `account/auth` 槽内嵌；移除 Customer `default_injections` 与 Customer 侧部件定义；`Login` 仍用 `login-shell`。
- 验收：主题编辑器点选登录部件可换 `background_image`；布局门禁允许（Theme 归属）；登录页含 `account-login-widget`。

## 2026-08-28 — 登录页部件化 + 可配置背景图

- 当前状态：`superseded`（由「Theme 模板内嵌」取代应用注入）
- 范围：曾用 Customer `default_injections`；已改为 Theme 布局内嵌。
- 验收：见上条。

## 2026-08-28 — 登录页亚马逊风格 + 默认主题头尾

- 当前状态：`implemented`
- 范围：`login.phtml` / `account-login.css` 对齐亚马逊 Sign-in 视觉（黄 CTA、边框卡片、次级注册按钮）；`Login` meta 默认 `showHeader`/`showFooter=true`，依赖 `account.auth` 布局 Partials。
- 验收：登录页含主题 header/footer；平板/PC 断点卡片可读。

## 2026-08-27 — 登录按钮 icon 水平对齐

- 当前状态：`implemented`
- 范围：`account-login.css` 为 `[data-w-login-idle-label]` / `[data-w-login-busy-label]` 增加 `inline-flex`，避免 login icon 叠在「登录」文字上方。

## 2026-08-26 — 登录页 Weline UI 2.0 重写

- 当前状态：`implemented`
- 范围：登录页 `login.phtml` 对齐 `forgot-password` 的 `w-card` + `w-form` 模式；新增 `account-login` 组件 CSS/JS；原型见 `doc/原型设计.md`。
- 移除模板内联样式，依赖主题 `auth-form-wflash.css` 与 `weline-customer-account-login` 页面包。

## 2026-08-22 — 找回密码页 section 语义标识修复

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-CUSTOMER-0002`
- 范围：仅为找回密码源模板添加 `customer.account.password_recovery`，保留用户现有认证页面改动。
- 验收：全量前端 section 规范检查 0 违规、主题编译、完整路由升级与独立 WLS 页面冒烟通过。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.1` | implemented_validated | `REQ-CUSTOMER-0001` | 2026-08-11 | 2026-08-11 | automated + frontend Browser pass |

## `1.0.1` — 前台每设备记住登录接入

- 基线版本：`1.0.0`
- 目标版本：`1.0.1`
- 当前状态：`implemented_validated`
- 需求 ID：`REQ-CUSTOMER-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：Customer 记住登录编排；设备数据/UI 归 SessionManager，不新增 Customer Schema。
- 负责人/确认人：主代理直接实施 / 用户。

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-customer-0001-前台每设备记住登录接入` | 2026-08-11 |
| 实现执行者/模型/范围已记录 | pass | 主代理直接实施，限定 remember/login/logout 入口 | 2026-08-11 |
| 架构/缺陷/安全复审通过 | pass | 安全扫描 `95c397ee-7f3e-4ac5-ad01-54acd8819510`；remember 轮换、迁移与撤销复审通过 | 2026-08-11 |
| 分层测试通过 | pass | 多模块聚焦 PHPUnit 合计 `149 tests / 544 assertions`；PHP/XML/JS 静态检查通过 | 2026-08-11 |
| 真实路径 WebUI/E2E 通过 | pass | 两个隔离上下文自动 E2E 与内置 Browser 登录、设备页、刷新/退出路径通过；console 0 error / 0 warn | 2026-08-11 |
| 可重复 E2E 固化并通过 | pass | `SESSION-DEVICE-FRONTEND-001`、`002` 在独立 WLS 通过 | 2026-08-11 |
| 功能/运营文档完成 | pass | SessionManager 架构、运营、需求、开发日志及 AI 索引 | 2026-08-11 |
| 范围级 QA 验收 | pass | `迁移前历史任务记录（已清理）` | 2026-08-11 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-11 07:54 | requirements | planned → requirements_confirmed | 固定需求/开发日志此前缺失；现有实现是每用户单个旧 Token | 建立 1.0.1 设备凭证接入基线 | 用户 | `doc/需求.md` |
| 2026-08-11 21:14 | implementation/validation | requirements_confirmed → implemented_validation_partial | 每设备 remember 签发、轮换、旧 Token 惰性迁移和退出撤销已实现；双上下文 E2E 通过 | 内置 Browser 验证码操作需用户授权，保留 partial | 主代理 | PHPUnit、SessionManager frontend E2E |
| 2026-08-11 23:22 | final validation | implemented_validation_partial → implemented_validated | 授权 CAPTCHA 登录、设备页、刷新保持、确认退出和退出后 remember 不恢复通过 | 前台真实用户路径闭合；QA 作范围级通过判定 | 主代理 | `acceptance-audit.md`、前台内置 Browser |

### 实现范围

- 变更路径/符号：Customer remember token service、AutoLogin、登录/退出调用点。
- 数据/配置/迁移：无 Customer Schema；旧 Token 经确认后惰性迁移。
- 兼容与回退：仅 Provider 未配置时继续旧流程。

## 2026-08-29 — 注册页 getAccountApi().then TypeError

- **现象**：`api`/`account` 预加载成功后，提交注册仍报 `getAccountApi(...).then is not a function`。
- **原因**：全量 `Weline.Api.resource()` 返回同步 Proxy；旧内联脚本未 `Promise.resolve`，且 Theme 部件 HTML 缓存仍吐旧版。
- **修复**：`register.phtml` 增加 `asPromise()`，所有路径保证返回 thenable；清理 Customer/Theme `view/tpl` 并重启 WLS。

## 2026-08-29 — 登录按钮多重 loading

- **现象**：提交时按钮上同时出现 3 个 spinner。
- **原因**：`data-w-login-spinner` + busy 文案内 `w-spinner` + `.w-button[aria-busy]::after` 三层叠加。
- **修复**：模板只保留 idle/busy 文案切换；busy 仅靠 foundation `aria-busy::after` 画一个 spinner；同步 `account-login.js`（Customer + Theme UI 页）。
