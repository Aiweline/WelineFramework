<!-- weline:module-development-baseline:generated-once -->
# Weline_Captcha 开发日志

## 2026-09-10 — 拆除 debug e4670c 埋点（1.0.48）

- 移除 Google/Captcha/CS/Framework CSP 中 7277 ingest 与 `debug-e4670c.log` NDJSON；保留弱票守卫、双脚本竞态修复与 empty_token_props 降级。
- 契约：RenderJs 断言无 7277/client_fail。

## 2026-09-10 — 拒绝过快/过短 Google token（1.0.48）

- 运行时：域名已在 Key 白名单；同 Key 下 ~2500 字/≈800ms 票 assessment `valid=true`，用户侧 ~612 字/≈5ms 票恒为 `BROWSER_ERROR`/`empty_token_props`。
- 变更：`tokenLooksWeak`（len&lt;1000 或 ms&lt;120）重试，仍弱则 `recaptcha_weak_token` → 本地图码降级，禁止把坏票提交校验。
- 版本：`1.0.47` → `1.0.48`

## 2026-09-10 — 禁止 enterprise.js 双载竞态（1.0.47）

- 根因假设：首个 `enterprise.js` onload 后 3s watch 仍可能 `loadNext` 再挂第二枚脚本，双 SDK 易铸 `BROWSER_ERROR`/`empty_token_props`。
- 变更：onload 后取消 watch；仅未 onload 超时或 onerror 才切备用 host；grace 拉长；ready/applyToken 记录 `hostIdx`/`scriptCount`。
- 版本：`1.0.46` → `1.0.47`

## 2026-09-10 — 供应商 CSP 经 Extends 汇总为应用默认（1.0.43）

- `CaptchaVendorsCsp`：`extends/module/Weline_Framework/Security/Csp/`，从 `CaptchaProviderRegistry` 聚合各供应商 `cspDirectives()`。
- Google：含 `recaptchaenterprise.googleapis.com`；腾讯：`turing.captcha.qcloud.com` + `captcha.tencentcloudapi.com`；本地图码空。
- Framework Defaults 去掉纯人机域名（改由 Captcha Extends 提供）。
- 测试：`CaptchaProviderCspDirectivesContractTest`。
- 版本：`1.0.42` → `1.0.43`。

## 2026-09-10 — empty_token_props 降级本地图码（1.0.43）

- 运行时：`BROWSER_ERROR` 且 hostname/action 皆空 → `browser_error:empty_token_props`（密钥域名未绑定，非「判定为机器人」）。
- 版本：`1.0.42` → `1.0.43`

## 2026-09-10 — Google action 禁止点号（1.0.42）

- 运行时：`customerservice.bind_email` 含 `.`，违反 Enterprise action 规则，assessment 返回 `BROWSER_ERROR`。
- 变更：`normalizeAction` 将非法字符收成 `_`；绑邮 INTENT → `customerservice_bind_email`。
- 版本：`1.0.41` → `1.0.42`

## 2026-09-10 — Provider 补齐 cspDirectives + BROWSER_ERROR 策略（1.0.41）

- 修复：`VerificationProviderInterface::cspDirectives` 落地到 Local/Google/Tencent，消除 LocalImageCaptcha fatal（challenge 502/断连）。
- 运行时：Enterprise 脚本优先 `google.com`；`browser_error` 由 CustomerService 侧不降级本地图码。
- 版本：`1.0.40` → `1.0.41`

## 2026-09-10 — 绑邮模态排除 captcha-lazy 抢刷新（1.0.40）

- 运行时：`#cs-bind-modal` 内 captcha 不再被 lazy `refreshWhenShown`/`degrade` 用默认 challenge 路由覆盖；Google fail 同域 beacon `client_fail`。
- 版本：`1.0.39` → `1.0.40`

## 2026-09-10 — onload 无 SDK 也换线（1.0.39）

- 运行时：脚本 onload 后仍无 `grecaptcha.enterprise` 时 2s 内改试下一主机；超时 finish 亦校验 SDK 是否已暴露。
- 运维：改 Provider 后需 `server:reload`，否则旧 Worker 仍吐 `tries>=48` 旧 HTML。
- 版本：`1.0.38` → `1.0.39`

## 2026-09-10 — Google SDK 主机挂起超时换线（1.0.38）

- 运行时：`enterprise.js` 首个主机无 onerror 挂起时，3s 切下一 URL（recaptcha.net → google.com）；总轮询放宽到 15s。
- 版本：`1.0.37` → `1.0.38`

## 2026-09-10 — 国内勿回落 Google 客户端 + 静默降级（1.0.35）

- 运行时：腾讯未配时 CN 回落 Google；浏览器加载 `enterprise.js` 失败（国内不可达）→ 自动降级 toast。
- 变更：国内 geo 回落链跳过 Google；绑邮箱在用户未提交前静默降级本地图码。
- 版本：`1.0.34` → `1.0.35`

## 2026-09-10 — Google 信任徽章仅 SDK ready 后显示（1.0.34）

- 审图：`web_ui` / `ui_shot`；箭头指向「受 Google 保护」——未就绪不应展示。
- 变更：徽章默认 `hidden`；`grecaptcha.enterprise.ready` 后揭示；script 失败/超时 conceal + degrade。
- CSS bust：`20260910-google-ready1`。
- 版本：`1.0.33` → `1.0.34`

## 2026-09-10 — 后台标明控制台「ID」即 Site Key（1.0.33）

- 字段 label 改为「Enterprise Site Key（即控制台 ID）」；描述/指南写明 Google 编辑页无「Site Key」字样，应复制「ID」。
- 版本：`1.0.32` → `1.0.33`

## 2026-09-10 — Google 校验失败细分 + Site Key 无效提示（1.0.32）

- 探活：`project=weline-framework` + 当前 Site Key → Google 400 `The specified siteKey is invalid`（API Key 形态已正确为 AIza…）。
- soft-fail 改为抛出 `google_*` 原因码，DEV toast 不再只显示 `provider_rejected`。
- Site Key 无效时给出控制台操作指引。
- 版本：`1.0.31` → `1.0.32`

## 2026-09-10 — 字段 cache-namespaces=storefront/captcha（1.0.31）

- 配置模板字段声明 `cache-namespaces="storefront/captcha"`，保存经 `w_changed` bump 与 Framework 清缓存。
- 版本：`1.0.30` → `1.0.31`

## 2026-09-10 — Enterprise 组内一键跳转凭据页（1.0.30）

- Site Key / API Key 字段旁增加 adapter：跳转 reCAPTCHA 控制台与 Cloud 凭据页（`project=weline-framework`）。
- 版本：`1.0.29` → `1.0.30`

## 2026-09-10 — 后台密钥对照写清 Secret Key 为何不用（1.0.29）

- 指南增加「密钥对照」：Site Key / Secret Key（旧版第三方）/ Cloud API Key（本模块）三栏说明；字段 description 写明 Google 显示 Secret 但 Enterprise Assessment 不用。
- 版本：`1.0.28` → `1.0.29`

## 2026-09-10 — 配置页明确禁止填 reCAPTCHA Secret Key（1.0.28）

- 原因：Google 密钥页 Site Key 与 Secret Key 均为 `6L…`，易被当成「Enterprise API Key」；旧文案只写「凭据页 API Key」，未点名勿用 Secret Key。
- 变更：字段 label/description 与使用指南步骤写明：API Key 必须来自 Cloud 凭据（AIza…），禁止填密钥页 Site/Secret Key。
- 版本：`1.0.27` → `1.0.28`

## 2026-09-10 — 拆除 debug e4670c 埋点（1.0.27）

- 用户确认修复后移除 `CaptchaProviderRouter` / `GoogleRecaptchaEnterprise` 中 NDJSON 调试写入；保留 DEV/生产误配分流与 Site Key 形态校验。
- 版本：`1.0.26` → `1.0.27`

## 2026-09-10 — Site Key 形态 API Key：DEV 提示错误，生产静默跳过（1.0.26）

- 需求：开发环境要提示误配；生产不提示。
- 变更：`isGoogleReady()` 对 6L 形态 api_key —— 生产返回 false（静默回落本地图码）；DEV 仍 ready，走 assessment 前置拦截中文错误，并由 Bind/Query 仅在 `DEV` 拼接 `captcha_detail`。
- 版本：`1.0.25` → `1.0.26`

## 2026-09-10 — Site Key 形态 API Key 时 Google 视为未就绪并跳过路由（1.0.25）

- 复现日志：`google_api_key_looks_like_site_key`（前缀仍 `6Ldrn5kt`），说明守卫生效但配置未换。
- 变更：`isGoogleReady()` 在仅有 6L 形态 api_key、无 OAuth 时返回 false；路由回落到本地图码/腾讯，避免绑定弹窗先踩 Google。
- 版本：`1.0.24` → `1.0.25`

## 2026-09-09 — API Key 误填 Site Key 形态时 DEV 明确报错（1.0.24）

- 日志/截图：`API key not valid`；运行时 `api_key` 前缀 `6Ldr…`（Site Key 形态），不是 `AIza…`。
- 变更：`googleApiKeyLooksLikeSiteKey()`；assessment 前拦截并中文说明；配置字段描述区分 Site Key / API Key。
- 版本：`1.0.23` → `1.0.24`

## 2026-09-09 — Google assessment HTTP 失败 DEV 透出响应体（1.0.23）

- 现象：服务端 Google 校验 HTTP 400，前端只见泛化降级文案，开发难定位。
- 变更：`createAssessment` 失败时附带响应体片段；`CaptchaManager`/`BindCaptchaGuard` 保留 `lastFailureDetail`；`DEV` 下绑定接口返回 `captcha_detail` 并拼进 message；生产仍只返回泛化文案。
- 版本：`1.0.22` → `1.0.23`

## 2026-09-09 — 降级本地图码后 Google 监听器覆盖 captcha_response（1.0.22）

- 现象：降级本地图码后仍「人机验证失败或已过期」。
- 日志：`provider=local_image` 但 `responseLen=770`（Google token），`passwordMatch=false`。
- 根因：降级后 Google `prepare-submit` 监听器仍在，再次提交把本地输入框写成 Enterprise token。
- 变更：Google/腾讯监听器仅在当前 `data-weline-captcha-provider` 匹配时写入；写入限定在对应 root 内；绑定弹窗按 captcha root 读字段。
- 版本：`1.0.21` → `1.0.22`

## 2026-09-09 — Google Enterprise 校验自动打洞出站代理（1.0.21）

- 现象：本机已配社媒出站代理 `customer/social_login/http_proxy=127.0.0.1:7892`，绑定邮箱人机验证仍直连 `recaptchaenterprise.googleapis.com` 超时/400，前端报发送失败。
- 根因：PHP curl 默认不读 `HTTPS_PROXY`；`GoogleRecaptchaEnterprise::requestAssessment` 未应用代理，社媒「打洞」未复用到 Captcha。
- 变更：新增 `CaptchaOutboundProxy`（captcha 自有配置 → 社媒 http_proxy → env）；assessment 请求 `CaptchaOutboundProxy::apply`。
- 版本：`1.0.20` → `1.0.21`

## 2026-09-09 — FPC 静态页 SSR Google 空密钥「需要网站密钥」（1.0.20）

- 现象：后台已配置 Enterprise Site Key / API Key，前台仍出现 Google「需要网站密钥」。
- 探针：`/weline_captcha/frontend/challenge` 动态返回有效 `site_key`（配置读取正确）；产品询价等路径把 `renderChallenge()` SSR 进 FPC 页壳，缓存会冻住空/旧密钥。
- 变更：`LazyCaptchaClientRuntime::hostMarkup()`；`InjectCaptchaIntoForm` / Product·Checkout Guard 只注入 lazy host；`GoogleRecaptchaEnterprise::render` 空 site key 直接返回空，禁止 `enterprise.js?render=`。
- 审图：附图1 前台 Google 错误提示（web_ui）；附图2 统一配置中心已覆盖 site_key（web_ui，配置面非主题 fail）。
- 版本：`1.0.19` → `1.0.20`

## 2026-09-09 — captcha-lazy MutationObserver delivery_storm（1.0.19）

- 现象：开发环境控制台 `[Weline:DEV] MutationObserver 反馈环已截断`，`reason=delivery_storm`，堆栈指向 `captcha-lazy.js` `boot`。
- 根因：整页 `childList+subtree+attributes` 观察者在延迟部件加载时 250ms 内超 40 次投递；回调内 `replaceWith` / 插入 stylesheet / `Form.mount` 再触发同一观察者。
- 变更：回调立即 `disconnect` + `rAF` 合并为一次 `ensureAll/track/refreshWhenShown`；自写 DOM 时 `withObserverPaused`；cache bust `20260909-mo-guard1`。
- 版本：`1.0.18` → `1.0.19`

## 2026-09-08 — Google 保护文案信任徽章（1.0.18）

- 需求：登录表单「此表单受 Google reCAPTCHA Enterprise 保护」旁加 Google 标签提升信任度。
- 变更：`GoogleRecaptchaEnterprise::render` 输出信任芯片（官方四色 G 标 +「受 Google 保护」+ 原文案 + Privacy/Terms）；样式落 `captcha-local.css`；cache bust `20260908-google-trust1`。
- 验收：契约测覆盖 badge；登录页可见 Google 徽章。
- 版本：`1.0.17` → `1.0.18`

## 2026-09-08 — Google Enterprise 内联脚本缺 `}`（语法错误）

- 问题：`GoogleRecaptchaEnterprise::render` 中 `fail` 的 `if(allowDegrade){...}` 未闭合，浏览器报 `Unexpected token ')'`。
- 变更：对齐腾讯云写法，闭合为 `}}));}};`；契约测 `GoogleRecaptchaEnterpriseRenderJsContractTest`。

## 2026-09-07 — 页面内 challenge 长期 pending（1.0.17）

- 现象：`/weline_captcha/frontend/challenge?...` 单独打开 200，结账页 Network 里一直「待处理」。
- 根因：`captcha-lazy` 首屏对**隐藏**宿主（关闭的 `<details>` 快速新增地址等）也 `ensureAll`；结账页约 3 个 lazy 宿主并行请求，叠加其它挂起连接后队列卡死。
- 变更：隐藏 / 关闭 details 内不请求；`toggle`/`open` 可见后再拉；同 URL inflight 去重；8s AbortController 超时；脚本版本 `20260907-pending1`。
- 版本：`1.0.16` → `1.0.17`

## 2026-09-06 — 国内国家改用主题地址选择器

- 当前状态：`implemented`
- 问题：国内国家用原生多选 listbox，未用 `<w:theme:address>`。
- 变更：`china_codes` 改为 `address_countries`；国家覆盖行国家列亦用 address 单选搜索。
- 版本：`1.0.15` → `1.0.16`

## 2026-09-06 — CaptchaConfig 构造 DI 注入 null（TypeError 500）

- 当前状态：`fixed`
- 问题：并行改动把 `CaptchaConfig::__construct` 扩成 `object` / 未注册的 `CaptchaSettingsReaderInterface`；`ObjectManager` 无法解析类型后注入 `null`，`w:form` 注入验证码时全站 500（如 `/shipping/backend/region`）。
- 修复：恢复 `ConfigReader` 类型注入；契约测试锁定类型；`setup:di:compile` + `server:reload` 清掉 worker 陈旧 opcache。
- 版本：`1.0.15`（行为修复，版本号不变）

## 2026-09-06 — 配置中心选择器 UI（供应商/国家）

- 当前状态：`implemented`
- 问题：路由供应商与国家覆盖需手填编码，易错且不符合主题选择组件习惯。
- 变更：
  - `captcha.phtml`：供应商改为 `select`；国内国家 / 可信国家头改为 `multiselect`；国家覆盖改为 `country_provider_map`（国家×供应商行式选择）；降级链改为预设 `select`（`+` 编码）。
  - SystemConfig：补 `multiselect` 渲染与数组→逗号串保存；支持 `options-source=i18n:countries`；新增国家覆盖行式编辑器 JS。
- 手填保留：腾讯/Google 密钥与 Project/Site Key、允许域名、分数阈值。

- 当前状态：`implemented`
- 问题：仅 Google Enterprise 时国内/弱网无法加载脚本导致表单无法提交；需要按国家选供应商且支持特殊国家覆盖。
- 变更：
  - 新增 `tencent_captcha` Provider（Web SDK + `DescribeCaptchaResult` + ticket 防重放；拒绝 `trerror_`）。
  - 新增 `ClientGeoResolver` / `CaptchaProviderRouter`：覆盖表 → 国内/海外/未知默认 → 就绪降级链 → `local_image`。
  - SystemConfig：路由组 + 腾讯云组；Google 组保留。
  - `CaptchaManager` 按路由选型；允许 `allow_local_degrade` 时接受本地降级提交。
  - lazy JS 监听 `weline:captcha:degrade`，以 `prefer=local_image` 重拉挑战。
- 版本：`1.0.14` → `1.0.15`

## 2026-09-06 — 删除旧版 Google v2/v3 与兼容迁移（1.0.14）

- 当前状态：`implemented`
- 问题：产品只需最新 Enterprise；旧 v2/v3 与 upgrade 迁移增加配置噪声。
- 变更：删除 `GoogleRecaptchaV2/V3`、`LegacyConfigMigrator`、`MigrateLegacyConfig` 与 SystemConfig 旧版分组；`CaptchaService` 不再映射 google_recaptcha_*。
- 版本：`1.0.13` → `1.0.14`

## 2026-09-06 — 去掉一键授权，改为手填+指南（1.0.13）

- 当前状态：`implemented`
- 问题：Google OAuth 一键授权依赖各环境 Redirect URI 白名单，本机/多实例无法稳定走通。
- 变更：SystemConfig 移除授权/选 Project/测试/撤销入口与 OAuth Client·Token 字段；首组改为「使用指南（开发者）」外链+步骤；主路径手填 Project ID / Site Key / API Key。
- 版本：`1.0.12` → `1.0.13`

## 2026-09-06 — 一键授权误吞 RedirectException（1.0.11）

- 当前状态：`implemented`
- 问题：`authorize()` 在 `try` 内 `redirect()` 到 Google；框架用 `ResponseTerminateException` 实现跳转，被 `catch (\Throwable)` 当成失败并回系统配置页。
- 变更：重抛 `ResponseTerminateException`。
- 版本：`1.0.10` → `1.0.11`

## 2026-09-06 — OAuth 外跳被开放重定向过滤（1.0.12）

- 当前状态：`implemented`
- 问题：`Backend ResponseRedirectBefore` 只允许本机 host，`redirect(accounts.google.com)` 被改写；真实 Location 无法出站。
- 变更：与 Payment Connect 一致，对 Google authorization_url 直接 `throw new RedirectException(...)`。
- 版本：`1.0.11` → `1.0.12`

## 2026-09-06 — Google 一键授权路由 404（1.0.9）

- 当前状态：`implemented`
- 问题：配置适配器与 OAuth 回调写了 `captcha/backend/google/*`，真实 router 为 `weline_captcha`，一键授权进前端 404。
- 变更：`captcha.phtml`、`Google.php`、`GoogleOAuthService::callbackUrl()` 统一为 `weline_captcha/backend/google/*`。
- 版本：`1.0.8` → `1.0.9`

## 2026-09-06 — 后台配置授权置顶（1.0.8）

- 当前状态：`implemented`
- 问题：SystemConfig 人手填 Project/Site Key/Token 与一键授权并列，分组杂乱，授权路径不突出。
- 变更：`captcha.phtml` 重组为「推荐：Google 一键授权」→「启用与风控参数」→「手动填写 Enterprise 密钥（可选）」→「旧版兼容」；Token 降为手动组并注明自动写入。
- 版本：`1.0.7` → `1.0.8`

## 2026-09-05 — 再次打开表单自动换新验证码（1.0.7）

- 当前状态：`implemented`
- 问题：结账地址等 lazy 验证码首次加载后，收起再打开仍显示旧图，易校验失败。
- 变更：`captcha-lazy.js` 跟踪隐藏态，`曾隐藏→可见` 时 `refresh`；cache `20260905-open-refresh1`。
- 配合 Shipping `2.4.8` 打开编辑器显式换码。
- 版本：`1.0.6` → `1.0.7`

## 2026-09-05 — 结账验证码「刷新」英文词条

- 当前状态：`implemented`
- 问题：英文结账地址表单验证码按钮仍显示「刷新 / 刷新验证码」。
- 变更：`en_US.csv` → `Refresh` / `Refresh captcha`。
- 版本：`1.0.5` → `1.0.6`

## 2026-09-05 — 验证码输入框跟随图+刷新行宽度

- 当前状态：`implemented`
- 问题：挑战图已固有比例后，`.weline-captcha-field` / 输入仍 `width:100%`，结账地址表单里输入框横贯整行显得过长。
- 修复：`.weline-captcha-stack` 改为 `width:fit-content`，视觉行 `width:auto`，输入框跟随图+刷新行；cache bust `20260905-input-fit1`。
- 验收：结账地址验证码输入框宽度约等于图+「刷新」一行。

## 2026-09-05 — 本地图验证码固有比例（结账宽表单不再拉扁）

- 当前状态：`implemented`
- 问题：`.weline-captcha-image` 使用 `flex:1` + 固定高度 + `object-fit:cover`，结账地址等宽表单把挑战图横向拉满，字符难读。
- 修复：改为 `flex:0 1 auto` + `aspect-ratio:168/40` + `object-fit:contain`；刷新按钮旁置；CSS cache bust `20260905-intrinsic1`。
- 验收：结账地址表单验证码图保持固有比例、与「刷新」同行；`LocalImageCaptchaLayoutContractTest` 通过。

## 2026-08-31 — lazy 注入丢失验证码布局样式

- 当前状态：`implemented`
- 问题：`captcha-lazy.js` 的 `replaceAnchor` 只保留 challenge HTML 的第一个元素，模板内联 `<style>` 被丢弃；供应商/询盘等 lazy 表单验证码图与刷新按钮各自占一行，需各页重复写 CSS。
- 修复：布局样式抽到 `view/statics/css/captcha-local.css`；`captcha-lazy.js` boot 时 `ensureStylesheet()`；`replaceAnchor` 额外 `hoistFragmentStyles()` 兼容旧响应；模板 `.weline-captcha-visual` 增加 `weline-captcha-row` 别名。
- 验收：任意 lazy 注入表单验证码图与「刷新」同一行，无需业务页重复纠正。

## 2026-08-29 — 验证码图与刷新按钮同一行

- 当前状态：`implemented`
- 问题：窄登录卡触发 `22rem` 换行，图/刷新/输入各自占一行。
- 修复：`weline-captcha-visual` 固定横向排图+刷新；输入单独一行；覆盖登录页按钮全宽。
- 验收：登录页验证码图与「刷新」同一行。

## 2026-08-29 — 自动注入即一律 lazy（与 required/auto 无关）

- 当前状态：`implemented`
- 问题：误把 `required`/`auto` 当成 SSR 路径，导致「注入验证码」不能自动 FPC 友好。
- 修复：`InjectCaptchaIntoForm` 凡注入只输出 lazy 占位+运行时，删除 SSR/`forbid`；`required`/`auto`/`lazy` 只决定是否注入。
- 验收：`required`/`auto` 单测期望 lazy host；注入 HTML 不含 `captcha_token`。

## 2026-08-29 — Captcha 根源支持 FPC lazy 运行时

- 当前状态：`implemented`
- 问题：各业务页各自复制 challenge 拉取/刷新逻辑，FPC 下 SSR 图码仍易被复用。
- 修复：新增 `view/statics/js/captcha-lazy.js`（`Weline.Captcha`）与 `LazyCaptchaClientRuntime`；`InjectCaptchaIntoForm` 在 `lazy`/`required`/`auto` 注入时 once 附带脚本；挑战仍走 `frontend/challenge` + forbid。
- 验收：任意 `captcha="lazy"` 表单无需业务定制 JS 即可加载/刷新；共享页 HTML 不含 `captcha_token`。

## 2026-08-29 — 登录验证码被缓存复用且 Challenge 500

- 当前状态：`implemented`
- 问题：登录页多次刷新仍同一 `captcha_token`（worker/共享缓存复用 SSR 挑战 HTML）；`/weline_captcha/frontend/challenge` 调 `json()` 500。
- 修复：SSR `required/auto` 注入前 `SharedResponseCachePolicy::forbid`；Challenge 改 `fetchJson` + forbid；登录改 `captcha="lazy"` 并由客户端拉取挑战；挑战 UI 增加刷新。
- 验收：Challenge JSON 每次新 token；登录页为 lazy host，刷新后图码变化。

## 2026-08-29 — 登录表单验证码标签未进请求模块词典

- 当前状态：`implemented`
- 问题：WLS 分层 Phrase 下 `LocalImageCaptcha` 用 `__()` 拼标签，英文站仍回落中文；与表单 `<lang>` 编译期烘焙不一致。
- 修复：挑战 UI 改为 `local-image-challenge.phtml`（`<lang>请输入图片中的验证码</lang>`）；`InjectCaptchaIntoForm` 渲染前 `addModule('Weline_Captcha')`。
- 验收：英文登录页验证码标签为 `Enter the code shown in the image`。

## 2026-08-26 — LocalImageCaptcha 哈希成本与懒加载模式

- 当前状态：`implemented`
- 问题：`password_hash(PASSWORD_DEFAULT)` 在本机约 400ms，header 等隐藏表单 SSR `captcha="required"` 拖垮整页 TTFB。
- 修复：本地图验证码改 bcrypt cost=5；`w:form captcha="lazy"` 只打标记，由客户端按需拉取挑战；保留 `Controller/Frontend/Challenge` 备用。
- 验收：`InjectCaptchaIntoFormTest` lazy 用例；LocalImageCaptcha render ≪100ms。

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.1`（文档基线） | docs_handoff | `REQ-CAPTCHA-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.0.1` — 文档制度启用基线

- 基线版本：`1.0.1`
- 目标版本：`1.0.1`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-CAPTCHA-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-CAPTCHA-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-CAPTCHA-0001` / `1.0.1` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
