<?php
/**
 * Skill listing page.
 *
 * @var \Weline\Framework\View\Template $this
 */
$skills = is_array($skills ?? null) ? $skills : [];
$categories = is_array($categories ?? null) ? $categories : [];
$currentCategory = trim((string)($current_category ?? ''));
$listingUrl = $this->getBackendUrl('*/backend/skill/listing');

$get = static function (mixed $item, string $key, mixed $default = ''): mixed {
    if (is_array($item)) {
        return $item[$key] ?? $default;
    }
    if (is_object($item)) {
        if (method_exists($item, 'getData')) {
            $value = $item->getData($key);
            return $value ?? $default;
        }
        if (isset($item->{$key})) {
            return $item->{$key};
        }
    }
    return $default;
};
?>

<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><?= __('Skill Management') ?></h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="<?= htmlspecialchars($listingUrl, ENT_QUOTES) ?>"><?= __('Bot') ?></a></li>
                    <li class="breadcrumb-item active"><?= __('Skills') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <form class="row g-2 mb-3" method="get" action="<?= htmlspecialchars($listingUrl, ENT_QUOTES) ?>">
                    <div class="col-md-4">
                        <label class="form-label"><?= __('Category') ?></label>
                        <select class="form-select" name="category">
                            <option value=""><?= __('All') ?></option>
                            <?php foreach ($categories as $key => $label): ?>
                                <option value="<?= htmlspecialchars((string)$key, ENT_QUOTES) ?>" <?= $currentCategory === (string)$key ? 'selected' : '' ?>>
                                    <?= htmlspecialchars((string)$label) ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                    <div class="col-md-3 d-flex align-items-end">
                        <button type="submit" class="btn btn-primary"><?= __('Filter') ?></button>
                    </div>
                </form>

                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead>
                        <tr>
                            <th>ID</th>
                            <th><?= __('Code') ?></th>
                            <th><?= __('Name') ?></th>
                            <th><?= __('Category') ?></th>
                            <th><?= __('Dangerous') ?></th>
                            <th><?= __('Active') ?></th>
                            <th><?= __('Actions') ?></th>
                        </tr>
                        </thead>
                        <tbody>
                        <?php if (!empty($skills)): ?>
                            <?php foreach ($skills as $skill): ?>
                                <?php
                                $id = (int) $get($skill, 'skill_id', 0);
                                $isDangerous = (int) $get($skill, 'is_dangerous', 0) === 1;
                                $isActive = (int) $get($skill, 'is_active', 0) === 1;
                                ?>
                                <tr>
                                    <td><?= $id ?></td>
                                    <td><?= htmlspecialchars((string) $get($skill, 'code', '')) ?></td>
                                    <td><?= htmlspecialchars((string) $get($skill, 'name', '')) ?></td>
                                    <td><?= htmlspecialchars((string) $get($skill, 'category', '')) ?></td>
                                    <td><span class="badge <?= $isDangerous ? 'bg-warning text-dark' : 'bg-light text-dark' ?>"><?= $isDangerous ? __('Yes') : __('No') ?></span></td>
                                    <td><span class="badge <?= $isActive ? 'bg-success' : 'bg-secondary' ?>"><?= $isActive ? __('Enabled') : __('Disabled') ?></span></td>
                                    <td>
                                        <a class="btn btn-sm btn-outline-primary"
                                           href="<?= htmlspecialchars($this->getBackendUrl('*/backend/skill/view?id=' . $id), ENT_QUOTES) ?>">
                                            <?= __('View') ?>
                                        </a>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        <?php else: ?>
                            <tr>
                                <td colspan="7" class="text-center py-4 text-muted"><?= __('No skill records found.') ?></td>
                            </tr>
                        <?php endif; ?>
                        </tbody>
                    </table>
                </div>
                <var>pagination</var>
            </div>
        </div>
    </div>
</div>
