## 2026-09-09 — LocalDescription 复合主键迁移修前缀（1.2.5）

- 根因：`fix_eav_local_description_composite_pk` 用无前缀表名，生产 `m_eav_*_local_description` 仍为单列 PK+serial，LocalModel `ON CONFLICT (id,local_code)` → 事务中止 25P02 空转。
- 修复：迁移改为 `LocalDescription::getTable()`；幂等检测复合主键；`id DROP DEFAULT`。
- 验证：`FixEavLocalDescriptionCompositePkContractTest`
- 版本：`1.1.20` → `1.2.5`

## 2026-09-09 — 拒绝 percent-encoded Option 当地名（1.1.20）

- 问题：AI/导入把 URL 编码串写入 Option `en_US` Local，店面规格轴直接展示 `%E5…`
- 修：`AttributeMetadataCatalog` 忽略含 `%XX` 的当地名回退源 value；`EavLocalTranslationService::upsertLocalValue` 拒绝再写入编码串
- 版本：`1.1.19` → `1.1.20`

## 2026-09-08 — 标量元数据缓存移除重复 ORM 装配（1.1.18）

- 实际已有限定的选项 SQL，PG 执行约0.42–0.53ms，原 ORM fetch 为13.3–18.4ms；已有复合索引且36个数据页全命中，未新增索引。原 `items()` 先把SQL行构造成模型，再拆回数组缓存，消费时又克隆模型。
- 复用同类已有 `fetchIterator()` 标量行接口，缓存行后仍为消费者克隆独立对象；缓存键、过滤、请求Context、DTO和展示翻译不变。最小行为用例临时查询模型构造10→0，相关回归35 tests / 487 assertions通过。
- 配置数据库只读复测24个商品规格URL：9个DB spans/8.35ms、整个该段36.18ms；参数散列与原实现严格一致（`0a9748861ef3073bc7df1890a3ec04ab8459ad45091be872f9e4810faa6014de`）。选项仍为1152行，不能误称78行；本次收益是消除重复装配。证据 `/tmp/weline-option-array-read-runtime.json`。编译重载后的默认新Worker首商品页934.15ms（App877.90ms），HTTP/2 200、FPC no-store/disabled、1,208,277字节与footer完整；70ms未达，未代替浏览器视觉验收。整页证据 `/tmp/weline-cold70-isolated/scalar-main-products.trace.json`。

## 2026-09-08 — Typed EAV 多实体多字段批量读（1.1.16）

- 最终定向验证：27 tests / 152 assertions 通过；10 个 PHP 文件 lint 与限定 diff check 通过。未由单元测试替代主任务 HTTP/浏览器验收。

- 新增可选 `ScopedAttributeBatchReaderInterface::readScopedValues`，现有 `EntityAttributeStore` 实现。调用者只传实体声明、owner ID、AttributeRecord、ScopeIdentity、locale；实体校验一次，按 value table/type 归组后 IN 批读，不由 Blog 拼表或 SQL。
- 单项与批量读取共用原 `EavScopeResolver` 和行聚合逻辑；返回所有 owner/attribute cell，包括 inherit/cleared，保留四层 scope、每层 locale→空 locale、多值聚合、typed 行过滤。旧单项 provider 接口无破坏性变化。
- SQLite 最小 RED：3 owner × 3 字段为 9 次而非两类型 2 次 SELECT；批量后四层 scope × 三 locale 与单值完整结果相等。真实 Blog 分类范围 443→17 DB，EAV 五字段落三表各 1 SELECT，见 Blog 开发日志。
- 未新增跨请求值缓存。独立 scoped 写当前是动态模型 delete/insert→fetch，未走 AbstractModel::save 的 after 事件，也没有规范 ResourceChange/提交后 namespace 发布。现有 EAV event.xml 的元数据/选项翻译事件不能替代此失效链；本轮不修改写语义。

## 2026-09-05 — 筛选共享选项批量加载

- `AttributeFilterService` 的metadata和实际有值属性通过统一选项读取方法批量预取未缓存桶，单属性入口复用该方法；保持scope_instance_id=0、option_id降序、全部字段、空桶复用与原clearCache语义，无新增缓存。
- 固定112件真实商品完整Panel结果一致，共享选项统计SQL 96 → 1；最小SQLite案例先RED再GREEN，覆盖范围排除、排序、映射和缓存清理后刷新。之前值表批量统计实现保持。
- 总页面性能仍需按真实HTTP单独测量；CLI整页面板样本有波动，不能将95次查询减少直接等同整页加速。Browser恢复探测的DOM读取仍超时，实际点击验收未完成。完整证据见Framework/doc/统一缓存范围与性能优化.md的“共享属性选项批量读取”节及 `/private/tmp/weline-perf-options/acceptance-report.md`。
- 交付地址：[英文商品](https://p05113ef3.weline.test:9555/en_US/products)。

## 2026-09-05 — 前台筛选属性值批量查询

- `AttributeFilterService` 按实际值表和连接配置将多个属性一次统计，保留 DISTINCT 实体计数、值过滤、输入顺序与跨表/连接隔离；无新增缓存或表结构变更。
- 固定 112 件真实商品输入：完整面板结果一致，值查询 138 → 2，面板总 SQL 259 → 123。SQLite 定向用例先复现 4 次而非 2 次查询，再验证批量及相同 ID 跨表/连接语义；既有测试替身对齐 `getAttributeId`。
- 原单属性接口、共享选项 scope、事件、三级业务范围与语言策略保持。真实 HTTP 及 Browser 边界、后续性能项见 Framework/doc/统一缓存范围与性能优化.md 的“筛选属性值按实际表批量统计”节；Browser 工具超时尚未完成操作验收。
- 交付地址：[英文商品](https://p05113ef3.weline.test:9555/en_US/products)。

## 2026-09-05 — EAV 只读元数据与标签查询继续优化

- 已落地：公共readonly DTO树按entity/locale请求内复用；商品私有选项仅复制必要节点，自由集SQL按set/product限定；标签原SQL的scope/code结果ID/null统一请求缓存，商品校验与旧数据、语言兼容保留。
- 验证：定向46 tests/286 assertions、PHP lint、diff check通过；真实DB中英文30组旧/新输出一致。默认两Worker平滑重载12.5秒。
- 真实冷请求：英文3.163s、中文2.680s，81商品字段投影约219/212ms；正常入口1.293s，后续FPC HIT约34.85ms。数据与机器负载变动，不承诺稳定延迟。
- 未完成：Mac锁屏阻止Browser页面操作验收；header输出体积、计时边界外的FPC尾部及PHP候选集分页仍待后续优化。详细边界和证据见 Framework/doc/统一缓存范围与性能优化.md 最新节。
- 交付地址：[英文商品](https://p05113ef3.weline.test:9555/en_US/products)、[中文分类](https://p05113ef3.weline.test:9555/categories)、[排序分页](https://p05113ef3.weline.test:9555/en_US/products?sort=price_asc&page=2)、[属性筛选](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_chao_dai=Ming+Style)、[价格筛选](https://p05113ef3.weline.test:9555/en_US/products?price=0-99)。后台/API：N/A。

## 2026-09-05 — 统一缓存优化实际运行记录

- default 两个 Worker 平滑重载成功（8.1 秒）；分类冷请求 200/10.214 秒，随后命中约 105 ms；首页约 111 ms，英文商品列表约 63–104 ms，均 200。
- 新查询已在真实 timing.log 出现：EAV options 增加 scope_instance_id 条件，重复全实体扫描已消除；trace 中 DB 记录约 453 ms（截断样本，不据此计算完整提升比例）。
- 未完成：后续 nocache 动态请求及复查收到维护页 503，维护状态未改；Browser 真实页面操作工具超时，不能宣称浏览器验收通过。冷目录模板前约 8.06 秒及大 HTML 后处理仍需继续定位。
- 完整策略、定向回归数据和剩余优化清单：Framework/doc/统一缓存范围与性能优化.md。

<!-- weline:module-development-baseline:generated-once -->
# Weline_Eav 开发日志

## 2026-09-05 — 商品元数据请求内复用与选项 SQL 范围收窄

- 变更：`AttributeMetadataCatalog` 统一使用 Framework `StorefrontScopeHotCache::rememberForRequest`，同请求内复用实体 ID、属性类型、属性集、分组、属性、放置关系及按 locale 区分的本地化字段读取。
- 范围：共享选项 `scope_instance_id=0` 只读一次，各商品单独读取自己的私有选项；条件在 SQL 执行前应用，仍按原规则合并并排序，产品自由属性可见性保持原有过滤。未引入跨请求持久缓存或新增业务失效约定。
- 可变状态：请求缓存保存标量行数组，每次供目录构建使用时重新实例化行模型；不共享可变 ORM 对象。
- 开发验证：`AttributeMetadataCatalogCacheTest` 通过真实 SQLite 查询记录验证公共读取复用、私有选项查询边界、自由属性隔离、locale 切换和新请求重读。真实站点分类/商品页面耗时由本次框架性能优化统一验收记录。

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 2026-09-05 — Options API 返回 scope_instance_id（1.1.14）

- 当前状态：`implemented`。
- 变更：`EavOptionsQuery::queryOptions` 在 `scope_instance_id>0` 时合并共享+实例私有，并在选项载荷中回传 `scope_instance_id`，供产品后台/主题选择器区分作用域。
- 验收：`AttributeOptionScopeContractTest`。
- 版本：`1.1.13` → `1.1.14`。

## 2026-09-04 — 实体实例私有选项 scope_instance_id（1.1.13）

- 当前状态：`implemented`。
- 问题：规格自定义/导入脏色一律进全局 `eav_attribute_option`，污染属性管理与全站筛选。
- 方案：同表增加 `scope_instance_id`（0=共享，>0=实体实例私有）；唯一键改为 `(attribute_id, code, scope_instance_id)`；`AttributeOptionStore::ensureInScope` 先匹配共享再写私有；全局 catalog/filter/管理端仅见共享；`catalogForProduct` 合并本实例私有。
- 验收：合同测试 `AttributeOptionScopeContractTest`；存量行默认 0，不做脏数据搬家。
- 版本：`1.1.12` → `1.1.13`。

## 2026-09-04 — 选项 metadata value 保留源文以便当地名匹配（1.1.12）

- 当前状态：`implemented`。
- 问题：商品 EAV 常存中文选项源文；`AttributeOptionMetadata.value` 被写成 option_id，店面 `resolve(中文源文)` 无法命中 en LocalDescription。
- 修复：value 优先保留 `Option::getValue()` 源文；label 仍用当前 locale LocalDescription。
- 验收：EN PDP Technical details 中「明制」等显示为 Ming Style。
- 版本：`1.1.11` → `1.1.12`。

## 2026-09-04 — 元数据目录合并 Option/Attribute LocalDescription（1.1.11）

- 当前状态：`implemented`。
- 问题：店面英文环境下 EAV 选项/属性名仍显示源文中文；`AttributeMetadataCatalog` 只读 `Option::getValue()`。
- 修复：按 `State::getLangLocal` 加载 Option/Attribute `LocalDescription`，有本地化则覆盖 label/name。
- 验收：EN PDP Color 显示 Pink/Green（需 Option LocalDescription 有 en_US）；常驻 Worker 改码后 reload。
- 版本：`1.1.10` → `1.1.11`。

## 2026-09-03 — Value 值表解析兼容 EntityDefinition 实体（1.1.10）


- 现象：Filters 侧栏无 EAV 属性面；`AttributeFilterService::getFilterableAttributes('product', …)` 抛「属性没有实体」。
- 根因：`product` 实体类 `ProductCatalogAttributeEntity` 实现 `EntityDefinitionInterface` 而非 `EavModel`；`hydrateAttribute` 无法 `current_setEntity`；`Value::getTable` 只走 `current_getEntity()`。
- 修复：`Value::getTable` 优先 `getEavEntity()->getCode()`，再回落 runtime entity；契约锁定回落顺序。
- 版本：`1.1.9` → `1.1.10`。

## 2026-09-03 — Legacy 属性列表页美化

- **页面**：`/eav/backend/attribute`（`view/templates/Backend/Attribute/index.phtml`）。
- **问题**：`table-rep-plugin` + 长 `code` 挤垮列宽；`is_system` 的 `<empty>/<if>/<else>` 嵌套导致系统属性操作列空白。
- **改动**：对齐实体列表壳层（hero + `w-card` + `eav-attribute-list.css`）；状态/系统徽章；时间截断；保留 Search + OffCanvas；入口链到 EAV 管理器。
- **版本**：`1.1.9`。

## 2026-09-03 — 后台实体列表页美化

- **页面**：`/eav/backend/entity`（`view/templates/Backend/Entity/index.phtml`）。
- **问题**：表头 9 列 / 数据 10 列错位；面包屑 `ol` 未重置为数字列表；主题下表格边框丢失；搜索框硬编码 `product`。
- **改动**：对齐 Manager 风格的 hero + `w-card` + 页面 CSS（`view/statics/css/eav-entity-list.css`）；补齐「代码」列；系统/自定义徽章；入口链到 EAV 管理器；搜索默认空值并仅回填请求参数。
- **版本**：`1.1.7`。

## 2026-09-03 — 属性组列表 PDO 报错与页面美化

- **现象**：`/eav/backend/attribute/group` 打开即 500；`PDOException: column entity_local.eav_entity_id does not exist`。属性集列表同因。
- **根因**：`EavEntity\LocalDescription` 主键 FK 列为 `id`，legacy `Attribute\Group::index` / `Attribute\Set::index` 误 join `entity_local.eav_entity_id`。
- **修复**：join 改为 `main_table.eav_entity_id=entity_local.id`；属性组列表/表单对齐 `w-eav-manager-page` + `w-card` 布局；契约测试 `AttributeGroupListContractTest` 锁定列名与壳层；搜索字段扩展与空态（`1.1.8`）。
- **版本**：`1.1.8`。

## 2026-08-29 — 选择属性集抽屉「不满足操作授权要求」

- **现象**：商品创建向导「选择属性集」属性结构加载失败；控制台 `BinQuery ERR call eav_admin.adminRequest 403`；同 Cookie HTTP `/eav/backend/manager/tree` 正常。
- **根因**：不在 EAV ACL/路由，而在 Backend Worker 鉴权：`FrontendWorkerBackendAttestationProvider` 与 `FrontendQueryGateway` 使用不同 `SessionFactory` 实例，principal 校验为空。
- **修复**：见 `Weline_Backend` 开发日志「BinQuery backend auth_error（SessionFactory 双实例）」；本模块无代码变更。

## 2026-08-29 — 属性集选择弹窗加载挂死

- **现象**：商品后台「选择属性集」选中节点后右侧长期「加载中」；控制台 `updateI18nScheduleButton is not defined`，并伴有 `eav_admin.adminRequest` BinQuery ERR。
- **根因**：定时翻译入口移除后挂载末尾仍调用已删除函数，导致 `eav-manager` 挂载抛错；`selectItem`/`renderForm` 的 `detailSequence` 竞态会吞掉后续错误，详情区无法退出 loading。
- **修复**：删除死调用；`renderForm` 支持复用 sequence 并自捕获错误；契约测试禁止 `updateI18nScheduleButton`；`theme:compile welineUi` 同步发布包。

## 2026-08-26 — compare_mode 与 Compare 集成

- **Schema**：`eav_attribute.compare_mode`（Migration `add_eav_attribute_compare_mode_20260826-v1.2.0.php`），枚举 `CompareMode`。
- **公开 API**：`AttributeMetadata::$compareMode`、`AttributeMetadataCatalogInterface::attributeIndexByEntityCode()`。
- **后台**：属性新建/编辑统一暴露可见/可筛选/可搜索/可比模式；`Manager::postAttributeSave` 校验并持久化。
- **消费方**：`Weline_Compare` 硬依赖 `Weline_Eav`，规格行高亮按元数据 `compare_mode` 决策。
- **Product bootstrap**：`ProductCatalogEavBootstrap` 为系统预置属性写入默认 `compare_mode` 并幂等回填。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.1.3`（文档基线） | docs_handoff | `REQ-EAV-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.1.3` — 文档制度启用基线

- 基线版本：`1.1.3`
- 目标版本：`1.1.3`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-EAV-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-EAV-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-EAV-0001` / `1.1.3` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
## 2026-09-08 — 共享规格身份按参与轴批量读取（1.1.17）

- 新增可选 `AttributeOptionIdentityCatalogInterface::sharedOptionIdentities`；复用完整目录的属性/placement 排序，只对本次缺失轴的 attribute IDs 读取 scope 0 选项，不加载 option/attribute 本地化描述。已有属性行从统一请求缓存复用，placement 引用已加载属性时不重复 load。
- 身份与已确认缺失轴挂现有 RequestContext；读异常不回填缺失，不增加尚无规范 changed/generation 失效支持的跨请求选项缓存。展示目录、关闭属性、同 code 顺序、商品私有继承保持原行为。
- 最小 RED 覆盖消费者尚未采用有限接口、异常后的身份重试；SQLite 行为验证选项 SQL 仅返回参与轴、24 次重复读取无新增 SQL、未知轴、同 code/placement 顺序、关闭属性、下一请求更新与原本地化显示。相关 Eav/Product 回归 34 tests / 482 assertions 通过。
- 当前配置的真实只读 CLI：24 商品链接选项从约 2070 条缩小至 4 轴；实际 WLS 三个批次共 1152 条（size/ID 34 为 113、style_type/ID 35 为 959，两轴合批 1072；color/ID 4 为 78；character/ID 118 为 2）。早先“4 轴 78 条”统计错误：探针误用联合主键语义的 `EavAttribute::getId()`，已改为 `getAttributeId()`；78 仅是颜色轴。14 DB / 102.18ms DB / 196.31ms 总段降为 9 DB / 20.40ms DB / 71.60ms 总段，两个本地化查询与 5 次已知 placement 属性读取消失。墙钟受负载影响，页面表现由主任务 WLS/浏览器验收，本文不声称达到 70ms。
