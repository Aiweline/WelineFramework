
<script>
(function(g){
  if(g.__bqAdmin_eav_admin) return;
  g.__bqAdmin_eav_admin=true;
  g.bqAdmin=g.bqAdmin||{};
  g.bqAdmin['eav_admin']=function(url, options){
    options=options||{};
    var body=options.body;
    if(body && typeof FormData!=='undefined' && body instanceof FormData){
      var p=new URLSearchParams(); body.forEach(function(v,k){ if(!(typeof File!=='undefined'&&v instanceof File)) p.append(k,String(v)); }); body=p.toString();
    } else if(body && typeof body!=='string'){ try{ body=JSON.stringify(body); }catch(e){ body=''; } }
    var run=function(api){ return api.resource('eav_admin').adminRequest({url:url, method:options.method||'POST', headers:options.headers||{}, body:body||''}); };
    var toResp=function(data){
      var _biz=g.WelineApiBusiness||(g.Weline&&g.Weline.ApiBusiness);
      if(_biz&&typeof _biz.wrapAdminBridgeResult==='function'){
        return _biz.wrapAdminBridgeResult(data);
      }
      var body=(data&&typeof data==='object'&&!Array.isArray(data))?data:{success:true,data:data};
      var ok=!(body&&body.success===false);
      var resp={ok:ok,status:ok?200:400,json:function(){return Promise.resolve(body);},text:function(){return Promise.resolve(typeof body==='string'?body:JSON.stringify(body==null?{}:body));}};
      Object.keys(body).forEach(function(k){ if(k==='ok'||k==='json'||k==='text'||k==='status') return; resp[k]=body[k]; });
      return resp;
    };
    var p=(g.Weline&&g.Weline.load)?g.Weline.load('api').then(run):Promise.resolve(run(g.Weline.Api));
    return p.then(toResp);
  };
})(window);
</script>
<?php
/**@var \Weline\Framework\View\Template $this */
?>
<!-- start page message -->
<div class="w-grid">
    <template>Weline_Component::message.phtml</template>
</div> <!-- end message -->
<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-card">
            <div class="w-card__body">
                <w:form id="set-edit" class="w-form" novalidate='' action='{{action}}' method='post'>
                    <!--基础信息-->
                    <has name="set">
                        <input name='set_id' type='hidden' value='{{set.set_id}}'/>
                    </has>
                    <div class="w-grid">
                        <div style="--w-span-md:4;">
                            <div style="--w-mb:var(--weline-space-4);">
                                <label class="w-field__label" for='validationName'>
                                    <lang>属性集名</lang>
                                </label>
                                <input type='text' class="w-input" maxlength='60' data-parsley-minlength='3' id='validationName' placeholder='@lang{属性集名}' value='{{set.name}}' name='name' required=''>
                                <div class="w-field__hint">
                                    <lang>填写正确！</lang>
                                </div>
                            </div>
                        </div>
                        <div style="--w-span-md:4;">
                            <div style="--w-mb:var(--weline-space-4);">
                                <label class="w-field__label" for='validationCode'>
                                    <lang>属性集代码</lang>
                                </label>
                                <input type='text' class="w-input" maxlength='60' data-parsley-minlength='3' id='validationCode' placeholder='@lang{属性集代码}' value='{{set.code}}' name='code' required=''>
                                <div class="w-field__hint">
                                    <lang>填写正确！</lang>
                                </div>
                            </div>
                        </div>
                        <div style="--w-span-md:4;">
                            <div style="--w-mb:var(--weline-space-4);">
                                <label class="w-field__label" for='validationEntity'>
                                    <lang>实体</lang>
                                </label>
                                <select type='text' class="w-input" name='eav_entity_id' data-parsley-minlength='3' id='validationEntity' required=''>
                                    <foreach name='entities'>
                                        <option value='{{v.eav_entity_id}}' @if{set.eav_entity_id=== v.eav_entity_id=> 'selected'}>{{v.name}}</option>
                                    </foreach>
                                </select>
                                <div class="w-field__hint">
                                    <lang>填写正确！</lang>
                                </div>
                            </div>
                        </div>
                    </div>
                    <button class="w-button" type='submit' data-tone="primary">
                        <empty
                            name='set'>
                            <lang>添加</lang>
                            <else/>
                            <lang>编辑</lang>
                        </empty>
                    </button>
                    <notempty name='set'>
                        <a class="w-button" href="@backend-url('*/backend/attribute/set/add')" data-tone="info">
                            <lang>继续新增</lang>
                        </a>
                    </notempty>
                </w:form>
            </div>
        </div>
    </div> <!-- end col -->
</div> <!-- end row -->

<js:part>
(function() {
    'use strict';
    
    var form = $('#set-edit');
    
    form.on('submit', function(event) {
        event.preventDefault();
        var action = form.attr('action');
        bqAdmin['eav_admin']((function(cfg){return typeof cfg==='string'?cfg:(cfg&&cfg.url);})(action, form.serializeArray(), function (data) {
            if (data['id']) {
                Weline.UI.dialog.request({
                    tone: 'success',
                    title: data.msg,
                    confirmLabel: '@lang{好的}',
                }).then(function(){
                    window.parent.location.reload();
                });
            } else if (data['msg']) {
                Weline.UI.dialog.request({
                    tone: 'danger',
                    title: '@lang{修改通知}',
                    message: data.msg,
                    confirmLabel: '@lang{好的}',
                });
            }
        });
    });
})();
</js:part>
