<script>
(function(g){
  if(g.__bqAdmin_eav_admin) return;
  g.__bqAdmin_eav_admin=true;
  g.bqAdmin=g.bqAdmin||{};
  g.bqAdmin['eav_admin']=function(url, options){
    options=options||{};
    var body=options.body;
    if(body && typeof FormData!=='undefined' && body instanceof FormData){
      var p=new URLSearchParams(); body.forEach(function(v,k){ if(!(typeof File!=='undefined'&&v instanceof File)) p.append(k,String(v)); }); body=p.toString();
    } else if(body && typeof body!=='string'){ try{ body=JSON.stringify(body); }catch(e){ body=''; } }
    var run=function(api){ return api.resource('eav_admin').adminRequest({url:url, method:options.method||'POST', headers:options.headers||{}, body:body||''}); };
    var toResp=function(data){
      var _biz=g.WelineApiBusiness||(g.Weline&&g.Weline.ApiBusiness);
      if(_biz&&typeof _biz.wrapAdminBridgeResult==='function'){
        return _biz.wrapAdminBridgeResult(data);
      }
      var body=(data&&typeof data==='object'&&!Array.isArray(data))?data:{success:true,data:data};
      var ok=!(body&&body.success===false);
      var resp={ok:ok,status:ok?200:400,json:function(){return Promise.resolve(body);},text:function(){return Promise.resolve(typeof body==='string'?body:JSON.stringify(body==null?{}:body));}};
      Object.keys(body).forEach(function(k){ if(k==='ok'||k==='json'||k==='text'||k==='status') return; resp[k]=body[k]; });
      return resp;
    };
    var p=(g.Weline&&g.Weline.load)?g.Weline.load('api').then(run):Promise.resolve(run(g.Weline.Api));
    return p.then(toResp);
  };
})(window);
</script>
<?php
/** @var \Weline\Framework\View\Template $this */
?>
<link rel="stylesheet" href="@static(Weline_Theme::ui/pages/weline-eav-manager.css)">

<div class="w-stack w-eav-manager-page" data-gap="md" data-testid="eav-attribute-group-form">
    <template>Weline_Component::message.phtml</template>

    <section class="w-card">
        <div class="w-card__header">
            <div class="w-stack" data-gap="xs">
                <h2 class="w-card__title">
                    <lang>属性组</lang>
                    <notempty name="group"><lang>编辑</lang><else/><lang>添加</lang></notempty>
                </h2>
                <p class="w-text" data-tone="muted"><lang>维护属性组名称、代码、所属实体与属性集</lang></p>
            </div>
        </div>
        <div class="w-card__body">
            <w:form class="w-stack needs-validation" data-gap="md" novalidate="" action="{{action}}" method="post">
                <div class="w-cluster" style="flex-wrap:wrap;align-items:flex-start;gap:1rem;">
                    <label class="w-field" style="flex:1 1 12rem;min-width:10rem;">
                        <span class="w-field__label" for="validationName"><lang>组名</lang></span>
                        <input class="w-input" type="text" maxlength="60" data-parsley-minlength="3"
                               id="validationName" placeholder="@lang{组名}"
                               value="{{group.name}}" name="name" required="">
                    </label>
                    <label class="w-field" style="flex:1 1 12rem;min-width:10rem;">
                        <span class="w-field__label" for="validationCode"><lang>组代码</lang></span>
                        <input class="w-input" type="text" maxlength="60" data-parsley-minlength="3"
                               id="validationCode" placeholder="@lang{组代码}"
                               value="{{group.code}}" name="code" required="">
                    </label>
                    <label class="w-field" style="flex:1 1 12rem;min-width:10rem;">
                        <span class="w-field__label" for="validationEntity"><lang>实体</lang></span>
                        <select class="w-select" name="eav_entity_id" id="validationEntity" required="">
                            <foreach name="entities">
                                <option value="{{v.eav_entity_id}}" @if{group.eav_entity_id== v.eav_entity_id=> 'selected'}>{{v.name}}</option>
                            </foreach>
                        </select>
                    </label>
                    <label class="w-field" style="flex:1 1 12rem;min-width:10rem;">
                        <span class="w-field__label" for="validationSet"><lang>属性集</lang></span>
                        <select class="w-select" name="set_id" id="validationSet" required="required" data-selected="{{group.set_id}}">
                            <if condition="group_set">
                                <option value="{{group_set.set_id}}" selected>{{group_set.name}}</option>
                            </if>
                        </select>
                    </label>
                </div>
                <div class="w-cluster" style="gap:0.5rem;">
                    <button class="w-button" data-tone="primary" type="submit">
                        <empty name="group"><lang>添加</lang><else/><lang>保存</lang></empty>
                    </button>
                    <notempty name="group">
                        <a class="w-button" data-tone="neutral" data-variant="outline" href="@backend-url('*/backend/attribute/group/add')">
                            <lang>继续新增</lang>
                        </a>
                    </notempty>
                </div>
            </w:form>
        </div>
    </section>
</div>

<js:part>
(function() {
    'use strict';

    var validationEntity = $('#validationEntity');
    var validationSet = $('#validationSet');

    validationEntity.change(function () {
        var selectedOption = $(this).find(':selected').val();
        renderAttributeSet(selectedOption);
    });

    var initSelected = validationEntity.find(':selected').val();
    if(!validationSet.find(':selected').val()){
        renderAttributeSet(initSelected);
    }

    function renderAttributeSet(eav_entity_id) {
        bqAdmin['eav_admin']((function(cfg){return typeof cfg==='string'?cfg:(cfg&&cfg.url);})("@backend-url('*/backend/attribute/set/api-search')?eav_entity_id=" + eav_entity_id).then(function(res) {
            var items = res['items'];
            validationSet.empty();
            for (var itemsKey in items) {
                var item = items[itemsKey];
                var selected = '';
                if (validationSet.attr('data-selected') === item.set_id) {
                    selected = 'selected';
                }
                validationSet.append("<option value='" + item.set_id + "' " + selected + ">" + window.lang(item.name) + "</option>");
            }
        });
    }
})();
</js:part>
