<script>
(function(g){
  if(g.__bqAdmin_eav_admin) return;
  g.__bqAdmin_eav_admin=true;
  g.bqAdmin=g.bqAdmin||{};
  g.bqAdmin['eav_admin']=function(url, options){
    options=options||{};
    var body=options.body;
    if(body && typeof FormData!=='undefined' && body instanceof FormData){
      var p=new URLSearchParams(); body.forEach(function(v,k){ if(!(typeof File!=='undefined'&&v instanceof File)) p.append(k,String(v)); }); body=p.toString();
    } else if(body && typeof body!=='string'){ try{ body=JSON.stringify(body); }catch(e){ body=''; } }
    var run=function(api){ return api.resource('eav_admin').adminRequest({url:url, method:options.method||'POST', headers:options.headers||{}, body:body||''}); };
    var toResp=function(data){
      var _biz=g.WelineApiBusiness||(g.Weline&&g.Weline.ApiBusiness);
      if(_biz&&typeof _biz.wrapAdminBridgeResult==='function'){
        return _biz.wrapAdminBridgeResult(data);
      }
      var body=(data&&typeof data==='object'&&!Array.isArray(data))?data:{success:true,data:data};
      var ok=!(body&&body.success===false);
      var resp={ok:ok,status:ok?200:400,json:function(){return Promise.resolve(body);},text:function(){return Promise.resolve(typeof body==='string'?body:JSON.stringify(body==null?{}:body));}};
      Object.keys(body).forEach(function(k){ if(k==='ok'||k==='json'||k==='text'||k==='status') return; resp[k]=body[k]; });
      return resp;
    };
    var p=(g.Weline&&g.Weline.load)?g.Weline.load('api').then(run):Promise.resolve(run(g.Weline.Api));
    return p.then(toResp);
  };
})(window);
</script>
<?php
/** @var \Weline\Framework\View\Template $this */
$attributes = is_array($attributes ?? null) ? $attributes : [];
$fmtTime = static function (mixed $value): string {
    $text = trim((string)$value);
    if ($text === '') {
        return '—';
    }

    return strlen($text) > 19 ? substr($text, 0, 19) : $text;
};
foreach ($attributes as &$attributeRow) {
    if (!is_array($attributeRow)) {
        continue;
    }
    $attributeRow['create_time_full'] = (string)($attributeRow['create_time'] ?? '');
    $attributeRow['update_time_full'] = (string)($attributeRow['update_time'] ?? '');
    $attributeRow['create_time_short'] = $fmtTime($attributeRow['create_time_full']);
    $attributeRow['update_time_short'] = $fmtTime($attributeRow['update_time_full']);
    $attributeRow['entity_label'] = (string)($attributeRow['entity_local_name'] ?? $attributeRow['entity_name'] ?? '');
    // Normalize flags for template <if condition> (empty/notempty is unreliable for int 0/1 here).
    $attributeRow['is_system'] = !empty($attributeRow['is_system']) ? 1 : 0;
    $attributeRow['basic_is_enable'] = !empty($attributeRow['basic_is_enable']) ? 1 : 0;
}
unset($attributeRow);
$this->assign('attributes', $attributes);
?>
<link rel="stylesheet" href="@static(Weline_Eav::css/eav-attribute-list.css)&v=20260903-attr-list4">

<div class="w-stack w-eav-attribute-list" data-gap="lg" data-testid="eav-attribute-list">
    <header class="w-eav-attribute-list__hero">
        <div class="w-eav-attribute-list__hero-icon" aria-hidden="true">
            <w:icon name="tag" size="lg" />
        </div>
        <div class="w-eav-attribute-list__hero-copy">
            <h1><lang>属性管理</lang></h1>
            <p>
                <lang>管理实体的属性定义、启用状态与本地化名称</lang>
                <notempty name="entity">
                    · <lang>当前实体</lang> <strong>{{entity.name}}</strong>
                </notempty>
            </p>
        </div>
        <div class="w-eav-attribute-list__hero-actions">
            <a class="w-button" data-tone="primary" data-variant="outline" data-size="sm"
               href="@backend-url('*/backend/manager')">
                <w:icon name="layers" size="sm" />
                <lang>打开 EAV 管理器</lang>
            </a>
            <block class="Weline\Component\Block\OffCanvas"
                   template="Weline_Component::off-canvas.phtml"
                   title="添加属性"
                   cache="0"
                   id="create_attr_off_canvas_new"
                   action="*/backend/attribute/add"
                   target-tag="button"
                   icon="plus"
                   submit-button-text="保存"
                   submit-button-class="btn btn-primary"
                   target-button-text="添加属性"
                   target-button-class="w-button"
                   target-tone="primary"
                   flush-button-text="刷新"
                   flush-button-class="btn btn-light"
                   flush="1"
                   save="1"
                   save-form="#attr-form"
                   close-button-show="1"
                   close-button-text="取消"
                   direction="right"
                   class-names="w-75"
                   off-canvas-body-style=""
            />
        </div>
    </header>

    <div>
        <template>Weline_Component::message.phtml</template>
    </div>

    <section class="w-card w-eav-attribute-list__card">
        <div class="w-card__header">
            <div>
                <h2 class="w-eav-attribute-list__card-title"><lang>属性列表</lang></h2>
                <p class="w-eav-attribute-list__card-desc"><lang>系统属性不可删除；可搜索代码、名称与实体</lang></p>
            </div>
            <div class="w-eav-attribute-list__toolbar">
                <block class="Weline\Component\Block\Form\Search"
                       id="entity-attribute-search"
                       action="*/backend/attribute"
                       method="get"
                       keyword="search"
                       value=""
                       placeholder="请输入属性关键词回车搜索"></block>
            </div>
        </div>

        <div class="w-card__body w-eav-attribute-list__body">
            <div class="w-eav-attribute-list__table-wrap">
                <table class="w-eav-attribute-list__table" id="eav-attribute-list-table">
                    <thead>
                    <tr>
                        <th><lang>ID</lang></th>
                        <th><lang>实体</lang></th>
                        <th><lang>代码</lang></th>
                        <th><lang>名称</lang></th>
                        <th><lang>状态</lang></th>
                        <th><lang>类型</lang></th>
                        <th><lang>创建时间</lang></th>
                        <th><lang>更新时间</lang></th>
                        <th><lang>操作</lang></th>
                    </tr>
                    </thead>
                    <tbody>
                    <empty name="attributes">
                        <tr>
                            <td colspan="9" class="w-eav-attribute-list__empty">
                                <lang>暂无属性记录</lang>
                            </td>
                        </tr>
                    </empty>
                    <foreach name="attributes" item="attribute" key="key">
                        <tr>
                            <td class="w-eav-attribute-list__id">{{attribute.attribute_id}}</td>
                            <td>
                                <div class="w-eav-attribute-list__entity">
                                    <span class="w-eav-attribute-list__entity-id">#{{attribute.eav_entity_id|0}}</span>
                                    <span class="w-eav-attribute-list__entity-name">{{attribute.entity_label}}</span>
                                </div>
                            </td>
                            <td>
                                <code class="w-eav-attribute-list__code">{{attribute.code}}</code>
                            </td>
                            <td class="w-eav-attribute-list__name">
                                <local model="Weline\Eav\Model\EavAttribute\LocalDescription"
                                       field="name"
                                       id="attribute.attribute_id"
                                       name="attribute-name">
                                    {{attribute.local_name|attribute.name}}
                                </local>
                            </td>
                            <td>
                                <if condition="attribute.basic_is_enable">
                                    <span class="w-badge" data-w-background="success"><lang>启用</lang></span>
                                    <else/>
                                    <span class="w-badge" data-w-background="danger"><lang>禁用</lang></span>
                                </if>
                            </td>
                            <td>
                                <if condition="attribute.is_system">
                                    <span class="w-badge" data-w-background="warning"><lang>系统</lang></span>
                                    <else/>
                                    <span class="w-badge" data-w-background="neutral"><lang>自定义</lang></span>
                                </if>
                            </td>
                            <td class="w-eav-attribute-list__meta" title="{{attribute.create_time_full}}">{{attribute.create_time_short}}</td>
                            <td class="w-eav-attribute-list__meta" title="{{attribute.update_time_full}}">{{attribute.update_time_short}}</td>
                            <td>
                                <div class="w-eav-attribute-list__actions">
                                    <if condition="attribute.is_system">
                                        <span class="w-text" data-tone="muted" data-size="sm"><lang>系统属性</lang></span>
                                        <else/>
                                        <block class="Weline\Component\Block\OffCanvas"
                                               template="Weline_Component::off-canvas.phtml"
                                               cache="0"
                                               id="off_canvas_attribute_edit"
                                               action="*/backend/attribute/edit"
                                               vars="attribute"
                                               save="0"
                                               action-params="{attribute_id: attribute.attribute_id, eav_entity_id: attribute.eav_entity_id,isAjax:attribute.attribute_id}"
                                               target-button-text="修改"
                                               target-button-class="w-button"
                                               target-tone="primary"
                                               target-variant="outline"
                                               target-size="sm"
                                               title="编辑属性"
                                               close-button-show="1"
                                               close-button-text="关闭"
                                               direction="right"
                                               class-names="w-75"
                                               off-canvas-body-style=""/>
                                        <a class="w-button ajax-delete"
                                           data-tone="danger"
                                           data-variant="outline"
                                           data-size="sm"
                                           data-href="@backend-url('*/backend/attribute/delete', ['id'=>$attribute['attribute_id']])">
                                            <w:icon name="trash" size="sm" />
                                            <lang>删除</lang>
                                        </a>
                                    </if>
                                </div>
                            </td>
                        </tr>
                    </foreach>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="w-eav-attribute-list__footer">
            {{pagination}}
        </div>
    </section>
</div>

<div id="optionConfig" class="modal fade" tabindex="-1" aria-labelledby="optionConfigLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="optionConfigLabel">
                    <lang>属性配置项弹窗</lang>
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="@lang{关闭}"></button>
            </div>
            <div class="modal-body">
                <h5><lang>配置属性的配置项</lang></h5>
                <div class="table-responsive">
                    <table class="table table-sm table-hover align-middle">
                        <thead>
                        <tr>
                            <th><lang>代码</lang></th>
                            <th><lang>配置项</lang></th>
                            <th><lang>所属实体代码</lang></th>
                            <th><lang>创建时间</lang></th>
                            <th><lang>更新时间</lang></th>
                            <th><lang>操作</lang></th>
                        </tr>
                        </thead>
                        <tbody id="attributeOption"></tbody>
                    </table>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light waves-effect" data-bs-dismiss="modal">
                    <lang>关闭</lang>
                </button>
                <button type="button" class="btn btn-primary waves-effect waves-light">
                    <lang>保存更改</lang>
                </button>
            </div>
        </div>
    </div>
</div>

<js:part>
(function() {
    'use strict';

    $('#optionConfig').on('show.bs.modal', function (event) {
        showLoading();
        var button = event.relatedTarget;
        var attribute_code = button.getAttribute('data-code');
        var eav_entity_id = button.getAttribute('data-eav_entity_id');
        var url = '@backend-url("*/backend/attribute/option/search")?eav_entity_id={eav_entity_id}&attribute_code={attribute_code}'
            .replace('{eav_entity_id}', eav_entity_id).replace('{attribute_code}', attribute_code);
        var req = bqAdmin['eav_admin'](url, { silent: true });
        req.then(function(res) {
            var resData = (res && res.data) || res;
            var attributeOption = $('#attributeOption');
            attributeOption.empty();
            var options = (resData && resData['option']) || resData;
            if (options && typeof options === 'object') {
                for (var opt in options) {
                    attributeOption.append('');
                }
            }
        }).catch(function() {}).finally(function() { hideLoading(); });
    });

    $('.ajax-delete').on('click', function (e) {
        e.preventDefault();
        var self = this;
        Swal.fire({
            title: '<lang>确认删除</lang>',
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: '<lang>确认</lang>',
            cancelButtonText: '<lang>取消</lang>',
            reverseButtons: true
        }).then(function(result) {
            if (result.isConfirmed) {
                showLoading();
                var delUrl = $(self).data('href');
                var req = bqAdmin['eav_admin'](delUrl, { silent: true });
                req.then(function(res) {
                    var data = (res && res.data) || res;
                    if (data && data['code'] == 1) {
                        if (typeof Swal !== 'undefined') {
                            Swal.fire({ title: '<lang>删除成功</lang>', icon: 'success', timer: 2000, confirmButtonColor: '#3085d6', confirmButtonText: '<lang>确认</lang>' }).then(function() { location.reload(); });
                        } else { location.reload(); }
                    }
                }).catch(function() {}).finally(function() { hideLoading(); });
            }
        });
    });
})();
</js:part>
