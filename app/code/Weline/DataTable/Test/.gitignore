# 本目录下的 phpunit.xml 每次运行都会重写这些文件（<logging><junit> / <coverage> / cacheResultFile），
# 属生成物，不应纳入版本控制。注意：test-results.xml 曾被误提交，已于本次一并从索引移除。
test-results.xml
.phpunit.result.cache
.phpunit.cache/
coverage.xml
coverage.txt
coverage-html/
