<!-- weline:module-development-baseline:generated-once -->
# Weline_DataTable 开发日志

## 2026-08-28 — 可逆纯展示列合并

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-DATATABLE-0009`
- 实现：字段配置对只读/前台表格也可见；辅助字段可合并到独立主字段，也可切回“独立显示”；列合并偏好仅存浏览器；当 `capabilities.preferences=true` 时继续以服务端 display/filter fields 为准，本地 fallback 不覆盖服务端字段偏好。
- 数据边界：查询、排序、筛选、导出、表单、API 与数据库继续使用原字段；禁止链式和循环合并。
- 自动化证据：`php bin/w phpunit:run --module=Weline_DataTable --phpunit` 为 83 tests / 664 assertions / 0 failures；JS/PHP lint、DataTable 定向 `git diff --check`、i18n 收集、资源编译及前端 section/layout 检查均通过。
- 运行时证据：专用 WLS `ai-datatable-merge-17878777`（9528）下入口返回 200；验收结束后该实例已完整停止。9555 交付实例 `datatable-delivery-17878780` 保持 4/4 Worker 运行，入口再次探活为 200。
- WebUI 操作证据：字段配置入口可见；把 Email 与 Order No 合并到 User Name 后，表头由 6 列变为 4 列，刷新后偏好保持；再把 Email 切回“独立显示”后恢复为 5 列，Order No 仍保持合并，证明可以逐字段解开。
- 响应式证据：`doc/evidence/ch2/column-merge-config-375.png`、`doc/evidence/ch2/column-merge-config-768.png`、`doc/evidence/ch2/column-merge-result-1024.png`、`doc/evidence/ch2/column-merge-result-1440.png`。
- 文档回退说明：项目智能 MCP 对 PNG 证据父计划及唯一子批次均返回 `CONTEXT_BATCH_PLANNED` 且缺失目标不变，记录为 `MCP_TARGET_UNAVAILABLE`；仅用受限原生回退更新本条开发日志，未改源码或其他文档。
- 交付入口：https://p05113ef3.weline.test:9555/datatable/test/join

## 2026-08-28 — 1.1.0 统一后台 DataTable 增强

- 当前状态：`implemented_validated`；DataTable 运行链路已通过，后台计划 dialog 的浏览器点击证据受现有 Admin 登录 404 阻断。
- 需求 ID：`REQ-DATATABLE-0004` 至 `REQ-DATATABLE-0008`。
- 安全边界：新增显式资源注册、统一 ORM/声明式元数据、写字段白名单；默认 Provider 的写操作要求 backend + 具体 ACL，旧 REST 兼容入口同步收口。
- 写入链路：`previewWrite` 输出完整字段、必填缺口、依赖、顺序和事务范围；仅可执行计划签发 5 分钟单次 token；`executeWrite` 校验载荷摘要后执行。
- 事务修正：真实 ORM 集成首次复现旧助手直接开启 PDO 后被框架判定为“未受事务协调器管理”；`executeInTransaction` 已切换到 `WriteIntentTransactionCoordinatorInterface`，模型写入复用统一 `TransactionContext`。
- Taglib/UI：新增 resource、filter-mode、filter-advanced、filter-collapsible、write-order、composite-write、confirm-write；local 支持 client 搜索/筛选/多列排序/分页和 localStorage 偏好；补写入计划 dialog、权限态、忙碌/错误/空状态、键盘与四断点样式。
- 文档：`连表管理与写入计划.md`、`原型设计.md`、`Taglib/doc/场景映射表.md`，并同步使用/API/local/需求文档。
- 实现回退说明：项目智能 MCP 的 `get_edit_bundle` 对新文件和本次目标返回终态 `CONTEXT_TARGET_NOT_FOUND`，记录为 `MCP_TARGET_UNAVAILABLE`；后续严格限制在 DataTable 精确路径与定向验证，未使用替代仓库索引。
- 自动化证据：`php bin/w phpunit:run --module=Weline_DataTable --phpunit` 为 80 tests / 653 assertions / 0 failures；其中 `CompositeWriteRuntimeTest` 在隔离 SQLite 中验证真实 ORM 写入与回滚。
- 配置运行时证据：在当前 PostgreSQL 配置下直接执行同一 `DemoTableService`，`datatable.write-plan.v1`、依赖主键回填、token 复用拒绝、载荷篡改拒绝、唯一键冲突回滚全部通过；本次创建的精确测试用户已清理。
- WebUI 证据：专用 WLS `ai-dt-0828-1640-01a043`（9527）下公开入口返回 200；Weline Chrome 实测 local 搜索及 375/768/1024/1440 四断点，截图见 `doc/evidence/ch1/`。后台 Demo 命中 ACL 并重定向登录，但 `/{backend_prefix}/admin/login` 返回 404，故未记录计划 dialog 的伪截图。验收结束后该专用实例已停止，`server:status --all` 仅保留原有 `default` 实例。
- 构建证据：`php bin/w resource:compile welineUi`、`php bin/w frontend:check-section-code`、`php bin/w setup:upgrade --route -m Weline_DataTable` 通过。全量 `setup:upgrade` 被既有 `Weline_Promotion 1.1.4` 两张表的 schema checkpoint 冲突阻断，未越界修改 Promotion。

## 2026-08-25 — local 模式 / 勾选 / 操作列悬浮文档化

- 当前状态：`docs_handoff` → 运行时修复已落地（Framework OwnsChildCompilation）
- 需求 ID：`REQ-DATATABLE-0003`
- 范围：补强 `mode=local`、`selectable`、`sticky-actions`、`row-actions`、`type=image` 占位后，写入 AI 硬约束文档，防止业务页乱用。
- 文档：`doc/本地模式与列悬浮.md`；同步 `使用指南.md`、`AI-INDEX.md`、`快速入门指南.md`、`需求.md`。
- 代码证据：`Taglib/Table.php`、`view/statics/js/datatable-manager.js`、`view/statics/js/datatable-common.js`；业务范例 `Weline_Product` catalog products（只读）。
- **缺陷修复（同日）**：商品列表 `row-actions="<?= ... ?>"` 使 `d-table` 走 runtime 编译时，旧 CodeGenerator 会先编译 `t-header`，抛出「必须指定 model 或位于 d-table 内」。已在 `Framework/View/Taglib/Generator/CodeGenerator.php` 对 OwnsChildCompilation 父标签改为传入原始子源码；清模板缓存并 `server:reload -r` 后验收。
- 验收：文档写清适用场景与反例；改 JS 后需 `resource:compile welineUi` 已在专题文档标明；catalog products 页不再 500。

## 2026-08-22 — 测试页 section 语义标识修复

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-DATATABLE-0002`
- 范围：仅为测试数据卡片添加 `datatable.test.demo_data`，保留现有页面改动。
- 验收：全量前端 section 规范检查 0 违规，主题编译和完整路由升级通过。

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 版本索引

### 2026-08-28 — 分页页大小可见性修复

- 用户反馈：分页页脚的“每页显示”下拉框只有箭头，当前数量不可辨识。
- 实现：在 `TableFooter` 输出可见当前页大小文本，并由 `datatable-manager.js` 在切换后同步；选择器补充无障碍名称。
- 验收：DataTable 80/80 测试通过；真实 `/datatable/test/join` HTML 可见默认 `6` 与 10/20/50/100 选项；主题编译通过。

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.0`（文档基线） | docs_handoff | `REQ-DATATABLE-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |
| local/sticky 契约文档 | docs_handoff | `REQ-DATATABLE-0003` | 2026-08-25 | 2026-08-25 | pending user / not released |
| `1.1.0` 统一后台 DataTable | implemented_validated | `REQ-DATATABLE-0004`–`0008` | 2026-08-27 | 2026-08-28 | DataTable runtime passed；Admin 登录可视验收 blocked / not released |

## `1.0.0` — 文档制度启用基线

- 基线版本：`1.0.0`
- 目标版本：`1.0.0`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-DATATABLE-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-DATATABLE-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-DATATABLE-0001` / `1.0.0` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
