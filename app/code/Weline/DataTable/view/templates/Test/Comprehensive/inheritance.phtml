<?php
$escape = static fn(mixed $value): string => htmlspecialchars((string) $value, ENT_QUOTES, 'UTF-8');
$adminConfig = json_encode([
    'verifyUrl' => (string) ($verifyTagsUrl ?? ''),
    'requestFailed' => (string) __('Verification failed.'),
], JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES | JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT);
?>
<?= \Weline\DataTable\Helper\UiAssets::renderBundles($this, ['datatable-demo-css', 'datatable-demo-admin-js']) ?>

<main class="w-container w-datatable-demo__admin" data-w-datatable-verification data-testid="datatable-inheritance-page">
    <header class="w-datatable-demo__heading">
        <div>
            <p class="w-datatable-demo__eyebrow"><?= __('DataTable') ?></p>
            <h1><?= __('Attribute Inheritance Verification') ?></h1>
            <p><?= __('This backend route isolates inherited table attributes and generated scopes while using the same complete Taglib implementation as the other demos.') ?></p>
        </div>
        <div class="w-cluster" data-justify="end">
            <a class="w-button" data-tone="neutral" href="<?= $escape($comprehensiveUrl ?? '') ?>"><w-icon name="arrow-left" size="sm"></w-icon><span><?= __('Back') ?></span></a>
            <a class="w-button" data-tone="neutral" href="<?= $escape(($tagTestUrl ?? '') . '?focus=attribute_inheritance') ?>"><w-icon name="check-circle" size="sm"></w-icon><span><?= __('Open Full Tag Report') ?></span></a>
        </div>
    </header>

    <?= $this->fetch('Weline_DataTable::backend/templates/test/layout-switcher.phtml') ?>

    <div class="w-alert" data-tone="info">
        <w-icon name="info" size="sm"></w-icon>
        <p><?= __('The verification endpoint exercises inherited model, sortable, searchable, API provider, dependency, and transaction attributes through TableContext::inheritTableAttributes().') ?></p>
    </div>

    <section class="w-card">
        <header class="w-card__header w-datatable-demo__section-heading">
            <h2 class="w-card__title"><?= __('Run Verification') ?></h2>
            <button type="button" class="w-button" data-testid="datatable-inheritance-run" data-w-datatable-verify data-section="attribute_inheritance"><w-icon name="play" size="sm"></w-icon><span><?= __('Run Now') ?></span></button>
        </header>
        <div class="w-card__body">
            <pre id="datatable-inheritance-output" class="w-datatable-demo__document" data-w-datatable-verify-output data-testid="datatable-inheritance-output" tabindex="0"><?= $escape(__('No verification run yet.')) ?></pre>
        </div>
    </section>

    <script type="application/json" id="w-datatable-admin-config"><?= $adminConfig ?: '{}' ?></script>
</main>
