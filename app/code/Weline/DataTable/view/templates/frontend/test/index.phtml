<?php
$escape = static fn(mixed $value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
$links = is_array($demo_links ?? null) ? $demo_links : [];
$demos = [
    ['basic', 'Basic', 'CRUD, filter, sort, pagination and export.', 'table', 'success'],
    ['join', 'Join', 'Alias fields and cross-table filtering.', 'link', 'info'],
    ['form', 'Form', 'Standalone create flow with table refresh.', 'edit', 'success'],
    ['upload', 'Upload', 'Image and file controls with previews.', 'upload', 'warning'],
    ['transaction', 'Transaction', 'Multi-model save in one transaction.', 'shield', 'warning'],
    ['dependency', 'Dependency', 'Dependency-ordered multi-model save.', 'tree', 'warning'],
    ['cascade', 'Cascade', 'Delete related records and reload both tables.', 'trash', 'warning'],
    ['performance', 'Performance', 'Generated fields and responsive large-table path.', 'chart', 'neutral'],
];
$config = [
    'apiProvider' => 'datatable',
    'clearConfirm' => (string)__('确定要清理所有测试数据吗？此操作不可恢复！'),
    'requestFailed' => (string)__('请求失败'),
];
?>
<?= \Weline\DataTable\Helper\UiAssets::renderBundles($this, ['datatable-demo-css', 'datatable-demo-index-js']) ?>

<div class="w-container w-datatable-demo__main" data-w-datatable-demo="index" data-demo-page="<?= $escape($page_key ?? 'index') ?>">
    <header class="w-datatable-demo__heading">
        <div>
            <p class="w-datatable-demo__eyebrow">Weline UI 2.0</p>
            <h1><?= $escape($page_title ?? 'DataTable Frontend Demo') ?></h1>
            <p>Frontend-safe DataTable demos backed by the native Weline Table, Form, Dialog and API components.</p>
        </div>
    </header>

    <?= $this->fetch('Weline_DataTable::backend/templates/test/layout-switcher.phtml') ?>

    <section class="w-card" aria-labelledby="w-demo-data-title" weline-code="datatable.test.demo_data">
        <header class="w-card__header">
            <h2 id="w-demo-data-title" class="w-card__title"><w-icon name="database" size="sm"></w-icon>Demo Data</h2>
        </header>
        <div class="w-card__body w-stack">
            <div class="w-cluster">
                <button type="button" class="w-button" data-testid="demo-init" data-w-demo-action="init"><w-icon name="plus" size="sm"></w-icon>Init Demo Data</button>
                <button type="button" class="w-button" data-tone="danger" data-testid="demo-clear" data-w-demo-action="clear"><w-icon name="trash" size="sm"></w-icon>Clear Demo Data</button>
            </div>
            <div class="w-alert" data-tone="info" data-w-demo-status data-testid="demo-status" role="status" aria-live="polite"><w-icon name="info" size="sm"></w-icon><span>Ready</span></div>
        </div>
    </section>

    <nav class="w-datatable-demo__grid" aria-label="DataTable demos">
        <?php foreach ($demos as [$key, $label, $description, $icon, $tone]): ?>
            <a class="w-card w-datatable-demo__link" data-testid="demo-link-<?= $escape($key) ?>" href="<?= $escape($links[$key] ?? ('/datatable/test/' . $key)) ?>">
                <div class="w-card__body">
                    <span class="w-datatable-demo__link-icon" data-tone="<?= $escape($tone) ?>"><w-icon name="<?= $escape($icon) ?>" size="lg"></w-icon></span>
                    <span><strong><?= $escape($label) ?></strong><small><?= $escape($description) ?></small></span>
                    <w-icon name="chevron-right" size="sm"></w-icon>
                </div>
            </a>
        <?php endforeach; ?>
    </nav>
</div>
<script type="application/json" id="w-datatable-demo-config"><?= json_encode($config, JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES | JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT) ?: '{}' ?></script>
