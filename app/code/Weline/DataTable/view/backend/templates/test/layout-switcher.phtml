<?php if (!empty($layoutOptions) && is_array($layoutOptions)): ?>
    <style>
        .dt-layout-switcher{display:grid;gap:12px;padding:16px 18px;border:1px solid #dbe4f0;border-radius:18px;background:#f8fbff}
        .dt-layout-switcher h3{margin:0;font-size:16px}
        .dt-layout-switcher p{margin:0;color:#526071}
        .dt-layout-switcher-actions{display:flex;flex-wrap:wrap;gap:10px}
        .dt-layout-switcher-chip{display:inline-flex;align-items:center;padding:8px 12px;border:1px solid #d5e3f4;border-radius:999px;background:#fff;color:#1f2937;text-decoration:none;transition:all .2s ease}
        .dt-layout-switcher-chip:hover{border-color:#84a9ff;background:#eef4ff}
        .dt-layout-switcher-chip.active{border-color:#2563eb;background:#2563eb;color:#fff}
    </style>

    <section class="dt-layout-switcher" data-testid="datatable-layout-switcher">
        <div>
            <h3><?= htmlspecialchars((string) ($layoutSwitcherTitle ?? __('Backend Layouts')), ENT_QUOTES, 'UTF-8') ?></h3>
            <?php if (!empty($layoutSwitcherDescription)): ?>
                <p><?= htmlspecialchars((string) $layoutSwitcherDescription, ENT_QUOTES, 'UTF-8') ?></p>
            <?php endif; ?>
        </div>
        <div class="dt-layout-switcher-actions">
            <?php foreach ($layoutOptions as $option): ?>
                <?php $active = !empty($option['active']); ?>
                <a class="dt-layout-switcher-chip<?= $active ? ' active' : '' ?>"
                   href="<?= htmlspecialchars((string) ($option['url'] ?? ''), ENT_QUOTES, 'UTF-8') ?>"
                   title="<?= htmlspecialchars((string) ($option['description'] ?? ''), ENT_QUOTES, 'UTF-8') ?>"
                   data-testid="datatable-layout-option-<?= htmlspecialchars((string) ($option['key'] ?? ''), ENT_QUOTES, 'UTF-8') ?>"
                    <?= $active ? 'aria-current="page"' : '' ?>>
                    <?= htmlspecialchars((string) ($option['title'] ?? ''), ENT_QUOTES, 'UTF-8') ?>
                </a>
            <?php endforeach; ?>
        </div>
    </section>
<?php endif; ?>
