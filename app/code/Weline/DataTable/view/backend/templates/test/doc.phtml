<?php $escape = static fn(mixed $value): string => htmlspecialchars((string) $value, ENT_QUOTES, 'UTF-8'); ?>
<?= \Weline\DataTable\Helper\UiAssets::renderBundles($this, ['datatable-demo-css']) ?>

<main class="w-container w-datatable-demo__admin">
    <?= $this->fetch('Weline_DataTable::backend/templates/test/layout-switcher.phtml') ?>

    <div class="w-datatable-demo__docs">
        <aside class="w-card w-datatable-demo__docs-sidebar">
            <header class="w-card__header w-datatable-demo__section-heading">
                <h1 class="w-card__title"><?= __('Documentation') ?></h1>
                <a class="w-button" data-tone="neutral" data-size="sm" href="<?= $escape($dashboardUrl ?? '') ?>"><w-icon name="arrow-left" size="sm"></w-icon><span><?= __('Back') ?></span></a>
            </header>
            <nav class="w-card__body w-stack" aria-label="<?= $escape(__('DataTable Documentation')) ?>">
                <?php foreach (($docs ?? []) as $doc): ?>
                    <?php $isActive = (string) ($selectedDoc['key'] ?? '') === (string) ($doc['key'] ?? ''); ?>
                    <a class="w-datatable-demo__doc-link"
                       data-state="<?= $isActive ? 'active' : 'idle' ?>"
                       href="<?= $escape($doc['url'] ?? $docUrl ?? '') ?>"
                       data-testid="datatable-doc-link-<?= $escape($doc['key'] ?? '') ?>"
                       <?= $isActive ? 'aria-current="page"' : '' ?>>
                        <strong><?= $escape($doc['title'] ?? '') ?></strong>
                        <small><?= $escape($doc['summary'] ?? '') ?></small>
                    </a>
                <?php endforeach; ?>
            </nav>
        </aside>

        <article class="w-card w-datatable-demo__docs-content" data-testid="datatable-doc-content">
            <header class="w-card__header w-datatable-demo__heading">
                <div>
                    <h2 class="w-card__title"><?= $escape($selectedDoc['title'] ?? '') ?></h2>
                    <code class="w-datatable-demo__code-inline"><?= $escape($selectedDoc['filename'] ?? '') ?></code>
                </div>
                <div class="w-cluster" data-justify="end">
                    <a class="w-button" data-tone="neutral" href="<?= $escape($comprehensiveUrl ?? '') ?>" data-testid="datatable-doc-open-comprehensive"><w-icon name="beaker" size="sm"></w-icon><span><?= __('Open Comprehensive Test') ?></span></a>
                    <a class="w-button" data-tone="neutral" href="<?= $escape($tagTestUrl ?? '') ?>" data-testid="datatable-doc-open-tag"><w-icon name="check-circle" size="sm"></w-icon><span><?= __('Open Tag Verification') ?></span></a>
                </div>
            </header>
            <div class="w-card__body w-stack">
                <p><?= $escape($selectedDoc['summary'] ?? '') ?></p>
                <pre class="w-datatable-demo__document"><code><?= $escape($selectedDoc['content'] ?? '') ?></code></pre>
            </div>
        </article>
    </div>
</main>
