<style>
    .dt-doc-page{display:grid;grid-template-columns:minmax(260px,320px) minmax(0,1fr);gap:20px}
    .dt-doc-sidebar,.dt-doc-content{padding:20px;border:1px solid #dbe4f0;border-radius:18px;background:#fff}
    .dt-doc-nav{display:grid;gap:12px}
    .dt-doc-link{display:block;padding:14px 16px;border:1px solid #e2e8f0;border-radius:14px;color:#1f2937;text-decoration:none}
    .dt-doc-link.active{border-color:#84a9ff;background:#eef4ff}
    .dt-doc-preview{font-size:13px;color:#526071;margin-top:8px}
    .dt-doc-body{white-space:pre-wrap;background:#f8fafc;border:1px solid #e2e8f0;border-radius:14px;padding:18px;max-height:70vh;overflow:auto}
    @media (max-width: 960px){.dt-doc-page{grid-template-columns:1fr}}
</style>

<?= $this->fetch('Weline_DataTable::backend/templates/test/layout-switcher.phtml') ?>

<div class="dt-doc-page">
    <aside class="dt-doc-sidebar">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h3 class="mb-0"><?= __('Documentation') ?></h3>
            <a class="btn btn-sm btn-outline-secondary" href="<?= $dashboardUrl ?>"><?= __('Back') ?></a>
        </div>
        <div class="dt-doc-nav">
            <?php foreach (($docs ?? []) as $doc): ?>
                <?php $isActive = (string) ($selectedDoc['key'] ?? '') === (string) ($doc['key'] ?? ''); ?>
                <a class="dt-doc-link<?= $isActive ? ' active' : '' ?>" href="<?= htmlspecialchars((string) ($doc['url'] ?? $docUrl), ENT_QUOTES, 'UTF-8') ?>" data-testid="datatable-doc-link-<?= htmlspecialchars((string) ($doc['key'] ?? ''), ENT_QUOTES, 'UTF-8') ?>">
                    <strong><?= htmlspecialchars((string) ($doc['title'] ?? ''), ENT_QUOTES, 'UTF-8') ?></strong>
                    <div class="dt-doc-preview"><?= htmlspecialchars((string) ($doc['summary'] ?? ''), ENT_QUOTES, 'UTF-8') ?></div>
                </a>
            <?php endforeach; ?>
        </div>
    </aside>

    <section class="dt-doc-content" data-testid="datatable-doc-content">
        <div class="d-flex flex-wrap gap-2 justify-content-between align-items-center mb-3">
            <div>
                <h2 class="mb-1"><?= htmlspecialchars((string) ($selectedDoc['title'] ?? ''), ENT_QUOTES, 'UTF-8') ?></h2>
                <div class="text-muted"><?= htmlspecialchars((string) ($selectedDoc['filename'] ?? ''), ENT_QUOTES, 'UTF-8') ?></div>
            </div>
            <div class="d-flex gap-2">
                <a class="btn btn-outline-secondary" href="<?= $comprehensiveUrl ?>" data-testid="datatable-doc-open-comprehensive"><?= __('Open Comprehensive Test') ?></a>
                <a class="btn btn-outline-secondary" href="<?= $tagTestUrl ?>" data-testid="datatable-doc-open-tag"><?= __('Open Tag Verification') ?></a>
            </div>
        </div>
        <p><?= htmlspecialchars((string) ($selectedDoc['summary'] ?? ''), ENT_QUOTES, 'UTF-8') ?></p>
        <div class="dt-doc-body"><?= htmlspecialchars((string) ($selectedDoc['content'] ?? ''), ENT_QUOTES, 'UTF-8') ?></div>
    </section>
</div>
