<?php
$escape = static fn(mixed $value): string => htmlspecialchars((string) $value, ENT_QUOTES, 'UTF-8');
$adminConfig = json_encode([
    'verifyUrl' => (string) ($verifyUrl ?? ''),
    'requestFailed' => (string) __('Verification request failed.'),
], JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES | JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT);
?>
<?= \Weline\DataTable\Helper\UiAssets::renderBundles($this, ['datatable-demo-css', 'datatable-demo-admin-js']) ?>

<main class="w-container w-datatable-demo__admin" data-w-datatable-verification>
    <section class="w-card w-datatable-demo__hero">
        <header class="w-card__header w-datatable-demo__heading">
            <div>
                <p class="w-datatable-demo__eyebrow"><?= __('DataTable') ?></p>
                <h1 class="w-card__title"><?= __('DataTable Tag Verification') ?></h1>
                <p><?= __('This page exercises tag registration, render callbacks, context propagation, attribute inheritance, and auto-generated table markup.') ?></p>
            </div>
            <div class="w-cluster" data-justify="end">
                <a class="w-button" data-tone="neutral" href="<?= $escape($dashboardUrl ?? '') ?>"><w-icon name="arrow-left" size="sm"></w-icon><span><?= __('Back To Dashboard') ?></span></a>
                <a class="w-button" data-tone="neutral" href="<?= $escape($comprehensiveUrl ?? '') ?>"><w-icon name="beaker" size="sm"></w-icon><span><?= __('Open Comprehensive Test') ?></span></a>
                <a class="w-button" data-tone="neutral" href="<?= $escape($docUrl ?? '') ?>"><w-icon name="book" size="sm"></w-icon><span><?= __('Open Docs') ?></span></a>
            </div>
        </header>
        <div class="w-card__body w-cluster">
            <span class="w-badge" data-tone="info" data-testid="datatable-tag-summary">
                <?= (int) ($report['summary']['passed_checks'] ?? 0) ?> / <?= (int) ($report['summary']['total_checks'] ?? 0) ?> <?= __('checks passing') ?>
            </span>
            <button type="button" class="w-button" data-testid="datatable-tag-refresh" data-w-datatable-verify-reload><w-icon name="refresh" size="sm"></w-icon><span><?= __('Reload Verification') ?></span></button>
        </div>
    </section>

    <?= $this->fetch('Weline_DataTable::backend/templates/test/layout-switcher.phtml') ?>

    <section id="datatable-tag-sections"
             class="w-grid w-datatable-demo__verification-grid"
             data-w-tag-sections
             data-focus="<?= $escape($focusSection ?? '') ?>"
             aria-label="<?= $escape(__('Verification Sections')) ?>">
        <?php foreach (($report['sections'] ?? []) as $sectionKey => $sectionResults): ?>
            <article class="w-card" id="section-<?= $escape($sectionKey) ?>" data-testid="datatable-tag-section-<?= $escape($sectionKey) ?>">
                <header class="w-card__header"><h2 class="w-card__title"><?= $escape($sectionKey) ?></h2></header>
                <div class="w-card__body w-stack">
                    <?php foreach ($sectionResults as $checkKey => $result): ?>
                        <?php $success = (string) ($result['status'] ?? '') === 'success'; ?>
                        <div class="w-datatable-demo__verification-item" data-state="<?= $success ? 'success' : 'error' ?>">
                            <w-icon name="<?= $success ? 'check-circle' : 'x-circle' ?>" size="sm" label="<?= $escape($success ? __('Passed') : __('Failed')) ?>"></w-icon>
                            <div>
                                <strong><?= $escape($checkKey) ?></strong>
                                <p><?= $escape($result['message'] ?? '') ?></p>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            </article>
        <?php endforeach; ?>
    </section>

    <script type="application/json" id="w-datatable-admin-config"><?= $adminConfig ?: '{}' ?></script>
</main>
