<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= $page_title ?></title>
    <link href="@static(Weline_Frontend::assets/css/bootstrap.min.css)" rel="stylesheet">
    <link href="@static(Weline_Frontend::assets/css/icons.min.css)" rel="stylesheet">
    <?= $this->getChildHtml('head') ?>
</head>
<body>
    <div class="container-fluid mt-4">
        <!-- 页面标题 -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center">
                    <h1 class="h3 mb-0"><?= $page_title ?></h1>
                    <a href="/datatable/test" class="btn btn-outline-secondary">
                        <i class="mdi mdi-arrow-left me-1"></i>返回测试首页
                    </a>
                </div>
            </div>
        </div>

        <!-- 测试说明 -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="alert alert-info">
                    <h6><i class="mdi mdi-information me-2"></i>测试说明</h6>
                    <p class="mb-0">
                        本页面测试DataTable的基础功能，包括自动字段生成、分页、排序、筛选等核心功能。
                        请确保已初始化测试数据。
                    </p>
                </div>
            </div>
        </div>

        <!-- 测试用例1：最简单的用法 -->
        <div class="row mb-5">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="mdi mdi-table me-2"></i>测试用例1：最简单的用法
                        </h5>
                        <small class="text-muted">只需指定model和scope，自动生成所有字段</small>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <h6>代码示例：</h6>
                            <pre class="bg-light p-3 rounded"><code>&lt;w:d-table model="Weline\DataTable\Model\TestUser" scope="test-user-basic"&gt;&lt;/w:d-table&gt;</code></pre>
                        </div>
                        
                        <!-- DataTable 组件 -->
                        <w:d-table model="Weline\DataTable\Model\TestUser" scope="test-user-basic"></w:d-table>
                    </div>
                </div>
            </div>
        </div>

        <!-- 测试用例2：带分页的表格 -->
        <div class="row mb-5">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="mdi mdi-format-list-bulleted me-2"></i>测试用例2：带分页的表格
                        </h5>
                        <small class="text-muted">设置页面大小和分页显示</small>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <h6>代码示例：</h6>
                            <pre class="bg-light p-3 rounded"><code>&lt;w:d-table model="Weline\DataTable\Model\TestUser" 
           scope="test-user-pagination" 
           page-size="3" 
           show-pagination="true"&gt;
&lt;/w:d-table&gt;</code></pre>
                        </div>
                        
                        <!-- DataTable 组件 -->
                        <w:d-table model="Weline\DataTable\Model\TestUser" 
                                   scope="test-user-pagination" 
                                   page-size="3" 
                                   show-pagination="true">
                        </w:d-table>
                    </div>
                </div>
            </div>
        </div>

        <!-- 测试用例3：自定义表头 -->
        <div class="row mb-5">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="mdi mdi-view-column me-2"></i>测试用例3：自定义表头
                        </h5>
                        <small class="text-muted">手动配置显示字段和属性</small>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <h6>代码示例：</h6>
                            <pre class="bg-light p-3 rounded"><code>&lt;w:d-table model="Weline\DataTable\Model\TestUser" scope="test-user-custom"&gt;
    &lt;w:t-header&gt;
        &lt;w:field belong="t-header" name="id" sortable="true" width="80"&gt;用户ID&lt;/w:field&gt;
        &lt;w:field belong="t-header" name="name" sortable="true" width="150"&gt;用户名&lt;/w:field&gt;
        &lt;w:field belong="t-header" name="email" sortable="true" width="200"&gt;邮箱地址&lt;/w:field&gt;
        &lt;w:field belong="t-header" name="status" sortable="true" width="100"&gt;状态&lt;/w:field&gt;
    &lt;/w:t-header&gt;
&lt;/w:d-table&gt;</code></pre>
                        </div>
                        
                        <!-- DataTable 组件 -->
                        <w:d-table model="Weline\DataTable\Model\TestUser" scope="test-user-custom">
                            <w:t-header>
                                <w:field belong="t-header" name="id" sortable="true" width="80">用户ID</w:field>
                                <w:field belong="t-header" name="name" sortable="true" width="150">用户名</w:field>
                                <w:field belong="t-header" name="email" sortable="true" width="200">邮箱地址</w:field>
                                <w:field belong="t-header" name="status" sortable="true" width="100">状态</w:field>
                            </w:t-header>
                        </w:d-table>
                    </div>
                </div>
            </div>
        </div>

        <!-- 测试用例4：带筛选器的表格 -->
        <div class="row mb-5">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="mdi mdi-filter me-2"></i>测试用例4：带筛选器的表格
                        </h5>
                        <small class="text-muted">添加筛选功能</small>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <h6>代码示例：</h6>
                            <pre class="bg-light p-3 rounded"><code>&lt;w:d-table model="Weline\DataTable\Model\TestUser" scope="test-user-filter" searchable="true"&gt;
    &lt;w:t-filter&gt;
        &lt;w:field belong="t-filter" name="name" type="text" placeholder="搜索用户名"&gt;&lt;/w:field&gt;
        &lt;w:field belong="t-filter" name="email" type="email" placeholder="搜索邮箱"&gt;&lt;/w:field&gt;
        &lt;w:field belong="t-filter" name="status" type="select" options="1:启用,0:禁用"&gt;&lt;/w:field&gt;
    &lt;/w:t-filter&gt;
&lt;/w:d-table&gt;</code></pre>
                        </div>
                        
                        <!-- DataTable 组件 -->
                        <w:d-table model="Weline\DataTable\Model\TestUser" scope="test-user-filter" searchable="true">
                            <w:t-filter>
                                <w:field belong="t-filter" name="name" type="text" placeholder="搜索用户名"></w:field>
                                <w:field belong="t-filter" name="email" type="email" placeholder="搜索邮箱"></w:field>
                                <w:field belong="t-filter" name="status" type="select" options="1:启用,0:禁用"></w:field>
                            </w:t-filter>
                        </w:d-table>
                    </div>
                </div>
            </div>
        </div>

        <!-- 测试用例5：产品表格测试 -->
        <div class="row mb-5">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="mdi mdi-package-variant-closed me-2"></i>测试用例5：产品表格测试
                        </h5>
                        <small class="text-muted">测试不同模型的表格显示</small>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <h6>代码示例：</h6>
                            <pre class="bg-light p-3 rounded"><code>&lt;w:d-table model="Weline\DataTable\Model\TestProduct" 
           scope="test-product-basic" 
           page-size="5" 
           show-pagination="true" 
           searchable="true" 
           sortable="true"&gt;
&lt;/w:d-table&gt;</code></pre>
                        </div>
                        
                        <!-- DataTable 组件 -->
                        <w:d-table model="Weline\DataTable\Model\TestProduct" 
                                   scope="test-product-basic" 
                                   page-size="5" 
                                   show-pagination="true" 
                                   searchable="true" 
                                   sortable="true">
                        </w:d-table>
                    </div>
                </div>
            </div>
        </div>

        <!-- 测试结果 -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="mdi mdi-check-circle me-2"></i>测试结果
                        </h5>
                    </div>
                    <div class="card-body">
                        <div id="test-results">
                            <div class="alert alert-info">
                                <i class="mdi mdi-information me-2"></i>
                                请检查上述表格是否正常显示，功能是否正常工作。
                            </div>
                        </div>
                        
                        <div class="mt-3">
                            <button class="btn btn-success" data-datatable-basic-action="run-basic-tests">
                                <i class="mdi mdi-play me-1"></i>运行自动化测试
                            </button>
                            <button class="btn btn-info ms-2" data-datatable-basic-action="check-table-features">
                                <i class="mdi mdi-magnify me-1"></i>检查表格功能
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="@static(Weline_Frontend::assets/libs/bootstrap/js/bootstrap.bundle.min.js)"></script>
    <script>

        function escapeHtml(value) {
            return String(value ?? '').replace(/[&<>"']/g, function (character) {
                return {
                    '&': '&amp;',
                    '<': '&lt;',
                    '>': '&gt;',
                    '"': '&quot;',
                    "'": '&#039;'
                }[character];
            });
        }
        // 运行基础功能测试
        function runBasicTests() {
            const resultsDiv = document.getElementById('test-results');
            resultsDiv.innerHTML = '<div class="alert alert-info"><i class="mdi mdi-loading mdi-spin me-2"></i>正在运行测试...</div>';
            
            const tests = [
                { name: '表格渲染', test: () => document.querySelectorAll('.datatable-container').length > 0 },
                { name: '表头显示', test: () => document.querySelectorAll('thead th').length > 0 },
                { name: '数据行显示', test: () => document.querySelectorAll('tbody tr').length > 0 },
                { name: '分页组件', test: () => document.querySelectorAll('.pagination').length > 0 },
                { name: '筛选器', test: () => document.querySelectorAll('.datatable-filter').length > 0 }
            ];
            
            let results = [];
            let passedTests = 0;
            
            tests.forEach(test => {
                try {
                    const passed = test.test();
                    if (passed) {
                        passedTests++;
                        results.push(`<li class="text-success"><i class="mdi mdi-check me-2"></i>${escapeHtml(test.name)}: 通过</li>`);
                    } else {
                        results.push(`<li class="text-danger"><i class="mdi mdi-close me-2"></i>${escapeHtml(test.name)}: 失败</li>`);
                    }
                } catch (error) {
                    results.push(`<li class="text-warning"><i class="mdi mdi-alert me-2"></i>${escapeHtml(test.name)}: 错误 - ${escapeHtml(error.message)}</li>`);
                }
            });
            
            const successRate = Math.round((passedTests / tests.length) * 100);
            const alertClass = successRate >= 80 ? 'alert-success' : successRate >= 60 ? 'alert-warning' : 'alert-danger';
            
            resultsDiv.innerHTML = `
                <div class="${alertClass}">
                    <h6><i class="mdi mdi-chart-bar me-2"></i>测试完成 - 通过率: ${successRate}% (${passedTests}/${tests.length})</h6>
                    <ul class="mb-0">${results.join('')}</ul>
                </div>
            `;
        }
        
        // 检查表格功能
        function checkTableFeatures() {
            const resultsDiv = document.getElementById('test-results');
            
            const features = [
                '✓ 自动字段生成功能正常',
                '✓ 分页功能正常工作',
                '✓ 排序功能可以使用',
                '✓ 筛选功能可以使用',
                '✓ 表格响应式布局正常',
                '✓ 数据加载正常'
            ];
            
            resultsDiv.innerHTML = `
                <div class="alert alert-success">
                    <h6><i class="mdi mdi-format-list-checks me-2"></i>功能检查清单</h6>
                    <ul class="mb-0">
                        ${features.map(feature => `<li>${escapeHtml(feature)}</li>`).join('')}
                    </ul>
                    <hr>
                    <small class="text-muted">请手动验证上述功能是否正常工作</small>
                </div>
            `;
        }
        
        // 页面加载完成后自动运行测试
        document.addEventListener('click', function(event) {
            const trigger = event.target.closest('[data-datatable-basic-action]');
            if (!trigger) {
                return;
            }
            event.preventDefault();
            switch (trigger.getAttribute('data-datatable-basic-action')) {
                case 'run-basic-tests':
                    runBasicTests();
                    break;
                case 'check-table-features':
                    checkTableFeatures();
                    break;
            }
        });

        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(() => {
                runBasicTests();
            }, 2000);
        });
    </script>
</body>
</html>
