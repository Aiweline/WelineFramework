<?php
/**
 * 结账成功页面
 */
$order = $this->getData('order');
?>
<div class="checkout-success-page">
    <div class="success-message">
        <h1><?= __('结账成功') ?></h1>
        <p><?= __('感谢您的购买！您的订单已成功创建。') ?></p>
    </div>
    
    <?php if ($order): ?>
    <div class="order-info">
        <h2><?= __('订单信息') ?></h2>
        <div class="info-row">
            <span><?= __('订单号') ?>:</span>
            <strong><?= $this->escapeHtml($order->getOrderNumber()) ?></strong>
        </div>
        <div class="info-row">
            <span><?= __('订单状态') ?>:</span>
            <span><?= $order->getStatusText() ?></span>
        </div>
        <div class="info-row">
            <span><?= __('支付状态') ?>:</span>
            <span><?= $order->getPaymentStatusText() ?></span>
        </div>
        <div class="info-row">
            <span><?= __('订单总额') ?>:</span>
            <strong><?= $this->escapeHtml($order->getCurrency()) ?> <?= number_format($order->getTotalAmount(), 2) ?></strong>
        </div>
        <div class="info-row">
            <span><?= __('创建时间') ?>:</span>
            <span><?= $this->escapeHtml($order->getCreatedTime()) ?></span>
        </div>
    </div>
    
    <div class="order-actions">
        <a href="<?= $this->getUrl('weline_checkout/frontend/order/view', ['order_id' => $order->getId()]) ?>" class="btn btn-primary">
            <?= __('查看订单详情') ?>
        </a>
        <a href="<?= $this->getUrl('weline_checkout/frontend/order/list') ?>" class="btn btn-secondary">
            <?= __('返回订单列表') ?>
        </a>
    </div>
    <?php endif; ?>
</div>
