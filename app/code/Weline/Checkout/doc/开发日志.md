## 2026-09-09 — 结账运力列表与报价同源（1.4.49）

- `CheckoutQueryProvider::loadShippingMethods` 改走 `shippingInfo.listQuoteOptions`（传入 cart lines / fulfillment_metadata），不再裸 `getByLocation`。
- 空列表文案：「当前地址下所选配送方案不可用…」。
- 版本：`1.4.48` → `1.4.49`。

## 2026-09-08 — 订单摘要商品列表最大高度可滚动

- 审图分流：web_ui（结账侧栏「订单摘要」商品区红框）
- 修：`.weline-checkout__items` `max-height: min(40vh, 22.5rem)` + `overflow-y: auto`；标题/合计/提交不进滚动区
- 交付：`/checkout`

## 2026-09-08 — 进入结账前置可售阻断（配合 Cart 1.3.30）

- 变更：`getData` 透传 `checkout_blocked`/`blocking_message`；失效时禁用提交并渲染行问题；`CheckoutHtmlRenderer` 增加 `is-unavailable`/`item-issue`
- 配合：`Weline_Cart` `REQ-CART-0006`

## 2026-09-07 — 结账摘要行标题不再被价区挤爆（1.4.46）

- 问题：Order summary 三列 grid / 与标题抢宽的价区，把标题挤成逐词换行，SKU/规格也竖排，右侧看起来乱。
- 变更：商品行两列（图 | 主栏）；价区在主栏内 `float:right`，标题绕排；现价+划线 `nowrap` 同行，活动标签下一行。
- 版本：`1.4.45` → `1.4.46`。
- 交付地址：`/checkout`。

## 2026-09-07 — 结账行价 Summary 头布局（1.4.45）

- 变更：`CheckoutHtmlRenderer` 现价+划线包入 `item-price-row`，活动标签单独下一行右对齐；CSS `column` + `nowrap`。
- 版本：`1.4.44` → `1.4.45`。
- 交付地址：`/checkout`。

## 2026-09-07 — 结账成功页 CTA 对齐 ink（1.4.44）

- 变更：`checkout/success` 主钮 `--amz-btn-primary-*` 改吃 `--color-primary`；契约断言同步。
- 版本：`1.4.43` → `1.4.44`

## Hotfix — 2026-09-07：结账行划线价与活动标签（1.4.43）

- 问题：结账摘要商品行仅显示成交价，缺少 PDP 划线原价与活动名。
- 变更：`CheckoutHtmlRenderer` / `CheckoutPageViewModel` 消费 Cart 透出的 `compare_at_minor` + `campaign_*`；行价横排现价+划线+活动（原型变体 A）。
- 版本：`1.4.42` → `1.4.43`。
- 交付地址：`/checkout`。

## Hotfix — 2026-09-05：地址保存后勿硬刷抹掉地址卡（1.4.42）

- 现象：编辑地址保存后触发 `address-updated` → `scheduleReload` 默认硬刷，收起态地址卡空白。
- 变更：`weline:checkout:address-updated` 改为 `scheduleReload({ hardOnFailure: false })`，只软刷配送方式/摘要。
- 配合 Shipping `2.4.7` 地址卡重绘修复。
- 版本：`1.4.41` → `1.4.42`。
- 交付地址：`/checkout`。

## Hotfix — 2026-09-05：结账英文界面相关词条补齐（1.4.41）

- 问题：英文结账页仍出现中文提示（优惠、快速加址、配送/验证码状态文案等）。
- 变更：`Checkout/i18n/en_US.csv` 补齐结账前台相关英文；协同 Shipping/Captcha/Cart 同批对齐。
- 版本：`1.4.40` → `1.4.41`。
- 交付地址：`/en_US/checkout`。

## Hotfix — 2026-09-05：收货信息标题间距对齐配送方式（1.4.40）

- 现象：`收货信息` 与部件内 `收货地址` 几乎贴在一起；配送方式标题下有 16px 再出选项卡。
- 变更：`.weline-checkout__shipping-address-slot { margin-top: 16px }`，与 `.weline-checkout__options` 对齐。
- TDD：`StorefrontCheckoutTemplateContractTest` slot 间距契约。
- 版本：`1.4.39` → `1.4.40`。
- 交付地址：`/checkout`。

## Hotfix — 2026-09-05：迷你购物车改车后刷新结账摘要（1.4.39）

- 现象：结账页改迷你购物车后，若软刷新失败会静默保留旧摘要，数据不匹配。
- 变更：`weline:cart-updated` 改走 `scheduleReload({ hardOnFailure: true })`；失败则 `location.reload()`。
- TDD：`StorefrontCheckoutTemplateContractTest::testCheckoutReloadsSummaryWhenMiniCartMutatesCart`。
- 版本：`1.4.38` → `1.4.39`。
- 交付地址：`/checkout`。

## Hotfix — 2026-09-05：规格色块与文字同高并可放大（1.4.38）

- 现象：有色图的规格芯片明显高于纯文字芯片。
- 变更：色块缩至 16px 对齐文字行高；点击预置 dialog 放大预览（无 createElement）。
- TDD：`CheckoutHtmlRendererTest` 16px + trigger + dialog 契约。
- 版本：`1.4.37` → `1.4.38`。
- 交付地址：`/checkout`。

## Hotfix — 2026-09-05：规格色块贴规格值（1.4.37）

- 现象：色块显示在规格类型（如「角色」）左侧，语义不清晰。
- 变更：DOM 改为「标签 · [色块] 值」，色块紧邻规格值。
- TDD：`CheckoutHtmlRendererTest` 顺序契约。
- 版本：`1.4.36` → `1.4.37`。
- 交付地址：`/checkout`。

## Hotfix — 2026-09-05：结账规格行芯片化布局（1.4.36）

- 现象：订单摘要规格区竖排像列表项，色块与文案割裂。
- 变更：规格改为横向 wrap 芯片（色块/色图 + 标签·值）；CSS 紧凑灰底卡片。
- TDD：`CheckoutHtmlRendererTest` 芯片 DOM 契约。
- 版本：`1.4.35` → `1.4.36`。
- 交付地址：`/checkout`。

## Hotfix — 2026-09-05：结账摘要商品行展示缩略图（1.4.34）

- 现象：订单摘要仅有商品名/SKU/规格，左侧无产品图。
- 修复：`CheckoutHtmlRenderer::renderItems` 输出 `image`/`image_src` 缩略图（无图走 `StorefrontImagePlaceholder`）；CSS 左图右文；首屏与异步 `items_html` 同源。
- TDD：`CheckoutHtmlRendererTest` 缩略图 XSS 与占位。
- 版本：`1.4.33` → `1.4.34`。
- 交付地址：`/checkout`。

## Hotfix — 2026-09-05：结账规格行展示图/色块（1.4.35）

- 变更：`CheckoutHtmlRenderer` 规格行渲染 `swatch_image`/`swatch_color` + 文字，与购物车/迷你购物车一致。
- 版本：`1.4.34` → `1.4.35`。
- 交付地址：`/checkout`。

## Hotfix — 2026-09-05：结账商品行展示已选规格（1.4.33）

- 现象：结账摘要仅显示商品名与数量，看不出颜色/尺码等规格。
- 修复：`CheckoutHtmlRenderer::renderItems` 输出 SKU 与 `options` 可读规格（XSS 转义）；样式与购物车对齐。
- TDD：`CheckoutHtmlRendererTest::testRenderItemsEscapesAndFormats`。
- 版本：`1.4.32` → `1.4.33`。
- 交付地址：`/checkout`。

## Hotfix — 2026-09-04：主题编辑器 buy-now 预览跳过目录 resolve（1.4.32）

- 现象：商品布局预览无 PDP identity 时 `product-buy-now` 经 `StorefrontOfferResolver` 可能触发慢路径，单 widget 可达数十秒。
- 修复：预览且无预置 offer / card_product_id 时跳过 resolve，渲染禁用按钮占位。
- 版本：`1.4.31` → `1.4.32`。

## Hotfix — 2026-09-04：顶栏配送地址列表超 5 条可滚动

- 现象：`checkout-delivery-context` 弹层已保存地址过多时整块拉长，挤压页面。
- 变更：`.address-list` 以约 5 条地址卡高度为 `max-height`，`overflow-y: auto` 垂直滚动；≤5 条时不出现多余滚动条。
- 自动验证：`CheckoutDeliveryQuickAddHookContractTest::testAddressListScrollsWhenExceedingFiveItems`。
- 版本：`1.4.29` → `1.4.30`。
- 交付地址：前台任意页顶栏「配送至」弹层。

## Hotfix — 2026-09-04：访客购物车从购物车页进入结账页丢失

- 当前状态：`implemented_validated`
- 现象：同一浏览器 `/cart` 显示 1 件商品与 CNY 48.00，进入 `/checkout` 后却显示「购物车是空的」，返回 `/cart` 商品仍在。
- 根因：存在两层断点：ViewModel 未在嵌套 Cart Query 前恢复可信 HttpOnly Cookie；异步 `checkout.getData` 又未先加载共享 Cart 会话。原 `issueGuestToken` 还会无条件轮换令牌，使前端无法安全接管已有购物车。
- 修复：ViewModel 在 Query 边界前解析并显式传递可信 Cookie；结账页先加载 Cart、重新读取会话，仅在缺失时调用已经改为「复用 Cookie 优先」的 `issueGuestToken`，再把令牌传给 `checkout.getData`。不记录令牌，也不允许浏览器选择客户、网站作用域、商品或金额。
- 自动验证：Checkout 19 tests / 228 assertions；Cart Query 与前台契约 15 tests / 118 assertions，全部通过。
- 浏览器验收：WLS 四 Worker 优雅热重载后，在新的 Codex 内置浏览器标签页中 `/cart` 显示 1 件、CNY 48.00；普通「去结算」进入 `/checkout` 后仍为同一商品 x1、商品小计与应付合计均为 CNY 48.00。
- 版本：Checkout `1.4.30` → `1.4.31`；Cart `1.3.4` → `1.3.5`。
- 交付地址：`/cart`、`/checkout`

## Hotfix — 2026-09-04：访客结账地址 id 被空字符串覆盖

- 现象：匿名 `saveDeliveryAddress` 成功但地址卡不出现；`selected.id` 为空。
- 根因：`saveGuestAddress` 用 `$normalized + ['id'=>$id]`，左侧空 `id` 盖掉生成的 `guest_*`；`guestBook()` 再丢弃空 id。
- 修复：`array_merge` 强制写入生成 id；Shipping 结账部件对匿名始终提供「使用此地址」+ 懒加载验证码。
- 版本：Checkout `1.4.27` → `1.4.28`，Shipping `2.2.12` → `2.2.13`。
- 交付地址：`/checkout`

## Hotfix — 2026-09-03：结账收货字段多字段同时报错

- 现象：缺收货人/电话/地址时，合并文案出现在邮编下方，未分别提示。
- 变更：后端一次返回全部 `field_errors`；各输入框下方独立红字；全局提示移到编辑器顶部；提交订单前走 `validateShippingFields`。
- 版本：`1.4.26` → `1.4.27`。
- 交付地址：`/checkout`

## Hotfix — 2026-09-03：saveDeliveryAddress 字段级校验响应

- 变更：捕获 `AddressValidationException`，返回 `success:false` + `field_errors`（如 `phone`），避免 DEV 下 QueryBin 泄漏 `*.php:行号`。
- 版本：`1.4.25` → `1.4.26`。
- 交付地址：`/checkout`

## Hotfix — 2026-09-03：结账页头引导文案去代码痕迹

- 现象：结账页副标题默认写「Weline 核心模块承接」，向顾客暴露内部实现。
- 变更：`checkout_page_subtitle` 可配置；默认改为「确认收货地址、配送方式和支付信息后即可提交订单。」；留空则不渲染副标题段落。
- 版本：`1.4.24` → `1.4.25`。
- 交付地址：`/checkout`

## Hotfix — 2026-09-04：访客结账地址 id 被空字符串覆盖

- 现象：匿名 `saveDeliveryAddress` 成功但地址卡不出现；`selected.id` 为空。
- 根因：`saveGuestAddress` 用 `$normalized + ['id'=>$id]`，左侧空 `id` 盖掉生成的 `guest_*`；`guestBook()` 再丢弃空 id。
- 修复：`array_merge` 强制写入生成 id；Shipping 结账部件对匿名始终提供「使用此地址」+ 懒加载验证码。
- 版本：`1.4.24` → `1.4.25`。

## Hotfix — 2026-09-03：结账地址保存登录免验证码

- 变更：`saveDeliveryAddress` 仅对 guest checkout 校验验证码；`toFormAddress` 透传 district。
- 版本：`1.4.23` → `1.4.24`。

## Hotfix — 2026-09-03：结账应付合计扣优惠 + 优惠/留言 Tab

- 现象：摘要已显示 DEMO10（-¥9.60），应付合计仍按小计+运费未扣优惠；优惠券与留言纵向堆叠。
- 变更：`CheckoutPageViewModel` / `getData` 透传 `discount_preview`；`renderTotals` 扣减优惠并展示优惠行；监听 `weline:cart-updated` 刷新；优惠券/留言包入 `mini-cart-drawer__extras` + `miniCartExtras` Tab。
- 版本：`1.4.22` → `1.4.23`（Marketing `1.1.8` → `1.1.9`，Theme `2.2.119` → `2.2.120`）。
- 交付地址：`/checkout`

## Hotfix — 2026-09-03：结账摘要订单留言槽

- 变更：`checkout-summary-note` 槽 + `checkout-storefront-slots` 目录声明；由 Order `order-notice` 默认注入。
- 版本：`1.4.21` → `1.4.22`
- 交付地址：`/checkout`

## Hotfix — 2026-09-03：快捷支付移入左栏

- 现象：快捷支付在 notice 下方整页通栏，视觉过宽。
- 变更：`checkout-express-payment` 槽移入 `.weline-checkout__main` 顶部（收货信息上方），随两栏网格落在左侧。
- 版本：`1.4.20` → `1.4.21`。
- 交付地址：`/checkout`

## Feature — 2026-09-03：结账快捷支付空槽 + freezeQuote 账单地址参数

- 需求：结账 notice 与收货信息之间预留快捷支付 slot；万能支付默认注入 PayPal；`freezeQuote` 前端允许 `billing_address`。
- 变更：`checkout-express-payment` 槽 + 目录声明；结账页 `weline:checkout:express-pay`；Query `freezeQuote` 参数补齐；Payment 部件 `enabled` 开关。
- 版本：`1.4.19` → `1.4.20`（Payment `1.9.31` → `1.9.32`）。
- 验收：契约测试通过；结账页可见快捷支付；快捷支付创建 PayPal sandbox approve URL（`sandbox.paypal.com/checkoutnow?token=…`）。
- 交付地址：`https://p05113ef3.test.weline.com:9555/checkout`


- 需求：结账 notice 与收货信息之间预留快捷支付 slot；由万能支付部件默认注入 PayPal。
- 变更：`checkout-express-payment` 槽 + `checkout-storefront-slots` 目录声明；结账页监听 `weline:checkout:express-pay` 复用 freeze/submit。
- 版本：`1.4.18` → `1.4.19`（Payment `1.9.31` → `1.9.32`）。
- 交付地址：`/checkout`

# Weline_Checkout 开发日志

## Feature — 2026-09-03：结账支付方式 logo / 可展开简介 / 供应商指南详情

- 需求：万能支付结账列表展示供应商 logo、可展开简介，详情跳转各供应商 `/guide/payment/{method}` 导航。
- 变更：`PaymentQueryProvider` payload 透出 `icon_url`/`guide_url`；`CheckoutQueryProvider` 归一化保留；`CheckoutHtmlRenderer::renderPaymentMethodOptions` 生成富展示 HTML；结账页 CSS + 展开交互。
- 版本：`1.4.16` → `1.4.17`（Payment `1.9.30` → `1.9.31`）。
- 交付地址：`/checkout`

## Note — 2026-09-02：成功页 guest-convert 空 HTML toast（Customer 修复）

- 现象：`/checkout/success` DEV toast「checkout-success-guest-convert: 部件 HTML 为空」。
- 根因与修复在 `Weline_Customer`：`already_bound` 等跳过路径改为 `hidden` 空态壳；本模块槽位不变。
- 记录：`MCP_TARGET_UNAVAILABLE` 后对本文件原生补记。

## Hotfix — 2026-09-02：结账成功页版心对齐主题 Token

- 现象：`/checkout/success` 的 `.amz-order-confirm` 使用私有 `max-width: 980px` 与 `1040px` fallback，与 Header/结账页 `--weline-layout-content-*` 版心不一致。
- 修复：按 Theme Shell B 改为 `width: min(100%, var(--weline-layout-content-max-width))` + `padding-inline` Token；契约测试锁定。
- 交付地址：`http://p05113ef3.test.weline.com:9555/checkout/success`

## Feature — 2026-09-02：成功页释放访客转化空槽

- 在 Amazon 风格 CTA 上方增加 `<w:slot id="checkout-success-guest-account">`；不内嵌客户业务。
- 转化由 `Weline_Customer` 应用部件 `default_injections` 完成。
- 版本：`1.4.14` → `1.4.15`。

## Hotfix — 2026-09-02：成功页 checkout_token 30 分钟过期误跳登录

- 现象：带 `checkout_token` 的 `/checkout/success` 在支付数小时后 302→`/customer/account/index#orders`→登录。
- 根因：`OrmCheckoutSessionStore` 默认 TTL 1800s；`submitted` 后未拉长；过期 `get()` 删行，`canAccess` 失败。
- 修复：`SUBMITTED` 使用 7 天成功页能力 TTL；支付发起时续期；访客 ACL 失败改跳 `/cart`。
- 版本：`1.4.13` → `1.4.14`。

## Hotfix — 2026-09-02：结账成功页订单信息 section 身份

- 当前状态：`验证中`
- 根因：legacy `$order` 分支的 `amz-order-confirm__panel order-info` 是全仓唯一缺少 `weline-code` 的前台 section，Theme 编辑器无法稳定定位。
- 修复：补充 `weline-code="checkout.success.order_info"`；DOM、样式、订单和支付逻辑不变。
- 验收：待重跑 `frontend:check-section-code`；无新增路由。
- 交付地址：https://p05113ef3.test.weline.com:9555/checkout/success

## Hotfix — 2026-09-02：结账成功页对齐 Payment Amazon 确认风格

- 现象：`/checkout/success` 仍为通用大标题卡片，与 Payment `/payment/success` 亚马逊确认页不一致。
- 修复：成功/取消横幅、订单信息卡、商品列表、黄 CTA 对齐 `amz-order-confirm`。
- 版本：`1.4.12` → `1.4.13`。

## Hotfix — 2026-09-02：PayPal 回跳落地 URL 带 checkout_token

- 现象：PayPal 支付成功后 `/checkout/success?order_uuid=…` 302 到登录页。
- 根因：游客成功页依赖 `checkout_token` 能力；`browser_landing_url` 未注入。
- 修复：`payCreatedOrders` 传入 `quote_token`；`CheckoutOrderPaymentService` 写入落地 query。
- 版本：`1.4.11` → `1.4.12`。

## Hotfix — 2026-09-02：PayPal payload.redirect_url 回传结账

- 现象：`submitV2`/`pay` 后 `redirect_url=null`，虽已创建 PayPal approve URL。
- 根因：`CheckoutOrderPaymentService::extractRedirectUrl` 未递归 `payload`。
- 修复：嵌套键增加 `payload`；单测覆盖 PayPal 形响应。
- 版本：`1.4.10` → `1.4.11`。

## Hotfix — 2026-09-02：结账页 guest_token 与 guest_session 对齐

- 现象：加购成功后结账页仍报购物车为空。
- 修复：`guestToken()` 优先 `WelineCart.getGuestSession()`。
- 版本：`1.4.9` → `1.4.10`。

## Hotfix — 2026-09-02：frontend worker 补 external=true

- 现象：与 Cart 同源，结账 Query 未进入 `external_areas.frontend`。
- 修复：`CheckoutQueryProvider` 全部 frontend 操作补 `external=true`。
- 版本：`1.4.8` → `1.4.9`。

## 2026-09-02 — 取消态文案：已取消 / 已取消成功

- 当前状态：`implemented`
- 变更：`/checkout/success?outcome=cancel&cancel_state=` 双文案。
- 版本：`1.4.7` → `1.4.8`。



## 2026-09-02 — 取消回跳共用结账成功页

- 当前状态：`implemented`
- 变更：`/checkout/success?outcome=cancel` 展示「支付已取消」；抑制 `checkout_success` 像素；与支付成功同壳落地。
- 版本：`1.4.6` → `1.4.7`。
- 交付地址：`/checkout/success?outcome=cancel`

## 2026-09-02 — 结账成功页硬切 /checkout/success

- 当前状态：`implemented`
- 变更：唯一成功页 `/checkout/success`（`Controller/Success`）；删除 `SuccessPage` 与 `Frontend/Checkout::successPage`；无旧路径兼容跳转。
- 版本：`1.4.5` → `1.4.6`。
- 交付地址：`/checkout/success`

## Hotfix — 2026-09-03：结账账单地址默认同收货 + formBillingAddress

- 当前状态：`implemented`
- 变更：结账页 `formBillingAddress`；`freezeQuote` 透传 `billing_address`；`CheckoutGroupSubmitService` session/options 记录账单地址。
- 协作：收货交互由 Shipping `checkout-shipping-address` 部件承担。
- 版本：`1.4.18` → `1.4.19`。

## Hotfix — 2026-09-03：结账收货信息槽空白（模块槽目录发现）

- 当前状态：`implemented`
- 问题：`/checkout`「收货信息」与优惠券槽为空；`checkout-shipping-address` / `checkout-summary-discount` 仅存在于 Checkout 模块页模板，Theme `findSlot` 不可见，required `default_injections` 被 `layout_slot_missing` 拦截，账本无注入决策。
- 修复：新增 `checkout-storefront-slots` 容器槽目录部件（对齐 Product `product-info` 嵌套槽），声明上述模块页槽供 `ThemeComponentCatalog::findSlot` 发现；回填 required 默认注入并发布 checkout 布局。
- 契约：`CheckoutStorefrontSlotsCatalogContractTest`。
- 版本：`1.4.17` → `1.4.18`。

## 2026-08-30 — 立即结账按钮改 data-weline-load=cart

- 当前状态：`implemented`
- 变更：`product-buy-now` / `product-card-buy-now` 挂 `data-weline-load="cart"`，删除 `@static` 直引；复用 Cart `product-purchase-actions` 模块。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。

## 2026-08-28 — 结账收货地址改为槽位 + Shipping 地址部件

- 当前状态：`implemented`
- 问题：结账页「收货信息」裸拼国家/省/市/详细地址 input，未使用地址选择标签，也未走部件默认注入。
- 实现：`checkout-shipping-address` 槽；由 `Weline_Shipping` 默认注入 `checkout-shipping-address` 部件（`<w:theme:address>` + 已存地址选择）；`formAddress`/`applyDefaultShippingAddress` 兼容级联字段并调用 `WelineThemeAddress.applyValues`。
- 验收：契约测试断言槽位与无裸 region input；Browser 结账页可见地址级联与账户地址选择。

## 2026-08-27 — 结账优惠券部件槽 + Amazon 默认样式

- 当前状态：`implemented`
- 问题：优惠券 Hook 直出模板且落在 `.weline-checkout__totals div` flex 行内，标签/输入/按钮被压成一行；应用按钮使用站点品红主色。
- 实现：`checkout-summary-discount` 槽 + `<w:widget type="marketing" name="checkout-coupon" />`；`checkout-coupon` 部件默认 Amazon 输入+灰底应用布局；移除 Marketing `summary-discount-before` Hook 直出。
- 验收：运费与应付合计之间全宽优惠券行；应用按钮灰底深字；契约测试通过。

## 2026-08-27 — 结账页色板对齐 Amazon（保留 Shopify 布局）

- 当前状态：`implemented`
- 问题：`/checkout` 两栏布局已是 Shopify 风格，但链接/主按钮仍落在蓝链 `#2563eb` 回退色，与站点 Amazon chrome 不一致。
- 实现：`view/frontend/checkout/index.phtml` 在 `.weline-checkout` 作用域引入 `--checkout-*` Amazon 色板；主 CTA 固定 `#ffd814` 深色字；契约测试断言不再允许 `#2563eb`。
- 文档：`checkout-ui.md` 补充「布局 Shopify / 色板 Amazon」。
- 验收：`/checkout` 计算样式链接色 `#007185`，提交按钮黄底深字；860px 断点布局不变。

## 2026-08-26 — Header 配送 quick-add 验证码改为懒加载

- 当前状态：`implemented`
- 问题：首页 TTFB 数秒；慢日志里 `checkout-delivery-context` / `com_quick-add` 常占 0.4–0.7s。误判为 WLS memory，实测 sidecar `cache_get` 亚毫秒。
- 根因：header 默认注入的 quick-add 表单 `captcha="required"` 在**每个页面 SSR** 调用 `LocalImageCaptcha::render()`；其中 `password_hash(PASSWORD_DEFAULT)` 在本机约 400ms（bcrypt cost≈12）。
- 修复：`captcha="lazy"` + checkout `getDeliveryCaptchaChallenge` 打开面板再拉挑战；`LocalImageCaptcha` 改为 bcrypt cost=5。
- 验收：首页 HTML 无验证码 PNG；打开配送面板后再出现；TTFB/timing 中 quick-add 不再含 captcha SSR。


> 按目标版本记录真实进度、调整与验收证据；2026-08-17 前的历史不补造。

## 2026-08-25 — Header quick-add address 泄漏拖垮分类页（归属 Theme）

- 当前状态：`documented`（修复在 `Weline_Theme\Taglib\Address::runtimeCallback`）
- 现象：店面分类页主内容空白；Header 配送 quick-add 泄漏 Taglib PHP。
- 说明：Checkout widget 仅委托 Shipping hook；根因是 Theme `theme:address` 缺运行期回调。店面路径不变。

## 2026-08-25 — 主题编辑器预览降载配送上下文

- 当前状态：`implemented`
- 问题：主题编辑器 iframe 渲染 header 时 `checkout-delivery-context` 全量 `getContext()` + quick-add/captcha，拖慢 `theme-preview/content`。
- 实现：`editor_mode=1` 时使用轻量 stub 上下文，跳过 quick-add Hook 与自动定位按钮；店面路径不变。
- 验收：`CheckoutDeliveryQuickAddHookContractTest::testEditorModeLightPathSkipsHeavyContextAndQuickAddHook`。

## 2026-08-25 — 配送上下文改为 default_injections

- 当前状态：`implemented`
- 问题：Theme header 内嵌 Checkout `checkout-delivery-context`，违反「仅 Theme 部件可布局内嵌」。
- 实现：部件注册补 `default_injections`（`layout_type:*` → `slot:delivery`）；header 仅留空槽 + Hook；契约测试断言无内嵌。
- 验收：`CheckoutDeliveryQuickAddHookContractTest`。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.4.4` | verification_in_progress | `REQ-CHECKOUT-0001..0003` | 2026-08-17 | 2026-08-17 | Browser pending |

## `1.4.4` — 商城支付三态与恢复闭环

- 基线版本：`1.4.3`
- 目标版本：`1.4.4`
- 当前状态：`verification_in_progress`
- 需求 ID：`REQ-CHECKOUT-0001`、`REQ-CHECKOUT-0002`、`REQ-CHECKOUT-0003`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：Checkout 支付编排、Query、默认模板、成功页授权及测试；不接 PayPal，不改 Provider 内部协议。
- 负责人/确认人：主执行者直接实现（未委派）/用户

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-checkout-0002-订单创建后的支付三态与恢复闭环` | 2026-08-17 |
| 实现执行者/模型/范围已记录 | pass | 本条“负责人/确认人”；主执行者直接执行 | 2026-08-17 |
| 架构/缺陷/安全复审通过 | pass | 支付三态、同订单恢复、游客 capability、Cart 发布接口边界均有聚焦回归 | 2026-08-17 |
| 分层测试通过 | pass | 站点 Cart/Checkout/Order：168 tests / 986 assertions；框架 peer：150 / 900 | 2026-08-17 |
| 真实路径 WebUI/E2E 通过 | pending | 待 9514 WLS Browser 支付闭环 | 2026-08-17 |
| 可重复 E2E 固化并通过 | partial | paid 主路径已有历史 Browser 证据；三态/重复提交/授权由聚焦测试覆盖，刷新后 Browser 待复验 | 2026-08-17 |
| 功能/运营文档完成 | pass | `checkout-v2.md`、`checkout-ui.md` 已更新 | 2026-08-17 |
| 用户/产品验收 | pending | 待用户确认 | 2026-08-17 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-17 | requirements | planned → requirements_confirmed | 订单提交先于支付，二值 UI 会丢失恢复语义 | 采用 paid/pending/failed 三态和同订单恢复 | 用户 | 当前任务“继续”确认 |
| 2026-08-17 | implementation | requirements_confirmed → implemented | 增加支付编排、恢复状态、`resumePaymentV2`、游客 capability 与前端恢复 UI | 订单创建结果与支付结果分层，失败不再诱发重复订单 | 用户 | `CheckoutOrderPaymentService`、`CheckoutPaymentRecoveryStateService`、QueryProvider、模板/JS |
| 2026-08-17 | verification | implemented → verification_in_progress | 双仓聚焦回归、升级、路由与编译通过，9514 WLS 就绪 | 等待绑定 Weline Chrome 后做真实 UI 支付复验 | 主执行者 | 168 / 986；150 / 900；`framework:compile` 102/103/0 |

### 实现范围

- 变更路径/符号：`CheckoutOrderPaymentService`、`CheckoutPaymentRecoveryStateService`、`CheckoutSessionAccessService`、`QueryProvider::submitV2/resumePaymentV2`、成功页控制器/模板、结账模板/JS、i18n 与测试。
- 数据/配置/迁移：无 Model Schema；Query descriptor/API 字段向后兼容增量。
- 兼容与回退：保留 `payment.paid`；关闭新恢复 UI 可回退到订单列表但不得回退为重复下单。

### 审查与整改

- 架构：pass；Checkout 通过 Cart/Order/Payment 发布边界编排，控制器保持薄层。
- 缺陷：pass；paid/pending/failed 均保留 group/order，失败恢复使用同一已提交 Session。
- 安全：pass；游客成功页要求 capability，Provider 响应被净化，跳转仅允许 http/https。
- 用户批准延期：none。

### 测试与验收

- 聚焦/单元/集成：pass，站点 168 tests / 986 assertions；框架 peer 150 / 900。
- 真实 WLS + WebUI：pending。
- E2E 固化：partial；核心状态机已固化，刷新后的真实 Browser 主路径待运行。
- 未验证项：9514 下真实加车→fake_card→成功页→账户订单及失败恢复 UI。

### 文档与运营交付

- 需求文档：`REQ-CHECKOUT-0001..0003`。
- 功能/API/架构文档：`doc/checkout-v2.md`、`doc/checkout-ui.md` 已更新。
- 运营文档：N/A（暂定）；若最终出现独立人工恢复入口则需补运营文档。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。 待生成器刷新。

### 版本结论

- 结论：verification_in_progress（实现与确定性门禁通过，尚未 accepted）。
- 完成/发布证据：未提交、未推送、未部署。
- 剩余事项与恢复条件：完成 9514 Weline Chrome 支付/恢复 Browser 验收与用户验收。
