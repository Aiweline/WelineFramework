# 汇审：结账收货地址 soft fallback 三清

- 日期：2026-09-21
- 模块：Weline_Checkout 1.5.9 / Weline_Shipping 2.9.17
- 结论：**通过**

## 根因

旧 soft fallback（槽内 `fetch` 直渲）与 required `default_injections` 叠渲；部件 `data-testid` 曾为 `shipping-checkout-address`，存在性检测失败后 overlay `$html . $inner` 再注一份。

## 三清结果

| 项 | 结果 |
|----|------|
| 删除 index/express-review soft fallback | 已删 |
| 槽 `exclusive="true"` | 已加；HTML `data-wslot-exclusive="true"` |
| marker 对齐 widget code | `data-testid` / `data-widget-code` = `checkout-shipping-address` |
| 负向契约禁回潮 | CheckoutTaxIdentity + CheckoutShippingAddressSlot |
| 未引用 `checkout-shipping-address.v20*.js` | 已删；e2e 改指现行 `.js` |
| HelpPay 懒加载 / `data-shipping-checkout-address` DOM 钩子 | 保留（非 soft fallback） |

## 证据

- UT：CheckoutTaxIdentity / CheckoutShippingAddressSlot / Shipping Widget+QuoteSelected → **8/8 PASS**
- curl/Browser：`sections=1`、`headings=2`（收货+账单）、`roots=1`、`oldTestid=0`、`exclusive=true`
- 验收 URL：`https://p05113ef3.test.weline.com:9555/checkout`（禁缓存）

## 席位签收

- Team:架构师 / 扩展点：单路径 required injection，旧链路清零
- Team:测试：契约 + DOM 计数
- Team:前端 / UI / 原型：Receiving information 下 Delivery Address / Billing 各一份
