<?php
/**
 * @var \Weline\Framework\View\Template $this
 * @var \Weline\Geo\Model\Platform $platform
 * @var array $supported_platforms
 */
$isEdit = $platform->getId() > 0;
?>
<div class="content-header">
    <h1><?= $isEdit ? __('编辑平台') : __('添加平台') ?></h1>
</div>

<div class="content-body">
    <w:form id="platform-form" class="form-horizontal">
        <input type="hidden" name="id" value="<?= $platform->getId() ?>">
        
        <div class="form-group">
            <label class="control-label"><?= __('平台代码') ?> <span class="required">*</span></label>
            <select name="platform_code" class="form-control" required>
                <option value=""><?= __('请选择') ?></option>
                <?php foreach ($supported_platforms as $code): ?>
                    <option value="<?= $code ?>" <?= $platform->getData('platform_code') === $code ? 'selected' : '' ?>>
                        <?= htmlspecialchars($code) ?>
                    </option>
                <?php endforeach; ?>
            </select>
        </div>

        <div class="form-group">
            <label class="control-label"><?= __('平台名称') ?> <span class="required">*</span></label>
            <input type="text" name="platform_name" class="form-control" 
                   value="<?= htmlspecialchars($platform->getData('platform_name') ?? '') ?>" required>
        </div>

        <div class="form-group">
            <label class="control-label"><?= __('API端点') ?></label>
            <input type="text" name="api_endpoint" class="form-control" 
                   value="<?= htmlspecialchars($platform->getData('api_endpoint') ?? '') ?>" 
                   placeholder="https://api.example.com/endpoint">
        </div>

        <div class="form-group">
            <label class="control-label"><?= __('Feed格式') ?></label>
            <select name="feed_format" class="form-control">
                <option value="json_feed" <?= ($platform->getData('feed_format') ?? 'json_feed') === 'json_feed' ? 'selected' : '' ?>>
                    JSON Feed
                </option>
                <option value="xml" <?= ($platform->getData('feed_format') ?? '') === 'xml' ? 'selected' : '' ?>>
                    XML
                </option>
                <option value="rss" <?= ($platform->getData('feed_format') ?? '') === 'rss' ? 'selected' : '' ?>>
                    RSS
                </option>
            </select>
        </div>

        <div class="form-group">
            <label class="control-label"><?= __('是否启用') ?></label>
            <div class="checkbox">
                <label>
                    <input type="checkbox" name="is_enabled" value="1" 
                           <?= $platform->getData('is_enabled') ? 'checked' : '' ?>>
                    <?= __('启用') ?>
                </label>
            </div>
        </div>

        <div class="form-group">
            <label class="control-label"><?= __('配置（JSON）') ?></label>
            <textarea name="config" class="form-control" rows="5"><?= htmlspecialchars($platform->getData('config') ?? '{}') ?></textarea>
        </div>

        <div class="form-group">
            <button type="submit" class="btn btn-primary">
                <i class="mdi mdi-content-save"></i> <?= __('保存') ?>
            </button>
            <a href="<?= $this->getUrl('geo/backend/platform') ?>" class="btn btn-secondary">
                <?= __('取消') ?>
            </a>
        </div>
    </w:form>
</div>

<!-- SweetAlert2 CSS -->
<link href='@static(Weline_Admin::assets/libs/sweetalert2/sweetalert2.min.css)' rel='stylesheet' type='text/css'/>
<!-- SweetAlert2 JS -->

<script>
(function(g){
  if(g.__bqAdmin_geo) return;
  g.__bqAdmin_geo=true;
  g.bqAdmin=g.bqAdmin||{};
  g.bqAdmin['geo']=function(url, options){
    options=options||{};
    var body=options.body;
    if(body && typeof FormData!=='undefined' && body instanceof FormData){
      var p=new URLSearchParams(); body.forEach(function(v,k){ if(!(typeof File!=='undefined'&&v instanceof File)) p.append(k,String(v)); }); body=p.toString();
    } else if(body && typeof body!=='string'){ try{ body=JSON.stringify(body); }catch(e){ body=''; } }
    var run=function(api){ return api.resource('geo').adminRequest({url:url, method:options.method||'POST', headers:options.headers||{}, body:body||''}); };
    var toResp=function(data){
      var _biz=g.WelineApiBusiness||(g.Weline&&g.Weline.ApiBusiness);
      if(_biz&&typeof _biz.wrapAdminBridgeResult==='function'){
        return _biz.wrapAdminBridgeResult(data);
      }
      var body=(data&&typeof data==='object'&&!Array.isArray(data))?data:{success:true,data:data};
      var ok=!(body&&body.success===false);
      var resp={ok:ok,status:ok?200:400,json:function(){return Promise.resolve(body);},text:function(){return Promise.resolve(typeof body==='string'?body:JSON.stringify(body==null?{}:body));}};
      Object.keys(body).forEach(function(k){ if(k==='ok'||k==='json'||k==='text'||k==='status') return; resp[k]=body[k]; });
      return resp;
    };
    var p=(g.Weline&&g.Weline.load)?g.Weline.load('api').then(run):Promise.resolve(run(g.Weline.Api));
    return p.then(toResp);
  };
})(window);
</script>
<script src='@static(Weline_Admin::assets/libs/sweetalert2/sweetalert2.min.js)'></script>
<script>
document.getElementById('platform-form').addEventListener('submit', function(e) {
    e.preventDefault();
    
    const formData = new FormData(this);
    
    var url = '<?= $this->getUrl('geo/backend/platform/save') ?>';
    var opts = { method: 'POST', body: formData };
    window.Weline.Api.request(url, opts)
    .then(function(res) {
        var data = (res && res.data) !== undefined ? res.data : res;
        if (data.success) {
            if (typeof Swal !== 'undefined') {
                Swal.fire({
                    icon: 'success',
                    title: '<?= __('成功') ?>',
                    text: '<?= __('保存成功') ?>',
                    timer: 2000,
                    showConfirmButton: false
                }).then(() => {
                    window.location.href = '<?= $this->getUrl('geo/backend/platform') ?>';
                });
            } else {
                console.log('<?= __('保存成功') ?>');
                window.location.href = '<?= $this->getUrl('geo/backend/platform') ?>';
            }
        } else {
            if (typeof Swal !== 'undefined') {
                Swal.fire({
                    icon: 'error',
                    title: '<?= __('保存失败') ?>',
                    text: data.message || '<?= __('保存失败') ?>',
                    confirmButtonText: '<?= __('确定') ?>'
                });
            } else {
                console.error(data.message || '<?= __('保存失败') ?>');
            }
        }
    })
    .catch(error => {
        console.error('Error:', error);
        if (typeof Swal !== 'undefined') {
            Swal.fire({
                icon: 'error',
                title: '<?= __('保存失败') ?>',
                text: '<?= __('请重试') ?>',
                confirmButtonText: '<?= __('确定') ?>'
            });
        } else {
            console.error('<?= __('保存失败') ?>');
        }
    });
});
</script>
