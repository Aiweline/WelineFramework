<?php
/**
 * 编辑账户 - 内容模板
 * @var \Weline\Framework\View\Template $this
 */

use Weline\Geo\Model\Platform;
use Weline\Geo\Model\PlatformAccount;
use Weline\Framework\Manager\ObjectManager;

$platform = $this->getData('platform') ?? null;
$account = $this->getData('account') ?? ObjectManager::getInstance(PlatformAccount::class);
$backendBase = $this->getBackendUrl('geo/backend/platform');
?>
<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
            <h4 style="--w-mb-sm:0;"><?= $account->getId() ? __('编辑账户') : __('添加账户') ?></h4>
            <div class="w-cluster">
                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                    <li class="w-breadcrumb__item"><a href="<?= $this->getBackendUrl() ?>"><?= __('首页') ?></a></li>
                    <li class="w-breadcrumb__item"><a href="<?= $backendBase ?>"><?= __('平台管理') ?></a></li>
                    <li class="w-breadcrumb__item" data-state="active"><?= __('编辑账户') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="w-grid">
    <template>Weline_Component::message.phtml</template>
</div>

<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-card">
            <div class="w-card__header">
                <h5 class="w-card__title" style="--w-mb:0;"><?= $account->getId() ? __('编辑账户') : __('添加账户') ?></h5>
            </div>
            <div class="w-card__body">
                <w:form method="post" action="@var($backendBase)/accountSave">
                    <input type="hidden" name="id" value="<?= $account->getId() ?>">
                    <input type="hidden" name="platform_id" value="<?= $platform ? $platform->getId() : 0 ?>">
                    
                    <div style="--w-mb:var(--weline-space-4);">
                        <label class="w-field__label"><?= __('账户名称') ?> <span class="w-text" data-tone="danger">*</span></label>
                        <input type="text" class="w-input" name="account_name" value="<?= htmlspecialchars($account->getData(PlatformAccount::fields_ACCOUNT_NAME)) ?>" required>
                    </div>
                    
                    <div style="--w-mb:var(--weline-space-4);">
                        <label class="w-field__label"><?= __('API密钥') ?></label>
                        <input type="password" class="w-input" name="api_key" placeholder="<?= $account->getId() ? __('留空则不修改') : __('请输入API密钥') ?>">
                        <input type="hidden" name="api_key_encrypted" value="1">
                        <small class="w-field__hint w-text" data-tone="muted"><?= __('API密钥将被加密存储') ?></small>
                    </div>
                    
                    <div style="--w-mb:var(--weline-space-4);">
                        <label class="w-field__label"><?= __('API密钥（Secret）') ?></label>
                        <input type="password" class="w-input" name="api_secret" placeholder="<?= $account->getId() ? __('留空则不修改') : __('如果需要，请输入API Secret') ?>">
                        <input type="hidden" name="api_secret_encrypted" value="1">
                    </div>
                    
                    <div style="--w-mb:var(--weline-space-4);">
                        <label class="w-field__label"><?= __('是否默认账户') ?></label>
                        <div class="w-switch">
                            <input type="checkbox" name="is_default" value="1" <?= $account->isDefault() ? 'checked' : '' ?>>
                            <label><?= __('设为默认') ?></label>
                        </div>
                    </div>
                    
                    <div style="--w-mb:var(--weline-space-4);">
                        <label class="w-field__label"><?= __('是否激活') ?></label>
                        <div class="w-switch">
                            <input type="checkbox" name="is_active" value="1" <?= $account->isActive() ? 'checked' : '' ?>>
                            <label><?= __('激活') ?></label>
                        </div>
                    </div>
                    
                    <div style="--w-mb:var(--weline-space-4);">
                        <button type="submit" class="w-button" data-tone="primary"><?= __('保存') ?></button>
                        <a href="<?= $backendBase ?>/account?platform_id=<?= $platform ? $platform->getId() : 0 ?>" class="w-button" data-tone="neutral"><?= __('返回') ?></a>
                    </div>
                </w:form>
            </div>
        </div>
    </div>
</div>
