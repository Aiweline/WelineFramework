# Weline_PlatformAppStore 开发日志

## 2026-09-10 — 审图：去凌乱（标题重复 / 页脚悬空）

- **分流**：web_ui / ui_shot（用户「不觉得很凌乱么」+ 红箭头）。
- **线稿问题**：布局 `w-backend-page__heading` 已有 h1，内容再写 h1「模块管理」；短空态导致 footer 贴在半屏。
- **已修**：内容区去掉重复 h1；空态 `min-height: calc(100dvh - 14rem)` 把页脚压底；有数据时仅保留「共 N 个」+ 操作。
- **验证**：Browser 禁缓存 DOM：页面仅 1 个 h1。

## 2026-09-10 — 设计审图：空态优先

- **分流**：web_ui / ui_shot（用户「设计」+附图）。
- **线稿**：无数据时仅标题+居中 empty 单主 CTA；有数据/有筛选时再出现紧凑筛选条。
- **原型**：空态优先；弃双主 CTA + 空态挂筛选卡。
- **已修**：去筛选卡与浮动标签叠字；空态唯一 primary；筛选无数据时隐藏。
- **验证**：`ModuleAdminServiceTest`；Browser 禁缓存。

## 2026-09-10 — 审图：模块管理空态与筛选

- **分流**：web_ui / ui_shot（用户附图后台模块管理空态）。
- **线稿**：顶栏标题+主 CTA → 标注筛选条 → w-empty（图标/说明/主 CTA）；去掉无下一步的大白卡。
- **原型**：B（toolbar + labeled filter + empty CTA）；弃 A 旧空卡、C 统计墙。
- **已修**：E 空态无下一步、筛选无标签；F 大空白卡；G 改用 `w-toolbar`/`w-field`/`w-empty`。
- **验证**：`ModuleAdminServiceTest`；Browser 禁缓存打开模块管理页。

## 2026-09-10 — 修复后台 Backend 路由 404

- **范围**：补齐 `Controller/Backend/{Module,Category,Order,License,Developer,Earnings,ApiConfig}` 与对应 `view/templates/Backend/*/index.phtml`；新增 `Service/ModuleAdminService` 与单测；原型 `prototype/module-listing.html`。
- **原因**：`etc/backend/menu.xml` 已声明 `*/backend/module` 等 action，但仅有 `Controller/Api`，菜单点击 404。
- **验证**：`phpunit:run --module Weline_PlatformAppStore --filter ModuleAdminServiceTest`；curl/Browser 探活 `.../platform-appstore/backend/module`。
