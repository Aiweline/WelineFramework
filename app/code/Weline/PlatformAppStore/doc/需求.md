# Weline_PlatformAppStore 需求

## 当前需求

- 后台菜单 `*/backend/module` 等 action 必须有对应 `Controller/Backend`，打开不再 404。
- 模块管理页展示平台模块列表（可空态），并提供状态/定价筛选；空态须给出「新建模块」下一步。
- 同菜单其余 Backend 入口（分类/订单/许可证/开发者/收益/API配置）同步可解析。
- 支持新建模块草稿（Vendor_Module 名 + 显示名）。

## 待确认

- 模块版本上传、发布审核与付费定价完整工作流。
