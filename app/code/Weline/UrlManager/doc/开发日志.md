## 2026-09-24 — 分块重写只返回本批 legacy 路径（1.0.10）

- 批次的指纹与NULL旧记录OR分支均绑定本批原始path集合，避免每256项重复传输全网站legacy行；指纹→NULL→空值及各层最新ID语义保持。
- 257目标legacy行+1无关行：原首批258行，现256/1，仍2SQL；单批最多771绑定参数。定向UrlManager Unit 27 tests / 171 assertions PASS。
- Model变更按门禁1.0.9→1.0.10；本机PG127.0.0.1:5432，`php bin/w setup:upgrade -m Weline_UrlManager` exit0，operation `setup-20260924062855-98dfbfa6722a6bf202ccb288c6104c85`。既有升级流程执行43语种404静态发布及自动通知1个WLS实例重载，未人工额外重载。最终全变更冻结后的编译与页面验收另行执行。

## 2026-09-24 — 重写缓存遵守共享池期限，单条冷查合批

- 删除观察者无 TTL 的私有进程缓存；跨请求仅使用框架 `url_rewrite` 池（300 秒），同请求继续复用正、负查询结果。`clearProcessCache()` 保留公开兼容入口。
- 保存/删除触发的既有 `invalidateCaches()` 同时移除当前请求的重写结果与共享池。其他并发请求仍保留各自请求内快照，不承诺跨 Fiber 即时一致。
- 单条冷 URL 将主路径和语言回退路径交给 `findLatestByWebsiteAndPaths()` 一次查询；主路径优先、空 rewrite 命中、网站与原始路径语义不变。
- 列表应调用 `Url::getFrontendUrls($paths)`，该入口保留输入键、预取重写并逐项派发既有事件。批次内部去重并由 Model 按 256 路径分块；消费者不得直接查询重写表或缓存最终公共 URL。
- 仅 Observer 与测试变更，不涉及 Model/Controller/注册面，按模块升级门禁不 bump、不执行 setup:upgrade。源码加载与运行验收由正式编译/重载完成。
- 定向验证：`php vendor/bin/phpunit --bootstrap app/bootstrap_phpunit.php --do-not-cache-result app/code/Weline/UrlManager/Test/Unit`，26 tests / 168 assertions；该结果不替代真实页面验收。

## 2026-09-20 — SEO rewrite 观察者自守 seo 开关（1.0.9）

- Framework 始终派发 `url_generate_rewrite`；本观察者 `!Env::get('seo')` 立即 return，避免 seo=off 仍做 path 重写。
- 版本：`1.0.8` → `1.0.9`。

## 2026-09-18 — SEO encode 进程 L1 + rewrite change 失效（1.0.8）

- `SeoUrlGenerateRewrite` 进程缓存；`UrlRewrite` save/delete 后 `invalidateCaches`（清进程 + `url_rewrite` 池）；挂 `process_cache_resetter`。
- 版本：`1.0.7` → `1.0.8`。

## 2026-09-14 — 跳过失效 PageBuilder 壳层重写（1.0.7）

- `RouterRewrite`：非 PageBuilder 站点上，指向 `pagebuilder/frontend/page/*` 且命中 Theme 壳层别名（或 Blog 拥有 `/blog`）的 pretty rewrite 不再解码劫持，避免 `/privacy` 等落到缺失 page_id 的 404。
- 单测：`RouterRewriteThemeShellSkipTest`。

## 2026-09-08 — 51 分类逐项重写查询合并（1.0.6）

由 1.0.5 增加模型批量入口和既有缓存预取：主/回退路径合并为一次候选查询，保持原始路径、范围、旧指纹和空重写语义；没有给 Model 添加缓存。新 Model/Observer/Framework 行为用例先 RED 后实现，联合精确路径组 26 tests /174 assertions 通过。测试显式加载 app/code 的 UrlManager 源码，避免 Composer 中旧 vendor 副本误测。真实数据库、官方编译与两实例 HTTP 结果见 Framework 性能文档；单元结果不替代运行验收。

<!-- weline:module-development-baseline:generated-once -->
# Weline_UrlManager 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.3`（文档基线） | docs_handoff | `REQ-URLMANAGER-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.0.5` — 重写查询前拒绝非法 UTF-8

- 基线版本：`1.0.4`；目标版本：`1.0.5`。
- 问题：非法 UTF-8 rewrite 路径作为 PDO 绑定参数会触发 PostgreSQL `SQLSTATE[22021]`，表现为店面 Runtime Error 500。
- 变更：`RouterRewrite` 在 `findRewriteByWebsiteAndRewrite` 前若 `Url::isValidUtf8Text($lookupUri)` 失败则直接返回，不访问数据库。
- 联防：依赖 Framework `Url::parser` 与 Server `WorkerPolicyKernel` 的入口 400；本层为 DB 前最后一道。

## `1.0.4` — URL 路径候选一次查询


- 基线版本：`1.0.3`；目标版本：`1.0.4`。
- 范围：将路径未命中时的三次 SQL 往返合并为一次，沿用既有网站与指纹索引；无新增缓存和表结构。
- 兼容：保持指纹、NULL、空字符串优先级，每层按最新 ID 选择，继续严格比较原始路径；非空错误指纹不进入兼容回退。
- 回归：旧方法 4 个定向用例均因查询数失败，候选 4 个用例、35 项断言通过；使用实际 PostgreSQL 编译器和 SQLite 内存执行，不写业务数据。
- 部署：执行 `php bin/w setup:upgrade -m Weline_UrlManager` 并重载网站 worker；固定商品数据与真实 HTTP 的最终观测见 `/private/tmp/weline-perf-rewrite/acceptance-report.md`。不以局部 SQL 优化宣称整页冷请求已解决。

## `1.0.3` — 文档制度启用基线

- 基线版本：`1.0.3`
- 目标版本：`1.0.3`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-URLMANAGER-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-URLMANAGER-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-URLMANAGER-0001` / `1.0.3` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
