<?php
/**@var \Weline\Framework\View\Template $this */
$currentQ = $this->getData('current_q') ?? '';
?>
<!-- start page title -->
<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
            <h4 style="--w-mb-sm:0;"><?= __('URL 管理') ?></h4>
            <div class="w-cluster">
                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                    <li class="w-breadcrumb__item"><span><?= __('系统配置') ?></span></li>
                    <li class="w-breadcrumb__item"><span><?= __('URL 管理器') ?></span></li>
                    <li class="w-breadcrumb__item" data-state="active"><?= __('URL 管理') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->
<div class="w-grid">
    <template>Weline_Component::message.phtml</template>
</div>
<div class="w-grid">
    <div style="--w-span-lg:12;">
        <div class="w-card">
            <div class="w-card__body">
                <h4 class="w-card__title" style="--w-mb:var(--weline-space-2);"><?= __('路由列表') ?></h4>
                <p class="w-text" data-tone="muted" style="--w-mb:var(--weline-space-6);">
                    <?= __('管理系统中可访问的 URL，可为关键页面设置更友好的自定义地址，提升 SEO 和可读性。') ?>
                </p>
                <w:form method="get" action="@backend-url('url-manager/backend/url/listing')" class="url-listing-search" style="--w-mb:var(--weline-space-6);">
                    <div class="w-field__control" style="max-width: 360px;">
                        <input type="text" class="w-input" name="q" value="<?= htmlspecialchars((string)($currentQ ?? '')) ?>" placeholder="<?= __('按路径搜索…') ?>">
                        <button type="submit" class="w-button" data-tone="primary">
                            <w:icon name="search" size="sm"></w:icon> <?= __('搜索') ?>
                        </button>
                    </div>
                </w:form>
                <div class="w-table-wrap">
                    <table class="w-table" style="--w-mb:0;">
                        <thead>
                        <tr>
                            <th style="width: 60px;">#</th>
                            <th><?= __('访问路径') ?></th>
                            <th><?= __('路由标识') ?></th>
                            <th><?= __('所属模块') ?></th>
                            <th><?= __('类型') ?></th>
                            <th><?= __('创建时间') ?></th>
                            <th><?= __('更新时间') ?></th>
                            <th style="width: 100px;"><?= __('操作') ?></th>
                        </tr>
                        </thead>
                        <tbody>
                        <foreach name="urls">
                            <tr>
                                <td>{{v.url_id}}</td>
                                <td><code style="font-size: 0.9em;" data-w-text-wrap="break">{{v.path}}</code></td>
                                <td><span class="w-text" data-tone="muted" data-size="sm">{{v.identify}}</span></td>
                                <td>{{v.module_name}}</td>
                                <td>
                                    <if condition="v.type == 'frontend'">
                                        <span class="w-badge" data-w-background="info"><?= __('前台') ?></span>
                                    <elseif condition="v.type == 'rewrite'"/>
                                        <span class="w-badge" data-w-background="success"><?= __('自定义') ?></span>
                                    <else/>
                                        <span class="w-badge" data-w-background="muted">{{v.type}}</span>
                                    </if>
                                </td>
                                <td>{{v.create_time}}</td>
                                <td>{{v.update_time}}</td>
                                <td>
                                    <if condition="v.can_rewrite">
                                        <a href="@backend-url('url-manager/backend/rewriter/form')?identify={{v.identify}}" class="w-button" data-tone="primary" data-variant="outline" data-size="sm"><?= __('重写') ?></a>
                                    <else/>
                                        <span class="w-text" data-tone="muted" data-size="sm">—</span>
                                    </if>
                                </td>
                            </tr>
                        </foreach>
                        </tbody>
                    </table>
                    <if condition="!has_urls">
                        <div class="w-text" data-align="center" data-tone="muted" style="--w-pt:var(--weline-space-12);--w-pb:var(--weline-space-12);">
                            <w:icon name="list" size="sm"></w:icon>
                            <p style="--w-mt:var(--weline-space-2);--w-mb:0;"><?= __('暂无路由数据') ?></p>
                            <p class="w-text" data-size="sm"><?= __('请检查相关模块是否正确注册') ?></p>
                        </div>
                    </if>
                    {{pagination}}
                </div>
            </div>
        </div>
    </div>
</div>
