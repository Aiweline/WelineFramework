# Weline_RecentlyViewed 开发日志

## 2026-09-06 — 最近浏览改声明式 Taglib（1.0.6）

- 变更：循环由 `ProductCardRenderer::render` 改为声明式 `<w:product:card density="shelf" class="wrv-card" …>`；去掉未用 Renderer import；货架 CSS 仍只管轨道/导航。
- 验证：`RecentlyViewedWidgetContractTest` 断言 Taglib + shelf。

## 2026-09-06 — 统一商品卡片 Taglib（1.0.5）

- 变更：最近浏览对齐 Product 统一卡片（当时循环仍经 Renderer）；货架 CSS 只负责轨道/导航尺寸。
- 验证：与 Product `ProductCardContractTest` / 店面最近浏览区块。

## 2026-09-05 — 副标题英中混用修复

- 当前状态：`implemented`
- 问题：标题走 `WidgetI18n`（路径语言）为 Recently viewed，副标题 `__()` 可能仍解析为中文「基于你最近的浏览」。
- 修复：副标题改为 `WidgetI18n::label('Based on your recent browsing')`；`WidgetI18n` preferredModules 增加 `Weline_RecentlyViewed`；同步 en/zh CSV。
- 验证：店面 Browser 验收英文副标题。

## 2026-09-04 — 主题编辑器预览跳过全量 publishedOffers（1.0.4）

- 当前状态：`implemented`
- 问题：预览 Cookie 若有 MRU id，`cards()` → `publishedOffersForProductIds` 仍冷刷全目录。
- 修复：预览用 `ThemeDemoCatalog`；店面 `cards()` 改为按 id 调 `livePublishedOffersForProduct`。
- 版本：`1.0.3` → `1.0.4`。

## 2026-09-04 — 轮播疏密度

- 当前状态：`implemented`
- 变更：桌面卡宽 `12.25rem`→`15rem`、轨道间距加大、左右 `2.75rem` 导航安全区；标题/价格垂直留白与长 SKU 断行；CSS `20260904-amz2`。
- 验证：店面 Browser 验收 Recently viewed 同屏约 4–5 卡。

## 2026-09-04 — 亚马逊货架风美化

- 当前状态：`implemented`
- 变更：白底货架、去历史图标、叠层圆形箭头、Amazon 蓝链标题、整/小数价格层级；CSS `20260904-amz1`；版本 `1.0.2` → `1.0.3`。
- 验证：`RecentlyViewedWidgetContractTest`；店面 Browser 验收最近浏览区块。

## 2026-09-04 — 默认展示数量改为 24

- 当前状态：`implemented`
- 变更：部件/`default_injections`/Service 默认 `limit` 由 6 改为 24，与 Cookie `MAX_ITEMS` 对齐；版本 `1.0.1` → `1.0.2`。
- 验证：`RecentlyViewedWidgetContractTest`；店面确认溢出时可左右滑。

## 2026-09-03 — 最近浏览部件美化

- 当前状态：`implemented`
- 变更：对齐 PDP `related-products` 的舞台/视口/横向轨道；少货时卡片定宽不拉满六列空白；侧向导航 + `data-weline-load=recentlyViewed`；空态补副标题与虚线底。
- 验证：`RecentlyViewedWidgetContractTest`；店面 Browser 验收最近浏览区块。

## 2026-09-02 — 新建模块 + slot 默认注入

- 当前状态：`implemented`
- 变更：新增 `Weline_RecentlyViewed`（Cookie MRU、`ProductViewedObserver`、`recently-viewed` 部件 `default_injections`→`product-recently-viewed`）；`Weline_Product` Detail 派发 `Weline_Product::product_viewed`；Theme 产品布局去掉内嵌 `recently-viewed`，Seeder 归属切到本模块并移除 Theme 部件注册。
- 验证：`RecentlyViewedServiceTest`、`RecentlyViewedWidgetContractTest`；Theme `RecentlyViewedEmptyShellContractTest` 仅保留 Slot 清理契约。
