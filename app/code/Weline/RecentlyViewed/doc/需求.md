# Weline_RecentlyViewed 需求

## 当前需求

### `REQ-RV-0001` 独立模块承载最近浏览

- 来源与确认：2026-09-02 用户确认新建模块，并用 slot / `default_injections` 注入。
- 业务逻辑：`Weline_RecentlyViewed` 拥有 Cookie MRU 记录与 `recently-viewed` 部件；监听 `Weline_Product::product_viewed`；`default_injections` required → `product` 布局 `product-recently-viewed`。
- 非目标：不在 Theme 内嵌业务部件；不做跨设备登录合并持久化（待确认）。
- 验收标准：部件注册含 `default_injections`→`product-recently-viewed`；Theme 产品布局槽仅留 Hook；PDP 浏览后 Cookie 可见记录。

## 待确认

- 登录用户是否需要独立 DB 持久化并与游客 Cookie 合并。
