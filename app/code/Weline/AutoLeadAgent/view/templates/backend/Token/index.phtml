<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */
$themeConfig = \Weline\Framework\Manager\ObjectManager::getInstance(\Weline\Backend\Block\ThemeConfig::class);

use Weline\AutoLeadAgent\Model\AgentToken;

$tokens = $this->getData('tokens') ?? [];
$total = $this->getData('total') ?? 0;
$page = $this->getData('page') ?? 1;
$limit = $this->getData('limit') ?? 20;
$totalPages = $this->getData('total_pages') ?? 1;
?>
<!DOCTYPE html>
<html lang="zh-CN" <?= ('rtl' === $themeConfig->getThemeModel()) ? ' dir="rtl"' : '' ?>>
<head>
    @template(Weline_Admin::common/head.phtml)
    <title><?= __('Token管理') ?></title>
</head>
<body <?= $themeConfig->getLayouts() ?? ''; ?>>
@template(Weline_Admin::common/page/loading.phtml)
<div id="layout-wrapper">
    <w:block class="Weline\Admin\Block\Backend\Page\Topbar"/>
    <w:block class="Weline\Admin\Block\Backend\Page\Topnav"/>
    @template(Weline_Admin::common/left-sidebar.phtml)

    <div class="main-content">
        <div class="page-content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                            <h4 class="mb-sm-0"><?= __('Token管理') ?></h4>
                            <div class="page-title-right">
                                <ol class="breadcrumb m-0">
                                    <li class="breadcrumb-item">
                                        <a href="@backend-url('admin/index/index')"><?= __('系统') ?></a>
                                    </li>
                                    <li class="breadcrumb-item active"><?= __('Token管理') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-body">
                                <?php if (empty($tokens)): ?>
                                    <div class="alert alert-info mb-0">
                                        <?= __('暂无Token记录') ?>
                                    </div>
                                <?php else: ?>
                                    <div class="table-responsive">
                                        <table class="table table-bordered table-hover align-middle">
                                            <thead class="table-light">
                                                <tr>
                                                    <th><?= __('Token ID') ?></th>
                                                    <th><?= __('授权域名') ?></th>
                                                    <th><?= __('过期时间') ?></th>
                                                    <th><?= __('WASM哈希') ?></th>
                                                    <th><?= __('创建时间') ?></th>
                                                    <th><?= __('操作') ?></th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <?php foreach ($tokens as $token): ?>
                                                    <tr>
                                                        <td><?= $token->getId() ?></td>
                                                        <td><?= htmlspecialchars($token->getData(AgentToken::fields_DOMAIN)) ?></td>
                                                        <td>
                                                            <?php 
                                                            $expiresAt = $token->getData(AgentToken::fields_EXPIRES_AT);
                                                            $isExpired = strtotime($expiresAt) < time();
                                                            ?>
                                                            <span class="badge bg-<?= $isExpired ? 'danger' : 'success' ?>">
                                                                <?= $expiresAt ?>
                                                            </span>
                                                        </td>
                                                        <td>
                                                            <code class="small"><?= htmlspecialchars(substr($token->getData(AgentToken::fields_WASM_HASH), 0, 16)) ?>...</code>
                                                        </td>
                                                        <td><?= $token->getData(AgentToken::fields_CREATED_AT) ?></td>
                                                        <td>
                                                            <a href="<?= $this->getBackendUrl('auto-lead-agent/backend/token/view/tokenId/' . $token->getId()) ?>" 
                                                               class="btn btn-sm btn-outline-primary">
                                                                <?= __('查看') ?>
                                                            </a>
                                                        </td>
                                                    </tr>
                                                <?php endforeach; ?>
                                            </tbody>
                                        </table>
                                    </div>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        @template(Weline_Admin::common/footer.phtml)
    </div>
</div>

@template(Weline_Admin::common/vendor-scripts.phtml)
</body>
</html>

