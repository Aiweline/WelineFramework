<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */
$themeConfig = \Weline\Framework\Manager\ObjectManager::getInstance(\Weline\Backend\Block\ThemeConfig::class);

use Weline\AutoLeadAgent\Model\WasmHash;

$wasmFiles = $this->getData('wasm_files') ?? [];
$paginationHtml = $this->getData('pagination_html') ?? '';
?>
<!DOCTYPE html>
<html lang="zh-CN" <?= ('rtl' === $themeConfig->getThemeModel()) ? ' dir="rtl"' : '' ?>>
<head>
    @template(Weline_Admin::common/head.phtml)
    <title><?= __('WASM管理') ?></title>
</head>
<body <?= $themeConfig->getLayouts() ?? ''; ?>>
@template(Weline_Admin::common/page/loading.phtml)
<div id="layout-wrapper">
    <w:block class="Weline\Admin\Block\Backend\Page\Topbar"/>
    <w:block class="Weline\Admin\Block\Backend\Page\Topnav"/>
    @template(Weline_Admin::common/left-sidebar.phtml)

    <div class="main-content">
        <div class="page-content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                            <h4 class="mb-sm-0"><?= __('WASM管理') ?></h4>
                            <div class="page-title-right">
                                <ol class="breadcrumb m-0">
                                    <li class="breadcrumb-item">
                                        <a href="@backend-url('admin/index/index')"><?= __('系统') ?></a>
                                    </li>
                                    <li class="breadcrumb-item active"><?= __('WASM管理') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-body">
                                <?php if (empty($wasmFiles)): ?>
                                    <div class="alert alert-info mb-0">
                                        <?= __('暂无WASM文件记录') ?>
                                    </div>
                                <?php else: ?>
                                    <div class="table-responsive">
                                        <table class="table table-bordered table-hover align-middle">
                                            <thead class="table-light">
                                                <tr>
                                                    <th><?= __('哈希ID') ?></th>
                                                    <th><?= __('文件哈希') ?></th>
                                                    <th><?= __('文件路径') ?></th>
                                                    <th><?= __('版本') ?></th>
                                                    <th><?= __('创建时间') ?></th>
                                                    <th><?= __('操作') ?></th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <?php foreach ($wasmFiles as $wasm): ?>
                                                    <tr>
                                                        <td><?= $wasm->getId() ?></td>
                                                        <td>
                                                            <code class="small"><?= htmlspecialchars(substr($wasm->getData(WasmHash::fields_HASH_VALUE), 0, 16)) ?>...</code>
                                                        </td>
                                                        <td><?= htmlspecialchars($wasm->getData(WasmHash::fields_WASM_PATH)) ?></td>
                                                        <td><?= htmlspecialchars($wasm->getData(WasmHash::fields_VERSION) ?? '1.0.0') ?></td>
                                                        <td><?= $wasm->getData(WasmHash::fields_CREATED_AT) ?></td>
                                                        <td>
                                                            <a href="<?= $this->getBackendUrl('auto-lead-agent/backend/wasm/view', ['hashId' => $wasm->getId()]) ?>" 
                                                               class="btn btn-sm btn-outline-primary">
                                                                <?= __('查看') ?>
                                                            </a>
                                                        </td>
                                                    </tr>
                                                <?php endforeach; ?>
                                            </tbody>
                                        </table>
                                    </div>
                                    
                                    <?php if ($paginationHtml): ?>
                                        <div class="mt-3">
                                            <?= $paginationHtml ?>
                                        </div>
                                    <?php endif; ?>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        @template(Weline_Admin::common/footer.phtml)
    </div>
</div>

@template(Weline_Admin::common/vendor-scripts.phtml)
</body>
</html>

