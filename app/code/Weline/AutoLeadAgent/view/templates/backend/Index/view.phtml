<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */


$task = $this->getData('task');
$candidates = $this->getData('candidates') ?? [];
$candidate_pagination = $this->getData('candidate_pagination') ?? '';
?>
<div class="row">
                    <div class="col-12">
                        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                            <h4 class="mb-sm-0"><?= __('任务详情') ?> #<?= $task->getId() ?></h4>
                            <div class="page-title-right">
                                <ol class="breadcrumb m-0">
                                    <li class="breadcrumb-item">
                                        <a href="@backend-url('admin/index/index')"><?= __('系统') ?></a>
                                    </li>
                                    <li class="breadcrumb-item">
                                        <a href="@backend-url('auto-lead-agent/backend/index/index')"><?= __('自动寻客') ?></a>
                                    </li>
                                    <li class="breadcrumb-item active"><?= __('任务详情') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

            <div class="row">
                <!-- 任务信息卡片 -->
                <div class="col-lg-4">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="card-title mb-0">
                                <i class="mdi mdi-information-outline me-1"></i>
                                <?= __('任务信息') ?>
                            </h5>
                        </div>
                        <div class="card-body">
                            <table class="table table-borderless mb-0">
                                <tbody>
                                    <tr>
                                        <th class="text-muted" style="width: 120px;"><?= __('任务ID') ?></th>
                                        <td><?= $task->getId() ?></td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted"><?= __('来源类型') ?></th>
                                        <td>
                                            <?php 
                                            $sourceType = $task->getData(\Weline\AutoLeadAgent\Model\SearchTask::fields_SOURCE_TYPE);
                                            $sourceId = $task->getData(\Weline\AutoLeadAgent\Model\SearchTask::fields_SOURCE_ID);
                                            if ($sourceType && $sourceId) {
                                                echo htmlspecialchars($sourceType) . ' (' . htmlspecialchars($sourceId) . ')';
                                            } else {
                                                $storeId = $task->getData(\Weline\AutoLeadAgent\Model\SearchTask::fields_STORE_ID);
                                                echo __('店铺ID') . ': ' . ($storeId ?: '-');
                                            }
                                            ?>
                                        </td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted"><?= __('找到数量') ?></th>
                                        <td>
                                            <span class="badge bg-info">
                                                <?= $task->getData(\Weline\AutoLeadAgent\Model\SearchTask::fields_FOUND_COUNT) ?: 0 ?>
                                            </span>
                                        </td>
                                    </tr>
                                    <?php 
                                    $selectedSearchEngines = $this->getData('selected_search_engines') ?? [];
                                    $selectedTargetWebsites = $this->getData('selected_target_websites') ?? [];
                                    ?>
                                    <tr>
                                        <th class="text-muted"><?= __('搜索引擎') ?></th>
                                        <td>
                                            <?php if (!empty($selectedSearchEngines)): ?>
                                                <div class="d-flex flex-wrap gap-1">
                                                    <?php foreach ($selectedSearchEngines as $engine): ?>
                                                        <span class="badge bg-primary"><?= htmlspecialchars($engine) ?></span>
                                                    <?php endforeach; ?>
                                                </div>
                                            <?php else: ?>
                                                <span class="text-muted"><?= __('未选择') ?></span>
                                            <?php endif; ?>
                                        </td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted"><?= __('目标网站') ?></th>
                                        <td>
                                            <?php if (!empty($selectedTargetWebsites)): ?>
                                                <div class="d-flex flex-wrap gap-1">
                                                    <?php foreach ($selectedTargetWebsites as $website): ?>
                                                        <span class="badge bg-success"><?= htmlspecialchars($website) ?></span>
                                                    <?php endforeach; ?>
                                                </div>
                                            <?php else: ?>
                                                <span class="text-muted"><?= __('未选择') ?></span>
                                            <?php endif; ?>
                                        </td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted"><?= __('状态') ?></th>
                                        <td>
                                            <?php
                                            $status = $task->getData(\Weline\AutoLeadAgent\Model\SearchTask::fields_STATUS);
                                            $badgeClass = 'secondary';
                                            if ($status === 'running') {
                                                $badgeClass = 'primary';
                                            } elseif ($status === 'completed') {
                                                $badgeClass = 'success';
                                            } elseif ($status === 'failed') {
                                                $badgeClass = 'danger';
                                            } elseif ($status === 'pending') {
                                                $badgeClass = 'warning';
                                            }
                                            ?>
                                            <span class="badge bg-<?= $badgeClass ?>"><?= __($status) ?></span>
                                        </td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted"><?= __('进度') ?></th>
                                        <td>
                                            <div class="progress" style="height: 20px;">
                                                <?php $progress = $task->getData(\Weline\AutoLeadAgent\Model\SearchTask::fields_PROGRESS); ?>
                                                <div class="progress-bar" role="progressbar" 
                                                     style="width: <?= $progress ?>%"
                                                     aria-valuenow="<?= $progress ?>" 
                                                     aria-valuemin="0" 
                                                     aria-valuemax="100">
                                                    <?= $progress ?>%
                                                </div>
                                            </div>
                                        </td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted"><?= __('创建时间') ?></th>
                                        <td><?= $task->getData(\Weline\AutoLeadAgent\Model\SearchTask::fields_CREATED_AT) ?></td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted"><?= __('更新时间') ?></th>
                                        <td><?= $task->getData(\Weline\AutoLeadAgent\Model\SearchTask::fields_UPDATED_AT) ?></td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <div class="card-footer">
                            <a href="<?= $this->getBackendUrl('auto-lead-agent/backend/index/index') ?>" class="btn btn-outline-secondary">
                                <i class="mdi mdi-arrow-left"></i> <?= __('返回列表') ?>
                            </a>
                        </div>
                    </div>
                </div>

                <!-- 潜在客户列表 -->
                <div class="col-lg-8">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="card-title mb-0">
                                <i class="mdi mdi-account-group me-1"></i>
                                <?= __('潜在客户') ?>
                                <span class="badge bg-info ms-2"><?= count($candidates) ?></span>
                            </h5>
                        </div>
                        <div class="card-body">
                            <?php if (empty($candidates)): ?>
                                <div class="alert alert-info mb-0">
                                    <?= __('暂无潜在客户数据') ?>
                                </div>
                            <?php else: ?>
                                <div class="table-responsive">
                                    <table class="table table-hover align-middle">
                                        <thead class="table-light">
                                            <tr>
                                                <th><?= __('ID') ?></th>
                                                <th><?= __('姓名/信息') ?></th>
                                                <th><?= __('联系方式') ?></th>
                                                <th><?= __('社媒账户') ?></th>
                                                <th><?= __('来源') ?></th>
                                                <th><?= __('匹配度') ?></th>
                                                <th><?= __('操作') ?></th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <?php foreach ($candidates as $candidate): 
                                                $profileData = json_decode($candidate->getData(\Weline\AutoLeadAgent\Model\LeadCandidate::fields_PROFILE_DATA), true) ?: [];
                                                $name = $profileData['name'] ?? '-';
                                                $email = $candidate->getData(\Weline\AutoLeadAgent\Model\LeadCandidate::fields_EMAIL);
                                                $phone = $candidate->getData(\Weline\AutoLeadAgent\Model\LeadCandidate::fields_PHONE);
                                                $socialMediaAccounts = json_decode($candidate->getData(\Weline\AutoLeadAgent\Model\LeadCandidate::fields_SOCIAL_MEDIA_ACCOUNTS), true) ?: [];
                                                $matchedTextSegments = json_decode($candidate->getData(\Weline\AutoLeadAgent\Model\LeadCandidate::fields_MATCHED_TEXT_SEGMENTS), true) ?: [];
                                                $sourceUrl = $candidate->getData(\Weline\AutoLeadAgent\Model\LeadCandidate::fields_SOURCE_URL);
                                                $score = $candidate->getData(\Weline\AutoLeadAgent\Model\LeadCandidate::fields_SCORE) ?? 0;
                                            ?>
                                                <tr>
                                                    <td><?= $candidate->getId() ?></td>
                                                    <td>
                                                        <div class="fw-bold"><?= htmlspecialchars($name) ?></div>
                                                        <?php if (!empty($profileData['bio'])): ?>
                                                            <small class="text-muted"><?= htmlspecialchars(mb_substr($profileData['bio'], 0, 50)) ?><?= mb_strlen($profileData['bio']) > 50 ? '...' : '' ?></small>
                                                        <?php endif; ?>
                                                    </td>
                                                    <td>
                                                        <?php if ($email): ?>
                                                            <div class="mb-1">
                                                                <i class="mdi mdi-email-outline text-primary"></i>
                                                                <a href="mailto:<?= htmlspecialchars($email) ?>"><?= htmlspecialchars($email) ?></a>
                                                            </div>
                                                        <?php endif; ?>
                                                        <?php if ($phone): ?>
                                                            <div>
                                                                <i class="mdi mdi-phone-outline text-success"></i>
                                                                <a href="tel:<?= htmlspecialchars($phone) ?>"><?= htmlspecialchars($phone) ?></a>
                                                            </div>
                                                        <?php endif; ?>
                                                        <?php if (!$email && !$phone): ?>
                                                            <span class="text-muted">-</span>
                                                        <?php endif; ?>
                                                    </td>
                                                    <td>
                                                        <?php if (!empty($socialMediaAccounts)): ?>
                                                            <div class="d-flex flex-wrap gap-1">
                                                                <?php foreach ($socialMediaAccounts as $platform => $accounts): ?>
                                                                    <?php if (is_array($accounts)): ?>
                                                                        <?php foreach ($accounts as $account): ?>
                                                                            <a href="<?= htmlspecialchars($account) ?>" target="_blank" class="badge bg-info text-white" title="<?= htmlspecialchars($account) ?>">
                                                                                <i class="mdi mdi-<?= $platform === 'linkedin' ? 'linkedin' : ($platform === 'twitter' ? 'twitter' : ($platform === 'facebook' ? 'facebook' : ($platform === 'instagram' ? 'instagram' : ($platform === 'youtube' ? 'youtube' : 'link')))) ?>"></i>
                                                                                <?= ucfirst($platform) ?>
                                                                            </a>
                                                                        <?php endforeach; ?>
                                                                    <?php endif; ?>
                                                                <?php endforeach; ?>
                                                            </div>
                                                        <?php else: ?>
                                                            <span class="text-muted">-</span>
                                                        <?php endif; ?>
                                                    </td>
                                                    <td>
                                                        <?php 
                                                        $sourceUrls = json_decode($candidate->getData(\Weline\AutoLeadAgent\Model\LeadCandidate::fields_SOURCE_URLS), true) ?: [];
                                                        if (!empty($sourceUrls)): ?>
                                                            <div class="dropdown">
                                                                <button class="btn btn-sm btn-outline-info dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                                                    <i class="mdi mdi-link"></i> <?= __('查看来源') ?> (<?= count($sourceUrls) ?>)
                                                                </button>
                                                                <ul class="dropdown-menu">
                                                                    <?php foreach ($sourceUrls as $urlInfo): ?>
                                                                        <li>
                                                                            <a class="dropdown-item" href="<?= htmlspecialchars($urlInfo['url'] ?? '') ?>" target="_blank">
                                                                                <i class="mdi mdi-<?= $urlInfo['engine'] === 'Baidu' ? 'magnify' : ($urlInfo['engine'] === 'Google' ? 'google' : 'link') ?>"></i>
                                                                                <?= htmlspecialchars($urlInfo['engine'] ?? '未知') ?>
                                                                            </a>
                                                                        </li>
                                                                    <?php endforeach; ?>
                                                                    <?php if ($sourceUrl): ?>
                                                                        <li><hr class="dropdown-divider"></li>
                                                                        <li>
                                                                            <a class="dropdown-item" href="<?= htmlspecialchars($sourceUrl) ?>" target="_blank">
                                                                                <i class="mdi mdi-account"></i> <?= __('客户主页') ?>
                                                                            </a>
                                                                        </li>
                                                                    <?php endif; ?>
                                                                </ul>
                                                            </div>
                                                        <?php elseif ($sourceUrl): ?>
                                                            <a href="<?= htmlspecialchars($sourceUrl) ?>" target="_blank" class="text-decoration-none">
                                                                <i class="mdi mdi-link"></i> <?= __('查看') ?>
                                                            </a>
                                                        <?php else: ?>
                                                            <span class="text-muted">-</span>
                                                        <?php endif; ?>
                                                    </td>
                                                    <td>
                                                        <span class="badge bg-<?= $score >= 80 ? 'success' : ($score >= 50 ? 'warning' : 'secondary') ?>">
                                                            <?= number_format($score, 1) ?>%
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <button type="button" class="btn btn-sm btn-outline-primary" 
                                                                data-bs-toggle="modal" 
                                                                data-bs-target="#candidateModal<?= $candidate->getId() ?>">
                                                            <i class="mdi mdi-eye"></i> <?= __('详情') ?>
                                                        </button>
                                                    </td>
                                                </tr>
                                                
                                                <!-- 候选客户详情模态框 -->
                                                <div class="modal fade" id="candidateModal<?= $candidate->getId() ?>" tabindex="-1">
                                                    <div class="modal-dialog modal-lg">
                                                        <div class="modal-content">
                                                            <div class="modal-header">
                                                                <h5 class="modal-title"><?= __('候选客户详情') ?> #<?= $candidate->getId() ?></h5>
                                                                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                                            </div>
                                                            <div class="modal-body">
                                                                <div class="row mb-3">
                                                                    <div class="col-md-6">
                                                                        <label class="form-label fw-bold"><?= __('姓名') ?></label>
                                                                        <p><?= htmlspecialchars($name) ?></p>
                                                                    </div>
                                                                    <div class="col-md-6">
                                                                        <label class="form-label fw-bold"><?= __('匹配度') ?></label>
                                                                        <p>
                                                                            <span class="badge bg-<?= $score >= 80 ? 'success' : ($score >= 50 ? 'warning' : 'secondary') ?>">
                                                                                <?= number_format($score, 1) ?>%
                                                                            </span>
                                                                        </p>
                                                                    </div>
                                                                </div>
                                                                
                                                                <?php if ($email || $phone): ?>
                                                                <div class="row mb-3">
                                                                    <div class="col-12">
                                                                        <label class="form-label fw-bold"><?= __('联系方式') ?></label>
                                                                        <div>
                                                                            <?php if ($email): ?>
                                                                                <div class="mb-2">
                                                                                    <i class="mdi mdi-email-outline text-primary"></i>
                                                                                    <a href="mailto:<?= htmlspecialchars($email) ?>"><?= htmlspecialchars($email) ?></a>
                                                                                </div>
                                                                            <?php endif; ?>
                                                                            <?php if ($phone): ?>
                                                                                <div>
                                                                                    <i class="mdi mdi-phone-outline text-success"></i>
                                                                                    <a href="tel:<?= htmlspecialchars($phone) ?>"><?= htmlspecialchars($phone) ?></a>
                                                                                </div>
                                                                            <?php endif; ?>
                                                                        </div>
                                                                    </div>
                                                                </div>
                                                                <?php endif; ?>
                                                                
                                                                <?php if (!empty($socialMediaAccounts)): ?>
                                                                <div class="row mb-3">
                                                                    <div class="col-12">
                                                                        <label class="form-label fw-bold"><?= __('社媒账户') ?></label>
                                                                        <div class="d-flex flex-wrap gap-2">
                                                                            <?php foreach ($socialMediaAccounts as $platform => $accounts): ?>
                                                                                <?php if (is_array($accounts)): ?>
                                                                                    <?php foreach ($accounts as $account): ?>
                                                                                        <a href="<?= htmlspecialchars($account) ?>" target="_blank" class="btn btn-sm btn-outline-info">
                                                                                            <i class="mdi mdi-<?= $platform === 'linkedin' ? 'linkedin' : ($platform === 'twitter' ? 'twitter' : ($platform === 'facebook' ? 'facebook' : ($platform === 'instagram' ? 'instagram' : ($platform === 'youtube' ? 'youtube' : 'link')))) ?>"></i>
                                                                                            <?= ucfirst($platform) ?>
                                                                                        </a>
                                                                                    <?php endforeach; ?>
                                                                                <?php endif; ?>
                                                                            <?php endforeach; ?>
                                                                        </div>
                                                                    </div>
                                                                </div>
                                                                <?php endif; ?>
                                                                
                                                                <?php if (!empty($matchedTextSegments)): ?>
                                                                <div class="row mb-3">
                                                                    <div class="col-12">
                                                                        <label class="form-label fw-bold"><?= __('匹配的文本段') ?></label>
                                                                        <div class="border rounded p-3 bg-light" style="max-height: 300px; overflow-y: auto;">
                                                                            <?php foreach ($matchedTextSegments as $segment): ?>
                                                                                <div class="mb-2 p-2 bg-white rounded border-start border-primary border-3">
                                                                                    <small class="text-muted d-block mb-1">
                                                                                        <i class="mdi mdi-magnify"></i> <?= __('关键词') ?>: <strong><?= htmlspecialchars($segment['keyword'] ?? '') ?></strong>
                                                                                    </small>
                                                                                    <p class="mb-0 small"><?= htmlspecialchars($segment['text'] ?? '') ?></p>
                                                                                </div>
                                                                            <?php endforeach; ?>
                                                                        </div>
                                                                    </div>
                                                                </div>
                                                                <?php endif; ?>
                                                                
                                                                <?php 
                                                                $sourceUrls = json_decode($candidate->getData(\Weline\AutoLeadAgent\Model\LeadCandidate::fields_SOURCE_URLS), true) ?: [];
                                                                if (!empty($sourceUrls)): ?>
                                                                <div class="row mb-3">
                                                                    <div class="col-12">
                                                                        <label class="form-label fw-bold"><?= __('搜索来源网址') ?></label>
                                                                        <div class="list-group">
                                                                            <?php foreach ($sourceUrls as $urlInfo): ?>
                                                                                <a href="<?= htmlspecialchars($urlInfo['url'] ?? '') ?>" target="_blank" class="list-group-item list-group-item-action">
                                                                                    <div class="d-flex w-100 justify-content-between">
                                                                                        <h6 class="mb-1">
                                                                                            <i class="mdi mdi-<?= $urlInfo['engine'] === 'Baidu' ? 'magnify' : ($urlInfo['engine'] === 'Google' ? 'google' : 'link') ?>"></i>
                                                                                            <?= htmlspecialchars($urlInfo['engine'] ?? '未知搜索引擎') ?>
                                                                                        </h6>
                                                                                    </div>
                                                                                    <p class="mb-1 small text-break"><?= htmlspecialchars($urlInfo['url'] ?? '') ?></p>
                                                                                </a>
                                                                            <?php endforeach; ?>
                                                                        </div>
                                                                    </div>
                                                                </div>
                                                                <?php endif; ?>
                                                                
                                                                <?php if ($sourceUrl): ?>
                                                                <div class="row mb-3">
                                                                    <div class="col-12">
                                                                        <label class="form-label fw-bold"><?= __('客户主页链接') ?></label>
                                                                        <p>
                                                                            <a href="<?= htmlspecialchars($sourceUrl) ?>" target="_blank" class="text-break">
                                                                                <i class="mdi mdi-account"></i> <?= htmlspecialchars($sourceUrl) ?>
                                                                            </a>
                                                                        </p>
                                                                    </div>
                                                                </div>
                                                                <?php endif; ?>
                                                                
                                                                <?php if (!empty($profileData)): ?>
                                                                <div class="row">
                                                                    <div class="col-12">
                                                                        <label class="form-label fw-bold"><?= __('完整画像数据') ?></label>
                                                                        <pre class="bg-light p-3 rounded small" style="max-height: 200px; overflow-y: auto;"><?= htmlspecialchars(json_encode($profileData, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE)) ?></pre>
                                                                    </div>
                                                                </div>
                                                                <?php endif; ?>
                                                            </div>
                                                            <div class="modal-footer">
                                                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"><?= __('关闭') ?></button>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            <?php endforeach; ?>
                                        </tbody>
                                    </table>
                                </div>
                                
                                <!-- 分页 -->
                                <?php if (!empty($candidate_pagination)): ?>
                                <div class="mt-3">
                                    <?= $candidate_pagination ?>
                                </div>
                                <?php endif; ?>
                            <?php endif; ?>
                        </div>
                    </div>
                </div>
            </div>
