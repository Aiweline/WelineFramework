## 2026-09-14 — FAQ 搜索类型源键稳定（1.0.12）

## 2026-09-14 — FAQ/chrome i18n（1.0.13）

- FAQ hub/SEO 改 WidgetI18n::label；Index 标题用「常问问题」源键；九语 i18n CSV。

- 现象：`FaqSearchProvider::label()` 用 `__('FAQ')`，缓存按语种烘焙后仍可能把「常问问题」带回 EN 分区标题。
- 变更：`label()` 固定返回源键 `FAQ`，店面 `type-filter` / `index.phtml` 再 `WidgetI18n`。
- 版本：`1.0.11` → `1.0.12`。

## 2026-09-14 — 默认站全语言 FAQ 种子入库（1.0.11）

- 需求：把当前默认网站已开通语言的 FAQ 正文全部补齐到 `weline_faq_item`（禁止 Ollama；由 Agent 自译）。
- 变更：`FaqSeedCopyCatalog`（四 pack × 10 locale + Hub × 10）；`FaqSeedLocaleResolver` 读默认站语言集；`FaqTemplateSeedService` / `FaqHubContent` 改走目录；Setup Upgrade 幂等回填。
- 语言：`zh_Hans_CN`/`en_US`/`ar_SA`/`bn_BD`/`es_ES`/`fr_FR`/`hi_IN`/`id_ID`/`pt_BR`/`ur_PK`。
- 写库：模板新建 96 行 + Hub 新建 64 行；合计 240 行（10 locale ×（4 pack×4 + hub×8））；二次种子 0。
- 验收：`/hi_IN/product/ds-cj-cjyd3153518/` 四问印地语、无英文回落；`ar_SA`/`fr_FR` 同源部件有本地正文。
- UT：`FaqWebsiteLocaleSeedContractTest`；契约对齐 `FaqPdpConfigSeedContractTest`。
- 版本：`1.0.10` → `1.0.11`。

## 2026-09-14 — 阿语 FAQ 正文 + 推荐副标漏译（1.0.10）

- 审图：`web_ui` / `ui_shot`；RTL PDP 标题阿语，四问英文（问号跑到左边），猜你喜欢副标中文。
- 线稿：`[الأسئلة الشائعة][EN Q&A]` + `[قد يعجبك أيضًا][根据当前商品为你推荐]`。
- 原型：不改手风琴 IA；问答与副标跟当前 locale；`dir=auto` 隔离混排标点。
- 变更：retail 模板种子 ar/hi/bn/es/fr/id/pt/ur；问答 `unicode-bidi:isolate`。
- 验证：`FaqPdpResolveServiceTest` 精确阿语优先；`FaqPdpConfigSeedContractTest`。

## 2026-09-14 — PDP FAQ 区块标题漏译（1.0.9）

- 标题走 `WidgetI18n`；空态改 `<lang>`；section 带当前 `lang`；手风琴 summary 用主题 token chevron。
- 根因在 Theme CSV CJK identity，见 I18n 1.0.56。

## 2026-09-14 — 印地语 PDP FAQ 四问（1.0.8）

- 缺 `hi_IN` 行时回落英文；已写入 retail 模板 shipping/returns/warranty/payment。

## 2026-09-14 — PDP FAQ 语言降级（1.0.7）

- 现象：`/hi_IN/product/...` DEV toast「商品 FAQ @product-faq: Widget HTML is empty」；印地语无 FAQ 行时部件 `$faqs===[]` 直接 `return` 空 HTML。
- 根因：`FaqPdpResolveService` 仅加载 `locale IN ('', 当前)`，种子只有 `zh_Hans_CN`/`en_US`，未接框架 `LocaleFallbackChain`。
- 变更：解析链对齐 `当前 → en_US（非中文）→ 网站默认 → 空 locale`；精确匹配优先，再按链位次分；UT 覆盖 `hi_IN`/`ar_SA`→英文、`en_US` 缺省→中文。
- 验收：`hi_IN` PDP FAQ 四问英文回落；无 empty toast。
- 版本：`1.0.6` → `1.0.7`。

## 2026-09-13 — FAQ 中英文种子齐全（1.0.6）

- 现象：英文店面 PDP FAQ 标题已英文化，问答仍为中文（截图：配送多久能到？等）。
- 根因：
  1. `FaqTemplateSeedService` / Hub 种子仅写中文且 `locale_code=''`，解析对空 locale 回落；
  2. `product-faq` 部件仅读 `Request::getLocal()`，英文 URL 下常为空，未落到 `State::getLangLocal()`。
- 变更：模板四 pack × `zh_Hans_CN`+`en_US`；空 locale 迁移为中文；`seedSiteHubFaqs` 按 locale 幂等种子；locale 匹配分远高于网站空 locale；部件补齐 State/RequestContext locale；UT 覆盖 locale 优先。
- 验收：`/en_US/product/...` FAQ 四问英文；`/zh_Hans_CN/...` 仍中文。
- 补强：Hub `seedSiteHubFaqs` 改为按 `hub_*` 键幂等；迁空 locale；清旧 slug 重复行；`FaqHubContent` 中英显式正文。
- 版本：`1.0.5` → `1.0.6`。

## 2026-09-12 — PDP 三层范围 FAQ（1.0.4）

- Schema：`store_code` / `channel_code` / `faq_key` + UNIQUE 索引；`FaqItemScopeKeyHealer` 在 `before_schema_diff_commit` 回填空 key。
- SPI：`TemplateFaqTypeProvider`；解析：`FaqPdpResolveService`（cascade / disabled 抑制 / merge）。
- 配置：SystemConfig `faq/pdp/*`（website,store,channel）；种子四套 pack；搜索跳过 template。
- 店面/SEO：widget 与 `seoFaqsForPdp` 同源；来源标记（主题 token）。
- 版本：`1.0.3` → `1.0.4`。

## 2026-09-11 — 后台管理 UI 对齐主题 / 原型（UI 2.0）

- 变更：`Backend/Page|Item` listing+edit 去掉 Bootstrap（`btn`/`form-control`/`table`）与私造 `--ha-*` 色板。
- 对齐：`w-toolbar` / `w-select` / `w-input` / `w-button` / `w-table` / `w-empty` / `w-field` / `w-badge`；空态可行动；不重复布局 H1。
- 契约：`FaqBackendListingContractTest::testBackendTemplatesUseWelineUiNotBootstrap`。

## 2026-09-10 — FAQ 文章页空白修复（布局链）

- 现象：`/faq/b2b-wholesale` HTTP 200 且 `content-length: 0`（空白）。
- 根因：`Controller/Frontend/View` SPI/CMS 成功路径裸 `return ''`，跳过 `fetch_file_after` FAQ 布局包装。
- 变更：成功路径 `return fetch(view.phtml)`（对齐 Index）；Theme `layouts/faq/default.phtml` 文章区优先 `meta.content`；404 改 `$this->request->getResponse()->setCode(404)`；版本 `1.0.1` → `1.0.3`。
- 附带：中断的 `setup:upgrade` 曾清空 extends 索引 → `extends:rebuild` + `se:reload` 恢复 B2B `FaqPageProvider`。
- UT：`FaqViewLayoutFetchContractTest`。

## 2026-09-10 — FaqPageProvider SPI（模块页注入）

- 变更：`FaqPageProviderInterface` + Registry；View/Hub 双源（SPI 优先）；Sitemap 纳 SPI；B2B 注入 `b2b-wholesale`。
- UT：`FaqPageProviderRegistryTest`。

## 2026-09-09 — Setup 补跑 1.0.1（表/种子/注入）

- 现象：模块已登记 `setup_version=1.0.0` 但 `w_weline_faq_item` 未建、种子未写入（升级被 Schema/锁打断）。
- 变更：版本 `1.0.0` → `1.0.1`；手动执行 Upgrade：建表种子 8 条 site FAQ、CMS path 迁移、product-faq publish、Search rebuild。
- 附带：`FaqSearchProvider::allowedClientParams()`。

# Weline_Faq 开发日志

## 2026-09-09 — 万能 FAQ 1.0.0（SPI / 存储 / 后台 / 部件 / 搜索 / SEO）

- SPI：`FaqTypeProviderInterface` + `extends.php`；内置 `ProductFaqTypeProvider` / `SiteFaqTypeProvider` + `FaqTypeRegistry`。
- 存储：`Model/FaqItem`（`weline_faq_item`）；`FaqService` 提供 `FaqSeoFactsInterface`。
- 后台：`Controller/Backend/Item` 列表/编辑/保存/删除 + menu「FAQ 条目」；保留 `Backend/Page` CMS 文章。
- Setup：建表、种子 site Hub FAQ、CMS `path_group` help→faq + identifier 改写、`ensureProductFaqPublished`、Search `rebuild('faq')`。
- Theme：商品布局 `product-faq` 空槽（reviews 与 tabs 之间）；Faq widget `default_injections`。
- 搜索：`FaqSearchProvider` + `FaqSearchIndexDocumentBuilder` + `FaqItemSearchIndexObserver`。
- SEO：`ProductFaqSeoProfileProvider` 在 `page_type=product` 注入 faqs（不重复 hub）。
- 测试：`ProductFaqTypeProviderTest`；既有 Router / SeoFacts 契约保持。
- 版本：统一为 `1.0.0`（万能 FAQ 基线）。

## 2026-09-09 — Hub 补齐 og:image（1.0.4）

- 现象：`/faq` SEO 审计 1 FAIL：`og:image` Missing from head（FAQPage JSON-LD 已 PASS）。
- 变更：`FaqSeoFactsBuilder::buildHubProfile` 写入默认 `image`/`image_alt`；`FaqSeoProfileProvider` 优先保留 context 图，否则回退 `organization.logo`。
- 版本：`1.0.3` → `1.0.4`

## 2026-09-09 — Hub description 软预算 ≥50（1.0.3）

- 现象：全站审计 `/faq` description 偏短（约 44 字）触发长度/缺漏类提示。
- 变更：`FaqSeoFactsBuilder::buildHubProfile` 描述补足中英文浏览说明。
- 版本：`1.0.2` → `1.0.3`

## 2026-09-08 — Theme 编辑器 URL typed Scope claims（1.0.1）

- 变更：`FaqCmsEditorService::buildThemeEditorUrl` 与 CMS 对齐，传入 `scope_kind`/`context_version` 及 Store claims，兼容新主题可视化编辑器。
- 版本：`1.0.0` → `1.0.1`

## 2026-09-08 — 后台按网站分组管理 + 交付地址

- 列表：网站筛选 + 按站点分块展示；每站「在此网站新建」带 `website_id`。
- 编辑页展示所属网站、公开路径 `/faq/{slug}`；返回列表保留站点过滤。
- 契约：`FaqBackendListingContractTest`。
- 运行时：`setup:upgrade -m=Weline_Faq` 注册模块；`etc/env.php` `router=faq`。
- 交付探活（`p05113ef3.test.weline.com:9555`）：前台 `/faq`、`/faq`；后台 `…/faq/backend/page/listing`（已登录可见网站筛选与「在此网站新建」）。

## 2026-09-08 — 模块首发：CMS PageKind=faq + Hub 迁移 + SEO

- 新建 `Weline_Faq`：认领 `/faq`、`/faq`；`FaqPageKindProvider` 注入 CMS `PageKind`；Hub FAQ/topics 从 Theme 布局迁入 `FaqHubContent`。
- SEO：`FaqSeoFactsBuilder` + `FaqSeoProfileProvider`（hub=`faq`+faqs）；Sitemap 主权；CMS sitemap skip `path_group=faq`。
- 后台：`faq/backend/page` 列表/新建/编辑；可视化编辑 URL 锁 `layout_type=faq`。
- Theme：`layouts/faq` 改为消费 Faq 数据；Faq 启用时 Theme Router 不再抢 `/faq|/faq`。
- 验证：CmsPageKind / Faq Router / SEO / HelpLayout 契约测试通过；见 Cms 开发日志回归条目。
