<?php
$page = is_array($this->getData('page')) ? $this->getData('page') : [];
$themeEditorUrl = (string)($this->getData('theme_editor_url') ?: '');
$cmsEditUrl = (string)($this->getData('cms_edit_url') ?: '');
$listingUrl = (string)($this->getData('listing_url') ?: '');
$websiteLabel = (string)($this->getData('website_label') ?: '');
$publicPath = (string)($this->getData('public_path') ?: '');
$escape = static fn($v): string => htmlspecialchars((string)$v, ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8');
$status = strtolower(trim((string)($page['status'] ?? '')));
?>
<div class="faq-page-edit">
    <p><a href="<?= $escape($listingUrl) ?>">&larr; <?= $escape(__('返回该网站列表')) ?></a></p>
    <h1 class="h4"><?= $escape($page['title'] ?? __('帮助页面')) ?></h1>
    <dl class="row">
        <dt class="col-sm-2"><?= $escape(__('网站')) ?></dt>
        <dd class="col-sm-10"><?= $escape($websiteLabel) ?>
            <span class="text-muted">(ID <?= (int)($page['website_id'] ?? 0) ?>
            · <?= $escape($page['website_code'] ?? '') ?>)</span>
        </dd>
        <dt class="col-sm-2"><?= $escape(__('ID')) ?></dt>
        <dd class="col-sm-10"><?= (int)($page['page_id'] ?? 0) ?></dd>
        <dt class="col-sm-2"><?= $escape(__('Slug')) ?></dt>
        <dd class="col-sm-10"><code><?= $escape($page['slug'] ?? '') ?></code></dd>
        <dt class="col-sm-2"><?= $escape(__('公开路径')) ?></dt>
        <dd class="col-sm-10"><code><?= $escape($publicPath) ?></code></dd>
        <dt class="col-sm-2"><?= $escape(__('状态')) ?></dt>
        <dd class="col-sm-10"><?= $escape($status) ?></dd>
        <dt class="col-sm-2"><?= $escape(__('path_group')) ?></dt>
        <dd class="col-sm-10"><code>faq</code></dd>
    </dl>
    <div class="d-flex flex-wrap gap-2">
        <?php if ($themeEditorUrl !== ''): ?>
            <a class="btn btn-primary" href="<?= $escape($themeEditorUrl) ?>" target="_blank" rel="noopener">
                <?= $escape(__('打开可视化编辑器（faq 布局）')) ?>
            </a>
        <?php endif; ?>
        <?php if ($cmsEditUrl !== ''): ?>
            <a class="btn btn-outline-secondary" href="<?= $escape($cmsEditUrl) ?>">
                <?= $escape(__('CMS 字段编辑（标题/发布/多语言）')) ?>
            </a>
        <?php endif; ?>
    </div>
    <p class="text-muted mt-3 mb-0">
        <?= $escape(__('可视化编辑锁定 layout_type=faq；标题、slug、发布状态与多语言请在 CMS 字段编辑中修改。')) ?>
    </p>
</div>
