<?php
/** @var array<string,mixed> $article */
$article = is_array($this->getData('faq_article')) ? $this->getData('faq_article') : [];
$payload = is_array($this->getData('faq_article_payload')) ? $this->getData('faq_article_payload') : [];
$contentHtml = '';
if (is_array($payload['content'] ?? null)) {
    $contentHtml = (string)($payload['content']['html'] ?? $payload['content']['body'] ?? '');
}
$title = htmlspecialchars((string)($article['title'] ?? ''), ENT_QUOTES, 'UTF-8');
?>
<article class="faq-article w-container" data-faq-article="1">
    <h1 class="faq-article__title"><?= $title ?></h1>
    <?php if ($contentHtml !== ''): ?>
        <div class="faq-article__body"><?= $contentHtml ?></div>
    <?php else: ?>
        <p class="faq-article__empty"><?= htmlspecialchars((string)__('此帮助文章暂无正文，请在帮助中心后台使用可视化编辑器完善。'), ENT_QUOTES, 'UTF-8') ?></p>
    <?php endif; ?>
    <p class="faq-article__back"><a href="/faq"><?= htmlspecialchars((string)__('返回帮助中心'), ENT_QUOTES, 'UTF-8') ?></a></p>
</article>
