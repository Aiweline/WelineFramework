<?php
/** Help hub body slot content — layout shell remains Theme layouts/help. */
$topics = $this->getData('help_topics') ?? [];
$faqs = $this->getData('help_faqs') ?? [];
$quickLinks = $this->getData('help_quick_links') ?? [];
?>
<div class="help-module-hub" data-help-hub="1" hidden>
    <!-- Assigned into Theme help layout via Template bag before layout materializes. -->
</div>
<?php
// Hub uses Theme layoutType=help; content arrays are consumed by the layout.
echo '';
