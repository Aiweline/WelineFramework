<!-- weline:module-development-baseline:generated-once -->
# Weline_Help 开发日志

## 2026-09-09 — Hub 补齐 og:image（1.0.4）

- 现象：`/help` SEO 审计 1 FAIL：`og:image` Missing from head（FAQPage JSON-LD 已 PASS）。
- 变更：`HelpSeoFactsBuilder::buildHubProfile` 写入默认 `image`/`image_alt`；`HelpSeoProfileProvider` 优先保留 context 图，否则回退 `organization.logo`。
- 版本：`1.0.3` → `1.0.4`

## 2026-09-09 — Hub description 软预算 ≥50（1.0.3）

- 现象：全站审计 `/help` description 偏短（约 44 字）触发长度/缺漏类提示。
- 变更：`HelpSeoFactsBuilder::buildHubProfile` 描述补足中英文浏览说明。
- 版本：`1.0.2` → `1.0.3`

## 2026-09-08 — Theme 编辑器 URL typed Scope claims（1.0.1）

- 变更：`HelpCmsEditorService::buildThemeEditorUrl` 与 CMS 对齐，传入 `scope_kind`/`context_version` 及 Store claims，兼容新主题可视化编辑器。
- 版本：`1.0.0` → `1.0.1`

## 2026-09-08 — 后台按网站分组管理 + 交付地址

- 列表：网站筛选 + 按站点分块展示；每站「在此网站新建」带 `website_id`。
- 编辑页展示所属网站、公开路径 `/help/{slug}`；返回列表保留站点过滤。
- 契约：`HelpBackendListingContractTest`。
- 运行时：`setup:upgrade -m=Weline_Help` 注册模块；`etc/env.php` `router=help`。
- 交付探活（`p05113ef3.test.weline.com:9555`）：前台 `/help`、`/faq`；后台 `…/help/backend/page/listing`（已登录可见网站筛选与「在此网站新建」）。

## 2026-09-08 — 模块首发：CMS PageKind=help + Hub 迁移 + SEO

- 新建 `Weline_Help`：认领 `/help`、`/faq`；`HelpPageKindProvider` 注入 CMS `PageKind`；Hub FAQ/topics 从 Theme 布局迁入 `HelpHubContent`。
- SEO：`HelpSeoFactsBuilder` + `HelpSeoProfileProvider`（hub=`faq`+faqs）；Sitemap 主权；CMS sitemap skip `path_group=help`。
- 后台：`help/backend/page` 列表/新建/编辑；可视化编辑 URL 锁 `layout_type=help`。
- Theme：`layouts/help` 改为消费 Help 数据；Help 启用时 Theme Router 不再抢 `/help|/faq`。
- 验证：CmsPageKind / Help Router / SEO / HelpLayout 契约测试通过；见 Cms 开发日志回归条目。
