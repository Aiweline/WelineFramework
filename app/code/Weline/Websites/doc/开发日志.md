# Weline_Websites 开发日志

## 2026-09-14 — FPC 命中装饰支持 br（1.8.25）

- `FrontendWorkerScopeBootstrapResponseService`：对 `Content-Encoding: br` 解码→注入 bootstrap→再 br 编码；失败仍 fail-closed。
- 配合 Framework FPC br 权威存货，避免 HIT 默认 br 后装饰 unsupported。

## 2026-09-13 — 默认站点补种保留管理配置（1.8.24）

- 根因：默认站点初始化逐字段比较安装默认值，再整行更新；已有合法 URL 会被重置为 `http://localhost`，名称与本地化配置也会丢失。网站列表和无绑定域名时的证书目录读取又触发了该写入口。
- 修复：保留缺失记录的初始化和旧默认 ID 迁移；已有行只维护默认 `code`，不覆盖业务配置。网站列表和活动证书域名只读路径移除补种调用。
- 回归：`Test/Integration/DefaultWebsiteConfigurationPreservationTest.php` 在真实数据库受管事务内配置默认站点，重复补种后核对 URL、名称、语言/货币、时区和 scope，最终回滚全部测试配置。前置 RED 已复现覆盖；执行命令为 `php vendor/bin/phpunit --bootstrap app/bootstrap.php app/code/Weline/Websites/Test/Integration/DefaultWebsiteConfigurationPreservationTest.php --no-progress`。
- 运行交付：主任务统一升级/加载共享 WLS 后，再通过默认站点正常保存入口修正当前 URL 并运行产品 Demo 全链路；本变更不硬编码实际 Host。

## 2026-09-11 — `<w:scope>` 作用范围标签使用指南（1.8.23）

- 新增 `doc/scope-select标签使用指南.md`：四级 Global/网站/店铺/渠道；与单域 website/store/channel Taglib 分工；职责边界（Websites 身份 + SystemConfig 目录 + Taglib 渲染）；提交解析与禁止项。
- `README.md` / `store-saleschannel-scope.md` / Taglib `场景映射表` 互链更新。
- 版本：`1.8.22` → `1.8.23`

## 2026-09-11 — StoreSelectOptions（store_id 选项）（1.8.22）

- 新增 `StoreSelectOptions`：`value=store_id`，跨模块经 `getStoreCatalogV1` 组装；供库存调整等 `<w:websites:store:select>`。
- 版本：`1.8.21` → `1.8.22`

## 2026-09-10 — WebsiteSelect 箭头/清除钮垂直居中（1.8.20）

- 审图：`web_ui` / `ui_shot`（站点范围触发条箭头偏下）
- 变更：`WebsiteSelect` trigger 去固定 height；actions/chevron/clear 用 inline-flex 居中；chevron 改 `▾`
- 版本：`1.8.19` → `1.8.20`

## 2026-09-09 — 站级爬虫保存后立即发布 RuntimePolicy（1.8.19）

- 问题：仅 `AttackDetector::updateRules` 写入 `domain_overrides`，Worker 吃的是不可变策略包，本站爬虫策略保存后线上不生效。
- 变更：`WebsiteCrawlerPolicyService::syncToWlsDomainOverrides` 同步规则后调用 `RuntimePolicyLivePublisher` 编译并 IPC 发布。
- 验收：保存默认站策略后探针 GPTBot=403 / Googlebot=200。
- 版本：`1.8.18` → `1.8.19`

## 2026-09-09 — 爬虫策略去掉封禁秒数（1.8.18）

- 审图：用户指出「拦截封禁秒数」多余；产品语义为直接拦截/禁用，不配置封禁时长。
- 变更：编辑页去掉秒数字段；`WebsiteCrawlerPolicyService` 恒写 `block_duration=0`（仅拒本次请求）；文案说明不额外封 IP。
- 验收：单测 normalize→0；Browser 无「拦截封禁秒数」。
- 版本：`1.8.17` → `1.8.18`

## 2026-09-09 — 爬虫策略 UI 定稿为可检索分页表格（1.8.17）

- 审图：卡片墙 / 芯片 / 原型底栏 fail；定稿只保留表格。
- 变更：`partials/crawler-policy.phtml` 异步查找 + 翻页（每页 10 条），隐藏域提交全量条目；去掉 A/B/C 变体与私有卡片样式。
- 验收：网站编辑页 Browser；单测 normalize 仍绿。
- 版本：`1.8.16` → `1.8.17`

## 2026-09-09 — 网站级爬虫拦截/放行策略（1.8.16）

- 目标：爬虫策略由 `Weline_Websites` 自管；编辑网站表单可增删条目，每条可选拦截或放行；保存后同步到 WLS `domain_overrides[host].rules.crawler_block`。
- 变更：`WebsiteCrawlerPolicy` 表；`WebsiteCrawlerPolicyService`；Observer `website_save_after`；编辑表单核心区 `partials/crawler-policy.phtml`（`?crawler_variant=A|B|C` 表格/芯片/卡片原型）；依赖 `CrawlerBlockCatalog` entries 匹配（放行优先）。
- 验收：`WebsiteCrawlerPolicyServiceTest`；后台网站编辑页 Browser 三变体。
- 版本：`1.8.15` → `1.8.16`

## 2026-09-08 — 网站子路径禁止语言/货币编码（1.8.14）

- 目标：新建与编辑网站时，`sub_path` 任意路径段不得携带语言码或货币码（大小写不敏感）；填写 debounce 即时报错，保存服务端拒绝。
- 变更：`WebsiteSubPathValidator`；`Website` add/edit/`quickSave` 路径解析断言；`DomainPool::getCheckConflict` 对齐；`form.phtml` 注入码表；`website-form.js` 300ms debounce 校验；`domain-select-mount.js` 全段保留/locale 检查。
- 验收：`WebsiteSubPathValidatorTest`（6）、`WebsiteAdminTemplateContractTest`（9）；Browser 添加页 `usd`/`en_US` 报错、`/shop` 通过；编辑页 `USD` 报错。
- 版本：`1.8.13` → `1.8.14`

## 2026-09-08 — Scope L2/L3 路径命中与店渠上下文粘贴（1.8.13）

- 目标：Website/Store/Channel 统一「路径推出 → 一次粘贴上下文 → 共享缓存」；L1 目录热缓存保留，补 L2 路径命中与 L3 `ScopeData`。
- 变更：`ScopePathMatchKey/Hit/Cache`、`ScopeData`、`ScopeResolver` 快路径与粘贴；`Store`/`SalesChannel` load 与 catalog `byId` 上下文优先。
- 验收：`ScopePathMatchCacheContractTest`、`ScopeResolverPathMatchFastPathTest`。
- 版本：`1.8.12` → `1.8.13`

## 2026-09-08 — 当前站读取优先上下文，命中后共享缓存（1.8.12）

- 目标：本请求命中网站后一次粘贴快照到 `WebsiteData`/`RequestContext`；普通 `Website::load` / `w_query getWebsiteById|ByCode` 先读上下文，再读 `website_detect` 共享快照；`forceReload` / `force_reload` 才打库。
- 变更：`WebsiteData::setWebsite` 一次粘贴语/币并 `publishSharedSnapshot`；`Website::load` 上下文/共享缓存优先；QueryProvider 对齐；`AbstractModel::load(..., forceReload: true)`。
- 验收：`WebsiteDataRequestIsolationTest`、`WebsiteLoadContextCacheContractTest`（6 项通过）。
- 版本：`1.8.11` → `1.8.12`

## 2026-09-08 — ScopeIdentityCatalog 水合存储 scope 的 website_id=0（1.8.11）

- 问题：Theme `theme-preview/content` 经 `ThemeRuntimeLayoutResolver` 从编码 scope 还原时 `website_id` 恒为 0；catalog 要求与真实 id 全等，非默认站 CMS/可视化编辑预览抛 `system_config_scope_claim_identity_mismatch`。
- 修复：`authoritativeIdentity` 对「code≠default 且 claimed id=0」按 code 水合；错误非零 id 仍拒绝；缺失网站改为 `system_config_website_scope_not_found`。
- 验收：`ScopeIdentityCatalogAuthoritativeHydrationTest`。
- 版本：`1.8.10` → `1.8.11`

## 2026-09-06 — 店铺/渠道目录缓存水合 DTO（1.8.10）

- 问题：共享热缓存 JSON 往返后 `StoreCatalog`/`SalesChannelCatalog` 的 `byCode` 等返回 array，触发 TypeError，认证页拉全局头尾布局失败。
- 修复：`remember()` 命中后把 array 水合回 `StoreSummary` / `SalesChannelSummary`。
- 版本：`1.8.9` → `1.8.10`

## 2026-09-05 — 默认站绑定当前项目 *.test.weline.com Host（1.8.9）

- 当前状态：`implemented`（宿主原生编辑）
- 变更：`DefaultWebsiteService` 本地域名初始化加入当前 WLS 标准项目 Host；`SyncWlsLocalDomainPool` 在标准项目 Host 注册时绑定 `WebsiteDomain` 到默认站。
- 验收：`DefaultWebsiteServiceLocalDomainContractTest`；对本机执行 `ensureDefaultWebsite(true)` 后默认站域名列表含 `p{hash}.test.weline.com`。
- 版本：`1.8.8` → `1.8.9`

## 2026-09-05 — 本地域名根对齐 test.weline.com（1.8.8）

- 当前状态：`implemented`
- 变更：AI 建站本地域名 / 通配证 / DomainSelect 本机判定跟随 Server `LocalDomainPolicy`，默认 `*.test.weline.com`，并兼容遗留 `*.weline.test`。
- 验收：与 Server `LocalDomainPolicyTest` 及本地域名就绪单测一致。
- 版本：`1.8.7` → `1.8.8`

## 2026-09-05 — 域名选择器触发器显示域名胶囊（1.8.7）

- 当前状态：`implemented`（宿主原生编辑）
- 问题：网站表单多选域名触发器显示「已选择 N 个域名」，看不出具体域名。
- 变更：`DomainSelect` 增加 `selected-domains` SSR/回填胶囊；编辑页传入真实域名；加载时 `hydrateInitialSelection`；去掉数量摘要文案。
- 验收：默认站 `edit?id=0` 触发器直接显示 `p05113ef3.test.weline.com` / `127.0.0.1` / `localhost` 胶囊，无「已选择 N 个」；`DomainSelectSelectedCapsuleContractTest`。
- 版本：`1.8.6` → `1.8.7`

## 2026-09-05 — 店铺/渠道 SEO 上下文与旧入口快照（1.8.6）

- 当前状态：代码已实现，真实 WLS/后台验收待主任务完成。
- 变更：现有 store_save_after/channel_save_after 增加 before；SEO Head 通过公开 Store/Channel Catalog 取得当前作用域，Store 名称/独立 URL 覆盖网站默认值，Channel 无独立字段时继承。
- 定向验证：SeoHeadScopeContextTest 验证默认网站下独立 Store 的名称、含端口路径的 URL、Store/Channel 身份。
- 未完成：setup:upgrade 后真实后台 Store/Channel 保存与 SEO/CDN 派生操作，实际前台 Head 验收。
- 交付地址：待主任务探活后记录；本子任务未执行服务重启或外部请求。

## 2026-09-05 — 默认 Store/Channel ID=0 可编辑保存（1.8.5）

- 当前状态：`implemented`
- 问题：编辑页用 `$id <= 0` 把合法默认店/渠道判成不存在；`postPositiveInt` 与 `!(int)getId()` 拒绝 ID=0，禁运等 Hook 无法配置默认作用域。
- 变更：模板改为 `array_key_exists('code')`；更新接口用 `postNonNegativeInt`；AdminService 用 `hasData`+主键比对。
- 验收：`edit-store?store_id=0` / `edit-channel?channel_id=0` 出表单；`StoreChannelManagementContractTest`。
- 版本：`1.8.4` → `1.8.5`

## 2026-09-04 — ScopeManagement 店铺/渠道编辑 + Hook/Event（1.8.4）

- 当前状态：`implemented`
- 变更：`ScopeManagement` 增加 `edit-store`/`edit-channel` 与 `update-store`/`update-channel`；列表带编辑入口。
- Hook：`backend::store|channel::form::sections-after`；Event：`store_save_after`/`channel_save_after`（payload 含 `post_data.extensions`）。
- 文档：`doc/hook/backend/store|channel/form/sections-after.md`、`doc/event/store_save_after.md`、`channel_save_after.md`。
- 版本：`1.8.3` → `1.8.4`

## 2026-09-03 — 主题编辑器基础信息身份 Provider（1.8.3）

- 当前状态：`implemented`
- 问题：主题「基础信息」改站名需真实改网站/店铺/渠道，不能只写 appearance.brand。
- 变更：`ThemeBrandBasicsIdentityProvider` 注册 `theme.brand_basics_identity.websites`；Website 写 `name` + Backend `site_name`/`site_description`；Store/Channel 写实体 `name`。
- 验收：Theme `ThemeBrandBasicsContractTest`；主题编辑器 Website Scope 基础信息身份字段。

## 2026-08-30 — 后台 Cookie 跟随 website_id 站点隔离

- 当前状态：`implemented`
- 问题：后台跳过 CookieScope 使用 `WELINE_SESSID_{port}`；前台发 `_w{id}` 时 Expire 无后缀别名，把后台登录 Cookie 清掉 → `auth.log` 大量 `cookie_present=false` 掉线。
- 变更：`CookieScopeResolve` 后台也按请求 `website_id` 加 `_w{id}`，`mount_path` 固定 `/`；ACL 掉线日志用 `hasRequestCookie()` + 记录 `cookie_name`；`REQ-WEBSITES-0002` 改为站点隔离。
- 验收：`CookieScopeResolveTest`；后台登录后访问前台再回后台，Session 名保持 `WELINE_SESSID_{port}_w{id}`。

## 2026-08-30 — CookieScope 根站空 URL 合成（登录 Session 粘性）

- 当前状态：`implemented`
- 问题：根站 `WELINE_WEBSITE_URL` 常为空，CookieScope 依赖 URL 时与 Session Cookie 名 `_w{id}` 不同步，登录后刷新踢回。
- 变更：`CookieScopeResolve` 在 URL 空时用请求 `HTTP_HOST` 合成根站 URL；配合 Framework Session 重断言。
- 验收：`CookieScopeResolveTest::testRootWebsiteWithEmptyConfiguredUrlStillQualifiesFromRequestHost`。

## 2026-08-28 — WebsiteSelect 支持名称与域名搜索

- 当前状态：`implemented`
- 问题：`<w:websites:website:select>` 仅按 label/编码过滤，输入域名搜不到站点。
- 变更：`WebsiteSelectOptions::fromRows` 写入 `url`/`domain`，`meta` 优先域名；标签搜索 haystack 含名称、域名、URL、编码；默认 placeholder 改为「搜索站点名称或域名」。
- 验收：`WebsiteSelectOptionsTest`、`WebsiteSelectThemeTokensContractTest` 域名搜索契约。

## 2026-08-28 — 编辑网站 website_id=0 误进 offcanvas 错误页

- 当前状态：`implemented`
- 现象：`/websites/admin/website/edit?website_id=0` 报「网站ID必须是非负规范整数」，错误 `msg` 被反复编码后看起来像无限嵌套。
- 根因：`edit()` 只读 URL 参数 `id`；列表用 `id`，交接/深链常用 `website_id`，缺 `id` 时空串触发校验失败。
- 修复：新增 `resolveEditTargetWebsiteIdFromRequest()`，`id` 优先，缺省回退 `website_id`（含默认站 `0`）；POST 同步兼容。
- 验收：`WebsiteEditTargetIdContractTest`；浏览器打开 `website_id=0` 应出编辑表单而非 getError。

## 2026-08-28 — 默认路径误用关联语言首项 en_US

- 当前状态：`implemented`
- 现象：默认网站 `default_language=zh_Hans_CN`，无语言前缀路径仍被 `Url::parser` 解成 `en_US`。
- 根因：`Url::parser` 在匹配网站前用 `State::getLang()` 预填 `WELINE_USER_LANG`；无 WebsiteData 时回落到 `preferredLanguageCodes()[0]`（关联表插入序常为 `en_US`），站点匹配后因非空未覆盖。
- 修复：站点匹配前不再预填；匹配后无路径语言时强制写入 `website.default_language`；`getWebsiteLanguageCodes()` 将默认语言排到首位。
- 验收：`UrlParserDefaultLanguageContractTest`；CLI `Url::parser` 断言 `language=zh_Hans_CN`；默认路径 HTML `data-lang=zh_Hans_CN`。

## 2026-08-28 — 商店/渠道管理网站选择 auto-submit

- 当前状态：`implemented`
- 需求：商店管理筛选去掉「切换网站」按钮，选择即切换。
- 实现：`WebsiteSelect` 增加 `auto-submit`（对齐 LanguageSelect）；`ScopeManagement/index.phtml` 筛选表单使用 `auto-submit="true"`。
- 验收：契约测试；商店/渠道管理页探活。

## 2026-08-28 — 安装注入默认店铺/渠道 ID=0

- 当前状态：`implemented`
- 需求：网站内管理 Store/渠道；系统安装时自动注入默认店铺与默认渠道，主键均为 `0`。
- 实现：`Store::ID_DEFAULT` / `SalesChannel::ID_DEFAULT`；`StoreChannelSeedService` 对 `website_id=0` 强制插入或迁移 `store_id=0`、`channel_id=0` 并改写引用列；Catalog/Query/ScopeKernel 合法化 ID `0`。
- 验收：`setup:upgrade` 后库内断言；`StoreChannelCatalogContractTest`；网站管理列表。

## 2026-08-28 — 编辑网站默认语言无法保存

- 当前状态：`implemented`
- 现象：高级选项里改「默认语言」保存后仍回到 `en_US`；默认货币空态文案也误显示「可多选」。
- 根因：同一表单先渲染 `domain:select multiple=true`，`AttributeCodeCompiler` 未清理 `Taglib__*`，后续默认语言/货币选择器未声明 `multiple` 却继承了 `true`；单选字段按多选提交时 PHP 只保留同名最后一项，常落回 `en_US`。
- 修复：Framework 编译器隔离标签属性；LanguageSelect/CurrencySelect 对省略的 `multiple` 强制 `false`；Website add/edit 归一化标量默认值并保证默认码进入关联列表。
- 验收：AttributeCodeCompiler / LanguageSelect / CurrencySelect / Website 表单契约测试；WLS 后台编辑网站保存后回读 `default_language`。

## 2026-08-28 — 网站管理列表 DataTable 重构

- 当前状态：`implemented_validated`（单元测试通过；运行时页面需 WLS + 后台登录验收）
- 变更：`index.phtml` 改用 `w:d-table mode="local"`；新增 `WebsiteAdminListPresenter` 合并列；删除嵌套 Store 卡片的 `table.phtml`。
- 列：`site_head` / `access_entry` / `market_cluster` / `store_channel_summary` + sticky `row-actions`。
- 依赖：模块 `requires` 增加 `Weline_DataTable`。

## 2026-08-22 — SystemConfig Scope 目录贡献注册修复

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-WEBSITES-0003`
- 根因：Websites 以公共接口键替换 SystemConfig fallback，与 Framework 的唯一 Provider 契约冲突。
- 实现方向：改为具名能力前缀贡献，目录实现仍由 Websites 官方 Catalog 组装；SystemConfig 负责唯一入口和委托校验。
- 验收：Provider Registry 与 Scope 委托测试通过；完整路由升级和独立 WLS 不再出现重复 Provider。

## 2026-08-18 — 后台请求不套用前台 Cookie 站点隔离

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-WEBSITES-0002`
- 根因：登录页与后续后台页解析 CookieScope 的时机不同，Session Cookie 会在 `WELINE_SESSID_9555` 与 `WELINE_SESSID_9555_w0` 之间切换；后者还会过期前者，表现为刚登录即 `not_logged_in`。
- 实现：`CookieScopeResolve` 在 `backend`/`rest_backend` 或 `is_backend` 时不贡献 `name_suffix`/`expire_unscoped_aliases`。
- 验收：`CookieScopeResolveTest::testBackendAreaDoesNotQualifySessionCookieOrExpireUnscopedAliases` 通过。热重载后后台登录与 ACL 均只发 `WELINE_SESSID_9555`，不再 Expire 无后缀名。2026-08-18 内置 Browser：登录后 ACL 角色 / 媒体 / ACL 新增 / 仪表盘连点保持登录。

## 2026-08-17 — 默认网站货币跟随应用配置

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-WEBSITES-0001`
- 根因：默认网站行与货币关联硬编码为 CNY，导致本地站点的 USD 配置在初始化后被回写。
- 实现：`DefaultWebsiteService::defaultCurrency()` 从 `Env::system('currency', 'CNY')` 读取并校验三位字母币种；默认行与必备关联统一使用该值。
- 数据处理：摩托车项目的默认站点已通过正式 Service/Model 接口归一为 USD 且只保留 USD；这是项目数据，不是核心服务的删除策略。
- 验收：聚焦契约测试通过；WLS 完整重启后回读为 `system=USD / website=USD / allowed=[USD]`；商品/搜索/详情/购物车/结账前置页浏览器验收通过。
