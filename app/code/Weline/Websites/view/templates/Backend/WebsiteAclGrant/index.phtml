<?php
/**
 * @var \Weline\Framework\View\Template $this
 */
$websites = $this->getData('websites') ?: [];
?>
<div class="w-container">
    <div class="w-cluster" data-justify="between" data-align="center" style="--w-mb:var(--weline-space-4);">
        <h4 style="--w-mb:0;"><lang>网站功能授权</lang></h4>
        <a class="w-button" href="@backend-url('*/admin/website/index')" data-tone="neutral" data-variant="outline" data-size="sm">
            <w:icon name="arrow-left" size="sm"></w:icon> <lang>返回网站管理</lang>
        </a>
    </div>
    <p class="w-text" data-tone="muted"><lang>为非默认网站勾选可用菜单与 ACL。未授权的子站后台菜单为空。</lang></p>
    <div class="w-table-wrap">
        <table class="w-table" data-w-vertical="middle">
            <thead>
            <tr>
                <th><lang>网站</lang></th>
                <th><lang>代码</lang></th>
                <th><lang>已授权资源数</lang></th>
                <th class="w-text" data-align="end"><lang>操作</lang></th>
            </tr>
            </thead>
            <tbody>
            <?php if ($websites === []): ?>
                <tr>
                    <td colspan="4" class="w-text" data-align="center" data-tone="muted" style="--w-pt:var(--weline-space-6);--w-pb:var(--weline-space-6);"><lang>暂无非默认网站</lang></td>
                </tr>
            <?php else: ?>
                <?php foreach ($websites as $row): ?>
                    <tr>
                        <td><?= htmlspecialchars((string)$row['name']) ?></td>
                        <td><code><?= htmlspecialchars((string)$row['code']) ?></code></td>
                        <td><?= (int)$row['grant_count'] ?></td>
                        <td class="w-text" data-align="end">
                            <?php
                            $editUrl = $this->getUrl('*/backend/website-acl-grant/edit', [
                                'website_id' => (int)$row['website_id'],
                            ]);
                            ?>
                            <a class="w-button" href="<?= htmlspecialchars((string)$editUrl, ENT_QUOTES, 'UTF-8') ?>" data-tone="primary" data-variant="outline" data-size="sm">
                                <w:icon name="edit" size="sm"></w:icon> <lang>授权</lang>
                            </a>
                        </td>
                    </tr>
                <?php endforeach; ?>
            <?php endif; ?>
            </tbody>
        </table>
    </div>
</div>
