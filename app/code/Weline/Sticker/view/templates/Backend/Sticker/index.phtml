<?php
/**
 * Sticker 管理列表页
 *
 * @var \Weline\Framework\View\Template $this
 */


$stickers = $this->getData('stickers') ?: [];
$total = (int)$this->getData('total');
$search = $this->getData('search') ?: '';
$targetModule = $this->getData('target_module') ?: '';
$sourceModule = $this->getData('source_module') ?: '';
$modules = $this->getData('modules') ?: [];
?>
<!-- 页面标题 -->
                <div class="w-grid">
                    <div style="--w-span:12;">
                        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
                            <h4 style="--w-mb-sm:0;">
                                <w:icon name="star" size="sm"></w:icon> <?= __('Sticker管理') ?>
                            </h4>
                            <div class="w-cluster">
                                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                                    <li class="w-breadcrumb__item"><a href="<?= $this->getBackendUrl('admin') ?>"><?= __('首页') ?></a></li>
                                    <li class="w-breadcrumb__item"><?= __('系统') ?></li>
                                    <li class="w-breadcrumb__item" data-state="active"><?= __('Sticker管理') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 消息提示 -->
                <div class="w-grid">
                    <template>Weline_Component::message.phtml</template>
                </div>

                <!-- 操作栏 -->
                <div class="w-grid" style="--w-mb:var(--weline-space-4);">
                    <div style="--w-span:12;">
                        <div class="w-cluster" data-justify="between" data-align="center">
                            <div>
                                <span class="w-badge" data-w-background="primary"><?= __('总数：%{1}', [$total]) ?></span>
                            </div>
                            <div>
                                <w:form method="get" class="w-cluster" style="--w-gap:var(--weline-space-2);">
                                    <input type="text" name="search" class="w-input" placeholder="<?= __('搜索模块名、文件路径') ?>" value="<?= htmlspecialchars($search) ?>" data-size="sm">
                                    <select name="target_module" class="w-select" data-size="sm">
                                        <option value=""><?= __('所有目标模块') ?></option>
                                        <?php foreach ($modules as $moduleName): ?>
                                            <option value="<?= htmlspecialchars($moduleName) ?>" <?= $targetModule === $moduleName ? 'selected' : '' ?>>
                                                <?= htmlspecialchars($moduleName) ?>
                                            </option>
                                        <?php endforeach; ?>
                                    </select>
                                    <select name="source_module" class="w-select" data-size="sm">
                                        <option value=""><?= __('所有来源模块') ?></option>
                                        <?php foreach ($modules as $moduleName): ?>
                                            <option value="<?= htmlspecialchars($moduleName) ?>" <?= $sourceModule === $moduleName ? 'selected' : '' ?>>
                                                <?= htmlspecialchars($moduleName) ?>
                                            </option>
                                        <?php endforeach; ?>
                                    </select>
                                    <button type="submit" class="w-button" data-tone="primary" data-size="sm">
                                        <w:icon name="search" size="sm"></w:icon> <?= __('搜索') ?>
                                    </button>
                                    <a href="<?= $this->getBackendUrl('*/backend/sticker/index') ?>" class="w-button" data-tone="neutral" data-size="sm">
                                        <w:icon name="refresh" size="sm"></w:icon> <?= __('重置') ?>
                                    </a>
                                </w:form>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Sticker列表 -->
                <div class="w-grid">
                    <div style="--w-span:12;">
                        <div class="w-card">
                            <div class="w-card__body">
                                <?php if (empty($stickers)): ?>
                                    <div class="w-text" data-align="center" style="--w-pt:var(--weline-space-12);--w-pb:var(--weline-space-12);">
                                        <w:icon name="star" size="sm"></w:icon>
                                        <p class="w-text" data-tone="muted" style="--w-mt:var(--weline-space-4);"><?= __('暂无Sticker') ?></p>
                                    </div>
                                <?php else: ?>
                                    <div class="w-table-wrap">
                                        <table class="w-table">
                                            <thead>
                                                <tr>
                                                    <th><?= __('目标模块') ?></th>
                                                    <th><?= __('目标文件') ?></th>
                                                    <th><?= __('来源模块') ?></th>
                                                    <th><?= __('Sticker文件') ?></th>
                                                    <th><?= __('规则数量') ?></th>
                                                    <th><?= __('操作') ?></th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <?php foreach ($stickers as $sticker): ?>
                                                    <tr>
                                                        <td>
                                                            <span class="w-badge" data-w-background="info"><?= htmlspecialchars($sticker['target_module']) ?></span>
                                                        </td>
                                                        <td>
                                                            <code class="w-text" data-tone="primary"><?= htmlspecialchars($sticker['target_file']) ?></code>
                                                        </td>
                                                        <td>
                                                            <span class="w-badge" data-w-background="success"><?= htmlspecialchars($sticker['source_module']) ?></span>
                                                        </td>
                                                        <td>
                                                            <code class="w-text" data-tone="muted"><?= htmlspecialchars($sticker['sticker_relative_path']) ?></code>
                                                        </td>
                                                        <td>
                                                            <span class="w-badge" data-w-background="warning"><?= $sticker['actions_count'] ?></span>
                                                        </td>
                                                        <td>
                                                            <a href="<?= $this->getBackendUrl('*/backend/sticker/detail', [
                                                                'target_module' => $sticker['target_module'],
                                                                'target_file' => $sticker['target_file'],
                                                                'source_module' => $sticker['source_module']
                                                            ]) ?>" class="w-button" data-tone="primary" data-size="sm">
                                                                <w:icon name="eye" size="sm"></w:icon> <?= __('查看详情') ?>
                                                            </a>
                                                        </td>
                                                    </tr>
                                                <?php endforeach; ?>
                                            </tbody>
                                        </table>
                                    </div>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
