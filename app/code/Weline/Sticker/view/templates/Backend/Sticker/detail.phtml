<?php
/**
 * Sticker 详情页
 *
 * @var \Weline\Framework\View\Template $this
 */


$targetModule = $this->getData('target_module') ?: '';
$targetFile = $this->getData('target_file') ?: '';
$sourceModule = $this->getData('source_module') ?: '';
$stickerFile = $this->getData('sticker_file') ?: '';
$stickerRelativePath = $this->getData('sticker_relative_path') ?: '';
$actions = $this->getData('actions') ?: [];
$stickerContent = $this->getData('sticker_content') ?: '';
$sourceContent = $this->getData('source_content') ?: '';
$sourceFilePath = $this->getData('source_file_path') ?: '';
?>
<!-- 页面标题 -->
                <div class="w-grid">
                    <div style="--w-span:12;">
                        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
                            <h4 style="--w-mb-sm:0;">
                                <w:icon name="star" size="sm"></w:icon> <?= __('Sticker详情') ?>
                            </h4>
                            <div class="w-cluster">
                                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                                    <li class="w-breadcrumb__item"><a href="<?= $this->getBackendUrl('admin') ?>"><?= __('首页') ?></a></li>
                                    <li class="w-breadcrumb__item"><a href="<?= $this->getBackendUrl('*/backend/sticker/index') ?>"><?= __('Sticker管理') ?></a></li>
                                    <li class="w-breadcrumb__item" data-state="active"><?= __('详情') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 消息提示 -->
                <div class="w-grid">
                    <template>Weline_Component::message.phtml</template>
                </div>

                <!-- 返回按钮 -->
                <div class="w-grid" style="--w-mb:var(--weline-space-4);">
                    <div style="--w-span:12;">
                        <a href="<?= $this->getBackendUrl('*/backend/sticker/index') ?>" class="w-button" data-tone="neutral">
                            <w:icon name="arrow-left" size="sm"></w:icon> <?= __('返回列表') ?>
                        </a>
                    </div>
                </div>

                <!-- 基本信息 -->
                <div class="w-grid" style="--w-mb:var(--weline-space-4);">
                    <div style="--w-span:12;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <h5 class="w-card__title" style="--w-mb:0;"><?= __('基本信息') ?></h5>
                            </div>
                            <div class="w-card__body">
                                <div class="w-grid">
                                    <div style="--w-span-md:6;">
                                        <p><strong><?= __('目标模块') ?>：</strong>
                                            <span class="w-badge" data-w-background="info"><?= htmlspecialchars($targetModule) ?></span>
                                        </p>
                                        <p><strong><?= __('目标文件') ?>：</strong>
                                            <code><?= htmlspecialchars($targetFile) ?></code>
                                        </p>
                                        <p><strong><?= __('来源模块') ?>：</strong>
                                            <span class="w-badge" data-w-background="success"><?= htmlspecialchars($sourceModule) ?></span>
                                        </p>
                                    </div>
                                    <div style="--w-span-md:6;">
                                        <p><strong><?= __('Sticker文件') ?>：</strong>
                                            <code><?= htmlspecialchars($stickerRelativePath) ?></code>
                                        </p>
                                        <p><strong><?= __('完整路径') ?>：</strong>
                                            <code class="w-text" data-tone="muted"><?= htmlspecialchars($stickerFile) ?></code>
                                        </p>
                                        <p><strong><?= __('规则数量') ?>：</strong>
                                            <span class="w-badge" data-w-background="warning"><?= count($actions) ?></span>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 规则列表 -->
                <div class="w-grid" style="--w-mb:var(--weline-space-4);">
                    <div style="--w-span:12;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <h5 class="w-card__title" style="--w-mb:0;"><?= __('修改规则') ?></h5>
                            </div>
                            <div class="w-card__body">
                                <?php if (empty($actions)): ?>
                                    <p class="w-text" data-tone="muted"><?= __('暂无规则') ?></p>
                                <?php else: ?>
                                    <div class="w-stack" id="rulesAccordion">
                                        <?php foreach ($actions as $index => $action): ?>
                                            <div class="w-disclosure" data-w-component="disclosure">
                                                <h2>
                                                    <button class="w-disclosure__trigger <?= $index !== 0 ? 'collapsed' : '' ?>" type="button" data-w-target="#rule<?= $index ?>" data-w-action="disclosure.toggle" data-w-disclosure-trigger aria-expanded="false">
                                                        <span class="action-badge action-<?= $action['type'] ?>">
                                                            <?= strtoupper($action['type']) ?>
                                                        </span>
                                                        <span class="ms-2">
                                                            <?= __('位置：%{1}', [$action['position'] ?? 'all']) ?>
                                                        </span>
                                                    </button>
                                                </h2>
                                                <div id="rule<?= $index ?>" class="w-disclosure__panel <?= $index === 0 ? 'show' : '' ?>" data-w-disclosure-panel hidden>
                                                    <div class="w-disclosure__body">
                                                        <div style="--w-mb:var(--weline-space-4);">
                                                            <strong><?= __('目标代码') ?>：</strong>
                                                            <div class="code-block" style="--w-mt:var(--weline-space-2);">
<?= htmlspecialchars(substr($action['target_original'] ?? $action['target'] ?? '', 0, 500)) ?>
                                                            </div>
                                                        </div>
                                                        <div>
                                                            <strong><?= __('修改代码') ?>：</strong>
                                                            <div class="code-block" style="--w-mt:var(--weline-space-2);">
<?= htmlspecialchars(substr($action['code_original'] ?? $action['code'] ?? '', 0, 500)) ?>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        <?php endforeach; ?>
                                    </div>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Sticker 文件内容 -->
                <div class="w-grid" style="--w-mb:var(--weline-space-4);">
                    <div style="--w-span:12;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <h5 class="w-card__title" style="--w-mb:0;"><?= __('Sticker 文件内容') ?></h5>
                            </div>
                            <div class="w-card__body">
                                <?php if (empty($stickerContent)): ?>
                                    <p class="w-text" data-tone="muted"><?= __('文件不存在或为空') ?></p>
                                <?php else: ?>
                                    <div class="code-block">
<?= htmlspecialchars($stickerContent) ?>
                                    </div>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 源文件内容 -->
                <div class="w-grid" style="--w-mb:var(--weline-space-4);">
                    <div style="--w-span:12;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <h5 class="w-card__title" style="--w-mb:0;"><?= __('源文件内容') ?></h5>
                                <?php if ($sourceFilePath): ?>
                                    <small class="w-text" data-tone="muted"><?= htmlspecialchars($sourceFilePath) ?></small>
                                <?php endif; ?>
                            </div>
                            <div class="w-card__body">
                                <?php if (empty($sourceContent)): ?>
                                    <p class="w-text" data-tone="muted"><?= __('源文件不存在或为空') ?></p>
                                <?php else: ?>
                                    <div class="code-block">
<?= htmlspecialchars($sourceContent) ?>
                                    </div>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
