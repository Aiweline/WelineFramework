# Weline_WlsDemoPlugin

Minimal WLS Panel marketplace demo plugin.

This module intentionally keeps runtime behavior small. Its purpose is to
prove that a marketplace package with `module:wls` and
`custom:wls-demo-plugin` can be discovered through WLS Panel and installed
through the AppStore package flow.
