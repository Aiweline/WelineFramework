## 2026-09-21 — 货币页 Extra 挂 price ns（1.0.13）

- `/currency` FPC 读指纹增加 `global/storefront/price`，与货币保存 bump 对齐。
- 版本：`1.0.12` → `1.0.13`。

## 2026-09-21 — 货币说明页 @Extra FPC（1.0.12）

- `/currency` 声明共享 FPC（ns=`website/default/theme`）。
- 版本：`1.0.11` → `1.0.12`。

## Fix — 2026-09-18：进程复位清汇率定义缓存（1.0.6）

- `process_cache_resetter.Weline_Currency` 增补 `CurrencyRateService::invalidateCachedDefinitions()`。
- 版本：`1.0.5` → `1.0.6`。

## Fix — 2026-09-18：SSE 进度窗成功/失败统计对比度（1.0.11）

- 审图：`ui_shot` — 成功/失败块白字叠浅绿/浅红，几乎不可读。
- 根因：`data-w-background="success|danger"`（浅底）+ `data-tone="inverse"`（按实心底白字）语义冲突。
- 修：与「总计」同边框卡片；数字/标签用 `data-tone="success|danger"`，去掉浅底+inverse。

## Fix — 2026-09-18：预览路由未注册导致弹窗空表（1.0.10）

- 审图：点「我已了解」弹出对照窗，但表为空、错误 `Not Found`。
- 根因：`previewBaseCurrencyChange` / `applyBaseCurrencyChangeStream` 未写入 `generated/routers/backend_pc.php`（此前全量 `s:up` 卡住）。
- 修：补跑 `setup:upgrade --stage=route_update --module=Weline_Currency`；预览改 XHR；摘要占位符改为前端字面模板替换；验收 CNY→USD 弹窗 11 行 + SSE 100% 写入 11 币。

## Fix — 2026-09-18：基准币确认弹窗 + SSE 进度（1.0.9）

- 审图：点「我已了解，确认切换」无弹窗。
- 根因：JS 用 `form.needs-validation`，实际表单是 `w-form`，监听未绑定。
- 修：`resolveForm()`；预览弹窗可靠打开；新增 `applyBaseCurrencyChangeStream` SSE，进度窗显示百分比 + 逐币日志。

## Fix — 2026-09-18：货币配置「基准货币」贴边（1.0.8）

- 审图：`ui_shot` — 基准货币行顶在侧栏分隔线。
- 根因：Cron 区块多关了一个 `</div>`，把后续字段挤出 `w-card__body`，失去卡片内边距。
- 修：删除多余闭合标签；基准货币 / 最后导入时间包 `w-field`。

## Feature — 2026-09-18：切换基准币先预览对照再确认写入（1.0.7）

- 流程：`previewBaseCurrencyChange` 只读算出各币「原含义/原 rate → 新含义/新 rate」；弹窗展示后，客户点「确认并应用」才带 `confirm_base_currency_apply` 保存并 `recalculateRatesForNewBase`。
- 取消恢复原基准选择，不写库。
- 验：CLI 预览 CNY→USD 含 EUR `7.6923 → 1.1462` 与含义文案。

## Fix — 2026-09-18：手动模式切换基准币也按已有汇率重算（1.0.6）

- 需求：手动模式各币已有 `rate` 比率，切换基准币应与自动模式一样可换算。
- 变更：保存配置 / `postUpdateRates` 不再因 `manual` 拦截基准币重算；`CurrencyImportService` 构造改为 `new`（避免接口 DI 失败）；API 兜底取新基准汇率时取倒数对齐存储语义。
- 文案：后台提示改为「按已有汇率比例换算到新基准」。
- 验证：手动模式下 `recalculateRatesForNewBase(CNY→USD)` 得到 EUR≈1.1462；再 USD→CNY 往返（`decimal(10,4)` 舍入误差约 1e-4 量级）。

## Ops — 2026-09-18：欧美货币补齐 + exchangerate-api 官方汇率（1.0.5）

- 默认站启用：`CNY,USD,EUR,GBP,CAD,CHF,SEK,NOK,DKK,PLN,MXN`（覆盖欧美主战场 + PayPal 可交易币）。
- 汇率源：`https://api.exchangerate-api.com/v4/latest/CNY`（模块既有 `ExchangeRateApi`）；存储语义仍为「1 外币 = rate CNY」。
- 修复：`CurrencyImportService` 对 API「1 基准 = r 外币」做倒数后再入库（此前会把 `0.149` 当成 USD rate，换算反向）。
- 种子：`CurrencyLocalDescriptionSeed` 补 CAD/CHF/SEK/NOK/DKK/PLN/MXN 中英名称。
- 本机抽检：`100 CNY → ~14.9 USD / ~13.0 EUR / ~11.1 GBP`（与当日 API 一致）。

## Fix — 2026-09-18：货币目录进程级缓存（1.0.4）

- `CurrencyCatalog::active()` 结果进程静态复用；保存/删除货币与 `process_cache_resetter.Weline_Currency` 清空。
- 版本：`1.0.3` → `1.0.4`。

## Fix — 2026-09-18：店面价格优先显示货币符号（1.0.3）

- 现象：商品卡 / 结账把 `USD`、`GBP` 当成价格前缀大字打印，看起来像货币号而不是金额。
- 变更：新增 `CurrencySymbol`（库内符号 → 常用字形表 → ICU）；金额展示优先 `$` / `£` / `€` / `¥`。
- 版本：`1.0.2` → `1.0.3`。

## Fix — 2026-09-18：GBP rate=0 导致店面全 0 / 不可买（1.0.2）

- 根因：启用 GBP 但 `rate=0`；`tryConvert` fail-closed → `unit_price_minor=0` / `sellable=false`；改汇率后定义缓存与 catalog offers 未可靠失效。
- 运维：本机 GBP `rate` 设为 `9.0`（1 GBP = 9 CNY）。
- 变更：`CurrencyRateService::invalidateCachedDefinitions`；`Currency::save_after` 顺带 `notifyCatalogChanged`；切换器隐藏 `rate<=0`（保留基准币）；种子 EUR/GBP 默认有汇率。
- 验证：GBP PDP 价格非零可买；相关 UT。

## Fix — 2026-09-09：展示币真换算，禁止只改符号

- 根因：`CurrencyRateService::convert()` 在目标币 `rate<=0` 时静默返回原金额；购物车 `presentLine` 仍强制贴展示币标签。
- 变更：新增 `tryConvert(): ?float`；`convert()` 缺汇率抛错；`format()` / 购物车 snapshot / `convertPriceMinor` 走可失败路径，不再对未换算金额贴目标币。
- 运维：前台启用 EUR/GBP 前须在后台配置 `rate>0`（相对基准币，如 `1 EUR = 7.8 CNY`）。

## Fix — 2026-09-03：货币指南页版心对齐全站 .w-container

- 确认：`.w-currency-guide` 使用 `max-width: var(--weline-layout-content-max-width, 72rem)` 另起版心；页面落在 Theme `default` 的 `.w-container` 内会双重缩进。
- 变更：改为 `width/max-width:100%` 填满父版心，去掉水平 padding；CSS 戳 `20260903-content-width1`。

## Hotfix — 2026-08-31：货币政策页 + 页脚部件

- 当前状态：`implemented`
- 变更：新增前台 `/currency`（支持货币列表与汇率政策）；`footer-currency-rates-link` 默认注入 `footer-payment-account-links`。
- 版本：`1.0.0` → `1.0.1`。

<!-- weline:module-development-baseline:generated-once -->
# Weline_Currency 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 2026-08-29 — 货币路径优先 + Query 回落

- 当前状态：`fixed`
- `currency.js`：`getCurrentCurrency` 为 **path → query → config**；`writeCurrencyPreference` 只清不写 `WELINE_USER_CURRENCY*`；切换器仍路径导航，硬默认 `CNY`。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.0`（文档基线） | docs_handoff | `REQ-CURRENCY-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.0.0` — 文档制度启用基线

- 基线版本：`1.0.0`
- 目标版本：`1.0.0`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-CURRENCY-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-CURRENCY-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-28 | implementing | bugfix | 网站编辑高级选项货币选择无可见弹层；`w_currency` 空表；CSS 未绑定 `--w-floating-top/left` 导致定位反馈发散 | 对齐 language-select floating CSS；种子在空目录时创建货币行；Taglib 空目录回退；图标改用 `cash` | Codex | `currency-select.css` / `CurrencyLocalDescriptionSeed` / `CurrencySelect.php` |
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-CURRENCY-0001` / `1.0.0` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
