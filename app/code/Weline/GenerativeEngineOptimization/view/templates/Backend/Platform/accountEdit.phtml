<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */
$themeConfig = \Weline\Framework\Manager\ObjectManager::getInstance(\Weline\Backend\Block\ThemeConfig::class);

use Weline\GenerativeEngineOptimization\Model\Platform;
use Weline\GenerativeEngineOptimization\Model\PlatformAccount;

$platform = $this->getData('platform') ?? null;
$account = $this->getData('account') ?? ObjectManager::getInstance(PlatformAccount::class);
$backendBase = $this->getBackendUrl('generativeengineoptimization/backend/platform');
?>
<!DOCTYPE html>
<html lang="zh-CN" <?= ('rtl' === $themeConfig->getThemeModel()) ? ' dir="rtl"' : '' ?>>
<head>
    @template(Weline_Admin::common/head.phtml)
    <title><?= __('编辑账户') ?></title>
</head>
<body>
@template(Weline_Admin::common/body.phtml)
<div class="main-content">
    <div class="page-content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                        <h4 class="mb-sm-0"><?= $account->getId() ? __('编辑账户') : __('添加账户') ?></h4>
                        <div class="page-title-right">
                            <ol class="breadcrumb m-0">
                                <li class="breadcrumb-item"><a href="<?= $this->getBackendUrl() ?>"><?= __('首页') ?></a></li>
                                <li class="breadcrumb-item"><a href="<?= $backendBase ?>"><?= __('平台管理') ?></a></li>
                                <li class="breadcrumb-item active"><?= __('编辑账户') ?></li>
                            </ol>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="card-title mb-0"><?= $account->getId() ? __('编辑账户') : __('添加账户') ?></h5>
                        </div>
                        <div class="card-body">
                            <form method="post" action="<?= $backendBase ?>/accountSave">
                                <input type="hidden" name="id" value="<?= $account->getId() ?>">
                                <input type="hidden" name="platform_id" value="<?= $platform ? $platform->getId() : 0 ?>">
                                
                                <div class="mb-3">
                                    <label class="form-label"><?= __('账户名称') ?> <span class="text-danger">*</span></label>
                                    <input type="text" class="form-control" name="account_name" 
                                           value="<?= htmlspecialchars($account->getData(PlatformAccount::fields_ACCOUNT_NAME)) ?>" required>
                                </div>
                                
                                <div class="mb-3">
                                    <label class="form-label"><?= __('API密钥') ?></label>
                                    <input type="password" class="form-control" name="api_key" 
                                           placeholder="<?= $account->getId() ? __('留空则不修改') : __('请输入API密钥') ?>">
                                    <input type="hidden" name="api_key_encrypted" value="1">
                                    <small class="form-text text-muted"><?= __('API密钥将被加密存储') ?></small>
                                </div>
                                
                                <div class="mb-3">
                                    <label class="form-label"><?= __('API密钥（Secret）') ?></label>
                                    <input type="password" class="form-control" name="api_secret" 
                                           placeholder="<?= $account->getId() ? __('留空则不修改') : __('如果需要，请输入API Secret') ?>">
                                    <input type="hidden" name="api_secret_encrypted" value="1">
                                </div>
                                
                                <div class="mb-3">
                                    <label class="form-label"><?= __('是否默认账户') ?></label>
                                    <div class="form-check form-switch">
                                        <input class="form-check-input" type="checkbox" name="is_default" value="1" 
                                               <?= $account->isDefault() ? 'checked' : '' ?>>
                                        <label class="form-check-label"><?= __('设为默认') ?></label>
                                    </div>
                                </div>
                                
                                <div class="mb-3">
                                    <label class="form-label"><?= __('是否激活') ?></label>
                                    <div class="form-check form-switch">
                                        <input class="form-check-input" type="checkbox" name="is_active" value="1" 
                                               <?= $account->isActive() ? 'checked' : '' ?>>
                                        <label class="form-check-label"><?= __('激活') ?></label>
                                    </div>
                                </div>
                                
                                <div class="mb-3">
                                    <button type="submit" class="btn btn-primary"><?= __('保存') ?></button>
                                    <a href="<?= $backendBase ?>/account?platform_id=<?= $platform ? $platform->getId() : 0 ?>" class="btn btn-secondary"><?= __('返回') ?></a>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@template(Weline_Admin::common/footer.phtml)
</body>
</html>

