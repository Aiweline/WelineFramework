<?php
/**
 * Image fallback is declared once on Theme layouts/base/body-end via data-weline-load.
 *
 * @hook-priority 45
 * @hook-sort-order 45
 */
$isBackend = str_contains(strtolower((string)($_SERVER['REQUEST_URI'] ?? '')), '/admin');
if ($isBackend) {
    return;
}
// Intentionally empty: storefrontImageFallback loaded by Theme body-end attribute declare.
?>
