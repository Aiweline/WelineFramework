<?php
/**
 * Header account dropdown: My Quotes (signed-in shell only).
 *
 * Hook: header-account-links
 *
 * @hook-priority 48
 * @hook-sort-order 8
 * @hook-solo false
 *
 * Hidden until account JS promotes the header to signed-in.
 * Badge count is painted by Weline_Frontend; SSR keeps empty shell.
 */

use Weline\Product\Model\ProductQuoteRequest;
use Weline\Theme\Helper\WidgetI18n;

$section = ProductQuoteRequest::ACCOUNT_SECTION;
$signal = ProductQuoteRequest::MENU_SIGNAL_CODE;
$label = WidgetI18n::label('我的询价');
?>
<li role="none" data-account-menu-auth="signed-in" hidden>
    <a href="@url{'customer/account/index'}#<?= htmlspecialchars($section, ENT_QUOTES, 'UTF-8') ?>"
       role="menuitem"
       data-account-menu-signal="<?= htmlspecialchars($signal, ENT_QUOTES, 'UTF-8') ?>">
        <?= htmlspecialchars($label, ENT_QUOTES, 'UTF-8') ?>
        <span class="account-menu-signal-badge" data-account-menu-signal-badge="1" hidden aria-hidden="true"></span>
    </a>
</li>
