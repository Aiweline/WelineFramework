<?php
/**
 * @deprecated `textile-heritage` is now a Weline_Theme native widget.
 *             This view delegates persisted Product widget instances to the
 *             Theme renderer and can be removed after layout migration.
 *
 * @var \Weline\Framework\View\Template $this
 *
 * @widget.code {textile-heritage}
 * @widget.name {织艺谱系（兼容）}
 * @widget.description {兼容旧布局；真实部件由 Theme 提供}
 * @widget.type {content}
 * @widget.area {frontend}
 * @widget.position {["content"]}
 * @widget.page_layouts {["homepage", "cms_page"]}
 * @widget.supports {["layout-homepage-brands","layout-cms-content","layout-default-content","brand-list","brands"]}
 */

$widgetData = $this->getData();
echo $this->fetch(
    'Weline_Theme::theme/frontend/widgets/content/textile-heritage/default.phtml',
    is_array($widgetData) ? $widgetData : []
);
