## 2026-09-12 — 商品编辑 offers-after 扩展点（1.0.232）

- 增：Hook `Weline_Product::backend::catalog::edit::offers-after` + `doc/hook/backend/catalog/edit/offers-after.md`；`edit.phtml` offers 面板末尾挂载。
- 用途：B2B 等模块注入批发启用与阶梯价编辑面；不自建第二套价表。
- 契约：`ProductWholesaleAdminHookContractTest`。

## 2026-09-12 — 商品目录来源过滤（1.0.231）

- 增：筛选栏 `source` 下拉（全部来源 / 无来源 / 当前列表出现的平台）；`ProductAdminReadService` 按 `source_platform` 与 `source_label` 过滤。
- 契约：`ProductAdminSurfaceContractTest`、`ProductAdminReadServiceBatchSearchTest::testSourcePlatformFilterMatchesPlatformLabelAndNone`。
- e2e：`catalog-source-filter.spec.js`。

## 2026-09-12 — 分类对客不露货源壳

- 增：`StorefrontCategoryPublicFilter`；PDP/分类面包屑跳过 `sourcing` / `sourcing/{provider}` 与货源品牌名。
- 改：壳不再创建直白顶层分类；树行仅保留供应商 code 标签。

## 2026-09-12 — 分类树徽章只显示本级 code

- 现象：CJ/货源导入后树行徽章刷满 `/sourcing/cj/...` 长路径，与缩进分级重复。
- 改：`categories.phtml` 树徽章只渲染 leaf `code`（完整 path 放 title）；有 `source_platform` 仍显示来源徽标。
- 契约：`ProductLayoutAdminPanelContractTest::testCategoryDefaultLayoutPanelExists`。

## 2026-09-12 — 非法 UUID 批量删除改物理清理（1.0.230）

- 现象：`E2E-DELPROD` / `probe-*` 等坏 `global_product_uuid` 多选删除「成功 0，失败 N」（`product_admin_product_uuid_invalid`）。
- 修：`ProductAdminBulkService` 对 `archive` + 非法 UUID + `product_id` 回退 `ProductPhysicalDeleteService::deleteByIds`；`bulkArchive` items 携带 `product_id`。
- 契约：`ProductAdminBulkInvalidUuidPurgeContractTest`。

## 2026-09-12 — 目录批量删除：链式归档 + 幂等（1.0.229）

- 现象：多选批量「删除」常失败——`published → archived` 非法；已归档再删报 `archived → archived`；接口 `success=true` 但 `failed` 全失败时前端仍 toast 成功。
- 修：`ProductAdminCommandService::lifecycleStepsToward` 对归档展开 `published→disabled→archived`；已归档幂等成功；`bulkArchive` 按 `data.failed/succeeded` 反馈；默认目录列表（无状态筛选）隐藏已归档。
- 契约：`ProductAdminArchiveTransitionContractTest`。

## 2026-09-12 — 目录批量删除改用主题确认框

- 现象：商品目录点「删除」弹出浏览器原生 `confirm`（带域名标题）。
- 修：`product-admin.js` 增加 `confirmTheme`，`bulkArchive` 改调 `Weline.UI.dialog.confirm`（不可用回退原生）；`catalog/index.phtml` bump `?v=20260912-theme-confirm`。
- 契约：`ProductAdminSurfaceContractTest::testCatalogListExposesBulkSelectionAndMediaColumns` 锁定主题确认通道。

## 2026-09-12 — 万能产品列表容错坏 UUID（1.0.228）

- 现象：后台「万能产品」整页红条「商品目录读取失败：uuid_invalid」，筛选商品 0。
- 根因：`ProductAdminReadService::search` 对每行 `resolveProductByUuid` 严格校验；分片表任一坏 `global_product_uuid` 即整页 503。
- 修：列表改 `resolveProductsByUuids(strict:false)` 批量容错，坏 UUID 回退分片字段；`searchSelectionRows` 仍 `strict:true`。
- 验收：`ProductAdminReadServiceBatchSearchTest` OK（含 fail-soft 用例）。

## 2026-09-11 — 货源属性集 + 分类来源标记（1.0.225）

- 增：`ProductCatalogEavBootstrap::ensureDropshipSchema()`（属性集 `dropship` / 货源商品）。
- 增：分类 EAV `source_platform`；分类树「来源」徽章；商品列表「来源」列。
- 版本：`1.0.224` → `1.0.225`。

## 2026-09-11 — 商品编辑/新增 Hook：B2B 批发开关注入点（1.0.224）

- 新增 Hook：`Weline_Product::backend::catalog::edit::basic-after` / `create::basic-after`（编辑基础信息末尾、创建向导交易库存后）。
- 文档：`doc/hook/backend/catalog/edit|create/basic-after.md`；模板 `getHook`；B2B 经 slot 注入「启用批发」与价目入口，Product 不自建阶梯价表。
- `product-admin.js`：`mergeWholesaleSellingModeFlag` 把 `selling_mode_tob` 并入编辑/创建 attributes。

## 2026-09-11 — 产品卡标题显示工厂 SKU（审图）

- 问题：中文店面产品网格标题显示 `HUACHAOJI-…` / `YUEYANICHANG-4D375D6D-…` 等编码，按钮仍为「加入购物车」。
- 根因：Catalog/Category 默认 `includeListingDetails=false` 时 `buildPublishedOffers` 把 `$attributeRows=[]` 传给快照；`??=` 视空数组为已提供，跳过最小 name EAV 加载，名称回退到 `product.sku`。
- 修复：summary 路径传 `null`；`resolveCatalogOffers` 将 `[]` 归一为 `null` 后再加载 name/product_type/quote_only。
- 证据：product 113 的 `zh_Hans_CN` name 为中文标题，summary 修复后 32/32 卡片标题非工厂码；合同测试锁定 null/空数组行为。列表 `show-sku` 仍按既有合同展示 SKU 行（与标题分离）。

## 2026-09-11 — API 文档演示商品下架

- 目标：将「API 文档演示商品」（`product_id=541`，SKU `API-DEMO-fff28a3a-…`，uuid `4fe1999c-745a-54e1-a37e-beec2fc31787`）从 `published` 正式下架。
- 操作：经 `ProductAdminCommandInterface::ACTION_DISABLE` 同步禁用 Website 投影、Offer `10017` 与全局 Identity。
- 结果：product/identity/offer 均为 `disabled`；店面 `/product/541` 重定向至 `/products/`。

## 2026-09-11 — API 文档演示商品下架

- 目标：将「API 文档演示商品」（`product_id=541`，SKU `API-DEMO-fff28a3a-…`，uuid `4fe1999c-745a-54e1-a37e-beec2fc31787`）从 `published` 正式下架。
- 操作：经 `ProductAdminCommandInterface::ACTION_DISABLE` 同步禁用 Website 投影、Offer `10017` 与全局 Identity。
- 结果：product/identity/offer 均为 `disabled`；店面 `/product/541` 重定向至 `/products/`。

## 2026-09-11 — Offer 搜索路由 + SearchSelect 防风暴

- 补齐 `OfferSearch` 进 `generated/routers/backend_pc.php`（此前控制器在盘、路由未注册 → 404）。
- `theme:search-select`：失败写 `cache=[]`、`searchInFlight`、非 2xx 不解析 JSON，避免 open 反复 `doSearch('')` 请求风暴。
- 探活：`offer-search/search` 200；Browser 点 Offer 仅 1 次请求且下拉有结果。

## 2026-09-11 — Offer 搜索路由 + SearchSelect 防风暴

- 补齐 `OfferSearch` 进 `generated/routers/backend_pc.php`（此前控制器在盘、路由未注册 → 404）。
- `theme:search-select`：失败写 `cache=[]`、`searchInFlight`、非 2xx 不解析 JSON，避免 open 反复 `doSearch('')` 请求风暴。
- 探活：`offer-search/search` 200；Browser 点 Offer 仅 1 次请求且下拉有结果。

## 2026-09-11 — Offer 选择 Taglib（1.0.223）

- 新增 `<w:product:offer:select>` + `OfferSelectSearchService` + `OfferSearch` JSON，供库存调整等后台按站搜索 Offer ID/SKU。
- 版本：`1.0.222` → `1.0.223`

## 2026-09-09 — PDP 对接 Weline_Faq product-faq 槽

- Theme product 布局在 reviews/tabs 间提供空槽 `product-faq`；默认部件由 Faq `default_injections` + Setup `publishLayout` 注入。
- UUID 契约：offer 入参优先，落库 `globalProductUuid`（见 `ProductFaqTypeProvider`）。
- 与 `product-tabs`/`product-qa` 分离：tabs 不默认注入 Faq。

## 2026-09-09 — 询价验证码改 FPC lazy host（1.0.221）

- 根因：`ProductQuoteRequestCaptchaGuard::renderChallenge` SSR Google HTML 进产品页壳，FPC 会冻住空/旧 site key → Google「需要网站密钥」。
- 变更：改 `LazyCaptchaClientRuntime::hostMarkup`，挑战仅经 `/weline_captcha/frontend/challenge` 动态拉取。
- 版本：`1.0.220` → `1.0.221`

## 2026-09-09 — 商品 changed 刷详情 FPC + 购买槽长期根因（1.0.220）

- `product_search_projection` `w_changed` 补 `impact.namespaces=website/{code}/catalog`；`afterCommit` → `notifyCatalogChanged` + `ProductStorefrontCacheInvalidator`
- 购买槽长期：Cart/Checkout Theme 注入控件改为始终输出可解锁 CTA（另包）
- 版本：`1.0.219` → `1.0.220`

## 2026-09-09 — 仅剩 N 件却「暂不可售」无法购买（1.0.219）

- 审图分流：web_ui（PDP「仅剩 1 件」+「当前规格暂不可售」、无加购/结账）
- 非最低库存：`InventoryService` `sellable: available > 0`；该规格 stock=1 且可售
- 根因：Theme 购买槽注入的 Cart/Checkout 控件在 widget 数据袋无 offer 时 early-return → 槽空；数量行仅 SSR sellable 才渲染；配送文案不随规格切换
- 修：父级 offer failsafe CTA（槽空则 JS 移入）；数量行始终渲染可 hidden；`renderPurchaseButtons` 同步配送/数量/按钮文案与启用
- 版本：`1.0.218` → `1.0.219`

## 2026-09-09 — 库存「仅剩 件」缺数字（1.0.218）

- 审图分流：web_ui（加购/详情 buybox「仅剩 件」红箭头；缺数量）
- fail（E）：`renderStock` 用 `__('仅剩 %{1} 件')` 无参；`__()` 默认 `args=''` 把 `%{1}` 吃空，JS `.replace('%{1}', stock)` 失效
- 修：改为 `__('仅剩 %{1} 件', ['%{1}'])` 保留占位；契约断言
- 版本：`1.0.217` → `1.0.218`

## 2026-09-09 — API 演示商品 541 店面 enrichment（数据）

- 现象：`/product/541` 空荡（仅 name、库存 0、无描述/图廊）——`API-DEMO-*` 壳商品。
- 变更：补 short/description/meta、库存 on_hand、多图廊；PDP 可见有货与购买区。

## 2026-09-09 — 规格选项英文 Local 乱码与错误机翻（1.0.217）

- 审图分流：web_ui（加购/规格轴 Type；en_US 出现 `%E5…` 与 Full Cou/Mantingf 错译）
- 根因：Option `en_US` LocalDescription 混入 percent-encoded 脏数据与错误 AI 译文
- 修：`StorefrontEavLabelResolver::usableOptionLabel` 拒绝编码当地名并回退源文；PurchasePanel 对编码 label 强制重解析；`hanfu-eav-en-locals` 补满庭芳系列；回填 Local
- 版本：`1.0.216` → `1.0.217`

## 2026-09-09 — 英文购物车优惠主题与选项 URL 乱码（1.0.216）

- 审图分流：web_ui（购物车行选项；en_US「优惠主题」未译、Type `%E5…`）
- 修：`presentThemeOption` 经 `TranslationResolver` + `Weline_Product` CSV；`StorefrontEavLabelResolver` 对 percent-encoded option token 解码后再 resolve/展示
- 版本：`1.0.215` → `1.0.216`

## 2026-09-09 — 恢复 product.getProductByIds QueryProvider（1.0.215）

- 问题：WeShop `ProductQueryProvider` 删除后，Affiliate 等仍调 `w_query('product','getProductByIds')` →「商品分享目标不可用」
- 修：新增精简 `ProductQueryProvider`（provider=`product`），用 `StorefrontCatalogViewService::publishedOffersForProductIds` 映射 `product_id/name/sku/handle/slug/status=published`
- 加购弹窗：`data-quick-add` 不 `history.replaceState` 改列表 URL
- 版本：`1.0.214` → `1.0.215`

## 2026-09-09 — 售卖模式槽 fail-soft 模块登记门禁（1.0.214）

- `product-info` 内联 B2B 模板：除 `is_file` 外要求 `Env` 已注册 `Weline_B2B`，避免卸载后误 `fetch`。
- 版本：`1.0.213` → `1.0.214`。

## 2026-09-09 — en_US 店面图 locale 回退（1.0.213）

- 问题：个人中心收藏等走 `publishedOffer` 全量投影时，`en_US` 下 `asset://` 解析为空 → 占位图（资源仅有 `zh_Hans_CN` 元数据且 Website 默认语言未绑定）。
- 变更：`StorefrontProductMediaUrlResolver::localeCandidates` 在默认语言缺失时追加 `zh_Hans_CN`；`resolveReference` 对空 URL 继续尝试下一候选。
- 验证：`StorefrontProductMediaUrlResolverTest::testEnglishRequestFallsBackToChineseMetadataWhenDefaultLanguageMissing`；en_US `publishedOffer` 探活。
- 版本：`1.0.212` → `1.0.213`。

## 2026-09-09 — Offer 配送方案松耦合绑定（1.0.212）

- Offer 分片列 `shipping_profile_code`（SCHEMA_VERSION `4.7.0`）；SPI `StorefrontShippingProfileCatalogProviderInterface` + Registry 空安全。
- 创建/编辑 UI「配送方案」；Cart snapshot 写入 `fulfillment_metadata.shipping_profile_code`。
- PDP：`previewHint`——仅 `is_free_shipping` 强标「包邮」，否则「运费以结算页为准」。
- 版本：`1.0.211` → `1.0.212`。

## 2026-09-09 — 产品详情可见面包屑恢复（1.0.211）

- `product-info` 渲染主路径 `nav[data-testid=product-breadcrumb]`；配合 Seo `breadcrumb_trails` 多条 JSON-LD。
- 版本：`1.0.210` → `1.0.211`。

## 2026-09-09 — 商品详情图补有用 alt（1.0.209）

- 现象：详情正文图常空 alt，SEO 面板/爬虫无法理解内容图。
- 变更：`StorefrontProductMediaUrlResolver::ensureDescriptionImageAlts`；`product-info` 渲染前补「商品名 · n」；规格色块 alt 带商品名。
- 版本：`1.0.208` → `1.0.209`。

## 2026-09-09 — best_sellers 布局补 seo::footer（1.0.208）

- 现象：`/best-sellers` 打开 Weline 面板无 SEO Tab。
- 变更：`view/theme/frontend/layouts/best_sellers/default.phtml` 补 `<w:hook>seo::footer</w:hook>`。
- 版本：`1.0.207` → `1.0.208`。

## 2026-09-09 — 分类进 Sitemap + 描述加厚（1.0.206）

- 新增 `CategorySitemapUrlProvider` / `CategorySitemapUrlService`；分类短 description 自动补全；缺图时写 image_alt。
- 版本：`1.0.205` → `1.0.206`。

## 2026-09-09 — EUR/en_US 商品列表 SEO 补齐（1.0.205）

- 现象：`/EUR/en_US/products` 面板可收录性/可理解性很低。
- 根因：列表缺 `og:image`/`keywords`；英文 title+品牌超 65；`resolveSupported` 不剥货币段导致 Provider 静默。
- 变更：surface 补 share 图/keywords；缩短英文 seo_title；剥 ISO 货币前缀；Catalog 写入 image/keywords。
- 版本：`1.0.204` → `1.0.205`。

## 2026-09-09 — GEO Feed 投影写入（1.0.204）

- `GeoFeedProjectionObserver` 订阅 `Weline_Framework::resource_changed`（product_search_projection）写入 GEO FeedItem。
- 可选依赖增加 `Weline_Geo`。
- 版本：`1.0.203` → `1.0.204`。

## 2026-09-09 — GEO Feed 投影写入（1.0.204）

- `GeoFeedProjectionObserver` 订阅 `Weline_Framework::resource_changed`（product_search_projection）写入 GEO FeedItem。
- 可选依赖增加 `Weline_Geo`。
- 版本：`1.0.203` → `1.0.204`。

## 2026-09-09 — 推荐产品卡片真实评论数（1.0.203）

- 审图：推荐产品卡片 `(261)` 等为 `mapOffer` 假公式
- 修：去掉假评分；可选依赖 `Weline_Review`，`withReviewAggregates` 批量回填真实评分/评论数
- 版本：`1.0.202` → `1.0.203`

## 2026-09-09 — 新品页 RSS 订阅按钮（1.0.202）

- 新品页英雄区标题旁增加「订阅 RSS」outline 按钮 → `/new-arrivals/rss.xml`。
- 版本：`1.0.201` → `1.0.202`。

## 2026-09-08 — 购物车切币 FX 回退（1.0.201）

- 审图：顶栏 USD 购物车仍 CNY；目录常仅存基准币价
- 修：`ProductCatalogCartItemSnapshotResolver` 展示币无显式价时，按 Currency 汇率从基准币换算（cleared 不回退）
- 版本：`1.0.200` → `1.0.201`

## 2026-09-08 — 新品读者 RSS（1.0.200）

- 入口：`/new-arrivals/rss.xml` → `Controller/Frontend/NewArrivalsRss.php`；数据复用 `newArrivalCards(24, 365)`。
- Discovery：新品页 `assign('seo.feeds')` + `CatalogSeoProfileProvider`（surface `new_arrivals`）。
- 版本：`1.0.199` → `1.0.200`。

## 2026-09-08 — 结账优惠主题显示名字（1.0.198）

- 审图分流：web_ui（结账行标签 `promotion_theme_id · 3`）
- 修：`StorefrontCampaignEntry::presentThemeOption`；快照 `buildOptions` / Cart 回退路径显示「优惠主题 · {campaign_label}」，不露字段名与裸 id
- 版本：`1.0.197` → `1.0.198`

## 2026-09-08 — PDP 多活动可选（1.0.195）

- 透传 `eligible_campaigns`；活动标 >1 时下拉选择；Cart `promotion_theme_id` 与 Assembler preferred 同源。
- 版本：`1.0.194` → `1.0.195`

## 2026-09-08 — PDP 内容图补 width/height（SEO CLS，1.0.194）

- 审图分流：附图为 SEO「当前页检测」面板（web_ui / 产品 URL）；扣分「图片缺少 width/height」落在商品详情抽样。
- 根因：`StorefrontProductMediaUrlResolver` sanitize 清属性后未写宽高；`product-info` 画廊主图/缩略图/色卡与动态 thumbs 裸 `<img>`。
- 修复：详情图保留显式宽高否则默认 800×800；画廊主图 800×800、缩略 64×64、色卡 48×48；动态画廊 JS 同步。
- 验收：单测 `StorefrontProductMediaUrlResolverTest`；curl/Browser 产品页内容图带 HTML width/height。
- 版本：`1.0.193` → `1.0.194`

## 2026-09-08 — 快捷加购 identity 回传（1.0.193）

- 变更：`PurchasePanel` JSON 增加 `identity`（登录/批发会员），并 assign 给 product-info 内 selling-mode；避免部件误显「申请批发身份」
- 版本：`1.0.192` → `1.0.193`

## 2026-09-08 — 商品卡/分类横幅补图片尺寸（1.0.189）

- 首页审计「图片缺少 width/height」：product-card 与分类 banner 补 800×800 / 1500×300。
- 版本：`1.0.188` → `1.0.189`。

## 2026-09-08 — 商品 description 补足到 80–170（1.0.188）

- 审计：抽样商品 meta description 约 60–65 字，触发「description 长度不理想」。
- 修复：`Detail` 与 `ProductSeoProfileProvider` 在短描述时追加场景/选购说明，截断至 170。
- 版本：`1.0.187` → `1.0.188`。

## 2026-09-08 — 审图：列表快捷加购面板（1.0.187，收口）

- 分流：web_ui（全部商品列表；箭头指向「加入购物车」；批发模式仅 toast 起订量）
- fail（A/E）：多规格盲加；批发起订量/阶梯价未在选规格前说明
- 修：
  - 列表卡 `data-open-purchase-panel` → `PurchasePanel` API 返回 `product-info` `quick_add` 精简模式（左画廊+规格，不加载详情描述）
  - 面板内 B2B 切换 + `qty-tiers` 起订量/阶梯价；加购读数量选择器
  - 路由补入 `frontend_pc` + WLS reload；Browser：批发数量默认 5/10/15…
- 版本：Product `1.0.187`（面板 API）；后续数量绑定见 B2B `2.6.13` / Cart `1.3.27`

## 2026-09-08 — 促销冷选择批量读取与搜索分类简化投影（1.0.185）

- 实际 `promotion.selection.search` 调用 Product 的 `ProductAdminReadService::searchSelectionRows`，不经过 Search 增量索引。旧选择路径按商品读取 StoreProduct 与权威身份，且状态筛选也读取报价 SKU/名称 EAV。
- 选择读取按既有 200 商品批次调用 `selectionMap`、`resolveProductsByUuids`；仅 SKU 筛选需要 Offer、仅名称筛选需要 EAV。后台完整展示搜索保持原路径，商品筛选顺序、店铺继承及日期/limit 不变。
- 权威身份批量结果按标准小写 UUID 查找；批量接口新增可选 `strict=false`，选择端使用 `strict=true` 保留原单条接口的 `uuid_invalid` 异常，其他批量消费者默认语义不变。
- 搜索分类消费者沿现有 `ProductCatalogQueryConsumer::tree` 获取基础分类数据，不再为只需要 ID/label/params 的下拉选项构建 navTree URL。范围/locale、三层深度和无分类时的原示例回退保持。
- 配置数据库的独立只读 CLI probe：搜索分类 `product.scopes` 从 116 DB / 280.29ms 降至 14 DB / 65.11ms，URL rewrite 查询 102 → 0，仍返回 6 个根分类；同数据库旧/新最终选项严格相等。复测为 14 DB / 109.66ms，墙钟受系统负载影响；确定改进是消除 102 次查询，不代表整页或浏览器时延。
- 行为 RED：205 商品旧实现 StoreProduct 205 次读取、有效身份 204 次读取；修复后每类 2 次。真实临时 SQLite 覆盖店铺排除/继承、另一店铺隔离、权威类型、大小写 UUID、缺失身份回退、非法 UUID；既有促销最强折扣、空筛选与 price_band 用例通过。
- 验证：联合定向回归 29 tests / 158 assertions 通过。促销测试补齐既有 URL 依赖夹具并按 Reflection 核验当前公开签名；不改折扣算法、不添加共享缓存，新增商品仍直接回源批量读取。真实页面总耗时由主任务验收，本文不声称达到冷启动 70ms。
- 版本：保留并行 REST 多语言 `1.0.184`，统一升级至 `1.0.185`。

## 2026-09-08 — 商品候选分批读取与请求报价复用（1.0.181）

- Detail 将选中报价写入框架 Context；独立 slot 与 bundle 复用同一报价，保留未选规格状态，相邻请求不共享；显式指定其他商品的卡片不使用 PDP 报价。
- 分类展示共享键包含实际读取的 locale；website 原始分类事实与 catalog generation 依赖不变，URL 仍在请求中投影。
- 新品候选只读取 product_id/created_at，键包含 cutoff 日期和分页位置；前批没有 Offer 时继续查后批，直到补足窗口内商品或候选结束。
- 小型摘要按代表 Offer 的原有顺序在 SQL 分页，已读 Offer 行传给投影阶段；仅批量读取对应 Product，跳过无效候选后继续补足。已有完整列表投影直接在 Context 中复用。摘要缓存逻辑键升 v2。
- 验证：先以行为 RED 复现缺失报价上下文、显式语言串用、全量读 100 行和新品错误回退；另覆盖代表 Offer 翻页去重。真实临时 SQLite 与内存缓存回归 44 tests / 296 assertions 通过。网站运行与浏览器验收由主任务执行，本文不将单测视作页面验收。
- 保留既有普通推荐/热度排序，不将其描述或实现为真实网站/店铺销量榜。
- 版本：当前工作树 `1.0.180` → `1.0.181`；保留并行任务既有产品价格修复。

## 2026-09-08 — PDP 多活动可选（1.0.194）

- 透传 `eligible_campaigns`；活动标 >1 时下拉选择；Cart `promotion_theme_id` 与 Assembler preferred 同源。
- 版本：`1.0.193` → `1.0.194`

## 2026-09-08 — PDP 多活动可选（1.0.194）

- 透传 `eligible_campaigns`；活动标 >1 时下拉选择；Cart `promotion_theme_id` 与 Assembler preferred 同源。
- 版本：`1.0.193` → `1.0.194`

## 2026-09-08 — PDP live 库存刷新抹掉今日特价（1.0.179）

- 审图：导航「今日特价」进详情只见 ¥218 +「今日精选」，无折后价/划线原价；列表同品为 ¥196.20 / 划线 ¥218。
- 根因：`variant-availability` 返回裸目录价；`mergeLiveOffers` 用 `unit_price_minor` 覆盖 SSR Assembler 特价。
- 变更：live 合并只刷新库存/可售/币种/消息；仅当 live 带 deal 字段时才采纳价格；`renderPrice` 同步 campaign 可见性。
- 验收：`ProductDetailTemplateContractTest`；Browser PDP 显示 196.20 + 划线 218。
- 版本：`1.0.178` → `1.0.179`

## 2026-09-08 — 清理已删 RouteContext 残留（1.0.178）

- 问题：PDP 抛 `Class "Weline\Product\Service\StorefrontProductRouteContext" not found`（1.0.174 已删类，Helper/Widget/cross-sell 与部分契约仍引用）。
- 变更：OfferResolver / WidgetCatalog::bundleCards / cross-sell 只走 template/SEO/Context；契约改为断言无该类。
- 验收：相关契约单测；Browser PDP 不再 Class not found。
- 版本：`1.0.177` → `1.0.178`

## 2026-09-08 — 店面 widget / 分类展示复用（1.0.177）

- 分类本地化展示升 `categoryLocalizedPresentationPolicy`（website+lang）；URL 仍请求内生成。
- 新品：`listRecentPublishedCreatedAt` + `newArrivalCandidatesPolicy`，去掉 `listAll`。
- 推荐/热销冷路径改 `publishedOfferSummaries`；PDP bundle/cross-sell 优先已解析 offer。
- 验收：`StorefrontProductWidgetCatalogReuseContractTest`。
- 版本：`1.0.176` → `1.0.177`

## 2026-09-08 — 编辑详情 WYSIWYG 解析 asset:// 预览（1.0.175）

- 问题：详情 HTML 以 `asset://` 存图，CKEditor 能挂载但图片 0×0，看起来像「没有详情」。
- 变更：`ProductAdminMediaPresenter::presentDescriptionForEditor` 换成可访问预览 URL + `data-weline-asset-id`/映射表；保存经 JS/`persistDescriptionHtml` 写回 `asset://`。
- 验收：`ProductAdminMediaPresenterTest`；Browser 详情区图片 naturalWidth>0。
- 版本：`1.0.174` → `1.0.175`

## 2026-09-08 — PDP 身份直接挂请求 Context（1.0.174）

- 去掉自造的 `StorefrontProductRouteContext`；Router/Detail 只写框架 `Context::input.query.id|slug`。
- Detail 解析后用 `carryResolvedIdentity()` 把 id 与 canonical slug 一并写回 Request/Context。
- Offer/Variant 解析器只读 Context / Request / SEO bag，不再二次拆 URL。
- Theme 对 `/product?id|slug` 继续让路。

## 2026-09-08 — 详情描述去双显 + 官方所见即所得（1.0.173）

- 问题：`<local>` 把整段 HTML 当触发按钮文案，与下方 textarea 双显；且为纯文本框。
- 修复：local 正文改为短标签；补齐 `w-product-local-label__trigger` CSS；`<w:editor-manager>` 挂载官方 CKEditor。顺带修 EditorManager 默认配置按 key 读取、Backend CLI `getDefaultConfigForKey`、CKEditor 适配器 `context.element`。
- 验收：契约 + Browser 基础信息无双显 HTML，可见 CKEditor 工具栏。
- 版本：`1.0.172` → `1.0.173`

## 2026-09-08 — 编辑页基础信息补齐详情描述多语言（1.0.172）

- 问题：创建向导有「详情描述」，编辑页「基础信息」仅名称/短描述/SEO，后台无法改 PDP 长详情与多语言。
- 变更：`LocalDescription` 增加 `description`；`writeAttributes` 保存并同步 Local↔EAV；`edit.phtml` 基础信息增加详情描述 + `<local>`；`editorPayload` 提交 `description`；EAV 编辑器 JSON 排除 `description` 避免双入口。
- 验收：契约 `ProductAdminSurfaceContractTest`；Browser 编辑页可见「详情描述」。
- 版本：`1.0.171` → `1.0.172`

## 2026-09-08 — 审图：商品卡两种垂直间距（1.0.171）

- 问题：附图价区与「加入购物车」空隙明显大于双按钮间距；根因是 `category/index.phtml` 把分页器 `margin:18px` 串进购买按钮选择器，叠加价区 `margin-top:auto`。
- 变更：拆开 PLP 按钮/分页 CSS；吸底改到 `.wpc-cta`；正文/按钮统一 `space-2`；「今日精选」改为芯片；原型 `product-card-vertical-rhythm.html`；契约 `ProductCardVerticalRhythmContractTest`。
- 版本：`1.0.170` → `1.0.171`

## 2026-09-08 — 商品卡促销标签 class 泄漏修复（1.0.170）

- 根因：`wpc-badges` 泄漏到 `.w-product-label`，布局选择器误伤导致胶囊宽度塌缩。
- 变更：`product-card.css` 仅对 `.w-product-labels` 定位，并为 `.w-product-label` 增加 space-3 包裹规则；契约 `ProductCardLabelWrapContractTest`（配合 Theme `labels.phtml` 清空 class）。
- 版本：`1.0.169` → `1.0.170`

## 2026-09-08 — REST 多语言创建、增量编辑与翻译准备（首版 1.0.176，复验 1.0.184）

- 关联需求：`REQ-PRODUCT-0044`。外部应用可按请求语言修改文案、批量提交多语言，或经已配置渠道准备全部译文后创建和编辑商品。
- 实现前真实复现：创建请求带中文 `locale` 和英文 `translations`，返回 201，但仅写入默认语言中文值，英文没有保存。
- 实现：REST 层将六类文案及 EAV 属性归入明确语言；公共商品字段继续走原命令。新建源文初始化默认回退，后续语言编辑抑制默认镜像；所有语言在一次商品事务中写入，保存沿用一次版本更新。店铺覆盖不反写全局本地化表。
- 自动翻译：调用已有公开 Query，只使用本次提交的源文，手工译文优先；全部译文准备好后进入商品事务。自动目标店铺继承已合并源语言组，保留目标显式店铺。渠道失败或不完整结果以 `product_api_translation_failed` 返回，不开始商品写入。
- 读取：保留原管理快照及全部原始属性行，增加目标语言 `content` 和各语言 `translations`；明确空描述不被默认值覆盖。
- 运行修复：真实保存触发的搜索增量队列事务冲突已复验恢复；WLS 请求头原名匹配不会把 `ACCEPT_LANGUAGE` 转为 `Accept-Language`，REST 现从公开 `ServerBag` 读取标准请求头。查询参数与请求体优先级保留，临时追踪已移除；历史失败与最终复验见 [多语言验收证据](rest-api-multilingual-acceptance.json)。
- 开发验证：Product REST 定向单测 11 tests / 58 assertions 已通过；这属于开发证据，不替代真实商品保存和浏览器页面操作。

Product REST 定向单测 11 tests / 58 assertions 已通过。多语言创建、请求体 locale 与请求头语言编辑、一次保存多个语言、自定义属性语言、409/400 回滚，以及无渠道自动创建和编辑返回 502 且不写入，已通过真实 HTTP 验证。最终主阶段为 13 次请求、47/47 项断言；手工译文配合 translate_to 的优先覆盖补充为 4 次请求、15/15 项断言；请求头选择及查询参数优先的两个只读复验也通过。 官方路由升级完成后，语言选择、手工译文持久化及文档页共 4/4 项回读通过。 环境翻译渠道记录为 0，真实自动翻译成功仍待配置后验收。API 文档页已恢复 HTTP 200、`text/html`、1,165,919 字节；独立 fresh Weline Chrome 新任务标签页 `goto` 30 秒超时或返回 `Debugger unattached`，另一入口创建内置浏览器立即返回 `Browser is not available: iab`；均未在页面 UI 发请求，在线调用尚未验收。 验收商品 324、339、340 已归档，最终版本分别为 1、9、2；应用 3、4 及令牌已撤销，旧令牌实测 HTTP 401，私有验收凭据文件已删除。

- 文档：本次同步 README、REST 接入指南、多语言指南、脱敏多语言验收证据、功能现状、开发日志与需求索引；原 1.0.166 的 16 次 REST HTTP 历史证据继续保留，本条不将其计为多语言验收。

## 2026-09-08 — 外部应用产品 REST 创建与编辑（首版 1.0.166）

- 新增：`Api/Rest/V1/Products.php` 提供 POST create、PUT edit、GET detail；`ProductRestInput` 将 JSON 适配到既有产品管理命令，增量编辑保留未提交字段。
- 授权：复用 Weline_Api 的应用安装/token，以及独立创建、编辑、读取 scope；global 安装显式传入 Website，应用不伪装为后台管理员。
- 验收：配置 WLS HTTPS 完成 16 次调用，真实产品 #322 创建 201、编辑/回读 200，版本 0→1，名称更新、SKU/短描述保留；幂等、401/403/400/404/409/415 均符合预期。存储回读 UUID/SKU 各一条。定向单测 6 tests / 31 assertions，PHP lint、官方路由/ACL 升级及 i18n 收集和缓存清理通过。
- 清理：临时应用/安装/token 已撤销；撤销后另一次真实 GET 返回 401。验收商品 #322 的既有公开归档命令报 `product_admin_internal_error`，事务后仍为未发布草稿，保留以供检查；没有直接删除数据或修改归档实现。
- 文档：[接入契约与 curl 示例](rest-api.md)、[脱敏运行证据](rest-api-acceptance.json)。新增功能首次提升 1.0.165→1.0.166，工作区并行任务的后续版本保留。
- 交付地址：[创建（POST）](https://p05113ef3.test.weline.com:9555/api/weline_product/rest/v1/products/create)、[编辑（PUT）](https://p05113ef3.test.weline.com:9555/api/weline_product/rest/v1/products/edit)、[详情（GET）](https://p05113ef3.test.weline.com:9555/api/weline_product/rest/v1/products/detail?website_id=0&global_product_uuid=728a1bbe-4b58-50dd-94a5-2a455a7710e3)，均需有效应用 token。

## 2026-09-08 — 「规格修复」指令：汇报须含明细与交付地址

- 调整：`dev/ai-command/product/规格修复.md` 收口强制输出修复内容表 + 每个 fixed 商品的 Markdown PDP 链接；`AI硬规则索引` / Cursor 技能同步。
- 版本：`1.0.168` → `1.0.169`

## 2026-09-08 — 规格修复扫描指令 + #84 回填

- 扫描：已发布商品中仅 #84（offer `896875413963`）仍缺 listing 技术属性；`--scan --apply` 回填 13 条（材质/领标/吊牌等）。
- 脚本：`remediate-product-listing-spec-attrs.php` 支持 `--scan` 批量 dry-run/apply。
- MCP 指令：`dev/ai-command/product/规格修复.md`（触发词「规格修复」即自动扫描补全）。
- 版本：`1.0.167` → `1.0.168`

## 2026-09-08 — 主产品技术细节回填（柒依阁 #83）

- 问题：PDP「技术细节」仅尺码/类型；用户质疑主产品不应只有两条。
- 根因：1688 remaining 导入对 offer `687945556432` 报 `product_attribute_option_invalid`——`OfferEavMapper` 把「货源类型」误映射进 `style_type`，产品级 listing 属性整单未写入；库内仅有规格轴。
- 修复：`货源类型/类别` → `hanfu_huo_yuan_lei_bie`（非轴）；含「含量」字段不再并入 `material`；脚本 `remediate-product-listing-spec-attrs.php` 回填 #83 材质/工艺/领标等；单测锁定映射。
- 验收：`OfferEavMapperTest` 6/6；Browser #83 技术细节 18 条（含材质/工艺/领标等）；顺带修 B2B `w:form` 动态 hidden 导致 PDP 整槽挂掉。
- 版本：`1.0.166` → `1.0.167`

## 2026-09-08 — PDP 售卖模式槽：include 改为 fetch（Taglib）

- 问题：`product-info` 对 B2B selling-mode 使用 PHP `include` 时绕过 Taglib，页面输出字面 `@static`/`@url`/`<lang>`。
- 变更：改为 `$this->fetch(...)`（工作区已落地）；本回合继续清模板缓存验收。

## 2026-09-08 — 产品分类修复指令化 + 童装误挂女装

- 缺陷：`汉服女` 正则误匹配 `汉服女童`，9 个童装仍挂 `/women*`；已收紧规则并 unlink 12 条。
- 残留探针：`women_title_on_men=0` `kids_on_men=0`；`kids_on_women_no_parent=2` 为成人礼/老师款标题带书童，属合理女装。
- 指令：新增 `dev/ai-command/product/产品分类修复.md`（触发词：产品分类修复 / 分类修复 等）。

## 2026-09-08 — 全量多分类挂靠（形制/用途/材质+商业树）

- 问题：先前只补了女装/男装/童装等受众根；汉服形制/用途/材质树全 0；一品应多类并存。
- 变更：扩展 `remediate-product-category-audience-append.php`（contract `multi-taxonomy-append.v1`），按标题+适用性别+朝代+材质追加商业叶子与 `/hanfu/style|occasion|material` 全树；保留冲突受众 unlink。
- 数据：222 品追加约 1629 条 link；汉服根/形制/用途/材质均 222；明制马面 32、唐制齐胸 77、涤纶 181、日常通勤 183 等；每品约 8–16 类。
- 验收：分类页探针 + Browser 抽样。

## 2026-09-08 — 男装混入女装 #320 修正

- 问题：`QITA-F37FFDFC-S8182-TDE4F`（明制马面裙汉服女…）主图为女装，因源属性「中性/男女均可」被追加到男装。
- 修复：unlink 男装；脚本将「汉服女」视为女装专属标题，中性属性不再挂成人男装。
- 验收：男装直链 15；#320 仅女装+马面裙+套装。

## 2026-09-08 — 按主图受众追加女装/男装/童装分类

- 问题：1688 导入只挂一个粗叶子（襦裙/马面裙/女童/圆领袍/日常常服套装等），女装/男装/童装根节点直链为 0；儿童马面、男款马面等错挂女装叶子，父类聚合会串台。
- 变更：新增 `scripts/remediate-product-category-audience-append.php`（`--dry-run`/`--apply`）。结合标题、适用性别与主图抽检规则，向现有商业树追加 `/women` `/men` `/kids` `/sets` `/accessories` 及细化叶子；受众冲突叶子 unlink。
- 二次修正：童装源属性常标 `适用性别=男`，禁止据此挂成人男装；已从男装摘掉 6 个儿童书童 SKU。
- 数据：website=0 直链约 女装 167 / 男装 16 / 童装 48；男装页 Browser 见成人男装主图，不再混儿童卡。
- 验收：`/category/women|men|kids` HTTP 200。

## 2026-09-07 — 变体库存接口轻量快照

- 根因：CDN 商品详情壳的实时变体请求复用了完整 `livePublishedOffersForProduct()`，每个 Offer 都重复做媒体 URL、选项元数据和详情投影。
- 变更：新增 `liveVariantAvailabilityForProduct()`，复用统一 Cart 快照解析器，只保留发布状态、价格、库存、店铺上架、组合键和询价标记；媒体与 options 阶段显式关闭，并按请求 memo。详情页原完整投影路径不变。
- 验证：定向 Product 回归 **18 tests / 110 assertions**，PHP lint、diff check 通过；真实接口 54 个 Offer 均 HTTP 200，首个干净请求服务端约 **443ms**，后续样本 **64–332ms**。

## 2026-09-07 — StorefrontPriceContext 扩展 + absolute_minor（1.0.165）

- 变更：`StorefrontPriceContext` 可选 `customerId` / `sellingMode`（`fromCatalogMinor` 兼容）；`StorefrontPriceAdjustment::TYPE_ABSOLUTE_MINOR`；Assembler 独占组内绝对价优先。
- 文档：`storefront-offer-price.md` 补充 B2B Provider 与含价缓存 vary（`selling_mode` + tob `group_id`）。
- 版本：`1.0.164` → `1.0.165`

## 2026-09-07 — PDP 画廊主图与规格图分层

- 问题：规格图并入画廊时把与 swatch 同 URL 的主图升格成「规格」，画廊只剩规格角标。
- 修复：先快照 PHP 主图 + `base_images`/Offer 媒体为 `kind=product`（无角标）；规格图单独追加 `kind=spec` 带「规格」；点击规格仍切图并可反选。

## 2026-09-07 — PDP 规格图与画廊双向同步

- 根因：`renderGallery` 在规格预览之后仍强制 `offer.image` 置顶，点击带图规格时主图不切换。
- 修复：有选中规格预览时不再被 Offer 主图覆盖；`swatch_image` 纳入选中图库；所有带图规格选项并入左侧画廊并标「规格」；点击规格缩略图反选对应选项。
- 契约：`ProductDetailTemplateContractTest` 断言 `collectSpecGalleryItems` / `galleryAxis` / 规格角标。

## 2026-09-07 — PDP 购买盒数量标签防挤（1.0.163）

- 根因：foundation `select{inline-size:100%}` 在 buybox flex 行把「数量:」压成竖排。
- 修复：`qty-label` nowrap + 不收缩；`qty-select` 覆盖为 `inline-size/width:auto`。
- 契约：`ProductDetailTemplateContractTest` 断言防挤 CSS。
- 版本：`1.0.162` → `1.0.163`

## 2026-09-07 — 询价弹层接入地址标签与邮编推算（1.0.164）

- 表单：`<w:theme:address postal postal-lookup detail>`；提交落库 `address_json`；后台列表展示地址摘要。
- 协同：Theme `2.2.219` 将邮编反查写入地址标签/JS，结账复用。
- 验证：契约测 + PDP Browser 填邮编回填。

## 2026-09-07 — 商品卡/PDP CTA 对齐 ink 主色（1.0.162）

- 问题：列表卡/PDP/相关推荐仍硬编码 Amazon 黄 `#ffd814` / 橙 `#ffa41c`，与云裳水墨品牌盘冲突。
- 变更：`product-card.css`、`product-info`、`related-products.css` 及 Theme featured/bestsellers CTA 改吃 `--color-primary` 族（加购浅底描边、购买/结账实心朱砂）。
- 版本：`1.0.161` → `1.0.162`

## 2026-09-07 — 购物车快照透出划线价与活动（1.0.161）

- 问题：Cart snapshot 只 bake 成交价，迷你车/车/结账无法跟随 PDP 划线价与活动名。
- 变更：`ProductCatalogCartItemSnapshotResolver` 经 Assembler 产出 `compare_at_minor` + `campaign_label/url` 写入 `CartItemSnapshot`。
- 版本：`1.0.160` → `1.0.161`

## 2026-09-07 — 详情尺码表双栏排版（1.0.160）

- 确认：用户选定原型推荐方案 B（男女双栏），拒绝贴左散排。
- 变更：`DetailDescriptionTextifier` 输出 `weline-detail-text--size-chart` 双栏 HTML；店面 sanitizer 保留该类名；`product-info` 全宽面板 CSS，窄屏堆叠；#84 中英文详情已回写。
- 验证：`DetailDescriptionTextifierTest`、`ProductDetailTemplateContractTest`；Browser #84。
- 版本：`1.0.159` → `1.0.160`

## 2026-09-07 — 详情尺码/文字烤图转 HTML + 多语言（1.0.159）

- 问题：1688 详情多为烤字图（尺码建议表等），英文店面无法翻译且常空白；用户截图确认情侣装男女分码体重表。
- 处理：OCR 扫描 2615 张详情图（硬命中 79）；`DetailDescriptionTextifier` 将烤图替换为语义 HTML（斤+kg）；`scripts/remediate-detail-text-charts.php` 写回 description。首款 #84 中英文尺码建议表已文字化；批量 `--zh-only` 覆盖约 73 款。
- 验证：`DetailDescriptionTextifierTest` 3/3；Browser #84 中/英 PDP 可见表格；证据 `var/hanfu-1688/detail-textify/`。
- 版本：`1.0.158` → `1.0.159`

## 2026-09-06 — 推荐产品 CSS 与统一卡解耦（1.0.158）

- 问题：`recommended-products.css` 复用 `.wpc-*`，与 `<w:product:card>` 内部类名撞车，覆盖标题色/按钮圆角渐变。
- 修复：部件 chrome 改 `wpr-*`；CSS 只保留货架布局；JS 步进选择器改 `.weline-product-card`。
- 验证：契约测断言无 `.wpc-cta/.wpc-title` 覆盖；Browser 分类/列表推荐槽。

## 2026-09-06 — 列表页商品卡统一 Taglib（1.0.157）

- 需求：全部商品 / 分类 / 搜索命中卡与货架同一 `<w:product:card>`，展示 SKU。
- 变更：`ProductCardRenderer::fromStorefrontOffer` 映射 `unit_price_minor`/活动划线与详情 query；`catalog/index`、`category/index`、`storefront-offer-card` 改声明式标签。
- 验证：目录/分类/搜索契约测；Browser `/products`。

## 2026-09-06 — 相关/推荐部件改声明式 Taglib（1.0.156）

- 需求：相关产品、推荐产品循环改用声明式 `<w:product:card>`，不再手写卡或直接调 `ProductCardRenderer::render`。
- 变更：`related-products.phtml` / `recommended-products.phtml` 使用 `show-wishlist` 等变量属性与 `class="wpr-card"`；推荐轨道 CSS 对准 `.weline-product-card.wpr-card`；契约测断言 Taglib。cross-sell 仍为 FBT 勾选组合 UI，未改。
- 验证：`StorefrontProductWidgetCatalogRelatedCardsTest`。

## 2026-09-06 — 统一前台商品卡片 Taglib（1.0.155）

- 需求：`<w:product:card>` 统一卡片，可选展示价格/评分/加购/收藏/对比/快览/标签/SKU，`density` 控制疏密。
- 变更：新增 `ProductCard` Taglib + `ProductCardRenderer` + partial/CSS；声明式模板可用 `<w:product:card product="product" … />`（`product` 为变量名）。新增后须 `taglib:collect`。
- 验证：`ProductCardContractTest`；RecentlyViewed/Related 契约；CLI Renderer 探针；店面 PDP 相关栈默认关闭故区块不可见。

## 2026-09-06 — 变体切换禁叠折 + 与购物车同源（1.0.154）

- 问题：目录 Assembler 后 `unit_price_minor` 已是成交价；变体 JSON 未带 `catalog_price_minor`，PDP JS `applyDealToMinor(unit)` 再折一次（如 119→107.10→96.39），迷你购物车/加购只折一次 → 用户以为购物车「不认活动价」。
- 修复：`StorefrontVariantSelectionService` 透传 `catalog_price_minor`/`compare_at_minor`/`has_deal`；PDP `priceFromOffer` 优先用服务端成交价，禁止对 unit 叠折。
- 验证：契约测 + 变体目录单测；Browser 披风白/均码与迷你购物车同价。

## 2026-09-06 — 推荐/部件卡保留活动划线价与活动名（1.0.153）

- 问题：推荐产品等部件卡活动名丢失，价序与 `/products` 不一致。
- 修复：`mapOffer` 透传 `currency`/`has_deal`/`campaign_*`；推荐卡改为成交价→划线原价→活动名。
- 验证：推荐卡与目录卡同显活动划线。

## 2026-09-08 — 修正文档 Demo 观察者运行契约（1.0.192）

- 实际故障：升级后 `/dev/tool/docs/api` HTTP 200，但 `config.error` 为“观察者必须继承于：Weline\Framework\Event\ObserverInterface”，接口数组为空；此前 PHP lint 与产品输入单测未覆盖事件分发契约。
- 根因与修复：`ApiDocDemoObserver` 只有兼容的 execute 方法而没有声明官方接口；对照现有 `StorefrontCatalogCacheInvalidator`，显式实现 `ObserverInterface`，不改收集后的 Product Demo 数据和动作逻辑。
- 最小运行契约脚本在修复前返回 `compatible=false` / exit 1；同脚本用于修复后验证。模块 1.0.191→1.0.192，正常升级后的真实文档页和登录 Demo 操作仍须验收，不以类契约检查代替。

## 2026-09-08 — 产品发布 REST 与文档 Demo（1.0.191）

- 新增独立 publish 动作、双版本请求映射及真实店面 URL 回读，复用原发布诊断、Product/Offer 事务和变更事件。
- 接入 Api 验证后的只读 API 用户身份；应用与用户幂等键互相隔离，不把 API 用户映射为后台管理员。新增发布 route ACL，保留 global 应用安装限制。
- Product 通过 API 文档收集后事件维护本模块 Demo 声明；创建、回读、发布、多语言编辑均调用已登记 API。页面不猜店面链接，也不在创建中暗含发布。
- 定向测试先复现 UUID/两版本丢失和用户幂等隔离缺口；原 simple 零价格/零库存及目标币种诊断用例通过。真正的 Demo UI 与店面/CDN 验收尚待本轮配置环境完成，不能用这些开发测试替代。
- 模块 1.0.190→1.0.191，增加可选 DeveloperWorkspace 集成；事件注册、路由/ACL、国际化需走正常编译与升级链。

## 2026-09-06 — 蔡宝首款导入 + CatalogCollector pageLimit 优雅退出（1.0.152）

- 继续：鑫耀 browser 批次完成后启动曹县蔡宝；公开列表恢复扫页触达 `pageLimit` 曾硬失败。
- 修复：`CatalogCollector` 在已有 unique offers 时触达 pageLimit → 跳出并走 `listing_total_drift_tolerated`（单测覆盖）。
- 导入：browser.capture.v1 → `hanfu-1688-20260906-factory-caibao-browser-v1`，offer `743337832233` → 商品 **#320** published（类型/尺码轴齐全）。
- 约束：禁止对本机 Vulkan Real-ESRGAN 结果上架。
- 验收：PDP `qi-ta-ming-zhi-ma-mian-qun-han-fu-nu2023xin-kuan-zhang-xiu-shang-f37ffdfc` 有「类型」「尺码」。

## 2026-09-06 — 回退月兔 Real-ESRGAN 损坏图为 1688 原图（1.0.151）

- 问题：本机 `realesrgan-ncnn-vulkan` 升清产生分块错位，规格主图花屏/错位，用户反馈乱七八糟。
- 修复：`remediate-product-image-hd.php --restore-source` 按 `metadata.source_url` 强制回写 1688 原图；对 `factory-xinyao/687143261814` 9 张规格图全部恢复。
- 验证：Browser 米色主图 `07-aaef59b393db.jpg` natural 680×815，可辨认完整商品。
- 约束：本机 Vulkan Real-ESRGAN 结果不可用于店面发布，禁止再对本批图跑 `--realesrgan`。

## 2026-09-06 — 主题部件卡划线价改读 Assembler（1.0.150）

- 问题：`StorefrontProductWidgetCatalog::mapOffer` 用假公式生成 `original_price`，与目录统一 Assembler 优惠不同源。
- 修复：成交价仍读 `unit_price_minor`；划线原价读 `catalog_price_minor`/`compare_at_minor`（仅当高于成交价）。
- 验证：部件卡与 `/products` 卡同源活动价。

## 2026-09-06 — 月兔规格图 Real-ESRGAN 全量高清化（1.0.149）

- 问题：#318 各 `style_type` 规格主图仍糊；部分源图仅 354–800 边长，此前 Lanczos 升清不够。
- 修复：`remediate-product-image-hd.php` 接入 `realesrgan-ncnn-vulkan`（`--realesrgan`，`--force-min-edge`）；优先下载 1688 原图再 `realesrgan-x4plus`（`-t 400`）；对 `factory-xinyao/687143261814` 9 张规格/画廊图全量替换。
- 验证：Browser 米色 `style_type=gui-hua-tu-m` 主图 `07-hd-*.jpg` naturalWidth=1360。

## 2026-09-06 — 目录投影统一 Assembler 供卡片活动价（1.0.148）

- 问题：目录 listing 快照改回原价后，分类/目录商品卡只读 `unit_price_minor`，看不到活动价；与 PDP/货架 Assembler 不同源。
- 修复：`StorefrontCatalogViewService` 投影结束统一调用 Assembler，写出 `catalog_price_minor`（原价）+ 成交 `unit_price_minor` + `has_deal`/`campaign_*`；分类/目录卡展示划线与活动名；PDP 以 `catalog_price_minor` 入 Assembler 防叠折。
- 验证：契约测；分类卡与指定 PDP 同价。

## 2026-09-06 — 目录快照返回原价，避免 PDP/货架叠折（1.0.147）

- 问题：`resolveCatalogOffers` 预写 Assembler 活动价到 `unit_price_minor`，PDP/活动货架再次 Assembler → 叠折（89→80.10→72.09）。
- 修复：目录 listing 快照只输出原始目录价；活动价由 PDP/货架/加购路径的 Assembler 单点施加。保留 `product.catalog.snapshot.deals` 计时相位。
- 验证：指定 PDP 与 `/promotion/deals` 同价 80.10/划线 89.00。

## 2026-09-06 — 详情正文剥离 1688 促销推荐商品表（1.0.146）

- 问题：PDP「关于该商品」仍展示火爆促销条与带￥价格的其他商品推荐卡（1688 详情 HTML 内嵌），与已关闭的布局推荐区无关。
- 修复：`StorefrontProductMediaUrlResolver::renderDescriptionHtml` 与 `OfferDetailParser` 删除促销关键词表、以及含货币价+图片/批发文案的推荐商品表；保留真实面料说明与本商品详情图。
- 验收：单元测；Browser 指定 PDP 无「火爆大促销 / WUYIKUANHUANGOU / ￥55」推荐卡。
- 版本：`1.0.145` → `1.0.146`

## 2026-09-06 — PDP 低清规格图防放大与定向升清（1.0.145）

- 问题：月兔 #318 `style_type=gui-hua-tu-f` 主图发糊；主图 natural 354×534 被 CSS `width:100%` 拉到 ~648px。1688 CDN 无更大原图（卖家规格图本身低清）。
- 修复：店面主图 `width:auto` + 按 `naturalWidth/devicePixelRatio` 限制 max-width；`remediate-product-image-hd.php` 增加 `--object-key-contains` 便于定向 `--upscale-fallback`。
- 验证：Browser 该 URL 主图不再超原图像素拉伸；定向升清 `factory-xinyao/687143261814` 低清规格图。

## 2026-09-06 — 鑫耀 browser 剩余两款导入发布（1.0.144）

- 范围：月兔马面裙 offer `687143261814` → product **#318**；兔绒绒马面裙 offer `693483394109` → product **#319**。
- 阻塞：CREATE 时 ensure 后轴值为中文但 `sku_overrides` 仍为来源 code → `variant_sku_override_unknown_combination`；非规格 `hanfu_*` 截断拼音 code 与共享选项 code 不一致 → `product_attribute_option_invalid`。
- 修复：`offerSpecs` 按 ensure 别名重映射 override keys；`hanfu1688AlignProductValuesToSharedEav` 按定义 label 对齐 product_values；`canonicalOption` 可按 label 命中。
- 验证：run `hanfu-1688-20260906-factory-xinyao-browser-remaining-v3` terminal=applied；Browser 两款 PDP 均见「类型」与 `?style_type=`。
- 交付：[月兔马面裙](http://p05113ef3.test.weline.com:9555/product/xin-yao-qiu-dong-kuan-yue-tu-ri-chang-gu-zhuang-han-fu-nu-ming-zhi-dc67c024)、[兔绒绒马面裙](http://p05113ef3.test.weline.com:9555/product/xin-yao-2022xin-kuan-tu-rong-rong-han-fu-nu-ming-zhi-ma-mian-qun-d-bfc0244b)。

## 2026-09-06 — 店面搜索性能：投影快照缓存与批量选品（1.0.143）

- 问题：`/search?type=all|product` TTFB 十余秒；CLI 探针 `commerce.rollout.search=off` 走 `product_direct`，`snapshotWebsite` 单次 5.8–7.4s（4498 docs），且对每个 product/offer 调 `isSelected` N+1。
- 修复：`StoreProductRepository::selectionMap`；`ProductSearchProjectionService` 按 website+watermark 进程缓存 + 批量 selection；`ProductSearchProvider` 空关键词短路、先去重分页再 `prepareRows`；前台空 `q` 跳过 hub。
- 验证：契约测 `ProductSearchProviderPerfContractTest`、`SearchIndexTemplateContractTest`；curl `/search?type=all&q=中国风套装`。

## 2026-09-06 — 搜索卡命中定向 hydrate（1.0.144）

- 问题：即便分页后再 `prepareRows`，`publishedOffersForProductIds($ids)` 仍先 `rememberPublishedOffers()` 全量重建目录，冷请求仍多秒。
- 修复：有明确 `productIds` 时走 `buildPublishedOffers(..., $filterIds, true)` 定向路径（`product.catalog.resolve_filtered`），不再为卡片命中重建全站投影。
- 验证：`StorefrontCatalogFilteredOffersContractTest`；curl 暖请求 `type=product` ≈1.2–2.3s。

## 2026-09-06 — 鑫耀 #311 类型轴重导修复（1.0.142）

- 问题：分流合并为 `style_type` 后重导失败链：`variant_offer_uuid_mismatch` → `product_attribute_option_invalid` → 发布 `variant_combination_value_not_allowed`（`style_type=白小袖浅黄裙子`）；前台曾只剩尺码。
- 根因：导入按 SKU/仅 key 预挂 uuid 与 reconcile SKU 迁移冲突；`mediaRows` 规范化 combination 未传 `productId`；identity 按截断拼音 code 把 `bai-xiao-xiu`（水蓝）误映射到共享「浅黄」；遗留 product `color` 未清除。
- 修复：uuid 仅 key+sku 同命中才挂；reconcile SKU 迁移忽略目标 key stale uuid；`mediaRows`/`canonicalOption` 带 productId；identity 仅 value/id；写入轴后 cleared 未用 color-family；option slug 加长至 24。
- 验证：`ProductVariantMatrixServiceTest`（含 stale-uuid 迁移）；reuse-snapshot `--publish-all` run `…-reaxis-v15` 成功；Browser PDP 见「类型」轴与 `?style_type=`，含白小袖浅黄/水蓝等。
- 交付：[鑫耀 · 对襟襦裙广袖](http://p05113ef3.test.weline.com:9555/product/xin-yao-gu-zhuang-han-fu-nu-xue-sheng-han-fu-dui-jin-ru-qun-guang-1af25240)。

## 2026-09-06 — 列表仅询价 deep-link 打开弹层（1.0.141）

- 问题：分类/列表卡「仅询价」灰色不可点。
- 修复：卡片 CTA 链到商详 `?quote=1`；PDP 检测到该参数自动打开询价弹层并清理 URL。
- 验证：`ProductQuoteRequestNativeContractTest`；`ProductCardAddToCartHookContractTest`。

## 2026-09-06 — OfferEavMapper 分流轴互斥不得整轴丢弃

- 问题：1688「颜色」经 `ColorAxisClassifier` 拆到 `color`/`style_type` 后，旧逻辑要求全 SKU 命中才保留轴，互斥标签导致两轴皆丢，鑫耀 #311 仅剩 `size=*`、前台无「类型」。
- 修复：`OfferEavMapper::catalog` 对分流族轴改为 ≥1 命中即保留；SKU `combination_key` 只写入命中轴+共享轴；单测 `testKeepsColorAndStyleTypeAxesWhenSkuLabelsAreMutuallyExclusive`；实施说明 §9.1.1 增硬规则与伪代码。
- 验证：`OfferEavMapperTest` 5/5；鑫耀 offer `639654800868` catalog axes=`color,size,style_type`，overrides=102。

## 2026-09-06 — 询价回复改调 mail-composer Taglib（1.0.140）

- 目标：询价「企业邮箱回复」打开全局写信浮层，不再用 compose GET body 深链。
- 实现：`ProductQuoteMailReplySlotService` 输出 `composer` 载荷；列表挂 `<w:mail-composer/>` + `WelineMailComposer.open`；个人中心询价「查看沟通」。
- 验证：契约测无 `compose=1` 深链；调用方禁止内嵌写信器。

## 2026-09-06 — 询价对接企业邮箱回复闭环（1.0.139）

- 目标：询价后台用企业邮箱回复并自动标已回复；超管可选客服/测试邮箱代发。
- 实现：`ProductQuoteMailReplySlotService`；观察 `Weline_Mail::mail_message_sent`；可选依赖 `Weline_Mail`。
- 验证：契约测；后台操作列出现邮件回复 slot；超管选 fake `support@quote-test.invalid` 代发后询价 `#1` → `replied`（`updated_at=2026-09-06 07:06:54` 与 `w_weline_mail_message` 发信一致）。
- 交付：[商品询价后台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/weline_product/backend/quote-request/index)。

## 2026-09-06 — 询价流转 CSRF 与路由注册（1.0.138）

- 目标：后台状态流转按钮可真实 POST 成功并写库/触发事件。
- 修复：流转表单改为 `w:form csrf="auto"`；`transition` action 经 `s:up --route` 注册。
- 验证：浏览器 POST `#1 processing→replied`，DB `status=replied` 且写入 `admin_reply_at`；契约测含 CSRF 断言。
- 交付：[商品询价后台](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/weline_product/backend/quote-request/index)。

## 2026-09-06 — 询价状态机与后台模板修复（1.0.137）

- 目标：询价单提供状态流转与事件；修复后台列表模板路径错误。
- 状态：`new/processing/replied/processed/cancelled`；`ProductQuoteRequestStateMachine` + can/before/changed 事件；提交派发 `quote_request_submitted`。
- 后台：`fetch('index')` → `templates/Backend/QuoteRequest/index.phtml`；按可用流转渲染操作按钮。
- 验证：状态机契约测；实站后台列表可打开并可流转。

## 2026-09-06 — 个人中心「我的询价」与菜单角标（1.0.136）

- 目标：账户侧栏/顶栏接入「我的询价」；未读角标由 JS 绘制（SSR 仅空壳）；不依赖 Inquiry。
- 实现：表增 `customer_id` / `admin_reply_at` / `customer_last_seen_at`；登录提交写 customer_id；`markProcessed` 写 reply 时间；静默邮箱认领；`ProductQuoteMenuSignalProvider`（`product.quotes`）；Hooks：`account.sidebar.group.commerce` / `account.sidebar.content` / `header-account-links`。
- 未读：已绑定顾客且 `admin_reply_at` 晚于 `customer_last_seen_at`；打开列表时 mark seen。
- 验证：`ProductAccountQuotesSignalContractTest` + 既有询价契约测通过；实站 guest SSR 角标空壳；登录后 `account.menuSignals` 返回 `product.quotes=1`；打开 `#product-quotes` 列表后 mark seen，信号归零。
- 交付：[个人中心·我的询价](https://p05113ef3.test.weline.com:9555/customer/account/index#product-quotes)。

## 2026-09-06 — Product 自研商品询价（1.0.135）

- 目标：仅询价规格可点选；PDP「提交询价」弹层自动携带当前 SKU/规格/参考价；落库并后台列表处理。不依赖 Inquiry。
- 实现：`ProductQuoteRequest` 表与 Service（honeypot/幂等/可选 Captcha/`quote_only` 校验）；Frontend API + `product_storefront.submitQuoteRequest`；后台 ACL/菜单与标记已处理；PDP 拆分可加购 vs 可选；Cart 对 `quote_only` 不渲染加购。
- 文档：`doc/product-quote-request.md`。
- 验证：`ProductQuoteRequestNativeContractTest`（6/6）；实站 PDP 规格可点、弹层带 SKU/参考价、提交成功 `quote_request_id=1`、后台列表可标记已处理。
- 交付：[仅询价 PDP](https://p05113ef3.test.weline.com:9555/product/meng-hui-han-tang-han-fu-qiu-zhuang-nu-xue-sheng-nu-zhuang-tang-zhuang-nu-fu-21caf39f?size=m&style_type=hf-s-bai-se-shang-yi-955de62)。

## 2026-09-06 — 前台成交价 Assembler（1.0.134）

- 目标：Product 统一读成交价，Marketing/Promotion 等以 Provider 扩展多种优惠；划线价旁展示活动名。
- 实现：`StorefrontPriceAdjustmentProviderInterface` + Registry + `StorefrontOfferPriceAssembler`；`extends.php` 槽位；PDP/Cart 快照改读 Assembler。
- 文档：`doc/storefront-offer-price.md`。
- 验证：`StorefrontOfferPriceAssemblerTest`、`StorefrontOfferPriceAssemblerContractTest`、`ProductCatalogCartQuoteOnlyDealContractTest`。

## 2026-09-05 — 仅询价拦截与活动价进购物车快照（1.0.133）

- `quote_only`：Cart snapshot 拒绝加购；PDP `isOfferSellable` 不再把仅询价当可售。
- 活动价：快照 `unit_price_minor` 经 `PromotionStorefrontActiveDealResolver` 与活动货架同源折扣；PDP 展示活动价+划线原价。
- 验证：`ProductCatalogCartQuoteOnlyDealContractTest`。

## 2026-09-05 — 英文目录数量翻译运行回读（后续进展）

- 承接下条的实站刷新失败，Framework 的 Collect 已接通既有运行控制接口。最新普通 HTTPS HTTP/1.1 英文目录实际读回 `1–24 of 125 products`，中文仍为 `第 1–24 件，共 125 件商品`；此前回读总数为 123，总数随真实数据变化，不作为固定门槛。空目录仍仅验证译文单测，未伪造业务数据。
- 本次缓存刷新 `ctrl_op_29` 在另一轮重载期间排队，命令正确警告等待期内未完成；后续只读同一操作终态确认 2/2 Worker 已应用。不是本任务发起的重载，不将较早独立的 1/2 ACK 失败误归于本次操作，也不以一次成功关闭稳定性问题。
- 最新 Collect/CacheClear/Product/Shipping 定向回归合计 `14 tests / 36 assertions` 通过；独立审查提出的测试函数隔离问题已真实复现并修正，生产改动未发现 Critical/Important 问题。
- Browser：已能只读列出内置标签页；首次标签选取被 auto-review 拒绝，经重新读取状态、动态限定当前任务唯一 iab 后选取仍超时。未转用外部 Chrome、未发布或下单；375/768/1024/1440、主题范围发布与正式支付配送验收仍未完成。下条保留当时失败的历史记录。
- 交付地址：[英文商品目录](https://p05113ef3.weline.test:9555/en_US/products)、[中文商品目录](https://p05113ef3.weline.test:9555/products)。后台/API：N/A。

## 2026-09-05 — 英文目录数量翻译（代码通过，实站刷新未通过）

- 实际问题：普通 `/en_US/products` 的标题及价格过滤链接已为英语/保留 `/en_US/`，但数量仍显示“第 1–24 件，共 … 件商品”。最新目录总数持续变化，不将旧的 79 件写死为验收条件。
- 根因与改动：`i18n/en_US.csv` 的“第 %{1}–%{2} 件，共 %{3} 件商品”和“0 件商品”误用中文 identity mapping；仅将两条译文补为 `%{1}–%{2} of %{3} products`、`0 products`。中文源、占位符、模板计数逻辑、商品数据、布局、URL 和主题草稿均未修改。
- 最小回归：新增 `test/Unit/View/CatalogQuantityTranslationTest.php`，执行真实 `TranslationResolver` 读取 Product 模块 CSV；两个用例先实际 RED，修复后 GREEN。与既有 Shipping 英文指南回归合计 `8 tests / 16 assertions` 通过；PHP lint、定向 diff check 通过。
- 运行验收未通过：`php bin/w i18n:collect Weline_Product` 返回 exit 0 和“翻译缓存清理成功”，但随后普通中英文 HTTPS HTTP/1.1 响应中，英文数量仍为旧中文，中文保持正常；不能把命令成功当作商城已更新。空目录译文仅完成单测，未伪造空目录业务数据。
- 刷新链诊断：Framework `DictionaryCompiler::clearTranslationCaches()` 清理 phrase 池并调用当前进程的 `Parser::clearWorkerCaches()`；`DictionaryCompileAfterObserver` 同样只清理当前进程的 I18n 状态。已读调用链未通知运行实例清理常驻 Worker 词典，而 Parser 的持久 Worker 层按 scope 常驻缓存。后续须接通既有运行时失效链并取得真实 Worker 完成及前台回读证据；本条未改 Framework/Server，也未重启服务。
- MCP：宿主 prepare 仍 Transport closed，复用官方 STDIO 当前代次并取得 ready；新测试的精确子请求终止于 `CONTEXT_TARGET_UNAVAILABLE`，按返回契约限定原生修改，不继续嵌套索引。
- Browser/全目标：内置浏览器明确返回 Mac 已锁定且自动解锁失败，本轮无新截图；按产品设计审查技能暂停视觉审查。375/768/1024/1440、真实筛选交互、主题范围发布及正式支付配送配置仍未完成；未声称商城可以上线。
- 交付地址（均本轮普通 HTTPS 200 回读）：[英文商品目录](https://p05113ef3.weline.test:9555/en_US/products)、[中文商品目录](https://p05113ef3.weline.test:9555/products)。后台/API：N/A（本条未新增或修改路由）。

## 2026-09-05 — 商品主图旁视频（本地 + 外链）（1.0.132）

- 需求：商品级视频；本地 FileAsset（mp4/webm）与 YouTube/Vimeo 链接或 iframe 代码；后台主图旁配置；前台详情画廊混排播放；不做规格级视频。
- 实现：`Media role=video`；外链规范化 `ProductVideoUrlNormalizer`（watch/short/embed/iframe → `video://provider/id`）；`MediaRepository`/`ProductAdminCommandService` 支持无 asset 的外链行；编辑页视频面板；`product-info` 画廊缩略图点开 iframe/`<video>`。
- 验收：单元 `ProductVideoUrlNormalizerTest` + 契约 24 tests / 604 assertions；Worker reload 后真实 PDP 画廊出现 YouTube 缩略图，点击舞台 iframe=`https://www.youtube.com/embed/dQw4w9WgXcQ`；后台编辑页需登录（契约断言视频面板）。
- 版本：`1.0.131` → `1.0.132`。
- 交付地址：[商品详情（含视频画廊）](http://p05113ef3.test.weline.com:9555/product/meng-hui-han-tang-han-fu-qiu-zhuang-nu-xue-sheng-nu-zhuang-tang-zhuang-nu-fu-21caf39f)

## 2026-09-05 — 产品图水印复扫与促销底栏清理（1.0.130）

- 当前状态：`implemented`（媒体直链已探活；前台 PDP 仍可能 404，不以商品页做视觉验收）。
- 问题：全量复扫后仍有 1688 促销红底条（现货速发/团购/网红爆款等）及个别店招/盗图条残留；半透明工厂字 OCR 易漏检。
- 处理：对确认的约 40 张候选从 `source_url` 重下；以全宽红条检测 + 有限上扩白条/徽章后 `crop_scale` 为主；难例（全胜 `876268926944` 04/05、舞美 `47-hd`）定向再裁/修补；原地替换 live 并更新 FileAsset sha/宽高/metadata。
- 证据：`var/hanfu-1688/watermark-scan/watermark-rescan-confirmed-20260905.json`、`watermark-rescan-apply-v5-20260905.json`；本轮 apply **39** 张（1 张字节未变跳过）。
- 版本：`1.0.129` → `1.0.130`。
- 交付地址：见本轮收口媒体直链（`04-b1702a2db098` / `05-5f4609a9f13f` / `47-hd-89cef9c66c6e`）。

## 2026-09-05 — 排序与分页保留属性筛选

- `Catalog::index` 复用按现有 Filters 语义归一化后的 `af_*` 生成排序、分页 URL，保留价格条件；切换排序回到第一页，默认排序清除 `sort`，分页保留当前排序。页面内部布局字段不透传到导航链接。
- `Weline_Product` 版本 `1.0.130` → `1.0.131`，运行入口为 `php bin/w setup:upgrade --route --module=Weline_Product`。
- 同轮 EAV 的范围字段存在性探测复用统一请求缓存，保持原 SQL、异常回退和新请求刷新；连接与表隔离、测试及性能样本说明见 Framework/doc/统一缓存范围与性能优化.md 本轮记录。
- 验收使用真实商品页的属性筛选、排序、默认排序及分页；HTTP 参数检查与浏览器操作分别记录。复现/验收脚本与响应位于 `/private/tmp/weline-sort-query-opt/`、`/private/tmp/weline-perf-continue/`。

交付地址：[属性筛选与排序](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_chao_dai=Ming+Style&sort=price_asc)、[属性筛选与分页](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_shi_yong_xing_bie=Women&sort=price_asc&page=2)。

## 2026-09-05 — EAV 只读元数据与标签查询继续优化

- 已落地：公共readonly DTO树按entity/locale请求内复用；商品私有选项仅复制必要节点，自由集SQL按set/product限定；标签原SQL的scope/code结果ID/null统一请求缓存，商品校验与旧数据、语言兼容保留。
- 验证：定向46 tests/286 assertions、PHP lint、diff check通过；真实DB中英文30组旧/新输出一致。默认两Worker平滑重载12.5秒。
- 真实冷请求：英文3.163s、中文2.680s，81商品字段投影约219/212ms；正常入口1.293s，后续FPC HIT约34.85ms。数据与机器负载变动，不承诺稳定延迟。
- 未完成：Mac锁屏阻止Browser页面操作验收；header输出体积、计时边界外的FPC尾部及PHP候选集分页仍待后续优化。详细边界和证据见 Framework/doc/统一缓存范围与性能优化.md 最新节。
- 交付地址：[英文商品](https://p05113ef3.weline.test:9555/en_US/products)、[中文分类](https://p05113ef3.weline.test:9555/categories)、[排序分页](https://p05113ef3.weline.test:9555/en_US/products?sort=price_asc&page=2)、[属性筛选](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_chao_dai=Ming+Style)、[价格筛选](https://p05113ef3.weline.test:9555/en_US/products?price=0-99)。后台/API：N/A。

## 2026-09-05 — 1688 汉服导入筛选、EAV 选项与真实规格收口

- 修复整店筛选回调方向：只有 `HanfuProductClassifier` 明确认定为汉服的商品才请求详情并进入写库路径；`非汉服` 等拒绝项不再反向取详情。单个失效详情不再触发整店桌面详情熔断，后续候选逐件独立尝试。
- 桌面详情 URL 统一补 `forcePC=1&td_page_id=PC-DEFAULT-2026`，异常链接连接/总超时收敛到 5/20 秒；详情仍必须通过解析完整性门禁，列表兜底不得发布。
- `ProductCatalogEavBootstrap::ensureHanfuAttributeOptions()` 现会把来源发现的正式选项实际持久化到「汉服」属性集，不再只建属性壳；规格和商品参数均走 EAV，不写 `type_config_json` 或高级 JSON。
- `OfferEavMapper` 只把来源 SKU 文本实际命中的选项纳入规格轴，避免通用款式/颜色描述生成不存在的笛卡尔积 SKU；发布校验以 Offer `combination` 中真实出现的轴为准，产品级普通多选 EAV 不再误判为规格轴。
- 首款真实验收：花朝记 `824496857539` → Product `169`，公开主 SKU `HUAZHAOJI-7339FF7C`，4 个来源真实 Offer，6 张商品图、13 张本地详情图；供应商店铺地址为 `https://shop2218ba33z8885.1688.com`，发布诊断 0 错误/0 警告，前台 HTTP 200、语义 Handle 与 SEO 标题/描述已输出。该来源没有 SKU 独立图片，因此不伪造规格图；存在来源规格图的商品仍按 `Media role=variant + canonical combination_key` 落库。
- 未完成：1688 对无登录批量详情请求返回登录风控页，Weline 登录浏览器可显示商品但开发接口批量导出超时；花朝记其余商品及其余待办供应商不得以列表残缺数据代替，待可稳定读取登录详情后继续。
- 交付地址：https://p05113ef3.test.weline.com:9555/product/hua-chao-ji-cheng-ren-han-fu-zhong-gong-ci-xiu-da-xiu-shan-qi-xiong-qu-7339ff7c

## 2026-09-05 — 全胜 catalog 水印漏网复清（1.0.129）

- 当前状态：`implemented`（媒体直链已探活；前台 PDP 仍 404，不以商品页做视觉验收）。
- 问题：用户反馈 `factory-quansheng/876294406862` 主图底部仍可见 `曹县全胜服饰有限公司` + `shop*.1688.com`；首轮 LaMa 对半透明双行水印易留鬼影或底部黑糊。
- 处理：从 FileAsset `source_url` 重下源图；对 offer 全部 25 张 catalog 采用裁掉水印带（`y<710`）再还原 800×800 的 `crop_scale` 方案；原地替换 live 文件、更新 sha256/宽高/metadata，并删除隔离原图副本。
- 证据：`var/hanfu-1688/watermark-scan/quansheng-final-results.json`、`quansheng-apply-final.json`；水印区相对源图像素 MAE≈34–40；媒体 URL HTTP 308→HTTPS 200。
- 版本：`1.0.128` → `1.0.129`。
- 交付地址：`https://p05113ef3.test.weline.com:9555/pub/media/catalog/hanfu/1688/factory-quansheng/876294406862/01-85fdd22c1624.jpg`、`…/03-424f23c77402.jpg`、`…/04-2eea1cc335b0.jpg`。

## 2026-09-05 — 商品列表冷构建轻量化与完整计时

- 已实现列表规格选项/详情正文媒体按用途分离、去除重复媒体解析、readonly标签DTO直接遍历；specifications供属性筛选继续保留，现有范围/语言缓存失效不变。
- Framework trace独立DB累计和32项phase summary解决4096明细截断后的计时缺失。定向38 tests/216 assertions通过；旧Trace/Runtime配置测试既有失败修改前后一致。
- 真运行：统一协调器失效后，英文/中文目录重建TTFB分别5.148s/7.091s，79商品投影1.901s/2.208s，媒体部分0.41/0.47ms；此前本轮英文10.246s/投影6.248s。不是受控压测，未承诺稳定延迟。
- HTTP：首屏24卡前后字段一致；明制属性筛选6件、价格区间及升序第2页正确。未完成：Browser连接/导航/截图超时；EAV共享DTO/私有回退、PHP全量候选分页、模板HTML体积及FPC命中入口长尾仍可优化。
- 详细机制、限制及证据见 Framework/doc/统一缓存范围与性能优化.md 的“目录冷请求投影优化与完整计时”。
- 交付地址：https://p05113ef3.test.weline.com:9555/categories 、https://p05113ef3.test.weline.com:9555/en_US/products 、https://p05113ef3.test.weline.com:9555/en_US/products?sort=price_asc&page=2 、https://p05113ef3.test.weline.com:9555/en_US/products?af_hanfu_chao_dai=Ming+Style 。后台/API：N/A。

## 2026-09-05 — 统一缓存优化实际运行记录

- default 两个 Worker 平滑重载成功（8.1 秒）；分类冷请求 200/10.214 秒，随后命中约 105 ms；首页约 111 ms，英文商品列表约 63–104 ms，均 200。
- 新查询已在真实 timing.log 出现：EAV options 增加 scope_instance_id 条件，重复全实体扫描已消除；trace 中 DB 记录约 453 ms（截断样本，不据此计算完整提升比例）。
- 未完成：后续 nocache 动态请求及复查收到维护页 503，维护状态未改；Browser 真实页面操作工具超时，不能宣称浏览器验收通过。冷目录模板前约 8.06 秒及大 HTML 后处理仍需继续定位。
- 完整策略、定向回归数据和剩余优化清单：Framework/doc/统一缓存范围与性能优化.md。

## 2026-09-05 — 目录缓存统一声明与读取去重

- 状态：实现与定向回归已完成，真实页面验收见本轮交付记录。
- StorefrontCatalogCacheCoordinator 集中声明分类树、分类商品关系、菜单和商品报价策略。原始索引按网站复用；含路由和展示差异的结果按渠道、语言和币种隔离。
- 分类五组展示属性改为 readPresentationMaps 一次读取；真实 SQLite 测试从 5 次读取降为 1 次，locale 回退及 cleared 行为保持。
- 单商品实时路径通过 ProductRepository::listByIds 下推 IN 查询，保留 ID 顺序和发布状态筛选；真实 SQLite 行访问由 [10,20,30] 收窄为目标 [20]。
- 跨网站后台变更通过既有 WebsiteTargetLookupInterface 解析目标 website_id 的 code，修复原本误清当前请求网站的问题；未知目标不会清错站。
- 相关回归覆盖原属性读写、目录仓储和新增效率行为；与统一缓存范围策略联测。共享 Worker 清理由既有广播和 Theme resetter 完成。
- 继续测量：目录列表全量投影、EAV 元数据重复枚举、页头搜索组件及静态资源体积；不得将本条视为全部前端性能问题已关闭。

## 2026-09-05 — 商品 Sitemap Provider 与 URL 变更影响（1.0.128）

- 当前状态：代码已实现，真实 WLS/后台验收待主任务完成。
- 修复：新增归属 Product 的 SitemapUrlProvider；公开 URL 遵循发布、Store 选品与 slug 继承。现有 product_search_projection 增加当前/旧 URL，完整后台保存合并内层商品事件以保留旧 slug。
- 定向测试：ProductSearchProjectionServiceIntegrationTest（临时 SQLite）覆盖发布、端口、slug、Store 下架和 Product 下架，以及 normal 同源店铺入 sitemap、独立店铺 URL 保留在事件、dev/test 排除；ProductPublicUrlChangeTest 覆盖单次事件与新旧 URL。
- 未完成：运行 setup:upgrade 后确认 Provider 发现与 sitemap 生成，真实后台商品保存/下架和 SEO/CDN 异步结果。无账户时只能验收到任务与 sitemap 边界。
- 交付地址：待主任务探活并记录；本子任务未操作运行服务或外部 API。

## 2026-09-05 — 声明店面报价筛选事件规约（1.0.127）

- 当前状态：`implemented`（Browser 已验收）。
- 问题：`/products?af_hanfu_zhi_wu=涤纶` 不收窄结果；侧栏「涤纶」不勾选。根因是 `Weline_Product::storefront_offers_filter` 仅有 Filters `event.xml` 观察者，未在 Product `event.php` 规约中声明，`generated/events.php` 无该事件，`EventsManager::hasObservers` 短路导致观察者永不执行。
- 修复：在 `Product/event.php` 声明 `storefront_offers_filter` 并重建注册表（挂上 `Weline_Filters::storefront_offers_attribute_filter`）；Framework `hasObservers` 在注册表未命中时走 scan 回退；Filters 侧栏 query 优先 `getQueryParams`/`QUERY_STRING`。
- 验收：https://p05113ef3.test.weline.com:9555/products?af_hanfu_zhi_wu=%E6%B6%A4%E7%BA%B6 →「第 1–1 件，共 1 件商品」，涤纶勾选，可清除筛选。
- 版本：`1.0.126` → `1.0.127`。

## 2026-09-05 — 产品画廊缩略图上下滚动（1.0.126）

- 当前状态：`implemented`。
- 问题：`product-info` 左侧缩略图列随图片数量拉高整页，无法在主图高度内浏览。
- 修复：有缩略图时画廊用 grid；`.product-native-detail__thumbs` 以 `height:0;min-height:100%` 跟随主图列高度并 `overflow-y:auto`；移动端仍横向滚动。
- 验收：`ProductDetailTemplateContractTest` 合同断言；Browser 多图 PDP 左侧可滚动。
- 版本：`1.0.125` → `1.0.126`。

## 2026-09-05 — 已发布汉服商品英文名称补齐与中点误判修复

- 当前状态：`implemented_pending_browser_acceptance`；本条不代表整个商城、主题发布或结账已可上线。
- 需求与真实 RED：`/en_US/products` 首屏 24 个商品名称为 `Product + SKU` 或裸 SKU；Product Repository 只读核对 website 0 当前 79 个 published 商品，中文 name 均存在，`en_US` name 均缺失。
- 架构与范围：Product 归属的精确中文源名词表 `data/hanfu-product-en-names.php` + `CuratedProductNameTranslation` + `scripts/remediate-hanfu-product-en-names.php`。复用 `AttributeValueRepository::writeExplicit`，只补 website 范围 `store_id=0 / entity_type=product / name / en_US`，不改中文、商品身份、价格库存或店铺范围；不重跑导入、不使用旧数码商品 seed。
- 写入规则：默认 `--dry-run`，`--apply` 才落库；保留独立译名和明确 cleared 覆盖层，只填空值或原文复制值。精确源名未在词表时不猜译。文案按原名保留形制、款式与用途，去除关键词堆砌，不增加正品保证、材质比例或履约承诺。
- 标点 bug：当前 PHP PCRE 中 `\p{Han}` 连中点 `·` 也匹配，商品输出将 `Feitian Dunhuang · Mamian Skirt and Hanfu Shirt` 误判为不支持的汉字并回退 `Product HANFU-DUNHUANG`。真实 projector 回归先 RED；改为主 Script 判断 `\p{sc=Han}`，普通标点不再触发误判，汉字匹配仍保留。
- TDD：精确源名补写、已有译名/cleared 保护、真实目录词表与中点输出用例分别观察 RED → GREEN；本轮相关聚焦回归 `33 tests / 297 assertions` 通过，PHP lint 与定向 diff check 通过。
- 数据验收：干运行 `published=79 / matched=79 / updated=0`；执行 `--apply` 返回 `updated=79`；再次干运行 `matched=0 / updated=0 / skipped=79`。商品表全量记录 SHA-256 及排除目标英文 name 后的属性行 SHA-256，前后均一致；只增加英文名称覆盖层。
- 缓存验收：写入后普通目录仍返回旧标题，双 bypass 已返回全部首屏译名，确认读模型缓存未失效。补数脚本接入已有 `StorefrontCatalogCacheCoordinator::notifyCatalogChanged`，只在本次确有写入时调用；已对首批补数补发同一目录变更通知。通知执行较慢但实际返回 `storefront_catalog_changed_dispatched`；未修改 Server 或重复重启实例。
- HTTP 实测：通知后，普通 `/en_US/products` 返回 200、`lang=en-US`，24 个商品标题均为英文名称，中点款式正常；普通商品详情返回 200，title/h1 为 `Republican-Era Inspired Tea Ceremony Top`。该轮处理时间分别约 3.55 秒和 7.98 秒，不能声称动态页面性能已达到上线要求。
- Browser：遵守用户指定的内置浏览器。首页地址/标题元数据可读，但 AX、定向 DOM、截图及 CDP 内容读取均超时；已向用户说明并请求保持页面可操作。未把 HTTP 结果冒充真实点击、响应式视觉、加购或主题范围发布验收。
- MCP 回退：沿用当前 ready 会话；`get_edit_bundle` 仍报 `PLAN_REQUIRED`，当前工具表缺 `submit_task_plan/get_task_plan`。按返回 workflow 的 `HOST_MCP_NOT_ATTACHED` 受限路径，在对话明确范围/架构/任务/验收后仅定向原生编辑，保留其他任务改动；未提交、未发布主题。
- 尚待验收：内置浏览器商品规格与加购操作、全目录分页视觉、主题范围草稿保存/发布回读、移动/平板/桌面，以及结账和动态访问长尾问题。79 条当地化记录存在并不等于所有页面和流程均已验收。
- 前台：https://p05113ef3.test.weline.com:9555/en_US/products ；详情：https://p05113ef3.test.weline.com:9555/en_US/product/qi-yi-ge-min-guo-feng-nu-zhuang-tang-zhuang-su-yi-chan-cha-fu-gai-l-5bf4b1ae?size=s80-90jin&style_type=fen-se-jin-shang-yi2307 。以上均完成普通 HTTPS 200 回读；后台/API：N/A（本条未新增路由）。

## 2026-09-06 — 基础选品读取跳过无用详情

- 新增可选 `ProductSelectionReadInterface::searchSelectionRows`，现 `ProductAdminReadInterface` 签名与完整后台 DTO 保持。ProductAdminReadService 的完整/基础投影复用同一私有筛选与排序实现，基础投影仅输出 product_id/updated_at，保留原必要身份和店铺筛选，跳过价格、媒体和展示店铺读取。
- 原200商品批次仍完整迭代；跨批次、SKU回退、全部筛选、更新时间及ID排序由最小用例对照完整search，真实Promotion消费另核对日期、limit、网站和店铺范围。未增加私有缓存、权限结果缓存或业务限制。
- 本轮实际配置数据库216商品完整输出基准905223字节；正式源码/数据库结果等价和WLS冷请求改善须以 `/private/tmp/weline-goal/acceptance-report.md` 的实测为准。冷请求<1秒和Browser整体目标未完成前继续保留，不用CLI或单测代替验收。
- 交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。

## 2026-09-06 — 冷目录标签索引按需构造与基础读取批量化

- 真实归因：商品投影对188项调用label.resolve2942次，原每商品会预展开全部EAV属性的option/alias。现在只为实际请求的attribute构造原有两个索引；保留forProduct与locale清理、别名覆盖顺序、空标签回退和私有选项可用范围，无新增缓存系统。
- 固定188个真实projectListing入参（本次实际locale为zh_Hans_CN）新旧输出855428B逐字节相等；独立CLI1174.51→666.74ms，DB均670次，峰值282→180MiB。新旧标签回归14tests85assertions通过；此数值不代替英文HTTP收益。
- ProductAdminReadService::search先按原规则筛除无效/店铺未选商品，再按200商品一批调用既有OfferRepository和AttributeValueRepository，按product_id/entity_id分组且保留原批内顺序；identity、SKU/name筛选、价格/媒体/店铺输出与最终排序不变。中间映射随批释放，不新增跨请求缓存。
- ProductAdminReadService::searchRows在上述批量结果中先按原name/sku/status条件排除不匹配商品，再读取global_product_uuid身份。name仍取现有属性排序中首个未cleared的名称，SKU仍保留offer与root回退；product_code、product_type、owner_website_id继续使用身份权威值及原有fallback，不新增缓存。最小真实Service查询边界用例先RED，完整批量筛选回归9tests94assertions通过。实际配置的独立CLI同scope、同活动SKU HF-SLY-Z230903返回相同空选品结果，226→10次查询（identity find216→0），134.755→68.585ms；此对照不等于HTTP验收，也不归因Worker中尚未拆明的其余查询，整页冷请求<1秒继续待验收。
- 真实同191商品、相同目录事实hash下，两SKU案例完整返回逐字节相同；空选品查询957→197，504.87→172.96ms；命中商品83查询969→209，611.28→190.98ms。固定输入、最小真实Service回归、正式应用/冷HTTP见 `/private/tmp/weline-goal/acceptance-report.md`。
- 本次仅Service/Test/文档，不修改Model/Controller或数据库结构。完整WLS冷请求<1秒和Browser目标继续保留。2026-09-06最新Browser状态：已按用户授权的新版字段核验唯一Weline及browserId，本任务商品标签1514405941实际存在，但getTab读取38.44秒观察超时、kernel reset，未取得可用绑定；不能把标签判作终止，也未重复创建。筛选、排序、分页和语言交互尚未验收，当前阻碍不是锁屏。
- 交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。

## 2026-09-05 — 私有规格英文读取、缓存隔离与当地名补齐

- 当前状态：`implemented_pending_browser_acceptance`；本条不代表汉服商城已可上线。
- 真实 RED：商品 83 的私有选项 77 已保存 `en_US=Pink Top Only 2307`，英文商品页仍显示中文源值；标签解析器只加载共享 metadata，随后从私有 Option 原值回退，绕过了当地名。
- 修复：`StorefrontEavLabelResolver` 经已有 `AttributeMetadataCatalogInterface::catalogForProduct` 读取商品级 metadata；同一解析器复用一次目录读取；切换 product 或 locale 时统一清理目录、属性名、选项别名及私有回退缓存。canonical option ID、code、原始 value 与 URL 组合身份不变，不新增跨模块 ORM 路径。
- TDD：3 个新增用例先 RED，再 GREEN，覆盖已译私有标签与三类别名、热缓存的跨商品/共享 scope 隔离、同商品连续切换 locale。标签测试 `11 tests / 53 assertions`；相关商品投影、规格选择、购物车图像契约聚焦回归 `29 tests / 127 assertions` 通过；PHP lint 与定向 diff check 通过。
- 数据：通过现有 `remediate-eav-en-locals.php`（未使用 `--force`）先补 71 个选项和 1 个属性，再补本页款式及共享尺码的 33 个选项，共 104 个选项、1 个属性。后者精确保留仅上衣/裤装套装/裙装区别、2307 款号与原始斤数，并提供 kg 等值。只写 `en_US` LocalDescription，不改商品事实、价格、库存、EAV source value/code/ID，也不覆盖已有独立译文。两批写入后分别 dry-run 均为待更新 0；最终匹配选项跳过 373 条。
- HTTP 证据：修复代码后，真实双缓存绕过英文 PDP 返回 200，`lang=en-US`，可见 swatch label 为 `Pink Top Only 2307`；同词中文只保留在数据身份中，不在可见文案节点中。补完其余 33 条后最初复测曾超时；本轮恢复后返回 200，6 个款式 label 均已英文，6 个尺码显示对应 kg 与原斤数，`QIYIGE-5BF4B-556432-VF9AC9609` 身份保留。这里只确认 HTTP HTML，未将 WebUI 点击效果标为已验收。
- 运行时问题：`cache:clear` 曾报 Worker 未全部完成清理，滚动重载曾报 `drain timeout`。使用框架命令 `server:start -r -f --instance default` 恢复本地实例（Master 14534，复用共享 Session/Memory）后观察到连续 Worker 热重载；本轮确认同工作区性能任务也在正常重载，不能把这些记录直接归因于 FileWatcher 自发循环。已协调不重复修改或重启 Server。HTTP/1.1 与 HTTP/2 均曾无首字节超时，动态路径仍有长尾，未新增重试门禁。
- Browser：按用户要求仅使用内置浏览器；`about:blank` 可读取，但真实商城页 AX、DOM snapshot 与截图均超时。不得将 HTTP 或单测替代规格点击、响应式视觉、加购或主题范围发布的操作验收。
- 工具回退：ensure 重试和当前回合工具发现仍缺 `submit_task_plan/get_task_plan`；MCP `get_edit_bundle` 返回 PLAN_REQUIRED，且其 workflow 明确允许记录 `HOST_MCP_NOT_ATTACHED`、在对话列计划后对精确已知路径原生编辑。已保留全部其他工作区改动；未提交或发布主题。
- 未完成：恢复真实页面连续响应与内置浏览器操作；核对补译后的全部款式/尺码与 SKU/媒体切换；继续补其余商品目录当地名；完成主题范围编辑、草稿保存、发布回读及移动/平板/桌面视觉验收。
- 前台验收目标：https://p05113ef3.test.weline.com:9555/en_US/product/qi-yi-ge-min-guo-feng-nu-zhuang-tang-zhuang-su-yi-chan-cha-fu-gai-l-5bf4b1ae?size=s80-90jin&style_type=fen-se-jin-shang-yi2307 。本轮已再次 200，但此前存在超时，仍不能作为稳定上线通过证据。
- 后台/API：N/A（本条未新增路由）；整体目标的主题编辑器仍为 https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/theme/backend/theme-editor ，匿名请求 302 到登录。

## 2026-09-05 — 产品图水印扫描清理（1.0.125）

- 当前状态：`implemented`。
- 问题：1688 导入产品图含工厂名/`shop*.1688.com` 等水印，污染前台图库与详情图。
- 处理：OCR 全量扫描 `pub/media/catalog/hanfu`（排除分类图）；确认 19 张命中；LaMa 去水印后原地替换 FileAsset 字节、更新 sha256/尺寸元数据，并删除原图隔离副本。
- 证据：`var/hanfu-1688/watermark-scan/watermark-hits-live-20260905.json`、`apply-watermark-cleanup-result-20260905.json`；媒体 URL 字节与清理后 sha 一致，OCR 复扫无 `1688.com`/工厂名硬残留。
- 说明：前台 `/product/{slug}` 当前整站 404（既有问题，非本次引入）；验收以媒体直链为准。
- 版本：`1.0.124` → `1.0.125`。

## 2026-09-05 — 英语站角色轴 swatch 丢失（1.0.124）

- 当前状态：`implemented`。
- 根因：`StorefrontProductDetailProjector::resolvePresentableAttribute` 在非汉字 locale 跳过含 Han 的属性值；`character` 等多选轴身份值常为中文，导致 `availableValues` 丢轴，媒体 `swatch_image` 无法挂到选项（en_US 仅文本 chip）。
- 修复：收集变体轴 `availableValues` 时 `allowHanIdentity=true`；规格文案仍按原 Han 过滤。
- 验收：`StorefrontProductDetailProjectorTest::testEnglishLocaleKeepsHanCharacterAxisSwatchesFromVariantMedia`；en_US PDP 角色缩略图。
- 版本：`1.0.123` → `1.0.124`。

## 2026-09-05 — 产品模糊/低清图高清替换（1.0.123）

- 当前状态：`implemented`。
- 问题：1688 导入等路径留下大量 `_b` 缩略图与低清像素，前台放大后模糊。
- 修复：新增 `scripts/remediate-product-image-hd.php`：扫描产品引用 FileAsset；优先去掉 alicdn 尺寸后缀拉原图；无更大源时可选 ffmpeg lanczos 放大；`MediaRepository::syncProductScope` 切换引用后 `deleteObject` 并 purge FileAsset/Locale 元数据。
- 验收：`--scan`/`--dry-run`/`--apply`/`--verify`；抽样产品详情页主图更清晰。
- 版本：`1.0.122` → `1.0.123`。

## 2026-09-05 — 颜色脏配件拆到独立规格轴（1.0.122）

- 当前状态：`implemented`。
- 问题：鞋套/布鞋/胡子/竹简/胶水/古书等误入共享 `color`，污染全局色板；国学服/A 款/如图等亦然。
- 修复：新增 `prop`（配件）轴；扩展 `ColorAxisClassifier`；脚本 `remediate-misfiled-color-options-to-axes.php` 将误分类 option 迁到 `prop`/`look_ref`/`style_type`，并改写商品属性值与 label 型 `combination_key`。
- 本库：13 条配件迁入 `prop`；17 条引用项迁出颜色；`residual_misfiled_color=0`；offer 标签轴改写 120 条。
- 版本：`1.0.121` → `1.0.122`。

## 2026-09-05 — Cart options 商品私有规格 swatch（1.0.121）

- 当前状态：`implemented`。
- 问题：PDP 有规格图/色块，但购物车快照 `options` 无 `swatch_image`/`swatch_color`（全局 EAV catalog 读不到 `scope_instance_id=product_id` 私有选项）。
- 修复：`eavOptionSwatches` 改用 `catalogForProduct`，并同时索引图/色；媒体图优先，EAV 图回退。
- 验收：`CartItemSnapshotImageResolutionContractTest`；Browser 迷你购物车/`/cart`/`/checkout`。
- 版本：`1.0.120` → `1.0.121`。

## 2026-09-05 — 新建产品布局嵌套 form 误提交（1.0.119）

- 当前状态：`implemented`。
- 问题：布局面板在 `#product-editor-form` 内嵌套 `method=dialog` 表单；创建按钮冒泡触发商品保存，`collectProviderConfiguration` 报「扩展类型配置必须是 JSON 对象」。
- 修复：去掉嵌套 form，改 `type=button` + `createProductLayout` / 定时保存 click；补 `.w-product-layout-dialog` 内边距；JS/CSS cache bust。
- 验收：`ProductLayoutAdminPanelContractTest` 7/7。
- 版本：`1.0.118` → `1.0.119`。

## 2026-09-05 — Cart options 填充规格图/色（1.0.120）

- 变更：`ProductCatalogCartItemSnapshotResolver::buildOptions` 从 variant 媒体与 EAV 选项填充 `swatch_image`/`swatch_color`。
- 版本：`1.0.119` → `1.0.120`。

## 2026-09-05 — Cart 快照填充可读规格 options（1.0.118）

- 当前状态：`implemented`。
- 变更：`ProductCatalogCartItemSnapshotResolver` 用 `StorefrontEavLabelResolver` 将 selection 解析为行级 `options`（轴标签 + 选项文案），供购物车/结账展示。
- 版本：`1.0.117` → `1.0.118`。

## 2026-09-05 — PDP 孤儿规格轴灰显仍可加购（1.0.117）

- 当前状态：`implemented`。
- 问题：产品级多选属性（如 `hanfu_liu_xing_yuan_su`/勾花）写入 offer.`variant_axes`，但 `combination_key` 仅含 color+size；`collectAxisCodes` 误把 `variant_axes` 并入轴码，抵消 `buildCatalog`「仅暴露 offer 实际轴」过滤，PDP 灰显不可选规格却仍可按 color+size 加购。
- 修复：`collectAxisCodes` 只从 combination/`combination_key` 收集轴码；`variant_axes` 仅用于已参与组合的轴标签/选项 enrichment。
- 验收：`StorefrontVariantSelectionServiceTest::testBuildCatalogOmitsOrphanAxesFromVariantAxesMetadata`；复现 PDP 不再展示流行元素灰轴。
- 版本：`1.0.116` → `1.0.117`。

## 2026-09-05 — 1688 汉服全量目录与高规格商品前台验收

- 当前状态：`runtime_validated`。
- 数据范围：严格汉服识别后发布 79 款、3341 个真实 Offer、4149 条媒体；供应商分别为曹县全胜服饰有限公司 4 款、曹县悦雅服饰有限公司 7 款、曹县柒依阁服饰有限公司 3 款、曹县舞美人生服装厂 13 款、曹县蕊宝服饰有限公司 22 款、曹县青途制衣有限公司 30 款。供应商店铺 URL 与地址、品牌、语义 slug、短 SKU、SEO、详情和本地图片均纳入目录审计。
- EAV 契约：商品规格只使用 EAV 属性选项与 Offer `combination_key`，发布 Offer 的 `type_config_json` 全为空；导入中使用的颜色 32/32、尺码 93/93、款式 313/313 均已有共享 EAV option，私有-only 与缺失 option 均为 0；缺少值补入汉服属性集可用的 EAV 轴，不把规格写入高级 JSON。
- 详情媒体：来源详情图 1004 张，其中源站仍可用 941 张已全部本地化并写入商品详情/描述；63 张源站已失效（HTTP 400/404）记录为不可用来源，不伪造远程图片。规格媒体按 EAV 组合关联，前台切换规格使用 Offer 主图与差异图库。
- 空页根因与修复：商品 109/111/112/127 含 322–378 个 Offer，旧页面把每个 Offer 相同的整套图库重复写入 catalog JSON，并在模板再次重建 Controller 已生成目录，触发 WLS 内存保护后返回 HTTP 200 空体。`StorefrontVariantSelectionService::compactCatalogMedia` 提取一次共用图库、保留 Offer 主图/差异图；`product-info` 复用已分配 catalog，浏览器组合共用图库与规格图。
- 路由恢复：运行环境一度缺少 `generated/routers` 四张路由表，导致首页、分类和商品页统一落入主题 404；执行仅路由模式 `setup:upgrade --route`（复用既有 Schema）后生成前台 631 条页面路由、306 条接口路由并滚动重载 2 个 WLS Worker，首页、`/products`、`/categories` 与商品详情恢复 HTTP 200。
- 验证：相关单元/模板契约 `31 tests / 208 assertions`；`audit-global-hanfu-catalog-20260905.php` 为 79/3341/4149、`failed_product_count=0`；EAV option coverage 缺失 0；可用详情图缺失 0；携带全新缓存标识的真实 WLS 全量商品页 `79/79` 通过 HTTP 200、非空正文、详情主体、本地图片、SEO description/keywords/canonical/LD+JSON 与详情正文检查，冷请求最大 20.707 秒、中位 2.048 秒。来源与当前 Offer 的标签集合/数量一致；仅 EAV 规范排序与 1688 来源顺序不同，不属于规格缺失或错配。
- Browser 验收：内置浏览器打开代表商品 85，在 `S` 尺码下由“白色上衣”点击切换为“红色上衣”；URL 的 EAV option code 从 `hf-s-bai-se-shang-yi-955de62` 更新为 `hf-s-hong-se-shang-yi-57e108c`，SKU 从 `MENGHUIHANTA-496347-V62148DD3` 更新为 `MENGHUIHANTA-496347-VDBA00694`，主图切到本地规格图 `05-b6eb9493db46.jpg`。同页可见品牌、供应商、尺码/款式 EAV、技术细节与完整“关于该商品”正文；`/categories` 真实 HTTP 返回 24 张商品卡和分页，79 个商品详情页全量真实 WLS 验收通过。
- 证据：`var/hanfu-1688/global-catalog-audit-20260905.json`、`var/hanfu-1688/imported-axis-option-coverage-20260905.json`、`var/hanfu-1688/source-detail-image-audit-20260905.json`、`var/hanfu-1688/all-product-frontend-acceptance-20260905.json`。

## 2026-09-05 — 私有选项：后台规范化 / 搜索关键词 / PDP 性能（1.0.116）

- 当前状态：`implemented`。
- 问题：`scope_instance_id=product_id` 后，后台 `normalizeRows` 仍用共享 catalog 校验会拒私有 option；搜索投影不含私有标签；多 Offer PDP 反复 `forProduct` 冷查导致卡死；调试埋点残留。
- 修复：`normalizeRows($rows, $productId)` + `writeAttributes` 传商品 id；`canonicalOption` 可按 option id 匹配；搜索投影收录本商品私有 option 文案；Projector 按商品缓存 scoped resolver、同商品 attrs/media 只查一次；删除 `17e82d` 调试埋点。全站 facet/EAV 管理仍仅共享。
- 验收：`ProductAttributeMetadataCatalogTest` / `ProductScopedOptionContractTest`；定向重载后目标 PDP 应远快于百秒级。
- 版本：`1.0.115` → `1.0.116`。

## 2026-09-04 — 产品信息编辑器画布：demo offer 短路（1.0.115）

- 当前状态：`implemented`。
- 变更：product-info 编辑器画布无 public route 时用 demo offer，跳过 `StorefrontOfferResolver` 与 variant catalog。
- 版本：`1.0.114` → `1.0.115`。

## 2026-09-04 — 主题编辑器预览：related/cross-sell 跳过冷目录（1.0.114）

- 当前状态：`implemented`。
- 问题：商品布局编辑器常无 PDP identity，`relatedCards`/`bundleCards` → `publishedOffers` 冷刷可卡数十秒。
- 修复：预览且无产品 id 时 related 用 `ThemeDemoCatalog`；cross-sell 无 seed 时跳过 `bundleCards`。
- 版本：`1.0.113` → `1.0.114`。

## 2026-09-04 — 店面私有规格选项标签解析（1.0.113）

- 当前状态：`implemented`。
- 问题：`character`/`look_ref`/`style_type` 私有化后 `scope_instance_id=product_id`，店面 `StorefrontEavLabelResolver` 仍只读共享 catalog，chip 显示 option_id 数字；#125 残留无角色的 color/style_type orphan offer，灰轴仍可用角色+尺码加购。
- 修复：`forProduct($productId)` 经 `AttributeOptionStore::assertUsableByInstance` 按 option_id 解析私有标签（避免 `catalogForProduct` 全量当地名扫描）；Detail/Projector 绑定商品 scope；`buildCatalog` 仅暴露 offer 实际轴；清理 #125/#127 少数派 orphan offer 与产品轴值。
- 验收：`StorefrontEavLabelResolverTest::testProductScopedPrivateOptionResolvesByOptionId`；PDP #125 角色显示「司马懿」等中文，无颜色/类型灰轴。
- 版本：`1.0.112` → `1.0.113`。

## 2026-09-04 — 角色/图款/类型规格私有化与多商品拆分（1.0.112）

- 当前状态：`implemented`。
- 问题：张飞、图一等已从颜色拆到 `character`/`look_ref`/`style_type`，但仍是共享 option；多商品共用时互相污染色板。
- 动作：脚本 `scripts/remediate-special-axis-options-to-private.php`：单商品引用→`scope_instance_id=product_id`；多商品→首商品保留原 option 私有，其余克隆私有 option，并改写分片 `attribute_value` 与 `offer.combination_key` 中的 option_id，保证前后台仍按本商品 option 解析标签。
- 本库结果：character 私有 57、look_ref 私有 16、style_type 私有 308（未引用模板共享 28）；克隆 70、attr 改写 69、offer 改写 445；editorCatalog 抽样：全局无张飞/图一，商品 125/124/127 可见各自私有项。
- 用法：`--dry-run` / `--apply`；可选 `--attributes=character,look_ref,style_type`。
- 版本：`1.0.111` → `1.0.112`。

## 2026-09-04 — 产品/分类布局面板 + Detail 解析链（1.0.111）

- 当前状态：`implementing`。
- 动作：商品编辑「布局」Tab（新建 option / 指派 / 定时）；分类编辑「默认产品布局」；`Detail` 经 `ProductLayoutResolveService` 写入有效 `layout_option`（定时→产品→分类默认→default）；对接 Theme Editor 产品模式深链。
- 验收：`ProductLayoutAdminPanelContractTest`；Browser：后台面板 + 店面详情切布局。
- 版本：`1.0.110` → `1.0.111`。

## 2026-09-04 — 单商品独有共享色改私有（1.0.110）

- 当前状态：`implemented`。
- 动作：脚本 `scripts/remediate-singleton-color-options-to-private.php` 扫描各 Website 分片 `color` 多选引用；仅被 1 个商品引用的共享 option 将 `scope_instance_id` 改为该商品 id（保留 option_id）。
- 本库结果：9 条私有（黑/绿/墨黑/墨色/大红/玫红/紫/浅绿/柠檬黄），12 条仍共享（含多商品色与未引用模板色）；已补齐 `scope_instance_id` 列。
- 用法：`--dry-run` / `--apply`；可选 `--attribute=color`。
- 版本：`1.0.109` → `1.0.110`。

## 2026-09-04 — EAV 实体私有规格选项接入（1.0.109）

- 当前状态：`implemented`。
- 问题：自定义/1688 规格值静默写入全局 option，共享色板膨胀。
- 修复：商品矩阵创建/保存经 `ensureAndCanonicalizeVariantAxes` 先匹配共享再写 `scope_instance_id=product_id` 私有选项；导入 `ensureHanfuAttributeOptions` 只确保属性壳、不落产品脏 option；CREATE 在拿到 productId 后再物化选项。
- 验收：`ProductScopedOptionContractTest`；规格仍唯一 EAV/`combination_key`，禁止 `type_config_json` 旁路。
- 版本：`Weline_Product 1.0.108 → 1.0.109`（依赖 `Weline_Eav 1.1.13`）。

## 2026-09-04 — EAV 属性/选项 en_US 当地名补数（1.0.108）

- 当前状态：`implemented`。
- 问题：英文 PDP Technical details 属性名与多数选项仍显示中文默认当地名。
- 修复：新增 `data/hanfu-eav-en-locals.php` 词典与 `scripts/remediate-eav-en-locals.php`，经 `EavLocalTranslationService::saveLocalizedField` 写入 Attribute/Option `en_US` LocalDescription；长 SKU 型选项未映射者仍回退中文。
- 验收：EN PDP 显示 Dynasty/Ming Style、Fabric Name/Polyester 等英文当地名。
- 版本：`Weline_Product 1.0.107 → 1.0.108`。
- 备注：新脚本路径因 MCP `CONTEXT_TARGET_UNAVAILABLE` 按精确路径原生落盘。

## 2026-09-04 — EN Technical details 保留默认当地名（1.0.107）

- 当前状态：`implemented`。
- 问题：英文 PDP Technical details 仅剩已有 en LocalDescription 的 Color/Size；其余有默认中文当地名的规格行被 `containsUnsupportedHanText` 整行丢弃。
- 修复：规格投影优先 `StorefrontEavLabelResolver`（EAV 属性/选项 LocalDescription）；无当前 locale 当地名时回退源文/默认文案并继续展示；汉字过滤仅保留给商品标题等中性回退路径。
- 验收：EN PDP 规格行数与中文侧接近；有 en 当地名仍显示英文；无 en 时显示默认中文。
- 版本：`Weline_Product 1.0.106 → 1.0.107`。

## 2026-09-04 — 店面 EN 分类/EAV 选项本地化（1.0.105）

- 当前状态：`implemented`。
- 问题：英文路径下分类 mega menu 与 PDP 变体轴仍显示中文源文；WLS 常驻 Worker 上 EAV 标签单例缓存不按 locale 失效，会把中文标签串到英文请求。
- 修复：`StorefrontEavLabelResolver`/`StorefrontVariantAxisResolver` 缓存按 `State::getLangLocal` 键控；`mergeOptionWithEav` 仅补空 label，不覆盖已解析本地化文案；`seed-hanfu-catalog` 同步写入 `en_US` 分类名；配合 `AttributeMetadataCatalog` 读 Option LocalDescription。改码后需 `server:reload`。
- 验收：EN 首页菜单英文化；EN PDP Color 为 Pink/Green；Worker reload 后探活通过。
- 版本：`Weline_Product 1.0.104 → 1.0.105`。

## 2026-09-04 — 回车新增规格弹窗卡死（1.0.105）

- 当前状态：`implemented`。
- 问题：规格与价格面板回车后页面像卡死：弹窗挂在 `hidden` 的分类/媒体面板内，`showModal` 得到 0×0 不可见层并锁滚动。
- 修复：规格值弹窗与规格图选择器移到各 `data-product-panel` 之外；修正弹窗表单 `min-height`；Enter 增加 IME/`stopPropagation`；clamp 的 ResizeObserver 改为 rAF 合并。
- 验收：Browser 回车后弹窗有可见尺寸；可关闭/确认；契约断言弹窗不在 taxonomy 面板内。
- 版本：`Weline_Product 1.0.104 → 1.0.105`。

## 2026-09-04 — 回车新增规格值先弹属性项表单（1.0.104）

- 当前状态：`implemented`。
- 问题：编辑页自定义规格值回车后只追加纯文字胶囊，无法按属性能力填写颜色/图片。
- 修复：回车改为打开「新增规格值」弹窗（外层已有编辑 form，弹窗内用 `div` 容器避免嵌套 form 被浏览器剥离）；字段按 `swatch_capabilities`（颜色/款式轴强制色+图）决定；确认后写入带 `swatch_color`/`swatch_image` 的勾选胶囊。
- 验收：契约测覆盖弹窗标记与 `openVariantAxisOptionDialog`；Browser 颜色轴回车可见颜色/图片字段，尺码轴仅名称与代码；确认后胶囊带色块。
- 版本：`Weline_Product 1.0.103 → 1.0.104`。

## 2026-09-04 — 误入颜色的角色/图款/套装拆轴（1.0.106）

- 当前状态：`implemented`。
- 问题：1688「颜色分类」把角色名、图一～图十六、套装描述灌进共享 `color` 选项。
- 修复：汉服属性集新增 `character`（角色）、`look_ref`（图款）；套装/款式复用 `style_type`；`ColorAxisClassifier` + `OfferEavMapper` 按值分流；一次性数据迁移已执行并将临时 remediate 脚本删除。
- 验收：分类器/映射合同测；异常商品轴不再落在颜色，无引用脏项已删。
- 版本：`Weline_Product 1.0.105 → 1.0.106`。

## 2026-09-04 — 后台规格轴芯片展示更多/收起（1.0.103）

- 当前状态：`implemented`。
- 问题：可配置商品编辑页规格轴（如颜色）选项胶囊过多，占满多行难以扫视。
- 修复：规格轴 chip 区复用前台技术细节 clamp：默认约两行 `max-height` + mask 淡出；溢出显示「展示更多 / 收起」；动态渲染/新增轴后重新判定；切换到「规格与价格」面板后再量高度，避免 hidden 面板初始化误判无溢出。
- 验收：颜色轴超多选项默认折叠；展开后可收起；少量选项无按钮；合同测试。
- 版本：`Weline_Product 1.0.102 → 1.0.103`。

## 2026-09-04 — 技术细节可收起（1.0.102）

- 当前状态：`implemented`。
- 问题：展开后按钮消失，无法再折叠。
- 修复：同一按钮在「展示更多 / 收起」间切换；展开后保留可切换态，收起恢复 mask 高度限制。
- 验收：展开→收起→再展开；短内容仍无按钮。
- 版本：`Weline_Product 1.0.101 → 1.0.102`。

## 2026-09-04 — 技术细节折叠改为透明度 mask 淡出（1.0.101）

- 当前状态：`implemented`。
- 问题：折叠区用白色 `linear-gradient` 遮罩，看起来像白块而非透明到实体。
- 修复：去掉白色 `::after`；对 `spec-clamp-body` 使用 `mask-image` 自不透明到透明淡出；「展示更多」背景透明。
- 验收：无白色遮罩；内容向下透明淡出；展示更多仍可点。
- 版本：`Weline_Product 1.0.100 → 1.0.101`。

## 2026-09-04 — 技术细节两列布局与展示更多叠渐变（1.0.100）

- 当前状态：`implemented`。
- 问题：规格单列占高；「展开更多」在渐变下方左侧，不符合标注。
- 修复：规格改为 `container-type` 网格（≥36rem 两列，过窄一列）；「展示更多」绝对居中叠在底部渐变之上可点展开。
- 验收：宽屏两列、缩窄一列；展示更多在渐变上；契约单测。
- 版本：`Weline_Product 1.0.99 → 1.0.100`。

## 2026-09-04 — 技术细节 EAV 中文标签与超高折叠（1.0.99）

- 当前状态：`implemented`。
- 问题：详情「技术细节」对未知属性码用 `ucwords` 把 `hanfu_chao_dai` 显示成 `Hanfu Chao Dai`；规格行过多占屏。
- 修复：`StorefrontEavLabelResolver::attributeLabel` 从 EAV 元数据取属性名；Projector 规格项输出 `label`；模板优先已知码/`label`/`__()`；表格默认约 15.5rem 渐变折叠，`scrollHeight` 不足时隐藏「展开更多」；窄屏收紧列宽与折叠高度。
- 验收：定向单测；中文商品页属性名为中文；超高可展开、不足高度无按钮。
- 版本：`Weline_Product 1.0.98 → 1.0.99`。

## 2026-09-04 — 1688/夹具清理迁入 Sample 样例面（1.0.98）

- 当前状态：`implemented`。
- 问题：`Service/Hanfu1688` 与 `Service/HanfuCleanup` 实为爬虫/ETL 与夹具运维，误放在业务 Service 层。
- 修复：迁至 `Sample/Hanfu1688`、`Sample/HanfuCleanup`（命名空间 `Weline\Product\Sample\*`）；单测改到 `Test/Unit/Sample/`；导入/清理脚本引用同步。因深度依赖 Product 内部 Repository，样例留在本模块而非独立 Demo 模块。
- 验收：`Service/` 无 Hanfu 残留；定向 Sample 单测；文档 `REQ-PRODUCT-0034`。
- 版本：`Weline_Product 1.0.97 → 1.0.98`。

## 2026-09-04 — 1688 汉服整店导入与前台商品投影收口

- 当前状态：`implemented_pending_browser_acceptance`。
- 导入规范：只接收经标题与主图复核的汉服；供应商必须保存核验后的 1688 店铺地址与完整公司地址。商品主图、图库、详情图和规格图全部本地化；颜色/款式与尺码只写 Product/Offer EAV，规格图只写 Media `role=variant` 与 canonical `combination_key`，`type_config_json` 必须为 `null`。缺少的尺码先加入「汉服」属性集后再落 Offer EAV，不允许以 JSON 代替。公开 SKU 使用短语义前缀与稳定指纹，Handle 使用品牌与标题语义，SEO 标题、描述、关键词随商品写入。
- 首批真实结果：曹县蕊宝服饰有限公司整店 255 款，严格筛选后导入并发布 22 款汉服（商品 86–108，跳过保留商品 47），共 260 个 Offer 与 260 条规格媒体；运行时核对 22/22 商品页 HTTP 200、260/260 Offer 可投影、152 个唯一本地图片引用均有实体文件、详情无远程图片、全部 `type_config_json=null`、SKU 最大 31 字符。
- PDP 根因与修复：旧的全局已发布 Offer 投影最多取 200 条，导致深层商品 Handle 查找不到且部分商品规格被截断。详情页现先按 `source_slug`/`slug` EAV 精确定位 Product，再只构建该商品全部已发布 Offer；旧投影仅保留为历史数据兼容回退。
- 列表根因与修复：全局缓存过去按前 200 个 Offer 截断，一个多规格商品会占满目录。目录缓存现每个商品只保留一个代表 Offer，最多 2000 个商品；PDP 仍读取目标商品的完整 Offer 集。`/categories` 实测第一页 24 个唯一商品、第二页 2 个，蕊宝 22 款全部可分页访问。
- 回归：`StorefrontCatalogDeepSlugRuntimeTest` 先复现深层商品 301，再验证精确 Handle 返回 200 且完整投影 2 个 Offer；`StorefrontCatalogListingRuntimeTest` 先复现目录仅 2 个唯一商品，再验证首批 22 款均能进入列表。真实批次校验同时覆盖 EAV 双轴、规格切图、短 SKU、本地媒体、详情、SEO、供应商和商品 47 保留。
- 后续批次：佳沫 16/16、儒文 13/13 均为非汉服，整店零入库；全胜 4、悦雅霓裳 7、舞美人生 14 个视觉汉服候选因 1688 详情风控未取得完整规格，完整性门槛已阻止残缺商品发布，待已登录浏览器恢复后继续补齐。

## 2026-09-04 — 织艺谱系归属迁移到 Theme

- 当前状态：`accepted_compatibility_runtime`。
- 变更：Product 删除 `textile-heritage` 注册与首页注入；旧 `TextileHeritageCatalog` 改为 Theme 数据代理，旧模板委托 Theme 渲染器，兼容已持久化布局。
- 边界：真实馆藏素材、来源许可、模板、样式、默认首页注入与后续复用均由 `Weline_Theme` 维护。
- 验收：Product 契约断言“不再拥有注册”并校验代理数据无 SVG；真实 WLS 首页由 Theme 注册渲染 6 项本地真实素材，首页与 6 个图片 URL 均 HTTP 200。

## 2026-09-03 — 织艺谱系部件归属 Product 并写入默认项（1.0.96）

- 当前状态：`implemented`。
- 问题：首页织艺谱系仅靠 Theme `brand-logos` 空列表运行时硬编码，部件参数侧无默认项，亦无配图与链接；归属也不在 Product。
- 修复：新增 `textile-heritage` 部件 + `TextileHeritageCatalog`；`params.brands.default` 与 `default_injections`（`homepage-brands`）写入六项（云锦/宋锦/蜀锦/苏绣/妆花/花罗）含静态图与 `/search?q=` 链接；Theme 首页去掉内嵌 `brand-logos`。
- 验收：契约测；首页 Browser 可见六项图与可点搜索链接。
- 版本：`Weline_Product 1.0.95 → 1.0.96`。

## 2026-09-03 — 首页新品部件空结果（1.0.97）

- 当前状态：`implemented`。
- 问题：`homepage-new-arrivals` 调用 `newArrivalCards` 恒为空；本地已发布商品 `created_at` 在 30 天内，但 SKU 为导入前缀（非 `HF-*`），方法只保留汉服 Offer 且无回退。
- 修复：对齐 `cards()`——HF 优先后补齐全部已发布 Offer；日窗仍无结果时回退 `cards()` 并标注 `is_new`。
- 验收：契约测；CLI `newArrivalCards(8,30)` 非空；首页 Browser 可见新品卡。
- 版本：`Weline_Product 1.0.96 → 1.0.97`。
- 备注：Theme 密封编辑 `CONTEXT_TARGET_UNAVAILABLE`，首页空态隐藏走原生精确路径。

## 2026-09-03 — 店面列表/分类分页（1.0.95）

- 当前状态：`implemented`。
- 问题：`/products`、`/category/*` 按 Offer 拆卡后一页渲染过多卡片（HTML 可达约 1.8MB），TTFB 与 body 偏大。
- 修复：`StorefrontCategoryListingFilter::paginate` 默认 24/页；Catalog/Category 控制器切片并下发页码链接；模板展示区间与分页导航。
- 验收：契约测；curl `/products` 与 `?page=2` 卡片数与体积对照。
- 版本：`Weline_Product 1.0.94 → 1.0.95`。

## 2026-09-03 — 详情页冷路径分钟级卡顿（1.0.94）

- 当前状态：`implemented`。
- 问题：`/product/hanfu-1688-604560496347` TTFB 可达约 100s；WLS `fpc=build_lock`，槽位 widget 重复 catalog/file_asset 解析。
- 修复：`rememberPublishedOffers` 将 `resolveOffers` 纳入热缓存；`resolveReference` 请求内去重；`enrichOffer`/reviews/related/cross-sell 在已有 PDP 快照时不再全量扫描 `cards(48)`/`publishedOfferBySlug`。
- 验收：单测；curl 商品页 TTFB 与 timing.log `action_execute`/`widget include_ms` 对照。
- 版本：`Weline_Product 1.0.93 → 1.0.94`。

## 2026-09-03 — 详情页技术细节置于详情之上（1.0.93）

- 当前状态：`implemented`。
- 问题：前台商品详情区默认先「关于该商品」再「技术细节」，用户希望技术细节在上。
- 修复：`product-info.phtml` 调换两区块顺序；契约断言 testid 先后。
- 验收：契约测；Browser 打开含规格与详情的商品页，技术细节在关于该商品之上。
- 版本：`Weline_Product 1.0.92 → 1.0.93`。

## 2026-09-03 — 规格公开 URL 改用 option code（1.0.92）

- 当前状态：`implemented`。
- 问题：详情选规格后地址栏为 `color=76&size=58` 等 EAV option 数字 id，列表深链也曾落到 opaque UUID。
- 修复：`StorefrontEavLabelResolver` 提供 id↔code；`Detail`/widget 解析前 canonicalize；`product-info` `syncUrl` 写 code；列表 `StorefrontOfferDetailQuery` 优先轴 code，无组合回退 `offer` UUID。内部 combination 仍用 option ID。
- 验收：契约测；Browser 详情 URL 为 `color=lan-se-jin-s&size=m90-100jin`，带 code 深链可预选。
- 版本：`Weline_Product 1.0.91 → 1.0.92`。

## 2026-09-03 — 列表子产品卡片深链预选规格（1.0.91）

- 当前状态：`implemented`。
- 问题：`/products` 等多规格商品按 Offer 拆卡展示，但整卡链接只有 `product/{slug}`，进入详情后默认落在首个规格，未选中所点子产品。
- 修复：catalog / category / storefront-offer-card 在存在 `global_offer_uuid` 时以 `@url{$path|['offer'=>$uuid]}` 深链；详情侧既有 `resolveSelectedOffer` 与 `variant_catalog.selected` 负责预选。
- 验收：契约测；Browser 从 `/products` 点击具体 SKU 卡进入详情，规格轴与加购 UUID 对应该 Offer。
- 版本：`Weline_Product 1.0.90 → 1.0.91`。

## 2026-09-03 — 汉服旧树缺图分类回填配图（1.0.90）

- 当前状态：`implemented`。
- 问题：店面「汉服」旧分类树（形制/用途/材质等）23 个节点无 `image`/`banner`，材质分类页子类无图标；女装等新树 28 个节点已有 R2 方图/横幅。
- 修复：新增 `scripts/remediate-hanfu-legacy-category-images.php`，按语义映射复用既有 `catalog/hanfu/r2/categories` 像素，为目标 code 落盘 icons/banners 并写入三 locale EAV；刷新店面分类缓存。
- 验收：`--verify` 通过；Browser 打开 `/category/hanfu/material` 可见分类横幅与子类图标。
- 版本：`Weline_Product 1.0.89 → 1.0.90`。

## 2026-09-03 — 库存矩阵 Offer/SKU 展示规格缩略图（1.0.89 增量）

- 当前状态：`implemented`。
- 问题：商品编辑「类型与库存」矩阵仅有 SKU 文本，多规格时难以按图识别。
- 修复：`withInventoryOfferImages` 从 Offer 矩阵行挂 `image_url`/`image_is_placeholder`；库存表 Offer/SKU 列内嵌与变体表同款缩略图（含占位图与悬停放大）。
- 验收：契约测；Browser 打开可配置商品编辑页库存面板，每行 Offer 旁可见缩略图。

## 2026-09-03 — 媒体资源点击打开文件管理器翻译（1.0.89）

- 当前状态：`implemented`。
- 问题：Website 图片媒体资源列只能看 UUID/缩略图，无法就地打开文件管理器编辑 FileAsset 描述与多语言翻译。
- 修复：资源列缩略图+文件名改为可点按钮；打开既有媒体 iframe 并追加 `asset_id`；MediaManager 新增 `find_asset` 与 URL 聚焦打开详情/locale workbench。
- 验收：契约测通过；Browser 商品 `#84` 点击主图资源打开媒体 iframe，URL 含 `asset_id`，预览区可见「多语言翻译」工作台。
- 版本：`Weline_Product 1.0.88 → 1.0.89`；`Weline_MediaManager → 1.3.2`。

## 2026-09-03 — Offer 无规格图改用 Theme 占位图（1.0.88）

- 当前状态：`implemented`。
- 问题：无 `role=variant` 媒体时回退主图会误导运营以为该组合已有规格图；破折号 `—` 也不如占位图直观。
- 修复：移除 `offerMatrix` 主图/图集回退；空图统一 `StorefrontImagePlaceholder::url()` 并标记 `image_is_placeholder`；编辑模板与 JS 始终渲染 `<img>`，占位图禁用悬停放大。
- 验收：契约测；Browser 商品 `#84` 的 `color=63` 行为 Theme 占位 SVG，非主图、非破折号。
- 版本：`Weline_Product 1.0.87 → 1.0.88`。

## 2026-09-03 — Offer 组合图缺规格时回退主图并放大缩略图（1.0.87）

- 当前状态：`implemented`。
- 问题：编辑页 Offer 表「产品图片」对无 `role=variant` 媒体的组合显示 `—`（如商品 `#84` 的 `color=63`）；有图时 2.5rem/64px 缩略过小，用户反馈图看不清楚。
- 修复：`offerMatrix` 在 combination/swatch 均无图时回退 `main`→`gallery`；`displayableImageUrl` 非 asset 预览改为 192px；Offer 表缩略图放大并可悬停放大。
- 验收：契约测；Browser 商品 `#84` Offer 表 `color=63` 行可见可辨认图片。
- 版本：`Weline_Product 1.0.86 → 1.0.87`。

## 2026-09-03 — 后台 Offer 矩阵按规格展示产品图（1.0.86）

- 当前状态：`implemented`。
- 变更：编辑页 Offer 组合表增加「产品图片」列；`ProductAdminReadService` 从 `role=variant` 媒体按 `combination_key`（非尺码轴回退）挂图，`ProductAdminMediaPresenter` 解析 `asset://` 为后台可展示 URL；规格轴色板同步回填。
- 保护：媒体面板不再编辑/误改 variant 行；保存时 JS 与 CommandService 双端保留既有规格图，避免主图/图集同步冲掉规格媒体。
- 验收：契约测；Browser 商品 `#84` Offer 表对有规格图的组合可见缩略图。

## 2026-09-03 — 编辑页 Website 分类改用万能分类选择标签（1.0.85）

- 当前状态：`implemented`。
- 问题：商品编辑「分类与媒体」Website 分类仍用手写 checkbox 卡片网格，无法按官方万能分类标签正常选择/回填。
- 修复：改用 `<w:catalog:category:select space="product">`；`value` 由 `category_assignments` 按 position 回填；`collectCategoryAssignments` 改读 `WelineCatalogCategorySelect['product-edit-categories']`。
- 验收：`ProductAdminSurfaceContractTest`；Browser 商品 `#84` 分类面板芯片回填「日常常服套装」「套装专区」，可勾选新增「女装」。
- 版本：`Weline_Product 1.0.84 → 1.0.85`。

## 2026-09-03 — 商品基础信息 LocalModel 与发布诊断名称回退（1.0.83）

- 当前状态：`implemented`。
- 问题：商品 `#84` 发布诊断报 `product_name_required`，尽管 EAV 已有 `locale=zh_Hans_CN` 的 name；编辑页基础信息缺少 SEO 与官方多语言入口。
- 修复：`ProductValidationContext` 空 locale 回退任意语言行；新增 `Model/Product/LocalDescription`（name/短描述/SEO）并与 EAV 双向同步；编辑页基础信息接入 `<local>`；保存 payload 写入 SEO 字段。
- 验收：`BuiltInProductValidationTest` / `ProductAdminSurfaceContractTest` 通过；商品 `#84` 诊断探针 `valid=true` 且无 `product_name_required`；Browser 发布诊断「0 项阻断」，基础信息可见名称/SEO 与 Local 触发按钮。

## 2026-09-03 — 编辑页 Website 图片媒体预览缩略图（1.0.84）

- 当前状态：`implemented`。
- 问题：商品编辑页「Website 图片媒体」资源列仅显示 FileAsset UUID/MIME，无法直观辨认图片。
- 修复：`ProductAdminMediaPresenter::presentAssignment` 经 FileAsset `resolveUrl(purpose=preview)` 补齐 `preview_url`；`edit.phtml` 资源列渲染 `w-product-media-thumb`；`appendProductMediaRow` 对齐创建页用 `resolveMediaPickerFileUrl`。
- 验收：契约测 / Presenter 单测；Browser 打开含媒体的商品编辑页，资源列可见缩略预览图。

## 2026-09-03 — 后台 Offer 矩阵轴名改为属性标签（1.0.82）

- 当前状态：`implemented`。
- 变更：规格轴左侧不再使用随意文本输入框；展示为属性标签（名称 + code），新增轴时从属性目录下拉选择。
- 验收：契约测；Browser 商品 `#84` 规格轴左侧为「颜色」类属性卡片而非输入框。

## 2026-09-03 — 后台 Offer 矩阵颜色标签胶囊（1.0.82）

- 当前状态：`implemented`。
- 变更：编辑页多规格 Offer 矩阵规格值改为可勾选颜色/标签胶囊（复用创建流 chip 视觉），并支持自定义值回车追加；规格组合列改为胶囊展示（含色块/图）；组合标签优先回填属性目录名称与色块。
- `ProductAdminReadService` 回填轴选项时附带 `swatch_color`/`swatch_image`。
- 验收：契约测 `ProductAdminSurfaceContractTest` / `ProductCreateVariantFlowContractTest` 通过；Browser 商品 `#84` 规格与价格面板可见可选颜色胶囊与组合胶囊（如「女款白色长袖上衣＋红短裙505 / S」）。

## 2026-09-03 — EAV 规格切换同步可见 Offer SKU

- 当前状态：`implemented_pending_browser_acceptance`。
- RED：真实前台从白色切换到蓝色后，URL、缩略图选中态和规格主图已更新，但页面 SKU 仍显示切换前白色 Offer；模板缺少可定位 SKU 节点及 `exactOffer.sku` 文本同步。
- 修复：为 SKU 文本增加稳定定位标记，并在同一 `renderPurchaseButtons()` 精确 Offer 投影中同步短 SKU；规格事实仍只来自 EAV/Offer，不写入任何高级 JSON。
- 回归：`ProductDetailTemplateContractTest` 覆盖 SKU 节点和 exact Offer 更新逻辑；继续以真实浏览器核对颜色/尺码、规格图片、URL、SKU 与加购购物车一致，不进入结算或支付。
- 前台地址：https://p05113ef3.test.weline.com:9555/product/zhi-zao-si-min-guo-feng-nu-zhuang-tang-zhuang-su-yi-chan-cha-fu-gai-l-9dfa4e25

## 2026-09-03 — 1688 汉服完整详情与详情图本地化（1.0.82）

- 当前状态：`implemented_pending_browser_acceptance`。
- RED：首商品页面缺少 data-testid=product-description，现有快照只有移动端标题、主图、规格和变体；导入脚本用来源/供应商/公开规格拼成 description，未请求桌面 description.fields.detailUrl；模板又统一转义描述。
- 详情链路修复：在现有 OfferDetailParser/AcceptedOfferDetailEnricher 内补公开 offer_details 解析、HTML 白名单清洗和仅汉服详情抓取；详情图经 FileManager 落盘并以 asset:// 持久化，且不加入主图图库；Storefront 媒体边界解析资产并二次清洗后才允许模板渲染。
- 真实容量根因：重跑 `604560496347` 时卖家完整详情解析出 64 个 img 引用、56 个唯一 URL（主商品正文 46 个，促销内容另有 10 个），图库、颜色规格图和详情图合计超过旧 `MAX_IMAGES=64`。MediaImporter 的有限上限调整为 128，URL 白名单、去重、单图体积、MIME 与像素校验不放宽，并新增容量契约回归。
- 真实源 404 根因：逐 URL GET 审计确认 55 个唯一详情 URL 可访问，卖家 offer_details 中的 `12213303042_1568160227.jpg` 源 CDN 返回 404。MediaImporter 仅对 detail-only 下载异常逐张跳过并返回 `skipped_detail_media`；主图/图库/规格图仍严格失败。OfferDetailParser 只移除调用方明确审计跳过的 img，导入报告写入跳过计数与明细。
- 幂等与逐商品品牌：reuse-snapshot 分别执行缺失来源品牌和详情补齐；移动 propsList/桌面 CpvEnhance 的品牌名称优先，按名称复用或创建 Website 品牌，CLI 品牌仅作源页明确无品牌时的回退。真实运行确认移动商品页会被 1688 间歇拦截，因此同一次品牌补全先读移动页、失败后读同 Offer 桌面页，两边都失败才阻止写入；桌面实页已解析 `687945556432=柒依阁`、`896875413963=柒依阁`，移动实页已解析 `604560496347=梦绘汉唐`。既有 configurable 商品按 source_offer_id 更新，不重复创建；商品规格仍只来自 EAV/Offer，未恢复高级 JSON。
- 受保护历史商品共存：订单预留 `b3e068e4-375d-48cf-8587-63f6ed8cd069` 关联的 legacy simple 商品 47 不取消、不删除、不修改。同源查找只选择 configurable 作为更新目标；只有 legacy simple 时创建新的 configurable sibling，报告记录 `preserved_legacy_product_ids=[47]`，后续复跑复用 sibling。
- 验收门禁：MediaImporter/Hanfu1688/Cart/Product 定向测试、三件汉服数据与 HTTP 探针、Weline Chrome 前台详情图/规格切图/短 SKU/加购及后台 EAV 与规格图片回读；不进入结算或支付。
- 前台地址：https://p05113ef3.test.weline.com:9555/product/zhi-zao-si-min-guo-feng-nu-zhuang-tang-zhuang-su-yi-chan-cha-fu-gai-l-9dfa4e25
- 第三件商品地址：完成成功重导并探活语义 handle 后记录。
- 后台地址：完成实际后台回读后记录。

## 2026-09-03 — cross-sell 空 HTML 健康误报（1.0.81）

- 当前状态：`implemented`
- 问题：DEV toast「经常一起购买 @product-cross-sell：部件 HTML 为空」。
- 根因：`cross-sell.phtml` 在 `bundleCards` 少于 2 个时 `return` 空串，触发 `WidgetHtmlHealthInspector`。
- 修复：对齐 recommended/related——输出 `hidden` 空态壳（`cross-sell-empty`）。
- 验收：`CrossSellEmptyShellContractTest`；详情页不再出现该 toast。
- 版本：`Weline_Product 1.0.80 → 1.0.81`。

## 2026-09-03 — 分类筛选交由 Filters 部件（1.0.80）

- 当前状态：`implemented_pending_browser_acceptance`。
- 变更：删除 Product `category`/`product-list` `filters-sidebar` Hook；Category/Catalog 可选应用 `Weline_Filters` 的 `af_*` 属性筛选；布局槽由 Filters `default_injections` 填充。
- 版本：`Weline_Product 1.0.79 → 1.0.80`。

## 2026-09-03 — 商品详情放大镜定位防盖主图（1.0.79）

- 当前状态：`implemented_pending_browser_acceptance`。
- RED：智能定位在右侧空间不足时翻到主图左侧并夹到 `left=12`，放大预览盖住主图。
- 修复：`placeZoomResult` 以整块 gallery 右缘为锚点；优先右侧（可叠购买信息）；不足时缩小预览，禁止翻到主图上。
- 版本：`Weline_Product 1.0.78 → 1.0.79`。

## 2026-09-03 — `/categories` 根列表空侧栏修复（1.0.78）

- 当前状态：`implemented_pending_browser_acceptance`。
- RED 证据：`/categories` 左侧 `category-layout__sidebar` 预留 220px，但 `category::filters-sidebar` 在无叶子分类 path 时 `return`，侧栏空白；`/category/*` 与 `/products` 正常。
- 根因：`StorefrontCatalogSurfaceResolver` 将 `/categories` 设为 `category` 布局；Catalog 不赋值 `storefront_category`；筛选 Hook 无法 resolve 空 path 后提前退出。
- 修复：根列表合成「全部类目」上下文，继续渲染部门树与价格筛选；当前根节点高亮为 current。
- 版本：`Weline_Product 1.0.77 → 1.0.78`。
- 验收：契约测 `testFiltersSidebarKeepsRootCategoriesListingFilled`；本机 Browser 打开 `/categories` 可见部门/价格侧栏。
- 前台地址：https://p05113ef3.test.weline.com:9555/categories

## 2026-09-03 — 商品详情主图放大镜（1.0.77）

- 当前状态：`implemented_pending_browser_acceptance`。
- 需求：用户在商品详情页要求主图「加一个放大镜」。
- 实现：`product-info` 主图舞台增加透镜与侧向放大预览；缩略图点击与规格图库刷新后同步放大源；`≤992px` 降级关闭。
- 版本：`Weline_Product 1.0.76 → 1.0.77`。
- 验收：契约测锁定 `data-image-zoom` / zoom result；本机 Browser 悬停主图验证。
- 前台地址：https://p05113ef3.test.weline.com:9555/product/hanfu-1688-604560496347

## 2026-09-02 — 产品 SKU 过长缩短（汉服导入/矩阵）

## 2026-09-03 — legacy 分类系统 SEO 标题国际化

- 当前状态：`fixed`（等待本轮真实路由与浏览器复验）。
- RED 证据：清除 FPC 后 `/en_US/categories` 仍输出 `分类 | 分类页面布局`；直接 provider 用例同样没有返回英文 title。
- 根因：系统默认值 allowlist 仅包含“分类页布局”，遗漏线上仍存在的“分类页面布局”历史变体。
- 修复：补齐标题与 `- Weline Framework` 描述变体；判定仍为精确 allowlist，自定义商家 title/description 保持不变。
- 版本：`Weline_Product 1.0.71 → 1.0.72`。

## 2026-09-03 — 非中文商品投影保真与 locale 连续导航（1.0.75）

- 当前状态：`implemented_pending_browser_acceptance`。
- 运行时复现：同一商品中文 PDP 有 12 张真实图；`en_US/ar_SA` PDP 的真实 FileManager 引用被清空并显示占位图，title/H1 回退中文；新品卡 href 丢失 locale。
- 根因：媒体 URL 解析强制要求目标 locale 元数据，未把商品像素视为 locale-neutral 事实；商品文案解析又会直接采用 Website 默认中文；共享卡片路由固定从根路径开始。
- 修复：FileManager 引用按“目标 locale → en_US → Website 默认”解析媒体；非汉字语言遇汉字候选时继续既定 overlay 链，最终使用 SKU/ID 的事实型英文标签；卡片路由拼接 `Url::getPrefix()`。不引入运行时机器翻译，不改价格、库存或商品事实。
- 验收门禁：新增媒体回退与文案投影单测，运行 Product focused PHPUnit、section/layout 扫描、模块升级、缓存清理、三语言 PDP/新品/SEO 探针及本机内置 Browser 点击链路。
- 前台地址：https://p05113ef3.test.weline.com:9555/product/zhi-zao-si-zhong-guo-feng-han-fu-chu-gao-zhong-xue-sheng-yun-dong-hui-9c4259d4 、https://p05113ef3.test.weline.com:9555/en_US/product/zhi-zao-si-zhong-guo-feng-han-fu-chu-gao-zhong-xue-sheng-yun-dong-hui-9c4259d4 、https://p05113ef3.test.weline.com:9555/ar_SA/product/zhi-zao-si-zhong-guo-feng-han-fu-chu-gao-zhong-xue-sheng-yun-dong-hui-9c4259d4 、https://p05113ef3.test.weline.com:9555/en_US/new-arrivals 、https://p05113ef3.test.weline.com:9555/ar_SA/new-arrivals
- 后台/API：N/A（本修复未新增后台或 API 表面）。

## 2026-09-03 — 国际汉服目录表面与本地化降级

- 当前状态：`implemented`（真实 Browser 操作与视觉验收待完成后收口）。
- RED 证据：`/products` 与 `/categories` 运行时共享“商品列表”标题、描述与 H1；`en_US/ar_SA` 商品详情在缺少当前语言属性时退化为数字来源 slug。
- 根因：共享 Catalog 控制器覆盖了真实公开路由；Product 投影器只读取当前语言与空 locale，没有国际站安全降级链。
- 实现：新增 `StorefrontCatalogSurfaceResolver`，按真实 URL 保留商品、分类、热销榜的页面语义与排序链接；新增 Product SEO provider，在保留商家自定义元数据的前提下提供中英文独立默认值；属性降级顺序调整为“当前 locale → 非中英文时 en_US → Website 默认语言/zh_Hans_CN → 空 locale”，cleared 仍终止继承；补充 `ar_SA` 商城核心文案的英文安全降级。
- 版本：`Weline_Product 1.0.70 → 1.0.71`。
- 验收命令：focused PHPUnit、PHP/PHTML lint、`php bin/w setup:upgrade --module=Weline_Product`、`php bin/w i18n:collect Weline_Product`、三语言真实 HTTP 矩阵及本机内置 Browser。
- 前台地址：https://p05113ef3.test.weline.com:9555/products 、https://p05113ef3.test.weline.com:9555/categories 、https://p05113ef3.test.weline.com:9555/best-sellers 、https://p05113ef3.test.weline.com:9555/product/hanfu-1688-604560496347
- 后台地址：N/A（本次不新增后台路由）。
- API 地址：N/A（本次不新增 API）。

## 2026-09-06 — 目录缓存解析与回源 timing

- 复用 Framework `measurePhase` 增加 `product.catalog.resolve` 和 `product.catalog.build`；后者只包裹现有 CachePolicy 的回源函数，保留原缓存、范围、返回值和失效机制。
- 同请求是否实际调用build用于辨认是否回源；不把未发生build直接解释为某一缓存层命中。既有products/offers/snapshots/attributes/projection继续供细分分析。
- 验证与真实日志见 `/private/tmp/weline-timing/acceptance-report.md`；本轮只增加观测，不改变商品投影或分页逻辑。
- 交付地址：https://p05113ef3.test.weline.com:9555/en_US/products?nocache=1&wls_trace=1 。无UI/新API。

## 2026-09-02 — PDP 派发 product_viewed

- 当前状态：`implemented`
- 变更：`Controller/Frontend/Detail` 在成功解析报价后派发 `Weline_Product::product_viewed`（供 RecentlyViewed / Affiliate 等监听）；版本 `1.0.68` → `1.0.69`。
- 验证：php_lint；RecentlyViewed Observer 契约。


- 当前状态：`accepted`
- 问题：汉服导入把「品牌+标题拼音」整段写进 `sku_prefix`，规格 option code 又长达 48，变体 SKU 中位约 115、最长 122，后台/购物车难读。
- 策略：公开 SKU 改为 `品牌短码-内容指纹8位`；规格 token 超过 12 字符压缩为 `前缀-4位指纹`；SEO Handle/`meta_*` 仍用品牌+标题语义拼音，不把 1688 编号写进公开 SKU。
- 实现：`hanfu1688Identity`、`OfferEavMapper::{optionCode,variantSku,compactSkuToken}`、`ProductVariantMatrixService::generatedSku`、后台 `generatedVariantSku`；存量长 SKU 用 `scripts/remediate-long-product-skus.php` 经 `renameSku`+alias 重映射（本地 75 Offer，avg 38 / max 42）。
- 验收：`Import1688HanfuCatalogContractTest`、`ProductVariantMatrixServiceTest`；店面 PDP Browser 显示 `ZHIZAOSI-9DFA4E25-BAI-SE-D60D-S80-90JIN`。

## 2026-09-02 — 店面分类导航缓存语言隔离（1.0.65）

- 当前状态：`fixed`
- 问题：中文站头部分类导航偶发阿语/英语（`نساء`/`Hanfu`），疑似热缓存串 locale。
- 根因：`StorefrontAllMenuCategoryTreeService` 经 `StorefrontScopeHotCache` SWR 刷新时，`build()` 实时读 `State::getLangLocal()`，可与 `KeyBuilder` 冻结的 `lang` 维度错位，把阿语树写入中文键。
- 修复：`navTree()` 在 remember 时用 `KeyBuilder::storefrontDimensions().lang` 捕获 locale 并传入 `build()`；logical key bump `v3` 清污染。
- 验收：`StorefrontAllMenuCategoryTreeServiceTest`；中文 `/` 与 `/ar_SA` 导航语言各自正确。

## 2026-09-02 — 热销榜 hero 文字配色对比度

- 当前状态：`fixed`
- 问题：深色 hero 上「热销榜」被全局 `h1` 深蓝覆盖，几乎不可读。
- 修复：提高选择器优先级，标题/说明强制浅色；眉题改为 `#ffd814`。
- 验收：`/best-sellers` hero 标题白字可读。

## 2026-09-03 — 国际化新品集合页（1.0.73）

- 当前状态：`implemented_pending_browser_acceptance`
- 根因：商城导航已公开 `/new-arrivals`，Product Router 与页面入口未落地，三种 locale 上下文均返回 404。
- 实现：新增新品路由与控制器，复用 `newArrivalCards` 并以真实目录卡片兜底；复用 Theme 默认 `product_list` 布局，新增水墨设计令牌驱动、RTL/响应式兼容的新品模板；补齐国际化 SEO surface、标题白名单和中英阿文案。
- 验证：`NewArrivalsPageContractTest`、三 locale HTTP、canonical/hreflang 与内置 Browser 导航验收。

## 2026-09-03 — 新品页完整布局与导入目录回退（1.0.74）

- 当前状态：`implemented`
- 运行时复现：`/new-arrivals`、`/en_US/new-arrivals`、`/ar_SA/new-arrivals` 均为 200，但响应缺少 `<!DOCTYPE html>` 且显示 0 件；`/products` 同时返回完整 Theme 文档与商品。
- 根因：新品控制器只写 request 的 `layout_type`，未同步 `FrontendController::$layoutType`；通用卡片回退又强制 HF SKU 前缀，国际导入商品没有该约定时返回空集。
- 修复：显式选择 `product_list` 布局；目录卡保持 Hanfu 优先，并从全部已发布商品补齐剩余卡位，不改跨模块扩展点。
- 验收门禁：`NewArrivalsPageContractTest`、`setup:upgrade --module=Weline_Product`、三语言 HTTPS 探针与内置 Browser 操作用例。
- 前台地址：https://p05113ef3.test.weline.com:9555/new-arrivals 、https://p05113ef3.test.weline.com:9555/en_US/new-arrivals 、https://p05113ef3.test.weline.com:9555/ar_SA/new-arrivals
- 后台/API：N/A（本修复未新增后台或 API 表面）。

## 2026-09-02 — 热销榜页面与 best_sellers 布局（1.0.64）

- 当前状态：`implemented`
- 需求：导航已有 `/best-sellers`，需 Product 落地页面并做成独立布局。
- 实现：`Router` 映射短路径；`Controller/Frontend/BestSellers` 设 `layoutType=best_sellers`；模块贡献 `layouts/best_sellers/default.phtml`；模板展示 Top3 领奖台 + 排名列表；`StorefrontProductWidgetCatalog::bestSellerCards` 提供排序与销量展示字段。
- 验收：`BestSellersPageContractTest`；实例 `/best-sellers` Browser 探活。

## 2026-09-02 — 1688 汉服导入回归 EAV 唯一规格源

- 当前状态：`implemented_pending_browser_acceptance`。
- 真实故障根因：Product 属性写入会把选项 code 规范化为 EAV option ID，而旧的 Offer 写入和 `combination_key` 仍保留 code，发布校验因此报 `variant_combination_value_not_allowed`。
- 唯一规范化边界：`ProductAttributeMetadataCatalog` 负责把输入的选项 code 或 ID 都转换为 canonical EAV option ID；后台创建、后台矩阵保存、正式导入和遗留矩阵种子必须复用该入口。
- 正确模型：后台表单 `axes_json` 仅是请求传输格式；商品允许值写入 Product EAV multiselect，单个 Offer 已选值写入 Offer EAV select，两边以及 `combination_key` 都使用同一 canonical EAV option ID。`combination_key` 只是排序后的非 JSON 索引，不是另一份规格事实。
- 删除旁路：configurable 商品不再把 `axes` 写入 `type_configuration`，Offer 不再把 `combination` 写入 `type_config_json`；导入器不再生成 `available_colors`、`available_sizes`、`source_public_specs`、`source_variant_combinations` 聚合属性。
- 后台界面：不显示、不读取“高级属性 JSON”来维护规格；缺少的颜色或尺码选项先补到「汉服」属性集，再以 EAV 选项选择规格。
- 属性集：`ProductCatalogEavBootstrap::ensureHanfuAttributeOptions()` 将 1688 实际出现的颜色、尺码和规格参数补入「汉服」属性集；默认尺码补齐 XXL、XXXL，前端按 EAV 标签显示。
- 选项身份幂等：同一属性内先按规范化显示值复用最早的 EAV option ID；生成 code 仅用于首次创建，后续 code 长度或算法变化不得复制选项、替换旧 ID 或破坏既有 Offer 组合。
- 重导规格匹配：导入器在读取既有 Offer 版本前，必须先把来源 option code/显示值解析成正式 EAV option ID，并以 canonical `combination_key` 匹配；同一规范化结果同时用于 Product 允许值、Offer 已选值、价格库存和 variant media，禁止先用来源 code 匹配、保存阶段才转换。
- 发布校验范围：矩阵重建后被标记为 `disabled`/`archived` 的历史 Offer 不再参与数量、SKU、价格、库存或 EAV 规格合法性校验；`draft`、`active`、`published` 等仍可发布的 Offer 必须完整通过原校验。
- 发布状态守恒：商品发布只发布当前仍可售的 Offer，必须跳过并保留 `disabled`/`archived` 历史行；禁止把矩阵重建刚淘汰的组合重新改回 `published`，否则会重复暴露规格并破坏 variant media 完整性。
- 导入身份：`source_offer_id` EAV 是 1688 幂等来源键；SKU 与 Handle 由品牌和标题语义生成，1688 编号不进入公开 SKU/URL；SEO 标题、描述、关键词随商品写入。
- 变体经营：导入创建 configurable 商品和真实 Offer，逐 SKU 写价格与库存；图片下载到本地 FileManager，供应商店铺 URL 必须回读一致。
- 规格图片：1688 颜色/款式图片在解析阶段保留到具体规格组合；落库使用 Media role=variant + canonical combination_key 的正式关系，绝不写入规格 JSON。前台只合并当前 Offer 的规格媒体，并把颜色/款式关联图作为选项图板。
- 规格图片出站：`asset://` 只允许作为 FileManager 内部持久引用；前台出站边界必须同时解析 Offer 主图/图库以及 `variant_axes.options` 的 `swatch_image`、`gallery_images`、`gallery_by_color`，同一资产单次缓存解析，失败引用直接移除，HTML 与规格切换数据不得出现 `asset://`。
- SKU 收敛：商品 SKU 使用品牌短码+8 位稳定摘要，变体 SKU 上限 48 字符；重新导入会同步更新历史长 SKU。
- 迁移规则：旧 simple/JSON 旁路商品先按已授权清理流程删除，再由同一来源快照重建；成功后清除旧五类 overlay。
- 前台 EAV 展示：选项 `id`、`code`、`value` 都只作为同一 EAV option 的输入身份，最终统一显示 `label`；技术参数和已选规格不得暴露 `48`、`57` 等内部 ID。
- 变体 SKU：Cart snapshot 与前台变体目录优先使用 Offer 自己的 SKU，仅当 Offer SKU 为空时回退父 Product SKU，避免所有规格显示或入车为同一个默认 SKU。
- 商品 SEO：Detail 控制器保留正文模板的 `product` 赋值，同时把同一份已发布 EAV 商品投影（含 `storefront_offers`）发布到共享页面档案 `seo.product`；独立 Head 模板先读本地 `product`，不存在时回退 `seo.product`。Product 通过 `Weline_Seo::SeoProfileProvider` 扩展点提供标题、描述、关键词、图片及 ProductGroup/variant 事实。由于对象容器不会为带默认值的 nullable 注册器参数自动注入，`PageSeoContextResolver` 在注册器为空时必须通过 ObjectManager 惰性创建 `HeadProviderRegistry`；注册器再按 `SeoProfileProvider` 扩展点过滤并逐 Provider 隔离失败，不能因未注入或无关扩展异常清空有效商品 Provider。规格事实只来自 EAV/Offer，不从 JSON 读取，且内部 `storefront_offers` 上下文不会输出到结构化数据。
- 运行时升级：`Weline_Product` 版本由 `1.0.67` 升至 `1.0.68`；落盘后必须执行 `php bin/w setup:upgrade --route --module=Weline_Product`。
- 真实迁移：来源快照严格过滤后仅接收标题含“汉服”的 3 条；商品 83（54 Offer、4 本地图）与商品 84（21 Offer、4 本地图）已发布，全部 Offer `type_config_json=NULL`，公开 Handle 为品牌+标题语义。旧商品 47 因受保护 reservation `b3e068e4-375d-48cf-8587-63f6ed8cd069` 保留，未擅自取消。
- 验收门禁：canonical EAV、Offer SKU、SEO provider 定向测试和真实 HTTPS/curl 必须通过；Weline Chrome 需完成后台与首商品选择规格、加购（不进入支付）的真实验收后才能改为 `accepted`。

## 2026-09-02 — 推荐产品空 HTML 健康误报（1.0.63）

- 当前状态：`implemented`
- 问题：DEV toast「推荐产品 @category-recommendations: 部件 HTML 为空」（支付取消回跳等页）。
- 根因：`recommended-products` / `related-products` 无卡片时 `return` 空串，触发 `WidgetHtmlHealthInspector`。
- 修复：对齐 Theme featured/new-arrivals——输出 `hidden` 空态壳（`recommended-products-empty` / `related-products-empty`）。
- 验收：`RecommendedProductsEmptyShellContractTest`；取消回调页不再出现该 toast。

## 2026-09-02 — Cart 回包 image 漏出 asset://

- 当前状态：`fixed`
- 问题：加购成功后控制台 `GET asset://UUID net::ERR_UNKNOWN_URL_SCHEME`；迷你购物车 `img.src` 吃到 FileManager 内部协议；Network 失败请求名即 UUID，发起者 `mini-cart-icon.js`。
- 根因：`ProductCatalogCartItemSnapshotResolver::image` 原样返回 Media `PATH`（`asset://…`），经 `cart.add` / `getCart` 出站；Cart 合并行不刷新 `image`，持久化脏值一直粘住。
- 修复：出站前经 `StorefrontProductMediaUrlResolver::resolveReference` 解析为公开 URL；无法解析的 `asset://` 置空；Cart `summary` 出口再消毒；mini-cart JS 拒绝不可展示协议。
- 验收：契约单测；加购后回包/Network 无 `asset://`。

## 2026-09-02 — 推荐产品加入购物车无通知

- 当前状态：`implemented`
- 问题：分类/列表「推荐产品」卡片点击「加入购物车」仅触发 WelineCTA/Pixel，无加购 API 与右上角成功通知。
- 根因一：`recommended-products.phtml` 手搓加购 markup，未挂 `data-weline-load="cart"` / `storefrontShopperToast`。
- 根因二（全局）：Frontend `loadModule` 只加载 `paths[0]`，`cart` 模块第二路径 `product-purchase-actions.js` 从未执行。
- 修复：推荐产品改用 Theme `partials/product/add-to-cart.phtml` + `ProductCardAddToCartParams`；Frontend `weline.js` 按序加载全部 `paths`。
- 验收：契约单测；硬刷新后点击加购出现右上角「已加入购物车」面板。

## 2026-09-02 — 汉服分类、商品媒体与推荐整改（1.0.61）

- 当前状态：`accepted`。
- 数据与素材：28 个汉服分类均配 800×800 形制图和 1500×300 分类横幅；3 个正式汉服商品共 25 张商品图；87 个分类、横幅及商品媒体资产均补齐 FileManager 中英双语元数据。
- 店面媒体：`StorefrontProductMediaUrlResolver` 将 FileManager `asset://UUID` 按当前语言解析为公开媒体 URL，投影与热缓存读取均兜底解析；商品卡与关联推荐保留真实图片，仅在缺图时使用占位。
- 推荐边界：店面推荐仅展示正式 `HF-` 汉服 SKU，按 `product_id` 去重并排除当前商品；移动端关联推荐装饰层限制在容器内，375px 不再横向溢出。
- 数据验证：`remediate-hanfu-commerce-media-r2.php --verify` 返回 categories=28、products=3、product_media=25、file_assets=87、locales_per_asset=2。
- WebUI：Weline Chrome 在 375/768/1024 断点验收中文/英文分类和商品页；无 `asset://`、无占位图、无坏图、无非汉服推荐、无横向溢出。
- 交付地址：
  - https://p05113ef3.test.weline.com:9555/category/men/yuanling
  - https://p05113ef3.test.weline.com:9555/en_US/category/men/yuanling
  - https://p05113ef3.test.weline.com:9555/product/taoyuan-qingmeng
  - https://p05113ef3.test.weline.com:9555/product/shenlong-yin-zhuanghua-mamian
  - https://p05113ef3.test.weline.com:9555/product/zuimeng-xifeng-xianhe-mamian
  - https://p05113ef3.test.weline.com:9555/en_US/product/taoyuan-qingmeng

## 2026-09-01 — 商品编辑页补面包屑并收紧页头

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，精确路径原生收尾）
- 问题：编辑页仅有「返回商品列表」，顶部无官方面包屑；页头层次偏散。
- 实现：`首页 / 商品 / 当前商品名` 使用 `w-breadcrumb`；页头增加「商品编辑」眉题，状态中文化；身份摘要并入 header；契约测断言 breadcrumb，并修正列表 DataTable `testId=product-edit-button`。
- 验收：编辑页顶部可见可点击面包屑；强制刷新（Cmd/Ctrl+Shift+R）后 CSS `?v=20260901-editor-crumb1` 生效。

## 2026-09-01 — all-menu 分类树透传 Banner/描述供 mega-menu

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，精确路径原生实现）
- 需求：店面顶栏 mega-menu 无子分类时展示分类 Banner/描述；有子分类时可选展示。
- 实现：`StorefrontAllMenuCategoryTreeService` 从 catalog flatRows 写入 `image`/`banner`/`summary`/`description`；缓存键升至 `v2`。模块 `1.0.53`。
- 协作：`Weline_Theme` mega-menu-panel + `show_banner_with_children`。

## 2026-09-01 — product-info 可配置显示品牌/供应商

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，精确路径原生实现）
- 需求：产品信息部件可控制是否显示品牌与供应商，默认显示；位置由实现选定。
- 位置：品牌在标题上方（商品身份）；供应商在购买盒价格与库存之间（交易侧）。
- 实现：
  - `product-info` 增加 `show_brand`/`show_supplier`（bool，默认 true）
  - Projector 顶层暴露 `brand`/`brand_code`（仍不进技术细节表）
  - `hydrateDetailOffer` 附带启用中的主供应商 `supplier_name`（注意 `website_id=0` 合法，不可用 `<=0` 跳过）
  - 契约测锁定参数与 `data-testid`
- 模块：`1.0.52`

## 2026-09-01 — 商品编辑页补面包屑并收紧页头

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，精确路径原生收尾）
- 问题：编辑页仅有「返回商品列表」，顶部无官方面包屑；页头层次偏散。
- 实现：`首页 / 商品 / 当前商品名` 使用 `w-breadcrumb`；页头增加「商品编辑」眉题，状态中文化；身份摘要并入 header；契约测断言 breadcrumb。模块 `1.0.51`。
- 验收：编辑页顶部可见可点击面包屑，列表链路回到商品列表。

## 2026-09-01 — 多规格 type_configuration 落库字段丢失

- 当前状态：`superseded`。
- 历史说明：该版本曾把规格轴/组合回退到 `type_configuration` 与 `type_config_json`；此做法已被 2026-09-02「1688 汉服导入回归 EAV 唯一规格源」完整替代。
- 现行约束：规格定义、可选值和 Offer 已选值只走 EAV；组合身份只走 `combination_key`，不得恢复规格 JSON 旁路。

## 2026-09-01 — 供应商/品牌选择器展示缩略图

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，精确路径原生收尾）
- 问题：`<w:product:catalog:select>` 胶囊与下拉仅有文字，未展示品牌 Logo / 供应商图片。
- 实现：
  - `catalogOptions` 供应商补 `image_url`；模板 options 传入 `image`
  - Taglib 归一化并经 `ProductAdminMediaPresenter` 解析可展示 URL；芯片/列表渲染缩略图
  - 模块 `1.0.49`
- 验收：编辑品牌已关联供应商时胶囊可见圆形缩略图；创建页供应商/品牌下拉可见图片。

## 2026-09-01 — 创建后立即发布校验失败可定位

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，精确路径原生收尾）
- 问题：创建向导选择「创建后立即发布」时报「发布校验未通过」，toast 无具体原因；刷新后 `product_type`/`status`/`price` 草稿未回填（`<select>` 被跳过，且 `status` 默认 draft 挡住空值回填）。
- 根因：`forceHydrateScopedHiddenFromDraft` 仅支持 input/textarea；发布失败前已清空草稿；失败消息未带 `diagnostics.errors`。
- 实现：
  - 回填支持 `HTMLSelectElement`/checkbox，并强制回填 status/price 等基础字段
  - 发布失败 toast/后端文案带具体校验项；全成功后再清草稿
  - 提交按钮随 status 切换文案；立即发布缺价时前端拦截
  - 模块 `1.0.47`
- 验收：刷新后类型/状态/售价从草稿回填；立即发布失败可见「Offer 缺少基础价」等具体项。

## 2026-09-01 — 品牌/供应商标签选择与创建级联

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，精确路径原生收尾）
- 需求：关联供应商/品牌改为可搜索胶囊多选（回填、叉叉删除）；万能产品创建先选供应商再级联品牌。
- 实现：
  - 新增 `<w:product:catalog:select>`（`CatalogEntitySelect`）
  - 品牌/供应商管理表单替换 checkbox 列表
  - 创建页供应商→品牌级联（`brand_ids` 映射 + `setAllowedValues`）；`parseIdList` 支持逗号分隔
  - 场景映射表登记；模块 `1.0.46`
- 验收：编辑品牌可见胶囊回填；创建商品选供应商后品牌仅显示关联项。

## 2026-09-01 — 供应商列表改用官方 DataTable

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，按门禁原生精确路径收尾）
- 问题：供应商管理右侧列表仍为手写 `<table class="w-table">`。
- 实现：`suppliers.phtml` 对齐品牌页，改为 `w:d-table mode="local"` + `#product-supplier-local-rows`；图片 `type="image"`；编辑 link / 停用 event→隐藏 CSRF 表单；契约测试补 DataTable 断言。模块 `1.0.45`。
- 验收：供应商管理页出现 `data-w-component="data-table"`；停用仍走 `disable-supplier` + `csrf=auto`。

## 2026-09-01 — 品牌列表改用官方 DataTable

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，按门禁原生精确路径收尾）
- 问题：品牌管理右侧「品牌列表」手写 `<table class="w-table">`，未走官方 `w:d-table`。
- 实现：`brands.phtml` 改为 `mode="local"` + `#product-brand-local-rows` JSON；主图 `type="image"`；操作列 `sticky-actions`（编辑 link / 停用 event→隐藏 CSRF 表单）；契约测试补 DataTable 断言。模块 `1.0.43`。
- 验收：品牌管理页出现 `data-w-component="data-table"`；停用仍走 `disable-brand` + `csrf=auto`。

## 2026-09-01 — 汉服品牌/供应商真实图片回填

- 当前状态：`implemented`（HOST_MCP_NOT_ATTACHED：本会话缺 `submit_task_plan`，精确路径原生）
- 脚本：`scripts/enrich-hanfu-entity-images.php [website_id]`
- 图源：重回汉唐官网 logo、美和/菁莱依官网图、买购/CNPP 公开品牌图、1688 工厂黄页展厅/主图（cbu01）
- 落盘：`/pub/media/catalog/hanfu-entities/{brands|suppliers}/`
- 回填：8 个品牌 `logo_url` + 20 个供应商 `image_url`；品牌列表支持本地 `/pub` 路径展示

## 2026-09-01 — 汉服品牌/供应商真实店铺回填

- 当前状态：`implemented`（HOST_MCP_NOT_ATTACHED：本会话缺 `submit_task_plan`，精确路径原生）
- 问题：种子里大量 `s.1688.com` 关键词检索页冒充店铺，用户要求实查真实工厂/旗舰店。
- 实现：
  - 新增 `scripts/enrich-hanfu-real-stores.php`：回填可核验 store_url，停用 `caoxian-*/suzhou-*` 等检索档口
  - 更新 `seed-hanfu-brands-suppliers.php`：品牌备注官方店；供应商改为官网 + 1688 工厂黄页 title 核验名单（质量优先，不再灌检索伪入口）
- 已核验样例：美和官网、菁莱依官网、柒依阁/花朝记（曹县）/鑫耀/蕊宝/蔡宝/曲瑶/舞美人生/青途/沧海/乐驰/佳沫/星启澜/梦然/全胜/悦雅/儒文/建州；品牌侧十三余/池夏/重回汉唐等

## 2026-09-01 — 汉服品牌/供应商公开资料种子

- 当前状态：`implemented`（已由真实店铺回填修订）
- 脚本：`scripts/seed-hanfu-brands-suppliers.php [website_id]`；回填：`scripts/enrich-hanfu-real-stores.php [website_id]`
- 说明：未公开核实的电话/邮箱不编造；店铺仅写可核验入口。

## 2026-09-01 — 汉服专营：去掉顶层「汉服」壳

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：数据脚本原生执行）
- 问题：分类树唯一顶层是「汉服」，前台首页菜单顶层只剩一项。
- 实现：`seed-hanfu-categories.php` 改为女装/男装/童装/配饰/套装直接顶级；空库重种；`scripts/flatten-hanfu-root-categories.php` 仅提升子节点并 **停用** 汉服壳（禁止 `delete()` 递归清空）。
- 验收：万能分类 / 商品创建分类选择顶层可见多项。

## 2026-09-01 — 分类回填走标签 select API（名称芯片）

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：精确路径原生收尾）
- 问题：草稿陈旧 `category_ids` 不在当前分类树时芯片显示 `#id`；弹出层列表被快速新建挤矮。
- 实现：依赖 Catalog `setValue`/`select` 内部勾选；hydrate 后若 ID 被过滤则 patch 本地草稿。模块 `1.0.41`。

## 2026-09-01 — 创建向导刷新停留在上次步骤

- 当前状态：`implemented`（无 Browser）
- 问题：在「规格维度」勾选后刷新，回到「基础信息」。
- 根因：草稿未就绪时 `variant-axes` 仍 hidden，resume 回退 basics 并 `rememberCreateWizardStep('basics')` 覆盖记忆；刷新后 `data-step-confirmed` 丢失，中间门禁再把步骤打回。
- 实现：步骤未可见时不改写记忆；hydrate 后 `force` 打开上次步骤并 `forceConfirmPreviousStepsForResume`；规格交互时再次 remember。静态 `?v=20260901-wizard-step-v6`。模块 `1.0.45`。

## 2026-09-01 — 动态商品属性改完立刻落盘

- 当前状态：`implemented`（无 Browser；跳过卡住的内置浏览器）
- 问题：属性修改后 Network 无保存请求，刷新无法回填；动态 `innerHTML` 渲染的字段疑似未入监听。
- 实现：`document` capture 监听 input/change；`renderCreateEavFields` 后对节点直绑；落盘后 `flushProductCreateDraftRemoteNow`（不再只靠 scope 5s 防抖）；静态资源 `?v=20260901-eav-draft-v5`。模块 `1.0.44`。
- 验收：改材质后约 0.5s 内应出现 `taglib/backend/scope` POST；刷新后回填。

## 2026-09-01 — 商品属性草稿被隐藏镜像区清空

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：无 Browser；跳过卡住的内置浏览器）
- 问题：填写材质/保养/质保后刷新仍空。
- 根因：`getCreateEavFieldRoots` 含隐藏 `#product-create-eav-editor`（与可见区同字段但未填写）；`persistCreateEavDraft` 按 code 后写覆盖，把可见区已填值写成空 JSON。
- 实现：查询根去掉 editor；不再向 editor 镜像 HTML；校验改为可见属性区。模块 `1.0.43`。
- 验收：填写后刷新，商品属性步应回填原值（硬刷新一次清掉旧空草稿后再测）。

## 2026-09-01 — 商品属性保存与刷新回填

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，按门禁原生精确路径收尾）
- 问题：创建向导填写材质/保养说明/质保等后刷新丢失；创建后编辑页也无法回填（对齐 WeShop 保存可回填任意属性）。
- 根因：
  1. 创建字段 `data-entity-id="0"` 原样写入，属性落到错误实体，编辑读真实 productId 时为空
  2. 隐藏镜像 `#product-create-eav-editor` 与可见区重复采集，易触发 `product_attribute_duplicate`
  3. 晚到草稿水合仅在「UI 空白」时 apply；`warranty_months` 默认值等会挡住回填
- 实现：`writeAttributes` 在 `entity_id<=0` 时绑定 `$productId`；`collectCreateAttributes` 只采可见区并省略 `entity_id<=0`；`shouldApplyCreateEavDraft` + `pagehide`/步骤下一步 flush；模块 `1.0.42`。
- 验收：刷新新建页属性仍在；创建后编辑页可见同值。

## 2026-09-01 — 品牌↔供应商 N:N 挂钩与主图

- 当前状态：`implemented`（HOST_MCP_NOT_ATTACHED：本会话缺 `submit_task_plan`）
- 需求：供应商与品牌多对多挂钩；供应商可选多个品牌；同时补品牌主图 / 供应商图片。
- 实现：
  - 新分片 `supplier_brand`（uk_supplier_brand）；供应商表单 `brand_ids[]`、品牌表单 `supplier_ids[]` 双向同步
  - 品牌 `logo_url`/`logo_asset_id`、供应商 `image_url`/`image_asset_id` + 媒体库选择器
  - Schema `4.5.0`，模块 `1.0.40`
- 验收：供应商编辑勾选品牌保存后列表显示品牌名；品牌编辑勾选供应商对称可见。

## 2026-09-01 — 创建草稿分类芯片强制回填

- 当前状态：`implemented`（HOST_MCP_SESSION_CATALOG_STALE：本会话缺 `submit_task_plan`，精确路径原生收尾）
- 问题：远端/本地草稿已有 `category_ids`，刷新后分类芯片仍「未选择分类」（hidden 或 localStorage 有值但芯片未水合）。
- 实现：`forceHydrateCategoriesFromDraft` 在 `hydrateProductCreateDraftUi`（0/400/1200ms）从草稿或 hidden 读 `category_ids`，当 `getValue()` 为空或芯片数为 0 时 `setValue(..., {silent:true})`。契约断言同步。模块 `1.0.39`。
- 文档：`Weline_Catalog/doc/catalog-category-select标签使用指南.md` §5 补充商品页双保险说明。

## 2026-09-01 — 商品供应商目录与创建供货信息

- 当前状态：`implemented`（HOST_MCP_NOT_ATTACHED：本会话缺 `submit_task_plan`，按门禁原生实现）
- 问题：万能产品创建仅有品牌，缺少可管理的供应商及供应商侧产品地址/供货价等字段。
- 设计：
  - `supplier` 分片：名称/编码/店铺地址(选填)/联系人/默认币种·付款条款·交期·MOQ/备注/状态
  - `product_supplier` 分片：商品↔供应商报价（产品 URL、货号、供货价/标价、MOQ、交期、装箱数、备注）
- 实现：供应商 CRUD（`/backend/catalog/suppliers`）对标品牌；创建向导供应商下拉 + 选中后展开供货信息；创建落库主供应商关联。Schema `4.4.0`，模块 `1.0.38`。

## 2026-09-01 — 创建向导步骤记忆防覆盖

- 问题：刷新后本应回到「属性集」等上次步骤，但早 hydrate 时 basics 未回填导致 resume 失败，并把 localStorage 步骤覆盖成 `basics`。
- 实现：resume 成功才 remember；失败保留原步骤；hydrate 前强制回填 type/name/sku；有记忆步骤时自动钉住创建面板。模块 `1.0.37`。

## 2026-09-01 — 创建草稿回填属性集/属性值/图片

- 问题：刷新后属性集被服务端默认值挡住、动态商品属性与规格预览图未进 `scope=product_create_draft`，媒体回填与远程 GET 竞态，导致属性内容与已传图丢失。
- 实现：本地草稿强制覆盖属性集；隐藏字段持久化 EAV/规格轴/规格图 JSON；选图/填写防抖落盘；hydrate 仅在空 UI 或换集时回填；创建成功仍清空草稿。模块 `1.0.36`。

## 2026-09-01 — 创建向导恢复上次步骤

- 当前状态：`implemented`
- 问题：刷新或再点「新建」总是回到基础资料，无法接着上次停在的步骤继续。
- 实现：`localStorage` 记录当前 `data-create-step`；进入创建/草稿回填后 `resumeCreateWizardStep` 打开该步，并解锁前置已就绪步骤；创建成功后清空。模块 `1.0.35`。
- 验收：停在「规格值预览图」后刷新或再进创建，仍打开该步（前置未完成则落到可继续的最前步）。

## 2026-09-01 — 规格值预览图选图不自动下一步

- 当前状态：`implemented`
- 问题：创建向导「规格值预览图」选一张样本图后自动跳到下一步，无法继续给其它规格值传图。
- 实现：去掉选图成功后的 `maybeAdvanceCreateWizard('variant-values')`；`variant-values` 完成态改为仅点「下一步」确认（与 `variant-preview` 一致）。模块 `1.0.34`。
- 验收：选图后停留本步；点「下一步」才进入后续；契约测通过。

## 2026-09-01 — 万能产品新建/管理面板模式固定

- 当前状态：`implemented`
- 问题：点击「新建」或「管理」后布局只在当前会话内存生效，刷新/再进页面会回到默认双栏。
- 实现：`product-admin.js` 将 `create`/`list` 写入 `localStorage`（`weline.product.catalog.panel_mode`），初始化时读取并恢复；仅点击另一侧按钮才切换。模块 `1.0.33`。
- 验收：选新建后刷新仍为 create 聚焦；再点管理后刷新仍为 list 聚焦。

## 2026-09-01 — 商品属性去掉品牌（专用品牌字段）

- 当前状态：`implemented`
- 问题：创建向导「身份资料」已有品牌目录下拉，属性集 basic 组仍挂 `brand`，商品属性步重复多余。
- 实现：`ProductCatalogEavBootstrap` 不再注册 brand 到属性组；`detachDedicatedIdentityAttributesFromSets` 拆除既有 placement/`set_id`；`editorCatalog` 与 `CREATE_EAV_SKIP_CODES` 过滤 `brand`/`brand_code`；创建仍经 `brand_id` 写 brand/brand_code。模块 `1.0.32`。
- 验收：商品创建/编辑属性区不再出现「品牌」；基础资料品牌下拉仍可用。

## 2026-09-01 — 商品主图媒体库弹层打不开

- 当前状态：`implemented`
- 问题：创建/编辑页「从媒体库添加」点击后弹层不显示，无法选图。
- 根因：原生 `<dialog>` 缺少 `data-w-component="dialog"`，`Weline.UI.dialog.open` 返回 false；JS 在 UI 存在时未回退 `showModal`。
- 实现：创建/规格图/编辑三处 dialog 补挂载属性；`openProductMediaDialog`/`closeProductMediaDialog` 失败时回退原生 API；契约测锁定标记；模块 `1.0.29`。
- 验收：点击「从媒体库添加」弹层 `dialog.open=true` 且可见媒体库 iframe；关闭按钮可关。

## 2026-09-01 — 分类标签支持树形多选与快速新建

- 当前状态：`implemented`
- 实现：创建页 `<w:catalog:category:select allow-create="true">` 传 `parent_id` + `website-id`；树形勾选与就地新建。
- 验收：下拉树可展开勾选；新建后自动选中并出现芯片。

## 2026-09-01 — 商品创建分类改用 Catalog 选择标签

- 当前状态：`implemented`
- 问题：创建页分类为长 checkbox 列表，不便多选与草稿回填展示。
- 实现：改用 `<w:catalog:category:select>`；`product-admin.js` 从标签 API / 隐藏域收集 `category_assignments`。
- 验收：创建向导「分类」为芯片选择；草稿 `scope=product_create_draft` 可回填。

## 2026-09-01 — 新增 SEO 跟随名称 + Handle=名称·SKU 拼装与可用性校验

- 当前状态：`implemented`
- 问题：SEO 标题不跟商品名；Handle 仅名称/SKU 回退且数字 SKU 会出 `p-` 前缀；无重复校验。
- 实现：SEO 标题自动跟名称（可手改）；Handle 由名称+SKU 拼装（去掉 `p-`）；`product_admin.checkSlug` 实时查重，占用/非法红框并禁提交；创建命令侧 `product_slug_taken` 兜底。
- 验收：输入名称+SKU 得 `name-sku` 形 Handle；SEO 标题跟名称；重复 Handle 红框无法提交。
- 模块：`1.0.31`

## 2026-09-01 — 商品新增自动生成前台 URL Handle

- 当前状态：`superseded`
- 问题：新建商品 SEO / URL 的「前台 URL Handle」需手填，易漏。
- 实现：`product-admin.js` 按名称（无 ASCII 时回退 SKU）自动 slugify 填充；手动改过则停止覆盖；草稿 hydrate 保留自定义 slug；契约测锁定自动生成标记。
- 验收：新增页输入英文名即时出 slug；手动改 slug 后改名称不再覆盖。
- 后续：被「SEO 跟随 + 名称·SKU 拼装 + checkSlug」条目取代。

## 2026-08-30 — 前台 JS 改 modules + data-weline-load（第二波）

- 当前状态：`implemented`
- 变更：登记 `view/statics/frontend/weline.modules.js`；部件/布局改 `data-weline-load`，删除 `@static`/`<js>` 直引。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。


## 2026-08-30 — 品牌管理页 ParseError 修复（真实验证）

- 当前状态：`implemented`
- 问题：`/backend/catalog/brands` 编译模板 `com_brands.phtml` 出现 `ParseError: unexpected token "*"`（`@backend-url(*/...)` 未加引号）。
- 实现：品牌页与创建页链接统一为 `@backend-url('*/...')`；契约测禁止未加引号写法；清 tpl 缓存后日志可见 `stage=return` 正常渲染（~334KB）。
- 验收：硬刷品牌管理页不再白屏/语法错误；可创建品牌。

## 2026-08-30 — 电商品牌管理（Website 分片目录）

- 当前状态：`implemented`
- 问题：创建商品品牌仅为自由文本，不符合电商「品牌目录 → 商品引用」习惯。
- 实现：新增 `brand` Website 分片（name/code/logo_url/description/status/position/uuid）；`ProductBrandAdminService` + 后台 brands CRUD 页与菜单；`creationContext.brands`；创建表改为品牌下拉，写 `brand`/`brand_code` 属性；Schema `4.3.0`。
- 验收：菜单「品牌」可增改停用；新建商品可选启用品牌；契约测锁定分片/菜单/创建联动。

## 2026-08-29 — 创建向导填写卡死/疑似死循环修复

- 当前状态：`implemented`
- 问题：catalog 新建页填写字段时浏览器易卡死，体感像死循环。
- 根因：basics/product-attributes 字段 `change`/`input` 触发 `maybeAdvanceCreateWizard` 连环开步+scroll+EAV/变体重绘；`updateCreateWizardVisibility` 每次强制重建变体 DOM；媒体 hydrate 对 scope 隐藏域反复 `dispatchEvent(input/change)`；scope 监听 `input change click` 放大主线程压力。
- 实现：去掉字段驱动自动跳步（只保留「下一步」按钮）；`maybeAdvance`/`toggle`/`openCreateWizardStep` 加重入锁；媒体 hydrate 静默 sync（值未变不 dispatch）；visibility 不再顺带重绘变体预览；SKU 预览 200ms 防抖；scope 事件收窄为 `change`。
- 验收：硬刷 `?v=20260829-no-hang` 后在基础资料连续填写名称/SKU/售价/描述不再跳步卡死；契约测锁定防环标记。

## 2026-08-29 — 创建基础资料布局改为分栏网格

- 当前状态：`implemented`
- 问题：基础资料字段全是通栏长条；售价/成本用 `w-cluster` 默认垂直居中，有帮助文案时错位成阶梯。
- 实现：引入 `w-product-create__basics-grid`（2/4 列、`align-items:start`、短字段并排、长文案 `data-span=full`）；仅描述类字段通栏。
- 验收：身份双列、交易/物流四列对齐；窄屏回落单列。

## 2026-08-29 — 创建向导基础资料面齐全化（对齐 WeShop）

- 当前状态：`implemented`
- 问题：基础资料被当成「几个字段凑合」，与万能产品定位不符；WeShop 级身份/库存/物流/分类等仍缺。
- 实现：basics 步改为分区基础资料面（身份、交易库存、物流尺寸、内容、SEO/URL、分类、主图图集），明确标注「不是属性集扩展属性」；补 SPU/品牌/条码/可见性/询价、币种、初始库存、长宽高；`creationContext` 下发分类；create 写库存与分类；可选创建后立即 publish。
- 验收：新建可见完整分区；填库存+分类+主图可创建；契约测锁定分区与字段。

## 2026-08-29 — 创建向导补齐主图与更多基础资料

- 当前状态：`superseded`（已被「基础资料面齐全化」覆盖）
- 缺口：相对 WeShop，万能产品新建仍缺详情描述、URL handle、重量、主图/图集；create 命令原先不写 media。
- 实现：basics 步增加 `description`/`slug`/`weight` + 媒体库多选（FileAsset，角色 main/gallery）；`media_assignments_json` 进 scope 备用回填；create 调用 `writeTaxonomyAndMedia`；slug/weight_kg/description 写入属性库。
- 说明：库存/上架状态仍走分片与发布流程（创建默认草稿），不在本次硬塞 WeShop 扁平列模型。
- 验收：新建可选主图；刷新可回填媒体 JSON；创建后编辑页可见媒体与 SEO/描述。

## 2026-08-29 — 创建向导基础信息对齐 WeShop（售价/成本/SEO）

- 当前状态：`implemented`
- 缺口：相对 WeShop 产品表单，框架创建向导基础步只有类型/名称/SKU，缺售价、成本、短描述与 SEO meta。
- 实现：basics 步补 `price`/`cost`/`short_description`/`meta_name`/`meta_description`/`meta_keywords`，均挂 `product_create_draft` scope；创建 payload 写 `price_minor`（元→分）与内容属性；`writeCreateBasicsContent` 落属性库；前台投影暴露 `meta_*`，`cost` 不进入 specifications。
- 验收：刷新可回填新字段；创建后价格/SEO 可查；契约测锁定字段与投影。

## 2026-08-29 — 新建商品草稿 scope 自动保存

- 当前状态：`implemented`
- 能力：接入 Taglib `scope-persistence`（`<w:scope container-id url>` + 字段 `scope` 属性），草稿经 `taglib/backend/scope`（与 SiteBuilder 一致；`backend/user-data` 经后台 Worker API 不可用）与 localStorage 暂存，刷新可恢复产品类型/名称/SKU/属性集。
- 清理：创建成功后清本地 scope 草稿并 POST 空值覆盖远端，避免下次打开残留。
- 验收：填写后刷新仍在；创建成功再进新建表单为空；契约测锁定挂载点。

## 2026-08-29 — 创建向导按属性组补齐非规格属性

- 当前状态：`implemented`
- 问题：选属性集后直接进规格维度芯片，业务集非规格组（如汉服 basic/specs/service）未按组露出填写；商品属性步排在规格之后且易被跳过。
- 实现：向导顺序改为属性集 → 商品属性 → 规格维度…；`resolveCreateAttributeCatalog` 优先 `creation_context` 回退 `snapshot`；空态与必填门禁；规格仍走 `*_variants` 芯片笛卡尔积。
- 决策：**不做**临时属性 / default 落点 / 假性属性组 overlay；缺字段走 EAV 正规挂到业务集对应组。
- 验收：汉服 configurable 先组表单后规格芯片；simple 仅组表单；契约测锁定步骤顺序与 catalog 门禁。

## 2026-08-29 — product-info 空 HTML 健康告警

- 当前状态：`implemented`
- 问题：DEV 健康检测 toast「产品主要信息 @product-main: Widget HTML is empty」；部件渲染路径 unsetData 清掉 Controller 赋值的 `storefront_offer`，无 offer 时模板直接空 return。
- 实现：对齐请求级 editor/preview 识别；无 offer 时输出 `storefront-product-detail-unavailable` 壳；由 Theme SlotRenderer 回注页面上下文。
- 验收：`/product/{slug}` 无 empty_html；契约含 unavailable testid。

## 2026-09-08 — 分类重写预取接入（当前模块 1.0.200）

分类 URL 原先每项调用单条入口，冷 51 项产生 102 SQL/102 WLS。现在仅汇集业务 path 并调用框架批量入口，范围/上下文/缓存由原 owner 统一管理；没有另写 Product URL 缓存。当前模块版本由并行工作推进至 1.0.200，本项不回退其版本。定向联合测试 26/174 通过，真实部署与请求结果记于 Framework 性能文档。

<!-- weline:module-development-baseline:generated-once -->
# Weline_Product 开发日志

## 2026-08-29 — 分类 URL 未走框架生成导致丢语言 path

- 当前状态：`fixed`
- 问题：英语店面点击顶栏/mega-menu 分类后跳到无 `/en_US` 前缀的 `/category/...`，回落到默认中文。
- 修复：`StorefrontAllMenuCategoryTreeService` / `StorefrontCategoryTreeIndex` / `StorefrontCategoryViewService` / 分类侧栏与控制器统一经 `UrlInterface::getFrontendUrl`/`getUrl` 生成；AllMenu 缓存维度含 lang+currency；`buildListingUrl` 兼容绝对 URL。
- 验收：`/en_US/...` 下分类 href 含语言前缀，点击后仍保持英语。

## 2026-08-29 — 经常一起购买批量加购改用亚马逊加购面板

- 当前状态：`fixed`
- 问题：「经常一起购买 → 全部加入购物车」成功后只弹出紧凑 toast（正在加入/已添加），与单件加购的亚马逊面板（小计、去结算、查看购物车）不一致。
- 修复：`cross-sell.js` 成功路径改为调用 `WelineCartPurchaseActions.showCartAddedNotice`；按钮仍短时显示「已添加」，加载中不再叠 toast；模板补充 `data-msg-success` / `data-cart-url`。
- 验收：详情页勾选搭配商品全部加入后出现含小计与「去结算/查看购物车」的 flyout。

## 2026-08-28 — 详情变体轴属性名未走 i18n

- 当前状态：`fixed`
- 问题：英文店面详情仍显示「类型/颜色/尺码」；`product-info.phtml` 优先输出未翻译的 `axis.label`。另有词典冲突：Acl/Theme 将「颜色」译为小写 `color`，覆盖 Product/Eav 的 `Color`。
- 修复：模板对已知规格码优先 `specificationLabels`+`__()`；Resolver/Catalog 同步；Acl/Theme `en_US.csv` 改为 `Color`；契约单测锁定。
- 验收：en_US 下变体轴为 Type/Color/Size；需 `i18n:collect` 刷新合并词典。

## 2026-08-28 — 默认店铺 store_id=0 合法化

- 当前状态：`fixed`
- 问题：活动主题预览在 Website=0 下触发 `store_product.store_id 不能为 0（Website 层）`；系统默认站默认店铺主键即为 `store_id=0`。
- 修复：`assertStoreOverlayId` 仅拒绝负数；Admin Query / 库存读取 / Cart Store 上架校验 / Search 投影同步接受 `store_id=0`。
- 验收：Website=0 商品列表/预览不再因默认店铺抛错。

## 2026-08-28 — 库存文案按场景区分：详情不点名、批量加购点名

- 当前状态：`fixed`
- 问题：详情页已显示「暂时缺货」仍再写「商品名 is out of stock」；用户指出点名只应发生在「经常一起购买 → 全部加入购物车」等多件加购失败时。
- 修复：快照库存不足改回通用「商品库存不足」；详情买盒不渲染该通用句；Cart 加购失败再带商品名。
- 验收：详情页无商品名库存句；FBT 全部加入失败 toast 含具体商品名。

## 2026-08-28 — 库存不足英文译句 CSV 转义损坏

- 当前状态：`fixed`
- 问题：英文店面 toast 仍显示中文「库存不足」（商品名为英文），因 `en_US.csv` 中 `「%{1}」库存不足` 译文引号转义损坏，i18n 回退 source。
- 修复：改为合法 CSV 译文 `%{1} is out of stock`；执行 `i18n:collect`。
- 验收：en_US 下加购失败 toast 为完整英文句。

## 2026-08-28 — 经常一起购买库存不足需点名商品

- 当前状态：`fixed`
- 问题：详情页「经常一起购买」点「全部加入购物车」时 toast 仅「商品库存不足」，未说明三个勾选商品中是哪一个不可售。
- 修复：`ProductCatalogCartItemSnapshotResolver` 在 `sellable=false` 时返回「「%{1}」库存不足」并带上商品名；同步 Product i18n。
- 验收：批量加购失败 toast 含具体商品名。

## 2026-08-28 — 404 推荐产品链接根相对路径

- 当前状态：`fixed`
- 问题：静态 404（`pub/errors/storefront-not-found/`）推荐卡片 href 被烘焙成 `http://product/{slug}`（CLI 空 host + `getUrl`），浏览器悬停显示 `product/{slug}`，嵌套 404 路径下解析错误。
- 修复：`StorefrontProductWidgetCatalog::mapOffer` 输出 `/product/{slug}`；`recommended-products` / `related-products` / `cross-sell` 用 `Url::getPrefix()` 拼根相对路径；`Url::getFrontendUrl` 在空 host 时退回路径；重发静态 404。
- 验收：契约单测 7/7；嵌套 404 探活 href=`/product/taoyuan-qingmeng`，商品页 HTTP 200。

## 2026-08-28 — 404 推荐产品加购点击无反应

- 当前状态：`fixed`
- 问题：404 布局缺少 `base::body-end`，未加载 Cart `product-purchase-actions.js`；推荐 CTA 仅有 pixel class，无 `data-action="add"`。
- 修复：`not_found` 补 `base::head-after/body-start/body-end`；推荐按钮改为 Cart 契约 markup；重发静态页。
- 验收：静态 HTML 含 purchase JS 与 `data-action="add"`；契约单测通过。

## 2026-08-27 — 前台 EAV 选项标签数字 code TypeError

- 当前状态：`fixed`
- 问题：商品详情投影调用 `StorefrontEavLabelResolver::resolve('warranty_months','12')` 时，PHP 将纯数字 option 数组键转为 `int`，`strcasecmp()` 在 PHP 8 下抛 `TypeError`（HTTP 500）。
- 修复：遍历选项时将 `$optionCode` 显式 `(string)` 后再比较。
- 验收：`/product/redmi-turbo-4-16-256` 与 `/en_US/product/redmi-turbo-4-16-256` HTTP 200，页内可见「质保(月)」；原报告 URL `/en_US/product/demo-13` 当前商品不存在（301→`/products/`，与本 TypeError 无关）。

## 2026-08-26 — M1/M4/M5 PostgreSQL 验证完成，M6 受 WLS 阻断

- 当前状态：`implemented_database_verified_runtime_blocked`
- 计划阶段：万能产品 v1.0 / M1、M4、M5、M6
- Setup：经用户授权将 `Weline_Mail` 版本从 `0.1.0` 提升到 `0.1.1`，完整 `php bin/w setup:upgrade` 退出 0；同时修复 `ProductCatalogAttributeEntity` 的 PostgreSQL `addColumn` 主键参数顺序，聚焦用例 1 test / 3 assertions 通过。
- Clone 底座：full clone 首次暴露 `pg_dump` 被完整保存在 PHP 内存、256MB 耗尽；`MigrationCloneService` 改为 `pg_dump --file` → `psql -f` 流式落盘，Framework 迁移底座 14 tests / 41 assertions 通过。测试 full clone 已按官方入口销毁，无遗留数据库。
- 身份迁移：在登记的 full clone `mig_clone_productcurrent20260810_20260810022347_e07e` 完成 dry-run/apply/幂等 replay/verify/cutover/rollback/再次 verify/最终 cutover；最终 26 Product、26 Offer、0 missing、0 conflict，`v2_authoritative` version 6，verified/source digest 均为 `59f890c70e34d54a68ad1094f71629e759fd516b524392ed8c1f3505222862d9`。
- PostgreSQL 门禁：Product 创建两个受控失败点完整回滚（1 test / 10 assertions）；跨站复制正式矩阵 4 tests / 88 assertions，覆盖同/跨站、多 Store、默认零库存、cleared、媒体、重放冲突、补抄、授权拒绝与目录+库存共同回滚。为此修复 PG 冷建表旧 55+hash/新 54+hash 索引重建冲突，并将每个复制用例隔离到随机 `product_copy_test_*` schema；结束后 schema 数为 0。
- 开发与消费链：Product 全量 205 tests / 1485 assertions（1 skip、1 PHPUnit deprecation）；Subscription Provider V2 4/20、Cart 8/50、Search direct 2/8、Inventory 10/40、Order topology 4/36、Order PostgreSQL 3/51 均通过。
- 运行阻断：独立 WLS `ai-test-product-v1-20260826-1609`（9693）启动后 Worker 因 managed-child Master lease owner evidence 不可观察而持续自治退出，HTTP/TLS 为 reset/timeout；真实后台 ACL/CSRF、五类产品前后台路径及 375/768/1024 Browser 证据无法取得。实例已用官方 stop 清理，9693 无监听；按用户决定可跳过不可用运行项，但总体仍不标记 VERIFIED/ACCEPTED。

## 2026-08-26 — Product/Offer Search 投影写触发闭环

- 当前状态：`implemented_pending_runtime_acceptance`
- 计划阶段：万能产品 v1.0 / M5
- 问题：Search current source 已能输出 OfferIdentity V2 文档，但 Offer、StoreOffer 以及 Product 的 versioned/lifecycle 写入口不会推进投影水位，可能留下旧 SKU、旧上下架状态或错误 Store 选品结果。
- 实现：四个 Product-owned Repository 统一复用 `ProductSearchProjectionMutationCoordinatorInterface`；Offer 写入发布父 Product 目标，StoreOffer 发布父 Product + Store 目标，不新增 Search target type；目录事实、watermark 与 `ResourceChange` 共用 Website 事务。
- 兼容：新增构造依赖均追加为可选参数；手工 modelFactory 测试默认 Noop，生产路径从容器取得真实 coordinator；StoreOffer 生产路径通过 OfferRepository 解析不可变父 Product。
- 门禁：先得到 9 次 Offer/StoreOffer 触发为 0 的红测，再补 Product update/lifecycle 红测；聚焦集成测试最终 1 test / 37 assertions 通过，Catalog Repository 与 Product Copy durable 回归通过；三个既有前台/文案红项已按真实契约收敛，Product 全量 192 tests / 1484 assertions 通过（1 skip、1 PHPUnit deprecation）。`setup:upgrade --route` 成功并恢复四类生成路由，默认 WLS `/` 与 `/products/` 均返回 200；完整数据库升级仍被另一项未提交的 Weline_Mail 表结构变更阻断，因此 PostgreSQL V2 迁移、真实后台与 Browser 全流程尚未验收，不标记 VERIFIED/ACCEPTED。

## 2026-08-26 — M5/M6 真实运行门禁盘点

- 当前状态：`runtime_blocked`
- 计划阶段：万能产品 v1.0 / M5、M6
- 已通过：官方 `setup:upgrade --route` 完整退出 0，生成 `frontend_pc`、`backend_pc`、`frontend_rest_api`、`backend_rest_api` 路由；默认 WLS 首页和商品列表 HTTP 200。
- 数据库阻塞：完整 `setup:upgrade` 在全量 SchemaDiff 检出 `Weline_Mail@0.1.0` 的 `w_weline_mail_domain` checkpoint 漂移，官方建议先将该模块版本提升为 `0.1.1`。Mail 当前有独立的大量未提交工作，Product 任务不得擅自改写或重绑它的 Schema checkpoint。
- 影响：Product V2 全局表尚未进入当前 PostgreSQL，`commerce:migrate-product-v2 inventory/dry-run` 因 `weline_product_identity_v2` 不存在而只读退出；五类产品、跨站复制、ACL/CSRF 与迁移切换不能取得真实验收证据。
- 浏览器：命令行直连 WLS 已受信并返回 200；Weline Chrome 与应用内浏览器的自动导航仍在等待页面完成加载时超时，不能用该工具超时替代 Browser 运行验收。
- 恢复条件：先由 Mail 工作负责人完成并确认其模块版本/Schema checkpoint，再重跑完整 setup、登记的 `mig_clone_*` PostgreSQL 迁移演练和 375 / 768 / 1024 Browser 验收。

## 2026-08-26 — Product/Offer V2 身份 clone 切换底座

- 当前状态：`implemented_pending_runtime_acceptance`
- 计划阶段：万能产品 v1.0 / M5
- 实现：新增持久化 `legacy → dual_read → v2_authoritative` 状态机；迁移后必须逐条验证 Product UUID、Offer UUID、归属关系、canonical SKU 和开放冲突，active legacy 源摘要变化会自动使验证失效。
- 兼容：公开 V1 resolver 在 dual/V2 模式优先读取 V2，并为历史 alias/冲突行保留旧表只读回退；V2 权威后旧 registry 的 claim/rename/ref_count/cleanup 统一拒绝。
- 回滚：只通过 expected-version 回退到 dual_read 或 legacy，不删除 V2 身份、别名和审计。
- 门禁：新增一次性 SQLite 全链路开发回归；正式 `mig_clone_*` PostgreSQL apply/verify/cutover/rollback、setup:upgrade 和独立 WLS 仍待运行，不标记 VERIFIED/ACCEPTED。

## 2026-08-25 — category_link Store overlay schema bump 收敛

- 当前状态：`implemented_pending_runtime_acceptance`
- 问题：主题编辑器分类预览与 `/category/electronics` 报 `column "store_id" does not exist`；物理表仍为 `link_id/category_id/product_id` + `uk_category_product(category_id,product_id)`，代码已按 SCHEMA_VERSION `4.0.0` 查询 `store_id`。
- 根因：同名唯一索引改列时 `PgsqlSchemaIndexNormalizer` / SchemaDiff 拒迁，`provisionWebsite(0)` 落入 `maintenance`。
- 修复：声明唯一索引逻辑名改为 `uk_store_category_product(store_id,category_id,product_id)`，使 diff 可 DROP 旧索引 + ADD 列 + ADD 新索引；文档对齐 `4.0.0`。
- 运维：对 website `0` 重新 `ProductShardProvisioner::provisionWebsite(0)` 后验收分类页与主题编辑器。

## 2026-08-25 — 发布诊断分组与生命周期原子闭环

- 当前状态：`implemented_pending_runtime_acceptance`
- 计划阶段：万能产品 v1.0 / M3
- 发布诊断：保留兼容 `errors/warnings`，新增 `severity/summary/groups`；按 Store、locale、currency、Offer 生成稳定分组、错误码与字段路径，后台初始渲染和重新校验共用同一结构。
- 范围规则：名称按 Store/locale 解析，`cleared` 终止继承；价格严格匹配目标币种，显式零价有效；Inventory 的真实 Store×Offer 可售量为零时只警告、不阻止发布。
- 生命周期：保存命令拒绝修改 archived；下架/归档在同一数据库事务内同步本地 Offer、父 Product 与归属身份，冲突整体回滚；后台只显示合法操作，归档页可浏览但经营控件只读。
- 门禁：新增红测后聚焦 17 tests / 310 assertions、完整 Product 169 tests / 1222 assertions 均零错误零警告；PHP/PHTML lint、JS syntax 与前端 section 扫描通过。
- 边界：本条仅证明开发与静态/单元门禁通过；独立 WLS、PostgreSQL、真实 HTTP/ACL/CSRF 与 Browser 流程仍在 M6 验收，不标记 ACCEPTED。

## 2026-08-25 — Provider schema 驱动类型配置编辑器

- 当前状态：`implemented_pending_runtime_acceptance`
- 计划阶段：万能产品 v1.0 / M3
- 问题：后台此前只暴露整块“类型配置 JSON”，Provider 的 `formSchema` 也只有分区/能力元数据，运营人员无法按类型安全编辑配置。
- 实现：五个内置 Provider 声明只读字段 schema；Read Service 统一规范化字段、选项、默认值和未知扩展键；后台按 schema 渲染文本、数字、布尔、单选、多选和 JSON 控件，并在保存时无损合并未声明扩展字段。
- 体验：字段具有显式 label 关联、必填提示、帮助文本、约束和响应式布局；无额外字段时给出清晰说明，未知扩展字段折叠到高级兼容区。
- 边界：`service_plans`、`download_assets`、`entitlement_policy`、`component_groups` 等复杂集合当前仍使用受 schema 管理的 JSON 控件；M4 再由虚拟、下载、组合专用编辑器接管，不据此宣称五类完成。
- 门禁：Provider schema 聚焦契约、PHP/PHTML lint、JS syntax、前端 section 扫描及完整 Product Unit；真实 WLS/PostgreSQL 页面验收留待 M6 统一收口。

## 2026-08-25 — 商品编辑器接入真实 Store×Offer 库存

- 当前状态：`implemented_pending_runtime_acceptance`
- 计划阶段：万能产品 v1.0 / M3
- 问题：库存与配送页签此前只有 Provider 能力说明，不能查看或修改真实库存。
- 实现：Product Admin Snapshot 通过 Inventory 公开能力聚合 Store×Offer 可用量；保存命令仅接受现货数量，并以 Inventory 事务包裹 Product 事务，实现库存失败时商品修改共同回滚。
- 后台：按 Store、SKU 展示策略、现货、预留、可售和购买状态；只为当前选中 Store 提交可编辑行，Inventory 缺失时不影响其他商品字段。
- 门禁：聚焦/全量 Product Unit、PHP/PHTML lint、JS syntax 与前端 section 扫描；真实 WLS/PostgreSQL 页面验收留待 M6 统一收口。

## 2026-08-25 — 商品前端图位接入 Theme SVG 占位标准

- 当前状态：`implemented`
- 需求 ID：`REQ-THEME-0010`、`REQ-PRODUCT-0011`
- 问题：相关/推荐/分类/目录/详情仍可能破图或使用字母占位，与搭配区标准不一致。
- 实现：`StorefrontProductWidgetCatalog` 委托 `StorefrontImagePlaceholder`；相关/推荐/分类/目录/`product-info` 模板统一 `data-storefront-img` + fallback。
- 验收：详情与列表无破图红字；无图位显示 Theme 标准 SVG。

## 2026-08-25 — 购物车 cross-sell 改为 Product 应用注入

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0011`（扩展 cart）
- 问题：购物车布局内嵌 `<w:widget name="cross-sell">`，违反「非 Theme 部件须 default_injections」。
- 实现：`cross-sell.default_injections` 增加 `cart`→`cart-recommendations`；Theme cart 布局仅留空槽/Hook。
- 验收：布局契约与注册契约测试。

## 2026-08-25 — 产品主要信息部件归属 Product 并默认注入详情主槽

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0013`
- 问题：详情主信息仍靠 Controller `contentTemplate` 写入 Theme `product-main`，无法在主题编辑器按部件编排，布局也缺少可替换的预览占位。
- 实现：新增 Product `product-info`（`default_injections`→`product-main`）；Detail 改为空壳 `detail-shell` 仅赋 `storefront_offer`；Theme 布局去掉 `contentTemplate`，保留 FCDC/标题预览占位；seeder 写入默认注入。
- 验收：部件契约与布局契约测试；详情页仍含 `storefront-product-detail`。

## 2026-08-25 — 经常一起买强制本地占位图

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0011`
- 问题：搭配 companion 远程图仍破图，露出红色 alt。
- 实现：`bundleCards` 统一写入本地 SVG `placeholderImage`；模板主图改用占位并 `alt=""`，链接保留 `aria-label`。
- 验收：搭配区无破图红字，四张均为本地占位风格。

## 2026-08-25 — 经常一起买区块破图兜底与版式收紧

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0011`
- 问题：详情搭配区 companion 使用失效 Unsplash URL，浏览器露出破图与红色 alt；卡片描边与大留白使版面松散。
- 实现：`StorefrontProductWidgetCatalog` 重写已知失效远程图并提供 `image_fallback`；cross-sell 模板/JS 失败时切本地 SVG 占位；CSS 改为无卡片描边的紧凑 Amazon 式组合行，+ 号相对图片垂直居中，汇总区贴右。
- 验收：搭配区无破图红字；产品行紧凑、勾选合计可用；响应式断点≈768 / ≥1024。

## 2026-08-25 — 分类页推荐产品部件归属 Product 并默认注入

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0012`
- 问题：分类页推荐区被隐藏，回退为 Theme `category-grid`；曾误在 Theme 布局内嵌 `w:widget`。
- 实现：新增 Product `recommended-products`（`cards` + 外部 CSS/JS + `default_injections`→`category-recommendations`）；Theme 取消隐藏并去掉布局内嵌 fallback；seeder/Upgrade 将槽归属切到 Product。
- 验收：分类页含 `storefront-recommended-products` 与真实商品名；布局无内嵌部件标签。

## 2026-08-26 — 产品列表页推荐产品归属 Product 并修复宽度容器

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0012`（延伸）
- 问题：`/products/` 仍用 Theme `featured-products` 演示占位；`list-recommendations` 槽仅纵向 padding，推荐区左右贴边。
- 实现：Product `recommended-products` 增加 `product_list` 默认注入；Theme 去掉布局内嵌 fallback；`product_list` 槽对子级应用 `--weline-layout-content-max-width`；部件 CSS 同步 max-width；Upgrade `2.1.6` 迁移槽归属。
- 验收：产品列表页含 `storefront-recommended-products` 与真实商品名；推荐区与主内容区同宽对齐。

## 2026-08-25 — 经常一起买部件归属 Product 并默认注入详情槽

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0011`
- 问题：详情页「经常一起购买」仍是 Theme 演示占位，未使用真实目录报价。
- 实现：新增 `StorefrontProductWidgetCatalog::bundleCards`；Product 注册 `cross-sell`（`default_injections`→`product-cross-sell`）并配外部 CSS/JS；Theme 去掉槽内嵌 fallback，seeder/normalizer/Upgrade `2.1.4` 将布局归属切到 `Weline_Product`。
- 验收：详情搭配区含 `storefront-cross-sell`、真实商品名与标题左右留白。

## 2026-08-25 — `/categories` 商品列表侧栏两列与密度优化

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0010`
- 问题：实页截图侧栏 Classification/Price 全宽堆在商品上方；`--sidebar-width` 等 token 为空导致 `product_list` 网格塌成单列；布局工具栏槽仍显示「拖拽」占位。
- 实现：`product_list` 布局 CSS 改 240px + rem 兜底（对齐 category）；筛选 Hook 在 `/categories` 下保留当前 listing 路径；catalog 卡片改为 Amazon 密度；Catalog `showToolbar=false` 隐藏空工具栏槽。
- 验收：桌面 `/categories` 侧栏≈240px 与商品并排；价格筛选 URL 仍在 `/categories`；无「工具栏区域」占位；平板抽屉可用。

## 2026-08-25 — 分类卡片图片占满媒体区

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0009`
- 问题：商品图 `object-fit:contain` + 8px 内边距导致媒体区四周留白，未占满卡片图区。
- 实现：`.amz-card__media img` 改为 `object-fit:cover` / `object-position:center`，去掉 padding。
- 验收：有图卡片图区无白边；标题价格仍在图下。

## 2026-08-25 — 分类卡片信息不再叠在图片上

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0009`
- 问题：有图商品 SVG 溢出 `.amz-card__media`（160px）盖住标题/价格，视觉上像信息悬浮在图上。
- 实现：media/media-wrap `overflow:hidden`；图片 `max-width/max-height:100%`；body 白底 + `z-index`。
- 验收：标题 top ≥ media bottom；有图卡与无图占位卡同为「图上 / 文下 / 按钮底」。

## 2026-08-25 — 分类 PLP 整卡可点

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0008`
- 问题：分类页卡片标题与加购之间的空白不可点，仅标题/图片可进详情。
- 实现：`amz-card` 增加整卡 hit-link（加购 `z-index:2`）；无 slug 时回退 `product/{id}`。
- 验收：点击标题下方空白区进入 PDP；加购仍独立。

## 2026-08-25 — 商品列表整卡可点与分类筛选恢复

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0008`
- 问题：`/products` 仅标题/图片可进详情；侧栏筛选槽为空占位，分类 filter 缺失。
- 实现：卡片使用整卡 hit-link（加购按钮 `z-index` 保留）；`product_list` 布局运行时 `filters-sidebar` Hook；Product 提供根分类+价格筛选；Catalog 应用 price/sort。
- 验收：卡片空白区可进 PDP；侧栏可见分类与价格；`?price=` 可过滤。

## 2026-09-03 — 汉服商品页与列表的国际化收口（1.0.75）

- 当前状态：`implemented_pending_browser_acceptance`。
- 页面：补齐 PDP、新品、热销及首页商品面；无定制布局时使用 Product 默认布局，确保新店无需先装修也能浏览与转化。
- URL：推荐/关联商品、独立热销榜与 Theme 商品卡统一避免重复语言/货币前缀；`en_US`、`ar_SA` 路由保持单一 locale 段。
- 内容：商品名、媒体和本地化字段按当前语言读取；目标语言缺失时优先使用维护中的英文中性副本，避免国际页面回落为中文业务名。
- SEO：PDP 与列表 canonical/hreflang 跟随真实 locale 路由，页面 `lang`/`dir` 与前缀一致。
- TDD：Product 聚焦 18 tests / 78 assertions通过；扩展聚焦 32 tests / 186 assertions通过。旧的实现形态契约仍需单独迁移，不计为本次运行时验收通过。
- 运行时：中文、`en_US`、`ar_SA` 的 PDP、新品与热销入口均返回 200；此前热销商品 URL 的重复 locale 前缀计数为 0。
- WebUI：本机内置浏览器因 macOS 锁屏尚未完成变体、购物车、立即购买与响应式操作验收。
- 商品：https://p05113ef3.test.weline.com:9555/product/taoyuan-qingmeng
- 英文新品：https://p05113ef3.test.weline.com:9555/en_US/new-arrivals
- RTL 热销：https://p05113ef3.test.weline.com:9555/ar_SA/best-sellers

## 2026-08-25 — Theme 可视化预览经 theme_public_route 解析具体商品

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0002`、`REQ-THEME-0009`
- 问题：Theme `theme-preview/content` URI 不含 `/product/{slug}`，product-info 无法解析真实商品。
- 实现：优先读 `theme_public_route=product/{slug}`，再回退 REQUEST_URI 段解析。
- 验收：可视化预览带 `theme_public_route=product/benq-screenbar` 时主信息为真实商品名。

## 2026-08-25 — 相关产品部件归属 Product 并默认注入详情槽

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0007`
- 问题：详情页相关区仍是 Theme 演示占位（「相关产品 1…」），未使用真实目录报价。
- 实现：新增 `StorefrontProductWidgetCatalog::relatedCards`；Product 注册 `related-products` 部件（`default_injections`→`product-related-products`）并配外部 CSS/JS；Theme 去掉槽内嵌 fallback 并将布局/seeder 归属切到 `Weline_Product`。
- 验收：详情相关区含 `storefront-related-products` 与真实商品名。

## 2026-08-25 — 商品详情 slug 路由与 product 布局回归

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0002`、`REQ-PRODUCT-0006`
- 问题：`/product/{slug}` 被 Theme 公共回退抢成空壳布局；修复路由后 Controller 仍声明不存在的 `product_detail`，详情退化为无 Head 裸片段。
- 实现：Theme `isInstalledModuleOwnedPublicRoute` 对齐 Product slug；Detail 恢复 `layoutType=product` / `page_type=product`。
- 验收：`benq-screenbar` 返回完整 HTML 壳与 `storefront-product-detail`。

## 2026-08-25 — 演示分类 home 更名为 home-living

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0005`
- 问题：演示根分类 path=`home`，与面包屑「首页/Home」撞名，用户感知为两个 home。
- 实现：根及子孙 path 统一改为 `home-living/...`；种子脚本迁移旧 `/home*`；`updateCategoryPath` 失效树缓存并 bump catalog。
- 验收：侧栏与面包屑显示 home living；`/category/home-living/kitchen/cups` 可用。

## 2026-08-25 — 部门导航非命中路径收起

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0005`
- 问题：全树展开后无关分支过长；用户要求「其他的收起来」。
- 实现：递归渲染时仅对 `active_path` / 当前节点展开子级；根级仍展示全部根分类。
- 验收：cups 页可见 books/home/electronics 根项，但 books/electronics 子树收起；home→kitchen 展开并见 plates/utensils。

## 2026-08-25 — 部门导航展示完整分类树

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0005`
- 问题：用户要「全部分类都展示，只命中当前路径」；此前只渲染祖先单链或仅当前层兄弟。
- 实现：`nestedRoots`/`activePathIds`；侧栏递归渲染整棵树；当前节点加粗，路径节点 `is-active-path`；种子补齐 home/kitchen/electronics。
- 验收：`/category/home/kitchen/cups` 可见 books/home/electronics 等根类及 kitchen 下 plates/utensils 等同级。

## 2026-08-25 — 部门导航展示同级兄弟姐妹

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0005`
- 问题：Department 只渲染祖先单路径+当前+子类，看不到同级类目。
- 实现：`StorefrontCategoryTreeIndex` 缓存按 `parent_id` 索引的分类树（禁 WLS 进程级 sticky memo）；`resolvePage` 返回 `siblings`；侧栏当前层渲染兄弟链接（当前加粗），子类挂在当前项下；创建分类时失效树缓存并 bump catalog generation。种子脚本：`scripts/seed-storefront-category-tree.php`。
- 验收：`/category/books/fiction/scifi` 部门区可见 fantasy/mystery/romance 等同级链接。

## 2026-08-25 — 部门导航可返回整条祖先链

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0005`
- 问题：部门区只渲染直接上级；根分类（如 books）无上级链接，无法返回。
- 实现：始终提供 `‹ 全部类目`→`/categories`；面包屑祖先全部可点并缩进；当前+子类挂在链下。
- 验收：books 可见 Any Department；coffee-tea 可见 food→drinks→当前。

## 2026-08-25 — 分类 PLP 视觉二次修复（实页截图）

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0005`
- 问题：实页截图仍乱——`--spacing-4` / layout padding / max-width 为空导致容器无间距；主题 `--weline-theme-primary=#ff0066` 污染 PLP 链接色；英文加购文案为 `add to the cart`。
- 实现：category 布局容器改 rem 兜底（1400px / 1rem gap / 1.5rem 内边距）；PLP/筛选色改为 Amazon 字面色；价格计数改为紧挨标签；`en_US` 加购改为 `Add to Cart`。
- 验收：coffee-tea 计算样式 `max-width:1400px`、`gap:16px`、`padding:0 24px`；面包屑/部门链 `#2162a1`；按钮 `Add to Cart`。

## 2026-08-25 — 亚马逊风格分类 PLP

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0005`
- 问题：分类页仍乱；用户要求按亚马逊分类页原型实现。
- 实现：Canvas 原型 `amazon-category-plp-prototype`；筛选侧栏改为部门+价格单选；结果区改为面包屑+结果条+四列紧凑卡片（黄底加购）。
- 验收：coffee-tea 可见面包屑、部门/价格筛选、结果条与 4 列网格。

## 2026-08-25 — 分类页筛选与产品布局修复

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0004`
- 问题：分类布局编译把 `category-filters` 写死为 scifi；价格筛选不可点；产品区缺工具栏、网格凌乱。
- 实现：布局改运行时 `filters-sidebar` Hook；`StorefrontCategoryListingFilter` + Category 控制器应用 price/sort；产品模板增加排序工具栏与整齐网格；清除写死缓存。
- 验收：coffee-tea 筛选芯片为本分类；`?price=` 可过滤；宽屏侧栏+网格并排。

## 2026-08-25 — 商品编辑页工作台补齐

- 当前状态：`implemented`
- 需求 ID：`REQ-PRODUCT-0003`
- 问题：编辑页仅有「改 SKU」表单，用户感知为“啥都没有”。
- 实现：`ProductAdminViewService::loadProductEdit` 聚合商品身份/主图/报价/媒体；Controller 叠加 Store0 站点文案；`edit.phtml` 展示工作台与跳转入口。SKU 保存路径保持不变。
- 验收：编辑页可见身份卡、关联报价/媒体/文案摘要；`product-edit-form` 仍可改 SKU。

## 2026-08-22 — 商品详情原生 product 布局修复

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-PRODUCT-0002`
- 根因：详情 Controller 使用不存在的 `product_detail` 布局，真实 WLS 返回裸内容片段并缺少前端 API 运行时。
- 实现：切换到 Theme 规范 `product` 布局与 `page_type=product`，保留现有商品投影和详情模板。
- 验收：契约测试 `3 tests / 24 assertions`；独立 WLS 的商品信息、Review 动态表单、图文视频提交与审核后回显通过。

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.16`（文档基线） | docs_handoff | `REQ-PRODUCT-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.0.16` — 文档制度启用基线

- 基线版本：`1.0.16`
- 目标版本：`1.0.16`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-PRODUCT-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-PRODUCT-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-PRODUCT-0001` / `1.0.16` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。

## 2026-08-30 保存品牌 404

- 根因：`Catalog::csrf()` 返回 `form_key`，而 `<w:form csrf="auto">` 写入的是 `csrf`（`Token::create`）；未登录时 ACL 未覆盖 `save-brand` 会落到控制器 CSRF 校验并 `noRouter(404)`。
- 修复：`csrf()` 改为返回 `csrf`；`saveBrand`/`disableBrand` 重命名为 `postSaveBrand`/`postDisableBrand`（`save-brand::POST`）；ACL 独立 source_id `brands:save` / `brands:disable`。
- 验证：未登录 POST `save-brand` → 302 登录；契约测 `ProductBrandAdminContractTest` + `ProductBrandSaveCsrfContractTest` 通过（锁定 csrf 字段名与 postSave* / ACL source）。

## 2026-08-28 Local 抽屉 en_US

- `i18n/en_US.csv` 同步补齐 Local 抽屉 UI 英文词条，与 Catalog 分类页共享键一致。

## 2026-09-03 — 默认店铺后台载荷闭环（1.0.76）

- 当前状态：`code_ready`；WebUI 浏览器验收仍待本机解锁。
- 问题：`store_id=0` 已在 Product 仓储、服务和 `product_admin.snapshot` Resource 合法化，但后台 `selectedStoreIds()` 仍按 `> 0` 过滤，导致默认店铺从创建/保存载荷丢失。
- 修复：浏览器载荷仅拒绝负数、NaN 与非整数，保留整数 `0`；Resource descriptor 的 `store_id.min` 同步为 `0`；Product 模块升至 `1.0.76`。
- 验证：源码红灯探针已复现旧谓词；修复后运行 `ProductAdminSurfaceContractTest`、`ProductAdminQueryProviderTest` 与 Product 全量 Unit；真实 WLS 继续验证中。
- 交付地址：前台 `https://p05113ef3.test.weline.com:9555/`；后台商品 `https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/admin`；API `https://p05113ef3.test.weline.com:9555/api/`。

## 2026-09-03 — 1688 汉服本地化覆盖与规格 SKU 收口

- 当前状态：`implemented_pending_runtime_acceptance`。
- RED：真实数据回读发现商品 83/84/85 的默认层已更新为来源品牌与语义 slug/SPU，但 `zh_Hans_CN` 仍覆盖旧“织造司”值；真实梦绘汉唐规格 SKU 长 48 字符，超过可读目标。
- 根因：创建基础字段只落当前 locale，更新时 `hanfu1688BasicRows()` 又只给部分内容写中文、给 slug/SPU 写默认层且不含品牌；规格 SKU 继续拼接压缩后的完整 option code。
- 修复：创建和更新共用同一组基础 EAV rows，对默认层与 `zh_Hans_CN` 同步写入描述、slug、SPU、SEO、可见性和来源品牌；规格 SKU 改为“语义商品前缀 + C/S 轴标记 + 4 位稳定指纹”，常规上限 36 字符，超限时使用组合指纹兜底。规格事实、组合与媒体仍只使用 EAV/Offer/Media，不写 JSON。
- 验收计划：先通过 OfferEavMapper 单测与导入契约，再重跑真实 3 商品并审计 locale、Offer UUID、媒体与受保护数据，最后用 Weline Chrome 完成前后台、加购和 375/768/1024 实际验收；不进入结算或支付。

## 2026-09-06 — 促销选品投影跳过无必要的 identity 读取

- `ProductAdminReadService::searchSelectionRows` 在仅有 status/name/SKU 等基础筛选时直接返回 `product_id/updated_at`，只有详情输出或 `product_code/type/owner_website_id` 条件才解析权威 identity。促销筛选的结果契约、排序、网站/店铺范围与异常回退保持不变。
- 这样避免对每个已发布商品逐行读取 identity；目标路径仍使用现有 `ProductSelectionReadInterface`，不新增缓存或改变商品身份数据来源。
- 回归：新增“基础筛选不读取 identity、身份筛选仍读取 identity”用例；ProductAdminReadServiceBatchSearchTest **10 tests / 96 assertions** 通过。
## 2026-09-08 — 规格链接使用有限共享身份投影（1.0.190）

- `StorefrontOfferDetailQuery → StorefrontEavLabelResolver::toPublicQuery` 原来为每页首个规格链接读取完整共享 EAV 目录及约 2070 条选项本地化。现 `toPublicQuery` / `canonicalizeAxisQuery` 批量收集实际轴，通过 Eav 可选接口只读 ID/code/原始值，后续卡片复用请求投影。
- 不采用新接口的 provider 保留原行为；`resolve`/属性展示及商品私有 `forProduct` 继续原目录。数值别名、大小写匹配、缺失 token、原始中文值、同 code 顺序不变；异常不记成空轴，下一请求观察最新身份。
- 消费层只持有方法内的批量 DTO，后续单项读取也直接复用 Eav owner 的 Context，不新增 resolver 请求缓存属性。
- 最小行为 RED 后联合定向回归 34 tests / 482 assertions 通过。当前数据库真实只读 24 商品的最终查询参数 SHA256 前后严格相同：`0a9748861ef3073bc7df1890a3ec04ab8459ad45091be872f9e4810faa6014de`；参与 4 轴，实际 WLS 三批共 1152 选项（size/ID 34 为 113、style_type/ID 35 为 959，两轴合批 1072；color/ID 4 为 78；character/ID 118 为 2），原约 2070。早先“4 轴 78”系探针误用 `EavAttribute::getId()`，已改为 `getAttributeId()`；78 仅是颜色轴。该段 14 → 9 DB、0 选项/属性本地化查询；完整页面由主任务验收。
- 保留并行图片尺寸 1.0.189，版本升级至 1.0.190。
