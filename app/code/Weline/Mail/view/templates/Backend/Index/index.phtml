<?php
/** @var \Weline\Framework\View\Template $this */
$environment = $environment ?? [];
$installPlan = $install_plan ?? [];
$domains = $domains ?? [];
$accounts = $accounts ?? [];
$dnsResult = $dns_result ?? null;
$clientSettings = $client_settings ?? null;
$checks = $environment['checks'] ?? [];
$escape = static fn($value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
?>
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><?= __('企业邮箱') ?></h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><?= __('系统服务') ?></li>
                    <li class="breadcrumb-item active"><?= __('企业邮箱') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xl-8">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><?= __('邮件服务环境') ?></h5>
                <p class="text-muted mb-3"><?= __('底层引擎：%{1}，平台：%{2}', [$escape($environment['engine'] ?? 'stalwart'), $escape($environment['platform'] ?? PHP_OS_FAMILY)]) ?></p>
                <div class="table-responsive">
                    <table class="table table-sm align-middle">
                        <thead>
                        <tr>
                            <th><?= __('检测项') ?></th>
                            <th><?= __('状态') ?></th>
                            <th><?= __('详情') ?></th>
                        </tr>
                        </thead>
                        <tbody>
                        <?php foreach ($checks as $check): ?>
                            <tr>
                                <td><?= $escape($check['name'] ?? '') ?></td>
                                <td>
                                    <?php if (!empty($check['ok'])): ?>
                                        <span class="badge bg-success"><?= __('通过') ?></span>
                                    <?php else: ?>
                                        <span class="badge bg-warning"><?= __('待处理') ?></span>
                                    <?php endif; ?>
                                </td>
                                <td class="text-muted" style="white-space: normal;"><?= $escape($check['detail'] ?? '') ?></td>
                            </tr>
                        <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><?= __('原生安装计划') ?></h5>
                <ol class="mb-3">
                    <?php foreach (($installPlan['steps'] ?? []) as $step): ?>
                        <li><?= $escape($step) ?></li>
                    <?php endforeach; ?>
                </ol>
                <code>php bin/w mail:env:install</code><br>
                <code>php bin/w env:install stalwart-mail-server -y</code>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xl-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><?= __('创建邮箱域名') ?></h5>
                <form method="post" action="<?= $this->getBackendUrl('weline_mail/backend/index/create-domain') ?>">
                    <div class="mb-3">
                        <label class="form-label"><?= __('邮箱域名') ?></label>
                        <input class="form-control" name="domain_name" placeholder="example.com" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('邮件主机名') ?></label>
                        <input class="form-control" name="hostname" placeholder="mail.example.com" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('默认容量 MB') ?></label>
                        <input class="form-control" name="default_quota_mb" type="number" min="128" value="1024">
                    </div>
                    <button class="btn btn-primary" type="submit"><?= __('创建域名') ?></button>
                </form>
            </div>
        </div>
    </div>
    <div class="col-xl-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><?= __('创建邮箱账号') ?></h5>
                <form method="post" action="<?= $this->getBackendUrl('weline_mail/backend/index/create-account') ?>">
                    <div class="mb-3">
                        <label class="form-label"><?= __('域名') ?></label>
                        <select class="form-select" name="domain_id" required>
                            <option value=""><?= __('请选择域名') ?></option>
                            <?php foreach ($domains as $domain): ?>
                                <option value="<?= (int)$domain->getId() ?>"><?= $escape($domain->getData('domain_name')) ?></option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('邮箱地址') ?></label>
                        <input class="form-control" name="email" placeholder="admin@example.com" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('显示名称') ?></label>
                        <input class="form-control" name="display_name" placeholder="<?= __('管理员') ?>">
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('容量 MB') ?></label>
                        <input class="form-control" name="quota_mb" type="number" min="128" value="1024">
                    </div>
                    <button class="btn btn-primary" type="submit"><?= __('创建账号') ?></button>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xl-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><?= __('DNS 检测') ?></h5>
                <form class="row gy-2 gx-2 align-items-end" method="get" action="<?= $this->getBackendUrl('weline_mail/backend/index') ?>">
                    <div class="col-md-5">
                        <label class="form-label"><?= __('邮箱域名') ?></label>
                        <input class="form-control" name="domain" value="<?= $escape($dnsResult['domain'] ?? '') ?>" placeholder="example.com">
                    </div>
                    <div class="col-md-5">
                        <label class="form-label"><?= __('邮件主机名') ?></label>
                        <input class="form-control" name="hostname" value="<?= $escape($dnsResult['hostname'] ?? '') ?>" placeholder="mail.example.com">
                    </div>
                    <div class="col-md-2">
                        <button class="btn btn-secondary w-100" type="submit"><?= __('检测') ?></button>
                    </div>
                </form>
                <?php if ($dnsResult): ?>
                    <div class="table-responsive mt-3">
                        <table class="table table-sm">
                            <thead>
                            <tr>
                                <th><?= __('类型') ?></th>
                                <th><?= __('主机') ?></th>
                                <th><?= __('建议值') ?></th>
                                <th><?= __('状态') ?></th>
                            </tr>
                            </thead>
                            <tbody>
                            <?php foreach ($dnsResult['records'] as $record): ?>
                                <tr>
                                    <td><?= $escape($record['type'] ?? '') ?></td>
                                    <td><?= $escape($record['host'] ?? '') ?></td>
                                    <td style="white-space: normal;"><?= $escape($record['value'] ?? '') ?></td>
                                    <td><?= !empty($record['ok']) ? __('存在') : __('未检测到') ?></td>
                                </tr>
                            <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
    <div class="col-xl-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><?= __('客户端配置') ?></h5>
                <?php if ($clientSettings): ?>
                    <div class="table-responsive">
                        <table class="table table-sm">
                            <tbody>
                            <tr><th>SMTP</th><td><?= $escape($clientSettings['smtp']['host'] ?? '') ?>:<?= (int)($clientSettings['smtp']['port'] ?? 0) ?> <?= $escape($clientSettings['smtp']['security'] ?? '') ?></td></tr>
                            <tr><th>IMAP</th><td><?= $escape($clientSettings['imap']['host'] ?? '') ?>:<?= (int)($clientSettings['imap']['port'] ?? 0) ?> <?= $escape($clientSettings['imap']['security'] ?? '') ?></td></tr>
                            <tr><th>POP3</th><td><?= $escape($clientSettings['pop3']['host'] ?? '') ?>:<?= (int)($clientSettings['pop3']['port'] ?? 0) ?> <?= $escape($clientSettings['pop3']['security'] ?? '') ?></td></tr>
                            </tbody>
                        </table>
                    </div>
                <?php else: ?>
                    <p class="text-muted"><?= __('输入域名和邮件主机名后显示客户端连接配置。') ?></p>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xl-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><?= __('邮箱域名') ?></h5>
                <div class="table-responsive">
                    <table class="table table-sm align-middle">
                        <thead><tr><th>ID</th><th><?= __('域名') ?></th><th><?= __('主机名') ?></th><th><?= __('状态') ?></th><th><?= __('操作') ?></th></tr></thead>
                        <tbody>
                        <?php foreach ($domains as $domain): ?>
                            <tr>
                                <td><?= (int)$domain->getId() ?></td>
                                <td><?= $escape($domain->getData('domain_name')) ?></td>
                                <td><?= $escape($domain->getData('hostname')) ?></td>
                                <td><?= $escape($domain->getData('status')) ?></td>
                                <td>
                                    <div class="d-flex flex-wrap gap-1">
                                        <form method="post" action="<?= $this->getBackendUrl('weline_mail/backend/index/set-domain-status') ?>">
                                            <input type="hidden" name="domain_id" value="<?= (int)$domain->getId() ?>">
                                            <input type="hidden" name="status" value="active">
                                            <button class="btn btn-sm btn-success" type="submit"><?= __('开启') ?></button>
                                        </form>
                                        <form method="post" action="<?= $this->getBackendUrl('weline_mail/backend/index/set-domain-status') ?>">
                                            <input type="hidden" name="domain_id" value="<?= (int)$domain->getId() ?>">
                                            <input type="hidden" name="status" value="suspended">
                                            <button class="btn btn-sm btn-outline-warning" type="submit"><?= __('停用') ?></button>
                                        </form>
                                        <form method="post" action="<?= $this->getBackendUrl('weline_mail/backend/index/set-domain-status') ?>">
                                            <input type="hidden" name="domain_id" value="<?= (int)$domain->getId() ?>">
                                            <input type="hidden" name="status" value="pending">
                                            <button class="btn btn-sm btn-outline-secondary" type="submit"><?= __('待同步') ?></button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><?= __('邮箱账号') ?></h5>
                <div class="table-responsive">
                    <table class="table table-sm align-middle">
                        <thead><tr><th>ID</th><th><?= __('邮箱') ?></th><th><?= __('名称') ?></th><th><?= __('容量') ?></th><th><?= __('状态') ?></th></tr></thead>
                        <tbody>
                        <?php foreach ($accounts as $account): ?>
                            <tr>
                                <td><?= (int)$account->getId() ?></td>
                                <td><?= $escape($account->getData('email')) ?></td>
                                <td><?= $escape($account->getData('display_name')) ?></td>
                                <td><?= (int)$account->getData('quota_mb') ?> MB</td>
                                <td><?= $escape($account->getData('status')) ?></td>
                            </tr>
                        <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
