<!-- weline:module-development-baseline:generated-once -->
# Weline_Mail 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 2026-09-07 — 个人中心邮件工作台回复与账号启停（0.2.4）

- 目标：个人中心 `#mail` 保持单一侧栏入口；读信可一键回复预填写邮件；折叠区可暂停/恢复账号。
- 实现：`mail-client.phtml` 增加 `mail_compose`/`mail_to`/`mail_subject`/`mail_body` 预填、回复链与 `suspend`/`resume` 表单；契约 `MailAccountMailClientContractTest`。
- 版本：`0.2.3` → `0.2.4`
- 交付地址：[个人中心·我的邮箱](http://p05113ef3.test.weline.com:9555/customer/account/index#mail)（需登录；默认 Host 为 `{hash}.test.weline.com`，不用 `*.weline.test`）。

## 2026-09-06 — 注册页企业邮箱 Tab 面板变体（0.2.3）

- 目标：配合 Customer 注册页 tabs；部件支持 `mail_register_variant=panel|card`。
- 实现：去掉 details 折叠；panel 供 Tab 面板，card 供个人中心侧栏。
- 版本：`0.2.2` → `0.2.3`

## 2026-09-06 — 注册页企业邮箱折叠次要入口（0.2.2）

- 目标：注册卡首屏不被企业邮箱申请表撑高；消除嵌套 form。
- 实现：`account-mail-register` 改为 `<details>` 默认折叠；`mail_register_open` 供个人中心侧栏默认展开；Customer `register.phtml` 挂载到主表单外。
- 契约：`MailFrontendRegisterLoginContractTest` 断言 Mail 在 `</w:form>` 之后。
- 版本：`0.2.1` → `0.2.2`

## 2026-09-06 — 去掉独立企业邮箱登录（0.2.1）

- 目标：企业邮箱只在注册申请；登录与普通顾客登录相同，不单独做企业邮箱登录页/Tab。
- 实现：移除 `account-mail-login` 部件、登录 Controller/Service、SystemConfig `mail/frontend_login`；`login.phtml` 不再 fetch。
- 版本：`0.2.0` → `0.2.1`

## 2026-09-06 — 全局 mail-composer Taglib 与前台注册/登录（0.2.0）

- 目标：企业邮箱写信做成可复用 Taglib 浮层；询价/个人中心调用；前台注册与解耦登录。
- 实现：`<w:mail-composer>` + `WelineMailComposer`；`MailMessage.source/source_id`；`MailComposerService`；前台 SystemConfig 开关 + 辅助邮箱验证 + 缓存清理；`MailAccount.password_hash` 登录。
- 文档：`doc/mail-composer-taglib.md`；场景映射表已登记；`taglib:collect` / `taglib:catalog:generate`。
- 验证：契约测 20+；询价页 `WelineMailComposer` 浮层发信 → `source=product_quote` → `replied`；注册/登录 `fetch` 挂载部件（对齐社媒）。

## 2026-09-06 — 询价代发深链与 mail_message_sent（0.1.2）

- 目标：支持业务模块带参打开代发页；发信成功派发事件；本机邮箱解析/列表 Query。
- 实现：`resolveLocalMailboxByEmail` / `listLocalMailboxes`；compose GET 预填；普通用户锁定本人邮箱；超管/`mail_send_as` 可选任意本机账号；`Weline_Mail::mail_message_sent`。
- 修复：`postSendAs` 派发事件须先赋 `$sentEvent` 再 `dispatch`（PHP 8.4 禁止内联数组传引用）；新增观察者后需 `php bin/w event:rebuild`。
- 验证：`MailComposeDeeplinkContractTest`；实站超管用 `support@quote-test.invalid` 代发成功，询价 `#1` `processing→replied`（`updated_at` 与发信时间一致）。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| 0.2.4（个人中心回复与账号启停） | implementation_complete / browser_login_pending | REQ-MAIL-0003 | 2026-09-07 | 2026-09-07 | pending logged-in WebUI |
| 0.2.0（企业邮箱闭环） | implementation_complete / live_acceptance_pending | REQ-MAIL-0002 | 2026-08-25 | 2026-08-26 | pending production E2E |
| 0.1.0（文档基线） | docs_handoff | REQ-MAIL-0001 | 2026-08-22 | 2026-08-22 | pending user / not released |

## `0.2.0` — 企业邮箱完整线上闭环

- 当前状态：`live_acceptance_in_progress`
- 需求 ID：`REQ-MAIL-0002`
- 2026-08-26 生产复现：站级 `Weline_Mail::*` ACL 生效后，真实 GET `weline_mail/backend` 进入控制器并在 `Index.php:84` 返回 500；根因为 `buildDomainLookup()` 返回数组，消费端却调用对象 `getData()`。
- 修复范围：仅修正数组引擎字段判断，增加 `IndexTest` 的 fake、Stalwart 与空条目回归用例；不改变邮箱数据、收发协议、ACL 或其他模块。
- 验收计划：聚焦 PHPUnit、PHP lint、diff check、生产 WLS 热重载后 HTTP 200 与后台内容检查，并统一收集 375/768/1024 页面证据。

## `0.1.0` — 文档制度启用基线

- 基线版本：`0.1.0`
- 目标版本：`0.1.0`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-MAIL-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-MAIL-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-MAIL-0001` / `0.1.0` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
