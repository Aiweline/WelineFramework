<?php
/**
 * @var \Weline\EditorManager\Controller\Backend\Index $this
 * @var array $editors
 * @var string $title
 */
?>

<div class="w-container">
    <div class="w-grid">
        <div style="--w-span:12;">
            <div class="w-card">
                <div class="w-card__header">
                    <h4 class="w-card__title" style="--w-mb:0;">
                        <w:icon name="edit" size="sm"></w:icon>
                        <?= $title ?>
                    </h4>
                </div>
                <div class="w-card__body">
                    <p class="w-text" data-tone="muted" style="--w-mb:var(--weline-space-6);">
                        <?= __('管理系统中可用的富文本编辑器。编辑器管理器允许您在不同的编辑器之间切换。') ?>
                    </p>
                    
                    <?php if (empty($editors)): ?>
                        <div class="w-alert" data-tone="info">
                            <w:icon name="info" size="sm"></w:icon>
                            <?= __('暂无已注册的编辑器管理器。') ?>
                        </div>
                    <?php else: ?>
                        <div class="w-table-wrap">
                            <table class="w-table">
                                <thead>
                                    <tr>
                                        <th style="width: 50px;">#</th>
                                        <th style="width: 150px;"><?= __('编辑器名称') ?></th>
                                        <th style="width: 300px;"><?= __('类名') ?></th>
                                        <th><?= __('描述/状态') ?></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <?php foreach ($editors as $index => $editor): ?>
                                        <tr>
                                            <td><?= $index + 1 ?></td>
                                            <td>
                                                <span class="w-badge" data-w-background="primary">
                                                    <?= htmlspecialchars($editor['name']) ?>
                                                </span>
                                            </td>
                                            <td>
                                                <code class="w-text" data-tone="muted" data-size="sm">
                                                    <?= htmlspecialchars($editor['class']) ?>
                                                </code>
                                            </td>
                                            <td>
                                                <?= $editor['description'] ?>
                                            </td>
                                        </tr>
                                    <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>
                    <?php endif; ?>
                    
                    <div style="--w-mt:var(--weline-space-6);">
                        <div class="w-alert" data-tone="muted">
                            <h6 class="weline-alert-heading">
                                <w:icon name="circle" size="sm"></w:icon>
                                <?= __('安装更多编辑器') ?>
                            </h6>
                            <p style="--w-mb:var(--weline-space-2);"><?= __('您可以通过 Composer 安装额外的编辑器管理器模块：') ?></p>
                            <code class="w-text" data-w-display="block" data-w-background="muted" data-w-radius="md" data-tone="muted" style="--w-pt:var(--weline-space-2);--w-pe:var(--weline-space-2);--w-pb:var(--weline-space-2);--w-ps:var(--weline-space-2);">
                                composer require weline/module-ckeditor-editor-manager
                            </code>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
