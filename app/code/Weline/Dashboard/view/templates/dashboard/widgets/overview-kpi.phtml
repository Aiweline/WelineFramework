<?php
try {
    $statsService = \Weline\Framework\Manager\ObjectManager::getInstance(\Weline\Dashboard\Service\DashboardStatisticsService::class);
    $items = $statsService->getKpiItems();
} catch (\Throwable) {
    $items = [
        ['label' => __('侧栏入口'), 'value' => '2', 'hint' => __('仪表盘 / 首页')],
        ['label' => __('已收入口'), 'value' => '0', 'hint' => __('改由面板部件承载')],
        ['label' => __('统计部件'), 'value' => '0', 'hint' => __('dashboard-widget')],
        ['label' => __('视图 / 站点'), 'value' => '0 / 0', 'hint' => __('Dashboard 范围')],
    ];
}
$h = static fn(mixed $value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
?>
<article class="w-card dashboard-widget dashboard-widget-kpi" data-w-height="full">
    <div class="w-card__header">
        <h3 class="w-card__title"><?= __('后台统计') ?></h3>
        <span class="w-text" data-tone="muted" data-size="sm"><?= __('Dashboard') ?></span>
    </div>
    <div class="w-card__body">
        <div class="w-stat-tiles dashboard-widget-kpi-grid" style="--w-stat-tile-min:7.5rem;--w-gap:var(--weline-space-3);">
            <?php foreach ($items as $item): ?>
                <div class="w-card" data-tone="muted" data-w-height="full">
                    <div class="w-card__body" style="--w-pt:var(--weline-space-3);--w-pb:var(--weline-space-3);--w-ps:var(--weline-space-3);--w-pe:var(--weline-space-3);">
                        <strong class="w-text" data-tone="primary" data-align="center" style="--w-font-size:1.375rem;"><?= $h($item['value'] ?? '') ?></strong>
                        <span class="w-text" data-align="center" data-size="sm" style="--w-mt:var(--weline-space-2);"><?= $h($item['label'] ?? '') ?></span>
                        <small class="w-text" data-tone="muted" data-align="center" data-size="sm" style="--w-mt:var(--weline-space-1);"><?= $h($item['hint'] ?? '') ?></small>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    </div>
</article>
