<?php
$rows = [
    [__('视图'), __('默认概览'), __('系统共享视图')],
    [__('布局'), __('4 个固定区域'), __('指标、分析、侧栏、明细')],
    [__('部件'), __('后台专用'), __('由后台统计部件提供')],
];
$h = static fn(mixed $value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
?>
<article class="w-card dashboard-widget dashboard-widget-detail-snapshot" data-w-height="full">
    <div class="w-card__header">
        <h3 class="w-card__title"><?= __('默认明细') ?></h3>
        <span class="w-text" data-tone="muted" data-size="sm"><?= __('Dashboard') ?></span>
    </div>
    <div class="w-card__body" style="--w-pt:0;--w-pb:0;">
        <div class="w-table-wrap">
            <table class="w-table" style="--w-mb:0;">
                <tbody>
                <?php foreach ($rows as $row): ?>
                    <tr>
                        <th scope="row"><span class="w-text" data-tone="muted"><?= $h($row[0] ?? '') ?></span></th>
                        <td>
                            <strong class="w-text" data-weight="strong"><?= $h($row[1] ?? '') ?></strong>
                            <span class="w-text" data-tone="muted" data-size="sm" style="--w-mt:var(--weline-space-1);"><?= $h($row[2] ?? '') ?></span>
                        </td>
                    </tr>
                <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</article>
