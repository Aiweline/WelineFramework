<?php
$rows = [
    [__('Widget 注册'), __('就绪')],
    [__('Theme 布局'), __('dashboard/default')],
    [__('视图范围'), __('站点 + 用户')],
];
?>
<section class="dashboard-widget dashboard-widget-status">
    <header>
        <h3><?= __('系统状态') ?></h3>
        <span><?= __('Backend') ?></span>
    </header>
    <table>
        <tbody>
        <?php foreach ($rows as $row): ?>
            <tr>
                <th><?= htmlspecialchars((string)$row[0]) ?></th>
                <td><?= htmlspecialchars((string)$row[1]) ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</section>
<style>
.dashboard-widget-status table {
    width: 100%;
    border-collapse: collapse;
}
.dashboard-widget-status th,
.dashboard-widget-status td {
    border-bottom: 1px solid var(--w-dashboard-border, var(--backend-color-border, #e5e7eb));
    padding: 10px 0;
    text-align: left;
    font-size: 13px;
    line-height: 1.35;
    vertical-align: top;
}
.dashboard-widget-status th {
    color: var(--w-dashboard-muted, var(--backend-color-text-secondary, #6b7280));
    font-weight: 500;
    white-space: nowrap;
    padding-right: 12px;
}
.dashboard-widget-status td {
    text-align: right;
    font-weight: 600;
    overflow-wrap: anywhere;
}
.dashboard-widget-status tr:last-child th,
.dashboard-widget-status tr:last-child td {
    border-bottom: 0;
}
</style>
