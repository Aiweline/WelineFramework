<?php
$rows = [
    [__('Widget 注册'), __('就绪')],
    [__('Theme 布局'), __('dashboard/default')],
    [__('视图范围'), __('站点 + 用户')],
];
$h = static fn(mixed $value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
?>
<article class="w-card dashboard-widget dashboard-widget-status" data-w-height="full">
    <div class="w-card__header">
        <h3 class="w-card__title"><?= __('系统状态') ?></h3>
        <span class="w-text" data-tone="muted" data-size="sm"><?= __('Backend') ?></span>
    </div>
    <div class="w-card__body" style="--w-pt:0;--w-pb:0;">
        <div class="w-table-wrap">
            <table class="w-table" style="--w-mb:0;">
                <tbody>
                <?php foreach ($rows as $row): ?>
                    <tr>
                        <th scope="row"><span class="w-text" data-tone="muted"><?= $h($row[0] ?? '') ?></span></th>
                        <td><strong class="w-text" data-weight="strong"><?= $h($row[1] ?? '') ?></strong></td>
                    </tr>
                <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</article>
