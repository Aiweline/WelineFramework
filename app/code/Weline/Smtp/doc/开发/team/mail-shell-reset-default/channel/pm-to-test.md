# pm-to-test · mail-shell-reset-default

Team:项目经理: 施工三席 closed。请执行 test-reset。

读 `contracts.md` UC-1 / UC-2。

最低：
1. 跑 `MailShellRegionStoreContractTest` + `MailShellHanfuDefaultsContractTest` + `MailTemplateUiSendContractTest`
2. 可选 WB：后台模板编辑页点「重置为默认」→ confirm 取消不提交；确认后壳区空、预览无库内炭黑朱砂页头覆盖
3. 写 channel/测试-closed.md；notify_pm

交付 Host 默认 `{hash}.test.weline.com`；Browser 非抢占、禁缓存、抹 webdriver。
