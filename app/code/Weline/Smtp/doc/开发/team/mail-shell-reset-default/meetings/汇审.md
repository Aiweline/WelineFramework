# 汇审 · mail-shell-reset-default

Team:项目经理:

## 用户需求对照

1. **删库内页头覆盖** — 已清空 `default`/`shop` 的 `smtp_mail_shell_regions`；HanfuDefaults 空头不再写回。
2. **可视化「重置为默认」回落默认模板（含壳）** — `postReset`→`RegionStore::clear`；confirm 文案中英已齐。

## 证据

- UT：11 tests / 235 assertions PASS（三契约）
- WB：blocked（WLS workers + `certificate_retirement_replay` lifecycle + cursor-ide-browser 建 tab 即丢）

## 汇审结论

**代码交付可收口**；**浏览器真机验收未完成**，不得宣称 WB pass。请用户在站点稳定后于模板编辑页点「重置为默认」自验：取消不提交；确认后页头回落 `shell.phtml`。

## 后续（非本波强制）

稳定 `certificate_retirement_replay` / workers 后，再派测试席补 WB。
