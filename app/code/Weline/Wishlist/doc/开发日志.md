# Weline_Wishlist 开发日志

## 2026-09-01 — 顶栏收藏移到账户左侧

- 当前状态：`implemented`
- 变更：Theme header `user-area` 内将 `header-wishlist-icon` 置于 account 之前（货币后 → 收藏 → 账户 → 订单 → 购物车）；契约测试断言 DOM 顺序；版本 `1.0.6` → `1.0.7`（Theme `2.2.70` → `2.2.71`）。
- 验证：`WishlistHeaderWishlistIconHookTemplateTest`；前台首页顶栏肉眼顺序。

## 2026-08-30 — 删除遗留 /wishlist 与 Theme 硬编码入口

- 当前状态：`implemented`
- 变更：删除 `Controller/Router`、`Controller/Frontend/Index` 与 Router 单元测试；Theme 账户下拉去掉硬编码「我的收藏」，改为挂载 `header-account-links`（由本模块贡献）；full-header / sidebar-menu 演示链改为 `customer/account/index#wishlist`；版本 `1.0.2` → `1.0.3`。
- 验证：Wishlist 单元测试；首页仅保留 Wishlist hook 贡献的收藏链接。

## 2026-08-30 — 补账户收藏 Hook 规约

- 当前状态：`implemented`
- 问题：已有 Hook 实现 `frontend/account/index/wishlist.phtml`，但缺少 `hook.php` 规约，导致 `setup:upgrade` 中断。
- 变更：新增 `hook.php` + `doc/hook/frontend/account/index/wishlist.md`；版本 `1.0.1` → `1.0.2`。

## 2026-08-30 — 个人中心收藏入口统一 customer/

- 当前状态：`implemented`
- 问题：侧栏/头部链接使用 `@url{'wishlist/frontend/index'}` → `/wishlist/frontend/index` 404；个人中心规范要求 `customer/` 前缀。
- 变更：全部收藏入口改为 `customer/account/index#wishlist`；补 `account.sidebar.content` 与账户分区 Hook；`Frontend/Index` 将 `/wishlist` 重定向到账户分区；模块版本 `1.0.0` → `1.0.1`。
- 验证：Wishlist 单元测试；探活 `customer/account/index`。

## 2026-08-30 — 列表页改 modules + data-weline-load

- 当前状态：`implemented`
- 变更：抽出 `wishlist-page.js`，`weline.modules.js` 注册 `wishlist`；列表根节点 `data-weline-load="wishlist,api"`，删除内联业务 script。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。

## 2026-08-26 · 列表页产品卡 hover 快捷按钮

- 范围：`view/templates/frontend/wishlist/index.phtml`、i18n、模板契约测试。
- 变更：愿望清单产品卡对齐 Theme `shopper-actions` 片段（收藏/对比/快速查看）；补充 `.product-actions` hover 样式；列表页心形按钮默认 `is-active`，点击走移除并刷新。
- 验证：单元测试 `WishlistPageTemplateContractTest`；Compare 全局 `product-card-actions.js` 负责对比与快速查看交互。

## 2026-08-26 · 列表页首次访问空数据修复 + 紧凑卡片

- 范围：`WishlistPagePresenter`、`WishlistSessionStore`、愿望清单模板与控制器。
- 变更：列表数据改在模板渲染阶段解析（对齐 Compare），Cookie 读取增加网站作用域 `$_COOKIE` 回退；产品卡改为 2/3/4/5 列紧凑网格。
- 验证：单元测试通过；首次访问 `/wishlist` 应直接展示已收藏商品。

## 2026-08-26 · 列表页版心对齐主题变量

- 范围：`view/templates/frontend/wishlist/index.phtml`
- 变更：愿望清单内容区改用 `--weline-layout-content-max-width` 与 `--weline-layout-content-padding-inline`，并与 Compare 页同样的全宽壳 + 版心内边距写法对齐。
- 验证：`/wishlist` 返回 200，页面 HTML 含 `storefront-wishlist-layout` 与主题版心变量。

## 文档初始化

- 由 MCP 自动修复流程补齐文档契约。
- 本记录不推断或补造模块历史；后续变更应记录版本、范围与验证证据。
