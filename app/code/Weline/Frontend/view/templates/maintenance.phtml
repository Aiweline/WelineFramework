<?php
$locale = 'zh_Hans_CN';
try {
    $locale = \Weline\Framework\App\State::getLangLocal() ?: \Weline\Framework\App\State::getLang() ?: $locale;
} catch (\Throwable) {
}
$logo = (string)($this->getData('maintenance_logo_dark') ?: $this->getData('maintenance_logo_light'));
$retryUrl = (string)($_SERVER['REQUEST_URI'] ?? '/');
?>
<!doctype html>
<html lang="<?= htmlspecialchars(str_replace('_', '-', $locale), ENT_QUOTES, 'UTF-8') ?>" data-local="<?= htmlspecialchars($locale, ENT_QUOTES, 'UTF-8') ?>" data-w-area="frontend" data-theme-preference="system" data-theme="light">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Cache-Control" content="no-store, no-cache, must-revalidate">
    <title><?= htmlspecialchars((string)__('网站维护中…'), ENT_QUOTES, 'UTF-8') ?></title>
    <w:template>Weline_Theme::partials/head/site-favicon.phtml</w:template>
    <css>Weline_Theme::ui/weline-foundation.css</css>
    <css>Weline_Theme::ui/weline-frontend.css</css>
    <js type="module">Weline_Theme::ui/weline-ui.js</js>
</head>
<body>
    <main class="w-maintenance-shell">
        <section class="w-card w-maintenance-card" weline-code="frontend.maintenance.card">
            <div class="w-card__body w-stack">
                <?php if ($logo !== ''): ?><img class="w-maintenance-logo" src="<?= htmlspecialchars($logo, ENT_QUOTES, 'UTF-8') ?>" alt="Weline"><?php endif; ?>
                <div class="w-empty-state"><w:icon name="settings" size="xl" /><h1><?= htmlspecialchars((string)__('网站正在维护中…'), ENT_QUOTES, 'UTF-8') ?></h1><p><?= htmlspecialchars((string)__('我们正在升级系统与数据，请稍后再试。'), ENT_QUOTES, 'UTF-8') ?></p><div class="w-cluster"><a class="w-button" href="<?= htmlspecialchars($retryUrl, ENT_QUOTES, 'UTF-8') ?>"><w:icon name="refresh" size="sm" /><?= htmlspecialchars((string)__('刷新页面'), ENT_QUOTES, 'UTF-8') ?></a><a class="w-button" data-tone="neutral" href="@url('/')"><w:icon name="home" size="sm" /><?= htmlspecialchars((string)__('返回首页'), ENT_QUOTES, 'UTF-8') ?></a></div></div>
            </div>
            <footer class="w-card__footer"><small>&copy; <?= date('Y') ?> Weline</small></footer>
        </section>
    </main>
</body>
</html>
