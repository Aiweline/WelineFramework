# 汇审：后台聊天控制台可对话

- **日期**：2026-09-21
- **席位**：Team:监工 / Team:架构师 / Team:后端 / Team:前端 / Team:测试
- **触发**：聊天控制台截图为「完整对话体验即将接入」占位，不能选角色发消息。

## 审图

- 图类型：`web_ui` + `ui_shot`
- 线稿：左角色/最近会话，右时间线 + composer
- 原型：角色列表 A/B/C 与聊天无关；本页按 Hook 侧栏/消息工具位做成单一可对话布局，不再做视觉变体。

## 改动

1. BinQuery `sendMessage` / `getChatHistory`（ACL `Weline_Agent::console_index`）。
2. `AgentAdminService::sendMessage` 走 `AgentEngine::execute`。
3. `AiService::generateStructured` 补齐引擎契约；场景适配器缺失时退回无场景生成。
4. `Chat/index`：可选角色、composer、时间线、最近会话、hook 位。
5. 版本 `1.1.3` → `1.1.4`。

## 验收

- UT：`AgentQueryProviderWriteParamsContractTest` 5/5 PASS。
- e2e：chapter `Weline_Agent-chat-console` PASS；plan-suite PASS。
- Browser：选中 SEO 助手，发送「用一句话介绍你自己」后时间线出现用户气泡；页面仍是 HTML。当前模型 `translategemma:12b` 连不上 `127.0.0.1:11434`，时间线显示系统错误气泡（未启动本地模型服务）。

## 结论

聊天控制台已可对话。模型服务未开时失败留在对话区，不整页 JSON。
