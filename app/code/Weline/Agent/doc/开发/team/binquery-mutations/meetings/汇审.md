# 汇审：Agent 写操作 BinQuery 收口

- **日期**：2026-09-21
- **席位**：Team:监工 / Team:架构师 / Team:后端 / Team:前端 / Team:测试
- **触发**：`/agent/backend/role/save` 浏览器整页 JSON（`{"success":true,"msg":"保存成功"}`）

## 审图

- 图类型：`web_ui` + `error_shot`（业务交互错误页，非审美 UI）
- 根因①：表单原生 POST 命中 `fetchJson` 控制器。
- 根因②：BinQuery 前端 worker 拒未声明参数（`Unknown frontend worker param: status`），`saveRole`/`saveSchedule` descriptor 缺字段。

## 改动

1. `AgentAdminService`：统一 save/delete/toggle/suggest/run/archive。
2. `extends/.../Query/AgentQueryProvider`：provider=`agent`；补齐 write params（含 `status` 等）。
3. `agent-admin-api.js`：`PAYLOAD_OPS` 自动包 `payload`；列表 `data-agent-op` 绑 BinQuery。
4. Role/Schedule 表单：`Weline.Api.resource('agent')`；禁整页 `fetch`。
5. `AgentHtmlSafeMutationTrait`：HTML Accept → MessageManager + redirect listing；`models`/`adapters`/`suggest` GET 亦防整页 JSON。
6. Skill/Session 写操作改走 AdminService + 同安全网；列表加启用/归档等 BinQuery 按钮。

## 验收证据

- UT：`AgentQueryProviderWriteParamsContractTest` 3/3 PASS。
- Browser：角色保存 → listing HTML；调度新建 → listing；`role/models|adapters` → listing；skill `toggleSkill` BinQuery OK；session/memory/chat 均为 HTML 壳。
- e2e：`php bin/w e2e:run --module=Weline_Agent` — plan-suite PASS；chapter TC-01~04 PASS。

## 结论

写链路对齐 BinQuery；整页 JSON 路径已堵死。可交付。
