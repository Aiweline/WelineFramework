# Weline_Agent（智能体）

## 模块定位

站内 AI **智能体**运行时：显式角色、技能/工具、会话、记忆与定时调度。依赖 `Weline_Ai` 做模型调用。

原模块名 `Weline_Bot` 已于 1.1.0 全面重命名为本模块。

## 知识维护约定

- 长期事实写入本模块 `doc/`。
- 不在本文复制全局规则或客户端规则。
- 无法由当前证据确认的行为必须标记待确认。
