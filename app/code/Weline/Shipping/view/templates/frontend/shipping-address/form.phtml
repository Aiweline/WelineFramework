<?php
/**@var \Weline\Framework\View\Template $this */
$address = $this->getData('address');
$frontendUrl = $this->getUrl('');
$isEdit = $address && $address->getId();
?>
<div class="shipping-address-form">
    <div class="page-header mb-4">
        <h2><?= $isEdit ? __('编辑发货地址') : __('新增发货地址') ?></h2>
        <a href="<?= $frontendUrl ?>shipping/address/index" class="btn btn-secondary">
            <i class="ri-arrow-left-line"></i> <?= __('返回列表') ?>
        </a>
    </div>

    <form id="addressForm" method="POST" action="<?= $frontendUrl ?>shipping/address/save">
        <?php if ($isEdit): ?>
            <input type="hidden" name="shipping_address_id" value="<?= $address->getId() ?>">
        <?php endif; ?>

        <div class="form-group mb-3">
            <label for="name" class="form-label"><?= __('地址名称') ?> <span class="text-danger">*</span></label>
            <input type="text" class="form-control" id="name" name="name" 
                   value="<?= $address ? htmlspecialchars($address->getData('name')) : '' ?>" required>
            <small class="form-text text-muted"><?= __('例如：主仓库、分仓库等') ?></small>
        </div>

        <div class="row">
            <div class="col-md-6 mb-3">
                <label for="contact_name" class="form-label"><?= __('联系人姓名') ?> <span class="text-danger">*</span></label>
                <input type="text" class="form-control" id="contact_name" name="contact_name" 
                       value="<?= $address ? htmlspecialchars($address->getData('contact_name')) : '' ?>" required>
            </div>
            <div class="col-md-6 mb-3">
                <label for="contact_phone" class="form-label"><?= __('联系电话') ?> <span class="text-danger">*</span></label>
                <input type="tel" class="form-control" id="contact_phone" name="contact_phone" 
                       value="<?= $address ? htmlspecialchars($address->getData('contact_phone')) : '' ?>" required>
            </div>
        </div>

        <div class="row">
            <div class="col-md-4 mb-3">
                <label for="country" class="form-label"><?= __('国家') ?></label>
                <input type="text" class="form-control" id="country" name="country" 
                       value="<?= $address ? htmlspecialchars($address->getData('country') ?: '中国') : '中国' ?>">
            </div>
            <div class="col-md-4 mb-3">
                <label for="province" class="form-label"><?= __('省份') ?> <span class="text-danger">*</span></label>
                <input type="text" class="form-control" id="province" name="province" 
                       value="<?= $address ? htmlspecialchars($address->getData('province')) : '' ?>" required>
            </div>
            <div class="col-md-4 mb-3">
                <label for="city" class="form-label"><?= __('城市') ?> <span class="text-danger">*</span></label>
                <input type="text" class="form-control" id="city" name="city" 
                       value="<?= $address ? htmlspecialchars($address->getData('city')) : '' ?>" required>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6 mb-3">
                <label for="district" class="form-label"><?= __('区县') ?></label>
                <input type="text" class="form-control" id="district" name="district" 
                       value="<?= $address ? htmlspecialchars($address->getData('district')) : '' ?>">
            </div>
            <div class="col-md-6 mb-3">
                <label for="postal_code" class="form-label"><?= __('邮政编码') ?></label>
                <input type="text" class="form-control" id="postal_code" name="postal_code" 
                       value="<?= $address ? htmlspecialchars($address->getData('postal_code')) : '' ?>" 
                       pattern="[0-9]{6}" maxlength="6">
            </div>
        </div>

        <div class="form-group mb-3">
            <label for="street" class="form-label"><?= __('街道地址') ?> <span class="text-danger">*</span></label>
            <textarea class="form-control" id="street" name="street" rows="3" required><?= $address ? htmlspecialchars($address->getData('street')) : '' ?></textarea>
        </div>

        <div class="form-check mb-3">
            <input class="form-check-input" type="checkbox" id="is_default" name="is_default" value="1" 
                   <?= $address && $address->isDefault() ? 'checked' : '' ?>>
            <label class="form-check-label" for="is_default">
                <?= __('设为默认地址') ?>
            </label>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn btn-primary">
                <i class="ri-save-line"></i> <?= __('保存') ?>
            </button>
            <a href="<?= $frontendUrl ?>shipping/address/index" class="btn btn-secondary">
                <?= __('取消') ?>
            </a>
        </div>
    </form>
</div>

<style>
.shipping-address-form {
    padding: 20px;
    max-width: 800px;
}
.page-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
}
.form-actions {
    margin-top: 20px;
    display: flex;
    gap: 10px;
}
</style>

