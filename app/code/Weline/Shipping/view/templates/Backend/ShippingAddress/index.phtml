<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */


use Weline\Shipping\Model\ShippingAddress;

$addresses = $this->getData('addresses') ?? [];
$total = $this->getData('total') ?? 0;
$page = $this->getData('page') ?? 1;
$limit = $this->getData('limit') ?? 20;
$totalPages = $this->getData('total_pages') ?? 1;
$keyword = $this->getData('keyword') ?? '';
$isEnabled = $this->getData('is_enabled');

$backendBase = $this->getBackendUrl('shipping/backend/shippingaddress');
$createUrl = $this->getBackendUrl('shipping/backend/shippingaddress/edit');
$dialogJsonFlags = JSON_HEX_TAG | JSON_HEX_APOS | JSON_HEX_QUOT | JSON_HEX_AMP | JSON_THROW_ON_ERROR;
$confirmMessages = [
    'confirmTitle' => (string)__('确认操作'),
    'confirmButton' => (string)__('确定'),
    'cancelButton' => (string)__('取消'),
    'setDefault' => (string)__('确定要设置为默认地址吗？'),
    'deleteAddress' => (string)__('确定要删除这个地址吗？'),
];
?>
<div class="row">
                    <div class="col-12">
                        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                            <h4 class="mb-sm-0"><?= __('发货地址管理') ?></h4>
                            <div class="page-title-right">
                                <ol class="breadcrumb m-0">
                                    <li class="breadcrumb-item"><a href="@backend-url('admin/index/index')"><?= __('系统') ?></a></li>
                                    <li class="breadcrumb-item active"><?= __('发货地址管理') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header">
                                <div class="row align-items-center">
                                    <div class="col-md-6">
                                        <h5 class="card-title mb-0"><?= __('发货地址列表') ?></h5>
                                    </div>
                                    <div class="col-md-6 text-end">
                                        <a href="<?= $createUrl ?>" class="btn btn-primary">
                                            <i class="mdi mdi-plus"></i> <?= __('新增地址') ?>
                                        </a>
                                    </div>
                                </div>
                            </div>
                            <div class="card-body">
                                <form method="get" action="<?= $backendBase ?>" class="mb-3">
                                    <div class="row g-3">
                                        <div class="col-md-4">
                                            <input type="text" name="keyword" class="form-control" 
                                                   placeholder="<?= __('搜索地址名称、联系人、电话或街道') ?>" 
                                                   value="<?= htmlspecialchars($keyword) ?>">
                                        </div>
                                        <div class="col-md-3">
                                            <select name="is_enabled" class="form-select">
                                                <option value=""><?= __('全部状态') ?></option>
                                                <option value="1" <?= $isEnabled === '1' ? 'selected' : '' ?>><?= __('启用') ?></option>
                                                <option value="0" <?= $isEnabled === '0' ? 'selected' : '' ?>><?= __('禁用') ?></option>
                                            </select>
                                        </div>
                                        <div class="col-md-3">
                                            <button type="submit" class="btn btn-primary w-100">
                                                <i class="mdi mdi-magnify"></i> <?= __('搜索') ?>
                                            </button>
                                        </div>
                                        <div class="col-md-2">
                                            <a href="<?= $backendBase ?>" class="btn btn-secondary w-100">
                                                <i class="mdi mdi-refresh"></i> <?= __('重置') ?>
                                            </a>
                                        </div>
                                    </div>
                                </form>

                                <div class="table-responsive">
                                    <table class="table table-bordered table-hover">
                                        <thead>
                                            <tr>
                                                <th><?= __('ID') ?></th>
                                                <th><?= __('地址名称') ?></th>
                                                <th><?= __('联系人') ?></th>
                                                <th><?= __('联系电话') ?></th>
                                                <th><?= __('地址') ?></th>
                                                <th><?= __('默认') ?></th>
                                                <th><?= __('状态') ?></th>
                                                <th><?= __('创建时间') ?></th>
                                                <th><?= __('操作') ?></th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <?php if (empty($addresses)): ?>
                                                <tr>
                                                    <td colspan="9" class="text-center"><?= __('暂无数据') ?></td>
                                                </tr>
                                            <?php else: ?>
                                                <?php foreach ($addresses as $address): ?>
                                                    <tr>
                                                        <td><?= $address[ShippingAddress::fields_ID] ?? '' ?></td>
                                                        <td><?= htmlspecialchars($address[ShippingAddress::fields_NAME] ?? '') ?></td>
                                                        <td><?= htmlspecialchars($address[ShippingAddress::fields_CONTACT_NAME] ?? '') ?></td>
                                                        <td><?= htmlspecialchars($address[ShippingAddress::fields_CONTACT_PHONE] ?? '') ?></td>
                                                        <td>
                                                            <?= htmlspecialchars(
                                                                ($address[ShippingAddress::fields_COUNTRY] ?? '') .
                                                                ($address[ShippingAddress::fields_PROVINCE] ?? '') .
                                                                ($address[ShippingAddress::fields_CITY] ?? '') .
                                                                ($address[ShippingAddress::fields_DISTRICT] ?? '') .
                                                                ($address[ShippingAddress::fields_STREET] ?? '')
                                                            ) ?>
                                                        </td>
                                                        <td>
                                                            <?php if (!empty($address[ShippingAddress::fields_IS_DEFAULT])): ?>
                                                                <span class="badge bg-success"><?= __('是') ?></span>
                                                            <?php else: ?>
                                                                <span class="badge bg-secondary"><?= __('否') ?></span>
                                                            <?php endif; ?>
                                                        </td>
                                                        <td>
                                                            <?php if (!empty($address[ShippingAddress::fields_IS_ENABLED])): ?>
                                                                <span class="badge bg-success"><?= __('启用') ?></span>
                                                            <?php else: ?>
                                                                <span class="badge bg-danger"><?= __('禁用') ?></span>
                                                            <?php endif; ?>
                                                        </td>
                                                        <td><?= $address[ShippingAddress::fields_CREATED_AT] ?? '' ?></td>
                                                        <td>
                                                            <div class="btn-group btn-group-sm">
                                                                <a href="<?= $this->getBackendUrl('shipping/backend/shippingaddress/edit', ['id' => $address[ShippingAddress::fields_ID]]) ?>" 
                                                                   class="btn btn-primary btn-sm">
                                                                    <i class="mdi mdi-pencil"></i>
                                                                </a>
                                                                <?php if (empty($address[ShippingAddress::fields_IS_DEFAULT])): ?>
                                                                    <a href="<?= $this->getBackendUrl('shipping/backend/shippingaddress/setDefault', ['id' => $address[ShippingAddress::fields_ID]]) ?>" 
                                                                       class="btn btn-info btn-sm" 
                                                                       data-shipping-address-confirm="setDefault">
                                                                        <i class="mdi mdi-star"></i>
                                                                    </a>
                                                                <?php endif; ?>
                                                                <?php if (!empty($address[ShippingAddress::fields_IS_ENABLED])): ?>
                                                                    <a href="<?= $this->getBackendUrl('shipping/backend/shippingaddress/disable', ['id' => $address[ShippingAddress::fields_ID]]) ?>" 
                                                                       class="btn btn-warning btn-sm">
                                                                        <i class="mdi mdi-cancel"></i>
                                                                    </a>
                                                                <?php else: ?>
                                                                    <a href="<?= $this->getBackendUrl('shipping/backend/shippingaddress/enable', ['id' => $address[ShippingAddress::fields_ID]]) ?>" 
                                                                       class="btn btn-success btn-sm">
                                                                        <i class="mdi mdi-check-circle"></i>
                                                                    </a>
                                                                <?php endif; ?>
                                                                <a href="<?= $this->getBackendUrl('shipping/backend/shippingaddress/delete', ['id' => $address[ShippingAddress::fields_ID]]) ?>" 
                                                                   class="btn btn-danger btn-sm" 
                                                                   data-shipping-address-confirm="deleteAddress">
                                                                    <i class="mdi mdi-delete"></i>
                                                                </a>
                                                            </div>
                                                        </td>
                                                    </tr>
                                                <?php endforeach; ?>
                                            <?php endif; ?>
                                        </tbody>
                                    </table>
                                </div>

                                <?php if ($totalPages > 1): ?>
                                    <nav aria-label="Page navigation">
                                        <ul class="pagination justify-content-center">
                                            <?php if ($page > 1): ?>
                                                <li class="page-item">
                                                    <a class="page-link" href="<?= $backendBase ?>?page=<?= $page - 1 ?>&limit=<?= $limit ?>&keyword=<?= urlencode($keyword) ?>&is_enabled=<?= $isEnabled ?>">
                                                        <?= __('上一页') ?>
                                                    </a>
                                                </li>
                                            <?php endif; ?>
                                            <?php for ($i = 1; $i <= $totalPages; $i++): ?>
                                                <li class="page-item <?= $i === $page ? 'active' : '' ?>">
                                                    <a class="page-link" href="<?= $backendBase ?>?page=<?= $i ?>&limit=<?= $limit ?>&keyword=<?= urlencode($keyword) ?>&is_enabled=<?= $isEnabled ?>">
                                                        <?= $i ?>
                                                    </a>
                                                </li>
                                            <?php endfor; ?>
                                            <?php if ($page < $totalPages): ?>
                                                <li class="page-item">
                                                    <a class="page-link" href="<?= $backendBase ?>?page=<?= $page + 1 ?>&limit=<?= $limit ?>&keyword=<?= urlencode($keyword) ?>&is_enabled=<?= $isEnabled ?>">
                                                        <?= __('下一页') ?>
                                                    </a>
                                                </li>
                                            <?php endif; ?>
                                        </ul>
                                    </nav>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
<script>
(function () {
    'use strict';

    const messages = <?= json_encode($confirmMessages, $dialogJsonFlags) ?>;

    function showLocalChoice(message) {
        return new Promise(function(resolve) {
            const overlay = document.createElement('div');
            overlay.style.cssText = 'position:fixed;inset:0;z-index:1055;background:rgba(15,23,42,.45);display:flex;align-items:center;justify-content:center;padding:20px;';

            const dialog = document.createElement('div');
            dialog.setAttribute('role', 'dialog');
            dialog.setAttribute('aria-modal', 'true');
            dialog.style.cssText = 'width:min(420px,100%);background:#fff;border-radius:8px;box-shadow:0 20px 60px rgba(15,23,42,.25);padding:24px;';

            const title = document.createElement('h4');
            title.textContent = messages.confirmTitle;
            title.style.cssText = 'margin:0 0 12px;font-size:18px;';

            const body = document.createElement('p');
            body.textContent = message;
            body.style.cssText = 'margin:0 0 20px;color:#475569;';

            const actions = document.createElement('div');
            actions.style.cssText = 'display:flex;justify-content:flex-end;gap:10px;';

            const cancelButton = document.createElement('button');
            cancelButton.type = 'button';
            cancelButton.className = 'btn btn-secondary';
            cancelButton.textContent = messages.cancelButton;

            const confirmButton = document.createElement('button');
            confirmButton.type = 'button';
            confirmButton.className = 'btn btn-primary';
            confirmButton.textContent = messages.confirmButton;

            function close(value) {
                document.removeEventListener('keydown', onKeydown);
                overlay.remove();
                resolve(value);
            }

            function onKeydown(event) {
                if (event.key === 'Escape') {
                    close(false);
                }
            }

            cancelButton.addEventListener('click', function() { close(false); });
            confirmButton.addEventListener('click', function() { close(true); });
            overlay.addEventListener('click', function(event) {
                if (event.target === overlay) {
                    close(false);
                }
            });

            actions.append(cancelButton, confirmButton);
            dialog.append(title, body, actions);
            overlay.appendChild(dialog);
            document.body.appendChild(overlay);
            document.addEventListener('keydown', onKeydown);
            confirmButton.focus();
        });
    }

    function showShippingAddressChoice(message) {
        if (typeof Swal !== 'undefined' && typeof Swal.fire === 'function') {
            return Swal.fire({
                icon: 'warning',
                title: messages.confirmTitle,
                text: message,
                showCancelButton: true,
                confirmButtonText: messages.confirmButton,
                cancelButtonText: messages.cancelButton
            }).then(function(result) {
                return !!(result && result.isConfirmed);
            });
        }

        return showLocalChoice(message);
    }

    document.querySelectorAll('[data-shipping-address-confirm]').forEach(function(link) {
        link.addEventListener('click', function(event) {
            const key = link.getAttribute('data-shipping-address-confirm');
            const message = messages[key];
            if (!message) {
                return;
            }

            event.preventDefault();
            showShippingAddressChoice(message).then(function(confirmed) {
                if (confirmed) {
                    window.location.href = link.href;
                }
            });
        });
    });
})();
</script>
