<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */


use Weline\Shipping\Model\ShippingAddress;

$addresses = $this->getData('addresses') ?? [];
$total = $this->getData('total') ?? 0;
$page = $this->getData('page') ?? 1;
$limit = $this->getData('limit') ?? 20;
$totalPages = $this->getData('total_pages') ?? 1;
$keyword = $this->getData('keyword') ?? '';
$isEnabled = $this->getData('is_enabled');

$backendBase = $this->getBackendUrl('shipping/backend/shippingaddress');
$createUrl = $this->getBackendUrl('shipping/backend/shippingaddress/edit');
$dialogJsonFlags = JSON_HEX_TAG | JSON_HEX_APOS | JSON_HEX_QUOT | JSON_HEX_AMP | JSON_THROW_ON_ERROR;
$confirmMessages = [
    'confirmTitle' => (string)__('确认操作'),
    'confirmButton' => (string)__('确定'),
    'cancelButton' => (string)__('取消'),
    'setDefault' => (string)__('确定要设置为默认地址吗？'),
    'deleteAddress' => (string)__('确定要删除这个地址吗？'),
];
?>
<div class="w-grid" data-testid="shipping-address-management">
                    <div style="--w-span:12;">
                        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
                            <h4 style="--w-mb-sm:0;"><?= __('发货地址管理') ?></h4>
                            <div class="w-cluster">
                                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                                    <li class="w-breadcrumb__item"><a href="@backend-url('admin/index/index')"><?= __('系统') ?></a></li>
                                    <li class="w-breadcrumb__item" data-state="active"><?= __('发货地址管理') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="w-grid">
                    <div style="--w-span:12;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <div class="w-grid" data-align="center">
                                    <div style="--w-span-md:6;">
                                        <h5 class="w-card__title" style="--w-mb:0;"><?= __('发货地址列表') ?></h5>
                                    </div>
                                    <div class="w-text" data-align="end" style="--w-span-md:6;">
                                        <a href="<?= $createUrl ?>" class="w-button" data-testid="shipping-address-create" data-tone="primary">
                                            <w:icon name="plus" size="sm"></w:icon> <?= __('新增地址') ?>
                                        </a>
                                    </div>
                                </div>
                            </div>
                            <div class="w-card__body">
                                <w:form method="get" action="@var($backendBase)" style="--w-mb:var(--weline-space-4);">
                                    <div class="w-grid" style="--w-gap:var(--weline-space-4);">
                                        <div style="--w-span-md:4;">
                                            <input type="text" name="keyword" class="w-input" placeholder="<?= __('搜索地址名称、联系人、电话或街道') ?>" value="<?= htmlspecialchars($keyword) ?>">
                                        </div>
                                        <div style="--w-span-md:3;">
                                            <select name="is_enabled" class="w-select">
                                                <option value=""><?= __('全部状态') ?></option>
                                                <option value="1" <?= $isEnabled === '1' ? 'selected' : '' ?>><?= __('启用') ?></option>
                                                <option value="0" <?= $isEnabled === '0' ? 'selected' : '' ?>><?= __('禁用') ?></option>
                                            </select>
                                        </div>
                                        <div style="--w-span-md:3;">
                                            <button type="submit" class="w-button" data-w-width="full" data-tone="primary">
                                                <w:icon name="search" size="sm"></w:icon> <?= __('搜索') ?>
                                            </button>
                                        </div>
                                        <div style="--w-span-md:2;">
                                            <a href="<?= $backendBase ?>" class="w-button" data-w-width="full" data-tone="neutral">
                                                <w:icon name="refresh" size="sm"></w:icon> <?= __('重置') ?>
                                            </a>
                                        </div>
                                    </div>
                                </w:form>

                                <div class="w-table-wrap">
                                    <table class="w-table">
                                        <thead>
                                            <tr>
                                                <th><?= __('ID') ?></th>
                                                <th><?= __('地址名称') ?></th>
                                                <th><?= __('联系人') ?></th>
                                                <th><?= __('联系电话') ?></th>
                                                <th><?= __('地址') ?></th>
                                                <th><?= __('默认') ?></th>
                                                <th><?= __('状态') ?></th>
                                                <th><?= __('创建时间') ?></th>
                                                <th><?= __('操作') ?></th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <?php if (empty($addresses)): ?>
                                                <tr>
                                                    <td colspan="9" class="w-text" data-align="center"><?= __('暂无数据') ?></td>
                                                </tr>
                                            <?php else: ?>
                                                <?php foreach ($addresses as $address): ?>
                                                    <tr>
                                                        <td><?= $address[ShippingAddress::schema_fields_ID] ?? '' ?></td>
                                                        <td><?= htmlspecialchars($address[ShippingAddress::schema_fields_NAME] ?? '') ?></td>
                                                        <td><?= htmlspecialchars($address[ShippingAddress::schema_fields_CONTACT_NAME] ?? '') ?></td>
                                                        <td><?= htmlspecialchars($address[ShippingAddress::schema_fields_CONTACT_PHONE] ?? '') ?></td>
                                                        <td>
                                                            <?= htmlspecialchars(
                                                                ($address[ShippingAddress::schema_fields_COUNTRY] ?? '') .
                                                                ($address[ShippingAddress::schema_fields_PROVINCE] ?? '') .
                                                                ($address[ShippingAddress::schema_fields_CITY] ?? '') .
                                                                ($address[ShippingAddress::schema_fields_DISTRICT] ?? '') .
                                                                ($address[ShippingAddress::schema_fields_STREET] ?? '')
                                                            ) ?>
                                                        </td>
                                                        <td>
                                                            <?php if (!empty($address[ShippingAddress::schema_fields_IS_DEFAULT])): ?>
                                                                <span class="w-badge" data-w-background="success"><?= __('是') ?></span>
                                                            <?php else: ?>
                                                                <span class="w-badge" data-w-background="muted"><?= __('否') ?></span>
                                                            <?php endif; ?>
                                                        </td>
                                                        <td>
                                                            <?php if (!empty($address[ShippingAddress::schema_fields_IS_ENABLED])): ?>
                                                                <span class="w-badge" data-w-background="success"><?= __('启用') ?></span>
                                                            <?php else: ?>
                                                                <span class="w-badge" data-w-background="danger"><?= __('禁用') ?></span>
                                                            <?php endif; ?>
                                                        </td>
                                                        <td><?= $address[ShippingAddress::schema_fields_CREATED_AT] ?? '' ?></td>
                                                        <td>
                                                            <div class="w-button-group">
                                                                <a href="<?= $this->getBackendUrl('shipping/backend/shippingaddress/edit', ['id' => $address[ShippingAddress::schema_fields_ID]]) ?>" class="w-button" data-tone="primary" data-size="sm">
                                                                    <w:icon name="edit" size="sm"></w:icon>
                                                                </a>
                                                                <?php if (empty($address[ShippingAddress::schema_fields_IS_DEFAULT])): ?>
                                                                    <a href="<?= $this->getBackendUrl('shipping/backend/shippingaddress/setDefault', ['id' => $address[ShippingAddress::schema_fields_ID]]) ?>" class="w-button" data-shipping-address-confirm="setDefault" data-tone="info" data-size="sm">
                                                                        <w:icon name="star" size="sm"></w:icon>
                                                                    </a>
                                                                <?php endif; ?>
                                                                <?php if (!empty($address[ShippingAddress::schema_fields_IS_ENABLED])): ?>
                                                                    <a href="<?= $this->getBackendUrl('shipping/backend/shippingaddress/disable', ['id' => $address[ShippingAddress::schema_fields_ID]]) ?>" class="w-button" data-tone="warning" data-size="sm">
                                                                        <w:icon name="close" size="sm"></w:icon>
                                                                    </a>
                                                                <?php else: ?>
                                                                    <a href="<?= $this->getBackendUrl('shipping/backend/shippingaddress/enable', ['id' => $address[ShippingAddress::schema_fields_ID]]) ?>" class="w-button" data-tone="success" data-size="sm">
                                                                        <w:icon name="check" size="sm"></w:icon>
                                                                    </a>
                                                                <?php endif; ?>
                                                                <a href="<?= $this->getBackendUrl('shipping/backend/shippingaddress/delete', ['id' => $address[ShippingAddress::schema_fields_ID]]) ?>" class="w-button" data-shipping-address-confirm="deleteAddress" data-tone="danger" data-size="sm">
                                                                    <w:icon name="trash" size="sm"></w:icon>
                                                                </a>
                                                            </div>
                                                        </td>
                                                    </tr>
                                                <?php endforeach; ?>
                                            <?php endif; ?>
                                        </tbody>
                                    </table>
                                </div>

                                <?php if ($totalPages > 1): ?>
                                    <nav aria-label="Page navigation">
                                        <ul class="w-pagination" data-justify="center">
                                            <?php if ($page > 1): ?>
                                                <li class="w-pagination__item">
                                                    <a class="w-pagination__link" href="<?= $backendBase ?>?page=<?= $page - 1 ?>&limit=<?= $limit ?>&keyword=<?= urlencode($keyword) ?>&is_enabled=<?= $isEnabled ?>">
                                                        <?= __('上一页') ?>
                                                    </a>
                                                </li>
                                            <?php endif; ?>
                                            <?php for ($i = 1; $i <= $totalPages; $i++): ?>
                                                <li class="w-pagination__item <?= $i === $page ? 'active' : '' ?>">
                                                    <a class="w-pagination__link" href="<?= $backendBase ?>?page=<?= $i ?>&limit=<?= $limit ?>&keyword=<?= urlencode($keyword) ?>&is_enabled=<?= $isEnabled ?>">
                                                        <?= $i ?>
                                                    </a>
                                                </li>
                                            <?php endfor; ?>
                                            <?php if ($page < $totalPages): ?>
                                                <li class="w-pagination__item">
                                                    <a class="w-pagination__link" href="<?= $backendBase ?>?page=<?= $page + 1 ?>&limit=<?= $limit ?>&keyword=<?= urlencode($keyword) ?>&is_enabled=<?= $isEnabled ?>">
                                                        <?= __('下一页') ?>
                                                    </a>
                                                </li>
                                            <?php endif; ?>
                                        </ul>
                                    </nav>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
<script>
(function () {
    'use strict';

    const messages = <?= json_encode($confirmMessages, $dialogJsonFlags) ?>;

    function showLocalChoice(message) {
        return new Promise(function(resolve) {
            const overlay = document.createElement('div');
            overlay.style.cssText = 'position:fixed;inset:0;z-index:1055;background:rgba(15,23,42,.45);display:flex;align-items:center;justify-content:center;padding:20px;';

            const dialog = document.createElement('div');
            dialog.setAttribute('role', 'dialog');
            dialog.setAttribute('aria-modal', 'true');
            dialog.style.cssText = 'width:min(420px,100%);background:#fff;border-radius:8px;box-shadow:0 20px 60px rgba(15,23,42,.25);padding:24px;';

            const title = document.createElement('h4');
            title.textContent = messages.confirmTitle;
            title.style.cssText = 'margin:0 0 12px;font-size:18px;';

            const body = document.createElement('p');
            body.textContent = message;
            body.style.cssText = 'margin:0 0 20px;color:#475569;';

            const actions = document.createElement('div');
            actions.style.cssText = 'display:flex;justify-content:flex-end;gap:10px;';

            const cancelButton = document.createElement('button');
            cancelButton.type = 'button';
            cancelButton.className = 'btn btn-secondary';
            cancelButton.textContent = messages.cancelButton;

            const confirmButton = document.createElement('button');
            confirmButton.type = 'button';
            confirmButton.className = 'btn btn-primary';
            confirmButton.textContent = messages.confirmButton;

            function close(value) {
                document.removeEventListener('keydown', onKeydown);
                overlay.remove();
                resolve(value);
            }

            function onKeydown(event) {
                if (event.key === 'Escape') {
                    close(false);
                }
            }

            cancelButton.addEventListener('click', function() { close(false); });
            confirmButton.addEventListener('click', function() { close(true); });
            overlay.addEventListener('click', function(event) {
                if (event.target === overlay) {
                    close(false);
                }
            });

            actions.append(cancelButton, confirmButton);
            dialog.append(title, body, actions);
            overlay.appendChild(dialog);
            document.body.appendChild(overlay);
            document.addEventListener('keydown', onKeydown);
            confirmButton.focus();
        });
    }

    function showShippingAddressChoice(message) {
        return Weline.UI.dialog.request({
            tone: 'warning',
            title: messages.confirmTitle,
            message: message,
            cancelable: true,
            confirmLabel: messages.confirmButton,
            cancelLabel: messages.cancelButton
        }).then(function(result) {
            return !!(result && result.confirmed);
        });
    }

    document.querySelectorAll('[data-shipping-address-confirm]').forEach(function(link) {
        link.addEventListener('click', function(event) {
            const key = link.getAttribute('data-shipping-address-confirm');
            const message = messages[key];
            if (!message) {
                return;
            }

            event.preventDefault();
            showShippingAddressChoice(message).then(function(confirmed) {
                if (confirmed) {
                    window.location.href = link.href;
                }
            });
        });
    });
})();
</script>
