<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */


use Weline\Shipping\Model\ShippingService;

$services = $this->getData('services') ?? [];
$carriers = $this->getData('carriers') ?? [];
$zones = $this->getData('zones') ?? [];
$embed = $this->getData('embed') ?? false;
?>
<?php if (empty($embed)): ?>
<div class="w-grid" data-testid="shipping-service-management">
                    <div style="--w-span:12;">
                        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
                            <h4 style="--w-mb-sm:0;"><?= __('配送服务') ?></h4>
                            <div class="w-cluster">
                                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                                    <li class="w-breadcrumb__item">
                                        <a href="@backend-url('admin/index/index')"><?= __('系统') ?></a>
                                    </li>
                                    <li class="w-breadcrumb__item" data-state="active"><?= __('配送服务') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>
<?php endif; ?>

                <div class="w-grid" style="--w-mb:var(--weline-space-4);"><div style="--w-span:12;"><div class="w-card" data-testid="shipping-service-create-form">
                    <div class="w-card__header"><h5 class="w-card__title" style="--w-mb:0;"><?= __('新增配送服务') ?></h5></div>
                    <div class="w-card__body"><w:form method="post" action="@backend-url('shipping/backend/shippingservice/save')"><div class="w-grid" style="--w-gap:var(--weline-space-4);">
                        <div style="--w-span-md:3;"><label class="w-field__label"><?= __('服务名称') ?></label><input class="w-input" name="service_name" maxlength="255" required></div>
                        <div style="--w-span-md:2;"><label class="w-field__label"><?= __('服务代码') ?></label><input class="w-input" name="service_code" maxlength="50" required></div>
                        <div style="--w-span-md:2;"><label class="w-field__label"><?= __('快递公司') ?></label><select class="w-select" name="carrier_id" required><option value=""><?= __('请选择') ?></option><?php foreach ($carriers as $carrier): ?><option value="<?= (int)$carrier->getId() ?>"><?= htmlspecialchars((string)$carrier->getData(\Weline\Shipping\Model\Carrier::schema_fields_CARRIER_NAME)) ?></option><?php endforeach; ?></select></div>
                        <div style="--w-span-md:2;"><label class="w-field__label"><?= __('配送区域') ?></label><select class="w-select" name="zone_id" required><option value=""><?= __('请选择') ?></option><?php foreach ($zones as $zone): ?><option value="<?= (int)$zone->getId() ?>"><?= htmlspecialchars((string)$zone->getData(\Weline\Shipping\Model\Zone::schema_fields_ZONE_NAME)) ?></option><?php endforeach; ?></select></div>
                        <div style="--w-span-md:1;"><label class="w-field__label"><?= __('最少天数') ?></label><input class="w-input" type="number" min="0" name="estimated_days_min" value="1"></div>
                        <div style="--w-span-md:1;"><label class="w-field__label"><?= __('最多天数') ?></label><input class="w-input" type="number" min="0" name="estimated_days_max" value="3"></div>
                        <div class="w-cluster" data-align="end" style="--w-span-md:1;"><input type="hidden" name="is_active" value="1"><button class="w-button" type="submit" data-testid="shipping-service-submit" data-w-width="full" data-tone="primary"><?= __('创建') ?></button></div>
                    </div></w:form></div>
                </div></div></div>

                <div class="w-grid">
                    <div style="--w-span:12;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <h5 class="w-card__title" style="--w-mb:0;"><?= __('配送服务列表') ?></h5>
                            </div>
                            <div class="w-card__body">
                                <?php if (empty($services)): ?>
                                    <div class="w-alert" data-tone="info" style="--w-mb:0;">
                                        <?= __('当前暂无配送服务配置，请后续在此页面完善配置。') ?>
                                    </div>
                                <?php else: ?>
                                    <div class="w-table-wrap">
                                        <table class="w-table" data-w-vertical="middle">
                                            <thead>
                                            <tr>
                                                <th><?= __('服务名称') ?></th>
                                                <th><?= __('服务代码') ?></th>
                                                <th><?= __('快递公司ID') ?></th>
                                                <th><?= __('区域ID') ?></th>
                                                <th><?= __('状态') ?></th>
                                            </tr>
                                            </thead>
                                            <tbody>
                                            <?php /** @var ShippingService $service */ ?>
                                            <?php foreach ($services as $service): ?>
                                                <tr>
                                                    <td><?= htmlspecialchars($service->getData(ShippingService::schema_fields_SERVICE_NAME)) ?></td>
                                                    <td><?= htmlspecialchars($service->getData(ShippingService::schema_fields_SERVICE_CODE)) ?></td>
                                                    <td><?= htmlspecialchars($service->getData(ShippingService::schema_fields_CARRIER_ID)) ?></td>
                                                    <td><?= htmlspecialchars($service->getData(ShippingService::schema_fields_ZONE_ID)) ?></td>
                                                    <td>
                                                        <?php if ($service->getData(ShippingService::schema_fields_IS_ACTIVE)): ?>
                                                            <span class="w-badge" data-w-background="success"><?= __('启用') ?></span>
                                                        <?php else: ?>
                                                            <span class="w-badge" data-w-background="muted"><?= __('禁用') ?></span>
                                                        <?php endif; ?>
                                                    </td>
                                                </tr>
                                            <?php endforeach; ?>
                                            </tbody>
                                        </table>
                                    </div>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
