<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */

use Weline\Shipping\Model\Carrier;

$carrier = $this->getData('carrier');
$isEdit = !empty($carrier) && $carrier->getId();
$id = $carrier ? $carrier->getId() : null;

?>
<div class="w-grid">
                    <div style="--w-span:12;">
                        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
                            <h4 style="--w-mb-sm:0;"><?= $isEdit ? __('编辑快递公司') : __('新增快递公司') ?></h4>
                            <div class="w-cluster">
                                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                                    <li class="w-breadcrumb__item"><a href="@backend-url('admin/index/index')"><?= __('系统') ?></a></li>
                                    <li class="w-breadcrumb__item"><a href="@backend-url{'shipping/backend/carrier'}"><?= __('快递公司') ?></a></li>
                                    <li class="w-breadcrumb__item" data-state="active"><?= $isEdit ? __('编辑') : __('新增') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="w-grid">
                    <div style="--w-span:12;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <h5 class="w-card__title" style="--w-mb:0;"><?= $isEdit ? __('编辑快递公司') : __('新增快递公司') ?></h5>
                            </div>
                            <div class="w-card__body">
                                <w:form method="post" action="@backend-url{'shipping/backend/carrier/save'}" data-testid="shipping-carrier-editor-form">
                                    <?php if ($id): ?>
                                        <input type="hidden" name="id" value="<?= $id ?>">
                                    <?php endif; ?>
                                    
                                    <div class="w-field" style="--w-mb:var(--weline-space-4)">
                                        <label class="w-field__label"><?= __('快递公司代码') ?> <span class="w-text" data-tone="danger">*</span></label>
                                        <input type="text" name="carrier_code" class="w-input" required value="<?= htmlspecialchars($carrier ? $carrier->getData(Carrier::schema_fields_CARRIER_CODE) : '') ?>" placeholder="<?= __('请输入快递公司代码，如：SF、ZTO等') ?>" <?= $isEdit ? 'readonly' : '' ?>>
                                            <small class="w-field__hint w-text" data-tone="muted"><?= __('代码必须唯一，编辑后不可修改') ?></small>
                                    </div>

                                    <div class="w-field" style="--w-mb:var(--weline-space-4)">
                                        <label class="w-field__label"><?= __('快递公司名称') ?> <span class="w-text" data-tone="danger">*</span></label>
                                        <input type="text" name="carrier_name" class="w-input" required value="<?= htmlspecialchars($carrier ? $carrier->getData(Carrier::schema_fields_CARRIER_NAME) : '') ?>" placeholder="<?= __('请输入快递公司名称，如：顺丰速运') ?>">
                                    </div>

                                    <div class="w-field" style="--w-mb:var(--weline-space-4)">
                                        <label class="w-field__label"><?= __('类型') ?> <span class="w-text" data-tone="danger">*</span></label>
                                        <select name="carrier_type" class="w-select" id="carrier_type" required>
                                                <option value="<?= Carrier::TYPE_MANUAL ?>" <?= (!$carrier || $carrier->getData(Carrier::schema_fields_CARRIER_TYPE) === Carrier::TYPE_MANUAL) ? 'selected' : '' ?>>
                                                    <?= __('手动') ?>
                                                </option>
                                                <option value="<?= Carrier::TYPE_API ?>" <?= ($carrier && $carrier->getData(Carrier::schema_fields_CARRIER_TYPE) === Carrier::TYPE_API) ? 'selected' : '' ?>>
                                                    <?= __('API') ?>
                                                </option>
                                            </select>
                                            <small class="w-field__hint w-text" data-tone="muted"><?= __('手动类型需要手动输入物流信息，API类型可通过接口自动获取') ?></small>
                                    </div>

                                    <div class="w-field" style="--w-mb:var(--weline-space-4)">
                                        <label class="w-field__label"><?= __('物流跟踪URL模板') ?> <span class="w-text" data-tone="danger">*</span></label>
                                        <input type="text" name="tracking_url_template" class="w-input" required value="<?= htmlspecialchars($carrier ? $carrier->getData(Carrier::schema_fields_TRACKING_URL_TEMPLATE) : '') ?>" placeholder="<?= __('例如：https://www.sf-express.com/track/{tracking_number}') ?>">
                                            <small class="w-field__hint w-text" data-tone="muted"><?= __('使用 {tracking_number} 作为物流单号占位符') ?></small>
                                    </div>

                                    <div class="w-field" id="api_config_section" style="display: <?= ($carrier && $carrier->getData(Carrier::schema_fields_CARRIER_TYPE) === Carrier::TYPE_API) ? 'block' : 'none' ?>; --w-mb:var(--weline-space-4);">
                                        <label class="w-field__label"><?= __('追踪API端点') ?></label>
                                        <input type="text" name="tracking_api_endpoint" class="w-input" value="<?= htmlspecialchars($carrier ? $carrier->getData(Carrier::schema_fields_TRACKING_API_ENDPOINT) : '') ?>" placeholder="<?= __('例如：https://api.example.com/track') ?>">
                                    </div>

                                    <div class="w-field" id="api_method_section" style="display: <?= ($carrier && $carrier->getData(Carrier::schema_fields_CARRIER_TYPE) === Carrier::TYPE_API) ? 'block' : 'none' ?>; --w-mb:var(--weline-space-4);">
                                        <label class="w-field__label"><?= __('API请求方法') ?></label>
                                        <select name="tracking_api_method" class="w-select">
                                            <option value="GET" <?= (!$carrier || $carrier->getData(Carrier::schema_fields_TRACKING_API_METHOD) === 'GET') ? 'selected' : '' ?>>GET</option>
                                            <option value="POST" <?= ($carrier && $carrier->getData(Carrier::schema_fields_TRACKING_API_METHOD) === 'POST') ? 'selected' : '' ?>>POST</option>
                                        </select>
                                    </div>

                                    <div class="w-field" id="api_config_json_section" style="display: <?= ($carrier && $carrier->getData(Carrier::schema_fields_CARRIER_TYPE) === Carrier::TYPE_API) ? 'block' : 'none' ?>; --w-mb:var(--weline-space-4);">
                                        <label class="w-field__label"><?= __('API配置') ?></label>
                                        <textarea name="api_config" class="w-textarea" rows="5" placeholder='<?= __('JSON格式，例如：{"api_key":"your_key","api_secret":"your_secret"}') ?>'><?= htmlspecialchars($carrier && $carrier->getData(Carrier::schema_fields_API_CONFIG) ? json_encode($carrier->getApiConfig(), JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE) : '') ?></textarea>
                                        <small class="w-field__hint w-text" data-tone="muted"><?= __('API配置信息，JSON格式') ?></small>
                                    </div>

                                    <div class="w-field" style="--w-mb:var(--weline-space-4)">
                                        <label class="w-field__label"><?= __('追踪支持状态') ?></label>
                                        <select name="tracking_support_status" class="w-select">
                                                <option value="<?= Carrier::TRACKING_SUPPORTED ?>" <?= (!$carrier || $carrier->getData(Carrier::schema_fields_TRACKING_SUPPORT_STATUS) === Carrier::TRACKING_SUPPORTED) ? 'selected' : '' ?>>
                                                    <?= __('支持') ?>
                                                </option>
                                                <option value="<?= Carrier::TRACKING_NOT_SUPPORTED ?>" <?= ($carrier && $carrier->getData(Carrier::schema_fields_TRACKING_SUPPORT_STATUS) === Carrier::TRACKING_NOT_SUPPORTED) ? 'selected' : '' ?>>
                                                    <?= __('不支持') ?>
                                                </option>
                                            </select>
                                    </div>

                                    <div class="w-field" style="--w-mb:var(--weline-space-4)">
                                        <label class="w-field__label"><?= __('排序') ?></label>
                                        <input type="number" name="sort_order" class="w-input" value="<?= htmlspecialchars((string)($carrier ? $carrier->getData(Carrier::schema_fields_SORT_ORDER) : 0)) ?>" placeholder="<?= __('数字越小越靠前，默认为0') ?>">
                                    </div>

                                    <div class="w-field" style="--w-mb:var(--weline-space-4)">
                                        <label class="w-field__label"><?= __('状态') ?></label>
                                        <div class="w-switch">
                                                <input type="checkbox" name="is_active" value="1" id="is_active" <?= (!$carrier || $carrier->getData(Carrier::schema_fields_IS_ACTIVE)) ? 'checked' : '' ?>>
                                                <label for="is_active">
                                                    <?= __('启用') ?>
                                                </label>
                                    </div>
                                    </div>

                                    <?php
                                    $coverageRows = $this->getData('coverage_rows') ?? [];
                                    $coverageJson = htmlspecialchars(json_encode($coverageRows, JSON_UNESCAPED_UNICODE) ?: '[]', ENT_QUOTES, 'UTF-8');
                                    $coverageLabels = $this->getData('coverage_labels') ?? [];
                                    $defaultsLabels = $this->getData('coverage_defaults_labels') ?? [];
                                    if (!is_array($coverageLabels)) {
                                        $coverageLabels = [];
                                    }
                                    if (!is_array($defaultsLabels)) {
                                        $defaultsLabels = [];
                                    }
                                    $coverageCount = \count($coverageLabels);
                                    $defaultsCount = \count($defaultsLabels);
                                    ?>
                                    <section
                                        class="shipping-carrier-coverage"
                                        data-testid="shipping-carrier-coverage-panel"
                                        aria-labelledby="shipping-carrier-coverage-title"
                                        style="--w-mb:var(--weline-space-4);padding:var(--weline-space-4);border:1px solid var(--weline-theme-border-subtle, var(--color-border, #e3e6e6));border-radius:var(--weline-radius-md, 8px);background:var(--weline-theme-surface-subtle, var(--color-surface-subtle, #f7f8f8));"
                                    >
                                        <div class="w-cluster" data-align="center" data-justify="between" style="--w-mb:var(--weline-space-3);">
                                            <h6 id="shipping-carrier-coverage-title" class="w-field__label" style="--w-mb:0;font-size:var(--weline-font-size-md, 1rem);">
                                                <?= __('支持范围') ?> <span class="w-text" data-tone="danger">*</span>
                                            </h6>
                                            <span class="w-badge" data-tone="neutral" data-testid="shipping-carrier-coverage-count">
                                                <?= __('已保存') ?> <?= (int)$coverageCount ?> · <?= __('Provider 默认') ?> <?= (int)$defaultsCount ?>
                                            </span>
                                        </div>

                                        <div class="w-alert" role="alert" data-tone="warning" data-testid="shipping-carrier-coverage-caution" style="--w-mb:var(--weline-space-3);">
                                            <w:icon name="warning" size="sm"></w:icon>
                                            <div class="w-alert__content">
                                                <strong class="w-alert__title"><?= __('谨慎操作') ?></strong>
                                                <span><?= __('缩减可运地区后，结账将不再提供该承运商。保存前请确认与真实可运范围一致。') ?></span>
                                            </div>
                                        </div>

                                        <div class="w-cluster" data-align="center" data-justify="between" style="--w-mb:var(--weline-space-3);flex-wrap:wrap;gap:var(--weline-space-2);">
                                            <label class="w-cluster" data-align="center" style="gap:var(--weline-space-2);">
                                                <input type="checkbox" name="coverage_keep_current" value="1" id="coverage_keep_current" checked>
                                                <span><?= __('保存当前选择（推荐）') ?></span>
                                            </label>
                                            <span class="w-text" data-tone="muted" style="font-size:var(--weline-font-size-sm, 0.875rem);">
                                                <?= __('取消勾选 = 保存时改用 Provider 默认区域') ?>
                                            </span>
                                        </div>

                                        <p class="w-text" data-tone="muted" style="--w-mb:var(--weline-space-3);font-size:var(--weline-font-size-sm, 0.875rem);">
                                            <?= __('在下方选择可运国家或更细地区。费用模板只算运费金额，不含到达地区。') ?>
                                        </p>

                                        <input type="hidden" name="coverage_json" id="carrier_coverage_json" value="<?= $coverageJson ?>" data-coverage-baseline="<?= $coverageJson ?>">

                                        <div class="shipping-carrier-coverage__picker" style="--w-mb:var(--weline-space-3);padding:var(--weline-space-3);border-radius:var(--weline-radius-sm, 6px);background:var(--weline-theme-surface, var(--color-surface, #fff));border:1px solid var(--weline-theme-border-subtle, var(--color-border, #e3e6e6));">
                                            <w:theme:address
                                                selection="multi"
                                                multi-levels="country|province|district"
                                                levels="country|province|district"
                                                code="carrier-coverage"
                                                catalog="global"
                                                searchable="true"
                                                name="carrier_coverage_picker"
                                            />
                                        </div>

                                        <details class="shipping-carrier-coverage__help" style="--w-mb:var(--weline-space-2);">
                                            <summary class="w-text" data-tone="muted" style="cursor:pointer;font-size:var(--weline-font-size-sm, 0.875rem);">
                                                <?= __('说明与参考列表') ?>
                                            </summary>
                                            <div style="--w-mt:var(--weline-space-2);display:grid;gap:var(--weline-space-3);">
                                                <div class="w-alert" role="status" data-tone="info" data-testid="shipping-carrier-system-embargo-note" style="--w-mb:0;">
                                                    <w:icon name="info" size="sm"></w:icon>
                                                    <div class="w-alert__content">
                                                        <strong class="w-alert__title"><?= __('系统禁运 · 不可选') ?></strong>
                                                        <span><?= __('地址选择器中标为「不支持」的系统禁运区优先级最高，写入覆盖也不会在结账放行。') ?></span>
                                                    </div>
                                                </div>
                                                <?php if ($defaultsLabels !== []): ?>
                                                    <div data-testid="shipping-carrier-coverage-defaults">
                                                        <div class="w-text" data-tone="muted" style="--w-mb:var(--weline-space-2);font-size:var(--weline-font-size-sm, 0.875rem);"><?= __('Provider 默认区域') ?></div>
                                                        <div class="w-cluster" style="flex-wrap:wrap;gap:var(--weline-space-1);">
                                                            <?php foreach ($defaultsLabels as $label): ?>
                                                                <span class="w-badge" data-tone="neutral"><?= htmlspecialchars((string)$label, ENT_QUOTES, 'UTF-8') ?></span>
                                                            <?php endforeach; ?>
                                                        </div>
                                                    </div>
                                                <?php endif; ?>
                                                <?php if ($coverageLabels !== []): ?>
                                                    <div data-testid="shipping-carrier-coverage-current">
                                                        <div class="w-text" data-tone="muted" style="--w-mb:var(--weline-space-2);font-size:var(--weline-font-size-sm, 0.875rem);"><?= __('当前已保存') ?></div>
                                                        <div class="w-cluster" style="flex-wrap:wrap;gap:var(--weline-space-1);">
                                                            <?php foreach ($coverageLabels as $label): ?>
                                                                <span class="w-badge" data-tone="primary"><?= htmlspecialchars((string)$label, ENT_QUOTES, 'UTF-8') ?></span>
                                                            <?php endforeach; ?>
                                                        </div>
                                                    </div>
                                                <?php endif; ?>
                                            </div>
                                        </details>

                                        <script src="/Weline/Theme/view/statics/js/address.js?v=20260906-catalog-empty1" data-shipping-address-direct data-no-extract="true" onload="window.__welineShippingAddressReady&&window.__welineShippingAddressReady()"></script>
                                        <script>
                                        (function () {
                                            var hidden = document.getElementById('carrier_coverage_json');
                                            if (!hidden) return;
                                            var panel = hidden.closest('.shipping-carrier-coverage') || hidden.parentElement;
                                            var root = panel
                                                ? panel.querySelector('[data-w-address], [data-address-code="carrier-coverage"]')
                                                : null;
                                            if (!root) {
                                                root = document.querySelector('[data-address-code="carrier-coverage"]');
                                            }
                                            if (!root) return;
                                            function sync() {
                                                var raw = root.getAttribute('data-multi-selection') || '[]';
                                                try { JSON.parse(raw); hidden.value = raw; } catch (e) {}
                                            }
                                            root.addEventListener('weline:address:multi-change', sync);
                                            try {
                                                root.setAttribute('data-multi-seed', hidden.value || '[]');
                                                root.setAttribute('data-multi-defaults', <?= json_encode($this->getData('coverage_defaults_json') ?: '[]') ?>);
                                            } catch (e) {}
                                            function boot() {
                                                if (!window.WelineThemeAddress || typeof window.WelineThemeAddress.boot !== 'function') {
                                                    return false;
                                                }
                                                root.dataset.wAddressReady = '';
                                                window.WelineThemeAddress.boot();
                                                sync();
                                                return true;
                                            }
                                            window.__welineShippingAddressReady = function () {
                                                if (!boot()) {
                                                    setTimeout(boot, 50);
                                                }
                                            };
                                            if (window.WelineThemeAddress) {
                                                window.__welineShippingAddressReady();
                                            }
                                            var form = hidden.closest('form');
                                            if (form && !form.dataset.coverageConfirmBound) {
                                                form.dataset.coverageConfirmBound = '1';
                                                form.addEventListener('submit', function (ev) {
                                                    sync();
                                                    var keep = document.getElementById('coverage_keep_current');
                                                    var applyingDefaults = !(keep && keep.checked);
                                                    var baseline = hidden.getAttribute('data-coverage-baseline') || '[]';
                                                    var current = hidden.value || '[]';
                                                    var changed = applyingDefaults || baseline !== current;
                                                    if (!changed) {
                                                        return;
                                                    }
                                                    var msg = <?= json_encode(
                                                        (string)__('即将保存承运商支持范围。缩减地区会导致对应目的地无法使用该承运商配送，请确认仍与真实可运范围一致。是否继续保存？'),
                                                        JSON_UNESCAPED_UNICODE
                                                    ) ?>;
                                                    if (!window.confirm(msg)) {
                                                        ev.preventDefault();
                                                        ev.stopPropagation();
                                                    }
                                                });
                                            }
                                        })();
                                        </script>
                                    </section>

                                    <div class="w-grid">
                                        <div class="offset-md-2" style="--w-span-md:10;">
                                            <button type="submit" class="w-button" data-testid="shipping-carrier-editor-submit" data-tone="primary">
                                                <w:icon name="save" size="sm"></w:icon> <?= __('保存') ?>
                                            </button>
                                            <a href="@backend-url{'shipping/backend/carrier'}" class="w-button" data-tone="neutral">
                                                <w:icon name="arrow-left" size="sm"></w:icon> <?= __('返回') ?>
                                            </a>
                                        </div>
                                    </div>
                                </w:form>
                            </div>
                        </div>
                    </div>
                </div>
<?php // tpl-bust-cov5 ?>
