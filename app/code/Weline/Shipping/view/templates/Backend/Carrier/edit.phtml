<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */


use Weline\Shipping\Model\Carrier;

$carrier = $this->getData('carrier');
$isEdit = !empty($carrier) && $carrier->getId();
$id = $carrier ? $carrier->getId() : null;

$backendBase = $this->getBackendUrl('shipping/backend/carrier');
$saveUrl = $this->getBackendUrl('shipping/backend/carrier/save');
?>
<div class="row">
                    <div class="col-12">
                        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                            <h4 class="mb-sm-0"><?= $isEdit ? __('编辑快递公司') : __('新增快递公司') ?></h4>
                            <div class="page-title-right">
                                <ol class="breadcrumb m-0">
                                    <li class="breadcrumb-item"><a href="@backend-url('admin/index/index')"><?= __('系统') ?></a></li>
                                    <li class="breadcrumb-item"><a href="<?= $backendBase ?>"><?= __('快递公司') ?></a></li>
                                    <li class="breadcrumb-item active"><?= $isEdit ? __('编辑') : __('新增') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title mb-0"><?= $isEdit ? __('编辑快递公司') : __('新增快递公司') ?></h5>
                            </div>
                            <div class="card-body">
                                <form method="post" action="<?= $saveUrl ?>">
                                    <?php if ($id): ?>
                                        <input type="hidden" name="id" value="<?= $id ?>">
                                    <?php endif; ?>
                                    
                                    <div class="row mb-3">
                                        <label class="col-md-2 col-form-label"><?= __('快递公司代码') ?> <span class="text-danger">*</span></label>
                                        <div class="col-md-10">
                                            <input type="text" name="carrier_code" class="form-control" required
                                                   value="<?= htmlspecialchars($carrier ? $carrier->getData(Carrier::fields_CARRIER_CODE) : '') ?>"
                                                   placeholder="<?= __('请输入快递公司代码，如：SF、ZTO等') ?>"
                                                   <?= $isEdit ? 'readonly' : '' ?>>
                                            <small class="form-text text-muted"><?= __('代码必须唯一，编辑后不可修改') ?></small>
                                        </div>
                                    </div>

                                    <div class="row mb-3">
                                        <label class="col-md-2 col-form-label"><?= __('快递公司名称') ?> <span class="text-danger">*</span></label>
                                        <div class="col-md-10">
                                            <input type="text" name="carrier_name" class="form-control" required
                                                   value="<?= htmlspecialchars($carrier ? $carrier->getData(Carrier::fields_CARRIER_NAME) : '') ?>"
                                                   placeholder="<?= __('请输入快递公司名称，如：顺丰速运') ?>">
                                        </div>
                                    </div>

                                    <div class="row mb-3">
                                        <label class="col-md-2 col-form-label"><?= __('类型') ?> <span class="text-danger">*</span></label>
                                        <div class="col-md-10">
                                            <select name="carrier_type" class="form-select" id="carrier_type" required>
                                                <option value="<?= Carrier::TYPE_MANUAL ?>" <?= (!$carrier || $carrier->getData(Carrier::fields_CARRIER_TYPE) === Carrier::TYPE_MANUAL) ? 'selected' : '' ?>>
                                                    <?= __('手动') ?>
                                                </option>
                                                <option value="<?= Carrier::TYPE_API ?>" <?= ($carrier && $carrier->getData(Carrier::fields_CARRIER_TYPE) === Carrier::TYPE_API) ? 'selected' : '' ?>>
                                                    <?= __('API') ?>
                                                </option>
                                            </select>
                                            <small class="form-text text-muted"><?= __('手动类型需要手动输入物流信息，API类型可通过接口自动获取') ?></small>
                                        </div>
                                    </div>

                                    <div class="row mb-3">
                                        <label class="col-md-2 col-form-label"><?= __('物流跟踪URL模板') ?> <span class="text-danger">*</span></label>
                                        <div class="col-md-10">
                                            <input type="text" name="tracking_url_template" class="form-control" required
                                                   value="<?= htmlspecialchars($carrier ? $carrier->getData(Carrier::fields_TRACKING_URL_TEMPLATE) : '') ?>"
                                                   placeholder="<?= __('例如：https://www.sf-express.com/track/{tracking_number}') ?>">
                                            <small class="form-text text-muted"><?= __('使用 {tracking_number} 作为物流单号占位符') ?></small>
                                        </div>
                                    </div>

                                    <div class="row mb-3" id="api_config_section" style="display: <?= ($carrier && $carrier->getData(Carrier::fields_CARRIER_TYPE) === Carrier::TYPE_API) ? 'block' : 'none' ?>;">
                                        <label class="col-md-2 col-form-label"><?= __('追踪API端点') ?></label>
                                        <div class="col-md-10">
                                            <input type="text" name="tracking_api_endpoint" class="form-control"
                                                   value="<?= htmlspecialchars($carrier ? $carrier->getData(Carrier::fields_TRACKING_API_ENDPOINT) : '') ?>"
                                                   placeholder="<?= __('例如：https://api.example.com/track') ?>">
                                        </div>
                                    </div>

                                    <div class="row mb-3" id="api_method_section" style="display: <?= ($carrier && $carrier->getData(Carrier::fields_CARRIER_TYPE) === Carrier::TYPE_API) ? 'block' : 'none' ?>;">
                                        <label class="col-md-2 col-form-label"><?= __('API请求方法') ?></label>
                                        <div class="col-md-10">
                                            <select name="tracking_api_method" class="form-select">
                                                <option value="GET" <?= (!$carrier || $carrier->getData(Carrier::fields_TRACKING_API_METHOD) === 'GET') ? 'selected' : '' ?>>GET</option>
                                                <option value="POST" <?= ($carrier && $carrier->getData(Carrier::fields_TRACKING_API_METHOD) === 'POST') ? 'selected' : '' ?>>POST</option>
                                            </select>
                                        </div>
                                    </div>

                                    <div class="row mb-3" id="api_config_json_section" style="display: <?= ($carrier && $carrier->getData(Carrier::fields_CARRIER_TYPE) === Carrier::TYPE_API) ? 'block' : 'none' ?>;">
                                        <label class="col-md-2 col-form-label"><?= __('API配置') ?></label>
                                        <div class="col-md-10">
                                            <textarea name="api_config" class="form-control" rows="5"
                                                      placeholder='<?= __('JSON格式，例如：{"api_key":"your_key","api_secret":"your_secret"}') ?>'><?= htmlspecialchars($carrier && $carrier->getData(Carrier::fields_API_CONFIG) ? json_encode($carrier->getApiConfig(), JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE) : '') ?></textarea>
                                            <small class="form-text text-muted"><?= __('API配置信息，JSON格式') ?></small>
                                        </div>
                                    </div>

                                    <div class="row mb-3">
                                        <label class="col-md-2 col-form-label"><?= __('追踪支持状态') ?></label>
                                        <div class="col-md-10">
                                            <select name="tracking_support_status" class="form-select">
                                                <option value="<?= Carrier::TRACKING_SUPPORTED ?>" <?= (!$carrier || $carrier->getData(Carrier::fields_TRACKING_SUPPORT_STATUS) === Carrier::TRACKING_SUPPORTED) ? 'selected' : '' ?>>
                                                    <?= __('支持') ?>
                                                </option>
                                                <option value="<?= Carrier::TRACKING_NOT_SUPPORTED ?>" <?= ($carrier && $carrier->getData(Carrier::fields_TRACKING_SUPPORT_STATUS) === Carrier::TRACKING_NOT_SUPPORTED) ? 'selected' : '' ?>>
                                                    <?= __('不支持') ?>
                                                </option>
                                            </select>
                                        </div>
                                    </div>

                                    <div class="row mb-3">
                                        <label class="col-md-2 col-form-label"><?= __('排序') ?></label>
                                        <div class="col-md-10">
                                            <input type="number" name="sort_order" class="form-control"
                                                   value="<?= htmlspecialchars((string)($carrier ? $carrier->getData(Carrier::fields_SORT_ORDER) : 0)) ?>"
                                                   placeholder="<?= __('数字越小越靠前，默认为0') ?>">
                                        </div>
                                    </div>

                                    <div class="row mb-3">
                                        <label class="col-md-2 col-form-label"><?= __('状态') ?></label>
                                        <div class="col-md-10">
                                            <div class="form-check form-switch">
                                                <input class="form-check-input" type="checkbox" name="is_active" value="1" id="is_active"
                                                       <?= (!$carrier || $carrier->getData(Carrier::fields_IS_ACTIVE)) ? 'checked' : '' ?>>
                                                <label class="form-check-label" for="is_active">
                                                    <?= __('启用') ?>
                                                </label>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row">
                                        <div class="col-md-10 offset-md-2">
                                            <button type="submit" class="btn btn-primary">
                                                <i class="mdi mdi-content-save"></i> <?= __('保存') ?>
                                            </button>
                                            <a href="<?= $backendBase ?>" class="btn btn-secondary">
                                                <i class="mdi mdi-arrow-left"></i> <?= __('返回') ?>
                                            </a>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
