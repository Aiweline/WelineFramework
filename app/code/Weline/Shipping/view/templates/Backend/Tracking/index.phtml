<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */


use Weline\Shipping\Model\Tracking;

$records = $this->getData('records') ?? [];
$embed = $this->getData('embed') ?? false;
?>
<?php if (empty($embed)): ?>
<div class="w-grid" data-testid="shipping-tracking-management">
                    <div style="--w-span:12;">
                        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
                            <h4 style="--w-mb-sm:0;"><?= __('物流跟踪') ?></h4>
                            <div class="w-cluster">
                                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                                    <li class="w-breadcrumb__item">
                                        <a href="@backend-url('admin/index/index')"><?= __('系统') ?></a>
                                    </li>
                                    <li class="w-breadcrumb__item" data-state="active"><?= __('物流跟踪') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>
<?php endif; ?>

                <div class="w-grid">
                    <div style="--w-span:12;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <h5 class="w-card__title" style="--w-mb:0;"><?= __('物流跟踪记录列表') ?></h5>
                            </div>
                            <div class="w-card__body">
                                <div class="w-alert" data-testid="shipping-tracking-read-only" data-tone="info"><?= __('物流跟踪记录由发货与承运商查询流程生成，此工作台仅用于审计查看。') ?></div>
                                <?php if (empty($records)): ?>
                                    <div class="w-alert" data-tone="info" style="--w-mb:0;">
                                        <?= __('当前暂无物流跟踪记录。') ?>
                                    </div>
                                <?php else: ?>
                                    <div class="w-table-wrap">
                                        <table class="w-table" data-w-vertical="middle">
                                            <thead>
                                            <tr>
                                                <th><?= __('物流单号') ?></th>
                                                <th><?= __('快递公司ID') ?></th>
                                                <th><?= __('状态') ?></th>
                                                <th><?= __('当前位置') ?></th>
                                                <th><?= __('最后查询时间') ?></th>
                                            </tr>
                                            </thead>
                                            <tbody>
                                            <?php /** @var Tracking $record */ ?>
                                            <?php foreach ($records as $record): ?>
                                                <tr>
                                                    <td><?= htmlspecialchars($record->getData(Tracking::schema_fields_TRACKING_NUMBER)) ?></td>
                                                    <td><?= htmlspecialchars($record->getData(Tracking::schema_fields_CARRIER_ID)) ?></td>
                                                    <td><?= htmlspecialchars($record->getStatusLabel()) ?></td>
                                                    <td><?= htmlspecialchars((string)$record->getData(Tracking::schema_fields_CURRENT_LOCATION)) ?></td>
                                                    <td><?= htmlspecialchars((string)$record->getData(Tracking::schema_fields_LAST_TRACKED_AT)) ?></td>
                                                </tr>
                                            <?php endforeach; ?>
                                            </tbody>
                                        </table>
                                    </div>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
