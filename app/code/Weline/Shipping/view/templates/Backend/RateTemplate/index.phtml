<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */


use Weline\Shipping\Model\RateTemplate;

$templates = $this->getData('templates') ?? [];
$embed = $this->getData('embed') ?? false;
?>
<?php if (empty($embed)): ?>
<div class="row">
                    <div class="col-12">
                        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                            <h4 class="mb-sm-0"><?= __('费用模板') ?></h4>
                            <div class="page-title-right">
                                <ol class="breadcrumb m-0">
                                    <li class="breadcrumb-item">
                                        <a href="@backend-url('admin/index/index')"><?= __('系统') ?></a>
                                    </li>
                                    <li class="breadcrumb-item active"><?= __('费用模板') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>
<?php endif; ?>

                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title mb-0"><?= __('费用模板列表') ?></h5>
                            </div>
                            <div class="card-body">
                                <?php if (empty($templates)): ?>
                                    <div class="alert alert-info mb-0">
                                        <?= __('当前暂无费用模板配置，请后续在此页面完善配置。') ?>
                                    </div>
                                <?php else: ?>
                                    <div class="table-responsive">
                                        <table class="table table-bordered table-hover align-middle">
                                            <thead class="table-light">
                                            <tr>
                                                <th><?= __('模板名称') ?></th>
                                                <th><?= __('模板代码') ?></th>
                                                <th><?= __('计算类型') ?></th>
                                                <th><?= __('状态') ?></th>
                                            </tr>
                                            </thead>
                                            <tbody>
                                            <?php /** @var RateTemplate $template */ ?>
                                            <?php foreach ($templates as $template): ?>
                                                <tr>
                                                    <td><?= htmlspecialchars($template->getData(RateTemplate::fields_TEMPLATE_NAME)) ?></td>
                                                    <td><?= htmlspecialchars($template->getData(RateTemplate::fields_TEMPLATE_CODE)) ?></td>
                                                    <td><?= htmlspecialchars($template->getData(RateTemplate::fields_CALCULATION_TYPE)) ?></td>
                                                    <td>
                                                        <?php if ($template->getData(RateTemplate::fields_IS_ACTIVE)): ?>
                                                            <span class="badge bg-success"><?= __('启用') ?></span>
                                                        <?php else: ?>
                                                            <span class="badge bg-secondary"><?= __('禁用') ?></span>
                                                        <?php endif; ?>
                                                    </td>
                                                </tr>
                                            <?php endforeach; ?>
                                            </tbody>
                                        </table>
                                    </div>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
