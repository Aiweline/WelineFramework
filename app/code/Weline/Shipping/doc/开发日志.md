# Weline_Shipping 开发日志

## 2026-09-23 — 退换政策语气柔化（实质不变）（2.9.29）

- 用户反馈规则生硬：政策实质不变（发货后个人原因难退；质量/错发/运输损坏可退；批发正常损耗不退；不做七日无理由店内福利；法定例外弱披露）。
- `guide/returns`（模块 + hanfu）：总述/1.1/1.4/不予退换标题/4.x/尺码等改为安抚+解释原因的柔和表述。
- 契约：`GuideEnglishTranslationTest` 对齐新 1.1 中英。
- 版本：`2.9.28` → `2.9.29`。

## 2026-09-23 — 跨境退换：发货后原则上不退 + 批发损耗（2.9.28）

- 权威：`ecommerce-advisor-ops-planner/channel/crossborder-returns-wholesale-ops-brief.md`。
- `guide/returns`（模块 + hanfu）：1.1–1.4 重写为发货后原则上不退；质量/错发/运输损坏可退；禁七日/十四日店内无理由；新增批发特别说明与交叉链；不予退换与 4.x/尺码软表述对齐。
- 契约：`GuideEnglishTranslationTest` 对齐新 1.1 中英。
- 版本：`2.9.27` → `2.9.28`。


## 2026-09-22 — 商家友好退换口径（质量为主）（2.9.27）

- 经营事实：国内发海外汉服，做不到七日无理由主推；个人试穿后不想要压力大。
- 模板：`guide/returns.phtml`（模块 + hanfu design）1.1 改为质量/错发可退，个人原因原则上不支持；1.4 法定例外弱披露；不予退换列表与 4.2 同步加硬。
- i18n：Shipping 中英 CSV 新旧键；契约 `GuideEnglishTranslationTest` 不再要求 statutory seven/fourteen 为主文案。
- 版本：`2.9.26` → `2.9.27`。

## 2026-09-22 — 订单配送方式 catalog 译名（2.9.26）

- `OrderShippingMethodCatalog` 对 `service_name`（如「美洲」）套 `__()`，供成功页/后台订单 chrome 随活跃 locale 展示。
- 版本：`2.9.25` → `2.9.26`。

## 2026-09-22 — 退换指南 1.1 合规：分域冷静期（2.9.25）

- 根因：`/guide/returns` 1.1 仍绝对宣称「签收后 7 日可退」，英文露 `within 7 days of delivery`。
- 修复：源串改为大陆法定七日 + 欧盟/EEA 十四日等强制冷静期 + 试穿/吊牌/二次销售 + 跨境尽早申请；保留 1.4。
- i18n：Shipping 中英 CSV（新旧 1.1 键 + 短句孤儿语义映射）；词典包 `dict-fill-shipping-returns-11-compliance.v1`（含 Theme trust-badges 新旧键）+ remediate `--apply` + publishLocale。
- 契约：`GuideEnglishTranslationTest` 对齐新语义。
- 版本：`2.9.24` → `2.9.25`。

## 2026-09-22 — 首页包邮门槛对齐 $49（WO-HP-P2-07）（2.9.24）

- 默认种子改启用 `SEED_FREE_49`、停用 `SEED_FREE_99`；`alignHomepageWave2Threshold49()` 对 website/0 存量翻转。
- 航线种子 `ensureFreeRule` 锚定 `SEED_FREE_49`。
- 契约：`FreeShippingRuleSeedContractTest` 断言默认激活档为 49。
- 版本：`2.9.23` → `2.9.24`。

## 2026-09-22 — 指南 TOC 锚点兼容 storefront base（2.9.23）

- 根因：前台 `<base href="/">` 使裸 `#id` 跳到首页。
- 修复：目录链接改为「当前 path#id」；去掉 panel `overflow:hidden`；加大 `scroll-margin-top`。
- 版本：`2.9.22` → `2.9.23`。

## 2026-09-21 — 国内仓发海外：配送/退换指南时效重写（2.9.22）

- 配送指南：中国大陆仓→海外；发货 1–5 工作日、国际 7–25 参考日；干线空窗≠丢件；清关延误不自动退款。
- 退换指南：寄回中国仓库；个人原因买家承担国际回程与税费；强制法外以本站约定为准。
- 协同 Theme `2.2.529` 配送/退款政策。
- 版本：`2.9.21` → `2.9.22`。

## 2026-09-21 — 配送/退换指南：大陆仓发海外商家友好口径（2.9.22）

- 配送说明：1–5 工作日发货；国际参考 7–25 工作日；干线空窗≠丢件；税费到付；清关延误不自动退款。
- 退换政策：跨境寄回中国仓；个人原因回程国际运费+税费买家承担；强制法以外以本站约定为准。
- i18n：Shipping 中英 CSV + 协同 `dict-fill-china-export-policy.v1`。
- 版本：`2.9.21` → `2.9.22`。

## 2026-09-21 — 字母邮编外区前缀回退（2.9.21）

- 问题：`SW1A 1AA` 等完整英/加/爱邮在 GeoNames 前缀目录下精确 miss。
- 修：`TsvGzReader::postalLookupNorms`——精确优先；带字母按 GB/CA/IE 格式回退；纯数字不回退。
- `postalCountries` / `postalLookup` 对候选 `IN` 查询，优先更完整匹配。
- 契约：`PostalCountriesEmbargoContractTest` 覆盖候选生成与调用链。
- 版本：`2.9.20` → `2.9.21`。

## 2026-09-21 — 续付「使用新地址」防强制收起（2.9.20）

- 现象：续付点「Use a new address」表单闪开后被 `presentOrderAddress` / sync 收回。
- 修：`data-user-address-editing` 粘性标记；`presentOrderAddress` 在 edit/picking/编辑态跳过收起；SSR `mode=new` 仍可首次收起；导出 `getMode`；bust `20260921-cpay-addr3`。
- 协同 Checkout `syncContinuePayAddressPanel` 同规则。
- 版本：`2.9.19` → `2.9.20`。

## 2026-09-21 — 配送/退换指南合规对齐（2.9.19）

- 配送范围口径与政策页一致（结算页可选 + 大陆覆盖说明）。
- 签收：运输破损 48h 查验窗 vs 质量问题走退换政策拆分；驿站签收表述为承运商节点。
- 补跨境税费指向配送政策；退换页标明为退换条件权威源。
- i18n：中英 CSV + 默认站词典 pack 同步。
- 版本：`2.9.18` → `2.9.19`。

## 2026-09-21 — 续付 presentOrderAddress（齐全收起）（2.9.18）

- JS API：`presentOrderAddress(address, {expand})`——订单地址齐全 → 本地卡 + `collapsed`；不全才展开编辑器。
- bust `20260921-cpay-addr1`（modules + css + mount rev）。
- 协同 Checkout `1.5.16` 续付 chrome/地址面板。
- 版本：`2.9.17` → `2.9.18`。

## 2026-09-21 — 结账地址 presence marker + 清 JS 快照（2.9.17）

- `data-testid` / `data-widget-code` 对齐 `checkout-shipping-address`；保留 `data-shipping-checkout-address` DOM 钩子。
- 删除未引用的 `checkout-shipping-address.v20*.js` 快照；契约/e2e 指向现行 `.js`。
- 版本：`2.9.16` → `2.9.17`。

## 2026-09-17 — 点开运输区块才加载 address.js（2.9.14）

- 禁运/目的地 partial：展开 disclosure 才注入 `address.js`（`ensureAddressScript`）；首屏不再直链脚本。
- bust `20260917-open-load1`。

## 2026-09-17 — 网站表单运输地址懒挂载（2.9.13）

- 联动 Theme：禁运/目的地 disclosure 展开前不 boot、不拉 `region.*`。
- partial：`data-address-lazy` + `bootWhenOpen`。

## 2026-09-17 — 恢复 region.embargo_regions 声明（2.9.12）

- 现象：网站管理页 BinQuery `region.embargo_regions` → 403 `capability_denied`（Frontend worker operation is not allowed）。
- 根因：改 TTL 时误删 descriptor 里的 `embargo_regions`，并污染 `embargo_countries` 字段。
- 修：恢复两国/子级禁运 operation；契约断言；`php bin/w framework:compile`。

## 2026-09-17 — embargo_regions 缓存 TTL + 前端去 fan-out（2.9.11）

- 联动 Theme 2.2.427：地址多选不再按国并发打 `embargo_regions`。
- Provider `embargo_countries` / `embargo_regions` cache_ttl 15→300；partial script bust 对齐。

## 2026-09-17 — Region JSON 头写入 Framework Response（2.9.10）

- 现象：后台地址选择器狂打 `query-bin` `region.embargo_*`（约 13s/次）。
- 根因：`Region::json()` 用原生 `header()`，WLS 下 Content-Type 仍是 `text/html`，CSP meta 插进 JSON，`fetch().json()` 失败后无缓存回退 BinQuery。
- 修：写 `request->getResponse()` 的 `application/json` + `Cache-Control: private, no-store`；契约测。
- 联动：Theme `address.js` 禁运 promise 缓存 + 容错解析；Framework CSP 跳过 JSON。

## 2026-09-16 — listQuoteOptions 失败带回 quote_diagnostics（2.9.9）

- 结账美国空配送含糊：失败响应 `data=[]` 吞掉 `missing_weight` 等 diagnostics。
- 修：`ShippingInfoQueryProvider::quoteOps` catch 写入 `getLastQuoteDiagnostics()`。
- 契约：`ShippingQuoteFailureDiagnosticsContractTest`。

## 2026-09-16 — 收货「同时用作结账」默认关 + ensureCheckoutPurpose（2.9.8）

- 二次纠偏：账户/Header quick-add 默认不勾选；账户 JS 新建不强制勾选。
- `ensureCheckoutPurposeForAddressId` 供下单成功转化。
- bust `20260916-purpose-tags2`。

## 2026-09-16 — 中国发运全球可达市场种子（2.9.7）

- 现象：结账美国地址「暂无可用配送」；用户要中国→世界各地实际邮费种子。
- 澄清：美国航线本有 `SEED_LANE_AMERICAS`（2kg=¥115）；旧种子仅 ~55 国，且目的地/承运商覆盖在已有 >5 国时不再扩容。
- 改：`default-markets/countries.tsv` 扩至 243 国（排除 AQ/BV/GS/HM/TF/UM/KP）；九档费率按中国发运经济小包校准（Americas 仍锁定 45/14）；目的地与承运商覆盖改为增量并入缺失国。
- 验收：`ch1_americas_2kg` PASS；US/DE/JP/BR/AU/AE/RU/HK 均有对应航线价；`WorldMarketSeedContractTest`。

## 2026-09-16 — stripCheckoutPurposeForCustomer（2.9.6）

- 配合 Checkout `clearDeliveryAddresses`：账户库可摘 `purpose_checkout`（演示清空结账簿），保留 `purpose_receiving`。
- 版本：`2.9.5` → `2.9.6`。

## 2026-09-16 — 结账/收货用途标签隔离（purpose_checkout / purpose_receiving）

- 澄清：运输=收货地址（非发货）；双标勾选默认开；无取消标签 UI；文案「收货地址/结账地址」。
- `DeliveryAddress` 增用途字段；Upgrade 存量双标回填。
- 结账列表 `address_purpose=checkout`；账户/Header `purpose_receiving`。
- 结账「同时用作收货地址」、账户/Header「同时用作结账地址」；更新只加标不摘标。
- bust `20260916-purpose-tags1`。

## 2026-09-15 — 结账地址人机：绑定 + 提交前取票 + Google 降级本地（2.x）

- 审图：`web_ui` / `error_shot`；「验证码校验失败」且无可见人机控件。
- 根因：编辑器缺 `id=checkout-shipping-address-editor`；保存按钮不派发 `prepare-submit`；lazy 注入脚本未执行。
- 修：编辑器补 id + `LazyCaptchaClientRuntime`；`ensureCaptchaTokenBeforeSave`；degrade→`prefer=local_image`；Checkout `getDeliveryCaptchaChallenge` 支持 prefer。
- bust `20260915-captcha-bind1`。

## 2026-09-16 — 电话全球兼容（E.164）

- 前后端统一：不绑 CN/US 本地死格式；数字 7–15 位（E.164）；允许 `+`、空格、`-`、`()`、`.`、`/`。
- 仍禁止字母；库字段 `contact_phone` varchar 20→32。
- bust `20260916-phone-intl1`。

## 2026-09-16 — 结账电话禁止字母输入

- 审图：电话框可输入 `dsdadaa`（ui_shot / 人性化 fail）。
- 收货/账单 `phone`：输入时剥离非 `0-9+ -()`；`beforeinput` 拦截非法字符；保存校验对齐 `AddressSchemaProvider::phone_pattern`（6–32）。
- bust `20260916-phone-guard1`。

## 2026-09-15 — 账单地址簿平行逻辑（选/换/编/新增）

- 澄清：与收货「一样」指交互逻辑平行，不是合并成一个功能。
- 账单独立：`data-billing-*` 模式（same/collapsed/picking/edit/new）；只读 `getDeliveryContext` 拉地址簿；**禁止** `selectDeliveryAddress`。
- UI：更换地址 / 使用新地址 / 编辑 / 取消 / 保存此地址；CSS 用 `data-billing-mode` 收起列表。
- bust `20260915-billing-book1`。

## 2026-09-15 — 账单地址补「保存此地址」收起

- 审图：`web_ui` / `ui_shot`（红箭头指账单表单底空档，缺收货同款保存收起）。
- 线稿：账单编辑器底右主 CTA「保存此地址」→ 收起为账单卡；卡上「编辑」再开表单。
- 修：`data-use-billing-address` + `data-billing-saved` 卡壳；本单本地收起（不调 saveDeliveryAddress）；契约 / bust `20260915-billing-save1`。

## 2026-09-15 — 结账账单地址对齐 theme:address 邮编反查

- 审图：`web_ui` / `ui_shot`（账单区纯文本国省市区，与收货组件不一致）。
- 线稿：账单标题 → 同收货勾选 → 收货人/电话 → 邮箱 → **邮编** → theme:address 级联 → 详细地址。
- 原型：与收货同序；取消勾选后填邮编自动选国省市区，仍可手改级联。
- 修：账单区改 `<w:theme:address code=checkout-billing-address meta-prefix=billing_ postal-lookup>`；部件 JS `applyBillingCascade`；契约断言。
- 协同：Theme `address.js` meta-prefix + 邮编作用域（见 Theme 日志）。

## 2026-09-15 — 结账航线名与 duty_notice 提示启用语种译文

- Checkout 对 `label` 走 `__()`；提示源串来自 `labelForDutyNoticeCode`。
- 种子航线名与三档 Incoterm 提示 upsert 默认站启用语种并 `publishLocale`；未走 Ollama。

## 2026-09-15 — duty_notice 店面显示可读名

- `ShippingIncotermService::labelForDutyNoticeCode`：机器码 → 中文源串标签；payload 仍保留 code。
- 结账配送副文禁止露出 `duties_taxes_not_included_in_shipping` 等 snake_case。

## 2026-09-15 — 轨迹链默认 17TRACK，禁止 example 占位

- `TrackingUrlResolver`：承运商回传可用 URL 优先；否则 `www.17track.net`；`example.com` 等占位主机禁止露出。
- 种子承运商模板改为 17TRACK；已有占位模板在 lane seed 时自动改写。

## 2026-09-15 — 订单发货槽内联「物流进程记录」

- 去掉「查看当前位置」点击弹层；部件服务端调用 `OrderShipmentTrackingQueryService` 直出进程列表。
- Local Provider 至少回传一条「运单已登记」节点；可选外链「打开承运商轨迹页」。

## 2026-09-15 — 订单发货槽「查看当前位置」（已演进为内联进程）

- Order 编辑发货面板只留 `backend-order-shipments` 空槽；物流查询由本模块实现。
- `OrderShipmentTrackingQueryService` → `ShippingFacade::queryTracking`（承运商缺失时回退结账承运商）。

## 2026-09-14 — 订单列表/详情发货部件默认注入

- 对齐 Payment：Order 空槽 + Hook；本模块部件 `backend-order-shipments` / `backend-order-list-shipping` + `default_injections`。
- `BackendOrderShipmentsService`：详情发货行投影；列表「已支付待发货」计数 + 发货管理入口。
- 验：`BackendOrderShippingWidgetsContractTest`。

## 2026-09-14 — PDP 运费提示源串 + 印地语 CSV（2.9.2）

- `previewHint` 不再 `__()` 预译（避免 Phrase 语言落后路径 locale）；返回中文源串由 PDP `WidgetI18n` 翻译。
- `hi_IN.csv`：运费以结算页为准 / 重货 / 重货配送说明。

## 2026-09-14 — missing_weight 进入报价 diagnostics

- 结账纽约空配送：美洲 `weight_table` 因行重为 0 抛 `missing_weight`，被吞成 `shipping_profile_conflict`。
- 修：`LocalTemplatePricingService` 汇总 `unavailable_reasons`；`ShippingServiceManager` diagnostics 增加 `missing_weight`；Checkout 空态文案分流。

## 2026-09-14 — 更换地址 picking 拉全量地址簿

- 现象：picking 显示「收起地址列表」却只有 1 张卡；账户有 CN×3 + US×1，接口按当前配送国过滤；且 `listLoaded=1` 跳过刷新。
- 修：`openAddressPicker` 每次 `getDeliveryContext({ list_all_addresses: true })`；Checkout `wantsAllAddresses`；cache-bust `picker-all-addr1`。

## 2026-09-14 — 弹层地址标题泄漏 Weline_Framework + 更换地址

- 审图：`web_ui` / `ui_shot`（代付/快捷购买地址步标题显示「Weline_Framework」）
- 根因：`Template` 单例 `init()` 把 `title` 默认成模块名；`renderDeliveryAddressWidget` 未覆盖 → 部件 `__($title)` 泄漏模块名。
- 修：Checkout `renderDeliveryAddressWidget` 显式 assign `收货地址`；部件忽略 `Weline_*` 模块名标题；有已存地址即露出「更换地址」；cache-bust `helppay1`。

## 2026-09-14 — 快捷购买弹窗「更换地址」对齐结账

- 现象：隔离弹窗本地保存用单卡 `renderSavedAddresses` 冲掉地址簿 → `data-address-count=1` → 工具栏「更换地址」隐藏。
- 修：`upsertLocalSavedAddress` 合并卡；隔离态有已存地址即露出「更换地址」；`openAddressPicker` 仍 `getDeliveryContext` 懒加载；挂载后 `syncChangeAddressLabel` 可重同步。
- cache-bust：`checkout-shipping-address.js?v=20260914-change-address-helppay1`。

## 2026-09-14 — 第4章闭环：退货 / DDP·DDU / COD / 分批发（2.9.0）

- 规格：`doc/开发/spec/shipping-ch4-commerce-edges.md`（status=done）。
- Shipping：`ShippingService.incoterm` + `duty_notice`；`ShippingCommercePolicy`；退货模板 `SEED_TPL_RETURN_*`；`ReturnShippingQuoteService`；`SplitShipmentShippingService`（`first_only`/`each_shipment` 真二次计费）；后台航线可编辑 Incoterm。
- Payment：`CodFeeCalculator`（`config.fee` → `cod_fee_amount_minor`）。
- Checkout/Order：freeze/submit 进总额；结账摘要 COD 行；`money_snapshot_json` / `shipping_snapshot_json` 扩展键；成功页/订单页/后台 totals 分行。
- UT/e2e/evidence：`CommerceEdgesChapter4ContractTest`；`Weline_Shipping-ch4-commerce-edges.spec.js`；plan-suite Ch1–4 PASS；`doc/evidence/ch4/`。
- **宣称**：运费防漏收（Ch1–4）已完成。

## 2026-09-14 — 第1–3章真行为验收纠偏（同源→quote harness）

- 规格 UC 补 Given/When/Then + `scenario_key`。
- 新增 `Test/e2e/backend/anti-undercharge-quote-fixture.php`：真实 `ShippingFacade::listRates`。
- 重写 ch1/ch2/ch3 e2e + `Weline_Shipping-anti-undercharge-plan-suite.spec.js`。
- UT：Americas 2kg=11500、超 max 拒表、加价/addon sum 行为断言。
- 证据：`doc/evidence/ch{1,2,3}/`（harness JSON + WB 截图）。
- **未宣称**防漏收做完（第4章未交）；plan-suite 仅覆盖 Ch1–3。

## 2026-09-14 — 店面 website 回退 website:0 种子层（结账/Express）

- 现象：店面 `website_id=1` 无自有航线/Profile/仓发货绑定时，`listQuoteOptions`/`quoteSplit` 空配送或 `shipping_profile_conflict`。
- 修：`resolveNearestServiceLayer` 链尾回退 website:0；Profile 解析候选含 website:0；仓发货地址查找同样回退；`quoteSplit` 接受具体 `service_code`（兼容结账列表）。
- 运维：本机补绑仓↔发货地址（含 `SYS-DEFAULT`）；`setup:upgrade` 落 2.8.0 季节/分箱表。

## 2026-09-14 — checkout-shipping-address 支持晚挂载 mount

- 动机：HelpPay 快捷购买按需注入地址 HTML，脚本可能早于 DOM。
- 改动：`function mount(root)` + `data-shipping-mounted`；无 root 时导出 stub `mount`，有 root 仍自动 mount（结账零回归）。
- 契约：`CheckoutShippingAddressMountContractTest`；modules cache-bust `?v=20260914-mount1`。

## 2026-09-14 — 第1–3章验收收口（UT/e2e/WB）

- UT 18 PASS；章通路 e2e ch1/ch2/ch3 共 6 PASS。
- WB-OP（cacheDisabled）：费用模板可见 max_weight/weight_table；结账/配送说明 200。
- 证据：`doc/evidence/ch{1,2,3}/README.md`。
- `setup:upgrade` 仍门禁占用 → 需本机稍后落库 2.8.0。
- **未宣称**「防漏收做完」（第4章未交）。

## 2026-09-14 — 第3章：同仓分箱 + 签名保价 + 旺季燃油（2.8.0）

- 规格：`doc/开发/spec/shipping-ch3-pack-addon.md`（含验收矩阵 / `ship-ch3-e2e`）。
- `ShippingPackingPolicy` + `PackingSplitter`；Local 分箱模板价相加。
- `ShippingCheckoutAddon`（签名/保价）；`ShippingSeasonalRule`（种子默认关）。
- `configVersion` 纳入 packing/seasonal/addons；版本 `2.7.0` → `2.8.0`。
- 第4章未交付，不得宣称防漏收做完。

## 2026-09-14 — 验收纠偏：章规格补齐 acceptance 矩阵

- 第1–2章规格补澄清记录 / UC 表 / `acceptance_ids`；新建第3章规格 `shipping-ch3-pack-addon.md`（ready-for-plan）。
- 主计划每章绑定章通路 e2e；组套件 `ship-anti-undercharge-plan-suite`。
- MCP：同会话 `prepare_project`→`resolve_task_context`；禁止跳澄清直写业务码。

## 2026-09-14 — 第2章：偏远加价 + 地址/危品门禁（2.7.0）

- 规格：`doc/开发/spec/shipping-ch2-gates-surcharge.md`；需求 SHIP-SURCHARGE-001 / SHIP-ADDR-CAP-001 / SHIP-HAZMAT-001。
- `ShippingSurchargeRule` + Local 报价叠加；免邮保留加价；种子 CN 六省。
- `allowed_point_types` / `accepted_hazard_classes`；Checkout 透传 `delivery_point_type` 与行危品。
- Product Offer `shipping_hazard_class`（SCHEMA `4.9.0`）。
- 版本：`2.6.0` → `2.7.0`。第3–4章未交付，不得宣称防漏收做完。

## 2026-09-14 — 第1章：Shopify 式阶梯表 + Shipping Profile（2.6.0）

- 规格：`doc/开发/spec/shipping-ch1-rate-profile.md`；需求 SHIP-RATE-TABLE-001 / SHIP-PROFILE-002。
- `RateTemplate`：`weight_table`/`price_table`、`rate_brackets`、`max_weight_kg`；Local 缺重/超重拒报。
- `ShippingProfile` + 关联表；种子 `SEED_PROFILE_GENERAL` / `HEAVY`；九档+两档重货一次 seed。
- 报价按 Profile 分组；同名合并或合成最低价之和；SPI 目录改为 Profile。
- Product：Category `shipping_profile_code`（SCHEMA 4.8.0）。
- 版本：`2.5.2` → `2.6.0`。第2–4章未交付，不得宣称防漏收做完。

## 2026-09-14 — 承运商支持范围 UI 人性化重设计（2.5.2）

- 后台 `Carrier/edit`：支持范围收成单一面板；警示一行；模式勾选+短说明；地址选择器为主；Provider 默认/已保存与系统禁运收入「说明与参考列表」折叠。
- 契约 `CarrierCoverageKeepCurrentContractTest` 保持通过。
- 版本：`2.5.1` → `2.5.2`。

## 2026-09-14 — Provider 写死可达覆盖（燕文国家级）（2.5.1）

- 口径：燕文 Open API 通达按 **国家**（`countryId` / ISO），不写省市区。
- `YanwenDestinationCoverage` + `YanwenProvider::defaultCoverageRegions()`；`checkAvailability` 拒不在目录的国。
- Registry `defaultCoverageForCode` 优先读 Shipping Provider；种子/后台默认按 `provider_code` 应用，不再把燕文目录混进 local。
- 版本：`2.5.0` → `2.5.1`。

## 2026-09-14 — 万能配送壳 + Local/Yanwen Provider（2.5.0）

- 需求：SHIP-SHELL-001；文档 `shipping-shell.md` / `provider-development.md`。
- 壳：`ShippingProviderInterface` + `AbstractShippingProvider` + Scanner/Manager/`ShippingFacade`；`quoteRates`/`TrackingService` 只委派 Provider，禁止供应商分叉。
- 内置：`LocalRateTemplateProvider`（模板+FX）；`YanwenProvider`（calc.list / order.create / order.get）+ SystemConfig `shipping/carrier/yanwen/*`。
- `Carrier.provider_code`；Upgrade 回填 `local` + Extends 种子 YANWEN。
- 版本：`2.4.98` → `2.5.0`。

## 2026-09-14 — 报价缺汇率可观测（2.4.98）

- 增：`quoteRates` `getLastQuoteDiagnostics().fx_skipped`；`listQuoteOptions` 回传 `quote_diagnostics`。
- 版本：`2.4.97` → `2.4.98`。

## 2026-09-14 — 结账选中地址优先报价（货币切换丢美洲航线）（2.4.97）

- 现象：纽约地址卡选中后切换 USD，配送变成「国内标快」；根因是货币整页刷新后隐藏级联表单仍为 CN，`getData` 用表单默认国报价。
- 修：地址部件 `resolveQuoteAddress` 优先已选卡；Checkout `formAddress`/`resolveShippingAddressForQuote` 按 `shipping_address_id`/配送上下文纠偏；`quoteRates` 拒绝非 ISO 国别静默落成 CN。
- 版本：`2.4.96` → `2.4.97`。

## 2026-09-14 — 全球国家目录按热点 sort_order（2.4.96）

- 问题：`catalog=global` 用 Intl 按中文名字母序覆盖库内国家，搜索/下拉看不到热点位次。
- 修：`applyGlobalCountryCatalog` 合并 DB `sort_order`（缺省回落 default-markets），再按位次排序；Theme `address.js` 同步按热点搜排。
- 版本：`2.4.95` → `2.4.96`。

## 2026-09-13 — 国家热门排序位次（2.4.95）

- 需求：邮编多国歧义时热门市场靠前，且后台可调位置，避免填热门邮编却先看到冷门国。
- 修：`Region.sort_order` 作为热门位次；`default-markets/countries.tsv` 增 `sort_order`（US=50 先于 KR=120）；Upgrade 种子；地区管理「热门国家排序」可保存；`postalCountries` 按位次排序。
- 契约：`PostalCountriesEmbargoContractTest`。
- 版本：`2.4.94` → `2.4.95`。

## 2026-09-13 — 结账邮编 10001 多国排序（2.4.94）

- 问题：输入 `10001` 时期望纽约，但 `postal_countries` 按国码字母序把 DZ/EE 等排前，美国·New York 被埋没。
- 根因：跨国家歧义仅按 `supported/embargoed` + `strcmp(country_code)`；同国多行取首条，级联完整度未参与排序。
- 修：`postalCountries` 优先「默认可达市场 ∩ 市/区级挂接」；同国保留更完整 cascade 行；`normalizePostal` 剥离 ZWNJ/ZWSP 等粘贴隐字符。
- 契约：`PostalCountriesEmbargoContractTest`（10001→US·New York 首位）。
- 版本：`2.4.93` → `2.4.94`。

## 2026-09-13 — PDP 运费提示 en_US 译文（2.4.93）

- 审图：`web_ui` / `ui_shot`；英文站 PDP 价格下仍显示「运费以结算页为准」。
- 根因：`i18n/en_US.csv` 将该 source 的英文列写成中文占位。
- 修：补齐 `运费以结算页为准` / `包邮` / `本配送方案为服务级免邮…` 英文；`php bin/w i18n:collect Weline_Shipping`；MCP `module_i18n_csv_collect` / surface `module_i18n_csv` 明确禁止 en 列中文占位；`Phrase\Parser` 读模块 CSV 跳过文件头 BOM。
- 版本：`2.4.92` → `2.4.93`。

## 2026-09-13 — 默认发货锚点回退（2.4.92）

- 问题：种子发货地址未标 `is_default` 时，结账 `resolveDefaultOriginId=0`，九档 `SEED_LANE_*` 被滤光，只剩通配航线。
- 修：`ServiceLaneMatchService::resolveDefaultOriginId` 回退仓权威绑定→首个启用地址；种子选用地址时提升为默认。
- 契约：`DefaultOriginFallbackContractTest`。
- 版本：`2.4.91` → `2.4.92`。

## 2026-09-13 — 航线列表显示承运商/模板/发货地址名（2.4.91）

- 审图：`web_ui` / `ui_shot`；承运商/模板/发货锚点列裸数字不可读。
- 修：`ShippingService/index.phtml` 用已 assign 的 carriers/templates/origins 建 id→name 映射渲染列表。
- 契约/e2e：`ShippingServiceLaneListLabelsContractTest`、`SHIPPING-LANE-LABELS-001`、plan-suite。
- 版本：`2.4.90` → `2.4.91`。

## 2026-09-13 — 种子航线锚定当前仓发货（2.4.90）

- 需求：种子数据从当前仓位置发货，不可通配异地。
- 修：`DefaultShippingLaneSeedService` 写入 `origin_shipping_address_id`=默认发货地址；同步 `WarehouseShippingOrigin` 绑定默认逻辑仓。
- 版本：`2.4.89` → `2.4.90`。

## 2026-09-13 — 多仓拆单运费（2.4.89）

- 需求：按仓履约拆包，各仓运费相加；仓↔发货地址权威绑定；Inventory 只读分仓规划；`listSplitOptions`/`quoteSplit`；Checkout 冻结含 packages+request_hash；禁止模板矩阵/按距离文案。
- 新：`WarehouseShippingOrigin*`、`SplitShippingQuote*`、`WarehouseLaneCopyService`；Inventory `FulfillmentSplitPlan*`；Dropship 读权威口；后台绑定+一键复制默认站航线。
- 版本：`2.4.88` → `2.4.89`；Inventory `2.5.22` → `2.5.23`。

## 2026-09-13 — 费用模板自建删除（2.4.86）

- 需求：费用模板列表为用户自定义增加删除；系统种子不可删。
- 修：`ShippingConfigurationAdminService::deleteRateTemplate`（SEED_TPL_* 拦截 + 配送服务引用拦截）；`RateTemplate::remove`；列表操作列对齐免邮自建删除。
- 版本：`2.4.85` → `2.4.86`。

## 2026-09-13 — 免邮规则页顶部 Tab 分段（2.4.87）

- 审图：`web_ui` / `ui_shot`；一页堆叠字典+表单+列表信息过密。
- 修：页内顶部 Tab `rules|create|conditions`（`?section=`）；默认仅规则列表；条件类型 Local / 新增各占一签。
- 对齐 MCP `feature_ui_keep_simple_top_tabs`（强化：分组强制顶部 Tab，次要块展开卡片）。
- 契约/e2e：`FreeShippingConditionTypeLocalModelContractTest`、`SHIP-FREE-SEED-001`。
- 版本：`2.4.86` → `2.4.87`。

## 2026-09-13 — 免邮条件类型 LocalModel（2.4.85）

- 审图：`web_ui` / `ui_shot`；条件类型列裸显 `order_amount` 不人性化。
- 修：`FreeShippingConditionType` + `LocalDescription`；种子 order_amount/region/mixed/member_level/coupon。
- UI：列表「条件类型」列显示当前语言展示名；上方「条件类型（多语言）」字典卡每类型一条 `<local>`（避免同 type_id 重复 local ID）；创建下拉读字典展示名。
- 契约：`FreeShippingConditionTypeLocalModelContractTest`；e2e `SHIP-FREE-SEED-001`（含展示名 + local 触发器）。
- 版本：`2.4.84` → `2.4.85`。

## 2026-09-13 — 费用模板九档航线种子补齐（2.4.84）

- 问题：列表仅见国内/港澳台/亚太三档 `SEED_TPL_*`，单价多为「—」，计算类型裸英文。
- 修：`DefaultShippingLaneSeedService` 九档模板始终 upsert（含美洲/欧洲/大洋洲/拉美/中东非洲/其他）；国际档按重量充实 `weight_rate`；列表中文计算类型 + 系统徽章 + 单价数值化。
- Upgrade 重跑 `seedDefaultLanes`；契约/e2e：`RateTemplateSeedCompletenessContractTest`、`Weline_Shipping-rate-template-seed-complete.spec.js`。
- 版本：`2.4.83` → `2.4.84`。

## 2026-09-13 — 费用模板可编辑 + 基础货币换算（2.4.83）

- 问题：费用模板管理不可编辑；列表无金额/货币；按 `currency_code` 精确匹配报价，变相要求每币种一份模板。
- 修：同页编辑/保存；列表展示基础费用、单价与货币；创建/更新强制 `CurrencyRateService::getBaseCurrency()`；`quoteRates` 经 `tryConvert` 换算请求币，缺汇率 fail-closed。
- 依赖：`requires` 增加 `Weline_Currency`。
- 契约/e2e：`RateTemplateAdminUiContractTest`、`RateTemplateBaseCurrencyContractTest`、`Weline_Shipping-rate-template-base-currency.spec.js`。
- 版本：`2.4.82` → `2.4.83`。

## 2026-09-13 — 免邮规则国际种子模板（2.4.82）

- 需求：免邮规则增加国际通用满额种子模板，系统标记且不可删除。
- 纠偏：多档同时默认可启用会使低门槛覆盖高门槛；改为仅 `SEED_FREE_99` 默认启用，其余档可启停。
- 实现：`FreeShippingRule.origin`；`FreeShippingRuleSeedService`（49/99/149/199/299/499）；Upgrade + LaneSeed 幂等；后台启停/自建删除门禁与「系统」徽章。
- 契约：`FreeShippingRuleSeedContractTest`；e2e 列表通路。
- 版本：`2.4.81` → `2.4.82`。

## 2026-09-10 — 禁运选中卡提交门禁（2.4.81）

- 审图：`web_ui` / `error_shot`；地址卡「系统不支持该省份」仍可点提交订单。
- 根因：结账页未 await 异步 `validateShippingFields`；选中禁运卡未单独拦截。
- 修：`selectedEmbargoCard` + `validateShippingFields` 优先拦截 `data-embargo-blocked`；支持 `skipRequired`；cache-bust `v20260910-submit-embargo1`。
- 契约：`CheckoutShippingAddressEmbargoCardContractTest`。
- 版本：`2.4.80` → `2.4.81`。

## 2026-09-10 — 报价 SPI 禁运硬门（2.4.80）

- 现象：选址/保存已 assert，但 V2 `freezeQuote`→`ScopedShippingQuoteService::quote` 未禁运校验，禁运地址仍可出报价并下单。
- 修：`listOptions`/`quote`（有实体配送行）调用 `EmbargoService::assertAllowed`，映射 `shipping_destination_embargoed`；memory/harness 跳过。
- 契约：`ScopedShippingQuoteEmbargoGateContractTest`。
- 版本：`2.4.79` → `2.4.80`。

## 2026-09-10 — 结账地址卡禁运红框（2.4.79）

- 审图：已选「浦东测试」仅橙框；要红框提示禁运。
- 修：`checkout-shipping-address` 卡片 `is-embargoed` + 文案；CSS 用 `--color-danger*`；JS `applyEmbargoOnCard`；模块 `v20260918`。
- 契约：`CheckoutShippingAddressEmbargoCardContractTest`。
- 版本：`2.4.78` → `2.4.79`。

## 2026-09-10 — 报价匹配透传 region_id（2.4.78）

- 配合结账禁运硬闸：`quoteRates`/`getAvailableServices` 经 `addressMeta` 合并 `province_region_id`/`city_region_id`/`district_region_id` 后再 `evaluateAddress`。
- 契约：`CarrierCoverageAddressMetaContractTest`。
- 版本：`2.4.77` → `2.4.78`。

## 2026-09-10 — 报价 config version 稳定 + 作用范围对齐（2.4.77）

- 现象：结账 `listQuoteOptions`/`quote` 同请求 `shipping_quote_version_mismatch`，配送方式空列表。
- 根因：`activeQuoteConfigVersion` 对共享 ORM 裸 `getData()` 入 hash，二次查询混入集合下标；且指纹扫全站服务，与 `resolveNearestServiceLayer` 报价层不一致。
- 修：白名单列 + 新 Model 实例；指纹按最近配置层服务/模板/免邮 + scope 链禁运/可售 + 层内航线 + 全局承运商覆盖；请求内按 `scope_type:scope_id` memo；`quoteRates`/`getAvailableServices` 贯通 context；`shippingInfo`/`Checkout` 传 scope。
- 验收：同请求 version 三次相等；CN 上海 `listQuoteOptions` 2 options；`quote SEED_LANE_DOMESTIC` 成功；契约 6/6。
- 版本：`2.4.76` → `2.4.77`。

## 2026-09-10 — 结账地址 floating：补 placeholder（2.4.76）

- 审图：`web_ui` / `ui_shot`；结账收货人/电话/邮箱/邮编空态仍停 notch。
- 根因：输入无 `placeholder`，foundation `:placeholder-shown` 不触发。
- 修：`checkout-shipping-address.phtml` 收货/账单关键字段补占位；CSS resting 字重/色回落；契约增 placeholder 断言。
- 版本：`2.4.75` → `2.4.76`。

## 2026-09-10 — 账户地址表单对齐 Outlined notch（2.4.75）

- `account-address.css`：级联/邮编/明细改吃 `w-field` notch（去掉 `.w-address__label` 堆叠与过大圆角）。
- 版本：`2.4.74` → `2.4.75`。

## 2026-09-10 — 结账地址编辑器对齐 Outlined notch（2.4.74）

- `checkout-shipping-address.phtml` / CSS：收货/账单字段改为 `w-field` + `w-field__label`；去掉 `w-form-label` 堆叠。
- 店面 delivery/shipping 地址表单 grid 格补 `w-field`；后台 Carrier/地址/免邮/费率/服务/区域创建表单包 `w-field`。
- 契约：`CheckoutShippingAddressOutlinedFieldContractTest`。
- 依赖 Theme Address Taglib/JS notch（同日）。

## 2026-09-09 — 审图：禁运省/国前台不可选（2.4.73）

- 分流：`web_ui`；附图系统禁运「上海」仍可在结账/账户地址选到。
- Fail（A/E）：省级禁运未打标；邮编回填可写入禁运省市区；`activeSubnationalRules` 控制器已调但服务缺方法；WLS Worker OPcache 仍跑旧类时 `embargo_regions` 报 undefined method → 前端规则空 → 省项可点。
- 修：`EmbargoService::activeSubnationalRules`；`address.js` 拉 `embargo_regions` 打标禁用；邮编回填后禁运清空下级；禁运菜单去掉首屏可点竞态；落地后 `php bin/w server:reload`。
- 验收：`embargo_regions?country_code=CN` → 上海市 `15846/31`；结账搜「上海」/「Antarctica」选项 `disabled` +「不支持配送」；契约测 5/5。
- Theme `2.2.285` / loader `?v=20260909-subnational-embargo-block4`。

## 2026-09-09 — 审图/验收：作用范围脚本泄漏 + MCP 视觉验收补齐

- 分流：`web_ui`（配送服务页作用范围工具条）。
- Fail（A/E）：`<script>` 注释含 Taglib 字样导致脚本破裂，`decodeSegment`/`navigateFromNode` 渲成正文；选范围不切换。
- 修：`scope-toolbar.phtml` 仅 UI + JSON i18n；逻辑外链 `view/statics/js/backend/shipping-work-scope.js`。
- 验收：禁缓存 Browser — `leakVisible=false`；点选 E2E → `website · id=1`；JS 静态资源 200；契约测 5/5。

## 2026-09-09 — 审图：禁运原因快捷管理 + LocalModel（2.4.72）

- 分流：`web_ui`；附图原因列已可读，但缺可维护字典与多语言。
- Fail（E）：原因仅硬编码 `__()`/下拉，无法后台增删与 Local 翻译。
- 修：`EmbargoReason` + `EmbargoReason\LocalDescription`；`EmbargoReasonAdminService`（seed/list/save/delete/label）；系统禁运页「禁运原因」快捷卡 + `<local>`；新增下拉读字典；Upgrade 建表种子；Catalog 可发现。
- 版本：`2.4.71` → `2.4.72`

## 2026-09-09 — 邮编匹配禁运地点仍显示不支持（2.4.71）

- 需求：`200100` 应匹配中国·浦东；上海禁运时仍出现并标「不支持」，不得静默缺失。
- 目录：`CN/postal.tsv.gz` 增别名 `200100→浦东新区`（同 `200120` 挂 `31/3101/310115`）。
- API：`RegionService::postalCountries` 对各国首条 place 做 `EmbargoService::evaluateAddress`，返回 `place_name` + `embargoed`（覆盖省级/区级禁运）。
- 版本：`2.4.70` → `2.4.71`

## 2026-09-09 — 作用范围隔离 + 默认可达市场种子（2.4.70）

- 需求：左上角/页头切换范围后，配送服务/费用模板/免邮各自独立；默认可达市场开箱可报价。
- 模型：`ShippingService`/`RateTemplate`/`FreeShippingRule` 增加 `scope_type`+`scope_id`（同范围代码唯一）。
- 后台：`<w:scope>` 工具条 + URL `target_scope`；写入口绑定当前范围。
- 报价：`ShippingConfigScopeService` channel→store→website 最近非空层。
- 种子：`DefaultShippingLaneSeedService` + `data/default-markets/countries.tsv`；承运商默认覆盖同步扩到可达国。
- 版本：`2.4.69` → `2.4.70`

## 2026-09-09 — 审图：系统禁运列表可读名 + 清伪种子中国（2.4.69）

- 分流：`web_ui`；附图列表地区/原因裸露 `AQ`/`territory`/`no_commerce`，中国 CN/SH 误标「系统」。
- Fail（E 人性化）：原因与地区不可读；伪种子中国不可删。
- 修：`listAll`/`rowFromModel` 富化 `country_name`/`region_name`/`label`/`reason_label`（Intl + LocalDescription + `__()`）；新增表单原因改为可读下拉；`purgeNonCanonicalSeeds` 删除非 TSV 国家级伪种子（含省份/CN）；`seedFromRows` 与 Upgrade 联动；省份禁止 `origin=seed`。
- 版本：`2.4.68` → `2.4.69`

## 2026-09-09 — 系统禁运删除改 remove，禁用 delete 动作名（2.4.68）

- 现象：打开/命中 `…/systemembargo/delete` 仍 404；且删除不能用动作名 `delete`（会被收成 HTTP DELETE）。
- 修：Controller `remove()`（与 deactivate/activate 同为空 `request_method`）；列表表单 POST `shipping/backend/systemembargo/remove`；去掉 `postDelete`/`delete` 控制器动作。
- 验收：路由含 `systemembargo/remove`；POST 非 404（未登录 302 登录）。
- 版本：`2.4.67` → `2.4.68`（登记随正式 upgrade）。

## 2026-09-09 — 系统禁运解禁/再启用 w_changed 清缓存（2.4.67）

- 问题：后台解禁/再启用只改库，未走 ResourceChange，前台地址选择可能吃到旧 Query/Worker 缓存。
- 修：`SystemEmbargoResourceChangePublisher`（`shipping_embargo` + `global/shipping/embargo`）；`deactivate`/`activate`/`delete` 写后 `w_changed` + namespace bump + runtime 广播。
- 验收：结账页 `region.embargo_evaluate` 对 CN/SH 随解禁/再启用立即跟随（HTTP 探活）。
- 版本：`2.4.66` → `2.4.67`（登记随正式 upgrade）。

## 2026-09-09 — 系统禁运自建删除 404：方法名 delete 被收成 HTTP DELETE（2.4.67）

- 现象：列表表单 POST `shipping/backend/systemembargo/delete` 报错/404；CLI `admin->delete()` 正常。
- 根因：Controller 方法名 `delete` 被路由扫描映射为 `::DELETE`，仅匹配 HTTP DELETE；表单 POST 无对应路由。且此前生成路由未收编 delete。
- 修：当时改为 `postDelete()`；已被 2.4.68 的 `remove()` 取代。
- 版本：`2.4.66` → `2.4.67`。

## 2026-09-09 — 区域相对当前位置航线报价（一期 2.4.66）

- 方案：配送航线（Lane Service），不复活 Zone；费用模板仍只计费。
- 模型：`ShippingService.origin_shipping_address_id`；`ServiceRegion`（`w_shipping_service_regions`）。
- 报价：`ServiceLaneMatchService` 按默认发货仓 + 最具体目的地过滤；免邮 `region`/`mixed`。
- 后台：航线表单 + 模板重量/体积/件数 + 免邮地区 Taglib；原型 `lane-relative-quote-ui.html`。
- 版本：`2.4.65` → `2.4.66`。

## 2026-09-09 — 审图：系统禁运重复新增提示已存在（2.4.65）

- 分流：`web_ui`；fail：人性化——已禁运条目再点「新增或启用」仍显示成功新增。
- 修：`addRegion` 返回 `outcome=already_active|reactivated|created`；Controller 对已存在发 warning「无需重复新增」。
- 版本：`2.4.64` → `2.4.65`。

## 2026-09-09 — 审图：系统禁运自建可删、种子标系统（2.4.64）

- 分流：`web_ui`；fail：人性化——一律「不可删除」无法区分种子与自建。
- 修：`EmbargoRegion.origin`（seed|manual，存量默认 seed）；种子写入 seed；后台新增 manual；`delete` 仅 manual；列表「来源」徽章 + 条件删除。
- 版本：`2.4.63` → `2.4.64`。

## 2026-09-09 — 系统禁运新增 500：getMessage 未定义（2.4.62）

- 现象：POST `shipping/backend/systemembargo/add` → `Call to undefined method …::getMessage()`。
- 根因：控制器误用 `getMessage()`；同模块后台统一为 `getMessageManager()->addSuccess/addError/addWarning`；并去掉不存在的 `backendUrl()`。
- 版本：`2.4.61` → `2.4.62`。

## 2026-09-09 — 系统禁运支持国家+省份（2.4.61）

- 问题：系统禁运后台仅 `levels=country`，无法按省份区域禁运（匹配层与表结构本已支持 province）。
- 修：`SystemEmbargoAdminService::addRegion`；Controller 读 `province_region_id`/`province_code`；弹窗 `theme:address levels=country|province cascade`；列表增「级别/地区」。
- 种子仍可仅国家；一期不做市/区/街系统禁运。
- 版本：`2.4.60` → `2.4.61`。

## 2026-09-09 — 审图：系统禁运新增弹窗去掉重复标签（2.4.60）

- 分流：`web_ui`（附图红箭：「国家/地区」叠两行）。
- fail：人性化/规范美观（外层手写 `w-field__label` 与 `<w:theme:address>` 内置标签重复）；主题契合应对齐 Region 弹窗（仅 Taglib 自带 label）。
- 修：删除 `SystemEmbargo/index.phtml` 弹窗外层「国家/地区」label，保留 address 控件标签。
- 版本：`2.4.59` → `2.4.60`。

## 2026-09-09 — 配送 Manager 去 iframe + 侧栏可定位（2.4.59）

- 审图 `web_ui`：Manager Tab iframe 嵌套灰框难受；URL 为 manager?tab= 导致侧栏无法高亮分区菜单。
- 修：对齐 Ai — `partials/manager-tabs.phtml` 真链 `a.w-tabs__tab`；`Manager::index` 按 tab 302 到真实分区页；Region/Carrier/…/SystemEmbargo/Tracking 非 embed 注入 tabs。
- 侧栏：各分区 URL 与 `menu.xml` action 一致（含系统禁运）；并修 Admin `isMenuActive` / Backend `pathVariants` 对省略 `/index` 的匹配。
- 版本：`2.4.58` → `2.4.59`。

## 2026-09-09 — 系统禁运侧栏菜单（2.4.58）

- 问题：直达 `shipping/backend/systemembargo` 时配送管理侧栏无「系统禁运」（仅有 Manager Tab）。
- 修复：`etc/backend/menu.xml` 增加 `Weline_Shipping::system_embargo`（order 95，配送服务与物流跟踪之间）。
- 收集：本机全量 `menu:collect` / `MenuXmlReader::read` 在 `Weline_Admin` 解析处卡住；已对 Shipping 条目用 `MenuRegistry::upsertManagedMenu` + `w_menu` 同步入库（route=`shipping/backend/systemembargo/index`）。门禁空闲后建议再跑一次 `php bin/w menu:collect -m Weline_Shipping` 对齐。
- 版本：`2.4.57` → `2.4.58`。

## 2026-09-09 — 审图：系统禁运新增改弹窗（2.4.57）

- 分流：`web_ui`；fail：页内「新增国家」整卡抢首屏（人性化/规范美观）。
- 原型：`view/prototypes/system-embargo-add-dialog-ui.html`（A 页内 vs B 列表+弹窗 → **B**）。
- 修：对齐地区管理——列表头 CTA「新增国家」→ `w-dialog`；保留 `<w:theme:address levels=country>`。
- 版本：`2.4.56` → `2.4.57`。

## 2026-09-09 — 审图：系统禁运新增改 theme:address（2.4.56）

- 分流：`web_ui`（后台「新增国家」手写 ISO 码）
- fail：人性化（要求记 ISO 黑话）/ 主题契合（未用官方地址 Taglib）/ 需求（地区选择须 theme:address）
- 修：`<w:theme:address selection=single levels=country catalog=global searchable>`；`country_code` 由 address.js 元字段写入；提交校验友好提示。
- MCP：`theme_address_for_region_pickers` / `taglib_before_hand_rolled_controls` / Taglib 场景映射表补「系统禁运·禁止手写 ISO」。
- 版本：`2.4.55` → `2.4.56`。

## 2026-09-09 — 系统禁运 + 配送方案绑定一期（2.4.55）

- 系统禁运：`SystemEmbargoAdminService`（list/add/deactivate/activate；禁删；禁 `replaceForScope(system)`）；自然唯一键 `uk_embargo_natural`；种子 `data/system-embargo/countries.tsv`（仅国家级）。
- Manager Tab 三处同步「系统禁运」；解禁确认含「全站生效」。
- `EmbargoService` 先加载 active system；`messageFor(system)`→「系统」。
- `activeQuoteConfigVersion` 纳入禁运+destination+carrier coverage；`quoteRates` 按 `shipping_profile_code` 过滤（空=不限制）。
- Product SPI：`ShippingServiceProfileCatalogProvider`；承运商编辑补充「系统禁运·不可选」；费用模板文案区分计费 vs 可达性/配送方案。
- 运维：本机 `setup:upgrade` 被并行 `module:remove` 占用时，已对手工执行 `w_shipping_embargo_regions` 扩列+`uk_embargo_natural`+系统种子 7 国；全站 `ProductShardProvisioner::provisionWebsite` → SCHEMA `4.7.0`（Offer 加 `shipping_profile_code`）。门禁空闲后仍建议跑一次正式 `setup:upgrade` 对齐模块版本登记。
- 版本：`2.4.54` → `2.4.55`。

## 2026-09-09 — 审图：收起态误藏「使用新地址」（2.4.53）

- 分流：`web_ui`（结账收货信息；仅一张地址卡+编辑，无新增/更换）
- 条件：
  - 有已存地址 → `data-mode=collapsed`；`[data-mode="collapsed"] [data-add-address]{display:none}` 把「使用新地址」藏掉（误伤）
  - `addressCount<=1` →「更换地址」故意 `hidden`（无可切换项）；`>1` 才显示，点开 `picking` 懒加载列表
- 修：collapsed 下改为 `display: inline-block` 显式保留「使用新地址」（覆盖旧缓存 `display:none`）；CSS `?v=20260909-csa-add2`
- 版本：`2.4.52` → `2.4.53`

## 2026-09-09 — region Query descriptor cache_ttl 对齐浏览器 TTL

- 变更：`RegionQueryProvider` descriptor `cache_ttl`：`list`/`children` 14400s（4h）、`country_profile` 43200s（12h），与 Frontend Worker 浏览器响应缓存策略对齐；实际浏览器缓存由 `weline-api-worker.js` 执行。

## 2026-09-08 — 结账收货地址「保存此地址」始终可见（审图）

- 图类型：`web_ui`（结账收货信息编辑态，附图缺主 CTA；双「邮编」）。
- 技能门禁：`frontend-design` / `prototype` / `weline-theme-development` 可用。
- 问题：
  1. 登录且无已存地址时 `$hasSaved || !$isLoggedIn` 不渲染操作区，编辑表单无「保存/收起」按钮。
  2. `postal-lookup` 未显式 `postal=false` 时 Taglib 默认壳内再渲邮编，与部件外 `data-postal-first` 重复。
- 需求：按钮文案「保存此地址」；点击 → `saveDeliveryAddress` 后收起为地址卡；不点也可填表下单。
- 修复：
  - `checkout-shipping-address.phtml` 始终渲染 `editor-actions`；`save_use` 改为「保存此地址」；i18n 补齐。
  - `<w:theme:address postal="false" postal-lookup="true">` 仅驱动壳外邮编。
- 验收：结账页可见「保存此地址」；仅一个收货邮编框；点击后 collapsed 地址卡。

## 2026-09-08 — 个人中心地址操作 toast 未跟多语言

- 问题：站点语言 ZH，保存收货地址 toast 仍显示英文 `Delivery address saved.`。
- 根因：`DeliveryAddressQueryProvider` / `ShippingAddressQueryProvider` 成功失败文案硬编码英文，未走 `__()`。
- 修复：改为中文 msgid + `__()`；`i18n/zh_Hans_CN.csv`、`en_US.csv` 补齐保存/删除/默认/会话等词条。
- 验收：ZH 下保存地址 toast 为「收货地址已保存」。

## 2026-09-08 — 个人中心保存地址 Unknown frontend worker param: street_id

- 问题：编辑收货地址点「保存」红条 `Unknown frontend worker param: street_id`。
- 根因：`street_id` 为新增地址字段；`<w:theme:address>` 已提交 `street_id`/`street_code`，但 `deliveryAddress.save` / `shippingAddress.save` Frontend worker params 未声明；Gateway 严格拒未知参数。另：运行态读 `generated/framework/query_providers.php`，仅改 PHP 不 compile 仍会拒。
- 修复：
  - 两 Provider descriptor 声明 `street_id`（mixed）与 `street_code`
  - `DeliveryAddressService` / `ShippingAddressService` 更新落库允许 `street_id`
  - 会话 `normalizeDeliveryAddress` / `syncToDatabase` 携带 `street_id`
  - `php bin/w framework:compile` + `server:reload` 刷新编译注册表与 Worker
- 验收：个人中心编辑地址 → 保存成功 → 无 worker param 红条。

## 2026-09-08 — 个人中心地址删除确认静默失败

- 问题：点击「删除」无弹窗、无请求；`confirmDelete` 仅认 `Theme.Notice.confirm`，前台账户页无该对象时直接 `Promise.resolve(false)`。
- 修复：`account-address-v3.js` 优先 Theme Notice，其次 `Weline.UI.dialog.confirm`（danger），最后 `window.confirm`；消息提示同步回退 `Weline.UI.toast`。
- 续修：确认后 `setTimeout(0)` 再调删除 API，避免在 `dialog.confirm` 结算微任务内打 Query Worker 导致「删除中…」卡住。
- 根因续：`Weline.Api.resource()` 返回 Proxy（`then` 为保留字），`getAddressApi().then(...)` 同步抛错，按钮停在「删除中…」；改为 `Promise.resolve(resource)`。
- 缓存：`shippingAccountAddress` 路径追加 `?v=20260908-delete-promise-resolve`，避免 sticky `_weline_dev` 继续命中旧脚本。
- 验收：收货地址点删除 → 确认对话框 → 确认后卡片移除。

## 2026-09-08 — 个人中心地址表单对齐结账邮编定位（审图）

- 图类型：`web_ui`（账户收货地址编辑表单）。
- 问题：表单邮编在级联之后且未开 `postal-lookup`，与结账「先填邮编定位」不一致。
- 修复：`account.sidebar.content.phtml` 改为 `<w:theme:address postal postal-lookup detail detail-name=street street=false>`；CSS 壳通栏；JS 回填邮编/详细地址。
- 协同：Theme `address.js` 跳过壳内 reparent（见 Theme 2.2.260），否则邮编壳被拆开。
- 验收：个人中心编辑地址 → 邮编在标签级联前 → 填邮编回填省市区。

## 2026-09-07 — 地区列表本地化批量读取

- 定位：地区列表原先在 `toRegionList()` 中逐行调用 `RegionLocalNameResolver`，一次返回 352 行就会重复触发地区本地化查询；query-bin 冷样本为 **16.99s**、**516 个 DB span**，`w_shipping_regions` 与 `w_shipping_region_local` 反复出现。这是调用逻辑的 N+1，当前证据不支持先改表结构或索引。
- 修复：先收集本次列表的 region id，再用 `namesByRegionIds()` 一次批量读取本地化名称，按 id 回填；保留原 locale 回退和无本地化名称时的旧路径。省/市/区列表因此只需固定数量的批量查询。
- 验证：改动后真实接口返回 HTTP 200，默认列表 **352 行**、CN 列表 **32 行**；timing 中默认冷请求只有 **47 个 DB span**，本地化名称通过固定数量的批量 `IN` 查询取得，热请求约 **0.86–1.04s**。定向 Shipping 回归 **11 tests / 35 assertions** 通过。

## 2026-09-07 — GeoNames→TSV→DB 对账（2.4.52 续）

- 对账：GeoNames ADM1=3865/228 国、ADM2≈47593；catalog 省 3871 / 市 88299 / 国 250；纯 GeoNames/合成国 **227/227** 对齐；G20/CSC/CN 包故意偏离 ADM（如 US 市 16731 vs ADM2 3143）。
- TSV↔DB：市 **88299=88299**；国 **250=250**；省曾多 2 条测试脏数据 `AF-TEST-AUTO`、`AR-DIALOG-T1`（已删），现省层一致。
- 冒烟：`--country=AF,AR,WF --layer=provinces|cities` 填缺导入为 0（已齐）。

## 2026-09-07 — DB 级联填缺 + 导入加速（2.4.52）

- 问题：catalog 已齐但本机 DB 仍有数十国省/市少行；逐条 upsert 导入近卡死；WF 缺国家节点。
- 修复：`AddressCatalogImporter::importRegionLayer` 改为按国 code-map 批量 INSERT 仅补缺失；全量 `--layer=countries/provinces/cities` 填缺；追踪邮编构建脚本、PostalPlace/Street/Local、profiles、region-locals 等地址配套资产。
- 验收：省/市相对 catalog partial=0；MW/RW/TO/CN/WS 对齐；WF 国家节点存在。
- 版本：`2.4.51` → `2.4.52`

## 2026-09-07 — address-catalog 纳入 Git 追踪（2.4.51）

- 问题：`data/address-catalog/` 约 16MB 国家 TSV 一直未 `git add`，换机/CI/生产无法从仓库复现级联源。
- 变更：目录加入版本库（含 `README.md` 追踪规则）；权威源须提交，禁止仅本机生成。
- 版本：`2.4.50` → `2.4.51`

## 2026-09-07 — 全球地址城市层补齐（2.4.50）

- 问题：catalog 已有 ADM2 城市（如 AF Badakhshan→Faizabad）但 DB 未灌入；另有 59 国 GeoNames 无 ADM2，cities 空导致结账「暂无可选地区」。
- 修复：补导入 AE/AF/AL/AM/AO/BE/MW/RS/RW/TO 城市；MW/RW/TO 缺省先补 provinces 再 cities；`build-global-cascade-from-geonames.php --fill-empty-cities` 按省合成 `{prov}-CITY`；`shipping:addresscatalog:import --country` 支持逗号多国。
- 验收：DB 有省必有市；AF Badakhshan children=28；SG/QA 可选手填同名合成市。
- 版本：`2.4.49` → `2.4.50`

## 2026-09-07 — 结账地址改用 theme:address 邮编推算（2.4.49）

- `checkout-shipping-address`：`postal-lookup="true"`；部件 JS 删除重复 `schedulePostalLookup`，交由 Theme `address.js`。
- 验证：结账填邮编仍回填级联。

## Hotfix — 2026-09-07：地址名字 LocalModel 补齐 PostalPlace（2.4.48）

- 问题：Region/Street 已有 LocalDescription；邮编反查 `PostalPlace.place_name` 在区划名缺失时可充当地址展示名，却无 LocalModel。
- 变更：新增 `PostalPlace\LocalDescription`（`w_shipping_postal_place_local`）；`RegionLocalNameResolver` / `postalLookup` 按 locale 读 Local；Upgrade 注册；契约测试覆盖三份地址名 Local。
- 版本：`2.4.47` → `2.4.48`

## Hotfix — 2026-09-06：地区列表筛选降空白（2.4.47）

- 问题：筛选区 chips 独占整行、控件拉满宽、灰底提示卡偏空。
- 修复：保留 `<w:theme:address selection=multi>`；限宽约 40rem、缩小 chips/列间距；提示降为 muted 一行浅层说明；卡片内边距收一档。
- 版本：`2.4.46` → `2.4.47`

## Hotfix — 2026-09-06：地区列表默认中国（2.4.46）

- 问题：无 `country_code(s)` 时筛选无种子，地址标签/异步列表易落到目录首项阿根廷（AR）。
- 修复：`Region::resolveSelectedCountryCodesFromRequest` 与模板种子均默认 `CN`。
- 版本：`2.4.45` → `2.4.46`

## Hotfix — 2026-09-06：地区页地址标签与列表全面异步（2.4.45）

- 问题：列表仍 SSR 省份行，选国家还整页重载，触发慢响应/输出膨胀；与「地址标签交给 JS」契约不符。
- 修复：`Region::index` 只透传 URL 国家码；去掉 countries/region_tree SSR；模板仅 `theme:address` 壳 + 空表；JS 调 `/shipping/frontend/region/list?mode=children` 异步填表，`history.replaceState` 同步 URL，禁止整页重载。
- 版本：`2.4.44` → `2.4.45`

## Hotfix — 2026-09-06：新增地区禁止重复代码（2.4.44）

- 问题：创建应以「新增不存在」为主；已有省代码无区县仍可能绕过前端；省已存在时整单失败无法只加新区。
- 修复：`createManualProvinceDistrict` 省已存在且无区县→拒绝；省已存在+新区代码→复用省只建区；区代码已存在→拒绝。弹窗提交前用列表浅层代码预检。
- 版本：`2.4.43` → `2.4.44`

## Hotfix — 2026-09-06：新增地区弹窗国家单选 UI（2.4.43）

- 问题：弹窗外层重复「国家/地区」标签；后台缺单选 address CSS，洲分组菜单内联泄漏；字段区无分区。
- 修复：国家改显式 `selection=single levels=country` 官方单选标签（去掉外层 label）；弹窗 overflow 可见；省/区分区标题；`applyValues` 预填当前国；依赖 Theme 2.2.196 单选基座样式。
- 版本：`2.4.42` → `2.4.43`

## Hotfix — 2026-09-06：地区列表禁止整树 SSR（2.4.42）

- 问题：`country_codes=AF,CN` 时 `Region::index` 对每国调用 `getTreeByCountryCode` 递归整树并渲染 HTML，触发 WLS `OverflowException`（output capture ~16MB）。
- 修复：后台列表改为 `getAdminListTreeByCountryCode` 仅省份浅层；筛选 JS 按省/市/区映射到省份行；模板提示市/区走上方筛选按需加载。
- 版本：`2.4.41` → `2.4.42`

## Hotfix — 2026-09-06：地区列表三级多选立即过滤（2.4.41）

- 问题：筛选标签已选中国，表格仍显示阿根廷；仅按「最后一个国家」跳转，无法多选多过滤，省/区与国家不同步。
- 修复：支持 `country_codes` 多国合并树；选中国家集合变化立即重载；同国下省/区多选做并集收窄；行数据补 `data-country-code`。
- 版本：`2.4.40` → `2.4.41`

## Hotfix — 2026-09-06：地区列表新增按钮右对齐（2.4.40）

- 问题：列表卡头把标题与「新增地区」包进单一 `w-stack`，Theme `w-card__header` 的 space-between 失效，按钮挤在标题旁中间。
- 修复：卡头仅保留标题 + 按钮为直接子元素；筛选地址行下移到 `w-card__body` 顶部全宽。
- 版本：`2.4.39` → `2.4.40`

## Hotfix — 2026-09-06：新增地区改弹窗严格创建（2.4.39）

- 问题：新增地区内联 multi 路径逻辑混乱——已存在仍可 upsert；省/区也是「搜索已有」而非新增录入；缺少点击「新增」再弹窗的交互。
- 修复：列表头「新增地区」打开 `w-dialog`；国家用 `theme:address levels=country` 选择；省/区手填名称+代码；`createManualProvinceDistrict` 已存在代码直接拒绝；去掉内联 multi 创建卡（列表筛选 multi 保留）。
- 版本：`2.4.38` → `2.4.39`

## Hotfix — 2026-09-06：地区地址选择横三列（2.4.38）

- 问题：地区列表/新增的 theme:address multi 在 Manager iframe 下因 `minmax(220px)` auto-fit 竖排。
- 修复：Region 页作用域 CSS 强制国家/省/区 `repeat(3, minmax(0,1fr))` 横排（极窄仍回落单列）。
- 版本：`2.4.37` → `2.4.38`

## Hotfix — 2026-09-06：新增地区按地址标签路径一次创建（2.4.37）

- 问题：新增地区只能选手动国家，且需手填代码/名称/类型。
- 修复：创建表单改为 `theme:address selection=multi`（country|province|district）；提交 `selection_json` 后 `createRegionsFromAddressSelection` 自动识别并幂等写入；保留 embed/country_code 回跳。
- 版本：`2.4.36` → `2.4.37`

## Hotfix — 2026-09-06：地区筛选改 theme:address 可搜索（2.4.36）

- 问题：地区列表用手写国家 `<select>`，不可搜索；与后台地址选择标签不一致。
- 修复：列表筛选与创建国家改为 `<w:theme:address selection="multi" searchable catalog=global>`（单级国家 / 多级 country|province|district 均可搜索）；选国家保留 embed 重载；选省/区收窄树表。
- 版本：`2.4.35` → `2.4.36`

## Hotfix — 2026-09-06：地区管理表单窄视口并排（2.4.35）

- 问题：Manager iframe 宽度常 <48rem，表单仅设 `--w-span-md` 回落全宽堆叠，创建按钮 `data-w-width=full` 巨宽。
- 修复：创建表单与列表头同时设 `--w-span`/`--w-span-md`（字段一行、创建按钮换行右对齐）；去掉创建按钮全宽；去掉筛选区 Bootstrap `me-2`。
- 版本：`2.4.34` → `2.4.35`

## Hotfix — 2026-09-06：地区国家改 address-catalog，移除 i18n 同步（2.4.34）

- 问题：地区管理国家下拉与「从i18n同步国家」依赖 I18n CountryRepository，与配送地址库权威源不一致。
- 变更：国家列表改 `RegionService::getCountries()`；删除 `syncFromI18n` 路由/ACL/UI；邮编支持国与已安装国家回退亦只读 Shipping Region。
- 版本：`2.4.33` → `2.4.34`

## Hotfix — 2026-09-06：embed 保留 Theme CSS、仅关壳层（2.4.33）

- 问题：`default.blank` 未注入 Theme Partials head，iframe 无 foundation/backend CSS，`<w:icon>` 放大失控。
- 变更：`ShippingBackendEmbedTrait` 改回 `default.default`，通过 `meta.showHeader/Sidebar/Footer/PageHeader=false` 去壳。
- 版本：`2.4.32` → `2.4.33`

## Hotfix — 2026-09-06：配送总览 embed 去双重壳 + 去重复页头（2.4.32）

- 问题：Manager Tab iframe 仍套 `default.default` 完整后台壳（topbar/sidebar/footer），且模板再输出一套页头，视觉双份混乱。
- 变更：`ShippingBackendEmbedTrait` 在 `embed=1` 时切 `default.blank` 并关闭 `layoutShowPageHeader`；Manager 模板不再自渲染 heading/`main.w-backend-page`。
- 版本：`2.4.31` → `2.4.32`

## Hotfix — 2026-09-06：配送总览 Theme tabs 对齐（2.4.31）

- 问题：`Manager/index` HTML 已用 `w-tabs__*` / `w-backend-page`，但内联 CSS/JS 仍绑 Bootstrap `.nav-tabs` / `.nav-link` / `.tab-pane`，样式与切换失效，页面显乱。
- 变更：改为 Theme `w-tabs__tab` + `data-w-component="tabs"`；iframe 懒加载监听 `weline:ui:tabs:change`；去掉失效 Bootstrap 选择器。
- 版本：`2.4.30` → `2.4.31`

## Hotfix — 2026-09-06：支持范围谨慎提示排版美化（2.4.30）

- 问题：裸 `w-alert` + `strong` 被 foundation 网格当成两列，标题竖排挤在左边。
- 变更：改为 `w-icon` + `w-alert__content` / `w-alert__title`；tone 用 `warning`；费用模板设计说明同步结构。
- 版本：`2.4.29` → `2.4.30`

## Hotfix — 2026-09-06：费用模板设计说明 + 承运商覆盖谨慎提示（2.4.29）

- 说明：费用模板仅计费公式（挂配送服务），不含到达地区费率矩阵；可达性由承运商覆盖 ∩ 可售/禁运。
- UI：承运商编辑「支持范围」红色谨慎提示；变更覆盖保存前 `confirm`；费用模板列表页设计说明条。
- 契约：`CarrierCoverageKeepCurrentContractTest`、`RateTemplateDesignNoteContractTest`。
- 版本：`2.4.28` → `2.4.29`

## Hotfix — 2026-09-06：支持范围勾选「当前覆盖默认」并默认勾选（2.4.28）

- 问题：原「保存时应用 Provider 默认（覆盖当前选择）」难懂，且默认未勾选。
- 变更：字段改为 `coverage_keep_current`（默认勾选）= 保存页面当前选择；取消勾选才 `applyProviderDefaults`。
- 契约：`CarrierCoverageKeepCurrentContractTest`。
- 版本：`2.4.27` → `2.4.28`

## Hotfix — 2026-09-06：承运商保存 302 误报（2.4.27）

- 根因：`Carrier::save/delete/toggle` 的 `catch (\Throwable)` 吞掉 `redirect()` 抛出的 `ResponseTerminateException`，把「Response terminate with status 302」写成顶部错误横幅。
- 变更：先 `catch (ResponseTerminateException)` 再抛出；真实业务错误仍 flash（后台由 Component `message.phtml` 转 toast）。
- 版本：`2.4.26` → `2.4.27`

## Hotfix — 2026-09-06：全球级联补齐 + 全量邮编 TSV/导入（2.4.26）

- 级联：无 GeoNames ADM1 的 22 国/领地写入合成省 `{CC}-00`；`countries.tsv` 补 XK；catalog 250 国均有 provinces。
- 邮编：`build-global-postal-from-geonames.php --from-all-countries`（修仓库根路径 + 流式写出）；GeoNames zip 有数据的约 121 国写入 postal.tsv.gz（CN 仍 adcode）。
- 导入：countries → provinces/cities → postal（按国 `bin/w`）。
- 版本：`2.4.25` → `2.4.26`

## Hotfix — 2026-09-06：GeoNames 全球级联 fill-missing（2.4.25）

- 根因：此前仅 G20/CSC pack 有省市区 TSV；GeoNames 只用于邮编。EG/PY 等有国家名、无下级。
- 变更：`scripts/build-global-cascade-from-geonames.php`（ADM1→provinces、ADM2→cities；默认 fill-missing 不覆盖 CN/已有包）；约 205 国补齐，catalog 含 provinces 约 228 国；契约 `GeonamesCascadeCatalogContractTest`。
- 导入：`php bin/w shipping:addresscatalog:import`（可按 `--country=EG` 增量）。
- 版本：`2.4.24` → `2.4.25`

## Hotfix — 2026-09-06：移除 JSON 地区包，仅保留 address-catalog（2.4.24）

- 说明：巴拉圭/埃及等显示「无下级」是因为 **address-catalog 未收录**（当时约 23 国：G20+港台澳新等），不是现实无省/州；TSV 对已收录国是主源，旧 `data/regions/*.json` 仅为构建中间态。
- 变更：删除 `data/regions/` 与 `build-g20/cn/address-catalog-from-packs` 脚本；`RegionCascadeEnsureService` 不再读 JSON，仅探测 catalog 覆盖；契约改验 `provinces.tsv.gz`。
- 版本：`2.4.23` → `2.4.24`

## Hotfix — 2026-09-06：分组跳转回点 + 区县芯片（2.4.23）

- 问题：省份跳转回点第一组失效；区县无正确省份跳转标签。
- 变更：跟随 Theme `address.js` `v=20260906-jump-toggle1`。
- 版本：`2.4.22` → `2.4.23`

## Hotfix — 2026-09-06：可售/禁运下级感知全部已选上级（2.4.22）

- 问题：多选国家后省份只显示有下级的国家（如 CN），无下级国（如 AI）不出现分组过滤。
- 变更：跟随 Theme `address.js` `v=20260906-parent-groups1`（空分组占位 + 跳转芯片）。
- 版本：`2.4.21` → `2.4.22`

## Hotfix — 2026-09-06：可售/禁运分组跳转芯片（2.4.21）

- 问题：国家/省份分组后仍需长滚定位。
- 变更：跟随 Theme `address.js` `v=20260906-group-jump1`（菜单顶分组芯片快速跳转）。
- 版本：`2.4.20` → `2.4.21`

## Hotfix — 2026-09-06：可售/禁运国家大洲分组（2.4.20）

- 问题：国家 249 项扁平列表难扫；需电商常用大洲（热门/亚/欧/美等）。
- 变更：跟随 Theme `address.js` `v=20260906-continent1`（单选与 multi 共用大洲分组）。
- 版本：`2.4.19` → `2.4.20`

## Hotfix — 2026-09-06：可售/禁运区县叶子国与菜单分组（2.4.19）

- 问题：已选澳大利亚+南澳后区县下拉找不到南澳下级（AU 行政到 city）；多国多省时列表难扫。
- 变更：跟随 Theme `address.js` `v=20260906-multi-group1`（city 叶子入区县池；省份按国家、区县按国家/省份分组）。
- 版本：`2.4.18` → `2.4.19`

## Hotfix — 2026-09-06：删除遗留配送区域 Zone（2.4.18）

- 问题：承运商覆盖 + 网站/店/渠道可售/禁运已成主路径；Zone 菜单与兜底回退易误配、语义分叉。
- 变更：删除 Zone/ZoneRegion 模型、ZoneService、后台 Controller/模板/菜单/总览 Tab；`ShippingServiceManager` 去掉 Zone 兜底（异常返回空）；`ShippingService` 去掉 `zone_id`；Upgrade 删除 `w_shipping_zones` / `w_shipping_zone_regions` 与 `zone_id` 列；e2e 与文档对齐。
- 版本：`2.4.17` → `2.4.18`

## 2026-09-06 — 地区 Local：主表有名即可作翻译源（2.4.17）

- 约定：主表 `region_name`/`street_name` 有值、Local 为空时，以主表为源经 LocalModelTranslation 补其它语言；不要求源语言 Local。
- 依赖：`Weline_I18n` `1.0.23`（大表分页扫描）。`seedAll` 只入队翻译。
- 版本：`2.4.16` → `2.4.17`

## Hotfix — 2026-09-06：禁运/可售省份空搜截断跟随 Theme（2.4.16）

- 问题：已选 AU+CN 时省份下拉看起来只有少数几条（Theme multi 空搜 `slice(0,12)`）。
- 变更：跟随 Theme `address.js` `v=20260906-multi-province1`；labels 增加 `poolCountHint`；清 tpl/FPC 后验收应见约 39 省并可搜。
- 版本：`2.4.15` → `2.4.16`

## 2026-09-05 — 地区名称改为 LocalModel 翻译链路（非英语专用种子）

- 问题：此前用 `CN.en_US.tsv` 自动灌英语，被视作「针对英语配置」；正确路径应是源语言 Local + `LocalModelTranslation` 补目标语言。
- 变更：`RegionLocalSeedService::seedAll` 只回填 AI 源语言 Local，并 `enqueue` LocalModel 翻译队列；`importLocalePack` 保留为可选离线工具；契约测试改为断言 Catalog 发现 Region/Street Local。
- 版本：`2.4.14` → `2.4.15`

## Hotfix — 2026-09-05：禁运区域强制刷新生效搜索多选（2.4.14）

- 问题：源码已是 multi，但用户后台仍见旧「类型/国家代码/地区 ID」手填表单（tpl/FPC 陈旧）。
- 变更：清 Shipping/Websites/Backend tpl + 相关 FPC；cache `v=20260905-multi-embargo1`；验收禁运与可售同交互。
- 版本：`2.4.13` → `2.4.14`

## Hotfix — 2026-09-05：禁运多选选择器直挂 address.js（2.4.13）

- 问题：禁运区已改为 multi，但动态注入脚本在部分后台页未稳定挂载，用户仍看到旧手填表单缓存。
- 变更：`embargo-form-section` 与可售目的地一致直挂 `address.js?v=20260905-multi-float1` + onload boot；清 tpl/FPC 后验收。
- 版本：`2.4.12` → `2.4.13`

## 2026-09-05 — 英语环境 Ship to 地名走 Region/Street LocalModel

- 问题：英语站 Header「Ship to」仍显示「成都市」等中文快照；运输模块此前无 Region/Street LocalDescription。
- 变更：新增 Region/Street `LocalDescription` 与展示解析；初版曾用 `CN.en_US.tsv` 自动灌英语（已在 2.4.15 改为源语言 Local + LocalModelTranslation，见上条）。
- 交付地址：[英语首页（Ship to）](http://p05113ef3.test.weline.com:9555/en_US/)。

## 2026-09-05 — 配送与退换指南英文正文补齐，真实响应验收通过

- 状态：`implemented_runtime_probes_passed_browser_pending`；仅本项英文正文 HTTP 验收通过，不代表汉服商城已可上线或多断点视觉已通过。
- 根因与实现：两份 Shipping 指南模板已有 `<lang>`，但 `i18n/en_US.csv` 中 76 项目录、正文与说明仍以中文回填。仅把这些英语值改为对应英文，保留中文来源键、八个原章节、目录锚点和既有政策。现货 1–3 个工作日、普通退换 7 日及适用条件、退款 3–15 个工作日等原期限和限定均未改变；未改模板、样式、路由、控制器或商业承诺。
- 最小回归：新增 `test/Unit/View/GuideEnglishTranslationTest.php`，使用真实 TranslationResolver 与模块 CSV；6 个用例先实际失败。修复后连同既有 Theme 服务页/新闻布局用例，最新运行 `10 tests / 20 assertions` 通过；新增 PHP 文件 lint 通过。
- 真实响应：新增只读 `test/Runtime/GuideEnglishProbe.php`。英语配送/退换原先各 6/8（正文仍中文、英文政策关键句缺失）；最终相同 HTTPS HTTP/1.1 地址各 8/8，通过正文无中文、原章节及目录链接保留、英文关联链接和原关键政策期限检查。此探针不是 Browser 视觉或交互替代品。
- 词典收集：正式 `php bin/w i18n:collect Weline_Shipping` 返回成功；收集后两指南内 78 条中文源句的中文值仍保留，两份模板哈希未变。收集会整理词典，不能声称整个 `zh_Hans_CN.csv` 字节未变，也不回退其他任务已有改动。
- 生效过程：收集后 Worker 尚未更新，真实页面仍各 6/8；本任务发出的 `ctrl_op_8 cache_clear` 失败，`expected=2/acked=0`，失败池包含 router/view/phrase。之后只读状态观察到另一项 `ctrl_op_12 reload_wait` 已完成，随后的真实页面才各 8/8。本任务没有发起该重载，不能把它或“清理已入队”记成缓存缺陷修复。
- 待完成：指南 Browser 视觉检查；商城编辑器按范围/语言保存发布、多断点整体视觉和真实购物结账验收。缓存清理与连接故障继续保持开放，见 Theme 本阶段记录。
- 交付地址：[英文配送说明](https://p05113ef3.weline.test:9555/en_US/guide/shipping)；[英文退换说明](https://p05113ef3.weline.test:9555/en_US/guide/returns)。无新增后台或 API；新增 CLI 仅为只读验收探针。

## Hotfix — 2026-09-05：禁运区域改为搜索多选地址选择器（2.4.11）

- 问题：网站/店/渠道「运输禁运区域」用手填类型/国家码/地区 ID，与可售目的地体验不一致。
- 变更：`embargo-form-section` 复用 Theme `address.js` multi（搜索下拉 + chips + 级联）；隐藏域仍写 `extensions[shipping][embargo][json]`；`EmbargoAdminService::listForScope` 补可读 label。
- 版本：`2.4.10` → `2.4.11`

## Hotfix — 2026-09-05：可售目的地下拉改接 Weline.UI 浮层（2.4.10）

- 问题：可售/覆盖搜索下拉曾自研定位，未吃 Theme 浮层 flip/portal。
- 变更：跟随 Theme `address.js` multi → `UI.floating.attach`；cache `v=20260905-multi-float1`。
- 版本：`2.4.9` → `2.4.10`

## Hotfix — 2026-09-05：可售目的地选择器跟随主题背景（2.4.9）

- 变更：跟随 Theme `address.js` multi 主题变量（surface/text/border）；cache `v=20260905-multi-theme1`。
- 版本：`2.4.8` → `2.4.9`

## Hotfix — 2026-09-05：非字段错误旁置保存按钮 + 打开换验证码（2.4.8）

- 现象：验证码失败等非字段错误顶在编辑区上方；再次打开编辑器仍是旧验证码。
- 变更：`data-shipping-message` 移入 action-row，与「使用此地址」同行；打开 edit/new 时 `refreshGuestCaptchaOnOpen`；`v20260917.js`；CSS `csa11`。
- 配合 Captcha `1.0.7`：lazy 在「曾隐藏→可见」时自动换新挑战。
- 版本：`2.4.7` → `2.4.8`

## Hotfix — 2026-09-05：编辑地址保存后地址卡空白（2.4.7）

- 现象：结账编辑地址提交后只剩标题/账单勾选，图2 选中地址卡消失。
- 根因：收起态隐藏未选中卡；首存无 saved shell 时 `renderSavedAddresses` 直接 return；地址 `id` 可能被空 `id` 字段盖住 `delivery_address_id`；线上加载版本化 JS。
- 变更：始终输出 saved shell；`ensureSavedShell` + `synthesizeAddressFromContext`；保存后强制选中可见卡；`v20260915.js`；CSS `v=20260905-csa10`；改版本后需 `static:compile` 刷新 Frontend `base/weline.modules.js`。
- 配合 Checkout `1.4.42`：`address-updated` 软刷且 `hardOnFailure: false`。
- 版本：`2.4.6` → `2.4.7`

## Hotfix — 2026-09-05：可售目的地下拉视口翻转（2.4.6）

- 问题：网站/店/渠道「可售目的地」与承运商覆盖搜索下拉固定向下，底部被裁且无向上展开。
- 变更：跟随 Theme `address.js` multi 视口边界检测 + fixed/向上展开；cache `v=20260905-multi-flip1`。
- 版本：`2.4.5` → `2.4.6`

## Hotfix — 2026-09-05：结账地址英文词条补齐（2.4.5）

- 问题：英文结账页「使用此地址 / 账单地址 / 账单地址与收货地址相同 / 使用新地址 / 选择收货地址」等仍为中文。
- 变更：`en_US.csv` 补齐结账地址部件与校验提示英文翻译。
- 版本：`2.4.4` → `2.4.5`

## Hotfix — 2026-09-05：可售目的地搜索下拉级联（2.4.4）

- 问题：选项铺满成墙；已选省份后区县仍提示先选省；多国省份混在一起难筛。
- 变更：自带下拉样式（默认收起）；省份仅来自已选国家；区县从已选省加载（直属 district/county 或 city→district）；cache `v=20260905-multi-cascade1`。
- 版本：`2.4.3` → `2.4.4`

## Hotfix — 2026-09-05：可售目的地改为搜索多选地址选择器（2.4.3）

- 问题：后台「可售目的地/支持范围」用长复选框列表，无法搜索，交互不符合地址选择器习惯。
- 变更：复用 Theme `address.js` multi 的搜索输入 + 下拉选项 + chips 多选；cache `v=20260905-multi-search3`。
- 版本：`2.4.2` → `2.4.3`

## Hotfix — 2026-09-05：后台覆盖/可售区真实可配（2.4.2）

- 问题：后台「支持范围/可售目的地」无法勾选；默认区域展示 raw JSON；此前验收曾直写库绕过 UI。
- 根因：
  1. multi 国家误用 `mode=children` 空参 → API 空；
  2. 后台 Taglib 把地区 API 编成 `/{admin}/shipping/frontend/...` → 404；
  3. `Weline.declare` 在后台可能不落地 address.js；`address.js` 尾部曾误入字面量 `\\n` 致语法错误；
  4. Website/Store/Channel hook 用 `include` 拉 partial 时 `<w:theme:address>` 不会编译，原样输出。
- 变更：国家走 `loadRegions(catalog)` + 前台 URL；后台页直挂 `address.js` + onload boot；destination partial 改为手写 address markup；默认/已保存改为徽章文案；cache `v=20260905-multi-cov5`。
- 真实验收：承运商编辑勾选「中国+四川省」保存后库内含 country+province；网站「可售目的地」展开后国家列表 249 项可勾选。
- 版本：`2.4.1` → `2.4.2`

## Hotfix — 2026-09-05：结账地址块内间距对齐配送方式（2.4.1）

- 现象：收货地址标题与灰底编辑区偏紧，与配送方式选项卡内边距节奏不一致。
- 变更：section `gap`→`1rem`；editor `gap/padding`→`1rem`/`1.25rem`；CSS `v=20260905-csa8`。
- 配合 Checkout `1.4.40` slot `margin-top:16px`。
- 版本：`2.4.0` → `2.4.1`

## Hotfix — 2026-09-05：承运商覆盖为主匹配 + 可售目的地（2.4.0）

- 表：`w_shipping_carrier_regions`、`w_shipping_destination_regions`。
- 匹配：`CarrierCoverageMatchService`（禁运 → 可售白名单可空 → 承运商覆盖祖先命中 → `carrier.sort_order` → Service）；`getAvailableServices` 主路径不再依赖空 `zone_regions`。
- Provider：`CarrierCoverageProviderInterface` + `shipping.carrier_coverage.default`（默认 CN）；启用承运商必须有覆盖。
- 后台：承运商编辑支持范围（`theme:address selection=multi`）；Website/Store/Channel 可售目的地区块与禁运并列。
- Theme：`selection=multi` / `multi-levels`；cache `v=20260905-multi-cov`。
- 版本：`2.3.8` → `2.4.0`

## Hotfix — 2026-09-05：邮编国家菜单持久置顶联动（2.3.8）

- 结账：`checkout-shipping-address.v20260914.js` 在邮编&lt;3 时 `clearPostalCountryPrompt`；单国命中 `setPostalCountryPin`；多国仍走 `promptPostalCountries`。
- 依赖 Theme `address.js` 置顶分组（cache `v=20260905-postal-pin`）。
- 版本：`2.3.7` → `2.3.8`

## Hotfix — 2026-09-04：网站/店铺/渠道禁运区域（2.3.7）

- 表：`w_shipping_embargo_regions`（`EmbargoRegion`）；Admin/Runtime：`EmbargoAdminService` / `EmbargoService`。
- 后台：Website/Store/Channel `sections-after` 注入；`website_save_after`/`store_save_after`/`channel_save_after` 落库。
- 命中：website ∪ store ∪ channel 并集；与 Zone「可运」语义独立。
- 前台：`region` `embargo_evaluate`/`embargo_countries`；`address.js` 打标；结账 + Delivery/ShippingAddress `assertAllowed`。
- 依赖：`requires Weline_Websites`；资源 `checkout-shipping-address.v20260914.js`。
- 版本：`2.3.6` → `2.3.7`

## Hotfix — 2026-09-04：结账地址默认单卡 + 更换时懒加载（2.3.4）

- UX：有多条已存地址时 SSR 只渲染已选卡；点「更换地址」后 `getDeliveryContext` 懒加载完整列表（`picking`），选中或收起后回到 `collapsed`。
- 资源：`checkout-shipping-address.v20260912.js` + CSS `v=20260904-csa7`；改版本后需 `static:compile` 刷新 Frontend `base/weline.modules.js` 合并路径。
- 版本：`2.3.3` → `2.3.4`

## Hotfix — 2026-09-04：NZ 邮编目录 + 1010 多国歧义（2.3.6）

- 数据：`regions/NZ.json` + `address-catalog/NZ`（GeoNames）；`shipping:addresscatalog:import --country=NZ`。
- 行为：`1010` → AU/NZ 多国待选；选 NZ 回填 Auckland。
- 资源：`checkout-shipping-address.v20260913.js`
- 版本：`2.3.5` → `2.3.6`

## Hotfix — 2026-09-04：postalLookup 补可读名（2.3.5）

- `postalLookup` 按 region_id 解析 `province_name`/`city_name`/`district_name`；避免前端拿 `US-C*` 代码当城市名（例：10112→New York City）。
- 版本：`2.3.4` → `2.3.5`

## Hotfix — 2026-09-04：邮编反查级联 loading（2.3.3）

- 结账：`checkout-shipping-address.v20260911.js` 在邮编 debounce 与反查期间调用 `setCascadeLoading`，回填完成/多国待选/短邮编后清除。
- 版本：`2.3.2` → `2.3.3`

- 邮编多国：清空固定国展示；`supported`（I18n 已安装启用）可选手，不支持仅展示禁用；选完再回填。
- 邮编跨国家歧义：`postal_countries` + 结账展开国家待选，选完再 `postal_lookup` 回填级联（例：10115→DE/KR/US）。
## Hotfix — 2026-09-04：CN 邮编库补齐（610500→新都区）

- 根因：`postal.tsv` 仅 2 条样本，`postalLookup(CN,610500)` 空结果，区县无法回填。
- 构建：`scripts/build-cn-postal-from-adcode-zip.php`（tombcato/china-zipcode-data adcode↔zip）→ `CN/postal.tsv.gz` 2856 行。
- 导入：`shipping:addresscatalog:import --country=CN --layer=postal`；验收 `610500` → 四川省/成都市/新都区。

## Hotfix — 2026-09-04：全球地址库分层固化（tsv.gz → DB）

- Schema：`Region.region_code` varchar(96)+UK；新增 `PostalPlace` / `Street`；地址表 `street_id`/`street`；模块 `2.3.0`（Model Col + bump + setup:upgrade）。
- 灌数：`shipping:addresscatalog:import [--wipe]`；`data/address-catalog/**/*.tsv.gz`；首导开启 `catalog_mode`，Ensure JSON 写路径 no-op。
- Query：`region.postal_lookup` / `has_streets` / `streets`；`children` 带 limit。
- UI：`<w:theme:address>` 支持 street（有数据可选、无数据手填）；结账邮编优先 debounce 反查；`address1` 作门牌明细；账单仍镜像不二次级联。
- 注意：wipe 会清 `zone_regions` 与地址 region/street id，导入后需重配配送区。

## Hotfix — 2026-09-04：G20 地区包补齐（区县能补则补）

- 数据：除 CN（仍 modood）外，G20 18 国写入 `data/regions/{CC}.json`（`dr5hn-csc-sqlite-v1`）；`FR`/`GB`/`IT` 映射嵌套行政为 province→city→district；其余到 city。
- 街道：无开源全球街道级联 → 仍用 `address1`；profiles 对有三级的国家打开 district。
- 脚本：`scripts/build-g20-region-packs.php`；清单 `data/regions/G20-PACK-MANIFEST.json`。
- 标签：`<w:theme:address>` 已接本地 suggest + country profile（此前已落地）；Taglib 文档补本地包说明。

## Hotfix — 2026-09-04：匿名结账始终可「使用此地址」

- 变更：无已存地址的访客也渲染「使用此地址」+ 懒加载验证码；配合 Checkout 访客 `guest_*` id 修复后卡片可出现。
- 版本：`2.2.12` → `2.2.13`。
- 交付地址：`/checkout`

## Hotfix — 2026-09-04：本地地址自动完成 + CN 完整区县包

- 策略：按国字段深度（`address-country-profiles.json`）+ 本地 `region.suggest`/`format_suggestion`；数据仅 `w_shipping_regions` / `data/regions`，不接外网 Places。
- 数据：`CN.json` 换为 modood `pca-code` 完整省市区（含成都锦江区等）；`pack_version` 升级时停用旧子树重导入。
- 前端：`Theme address.js` 输入时合并本地 suggest 回填；锁定国家后按 profile 显隐 district。
- 验收：单元契约（CN 包/profile/suggest 声明）；结账选中国可搜区县。

## Hotfix — 2026-09-03：结账地址字段级校验提示

- 现象：保存地址失败时在邮编下方展示 `Exception: … in /path/AddressValidationService.php`；未定位到电话字段。
- 变更：`AddressValidationException` + `field_errors`；输入框下方字段级红字；自动滚动/聚焦首个错误；DEV 消息剥离路径泄漏；多字段同时报错；全局提示移到编辑器顶部。
- 版本：`2.2.10` → `2.2.12`。
- 交付地址：`/checkout`

## Hotfix — 2026-09-03：结账地址「使用此地址」保存并刷新卡片

- 现象：编辑/新地址后点「使用此地址」仅收起表单，卡片与 session 未更新，看起来像没反应；匿名保存还被验证码拦截。
- 变更：`commitEditedAddress` 调 `saveDeliveryAddress` 并重绘地址卡；登录用户跳过验证码；匿名懒加载验证码；`weline:checkout:address-updated` 触发运费重算。
- 版本：`2.2.9` → `2.2.10`（Checkout `1.4.23` → `1.4.24`）。
- 交付地址：`/checkout`

## Hotfix — 2026-09-03：唯一地址自动设为默认

- 当前状态：`implemented`
- 问题：个人中心仅有一条收货地址时仍显示「设为默认」，`is_default=0`。
- 修复：`DeliveryAddressService` / `ShippingAddressService` 在 create 首条、delete 后剩一条、以及 list 惰性修复时强制唯一地址为默认。

## Hotfix — 2026-09-03：个人中心地址与顶部配送语义对齐

- 当前状态：`implemented`
- 问题：顶部「配送至」已有已登录地址，个人中心默认看到「发货地址」空状态，用户误以为地址未保存。
- 根因：顶部配送 = `DeliveryAddress`（收货）；个人中心「发货地址」= `ShippingAddress`（店铺寄出），入口顺序把发货放在前面。
- 修复：账户菜单/侧栏/内容区将「收货地址」置前；发货空状态提示并链到收货地址；文案标明与顶部配送同源。

## Hotfix — 2026-09-03：结账地址分段 + Amazon 卡（收起/单选/编辑/新增 + 账单同配送）

- 当前状态：`implemented`
- 交互：有已存地址时默认 `collapsed` 收起表单；地址卡 radio 单选；支持编辑/使用新地址；账单默认「与收货地址相同」，取消勾选后展开独立账单字段。
- 布局/样式：分段标题 + Amazon 选中橙边卡片 / 链接色编辑；沿用 checkout Amazon token。
- 后端：`freezeQuote` 可带 `billing_address` 写入 session；提交 options 附带 billing。
- 版本：`2.2.7` → `2.2.8`。

## Hotfix — 2026-09-03：结账收货地址槽因模块槽不可发现而未注入

- 当前状态：`implemented`（归属 Checkout 槽目录 + Theme 注入回填；Shipping 部件声明本身无改）
- 问题：`checkout-shipping-address` required 默认注入从未写入主题账本，结账页收货区空白。
- 根因：目标槽在 Checkout 模块页，不在 Theme 布局/组件槽目录 → `layout_slot_missing`。
- 修复协作：Checkout `checkout-storefront-slots` 声明嵌套槽后，本部件 `default_injections` 可被发现并安装。

## Hotfix — 2026-09-03：指南页改 Theme guide 壳 + 亚马逊面板（2.2.9）

- 确认：用户要求 `/guide/returns` 等按主题宽度、亚马逊风格，并用页面包裹器；避免贴页头页脚。
- 变更：Guide 控制器 `layoutType=guide.default`；正文改为 `amazon-doc` 面板；Theme 提供 guide 全页壳。
- 版本：`2.2.8` → `2.2.9`。

## Hotfix — 2026-09-03：帮助中心配送/退换指南正文充实

- 当前状态：`implemented`
- 变更：`guide/shipping`、`guide/returns` 由摘要扩为完整政策章节（发货/运费/签收/异常；退换条件/流程/到账/尺码等），并链到退款政策与帮助中心；契约测试断言关键章节与正文长度。
- 版本：`2.2.6` → `2.2.7`。

## Hotfix — 2026-09-02：header-account-links 固定访客默认 hidden

- 当前状态：`implemented`
- 变更：不再读 Session；发货/收货链接 SSR 一律 `hidden`，由 account JS 升迁。
- 版本：`2.2.5` → `2.2.6`。

## Hotfix — 2026-09-01：页头账户地址链接支持 soft-reconcile

- 当前状态：`implemented`
- 变更：`header-account-links` 始终输出发货/收货入口并标 `data-account-menu-auth="signed-in"`，游客 SSR 时 `hidden`，由 Frontend 切态后显示。
- 版本：`2.2.4` → `2.2.5`。

## Hotfix — 2026-08-31：页脚帮助中心注入配送说明/退换政策

- 当前状态：`implemented`
- 变更：新增前台 `guide/shipping`、`guide/returns` 页面与 `Controller/Router` 公共别名；`footer-shipping-info-link` / `footer-returns-policy-link` 经 `default_injections` 注入 Theme `footer-help-links`。
- 版本：`2.2.2` → `2.2.3`。

## 2026-08-30 — 前台 JS 改 modules + data-weline-load（第二波）

- 当前状态：`implemented`
- 变更：登记 `view/statics/frontend/weline.modules.js`；部件/布局改 `data-weline-load`，删除 `@static`/`<js>` 直引。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。

# Weline_Shipping 开发日志

## 2026-08-28｜结账收货地址默认部件注入（theme:address + 已存地址选择）

- 需求：结账页账户/收货地址改用地址选择标签；由 Shipping 用部件默认注入结账槽，禁止 Checkout 裸拼地区 input。
- 扩展点：Widget + Slot（对齐 Marketing `checkout-coupon` → `checkout-summary-discount`）。
- 实现：`checkout-shipping-address` 部件 + `default_injections` → `checkout-shipping-address`；模板含已存地址标签与 `<w:theme:address code="checkout-shipping-address">`。
- 契约：`CheckoutShippingAddressWidgetContractTest`；View warmup 收录模板与静态资源。
- 验收：`/checkout` 收货区可见级联地址与可选已存地址；表单字段兼容 Checkout `formAddress`。

## 2026-08-25｜Header 配送 quick-add 触发 theme:address 运行时泄漏（分类页崩坏）

- 现象：店面分类页主内容消失；Header 配送「快速添加地址」区域泄漏 Taglib PHP 源码。
- 根因：`view/hooks/.../checkout-delivery-context/quick-add.phtml` 中 `<w:theme:address country="<?= $esc($countryCode) ?>">` 走 `renderRuntimeTag`，而 `Weline_Theme` Address 标签缺少 `runtimeCallback`（编译期 PHP 被当 HTML 输出）。
- 修复归属：`Weline_Theme/Taglib/Address.php` 补齐运行期回调；本模块 hook 模板契约不变（仍用 `levels="province,city,district"`）。
- 验收：分类页无 `$Taglib__`/`Weline_Taglib_resolve` 泄漏；quick-add 仍渲染 `data-w-address`。

## 2026-08-17｜个人中心地址侧栏修复

- 需求：`SHIP-ADDR-ACCOUNT-001`。
- 现象：个人中心懒加载地址区域时返回 HTTP 500，运行日志显示编译模板在静态闭包中使用 `$this`。
- 根因：地址卡片和地址表单的源模板渲染闭包声明为 `static`，而闭包内的 Weline 模板标签编译后需要模板对象上下文。
- 实现：移除两个渲染闭包的 `static` 声明，保留纯数据辅助闭包的静态属性；不修改 `view/tpl` 生成文件。
- 回归：新增模板契约测试，防止包含模板标签的两个闭包再次改为静态闭包。
- 数据与版本：无 schema 变化，不提升 `setup_version`。
- 验收状态：PHP 语法检查通过；聚焦测试 1 个、7 个断言通过；WLS 热重载成功；浏览器分别验证发货地址与收货地址空状态可见，页面无提示框，干净标签页前端错误/警告日志为空。

## 2026-09-03｜配送与退换货指南纳入可视化区块编辑

- 需求：满足主题区块语义标识硬规则，使配送、退换货指南的内容分区可被可视化主题编辑器稳定识别。
- 实现：为 `shipping-s1`～`shipping-s8`、`returns-s1`～`returns-s8` 增加稳定且唯一的 `weline-code`；不改变布局、样式、文案和路由。
- 验收：`frontend:check-section-code`、主题布局部件检查、聚焦契约测试与真实页面探测。
