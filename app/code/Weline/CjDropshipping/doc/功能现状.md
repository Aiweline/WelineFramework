# 功能现状 · Weline_CjDropshipping

| 能力 | 状态 |
|------|------|
| `CjProvider` code=`cj` | 已实现 |
| `CjApiClient` token/probe/GET/POST | 已实现 |
| SystemConfig backend `cj.phtml` | 已实现 |
| 订单沙盒 `dropship/channel/cj/order_sandbox` → createOrderV3 `isSandbox=1` + confirmOrder + simulatePay/运单/状态推进；商家物流强制 CN 起点 + QPS 节流 | 已实现（`1.0.17`） |
| cancel | `skipped:unsupported`（计划允许） |
| 选品浏览 `listCategories` / `searchProducts` | 已实现；商品名跟随管理语言（`/product/list` 中英字段）；分类名 zh* 本地映射覆盖（CJ OpenAPI 仅英文） |
| `getProduct` → Snapshot | 已实现；gallery/variants + description/attributes/category_path（`1.0.13`） |
