# 功能现状 · Weline_CjDropshipping

| 能力 | 状态 |
|------|------|
| `CjProvider` code=`cj` | 已实现 |
| `CjApiClient` token/probe/GET/POST | 已实现 |
| SystemConfig backend `cj.phtml` | 已实现 |
| 订单沙盒 `dropship/channel/cj/order_sandbox` → createOrderV3 `isSandbox=1` + confirmOrder + simulatePay/运单/状态推进；商家物流强制 CN 起点 + QPS 节流 | 已实现（`1.0.17`） |
| 官方 ORDER webhook `params.cjOrderId/orderStatus` 解析 + 空运单不覆盖 | 已实现（`1.0.20`） |
| 官方多 topic webhook → 壳标准 DTO + capability | 已实现（`1.0.21`） |
| cancel | `skipped:unsupported`（计划允许） |
| 选品浏览 `listCategories` / `searchProducts` | 已实现；商品名跟随管理语言（`/product/list` 中英字段）；分类名 zh* 本地映射覆盖（CJ OpenAPI 仅英文） |
| `getProduct` → Snapshot | 已实现；gallery/variants + description/attributes/category_path（`1.0.13`） |

## Webhook 能力矩阵（壳已接 / 沙盒真推 / 沙盒不可用）

> **壳子能力一律保留**（`webhook_*`=true）。下表只描述 **CJ 沙盒账号实测边界**，不是拆 capability。

| CJ `type` / capability | 壳已接 | 沙盒真推 | 备注 |
|------------------------|--------|----------|------|
| `ORDER` / `webhook_order` | ✅ | ✅ 已实测 | 沙盒建单推进后会推 IN_CART→…→DELIVERED |
| `ORDERSPLIT` / `webhook_order` | ✅ | ⚠️ 未沙盒实推 | 解析+投影保留 |
| `LOGISTIC` / `webhook_logistics` | ✅ | ⚠️ 未沙盒实推 | 解析+履约运单保留 |
| `MAKEUP` / `webhook_makeup` | ✅ | ⚠️ 未沙盒实推 | 官方信封注入已通 |
| `PRODUCT`/`VARIANT` / `webhook_product` | ✅ | ⚠️ 未沙盒实推 | 目录跟随入队 |
| `STOCK` / `webhook_stock` | ✅ | ⚠️ 未沙盒实推 | 目录跟随入队 |
| `PRIVATE_ORDER` / `webhook_private_order` | ✅ | ⚠️ 未沙盒实推 | 解析保留 |
| `DISPUTE(S)` / `webhook_dispute` | ✅ | ❌ **沙盒不可用** | `disputes/create` 返回 `The Sandbox account is not supported for this feature.`（code 801）；壳仍保留解析/投影，供正式账号真推 |

权威入口：`dropship/frontend/callback/notify?endpoint_code=cj.{sandbox|prod}.default`。
