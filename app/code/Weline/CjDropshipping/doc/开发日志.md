# 开发日志 · Weline_CjDropshipping

## 2026-09-12 — create Order exist 幂等恢复（1.0.18）

- 加：`isDuplicateCreateMessage`；`createFulfillment` 遇「Order exist / do not duplicate create」时从本地 `DropshipFulfillment` 或 `getOrderDetail(orderNumber)` 恢复，返回 `ok` + `recovered_existing`。
- 验：契约 UT；与 Dropship Outbox dead 回收联动。
- 版本：`1.0.17` → `1.0.18`。

## 2026-09-12 — 商家物流 fromCountry=CN + 沙盒 QPS（1.0.17）

- 修：短 areaId 降级商家物流时强制 `fromCountryCode=CN`（避免仓映射 US 导致 confirm Logistic not found / 美仓库存不足）。
- 修：沙盒推进步骤间 sleep + QPS(1600200) 一次重试，避免卡在末态 updateStatus。
- UT：`testBuildCreateOrderPayloadForcesCnFromCountryOnSellerLogistics` / `testIsCjRateLimitedDetectsQps`。
- 版本：`1.0.16` → `1.0.17`。

## 2026-09-12 — 沙盒履约：vid 解析 + confirmOrder（1.0.16）

- 修：listing 货号 SKU 自动解析 CJ `vid`；有 vid 时不传冲突 sku。
- 修：沙盒推进先 `PATCH confirmOrder`（CREATED→UNPAID）再 `simulatePay`/运单/状态。
- 改：`OrderPaidObserver` 透传 `external_spu`；create 补 `platform=Api`。
- 修：短 areaId 不当 storageId（避免 5027）；仅 UUID/长 hex 走平台物流。
- 版本：`1.0.15` → `1.0.16`。

## 2026-09-12 — 沙盒履约自动推进（1.0.15）

- 改：`createOrderV3` 补齐 `shippingCountry`/`logisticName`；无仓用 `shopLogisticsType=2`；UUID 行识为 `vid`。
- 增：沙盒创建成功后自动 `simulatePay` → `updateTrackNumber` → `updateStatus` 400/500/600，履约回写运单与 `shipped`。
- UT：payload / sandboxAdvanceSteps。
- 版本：`1.0.14` → `1.0.15`。

## 2026-09-11 — 订单沙盒开关（1.0.14）

- 增：配置 `dropship/channel/cj/order_sandbox`（默认关）；开启后 `createFulfillment` → `createOrderV3` 传 `isSandbox=1`。
- 增：`CjApiClient::isOrderSandboxEnabled`；`CjProvider::buildCreateOrderPayload` 可测组装。
- UT：`testBuildCreateOrderPayloadSetsIsSandboxWhenEnabled` / `testConfigSchemaDeclaresOrderSandboxField`。
- E2E：`Weline_CjDropshipping-order-sandbox-config.spec.js`（CK-CJ-ORDER-SANDBOX-001）。
- 版本：`1.0.13` → `1.0.14`。

## 2026-09-11 — Snapshot 组装描述/属性/分类路径（1.0.13）

- 改：`mapProductDetail` 写入 `description`、`attributes`（`source_platform`/`source_public_specs`）、层级 `buildCategoryPath`；壳只消费一等字段。
- 版本：`1.0.12` → `1.0.13`。

## 2026-09-11 — 选品标题中文化 + 原价货币（1.0.12）

- 改：`pickLocalizedTitle` 按 CJK 择优；US 仓英文字段时优先完整 `productNameEn`，再批量 `CjProductTitleLocalizer`（缓存 + TranslationService/MyMemory 软降级）。
- 改：`origin_currency` 映射（缺省 USD）；壳 Listing SSR/JS `money()` 展示 `USD 20.68`。
- 审图：管理语言中文但标题英/原价无币种 → fail → 已修。
- 版本：`1.0.11` → `1.0.12`。

## 2026-09-11 — 选品跟随管理语言（1.0.11）

- 改：`searchProducts` 改走 `GET /product/list`（含 `productName` 中文 + `productNameEn`）；关键词按 locale 写入对应字段。
- 增：`CjCategoryLocalizer` + `etc/data/cj_category_zh.php`——CJ `getCategory` 仅英文，管理端 zh* 时用本地映射覆盖分类名/路径。
- 审图：货源选品分类栏英文名相对中文壳 → fail（内容语言）→ 已修；本机维护模式阻断 Browser 实页验收，PHP 直连 CJ 已验中文分类/商品名。
- 版本：`1.0.10` → `1.0.11`。

## 2026-09-11 — 缩略图 media + listV2 嵌套（1.0.10）

- 改：`extractProductRows` 展开 `content[].productList`；`mapProductRow` 写入 `media`（`bigImage`→thumb）；HTTP timeout 60s。
- 版本：`1.0.9` → `1.0.10`。

## 2026-09-11 — 选品语言（1.0.9）

- 改：`mapProductRow` / `pickLocalizedTitle` 按 BrowseQuery `locale` 优先中文 `productName` 或英文 `productNameEn`。
- 版本：`1.0.8` → `1.0.9`。

## 2026-09-11 — 选品器兼容（1.0.8）

- 增：`capabilities.browse` / `browse_country_filter`；分类节点补 `parent_id`；Snapshot 一等 `category_id`/`category_path`。
- 版本：`1.0.7` → `1.0.8`。

## 2026-09-11 — CJ 选品浏览（1.0.7）

- 增：`DropshipCatalogBrowseProviderInterface::listCategories` → `GET /product/getCategory` 展平三级分类。
- 改：`searchProducts` 支持无关键词按 `countryCode`/`categoryId`/`page` 浏览；API 错误上抛给壳展示。
- 版本：`1.0.6` → `1.0.7`。

## 2026-09-11 — CJ 目录映射加固（1.0.6）

- 改：`searchProducts` 经 `extractProductRows` / `mapProductRow` 解析 listV2，过滤无 pid/标题的空行，兼容多字段别名。
- 版本：`1.0.5` → `1.0.6`。

## 2026-09-11 — CJ 仓映射对接（1.0.5）

- 增：`CjWarehouseMapper` 解析 `globalWarehouseList`（优先 `storageId`，否则 `areaId`）。
- 改：`pullWarehouses` 失败抛可读异常；`listWarehouses` 支持 `country_code` 过滤；`createOrderV3` 仅在有映射仓时传 `storageId`。
- 版本：`1.0.4` → `1.0.5`。

## 2026-09-11

- 对齐万能壳 Webhook：`verifyWebhook` + `parseWebhook` 输出标准 `fulfillment`；探活码改为 `credentials_missing`/`probe_ok`/`token_empty`。版本 `1.0.2`→`1.0.3`。

## 2026-09-10

- 凭证弹窗嵌入：`config_center.group=dropship_channel_cj`；`api_key` 改为 `type=text`（便于 config:embed 编辑，非 sensitive_readonly）。版本 `1.0.1`→`1.0.2`。
- `getConfigSchema()[config_center]`：货源平台「配置凭证」深链到统一配置中心 CJ Email/API Key（非壳货源配置）。版本 `1.0.0`→`1.0.1`。
- 新建模块并注入 `CjProvider`；凭证键 `dropship/channel/cj/*`。
