<?php
/** @var \Weline\Framework\View\Template $this */

$max = 8;
$labels = [
    'title' => (string)__('商品对比'),
    // Phrase 无参会吃掉 %{n}；传回字面占位供 compare-page.js 客户端替换
    'meta' => (string)__('已选 %{1} / %{2} 件', ['%{1}', '%{2}']),
    'loading' => (string)__('加载对比商品…'),
    'empty' => (string)__('对比栏暂无商品'),
    'cta' => (string)__('去逛逛'),
    'attr' => (string)__('属性'),
    'image' => (string)__('图片'),
    'price' => (string)__('价格'),
    'rating' => (string)__('评分'),
    'sku' => (string)__('SKU'),
    'specs' => (string)__('规格参数'),
    'action' => (string)__('操作'),
    'remove' => (string)__('移除'),
    'failed' => (string)__('加载对比数据失败，请稍后重试'),
];
?>
<div class="storefront-compare-layout">
<section class="storefront-compare"
         data-compare-page
         data-weline-load="comparePage"
         data-compare-max="<?= (int)$max ?>"
         data-testid="storefront-compare-page"
         weline-code="compare.page.section_1">
    <script type="application/json" id="weline-compare-page-labels"><?= json_encode($labels, JSON_UNESCAPED_UNICODE | JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT) ?></script>
    <header class="storefront-compare__header">
        <h1 class="storefront-compare__title"><?= htmlspecialchars($labels['title'], ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?></h1>
        <p class="storefront-compare__meta" data-compare-page-meta><?= htmlspecialchars((string)__('已选 %{1} / %{2} 件', [0, $max]), ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?></p>
    </header>

    <div class="storefront-compare__loading" data-compare-page-loading>
        <p><?= htmlspecialchars($labels['loading'], ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?></p>
    </div>

    <div class="storefront-compare__empty" data-compare-page-empty hidden>
        <p><?= htmlspecialchars($labels['empty'], ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?></p>
        <a class="storefront-compare__cta" href="@url{'/'}"><?= htmlspecialchars($labels['cta'], ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?></a>
    </div>

    <div data-compare-page-content></div>
</section>
</div>

<style>
.storefront-compare-layout {
    box-sizing: border-box;
    width: 100vw;
    max-width: 100vw;
    margin-inline: calc(50% - 50vw);
    padding-block: var(--weline-space-2, 0.5rem) var(--weline-space-6, 1.5rem);
}
.storefront-compare {
    box-sizing: border-box;
    width: 100%;
    max-width: var(--weline-layout-content-max-width, var(--weline-theme-container-max, 1400px));
    margin-inline: auto;
    padding-inline: var(--weline-layout-content-padding-inline, 1.5rem);
    min-width: 0;
    color: var(--weline-theme-body-text, #0f1111);
}
.storefront-compare__title { margin: 0 0 .5rem; font-size: 1.75rem; }
.storefront-compare__meta { margin: 0 0 1.5rem; color: #565959; }
.storefront-compare__loading,
.storefront-compare__empty {
    padding: 3rem 1.5rem;
    border: 1px dashed #e7e7e7;
    border-radius: 12px;
    text-align: center;
    background: #f8fafc;
}
.storefront-compare__cta {
    display: inline-block;
    margin-top: 1rem;
    padding: .5rem 1rem;
    border-radius: 8px;
    background: var(--weline-theme-primary, #ff9900);
    color: #111;
    text-decoration: none;
    font-weight: 600;
}
.storefront-compare__table-wrap {
    width: 100%;
    max-width: 100%;
    overflow-x: auto;
    overscroll-behavior-x: contain;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: thin;
    scroll-snap-type: x proximity;
}
.storefront-compare__table {
    width: max(100%, calc(var(--compare-label-col-width, 11rem) + (var(--compare-product-cols, 1) * var(--compare-product-col-width, 13rem))));
    min-width: 100%;
    border-collapse: separate;
    border-spacing: 0;
    background: #fff;
    border: 1px solid #e7e7e7;
    border-radius: 12px;
    table-layout: fixed;
}
.storefront-compare__table th:first-child,
.storefront-compare__table td:first-child {
    width: var(--compare-label-col-width, 11rem);
    min-width: var(--compare-label-col-width, 11rem);
    max-width: var(--compare-label-col-width, 11rem);
    position: sticky;
    left: 0;
    z-index: 2;
    background: #fff;
    box-shadow: 1px 0 0 #f0f0f0;
}
.storefront-compare__table thead th:first-child {
    z-index: 3;
    background: #f8fafc;
}
.storefront-compare__table th:not(:first-child),
.storefront-compare__table td:not(:first-child) {
    min-width: var(--compare-product-col-width, 13rem);
    width: var(--compare-product-col-width, 13rem);
    scroll-snap-align: start;
}
.storefront-compare__table th,
.storefront-compare__table td {
    padding: .75rem 1rem;
    border-bottom: 1px solid #f0f0f0;
    vertical-align: top;
    text-align: left;
    font-size: .875rem;
    word-break: break-word;
}
.storefront-compare__table thead th a {
    color: inherit;
    text-decoration: none;
}
.storefront-compare__table thead th a:hover {
    color: var(--weline-theme-primary, #ff9900);
    text-decoration: underline;
}
.storefront-compare__table thead th {
    background: #f8fafc;
    font-weight: 700;
}
.storefront-compare__section th {
    background: #eef2ff;
    color: #3730a3;
    font-size: .8125rem;
    letter-spacing: .04em;
    text-transform: uppercase;
}
.storefront-compare__label--hit {
    color: var(--weline-theme-primary, #ff9900);
    font-weight: 700;
}
.storefront-compare__cell--hit {
    color: var(--weline-theme-primary, #ff9900);
    font-weight: 600;
}
.storefront-compare__cell--best-price {
    color: #b91c1c;
    font-weight: 700;
}
.storefront-compare__thumb {
    width: 96px;
    height: 96px;
    object-fit: cover;
    border-radius: 8px;
}
.storefront-compare__remove {
    border: 0;
    background: transparent;
    color: #007185;
    cursor: pointer;
    text-decoration: underline;
}
</style>
<?php // comparePage：由 section data-weline-load 声明，禁止 @static 直引 ?>
