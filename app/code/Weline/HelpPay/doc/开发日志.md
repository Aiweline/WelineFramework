# Weline_HelpPay 开发日志

## 2026-09-14 — 分享结果展示当前规格（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（「分享规格给朋友」只有链接/二维码，看不出规格）
- 线稿：标题/提示 → **当前规格摘要** → 链接 | 扫码
- 原型：header 下全宽规格块（品名 + 轴标签/值 + 数量/SKU），无规格则不占位
- 实现：`readCurrentSpecSummary` / `buildShareSpecHtml`；i18n `specSection`；CSS Token；cache-bust `20260914-share-spec1`；版本 `1.0.5`
- 规格：`doc/开发/spec/share-result-show-current-spec.md`

## 2026-09-14 — 代付页账单地址复用结账选/改/增（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（支付区「账单地址」单行 input）
- 线稿：支付方式 → 优惠提示 → **单行账单** → 确认付款
- 原型调整（选定）：改为结账同款地址部件（选/换/新地址）；会话隔离；隐藏「账单同收货」；确认付款前校验结构化账单，永不写回收货
- 规格：`doc/开发/spec/helppay-payer-billing-address.md`
- 实现：`Payer` SSR 部件；`payer.phtml` 挂载；`ensurePayerBillingMounted` / `confirmPayerBilling`；cache-bust `20260914-payer-billing1`
- Shipping：部件 `title` / `saved_heading` 真正驱动标题文案

## 2026-09-14 — PDP 购买区四钮竖排改主次布局（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（用户问「这些按钮就这么摆着么」——四枚胶囊纵向堆叠）
- 线稿：加购 / 结账 / 分享 / 快捷购买 同列竖排 → 找朋友代付
- 原型调整（选定）：主购买全宽竖排；分享+快捷购买 **2 列并排**；代付 quiet 仍通栏。禁止四钮同权竖排。
- 修复：`helppay-share.css` `:has(.product-native-detail__secondary)` 两列 grid（适配 `.widget-wrapper`）；cache-bust `20260914-pdp-actions-2up2`
- 验收：Browser 禁缓存目视 2+1 结构

## 2026-09-14 — 快捷购买弹窗内地址+物流+支付闭环（隔离万能结账）

- 需求纠偏：弹窗完成地址→物流→支付；禁止主路径 `location.assign(/q/)`；不得改写万能结账配送会话。
- 规格：`doc/开发/spec/helppay-quick-pay-popup-checkout.md`（并修订 inline / address-modal）。
- 实现：
  - `helppay-share.js`：三步 `address-pick` / `shipping-pick` / `payment`；物流 `listQuoteOptions` 展示 label；支付 `openPayPopup` 子窗。
  - `createQuickPay`：`shipping_locked` + `session_isolation`；金额含运费；service 写入 snapshot/meta（顶层仅白名单字段）。
  - `checkout-shipping-address.js`：`data-session-isolation` 跳过结账写接口。
- 验收：合约 UT PASS；Browser 禁缓存 PDP：美洲运费可读名 → 支付步应付 → 主窗口仍在 PDP。

## 2026-09-14 — 规格弹窗内代付层被遮（智能层级）

- 现象：加购 `dialog.showModal()` top layer 盖住挂在 body 上、`z-index:80` 的代付层
- 修：`revealDialog` 挂入当前打开的上级 `dialog`（Theme toast 同款）+ `Weline.UI.stack.apply` / 回退「上级峰值 +1」；`hideDialog` 还原 overflow 并移回 body
- cache-bust：`20260914-overlay-stack1`

## 2026-09-14 — PDP「分享给朋友 / 快捷购买」与加购结账不协调（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（购买区四钮：上两胶囊、下两矩形+全宽）
- 线稿：数量 → 加购(outline 胶囊) / 结账(实心胶囊) → **分享(矩形) / 快捷购买(矩形全宽)** → 找朋友代付(quiet)
- 原型调整（选定）：两钮改 `product-native-detail__secondary`——同族 `border-radius:999px`、内容宽、`justify-self:start`、主色描边浅底；禁止 `w-button--secondary` 圆角方言与 `w-helppay-cta__btn` 单钮全宽
- 修复：两 widget phtml；`helppay-share.css` Token 样式；cache-bust `20260914-pdp-secondary-pill1`；契约 UT 断言新 class
- 验收：Browser 禁缓存 PDP 目视四钮同族胶囊；主实心仍为「立即结账」

## 2026-09-14 — 规格弹窗「找朋友代付」原生按钮（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（规格选择/购买操作区「找朋友代付」呈 UA 原生灰钮）
- 线稿：数量 → 加购/结账/分享/快捷购买（主题钮）→ **找朋友代付（原生）** → 快捷支付
- 原型调整：对齐结账/购物车 quiet 文字链（下划线、无边框），禁止做成 secondary 实心钮
- 根因：
  1. `.w-helppay-cta__link` 未 `appearance:none`，WebKit 仍画原生按钮皮
  2. 列表「选择规格」弹窗 AJAX 注入后未二次 `Weline.load(helpPayShare)`，CSS/委托点击可能缺失
- 修复：
  - CSS：`appearance`/`-webkit-appearance: none` + 清边框阴影（`statics/css` 与 `frontend/css`）
  - JS：`boot` 改为 document 委托点击且可重入；导出 `ensureShareCss`
  - Cart `product-purchase-actions.js`：面板注入后 `loadInjectedAttributeModules` → `helpPayShare.boot`
  - cache-bust：`20260914-quiet-cta-appearance1` / `20260914-helppay-panel-load`
- 验收：契约 UT 11；Browser 禁缓存 PDP + 首页加购面板均见 quiet 下划线链、非原生钮

## 2026-09-14 — 快捷购买主路径改为页内结账（非分享）

- 需求纠偏：用户明确「快捷购买是立即弹窗、当前页结账，不是分享」。
- 规格：`doc/开发/spec/helppay-quick-pay-inline-checkout.md`
- 线稿：地址确认 →「正在进入付款」→ `/q/` 付款页；禁止结果态分享网格。
- 修复：`renderQuickCheckoutLaunch` + `launchQuickPayCheckout`；地址 CTA「确认并付款」；`/q/` 以立即支付为主、跨设备折叠次要。
- cache-bust：`20260914-quick-checkout1`

## 2026-09-14 — 快捷购买结果态误用「分享给朋友」文案（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（本人快捷购买标题 + 发给朋友提示矛盾）
- 需求对齐：`quick_pay_self` 产出本人付款短链（出链双形态保留）；**不是** selection_share / 代付分享语义。
- 线稿：标题「本人快捷购买」→ 副文案却「分享规格/发给朋友」→ 步骤末项「分享」。
- 原型调整：步骤 `地址|付款`（隐藏规则）；结果区用付款链接/扫码付款提示。
- 修复：`applyDialogFlow('quick')` + `renderShareResult(..., 'quick')` + i18n `quickHint/quickTip*`；cache-bust `20260914-quick-self1`。
- 验收：契约 UT；e2e 断言 flow=quick 且无「发给朋友」；Browser 禁缓存。


- 现象：PDP「快捷购买」地址确认后弹「无法读取商品金额，请刷新后重试」。
- 根因：`readAmountMinor` 只认 `data-price-minor` / 按钮 `data-amount-minor`；PDP 实际暴露 `data-catalog-price-minor`，且无 `data-offer-price-minor`。
- 修复：
  - PDP `product-info` SSR + `renderPrice` 写入/同步 `data-offer-price-minor`。
  - `helppay-share.js` 按 offer → reference → catalog → 价格文案回退读单价，再 × `product-qty`。
  - cache-bust `20260914-amount-fix`。
- 验收：HelpPay UT；e2e（可售汉服 PDP 确认后不得再出现金额错误）；Browser 禁缓存出链到分享结果。

## 2026-09-14 — 帮我付弹层完整地址编辑（续跑收口）

- 需求：结账/购物车/详情代付地址步与结账一样可编辑（选/改/增），禁止只读预览；PDP quiet「找朋友代付」。
- 实现（本会话续跑）：
  - 同步 `statics/css` ↔ `statics/frontend/css`；cache-bust `20260914-addr-edit2` + `resource:compile welineModules`。
  - e2e PDP 改用可用 slug（数字 `/product/558` 当前 404）。
  - 验收前关闭误开的维护模式。
- 既有实现：
  - `openFlow` / PDP 统一 `address-pick` + `ensureHelpPayAddressMounted`；`resolveQuoteAddress` → `createHelpPay`。
  - 结账双挂载备份/恢复；去掉只读 `help-pay-address-preview`。
- 规格：`doc/开发/spec/helppay-editable-address-modal.md`
- 验收：HelpPay/Checkout/Cart 契约 UT 12/12；e2e PDP help-pay / quick-pay / plan-suite PASS；Browser 禁缓存结账与 PDP 地址步见「编辑 / 使用新地址 / 更换地址」，无「尚未填写完整收货地址」误判。

## 2026-09-14 — 商品详情「找朋友代付」弱入口

- 需求：PDP 增加找朋友代付；入口不要做成显眼按钮。
- 实现：
  - 新 widget `product-help-pay`：quiet 文字链注入 `product-purchase-actions`（sort 50）；Product 槽 max 5→6。
  - JS：`placement=product` → 规则步 → `address-pick` 懒加载地址 → `createHelpPay`（带 line_summary / amount）。
  - Setup `1.0.3` 种子 product 布局；样式沿用 `w-helppay-cta--quiet`。
- 验收：契约 UT；e2e `product-help-pay.spec.js`；Browser 禁缓存验 quiet 链与规则弹层。
- 规格：`doc/开发/spec/helppay-product-help-pay.md`

## 2026-09-14 — 快捷购买弹窗懒加载结账地址（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（缺地址仅「无法完成操作 + 关闭」死胡同）。
- 需求：弹窗内填写/选择/更换确认地址，交互与结账一致；**禁止** PDP SSR 大块地址 DOM。
- 实现：
  - Checkout `renderDeliveryAddressWidget` 懒返回 `checkout-shipping-address` HTML（对齐 captcha 懒加载）。
  - Shipping `shippingCheckoutAddress.mount` 支持晚挂载。
  - HelpPay `helppay-share.js` 快捷购买先进 `address-pick`，确认后 `resolveQuoteAddress` → `createQuickPay`。
- 验收：契约 UT；e2e 断言地址宿主；PDP 无 `[data-shipping-checkout-address]` SSR。

## 2026-09-14 — 帮我付误判「无地址」（审图 error_shot）

- 现象：结账已选「纽约测试地址」，弹层仍写「尚未填写完整收货地址」。
- 根因：`readCheckoutAddressPreview` 只扫隐藏表单 `[name=…]`，忽略已选 `[data-address-card]` 的 `data-address-json`。
- 修复：优先读选中地址卡 JSON，再回落 shipping 表单字段；出链 payload 补齐省市区邮编。
- 验收：契约 UT；Browser 禁缓存点开代付见完整地址预览。

## 2026-09-14 — 帮我付弹层重设计（审图 ui_shot）

- 审图：规则步空白过大、标题无说明、规则链刺眼、主按钮左对齐无层次。
- 线稿/原型：步骤条（规则→地址→分享）+ 标题/导语 + 要点列表 + 弱规则链 + 同意勾选 + 全宽主 CTA。
- 改动：`ensureDialog` 结构化；`ShareModalI18n` 补弹层文案；结账/购物车 CTA 注入 `data-helppay-i18n`；CSS Token 仅。
- 验收：契约 UT；Browser 禁缓存点「找朋友代付」见新弹层。

## 2026-09-14 — 结账帮我付下移并弱化（审图 ui_shot）

- 审图：extras 胶囊 + 大号次级按钮夹在合计与提交之间，抢主 CTA。
- 线稿/原型：主路径只留「提交订单」；帮我付改为提交下方文字链「找朋友代付」。
- 改动：Checkout Slot 移出 `weline-checkout__extras`、放到 `data-submit` 之后；CTA 去 `w-button--secondary` / tab-label，改 `w-helppay-cta--quiet` + 主题 link Token。
- 验收：契约 UT；Browser 禁缓存见提交下弱链、extras 无「帮我付」页签。

## 2026-09-14 — 结账/购物车帮我付槽空（部件未注入）

- 现象：结账/购物车摘要旁看不到「帮我付」；Slot 在 HTML 里但为空。
- 根因：一期只登记 Slot + `default_injections`，无 Setup 写入主题布局；且 CTA 默认 `hidden`，extras 页签会跳过全隐藏槽。
- 修复：`Setup/Upgrade` `1.0.2` 对 `cart-summary-help-pay` / `checkout-summary-help-pay` 做 `initSlotDefaultInjections` + `publishLayout`；CTA 去掉初始 `hidden`，补 `data-mini-cart-tab-label`。
- 验收：契约 UT；结账/购物车 HTML 含 `data-testid="*-summary-help-pay"` 与「帮我付」按钮。

## 2026-09-14 — 分享规格弹层 JS 硬编码中文（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（英文店面弹层仍为「分享规格给朋友」等中文）。
- 根因：`helppay-share.js` 的 `renderShareResult` 写死中文；CSV 已有英译但 JS 路径未消费。
- 修复：CTA `data-helppay-i18n` 由 `ShareModalI18n::json()` 注入（禁止模板旁 `require`，避免编译进 `view/tpl` 丢文件）；JS `mergeShareI18n`/`t()` 渲染；cache-bust `20260914-share-i18n`；版本 `1.0.1`。
- 验收：弹层标题/分区/按钮/提示均为英文且无汉字。

## 2026-09-13 — PDP 英文站残留中文 CTA（审图 ui_shot / 翻译）

- 现象：`en_US` 商品页「分享给朋友」「快捷购买」仍显示中文（模块无 i18n CSV）。
- 修复：补齐 `HelpPay/i18n/{zh_Hans_CN,en_US}.csv`（含 CTA：Share with friends / Buy now）；顺带修 Shipping BOM 导致「运费以结算页为准」未命中、Payment `使用 %{1} 快捷支付`、Product 运费提示词条。
- 验收：`/en_US/product/558?offer=…` 可见英文 CTA；`php bin/w i18n:collect`。

## 2026-09-13 — 分享复制常驻小贴士（审图 ui_shot）

- 审图：底部全局「已复制」与按钮文案重复，且会消失；客户不知下一步。
- 线稿：链接列 / 二维码列各一条 tip，按钮下常驻，不自动清空。
- 文案（客户向）：链接「已复制，可以分发给朋友」；二维码「已复制，可直接粘贴图片到聊天里发送」。默认 tip 说明粘贴用途。
- 改动：去掉 `help-pay-copy-feedback`；`data-helppay-copy-tip`；`wireShareResult` 先绑链接复制再拉 QR；cache-bust `20260913-share-copy-tips2`。

## 2026-09-13 — 分享弹层复制反馈

- 审图 ui_shot：复制链接/二维码点击无反馈。
- 原型：按钮文案切「已复制」+ `data-tone=success`（约 2s 恢复）；`aria-live` 同步播报；失败显示「复制失败」。
- 改动：`flashCopyFeedback` + `help-pay-copy-feedback`；cache-bust `20260913-share-copy-feedback`。

## 2026-09-13 — 分享弹层美化（审图 ui_shot）

- 附图：分享规格结果弹层双列按钮错位、嵌套 panel 双边框、双主色 CTA 抢注意力。
- 线稿：标题行 → 等宽「链接分享 / 扫码分享」两卡 → 底对齐操作条。
- 原型：压平 dialog 内 panel；宽幅 42rem；主 CTA=复制链接，次 CTA=复制二维码；Token 间距/圆角。
- 改动：`helppay-share.css` + `renderShareResult` + `help-pay-share-result.phtml`；cache-bust `20260913-share-beautify`。

## 2026-09-13 — 分享给朋友 cart_type 参数白名单

- 根因：店面 JS 传 `cart_type`（一期 toc 门禁），但 `HelpPayQueryProvider` 未声明该入参，`FrontendQueryGateway` 拒为 `Unknown frontend worker param: cart_type`。
- 修复：`createHelpPay` / `createSelectionShare` / `createQuickPay` 补齐 `cart_type` 及 JS 已用可选入参；错误弹层对人话映射未知参数。
- 契约：扩展 `HelpPayQueryFrontendExposureContractTest`；e2e 复跑分享通路。

## 2026-09-13 — 快捷购买 frontend worker 暴露与空白弹层

- 根因：`HelpPayQueryProvider` 未声明 `frontend/external/mode`，店面 `Weline.Api.resource('helpPay')` 被 `FrontendQueryGateway` 拒为 `Operation is not exposed to frontend worker API`；JS 抛错后只打开空 result 步。
- 修复：出链操作补齐 frontend worker 白名单；`helppay-share.js` try/catch 展示可读错误态；缺货/批发态同步禁用「快捷购买」；去掉失败时的 demo 假链。
- 契约：`HelpPayQueryFrontendExposureContractTest`；e2e `product-quick-pay.spec.js`。

## 2026-09-11 — 快捷买短链过期修复

- 根因：`createQuickPay` 默认 TTL 仅 3600s，验收 token 过期后显示「链接不可用」。
- 默认 TTL 改为 7 天；`PaymentLinkService::load` 每次以文件仓为准，避免 Worker 内存缓存挡住磁盘复活。
- 已复活验收 token `1cc04c171a0cfb54a9b7e696b58de9fa`（30 天）；过期页文案可区分 `quick_pay_expired`。

## 2026-09-11 — 短链页 Theme 布局壳

- `/q` `/h` `/s` 控制器设置 `layoutType=default`，纳入 Theme 前台壳（header/footer/主题 CSS）。
- 去掉模板 `@static` 直拉；新增 `view/statics/frontend/weline.modules.js` 登记 `helpPayShare`。
- 快捷购买/代付/分享结果区改为 `w-helppay-panel` + `w-*` 商用结构；CSS 仅主题 Token。
- 契约测：`HelpPayFrontendLayoutContractTest`、Widget 禁止 `@static`。

## 2026-09-11 — 一期骨架

- 新模块 `Weline_HelpPay`：编排、短链路由、Slot UI、出链双形态、Query、文档。
- Payment 新增 `PaymentLinkServiceInterface` / `PaymentLink` 模型 / 单测。
- Cart/Checkout 登记 help-pay 槽，禁止写死 HelpPay widget。
- Faq 对客篇：`help-pay-rules` / `help-pay-privacy` / `help-pay-payer` / `selection-share`。
- 验收：PHPUnit 契约测；Browser 槽与帮助链接待环境探活。
