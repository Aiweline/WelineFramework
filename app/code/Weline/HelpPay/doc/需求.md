# Weline_HelpPay 需求

## 目标

落地三种店面能力（一期 toc only）：

| 模式 | 短链 | 归属 |
|---|---|---|
| 帮我付 `help_pay` | `/h/{token}` | 订单永远归发起人 A；B 只付款 |
| 纯分享 `selection_share` | `/s/{token}` | 朋友灌自己的车→自己结账下单 |
| 本人快捷买 `quick_pay_self` | `/q/{token}` | 本人付款 |

## 铁律

1. **Slot 解耦**：Cart/Checkout 只声明 `cart-summary-help-pay` / `checkout-summary-help-pay`；HelpPay 用 `default_injections` 注入；禁止模板写死按钮；placement 可分别关闭（部件 `enabled`）。
2. **收货隐私**：帮付单店面（代付页、订单主详情/待付/成功）`shipping_redacted`，发起人也不见；出链前弹窗内完整确认地址；之后改址=撤销重开。
3. **billing**：卡支付时代付人可填「付款账单地址」，永不写回 shipping。
4. **禁优惠**：代付人不可用券/积分等；仅允许物流导致运费变化。
5. **会话隔离**：登录 B 打开 `/h/` 只加载 A 账单，不碰 B 的车。
6. **合规**：弹层帮助中心规则链接 + 默认勾选已阅读；代付页付款/隐私说明链接。
7. **出链双形态**：所有可分享短链成功后：复制链接 + 复制二维码图片 + 页内双展示；QR 只含 URL。

## 模块边界

| 模块 | 职责 |
|---|---|
| HelpPay | 编排、代付/分享页、Slot widget、Query `helpPay`、QR |
| Payment | `PaymentLinkServiceInterface` + `weline_payment_link` |
| Cart/Checkout | 仅槽目录与模板 `<w:slot>` |
| Order | 帮付单店面投影交叉见 Order 需求 |
| Faq | `/faq/help-pay-*` 对客篇 |

## 错误码

- `helppay_rules_not_accepted`
- `helppay_address_not_confirmed`
- `helppay_shipping_incomplete`
- `helppay_toc_only`
- `helppay_selection_empty`
- `helppay_qr_url_invalid`

## 交叉引用

- Payment `doc/需求.md` → payment_link / shipping_locked
- Cart/Checkout `doc/需求.md` → help-pay Slot
- Order `doc/需求.md` → shipping_redacted
