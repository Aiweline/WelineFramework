# Weline_Cart 开发日志

## 2026-09-15 — 空态 sibling CTA 整句 i18n（半截中文）

- 审图：`web_ui` / `ui_shot`（印地语空态按钮「浏览खुदरा कार्ट 2件商品」混语）。
- 线稿：空标题 / `[浏览商品]` / `[浏览零售车 N件]`；问题在第二钮静态片硬编码。
- 根因：`index.phtml` / Theme `mini-cart-icon.js` 用 `'浏览'+label+' '+count+'件商品'` 拼接；仅 `label` 已译。
- 修：整句模板 `__('浏览%1 %2件商品')`（勿用 `%{n}`，会被 Phrase 吃掉）→ `siblingBrowseLabel` / `data-i18n-sibling-browse`；九语 CSV；契约更新；`mini-cart-icon.js?v=20260915-sibling-i18n2`。
- 验收：UT PASS；hi_IN 页 `siblingBrowse`/`data-i18n-sibling-browse`=`%1 देखें · %2 आइटम`；样例 `खुदरा कार्ट देखें · 2 आइटम`（无中文片）。
- 版本叙事：承接 1.3.71 空态 sibling。

## 2026-09-15 — cart_signals 遗弃事实面（1.3.71）

- Chapter 1：只读 `cart_signals` QueryProvider（`list_stale_carts` / `get_stale_cart`）。
- `AbandonedCartSignalService`：非空未过期车（工程 TTL）；**不**因无邮箱过滤；`ContinueCartUrlBuilder` → 绝对 `/cart`。
- DTO：`cart_key`（`customer:{id}` / `guest:{token}`）、`has_email`、`reachable`、`continue_cart_url`、`line_items`、`totals` 等。
- 规格：`doc/开发/spec/cart-abandon-signals.md`；契约：`AbandonedCartSignalContractTest`。
- 版本：`1.3.70` → `1.3.71`。

## 2026-09-14 — 非批发 Offer 加购 remap toc（1.3.69）

- 配合 B2B：新增 `CommerceCartOfferRoutingInterface`；`CartService::add` 快照后可选 remap；FE 零售-only 强制 toc 并切迷你车。
- 版本：`1.3.68` → `1.3.69`。

## 2026-09-13 — getCart 接受 coupon_code 供折扣预览（1.3.68）

- 问题：迷你车优惠券芯片有额，小计不扣；`discount_preview` 仅靠 Marketing 会话读码。
- 修：`getCart` 描述符增加可选 `coupon_code`；购物车页 `cart-updated` 强制刷新并乐观合并预览；明细无 items 时兜底一行。
- 版本：`1.3.67` → `1.3.68`。

## 2026-09-11 — tob 行价按展示币换算（1.3.67）

- 现象：批发信用异币抵扣时，无批发价行的 tob 车可能仍用快照币数字却标展示币，造成按数字 1:1。
- 修：`presentLine` 对 tob 在非 preserveWholesale 时也尝试 `convertPriceMinor` 到展示币。
- 版本：`1.3.66` → `1.3.67`。

## 2026-09-10 — DEV 控制台打印收到的生命周期事件（1.3.65）

- `cart.js` 监听处 DEV 下 `console.info('[WelineCart]', 'listen', …)`。
- 版本：`1.3.64` → `1.3.65`。

## 2026-09-10 — recovery hash / DOM 纳入 success surface（1.3.64）

- 补：`isCheckoutSuccessSurface` 识别 `#payment-recovery` 与可见 `[data-checkout-payment-recovery]`；cache-bust `listen2`。
- 版本：`1.3.63` → `1.3.64`。

## 2026-09-10 — 订阅支付/结账生命周期事件清车（1.3.63）

- 审图：`web_ui` / `error_shot`；支付处理中页迷你车仍显示批发 5 件。
- 修：`cart.js` 监听 `weline:checkout:order-created`、`weline:payment:paid|pending|outcome` → `invalidateAfterCheckout`；handoff 纳入 success surface；cache-bust `payment-lifecycle-listen1`。
- 版本：`1.3.62` → `1.3.63`。

## 2026-09-10 — 结账成功清理 summary_cache + 回源标签

- 审图分流：`web_ui` / `error_shot`（`/checkout` 主区空车，浮动批发车仍回填旧行）。
- 根因：迷你车 `preferCache` 在本地 `summary_cache` 有行时短路不回源；结账成功未清缓存。
- 修：`cart.js` `invalidateAfterCheckout`（清分桶缓存 + `markCartEmpty` + `weline.cart.summary_needs_origin`）+ 成功页 boot / `weline:checkout:success`；Checkout `navigateToSuccess`/空车薄调用；Theme 迷你车尊重 `needsOriginRefresh` → `forceNetwork` 后 consume。
- 后续：空车薄调用的 `source=checkout-empty` 须由 Checkout 忽略以免 `cart-updated` 反馈环（见 Checkout 1.4.59）。
- 契约：`CartJsPendingCouponContractTest` + `MiniCartShopifyDrawerContractTest`。

## 2026-09-10 — 批发车删除找不到行：mutate 用页面 chrome（1.3.62）

- 审图分流：`web_ui` / `error_shot`（批发车点删除 →「未找到要移除的购物车商品」）。
- 根因：`mutateCart` 用 `preferredCartType()`（全局偏好，常仍是 toc），而页签已 `data-cart-type=tob` 并渲染 tob 行 → remove 打到 toc 桶。
- 修：`activeCartType()` 优先读页面 `data-cart-type`；契约 + UT `testRemoveTobItemFailsWhenPreferenceIsToc`。
- 版本：`1.3.61` → `1.3.62`。

## 2026-09-10 — 零售空态 sibling 仍不可见：空突变+探测+CSS（1.3.61）

- 审图分流：`web_ui` / `ui_shot`（用户红框「没看到」浏览批发车按钮）。
- 根因：`remove/clear/update` 空摘要未附 `sibling_carts`；`display:contents`+`[hidden]` 在部分浏览器不可靠；已登录空 toc 若服务端未带回 sibling 需二次探测 tob。
- 修：`attachSiblingCartsIfEmpty`；`enrichEmptySiblings`；siblings `inline-flex`；空 mutate 强制 `getCart`。
- 版本：`1.3.60` → `1.3.61`。

## 2026-09-10 — 零售空态对称 sibling 提示（1.3.60）

- 审图分流：`web_ui` / `ui_shot`（零售空态仅「浏览商品」；用户要求其它类型有货也应提示）。
- 线稿：标题「零售车」/ 空态标题 / ~~引导长文~~ / `[浏览商品][浏览批发车 N件商品]` 居中并排。
- 原型调整：有货 sibling 一律 secondary twin（含 gate）；隐藏 empty-lead；服务端 UT 锁 empty toc→tob。
- 修：`renderSiblingCarts` 对称按钮；`testEmptyTocHasTobSiblingWhenTobHasLines`；契约；cache-bust。
- 版本：`1.3.59` → `1.3.60`。

## 2026-09-10 — 批发加购污染零售：cart_type 偏好优先（1.3.59）

- 现象：批发模式加购后零售车也出现同款。
- 根因：`product-purchase-actions` 解析 `cart_type` 时 **FPC `html[data-selling-mode]` 优先于** B2B `preferredMode()`/explicit session；列表卡 `product-card-add-to-cart` 未进 `syncButtons`。
- 服务端：`CartService::add` typed 键本已隔离；UT `testTobCustomerAddDoesNotMutateTocCart` 锁定。
- 修：`resolveAddCartType`（preferredMode → explicit → button → html）；`syncButtons` 覆盖卡按钮；`welineModules` cache-bust。
- 版本：`1.3.58` → `1.3.59`。

## 2026-09-10 — 空态双按钮居中并排（1.3.58）

- 审图分流：`web_ui` / `ui_shot`（空消息附图：两端 space-between 拉开丑）。
- 线稿修正：`[浏览商品][浏览零售车 N件]` 居中紧挨，不再拉满空态宽。
- 修：去掉 empty-actions `:has` space-between 与 sibling `margin-inline-start:auto`。
- 版本：`1.3.57` → `1.3.58`。

## 2026-09-10 — 空态 sibling 去说明卡：双次要按钮右置（1.3.57）

- 审图分流：`web_ui` / `ui_shot`（用户拒粉卡说明「丑死了」；要与「浏览商品」同款按钮横排右置）。
- 线稿：空标题 / ~~粉卡+主色 CTA~~ → `[浏览商品] …… [浏览零售车 N件商品]`。
- 原型调整：去掉 callout；sibling 仅 secondary twin；`empty-actions` `space-between` 右置。
- 修：`index.phtml` 结构+文案；`cart-page-amazon.css`；Theme 迷你车同源 secondary；契约更新。
- 版本：`1.3.56` → `1.3.57`。

## 2026-09-10 — 空态 sibling 审图：对比度 + 扁平 callout（1.3.56）

- 审图分流：`web_ui` / `ui_shot`（用户附图批发空态「丑」；非报错→改 UI）。
- 线稿：标题「批发车 (0)」/ 空态文案 / 泥感 muted 套卡 sibling / 朱砂底深灰字 CTA / 次要继续购物。
- 原型调整（variant A）：扁平 primary-bg-subtle + 左边强调条；主 CTA 实心 + on-primary 纯白；空态有 sibling 时弱化标题/隐藏引导文案/降权浏览 CTA。
- 修：`cart-page-amazon.css` sibling 结构；Theme `mini-cart-drawer` 同源；`_ink.css` `--color-on-primary` `#ffffff`（朱砂实心底奶油字发闷）；契约 `CartEmptySiblingUiContractTest`。
- Browser：禁缓存；computed `cta.color=rgb(255,255,255)` / callout `primary-bg-subtle` + `border-inline-start 3px`。
- 版本：`1.3.55` → `1.3.56`。

## 2026-09-10 — 空 preferCache 串态：说零售有但零售空（1.3.55）

- 审图分流：`web_ui` / `ui_shot`（批发空态「零售车还有 6 件」；切零售却「购物车是空的」）。
- 线稿：分车页签 / 标题 / 空态文案 / sibling callout（声称有货）/ 浏览商品；状态不一致点在 sibling 计数 vs 列表。
- 原型调整（逻辑）：空 typed `summary_cache` 禁止终端短路；sibling 有货失效对侧空桶；有货仍可 soft paint。
- 修：`index.phtml` `localCartSummaryHasItems` + `invalidateEmptyCachesClaimedBySiblings`；Theme 迷你车同源；契约锁定。
- 版本：`1.3.54` → `1.3.55`。

## 2026-09-10 — 空态 sibling CTA 点不动 + 布局审图修复（1.3.54）

- 审图分流：`web_ui` / `ui_shot`（批发空态「零售车还有 N 件」+「查看零售车」箭头；用户报点不动、布局丑）。
- 线稿：标题 / 引导文案 / sibling 提示+裸 button / 浏览商品次 CTA；双按钮视觉方言不一致。
- 原型调整：sibling 收成一块 callout；「查看*车」主色实心 CTA；「浏览商品」保持 secondary。
- 根因：`requestCartType(forceNetwork)` 只点 chrome → `setMode` 再派无 FN Event → `preferCache` 可盖住有货 sibling；B2B 适配二次 emit 加剧竞态。
- 修：`forceNetwork` 强制派 Event 且跳过 chrome click；B2B `setMode(..., {emit:false})`；空态 sibling Token 化样式（Cart + 迷你车）。
- 版本：`1.3.53` → `1.3.54`。

## 2026-09-10 — cart_type 切换解耦（1.3.53）

- Cart 拥有 `weline:cart-type-changed` + `WelineCart.requestCartType`；sibling CTA 不再直点 B2B DOM。
- 版本：`1.3.52` → `1.3.53`。

## 2026-09-10 — 权威同步 + 空车 sibling_carts 交叉推荐（1.3.52）

- 契约：Identity(Cookie)/Persistence(DB)/Presentation(cache) 三平面；空 `getCart` 附 `sibling_carts`；访客读 tob 软空壳+推荐 toc；Cookie 优先于参数 token。
- Inspection：`guest_token` 尾缀追查 + `expires_at`/`updated_at`。
- 店面：购物车空态 / 迷你车空抽屉 CTA；`summary_cache` `expires_at_ms` + paint 后回源；sibling CTA 走 `weline:selling-mode-changed` + 页签 `setMode`。
- 版本：`1.3.51` → `1.3.52`。

## 2026-09-10 — 禁止 summary_cache 幽灵有货（1.3.51）

- 审图分流：`web_ui` / `ui_shot`（迷你车零售车有货+DEMO10；用户报未登录久置后 /cart 空、后台查不到）。
- 根因：权威车在 `weline_cart` + HttpOnly `weline_cart_guest_token`（15d）；`localStorage` `summary_cache` 无 guest 会话时仍可渲染有货，导致抽屉「假有货」、`getCart`/后台按 token 查不到。
- 修：`cart.js` 有货摘要默认 `requireTokenMatch`；购物车页 `readLocalCartSummary` 校验 token；契约 `testSummaryCacheRejectsGhostCartWithoutGuestToken`。
- 版本：`1.3.50` → `1.3.51`。

## 2026-09-09 — 访客默认零售，不误报售卖类型需登录（1.3.50）

- 现象：默认本是 toc，但残留 `weline_selling_mode=tob` 时先打批发闸门，文案「该售卖类型需要登录」贴在零售车上，被理解成「零售也要登录」。
- 修：`storefrontSummary` 访客 tob 偏好静默改 toc，闸门回落 `message=''`；购物车页访客 `preferredCartType` 恒 toc，闸门回落不再写入 messageNode。
- 契约：`CartServiceCommerceTypeTest` / `CartPageDealChromeContractTest` 断言更新。
- 版本：`1.3.49` → `1.3.50`。

## 2026-09-09 — /cart 页 JS 闸门回落 toc（1.3.49）

- 审图分流：`web_ui` / `error_shot` 收口后，访客 `selling_mode=tob` 仍可能因 `getCart(tob)` soft-error / `business_error` 落入「暂时无法加载购物车」。
- 修：`index.phtml` `loadCart` 识别 `cart_commerce_type_*` / 登录闸门文案后二次 `getCart(toc)` 渲染，保留 `gate_reason`/`preferred_cart_type`；显式突变 fail-closed 不变。
- 契约：`CartPageDealChromeContractTest::testCartPageLoadCartSoftFallsBackOnCommerceTypeGate`。
- 版本：`1.3.48` → `1.3.49`。

## 2026-09-09 — 访客 tob cookie 打开 /cart 不再 500（1.3.48）

- 审图分流：`web_ui` / `error_shot`（`/cart` WLS Runtime Error：`该销售类型需要登录` / `SellingTypeResolver` guest+tob）。
- 根因：`storefrontSummary()` 直调 `getCart(tob)`，HTML 热路径未像 QueryBin 那样吞冲突；`selling_mode=tob` Cookie + 未登录 → 500。
- 修：`storefrontSummary` 捕获 `LOGIN_REQUIRED`/`MEMBERSHIP_REQUIRED` 后回落 `toc`，写入 `gate_reason`；`getCart`/`add` guest+tob 仍 fail-closed。
- 契约：`CartServiceCommerceTypeTest::testStorefrontSummarySoftFallsBackWhenGuestPrefersTob`。
- 版本：`1.3.47` → `1.3.48`。

## 2026-09-09 — PDP 加购控件不空返回（1.3.47）

- Theme 购买槽注入时，无 offer / 不可售仍输出可解锁「加入购物车/暂不可售」按钮，交给 product-info JS 解锁。
- 版本：`1.3.46` → `1.3.47`

## 2026-09-09 — 权威存储改为数据库（1.3.46）

- 纠正：登录客户与匿名游客车篮均应 DB 持久化，不以 file/`wls_memory` 为权威。
- 修：`Model/Cart`（`weline_cart` / 物理表 `w_weline_cart`）+ `CartDbStore` 实现 `CartStoreInterface`；guest `expires_at=+15d`，customer `expires_at=NULL`；`CartService`/Inspection/`provides` 默认 DB；`CartCacheStore` 降为遗留。
- Schema：全量 `setup:upgrade` 被无关 Shipping 索引 `uk_service_lane_region` 表达式读回阻断；本机已用 PDO 确保 `w_weline_cart` 建表以便验收。Shipping 门禁需另修后才能正规 SchemaDiff 收口。
- 验收：WLS `getCart`/`/cart` 读出 DB 探针行；UT `CartPersistencePolicy` + `CartDbStoreDefaultWiringContract` 绿。

## 2026-09-09 — 几分钟丢车：WLS file→wls_memory 假写（1.3.45）

- 现象：加购后几分钟/一刷新空车（非 7 天 TTL）。
- 根因：WLS 常驻把 `file` 缓存劫持为 `wls_memory`；侧车抖动时 `set` 返回 false，Cart 仍对前端报成功；内存池易失。
- 修：`CacheManager` 池级 `hijack_exempt`/`durable`；预定义 `cart` 池免劫持落 file；`CartCacheStore` 写失败 `cart_persist_failed` fail-closed；`app/etc/env.php` 强化 cart 池。
- 验收：`CacheManagerRoutingTest` + `CartCacheDurableRoutingContractTest`；Browser 加购刷新。
- 版本：`1.3.44` → `1.3.45`

## 2026-09-09 — 购物车持久化：游客 15 天 / 客户永久（1.3.44）

- 现象：万能购物车「总是掉」；游客与登录客户均被 `CartCacheStore` 统一 7 天 TTL 蒸发。
- 根因：服务端 cache、guest Cookie、前端 `guest_session` 全是 7 天；客户车无独立更长 TTL，且读路径不续期。
- 修：`CartPersistencePolicy`（guest=15d，customer≈50y durable）；`CartCacheStore` 按 owner 分 TTL；Query `issueGuestToken`/`renewGuestSession` 与 `cart.js`/`product-purchase-actions` 对齐 15 天。
- 验收：`CartPersistencePolicyTest` + `CartGuestSessionTtlContractTest`
- 版本：`1.3.43` → `1.3.44`

## 2026-09-09 — 行选项展示解码 percent-encoded value（1.3.43）

- 审图：en_US 购物车 Type 显示 `%E5…`；配合 Product `StorefrontEavLabelResolver::displayOptionToken`
- 修：`normalizeOptions` 对 value_label/value 的 `%XX` 展示解码；主题标签仍走 Product `presentThemeOption`
- 版本：`1.3.42` → `1.3.43`

## 2026-09-09 — 加购弹窗脚本重注入保留 type（1.3.42）

- 问题：`openPurchasePanel` 重插 `<script>` 时丢失 `type`，`application/json` 规格目录被当 JS 执行。
- 修：复制全部 attribute；非 executable type 仅落盘不执行；`?v=20260909-purchase-panel8`
- 版本：`1.3.41` → `1.3.42`

## 2026-09-09 — 卸批发后未注册 tob 回落 toc（1.3.41）

- 审图：`/cart` WLS 500「未注册的售卖类型：tob」；B2B 已卸但 cookie/会话仍偏好 tob。
- 修：`SellingTypeResolver` 对 Registry 无 code 的偏好 fail-soft 到内置 `toc`，禁止热路径抛 `CartConflictException`。
- 单测：`SellingTypeResolverTest::testUnregisteredPreferenceFallsBackToToc`
- 版本：`1.3.40` → `1.3.41`

## 2026-09-09 — 摘要券行自适应 + 配合批发默认留言（1.3.40）

- 摘要 extras 优惠券 `input/apply` 补 `flex` 自适应；配合 B2B tob 默认切到订单留言页签
- 版本：`1.3.39` → `1.3.40`；CSS `?v=20260909-tob-note-row1`

## 2026-09-09 — 审图：顶栏 USD 购物车仍 CNY（1.3.39）

- 分流：web_ui（顶栏 USD $，行价/小计仍 CNY）
- 根因：Frontend QueryBin 把路径 USD 滤空（见 Frontend 2.0.57）；本模块加固 `currentDisplayCurrency` 优先路径段，`index.phtml` render 用展示币标签。
- 版本：`1.3.38` → `1.3.39`

## 2026-09-09 — 审图：切语言/货币购物车未跟进（1.3.38）

- 分流：web_ui（EN + EUR €，批发车仍中文 Tab；价签 CNY / ¥1440）
- fail（A/B/E）：① B2B `en_US` 分车文案未译；② 本地 `summary_cache` 不校验 currency/locale，切币后仍画旧 CNY；③ tob 行价 `currency` 未强制跟展示币
- 修：缓存写入 currency+locale，不匹配视为 miss 强制 getCart；`presentLine` 行 `currency`=展示币并对 preserveWholesale 做 FX；B2B EN「Retail/Wholesale cart」
- 版本：`1.3.37` → `1.3.38`

## 2026-09-09 — toc/tob 本地摘要分桶，切类型无变更不拉 getCart（1.3.37）

- 问题：多了 `cart_type` 后切零售/批发仍强制刷网；`cart.js` 单桶 `summary_cache` 会互盖，像「本地不存了」
- 修：`rememberSummary`/`getCachedSummary`/`clearCachedSummary` 按 `summary_cache.{toc|tob}` 分桶（toc 仍镜像旧无类型键）；购物车页切类型 `preferCache` 命中则 `render` 不调 `getCart`
- 版本：`1.3.36` → `1.3.37`

## 2026-09-09 — 审图：订单摘要优惠券/留言页签（1.3.36）

- 分流：web_ui（订单摘要优惠券与留言纵向堆叠）
- fail（D/E）：摘要侧栏垂直占位过大；应对齐迷你车 extras 页签
- 修：两 slot 包入 `data-cart-summary-extras`；复用 Theme `mini-cart-extras-tabs`；cart-page CSS 下划线页签；B2B tob 堆叠仅限迷你车
- 版本：Cart `1.3.35` → `1.3.36`；Theme extras-tabs `?v=20260909-cart-summary-tabs1`

## 2026-09-08 — 审图：切币后购物车仍显示 CNY（1.3.35）

- 分流：web_ui（顶栏 USD $，行价/小计仍 `CNY 108.00`）
- fail（A/C/E）：`summary.currency` 锁购物车入库币；缺展示币价时未按基准 CNY+FX 换算（Currency `rate-mode-and-storefront-conversion`）
- 修：`CartService::presentationCurrency` 优先 `RequestContext` 展示币；`ProductCatalogCartItemSnapshotResolver` 展示币无价时 FX 回退基准币
- 单测：`CartServiceTest::testGetCartPresentationCurrencyFollowsRequestDisplayCurrency`
- 版本：`1.3.34` → `1.3.35`

## 2026-09-08 — 审图：购物车数量加减背景（1.3.34）

- 分流：web_ui（购物车行 qty 步进器；用户附图指加减背景别扭）
- fail（E/F/G）：左右按钮填 `surface-muted`、中间白底凸出；qty=1 时减号 `disabled` 半透明与加号米色形成色差；硬编码 hover `#e3e6e6` + 外层 box-shadow；未 `appearance:none`
- 修：对齐迷你车——单一 `surface`、按钮/输入透明、hover 才 muted；圆角/描边走主题 token；去掉阴影与硬编码 hex；`index.phtml` 静态版本 `qty-stepper1`
- 版本：`1.3.33` → `1.3.34`
- 验收：禁缓存打开 `/cart`；Worker 容量耗尽无法拉真实行，注入同 class 探针；computed：左右 `transparent`+同高、无 box-shadow、`appearance:none`
- 宿主原生编辑。

## 2026-09-08 — 购物车左侧商品列表限高滚动（1.3.33）

- 分流：web_ui（零售/批发车左侧产品区；多件时整页被撑长）
- fail（C/E）：列表无限增高，右侧摘要与结算难对照；对照 Theme `overflow:auto` + `min(vh, rem)` 模式
- 修：`cart-page-amazon.css` 为 `.weline-cart-shell__list` 设 `--cart-list-max-height` + `overflow-y:auto`；标题/价格头与摘要不进滚动
- 版本：`1.3.32` → `1.3.33`

## 2026-09-08 — 批发车 Tab 串零售车数据（1.3.32）

- 分流：web_ui（顶栏「批发 ¥97.20」vs 页内批发车行价 CNY 108.00）
- fail（A/B/C）：B2B `syncCartPageChrome` 只改壳，页内仍用 SSR `toc` 首拉；`preferredCartType` 在 B2B 未就绪时无 cookie 回退
- 修：`readSellingModeCookie` + `cartIdentity(forcedMode)`；`loadCart({cartType})`+`cartLoadSeq`；`weline:selling-mode-changed` 按类型重载
- 版本：`1.3.31` → `1.3.32`

## 2026-09-08 — 行选项优惠主题显示名字

- 审图：结账/购物车选项勿露 `promotion_theme_id` 与数字 id
- 修：`optionsFromSelection` 走 Product `StorefrontCampaignEntry::presentThemeOption`

## 2026-09-08 — remove/update 登记 cart_type Worker 参数（1.3.31）

- 分流：web_ui（零售/批发车点删除 → toast `Unknown frontend worker param: cart_type`）
- 根因：`mutationParams()` 未并入 `sellingModeParams()`，前端已传 `cart_type` 识别 toc/tob，Worker 校验拒收
- 修：`update`/`remove`/`previewDiscount` 允许 `cart_type`+`selling_mode`；契约单测断言
- 版本：`1.3.30` → `1.3.31`

## 2026-09-08 — 购物车更新前置可售校验（1.3.30）

- 分流：web_ui（结账提交 toast「Offer 不存在」；用户要求更新/进车/进结账即校验）
- 缺口：`presentLine` 已 resolve 但 `found=false` 静默忽略，拖到提交才暴露
- 修：`presentLine`/`summary` 写入 `found`/`sellable`/`checkout_blocked`/`line_issues`；`updateItem` 更新前 resolve；Checkout `getData` 阻断提交；文案人性化
- 单测：`CartServiceTest::testGetCartAndUpdateSurfaceMissingOfferBeforeCheckoutSubmit`
- 版本：`1.3.29` → `1.3.30`

## 2026-09-08 — 快捷加购应用 B2B identity（1.3.29）

- 变更：`openPurchasePanel` 读取 API `identity` 并调用 `WelineB2BSellingMode.applyIdentity`，避免面板 SSR 丢会话时误显申请
- 版本：`1.3.28` → `1.3.29`

## 2026-09-08 — 快捷加购面板 data-size=lg（1.3.28）

- 变更：purchase panel `data-size` 从无效 `xl` 改为 Theme 已有 `lg`
- 版本：`1.3.27` → `1.3.28`

## 2026-09-08 — 列表快捷加购捕获拦截（1.3.27）

- 变更：`openPurchasePanel` 在捕获阶段 `stopImmediatePropagation`，避免与迷你车并行打开；面板注入后调用 `WelineB2BSellingMode.bindAll`
- 版本：`1.3.26` → `1.3.27`

## 2026-09-08 — 列表快捷加购拦截（1.3.26）

- 变更：卡片加购 `openPurchasePanel`；面板内加购读取 qty select；成功后关闭面板
- 版本：`1.3.25` → `1.3.26`

## 2026-09-08 — 购物车状态键收归 Cart，脱离 weline-api（1.3.24）

- 变更：`installCartStatus` / `markCartActive|Empty` 接管 cookie/localStorage 与 `enableAutoRequests` 信号；不再向 `Weline.Api.config` 注入 cart 键。
- 版本：`1.3.23` → `1.3.24`

## 2026-09-08 — 延后加载与 wait-gift 券逻辑收归 Cart（1.3.23）

- 当前状态：`implemented`
- 变更：`weline.modules.js` 登记 `load: "defer"`；`cart.js` 注入 Api 购物车状态键；监听 `weline:maintenance:wait-gift-redeemed` 写入 pending coupon（不再由 `weline.js` 写购物车键/兑券）。
- 版本：`1.3.22` → `1.3.23`

## 2026-09-07 — 后台类型徽章与类型化 Cart 事件（1.3.22）

- 变更：`Inspection` 增加 `cart_type` 列（Theme `w-badge` + Registry label/tone）与已注册类型筛选；文案标明按 `scope_key` 查询非全局列表。新增 `CartTypeEventEnvelope`；`CartService` 在 add/clear/merge 成功路径派发 `cart_item_added` / `cart_cleared` / `cart_merged` 并追加 `cart_type`+`type_payload`。
- 单测：`CartTypeEventEnvelopeTest`。
- 版本：`1.3.21` → `1.3.22`

## 2026-09-07 — 可选 qty 策略 SPI（1.3.21）

- 变更：新增 `Api/CommerceCartQtyPolicyInterface`；`CartService` add/update 在非 toc 车上经 `RuntimeProviderResolver` 可选调用（无 Provider 则跳过）。
- B2B 实现：`B2BCartQtyPolicy`（tob moq/step）。
- 版本：`1.3.20` → `1.3.21`

## 2026-09-07 — CartService `cart_type` 分车落地（1.3.20）

- 变更：`CartService` 键升级为 Scope+owner+`|type:{code}`（默认 toc）；旧无后缀键在 toc 读路径一次迁移；公开 guest/customer 方法经 `SellingTypeResolver`；guest+tob 登录拒绝；`mergeGuest` 仅同类型且 tob 无游客车；`summary` 含 `cart_type`+`type_payload`；tob `presentLine` 保留 `b2b_*` 批发价；`CheckoutCartSnapshotService` 冻结透传 `cart_type`。
- 单测：`CartServiceCommerceTypeTest`（默认 toc 键、typed 键、guest 禁 tob、同类型合车）。
- 版本：`1.3.19` → `1.3.20`

## 2026-09-07 — ToC/ToB 一期：`cart_type` 合同文档

- 当前状态：`docs_planned`（仅文档）
- 变更：`doc/cart.md` 增加「售卖类型 `cart_type`」节（分车键 Scope+owner+type、旧键=toc 迁移、mismatch fail closed、无游客 tob、摘要 `cart_type`+`type_payload`、`presentLine` 必须带类型）；`doc/需求.md` 追加 `REQ-CART-0005` 商城购物车类型 SPI。
- 关联：`Weline_B2B` `REQ-B2B-0002` 双注册 `tob`。

## 2026-09-07 — 迷你车摘要本地缓存与即时刷新（1.3.18）

- 问题：加购后头部迷你车金额常停在 ¥0.00，点开抽屉才更新；无变更时仍反复 `getCart`。
- 变更：`cart.js` 增加 `weline.cart.summary_cache`（`rememberSummary` / `getCachedSummary`）；加购 `notifyCartUpdated` 写缓存并确保加载 `miniCartIcon` 后 `applyCachedSummary`。
- 版本：`1.3.17` → `1.3.18`

## 2026-09-07 — 行价改 Summary 头布局（1.3.17）

- 变更：现价+划线同行，活动标签单独右对齐下一行（`line-price-row`）；价列加宽 + `flex-wrap: nowrap` 防窄列折行。
- 版本：`1.3.16` → `1.3.17`

## 2026-09-07 — 购物车结账 CTA 对齐 ink（1.3.16）

- 问题：全页购物车结账钮仍用 Amazon 黄渐变，与水墨品牌盘不一致。
- 变更：`cart-page-amazon.css` 的 `--cart-cta-*` / 价格色改吃 `--color-primary` 族。
- 版本：`1.3.15` → `1.3.16`

## 2026-09-07 — 行价划线原价与活动标签（1.3.15）

- 问题：迷你购物车 / 购物车 / 结账仅显示成交价，PDP 上的划线原价与活动名（如 Today's Picks）不同步。
- 修复：`CartItemSnapshot` 透出 `compare_at_minor` / `campaign_label` / `campaign_url`；`presentLine` 与结账 freeze 同源 Assembler；购物车页与迷你车 UI 收口原型变体 A（PDP inline）。
- 原型：`view/prototypes/cart-line-price-variants.html`（?variant=A|B|C）。
- 版本：`1.3.14` → `1.3.15`

## 2026-09-06 — 迷你购物车展示重解析成交价（1.3.14）

- 问题：摘要只刷新 options/图片，`unit_price_minor` 钉死加购瞬间；活动价与 PDP Assembler 不同步时用户看到「购物车不认活动」。
- 修复：`presentLine` 一次 snapshot resolve，展示时刷新 unit/row_total（与 checkout freeze 同源）。
- 版本：`1.3.13` → `1.3.14`

## 2026-09-06 — 仅询价配色可读（1.3.13）

- 问题：卡片「仅询价」复用 buy-now 红底，字色对比差，观感像禁用。
- 修复：改用独立 `__quote` 类（浅底主色描边/字色）；契约测覆盖。
- 版本：`1.3.12` → `1.3.13`

## 2026-09-06 — 卡片仅询价可点进商详（1.3.12）

- 问题：列表卡 `quote_only` 渲染灰色 disabled「仅询价」，无法进入询价。
- 修复：`product-card-add-to-cart` 在 `quote_only` 时改为可点链接（`?quote=1` 进商详），仍禁止 `data-action=add`；契约测覆盖。
- 版本：`1.3.11` → `1.3.12`

## 2026-09-05 — 仅询价禁止加购（1.3.11）

- 问题：PDP「仅询价」按钮仍可 `data-action=add` 并提示已加入购物车。
- 修复：`product-add-to-cart` / `product-card-add-to-cart` 在 `quote_only` 时禁用且去掉 add action；契约测覆盖。
- 版本：`1.3.10` → `1.3.11`

## 2026-09-05 — 结账相关摘要英文词条补齐（1.3.10）

- 当前状态：`implemented`
- 变更：`en_US.csv` 补齐优惠/小计/购物车优惠券等前台摘要词条，避免英文结账摘要残留中文。
- 版本：`1.3.9` → `1.3.10`

## 2026-09-05 — 摘要优先目录重解析规格 swatch（1.3.9）

- 当前状态：`implemented_pending_browser_acceptance`。
- 变更：`presentLineOptions` 有 Scope/Offer 时优先目录快照 options（含 swatch），再回退持久行；避免旧行永久无图。
- 版本：`1.3.8` → `1.3.9`。
- 交付地址：`/cart`、迷你购物车抽屉、`/checkout`。

## 2026-09-05 — 规格图/色块随 options 展示（1.3.8）

- 当前状态：`implemented_pending_browser_acceptance`。
- 变更：行级 `options` 携带 `swatch_image`/`swatch_color`；购物车页规格行有图显示缩略图、有色显示色块，文字始终保留。与迷你购物车、结账列表同一契约。
- 版本：`1.3.7` → `1.3.8`。
- 交付地址：`/cart`、迷你购物车抽屉、`/checkout`。

## 2026-09-05 — 购物车行展示已选 EAV 规格（1.3.7）

- 当前状态：`implemented_pending_browser_acceptance`。
- 现象：购物车仅显示长 SKU，看不出颜色/尺码/角色等已选规格。
- 修复：行级 `options`（`code/label/value/value_label`）由 Product 快照经 `StorefrontEavLabelResolver` 填充；`CartService` 摘要展示并回填；购物车页在 SKU 下渲染规格文案。
- TDD：`CartStorefrontQueryBinContractTest::testCartPageRendersSelectedOptionLabels`。
- 版本：`1.3.6` → `1.3.7`。
- 交付地址：`/cart`。

## 2026-09-04 — 主题编辑器预览：product-add-to-cart 跳过 offer resolve（1.3.6）

- 当前状态：`implemented`。
- 变更：product-add-to-cart 预览跳过 offer resolve（与 buy-now 同模式）。
- 版本：`1.3.5` → `1.3.6`。

## 2026-09-04 — 结账恢复共享访客购物车时不轮换令牌

- 当前状态：`implemented_validated`。
- 现象：同一浏览器 `/cart` 显示商品，进入 `/checkout` 后却显示空购物车；浏览器可读会话缺失时，结账页无法向 `checkout.getData` 显式传递已有访客身份。
- 根因：`issueGuestToken` 无条件生成新令牌；因此结账页不能安全地用该操作恢复 HttpOnly Cookie 对应的既有购物车，否则会被切换到空车。
- 修复：Cart Query 先复用当前网站作用域内的可信 Cookie，仅在确实不存在时签发新令牌；结账页随后可安全接管并缓存该会话。客户身份、网站作用域、商品与金额仍由服务端决定。
- TDD：新增作用域 Cookie 回归用例，修改前返回随机新令牌，修改后复用现有令牌；Cart Query 与前台契约 15 tests / 118 assertions 全部通过。
- 浏览器验收：WLS 四 Worker 优雅热重载后，在新的 Codex 内置浏览器标签页中 `/cart` 显示 1 件、CNY 48.00；普通「去结算」进入 `/checkout` 后仍为同一商品 x1、CNY 48.00。
- 版本：`1.3.4` → `1.3.5`。
- 交付地址：`/cart`、`/checkout`。

## 2026-09-03 — 优惠券小计与 Marketing 报价对齐

- 当前状态：`implemented`
- 说明：小计不扣减根因在 Marketing/Cart 客户身份不一致 + DEMO10 每人限用已耗尽；Cart `buildDiscountPreview` 逻辑本身在可用券时正确扣减。清理临时调试日志。

## 2026-09-03 — 购物车页先接管共享游客会话再签发令牌

- 当前状态：`implemented_pending_browser_acceptance`。
- 现象：商品详情选中 EAV 颜色/尺码后加购成功，但跳转 `/cart` 时页面先调用 `issueGuestToken`，导致刚加入的游客购物车显示为空。
- 根因：详情页由 Cart 模块把令牌保存在共享 `guest_session`；购物车页内联脚本在 Cart 模块尚未加载时只读取旧 `sessionStorage`，未命中后直接签发新令牌。
- 修复：购物车页在 `issueGuestToken` 前显式加载 `cart` 模块并重新读取共享游客会话；已有令牌直接复用，只有确认不存在时才签发新令牌。
- TDD：`CartStorefrontQueryBinContractTest::testCartPageAdoptsTheSharedGuestSessionBeforeIssuingANewToken` 修改前失败；修复后须通过，并用真实商品规格复验详情页 → 加购 → 购物车，禁止进入结账或支付。
- 交付地址：`https://p05113ef3.test.weline.com:9555/cart`。

## 2026-09-03 — 购物车摘要小计对齐迷你购物车

- 当前状态：`implemented`
- 变更：订单摘要改为「优惠券/留言 → 商品小计+优惠行 → 应付小计」；有 `discount_preview` 时扣减应付并展示绿色优惠行；监听 `weline:cart-updated` 刷新。
- 版本：`1.3.3` → `1.3.4`

## 2026-09-03 — 购物车摘要优惠券/留言槽

- 当前状态：`implemented`
- 变更：`cart-summary-discount` / `cart-summary-note` 槽 + `cart-storefront-slots` 目录；Marketing `cart-coupon`、Order `order-notice` 默认注入；迷你购物车保持 `footer-extras` tab。
- 版本：`1.3.2` → `1.3.3`
- 契约：`CartStorefrontSlotsCatalogContractTest`

## 2026-09-03 — 商品详情加购携带所选 EAV 规格

- 当前状态：`implemented_pending_browser_acceptance`。
- 问题：商品详情页切换 Offer UUID 后，加购脚本未提交当前颜色/尺码 selection，购物车无法保留用户实际选择的 EAV 规格。
- 修复：`product-purchase-actions.js` 从当前详情根节点的选中规格按钮读取 `data-variant-axis` / `data-variant-value`，稳定排序后随 Cart `add` 提交；不读取或写入商品规格 JSON。
- TDD：`ProductAddToCartWidgetContractTest::testPurchaseActionsSubmitSelectedEavVariantValues` 修改前失败，修改后 4 tests / 23 assertions 通过；JS 语法检查通过。
- WebUI：待在真实 Weline 浏览器完成颜色/尺码切换、规格图切换、加购并在购物车核对规格与短 SKU；禁止进入结账或支付。
- 交付地址：`https://p05113ef3.test.weline.com:9555/product/zhi-zao-si-min-guo-feng-nu-zhuang-tang-zhuang-su-yi-chan-cha-fu-gai-l-9dfa4e25`、`https://p05113ef3.test.weline.com:9555/cart`。

## Hotfix — 2026-09-02：店面默认 Scope 读取现行导航契约

- 现象：真实商品详情页点击「加入购物车」后，前端等待 QueryBin worker 超时，购物车仍为 0；同时间日志反复报告 `StorefrontNavigationScope::$scope` 未定义。
- 根因：`StorefrontScopeInstallerInterface::installNavigationScope()` 已返回以 `identity` 承载 Channel Scope 的 `StorefrontNavigationScope`，Cart 仍读取已不存在的旧属性 `scope`，于是可信 Channel Scope 被降级为 Website。
- 修复：`CartScopeResolver::serverResolvedDefaultScope()` 改读 `StorefrontNavigationScope::$identity`；不改 Product 导航、购买部件或购物车 UI。
- TDD：新增真实 Provider registry + installer 契约用例；修改前 1/9 失败（期望 Channel，实际 Website），修改后 `CartScopeResolverTest` 9 tests / 14 assertions 全通过且无 warning。
- 运行：`php bin/w server:reload` 完成 4 Worker 滚动重启；PDP、`/cart`、`/checkout` 恢复探针均为 HTTP 200。
- WebUI：本机内置浏览器复验因 macOS 当前锁屏暂待续跑；解锁后必须完成同一商品数量加购、购物车持久化，并确认未产生新的 undefined-property warning，方可将本条标记为 accepted。
- 版本：服务内部兼容修复，无 Model/Controller/注册表变更，保持 `1.3.2`。
- 交付地址：`https://p05113ef3.test.weline.com:9555/product/hanfu-1688-604560496347`、`https://p05113ef3.test.weline.com:9555/cart`、`https://p05113ef3.test.weline.com:9555/checkout`。

## Hotfix — 2026-09-02：加购不再自动弹出迷你购物车

- 决策：加购成功只保留 toast / 角标 / inline 成功反馈；不自动打开右侧 drawer。
- 变更：移除 `product-purchase-actions` 的 `openMiniCartDrawer()` 及成功路径调用；Theme `Weline.MiniCart.open/close` API 保留供手动打开。

## Hotfix — 2026-09-02：加购成功打开右侧 mini-cart 抽屉

- 现象：`/products` 加购后角标更新，但用户期望的右侧抽屉未打开；Amazon toast 在窄屏易撑满。
- 修复：`product-purchase-actions` 成功路径曾调用 `openMiniCartDrawer()`；toast 宽度改为像素钳位。
- Theme：`mini-cart-icon.js` 暴露 `Weline.MiniCart.open/close`。
- 后续：按产品决策撤销自动开抽屉（见上条）。

## Hotfix — 2026-09-02：结账/购物车页未读 guest_session 导致空车

- 现象：加购 toast 有件数，打开 `/cart` `/checkout` 却空车。
- 根因：`product-purchase-actions` 优先写/读 `WelineCart.guest_session`；购物车页与结账页只读 `sessionStorage.guest_token`，token 分裂。
- 修复：cart/checkout/mini-cart 统一优先 `getGuestSession()`。
- 版本：`1.3.1` → `1.3.2`。

## Hotfix — 2026-09-02：frontend worker 补 external=true

- 现象：店面加购报 `Frontend worker operation is not allowed.`
- 根因：`QueryProviderRegistry` 前端暴露要求 `frontend=true` **且** `external=true`；Cart 描述符仅有 frontend。
- 修复：`CartQueryProvider` 全部 frontend 操作补 `external=true`；安全测试断言。
- 版本：`1.3.0` → `1.3.1`。

## Hotfix — 2026-09-02：加购无弹窗（Provider 扩展点 + add-v2 残留）

- 现象：商品列表点「加入购物车」无弹窗；易被误判为 header/footer 不统一。
- 实测：`/products` 已有统一 `data-w-mini-cart`；真因两处——
  1. `CartItemSnapshotProviderRegistry::extensionClass` 误读 ExtendsData 不存在的 `file`，Product provider 未注册 → `未找到 Cart Provider：product`
  2. `pub/static` / 浏览器缓存仍执行 `[data-action="add-v2"]`，按钮已是 `add` → 点击未绑定
- 修复：恢复 relative_path 解析；契约禁止 `add-v2`；同步 pub/static 并浏览器验收弹窗。

## 2026-09-02 — 去掉购物车「V2」称谓，旧会话车删除

### 变更
- 删除 `CartSession` 会话车与旧 `CartService` 双轨；原 `CartV2Service` 升格为唯一 `CartService`。
- Query：`addV2/getV2Cart/updateV2/removeV2/clearV2` → `add/getCart/update/remove/clear`；同 PR 更新 Theme/Checkout/Product 调用方。
- SPI：`CartItemSnapshotProviderV2*` → `CartItemSnapshotProvider*`；扩展点目录同步；删除无实现的 Legacy V1 adapter。
- 缓存 identity：`cart_v2` → `cart`（在途游客车清空一次）。
- 文档 `cart-v2.md` → `cart.md`。

### 验收
- `CartServiceTest` + `CartQueryProviderSecurityTest` + `LoginMergeGuestCartTest` + 加购契约：27 tests OK。


> 按目标版本记录真实进度、调整与验收证据；2026-08-17 前的历史不补造。

## 2026-09-02 — 加购 Network 失败：asset:// 漏出 mini-cart

- 当前状态：`fixed`
- 现象：点击加入购物车后 Network 出现由 `mini-cart-icon.js` 发起、名称为 UUID、状态失败的请求。
- 根因：Cart 行 `image` 持久化为 FileManager `asset://UUID`；`getCart` 直出；迷你购物车 `img.src` 触发 `ERR_UNKNOWN_URL_SCHEME`。合并加购时也不刷新 `image`，旧协议会一直粘住。
- 修复：`CartService::summary` 出口消毒（`asset://` 与其它非 http(s) 协议置空）；`add` 合并行刷新 `image`/`sku`；Product snapshot 出站解析（既有）；Theme `mini-cart-icon.js` 拒绝不可展示协议。
- 验收：`CartServiceTest` 新增摘要消毒与合并刷新用例；Browser 复验 `getCart` / mini-cart 无 `asset://`。

## 2026-08-30 — 维护礼金券浏览器存续 + 游客购物车 7 日续期

- 当前状态：`implemented`
- `cart.js`：`weline.cart.pending_coupon`（7 天）；监听 `weline:cart:apply-coupon` → Marketing `applyCoupon`；用过/成功后清除；过期删除。
- 游客：`weline.cart.guest_session` 7 天；最后一天访问/停留调用 `cart.renewGuestSession` 续期一周（cookie + Cart cache touch）。
- `weline.js` 维护兑礼成功后 `preLoad('cart')` 并派发优惠券事件。
- 版本：`1.2.3`。

## 2026-08-30 — 万能购物车改 modules + data-weline-load

- 当前状态：`implemented`
- 变更：`view/statics/frontend/weline.modules.js` 注册 `cart`；加购/卡片部件与 Theme 购买槽挂 `data-weline-load="cart"`；删除部件/`body-end` 直引 `product-purchase-actions.js`。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。

## 2026-08-30 — 游客无 guest_token 时 getCart 误报错

- 当前状态：`fixed`
- 现象：登录页等未加购场景下 mini-cart 调用 `cart.getCart`，BinQuery 红字 `cart_guest_token_required` / business_error（游客加购需要 guest_token）。
- 根因：`getCart` 经 `cartKey` 对空 token 抛错；该校验只适合加购/变更，不适合只读拉空车。
- 修复：`CartService::getCart` 在无客户且无 guest_token 时返回空车成功摘要；加购等变更路径仍要求 token。
- 验收：`CartServiceTest::testGetCartWithoutGuestTokenReturnsEmptySummary`；登录页 `getCart` 不再 ERR。

## 2026-08-29 — 导出亚马逊加购面板供批量加购复用

- 当前状态：`fixed`
- 问题：Product「经常一起购买」批量加购无法复用单件加购的 `.w-amz-cart-added` 面板。
- 修复：`WelineCartPurchaseActions.showCartAddedNotice` 导出 `showAmazonCartAddedNotice`；`body-end` / 买盒脚本缓存版本抬升。
- 验收：FBT 全部加入后出现与单件加购同款小计/去结算/查看购物车面板。

## 2026-08-28 — 结账页 Worker nonce / 购物车预计优惠递归 OOM

- 当前状态：`fixed`
- 现象：结账冷加载出现 `Worker nonce has already been used.`，摘要空车；WLS Worker 在 `CartQueryProvider` / `CartService` 路径 OOM（512MB）。
- 根因：`getCart` → `enrichSummaryWithDiscountPreview` → `buildDiscountPreview` → `resolveStorefrontSummary` → `getCart` 在存在 `guest_token` 时无限递归；nonce 在业务执行前已消费，崩溃/重放后客户端看到 nonce 复用错误。
- 修复：`buildDiscountPreview` 支持传入已加载 summary；`enrichSummaryWithDiscountPreview` / `storefrontSummaryPayload` 复用 summary，禁止再入 resolve。
- 验收：契约测试 `CartQueryProviderDiscountPreviewRecursionContractTest`；Browser 加购后打开结账无 nonce 红字且摘要有商品。

## 2026-08-28 — 库存不足提示按场景点名

- 当前状态：`fixed`
- 问题：详情页买盒再写「商品名库存不足」多余；「经常一起购买 → 全部加入购物车」不点名则无法判断是哪一件。
- 修复：Product 快照仍返回通用「商品库存不足」；`CartService` / `CartService` 在通用/空 message 时升级为「「%{1}」库存不足」；库存打满/下调文案继续带商品名；Cart 英文 `商品库存不足` → `Out of stock`。
- 关联：详情模板隐藏通用库存 message，只保留 Temporarily out of stock 等买盒状态。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.2.1` | verification_in_progress | `REQ-CART-0001` | 2026-08-17 | 2026-08-17 | Browser pending |

## `1.2.1` — 公开可信 Scope 解析边界

- 基线版本：`1.2.0`
- 目标版本：`1.2.1`
- 当前状态：`verification_in_progress`
- 需求 ID：`REQ-CART-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：Cart API/Service/DI 与聚焦测试；不改 Model Schema，不改变客户端身份规则。
- 负责人/确认人：主执行者直接实现（未委派）/用户

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-cart-0001-服务端权威购物车身份与-scope` | 2026-08-17 |
| 实现执行者/模型/范围已记录 | pass | 本条“负责人/确认人”；主执行者直接执行 | 2026-08-17 |
| 架构/缺陷/安全复审通过 | pass | `CartScopeResolverInterface` 为公开边界；Checkout 不再依赖 Cart 具体 Service | 2026-08-17 |
| 分层测试通过 | pass | 站点 Cart/Checkout/Order：168 tests / 986 assertions；框架 peer：150 / 900 | 2026-08-17 |
| 真实路径 WebUI/E2E 通过 | pending | 待 9514 WLS Browser 加车→结账 | 2026-08-17 |
| 可重复 E2E 固化并通过 | pending | 待更新/运行 Checkout E2E | 2026-08-17 |
| 功能/运营文档完成 | pass | `doc/cart.md` 已补公开 Scope API；无新增运营配置 | 2026-08-17 |
| 用户/产品验收 | pending | 待用户确认 | 2026-08-17 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-17 | requirements | planned → requirements_confirmed | Checkout 直接引用 CartScopeResolver 具体类 | 发布接口并按接口解析 | 用户 | 当前任务“继续”确认 |
| 2026-08-17 | implementation | requirements_confirmed → implemented | Cart 发布 `CartScopeResolverInterface`，模块 `provides` 绑定现有实现 | 保留可信 Scope 语义并消除跨模块具体类耦合 | 用户 | `Api/CartScopeResolverInterface.php`、`etc/module.php` |
| 2026-08-27 | storefront-ui | — | `/cart` 页面 Amazon 风格化：行列表 + 商品图 + 步进器 + 黄色结算 CTA | 对齐 Checkout/minicart Amazon token | 用户 | `view/statics/css/cart-page-amazon.css`、`view/templates/frontend/cart/index.phtml`；`CartStorefrontQueryBinContractTest` 45 pass |
| 2026-08-27 | implementation | verification_in_progress（增量） | 加购成功悬浮提醒改为亚马逊专用 flyout（绿勾标题、小计左右分布、`CNY 180.00` 格式、去结算/查看购物车按钮），不再走通用 `w-toast` | `showAmazonCartAddedNotice` + `.w-amz-cart-added` 样式；缓存需 `cache:clear` 后生效 | 用户 | `/products` 加购验收：小计 `CNY 1548.00`、按钮「去结算」「查看购物车」 |

### 实现范围

- 变更路径/符号：`Api/CartScopeResolverInterface.php`、`Service/CartScopeResolver.php`、`etc/module.php` 及聚焦测试。
- 数据/配置/迁移：无 Model Schema；预计仅 `provides` 增量。
- 兼容与回退：保留现有 Resolver 行为；可回退接口消费而不改存量数据。

### 审查与整改

- 架构：pass；跨模块只消费 Cart 发布接口。
- 缺陷：pass；核心刷新暴露的 Cart hydration/session 回归已恢复并纳入全套测试。
- 安全：pass；显式 Scope 与可信请求 Scope 冲突仍 fail closed。
- 用户批准延期：none。

### 测试与验收

- 聚焦/单元/集成：pass，站点 168 tests / 986 assertions；框架 peer 150 / 900。
- 真实 WLS + WebUI：pending。
- E2E 固化：pending。
- 未验证项：本轮核心刷新后的真实浏览器加车→结账路径。

### 文档与运营交付

- 需求文档：`REQ-CART-0001`。
- 功能/API/架构文档：`doc/cart.md` 已更新。
- 运营文档：N/A；本变更无新增运营入口或配置。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。 待生成器刷新。

### 版本结论

- 结论：verification_in_progress（实现与确定性门禁通过，尚未 accepted）。
- 完成/发布证据：未提交、未推送、未部署。
- 剩余事项与恢复条件：绑定并核验 `Weline` Chrome 实例，完成 9514 真实加车→结账 Browser 验收及用户验收。
