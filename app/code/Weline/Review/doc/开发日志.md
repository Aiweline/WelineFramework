# Weline_Review 开发日志

> 按目标版本记录真实进度、调整与验收证据；不粘贴原始终端日志，不补写未经证实的历史。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.0` | testing_passed | `REQ-REVIEW-0001`、`REQ-REVIEW-0002` | 2026-08-22 | 2026-08-22 | user_acceptance_pending |

## `1.0.0` — 万能评论与后台审核

- 基线版本：`1.0.0`
- 目标版本：`1.0.0`
- 当前状态：`testing_passed`
- 需求 ID：`REQ-REVIEW-0001`、`REQ-REVIEW-0002`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：同步后台评论、四项商品评分、直接选星、扩展评分持久化/回读、模板、i18n 与聚焦测试；不改 Schema、不发布、不部署。
- 负责人/确认人：当前主 Codex 直接实现；用户确认需求与跨仓写入。

### 既有实现基线

- 建立 `product` 独立评论表与公共媒体表，并添加面向范围、实体、状态、时间的组合索引。
- 建立类型 Provider/Registry，字段定义同时驱动浏览器表单和服务端校验。
- 支持匿名及登录评论，图片 6 张、视频 2 个，媒体先暂存票据再与评论绑定。
- 产品页切换到 `Weline_Review` Hook，旧 `WeShop_Review` Provider 降级为不冲突的兼容占位。

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-review-0002-后台统一评论审核管理` | 2026-08-22 |
| 实现执行者/模型/范围已记录 | pass | 当前主 Codex 直接实现；无子代理；范围见本条实现范围 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 站点实现按规范框架 `getPaginationState()` 分页契约适配；ACL、CSRF、状态校验、输出转义和媒体限制已复核 | 2026-08-22 |
| 分层测试通过 | pass | Review PHP lint、后台 E2E JS 语法、主题编译通过；聚焦 PHPUnit `4 tests / 24 assertions` | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | pass | 规范框架独立 WLS `9566` 完成真实 BinQuery 表单、图文/视频上传、后台审核和公开回读；控制台 0 error | 2026-08-22 |
| 可重复 E2E 固化并通过 | pass | `Test/e2e/backend/Weline_Review-moderation.spec.js` 两项后台用例通过 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/运营/评论审核.md`、`resolve_task_context` | 2026-08-22 |
| 用户/产品验收 | pending | 框架侧技术验收已通过，等待用户检查 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 14:51 | requirements | planned → requirements_confirmed | 原需求覆盖万能评论但未覆盖后台审核工作台 | 用户确认按双向差异写回框架，并要求在框架测试 | 用户 | `doc/需求.md` 版本变更记录 |
| 2026-08-22 15:18 | implementation/review | requirements_confirmed → review_passed | 站点旧核心的 `getPagination()` 返回 HTML，不能作为框架侧数组使用 | 改用规范框架 `getPaginationState()` 与 `totalSize`，同步后台模板字段和回归断言 | 当前主 Codex | `Service/ReviewService.php`、`Service/ReviewAdminService.php`、聚焦测试 |
| 2026-08-22 15:37 | runtime_acceptance | review_passed → testing_passed | 实际后台验收发现表格动作列溢出和浅色粘性列遮挡 | 为管理表格增加横向滚动、粘性动作列及后台主题色回退；修正 E2E 对真实后台壳层与筛选流程的断言 | 当前主 Codex | 后台模板、后台 E2E、隔离 WLS Browser 截图 |
| 2026-08-22 17:05 | canonical_runtime_acceptance | testing_passed → testing_passed | 规范仓真实运行暴露 Review 表未升级、Theme 抢占商品路由、商品详情布局不渲染 Controller 内容三个前置缺口 | 执行正式 Schema/路由升级，并在 Theme/Product owner 中修复路由归属与 product 布局内容回退；随后完成游客图文视频提交、后台通过、前台公开回显 | 当前主 Codex | 独立 WLS `9566`、Browser DOM、媒体 HTTP 200、聚焦测试 |
| 2026-08-22 22:45 | rating_runtime_acceptance | testing_passed → testing_passed | 站点侧仅增加四项评分表单，服务端仍会丢弃扩展评分，后台也只显示总体分 | 在规范仓补齐四项必填校验、`extra_json` 写入/公开回读、原生星级单选与后台评分明细，并完成真实 BinQuery → 后台审核 → 公开回读 | 当前主 Codex | 独立 WLS `9515`、Browser DOM、数据库快照、Review E2E `2 passed` |
| 2026-08-24 17:22 | seo_rating_inject | testing_passed → implementation | 商品评论页未向 Seo 提供 reviews/AggregateRating 输入；展示名需当前语言翻译而非全量多语 | 新增 `ReviewService::seoFacts` 与 `ProductReviewSeoProfileProvider`，注入总体分汇总与抽样评论事实 | 用户确认 | Provider 契约测试、需求 `REQ-REVIEW-0003` |
| 2026-08-25 17:20 | product_widget_injection | implementation → testing | 详情内联 Hook + 摩托车深色皮肤，布局无法编排 | 新增 `product-reviews` 大部件与 `default_injections`；Theme product 布局只放评论容器；Product detail 移除直接 Hook；CSS 改主题 token | 用户 | 部件契约测试、商品详情页 |
| 2026-08-25 20:42 | editor_preview_shell | testing → testing_passed | 应用 Tab「已应用」但编辑器预览空；根因 `SlotRenderer` 硬编码 `preview_mode=false`，无 offer 时部件直接 return | 编辑器/live 传 `preview_mode=true`；部件识别 editor 参数；顺带修 media.`store_id` 与维护模式导致的验收中断 | 用户 | `/zh_Hans_CN/product/2` 含 `storefront-product-reviews` |
| 2026-08-25 21:32 | editor_preview_form | testing_passed → testing_passed | 预览壳已出但表单仍 `schemaFailed`：无 `entity_uuid` 时 JS 不调 API 直接失败 | 预览写入 `data-preview-schema`；无 UUID 时内联 `renderForm`（提交禁用）；列表失败不再冲掉已渲染表单；静态 `&v=` 与 WLS reload 后生效 | 用户 | 预览 iframe 见四项星级与字段；文案 `previewOnly` |

### 实现范围

- 变更路径/符号：`ProductReviewTypeProvider::fields/normalizeValues`、`ReviewService::list`、`ReviewAdminService::listing/ratingValues`、前后台模板、i18n、聚焦测试与功能文档。
- 数据/配置/迁移：无 Schema 变更；需要 `php bin/w setup:upgrade --route` 刷新路由。
- 兼容与回退：新增后台入口；删除新增文件并恢复 `ReviewService::list` 计数小段即可回退。

### 审查与整改

- 架构：通过；后台能力由 `Weline_Review` 自有 Controller/Service/模板提供，默认商品类型不反向依赖 WeShop。
- 缺陷：通过；保留规范框架 `getPaginationState().totalSize`，修复扩展评分只渲染不持久化、公开列表不回读及后台不可见问题。
- 安全：通过；列表/审核 ACL、CSRF、POST 参数与目标状态校验、输出转义及 MIME/大小/数量校验完整。
- 用户批准延期：none。

### 测试与验收

- 聚焦/单元/集成：Review PHP/PHTML lint 与 `php bin/w theme:compile` 通过；`ProductReviewTypeProviderTest` 为 `7 tests / 21 assertions`，`ReviewAdminSurfaceContractTest` 为 `1 test / 20 assertions`；Theme 覆盖测试 `9 tests / 25 assertions / 1 skipped`。
- 真实 WLS + WebUI：在规范框架工作树的独立 WLS `https://p05113ef3.weline.test:9566/` 验证 BinQuery 动态表单、PNG/WebM 上传、待审评论、后台审核通过与公开回读；公开结果为 5.0 分、1 条评论、1 张图片和 1 个视频，前台控制台 0 error。
- E2E 固化：显式绑定专用实例域名 `https://p05113ef3.weline.test:9515` 后，后台 `REVIEW-BACKEND-001`、`REVIEW-MODERATION-001` 共 2 项通过；另以 Browser 验证前台四项选星、必填提示、键盘操作、BinQuery 提交与公开回读。
- Browser：内置 Browser 检查后台面包屑、筛选、媒体标记、审核状态与动作列；实际发现并修复动作列溢出和浅色遮挡，复查后可读。
- 完整门禁：修复三个模板的语义 `weline-code`、SystemConfig/Websites Scope Provider 冲突并执行既有 Theme locale migration 后，规范仓 `frontend:check-section-code`、`theme:compile`、完整 `setup:upgrade --route` 均通过。

### 文档与运营交付

- 需求文档：`REQ-REVIEW-0001`、`REQ-REVIEW-0002`。
- 功能/API/架构文档：`resolve_task_context`、`doc/hook/frontend/review/product.md`。
- 运营文档：`doc/运营/评论审核.md`。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：`testing_passed`；框架侧逐文件合并保留了规范仓后台、分页、E2E 与运营文档，并完成四项评分、图文视频、后台审核及公开回读的真实运行时技术验收；临时评论、媒体与 SKU 注册夹具已精确清理。
- 完成/发布证据：任务目录 `result.md`、后台 E2E、聚焦 PHPUnit 与隔离 WLS/Browser 验收记录。
- 剩余事项与恢复条件：仅等待用户进行产品验收；本次临时评论、媒体文件和 SKU 注册夹具已精确清理。
