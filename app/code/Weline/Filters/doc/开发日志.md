## 2026-09-26 — filter 侧栏贴边补 padding（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（店面 `/products` 左栏 `category-filters` 文案与勾选贴左缘）。
- 修复：`widget-category-filters.css` 的 `.w-filters` 增加 `padding-block/inline`（`--spacing-md` / `--spacing-component-padding`）+ `box-sizing:border-box`。
- 契约：`CategoryFiltersWidgetContractTest::testWidgetCssPadsFilterPanelAwayFromSidebarEdge`。
- 验收：本机 `/products` 侧栏 `data-testid="storefront-filters-panel"` 内容不再贴边。

## 2026-09-24 — DaoCharms W158 `/products` list-filters 固化可见（P-filters）

- 现象：Website 158 侧栏仅 Filters 占位；默认站 `/products` 已有 `category-filters`。
- 根因：非 Filters 声明缺失（账本 `products/list-filters required` 已入库）；主题 products 发布指针停在非法 `d0`，店面 fragments 解析失败。
- 本席：等主题席 markPublished→`r314` 后 `widget:refresh` + `rebakeAfterInjectionCollect(4)`；未改 widget.php / 未布局内嵌。
- 验：`/daocharms/products` 侧栏 `data-widget-code="category-filters"` + `data-testid="storefront-filters-panel"`。

## 2026-09-14 — EN 筛选其它分面残留汉字（Brand / Type / Season）

- 全量扫描：组名已英文化；真正含汉字的选项为 Brand 中文店名、Type 残留 2 条、Season `winter` 大小写。
- 修复：词典补品牌拉丁转写、`【寄相思】…` / `【Shans寺桃花】…`、`winter→Winter`。
- 版本：`1.0.10` → `1.0.11`。

## 2026-09-14 — EN 筛选选项残留中文（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（Gender 下「男女同款」等选项值仍中文）。
- 修复：Filters 词典补 `男女同款→Unisex style`、`其它→Other`、纯中文形制/默认项等；`StorefrontFacetTranslator` 按 CSV mtime 失效进程内缓存。
- 验收：`/en_US/products` Gender 无「男女同款」。
- 版本：`1.0.9` → `1.0.10`。

## 2026-09-13 — EN 筛选侧栏 EAV 属性名残留中文（审图 ui_shot）

- 根因：`StorefrontEavLabelResolver` 用 `strcasecmp(name, code)` 把英文当地名 `Size`/`Material` 误判成 code 占位而丢弃；模板再 `__()` 把已英文化标签反查回中文（`Size`→`大小`）。
- 修复：精确相等才忽略 code 占位；`buildPanel` 缓存后强制 `localizePanelLabels`（resolve 回退为 value 时保留已有选项标签）；模板对分面名/选项直接输出（不再 `__()`）；FacetTranslator 优先 `RequestContext`；词典补 `大小→Size`。
- 验收：`/en_US/products` 侧栏 12 组分面摘要均为英文（`chinese_summaries=0`）；Browser 可见 Size/Material/Occasion/Style。
- 版本：`1.0.8` → `1.0.9`。

## 2026-09-08 — 缓存面板动态标签统一批量预取（1.0.8）

- 现场：独立实例 `codex-perf70` 新 Worker 请求 `b232a87f530a1eb7-98172661948916` 中，`Weline_Filters::category-filters` 渲染 414.30ms；面板 builder 阶段未执行，但模板仍逐项调用 `__()` 解析动态标签。全页公共词解析累计 266 个 DB spans / 1066 个 WLS spans，不能把这些全页计数全部归给该部件。
- 修复：`buildPanel()` 在缓存与计算路径汇合后，收集价格标签、属性名和选项标签，复用 `Parser::prefetchWords()`，不只在缓存 builder 中预热。使用 Parser 已有 L1、WLS L2 与失效周期，不添加模块自管词缓存。
- 保持：面板数组、URL、筛选值及所有模板 `__()` 不变，仍允许请求/模块词典覆盖；折叠选项同样预取，不扫描全词典。
- 测试先行：真实服务/Parser + 内存 Provider 的缓存命中用例先观察到 207 次单词读取失败，再验证改为 en_US 两批（200 + 7），后续逐词读取及重复面板读取均不再查 Provider；覆盖优先级用例通过。2 cases / 12 assertions。实际新 Worker 页面验收待主任务完成。
- 版本：`1.0.7` → `1.0.8`。

## 2026-09-05 — 店面分面 Filters 词典独立翻译（1.0.7）

- 现象：英语 `/en_US/products` 侧栏仍出现「场合」「样式」「涤纶」「汉服」等中文；`__()` 在 Product 列表请求只加载 Product 词典，Filters `i18n/*.csv` 未参与动态属性名/选项翻译。
- 修复：新增 `StorefrontFacetTranslator` 按当前 `State::getLang()` 读取 Filters CSV；面板组名与选项标签在 EAV 当地名之后再走该翻译器；补齐场合/样式/常见选项英文与阿语词条。
- 版本：`1.0.6` → `1.0.7`。

## 2026-09-05 — 筛选链接跟随语言货币前缀（1.0.6）

- 现象：英语环境 `/en_US/products` 点击价格/属性筛选跳到无语言前缀的 `/products?...`，回到默认语言；部分属性组名（如「织物」）仍中文。
- 修复：`StorefrontAttributeListingFilter::buildListingUrl` 与部门链统一经 `Url::getFrontendUrl` 生成；属性组名/选项标签优先 `StorefrontEavLabelResolver`；词典补 `织物`。
- 版本：`1.0.5` → `1.0.6`。

## 2026-09-05 — 店面 af_* 筛选生效与侧栏勾选（1.0.5）

- 现象：`/products?af_hanfu_zhi_wu=涤纶` 仍显示全部约 79 件，侧栏「涤纶」不勾选；价格筛选能缩结果但也不勾选。
- 根因：Product 未在 `event.php` 声明 `storefront_offers_filter`，注册表无事件导致观察者短路；侧栏只从 `REQUEST_URI` query 解析，WLS 下常无 query 段。
- 修复：侧栏优先 `getQueryParams()` / `QUERY_STRING` / `WELINE_ORIGIN_REQUEST_URI`；配合 Product 事件规约与 Framework `hasObservers` 扫描回退。
- 验收：FPC MISS 与 Browser 均为「第 1–1 件，共 1 件」；涤纶 `is-active`/`aria-current=true`；出现「清除筛选条件」。
- 版本：`1.0.4` → `1.0.5`。

## 2026-09-04 — 店面分面策展与中英阿渐进展示（1.0.4）

- 现象：分类页把采购、抽检、跨境出口等后台 EAV 字段全部暴露到顾客筛选栏；属性组与选项过长，商品区被挤到长筛选列表之后。
- 修复：新增 `StorefrontFacetPresentationPolicy`，按朝代、形制、性别、颜色、尺码、面料、场合、季节、风格、工艺、图案、品牌的购买决策顺序策展，并对同义属性只选用一个有值来源。
- 交互：前四组默认展开，其余渐进展开；每组先显示 6 项，保留当前已选项优先可见，长列表使用原生 `details/summary` 继续展开。
- 国际化：修正 `en_US` 词典方向，补齐 `ar_SA` 与动态属性/选项翻译入口。
- 版本：`1.0.3` → `1.0.4`。

## 2026-09-03 — 分类详情部门树展开（1.0.3）

- 澄清：`/category/*` 已注入 Filters 部件；女装等无商品链接时价格/属性面为空属数据事实，非缺注入。
- 修复：部门导航沿当前分类 `activePathIds` 展开并高亮当前节点；模板支持 depth 缩进。
- 版本：`1.0.2` → `1.0.3`。

## 2026-09-03 — EAV 属性面：offer 投影回填（1.0.2）

- 澄清：不是 EAV「没向 Filters 推过滤项」——Filters 主动拉 `AttributeFilterService`。
- 现象：侧栏无属性组。EAV 有 `frontend_is_filterable` 定义，但经典值表空；汉服目录属性在 offer `brand`/`specifications`。
- 修复：EAV `Value::getTable` 兼容 EntityDefinition 实体；Filters 在 EAV counts 空时用 offer 投影计面；`af_*` 按 offer 值过滤。
- 版本：`1.0.1` → `1.0.2`。

## 2026-09-06 — 筛选面板统一 timing 标签

- `buildPanel` 沿用原公开签名，将原计算委托给私有方法，通过 Framework `measurePhase` 记录 panel 总耗时，以及部门、元数据、EAV统计、offer统计、标签/选项URL组装子阶段。
- 统计在明细截断后仍保留，包含各阶段DB/WLS调用增量；均为inclusive，父子不可相加。原结果、异常容错和筛选URL行为不变，未新增业务缓存。
- 最小回归、固定面板输出及真实HTTP日志见 `/private/tmp/weline-timing/acceptance-report.md`；测量结果用于决定下一步优化。
- 交付地址：https://p05113ef3.test.weline.com:9555/en_US/products?nocache=1&wls_trace=1 。无UI变更。

## 2026-09-03 — Filters 部件补齐 offers 自解析与壳 class（1.0.1）

- 现象：侧栏已注入但仅部门树，价格/属性为空；健康检查 `broken_widget_shell`。
- 根因：Theme 部件渲染 `unsetData` 后页面 `storefront_offers*` 未回注到 Filters；根节点缺 `wc-theme_widget_category_filters`。
- 修复：`StorefrontFilterPanelService::resolveListingOffers`；模板回落自解析；根 class 补齐。Theme `capturePageRenderContext` 增 `storefront_offers_unfiltered`/`storefront_category`。
- 版本：`1.0.0` → `1.0.1`。

## 2026-09-03 — 分类侧栏 Filters 部件注入（1.0.0）

- 当前状态：`implemented_pending_browser_acceptance`。
- 需求：用户要求分类布局左侧留下 slot，由 Filters 模块经部件注入，并具备属性筛选。
- 实现：新建 `Weline_Filters`；`category-filters` 部件 `default_injections`→`category-filters`/`list-filters`；面板含部门、价格、EAV 属性；Theme 布局去掉 getHook 占槽。
- 版本：`Weline_Filters 1.0.0`。
- 前台地址：https://p05113ef3.test.weline.com:9555/categories 、https://p05113ef3.test.weline.com:9555/category/women
