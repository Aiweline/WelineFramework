# 停工（已解除）

- 解除：2026-09-19 用户选择「只留 Widget 数组项」
- 接着做：技术方案波

- 从哪一波停：立项会之后，技术方案波之前
- 确认后从：技术方案波（架构师）接着做

## 矛盾

部件多项设置有两套外壳，而且都在用：

- 主题编辑器主路径：`theme-editor.js` 的 `.array-item` 卡片（轮播图片现在长这样）
- 布局配置：Widget `ArrayType` 的 `.w-param-array`

选图已经都嵌了同一个 file-picker。项上的标题、按钮等多语言包装只在 PHP 的 `ArrayType` 里有。主题编辑器那套卡片不包。

## 为什么定不了

这是两套都成立、又互斥的扩展点。收成一套就会停掉另一套的主路径。需求分析不能替用户选使用面以外的实现归属；领域探查明确不能拍板外壳留在 Theme 还是收到 Widget。

## 请确认

1. 项外壳只留 Widget 的数组项契约，主题编辑器不再自绘第二套卡片。单文件和多文件都用现有 file-picker，不新建选图控件。调用组件时用参数声明指定 item 字段和字段 UI。
2. 或者你要保留主题编辑器自己的卡片，只把选图壳统一。这样标题/按钮的多语言还得在编辑器卡片上再做一遍，以后仍有两套项外壳。

用户原话已确认、本停工不再问：同一套选择 UI、项内可扩展字段、字段装在 item 上并配置多语言、使用组件时指定字段 UI、覆盖以后的同类部件设置。
