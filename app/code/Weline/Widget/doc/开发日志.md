# Weline_Widget 开发日志

## Hotfix — 2026-09-01：REQ-THEME-0014 默认安装对账（1.0.4）

- 当前状态：`implemented`
- 变更：账本 `created|updated` 且含 `default_injections` 均进入 `widget_install_after` 对账；文档明确 `required` = 默认安装 / 推荐。
- 版本：`1.0.3` → `1.0.4`

## Hotfix — 2026-08-31：参数表单身份属性对齐 node_uid（1.0.3）

- 当前状态：`partial`（配合 Theme 绿field；非独立产品完成）
- 变更：
  1. `AbstractParamType::widgetIdentityAttrMap/Html`：32-hex 只写 `data-node-uid`；非 hex 写 `data-layout-id`
  2. i18n 按钮/面板与 `ParamTypeRenderer` 表单/删除按钮改走助手，去掉硬编码双写
- 版本：`1.0.2` → `1.0.3`（`register.php` 与 `etc/module.php` 对齐）
- 证据：Theme CopyScoped `testWidgetParamTypePrefersNodeUidIdentityAttrs`；助手运行时断言 hex/数字分流

## 2026-08-30 — 含 challenge_token 的部件 HTML 禁止请求内缓存

- 当前状态：`implemented`
- 变更：`Taglib/Widget.php` 对含 `challenge_token` / `data-w-challenge-token` 的输出与 `form_key` 一样跳过 render cache。
- 版本：`1.0.1` → `1.0.2`。

## 当前开发线（版本号待确认）

- 关联需求：REQ-WIDGET-0001
- 状态：本次回归修复已验收
- 功能文档：`doc/README.md`、`doc/使用手册.md`
- 运营文档：N/A；本次恢复既有参数控件兼容，不新增运营流程、权限或数据操作。

### 2026-08-29 图片选择比例硬约束

| 阶段 | 状态 | 证据 |
|---|---|---|
| 需求 | 已确认 | 选择 UI 标死比例并透传；MediaManager 按比例硬拦。 |
| 实现 | 已完成 | `media_options.aspect_ratio` / 成对 recommend 推导；预览框 `aspect-ratio` + 角标；打开 URL 携带 `aspect_ratio` 并 Toast。 |
| 聚焦测试 | 待跑 | ParamTypeRenderer 契约断言 `data-aspect-ratio`。 |

### 2026-08-22 图片参数媒体选择兼容

| 阶段 | 状态 | 证据 |
|---|---|---|
| 需求基线 | 已确认 | 用户确认“之前可以”，模块 README 与 `ImageType` 源码均证明图片选择是既有参数能力。 |
| 问题定位 | 已完成 | Theme Editor 已恢复参数字段后，`image` 控件输出 `.w-param-image-select`，而当前媒体脚本绑定 `.w-param-media-image-select`，导致“选择”按钮无响应。 |
| 实现 | 已完成 | `ImageType` 保留旧 class 并补通用媒体选择 class 与目录参数，复用现有同源媒体管理器；聚焦断言已加入现有 Widget 参数测试。 |
| 架构/缺陷/安全复核 | 已通过 | 首轮低风险类型边界已整改；复审确认 Widget 持有控件契约、Theme 复用公开表单，同源媒体地址、ACL、持久化与校验边界不变。 |
| 聚焦测试 | 已通过 | Widget 参数归一化用例 `13 tests / 79 assertions`；相关 PHP 语法和目标差异检查通过。 |
| 真实路径验收 | 已通过 | 用户指定的 9555 Theme Editor 当前 4 个 Banner 均有媒体入口；layout `687` 成功打开同源 MediaManager，默认目录为 `banner`，关闭且未选择或保存数据。 |
| 发布 | 未请求 | 不提交、不推送、不分发站点。 |

## 历史状态

模块此前缺少固定开发日志；无法由仓库验证的历史版本和阶段不补写，统一待确认。
