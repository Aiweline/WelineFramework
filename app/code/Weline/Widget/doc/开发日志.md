## 2026-09-23 — 默认注入结构差异与注册兼容（1.0.12）

- 注册刷新只为结构声明差异派发注入变更，覆盖可选声明和最后一条删除；配置、说明变更不触发结构固化。
- 模板路径作数组键的参数覆盖格式现在参与扫描；旧 `page_layouts` 按布局展开且不改变 required 语义。
- 真实刷新更新1条并派发事件；关联定向测试通过。完整主题浏览器验收见 Theme 开发日志，尚未通过。

# Weline_Widget 开发日志

# Weline_Widget 开发日志

## 2026-09-17 — 多语言包装改为 Weline.UI.dialog

- `AbstractParamType::renderI18nPanel`：空面板改为 `<dialog class="w-dialog w-param-i18n-dialog">`（surface/header/body/footer）。
- `widget-param-types.css`：内联面板样式改为弹窗尺寸与滚动；同步 Theme 静态副本。
- 契约：`I18nDialogPanelContractTest`（数组 intro textarea 无 `data-i18n-media`）。

## 2026-09-16 — 可选 ParamType `product_picker`


- `ParamTypeRenderer` 增加 `OPTIONAL_TYPE_CLASSES`：`product_picker` → Product `ProductPickerType`（模块不在场则归一为 text）。
- `widget-param-types.js`：`initProductAdminPickers` 调用 `Weline.Product.AdminPicker.mount`。
- 验证：`ProductPickerTypeContractTest`；`resource:compile welineUi`。

## 2026-09-16 — media_image 改用 WelineMedia/file-picker

- `AbstractParamType::renderMediaLibraryPickerHtml`：优先 `WelineMedia` + `value_mode=file-image`；block 渲染失败（如 CLI）回退旧 `.w-param-media-image-select`。
- `MediaImageType`/`ImageType` 共用该渲染；仍保留隐藏 select 供 i18n 媒体面板识别。
- 验证：`ParamTypeRendererRenderNormalizationTest`。

## 2026-09-16 — 图片 UI 默认多语言（1.0.9）

- `ParamDefinition`：`media_image`/`image`/`image_picker`/`file_image`（含 `ui_type`）默认可译；`i18n=false` 关闭。
- `AbstractParamType` 多语言面板对图片字段隐藏 AI，并标记 `data-i18n-media`。

## Fix — 2026-09-06：认证回跳相关部件禁止请求内 HTML 缓存

- 需求：含 `redirect_url` / `data-social-quick` 的部件不得复用空回跳 HTML。
- 变更：`Taglib/Widget::isCacheableWidgetHtml` 额外排除 `name="redirect_url"`、`data-social-quick`、`data-w-auth-return`。
- 版本：`1.0.6` → `1.0.7`

## Hotfix — 2026-09-01：REQ-THEME-0014 默认安装对账（1.0.4）

- 当前状态：`implemented`
- 变更：账本 `created|updated` 且含 `default_injections` 均进入 `widget_install_after` 对账；文档明确 `required` = 默认安装 / 推荐。
- 版本：`1.0.3` → `1.0.4`

## Hotfix — 2026-08-31：参数表单身份属性对齐 node_uid（1.0.3）

- 当前状态：`partial`（配合 Theme 绿field；非独立产品完成）
- 变更：
  1. `AbstractParamType::widgetIdentityAttrMap/Html`：32-hex 只写 `data-node-uid`；非 hex 写 `data-layout-id`
  2. i18n 按钮/面板与 `ParamTypeRenderer` 表单/删除按钮改走助手，去掉硬编码双写
- 版本：`1.0.2` → `1.0.3`（`register.php` 与 `etc/module.php` 对齐）
- 证据：Theme CopyScoped `testWidgetParamTypePrefersNodeUidIdentityAttrs`；助手运行时断言 hex/数字分流

## 2026-08-30 — 含 challenge_token 的部件 HTML 禁止请求内缓存

- 当前状态：`implemented`
- 变更：`Taglib/Widget.php` 对含 `challenge_token` / `data-w-challenge-token` 的输出与 `form_key` 一样跳过 render cache。
- 版本：`1.0.1` → `1.0.2`。

## 当前开发线（版本号待确认）

- 关联需求：REQ-WIDGET-0001
- 状态：本次回归修复已验收
- 功能文档：`doc/README.md`、`doc/使用手册.md`
- 运营文档：N/A；本次恢复既有参数控件兼容，不新增运营流程、权限或数据操作。

### 2026-08-29 图片选择比例硬约束

| 阶段 | 状态 | 证据 |
|---|---|---|
| 需求 | 已确认 | 选择 UI 标死比例并透传；MediaManager 按比例硬拦。 |
| 实现 | 已完成 | `media_options.aspect_ratio` / 成对 recommend 推导；预览框 `aspect-ratio` + 角标；打开 URL 携带 `aspect_ratio` 并 Toast。 |
| 聚焦测试 | 待跑 | ParamTypeRenderer 契约断言 `data-aspect-ratio`。 |

### 2026-09-02 AI Widget 面板契约回归修复（1.0.6）

| 阶段 | 状态 | 证据 |
|---|---|---|
| 问题定位 | 已完成 | `theme:compile welineUi` 证明 Theme 编辑器 AI Widget 的权威源属于 Weline_Widget；旧源码缺少统一 Toast、独立面板结构函数及可访问状态区，编译后触发 Theme 能力契约失败。 |
| 实现 | 已完成 | 新增 `showAiWidgetToast` 与 `buildAiWidgetPanelMarkup`；面板底部改用 Weline UI dialog footer，状态区加入 `role="status"` / `aria-live="polite"`，生成校验、保存、放置与失败均提供 Weline UI Toast。 |
| 版本 | 已完成 | `etc/module.php` 与 `register.php` 对齐为 `1.0.6`。 |
| 参数渲染 | 已完成 | `resolveUiType` 改为显式 `ui_type` → `input` → `ui` → semantic `type`，修复颜色/媒体字段被文本框吞掉。 |
| Unit 装配 | 已完成 | `ParamSchemaRegistryExpandTest` 显式注入 `ParamSchemaScanner` mock，不再依赖未初始化的全局 ObjectManager。 |
| 聚焦测试 | 待执行 | 重新编译 Weline UI 后运行 Theme 编辑器能力、发布版本、作用域配置与嵌套部件注入回归。 |
| 真实路径验收 | 待执行 | 使用本机内置浏览器验证 AI Widget 面板、主题范围编辑、预览与发布路径。 |
| 交付地址 | 待验收 | 前台：https://p05113ef3.test.weline.com:9555/ ；后台主题编辑器：https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/theme/backend/theme-editor?theme_id=1&page_type=homepage&layout_option=default&editor_area=frontend&preview_area=frontend&status=draft&interaction_mode=edit |

### 2026-08-22 图片参数媒体选择兼容

| 阶段 | 状态 | 证据 |
|---|---|---|
| 需求基线 | 已确认 | 用户确认“之前可以”，模块 README 与 `ImageType` 源码均证明图片选择是既有参数能力。 |
| 问题定位 | 已完成 | Theme Editor 已恢复参数字段后，`image` 控件输出 `.w-param-image-select`，而当前媒体脚本绑定 `.w-param-media-image-select`，导致“选择”按钮无响应。 |
| 实现 | 已完成 | `ImageType` 保留旧 class 并补通用媒体选择 class 与目录参数，复用现有同源媒体管理器；聚焦断言已加入现有 Widget 参数测试。 |
| 架构/缺陷/安全复核 | 已通过 | 首轮低风险类型边界已整改；复审确认 Widget 持有控件契约、Theme 复用公开表单，同源媒体地址、ACL、持久化与校验边界不变。 |
| 聚焦测试 | 已通过 | Widget 参数归一化用例 `13 tests / 79 assertions`；相关 PHP 语法和目标差异检查通过。 |
| 真实路径验收 | 已通过 | 用户指定的 9555 Theme Editor 当前 4 个 Banner 均有媒体入口；layout `687` 成功打开同源 MediaManager，默认目录为 `banner`，关闭且未选择或保存数据。 |
| 发布 | 未请求 | 不提交、不推送、不分发站点。 |

## 历史状态

模块此前缺少固定开发日志；无法由仓库验证的历史版本和阶段不补写，统一待确认。
