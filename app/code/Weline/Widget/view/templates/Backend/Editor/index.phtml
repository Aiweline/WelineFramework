<?php
/**@var \Weline\Framework\View\Template $this */
/**@var \Weline\Backend\Block\ThemeConfig $themeConfig */
$themeConfig = \Weline\Framework\Manager\ObjectManager::getInstance(\Weline\Backend\Block\ThemeConfig::class);
$page = $this->getData('page');
$widgets = $this->getData('widgets') ?? [];
$widgetsByType = $this->getData('widgetsByType') ?? [];
$allowedTypes = $this->getData('allowedTypes') ?? [];
?>
<!DOCTYPE html>
<!--
 *@Author       秋枫雁飞
 *@Email        aiweline@qq.com
 *@Forum        https://bbs.aiweline.com
 *@DESC         可视化编辑器
-->
<html lang='en' <?= ('rtl' === $themeConfig->getThemeModel()) ? " dir=\"rtl\"" : '' ?>>

<head>
    @template(Weline_Admin::common/head.phtml)
    <title><?= $page_title ?? __('可视化编辑器') ?></title>
    <link rel="stylesheet" href="<?= $this->getStaticUrl('Weline_Widget::css/editor.css') ?>">
</head>

<body <?= $themeConfig->getLayouts() ?? ''; ?>>

<!--页头工具条 开始-->
@template(Weline_Admin::common/page/loading.phtml)
<!--页头工具条 结束-->

<!-- 开始 页面 -->
<div id="layout-wrapper">

    <!--页头工具条 开始-->
    <w:block class='Weline\Admin\Block\Backend\Page\Topbar'/>
    <!--页头工具条 结束-->

    <!-- ========== Topnav Start ========== -->
    <!--页头菜单栏 开始-->
    <w:block class='Weline\Admin\Block\Backend\Page\Topnav'/>
    <!--页头菜单栏 结束-->

    <!-- ========== Left Sidebar Start ========== -->
    <!--左侧菜单栏 开始-->
    @template(Weline_Admin::common/left-sidebar.phtml)
    <!--左侧菜单栏 结束-->

    <!-- ============================================================== -->
    <!-- Start right Content here -->
    <!-- ============================================================== -->
    <div class="main-content">

        <div class='page-content'>
            <div class="container-fluid">
                
                <!-- 页面标题 -->
                <div class="row">
                    <div class="col-12">
                        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                            <h4 class="mb-sm-0"><?= $page_title ?? __('可视化编辑器') ?></h4>
                            <div class="page-title-right">
                                <ol class="breadcrumb m-0">
                                    <li class="breadcrumb-item">
                                        <a href="javascript: void(0);"><?= $breadcrumb_parent ?? __('内容管理') ?></a>
                                    </li>
                                    <li class="breadcrumb-item active"><?= $breadcrumb_current ?? __('可视化编辑器') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 编辑器工具栏 -->
                <div class="row mb-3">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <button type="button" class="btn btn-primary" id="save-page-btn">
                                            <i class="ri-save-line me-1"></i><?= __('保存') ?>
                                        </button>
                                        <button type="button" class="btn btn-secondary" id="preview-page-btn">
                                            <i class="ri-eye-line me-1"></i><?= __('预览') ?>
                                        </button>
                                    </div>
                                    <div>
                                        <input type="text" class="form-control d-inline-block" id="page-title-input" 
                                               placeholder="<?= __('页面标题') ?>" 
                                               value="<?= htmlspecialchars($page ? $page->getData('title') : '') ?>"
                                               style="width: 200px;">
                                        <input type="text" class="form-control d-inline-block ms-2" id="page-handle-input" 
                                               placeholder="<?= __('页面标识') ?>" 
                                               value="<?= htmlspecialchars($page ? $page->getData('handle') : '') ?>"
                                               style="width: 200px;">
                                        <input type="hidden" id="page-id" value="<?= $pageId ?>">
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 编辑器主体 -->
                <div class="row">
                    <!-- 左侧：部件选择面板 -->
                    <div class="col-md-3">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title mb-0"><?= __('部件库') ?></h5>
                            </div>
                            <div class="card-body p-0">
                                <div class="widget-panel">
                                    <div class="input-group p-2">
                                        <input type="text" class="form-control" id="widget-search" 
                                               placeholder="<?= __('搜索部件...') ?>">
                                    </div>
                                    <div class="widget-list" id="widget-list">
                                        <?php foreach ($widgetsByType as $type => $typeWidgets): ?>
                                            <div class="widget-type-group" data-type="<?= htmlspecialchars($type) ?>">
                                                <div class="widget-type-header" data-bs-toggle="collapse" 
                                                     data-bs-target="#widget-type-<?= htmlspecialchars($type) ?>">
                                                    <i class="ri-arrow-down-s-line"></i>
                                                    <strong><?= htmlspecialchars(ucfirst($type)) ?></strong>
                                                    <span class="badge bg-secondary"><?= count($typeWidgets) ?></span>
                                                </div>
                                                <div class="collapse show" id="widget-type-<?= htmlspecialchars($type) ?>">
                                                    <div class="widget-type-items">
                                                        <?php foreach ($typeWidgets as $name => $widget): ?>
                                                            <div class="widget-item" 
                                                                 data-type="<?= htmlspecialchars($type) ?>" 
                                                                 data-name="<?= htmlspecialchars($name) ?>"
                                                                 data-widget='<?= json_encode($widget, JSON_UNESCAPED_UNICODE) ?>'>
                                                                <div class="widget-item-name"><?= htmlspecialchars($widget['name'] ?? $name) ?></div>
                                                                <div class="widget-item-desc"><?= htmlspecialchars($widget['description'] ?? '') ?></div>
                                                                <div class="widget-item-module">
                                                                    <small class="text-muted"><?= htmlspecialchars($widget['module'] ?? '') ?></small>
                                                                </div>
                                                            </div>
                                                        <?php endforeach; ?>
                                                    </div>
                                                </div>
                                            </div>
                                        <?php endforeach; ?>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- 中间：画布区域 -->
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title mb-0"><?= __('画布') ?></h5>
                            </div>
                            <div class="card-body">
                                <div class="editor-canvas" id="editor-canvas">
                                    <div class="canvas-placeholder" id="canvas-placeholder">
                                        <i class="ri-drag-move-2-line" style="font-size: 48px; color: #ccc;"></i>
                                        <p class="text-muted"><?= __('从左侧拖放部件到这里') ?></p>
                                    </div>
                                    <div class="canvas-widgets" id="canvas-widgets">
                                        <!-- 部件将在这里显示 -->
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- 右侧：属性配置面板 -->
                    <div class="col-md-3">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title mb-0"><?= __('属性配置') ?></h5>
                            </div>
                            <div class="card-body">
                                <div class="widget-properties" id="widget-properties">
                                    <div class="text-muted text-center py-5">
                                        <i class="ri-settings-3-line" style="font-size: 48px;"></i>
                                        <p><?= __('选择一个部件进行配置') ?></p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>

        <!-- 页脚 -->
        @template(Weline_Admin::common/footer.phtml)

    </div>
    <!-- end main content-->

</div>
<!-- END layout-wrapper -->

<!-- 代码编辑器模态框 -->
<div class="modal fade" id="code-editor-modal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"><?= __('编辑代码') ?></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <textarea class="form-control" id="code-editor" rows="20" 
                          placeholder='<w:widget type="header" name="default" />'></textarea>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"><?= __('取消') ?></button>
                <button type="button" class="btn btn-primary" id="code-editor-save"><?= __('保存') ?></button>
            </div>
        </div>
    </div>
</div>

@template(Weline_Admin::common/foot.phtml)
<script src="<?= $this->getStaticUrl('Weline_Widget::js/editor.js') ?>"></script>
<script>
    // 初始化编辑器
    document.addEventListener('DOMContentLoaded', function() {
        if (typeof WidgetEditor !== 'undefined') {
            const editor = new WidgetEditor({
                pageId: <?= $pageId ?>,
                pageContent: <?= json_encode($pageContent, JSON_UNESCAPED_UNICODE) ?>,
                widgets: <?= json_encode($widgets, JSON_UNESCAPED_UNICODE) ?>,
                saveUrl: '<?= $this->getUrlBuilder()->getBackendUrl('widget/backend/editor/save') ?>',
                previewUrl: '<?= $this->getUrlBuilder()->getBackendUrl('widget/backend/preview/widget') ?>'
            });
            widgetEditor = editor;
            editor.init();
        }
    });
</script>

</body>
</html>

