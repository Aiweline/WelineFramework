<?php
/**@var \Weline\Framework\View\Template $this */

$page = $this->getData('page');
$widgets = $this->getData('widgets') ?? [];
$widgetsByType = $this->getData('widgetsByType') ?? [];
$allowedTypes = $this->getData('allowedTypes') ?? [];
$jsonAttribute = static function (mixed $value): string {
    $json = \json_encode(
        $value,
        JSON_UNESCAPED_UNICODE | JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT
    ) ?: '{}';
    return htmlspecialchars($json, ENT_QUOTES, 'UTF-8');
};
?>
<!-- 页面标题 -->
                <div class="w-grid">
                    <div style="--w-span:12;">
                        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
                            <h4 style="--w-mb-sm:0;"><?= $page_title ?? __('可视化编辑器') ?></h4>
                            <div class="w-cluster">
                                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                                    <li class="w-breadcrumb__item">
                                        <span><?= $breadcrumb_parent ?? __('内容管理') ?></span>
                                    </li>
                                    <li class="w-breadcrumb__item" data-state="active"><?= $breadcrumb_current ?? __('可视化编辑器') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 编辑器工具栏 -->
                <div class="w-grid" style="--w-mb:var(--weline-space-4);">
                    <div style="--w-span:12;">
                        <div class="w-card">
                            <div class="w-card__body">
                                <div class="w-cluster" data-justify="between" data-align="center">
                                    <div>
                                        <button type="button" class="w-button" id="save-page-btn" data-tone="primary">
                                            <w:icon name="save" size="sm"></w:icon><?= __('保存') ?>
                                        </button>
                                        <button type="button" class="w-button" id="preview-page-btn" data-tone="neutral">
                                            <w:icon name="eye" size="sm"></w:icon><?= __('预览') ?>
                                        </button>
                                    </div>
                                    <div>
                                        <input type="text" class="w-input" id="page-title-input" placeholder="<?= __('页面标题') ?>" value="<?= htmlspecialchars($page ? $page->getData('title') : '') ?>" style="width: 200px;" data-w-display="inline-block">
                                        <input type="text" class="w-input ms-2" id="page-handle-input" placeholder="<?= __('页面标识') ?>" value="<?= htmlspecialchars($page ? $page->getData('handle') : '') ?>" style="width: 200px;" data-w-display="inline-block">
                                        <input type="hidden" id="page-id" value="<?= $pageId ?>">
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 编辑器主体 -->
                <div class="w-grid">
                    <!-- 左侧：部件选择面板 -->
                    <div style="--w-span-md:3;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <h5 class="w-card__title" style="--w-mb:0;"><?= __('部件库') ?></h5>
                            </div>
                            <div class="w-card__body" style="--w-pt:0;--w-pe:0;--w-pb:0;--w-ps:0;">
                                <div class="widget-panel">
                                    <div class="w-field__control" style="--w-pt:var(--weline-space-2);--w-pe:var(--weline-space-2);--w-pb:var(--weline-space-2);--w-ps:var(--weline-space-2);">
                                        <input type="text" class="w-input" id="widget-search" placeholder="<?= __('搜索部件...') ?>">
                                    </div>
                                    <div class="widget-list" id="widget-list">
                                        <?php foreach ($widgetsByType as $type => $typeWidgets): ?>
                                            <div class="widget-type-group" data-type="<?= htmlspecialchars($type) ?>">
                                                <div class="widget-type-header" data-w-target="#widget-type-<?= htmlspecialchars($type) ?>" data-w-action="disclosure.toggle">
                                                    <w:icon name="arrow-down" size="sm"></w:icon>
                                                    <strong><?= htmlspecialchars(ucfirst($type)) ?></strong>
                                                    <span class="w-badge" data-w-background="muted"><?= count($typeWidgets) ?></span>
                                                </div>
                                                <div class="w-disclosure__panel" id="widget-type-<?= htmlspecialchars($type) ?>" data-w-disclosure-panel>
                                                    <div class="widget-type-items">
                                                        <?php foreach ($typeWidgets as $name => $widget): ?>
                                                            <div class="widget-item" 
                                                                 data-type="<?= htmlspecialchars($type) ?>" 
                                                                 data-name="<?= htmlspecialchars($name) ?>"
                                                                 data-widget='<?= $jsonAttribute($widget) ?>'>
                                                                <div class="widget-item-name"><?= htmlspecialchars($widget['name'] ?? $name) ?></div>
                                                                <div class="widget-item-desc"><?= htmlspecialchars($widget['description'] ?? '') ?></div>
                                                                <div class="widget-item-module">
                                                                    <small class="w-text" data-tone="muted"><?= htmlspecialchars($widget['module'] ?? '') ?></small>
                                                                </div>
                                                            </div>
                                                        <?php endforeach; ?>
                                                    </div>
                                                </div>
                                            </div>
                                        <?php endforeach; ?>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- 中间：画布区域 -->
                    <div style="--w-span-md:6;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <h5 class="w-card__title" style="--w-mb:0;"><?= __('画布') ?></h5>
                            </div>
                            <div class="w-card__body">
                                <div class="editor-canvas" id="editor-canvas">
                                    <div class="canvas-placeholder" id="canvas-placeholder">
                                        <w:icon name="circle" size="sm"></w:icon>
                                        <p class="w-text" data-tone="muted"><?= __('从左侧拖放部件到这里') ?></p>
                                    </div>
                                    <div class="canvas-widgets" id="canvas-widgets">
                                        <!-- 部件将在这里显示 -->
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- 右侧：属性配置面板 -->
                    <div style="--w-span-md:3;">
                        <div class="w-card">
                            <div class="w-card__header">
                                <h5 class="w-card__title" style="--w-mb:0;"><?= __('属性配置') ?></h5>
                            </div>
                            <div class="w-card__body">
                                <div class="widget-properties" id="widget-properties">
                                    <div class="w-text" data-align="center" data-tone="muted" style="--w-pt:var(--weline-space-12);--w-pb:var(--weline-space-12);">
                                        <w:icon name="settings" size="sm"></w:icon>
                                        <p><?= __('选择一个部件进行配置') ?></p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
