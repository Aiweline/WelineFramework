# WLS (Weline Server) 架构图

## 概述

WLS 是 Weline Framework 的高性能常驻内存 HTTP/HTTPS 服务器，支持多种运行模式和攻击防护能力。

## 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                                    WLS 服务器架构                                        │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                         │
│  ┌─────────────────────────────────────────────────────────────────────────────────┐   │
│  │                              客户端请求入口                                       │   │
│  │                         (HTTPS :443 / HTTP :80)                                  │   │
│  └─────────────────────────────────────────────────────────────────────────────────┘   │
│                                         │                                              │
│                                         ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────────────────┐   │
│  │                                                                                  │   │
│  │                          ┌─────────────────────────┐                            │   │
│  │                          │     Master Process      │                            │   │
│  │                          │      (主控进程)          │                            │   │
│  │                          │  - 进程生命周期管理       │                            │   │
│  │                          │  - 信号处理              │                            │   │
│  │                          │  - 热重载控制            │                            │   │
│  │                          └───────────┬─────────────┘                            │   │
│  │                                      │                                          │   │
│  │                    ┌─────────────────┴─────────────────┐                        │   │
│  │                    ▼                                   ▼                        │   │
│  │  ┌─────────────────────────────────┐  ┌─────────────────────────────────────┐  │   │
│  │  │    TCP Passthrough Dispatcher   │  │    HTTP Redirect Worker (可选)      │  │   │
│  │  │        (透传分流器)              │  │     (HTTP→HTTPS 重定向)             │  │   │
│  │  │                                 │  │          监听 :80                   │  │   │
│  │  │  ┌───────────────────────────┐  │  └─────────────────────────────────────┘  │   │
│  │  │  │    攻击探测模块            │  │                                          │   │
│  │  │  │    AttackDetector         │  │                                          │   │
│  │  │  │  ┌─────────────────────┐  │  │                                          │   │
│  │  │  │  │ 规则引擎             │  │  │                                          │   │
│  │  │  │  │ - 频率限制          │  │  │                                          │   │
│  │  │  │  │ - 路径扫描检测       │  │  │                                          │   │
│  │  │  │  │ - 恶意特征匹配       │  │  │                                          │   │
│  │  │  │  │ - User-Agent 检测   │  │  │                                          │   │
│  │  │  │  │ - 保护路径          │  │  │                                          │   │
│  │  │  │  │ - Slowloris 检测    │  │  │                                          │   │
│  │  │  │  └─────────────────────┘  │  │                                          │   │
│  │  │  └───────────────────────────┘  │                                          │   │
│  │  │                                 │                                          │   │
│  │  │  ┌───────────────────────────┐  │                                          │   │
│  │  │  │    SNI 解析模块           │  │                                          │   │
│  │  │  │    SniParser              │  │                                          │   │
│  │  │  │  - MSG_PEEK 读取          │  │                                          │   │
│  │  │  │  - ClientHello 解析       │  │                                          │   │
│  │  │  │  - SNI 提取               │  │                                          │   │
│  │  │  └───────────────────────────┘  │                                          │   │
│  │  │                                 │                                          │   │
│  │  │  ┌───────────────────────────┐  │                                          │   │
│  │  │  │    路由缓存服务           │  │                                          │   │
│  │  │  │    RoutingCacheService    │  │                                          │   │
│  │  │  │  - SNI → Worker 映射      │  │                                          │   │
│  │  │  │  - IP → Worker 映射       │  │                                          │   │
│  │  │  │  - 连接 ID 映射           │  │                                          │   │
│  │  │  │  - 学习模式 (从响应学习)   │  │                                          │   │
│  │  │  └───────────────────────────┘  │                                          │   │
│  │  │                                 │                                          │   │
│  │  └────────────────┬────────────────┘                                          │   │
│  │                   │                                                            │   │
│  │     ┌─────────────┼─────────────┬─────────────┬─────────────┐                 │   │
│  │     │             │             │             │             │                 │   │
│  │     ▼             ▼             ▼             ▼             ▼                 │   │
│  │  ┌──────┐      ┌──────┐     ┌──────┐     ┌──────┐     ┌──────┐               │   │
│  │  │Worker│      │Worker│     │Worker│     │Worker│     │ ...  │               │   │
│  │  │ :9501│      │ :9502│     │ :9503│     │ :9504│     │      │               │   │
│  │  │      │      │      │     │      │     │      │     │      │               │   │
│  │  │ SSL  │      │ SSL  │     │ SSL  │     │ SSL  │     │ SSL  │               │   │
│  │  │握手  │      │握手  │     │握手  │     │握手  │     │握手  │               │   │
│  │  │      │      │      │     │      │     │      │     │      │               │   │
│  │  │ HTTP │      │ HTTP │     │ HTTP │     │ HTTP │     │ HTTP │               │   │
│  │  │处理  │      │处理  │     │处理  │     │处理  │     │处理  │               │   │
│  │  │      │      │      │     │      │     │      │     │      │               │   │
│  │  │Route │      │Route │     │Route │     │Route │     │Route │               │   │
│  │  │Hint  │      │Hint  │     │Hint  │     │Hint  │     │Hint  │               │   │
│  │  │自报告│      │自报告│     │自报告│     │自报告│     │自报告│               │   │
│  │  └──────┘      └──────┘     └──────┘     └──────┘     └──────┘               │   │
│  │                                                                               │   │
│  └───────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

## 攻击检测与 CDN 联动架构（非阻塞模式）

采用文件信号 + Cron 定时任务的方式，完全解耦攻击检测和 CDN 通知流程，避免阻塞请求。

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                         攻击检测与 CDN 联动流程（非阻塞）                                  │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                         │
│  ┌──────────────────┐                                                                  │
│  │   恶意请求入口    │                                                                  │
│  │  (攻击者 IP)      │                                                                  │
│  └────────┬─────────┘                                                                  │
│           │                                                                            │
│           ▼                                                                            │
│  ┌────────────────────────────────────────────────────────────────┐                    │
│  │                     Dispatcher 层                               │                    │
│  │  ┌──────────────────────────────────────────────────────────┐  │                    │
│  │  │                  AttackDetector                           │  │                    │
│  │  │                                                           │  │                    │
│  │  │   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │  │                    │
│  │  │   │ 频率限制    │    │ 路径扫描    │    │ 恶意特征    │  │  │                    │
│  │  │   │ rate_limit  │    │ path_scan   │    │ malicious   │  │  │                    │
│  │  │   │ 100次/10秒  │    │ 20次404     │    │ SQL/XSS     │  │  │                    │
│  │  │   └─────────────┘    └─────────────┘    └─────────────┘  │  │                    │
│  │  │                                                           │  │                    │
│  │  │   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │  │                    │
│  │  │   │ User-Agent  │    │ 保护路径    │    │ Slowloris   │  │  │                    │
│  │  │   └─────────────┘    └─────────────┘    └─────────────┘  │  │                    │
│  │  │                                                           │  │                    │
│  │  └──────────────────────────┬───────────────────────────────┘  │                    │
│  │                             │                                   │                    │
│  │              ┌──────────────┴───────────────┐                   │                    │
│  │              │                              │                   │                    │
│  │              ▼                              ▼                   │                    │
│  │     ┌────────────────┐            ┌────────────────┐            │                    │
│  │     │  是 (阻断)     │            │  否 (放行)     │            │                    │
│  │     │                │            │                │            │                    │
│  │     │ 1. 返回 403   │            │ 转发到 Worker  │            │                    │
│  │     │ 2. 写入信号   │            │                │            │                    │
│  │     │    文件(非阻塞)│            └────────────────┘            │                    │
│  │     └───────┬────────┘                                          │                    │
│  │             │                                                   │                    │
│  └─────────────┼───────────────────────────────────────────────────┘                    │
│                │                                                                        │
│                ▼                                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐                    │
│  │              攻击信号文件 (非阻塞写入)                            │                    │
│  │                                                                  │                    │
│  │  var/server/attack-signals/                                      │                    │
│  │  ├── active.json         ← 当前活跃攻击信号                       │                    │
│  │  ├── attack-count.json   ← 攻击计数（60秒窗口）                   │                    │
│  │  ├── attack-mode.json    ← 攻击模式状态                          │                    │
│  │  ├── cdn-notified.json   ← CDN 已通知标记                        │                    │
│  │  └── history/            ← 历史攻击记录                          │                    │
│  │                                                                  │                    │
│  └──────────────────────────────┬───────────────────────────────────┘                    │
│                                 │                                                        │
│                                 │  ← Cron 定时任务每分钟检查                              │
│                                 ▼                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐                    │
│  │              Server Cron 定时任务                                │                    │
│  │              AttackSignalMonitor                                 │                    │
│  │                                                                  │                    │
│  │  ┌────────────────────────────────────────────────────────────┐ │                    │
│  │  │  每分钟执行 (* * * * *)                                     │ │                    │
│  │  │                                                             │ │                    │
│  │  │  1. 检查攻击计数是否达到阈值 (5次/60秒)                      │ │                    │
│  │  │     ├── 是 → 广播 Weline_Cdn::security::attack_detected     │ │                    │
│  │  │     │       → 设置攻击模式                                   │ │                    │
│  │  │     │       → 归档攻击记录                                   │ │                    │
│  │  │     └── 否 → 继续监控                                       │ │                    │
│  │  │                                                             │ │                    │
│  │  │  2. 检查是否应该恢复 (无新攻击超过 5 分钟)                   │ │                    │
│  │  │     ├── 是 → 广播 Weline_Cdn::security::attack_recovered    │ │                    │
│  │  │     │       → 关闭攻击模式                                   │ │                    │
│  │  │     └── 否 → 继续等待                                       │ │                    │
│  │  │                                                             │ │                    │
│  │  │  3. 清理过期数据                                            │ │                    │
│  │  └────────────────────────────────────────────────────────────┘ │                    │
│  │                                                                  │                    │
│  └──────────────────────────────┬───────────────────────────────────┘                    │
│                                 │                                                        │
│           ┌─────────────────────┴─────────────────────┐                                 │
│           │                                           │                                 │
│           ▼                                           ▼                                 │
│  ┌─────────────────────────┐             ┌─────────────────────────┐                    │
│  │   攻击检测事件           │             │   攻击恢复事件           │                    │
│  │   attack_detected       │             │   attack_recovered       │                    │
│  └───────────┬─────────────┘             └───────────┬─────────────┘                    │
│              │                                       │                                  │
│              ▼                                       ▼                                  │
│  ┌─────────────────────────────────────────────────────────────────┐                    │
│  │                      CDN 模块 (Weline_Cdn)                       │                    │
│  │                                                                  │                    │
│  │  ┌─────────────────────────┐    ┌─────────────────────────────┐ │                    │
│  │  │  AttackSignalHandler    │    │  AttackRecoveryHandler      │ │                    │
│  │  │                         │    │                             │ │                    │
│  │  │  开启攻击防护模式       │    │  关闭攻击防护模式           │ │                    │
│  │  │  enableAttackMode()     │    │  disableAttackMode()        │ │                    │
│  │  └───────────┬─────────────┘    └───────────┬─────────────────┘ │                    │
│  │              │                              │                   │                    │
│  └──────────────┼──────────────────────────────┼───────────────────┘                    │
│                 │                              │                                        │
│           ┌─────┴──────────────────────────────┴─────┐                                  │
│           │                                          │                                  │
│           ▼                                          ▼                                  │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐                      │
│  │   Cloudflare    │    │     其他 CDN     │    │   自定义 CDN    │                      │
│  │   Adapter       │    │    Adapter       │    │   Adapter       │                      │
│  │                 │    │                 │    │                 │                      │
│  │ - Under Attack  │    │ - 开启/关闭     │    │ - 开启/关闭     │                      │
│  │   Mode          │    │   防护模式      │    │   防护模式      │                      │
│  │ - IP Block      │    │                 │    │                 │                      │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘                      │
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

### 非阻塞架构优势

1. **不阻塞请求**: Dispatcher 只写入信号文件，不等待 CDN 响应
2. **异步处理**: Cron 定时任务独立运行，不影响请求处理
3. **自动恢复**: 无新攻击超过 5 分钟后自动关闭防护模式
4. **去重机制**: 已在攻击模式中不重复通知
5. **历史归档**: 攻击记录自动归档，便于后续分析

## 组件说明

### Master Process (主控进程)

- 管理 Dispatcher 和 Worker 进程的生命周期
- 处理信号 (SIGTERM, SIGHUP 热重载)
- 监控子进程状态

### TCP Passthrough Dispatcher (透传分流器)

- 纯 TCP 层面的流量转发
- 不解密 SSL，只透传加密流量
- 使用 MSG_PEEK 读取 ClientHello 提取 SNI
- 集成攻击探测功能

### AttackDetector (攻击探测器)

| 检测类型 | 说明 | 默认阈值 |
|---------|------|---------|
| rate_limit | 请求频率限制 | 100次/10秒 |
| path_scan | 路径扫描检测 | 20次404/60秒 |
| malicious_pattern | 恶意特征匹配 | SQL注入/XSS/命令注入 |
| bad_user_agent | 恶意UA检测 | 扫描工具/恶意爬虫 |
| protected_path | 保护路径 | .env/.git/wp-admin |
| slowloris | 慢速攻击 | 30秒/请求 |

### RoutingCacheService (路由缓存服务)

- SNI 到 Worker 端口的映射缓存
- IP 到 Worker 端口的映射缓存
- 连接 ID 映射（Keep-Alive 场景）
- 学习模式：从 Worker 响应的 X-Weline-Route-Hint 头学习路由

### Worker (工作进程)

- SSL 握手和解密
- HTTP 请求处理
- 业务逻辑执行
- Route Hint 自报告（添加 X-Weline-Route-Hint 响应头）

## 数据流向

```
客户端 ──TCP──▶ Dispatcher ──攻击检测──▶ 路由决策 ──TCP──▶ Worker ──SSL握手──▶ 业务处理
                   │                       │                │
                   │                       │                │
                   ▼                       ▼                ▼
              攻击日志            路由缓存更新         Route Hint 响应
                   │                       │                │
                   │                       │                │
                   └───────────────────────┴────────────────┘
                                  │
                                  ▼
                           攻击信号处理
                                  │
                                  ▼
                         CDN 攻击模式开启
```

## 配置文件

### 攻击检测规则

`var/server/security-rules.json`

```json
{
  "rate_limit": {
    "enabled": true,
    "max_requests": 100,
    "window_seconds": 10
  },
  "path_scan": {
    "enabled": true,
    "max_404": 20,
    "window_seconds": 60
  },
  "malicious_patterns": {
    "enabled": true,
    "patterns": ["union.*select", "<script", "etc/passwd"]
  },
  "bad_user_agents": {
    "enabled": true,
    "patterns": ["sqlmap", "nikto", "masscan"]
  },
  "protected_paths": {
    "enabled": true,
    "paths": [".env", ".git", "wp-admin", "phpmyadmin"]
  },
  "slowloris": {
    "enabled": true,
    "max_request_time": 30
  }
}
```

## 相关文件

### Server 模块

| 文件 | 说明 |
|------|------|
| `Dispatcher/PassthroughDispatcher.php` | TCP 透传分流器 |
| `Dispatcher/PassthroughCore.php` | 透传核心逻辑 |
| `Dispatcher/DispatcherCore.php` | HTTP 模式分流核心 |
| `Dispatcher/SniParser.php` | SNI 解析器 |
| `Dispatcher/RoutingCacheService.php` | 路由缓存服务 |
| `Security/AttackDetector.php` | 攻击探测器 |
| `Service/AttackSignalFileService.php` | 攻击信号文件服务（非阻塞） |
| `Cron/AttackSignalMonitor.php` | 攻击信号监控定时任务 |
| `Service/RouteHintService.php` | 路由提示服务 |
| `bin/dispatcher_passthrough.php` | 透传 Dispatcher 启动脚本 |
| `bin/worker_ssl.php` | SSL Worker 脚本 |

### CDN 模块

| 文件 | 说明 |
|------|------|
| `event.php` | 事件定义（`attack_detected` + `attack_recovered`） |
| `etc/event.xml` | 事件观察者注册 |
| `Api/AdapterInterface.php` | CDN 适配器接口（`enableAttackMode`/`disableAttackMode`） |
| `Adapter/Cloudflare.php` | Cloudflare 适配器（Under Attack Mode + IP Block） |
| `Observer/AttackSignalHandler.php` | 攻击信号处理（开启防护） |
| `Observer/AttackRecoveryHandler.php` | 攻击恢复处理（关闭防护） |
| `Service/AdapterResolver.php` | 适配器解析服务 |

## CDN 适配器接口

所有 CDN 适配器必须实现 `AdapterInterface`，攻击防护相关方法：

```php
interface AdapterInterface
{
    // ... 其他方法 ...
    
    /**
     * 开启攻击防护模式
     */
    public function enableAttackMode(string $zoneId, array $credentials, array $attackData = []): array;
    
    /**
     * 关闭攻击防护模式
     */
    public function disableAttackMode(string $zoneId, array $credentials): array;
    
    /**
     * 检查是否支持攻击防护模式
     */
    public function supportsAttackMode(): bool;
}
```

## Cloudflare 攻击防护实现

Cloudflare 适配器实现了以下攻击防护功能：

1. **Under Attack Mode**: 通过 `settings/security_level` API 设置为 `under_attack`
2. **IP 封禁**: 通过 `firewall/access_rules/rules` API 封禁攻击者 IP
3. **自动恢复**: `disableAttackMode` 将安全级别恢复为 `medium`

```php
// Cloudflare API 调用示例
// 开启攻击模式
PATCH /zones/{zone_id}/settings/security_level
{ "value": "under_attack" }

// 封禁 IP
POST /zones/{zone_id}/firewall/access_rules/rules
{
    "mode": "block",
    "configuration": { "target": "ip", "value": "1.2.3.4" },
    "notes": "Auto-blocked by WLS attack detection"
}
```

## Fiber 池与自动预热

WLS 的 Fiber 设计采用“可复用请求执行单元”模型，而不是“每个请求创建一次性 Fiber”模型。

- 一个请求在执行期独占绑定一个 Fiber。
- 请求结束后，Fiber 不立即销毁，而是回到空闲池等待下一个请求复用。
- Fiber 的角色更接近 FPM 的 worker 进程，只是调度粒度位于单个 Worker 进程内部。
- 只有空闲超过 5 分钟且未再次被请求复用的 Fiber，才允许销毁回收。

### 目标规则

- Worker 内必须维持 Fiber 池，不应等到新连接到达时才临时创建全部 Fiber。
- Fiber 池需要自主预热，保持可立即接单的空闲 Fiber。
- 池繁忙度定义为 `busy_fibers / total_fibers`。
- 当繁忙度达到或超过 `90%` 时，Worker 应主动继续预热新 Fiber。
- Fiber 总量的保底值为 `100`，即使当前流量很低，也应尽量维持不少于 `100` 个可复用 Fiber。

### 生命周期

1. Worker 启动后先自主预热 Fiber 池。
2. 新请求优先复用空闲 Fiber。
3. 当空闲 Fiber 不足且繁忙度逼近 90% 时，后台继续补充 Fiber。
4. 请求处理完成后，Fiber 清理请求级上下文并返回空闲态。
5. 空闲 Fiber 超过 5 分钟未被复用时，按回收策略销毁。

### 实现约束

- Fiber 必须是常驻循环执行体，能够在“空闲等待 -> 接收请求 -> 执行 -> 清理 -> 回到空闲”之间切换。
- 已经 `terminated` 的 Fiber 不能作为复用对象，因此复用模型不能建立在“一请求一 Fiber、结束即终止”的实现上。
- 请求级状态必须在 Fiber 返回空闲前完整清理，包括 RequestContext、SseContext、HeaderCollector、Session、ObjectManager 请求实例等。
- 自动预热与回收需要同时适配 `worker.php` 与 `worker_ssl.php`，保持一致的 Fiber 池语义。
