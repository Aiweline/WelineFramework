# Weline_MediaManager 开发日志

> 按目标版本记录真实进度、调整与验收证据；不粘贴原始终端日志，不补写未经证实的历史。2026-08-18 起启用固定文档制度。

## 2026-09-24 — AI 画图默认 WebP

- `AiDrawService`：`output_format` 默认 `webp`；生成结果若非目标格式则 GD 转码（`ensurePreferredRasterFormat`）。
- 对齐 MCP `generated_images_prefer_webp`。
- 版本：`1.3.8` → `1.3.9`。

## 2026-09-20 — file-image usage 优先图库 CONFIG locale

- 主题编辑器全语言布局选图：usage 曾写死 `file.locale_code`（资源行语），与编辑器站点默认语不一致。
- 修：`confirmImageUsageSnapshots` 写 `CONFIG.localeCode || file.locale_code`。
- 版本：`1.3.7` → `1.3.8`。

## 2026-09-18 — 可选指定覆盖目标（上传/拖入/AI）

- 规格：`doc/开发/spec/optional-target-overwrite-pick.md`（ready-for-plan；Plan Mode 用户拒绝后按规格直落）。
- 实现：拖入/粘贴时弹出居中 `#mmf-overwrite-pick-overlay`（当前目录搜索、每文件可选映射）；映射后改用目标文件名 + `overwrite`→`replaceContent`；点「上传」不弹。冲突对话框未改。AI 保存「覆盖指定文件」仍走 `MediaStorageService::replaceExistingFile`。底部 **`.mmf-statusbar`（N items）** 始终贴在文件区底部悬浮，不随网格滚出视口。
- 2026-09-18 审图纠正：用户截图要悬浮的是状态栏「N items」，不是覆盖选择；已撤掉常驻覆盖底栏，覆盖选择恢复为拖入/粘贴才弹的居中层。

## 2026-09-16 — 选图顶部「当前身份引用」标签过滤

- 规格：`doc/开发/spec/identity-top-tag-filter.md`（simple 跳过 Plan）。
- 实现：`mmf-identity-bar`（工具栏上方）；按大类/身份/范围/槽位分组 chips；点击标签或组名 `by-tags` 过滤；「全部」清除；与左栏引用筛同步。
- 审图：ui_shot；箭头空带落位身份条；主题 token 描边/主色 active。
- 验证：`MediaReferenceProtocolContractTest`；Browser 进店音乐选曲弹层可见身份条。

## 2026-09-16 — 图库选择器描述区展示推荐比例（1.3.5）

- 现象：广告横幅等部件 `@param … media_options={aspect_ratio:"16/5",…}` 已透传到选择器，但侧栏「从图库选择」下只显示类型/大小，缺比例提示。
- 修：`FileManager` 解析并 `assign` `aspect_ratio_hint` / `recommend_size_display`；`weline-media.phtml` 按钮下与弹层副标题同步展示。
- 验证：`FileManagerMediaConstraintHintContractTest` + `WelineMediaConnectorOriginContractTest`；Theme Editor 广告图片字段 Browser 验收。

## 2026-09-16 — WelineMedia bind URL 改 PHP 计算

- 现象：部件 `paramrender/form` 渲染 `weline-media.phtml` 时 `@backend-url{weline_filemanager…}` 炸常量，`media_image` 回退旧选图按钮。
- 修：`Block/WelineMedia` 用 `getBackendUrlPath` 赋值 `bind_url`；模板消费 `data-w-bind-url`。
- 验证：`WelineMediaConnectorOriginContractTest`；Browser paramrender 含 `w-file-picker` + `file-image`。

## 2026-09-16 — 上传冲突弹窗新名自动补齐扩展名

- 当前状态：`implemented_validated`
- 需求：冲突弹窗新文件名可带可不带扩展；不带则按源文件自动补齐，避免无扩展上传报错。
- 实现：`manager.js` `ensureUploadFileExtension`（`confirmUploadName` 调用）；弹窗聚焦只选中主名；暴露 `WelineMediaManager.ensureUploadFileExtension`；规格 `doc/开发/spec/upload-conflict-auto-extension.md`；`pub/static` 副本已同步。
- 验收：UT `UploadConflictAutoExtensionContractTest` 8 PASS；e2e `MEDIAMANAGER-UPLOAD-AUTO-EXT-001` + plan-suite PASS；Browser 禁缓存后 Runtime 验 `新图`→`新图.png`。

## 2026-09-16 — 选择确认缺元数据立即弹窗补充

- 当前状态：`implemented_validated`
- 需求：确认选用时若无可选 FileAsset / 当前语言名称·alt·描述未审核，立即弹窗补充，不再仅 toast。
- 实现：`manager.js` `supplementAssetMetadataForSelection`；无 `asset_id` 走 Connector `asset_ensure` → `FileAssetLibrary::registerExistingObject`；规格 `doc/开发/spec/select-missing-asset-metadata-prompt.md`。
- 验收：MediaManager/FileManager 契约 UT PASS；本机 Browser：对未注册文件点「确认选择」弹出「资源名称」窗体，无 `assetMetadataRequired` 红 toast。

## 2026-09-15 — 上传同名允许覆盖并继承多语言文案

- 当前状态：`implemented_validated`（本机 UT + Browser WB；Playwright e2e 因 admin session bootstrap/fpm 未登录阻断，未绿）
- 需求：冲突弹窗可「覆盖已有」并二次确认；覆盖原位替换字节、保留同一 `asset_id`/locales，跳过 alt/描述/一键翻译。
- 实现：`FileAssetLibrary::replaceContent`；Connector/Upload/Resumable 认 `overwrite`；`manager.js` 冲突三级按钮 + 确认层；规格 `doc/开发/spec/upload-overwrite-inherit-locale.md`；修订 `REQ-MEDIA-0004`。
- 验收：FileManager/MediaManager 契约与 overwrite UT PASS；Browser 禁缓存打开管理器，冲突弹窗可见「覆盖已有」与确认文案。

## 2026-09-15 — /media/image 缩略图走框架 Response（1.3.4）

- 现象：选图后刷新预览变 📄；`/media/image/…?w&h` 返回 `text/html`（CSP meta 盖住缩略图字节）。
- 根因：`Image::getIndex` 用 PHP `header()` + return string，被 `Response::normalize` 当成 HTML。
- 改：`Response::fromContent` + `ResponseTerminateException`；按扩展 MIME；仍写 `pub/media/thumbnail/` 缓存。
- 版本：`1.3.3` → `1.3.4`。

## 2026-09-15 — 选图按钮标题避开 Template 保留 title（1.3.3）

- 根因：`Template::init` 用模块名覆盖 block `title`，Smtp 页内嵌选图按钮显示 `Weline_Smtp` 并被拉满宽显贴边。
- 改：block 改用 `picker_title`；按钮/对话框文案读 `picker_title`。
- 版本：`1.3.2` → `1.3.3`。

## 2026-09-14 — 多选删除误报「未选择删除项目」

- 当前状态：`implemented_validated`
- 现象：网格已勾选多项后点删除，Toast 仍报「未选择删除项目」。
- 根因：`api()` 始终附带 `lockPath`，但 `media_manager.connector` Query 参数未声明 → Worker 报 Unknown param 后回退 `connectorNativeRequest`；FormData 用 `String(targets)` 把数组变成 `"a,b,c"`，后端 `requiredTargetHashes` 判定非数组即抛「未选择删除项目」。iframe 原生通道同理。
- 修复：`appendConnectorFormField` 按 `targets[]` 写入；Query 声明 `lockPath`/`lockRoot`（及 snake_case）；审图调整文件名两行 clamp、iframe 工具栏按钮不被裁切；`framework:compile` 刷新 Provider 索引。
- 验收：契约单测 OK；定向 e2e `MEDIAMANAGER-MULTI-RM-001` PASS；本机 Browser 多选两临时目录删除成功，确认文案「确定删除 2 个项目？」，无「未选择删除项目」。

## 2026-09-03 — iframe `asset_id` 聚焦打开详情/翻译（1.3.2）

- 当前状态：`implemented`
- 问题：嵌入方（如商品媒体行）需要按 FileAsset UUID 打开管理器并编辑描述翻译，仅靠 path/`initialValue`/`search` 不够。
- 实现：Connector `find_asset`；`manager.js` 读 URL `asset_id` → `applyFocusAssetId` 导航选中并 `openAssetDetails`（locale workbench）。
- 验收：商品 `#84` Browser 点击媒体资源后 iframe URL 含 `asset_id`，文件已定位且预览区可见多语言翻译工作台；契约断言 `find_asset`/`applyFocusAssetId`。
- 版本：`1.3.1 → 1.3.2`。

## 2026-09-01 — iframe 多选选择器默认进入选择模式

- 当前状态：`implemented`
- 问题：商品主图等 `multi=1` 嵌入选择器虽已多选参数，但未进入 `SELECTION_MODE`，单击会覆盖选中，需 Ctrl/右键才能多选。
- 实现：`init`/`setupIframeMode` 在 `IFRAME_MODE && MULTI_SELECT` 时自动 `enterSelectionMode`；`exitSelectionMode`/`clearSelection` 保持多选模式；右键菜单隐藏「退出选择模式」；模块 `1.3.1`。
- 验收：打开商品「从媒体库添加」后文件项显示勾选框，单击可累加多选。

## 2026-08-29 — 独立页 Worker API 未就绪修复

- 当前状态：`implemented_validated`
- 现象：打开 `/media/backend/manager` 控制台报「后台 Worker API 尚未就绪」，文件区空白。
- 根因：独立 `index` 未套布局（TemplatePerf `before/after≈0`），页面无 `Weline.Api`；`mmResource` 非 iframe 时强依赖 Worker API。
- 修复：`Manager::index` 强制 `default.blank`；`connector` 在 API 不可用时回退 `connectorNativeRequest`。
- 验证：已登录硬刷新后 `Weline.Api`/`meta` 就绪，LayoutPerf 套 `default/blank.phtml`，目录与文件列表正常渲染。

## 2026-08-29 — FileAssetLocale 多语言工作台与 AI 补缺

- 当前状态：`implementing`
- 需求：上传按当前语言保存；填完后可选「确认上传 / 一键翻译」；右侧详情语言 Tab 编辑；工具栏「AI 自动翻译」开关（仅 cron，不影响一键翻译）。
- Connector：`asset_locales` / `asset_translate_missing` / `translation_config`；能力来自 FileManager `FileAssetLocaleTranslationInterface`。
- 验证：待定向契约与真实后台上传/翻译路径。

## 2026-08-29 — 主题 Scope 目录锁定与解锁

- 当前状态：`implemented_pending_runtime`
- 契约：嵌入选择器支持 `lockRoot` + `lockPath`；Theme 默认 `lock_root=websites/{website}/{store}[/channel]`（channel 省略 `default`），`start_path=lock_root/{default_directory}`；工具栏可解锁浏览全库后再锁定；Connector 在 `lockPath=1` 时校验目标相对路径 ∈ `lockRoot`。
- 透传：`Manager::getIframe` assign `lock_root`、`manager.phtml` CONFIG、API/upload 携带 `lockPath`/`lockRoot`、`FileManagerConnector`/`Block::getParams`。
- 验证：定向契约通过（MediaManager lock UI 31 assertions、Widget/Theme path helpers 18 assertions、FileManager lockRoot 透传 23 assertions）；Theme Editor / 锁定 iframe 探活 `302`；已登录 768/1024 交互待宿主后台会话。
- 体验调整：锁定开关移到路径栏右侧（与面包屑同排）；锁定态路径行高亮；面包屑走 `FILES|TREE` 祖先链；iframe 内仅显示锁图标以省宽。
- 非范围：不迁移历史根下 `banner/`；独立文件管理全页仍自由浏览。

## 2026-08-29 — 嵌入式选择器图片比例硬约束

- 当前状态：`implemented_pending_runtime`
- 契约：iframe 选择器支持 `aspect_ratio`（或由成对 `recommend_width/height` 推导）；默认容差 `0.02`；缺尺寸或比例不符的图片灰显；单击可点选并刷新右侧预览，双击/确认回填仍硬拦；顶栏提示目标比例。
- 透传：`Manager::getIframe`、`manager.phtml` CONFIG、`FileManagerConnector`/`Block::getParams`、Widget/Theme 图片控件 `data-aspect-ratio`。
- 验证：定向契约单测通过（MediaManager picker 21 assertions、Widget image field 18 assertions、FileManager connector 20 assertions）；Theme Editor / iframe 探活 `302`（需登录）；已登录多断点交互待宿主会话完成。

## 2026-08-29 — 选择器单击预览联动

- 当前状态：`implemented_pending_runtime`
- 需求 ID：`REQ-MEDIA-0006`（体验补丁）
- 根因：`data-selection-error` 单击提前 `return`，未写入 `SELECTED`，右侧预览停留占位；仅改 JS 后宿主仍可能缓存旧脚本。
- 实现：单击仍走选中与 `updatePreviewPanel`；toast 提示原因；确认/双击继续拦截。预览根节点增加 `mmf-preview--active` + `empty[hidden]` + CSS `display:none !important`；选中且比例不符项用 primary outline 可辨认。
- 验证：定向契约断言更新后执行；需关闭并重开媒体选择弹层以加载新静态资源。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.2.1` | implemented_validated | `REQ-MEDIA-0001` | 2026-08-18 | 2026-08-18 | 媒体管理 WebUI 通过 |
| `1.2.2` | implemented_validated | `REQ-MEDIA-0002` | 2026-08-22 | 2026-08-22 | 定向契约、真实 WebUI 与 375px E2E 通过 |
| `1.2.3` | implemented_pending_runtime | `REQ-MEDIA-0003` | 2026-08-22 | 2026-08-22 | 需求已确认；等待本次真实 WLS 与 E2E 结果 |
| `1.3.0` | implementing | `REQ-MEDIA-0004` | 2026-08-22 | 2026-08-22 | 用户确认元数据详情与全部审查遗漏闭环；实现中 |
| `1.3.0` | implemented_pending_runtime | `REQ-MEDIA-0005` | 2026-08-27 | 2026-08-27 | 定向契约通过；等待真实浏览器多断点验收 |

## 2026-08-27 — 嵌入式媒体管理器底部滚动修复

- 当前状态：`implemented_pending_runtime`
- 需求 ID：`REQ-MEDIA-0005`
- 根因：iframe 内 `html/body/main/.mmf-wrap` 被脚本写成按 `clientHeight` 测得的固定像素高度，外层 frame 又重复写入 body 测量高度；两次取整和弹窗头部变化会让底部状态栏或详情动作超出 iframe 后被裁。
- 实现：移除内外两层像素高度锁定，保留 CSS 的百分比 flex/grid 高度链；目录树、中央内容和详情信息独立滚动，状态栏与详情动作固定在各自布局末端；触控滚动启用惯性与边界隔离。
- 验证：JS/PHP 语法通过；`testEmbeddedPickerKeepsBottomContentReachableWithoutPixelHeightLocks` 为 `1 test / 11 assertions`，带项目 bootstrap 的完整视图契约为 `17 tests / 289 assertions`，均通过；真实登录态页面可读且路由可达，但浏览器控制连接在接管标签页时中断，375/768/1024/1440 交互验收待恢复后执行。

## 2026-08-22 — 后台 Global FileAccessContext 回归修复

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-MEDIA-0001`、`REQ-THEME-0004`
- 根因：已认证的 backend Query/Controller 不经过 storefront Scope 冻结，但 ConnectorService 为文件浏览同样强制读取 `RequestContext::scopeIdentity()`，导致页面壳恢复后文件树仍报“媒体文件访问缺少冻结的 ScopeIdentity”。
- 实现：仅当没有 storefront identity 且适配器已提供正数后台用户 ID 时，使用框架正式的 `ScopeIdentity::global()` 构建 `FileAccessContext`；不写入 RequestContext。无 actor 继续 fail-closed，已有 website/store/channel identity 原样保留，私有资源继续校验 owner、allowed scope 与 role。
- 验收：新增回归 `2 tests / 10 assertions`；PHP 语法与差异检查通过；`default:9555` 四 Worker 滚动重载后，真实 Weline Chrome 中媒体管理器文件树、11 个根项目与 `image.png` 正常加载，嵌入式图片选择器完成类型过滤、预览和选择，连接器请求均为 200。
- 说明：用户提供的 127.0.0.1 URL 因后台登录 Cookie 绑定正式域名而跳转登录页；同一路由未再出现 500，完整已登录验收在 `p05113ef3.test.weline.com:9555` 完成。

## `1.3.0` — 统一 FileAsset 与可查看元数据详情

- 基线版本：`1.2.3`
- 目标版本：`1.3.0`
- 当前状态：`implementing`
- 需求 ID：`REQ-MEDIA-0004`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围：右键/选中详情；Weline.Api 上传；显式请求上传数据；本地/扩展磁盘 FileAsset 一致性；非法 hash、大小、类型、重名保护；逐文件多语言元数据；单测/E2E/文档闭环。
- 依赖边界：MediaManager 只依赖 FileManager/Storage 发布接口，不引用对方具体 Model/Service；不修改 Framework 业务语义。
- 负责人/确认人：当前 Codex 主代理直接实现（未委派）/ 用户确认。

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-media-0004`；用户明确要求详情入口并修复全部审查遗漏 | 2026-08-22 |
| 实现 | in_progress | MediaManager/FileManager 当前工作树与本任务记录 | 2026-08-22 |
| 架构/缺陷/安全复审 | pending | 实现后执行完整差异复审 | 2026-08-22 |
| 分层测试 | pending | 浏览器请求/超全局守卫、PHP/JS lint、聚焦 PHPUnit | 2026-08-22 |
| 真实 WLS + WebUI | pending | 右键详情、选中详情、上传、粘贴、移动、选择 | 2026-08-22 |
| 可重复 E2E | pending | `MEDIAMANAGER-DROP-PASTE-MOVE-001` 与元数据详情路径 | 2026-08-22 |
| 功能/运营文档 | pending | README、运营说明、功能快照 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 09:19 | requirements | confirmed → implementing | 用户要求右键/选中均可查看元数据，并把审查遗漏一并修复 | 合并为 REQ-MEDIA-0004 / 1.3.0 闭环，测试与 E2E 资产明确在授权范围 | 用户 | 当前任务与 `doc/需求.md` |

## `1.2.3` — 拖拽、粘贴上传与文件拖入目录

- 基线版本：`1.2.2`
- 目标版本：`1.2.3`
- 当前状态：`implemented_pending_runtime`
- 需求 ID：`REQ-MEDIA-0003`
- 范围/排除：外部拖放、剪贴板 File 上传、普通文件拖入网格/树目录、provider-aware move；排除目录移动、同名覆盖与远端上传扩容
- 安全边界：专用 DataTransfer MIME；普通文件/根边界/目标目录/同名预检；远端能力不足不回退本地；批量失败尽力回滚
- 自动化入口：`ConnectorServiceParameterSourceTest`、`ManagerJsBinQueryContractTest`、`MEDIAMANAGER-DROP-PASTE-MOVE-001`
- 负责人/确认人：实现 Codex / 确认 用户
- 说明：本记录在实现事务中保守标记为待运行时验收；实际执行证据以本次任务最终报告为准，用户/产品验收仍需用户确认。

## `1.2.2` — 目录右键管理统一接入存储提供者

- 基线版本：`1.2.0`
- 目标版本：`1.2.2`
- 当前状态：`implemented_validated`
- 需求 ID：`REQ-MEDIA-0002`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：目录浏览、新建、重命名、删除、菜单定位和响应式；排除远端上传/下载/预览能力
- 负责人/确认人：实现 Codex / 确认 用户

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-media-0002` | 2026-08-22 |
| 提供者安全边界 | pass | `StorageDirectoryManagerInterface`；根目录、穿越、覆盖保护 | 2026-08-22 |
| 分层测试 | pass | Storage 4/4；Connector 2/2；Manager UI contract 8/8；Taglib 9/9 | 2026-08-22 |
| 真实路径 WebUI | pass | WLS `default:9555`：树/网格右键、触控更多、1280/375 边界、普通刷新资源指纹 | 2026-08-22 |
| Weline UI 原生菜单主题/定位回归 | pass | `MEDIAMANAGER-UI-001`；内置 Browser 深浅色、右边界 8px、同点 3 次不漂移、Escape/外部点击/刷新关闭 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认 | 2026-08-22 |

### 实现范围

- 浏览器请求继续只走 `Weline.Api.resource('media_manager')`，并自动附带当前存储源。
- 外部存储只通过数据化公共契约执行命令，不暴露 `StorageManager`、驱动对象或凭据。
- 左侧目录树与右侧媒体网格保留完整桌面右键、键盘和触摸更多功能；菜单外壳改为全局 `Weline.UI` Menu，业务仅生成动作项，主题 token、portal、视觉视口限界/翻转/重排、键盘和关闭生命周期由公共内核统一负责。
- 开发环境静态 CSS/JS 使用框架内容指纹，资源改动后普通刷新即取得新 UI，避免固定 URL 的短缓存继续执行旧脚本。
- 删除确认使用语义化 dialog、焦点陷阱与显式不可撤销提示。

## `1.2.1` — 媒体 Query 身份取自已证明的 backend Worker 上下文

- 基线版本：`1.2.0`
- 目标版本：`1.2.1`
- 当前状态：`implemented_validated`
- 需求 ID：`REQ-MEDIA-0001`
- 任务记录：N/A
- 范围/排除：`MediaManagerQueryProvider` 身份解析；排除 runtime_task 协议、AI 模型绑定、路径穿越校验
- 负责人/确认人：实现 Auto / 确认 用户

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-media-0001` | 2026-08-18 |
| 实现执行者/模型/范围已记录 | pass | Auto；`MediaManagerQueryProvider::backendUserId` 优先 Worker 上下文 | 2026-08-18 |
| 架构/缺陷/安全复审通过 | pass | 去掉 runtime_task owner；Worker 上下文取身份 | 2026-08-18 |
| 分层测试通过 | pass | `MediaManagerQueryProviderBackendOwnerTest` 4/4 | 2026-08-18 |
| 真实路径 WebUI/E2E 通过 | pass | test 登录文件管理：目录树 Media Files/ai-generated/backend，网格 5 项，无红色 toast | 2026-08-18 |
| 可重复 E2E 固化并通过 | N/A | 用户未要求铺 E2E | 2026-08-18 |
| 功能/运营文档完成 | pass | `doc/README.md`、`doc/需求.md` | 2026-08-18 |
| 用户/产品验收 | pending | 等待用户确认 | 2026-08-18 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-18 17:38 | requirements | planned → requirements_confirmed | connector/config 500，日志为 Session 与 backend 证明不一致 | 普通 Query 改走 backend Session；身份称 backend | 用户 | QueryBin 日志 |
| 2026-08-18 17:45 | implementing | requirements_confirmed → implementing | 去掉 `ResumableTaskOwnerResolver` | 未登录抛 401 `auth_error` | Auto | 源码 |
| 2026-08-18 17:50 | implementing | implementing | QueryBin 下 `Session->start()` 误报未登录 | 优先读已证明 backend Worker 用户 ID | Auto | WebUI 401 |
| 2026-08-18 18:12 | verifying | implementing → implemented_validated | test 登录文件管理，connector/config 成功，无红色 toast | WebUI 通过 | Auto | 内置 Browser |

### 实现范围

- 变更路径/符号：`MediaManagerQueryProvider::backendUserId`
- 数据/配置/迁移：无 Schema 变更
- 兼容与回退：runtime_task 仍用原 owner 解析器
