## 2026-09-08 — 分类 URL 批量上下文复用（Framework 2.5.43 / UrlManager 1.0.6 / Product 1.0.197）

### 实现与兼容

`Url::getFrontendUrls` 与单条入口共用原始 URL 构造；先派发可选 `url_generate_rewrite_prefetch`，再逐条经过原 `extractedUrl` 与重写事件。分类服务仅提交去重后的业务 path，没有另造缓存或依赖重写 Model。

`SeoUrlGenerateRewrite::prefetch` 一次读取原共享键，命中复制到现有 RequestContext；缺失按 website 汇集带语言与剥语言的路径，由 Model 每 256 个原始路径一批查询。结果沿用原 request/persistent key 与 300 秒 TTL，MSET 发布；失败不发布负缓存，单条 Model 保持实时查询。严格原始路径、指纹→NULL→空指纹、各层最新 ID、primary 优先、空 rewrite 命中、关联数组返回均保留。URL 最终值及可变 Model 不进入共享缓存。UrlRewrite 写侧正式 changed 仍不完整，本轮不新建更长期 L1 或延长旧缓存 TTL。

### 运行证据

正式 app/bootstrap 与实际 CategoryRepository 读取 website=0 的 51 类，102 个主/回退原始路径：单条 102 SELECT 与批量 1 SELECT 的完整 row 数组严格相同，SHA256 为 `015fcb8ce647be46e1fa3fad0d475a6ad4886e7b6100255fd573e57dead75a13`。该次批量单 SQL 遇到约 47ms 等待，不用单次耗时推断稳定比例。

进一步以真实 Observer、CachePool、WlsMemoryAdapter（关闭该探针 Adapter 本地缓存）、MemoryStateFacade 测试。仅使用独立临时池 `_scalar/_batch`，300 秒自然失效，不清业务缓存或写业务数据：

| 51 条实际分类 URL | SQL | WLS 通信 | 整段耗时 |
|---|---:|---:|---:|
| 原逐条冷读 |102|102（51 GET +51 SET）|90.743ms|
| 新批量冷读 |1|2（MGET +MSET）|4.024ms|
| 新 RequestContext 复用 |0|1 MGET|2.052ms|
| 新 PHP 进程复用同池 |0|1 MGET|7.268ms|

主进程 PID7896，2026-09-08 14:51:41 UTC；另一进程 PID7969，14:51:47。全部完整 URL 严格一致，SHA256 为 `5c5ce6c5c9b6b9fe79cef162dc2e484354f330d41080a7786d24744c8660c1e0`。实际库这 102 个路径均 miss；正向、旧指纹及空重写由 PostgreSQL 编译器/SQLite 定向用例覆盖。探针 `/tmp/weline-category-url-{batch,observer}-runtime.php`。这是 URL 子链证据，不等于网页冷启动达 70ms。

### 检查与部署记录

最小 RED 后联合模型、Observer、批量 URL、挂载与分类契约 26 tests /174 assertions 通过。旧语言前缀组原/新类精确对照均 6 tests /34 assertions /4 failures，失败位置82/104/132/147与内容相同，非本轮引入，不计入通过数。代码 lint、精确 diff 检查通过。

reflection:compile 完成 1388 热点类、3845 工厂；framework:compile 的多次尝试均在源码快照阶段发现运行时模板或并行模块输入变化而拒绝发布（另一次等待既有编译锁超过 60 秒）。未绕过 manifest、未人工编辑 generated，也未滚动重载未发布的代码；9 个本轮源码/版本/事件路径 hash 记录在 `/tmp/weline-cold70-isolated/url-batch-source-manifest.json`。正式产物发布与 HTTP 页面验收需在站点流量和并行源码写入暂停后重试。

### 继续优化的明确边界

- 32 卡模板的 `getCurrentUrl` 重复调用在真实 Fiber 环境仅约 7ms；保留重写事件的纯正规化 memo 对照仅省 0.425ms，因此没有增加这层缓存。早先不完整 CLI 上下文的 58ms 不能代表 WLS 收益。
- 商品目录构建 339→340 商品两个真实样本为345.64/422.53ms；其中媒体行38.46/58.69ms，媒体资料与 URL 解析72.31/176.80ms，促销装配178.33/113.60ms，均为父阶段子项，不与父项相加。只套 Product 事实缓存无法省掉全部成本。
- 公共商品事实应存纯 product/offer/显式 EAV 行/媒体引用，由中央批量入口读取，命名空间独立于 catalog 父域；外层目录、price、config、i18n 失效保持。实体 namespace 可用 `website/{code}/product-facts/product/{id}`；不能放在 `catalog/product/{id}` 后仍继承整个 catalog generation。
- 实施前需为已有 ProductSearchProjectionMutation changed 增加实体 impact namespace，并覆盖 AttributeValueRepository 独立 upsert/clear/delete、MediaRepository 独立写入口。现有 `website:eventSeq` 不是稳定商品事实修订，不能仅用 publish_version。库存、最终促销价格、访问权限与签名 URL 保持原各自 owner 语义。
- FileManager 的共享事实应只含已发布资产/locale 元数据；最终 URL 和逐请求访问校验不混入公共缓存。下一步先核查已有批量 hydrate 是否重复构造完整 Model，再决定是否需要新增跨请求事实缓存。

## 2026-09-08 — 成功慢缓存被误判不可用（Server 2.0.42 / Framework 2.5.42）

### 确认的缺陷与修复

WlsMemoryAdapter::remoteCall 原来将任何≥75ms操作视为服务不可用，包括已成功返回共享值的操作。它设置Worker全局2秒冷却和当前请求标识，后续所有pool的get直接null、set直接false，已有L2数据变成表面上的miss。675DB样本15次shared_read只有3次WLS与此机制吻合，但没有该次请求的故障状态证据，不能认定8.43秒全部由此造成。

已移除纯耗时判据。仅Adapter内部创建MemoryStateFacade时启用throw_on_transport_failure，Facade透传给SharedStateClient；客户端在原finally回收连接后明确报告acquire/send/read/解析失败，Adapter原异常冷却仍处理真实故障。普通客户端默认nullable合同不变，协议数组里的合法null/false/空结果/CAS冲突继续返回。未添加健康ping，不凭payload真假判断服务故障，也未扩大真实失败冷却策略。

### 可重复证据

- 生产Adapter的独立fixture首个83ms读取成功，下一池已存在值却null、写false且无远端调用；修复前7项RED，修复后Atomic/Batch/Client25 tests /189 assertions通过。
- 实际共享内存服务，两个独立临时pool（TTL90秒自动过期）、受控80ms客户端延迟：原首读84.733ms成功，第二池null、写false、available=false、仅1WLS；新首读84.567ms成功，第二池原值正确、写true且回读相同、available=true、4WLS。两组均0业务DB，脚本`/tmp/weline-slow-cache-runtime-probe.php before|after`，JSON同名前后证据。该延迟人为受控，不能冒充自然网络延迟或整页提速比例。
- Framework中央HotCache原5种phase新增resource、逻辑/实际key摘要、已计算依赖指纹/namespace路径、L1/L2/recheck状态及TTL截止时间。21 tests /102 assertions确认没有额外版本/缓存/IPC调用、无key/TTL变化。只在trace开启时生成额外摘要。timing.trace_top保留慢builder的独立meta；phase汇总只保留最后一项，详情库另有采样、600条及字段白名单限制，不能当全量miss历史。

### 下一项明确热点

分类树冷构建的51个分类URL产生102次DB及102次WLS，旧样本约80.92ms。链路为 StorefrontCategoryTreeIndex::categoryUrl → Url::getFrontendUrl/extractedUrl → SeoUrlGenerateRewrite → UrlRewrite::findLatestByWebsiteAndPath；每类先查带locale路径，未命中再查剥locale路径。建议在统一UrlManager owner增加批量预取并挂既有Context，两组路径一次取回，保留website、语言前缀、path_fingerprint及legacy NULL/空指纹优先级，单个Url调用消费同一已装载结果。不要让分类模块私写URL缓存或绕过rewrite事件；升到公共L1/L2之前先核对UrlRewrite正式changed失效覆盖。本项仍是后续计划，尚未实现。

### 编译、部署和新日志结论

reflection:compile成功：1388热点类、3845工厂。第一次framework:compile因编译期间输入变化拒绝结果；重试成功113模块/102QueryProvider/0延迟Provider。本轮4个生产文件及Server/Framework版本共6个固定路径hash在编译及部署后保持一致。codex-perf70单Worker重载6.2秒，default四Worker重载6.0秒。

| no-store、HTTP/2 200 | PID /请求序号 | TTFB | App | DB /WLS | HTML字节 |
|---|---|---:|---:|---|---:|
| 独立新Worker首商品（保留L2） |26418 /1|1781.56ms|1698.12ms|120 /349|1,281,986|
| 紧邻商品重复 |26418 /2|493.01ms|450.55ms|33 /87|1,282,922|
| 随后首页 |26418 /3|605.11ms|594.68ms|29 /74|972,896|
| 默认新Worker首商品 |27672 /1|1989.45ms|1956.89ms|327 /433|1,281,349|
| 默认同Worker随后首页 |27672 /2|490.89ms|481.49ms|167 /91|972,872|
| 独立同Worker约2分钟后商品 |26418 /4|1742.23ms|1689.75ms|260 /250|1,282,285|

全部router_profile.fpc=disabled，Content-Length与下载字节严格相同，尾部html完整且有footer。重复商品无cache builder；32卡109.71ms、21组件66.54ms、4轮边界0.31ms，父子inclusive时长不求和。不能把本组与不同负载的8.43秒样本算作稳定加速比；70ms目标未达，浏览器控制入口仍超时，未宣称随机pending/下半页视觉验收完成。

新增meta已在真实timing里出现：首商品仅`product.filter_panel.v2`一次重建164.5ms，L1/L2/recheck均absent；后续无builder。默认商品14:23:27 UTC与独立后续14:24:11的filter逻辑键SHA同为`9c5870b8…`，依赖指纹却从`e5807473…`变成`77ecbe2f…`，实际键随之变化。只读DB在14:25:05确认`website/default/catalog=1023`最后更新14:23:45，确实位于这两个请求之间；i18n仍82/13:19:01，global config/price与website config/price均早于样本。该次重建有目录版本变化证据，不再仅凭耗时猜测缓存丢失。

后续商品的慢builder明确是`product.catalog_offers`422.54ms、`product.filter_panel.v2`273.43ms、`theme.storefront_chrome`212.43ms、`product.category_menu`54.81ms。下一步保留聚合对目录更新的正确依赖，把底层商品事实按scope+product身份独立共享并通过正式changed失效；聚合重建尽量组装已有快照，只对变化实体回源。不能只删外层namespace依赖来维持旧值。分类URL批量预取亦按上一小节统一owner实施。

证据位于 `/tmp/weline-cold70-isolated/cache-health-*`，含源码manifest、完整HTTP、trace、逐阶段对照；最后权威读取 `/tmp/weline-cache-health-authority-after.json`。上述下一步尚未实施，本轮已完成的修复与整体冷启动目标分别跟踪。

## 2026-09-08 — 前台轻量发布态、后缀复用及目标插槽扫描（Theme 2.2.262）

### 已改动与边界

1. ThemeBrandResolver 通过 RequestContext::scopeIdentity 直接使用已经匹配的身份；前台整份品牌配置采用可选 readPublishedSnapshot，只读取 payload/release/source_scope，跳过工作区 source_rules/patch 等编辑器元数据。沿用工作区请求 memo 与写入口 tracked keys，无独立 brandKey 缓存；事务按实际访问键跳过旧 memo，原 default-locale、target、祖先回退及旧 Provider/preview 保留。目前没有把 appearance payload 提升为公共 L1/L2。
2. ResolveThemeCacheSuffix 通过中央 rememberForRequest 解释 URI、实时显式预览参数及范围/语言。Framework 仍派发全部事件，ThemeData、Token 校验、filename 后缀和开发文件新鲜度仍执行，不缓存可变模型或 HTML。
3. SlotBoundaryScanner 仍完整配对所有 marker、计算原深度，只为 Renderer 当前未填充目标计算 wrapper 正文边界。全局重复或畸形输入保留原全集/兼容逻辑，每轮重算变化后的 HTML offset；新生成子槽、孤儿判定、opaque 恢复和未标记页脚填充保持。

### 开发证据

- 联合边界/后缀/发布快照/ThemeContext/rollback 聚焦用例 42 tests /282 assertions 通过；独立发布快照相关组19/116与此重叠，不相加。
- 11 个新旧生产边界差分夹具 full HTML hash/字节、部件执行顺序、孤儿列表、footer 严格一致；探针只将 doRenderWidget 换为确定性输出，其余生产边界过程不变。它验证算法语义，不替代网络/浏览器验收。脚本 `/tmp/weline-slot-boundary-diff-probe.php baseline|current`。
- 真实最终 HTML 的 park 后450,676bytes，完整19 regions扫描中位5.009ms，对2个目标1.636ms；该目标为CPU诊断选择，不冒充真实页面每轮目标数量。后缀真实配置288事件CPU26.803→18.654ms、convert33.111→24.436ms，充分热32卡CLI中位127.425→114.897ms；字节/归一化hash一致，非网络速度承诺。
- 本轮预部署两个商品样本间隔129秒，第二次12次cache builder /806.88ms；两次的Worker/缓存状态和外部业务变更不完全受控，不能将其当稳定前后基线，也不能未经版本证据断言后台索引或i18n是根因。

### 真实配置只读对照

实际 scope=default.__store__.__channel__、theme=1、en_US，appearance 有效 Release=64/source_scope=default.__website__.default；旧 load(false) 与新 readPublishedSnapshot 的完整 payload、来源范围、release ID 严格相同（正反序两组）。新 RequestContext 旧7DB/8.834ms、新4DB/2.404ms；反序新4DB/2.697ms、旧7DB/4.294ms。同 Context 首次 favicon 4DB/2.369ms，随后 apple_touch_icon/logo_light/logo_dark/favicon 四键0DB0WLS共0.344ms。脚本和输出 `/tmp/weline-theme-brand-snapshot-runtime.{php,json}`，全程只读实际数据，不是空base fallback。

缓存重建追加核查：global/i18n generation=82、更新时间13:19:01 UTC，早于13:39:37/13:41:46两样本；global/storefront/config=173亦更早。本窗口不能归因全局翻译版本变化。website/default/catalog最新更新时间13:47:07在采样之后，现有latest-row不足以还原两次请求之间是否改变；无历史journal及逐键miss原因时保留未知。只读证据 `/tmp/weline-cache-rebuild-authority-probe.php`。

### 实际编译与 HTTP

官方 reflection:compile 完成3845工厂；framework:compile完成113模块/102QueryProvider/0延迟Provider。编译完成后独立实例滚动重载6.7秒，default四Worker重载11.8秒。6个运行源码文件的hash保持一致；module.php在并行工作期间由2.2.262变为2.2.263，本轮未回退他人版本，也不声称整个工作树固定。

| 全部no-store、HTTP/2 200 | PID /请求序号 | TTFB | App | DB /WLS | 完整HTML字节 |
|---|---|---:|---:|---|---:|
| 独立新Worker首商品 |72978 /1|3496.17ms|3462.56ms|326 /423|1,284,556|
| 紧邻商品重复 |72978 /2|1068.00ms|1043.48ms|37 /35|1,283,430|
| 随后首页 |72978 /3|984.02ms|962.80ms|167 /87|976,251|
| 同Worker约一分钟后商品复查 |72978 /4|8430.30ms|8373.34ms|675 /184|1,286,808|
| 默认Worker商品 |78151 /2|1656.62ms|1626.51ms|167 /488|1,282,515|
| 默认同Worker随后首页 |78151 /3|570.08ms|557.52ms|30 /106|976,091|

每个响应Content-Length严格相符，尾部html完整且含footer，router_profile.fpc=disabled。独立重复商品扫描4轮0.59ms，默认商品0.42ms、首页0.64ms，均出现新targets元信息；此前样本29.19ms。本轮运行证实新扫描路径生效，不能用不同负载样本保证整页加速。

重复商品32卡204.67ms、21组件166.61ms、expand_child_ids151.12ms/3DB。675DB复查中的15次builder累计3583.69ms为inclusive（存在嵌套，不可相加当作独占时间），并且trace达到4096 spans截断。现场主机load averages13.41/12.83/10.73，但查询数变化不能只归因CPU负载；后续须带键/版本观察真正的cache miss原因。70ms动态冷目标未达标。源manifest、headers/html、trace和phase对照存于 `/tmp/weline-cold70-isolated/render-read-*`。

浏览器本轮只读重新确认唯一profileName=Weline，实际browserId=2。创建商品页仍30秒超时并重置控制内核；未控制其他profile。HTTP完整不等于浏览器下半页/随机pending已通过验收。

## 2026-09-08 — 主题上下文重复查询（Theme 2.2.261）

### 根因与落地

`ThemeComponentCatalog::find` 命中已有定义目录之前，每次先执行 ThemeContextService 解析；后者每次重新加载同一 theme 行。实际配置进程中重复24次产生24条DB查询、18–21ms。ThemeContextService 现统一经中央 HotCache 保存按显式 Scope + area 索引的已发布绑定描述符；Context 仅携带模型纯数据，返回独立模型，不把可变对象放进进程或共享缓存。public key 不带requestId，area和显式Scope仍隔离，事务绕过复用。

无Release只缓存缺省标识，active theme结果不进入公共快照。WelineTheme 独立 save 尚无统一 changed，故主题行目前仅请求内复用。批量回滚先补齐同写事务的既有 changed 事件，再接入发布绑定依赖；现有global/storefront/theme覆盖所有资源及后代，本轮未擅自缩小写失效范围。

### 模板初始化小幅优化（Framework 2.5.40）

保留所有 init，只将无状态 req/env 代理放到既有 Context，并让同次 init 共用 State 的语言结果。动态 URL、标题、语言切换和嵌套模块字典不改变。Refresh/FiberScoped 共13 tests/53 assertions通过，有7个既有按引用传临时Request notices；额外HookI18nContext因未安装WeShop_Notification在旧新代码均失败。CLI对照各3轮，旧control中位134.272ms/init55.408ms，新126.104ms/init49.455ms；288次init均保留，244810bytes及归一化hash相同，不冒充网络Worker收益。

### 真实配置与WLS证据

- 原始基线 `/tmp/weline-theme-context-probe.php`：同请求24次为17.99/20.73ms、24DB；改后0.98/0.90ms、0DB0WLS，后续请求首读0.48ms、1次版本检查。
- 两个独立PHP进程 PID21266/21271 使用正式 NamespaceScopedCachePool→WlsMemoryAdapter（探针在设置WLS模式后构造标准CacheManager，避免CLI bootstrap先创建FileAdapter）：同请求24次0.782/0.769ms，0DB0WLS；换请求24次1.365/1.258ms，仅一次namespace clock DB。进程A构建显式website/backend/global键，进程B首次均零DB且实际cache_get命中；4组scope/area键不同，8组业务行与基线严格一致、模型marker不泄漏。
- 脚本 `/tmp/weline-theme-binding-runtime-probe.php`，命令参数 `wls-a --fresh-wls-manager` / `wls-b --fresh-wls-manager`；结果 `/tmp/weline-theme-binding-wls-{a,b}.jsonl`。早先after-a/b使用FileAdapter，只作文件共享证据，不能冒充WLS。
- 当前实际 resolver 都为 `theme-package-default`、sourceReleaseId=null；虽有历史workspace/release行，但无当前有效绑定覆盖。真实验证只覆盖无Release共享回退分支；正绑定由行为测试覆盖，未为验收改动业务主题发布数据。回滚行为夹具用SQLite持久行、生产Workspace/TransactionCoordinator/ResourceChangeFactory，revision/observer边界为测试替身，不冒充配置环境发布验收。
- scoped定向回归38 tests /177 assertions通过；独立代理另跑12/80（与该组重叠，不能相加）。HTTP新Worker与动态重复结果在本节后续补充。

### 编译、运行与实际限制

官方 reflection:compile 完成1387热点类/3845工厂；framework:compile完成113模块/102QueryProvider/0延迟Provider。ThemeContextService、Workspace、Template及两模块版本共5个固定路径的hash在编译和重载后保持一致（不代表整个并行工作树未变）。Theme 2.2.261 / Framework 2.5.40 已滚动重载到 codex-perf70 与 default。

| 动态场景（全部no-store） | PID / 请求序号 | TTFB | App | DB / WLS | HTML |
|---|---|---:|---:|---|---|
| 修改前独立Worker重复商品 |73792 / 8|370.05ms|359.54ms|76 /29|1,210,675bytes，完整+footer|
| 修改后独立新Worker首商品，保留L2 |27184 /1|471.64ms|452.14ms|43 /123|1,211,704bytes，完整+footer|
| 紧接商品重复 |27184 /2|392.58ms|384.04ms|45 /28|1,211,704bytes，完整+footer|
| 同Worker随后首页 |27184 /3|287.80ms|278.07ms|37 /57|974,590bytes，完整+footer|
| 默认Worker商品（已有外部请求，不称首请求） |27778 /3|508.51ms|以trace为准|45 /111|1,211,704bytes，完整+footer|
| 默认另一Worker首首页 |27849 /1|593.07ms|以trace为准|83 /249|973,654bytes，完整+footer|

全为HTTP/2 200，Content-Length严格匹配且尾部有完整html结束标签；router_profile.fpc=disabled。重复商品query数76→45，插槽expand_child_ids为24DB/33.60ms→3DB/21.54ms，theme.layout.context从7DB/2.68ms→0DB/0.19ms。整页重复TTFB 370→393ms，没有证据声称这轮整体稳定加速；DB时间19.69→42.76ms存在波动，并行站点/业务变化也不受控。冷新Worker只清进程L1，保留共享L2。70ms动态冷目标仍未达标。

剩余主要CPU阶段：当前商品卡片32次84.68ms、组件21次49.66ms、边界扫描4次29.19ms（父子inclusive阶段不得相加）。扫描器当前在过滤已填充/无部件slot前，就为每个region计算wrapper并遍历内文。只读真实最终HTML探针发现park后449667bytes/19regions，全scan中位4.578ms，纯marker配对0.018ms。下一步可先筛当前未填充目标ID，再计算所需wrapper；每轮HTML确实变化，不得机械复用旧offset，也不得跳过恢复及fillRemainingUnmarkedSlotWidgets（包含footer子槽）。证据`/tmp/weline-slot-boundary-cpu-probe.{php,json}`。

本轮浏览器重新核验唯一Chrome profileName=Weline且browserId匹配；读取已有en_US/products标签仍25秒超时并重置自动化内核。未另造标签、未连接其他profile；仅可确认HTTP完整，原随机pending/下半页的视觉结果尚不能宣布已验收。证据统一`/tmp/weline-cold70-isolated/theme-context-*`，含源码manifest、curl、headers、html、trace和phase comparison。

### 后续优化项

1. `WelineTheme::save_after` 及 `ThemeAiDraftService::publishDraft` 缺完整changed；先补权威事务和失效覆盖，才可提升模型行/组件定义到公共缓存。
2. Template嵌套卡片初始化：32张完整合成卡片触发288次init和路径定位；保留必要动态操作，逐项移除重复读取。CLI上限实验并非实际WLS收益，不能无条件skip init。
3. Template路径L1命中前重复environmentHash及主题后缀事件；应在既有Context复用已解析路径，但必须包含模块、主题/预览、语言、force/skip及开发文件新鲜度，不缓存个性化HTML。
4. 商品投影仍有message、name回退及campaign_label/eligible_campaigns默认标签的翻译依赖。只拆库存消息不能解除global/i18n失效，应一次划清事实与展示边界；本轮不实施无已测收益的局部重构。

## 2026-09-08 — 翻译领域定向失效与真实运行验证（Framework 2.5.39 / I18n 1.0.33）

### 本轮落地

- `DictionaryCacheNamespace` 统一 `global/i18n`，复用 NamespaceGenerationSnapshot、RequestContext 和原有 L1 数组 / namespace L2。公共缓存键不带 requestId；事务和权威服务不可用时不发布公共结果；多代数组有容量边界。I18n Parser 只是 Framework 委托，不另建缓存系统。
- Dictionary upsert/delete、AI save、语言六个生命周期入口及 Locals save 让事实与 `w_changed` 共用事务。I18n ResourceChanged 从异步清池改为 critical sync 推进版本，afterCommit 仅使用既有 namespace 通知；普通翻译更新不再触发 RuntimeControl `cacheClear`、所有池清空、ObjectManager 清空或全局 OPcache reset。
- 翻译后展示值也必须声明依赖：Search provider_types、Product category_menu 与三种 catalog offer 投影通过中央 CachePolicy 声明 `global/i18n`。原始 category_tree / category_links / category_presentation 保留 catalog 依赖。三种 offer 投影已包含“库存不足”“仅询价”译文，因此本轮不能将它们当纯事实保留旧值；后续应把翻译移到渲染层，以减少重建。
- FPC 常规向量包含 global/i18n；首页 receipt v2 保存 scope 和 namespace fingerprint。Worker 的早期查询用临时 Context 检查权威 @clock，结束恢复原上下文；旧回执、事务回执与过期版本回退正常路径。SQLite 测试验证漏通知仍能拒旧，静态 JS 无回执时零新增版本查询。
- 语言文件在业务提交后发布；按现有 i18n_pack 修订行事务锁串行完成读词典、原子替换与 changed。changed/版本异常在释放锁前恢复旧文件，显式发布保留版本推进，取消不安全的“文件内容相同就直接成功”。物理 DB commit 失败下的跨介质完全原子性不作保证；未添加新发布状态机。

### 真实证据

- 反射编译：1385 热点类 / 3844 工厂；框架编译：113 模块 / 102 QueryProvider / 0 延迟 Provider。源码清单 30 路径除 Product 模块版本被并行任务继续推进外均保持原 hash；不声称整个脏工作树不可变。默认 4 Worker 与 codex-perf70 1 Worker 重载成功。
- 真实 PostgreSQL + WLS 共享池，两持久 PHP 进程（PID 73308/73309，WLS 模式，非冒充网络 Worker）完成 17 步：负缓存→alpha→beta→删除均被下一请求正确读取；回滚时 @clock 1682→1681、译文恢复 alpha；另一语言文件在未提交事务内不存在，回滚后词条不存在。无关 product 测试缓存保留。词条/测试键通过正式入口精确清理，审计修订记录遵循既有持久化规则。
- 实际 en_US 文件发布 PID 79250 成功，195.34ms / 14 DB / 18 WLS；前后 product 测试键都存在，完成后删除。不是伪造通知或手工 bump 版本。

| 场景 | Worker / 请求序号 | TTFB | App | DB / WLS | 结果 |
|---|---|---:|---:|---|---|
| 独立实例重载后首个商品页，保留已有 L2，no-store | 73792 / 1 | 585.69ms | 557.00ms | 74 / 118 | H2 200，1,210,539 bytes，完整+footer |
| 稍后第二商品请求，中间状态不受控 | 73792 / 2 | 1121.20ms | 1105.47ms | 302 / 231 | H2 200，出现 12 个缓存 builder，不能称稳定暖样本 |
| 首页发布前命中 | 73792 / early FPC | 6.91ms | 绕过 App | 无 trace | 完整+footer |
| 正式语言文件发布后首页 | 73792 / 4 | 766.56ms | 以 trace 为准 | 385 / 719 | 拒旧回执，MISS 重建，完整+footer |
| 随即首页重新命中 | 73792 / early FPC | 5.48ms | 绕过 App | 无 trace | 完整+footer |
| 语言发布后商品动态重建，no-store | 73792 / 5 | 935.98ms | 920.10ms | 331 / 227 | 完整+footer |
| 紧接上项商品动态重复，no-store | 73792 / 6 | 382.44ms | 373.79ms | 78 / 37 | 完整+footer |
| 默认新 Worker 首个商品，no-store | 82094 / 1 | 543.47ms | 以 trace 为准 | 76 / 114 | 完整+footer |
| 默认另一新 Worker 首页，no-store | 82095 / 1 | 606.27ms | 以 trace 为准 | 139 / 268 | 完整+footer |

动态样本 router_profile.fpc=disabled；“新 Worker 冷请求”保留共享 L2，不等同全库/全缓存清空。不能把 5–7ms FPC 命中当成动态冷启动达到 70ms。请求 ID、完整 timing 和 curl 证据在 `/tmp/weline-cold70-isolated/i18n-selective-*.{json,trace.json,headers,html}`；事务证据 `i18n-selective-runtime.json`，发布日志 `i18n-selective-publish-runtime.log`。浏览器本轮唯一 Weline 实例已核验，但创建标签仍 20s 超时并重置自动化内核；不宣布原“半截页面/pending资源”的视觉验收完成。

### 下一步（70ms 目标仍进行中）

1. 动态重复请求 App 373.79ms 中 DB 仅 25.22ms / WLS 8.52ms；32 个 card render 74.43ms、21 个组件 render 51.23ms、4 次边界扫描 29.06ms。组件父阶段为 inclusive，不相加制造总量。继续收敛卡片和组件模板重复组装。
2. `theme.slots.expand_child_ids` 28.08ms 包含 24 次 DB（5.77ms），现有 ThemeComponentCatalog / ThemeContextService 的已发布主题与组件上下文仍值得合并和批读，按发布版本共享，草稿不进入公共池。
3. 发布翻译后 8 个共享 builder 包含 237 DB / 464.53ms；将 catalog 投影的固定提示翻译移到渲染层，再让原始商品投影与词典更新解耦。当前依赖补齐保证正确性，并非此处性能已完成。
4. 继续收敛其它已确认全清入口（Website legacy fallback、Theme 清理），以及直接词典收集/编译入口的变更规范；不擅自打开 publisher_enabled 或关闭 legacy fallback，先完成各自读写依赖验证。

本轮 MCP prepare/get_edit_bundle 返回 MCP_RUNTIME_STALE，已按限定精确路径原生回退；回执 `weline-mcp-1788869667977-836888a41404882f` / `weline-mcp-1788869696299-906f8dfae3e41438`。未提交、重置或覆盖其他 AI 的工作树修改。

# 统一缓存范围与性能优化

## 2026-09-08 — 冷路径批量读取与独立实例测量

- 2.5.38最终编译及运行：113模块/102QueryProvider/0延迟Provider，所涉6个源码/版本文件编译前后SHA256一致；默认四Worker及独立实例均滚动重载成功。默认商品页新PID53937/count1：TTFB1640.47ms、App1584.69ms、374DB/195.04ms；首页新PID53895/count1：TTFB555.90ms、App543.69ms、126DB/31.87ms。两页HTTP/2 200、no-store且trace fpc=disabled，1,209,490/976,810字节和footer/html完整。全局模块词典批量读取分别42/17次、均0DB、零异常，模块CSV批读也零异常，跨Worker共享入口的局部目标已验证。
- 同批独立实例PID52394/count1/2：商品页TTFB1758.93/449.50ms，App1703.35/440.25ms，DB384/76。新Worker仍有12次业务cache builder合计848.40ms（inclusive）；默认商品页9次builder756.11ms，不能与包含它们的header/controller再累加。响应完整不等于70ms达标，也不代表浏览器视觉验收完成。证据前缀 module-shared-cold/warm、module-shared-main-products/home，源清单位于module-shared-source-manifest.json。
- 默认追加一次同页检查实际落到原PID53937/count246，TTFB1947.93ms、App1914.63ms、643DB；不是新Worker peer验收。距首样本约130秒，全局词典重新20DB、业务builder8次995.34ms/510DB，说明还存在缓存再次变冷。源码发现I18n变更广播全量cacheClear会连带所有pool、OM和OPcache清理；目前尚未将该样本与某次I18n广播在运行记录中一一对应，不能称已证实此次慢请求因果。证据module-shared-main-products-peer。
- GlobalDictionary稳定模块共享已实现（Framework2.5.38）：[A,B]→另一进程[A]→[A,B]仅首次一次批量SQL，两个模块键；保留原single-flight和旧Provider路径，失败后公开Parser::parse可恢复，交错回填合并最新进程快照。配置DB只读验证PID42533首次Filters+Product为1 DB/2.28ms、359词，PID42581两次复用均0DB；与原words()输出散列严格一致。使用隔离探针前缀，不清正式缓存；探针两键已删除。相关完整Phrase、namespace和Provider回归57 tests/228 assertions通过，整页重载数据已见本节顶部。
- 全局池批量包装已修复（Framework2.5.37）：真实 CacheManager 返回的无namespace包装器保留原始键/标签；9个新用例及8个范围用例17 tests/58 assertions通过。配置运行时两个独立CLI进程通过同一phrase池共享203项，1次cache_mset/7.24ms、1次cache_mget/4.47ms，严格值一致、零DB；探针TTL60秒，仅清理自有探针键。后续真实网页 `namespace-readiness` 的 module_cache_get 22次、3.46ms、errors=0、22次WLS，证明批量入口已生效；54次module_cache_set表示本样本CSV快照仍有L2缺失，不能冒充全部热命中。
- 上述网页采样时维护页已恢复，但新PID25413/count1不是本任务主动受控重载；该商品页TTFB2555.59ms、App2138.02ms，完整HTTP/2 200且FPC disabled。curl同时记录TCP0.17ms、TLS完成377.19ms，差值主要包含握手；这不能据此归因于App末尾日志。随后同一静态JS的四次采样，TLS为3.18–20.66ms、TTFB9.32–28.74ms，说明握手尖峰不能代表常态。证据 `namespace-readiness.curl.json`、`tls-static-samples.json`；未更改TLS调度代码。
- 最新批次：模块词典共享键批量读取、EAV 规格身份投影和标量行装配均已编译并重载。默认实例新 Worker PID 12983/count=1 的请求 `f922aa4d02a3aa56-102248488546791` 为 HTTP/2 200，TTFB 934.15ms、总计950.31ms、App 877.90ms，166 DB spans/113.28ms、282 WLS spans/38.23ms。使用 `Cache-Control: no-store` 且 trace 明确 `fpc=disabled`；1,208,277字节与声明一致，footer/html 完整。证据 `scalar-main-products.{json,headers,html,trace.json}`。此为EAV/模块CSV批次的历史默认实例结果，最新批次见顶部；不是浏览器视觉验收。
- 同批独立实例串行重载后 count=1/2 分别为 TTFB 1524.50/713.28ms、App 1457.86/472.17ms、DB spans 296/80。与前批身份投影的1538.42/348.60ms相比，共享L2 builder、运行负载和页面数据不同，不能宣称整体稳定提速；热样本 App 与首字节还相差约241ms，归属未确认。证据 `scalar-row-cold`、`scalar-row-warm` 及 `identity-batch-cold`、`identity-batch-warm`。
- 本次局部验证：模块词典模拟池2模块×2语言由4个共享单读变1个批读，相关22 tests/91 assertions通过，但真实池cold/main分别19/18次全部异常；NamespaceScopedCachePool空namespace的批量路径拒绝空指纹，尚不能声称实际批量读取生效。24商品规格URL由14个DB spans变9个，原始参数散列严格一致。相关选项实际1152行（1072+78+2），不是78行。现有PG索引下1072行SQL仅0.42–0.53ms、36页全命中；重复ORM装配才是这段额外开销。改为已有 fetchIterator 数组接口后该URL段36.18ms、DB spans总8.35ms，消费者仍取得独立对象；35 tests/487 assertions通过。独立实例热请求的相关选项DB span由前批10.84ms降至本批2.08ms，只说明该段，不等同整页同幅度下降。
- 默认实例也已编译并完成四 Worker 滚动重载（8.6 秒）。新 Worker PID 50193 的首请求 `61af8b7bc4e45be7-99722737465708`（request_count=1，FPC no-cache/MISS）为 HTTP/2 200，TTFB 886.68ms、总计897.86ms；187 DB spans/98.06ms、350 WLS spans/55.22ms。1,203,483 字节与 Content-Length 一致，footer/html 尾部完整。它是默认实例的运行证据，仍未达到70ms；共享业务L2保留，不能称为全缓存冷启动。证据 `main-products-accepted.{json,headers,html,trace.json}` 位于 `/tmp/weline-cold70-isolated/`，文件名不表示浏览器视觉验收完成。
- Filters 1.0.8 在范围缓存返回最终面板后，统一预取价格、属性及选项标签，原模板 `__()` 优先级保留。207 词行为用例由逐词读取变为 200+7 两批；真实新 Worker 的样本 `c147788a76676e8f-98930660536583` 为 890.65ms，词典逐词查询 0、有限批量查询 2。共享 L2 未清空，不属于全业务缓存冷启动。
- Template 同值路径写入修复后，串行重载及两次 FPC no-cache 请求排除了并发验收抢占首请求：PID 44292/request_count 1 和 2，TTFB 分别 1235.69/543.32ms，DB spans 178/81，WLS spans 332/85。32 张卡片阶段冷/热 WLS 分别为 19/5；此前31购买区×2 Hook 的62次固定同值写已由行为测试证明消除，仍保留其他真实读取。响应均 HTTP/2 200、字节和尾部完整。这两个样本属于模板修改批次，后续批次结果见本节顶部；不能用更早低负载样本替代实际新数据。
- gzip, deflate, br 协商的单连接 26 并发也通过：两个 HTML 实际为 gzip（145454→1196832、120202→974462 字节），其余24资源 identity；均正常结束且线上长度匹配，解压后 footer/body/html 完整。本次最长281.49ms，证据 `h2-compressed-transfer-results.json`。它曾先于一次预期冷测发出，故 `products-after-template-cold` 实际 request_count=4，不计作冷启动；随后已重新串行重载得到上述 count=1/2 样本。

### 尚未收口的目标与依赖

- I18n全清还有第二个生产入口：AiTranslationPublisher::publishLocale→refreshRuntimeCaches；AI每批翻译成功可能直接触发，当前未走w_changed。因此仅改ResourceChanged Observer不足。建议统一global/i18n namespace，生产者事务内声明impact，沿现有critical CacheNamespaceObserver→bumpMany→提交后publisher；非persistent语言PHP文件只做定向stat/OPcache失效。Parser及I18n TranslationResolver/ActiveLocaleCodeProvider公共L1/L2都需消费请求捕获的generation，避免通知时清静态数组与其他Fiber回填交错。
- FPC必须同步补翻译依赖：StorefrontCacheKeyContextResolver当前没有i18n namespace；首页internal warmup receipt只校验形状/URL/摘要，没有复核版本向量，不能仅增加普通FPC key。receipt须使用既有请求authority校验，失效后回正常路径。当前Framework默认publisher_enabled=false/legacy_full_clear_fallback=true；publisher是加速器，NamespaceGenerationSnapshot每个新请求首次已有DB @clock校验，不能把disabled直接等同正确性失败，也不能未经兼容性审查关闭其他模块legacy fallback。Website/Theme还存在各自全清入口，需分开收敛。
- 因果证据边界：真实主库Outbox/Delivery总行数均0，cache_clear控制回执只保留内存last，无该区间历史；尚不能认定11:59至12:01这次重冷由I18n事件造成。未主动发全清来制造复现，未改全局开关。下一轮应以双Worker更新、旧首页receipt失效、事务回滚、漏通知纠正、Product/分类缓存仍命中为定向失效验收。
- **优先核查失效范围**：I18n ResourceChanged已先清phrase/i18n池，随后RuntimeCacheBroadcaster又调用无范围cacheClear，广播全部运行实例；worker_ssl的处理会opcache_reset、clearFrameworkPools和clearInstances，业务事实一起变冷。现成namespace invalidate协议只更新代次，不会自动清Parser静态L1；修复需同时接入词典L1/L2与翻译HTML/FPC依赖，不能简单删广播或只替换IPC。此缺陷已由代码确认，当前正在核对真实事件与清理时间，尚未修改该链。
- 实测模板CPU优先级：`scalar-row-warm`的32次商品卡累计114.40ms，DB0/WLS0.99ms；head47.05ms与body-end45.64ms是顺序分支，合计92.69ms，不能再与包含它们的layoutFetch108.08ms相加。现有证据不足以把卡片全部归因于Template初始化（单次init约0.05ms）；后续最小标签应覆盖labels/shopper-actions/add-to-cart三个子模板和空Hook。保留head因SEO而不共享整段HTML的语义，只复用语言目录、favicon等公开输入事实。
- Slot字符串处理有明确上限：warm opaque park8.09ms、boundary scan四轮26.06ms、restore2.56ms、batch replace0.17ms，互不嵌套合计36.88ms；不能把processSlots175.41ms都算作字符串复制。可研究SlotBoundaryScanner对子树重复扫描，保持raw-text、引号、嵌套和畸形标签处理；`expand_child_ids`26.72ms还包含24次DB读取部件定义，应单独核查现有Registry/Context复用。1,209,771字节HTML中script333,059/style133,622，内联Visitor脚本165,509字节；这些体积影响浏览器解析但本轮未实测渲染CPU，不据此猜测半页空白根因。
- 全局词典模块组合缺陷已在2.5.38修复，见本节首部。仍沿用既有phrase池清理及Worker广播，未增加数据库namespace代次；裸Dictionary::upsert本身没有发布ResourceChange，不能把调用方现有事件覆盖误报为所有独立写入口均自动失效。
- 诊断开销已单独分析：当前 DEV 自动全量采集，面板采样在采集之后，false配置和`X-Weline-Trace:0`不覆盖DEV。8720次真实类/Fiber微基准为关闭约5.00ms、4096条明细加4624丢弃约15.49ms，导出约0.68ms；不能把整页873ms都归因于trace，也不能只看尾部telemetry=2.55ms。现有单实例启动参数未提供已核实的DEV/trace独立关闭入口，未改全局env。Trace已用state对象更新，不能把span数量当成RequestContext::set次数。证据 `/tmp/weline-trace-cpu-microbench.{php,json}`。
- 后续范围缓存审查发现：`Theme/Service/SlotRendererService::runtimeCacheGet/runtimeCacheSet` 目前分别恒定返回 null/空操作，外层已发布布局只有私有进程缓存；不能把这条链视为已经完成 WLS 跨 Worker 复用。历史注释记录共享 IPC 曾慢，不是继续另造私有缓存的理由。建议把公开的已发布布局投影接入同一 CachePolicy/HotCache，并复用 Theme 发布时已有 `w_changed` namespace；键须保留 area/theme/layout_option/page_type/target/locale 和完整范围，预览、草稿及编辑事务不进入公共缓存。先以真实冷 Worker 的 `getLayoutData` 查询与耗时验证收益。
- `ThemeScopedWorkspace::load(context, false)` 虽有请求 memo，仍返回工作区 revision、draft_revision_id 等编辑信息，不能为了前台速度直接把整个返回数组跨请求缓存。应复用同一上下文，但抽取明确的 published payload/release 投影用于前台，并让发布指针与祖先继承版本参与失效；编辑器仍使用权威工作区读取。`ThemeScopedPreviewResolver` 的布局/翻译/外观等读者可以共享该公开投影，避免下一请求重新组装相同 Release。
- Theme 已有单项/批量发布在事务内调用规范 changed，但 `ThemeScopedWorkspace::rollbackReleaseBatch` 主动回滚已发布版本时更新 Release 指针后没有调用同一 helper，目前依赖 HTTP RequestService 的提交后清理。接共享 published 投影前须补正式 API 的主动回滚失效。此处区别于数据库事务 rollback：前者成功提交必须推进版本，后者必须保持原版本且不广播。复用 `ThemeEditorContext::identityParts` 和现有 `ThemeRuntimeCacheCleaner::scopeThemeNamespace`；当前每次发布还会推进 global Theme 版本，失效正确但范围偏宽，不能承诺兄弟网站缓存不受影响。建议公开投影仅含 payload/effective_release_id/source_scope/fingerprint/has_release/no_widget_placements，staleTTL=0；验收覆盖继承、target/locale、空布局、草稿、发布及两类回滚。
- `RequestContext::set/remove` 会取出并回写整个 storage 数组，存在随 memo 键数增加的复制成本。仅本地微基准已观察 5000 次写入在100/1000/5000键下约4.55/18.39/129.30ms；尚未取得真实页面键数/写入次数，因此暂不修改核心上下文或把它认定为当前主因。若后续实测证明显著，优化应在 Context 内原位处理单个 literal key，并保留点号键、null/has、快照及 Fiber 隔离语义，不另建静态请求袋。
- 70ms 动态冷启动未达标：2.5.38最终默认实例新 Worker、FPC bypass的首次商品页1640.47ms，独立实例1758.93ms；业务缓存状态持续变化，不能用旧的934.15ms代替当前数据；整页缓存 HIT 的48.69ms不能替代这一口径。另一个并行任务已调整 FPC：Framework 2.5.35 起请求 `no-cache` 仍可命中整页缓存；后续动态验收改用现有 `no-store` 旁路，并核对实际 FPC 状态，不能沿用旧脚本假设。
- 浏览器控制连接超时，不能宣称浏览器下半页和所有 pending 资源已恢复。HTTP/2 identity/gzip 并发验收仅证明这些受测请求的传输完整。
- 对保存的默认实例 HTML 和直接引用启动脚本只读复核：main/footer 已服务端输出、祖先无 hidden 属性；5个同步外链均在 body 之前，不能单独解释已显示上半页后的截断；customer-service 通过 window.load 后的模块加载器启动。未找到这些脚本主动隐藏后半页的确定路径，因此不添加猜测性的 lazy/卡片修复。模块配置加载 `ensureModulesConfig()` 的 async script 缺少自己的超时，确实可让后续模块初始化等待，但没有证据证明它隐藏 SSR footer，须与传输/浏览器问题分开验收。
- typed EAV 值若加入跨请求缓存，须先补原子写入与规范失效：`EntityAttributeStore::upsertScopedRow` 的事实更新及 namespace DB 版本推进放入同一事务，复用现有 `w_changed` / `CacheNamespaceObserver`；仅内存快照更新和广播在提交后执行，回滚不发布新版本。不得用博客模型保存事件冒充独立属性写事件。
- EAV 薄协调器可复用 `CachePolicy + StorefrontScopeHotCache`，按 global/website/store/channel 及祖先回退定义 namespace，逻辑值键含明确 scope/entity/owner/attribute/locale；这些 typed 值 namespace 尚未实施。外层 FPC/分类树聚合还需声明同一依赖，否则底层值刷新后仍可能命中旧 HTML；当前 FPC 向量只有 config/catalog/price/theme/auth。

### 前序测量明细

- 促销选择复用现有选品关系/权威身份批量接口：205 个商品的关系读取 205→2，身份读取 204→2；保持店铺继承、排除、类型和 UUID 校验。搜索分类直接消费基础分类树，移除不用的导航 URL 生成，真实查询 116→14，输出严格一致。
- Blog 分类五个 typed EAV 字段走正式批量接口，按实际值表查询；真实 86 分类读取 443→17 SQL，同请求重复读取 0 SQL。独立 typed EAV 写尚无规范 changed/提交后 namespace 失效，本轮只做请求复用，不把这些值写入跨请求缓存。
- CachePool/Server 接通已有 MGET/MSET；两个独立 PHP 进程通过真实 WLS 共享 203 项仅需一次写 RPC 和一次读 RPC。Parser 已知词预取复用原有 locale/词条信封，第二 Worker 可直接读取确认命中和缺词。
- 额外定位 EventsManager 未注册事件回退全模块扫描，连带执行 event.php 内翻译；已修复为读取 Registry 快照。三管理器×两未知事件的 XML 读取 6→0，动态事件、刷新失效和显式扫描均通过行为验证。
- 独立测试实例 `codex-perf70` 为 1 Worker、19655 端口；使用相同项目和共享服务，并未清空所有业务 L2。事件修复重载前的第一个商品页请求 `80e1635c52273dbf-97715485002458`（PID 90858、request_count=1）为 HTTP/2 200，TTFB 1663.84ms，796 DB spans / 253.63ms，1663 WLS spans / 205.21ms；仅 2 个范围缓存 builder，许多公共事实已命中共享缓存。
- 同实例重复请求 FPC HIT 为 48.69ms；`Cache-Control: no-cache` 重新渲染商品页为 1017.80ms，156 DB spans / 208.97ms。响应均与 Content-Length 一致，含 footer 和 html 结束标签。FPC HIT 不能作为 70ms 冷启动达标证据。
- 事件修复编译后独立实例滚动重载 6.1 秒完成。新 Worker PID 10730 第一个商品页（FPC no-cache）TTFB 3749.62ms / app 2658.69ms，499 DB spans / 389ms，1450 WLS spans / 372.84ms；随后首页 329ms、博客 605ms，均 200 完整。与前样本负载、缓存状态不同，不能只按时长判断改动收益；70ms 未达标。
- HTTP/2 单连接真实 26 并发（2 HTML、10 JS、8 CSS、6 图片），接收窗口为流 16MiB、连接 64MiB：26/26 状态 200、END/close 完成、Content-Length 一致，无超时/错误/GOAWAY，共 2,713,617 字节。两个大 HTML 的 footer 与尾部完整，商品 TTFB 1521.20ms、首字节后 1.71ms 传完；本次主要耗时在响应生成。证据 `h2-transfer-results.json`，脚本 `h2-transfer-check.mjs` 均位于上述目录。
- 浏览器导航及网络诊断连接仍超时，尚未验证下半页实际渲染和 pending 资源消失。原始 HTTP/trace 证据：`/tmp/weline-cold70-isolated/`；此处未用纯 HTTP 替代浏览器视觉验收。

## 2026-09-08 — 复用链修正与运行验收

- Framework 修正 Fiber 让出执行后 namespace 进程快照回退/丢成员；State 直接复用已有冻结语言货币。SystemConfig 写侧保持权威查询，写后清理继承版本向量及精确模块行；ACL 请求菜单快照绑定现有 generation。
- Product 修正详情报价 Context 生产消费、分类显式语言键、新品日期窗口/分页键、有限推荐候选查询与有效结果补足。Blog 公共原始行接同一 HotCache，使用显式网站/语言维度，关键词和分类批量读取；Post/Category/LocalDescription 通过正式 changed 事务推进内容版本，提交后通知 Worker。
- 当前工作树经 reflection:compile、framework:compile 后，default 四 Worker 在 6.4 秒完成滚动重载；首页/商品列表/商品详情返回完整 HTTP 200。首字节样本分别为 957.87 / 3559.94 / 1604.24ms，不能称为 70ms 冷启动达标，也不能把其他 AI 同时改动及运行负载的影响全部归因于这次修正。
- 商品列表请求 `01a1e5dc1f3ea352-94282431419458`（PID 2256，worker 3，第 3 个请求）：网站匹配 0.38ms，整个 URL 解析及上下文安装 44.69ms；后续页头 895.61ms、目录构建 582.93ms、卡片渲染 447.92ms。促销筛选仍为 532 DB spans / 304.25ms，页头搜索类型为 443 DB spans / 285.51ms。下一步应按这些实际读取链合并查询并复用范围事实，不应继续把主要原因归到网站匹配。
- 翻译逐词回退仍有 275 DB spans，但此样本仅 195.29ms；可在已知标签/规格词列表处调用现有 `Parser::prefetchWords()` 批量预取。L1/L2 已缓存确认缺词，不应增加另一套词缓存。完整 trace 是 inclusive 且明细截断，不能累加父子耗时或将所有子 DB spans 认定为词典 SQL。
- 博客在真实配置数据库创建临时草稿 328，验证提交后版本 2→3、同请求 fingerprint 更新、修改读回、回滚保持数据及版本、WHERE 删除推进版本，全部通过并已删除草稿。脚本/结果：`/tmp/weline-cache-remediation-blog-runtime.php`、`.json`。
- 浏览器控制连续超时；只验证了 HTTP 包含 footer 和结束标签，未完成浏览器下半页视觉验收。后续 `/blog` 和重复商品页请求返回维护模式 503，`app/etc/env.php` 的 `system.maintenance=true`；没有关闭其他任务的维护状态。HTTP/trace 证据：`/tmp/weline-cache-remediation-http/`。

## 2026-09-09 — 缓存类自带进程内存储（设计口径；代理层落地已撤销）

- **取消** Agent MCP 硬规则 `cache_lookup_tier_process_shared_db`（不再要求业务类自己叠「进程内 → 共享 → 库」）。
- 曾尝试在 `CachePool` 代理层加 `processStore`（同 key 读写清）；**因多 Worker 进程内不对等已撤销**（本 Worker 清不掉他 Worker 的进程内袋，会读到陈旧值并跳过驱动）。
- 跨 Worker 一致的进程内袋须有代次/广播（如 WlsMemoryAdapter 的 local epoch），不能只在无协调的代理层数组上做。
- 业务类仍禁止再自做平行类属性/`static` 袋绕开共享缓存 API；RequestContext 仅作请求级编排。

## 2026-09-08 — 上下文与范围缓存复用读取链

- 历史落地：现有 Context / HotCache / WLS 共享池与 DB 的复用链；进程内加速留给带失效协调的 Adapter/专用组件，不在无 epoch 的 CachePool 代理层默认加袋。
- `RequestContext::get/set` 与 `Context` 同源，禁止同一份数据双写；公共键含业务范围 + 语/币 + 版本向量，**不含 request id**。
- 本轮落地：`NamespaceGenerationSnapshot` 同 clock 补齐；`SystemConfig` 精确模块行快照 + 版本向量请求内一次；分类本地化展示 `rememberPolicy`；产品 widget 有限候选/`publishedOfferSummaries`；博客正文 Context + slug 批量；ACL 公共菜单源；维护关闭早退。
- 禁止再造平行 `*Data` / 私有 RouteContext 袋；店面实体复用 HotCache / Model identity map / `w_query`。

## 2026-09-08 — 后台菜单公共词典精确批量预取

- 询价编辑页实测请求 `7daf04adde8bad86-78442110694916` 总耗时 22.03 秒，侧栏约 9.92 秒；`i18n.phrase.global_word` 552 次调用包含 467 次数据库查询。这里是不同菜单词冷查询的 N+1；原有逐词缓存已缓存空结果，问题不是同一缺词反复查询。
- `Parser::prefetchWords()` 接收已知词列表，按现有语言回退顺序、每批最多 200 个词调用可选 `BatchGlobalDictionaryProviderInterface::exactWords()`。只填充已有公共词 Worker 缓存（含确认缺词），复用 `clearWorkerCaches()` 失效；请求覆盖、模块词典和语言回退优先级不变。查询失败不写负缓存。
- 入口不使用逐词共享缓存读写，也不产生任意词组合的共享缓存键：当前 `CachePool::getMultiple/setMultiple` 内部逐项调用，直接精确批量 DB 查询避免把 DB N+1 替换成 RPC N+1。普通逐词查询的共享 phrase 缓存保持原有行为。
- 当前配置数据库上的 200 个实际后台菜单词（`en_US`，17 个译文命中）验证：逐词 200 条 SQL / 355.59ms；批量 1 条 SQL / 16.20ms；预取后逐词 0 条 SQL / 0.09ms，所有命中和缺词结果一致。这是词典路径验证，不能替代浏览器整页验收，也不能据此认定所有卡顿已解决。

## 2026-09-07 — 冷请求瓶颈实测与变体接口轻量化

- 结论：本次冷请求的首要瓶颈是缓存未命中后的页面组装与重复投影，不是已经被证实的表结构或索引问题。受控商品 PDP 首次请求 HTTP 200，curl **26.818s**、应用 `total_ms=25,965.86`；其中 DB 累计 **11,671.57ms / 1,359 spans**，但最大的业务阶段是 `storefront.cache.builder`（含页头、分类树等多次冷构建）、`theme.partials.fetch.header` **10,815.34ms**、`theme.header.search_types` **7,201.47ms**、完整 `product.catalog.live_request` **3,558.92ms** 和 `product.catalog.projection` **2,712.35ms**。父子阶段为 inclusive，不能直接相加。
- 同一页面暖缓存再次请求为 HTTP 200，curl **1.691s**、应用 **1,639.66ms**；`live_request` 降到 **332.21ms**，说明长尾主要来自冷缓存构建/重复模板与目录投影，而非每次都固定执行单条慢 SQL。当前证据不足以安全增加索引；后续数据库动作必须补齐同一请求的慢 SQL 与 `EXPLAIN`。
- 已修复变体接口的确定性重复开销：`StorefrontVariantAvailabilityService` 改用 `StorefrontCatalogViewService::liveVariantAvailabilityForProduct()`，只读取发布商品、发布报价、价格、库存和店铺上架状态，调用统一 Cart 快照解析器时跳过媒体 URL 与选项元数据；保留请求 memo、价格/库存权威性、网站/店铺/渠道范围和语言货币维度。旧的完整 PDP 投影仍供详情页使用。
- 实测变体接口在 WLS 全量重启后的首个请求 HTTP 200、54 个报价、curl **0.844s**，服务端 `total_ms=443ms`；随后四次请求均 HTTP 200，服务端 **64–332ms**，不再触发 `product.catalog.projection`、媒体或选项阶段。
- 页头搜索类型已限定为 `area: 'frontend'`，避免前台冷请求发现后台 provider；其缓存仍由统一 channel + lang/currency + config policy 管理。翻译继续按 global + locale 共享，店铺/页面覆盖仍由请求层解析，不复制成渠道缓存。
- 本轮重启并清空诊断日志后重新实测：首个商品列表 HTTP 200、curl **9.34s**，应用 `total_ms=9,204.31`；首个 PDP HTTP 200、curl **5.62s**，应用 `total_ms=5,233.13`。随后同一作用域的列表降到 **0.09–0.14s**，变体接口降到约 **0.09s**，说明目前最明显的长尾仍是 Worker/模板/共享缓存的冷构建，而不是热路径固定慢查询。
- 地区列表的数据库瓶颈已确认并修复：原 query-bin 冷样本 **16.99s / 516 DB spans / DB 约 10.4s**，原因是 `RegionService::toRegionList()` 对每个 region 单独读取本地化名称；现在改为一次 `namesByRegionIds(IN (...))` 批量回填。改后真实默认列表仍返回 **352 行**，冷请求只有 **47 DB spans**，热请求约 **0.86–1.04s**，没有再出现逐地区本地化查询。
- 当前 `server:status --doctor --json` 为 `optimal`：4 个 Worker、Master 共享监听、Session/Memory sidecar 均健康，HTTP/2 已启用；唯一提示是 OPcache JIT 关闭。当前页面以 DB/WLS/模板 I/O 为主，JIT 只作为 CPU 占比升高后的验证项，不能直接打开替代业务优化。

### 已确认问题与后续方案

| 问题 | 证据 | 方案 |
|---|---|---|
| 首次 HTML 冷构建瀑布 | Header 10.8s、搜索类型 7.2s、cache builder 多次冷构建 | 发布/重载后按网站/店铺/渠道和语言货币预热 chrome、分类树、搜索 provider；对同 key 保留统一 single-flight，继续观察高并发等待 |
| PDP 完整目录投影过重 | 单次 `live_request` 3.56s，projection 2.71s，54 个 Offer 仍组装详情字段 | 建立可失效的商品详情读模型/共享 JSON（channel + lang + currency）；库存、可售和价格继续实时快照，不写入长期详情缓存 |
| 隐藏菜单与 Slot HTML 过大 | 响应约 **895KB**，`theme.slots.html_slot_ids` 约 **619KB** | Mega menu/首屏非关键 widgets 改按需或延迟输出；开启压缩/CDN 后复测 TTFB 与浏览器解析时间 |
| 数据库累计时间偏高但归因未闭合 | 冷样本 1,359 DB spans；暖样本约 275ms | 在低负载下按同一 SQL 指纹采集慢日志并运行 `EXPLAIN (ANALYZE, BUFFERS)`；确认重复读取后再改查询/索引 |
| 地区列表本地化 N+1 | 原 query-bin 16.99s、516 个 DB span；`w_shipping_regions` 与 `w_shipping_region_local` 按单个 region 重复查询 | 已改为按 region id 批量读取本地化名称；继续为 Shipping 注册 website + locale 的统一 CachePolicy，并在目录变更时按领域代次失效 |
| WLS 曾出现部分重载与缓存广播失败 | 旧 worker 返回新方法不存在；日志有 `shared Session/Memory sidecar recovery failed`，以及一次慢共享内存后 `cache_pool_clear_failed` | 重载前先做健康检查，禁止在 `setup:upgrade`/维护期间切流；缓存清理成功后重置慢熔断，真实传输失败仍负 ACK；修复 AgentScanner 接口错误后再做滚动重载。当前已恢复 4 个 worker 与 Session/Memory sidecar |

本轮没有启用第二实例：代码、timing.log 和真实 WLS 请求已能稳定复现并定位上述瓶颈。定向单测、PHP lint、diff check 与实时 HTTP 证据均已完成；高并发容量和浏览器主线程解析仍需后续受控样本。

## 范围声明

业务缓存继续使用 Framework 的 CacheManager、StorefrontScopeHotCache 和 namespace generation，不自行维护 Worker 静态数组、缓存键拼接或刷新锁。每项资源声明 CachePolicy，统一入口解释范围、变体、依赖和有效期。

| scope | 可共享边界 | 缓存键中的身份 |
|---|---|---|
| global | 所有网站、店铺、渠道 | 不含租户身份；按声明的语言等变体区分 |
| website | 同一网站的所有店铺、渠道 | website_code |
| store | 同一网站、店铺 | website_code、store_code、store_mode |
| channel | 指定网站、店铺、渠道 | website_code、store_code、store_mode、channel_code |

默认网站 `default` 和 website_id=0 仍属于网站范围，不是 global。非 global 资源在请求范围未冻结时保留 request fence，不能以缺省身份跨请求共享。原有 `website=true` 维度接口保持兼容，其含义仍是完整店面上下文；新增业务使用显式 scope。

`vary` 支持 lang（locale 别名）、currency、area；数据确实依赖这些维度才声明。公共 UI 翻译按 locale、模块和字典版本全局共享，不能把所有翻译按渠道复制。店铺或页面提供的覆盖译文按请求 EventDictionary 解析，不能写入公共 Worker 词条缓存。

## 统一入口

```php
$policy = new CachePolicy(
    resource: 'product.category_tree',
    pool: 'weline_product_storefront_category_tree',
    scope: 'website',
    dependencies: ['catalog'],
    freshTtlSeconds: 3600,
    staleTtlSeconds: 86400,
);
$value = $hotCache->rememberPolicy($policy, $logicalKey, $builder);
$hotCache->forgetPolicy($policy, $logicalKey);
```

传入策略时自动在 CacheManager 登记，之后可按 resource 名使用同一入口。`registerPolicy()`、`getPolicy()`、`getPolicies()` 提供统一注册和查询；业务资源的声明放在所属模块协调器中。Product 的声明统一位于 StorefrontCatalogCacheCoordinator。

策略签名、scope 投影、vary 和声明依赖的代次均参与键生成。fresh TTL 表示新鲜窗口，stale TTL 表示其后允许返回旧值的额外时间。仅通过显式策略的 getCustom/setCustom 路径访问底层池，避免池的 environment_scoped 再次扩大已声明的范围。

## 依赖与失效

`dependencies` 使用 catalog、config、price 等领域路径。沿用 NamespacePath 和 NamespaceGenerationRepository：网站依赖包括 global/storefront 下的对应领域和 website/{code} 下的领域；店铺、渠道在网站领域路径下细分。祖先代次自然影响后代，无需遍历删除。

分类原始索引与分类商品关系只依赖 catalog，不随无关价格、主题变更失效。含 URL 的菜单按渠道并保留语言、货币、区域变体，依赖 catalog/config；商品报价投影按渠道及语言货币，依赖 catalog/price/config。

`forgetPolicy` 删除当前策略和逻辑键的当前变体；影响全范围或多语言时由领域协调器 bump namespace，不能枚举少数语言代替范围失效。Product 写后继续通过 StorefrontCatalogCacheCoordinator 通知和失效。

## 热缓存行为

namespace 补齐可能在数据库读取时让出 Fiber。返回后必须核对最新进程代次：已被提交事件推进的快照不能由旧请求降级；同代次只能合并到最新成员集合，不能覆盖其他 Fiber 已补齐的 namespace。请求持有自己的快照，公共 L1 不按请求 ID 拆分。

URL 和范围解析完成后，`State::getLang/getCurrency` 直接复用已冻结的 `StorefrontCacheKeyContext`。解析前继续使用原有路径、查询参数及网站默认值回退；显式语言覆盖仍通过 `setRequestLanguageOverride` 管理，保留本地化结果重置。

进程 L1 统一限制条目数量，默认 1024 项，命中更新淘汰顺序。冷构建和 stale 刷新复用现有 SingleFlight；获取构建机会后再次读取共享池，复用其他 Worker 刚生成的数据。后台刷新保留入队时的缓存键，不能把旧代次构建写进新代次。

CacheManager 全部清理同时重置本 Worker 热缓存。完整 WLS 清理仍走现有 cache:clear 的 Worker 广播流程；单池底层 clear 与领域失效语义不可混淆。

## 已定位的业务重复开销

- 分类展示属性 name/image/banner/summary/description 一次批量读取，保持现有 locale 回退与 cleared 语义。
- 指定商品实时读取使用 ProductRepository::listByIds 的 IN 查询，保留排序及发布状态筛选。
- EventDictionary 覆盖先于公共词条缓存，独占空层不污染公共层；公共字典继续全局共享。
- EAV 公共元数据与共享选项通过 rememberForRequest 每请求复用；商品私有选项在 SQL 中按 scope_instance_id 筛选，只缓存标量行，重新克隆模型交给消费者。
- 后台目录变更按目标 website_id 的公开查询接口解析 code 后失效，修复跨站修改时清错当前网站。

## 2026-09-07 — 商品筛选只读取发布态报价

`OfferRepository::listByProductIds()` 继续保留全状态读取，供后台和需要审计草稿的调用方使用；storefront 目录增加 `listPublishedByProductIds()`，在同一个网站分片查询中加入 `status = published`，并由数据库按 `offer_id ASC` 返回。`StorefrontCatalogViewService::buildPublishedOffers()` 使用该专用入口，保留原来的代表报价、过滤、排序和输出语义，避免先把草稿、停用和归档报价全部读入 PHP 再丢弃。集成用例同时断言通用入口仍返回草稿、专用入口只返回发布态。

真实筛选请求使用 `Cache-Control: no-cache` 和 FPC bypass，HTTP 200，应用计时 **659.05ms**、进程 **708.61ms**、curl **778.72ms**；筛选面板含 **1** 个结果范围文本 `1–6 of 6`，正文仍含 8 个筛选值。该次目录共享缓存已命中，不能从它推算回源加载条数；此前同一筛选路径的 trace 明确记录过 **4,814 → 220** 的旧读取边界。当前请求 trace 的 DB 累计 **89.93ms**、WLS **21.27ms**，主要剩余阶段是 head **39.51ms**、regular widgets **94.01ms**、Slot region fill **97.09ms** 和约 **1.21MB** 的 opaque layout 处理。

定向 `CatalogRepositoryIntegrationTest`、`StorefrontCatalogReadEfficiencyTest`、`StorefrontCatalogFilteredOffersContractTest`、`StorefrontListingProjectionTest` 均通过；相关 PHP lint 和 `git diff --check` 通过。冷请求仍受 Worker 首轮模板/Slot 组装波动影响，不能把单次筛选样本写成所有页面固定低于 1 秒。

## 2026-09-07 — 预热 Host 身份与 EAV 空候选边界

- Deferred storefront warmup 仍在 Worker 进程内直接处理，但优先采用实例的公网 Host 作为 `Host` 头；只有实例元数据没有可用公网域名时才回退到回环地址。FPC key 包含 authority，使用 `127.0.0.1` 会预热出另一份 key，无法保证公网首个请求命中。此调整不经过外部网络，也不改变网站、店铺、渠道或语言维度。
- 滚动重载后的 `wls-storefront-warmup.log` 已记录两 Worker 使用 `p05113ef3.test.weline.com:9555`，第一 Worker 预热/共享命中后第二 Worker 进程命中；随后公网 `/en_US/products` HTTP 200、FPC `HIT/process`，应用 **28.43ms**、curl **78.54ms**，正文 **1,664,620 bytes**。此前回环 Host 预热后公网请求曾出现新的 FPC MISS，说明该 key 对齐是必要的。
- `AttributeFilterService::getFilterableAttributes()` 在空实体候选时继续把 `includeOptions=false` 传给 metadata 路径，避免无结果筛选仍批量读取 options；有实体候选时的批量值/计数和公共翻译范围不变。
- 定向回归：`WlsRuntimeStorefrontWarmupContractTest` **8/8、34 assertions**，`AttributeFilterServiceTest` **10/10、40 assertions**，`PartialsChromeCachePolicyTest` **12/12、40 assertions**，`CategoryFiltersWidgetContractTest` **12/12、55 assertions**；相关 PHP lint、`git diff --check` 和滚动重载通过。

当前冷页面的剩余主耗时仍是 category/regular Slot、过滤面板、隐藏 mega menu/opaque HTML 和 FPC build-lock 并发等待；本轮没有足够受控 `EXPLAIN` 证据支持修改表结构或索引。

## 后续优化清单

以下项目按真实 profile 和收益验证后继续，不能将建议视为已完成：

| 优化项 | 验证重点 |
|---|---|
| 商品目录冷构建进一步拆分 | 只读DTO与scope/code查询请求复用已落地；81商品投影约0.21–0.22秒，继续定位模板与完整候选分页 |
| 模板后处理与页头搜索 | 新请求布局仅 188 ms，fetch 后处理约 1.36 秒；旧请求搜索慢项需更多稳定样本核对 |
| 后端真正批量 get/set | 现有池 batch 接口是否只是循环，以及各适配器支持情况 |
| 缓存统计与预热声明接入 | 在现有统计、CacheWarmerRegistry 上补 L1/L2 与资源维度，避免范围语言笛卡尔积 |
| 目录列表的分页和投影 | 不改变过滤/排序语义，避免仅展示一页却投影全部商品 |
| 模板编译与静态资源 | 编译缓存按源码依赖复用；CSS/JS 压缩、按需模块加载和页面 DOM 体积 |
| 公共翻译各入口归并 | 复用既有全局词典 API；语言变更事件与缓存清理保持一致 |
| Worker 与共享缓存调优 | 依据真实等待、CPU、I/O 和命中率调参，不盲目增加 Worker 数 |

## 验收

开发证据使用范围共享/隔离、global 与网站领域代次、并发再读、SWR 旧代次写回、真实 SQLite 查询次数及翻译覆盖的定向用例。运行证据使用配置中的 WLS 服务和实际页面响应；HTTP 成功或单测不替代浏览器操作。实际耗时与未完成的运行事项记录在本轮开发日志及交付报告中。

## 2026-09-05 实施验证记录

- 已修改 Framework、Product、Eav 共 23 个文件，保留原工作区修改；PHP lint 和定向 diff check 通过。
- 实际仓库源代码：新增聚焦用例 30 tests / 117 assertions；核心与既有兼容回归 34 tests / 140 assertions；其他相关回归 14 tests / 149 assertions，全部通过。三组包含交叉用例，不相加为独立总数。
- 配置服务：default 两个 Worker 平滑重载完成，耗时 8.1 秒。
- 实际访问：分类首次 200，TTFB 10.214 秒；随后 FPC 命中 105 ms；首页命中 111 ms；英文商品列表命中 104/63 ms。标题、locale 和正文内容正确，未出现 fatal/parse error。
- 实际 SQL：EAV 共享选项包含 scope_instance_id IN(0)，示例 12.09 ms；私有选项示例 1.33 ms。分类新请求 trace 中记录的 DB 累计约 453 ms，旧超时请求记录约 11,960 ms；trace 有截断，不能据此宣称完整加速比例。
- 未完成运行验证：后续 nocache 动态请求与复查返回站点维护页 503；保留维护状态。Chrome 已按 Weline profile 验证连接，真实分类页操作返回工具超时，Browser 操作验收未完成。不能把上述 HTTP 成功或单测当作 Browser 通过。
- 剩余冷路径：模板前约 8.06 秒尚需分段定位；HTML 约 1.48 MB，模板后处理约 1.36 秒；一个 product 共享缓存写入约 321 ms，phrase 读取示例 210/99 ms。先恢复稳定动态访问再继续测量和优化。

## 2026-09-05 — 共享属性选项批量读取

- 在此前属性值统计批量化的基础上，`AttributeFilterService` 通过同一个选项读取方法加载当前缺失的属性桶；metadata 入口批量预取，有值入口仅预取实际 values 非空的属性。保留既有实例 `optionCache` 及 `clearCache()` 语义，不增加独立缓存、持久缓存或新的范围规则。
- SQL 保持共享 `scope_instance_id=0`、原 `option_id DESC` 顺序及全部选项字段，按 attribute_id 分桶；空桶也可复用。单属性入口走同一个读取方法。查询和结果转换完成后再写回，不缓存失败查询的半成品。
- 固定 112 件真实商品，完整面板 SHA 与前轮相同；共享选项 SQL 96 → 1（统计限定 scope 条件和排序的该查询形状，其他标签查询不混入），面板 DB spans 437 → 342。metadata 样本 115.24 → 56.92ms；面板整体 1199.90 → 2815.13ms，说明其他加载及环境变化仍影响总时间，不能用本项查询减少声称整页稳定加速。
- 最小 SQLite 用例先验证字段、scope0/private 排除、每属性降序和空桶，再以原实现3次而期望1次查询复现RED；候选通过同实例复用和 clearCache 后刷新。既有值统计和复合 ID 回归保持。
- 本轮目录冷请求基线 HTTP 200 / TTFB 3.133s，与前轮9.991s相比 Worker/其他缓存状态和环境均有变化；所有样本、应用后的定向测试和真实页面结果见 `/private/tmp/weline-perf-options/acceptance-report.md`。
- Browser恢复探测仍受CUA内容读取约28.26秒超时阻塞；只读确认Weline Chrome身份及商品页标题，没有执行排序分页点击，不将HTTP称作Browser通过。统一缓存RPC目前已定位到实际注册的 `Weline\Server\Cache\Adapter\WlsMemoryAdapter`，后续测量须沿该入口，不能混用旧Framework/Rpc服务。

交付地址：[英文商品](https://p05113ef3.weline.test:9555/en_US/products)、[明制筛选排序](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_chao_dai=Ming+Style&sort=price_asc)、[Women分页](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_shi_yong_xing_bie=Women&sort=price_asc&page=2)。

## 2026-09-05 — 筛选属性值按实际表批量统计

- 实测定位：Filters 面板通过 `AttributeFilterService::buildAttributeDataWithValues` 对 138 个属性分别统计；这批值实际只分布在两张 EAV 表中。问题位于读取逻辑，本轮未修改数据库结构或新增缓存。
- 实现：按值模型类、完整物理表名和当前 ConfigProvider 的连接身份分组，每组执行一次 `GROUP BY attribute_id, value`；保留实体 ID 条件、`COUNT(DISTINCT entity_id)`、NULL/空串过滤和字符串 `0`。结果按原属性输入索引回填，同编号跨表或跨连接保持隔离。原单属性接口、共享选项范围、事件及网站/店铺/渠道和翻译策略保持。
- 固定 112 件真实商品输入的 CLI 对照：完整面板内容及 SHA256 一致；值统计 SQL 138 → 2，面板全部 SQL 259 → 123。该次 EAV 阶段 801.44 → 318.87ms，面板 1272.55 → 775.24ms；为实际样本，不是并发压测或延迟承诺。证据：`/private/tmp/weline-perf-next/fixed-runtime-comparison.json`。
- 定向回归以 SQLite 执行生产服务构造的 SQL：旧版内容断言通过但查询次数 4 ≠ 2（RED），批量后同表与跨表/连接案例通过；覆盖重复实体、范围外实体、无值属性、NULL/空串/0。同步修正既有测试对 `getAttributeId` API 的替身，未为旧 mock 改生产 ID 语义。
- 本轮 HTTP 冷基线为 200 / TTFB 7.991s；应用后的冷请求、排序分页与组合筛选结果统一记录于 `/private/tmp/weline-perf-next/acceptance-report.md`。Browser 的 Weline Chrome DOM 和唯一截图替代分别超时，未完成点击验收，HTTP 不替代该结果。
- 后续：96 次共享选项读取仍可研究批量化；缓存 RPC 样本和页面组装仍有耗时。未从截断 trace 推断同 key 重复缓存，也未将本次局部优化称作整页冷请求已解决。

交付地址：[英文商品](https://p05113ef3.weline.test:9555/en_US/products)、[明制筛选排序](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_chao_dai=Ming+Style&sort=price_asc)、[Women 分页](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_shi_yong_xing_bie=Women&sort=price_asc&page=2)。

## 2026-09-05 — URL 重写兼容候选合并查询

- 固定 112 件商品的面板样本中，URL 重写共有 312 次 SQL：104 次指纹查询、104 次 NULL 旧行查询、104 次空字符串旧行查询。优化目标是同样 104 次路径解析只执行 104 次 SQL。
- `Weline_UrlManager 1.0.4` 将三类候选放进一次绑定查询，两条 OR 分支均按网站隔离，保留原候选优先级和 PHP 路径字节严格比较。既有 `(website_id, path_fingerprint, rewrite_id)` 索引继续可用。
- 此处直接减少查询往返，没有增加请求级或持久化缓存，因此无需为模型保存、直接更新、删除和事务另建失效协议。其他业务缓存仍走统一范围策略：网站/店铺/渠道按数据可用范围处理，翻译全局按语言区分。
- 4 个最小回归用例以真实 PostgreSQL 编译器生成 SQL，在 SQLite 内存库验证查询数量、网站隔离、迁移兼容顺序和写后可见性。固定面板完整输出及真实 HTTP 验收结果统一记录于 `/private/tmp/weline-perf-rewrite/acceptance-report.md`；临时目录模型因框架按源码路径识别数据库配置而产生的无效样本必须剔除。
- 国旗重复内联评估：样本 44 个 SVG 共约 414KB，其中西班牙旗重复 4 次约 324KB。尚未确认供应商国旗的现成静态发布映射，本轮不修改语言切换器或另建私有资源缓存；列为后续静态资源发布优化。

## 2026-09-06 — 主题布局阶段观测

- Theme ControllerFetchFileBefore::execute 复用同一个 measurePhase/log 出口，细分 context、performance_load、virtual、compiled_path、definitions、params 六阶段。用于解释目录命中后剩余渲染开销，保持业务和缓存行为不变。
- 对照必须按响应request_id匹配pid/request_count：显式wls_trace参数移除后本地DEV配置仍开启trace，耗时降低不能直接归因关闭调试；不同Worker首次请求、并发商品导入及目录失效都需标注。

## 2026-09-06 — Darwin 出生指纹读取避免无用命令行探测

- 实际只读 PID 探针确认：稳定 libproc 出生证据约0.001–0.026ms，完整进程观察/ps执行入口约39–155ms。该独立 CLI 测量指出具体重复工作，不等同整个HTTP请求改善，也不能把每2秒的长尾仅凭周期直接归因。
- `MasterLeaseRuntimeIdentity` 对不使用可变 name/argv 的出生指纹读取，沿用 Windows 已有的按需元数据模式：Darwin 原有两次一致的 libproc birth 校验成功后直接返回 kernel start_ticks/start_time。只需判断缺失的正PID谓词复用该路径；非正PID、注入resolver和其他平台保持原入口。
- 默认 `inspectProcess()` 与管理进程名称校验仍获取完整ps元数据；保留原原生读取重试和失败回退、出生hash、自进程缓存、PID/namespace/epoch/token/lease及父子凭据验证。不添加新缓存，不把未知证据当作匹配；仅避免读取随后不会使用的命令行。
- 前置真实Darwin/独立PHP进程用例核对同一存活外部PID的完整观察与birth-only输出hash一致、正确/错误birth分别MATCH/MISMATCH，并用命令执行器未加载证明birth-only未进入ps；默认完整观察作为阳性对照。正式回归和冷HTTP证据记录于 `/private/tmp/weline-goal/acceptance-report.md`；完整冷请求<1秒目标与Browser验收继续保留。

## 2026-09-06 — 统一阶段 timing 调试

- 统一入口：`RequestLifecycleTrace::measurePhase($name, $callback, $meta)`。开启现有 DEV/DEBUG、`wls.debug.request_trace` 或 `X-Weline-Trace: 1` / `wls_trace=1` 时计时；关闭时直接执行回调。用稳定的业务名称，不把商品 ID、原始请求参数或秘密放入标签。返回值和异常原样保留，异常路径仍在 finally 记录耗时。
- `trace_summary.phases` 中同名 measurePhase 累计 `calls`、`duration_ms`、`max_ms`、`errors`，并提供阶段内 DB/WLS span 次数及耗时增量；每条明细只存本次时长，不把累计值反复写成 span。阶段名仍最多 32 个，已有名称可继续更新，meta 复用原有长度限制及认证 SQL 脱敏。
- `trace_summary.wls_span_count/wls_duration_ms` 与 DB 标量一样，在明细达到 cap 后继续累计；不能再把缺失明细当作没有缓存调用。DB/WLS 都是已埋点客户端调用时间，可能包括执行、传输、调度和 Fiber 挂起，不等于服务器 CPU 或单独锁等待，也不代表缓存命中数。
- `measurement=inclusive` 明确父阶段包含子阶段。不要相加父子耗时，不把 `phase时间 - DB - WLS` 当作精确 CPU 时间；既有按 parent 名称聚合的明细不能用于同名实例的 self-time。原 `recordPhase` 保持单值更新语义，与 measurePhase 标签分开命名。
- 商品目录：`product.catalog.resolve` 覆盖统一缓存解析；`product.catalog.build` 仅在缓存回源函数实际执行时记录。同请求 resolve/build 的调用次数可以说明是否发生回源；没有 build 不能进一步推断是 L1、L2 还是 stale 命中。既有 products/offers/snapshots/attributes/projection 子阶段继续保留。
- 筛选：`storefront.filters.panel` 总边界，以及 departments、metadata、eav_counts、offer_counts、options 子边界。保留原筛选结果、URL、容错与缓存范围。渲染继续查同请求的 `template_profile` 和 `router_profile`，不新建日志出口或私有缓存。
- 查看现有 `var/log/wls/timing.log`，先按 `request_id`（响应头 `X-Weline-Request-Id`）、URI、PID、request_count 关联，再看 `trace_summary.phases` 与 truncated/dropped_span_count。`url_parser_perf` 是累计里程碑，需相邻差分；模板父子区间也不能相加。
- 共享缓存与会话的统一通信入口 `SharedStateClient::request` 在 Trace 开启时记录单个 `wls.rpc.request`（category=`rpc`）。meta 包含 acquire_ms/encode_ms/send_ms/read_ms/dispose_ms、已有编码字符串长度 encoded_bytes、command/namespace 和 key_hash，不记录原始 key、value、sid，不额外编码或序列化。未执行阶段为 null，send_called/send_succeeded/read_called/response_received 只说明实际调用结果；未采集连接池内部重试数，不能把一次 RPC 当作无连接重试。 RPC/read 另保留 rpc_start_monotonic_us、rpc_end_monotonic_us、read_start_monotonic_us、read_end_monotonic_us，均为同一 hrtime 纳秒读数经 intdiv(..., 1000) 得到的整数微秒；未调用 read 时其两个坐标均为 null，read 抛异常时仍记录实际边界。复用原时钟读取，关闭 Trace 不增加时钟调用或时间转换，不改变 compact/full span 格式。四个坐标供本机同次系统启动内的并行区间对照使用；本轮两个 PHP 进程的8次管道握手已验证子进程读数落在父进程握手边界内。它们不是 Unix 时间，不能用 timing 中秒级完成 timestamp 倒推，也不保证跨主机或系统重启可比。是否重叠应以实际区间交集确认，重叠本身不能证明慢请求因果。
- `ServiceOrchestrator::resolveServiceEndpoint` 在本地 Registry 无可用 Session/Memory 实例时，复用 `SharedStateRuntimeOptions` 解析共享服务端点，保留已配置远端 host/port 与项目默认端口偏移；本地 READY 实例和已分配端口实例仍优先。共享 sidecar 不在本地 Registry 时不再向 Worker 广播未偏移的19970/19971。该改动由常驻 Orchestrator 发布，验收需让正式实例生命周期加载新 Orchestrator，再核对同请求 RPC 的实际端点；仅重载 Worker 不能证明生效。
- RPC 的总 span 包含原有连接回收与 metrics 工作，各阶段按单调时钟记录；read 包含服务端处理、传输、客户端解码以及潜在 Fiber 恢复等待，不等于纯网络耗时。独立 rpc 类别避免把缓存/会话父 span 的时间再次加入 wls 标量。参数、协议、池超时/重试、acquire 异常传播、callback 异常返回 null 和 release/invalidate 行为保持。该细分用于定位真实观测到的319–487ms缓存写入与338ms会话写入，结果见 `/private/tmp/weline-goal/acceptance-report.md`。
- 连接池/连接继续复用 recordAcquireMetric、recordPhaseMetric、recordAuthMetric 三个集中出口：Trace 开启时，仅失败或实际至少10ms的成功调用记录 wls.pool.acquire / wls.connection.<phase> / wls.connection.auth，类别 rpc。记录实际 host/port、timeout_sec/connect_timeout_sec 和原有 result；获取阶段包含本次等待循环 retry_count（不是RPC重试或TCP建连次数）、返回时池 busy/idle/total/max_size/profile/cooldown 及明确为历史状态的 last_connect_failure_reason；认证保留原 reason，不记录 token 或凭据路径。返回时池快照不能代表整个等待区间，原 token_mismatch 也可能涵盖未收到认证回复，须结合请求阶段判断。
- 认证诊断继续复用 `wls.connection.auth` 与 `.last_failure`，新增 `attempts`、`failure_stage`、`last_attempt_ms`。attempts 是实际 AUTH 尝试方法的调用数，不等同 TCP 建连数；last_attempt_ms 仅为最后一次该尝试的耗时，不含刷新间隔或重新连接。failure_stage 的 read 表示未获得完整数组回复（可能是等待、EOF、协议或读取失败），response 表示已收到非成功数组回复；deadline/write/reconnect/token_load 分别标记预算已尽、发送失败、重新连接失败和初次凭据不可用。成功标记 success。旧 reason/counter 保留兼容，token_mismatch 仍不能单独证明密钥不匹配。仅输出固定标签和数值，不记录凭据、路径或回复内容；原认证预算、重试、连接及返回行为保持。
- 为保留明细 cap 之后的快速失败，复用既有 recordPhase 保存 wls.pool.acquire.last_failure、wls.connection.io.last_failure、wls.connection.auth.last_failure 三个固定最后失败快照，measurement=last_failure。使用实际 duration，不提高到10ms；快照不是累计调用数/耗时，仍受既有32阶段上限及请求/Fiber生命周期约束。失败同时有rpc与phase观察，不能相加；不改WLS标量、池容量、超时、重试、协议或业务缓存。
- 最小验证覆盖真实无响应socket读取超时并关闭、满池获取超时保留原busy租约、缺少凭据的连接失败在明细上限后仍保留脱敏原因。此前两次实际HTTP已定位匿名主题cfg获取约200ms而未发送、session read约50ms未收到数组回复，具体原因需用本轮实际运行日志确认；候选验证不冒充正式应用或冷请求达标。
- 本次加标签前，真实商品请求在客户端出现 HTTP/2 framing error / 45秒超时；服务端同URI日志分别 27.96s / 78.39s。后者 action 76.84s、DB累计22.06s、明细丢22765条，不能称HTTP验收成功。另一次只读PG观测到 transactionid 锁等待41–62秒，但捕获阻塞关系时已消失，未能归属到该HTTP请求；10核系统load约38–42，不能直接等同CPU百分比或断言某进程是根因。
- 最小回归与真实采样最终结果见 `/private/tmp/weline-timing/acceptance-report.md`；范围是观测与归因，不承诺本轮降低整页延迟。本轮不修改Model/Controller/模块版本或UI。
- I/O 调度里程碑继续合入同一 `runtime.io.await_socket`：Trace 开启的 await 创建仅含标量的可选 `io_timing` 载体，由既有 Observer/waiter 传递；Server 只填时间，原请求 Fiber 恢复后统一记录。`io_registered_monotonic_us`、`io_deadline_monotonic_us` 来自实际注册/截止时间；`io_first_poll_start_monotonic_us`、`io_first_poll_end_monotonic_us` 只标记本次真实底层 poll 输入集合中的载体，返回 false/抛出亦保留结束边界，不标记 poll 内新注册的 waiter。`io_resolved_monotonic_us` 与 `io_resolution` 记录首次原逻辑的 ready/timeout/invalid_stream；`io_before_resume_start_monotonic_us`、`io_before_resume_end_monotonic_us` 包围原上下文恢复回调，`io_resume_call_monotonic_us` 位于原 Fiber::resume 调用之前。未发生的节点为 null，不新增 phase、缓存或调度动作。
- 同一 await 的注册至首 poll、首 poll 结束至 resolved、resolved 至 beforeResume，以及恢复回调分别分析；后续多次 poll 混在中间区间，不能称纯网络时间。Fiber::resume 返回包含恢复后的业务执行并可能晚于本 await span，不计入这次等待。已 ready 的 waiter 晚于 deadline 恢复仍保留原结果；本观测不改变截止时间判定、原三参数 API、布尔/异常或 Worker 循环顺序。未加载/关闭 Trace 不创建载体，主事件循环不直接调用请求 Trace，避免记录到其他 Fiber。
- 首次 poll 前另有 `io_yield_return_monotonic_us`、`io_after_resume_start_monotonic_us`、`io_after_resume_end_monotonic_us` 和 `io_collect_seen_monotonic_us`：Scheduler 在 resume 返回后，从现有 waiter 集合找到目标 Fiber 新登记的 Trace 载体，记录观察到的挂起返回与原 afterResume 回调边界；不得写入上一次 await 的载体。回调内同一载体重新注册时，不回填旧区间结束时间。直接 Fiber::start 不经过此接缝，前三项为 null，不能当作零耗时。collect_seen 是既有收集循环首次看到该有效未决 waiter 的时刻，非 wait 方法入口；仅实际载体读取时钟，未开启 Trace 不创建载体、不自动加载 Trace。由此可分开上下文保存回调、回调后至收集、收集至底层 poll；中间仍可能包含其他 Fiber、Worker 循环或系统调度，不能凭 wall time 判定具体函数。
- `io_guard_start_monotonic_us` / `io_guard_end_monotonic_us` 包围 Worker 主循环原有 `ChildMasterGuard::shouldExit()` 调用。只快照调用前已登记、尚未首次收集/poll 的未决 I/O 载体；在 finally 回填同一次登记的第一组起止，异常仍原样抛出，期间新增或重新登记的等待不补标。无符合载体时直接执行原检查，不读取计时时钟、不加载 Trace。没有经过此接缝的字段为 null；这个区间也可能只执行检查间隔的快速返回，不能据此断言发生了 ps 探测。应与同一 await 的 afterResume_end、collect_seen 比较重叠后再归因，不改变主进程授权、退出、重试或调度顺序。

交付地址：[商品请求](https://p05113ef3.test.weline.com:9555/en_US/products?nocache=1&wls_trace=1)。新增UI/API：N/A。

## 2026-09-06 PostgreSQL 流式读取统一计时

- PostgreSQL `Query::fetchIterator()` 的 SELECT 路径也进入既有 `RequestLifecycleTrace` 数据库计时：已开始的迭代在耗尽、抛错或生成器释放时仅记录一个 `db` span；未开始的生成器不记录，非 SELECT 委托沿用原 `fetchArray()` 计时，不重复记录。`fetch_mode=iterator`、`measurement=driver_active_excluding_yield` 表示准备、执行、读取、模型构造和清理所占的驱动活跃墙钟时间，使用单调时钟并扣除每次 `yield` 暂停的消费者时间；它不是数据库服务器纯执行时间，也不能与外层 inclusive phase 相加。SQL 元数据复用统一脱敏，并保留绑定占位符，不展开绑定值。
- `db_span_count` 始终表示统一计时记录数，包含失败尝试，不应泛化为物理 SQL 执行次数。此前 EAV 预取用 `fetchIterator()` 的批量 SQL 未进入该标量，因此旧真实样本的 1,777→1,345 应表述为少 432 个 DB span；固定 SQLite 夹具的查询次数对比是独立证据。补齐后应以新的同请求计时验收批量 SQL 的可见性，不能直接比较不同计时覆盖口径下的 SQL 总数或 DB 时间。

## 2026-09-06 — 促销选品按需读取基础投影

- `ProductSelectionReadInterface::searchSelectionRows` 是 Product Reader 的可选能力，仅返回 `product_id`、`updated_at`，与原完整 `search` 共用筛选、200商品分批及最终更新时间/ID排序。身份、SKU回退、名称、类型、状态、归属网站和必要店铺选择检查保留；选品不再加载后续不用的价格、媒体主图和展示店铺数据。
- Promotion 的 `searchIds` 优先使用可选能力，原 Reader 接口和旧实现不变；不支持时仍走原 `search`。活动范围合并、日期筛选、limit、异常/空集处理保持；需要图片的后台选品面板继续使用完整列表。
- 此优化集中在既有查询入口，不新增缓存、RequestContext数据或权限/签名URL复用。两个前置真实Service/Repository用例先通过结果与范围断言，再因多余详情读取RED；正式回归及216商品实际数据库/冷HTTP对照记录于 `/private/tmp/weline-goal/acceptance-report.md`。冷请求<1秒和Browser验收目标仍保留。

## 2026-09-06 语言控件与商品快照选项计时

继续使用 `RequestLifecycleTrace::measurePhase` 与现有 `timing.log`，不另建日志或缓存。新增 `i18n.language_switcher.render`（整个控件，含缓存命中）、`i18n.language_switcher.catalog`（作用范围内语言集合取得）、`i18n.language_switcher.flag`（XML 声明处理与内联 SVG ID 隔离）和 `product.catalog.snapshot.options`（目录每个有效 Offer 的原 buildOptions 调用）。不记录旗帜正文、语言数据、商品属性或选项值；原返回、异常、缓存与作用范围保持。
- 短语解析继续复用同一出口，新增 `i18n.phrase.layered_words`（请求层组合）、`i18n.phrase.resolve`（实际词条解析）、`i18n.phrase.module_cache_get/set`（全局模块词典共享缓存读写）和 `i18n.phrase.global_word`（全局词典单词回退）。这些标签只记录固定阶段、调用次数及 DB/WLS 增量，不记录原始词、缓存 key 或翻译正文；翻译层次、全局语言范围、失效与异常回退保持不变。

从同一 `request_id` 的 `trace_summary.phases` 查看 duration_ms、calls、max_ms、errors 及已埋点 DB/WLS 指标。render 已包含 catalog 和 flag，snapshot.options 已包含在 catalog.snapshots 中；不得重复相加。重复 options 调用次数由真实请求给出，不能预设等于商品数。内联 SVG 体积只作为候选证据，不能代替这几段计时或浏览器解析耗时。

目录快照进一步细分为 `product.catalog.snapshot.attributes/prices/media_rows/media_url/availability/deals`。这些标签使用同一个 measurePhase，只覆盖原调用，便于核对逐商品工作和批量读取；所有汇总按同一请求解释，已包含在 snapshots 父阶段，不记录属性/价格/媒体/促销正文。现有四类语言/选项标签保持，阶段总数继续受框架既有上限约束。

完整后台列表的 `ProductAdminReadService::search` 在原200商品批次内先过滤，再批量读取命中的价格/媒体并按 Offer/Product 分组；活动店铺每次 search 首次命中只取一次。无命中不增加详情读取；全部 DTO 字段、价格顺序、主媒体、Store 选品和最终排序保持。Price/Media 的既有批量 API 改用 fetchIterator 后保留原转换/排序及媒体旧分表回退，完整大批次不会触发 fetchArray 的10000行上限，不修改该保护或新增缓存。改动前后性能须以同数据 DTO 对照和真实 WLS 请求分别验证，不能将 CLI 仓储耗时当作整页收益。

## 2026-09-06 — Worker socket 等待边界计时

`SchedulerSystem::awaitSocket` 在已有 RequestLifecycleTrace 类已加载且启用时，用同一 `runtime.io.await_socket` span 记录至少10ms的等待或异常。类别为 `runtime`，不再次累加 DB/WLS，不新增 phase 汇总项；未加载或关闭 Trace 直接走原等待逻辑，不为 socket 等待自动加载 Trace。

该 span 的 prepare、dispatch、suspend、select 字段包含各自耗时和单调开始/结束坐标，未走到的阶段为 null。prepare 包含原输出缓冲 flush；dispatch 包含原 dispatcher/事件调用；suspend 表示调用 Fiber suspend 至恢复返回的墙钟区间，含等待与调度恢复，不能称为纯网络时间；select 只用于原同步/prepare 未获准时的回退。记录在原调用返回或抛出后执行，保留 bool、原异常对象和既有 Fiber 关闭行为，不修改 timeout、输出 flush、等待注册或协议。

通过同一请求中的客户端 RPC/read 单调区间定位嵌套等待；坐标仅在同主机同次启动内比较，父子 span 不可重复相加。只记录方向、路径、超时、结果、异常类与固定阶段坐标，不记录 socket、载荷或异常消息。明细仍受 Trace 的原有容量与慢样本保留规则约束，缺少 top 样本不能证明该等待未发生。正式收益与归因以实际 Worker reload 后的同请求日志为准，临时 Fiber/socket pair 用例不替代真实请求验收。

模板插槽同时记录 `theme.slots.cooperative_yield` 与 `theme.slots.boundary_scan` 的统一 measurePhase，分别只包围原有 SchedulerSystem::yield 与每轮 enumerateRegions；不通过会另行 yield 的 traceCall 添加观测，也不改变原调用次数。前者反映主动让出至恢复的累计墙钟时间，后者反映实际边界扫描次数与累计耗时，均已包含于其父阶段；二者仍受统一 phase 数量上限管理。


### 2026-09-06 — 选品与主题渲染统一阶段归属

复用 `RequestLifecycleTrace::measurePhase`，新增 `promotion.selection.reader`（原 productReader 获取/构造）与 `promotion.selection.search`（实际 selection/兼容 search 调用），以及 `theme.slots.widgets.regular`、`theme.slots.widgets.cow` 和 `theme.partials.fetch.header/head/footer/other` 固定分类。只在既有调用执行时记录 calls/max/errors 与 DB/WLS 增量，保持原异常、返回值、yield、范围和缓存策略。readerFilters/店铺解析及后续活动过滤不在上述两个选品子阶段内，不能把父阶段残差直接归因到 DI 或 SQL；所有父子时长均为包含关系，不可相加。

当前真实日志已占用原有 32 个聚合名称，统一上限调整为 48，容纳新增最多 8 类并保留原尾部阶段；明细上限、慢样本容量、请求/Fiber 隔离和认证 SQL 脱敏不变。未执行的 COW/partial 类型无需人为产生标签，禁止动态业务 ID 进入 phase 名称。统一 Trace 在 DEV/WLS 开发模式可自动启用，不能仅凭 URL 没有 wls_trace 就认定诊断关闭。

实施前基线：普通商品 URL 在目录失效后重建 HTTP200、2.454703秒（PID38548/request4，非已证实 Worker 首请求），deals 为445个DB span/190.66ms；同路径后续明确 FPC:HIT 为78.852ms，无应用 request_id/phase，不伪造应用计时。真实浏览器 about:blank 可读取，商品标签的 Console/Runtime/AX 观察超时；目前不能区分调试通道和页面主线程。完整冷请求<1秒及筛选/分页验收仍未完成。应用后的同请求阶段归属与回归结果统一记录在 `/private/tmp/weline-goal/timing-next-runtime-report.md`；日志仪表完善不等于已完成性能目标。

## 2026-09-07 — 冷请求并发锁等待收敛与插槽子阶段计时

- `StorefrontScopeHotCache::rememberPolicy()` 的 storefront single-flight 抢锁改为非阻塞尝试（timeout=0）。缓存 miss 仍先做共享缓存二次读取；抢不到锁时只构建一次当前请求，不再为远端锁排队 1.5 秒。原逻辑在等待结束后无论是否拿到 token 都会继续构建，多个目录、过滤面板和主题片段叠加后形成 5–10 秒瀑布；这属于缓存协调逻辑问题，不是数据库表结构问题。
- 统一 cache timing 现在包含 `storefront.cache.shared_read`、`storefront.cache.singleflight_acquire`、`storefront.cache.shared_recheck`、`storefront.cache.builder`、`storefront.cache.shared_write`；Slot widget 进一步拆分为 `theme.slots.widget.component_render` 与 `theme.slots.widget.wrapper`。标签只记录模块、部件代码和类型，不记录商品 ID、原始 query 或秘密。
- 模板 Hook 的共享输出写入先进入 Worker 进程缓存，再以 `PostResponseTaskQueue` 的 2 秒 idle grace 延后；队列有活动请求时不执行，避免一次 WLS Memory `cache_set`（实测约 175ms）阻塞下一条首屏请求。CLI/FPM 及 post-response drain 内部仍保留同步写入，缓存失效和跨 Worker 最终一致性不变。
- 持久 Worker 的全局翻译 single-flight 同样改为非阻塞；CLI/非持久模式继续保留 40ms 协调等待。`getUsedWordsWithTranslations()` 在一个请求内固定最终语言层并复用已有翻译 memo，避免渲染结束后再次为每个词重组模块层。翻译仍是 global 范围，词典发布和请求覆盖优先级不变。
- Slot 边界处理继续使用一次扫描；同一深度的互不重叠区域通过 `SlotBoundaryScanner::replaceWrapperInners()` 一次性按源码顺序替换，保留重叠/非法范围的原 HTML 回退。
- 真实 WLS 冷启动验收：完整停止 Master/Worker 后重新启动，两个新 Worker 的商品列表两路并发均 HTTP 200，最新 benchmark 延迟 **416–732ms**；分类页两路并发均 HTTP 200，最新 benchmark 延迟 **413–464ms**。冷 trace 的 `storefront.cache.singleflight_acquire` 约 **0.1–0.5ms**，`storefront.cache.shared_write` 约 **0.3–0.6ms**，`theme.slots.widget.component_render` 约 **47–136ms**，`product.card.render` 约 **75–98ms**，`i18n.phrase.global_word` 约 **0–26ms**；模板 Hook 的共享 `cache_set` 未进入请求 timing。没有再出现原先约 1.5 秒/次的锁等待。该结果是受控两路样本，不替代高并发容量基准。
- 定向回归：`PostResponseTaskQueueTest` **2/13**、`SlotBoundaryEngineTest` **8/22**、`SlotHtmlBoundaryBehaviorTest` **19/147**、`SlotRendererContainerChildSlotFilterTest` **3/16**、`StorefrontScopeHotCacheTest` **2/7**、`CachePolicyTest` **16/53**、`WlsMemoryAdapterTest` **4/12**、`WlsRuntimeStorefrontWarmupContractTest` **9/37**、`ParserRequestModuleCacheTest` **3/16** 均通过；新增非阻塞锁合同断言 timeout=0 与 post-response idle grace 合同。PHP lint 已通过，服务已滚动重载。
- 浏览器点击验收仍受当前 CUA 锁定影响；本轮以真实 Worker/HTTP/Timing 日志完成运行验收，未把浏览器未执行误报为通过。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。

## 2026-09-07 — 分类 targeted 投影缓存与导航 timing 细分

- \`StorefrontCatalogViewService::publishedOffersForProductIds()\` 现在通过 \`StorefrontCatalogCacheCoordinator::catalogTargetedOffersPolicy()\` 复用显式商品 ID 的列表投影。策略范围为 \`channel\`，变体为 \`lang/currency\`，依赖 \`catalog/price/config\`；商品 ID 排序后生成稳定 key，\`full\` 与 \`summary\` 分开。目录代次提升会使旧 key 自动失效，不在调用方散落清理逻辑。
- Header 导航新增 \`theme.header.mega_panel.cache\`、\`theme.header.mega_panel.render\`、\`theme.header.sidebar.cache\`、\`theme.header.sidebar.render\` 阶段；Header 输出指纹同时包含账户、横向导航、侧栏导航和 Mega 模板，嵌套模板变更不会继续复用旧 HTML。
- 移动分类抽屉不再在首屏重复 SSR Mega panel；抽屉打开时从桌面 panel 深克隆并重映射 ID/ARIA，保留桌面悬停、移动抽屉和键盘 tab 交互。没有桌面源 panel 时仍保留分类链接回退。
- 受控真实 HTTP：重载并等待两个 Worker 的商品 FPC 预热完成后，\`/en_US/products?sort=price_desc&wls_trace=1\` 的 MISS 样本 curl **0.981s**、应用 **919.41ms**；分类 \`women?sort=price_desc\` MISS curl **0.853s**、应用 **747ms**。随后不同诊断 key 的 MISS 样本商品 **0.623s/585.37ms**、分类 **0.635s/598.95ms**，均 HTTP 200。
- targeted 投影首次构建会与 Worker 预热争用资源；一次样本为 curl **2.311s**、应用 **1,581.62ms**。同一分类第二次请求命中 targeted 投影后为 curl **0.670s**、应用 **625.72ms**，\`product.catalog.resolve_filtered\` **0.63ms** 且无 \`build_filtered\`，证明跨请求复用生效。该并行争用仍需在低负载和部署预热窗口继续观察。
- 本轮定向契约测试全部通过：Header timing/缓存指纹、主题片段、导航片段、Filters、targeted catalog、分类页面共 **41 tests / 318 assertions**（各命令均仅有既有 PHPUnit deprecation 提示）；相关 PHP lint 与 \`git diff --check\` 通过。

当前冷启动目标在预热完成后已能取得约 0.6–1.0 秒样本，但 Worker 重载与首次 targeted 构建并发时仍可能超过 1 秒；不能把单次热命中当作所有冷请求的固定承诺。继续优化应优先处理 Worker 预热争用、首轮 Slot/regular widgets 和数据库分页前的代表 offer 查询。

## 2026-09-07 — 分类子树、统一 Facet 读模型与 FPC v8 验收

- 根分类页现在从已缓存的 `product.category_tree` 一次取出当前节点的整棵后代子树，再用一个 `categoryLinks` 批量查询收集商品 ID。分类页不会为每个子分类递归发起关系查询；本次真实 `/en_US/category/women` 保持 **120** 个结果。
- 分类默认摘要路径只读取代表报价和商品显式属性行，商品属性行在请求内同时供目录快照和 facet 统计复用；只有存在 `af_*` 条件时才进入完整列表投影。筛选面板的 Product 分片 facet 读模型按 `channel + lang + currency` 使用 `product.catalog_facet_counts` 策略缓存，并通过 `filter_panel.v2` 与 FPC schema v8 淘汰旧的“只有 Price”页面缓存。
- facet 统计优先使用 Product 商品属性和发布态代表报价的 `combination` 值，随后统一经过标签解析器归一化。因此 Han Dynasty、Women 等选项不会因为数字 ID、中文原值和已翻译文本同时存在而重复显示；带 `af_hanfu_shi_yong_xing_bie=Women` 的真实请求返回 **12** 件并保留 12 组筛选。
- 最新重载后的真实入口结果：分类首次 FPC MISS HTTP 200，curl **0.848s**、应用 **777.36ms**；同 URL FPC HIT curl **0.094s**、应用 **62.36ms**。Women 条件首次构建 HTTP 200，curl **0.988s**、应用 **939.29ms**。无筛选 `/en_US/products` 正常命中约 **65ms**；关闭 FPC 的诊断请求先测得约 **0.991s / 944.03ms**，随后新 Worker 样本降至 curl **0.639s**、应用 **583.55ms**；没有复现 10 秒等待。
- 诊断阶段表明剩余冷 SSR 成本主要在首轮 Slot/regular widgets、head 和约 **1.5MB** HTML 的模板组装；数据库累计约为几百毫秒，当前证据仍不足以直接修改表结构或增加索引。下一步应对隐藏 mega menu 按需输出、首屏 regular widgets、FPC 同 key build-lock 等候做受控 profile，再决定是否下推完整候选过滤与分页。

定向回归：`CategoryFiltersWidgetContractTest` **13/13、57 assertions**，`StorefrontCatalogFilteredOffersContractTest` **1/1、25 assertions**，`StorefrontCategoryViewServiceContractTest` **1/1、5 assertions**，`ProductCategoryTemplateContractTest` **3/3、35 assertions**；相关 PHP lint 和 `git diff --check` 通过。Chrome 当前 CUA 仍锁定，未把 HTTP 验收冒称为浏览器点击验收。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。

## 2026-09-07 — 首请求预热让行与插槽边界扫描收敛

- deferred bootstrap warmup 在首轮事件循环完成后再额外等待 **3.0–3.7 秒**，并继续检查活动请求、请求/写缓冲、TLS/HTTP2 pending 和监听队列。预热仍由两个 Worker 各自执行，FPC 发布和共享缓存语义不变；该窗口用于避免预热 Fiber 在首次挂起前占住首个真实请求。
- `SlotBoundaryScanner::enumerateRegions()` 对正常边界标记改用一次 marker 配对和栈深度计算，只有预览 HTML 出现错序标记时才回退旧的容错路径。区域排序、wrapper 偏移和未闭合标记行为保持；新增 sibling/only-slot 回归覆盖。
- 预热延迟后的真实请求均 HTTP 200：商品列表首次诊断 MISS curl **1.166s**、应用 **1,097.48ms**，同路径第二次 **0.659s/569.71ms**；分类首次 **1.008s/968.11ms**，第二次 **0.662s/554.13ms**。后续分类数据阶段命中 targeted projection，`product.catalog.resolve_filtered` 约 **2.35ms**，没有重新执行 `build_filtered`。
- 该轮日志仍显示约 **1.1–1.5MB** HTML 的首轮 Slot/regular widgets、head 和卡片模板是主要 SSR 成本；插槽边界扫描约 **34–36ms**，数据库累计通常低于 **110ms**。因此没有凭单次请求新增索引或改变表结构。

本轮新增预热契约 **9 tests / 37 assertions**、边界扫描 **8 tests / 22 assertions** 均通过；并发执行 PHPUnit 时曾因共享临时配置出现一次 `config.xml` 写入竞争，随后串行重跑全部通过。Chrome 当前 CUA 仍锁定，HTTP/Worker runtime 验收有效。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。

## 2026-09-07 — 前台 Head 跨 Worker 共享缓存验收

- 前台 `head` 片段已经和 header/footer 共用 `StorefrontThemeCacheCoordinator::storefrontChromePolicy()`，范围为当前 channel，按语言和货币变体，并沿用 catalog/config 代次。`Partials` 仍把页面布局、主题、颜色、站点名和源文件指纹放入逻辑键；后台 head 不进入这个前台共享分支，预览/未冻结请求仍保留 request fence。
- 重载两个 Worker 后，用同一 `nocache=1` 页面连续请求验证跨进程复用：Worker 1 的 `theme.partials.fetch.head` 为 **34.65ms** 且产生 head 模板 include；Worker 2 为 **5.38ms**，对应 `template_profile` 不再出现 head 文件，说明第二个 Worker 从共享 head 缓存返回。两次响应均 HTTP 200，页面主体结构保持。
- 同次验证的整页动态耗时为应用 **601.16ms / 762.28ms**、curl **0.675s / 0.811s**；差异来自剩余 Slot/目录/模板工作，不把单个 head 命中时间当作整页冷启动承诺。一次并发构建实验还观察到两个未参与 FPC 维度的诊断参数共享同一 FPC variant，后到请求出现 `build_lock` 等待；这属于 FPC/Worker 并发长尾候选，后续需要用受控同 key 请求单独 profile，不能用任意 query 参数作冷请求隔离。
- 相关契约测试 **12 tests / 40 assertions**、PHP lint 和定向 `git diff --check` 通过；随后统一 benchmark **8/8 HTTP 200**，平均 **86.605ms**、P95 **113.572ms**，两个 Worker 各命中 4 次（报告：`var/log/wls/benchmark_report_20260906_212610_489297_en-us-products_pid13268.json`）。当前真正冷 SSR 仍有约 **1.15s** 样本，主要阶段仍是 Slot/regular widgets、目录投影和模板组装；数据库与翻译批量读取已不是唯一主因。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。

## 2026-09-07 — 代表报价 SQL 与筛选推荐位请求复用验收

- 商品列表的代表报价读取改为两步查询：先按 `product_id`、发布状态分组取最小报价 ID，再按这些 ID 回表取完整报价行。列表只保留每个商品一条代表报价，PDP 的全量报价读取不变；这样避免把同一商品的多个发布报价全部传入列表投影，同时保留完整字段和稳定顺序。
- 带 `af_*` 条件的列表请求会让推荐位复用主列表的完整目录投影缓存键。推荐卡不再在同一请求中重新构建 summary 目录；受控筛选冷请求的 `product.catalog.build` 从此前 **2 次** 收敛为 **1 次**，读取边界为 **220** 条代表行，输出商品与筛选结果保持一致。
- 新增 `product.card.render` 阶段计时后，最新列表样本的卡片渲染约 **67–91ms**，不是当前冷请求的第一瓶颈；主要剩余成本在主题 Slot/regular widgets、head 与约 **1.5MB** HTML 的模板组装。未凭单次样本增加数据库索引，后续如需下推筛选分页应先做受控 `EXPLAIN` 与结果哈希对照。
- 真实运行验收：不带诊断参数的 `/en_US/products` 并发 **4/8** 全部 HTTP 200，P95 **132.836ms**、QPS **31.87**，两个 Worker 均有命中；带 `Ming Style` 筛选并关闭 FPC 的单请求 HTTP 200，客户端 **643.072ms**，服务端 action 约 **588–674ms**。Worker 重载后的预热日志显示首个共享 FPC MISS **2.06s** 后探针命中 process，另一 Worker 共享 HIT **0.463s**；这条链路没有再出现 10 秒级等待。
- 同一批次的无筛选 FPC bypass 请求 HTTP 200，客户端 **1.039s**，服务端 action **775–950ms**，数据库累计 **123–157ms**、WLS **18–30ms**；剩余波动来自主题 Slot 和模板组装，已落在 1 秒附近，不能继续归因于数据库单点。
- 定向回归：代表报价仓储、筛选目录契约、推荐位目录契约、商品卡片计时相关套件均通过；4 个改动 PHP 文件 lint 及定向 `git diff --check` 通过。缓存范围仍由统一策略管理：翻译 global，目录按 website/store/channel，导航和主题片段按 channel。

当前剩余优化项是首轮 Slot/regular widgets、head 输出体积和入口等待；应继续用 `wls_trace=1` 阶段数据确认收益，避免把 FPC 命中或诊断参数串行构建误当作冷启动结论。

## 2026-09-07 — 首轮事件循环让行与 EAV 筛选选项读取

- 两个生产 Worker 的 deferred bootstrap warmup 现在必须先完成一轮事件循环，并且确认当前没有活动请求、请求/写缓冲、TLS peek/handshake、HTTP/2 pending 或监听队列后才启动。预热 Fiber 在首次挂起前仍是同步 CPU 工作；这个门槛用于避免刚到达的首个请求被整页预热占住。空闲 Worker 仍会在后续循环执行原有预热，预热范围、发布锁和 FPC 语义不变。
- 该门槛集中在 `worker_runtime_common.php` 的纯判断函数，HTTP 与 TLS Worker 共享；`stream_select(..., 0, 0)` 只探测监听可读性，不消费连接。新增契约用例覆盖首轮未完成、已有 pending work、空闲可启动和空 socket 四种结果。
- 筛选面板的元数据与 EAV 数量路径继续显式跳过未使用的 option DTO；最新真实筛选 no-cache 请求客户端约 **0.922s**，服务端 action 约 **0.820–0.940s**。标签解析器为了显示 facet label 仍会读取一次共享 option 元数据，单次观测约 **17–23ms**；本轮没有在缺少完整失效协议时另建 labels-only 持久缓存。
- 重载后的唯一查询商品页真实 FPC MISS 为服务端约 **0.984s**、客户端约 **1.046s**（1.66MB identity body）；同路径后续 FPC HIT 应继续单独以 warm benchmark 观察。根路径历史 MISS 仍有 **1.58s** 样本，因此“所有页面冷请求低于 1 秒”尚未成立；剩余主成本仍是 Slot/regular widgets、head 和模板组装，不宜继续归因于数据库设计。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。

## 2026-09-06 目录媒体批量读取与共用模型水合

- FileManager 通过可选 `FileAssetBatchUrlResolverInterface` 集中读取本次调用的原始 asset/locale 行，按 200 项分块；保持完整 scope、语言候选、每输入独立模型、权限与 URL 生成。原始行只在调用内使用，没有新增跨请求或最终 URL 缓存。
- `QueryDelegator::hydrateFetchResult(AbstractModel, mixed)` 是共用结果水合入口。调用者先保留 queryData；原 delegate 仍在原处执行 SQL 和删除 before/after 流程，然后委托同一水合段。fetch hooks、object/array/insert-ID 结果、find-fields 返回及 query/delete 状态清理保持，批量读取不自行复制这些规则。
- 商品目录沿用 `product.catalog.snapshot.media_url` 标签包围整批解析一次；calls 从逐商品次数变为批次数，duration 与 DB span 仍用于完整工作量比较。父子计时不能相加；DB span 不等同数据库服务整体耗时。
- 最小开发回归使用真实 SQLite、206 个唯一 asset ID/207 个输入，跨 200 分块且逐输入独立输出：原路径 621 SQL，候选 4 SQL；中心水合与 Storefront 组合 10 tests / 514 assertions 通过。该结果仅为开发证据，不能冒充真实 HTTP 提速或完整冷启动验收。
- 已运行正式仓库代码的媒体回归（10 tests / 514 assertions），完成 default 两 Worker 滚动重载及真实 HTTP。媒体 URL 的 DB spans 从原预热样本 623 降到 6；预热阶段 168.16→45.23 ms。新 Worker 首请求 5.626038 s（request 1）、预热目录重建 2.865764 s（request 10）、缓存命中 1.266134 s（request 7）；不同 Worker/负载条件不构成整页稳定提速证明。完整指标与请求 ID 见 `/private/tmp/weline-goal/media-batch-runtime-report.json`。
- 三份实际响应的 19 项结构检查通过，构建请求七个 snapshot phase 正常；24 张商品卡的顺序/主图 URL/alt/价格及 150 个购买按钮数量和可用状态保持。原整页 162 个 img 的完整属性均保留，多一个隐藏账户头像占位（无 src）使总数为 163，总数断言仍失败；该模板未在本补丁修改，触发原因未证实。不能将未暴露在 HTML 中的精确库存量宣称已经核验。
- 促销首次选品的下一步：ProductAdminReadService::searchRows 在既有批量数据上先执行原 name/sku/status 拒绝，再读取 identity；匹配项的 code/type/owner 权威值、fallback、scope、输出和排序保持，不新增缓存。候选最小 RED 后完整 9 tests / 94 assertions 通过；真实配置独立 CLI 同 SKU、范围和结果下 DB spans 226→10，identity find 216→0，134.755→68.585 ms。此为候选开发与 CLI 证据，正式重载后 HTTP 仍须单独核验，未将原 Worker deals 661 的其余计数强行归因。
- 不带 query 的正常商品请求仍可能记录完整 trace：DEV/DEBUG/WLS_DEV_MODE 或环境配置可自动启用；去掉 wls_trace 参数不代表关闭诊断，不能将此作为无 tracing 对照。原正常 GET 1.182354 s，request 37，实际仍记录 4096 spans、丢弃 5567 明细。
- 仍需完成：完整 WLS 冷请求低于 1 秒、Browser 筛选/排序/分页与语言交互，以及模板与促销改动的真实计时收口。当前 Browser 标签已核验存在，但界面读取超时，未冒称交互通过。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。后台/API：N/A（无新增路由）。

## 2026-09-05 目录冷请求投影优化与完整计时

状态：代码与定向回归、HTTP 实际请求已验证；Browser 操作验收未完成。

- `StorefrontProductDetailProjector::projectListing` 复用原来的语言/店铺覆盖与规格摘要投影，跳过 PDP 的全部规格选项及 availableValues 二次解析。`specifications` 仍供 Filters 的 `af_*` 匹配与数量统计；品牌、报价状态、组合身份、名称、SEO/原始描述等字段保持。
- `StorefrontProductMediaUrlResolver::resolveListingOffer` 只解析卡片媒体，保留原始 description 数据，省去列表不使用的 description_html 和详情正文图片资源解析。完整 `resolveOffer/project` 继续服务详情页，保留原富文本净化。目录 builder 原先外层的重复 resolveOffers 已删除。
- 标签索引直接遍历已校验的 readonly EAV DTO，避免两次 toArray 展开完整选项树。未新增业务静态缓存，现有统一 CachePolicy、网站/店铺/渠道、语言与币种及代次失效保持；目录逻辑键更新为 `product.catalog_offers.listing.v3.{websiteId}`，读取和失效都由协调器生成。
- `RequestLifecycleTrace::getAggregateSummary` 的 db_span_count/db_duration_ms/dropped_span_count 在4096条明细截断后继续计数。它表示已埋点DB操作，不等同SQL语句数、数据库连接等待或整个数据库服务耗时。
- 真实 HTTP 曾出现 action 1126.27ms、template 197.89ms，但 cap 后 trace_top 最大仅13.03ms，已复现晚结束父段丢失。现由统一 Trace 在 cap 外另保留最多40条、每条至少10ms的最慢样本；快速丢弃先于元数据处理，样本沿用脱敏与截断，不增长 compact 行或字典。getSpans 与 compact 汇总仍只含原始明细；getSpansWithDbSummary 另含顶层标记 sampled_after_cap 的样本，trace_top 包含这些慢样本，summary 的 slow_overflow_span_count/limit 明示采样。top/category 汇总仍为不完整保留集合；父级 db_duration_ms 仅从原始明细计算，不能当完整 DB 归因。样本随请求、Fiber 和 reset 隔离清理。
- 通用 `recordPhase(name, durationMs, meta)` 在 summary.phases 保留最多32个阶段，同名更新，元数据复用原脱敏规则；生命周期与请求/Fiber隔离保持，独立于明细cap。Product记录products/offers/snapshots/attributes/projection五段；projection另列fields_ms/media_ms。
- 商品显式属性集合通过 `AttributeValueRepository::listExplicitRows` 的现有 `fetchIterator()` 逐行读取，单个商品或整个候选集超过10000条属性仍完整返回；保留website分表、entity_type/entity_ids/store_ids筛选、所有locale与attribute_code、cleared/typed值转换及最终entity_id/store_id/attribute_code/locale排序。原始行逐条转换为既有返回数组，避免额外暂存完整raw数组；未修改框架 `fetchArray()` 的10000行保护阈值。
- 插槽字符串扫描优化：attributeValue 对不含目标字面属性名的标签先跳过全属性解析，findTagEnd 以字符串查找跳过普通文本及成对引用值，保留原引号/未闭合输入和字节边界。固定真实HTML1644845B、11321tags、27slots/regions同进程old/new交替回放，wrapper查找old462.61/913.80ms、new176.66/214.48ms，tags/bounds/regions哈希完全相同；此为已渲染HTML的纯扫描回放，不能冒充整页HTTP或原始pre-slot输入。真实HTTP与功能验收见 `/private/tmp/weline-goal/acceptance-report.md`；低于1秒目标尚未完成。

执行及验证：

- 最小用例先RED再GREEN：列表不加载详情规格选项、卡片不加载正文资源、DB明细截断后继续累计、阶段独立汇总。完整定向集合38 tests / 216 assertions通过；既有旧Trace/Runtime配置用例的失败经修改前后源码对照相同，未混报全套测试通过。
- 用已有 `StorefrontCatalogCacheCoordinator::notifyCatalogChanged(0, ...)` 定向失效后立即请求，未清业务数据。英文新目录重建200/TTFB 5.148s；products 1.99ms、offers 9.77ms（3341→79代表Offer）、snapshots 116.05ms、attributes 14.98ms、projection 1901.38ms（fields 1900.87ms、media 0.41ms）；DB span累计833.54ms。
- 中文分类重建200/TTFB 7.091s，projection 2208.24ms（fields 2207.69ms、media 0.47ms），DB span累计685.69ms。此前本轮未拆正文媒体时英文10.246s、projection 6247.71ms。环境存在其他并行工作与Worker重载，这些是真实样本，不是受控压测或保证值。
- 未重建目录的动态样本1.90s/2.40s；另一次英文请求6.36s没有phase，不能冒充目录冷构建。FPC明确命中也曾TTFB 3.08s，入口排队/连接/Worker调度仍需独立测量，不能只凭缓存命中声称请求已快。
- HTTP回读：中英文分类/列表均24卡；优化前后英文首屏24卡的ID、UUID、名称、SKU、价格与链接逐项一致；price_asc第2页价格升序；Ming Style属性筛选返回6件；0–99价格筛选本页24件全部位于区间。
- Browser：只连接已核验Weline profile。初次连接失效；重新发现当前实例后，真实导航CDP超时、截图再超时，未完成页面点击验收。没有使用其他Chrome profile，也没有把curl当Browser通过。
- 证据目录：`/private/tmp/weline-cold-opt-runtime/`（headers/html/time/timing.json、html-results.json）、`/private/tmp/weline-cold-opt-product/verified-tests.txt`；增量与前后哈希保存在同名product/trace/labels诊断目录。

仍需继续的优化（未完成）：共享只读EAV元数据骨架与私有文本选项回退查询；完整候选集过滤后分页仍在PHP，尚未改成数据库分页；模板/事件与约1.62MB HTML体积；FPC命中后的入口响应长尾；真实Browser操作验收。未在缺少执行计划证据时调整表结构或索引。

交付地址：[分类](https://p05113ef3.test.weline.com:9555/categories)、[英文商品](https://p05113ef3.test.weline.com:9555/en_US/products)、[排序分页](https://p05113ef3.test.weline.com:9555/en_US/products?sort=price_asc&page=2)、[属性筛选](https://p05113ef3.test.weline.com:9555/en_US/products?af_hanfu_chao_dai=Ming+Style)。后台/API：N/A（本轮无新增路由）。

## 2026-09-05 — 属性筛选导航与 EAV 表结构查询复用

- `Catalog::index` 按 Filters 已有语义提取标量 `af_*`，供排序和分页链接共用；保留价格筛选，切换排序回到第一页，默认排序省略 `sort`。框架内部布局参数不进入链接。
- `EntityAttributeStore::valueTableHasScopeColumns` 接入统一 `StorefrontScopeHotCache::rememberForRequest`。按实际 connector 的类型、主机、端口、数据库、文件路径、用户名以及完整表名和列名区分；同逻辑连接的不同模型可复用结果，切换数据库配置后重新查询。表结构事实与网站、店铺、渠道的业务数据缓存分别处理，不改变既有业务范围和翻译策略。
- 正常返回的 `true` 和 `false` 均仅在当前请求复用；原 SQL 保持不变。查询异常沿用返回 `false` 的兼容行为，但异常结果不写入缓存；新请求重新检查，无请求 Context 的 CLI 按原方式查询。现有 `provisionValueTables` 在新建表时写入范围字段，未增加持久 schema 缓存或 DDL 控制逻辑。
- Product Controller 变更对应模块版本 `1.0.130` → `1.0.131`；应用后使用 `php bin/w setup:upgrade --route --module=Weline_Product` 刷新路由。
- 定向验收覆盖：真实 HTML 中的排序/分页参数保留、默认排序与页码复位；生产 EAV 方法的重复查询计数、表/连接隔离、异常重试及请求结束刷新。真实页面与冷请求耗时需分别记录，不能用单测或 FPC 命中代替冷请求验收。
- 本轮修复前样本：97 商品 / 3536 offers，清理目录缓存后的英文请求 HTTP 200、TTFB 20.756 秒；DB spans 3029 条、累计 6509.48ms。机器负载和目录数据持续变化，该样本不作为稳定延迟承诺。证据保存在 `/private/tmp/weline-perf-continue/` 与 `/private/tmp/weline-perf-eav-schema/`。

交付地址：[英文商品](https://p05113ef3.weline.test:9555/en_US/products)、[属性筛选与排序](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_chao_dai=Ming+Style&sort=price_asc)、[属性筛选与分页](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_shi_yong_xing_bie=Women&sort=price_asc&page=2)。

## 2026-09-06 — timing确认活动价与目录选品递归

- 上轮空响应不是Worker已结束：后来同request日志显示617.89/658.34秒、catalog.build234/275次。阶段为inclusive，约数千万毫秒累计代表嵌套而非独占；请求级DB spans分别279758/326695，未由阶段汇总反向放大。
- 根因闭环位于Product快照活动价→Promotion活动选品→catalogFallbackIds→Product目录；已有产品/offer阶段反复出现而快照阶段未返回，与源码及真实活动探针一致。当前活动筛选SKU在基础数据中无匹配，应保留空结果。
- Promotion删除选品对完整定价目录的回退，保留Product基础读取与筛选规则；空集合不代表全场优惠。活动及theme+scope选品使用既有StorefrontScopeHotCache::rememberForRequest复用，只在请求Context内存在，完整scope含网站/店铺/渠道，不增加第二缓存系统。
- 最小RED/GREEN与应用后HTTP证据：`/private/tmp/weline-goal/acceptance-report.md`。框架冷请求低于1秒仍为完整目标；未达到不得以部分修复或FPC命中缩小验收口径。
- 交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products?nocache=1&wls_trace=1)。

## 2026-09-06 — 商品计算与基础查询减量

- StorefrontEavLabelResolver沿用原有商品/locale隔离索引，仅按实际属性懒建，不为每个商品展开整个EAV目录所有options与aliases。固定188输入逐字节等价；独立CLI投影1174.51→666.74ms，DB同670次，峰值282→180MiB。
- ProductAdminReadService复用Repository批量API，offers/attributes以200商品为批读取后分组，不新增缓存。真实191商品SKU案例查询957→197与969→209，结果完全一致。数据库schema/index未改，优化针对已测得的重复读取与组装。
- WLS真实独立临时key微基准：1KB/256KB/1MB各2次写读相等并逐key清理；1MB写9.11–15.69ms，没有重现HTTP cache_set约200ms。底层已TCP_NODELAY，不能凭台阶改传输或归因Nagle；需要同HTTP payload/acquire/encode/write/read证据再处理。
- 所有CLI时长都是局部样本，最终冷请求、pid/request_count、HTTP/2与功能证据见 `/private/tmp/weline-goal/acceptance-report.md`。完整目标尚未宣告完成。

## 2026-09-06 — Session 预热使用业务连接预算

Session 客户端与 Session 连接预热统一从 `SharedStatePoolDefaults::sessionClientOptions()` 读取选项；`wls.session` 的显式连接、读取和获取预算保留原有含义。Session 预热继续进入相同 endpoint、token authority 和 profile 的业务池，不使用 Memory 策略或 `wls.shared_state.prewarm_*` 的短超时覆盖业务预算。默认值保持现有 Session 行为：连接 0.5 秒、读取 1 秒、获取 0.1 秒。

原因是共享连接池按相同身份合并配置，并取较小的超时；原预热将 Memory 的 50 毫秒预算合入 Session 后，后续业务访问无法恢复原预算。此次调整仅统一调用方配置来源，不改变连接池合并规则、Memory 预热、安全检查或重试行为。

回归通过真实 Session 选项构建、预热方法和连接池验证默认与显式配置，使用零个预建连接避免打开 socket；断言预热前后池身份相同且超时不被缩小。真实 HTTP 验收仍需在 Worker 加载新代码后确认 Session RPC 的实际预算与结果。

## 2026-09-06 — Worker 策略状态连接池隔离

`WorkerPolicyKernel::boot` 与 `reconnectSharedStateIfDue` 通过现有 `pool_profile=worker_policy` 复用同一个策略状态池；普通 Memory 业务仍使用原来的默认 profile。策略状态的连接和读取预算保持启动 20 毫秒、重连 10 毫秒，获取预算仍为 5/3 毫秒。此处不改变安全策略、失败后的保守处理、重连条件、业务 Memory 默认值或共享池的最小值合并规则。

原两个策略入口没有指定 profile，在同一 Worker 内按相同 endpoint/token/authority 与业务池合并，将默认业务读取预算降为 20 毫秒，后续重连还可能降为 10 毫秒。独立 profile 使用已有池身份机制，避免不同用途的调用互相覆盖预算。此修复仅覆盖真实 Worker 的启动和重连入口。

最小回归执行真实 Worker 启动与重连方法，用内存中的策略描述和零个预建连接隔离外部系统；断言业务池与策略池不同、策略启动和重连复用同一池、各自预算保持预期，且全程连接数为零。正式验收需重载 Worker 后结合现有 RPC 观测核对实际业务连接预算和结果；单测不是整页性能结论。

## 2026-09-06 — EAV 完整候选批量预取与统一计时

- 商品目录在完整候选投影前，通过 Eav 的可选批量能力预取私有 options、free-set group/attribute 行，保留完整筛选、排序、分页和总数；原逐商品读取继续使用同一 `eav.metadata` 请求缓存键。按最多 200 个商品批量 `fetchIterator()`，空结果也写入原键，不新增私有/跨请求缓存，不提高 ORM 读取阈值。第三方元数据提供方未实现可选接口时继续原读取路径。
- `product.catalog.metadata_prefetch` 在投影层使用既有 `measurePhase()`，记录全部候选数及 DB/WLS 增量，包含在 `product.catalog.projection` 的 inclusive 时长内。预取异常在 phase 记录错误后回到原逐字段标签降级路径，不缓存失败标记或把失败查询当作空结果；Eav 原始读取接口的异常语义不变。不能把父子阶段相加，也不能仅凭 fields_ms 下降认定整体收益。固定 SQLite 216 候选验证 options/group 含共享查询各 217→3、attribute 3→2，完整序列化 DTO 逐字节一致；此为开发证据，实际 HTTP 收益另行验收。
## 2026-09-05 — EAV 只读树复用与标签查询请求缓存

- `AttributeMetadataCatalog::catalog` 通过现有 `StorefrontScopeHotCache::rememberForRequest` 按 entity/locale 复用 readonly DTO 树；`catalogForProduct` 再按商品/freeSetCode 区分，仅重建包含私有选项的节点，保留所有字段与排序。可变 ORM 行继续按原方式克隆，不共享可变模型。
- 自由集 ID 在请求内复用，group/attribute 查询加入 set_id 与 scope_product_id；没有分组时保留原空自由集输出并提前结束。公共 scope<=0 的既有兼容逻辑保持。
- `StorefrontEavLabelResolver` 对原 SQL 的 (scope_instance_id, 原始 code) 结果 ID/null 使用同一请求缓存入口。共享查找可以跨商品复用，每次返回 ID 仍执行当前商品的 assertUsableByInstance；查询异常不缓存为空结果。不按文本类型跳过查询，不改变数据库比较规则、旧选项回源或语言标签。
- 原实现已由最小用例复现重复构建/查询；应用后定向套件 **46 tests / 286 assertions** 通过，4 个 PHP 文件 lint 与定向 diff check 通过；有界差异复核无新增 P1/P2。保留工作区已有改动，无表结构、模块版本或持久缓存失效协议变更。
- 真实数据库：中英文各公共目录+14个商品，共30组输出与候选实现全部相同；一次独立 CLI 样本总构建 955.05ms→174.71ms。随后并行导入新增选项使旧快照漂移，已用原源码副本与落地源码在同一请求读取同批行重新对照，30组哈希仍全部一致。CLI 数值不是整页性能保证。
- 默认两个 Worker 已平滑重载，耗时12.5秒。通过既有 notifyCatalogChanged 定向失效后立即请求：英文200/TTFB **3.163s**，中文200/**2.680s**，两条 summary.phases 均证明目录冷构建；81商品投影分别 **219.08ms / 211.87ms**（字段218.79/211.54ms），DB span累计940.57/734.84ms。上轮79商品投影1901.38/2208.24ms。商品数与机器负载有变化，属于实际样本而非受控压测。
- HTTP验收：英中首屏24件ID一致；price_asc第2页价格升序；Ming Style筛选6件；0–99价格筛选本页24件均在区间，均200。正常入口1.293s，随后FPC HIT(process) TTFB **34.85ms**，应用计时24.77ms。
- 环境过程：早期系统load约79，冷请求服务端47.844s/DB21.580s；随后WLS短时停止，普通启动因hosts授权/manifest路由不匹配失败；入口后来恢复并已重载验收。未把异常高负载样本当作代码退化或优化比例，也未清理业务数据/运行资料。
- **未完成：Browser页面点击验收。** 初期Chrome自动化超时；恢复后工具明确报告Mac锁屏，需解锁才能继续。HTTP与单测不替代浏览器通过。
- 后续优先项：header模板工作量与输出体积（上轮header约893KB、include636ms，内部搜索栏约157KB/498ms，父子不可相加）；FPC在应用计时之后的telemetry装饰/序列化与入口等待需分别计时；PHP全候选集过滤分页与批量缓存API继续依据profile优化。复用现有Template片段缓存，暂不凭猜测加索引或Worker。
- 证据：`/private/tmp/weline-eav-opt/`（应用前后hash、46项回归、真实DB对照、冷请求timing、HTTP字段验收）；`/private/tmp/weline-cold-opt-runtime/eav-final-*`（HTTP headers/html/time）。

交付地址：[英文商品](https://p05113ef3.weline.test:9555/en_US/products)、[中文分类](https://p05113ef3.weline.test:9555/categories)、[排序分页](https://p05113ef3.weline.test:9555/en_US/products?sort=price_asc&page=2)、[属性筛选](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_chao_dai=Ming+Style)、[价格筛选](https://p05113ef3.weline.test:9555/en_US/products?price=0-99)。后台/API：N/A（无新增页面或路由）。

## 2026-09-06 — 全局翻译批量读取与首屏重复查询收口

- 常驻 WLS worker 在内存余量满足既有阈值时，`Phrase\\Parser::loadLocaleWords` 按 locale 和当前请求模块集合批量读取全局字典，并复用统一 phrase 共享缓存；内存压力或共享缓存不可用时继续逐词读取兜底。请求覆盖字典仍先于公共字典，语言翻译保持 global 范围，不按网站、店铺或渠道复制。
- 新增 `i18n.phrase.global_dictionary_batch` timing 阶段，记录批量字典的调用、耗时、错误及 DB/WLS 增量，不记录词条正文。`i18n.phrase.global_word` 保留，用于观测批量字典未覆盖词条的精确兜底。
- Header 热词和搜索类型通过 `StorefrontScopeHotCache::rememberForRequest` 做请求级复用，并保留统一的 `theme.header.hot_words`、`theme.header.search_types` 阶段；没有新增跨请求缓存或改变网站、店铺、渠道边界。
- Promotion 手工选品按网站、店铺、渠道分组后一次读取已发布基础商品，再按绑定顺序过滤，消除每个绑定商品重复扫描；保留原 500 条读取上限、发布状态和输出顺序。
- 定向验证：Phrase 请求模块缓存 2 tests / 10 assertions、Promotion 5 tests / 13 assertions、Header 新契约 1 test / 4 assertions，PHP lint 与 diff check 通过。WLS 两个 Worker 滚动重载成功，Session/Memory sidecar 未重启。
- 真实英文商品页在新 Worker 上结构完整（HTTP 200、182 个商品节点、Filters、分页和推荐区存在），无缓存动态 trace 约 1.15s，后续同 Worker 热请求约 0.72s；全局字典批量阶段约 10ms，逐词兜底约 20ms。中文 locale 页面标题、中文商品页标题和分页结构正常，动态请求约 1.06s。普通入口 HTTP TTFB 约 0.93s，重复 FPC 命中约 20ms。
- 偶发 10s 长尾仍与 WLS Memory 连接超时/冷却、Worker 预热及首屏 widgets/head 组装叠加有关；最新 trace 的主要可优化项已从逐词 DB 查询转为首屏 regular widgets、head 和过滤面板，不能把单次动态样本当作固定延迟承诺。

交付地址：[英文商品](https://p05113ef3.test.weline.com:9555/en_US/products)。后台/API：N/A（无新增页面或路由）。

## 2026-09-06 — 首屏摘要投影、过滤面板策略与 WLS 慢读熔断

- 商品列表增加 `StorefrontProductDetailProjector::projectListingSummary` 摘要投影。没有 `af_*` 属性筛选时，`Catalog::index` 使用 `includeListingDetails=false`，只解析商品名称、slug、卡片价格/库存和必要组合字段，不展开 PDP 才需要的 EAV 规格、变体轴和媒体；筛选、排序及 PDP 继续使用完整投影。目录缓存的 `full` 与 `summary` key 一起失效，避免摘要数据和完整数据出现不同步。
- 过滤面板通过 `StorefrontCatalogCacheCoordinator::filterPanelPolicy()` 进入统一热缓存，范围为渠道，维度为 locale/currency，依赖目录、价格和配置。缓存 key 会规范化价格、排序和 `af_*` 条件，并排除 `page`、`nocache`、`wls_trace`、`perf_case` 等诊断参数；请求内仍复用已有 memo，未新增散落的缓存实现。
- 主题 `SiteBrand` 的配置读取在请求内 memoize；首页 bestseller、new arrivals、related、bundle 等卡片池改用摘要目录投影，减少每个卡片重复构建 EAV/PDP 数据。对应的定向契约测试先 RED 后 GREEN，保持原输出字段和顺序。
- WLS 适配器在 L1 为空时跳过一次无意义的共享 epoch 探针，首次业务读仍访问共享缓存；当 worker 已有本地条目时继续按请求同步 epoch，保留跨 worker 清空后的失效正确性。
- WLS Memory 的实际注册适配器（`Weline\Server\Cache\Adapter\WlsMemoryAdapter`）及 Framework 兼容适配器统一包住 get/set/delete/clear/CAS/has/epoch/incr 等远端操作并记录单次耗时。默认远端慢读阈值为 75ms（可用 `remote_slow_threshold_ms` 配置）；慢响应会断开当前 facade，开启约 2 秒的进程级冷却，并在当前请求内直接走本地/数据库回退，防止同一请求反复等待共享 Memory。`clearAllMemory()` 会清理冷却状态，未写入业务数据，也没有重启 Session/Memory sidecar。
- 回归结果：Framework 适配器 6 tests/16 assertions，Server 适配器 6/24，目录摘要 3/37，过滤面板 12/55，SiteBrand 7/67，Phrase 全局字典 2/10；相关 PHP lint 与定向 diff check 通过。
- 真实运行证据：两台 HTTP Worker 已滚动重载，`server:status default` 显示 Master、Session、Memory、2 个 HTTP Worker 均运行中。新 Worker 上无 trace 冷请求 HTTP 200，TTFB **1.049s**、总耗时 **1.058s**；随后请求 TTFB **0.899s**、总耗时 **0.908s**。加入空 L1 epoch 优化并启用 gzip 后，连续三次为 **1.138s / 0.954s / 0.659s**，五次样本范围 **0.554–1.249s**，均 HTTP 200；trace 的 WLS 阶段约 **20–71ms**、DB 累计 **63–211ms**，没有再次出现 10 秒级 Memory 等待。旧的 10 秒长尾与共享 Memory 远端阻塞和重复重试一致，当前证据更支持运行时/连接逻辑问题，而不是先调整数据库表结构；索引和 schema 仍应以受控 SQL profile 为依据。
- 已在核验的 Weline Chrome 实例中完成真实商品页操作：英文商品页排序到 `Price: Low to High` 后首件价格为 **¥16.20**；价格筛选后显示 **106** 件；进入第 2 页显示 **25–48 of 106**，URL 保留 `price`、`sort`、`page` 参数。页面结构包含 Filters、分页、推荐区和页脚，HTTP 均为 200。

仍需持续观察：冷启动首个请求仍可能受 Worker 预热、模板/事件组装和机器负载影响而接近 1 秒；应继续通过 `wls_trace=1` 的阶段标签区分 PHP、DB、WLS 和输出序列化，不能用单次样本承诺固定延迟。后续优先级是首屏 widgets/head 输出体积、FPC 命中后的入口等待，以及有 profile 证据支持时再做数据库分页或索引调整。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。后台/API：N/A（无新增页面或路由）。

## 2026-09-07 — 目录与 PDP 价格装配、重复投影收敛

- `StorefrontCatalogViewService` 对价格装配器只解析一次，并用统一 `StorefrontScopeHotCache::rememberForRequest` 按商品、原价币种复用同一请求内的 `StorefrontOfferPriceView`。促销 Provider 列表和主题活动元数据也分别使用请求级复用；没有新增跨请求价格缓存，避免活动变更后的失效边界失控。
- PDP 的 `livePublishedOffersForProduct()` 复用批量快照已经完成的 EAV 投影、媒体 URL 和统一价格，只补一次主供应商信息，去掉原先的二次 EAV/媒体读取、二次投影和二次算价；完整详情路径的媒体行按商品批量读取，保留画廊/视频。目录结果同时携带首个活动的折扣类型和值，`product-info` 模板优先读取统一字段；旧缓存缺字段时保留原装配回退。
- PDP slug 解析和同请求的 live offer 解析都进入 `StorefrontScopeHotCache::rememberForRequest`，key 包含 scope canonical key、store、locale、currency 和 product/slug；评论槽、商品主部件等重复调用只复用当前请求结果，不跨请求缓存库存。
- 单商品 PDP 会跳过只适用于多商品列表的 metadata prefetch；54 个变体通过 `projectMany()` 共享一次商品级 EAV、媒体索引和 variant axes 上下文，保留每个变体的图片与输出形状。一次进程热样本的批量投影为 **91.94ms**，较逐变体重复解析更低；空可用属性时还会跳过无效的 axes metadata 构建。
- 实际详情页复验：`/en_US/product/<slug>` HTTP 200，商品画廊保留 4 个图片节点且活动折扣标记仍存在；最新重载后的动态样本应用计时 **1,141.70ms**、curl **1.203s**，`product.catalog.live_request` **218.27ms**（54 个变体）、`product.catalog.projection` **161.12ms**（bulk fields **122.67ms**、media **38.41ms**），主题 regular widgets 约 **161.98ms**、head 约 **101.80ms**。同一路径此前进程热样本为应用 **752.29ms**、curl **1.044s**；剩余耗时主要在主题 Slot/regular widgets、header/head 和首次输出组装，不能把本次目录收敛误报成整页固定低于 1 秒。
- 最新动态 trace 的 DB 累计约 **277.68ms**、WLS 阶段约 **69.83ms**，其余时间落在 `LayoutSlotRenderer`、`slot_renderer::processSlotsWithBoundaries`、header/category mega menu 与模板输出；当前证据不支持先改表结构，下一步应对首屏 widgets/head 体积和隐藏导航输出做受控 profile。
- 继续验收的真实 HTTP 样本：普通 `/en_US/products` 在 `Accept-Encoding:gzip` 下 HTTP 200、`X-Weline-FPC: HIT`、进程应用计时 **23.63ms**，curl **99.88ms**，压缩正文 **243,716 bytes**；动态 PDP `timing_probe=20260907accept` HTTP 200、FPC MISS、应用计时 **661.30ms**，curl **705.43ms**，压缩正文 **220,745 bytes**，画廊 4 图与 `data-deal-type="percentage"` 均保留。该次 PDP trace 的 DB 累计 **250.69ms**、WLS **15.81ms**、`product.catalog.live_request` **174.99ms**、批量投影 **108.63ms**、Slot 处理约 **297.08ms**；10 秒长尾未复现，剩余波动来自 Worker/片段缓存状态与主题 SSR 组装。
- 输出画像显示默认商品布局约 **1,001,486 bytes**，`theme.slots.opaque_park` 约 **988,504 bytes**；Header 源模板约 **194,914 bytes**，隐藏分类侧栏/mega menu 是后续首屏输出与浏览器解析的主要候选。已为关闭状态的移动分类抽屉加入 `content-visibility:hidden`，不改变 HTML、ARIA 或打开后的菜单逻辑；若继续压低冷 SSR，应在可访问性验收后把隐藏导航拆为按需片段。
- 普通商品列表继续通过统一 FPC 预热与共享命中；动态 bypass 仅作为诊断链。所有缓存仍由统一 `CachePolicy` 和 `StorefrontScopeHotCache` 管理，目录按 channel（含语言/币种）变化，分类基础数据按 website，导航按 channel，翻译词典保持 global。
- 定向验证：空 EAV 行摘要快路径 **1 test / 9 assertions**、价格装配器 **2 tests / 9 assertions**、WLS storefront warmup **6 tests / 27 assertions** 通过；相关 PHP lint 与 `git diff --check` 通过。完整列表投影套件仍有既有无 bootstrap 的 `__()` 错误，PDP 契约套件在直接 PHPUnit 下缺少既有 `BP` 常量，未将环境错误计入本次通过。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。

## 2026-09-06 — 商品列表 READY 预热与冷启动重复渲染收敛

- Product 通过 `ViewWarmupContributionProvider` 向 Framework 发布有限的匿名商品列表路径（`/en_US/products`、`/zh_Hans_CN/products`、`/ar_SA/products`）。READY 动态预热和 FPC build-ahead 共用 `FpcWarmupProviderInterface`，不再把业务 URL 写死在 Framework；示例配置的 `dynamic_ready_gate_paths` 留空时由模块提供路径。
- 当只预热一个关键业务路径时，由配置指定的 HTTP Worker 执行共享 FPC 冷构建，其他 Worker 复用已发布正文，避免多个 Worker 同时渲染同一路径并争用 build lock。动态 READY 证明优先使用首页样本，首页未采样时使用首个业务样本，状态接口与真实预热结果一致。
- 动态预热默认只在显式严格目标门禁时因 `target_ms` 重试；普通“已成功但超过诊断目标”的样本不重复渲染同一路径，避免 70ms 目标与实际首屏成本不匹配时把 Worker 启动放大三倍。示例配置同步为 fail-open，预热异常不会让健康 Worker 无法 READY。
- 重载后的真实动态首渲染基准：`/en_US/products` 在 FPC bypass、HTTP 200 条件下为 **541.64ms** 和 **563.03ms** 两次样本；同批 Worker READY 耗时约 **4.6–4.7s**。这证明商品列表的路由/模板/目录链已在 READY 前完成一次初始化，但由于当前执行环境的本机控制端口清理受到权限限制，尚未把共享 FPC 清空后的首个公共请求作为最终冷 FPC 证明。
- 新增回归覆盖模块预热路径、业务路径 READY 证明、示例配置和慢诊断不重复渲染；5 tests / 17 assertions 通过，相关 PHP lint 通过。继续验收时应先在可访问控制端口的环境清理 FPC，再用 Weline Chrome 记录 `X-WLS-FPC-STATUS`、TTFB 和 `timing.log` 同一 request_id。

## 2026-09-07 — 商品关键路径改为统一 FPC 预热并收敛并发重复构建

- READY 后的商品关键路径预热改为调用统一 `FullPageCacheCoordinator` 正常构建链，不再用 FPC bypass 的动态渲染冒充公共缓存。每个路径先构建一次，再发起一次内部命中探针；只有探针返回 `HIT` 且有正文时才记录 `ready:fpc-hit`。预热日志写入 `var/log/wls-storefront-warmup.log`，包含 Worker、路径、构建/探针耗时、FPC 状态、正文大小和失败原因，不记录 Cookie 值。
- Product 只在显式 `WLS_INTERNAL_STOREFRONT_CHAIN_WARMUP=1` 的诊断链预热中缩为一张代表卡并跳过分页链接生成；正常 FPC 预热保持公开 24 卡页面形状，避免把一张卡的内部响应发布给访客。
- 持久 Worker 的普通 FPC 等待预算仍保持原 250ms；带 `WLS_INTERNAL_STOREFRONT_WARMUP=1` 的内部构建请求使用 `wls.worker.storefront_deferred_warmup_peer_wait_ms`（示例 5000ms）。默认 `wls.worker.storefront_deferred_warmup_owner_worker_id=1`，让单个 Worker 负责发布共享正文，其他 Worker 不再进入同一条冷构建链；设置为 0 可兼容旧的全 Worker 预热行为。
- 实际重载验证：首个预热在已有共享正文时为 **652–972ms**，内部探针为 **45–49ms** 且命中 `shared/process`；公开商品页随后为 `X-Weline-FPC: HIT`、应用计时 **29.61–53.38ms**，curl 总耗时 **55.83–106.98ms**。FPC bypass 的诊断请求仍约 **1.26s**，其中 DB 累计 **182.56ms**，主要时间在 `LayoutSlotRenderer`/`slot_renderer::processSlotsWithBoundaries`，说明真正的冷 SSR 瓶颈是主题 Slot 与模板组装，而不是单一数据库查询。
- 定向回归：WLS storefront warmup contract **6 tests / 24 assertions** 通过；Framework/Product/FPC 相关 PHP lint 通过。FullPageCacheCoordinator 完整定向套件在当前沙箱因 PostgreSQL loopback 权限受限出现既有连接错误，另有一个环境相关 Session cookie 名称断言失败，未将其计为本次改动通过。

## 2026-09-07 — 预热单归属与促销摘要投影

- 清理 `var/log` 下 **947** 个 `.log` 文件后重新采样。清理前约 **703,248,928 bytes**，文件保留不删除；新日志显示旧逻辑会让 4 个 Worker 同时预热 `/en_US/products`，耗时 **5.6–23.7s**，并伴随共享 WLS/数据库等待。
- Deferred storefront warmup 现在通过 `WLS_WORKER_STOREFRONT_DEFERRED_WARMUP_OWNER_WORKER_ID` / `wls.worker.storefront_deferred_warmup_owner_worker_id` 选出一个构建者，默认 Worker 1；其他 Worker 等待共享 FPC 结果，不重复冷渲染。重载后日志只出现 Worker 1 的预热记录。
- 促销页只需要 12 张商品卡，改为调用目录摘要投影并跳过整批 EAV 属性读取。此前 `/promotion` 首次请求约 **18.6s**，改后真实 HTTP 为 **0.58–0.86s**；`/en_US/products` 改后连续请求为 **0.39–0.51s**，均 HTTP 200。
- 新增预热归属、促销摘要和属性读取边界契约；相关 PHPUnit、PHP lint、`git diff --check` 与滚动重载通过。

## 2026-09-06 — 冷目录构建证据与统一预热入口

- 滚动重载后的唯一英文商品页探针 `guest_scope_cache=20260906q&wls_trace=1` 首次浏览器导航约 **6.37s**，服务端 `total_ms=5790.62`，FPC 为 `MISS/build_lock`。耗时集中在首屏 SSR：`list-filters` 约 **3106ms**，`product.catalog.resolve` 约 **2990ms**（build **1433ms**、projection **696ms**），LayoutSlotRenderer 约 **4137ms**；DB 累计 **619.88ms**、WLS 累计 **442.12ms**，另有一次 Memory read timeout **59.56ms**。这组数据说明冷长尾主要来自过滤面板/目录构建与插槽组装，数据库是组成部分但不是唯一根因。
- 同一 Worker 在共享目录与过滤面板缓存升温后，唯一探针 `guest_scope_cache=20260906r&wls_trace=1` 浏览器导航约 **1.30s**，服务端 `total_ms=842.04`；`list-filters` 不再位于耗时顶部，LayoutSlotRenderer 降至 **306.49ms**，DB **136.95ms**、WLS **58.48ms**。`q` 的同 URL 共享 FPC 命中服务端 **185.78ms**，浏览器约 **0.85s**（入口仍包含浏览器导航与传输等待）。因此后续应优先压低冷目录/过滤面板构建和入口等待，数据库索引或表结构只有在受控 `EXPLAIN`/SQL profile 证明后再调整。
- `cache:warm` 的 WarmCache 模块命令已接入统一 `CacheWarmerRegistry`：无域名调用、`--pool=<pool>` 和 `--list` 走集中注册表，并注册 `theme.storefront_fpc`；传入旧式位置域名时保留兼容路径。CLI 已确认 `--pool=fpc` 只选择 FPC 预热器，不再把 `--pool=fpc` 拼成错误 URL；当前沙箱不能连本机 9555 端口，所以预热器的 curl 失败仅是执行环境限制，真实 Worker/浏览器路径仍按实际 FPC 验收。
- 统一预热、Website/Store/Channel 目录缓存和过滤面板缓存继续沿用既有 `catalog` 命名空间失效；翻译仍为 global 范围。后续优先级：让部署后的预热任务实际执行、压缩过滤面板与 regular widgets/head 的首屏组装、继续用 timing 阶段定位 WLS 慢读；不凭单次冷请求样本增加数据库索引。

## 2026-09-06 — 匿名首屏保持可缓存与共享命中复验

- 首次访问公共商品页时，`Frontend` header、Checkout delivery context 和 Shipping quick-add 不再主动创建 Session 或 CSRF token；已有 Session cookie 的访问仍保持原登录态和地址上下文。匿名 shell 的运行时配置使用 `user_id=0`、空 `csrf_token`，首次交互再通过原有 QueryBin/异步流程读取动态状态。
- `Framework\App::syncCookieRouteStateFromServer` 对无 cookie 的前端 GET/HEAD 延后写入 Website/Store/Channel 路由状态 cookie。网站、店铺、渠道仍由 host/path 和当前请求上下文解析，带 cookie 的用户、后台和非 GET 请求继续使用原同步逻辑。这样匿名响应不会因为 `Set-Cookie` 被 Full Page Cache 拒绝。
- 最新 Worker 的唯一匿名探针 `guest_fpc_probe=20260906b` 首次响应 HTTP 200、TTFB **约 1.22s**、`X-WLS-FPC-STATUS: MISS`，响应没有 `Set-Cookie`；同 URL 第二次请求为共享 FPC HIT，TTFB **约 0.09s**，应用计时约 **60ms**。本次验证还确认 runtime config 中匿名用户为 0 且没有 CSRF token，未改变登录用户行为。
- 仍需把冷 SSR 的 action/slot/template 组装继续压低：该请求 timing 显示 `action_execute` 约 1.06s，`fetch_file_after` 约 0.80s，`list-filters` 约 0.15s，head/body-end 等主题片段合计仍有可观开销；这些是首屏生成成本，不能用 FPC 命中时间替代。WLS 慢读熔断和空 L1 epoch 优化已避免再次出现 10 秒级共享 Memory 阻塞。
- 新增定向回归：匿名 FPC 合同 **3 tests / 11 assertions**，App 匿名路由状态测试通过；Framework WLS 适配器 **9 tests / 27 assertions**，Server 实际注册适配器 **6 tests / 24 assertions**。相关 PHP lint 与 `git diff --check` 通过。完整 App 测试中仍有原有 CLI request URI 断言失败，和本轮匿名逻辑无关。
- 发布态插槽过滤新增 `ThemeComponentCatalog` identity index；组件定义仍按主题和区域缓存，`find()` 从索引 O(1) 命中，`clearCache()` 同步清除索引。这样容器子槽展开不再为每个 widget 线性扫描全部定义，输出和主题失效边界保持不变；回归用例 **1 test / 3 assertions** 通过。
- 运行态复验发现 CaptchaConfig 的内建 `object` 构造参数无法由 Weline ObjectManager 注入，导致验证码异步请求将 TypeError 显示到页面。构造函数已恢复为可由 ObjectManager 解析的 `ConfigReader` 契约，并补充构造函数契约回归；Captcha 定向套件 **11 tests / 42 assertions** 通过，真实 challenge 接口 HTTP 200、返回 2148 字节 challenge HTML，商品页复验无 CaptchaConfig 异常文本。

后续优化应继续使用 `wls_trace=1` 阶段数据，优先检查过滤面板、首屏 regular widgets、head 输出体积及 FPC 命中前的入口等待；数据库表结构或索引调整需先有受控 SQL profile 证据。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。后台/API：N/A（无新增页面或路由）。

## 2026-09-07 — 前台主题缓存策略统一与 PDP 规格模板复用

- 新增 `StorefrontThemeCacheCoordinator`，集中声明前台主题缓存策略。Header 导航片段与 Header/Footer 公共片段都使用 `channel` 范围，并统一按 `lang`、`currency` 变体，依赖 `catalog`、`config` 代次；`Partials` 不再在热路径直接传 `website/lang` 标志。预览、后台和未冻结的请求仍按原规则回退，不向默认租户共享。
- Full Page Cache 的共享 stale 判断改为检查外置后的载荷：超过共享内存阈值的 HTML 先落文件、共享值只保留安全指针，约 1.65MB 的商品列表也能在 fresh TTL 到期后走 stale，避免因为原始 HTML 大小被迫整页重建；未能外置或压缩载荷仍超过 1MB 时继续跳过 stale。
- 前台 Head 默认片段从显式 `off` 改为 guest chrome 缓存。主题数据、颜色、磁盘主题链接和源文件指纹仍进入现有片段身份，发布/配置失效沿用主题缓存清理流程。
- 单商品 PDP 的 54 个变体共享一份商品级规格模板；每个变体只重算其组合轴值，保留图片、活动价、规格排序、轴标签及旧输出字段。定向回归验证个体投影与批量投影规格逐项一致。
- 定向验证：主题缓存策略与片段合同 **14 tests / 52 assertions**，PDP 投影合同 **1 test / 17 assertions**，规格批量复用 **1 test / 4 assertions**；相关 PHP lint 和 `git diff --check` 通过。完整投影套件仍受既有无 bootstrap 的 `BP`/`w_env` 环境错误影响，未将其计入本轮失败。
- FPC 外置 stale 合同 **1 test / 2 assertions** 通过；完整 `FullPageCacheCoordinatorTest` 的其余环境错误仍来自当前沙箱 PostgreSQL loopback 权限和既有端口化 Session cookie 断言，不影响本次新增判断。
- 真实运行：重载后的动态 PDP MISS 首次应用计时 **1,029.52ms**（curl **1.095s**），画廊 4 图和活动折扣字段完整；同 Worker 不同诊断键的动态 MISS 应用 **434.42ms**（curl **488ms**），Head 阶段由首次约 **121.52ms** 降至 **21.88ms**，Header 降至约 **0.24ms**，批量投影字段约 **73.64ms**。同 URL 重复请求为 FPC HIT，应用约 **39.84ms**。
- 真实 trace 仍显示首个冷请求的主要波动来自 Worker/主题首轮组装：最近样本数据库累计约 **281.40ms**、WLS **63.89ms**，`slot_renderer::processSlotsWithBoundaries` **172.11ms**，`product.catalog.live_request` **190.79ms**。证据仍不支持直接改表结构；应继续观察 regular widgets、首轮布局读取和隐藏导航按需拆分。
- 最终入口复验：普通 `/en_US/products` 的一次 FPC MISS 为 curl **1.697s**、应用 **1,624.36ms**，紧接着同 URL FPC HIT 为 curl **108ms**、应用 **59.46ms**；排序分页 FPC 构建页 HTTP 200、curl **1.058s**，紧接着命中 **96ms**；重载后 PDP MISS HTTP 200、curl **1.032s**、应用 **952.69ms**，trace 的 DB **250.38ms**、WLS **37.63ms**、Slot 边界 **299.85ms**，画廊 4 图、活动折扣和隐藏导航样式保持。当前环境无法清空共享 FPC 来直接等待 stale 过期样本，不能把 stale 路径宣称为已完成的线上冷请求证明。

当前目标仍是把真正冷启动稳定压到 1 秒以内；首次请求尚有约 1.0–1.1 秒波动，不能用同 Worker 热样本或 FPC HIT 代替冷启动结论。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。

## 2026-09-07 — Theme workspace 请求级复用与列表/PDP 冷样本复验

- `ThemeScopedWorkspace::findWorkspace()` 对非锁定读取接入请求级 memo，key 使用完整 `ThemeEditorContext::identityHash()`；返回模型先 clone，避免 head/header/footer/Slot 之间共享可变 ORM 状态。写入前统一清空 workspace 行和既有 load 状态；`FOR UPDATE` 锁定读取始终绕过 memo，保留发布、回滚和并发冲突语义。
- 重载后的 PDP trace 证明同一请求内 `w_theme_scope_workspace` 从此前 **422 次 / 100.75ms** 降为 **49 次 / 12.23ms**；第二个 Worker 的热路径样本为 **7 次 / 2.29ms**。这部分是请求内重复读取的逻辑问题，当前证据不支持先改表结构或索引。
- 滚动重载后两个不同 Worker 的 PDP 动态 MISS 均 HTTP 200：应用计时 **1,066.92ms** 与 **464.03ms**，curl **1.143s** 与 **0.515s**；首个样本的数据库累计 **224.54ms**、WLS **75.36ms**，第二个样本数据库 **114.60ms**、WLS **16.69ms**。差异仍主要来自 Worker 首轮主题/模板组装，不能把单个热样本当成稳定冷启动承诺。
- PDP 核心数据保持：每个样本批量投影 **54 个 offer**，动态商品读取分别 **236.19ms / 135.76ms**，画廊 4 图和活动折扣字段保留；同 URL 重复请求为 FPC HIT，应用约 **38.91ms**、curl **113ms**。
- 商品列表正常路径 HTTP 200、FPC HIT、curl **180ms**；排序分页和属性筛选在不同 Worker 首次请求的服务端计时分别出现 **6,089.05ms** 与 **3,396.22ms**，但紧接着带诊断 key 的受控样本降为排序 **558.15ms / curl 618ms**、筛选 **1,255.19ms / curl 1.310s**。筛选 trace 的主要阶段是 Slot regular **452.43ms**、目录 build **212.35ms**、快照 **159.30ms**，读取了 **4,814** 个 offer 后选出 **220** 个代表项；这是后续数据库分页/代表 offer SQL 的候选，但需要受控执行计划和结果对照后再改。
- 当前优先级收敛为：继续压低首轮 Slot/regular widgets 与 head 输出；评估把代表 offer、筛选和排序下推到搜索/数据库投影；验证 1.65MB 页面在 stale TTL 到期后的真实共享命中。翻译继续使用 global 缓存，目录按 website/store/channel 边界，导航按 channel；所有业务缓存仍从统一协调器声明和失效，不在模板中新增散落 key。

交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。
