# WelineFramework 模块开发完整指南

**版本**: 2.0  
**更新日期**: 2025-10-09  
**基于**: Weline_Ai模块实际开发经验

---

## 目录

1. [模块结构](#模块结构)
2. [模块注册](#模块注册)
3. [数据库操作](#数据库操作)
4. [Model实体开发](#model实体开发)
5. [Service服务层](#service服务层)
6. [Controller控制器](#controller控制器)
7. [路由配置](#路由配置)
8. [测试开发](#测试开发)
9. [常见问题](#常见问题)

---

## 模块结构

### 标准目录结构

```
app/code/Vendor/ModuleName/
├── register.php              # 模块注册文件（必需）
├── etc/
│   ├── module.xml           # 模块配置（必需）
│   └── env.php              # 可选的路由前缀配置
├── Setup/
│   ├── Install.php          # 安装脚本
│   └── Upgrade.php          # 升级脚本（可选）
├── Model/                   # 数据模型
│   ├── Entity1.php
│   └── Entity2.php
├── Service/                 # 业务服务层
│   ├── Service1.php
│   └── Service2.php
├── Controller/              # 控制器
│   ├── Api/                # REST API控制器
│   ├── Backend/            # 后台控制器
│   └── Frontend/           # 前端控制器
├── Helper/                  # 辅助类
├── Observer/                # 事件观察者
├── Console/                 # 命令行工具
│   └── Command/
├── tests/                   # 测试文件
│   ├── phpunit.xml
│   ├── TestCase.php
│   ├── unit/
│   ├── integration/
│   └── contract/
└── docs/                    # 文档
```

---

## 模块注册

### 1. 创建register.php

**位置**: `app/code/Vendor/ModuleName/register.php`

```php
<?php
\Weline\Framework\Register\Register::register(__DIR__, \Weline\Framework\Register\Register::MODULE);
```

**要点**:
- ✅ 必须使用 `Weline\Framework\Register\Register::register()`
- ✅ 第一个参数必须是 `__DIR__`
- ✅ 第二个参数必须是 `Register::MODULE`
- ❌ 不要添加任何其他代码

### 2. 创建module.xml

**位置**: `app/code/Vendor/ModuleName/etc/module.xml`

```xml
<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
        xsi:noNamespaceSchemaLocation="urn:weline:framework:module:etc/module.xsd">
    <module name="Vendor_ModuleName" setup_version="1.0.0">
        <sequence>
            <module name="Weline_Framework"/>
            <!-- 其他依赖模块 -->
        </sequence>
    </module>
</config>
```

**要点**:
- ✅ 模块名格式: `Vendor_ModuleName`（驼峰命名）
- ✅ 必须包含 `setup_version`
- ✅ 通常依赖 `Weline_Framework`
- ✅ `<sequence>` 标签定义模块依赖顺序

### 3. 安装模块

```bash
php bin/w setup:upgrade
```

**验证模块安装**:
```bash
# 查看模块列表
php bin/w module:list | grep "ModuleName"

# 应该显示: Vendor_ModuleName    # 开启
```

---

## 数据库操作

### Setup/Install.php

**位置**: `app/code/Vendor/ModuleName/Setup/Install.php`

```php
<?php

declare(strict_types=1);

namespace Vendor\ModuleName\Setup;

use Weline\Framework\Setup\InstallInterface;
use Weline\Framework\Setup\Setup;
use Weline\Framework\Setup\Model\Context;

class Install implements InstallInterface
{
    public function setup(Setup $setup, Context $context)
    {
        // 创建表1
        $setup->getConnection()->createTable(
            'your_table_name',
            function (\Weline\Framework\Setup\Schema\Table $table) {
                $table->addColumn('id', 'int', 10, 'PRIMARY KEY AUTO_INCREMENT', 'ID')
                      ->addColumn('name', 'varchar', 255, 'NOT NULL', '名称')
                      ->addColumn('status', 'varchar', 50, 'NOT NULL DEFAULT \'active\'', '状态')
                      ->addColumn('created_at', 'timestamp', 0, 'NOT NULL DEFAULT CURRENT_TIMESTAMP', '创建时间')
                      ->addColumn('updated_at', 'timestamp', 0, 'NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP', '更新时间');
            },
            '表描述'
        );
        
        // 创建表2...
    }
}
```

**要点**:
- ✅ 必须实现 `InstallInterface`
- ✅ 必须实现 `setup(Setup $setup, Context $context)` 方法
- ✅ 使用 `$setup->getConnection()->createTable()` 创建表
- ✅ 表名使用小写+下划线命名
- ✅ 主键通常命名为 `id`
- ✅ 建议添加 `created_at` 和 `updated_at` 时间戳

**列类型参考**:
```php
// 整数
->addColumn('field', 'int', 10, 'NOT NULL', '描述')

// 字符串
->addColumn('field', 'varchar', 255, 'NOT NULL', '描述')
->addColumn('field', 'text', 0, 'NULL', '描述')

// 时间
->addColumn('field', 'timestamp', 0, 'NOT NULL DEFAULT CURRENT_TIMESTAMP', '描述')
->addColumn('field', 'date', 0, 'NOT NULL', '描述')

// 小数
->addColumn('field', 'decimal', '10,2', 'NOT NULL DEFAULT 0', '描述')

// 布尔（使用tinyint）
->addColumn('field', 'tinyint', 1, 'NOT NULL DEFAULT 0', '描述')
```

---

## Model实体开发

### 基础Model

**位置**: `app/code/Vendor/ModuleName/Model/Entity.php`

```php
<?php

declare(strict_types=1);

namespace Vendor\ModuleName\Model;

use Weline\Framework\Database\Model;
use Weline\Framework\Setup\Db\ModelSetup;
use Weline\Framework\Setup\Data\Context;

/**
 * Entity Model
 * 
 * @package Vendor_ModuleName
 */
class Entity extends Model
{
    // 状态常量
    public const STATUS_ACTIVE = 'active';
    public const STATUS_INACTIVE = 'inactive';
    
    /**
     * 初始化模型
     */
    public function _init(): void
    {
        $this->_table = 'your_table_name';
        $this->_id_field_name = 'id';
    }
    
    /**
     * 模型设置（必需实现）
     */
    public function setup(ModelSetup $setup, Context $context): void
    {
        // 表创建在 Setup/Install.php 中处理
    }
    
    /**
     * 模型升级（必需实现）
     */
    public function upgrade(ModelSetup $setup, Context $context): void
    {
        // 升级逻辑
    }
    
    /**
     * 模型安装（必需实现）
     */
    public function install(ModelSetup $setup, Context $context): void
    {
        // 安装逻辑
    }
    
    /**
     * 检查是否活跃
     */
    public function isActive(): bool
    {
        return $this->getData('status') === self::STATUS_ACTIVE;
    }
    
    /**
     * 验证数据
     */
    public function validate(): bool
    {
        if (empty($this->getData('name'))) {
            throw new \InvalidArgumentException('Name is required');
        }
        return true;
    }
    
    /**
     * 保存前钩子
     */
    public function beforeSave(): self
    {
        $this->validate();
        return parent::beforeSave();
    }
}
```

**关键要点**:

#### 1. 必须继承 `Weline\Framework\Database\Model`
```php
use Weline\Framework\Database\Model;

class YourModel extends Model
```

#### 2. 必须实现 `_init()` 方法
```php
public function _init(): void
{
    $this->_table = 'your_table_name';      // 数据库表名
    $this->_id_field_name = 'id';           // 主键字段名
}
```

#### 3. 必须实现 ModelInterface 的三个方法
```php
use Weline\Framework\Setup\Db\ModelSetup;
use Weline\Framework\Setup\Data\Context;

public function setup(ModelSetup $setup, Context $context): void {}
public function upgrade(ModelSetup $setup, Context $context): void {}
public function install(ModelSetup $setup, Context $context): void {}
```

**注意**: 
- ⚠️ 参数类型必须是 `ModelSetup` 和 `Context`，不是 `Setup`
- ⚠️ 必须有返回类型声明 `: void`
- ⚠️ 通常这三个方法留空，表创建在 `Setup/Install.php` 中

#### 4. 常用Model方法

```php
// 基础CRUD
$model->load($id);                   // 加载记录
$model->load($value, 'field');       // 通过字段加载
$model->save();                      // 保存
$model->delete();                    // 删除
$model->getId();                     // 获取ID

// 数据操作
$model->getData('field');            // 获取字段值
$model->getData();                   // 获取所有数据
$model->setData('field', $value);    // 设置字段值
$model->setData([...]);              // 批量设置

// 查询
$model->where('field', $value)->find()->fetch();  // 单条查询
$model->where('field', $value)->select()->fetch(); // 多条查询

// JSON字段处理
$config = $model->getData('config');
if (is_string($config)) {
    $config = json_decode($config, true);
}

// 保存JSON字段
if (is_array($this->getData('config'))) {
    $this->setData('config', json_encode($this->getData('config')));
}
```

---

## Service服务层

### 基础Service

**位置**: `app/code/Vendor/ModuleName/Service/EntityService.php`

```php
<?php

declare(strict_types=1);

namespace Vendor\ModuleName\Service;

use Vendor\ModuleName\Model\Entity;

/**
 * Entity Service
 * 
 * 业务逻辑处理层
 * 
 * @package Vendor_ModuleName
 */
class EntityService
{
    public function __construct(
        private readonly Entity $entity
    ) {
    }
    
    /**
     * 通过ID获取实体
     *
     * @param int $id
     * @return Entity
     * @throws \RuntimeException
     */
    public function getById(int $id): Entity
    {
        $entity = clone $this->entity;
        $entity->load($id);
        
        if (!$entity->getId()) {
            throw new \RuntimeException("Entity with ID {$id} not found");
        }
        
        return $entity;
    }
    
    /**
     * 创建实体
     *
     * @param array $data
     * @return Entity
     */
    public function create(array $data): Entity
    {
        $entity = clone $this->entity;
        $entity->setData($data);
        $entity->save();
        
        return $entity;
    }
    
    /**
     * 更新实体
     *
     * @param int $id
     * @param array $data
     * @return Entity
     */
    public function update(int $id, array $data): Entity
    {
        $entity = $this->getById($id);
        $entity->setData($data);
        $entity->save();
        
        return $entity;
    }
    
    /**
     * 删除实体
     *
     * @param int $id
     * @return bool
     */
    public function delete(int $id): bool
    {
        $entity = $this->getById($id);
        return $entity->delete();
    }
}
```

**要点**:
- ✅ 使用 `private readonly` 依赖注入
- ✅ Service处理业务逻辑，不直接操作数据库
- ✅ 通过Model进行数据访问
- ✅ 使用 `clone $this->entity` 创建新实例
- ✅ 抛出有意义的异常

---

## Controller控制器

### REST API Controller

**位置**: `app/code/Vendor/ModuleName/Controller/Api/Entity.php`

```php
<?php

declare(strict_types=1);

namespace Vendor\ModuleName\Controller\Api;

use Weline\Framework\App\Controller\FrontendRestController;
use Vendor\ModuleName\Service\EntityService;

/**
 * Entity API Controller
 * 
 * @package Vendor_ModuleName
 */
class Entity extends FrontendRestController
{
    public function __construct(
        private readonly EntityService $entityService
    ) {
    }
    
    /**
     * GET /api/v1/entity/{id}
     */
    public function get(): array
    {
        try {
            $id = (int) $this->request->getParam('id');
            
            if (!$id) {
                return $this->error('ID is required', 400);
            }
            
            $entity = $this->entityService->getById($id);
            
            return $this->success('请求成功', [
                'id' => $entity->getId(),
                'name' => $entity->getData('name'),
                'status' => $entity->getData('status'),
            ]);
            
        } catch (\RuntimeException $e) {
            return $this->error($e->getMessage(), 404);
        } catch (\Exception $e) {
            return $this->error('Internal server error', 500);
        }
    }
    
    /**
     * POST /api/v1/entity
     */
    public function post(): array
    {
        try {
            $data = $this->request->getBodyParams();
            
            // 验证必需字段
            if (empty($data['name'])) {
                return $this->error('Name is required', 400);
            }
            
            $entity = $this->entityService->create($data);
            
            return $this->success('创建成功', [
                'id' => $entity->getId(),
            ]);
            
        } catch (\InvalidArgumentException $e) {
            return $this->error($e->getMessage(), 400);
        } catch (\Exception $e) {
            return $this->error('Internal server error', 500);
        }
    }
}
```

**关键要点**:

#### 1. 继承正确的基类
```php
// REST API控制器
use Weline\Framework\App\Controller\FrontendRestController;
class Entity extends FrontendRestController {}

// 或后台REST API
use Weline\Framework\App\Controller\BackendRestController;
class Entity extends BackendRestController {}
```

#### 2. 使用父类的 success() 和 error() 方法
```php
// ✅ 正确
return $this->success('消息', $data);
return $this->error('错误消息', $code);

// ❌ 错误 - 不要自定义这些方法
private function success() {} // 会导致访问级别冲突
```

#### 3. 获取请求数据
```php
// GET参数
$id = $this->request->getParam('id');

// POST数据
$data = $this->request->getBodyParams();

// Headers
$token = $this->request->getHeader('Authorization');
```

#### 4. HTTP状态码
```php
return $this->error('Not found', 404);
return $this->error('Bad request', 400);
return $this->error('Unauthorized', 401);
return $this->error('Server error', 500);
```

---

## 路由配置

### 控制器自动发现

Weline 不使用 `routes.xml`。路由由控制器目录、继承的 area 基类和公开方法自动生成：

```text
Controller/Entity.php                 -> 前台 PC
Controller/Backend/Entity.php         -> 后台 PC
Controller/Api/Entity.php             -> 前台 REST API
Controller/Backend/Api/Entity.php     -> 后台 REST API
```

`etc/env.php` 只用于声明模块路由前缀：

```php
<?php

return [
    'router' => 'entity',
];
```

控制器变更后执行：

```bash
# 全量从空快照重建四类路由，并清理陈旧条目
php bin/w setup:upgrade --route

# 仅清理并重建指定模块，保留其他模块
php bin/w setup:upgrade --route --module=Vendor_ModuleName
```

`generated/routers/*.php` 是生成产物，不得手工编辑。

---

## 测试开发

### phpunit.xml

**位置**: `app/code/Vendor/ModuleName/tests/phpunit.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/9.3/phpunit.xsd"
         bootstrap="./TestCase.php"
         colors="true">
    <testsuites>
        <testsuite name="ModuleName_Test_Suite">
            <directory>./unit</directory>
            <directory>./integration</directory>
            <directory>./contract</directory>
        </testsuite>
    </testsuites>
</phpunit>
```

### 测试基类

**位置**: `app/code/Vendor/ModuleName/tests/TestCase.php`

```php
<?php

namespace Vendor\ModuleName\Test;

use PHPUnit\Framework\TestCase as BaseTestCase;

class TestCase extends BaseTestCase
{
    protected function setUp(): void
    {
        parent::setUp();
        // 初始化
    }
    
    protected function tearDown(): void
    {
        parent::tearDown();
        // 清理
    }
}
```

### 合约测试示例

**位置**: `app/code/Vendor/ModuleName/tests/contract/EntityGetTest.php`

```php
<?php

namespace Vendor\ModuleName\Test\Contract;

use Vendor\ModuleName\Test\TestCase;

class EntityGetTest extends TestCase
{
    public function testEntityGetContract()
    {
        $this->markTestIncomplete('TDD: 测试待实现');
        
        // 测试GET /api/v1/entity/{id}
        // 验证响应格式符合contract定义
    }
}
```

---

## 常见问题

### 1. 模块不显示或未激活

**问题**: 运行 `php bin/w module:list` 看不到模块

**解决方案**:
```bash
# 1. 检查register.php
cat app/code/Vendor/ModuleName/register.php

# 2. 检查module.xml
cat app/code/Vendor/ModuleName/etc/module.xml

# 3. 重新运行安装
php bin/w setup:upgrade

# 4. 清除缓存
php bin/w cache:clear
```

### 2. 数据库表未创建

**问题**: Setup/Install.php 执行了但表没创建

**常见原因**:
1. ❌ `createTable()` 方法调用不正确
2. ❌ 表名已存在
3. ❌ SQL语法错误

**解决方案**:
```php
// 在 Setup/Install.php 中添加调试
public function setup(Setup $setup, Context $context)
{
    echo "Creating tables...\n";
    
    try {
        $setup->getConnection()->createTable(
            'your_table',
            function ($table) {
                // 表结构
            },
            '表描述'
        );
        echo "Table created successfully\n";
    } catch (\Exception $e) {
        echo "Error: " . $e->getMessage() . "\n";
    }
}
```

### 3. Model接口方法签名错误

**错误**: `Declaration of Model::setup() must be compatible with...`

**原因**: 方法参数类型不匹配

**正确写法**:
```php
use Weline\Framework\Setup\Db\ModelSetup;  // ✅ 使用 ModelSetup
use Weline\Framework\Setup\Data\Context;

public function setup(ModelSetup $setup, Context $context): void {}
public function upgrade(ModelSetup $setup, Context $context): void {}
public function install(ModelSetup $setup, Context $context): void {}
```

**错误写法**:
```php
use Weline\Framework\Setup\Setup;  // ❌ 不要用 Setup

public function setup(Setup $setup, Context $context) {} // ❌ 缺少返回类型
```

### 4. Controller方法访问级别错误

**错误**: `Access level to Controller::success() must be protected`

**原因**: 重写了父类的protected方法

**解决方案**:
```php
// ❌ 错误 - 不要自定义success/error方法
private function success() {}
private function error() {}

// ✅ 正确 - 直接使用父类方法
return $this->success('消息', $data);
return $this->error('错误', $code);
```

### 5. 路由不工作

**问题**: API端点404

**检查清单**:
```bash
# 1. 确认控制器目录、area 基类和 etc/env.php 前缀正确
php -l app/code/Vendor/ModuleName/Controller/Api/Entity.php

# 2. 重建该模块路由
php bin/w setup:upgrade --route --module=Vendor_ModuleName

# 3. 检查路由是否注册
rg "your-route-path" generated/routers

# 4. 测试端点
php bin/w http:request rest/v1/entity/1 -api
```

### 6. 依赖注入失败

**错误**: `Class does not exist`

**解决方案**:
```php
// ✅ 正确 - 使用完整命名空间
public function __construct(
    private readonly \Vendor\ModuleName\Service\EntityService $service
) {}

// ✅ 或使用use导入
use Vendor\ModuleName\Service\EntityService;

public function __construct(
    private readonly EntityService $service
) {}
```

---

## 开发检查清单

### 新模块创建

- [ ] 创建 `register.php`
- [ ] 创建 `etc/module.xml`
- [ ] 运行 `php bin/w setup:upgrade`
- [ ] 验证模块已激活

### 数据库

- [ ] 创建 `Setup/Install.php`
- [ ] 定义表结构
- [ ] 重新运行 `setup:upgrade`
- [ ] 验证表已创建

### Model

- [ ] 继承 `Weline\Framework\Database\Model`
- [ ] 实现 `_init()` 方法
- [ ] 实现 `setup()`, `upgrade()`, `install()` 方法
- [ ] 使用正确的参数类型

### Service

- [ ] 使用依赖注入
- [ ] 处理业务逻辑
- [ ] 抛出有意义的异常

### Controller

- [ ] 继承正确的基类
- [ ] 使用父类的 `success()` 和 `error()`
- [ ] 添加异常处理

### 路由

- [ ] 不创建 `routes.xml`
- [ ] 控制器继承正确的 area 基类
- [ ] `etc/env.php` 的 `router` 前缀正确
- [ ] 执行 `php bin/w setup:upgrade --route`

### 测试

- [ ] 创建 `tests/phpunit.xml`
- [ ] 创建测试基类
- [ ] 编写单元测试
- [ ] 编写集成测试

---

## 最佳实践

### 1. 命名规范

```php
// 模块名: 大写驼峰 + 下划线
Vendor_ModuleName

// 类名: 大写驼峰
class EntityService

// 方法名: 小写驼峰
public function getById()

// 常量: 大写 + 下划线
public const STATUS_ACTIVE = 'active';

// 数据库表: 小写 + 下划线
your_table_name

// 数据库字段: 小写 + 下划线
created_at, user_id
```

### 2. 类型声明

```php
// ✅ 使用严格类型
declare(strict_types=1);

// ✅ 参数类型提示
public function create(int $id, string $name): Entity

// ✅ 返回类型声明
public function getData(): array

// ✅ 属性类型声明
private readonly EntityService $service;
```

### 3. 文档注释

```php
/**
 * 获取实体
 *
 * @param int $id 实体ID
 * @return Entity
 * @throws \RuntimeException 当实体不存在时
 */
public function getById(int $id): Entity
```

### 4. 错误处理

```php
// ✅ 使用有意义的异常
throw new \InvalidArgumentException('Name is required');
throw new \RuntimeException('Entity not found');

// ✅ Controller中捕获并返回合适的HTTP状态码
try {
    // 业务逻辑
} catch (\InvalidArgumentException $e) {
    return $this->error($e->getMessage(), 400);
} catch (\RuntimeException $e) {
    return $this->error($e->getMessage(), 404);
} catch (\Exception $e) {
    return $this->error('Internal server error', 500);
}
```

---

## 总结

本指南基于Weline_Ai模块的实际开发经验编写，涵盖了WelineFramework模块开发的所有核心知识点。

**关键要点**:
1. 严格遵循目录结构和命名规范
2. 正确实现Model接口方法（注意参数类型）
3. Controller不要重写父类的protected方法
4. 使用Setup/Install.php创建数据库表
5. 遵循TDD原则，先写测试
6. 使用PHP 8.2+特性（类型声明、readonly属性）

**参考资源**:
- WelineFramework官方文档
- 现有模块代码（Backend, Frontend等）
- 本指南的实际案例

---

**维护者**: WelineFramework开发团队  
**基于模块**: Weline_Ai v1.0.0  
**最后更新**: 2025-10-09
