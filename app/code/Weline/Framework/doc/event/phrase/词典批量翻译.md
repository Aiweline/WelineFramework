# Weline_Framework_Phrase::dictionary_translate

维护冷路径：批量 AI/机器翻译并 publish 到 `generated/language/{locale}.php`。

Framework 只 dispatch；I18n Observer 写 DB 与 locale 文件。

推荐调用：`DictionaryEvents::translate($module, $targetLocale, $options)`。
