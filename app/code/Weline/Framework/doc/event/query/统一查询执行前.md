# Weline_Framework_Query::before_execute

用于统一查询执行前扩展鉴权、参数校验、限流和拦截。

事件数据：

- `provider` 查询提供者
- `operation` 操作名
- `params` 参数
- `area` 区域（`frontend`/`backend`）
- `allow` 是否允许（观察者可改为 `false`）
- `error` 拒绝原因
- `result` 提前返回结果

