# Weline Framework 模块 - 事件文档

## 概述

本文档详细说明了 Weline Framework 模块提供的 `Framework_View::head` 事件及其使用方法。该事件在渲染视图头部时触发，允许其他模块注入头部内容。

## 事件列表

### 1. Framework_View::head - 视图头部事件

#### 基本信息

- **事件名称**：`Framework_View::head`
- **事件类型**：视图渲染事件
- **触发时机**：在渲染视图头部时
- **触发位置**：`app/code/Weline/Framework/View/TraitTemplate.php` 第 62 行
- **配置文件**：`app/code/Weline/Framework/etc/event.xml`

#### 功能说明

`Framework_View::head` 事件在渲染视图头部时触发，允许其他模块注入头部内容。通过设置 `class` 字段来指定头部渲染类。

该事件主要用于：
- 注入头部内容
- 自定义头部渲染
- 添加CSS、JS等资源
- 设置页面元信息

#### 触发时机

```php
// app/code/Weline/Framework/View/TraitTemplate.php
$eventData = ['position' => $position, 'is_backend' => $is_backend, 'class' => ''];
$this->eventsManager->dispatch("Framework_View::{$position}", $eventData);
$class = $this->eventsManager->getEventData("Framework_View::{$position}")->getData('class');
```

#### 使用场景

- **注入头部内容**：在页面头部注入CSS、JS、Meta标签等
- **自定义头部**：自定义头部渲染逻辑
- **资源管理**：管理页面资源加载
- **SEO优化**：设置SEO相关的Meta标签

#### 使用方法

##### 基本用法

在模块的 `etc/event.xml` 文件中注册观察者：

```xml
<?xml version="1.0"?>
<config xmlns:xs="http://www.w3.org/2001/XMLSchema-instance"
        xs:noNamespaceSchemaLocation="urn:Weline_Framework::Event/etc/xsd/event.xsd"
        xmlns="urn:Weline_Framework::Event/etc/xsd/event.xsd">
    <event name="Framework_View::head">
        <observer name="Your_Module::inject_head_content"
                  instance="Your\Module\Observer\InjectHeadContentObserver"
                  disabled="false"
                  shared="true"
                  sort="100"/>
    </event>
</config>
```

创建观察者类：

```php
<?php

namespace Your\Module\Observer;

use Weline\Framework\Event\Event;
use Weline\Framework\Event\ObserverInterface;

class InjectHeadContentObserver implements ObserverInterface
{
    /**
     * 执行观察者逻辑
     *
     * @param Event $event
     * @return void
     */
    public function execute(Event &$event): void
    {
        // 设置头部渲染类
        $event->setData('class', 'Your\Module\Block\Head');
    }
}
```

#### 事件数据

`Framework_View::head` 事件传递的数据：

| 字段 | 类型 | 说明 |
|------|------|------|
| position | string | 位置名称（head） |
| is_backend | bool | 是否是后端 |
| class | string | 头部渲染类名，可以设置此值来指定渲染类 |

**获取和修改数据**：

```php
// 获取数据
$position = $event->getData('position');
$isBackend = $event->getData('is_backend');

// 设置头部渲染类
$event->setData('class', 'Your\Module\Block\Head');
```

#### 使用示例

##### 示例 1：注入CSS和JS

```php
<?php

namespace Your\Module\Observer;

use Weline\Framework\Event\Event;
use Weline\Framework\Event\ObserverInterface;

class InjectHeadResourcesObserver implements ObserverInterface
{
    public function execute(Event &$event): void
    {
        // 设置自定义头部渲染类
        $event->setData('class', 'Your\Module\Block\Head');
    }
}
```

然后在 `Your\Module\Block\Head` 类中：

```php
<?php

namespace Your\Module\Block;

class Head
{
    public function render(): string
    {
        $html = '<link rel="stylesheet" href="/static/css/custom.css">';
        $html .= '<script src="/static/js/custom.js"></script>';
        return $html;
    }
}
```

#### 注意事项

1. **class设置**：必须设置 `class` 字段来指定头部渲染类
2. **执行顺序**：观察者按照 `sort` 属性排序执行，数值越小越先执行
3. **覆盖行为**：后执行的观察者可以覆盖先执行的观察者的设置
4. **性能考虑**：该事件在每次渲染视图时都会触发，应避免执行耗时操作

#### 系统行为说明

1. **默认行为**：
   - 默认 `class` 为空
   - 如果未设置 `class`，使用默认头部渲染

2. **执行顺序**：
   - 观察者按照 `sort` 属性从小到大排序执行
   - 后执行的观察者可以覆盖先执行的观察者的设置

#### 相关文件

- **事件配置**：`app/code/Weline/Framework/etc/event.xml`
- **事件定义**：`app/code/Weline/Framework/event.php`
- **触发位置**：`app/code/Weline/Framework/View/TraitTemplate.php`（第 62 行）
- **框架开发文档**：`docs/dev/开发文档.md`

## 扩展开发

如果需要扩展视图头部功能，可以：

1. **创建新的观察者**：实现 `ObserverInterface` 接口
2. **注册观察者**：在模块的 `etc/event.xml` 中注册
3. **设置执行顺序**：通过 `sort` 属性控制执行顺序
4. **处理异常**：确保观察者中的异常被正确处理

## 更新日志

- **2024-12-19**：初始版本，添加 `Framework_View::head` 事件文档

## 相关资源

- [Weline Framework 事件系统文档](../../doc/开发/服务器事件系统.md)
- [事件调试功能使用指南](../../../../../docs/事件调试功能使用指南.md)
- [观察者模式最佳实践](../../../../../docs/dev/开发文档.md)

