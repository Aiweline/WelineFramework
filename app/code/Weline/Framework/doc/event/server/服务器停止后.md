# Weline Framework - 服务器停止后事件（待实现）

> **目标事件名**：`Weline_Server::stop_after`  
> 历史名 `Weline_Framework_Server::stop_after` 已废弃。

当前 **无** PHP 派发与 `Server/event.php` 规约。需要停止生命周期钩子时，在 Server 停止主路径补齐规约+dispatch 后再订阅；勿监听旧 Framework 名。
