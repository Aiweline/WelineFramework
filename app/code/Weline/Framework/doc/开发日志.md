# Weline_Framework 开发日志

## 2026-09-23 — P8-FIX O1：locale skip 后短路 post_locale（2.5.171）

- 席：Team:后端: · team `wls-perf-regression-20260923` · channel msg-29/30 · UC-post-locale · **禁自 reload**。
- **缺陷**：B′ `locale_idle_skipped` 后仍无条件 `primeDeferredStorefrontHotCacheBags('post_locale')` ≈3875ms（pid 74859）。
- **落地**：`$localeSsrRan` 门控；未跑 locale FPC → stage `post_locale_skipped`（reason=`locale_idle_skipped`/`locale_ssr_not_run`）；仅实际 SSR 后有界 retouch。B′ 保留。
- 契约：DeferredBagPrime + AdoptHomepage · **7/113 OK**（`meetings/backend-ut-p8-20260923-024050.txt`）。
- 纪要：`doc/开发/team/wls-perf-regression-20260923/meetings/后端-construction-p8.md` · channel msg-31。
- **NEED_PM 受控 reload** → 性能 P8 绝对值复审。
- 版本：`2.5.170` → `2.5.171`。

## 2026-09-23 — HF-ED-P0-01：主题编辑器 500 根因＝重启窗口（2.5.169）

- 席：Team:后端: · team `hanfu-theme-editor-optimize` · HF-ED-P0-01。
- **定性**：非现存设计洞。`maybeCaptureHotCacheBagPrimeBeforeReset` → `isHotCacheBagPrimePendingForCurrentFiber` 在脏改落地/WLS drain 窗口（≈01:38–01:40+08）被旧 worker 字节码打中；master epoch144 于 ≈01:42 重启后齐。
- **证据**：`var/log/wls/default/error-2026-09-22.log` 17:38:39–17:40:53（UTC=本地+8 01:38–01:40）；startup-trace epoch144 @17:42:52Z；DoD 登录后 theme_id=3 编辑器 **HTTP 200**×2。
- **动作**：无改 HotCache 运行时；加固 `WlsRuntimeDeferredHotCacheBagPrimeContractTest`（断言 call+private 定义同在）；落盘 `Theme/.../channel/hf-ed-p0-01-backend-done.md`。
- **性能席**：不需为本项会诊（设计已在 P5 O2；本项为加载窗口）。
- 版本：`2.5.168` → `2.5.169`。

## 2026-09-23 — P5-fiber-yield O2：idle-gate + Fiber bag-prime latch（2.5.168）

- 席：Team:后端: · team `wls-perf-regression-20260923` · channel msg-10 / 性能复审 O2 · dirty-load 收口 · **禁自 reload**。
- **idle-gate**：`critical_sealed` 后 `awaitDeferredStorefrontIdleGate`（quiet-window · 有界）再跑 `post_critical_heavy` / `locale_idle`；locale path 间 `yieldDelay`。
- **capture_miss**：bag-prime latch 改 Fiber-local WeakMap；live Fiber 禁偷 latch；miss 一次 `capture_retry`。禁假 HIT；禁关 deferred 种袋。
- 协同 Product **1.0.301**（heavy `yieldDelay(15)`）。Theme chrome 交主题席。
- 契约：DeferredBagPrime + AdoptHomepage + Product · **9/103 OK**（`meetings/backend-ut-p5-20260923.txt`）。
- 纪要：`doc/开发/team/wls-perf-regression-20260923/meetings/后端-construction-p5.md`。
- **NEED_PM 干净 `server:reload -n`** → 性能再审；禁 upgrade 叠窗；claim_sla=false。
- 版本：`2.5.167` → `2.5.168`。

## 2026-09-23 — wls-perf-regression A+B+D：deferred 瘦身种袋 + locale idle（2.5.165）

- 席：Team:后端: · team `wls-perf-regression-20260923` · channel msg-5 冻结 · **禁自 reload**。
- **A**：`pre_critical` 仅轻袋/peek；`post_critical_heavy` 在 `critical_sealed` 后分片冷种（协同 Product 1.0.300）。
- **B**：fail-open 近处女窗 `localeBudget=0`；locale extras→idle（`locale_idle_begin`）；Provider 仍可声明。
- **D**：`hot_cache_bags_primed` 记 bag 内 DB/WLS span 只读计数。
- 契约：`WlsRuntimeDeferredHotCacheBagPrimeContractTest` + `WlsRuntimeAdoptHomepageFpcMetaContractTest`（9/87 OK）。
- 纪要：`doc/开发/team/wls-perf-regression-20260923/meetings/后端-construction.md`。
- **NEED_PM 干净 `server:reload -n`** → 性能复审；禁 upgrade 叠窗；claim_sla=false。
- 版本：`2.5.164` → `2.5.165`。

## 2026-09-22 — wave8-8c8：协同 Theme chrome/header 键对齐（2.5.164）

- **对齐** Theme `2.2.579`：website-scope Policy + guest-neutral header 键（msg-89 owner absent / header 846ms）。
- Framework 本波无运行时逻辑变更；升版吃同批干净 reload。
- **NEED_PM 干净 `server:reload -n`** → 立刻 8v（rc≲80）；禁 upgrade 叠窗；禁自 reload；claim_sla=false。
- 版本：`2.5.163` → `2.5.164`。

## 2026-09-22 — wave8-8c7：stage 经 RequestContext + chrome 空投影种袋（2.5.163）

- **根因（msg-85）**：ServerBag 剥掉 `WLS_PRIME_*` → Product 把空 stage 当 heavy，peer_hydrate 重建 catalog.full（total 3214）；chrome bake 缺失抛错未写入投影 → peer 仍 absent。
- **修**：① `deferredHotCacheBagPrimeStage` + RequestContext stage；② peer delay/重试；③ 协同 Product 仅 pre_critical heavy、Theme chrome miss→remember `[]`。保留 fail-open/no-store；禁假 HIT。
- 契约：`WlsRuntimeDeferredHotCacheBagPrimeContractTest`。S `2.0.78` / Product `1.0.297` / Theme `2.2.578`。
- **NEED_PM 干净 `server:reload -n`** → 立刻 8v（rc≲80）；禁 upgrade 叠窗；禁自 reload；claim_sla=false。
- 版本：`2.5.162` → `2.5.163`。

## 2026-09-22 — wave8-8c6：全 Worker peer 种袋 hydrate（2.5.162）

- **根因（msg-81）**：deferred 种袋仅 owner；探针 sticky 打 peer → `chrome_slot_projection` **l1/l2 absent ~651ms**；total 2401 劣于 2152。
- **修**：非 owner `runDeferredStorefrontPeerHotCacheBagHydrate`（delay 后 `peer_hydrate` 轻种袋）；协同 Theme `2.2.577`（slot peek 确认 + MRU 末位）。保留 fail-open/no-store；禁假 HIT。
- 契约：`WlsRuntimeDeferredHotCacheBagPrimeContractTest`。Server `2.0.77`。
- **NEED_PM `server:reload -n`** → 重开 8v；禁本席自 reload；claim_sla=false。
- 版本：`2.5.161` → `2.5.162`。

## 2026-09-22 — wave8-8c5：撤销 FPC bypass 种袋 thrash + Product→Theme 序（2.5.161）

- **根因（msg-75 回退）**：8c4 `X-WLS-Fpc-Bypass` 使 pre/critical/post_locale **三次全量 SSR** + `catalog.full` 反复重建，冲掉 Shared/Process LRU；deferred 仅 owner worker 种袋，探针打到 peer worker 时 chrome/builder **l1/l2 absent**（绝对残 B 1293→3091）。
- **修**：① 去掉 bag-prime 的 FPC bypass；② Provider 序 **Product→Theme**（header 后写保 MRU）；③ 协同 Product `1.0.296` 仅 `pre_critical` 跑重种袋。保留 fail-open/no-store；禁假 HIT。
- 契约：`WlsRuntimeDeferredHotCacheBagPrimeContractTest`。Server `2.0.76`。
- **NEED_PM `server:reload -n`** → 重开 8v；禁本席自 reload；claim_sla=false。
- 版本：`2.5.160` → `2.5.161`。

## 2026-09-22 — wave8-8c4：bag-prime FPC bypass + full catalog 种袋（2.5.160）

- **证据（msg-70）**：残 B 1293（builder 933+header 342）；warmup 日志 `pre_critical` 已 `in_request:true` 种 6 袋，但 builder 仍冷——Product 只种 summary/candidates，**未**种 `catalog_offers.full`/`summary-slug2`（targeted_reuse 空跑）；critical 回填 header 报 ScopeIdentity 缺失（FPC HIT 跳过 SSR）。
- **修**：bag-prime 请求加 `X-WLS-Fpc-Bypass` + `Accept-Encoding: identity` 强制 SSR 冻身份；协同 Product `1.0.295` full 种袋、Theme `2.2.576` header theme_id 对齐。保留 fail-open/no-store；禁假 HIT。
- 契约：`WlsRuntimeDeferredHotCacheBagPrimeContractTest`。Server `2.0.75`。
- **NEED_PM `server:reload -n`**（与 8s4 同批）→ 重开 8v；禁本席自 reload；claim_sla=false。
- 版本：`2.5.159` → `2.5.160`。

## 2026-09-22 — wave8-8c3：种袋更早 + 修 capture_miss（2.5.159）

- **根因（msg-65 残 B 82.8%）**：`hot_cache_bags_primed` 恒 `capture_miss/in_request:false`——`ServerBag::getServer` 读不到自定义 `WLS_PRIME_HOT_CACHE_BAGS`，finally 未在 Context 内种袋；袋又在 critical FPC 多秒 SSR **之后**才种，公网首击可抢空窗。header 557 因 Theme 未种 `theme.partials.fetch.header` HTML。
- **修**：① `deferredHotCacheBagPrimePending` 实例旗标驱动 finally 捕获；② `pre_critical` 在 FPC seal **前**种袋，seal 后再 `critical` 回填；③ 协同 Theme `2.2.573` Partials header。保留 fail-open/no-store；禁假 HIT。
- 契约：`WlsRuntimeDeferredHotCacheBagPrimeContractTest`。Server `2.0.74`。
- **NEED_PM `server:reload -n`**（与 8s3 同批）→ 重开 8v；禁本席自 reload；claim_sla=false。
- 版本：`2.5.158` → `2.5.159`。

## 2026-09-22 — wave8-8c2：builder 袋真种（registry 回落 + 请求内 Context）（2.5.158）

- **根因（msg-59 builder 5992）**：8c `hot_cache_bags_primed` 日志恒为 `seeded=0/bags=[]`——`generated/framework/modules.php` 未含 `storefront_hot_cache_bag_warmup.*`；且种袋在 FPC handle reset **之后**跑，无店面 CachePolicy Context。Theme 种袋未覆盖 `product.catalog_offers*`（真正的 `storefront.cache.builder`）。
- **修**：① Provider 发现：compiled prefix + Theme/Product FQCN 回落；② `WLS_PRIME_HOT_CACHE_BAGS` 专用 `/` 内请求，在 handle finally（reset 前）种袋；③ 协同 Product `1.0.294` catalog summary/listing 种袋。保留 fail-open/no-store；禁假 HIT。
- 契约：`WlsRuntimeDeferredHotCacheBagPrimeContractTest` 扩断言。Server `2.0.73`。
- **NEED_PM `server:reload -n`**（与 8s2 同批）→ 重开 8v；禁本席自 reload；claim_sla=false。
- 版本：`2.5.157` → `2.5.158`。

## 2026-09-22 — WB-OP 默认非抢占后台（browser_operator_non_preemptive · 2.5.157）

- 门禁 A 增 **3a**：操作员 Browser 默认后台非抢占；与 Ai MCP 硬规则同波。
- 打开顺序：`prefer_background_non_preemptive_navigate` → 禁缓存 → 抹标志 → 导航 → WB-OP。
- 版本：`2.5.156` → `2.5.157`。

## 2026-09-22 — wave8-8c：deferred 公网首击前真种 header/builder HotCache 袋（2.5.156）

- **对齐** msg-49/50：残 B≈48%（builder+header）；deferred 已暖 FPC HTML，但 Process/HotCache 袋未种满。
- **修**：① `StorefrontHotCacheBagWarmupProviderInterface`（capability 前缀）；② critical seal 后 / locale 后再跑 `primeDeferredStorefrontHotCacheBags`：`PostResponseTaskQueue::drain(250ms,64)` 冲掉 wave7 chrome 延后种 + 调用 Theme 等种袋 Provider；③ 保留 fail-open / 出站 no-store；禁假 HIT。
- 契约：`WlsRuntimeDeferredHotCacheBagPrimeContractTest`。协同 Theme `2.2.566` 种袋 API、Server `2.0.72`。
- **NEED_PM `server:reload -n`**（与 8s 同批）→ 8v；禁本席自 reload；claim_sla=false。
- 版本：`2.5.155` → `2.5.156`。

## 2026-09-22 — wave7-7c：deferred 两阶段 critical seal + `/products` L1 pin（2.5.155）

- **对齐** msg-40：压近处女 B 回流；保留 fail-open / 出站 no-store；禁假 HIT。
- **修**：① fail-open/未 HIT 时强制 `/`+`/products` 进 critical；② Phase A critical → `critical_sealed` + L1 retouch，再跑 locale（fail-open 下 locale≤2）；③ Process L1 pin 扩到 cookieless `/products` catalog receipt。
- 契约：`WlsRuntimeAdoptHomepageFpcMetaContractTest`；`FullPageCacheProcessL1HomepagePinContractTest`。
- 协同 Server `2.0.71` deferred 空窗 3s→0.35s。**NEED_PM `server:reload -n`**（与 Theme 7s 2.2.563 同批）；禁本席自 reload。
- 版本：`2.5.154` → `2.5.155`。

## 2026-09-22 — BackendRestController 接受 Admin Bearer（2.5.154）

- **问题**：Admin REST Demo 用 Backend Auth Bearer 时，`BackendRestController` 只看 `rest_backend` Session → 401「请先登录」。
- **修**：构造门禁在 Session 未登录时回落 `BackendApiAuthenticationInterface::getUserByToken` + `loadActor` 并 `session->login`。
- 协同 Api `1.0.9`（route_before 亦认 Backend Token）。
- 版本：`2.5.153` → `2.5.154`。

## 2026-09-22 — wave5-5c：HotCache peekPolicy（2.5.153）

- **问题**：`GET /api/api-demo/download` 外层 `/api` 为 `rest_frontend`；模块 router 同为 `api` 时 FrontendController 不可达；改 REST 后仍 401。
- **修**：① `Module\Helper\Data::collectFrontendApiDemoRouteAlias`（同 query-bin 剥 `api/`）；② `PublicApiAuthRouteMatcher` DEMO 白名单 `api-demo/download`（公开文档 zip，无 Token）。
- 契约：`PublicApiAuthRouteMatcherTest::testMatchesApiDemoDownloadPublicRoute`；协同 Api `1.0.8`。
- 版本：`2.5.151` → `2.5.152`。

## 2026-09-22 — i18n:collect 静态单飞锁（2.5.151）

- **问题**：多 Agent/shell 同时拉起 `i18n:collect`（全量+多模块）叠跑，PHP 在 `zend_array_dup/destroy` 热路径空烧多核。
- **修**：`DictionaryCollectGate`（`var/lock/i18n/dictionary-collect.lock`，`LOCK_EX|LOCK_NB` 失败即 BUSY）；`DictionaryCompiler::compile` 入口 acquire/release；同进程可重入；僵死持有者 SIGTERM 后重试；`i18n:collect` 遇 BUSY 退出码 `75`。
- 契约：`DictionaryCollectGateContractTest`。
- 版本：`2.5.150` → `2.5.151`。

## 2026-09-22 — wave4-4c：deferred 热路径槽 + Process L1 首页 pin（2.5.150）

- **对齐** wave3c 轴 C / 架构师：保留 fail-open；禁假 HIT；禁拆出站 `private,no-store`；禁无 epoch 平行袋。
- **修**：① deferred 显式热路径预留 `/`+`/products`，locale 只吃剩余 `max_paths`；② 连续 locale probe/build 失败≥2 跳过余下 locale，勿挤关键 HIT；③ Process L1 淘汰优先非 homepage-receipt pin，locale 插入在仅剩 pin 时拒写（Shared 仍持 locale）；HIT 读 LRU touch。
- 契约：`FullPageCacheProcessL1HomepagePinContractTest`；`WlsRuntimeAdoptHomepageFpcMetaContractTest` 扩断言。
- 版本：`2.5.149` → `2.5.150`。**NEED_PM `server:reload`**（禁本席自 reload）。

## 2026-09-22 — P1-9/M1：`deleteMultiple` 真批量（2.5.149）

- **缺口**：`getMultiple`/`setMultiple` 已走 `BatchCacheAdapterInterface`；`deleteMultiple` 仍 foreach 单键；接口无删面 → 批量失效仍 RPC N+1。
- **修**：接口补 `deleteMultiple`；`CachePool` 优先批量（空键短路；非批量 Adapter 仍逐键 fallback）。协同 Server `2.0.69`：`mdel` 协议 + `SharedCacheBatchStateInterface::deleteCacheMultiple` + `WlsMemoryAdapter` 真批量删。
- 契约：`CachePoolBatchAdapterContractTest`（含 delete 批/逐键/空批）。
- 版本：`2.5.148` → `2.5.149`。**需项目经理批 `server:reload`**（Memory sidecar 认 `mdel`）；禁本席自 reload。

## 2026-09-22 — B5：homepage adopt receipt + `/products` 预热优先（2.5.148）

- **根因**：`/` probe 已 HIT（ready=true），但 adopt 等到全部路径结束后才查 `resolveRootHomepageProcessReceipt`；期间多 MB locale SSR 挤掉 Process L1 → receipt 被删 → `homepage-ready-but-receipt-missing`。`/products` 排在 locale 之后，warmup probe 常 MISS，公网冷窗易持续 MISS。
- **修**：① deferred 路径序改为 `/` → `/products` → locales；② `/` probe HIT 后**立刻** adopt；③ sample 记录 `full_uri`，adopt 优先用该 URI；④ receipt 解析在 Process miss 时从 Shared hydrate 回填，不再直接丢 receipt。
- 契约：`WlsRuntimeAdoptHomepageFpcMetaContractTest`；`FullPageCacheHomepageReceiptSharedRehydrateContractTest`。
- 版本：`2.5.147` → `2.5.148`。**需项目经理批 `server:reload`**（禁本席自 reload）。期望：warmup adopted；公网 `/products` #2 `x-weline-fpc:HIT`。

## 2026-09-22 — B4：deferred 默认语权威 + warmup 钉住 FPC 变体（2.5.147）

- **根因（四次 reload / Worker 98447）**：Website 共享 snapshot 冷时，`resolveDeferredWarmupWebsiteDefaults` 回退到 Env `lang=zh_Hans_CN`，而 App 301 / 店面默认实为 `en_US` → `/en_US/` 槽回潮；`/`+`/products` 两次 warmup 的 FPC variant/key 分叉 → probe 仍 MISS（body 长度漂移），`/ar_SA/` 路径语稳定可 HIT。出站 `Cache-Control: private, no-store` 仅标冷 SSR、不阻断入池；进程 MISS 主因是键不稳定而非 no-store。
- **修**：defaults 对齐 App（`website.language` / `WELINE_WEBSITE_LANGUAGE` / snapshot）并以 `State::resolveWebsiteDefaultLanguage/Currency` 收口，**禁止** Env `lang` 冒充站默认；`runStorefrontFpcWarmupAttempt` 写入 `WELINE_WEBSITE_*` 钉住变体；Accept-Encoding 改 `gzip, deflate`。
- 契约：`WlsRuntimeDeferredWarmupDefaultLocalePathContractTest` 扩断言。协同 Index `1.0.4` / Product `1.0.290` / Server `2.0.64`（`server:reload` 后刷新 nginx）。
- 版本：`2.5.146` → `2.5.147`。**需项目经理批 `server:reload`（等待模式会顺带 `ManagedNginxService::reload` 写出 `|fpc2`）**；禁止本席自 reload。

## 2026-09-22 — 公网 `/products` edge 误缓存 FPC MISS + adopt 回写 homepage_fpc（2.5.146）

- **根因**：deferred warmup 为 **in-process**（公网 Host 权威），进程 FPC 对 `/`/`/products` 可 HIT；公网 cookieless 探针经托管 Nginx edge，首次冷 SSR（`x-wls-fpc-status:MISS`）被 `proxy_cache_valid 200` 写入 edge，随后长期 `edge STALE/HIT` 且头仍 MISS → 验收误判「进程未暖」。Worker 直连 `:19655` 同 Host 已 HIT。
- **修（Framework）**：`FullPageCacheCoordinator::publishResponse` 在 payload 快照入池后，对**出站** SSR 响应加 `Cache-Control: private, no-store`（入池头不受影响），避免 edge 再存 MISS。`adoptDeferredHomepageWarmupProof` 调用 `WorkerReadinessState::markBusinessHomepageHot` 镜像进程内 proof。
- 协同 Server `2.0.63`：edge `proxy_no_cache` 跳过 upstream FPC MISS/BYPASS；HTML cache key `|fpc2` 淘汰旧毒条目；status_report 回写 Master `homepage_fpc`。
- 契约：`FullPageCachePublishMissNoStoreEdgeContractTest`；`WlsRuntimeAdoptHomepageFpcMetaContractTest`。
- 版本：`2.5.145` → `2.5.146`。**需项目经理 `server:reload`（会重写 nginx）**；禁止本席自 reload。

## 2026-09-22 — 匿名首页预热 Set-Cookie×4（CSRF Session）+ 默认语路径 301（2.5.145）

- **根因**：首页 Newsletter `w:form csrf=auto` → `Token::create` → 原始 `Session::set/get` → 分配 `WELINE_CUSTOMER_SESSID` + 同族 Expire×3（共 4）；`FullPageCacheCoordinator` 拒收 Set-Cookie → deferred `/` ready=false → `homepage-not-ready` 无 adopt。`/products` 无 CSRF 输入故 cookies=0 可 HIT。locale 槽：`/en_US/`、`/en_US/products` 被 `App::redirectDefaultLocalizationPrefixIfNeeded` 301 剥默认语前缀（本站 default=`en_US`）。
- **修**：`Token::mayPersistInSession` — cookieless frontend GET/HEAD 不碰 Session；`Csrf::render` 空令牌不输出；deferred `filterDeferredStorefrontWarmupPath` 用 `State::canonicalizeStorefrontLocalizationPath` 丢掉会 301 的路径；warmup sample 记 `set_cookie_names`。
- 契约：`TokenAnonymousStorefrontFpcContractTest`；`WlsRuntimeDeferredWarmupDefaultLocalePathContractTest`。协同 Index `1.0.3` / Product `1.0.289` provider 跳过默认语前缀。
- 版本：`2.5.144` → `2.5.145`。**需项目经理 `server:reload`**；勿自 reload。

## 2026-09-22 — 验收 Browser 抹掉自动化标志（2.5.144）

- 门禁 A 增补 **`browser_strip_automation_flags`**：开验收 Browser 须与禁缓存同序抹掉 `navigator.webdriver` 等自动化标志；禁止把 reCAPTCHA 拦自动化当 WB-OP pass。
- 权威与 MCP 硬规则、测试席提示词同波对齐（见 `Weline_Ai` 1.3.11）。
- 版本：`2.5.143` → `2.5.144`。

## 2026-09-22 — Deferred storefront 预热公网 Host 权威（2.5.143）

- **根因**：`resolveCurrentInstanceWarmupHosts` 用 `public_host`+`main_port`（Worker 口 `:19655`）作 FPC Host；实例已有 `public_origin`（验收口 `:9555`）未参与选择 → deferred 写入不可被公网探针命中的 key。`warmup.log` 有 `begin` 后约 35s 出现 `failed`（非卡死）；`/` 在错误 Host 下 `Set-Cookie×4` 未 ready → 无 `adopted`。
- **修**：`resolveWorkerPublicOrigin` 回落实例 `public_origin`；`resolveCurrentInstanceWarmupHosts` / `resolveProcessLocalDynamicWarmupHosts` 优先公网 authority；deferred 路径保留 provider 尾斜杠；`adoptDeferredHomepageWarmupProof` 写 `adopted`/`incomplete` 阶段日志。
- 契约：`WlsRuntimePublicOriginWarmupHostContractTest`；既有 FailOpenDeferred 仍绿。
- 版本：`2.5.142` → `2.5.143`。**需 `server:reload`** 后看 warmup.log hosts=`:9555` 与 proof adopt。

## 2026-09-22 — 事件生命周期闭环契约（2.5.142）

- 新增 `EventLifecycleCloseoutContractTest`（登录像素事件名、悬空 Observer 防护、`Weline_Server::start_after` 派发点）。
- 文档：`start_after` 迁 Server；系统消息/Controller fetch_file/View header 命名更正条。
- 版本：`2.5.141` → `2.5.142`。

## 2026-09-22 — CachePool 真批量回归 + fail-open deferred 强制预热（2.5.141）

- **R1 回归**：`doc/开发日志` 曾记「`CachePool::getMultiple/setMultiple` 已优先批量驱动」，但实现仍是 `foreach`→单键 `get`/`set`。`Weline\Server\Cache\Adapter\WlsMemoryAdapter` 虽实现 `BatchCacheAdapterInterface`（真 MGET/MSET），Pool 未走接口 → 业务「批量预取」变 RPC N+1。
- **修 R1**：`CachePool` 在 adapter `instanceof BatchCacheAdapterInterface` 时走 `getMultiple`/`setMultiple`（保留 buildKey、禁用池、命中统计；整批一次 TTL 抖动；非批量适配器仍逐键 fallback）。`deleteMultiple` 仍逐键（接口无批量删除）。
- **修 R2**：homepage READY 默认 fail-open 跳过进程内 `/` prime；但 `storefront_deferred_warmup_enabled` 历史默认 `0`，deferred 从不跑 → 首击全量 SSR。现当 fail-open 或 homepage proof 未 HIT 时，强制调度 deferred storefront critical warmup（仍受 owner Worker 选举；严格模式已 HIT 时仍只看显式 flag）。
- 契约：`CachePoolBatchAdapterContractTest`；`WlsRuntimeFailOpenDeferredWarmupContractTest`；既有 `Server/WlsMemoryAdapterBatchTest` 应恢复绿。
- 版本：`2.5.140` → `2.5.141`（无 Schema/路由注册变更，不必 setup:upgrade）。

## 2026-09-22 — BinQueryOperation 默认拒绝暴露（2.5.140）

- 问题：Attribute 默认 `external=true`/`frontend=true`，新 op 未显式声明即进 Worker 与 `/bin/query` 候选面。
- 改：`BinQueryOperation` 默认 `external=false`、`frontend=false`；公开/站内/站外须显式 opt-in + `auth`。
- 文档：`Provider开发指南.md` 增「权限默认拒绝」；示例补 `auth: 'any'`。
- 版本：`2.5.139` → `2.5.140`。

## 2026-09-22 — Query 网关 boolean 接受 JS number 0/1（2.5.139）

- 现象：Checkout 续付 `continue_pay=1` → `Invalid param type: continue_pay` toast 循环。
- 根因：WQB1 保留 number；`normalizeParamValue` 只把字符串 `'1'`/`'0'` 收成 bool。
- 修：`FrontendQueryGateway` / `BinQueryGateway` 对 boolean 声明把 int/float `0`/`1` 规范化为 false/true。
- 协同 Checkout `1.5.28` 改传 `true`。
- 版本：`2.5.138` → `2.5.139`。

## 2026-09-21 — Phrase 模块词典补拉 NULL source_module 全局层（2.5.138）

- 现象：结账 en_US 仍中文「运费不含关税与税费」；词典已有全语种但 `source_module` 为空，`IN (modules)` 匹配不到。
- 修：`ModuleGlobalDictionaryProviderInterface::NULL_SOURCE_MODULE_KEY`；`Parser` 与请求模块一并单次加载全局层（不向每模块复制）。
- 协同 I18n `1.0.73` Provider；Checkout CSV 双写关税提示/航线名。
- 版本：`2.5.137` → `2.5.138`。

## 2026-09-21 — required injection 槽旁叠渲静态门禁（2.5.137）

- 根因：保证层只做「整槽 once / 页级 presence」，模板仍可在声明槽旁 `fetch` 同部件 → 登录社媒再次双渲；截图驱动热修不可持续。
- 修：`RequiredInjectionSiblingFetchScanner` + Rule（setup:upgrade 致命）+ CLI `frontend:check-required-injection-sibling-fetch`。
- 协同 Theme `2.2.514` 槽内至多一份断言；Product `1.0.288` 去掉 PDP sibling fetch。
- 版本：`2.5.136` → `2.5.137`。

## 2026-09-21 — 响应发射层自愈商品卡 CSS（2.5.136）

- 问题：店面 PDP 猜你喜欢有 `weline-product-card` 无 `data-weline-product-card-css`；`?nocache` / 私有响应跳过 FPC `publishResponse`，原 heal 只挂在 publish 路径。
- 修：`Response::prepareForEmission` → `healStorefrontProductCardCssIfNeeded`（与 FPC publish heal 共用 `HtmlCacheAdmission`）。
- 契约：`FullPageCacheProductCardCssIntegrityContractTest::testResponseEmissionHealsProductCardCssIndependentOfFpcPublish`。
- 版本：`2.5.135` → `2.5.136`。

## 2026-09-21 — Cookie::static_file 清请求 Cookie（2.5.135）

- 问题：`static_file()` 只 `header_remove(Set-Cookie)`，不清理 `$_COOKIE` / `HTTP_COOKIE`，易被误认为「框架已丢弃静态请求 Cookie」。
- 修复：静态响应前清空 `$_COOKIE`，并将 `HTTP_COOKIE` / `HTTP_AUTHORIZATION` 置空。
- 单测：`CookieStaticFileRequestIdentityTest` PASS。
- 版本：`2.5.134` → `2.5.135`。

## 2026-09-21 — 删除 Phrase 翻译路径旧事件与死分支（2.5.134）

- 删：`event.php` 中 `dictionary_collect` / `dictionary_ready_after` / `word_missing` 及对应 `doc/event/phrase/请求词典*.md`。
- `EventDictionary` 瘦身为请求覆盖词典只读 peek；无 `ensureCollected` / 无事件派发。
- `Parser`：`getLayeredWords` 不再装 DB locale 层；`loadLocaleWords` 文件-only；translate 不挂钩 prefetch/global。
- 测：Phrase 相关 UT **105/105 PASS**。
- 版本：`2.5.133` → `2.5.134`。

## 2026-09-21 — __() 翻译只读契约（2.5.133）

- 根因：`__()` / `processWords` 在 CLI 会整表 hydrate `w_i18n_locale_dictionary`，并在 miss 时写回词典 / 触发 `dictionary_collect`，setup:upgrade 卡在 `EnsureDashboardPages`（约 235s，CPU=`zend_array_dup`）。
- 修：翻译路径只读文件层 + 进程/共享缓存；缺词原样返回；禁止 DB 全局词典回源、禁止 miss 写回、禁止翻译路径自动收集。
- `EventDictionary`：只读 peek（后续 2.5.134 删事件）。
- `clearWorkerCaches` 不再清掉 `setupUpgradeLightDictionary`。
- 测：`ParserTranslateReadOnlyContractTest` + 相关 Phrase UT；`ensurePages` 约 **0.55s**（原约 235s）。
- 版本：`2.5.132` → `2.5.133`。

## 2026-09-21 — FPC 上收 Framework · Bypass/Store 适配器（2.5.132）

- Extra `type=fpc` + `FpcCapability` 迁 Framework；Capability 只经 `FpcStoreAdapter`。
- 新增 `FpcBypassRuleProvider` / `FpcBypassEvaluator`（侧车 + 内置回退）/ `FpcStoreAdapterRegistry`；`ProcessFpcStoreAdapter` 保底。
- Coordinator / 文档：`doc/开发/FPC上收Framework与Store适配器.md`。
- 测：`FpcBypassAndStoreContractTest`；协同 Theme `2.2.510` / Server `2.0.59`。
- 版本：`2.5.131` → `2.5.132`。

## 2026-09-21 — Title×locale 修复收口（2.5.131）

- 根因终认：`routeTitle` 人化 URL 段（Seo）；协同禁用人化回退 + Theme 发布 title 到 SEO 袋。
- 辅修：Phrase `ParserRequestState` Fiber 隔离；`State::getLang` 路径优先；WidgetI18n 禁 `$_SERVER`。
- 验收：交叉 `__title_locale` **mismatch=0**。
- 版本：与 Seo `1.2.96` / Theme `2.2.509` 同波。

## 2026-09-21 — Title×locale：State 路径语种优先 + Phrase Fiber 袋（2.5.131）

- 证据：并发下 `<title>`=Contact 而 H1=`联系我们`（同请求早晚 `__()` 语种不一致）。
- 修：`State::getLang` 路径/PATH_LANG 压过冻结 StorefrontCacheKeyContext；`ParserRequestState` 迁 RequestContext；Theme `WidgetI18n` 禁止回退进程 `$_SERVER`。
- 协同 Theme `2.2.508` layout_params 键 `storefrontLocale`。
- 测：`ParserFiberRequestStateIsolationTest` + 交叉 `__title_locale`。
- 版本：`2.5.130` → `2.5.131`。

## 2026-09-21 — Title×locale 串扰根因：Phrase 请求袋 Fiber 隔离（2.5.130）

- 根因：`Parser` 请求级袋（layeredWords / translatedWords / usedWords / isLoadingWords）为进程 static，WLS 多 Fiber 并发串语种 → `__()` 与 `<title>` 交叉。
- 修：`ParserRequestState` 迁入 `RequestContext`；CLI 无 Context 回退进程袋。
- 协同 Theme `2.2.508`：layout_params L1 键用 `WidgetI18n::storefrontLocale()`。
- 测：`ParserFiberRequestStateIsolationTest`；交叉 `__title_locale` 压测。
- 版本：`2.5.129` → `2.5.130`。

## 2026-09-21 — 店面 Template frontendAuth 恒游客（2.5.128→2.5.129）

- `Template::frontendAuthStaticHookCacheContext` 恒 `frontend-auth:0`，不再按 Session 分片 hook/header 缓存。
- 契约：`TemplateFrontendAuthGuestContextContractTest`。
- 协同：店面 SSR 匿名 + Account/B2B 交互鉴权。
- 版本：`2.5.128` → `2.5.129`。

## 2026-09-21 — Title×locale 串扰可观测埋点（2.5.127→2.5.128）

- 增：`View\Helper\TitleLocaleProbe`（`analyze` / `shouldEmit` / `applyToResponse` / `armFromRequest`）；`WlsRuntime` 返回前写共享 `HeaderCollector`；`Response::prepareForEmission` 覆盖 FPC `toHttpString` 路径。
- 头：`X-Weline-Title-Locale` / `X-Weline-Title-Locale-Mismatch`；mismatch 时 `MemDiag::event`。
- 测：`TitleLocaleProbeTest` 7 tests / 22 assertions；交叉脚本追加 `__title_locale=1` 读头，mismatch 硬失败 `title_locale_probe_mismatch`。
- 范围：仅埋点，不改 FPC 键 / Memory 服务 / title 根因。
- 注：armed 走 `RequestContext`（Fiber 安全）；pending 必须写 `HeaderCollector::getInstance()`（Request Response 为 detached）。
- 版本：`2.5.126` → `2.5.128`（本波埋点）。

## 2026-09-21 — 进程 L1 需求产品验收 UC-1…UC-6（2.5.126）

- 增：`ProcessL1PressureEvictionAcceptanceTest`（规格主用例全覆盖）。
- 证据：同波 46 tests / 206 assertions（Acceptance + Store/OM/Phrase + Guard + Adapter shared-L1）。
- 汇审：验收通过（W1+W2）；W3/W4 仍非本范围。
- 版本：`2.5.125` → `2.5.126`。

## 2026-09-21 — 观测头瘦身产品闭环（2.5.125）

- 补闸：`X-WLS-Redirect-Cookies`；契约 UT `ResponseObservabilityLeanContractTest` 锁接线。
- 汇审：产品验收证书，**无遗留项**。
- 版本：`2.5.124` → `2.5.125`。

## 2026-09-21 — 观测头闭环：Process-Time/Server-Timing 同闸（2.5.124）

- 增：`processTimingHeadersEnabled()`（同 `performanceBreakdown`）。
- 闭环：上轮 P2 的 Server-Timing 收进闸门；汇审**无遗留**；Cookie 清场属会话契约，不在本需求。
- 证据：UT + 本机 MISS/HIT 无 Process-Time/Server-Timing。
- 版本：`2.5.123` → `2.5.124`。

## 2026-09-21 — Phrase 进程袋迁 ProcessMemoryStore（2.5.123 · W2）

- 改：`DictionaryCacheNamespace` 内部委托 `ProcessMemoryStore`（`bucket=locale`）；重语种驻留默认 ≤4；事务 `fingerprint=null` 仍 ephemeral。
- 增：`PhraseProcessMemoryReclaimable`（挂 `ProcessMemoryReclaimableAdapter`，淘汰后回扫袋键）；`Parser::clearWorkerCaches` 清 Store。
- 证据：`DictionaryCacheNamespaceProcessStoreMigrationTest` + 既有 Phrase UT。
- 版本：`2.5.122` → `2.5.123`。

## 2026-09-21 — 响应观测头闸门瘦身（2.5.122）

- 增：`ResponseObservabilityPolicy`（identity / performanceBreakdown / dynamicObservability / poweredBy）。
- 改：`worker.php` 身份头、`WlsRuntime` Performance/First-Render 分流、`FullPageCacheCoordinator` HIT 调试头、`Response` X-Powered-By；保留 `X-Weline-FPC` / `X-WLS-FPC-Status` / `X-Weline-Request-Id`。
- env.sample：生产默认关；本机 `env.php` 显式 false 验收瘦身。
- 证据：`ResponseObservabilityPolicyTest` + 汇审。
- 版本：`2.5.121` → `2.5.122`。

## 2026-09-21 — 进程 L1：ProcessMemoryStore + Policy 压力淘汰（2.5.121 · W1）

- 增：`ProcessMemoryStore` / `ProcessMemoryEvictionPolicy` / `ProcessMemoryReclaimableAdapter` / `MemoryPressureAwareInterface`。
- 改：`ObjectManager::relieveMemoryPressure` soft 半淘汰、hard 全清（禁 soft 盲清）；`WlsMemoryAdapter` 实现 PressureAware。
- 方案：`doc/开发/team/process-l1-pressure-eviction/` + `doc/开发/spec/process-l1-pressure-eviction.md`（首发 W1+W2）。
- 证据：ProcessMemoryStore + OM compaction UT PASS。
- 版本：`2.5.120` → `2.5.121`。

## 2026-09-21 — 公开文案槽 + HTML 缓存入库闸（2.5.120）

- 不变量：公开 title 槽禁模块码；FPC/hook/reusable 共用 `HtmlCacheAdmission`（拒存毒化 HTML，不 scrub 入库）。
- 增：`EmbeddedPageTitle::sanitizePublicSlots` / `htmlAdmissible`；`HtmlCacheAdmission`；Template 写入/extract 清洗；layout_params 同闸。
- 撤：`Response::setBody` scrub、about 专用 FPC 正则、about H1 核化写死；Theme/hanfu about 恢复清洗后 `$title`。
- 证据：UT 11/48 PASS；about 并发 0×`Weline_Theme`；locale_cross theme 泄漏清零；汇审见 `doc/开发/team/public-text-cache-admission/meetings/汇审.md`。
- 版本：`2.5.119` → `2.5.120`。

## 2026-09-21 — FPC 读写闭环复诊 + e2e F 档（2.5.119）

- 复诊：FAQ 写路径缺 `w_changed`；`/currency` Extra 未挂货币保存 bump 的 `price` ns。
- 增：闭环探针 + e2e `F-invalidation-closed-loop`；契约 UT 纳入 FaqItemChangedType。
- 版本：`2.5.118` → `2.5.119`。

## 2026-09-21 — A 档缺口：Theme 壳 + Currency Extra（2.5.118）

- 正式审查：`/about` `/sitemap` `/guide` `/qa` `/activity` `/policy` `/currency` 此前 Extra NO_MATCH。
- Policy Extra 扩壳路径；Currency Index 声明 `/currency`；契约 UT 同步。
- 侧车重收集；汇审补「已做 vs 剩余」。
- 版本：`2.5.117` → `2.5.118`。

## 2026-09-21 — A 档店面 Extra FPC 批量声明（2.5.117）

- 增：政策/FAQ/博客/指南等 `@Extra type=fpc`；`TierAStorefrontExtraFpcContractTest`；e2e E 档内容页探活。
- CMS/Blog 写路径 bump 父级 ns（`website/*/cms`、`global/storefront/blog`）对齐 Extra 读指纹。
- Blog：`BlogPostChangedType` / `BlogCategoryChangedType`。
- 侧车：`generated/framework/controller_extra.php` 已重收集。
- 版本：`2.5.116` → `2.5.117`。

## 2026-09-20 — Worker 会话仓：慢成功不得冷却 + 健康信号（2.5.116）

- 问题：主题编辑器等后台 BinQuery 报 `Invalid worker session token`；握手 CAS 写入 `wls_memory` 若 >75ms 成功也会 `markRemoteSlow`，随后同进程 `get` 在冷却窗直接 null，会话快照被当成空仓。
- 修：`WlsMemoryAdapter::remoteCall` 仅对「慢且 null」开冷却；实现 `CacheAdapterHealthInterface` + `recoverRemoteProbe`；`LockedCacheFrontendWorkerStateStore` 在 get 后不可用时 503，禁止空快照覆盖。
- 契约：`WlsMemoryAdapterTest`（慢成功不冷却 / recover / 慢 null 仍冷却）。
- 版本：`2.5.115` → `2.5.116`。

## 2026-09-20 — Taglib 编译期静态镜像契约（2.5.115）

- 增：`Taglib/StaticMirrorCapableInterface`、`Taglib/CompileTimeStaticMirror`（字面量探测）。
- 文档：`View/doc/Taglib/编译管道.md`；规格 `doc/开发/spec/taglib-compile-time-static-mirror.md`。
- 契约：`CompileTimeStaticMirrorTest`（3 PASS）。
- 版本：`2.5.114` → `2.5.115`。

## 2026-09-20 — URL 最终输出钩子 url_generate_params（2.5.114）

- 纠偏：业务身份 query **禁止**挂 `url_generate_rewrite`（会干扰 Seo，且 seo=off / 不走重写的 URL 带不上参）。
- `extractedUrl`：rewrite 恢复仅 `seo=on`；末尾**始终**派发 `url_generate_params`（最终吐出前追加 query）。
- 文档：`doc/event/url/URL生成参数.md`。
- 契约：`UrlFrontendBatchTest`。
- 版本：`2.5.113` → `2.5.114`。

## 2026-09-20 — URL 生成 rewrite 始终派发（2.5.113）

- 变更：`Url::extractedUrl` 不再用 `Env::seo` 挡住 `url_generate_rewrite`；SEO path 重写由 UrlManager 观察者自早退。
- 用途：Theme 画布可在 seo=off 时仍注入 `theme_id`。
- 契约：`UrlFrontendBatchTest` 更新 seo=off 仍派发 rewrite、不改 path。
- 版本：`2.5.112` → `2.5.113`。

## 2026-09-20 — Phrase 预取词条只读查询 API（2.5.112）

- 用途：后台菜单跨语搜索等场景在 `prefetchWords` 后按 locale+word 读 Worker/请求缓存，避免业务侧 `include generated/language/*.php`。
- 增：`Parser::getPrefetchedGlobalWord`（仅缓存，不打 DB）。
- 版本：`2.5.111` → `2.5.112`。

## 2026-09-20 — request-fence 禁共享落盘 / 缓存键剥离请求 ID（2.5.111）

- 问题：未冻结 Storefront 上下文时，HotCache/KeyBuilder 把 `requestId+nonce` 指纹编进 `request_fence` / `cache_version`，每请求唯一键写入 L1/L2，等价永久 miss 并在 WLS Memory 堆积。
- 修：`KeyBuilder::policyAllowsSharedCache`；fence 维去掉请求唯一 fingerprint；`StorefrontScopeHotCache::rememberPolicy`/`remember(website)` 改走 `rememberForRequest`。
- 契约：`CachePolicyTest`（含 fence 零共享写）、`StorefrontCacheKeyContextTest`；定向 41 tests / 184 assertions PASS。
- 版本：`2.5.110` → `2.5.111`。

## 2026-09-20 — setup:upgrade 轻词典 + Context from_setup（2.5.110）

- 问题：CLI `s:up` 中 `__()` 经 Phrase 全量拉取 `w_i18n_locale_dictionary`，与 Theme 重迁移叠加表现为卡死（`zend_array_dup` CPU 打满）。
- 修：`Parser::setSetupUpgradeLightDictionary` 仅在 `setup:upgrade` 入口开关，并入 `shouldSkipHeavyLocaleDictionaryLoad`；CLI `loadLocaleWords` / `loadGlobalDictionary*` 在 hydrate **之前**短路；`Setup\Data\Context::getFromSetupVersion()` + Handle 注入 `from_setup`。
- 契约：`ParserSetupUpgradeLightDictionaryTest`、`SetupContextFromSetupVersionTest`。
- 版本：`2.5.109` → `2.5.110`。

## 2026-09-20 — 后台默认语言与网站前台默认语言分开

- 无语言前缀的后台请求改用 `backend_default_language`（env 扁平键），不再回落网站 `default_language`。
- 有效默认再叠一层：管理员个人语言（`BackendUserConfig`）> 全局 `backend_default_language`。
- `Url::getBackendUrl` 省略语言段时对比有效后台默认语；前台 `getPrefix()` 仍对比网站默认语。
- 契约：`BackendPersonalLanguagePriorityTest`、`BackendUrlLocalePrefixContractTest`。

## 2026-09-19 — 表单运行时改走 DOM 观察总线（2.5.109）

- 问题：`FormRenderer::runtimeBootstrap` 用裸 `MutationObserver` + 双 rAF + 每次 `mountAll(document)`。这是被否定的合并做法，密级 hydrate 时仍会把整页观察打成反馈环。
- 修：优先 `Weline.dom.observe`，只挂新出现的 `form[data-weline-form]`；无总线时安静窗 100ms 再观察，禁止双 rAF。
- 验：`FormRendererTest::testRuntimeBootstrapCoalescesMutationObserver`。
- 版本：`2.5.108` → `2.5.109`。

## 2026-09-18 — 静态错误页指纹 404v6 + 发布上下文旗标（2.5.108）

- `storefront404InputHash` → `404v6|…`（配套 Theme 扣客户端词典）；`StaticErrorPagePublisher::CTX_PUBLISHING` 供 Hook 跳过多语词表注入。
- 契约：`UpgradeSpeedSkipContractTest` 断言 `404v6|`。
- 版本：`2.5.107` → `2.5.108`。

## 2026-09-18 — orphan 前强制 flush 控制器 ACL（2.5.107）

- 问题：`setup:upgrade -m Weline_Theme` 在 defer ACL 未落盘时就跑 `after_route_collection` orphan；方法级 `theme_visual_editor_scope_*` 不在 live set → 被删，超管仍 `backend_acl_denied`。
- 修：路由扫描后、orphan 前先 `flushDeferredControllerAttributes`；委托 `--route` 路径同样处理。
- 另：`RouteUpdateStage::commit` 不再提前 `clear` registry（`--route` 曾在 flush 后清空 live set，orphan 把刚写入的方法级 ACL 删光）。
- 验：`UpgradeSpeedSkipContractTest` / `RouteUpdateBatchDeferContractTest`。
- 版本：`2.5.106` → `2.5.107`。

## 2026-09-18 — BinQuery ACL 拒绝映射为 backend_acl_denied（2.5.106）

- 问题：`FrontendWorkerBackendAuthorizationException` 落入通用 `\Throwable`，DEV 变成带堆栈的 `business_error`，生产抹掉 debug 后客户端无法识别。
- 修：`BinQuery` / `QueryBin` 在通用 Throwable 前 catch，返回 403 + `backend_acl_denied` + 人话 message。
- 验：`QueryBinBackendAclDeniedMappingTest` 3/3。
- 版本：`2.5.105` → `2.5.106`。

## 2026-09-18 — 语/币允许表与站默认值进程级共享（2.5.105）

- 问题：`State` 语/币允许表是单槽进程缓存，多站 Fiber 会互相覆盖；`getLang`/`getCurrency` 在 URL 已解析后仍反复拆路径。
- 修：允许表与站点默认语/币按 `website_id`（或 code）进程 map 共享；`url_parsed` 后优先读 `route.language`/`route.currency`。网站变更走 `clearProcessLocalizationCaches`。
- 版本：`2.5.104` → `2.5.105`。

## 2026-09-18 — 请求进行中不再回写进程 $_SERVER（2.5.104）

- 问题：`RequestContext`、`Url`、`WlsRuntime` 在请求处理中仍读写进程级 `$_SERVER`，Fiber 切换后会看到另一个请求的 URI/语言/站点。
- 修：这些读写改走 `WelineEnv`。WLS（persistent）不再把结果写回 `$_SERVER`；FPM 仍同步进程表。入口 `GlobalsEmulator` / `syncFromServer` 抄写保留。
- 版本：`2.5.103` → `2.5.104`。

## 2026-09-18 — 请求热路径超全局读取改走上下文（2.5.103）

- 问题：`Template` / `State` / `Url` / 错误页 / CSP / HeaderCollector / KeyBuilder 等在已有 Fiber Context 时仍直接读 `$_GET` / `$_SERVER` / `$_COOKIE`。
- 修：这些读取改为 `WelineEnv::server` / `getGet` / `getCookie`。进程超全局只留在 `WelineEnv` 无 Context 时的回退，以及 `GlobalsEmulator` / `WlsRuntime` 入口抄写。
- 版本：`2.5.102` → `2.5.103`。

## 2026-09-18 — 请求期残留进程级 ob 迁 FiberOutputBuffer（2.5.102）

- 问题：`ErrorPageRenderer` / `BinQuery`/`QueryBin` 整栈 `ob_get_clean` / `PcController::fetchJson` 的 `ob_clean` 在 WLS 下会拆 Fiber handler 或串缓冲；`ClassGenerator` 生成物仍写原生 ob。
- 修：错误页 include 捕获改 `FiberOutputBuffer`；抽出 `Http/BinaryOutputGuard`（persistent 只 fiber 捕获且不 `ini_set`）；`fetchJson` persistent 用 `resetCurrent`；`ClassGenerator::render` 生成 Fiber 捕获。
- 验：契约+Fiber 单测 24（2 skip）；产品页 dropdown ~100KB+；query-bin 二进制 400（无 HTML 污染）。
- 版本：`2.5.101` → `2.5.102`。

## 2026-09-18 — w:form / Taglib 子缓冲迁离进程级 ob_start（2.5.101）

- 问题：高并发 WLS 下原生 `ob_start` 与 Fiber 交错会串缓冲/丢 HTML；`<w:form>`（含 `@url` 动态属性走 AST `CodeGenerator::generateRuntimeTag`）与 legacy children buffer 仍是进程级 ob。
- 修：`CodeGenerator` / `buildChildrenBufferExpression` / form compile callback 一律 `FiberOutputBuffer`；`COMPILER_GENERATION=v2` 写入模板 hash 强制重编译。
- 验：`FiberOutputBuffer`+`FormFiberCapture` 12/12；`com_header-bar` 已发 `beginCapture`；产品页 dropdown ~80–100KB；20 并发 curl dropdown 全过。
- 版本：`2.5.100` → `2.5.101`。

## 2026-09-18 — FiberOutputBuffer 在原生 ob 下嵌套捕获不再丢 HTML（2.5.100）

- 根因：`<w:form>` 编译为原生 `ob_start`；其内 `Template::fetch` 仍推 Fiber 逻辑帧，echo 进表单缓冲，`endCapture` 恒返回 `''`（timing 见 `type-dropdown` bytes=0）；依赖返回值或异常路径会空 HTML。
- 修：handler 上方有原生缓冲时改走 nested native capture；用 fiber-local mode/baseline 栈保证 begin/end 配对（避免误 pop 祖先 baseline 把整段 header 吞进子模板）。
- 版本：`2.5.99` → `2.5.100`。

## 2026-09-18 — getPrefix 只按网站默认抑制货币/语言（2.5.99）

- 问题：`Url::getPrefix` 同时对比框架默认 CNY，导致 USD 站上 CNY 路径被省略，且 `website.currency` 空时误拼 `/USD/`。
- 修：网站默认取 `State::resolveWebsiteDefaultCurrency/Language`；仅网站默认省略段（对齐 `LocalizedUrlBuilder`）。
- 版本：`2.5.98` → `2.5.99`。

## 2026-09-17 — 绝对 URL redirect 空 params 不再尾随 &（2.5.97）

- 问题：`redirect(https://...?node=…)` 且 `$params=[]` 时拼出 `...?node=…&`。
- 修：`PcController::redirect` 仅在 query 非空时追加。
- 版本：`2.5.96` → `2.5.97`。

## 2026-09-17 — getBackendUrl `*` 空展开防 404（2.5.96）

- 问题：`*/admin/...` 在 Request router 为空时变成 `/admin/...`（缺模块 frontName），再叠加 locale 成 `/{lang}/admin/...` → 404。
- 修：`expandModuleStarPath` 回退模块 `etc/env.php` router / 模块名末段；`getBackendUrl` 改走该助手。
- 版本：`2.5.95` → `2.5.96`。

## 2026-09-17 — AbstractModel UPDATE 回写禁止整表覆盖（2.5.95）

- 问题：`checkUpdateOrInsert` 更新后 `setData($changedOnly)` 整表替换 `_model_fields_data`，未变更字段（如 Website `code`）从内存消失；默认站保存首页路由时抛「网站身份无效」。
- 修：按字段 `setData($field, $value)` 合并回写。
- 版本：`2.5.94` → `2.5.95`。

## 2026-09-17 — 客户登录独立 Session Cookie（WELINE_CUSTOMER_SESSID）

- 问题：前后台共用 `WELINE_SESSID`，主题预览与真实客户登录交织导致反复掉线。
- 变更：`SessionCookieNameResolver::CUSTOMER_NAME`；`SessionFactory` 按 Cookie 族拆实例；Wls/Fpm 同族 Expire；`FrontendSessionCookieMigrator`；预览普通 `login()`；sibling rebind 置空；FPC 只扫客户族。版本 `2.5.94`。
- 验收：`SessionCookieNameResolverTest::testCustomerAreaUsesIsolatedCookieFamily`；`SessionFactoryTest::testFrontendAndBackendSessionsUseDistinctUnderlyingSessions`。

## 2026-09-17 — trustedReuse 登记失败不清除现有登录（2.5.93）

- 场景：主题预览 `trustedReuse` 登录在设备登记失败时仍 `clearAuthenticationState`，把同 Cookie 上已登录的店面顾客清掉（「掉线」）。
- 修：仅非 `trusted_reuse` 路径在 register 失败时清登录态。
- 版本：`2.5.92` → `2.5.93`。

## 2026-09-17 — Hook getFileListWithMeta 合并残缺注册表（2.5.92）

- 现象：店面 `#customer-service-widget` 未注入；页头/页脚「联系客服」有，全局底部浮层无。
- 根因：`Template::getHook` 优先 `getFileListWithMeta()`；该方法在 `generated/hooks.php` 非空但不完整时不合并磁盘实现，丢掉 `Weline_CustomerService` 等 body-end Hook（`getFileList()` 已有合并）。
- 修：`getFileListWithMeta` 与 `getFileList` 同走 `mergeRegistryWithFilesystem`；`hook:rebuild` 补全注册表。
- 契约：`HookReaderIsolationGuardTest::testGetFileListWithMetaMergesFilesystemWhenRegistryNonEmpty`。
- 版本：`2.5.91` → `2.5.92`。

## 2026-09-17 — CSP meta 禁止污染 JSON 正文（2.5.91）

- 现象：`shipping/frontend/region/list` 响应为 `text/html` + CSP `<meta>` + JSON，前端 `response.json()` 失败。
- 修：`SecurityHeaderPolicyService::ensureDocumentCspMeta` 跳过 `{`/`[` 正文；`FullPageCacheCoordinator::publishResponse` 仅对 HTML Content-Type 注入 meta。
- 契约：`testEnsureDocumentCspMetaSkipsJsonBodies`、`testJsonBodyIsNotRewrittenByDocumentCspMetaEvenWithHtmlContentType`。

## 2026-09-17 — 认证 trustedReuse：可复用会话 id 登录（2.5.90）

- 场景：主题可视化 iframe 与后台共享 `WELINE_SESSID`；前台 `login()` 默认 `regenerate` 会毁掉后台登录且 Cookie 仍指旧 id。
- 修：`AuthenticatedLoginContext::SOURCE_TRUSTED_REUSE` / `trustedReuse()`；`AuthenticatedSession::login` 对该 source 跳过轮转与 revoke-previous。
- 版本：`2.5.89` → `2.5.90`。

## 2026-09-15 — 404 静态页指纹 404v5（brand token）

- `StaticErrorPagePublishFingerprint::storefront404InputHash` → `404v5|…|brandToken`，与 Theme 404 生成器范围 brand 修复配套，避免旧 HTML 因主题未变而跳过重发。

## 2026-09-15 — WebUI 门禁 A2：Playwright 仅正式 runner

- `WebUI浏览器验收与交付地址门禁.md` 门禁 A2 增补：禁止 `node -e` / `chromium.launch` 探活；须 `php bin/w e2e:run` / `npx playwright test`（MCP `e2e_playwright_formal_runner_only`）。

## 2026-09-15 — 站内 URL 生成硬规则对齐（2.5.88）

- `06-url标签使用指南.md` 补 PHP/Service 必须 `Url::getUrl` 一节；禁止 `'/' . $route`。

## 2026-03-24 — getBackendUrl 补齐非默认语言/货币路径段（2.5.87）

- 问题：前台 `getFrontendUrl` 会经 `getPrefix()` 写入非默认 locale，后台 `getBackendUrl` 没有；侧栏菜单/`@backend-url` 在切语后仍生成无语言段链接，点击回退网站默认语言。
- 修复：`getBackendUrl` / `getOriginBackendUrl` 与前台一致拼入 `getLocalePrefix()`（默认语/币省略）。
- 契约：`BackendUrlLocalePrefixContractTest`。
- 版本：`2.5.86` → `2.5.87`

## 2026-09-15 — setup:upgrade -f / --force-menu 强制菜单重收集（2.5.86）

- 问题：菜单跳过只看 `menu:*` 源指纹；`-f` 原先仅跳过环境检测；手删指纹若进程内 memoryStore 未失效仍可能跳过；prepare 阶段强制后中途 `MenuCollector` 会熄灭 sticky，真正收集时又假命中跳过。
- 修复：`-f/--force` 与 `--force-menu` 调用 `MenuXmlReader::requestForceFull`（清 `menu:*` + hold 粘性）；收集派发前 `assertForceMenuBeforeCollect` 再清指纹；`SetupSourceFingerprint` 磁盘仓缺失时清空内存；`forgetByPrefix`。
- 契约：`UpgradeSpeedSkipContractTest::testSkipRequiresDestinationArtifacts`。
- 版本：`2.5.85` → `2.5.86`

## 2026-09-15 — generated 注册表统一原子写、失败不删不写回

- 新增 `Registry/Service/GeneratedPhpArrayPublisher`（复用 `AtomicCompiledFilePublisher`）：temp+rename，禁止先删 live。
- 接入：Event / Hook / Plugin / Extends / Taglib / Widget / ParamSchema / Driver / `commands.php`（`command:upgrade` 去掉 `rm`）。
- 写回门禁：旧表有条目而新表为 0 则拒绝；refresh 失败保留旧文件。
- UT：`GeneratedPhpArrayPublisherTest`。

## 2026-09-15 — EventRegistry 未完成不写回 / 原子替换

- `refresh` / `refreshForModules`：仅内存组装，失败直接中止并保留磁盘上的 `generated/events.php`。
- `saveRegistry`：写前回门禁（缺 `events`、或新旧对比「旧有观察者而新表全空」则拒绝写回）；临时文件校验后 `rename` 替换，禁止先删后写。
- 增量 `collectObserversForModules` 失败改为抛错，避免清空模块切片后仍落盘。
- UT：`EventRegistryPersistGuardTest`。

## 2026-09-15 — EventRegistry 收集失败不再静默写空表

- `EventRegistry::collectObservers` 捕获后改为抛出 `RuntimeException`，禁止中途失败仍返回空 map 覆盖 `generated/events.php`（曾导致店面无 layout wrap、HTTP 200 空正文）。
- 触发链：`Weline_Tax` 非法 `failure=non_critical` → XmlReader 校验失败 → 空 registry。

## 2026-09-14 — FAQ/chrome i18n（2.5.85）

- ErrorPageRenderer 404 目录文案运行时翻译；`loadStorefrontNotFoundStaticHtml` 优先 `REQUEST_URI`。
- `StorefrontNotFoundStaticPage::resolveLang` 在路径已剥 locale 时回退请求内 `WELINE_USER_LANG`（仍忽略语言 Cookie）；重生九语静态 404。

## 2026-09-14 — FAQ/chrome i18n（2.5.84）

- ErrorPageRenderer 404 等目录文案运行时翻译，避免「404 · 中文」硬编码。

## 2026-09-14 — DateTime\Timezone 统一站点墙钟↔UTC（活动时间窗）

- 新增 `DateTime/Timezone.php`：`localInputToUtcSql` / `utcSqlToLocalInput` / `utcNowSql` / `isWithinUtcWindow` / `migrateNaiveLocalToUtcSql`。
- `ScheduleWindowUtcMigrator` 一次性把 Marketing/Theme/Promotion 无偏移窗按站点 TZ 改写 UTC。
- 硬约束：业务开始/结束判定禁止裸 `date()`/`strtotime()`。
- 验证：`Test/Unit/DateTime/TimezoneTest`。

## 2026-09-14 — OAuth 回调剥离店面语言/货币路径（2.5.83）

- `Url::withoutStorefrontLocalizationPrefix()`：从绝对/相对 URL 去掉 `/CURRENCY` 与 locale 路径段，供 Google 等精确匹配 `redirect_uri`。
- 验证：`UrlPrefixNormalizationTest::testWithoutStorefrontLocalizationPrefixStripsCurrencyAndLanguage`。

## 2026-09-14 — Phrase compile_after 下发源语言 collected_words（2.5.82）

- `i18n:collect` 只写 generated 时，I18n 无法把新源词登记进公共词典。
- `dictionary_compile_after` payload 增加 `source_translations` / `collected_words`（默认 locale + 源码扫描），Framework 仍不写 DB。
- 验证：`DictionaryDecouplingContractTest`。

## 2026-09-14 — Phrase 模块 CSV 热层不再预灌 en_US（2.5.81）

- 问题：非中文目标语（hi_IN/ar_SA）在 WLS 持久进程里，`loadModuleWordsForLocaleChain` 把 en_US 模块 CSV 合并进 module_words，先于目标 locale 全局词典命中 → 结账等页大量英文壳。
- 改法：热层只加载目标 locale 的模块 CSV；回退仍由 `LocaleFallbackChain` + `loadGlobalDictionaryWord` 负责。
- 验证：`ModuleCsvFallbackMustNotMaskTargetLocaleContractTest`、`LocaleFallbackChainTest`；店面 hi_IN checkout SSR 主标题改印地语。

## 2026-09-14 — CSP 响应头瘦身：meta 交付 + 去重 Report-Only（2.5.80）

- 问题：强制 CSP 与 Report-Only 各 ~3KB 且常完全相同，安全头合计 ~6KB，易触 nginx/proxy 响应头缓冲上限。
- 默认 `csp_delivery=meta`：策略写入 HTML `<meta http-equiv>`（文档一次），HTTP 头不再带大 CSP；`frame-ancestors` 仍靠 `X-Frame-Options`。
- Report-Only 默认空；与强制 CSP 相同时 header 模式也省略。
- `compactForWire`：已有 `https:` 时剔除被覆盖的 `https://…` 主机。
- 可缓存策略端点 `GET /api/framework/csp-policy`（ETag / 公开只读）；FPC `securityVariantMaterial` 含 CSP digest。
- 验证：相关 UT 21/21；Worker 首页响应头无 CSP、body 含 `data-weline-csp`；策略端点 200 + Cache-Control。

## 2026-09-14 — FPC br 收尾续跑（resume e3688d5c）

- 核对：FPC br 权威改动仍在（Framework `2.5.78` / Websites `1.8.25` / Server `2.0.53`）；UT `FullPageCacheCoordinatorTest`+`WorkerFullPageCacheFastPathTest` 32/32。
- 阻塞解除：工作树 `Weline_Shipping` 已为 `2.9.0`（高于原旁注 `2.8.1`）；`setup:upgrade` EXIT:0；`setup:schema:check --module=Weline_Shipping` 无漂移。
- 复验：Worker 直连首页 `Fpc-Hit=1 source=shared`，`Accept-Encoding: br` → `content-encoding: br` ~101KB 可解 ~1.09MB；gzip/identity 正常；外置 `.br` ~100–152KB。公网 9555 Nginx 入口可能改写编码，权威以 Worker 为准。
- WB：禁缓存打开 `https://p05113ef3.test.weline.com:9555/` 首页标题与主导航可见。
- 压测（`server:benchmark`，8 Worker，质量门禁均 PASS，压后仍 HIT br、9/9 运行）：
  - 公网 `https://p05113ef3.test.weline.com:9555/` `-c100 -n5000 --accept-encoding br,gzip` → HTTP/3、线体 gzip ~159KB、QPS≈924、错误率 0%。
  - 公网同入口 `-c200 -n10000 --accept-encoding auto`（CLI `memory_limit=1024M`）→ 协商 **br** 线体 ~101KB / 逻辑 ~1.09MB、QPS≈951、P95≈281ms、错误率 0%。
  - Worker 直连 `-p19655` `-c50 -n3000 --accept-encoding br,gzip` → **br** ~100KB、QPS≈751、P95≈70ms、错误率 0%。

## 2026-09-14 — FPC br 权威存储（2.5.78）

- 权威：有 brotli 时 Shared/Process 以 `fpc_br_b64` / 外置 `.br` 为存货；明文仅请求内瞬时解压，不回写。
- 无 brotli：继续明文权威。gzip 仍按量物化。大页外置只留指针，禁止 file+内联 br 双存。
- 命中装饰：WorkerScope / WlsRuntime telemetry 支持 br 全环。
- 验证：`FullPageCacheCoordinatorTest` 19/19；本机 2w 首页 HIT `content-encoding: br` / gzip / identity 均可解；新外置 `.br` ~100KB（旧 `.html` ~1MB）。

## 2026-09-13 — 路由收集降噪 + ACL defer

- 现象：`setup:upgrade` 路由阶段每模块刷「更新路由/已更新」，体感像「跑一个写一个」；全量路由文件本已在 `RouteUpdateStage::commit` 一次 `flushBatchRouters`，但每模块仍同步写 ACL。
- 变更：收集期启用 `Module\Helper\Data::enableDeferControllerAttributes`；commit 先 `flushDeferredControllerAttributes` 再写路由文件；`ControllerAttributes` 支持多模块一次派发按 module 分组；批量收集期每 20 模块一行进度。
- 验证：`RouteUpdateBatchDeferContractTest` 4/4；`--stage=route_update -m=…` 收集期不再每模块「已更新」。

## 2026-09-13 — CookieScope 重入循环 OOM

- 问题：`resolvePolicy` → `EventsManager::dispatch` →（观察者/`WelineEnv::getCookie`）→ `qualifyName` → 再 `resolvePolicy`，重入直至 `CookieScope.php:181` 内存耗尽；`reflection:compile` / Worker 均表现为 FATAL。
- 修：`CookieScope` 加 `$resolving` 重入守卫（重入返回 identity）+ 请求级策略缓存；`StateManager::reset` 清缓存；`reflection:compile` 抬升 CLI 内存至 2048M 并周期 GC。
- 验证：`CookieScopeTest`（含重入用例）5/5；`CookieScopeResolveTest` 8/8；`php -d memory_limit=256M bin/w reflection:compile` 118/118 完成、无 FATAL。

## 2026-09-13 — 结构缓存键排除语言/货币/request_id（文档）

- 规则：布局/Slot 等结构资源建键须走 `w_cache` Custom / `KeyBuilder::applyDimensionFlags` / `CachePolicy(vary:[])`，排除 lang、currency、request_id；呈现层（FPC/HTML）仍可按语言货币分键。
- 文档：`3-开发/缓存使用指南.md` 新增「结构缓存 vs 呈现缓存」；Theme `layout-slot-cache-keys.md`；`统一缓存范围与性能优化.md` 已发布布局键建议去掉 locale。
- 无运行时改动。

## 2026-09-13 — 请求链路只认 Weline 面板开/关

- 行为：打开面板签发签名 Cookie `w_weline_trace_panel` 后记录链路；关闭/折叠面板清除 Cookie 即停止。
- 删除：5 分钟租约 TTL、心跳续约、`wls.debug.request_trace_lease_ttl`；`isEnabled()` 不再认 `wls.debug.request_trace` / `wls_trace` / `X-Weline-Trace`。
- API：`POST /dev/tool/rest/v1/trace/panel` body `{enabled:true|false}`。
- 验证：`RequestLifecycleTraceTest` 开闭用例；e2e `Weline_DeveloperWorkspace-trace-lease*.spec.js`。

## 2026-09-13 — exception.log 指纹限流（防 AI/翻译刷屏）

- 问题：Ollama 宕机 / `AI_TRANSLATION_BUSY` / 无界 SELECT 等可预期失败经 `Framework\App\Exception` 构造即写 `exception.log`（含完整堆栈），一夜可至数十 GB。
- 修：`ExceptionLogThrottle` + `ExceptionFingerprint::generateFloodKey`（折叠 AI 包装前缀与 connect 目标）；`w_log_exception` 同指纹 5 分钟窗口内抑制，窗口末/每 100 次心跳写摘要。
- 验证：`ExceptionLogThrottleTest`。
- 版本：`2.5.76` → `2.5.77`

## 2026-09-12 — Stripe CSP 从 Defaults 下沉到 Payment Provider（2.5.75）

- `SecurityHeaderDefaults` / Env 镜像移除 `js.stripe.com` / `api.stripe.com` 占位。
- Stripe 域名由 `Weline_Payment` `StripeProvider::cspDirectives()` → `PaymentVendorsCsp` 贡献。
- 契约：`SecurityHeaderDefaultsContractTest` 断言 Defaults 不含 Stripe。
- 版本：`2.5.74` → `2.5.75`。

## 2026-09-12 — E2E 默认无头（避免 Agent 弹 Chromium）

- `e2e:run` 默认无头（`PLAYWRIGHT_HEADLESS=1`），不再自动追加 `--headed`；显式 `--headed`/`--ui` 仍可用。
- Playwright 配置默认 headless；MCP `e2e_playwright_headless_default`；门禁 A2 对齐。
- 验证：`E2eRunArgumentsTest`；后台「UI 测试」开关语义不变（开=headed，关=headless）。
- 版本：`2.5.73` → `2.5.74`。

## 2026-09-12 — CSP 业务域名下沉到模块 Extends（2.5.74）

- 基线瘦身：Defaults/Env/security-headers 仅保留自有面（当时暂留 Stripe 占位；2.5.75 已下沉到 Payment Provider）。
- 模块贡献：Visitor 分析、Product 视频嵌入、Social 平台、DataTable 公共 CDN（另见各模块 Extends）。
- 版本：`2.5.73` → `2.5.74`。

## 2026-09-12 — Provider 自报 CSP 三域同构（支付/社媒/人机）

- 文档：安全响应头策略明确 Payment `cspDirectives`→`PaymentVendorsCsp`、SocialLogin→`SocialLoginVendorsCsp`，与 Captcha 同构。
- 版本：`2.5.72` → `2.5.73`。

## 2026-09-12 — WebUI 门禁 A2：章通路 e2e + 计划组套件

- 对齐 MCP `plan_full_pathway_e2e_suite`：feature 每章完整功能通路 e2e；收口统一 `e2e-plan-suite`；禁止半截汇报与甩测。
- 文档：`doc/3-开发/WebUI浏览器验收与交付地址门禁.md` 门禁 A2。
- 版本：`2.5.71` → `2.5.72`。

## 2026-09-10 — 模板编译环境纳入 hooks.php（防页头 bake 菜单过期）

- 问题：`w:widget` 编译期渲染会固化嵌套 `<w:hook>`（如 `header-account-links`）；hooks 变更后源模板未变 → 顶栏账户下拉长期缺个人中心入口。
- 修：`TraitTemplate::viewEnvironmentCacheSuffix` / `Template::stableTemplateCompileDirectory` / `TemplateCacheManager::sourceCacheEnvironmentHash` 纳入 `generated/hooks.php` mtime+size。
- 契约：`TemplateCacheManagerEnvironmentKeyTest::testCompiledTemplateCacheVariesWhenHooksRegistryFileChanges`。
- 版本：`2.5.70` → `2.5.71`。

## 2026-09-10 — 开发态 CSP tooling 迁 Env

- 片段键：`security.headers.csp_developer_tooling`（Framework 默认空；本机写 `app/etc/env.php`）
- 删除 `SecurityHeaderDefaults::CSP_DEVELOPER_TOOLING`；Service 仅 DEV/DEBUG 读 Env 非空片段 union
- 测试：开/关/空片段三条路径

## 2026-09-10 — 开发态放行 Cursor CSP ingest（connect-src）

- （已纠偏）片段改 Env，不再用 Framework 常量硬编码 7277
- `SecurityHeaderPolicyService::resolveCurrentResponseHeaders` 在 DEV/DEBUG 时运行时 union；不进 Extends/LKG/生产基线

## 2026-09-10 — 人机 CSP 下沉到 Captcha Extends（2.5.69）

- Defaults 去掉 `recaptcha.*` / `turing.captcha.qcloud.com` / `captcha.tencentcloudapi.com`；由 `Weline_Captcha` Extends `CaptchaVendorsCsp` 提供。
- 版本：`2.5.68` → `2.5.69`。

## 2026-09-10 — CSP 仅 Extends，删除 provides 前缀残留

- 删除 `CspSourceContributionProviderInterface::CAPABILITY_PREFIX`（`csp_source_contribution.*`）。
- 文档明确：**禁止** `etc/module.php` provides；唯一入口 `extends/module/Weline_Framework/Security/Csp/`。

## 2026-09-10 — CSP 改 Extends 应用默认 + 配置提示不可覆盖（2.5.68 / SystemConfig 1.3.47）

- 模块贡献改走 `extends/module/Weline_Framework/Security/Csp`；`CspSourceContributionRegistry` 读 ExtendsData。
- 应用默认 floor：保存拒绝删除（`security_policy_app_defaults_immutable`）；运行时 intersect 后强制 union 回。
- 配置中心：静态 hint + `SystemConfigCenterService` 动态列出当前应用默认。
- 文档：`安全响应头策略.md`、`extends.php`。
- 版本：Framework `2.5.67`→`2.5.68`；SystemConfig `1.3.46`→`1.3.47`。

## 2026-09-10 — CSP 模块可配置 + 大厂默认允许（2.5.67）

- `SecurityHeaderDefaults`：默认放行社媒/支付/主流 CDN（Facebook、YouTube、X/Twitter、LinkedIn、Instagram、TikTok、微信、Stripe、PayPal、jsDelivr 等）；`img/font/media` 允许 `https:`。
- ~~扩展点：`csp_source_contribution.<Module>` provides~~ → **已废弃**，见上条改为 Extends `Security/Csp`。
- 对齐：`Env.php` / `env.sample.php` / `env.php` / `security-headers.phtml` / `安全响应头策略.md`。
- 版本：`2.5.66` → `2.5.67`。

## 2026-09-10 — toast 多行正文可读（2.5.65）

- `process_message` 对正文 `nl2br`；`htmlToPlainText` 保留换行（不再压成一行）。
- 版本：`2.5.64` → `2.5.65`。

## 2026-09-10 — 配置 cache_ops 闭环说明（文档）

- `缓存使用指南.md`：四类绑定 + 预 bump 指纹键进 `cache_ops`（SystemConfig 1.3.43 实现）。
- Framework 代码未改版本号。

## 2026-09-10 — Phrase L1：无 RequestContext 按进程缓存（2.5.64）

- 问题：`DictionaryCacheNamespace::fingerprint()` 在无 request id 时直接 `null`，`localCache()` 每次返回新空数组；CLI cron（如 `i18n_ai_translation`）每次 `__()` 都 `opcache_invalidate`+编译 `generated/language/*.php`，高 CPU。
- 设计修正：仅**活跃 DB 事务**禁止发布 L1；无 RequestContext 时用 `process|{generation}` 进程级指纹，L1 写入同一底层数组。
- 测试：`DictionaryCacheNamespaceProcessCacheContractTest`；`ParserTranslateWordFromLayersNullCacheTest` 改为事务 ephemeral 场景。
- 版本：`2.5.63` → `2.5.64`。

## 2026-09-10 — CSP 放行 QueryBin Worker（worker-src）（2.5.63）

- 基线 CSP 增加 `worker-src 'self' blob:`，避免 Blob Worker 被 script-src 拒绝。
- 对齐：`SecurityHeaderDefaults` / `Env.php` / `env.php` / `env.sample.php` / 配置模板 / 安全响应头文档。
- 版本：`2.5.62` → `2.5.63`。

## 2026-09-10 — 配置保存经 w_changed：cache_ops + namespaces 送达

- 变更：`impact.cache_ops` 可选契约；`CacheImpactObserver` afterCommit 删键；SLA=送达+防风暴（毫秒窗口可接受）；禁配置热重载/全量 clear 顶替。
- 测试：`ResourceChangeCacheOpsContractTest`。
- 文档：`resource_changed.md`、`缓存使用指南.md`。
- 版本：`2.5.61` → `2.5.62`。

## 2026-09-10 — 安全响应头非空安全默认（CSP/CSP-RO）

- 审图分流：`web_ui` / `ui_shot`（统一配置中心「安全响应头」CSP 默认值留空）。
- 线稿：字段列「默认值」空预览；保存值 textarea 空；继承勾选。
- 原型调整：Env Global + 配置模板写入后台可用 CSP；CORS 留空=禁止跨域。
- 变更：`SecurityHeaderDefaults`；`baselineFromEnv` 空串回退；`Env.php` / `env.sample.php` / `env.php` / `security-headers.phtml`；契约 + `CoreSecurityHeadersTest`。
- 文档：`3-开发/安全响应头策略.md`。
- 版本：`2.5.60` → `2.5.61`。
- 宿主原生编辑。

## 2026-09-09 — Phrase 空 fingerprint 读取加固（remove-widget 复发）

- 追加：`doTranslateWordFromLayers` + 外层 try/catch；`parse` 请求词缓存 `is_string`；`loadGlobalDictionaryWord` 经 localCache 判键。
- 本机已 `server:reload` 滚动重启 Worker。

## 2026-09-09 — Phrase translateWordFromLayers 空 fingerprint 不下标出 null

- 现象：主题编辑器移除失效部件 JSON 失败：`translateWordFromLayers() Return value must be of type string, null returned`。
- 根因：`fingerprint()` 为空时 `localCache` 返回临时空数组，读取却先对 raw worker cache `array_key_exists` 再下标 localCache。
- 修：`Parser::translateWordFromLayers` 仅经 `localCache` 读缓存并 `is_string` 守卫。
- 测试：`ParserTranslateWordFromLayersNullCacheTest` OK。

## 2026-09-09 — Pgsql 表达式索引不再阻断 setup:upgrade（2.5.60）

- 问题：完全安装/系统更新时 SchemaDiff 读回 `w_shipping_service_regions.uk_service_lane_region` 等含 COALESCE 表达式的唯一索引，硬抛「尚不支持的表达式、谓词或 INCLUDE 列」，全量升级中断。
- 修：`Pgsql\Connector::getTableIndexes` 对非框架 FULLTEXT 的表达式/谓词/INCLUDE 索引改为可读回（列位用 keydef 占位）；`SchemaDiffEngine` 对同名索引定义漂移改为 `DROP`+`ADD` 自愈到声明式普通列索引。
- 单测：`SchemaDiffEngineTest::testSameNameIndexDefinitionDriftEmitsDropThenAdd`
- 版本：`2.5.59` → `2.5.60`

## 2026-09-09 — Cache 池 hijack_exempt（购物车耐久）（2.5.59）

- 问题：WLS 常驻 file→`wls_memory` 劫持导致购物车侧车抖动「假写成功、几分钟空车」。
- 修：`CacheManager::resolveDriver` 尊重池级 `hijack_exempt`/`durable`；预定义 `cart` 池免劫持。
- 单测：`Cache/test/CacheManagerRoutingTest::testWlsModeKeepsDurableCartPoolOnFile`

## 2026-09-09 — RssFeedWriter 支持 item author（2.5.58）

- 变更：`RssFeedWriter` 可选输出 `<author>`；配合 Blog RSS / Geo E-E-A-T 作者传播。
- 版本：`2.5.57` → `2.5.58`。

## 2026-09-09 — Worker Atomic 仓不可用退避加强（2.5.57）

- 配合 Backend 首次访问 attestation：CAS 上限 16→24；Memory 不可用恢复后退避至约 80ms，降低冷重连立即再失败。
- 版本：`2.5.56` → `2.5.57`

## 2026-09-09 — module:remove 删目录 `rm` 参数顺序（2.5.56）

- 问题：`Handle::remove` 执行 `rm $path -rf`，BSD/GNU 均把路径当操作数、`-rf` 当文件名，目录删不掉。
- 修正：改为 `rm -rf -- escapeshellarg($path)`。
- 版本：`2.5.55` → `2.5.56`

## 2026-09-09 — Locked Worker 仓入口无条件 recover（2.5.55）

- Locked 路径原先仅在 `isAvailable=false` 时 recover；改为获锁后先 `recoverRemoteProbe` 再健康检查，与 Atomic 仓一致。
- 版本：`2.5.54` → `2.5.55`

## 2026-09-09 — 后台偶发 Shared worker session state is unavailable（2.5.54）

- 审图：客服人员页黑底英文 `backend_attestation_unavailable`，刷新即好。
- 根因：同请求其它缓存池 Memory 传输失败后，WlsMemory 把整请求+2s 标不可用；签发 Worker bootstrap 的 Atomic 仓直接 503。
- 修复：仓事务开始 `recoverRemoteProbe` 并在健康失败时再探测；DEV 错误页附中文「请稍后刷新」。Server `2.0.44` 提供 recover 入口。
- 版本：`2.5.53` → `2.5.54`

## 2026-09-09 — Form 运行时 MutationObserver delivery_storm（2.5.53）

- 问题：`FormRenderer::runtimeBootstrap` 整页 childList 观察在延迟部件加载时 250ms 内投递 >40，DEV 截断。
- 变更：回调立即 disconnect，双 rAF 合并一次 `mountAll(document)`。
- 版本：`2.5.52` → `2.5.53`

## 2026-09-13 — IPC 缓存清理保留挂起请求实例（2.5.76）

- 真实 REST PUT 在 ACL 前出现空 router；复现表明主事件循环调用 `ObjectManager::clearInstances()` 会删除正在等待 I/O 的 Fiber 请求实例，恢复后取到新 Request。
- 新增 `clearProcessInstances()` 仅清进程共享实例、原始实例及反射缓存；现有全量 `clearInstances()` 语义保持不变。Server 的 IPC 缓存清理使用前者，请求实例仍由正常请求结束流程释放。
- 回归：挂起请求跨进程缓存清理后，Request、origin 实例和 ACL 权限拒绝结果必须保留；权限判定不作降级。

## 2026-09-09 — 撤销 CachePool 代理层 processStore（2.5.52）

- 原因：多 Worker 下进程内袋与共享驱动**不对等**——本进程 `delete/clear` 清不掉其他 Worker 的 processStore，读会长期命中陈旧自身值、跳过驱动。
- 已还原 `CachePool.php` / `CachePoolTest.php` 至无 processStore；Adapter 仍各自负责（如 WlsMemory 自有 epoch 本地袋）。
- 版本：`2.5.51` → `2.5.52`

## 2026-09-09 — CachePool 代理层进程内存储（2.5.51）【已撤销】

- 曾落地 `processStore`；因 Worker 间不对等，见上条撤销。
- 版本：`2.5.50` → `2.5.51`

## 2026-09-09 — 取消业务侧三层硬规则；改为缓存类进程内+驱动设计口径（2.5.50）

- 撤销对 Ai `cache_lookup_tier_process_shared_db` 的对齐表述。
- `开发标准与验收` / `统一缓存范围与性能优化`：缓存能力归属 CachePool/Adapter（进程内→驱动、同 key 同步清理；单读命中自身不打驱动）；业务禁止再自做一层。运行时代码改造另开。
- 版本：`2.5.49` → `2.5.50`

## 2026-09-09 — 缓存查找层级写入开发标准与 MCP 硬规则对齐（2.5.48）【已修订】

- 曾与已取消的 MCP 硬规则对齐；以 2.5.50 设计口径为准。
- 版本：`2.5.47` → `2.5.48`

## 2026-09-09 — Worker Session 默认 cache（2.5.49）

- 默认 `wls.frontend_worker_session_store_driver=cache`：装配 `frontend_worker_credential` 池底层适配器。
- 有 `compareAndSet`（`wls_memory` / Redis）→ `AtomicCacheFrontendWorkerStateStore`；否则 `LockedCacheFrontendWorkerStateStore`（自建 flock）。
- Redis 适配器补 CAS；池 facade 暴露 `getAdapter`。
- 显式 `database` / `local` / `redis` 保留。单测默认 cache。

## 2026-09-09 — Worker SESSION 查询 L1→L2→DB（2.5.47）

- `FrontendWorkerSessionPayloadCache`：类属性进程缓存 → `frontend_worker_credential` 池 → DB。
- WLS 常驻下 file 池劫持为 `wls_memory`，同机其他 HTTP Worker 可读 L2。
- `DatabaseFrontendWorkerCredentialTransaction::find` 仅对 SESSION 走该读路径；insert/replace 回填，consume 失效。
- 权威仍 database；nonce 仍落库。单测 `FrontendWorkerSessionPayloadCacheTest`。

## 2026-09-09 — Frontend Worker Session 默认 database/pgsql（2.5.46）

- 未显式配置时：主库 `pgsql`/`mysql`/`mariadb` → `database`，否则 `local`；显式配置仍优先。
- `env.sample` 默认 `database` 并附带可替换的 local-dev keyring；本地 `env.php` 已切 database。
- `WlsRuntime::assertFrontendWorkerCredentialStoreReady` 与解析入口共用 `resolveConfiguredSessionStoreDriver`。
- 单测覆盖显式覆盖与 pgsql/sqlite 默认分支。

## 2026-09-08 — 读者 RSS 协议与静态路径豁免（2.5.45）

- 新增 `Xml\RssFeedWriter`（RSS 2.0 + CDATA 安全）；单测 `RssFeedWriterTest`。
- `weline_is_static_file_path` 豁免 `blog/rss.xml`、`blog/category/*/rss.xml`、`new-arrivals/rss.xml`，避免被当成磁盘静态资源 404。
- 版本：`2.5.44` → `2.5.45`。

## 2026-09-09 — Worker 未绑定会话容量回收对齐（2.5.44）

本地一人开发也会 toast「Worker session capacity is exhausted」：`MAX_ACTIVE_SESSIONS=4096` 按握手次数计，遗留未绑定会话曾写 `SESSION_TTL=7200`，而 Array 仓 `countRetained`/`deleteExpired` 只看原始 `expires_at`，未走 `effectiveSessionExpiresAt`（`UNBOUND_SESSION_TTL=600`），导致废弃握手占满后阻塞后台 QueryBin（客服配置 / SystemConfig embed）。现 Array SESSION 活跃判定与 `FrontendWorkerSessionService::effectiveSessionExpiresAt` 对齐；补单元测试。

## 2026-09-08 — Frontend Worker 无绑定会话 TTL 误用导致容量耗尽（客服开关）

- 问题：后台客服配置点击「启用 AI 自动回复」toast `Worker session capacity is exhausted.`；本地 `var/cache/frontend_worker/store.json` 会话数顶满 `MAX_ACTIVE_SESSIONS=4096`（约 4064 条无 Scope/Backend 绑定的前台会话）。
- 根因：`SESSION_TTL` 为配合后台 SSE 从 600 抬到 7200 后，无绑定握手也吃了 2 小时 TTL，会话堆积无法自然回收；容量断言曾先计数后 `deleteExpired`。
- 修复：拆分 `UNBOUND_SESSION_TTL=600`；后台仍用 `SESSION_TTL=7200`；创建前先清理再断言；`effectiveSessionExpiresAt` + Array store 对遗留无绑定行按 `created_at+600` 回收；容量错误改为中文「工作会话已达上限，请刷新页面后重试。」
- 验收：探针 `createSession` 成功且 TTL≈600；store 从 4096 回收到数十条；单元 `FrontendWorkerSessionServiceTest` / `StreamTest` TTL 契约。
- 审图：后台配置开关本身布局 pass；E 人性化 fail→pass（英文内部黑话改为可读下一步指引）。

## 2026-09-08 — 分类调用统一 URL 批量入口（2.5.43）

新增轻量批量准备事件，业务无需依赖 URL 重写模块；原构造逻辑提为共用函数，最终逐条事件和参数处理不变。新入口 3 项缺 API 的 RED 后通过，联合 UrlManager/挂载/分类用例 26 tests /174 assertions。旧 UrlPrefixNormalizationTest 原/新类独立对照均为 6 tests /34 assertions /4 failures，失败位置与内容一致，确认非本轮引入，不将其计入通过；运行证据见《统一缓存范围与性能优化》。

## 2026-09-08 — HotCache 既有 timing 增强（2.5.42）

- 真实同Worker从37DB升675DB的样本缺逐键证据，现为原5个phase补资源/键摘要/已算依赖指纹/L1L2状态/截止时间，正常缓存行为和phase名不变。不额外调用authority、cache或IPC，关闭trace不构造这些摘要。
- 新增两项行为RED后实现；联合HotCache/CachePolicy 21 tests /102 assertions通过，lint和精确diff检查通过。未修改RequestLifecycleTrace；其另跑旧suite有3个Reflection字段错误及2项旧fixture失败，本轮不宣称全部trace测试通过。
- phase汇总保留lastmeta，原始每次span和timing.trace_top有独立meta。WlsPerformanceTraceStore另有字段白名单/600条限制，未为本任务扩大采样或全局日志。

## 2026-09-08 — REST error() 同步 HTTP 状态码（2.5.41）

- 问题：`AbstractRestController::error/success/exception` 用 `Response::json(..., $code)` 后只 `getBody()`，Router 收到字符串后默认 HTTP 200，API 文档页出现「状态码 200 / 响应 Code 500」。
- 修复：返回 body 前把状态码与 Content-Type 写回 request-scoped Response；保持 string 返回兼容 `: string` action。
- 契约：`ControllerResponseHelperTest::testAbstractRestErrorSyncsHttpStatusOntoRequestResponse`。
- 版本：`2.5.40` → `2.5.41`。

## 2026-09-08 — 嵌套模板初始化的上下文复用（2.5.40）

- Template每次init仍执行，保持标题、URL、语言方向、嵌套模块字典及数据重绑。无状态TemplateRequestView/TemplateEnvView由现有Context携带，同次init的local复用State语言读取结果；新请求重新创建载体，动态Request/语言覆盖保持实时。
- 定向Refresh + FiberScoped 13 tests /53 assertions通过；7个notices来自既有测试按引用传临时Request。额外HookI18nContext在原/新代码均因缺WeShop_Notification模块失败，未修改该无关依赖。没有修改模板路径策略或缓存商品HTML。
- 配置环境合成32卡片，288次init保留：隔离CLI各3轮中位，原control134.272ms/init55.408ms，新126.104ms/init49.455ms；HTML均244810bytes，归一化动态UID/加载优先级后的hash一致。不是实际网络Worker收益；运行结果见《统一缓存范围与性能优化》。

## 2026-09-08 — 公共词典版本与页面缓存一致性（2.5.39）

- 新增 DictionaryCacheNamespace，公共词典 L1/L2 复用 global/i18n 权威版本；请求上下文只携带当前快照，事务及版本不可用时不向公共缓存发布。FPC 声明词典依赖，首页回执 v2 按当前 @clock 验证。CachePolicy 支持声明规范 global/... 依赖，供翻译后的范围展示数据统一使用。
- 本轮验收：100 tests / 629 assertions、读侧与批量缓存 73 / 277、发布与 Product 策略 14 / 100 分组通过（含重复事务用例，不将组数相加冒充唯一测试数）；反射/框架编译及两实例滚动重载通过。真实双进程读写回滚和 WLS 首页版本失效通过。默认新 Worker 首个 no-store 商品页 543.47ms，独立实例连续动态请求 382.44ms；70ms 冷启动仍未达标，详见《统一缓存范围与性能优化》。

## 2026-09-08 — 全局数据库词典模块独立共享（2.5.38）

- 原共享键包含当前Worker缺失模块组合，A先加载[A,B]后另一个Worker分别加载[A]和[B]仍重复查库。新增可选模块分组读取接口，I18n Provider仍一次source_module IN查询，Parser使用稳定语言/模块键MGET/MSET。
- 保留原phrase池清理、Worker清理广播、语言回退和模块优先级；v1是格式版本，不冒充数据库generation。以缺失模块首个真实键复用原remember/single-flight，持有执行权后重查共享结果；不造组合缓存、私有锁或新的等待策略。
- 查询失败不冻结外层语言/分层快照，公开翻译入口可以恢复；并发回填合并已有最新模块快照。行为及真实运行证据补充在《统一缓存范围与性能优化》，70ms目标未完成。
- 完整Phrase、namespace和Provider定向回归57 tests/228 assertions通过；配置数据库双进程359词严格一致，首次一次批量DB，另一进程不同模块增长顺序零DB。未新增组合事实缓存。
- 反射/框架编译和两实例重载通过，默认新Worker商品页/home均完整HTTP/2 200且FPC disabled，全局模块词典42/17次均零DB。整页TTFB1640.47/555.90ms，70ms未达；后续同Worker出现再次回源，转向审查I18n全量cacheClear的连带清理，不将局部通过误报为整体完成。

## 2026-09-08 — 全局缓存池批量与标签接口一致性（2.5.37）

- 真实新Worker的模块词典批量读全部在RPC前异常，原单元替身遗漏了CacheManager实际返回的NamespaceScopedCachePool包装层。空namespace生成空指纹，批量和标签方法却要求对它执行SHA-256装饰，导致回退CSV；单键路径原本正常。
- 统一包装层让空namespace的批量读、写、删和标签操作保持原始key/tag；非空namespace仍每批只解析一个指纹，保持范围隔离，不修改底层装饰器校验。
- 最小RED为8个空namespace操作异常，修复后新旧namespace行为17 tests / 58 assertions通过。运行验收在《统一缓存范围与性能优化》持续记录；此局部通过不能代替70ms目标。
- 真实CacheManager默认phrase包装器，两个独立进程共享203项只需一次MSET和一次MGET（7.24/4.47ms），严格值一致且零DB。真实Worker模块批读22次均无异常并实际触达WLS；相比此前RPC前全部失败，入口修复已生效。整页时延仍受动态渲染、缓存缺失和握手波动影响。

## 2026-09-08 — 模块词典共享快照批量读取（2.5.36）

- `Parser::getLayeredWords` 已知本轮模块和语言回退链，先对 L1 缺失的原有模块词典键批量读取共享池；批量未命中或传输失败直接走原 CSV 回退，避免再逐项 GET。保持模块优先级、空词典命中、文件版本键、Worker 清理及旧二参加载接口。
- 行为验证：2模块×2语言由4次单读变1次批量，新模块只补2键，L1重复读零增量；22 tests / 91 assertions、PHP lint及diff检查通过。已编译并重载；合并本轮EAV优化后的默认新Worker首商品页934.15ms，FPC no-store/disabled，HTTP/2 200且HTML完整。运行复核发现独立实例冷样本 module_cache_get 19次全部异常、合计0.36ms且零DB/WLS；这不是L1命中，而是空namespace包装器在RPC前拒绝空指纹，已列为统一层必须修复项。详见《统一缓存范围与性能优化》，70ms未达标。
- 当前默认开发模式会自动开启完整请求 trace，面板采样在采集后发生。本地8720 span微基准关闭约5.0ms、开启4096条并丢弃其余约15.5ms；不是整页873ms主因的证据，不擅自修改全局运行模式。当前无已核实的单实例DEV关闭入口。

## 2026-09-08 — DevTools no-cache 不再旁路公共 FPC serve（2.5.35）

- 问题：Chrome DevTools「Disable cache」对文档请求固定发送 `Cache-Control: no-cache`；此前仍强制 `fpc=disabled` 全量 SSR。debug `70285b` 文档旁路计数多为 `no-cache`，与软刷新 HIT 对照。
- 修复：`clientCacheControlRequestsBypass` 仅对 `no-store` 旁路 serve；有 Cache-Control 时忽略配套 Pragma。响应侧含 `no-cache` 仍不得 **发布** 到公共 FPC（既有发布门禁不变）。
- 验收：`testBrowserReloadMaxAgeZeroDoesNotBypassSharedFpcServe`；暖缓存后 `curl -H 'Cache-Control: no-cache'` 应 `x-weline-fpc: HIT`。用户确认修复后已拆除 session `70285b` 临时 NDJSON 埋点（Router/Core、FPC Coordinator、Index、worker_http_message）。
- 版本：`2.5.34` → `2.5.35`

## 2026-09-08 — 统一批量缓存与事件冷路径（2.5.32）

- `TraitTemplate::fetchTagSource` 保留 DEV 源文件检查，但相同编译路径不再逐 Hook 写回 WLS。31 个购买槽×2 Hook 的计数行为由暖路径 62 次写变为 0、冷路径 62 次写变为 2，路径变化仍更新；7 tests / 26 assertions 通过。

- `CachePool::getMultiple/setMultiple` 优先使用可选批量驱动；保留 buildKey/namespace、禁用缓存、旧适配器与统计语义。整批共用一次 TTL 抖动。
- `Parser::prefetchWords` 按每批 200 个实际词条先读 L2，只查询缺失词条并回填既有 found/translation 信封；跨 Worker 复用确认命中和缺词，保留覆盖词典与语言回退。
- `EventsManager` 普通未注册事件直接消费权威 Registry，不再回退全模块 XML/event.php 扫描；正负结果随 Registry 快照刷新失效，保留动态事件和显式扫描。最小复现三管理器×两未知事件 XML 读取 6→0。
- 定向验证：缓存 12 tests / 60 assertions，Phrase 19 tests / 73 assertions，Event 10 tests / 37 assertions。真实两个独立 PHP 进程经 CachePool 共享 203 项，各仅 1 次 MSET/MGET，零 DB，值一致，首次连接耗时 21.91/19.90ms；临时缓存 TTL 120 秒。
- 局部证据不代表整页 70ms 冷启动达标；整页测量记录在《统一缓存范围与性能优化》。

## 2026-09-08 — 访客 version API 匿名路径（2.5.31）

- `PublicApiAuthRouteMatcher::GUEST_FRONTEND_PATH_PATTERNS` 增加 `visitor/rest/v1/version`（path-only 兜底）。
- 场景：像素加密版本号供店面匿名拉取；路由未带 FQCN 时 controller 回退不可用会误 401。
- 契约：`PublicApiAuthRouteMatcherTest::testMatchesVisitorVersionCurrentAsGuestPathOnly`。
- 版本：`2.5.30` → `2.5.31`

## 2026-09-08 — 请求注入探针 fail-closed（2.5.27）

- `Url::looksLikeInjectionProbe()`：高置信 SQLi/XSS/路径遍历/伪协议检测，并扫描至多两次百分号解码变体。
- `Url::parser()` 在 UTF-8 校验后调用；命中抛 `NoRouterException(403)`，覆盖 FPM/非 WLS 入口。
- 权威拦截仍在 Server `WorkerPolicyKernel` attack_rules；本层为应用侧纵深。
- 版本：`2.5.26` → `2.5.27`

## 2026-09-08 — 并发缓存回填与已解析本地化复用（2.5.26）

- 修复 namespace 补齐等待期间被其他 Fiber 推进代次后，旧加载结果覆盖新进程快照的问题；同代次回填合并最新成员，保留其他 Fiber 已补齐的数据。
- `State::getLang/getCurrency` 优先读取已有冻结 `StorefrontCacheKeyContext`，不重复解析路径、允许列表和网站默认值。显式语言预览覆盖保留原有优先级及重置行为。
- 行为验证：两个 namespace 交错用例修复前失败，修复后 `NamespaceContractTest` 8 项、33 断言通过；冻结本地化复用用例修复前失败，修复后 `StateTest` 14 项、45 断言通过。
- 真实运行：反射与框架编译通过，default 四 Worker 滚动重载完成。2026-09-08 09:14 UTC 的首页、商品列表、PDP 均 HTTP 200，首字节分别 957.87 / 3559.94 / 1604.24ms；响应长度与 Content-Length 一致并含 footer、body/html 结束标签。不是清空所有 L1/L2 后的受控冷启动，70ms 未达标。
- 商品列表新 trace：URL 网站匹配 0.38ms，页头 895.61ms、目录构建 582.93ms、卡片渲染 447.92ms；促销筛选 532 DB spans、搜索类型 443 DB spans 仍待后续定向优化。父子阶段不可累加。
- 验收边界：浏览器导航与 DOM 诊断接口超时，未声明下半页视觉问题已解决；后续博客及重复请求遇到 HTTP 503 维护页，运行配置 system.maintenance=true，未擅自关闭。原始证据在 /tmp/weline-cache-remediation-http/；博客真实草稿提交、更新、回滚和条件删除已单独通过，测试草稿已清理。

## 2026-09-08 — 路由 URI UTF-8 fail-closed（2.5.25）

- 审图：店面 Runtime Error（`SQLSTATE[22021]` / `0xef 0xbc 0x5f`）判定为 `web_ui`；根因是非法 UTF-8 经 `rawurldecode` 后进入 Url 解析与重写查询。
- 变更：`Url::isValidUtf8Text()`；`Url::parser()` 在网站匹配 / `seo_decode` 前对 URI 做 UTF-8 校验，失败抛 `NoRouterException(400)`。
- 联防：`Weline_Server` WorkerPolicyKernel、`Weline_UrlManager` RouterRewrite 同步 fail-closed，避免静态缓存关闭时绕过边缘校验落入 PG。
- 版本：`2.5.24` → `2.5.25`

## 2026-09-08 — 上下文与范围缓存复用（2.5.24）


- 读取链：Context → 进程范围缓存 → WLS 共享 → DB；禁止业务平行造袋。
- `NamespaceGenerationSnapshot`：同 clock 缺失 namespace 先补进程快照，只批量加载剩余并回填。
- `State::resolveWebsiteDefaultLanguage/Currency`：优先 `WebsiteData`，避免先堆回退候选。
- 文档：`统一缓存范围与性能优化.md` 增补读取链说明。
- 版本：`2.5.23` → `2.5.24`

## 2026-09-08 — AbstractModel::load 支持 forceReload（2.5.23）

- 目标：配合 Websites 当前站上下文优先读取；普通 load 可跳过请求 identity map 强制查库。
- 变更：`AbstractModel::load($pk, $value = null, bool $forceReload = false)`；`hydrateFromLoadedRow()` 供上下文/缓存灌入。
- 版本：`2.5.22` → `2.5.23`

## 2026-09-08 — 公共词典增加已知词列表批量预取

- `Parser::prefetchWords()` 统一管理后台菜单等已知词列表的公共回退预取；可选批量 Provider 保持旧 Provider 兼容，按 200 词分批填已有 Worker 命中/缺词缓存，不新增共享缓存键。
- 行为验证覆盖命中/缺词、失败重试、模块/请求覆盖优先级和语言回退。真实数据库 200 菜单词由 200 条精确 SQL 降至 1 条，结果一致；预取后逐词零 SQL。整页性能仍需 WLS 浏览器验收。

## 2026-09-08 — 核心前台 i18n JS 模组归 Framework（2.5.22）

- 目标：核心 `i18n` JS（字典 / translate / switchLang / `Weline.i18n`）由框架提供；**不**重命名 `Phrase`（与小写 `i18n/` CSV 冲突）。
- 变更：`View/statics/js/i18n.js` + `View/statics/frontend/weline.modules.js` 登记；Theme declare 加载；`Weline_I18n` 仅增强。
- 版本：`2.5.21` → `2.5.22`

## 2026-09-08 — 中文注释与友好代码风格（2.5.21）

- 目标：与 Ai `chinese_comments_friendly_style`（1.2.20）对齐，开发标准写明注释语言与可读风格。
- 变更：`doc/3-开发/开发标准与验收.md` 增「注释用简体中文、代码风格要友好」条款。
- 版本：`2.5.20` → `2.5.21`。

## 2026-09-07 — 验收 Browser 打开即禁用缓存（2.5.20）

- 目标：与 Ai `browser_cache_disabled_on_open`（1.2.18）对齐，堵住默认磁盘缓存导致的静态资源误判。
- 变更：`doc/3-开发/WebUI浏览器验收与交付地址门禁.md` 门禁 A 增 WB-CACHE；收口顺序改为先禁用缓存再跑 WB-OP；纠正清单补缓存项。
- 版本：`2.5.19` → `2.5.20`。

## 2026-09-07 — 验收 Browser 汇报后必须关闭（2.5.19）

- 目标：与 Ai `browser_release_after_delivery`（1.2.17）对齐，堵住验收标签不关导致的空转卡顿。
- 变更：`doc/3-开发/WebUI浏览器验收与交付地址门禁.md` 新增门禁 D（WB-REL）；收口顺序改为写完「交付地址」后立即关闭 Browser。
- 版本：`2.5.18` → `2.5.19`。

## 2026-09-07 — 交付 Host 规则专节与纠正清单（2.5.18）

- 目标：本地 WebUI 门禁文档专节写清默认 Host 与 MCP 契约指针，与 Ai 1.2.16 对齐。
- 变更：`doc/3-开发/WebUI浏览器验收与交付地址门禁.md` 增加「本机默认 Host（硬）」表，示例改为 `{hash}.test.weline.com`。
- 版本：`2.5.17` → `2.5.18`。

## 2026-09-07 — 交付 Host 默认 {hash}.test.weline.com（2.5.17）

- 目标：WebUI 交付地址门禁明确默认 Host 为 `{project_hash}.test.weline.com`，禁止主验收使用 `*.weline.test`。
- 变更：`doc/3-开发/WebUI浏览器验收与交付地址门禁.md` 表格与纠正清单。
- 版本：`2.5.16` → `2.5.17`。

## 2026-09-06 — WQB1 list 上限 200→2000（2.5.16）

- 问题：`region.list`（尤其 `catalog=global` 全球国家）与 `region.children` 返回列表可超过 200，query-bin 编码抛 `List exceeds 200 item limit.` → 前端 500。
- 变更：`Binary\Limits::LIST_ITEMS` / 错误文案改为 2000，对齐 Shipping `children` max；协议文档同步。Frontend `weline-api-worker.js` 须一并升级。
- 版本：`2.5.15` → `2.5.16`。

## 2026-09-06 — Worker cache_clear 重载 Env 盘面（2.5.15）

- 问题：其它进程 bump `theme_static_version` 写盘后，WLS Worker 仍持旧 Env，清模板后重编译会再次 bake 旧 `?v=`；即使 `opcache_reset` 后直接 `include env.php` 仍可能吃到粘滞 OPcache 条目。
- 变更：新增 `Env::reloadPersistentConfigFromDisk()`（`opcache_invalidate` + 拷贝到临时文件再 include）；由 Server `cache_clear` 调用。
- 版本：`2.5.14` → `2.5.15`。

## 2026-09-06 — @static/css/js 回退编译期求值（2.5.14）

- 问题：把 `@static`/`<css>`/`<js>` 改成输出运行时 PHP 后，`@static` 嵌在 `$x='@static(...)'` 会 ParseError；`<js>`/`<css>` 转义还把 `Taglib.php` 本身弄挂。
- 变更：三者全部恢复编译期 `fetchTagSource` URL 字面量；主题发布靠 `ThemeRuntimeCacheCleaner` 清 `TemplateCacheManager`/taglib/view/FPC 刷新 `?v=`。
- 版本：`2.5.13` → `2.5.14`。

## 2026-09-06 — @static/<css>/<js> 运行时解析静态版本（2.5.13）

- 问题：内置 `@static`/`<css>`/`<js>` 在模板编译期把带 `?v=` 的 URL 写死进 `var/cache/template`，主题发布 bump `theme_static_version` 后仍吐旧戳。
- 变更：三者回调改为输出运行时 `fetchTagSource` PHP，版本号随请求解析。
- 版本：`2.5.12` → `2.5.13`。
- 更正：已由 2.5.14 整段回滚；发布清模板缓存为权威方案。

## 2026-09-06 — 店面缓存向量纳入 storefront/auth

- 变更：`StorefrontCacheKeyContextResolver` 增加 `global/storefront/auth` 与 `website/{code}/auth`，供社媒登录等认证壳配置经 `system_config` ResourceChange bump 后使登录页 FPC 代次失效。
- 版本：`2.5.11` → `2.5.12`

## 2026-09-05 — EAV 只读元数据与标签查询继续优化

- 已落地：公共readonly DTO树按entity/locale请求内复用；商品私有选项仅复制必要节点，自由集SQL按set/product限定；标签原SQL的scope/code结果ID/null统一请求缓存，商品校验与旧数据、语言兼容保留。
- 验证：定向46 tests/286 assertions、PHP lint、diff check通过；真实DB中英文30组旧/新输出一致。默认两Worker平滑重载12.5秒。
- 真实冷请求：英文3.163s、中文2.680s，81商品字段投影约219/212ms；正常入口1.293s，后续FPC HIT约34.85ms。数据与机器负载变动，不承诺稳定延迟。
- 未完成：Mac锁屏阻止Browser页面操作验收；header输出体积、计时边界外的FPC尾部及PHP候选集分页仍待后续优化。详细边界和证据见 Framework/doc/统一缓存范围与性能优化.md 最新节。
- 交付地址：[英文商品](https://p05113ef3.weline.test:9555/en_US/products)、[中文分类](https://p05113ef3.weline.test:9555/categories)、[排序分页](https://p05113ef3.weline.test:9555/en_US/products?sort=price_asc&page=2)、[属性筛选](https://p05113ef3.weline.test:9555/en_US/products?af_hanfu_chao_dai=Ming+Style)、[价格筛选](https://p05113ef3.weline.test:9555/en_US/products?price=0-99)。后台/API：N/A。

# Weline_Framework 开发日志

## 2026-09-08 — 核心前台 i18n JS 模组归 Framework（2.5.22）

- 目标：核心 `i18n` JS（字典 / translate / switchLang / `Weline.i18n`）由框架提供；不重命名 `Phrase`（与小写 `i18n/` CSV 冲突）。
- 变更：`View/statics/js/i18n.js` + `View/statics/frontend/weline.modules.js` 登记；Theme declare 加载；`Weline_I18n` 仅增强。
- 版本：`2.5.21` → `2.5.22`

## 2026-09-05 — i18n:collect 接通持久 Worker 翻译刷新

- 实际缺口：收集命令原先只调用当前进程的 `DictionaryCompiler::clearTranslationCaches()`，未通过运行控制接口通知常驻 Worker，英文目录因此曾在 collect 成功后仍显示中文数量。
- 最小修复：`Console/Console/I18n/Collect.php` 复用 `cache:clear` 的可选 `RuntimeControlBroadcasterInterface` 与 `RuntimeProviderResolver`；编译、本地清理后调用 `cacheClearAndWait(null, 12.0)`。仅完成结果报告清理成功，连接失败/未完成沿既有 warning 输出，收集结果保留；无 provider 时保持本地行为。不改 Parser、DictionaryCompiler、I18n Observer 或 Server，不添加重启、发布或新控制状态。
- 定向验证：新增 Collect 的完成、未完成、传输异常、编译失败四个用例，先 RED 后 GREEN。独立审查发现测试发现阶段的函数边界污染，真实只读复现后改为仅在隔离子进程 `setUp()` 注册；再次仅加载文件确认无函数泄漏。使用 `app/bootstrap.php` 引导，与既有 CacheClear、Product 数量、Shipping 指南合计 **14 tests / 36 assertions** 通过；PHP lint 与定向 diff check 通过。命令控制测试隔离文案/共享缓存边界，Product/Shipping 测试仍运行真实 `TranslationResolver`。
- 实际运行：`php bin/w i18n:collect Weline_Product` 的本次 `ctrl_op_29` 在其他任务 `ctrl_op_28` 重载期间排队，12 秒等待未完成时正确显示 warning；随后只读回执确认同一 `ctrl_op_29` 为 `cache_clear/completed/success=true`、**2/2 Worker applied=true**、无 failures。未主动重载或重启。先前一次沙箱调用因本机 IPC `Operation not permitted` 输出具体 warning，按正常权限流程重跑，不把该失败当完成。
- 边界：早先观察到的 `ctrl_op_27` 为独立操作，1/2 ACK、Worker 2 `cache_reset_failed`；本次 2/2 不关闭此前刷新稳定性及 HTTP/2 问题。实站中英文数量回读与 Browser 截图验收分别记录；内置浏览器可列页但选取编辑器超时，本轮没有新截图，不声称商城可上线。
- 宿主原生编辑。
- 交付地址：[英文商品目录](https://p05113ef3.weline.test:9555/en_US/products)、[中文商品目录](https://p05113ef3.weline.test:9555/products)。后台/API：N/A（本条无新增路由）。

## 2026-09-05 — 本机 Host 判定对齐 test.weline.com（2.5.11）

- 当前状态：`implemented`
- 变更：`RequestFilter` 本机框架 Host 识别支持 `*.test.weline.com`，并保留 `*.weline.test`。
- 版本：`2.5.10` → `2.5.11`

## 2026-09-05 — CLI 分页跳过 HTML 跳转表单（2.5.10）

- 问题：队列/CLI 调用 `AbstractModel::pagination()` 会渲染分页 `w:form`；空 host 时 action 变成 `http://?page=...`，触发「危险协议」并拖垮 I18n/Meta 等翻译任务。
- 修复：`cli`/`phpdbg` SAPI 下只保留查询分页状态，不渲染 HTML。
- 版本：`2.5.9` → `2.5.10`。

## 2026-09-06 — 统一 measurePhase 与完整 WLS 累计

- 需求：通过 timing 标签定位商品请求长尾，再决定优化。新增统一 `measurePhase`：finally 结束、返回/异常保持，同名次数/总耗时/最大值/错误数与阶段DB/WLS增量；原recordPhase语义保持。
- DB之外的WLS标量统计也独立于详细trace cap；复用32阶段限制、请求/Fiber隔离和现有timing.log出口，不创建第二logger。
- 所有阶段声明inclusive，明确不能父子相加或当作纯CPU/DB锁等待；现有控制面禁用规则不变。
- 最小回归先RED，正式应用与真实HTTP证据记录于 `/private/tmp/weline-timing/acceptance-report.md`。初始采样发现高负载和PG事务等待，但尚未建立页面因果关系，不据此改业务控制逻辑。
- 交付地址：https://p05113ef3.test.weline.com:9555/en_US/products?nocache=1&wls_trace=1 。本轮没有UI或新API。

## 2026-09-06 — 短语共享缓存 timing 细分

- 需求：真实商品页的主题插槽仍有未归因的 WLS 等待，需要区分短语层组合、词条解析以及模块词典共享缓存读写。
- 实现：在 `Phrase\Parser` 的既有调用点复用统一 `RequestLifecycleTrace::measurePhase`，新增 `i18n.phrase.layered_words`、`i18n.phrase.resolve`、`i18n.phrase.module_cache_get/set` 与 `i18n.phrase.global_word`。只增加可观测性，不改变翻译顺序、全局语言范围、缓存键、TTL、失效或异常回退。
- 验收：先以已有真实 timing 作为 RED（上述四类阶段缺失、筛选插槽累计 WLS 2918.91ms），再执行 PHP 语法检查；真实 HTTP 复测在重新加载 Worker 后进行，不能把调试请求的耗时当作冷请求达标。

## 2026-09-05 — 商品列表冷构建轻量化与完整计时

- 已实现列表规格选项/详情正文媒体按用途分离、去除重复媒体解析、readonly标签DTO直接遍历；specifications供属性筛选继续保留，现有范围/语言缓存失效不变。
- Framework trace独立DB累计和32项phase summary解决4096明细截断后的计时缺失。定向38 tests/216 assertions通过；旧Trace/Runtime配置测试既有失败修改前后一致。
- 真运行：统一协调器失效后，英文/中文目录重建TTFB分别5.148s/7.091s，79商品投影1.901s/2.208s，媒体部分0.41/0.47ms；此前本轮英文10.246s/投影6.248s。不是受控压测，未承诺稳定延迟。
- HTTP：首屏24卡前后字段一致；明制属性筛选6件、价格区间及升序第2页正确。未完成：Browser连接/导航/截图超时；EAV共享DTO/私有回退、PHP全量候选分页、模板HTML体积及FPC命中入口长尾仍可优化。
- 详细机制、限制及证据见 Framework/doc/统一缓存范围与性能优化.md 的“目录冷请求投影优化与完整计时”。
- 交付地址：https://p05113ef3.test.weline.com:9555/categories 、https://p05113ef3.test.weline.com:9555/en_US/products 、https://p05113ef3.test.weline.com:9555/en_US/products?sort=price_asc&page=2 、https://p05113ef3.test.weline.com:9555/en_US/products?af_hanfu_chao_dai=Ming+Style 。后台/API：N/A。

## 2026-09-05 — hasObservers 注册表假阴性回退扫描（2.5.9）

- 现象：仅有 `etc/event.xml` 观察者、事件规约尚未写入 `generated/events.php` 时，`dispatch` 因 `hasObservers` 假阴性直接返回，扫描回退永不触发。
- 修复：`EventsManager::hasObservers` 在注册表未命中时调用 `getEventObservers($name, true)`，与 dispatch 的 scan-capable 路径对齐。
- 版本：`2.5.8` → `2.5.9`。

## 2026-09-05 — 事件冷扫描在单次读取内复用模块规约

- 真实故障：Sitemap 模型首次保存进入事件目录冷扫描，在 1/2/3 秒采样中反复 include 同模块 `event.php`；事件文案翻译持续触发网站语言查询，79 条 URL 的同步耗时约 6 分钟。
- 修复：`Event/Config/XmlReader::loadModuleEventSpecs()` 在同一次 `read()` 内按模块复用已加载规约，包含不存在或空结果；每次新 `read()` 开始清空，因此新事件声明、观察者和模块启停仍重新读取。保留全部事件规约与异步策略校验。
- 定向回归：`Test/Unit/Event/Config/xml-reader-spec-cache-contract.php` 先复现空/非空规约重复 include，再验证每模块每次 read 仅 include 一次、下一次 read 发现空规约新增事件、停用模块移出结果。
- 真实验收：同一正常 `SitemapUrl::save()` 探针的两个独立进程，修复前冷保存 412.168 秒，修复后 2.890 秒；分别回读确认临时记录为停用状态，正常模型删除耗时 0.003/0.002 秒，finally 后两条临时记录均复查为 0。未发布 Sitemap、未调用外部提交 API。
- 版本：在现有 2.5.7 上递增至 2.5.8；未覆盖既有缓存性能修改，未修改翻译、数据库或模型保存路径。

## 2026-09-05 — 统一缓存优化实际运行记录

- default 两个 Worker 平滑重载成功（8.1 秒）；分类冷请求 200/10.214 秒，随后命中约 105 ms；首页约 111 ms，英文商品列表约 63–104 ms，均 200。
- 新查询已在真实 timing.log 出现：EAV options 增加 scope_instance_id 条件，重复全实体扫描已消除；trace 中 DB 记录约 453 ms（截断样本，不据此计算完整提升比例）。
- 未完成：后续 nocache 动态请求及复查收到维护页 503，维护状态未改；Browser 真实页面操作工具超时，不能宣称浏览器验收通过。冷目录模板前约 8.06 秒及大 HTML 后处理仍需继续定位。
- 完整策略、定向回归数据和剩余优化清单：Framework/doc/统一缓存范围与性能优化.md。


## 2026-09-05 — 统一缓存范围策略与公共翻译隔离

- 状态：实现与定向回归已完成，真实页面验收见本轮交付记录。
- CachePolicy 集中声明 global/website/store/channel、vary、领域依赖、fresh/stale TTL；CacheManager 统一注册与查询，HotCache 的 rememberPolicy/forgetPolicy 复用现有池。
- 网站资源不再自动包含店铺和渠道；继承 global 与本级领域代次，非全局未冻结上下文仍保持 request fence。旧维度接口兼容。
- HotCache 限制 L1 容量，冷构建和后台刷新统一使用 SingleFlight，并保留入队时的代次键。rememberForRequest 复用 RequestContext，为重复元数据提供请求内统一复用入口。
- 公共 Phrase CSV/词典仍全局共享；EventDictionary 覆盖先于公共缓存且不写公共结果，独占空层不污染 Worker 公共词典。修复 EventDictionary 对请求模块解析方法的可见性调用。
- 定向回归覆盖四层共享/隔离、精确依赖、Global 继承失效、空值/请求生命周期、冷构建再读、SWR 旧代次写回及翻译跨请求污染。
- 规范与后续优化项：doc/统一缓存范围与性能优化.md。实际冷暖访问及 Browser 状态由交付报告记录，不以单测代替运行验收。


## 2026-09-05 — 自然匿名首页命中后建立 Worker 精确缓存入口

- 当前状态：`implemented`。
- 现象：真实首页已返回 `X-Weline-FPC: HIT`，仍执行 `App::applyParsedUrl`；采样中该阶段约 63–970ms，并出现超过 10 秒的长尾。耗时受本机负载影响，不能仅凭少量采样认定完整页面加速比例。
- 根因：默认 READY fail-open 跳过首页 prime；已有 receipt 仅在 prime 或本地化首页中登记，裸根首页自然命中后没有 Worker 可用的精确缓存身份。
- 修复：复用既有 homepage Process L1 receipt 注册机制，补齐无 Cookie、无查询参数的自然根首页；Worker 优先 READY receipt，缺失时采用自然访问记录的精确身份。仍检查来源、Cookie、缓存键身份与 payload 有效性，不重新推导共享缓存变体。
- 定向回归：自然根首页可建立快速入口；Cookie 偏好、私有 session、不同来源和已清理/过期缓存不串用；原本地化路径与 READY receipt 行为保持兼容。
- 运行时验收目标：同 Worker 再次匿名访问首页时 `X-Wls-Performance-Urlparserapply: 0`；实际耗时与浏览器验收状态由本次交付报告记录。

## 2026-09-05 — Unicode 队列名 lease 可删除 + 快速清理死 pid.json

- 当前状态：`implemented`
- 问题：含中文等非 ASCII 的队列 task name 作为裸 token 传入 `buildManagedIdentity` 时，被 ASCII 正则拒绝并哈希成 `weline-cmd-*`，导致 `removeManagedProcessLeaseRecord` 永远找不到真实 `--name=queue-…` 租约；`var/process/pid` 堆积数千 `queue-*-pid.json`，进而触发 `server:stop` 目录条目硬上限。
- 变更：裸 token 允许 Unicode 字母数字；新增 `cleanupDeadPidJsonOrphansFast()`（posix 探测 + 一次索引压缩）。
- 验收：`ProcesserUnicodeManagedIdentityTest`。

## 2026-09-03 — cache:clear 等待 WLS Worker 后再预热

- 当前状态：`implemented`
- 问题：`cache:clear` 通过异步 `cacheClear()` 排队后立即预热店面 FPC；旧 Worker 尚未清理模板与进程 L1 时，可能把旧页面重新写回 FPC，源码变更要等缓存 TTL 才可见。
- 变更：CLI 改用 `cacheClearAndWait(null, 12.0)`；仅在所有目标 Worker 回执 `success=true, completed=true` 后执行店面预热，未完成时明确跳过，避免旧缓存回灌。
- 验收：`ConsoleCacheClearRuntimeSyncTest` 覆盖“确认后预热”和“未完成不预热”；真实 `php bin/w cache:clear` 收到 WLS 完成回执并预热 3 条路径，随后首页连续 3 次即时探测均为新模板。

## 2026-09-03 — getModuleViewDir 绝对路径 preg 定界符冲突

- 当前状态：`implemented`
- 问题：后台 Dashboard 等模块首次实例化 `Template` 时，`getModuleViewDir` 需 mkdir `view/tpl/`；`preg_match('/^(?:[A-Za-z]:[\\\/]|[\\\/])/', …)` 因字符类内 `/` 提前结束定界符触发 `Unknown modifier ]`，绝对路径被误判为非绝对，抛出「拒绝在不安全路径创建模板目录」。
- 变更：绝对路径判定改为 `#^[A-Za-z]:/#` + `str_starts_with('/'|'\\')`，与 `resolvePublicThemeNamespace` 一致。
- 验收：`ProcessFileSourceCompileDirTest` 覆盖绝对模块 `view/tpl` 可创建、仓库根 `BP/view/tpl` 仍拒绝；Dashboard 后台页不再因该门禁 500。

## 2026-09-03 — CLI 自动修复被污染的 Unix HOME

- 当前状态：`implemented`
- 问题：Agent shell 偶发将 `HOME` 设为站点 URL 或 HTTP 状态码，导致在仓库 cwd 下生成 `https:` 相对目录。
- 变更：`PhpCliRuntimePreflight::sanitizeUnixHomeEnvironment`（`bin/w`/`bin/m` 入口 enforce 最先调用）恢复为登录目录 HOME，并清掉不安全的 `XDG_STATE_HOME`/`WLS_EDGE_STATE_HOME`。
- 验收：`HOME=https://host php -r` 恢复为 `/Users/weline`；关联 Server `ProjectIdentityStore` URI HOME 拒收单测。

## 2026-09-03 — 共享 Session regenerate 后重绑 sibling 设备

- 当前状态：`implemented`
- 问题：前后台共用 `WELINE_SESSID`；一区 `login`→`regenerate` 后另一区设备 `session_digest` 过期，下一跳 `session_rebound` 清登录。
- 变更：`AuthenticatedSession` 在 `login`/`regenerate` 后调用 registry `rebindToCurrentSession`；接口新增该方法。
- 验收：见 SessionManager `1.2.1` 单测；Framework `AuthenticatedSessionDeviceRegistryTest` Fake 已实现新方法。

## 2026-09-02 — AuthenticatedSession 拒绝无效 principal 半登录

- 当前状态：`implemented`
- 问题：`login_id=0`/`"0"` 仍被 `isLoggedIn()` 视为已登录；`getUser()` 在 load 失败后仍缓存空 Authenticable，导致前台「已登录壳」与 Query「请先登录」分裂。
- 变更：`login`/`isLoggedIn`/`getUser` 统一 `isValidPrincipalId`（正整数或非空非 `"0"` 字符串）；无效态清登录键。
- 验收：`AuthenticatedSessionTest` 覆盖 login 拒绝 id=0、isLoggedIn 拒绝已存 0。

## 2026-08-30 — FPC 登录旁路对齐 Session Cookie 候选名

- 当前状态：`implemented`
- 问题：登录后首页仍命中游客 FPC，`header/account` 显示「登录/注册」；`account` 模块已加载但无 `w_auth` 时无法切壳。根因是 `cookieHeaderHasLoggedInFrontendSession` 只查 `resolve()` 单一名（如 `WELINE_SESSID_9555_w0`），忽略文档登录常见的 `WELINE_SESSID_{port}` 等同族候选。
- 变更：FPC 登录判定改为 `requestCookieCandidates` + Cookie 头内 `WELINE_SESSID*` 扫描；非法 SID 仍 fail-closed 旁路。
- 验收：`FullPageCacheCoordinatorTest` 覆盖 `_w0` 别名；已登录持任一候选 Cookie 时不再 serve 游客 HTML。

## 2026-09-03 — 店面词典确定性回退链

- 当前状态：`implemented`
- 问题：目标语言缺少模块词条时会直接泄漏中文源文；持久化 WLS 还可能继续命中变更前的 FPC。
- 变更：`LocaleFallbackChain` 与 `Phrase\\Parser` 统一采用“目标语言 → 维护中的 `en_US` 中性文案 → 网站默认语言 → 源文”的确定性顺序；中文目标保留中文源文，模块 CSV 中空译文或与源文相同的占位行不覆盖有效英文译文。
- 缓存：更新语言包或翻译作用域后执行 `php bin/w i18n:collect Weline_Module`、`php bin/w cache:clear`，持久化 WLS 才会丢弃旧页面与词典缓存。
- 验收：`LocaleFallbackChainTest` 5 tests / 11 assertions、`PhraseTest` 1 test / 3 assertions通过；`ar_SA` About/Contact 实际响应为 `lang=ar-SA`、`dir=rtl`，标题与 H1 回退英文。
- 版本：`Weline_Framework` 2.5.4 → 2.5.5。

## 2026-08-30 — Windows 框架命令降权（管理员安装可继续）

- 当前状态：`implemented`
- 需求：`REQ-FRAMEWORK-0008`
- 问题：先前误做成 elevated 直接报错中断；目标应是管理员可装系统组件，仅 `bin/w`/composer 等以部署用户运行防 ACL/schtasks 污染。
- 变更：撤销 install/CLI 的 elevated 拒绝；`DeployUserCommandRunner` 用 Limited 计划任务 / `WELINE_USER_PASSWORD` 凭据降权；`Cli` elevated 时重执行。
- 验收：管理员跑 `install.bat` 不因门禁失败；框架命令日志出现 deploy user privilege drop。

## 2026-08-30 — 安装入口 root 降权（防 crontab/var 属主污染）

- 当前状态：`implemented`
- 需求：`REQ-FRAMEWORK-0008`
- 问题：`bin/install.bash` 虽会 root→weline，但直接 `php setup/server_installer/run.php` 仍可能以 root 跑 `bin/w`/`composer`，把 cron 写入 root crontab、把 `var/` 写成 root。
- 变更：新增 `DeployUserCommandRunner`；`run.php` 以 root 进入时整段重执行为部署用户，并对 `bin/w`/composer 命令做 `runuser`/`sudo -u` 包装；`install.bash` 调用时导出 `WELINE_USER`；同步 `INSTALL_SYNC.md`。
- 验收：辅助类语法检查通过；非 root 下 `wrapShellCommand` 原样返回；Linux root 场景应看到 re-exec / deploy-user 包装日志。

## 2026-08-30 — CLI 用户检测误用脚本属主（root 绕过）

- 当前状态：`implemented`
- 需求：`REQ-FRAMEWORK-0007`
- 问题：`Env::user()` 调用 `get_current_user()`（脚本文件属主）。root 执行属主为 `weline` 的 `bin/w` 时，`check_user` 误放行；`cron:install` 写入 root crontab，业务 cron 以 root 写 `var/`，官网 WLS 无法自愈并对外 502。
- 变更：`Env::user()` 改为 `posix_geteuid`/`posix_getpwuid`（无 posix 则 `USERNAME`/`USER`/`LOGNAME`）；模块版本 `2.5.2`；单测 `EnvProcessUserContractTest`。
- 验收：聚焦单测通过；root 跑 `bin/w` 在 `env.user=weline` 时失败退出。

## 2026-08-30 — Session setCookie 未清理同族 `_w0`（展开矩阵掉线）

- 当前状态：`implemented`
- 问题：矩阵 SSE 触发 remember 恢复后只写出 `WELINE_SESSID_{port}`，浏览器仍持 `WELINE_SESSID_{port}_w0` 指向已销毁会话 → 下一跳 `session_rebound` / 踢回登录。
- 变更：`WlsStrategy::setCookie` 写入当前 jar 后过期其它 `requestCookieCandidates()` 同族名。
- 验收：展开 AI 翻译模块语言进度保持登录。

## 2026-08-30 — WLS SSE 首包漏发 Set-Cookie（一点击写回就掉线）

- 当前状态：`implemented`
- 问题：后台矩阵/写回 SSE 在 `__init` 里 remember 恢复并 regenerate Session，但 `SseWriter::start()` 在 WLS 下只写 `200`/`event-stream` 头，不带 `HeaderCollector` 的 `Set-Cookie`；浏览器仍持旧 Cookie，随后写回触发 `未登录` / `session_rebound`。401 SSE 失败响应还会被 Worker 合并空 Session Cookie，冲掉登录态。
- 变更：`SseWriter` 首包附带并清空 pending cookies；`HeaderCollector::formatSetCookieHeaderLines`；SSE 401 丢弃 `WELINE_SESSID*` pending cookie。
- 验收：展开模块后写回不再立即未登录；`SseWriterTest::testStartEmitsPendingSessionCookiesOnWlsSocket`。

## 2026-08-30 — 后台 Session Cookie 跟随 website_id（与前台同后缀）

- 当前状态：`implemented`
- 问题：`App::reassertFrontendSessionCookieWire` 跳过后台；`CookieScopeResolve` 也曾跳过后台 → 后台一直写 `WELINE_SESSID_{port}`，被前台 Expire 别名清掉。
- 变更：重断言对后台同样生效；Websites CookieScope 后台按 `website_id` 加 `_w{id}`（缺省 0），Path=/。
- 验收：见 `Weline_Websites` 开发日志同日条目；`CookieScopeResolveTest`。

## 2026-08-30 — CookieScope 激活后重断言 Session Cookie（登录踢回）

- 当前状态：`implemented`
- 问题：同请求网站 Cookie 发 `WELINE_*_w0`，Session 仍发 `WELINE_SESSID_{port}`；登录写入无后缀 jar，后续作用域请求 Expire 别名后整页踢回 login。根站 `WELINE_WEBSITE_URL` 常为空，CookieScope 依赖 URL 时作用域抖动。
- 变更：`CookieScopeResolve` 在 URL 空时用请求 Host 合成根站 URL；`WlsStrategy::setCookie` 只传权威未加后缀名，由 `HeaderCollector` 统一 qualify；`Session::reassertCookieWire` + `App` 在 sync 网站 Cookie / Session 启动后重断言。
- 验收：登录后硬刷新 `/en_US/customer/account` 保持 Personal center；响应含 `WELINE_SESSID_{port}_w{id}`；单测 `CookieScopeResolveTest`。

## 2026-08-29 — 前台 Session Cookie 无后缀与 `_w{id}` 分裂

- 当前状态：`implemented`
- 问题：登录页常发 `WELINE_SESSID_{port}`，QueryBin 在 CookieScope 激活后发 `WELINE_SESSID_{port}_w{id}` 并 Expire 前者；API `Already signed in` 但整页踢回 login；logout 若只清无后缀则 QueryBin 侧仍登录。
- 变更：`SessionCookieNameResolver::requestCookieCandidates` / `readRequestSessionId`；`WlsStrategy::initialize` 回退读别名并在当前作用域重写 Cookie；`clearCookie` 清理全部候选。
- 验收：logout 后 API 登录进 `/en_US/customer/account` 保持 Personal center；单测 `SessionCookieNameResolverTest`。

## 2026-08-29 — QueryBin 登录不写 Session Cookie

- 当前状态：`implemented`
- 问题：前台 `account.login` 走 QueryBin；`Response::fromContent` 使用 detached HeaderCollector，Session `setCookie` 写在请求级 Collector，二进制响应未带 `WELINE_SESSID`，API 报已登录但整页仍未登录。
- 变更：`QueryBin::attachPendingCookies` 在二进制响应发出前合并请求级 Cookie（对齐登录 302 路径）；环境 `session.cookie_partitioned=false` + `cookie_samesite=Lax` 避免 Cursor 内嵌浏览器丢弃 CHIPS Cookie。
- 验收：登录页 API 登录后跳转 `/en_US/customer/account` 保持登录；响应头可含 `X-Weline-Query-Bin-Cookies`。

## 2026-08-29 — 前台 Session 活动滑动续期

- 当前状态：`implemented`
- 问题：`Session::save()` 仅 dirty 落盘，只浏览不写时存储 TTL/文件 mtime 不刷新，易与 WLS Session Server 默认 3600s、镜像 GC 叠加导致短时掉线。
- 变更：`Session::save` 节流 touch；`FileStorage::read` 成功后 touch mtime；`session_server` 无 `wls.session.session_ttl` 时对齐 `session.lifetime`；`SessionStore::touch` 每 60s markDirty 持久化续期。

## 2026-08-29 — @backend-url 族管道参数仍误编译为按位或

- 当前状态：`implemented`
- 问题：今日已修 `@url`/`@frontend-url`/`@api`，但 `@backend-url`/`@admin-url`/`@backend-api` 仍把 `|` 原样塞进 `getBackendUrl(...)`，Dashboard 视图链 `@backend-url{'...'|$viewParams}` 运行时 TypeError（array | string）。
- 修复：三族标签与 `url` 对齐，按 `|` 拆成 path + params；扩展 CompileTest；需清模板编译缓存后重生。

## 2026-08-29 — @url 内联管道参数误编译为按位或

- 当前状态：`implemented`
- 问题：`@url{'path'|['k'=>$v]}` 在 `@tag{}` 分支未拆 `|`，生成 `getUrl('path'|array)`，注册/登录带 `redirect_url` 时 TypeError（array | string）。
- 修复：`url` / `frontend-url` / `api` 内联默认分支按 `|` 拆成 path + params；契约测试覆盖。

## 2026-08-29 — 路径优先 + Query 回落（语/币对称）

- 当前状态：`fixed`
- 契约：`State::getLang()` / `getCurrency()` 顺序为 **路径段 →（语言）请求 override → query → 网站默认 → 硬默认 `zh_Hans_CN` / `CNY`**；不读不写语/币偏好 Cookie。
- Query 键：语言 `locale` / `locale_code` / `lang`；货币 `currency`。有路径段时 query 不得压过路径。
- 对齐：`Url::parser`、静态 404/维护 `resolveLang`、FPC `mergePathVariant`（无路径时跟 `get*`）、前后台 `weline-api` / `i18n.js` / `currency.js` / `url-*` / Pixel（inject→path→query/`data-*`→默认）。
- 保留：`WELINE_WEBSITE_*` Set-Cookie；请求内 `WELINE_USER_*` 服务端镜像（来源改为路径/query/`get*`）；官方切换器仍路径导航。
- 过期：残留 `WELINE_USER_LANG` / `WELINE_USER_CURRENCY` 由 `App::syncCookieRouteStateFromServer` 清除，不影响无前缀页。

## 2026-09-03 — 模块定向事件刷新保留跨模块观察者

- 当前状态：`implemented`；真实运行验收在本次模块升级后执行。
- 问题：`EventRegistry::refreshForModules()` 清除事件 owner 模块时会删除完整事件条目，连带丢失其他活跃模块对该事件注册的观察者。`Weline_Seo` 定向升级后，`Weline_Seo::integration::head_context_resolve` 从 3 个观察者退化为 0；全量 `event:rebuild` 可恢复，证明缺陷位于增量路径。
- 修复：清除前仅捕获 owner 条目上的非目标模块观察者；目标模块规约重新合并后，只向仍存在的普通事件/动态模式恢复观察者，并按 `sort` 与稳定身份排序、按 `observer_key` 去重。若 owner 已删除规约，不恢复事件或观察者。
- 回归：`EventRegistryIncrementalObserverPreservationTest` 覆盖普通事件、动态模式、排序及 owner 删除规约不产生幽灵事件。
- WebUI：N/A（框架注册表逻辑）；商城页面 SEO 由归属模块另行执行真实 Browser 验收。

## 2026-08-28 — QueryBin / Worker 路径语言（BinQuery context.locale）

- 当前状态：`fixed`
- 问题：语言 Cookie 移除后，若 QueryBin worker 仍只吃 `runtimeConfig.currentLang` / 遗留偏好，会向 `/api/framework/query-bin` 提交错误 `context.locale`；WLS homepage warmup 若仍发 `WELINE_USER_LANG` Cookie 预热 `/`，也与路径权威不一致。
- 修复：`Frontend/Backend weline-api.js` 与 `weline-api-worker.js` 以 path → query → `data-lang` → config 解析 locale，并下发 `pathname`；`FullPageCacheCoordinator::buildFpcVariantFromCookieHeader` 不再用 Cookie 语言分叉；homepage receipt `cookie_header` 为空。
- 保留：变体身份在 `full_uri` / `cache_key` / 已解析 `variant.lang|currency`，不是浏览器语/币偏好 Cookie。

## 2026-08-28 — 语言 Cookie 彻底移除（路径识别）

- 当前状态：`fixed`
- 问题：`WELINE_USER_LANG` Cookie 与路径权威并存，易污染无前缀 FPC，并误导前端切换器。
- 修复：`App::syncCookieRouteStateFromServer` 不再回写语言 Cookie，并对残留 `WELINE_USER_LANG(+_wN)` 执行过期删除；404/维护静态页 `resolveLang` 忽略 Cookie；前台/后台语言切换 JS 只清 Cookie、按路径导航。
- 补齐：`url-backend.js` / `url-frontend.js` / `Frontend/weline.js` 停止用 Cookie 识别当前语言（改为 path → `data-lang` → config）；`select_language` 只导航路径；`WlsRuntime::buildEarlyRedirectResponse` 不再写语言 Cookie。
- 保留：请求内 `user.lang` / `WELINE_USER_LANG` 服务端镜像（由路径/query/`get*` 同步）；语/币偏好 Cookie 均不写。

## 2026-08-28 — 路径优先：默认语/币种段 301 剥前缀

- 当前状态：`fixed`
- 问题：默认语 `zh_Hans_CN` 仍可出现在地址（`/zh_Hans_CN/product/...`），与「默认本地码不上地址、非默认才出现」不一致。
- 修复：`State::canonicalizeStorefrontLocalizationPath()` + `App::redirectDefaultLocalizationPrefixIfNeeded()`，前台 GET/HEAD 在 FPC 前对默认语言/默认货币段发 301；非默认 `/en_US/`、`/USD/` 保留。
- 回归修复：301 默认值只取 `website.language` / `WELINE_WEBSITE_LANGUAGE`（及 parse 同源字段），空则 fail-open；禁止 `resolveWebsiteDefaultLanguage()` / `preferredLanguageCodes`（曾误判 `en_US` 为默认并把 `/en_US/` 剥成无前缀）。
- 关联：无前缀渲染强制网站默认语（防 Cookie 污染 FPC）。

## 2026-08-28 — 无前缀 URL 强制网站默认语言（修复 FPC 英文污染）

- 当前状态：`fixed`
- 问题：默认站默认语为 `zh_Hans_CN` 时，`/product/...` 无语言前缀应显示中文；若 Cookie 残留 `en_US`，渲染成英文却写入「默认语」FPC 键，导致无 Cookie 访问仍命中英文页。默认语本地码也不应出现在地址上。
- 修复：`State::getLang()` / `App::synchronizeParsedLocalization` 在无语言路径段时改用 `resolveWebsiteDefaultLanguage()`，忽略 Cookie；非默认语仍走 `/en_US/...`。语言切换器已通过 `LocalizedUrlBuilder` 省略默认语段。
- 验收：清 FPC 后带 `WELINE_USER_LANG=en_US` 访问无前缀商品页应 `data-lang=zh_Hans_CN`。

## 2026-09-08 — App 持久化快路径只读 L1 漏 shared HIT

- 当前状态：`implemented`
- 问题：`App::tryPersistentFpcFastPath` 仅 `getCachedResponse(GET, processOnly=true)`；L1 空而 shared 已暖时仍落入后续渲染。debug `70285b` 见 `process-miss`+`shared_hit=true`（已登录 `/`、`/products`）。用户侧带 `Cache-Control: no-cache`（DevTools Disable cache）的文档请求在 **2.5.34** 仍会故意 `fpc=disabled` SSR；**2.5.35** 起 `no-cache` 也可 HIT 公共 FPC。
- 修复：L1 miss 后回退一次 shared 读；processOnly miss 不再写误导性 shared 诊断日志。
- 验收：暖缓存后已登录软刷新 `/`、`/products` 应 HIT；`no-cache` 在 2.5.35+ 亦应 HIT（仅 `no-store` 强制 SSR）。

## 2026-09-08 — deferred warmup max_paths 过小导致目录页冷 SSR

- 当前状态：`implemented`
- 问题：homepage fail-open 把 `/` 插入 deferred 首槽后，默认 `storefront_deferred_warmup_max_paths=1` 挤掉 Product 贡献的 `/en_US/products` 等；已登录访问 `/products` 只能 wait_miss 全量 SSR（debug `70285b`），而根 `/` 已 HIT。
- 修复：默认 max_paths 改为 6；Product 预热改用无前缀 `/products`（去掉会 301 的默认语前缀）；Index 贡献 `/en_US/`、`/ar_SA/`。
- 验收：reload 后 warmup `paths` 含 `/`、`/products`、`/en_US/products` 等；已登录软刷新这些路径应 FPC HIT。

## 2026-09-08 — wait(timeout=0) 只读 process L1 漏掉 shared HIT

- 当前状态：`implemented`
- 问题：`waitForPublishedResponse` 在持久化默认 timeout=0 时调用 `getCachedResponse($method, true)`，只查 process L1；同伴已写入 shared（外置 body 可 hydrate）仍 `process-miss` → `wait_miss` 再全量 SSR（debug `70285b`：`hit_source=process-miss` 且 `shared_hit=true`/`hydrate_ok=true`）。
- 修复：timeout≤0 时改为读 shared（`processOnly=false`），再回退 stale。
- 另：复验中大量首页 `Cache-Control: no-cache`（DevTools Disable cache）会故意 `fpc=disabled`；关 Disable cache 后已登录探针可 HIT（约十几毫秒）。

## 2026-09-08 — 已登录等游客 publish：延长 peer wait

- 当前状态：`implemented`
- 问题：已登录可 HIT 公共 FPC 但不可 build；持久化默认 `fpc_build_wait_timeout_ms=0`，同伴游客正在 `build_lock` SSR（数秒）时已登录立即 `wait_miss` 再全量 SSR（debug `70285b`）。
- 修复：当已登录且检测到 build lock 被占用时，等待最多 `wls.performance.fpc_logged_in_publish_wait_ms`（默认 8000ms）继承 HIT；hydrate 失败的外置 body stub 会删除以便游客重建。
- 验收：游客冷 SSR 期间已登录刷新应出现 `logged-in extended publish wait` 后 `fpc serve hit` / `X-Weline-FPC: HIT`，而非并行长 SSR。

## 2026-09-08 — 已登录可 HIT 公共游客 FPC（禁止 build）

- 当前状态：`implemented`
- 问题：P1a 将登录态整页硬旁路（serve/build/publish 全 false）；已登录首页每次全量 SSR（debug `70285b` 实测 1–8s+），而游客暖缓存 HIT 仅几十毫秒。Customer 已具备游客 SSR + JS soft-reconcile（`account-session` / `w_auth`）。
- 修复：`canServeCachedResponse` 与 Worker pre-router / `hasSharedCachedResponseForFullUri` 允许登录 HIT 公共键；`canBuild`/`canPublish` 与 `warmProcessCacheForFullUri` 仍拒绝登录写缓存；私有会话 FPC 保持关闭。
- 验收：`testPortQualifiedLoggedInSessionMayServePublicFpcButNeverBuild`；暖缓存后带登录 Cookie 访问首页应 `X-Weline-FPC: HIT`（2.5.35+ 下 DevTools `Cache-Control: no-cache` 亦可 HIT；仅 `no-store` 强制 SSR）。

## 2026-09-08 — 浏览器普通刷新 max-age=0 误旁路 FPC

- 当前状态：`implemented`
- 问题：Chrome/Safari 普通刷新发送 `Cache-Control: max-age=0`（常附带 `Pragma: no-cache`），被当成强制跳过公共 FPC → 每次 F5 全量 SSR（实测约 0.5–2s+，高负载可达十余秒）；无该头的首访/ curl 则为 HIT（约几十毫秒）。
- 修复：`clientCacheControlRequestsBypass` 不再把 `max-age=0` 当旁路；有 Cache-Control 时忽略配套 Pragma。后续 2.5.35 进一步不再把请求侧 `no-cache` 当旁路，仅 `no-store` 强制 SSR。
- 验收：`FullPageCacheCoordinatorTest::testBrowserReloadMaxAgeZeroDoesNotBypassSharedFpcServe`；curl `-H 'Cache-Control: max-age=0'` 应 `x-weline-fpc: HIT`。

## 2026-09-08 — 首页 deferred 预热漏掉 `/` 导致冷 SSR 极慢

- 当前状态：`implemented`
- 问题：`ready_gate_homepage_fail_open=1` 时 READY 跳过 in-process 首页 FPC 证明；`runDeferredStorefrontCriticalWarmup` 又显式排除 `/`，Worker 启动后首页永不预热，匿名首访/硬刷新后的冷路径 `action_execute` 可达数秒至十余秒（高负载时更明显）。
- 运行证据：debug session `70285b` — 首页 19 次全走 SSR（`fpc=disabled|build_lock`），`homepage-shell` fetch 最高约 15s；无 Cookie 暖请求 `x-weline-fpc: HIT` 约几十毫秒。
- 修复：当 homepage READY 为 fail-open 或 proof 未 HIT 时，延迟预热优先纳入 `/`（默认 `max_paths=1` 时占首槽）。
- 验收：`WlsRuntimeStorefrontWarmupContractTest`；`server:reload` 后看 `var/log/wls-storefront-warmup.log` 的 deferred `paths` 含 `/`，匿名软刷新首页 FPC HIT。

## 2026-08-26 — Product V2 验收暴露的 full clone 与 PostgreSQL 冷建索引修复

- 当前状态：`implemented_validated`
- 关联：Weline_Product 万能产品 v1.0 / M4、M5、M6。
- full clone：`MigrationCloneService` 从捕获完整 `pg_dump` stdout 改为 `pg_dump --file` 流式临时文件，再用 `psql -f` 恢复，避免 256MB CLI 内存耗尽；迁移底座 14 tests / 41 assertions 通过并完成真实 full clone。
- 冷建索引：PostgreSQL Create adapter 统一使用 `PgsqlIndexName::canonicalPhysical()`，消除旧 55+hash 与新 54+hash 并存导致的重复索引和 shard provision 不收敛；聚焦 4 tests / 12 assertions。
- 运行证据：修复后 Product PostgreSQL 跨站复制矩阵 4 tests / 88 assertions 通过；随机测试 schema 为 0，临时 full clone 已通过官方 `clone-destroy` 清理。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `2.4.0` | implemented | `REQ-FRAMEWORK-0006` | 2026-08-25 | 2026-08-25 | homepage READY fail-open default |
| `2.4.0` | implemented | `REQ-FRAMEWORK-0005` | 2026-08-21 | 2026-08-21 | HTTP 状态回执页：ErrorPageRenderer + pub/errors 多码模板 |
| `2.4.0` | implemented_validated | `REQ-FRAMEWORK-0004` | 2026-08-18 | 2026-08-18 | 媒体管理 WebUI：目录树/网格可见，无 Internal server error |
| `2.4.0` | implemented_validated | `REQ-FRAMEWORK-0003` | 2026-08-18 | 2026-08-18 | curl Cookie 名收敛 + 后台 Browser 连点通过 |
| `2.4.0` | implemented_validated | `REQ-FRAMEWORK-0001` | 2026-08-11 | 2026-08-11 | automated + frontend/backend Browser pass |
| `2.4.0` | implemented_validated | `REQ-FRAMEWORK-0002` | 2026-08-17 | 2026-08-17 | 21 focused tests / 94 assertions + storefront Browser pass |

## `2.4.0` — 首页 READY 预热默认 fail-open

- 当前状态：`implemented`
- 需求 ID：`REQ-FRAMEWORK-0006`
- 范围：`WlsRuntime::runRequiredHomepageProcessFpcReadyGate` / `isHomepageReadyGateFailOpen`；与 Server Orchestrator / Worker 启动门禁对齐。
- 事实：模板 `declare(strict_types=1)` 与编译器 hash 头冲突时产生不可捕获 `E_COMPILE_ERROR`，READY 前 in-process 渲染会杀死 Worker。
- 实现：默认 `ready_gate_homepage_fail_open=1`，READY 前跳过 in-process 首页证明并返回 `deferred-after-ready` proof；严格模式才走旧路径。
- 验收：与 `REQ-SERVER-0012` 同批；需 `server:reload` 后确认启动不再因首页未 hot 而 `WLS_STARTUP_FAIL_FAST`。

## `2.4.0` — HTTP 状态回执页可定制

- 当前状态：`implemented`
- 需求 ID：`REQ-FRAMEWORK-0005`
- 范围：`ErrorPageRenderer`、`NoRouterException`、`WlsRuntime`、`HtmlRenderer` 生产页、`pub/errors/{400,401,403,404,410,429,500,502,503,default,_shell}`、事件 `Weline_Framework_Http::error_page_render`。无 schema 变更。
- 事实/发现：旧模板 `echo 404`；WLS 二次 include 未注入 `$msg`，生产出现纯文本三字节正文。
- 实现：统一渲染器；异常正文与 WLS 同源；站点可覆盖 `pub/errors` 或事件写 `html`。
- 验收：聚焦单测 `ErrorPageRendererTest`；回灌站点后抽测 404 HTML 非纯 `404`。

## `2.4.0` — QueryBin 不得把 runtime_task 拒绝伪装成 500

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-FRAMEWORK-0004`
- 范围：`QueryBin` 对 `ResumableTaskAccessDeniedException` 映射为 401；无 schema 变更。
- 事实/发现：媒体管理 `media_manager.connector/config` 返回 500 `Internal server error.`，日志为 `Runtime task backend authority no longer matches the Session.`。
- 实现：在通用 `Throwable` 之前拦截该异常，返回 `backend_attestation_invalid`。
- 验收：`QueryBinResumableAccessDeniedMappingTest` 2/2。2026-08-18 内置 Browser，test 登录文件管理：`connector open` 返回 Media Files / 5 files / tree 3，`config` 成功，页面无红色 toast。

## `2.4.0` — 请求 authority 与 Session Cookie 身份

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-FRAMEWORK-0003`
- 范围：`WlsRequest` 在 Host 省略端口时规范直连 `WLS_PORT`，在可信 Dispatcher 转发时恢复公共 Host/scheme/port，并由 `GlobalsEmulator` 原样保留 authority 快照；`SessionCookieNameResolver` 信任有效 `SERVER_PORT`。设备校验失败保留脱敏日志。无 schema 变更。
- 事实/发现：2026-08-18 直连 `https://host:9555` 曾因 HTTP/2 authority 省略端口造成 Cookie 名漂移；2026-08-26 生产公网 443 经负载均衡到 Worker 23922/23923/23924 时，先观察到内部端口污染 Session Cookie，随后又确认 `GlobalsEmulator` 从 raw Host 二次覆盖已规范化快照，导致页面 `<base>`、菜单和重定向仍含内部端口。
- 实现：非受信直连且 Host 无端口时，由 `WlsRequest` 用 `WLS_PORT` 写入规范化 `HTTP_HOST`/`SERVER_PORT`；仅当传输层已确认可信 Dispatcher 时，从其保存的 `Weline-Original-Host` 与 original scheme/port 恢复公共 authority。`GlobalsEmulator` 对 HTTP_HOST、SERVER_NAME、SERVER_PORT、HTTPS、REQUEST_SCHEME 和 WELINE_FULL_REQUEST_URI 优先使用不可变快照，仅在缺失时回退 Request API。
- 验收门禁：`WlsRequestForwardedOriginTest` 覆盖直连非标准端口、可信 Dispatcher wire Host=`app.example.com:23922` 以及 `GlobalsEmulator` 前后公共 HTTPS 443 不变；`SessionCookieNameResolverTest` 覆盖 `SERVER_PORT=443`、`WLS_PORT=23922` 仍为无后缀 `WELINE_SESSID`。生产需同时验证跨 Worker 登录态与页面所有绝对 URL/Location 均不含 23922/23923/23924。

## `2.4.0` — 网站有效货币解析契约

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-FRAMEWORK-0002`
- 范围：`State::getCurrency()`、网站默认货币回退及聚焦回归；无 schema 变更。
- 事实/发现：网站默认及允许币种均为 USD 时，遗留 `user.currency=CNY` 仍被无条件采纳，导致默认商品与搜索路由显示 CNY 0.00；显式 `/USD` 路由正常。
- 实现：保留显式 URL 币种优先级；用户偏好通过当前网站允许集合校验；新增与默认语言对称的网站默认货币解析；在重建 `RequestContext` 前归一化币种；修正 `Env::system/router/dev()` 通过 `Env::get()` 读取大写默认值时被误认为模块名的歧义。
- 验收：先以失败用例复现 `expected USD / actual CNY` 及大写默认值返回 `null`；实现后 21 个聚焦测试 / 94 个断言通过，9514 的默认商品、搜索、详情、购物车、结账前置页及四种本地化路径均使用 USD，控制台 0 error / 0 warning。

## `2.4.0` — 认证设备生命周期契约

- 基线版本：`2.3.5`
- 目标版本：`2.4.0`
- 当前状态：`implemented_validated`
- 需求 ID：`REQ-FRAMEWORK-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：仅中立 Session 契约与生命周期；设备 ORM、Cookie、UI 和业务策略归 SessionManager/调用模块。
- 负责人/确认人：主代理直接实施 / 用户。

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-framework-0001-可选的认证设备生命周期契约` | 2026-08-11 |
| 实现执行者/模型/范围已记录 | pass | 主代理直接实施，限定 Framework Session/Runtime 契约 | 2026-08-11 |
| 架构/缺陷/安全复审通过 | pass | 安全扫描 `95c397ee-7f3e-4ac5-ad01-54acd8819510`；Fiber 隔离、失败关闭和敏感值处理复审通过 | 2026-08-11 |
| 分层测试通过 | pass | 多模块聚焦 PHPUnit 合计 `149 tests / 544 assertions`；PHP/XML/JS 静态检查通过 | 2026-08-11 |
| 真实路径 WebUI/E2E 通过 | pass | 前后台独立上下文 E2E 与两端内置 Browser 用户路径通过；console 0 error / 0 warn | 2026-08-11 |
| 可重复 E2E 固化并通过 | pass | 前后台四个 SessionManager 用例在独立 WLS 通过 | 2026-08-11 |
| 功能/运营文档完成 | pass | Framework 需求/开发日志与 SessionManager 架构、运营文档 | 2026-08-11 |
| 范围级 QA 验收 | pass | `迁移前历史任务记录（已清理）` | 2026-08-11 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-11 07:54 | requirements | planned → requirements_confirmed | 固定需求/开发日志此前缺失；用户已批准完整设备管理计划 | 建立从 2.4.0 开始的诚实基线 | 用户 | `doc/需求.md` |
| 2026-08-11 21:14 | implementation/validation | requirements_confirmed → implemented_validation_partial | 中立设备契约、登录上下文、请求级验证、Provider 失败关闭及 Fiber 隔离已实现并通过自动化 | 前台内置 Browser 验收需验证码授权，保持 partial | 主代理 | PHPUnit、安全扫描、前后台 E2E |
| 2026-08-11 23:22 | final validation | implemented_validation_partial → implemented_validated | 前台授权 CAPTCHA 用户路径、退出后 remember 不恢复、数据清理与两端 console 检查通过 | 本功能全部批准门禁已闭合；QA 作范围级通过判定 | 主代理 | `acceptance-audit.md`、`security-report.md` |

### 实现范围

- 变更路径/符号：`Session/Auth` 中立契约、`AuthenticatedSession`、`SessionFactory`。
- 数据/配置/迁移：Framework 无 Schema；Provider 映射由 SessionManager 提供。
- 兼容与回退：Provider 未声明保持旧行为；声明故障失败关闭。

## 2026-09-13 — 命名空间事务提交完整变更集合通知

- 起因：I18n 同一主库事务内更新多个语言时，固定键的 afterCommit 回调只保留首次闭包捕获的局部版本集合，后续语言变更无法一并通知其他 Worker。
- 在既有 `NamespaceGenerationRepository::bumpMany(array $namespaces, array $committedCallbacks = []): array` 增加可选命名回调，复用原 `transactionState` 中累计的 `authority_clock + changes`；每个键只保留首个回调。原提交点先取出并释放事务状态，再推进进程快照，向回调传递最终完整集合；回滚沿原路径丢弃集合和通知。
- 保持所有单参数调用、每个 namespace/owner transaction 只增一次和 `@clock` 只增一次的语义；不新增版本存储、队列、重试或缓存池清理。
- TDD：新增 `Test/Unit/Cache/Namespace/NamespaceGenerationCommitCallbacksTest.php` 在原实现运行 3 tests / 9 assertions，其中完整通知和回滚后下个事务通知两项 RED；修改后与 I18n 写入、文件发布及语言生命周期测试合计 **38 tests / 266 assertions PASS**，正式引导为 `app/bootstrap_phpunit.php`。
- 本节仅记录源码及定向测试结果；官方模块升级、编译与真实跨 Worker 请求验收由主任务统一完成后补记。
