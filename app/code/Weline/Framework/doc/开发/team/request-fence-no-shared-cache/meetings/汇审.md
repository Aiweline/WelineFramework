# 汇审 — request-fence 禁共享落盘

日期：2026-09-20  
模块：Weline_Framework `2.5.111`  
结论：通过

## 变更

- 共享/进程缓存键不再嵌入请求 ID 指纹；fence 仅标记 `scope_state=request-fence`。
- `StorefrontScopeHotCache` 在 fence 下只走 `rememberForRequest`，零 L1/L2 读写。
- 请求 ID 仍只用于 RequestContext / 请求内 memo（上下文控制）。

## 证据

- `php vendor/bin/phpunit --bootstrap app/bootstrap_phpunit.php`  
  `CachePolicyTest` + `StorefrontCacheKeyContextTest` + `KeyBuilderStorefrontTest`：41 tests / 184 assertions PASS。

## 未纳入

- `Template::$scopedInstances['req:…']` 实例分桶（非共享缓存键）。
- 本机维护模式开启，未做 WLS Worker RSS 曲线。
