# 技术方案会 — setup-upgrade-theme-hang

## 决议

Team:架构师: **同意** 双轨方案并冻结：

1. Framework Phrase：`setup:upgrade` 入口轻词典 flag → `shouldSkipHeavyLocaleDictionaryLoad`
2. Framework Setup Context：`getFromSetupVersion()` + Handle 注入
3. Theme Upgrade：`version_compare(from, VERSION, '>=')` 跳过历史重迁移

## 表态

| 席位 | 态度 |
|------|------|
| 架构师 | 同意 |
| 后端 | 同意（已实施） |
| 测试 | 同意（契约测绿） |
