# 性能复审 — fiber-ob-migrate-20260923

**席**：Team:性能检查工程师:  
**ts**：2026-09-23  
**channel**：`channel/perf-pm.md` msg-1 brief → 本纪要 + msg-2 stance  
**wave**：fiber-ob-migrate-20260923  
**result**：**pass**  
**notify_pm**：true  
**skill**：`get_skill(performance_check)` → DISABLED；已宿主 Read `dev/ai-command/ai/性能检查.md` + 薄镜像 + 本波 surfaces/contracts/架构-冻结  

## 业务特性摘要（本波）

- **面**：请求期 HTML/GD 字节捕获边界迁移（店面 Taglib/Theme include、后台静音/JSON/验证码/AiDraw），非新业务功能。  
- **热路径**：店面 `/` `/products` `/guide/returns` 渲染（含头栏 Search + LanguageSelect）与 FPC 读路径。  
- **缓存主张**：本波**不改** FPC Policy / HIT·MISS 语义；禁假 HIT、关 FPC、拆 chrome。  

## 复审范围

对照 `surfaces.md` A1–A12、B1–B4；对照 backend-pm msg-2 / theme-pm msg-2 交付清单。只读 rg + 轻量 HTTP 探针；**未**改业务、**未** reload、**未**擦脏。

## UC 对照

| UC | 证据 | 判定 |
|----|------|------|
| UC-no-process-ob-html | A/B 16 目标文件 `\bob_start\s*\(` → **0**（Slot 仅注释提及禁因） | **pass** |
| UC-fiber-api | 各点可见 `beginCapture`/`endCapture`/`discardCapture`（JSON/二进制事后用 `resetCurrent`） | **pass** |
| UC-no-drain-fiber-handler | Dictionary×2 / Login / AiDraw：`while ob_end_clean` **仅** `!Runtime::isPersistent()` else；persistent → `resetCurrent` | **pass**（允许的 FPM-only 例外） |
| UC-gd-via-fiber | Login / LocalChallengeImage / AiDrawService → Fiber 捕获 `imagepng` | **pass** |
| UC-fpc-unchanged | 见下节探针 | **pass** |
| UC-no-chrome-teardown | A/B 目标无拆壳/卸 required 药方；Chrome/SlotFiller 仅缓冲 API；店面头栏 Search/语言非空 | **pass** |
| UC-no-private-reload | 本席未执行 `server:reload`；交付方 stance 声明未触 | **pass** |
| UC-storefront-smoke | `/` `/products` `/guide/returns` → **200**；头栏标记非空 | **pass** |

## FPC 探针（本机验收 Host）

Host：`https://p05113ef3.test.weline.com:9555`

| 请求 | HTTP | `x-weline-fpc` | 备注 |
|------|------|----------------|------|
| GET `/`（暖） | 200 | **HIT** | body ~1.27MB；`Vary: Cookie` |
| GET `/products`（暖） | 200 | **HIT** | body ~1.85MB |
| GET `/guide/returns`（暖） | 200 | **HIT** | body ~0.92MB |
| GET `/?fiber-ob-perf-<ts>` 第 1 次 | 200 | （无 HIT 头） | `cache-control: private, no-store…` — 非「永远假 HIT」 |
| 同 URL 第 2 次 | 200 | **HIT** | 写入后再 HIT，语义正常 |
| GET `/products?fiber-ob-perf-<ts>` 同模式 | 200 | 第1无 HIT → 第2 HIT | 同上 |

结论：FPC **未关**；暖路径 HIT 合法；带新 query 首包非 HIT、次包 HIT → **未伪造全程 HIT**。迁缓冲未见 HIT/MISS 语义被改。

## 店面头栏抽检

`/` HTML：

- `header-search-panel` / `w-search-root` / `data-w-search` 存在（Search 面板非空壳）  
- `w-language-switcher` + `data-i18n-switcher` + 多 locale 属性存在（语言切换非空壳）  
- 计数：`header-search`≥17 · `search-panel`≥6 · `language-switch`/`data-language` 大量命中  

## A/B 目标 rg 清单（零裸 `ob_start`）

A1 Slot · A2 Chrome · A3 SlotFiller · A4 Search · A5 LanguageSelect · A6 DevToolPanel · A7 MaintenanceStatic · A8 form widget · A9 mini-cart · A10 Backend header base · A11 Dictionary · A12 RouterRunBefore · B1 Login · B2 LocalChallengeImage · B3 AiDrawService · B4 AiDraw — **全部 OK**。

FPM-only `while ob_end_clean`（允许）：Dictionary.php:1253–1254 / 1610–1611 · Login.php:823–824 · AiDraw.php:230–231（均在 `isPersistent()` else）。

## 硬禁自检

| 项 | 本席 |
|----|------|
| 假 HIT / 关 FPC | 未发现；探针反证 |
| 拆壳 / 卸 required | 未建议、未见本波拆除 |
| 私自 reload | 未执行 |
| 擦脏 / 业务大重构 | 未改业务代码 |

## 缓存合规（本波）

无新增业务平行袋 / 无改 FPC Policy / 无删依赖造 HIT。缓冲迁移落在 `FiberOutputBuffer`（与架构冻结一致）。

## verdict

**pass** — A/B 缓冲迁移复审通过；可进入汇审收口。无需 escalate 返工。

@项目经理：本席已交付/上报，请检查并更新 SESSION。
