# 架构冻结 — fiber-ob-migrate-20260923

**席**：Team:架构师:  
**ts**：2026-09-23  
**channel**：`channel/architect-pm.md` msg-1 brief → 本纪要  
**architect_joint**：true  
**result**：closed  
**notify_pm**：true  

## 背景

用户要求「全部迁移 FiberOutputBuffer」。权威：`Framework/Runtime/FiberOutputBuffer.php`（进程级 `ob_start` 在 WLS Fiber 交错下不安全）；先例开发日志 **2.5.101**（form/Taglib）与 **2.5.102**（ErrorPage/BinaryOutputGuard）。盘点 `meetings/inventory-raw.txt` + 本席补 `rg`。

## dirty-load / 补 rg 结论

1. API：`beginCapture` / `endCapture` / `discardCapture` / `hasActiveCapture` / `resetCurrent` / `install|ensureInstalled`（D 类内部）。persistent 下单进程 handler + fiber-local 帧；legacy 上层裸 ob 时走 nested native（勿在业务侧再叠裸层）。  
2. inventory 约 44 hits 覆盖主链；**漏网补录**：
   - `Backend/view/blocks/header/base.phtml`
   - `Theme/view/theme/backend/widgets/data/form/default.phtml`
   - `Theme/view/theme/frontend/widgets/header/mini-cart-icon/default.phtml`
   - 大量 Test/CLI `ob_start`（归 E/C）
   - ElFinder `connector.maximal.php-dist`（C vendored）
   - `Framework/Event/Event.php` `@ob_flush`（C CLI）
3. **Widget `AbstractParamType`**：仓内无 `ob_*` — **非漏网待迁项**（核对 PASS）。

## 冻结分类（权威落点：`surfaces.md`）

| 类 | 裁定 |
|----|------|
| **A** | Theme Slot/Chrome/SlotFiller、Search Taglib、I18n LanguageSelect、DevToolPanel、Maintenance 静态 HTML、Theme 两处 widget phtml、Backend header base、I18n Dictionary 请求期 drain/静音、MediaManager RouterRunBefore — **必须迁 Fiber** |
| **B** | Login/Captcha/AiDraw GD `imagepng` 捕获及 AiDraw 整栈 clean — **迁 Fiber**（本波**不**批「保留裸 ob」书面例外） |
| **C** | SSE flush、FpmEmitter、PcController FPM `ob_clean`、BinaryOutputGuard 已达标、CLI Setup/`Event` flush、ElFinder dist、CLI remediate 脚本 — **例外或保留**（见 surfaces 表；SSE/drain 仍受「勿拆 Fiber handler」约束） |
| **D** | `FiberOutputBuffer` 内部 — **保留** |
| **E** | 契约 UT 改禁裸 ob / 允许 Fiber；PHPUnit CLI 隔离可保留裸 ob |

## 迁移模式

统一 `FiberOutputBuffer::beginCapture` → 工作 → `endCapture`；异常 `discardCapture`。Taglib 编译字符串禁止再写 `ob_start()`。禁平行发明第二套缓冲。

## 硬禁

假 HIT、关 FPC、拆壳、私自 reload、借机业务大重构。本席**只冻结不施工**。

## suggested_seats（施工波）

1. **Team:后端:** — A4–A7、A10–A12、B 全、C harden（SSE/AiDraw drain 边界）  
2. **Team:主题开发工程师:** — A1–A3、A8–A9、E Theme Slot 契约 UT ∥ 后端  

依赖：见 `deps.md`（架构冻结 → 后端∥主题 → 性能/架构复审）。

## 耦合提示 / 需求纠偏

- 「全部迁移」≠ 拆掉 SSE/FPM/CLI 一切 `ob_*`；流式与 FPM emitter 保留例外，但请求期 HTML/GD **无例外**。  
- Dictionary / RouterRunBefore 的「清缓冲」在 WLS 下比 HTML 捕获更危险（可拆 installed handler）— 升为 **A** 而非软 C。  
- 勿为迁缓冲触碰 chrome bake / FPC Policy。
