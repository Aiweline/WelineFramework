# 汇审 — FPC 上收 Framework · Theme 旁路 · WLS 适配器

日期：2026-09-21  
模块：Framework `2.5.132` / Theme `2.2.510` / Server `2.0.59`  
计划：FPC Framework Ownership（旁路≠全部不可缓存；热路径只读侧车；Capability 只经 StoreAdapter）

## 结论

**通过。** 团队自验完成；未甩测给用户。

## 交付对照

| 计划项 | 状态 |
|--------|------|
| Extra/Capability 搬家 Framework | 完成（Server 副本已删） |
| BypassProvider + 侧车 + Evaluator 纯函数 | 完成 |
| Store 接口 + 单活跃适配器；无适配器 FPC 关 | 完成（`process` 保底 + prefer `wls`） |
| Coordinator / FastPath 共用 Evaluator | 完成 |
| Theme Provider + preview 文档 | 完成 |
| Capability 不直捅 CacheManager | 完成 + 契约 UT |
| 契约 UT / 早路径 HIT | PASS（正式 `phpunit:run`） |

## 团队自验证据（正式 runner）

1. `php bin/w extends:rebuild` → Theme Bypass + WLS Store/Bypass + Process Store 入 `generated/extends.php`
2. `FpcBypassCollector::collectAndPersist()` → **rules=4**（theme×3 + wls×1）；`active=wls`（all=`process,wls`）
3. `php bin/w phpunit:run --phpunit --name=FpcBypassAndStoreContractTest` → **4/4 PASS**
4. `… --name=ExtraTypeRegistryContractTest` → **3/3 PASS**
5. `… --name=FullPageCacheDynamicBypassTest` → **2/2 PASS**
6. `… --name=WorkerFullPageCacheFastPathTest` → **13/13 PASS**（HIT 断言保留；Performance 头随观测闸门）

## 文档

- Framework `doc/开发/FPC上收Framework与Store适配器.md`
- Theme `preview-and-runtime-modes.md` §FPC 旁路

## 非目标（本波）

- 第二存储器；Coordinator 仓读写全量迁 Store get/set；每请求 bypass 事件

## MCP 合规注记

权威：`forbid_user_manual_test_handoff` / `agent_self_verify_before_done`（见 `AI硬规则索引.md`）。验收由 Agent 团队自跑正式 `phpunit:run`，禁止「请用户跑 upgrade / 自测」。
