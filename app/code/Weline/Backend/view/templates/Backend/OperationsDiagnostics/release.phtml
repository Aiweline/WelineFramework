<?php
/** @var array<string,mixed> $diagnostics */
$escape = static fn(mixed $value): string => htmlspecialchars((string)$value, ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8');
$environment = is_array($diagnostics['environment'] ?? null) ? $diagnostics['environment'] : [];
$current = is_array($diagnostics['current'] ?? null) ? $diagnostics['current'] : [];
$core = is_array($diagnostics['core'] ?? null) ? $diagnostics['core'] : [];
?>
<div class="w-container" data-testid="operations-release-diagnostics">
    <div class="w-cluster" data-align="center" data-justify="between" style="--w-mb:var(--weline-space-4);">
        <div>
            <h4 style="--w-mb:var(--weline-space-1);"><lang>发布诊断</lang></h4>
            <div class="w-text" data-tone="muted"><lang>只读查看当前发布标记；不提供发布、回滚或 Webhook 回放操作。</lang></div>
        </div>
        <span class="w-badge" data-w-background="muted"><lang>只读</lang></span>
    </div>

    <div class="w-card" style="--w-mb:var(--weline-space-4);"><div class="w-card__body">
        <div class="w-grid" style="--w-gap:var(--weline-space-4);">
            <div style="--w-span-md:4;"><strong><lang>部署模式</lang></strong><div><?= $escape($environment['deploy_mode'] ?? '') ?></div></div>
            <div style="--w-span-md:4;"><strong><lang>发布版本</lang></strong><div><?= $escape($environment['deploy_version'] ?? '') ?></div></div>
            <div style="--w-span-md:4;"><strong>Worker Build ID</strong><div><?= $escape($environment['worker_build_id'] ?? '') ?></div></div>
        </div>
    </div></div>

    <div class="w-card" style="--w-mb:var(--weline-space-4);">
        <div class="w-card__header"><lang>当前发布标记</lang></div>
        <div class="w-card__body">
            <?php if (empty($diagnostics['current_marker_present'])): ?>
                <div class="w-text" data-tone="muted"><lang>未找到当前发布标记文件</lang></div>
            <?php else: ?>
                <div class="w-table-wrap"><table class="w-table" style="--w-mb:0;"><tbody>
                <?php foreach ($current as $key => $value): ?>
                    <tr><th><?= $escape($key) ?></th><td><?= $escape($value) ?></td></tr>
                <?php endforeach; ?>
                </tbody></table></div>
            <?php endif; ?>
        </div>
    </div>

    <div class="w-card"><div class="w-card__header"><lang>内核版本通知状态</lang></div><div class="w-card__body">
        <div class="w-grid" style="--w-gap:var(--weline-space-4);">
            <div style="--w-span-md:6;"><strong><lang>检查值</lang></strong><div><?= $escape($core['check'] ?? '') ?></div></div>
            <div style="--w-span-md:3;"><strong><lang>通知指纹</lang></strong><div><?= $escape($core['last_notified_fingerprint'] ?? '') ?></div></div>
            <div style="--w-span-md:3;"><strong><lang>通知时间</lang></strong><div><?= $escape($core['last_notified_at'] ?? '') ?></div></div>
        </div>
    </div></div>
</div>
