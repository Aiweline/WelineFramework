<css>Weline_Backend::css/backend-account-shell.css</css>
<div class="w-backend-account">
    <?php echo $this->fetch('Weline_Backend::templates/Backend/Profile/partials/sidebar.phtml'); ?>
    <section class="w-backend-account__main">
        <div class="w-backend-account__panel">
            <p class="w-backend-account__panel-desc"><lang>只改用户名和邮箱。头像、语言、密码请到左侧对应栏目。</lang></p>
            <form method="post" action="@backend-url('system/backend/profile')" class="w-stack" data-align="start" style="--w-gap:var(--weline-space-4);">
                <div class="w-field">
                    <label class="w-field__label" for="profile-username"><lang>用户名</lang></label>
                    <input
                        class="w-input"
                        type="text"
                        id="profile-username"
                        name="username"
                        value="<?= htmlspecialchars((string)($profile_username ?? ''), ENT_QUOTES, 'UTF-8') ?>"
                        required
                        autocomplete="username"
                    />
                </div>
                <div class="w-field">
                    <label class="w-field__label" for="profile-email"><lang>邮箱</lang></label>
                    <input
                        class="w-input"
                        type="email"
                        id="profile-email"
                        name="email"
                        value="<?= htmlspecialchars((string)($profile_email ?? ''), ENT_QUOTES, 'UTF-8') ?>"
                        required
                        autocomplete="email"
                    />
                </div>
                <button type="submit" class="w-button" data-tone="primary" data-size="sm"><lang>保存</lang></button>
            </form>
        </div>
    </section>
</div>
