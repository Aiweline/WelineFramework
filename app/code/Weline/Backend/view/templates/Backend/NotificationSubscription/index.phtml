<?php
/**
 * @var \Weline\Framework\View\Template $this
 * @var array $topics_grouped
 * @var array $subscription_map
 * @var array $channels
 * @var array $types
 * @var array $user_contacts
 */
$topics_grouped = $topics_grouped ?? [];
$subscription_map = $subscription_map ?? [];
$channels = $channels ?? [];
$types = $types ?? [];
$user_contacts = $user_contacts ?? [];
?>

<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
            <h4 class="w-text" style="--w-mb-sm:0;--w-font-size:1.125rem;"><lang>消息订阅</lang></h4>
            <div>
                <a href="<?= $this->getBackendUrl('*/backend/contact') ?>" class="w-button" data-tone="primary" data-variant="outline">
                    <w:icon name="user" size="sm"></w:icon><lang>管理联系人</lang>
                </a>
            </div>
        </div>
    </div>
</div>

<div class="w-grid">
    <div style="--w-span:12;">
        <template>Weline_Component::message.phtml</template>
    </div>
</div>

<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-card">
            <div class="w-card__body">
                <div class="w-alert weline-subscription-tip" data-tone="info">
                    <w:icon name="info" size="sm"></w:icon>
                    <lang>选择您希望通过哪些渠道接收不同类型的消息通知。外部渠道需要先在联系人里配置对应渠道信息。</lang>
                    <a href="<?= $this->getBackendUrl('*/backend/contact') ?>"><lang>管理联系人</lang></a>
                </div>

                <?php if (empty($topics_grouped)): ?>
                    <div class="weline-subscription-empty w-text" data-align="center" style="--w-pt:var(--weline-space-12);--w-pb:var(--weline-space-12);">
                        <w:icon name="bell" size="sm"></w:icon>
                        <p style="--w-mt:var(--weline-space-4);"><lang>暂无可订阅的消息主题</lang></p>
                    </div>
                <?php else: ?>
                    <?php foreach ($topics_grouped as $groupName => $groupData): ?>
                        <?php $groupTopics = $groupData['topics'] ?? []; $groupDisplayName = $groupData['group_name'] ?? $groupName; ?>
                        <div class="weline-subscription-group" style="--w-mb:var(--weline-space-6);">
                            <h5 class="weline-subscription-group-title">
                                <w:icon name="folder" size="sm"></w:icon><?= htmlspecialchars((string) $groupDisplayName) ?>
                            </h5>

                            <div class="w-table-wrap">
                                <table class="w-table weline-subscription-table">
                                    <thead>
                                    <tr>
                                        <th style="width: 30%"><lang>消息主题</lang></th>
                                        <?php foreach ($channels as $channelCode => $channelInfo): ?>
                                            <th class="w-text" style="width: <?= (70 / max(count($channels), 1)) ?>%" data-align="center">
                                                <i class="mdi <?= htmlspecialchars($channelInfo['icon'] ?? 'circle', ENT_QUOTES, 'UTF-8') ?> me-1"></i><?= htmlspecialchars((string) ($channelInfo['name'] ?? $channelCode), ENT_QUOTES, 'UTF-8') ?>
                                                <?php if ($channelCode !== 'backend'): ?>
                                                    <?php $hasContact = !empty($user_contacts[$channelCode]); ?>
                                                    <br>
                                                    <small class="<?= $hasContact ? 'text-success' : 'text-warning' ?>">
                                                        <?= $hasContact ? __('已配置联系人') : __('未配置联系人') ?>
                                                    </small>
                                                <?php endif; ?>
                                            </th>
                                        <?php endforeach; ?>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    <?php foreach ($groupTopics as $topic): ?>
                                        <tr>
                                            <td>
                                                <div class="w-cluster" data-align="center">
                                                    <?php if (!empty($topic['icon'])): ?>
                                                        <i class="<?= htmlspecialchars((string) $topic['icon'], ENT_QUOTES, 'UTF-8') ?> me-2" style="color: <?= htmlspecialchars((string) ($topic['color'] ?? 'inherit'), ENT_QUOTES, 'UTF-8') ?>"></i>
                                                    <?php endif; ?>
                                                    <div>
                                                        <strong><?= htmlspecialchars((string) ($topic['topic_name'] ?? ''), ENT_QUOTES, 'UTF-8') ?></strong>
                                                        <?php if (!empty($topic['description'])): ?>
                                                            <br><small class="w-text" data-tone="muted"><?= htmlspecialchars((string) $topic['description'], ENT_QUOTES, 'UTF-8') ?></small>
                                                        <?php endif; ?>
                                                    </div>
                                                </div>
                                            </td>
                                            <?php foreach ($channels as $channelCode => $channelInfo): ?>
                                                <?php
                                                $topicCode = (string) ($topic['topic_code'] ?? '');
                                                $subKey = $topicCode . '_' . $channelCode;
                                                $sub = $subscription_map[$subKey] ?? null;
                                                $isEnabled = $sub ? (bool) ($sub['is_enabled'] ?? false) : false;
                                                $minType = $sub ? (string) ($sub['min_type'] ?? 'info') : 'info';
                                                ?>
                                                <td class="w-text weline-subscription-cell" data-align="center">
                                                    <div class="w-switch" data-w-display="inline-block">
                                                        <input class="weline-subscription-toggle" type="checkbox" <?= $isEnabled ? 'checked' : '' ?>
 data-topic="<?= htmlspecialchars($topicCode, ENT_QUOTES, 'UTF-8') ?>" data-channel="<?= htmlspecialchars((string) $channelCode, ENT_QUOTES, 'UTF-8') ?>">
                                                    </div>
                                                    <?php if ($isEnabled): ?>
                                                        <div style="--w-mt:var(--weline-space-1);">
                                                            <select class="w-select weline-subscription-min-type" data-topic="<?= htmlspecialchars($topicCode, ENT_QUOTES, 'UTF-8') ?>" data-channel="<?= htmlspecialchars((string) $channelCode, ENT_QUOTES, 'UTF-8') ?>" style="font-size: 0.75rem; padding: 0.1rem 1.5rem 0.1rem 0.25rem;" data-size="sm">
                                                                <?php foreach ($types as $value => $label): ?>
                                                                    <option value="<?= htmlspecialchars((string) $value, ENT_QUOTES, 'UTF-8') ?>" <?= $value === $minType ? 'selected' : '' ?>>
                                                                        <?= __('≥') ?> <?= htmlspecialchars((string) $label, ENT_QUOTES, 'UTF-8') ?>
                                                                    </option>
                                                                <?php endforeach; ?>
                                                            </select>
                                                        </div>
                                                    <?php endif; ?>
                                                </td>
                                            <?php endforeach; ?>
                                        </tr>
                                    <?php endforeach; ?>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    <?php endforeach; ?>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<style>
.weline-subscription-tip {
    background: var(--backend-color-info-bg, #cff4fc);
    border-color: var(--backend-color-info-border, #9eeaf9);
    color: var(--backend-color-info-text, #055160);
}
.weline-subscription-empty-icon {
    font-size: 4rem;
    color: var(--backend-color-text-muted, #6c757d);
}
.weline-subscription-group-title {
    padding: 0.75rem 0;
    border-bottom: 2px solid var(--backend-color-primary, #556ee6);
    color: var(--backend-color-text-primary, #212529);
    margin-bottom: 1rem;
}
.weline-subscription-table {
    background: var(--backend-color-card-bg, #fff);
}
.weline-subscription-table th {
    background: var(--backend-color-card-header-bg, #f8f9fa);
    color: var(--backend-color-text-primary, #212529);
    font-weight: 600;
}
.weline-subscription-table td {
    vertical-align: middle;
}
.weline-subscription-cell {
    min-width: 120px;
}
.weline-subscription-toggle {
    width: 2.5rem;
    height: 1.25rem;
}
.weline-subscription-min-type {
    max-width: 100px;
    margin: 0 auto;
}
</style>


<script>
(function(g){
  if(g.__bqAdmin_backend_admin) return;
  g.__bqAdmin_backend_admin=true;
  g.bqAdmin=g.bqAdmin||{};
  g.bqAdmin['backend_admin']=function(url, options){
    options=options||{};
    var body=options.body;
    if(body && typeof FormData!=='undefined' && body instanceof FormData){
      var p=new URLSearchParams(); body.forEach(function(v,k){ if(!(typeof File!=='undefined'&&v instanceof File)) p.append(k,String(v)); }); body=p.toString();
    } else if(body && typeof body!=='string'){ try{ body=JSON.stringify(body); }catch(e){ body=''; } }
    var run=function(api){ return api.resource('backend_admin').adminRequest({url:url, method:options.method||'POST', headers:options.headers||{}, body:body||''}); };
    var toResp=function(data){
      var _biz=g.WelineApiBusiness||(g.Weline&&g.Weline.ApiBusiness);
      if(_biz&&typeof _biz.wrapAdminBridgeResult==='function'){
        return _biz.wrapAdminBridgeResult(data);
      }
      var body=(data&&typeof data==='object'&&!Array.isArray(data))?data:{success:true,data:data};
      var ok=!(body&&body.success===false);
      var resp={ok:ok,status:ok?200:400,json:function(){return Promise.resolve(body);},text:function(){return Promise.resolve(typeof body==='string'?body:JSON.stringify(body==null?{}:body));}};
      Object.keys(body).forEach(function(k){ if(k==='ok'||k==='json'||k==='text'||k==='status') return; resp[k]=body[k]; });
      return resp;
    };
    var p=(g.Weline&&g.Weline.load)?g.Weline.load('api').then(run):Promise.resolve(run(g.Weline.Api));
    return p.then(toResp);
  };
})(window);
</script>
<script>
(function() {
    'use strict';

    var WelineSubscription = {
        toggle: function(checkbox) {
            var topicCode = checkbox.dataset.topic;
            var channel = checkbox.dataset.channel;

            bqAdmin['backend_admin']('<?= $this->getBackendUrl('*/backend/notificationsubscription/toggle') ?>', {
                method: 'POST',
                headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                body: 'topic_code=' + encodeURIComponent(topicCode) + '&channel=' + encodeURIComponent(channel)
            })
            .then(function(response) {
                var data = (function(r){ var b=window.WelineApiBusiness||(window.Weline&&window.Weline.ApiBusiness); return (b&&typeof b.unwrapBusiness==='function')?b.unwrapBusiness(r):r.data; })(response);
                if (response.ok && data && data.success) {
                    Weline.UI.toast.success(data.message);
                    setTimeout(function() { window.location.reload(); }, 500);
                } else {
                    Weline.UI.toast.error((data && data.message) || '<?= __('操作失败') ?>');
                    checkbox.checked = !checkbox.checked;
                }
            })
            .catch(function(error) {
                console.error('Error:', error);
                Weline.UI.toast.error((error && error.message) || '<?= __('操作失败') ?>');
                checkbox.checked = !checkbox.checked;
            });
        },

        saveMinType: function(select) {
            var topicCode = select.dataset.topic;
            var channel = select.dataset.channel;
            var minType = select.value;

            bqAdmin['backend_admin']('<?= $this->getBackendUrl('*/backend/notificationsubscription/save') ?>', {
                method: 'POST',
                headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                body: 'topic_code=' + encodeURIComponent(topicCode) +
                    '&channel=' + encodeURIComponent(channel) +
                    '&min_type=' + encodeURIComponent(minType) +
                    '&is_enabled=1'
            })
            .then(function(response) {
                var data = (function(r){ var b=window.WelineApiBusiness||(window.Weline&&window.Weline.ApiBusiness); return (b&&typeof b.unwrapBusiness==='function')?b.unwrapBusiness(r):r.data; })(response);
                if (response.ok && data && data.success) {
                    Weline.UI.toast.success(data.message);
                } else {
                    Weline.UI.toast.error((data && data.message) || '<?= __('保存失败') ?>');
                }
            })
            .catch(function(error) {
                console.error('Error:', error);
                Weline.UI.toast.error((error && error.message) || '<?= __('保存失败') ?>');
            });
        }
    };

    window.WelineSubscription = WelineSubscription;

    document.addEventListener('change', function(event) {
        if (event.target.matches('.weline-subscription-toggle')) {
            WelineSubscription.toggle(event.target);
            return;
        }

        if (event.target.matches('.weline-subscription-min-type')) {
            WelineSubscription.saveMinType(event.target);
        }
    });
})();
</script>
