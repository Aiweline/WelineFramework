<?php
/**
 * @var \Weline\Framework\View\Template $this
 * @var string $error
 */

$error = (string)($error ?? __('发生错误'));
$listUrl = $this->getBackendUrl('system/backend/notification');
$escape = static fn(mixed $value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
?>
<link rel="stylesheet" href="@static(Weline_Backend::css/notification-center.css)">

<section class="w-notification-error w-stack" data-w-component="notification-center" data-w-mode="error">
    <header class="w-notification-detail__chrome">
        <a href="<?= $escape($listUrl) ?>" class="w-notification-detail__back"><w:icon name="arrow-left" size="sm" /><lang>返回通知中心</lang></a>
    </header>

    <div class="w-card w-notification-error__card">
        <div class="w-card__body">
            <div class="w-empty-state w-notification-error__content" role="alert">
                <span class="w-notification-error__icon"><w:icon name="warning" size="xl" /></span>
                <h2><?= $escape($error) ?></h2>
                <a href="<?= $escape($listUrl) ?>" class="w-button" data-tone="primary"><w:icon name="arrow-left" size="sm" /><lang>返回通知中心</lang></a>
            </div>
        </div>
    </div>
</section>
