<?php
/**
 * 访问日志列表页面（内容模板）
 * 
 * 只包含页面内容，由布局系统自动包裹 HTML 框架
 * 布局由 BackendController::$layoutType = 'default.default' 控制
 *
 * @var \Weline\Framework\View\Template $this
 * @var array $logs
 * @var int $total
 * @var int $page
 * @var int $limit
 * @var int $total_pages
 * @var string $keyword
 */
$logs = $logs ?? [];
$total = (int)($total ?? 0);
$page = (int)($page ?? 1);
$limit = (int)($limit ?? 20);
$totalPages = (int)($total_pages ?? 0);
$keyword = $keyword ?? '';
?>
<!-- 页面标题 -->
<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
            <h4 class="w-text" style="--w-mb-sm:0;--w-font-size:1.125rem;"><lang>访问日志</lang></h4>
        </div>
    </div>
</div>

<!-- 消息提示区域 -->
<div class="w-grid">
    <div style="--w-span:12;">
        <template>Weline_Component::message.phtml</template>
    </div>
</div>

<!-- 提示信息 -->
<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-alert" data-tone="info">
            <w:icon name="info" size="sm"></w:icon>
            <strong><lang>提示</lang>:</strong> <lang>访问日志功能正在开发中，敬请期待。</lang>
        </div>
    </div>
</div>

<!-- 搜索 -->
<div class="w-grid">
    <div style="--w-span-lg:12;">
        <div class="w-card">
            <div class="w-card__body">
                <w:form method="get" action="@var($this->getBackendUrl('*/backend/access-log'))" class="w-grid" style="--w-gap:var(--weline-space-4);">
                    <div style="--w-span-md:9;">
                        <label class="w-field__label"><lang>关键词</lang></label>
                        <input type="text" class="w-input" name="keyword" value="<?= htmlspecialchars($keyword) ?>" placeholder="<?= __('搜索IP地址或URL') ?>">
                    </div>
                    <div style="--w-span-md:3;">
                        <label class="w-field__label">&nbsp;</label>
                        <div>
                            <button type="submit" class="w-button" data-tone="primary">
                                <w:icon name="search" size="sm"></w:icon><lang>搜索</lang>
                            </button>
                            <a href="<?= $this->getBackendUrl('*/backend/access-log') ?>" class="w-button" data-tone="neutral">
                                <w:icon name="refresh" size="sm"></w:icon><lang>重置</lang>
                            </a>
                        </div>
                    </div>
                </w:form>
            </div>
        </div>
    </div>
</div>

<!-- 日志列表 -->
<div class="w-grid">
    <div style="--w-span-lg:12;">
        <div class="w-card">
            <div class="w-card__body">
                <h4 class="w-card__title" style="--w-mb:var(--weline-space-6);"><lang>日志列表</lang></h4>
                
                <?php if (!empty($logs)): ?>
                <div class="w-table-wrap">
                    <table class="w-table" data-w-vertical="middle" style="--w-mb:0;">
                        <thead>
                            <tr>
                                <th><lang>时间</lang></th>
                                <th><lang>IP地址</lang></th>
                                <th><lang>URL</lang></th>
                                <th><lang>状态码</lang></th>
                                <th><lang>User Agent</lang></th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($logs as $log): ?>
                            <tr>
                                <td><?= $log['time'] ?? '-' ?></td>
                                <td><?= htmlspecialchars($log['ip'] ?? '-') ?></td>
                                <td><?= htmlspecialchars($log['url'] ?? '-') ?></td>
                                <td><?= $log['status_code'] ?? '-' ?></td>
                                <td><?= htmlspecialchars($log['user_agent'] ?? '-') ?></td>
                            </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
                <?php else: ?>
                <div class="w-text" data-align="center" data-tone="muted" style="--w-pt:var(--weline-space-12);--w-pb:var(--weline-space-12);">
                    <w:icon name="file" size="sm"></w:icon>
                    <p style="--w-mt:var(--weline-space-4);"><lang>暂无访问日志记录</lang></p>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
