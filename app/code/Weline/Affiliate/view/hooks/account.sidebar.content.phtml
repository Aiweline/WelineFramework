<?php
/**
 * Account sidebar content bridge.
 *
 * Hook: account.sidebar.content
 *
 * @hook-priority 165
 * @hook-sort-order 18
 * @hook-solo false
 */
use Weline\Customer\Service\AccountSidebarContentGate;
use Weline\Framework\Runtime\RequestContext;

if (!AccountSidebarContentGate::accepts('affiliate')) {
    return;
}

$renderKey = 'weline_affiliate.account_sidebar_content.rendered';
if (RequestContext::get($renderKey, false)) {
    return;
}

RequestContext::set($renderKey, true);
?>
<section class="account-hook-section account-index__section motor-account-section" id="affiliate-section" data-account-section="affiliate" hidden weline-code="affiliate.account.sidebar">
    <?= $this->getHook('Weline_Affiliate::frontend::account::index::affiliate') ?>
</section>
