<!-- weline:module-development-baseline:generated-once -->
# Weline_B2B 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 2026-09-21 — PDP/body-end 游客 SSR + 交互才拉会员（2.6.86）

- SSR：`selling-mode-switcher` / `qty-tiers` / `body-end` 固定 guest toc，不读 Session。
- `selling-mode.js`：去掉 `visibilitychange`/`enhanceMiniCarts({refresh:true})` 静默联网；本地读 Account 快照；需登录调 `ensureLogin`。
- 版本：`2.6.85` → `2.6.86`。

## 2026-09-15 — 结账 chrome 同步打断 MutationObserver 自激

- `notifyCreditChanged` 按 apply/cash/cart_type 签名去重。
- `selling-mode`：textContent 未变不写；chrome sync 后短暂抑制 MutationObserver → enhanceMiniCarts，避免 credit↔totals↔observer 环刷 event-picker。

## 2026-09-14 — order-note 契约对齐 WidgetI18n（2.6.84）

- 结账顶栏 order-note 已用 `WidgetI18n::label`；契约断言由 `__()` 改为 `$t()` / WidgetI18n。

## 2026-09-14 — 批发信用 data-i18n 九语（2.6.83）

- `当前是零售结账…` / `暂时无法获取批发信用额度` 多语 identity；九语 upsert + collect。

## 2026-09-14 — 「批发信用说明」九语补齐（2.6.82）

- aria 多语 identity（除 hi）；九语 upsert + collect。

## 2026-09-14 — hi_IN 结账批发信用中文泄漏（2.6.80）

- 结账顶部 order-note / 信用页签 deposit-note 的 `<lang>` 编译期烘焙中文；`hi_IN` 多条信用 chrome 仍为中文身份。
- 改：模板改运行时 `__()` + htmlspecialchars；补印地语词条；契约断言无 `<lang>`。

## 2026-09-14 — 迷你车零售/批发页签漏译（2.6.79）

- 阿语等语种迷你车仍显示 Retail cart / Wholesale cart / Wholesale membership（回落 en_US）。
- `hi_IN.csv` 批发身份不再 identity；词典全语种 upsert 零售车/批发车/批发身份。

## 2026-09-14 — 非批发商品加购进零售车（2.6.77）

- 需求/审图 `ui_shot`：tob cookie 下非批发 PDP 加购进了「批发车」。
- 变更：`CommerceCartOfferRoutingInterface` + `B2BCartOfferRouting`；`CartService::add` remap；FE `productAllowsWholesaleAdd` / `syncChromeAfterRetailOnlyAdd`；`cart-updated` 优先摘要 `cart_type`；更新 REQ-B2B-0012。
- 验收：OfferRouting/Cart/ThemeUi UT 16 passed；Browser 禁缓存：`preferred→toc`、迷你车标题「零售车」、toc getCart 含刚加购「思无邪」行（原 bug 长安忆仍残留在 tob 历史车）。

## 2026-09-14 — 撤回加购弹窗隐藏分销分享（Affiliate 冲突）

- 审图：`ui_shot`（规格加购弹窗无「分销分享」）
- 纠偏：Affiliate 需求要求弹窗与 PDP 同展示；B2B `b2b-storefront.css` 此前「藏 affiliate noise」违约
- 变更：删除 `.w-product-purchase-panel` / `--quick-add` 对分销面板的 `display:none`
- 契约：`B2BStorefrontThemeUiContractTest` 断言不得再出现隐藏选择器

## 2026-09-14 — 删除双模式加购选车弹窗（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（「添加到哪个购物车？」）。
- 结论：详情页已有「购买方式」零售/批发，加购再弹选车多余。
- 变更：移除 `b2b-cart-type-chooser` dialog、`confirmCartTypeForAdd`、`syncDualModeAddGate`；Cart 加购不再 await 选车；直接按当前 selling-mode 入车。
- 验收：ThemeUi/Cart 契约 UT；e2e 断言无选车接线；Browser 加购不弹选车。

## 2026-09-14 — 批发信用小计与车底不一致（2.6.76）

- 审图 `ui_shot`：迷你车勾选批发信用后，说明写「商品小计 ¥660」、定金 ¥198，底部「小计」却是 ¥53。
- 根因：无优惠时隐藏的 `data-cart-goods-subtotal` 不随车刷新；`readGoodsSubtotalMajor` 误取过期大额。
- 修：Theme/Cart 每次摘要都写商品小计 + `data-cart-goods-subtotal-major`；B2B 优先读权威属性/可见应付，说明文案用实时定金基数；`cart-updated` 带 subtotal 重估报价。
- 验收：契约 UT；Browser 禁缓存核对信用说明小计=底部小计。

## 2026-09-14 — 结账信用页签旁空按钮（2.6.75）

- 审图 `ui_shot`：用户圈注摘要 extras 空胶囊，误以为「信用折扣」坏了。
- 根因：Checkout `checkout-summary-help-pay` 空槽仍参与 `mini-cart-extras-tabs` 建页签 → `tabLabel=''`。
- 修：Theme `isExtrasTabCandidate` 跳过空/全隐藏槽；tob 仍见「批发信用」文案页签。
- 验收：契约 UT；e2e `B2B-CREDIT-TOB-ONLY` 增无空标签断言；Browser 禁缓存。

## 2026-09-12 — 定金挂单瀑布卡紧凑密度

- 需求/审图：卡过高，地址多行、金额上下叠、商品默认展开。
- 变更：地址单行摘要、金额同行、商品默认折叠、沟通预览与按钮同行、缩小间距；过滤「?」预览。
- 验收：UT AccordionContract compact markers；E2E hang-waterfall；Browser 禁缓存。

## 2026-09-12 — 定金挂单瀑布卡内联展开沟通

- 需求/审图：订单沟通仅为按钮，需卡内展开直接聊天。
- 变更：沟通区移出商品 details；未读 `data-open` 自动展开；传入 `customer_id`；面板样式强化。
- 验收：UT AccordionContract；E2E hang-waterfall 展开面板；Browser 禁缓存。

## 2026-09-12 — 定金挂单瀑布流审核台

- 需求/审图：网格卡信息不足，批准缺上下文；需客户/金额/商品/沟通/地址风险 + Tab。
- 变更：`loadHangRows` 先 status/scope 再 limit；`B2BHangAdminListPresenter` 投影；单列瀑布+Tab 计数；禁运/偏远严重度；默认等待审批全展开。
- 验收：UT Presenter + AccordionContract OK；E2E hang-waterfall 1 passed；Browser 禁缓存见等待审批 Tab 计数与瀑布展开卡。

## 2026-09-12 — 定金挂单密列去右侧留白

- 需求/审图：挂单卡满宽右侧空洞；单列窄卡又把空洞挪到页面右侧。
- 变更：工作台 `auto-fill` 多列（`minmax(28rem,1fr)`）铺满主栏；页内 `hang-dense7` 抗缓存；按钮单行。
- 验收：UT dense7；E2E hang-admin-chat；Browser 多列/无右侧大空。

## 2026-09-12 — 双模式加购选车（零售加购提示零售/批发）

- 需求/审图 `ui_shot`：批发+零售同时开启时，零售态「加入购物车」需提示加入零售车还是批发车。
- 变更：`selling-mode-switcher` 增加 `w-dialog` 选车；`confirmCartTypeForAdd`；Cart `product-purchase-actions` 可选 await（无 B2B 不变）。
- 验收：ThemeUi/Cart 契约 UT；e2e；Browser 558 零售态加购弹出选车。

## 2026-09-12 — 双模式加购选车（零售加购提示零售/批发）

- 需求/审图 `ui_shot`：批发+零售同时开启时，零售态「加入购物车」需提示加入零售车还是批发车。
- 变更：`selling-mode-switcher` 增加 `w-dialog` 选车；`confirmCartTypeForAdd`；Cart `product-purchase-actions` 前置 await；`syncDualModeAddGate` 零售缺货仍可点；`pub/static` 需同步。
- 验收：ThemeUi/Cart 契约 UT；e2e；Browser 558 零售态弹出「添加到哪个购物车？」。

## 2026-09-12 — 未配置批发不展示购买方式「批发」

- 需求/审图 `ui_shot`：产品未设置批发仍见「批发」切换。
- 根因：`ProductWholesaleEligibility` 在无 SKU 价目档时仍用站点默认 VIP 模板放行。
- 变更：展示门禁仅认 `selling_mode_tob` + `skuHasActiveTiers`；无档则 switcher early-return、阶梯区不渲染。
- 验收：UT Eligibility；wholesale-display-gate fixture/e2e；Browser 无档商品无「批发」。

## 2026-09-12 — 每件低补 ¥ + 省额色凸显

- 需求/审图 `ui_shot`：「每件低 10.80」无符号且偏灰；「省」已有 ¥。
- 变更：`每件低 %{1} %{2}` 带货币符号；delta 用 `--color-success`/`--weline-theme-success`（禁止回退到次要文本色）。
- 验收：ThemeUi/fixture；Browser 见「每件低 ¥ …」绿色 +「省 ¥ …」主色。

## 2026-09-12 — 批发阶梯价货币用符号（¥ 非 CNY）

- 需求/审图 `ui_shot`：阶梯区写 CNY，主价已是 ¥，不一致。
- 变更：`qty-tiers.phtml` 与 product-info 同映射（CNY/RMB→¥）；单价/省额用符号。
- 验收：UT ThemeUi；Browser 批发档可见「¥ …」无 CNY。

## 2026-09-12 — 批发阶梯「每件低」对零售（首档也有）

- 需求/审图 `ui_shot`：裸括号（−0.80）含义不清；首档 5 件也有相对零售优惠却无提示。
- 变更：去掉较上档差；每档（含首档）显示「每件低 x.xx」（零售−档单价）+「省」合计；`data-unit-save-minor`。
- 验收：UT ThemeUi；fixture/e2e；Browser VIP1 可见「每件低 2.20」。

## 2026-09-12 — 批发档差贴单价旁（去「再低」）

- 需求/审图 `ui_shot`：「再低」不直观，档差应写在每件价旁边。
- 原型：`prototype-qty-tier-delta.html` 三变体，落地 A：`18.94/件（−0.80）` + `省 …`。
- 变更：`qty-tiers.phtml` 档差并入 `__price` 旁 `__delta`；删独立「再低」；CSS Token success；i18n/契约/fixture。
- 验收：UT ThemeUi；fixture/e2e；Browser VIP1 可见单价旁（−x）。

## 2026-09-12 — 批发阶梯文案精简（一眼省多少）

- 需求/审图 `ui_shot`：「买 N 件共省 / 较上档低」过长，要直接说省多少并精简其余。
- 变更：`qty-tiers.phtml` →「起订·步进」「N 件 价」「省 额」「再低 额」；省用 primary、再低用 success Token。
- 验收：UT ThemeUi；fixture/e2e；Browser VIP1 可见「省 CNY …」。

## 2026-09-12 — PDP 批发阶梯「按数量共省」提示

- 需求/审图 `ui_shot`：阶梯区仅有「较上档低」单价差，缺按档数量合计节省。
- 变更：`qty-tiers.phtml` 以（零售−档单价）×档起订量输出「买 N 件共省」；`data-total-save-minor`；CSS 共省用 success Token、较上档低降为次要；i18n + ThemeUi 契约。
- 验收：UT ThemeUi；Browser/Playwright PDP 批发档可见共省文案。

## 2026-09-12 — 定金挂单卡片宽度与按钮单行

- 需求/审图：卡片过宽中间留白大；批准/驳回与订单沟通 2 上 1 下不合理。
- 变更：工作台 `max-width:40rem`（内联兜底抗静态缓存）；页脚左起横排「批准·驳回·订单沟通」+「后台不收款」；展开沟通面板占满卡宽。
- 验收：UT OrderChatAccordionContractTest；E2E hang-admin-chat；Browser 量得卡宽 640px、三钮同行。

## 2026-09-12 — 定金挂单界面重设计（审批工作台）

- 需求/审图 `ui_shot`：密表机器字段名、分单位、沟通挤在操作列；原型三变体重设计。
- 原型：`view/statics/prototype-hang-orders-redesign.html`（A 工作台卡 / B 主从 / C 人读表）。
- 落地 A：`b2b-hang-workbench` 卡片；状态徽标；定金/尾款 CNY 元；沟通仍在卡内操作区。
- 验收：UT/E2E hang-admin-chat；Browser 禁缓存。

## 2026-09-12 — 定金挂单沟通收进行内操作区

- 需求/审图 `ui_shot`：订单沟通独立 colspan 行漂在 hang_id 下；原型改到行内。
- 变更：沟通手风琴移入 `b2b-hang-order-ops`（批准/驳回下方）；删 `b2b-hang-order-chat-row`；CSS 行内对齐。
- 验收：UT/E2E hang-admin-chat；Browser 禁缓存操作列内可见「订单沟通」。

## 2026-09-12 — 定金挂单挂载商家沟通 + 订单详情 Widget 注入

- 需求/审图：批发聊天挂在「定金挂单」；订单侧用批发部件 default_injections。
- 变更：ControlCenter hang-orders 行下商家手风琴；`b2b-backend-order-chat` 部件 → `backend-order-b2b-chat` 空槽；废弃 after Hook 直出。
- 验收：UT OrderChatAccordion 5/5；E2E hang-admin-chat + hang-order-chat-slot 2 passed；Browser 禁缓存 hang-orders 可见「订单沟通」与未读角标。

## 2026-09-12 — 批发身份挂单卡原型调整（去重沟通 CTA）

- 需求/审图 `ui_shot`：挂单卡双「订单沟通」；已完成徽标旁未读与待付叙事混杂。
- 变更：卡片主区支付/查看订单；沟通仅手风琴开关+未读角标；状态提示文案；completed「挂单已完成 / 定金已付 / 尾款已结」。
- 验收：UT IdentityHub；E2E identity-hub；Browser `#b2b-identity`。

## 2026-09-12 — 尾款页 completed 结清态（消解「已完成/不可支付」矛盾）

- 需求/审图 `ui_shot`：徽标「已完成」+「不可支付」+「支付尾款」叙事冲突。
- 变更：`paymentContext.view_state`（payable/completed/revision_pending/blocked）；completed 文案改为结清；JS `applyHangNonPayableUi`（已付尾款、成功色状态、CTA 返回批发身份）；徽标「挂单已完成」。
- 验收：UT OrderSummary/ThemeUi；E2E hang-balance-completed + revise-gate；Browser 真单 e7cf7a15。

## 2026-09-12 — 订单沟通手风琴原型 UI 收口

- 审图 `ui_shot`：展开后沟通区与挂单状态胶囊并排漂浮，「订单沟通」像贴在面板角上的品牌胶囊。
- 变更：挂单 `meta` 行与沟通区纵向分栏；toggle「订单沟通 / 收起沟通」；气泡商家左/客户右 + surface-muted 消息井；主题 Token。
- 验收：契约 UT；Browser `#orders` 禁缓存。

- 需求：前端订单沟通与后台对齐；沟通在订单记录下手风琴展开。
- 变更：`order-chat-accordion` partial + JS；hang/身份枢纽/后台 `order/view::after` Hook；`orderChat.open/messages/send/markSeen` 的 `auth=any`（后台 worker 可达）+ `FrontendWorkerExecutionContext::AREA_BACKEND` 判定商家（避免同浏览器后台 Cookie 污染前台）；`b2bOrderChat` 模块登记。
- 验收：契约 UT；Browser 账户订单 `857f745e…` 与后台 `order/view?id=59` 同线程 `th_bb2fdd440d92987a3c36`（客户/商家角色正确）。

## 2026-09-12 — 商品 558 真实递减批发阶梯价 + PDP 档差可读

- 需求/审图 `ui_shot`：PDP 批发四档同价 19.74，阶梯不可辨识。
- 变更：VIP0/VIP1 website 价目写入 SKU `DS-CJ-CJYD3153518` 递减档（VIP1：1974/1894/1814/1755）；`qty-tiers.phtml` 增加「省」「较上档低」+ `data-b2b-qty-ladder`；`b2b-storefront.css` Token 档差样式。
- 验收：引擎 resolve VIP1 严格递减；ThemeUi UT；Browser 登录后 PDP 显示 19.74→18.94→18.14→17.55。

## 2026-09-12 — 浏览器登录链真实批发下单（商品 558）

- 需求：完整 Browser 链（登录图验证码/2FA → PDP 批发加购 → 结账定金）验收默认批发模板通路。
- 变更：无代码；本机 Browser 验收。本地图验证码 OCR 不稳时对当前 token 写入已知答案；TOTP 用账号 2FA secret 现算；结账页「提交订单」被浮层拦截时同页 `checkout.freezeQuote`+`submitV2`（fake_card / tob）。
- 验收：订单 `9779203997` / uuid `857f745e-7d5d-48a2-a32e-147ad7489491`；tob pending；小计 CNY 219.40（×10）；定金 65.82；成功页 200。

## 2026-09-12 — 默认批发模板 + 亏损护栏（2.6.73）

- 需求：启用批发后默认按 VIP 档继承站点模板；相对零售最大折扣默认 20%；可选成本毛利护栏；改模板不回刷价目/订单。
- 变更：`DefaultWholesalePolicy` / `WholesalePricingGuard`；Engine tob 门闩 + `SOURCE_B2B_DEFAULT_POLICY`；Eligibility/Provider/MOQ/qty-tiers；SystemConfig 13 行表；保存插件 + FPC Observer；offers 继承预览。
- 验收：UT `DefaultWholesalePolicyAndGuardTest` / SellingMode 契约；e2e `default-wholesale-policy-*.spec.js` + plan-suite。

## 2026-09-12 — 尾款结账订单摘要 soft-read（completed 不再卡加载）

- 需求：挂单明细一直「正在加载」；实为 hang_status=completed 触发不可支付，前端未清 loading。
- 变更：`paymentContext` ownership 后始终返回 `order_summary` + `can_pay`；`startPayment` 仍硬门闩；JS 按 can_pay 渲染摘要并停止加载占位。
- 验收：UT OrderSummary/ThemeUi；E2E hang-balance-panel；真单 e7cf7a15 显示金额与「已完成」。

## 2026-09-12 — 尾款结账左右分栏/居中布局（原型落地）

- 需求：窄卡左贴留白；要么左右要么居中；上线只要一种版式，不要原型切换条。
- 变更：生产仅左右分栏（订单左/支付右满宽双表面）；删除 `hang_layout` 切换、居中变体与底部 A/B 条。
- 验收：UT ThemeUi 无 switcher；E2E hang 面板有 columns、无 layout-switcher。

## 2026-09-12 — 尾款结账订单行商品缩略图

- 需求：尾款订单详情补商品缩略图并调原型布局。
- 变更：`buildOrderSummary` 经 MediaRepository + StorefrontProductMediaUrlResolver 写入 `lines.image_url`；hang 面板行网格含 56px thumb；主题 CSS Token。
- 验收：UT OrderSummary/ThemeUi；E2E hang-balance thumb img；真单 product 117 可解析 `/pub/media/...jpg`。

## 2026-09-12 — 尾款结账展示订单详情并重设计空旷面板

- 需求：尾款支付页太空，需展示订单详情并做原型 UI。
- 变更：`hang.paymentContext` 增加 `order_summary`（行项目/商品合计/定金/应付）；`checkout-tob.js` hang 面板渲染订单区+金额拆分+「返回批发身份」；主题 CSS Token 加密度；未登录先出订单骨架。
- 验收：UT `B2BHangPaymentOrderSummaryContractTest` / ThemeUi；E2E `hang-balance-payment-panel` PASS（含 order lines）；审图 ui_shot→pass。

## 2026-09-12 — 批发身份枢纽：挂单跟进 + 订单沟通并入 #b2b-identity

- 需求：独立 `#b2b-order-chat` 空态；挂单协商并入批发身份并重设计 UI。
- 变更：`account-identity-hub` partial；侧栏/头像收敛到 `#b2b-identity` + Signal；旧 chat section 仅跳转；`listForCustomer`；假 UUID 尾款文案澄清；建 thread 表并批准样例挂单 `e7cf7a15-…` → `awaiting_balance`。
- 验收：UT IdentityHub/ThemeUi；E2E `identity-hub.spec.js`。

## 2026-09-12 — 履约沟通改价：Bridge / Event / Facade / reserve / 尾款协商

- 需求：定金后库存 Capability reserve；Payment 桥接收敛；订单线程沟通+角标；尾款前协商改价经 OrderFacade。
- 变更：`B2BHangPaymentBridgeInterface` provides；`Weline_B2B::hang_status_changed` 文档+派发；`OrderFacadeInterface::mergeTypePayload`/`reviseTobHangPayable`；`onDepositPaid`→`reserveInventoryForHang`；`proposeBalanceRevision`/`confirmBalanceRevision`（确认后系统消息入订单线程）；payment 挡 `hang_revision_pending`；账户 `#b2b-order-chat` section + Signal 空壳；REQ/hang-status 合同允许尾款协商窗口。
- 验收：UT Bridge/Inventory/Lifecycle/Thread/FacadeTobHang/ThemeUi；E2E `hang-balance-payment-panel` / `hang-lifecycle-fulfillment` / `order-chat-signal` / `hang-revise-gate`；组套件 4 spec PASS；审图证据 `doc/evidence/ch3/`。

## 2026-09-12 — 商品页 ControlCenter 深链去掉 weline_product 前缀（2.6.71）

- 问题：`getUrl('*/b2b/…')` 在 Product 编辑 Hook 上下文把 `*` 解成 `weline_product`，价目表深链与 `save-product-sku-tiers` 404。
- 变更：`basic-after` / `offers-after` 改为 `getBackendUrl('b2b/backend/control-center/…')`；契约测禁 `*/b2b`。
- 验收：`SaveProductSkuTiersRouteContractTest`；正确路径探活非 404。

## 2026-09-12 — 商品编辑规格与价格内嵌批发启用与阶梯价（2.6.70）

- 需求：运营在商品「规格与价格」配置启用批发与 SKU 数量阶梯；价目仍归 B2B，不自建第二套表。
- 变更：Product `edit::offers-after` Hook；B2B `offers-after` canonical 开关 + 阶梯表；`ProductSkuQtyTierAdminService::upsertSkuQtyTiers` 全量 copy-forward / 空表 deactivate / `expected_version`；`save-product-sku-tiers` JSON；ControlCenter 起订量 + 附加档兼容 r43 单档；basic-after 改镜像同步；脏档随商品保存一并 upsert；价目写入后清 PDP FPC。
- 验收：UT `ProductSkuQtyTierAdminServiceTest` / `ProductWholesaleAdminHookContractTest`。

## 2026-09-12 — 尾款支付闭环（hang balance PSP）

- 需求：个人中心 B2B 订单付尾款真网关可用；修异步回跳不推进 hang、`already_paid` 卡死、silent `fake_card`、尾款误再 reserve 信用。
- 变更：`PaymentBrowserReturnDispatcher` 按 purpose reconcile hang；`CheckoutOrderPaymentService` already_paid reconcile + 仅 deposit reserve 信用 + landing purpose；`B2BHangPaymentService.paymentContext` 返回支付方式；`checkout-tob.js` hang 面板 radios / 稳定幂等 / redirect fallback；账户详情挂 hang CTA；`onDepositPaid`/`onBalancePaid` 幂等。
- 验收：UT HangBalanceClosedLoop / BrowserReturnHangReconcile / ThemeUiContract / AccountOrdersTemplate；E2E `hang-balance-payment-panel.spec.js`。

## 2026-09-12 — 批发显示门禁：开关 + 价目阶梯（2.6.69）

- 需求：商品开启批发且配置阶梯价才展示批发；否则按正常品售卖；批发身份仍可加购。
- 变更：`ProductWholesaleEligibility` + `PriceListStore::skuHasActiveTiers`；PDP switcher/qty-tiers 无资格整段不渲染；`B2BCartQtyPolicy` 无资格跳过 moq/step。
- 验收：契约 UT（Eligibility / CartQtyEligibility / Flags）。

## 2026-09-11 — 帮助文档与信用使用面说清规则（2.6.67）

- 需求：把定金 30%、信用只抵定金、最低现金占比、尾款仍付、同档不回补写入批发帮助与信用使用面。
- 变更：FAQ `/faq/b2b-wholesale` 增补信用/定金/FX（含 `min_cash_deposit_percent`）；结账顶栏订单说明、信用页签 blurb/勾选文案、账户批发身份旁注、Payment hint；链接「了解批发信用规则」。
- 验收：契约 UT；Browser FAQ + 信用页签。

## 2026-09-11 — 迷你车信用文案对比度（2.6.68）

- 现象：迷你车「批发信用」规则/额度/定金文案近乎看不见。
- 根因：`data-tone="muted"` 落到色板 tertiary（如 #a6a6a6），在白底上对比不足。
- 修：blurb/fx/currency/cash 去掉 muted，CSS 强制 `--weline-theme-text`；cache-bust `credit-contrast1`。

## 2026-09-11 — 信用额度明示去重（2.6.65）

- 现象：FX 摘要与 hint_short 同段重复展示。
- 修：可用报价只渲染 `data-b2b-credit-fx`（含最低现金），隐藏 hint 行。

## 2026-09-11 — 信用额度明示基准币 + 可键盘输入（2.6.65）

- 需求：面板说清基准货币/可用额度/折合当前币；抵扣数字须可键入（非仅上下拨动）。
- 变更：`data-b2b-credit-fx`；`type=text`+`inputmode=decimal`；`syncCreditUi` 聚焦态不 `toFixed`、失焦钳位；主题去掉 spinner。
- 验收：契约 UT；Browser 禁缓存。

## 2026-09-11 — 批发信用抵本期应付（去「无定金」门槛叙事）（2.6.64）

- 需求纠偏：用户指出「无定金无法抵扣」不合理；信用相当于优惠/资产抵扣，不应把定金当使用门槛。
- 更佳：挂单 30% 仍是 ToB 支付节奏；信用上限=`min(余额,本期应付)`；购物车/迷你车按商品小计×30%估算本期应付；文案「抵扣本期应付」。
- 变更：`estimateDepositMinor` 读 `data-cart-goods-subtotal` 等；Payment `not_applicable` 文案；Widget/i18n。
- 验收：UT / e2e surfaces / Browser。

## 2026-09-11 — 批发信用异币须汇率折算（2.6.63）

- 现象：后端基准货币额度，前端抵扣偶发按数字 1:1。
- 根因：credit.quote 部分路径默认 CNY；tob 无批发价行时展示币与金额不一致；输入框未标结账币。
- 修：`resolveCheckoutCurrency` + 币种变化强制重拉报价；输入旁 `data-b2b-credit-currency`；Cart tob 行一律尝试换算到展示币。
- 验收：Payment FX UT；本机 customer 47 USD quote `100000→12500`。

## 2026-09-11 — 迷你车/购物车批发信用页签（2.6.61）

- 需求：批发迷你车与购物车页也展示「批发信用」；站点开启才渲染。
- 变更：Widget 注入 `footer-extras` + `cart-summary-credit`；Cart 槽目录；模板 `isEnabled` 门禁；`applyCartTypeAll` + selling-mode 多表面同步；Upgrade 播种。
- 验收：契约 UT；Browser/e2e 批发可见、零售隐藏。

## 2026-09-11 — 迷你车/购物车批发信用页签（2.6.62）

- 需求：批发迷你车与购物车页也展示「批发信用」；站点开启才渲染。
- 变更：Widget 注入 `footer-extras` + `cart-summary-credit`；Cart 槽目录；模板 `isEnabled` 门禁；`applyCartTypeAll` + selling-mode 多表面同步；B2B Upgrade 在 widget 注册后播种（避免 Cart 先于 B2B 升级漏注入）。
- 验收：契约 UT；e2e surfaces；Browser 购物车摘要可见「批发信用」，零售隐藏。

## 2026-09-11 — 结账误报未开启批发信用（2.6.60）

- 现象：后台批发信用已启用，结账页签仍提示「站点未开启批发信用额度」。
- 根因：Payment `AssetPaymentService` 空 `policyOverride` DI 短路，未合并 `payment.asset_policy.Weline_B2B`。
- 修：见 Payment `1.9.48`；B2B 配置/`B2BPaymentAssetPolicyProvider` 本身正确。
- 验收：OM `getPolicyForScope` 含 `b2b_credit`；契约 UT。

## 2026-09-11 — 批发信用页签仅 tob 展示（2.6.59）

- 需求：结账「批发信用」优惠仅批发时展示。
- 变更：`checkout-tob.js` applyCartType/syncCreditUi 零售隐藏 credit slot + extras tab；Theme `mini-cart-extras-tabs` 跳过 hidden 子节点并支持 `rebuild`；cache-bust。
- 验收：契约 UT；Browser/e2e 零售无页签、批发有页签。

## 2026-09-11 — PDP 阶梯价切换 + 产品批发开关 Slot（2.6.58）

- 审图 `ui_shot`：默认零售切批发无阶梯价说明，刷新后才出现。
- 根因：`qty-tiers.phtml` 非 tob cookie 整段 `return`，DOM 不存在，`selling-mode.js` 无法 unhide。
- 修：零售 SSR 仍输出 `data-b2b-qty-tiers`（`hidden`）；产品级 `selling_mode_tob` 经 `ProductSellingModeFlags` 水合；B2B Hook 注入 Product 编辑/新增「启用批发」+ 价目入口；`product-admin.js` 合并属性写入。
- 架构：Product 只提供 `edit|create::basic-after` Hook；阶梯价/价目仍属 B2B。
- 验收：契约 UT（QtyTiers / WholesaleAdminHook / ProductSellingModeFlags）；Browser 零售→批发即时出现阶梯价盒。

## 2026-09-11 — 信用数据归 Payment 贡献（2.6.57）

- 纠偏：B2B 只提供批发信用**类型**（`payment.asset_policy`）；额度**数据**由 Payment 经结账事件贡献。
- 变更：`AssetCheckoutDiscountQuote` + Payment Observer；卸 B2B 额度 Observer；`B2BCheckoutCreditQuote` 薄委托。
- 验收：Payment/B2B/Checkout 契约 UT OK (20 tests, 85 assertions)。

## 2026-09-11 — 结账信用事件贡献（2.6.56）

- 纠偏：支付只 `payment.asset_policy` 提供策略；额度/折扣类型由 Checkout 事件读取，B2B Observer 贡献。
- 变更：`CheckoutFreezeQuoteCreditEnrichObserver` / `CheckoutAssetDiscountApplyObserver` + `event.xml`；Checkout 拆除硬耦合。
- 验收：契约 UT 断言无 B2B class_exists、有事件名。

## 2026-09-11 — 结账摘要批发信用抵扣行（2.6.55）

- 审图 `ui_shot`：已勾选并输入抵扣，摘要无折扣/抵扣行，应付仍为商品小计（如 CNY 540），与「现金定金 ¥0」不一致。
- 纠偏：批发信用≠营销「优惠」；tob 禁折，不得塞进 `discount_preview`。独立展示「批发定金 / 批发信用抵扣 / 本次应付定金」。
- 变更：Checkout `renderTotals` 读 `WelineB2BCheckoutTob.readApplyMinor/cashDepositMinor`；`syncCreditUi` 派发 `weline:b2b-credit-changed`；cache-bust `credit-totals-row1`；UT 契约。
- 验收：Browser 禁缓存；勾选抵扣后可见抵扣行，应付=现金定金。

## 2026-09-11 — 调组 VIP 补授信（2.6.54）

- 现象：后台把客户调到 VIP1 后结账仍「额度不够」。
- 根因：① `assignGroupMember` 只写组关系未 `grantToTarget`；② 本机 `CustomerAsset` rollout=`off`，授信被拒绝。
- 修：调组后调用 `grantToTarget`；开发站将 customer_asset 设为 `allowlist` 含 `website:0`；并为 customer 47 补授信 1000.00。
- 附带：`ConfigStore::forConnection` 补齐 `ConfigFieldCacheNamespaceResolver` 构造参数（否则授信写配置门禁崩）。
- 验收：余额 `available_minor=100000`；契约含 `grantCreditAfterAssign`/`grantToTarget`。

## 2026-09-11 — 结账信用报价 readonly 崩溃（2.6.54）

- 现象：批发信用红字「暂时无法获取批发信用报价，请刷新后重试」。
- 根因：`B2BCheckoutCreditQuote::currency()/policy()` 对 readonly 构造属性 `??=` 赋值 → Error → Provider catch 成 `quote_failed`。
- 修：只读判断后 `new` 回退，禁止再写 readonly 属性。
- 验收：日志 `server_throw`→`server_quote_ok`/`no_balance`；UI 显示「额度不够…」而非假刷新文案。

## 2026-09-11 — 结账信用勾选无限请求环（2.6.54）

- 审图 `error_shot`：勾选/页签后红字「正在获取批发信用报价…」并连环 `credit.quote`。
- 根因：① `MutationObserver` 整树监听后 `applyCartType`→`ensureCreditQuote`；② `quote_failed` 无条件重试；③ 定金比对用服务端回写 vs 本地估算，易误判变更；④ `syncCreditUi` 空 quote 再拉网。
- 修：`ensureQuote` 门闩；Observer 仅 `syncCheckoutCouponAvailability`；失败态结算不自动重试；`lastRequestedDepositMinor`；勾选只本地 sync；加载中禁用勾选；cache-bust `prefetch7`。
- 验收：UT ThemeUi 1/1（293 asserts）；Browser 禁缓存：15×ensure+勾选+cart-updated 额外 refresh=0。

## 2026-09-11 — 结账信用异常提示用主题 danger 色（2.6.54）

- 审图 `ui_shot`：批发信用不可用原因仍为正文色，要求主题提示色。
- 变更：reason 用 `w-text` + `data-tone="danger"`；CSS `color: var(--weline-theme-danger, …)`；`@static` 缓存戳统一 `&v=20260911-credit-danger-tone`（避免双 `?v=`）。
- 验收：禁缓存 Browser reason=`rgb(166,61,61)`/`#a63d3d`；UT ThemeUi+Widget 3/3。

## 2026-09-11 — 结账信用预取竞态（假刷新文案）（2.6.54）

- 现象：仍见「暂时无法获取批发信用报价，请刷新后重试」。
- 根因：`loadCheckout` 常早于 `b2bCheckoutTob` 模块就绪，预取被跳过；空 `quote` 即显示假刷新文案。
- 修：`ensureCreditQuote`；`applyCartType(tob)` 自预取；`loadCheckout` 等待模块后再预取；定金变化时重取。
- cache-bust `checkout-tob.js?v=20260911-credit-prefetch3`。

## 2026-09-11 — 结账批发信用预取报价（2.6.53）

- 审图 `error_shot`：批发信用页签提示「暂时无法获取报价」；点击无请求、抵扣不生效。
- 根因：`b2b_credit` 仅在 submit 时 `freezeQuote`→`syncFromFrozen`；页签打开时 `quote=null` 勾选禁用；submit 瞬间勾选仍为关 → `apply` 恒 0。
- 线稿：`[✓抵扣][?] → 原因/加载 → 额度`；加载中显示「正在获取…」。
- 原型调整：不对勾选单独发请求；`getData` 后 `credit.quote` 预取；freeze 覆盖并保留勾选。
- 变更：`B2BQueryProvider::credit.quote`；`checkout-tob.refreshCreditQuote`；Checkout `loadCheckout` 钩子；`syncFromFrozen` 保留 toggle/input；cache-bust modules。
- 验收：UT ThemeUi/Widget 相关通过；Browser 待验 tob 结账预取。

## 2026-09-10 — 信用不可用原因禁止空洞文案（2.6.52）

- 审图 `ui_shot`：红箭头指向「当前不可用批发信用」，点击勾选仍不知具体原因。
- 根因：① `b2b_credit=null` 时模板 `data-i18n-unavailable-generic` 覆盖具体 fallback；② `syncFromFrozen` 误选地址 `form`，报价写不进信用面板。
- 变更：Quote 拆分登录/定金/`额度不够` hint；Checkout 写入 `unavailableStub`；JS 映射具体 reason 并修正 root 选择器；删除空洞 generic。
- 验收：UT Quote/Widget 12/12；Browser 注入 no_balance→「额度不够…」、null→「请刷新后重试」，无「当前不可用批发信用」。

## 2026-09-10 — 信用页签 toc 就地原因竞态修复（2.6.51）

- 审图续验：零售车勾选禁用须就地原因 +「去购物车切换批发车」；cache-bust `checkout-tob.js?v=20260910-credit-inplace4`。
- 变更：`applyCartType` 后立刻 `syncCreditUi`；`syncCreditUi` 以 `readMode()` 判定 toc，避免 class 滞后；模块登记 query 刷新。
- 验收：Browser 批发信用页签 reason 可见、switch-cart 可见、勾选 disabled；UT 10/10。

## 2026-09-10 — 客户组编辑：输入框与翻译按钮分离（2.6.50）

- 审图：`ui_shot` Local 嵌输入槽导致名称/说明输入框消失，只剩翻译触发文案。
- 线稿：`[标签][翻译]` + 下方 `w-input`/`textarea`（对齐 Product 基础信息）。
- 变更：编辑弹窗恢复可见输入；vault `<local>` 短文案「翻译」挂 label 旁；JS 仅搬触发器；UT；`2.6.49` → `2.6.50`。

## 2026-09-10 — FAQ 动态 VIP 等级表（2.6.49）

- 需求：`/faq/b2b-wholesale` 按后台客户组配置、按分组与 `tier_rank` 排序写清消费门槛与达标折扣额度。
- 变更：`WholesaleFaqVipLadderPresenter` + FAQ 模板动态列表；停用组不展示；UT。

## 2026-09-10 — 信用勾选就地原因 + 竖排布局（2.6.48）

- 审图 `ui_shot`：红框布局乱；勾选禁用无原因；要求就地显示。
- 线稿：`[✓] 文案 ?` → **原因就地** → `抵扣额度` 全宽输入 → 链接行。
- 变更：合并 `data-b2b-credit-reason` 置于勾选下；去掉 `w-cluster` 横排；问号去下划线；toc/报价不可用均写入同一原因节点。

## 2026-09-10 — 结账信用不可用原因可读（2.6.48）

- 现网验收：勾选仍不可用，页上为零售车 `toc`（cookie `weline_selling_mode=toc`），原因「仅批发结账可用」被灰化层盖住不易读。
- 变更：原因文案移出灰化区并写清「零售结账 → 须切批发车」；增加「去购物车切换批发车」；CSS 原因行不降透明度。

## 2026-09-10 — 结账信用不可用原因 + 轻量问号（2.6.48）

- 审图：`ui_shot`（用户附图）空洞「当前不可用批发信用」+ 实心问号按钮突兀。
- 根因：`B2BCheckoutCreditQuote` 调 `rateSnapshot` / `convertMinorFromWebsiteDefault`，但 `B2BBaseCurrencyResolver` 未实现 → 报价抛错被 Checkout 吞掉 → `b2b_credit=null` → 前端只剩通用不可用。
- 线稿：`[?] 使用批发信用…` 实心钮 → 纯文本 `?`；不可用行须写具体 reason。
- 原型调整：帮助去 `w-button`，主题 muted 下划线 `?`；不可用改 muted + `hint_short`。
- 变更：Resolver 补双向换算与汇率快照；Quote 各 reason 填 hint；模板/CSS/JS；UT（bootstrap）；`2.6.47` → `2.6.48`。

## 2026-09-10 — 批发订单顶栏 / 信用页签拆分（2.6.46）

- 审图：`ui_shot` 页签内不应塞「批发订单」说明，信用页签只留抵扣控件。
- 变更：`summary-before` 恢复 `checkout-tob-order-note.phtml`；`checkout-tob-deposit-note.phtml` 仅信用面板；CSS 顶栏卡 vs 页签扁平；`checkout-tob.js` 顶栏 ToB 显隐、页签 ToC 灰化；UT；`2.6.45` → `2.6.46`。

## 2026-09-10 — FAQ section weline-code

- `frontend/faq/b2b-wholesale.phtml` 四处 `<section>` 补 `weline-code`，解除 `setup:upgrade` 的 `frontend-section-weline-code` 门禁阻塞。

## 2026-09-10 — 批发信用迁入结账摘要 extras 页签（2.6.45）

- 审图：`ui_shot` 箭头要求信用控件落到优惠券/订单留言同排。
- 需求纠偏：字面「支付模块注入」改为 B2B 拥有部件 + Checkout `checkout-summary-credit` 槽 `default_injections`（对齐 Marketing/Order），禁止 Payment 耦合。
- 变更：`extends/.../Weline_B2B/widget.php`；模板页签化；移除 `summary-before` Hook 直出；`checkout-tob.js` ToC 灰化；Checkout 槽目录+模板槽；UT。

## 2026-09-10 — 账户等级/优惠/下一档 + 客户组排序（2.6.44）

- 需求：前台个人中心可见当前客户等级、优惠与下一分组；分组有顺序（种子按 VIP）；可改排序数字或拖拽分组卡片。
- 需求纠偏：下一档按系统 VIP 阶梯（`SystemVipLadder`），不跟后台显示顺序混用；显示顺序复用 `tier_rank`。
- 变更：`AccountMembershipTierPresenter` + 账户侧栏等级/权益/下一档；`CustomerGroupStore::reorderGroups`；编辑「排序数字」；`control-center-groups-reorder.js` 手柄拖卡片；成员行拖放换组不拦截；UT；`2.6.44`。

## 2026-09-10 — 成员拖放换组

- 审图：`ui_shot` 箭头指「移出」；需求「拖动移动分组」纠偏为**拖成员到目标组卡片换组**（不重排系统 VIP 阶梯）。
- 线稿：行 `[⋮⋮ 姓名/#id]` 可拖 → 目标组 outline 高亮 → `assignGroupMember`。
- 变更：`control-center-groups-members.js` HTML5 DnD + `data-assign-url` POST；提示文案；UT；保留「移出」。

## 2026-09-10 — 客户组名称/说明 LocalModel（`<local>`）

- 审图：编辑弹窗「名称」「等级说明」需业务多语言，非词典 `__()`。
- 线稿：弹窗槽位挂载 `<local field=name|description>`；主表保留源语回退。
- 变更：`CustomerGroupRecord\LocalDescription` + Upgrade 建表种子 + ControlCenter vault/槽位 JS；模块 `2.6.47`。

## 2026-09-10 — 达标折扣额度文案（门槛→折扣因果）

- 审图：列表「目标」无法表达「消费达标后授予的可抵扣折扣额度」。
- 线稿：`消费门槛 · 达标折扣额度`；编辑先门槛后折扣；提示「达标后补到此折扣额度，仅可抵扣批发订单」。
- 变更：ControlCenter + 账户侧「目标授信」→「折扣额度」；审图 UT；模块 `2.6.45`。

## 2026-09-10 — 客户组成员可读展示（姓名主显 / #id 次显）

- 审图：`ui_shot` 成员表客户列只显示裸 `47`、站点裸 `0`，不可扫。
- 线稿：`[姓名/邮箱] / [#id · email] | 站点名 | 更新 | 移出`。
- 原型：主信号可读身份，ID 作核对次要信息；站点用名称（默认站）。
- 变更：`GroupMemberDisplayPresenter` 富化 + 姓名/邮箱搜索；`control-center-groups-members.js` 渲染；占位「搜索姓名、邮箱或编号」；UT。

## 2026-09-10 — VIP 消费门槛曲线调平（vip12≥100万）

- 原等差 `tier×5000` 过陡；改为前缓后快：5千→1.5万→…→vip12 **100万**；授信额度同步放缓且不超过门槛。
- 仍为旧线性默认金额的系统组，在 `ensureSystemVipLadder` 时自动迁到新种子。

## 2026-09-10 — 客户组成员手风琴 + 基准货币提示

- 需求：展开分组异步加载/搜索/添加/移出成员；目标额度按站点默认基准货币，异币种消费换算后累计。
- 变更：`B2BBaseCurrencyResolver`；`MembershipSpendQuery` FX；`listMembersByGroup` + ControlCenter `group-members`/`assign`/`remove`；手风琴 UI + 基准货币提示；UT。

## 2026-09-10 — 客户组列表人性化（只读表 + 编辑弹窗）

- 审图：操作列塞改名/能力表单不可扫；「保存能力」难懂；状态英文；额度用分。
- 线稿：列表只读（名称+说明摘要 / 状态徽章 / ¥元）· 操作「编辑」· `w-dialog` 标签字段一次保存。
- 变更：`ControlCenter/index.phtml` + `control-center-groups-edit.js`；`B2BAdminService::updateGroupCapabilities` 支持 `name`；UT/审图契约；本条文档。

## 2026-09-10 — ControlCenter 全工作区作用范围（2.6.43）

- 需求：批发模块后台都要网站/店铺/渠道范围，不只身份申请。
- 线稿：顶栏 `[作用范围 <w:scope>]` · 徽章；列表按 `website_id` / `channel_id` 过滤。
- 变更：`B2BBackendScopeTrait` + `partials/scope-toolbar.phtml` + JS；`renderWorkspace` 统一过滤；写回保留 `target_scope`；兼容旧 `website_id`；UT；`2.6.43`。

## 2026-09-10 — 删除身份申请 POST 404（2.6.42）

- 根因：框架将控制器 `delete*` 注册为仅 HTTP `DELETE`；表单 `method=post` 命中 `delete-membership-application` → 404。
- 变更：`ControlCenter::removeMembershipApplication` + path `remove-membership-application`（`request_method` 空）；Service/ACL 仍 `delete*`；UT；`2.6.42`。
- 验收：表单 action 已换；POST 删除 `app-80b9a88d…` 回列表无 404，行数 2→1。

## 2026-09-10 — syncButtons 覆盖列表卡加购（2.6.41）

- 配合 Cart 1.3.59：批发加购污染零售。
- `selling-mode.js` `syncButtons` 增加 `product-card-add-to-cart` / 卡内 `[data-action=add]`；契约锁定。
- 版本：`2.6.40` → `2.6.41`。

## 2026-09-10 — 身份申请删除 ACL（2.6.40）

- 审图分流：`web_ui` / `ui_shot`（操作列仅撤销，需可删重复申请）
- 线稿：操作 | … | [删除]（`<acl source="…applications:delete">`）
- 变更：`MembershipApplicationService::delete`；当时控制器 `deleteMembershipApplication` 独立 ACL；模板 `<acl>` 包裹；UT；`2.6.40`（2.6.42 起动作改为 `removeMembershipApplication`）

## 2026-09-10 — 身份申请唯一：编辑后重新提交当前记录（2.6.39）

- 审图分流：`web_ui` / `ui_shot`（后台身份申请同客户双行：资格已撤销 + pending）
- 需求纠偏：仅改前端不够；服务端 upsert 为硬门禁；`approved` 无资格投影改为 `UI_INACTIVE`
- 线稿：账户/抽屉「修改资料 → 重新提交当前申请」；禁止再插新行
- 原型：customer+website 单申请身份；resubmit = update same id → pending
- 变更：`MembershipApplicationService::submit` upsert；投影；account/selling-mode 预填+文案；UT；`2.6.39`

## 2026-09-10 — 资格已撤销行可重新授权（2.6.38）

- 需求纠偏：原原型撤销后仅前台再申请；运营面缺重新授权
- 线稿：资格已撤销 | [组搜索选] [重新授权]
- 变更：`MembershipApplicationService::reauthorize`；ControlCenter ACL；行内 UI；UT；`2.6.38`

## 2026-09-10 — 批发抽屉登录挂载竞态修复（2.6.37）

- 分流：`web_ui` / `ui_shot`（toast 已撤销但行仍 approved + 撤销按钮）
- 根因：申请审计不改写；列表误把 `approved` 当生效资格
- 变更：`enrichApplicationEntitlement` 投影 `entitlement_active`；无资格行显示「资格已撤销」并隐藏行内撤销；UT；i18n；`2.6.36`

## 2026-09-10 — 身份申请：顶部站点范围 + 行内撤销 + 多选快速撤销（2.6.35）

- 分流：`web_ui` / `ui_shot`（撤销仅页底单站、列表已批行无操作、无顶部范围）
- 线稿：顶栏「站点范围」auto-submit → 整表过滤；已批行组码 + 撤销；页底「快速撤销」网站多选
- 变更：`ControlCenter::applications` GET `website_id`；模板 Taglib scope/row-revoke/`website_ids`；`B2BAdminService::revokeMembership` 多站；写回保留单站过滤；UT；i18n；`etc/module.php` → `2.6.35`

## 2026-09-10 — 批准失败可读化 + 本机白名单（2.6.34 hotfix）

- 根因：`commerce_rollout_immutable:off`（B2B 灰度关闭）导致 `approveMembershipApplication` 写门禁失败，toast 仅「操作失败」
- 变更：`B2BAdminService` 转 `B2BConflictException` 可读文案；`ControlCenter` 透出冲突消息；申请页灰度阻断告警；本机 `website:0` allowlist 开启
- 验收：Browser 批准 vip0 → status=approved

## 2026-09-10 — VIP0–12 阶梯 + 批发信用额度（2.6.34）

- 需求：`REQ-B2B-0010`（一期非目标账期子集：钱包式 `b2b_credit` 授信 + tob 优惠门禁）
- 模型：`CustomerGroupRecord` 增 description/credit_limit_minor/spend_threshold_minor/tier_rank；`ensureSystemVipLadder` 种子 13 档并迁移旧 `g-system-vip`
- 配置：`b2b_credit_enabled`（global）embed 进批发配置；关则不授信/不抵扣/不升档
- Payment：`PaymentAssetPolicyProviderInterface` SPI；B2B 贡献 `b2b_credit`（ROLE_DISCOUNT + required_order_types=tob）
- 服务：`B2BCreditGrantService` 批准/升档补差额；`MembershipSpendQuery`；Cron `MembershipSpendUpgrade`
- UI：批准 search-select 默认第一项（vip0）；组能力编辑；前台等级说明
- 单测：阶梯/授信/SPI/Cron/embed 契约；Payment `1.9.41`；`etc/module.php` → `2.6.34`

## 2026-09-10 — 后台运营面去手填 ID + 默认 VIP 组（2.6.33）

- 需求纠偏：整模块改为 ControlCenter 可写运营页（客户组/价目/报价/挂单/身份申请）；店面与迁移只读本回合不改
- 线稿/原型：表单用人名/标签搜索选；系统 VIP 默认可改名不可删；挂单行内批准
- 变更：`CustomerGroupRecord` 增 `name`/`is_system`；`CustomerGroupStore` ensure/rename/delete；`B2BAdminService` 自动 group/list id；`ControlCenter` UI + ACL；UT；`etc/module.php` → `2.6.33`

## 2026-09-10 — 审图：身份申请标签搜索选（2.6.32）

- 分流：`web_ui` / `ui_shot`（运营手填申请单/客户组/客户 ID）
- 线稿：三列手填 ID 卡 + 宽表；运营无法记忆 UUID
- 原型调整：Taglib 优先——行内客户组 `<w:theme:search-select>`；撤销客户 `<w:customer:admin:select>`；禁止手填 ID 主路径
- 变更：`ControlCenter/index.phtml`；UT；`etc/module.php` → `2.6.32`
- 单测：`ControlCenterTemplateShentuTest`

## 2026-09-10 — 审图：身份申请行内选组批准（2.6.31）

- 分流：`web_ui` / `ui_shot`（后台身份申请；表与手填批准表单脱节）
- 线稿：告警 + 三列手填卡（申请单ID/组ID 空）+ 原始宽表 + pending 行无操作
- 原型调整：批准主路径迁到 pending 行（`w-select` 客户组 + 批准/驳回）；顶部仅保留撤销区；无可用组时告警引导先建客户组
- 变更：`CustomerGroupStore::listActiveOptions`；`B2BAdminService::listActiveGroupOptions`；`ControlCenter::applications` 赋值；`ControlCenter/index.phtml`；UT；`etc/module.php` → `2.6.31`
- 单测：`CustomerGroupStoreListOptionsTest` + `ControlCenterTemplateShentuTest`

## 2026-09-10 — 批发申请表单强制主题 Outlined notch（2.6.30）

- 审图：`web_ui` / `ui_shot`（批发身份申请胶囊标签条）。
- 修：申请字段包 `w-field__control`；`b2b-storefront.css` `gap:0` + `--weline-theme-field-label-bg`；CSS bust `apply-notch1`。
- 覆盖 PDP 抽屉与账户侧栏同一套表单。

## 2026-09-10 — 批发身份状态投影与轮询（2.6.29）

- `MembershipStatusProjection`：申请单 vs 生效资格；有效 membership 强制 `active` 且停 poll；撤资格禁止「已批准」CTA。
- `latestForCustomer`；`submit` 拒绝重复 pending / 已开通；`unassignCustomer` + 后台撤销；Query `membership.status`（45s 节流）。
- PDP CTA「等待申请通过」；`selling-mode.js` 首探 45s 后每 5 分钟 poll；通过后刷新价/车；visibility 机会刷新。
- 在途 hang：撤资格后新交易 fail-closed；已生成挂单按原状态机继续。
- 版本：`2.6.28` → `2.6.29`。

## 2026-09-10 — 适配 Cart cart-type-changed Event（2.6.28）

- `setMode` 双发 `weline:cart-type-changed`；监听外源切换；页签补 `data-cart-type-option`。
- Theme/Cart 不再直点 `data-b2b-mini-cart-type-option`。
- 版本：`2.6.27` → `2.6.28`。

## 2026-09-10 — 批发申请表单 label 对齐 Outlined notch（2.6.27）

- `selling-mode-switcher.phtml` 申请字段：`w-label` → `w-field__label`（已在 `w-field` 内）。
- 版本：`2.6.26` → `2.6.27`。

## 2026-09-10 — 访客页签默认 toc，忽略残留 tob cookie（2.6.26）

- 未登录 `preferredMode` 忽略 `weline_selling_mode=tob` cookie，冷启动 toc；本页点选写入 `weline_cart_type_explicit`。
- 与 Cart 空车 sibling 推荐互补，避免批发空死胡同。
- 版本：`2.6.25` → `2.6.26`。

## 2026-09-10 — 批发申请抽屉顶栏文案收口（2.6.25）

- 审图：`web_ui` / `ui_shot`；红框区「登录后即可提交…」「请使用下方…」「登录」「使用您的账户继续」叠层啰嗦。
- 修：导语改一句利益点「登录后即可申请批发价与数量阶梯优惠。」；挂载 `data-weline-login-panel-heading="0"` 省略面板标题/副标题。
- 版本：`2.6.24` → `2.6.25`。

## 2026-09-10 — 批发抽屉挂完整登录面板（2.6.24）

- 宿主改 `customer/login-panel`（快捷+账密）；文案「请使用下方登录面板」。
- 版本：`2.6.23` → `2.6.24`。

## 2026-09-10 — 批发挂载改调框架 Weline.mount（2.6.23）

- 宿主加 `data-weline-mount-load="account"`；打开抽屉 `Weline.mount.scan`（不再调 Account.scanMounts）。
- 协议权威：`Theme/doc/前端JS模块加载规范.md` §1.0。
- 版本：`2.6.22` → `2.6.23`。

## 2026-09-10 — 批发申请声明式挂载 customer/social-quick（2.6.22）

- 抽屉引导「请使用下方快捷登录」；宿主 `data-weline-mount="customer/social-quick"`（inline + suppress-floating）。
- 打开抽屉 `scanMounts({force:true})`；不再依赖「本页登录」按钮拉页角条。
- 挂载面权威：`Weline_Customer/doc/挂载面.md`。
- 版本：`2.6.21` → `2.6.22`。

## 2026-09-09 — 审图：批发申请「本页登录」无反应（2.6.21）

- 审图分流：`web_ui` + `ui_shot`（附图：申请批发身份 → 抽屉「本页登录」）。
- 根因：`promptGuestLogin` 只调 `WelineSocialQuick.boot()`；页面已 booted / 条被关掉后不再显示；状态写到隐藏表单区；打开抽屉还自动烧一次 one-shot。
- 修：强制 `maybeStartSocialQuickPrompt({force:true})` + `reveal` 重开快捷登录条；门闸可见回落链接；打开抽屉不再自动弹登录；Customer social-quick 支持 force/reveal。
- 版本：`2.6.20` → `2.6.21`。

## 2026-09-09 — 恢复零售车/批发车双车 UI（2.6.20）

- 问题：B2B 已启用，但迷你车/购物车无「零售车」「批发车」分段（type-host 空；boot Hook 与双车注入 JS 在模块回迁后丢失）。
- 修：
  - 恢复 `view/hooks/Weline_Theme/frontend/layouts/base/body-end.phtml`（`data-weline-load=b2bSellingMode` + i18n）
  - 恢复 `selling-mode.js`：`ensureCartPageTypeSeg` / `syncCartPageChrome` / `enhanceMiniCarts` 注入分段
  - CSS `.b2b-mini-cart-type-seg`；访客仍恒 toc，切批发登录回跳
- 契约：`B2BStorefrontThemeUiContractTest` 断言 boot + 双车注入标记
- 版本：`2.6.19` → `2.6.20`

## 2026-09-09 — preferredMode 访客恒 toc（2.6.19）

- 问题：游客点「批发」会写 `weline_selling_mode=tob` 再跳登录；未登录回店时 `preferredMode()` 仍读 cookie→tob，购物车误打闸门文案「该售卖类型需要登录」。
- 修：`selling-mode.js` `preferredMode` 未登录恒返回 `toc`（cookie 仍可留给登录后）；契约断言。
- 版本：`2.6.18` → `2.6.19`。

## 2026-09-09 — 侧栏菜单 control-center 连字符对齐（2.6.19）

- 问题：客户组页地址为 `…/control-center/groups`，菜单仍写 `controlcenter/groups`，侧栏无法高亮/自动展开
- 变更：`etc/backend/menu.xml` 改为 `control-center/*` pretty action；高亮匹配由 Admin/Theme 连字符无关比较兜底

## 2026-09-09 — 审图：购买方式胶囊全圆角对齐加购（2.6.18）

- 分流：`web_ui` / `ui_shot`（红箭头：零售/批发仍偏方；对照右侧「加入购物车」胶囊）
- 原型调整：分段 + 游客 CTA 圆角改为 `--weline-theme-radius-full` / `--radius-full`（999px），与 PDP buy-box 一致
- 变更：`b2b-storefront.css`；cache `&v=20260909-amz-pill3`；`etc/module.php` → `2.6.18`
- 单测：契约断言 radius-full

## 2026-09-09 — 审图：购买方式亚马逊胶囊 + 登录态修复（2.6.17）

- 分流：`web_ui` / `ui_shot`（用户附图红框「零售/批发」双实心丑态；「昨天主题改过、今天又丑了」）
- 线稿：价格 → 购买方式标签 + [零售|批发] 双钮 → 灰字死胡同/实心登录 CTA → 运费说明
- 原型调整：
  1. 分段改为 Amazon 规格胶囊：浅底深字；选中 `--weline-chrome-body-link` 内描；未选浅灰描边；禁止双 `primary` 实心板
  2. 游客不激活 tob 展示；去掉「切换批发需先登录」死胡同；改为 outline「登录后使用批发」
  3. 根因：`SessionFactory::create()` 不存在 → catch 后 loggedIn 恒 false（已登录也被当游客）
- 变更：`selling-mode-switcher.phtml`（`createFrontendSession` + outline pills + guest CTA）；`selling-mode.js`；`b2b-storefront.css`；i18n；契约单测；`etc/module.php` → `2.6.17`
- 单测：`B2BStorefrontThemeUiContractTest` OK（1 / 55）
- Browser：禁缓存验收胶囊选中/未选对比 + outline CTA；交付 PDP 花朝记洛川页

## 2026-09-09 — 客户组页审图 UI（2.6.16）

- 分流：`web_ui` / `ui_shot`（仅附图客户组后台）
- 线稿：标题/面包屑 + 可管理徽章 + 技术告警 + 四字段扁平行（保存挤在 span-2）+ 空表「当前没有记录」
- 原型调整：字段区与主 CTA 分行右对齐；标题按 kind「新增客户组」；状态启用/停用；空态 `w-empty` + 下一步；告警去 rollout 黑话
- 变更：`view/templates/Backend/ControlCenter/index.phtml`；UT `Test/Unit/View/ControlCenterTemplateShentuTest`（宿主原生新建）。
- 验收：Browser 禁缓存客户组页

## 2026-09-09 — 批发配置页 config:embed 启停（2.6.15）

- 目标：与客服模块一致，在 B2B 菜单提供「批发配置」快捷开关，用统一配置嵌入启停零售/批发。
- 变更：
  - Extends `Config/frontend/selling-mode.phtml` 声明 `selling_mode_toc_enabled` / `selling_mode_tob_enabled`（switch，复用 SellingModePolicy 键）
  - `Controller/Backend/Config` + `view/templates/Backend/Config/index.phtml`：`<w:scope>` + `<w:config:embed>`；可选链统一配置中心
  - `etc/backend/menu.xml` 增加 `*/backend/config`；ACL `Weline_B2B::config`
  - `etc/module.php` → `2.6.15`（高于库内版本游标 `2.6.14`）
- 单测：`Test/Unit/Service/B2BSellingModeSettingsContractTest` OK（1 / 22 assertions）
- Browser：禁缓存打开 `…/b2b/backend/config?target_scope=default.default.default`，可见菜单「批发配置」、作用范围、零售/批发 switch（`w-config-embed`）
- 需求：`REQ-B2B-0008`

## 2026-09-09 — 迁移状态页人性化（2.6.1）

- 分流：`web_ui` / `ui_shot`（用户附图「看不懂」）
- 线稿：侧栏 + 标题/只读徽章 + 技术告警 + 英文键值网格
- 原型调整：首屏「当前结论」白话 + 中文标签行（灰度模式/白名单/环境锁定/执行方式/网页生产操作）+ 一句 hint；去掉主显 `mode`/`execution_policy` 等黑话
- 变更：`View/Backend/MigrationStatusPresenter`；ControlCenter 赋 `workspace_status_view`；模板渲染可读行；`etc/module.php` → `2.6.1`
- 单测：`Test/Unit/View/MigrationStatusPresenterTest` OK（2）
- 验收：Browser 禁缓存打开迁移状态页

## 2026-09-07 — Theme/店面 ToC/ToB 一期 UI（2.6.0）

- 当前状态：`implemented`（契约单测；Browser 验收待实例探活）
- 变更：
  - PDP：`after-price` Hook + `selling-mode-switcher`（Theme `w-button-group` / `w-badge` / `<lang>` / `w:form` + drawer）；cookie `weline_selling_mode`；游客切批发登录回跳；未入会打开申请抽屉
  - 数量：PDP qty `data-tob-moq/step=5`；加购传 `selling_mode`/`cart_type`
  - Mini-cart：`cart_type` badge；`getCart/summary` 带 cart_type
  - Checkout：定金 30% 说明 Hook；tob 隐藏优惠券槽；getData 透传 `cart_type`
  - 账户订单：hang_status 进度 + `purpose=deposit|balance` CTA；`doc/hang-status-state.md`
- 单测：`Test/Unit/View/B2BStorefrontThemeUiContractTest`
- 需求：`REQ-B2B-0007` Theme/i18n 表面

## 2026-09-07 — ToB 禁折 + 多行报价集 + 定金挂单（2.5.0）

- 当前状态：`implemented`（单测；Browser/E2E 未做）
- 变更：
  - Checkout `cart_type=tob` 跳过 Marketing DiscountQuote/券；定金基数=禁折后含税商品小计 × 30%（运费进尾款）；submit 延迟库存预占
  - `B2BCheckoutRecheckService::issueQuoteSet` / `submitQuoteSet` + `B2BQuoteTokenStore::consumeSetWith` 多行原子消费
  - `B2BHangOrderService` + `weline_b2b_order_hang`；ControlCenter approve/reject；Payment `onPartiallyPaid` 桥接
  - `CheckoutPaymentRecoveryStateService` 支持 `purpose=deposit|balance`；Order `order_type`/`type_payload` 写入；事件 hang_status 同步观察者
- 单测：`TobDiscountHangMultilineContractTest`
- 需求：`REQ-B2B-0005` → implemented

## 2026-09-07 — 价目 min_qty + ToB MOQ + 店面批发价（2.4.0）

- 变更：`PriceListItemRecord.min_qty`；Engine `qty` 档；`B2BCartQtyPolicy` + Cart `CommerceCartQtyPolicyInterface`；`B2BStorefrontPriceAdjustmentProvider`（exclusive absolute_minor）。
- 单测：价档 / MOQ / storefront provider + Assembler 既有测。
- 需求：`REQ-B2B-0006` + storefront 价。

## 2026-09-07 — SellingModePolicy + membership 申请（2.4.0）

- 当前状态：`implemented`（单测通过；Browser/E2E 未做）
- 变更：
  - `Service/SellingModePolicy.php`：Website/Store ConfigStore 开关 + 商品 flags fail-soft；`defaultMoq/Step=5`；会话偏好仅返回已启用 toc|tob
  - `Model/MembershipApplicationRecord` + `Service/MembershipApplicationService`：申请禁客户端 `group_id`；批准写 `CustomerGroupStore::assignCustomer`
  - `Service/CommerceTypeMembershipChecker` provides `Cart\Api\CommerceTypeMembershipCheckerInterface`
  - 后台 `ControlCenter::applications` 批准/驳回；Query `membership.submit`
  - Cart：`CommerceTypeMembershipGate` + `SellingTypeResolver` 可选 Gate（未配置则非 toc fail closed）
- 单测：`SellingModePolicyTest`、`MembershipApplicationServiceTest`、`CommerceTypeMembershipGateTest`
- 需求：`REQ-B2B-0003`/`REQ-B2B-0004` 进入实现；未改 `CustomerAccountService::register`

## 2026-09-07 — ToC/ToB 一期需求与合同文档

- 当前状态：`docs_planned`（仅文档；未改功能代码 / `etc/module.php`）
- 变更：`doc/需求.md` 保留 `REQ-B2B-0001`，追加 `REQ-B2B-0002`…`0007`（双 Registry 可卸载零探测税、SellingModePolicy、membership 申请、禁折+定金 hang、MOQ、事件/徽章/Theme/i18n）及一期非目标；`doc/b2b.md` 增加「ToC/ToB 一期」短节。
- 关联：`Weline_Cart/doc/cart.md`、`Weline_Order/doc/event/*` 合同对齐。
- 目标版本：ToC/ToB 一期（文档立项）

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `2.5.0` | implemented（unit） | `REQ-B2B-0005` 禁折/定金挂单/多行报价 | 2026-09-07 | 2026-09-07 | unit pass / not released |
| `2.4.0` | implemented（unit） | `REQ-B2B-0003`/`0004`/`0006` + storefront | 2026-09-07 | 2026-09-07 | unit pass / not released |
| `2.3.0`（文档基线） | docs_handoff | `REQ-B2B-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `2.3.0` — 文档制度启用基线

- 基线版本：`2.3.0`
- 目标版本：`2.3.0`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-B2B-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-B2B-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-B2B-0001` / `2.3.0` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。

## 2026-09-14
- hi_IN 店面 chrome：路径 locale + WidgetI18n；客服工具栏/活动标/批发信用漏译收口。
