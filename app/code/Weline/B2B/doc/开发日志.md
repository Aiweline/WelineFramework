<!-- weline:module-development-baseline:generated-once -->
# Weline_B2B 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 2026-09-09 — 恢复零售车/批发车双车 UI（2.6.20）

- 问题：B2B 已启用，但迷你车/购物车无「零售车」「批发车」分段（type-host 空；boot Hook 与双车注入 JS 在模块回迁后丢失）。
- 修：
  - 恢复 `view/hooks/Weline_Theme/frontend/layouts/base/body-end.phtml`（`data-weline-load=b2bSellingMode` + i18n）
  - 恢复 `selling-mode.js`：`ensureCartPageTypeSeg` / `syncCartPageChrome` / `enhanceMiniCarts` 注入分段
  - CSS `.b2b-mini-cart-type-seg`；访客仍恒 toc，切批发登录回跳
- 契约：`B2BStorefrontThemeUiContractTest` 断言 boot + 双车注入标记
- 版本：`2.6.19` → `2.6.20`

## 2026-09-09 — preferredMode 访客恒 toc（2.6.19）

- 问题：游客点「批发」会写 `weline_selling_mode=tob` 再跳登录；未登录回店时 `preferredMode()` 仍读 cookie→tob，购物车误打闸门文案「该售卖类型需要登录」。
- 修：`selling-mode.js` `preferredMode` 未登录恒返回 `toc`（cookie 仍可留给登录后）；契约断言。
- 版本：`2.6.18` → `2.6.19`。

## 2026-09-09 — 侧栏菜单 control-center 连字符对齐（2.6.19）

- 问题：客户组页地址为 `…/control-center/groups`，菜单仍写 `controlcenter/groups`，侧栏无法高亮/自动展开
- 变更：`etc/backend/menu.xml` 改为 `control-center/*` pretty action；高亮匹配由 Admin/Theme 连字符无关比较兜底

## 2026-09-09 — 审图：购买方式胶囊全圆角对齐加购（2.6.18）

- 分流：`web_ui` / `ui_shot`（红箭头：零售/批发仍偏方；对照右侧「加入购物车」胶囊）
- 原型调整：分段 + 游客 CTA 圆角改为 `--weline-theme-radius-full` / `--radius-full`（999px），与 PDP buy-box 一致
- 变更：`b2b-storefront.css`；cache `&v=20260909-amz-pill3`；`etc/module.php` → `2.6.18`
- 单测：契约断言 radius-full

## 2026-09-09 — 审图：购买方式亚马逊胶囊 + 登录态修复（2.6.17）

- 分流：`web_ui` / `ui_shot`（用户附图红框「零售/批发」双实心丑态；「昨天主题改过、今天又丑了」）
- 线稿：价格 → 购买方式标签 + [零售|批发] 双钮 → 灰字死胡同/实心登录 CTA → 运费说明
- 原型调整：
  1. 分段改为 Amazon 规格胶囊：浅底深字；选中 `--weline-chrome-body-link` 内描；未选浅灰描边；禁止双 `primary` 实心板
  2. 游客不激活 tob 展示；去掉「切换批发需先登录」死胡同；改为 outline「登录后使用批发」
  3. 根因：`SessionFactory::create()` 不存在 → catch 后 loggedIn 恒 false（已登录也被当游客）
- 变更：`selling-mode-switcher.phtml`（`createFrontendSession` + outline pills + guest CTA）；`selling-mode.js`；`b2b-storefront.css`；i18n；契约单测；`etc/module.php` → `2.6.17`
- 单测：`B2BStorefrontThemeUiContractTest` OK（1 / 55）
- Browser：禁缓存验收胶囊选中/未选对比 + outline CTA；交付 PDP 花朝记洛川页

## 2026-09-09 — 客户组页审图 UI（2.6.16）

- 分流：`web_ui` / `ui_shot`（仅附图客户组后台）
- 线稿：标题/面包屑 + 可管理徽章 + 技术告警 + 四字段扁平行（保存挤在 span-2）+ 空表「当前没有记录」
- 原型调整：字段区与主 CTA 分行右对齐；标题按 kind「新增客户组」；状态启用/停用；空态 `w-empty` + 下一步；告警去 rollout 黑话
- 变更：`view/templates/Backend/ControlCenter/index.phtml`；UT `Test/Unit/View/ControlCenterTemplateShentuTest`（新建文件走 MCP_TARGET native create）
- 验收：Browser 禁缓存客户组页

## 2026-09-09 — 批发配置页 config:embed 启停（2.6.15）

- 目标：与客服模块一致，在 B2B 菜单提供「批发配置」快捷开关，用统一配置嵌入启停零售/批发。
- 变更：
  - Extends `Config/frontend/selling-mode.phtml` 声明 `selling_mode_toc_enabled` / `selling_mode_tob_enabled`（switch，复用 SellingModePolicy 键）
  - `Controller/Backend/Config` + `view/templates/Backend/Config/index.phtml`：`<w:scope>` + `<w:config:embed>`；可选链统一配置中心
  - `etc/backend/menu.xml` 增加 `*/backend/config`；ACL `Weline_B2B::config`
  - `etc/module.php` → `2.6.15`（高于库内版本游标 `2.6.14`）
- 单测：`Test/Unit/Service/B2BSellingModeSettingsContractTest` OK（1 / 22 assertions）
- Browser：禁缓存打开 `…/b2b/backend/config?target_scope=default.default.default`，可见菜单「批发配置」、作用范围、零售/批发 switch（`w-config-embed`）
- 需求：`REQ-B2B-0008`

## 2026-09-09 — 迁移状态页人性化（2.6.1）

- 分流：`web_ui` / `ui_shot`（用户附图「看不懂」）
- 线稿：侧栏 + 标题/只读徽章 + 技术告警 + 英文键值网格
- 原型调整：首屏「当前结论」白话 + 中文标签行（灰度模式/白名单/环境锁定/执行方式/网页生产操作）+ 一句 hint；去掉主显 `mode`/`execution_policy` 等黑话
- 变更：`View/Backend/MigrationStatusPresenter`；ControlCenter 赋 `workspace_status_view`；模板渲染可读行；`etc/module.php` → `2.6.1`
- 单测：`Test/Unit/View/MigrationStatusPresenterTest` OK（2）
- 验收：Browser 禁缓存打开迁移状态页

## 2026-09-07 — Theme/店面 ToC/ToB 一期 UI（2.6.0）

- 当前状态：`implemented`（契约单测；Browser 验收待实例探活）
- 变更：
  - PDP：`after-price` Hook + `selling-mode-switcher`（Theme `w-button-group` / `w-badge` / `<lang>` / `w:form` + drawer）；cookie `weline_selling_mode`；游客切批发登录回跳；未入会打开申请抽屉
  - 数量：PDP qty `data-tob-moq/step=5`；加购传 `selling_mode`/`cart_type`
  - Mini-cart：`cart_type` badge；`getCart/summary` 带 cart_type
  - Checkout：定金 30% 说明 Hook；tob 隐藏优惠券槽；getData 透传 `cart_type`
  - 账户订单：hang_status 进度 + `purpose=deposit|balance` CTA；`doc/hang-status-state.md`
- 单测：`Test/Unit/View/B2BStorefrontThemeUiContractTest`
- 需求：`REQ-B2B-0007` Theme/i18n 表面

## 2026-09-07 — ToB 禁折 + 多行报价集 + 定金挂单（2.5.0）

- 当前状态：`implemented`（单测；Browser/E2E 未做）
- 变更：
  - Checkout `cart_type=tob` 跳过 Marketing DiscountQuote/券；定金基数=禁折后含税商品小计 × 30%（运费进尾款）；submit 延迟库存预占
  - `B2BCheckoutRecheckService::issueQuoteSet` / `submitQuoteSet` + `B2BQuoteTokenStore::consumeSetWith` 多行原子消费
  - `B2BHangOrderService` + `weline_b2b_order_hang`；ControlCenter approve/reject；Payment `onPartiallyPaid` 桥接
  - `CheckoutPaymentRecoveryStateService` 支持 `purpose=deposit|balance`；Order `order_type`/`type_payload` 写入；事件 hang_status 同步观察者
- 单测：`TobDiscountHangMultilineContractTest`
- 需求：`REQ-B2B-0005` → implemented

## 2026-09-07 — 价目 min_qty + ToB MOQ + 店面批发价（2.4.0）

- 变更：`PriceListItemRecord.min_qty`；Engine `qty` 档；`B2BCartQtyPolicy` + Cart `CommerceCartQtyPolicyInterface`；`B2BStorefrontPriceAdjustmentProvider`（exclusive absolute_minor）。
- 单测：价档 / MOQ / storefront provider + Assembler 既有测。
- 需求：`REQ-B2B-0006` + storefront 价。

## 2026-09-07 — SellingModePolicy + membership 申请（2.4.0）

- 当前状态：`implemented`（单测通过；Browser/E2E 未做）
- 变更：
  - `Service/SellingModePolicy.php`：Website/Store ConfigStore 开关 + 商品 flags fail-soft；`defaultMoq/Step=5`；会话偏好仅返回已启用 toc|tob
  - `Model/MembershipApplicationRecord` + `Service/MembershipApplicationService`：申请禁客户端 `group_id`；批准写 `CustomerGroupStore::assignCustomer`
  - `Service/CommerceTypeMembershipChecker` provides `Cart\Api\CommerceTypeMembershipCheckerInterface`
  - 后台 `ControlCenter::applications` 批准/驳回；Query `membership.submit`
  - Cart：`CommerceTypeMembershipGate` + `SellingTypeResolver` 可选 Gate（未配置则非 toc fail closed）
- 单测：`SellingModePolicyTest`、`MembershipApplicationServiceTest`、`CommerceTypeMembershipGateTest`
- 需求：`REQ-B2B-0003`/`REQ-B2B-0004` 进入实现；未改 `CustomerAccountService::register`

## 2026-09-07 — ToC/ToB 一期需求与合同文档

- 当前状态：`docs_planned`（仅文档；未改功能代码 / `etc/module.php`）
- 变更：`doc/需求.md` 保留 `REQ-B2B-0001`，追加 `REQ-B2B-0002`…`0007`（双 Registry 可卸载零探测税、SellingModePolicy、membership 申请、禁折+定金 hang、MOQ、事件/徽章/Theme/i18n）及一期非目标；`doc/b2b.md` 增加「ToC/ToB 一期」短节。
- 关联：`Weline_Cart/doc/cart.md`、`Weline_Order/doc/event/*` 合同对齐。
- 目标版本：ToC/ToB 一期（文档立项）

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `2.5.0` | implemented（unit） | `REQ-B2B-0005` 禁折/定金挂单/多行报价 | 2026-09-07 | 2026-09-07 | unit pass / not released |
| `2.4.0` | implemented（unit） | `REQ-B2B-0003`/`0004`/`0006` + storefront | 2026-09-07 | 2026-09-07 | unit pass / not released |
| `2.3.0`（文档基线） | docs_handoff | `REQ-B2B-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `2.3.0` — 文档制度启用基线

- 基线版本：`2.3.0`
- 目标版本：`2.3.0`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-B2B-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-B2B-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-B2B-0001` / `2.3.0` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
