<!-- weline:module-development-baseline:generated-once -->
# Weline_B2B 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 2026-09-10 — 信用不可用原因禁止空洞文案（2.6.52）

- 审图 `ui_shot`：红箭头指向「当前不可用批发信用」，点击勾选仍不知具体原因。
- 根因：① `b2b_credit=null` 时模板 `data-i18n-unavailable-generic` 覆盖具体 fallback；② `syncFromFrozen` 误选地址 `form`，报价写不进信用面板。
- 变更：Quote 拆分登录/定金/`额度不够` hint；Checkout 写入 `unavailableStub`；JS 映射具体 reason 并修正 root 选择器；删除空洞 generic。
- 验收：UT Quote/Widget 12/12；Browser 注入 no_balance→「额度不够…」、null→「请刷新后重试」，无「当前不可用批发信用」。

## 2026-09-10 — 信用页签 toc 就地原因竞态修复（2.6.51）

- 审图续验：零售车勾选禁用须就地原因 +「去购物车切换批发车」；cache-bust `checkout-tob.js?v=20260910-credit-inplace4`。
- 变更：`applyCartType` 后立刻 `syncCreditUi`；`syncCreditUi` 以 `readMode()` 判定 toc，避免 class 滞后；模块登记 query 刷新。
- 验收：Browser 批发信用页签 reason 可见、switch-cart 可见、勾选 disabled；UT 10/10。

## 2026-09-10 — 客户组编辑：输入框与翻译按钮分离（2.6.50）

- 审图：`ui_shot` Local 嵌输入槽导致名称/说明输入框消失，只剩翻译触发文案。
- 线稿：`[标签][翻译]` + 下方 `w-input`/`textarea`（对齐 Product 基础信息）。
- 变更：编辑弹窗恢复可见输入；vault `<local>` 短文案「翻译」挂 label 旁；JS 仅搬触发器；UT；`2.6.49` → `2.6.50`。

## 2026-09-10 — FAQ 动态 VIP 等级表（2.6.49）

- 需求：`/faq/b2b-wholesale` 按后台客户组配置、按分组与 `tier_rank` 排序写清消费门槛与达标折扣额度。
- 变更：`WholesaleFaqVipLadderPresenter` + FAQ 模板动态列表；停用组不展示；UT。

## 2026-09-10 — 信用勾选就地原因 + 竖排布局（2.6.48）

- 审图 `ui_shot`：红框布局乱；勾选禁用无原因；要求就地显示。
- 线稿：`[✓] 文案 ?` → **原因就地** → `抵扣额度` 全宽输入 → 链接行。
- 变更：合并 `data-b2b-credit-reason` 置于勾选下；去掉 `w-cluster` 横排；问号去下划线；toc/报价不可用均写入同一原因节点。

## 2026-09-10 — 结账信用不可用原因可读（2.6.48）

- 现网验收：勾选仍不可用，页上为零售车 `toc`（cookie `weline_selling_mode=toc`），原因「仅批发结账可用」被灰化层盖住不易读。
- 变更：原因文案移出灰化区并写清「零售结账 → 须切批发车」；增加「去购物车切换批发车」；CSS 原因行不降透明度。

## 2026-09-10 — 结账信用不可用原因 + 轻量问号（2.6.48）

- 审图：`ui_shot`（用户附图）空洞「当前不可用批发信用」+ 实心问号按钮突兀。
- 根因：`B2BCheckoutCreditQuote` 调 `rateSnapshot` / `convertMinorFromWebsiteDefault`，但 `B2BBaseCurrencyResolver` 未实现 → 报价抛错被 Checkout 吞掉 → `b2b_credit=null` → 前端只剩通用不可用。
- 线稿：`[?] 使用批发信用…` 实心钮 → 纯文本 `?`；不可用行须写具体 reason。
- 原型调整：帮助去 `w-button`，主题 muted 下划线 `?`；不可用改 muted + `hint_short`。
- 变更：Resolver 补双向换算与汇率快照；Quote 各 reason 填 hint；模板/CSS/JS；UT（bootstrap）；`2.6.47` → `2.6.48`。

## 2026-09-10 — 批发订单顶栏 / 信用页签拆分（2.6.46）

- 审图：`ui_shot` 页签内不应塞「批发订单」说明，信用页签只留抵扣控件。
- 变更：`summary-before` 恢复 `checkout-tob-order-note.phtml`；`checkout-tob-deposit-note.phtml` 仅信用面板；CSS 顶栏卡 vs 页签扁平；`checkout-tob.js` 顶栏 ToB 显隐、页签 ToC 灰化；UT；`2.6.45` → `2.6.46`。

## 2026-09-10 — FAQ section weline-code

- `frontend/faq/b2b-wholesale.phtml` 四处 `<section>` 补 `weline-code`，解除 `setup:upgrade` 的 `frontend-section-weline-code` 门禁阻塞。

## 2026-09-10 — 批发信用迁入结账摘要 extras 页签（2.6.45）

- 审图：`ui_shot` 箭头要求信用控件落到优惠券/订单留言同排。
- 需求纠偏：字面「支付模块注入」改为 B2B 拥有部件 + Checkout `checkout-summary-credit` 槽 `default_injections`（对齐 Marketing/Order），禁止 Payment 耦合。
- 变更：`extends/.../Weline_B2B/widget.php`；模板页签化；移除 `summary-before` Hook 直出；`checkout-tob.js` ToC 灰化；Checkout 槽目录+模板槽；UT。

## 2026-09-10 — 账户等级/优惠/下一档 + 客户组排序（2.6.44）

- 需求：前台个人中心可见当前客户等级、优惠与下一分组；分组有顺序（种子按 VIP）；可改排序数字或拖拽分组卡片。
- 需求纠偏：下一档按系统 VIP 阶梯（`SystemVipLadder`），不跟后台显示顺序混用；显示顺序复用 `tier_rank`。
- 变更：`AccountMembershipTierPresenter` + 账户侧栏等级/权益/下一档；`CustomerGroupStore::reorderGroups`；编辑「排序数字」；`control-center-groups-reorder.js` 手柄拖卡片；成员行拖放换组不拦截；UT；`2.6.44`。

## 2026-09-10 — 成员拖放换组

- 审图：`ui_shot` 箭头指「移出」；需求「拖动移动分组」纠偏为**拖成员到目标组卡片换组**（不重排系统 VIP 阶梯）。
- 线稿：行 `[⋮⋮ 姓名/#id]` 可拖 → 目标组 outline 高亮 → `assignGroupMember`。
- 变更：`control-center-groups-members.js` HTML5 DnD + `data-assign-url` POST；提示文案；UT；保留「移出」。

## 2026-09-10 — 客户组名称/说明 LocalModel（`<local>`）

- 审图：编辑弹窗「名称」「等级说明」需业务多语言，非词典 `__()`。
- 线稿：弹窗槽位挂载 `<local field=name|description>`；主表保留源语回退。
- 变更：`CustomerGroupRecord\LocalDescription` + Upgrade 建表种子 + ControlCenter vault/槽位 JS；模块 `2.6.47`。

## 2026-09-10 — 达标折扣额度文案（门槛→折扣因果）

- 审图：列表「目标」无法表达「消费达标后授予的可抵扣折扣额度」。
- 线稿：`消费门槛 · 达标折扣额度`；编辑先门槛后折扣；提示「达标后补到此折扣额度，仅可抵扣批发订单」。
- 变更：ControlCenter + 账户侧「目标授信」→「折扣额度」；审图 UT；模块 `2.6.45`。

## 2026-09-10 — 客户组成员可读展示（姓名主显 / #id 次显）

- 审图：`ui_shot` 成员表客户列只显示裸 `47`、站点裸 `0`，不可扫。
- 线稿：`[姓名/邮箱] / [#id · email] | 站点名 | 更新 | 移出`。
- 原型：主信号可读身份，ID 作核对次要信息；站点用名称（默认站）。
- 变更：`GroupMemberDisplayPresenter` 富化 + 姓名/邮箱搜索；`control-center-groups-members.js` 渲染；占位「搜索姓名、邮箱或编号」；UT。

## 2026-09-10 — VIP 消费门槛曲线调平（vip12≥100万）

- 原等差 `tier×5000` 过陡；改为前缓后快：5千→1.5万→…→vip12 **100万**；授信额度同步放缓且不超过门槛。
- 仍为旧线性默认金额的系统组，在 `ensureSystemVipLadder` 时自动迁到新种子。

## 2026-09-10 — 客户组成员手风琴 + 基准货币提示

- 需求：展开分组异步加载/搜索/添加/移出成员；目标额度按站点默认基准货币，异币种消费换算后累计。
- 变更：`B2BBaseCurrencyResolver`；`MembershipSpendQuery` FX；`listMembersByGroup` + ControlCenter `group-members`/`assign`/`remove`；手风琴 UI + 基准货币提示；UT。

## 2026-09-10 — 客户组列表人性化（只读表 + 编辑弹窗）

- 审图：操作列塞改名/能力表单不可扫；「保存能力」难懂；状态英文；额度用分。
- 线稿：列表只读（名称+说明摘要 / 状态徽章 / ¥元）· 操作「编辑」· `w-dialog` 标签字段一次保存。
- 变更：`ControlCenter/index.phtml` + `control-center-groups-edit.js`；`B2BAdminService::updateGroupCapabilities` 支持 `name`；UT/审图契约；本条文档。

## 2026-09-10 — ControlCenter 全工作区作用范围（2.6.43）

- 需求：批发模块后台都要网站/店铺/渠道范围，不只身份申请。
- 线稿：顶栏 `[作用范围 <w:scope>]` · 徽章；列表按 `website_id` / `channel_id` 过滤。
- 变更：`B2BBackendScopeTrait` + `partials/scope-toolbar.phtml` + JS；`renderWorkspace` 统一过滤；写回保留 `target_scope`；兼容旧 `website_id`；UT；`2.6.43`。

## 2026-09-10 — 删除身份申请 POST 404（2.6.42）

- 根因：框架将控制器 `delete*` 注册为仅 HTTP `DELETE`；表单 `method=post` 命中 `delete-membership-application` → 404。
- 变更：`ControlCenter::removeMembershipApplication` + path `remove-membership-application`（`request_method` 空）；Service/ACL 仍 `delete*`；UT；`2.6.42`。
- 验收：表单 action 已换；POST 删除 `app-80b9a88d…` 回列表无 404，行数 2→1。

## 2026-09-10 — syncButtons 覆盖列表卡加购（2.6.41）

- 配合 Cart 1.3.59：批发加购污染零售。
- `selling-mode.js` `syncButtons` 增加 `product-card-add-to-cart` / 卡内 `[data-action=add]`；契约锁定。
- 版本：`2.6.40` → `2.6.41`。

## 2026-09-10 — 身份申请删除 ACL（2.6.40）

- 审图分流：`web_ui` / `ui_shot`（操作列仅撤销，需可删重复申请）
- 线稿：操作 | … | [删除]（`<acl source="…applications:delete">`）
- 变更：`MembershipApplicationService::delete`；当时控制器 `deleteMembershipApplication` 独立 ACL；模板 `<acl>` 包裹；UT；`2.6.40`（2.6.42 起动作改为 `removeMembershipApplication`）

## 2026-09-10 — 身份申请唯一：编辑后重新提交当前记录（2.6.39）

- 审图分流：`web_ui` / `ui_shot`（后台身份申请同客户双行：资格已撤销 + pending）
- 需求纠偏：仅改前端不够；服务端 upsert 为硬门禁；`approved` 无资格投影改为 `UI_INACTIVE`
- 线稿：账户/抽屉「修改资料 → 重新提交当前申请」；禁止再插新行
- 原型：customer+website 单申请身份；resubmit = update same id → pending
- 变更：`MembershipApplicationService::submit` upsert；投影；account/selling-mode 预填+文案；UT；`2.6.39`

## 2026-09-10 — 资格已撤销行可重新授权（2.6.38）

- 需求纠偏：原原型撤销后仅前台再申请；运营面缺重新授权
- 线稿：资格已撤销 | [组搜索选] [重新授权]
- 变更：`MembershipApplicationService::reauthorize`；ControlCenter ACL；行内 UI；UT；`2.6.38`

## 2026-09-10 — 批发抽屉登录挂载竞态修复（2.6.37）

- 分流：`web_ui` / `ui_shot`（toast 已撤销但行仍 approved + 撤销按钮）
- 根因：申请审计不改写；列表误把 `approved` 当生效资格
- 变更：`enrichApplicationEntitlement` 投影 `entitlement_active`；无资格行显示「资格已撤销」并隐藏行内撤销；UT；i18n；`2.6.36`

## 2026-09-10 — 身份申请：顶部站点范围 + 行内撤销 + 多选快速撤销（2.6.35）

- 分流：`web_ui` / `ui_shot`（撤销仅页底单站、列表已批行无操作、无顶部范围）
- 线稿：顶栏「站点范围」auto-submit → 整表过滤；已批行组码 + 撤销；页底「快速撤销」网站多选
- 变更：`ControlCenter::applications` GET `website_id`；模板 Taglib scope/row-revoke/`website_ids`；`B2BAdminService::revokeMembership` 多站；写回保留单站过滤；UT；i18n；`etc/module.php` → `2.6.35`

## 2026-09-10 — 批准失败可读化 + 本机白名单（2.6.34 hotfix）

- 根因：`commerce_rollout_immutable:off`（B2B 灰度关闭）导致 `approveMembershipApplication` 写门禁失败，toast 仅「操作失败」
- 变更：`B2BAdminService` 转 `B2BConflictException` 可读文案；`ControlCenter` 透出冲突消息；申请页灰度阻断告警；本机 `website:0` allowlist 开启
- 验收：Browser 批准 vip0 → status=approved

## 2026-09-10 — VIP0–12 阶梯 + 批发信用额度（2.6.34）

- 需求：`REQ-B2B-0010`（一期非目标账期子集：钱包式 `b2b_credit` 授信 + tob 优惠门禁）
- 模型：`CustomerGroupRecord` 增 description/credit_limit_minor/spend_threshold_minor/tier_rank；`ensureSystemVipLadder` 种子 13 档并迁移旧 `g-system-vip`
- 配置：`b2b_credit_enabled`（global）embed 进批发配置；关则不授信/不抵扣/不升档
- Payment：`PaymentAssetPolicyProviderInterface` SPI；B2B 贡献 `b2b_credit`（ROLE_DISCOUNT + required_order_types=tob）
- 服务：`B2BCreditGrantService` 批准/升档补差额；`MembershipSpendQuery`；Cron `MembershipSpendUpgrade`
- UI：批准 search-select 默认第一项（vip0）；组能力编辑；前台等级说明
- 单测：阶梯/授信/SPI/Cron/embed 契约；Payment `1.9.41`；`etc/module.php` → `2.6.34`

## 2026-09-10 — 后台运营面去手填 ID + 默认 VIP 组（2.6.33）

- 需求纠偏：整模块改为 ControlCenter 可写运营页（客户组/价目/报价/挂单/身份申请）；店面与迁移只读本回合不改
- 线稿/原型：表单用人名/标签搜索选；系统 VIP 默认可改名不可删；挂单行内批准
- 变更：`CustomerGroupRecord` 增 `name`/`is_system`；`CustomerGroupStore` ensure/rename/delete；`B2BAdminService` 自动 group/list id；`ControlCenter` UI + ACL；UT；`etc/module.php` → `2.6.33`

## 2026-09-10 — 审图：身份申请标签搜索选（2.6.32）

- 分流：`web_ui` / `ui_shot`（运营手填申请单/客户组/客户 ID）
- 线稿：三列手填 ID 卡 + 宽表；运营无法记忆 UUID
- 原型调整：Taglib 优先——行内客户组 `<w:theme:search-select>`；撤销客户 `<w:customer:admin:select>`；禁止手填 ID 主路径
- 变更：`ControlCenter/index.phtml`；UT；`etc/module.php` → `2.6.32`
- 单测：`ControlCenterTemplateShentuTest`

## 2026-09-10 — 审图：身份申请行内选组批准（2.6.31）

- 分流：`web_ui` / `ui_shot`（后台身份申请；表与手填批准表单脱节）
- 线稿：告警 + 三列手填卡（申请单ID/组ID 空）+ 原始宽表 + pending 行无操作
- 原型调整：批准主路径迁到 pending 行（`w-select` 客户组 + 批准/驳回）；顶部仅保留撤销区；无可用组时告警引导先建客户组
- 变更：`CustomerGroupStore::listActiveOptions`；`B2BAdminService::listActiveGroupOptions`；`ControlCenter::applications` 赋值；`ControlCenter/index.phtml`；UT；`etc/module.php` → `2.6.31`
- 单测：`CustomerGroupStoreListOptionsTest` + `ControlCenterTemplateShentuTest`

## 2026-09-10 — 批发申请表单强制主题 Outlined notch（2.6.30）

- 审图：`web_ui` / `ui_shot`（批发身份申请胶囊标签条）。
- 修：申请字段包 `w-field__control`；`b2b-storefront.css` `gap:0` + `--weline-theme-field-label-bg`；CSS bust `apply-notch1`。
- 覆盖 PDP 抽屉与账户侧栏同一套表单。

## 2026-09-10 — 批发身份状态投影与轮询（2.6.29）

- `MembershipStatusProjection`：申请单 vs 生效资格；有效 membership 强制 `active` 且停 poll；撤资格禁止「已批准」CTA。
- `latestForCustomer`；`submit` 拒绝重复 pending / 已开通；`unassignCustomer` + 后台撤销；Query `membership.status`（45s 节流）。
- PDP CTA「等待申请通过」；`selling-mode.js` 首探 45s 后每 5 分钟 poll；通过后刷新价/车；visibility 机会刷新。
- 在途 hang：撤资格后新交易 fail-closed；已生成挂单按原状态机继续。
- 版本：`2.6.28` → `2.6.29`。

## 2026-09-10 — 适配 Cart cart-type-changed Event（2.6.28）

- `setMode` 双发 `weline:cart-type-changed`；监听外源切换；页签补 `data-cart-type-option`。
- Theme/Cart 不再直点 `data-b2b-mini-cart-type-option`。
- 版本：`2.6.27` → `2.6.28`。

## 2026-09-10 — 批发申请表单 label 对齐 Outlined notch（2.6.27）

- `selling-mode-switcher.phtml` 申请字段：`w-label` → `w-field__label`（已在 `w-field` 内）。
- 版本：`2.6.26` → `2.6.27`。

## 2026-09-10 — 访客页签默认 toc，忽略残留 tob cookie（2.6.26）

- 未登录 `preferredMode` 忽略 `weline_selling_mode=tob` cookie，冷启动 toc；本页点选写入 `weline_cart_type_explicit`。
- 与 Cart 空车 sibling 推荐互补，避免批发空死胡同。
- 版本：`2.6.25` → `2.6.26`。

## 2026-09-10 — 批发申请抽屉顶栏文案收口（2.6.25）

- 审图：`web_ui` / `ui_shot`；红框区「登录后即可提交…」「请使用下方…」「登录」「使用您的账户继续」叠层啰嗦。
- 修：导语改一句利益点「登录后即可申请批发价与数量阶梯优惠。」；挂载 `data-weline-login-panel-heading="0"` 省略面板标题/副标题。
- 版本：`2.6.24` → `2.6.25`。

## 2026-09-10 — 批发抽屉挂完整登录面板（2.6.24）

- 宿主改 `customer/login-panel`（快捷+账密）；文案「请使用下方登录面板」。
- 版本：`2.6.23` → `2.6.24`。

## 2026-09-10 — 批发挂载改调框架 Weline.mount（2.6.23）

- 宿主加 `data-weline-mount-load="account"`；打开抽屉 `Weline.mount.scan`（不再调 Account.scanMounts）。
- 协议权威：`Theme/doc/前端JS模块加载规范.md` §1.0。
- 版本：`2.6.22` → `2.6.23`。

## 2026-09-10 — 批发申请声明式挂载 customer/social-quick（2.6.22）

- 抽屉引导「请使用下方快捷登录」；宿主 `data-weline-mount="customer/social-quick"`（inline + suppress-floating）。
- 打开抽屉 `scanMounts({force:true})`；不再依赖「本页登录」按钮拉页角条。
- 挂载面权威：`Weline_Customer/doc/挂载面.md`。
- 版本：`2.6.21` → `2.6.22`。

## 2026-09-09 — 审图：批发申请「本页登录」无反应（2.6.21）

- 审图分流：`web_ui` + `ui_shot`（附图：申请批发身份 → 抽屉「本页登录」）。
- 根因：`promptGuestLogin` 只调 `WelineSocialQuick.boot()`；页面已 booted / 条被关掉后不再显示；状态写到隐藏表单区；打开抽屉还自动烧一次 one-shot。
- 修：强制 `maybeStartSocialQuickPrompt({force:true})` + `reveal` 重开快捷登录条；门闸可见回落链接；打开抽屉不再自动弹登录；Customer social-quick 支持 force/reveal。
- 版本：`2.6.20` → `2.6.21`。

## 2026-09-09 — 恢复零售车/批发车双车 UI（2.6.20）

- 问题：B2B 已启用，但迷你车/购物车无「零售车」「批发车」分段（type-host 空；boot Hook 与双车注入 JS 在模块回迁后丢失）。
- 修：
  - 恢复 `view/hooks/Weline_Theme/frontend/layouts/base/body-end.phtml`（`data-weline-load=b2bSellingMode` + i18n）
  - 恢复 `selling-mode.js`：`ensureCartPageTypeSeg` / `syncCartPageChrome` / `enhanceMiniCarts` 注入分段
  - CSS `.b2b-mini-cart-type-seg`；访客仍恒 toc，切批发登录回跳
- 契约：`B2BStorefrontThemeUiContractTest` 断言 boot + 双车注入标记
- 版本：`2.6.19` → `2.6.20`

## 2026-09-09 — preferredMode 访客恒 toc（2.6.19）

- 问题：游客点「批发」会写 `weline_selling_mode=tob` 再跳登录；未登录回店时 `preferredMode()` 仍读 cookie→tob，购物车误打闸门文案「该售卖类型需要登录」。
- 修：`selling-mode.js` `preferredMode` 未登录恒返回 `toc`（cookie 仍可留给登录后）；契约断言。
- 版本：`2.6.18` → `2.6.19`。

## 2026-09-09 — 侧栏菜单 control-center 连字符对齐（2.6.19）

- 问题：客户组页地址为 `…/control-center/groups`，菜单仍写 `controlcenter/groups`，侧栏无法高亮/自动展开
- 变更：`etc/backend/menu.xml` 改为 `control-center/*` pretty action；高亮匹配由 Admin/Theme 连字符无关比较兜底

## 2026-09-09 — 审图：购买方式胶囊全圆角对齐加购（2.6.18）

- 分流：`web_ui` / `ui_shot`（红箭头：零售/批发仍偏方；对照右侧「加入购物车」胶囊）
- 原型调整：分段 + 游客 CTA 圆角改为 `--weline-theme-radius-full` / `--radius-full`（999px），与 PDP buy-box 一致
- 变更：`b2b-storefront.css`；cache `&v=20260909-amz-pill3`；`etc/module.php` → `2.6.18`
- 单测：契约断言 radius-full

## 2026-09-09 — 审图：购买方式亚马逊胶囊 + 登录态修复（2.6.17）

- 分流：`web_ui` / `ui_shot`（用户附图红框「零售/批发」双实心丑态；「昨天主题改过、今天又丑了」）
- 线稿：价格 → 购买方式标签 + [零售|批发] 双钮 → 灰字死胡同/实心登录 CTA → 运费说明
- 原型调整：
  1. 分段改为 Amazon 规格胶囊：浅底深字；选中 `--weline-chrome-body-link` 内描；未选浅灰描边；禁止双 `primary` 实心板
  2. 游客不激活 tob 展示；去掉「切换批发需先登录」死胡同；改为 outline「登录后使用批发」
  3. 根因：`SessionFactory::create()` 不存在 → catch 后 loggedIn 恒 false（已登录也被当游客）
- 变更：`selling-mode-switcher.phtml`（`createFrontendSession` + outline pills + guest CTA）；`selling-mode.js`；`b2b-storefront.css`；i18n；契约单测；`etc/module.php` → `2.6.17`
- 单测：`B2BStorefrontThemeUiContractTest` OK（1 / 55）
- Browser：禁缓存验收胶囊选中/未选对比 + outline CTA；交付 PDP 花朝记洛川页

## 2026-09-09 — 客户组页审图 UI（2.6.16）

- 分流：`web_ui` / `ui_shot`（仅附图客户组后台）
- 线稿：标题/面包屑 + 可管理徽章 + 技术告警 + 四字段扁平行（保存挤在 span-2）+ 空表「当前没有记录」
- 原型调整：字段区与主 CTA 分行右对齐；标题按 kind「新增客户组」；状态启用/停用；空态 `w-empty` + 下一步；告警去 rollout 黑话
- 变更：`view/templates/Backend/ControlCenter/index.phtml`；UT `Test/Unit/View/ControlCenterTemplateShentuTest`（新建文件走 MCP_TARGET native create）
- 验收：Browser 禁缓存客户组页

## 2026-09-09 — 批发配置页 config:embed 启停（2.6.15）

- 目标：与客服模块一致，在 B2B 菜单提供「批发配置」快捷开关，用统一配置嵌入启停零售/批发。
- 变更：
  - Extends `Config/frontend/selling-mode.phtml` 声明 `selling_mode_toc_enabled` / `selling_mode_tob_enabled`（switch，复用 SellingModePolicy 键）
  - `Controller/Backend/Config` + `view/templates/Backend/Config/index.phtml`：`<w:scope>` + `<w:config:embed>`；可选链统一配置中心
  - `etc/backend/menu.xml` 增加 `*/backend/config`；ACL `Weline_B2B::config`
  - `etc/module.php` → `2.6.15`（高于库内版本游标 `2.6.14`）
- 单测：`Test/Unit/Service/B2BSellingModeSettingsContractTest` OK（1 / 22 assertions）
- Browser：禁缓存打开 `…/b2b/backend/config?target_scope=default.default.default`，可见菜单「批发配置」、作用范围、零售/批发 switch（`w-config-embed`）
- 需求：`REQ-B2B-0008`

## 2026-09-09 — 迁移状态页人性化（2.6.1）

- 分流：`web_ui` / `ui_shot`（用户附图「看不懂」）
- 线稿：侧栏 + 标题/只读徽章 + 技术告警 + 英文键值网格
- 原型调整：首屏「当前结论」白话 + 中文标签行（灰度模式/白名单/环境锁定/执行方式/网页生产操作）+ 一句 hint；去掉主显 `mode`/`execution_policy` 等黑话
- 变更：`View/Backend/MigrationStatusPresenter`；ControlCenter 赋 `workspace_status_view`；模板渲染可读行；`etc/module.php` → `2.6.1`
- 单测：`Test/Unit/View/MigrationStatusPresenterTest` OK（2）
- 验收：Browser 禁缓存打开迁移状态页

## 2026-09-07 — Theme/店面 ToC/ToB 一期 UI（2.6.0）

- 当前状态：`implemented`（契约单测；Browser 验收待实例探活）
- 变更：
  - PDP：`after-price` Hook + `selling-mode-switcher`（Theme `w-button-group` / `w-badge` / `<lang>` / `w:form` + drawer）；cookie `weline_selling_mode`；游客切批发登录回跳；未入会打开申请抽屉
  - 数量：PDP qty `data-tob-moq/step=5`；加购传 `selling_mode`/`cart_type`
  - Mini-cart：`cart_type` badge；`getCart/summary` 带 cart_type
  - Checkout：定金 30% 说明 Hook；tob 隐藏优惠券槽；getData 透传 `cart_type`
  - 账户订单：hang_status 进度 + `purpose=deposit|balance` CTA；`doc/hang-status-state.md`
- 单测：`Test/Unit/View/B2BStorefrontThemeUiContractTest`
- 需求：`REQ-B2B-0007` Theme/i18n 表面

## 2026-09-07 — ToB 禁折 + 多行报价集 + 定金挂单（2.5.0）

- 当前状态：`implemented`（单测；Browser/E2E 未做）
- 变更：
  - Checkout `cart_type=tob` 跳过 Marketing DiscountQuote/券；定金基数=禁折后含税商品小计 × 30%（运费进尾款）；submit 延迟库存预占
  - `B2BCheckoutRecheckService::issueQuoteSet` / `submitQuoteSet` + `B2BQuoteTokenStore::consumeSetWith` 多行原子消费
  - `B2BHangOrderService` + `weline_b2b_order_hang`；ControlCenter approve/reject；Payment `onPartiallyPaid` 桥接
  - `CheckoutPaymentRecoveryStateService` 支持 `purpose=deposit|balance`；Order `order_type`/`type_payload` 写入；事件 hang_status 同步观察者
- 单测：`TobDiscountHangMultilineContractTest`
- 需求：`REQ-B2B-0005` → implemented

## 2026-09-07 — 价目 min_qty + ToB MOQ + 店面批发价（2.4.0）

- 变更：`PriceListItemRecord.min_qty`；Engine `qty` 档；`B2BCartQtyPolicy` + Cart `CommerceCartQtyPolicyInterface`；`B2BStorefrontPriceAdjustmentProvider`（exclusive absolute_minor）。
- 单测：价档 / MOQ / storefront provider + Assembler 既有测。
- 需求：`REQ-B2B-0006` + storefront 价。

## 2026-09-07 — SellingModePolicy + membership 申请（2.4.0）

- 当前状态：`implemented`（单测通过；Browser/E2E 未做）
- 变更：
  - `Service/SellingModePolicy.php`：Website/Store ConfigStore 开关 + 商品 flags fail-soft；`defaultMoq/Step=5`；会话偏好仅返回已启用 toc|tob
  - `Model/MembershipApplicationRecord` + `Service/MembershipApplicationService`：申请禁客户端 `group_id`；批准写 `CustomerGroupStore::assignCustomer`
  - `Service/CommerceTypeMembershipChecker` provides `Cart\Api\CommerceTypeMembershipCheckerInterface`
  - 后台 `ControlCenter::applications` 批准/驳回；Query `membership.submit`
  - Cart：`CommerceTypeMembershipGate` + `SellingTypeResolver` 可选 Gate（未配置则非 toc fail closed）
- 单测：`SellingModePolicyTest`、`MembershipApplicationServiceTest`、`CommerceTypeMembershipGateTest`
- 需求：`REQ-B2B-0003`/`REQ-B2B-0004` 进入实现；未改 `CustomerAccountService::register`

## 2026-09-07 — ToC/ToB 一期需求与合同文档

- 当前状态：`docs_planned`（仅文档；未改功能代码 / `etc/module.php`）
- 变更：`doc/需求.md` 保留 `REQ-B2B-0001`，追加 `REQ-B2B-0002`…`0007`（双 Registry 可卸载零探测税、SellingModePolicy、membership 申请、禁折+定金 hang、MOQ、事件/徽章/Theme/i18n）及一期非目标；`doc/b2b.md` 增加「ToC/ToB 一期」短节。
- 关联：`Weline_Cart/doc/cart.md`、`Weline_Order/doc/event/*` 合同对齐。
- 目标版本：ToC/ToB 一期（文档立项）

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `2.5.0` | implemented（unit） | `REQ-B2B-0005` 禁折/定金挂单/多行报价 | 2026-09-07 | 2026-09-07 | unit pass / not released |
| `2.4.0` | implemented（unit） | `REQ-B2B-0003`/`0004`/`0006` + storefront | 2026-09-07 | 2026-09-07 | unit pass / not released |
| `2.3.0`（文档基线） | docs_handoff | `REQ-B2B-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `2.3.0` — 文档制度启用基线

- 基线版本：`2.3.0`
- 目标版本：`2.3.0`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-B2B-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-B2B-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-B2B-0001` / `2.3.0` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
