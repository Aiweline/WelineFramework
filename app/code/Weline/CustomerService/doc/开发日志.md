## 2026-09-21 — 客服 body-end 游客 SSR（1.3.84）

- `isLoggedIn`/`customerId` 固定游客；TemplateCachePolicy → `static`；登录态交互水合。
- 版本：`1.3.83` → `1.3.84`。

## 2026-09-18 — 静态错误页发布不注入客服多语词典（1.3.83）

- `body-end` 在 `StaticErrorPagePublisher::CTX_PUBLISHING` 时 `$widgetTranslations = []`（按语种 SSR 快照无需客户端词表）。
- 契约：`CustomerServiceWidgetUiContractTest::testBodyEndHookOmitsWidgetTranslationsDuringStaticErrorPublish`。
- 版本：`1.3.82` → `1.3.83`。

## 2026-09-18 — 页头「客户服务」默认站全语种（1.3.82）

- 各启用语 `i18n/{locale}.csv` 补 `客户服务` 真译文（配合 Theme chrome 按语种快照）。

## 2026-09-17 — 店面全局浮层未注入（Framework Hook meta）

- 现象：页头/页脚客服入口在，右下角 `#customer-service-widget` 不在 HTML。
- 根因：Framework `HookReader::getFileListWithMeta` 未合并残缺 `generated/hooks.php`（见 Framework 2.5.92）；本模块 Hook 文件与开关均正常。
- 协同：Framework 修复 + `hook:rebuild`；本模块无代码变更。

## 2026-09-16 — 预览同构：客服 Hook 不再跳过预览（1.3.81）

- 问题：`visual_editor` / `preview=1` / `workspace-preview` 直接 return，预览无悬浮客服。
- 修：仅账号路由跳过（发布与预览同一规则）；遵守 `preview_storefront_delivery_parity`。
- 契约：`CustomerServiceWidgetUiContractTest::testBodyEndHookDoesNotSkipOnPreviewOrVisualEditor`。
- 版本：`1.3.80` → `1.3.81`。

## 2026-09-15 — 页头客户服务部件默认注入右簇（1.3.80）

- 新增 `header-contact-service-link` → `header-nav-extensions`（homepage required，sort 20）；点击打开悬浮客服（同页脚联系客服）。
- Theme 去掉硬编码「客户服务」→ `/faq`。
- 契约：`HeaderContactServiceLinkWidgetContractTest`。
- 版本：`1.3.79` → `1.3.80`。

## 2026-09-15 — 看过刷新后仍显示 1 条未读

- 根因：一点开就把 `unreadCount=0` 写入 localStorage，但 `lastReadMessageId` 仍是旧水位；刷新后按旧水位重算又出现未读。
- 修复：开窗先 `markChatRead()` 抬水位再落盘；`markChatReadFromServer` 异步完成前已有正确水位；水位≥当前最大 id 强制清零；`message_id`/`id` 兼容；已读与 `sessionId` 绑定。
- 契约：`CustomerServiceUnreadBreathContractTest`。
- 交付：[店面首页](https://p05113ef3.test.weline.com:9555/)

## 2026-09-15 — 店面浮钮收起后未读跳动/条数失效

- 根因：① 收起聊天 `stopPolling()` 停掉消息轮询；② 右下角「快速登录」`z-index:2147483000` 盖住客服浮钮。
- 修复：收起仅停状态轮询，保持/`startBackgroundUnreadWatch` 消息轮询 + 基线防历史刷未读；未读/开窗时 widget `z-index:2147483001`。
- 契约：`CustomerServiceUnreadBreathContractTest`。
- 交付：[店面首页](https://p05113ef3.test.weline.com:9555/)

## 2026-09-15 — 点开看过即清空未读，下次从零累积

- 问题：看过仍提示，条数也不对。
- 修复：`markChatReadFromServer` 打开/收起时拉齐服务端最大 message_id 作已读水位并强制 `unreadCount=0`；关闭时只统计水位之后的新客服消息；无水位时先打基线不堆历史。
- 交付：[店面首页](https://p05113ef3.test.weline.com:9555/)

## 2026-09-15 — 已打开看过的会话不再未读提示

- 问题：点开看过仍提示未读。
- 根因：开窗轮询只清 `unreadCount` 并 `saveState`，未抬高 `lastReadMessageId`，收起/刷新后又按旧水位重算。
- 修复：开窗加载/轮询/收起均 `markChatRead`（含 DOM 最大 message_id）；开窗时同步抬已读水位。
- 交付：[店面首页](https://p05113ef3.test.weline.com:9555/)

## 2026-09-15 — 刷新后未读提示仍保留

- 问题：有未读时刷新页面，角标/呼吸消失。
- 修复：持久化 `lastReadMessageId` + `unreadCount`；启动先恢复本地未读再 `syncUnreadFromServer` 校正；打开/收起时 `markChatRead`；加强呼吸动画并避免 transform transition 冲掉动画。
- 交付：[店面首页](https://p05113ef3.test.weline.com:9555/)

## 2026-09-15 — 店面客服收起后未读跳动/条数

- 根因：收起聊天时 `stopPolling()` 停掉消息轮询；右下角社交快速登录条 z-index 盖住浮钮。
- 修复：收起仅停状态轮询、保持消息轮询；有会话 token 时后台基线+盯未读；未读/打开时浮钮上移并抬高层级盖过社交条。
- 契约：`CustomerServiceUnreadBreathContractTest`。
- 交付：[店面首页](https://p05113ef3.test.weline.com:9555/)

## 2026-09-15 — 工作台会话列表登录用户显示姓名

- 审图：`web_ui` / `ui_shot`（「我的会话」标题仅 `#6`）。
- 修复：`enrichConsoleSessionRows` 附带 `customer_kind` / `customer_display_name` / `list_title`；SSR + `backend-console.js` 登录用户优先显示姓名，访客仍 `#id`；聊天头同步。
- 契约：`ConsoleSessionPriorityContractTest` 标题断言。
- 验收：会话 `#6` → `We Line`；UT PASS。
- 交付：[客服工作台](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## 2026-09-15 — 未读呼吸浮钮 + load 后延 3s 加载

- 未读：`cs-chat-button.has-unread` 呼吸缩放；`#cs-presence-dot` 绿色点；打开会话后清除。
- 加载：去掉 idle 抢跑；`window.load` 完成后再 `setTimeout(3000)` 拉 `customer-service.js`，减少与正文争抢。
- 契约：`CustomerServiceUnreadBreathContractTest` + Lazy/UI 契约断言。

## 2026-09-14 — 客服设置面板九语漏译（WIDGET_KEYS）

- 审图：`web_ui` / `ui_shot`（「我的语言」已选 हिन्दी/العربية，设置壳仍中文：客服服务/我的语言/显示模式/仅显示译文/AI 智能客服）。
- 根因：`hi_IN`/`bn_BD`/`id_ID`/`ur_PK` 等 CSV 对 `WIDGET_KEYS` 中文透传；`fr_FR`/`es_ES`/`pt_BR` 亦有次要键缺口；FPC 旧页可暂存旧字典。
- 修复：补齐默认站九语客服 CSV 设置/状态/工具键；`i18n:collect Weline_CustomerService`；契约 `WidgetSettingsLocaleCsvContractTest`（ar/hi/bn/id/ur/en/fr/es/pt）。
- 验收：阿语站打开设置 → 阿语文案；切 हिन्दी → 天城文；再切回阿语 OK。UT 56/56 PASS。

## 2026-09-14 — 默认站 chrome 漏译补齐（1.3.79）

- 默认站八语补齐店面 chrome 漏译：图片/文件/客服服务/设置/收起（bn_BD/id_ID/ur_PK）。

## Hotfix — 2026-09-14：九语「修改邮箱身份 / 聊天工具」（1.3.78）

- 壳层 aria/title 多语 identity；九语 upsert + collect。

## Hotfix — 2026-09-14：hi_IN 身份芯片中文泄漏（1.3.75）

- 现象：客服身份芯片 `title`/`点击修改邮箱身份` 在 hi_IN 仍中文身份。
- 修复：`i18n/hi_IN.csv` 改为天城文；契约断言 Devanagari。
- 版本：`1.3.74` → `1.3.75`

## Hotfix — 2026-09-13：PDP sticky 时客服浮钮上移

- 审图：`web_ui` / `ui_shot`（客服圆钮压住 sticky「暂不可售/立即购买」）。
- 修复：`.customer-service-widget` 的 `bottom` 叠加 `--weline-product-sticky-purchase-clearance`（Product 在 sticky 可见时注入；缺省 0）。
- 验证：Product sticky e2e `e2e-ch2-floats` / plan-suite PASS。

## Hotfix — 2026-09-10：工具栏切英语仍中文（1.3.73）

- 审图：`web_ui` / `ui_shot`（My Language=English 但 表情/图片/文件/截图 仍中文）。
- 根因：`en_US` 等 CSV 身份译文 + `updateWidgetLocaleText` 未重绘 composer 工具按钮。
- 修复：补齐 widget 字典键与多语译文；切语言时同步 `data-cs-tool` 文案与 toolbar aria-label。
- 版本：`1.3.72` → `1.3.73`

## Hotfix — 2026-09-10：工具栏切英语仍中文（1.3.73）

- 审图：`web_ui` / `ui_shot`（语言已 English，表情/图片/文件/截图仍中文）。
- 根因：`en_US.csv` 工具键未译；`updateWidgetLocaleText` 未重绘 `data-cs-tool` 按钮。
- 修复：补齐 widget 字典键与多语言 CSV；切语言时同步工具栏文案与 `aria-label`。
- 版本：`1.3.72` → `1.3.73`

## Hotfix — 2026-09-10：截图改浏览器原生捕获（1.3.72）

- 运行时：modern-screenshot 选区仍超时约 23s；html2canvas 报不支持 `color()`。
- 说明：并非自写截图引擎，是第三方 DOM/FO 库在本站主题上不可用。
- 修复：主路径改为 `getDisplayMedia` 真像素裁剪；弹权限前保持聊天可见；取消共享不跌入 FO 长阻塞。
- 版本：`1.3.71` → `1.3.72`

## Hotfix — 2026-09-10：截图完成卡顿（1.3.71）

- 运行时：`apiReady` 后整页视口 `domToCanvas`（path1）挂起，聊天已 hidden 迟迟不 restore。
- 修复：去掉视口/整文档连截；仅选区 translate；`CS_SHOT_CAPTURE_MS` 硬超时后走 html2canvas 区域降级。
- 版本：`1.3.70` → `1.3.71`

## Hotfix — 2026-09-10：客服 idle 加载等待 Weline.load（1.3.62）

- 现象：`requestIdleCallback` 早于 `weline.js` → `[CustomerService] Weline.load unavailable`。
- 变更：`waitForWelineLoad` 轮询后再 `Weline.load`；失败则直挂 `customer-service.js` fallback。
- 版本：`1.3.61` → `1.3.62`

## Hotfix — 2026-09-10：截图完成不丢聊天卡（1.3.70）

- 根因：截图全程 `visibility:hidden` 聊天卡，完成时又可能弹出 `getDisplayMedia` 授权层，看起来像聊天卡没了。
- 修复：框选阶段保持聊天卡；仅抓帧瞬间隐藏并必 `restoreChatWidgetAfterShot`；取消自动 display-media。

## Hotfix — 2026-09-10：截图改用 modern-screenshot（1.3.69）

- 审图：`web_ui` / `error_shot`（截图仍白块）。
- 根因：主题 CSS `color()`/`color-mix()` 导致 html2canvas 重绘失败出白图。
- 选型：开源 [modern-screenshot](https://github.com/qq15725/modern-screenshot)（foreignObject，MIT）为主；视口截取再裁剪。
- 降级：html2canvas FO → 经典重绘；再失败则 Chrome `getDisplayMedia({preferCurrentTab})` 真像素裁剪。

## Hotfix — 2026-09-10：聊天截图空白修复（1.3.68）

- 审图：`web_ui` / `error_shot`（截图消息白块）。
- 根因：`onclone` 对克隆节点 `getBoundingClientRect` 误剪清空内容。
- 修复：真实 DOM 标记 keep → 克隆按属性剪枝；空白检测重试；`imageTimeout` 提高。

## Hotfix — 2026-09-10：验证成功回聊天收口（1.3.67）

- 需求：点开验证链绑定成功后，不应停在成功页；原页关绑邮弹层、提示已绑定、身份芯片显示邮箱并继续聊。
- 实现：verify 页 BroadcastChannel + localStorage 广播；widget `bindEmailBoundSync` 刷新 session/消息并 `notify`。

## Hotfix — 2026-09-10：DEV 验证链接可点新开（1.3.66）

- 审图分流：`web_ui` / `error_shot`（发送失败弹层 URL 不可点）。
- API 回传 `verification_url`；`notifyAlert` 渲染「打开验证链接」`a[target=_blank]`；URL 构建避免双重 encode。

## Hotfix — 2026-09-10：绑邮校验 + 会话身份/日节点/通知（1.3.65）

- 纠偏：DEV SMTP fallback **禁止**静默 `bind`；严格 `isValidEmail` 前后端共用。
- 会话 `identity`：访客邮箱可点改绑；登录展示用户名/头像；换绑写 system 消息。
- 聊天：日分隔（今天/昨天/日期）；关闭面板时未读角标 + Browser Notification。

## Hotfix — 2026-09-10：empty_token_props 切本地 + 不展示技术码（1.3.61）

- `empty_token_props` 时 `shouldOfferLocalDegrade=true`；DEV 不再把 `google_token_invalid:…` 拼进用户文案。
- 版本：`1.3.60` → `1.3.61`

## Hotfix — 2026-09-10：绑邮 captcha action 去点号（1.3.60）

- INTENT / form intent：`customerservice.bind_email` → `customerservice_bind_email`（Google Enterprise 合法 action）。
- 版本：`1.3.59` → `1.3.60`

## Hotfix — 2026-09-10：BROWSER_ERROR 不降级本地图码（1.3.59）

- 运行时证据：`google_token_invalid:browser_error` 可重试，却被当成云端不可用切本地图码。
- 变更：`BindCaptchaGuard::shouldOfferLocalDegrade` 对 `browser_error` 返回 false；提示「请再试一次」并保持 Google。
- 版本：`1.3.58` → `1.3.59`

## Hotfix — 2026-09-10：绑邮降级 stopPropagation + server reason（1.3.58）

- 降级事件在绑邮表单 `stopPropagation`，避免与 captcha-lazy 竞态；服务端降级刷新带 `degrade_reason`。
- 版本：`1.3.57` → `1.3.58`

## Hotfix — 2026-09-10：绑邮降级带 degrade_reason（1.3.57）

- 自动降级 `prefer=local_image` 时附带 `degrade_reason`，便于服务端对照 Google SDK 失败原因。
- 版本：`1.3.56` → `1.3.57`

## Hotfix — 2026-09-10：静默降级后去掉告警式本地图码 hint（1.3.55）

- 验证：`showBindPrompt` 已落到 `local_image`，且未再弹「请填写本地图码」toast；但仍有 hint「云端人机验证暂不可用…」。
- 修复：静默降级时 `quietHint` → 中性文案「请填写下方图码后发送验证邮件」。
- 版本：`1.3.54` → `1.3.55`

## Hotfix — 2026-09-10：国内 Google 不可达仍弹降级 toast（debug e4670c）

- 根因：腾讯未配 → CN/回落选 Google；浏览器加载 `enterprise.js` 失败后自动 toast。
- 修复：加载期静默降级本地图码（用户未点发送不弹「请填写本地图码」）；配合 Captcha 国内跳过 Google。
- 版本：`1.3.53` → `1.3.54`

## Hotfix — 2026-09-10：绑邮箱 Google 空票 google_empty_token（审图 error_shot）

- 分流：`web_ui` / `error_shot`；toast `[google_empty_token]`。
- 根因：Google token 靠 `weline:form:prepare-submit` 异步写入；绑邮箱 fetch 在未取票时直发空 `captcha_response`。
- 修复：`ensureRemoteBindCaptchaToken` 在提交前派发 prepare-submit，等 token / pending；禁止空票打服务端。
- 版本：`1.3.52` → `1.3.53`

## Hotfix — 2026-09-09：截图点完成整页渲染卡顿

- 问题：选区后点「完成」很卡；算法先整页克隆再裁切，首页 DOM 过重。
- 修复：`captureClientRegionDirect` 只渲选区；`ignoreElements` 跳过选区外大块子树 + `pruneCloneOutsideRegion`；去掉整页 prefetch；动态 scale。Browser 小选区约 0.6s。
- 版本：`1.3.49` → `1.3.50`
- 交付：[店面首页](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：点击截图主线程卡顿

- 问题：一点「截图」整页立刻 `html2canvas(document.body)`，主线程被占满，蒙层拖选卡死。
- 修复：蒙层即时出现；有效选区 `pointerup` 后再 `ensureSnapshotPrefetch`；scale 0.45 + onclone 瘦身。
- 版本：`1.3.48` → `1.3.49`
- 交付：[店面首页](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：截图完成上传报 Unknown frontend worker param: locale（审图 error_shot）

- 分流：`web_ui` / `error_shot`；附图「操作失败」+ `Unknown frontend worker param: locale`。
- 根因：`upload` Query 描述符不含 `locale`，前端 `uploadAndSendAttachment` 误传。
- 修复：去掉 upload 的 `locale`；保留 `sendMessage` 的 locale；未知 worker 参数人性化提示。
- 版本：`1.3.47` → `1.3.48`
- 交付：[店面首页](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：截图生成加速 + 选区后按钮高亮

- 需求：`REQ-CUSTOMERSERVICE-FRONTEND-001`
- 问题：生成截图慢；选区后底部按钮发灰，误以为不可点。
- 实现：预加载 html2canvas；截取限制为视口 window 尺寸+降 scale；选中加 `cs-shot-crop--ready` 高亮「完成」；确认中显示「生成中…」。
- 版本：`1.3.46` → `1.3.47`
- 交付：[店面首页](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：截图改为先框选再截取

- 需求：`REQ-CUSTOMERSERVICE-FRONTEND-001`
- 问题：先整页截图再选区等待久，且不符合微信/系统截图习惯。
- 实现：`openLiveRegionPicker` 蒙层即时框选 → `captureViewportRegion` 只截选区 → JPEG≤2MB。
- 版本：`1.3.45` → `1.3.46`
- 交付：[店面首页](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：截图改为直接截当前页面

- 需求：`REQ-CUSTOMERSERVICE-FRONTEND-001`
- 问题：共享屏幕弹窗体验差；用户要「当前界面」直接截。
- 实现：默认 `html2canvas` 截 viewport（隐藏客服浮窗）→ 框选 → JPEG≤2MB；失败回退 `getDisplayMedia`。
- 版本：`1.3.44` → `1.3.45`
- 交付：[店面首页](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：店面截图改为微信式框选（≤2MB）

- 需求：`REQ-CUSTOMERSERVICE-FRONTEND-001`
- 问题：整页/整帧 PNG 易超 2MB。
- 原型：`frontend-composer-screenshot.html` 选型 **A**（取帧→框选→JPEG 压缩）。
- 实现：`openScreenshotRegionPicker` + `compressCanvasToJpegUnderLimit(2MB)`；CSS `.cs-shot-crop*`。
- 版本：`1.3.43` → `1.3.44`
- 交付：[店面首页](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：店面客服表情/图片/文件 + 截图发送

- 对象：店面 `customer-service` widget 输入区此前仅有文本框。
- 原型：`view/statics/prototype/frontend-composer-screenshot.html`（A/B/C）→ 选型 **A**（独立截图钮 + 屏幕捕获，失败回退选图；支持粘贴）。
- 实现：`ChatMediaUploader`；QueryProvider `upload` + `sendMessage` 附件字段；工具条表情/图片/文件/截图；`getDisplayMedia` 截帧上传。
- 版本：`1.3.42` → `1.3.43`
- 交付：[店面首页（开客服）](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：话术统一搜索 + 分类 Tab（审图）

- 分流：`web_ui`；附图话术面板仅有列表+始终展开编辑，无搜索、无分类 Tab。
- 技能门禁：`frontend-design` / `prototype` / `weline-theme-development` 可用。
- 原型：`view/statics/prototype/console-phrase-search-tabs.html`（A/B/C）→ 选型 **A**（搜索优先 + 横向分类 Tab）。
- 实现：`AgentPhrase.category` + `AgentPhraseCategory`；`phrases` 返回 `{phrases,categories}`；面板统一搜索跨分类定位；`+` 新建分类即 Tab；添加话术折叠；删除次要化。
- 审图：E 人性化 fail→pass（选用主路径优先）；F 规范美观 fail→pass（搜索/Tab/列表节奏）；G 主题契合 pass（`w-input`/`w-button`/`w-select` + token）。
- 版本：`1.3.41` → `1.3.42`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：默认选中最近会话 / 侧栏优先级 / 收消息反馈

- 分流：`web_ui`；附图进入工作台停在空态，箭头指向最近会话 #6；缺收消息提醒。
- 原型：`view/statics/prototype/console-session-priority-logic.html`。
- 实现：默认选中「我的会话」优先排序首位；未读按等待起点升序（等最久更靠前），其余按最新消息降序；未读增量 toast + 列表脉冲 + 标题闪烁 + 轻提示音。
- 版本：`1.3.40` → `1.3.41`
- 交付：[客服工作台](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：工作台表情 / 图片文件 / 个人话术库

- 分流：`web_ui`；附图聊天输入区仅有输入框与发送，缺附件与话术能力。
- 原型：`view/statics/prototype/console-composer-toolbar.html`（上工具条 + 面板）。
- 实现：表情插入；图片/文件 base64 上传 `postUpload`；`__CSJSON__` 附件协议；`AgentPhrase`/`w_cs_agent_phrase` 个人话术 CRUD；气泡渲染图片/文件；店面 `customer-service.js` 可解析附件。
- 验收：工具条四钮可见；表情可插入；话术可保存/选用；上传+发送图片气泡可达（会话 #2 / probe.png）。
- 版本：`1.3.39` → `1.3.40`
- 交付：[客服工作台](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：工作台发消息失败

- 分流：`web_ui`；附图会话 #6 输入「不再」toast「发送失败，请稍后重试」。
- 根因：`adminRequest` 业务失败会 throw，前端 catch 吞掉真实文案；`postSendMessage` 等 agent 查询未 `reset()`，单例脏条件可导致误判无权/非客服。
- 修复：Console agent 查询统一 `reset()`；`csAdmin` 默认 `keepBusinessResult`；`csExtractErrorMessage` 展示真实原因。
- 版本：`1.3.38` → `1.3.39`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：选会话显示最新消息，上滑查更旧

- 分流：`web_ui`；附图侧栏已选 #6，主区仍停「请选择会话」/加载感；期望正常聊天：最新在底，上滑更旧。
- 原型：`view/statics/prototype/console-chat-history-logic.html`（先壳→最新页→before_id prepend）。
- 修复：选中立即渲染聊天壳「正在加载最新消息…」；`getMessages` 按 `message_id` 取最新页；新增 `before_id` / `getMessagesBefore`；消息区顶部上滑加载更旧并锁定视口；列表刷新恢复 active。
- 版本：`1.3.37` → `1.3.38`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：侧栏会话组互斥展开

- 分流：`web_ui`；附图多组可同时展开，要求展开一个关闭其他。
- 修复：`initSessionGroupAccordion` 监听 `weline:ui:disclosure:open` 关闭兄弟组；SSR 首屏仅一组 open（我的→转让→等待）；`init` 内手风琴绑定提前到 `bindEvents` 之前，避免后续异常跳过。
- 验收：Browser 禁缓存后 `bound=1`，依次点等待/转让/我的，始终仅一组 `open`；UT `BackendConsoleSessionAccordionContractTest` 12 assertions OK。
- 版本：`1.3.36` → `1.3.37`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：工作台聊天区内滚动（不撑开整卡）

- 分流：`web_ui`；附图会话 #6 消息把聊天卡一直往下撑，输入区被顶走。
- 原型：`view/statics/prototype/console-chat-shell.html`（A 锁高壳 / B 仅限高 / C 撑开坏态）→ 采用 A；生产落点为 `.console-layout` 限 `100dvh - --cs-layout-offset`，消息区 `min-height:0` + 区内滚动。
- 修复：顶栏/输入 `flex-shrink:0`；统计区不参与撑开；桌面聊天网格锁高。
- 版本：`1.3.35` → `1.3.36`
- 交付：[客服工作台](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：在线客服优先分配 + 等待列表 #0

- 分流：`web_ui`；附图 admin 在线，等待列表全是 `#0`，前端消息进不了当前客服。
- 根因：`getSessions` 把 ChatSession Model 直接 JSON，无顶层 `session_id`；`assignAgent` 只看激活不看心跳，且 waiting 会话客户再发消息不重试分配。
- 修复：`normalizeSessionRows`；分配排序在线优先+最少负载；`sendMessage` 对 waiting 重试 `assignAgent`；JS `sessionRowId` 兜底。
- 版本：`1.3.34` → `1.3.35`
- 交付：[客服工作台](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：转让来源=原归属客服，非操作者

- 问题：超管操作转让后，工作台显示「来自 admin」，用户以为记成了操作者。
- 说明：`transferred_from_agent_id` 一直是人员列表所点「转出」行的客服；超管账号也挂了名为 admin 的客服身份时，从该行转出就会显示 admin。
- 修复：弹窗增加只读「转出客服」与「接收客服」；列表文案改为「来自客服「…」」并可附带关联用户名；需求注明与操作者无关。
- 版本：`1.3.33` → `1.3.34`

## Hotfix — 2026-09-09：转让空态澄清（非数据丢失）

- 分流：`web_ui`；附图 admin 工作台「转让分配」空，误以为会话丢失。
- 根因：#1/#16 仍在接收方 agent2（`wls_probe_ui`）；「转让分配」只列**转进**会话；转让方（admin，可见 #15）侧为空属预期。
- 修复：空态文案补充「转出的会话只显示在接收方工作台」。
- 版本：`1.3.32` → `1.3.33`
- 交付：[客服工作台](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：消息自动同步 / 未读清零 / 转让响应时间分账

- 分流：`web_ui`；附图转让会话已回复仍显示未读 1；前端发消息后台不更新。
- 根因：`last_read_time` 字段缺失且从未写入；轮询 `message_id` 类型比较失败且只认客户消息；响应时间统计忽略 `transferred_at`。
- 修复：补 `ChatSession.last_read_time`；打开/回复/转让时 mark-read；轮询改 `since_id` 并同步双方消息；统计按转让窗口分账。
- 版本：`1.3.31` → `1.3.32`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：转让会话点选加载失败（有消息 TypeError）

- 分流：`web_ui`；附图点转让 #16 toast「加载会话失败」，主区仍停在 #15。
- 根因：`Console::getMessages` 的 `array_map` 把 `ChatMessage` 模型标成 `array`，有消息时 TypeError；空会话侥幸成功。
- 修复：回调改为 `ChatMessage|array`；`loadSession` 失败回滚选中态。
- 版本：`1.3.30` → `1.3.31`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：工作台接单/关闭确认与 POST 体失效

- 分流：`web_ui`；附图转让会话无「接单」按钮，等待「接单」点击无反应。
- 根因：`BackendConfirm` 未加载时 `confirmAction` 静默 `false`；`csAdmin` 把 `URLSearchParams` `JSON.stringify` 成 `{}`，接单 POST 丢 `session_id`。
- 修复：确认改走 `Weline.UI.dialog.confirm` 回退；`csAdmin` 与人员页对齐序列化 URLSearchParams；加载消息失败 toast。
- 版本：`1.3.29` → `1.3.30`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：会话侧栏手风琴置顶「我的会话」

- 需求：我的会话置顶；多会话区内滚动；手风琴标题有会话时显示数字便于展开。
- 修复：三组改 `w-disclosure`；顺序为我的→转让→等待；`updateSectionCount` 同步徽章；展开面板限高滚动。
- 版本：`1.3.28` → `1.3.29`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：转让分配明文显示来源客服

- 需求：接收方能一眼看到「来自谁的转让」，不能只靠徽章 title 悬停。
- 修复：`withTransferBadges` 增加 `transfer_from_label`；「转让分配」SSR/AJAX 列表增加 `.session-transfer-from` 明文行。
- 版本：`1.3.27` → `1.3.28`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：等待分配与转让分配分组

- 需求：侧栏将「等待分配」与「转让分配」分组展示。
- 修复：`partitionAgentSessions`；新增「转让分配」区块；「我的会话」仅非转让活跃会话；AJAX 返回 `transferred_sessions`。
- 版本：`1.3.26` → `1.3.27`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：转进会话显示转让标记

- 需求：接收方「我的会话」对转让进来的会话显示「转让」徽章。
- 修复：`ChatSession` 持久化 `transferred_from_agent_id`/`transferred_at`；`ChatService::withTransferBadges`；Console SSR/AJAX 装饰；列表 `w-badge`。
- 版本：`1.3.25` → `1.3.26`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：等待分配限高可滚动（审图）

- 分流：`web_ui`；附图会话列表「等待分配」卡片过多占满侧栏。
- 修复：`.waiting-sessions` `max-height: min(42vh, 18rem)`；`#waiting-sessions-list` 区内 `overflow-y: auto`。
- 版本：`1.3.24` → `1.3.25`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：转让 hover 空白白条（审图）

- 分流：`web_ui`；附图转让按钮上方空白白条。
- 根因：Theme 浮动层规则把 portaled `.w-tooltip` 强制白底，字色仍为白色（见 `Weline_Theme` 2.2.278）。
- 版本：页面侧无需改；依赖 Theme 修复。
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：人员列表多列合并（审图）

- 分流：`web_ui`；附图 +「能多列合并的合并」。
- Fail：8 列过碎（ID/名称/用户/邮箱/语言/状态/最大会话/操作）抢视线。
- 修复：合并为 **客服**（名称+#ID+用户·邮箱）/ **语言** / **接待**（状态+最多 N 会话）/ **操作**；Token 化间距。
- 版本：`1.3.22` → `1.3.23`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：转让改为异步不刷新

- 需求：确认转让后不要整页刷新，提示要能看见。
- 修复：`submitTransferSessions` 成功后关闭弹层 + toast；本地更新 `open_session_count`（源减目标加）；按钮忙碌态防重复提交。
- 版本：`1.3.21` → `1.3.22`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：转让确认刷新冲掉提示

- 问题：确认转让后 `location.reload()` 过快，toast 未看清即被冲掉。
- 修复：`csFlashThenReload` 写入 `sessionStorage`，刷新后 `csConsumeFlashToast` 再展示。
- 版本：`1.3.20` → `1.3.21`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：转让按钮无提示（审图）

- 分流：`web_ui`；附图人员列表 +「转让没提示，要加上」。
- Fail：分享图标语义不清、仅有原生 title；无会话时弹空弹层；转让成功只 reload 无 toast。
- 修复：按钮可见文案「转让」+ `data-w-component=tooltip`；无未关闭会话时 `csToastInfo`；成功/零转让 `csToastSuccess`/`info` 后再刷新。
- 版本：`1.3.19` → `1.3.20`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：客服人员首次进入空白（审图）

- 分流：`web_ui`；侧栏点进「客服人员」主区全白，刷新后正常。
- 根因：非 native `.w-dialog:not(dialog)` 为 `position:fixed; inset:0`；关闭态若丢 `hidden`（或 `openHandoffDialog` 先 `hidden=false`）会白遮罩盖住主区。服务端 HTML 始终含内容（LayoutPerf ~400KB）。
- 修复：handoff 开关改走 `Weline.UI.dialog`；boot/bfcache 强制关闭且禁止 `style.display=none`；`backend-agent.css` / foundation 关闭态 `display:none !important`。
- 版本：`1.3.23` → `1.3.24`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：转让会话 Action missing（审图）

- 分流：`web_ui`；附图 toast `Action missing … postTransfer Sessions`（带空格）。
- 根因：与「查看统计」同源——`adminRequest` kebab→Studly 未去空格时候选为 `postTransfer Sessions`；源码已修但常驻 Worker 未热载时仍会复现。另：弹窗文案用 `%{1}` 经 `__()` 后被吃空，摘要变成「客服「」」。
- 修复：确认 Studly `str_replace(' ', '', ucwords(...))` + `server:reload`；弹窗改 `__AGENT__`/`__COUNT__` 占位；契约测 `transfer-sessions`→`postTransferSessions`。
- 验收：Browser 禁缓存后确认转让 `success:true`、`transferred:3`，无 `Action missing`；摘要显示客服名。
- 版本：`1.3.18` → `1.3.19`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：客服人员「查看统计」失败（审图）

- 分流：`web_ui`；附图红箭头：图表按钮 → toast「获取统计数据失败」。
- 根因：`CustomerServiceQueryProvider::adminRequest` 将 `agent-statistics` 转成 `getAgent Statistics`（kebab→ucwords 后未去掉空格），桥接找不到 `getAgentStatistics`；同理 `transfer-sessions` 也会受影响。
- 修复：Studly 转换改为 `str_replace(' ', '', ucwords(...))`；前端 `viewStatistics` catch/`success:false` 走 `csFormatError`。
- 版本：`1.3.17` → `1.3.18`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：降级本地图码后 Google 监听器覆盖输入

- 分流：`web_ui` / `error_shot`；本地图码仍「人机验证失败或已过期」。
- 日志：`provider=local_image` 但 `responseLen=770`（Google token），`passwordMatch=false`。
- 修复：Captcha `1.0.22` Google/腾讯 `prepare-submit` 仅在当前 provider 匹配时写入；绑定弹窗按 captcha root 读字段。
- 验收：post-fix `responseLen=6` + `passwordMatch=true` + `manager_verify_result.ok=true`。
- 版本：`1.3.51` → `1.3.52`
- 交付：[店面首页客服浮窗](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：人机验证自动打洞（出站代理 + 客户端降级）

- 分流：`web_ui` / `error_shot`；附图「发送失败 / 人机验证失败或已过期」；用户判断「本机也不行 → 没自动打洞」。
- 根因：① Captcha Google 校验未走已配置社媒出站代理；② 绑定弹窗未监听 `weline:captcha:degrade`，云端脚本失败时不会自动 `prefer=local_image`。
- 修复：Captcha `1.0.21` 出站代理；`customer-service.js` 增加 `autoDegradeBindCaptcha`（verification-error / captcha:degrade → 本地图码）。
- 版本：`1.3.50` → `1.3.51`
- 交付：[店面首页客服浮窗](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：绑定邮箱验证码被页面缓存/SSR（审图）

- 分流：`web_ui`；附图「人机验证失败或已过期」；用户判断「验证码没有动态更新、被缓存」。
- 根因：`body-end` 把 `renderChallenge()` SSR 进弹窗模板（可进 FPC）；打开弹窗未 await 刷新；挑战接口未 `SharedResponseCachePolicy::forbid`。
- 修复：slot 留空、打开弹窗强制 `cache:no-store` 拉新挑战；接口 forbid；失败必刷新；空 provider 也降级本地图码。
- 版本：`1.3.16` → `1.3.17`
- 交付：[店面首页客服浮窗](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：客服删除须处理会话 / 可转让（审图）

- 分流：`web_ui`；附图人员列表 +「删除或者可以转让会话给其他客服」。
- Fail：删除直删不处理未关闭会话；无转让入口。
- 修复：`ChatService` 转让/释放；`remove` 有会话时须转让或释放；新增 `transfer-sessions`；列表「转让」按钮 + 弹层选目标客服。
- 版本：`1.3.15` → `1.3.16`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：客服工作台左右栏对调（审图）

- 分流：`web_ui`；附图 +「会话列表改到右边，聊天在左边」。
- 修复：DOM 顺序改为聊天→会话列表；CSS `grid-template-columns` 宽列在左、窄列在右；空态文案「左侧」→「右侧」。
- 版本：`1.3.14` → `1.3.15`
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：绑定邮箱人机验证英文失败 + Google 不可达（审图）

- 分流：`web_ui`；附图「发送失败」英文 `Captcha verification failed or expired`。
- 根因：① Query `sendVerification` 用英文源串且 `zh_Hans_CN` 误映射为英文；② 本机连不上 Google reCAPTCHA Enterprise（connection timeout），校验必失败，且未降级本地图码。
- 修复：中文源串 + i18n；`captcha_error` 返回 `captcha_degrade=local_image`；挑战接口/`refreshBindCaptcha` 支持 `prefer=local_image`。
- 版本：`1.3.14` → `1.3.15`
- 交付：[店面首页客服浮窗](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：人机验证出网失败降级 UX（审图 error_shot）

- 分流：`web_ui` / `error_shot`；附图「发送失败」+「人机验证服务暂不可用，已切换为本地图码」。
- 诊断：本机 `www.google.com` / `recaptchaenterprise.googleapis.com` 出站失败（打洞/墙外连通未通）；`www.recaptcha.net` 可达但 Enterprise API 不在该域。
- 修复：降级路径改为 `warning`「请填写本地图码」+ 表单内联提示 + `prefer=local_image` 刷新并聚焦图码输入，避免误读为整单失败。
- 交付：[店面首页客服浮窗](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：未验证邮箱游客单条消息闸门

- 需求：`REQ-CUSTOMERSERVICE-GUEST-001`
- 未登录且未绑定邮箱：最新消息为 customer 时锁定发送，提示等待客服回复；「验证邮箱」可跳过闸门；agent 回复后可再发。
- 实现：`ChatService::resolveGuestSendGate` / `assertCustomerMaySend`；Query + Frontend Chat 返回 `guest_send`；浮窗 `#cs-guest-send-gate` + `applyGuestSendGate`。
- 附带修复：`assignAgent` 对 `getItems()` 返回 Model 时误 `setData(Model)` 导致新建游客会话 500。
- 版本：`1.3.13` → `1.3.14`
- 交付：[店面首页客服浮窗](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：客服工作台美化（审图）

- 分流：`web_ui`；附图+`/customerservice/backend/console`「美化」。
- Fail：E/F/G — 时段/接单为浏览器原生按钮（模板 `weline-btn-*` 与 CSS `.btn-*` 错位）；统计区硬编码 hex + 彩虹渐变图标；空态仅「暂无会话」无下一步；且 `getViewFileUrl()` 解析为空导致 `<link href="">`，样式根本未加载。
- 原型取向：对齐 `cs-agent-page` 运营工作台（指标条 + 双栏会话/聊天），不另造色板。
- 修复：模板改 `@static` + `w-button`/`w-card`/`w-badge`/`w-empty`；CSS 仅 `--weline-theme-*`；JS 动态列表/关闭会话/时段切换同步组件态；`1.3.10` → `1.3.11`。
- 交付：[客服工作台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/console)

## Hotfix — 2026-09-09：绑定邮箱发验证码 captcha_response 超长（审图）

- 分流：`web_ui`；弹窗「发送失败」`Param string exceeds max length: captcha_response`。
- 根因：`sendVerification` descriptor 将 `captcha_response` 限为 `512`，现代人机验证 token 常更长；Account/Checkout 已用 `8192`。
- 修复：对齐 `8192`；契约测试；`framework:compile` 刷新 Query 索引。
- 版本：`1.3.12` → `1.3.13`
- 交付：[店面首页客服浮窗](http://p05113ef3.test.weline.com:9555/)

## Hotfix — 2026-09-09：客服浮钮状态小圆点删除（审图）

- 分流：`web_ui`；用户：「那小圆点删了」。
- 对象：悬浮 `#cs-chat-button` 右下角 `#cs-status-dot`（离线灰点）；保留未读 `cs-badge` 与窗内 `cs-status-label`。
- 修复：钩子去掉 status-dot；JS 只更窗内文案；删除 `.cs-status-dot` CSS。
- 版本：`1.3.11` → `1.3.12`
- 交付：店面前台（任意页右下角客服浮钮）

## Hotfix — 2026-09-09：编辑客服不回填（审图）

- 分流：`web_ui`；标题「编辑客服」但关联用户/名称/邮箱为空。
- 根因：`getItems()` 返回 Model，`json_encode` 成模型元数据（无 `agent_id/name/...`），`AgentManager.agents.find` 失败后静默留空。
- 修复：控制器输出纯数组行；JS `findAgentRecord` 兜底 + 找不到时 toast。
- 版本：`1.3.9` → `1.3.10`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：客服人员列表审图降噪（仅附图）

- 分流：`web_ui`；空字附图默认审列表页。
- Fail：E/F — 列表塞入总会话/总消息/平均响应（多为 0/-）与身份列抢注意力；图标操作仅有 `title` 无 `aria-label`。
- 修复：列表只保留身份/配置/状态/操作；统计走「查看统计」；操作补 `aria-label`；操作列 sticky（主题 token）。
- 版本：`1.3.8` → `1.3.9`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：添加客服保存 500 NoRouterException（审图续）

- 分流：`web_ui`；descriptor 开放后仍失败：`customerService.adminRequest 500` / `NoRouterException … 404`；修好桥后前端又报 `response.json is not a function`。
- 根因：`adminRequest` 曾用 `ObjectManager` 拉起 `BackendController::__init()`；前端 `csAdmin` 把 BinQuery 直返对象当 `fetch` Response。
- 修复：`AdminControllerBridge::invoke`；动作段 `ucwords` 驼峰；`csAdmin` 对齐 Eav `wrapAdminBridgeResult`/`toResp`。
- 版本：`1.3.7` → `1.3.8`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：添加客服保存失败（审图）

- 分流：`web_ui`；toast「保存失败」，控制台 `customerService.adminRequest 403 Frontend worker operation is not allowed`。
- 根因：`getDescriptor()` 未登记 `adminRequest`；同时 `URLSearchParams` 被 `JSON.stringify` 成 `{}`，即使开放也会空 POST。
- 修复：descriptor 补 `frontend/auth=backend/backend_acl=self`；`csAdmin` 正确序列化表单；catch 用可读中文下一步；`framework:compile` 后索引含 `adminRequest`。
- 版本：`1.3.6` → `1.3.7`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：添加客服选用户自动填名称/邮箱（审图）

- 分流：`web_ui`；用户箭头：关联后台用户 → 邮箱，文案「选择直接填上，可以改」。
- 修复：`user_id` option 挂 `data-name`/`data-email`，change 时写入 `#name`/`#email`，字段仍可编辑。
- 版本：`1.3.5` → `1.3.6`
- 交付：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：添加客服弹层偏左（审图）

- 分流：`web_ui`；根因在 Theme `.w-dialog:not(dialog)` 未重置基类 `inline-size`（见 Theme 2.2.268）。
- 本模块：`data-size` 挪到 dialog 根节点。
- 版本：`1.3.4` → `1.3.5`
- 交付：[客服人员管理](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：添加客服恢复为弹窗（审图）

- 分流：`web_ui`（客服人员页「添加客服」成页内表单）
- 根因：自定义 overlay 依赖 `backend-agent.css`，静态 404 时无 `position:fixed`，打开后落成文档流块。
- 修复：改为 Theme `w-dialog` + `Weline.UI.dialog.open/close`；样式改 `@static(...)`；列表页样式仍走 `backend-agent.css`。
- 版本：`1.3.3` → `1.3.4`
- 交付地址：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-09：客服人员页 getItems() on false（审图）

- 分流：`web_ui`（WLS Runtime Error /customerservice/backend/agent）
- 根因：`Backend/Agent/index.phtml` 将 JS 助手名 `csAdmin` 误写成 PHP 查询链 `select()->csAdmin()->getItems()`，`csAdmin()` 返回 false 后致命错误；控制器仅 catch Exception，无法兜住 Error。
- 修复：控制器经 `BackendUserDirectoryInterface::all()` 组装 `backend_users` 并 assign；模板只读 view 数据；catch 改为 `Throwable`。模块 `1.3.2` → `1.3.3`。
- 审图：E/C 从整页崩溃恢复；语言选择仍用官方 `w:i18n:language:select`；关联用户下拉恢复可用。
- 交付地址：[客服人员管理](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/agent)

## Hotfix — 2026-09-08：启用 AI 自动回复开关报 Worker session capacity exhausted

- 现象：客服配置页点 `customer_service/ai/enabled` toast「Worker session capacity is exhausted.」
- 根因：Framework Frontend Worker 无绑定会话误用 7200s TTL，本地会话 store 顶满 4096，QueryBin 握手失败导致 config:embed 开关无法保存。
- 修复：见 Framework 开发日志同日条目（`UNBOUND_SESSION_TTL=600` + 遗留会话回收 + 中文容量提示）。
- 交付地址：[客服配置](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/config)

## Hotfix — 2026-09-08：语言/模型选择器动态属性 PHP 泄漏修复 + 视觉验收

- 根因：`StageResolver` 对含 `<?= ?>` 的动态属性走 RUNTIME，`renderRuntimeTag` 若无 `runtimeCallback` 会把编译期 PHP 源码 echo 成页面文本。
- 修复：`LanguageSelect` / `ModelSelect` 对齐 `theme:search-select`，增加 `runtimeCallback` + `buildMarkup` 直接出 HTML；Worker 热重载后生效。
- 验收：禁缓存 Browser — 无 PHP 泄漏；语言选择器可打开搜索（留空/按国家）；模型选择器显示 `gpt-4o`。截图：`doc/evidence/cs-config-language-model-select-20260908.png`
- 版本：I18n `1.0.33` → `1.0.34`；Ai `1.2.39` → `1.2.40`
- 交付地址：[客服配置](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/config)

## Hotfix — 2026-09-08：客服配置语言/模型改用官方选择标签

- 语言字段继续 `type=locale`，由 SystemConfig 渲染 `<w:i18n:language:select>`；AI 模型改为 `type=ai_model` → `<w:ai:model:select>`。
- 版本：`1.3.1` → `1.3.2`
- 交付地址：[客服配置](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/config)

## Hotfix — 2026-09-08：客服配置页用 config:embed 就地展示（取消 301 到配置中心）

- 目标：菜单与入口仍在 `customerservice/backend/config`；字段来自已声明的统一配置组，用 `<w:config:embed>` 嵌入本页；范围走 URL `target_scope`。
- 变更：
  1. `Config` 控制器恢复 `fetch()`；解析 `target_scope`（无显式范围时规范到本页 URL，不跳配置中心）
  2. 模板：`<w:scope>` 切范围 + `<w:config:embed module="Weline_CustomerService" area="frontend">`；可选链到统一配置中心
  3. 菜单 `action="*/backend/config"`
- 版本：`1.3.0` → `1.3.1`
- 交付地址：[客服配置](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/config)

## Hotfix — 2026-09-08：客服配置迁入统一配置（范围 + 网站语言回退）

- 目标：客服配置支持 Global/Website/Store 范围；默认语言未配置时跟随网站默认语言；读写调用点全部改走统一配置。
- 变更：
  1. Extends 声明 `Config/frontend/customer-service.phtml`（enabled / 默认客服·客户语言 / AI）
  2. 新增 `CustomerServiceSettings`（ConfigReader + 旧表一次性迁移 + `WebsiteData::getDefaultLanguage` 回退）
  3. 后台入口曾临时跳转配置中心；已改为本页 embed（见上条）
  4. Chat / QueryProvider / ChatService / 浮窗 hook / 旧 Model 门面全部改读 Settings
  5. SystemConfig `1.3.38`：字段 type `locale|language` 可编辑（留空=网站默认语言）
- 版本：CustomerService `1.2.10` → `1.3.0`；SystemConfig `1.3.37` → `1.3.38`
- 交付地址：[客服配置](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/config)

## Hotfix — 2026-09-08：客服配置页审图（Taglib / UI 2.0）

- 分流：附图 `web_ui`（后台 `customerservice/backend/config`）。
- 技能门禁：`frontend-design` / `prototype` / `weline-theme-development` / `weline-taglib-first` / `local-browser-urls` 本回合可用。
- fail → 已修：
  1. 手写中英 locale `<select>` → `<w:i18n:language:select catalog="installed">`
  2. 启用/AI 启用裸下拉 → `w-switch`（hidden `0` + checkbox `1`）
  3. AI 模型自由文本 + 内部模块名提示 → `<w:ai:model:select>` + 人性化说明
  4. 硬编码 `#6b7280/#9ca3af` 与重复 h1 → `w-stack`/`w-field__hint`/`data-tone="muted"` Token；文案改 `<lang>`
  5. 路由资源：`customer-service.config` 挂 `language-select` + `ai-default-model` bundles
- 版本：`1.2.9` → `1.2.10`
- 交付地址：[客服配置](https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/customerservice/backend/config)

## Hotfix — 2026-09-03：客服浮层头部文字对比度

- 根因：Theme 未分层 `.w-panel-header` 使用浅色 `--weline-layout-surface-secondary`，优先级高于 `@layer page` 内 `.cs-chat-header` 的 `--cs-header-bg`；文字仍用 `--cs-header-text` 浅色，水墨主题下标题/Offline 几乎不可见。
- 变更：在 `customer-service.css` 层外增加 `.customer-service-widget .cs-chat-header.w-panel-header` 强制 `background/color` 与状态标签、图标按钮配色，恢复 chrome 深色头可读性。
- 回归：`CustomerServiceWidgetUiContractTest` 锁定 unlayered 覆盖与 `--cs-header-bg/--cs-header-text`；内置浏览器打开前台客服窗验收标题与 Offline。
- 版本：`1.2.8` → `1.2.9`。
- 交付地址：前台首页（打开右下角客服浮层）；后台/API N/A。

## Hotfix — 2026-09-03：客服懒加载脚本按文件版本刷新

- 根因：组件模板已计算 `customer-service.js` 的文件修改时间，但 `window.Weline.load('customerService')` 未传入该版本；开发环境最终只附加与该文件无关的全局 `_weline_dev`，而静态响应缓存 7 天，浏览器重载仍可能执行旧脚本。
- 变更：保留声明式 `weline.modules.js`、官方模块加载器、懒加载与长缓存，调用 `Weline.load` 时显式传入 `Weline_CustomerService::js/customer-service.js?v=<filemtime>`；模块加载器继续附加全局部署版本，任一版本变化都会生成新缓存键。
- 回归：`CustomerServiceWidgetLazyBindModalTest` 约束模板声明 JS 文件版本并将其传给官方加载器，同时禁止退回无版本调用；运行验收检查页面实际脚本 URL、RTL/LTR 与中英文文案。
- 版本：`1.2.7` → `1.2.8`。
- 交付地址：前台 `https://p05113ef3.test.weline.com:9555/`、购物车 `https://p05113ef3.test.weline.com:9555/cart`、英文 `https://p05113ef3.test.weline.com:9555/en_US`、阿拉伯语 `https://p05113ef3.test.weline.com:9555/ar_SA`；后台/API N/A（本次未改路由或后台页面）。

## Hotfix — 2026-09-03：客服组件动态语言方向同步

- 根因：浮窗在中文页面动态切换到 `ar_SA` 后，标题和输入提示已翻译，但组件根节点未同步 `dir`，浏览器实测计算方向仍为 `ltr`。
- 变更：复用 `updateWidgetLocaleText()` 的初始化与切换路径，仅为 `#customer-service-widget` 同步规范化 `lang` 和组件级 `dir`；阿拉伯语、波斯语、希伯来语、乌尔都语使用 `rtl`，切回中文/英文恢复 `ltr`，不改页面 `documentElement` 方向。
- 回归：`CustomerServiceWidgetAccessibilityContractTest` 约束 RTL 识别、`lang` 规范化与双向恢复；CustomerService 全量 Unit 与内置浏览器 zh→en_US→ar_SA→en_US 动态切换作为运行验收。
- 版本：`1.2.6` → `1.2.7`。
- 交付地址：前台 `https://p05113ef3.test.weline.com:9555/`、英文 `https://p05113ef3.test.weline.com:9555/en_US`、阿拉伯语 `https://p05113ef3.test.weline.com:9555/ar_SA`；后台/API N/A（本次未改路由或后台页面）。

## Hotfix — 2026-09-03：客服组件 JS 词典键完整性

- 根因：`customer-service.js` 的 31 个本地化字面量中有 9 个未列入 `WidgetTranslationService::WIDGET_KEYS`；13 份 CSV 已有译文，但运行时序列化会漏掉“发送”等键，组件内切换英语或阿拉伯语时仍回退中文。
- 变更：将缺失键纳入组件词典白名单，不改既有语言包与路由；所有 13 个支持语言继续由同一服务生成运行时字典。
- 回归：`CustomerServiceWidgetAccessibilityContractTest` 自动提取 JS 中单引号和双引号 `__()` 字面量，与服务白名单做差集，防止后续新增文案再次漏序列化。
- 验收口径：CustomerService 全量 Unit、`setup:upgrade`、运行时 en_US/ar_SA 词典探针，以及内置浏览器 zh→en_US→ar_SA 控件与发送按钮可访问名称联动。
- 版本：`1.2.5` → `1.2.6`。
- 交付地址：前台 `https://p05113ef3.test.weline.com:9555/`、英文 `https://p05113ef3.test.weline.com:9555/en_US`、阿拉伯语 `https://p05113ef3.test.weline.com:9555/ar_SA`；后台/API N/A（本次未改路由或后台页面）。

## Hotfix — 2026-09-03：客服浮窗语言切换字典闭环

- 根因：浮窗列出 13 种可选语言，但首屏只序列化当前站点语言和中文回退字典；在中文页面切换 English/العربية 时，`changeLanguage()` 会更新选中值，却只能回退中文界面。
- 变更：从 `supportedLocales` 统一生成序列化语言代码，并保留当前语言与 `zh_Hans_CN` 兜底；既有 `updateWidgetLocaleText()` 同步标题、设置标签、输入占位、显示模式选项及发送按钮可访问名称。
- 测试：扩展 `CustomerServiceWidgetAccessibilityContractTest`，约束所有受支持语言字典进入页面配置；真实验收覆盖中文首页内 zh→en_US→ar_SA 切换。
- 版本：`1.2.4` → `1.2.5`。
- 交付地址：前台 `https://p05113ef3.test.weline.com:9555/`、英文 `https://p05113ef3.test.weline.com:9555/en_US`、阿拉伯语 `https://p05113ef3.test.weline.com:9555/ar_SA`；后台/API N/A（本次未改路由或后台页面）。

## Hotfix — 2026-09-03：悬浮客服表单无障碍关联

- 当前状态：`code_ready_webui_pending`；不将 HTTP/单测替代 Browser 验收。
- 变更：发送图标按钮使用既有“发送”翻译键提供 `aria-label`，并在浮层语言切换时同步更新；“我的语言”“显示模式”标签分别以 `for` 关联原有 select。
- 测试：新增 `CustomerServiceWidgetAccessibilityContractTest`，约束标签关联、初始可访问名称和动态翻译同步。
- 版本：`1.2.3` → `1.2.4`。
- 运行验收：待 `setup:upgrade`、缓存清理、服务重载与 zh/en/ar 页面探针；WB-OP 随汉服商城内置 Browser 总验收执行。
- 交付地址：前台 `https://p05113ef3.test.weline.com:9555/`、英文 `https://p05113ef3.test.weline.com:9555/en_US`、阿拉伯语 `https://p05113ef3.test.weline.com:9555/ar_SA`；后台/API N/A（本次未改路由或后台页面）。

## Hotfix — 2026-08-31：页脚联系客服打开悬浮聊天

- 当前状态：`implemented`
- 变更：新增 `footer-contact-service-link`，经 `default_injections` 注入 Theme `footer-help-links`；点击调用 `__WelineLoadCustomerServiceWidget(true)` 打开悬浮客服窗。
- 范围契约：`page_layouts=['*']` 保持编辑器内全布局可用；`default_injections.layout_type='homepage'` 仅表示默认主题自动注入首页。
- 版本：`1.2.2` → `1.2.3`。

## 2026-08-30 — 前台 JS 改 modules + data-weline-load（第二波）

- 当前状态：`implemented`
- 变更：登记 `view/statics/frontend/weline.modules.js`；部件/布局改 `data-weline-load`，删除 `@static`/`<js>` 直引。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。

<!-- weline:module-development-baseline:generated-once -->
# Weline_CustomerService 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.2.1`（文档基线） | docs_handoff | `REQ-CUSTOMERSERVICE-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.2.1` — 文档制度启用基线

- 基线版本：`1.2.1`
- 目标版本：`1.2.1`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-CUSTOMERSERVICE-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-CUSTOMERSERVICE-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-CUSTOMERSERVICE-0001` / `1.2.1` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。

## 2026-09-14
- hi_IN 店面 chrome：路径 locale + WidgetI18n；客服工具栏/活动标/批发信用漏译收口。
