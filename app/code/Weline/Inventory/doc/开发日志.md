<!-- weline:module-development-baseline:generated-once -->
# Weline_Inventory 开发日志

## 2026-09-11

- 库存后台页满宽：根壳由 `.w-container` 改为 `.w-backend-page`，吃满主栏。版本 `2.5.21`→`2.5.22`。
- 授权页布局收拾：列表卡与授权卡分离；修授权表单超 12 列折行；去掉重复网站选择。版本 `2.5.20`→`2.5.21`。
- WarehouseSelect 透传 `options-json`，已选仓可读名回填。版本 `2.5.19`→`2.5.20`。
- 审图：授权页手风琴默认收起，分页上移到列表上方，降低首屏密度。版本 `2.5.18`→`2.5.19`。
- 修复授权页翻页/重置：后台 `<base href>` 下相对 `?query` 会跳仪表盘，改为完整 `@backend-url(.../authorizations)`。版本 `2.5.17`→`2.5.18`。
- 授权页仓库手风琴补关键词搜索与翻页（`keyword`/`page`/`limit`）。版本 `2.5.16`→`2.5.17`。
- 默认仓树扩国：`defaultCatalog` 补齐 BR/ES/MX/NG/PH/RO/TH/VN，对齐 CJ 等货源远程履约国，幂等 `ensureDefaultTree`。版本 `2.5.15`→`2.5.16`。
- 授权页改为仓库手风琴：按仓展开已授权店铺；去掉 `?variant=A|B|C` 店→仓树原型。版本 `2.5.14`→`2.5.15`。
- 默认站店种子仓挂载：`ensureDefaultStoreWarehouseMount` 同步全球仓树叶子到 store=0，保留 `SYS-DEFAULT` 默认逻辑仓；授权页树形原型 `?variant=A|B|C`。版本 `2.5.13`→`2.5.14`。
- 默认站/店仓授权种子：`WarehouseStoreAuthorization.is_seed`；`ensureDefaultSiteAuthorization` 幂等补 `SYS-DEFAULT` 逻辑仓 + website=0/store=0 默认授权；种子禁删、默认可改绑；后台授权表徽章/锁定与同步 CTA。版本 `2.5.12`→`2.5.13`。
- 库存调整/授权表单标签化：店铺 `<w:websites:store:select>`、Offer `<w:product:offer:select>`、在手 `<w:inventory:on-hand:field>`、命令 ID `<w:inventory:command-id:field>`（可生成）、授权仓 `<w:inventory:warehouse:select>`；`StoreSelectOptions` 组装 store_id。版本 `2.5.11`→`2.5.12`。
- 审图修复：调整表单「提交调整」去掉 `--w-span-md:1` + `data-w-width=full` 压扁；改为 span2 + 内容宽按钮。
- 库存/库存调整后台可读化：中文列（商品/在手件/可售等）、SKU·Offer 联查、副标题「库存管理」；调整流水按事件类型 + LIMIT 避免无界账本 SELECT 503。契约 UT 6 通过；Browser 验收 stocks/adjustments。
- 仓库页同页 Tab「码别名」：`WarehouseCodeLabel` + LocalModel 可点翻译；树表别名读 DB。版本 `2.5.10`→`2.5.11`。
- 种子仓禁删（`is_seed`）、英文码 `WarehouseCodeAlias` 别名、名称 `LocalDescription` + `<local>` 可点翻译。版本 `2.5.9`→`2.5.10`。
- 仓库国家选择改为 `<w:theme:address-quick>`：添加与搜索过滤共用，选择立即过滤树表。版本 `2.5.8`→`2.5.9`。
- 仓库后台改为国家→省份→仓树形：`Warehouse` 增 `parent_id/node_kind/country_code/region_code`；`WarehouseHierarchyService` 默认全球履约国种子、父码前缀累接、仓码不可变、有库存/预占禁删；树表行内加仓/加省。版本 `2.5.7`→`2.5.8`。
- 新增 `<w:inventory:warehouse:select>` + `WarehouseSearch` JSON（`warehouse-search/search`）；供 Dropship 仓映射选本地仓。版本 `2.5.6`→`2.5.7`。

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `2.5.5`（文档基线） | docs_handoff | `REQ-INVENTORY-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `2.5.5` — 文档制度启用基线

- 基线版本：`2.5.5`
- 目标版本：`2.5.5`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-INVENTORY-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-INVENTORY-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-INVENTORY-0001` / `2.5.5` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
