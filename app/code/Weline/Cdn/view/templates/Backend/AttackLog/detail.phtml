<?php
/**
 * CDN攻击日志详情页
 *
 * @var \Weline\Framework\View\Template $this
 */

/** @var \Weline\Cdn\Model\AttackLog $log */
$log = $this->getData('log');
$cdnResponse = $log->getData('cdn_response_parsed') ?: [];
$cdnResponseJson = \json_encode($cdnResponse, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE) ?: '[]';
?>

<!-- 消息提示 -->
<div class="w-grid" style="--w-mb:var(--weline-space-4);">
    <template>Weline_Component::message.phtml</template>
</div>

<!-- 基本信息 -->
<div class="w-card" style="--w-mb:var(--weline-space-6);">
    <div class="w-card__header">
        <w:icon name="info" size="sm"></w:icon><?= __('攻击基本信息') ?>
    </div>
    <div class="w-card__body">
        <div class="w-grid">
            <div style="--w-span-md:6;--w-span-lg:4;--w-mb:var(--weline-space-4);">
                <div class="w-text" data-transform="uppercase" data-tone="muted" data-size="sm"><?= __('被攻击域名') ?></div>
                <div class="w-text" data-weight="medium"><?= htmlspecialchars($log->getData('domain')) ?></div>
            </div>
            <div style="--w-span-md:6;--w-span-lg:4;--w-mb:var(--weline-space-4);">
                <div class="w-text" data-transform="uppercase" data-tone="muted" data-size="sm"><?= __('攻击者IP') ?></div>
                <div class="w-text" data-weight="medium" data-family="mono"><?= htmlspecialchars($log->getData('attacker_ip') ?: '-') ?></div>
            </div>
            <div style="--w-span-md:6;--w-span-lg:4;--w-mb:var(--weline-space-4);">
                <div class="w-text" data-transform="uppercase" data-tone="muted" data-size="sm"><?= __('攻击类型') ?></div>
                <div>
                    <span class="w-badge" data-w-background="muted"><?= $log->getAttackTypeLabel() ?></span>
                    <small class="w-text ms-2" data-tone="muted">(<?= $log->getData('attack_type') ?>)</small>
                </div>
            </div>
            <div style="--w-span-md:6;--w-span-lg:4;--w-mb:var(--weline-space-4);">
                <div class="w-text" data-transform="uppercase" data-tone="muted" data-size="sm"><?= __('攻击次数') ?></div>
                <div>
                    <span class="w-badge fs-6" data-w-background="danger"><?= $log->getData('attack_count') ?></span>
                </div>
            </div>
            <div style="--w-span-md:6;--w-span-lg:4;--w-mb:var(--weline-space-4);">
                <div class="w-text" data-transform="uppercase" data-tone="muted" data-size="sm"><?= __('当前状态') ?></div>
                <div>
                    <?php
                    $statusVal = $log->getData('status');
                    $statusClass = match($statusVal) {
                        'active' => 'bg-danger',
                        'recovered' => 'bg-success',
                        default => 'bg-secondary'
                    };
                    ?>
                    <span class="w-badge <?= $statusClass ?> w-cluster" data-align="center" style="--w-gap:var(--weline-space-1);">
                        <i class="mdi mdi-<?= $statusVal === 'active' ? 'alert-circle' : ($statusVal === 'recovered' ? 'check-circle' : 'close-circle') ?>"></i>
                        <?= $log->getStatusLabel() ?>
                    </span>
                </div>
            </div>
            <div style="--w-span-md:6;--w-span-lg:4;--w-mb:var(--weline-space-4);">
                <div class="w-text" data-transform="uppercase" data-tone="muted" data-size="sm"><?= __('执行动作') ?></div>
                <div class="w-text" data-weight="medium"><?= $log->getActionLabel() ?></div>
            </div>
            <div style="--w-span-md:6;--w-span-lg:4;--w-mb:var(--weline-space-4);">
                <div class="w-text" data-transform="uppercase" data-tone="muted" data-size="sm"><?= __('持续时间') ?></div>
                <div class="w-text" data-weight="medium"><?= $log->getDurationFormatted() ?></div>
            </div>
            <div style="--w-span-md:6;--w-span-lg:4;--w-mb:var(--weline-space-4);">
                <div class="w-text" data-transform="uppercase" data-tone="muted" data-size="sm"><?= __('记录时间') ?></div>
                <div class="w-text" data-weight="medium"><?= $log->getData('created_at') ?></div>
            </div>
        </div>
        
        <?php if ($log->getData('reason')): ?>
        <div style="--w-mt:var(--weline-space-4);">
            <div class="w-text" data-transform="uppercase" data-tone="muted" data-size="sm" style="--w-mb:var(--weline-space-2);"><?= __('攻击原因/详情') ?></div>
            <div class="w-alert" style="border-left: 4px solid #dc3545;" data-tone="danger">
                <?= nl2br(htmlspecialchars($log->getData('reason'))) ?>
            </div>
        </div>
        <?php endif; ?>
    </div>
</div>

<!-- 时间线 -->
<div class="w-card" style="--w-mb:var(--weline-space-6);">
    <div class="w-card__header">
        <w:icon name="clock" size="sm"></w:icon><?= __('攻击时间线') ?>
    </div>
    <div class="w-card__body">
        <div class="ps-4" data-w-position="relative">
            <div style="width: 2px; background: var(--backend-color-border); left: 10px;" data-w-position="absolute"></div>
            
            <div data-w-position="relative" style="--w-mb:var(--weline-space-6);">
                <div style="left: -24px; top: 6px; width: 10px; height: 10px; border-radius: 50%; background: #dc3545; border: 2px solid #fff; box-shadow: 0 0 0 2px #dc3545;" data-w-position="absolute"></div>
                <div class="w-text" data-tone="muted" data-size="sm"><?= $log->getData('started_at') ?></div>
                <div>
                    <strong><?= __('检测到攻击') ?></strong> - 
                    <?= __('来自 %{1} 的 %{2} 攻击', [
                        htmlspecialchars($log->getData('attacker_ip') ?: __('未知IP')),
                        $log->getAttackTypeLabel()
                    ]) ?>
                </div>
            </div>
            
            <?php if ($log->getData('action') === 'cdn_notified' || !empty($cdnResponse)): ?>
            <div data-w-position="relative" style="--w-mb:var(--weline-space-6);">
                <div style="left: -24px; top: 6px; width: 10px; height: 10px; border-radius: 50%; background: #ffc107; border: 2px solid #fff; box-shadow: 0 0 0 2px #ffc107;" data-w-position="absolute"></div>
                <div class="w-text" data-tone="muted" data-size="sm"><?= $log->getData('created_at') ?></div>
                <div>
                    <strong><?= __('已通知CDN') ?></strong> - 
                    <?= __('向CDN服务商发送攻击防护请求') ?>
                </div>
            </div>
            <?php endif; ?>
            
            <?php if ($log->getData('status') === 'recovered'): ?>
            <div data-w-position="relative">
                <div style="left: -24px; top: 6px; width: 10px; height: 10px; border-radius: 50%; background: #28a745; border: 2px solid #fff; box-shadow: 0 0 0 2px #28a745;" data-w-position="absolute"></div>
                <div class="w-text" data-tone="muted" data-size="sm"><?= $log->getData('ended_at') ?></div>
                <div>
                    <strong><?= __('攻击已恢复') ?></strong> - 
                    <?= __('持续时间 %{1}', [$log->getDurationFormatted()]) ?>
                </div>
            </div>
            <?php endif; ?>
        </div>
    </div>
</div>

<!-- CDN响应 -->
<?php if (!empty($cdnResponse)): ?>
<div class="w-card" style="--w-mb:var(--weline-space-6);">
    <div class="w-card__header">
        <w:icon name="cloud" size="sm"></w:icon><?= __('CDN响应详情') ?>
    </div>
    <div class="w-card__body">
        <pre class="w-text" style="background: var(--backend-color-bg-secondary); overflow-x: auto; white-space: pre-wrap; word-break: break-all;" data-w-radius="md" data-family="mono" data-size="sm"><?= htmlspecialchars($cdnResponseJson, ENT_QUOTES, 'UTF-8') ?></pre>
    </div>
</div>
<?php endif; ?>

<!-- 操作栏 -->
<div class="w-cluster" style="--w-gap:var(--weline-space-2);">
    <a href="@backend-url('*/backend/attackLog/index')" class="w-button" data-tone="neutral">
        <w:icon name="arrow-left" size="sm"></w:icon> <?= __('返回列表') ?>
    </a>
</div>
