# 合规复审 — remote-translation-rest（2026-09-22）

| 席位 | 结论 | 证据 |
|------|------|------|
| API | pass | 薄 Rest 仅 `w_query`；Websites/I18n 分模块；文档 + 升版 1.8.46 / 1.0.77 |
| 查询 | pass | Provider `i18n_remote_translation` 四 op；`auth=backend` + `backend_acl.kind=source`；契约 UT 6/6 |
| i18n | pass | ingest→LocaleDictionary+publishLocale；collect `enqueueAiTranslation=false`；禁 CSV |
| 安全 | pass | ACL 拆分；task 非所有者 404；limit≤200 / items≤100；单飞+TTL；HTTP 未鉴权 401 |
| Setup | pass* | 路由已入 `generated/routers/backend_rest_api.php`；并发 cron 占 setup 门禁时 `--route` 可延后（路由已存在） |

## 交付闭环（不可跳过）

| 层 | 结果 | 证据 |
|----|------|------|
| UT | **PASS 6/6** | `I18nRemoteTranslationQueryProviderContractTest` + `RemoteTranslationCatalogContractTest` |
| RT（Query 全链路） | **PASS** | `php app/code/Weline/I18n/scripts/smoke-remote-translation-rest.php` → `generated/tmp/remote-translation-rest-smoke.json` |
| RT 步骤 | websites_list / locales / pending / ingest 写+invalid / ingest skip / collectStart / status / 异主 404 / HTTP 401 | 全部 `ok:true` |
| WB | N/A | 本需求为 Admin REST/Query，无前台 Theme 页 |

### 冒烟要点（摘自 JSON）

- `ingest_write_and_invalid`：`written=1`，`invalid=2`（`duplicate_in_batch` + `empty_source`），约 33s（曾与词典收集争用）
- `ingest_skip_conflict`：`skipped=1`，`written=0`
- `collect_status_foreign_404`：`code=404`
- `http_unauth_401`：`rest_backend` 前缀下 catalog 路由返回 401

鉴权后 REST 体抽检：与 Query 核同路径；未落盘生产 Token。本机有后台 Session 时可对同一 URL 带 Cookie 复验。

## 路由（本机）

- `/{rest_backend}/websites/rest/v1/remote-translation-catalog/{websites|languages}`
- `/{rest_backend}/i18n/rest/v1/remote-translation/{pending|ingest|collect-start|collect-status}`
- `rest_backend` ≡ `env.router.area_routes.rest_backend.prefix`
