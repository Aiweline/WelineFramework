## 2026-09-26 — 配送服务名「国际标快」词典补齐（1.0.90）

- 包：`dict-fill-shipping-lane-names.v1.php` 增补「国际标快」×39 locale；remediate 兼 MySQL（hanfu）/PgSQL。
- 伴生 Shipping CSV + `i18n:collect`；未启 Ollama。
- 版本：`1.0.89` → `1.0.90`。

## 2026-09-26 — Theme Address 标签词典补齐（默认站全 locale）

- 包：`dict-fill-theme-address-labels.v1.php` + remediate；11 源串 × 39 locale upsert + `publishLocale`。
- 覆盖：请选择省/市/区/街道、可直接输入该地区、邮编匹配/不支持配送等（省份/城市/区县本体原已有）。
- 未启 Ollama；人工译写。

## 2026-09-22 — 远程翻译 Demo AI 操作说明 + 技能（1.0.85）

## 2026-09-25 — N4 LocaleCatalog / LanguageSwitcher 改读 RenderContext（1.0.89）

- 旁路审计：`LocaleCatalogScopeResolver` / `LanguageSwitcher::{defaultLanguage,defaultCurrency,filterFrontendLanguages}` 优先 `StorefrontRenderContextReader`（locale_catalog / website_table_snapshot / currency）；miss 再 w_query / WebsiteData。
- 禁平行袋；禁清 207。
- UT：`LocaleCatalogRenderContextReaderContractTest`。
- 版本：`1.0.88` → `1.0.89`。

## 2026-09-24 — 语言切换器目录 HotCache（P9）（1.0.88）

- `LanguageSwitcher::buildLanguagesFromScope`：稳定语言目录走 `StorefrontScopeHotCache`（请求 memo + `i18n` 池 website 策略）；禁止把当前 URL/DOM id 写入目录缓存。
- 路径绑定 HTML 外壳仍用进程 `$htmlCache`。
- 契约 UT：`LanguageSwitcherCatalogHotCacheContractTest`。
- 版本：`1.0.87` → `1.0.88`。

## 2026-09-23 — 退换政策语气柔化（实质不变）（1.0.87）

- 包：`dict-fill-soft-tone-returns.v1.php` + remediate（默认站非中文 locale 全覆盖）。
- 版本：`1.0.86` → `1.0.87`。

## 2026-09-23 — 跨境退换词典全语种包（1.0.86）

- 包：`dict-fill-crossborder-returns-wholesale.v1.php` + remediate（默认站非中文 locale）。
- 覆盖 returns/refund/trust-badges/B2B 新中文源串。
- 版本：`1.0.85` → `1.0.86`。


- 增：`doc/远程翻译API-Demo下载与验收.md`（下载 zip、Admin Bearer、三 type 验收、WLS reload、禁止项）。
- 增技能：`doc/ai/skills/remote-translation-api-demo/SKILL.md`（MCP `doc/ai/skills` 索引）。
- 交叉链：`rest-api-remote-translation.md`、包内 README；Api 指针 `Weline_Api/doc/api-demo-download.md`。
- 版本：`1.0.84` → `1.0.85`。

## 2026-09-22 — 远程翻译 Demo 下载包 TLS/README（1.0.84）

- JS Demo：本机 `*.test.weline.com` / localhost 默认放宽 TLS（对齐 PHP curl）；可选 `WELINE_TLS_INSECURE=1`。
- php/js 目录内附 README，下载 zip 含说明。
- 版本：`1.0.83` → `1.0.84`。

## 2026-09-22 — 远程翻译 type 扩面 phrase|meta|local_model（1.0.83）

- Path/ACL 不变；body `type` 默认 `phrase`。
- `RemoteDictionaryAssistService`：phrase/meta pending 切分（排除 / 仅 `@meta::`）+ ingest shape 校验。
- `LocalModelTranslationService::remotePending/remoteIngest`：未译字段展开 + Local upsert（冲突 skip）。
- Query/Rest 透传 `type`；`collect*` 对 `local_model` → 422。
- Demo：`source/api-demo/i18n_remote_translation` + `WELINE_REMOTE_TYPE`；api.demo / Provider params 补 type。
- 契约 UT + 文档 `rest-api-remote-translation.md` / contracts。
- 版本：`1.0.82` → `1.0.83`。

## 2026-09-22 — 词典收集 cron 识别单飞 BUSY（1.0.82）

- 协同 Framework `2.5.151`：`i18n:collect` 遇单飞锁退出码 `75`；cron `DictionaryCollect` 记为「跳过：已有收集进行中」，不再误报完成。
- 契约：`DictionaryCollectCronContractTest`。
- 版本：`1.0.81` → `1.0.82`。

## 2026-09-22 — 远程协助翻译 API Demo 并块（1.0.81）

- Query `i18n_remote_translation`：顶层 `'demo'=>true`；ops `frontend=true`，保持 `external=false` + `auth=backend` ACL。
- 可下载示例：`source/api-demo/i18n_remote_translation/{php,js}` + README（Admin REST / env Token，无真实密钥）。
- Backend `api.demo`：`RemoteTranslationApiDemoDescriptor` id=`remote-translation-assist-demo` + `ApiDocDemoObserver`（`api_doc_collect_after`）。
- 契约 UT：descriptor `demo=>true` 且各 op `frontend=true`。
- 版本：`1.0.80` → `1.0.81`。

## 2026-09-22 — 商家友好退换文案默认站全语种词典（1.0.80）

- 包：`scripts/data/dict-fill-merchant-leaning-returns.v1.php`（8 源串×39 locale）+ `remediate-dict-fill-merchant-leaning-returns.php`；`--apply` + `publishLocale`。
- 覆盖：Shipping returns 1.1/1.2/1.4/不予退换/4.2 + Theme refund 1.2/说明/可退款条件（1）。
- 版本：`1.0.79` → `1.0.80`。

## 2026-09-22 — 货币切换点击无跳转（1.0.79）

- **现象**：顶栏语言可开可切；货币菜单能开，点选项无跳转（像“点了没效果”）。
- **根因**：选项是无 `href` 的 `button`，依赖 `currency.js`；assets 用 `$GLOBALS` 去重，WLS Worker 复用泄漏后整页跳过 `data-weline-load="i18n,currency"`，静态 hook 缓存还会被毒化。
- **改**：去重改 `RequestContext`；切换器根节点常驻 `data-weline-load="currency"`（缓存命中也带模块）。
- **契约**：`HeaderChoiceSelectorAssetsContractTest`。
- 版本：`1.0.78` → `1.0.79`。

## 2026-09-22 — 政策合规整改文案默认站全语种词典（1.0.78）

- **背景**：Theme 合规整改仅写了 zh/en CSV；默认站 40 语种中其余 39 语对新源串大多未进系统词典。
- **改**：`scripts/data/dict-fill-policy-compliance-remediation.v1.php`（8 源串×39 locale）+ `remediate-dict-fill-policy-compliance-remediation.php`；`--apply` → 312 upsert + `publishLocale`×39。
- **覆盖**：顶栏「国际配送，售后有保障」、隐私 4.2、Cookie 先同意、发货/退换新旧源键对齐新语义。
- **抽检**：de_DE / hi_IN / ar_SA 等非中英 ≠ 中文 source。
- **未做**：`w_weline_faq_item`「签收后 7 日内」实体文案另案（非本波 Theme 源串）。
- 版本：`1.0.77` → `1.0.78`。

## 2026-09-22 — 远程协助翻译 REST（1.0.77）

- 增：`RemoteDictionaryAssistService` / `RemoteCollectTaskService` + Queue；Query `i18n_remote_translation`；薄 Rest `Api/Rest/V1/RemoteTranslation`。
- `DictionaryCollectService::collect` 远程路径 `enqueueAiTranslation=false`；ingest → LocaleDictionary + publishLocale。
- 文档：`doc/rest-api-remote-translation.md` + team contracts；契约 UT。
- **交付闭环**：契约 UT 6/6；运行时冒烟脚本 `scripts/smoke-remote-translation-rest.php` → `generated/tmp/remote-translation-rest-smoke.json`（pending/ingest/skip/collect/HTTP401 全绿）；合规复审见 `doc/开发/team/remote-translation-rest/meetings/合规复审.md`。
- 版本：`1.0.76` → `1.0.77`。

## 2026-09-22 — /policy/accessibility 默认站全语种补译

- Theme i18n zh/en CSV 补 23 条无障碍正文；`php bin/w i18n:collect Weline_Theme`
- 词典包 `dict-fill-policy-accessibility.v1.php` upsert 39 locale×23 词 + publishLocale；抽检 en/de/fr/ar/bg/hi meta+正文
- 修：Policy 面包屑/标题走 WidgetI18n；Seo `PageSeoContextResolver` meta 走 WidgetI18n（避免 `__()` 粘默认 en）

## 2026-09-22 — LanguageSwitcher chrome 非中英语种走全局词典（1.0.76）

- 问题：模块 CSV 仅 zh/en 后，`translateChrome` 读 `i18n/{locale}.csv` 对 bg_BG 等恒空，回退 `__()` 未预取 → 顶栏仍「切换语言」。
- 改：渲染前对 chrome 源串 `Parser::prefetchWords`；`translateChrome` 优先 `getPrefetchedGlobalWord`。
- 协同 Search Taglib 预取「搜索」按钮文案；Admin 侧栏当前语预取。
- 版本：`1.0.75` → `1.0.76`。

## 2026-09-22 — 后台菜单 chrome 词典 pack（1.0.75）

- 增：`scripts/data/dict-fill-backend-menu-chrome.v1.php` + `remediate-dict-fill-backend-menu-chrome.php`（`平台应用商店` → 默认站全 locale upsert + `publishLocale`）。
- 协同 Admin `2.0.11` 当前语种侧栏预取。
- 版本：`1.0.74` → `1.0.75`。

## 2026-09-21 — 大陆仓发海外政策词条词典 pack（1.0.74）

- 增：`scripts/data/dict-fill-china-export-policy.v1.php` + `remediate-dict-fill-china-export-policy.php`。
- 协同 Theme `2.2.529` / Shipping `2.9.22`：时效参考化、关税默认买家、清关延误不自动退款。
- 版本：`1.0.73` → `1.0.74`。

## 2026-09-21 — 结账关税提示/航线名漏译：全局词典模块过滤修复（1.0.73）

- 现象：en_US 结账 Shipping Method 仍中文「运费不含关税与税费」「中东非洲」；词典已有全语种但 `source_module` 为空。
- 根因：Phrase 按请求模块 `wordsByModule` 的 `IN (modules)` 永匹配不到 NULL `source_module`。
- 修：`GlobalDictionaryProvider` 支持 `NULL_SOURCE_MODULE_KEY` 单次加载全局词；`words()` 扁合并入；协同 Framework `2.5.138` Parser。
- 版本：`1.0.72` → `1.0.73`。

## 2026-09-21 — 政策合规词条默认站全语种词典 pack（1.0.72）

- 增：`scripts/data/dict-fill-policy-compliance.v1.php` + `remediate-dict-fill-policy-compliance.php`（website_id=0 全 locale upsert）。
- 协同 Theme `2.2.527` / Shipping `2.9.19` 政策文案纠偏；抽检 de/hi/ar/es 非中文。
- 版本：`1.0.71` → `1.0.72`。

## Hotfix — 2026-09-21：choice-filter 重复定义 Uncaught Error

- 现象：首页控制台 `Weline.UI component already defined: choice-filter`；同页 FPC/多片段注入多份 `weline-choice-selector.js`（不同 `?v=`）。
- 修：`header-choice-selector.js` 改为 `export function register` + already-defined 幂等；Hook 资产模板去掉显式 CSS/JS，改由 `weline-ui` lazyComponentSources 加载；assets 去重改 `$GLOBALS`。
- Theme：`lazyComponentSources/Styles` 与 `weline-ui-assets.json` lazy_components 登记 `choice-filter`；`resource:compile welineUi`。
- 验：契约 `HeaderChoiceSelectorAssetsContractTest` 3 tests PASS；Browser 首页 `choice-filter` 已注册且仅 1 个 script。

## 2026-09-21 — 活跃 locale 必须显示目标语（规范专节，1.0.71）

- 增：`模块翻译CSV规范.md`「活跃 / 默认语言必须显示对应译文」；对齐 MCP `active_locale_must_show_target_language`。
- 要点：源串中文正确；非中文默认/活跃 locale 不得露出中文占位；禁止把模板改成英文源串凑合。
- 版本：`1.0.70` → `1.0.71`。

## 2026-09-20 — 模块 CSV 仅中英；多余语种入系统词典后删文件（1.0.70）

- **策略**：模块 `i18n/` 只保留 `zh_Hans_CN.csv` + `en_US.csv`；其它 locale 译文只进系统词典（`LocaleDictionary`）。
- **迁移**：`php bin/w ai:import-csv --all --locale={43}` 共处理 223 个多余 CSV，新导入约 4501 条（已存在跳过约 15.6 万）；随后删除全部非中英模块 CSV。
- **门禁**：`I18nCsvCodec::isModuleCsvLocale`；`AiTranslationExportService` 写回非中英语种直接跳过 CSV。
- **规范**：`模块翻译CSV规范.md`「默认网站全语种」改为中英落盘 CSV、其它进词典；MCP 硬规则同步。
- 版本：`1.0.69` → `1.0.70`。

## 2026-09-20 — 后台默认语言与网站前台默认语言分开

- 后台未写语言的地址使用 `backend_default_language`，不再跟着网站 `default_language`。
- 个人语言优先：管理员在「个人中心 → 我的资料」设置；全局键仅作未登录/未设置时的回落。
- 显式路径语言仍然优先。前台无前缀地址仍是网站默认语言。

## 2026-09-18 — AI 待译词按 100 条循环读（1.0.69）

- 取词和计数都按 `PENDING_READ_PAGE_SIZE = 100` 翻页。本批要 1000 也是每次 100，凑满或本页不足就停。
- 不再 `COUNT(*)` 一次扫完全部未译词。
- 版本：`1.0.68` → `1.0.69`。

## 2026-09-18 — AI 翻译不再扫词（1.0.68）

- 删掉 AI 批次里的全表候选收集，以及 CSV / 菜单 / generated 自找词。收词只留 `i18n:collect`。
- 待译词改为一条 `NOT EXISTS` + `LIMIT` 查询，只取下一批；不再把词典装进 PHP 数组。
- 版本：`1.0.67` → `1.0.68`。

## 2026-09-18 — ActiveLocale 进程缓存 + locale_catalog_changed 失效（1.0.67）

- `ActiveLocaleCodeProvider` 改进程 static；`CountryLocaleLifecycleService` afterCommit 与 `LocaleCatalogChangedClearActiveLocales` 观察者调用 `reset()`。
- 版本：`1.0.66` → `1.0.67`。

## 2026-09-18 — 货币切换器触发钮优先显示符号（1.0.66）

- 现象：顶栏当前货币用粗体印 `USD` / `GBP`，符号挤在旁边。
- 变更：触发钮主文案改符号，ISO 码改为次要小号；无字形时才回退代码。
- 版本：`1.0.65` → `1.0.66`。

## 2026-09-18 — 货币切换器隐藏不可换算币种（1.0.65）

- 现象：启用但 `rate=0` 的 GBP 仍可切换，店面价格全 0 / 不可买。
- 变更：`header-currency-switcher` 过滤 `rate<=0`（保留基准币）；与 Currency `1.0.2` 汇率失效联动。
- 版本：`1.0.64` → `1.0.65`。

## 2026-09-18 — hreflang/canonical 货币段对齐（1.0.64）

- 现象：SEO 面板 `canonical path` / `hreflang canonical parity` FAIL；Expected 带 `/USD/`，Got 无货币段。
- 根因：`InternationalSeoContextService::localePrefix` 用 CNY 硬回退并重注 `user.currency`，与站点默认 USD（路径省略）及 `LocalizedUrlBuilder` 不一致。
- 修复：改走 `LocalizedUrlBuilder`；货币仅从 canonical 路径提取；当前语种 alternate 强制等于 canonical；`State::resolveWebsiteDefaultCurrency()` 作默认货币。
- 契约：`InternationalSeoContextServiceTest` 增货币/self 对齐用例。
- 版本：`1.0.63` → `1.0.64`。

## 2026-09-17 — 顶栏语言国旗首屏空槽（1.0.63）

- 现象：`<span class="w-language-switcher__flag" data-country-flag="us">` 占位留白，国旗不可见。
- 根因：trigger 也走空槽 + `requestIdleCallback` 水合；`syncLanguageSwitcherTriggerFlag` 会把未水合 option 的空 `innerHTML` 盖回 trigger。
- 修复：`CountryFlagMarkup::triggerHtml` 对小体积国旗 SSR data-URI `<img>`；panel 仍空槽；trigger 立即 microtask 水合 + `loading=eager`；sync 禁止擦空。
- `SWITCHER_MARKUP_VERSION` → `component-26-trigger-flag-ssr`。
- 版本：`1.0.62` → `1.0.63`。

## 2026-09-16 — Local 触发器标签级浅蓝底（1.0.62）

- `.w-local-translation__trigger` 统一浅 primary 底（约 10%），覆盖 `data-tone="quiet"`，所有 `<local>` 一眼可辨。
- 静态缓存 `local-translation.css?v=20260916-trigger-bg2`。
- 版本：`1.0.61` → `1.0.62`。

## 2026-09-16 — Local 触发器外壳改 div 约束宽度（1.0.61）

- `<local>` 外壳与触发文案由 `span` 改为 `div`；`wrap`/`trigger` 强制 `inline-size:100%`，避免 inline 按内容撑宽导致三行省略失效。
- 静态缓存 `local-translation.css?v=20260916-trigger-div1`。
- 版本：`1.0.60` → `1.0.61`。

## 2026-09-16 — Local 触发器长文案三行省略（1.0.60）

- `.w-local-translation__trigger` 覆盖 `.w-button` 的 `nowrap`，按容器宽度折行；`.w-local-translation__trigger-text` 最多三行 `line-clamp` + 省略号。
- 静态缓存 `local-translation.css?v=20260916-trigger-clamp3`。
- 版本：`1.0.59` → `1.0.60`。

## 2026-09-16 — 网站语言变更默认入队 LocalModelTranslation（1.0.59）

- `WebsiteLocaleTranslationSync::onWebsiteLocalesChanged` 在词典 AI 入队后，再 `LocalModelTranslationQueueService::enqueue`，使 Website LocalDescription（站名/简介）按已选语言默认补译。
- 版本：`1.0.58` → `1.0.59`。

## 2026-09-15 — 提到翻译即覆盖默认网站已选语种（1.0.58）

- MCP 硬规则 `user_mentions_translation_all_default_website_locales`：用户把「翻译」当任务时，按默认网站 `language_codes`（website_id=0）全语种写 CSV，禁止只译 en_US。
- 规范：[模块翻译CSV规范.md](./模块翻译CSV规范.md) 增「默认网站全语种」；README 指针。
- 版本：`1.0.57` → `1.0.58`

## 2026-09-14 — 快捷登录词典（默认站启用语种）

- 源串：`快捷登录`、`使用 %{1} 登录`。默认网站 `default` 启用语种（含默认 `zh_Hans_CN`）词典补齐；JS 改走 bootstrap `__()`，不再写死中文。
- 未走 Ollama；人工词典 upsert 后 `publishLocale`。

## 2026-09-14 — 非中文 locale 跳过 CJK identity 占位（1.0.56）

- 问题：Theme `hi_IN`/`ar_SA` CSV 把「常见问题」「猜你喜欢」等写成中文=中文；`WidgetI18n` 优先命中后不再查词典，印地语/阿语店面标题漏译。
- 改法：`TranslationResolver::translate` 对非 zh locale 的 CJK identity 视为未译，回落到公共词典；拉丁 identity（Hanfu/Silk）仍短路。
- 验证：`TranslationResolverIdentityMappingTest` 2/2。

## 2026-09-14 — PDP 运费提示印地语词典对齐

- `运费以结算页为准` / `重货` / 重货配送说明 upsert `hi_IN` 并随 collect 进入语言包。店面渲染改由 `WidgetI18n`（路径 locale），避免 `__()` 落后。

## 2026-09-14 — ai:import-csv 读 CLI `$args` 且跳过 CJK 占位（1.0.55）

- 问题：`ImportCsv` 只读命令注册 `$data`，`--file/--locale` 进 `$args` 后报参数为空；把 collect 占位（中文=中文）写入非中文词典会挡住 AI。
- 改法：与 `ai:translate` 一致优先 `$args`；`importFromCsv` 跳过非 zh 的 CJK identity。
- 契约：`AiImportCsvArgsContractTest`。

## 2026-09-14 — 店面 chrome 漏译全语种 upsert

- 源串：找朋友代付 / 点开选曲 / 我的询价 / 批发身份 / 零售车 / 批发车 / 税费与运费将在结算时计算 / 缺重运费空态。
- 语种：ar/es/fr/hi/bn/id/pt/ur/ru/th/vi/de/ja/ko/zh_Hant。
- 分类导航「Home garden furniture」等为目录名，词典仅作短语回退。

## 2026-09-14 — 印地语店面其余漏译入库

- HelpPay/Payment/Shipping/Affiliate/B2B/StoreMusic/Product/Theme 可见词 upsert 并 republish `hi_IN`。

## 2026-09-14 — collect 后 republish 启用语种，避免冲掉 DB 译文（1.0.57）

- 问题：`i18n:collect` 用模块 CSV 覆写 `generated/language/{locale}.php`，把已 publish 的扁平词典（含 hi_IN 结账成功页译文）冲成嵌套 CSV 占位。
- 改法：`dictionary_compile_after` 在 persist 后对启用语种 `AiTranslationPublisher::publishLocale`。
- 验证：Observer 契约含 republish；Browser 成功页英雄区印地语。

## 2026-09-14 — i18n:collect 编译后登记源词并入队空缺翻译（1.0.54）

- 问题：结账成功页源键已在模块 CSV，en_US 词典已有 AI 译文，但不在 `i18n_dictionary`，hi_IN 等对照源语言扫空缺时漏译、回落英文。
- 改法：`dictionary_compile_after` 把默认源语言 `collected_words` 写入公共词典；新词 `enqueueEnabledLocales`。
- 验证：Observer/Collect persist 契约 UT。

## 2026-09-14 — 印地语店面 chrome 漏译入库

- 页脚/推荐部件中文源在 en_US 已有译文但无 hi_IN 行；已 upsert 店面 launch 缺词并 republish。
- 与 Theme 路径 locale 扩到 `hi_IN` 配套。

## 2026-09-14 — 支付方式简介漏译（ar/es/ru/th/vi）+ RTL isolate

- Fake/PayPal/Stripe 结账简介在 ar_SA 等语种词典仍是英文或半英；已 upsert 并 `publishLocale`。
- 英文源键同步写入，避免 Provider 已译成英文后二次 `__()` 仍漏。

## 2026-09-14 — 全站启用语种结账关键词补齐 + 全 locale republish

- 范围：与 hi_IN 同批漏译的结账壳层词（快捷支付/更换地址/账单地址等）同步写入 ar/bn/de/es/fr/id/ja/ko/pt/ru/th/ur/vi/zh_Hant（266 条 upsert）并 republish。
- 店面启用语种（网站联合）：`ar_SA bn_BD es_ES fr_FR hi_IN id_ID pt_BR ur_PK`；`de_DE/ja_JP` 等未挂站会回落默认中文，属配置而非译库。
- 修正 `Checkout/Product` `ar_SA.csv` 中 `快捷支付` 中文占位；`i18n:collect` 后阿语结账标题为「دفع سريع」。
- 存量缺译（相对 en_US 约 2.5k–2.9k/语）交由队列 `I18n AI翻译 *` 续跑（当前 ar_SA running，其余已尝试入队）。
- 验证：语言包键数对齐 DB；`/ar_SA/checkout` `/fr_FR/checkout` 主壳目标语；`/hi_IN/checkout` 已验收。

## 2026-09-14 — hi_IN 结账漏译：发布器无界 SELECT + 模块 CSV 英文热层盖住目标词典

- **审图分流**：店面结账截图 `web_ui` / `ui_shot`（非布局丑点，主因 i18n 回退错误导致英/中混排）。
- **线稿**：顶栏语言 HI → 主列（快捷支付/收货/配送/支付）+ 右栏订单摘要；指控点为标题/标签英文化、部分中文残留。
- **原型调整**：不改结账 IA；保证目标 locale 全局词典优先于 en_US 模块 CSV；发布器流式写出 `generated/language/{locale}.php`。
- **根因**：
  1. `AiTranslationPublisher` 对 locale 词典无界 `fetchArray`（>10000 行抛 Unbounded SELECT），`hi_IN`/`ar_SA` 文件停更（hi 仅 ~6k 键，DB 已有 ~3.3 万）。
  2. WLS Phrase `loadModuleWordsForLocaleChain` 把 en_US 模块 CSV 合并进热层，`translationFromLoadedLayers` 先命中英文，盖住已发布的 hi_IN 译文。
- **修复**：发布器改 `fetchIterator`；模块热层只读目标 locale CSV；缺译仍走 `LocaleFallbackChain` + `loadGlobalDictionaryWord`。补结账可见缺词印地语并 republish。
- **验证**：`AiTranslationPublisherStreamContractTest`、`ModuleCsvFallbackMustNotMaskTargetLocaleContractTest`、`LocaleFallbackChainTest`（8/8）；Browser 禁缓存 `https://p05113ef3.test.weline.com:9555/hi_IN/checkout` 主壳已印地语（सुरक्षित चेकआउट / शिपिंग जानकारी / ऑर्डर सारांश / त्वरित भुगतान）。
- **版本**：`1.0.52` → `1.0.53`（I18n）；Framework `2.5.80` → `2.5.81`。

## 2026-09-13 — language-select 有胶囊时 floating label 上浮

- 问题：多选胶囊不被 Outlined 浮标识别，标签压在胶囊上。
- 改法：`language-select.js`（及 Theme 镜像）选中时打 `has-value` / `data-has-value`；Theme foundation 单独识别。
- 验证：`LanguageSelectSearchFocusContractTest`、`OutlinedFieldLabelContractTest`。
- 版本：`1.0.51` → `1.0.52`

## 2026-09-11 — 词多次翻译失败缓存绕过

- 问题：占位符丢失等同词每批都失败仍反复入队，队列空转刷错。
- 改法：`AiTranslationWordSkipStore`（Cache 池 `i18n_ai_word_skip`）按语言 map 记失败次数；达 3 次则选词/计数绕过并报「多次失败已绕过」；成功清零；map TTL 7 天 + 每语言最多 500 条按最旧剪枝。
- 验证：`AiTranslationWordSkipStoreTest`。
- 版本：`1.0.47` → `1.0.48`

## 2026-09-11 — AI 翻译页：进度 / 配置 顶部 Tab 分段

- 问题：翻译进度板与按语言/按模块配置同页堆叠，一屏过载。
- 改法：顶栏 Tab「翻译进度 | 配置与操作」；配置段内再二级「按语言 | 按模块」。默认 `?tab=progress`。
- 验证：`AiTranslationProgressContractTest`。
- 版本：`1.0.46` → `1.0.47`

## 2026-09-11 — `<local>` 兼容编译期 tag-start

- 问题：新 Taglib 生成器对成对标签派发 `tag-start`/`tag-end`，`Local` 仅 match `tag` → UnhandledMatchError；运行期再把 `<?= ?>` 塞进 HTML 导致抽屉 id 损坏；多行同 field 误报 ID 重复。
- 修复：`tag-start` 同渲染、`tag-end` 空串；`bindRuntimeRecordId()` 绑定循环记录 ID；唯一键附带 `name`；bulk 识别含 `tag-start`。模块 `1.0.46`。

## 2026-09-10 — 进度板「待写回」明示 CSV

- 口径：待写回 = AI 译文尚未导出到模块 `i18n/{locale}.csv`。
- 文案：顶栏/列头/单元格改为「待写回 CSV」「模块 CSV」「CSV 已对齐」。
- 验证：`AiTranslationProgressContractTest`。

## 2026-09-10 — AI 翻译页进度去重

- 问题：新「翻译进度」板与旧「语言配置」表重复展示词典进度/队列。
- 改法：进度只留总览板；删四统计卡；语言表改为「开关+操作」；LocalModel 仅顶栏共享状态。
- 验证：`AiTranslationProgressContractTest`。

## 2026-09-10 — AI 翻译后台：语言×类型进度看板

- 新增 `AiTranslationProgressService`：聚合词典 / LocalModel（共享队列）/ 模块写回；队列状态人话化（模型忙碌/待续跑等），去掉原文堆砌。
- 视图：进度总览表置顶；语言配置表「词典进度」进度条 +「队列状态」精简列。
- 契约：`AiTranslationProgressContractTest` PASS。
- 版本：文档记录；需 `reflection:compile` 以注入新依赖。

## 2026-09-10 — AI 自动翻译停滞：假 running + UTF-8 reconcile 失败

- 现象：cron 仍入队，但 `i18n_locale_dictionary` 无新 `is_ai` 写入；队列 `#46591`（es_ES）`running`+死 PID。
- 根因：Queue `boundResultText` 尾截断切坏 UTF-8 → reconcile 写库抛 `22021` 整轮中断。
- 协作：`Weline_Queue` `1.2.14` 修复截断/隔离；本机 reconcile `#46591`→`done`。
- 恢复探针：`en_US` 词典 `is_ai` 近 15 分钟写入 100 条（最新 `2026-09-10 12:09:55`）；LocalModel 占用车道时词典侧会出现 `AI_TRANSLATION_BUSY`（互斥，非再次卡死）。
- 版本：文档记录，无本模块代码变更。

## 2026-09-09 — 定时词典收集复用 i18n:collect（1.0.45）

- 新增 cron `i18n_dictionary_collect`（`10 * * * *`）：注入并调用 Framework `Console\I18n\Collect::execute`，与 `php bin/w i18n:collect` 同入口，不自写扫描。
- 编译链仍走 `dictionary_compile` / `dictionary_register`；新词入 DB 后由既有 `i18n_ai_translation` 入队翻译。
- 验证：`DictionaryCollectCronContractTest`。
- 版本：`1.0.44` → `1.0.45`

## 2026-09-09 — AI 未译词只从 DB 取；LocalModel 仅扫已安装模块（1.0.44）

- 词典 AI：`collectCandidateWords` 仅 `appendDictionaryWords`；`shouldTranslateWord` 只查 DB locale 词典，不再用 CSV/menu/generated 自找词或判已译（避免 cron 编译 `generated/language/*.php`）。
- LocalModel：`LocalModelTranslationCatalog` 只扫 `Env::getActiveModules()` 的 `Model/*Local.php|*LocalDescription.php`，descriptors 落盘缓存；`enqueue` 先 `findActiveFamilyQueueId` 再 `collectWorkItems`。
- cron / 队列展示名热路径改用明文，避免 Phrase 拉语言包。
- 验证：`AiTranslationDbOnlyCandidatesContractTest` + Catalog/Cron 契约；入队探针不打开 generated language。
- 版本：`1.0.43` → `1.0.44`

## 2026-09-09 — LocalModel 假 running 死 PID 不再占槽（1.0.43）

- 问题：`findActiveFamilyQueueId` 把 `status=running` 且 PID 已死的队列当作 active，cron/EAV 无法再入队；配合僵死 worker 占翻译锁后词典侧大量 BUSY/空转。
- 修复：`isLiveActiveQueueRow`——pending 仍算 active；running 仅在 `posix_kill(pid,0)`（或 ps 回退）存活时算 active，并多探几条 offset 续队。
- 运维收口：清理假 running/半终态、清空僵死 lane lock、双车道（词典+LocalModel）恢复写入。
- 验证：`LocalModelTranslationCronContractTest`；本机近 5 分钟词典更新 >0 且双 lane 锁 PID 存活。
- 版本：`1.0.42` → `1.0.43`

## 2026-09-09 — AI 翻译页 queue_result 撑爆 WLS capture_limit（1.0.42）

- 现象：打开 `/i18n/backend/ai-translation` 报 `WLS output capture exceeded safe memory limits`（chunk≈66MB / max=16MB）。
- 根因：`en_US` 等语言的 `queue.result` 被调度器反复追加进程日志至数十 MB；模板 `<pre>` 整段输出触发 FiberOutputBuffer。
- 修复：`AiTranslation::buildLocaleRows` 对展示用 `queue_result` 尾部截断（4KB）；Queue `1.2.11` 对 result/日志读写加 64KB 上界。
- 验证：`AiTranslationQueueResultBoundContractTest` + `QueueDispatchServiceTest::testQueueResultAndProcessOutputAreByteBounded`；Browser 复开页面。
- 版本：`1.0.41` → `1.0.42`

## 2026-09-09 — LocalModel 保存失败误判 BUSY 空转（1.0.41）

- 根因：准备阶段 / 目标写入的 persist/SQL（含 EAV `25P02`）被标成 `aborted_busy`，且 AI 失败恒 `|| true`，导致每 5 分钟从 offset=0 空转。
- 修复：`processBatch` 区分 `aborted_busy`（真 BUSY）/ `aborted`（其它 AI 失败）/ `skipped_persist`（保存跳过并继续）；源语言 Local 写入失败不阻断目标语 AI（源可来自主表）；`upsertLocalValue` 对 aborted txn / 唯一冲突 `ROLLBACK` 后 `forceCheck` 重试；非 CJK 原文等值视为已译。
- 配套：Eav `1.2.5` 修复 LocalDescription 复合主键迁移（原无前缀表名导致生产未改 PK）。
- 验证：`LocalModelTranslationPersistBusyContractTest` + `FixEavLocalDescriptionCompositePkContractTest`；生产剩余 27→0。
- 版本：`1.0.40` → `1.0.41`

## 2026-09-09 — 死 worker 自动恢复假 running（1.0.40）

- `AiTranslateQueue` / `LocalModelTranslationQueue` 实现 `DeadWorkerRecoveryPatchQueueInterface`：PID 死后回 `pending`（最多 5 次），由 cron reconcile+dispatch 再拉起。
- 依赖 Queue `>=1.2.10` 的死 PID `error+finished=1` 收口。
- 验证：`DeadWorkerRecoveryContractTest`
- 版本：`1.0.39` → `1.0.40`

## 2026-09-09 — LocalModel 与词典 AI 并行（1.0.38）

- LocalModel `translateBatch` 传 `concurrency_lane=local-model`，与词典默认车道分锁，避免互相 BUSY。
- 版本：`1.0.37` → `1.0.38`

## 2026-09-08 — language:select runtimeCallback 防 PHP 泄漏（1.0.34）

- 动态属性（foreach / embed 内 `<?= ?>`）走 `renderRuntimeTag` 时，编译期回调吐出的 PHP 源码会被 echo 成页面文本。
- 对齐 `theme:search-select`：新增 `runtimeCallback` + `buildMarkup`，直接输出语言选择器 HTML；静态属性仍走编译期 `buildMarkup` 调用。
- 版本：`1.0.33` → `1.0.34`。

## 2026-09-08 — 翻译定向失效与写入事务（1.0.33）

- 词典 upsert/delete、AI 保存、语言启停和 Locals 保存接入既有事务与 changed；ResourceChanged 作为 critical sync observer 推进 global/i18n，提交后仅发送版本通知。公共词典、语言目录和控件缓存随代际复用；不再因常规翻译更新清所有 Worker 的业务缓存。语言文件发布延后提交并原子替换，失败恢复与并发发布验证随本轮记录。
- 本轮验收：真实 PostgreSQL + WLS 共享池双进程 17 步通过，译文更新/删除均传播、回滚恢复词条和 @clock、未提交语言文件不发布，无关商品测试缓存仍命中并已精确清理。正式 en_US 文件发布成功（195.34ms），旧首页回执拒绝后重新命中。语言文件显式发布仍推进版本，未保留不安全的内容相同 no-op；物理 DB commit 故障下的文件/数据库跨介质完全原子性不在本轮保证范围。

## 2026-09-08 — 公共词典按模块稳定共享（1.0.32）

- 新增可选 Framework `ModuleGlobalDictionaryProviderInterface`，本模块 `GlobalDictionaryProvider::wordsByModule` 一次 `source_module IN (...)` 查询分组返回独立模块词表，保留原词等值行、locale 边界和确认空模块；异常抛出，不伪造空词典。
- Parser 在原 phrase 池按语言/模块 MGET、缺失集合单次 SQL、按模块 MSET。首个缺失模块的真实缓存键复用原 `remember` / single-flight 参数；获得执行权后重读，避免其他 Worker 已发布仍重复查询。不新增组合事实键、私锁或数据 generation。
- 复用既有 `resource_changed → phrase.clear → Parser.clearWorkerCaches + runtime cacheClear 广播`。新读取路径未改变 Dictionary 裸写的事件行为，也不声称所有 insert/fetch 自动发布变更。
- 最小 RED 验证跨 Worker 不同模块增长顺序、失败后的外层空快照/公开 `Parser::parse` 原词冻结、交错补模块覆盖。修复后联合定向 15 tests / 65 assertions 通过，包含确认空模块、locale、原 Provider/CLI 全词典和 single-flight 二次读取；原模块 CSV/请求覆盖/exclusive 用例通过。
- 当前配置真实只读双进程：PID 42533 首次 `[Weline_Filters,Weline_Product]` 为 1 DB / 2.28ms；PID 42581 先 Filters 再 Filters+Product 均为 0 DB（0.21ms / 0.29ms）。Filters 无行、Product 为 359 词，最终 map SHA256 `7031a0a943226dd4c45823fbf612a0084b01fa02176143f51cf8bba338a7c7b1` 与原 `words()` 严格相同。使用真实 `phrase` 池的独立探针前缀，写入传 TTL 60，结束仅删除两个探针键；未清正式缓存、未写业务数据、未重载，页面由主任务验收。

## 2026-09-08 — 本机关闭 Ollama：LocalModel 队列会自行退出

- 观察：kill Ollama 后，正在执行的 LocalModel `queue:run` 约数秒内退出；无模型时再被 cron 拉起也会较快以 BUSY/请求失败结束，不会一直卡在 curl。
- 协作：`Weline_Ai` 1.2.35 修复翻译超时参数未生效与连接失败无效重试，缩短本机模型不可用时的空等。
- 版本：文档记录，无本模块代码变更。

## 2026-09-08 — 公共词典支持已知词精确批量查询

- `GlobalDictionaryProvider::exactWords()` 通过现有 `Dictionary::getEntries()` 执行 md5 IN 查询；按原输入词返回有效译文，空译文/原文等值和 `word()` 语义一致，查询失败向调用方抛出，避免错误负缓存。
- 此接口供 Framework 的统一 Phrase 预取使用；不改变按模块词典、LocalModel 自动翻译队列或搜索索引。当前数据库 200 个菜单词批量/逐词结果完全一致，SQL 数 200 → 1。

## 2026-09-08 — AI 翻译 cron 改为每 5 分钟 + LocalModel 批加大（1.0.31）

- 问题：`i18n_ai_translation` 原 `0 * * * *`；遇 `AI_TRANSLATION_BUSY` 后不立刻续队，最长空等约 1 小时。
- 修复：cron 改为 `*/5 * * * *`（幂等入队，已有 pending/running 不重复）；LocalModel `DEFAULT_BATCH_SIZE` 20→40（AI 仍按 20 分块）。
- 验收：`LocalModelTranslationCronContractTest` 断言 `*/5`；`php bin/w cron:task:collect` 刷新任务表时间表达式。
- 版本：`1.0.30` → `1.0.31`。
- 宿主原生编辑。

## 2026-09-08 — LocalModel 按目标语真正批量打 AI（1.0.30）

- 问题：`processBatch` 虽调用 `translateBatch`，但每次只传 `[$sourceText]`，后台 LocalModel 队列对 Ollama 仍是逐条往返（约 1.5–2s/条），吞吐被解码开销淹没。
- 修复：先收集本批待翻项，再按目标 locale 合并源文，以 `AI_TEXT_CHUNK_SIZE=20` 分块一次 `translateBatch`；保持 BUSY/失败早停与 `consumed` 不越过未完成项。
- 验收：`LocalModelTranslationBatchContractTest`；队列日志可见单次 chat/completions 的 prompt token 明显大于单条。
- 版本：`1.0.29` → `1.0.30`。

## 2026-09-08 — 核心 i18n JS 归框架，本模块仅增强（1.0.29）

- 变更：移除本模块 `weline.modules.js` / `js/i18n.js`；核心迁至 `Weline_Framework::js/i18n.js`。保留 language-switcher 等增强。
- 版本：`1.0.28` → `1.0.29`

## 2026-09-08 — 前台 i18n JS 模块化加载（1.0.28）

- 目标：`Weline.i18n` 不再内嵌 `weline.js`；由本模块 `weline.modules.js` 登记 `i18n`（`load: defer`），主题 head/语言部件 declare。
- 变更：`i18n.js` 安装 `window.Weline.i18n` facade；`header-choice-selector-assets` 改为 `data-weline-load="i18n,currency"`。
- 版本：`1.0.27` → `1.0.28`

## 2026-09-07 — AI 翻译队列幂等入队（1.0.27）

- 字典 / LocalModel 翻译入队改走 `Queue\IdempotentQueueAdmission`（`createIfAbsent` slot）。
- `Weline_Queue` 保持 optional（`>=1.2.5`），避免 I18n↔Queue↔Eav 依赖环。
- 契约：`AiTranslationDedupeContractTest`。

## 2026-09-07 — 语言切换国旗水合导致浏览器卡死（1.0.26 续）

- 现象：访问首页后浏览器转圈卡死。
- 根因：多实例切换器在首屏对 `data-i18n-supported-countries` 全量 `getCountryFlags`（含 ES ~81KB SVG），再写入 localStorage / data-URI，主线程被撑爆。
- 修复：首屏仅水合 trigger 国旗；打开面板再拉列表；全局 inflight/内存去重；`>12KB` SVG 不写 localStorage。

## 2026-09-07 — 语言切换器标签声明支持语言集合（1.0.26 续）

- 根节点增加 `data-i18n-supported-locales` / `data-i18n-supported-countries`（当前范围排序列表）。
- 前端与 `localStorage` 上次集合对比：集合变化时仅对**新增国家码**强制走 `getCountryFlags`，避免漏拉新语言国旗。
- 排查「HTML 像被截断」：整页 `</html>` 完整；实为 chrome partial 旧缓存与新 markup 混服（3 个内联 SVG 切换器 + 1 个占位切换器）。`chrome-partial-v9` + `SWITCHER_MARKUP_VERSION` 打入缓存键淘汰旧 HTML。

## 2026-09-07 — 语言切换国旗 SSR 占位 + binquery 分组 CDN 缓存（1.0.26）

- 目标：语言切换器 SSR **不再输出**国旗 SVG/img；改为 `data-country-flag` 空槽。
- 加载：`i18n.getCountryFlags`（`external=true` + `BinQueryCache` ttl=`7d`，可 CDN）按国家码分组返回 SVG；`language-switcher.js` 写入 `localStorage` 后用 data-URI `<img>` 填充。
- 配套：`LanguageSelect` / `getInstalledLocales` 目录项不再嵌入旗面；后台 `getCountryFlag` 仍读 vendor SVG 供管理端入库。
- 验收：curl 首页无超大 flag path；单测 `CountryFlagMarkupTest`；**不**用 Browser 打开本机页。
- 版本：`1.0.25` → `1.0.26`。

## 2026-09-07 — 语言切换国旗改为外链 img，避免 DOM/JS 内存爆炸（1.0.25）

- 根因：`getCountryFlag` / 语言切换器把 `lipis/flag-icons` 完整 SVG 内联进主文档；西语旗单文件约 81KB / 500+ path，首页重复多份后 HTML ~1.4MB、数千 path，浏览器一打开即内存耗尽。
- 修复：新增 `Helper/CountryFlagMarkup`；国旗改为 `/Weline/I18n/view/statics/flags/4x3/{cc}.svg` 的 `<img>`（statics 软链到 vendor）；切换器渲染时把遗留内联 SVG 也改写为 img；拒绝无法识别国家码的超大 SVG。
- 说明：WLS 拒绝 softlink 静态目录后，1.0.26 改为 SSR 占位 + binquery。
- 验收：curl 对比首页体积与 `<path` 数量；单测 `CountryFlagMarkupTest`；不使用 Browser 打开本机页。
- 版本：`1.0.24` → `1.0.25`。

## 2026-09-07 — `@lang{'…'}` collect 键去引号（1.0.24）

- 问题：`@lang{'源文'}` 运行时去引号查词，但 `TranslationCollector` / `JsTranslationsExtractor` 把引号写入 CSV 键，导致英文页仍显示中文源文。
- 变更：收集 `@lang{}` 时剥离包裹引号；与 Inquiry 供应商页英文修复配套。
- 版本：`1.0.23` → `1.0.24`。

## 2026-09-06 — 翻译遇忙/僵死停轮（协作 Ai 1.2.12）

- 词典 / LocalModel 队列已在 1.0.22 做到 BUSY 不立刻续队；Ai 闸门现对僵死占锁 `SIGTERM` 断开，避免空等。
- 本模块无版本变更。

## 2026-09-06 — LocalModel 翻译取源：主表优先 + 大表分页扫描（1.0.23）

- 约定：主表同名字段有值时，即使 Local 无源语言行，也以主表为源翻到其它启用语言。
- 修复：`iterateParentRows` 分页，避免 Shipping Region 等大表无界 SELECT 导致整表扫不到。
- 版本：`1.0.22` → `1.0.23`。

## 2026-09-06 — 定时翻译遇忙/错停本轮、不立刻续队（1.0.22）

- LocalModel / 词典 AI：BUSY 或本批失败时结束本轮，**不** `enqueueContinuation`；有空由下一轮 cron 再入队。
- LocalModel：任意 AI `success=false` 或硬异常即早停，不空推进。
- 版本：`1.0.21` → `1.0.22`。

## 2026-09-05 — AiTranslateQueue 放行 social_login_guide（1.0.21）

- 变更：`requested_by` / `domain` 增加 `social_login_guide`，与支付指南一样支持定向 AI 批次。
- 协作：`Weline_Customer` 社媒登录指南 i18n CLI/队列。

## 2026-09-05 — LocalModel 遇 AI 繁忙早停续队（1.0.20）

- 配合 `Weline_Ai` 翻译单飞闸门：`processBatch` 识别 `AI_TRANSLATION_BUSY` 后停止推进，按 `consumed` 续队，避免多 worker 空等。
- 版本：`1.0.19` → `1.0.20`。

## 2026-09-05 — LocalModel 只翻 AI 启用语种（1.0.19）

- 根因：`collectWorkItems`/`processBatch` 使用全部已安装激活语种（16 个），而 AI 配置仅启用 `en_US`；批次长时间占用 Ollama 写冷门语种，店面关心的 en_US 几乎看不到新增。
- 修复：目标语改为 `getEnabledLocaleCodes()`；无启用语种则跳过；入队 batch_size 固定为 LocalModel 默认 20（不再套用词典 100）。
- 版本：`1.0.18` → `1.0.19`。
- 宿主原生编辑。

## 2026-09-05 — LocalModel 增量收集，避免每批全表扫描（1.0.18）

- 问题：每批 `collectWorkItems()` 物化全部待译项，队列长时间 `running`；与词典 AI 争用 Ollama 时卡在 `curl_exec`；复合主键 upsert 误判不存在导致空行插入。
- 修复：`collectWorkItems($offset,$limit)` 早停 + 跳过目标语已齐字段；队列只取 `batchSize+1`；upsert 按 `(id,locale)` 判定存在。
- 版本：`1.0.17` → `1.0.18`。
- 宿主原生编辑。

## 2026-09-05 — 堵住翻译队列空转并行链（1.0.17）

- 根因：LocalModel 续队 biz_key 为 `base:offset:N`，cron 只查精确 `base`，会并行再开 offset=0；源语言 upsert 用 insert 撞唯一键刷错误。
- 修复：`findActiveFamilyQueueId` 覆盖 offset 族；续队走 `enqueueContinuation`；upsert 改为先查后改/插，源语言已一致则跳过。
- 版本：`1.0.16` → `1.0.17`。

## 2026-09-05 — 修复 AI 翻译队列空转（1.0.16）

- 问题：`i18n_ai_translation` / LocalModel 队列每小时入队后秒失败；词典扫描走 `Model::pagination()` 触发 CLI 空 host 的 `w:form` 危险协议；LocalModel 发现器扫描到测试目录重复 `ReaderTest` 类声明。
- 修复：`appendDictionaryWords` 改为 `limit/offset`；`LocalModelTranslationCatalog` 跳过 `test/Test/UnitTest/tests` 路径。
- 验收：契约单测 + `queue:run` 复现不再出现 form/ReaderTest 编译失败。
- 版本：`1.0.15` → `1.0.16`。
- 宿主原生编辑。

## 2026-09-05 — 补齐 TranslationResolver CSV 恒等词条读取

- 真实根因：`translate()` 已接受 `Hanfu,Hanfu`，但 `moduleWords()` 读取 CSV 时仍以 `$value !== $key` 丢弃恒等项，随后旧词典回退仍可把英文分类名变为中文。
- 最小修复：CSV 读取保留所有非空恒等项；显式 locale、模块优先级、scope 查询和现有 reset 接口不变。没有新增翻译数据或修改模块版本。
- TDD：`TranslationResolverIdentityMappingTest` 读取真实 Theme CSV，仅替换外部词典依赖；修改前 `Hanfu` 实际返回 `汉服`，修改后 `Hanfu / Silk` 均保留。与 `TranslationResolverProcessCacheResetTest` 联合验证共 **3 tests / 7 assertions** 通过。
- 真实环境：配合 Theme 菜单消费端修复，在 default WLS 两个 Worker 均确认清缓存后，英文首页完整 12 项运行探针通过。浏览器实际导航/截图尚未验收，不能以本条替代。
- 交付地址：[英文汉服首页](https://p05113ef3.test.weline.com:9555/en_US/)。

## 2026-09-04 — TranslationResolver 不再用全局 __() 反向翻 EN 展示串（1.0.15）

- 当前状态：`implemented`。
- 问题：`WidgetI18n`/`TranslationResolver` 在 en_US CSV 恒等映射（`Hanfu,Hanfu`）被跳过后回退 `__()`，而 KeyBuilder 仍为 zh，把店面已英文化的分类名翻成 `汉服`。
- 修复：尊重 CSV 恒等映射；显式 locale 查找无命中时返回原文，不再回退全局 `__()`。
- 验收：EN 首页侧栏 `Hanfu`；`WidgetI18n::label('Hanfu')` 在 `/en_US/` 下保持 Hanfu。
- 版本：`1.0.14` → `1.0.15`。

## Hotfix — 2026-09-03：跨 Worker/FPC 的内联国旗 SVG DOM 命名空间（1.0.14）

- 当前状态：`implemented`；真实 WLS 与内置 Browser 门禁执行中。
- 运行复现：现代 `Dom\\HTMLDocument` 对 `/`、`/best-sellers`、`/ar_SA`、`/cart` 连续三轮审计时，`/ar_SA` 一次响应出现 `flag-icons-sa-u67`、`sa-a-u67` 等 20 个 SVG 内部 id 各 2 次；菜单外层随机实例 id 和全部 `aria-controls` 均正常，排除 1.0.13 外层命名空间回归。
- 根因：`InlineSvgIdUniquifier` 使用 Worker 进程局部递增序号；独立 Worker/Hook/FPC 片段都可能生成相同 `-uN`，最终拼装到一个页面时发生碰撞。
- 修复：每次内联 SVG 插入改用 96-bit 随机命名空间，并同步改写 `id`、`href` / `xlink:href` 与 `url()` 引用；保留三个 placement、视觉、语言目录、菜单和 Theme scope/edit/publish 语义。
- 回归门禁：单测显式重置旧序号边界后两次输出仍必须拥有不同的 24-hex 命名空间；最终以纯 WLS HTTPS、现代 HTML5 DOM 三轮审计和内置 Browser 三处菜单交互验收。
- 交付地址：https://p05113ef3.test.weline.com:9555/ 、https://p05113ef3.test.weline.com:9555/best-sellers 、https://p05113ef3.test.weline.com:9555/ar_SA 、https://p05113ef3.test.weline.com:9555/cart

## Hotfix — 2026-09-03：跨独立 Hook 上下文的切换器 DOM 命名空间（1.0.13）

- 当前状态：`implemented`；WLS DOM 与内置 Browser 操作验收待升级后执行。
- 二次运行证据：清理模板缓存、预热 FPC 并滚动重载 4 个 Worker 后，header/footer 仍各自从局部序号 `1` 开始；`best-sellers` 与 `ar_SA` 的语言前缀也在两个独立渲染上下文中相撞，排除旧缓存残留。
- 根因校正：同一响应里的 Theme placement 可由彼此独立的 Hook/Template 上下文渲染，请求级或可重置的进程计数器不构成页面级实例身份。
- 修复：语言与货币切换器统一使用每次服务端 render 的 96-bit 随机命名空间；同一实例的 trigger/panel/dialog 继续共享该命名空间，Hook HTML 缓存仍关闭，三处 placement、视觉及 Theme scope/edit/publish 语义不变。
- 回归门禁：独立调用生成器必须得到不同且格式有效的 id；四条真实路由以 DOM 节点审计重复 id 与每个 `aria-controls` 的一对一目标；最后补内置 Browser 三处菜单交互。

## Hotfix — 2026-09-02：同页多实例语言/货币切换器 DOM ID 隔离（1.0.12）

- 当前状态：`implemented`；真实 Browser 操作验收待本机解锁后补齐。
- 运行复现：`/`、`/best-sellers`、`/ar_SA`、`/cart` 的真实 WLS HTML 中，页头/侧栏/页脚三个货币控件共用 `w-currency-switcher-menu`；部分页面的页头/页脚语言控件也复用整套 id。
- 根因：`TemplateCachePolicyProvider` 将带实例 DOM id 的语言/货币 Hook aggregate 与输出 HTML 标成可缓存，Framework 随后复用完整片段；货币模板还使用固定 panel id。
- 修复：动态切换器 Hook 不再缓存已渲染 HTML；`LanguageSwitcher` 保留每 render 序号；货币模板使用 `RequestContext` 请求内序号，trigger `aria-controls` 与本实例 panel `id` 成对输出。三个入口、数据、导航与视觉不变。
- 定向门禁：`HeaderLanguageSwitcherTemplateTest`、`HeaderCurrencySwitcherTemplateTest`、`LanguageInjectLocalesContractTest`、PHP lint、`setup:upgrade -m Weline_I18n`、WLS DOM duplicate-id probe。
- 交付地址：https://p05113ef3.test.weline.com:9555/ 、https://p05113ef3.test.weline.com:9555/best-sellers 、https://p05113ef3.test.weline.com:9555/ar_SA 、https://p05113ef3.test.weline.com:9555/cart

## Hotfix — 2026-09-01：语言切换器长列表滚动与申请入口（1.0.11）

- 当前状态：`implemented`
- 问题：语言较多时弹出层无法下滑，底部「申请语言」不可达
- 修复：`LanguageSwitcher` 输出 `search / list / footer` 分区；HTML 缓存戳 `component-22`；列表独立 `overflow:auto`，申请钉在底栏
- 协同：Theme `weline-ui` 浮层忽略面板内 scroll（见 Theme 开发日志 2.2.68）
- 宿主原生编辑。

## Hotfix — 2026-09-01：语言切换器内联国旗 SVG 重复 id（1.0.10）

- 当前状态：`implemented`
- 问题：主题编辑器页脚健康检测「部件内重复 id: flag-icons-cn×2, cn-a×2」
- 根因：同一国旗 SVG（含 `id`/`xlink:href`）在切换器触发按钮与菜单项各内联一次
- 修复：`InlineSvgIdUniquifier` 在 `LanguageSwitcher::sanitizeInlineFlagMarkup` 每次输出前后缀化 id 并改写引用；HTML 缓存戳 `component-21`
- 验收：`InlineSvgIdUniquifierTest`
- 宿主原生编辑。

## 2026-08-30 — 前台 JS 改 modules + data-weline-load（第二波）

- 当前状态：`implemented`
- 变更：登记 `view/statics/frontend/weline.modules.js`；部件/布局改 `data-weline-load`，删除 `@static`/`<js>` 直引。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。

<!-- weline:module-development-baseline:generated-once -->
# Weline_I18n 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## Hotfix — 2026-08-30：乱码 CSV 行直接丢弃不写回

- **策略**：键或译文含 UTF-8 BOM / U+FEFF / U+FFFD 的行视为乱码，**只丢该条**，绝不因单条乱码丢掉整批翻译/整文件写回；不做 strip 挽救；缺词留给下次 AI 翻译。
- **实现**：`I18nCsvCodec::isGarbledText` + `readWords`/`writeWords`/`sanitizeFile`；文件头仅跳过 1 个 BOM。模块 `1.0.8`。

## Hotfix — 2026-08-30：AI 写回 CSV 键前出现 U+FFFD / BOM 堆积

- **现象**：`Weline_Acl` 等模块 `i18n/*.csv` 词条前出现 ``，俄文等译文本身正常；`无权限访问重定向前` 等同键重复十余行且 BOM 递增。
- **根因**：写 CSV 时每次文件头追加 UTF-8 BOM，读侧（`AiTranslationExportService` / `DictionaryModuleCatalogService`）未剥离，同步源语言 CSV 时把 BOM 吃进第一列键；写回 `append` 把「BOM+源串」当成新键。
- **修复**：新增 `I18nCsvCodec` 统一 UTF-8 读写（读剥光连续 BOM、写最多 1 个文件头 BOM、键正规化去重）；写回改为 merge 后整文件重写；清理 `Weline_Acl/i18n` 已污染文件；单测覆盖多 BOM 往返。模块版本 `1.0.7`。

## Hotfix — 2026-08-30：模块列表状态展示待处理语言/词数

- **诉求**：模块行状态不只显示「有待办」，还要看到还有多少语言、多少词未处理。
- **实现**：`summarizeModuleWorkOverview`（CSV 对照源语言）；列表状态 `待处理 N/M 种语言 · K 词`；展开矩阵后回写刷新该行。脚本 `20260830-module-status1`。

## Hotfix — 2026-08-30：SSE 断线自动重连

- **诉求**：语言矩阵 / 一键写回 SSE 容易断流，断线后应自动重连。
- **实现**：前端 XHR SSE 对意外断开（onerror、异常结束、5xx）指数退避重连（最多 8 次）；用户取消与 401/403 不重连。脚本 `20260830-sse-reconnect1`。

## Hotfix — 2026-08-30：展开矩阵 remember 恢复后仍掉线（同族 Session Cookie）

- **根因**：矩阵 SSE remember 恢复 regenerate 只刷新 `WELINE_SESSID_{port}`，遗留 `_w0` 旧 jar → `session_rebound`。
- **修复**：Framework `WlsStrategy::setCookie` 过期同族 Session Cookie；脚本 `20260830-sess-sibling1`。

## Hotfix — 2026-08-30：一点击写回就「未登录」

- **根因**：WLS `SseWriter::start()` 漏发 Session/remember 的 `Set-Cookie`；矩阵 SSE remember 恢复的新会话到不了浏览器，写回仍用旧 Cookie → 掉线。
- **修复**：Framework SSE 首包携带 pending cookies；401 不再合并空 Session Cookie。需 `server:reload`。

## Hotfix — 2026-08-30：写回流水线三步 + 禁止「0/0 已完成」

- **现象**：进度弹窗语言行显示「已完成」但翻译/写回均为 0；流水线未先写回源语言计数、未补 CSV 差额、无对齐校验。
- **约定**：① 收集源语言并写回 CSV，推送词数；② 每语言先补词典→CSV 差额，再 AI 翻译并写回；③ 校验相对源语言差额为 0。
- **实现**：`exportModuleCsvGaps`；`runTranslateAndWriteback` 增加 `source`/`verify` 与 `status=completed|aligned|failed`；进度节点 collect→translate→writeback→verify→finish；脚本 `20260830-wb-verify1`。

## Hotfix — 2026-08-30：展开语言进度误清登录态

- **现象**：刚登录（含信任设备）后展开模块，提示「未登录或登录已过期」，随后页面也掉线。
- **根因**：矩阵用原生 `EventSource`；HTTPS 非标端口 Session 为 `SameSite=None; Partitioned`，EventSource 常不带 Cookie，服务端新建空 Session 写回 Cookie / 触发 `session_rebound` 清认证。
- **修复**：矩阵 SSE 改与写回相同的 `XMLHttpRequest` + `withCredentials` + `Accept: text/event-stream`，脚本版本 `20260830-matrix-xhr-sse1`。

## Hotfix — 2026-08-30：语言进度矩阵对齐源语言词数

- **诉求**：模块顶部显示源语言词数；每语言展示词数 / 已译 / 未译 / 相对源语言差额；展开先采集并回写源语言 CSV，再刷新差额；一键翻译仍逐语言进度。
- **实现**：`countModuleSourceWords` + `summarizeModuleLocaleAlignment`；矩阵 SSE 先 `syncModuleCollectedWords` 再推 `source`/`locale`；前端源语言横幅与新列；契约测试对齐。

## Hotfix — 2026-08-30：写回 SSE 进度弹窗 + 源语言只收集一次

- **约定**：模板词只收集并回写源语言（中文）CSV；目标语言对照源语言翻译后写回各自 CSV，循环内不再扫描。
- **UI**：Theme `dialog` 进度弹窗（节点条 collect→translate→writeback→finish + `w-progress` + 语言表 + 事件流）；行内「查看进度」chip 可重开；确认后跟播打开。
- **后端**：`syncModuleCollectedWords` 不再给其它语言灌占位；`translateModule(..., $syncFirst=false)`；SSE 事件带 `node`；观察者回退到配置源语言。

## Hotfix — 2026-08-29：确认框点「确定」不关且写回不启动

- **现象**：展开模块后点「一键翻译并写回」→ 确认框「确定」后对话框仍在 / 或关掉了但写回不跑。
- **根因**：① Theme `requestDialog` 的 `UI.dialog.close` 失败时 Promise 挂死；② 写回确认依赖 MutationObserver 绑按钮，`showModal` 只改 `open` 属性时绑不上；③ `confirming=1` 卡住后挡住后续点击；④ WLS 静态缓存 / 编译 tpl 导致浏览器长期跑旧 `backend-admin.js`。
- **修复**：写回确认在 `document` 捕获阶段监听确定/取消并立即 resolve；无框时回收卡住的 `confirming`；脚本改 `filemtime` 版本号；Theme close 失败强制收尾。验收：确定后框关、`asyncBusy=1`、按钮「处理中…」、XHR 打到 `module-translate-and-writeback`。

## Hotfix — 2026-08-29：一键翻译并写回改为 SSE 阶段进度

- **诉求**：按钮只显示「处理中…」，看不到翻译/写回阶段、当前语言、批次与剩余。
- **实现**：`streamTranslateAndWriteback` 推送 `start` / `progress`（`stage=sync|translate|writeback`，含 locale/round/batch/remaining）/ `locale_done` / `done`；前端对已有 POST 路由用 `XMLHttpRequest` + `Accept: text/event-stream` 流式解析（避开未注册 GET 路由 404 与 bin-query）；hint/按钮/行状态实时更新。脚本 `?v=20260829-wb-sse5`（修复确认框点「确定」不关闭、写回未启动；Theme `requestDialog` close 失败时强制收尾）。

## Hotfix — 2026-08-29：写回按钮仍 403（改走 HTTP 直连）

- **现象**：强制刷新后点「一键翻译并写回」仍 toast「不满足操作授权要求」，控制台 `BinQuery … 403`。
- **根因**：模块写回仍先打 Worker bin-query；鉴权失败时回退逻辑可能未随静态缓存加载到浏览器；即便改 HTTP，`window.fetch` 仍可能被 Worker bridge 劫持成 Failed to fetch。
- **修复**：`data-ai-module-writeback-form` **优先**用 `XMLHttpRequest` POST 到表单 `action`（后台 Cookie）；脚本 `?v=20260829-wb-xhr2` 强刷。

## Hotfix — 2026-08-29：一键写回误判非开发环境 / 表单 CSRF 干扰

- **现象**：点「一键翻译并写回」可能先过 ACL 后报「写回模块 CSV 仅允许在开发环境」，或 toast「不满足操作授权要求」。
- **根因**：`isDevEnvironment()` 只看 `dev.mode`/`wls.debug`，本机 `deploy=dev` 且 `DEV=true` 仍判为 false；表单 `csrf` 被塞进 bin-query payload 无必要；Worker bin-query 鉴权偶发失效时页面仍有后台 Cookie。
- **修复**：开发环境判定对齐 `DEV` 常量与 `Env::system('deploy')`；`formPayload` 忽略 csrf；bin-query 鉴权失败时回退 HTTP POST 到表单 `action`（同 SSE 会话 Cookie）。

## Hotfix — 2026-08-29：模块「一键翻译并写回」写回改为增量

- **说明**：翻译本身已是增量（只取未译词 + 分批）。写回原先会拉该模块全部 AI 行再跳过 CSV 已有词，与「待写回」口径不一致且偏重。
- **修复**：`exportModuleTranslations(..., aiOnly=true)` 只查 `IS_AI=1 AND exported_at IS NULL`；CSV 已有译文时仍 `markExported` 清零待写回；跳过源 CSV 全量回填扫描。

## Hotfix — 2026-08-29：SSE progress 后永久卡住（Fiber::suspend）

- **现象**：已推到「正在统计 英语（美国）（1/12）」，但永远收不到 `locale`。
- **根因**：`countUntranslated` 仍走 `collectLazy`；`TranslationCollector::yieldFiberCheckpoint` 裸调 `Fiber::suspend()`，在 WLS SSE Fiber 内不会被 `SchedulerSystem` 唤醒。
- **修复**：checkpoint 改 `SchedulerSystem::yield()`；SSE 快路径（`includeDb=false`）跳过 `scanModuleCollectedWords`，只比 CSV。

## Hotfix — 2026-08-29：SSE 已连通但仍卡在 0/12

- **现象**：`start` 已到（提示 0/12），但首语言 `countUntranslatedModuleWords` 的 DB IN 在连接池压力下卡住；EventSource 重连加重占坑。
- **说明**：列表「AI 待写回 1781」是跨语言 AI 待导出条数合计，不是模块词条数（`Weline_Acl` 源 CSV 约 354）。
- **修复**：SSE 待译改 CSV/收集词快路径（不跑大 IN）；推送 `progress`；前端 10s 无 `locale` 看门狗；增大 SSE retry。

## Hotfix — 2026-08-29：语言矩阵改为 SSE 实时推送

- **诉求**：`module-locale-matrix` 整包 JSON 易卡在「正在加载」；要按语言实时出表。
- **实现**：`Accept: text/event-stream` 或 `?sse=1` 时 `streamModuleLocaleMatrix` 推送 `start` / `locale` / `done`；前端 `EventSource` 逐行渲染，收起关闭流。普通 JSON 仍保留给 bin-query。

## Hotfix — 2026-08-29：按模块语言进度仍卡住（buildLocaleRows）

- **根因**：`getModuleLocaleMatrix` 仅为补显示名调用了 `buildLocaleRows`（全语言 COUNT / 全量待译 / 队列 / export 扫描）。RouterPerf `action_execute_ms` 可达数十秒，前端表现为无限「正在加载」。
- **修复**：矩阵接口只做 `getLocaleName` 轻量取名；前端优先 HTTP GET，失败再短超时 bin-query。

## Hotfix — 2026-08-29：按模块语言矩阵加载卡住

- **根因**：展开矩阵会 `syncModuleCollectedWords` 写 CSV；多请求并发时写盘阻塞，前端 fetch/bin-query 一直挂起；`data-loaded=loading` 也无法重试。
- **修复**：矩阵改为只读统计（收集补词仍在「一键翻译并写回」时落盘）；前端 bin-query 15s / fetch 12s 硬超时；收起可重置 loading；URL 可从 pathname 推导。

## Hotfix — 2026-08-29：按模块展开语言区重设计

- **问题**：语言旁 `…` 徽章像菜单/省略操作，语义不清；矩阵异步还因 ACL 未登记报「不满足操作授权要求」。
- **设计**：取消芯片+省略号占位。展开后为独立「语言进度」面板：加载中显示表头+明确文案，成功后表格列=勾选/语言/待译/待写回/状态；有待办默认勾选；「一键翻译并写回」移入面板；摘要「有待办 N / 共 M」。
- **静态**：去掉错误的双重 `?v=`；矩阵加载优先 bin-query，失败时回退 GET `ai-translation/module-locale-matrix`。
- **授权**：`ACTION_ACL_SOURCES` 补齐 `ai-module-matrix` / `ai-module-translate-writeback` → `Weline_I18n::i18n_ai_translation`；需 `framework:compile` 刷新 QueryProvider 索引。

## Hotfix — 2026-08-29：按模块「展开」无响应

- **根因**：`backend-admin.js` 在模板顶部同步执行时，`[data-ai-module-workspace]` 尚未入 DOM，`bindAiModuleWorkspace` 直接 return，点击监听未挂上。
- **修复**：`DOMContentLoaded` 后再绑定；脚本改到页面底部；防重复绑定。

## Hotfix — 2026-08-29：按模块支持模板收集词补进源 CSV

- **诉求**：模块维度不要只处理已有 CSV；要实时读 CSV，并把模板/`__()` 收集到的本模块词补进源语言（中文）CSV。
- **实现**：`DictionaryModuleCatalogService::syncModuleCollectedWords()` 用 `TranslationCollector` 扫描；开发环境写回源/目标 CSV 缺键；展开矩阵与一键翻译写回前都会同步。

## Hotfix — 2026-08-29：AI 翻译「按模块」从系统读取模块列表

- **现象**：`?tab=modules` 默认空表，必须先搜索才有行。
- **修复**：打开 Tab 即从 `Env::getActiveModules()` 列出带 `i18n/` 的活跃模块；待写回数量一次 GROUP BY 聚合；搜索仅作过滤。

## Hotfix — 2026-08-29：AI 翻译页「按模块」一键翻译并写回

- **诉求**：开发补模块翻译时，要快速只翻译某个模块并写回该模块 CSV，而不是按语言全站跑。
- **实现**：AI 翻译页增加「按语言 / 按模块」Tab；`AiTranslationModuleWorkspaceService` 提供模块列表、语言矩阵、`translateAndWriteback`；Query 动作 `ai-module-matrix` / `ai-module-translate-writeback`；写回仍限开发环境。
- **用法**：打开按模块 → 搜索如 `Weline_Product` →（可选）展开勾选语言 →「一键翻译并写回」。

## Hotfix — 2026-08-29：AI 翻译页队列错误与导出选模块

- **现象**：最近队列只显示 `error` 徽章，看不到失败原因；「导出 AI 到模块」在语言行直接提交，用户不知道写到哪些模块。
- **修复**：展示 `queue_result` 摘要/`details` 全文；导出改为对话框，可选全部或指定来源模块；成功消息列出涉及模块。

## Hotfix — 2026-08-29：本地化页统计瓷片过大

- **现象**：`/i18n/backend/localization` 顶部 5 个统计卡各占半宽，留白过多。
- **根因**：`--w-span:6` 固定两列；紧凑 padding 误绑 `.card-body`（实际为 `.w-card__body`）。
- **修复**：改用 `w-stat-tiles`（`--w-stat-tile-min:7.5rem`）；CSS 改绑 `.w-card__body` 并缩小字号/内边距。

## Hotfix — 2026-08-29：AI 翻译页 Unbounded SELECT

- **现象**：打开 `/i18n/backend/ai-translation` 抛 `DbException: Unbounded SELECT detected`（阈值 10000）。
- **根因**：`getLocaleTranslatedWordIndex()` 对 `w_i18n_locale_dictionary` 无界 `fetchArray`；源语言也跑 `countUntranslatedWords`。
- **修复**：索引改为 `limit` 分页；源语言 pending=0；契约测试覆盖。

## 2026-08-29 — getCurrentLang 路径优先 + Query 回落

- 当前状态：`fixed`
- `i18n.js`：`getCurrentLang` 顺序改为 **path → query → data-lang/config**；仍只清不写 `WELINE_USER_LANG*`；切换器保持路径导航。

## 2026-08-28 — 语言 Cookie 移除（路径识别）

- 当前状态：`fixed`
- `i18n.js` / `language-switcher.js`：切换语言只导航路径并过期清除 `WELINE_USER_LANG*`，不再写入 Cookie / `weline_user_lang` localStorage；`getCurrentLang` 不再读 Cookie。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.6`（文档基线） | docs_handoff | `REQ-I18N-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.0.6` — 文档制度启用基线

- 基线版本：`1.0.6`
- 目标版本：`1.0.6`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-I18N-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-I18N-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-25 | implementing | implementing → implementing | 国家列表翻页呈竖排项目符号：`getPagination()` 仍输出 Bootstrap `pagination/page-item`，后台只加载 `weline-foundation` | 框架分页 HTML 改 `w-pagination`；I18n CSS 选择器同步；foundation 补 wrap/disabled | Composer | `AbstractModel::getPaginationData`、`backend-admin.css`、`foundation.css` |
| 2026-08-25 | implementing | docs_handoff → implementing | 主题预览 iframe 无路径语言段时 Cookie 压过 `html[data-lang]`，切换器钉死站外语言 | `getCurrentLang` 优先 document/query locale，再 Cookie；契约测试补齐 | Composer | `view/statics/js/i18n.js`、`I18nJsAuthoritativeHrefBehaviorTest.js` |
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-I18N-0001` / `1.0.6` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。

## 2026-08-28 — Local AI 翻译确认关掉抽屉

- 现象：点「AI 翻译」后面板收起。
- 根因：已有译文时用 `window.confirm` 二次确认；原生确认框关闭后点击穿透到 `.w-overlay`，触发抽屉 `close('backdrop')`。
- 修复：改为 `Weline.UI.dialog.confirm` / `BackendConfirm`；AI 点击 `stopPropagation`；禁止回退到 `window.confirm`。
- 契约：`LocalTranslationNativePanelContractTest` 断言无 `window.confirm`。

## 2026-08-28 — Local 多语言抽屉暗色发黑（半透明 surface / lg 铺满）

- 现象：后台分类等页打开 `<local>` 抽屉，暗色主题下整块发黑，像被遮罩盖住。
- 根因：① 暗色 `--weline-theme-surface` 半透明透出遮罩；② `data-size="lg"` 接近全屏 + 重黑 overlay，观感像整屏黑屏。
- 修复：面板改不透明 raised；去掉 `data-size="lg"` 改为默认侧栏宽；弱化 `[data-w-drawer-backdrop]`；暗色抬升背景；CSS `?v=20260828-local-side1`。
- 契约：`Theme/test/Unit/View/LocalTranslationNativePanelContractTest.php`。

## 2026-08-28 — `<local>` 多语言抽屉按站点关联语言收敛

- 需求：多语言标签不要读取全部已安装语言，只展示/翻译当前站点支持的语言。
- 改动：`TaglibLocalFormService` 通过 `w_query('websites','getWebsiteLanguageCodes')` 解析当前站点关联语言，与已安装激活 Locale 求交集；`buildFormPayload` / `aiTranslate` / `isFieldFullyTranslated` 共用该边界；站点无关联语言时禁止回退全量目录。
- 契约：`TaglibLocalFormServiceContractTest`、`TaglibLocalFormServiceBulkTranslateTest`。
- 文档：`local标签与LocalModel指南.md`、`README.md`。

## 2026-08-28 — LanguageSelect 防 multiple 泄漏

- 现象：网站编辑页默认语言选择器出现可移除标签（多选 UI），保存后回到 `en_US`。
- 根因：同模板前序 Taglib 写入的 `Taglib__multiple` 泄漏；`LanguageSelect` 在属性未声明时仍读取该变量。
- 修复：`AttributeCodeCompiler` 每次编译先清理 `Taglib__*`；`LanguageSelect` 在未声明 `multiple` 时强制 `$Taglib__multiple = false`。
- 契约：`LanguageSelectFieldNameLiteralContractTest`。

## 2026-08-27 — 多语言抽屉跨 iframe 保存

- 现象：父页点「保存」报 `Translation form is unavailable.`，iframe 内表单可见。
- 根因：父窗口对 iframe 内节点使用 `instanceof HTMLFormElement`（跨 realm 恒为 false）；且 `pub/static` 仍在提供旧 JS。
- 修复：`weline-local-translation.js` 改为 `tagName` 识别；表单增加 `data-w-local-form`；同步源文件与 `pub/static` 发布副本。
- 保存 500：`description[locale][field]` 扁平 payload 未还原为数组，导致空 insert；`I18nAdminQueryProvider::normalizeActionPayload()` + JS `assignNestedFormValue()` 修复。
- 保存 500（EAV）：表单 hidden 可能带 `set_id`/`group_id` 等父级别名，LocalDescription 表实际列名为 `id`；`TaglibLocalDescriptionNormalizer` 入库前 remap + 过滤未知列。
- 保存 500（主键）：`w_eav_*_local_description` 误用单列主键 `(id)`，无法按 locale upsert；模型补 composite PK + migration 改为 `(id, local_code)`。
- 保存 500（bin-query）：`taglib-local-save` 不应走 i18n 词典资源变更发布；`invokeController` 改走 `AdminControllerBridge` 以正确识别 JSON 异步请求。
- 契约：`Theme/test/Unit/View/LocalTranslationCrossIframeContractTest.php`、`I18n/test/Unit/Query/I18nAdminQueryProviderPayloadContractTest.php`、`I18n/test/Unit/Service/TaglibLocalDescriptionNormalizerTest.php`。

## Hotfix · 2026-08-28 · 分类页 AI 翻译关闭抽屉（页面 HTML）

- **页面 HTML**：`<local>` 抽屉嵌在 `data-catalog-form` 内，表单语义与遮罩叠层互相干扰。
- **关闭链**：`UI.dialog.confirm` 使用原生 `showModal`，关闭后合成 `click` 落到抽屉 `.w-overlay`，触发 `close(backdrop)`。
- **修复**：抽屉若在 `form` 内则 portal 到 `body`；遮罩改 `pointerdown` + `topOverlay` 守卫；确认期间 `data-w-backdrop=static`。

## Hotfix · 2026-08-28 · 多语言 AI 按钮双 loading

- **原因**：`aria-busy` 触发 foundation `.w-button::after` spinner，同时按钮内还有 `data-w-local-ai-spinner`。
- **修复**：去掉内嵌 spinner，仅保留官方 `aria-busy` 指示器。

## Hotfix · 2026-08-28 · 多语言抽屉英文 CSV

- **现象**：后台切到英文后，Local 多语言抽屉仍显示「原文 / 译文 / AI翻译」等中文。
- **原因**：`en_US_total` 后写覆盖；`Weline_Eav` / `Meta` / `Promotion` 等同词条未译，盖掉了 `Weline_I18n` 的英文。
- **修复**：补齐相关模块 `en_US.csv`，标题去掉 `__()` + `<lang>` 双层包装。

## 2026-08-28 Local 抽屉 en_US CSV

- 修复 Catalog/Product/Meta/Websites/Mail/Search 等模块 `en_US.csv` 中与 Local 抽屉重叠且仍等于中文原文的词条（如「多语言翻译」「刷新」「语言」），避免英文后台仍回退中文。
- 补全 I18n Local 批译相关词条英文（一键 AI 翻译等）。
- Eav 增加「多语言翻译」英文，保证分类/EAV 页面模块层可命中。

## 2026-08-28 Local 触发图标主色与间距

- 现象：规则名旁「文A」语言图标与标题同色、贴得过近，不易识别可点。
- 修复：`local-translation.css` 中 `.w-local-translation__trigger` 图标使用自研主题主色（`--backend-theme-primary` / `--weline-theme-primary`），并加大 `gap` + `margin-inline-start`；缓存版本 `trigger-primary1`。
- 契约：`Theme/test/Unit/View/LocalTranslationNativePanelContractTest.php`。

## 2026-08-28 Local 母本语言星标与空母本反哺

- 指定母本：网站 `default_language`（面板行标 ★）。
- 有效翻译源：母本有内容用母本；母本为空则取站点关联语言中第一个已填语言，反哺其它语言（含空母本）。
- 母本≠有效源时，原文区提示「母本为空，当前以 {locale} 为翻译源」。

## 2026-09-13 — 语言回退快照与 FPC 回执对齐（1.0.50）

- Framework 的不可变 Storefront 缓存上下文统一携带主语言、网站默认语言和有序翻译回退链；普通键、FPC 键及命名空间版本使用同一快照。默认语言改变会重新冻结；同请求既有版本不因后台提交而改变。
- FPC 首页回执保存语言证据，跨请求核验按回执原语言链读版本，不读取后续请求的语言。WLS READY/IPC 规范化保留这些字段；缺少语言证据的旧回执交 FPC 回落，保留原身份、事务及完整性校验。显式 URL 货币继续走已解析货币规则。
- I18n 整本词库保留无参 DictionaryCacheNamespace 的 content 聚合失效；语言名称、安装状态和旗帜目录的共享池明确保持 global/i18n 根范围。LanguageSelect 含翻译的国家分组信息按实际语言链缓存；LanguageSwitcher 本地 CSV 按其 locale、HTML 按显示语言与当前回退链依赖版本。
- 最小 RED：Context/FPC 缺少快照 2 tests / 3 assertions / 2 failures；当前语言更新后 chrome 键不变 1 test / 4 assertions / 1 failure；WLS 规范化删掉语言证据 1 test / 5 assertions / 1 failure。正式 PHPUnit bootstrap 加候选源验证 Context、FPC、I18n namespace 原有及新增用例合计 36 tests / 267 assertions PASS。
- 本次 Model 调整依 MCP 模块规范与版本 1.0.49 → 1.0.50 一起交付。官方 setup:upgrade、compile 和真实多 Worker / 多语言验收由主任务统一执行，尚不以单测代替运行验收。

## 2026-09-13 — 可信译文写入按语言发布范围失效

- `I18nResourceChangePublisher::publishAction($action, $payload, ?string $persistedLocale = null)` 新增显式实际持久化目标；只由 `Locale/Dictionary::upsert/deleteEntry`、`AiTranslationService::saveTranslation` 和 `publishLocaleFile` 提供。服务 CSV 导入经 saveTranslation 复用同一链路；不从后台原始 HTTP payload、md5 或混合导入猜测语言。
- 已知有效 locale 发布 `global/i18n/content` 与该 locale 叶子：前者供整本词库和旧无参 reader，后者供明确语言 reader；未知、空语言、旧入口、国家/语言目录和全局清理保留 `global/i18n` 父级失效。Observer 消费同一 impact 合同，旧事件或不完整范围保守回退父级。
- Observer 使用 Framework 既有 namespace 仓库的事务完整集合通知；同事务更新 en_US/fr_FR 并重复 en_US，提交后仅一次广播完整 content/en_US/fr_FR 变更，回滚不发布。IPC 最终 Worker applier 只应用版本 delta，没有 Parser 或业务缓存池全清。
- 记录写入口边界：后台 Query 包装只覆盖 dictionary-add/edit/quick-save；后台 md5 删除与上传 CSV 入口当前未见本显式 changed 链，不把 Publisher 能识别的 action 当成已接入入口。本补丁不扩大这些旧入口的行为。
- 最小新增测试 `test/Unit/Service/I18nLocaleNamespaceChangeTest.php`（隔离 SQLite fixture）在原实现获得 11 tests / 54 assertions、9 个范围合同 RED；与事务回调、旧写入、广播、原子文件发布和语言生命周期组合，正式 `app/bootstrap_phpunit.php` 引导 **38 tests / 266 assertions PASS**。fixture 仅替代不在本次范围的 revision 分配及外部 IPC，实际 publisher / observer / namespace repository / transaction / dictionary 写入均运行。
- MCP 模型注册规则要求同计划将模块版本 `1.0.48 → 1.0.49`。本节仅记录源码与定向验证，官方升级、编译与真实跨 Worker 缓存验收由主任务统一执行后补记。


## 2026-09-13 — 国家/语言重复激活不再无变化失效目录

- 根因：`activateLocale`、`activateCountry` 在已安装且启用、关联完整时仍执行保存调用、无条件更新 `Locals` 镜像并发布 `locale-catalog-changed`。真实原方法的隔离 SQLite 用例中，每次完整语言激活实际更新两条镜像，并提交一次目录事件。
- 修复：沿原 `TransactionCoordinator` 保留完整补全流程，用操作局部变化标记归集持久化结果；仅实际数据或关联变化后发布一次原 changed。国家内部激活语言共用同一事务变化标记；完整输入不再调用状态保存或镜像更新，不新增缓存。状态按数值比较以兼容 PDO 字符串。名称缺行或空值、ISO/国旗/国家关联与镜像补全均保留。
- 验证：原实现最小 RED 为 3 例、38 断言、2 失败；正式源码与既有安装合同组合以 `app/bootstrap_phpunit.php` 引导 **8 tests / 644 assertions PASS**。真实模型/SQLite/事务用例覆盖两入口、重复激活、数值字符串、补全后再次无操作、外层回滚及 changed 发布失败。实际保存/更新调用计数隔离探针由语言 4 次、国家 3 次降到 0；它只是调用层开发证据。
- 主任务真实验收：首次 `activateLocale(zh_Hans_CN)` 补齐现库缺失的 en/en_US 国家和语言名称，目录根代次与修订各增加一次；首次 `activateCountry(CN)` 补齐该国家其他目录资料，各自第二次调用均不再推进目录根代次。随后对含 PostgreSQL `xmin` 的全 CN 目录快照（7 个 locale、21 条 locale 名称等）连续执行语言两次、国家两次，四次均无行版本或目录修订变化，`global/i18n = 49495`、目录 revision `8814` 及相关语言叶子均保持不变；耗时分别 50.12/18.37/79.25/44.75 ms。证据：`/tmp/weline-sept13-catalog-noop-results.json`。后台译文可能独立推进 `global/i18n/content` 和 `@clock`，本验收不声明它们全局静止。
- 边界：采样发现目录资源修订与 `global/i18n` 父代次同步增长，但实际运行调用者仍未知；本次只修复已复现的无变化发布，不把该相关性认定为所有冷请求尖峰的唯一原因。无字段/Schema/模块版本变化，未操作共享服务。

## 2026-09-17 — 顶栏国旗闪一下消失（legacy sync 擦 SSR）

- 现象：语言切换器 trigger 国旗首屏 SSR 可见，随即空白；文案 `EN` 仍在。
- 根因：`Framework/View/statics/js/i18n.js`（及遗留 `Theme/.../theme.js`）的 `updateCurrentLanguageDisplay` 无条件 `currentFlag.innerHTML = optionFlag.innerHTML`；面板 option 仍为空占位，把已 SSR 的 trigger `<img>` 擦掉。
- 修复：仅当 option 已绘出国旗（`img.w-flag-icon`/`svg`）时才复制 innerHTML；否则保留 trigger。契约：`LanguageSwitcherClickRaceContractTest` 断言 `optionPainted` / `Never wipe a painted`。
- 验收：`phpunit` 4 tests / 82 assertions PASS；Browser 禁缓存打开 `https://p05113ef3.test.weline.com:9555/en_US`，主动调用 `Weline.i18n.updateCurrentLanguageDisplay()` 后 trigger 仍有 US 国旗（28×28）。
