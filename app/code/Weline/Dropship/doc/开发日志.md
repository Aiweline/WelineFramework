# 开发日志 · Weline_Dropship

## 2026-09-13 — 运费失败策略改供应商自有配置（1.0.86）

- 壳：`DropshipFreightPolicy` + Provider `freightOnFailure()`；失败时按配置 `fallback_local` / `block_checkout`。
- CJ：`dropship/channel/cj/freight_on_failure` 下拉（默认回退本站）。
- 验：UT fallback/block 双路径；Cj schema 契约。
- 版本：`1.0.85` → `1.0.86`。

## 2026-09-14 — 货源平台能力芯片中文别名（1.0.87）

- 改：`Channel/index` `$capLabels` 补齐 browse / webhook_* / warehouse；徽章 `title` 保留原始 key。
- 审图：能力列英文 snake_case 不直观 → 中文别名。
- 验：`ChannelIndexTemplateContractTest`。
- 版本：`1.0.86` → `1.0.87`。

## 2026-09-13 — 运费失败策略改供应商配置（1.0.86）

- 增：`DropshipFreightPolicy` + Provider `freightOnFailure()`；壳按策略回退或阻断。
- CJ：配置 `dropship/channel/cj/freight_on_failure`（默认 `fallback_local`）。
- 验：UT fallback/block；CJ schema 契约。
- 版本：`1.0.85` → `1.0.86`。

## 2026-09-13 — 货源运费试算失败软降级（1.0.85）

- 改：CJ/货源试算失败不再卡结账；回退本站 Shipping，并记 `dropship_freight_degraded`。
- 验：UT `testOverlayAmountFallsBackToLocalWhenQuoteEmpty`；Checkout 契约去掉阻断文案。
- 版本：`1.0.84` → `1.0.85`。

## 2026-09-13 — 结账识别货源行介入运费试算（1.0.84）

- 增：`DropshipCheckoutFreightService`（listing.`offer_id` → freight Provider → Aggregator；换汇；拆包替换）。
- 挂：观察 `checkout::shipping_quote::overlay` / `shipping_methods::enrich`；试算失败 fail-closed。
- 修：`DropshipFreightAggregator` 按 Provider 取最低档合计（不再把多物流方案相加）。
- 验：UT `DropshipCheckoutFreightServiceTest`；契约 Observer/Checkout 事件；spec `checkout-dropship-freight-overlay.md`。
- 版本：`1.0.83` → `1.0.84`。

## 2026-09-12 — 发布 ensure 前分类 path 汉化 Interface

- 增：`DropshipCategoryPathLocalizerInterface`；`DropshipPublishService::resolveCategoryIds` 在 ensure 前若供应商实现该 Interface 则 `localizeCategoryPath(..., zh_Hans_CN)`。
- 解耦：壳不硬依赖 Cj；CjProvider 实现 Interface。
- 验：UT `DropshipCategoryPathLocalizerContractTest`；e2e `CK-DROPSHIP-CATEGORY-PATH-ZH-001/002`。

## 2026-09-12 — 标记 Webhook 壳能力与沙盒边界（1.0.82）

- 文档：功能现状 / dropship-shell / provider-development 增加「壳已接 / 沙盒真推 / 沙盒不可用」矩阵；纠纷沙盒不可用但 **保留** `webhook_dispute`。
- UT：`WebhookCapabilityMatrixContractTest`。
- 版本：`1.0.81` → `1.0.82`。

## 2026-09-12 — 自造沙盒单调试纠纷/退款通知（1.0.81）

- 加：探针 `cj-dispute-refund-notify-probe.php`——真实沙盒建单；沙盒不支持 `disputes/create` 时注入官方 `DISPUTES`/`MAKEUP`/`ORDER(REFUND_COMPLETE)` 到本机 notify。
- e2e：`CK-DROPSHIP-CJ-DISPUTE-REFUND-001` / PLAN-001。
- 版本：`1.0.80` → `1.0.81`。

## 2026-09-12 — 多 topic Webhook 壳处理（1.0.80）

- 扩：`parseWebhook` 标准 `topic`/`catalog`/`makeup`；capability `webhook_order|product|stock|logistics|makeup|private_order|dispute`。
- 壳：`processOne` 履约投影 + 目录跟随入队；空运单不覆盖；按 capability 跳过。
- CJ：官方 PRODUCT/VARIANT/STOCK/LOGISTIC/ORDER/ORDERSPLIT/MAKEUP/PRIVATE_ORDER/DISPUTES 映射。
- 验：契约 UT；e2e `CK-DROPSHIP-WEBHOOK-MULTI-TOPIC-001` / PLAN-001。
- 版本：`1.0.79` → `1.0.80`。

## 2026-09-12 — Webhook 空履约自愈投影（1.0.79）

- 修：`DropshipWebhookInboxService::processOne` 在 `fulfillment.external_order_id` 空时用信封 `raw_body`/`raw` 经 Provider `parseWebhook` 重解析并写履约；done 漏投影可自愈。
- 说明：万能壳 Fake/CJ 同 `DropshipWebhookProviderInterface`；PRODUCT/STOCK 只进 Inbox，不进履约投影。
- 验：契约 UT `testInboxServiceReparsesEmptyFulfillmentViaProviderRawBody`；本机 ORDER inbox 回填。
- 版本：`1.0.78` → `1.0.79`。

## 2026-09-12 — CJ 沙盒真实 ORDER webhook 通路（1.0.78）

- 探针改为真实 `createOrderV3(isSandbox)` + sandboxAdvance，等待 CJ 官方 `ORDER` 推送到本机 inbox 再投影履约。
- e2e：`CK-DROPSHIP-CJ-ORDER-STATUS-HOOK-001` / PLAN-001。
- 版本：`1.0.77` → `1.0.78`。

## 2026-09-12 — CJ orderStatus Webhook 真通路实测（1.0.77）

- 加：探针 `cj-order-status-webhook-probe.php` + e2e `CK-DROPSHIP-CJ-ORDER-STATUS-HOOK-001`（HTTP notify→inbox→processOne→fulfillment）。
- 验：本机 POST `cj.sandbox.default` 返回 `ok`+inbox；履约投影 `SHIPPED`+运单。
- 版本：`1.0.76` → `1.0.77`。

## 2026-09-12 — 货源中继页写清何时开启与怎么用（1.0.76）

- 说明页补「何时开启 / 怎么用（万能货源）」；强调开关在支付 DevRelay。
- 契约 UT 锁定引导文案。

## 2026-09-12 — 货源 Webhook 复用支付 DevRelay（1.0.75）

- 加：`DropshipWebhookDevRelayBridge`——生产 `isOnlineRelayHost` 时把 inbox 以 `dropship:{id}` 写入支付 EventStore。
- 改：`Callback` 信封保留 `raw_body`/`headers`；Payment LocalConnect 对 module=dropship 重放到本机 `dropship/frontend/callback/notify`。
- 加：后台「Webhook 中继」说明页（链支付 DevRelay + CJ 生产回调 URL）。
- 验：契约 UT；e2e `dev-relay-hint`；推 `dev` + `ssh weline` `core:update -b dev`。
- 版本：`1.0.74` → `1.0.75`。

## 2026-09-12 — 售后异常空态审图美化（1.0.74）

- 审图 ui_shot：空态飘在暗底、说明与筛栏无表面、无下一步。
- 线稿：标题区 | 筛栏 | 空态/表 → 原型：对齐 Channel/Order `w-card`；空态 CTA→履约订单/货源配置。
- 改：AfterSale `w-card` 头说明+筛平台字段；空态区分筛选态文案；清除筛选。
- 验：契约 UT；e2e aftersale/plan-suite；Browser 禁缓存。
- 版本：`1.0.73` → `1.0.74`。

## 2026-09-12 — Order exist 幂等恢复（勿误退款）（1.0.73）

- 纠偏：CJ `Order exist` 表示远端已建单，不再进永久失败白名单/自动退款。
- 加：`maybeRecoverExistingFulfillment`——dead 且本地已有 `external_order_id` → `done` + compensation `recovered_existing`。
- 改：售后页优先回收；`refund_failed` 展示 `error_code`（如 items_unmapped）。
- 验：契约 UT；processOne/售后加载样例；e2e/Browser。
- 版本：`1.0.72` → `1.0.73`。

## 2026-09-12 — 售后 dead 补偿回填与中文化（1.0.72）

- 加：永久失败白名单含 `Order exist` / `do not duplicate create`（CJ 重复建单）。
- 加：`maybeBackfillTerminalCompensation`——历史 `dead` 无 compensation 时在售后页/再次 processOne 懒补派 `push_terminal`（退款幂等）。
- 改：售后表中文列头；状态「无法履约」等徽章；空补偿显示「待补偿」警示色，不再裸露 `dead`/`—`。
- 验：契约 UT；e2e aftersale/plan-suite；Browser 禁缓存。
- 版本：`1.0.71` → `1.0.72`。

## 2026-09-12 — 无法履约自动退款 + 安慰邮件（1.0.71）

- 宿主原生编辑。
- 加：跃迁 `dead` 单次 `Weline_Dropship::push_terminal` → Observer → `OrderRefundCoordinator` **provider 行级**退款（幂等 `dropship:auto-refund:{biz_key}`）；**禁止**派发 `order_refunded`。
- 加：退款 ok 后 SMTP 安慰邮件 channel `Weline_Dropship::fulfillment_consolation`（文案「已启动退款」）；邮件失败不回滚退款。
- 加：货源设置开关 `dropship/ops/auto_refund_on_push_fail`（默认开）；AfterSale 纳入 `dead` + compensation 列。
- 验：契约 UT（terminal / compensation / aftersale / config）；e2e switch / aftersale / plan-suite。
- 版本：`1.0.70` → `1.0.71`。

## 2026-09-12 — 已刊登本地商品状态反馈（1.0.70）

- 加：已刊登状态列双徽章——货源 `sync_status` + 本地商品 lifecycle（草稿/已发布/已下架/已归档），可见「能不能用」。
- 加：`enrichListedLocalMeta` 按 website 批量读 Product 分片 `status` → `local_product_status*`。
- 改：`DropshipListedLocalDetailService` / `listed-accordion.js` 输出中文 `status_label`（对齐目录编辑页）。
- 验：契约 UT；Accordion JS；e2e `listed-product-status`；Browser 禁缓存看 DS-CJ-CJYD3153518。
- 版本：`1.0.69` → `1.0.70`；`listed-accordion.js?v=1.0.70`。

## 2026-09-12 — 履约订单商品上图（1.0.69）

- 加：`Order::enrichLinesWithImages` 优先行内 image*，否则按 offer/sku 读 `DropshipListing.thumb_url`。
- 改：`order-accordion.js` 商品列 thumb+name；无图 `thumb-ph`；主题 Token 样式；导出 `renderLines`。
- 审图：ui_shot 缺图 → 图文并排；线稿/原型对齐已刊列表缩略图。
- 验：`OrderLineImageContractTest`；`OrderAccordionRenderTest.js`；e2e 断言 thumb；Browser 禁缓存。
- 版本：`1.0.68` → `1.0.69`。

## 2026-09-12 — 履约订单手风琴点击修复（1.0.68）

- 修：模板内联 `ensureOrderAccordion`（对齐已刊列表），绕过 `data-weline-load` 竞态/静态长缓存。
- 修：`order-accordion.js` 点击目标归一化 + capture 监听；展开时显式移除 `hidden`。
- 验：契约 UT；e2e/Browser 点击展开商品。
- 版本：`1.0.67` → `1.0.68`。

## 2026-09-12 — 履约订单行展开商品手风琴（1.0.67）

- 加：`Order::getLines` JSON；`order-accordion.js` + `dropshipOrderAccordion` 模块登记。
- 改：履约表行可点击展开，异步展示本站订单商品（名称/SKU/数量/单价/小计）；沙盒键降级读 OrderLine/Outbox。
- 验：契约 UT；e2e 展开；`resource:compile welineModules`。
- 版本：`1.0.66` → `1.0.67`。

## 2026-09-12 — 壳分类禁止直白货源顶层

- 改：`DropshipCategoryEnsureService` 不再创建「货源商城 / CJ货源」等顶层；只 ensure 真实类目段，靠 `source_platform` 标签识别。
- 改：ensure 时抬升历史 `/sourcing/{provider}` 子树到网站根并停用壳节点。
- 店面：面包屑过滤壳组织节点/货源品牌词；e2e 改为禁止出现。

## 2026-09-12 — 履约订单页人性化（1.0.66）

- 改：后台 `Order/index` 对齐 Channel 的 `w-card`/`w-table`/`w-badge`；中文列、状态语义色、本站订单号层次、承运人+运单、更新时间；Outbox 改称「推送队列」。
- 改：`Controller/Backend/Order` enrich 展示字段（平台标题、状态文案、OrderFacade 展示号）。
- 验：`OrderIndexTemplateContractTest`；Browser 审图；order-smoke e2e 断言中文列。
- 版本：`1.0.65` → `1.0.66`。

## 2026-09-12 — order_paid 读 catalog 快照入队（1.0.65）

- 修：`OrderPaidObserver` 在 `getItems()` 为空时改读 `catalog_snapshot_json` / `OrderItem`，并解码 `shipping_address` JSON 与 `shipping_snapshot_json.address`；`qty_minor` 可作数量。
- 验：`OrderPaidObserverCatalogLinesContractTest`；前台 CJ 已付订单可 Outbox→CJ shipped。
- 版本：`1.0.64` → `1.0.65`。

## 2026-09-12 — CJ/壳分类导入本级 slug（路径去前缀）

- 改：`DropshipCategoryEnsureService::parseSegments` 支持 `providerCode`，剥离已带的 `sourcing`/`dropship`/`{provider}` 前缀；子段只保留 leaf code。
- 验：`DropshipCategoryEnsureServiceTest` 覆盖 `/sourcing/cj/...` 与 `sourcing / cj / ...` 两种已累积路径。
- 说明：Product `path` 列仍由父级累积（店面 URL）；壳只负责本级 name/code。

## 2026-09-12 — 主表售价旁货源币对照（1.0.64）

- 改：主表「售价」下补 muted `≈ US$…`（`saleCompareInOriginCurrency`），与货源原价同币对比。
- 审图 ui_shot：红箭头指向主表 CNY 售价 → 对照导入货币价；主价加粗、对照 muted。
- 版本：`1.0.63` → `1.0.64`。

## 2026-09-12 — 已刊本地售价换算美元对照（1.0.63）

- 加：`DropshipPricingService::saleCompareInOriginCurrency`（本地售价→货源币；同币/换汇失败返回 null）。
- 改：`DropshipListedLocalDetailService` 的 `sale`/`variants` 附带 `compare_amount_minor` + `compare_currency`。
- 改：`listed-accordion.js` 本地售价与规格售价下列 muted `≈ US$…`，便于与货源原价同币对比。
- 审图 ui_shot：红箭头指向 ¥21.94 → 补对照美元价；主题 Token muted/sm，无私造色板。
- 版本：`1.0.62` → `1.0.63`；`listed-accordion.js?v=1.0.63`。

## 2026-09-12 — 货源配置页跟随开关文案（1.0.62）

- 改：`dropship/ops/follow_enabled` 标签改为「启用库存/价格跟随」，补充 description（Cron 每 6 小时同步远程库存与价格；涨价跟 uplift、降价 tip、可锁价）。
- 验：`ConfigIndexTemplateContractTest` 断言 label/description；Config 页 embed 本已含该 key。
- 版本：`1.0.61` → `1.0.62`。

## 2026-09-12 — 已刊货源变动/换汇成本/毛利与运维次要信息（1.0.61）

- 加：`DropshipPricingService::economicsSnapshot`（换汇成本、毛利、货源变动）。
- 改：主表定价格增加「变动」「毛利」；展开区次要展示换汇成本/毛利/同步/锁价/降价提示。
- 版本：`1.0.60` → `1.0.61`；`listed-accordion.js?v=1.0.61`。

## 2026-09-12 — 已刊定价三元组与主品规格清晰展示（1.0.60）

- 改：主表「原价」→「定价」单元格（货源/售价/涨幅一眼可见）。
- 改：手风琴展开：主产品身份 + 定价三元组（货源原价/涨价比例/本地售价）+ 规格表（规格/SKU/原价/售价/涨幅/状态）。
- 纠偏：扫表优先看定价，不只在展开区塞价格。
- 版本：`1.0.59` → `1.0.60`；`listed-accordion.js?v=1.0.60`。

## 2026-09-12 — Outbox 仓映射缺失降级（1.0.59）

## 2026-09-11 — 已刊手风琴报价列误显 SKU（1.0.58）

- 修：简单品「报价」列改为渲染货源原价（`payload.origin`），不再填本地 SKU；售价列仍为本地售价。
- 审图 ui_shot：报价列语义错位 + 空图「—」像减号 → 空占位无字；导出 `renderDetail` 供 node:test。
- 版本：`1.0.57` → `1.0.58`；`listed-accordion.js?v=1.0.58`。

## 2026-09-11 — 删除刊登 NEQ 操作符报错（1.0.57）

- 修：`otherListingUsesProduct` where 操作符 `neq` → `!=`（框架仅允许 `!=`/`<>` 等）。
- 修：删除前按 `listing_id` 重新加载行，避免 ObjectManager 单例 Model 被 `where/find` 冲掉主键导致 `delete()` 失败。
- 审图：error_shot deleteSelected DbException → 已修。
- 版本：`1.0.56` → `1.0.57`。

## 2026-09-11 — 删除确认被浮层挡住（1.0.56）

- 修：`.ds-modal` `z-index` 提到 `2147483600`，避免「事件监视」等浮层挡住「确认删除」。
- 改：删除弹窗改为原生 POST 表单 + CSRF；确认按钮 `type=submit`。
- 审图：ui_shot 确认删除无反应 → 遮挡 fail → 已修。
- 版本：`1.0.55` → `1.0.56`。

## 2026-09-11 — 已刊手风琴真实点击（1.0.56）

- 修：展开 hint 从 `<button>` 改为 `span[role=button]`，避免静态 JS 长缓存旧版「忽略 button」导致点了不展开。
- 修：页面内联 `ensureListedAccordion` 强制 `listed-accordion.js?v=1.0.56`；modules 路径 cache-bust。
- 版本：`1.0.55` → `1.0.56`。

## 2026-09-11 — 已刊手风琴展开可见性（1.0.55）

- 修：展开后 `scrollIntoView`；hint 改为可点按钮并切换「展开/收起」；detail 行用 `.is-open` 强制 `display:table-row`，避免折线下像没展开。
- 版本：`1.0.54` → `1.0.55`。

## 2026-09-11 — 删除刊登可选本地商品处理（1.0.54）

- 改：`DropshipListingDeleteService` 支持 `local_product_action=keep|disable|archive`；默认 `disable`。
- 纠偏：目录治理禁止运营抹库，「物理删除」= `ProductAdminCommand` ARCHIVE（published 先 DISABLE）。
- UI：删除确认弹窗三选一（保留 / 停用 / 物理删除归档）。
- 版本：`1.0.53` → `1.0.54`。

## 2026-09-11 — 已刊行手风琴本地详情（1.0.53）

## 2026-09-11 — 原币换汇后再涨价（1.0.51）

- 改：`DropshipPricingService` 先 `CurrencyFormatter::convert` 原币→站点货币，再按 `uplift_percent` 加价；`Publish`/`Follow` 走换汇售价。
- 改：Product `1.0.226` SAVE `writePrices` 对齐 CREATE，支持 `price_minor`（复刊才能把涨价写到 offer）。
- 验：listing 21/22 → CNY `10868`/`2194`（USD 原价×汇率×1.3）；PDP `/product/557` `108.68`、`/product/558` `21.94`。
- 版本：`1.0.50` → `1.0.51`。

## 2026-09-11 — 货源顶层分类与属性集（1.0.48）

- 改：分类 ensure 改为顶层「货源商城」`/sourcing` +「CJ货源」；拼音/Latin slug + EAV 可读名；分类 `source_platform` 标记。
- 改：属性集 `dropship`（货源商品）；刊登写 `attribute_set`/`source_platform`；产品列表「来源」列。
- 版本：`1.0.47` → `1.0.48`。

## 2026-09-11 — 万能壳分类/属性/描述落地（1.0.47）

- 改：Provider 组装 `description`/`attributes`/`category_path`；壳 `DropshipPublishService` CREATE/SAVE 写入 `category_assignments`+已注册 EAV；`DropshipCategoryEnsureService` 确保 `/dropship/{provider}/…`（ASCII slug，CJK→hash）。CJ：`source_platform=cj` + `source_public_specs`（材质等）。复用 listing 时 SAVE 补全。
- 版本：`1.0.46` → `1.0.47`。

## 2026-09-11 — 真实刊登 CREATE+PUBLISH（1.0.46）

- 改：`DropshipPublishService` 经 `ProductAdminCommandInterface` CREATE+PUBLISH（站点货币/`local_version`）；`variants.axes` → configurable；失败 fail-closed 不标 active；`publishSelected` Provider `getProduct` 回灌规格；Fake 简单+规格货源。已跑通 listing 18/19 → product 553/554 published。
- 版本：`1.0.45` → `1.0.46`。

## 2026-09-11 — 已刊登本地 SKU + 编辑入口（1.0.50）

- 增：`enrichListedLocalMeta` 按 `local_offer_id` 读 Offer SKU；`sync_status=active` 时生成 `weline_product/backend/catalog/edit-product` 深链。
- 改：已刊登商品列展示「本地 SKU」与「编辑商品」。
- 审图：已刊登行缺本地 SKU/编辑入口 → fail → 已修。
- 版本：`1.0.49` → `1.0.50`。

## 2026-09-11 — 选品原价带货币（1.0.45）

- 改：Listing SSR/JS `money(minor, currency)` 展示 `USD 20.68`（读 Snapshot `origin_currency`），避免无币种裸数字影响基础货币换算。
- 版本：`1.0.44` → `1.0.45`。

## 2026-09-11 — 后台 UI 对齐主题 / 原型（UI 2.0）

- 变更：AfterSale/Dashboard/Listing/Order 去掉 Bootstrap，改用 `w-card`/`w-field`/`w-table`/`w-empty`/`w-badge`。
- Listing search：禁止 `fetch('Backend/Listing/search')` 路径加倍；相对 `fetch('search')`。
- 规则：不重复布局 H1；空态可行动；主题 Token 边框。

# 开发日志 · Weline_Dropship

## 2026-09-11

- 真实刊登：`DropshipPublishService` 经 `ProductAdminCommandInterface` 执行 CREATE+PUBLISH（站点货币/`local_version`）；有 variants.axes 则 configurable；失败 fail-closed 不标 active；`publishSelected` 从 Provider `getProduct` 回灌规格；Fake 提供简单+规格演示货源。已跑通 listing 18/19 → product 553/554 published。版本 `1.0.45`→`1.0.46`。
- 刊登选中：默认站 `website_id=0` 不再被 `publishSelected` 误判为 `scope_or_provider_required`；门禁拆为 `provider_required` / `scope_required`。版本 `1.0.43`→`1.0.44`。
- 货源供应商筛选：新增 `<w:dropship:provider:select>`（Theme SearchSelect 多选 chips）；已刊登默认全部、空值不过滤、变更 auto-submit；禁止原生 multiple select。版本 `1.0.42`→`1.0.43`。
- 已刊登表审图：商品优先行（缩略图 + 标题/供应商·SPU）；Listing 持久化 `title`/`thumb_url`（admit/publish/follow）；状态中文化；仅待刊登可勾选。版本 `1.0.41`→`1.0.42`。
- 刊登选中弹窗改用官方 `<w:scope>` 四级范围（Global/网站/店铺/渠道）；`target_scope` 经 `SystemConfigTargetScopeService` 解析；禁止拆分 website/store 选择。版本 `1.0.40`→`1.0.41`。
- 已入列表禁止勾选/行选（与已拉取同无 checkbox）；点「已入列表」徽章移出。版本 `1.0.39`→`1.0.40`。
- 刊登选中弹窗：网站/店铺改为官方 `<w:websites:website:select>` / `<w:websites:store:select>` 标签选择；选站联动店铺选项；禁止手填 ID。版本 `1.0.38`→`1.0.39`。
- 选品分类本地缓存：壳层 `DropshipCatalogCategoryCacheService`（provider+locale+country，TTL 24h）覆盖 CJ/其它 Browse Provider，browse 返回 `categories_cache`。版本 `1.0.37`→`1.0.38`。
- 选品表：点击整行即勾选/取消勾选（已拉取不可点）。版本 `1.0.36`→`1.0.37`。
- 选品远程货源异步：index SSR 不再同步 `listCategories`/`searchProducts`；`listing/browse` JSON + 前端 loading 渲染，避免卡整页。版本 `1.0.35`→`1.0.36`。
- 选品悬浮栏：改为视口底居中 `position:fixed`，无勾选时隐藏；取消选择仅对「已入列表」启用。版本 `1.0.34`→`1.0.35`。
- 选品悬浮操作栏：选择→加入货源列表（AJAX）；取消选择→移出 pending 篮；已刊登 scope 显示「已拉取」不可操作。版本 `1.0.33`→`1.0.34`。
- 选品缩略图：表列挤扁修复（`ds-pick-thumb-cell`/`thumb-box` 固定 3.5rem 方图 + object-fit:cover）。版本 `1.0.32`→`1.0.33`。
- 选品：供应商/分类可逐级收起（`data-rail-collapsed`/`data-cats-collapsed`），localStorage 分键持久化，刷新仍固定直至再展开。版本 `1.0.31`→`1.0.32`。
- 选品：供应商/分类侧栏可收起（`data-filters-collapsed` + localStorage）；结果表缩略图列读 Snapshot `media`。版本 `1.0.30`→`1.0.31`。
- 选品 BrowseQuery 注入 `locale`（`State::getLangLocal`）；Fake 中/英分类与商品名；CJ 按 locale 择 `productName`/`productNameEn`。版本 `1.0.29`→`1.0.30`。
- 选品器三栏工作台（供应商兼容）：能力门控 `browse`/`browse_country_filter`；BrowseQuery 规范化；Snapshot/Listing `category_*`；DraftAdmit + 批量刊登；抛开原型 `view/prototype/listing-picker.html`。版本 `1.0.28`→`1.0.29`。
- 搜索商品 Tab 改为选品浏览：国家/分类/可选关键词/分页；新增 `DropshipCatalogBrowseProviderInterface`。版本 `1.0.27`→`1.0.28`。
- 搜索商品：CJ 空关键词不再盲调 API/误报「暂无结果」，空态改为「请填写关键词」。版本 `1.0.26`→`1.0.27`。
- 货源商品：远程搜索并入同页 Tab（已刊登 / 搜索商品）；旧 `/listing/search` 重定向到 `index?tab=search`。版本 `1.0.25`→`1.0.26`。
- 行内本地仓选择回填可读名：`options-json` + `local_label`（如 `AE-WH01 — 阿联酋 默认仓`），不再只显示数字 ID。版本 `1.0.24`→`1.0.25`。
- 货源商品列表/搜索：修 `fetch('Backend/Listing/search')` 路径加倍导致 500；index/search 对齐 Channel `w-card`/`w-table`/`w-field`，中文表头与供应商下拉，搜索异常可读展示。版本 `1.0.23`→`1.0.24`。
- 仓映射去「保存映射」面板：覆盖行内 `warehouse:select` + 绑定/改绑 AJAX；覆盖列表 `page/page_size` 翻页与缺口过滤联动。版本 `1.0.22`→`1.0.23`。
- 货源轨选中态：浅色 surface；选中用实心 `--weline-theme-primary` + `on-primary`（同分页 aria-current），hover 用 primary-surface。版本 `1.0.21`→`1.0.22`。
- 变体 A 确认落地：修复 `resolveFromInput` 被空 `website_code` 键误判为 Global；网站级映射强制 `store_id=0`；默认优先 `cj` + `default.__website__.default`。版本 `1.0.20`→`1.0.21`。
- 货源履约仓对接变体 A：左货源轨 + 右远程覆盖行 + 顶栏售卖范围/拉取/自动配对；绑定面板预填改绑。
- 仓映射按国家多行：唯一键改为供应商+网站+店铺+`remote_country_code`；`syncCountryPairs` 按国家配对本地物理仓↔远程仓；后台「按国家自动配对」。版本 `1.0.19`→`1.0.20`。
- 默认网站仓映射：修正 Global 误判（仅 `default.default.default` 为 Global；`default.__website__.default` 为默认网站）；列表按 website_id 升序，默认站置顶；补默认网站 CJ→本地仓映射；`store_id=0` 可读名。版本 `1.0.18`→`1.0.19`。
- 仓映射人性化：已有映射表展示供应商/网站/店铺/远程仓/本地仓可读名；空渠道「—」；表 `data-size=sm`。版本 `1.0.17`→`1.0.18`。
- 仓映射 UI：供应商 select 防 grid 拉伸；取消默认选中供应商；已有映射默认全量展示；补 CJ 示例映射行。版本 `1.0.16`→`1.0.17`。
- 仓映射空态：默认预选供应商（优先 `cj`）；远程仓/Global/已有映射空态引导文案。版本 `1.0.15`→`1.0.16`。
- CJ 仓对接收口：`postRemotePull` 透出 Provider 异常；`remoteSearch` 支持 `country_code`；远程仓标签随国家过滤并回填国家。版本 `1.0.14`→`1.0.15`。
- 审图/MCP：仓映射「拉取远程仓」去掉 `window.alert`，改 `Weline.UI.toast`（warning/success/error）；契约禁 `window.alert/confirm/prompt`。MCP 硬规则拆出独立 `no_native_js_dialogs`（原埋在 `no_generated_no_routes_xml`）。
- 禁止旧兼容：仓映射 `cj_*` 一次性迁到 `remote_*` 后 DROP；去掉读兜底。Provider `controller*` 经壳 `ProviderGateway` 调度（对齐支付）。版本 `1.0.13`→`1.0.14`。
- 仓映射标签化：`<w:inventory:warehouse:select>` + `<w:dropship:remote-warehouse:select>`；`DropshipWarehouseProviderInterface`（list/pull）；Fake/CJ 私有仓表；壳 `remoteSearch`/`remotePull` 仅编排。版本 `1.0.12`→`1.0.13`。
- 万能壳去 CJ 化重构：仓映射列 `remote_country_code`/`remote_storage_id`；Webhook `verifyWebhook` + 标准 `fulfillment` DTO；Inbox 只读标准键；Listing/Settings 默认不再绑 `cj`；探活通用码。契约 `UniversalShellContractTest`。版本 `1.0.11`→`1.0.12`。
- 壳+Provider 同构硬规定：业务进 Provider；文档 `dropship-shell.md` + `provider-development` §0；对齐 MCP `shell_provider_business_isomorph`；契约 `ShellProviderIsomorphContractTest`。版本 `1.0.10`→`1.0.11`。
- 仓与国家映射：国家改为 `<w:theme:address selection=single levels=country catalog=global searchable>`（`country-name=remote_country_code`），禁止手填 ISO。版本 `1.0.9`→`1.0.10`。

## 2026-09-10

- 已连接仍可改配置：操作列去掉 `!$ok` 门闩，有 `credentials_embed/url` 即常驻「配置」与「回调地址」并列；Fake 无凭证 schema 仍仅回调。Browser：CJ 已连接行打开「CJ 凭证」弹窗。
- 凭证弹窗「测试连接」：`Channel::probe` → Provider `probeConnection`；页脚按钮 + toast；`probe_url` 经 `UrlBuilder::getBackendUrl`；前端用 `fetch` 直调（勿 `adminRequest('dropship')`）。版本 `1.0.8`→`1.0.9`。
- 万能 Fake 与 CJ 同款 Webhook Hook：`parseWebhook` 对齐 CJ 字段回落；货源平台「回调地址」展示 `endpoint_code={code}.sandbox.default`。版本 `1.0.6`→`1.0.7`。
- 凭证弹窗补「作用范围」：`<w:scope>` + `target_scope`/`cred_dialog` 切换重开；对齐 Affiliate/货源配置壳。版本 `1.0.5`→`1.0.6`。
- 「配置凭证」统一配置嵌入：行内按钮打开 `w-dialog`，正文 `<w:config:embed group>`；深链退居弹窗页脚「在统一配置中心打开」。版本 `1.0.4`→`1.0.5`。
- 「配置凭证」深链：Provider `config_center` → 统一配置中心（不再误跳壳货源配置）；`DropshipChannelConfigDeepLinkBuilder`；版本 `1.0.3`→`1.0.4`。
- 货源平台启用改为标签多选：`dropship/platforms/enabled` → `type="multiselect"` + `options`；SystemConfig embed 补齐 multiselect/`search-select multiple`（1.3.48）。版本 Dropship `1.0.2`→`1.0.3`。
- 货源配置 embed 红标修复：`fields` 改为声明 key 字面量 `dropship/platforms/enabled,dropship/pricing/uplift_percent,dropship/ops/auto_push,dropship/ops/follow_enabled`（此前误用 `platforms_enabled` 等自造短名）。Config 页对齐 Affiliate/B2B（`selected_scope` + 统一配置中心链）；声明模板文案「分销」→「货源」。版本 `1.0.1`→`1.0.2`。MCP 学习：`systemconfig_config_embed_declared_keys` + AI硬规则索引 + `config-embed标签使用指南.md`。
- 交付（货源配置）：[货源配置](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/dropship/backend/config?target_scope=default.default.default)
- 新建万能货源壳：SPI、壳表、后台菜单「货源代发」、FakeProvider、SystemConfig、Publish/Follow/Outbox/Webhook/Freight/SellGate。
- 对齐 Payment：`extends.php` + Inbox 幂等 + 范围配置键路径。
- 文档：`provider-development.md` / `extends.md` / 三件套。
- Browser 验收：货源平台页可见 `fake`/`cj`；探活状态徽标与空态文案；菜单「货源代发」。
- 审图（ui_shot）：能力改为可读芯片；探活中文「已连接/缺少凭证」；对齐 `w-table`/`w-badge`/`w-button`；行内「配置凭证」。
- 审图（error_shot / 空白主区）：日志见间歇整页约 88KB（正常约 329KB）。加固 `PreparedContentStore`（scopeId 空时不再静默丢 put）；后端布局 `buildLayoutFetchData` 保留 `content` 回退；Channel `probeConnection` 单供应商隔离；模板去掉与 PageHeader 重复的 h1。
- 交付：[货源平台](http://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/dropship/backend/channel/index)
