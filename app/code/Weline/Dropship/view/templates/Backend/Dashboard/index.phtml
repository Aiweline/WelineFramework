<?php
/** @var \Weline\Framework\View\Template $this */
$s = (array)$this->getData('stats');
$pf = (string)$this->getData('provider_filter');
$escape = static fn($v): string => htmlspecialchars((string)$v, ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8');
$cards = [
  ['listings', __('刊登数')],
  ['price_drop_tips', __('降价提示待处理')],
  ['fulfillments', __('履约单')],
  ['outbox_errors', __('推送失败')],
];
?>
<div class="w-container" style="--w-pt:0;" data-dropship-admin="dashboard" data-testid="dropship-dashboard">
  <form method="get" class="w-cluster" data-align="center" data-wrap="true" style="--w-gap:var(--weline-space-2);--w-mb:var(--weline-space-4);">
    <input class="w-input" name="provider" data-size="sm" value="<?= $escape($pf) ?>" placeholder="provider e.g. cj" aria-label="provider" style="width:auto;max-inline-size:16rem;flex:0 1 14rem;">
    <button class="w-button" type="submit" data-tone="neutral" data-variant="outline" data-size="sm"><?= __('按平台筛') ?></button>
  </form>
  <div class="w-cluster" data-wrap="true" style="--w-gap:var(--weline-space-3);">
    <?php foreach ($cards as [$key, $label]): ?>
      <div style="flex:1 1 10rem;min-inline-size:9rem;padding:var(--weline-space-3);border:1px solid var(--weline-theme-border, var(--color-border));border-radius:var(--weline-radius-md, 0.5rem);">
        <div class="w-text" data-tone="muted" data-size="sm"><?= $escape($label) ?></div>
        <div class="w-text" data-weight="strong" data-size="lg"><?= (int)($s[$key] ?? 0) ?></div>
      </div>
    <?php endforeach; ?>
  </div>
</div>
