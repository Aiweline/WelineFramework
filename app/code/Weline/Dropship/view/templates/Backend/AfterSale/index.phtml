<?php /** @var \Weline\Framework\View\Template $this */ $pf = (string)$this->getData('provider_filter'); ?>
<div class="container-fluid py-3" data-dropship-admin="aftersale">
  <div class="d-flex flex-wrap align-items-center justify-content-between gap-2 mb-3">
    <h1 class="h3 mb-0"><?= $this->getData('page_title') ?></h1>
    <form method="get" class="d-flex gap-2">
      <input class="form-control form-control-sm" name="provider" value="<?= htmlspecialchars($pf) ?>" placeholder="provider">
      <button class="btn btn-sm btn-outline-primary" type="submit"><?= __('按平台筛') ?></button>
    </form>
  </div>
  <table class="table table-sm table-striped"><thead><tr><th>biz</th><th>provider</th><th>status</th><th>error</th></tr></thead><tbody>
  <?php foreach ((array)$this->getData('rows') as $r): ?>
    <tr><td><code><?= htmlspecialchars((string)$r['biz_key']) ?></code></td><td><?= htmlspecialchars((string)$r['provider_code']) ?></td><td><?= htmlspecialchars((string)$r['status']) ?></td><td><?= htmlspecialchars((string)($r['last_error'] ?? '')) ?></td></tr>
  <?php endforeach; ?>
  </tbody></table>
</div>
