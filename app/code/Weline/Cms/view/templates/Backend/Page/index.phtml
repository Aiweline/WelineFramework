<?php
/**@var \Weline\Framework\View\Template $this */
?>
<w:template>Weline_Admin::Backend/page-layout/main-content-before.phtml</w:template>

<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0">
                <?= $this->getData('page_title') ?: __('CMS内容管理系统') ?>
            </h4>

            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item">
                        <a href="javascript: void(0);">
                            <?= $this->getData('breadcrumb_parent') ?: __('内容管理') ?>
                        </a>
                    </li>
                    <li class="breadcrumb-item active">
                        <?= $this->getData('breadcrumb_current') ?: __('CMS内容管理系统') ?>
                    </li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- start page message -->
<div class='row'>
    <template>Weline_Component::message.phtml</template>
</div>
<!-- end message -->

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">
                    <lang>页面管理</lang>
                </h4>
                
                <div class="card-title-desc">
                    <div class='row'>
                        <div class='col-6'>
                            <div class="col-md-8">
                                <form action="<?= $this->getBackendUrl('*/backend/page/index') ?>" method="get">
                                    <div class="input-group">
                                        <input type="text" class="form-control" name="search" 
                                               value="<?= htmlspecialchars($this->request->getGet('search', '')) ?>"
                                               placeholder="<?= __('搜索页面名称、句柄...') ?>">
                                        <button class="btn btn-primary" type="submit">
                                            <i class="mdi mdi-magnify"></i> <?= __('搜索') ?>
                                        </button>
                                    </div>
                                </form>
                            </div>
                        </div>
                        <div class='col-6'>
                            <acl source="Weline_Cms::page_builder_create">
                                <a class='btn btn-primary pull-right mt-1 mb-1'
                                   href="<?= $this->getBackendUrl('*/backend/page/create') ?>">
                                    <i class="mdi mdi-plus"></i> <lang>新建页面</lang>
                                </a>
                            </acl>
                        </div>
                    </div>
                </div>

                <?php
                // 组织页面数据为树形结构
                $pages = $this->getData('pages') ?: [];
                $parentPages = [];
                $childPages = [];
                
                // 分组：父页面和子页面
                foreach ($pages as $page) {
                    $parentId = $page->getData('parent_id');
                    if (empty($parentId) || $parentId == 0) {
                        $parentPages[] = $page;
                    } else {
                        if (!isset($childPages[$parentId])) {
                            $childPages[$parentId] = [];
                        }
                        $childPages[$parentId][] = $page;
                    }
                }
                ?>
                
                <?php if (empty($parentPages)): ?>
                    <div class="text-center text-muted py-5">
                        <i class="mdi mdi-file-document-outline" style="font-size: 64px;"></i>
                        <p class="mt-3 fs-5"><lang>暂无页面，点击右上角新建页面开始使用</lang></p>
                    </div>
                <?php else: ?>
                    <!-- 树形手风琴列表 -->
                    <div class="accordion accordion-flush" id="pageAccordion">
                        <?php foreach ($parentPages as $index => $page): ?>
                            <?php 
                            $hasChildren = isset($childPages[$page->getId()]) && !empty($childPages[$page->getId()]);
                            $accordionId = 'page-' . $page->getId();
                            $isFirst = $index === 0; // 判断是否是第一个
                            ?>
                            
                            <div class="accordion-item border rounded mb-2">
                                <!-- 父页面头部 -->
                                <h2 class="accordion-header" id="heading-<?= $accordionId ?>">
                                    <div class="d-flex align-items-center p-3 bg-light">
                                        <!-- 展开按钮 -->
                                        <?php if ($hasChildren): ?>
                                            <button class="accordion-button <?= $isFirst ? '' : 'collapsed' ?> p-0 me-3 bg-transparent border-0 shadow-none" 
                                                    type="button" 
                                                    data-bs-toggle="collapse" 
                                                    data-bs-target="#collapse-<?= $accordionId ?>" 
                                                    aria-expanded="<?= $isFirst ? 'true' : 'false' ?>" 
                                                    aria-controls="collapse-<?= $accordionId ?>"
                                                    style="width: 30px; height: 30px;">
                                            </button>
                                        <?php else: ?>
                                            <div class="me-3" style="width: 30px;"></div>
                                        <?php endif; ?>
                                        
                                        <!-- 页面信息 -->
                                        <div class="flex-grow-1 d-flex align-items-center gap-3">
                                            <div>
                                                <span class="badge bg-primary">ID: <?= $page->getId() ?></span>
                                            </div>
                                            <div>
                                                <h6 class="mb-0">
                                                    <i class="mdi mdi-file-document text-primary"></i>
                                                    <?= htmlspecialchars((($page->getData('name') ?? '')) ?? '') ?>
                                                </h6>
                                                <small class="text-muted">
                                                    <i class="mdi mdi-link-variant"></i>
                                                    <?= htmlspecialchars((($page->getData('handle') ?? '')) ?? '') ?>
                                                </small>
                                            </div>
                                            <div>
                                                <?php if ($page->getData('status') == 1): ?>
                                                    <span class="badge bg-success"><i class="mdi mdi-check"></i> <?= __('已发布') ?></span>
                                                <?php else: ?>
                                                    <span class="badge bg-secondary"><i class="mdi mdi-clock-outline"></i> <?= __('草稿') ?></span>
                                                <?php endif; ?>
                                            </div>
                                            <?php if ($hasChildren): ?>
                                                <div>
                                                    <span class="badge bg-info">
                                                        <i class="mdi mdi-folder-outline"></i> 
                                                        <?= count($childPages[$page->getId()]) ?> <?= __('个子页面') ?>
                                                    </span>
                                                </div>
                                            <?php endif; ?>
                                        </div>
                                        
                                        <!-- 操作按钮 -->
                                        <div class="d-flex gap-1 ms-3 flex-nowrap">
                                            <?php
                                            // 生成前端访问链接（使用 URL 重写后的地址）
                                            $frontendUrl = rtrim(rtrim($this->request->getBaseHost(), '/'), ':') . '/' . ltrim($page->getData('handle'), '/');
                                            $previewUrl = $frontendUrl . '?preview=1';
                                            $isPublished = $page->getData('status') == 1;
                                            ?>
                                            
                                            <!-- 预览按钮（始终可用） -->
                                            <a class='btn btn-sm btn-outline-info d-inline-flex align-items-center' 
                                               href="<?= $previewUrl ?>"
                                               target="_blank"
                                               style="min-width: 70px; white-space: nowrap;"
                                               title="<?= __('预览页面效果') ?>">
                                                <i class="mdi mdi-eye-check-outline me-1"></i><?= __('预览') ?>
                                            </a>
                                            
                                            <?php if ($isPublished): ?>
                                                <!-- 已发布：查看链接按钮 -->
                                                <a class='btn btn-sm btn-outline-primary d-inline-flex align-items-center' 
                                                   href="<?= $frontendUrl ?>"
                                                   target="_blank"
                                                   style="min-width: 70px; white-space: nowrap;"
                                                   title="<?= __('在新标签页查看页面') ?>">
                                                    <i class="mdi mdi-eye-outline me-1"></i><?= __('查看') ?>
                                                </a>
                                                
                                                <!-- 已发布：拷贝链接按钮 -->
                                                <button class='btn btn-sm btn-outline-secondary copy-link-btn d-inline-flex align-items-center' 
                                                        data-url="<?= htmlspecialchars($frontendUrl ?? '') ?>"
                                                        data-page-name="<?= htmlspecialchars((($page->getData('name') ?? '')) ?? '') ?>"
                                                        style="min-width: 85px; white-space: nowrap;"
                                                        title="<?= __('复制链接到剪贴板') ?>">
                                                    <i class="mdi mdi-content-copy me-1"></i><?= __('拷贝') ?>
                                                </button>
                                            <?php else: ?>
                                                <!-- 未发布：禁用的查看按钮 -->
                                                <button class='btn btn-sm btn-outline-secondary d-inline-flex align-items-center' 
                                                        disabled
                                                        style="min-width: 70px; white-space: nowrap;"
                                                        title="<?= __('页面发布后才能查看') ?>">
                                                    <i class="mdi mdi-eye-off-outline me-1"></i><?= __('查看') ?>
                                                </button>
                                                
                                                <!-- 未发布：禁用的拷贝按钮 -->
                                                <button class='btn btn-sm btn-outline-secondary d-inline-flex align-items-center' 
                                                        disabled
                                                        style="min-width: 85px; white-space: nowrap;"
                                                        title="<?= __('页面发布后才能复制链接') ?>">
                                                    <i class="mdi mdi-content-copy me-1"></i><?= __('拷贝') ?>
                                                </button>
                                            <?php endif; ?>
                                            
                                            <div class="border-end mx-1" style="height: 28px;"></div>
                                            
                                            <acl source="Weline_Cms::page_builder_create">
                                                <a class='btn btn-sm btn-success d-inline-flex align-items-center' 
                                                   href="<?= $this->getBackendUrl('*/backend/page/create') ?>?parent_id=<?= $page->getId() ?>"
                                                   style="min-width: 70px; white-space: nowrap;"
                                                   title="<?= __('新增子页面') ?>">
                                                    <i class="mdi mdi-plus me-1"></i><?= __('子页面') ?>
                                                </a>
                                            </acl>
                                            <acl source="Weline_Cms::page_builder_edit">
                                                <a class='btn btn-sm btn-info d-inline-flex align-items-center'
                                                   href="<?= $this->getBackendUrl('*/backend/page/edit') ?>?id=<?= $page->getId() ?>"
                                                   style="min-width: 70px; white-space: nowrap;">
                                                    <i class="mdi mdi-pencil me-1"></i><?= __('编辑') ?>
                                                </a>
                                            </acl>
                                            <acl source="Weline_Cms::page_builder_delete">
                                                <form action="<?= $this->getBackendUrl('*/backend/page/delete') ?>"
                                                      method='post' 
                                                      style="display:inline;"
                                                      onsubmit="return confirm('<?= __('确定要删除此页面吗？如果有子页面，子页面也会被删除！') ?>');">
                                                    <input type='hidden' name='id' value='<?= $page->getId() ?>'>
                                                    <button type='submit' class='btn btn-sm btn-danger d-inline-flex align-items-center' style="min-width: 70px; white-space: nowrap;">
                                                        <i class="mdi mdi-delete me-1"></i><?= __('删除') ?>
                                                    </button>
                                                </form>
                                            </acl>
                                        </div>
                                    </div>
                                </h2>
                                
                                <!-- 子页面列表 -->
                                <?php if ($hasChildren): ?>
                                    <div id="collapse-<?= $accordionId ?>" 
                                         class="accordion-collapse collapse <?= $isFirst ? 'show' : '' ?>" 
                                         aria-labelledby="heading-<?= $accordionId ?>"
                                         data-bs-parent="#pageAccordion">
<<<<<<< HEAD
                                        <div class="accordion-body bg-white p-0">
=======
                                        <div class="accordion-body p-0">
>>>>>>> dev-new
                                            <div class="list-group list-group-flush">
                                                <?php foreach ($childPages[$page->getId()] as $childPage): ?>
                                                    <div class="list-group-item">
                                                        <div class="d-flex align-items-center">
                                                            <!-- 缩进指示器 -->
                                                            <div class="me-3 text-muted">
                                                                <i class="mdi mdi-subdirectory-arrow-right" style="font-size: 20px;"></i>
                                                            </div>
                                                            
                                                            <!-- 子页面信息 -->
                                                            <div class="flex-grow-1 d-flex align-items-center gap-3">
                                                                <div>
                                                                    <span class="badge bg-light text-dark">ID: <?= $childPage->getId() ?></span>
                                                                </div>
                                                                <div>
                                                                    <h6 class="mb-0">
                                                                        <i class="mdi mdi-file-outline text-secondary"></i>
                                                                        <?= htmlspecialchars((($childPage->getData('name') ?? '')) ?? '') ?>
                                                                    </h6>
                                                                    <small class="text-muted">
                                                                        <i class="mdi mdi-link-variant"></i>
                                                                        <?= htmlspecialchars((($childPage->getData('handle') ?? '')) ?? '') ?>
                                                                    </small>
                                                                </div>
                                                                <div>
                                                                    <?php if ($childPage->getData('status') == 1): ?>
                                                                        <span class="badge bg-success"><i class="mdi mdi-check"></i> <?= __('已发布') ?></span>
                                                                    <?php else: ?>
                                                                        <span class="badge bg-secondary"><i class="mdi mdi-clock-outline"></i> <?= __('草稿') ?></span>
                                                                    <?php endif; ?>
                                                                </div>
                                                            </div>
                                                            
                                                            <!-- 子页面操作 -->
                                                            <div class="d-flex gap-1 ms-3 flex-nowrap">
                                                                <?php
                                                                // 生成子页面前端访问链接（使用 URL 重写后的地址）
                                                                $childFrontendUrl = rtrim(rtrim($this->request->getBaseHost(), '/'), ':') . '/' . ltrim($childPage->getData('handle'), '/');
                                                                $childPreviewUrl = $childFrontendUrl . '?preview=1';
                                                                $childIsPublished = $childPage->getData('status') == 1;
                                                                ?>
                                                                
                                                                <!-- 预览按钮（始终可用） -->
                                                                <a class='btn btn-sm btn-outline-info d-inline-flex align-items-center' 
                                                                   href="<?= $childPreviewUrl ?>"
                                                                   target="_blank"
                                                                   style="min-width: 70px; white-space: nowrap;"
                                                                   title="<?= __('预览页面效果') ?>">
                                                                    <i class="mdi mdi-eye-check-outline me-1"></i><?= __('预览') ?>
                                                                </a>
                                                                
                                                                <?php if ($childIsPublished): ?>
                                                                    <!-- 已发布：查看链接按钮 -->
                                                                    <a class='btn btn-sm btn-outline-primary d-inline-flex align-items-center' 
                                                                       href="<?= $childFrontendUrl ?>"
                                                                       target="_blank"
                                                                       style="min-width: 70px; white-space: nowrap;"
                                                                       title="<?= __('在新标签页查看页面') ?>">
                                                                        <i class="mdi mdi-eye-outline me-1"></i><?= __('查看') ?>
                                                                    </a>
                                                                    
                                                                    <!-- 已发布：拷贝链接按钮 -->
                                                                    <button class='btn btn-sm btn-outline-secondary copy-link-btn d-inline-flex align-items-center' 
                                                                            data-url="<?= htmlspecialchars($childFrontendUrl ?? '') ?>"
                                                                            data-page-name="<?= htmlspecialchars((($childPage->getData('name') ?? '')) ?? '') ?>"
                                                                            style="min-width: 85px; white-space: nowrap;"
                                                                            title="<?= __('复制链接到剪贴板') ?>">
                                                                        <i class="mdi mdi-content-copy me-1"></i><?= __('拷贝') ?>
                                                                    </button>
                                                                <?php else: ?>
                                                                    <!-- 未发布：禁用的查看按钮 -->
                                                                    <button class='btn btn-sm btn-outline-secondary d-inline-flex align-items-center' 
                                                                            disabled
                                                                            style="min-width: 70px; white-space: nowrap;"
                                                                            title="<?= __('页面发布后才能查看') ?>">
                                                                        <i class="mdi mdi-eye-off-outline me-1"></i><?= __('查看') ?>
                                                                    </button>
                                                                    
                                                                    <!-- 未发布：禁用的拷贝按钮 -->
                                                                    <button class='btn btn-sm btn-outline-secondary d-inline-flex align-items-center' 
                                                                            disabled
                                                                            style="min-width: 85px; white-space: nowrap;"
                                                                            title="<?= __('页面发布后才能复制链接') ?>">
                                                                        <i class="mdi mdi-content-copy me-1"></i><?= __('拷贝') ?>
                                                                    </button>
                                                                <?php endif; ?>
                                                                
                                                                <div class="border-end mx-1" style="height: 28px;"></div>
                                                                
                                                                <acl source="Weline_Cms::page_builder_edit">
                                                                    <a class='btn btn-sm btn-info d-inline-flex align-items-center'
                                                                       href="<?= $this->getBackendUrl('*/backend/page/edit') ?>?id=<?= $childPage->getId() ?>"
                                                                       style="min-width: 70px; white-space: nowrap;">
                                                                        <i class="mdi mdi-pencil me-1"></i><?= __('编辑') ?>
                                                                    </a>
                                                                </acl>
                                                                <acl source="Weline_Cms::page_builder_delete">
                                                                    <form action="<?= $this->getBackendUrl('*/backend/page/delete') ?>"
                                                                          method='post' 
                                                                          style="display:inline;"
                                                                          onsubmit="return confirm('<?= __('确定要删除此子页面吗？') ?>');">
                                                                        <input type='hidden' name='id' value='<?= $childPage->getId() ?>'>
                                                                        <button type='submit' class='btn btn-sm btn-danger d-inline-flex align-items-center' style="min-width: 70px; white-space: nowrap;">
                                                                            <i class="mdi mdi-delete me-1"></i><?= __('删除') ?>
                                                                        </button>
                                                                    </form>
                                                                </acl>
                                                            </div>
                                                        </div>
                                                    </div>
                                                <?php endforeach; ?>
                                            </div>
                                        </div>
                                    </div>
                                <?php endif; ?>
                            </div>
                        <?php endforeach; ?>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<w:template>Weline_Admin::Backend/page-layout/main-content-after.phtml</w:template>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // 拷贝链接功能
    const copyButtons = document.querySelectorAll('.copy-link-btn');
    
    copyButtons.forEach(function(button) {
        button.addEventListener('click', function(e) {
            e.preventDefault();
            
            const url = this.getAttribute('data-url');
            const pageName = this.getAttribute('data-page-name');
            const originalHtml = this.innerHTML;
            const originalClass = this.className;
            
            // 使用现代 Clipboard API
            if (navigator.clipboard && navigator.clipboard.writeText) {
                navigator.clipboard.writeText(url)
                    .then(() => {
                        // 成功提示
                        this.innerHTML = '<i class="mdi mdi-check"></i> <?= __("已复制") ?>';
                        this.className = 'btn btn-success btn-sm copy-link-btn';
                        
                        // Toast 通知
                        showToast('success', '<?= __("链接已复制") ?>', 
                                  '<?= __("页面") ?>: ' + pageName + '<br><?= __("链接") ?>: ' + url);
                        
                        // 2秒后恢复原状
                        setTimeout(() => {
                            this.innerHTML = originalHtml;
                            this.className = originalClass;
                        }, 2000);
                    })
                    .catch(err => {
                        console.error('<?= __("复制失败") ?>:', err);
                        showToast('error', '<?= __("复制失败") ?>', '<?= __("请手动复制链接") ?>');
                    });
            } else {
                // 降级方案：使用传统的 execCommand 方法
                const textArea = document.createElement('textarea');
                textArea.value = url;
                textArea.style.position = 'fixed';
                textArea.style.left = '-999999px';
                textArea.style.top = '-999999px';
                document.body.appendChild(textArea);
                textArea.focus();
                textArea.select();
                
                try {
                    const successful = document.execCommand('copy');
                    if (successful) {
                        // 成功提示
                        this.innerHTML = '<i class="mdi mdi-check"></i> <?= __("已复制") ?>';
                        this.className = 'btn btn-success btn-sm copy-link-btn';
                        
                        showToast('success', '<?= __("链接已复制") ?>', 
                                  '<?= __("页面") ?>: ' + pageName + '<br><?= __("链接") ?>: ' + url);
                        
                        setTimeout(() => {
                            this.innerHTML = originalHtml;
                            this.className = originalClass;
                        }, 2000);
                    } else {
                        throw new Error('execCommand failed');
                    }
                } catch (err) {
                    console.error('<?= __("复制失败") ?>:', err);
                    // 最后降级方案：提示用户手动复制
                    prompt('<?= __("请手动复制以下链接") ?>:', url);
                } finally {
                    document.body.removeChild(textArea);
                }
            }
        });
    });
    
    // Toast 通知函数
    function showToast(type, title, message) {
        // 检查是否有全局的 Toast 方法（如 Bootstrap Toast）
        if (typeof Toastify !== 'undefined') {
            // 使用 Toastify 库
            Toastify({
                text: title + (message ? '<br>' + message : ''),
                duration: 3000,
                gravity: "top",
                position: "right",
                backgroundColor: type === 'success' ? "#28a745" : "#dc3545",
                escapeMarkup: false
            }).showToast();
        } else if (typeof toastr !== 'undefined') {
            // 使用 toastr 库
            toastr[type](message || '', title);
        } else {
            // 降级为简单的 alert（开发环境可能会用到）
            if (type === 'success') {
                // 创建简单的通知元素
                const toast = document.createElement('div');
                toast.style.cssText = 'position:fixed;top:20px;right:20px;background:#28a745;color:white;padding:15px 20px;border-radius:5px;box-shadow:0 4px 6px rgba(0,0,0,0.1);z-index:9999;max-width:400px;';
                toast.innerHTML = '<strong>' + title + '</strong><br>' + (message || '');
                document.body.appendChild(toast);
                
                setTimeout(() => {
                    toast.style.transition = 'opacity 0.3s';
                    toast.style.opacity = '0';
                    setTimeout(() => document.body.removeChild(toast), 300);
                }, 3000);
            } else {
                alert(title + '\n' + (message || ''));
            }
        }
    }
});
</script>

