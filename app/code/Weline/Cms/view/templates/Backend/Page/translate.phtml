<?php
/**
 * CMS页面翻译 - 内容模板
 * 由布局系统自动包裹
 *
 * @var \Weline\Framework\View\Template $this
 * @var \Weline\Cms\Model\Page $page
 * @var \Weline\Cms\Model\Page\LocalDescription $translation
 */
$page = $this->getData('page');
$translation = $this->getData('translation');
$locale = $this->getData('locale');
$localeName = $this->getData('locale_name');
?>

<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0">
                <?= $this->getData('page_title') ?: __('翻译页面') ?>
            </h4>

            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item">
                        <a href="<?= $this->getBackendUrl('*/backend/page/index') ?>">
                            <?= __('CMS内容管理系统') ?>
                        </a>
                    </li>
                    <li class="breadcrumb-item">
                        <a href="<?= $this->getBackendUrl('*/backend/page/edit', ['id' => $page->getId()]) ?>">
                            <?= htmlspecialchars((($page->getData('name') ?? '')) ?? '') ?>
                        </a>
                    </li>
                    <li class="breadcrumb-item active">
                        <?= __('翻译到 %{1}', $localeName) ?>
                    </li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- start page message -->
<div class='row'>
    <template>Weline_Component::message.phtml</template>
</div>
<!-- end message -->

<form action="<?= $this->getData('action') ?>" method="post" class="needs-validation" novalidate>
    <div class="row">
        <!-- 左侧：原文内容 -->
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title mb-4">
                        <i class="mdi mdi-text"></i> <?= __('原文内容') ?>
                    </h4>

                    <div class="mb-4">
                        <h6 class="text-muted"><?= __('页面名称') ?></h6>
                        <p><?= htmlspecialchars((($page->getData('name') ?? '')) ?? '') ?></p>
                    </div>

                    <div class="mb-4">
                        <h6 class="text-muted"><?= __('页面标题') ?></h6>
                        <p><?= htmlspecialchars((($page->getData('title') ?? '')) ?? '') ?></p>
                    </div>

                    <div class="mb-4">
                        <h6 class="text-muted"><?= __('页面内容') ?></h6>
                        <div class="border p-3 bg-light" style="max-height: 300px; overflow-y: auto;">
                            <?= nl2br(htmlspecialchars((($page->getData('content') ?? '')) ?? '')) ?>
                        </div>
                    </div>

                    <hr>

                    <h5 class="mb-3"><?= __('SEO信息') ?></h5>

                    <div class="mb-4">
                        <h6 class="text-muted"><?= __('SEO标题') ?></h6>
                        <p><?= htmlspecialchars(($page->getData('meta_title') ?? '') ?: $page->getData('title')) ?></p>
                    </div>

                    <div class="mb-4">
                        <h6 class="text-muted"><?= __('SEO描述') ?></h6>
                        <p><?= htmlspecialchars((($page->getData('meta_description') ?? '')) ?? '') ?></p>
                    </div>

                    <div class="mb-4">
                        <h6 class="text-muted"><?= __('SEO关键词') ?></h6>
                        <p><?= htmlspecialchars((($page->getData('meta_keywords') ?? '')) ?? '') ?></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- 右侧：翻译内容 -->
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title mb-4">
                        <i class="mdi mdi-translate"></i> <?= __('翻译内容（%{1}）', $localeName) ?>
                    </h4>

                    <!-- 翻译页面名称 -->
                    <div class="mb-3">
                        <label for="name" class="form-label">
                            <?= __('页面名称') ?> <span class="text-danger">*</span>
                        </label>
                        <input type="text" 
                               class="form-control" 
                               id="name" 
                               name="name" 
                               value="<?= htmlspecialchars(($translation ? $translation->getData('name') ?? '') : '') ?>"
                               placeholder="<?= __('输入翻译后的页面名称') ?>"
                               required>
                        <div class="invalid-feedback"><?= __('请输入翻译后的页面名称') ?></div>
                    </div>

                    <!-- 翻译页面标题 -->
                    <div class="mb-3">
                        <label for="title" class="form-label">
                            <?= __('页面标题') ?> <span class="text-danger">*</span>
                        </label>
                        <input type="text" 
                               class="form-control" 
                               id="title" 
                               name="title" 
                               value="<?= htmlspecialchars(($translation ? $translation->getData('title') ?? '') : '') ?>"
                               placeholder="<?= __('输入翻译后的页面标题') ?>"
                               required>
                        <div class="invalid-feedback"><?= __('请输入翻译后的页面标题') ?></div>
                    </div>

                    <!-- 翻译页面内容 -->
                    <div class="mb-3">
                        <label for="content" class="form-label"><?= __('页面内容') ?></label>
                        <textarea class="form-control editor" 
                                  editor="1"
                                  id="content" 
                                  name="content" 
                                  rows="10"
                                  placeholder="<?= __('输入翻译后的页面内容') ?>"><?= htmlspecialchars(($translation ? $translation->getData('content') ?? '') : '') ?></textarea>
                        <div class="form-text"><?= __('使用所见即所得编辑器编辑翻译内容') ?></div>
                    </div>

                    <hr>

                    <h5 class="mb-3"><?= __('SEO信息翻译') ?></h5>

                    <!-- 翻译SEO标题 -->
                    <div class="mb-3">
                        <label for="meta_title" class="form-label"><?= __('SEO标题') ?></label>
                        <input type="text" 
                               class="form-control" 
                               id="meta_title" 
                               name="meta_title" 
                               value="<?= htmlspecialchars(($translation ? $translation->getData('meta_title') ?? '') : '') ?>"
                               placeholder="<?= __('输入翻译后的SEO标题') ?>">
                        <div class="form-text"><?= __('留空则使用页面标题') ?></div>
                    </div>

                    <!-- 翻译SEO描述 -->
                    <div class="mb-3">
                        <label for="meta_description" class="form-label"><?= __('SEO描述') ?></label>
                        <textarea class="form-control" 
                                  id="meta_description" 
                                  name="meta_description" 
                                  rows="3"
                                  placeholder="<?= __('输入翻译后的SEO描述') ?>"><?= htmlspecialchars(($translation ? $translation->getData('meta_description') ?? '') : '') ?></textarea>
                    </div>

                    <!-- 翻译SEO关键词 -->
                    <div class="mb-3">
                        <label for="meta_keywords" class="form-label"><?= __('SEO关键词') ?></label>
                        <input type="text" 
                               class="form-control" 
                               id="meta_keywords" 
                               name="meta_keywords" 
                               value="<?= htmlspecialchars(($translation ? $translation->getData('meta_keywords') ?? '') : '') ?>"
                               placeholder="<?= __('输入翻译后的关键词，用逗号分隔') ?>">
                    </div>

                    <!-- 操作按钮 -->
                    <div class="mt-4">
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="mdi mdi-content-save"></i> <?= __('保存翻译') ?>
                            </button>
                            <a href="<?= $this->getBackendUrl('*/backend/page/edit', ['id' => $page->getId()]) ?>" 
                               class="btn btn-secondary">
                                <i class="mdi mdi-arrow-left"></i> <?= __('返回编辑页面') ?>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

<script>
// 表单验证
(function() {
    'use strict';
    var forms = document.querySelectorAll('.needs-validation');
    Array.prototype.slice.call(forms).forEach(function(form) {
        form.addEventListener('submit', function(event) {
            if (!form.checkValidity()) {
                event.preventDefault();
                event.stopPropagation();
            }
            form.classList.add('was-validated');
        }, false);
    });
})();
</script>


