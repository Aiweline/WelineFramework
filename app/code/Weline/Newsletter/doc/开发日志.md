# Weline_Newsletter 开发日志

## 2026-09-24 · 弹窗 cookie 按网站 Path 隔离

- `newsletter-subscribe.js`：`cookiePath()` 取 runtime `baseUrl` pathname（如 `/daocharms/`）；cookie 名加 `cookieSiteKey()`；清除时兼清旧 `path=/`。
- 配合 Theme overlay 修复后 DaoCharms 首页可弹出；`?newsletter_popup=1` 验收钩仍可用。
- 合同：`NewsletterWidgetOwnerContractTest` 断言 `cookiePath` / `cookieSiteKey`。

## 2026-09-22 · 订阅邮件主题/标题注入品牌名（1.0.12）

- 示范主题与正文 H1 统一带 `{{var.brand_display_name}}`（随 MailBrand 合并，如「长安汉服 · Hanfu Atelier」）。
- 配合 Smtp `1.4.56` From 显示名品牌回退；补发验证发件人不再显示 `aiweline`。
- 版本 `1.0.12`。

## 2026-09-22 · WO-HP-P2-06 Newsletter 弹窗延后触发

- 默认触发改为 `deferred`：停留约 15s **或** 滚动约 40% **或** 退出意向（OR）；`min_open_seconds=3` 首屏硬门槛。
- 旧配置 `trigger=delay` 且 `delay_seconds<15`：模板 + JS 自动升级为组合延后，禁止首访首屏立即弹。
- 关闭：按钮 / 蒙层 / Esc 仍可用；cookie 14 天频控不变。
- 合同：`NewsletterWidgetOwnerContractTest` 断言 deferred 默认、data-min-open、requestShow。
- 版本 `1.0.10`；JS `?v=20260922-deferred`。

## 2026-09-22 · 古风宣纸信笺（去红包）

- 审图：红包信封气质 fail；改为纯宣纸信笺（回纹框 + 朱印）。
- 资源：`newsletter-xinjian-gufeng.webp`（约 12KB，纸面实心）；删除红包壳图。
- 版本 `1.0.11`。

## 2026-09-22 · 审图：信笺必须实心不透字

- 分流 `web_ui`/`ui_shot`：对照原型，纸面穿透明 → 字不可读（fail）。
- WebP：纸面区域强制 α=255 宣纸色；外沿真透明；约 29KB。
- CSS：`.letter-shell::before` + `.letter-shell__form` 实心 `surface-raised` 垫层，双保险可读。
- 蒙版加浓；版本保持 `1.0.9`。

## 2026-09-22 · 真透明 WebP + 纯蒙版

- 去掉山水遮罩图；仅 `theme-overlay` 半透明蒙版。
- 信封信笺：`newsletter-letter-envelope-shell.webp`（真 Alpha，约 24KB）。
- 版本 `1.0.9`。

## 2026-09-22 · 订阅弹窗偶发锁死页面滚动（修复）

- 现场：首页 `body{overflow:hidden}` + 弹层 `display:block` 但无 `is-open`（opacity:0）仍拦截滚轮/点击，表现为偶发无法上下滑动。
- JS：滚动锁打标 `data-newsletter-scroll-lock`；关闭立即解锁；防双绑；cookie 改稳定 `data-widget-code`；自愈脏态。
- CSS：非 `.is-open` 时根/遮罩/舞台 `pointer-events:none`。
- 合同：`NewsletterWidgetOwnerContractTest::testSubscribeJsGuardsScrollLockAndStableCookie`。
- 版本 `1.0.8`。

## 2026-09-22 · 古风信封信笺整图 PNG（部件席）

- 按用户参考稿重画：`newsletter-letter-envelope-shell.png`（朱红信封+宣纸信笺+火漆印，透明底）+ `newsletter-popup-mist-bg.png` 遮罩山水。
- 弹窗改为整图壳 + 表单叠写区；弃 CSS 拼袋/封舌。
- 版本 `1.0.7`。

## 2026-09-22 · 信封掀开信笺弹窗（部件席）

- 审图 ui_shot：原宽扁「Subscribe for offers」卡缺信笺/开信感；改为信封袋 + 封舌 `rotateX` 掀开 + 宣纸信笺升起。
- PNG：`view/statics/images/newsletter-letter-xuan.png`、`newsletter-envelope-flap.png`；空 `image` 参数时默认挂信笺底。
- JS：`showPopup` 双 rAF 加 `is-open`；关闭先卸 class 再延时隐藏。
- 合同：`NewsletterWidgetOwnerContractTest` 断言信笺/信封资源与 `newsletter-letter-sheet`。
- 版本 `1.0.6`。

## 2026-09-22 · 店面部件视觉返工（部件席）

- `footer-newsletter` Variant A：根节点自带全宽条带底（surface + primary 轻混），文案用 `theme-text`/`text-muted`；内层对齐 `--weline-layout-content-max-width`；`w-input`+`w-button`；checkbox `accent-color: primary`；平板以下堆叠。
- `newsletter-popup` Variant A：窄信笺 `max-inline-size: min(22rem, 92vw)`；朱印实心「礼」；CTA `inline-size: auto` 居中；同样 w-* 控件 + accent checkbox。
- `widget.php`：`footer-newsletter` / `newsletter-popup` 补 `placement=injection`（跨模块注入硬路径）。
- i18n：源串简中；`php bin/w i18n:collect`（全量）。
- 版本 `1.0.5`；合规复审见 `doc/开发/team/newsletter-subscribe/meetings/部件-review.md`。
- MCP：会话 stale，宿主 Read `AI硬规则索引.md` + `部件开发指南.md` 继续；禁止假称已 prepare。

## 2026-09-22 · 订阅名单搜索紧凑化 + 中英 CSV（前端席）

- `backend/subscriber/index.phtml`：标题行右侧紧凑横排搜索（`max-inline-size:28rem`，`w-input`/`w-button` `sm`），禁止全宽竖叠。
- `i18n/zh_Hans_CN.csv` / `en_US.csv`：列表文案中英齐写；`php bin/w i18n:collect`（全量，避免 `--module` 暂态冲掉 zh 包）。
- 版本 `1.0.4`。MCP：前端席挂 `template_i18n`+`module_i18n_csv`；硬规则 `frontend_ui_requires_zh_en_csv`；后台搜索条紧凑写入 `backend_admin_ui_requires_frontend_theme_skills`。
- 附：修正 `w_i18n_locale_dictionary` 中 zh_Hans_CN 误存英文的 Newsletter 词条；Framework `Phrase\Parser` 中文身份译不得回落到 en 层（契约测 `ZhIdentityMustNotFallThroughToEnContractTest`）。

## 2026-09-22 · 后台 UI 2.0 主题化（前端席）

- `backend/campaign/config.phtml`、`backend/subscriber/index.phtml` 套 `w-backend-page` / `w-card` / `w-field` / `w-table`；`data-testid`：`newsletter-campaign-config` / `newsletter-subscriber-listing`。
- 版本 `1.0.3`；核验 footer-above 注入与合同测（Widget Owner / FooterAbove / FooterContainer）。
- 前台 footer-newsletter / newsletter-popup：已用主题 Token，无 hex 硬编码，判已合规。

## 2026-09-22 · D9/D10 Widget 迁入 + Theme 删壳（前端席同发）

- D9：`extends/.../Weline_Newsletter/widget.php` 注册 `footer-newsletter` / `newsletter-popup` / `sidebar-newsletter`；模板迁入；BinQuery JS；`data-testid` 钩子；topic 默认勾选；cookie_days=14。
- D10（代主题同发）：Theme `widget.php` 去三项；删 Theme 三模板；`FooterPartialComposer::renderNewsletter` 空实现。
- 验证：`php bin/w widget:refresh`；`NewsletterWidgetOwnerContractTest` + `FooterPartialComposerTest`。

## 2026-09-22 · D11/D12 T2 结账邮箱匹配

- T1 确认：`SubscribeService` 发券后调 `applyIssuedCoupon`；修正为 `applyCoupon($code, DiscountQuoteServiceInterface, $params)`。
- T2：Observer 挂 `identity::resolve::after` + `guest::validate::after`；台账 `gift_status=issued` → apply；`Api/NewsletterCheckoutCouponAutoApplyInterface` 并存。
- 版本 `1.0.1`；合同测 `CheckoutAutoApplyContractTest`。

## 2026-09-22 · 施工-1→2 后端落地

- 范围：D1 Setup、D2 ACL（`#[Acl]` + menu）、D4 SubscribeService、D5 BinQuery、D6 Sync、D7 Issuer（传 `valid_days=14`）、D8 MailChannel、T1 AutoApply stub。
- 验证：`php bin/w setup:upgrade --module=Weline_Newsletter`；合同测 `SubscribeServiceContractTest`。
- 未做：D9 Widget 迁入 / Theme 删壳（主题+前端席）。

## 文档初始化

- 由 MCP 自动修复流程补齐文档契约。
- 本记录不推断或补造模块历史；后续变更应记录版本、范围与验证证据。
