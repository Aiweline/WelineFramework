# Weline_Newsletter 功能现状

## 模块状态（2026-09-22 施工 D11/D12）

| 项 | 状态 |
|----|------|
| register / module **1.0.1** | 已落地 |
| 表 `weline_newsletter_subscriber` | Model 已声明；setup:upgrade 已跑 |
| 前台 `newsletter/subscribe` | Router + `Controller/Frontend/Subscribe` → `SubscribeService` |
| BinQuery `newsletter.subscribe` | `NewsletterQueryProvider` |
| 订阅有奖 | `SubscribeGiftCampaignSyncService` / `SubscribeGiftIssuer`（`valid_days` 默认 14） |
| Smtp 渠道 | `subscribe_welcome` / `subscribe_gift` |
| 后台 | 邮件订阅名单 + 有奖配置（ACL 属性 + menu） |
| T1 自动用券 | `CheckoutAutoApplyService::applyIssuedCoupon`（正确传 DiscountQuote） |
| T2 结账邮箱匹配 | Observer × identity/guest validate after + `NewsletterCheckoutCouponAutoApplyInterface` |
| Widget 迁入 | **未做**（deps D9，留给下一波） |

## 边界

- ≠ `Weline_Subscription`（周期订购）
- ≠ `Weline_Mail`（企业邮箱）
- 发券/用券仅经 Marketing SPI；不直改 Coupon Model
