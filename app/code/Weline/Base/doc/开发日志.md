# Weline_Base 开发日志

## 当前基线

- `weline/base` 作为 `weline-module` 类型的默认发行聚合包维护。
- 当前要求 PHP `^8.4`，并通过 `require` 汇总 Framework、后台、AI、主题、队列、部署等默认模块。
- 本次 `Weline_Ai 1.2.0` 知识迁移补齐三文档契约，未调整依赖集合或运行时行为。

## 后续记录要求

- 每次新增、移除或改名依赖时记录目标、兼容影响和 Composer 验证结果。
- 不补造本文件建立前的发行历史；历史事实以 Git 和已发布 Composer 元数据为准。
