# 汇审 — 菜单 HTML 进程缓存有界化

日期：2026-09-20  
模块：Weline_Admin `2.0.8`  
结论：通过

## 需求 / 架构

技术方案会冻结：本波 A+C；暖基线与骨架缓存 B 不动。  
`work_kind=bugfix`，`fe_be_scope=BE-only`，UI 技能 skip。

## 修复

1. `$renderedMenuCache`：`MAX=16` + TTL 扫 + LRU；键改 `activeFingerprint`（激活 `source_id` 集合 md5），去掉 raw `currentUrl`。
2. `$frequentMenusCache`：`MAX=32` + 同步清理。
3. `MenuRenderService::clearProcessCache()`；Admin `ProcessCacheResetter` soft/hard/显式一律调用（返回计数 2）。

## 证据

`MenuRenderServiceRenderedMenuCacheContractTest`：5 tests / 19 assertions PASS（`app/bootstrap_phpunit.php`）。

## 未做（有意）

- 暖基线（Phrase / 静态文件 / FPC）容量调整  
- 方案 B：中性 HTML + 运行时/前端标 active  
