# 汇审 — backend-menu-cross-locale-db-index

date: 2026-09-22
status: passed_local

## 证据

1. UT：`MenuRenderServiceCrossLocaleSearchContractTest` + `BackendMenuSearchProviderContractTest` → 7 tests OK（1 skipped：phpunit sqlite 无索引表）。
2. `BackendMenuSearchIndexRebuilder::rebuild(0)` → **312** 条，离线约 16s（可接受）。
3. 冷 `renderMenu(role1)`：**227ms**，`html_bytes≈237KB`，**无** `data-search-text`（对比原 10–26s / ~600KB）。
4. 原始索引搜 `Products`：命中 `Weline_Product::commerce:catalog:products`；execute 无登录用户时 ACL 过滤为空（预期）。
5. 挂钩：`menu:collect` 末尾 rebuild；`Weline_Acl::role_access_cache_invalidated` Observer；Admin `Upgrade` 2.0.10。
6. **架构复审否决已修**：`nav-filter` `applyFilterBySourceIds` 按子树/祖先解开 `hidden`（ui + statics）；e2e 断言祖先非 hidden。

## 结论

交叉能力保留；打开后台不再因全语烘焙卡顿。侧栏交叉改 debounce 查索引；嵌套菜单异语命中可见。

## 后续

本机 `php bin/w setup:upgrade`（Admin 2.0.10）+ WLS reload 后 Browser 验侧栏搜 Products。
