# Weline_Ai 需求

> 本文是本模块当前有效需求与业务逻辑的唯一文档 owner。原始聊天、代码现状和开发日志不是需求授权。

## 文档信息

- 模块：`Weline_Ai`
- 当前基线版本：`1.1.3`
- 下一目标版本：`1.2.0`
- 最后确认人：用户（当前会话）
- 最后确认日期：`2026-08-23`

## 需求索引

| 需求 ID | 标题 | 状态 | 引入版本 | 最近变更版本 | 验收状态 | 关联功能文档 |
|---|---|---|---|---|---|---|
| `REQ-AI-0001` | 场景图像生成参考媒体契约 | implemented | `1.1.3` | `1.1.3` | pending | [README](./README.md#场景图像生成参考媒体契约) |
| `REQ-AI-0002` | 统一项目知识库与强制 MCP 准备协议 | confirmed | `1.2.0` | `1.2.0` | pending | [README](./README.md) |
| `REQ-AI-0005` | 本机交付 Host 默认 {hash}.test.weline.com | confirmed | `1.2.16` | `1.2.16` | passed contract | [WebUI浏览器验收与交付地址门禁](../../Framework/doc/3-开发/WebUI浏览器验收与交付地址门禁.md)；MCP `feature_delivery_urls` |
| `REQ-AI-0006` | 汇报交付地址后必须关闭验收 Browser | confirmed | `1.2.17` | `1.2.17` | passed contract | [WebUI浏览器验收与交付地址门禁](../../Framework/doc/3-开发/WebUI浏览器验收与交付地址门禁.md) 门禁 D；MCP `browser_release_after_delivery` |
| `REQ-AI-0007` | 每次打开验收 Browser 必须禁用 HTTP 缓存 | confirmed | `1.2.18` | `1.2.18` | passed contract | [WebUI浏览器验收与交付地址门禁](../../Framework/doc/3-开发/WebUI浏览器验收与交付地址门禁.md) 门禁 A WB-CACHE；MCP `browser_cache_disabled_on_open` |
| `REQ-AI-0008` | MCP 正式提供可按需拉取的工程技能 | confirmed | `1.2.30` | `1.2.30` | passed contract | [统一知识库与MCP协议](./统一知识库与MCP协议.md)；`mcp-skills.v1` / `mcp_skills_fetch_from_mcp` |
| `REQ-AI-0009` | CSS/主题任务必须齐读 UI+原型+主题三技能 | confirmed | `1.2.32` | `1.2.32` | passed contract | [AI硬规则索引](./AI硬规则索引.md)；`css_or_theme_requires_ui_prototype_theme_skills` |
| `REQ-AI-0010` | 提取技能：doc 索引进 MCP + 打招呼列清单 | confirmed | `1.2.33` | `1.2.33` | passed contract | [统一知识库与MCP协议](./统一知识库与MCP协议.md)；`dev/ai-command/ai/提取技能.md` |
| `REQ-AI-0011` | 视觉 UI 信号强制原型/UI/主题参与 | confirmed | `1.2.70` | `1.2.70` | passed contract | [AI工程交付流程](./AI工程交付流程.md)；`ui_skill_surface_signal_gate` |
| `REQ-AI-0012` | AI 硬规则按主题拆分为六分册，索引只作调用范围与任务路由 | confirmed | `1.3.27` | `1.3.27` | passed contract | [AI硬规则索引](./AI硬规则索引.md) + [规则/](./规则/)；`route_via_hard_rules_index` |

## 当前有效需求

### `REQ-AI-0001` 场景图像生成参考媒体契约

- 来源与确认：用户于 `2026-08-11` 确认 PageBuilder 应参考给定视觉样例生成高质、背景透明的 Logo，并确认把通用参考媒体边界同步到官方框架仓库。
- 目标版本：`1.1.3`
- 背景/问题：`ScenarioImageGenerationGateway` 的 provider-neutral media allowlist 会过滤调用方提交的 `image`，导致已在会话摄入层选择的参考图无法到达图像生成 Provider；仅靠文字描述难以稳定复现参考图的金属倒角、酒红珐琅和徽章层次。
- 用户与运营场景：PageBuilder 或其他已发布接口调用方按场景发起图像生成时，可以在媒体契约中附带一个参考图值；运行时仍由场景配置决定模型、Provider 和账号。
- 业务逻辑：
  1. `ScenarioImageGenerationGateway::generate()` 从 `$mediaContract` 仅保留显式 allowlist 字段。
  2. `image` 是 provider-neutral 媒体字段，必须原样进入 `AiService::generateImage()` 的 `$params`。
  3. `model`、`model_code`、`provider`、`account`、`endpoint`、`api_key` 等路由或敏感字段继续在任意嵌套层级 fail-closed；新增 `image` 不得绕过该校验。
  4. 无 `image` 的既有文生图调用保持原行为。
  5. 参考图选择、背景透明化、Logo 文字策略、OCR/Vision 验收和精确品牌字合成属于调用模块；`Weline_Ai` 不引入 PageBuilder 业务依赖。
- 范围：通用 `image` 媒体参数透传、聚焦 PHPUnit 回归测试、公开契约文档。
- 非目标：不在核心模块实现 Logo 设计、去背、OCR、Vision、PHP 品牌字排版、模型切换、Provider 切换或账号修复；不把 `GuoLaiRen/PageBuilder` 代码合入框架仓库。
- 数据/权限/安全：不新增持久化字段、权限或凭据；调用方与下游 Provider 继续负责参考图内容、URL/字节格式及大小的既有校验。路由字段禁入规则保持不变。
- 兼容/迁移：纯向后兼容的可选媒体字段；无数据库、Model Schema 或路由迁移。`etc/module.php` 本次保持 `1.1.2`，目标版本仅作开发追踪，待正式发布流程决定版本文件。
- 验收标准：聚焦测试证明 exact `image` 值到达 `AiService::generateImage()`；错误删除 allowlist 项时测试失败；目标源码与测试通过 PHP lint；两仓会话候选文件普通磁盘 diff 对齐。
- WebUI/E2E：整体 PageBuilder 能力须在真实 WLS 的工作台点击 Logo 重生成，看到透明高质徽章、无重复模型烤字、资产 URL 更新，并保留截图；该产品门禁不能由核心单测替代。
- 运营与支持影响：无新增后台配置；若参考图未生效，先检查调用模块是否在 `$mediaContract['image']` 提交有效值，再检查场景 Provider 对参考图的支持与日志。
- 依赖与风险：Provider 对图生图参数的实际支持、PageBuilder 参考图摄入完整性、真实 WLS/WebUI 可用性。核心只保证媒体边界不再静默过滤该字段。

### `REQ-AI-0002` 统一项目知识库与强制 MCP 准备协议

- 来源与确认：用户于 `2026-08-23` 明确要求完整实施《Weline_Ai 统一知识库与 MCP 迁移计划》，包括独立 MCP 双端封版、旧 AI 规则清理、模块文档自动索引、Codex/Cursor 强制入口、Deploy 结构化桥接与旧集中式 AI 目录历史清理。
- 目标版本：`1.2.0`。
- 背景/问题：框架规范曾分散在旧集中式 AI 目录、静态 Skills、客户端镜像和模块派生索引中，AI 每次需要重复加载大段上下文；独立 `Weline-Codex-Mcp` 与重复知识模块又形成了多个维护源。
- 用户与运营场景：支持本地 STDIO MCP 的 AI 客户端在开始框架开发前自动启动内置 MCP，完成项目身份、模块清单、三文档契约与索引新鲜度检查；仅在返回有效 readiness 后按当前任务取得最小必要规范和源码上下文。
- 业务逻辑：
  1. `Weline_Ai/Mcp` 是 MCP 唯一维护源，保持 `LearningMcp` 命名空间、Apache-2.0 子目录许可、PHP 8.2 兼容和对 Weline/WLS/业务数据库零运行时依赖。
  2. `prepare_project` 返回 `project-readiness.v1` 的 `ready`、`needs_repair` 或 `blocked`；readiness 与项目 revision、模块清单、文档 Hash 和客户端会话绑定。
  3. 除 health、安装和修复说明外，知识与索引工具必须校验 `readiness_id`；项目或文档变化会使旧 readiness 失效。
  4. `resolve_task_context` 返回有来源与 Hash 的 `guidance-bundle.v1`；临时决定只记在会话笔记或宿主对话上下文，不写仓库、SQLite 长期知识或凭据。
  5. 每个有效知识单元长期维护 `doc/README.md`、`doc/需求.md`、`doc/开发日志.md` 与必要专题文档；派生索引仅在项目隔离 SQLite 中，外部文档修改必须在下一次工具调用可见。
  6. 根 `AGENTS.md` 是唯一中立入口且**唯一提交**的智能编辑器协议文件；Codex/Cursor/Claude/VS Code 等编辑器私有协议与 MCP 注册（`.cursor/`、`.codex/`、`CLAUDE.md`、`.mcp.json` 等）不进入 Git，由 Agent 在本机按 ensure 安装。
  7. 现有 `Weline_Ai` Agent、Scenario、Provider、业务 Skill、Style、导入导出和后台功能保持兼容；重复知识模块退役，但不静默删除可能存在的 `ai_knowledge_call_history` 生产表。
  8. 部署能力只经 `Weline_Deploy` 公开 JSON CLI 获取只读预检和计划；未指定目标/ref 类型或未明确授权时不得真实发布。
  9. 同一宿主只允许一个 Weline MCP 服务器注册：Codex 与 Cursor 共用引导脚本维护的显式 STDIO 注册；Codex 插件只提供生命周期 Hook 与任务约束，不再声明第二个 MCP 服务器。
  10. MCP 进程必须公开 PID、启动时间、加载源码代次与当前磁盘代次；宿主启动 Epoch 必须由 `ps etime` 推导，不能把无时区 `lstart` 按 PHP 默认时区解析。源码变化后的旧进程拒绝 `prepare_project` 等业务调用。仅 Hook 的 Codex 插件产物刷新在当前 STDIO MCP 与宿主运行代次健康时后台收敛且不阻断；实际 MCP 注册变化或运行代次过期仍必须重载后才返回 `ready`。
  11. MCP 的自愈、宿主重载、插件生成与回滚不得丢弃任何既有 tracked、staged、untracked 或 ignored 脏改；**宿主 Agent Shell 禁止**为清场便利 `git checkout --` / restore / clean / stash 擦脏；编辑须 **dirty-load** 当前磁盘脏改再改，禁止用其它会话/对话旧版本写回导致相互覆盖；所有内部 Git 子进程 fail-closed 且只允许只读检查，禁止分支切换、config/helper/pager 注入和所有 Git 写操作。
- 范围：内置 MCP、readiness/guidance/session 协议、模块知识扫描与 SQLite 派生索引、统一客户端入口、旧知识系统退役、迁移/发布/历史清理工具与文档。
- 非目标：不新增知识管理后台 UI；不重排模块业务源码；不把临时会话决定自动晋升为长期规范；不在本需求中执行任何真实预发/生产部署。
- 数据/权限/安全：索引按规范化项目目录隔离；修复文档必须显式授权；会话笔记和凭据不落盘；Deploy 默认只读；旧历史表只提供显式清理说明。
- 兼容/迁移：独立 MCP `v0.12.2` 先发布，迁入后导出最终 `v0.13.0` 并冻结独立仓；旧 `resolve_skill/get_skill` 暂作动态知识别名；业务 AI 契约保持兼容。
- 验收标准：当前 Git 树可证明的 92 个运行时模块与 1 个 Base Composer 聚合包（共 93 个有效知识单元）满足三文档契约；缺文档阻断且修复可控；readiness 失效/并发/回滚/外部编辑新鲜度测试通过；同一任务只出现一个 Weline MCP 命名空间，宿主重启后 PID 与启动时间更新且 `loaded_generation=current_generation`；Codex/Cursor 全新克隆门禁通过；Weline 真实升级无业务回归；双远端版本和重写后 refs 一致。
- WebUI/E2E：MCP 本身以真实 STDIO 客户端和 CLI 全栈验收；Weline_Ai 既有后台能力在真实 WLS 做回归冒烟。不存在知识管理新 UI。
- 运营与支持影响：模块维护者只更新归属模块文档；客户端启动失败或 `needs_repair` 时按确定性 Bundle 修复；独立 MCP `v0.13.0` 后不再维护。
- 依赖与风险：客户端必须支持本地 STDIO MCP；历史重写会改变受影响分支后代 SHA；托管平台失联对象 GC 时间不可控。

### `REQ-AI-0005` 本机交付 Host 默认 {hash}.test.weline.com

- 来源与确认：用户于 `2026-09-07` 明确要求默认域名为 `{项目hash}.test.weline.com`，并要求 MCP 与本地文档都描述清楚。
- 目标版本：`1.2.16`。
- 业务逻辑：主验收/交付 URL 默认 Host 为 `{project_hash}.test.weline.com`；禁止把 `*.weline.test` 当作主验收 Host；仅无前者时回退 `127.0.0.1`。
- 范围：MCP `feature_delivery_urls` / `closeout_delivery_reminder` / `webui_browser_closeout`、硬约束、`AI硬规则索引`、`AI工程交付流程`、`AI开发治理`、Framework WebUI 门禁、Cursor `local-browser-urls` 技能与规则。
- 验收标准：`guidance-workflow-contract.php` 断言 `default_local_host`、禁止 `*.weline.test`、closeout 摘要与 webui 表面规范通过。
- WebUI/E2E：N/A（规范与契约）。

### `REQ-AI-0006` 汇报交付地址后必须关闭验收 Browser

- 来源与确认：用户于 `2026-09-07` 明确要求验收 Browser 用完自行关闭，且每次汇报交付地址后关闭，避免残留 webview 卡顿。
- 目标版本：`1.2.17`。
- 业务逻辑：WB-OP 完成后，面向用户写出「交付地址」小节，然后立即关闭本回合打开的全部验收 Browser（Cursor：unlock + `browser_tabs` close）。用户明确要求保留除外；从未打开记 N/A。
- 范围：MCP `browser_release_after_delivery` / `closeout_delivery_reminder` / `webui_browser_closeout`、硬约束、`AI硬规则索引`、`AI工程交付流程`、`AI开发治理`、Framework WebUI 门禁、Cursor `local-browser-urls` 技能与规则。
- 验收标准：`guidance-workflow-contract.php` 断言硬规则 id、closeout `browser_release_*`、mandatory closeout 项与 webui 表面 norm 通过。
- WebUI/E2E：N/A（规范与契约；本条不新开验收页）。

### `REQ-AI-0007` 每次打开验收 Browser 必须禁用 HTTP 缓存

- 来源与确认：用户于 `2026-09-07` 明确要求：无法清整盘缓存时，改为规则限制——每次打开验收 Browser 都禁用缓存。
- 目标版本：`1.2.18`。
- 业务逻辑：每次打开/导航验收页前禁用该会话 HTTP 缓存（Cursor：`Network.setCacheDisabled`；被拒则 `Page.reload({ignoreCache:true})` 降级）。不要求清空整个浏览器用户配置。禁止用默认磁盘缓存验本回合 CSS/JS/HTML。
- 范围：MCP `browser_cache_disabled_on_open` / `closeout_delivery_reminder.browser_open_order` / `webui_browser_closeout`、硬约束、`AI硬规则索引`、`AI工程交付流程`、`AI开发治理`、Framework WebUI 门禁、Cursor `local-browser-urls` 技能与规则。
- 验收标准：`guidance-workflow-contract.php` 断言硬规则 id、closeout `browser_cache_*` / `browser_open_order`、webui 表面 norm 通过。
- WebUI/E2E：N/A（规范与契约；本条不新开验收页）。

### `REQ-AI-0008` MCP 正式提供可按需拉取的工程技能

- 来源与确认：用户于 `2026-09-08` 确认「MCP 自己提供技能，AI 用时从 MCP 取」。
- 目标版本：`1.2.30`。
- 业务逻辑：
  1. `prepare_project.agent_guidance.mcp_skills` 下发 `mcp-skills.v1` 策略与目录摘要。
  2. `resolve_skill(task)` 匹配技能列表；`get_skill(skill_id|alias)` 返回 Markdown 正文。
  3. 技能由 workflow surfaces 编译；`static_skill_files=false`；不恢复仓库 Skill 投影。
  4. 宿主 Agent Skills 仅可选薄壳；权威在 MCP。
  5. 硬约束 id：`mcp_skills_fetch_from_mcp`。
- 范围：`McpSkillCatalog`、Intelligence/Tool/Readiness、硬约束、Ai 文档、契约测。
- 非目标：不向 Cursor Agent Store 自动安装 SKILL.md；不复活 `auto_generate_skills`。
- 验收标准：`mcp-skills-catalog.php`、`guidance-workflow-contract.php`、`project-readiness.php` 相关断言通过。
- WebUI/E2E：N/A。

### `REQ-AI-0009` CSS/主题任务必须齐读 UI+原型+主题三技能

- 来源与确认：用户于 `2026-09-08` 要求——以后只要提到 CSS 和主题，必须看 UI 技能、原型技能和主题技能，避免主题开发跑偏。
- 目标版本：`1.2.32`。
- 业务逻辑：任务/需求含 CSS 或主题/theme 时，写样式前必须加载 `frontend-design`、`prototype`、`weline-theme-development`（MCP `get_skill`）；主题 Token 仍优先。硬约束 id：`css_or_theme_requires_ui_prototype_theme_skills`。
- 验收标准：`guidance-workflow-contract.php` / `mcp-skills-catalog.php` 断言通过。
- WebUI/E2E：N/A。

### `REQ-AI-0010` 提取技能：doc 索引进 MCP + 打招呼列清单

- 来源与确认：用户于 `2026-09-08` 要求指令「提取技能」——从框架/模块 doc 技能目录映射到 MCP 技能索引并列全；打招呼 hi 列出 MCP 技能与指令。
- 目标版本：`1.2.33`。
- 业务逻辑：只读扫描 `doc/ai/INDEX.json`、`doc/ai/skills/**/SKILL.md`，并为有 `AI-INDEX.md` 的模块合成定位技能（MCP 内存，不写仓库投影）；`resolve_skill(list_all)` / 指令「提取技能」列全；打招呼列出技能+`dev/ai-command` 指令。硬约束：`greeting_lists_mcp_skills_and_commands`。
- 验收标准：`doc-skill-extract.php` 通过。
- WebUI/E2E：N/A。

### `REQ-AI-0011` 视觉 UI 信号强制原型/UI/主题参与

- 来源与确认：用户于 `2026-09-12` 要求——开发中原型技能常不参与调页；希望需求能被原型与 UI 正确规划出符合 Weline 风格的方案，并更新到 MCP。
- 目标版本：`1.2.70`。
- 业务逻辑：工程计划检测 goal/requirements/implicit/scope 中的视觉 UI 信号（`.phtml`/`.css`/view 模板、改UI/调样式/页面布局/信息架构/主题等）时，拒绝 `ui_skill_decision=skip`，强制 `participate`，且 `skill_participation` 必须含 `prototype`、`frontend-design`、`weline-theme-development`，并规划 `type=shentu`。纯后端/API/Provider/MCP 门禁无视觉信号仍可 skip（须 rationale）。硬约束 id：`ui_skill_surface_signal_gate`。
- 验收标准：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php` 通过。
- WebUI/E2E：N/A（契约门禁）。

## 版本变更记录

### 1.2.70 — 2026-09-12

| 需求 ID | 变更前 | 变更后 | 业务逻辑/原因 | 用户决定 | 影响范围 |
|---|---|---|---|---|---|
| `REQ-AI-0011` | 仅靠 Agent 自判 participate/skip，易错误 skip | 视觉 UI 信号机器强制 participate + 三技能 | 原型/UI 不参与导致风格跑偏 | confirmed | HardConstraints、Guidance、ToolService、Ai 文档、契约测 |

### 1.2.32 — 2026-09-08

| 需求 ID | 变更前 | 变更后 | 业务逻辑/原因 | 用户决定 | 影响范围 |
|---|---|---|---|---|---|
| `REQ-AI-0009` | 仅「用 UI 技能时叠加主题技能」 | 提到 CSS/主题即强制 UI+原型+主题三技能齐读 | 避免主题开发跑偏 | confirmed | HardConstraints/GuidanceWorkflow/McpSkillCatalog、Ai 文档、契约测 |

### 1.2.31 — 2026-09-08

| 需求 ID | 变更前 | 变更后 | 业务逻辑/原因 | 用户决定 | 影响范围 |
|---|---|---|---|---|---|
| `REQ-AI-0008` | `resolve_skill`/`get_skill` 仅为 Guidance Bundle 别名 | MCP `mcp-skills.v1` 目录 + 按需取正文 | 用户要求 MCP 提供技能、AI 用时再取 | confirmed | McpSkillCatalog、Intelligence/Tool/Readiness、硬约束、Ai 文档、契约测 |

### 1.2.18 — 2026-09-07

| 需求 ID | 变更前 | 变更后 | 业务逻辑/原因 | 用户决定 | 影响范围 |
|---|---|---|---|---|---|
| `REQ-AI-0007` | 验收 Browser 默认可读磁盘缓存 | 每次打开/导航前必须禁用 HTTP 缓存 | 无法清整盘缓存；避免旧静态资源误判 | confirmed | HardConstraints/GuidanceWorkflow、Ai/Framework 文档、契约测、Cursor 技能/规则 |

### 1.2.17 — 2026-09-07

| 需求 ID | 变更前 | 变更后 | 业务逻辑/原因 | 用户决定 | 影响范围 |
|---|---|---|---|---|---|
| `REQ-AI-0006` | 规范强制开 Browser 验收，但未要求关闭 | 写完「交付地址」后必须关闭本回合验收标签 | 残留 Glass/ide-browser 空转导致本机卡顿 | confirmed | HardConstraints/GuidanceWorkflow、Ai/Framework 文档、契约测、Cursor 技能/规则 |

### 1.2.16 — 2026-09-07

| 需求 ID | 变更前 | 变更后 | 业务逻辑/原因 | 用户决定 | 影响范围 |
|---|---|---|---|---|---|
| `REQ-AI-0005` | 交付 Host 易误用 `*.weline.test`；MCP/文档表述不齐 | 默认 `{project_hash}.test.weline.com`；MCP+本地全文对齐 | 用户要求 MCP 与本地都描述清楚 | confirmed | GuidanceWorkflow/HardConstraints、Ai/Framework 文档、契约测、Cursor 技能/规则 |

### 1.2.0 — 2026-08-23

| 需求 ID | 变更前 | 变更后 | 业务逻辑/原因 | 用户决定 | 影响范围 |
|---|---|---|---|---|---|
| `REQ-AI-0002` | 规范、静态 Skills、客户端镜像与多个 MCP/知识模块并存 | `Weline_Ai` 内置唯一 MCP，按 readiness 和任务动态提供最小知识 | 降低重复维护与上下文消耗，并让所有兼容 AI 使用同一框架知识入口 | confirmed | `Weline_Ai/Mcp`、模块 docs、AI 客户端适配、Deploy CLI、旧系统退役与发行流程 |
| `REQ-AI-0002` | Codex 插件与显式配置重复启动 MCP，自检只证明独立 STDIO 可用；无时区 `ps lstart` 被 PHP 按 UTC 解析后会把旧宿主误判为未来启动 | 单一共享注册；以 `ps etime` 推导宿主启动 Epoch；旧进程按源码代次拒绝调用；实际 MCP 注册或运行代次过期时一次性重启；仅 Hook 插件产物刷新在健康运行代次上非阻断收敛 | 消除旧句柄、重复命名空间和“假 ready”，同时避免无效重启打断当前任务 | confirmed | `Mcp/scripts/install.php`、`ensure-project-guidance.php`、`project-guidance-runtime-time.php`、`project-guidance-reload-policy.php`、`ToolService.php` |
| `REQ-AI-0002` | MCP 脏改保护主要依赖调用者纪律 | 内部 Git 子进程统一 fail-closed 且只读；连普通分支切换也不由 MCP 执行，同时禁止 config/helper/pager 注入 | 用户要求 MCP 绝对不允许丢弃脏改 | confirmed | `GitSafetyPolicy.php`、`ProcessRunner.php`、`ensure-project-guidance.php`、硬约束与聚焦测试 |

### 1.1.3 — 2026-08-11

| 需求 ID | 变更前 | 变更后 | 业务逻辑/原因 | 用户决定 | 影响范围 |
|---|---|---|---|---|---|
| `REQ-AI-0001` | 固定需求文档缺失；场景图像媒体 allowlist 不包含 `image` | 接受 provider-neutral `image`，且保持路由字段禁入和调用模块验收职责 | 让参考图到达已配置的图像 Provider，同时不把 PageBuilder 语义带入核心 | confirmed | `ScenarioImageGenerationGateway`、聚焦单测、模块文档 |

## 待确认

| 项目 | 现有证据 | 不确定点 | 需要谁确认 | 阻断范围 |
|---|---|---|---|---|
| `Weline_Ai` 1.1.2 以前的完整历史需求 | 当前 README、API 文档、源码与模块版本 | 旧版本的原始产品决策和逐版验收证据未进入固定需求文档 | 模块维护者 | 不阻断 `REQ-AI-0001`；仅阻断对旧历史的完整追溯声明 |
