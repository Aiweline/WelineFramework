## 波 2.1 — 破坏性操作「慎重」升格（用户确认）

开篇新增 **「破坏性操作高压线」**：Factory Reset 清库表清单、禁自写 SQL 删 scope、禁改 generated、排障默认顺序（先禁缓存/binding/compile，**禁止先 Reset**）。席位 prompt / surface norm 同步加重。

---

## 波 2 已补入（摘要）

| 项 | 落点 |
|----|------|
| `theme:scope:migrate` 幻影命令 | § theme_binding + scope-switching.md 纠偏 |
| scan ≠ disk / publish-not-found / ui:audit | § 编译矩阵 |
| Weline.Api / JS declare / 浮层 | § Browser·JS·浮层 |
| Editor 双入口 + 预览同构 | § Editor 双入口 |
| layoutType / 结构缓存键 / 度量 Token | 对应 P0 节 |
| Factory Reset 清库级 | 开篇高压线 + § 生成物禁区 |

## 仍属触及再读

Editor 全树、反色 chrome、Hook 新建、media_image、backend README、`theme:remove` 现状、Theme→I18n 禁 Model。
