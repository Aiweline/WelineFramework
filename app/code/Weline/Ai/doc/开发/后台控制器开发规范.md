# Weline框架后台控制器开发规范

## 控制器构造函数规范

### ❌ 错误做法
```php
public function __construct(AiModel $aiModel, ModelCollector $modelCollector) {
    $this->aiModel = $aiModel;
    $this->modelCollector = $modelCollector;
    parent::__construct(); // ❌ 错误！父类没有__construct()方法
}
```

### ✅ 正确做法
```php
public function __construct(AiModel $aiModel, ModelCollector $modelCollector) {
    $this->aiModel = $aiModel;
    $this->modelCollector = $modelCollector;
    // ✅ 不需要调用parent::__construct()
}
```

## 关键要点

1. **Weline框架的继承链**：
   ```
   YourController → BackendController → PcController → Core
   ```

2. **父类都没有 `__construct()` 方法**：
   - `BackendController` 没有 `__construct()`
   - `PcController` 没有 `__construct()`
   - `Core` 没有 `__construct()`

3. **初始化机制**：
   - `__construct()` → 仅用于依赖注入
   - `__init()` → 用于实际的初始化工作

## 依赖注入失败处理

### 问题现象
```
Fatal error: Call to a member function select() on false
```

### 解决方案
```php
/**
 * 确保模型对象有效
 */
private function getAiModel(): AiModel {
    if (!$this->aiModel || $this->aiModel === false) {
        $this->aiModel = ObjectManager::getInstance(AiModel::class);
    }
    return $this->aiModel;
}

// 使用方式
public function index(): string {
    $models = $this->getAiModel()->reset()
        ->pagination($page, $pageSize)
        ->select()
        ->fetch();
}
```

## 后台模板布局规范

### ❌ 简化布局（不完整）
```php
<?= $this->getChildHtml('Weline_Admin::Backend/page-layout/main-content-before') ?>
<div class="container-fluid">
    <!-- 内容 -->
</div>
<?= $this->getChildHtml('Weline_Admin::Backend/page-layout/main-content-after') ?>
```

### ✅ 完整布局（推荐）
```php
<?php
/**@var \Weline\Framework\View\Template $this */
/**@var \Weline\Backend\Block\ThemeConfig $themeConfig */
$themeConfig = \Weline\Framework\Manager\ObjectManager::getInstance(\Weline\Backend\Block\ThemeConfig::class);
?>
<!DOCTYPE html>
<html lang='en' <?= ('rtl' === $themeConfig->getThemeModel()) ? " dir=\"rtl\"" : '' ?>>
<head>
    @template(Weline_Admin::common/head.phtml)
    <title>页面标题</title>
</head>
<body <?= $themeConfig->getLayouts() ?? ''; ?>>

<!--页头工具条 开始-->
@template(Weline_Admin::common/page/loading.phtml)
<!--页头工具条 结束-->

<!-- 开始 页面 -->
<div id="layout-wrapper">

    <!--页头工具条 开始-->
    <w:block class='Weline\Admin\Block\Backend\Page\Topbar'/>
    <!--页头工具条 结束-->

    <!-- ========== Topnav Start ========== -->
    <!--页头菜单栏 开始-->
    <w:block class='Weline\Admin\Block\Backend\Page\Topnav'/>
    <!--页头菜单栏 结束-->

    <!-- ========== Left Sidebar Start ========== -->
    <!--左侧菜单栏 开始-->
    @template(Weline_Admin::common/left-sidebar.phtml)
    <!--左侧菜单栏 结束-->

    <!-- ============================================================== -->
    <!-- 开始 主要内容 -->
    <!-- ============================================================== -->
    <div class="main-content">
        <div class="page-content">
            <div class="container-fluid">

                <!-- 页面标题 -->
                <div class='row'>
                    <div class='col-12'>
                        <div class='page-title-box d-sm-flex align-items-center justify-content-between'>
                            <h4 class='mb-sm-0'>
                                <i class="mdi mdi-icon me-2"></i>
                                页面标题
                            </h4>
                            <div class='page-title-right'>
                                <ol class='breadcrumb m-0'>
                                    <li class='breadcrumb-item'><a href='javascript: void(0);'>父级菜单</a></li>
                                    <li class='breadcrumb-item active'>当前页面</li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- 页面标题结束 -->

                <!-- 页面内容 -->
                <div class="row">
                    <div class="col-12">
                        <!-- 你的内容 -->
                    </div>
                </div>

            </div>
        </div>
    </div>
    <!-- 主要内容结束 -->

</div>
<!-- 页面结束 -->

<!-- 页脚 -->
@template(Weline_Admin::common/footer.phtml)

</body>
</html>
```

## 常见错误总结

1. **调用不存在的父类构造函数** → 导致报错
2. **依赖注入失败** → 使用ObjectManager作为备用方案
3. **使用简化布局** → 缺少完整的后台界面元素

## ORM方法规范

### ❌ 错误的方法名
```php
// 错误：使用了不存在的orderBy方法
$models = $this->aiModel->reset()
    ->orderBy(AiModel::fields_CREATED_TIME, 'DESC') // ❌ 错误！
    ->select()
    ->fetch();
```

### ✅ 正确的方法名
```php
// 正确：使用order方法
$models = $this->aiModel->reset()
    ->order(AiModel::fields_CREATED_TIME, 'DESC') // ✅ 正确！
    ->select()
    ->fetch();

// 多字段排序
$models = $this->aiModel->reset()
    ->order(AiModel::fields_SUPPLIER, 'ASC')
    ->order(AiModel::fields_NAME, 'ASC')
    ->select()
    ->fetch();
```

### 框架ORM方法参考
```php
// 基本查询方法
->where('field', 'value')           // 条件查询
->where('field', 'value', 'like')   // LIKE查询
->order('field', 'DESC')            // 排序（DESC/ASC）
->pagination($page, $pageSize)      // 分页
->select()                          // 执行查询
->fetch()                          // 获取结果
->find()                           // 查找单条记录
->load($id)                        // 根据ID加载
->save()                           // 保存数据
->delete()                         // 删除数据
```

## 最佳实践

1. **构造函数**：只进行依赖注入，不调用 `parent::__construct()`
2. **模型获取**：添加备用的ObjectManager获取机制
3. **模板布局**：使用完整的HTML结构和后台布局组件
4. **错误处理**：在控制器方法中添加try-catch错误处理
5. **ORM方法**：使用正确的方法名，如 `order()` 而不是 `orderBy()`
