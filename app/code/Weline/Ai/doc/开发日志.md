> **归档说明（2026-09-14）**：工程任务在 MCP 已挂载/可挂载时必须 `prepare_project` 并遵守 `hard_constraints`；编码用宿主原生编辑。2026-09-13「可选知识面」表述已由冷启动门禁收紧。下文压缩条目勿再当作活约定。

## 2026-09-17 — MCP：前端业务接口 = BinQuery 默认唯一（非回退）

- 问题：主题技能 / hard rule 未把 BinQuery 写成默认唯一通道，易被当成 HTTP 失败后的回退。
- 修：`browser_api_binquery_default` + `weline_api_not_raw_fetch`；`frontend_development` 描述/docs/forbidden；宿主薄镜像 `weline-theme-development`；`Weline.Api使用指南` 开篇；硬规则索引行；契约断言。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`；`php app/code/Weline/Ai/Mcp/tests/mcp-skills-catalog.php`。

## 2026-09-17 — AI 失败提示带供应商与模型

- 问题：主题配置 AI 翻译等 toast 只报 `API请求失败: 127.0.0.1:11434`，看不出用的哪家供应商/哪个模型。
- 修：`ErrorMessageHelper::formatAiCallFailureMessage`；`AiService` 非流式/流式包装统一带「供应商 + 模型」；`ExceptionFingerprint` 剥离新前缀以免限流桶分裂。
- 验证：UT（Helper + ExceptionLogThrottle）；CLI `AiService::generate(translategemma:12b)` → `AI生成失败（供应商：ollama_webui_test，模型：translategemma:12b）：…`。

## 2026-09-17 — MCP：计划正文只写背景/方案/细节（plan_content_focus_only）

- 问题：Plan Mode / 计划笔记常夹无关流程复述、愿景散文，主题飘逸。
- 修：硬规则 `plan_content_focus_only`——计划正文仅 **背景 + 方案 + 细节**；同步 `host_plan_mode_for_planning`、交付流程 §3、硬规则索引、澄清指令、workflow notes、契约断言。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`。

## 2026-09-16 — 浮层硬规则覆盖 Dialog / 选品弹出

- 扩大 `weline_ui_floating_primitives`：dialog、产品/媒体/图标选择器、MCP/Agent 前端弹出必须用 `Weline.UI.dialog` / floating 原语；禁止私造 modal。
- 同步 `AI硬规则索引`、`GuidanceWorkflowCatalog`；契约断言 summary 含 dialog/picker。

## 2026-09-16 — 产品优化：三子智能体（图+详情+翻译）硬门禁

- 父指令/技能改为必须并行开 ①`ecommerce-product-image` ②`ecommerce-detail-suite` ③`ecommerce-product-i18n`。
- 新增指令 `dev/ai-command/product/翻译优化.md`；硬规则与 `product_optimize_detail_suite_bundle` 同步。
- 测试：`mcp-skills-catalog.php` / `doc-skill-extract.php` 覆盖三分支。

## 2026-09-16 — 产品优化（父）包含详情优化（子）

- 纠正「产品优化=详情优化同入口」：父指令 `产品优化.md` + 技能 `ecommerce-product-optimize`；子 `详情优化.md` + `ecommerce-detail-suite`。
- 硬规则/bundle/契约测试对齐；`AI硬规则索引` 行更新。

## 2026-09-16 — 详情优化跳过标记 data-weds=xq

- 技能 §1.1 + 指令流程：`data-weds="xq"` / `<!--weds:xq-->`；有则跳过；收口必写。
- MCP bundle `skip_marker` + hard_constraints 摘要；契约断言 PASS。

## 2026-09-16 — 内容运营技能跳过 MCP（content_ops_skills_skip_mcp）

- 问题：产品优化/详情优化/新建文章等走 prepare_project + 全量技能索引，过重且非编码冷启动。
- 修：新增硬规则 `content_ops_skills_skip_mcp`；扩 `mcp_call_scope` / `mcp_skills_fetch_from_mcp` / coldstart / AGENTS / 指令与仓内技能标明 **SKIP MCP**，权威改为宿主 Read `doc/ai/skills` + `dev/ai-command`。
- 验证：`guidance-workflow-contract.php`、`mcp-skills-catalog.php`、`Mcp/tests/run.php`。

## 2026-09-16 — 指令/技能：新建文章（blog_article_methodology_gate）

- 新增 `dev/ai-command/blog/新建文章.md`；仓内技能 `Blog/doc/ai/skills/weline-blog-article/`；硬规则 `blog_article_methodology_gate`；bundle `blog_article_methodology_bundle`。
- `AI硬规则索引` 任务路由表增加「新建文章/审查文章」行；MCP 测试断言硬规则与 bundle 路径。

## 2026-09-16 — 指令：详情优化（product_optimize_triggers_detail_suite）

- 新增 `dev/ai-command/product/详情优化.md`；硬规则 `product_optimize_triggers_detail_suite`；bundle `product_optimize_detail_suite_bundle`；`DocSkillCatalog` hard_trigger=`product_pdp_url`。
- 触发：`详情优化`/`商详优化`/`产品优化`/`商品优化`；店面 `/product/` URL 须带优化意图（裸分享不误触）。
- 技能：宿主 `ecommerce-detail-suite` + `weline-image-pipeline.md`；`AI硬规则索引` 增行。
- 契约：`doc-skill-extract.php` / `mcp-skills-catalog.php` PASS。

## 2026-09-16 — MCP：预览与店面同逻辑（1.2.83）

- 收紧 `preview_storefront_delivery_parity`：预览/editor_mode 必须与真实店面走**同一套业务逻辑与交付路径**；禁止 early-return / 跳过 / 抽空 Hook、部件、浮层、嵌套 Hook、quick-add、自动定位等。
- 同步：`AI硬规则索引` 主题行；`guidance-workflow-contract` 断言 `SAME business logic` + `early-return`。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php` PASS。
- 版本：`1.2.82` → `1.2.83`

## 2026-09-15 — MCP：Playwright 仅正式 runner（1.2.82）

- 问题：Agent 用 `node -e` + `chromium.launch` 探活挂后台，留下 `chrome-headless-shell`（含店内音乐音频残留）。
- 修：硬规则 `e2e_playwright_formal_runner_only`——只允许 `php bin/w e2e:run` / `npx playwright test`；禁止临时探活；缺覆盖则补 `.spec.js`。对齐 WebUI 门禁 A2、硬规则索引、`GuidanceWorkflowCatalog`、`mcpInstructions`。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`；`context-response-budget.php` bootstrap ≤3200。
- 版本：`1.2.81` → `1.2.82`；ToolService `0.13.4` → `0.13.5`

## 2026-09-15 — MCP：站内 URL 必须 url 标签或 Url 类（1.2.81）

- 硬规则 `storefront_internal_url_via_url_helper`：模板 `@url`/`<url>`，PHP `Url::getUrl`；禁止 `'/' . $path`。
- 权威：`Framework/doc/4-内置标签/06-url标签使用指南.md`。
- 版本：`1.2.80` → `1.2.81`

## 2026-09-15 — MCP：提到翻译即覆盖默认网站已选语种（1.2.81）

- 问题：用户说「翻译」时 Agent 常只补 `en_US`；店面搜索 placeholder 在 ur_PK 等 locale 仍显示中文 source。
- 修：硬规则 `user_mentions_translation_all_default_website_locales`；`mcpInstructions` 短句；CSV 规范 / 硬规则索引 / 交付流程 / `module_i18n_csv` surface；收口项 `default_website_locales_translated_when_user_asks_translation`。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`；ToolService `0.13.4`；`context-response-budget` bootstrap ≤3200。
- 版本：`1.2.79` → `1.2.81`

## 2026-09-15 — MCP：冷启动上下文丢失自愈 + 禁自写 rules

- 问题：长会话压缩后 MCP 引导易丢；有人想让 Agent 每会话自写 `.cursor/rules` 记住引导，易分叉且违反 `host_editor_rules_mcp_generated_only`。
- 修：`HostEditorRulesGenerator::coldStartMdc` 增补「上下文丢失须重 prepare」与「禁止为记住引导而手写 rules」；硬规则索引 `mcp_call_scope` 加指针；ensure 同步 coldstart；配套 User Rule 短指针与个人技能 `weline-mcp-coldstart`（宿主侧，细则仍只在 prepare）。
- 验证：`php app/code/Weline/Ai/Mcp/tests/host-editor-rules-generator.php`；ensure 写出校验。
- 版本：`1.2.78` → `1.2.79`

## 2026-09-14 — MCP：冷启动强制遵守 hard_constraints

- 问题：新会话不调 `prepare_project`，只在被点名时才遵守 MCP 规则；alwaysApply 几乎空着。
- 修：`mcp_call_scope` 工程任务强制 ensure→prepare→遵守 hard_constraints；新增 `HostEditorRulesGenerator`，ensure 写出 `.cursor/rules/weline-mcp-coldstart.mdc`；收紧 AGENTS / 硬规则索引 / 交付流程 / session_startup_notices / MCP instructions；ToolService `0.13.3`；Ai `1.2.78`。
- 验证：`php app/code/Weline/Ai/Mcp/tests/host-editor-rules-generator.php`；`guidance-workflow-contract.php`；`host-mcp-registration-contract.php`；`run.php --quick`。
- 版本：`1.2.77` → `1.2.78`

## 2026-09-13 — MCP：简单可跳计划 / 需求必验收 / 前后端分析 / 审图强制原型+UI

- 问题：计划过重；跳 e2e 后无本机 Browser；未分析前后端；布局/吐槽/审图时常只点评不调整。
- 修：`host_plan_mode_for_planning` 允许 simple skip；新增 `requirement_acceptance_always`、`requirement_fe_be_scope_analysis`；无 e2e 仍须 WB-OP 视觉+逻辑；布局调整/不够人性化/被吐槽/审图强制 prototype+frontend-design 调整。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`。
- 版本：`1.2.76` → `1.2.77`

## 2026-09-13 — MCP：计划阶段强制宿主 Plan Mode（host_plan_mode_for_planning）

- 问题：澄清/规格后仍常在 Agent 模式边计划边写码。
- 修：硬规则 `host_plan_mode_for_planning`——计划/架构阶段须启用宿主 Plan Mode（Cursor `SwitchMode`→`plan`），用户批准实现后再切 `agent`；无 Plan Mode 记 `unavailable`+理由；同步交付流程 §1c/§3、澄清指令、mandatory_before_code、契约测试。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`。
- 版本：`1.2.75` → `1.2.76`

## 2026-09-13 — MCP：需求澄清与用例规格（requirement_clarify_use_case_spec）

- 问题：有隐形需求/审视/e2e/交付地址门禁，缺 Kiro/Spec Kit 式「澄清 + EARS + 用例」产品形态，易一句话直改码。
- 修：新增指令 `dev/ai-command/ai/需求澄清与用例规格.md`；硬规则 `requirement_clarify_use_case_spec`；workflow surface/skill `requirement_clarify_use_case`（别名 `weline-req-clarify`）；feature 须落盘 `doc/开发/spec/{slug}.md` 至 `ready-for-plan`；同步交付流程 §1b、硬规则索引、mandatory_before_code、CHECKLIST、契约测试。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`；`mcp-skills-catalog.php`。
- 版本：`1.2.74` → `1.2.75`

## 2026-09-13 — AI 运输层/BUSY 安静异常（防 exception.log 刷屏）

## 2026-09-13 · MCP 索引面文档复查

- 复查并清理文档/引导中仍像活约定的写码路径表述；统一为可选知识面 + 宿主原生编辑。
- 验证：`tests/run.php --quick`。

## 2026-09-13 · MCP 定位对齐（索引 / 代码地图）

- 约定：MCP = 可选技能索引 + 代码地图 + 领域硬规则下发；编码用宿主原生编辑。
- 改写：硬约束目录、引导目录、`AGENTS.md`、Ai/MCP 权威文档措辞。
- 保留：Theme/Taglib/Payment/e2e/汇审等纯领域硬规则；`preserve_dirty_workspace` 禁止擦脏。
- 未改：各模块历史开发日志；未 git commit。

## 2026-09-13 — MCP：功能页分组强制顶部 Tab + 展开卡片（feature_ui_keep_simple_top_tabs）

- 问题：Agent 仍常把列表/表单/字典堆同一长页，信息压力大；原型未默认顶部 Tab。
- 修：加强 `feature_ui_keep_simple_top_tabs`——所有页面分组默认顶部 Tab；次要块用点击展开卡片（默认收起）；不符合密页须同需求改 Tab；审图 E 清单对齐；交付流程/硬规则索引同步。
- 范例落地：Shipping 免邮规则页 `section=rules|create|conditions` 页内顶部 Tab。
- 验证：``；免邮 e2e。
- 版本：`1.2.72` → `1.2.73`

## 2026-09-13 · （已归档）MCP 索引化退役说明

- 覆盖原 2026-08-30～2026-09-12 间已退役章节（共压缩 10 条）；MCP 现仅索引/技能/规则检索。
- 领域硬规则、e2e、翻译、主题等业务条目仍保留于下文各日记录。

## 2026-09-12 — MCP/E2E：默认无头（e2e_playwright_headless_default）

- 问题：`php bin/w e2e:run` 未传 `--headless` 时自动追加 `--headed`，Agent 自测总弹 Chromium。
- 修：`Test/Console/E2e/Run`（及遗留 UnitTest 路径）默认 `PLAYWRIGHT_HEADLESS=1`，仅显式 `--headed`/`--ui` 有界面；`tests/e2e/playwright.config.js` / cart 配置默认 headless；硬约束 `e2e_playwright_headless_default`；门禁 A2 / 硬规则索引对齐。
- 验证：`vendor/bin/phpunit app/code/Weline/Framework/Test/Unit/Console/E2eRunArgumentsTest.php`；`php bin/w e2e:run --help`。
- 版本：`1.2.71` → `1.2.72`

## 2026-09-12 — MCP：横切触点清单 impact_surfaces（requirement_cross_layer_impact_gate）

- 问题：隐形需求仅自由文本，Agent 对「给订单增加类型」易字面只改一层，漏前后台/API/i18n/测试。
- 修：新增 `plan.impact_surfaces` + 硬规则 `requirement_cross_layer_impact_gate`；实体+字段+变更信号命中时强制层清单（schema_model/service_api/admin_ui|storefront_ui/i18n/tests_e2e），拒绝空/无式隐形需求，in_scope 对齐 dev_tasks；`framework_candidates.impact_candidates`；CHECKLIST 增 cross_layer 三项；同步 Gate/Workflow/ToolService/Guidance/交付流程与硬规则索引。
- 验证：``（含订单加类型红绿样例）。
- 版本：`1.2.70` → `1.2.71`

## 2026-09-12 — MCP：视觉 UI 信号强制原型/UI/主题参与（ui_skill_surface_signal_gate）

- 问题：Agent 常对页面/布局/CSS 需求错误设 `ui_skill_decision=skip`，原型与 UI 技能不参与，风格跑偏。
- 纠偏：非「凡 feature 强制原型」；改为机器检测视觉信号（`.phtml`/`.css`/view 模板、改UI/调样式/信息架构/主题等）时拒绝 skip，并强制 `prototype`+`frontend-design`+`weline-theme-development`+`type=shentu`；纯后端仍可 skip。
- 落点：`GuidanceWorkflowCatalog`::planSignalsUiSkillParticipate`、`HardConstraintsCatalog`::FEATURE_REQUIRED_SKILLS`、硬规则 `ui_skill_surface_signal_gate`、Guidance/ToolService、交付流程与硬规则索引。
- 验证：``。
- 版本：`1.2.69` → `1.2.70`

## 2026-09-12 — MCP：architecture_design 结构化 + framework_candidates

- 问题：`architecture`/`requirement_scrutiny` 靠散文与关键词易套话通过；Agent 未先读框架候选就选型。
- 修：必填 `architecture_design`（mechanism/owning_module/reuse/invent/not_to_do/req_map）与 `scrutiny_basis`（checklist≥2 + doc_paths≥1）；`resolve_task_context` 注入 `framework_candidates.v1`；反模式机检（手写 select、跨模块 new Service/Model）；硬规则 `architecture_design_structured`；对齐 Gate/Workflow/ToolService/交付流程。
- 验证：``；`guidance-workflow-contract.php`。
- 版本：`1.2.68` → `1.2.69`

## 2026-09-12 — MCP：章完整通路 e2e + 计划组套件（1.2.68）

- 问题：可仅有全局一条 e2e 或半截章节就汇报；未强制收口前统一组测整条功能链路；易甩测给用户。
- 修：硬规则 `plan_full_pathway_e2e_suite`；feature 须每章通路 e2e + `e2e-plan-suite`；Gate/Workflow 缺口 `feature_plan_suite_e2e_*` / `chapter_e2e_pathway_weak`；同步交付流程、WebUI 门禁 A2、硬规则索引。
- 验证：``。
- 版本：`1.2.67` → `1.2.68`

## 2026-09-12 — MCP：章节 acceptance_ids 硬绑定闭环

- 问题：章节 Done 不要求绑定验收完成；「每章=e2e闭环」仅靠标题关键词，可与全局单条 e2e 脱节。
- 修：`dev_tasks.acceptance_ids`（+`covers_requirements`）硬绑定；feature 章须含互斥 e2e；submit/progress/review 拦截缺绑定、共用 e2e、未完成验收就 done；同步硬规则与交付流程。
- 验证：。
- 版本：`1.2.66` → `1.2.67`

## 2026-09-12 — MCP：运行/状态查询默认本机（runtime_status_query_local_first）

- 问题：Agent 查定时翻译/队列状态时常因历史会话或 SSH「默认 profile=weline」误连生产；并无「翻译必须查线上」规定。
- 修：新增 operational 硬规则 `runtime_status_query_local_first`；`mcpInstructions` 增加 LOCAL-FIRST 指针；`session_startup_notices` 增双语提示；`AI硬规则索引` 增专节/路由/纠正清单；澄清 SSH 默认 profile≠查询目标默认值。
- 验证：`guidance-workflow-contract.php` 断言新规则与 instructions 含 LOCAL-FIRST。
- 版本：`1.2.64` → `1.2.65`

## 2026-09-12 — MCP：任何 feature 必须 e2e 自测 PASS，禁止甩测给用户

- 问题：仅 UI 路径强制 e2e；后端 feature 可用 unit/curl 收口，并出现「请刷新后再试」甩测给用户。
- 修：`planRequiresE2eAcceptance` → `work_kind=feature` 一律强制；收口仅接受 e2e `passed`+强 evidence（禁止 skipped/na）；新增硬规则 `forbid_user_manual_test_handoff`；拓宽 `ui_feature_requires_e2e` / `agent_self_verify_before_done`；同步启动提示、WebUI 门禁 A2、硬规则索引。
- 落点：`GuidanceWorkflowCatalog`/`HardConstraintsCatalog`/`HardConstraintsCatalog`/`GuidanceWorkflowCatalog`、契约测试、`WebUI浏览器验收与交付地址门禁.md`、`AI硬规则索引.md`。
- 验证：、`guidance-workflow-contract.php`。
- 版本：`1.2.63` → `1.2.64`

## 2026-09-11 — MCP 配置词统一命中 SystemConfig / config:embed

- 问题：MCP 检索仅窄命中「统一配置中心」，「配置/嵌入配置」等词未稳定落到框架统一配置与嵌入配置。
- 修：新增 `SystemConfigTermRouting` 词表；`ProjectRetriever` path-intent / `expandRetrievalQuery` / `expectedContextRoles` 接入；硬约束 `systemconfig_unified_config_terms`；`AI硬规则索引` 任务路由增补配置关键词行。
- 验证：`systemconfig-term-routing.php`、。

## 2026-09-11 — MCP 强制 UI 端到端（type=e2e / ui_feature_requires_e2e）

- 问题：UI 功能仅用 curl/CDP 冒充验收即可收口。
- 修：acceptance 增 `e2e`；participate / type=browser / feature+UI 路径强制 `type=e2e`；evidence 须含 `e2e:run`/`playwright`/`.spec.js`；curl/CDP-only → `e2e_evidence_weak`。
- 落点：`HardConstraintsCatalog`/`GuidanceWorkflowCatalog`/`HardConstraintsCatalog`/`ToolService`/`GuidanceWorkflowCatalog`、WebUI 门禁 A2、`AI硬规则索引`。
- 验证：、`guidance-workflow-contract.php`。
- 版本：`1.2.62` → `1.2.63`

## 2026-09-11 — 批量翻译截断 JSON 分块抢救（解析空转）

- 问题：词典 `batch_size=100` 一次打模型，`max_tokens` 不足时 JSON 截断 → `parse` 返回 0 → 整批失败空转。
- 修复：`MODEL_CHUNK_SIZE=20` 先 `array_chunk`；截断用 `salvageCompleteJsonArrayItems` 保留完整前缀并重试剩余；解析失败对半 `splitAndRetryBatchTranslate`（禁止多条串行 `translate`）；按输入抬高 `max_tokens`（≤8192）。
- 验证：`TranslationBatchChunkingContractTest`、`TranslationBatchNoSerialFallbackContractTest`、`TranslationBatchJunkParseTest`。
- 版本：`1.2.61` → `1.2.62`

## 2026-09-11 — 隐形需求分析与原型/UI 按分析决策（requirement_implicit_analysis_skill_decision）

- 硬规则：编码需求须分析当前环境隐形需求（`implicit_requirements`），再合理规划；`ui_skill_decision=participate|skip` 决定原型/UI 是否参与，禁止凡 feature 一律强制。
- 落点：`HardConstraintsCatalog`、`HardConstraintsCatalog`/`GuidanceWorkflowCatalog`、`ToolService`、`GuidanceWorkflowCatalog`、`McpSkillCatalog`、`AI硬规则索引`、`AI工程交付流程`。
- 验证：、`guidance-workflow-contract.php`、`mcp-skills-catalog.php`。
- 版本：`1.2.60` → `1.2.61`

# Weline_Ai 开发日志

## 2026-09-11 — 壳+Provider 业务同构硬规则（shell_provider_business_isomorph）

- 硬规则：Payment/Dropship（及同构壳）供应商/渠道业务逻辑必须落在 Extends Provider；壳 Controller 只编排，禁止按供应商重写业务。
- 落点：`HardConstraintsCatalog`、`AI硬规则索引`、Payment `payment-shell`/`provider-development`、Dropship `dropship-shell`/`provider-development`。
- 验证：`guidance-workflow-contract.php`；Dropship `ShellProviderIsomorphContractTest`。
- 版本：`1.2.59` → `1.2.60`

## 2026-09-11 — 功能页一页宜简、顶部 Tab 分段（feature_ui_keep_simple_top_tabs）

- 硬规则：功能 Web UI 一屏一个主职责；进度与配置等不同职责用顶部 Tab 切换，禁止默认堆同一长页。
- 落点：`HardConstraintsCatalog`、`AI工程交付流程`、`AI硬规则索引`、`guidance-workflow-contract` 断言；范例 I18n AI 翻译页。
- 版本：`1.2.58` → `1.2.59`

## 2026-09-11 — 选择性控件先查标签库（强化 taglib_before_hand_rolled_controls）

- 硬规则：写任何选择性/领域控件前必须先读 Taglib 场景映射表；架构上选项优先官方标签。
- 落点：`HardConstraintsCatalog`、`GuidanceWorkflowCatalog`、`AI硬规则索引`、`Taglib/场景映射表`（含货源仓映射选国）。
- 范例：Dropship 仓映射国家改为 `<w:theme:address selection=single levels=country>`。
- 版本：`1.2.57` → `1.2.58`

## 2026-09-10 — 加功能须先审当前图（feature_add_requires_current_ui_review）

- 硬规则：既有 Web UI 加功能前必须截当前页审图；现页已乱则同需求重设计。
- 落点：`HardConstraintsCatalog`、`AI工程交付流程`、`AI硬规则索引`、`dev/ai-command/theme/审图.md`。
- 同回合落地 I18n AI 翻译进度看板作范例。
- 版本：`1.2.56` → `1.2.57`

## 2026-09-10 — 业务范围继承写进硬规则

- `weline_business_scope_hierarchy` 增补 channel←store←website←global 继承（对齐 SystemConfig `getFallbackScopes`）
- 版本：`1.2.55` → `1.2.56`

## 2026-09-10 — 业务范围层级硬规则（weline_business_scope_hierarchy）

- 问题：Agent/产品易把「范围」理解成 URL 路径通配，忽略 Website→Store→Channel
- 变更：`HardConstraintsCatalog` 增 `weline_business_scope_hierarchy`；`AI硬规则索引` 任务路由 + 纠偏清单；权威指 `Websites/doc/store-saleschannel-scope.md`
- 验证：/ `guidance-workflow-contract.php`
- 版本：`1.2.54` → `1.2.55`

## 2026-09-12 — MCP workflow_contract.hard_rules 索引化

- 问题：`GuidanceWorkflowCatalog::contract()` 仍嵌入 `workflowHardRules()` 全文 ~36KB（与 prepare `hard_constraints.rules` 重复），`resolve_task_context` 易二次膨胀。
- 变更：新增 `HardConstraintsCatalog::workflowHardRulesRef()`（`hard-rules-ref.v1`：rule_ids + operational_ids + source 指针）；contract 改用 ref；`workflowHardRules()` 保留供测试/兼容校验全文意图。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`；体积探针 contract.hard_rules ≪ 36KB
- 未改 Model/Controller/event/hook/register，不 bump `etc/module.php`

## 2026-09-12 — MCP 引导包索引压缩（防上下文漂移）

- 问题：`prepare_project.agent_guidance` 约 130KB（`mcp_skills` ~72KB 含 description 重复 + greeting 再嵌一份；`workflow_contract.hard_constraints` 再整包嵌入 package ~44KB），易挤占上下文导致漂移。
- 变更（不丢意图）：
 1. `McpSkillCatalog::summary/policy`：catalog 改为 index-only（`skill_id/name/aliases/kind/surface_id`）；正文仍 `get_skill`/`definitions`
 2. `DocSkillCatalog::greetingCatalog`：改为 `workflow_preview` + `module_doc_count` + `commands`，去掉 description 重复树
 3. `GuidanceWorkflowCatalog::contract`：`hard_constraints` 改为指针（`source=prepare_project.agent_guidance.hard_constraints`）；全文仍由 `HardConstraintsCatalog::package()` 进 prepare
- 验证：`php app/code/Weline/Ai/Mcp/tests/mcp-skills-catalog.php`；`doc-skill-extract.php`；`guidance-workflow-contract.php`；体积探针 agent_guidance ~87KB
- 未改 Model/Controller/event/hook/register，不 bump `etc/module.php`

## 2026-09-10 — Cursor 调试 CSP tooling 写入 MCP 硬约束

- 规则 id：`cursor_debug_csp_developer_tooling`
- 要求：Agent 依赖 Cursor ingest（`127.0.0.1:7277`）前，确保 `app/etc/env.php` 的 `security.headers.csp_developer_tooling` 放行 connect-src；仅 DEV/DEBUG 响应 union
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 未改 Model/Controller/event/hook/register，不 bump `etc/module.php`

## 2026-09-10 — 框架审视纠偏硬门禁（requirement_framework_scrutiny）

- 问题：用户需求经框架审视后若不合理，Agent 仍可能字面照做，缺少强制纠偏与更合理做法。
- 变更：硬约束 `requirement_framework_scrutiny`；`plan.requirement_scrutiny` 必填（合理/无调整，或「问题+更合理做法」）；收口缺字段拒绝；收口强制「需求纠偏」小节；blueprint 增加审视步骤；mandatory_before_code/closeout 与 ToolService schema 对齐。
- 验证：``；`guidance-workflow-contract.php`
- 版本：文档/MCP 进程门禁；未改 Model/Controller/event/hook/register，不 bump `etc/module.php`

## 2026-09-10 — 框架解耦硬门禁 + 耦合提示汇报（framework_decoupled_only）

- 问题：改动能跳过框架扩展点信息直接耦合实现；发现耦合也不强制在汇报中提示。
- 变更：硬约束 `framework_decoupled_only`；`architecture` 须含框架+解耦信号；`coupling_findings` 必填（无则「无」）；收口缺 findings 拒绝；closeout 强制「耦合提示」小节；mandatory_before_code/closeout 与 ToolService schema 对齐。
- 验证：``；`guidance-workflow-contract.php`
- 版本：文档/MCP 进程门禁；未改 Model/Controller/event/hook/register，不 bump `etc/module.php`

## 2026-09-10 — 架构层映射需求硬门禁（architecture_first_for_requirements）

- 问题：`plan.architecture` 仅建议填写，Agent 易从需求直接跳到补丁。
- 变更：`HardConstraintsCatalog`/`GuidanceWorkflowCatalog` 将 architecture 升为硬必填（含 trivial，≥40 字，须映射 requirements 并含架构信号）；与收口缺 architecture 拒绝；硬约束 `architecture_first_for_requirements`；`mandatory_before_code` 增加 `architecture_mapped_to_requirements`；ToolService schema required 含 architecture；权威文档对齐。
- 验证：``；`guidance-workflow-contract.php`
- 版本：文档/MCP 进程门禁；未改 Model/Controller/event/hook/register，不 bump `etc/module.php`

## 2026-09-09 — 审图：非报错改 UI + 线稿/原型/CSS 联合流水线（1.2.54）

- 问题：附图触发仍偏弱；非报错图易被当插图只点评；缺线稿与原型→主题 CSS 同回合联合。
- 变更：`dev/ai-command/theme/审图.md` 增 `error_shot`/`ui_shot`、线稿抽取、联合流水线；`HardConstraintsCatalog` / `image_attachment_shentu_bundle` 对齐；宿主 `weline-ui-shentu`/`frontend-design`/`prototype` 描述增强触发词；硬规则索引摘要更新。
- 验证：`php app/code/Weline/Ai/Mcp/tests/mcp-skills-catalog.php`；`guidance-workflow-contract.php`
- 版本：`1.2.53` → `1.2.54`

## 2026-09-09 — 翻译车道死持有者重试抢锁（1.2.53）

- 问题：`disconnectStaleHolder` 发现死 PID 后仍抛 `AI_TRANSLATION_BUSY`，调用方空转。
- 变更：检测到死/过期持有者后 `continue` 重试 flock，不再 throw「已断开僵死占用」。
- 验证：`TranslationConcurrencyGateContractTest`
- 版本：`1.2.52` → `1.2.53`

## 2026-09-09 — 自行验证硬规则与 evidence 收口门禁（1.2.51）

- 问题：开发功能后 Agent 常只改代码不跑验收。
- 变更：硬规则 `agent_self_verify_before_done`；`passed|skipped|na` 必须非空 evidence，否则 update 拒绝、`closeout_allowed=false`；session/verify/closeout 提示对齐。
- 验证：`guidance-workflow-contract.php`；- 版本：`1.2.50` → `1.2.51`

## 2026-09-09 — 翻译全类型分车道 + 防空占锁饿死（1.2.50）

- 各产品面独立 flock：`dictionary` / `local-model` / `meta` / `file-asset` / `document` / `eav` / `inquiry` / `blog` / `taglib`。
- 防饿死：无非空待译词不 `acquire`；LocalModel 无待办不入队；空类型不再占共享通道挡住有词类型。
- 版本：`1.2.49` → `1.2.50`

## 2026-09-09 — Meta 翻译独立车道（1.2.49）

- MetaLocal 与词典/LocalModel 同属不同产品面；`LANE_META` → `meta-translate.lock`。
- 版本：`1.2.48` → `1.2.49`

## 2026-09-09 — 翻译闸门分车道（词典 ∥ LocalModel，1.2.48）

- 需求：词典 AI 与 LocalModel 字段翻译互不 `AI_TRANSLATION_BUSY`，可并行。
- 变更：`TranslationConcurrencyGate` 按 `lane` 分 flock（默认 `machine-translate.lock`；LocalModel `local-model-translate.lock`）；`TranslationService` / `TranslationRequestObserver` 透传 `concurrency_lane`；I18n LocalModel 传 `'local-model'`。
- 验证：`TranslationConcurrencyGateContractTest`、`LocalModelTranslationBatchContractTest`
- 版本：`1.2.47` → `1.2.48`

## 2026-09-09 — 硬规则：系统禁运等禁止手写 ISO（1.2.47）

- `AI硬规则索引`：地区/国家选择行补「系统禁运新增国家」与禁止手写 ISO text input；对齐 MCP `theme_address_for_region_pickers`。
- HardConstraints / GuidanceWorkflowCatalog / Taglib 场景映射同步。
- 版本：`1.2.46` → `1.2.47`

## 2026-09-09 — 翻译请求超时加大（隧道 Ollama，1.2.46）

- 现象：生产经 SSH 反向隧道调 `translategemma:12b`，非流式整段生成无中间字节，`low_speed_time=45` 触发 `Operation too slow`，词典队列失败。
- 变更：`TranslationService` `REQUEST_TIMEOUT_SECONDS`/`REQUEST_LOW_SPEED_SECONDS` → **900**；`TranslationConcurrencyGate::STALE_HOLD_SECONDS` → **960**；契约测试同步。
- 验证：`TranslationConcurrencyGateContractTest`
- 版本：`1.2.45` → `1.2.46`

## 2026-09-09 — 取消 cache_lookup_tier_process_shared_db（1.2.44）

- 用户纠正：不应要求业务类自做三层；进程内+驱动应落在 **缓存类**，同 key 读写清理同步；单读命中进程内不打驱动。
- 变更：删除 `HardConstraintsCatalog` 规则与 `workflowHardRules` 行；契约测试改为断言该 id **不存在**；`AI硬规则索引` / Framework 标准与统一缓存文档改为新设计口径（运行时改造另开）。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.43` → `1.2.44`

## 2026-09-09 — 缓存查找层级硬规则（cache_lookup_tier_process_shared_db，1.2.43）【已撤销】

- 曾新增 MCP 硬规则要求业务侧进程内→共享→库；**1.2.44 起取消**，见上条。
- 版本：`1.2.42` → `1.2.43`

## 2026-09-09 — 硬约束加强：禁止擦脏（preserve_dirty_workspace，1.2.42）

- 事故：Agent 为清场对脏模板执行 `git checkout --`，险丢未提交改动。
- 变更：`AI硬规则索引` / `AI开发治理` / `HardConstraintsCatalog.preserve_dirty_workspace` / MCP instructions 明确：**宿主 Agent Shell 同等禁止** checkout/restore/clean/stash 擦脏；编辑必须 **dirty-load** 磁盘哈希；契约测试断言 `Agent Shell` + `git checkout` + `dirty-load`。`AGENTS.md` 增指针。
- **未**手写 `.cursor/rules`（`host_editor_rules_mcp_generated_only`）。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.41` → `1.2.42`

## 2026-09-09 — ai:model:select 独立懒加载组件包（1.2.41）

- 审图：客服配置/config:embed 已输出 `<w:ai:model:select>` DOM，但 JS/CSS 仅打进默认模型页包，控件无法展开供应商筛选与搜索。
- 变更：拆出 `css/ai-model-select.css`；Theme `weline-ui-assets` 登记 `ai-model-select` 懒加载组件（对齐 language-select）。
- 版本：`1.2.40` → `1.2.41`

## 2026-09-08 — ai:model:select runtimeCallback 防 PHP 泄漏（1.2.40）

- 动态属性路径补 `runtimeCallback` + `buildMarkup`，避免 `renderRuntimeTag` echo 编译期 PHP 源码。
- 版本：`1.2.39` → `1.2.40`

## 2026-09-08 — 审图：截图不说默认 UI+原型硬审（1.2.39）

- 用户批评：空截图被当成确认上下文，未用 UI/原型技能审丑点。
- 变更：`审图.md` 增「仅附图/截图不说」硬默认；`HardConstraintsCatalog` / `McpSkillCatalog.silent_shot_default`；硬规则索引 + 宿主 `weline-ui-shentu` 描述。
- 版本：`1.2.38` → `1.2.39`

## 2026-09-08 — 审图：缺 UI/原型技能须提示并自行安装（1.2.38）

- 用户要求：审图若没有 UI 技能与原型技能，要提示，并由 Agent 自行处理。
- 变更：`dev/ai-command/theme/审图.md` 增「宿主 UI / 原型技能门禁」；`image_attachment_shentu_bundle.missing_host_skills_gate`；硬规则摘要 + 宿主 `weline-ui-shentu` 薄壳；本机将 `prototype` 同步进 Cursor Agent Store（与 `frontend-design` 并列）。
- 验证：`mcp-skills-catalog.php`；AgentStores 存在 `frontend-design`/`prototype`。
- 版本：`1.2.37` → `1.2.38`

## 2026-09-08 — 图片宽高 CLS 硬规则（1.2.37）

- MCP `image_explicit_width_height_css`：凡图片/`file:image` 须 HTML width+height（或 aspect_ratio）+ CSS `max-width:100%;height:auto`。
- 同步 `AI硬规则索引`、`GuidanceWorkflowCatalog` frontend surface、契约测试。
- 协作：FileManager 1.1.14 Taglib；Theme 2.2.249 foundation。
- 版本：`1.2.36` → `1.2.37`

## 2026-09-08 — 审图能力：人性化/美观/主题契合须用 UI+原型技能（1.2.36）

- 用户要求：审图须用 UI 技能与原型技能审查人性化、规范美观审美与主题契合，不符合则调整。
- 变更：`dev/ai-command/theme/审图.md` 增 E/F/G 清单与汇报项；`HardConstraintsCatalog` / `image_attachment_shentu_bundle` 写入 required_actions + review_dimensions；硬规则索引与宿主 `weline-ui-shentu` 薄壳对齐。
- 验证：`mcp-skills-catalog.php`、`guidance-workflow-contract.php`。
- 版本：`1.2.35` → `1.2.36`

## 2026-09-08 — 本机 Ollama 关闭后翻译请求快速失败（1.2.35）

- 观察：强制关闭 Ollama 后，正在 `curl` 的 `queue:run`（LocalModel）约 5–10s 自行退出，不会无限挂死；随后 cron 仍可能再派发，但无模型时也会较快结束。
- 根因：(1) `TranslationService` 传的 `timeout_seconds` 在 `AiService::generate` 未映射为 `timeout`，实际仍按默认 900/120 空等；(2) curl `Operation too slow` / `Couldn't connect` 被当成可重试错误，最多再拖 3 轮。
- 修复：`generate` 映射 `timeout_seconds`；翻译显式 `low_speed_time=45`、`connect_timeout=5`；OpenAI 兼容提供者对连接失败与低速超时不重试。
- 验收：`ProviderTimeoutPolicyTest`、`TranslationConcurrencyGateContractTest`；本机关 Ollama 后 worker 自行退出。
- 版本：`1.2.34` → `1.2.35`

## 2026-09-08 — 审图硬触发未注入修复（1.2.34）

- 根因：权威 `dev/ai-command/theme/审图.md` 已写「附图即审」，但 (1) Cursor 薄壳 `weline-ui-shentu` 描述误收窄为「仅店面零售/B2B」；(2) `HardConstraintsCatalog` / `mcp_skills` 未注入 `user_image_attachment_triggers_shentu`，Agent 可把附图当闲聊插图跳过。
- 修复：硬规则 + `image_attachment_shentu_bundle`；`DocSkillCatalog` 解析 `**硬触发**` 为 `hard_trigger.kind=image_attachment`；索引/指令正文对齐；宿主薄壳描述改为任意附图。
- 验证：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`、`mcp-skills-catalog.php`、`doc-skill-extract.php`。

## 2026-09-08 — 审图：发图即审 + 图类型分流

- 指令：`dev/ai-command/theme/审图.md` —— 用户消息含图片即触发；`web_ui` 按主题/UI/原型约定；其它图自行识别是否本仓前端，非前端不套店面修复。
- 交付地址：N/A（指令文档）。

## 2026-09-08 — 指令「提取技能」：doc 技能索引进 MCP 目录 + 打招呼列清单（1.2.33）

- 目标：从模块 `doc/ai` 技能索引与 `AI-INDEX` 映射开发技能到 MCP `mcp-skills`；`提取技能` / 打招呼 `hi` 列出全部技能与 `dev/ai-command` 指令。
- 实现：`DocSkillCatalog`；`McpSkillCatalog` 合并；`resolve_skill(list_all)`；硬约束 `greeting_lists_mcp_skills_and_commands`；指令 `dev/ai-command/ai/提取技能.md`。
- 验收：`php app/code/Weline/Ai/Mcp/tests/doc-skill-extract.php`；`mcp-skills-catalog.php`。
- 版本：`1.2.32` → `1.2.33`
- 交付地址：N/A（MCP 目录/指令，无 Web UI；本回合未开验收 Browser）。

## 2026-09-08 — CSS/主题任务必须齐读 UI+原型+主题三技能（1.2.32）

- 目标：凡提到 CSS 或主题/theme，写样式前必须加载 `frontend-design`、`prototype`、`weline-theme-development`（MCP get_skill），避免主题开发跑偏。
- 硬规则：`css_or_theme_requires_ui_prototype_theme_skills`；`frontend_development.required_companion_skills`；`mcp_skills.css_or_theme_skill_bundle`。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`；`mcp-skills-catalog.php`。
- 版本：`1.2.31` → `1.2.32`
- 交付地址：N/A（规则/契约，无独立 Web UI；本回合未开验收 Browser）。

## 2026-09-08 — 批量翻译禁止静默回退逐条（1.2.31）

- 问题：`batchTranslate` 解析失败时回退为 N 次 `translate()`，LocalModel 按语种合并后仍被打成逐条 Ollama 请求，吞吐起不来。
- 修复：加强 JSON 数组抽取；多条文案解析失败直接抛错；提高批量 `max_tokens`/`temperature=0.2`。单条仍可回退。
- 协作：`Weline_I18n` 1.0.30 LocalModel 按 locale 分块。
- 验收：`TranslationBatchNoSerialFallbackContractTest`。
- 版本：`1.2.30` → `1.2.31`

## 2026-09-08 — MCP 正式提供可按需拉取的工程技能（1.2.30）

- 目标：工程技能由 MCP `mcp-skills.v1` 提供；Agent 用 `resolve_skill` / `get_skill` 按需取正文；宿主 `SKILL.md` 仅可选薄壳。
- 实现：`McpSkillCatalog`；`IntelligenceService`/`ToolService` 接线；`prepare_project.agent_guidance.mcp_skills`；硬约束 `mcp_skills_fetch_from_mcp`。
- 验收：`php app/code/Weline/Ai/Mcp/tests/mcp-skills-catalog.php`；`guidance-workflow-contract.php`；`project-readiness.php` 相关断言。
- 版本：`1.2.29` → `1.2.30`
- 交付地址：N/A（MCP 协议/契约，无独立 Web UI；本回合未开验收 Browser）。

## 2026-09-08 — 「规格修复」指令收口：明细 + 交付地址（1.2.29）

- 调整：`dev/ai-command/product/规格修复.md` / 硬规则索引 / Cursor 技能要求汇报修复内容表，并为每个 fixed 商品附 Markdown PDP 地址。
- 版本：`1.2.28` → `1.2.29`

## 2026-09-08 — 硬约束：基础组件只用主题规范变量（1.2.28）


- 目标：主题开发时基础组件禁止私写 hex/rgb；默认色盘可继承。
- 硬规则：新增 `theme_base_components_token_only`；索引/交付流程/语义色矩阵对齐。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`；Theme 色盘契约测试。
- 版本：`1.2.27` → `1.2.28`
- 交付地址：N/A（规则/色盘契约，无独立 Web UI）。

## 2026-09-08 — 硬约束：UI 技能必须叠加主题技能（1.2.27）

- 目标：使用宿主 `frontend-design`/UI 技能写本仓界面时，必须同时读并服从 `weline-theme-development` 与主题 Token；禁止自造颜色/间距。
- 硬规则：新增 `ui_skill_requires_theme_skill`；`frontend_development.authoritative_skill=weline-theme-development`。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.26` → `1.2.27`
- 交付地址：N/A（规则/契约，无 Web UI；本回合未开验收 Browser）。

## 2026-09-08 — 硬约束：JS 模块改后须收集（1.2.26）

- 目标：`theme_js_module_declare_only` + `frontend_development` 强制改 `weline.modules.js` 后 `php bin/w resource:compile welineModules`；AI硬规则索引验收列同步。
- 版本：`1.2.25` → `1.2.26`

## 2026-09-08 — 硬约束：禁止路径启发式加载（1.2.25）

- 目标：`weline_js_loader_framework_only` 明确仅 declare / data-weline-load|declare；禁止 path-heuristic。
- 版本：`1.2.24` → `1.2.25`

## 2026-09-08 — 硬约束：核心 i18n 归 Framework（1.2.24）

- 目标：`weline_js_loader_framework_only` 明确核心 i18n JS 归属 `Weline_Framework`；`Weline_I18n` 仅增强；勿把 Phrase 改名为 I18n。
- 版本：`1.2.23` → `1.2.24`

## 2026-09-08 — 硬约束：i18n 禁止内嵌 weline.js（1.2.23）

- 目标：`weline_js_loader_framework_only` 明确禁止内嵌 `i18n`（字典/translate/switchLang）；由 I18n 模块登记 + Theme declare 加载。
- 版本：`1.2.22` → `1.2.23`

## 2026-09-08 — 硬约束：宿主编辑器规则仅 MCP 可生成（1.2.22）

- 目标：Cursor/Codex 等宿主规则可由 MCP 生成，但 Agent **禁止手写**进 `.cursor/rules` 等私有文件（换项目会丢）。
- 硬规则：新增 MCP 运维约束 `host_editor_rules_mcp_generated_only`；权威在 `AI硬规则索引`；`AGENTS.md` 仅指针。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.21` → `1.2.22`
- 交付地址：N/A（规则/契约，无 Web UI；本回合未开验收 Browser）。

## 2026-09-08 — 硬约束收紧：weline.js 禁止 account 等业务（1.2.21）

- 目标：`weline.js` 强制只做 ModuleLoader + 维护时懒加载维护模块 JS；明确禁止 cart/account 等业务名与业务逻辑。
- 硬规则：收紧 MCP `weline_js_loader_framework_only`（`MANDATORY`；允许 api/dom 传输别名；**NOT account**）；`GuidanceWorkflowCatalog` frontend norm 对齐。
- 权威：`AI硬规则索引`；Theme `前端JS模块加载规范.md`。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.20` → `1.2.21`
- 备注：既有 `Weline.Account` 代理与路径启发式业务路由仍为残留债，须另开任务迁出；本条先把强制口径写入 MCP。
- 交付地址：N/A（规则/契约，无 Web UI；本回合未开验收 Browser）。

## 2026-09-08 — 硬约束：中文注释与友好代码风格（1.2.20）

- 目标：把「注释用简体中文、代码风格友好」固化进 MCP `hard-constraints.v1`，跨会话对 Agent 生效；本会话另用 即时生效。
- 硬规则：新增 MCP `chinese_comments_friendly_style`；`workflowHardRules` + `GuidanceWorkflowCatalog` frontend/taglib norms 对齐。
- 权威：`AI硬规则索引` 任务路由/纠正清单；Framework `开发标准与验收.md`；MCP `chinese_comments_friendly_style`。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.19` → `1.2.20`
- 交付地址：N/A（规则/契约，无 Web UI；本回合未开验收 Browser）。

## 2026-09-08 — 硬约束：weline.js 仅框架加载引擎（1.2.19）

- 当前状态：`implemented`
- 变更：`HardConstraintsCatalog` 新增 `weline_js_loader_framework_only`；`AI硬规则索引` / `前端JS模块加载规范` 对齐：禁止在 `weline.js` 写死业务模块名或业务逻辑。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.18` → `1.2.19`

## 2026-09-07 — 验收 Browser 打开即禁用缓存（1.2.18）

- 目标：无法清整盘缓存时，用规则强制每次打开/导航验收 Browser 都禁用 HTTP 缓存，避免旧静态资源误判。
- 硬规则：新增 MCP `browser_cache_disabled_on_open`；`closeout_delivery_reminder.browser_cache_disabled_on_open_required` + `browser_open_order`；`webui_browser_closeout` 表面 norms/required/forbidden 对齐。
- 权威：Framework `WebUI浏览器验收与交付地址门禁.md` 门禁 A WB-CACHE；`AI硬规则索引` / `AI工程交付流程` §6–§7 / `AI开发治理`；`.cursor/rules/local-browser-urls.mdc` + 技能 `local-browser-urls`。
- Cursor 动作：`Network.enable` → `Network.setCacheDisabled({cacheDisabled:true})` → navigate；被拒则 `Page.reload({ignoreCache:true})` 降级。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.17` → `1.2.18`
- 宿主原生编辑。
- 交付地址：N/A（规则/契约，无 Web UI；本回合未开验收 Browser）。

## 2026-09-07 — 验收 Browser 汇报后必须关闭（1.2.17）

- 目标：堵住「开 Browser 验收却不关标签」导致 Cursor webview 空转卡顿的流程缺口。
- 硬规则：新增 MCP `browser_release_after_delivery`；`closeout_delivery_reminder.browser_release_after_delivery_required` + `browser_release_order`；`mandatory_before_closeout` 增 `webui_browser_released_after_delivery_or_na`；`webui_browser_closeout` 表面 norms/required/forbidden 对齐。
- 权威：Framework `WebUI浏览器验收与交付地址门禁.md` 门禁 D + 收口顺序；`AI硬规则索引` / `AI工程交付流程` §7 / `AI开发治理`；`.cursor/rules/local-browser-urls.mdc` + 技能 `local-browser-urls`。
- 顺序：WB-OP → curl → 写「交付地址」→ **立即 close 验收标签**（Cursor：unlock + `browser_tabs` close）；用户明确要求保留除外。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.16` → `1.2.17`
- 交付地址：N/A（规则/契约，无 Web UI；本回合未开验收 Browser）。

## 2026-09-07 — 交付 Host 规则 MCP+本地全文对齐（1.2.16）

- 目标：MCP 契约与本地权威文档都写清默认 Host `{project_hash}.test.weline.com`、禁止主链 `*.weline.test`。
- MCP：`feature_delivery_urls` 增 `default_local_host` / `forbidden_primary_hosts`；`closeout_delivery_reminder` / `session_startup_notices` / `webui_browser_closeout` 表面规范；`HardConstraintsCatalog.feature_delivery_urls` summary。
- 本地：`AI硬规则索引`、`AI工程交付流程` §7、`AI开发治理`、`WebUI浏览器验收与交付地址门禁`「本机默认 Host」专节；`.cursor/rules/local-browser-urls.mdc`；技能 `local-browser-urls`。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.15` → `1.2.16`
- 宿主原生编辑。

## 2026-09-07 — 交付 Host 默认 {hash}.test.weline.com（1.2.15）

- 目标：本机验收/交付主 Host 统一为 `{project_hash}.test.weline.com`；禁止主链使用 `*.weline.test`。
- 变更：`GuidanceWorkflowCatalog::featureDeliveryUrls` url_rules / forbidden / examples；`HardConstraintsCatalog` summary；契约测断言；对齐 Framework WebUI 门禁与 `local-browser-urls` 技能。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.14` → `1.2.15`
- 宿主原生编辑。

## 2026-09-06 — 硬约束：地区/地址选择必须 theme:address（1.2.14）

- 背景：后台地区列表等筛选若手写国家 select / 自造 chips，会与 Theme Address 标签分叉。
- 变更：新增 MCP `theme_address_for_region_pickers`；`GuidanceWorkflowCatalog` frontend forbidden/required 对齐；`AI硬规则索引` 与 Taglib 场景映射表补「后台地区筛选」。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.13` → `1.2.14`

## 2026-09-06 — 硬约束：禁止注释内 PHP 开标签（1.2.13）

- 问题：文件头等注释中残留生成器短回显（如日期行里的 `<?= date(...) ?>`），易被当成可执行代码或误导生成链路。
- 变更：
 1. `HardConstraintsCatalog` 新增 `no_php_tags_in_comments`（口径：注释内 `<?=` / `<?php` 开标签；**非**禁止普通注释掉语句）
 2. `workflowHardRules` + `GuidanceWorkflowCatalog` frontend norm 对齐
 3. `AI硬规则索引` 任务路由/纠正清单；`Framework/doc/3-开发/开发标准与验收.md` 指针
 4. 契约测试断言规则 id
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.12` → `1.2.13`

## 2026-09-06 — 翻译占锁僵死断开，禁止空转（1.2.12）

- 问题：持锁进程卡死（如 FileManager 队列占锁数十分钟）时，其它翻译任务只收到 `AI_TRANSLATION_BUSY` 却无法推进，表现为空等。
- 修复：`TranslationConcurrencyGate` 在占锁超时（`STALE_HOLD_SECONDS=210`，对齐请求超时 180s+宽限）或持锁 PID 已死后，`SIGTERM` 断开占用并立即 BUSY 结束本轮，不 sleep 空转；下一轮 cron/队列再续。
- 协作：Document 翻译走同一闸门并带 `timeout_seconds=180`；Meta Local 遇 BUSY 停轮。
- 版本：`1.2.11` → `1.2.12`。

## 2026-09-06 — 翻译闸门默认零等待，避免定时任务空转（1.2.11）

- 问题：`DEFAULT_WAIT_SECONDS=20` 时争用方会 sleep 空等；队列 BUSY 后立刻续队，继续占满模型。
- 修复：默认 `DEFAULT_WAIT_SECONDS=0`（拿不到锁立即 `AI_TRANSLATION_BUSY`）；可选 `maxWaitSeconds>0` 仍走 `SchedulerSystem::sleep`。
- 协作：I18n / FileManager 队列遇忙/错不立刻续队，等下一轮 cron。
- 版本：`1.2.10` → `1.2.11`。

## 2026-09-05 — 翻译闸门改用 SchedulerSystem 等待（1.2.10）

- 问题：`TranslationConcurrencyGate` 使用原生 `sleep(1)`，违反框架「请求可达路径禁止原生 sleep/usleep」约定。
- 修复：改为 `SchedulerSystem::sleep`（WLS Fiber 让出 / FPM·CLI 原生回退）。
- 版本：`1.2.9` → `1.2.10`。

## 2026-09-05 — 翻译并发单飞闸门，避免多路空等耗资源（1.2.9）

- 问题：AI 支持并发发起，但本机 Ollama `-np 1` 时多 worker 同时 `curl` 会长时间阻塞；等待虽不空转 PHP CPU，却占满队列与模型连接。
- 修复：新增 `TranslationConcurrencyGate`（flock 单飞；`sleep(1)` 限时等待后 `AI_TRANSLATION_BUSY`）；`TranslationService` 请求带 `timeout_seconds=180`；BUSY 不落入逐条回退。
- 版本：`1.2.8` → `1.2.9`。
- 宿主原生编辑。

## 2026-09-05 — Cursor 孤儿 mcp-process 导致 HOST_MCP_NOT_ATTACHED（1.2.8）

- 问题：本地 STDIO `tools/list` 齐全且 `cursor-agent mcp list` 显示 ready，但 IDE 会话 `CallDynamicTool`/`prepare_project` 超时（Transport closed）。根因是 Cursor Helper `mcp-process` 无 `learning-mcp` 子进程，却仍缓存工具目录；ensure 未识别 `CURSOR_AGENT` 宿主，跳过 bounce。
- 修复：
 1. `DetectPrimaryHost` / `HostRuntimeState` 识别 `CURSOR_AGENT` / `CURSOR_EXTENSION_HOST_ROLE`
 2. `CursorMcpProcessState` 检测 `orphan_no_learning_mcp_child` 并标记 `current=false`
 3. ensure 在 `hostReady!==false` 时 bounce 孤儿 helper + touch `~/.cursor/mcp.json`，返回 `host_repair_needed`
- 验收：`host-detection.php`、`project-guidance-reload-policy.php`；ensure 实测 `primary_host=cursor`、`reason=cursor_mcp_process_orphan`、`bounced=true`
- 本会话附着：bounce 后需**新开 Agent 回合**（或 Developer: Reload Window）才能重建传输；本回合仍记 `HOST_MCP_NOT_ATTACHED`
- 版本：`1.2.7` → `1.2.8`

## 2026-09-05 — 浮层必须走 Weline.UI 升为硬约束（1.2.7）

- 问题：Theme 浮层规范在文档里，但硬约束/工作流未抬高；address multi 任务易手写 left/top。
- 变更：`HardConstraintsCatalog` / `GuidanceWorkflowCatalog` 新增 `weline_ui_floating_primitives`；`AI硬规则索引` 任务路由/决策流/纠正清单对齐；契约测试断言规则 id。
- 验收：`php app/code/Weline/Ai/Mcp/tests/guidance-workflow-contract.php`
- 版本：`1.2.6` → `1.2.7`
- MCP：本轮 `prepare_project` 超时，按受限回退改已定位路径。

## 2026-09-03 — Hook type 段命名硬约束

- 当前状态：`fixed`
- 问题：Agent 创建 Hook 时把 `theme-editor` 等功能名写入 type 段，导致 `setup:upgrade` 致命错误。
- 修复：
 1. `HardConstraintsCatalog` 新增 `hook_name_type_partial_or_layout`
 2. `GuidanceWorkflowCatalog::hookExtensionSurface` 补充五段命名 norm/forbidden/required
 3. `Hook创建规范.md` 新增 type 段专节；Theme brand-basics Hook 重命名作正例
- 验收：`guidance-workflow-contract.php`；`php bin/w setup:upgrade --route` 不再报该 Hook 名错误
- 版本：`1.2.5` → `1.2.6`

## 2026-09-03 — MCP 调用范围：非编码禁止调用

- 当前状态：`fixed`
- 问题：宿主 `AGENTS.md` 写「任何任务之前」强制 ensure/`prepare_project`，闲聊与概念问答也会打满 MCP，浪费。
- 修复：
 1. `mcp_call_scope`：非编码禁止 MCP；仅编码/工程强制标准路径
2. `HardConstraintsCatalog::mcpInstructions` 前置 CALL SCOPE； / plan trigger 收窄为 `every_coding_user_requirement`
 3. `AGENTS.md`、`AI硬规则索引.md`、`AI工程交付流程.md`、协议文档与契约测试对齐
- 验收：``；`guidance-workflow-contract.php`；instructions 含 CALL SCOPE
- 宿主原生编辑。

## 2026-09-02 — MCP 引导收敛至 AGENTS.md

- 当前状态：`fixed`
- 问题：MCP 引导分散在 `CLAUDE.md`、`.cursorrules`、`.cursor/rules/weline-mcp-readiness.mdc` 等编辑器私有文件。
- 修复：删除上述重复引导；`.gitignore` 整体忽略 `.cursor/`；`AGENTS.md` 为唯一 MCP 接通入口；`ProjectIndexer` 仅保留 `AGENTS.md` 为宿主知识路径。
- 验收：`host-mcp-registration-contract.php`

## 2026-09-02 — 跨宿主 MCP 引导改为会话安装（host-mcp-install-guidance.v1）

- 当前状态：`fixed`
- 问题：上一版 bootstrap 直接写入 `.cursor/mcp.json` / `.mcp.json` / `.vscode/mcp.json` 与 Git hooks，与用户「只要引导、会话自己装 MCP」冲突。
- 修复：
 1. 新增 `project-guidance-mcp-install.php`：探测宿主 + 返回 `host_mcp_install` 步骤（`write_json` / `shell`）
 2. `ensure-host-mcp-registrations.php`、`ensure-cursor-mcp.php` 改为纯引导（`writes_host_config=false`）
 3. 删除 `ensure-claude-mcp.php`、`ensure-vscode-mcp.php`、`install-git-hooks.php`
 4. `ensure-project-guidance` 输出 `host_install_needed`；不再 touch 用户 MCP 配置
- 验收：`php app/code/Weline/Ai/Mcp/tests/host-mcp-registration-contract.php`

## 2026-09-02 — 跨宿主 MCP 自动注册（host-mcp-registration.v1）【已回退为会话安装】

- 曾直接写入多宿主 MCP 配置；见上条修正。

## 2026-08-29 — MCP 每项目独立 data_dir

- 当前状态：`fixed`
- 问题：即便已 bound 仓库，多仓仍可能共用 `~/.learning-mcp` 磁盘与 sidecar；用户需要「自己管自己项目」。
- 修复：`ensure-cursor-mcp` 注入 `LEARNING_MCP_DATA_DIR=~/.learning-mcp/projects/<sha256>` + 既有 `BOUND_REPOSITORY`；一次性把当前仓索引从共享 `indexes/` 迁入项目目录；`RepositoryScope::projectDataDir` 统一路径算法。
- 验收：ensure 输出含 `data_dir`；mcp.json env 双键齐全；迁后项目 indexes 仅本仓 generation。

## 2026-08-29 — MCP 单项目索引绑定

- 当前状态：`fixed`
- 问题：共享 `~/.learning-mcp/indexes` 累积多仓 generation（约 26GB），GC/sidecar/刷新会扫其它项目，CPU 飙高。
- 修复：`RepositoryScope` + `LEARNING_MCP_BOUND_REPOSITORY` / `index.bound_repository`；Cursor `ensure-cursor-mcp` 注入绑定到框架仓；工具/sidecar 拒绝越界仓库；GC `purge_unbound` 隔离其它 generation。
- 验收：`tests/bound-repository.php`；ensure 后 mcp.json 含 bound env。

## 2026-08-29 — MCP 硬约束：@lang 源文禁止未加引号的逗号

- 当前状态：`fixed`
- 问题：Theme Editor `@lang{支持 .ico, .png, .svg}` 编译为 `<?=__('支持 .ico', .png, .svg)?>`，页面 500 ParseError；属 i18n 内联语法参数切分坑，易被误判为「标签里写 PHP」。
- 修复：`HardConstraintsCatalog` 新增 `at_lang_no_unquoted_comma` + 扩展 `template_lang_not_php_i18n`；`template_i18n` surface 禁令/必做同步；`AI硬规则索引` / 交付流程 / lang 权威指南 / Theme 总指南 / Taglib 场景映射表对齐。
- 文档强化（同日）：`01-lang标签使用指南.md` 摘要前置决策表 + 三种语法专节标明逗号语义；`Theme/doc/README.md` §前端模板文案补一票否决式说明。
- 验收：`guidance-workflow-contract.php` 断言新 rule id；`run.php --quick`。

## 2026-08-29 — 硬约束搬入 MCP：引导只指路

- 当前状态：`fixed`
- 问题：`session_startup_notices` / AGENTS / ToolService INSTRUCTIONS 重复展开 Theme/Taglib 等框架法，引导层过重。
- 修复：新增 `HardConstraintsCatalog`（`hard-constraints.v1`，权威 `AI硬规则索引.md`）；`prepare_project.agent_guidance.hard_constraints` + `ToolService::instructions()` 前置下发；`session_startup_notices` 与 `AGENTS.md` 改为指针；`workflow_contract.hard_rules` 改由 Catalog 编译。
- 验收：`guidance-workflow-contract.php` 断言 notices 不含 UI 细则、含 hard-constraints 指针；`run.php --quick` 通过。

## 2026-08-29 — MCP 不可用时继续宿主原生编辑

- 当前状态：`fixed`
- 问题：引导未明确：MCP 未挂载或检索失败时，可直接用宿主原生编辑继续开发。
- 修复：`GuidanceWorkflowCatalog::sessionStartupNotices` + `AI工程交付流程` / `AGENTS.md` / 治理与协议文档对齐。
- 验收：契约测试通过。

## 2026-08-29 — MCP parser decode/inline 内存门槛按 payload 自适应

- 当前状态：`fixed`
- 问题：`MINIMUM_DECODE_FREE_BYTES` / inline reserve 固定 64MiB，导致 ~15KB 的 `auth.phtml` 也报 `decode memory reserve`，误判为「文件很大」。
- 修复：decode 改为 `min(64MiB, max(4MiB, payload×32))`；小文件 inline 改为 `min(64MiB, max(8MiB, size×64))`；错误信息带上 payload/need/have；高水位测试同步。
- 验收：`run.php --quick` 通过；小 payload 不再因固定 64MiB 误伤。

## 2026-08-28 — MCP PHP parser 容量失败不再阻断整仓 readiness

- 当前状态：`fixed`
- 问题：Theme 大文件在父进程内存紧张时触发 `decode memory reserve`，写入 `index.errors` 后 `prepare_project`/ 整仓 `PROJECT_BLOCKED`。
- 修复：`ProjectIndexer` 将容量类解析失败计入 `skipped.parser_capacity` + warning，保留上一版索引，不写 blocking errors；isolation 契约改为期望 `freshness=current`。
- 验收：`php app/code/Weline/Ai/Mcp/tests/run.php --quick` 相关用例通过。

> 按目标版本记录真实进度、调整与验收证据；不粘贴原始终端日志，不补写未经证实的历史。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.2.0` | implemented_release_pending | `REQ-AI-0002` | `2026-08-23` | `2026-08-23` | runtime pass / release pending |
| `1.1.3` | implemented_webui_blocked | `REQ-AI-0001` | `2026-08-11` | `2026-08-11` | pending |

## `1.2.0` — 统一项目知识库与 MCP 迁移

- 基线版本：`1.1.3`
- 目标版本：`1.2.0`
- 当前状态：`implemented_release_pending`
- 需求 ID：`REQ-AI-0002`
- 任务记录：N/A；最终治理结构不保留 `迁移前 AI 资料（已清理）`，执行证据记录在本版本条目与发布提交/标签中。
- 范围/排除：按用户确认计划迁移 MCP、文档索引和客户端入口；保留业务 AI 功能；排除知识后台 UI 与未授权真实部署。
- 负责人/确认人：当前 Codex 主代理直接实施（未委派）/用户。

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-29 | MCP 引导：插件刷新自愈 | plugin refresh blocking → non-blocking | `mcp_stdio.ready=true`、`mcp_host.ready=true`、`host_runtime.current=true` 时，仅 Hook 插件产物刷新仍连续返回 `host_repair_needed` 并重启宿主 | 将重载策略独立为可测试纯函数；仅插件产物刷新标记 `plugin_refresh_non_blocking` 并继续 ready，真实 MCP 注册变化或运行代次过期仍重载阻断 | 用户 | `scripts/project-guidance-reload-policy.php`、`scripts/ensure-project-guidance.php`、`tests/project-guidance-reload-policy.php` |
| 2026-08-28 | MCP 引导：自研主题 UI 高压线 | session_startup + frontend_development | 结账等前端反复未走 Weline UI 2.0 / 主题变量，手写地址控件 | `session_startup_notices` + `frontend_development.weline_ui_theme_first` + hard_rules + ToolService INSTRUCTIONS + 交付流程/硬规则索引明确强调自研主题与 CSS 变量 | 用户 | `GuidanceWorkflowCatalog.php`、`ToolService.php`、`AI工程交付流程.md`、`AI硬规则索引.md`、`guidance-workflow-contract.php` |
| 2026-08-27 | Taglib callback 裸 `@static` 404 坑 | taglib callback static | Promotion 活动主题表单加载即 404 `.../@static(Weline_Product::css/...)`；`.phtml` 顶部 @static 正常，`<w:product:admin:picker>` callback 未解析 | MCP hard_rules + taglib surface + AI硬规则索引 + Taglib/如何自定义Tag + Framework/static-resource-versioning；修复 AdminProductPicker | 用户 | `GuidanceWorkflowCatalog.php`、`如何自定义Tag.md`、`AdminProductPicker.php` |
| 2026-08-30 | 验收 URL 短规则落地 | local-browser-urls | 旧 alwaysApply 规则强制 127.0.0.1 + simpleBrowser 主链，与 MCP 冲突；项目 `.mdc` 曾缺失 | 重建精简 `.cursor/rules/local-browser-urls.mdc`；技能收短；主链 Markdown http(s)+Host 优先 | 用户 | `.cursor/rules/local-browser-urls.mdc`、`~/.cursor/skills/local-browser-urls/SKILL.md` |
| 2026-08-29 | MCP 约束矛盾审计 | closeout gate reconcile | 发现 simpleBrowser/https-only/Catalog 原型路径写死/WB-VIS 绝对强制 等与宿主无关 Browser 规则打架 | 对齐 feature_delivery_urls 为 http(s)+主链 Markdown；WB-VIS 有条件；prototype_doc 改为 `{Module}`；同步 local-browser-urls 技能 | 用户 | `GuidanceWorkflowCatalog.php`、`HardConstraintsCatalog.php`、`WebUI浏览器验收与交付地址门禁.md` |
| 2026-08-29 | WebUI Browser 自测 + 交付地址硬门禁 | closeout gate | 规则曾写过但 AI 常跳过 Browser 自测与交付地址汇报；初稿误写死 Cursor 内置 | 新增 Framework 专项 doc + hard rule `browser_operator_self_test`（宿主无关真实 Browser）+ surface `webui_browser_closeout`；收口 mandatory 增项 | 用户 | `WebUI浏览器验收与交付地址门禁.md`、`HardConstraintsCatalog.php`、`GuidanceWorkflowCatalog.php` |
| 2026-08-27 | 模块翻译 CSV 门禁 | i18n csv + collect | 改 CSV 不 collect 翻译不生效；前后台词条不对齐 | 新增 I18n/doc/模块翻译CSV规范.md + MCP surface `module_i18n_csv` + 硬规则/ALWAYS/验收表 | 用户 | `模块翻译CSV规范.md`、`GuidanceWorkflowCatalog.php` |
| 2026-08-27 | 模块版本与升级门禁 | module version bump | 改 Model/Controller 不升 version 导致 Schema/路由报错 | 新增 Framework 专项 doc + MCP surface `module_upgrade_gate` + 硬规则索引 | 用户 | `模块版本与升级门禁.md`、`GuidanceWorkflowCatalog.php` |
| 2026-08-27 | AI 硬规则索引与 MCP 引导优化 | hard-rules-index + surfaces | AI 反复违规 Taglib/Hook/Event/i18n/weline-code | 新增 AI硬规则索引、场景映射表、Hook/Event 规范、标签全量索引生成器；扩展 GuidanceWorkflowCatalog 五 surface + pinned；IntelligenceService 任务匹配 active_surfaces | 用户 | `Ai/doc/`、`Taglib/doc/`、`Hook/doc/`、`GuidanceWorkflowCatalog.php`、`guidance-workflow-contract.php` pass |
| 2026-08-27 | MCP 单注册与宿主代次修复 | false-ready → single-generation | Codex 插件和 `.cursor/mcp.json` 同时启动 MCP；安装器未清理下划线别名；独立 STDIO 探测无法证明当前宿主已换代；无时区 `ps lstart` 被 PHP UTC 误读会产生未来启动时间 | 插件改为 Hook-only；显式 STDIO 成为唯一服务器注册；工具响应公开 PID/代次并拒绝旧进程；宿主 Epoch 改由 `ps etime` 推导；自检按源码时间和插件产物安排一次性 app-server 重启 | 用户 + 当前主代理 | `scripts/install.php`、`scripts/ensure-project-guidance.php`、`scripts/project-guidance-runtime-time.php`、`src/ToolService.php` |
| 2026-08-29 | MCP 脏改不可丢弃门禁 | caller discipline → fail-closed runtime guard | 用户明确要求 MCP 的修复与回滚绝不能丢弃 tracked/staged/untracked/ignored 脏改 | 新增统一 Git 安全策略并接入引导与内部进程执行器；MCP 内部 Git 完全只读，分支切换与 config/helper/pager 注入同样拒绝；回滚须校验 postimage Hash，防止覆盖外部变化 | 用户 + 当前主代理 | `GitSafetyPolicy.php`、`git-worktree-safety.php`、`guidance-workflow-contract.php` |
| 2026-08-26 | MCP 引导：交付 URL 改直接 https 链接 | feature_delivery_urls.link_format | simpleBrowser 伪协议在聊天中不可点 | 主验收改为 `[label](https://…)`；禁止 command:simpleBrowser 作主链；AGENTS/交付流程/契约测试同步 | 用户 | `GuidanceWorkflowCatalog.php`、`guidance-workflow-contract.php` |
| 2026-08-26 | MCP 引导：交付 URL 须真实可点链接 | feature_delivery_urls.link_format | 上一轮「打开」仅变色不可点 | 明确 Markdown 链接格式与 forbidden 模式；交付流程/AGENTS/治理同步 | 用户 | `GuidanceWorkflowCatalog.php`、`AI工程交付流程.md`、`guidance-workflow-contract.php` |
| 2026-08-26 | MCP 引导：功能交付地址清单 | workflow_contract 增补 | 功能完成后用户常收不到可访问的前后台 URL | `feature_delivery_urls.v1`、`session_startup_notices`、收口 hard_rules；交付流程/AGENTS/治理同步 | 用户 | `GuidanceWorkflowCatalog.php`、`AI工程交付流程.md`、`AGENTS.md`、`ToolService.php`、`guidance-workflow-contract.php` |
| 2026-08-25 | MCP 引导强化 | workflow_contract 增补 | 功能交付常漏文档；Web UI 易忽略平板/PC 断点 | `session_startup_notices` + hard_rules + frontend norms；`prepare_project.agent_guidance`；交付流程/AGENTS 同步 | 用户 + Composer | `GuidanceWorkflowCatalog.php`、`ProjectReadinessService.php`、`ToolService.php`、`AI工程交付流程.md`、`AGENTS.md`、`guidance-workflow-contract.php` |

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| MCP 单注册与宿主代次门禁 | pass | 安装器 dry-run 产物不再声明第二服务器；自检返回源码代次、宿主 PID/启动时间与一次性重启回执；新进程工具响应满足 `source_current=true` | `2026-08-27` |
| 需求差异已确认并写入 | pass | `doc/需求.md#req-ai-0002-统一项目知识库与强制-mcp-准备协议`；用户要求完整实施计划 | `2026-08-23` |
| 实现执行者/模型/范围已记录 | pass | 当前主代理直接实施；范围为确认计划中的双仓和模块文档 | `2026-08-23` |
| 架构/缺陷/安全复审通过 | pass | MCP 独立运行时、readiness 会话门禁、最小上下文、Deploy CLI 隔离及凭据边界复审 | `2026-08-23` |
| 分层测试通过 | pass | MCP 222/222；AI 关键兼容 64/64（332 assertions）；Deploy 4/4（22 assertions） | `2026-08-23` |
| 真实路径 WebUI/E2E 通过 | pass | 真实 WLS 升级；后台智能体 3、适配器 11、业务 Skill 1、供应商账户均可见且无新增浏览器错误 | `2026-08-23` |
| 可重复 E2E 固化并通过 | pass | readiness/repair/freshness/session 套件；智能体模板拼接回归用例 | `2026-08-23` |
| 功能/运营文档完成 | pass | 93 个知识单元、279 份必需文档；旧入口与静态索引清理完成 | `2026-08-23` |
| 用户/产品验收 | pending | 等待最终交付 | `2026-08-23` |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| `2026-08-23` | requirements | planned → requirements_confirmed | 用户确认唯一维护源、严格 readiness、双端发行和历史重写 | 以已发布独立 `v0.12.2` 为唯一迁移源，旧内嵌 `0.9.0` 和 AiKnowledge 不合并 | 用户 | `doc/需求.md`、独立仓标签 `v0.12.2` |
| `2026-08-23` | baseline | requirements_confirmed → implementing | 框架与独立仓均存在大量既有脏改 | 分别形成独立基线；独立 MCP 通过 219/219、220/220、lint、Node、STDIO 后双端发布 | 当前主代理 | 框架 `8ec7a2c50`；MCP `be2113099` |
| `2026-08-23` | readiness | implementing → testing | 当前 Git 树为 92 个运行时模块 + 1 个 Base 聚合包；旧计划数量多计 1 个 | 以 `prepare_project` 实际扫描的 93 个知识单元 / 279 份必需文档为验收基线，不创建虚构模块 | 当前主代理（依 Git 与 readiness 证据） | `project-readiness.v1 status=ready` |
| `2026-08-23` | runtime | testing → runtime_validated | 真实模块升级完成；Agent/Adapter 扫描为 3/11，业务 Skill 注册与导入导出往返正常 | 用真实 WLS 后台逐页核对 Agent、Adapter、Skill、Provider，不以 CLI 结果替代 UI | 当前主代理 | `setup:upgrade`、CLI 与 Browser 运行结果 |
| `2026-08-23` | defect | runtime_validated | 智能体页发现既有内联脚本字符串拼接缺少 `+`，导致表格不渲染 | 先增加 1 个失败回归用例，再修复模板；重载后 3 行均渲染且无新增错误 | 当前主代理 | `AgentCatalogServiceTest`；真实 `/ai/backend/agent` |
| `2026-08-23` | handoff | runtime_validated → implemented_release_pending | 实现、文档、测试和真实运行时验收已通过 | 保持发布待定，直到内置 MCP 快照、独立 `v0.13.0` 与历史重写双端验收完成 | 当前主代理 | 本版本门禁表 |
| `2026-08-23` | release | implemented_release_pending → release_snapshot_published | 独立仓由内置 MCP 生成最终一致快照，完整测试 223/223 通过 | 发布并冻结 `v0.13.0`；GitHub、Gitee 的 `main`、标签对象和剥离提交均逐项核验一致 | 当前主代理 | 框架 `a27fa01e6`；MCP `6463dfccd`；标签 `v0.13.0` |
| `2026-08-23` | history | release_snapshot_published → history_rewritten_platform_refs_pending | 双端全部可写且受影响的 heads/tags 已在统一 bare mirror 中仅过滤 `dev/ai/**`，并以旧 SHA lease 原子强推 | 普通 refs 已无该路径；GitHub 的 4 个已关闭/合并 `refs/pull/*` 为平台只读，Git push 与 Git References API 均不能改写 | 当前主代理 | 新 `dev=e51f355c7`、`master=9ea99c834`；GitHub API `422 refs/pull/* is read-only` |
| `2026-08-23` | cursor-adapter | acceptance → cursor_mcp_ensure | Cursor 会话偶发未挂上 `weline_project_intelligence`，引导却把 Settings 手写配置推给用户 | 新增 `scripts/ensure-cursor-mcp.php`：用当前 `PHP_BINARY`+绝对入口+仓库 `cwd` 修复 `.cursor/mcp.json`；`AGENTS.md`/Cursor 规则改为「先 ensure，再宿主重载/批准」，禁止首推 Settings | 当前主代理 / 用户确认引导职责 | `cursor-mcp-ensure.v1 status=ok`；本机 `.cursor/mcp.json` 已重写 |

### 实现范围

- 变更路径/符号：`app/code/Weline/Ai/Mcp/**`、AI/Deploy 模块版本与文档、根 AI 客户端适配、模块 `doc/**`、旧系统删除与发行脚本。
- 数据/配置/迁移：派生 SQLite 可重建；不自动删除 `ai_knowledge_call_history`；无真实环境配置写入。
- 兼容与回退：业务 Agent/Scenario/Provider/Skill/Style 保持；迁移提交可回退，历史重写另有双端镜像与旧 SHA 租约保护。

### 审查与整改

- 架构：pass。`LearningMcp` 继续保持 PHP 8.2、无 WLS/DI/业务数据库依赖；Weline_Ai 仅为代码所有者和发行容器。
- 缺陷：pass。readiness 绑定项目 revision、文档 Hash 与会话；外部文档编辑在下一次查询前刷新；真实 UI 发现的智能体模板语法错误已由 RED→GREEN 用例覆盖。
- 安全：pass。会话 directives 不落盘且不存凭据；修复需显式授权；Deploy 桥接只返回计划，未授权时不执行发布；旧生产历史表不自动删除。
- 用户批准延期：none。

### 测试与验收

- 聚焦/单元/集成：独立最终快照完整 MCP 223/223；项目准备专项全绿；AI 业务兼容 64 tests / 332 assertions；Deploy 4 tests / 22 assertions；PHP/Node/JSON/lint 与安装器 dry-run/STDIO 冒烟通过。
- 真实 STDIO / Weline：实际 JSON-RPC 初始化、`prepare_project`、session directives、Framework/Theme/Taglib 上下文与 local Deploy 计划通过；同一 readiness 下外部文档改动与恢复均在下一次查询返回新 Hash。真实 `setup:upgrade` 成功并完成 92 个运行时模块编译。
- 全新克隆：GitHub、Gitee 普通 refs 的 `dev/ai` 历史命中与可达路径均为 0；两端最终 `dev` 都真实返回 ready、93 个知识单元、279 份必需文档。Codex Doctor 加载 0.13.0 插件且 0 fail；Cursor 3.17.8 从项目 `.cursor/mcp.json` 发现 MCP，按客户端安全模型完成一次 workspace approval 后状态为 ready，并列出 18 个公共工具。
- 真实 WebUI：Weline Chrome 配置连接真实 WLS；Agent 3 行、Adapter 11 条、业务 Skill 1 条及 Provider 页面可见；旧 AiKnowledge 菜单与运行时引用均不存在；验收导航后没有新增浏览器错误。
- E2E 固化：readiness/repair/freshness/session 测试与智能体模板回归测试可重复运行。
- 说明：旧模块全量收集命令仍会误执行非 PHPUnit 的 `BatchTest.php`，并包含与本迁移无关的历史失败；本次以变更相关测试、真实 CLI/STDIO 与 WLS 页面完成验收，不据此扩大业务修复范围。

### 文档与运营交付

- 需求文档：`REQ-AI-0002` / `1.2.0`。
- 功能/API/架构文档：`doc/README.md`、`doc/统一知识库与MCP协议.md` 与内置 MCP `docs/**`。
- 运营文档：不新增 UI；MCP/CLI/模块文档为管理面。
- 知识索引：派生到 MCP SQLite，不再维护仓库内索引文件；任务上下文由 `resolve_task_context` 动态返回。

### 版本结论

- 结论：history_rewritten_platform_refs_pending。
- 完成/发布证据：独立 `v0.12.2`、最终冻结版 `v0.13.0` 均已双端发布；框架迁移提交、文档、测试、真实 STDIO、升级与后台验收通过。
- 剩余事项与恢复条件：严格“所有公开 ref”验收仍需 GitHub 平台或 Support 清除/重写只读的历史 PR refs，仓库管理员权限无法自行完成；其余实现、发布、普通 ref 历史与双端全新克隆验收均已完成。

## `1.1.3` — 场景图像参考媒体透传

- 基线版本：`1.1.2`
- 目标版本：`1.1.3`
- 当前状态：`implemented_webui_blocked`
- 需求 ID：`REQ-AI-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：只合并当前会话的 `ScenarioImageGenerationGateway.php` allowlist hunk与对应新单测；排除 PageBuilder 业务实现、模型/Provider/账号切换、Schema、部署、发布及会话外两仓漂移。
- 负责人/确认人：当前 Codex 主代理直接实施（未委派）；用户确认需求与 `KEEP_SITE→FW` 合并方向。

### 门禁状态

状态只使用 `pending`、`pass`、`fail`、`blocked` 或 `N/A`；`N/A` 必须写可核验理由。

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-ai-0001-场景图像生成参考媒体契约` | `2026-08-11` |
| 实现执行者/模型/范围已记录 | pass | 当前主代理直接实施；未委派；仅两个会话候选代码/测试文件 | `2026-08-11` |
| 架构/缺陷/安全复审通过 | pass | 通用媒体边界不依赖 PageBuilder；参数覆盖、递归路由字段禁入与敏感字段边界保持不变 | `2026-08-11` |
| 分层测试通过 | blocked | 聚焦与 PageBuilder 回归通过；邻近 VectorEngine 两例被既有 Composer vendor/source 偏移阻断 | `2026-08-11` |
| 真实路径 WebUI/E2E 通过 | blocked | 专用实例重载触发 managed-child 身份错误；正常停机后重启被 `WLS state lock changed while being opened` 拒绝 | `2026-08-11` |
| 可重复 E2E 固化并通过 | blocked | 依赖真实 WLS 工作台路径先恢复 | `2026-08-11` |
| 功能/运营文档完成 | pass | `doc/README.md`、`doc/需求.md`、`doc/开发日志.md` 与隔离生成结果的两个索引项 | `2026-08-11` |
| 用户/产品验收 | pending | 用户已确认视觉目标和实施方向；最终可见结果尚待 WebUI | `2026-08-11` |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| `2026-08-11 14:00` | requirements | planned → requirements_confirmed | 用户确认目标是高质、背景透明、无重复烤字的 Logo，并授权按建议脏改合入官方框架 | 核心只补通用 `image` 媒体边界；PageBuilder 继续拥有透明/OCR/Vision/品牌字逻辑 | 用户 | `doc/需求.md` |
| `2026-08-11 14:10` | merge_decision | requirements_confirmed | 两边实时磁盘 diff 表明源码仅差一行，官方测试文件缺失 | 两个候选均执行 `KEEP_SITE→FW`；禁止整文件覆盖或 Git 合并 | 用户 | 普通 `diff -u` 结果；本任务计划 |
| `2026-08-11 14:12` | context | requirements_confirmed | Weline 项目智能服务返回 `Transport closed` | 按全局规则退回精确路径读取、普通磁盘 diff 与 `apply_patch` | 当前主代理 | 任务进度记录 |
| `2026-08-11 14:20` | tdd_red | requirements_confirmed → implementing | 官方仓库新增聚焦测试后，未修改网关时 exact `image` 断言失败 | 保留失败证据后只增加一个 allowlist 项 | 当前主代理 | `ScenarioImageGenerationGatewayTest`: 1 failure |
| `2026-08-11 14:25` | implementation | implementing → testing | 官方网关增加 `'image' => true`；官方测试与站点测试逐字节一致 | 不整文件覆盖源码，不复制任何 PageBuilder 业务文件 | 当前主代理 | 两个候选普通磁盘 `cmp` |
| `2026-08-11 15:25` | validation | testing | 官方网关 1/1、尺寸解析 4/4；QiPai PageBuilder 108/862、网关 1/1；PHP lint 与双边对齐通过 | 邻近 VectorEngine 2 例单列为既有 vendor/source autoload 偏移，不扩大本次修复范围 | 当前主代理 | PHPUnit、ReflectionClass、`cmp` |
| `2026-08-11 15:31` | webui | testing → implemented_webui_blocked | 专用 `ai-test-logo-webui-081018` 滚动重载在新 Worker 身份绑定失败；正常停机后原配置重启仍遇 WLS 状态锁变化 | 不强制重载、不修改锁文件、不借用其他任务实例；WebUI 门禁保持 blocked | 当前主代理 | WLS status、doctor、实例错误日志 |

### 实现范围

- 变更路径/符号：`Service/ScenarioImageGenerationGateway.php` 的 `MEDIA_KEYS`；新建 `Test/Unit/Service/ScenarioImageGenerationGatewayTest.php`。
- 数据/配置/迁移：N/A；没有 Model Schema、数据库、路由或模块配置变更。
- 兼容与回退：无 `image` 时行为不变；移除新增 allowlist 行即可回退核心透传，但会恢复参考图静默丢失缺陷。

### 审查与整改

- 架构：通过。`image` 位于 provider-neutral 媒体契约；核心未引入 `GuoLaiRen_PageBuilder` 依赖，透明化、OCR/Vision、品牌字合成仍归调用模块。
- 缺陷：通过。媒体 allowlist 在运行时路由参数之后独立合并，新增值不会覆盖运行时字段；删除该项会稳定触发聚焦测试失败。
- 安全：通过。递归禁入校验先于 allowlist，`model`、`provider`、`account`、`endpoint`、`api_key` 等路由/敏感字段仍 fail-closed；网关不新增远程抓取或凭据处理。
- 用户批准延期：none。

### 测试与验收

- 聚焦/单元/集成：官方 RED 已捕获；GREEN 为网关 `1 test / 1 assertion`、尺寸解析 `4 tests / 4 assertions`。QiPai 标准 bootstrap 下 PageBuilder 相关 `108 tests / 862 assertions`、站点网关 `1 test / 1 assertion` 全绿；两个变更 PHP 文件 lint 通过；双边候选 `cmp` 通过。
- 邻近测试阻塞：官方 `VectorEngineProviderResponseFormatTest` 的 2 例在初始化时因 `AiModel::PRIMARY_MODALITY_EMBEDDING` 未定义而 error。Reflection 证明 Composer 加载 `vendor/weline/module-ai/Model/AiModel.php`（无该常量），而工作树 `app/code/Weline/Ai/Model/AiModel.php` 已有该常量；属于本次会话外的 vendor/source 偏移。
- 真实 WLS + WebUI：已尝试专用实例生命周期；因 WLS managed-child 身份与状态锁错误阻塞，未进入 Browser 点击路径，不得据此宣称产品视觉验收完成。
- E2E 固化：尚未执行。
- 未验证项：Provider 实际图生图调用与 PageBuilder 工作台可见透明结果。

### 文档与运营交付

- 需求文档：`REQ-AI-0001` / `1.1.3`。
- 功能/API/架构文档：`doc/README.md` 已写明通用参考媒体契约与 PageBuilder 责任边界。
- 运营文档：N/A；没有新增运营配置或独立操作入口，操作入口属于 PageBuilder。
- 模块索引：全仓生成器 dry-run 会触碰 42 个模块；本次用隔离副本运行原生成器，只回填本会话新增的 `需求.md`、`开发日志.md` 两个索引项，未带入会话外计数/排序漂移。

### 版本结论

- 结论：blocked
- 完成/发布证据：核心实现、三维复审、聚焦/调用模块测试、lint 与两仓候选对齐已完成；未执行发布。
- 剩余事项与恢复条件：修复或恢复 WLS 的 managed-child/状态锁运行时后，用专用实例完成工作台 Logo 重生成、透明/文字/资产 URL 检查并保存截图；同时在独立维护任务解决官方 Composer vendor/source 偏移后重跑邻近两例。本任务不授权发布。

## `2026-09-07` — MCP 就绪与索引成本优化

### 当前状态

- 状态：`implementing`
- 范围：仅调整 `Weline_Ai/Mcp` 的就绪刷新编排、增量 SQLite 写入和运营说明；保留现有索引能力、编辑 guard、宿主注册协议与当前脏工作区。
- 根因：本次 `prepare_project` 的宿主调用在 180 秒超时，但 MCP 进程仍完成了约 14 分钟的同步索引；这不是 JSON-RPC/STDIO 握手失败。空闲后 `health` 约 0.2 秒、`project_index_status` 约 0.3 秒。

### 调整

- `ProjectReadinessService` 现在优先读取持久化索引状态；近期 `current` 索引不因新 MCP 进程而重复全量发现，revision=0、过期/partial 和显式路径仍刷新。
- `ProjectIndexer::writeFile` 复用 chunk、稀疏向量、symbol、relation 的 PDO prepared statement，避免在循环中反复编译同一 SQL；索引数据结构和查询契约未改变。
- 增量关系回填不再对全表未解析关系执行全局更新：只收集删除/变更文件的旧符号名、当前符号名和关系目标名，经 `relations_target_idx` 建临时匹配表后按目标名范围写回；完整 `full` 维护仍保留全量回填。
- `SessionStart` Hook 现在只读取轻量索引状态并在响应后启动 detached 增量刷新；sidecar 可合并请求，不支持 sidecar 时退回单次 detached worker，避免把冷维护同步算进连接耗时。
- 运营策略采用“近期缓存 → 显式目标 → 过期维护”三级成本模型。扫描结果先验证为可复现缺陷，再进入测试、修复和真实探针，不把低置信关系或候选项自动当成代码问题。
- 本机 `~/.learning-mcp/config.yaml` 的兜底 `index.refresh_interval` 已从 `60s` 调整为 `10m`；显式目标和 Hook/MCP 写入仍按即时路径处理。
- 宿主连接修复：确认 Codex 插件为 `installed=true/enabled=true`，但共享 app-server（PID `50920`）没有 Weline `learning-mcp` 子进程；同一入口的直接 STDIO `health`/`tools/list` 正常，因此判定为已加载宿主 transport/catalog 失效，而非 PHP 或 SQLite 服务故障。`ensure-project-guidance.php` 现在校验插件缓存的 `.weline-generation.json`，只在缺失、重复注册或代次落后时调用既有单注册安装器，代次匹配时不重复注册。

### 证据与未完成项

- 基线索引约 14,532 个文件、133,620 个 chunks、64,137 个 symbols、490,619 条 relations、313 万个向量词项；项目 SQLite 约 1.8–1.9GB。83 个变更文件写入 3,531 个 chunks、19,287 条 relations，采样热点位于 SQLite/FTS5/向量写入。
- TDD：旧实现上的 `project-readiness.php` 新增刷新边界断言、关系回填专项、SessionStart detached refresh 专项和 Codex 插件代次刷新断言均按预期失败；实现后均通过，Weline MCP quick tests 为 `257/257`；PHP lint、`node --check bin/weline-mcp.js` 和 `git diff --check` 通过。
- 热路径探针：隔离小项目上的真实 STDIO MCP 可完成初始化、`prepare_project` 与 `project_index_status`；首次索引写入 7ms，第二个新鲜 MCP 进程复用持久化索引，进程耗时约 0.28s。实际项目冷 `prepare_project` 自然完成耗时 `433.67s`；随后同一缓存窗口的新鲜进程耗时 `0.82s` 并返回 `ready`。
- 实测：新鲜子进程可正常完成 JSON-RPC 初始化和 `health`；实际项目因持久化索引过期且存在约 443 个需重写文件，冷增量刷新持续超过 23 分钟后被诊断进程安全中止，未把它伪报成连接失败。随后最终真实 STDIO `project_index_status` 已读到 `phase=idle/freshness=current`、revision `1964`，最近一次增量耗时 `9809ms`（发现 14,560、未变 14,531、写入 2、删除 21）；定向关系回填已由 `relation-resolution-scope.php` 覆盖。
- 宿主状态：本回合 `ensure-project-guidance.php` 的本地 STDIO 探针为 `ready`、22 个工具齐全；宿主工具通道曾返回 `Transport closed`，原因是旧子进程在 SQLite B-tree 扫描期间失去响应，已只终止该旧子进程，未重启共享 app-server；新鲜直接 STDIO 已完成 `prepare_project`、revision `1968`、`phase=idle/freshness=current`。
- 本回合复验：插件缓存已重新安装为当前 source generation，第二次 ensure 返回 `repairs=[]`、`changed=false`、`source_generation_current=true`，直接 STDIO 仍为 22 个工具齐全；当前聊天的 Weline 动态调用仍返回 `Transport closed`，且 app-server 仍未生成 Weline 子进程。宿主未提供可调用的 MCP-only refresh，按共享进程安全规约未重启 app-server；因此保留本地可运行状态，并把现有会话重连作为剩余外部动作。
- 待完成：宿主会话仍需新 Agent 回合/仅重连 MCP 目录后完成 task-plan review；不通过手工改索引状态来制造 ready。
