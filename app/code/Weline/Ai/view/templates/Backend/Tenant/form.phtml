<?php
/**
 * 租户创建/编辑表单
 *
 * @var \Weline\Framework\View\Template $this
 * @var \Weline\Ai\Model\AiTenant $tenant
 */

$tenant = $this->getData('tenant');
$isEdit = $tenant && $tenant->getId();
?>

<style>
.tenant-form-container {
    padding: 20px;
    background: #fff;
}

.form-section {
    margin-bottom: 24px;
}

.form-section-title {
    font-size: 16px;
    font-weight: 600;
    color: #2c3e50;
    margin-bottom: 16px;
    padding-bottom: 8px;
    border-bottom: 2px solid #e9ecef;
}

.form-group {
    margin-bottom: 16px;
}

.form-label {
    display: block;
    font-size: 14px;
    font-weight: 500;
    color: #495057;
    margin-bottom: 6px;
}

.form-label .required {
    color: #dc3545;
    margin-left: 2px;
}

.form-input, .form-textarea {
    width: 100%;
    padding: 8px 12px;
    font-size: 14px;
    line-height: 1.5;
    color: #495057;
    background-color: #fff;
    border: 1px solid #ced4da;
    border-radius: 4px;
}

.form-input:focus, .form-textarea:focus {
    border-color: #80bdff;
    outline: 0;
    box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
}

.form-textarea {
    min-height: 80px;
    resize: vertical;
}

.form-hint {
    display: block;
    margin-top: 4px;
    font-size: 12px;
    color: #6c757d;
}

.form-check {
    display: flex;
    align-items: center;
    padding: 10px;
    background: #f8f9fa;
    border-radius: 4px;
}

.form-check-input {
    width: 20px;
    height: 20px;
    margin-right: 8px;
    cursor: pointer;
}

.form-check-label {
    font-size: 14px;
    color: #495057;
    cursor: pointer;
    margin: 0;
}

.form-actions {
    margin-top: 24px;
    padding-top: 16px;
    border-top: 1px solid #e9ecef;
    text-align: right;
}

.btn-submit {
    padding: 10px 24px;
    font-size: 14px;
    font-weight: 500;
    color: #fff;
    background: #007bff;
    border: none;
    border-radius: 4px;
    cursor: pointer;
}

.btn-submit:hover {
    background: #0056b3;
}

.btn-submit:disabled {
    background: #6c757d;
    cursor: not-allowed;
}
</style>

<div class="tenant-form-container">
    <form id="tenantForm" method="post" action="<?= $this->getBackendUrl('*/backend/tenant/save') ?>">
        <input type="hidden" name="id" value="<?= $isEdit ? $tenant->getId() : '' ?>">
        
        <div class="form-section">
            <div class="form-section-title"><?= __('基本信息') ?></div>
            
            <div class="form-group">
                <label class="form-label">
                    <?= __('租户名称') ?><span class="required">*</span>
                </label>
                <input type="text" 
                       class="form-input" 
                       name="name" 
                       value="<?= $isEdit ? $tenant->getData('name') : '' ?>"
                       placeholder="<?= __('请输入租户名称') ?>"
                       required>
            </div>

            <div class="form-group">
                <label class="form-label"><?= __('域名') ?></label>
                <input type="text" 
                       class="form-input" 
                       name="domain" 
                       value="<?= $isEdit ? $tenant->getData('domain') : '' ?>"
                       placeholder="<?= __('例如：example.com（可选）') ?>">
            </div>

            <div class="form-group">
                <label class="form-label"><?= __('描述') ?></label>
                <textarea class="form-textarea" 
                          name="description" 
                          placeholder="<?= __('租户描述信息（可选）') ?>"><?= $isEdit ? $tenant->getData('description') : '' ?></textarea>
            </div>
        </div>

        <div class="form-section">
            <div class="form-section-title"><?= __('配额设置') ?></div>
            
            <div class="form-group">
                <label class="form-label"><?= __('最大用户数') ?></label>
                <input type="number" 
                       class="form-input" 
                       name="max_users" 
                       value="<?= $isEdit ? $tenant->getData('max_users') : '0' ?>"
                       placeholder="<?= __('0表示无限制') ?>"
                       min="0">
                <span class="form-hint"><?= __('该租户允许的最大用户数，0表示无限制') ?></span>
            </div>

            <div class="form-group">
                <label class="form-label"><?= __('每日API调用限制') ?></label>
                <input type="number" 
                       class="form-input" 
                       name="max_api_calls_per_day" 
                       value="<?= $isEdit ? $tenant->getData('max_api_calls_per_day') : '0' ?>"
                       placeholder="<?= __('0表示无限制') ?>"
                       min="0">
                <span class="form-hint"><?= __('该租户每日API调用次数限制，0表示无限制') ?></span>
            </div>

            <div class="form-group">
                <div class="form-check">
                    <input type="checkbox" 
                           class="form-check-input" 
                           id="is_active" 
                           name="is_active" 
                           value="1"
                           <?= (!$isEdit || $tenant->getData('is_active')) ? 'checked' : '' ?>>
                    <label class="form-check-label" for="is_active">
                        <?= __('激活租户') ?>
                    </label>
                </div>
                <span class="form-hint"><?= __('禁用后该租户下的所有用户无法使用服务') ?></span>
            </div>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn-submit" id="submitBtn">
                <?= __('保存') ?>
            </button>
        </div>
    </form>
</div>

<!-- SweetAlert2 CSS -->
<link href='@static(Weline_Admin::assets/libs/sweetalert2/sweetalert2.min.css)' rel='stylesheet' type='text/css'/>
<!-- SweetAlert2 JS -->
<script src='@static(Weline_Admin::assets/libs/sweetalert2/sweetalert2.min.js)'></script>
<script>
document.getElementById('tenantForm').addEventListener('submit', function(e) {
    e.preventDefault();
    
    const formData = new FormData(this);
    const submitBtn = document.getElementById('submitBtn');
    const originalText = submitBtn.textContent;
    
    submitBtn.disabled = true;
    submitBtn.textContent = '<?= __("保存中...") ?>';
    
    fetch(this.action, {
        method: 'POST',
        body: formData
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            if (typeof Swal !== 'undefined') {
                Swal.fire({
                    icon: 'success',
                    title: '<?= __("成功") ?>',
                    text: data.message,
                    timer: 2000,
                    showConfirmButton: false
                }).then(() => {
                    if (window.parent) {
                        window.parent.location.reload();
                    } else {
                        location.reload();
                    }
                });
            } else {
                console.log(data.message);
                if (window.parent) {
                    window.parent.location.reload();
                } else {
                    location.reload();
                }
            }
        } else {
            if (typeof Swal !== 'undefined') {
                Swal.fire({
                    icon: 'error',
                    title: '<?= __("保存失败") ?>',
                    text: data.message,
                    confirmButtonText: '<?= __('确定') ?>'
                });
            } else {
                console.error('<?= __("保存失败：") ?>' + data.message);
            }
            submitBtn.disabled = false;
            submitBtn.textContent = originalText;
        }
    })
    .catch(error => {
        if (typeof Swal !== 'undefined') {
            Swal.fire({
                icon: 'error',
                title: '<?= __("请求失败") ?>',
                text: error,
                confirmButtonText: '<?= __('确定') ?>'
            });
        } else {
            console.error('<?= __("请求失败：") ?>' + error);
        }
        submitBtn.disabled = false;
        submitBtn.textContent = originalText;
    });
});
</script>

