<?php
/**
 * AI 使用量统计面板
 * @var string $period day|week|month|year
 * @var array $summary [total_requests, total_tokens, total_cost, total_cost_formatted]
 * @var array $modelRanking [{model_code, total_tokens, total_cost, total_cost_formatted}]
 * @var array $periodRange [from, to]
 */
$summary = $summary ?? ['total_requests' => 0, 'total_tokens' => 0, 'total_cost' => 0, 'total_cost_formatted' => '0.00'];
$modelRanking = $modelRanking ?? [];
$periodRange = $periodRange ?? ['', ''];
$period = $period ?? 'day';
$statsUrl = $this->getBackendUrl('ai/backend/statistics');
?>
<template>Weline_Ai::Backend/Manager/tabs.phtml</template>
<?php if (empty($embed)): ?>
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0">
                <i class="mdi mdi-chart-box-outline me-2"></i>
                <lang>统计面板</lang>
            </h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="javascript:void(0);"><lang>AI 中心</lang></a></li>
                    <li class="breadcrumb-item active"><lang>统计面板</lang></li>
                </ol>
            </div>
        </div>
    </div>
</div>
<?php endif; ?>

<style>
.weline-ai-stats-card { border-left: 4px solid; }
.weline-ai-stats-card.requests { border-left-color: var(--backend-color-primary, #0d6efd); }
.weline-ai-stats-card.tokens { border-left-color: var(--backend-color-success, #198754); }
.weline-ai-stats-card.cost { border-left-color: var(--backend-color-warning, #ffc107); }
.weline-ai-stats-period .nav-link { color: var(--backend-color-text-secondary); }
.weline-ai-stats-period .nav-link.active { color: var(--backend-color-primary); font-weight: 600; }
</style>

<!-- 周期 Tab：今日 / 本周 / 本月 / 本年 -->
<div class="row mb-3">
    <div class="col-12">
        <ul class="nav nav-pills weline-ai-stats-period" role="tablist">
            <li class="nav-item">
                <a class="nav-link <?= $period === 'day' ? 'active' : '' ?>" href="<?= $statsUrl ?>?period=day<?= !empty($embed) ? '&embed=1' : '' ?>"><lang>今日</lang></a>
            </li>
            <li class="nav-item">
                <a class="nav-link <?= $period === 'week' ? 'active' : '' ?>" href="<?= $statsUrl ?>?period=week<?= !empty($embed) ? '&embed=1' : '' ?>"><lang>本周</lang></a>
            </li>
            <li class="nav-item">
                <a class="nav-link <?= $period === 'month' ? 'active' : '' ?>" href="<?= $statsUrl ?>?period=month<?= !empty($embed) ? '&embed=1' : '' ?>"><lang>本月</lang></a>
            </li>
            <li class="nav-item">
                <a class="nav-link <?= $period === 'year' ? 'active' : '' ?>" href="<?= $statsUrl ?>?period=year<?= !empty($embed) ? '&embed=1' : '' ?>"><lang>本年</lang></a>
            </li>
        </ul>
        <?php if (!empty($periodRange[0])): ?>
        <p class="text-muted small mt-2 mb-0"><lang>统计区间</lang>: <?= htmlspecialchars($periodRange[0]) ?> ~ <?= htmlspecialchars($periodRange[1]) ?></p>
        <?php endif; ?>
    </div>
</div>

<!-- 汇总卡片 -->
<div class="row mb-4">
    <div class="col-md-4">
        <div class="card weline-ai-stats-card requests">
            <div class="card-body">
                <h6 class="text-muted mb-2"><lang>总请求数</lang></h6>
                <h3 class="mb-0"><?= (int)($summary['total_requests'] ?? 0) ?></h3>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card weline-ai-stats-card tokens">
            <div class="card-body">
                <h6 class="text-muted mb-2"><lang>总令牌数</lang></h6>
                <h3 class="mb-0"><?= number_format((int)($summary['total_tokens'] ?? 0)) ?></h3>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card weline-ai-stats-card cost">
            <div class="card-body">
                <h6 class="text-muted mb-2"><lang>约花费</lang></h6>
                <h3 class="mb-0">¥ <?= htmlspecialchars($summary['total_cost_formatted'] ?? '0.00') ?></h3>
            </div>
        </div>
    </div>
</div>

<!-- 按模型排名（倒序：消耗 tokens 最多在前） -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0"><i class="mdi mdi-format-list-numbered me-2"></i><lang>按模型消耗排名</lang></h5>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead>
                            <tr>
                                <th><lang>排名</lang></th>
                                <th><lang>模型</lang></th>
                                <th><lang>总令牌数</lang></th>
                                <th><lang>约花费</lang></th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php
                            $rank = 1;
                            foreach ($modelRanking as $row):
                                $code = $row['model_code'] ?? '';
                                $tokens = (int)($row['total_tokens'] ?? 0);
                                $costFormatted = $row['total_cost_formatted'] ?? '0.00';
                            ?>
                            <tr>
                                <td><?= $rank++ ?></td>
                                <td><code><?= htmlspecialchars($code) ?></code></td>
                                <td><?= number_format($tokens) ?></td>
                                <td>¥ <?= htmlspecialchars($costFormatted) ?></td>
                            </tr>
                            <?php endforeach; ?>
                            <?php if (empty($modelRanking)): ?>
                            <tr>
                                <td colspan="4" class="text-center text-muted"><lang>暂无数据</lang></td>
                            </tr>
                            <?php endif; ?>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
