<?php
/**
 * 模型基准测试页面模板
 * 
 * @var array $models
 * @var array $test_history
 * @var array $stats
 */
/**@var \Weline\Framework\View\Template $this */
?>
<!-- 面包屑导航 -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box">
            <h4 class="page-title">
                <i class="mdi mdi-speedometer"></i> <lang>模型基准测试</lang>
            </h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="javascript: void(0);"><lang>AI助手</lang></a></li>
                    <li class="breadcrumb-item active"><lang>模型基准测试</lang></li>
                </ol>
            </div>
        </div>
    </div>
</div>

<!-- 统计卡片 -->
<div class="row">
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-grow-1">
                        <p class="text-truncate font-size-14 mb-2"><lang>总测试次数</lang></p>
                        <h4 class="mb-2"><?= $stats['total_tests'] ?? 0 ?></h4>
                    </div>
                    <div class="avatar-sm">
                        <span class="avatar-title bg-primary-subtle text-primary rounded-3">
                            <i class="mdi mdi-chart-line font-size-18"></i>
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-3 col-md-6">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="flex-grow-1">
                        <p class="text-truncate font-size-14 mb-2"><lang>可用模型数</lang></p>
                        <h4 class="mb-2"><?= $stats['total_models'] ?? 0 ?></h4>
                    </div>
                    <div class="avatar-sm">
                        <span class="avatar-title bg-success-subtle text-success rounded-3">
                            <i class="mdi mdi-robot font-size-18"></i>
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- 基准测试主内容 -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="page-header d-flex justify-content-between align-items-center mb-3">
                    <h1><?= __('模型基准测试') ?></h1>
                    <div class="actions">
                        <button type="button" class="btn btn-primary" onclick="runBenchmark()">
                            <i class="mdi mdi-play"></i> <?= __('运行基准测试') ?>
                        </button>
                    </div>
                </div>

                <div class="alert alert-info">
                    <i class="mdi mdi-information"></i>
                    <?= __('选择要测试的模型，系统将自动运行基准测试并生成性能报告。') ?>
                </div>

                <!-- 模型选择区域 -->
                <div class="card mt-3">
                    <div class="card-header">
                        <h5><?= __('选择测试模型') ?></h5>
                    </div>
                    <div class="card-body">
                        <?php if (!empty($models)): ?>
                            <div class="row">
                                <?php foreach ($models as $model): 
                                    $modelId = $model['id'] ?? '';
                                    if (empty($modelId)) continue;
                                ?>
                                    <div class="col-md-4 mb-3">
                                        <div class="form-check">
                                            <input class="form-check-input model-checkbox" 
                                                   type="checkbox" 
                                                   name="model_ids[]" 
                                                   value="<?= htmlspecialchars((string)$modelId) ?>" 
                                                   id="model_<?= htmlspecialchars((string)$modelId) ?>">
                                            <label class="form-check-label" for="model_<?= htmlspecialchars((string)$modelId) ?>">
                                                <strong><?= htmlspecialchars($model['name'] ?? '') ?></strong>
                                                <br>
                                                <small class="text-muted">
                                                    <?= htmlspecialchars($model['vendor'] ?? '') ?> - 
                                                    <?= htmlspecialchars($model['model_code'] ?? '') ?>
                                                </small>
                                            </label>
                                        </div>
                                    </div>
                                <?php endforeach; ?>
                            </div>
                        <?php else: ?>
                            <div class="alert alert-warning">
                                <i class="mdi mdi-alert"></i>
                                <?= __('暂无可用模型，请先添加AI模型。') ?>
                            </div>
                        <?php endif; ?>
                    </div>
                </div>

                <!-- 测试历史 -->
                <?php if (!empty($test_history)): ?>
                <div class="card mt-3">
                    <div class="card-header">
                        <h5><?= __('测试历史') ?></h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th><?= __('测试时间') ?></th>
                                        <th><?= __('模型') ?></th>
                                        <th><?= __('测试套件') ?></th>
                                        <th><?= __('得分') ?></th>
                                        <th><?= __('状态') ?></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <?php foreach ($test_history as $test): ?>
                                        <tr>
                                            <td><?= $test['tested_at'] ?? '' ?></td>
                                            <td><?= htmlspecialchars($test['model_name'] ?? '') ?></td>
                                            <td><?= htmlspecialchars($test['benchmark_type'] ?? '') ?></td>
                                            <td><?= $test['score'] ?? 0 ?></td>
                                            <td>
                                                <span class="badge badge-success"><?= __('已完成') ?></span>
                                            </td>
                                        </tr>
                                    <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                <?php else: ?>
                <div class="card mt-3">
                    <div class="card-body">
                        <div class="text-center py-5">
                            <i class="mdi mdi-chart-bar" style="font-size: 80px; color: var(--backend-color-border);"></i>
                            <h3 class="mt-3"><?= __('暂无测试记录') ?></h3>
                            <p class="text-muted"><?= __('运行基准测试后，测试记录将显示在这里。') ?></p>
                        </div>
                    </div>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<!-- SweetAlert2 CSS -->
<link href='@static(Weline_Admin::assets/libs/sweetalert2/sweetalert2.min.css)' rel='stylesheet' type='text/css'/>
<!-- SweetAlert2 JS -->
<script src='@static(Weline_Admin::assets/libs/sweetalert2/sweetalert2.min.js)'></script>

<script>
(function() {
    'use strict';

    window.runBenchmark = function() {
        var checkboxes = document.querySelectorAll('input.model-checkbox:checked');
        var modelIds = [];
        
        checkboxes.forEach(function(cb) {
            var value = cb.value;
            if (value && value.trim() !== '') {
                modelIds.push(value.trim());
            }
        });
        
        if (modelIds.length === 0) {
            Swal.fire({
                icon: 'warning',
                title: '<?= __('提示') ?>',
                text: '<?= __('请至少选择一个模型进行测试') ?>',
                confirmButtonText: '<?= __('确定') ?>'
            });
            return;
        }
        
        Swal.fire({
            title: '<?= __('确认运行基准测试') ?>',
            text: '<?= __('确定要对选中的模型运行基准测试吗？') ?>',
            icon: 'question',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: '<?= __('确定') ?>',
            cancelButtonText: '<?= __('取消') ?>'
        }).then(function(result) {
            if (result.isConfirmed) {
                var data = new FormData();
                modelIds.forEach(function(id) {
                    data.append('model_ids[]', id);
                });
                data.append('test_suite', 'default');
                
                Swal.fire({
                    title: '<?= __('测试中...') ?>',
                    text: '<?= __('正在运行基准测试，请稍候...') ?>',
                    allowOutsideClick: false,
                    didOpen: function() {
                        Swal.showLoading();
                    }
                });
                
                fetch('<?= $this->getBackendUrl('*/backend/modelbenchmark/run-benchmark') ?>', {
                    method: 'POST',
                    body: data
                })
                .then(function(response) { return response.json(); })
                .then(function(data) {
                    if (data.success) {
                        Swal.fire({
                            icon: 'success',
                            title: '<?= __('测试任务已创建') ?>',
                            text: data.message || '<?= __('基准测试任务已成功创建！') ?>',
                            showConfirmButton: false,
                            timer: 2000
                        }).then(function() {
                            window.location.reload();
                        });
                    } else {
                        Swal.fire({
                            icon: 'error',
                            title: '<?= __('测试失败') ?>',
                            text: data.message || '<?= __('基准测试创建失败') ?>',
                            confirmButtonText: '<?= __('确定') ?>'
                        });
                    }
                })
                .catch(function(error) {
                    Swal.fire({
                        icon: 'error',
                        title: '<?= __('请求失败') ?>',
                        text: '<?= __('请求失败：') ?>' + error.message,
                        confirmButtonText: '<?= __('确定') ?>'
                    });
                });
            }
        });
    };
})();
</script>
