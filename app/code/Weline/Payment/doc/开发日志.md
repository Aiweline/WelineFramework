## 2026-09-12 — DevRelay 开启反馈置顶 + 静态版本 bust（1.9.61）

- 现象：用户浏览器仍无反应（HTML 已新、JS 被模板缓存/`@static` 钉死旧 `?v=` + `max-age=604800`）。
- 修：反馈告警移到按钮上方；脚本 URL 强制 `?v=1.9.61-{theme_static_version}`；清 `var/cache/template` + `server:reload`。
- 验收：`PLAYWRIGHT_TARGET_ORIGIN=https://p05113ef3.test.weline.com:9555` 真跑 `CK-PAYMENT-DEV-RELAY-UI-001` **1 passed**（本回合两次）；Browser 禁缓存点击后 danger 告警可见。

## 2026-09-12 — PDP 快捷支付弹窗 + 轻量回头确认（1.9.60）

- 需求：PDP 快捷支付打开支付商窗体；回跳 prepare 不 capture；`express-review` 确认后再 capture。
- 变更：`CONTINUE` + `patchOrder`；landing `express_review`；webhook `express_awaiting_confirm` 禁自动扣款；cancel 时 abandon 未支付 express；`product-express-pay.js` 弹窗链。
- 验收：Express*ContractTest。

## 2026-09-12 — DevRelay 开启中继空 Token 无可见反馈（1.9.60）

- 现象：Token 空时点「开启中继」仅写底部日志，看起来像无动作；状态区与头徽章重复「已停止」。
- 修：按钮下 `w-alert` 就地告警 + `aria-invalid` 聚焦 Token；摘要改为「连接详情」；e2e/契约同步。

## 2026-09-12 — PDP 快捷支付弹窗 + 轻量回头确认（1.9.60）

- 纠偏：PDP 不再跳完整结账；`product-express-pay.js` → `startExpressCheckout` + `window.open`。
- 壳：回跳 `express_prepare_only` 不 capture；`express_awaiting_confirm`；landing `checkout/express-review`；PayPal `CONTINUE` + `patchOrder`。
- 验收：契约测 ExpressAwaitingConfirm / evaluateExpressProfile。

## 2026-09-12 — DevRelay 后台美化对齐 UI 2.0（1.9.58）

- 审图：`web_ui` / `ui_shot`；线稿为侧栏+双区配置扁平行；Bootstrap `card/btn/form-check` 与同模块诊断页 `w-*` 方言冲突。
- 原型：Ops console panels——设置卡 / 本机中继卡 / 日志卡；主 CTA `primary`，关闭 `danger outline`，状态 `w-badge`。
- 变更：`Backend/DevRelay/index.phtml` + `dev-relay.js` 状态渲染；e2e `CK-PAYMENT-DEV-RELAY-UI-001`。
- 版本：`1.9.57` → `1.9.58`。

## 2026-09-12 — DevRelay 后台模板路径缺 templates/ 前缀（1.9.57）

- 现象：`payment/backend/dev-relay/index` 抛「模板文件不存在」指向 `view/Backend/DevRelay/index.phtml`。
- 根因：控制器 `fetch` 写成 `Weline_Payment::Backend/...`，框架解析不到已有的 `view/templates/Backend/DevRelay/*.phtml`。
- 修：index/console 改为 `Weline_Payment::templates/Backend/DevRelay/*.phtml`；契约测锁定。
- 版本：`1.9.56` → `1.9.57`。

## 2026-09-12 — 支付对账不变量按主题语义色等级着色

- 审图：`ui_shot` 支付诊断不变量表；状态/异常数/修复策略原先同色或 muted，未表达主题语义等级。
- 变更：`Dashboard::resolveInvariantTones`（可修复异常→danger，仅告警异常→warning，通过→success；策略 warning/info）；模板 `w-badge`/`w-text` 只绑 `data-tone`。
- 验收：`DashboardInvariantToneTest` + e2e `CK-R43-PAYMENT-INVARIANT-TONE-001` + Browser 禁缓存。

## 2026-09-12 — 万能支付后台快捷支付开关（1.9.56）

- 需求：不止主题部件；万能支付 SystemConfig 可开启/关闭快捷智能支付。
- 变更：壳级 `payment/general/express_checkout_enabled`；PayPal `payment/method/paypal/express_enabled`；`narrowCapabilitiesByConfig` + Orchestrator 壳门禁；部件空列表不再假按钮兜底。
- 验收：契约测 + 配置页探活。

## 2026-09-11 — PDP 快捷智能支付槽（1.9.55）

- 需求：商品基础信息「立即结账」下方默认注入万能支付快捷支付；点击直接走 express。
- 变更：`product-express-payment` Slot + Payment 部件默认注入；`productExpressPay` JS（加车后 `?express_pay=`）；结账自动 `weline:checkout:express-pay`。
- 验收：契约测 + Browser PDP。

## 2026-09-11 — 快捷智能支付壳 + PayPal 首发（1.9.54）

- 需求：万能支付壳落地快捷智能支付；PayPal 首发；结账页快捷与壳同一逻辑（非 HelpPay `/q`）。
- 变更：`PaymentExpressFacadeInterface` / `ExpressCheckoutOrchestrator`；`express_checkout` capability；PayPal `GET_FROM_FILE` + `express_profile`；`payment.express_address_sink.Weline_Checkout`；结账 `submitV2(express_checkout)`；部件按壳列表渲染。
- 验收：`ExpressCheckoutOrchestratorContractTest`、部件契约测。

## 2026-09-11 — 批发信用 hint 对齐定金与最低现金（1.9.52）

- 需求：报价 hint 与 FAQ/结账文案一致——只抵本期定金、最低现金占比、不抵尾款。
- 变更：`AssetCheckoutDiscountQuote` `not_applicable`/`hint_detail`/`unavailableStub` 文案；i18n。
- 验收：UT quote。

## 2026-09-11 — 批发信用 hint 明示基准币额度与折合（1.9.51）

- 需求：短提示须写清基准货币、可用额度、折合结账币、本单最多可抵。
- 修：`AssetCheckoutDiscountQuote` `hint_short` 同币/异币均带数字口径。
- 版本：`1.9.50` → `1.9.51`。

## 2026-09-11 — 批发信用 not_applicable 改为本期应付口径（1.9.50）

- 纠偏：`deposit_amount_minor<=0` 不再提示「当前订单无定金…」；改为「暂无法估算本期应付…」。
- 版本：`1.9.49` → `1.9.50`。

## 2026-09-11 — 批发信用异币抵扣禁止按数字 1:1（1.9.49）

- 现象：基准货币额度在异币结账时仍可能按数字折扣。
- 根因：① `clampApply` 在 `checkout_currency` 空串时走 identity；② FX UT 未断言 `available_checkout_minor` 折算。
- 修：空结账币 fail-closed；`testFxRoundTripClampsToAvailableBase` 断言 100 CNY → 14.29 USD。
- 版本：`1.9.48` → `1.9.49`。

## 2026-09-11 — 空 policyOverride 不再短路资产策略 SPI（1.9.48）

- 现象：后台已开 `b2b_credit_enabled`，结账仍报「站点未开启批发信用额度」。
- 根因：ObjectManager 将 `?array $policyOverride = null` 注成 `[]`，`getPolicyForScope` 把空数组当测试覆盖提前返回，跳过 `payment.asset_policy.*` SPI，B2B `b2b_credit` 丢失。
- 修：构造器将 `[]` 规范为 `null`；非空 override 仍供 UT。
- 契约：`PaymentAssetPolicySpiContractTest::testEmptyPolicyOverrideDoesNotShortCircuitSpiMerge`。
- 版本：`1.9.47` → `1.9.48`。

## 2026-09-11 — 结账资产折扣数据贡献（1.9.47）

- 边界：B2B 只 SPI 声明批发信用类型；Payment 经 `freeze_quote::enrich` / `asset_discount::apply` 贡献额度数据。
- 变更：`AssetCheckoutDiscountQuote`、`WebsiteBenchmarkCurrencyResolver`、Checkout 资产 Observer；`event.xml` 注册。

## 2026-09-10 — 缺订单字段不派发 + anomaly（1.9.46）

- `payment-lifecycle.js`：`validatePayload` 要求订单标识+amount+currency；失败 `console.warn` + `weline:payment:anomaly`，不派发 paid/pending。
- 版本：`1.9.45` → `1.9.46`。

## 2026-09-10 — DEV 控制台打印生命周期事件（1.9.45）

- `payment-lifecycle.js`：`window.DEV` / `WELINE_ENV=DEV` 时 `console.info('[WelinePayment]', …)`。
- 版本：`1.9.44` → `1.9.45`。

## 2026-09-10 — 支付返回页挂载生命周期事件（1.9.44）

- `/payment/success` 实际渲染 `return.phtml`；补 `data-weline-load="cart,paymentLifecycle"` + 按态 `data-payment-lifecycle`（paid/pending/failed/cancelled）。
- handoff / payment-success 模板同步挂载；`load:eager` 仅表示不 idle-defer，须页面声明加载。
- 版本：`1.9.43` → `1.9.44`。

## 2026-09-10 — 支付生命周期统一事件（1.9.43）

- 审图：`web_ui` / `error_shot`；订单已创建/支付处理中头栏迷你车仍显示旧件数。
- 根因：支付成功/中转页无统一 `weline:payment:*`，结账 recovery 也不派发；Cart 只听 `checkout:success`/path。
- 修：`payment-lifecycle.js`（`emitOutcome` / page boot）+ `weline.modules.js` eager；success/handoff 打 `data-payment-lifecycle`。
- 契约：`PaymentLifecycleScriptContractTest`。
- 版本：`1.9.42` → `1.9.43`。

## 2026-09-10 — FakeProvider 声明优惠动作 + supportsDiscountAction 空能力全支持（1.9.42）

- 审图：`web_ui` / `error_shot`；结账报「不支持已选优惠方式：discount_fixed_amount」但优惠券区空、合计无优惠。
- 根因：`fake_card` capabilities 未声明 `supported_discount_actions`，`supportsDiscountAction` 把 provider `null` 当成拒绝；与 `getSupportedActions`「空=全支持」冲突。
- 修：FakeProvider 声明常见 Marketing 动作；`PaymentMethod::supportsDiscountAction` 对 null 放行、[] 仍拒绝。
- 契约：`PaymentMethodDiscountSupportContractTest`。
- 版本：`1.9.41` → `1.9.42`。

## 2026-09-10 — 资产策略 SPI（1.9.41）

- Payment 增加 `PaymentAssetPolicyProviderInterface`（capability `payment.asset_policy.*`）
- `AssetPaymentService::getPolicyForScope` 合并 SPI；`required_order_types` 门禁
- 版本：`1.9.40` → `1.9.41`

## 2026-09-10 — DevRelay 表单对齐 Outlined notch（1.9.40）

- `Backend/DevRelay/index.phtml`：`form-label` → `w-field__label`，控件补 `w-input`/`w-select`，包入 `w-field`。
- 版本：`1.9.39` → `1.9.40`（若无 1.9.39 条目则为相对 1.9.38 的累计 bump）。

## 2026-09-07 — 对账扫描 TypeError：PaymentTransaction 基类（1.9.38）

- 现象：后台「支付对账不变量」报 `PaymentReconciliationService::newModel(): Return value must be of type Model, PaymentTransaction returned`。
- 原因：`PaymentTransaction` 继承 `AbstractModel`，而服务层 `newModel()` 声明返回 `Model`（`Model extends AbstractModel`）。
- 变更：`PaymentTransaction` 改为继承 `Model`，与同模块 Intent/Attempt/Refund 等模型一致。
- 版本：`1.9.37` → `1.9.38`

## 2026-09-07 — 支付回跳页 CTA 对齐 ink（1.9.37）

- 变更：`checkout/return` 主钮 `--amz-btn-primary-*` 改吃 `--color-primary`。
- 版本：`1.9.36` → `1.9.37`

## Hotfix — 2026-09-03：页脚「支付与账户」中英文 i18n 颠倒

- 现象：中文站页脚第三列标题显示 `Payments & Account`。
- 原因：`i18n/zh_Hans_CN.csv` 将「支付与账户」误译为英文并写入 `i18n_locale_dictionary`；`TranslationResolver` 对同源译文会跳过 CSV，优先吃词典库坏词；`en_US.csv` 反而保留中文。
- 变更：CSV 中文保持「支付与账户」、英文对齐 Theme 为 `Payment & Account`；删除词典库 `zh_Hans_CN` 坏词条后清缓存并 reload。
- 版本：`1.9.35` → `1.9.36`。

## Hotfix — 2026-09-03：L2 中转页确认中体验美化

- 现象：`/payment/handoff` 仅「正在确认订单…」纯文本，缺少样式与加载反馈。
- 变更：`handoff.phtml` 补齐 spinner、进度条、三步确认与 `amz-order-confirm` 视觉壳；轮询/跳转/像素抑制不变。
- 版本：`1.9.34` → `1.9.35`。
- 交付地址：`/payment/handoff`

## Hotfix — 2026-09-03：快捷支付按钮居中与可配 Logo

- 现象：快捷支付按钮左对齐；logo 仅文字伪标，无法后台配置。
- 变更：`justify-content:center`；新增默认 `paypal-express.svg`；SystemConfig `payment/method/paypal/express_logo` + 部件 `logo`（image）；优先级：部件 > 系统配置 > 默认资源。
- 版本：`1.9.33` → `1.9.34`。
- 交付地址：`/checkout`

## Hotfix — 2026-09-03：快捷支付按钮缩短为多按钮网格

- 现象：PayPal 快捷支付按钮通栏过长。
- 变更：`checkout-express-payment` methods 改为 `auto-fit` 定宽网格（约 148–200px），按钮文案收为品牌标；预留多快捷方式并排。
- 版本：`1.9.32` → `1.9.33`（样式热修，契约测试锁定网格）。
- 交付地址：`/checkout`

## Feature — 2026-09-03：结账快捷支付部件（PayPal）

- 需求：万能支付应用部件默认注入结账 `checkout-express-payment` 槽；提供 `enabled` 展示开关；对接 PayPal 快捷支付。
- 变更：新增 `checkout-express-payment` 部件（Shopify Express 布局 + Amazon 色板），派发 `weline:checkout:express-pay`；契约测试覆盖。
- 版本：`1.9.31` → `1.9.32`。
- 交付地址：`/checkout`

# Weline_Payment 开发日志

## Feature — 2026-09-03：结账支付方式展示元数据透出指南链接

- 变更：`getCheckoutPaymentMethods` payload 增加 `icon_url`、`guide_url`/`guide_route`、`has_guide`，供 Checkout 结账列表展示 logo 与供应商支付指南详情入口。
- 版本：`1.9.30` → `1.9.31`。

## Hotfix — 2026-09-02：浏览器回跳成功后通知订单已支付

- 当前状态：`implemented`
- 现象：PayPal capture 后交易 `success`，订单仍 `pending`。
- 修复：`PaymentBrowserReturnDispatcher` 在 `STATUS_PAID` 后调用 `OrderFacade::notifyOrderPaid`。
- 版本：`1.9.29` → `1.9.30`。

## Hotfix — 2026-09-02：订单详情支付记录部件槽

- 当前状态：`implemented`
- 变更：新增 `backend-order-payment-records` 部件（`default_injections` → Order 空槽）+ Hook 实现从 `PaymentAttempt` 投影；`BackendOrderPaymentRecordsService`。
- 版本：`1.9.28` → `1.9.29`。

## Hotfix — 2026-09-02：取消文案区分已取消 / 已取消成功

- 当前状态：`implemented`
- 变更：`cancel_state=already|done`；已取消显示「已取消」，本次取消显示「已取消成功」。
- 版本：`1.9.27` → `1.9.28`。

## Hotfix — 2026-09-02：取消回跳对接结账成功页

- 当前状态：`implemented`
- 变更：`outcome=cancel` 有 CheckoutSession landing / order_uuid 时直达 `/checkout/success?outcome=cancel`；无交易时 status 页展示「支付已取消」，不再空交易号文案。
- 版本：`1.9.26` → `1.9.27`。
- 交付地址：`/payment/frontend/callback/paypal?target_scope=default.default.default&outcome=cancel`、`/checkout/success?outcome=cancel`

## Hotfix — 2026-09-02：成功链硬切短路径

- 当前状态：`implemented`
- 变更：L1/L2 仅 `/payment/success`、`/payment/handoff`（status：`/payment/handoff/status`）；删除 `Frontend/Checkout` 上 successPage/handoff；无旧路径兼容跳转；Checkout L3 `/checkout/success`。
- 版本：`1.9.25` → `1.9.26`。
- 交付地址：`/payment/success`、`/payment/handoff`、`/checkout/success`

## Hotfix — 2026-09-02：唯一 callback/{method} + outcome=cancel

- 当前状态：`implemented`
- 变更：公网只保留 `callback/{method_code}`；取消同路径 `outcome=cancel`；废除 `paypal.cancel` 与嵌套 return/cancel。
- 版本：`1.9.24` → `1.9.25`。
- 交付地址：`/payment/frontend/callback/paypal?target_scope=default.default.default`

## Hotfix — 2026-09-02：扁平 callback/{method}[.cancel]

- 当前状态：`implemented`
- 变更：公网改为 `callback/paypal`、`callback/paypal.cancel`；废除 `callback/return/*`、`callback/cancel/*`。
- 版本：`1.9.23` → `1.9.24`。
- 交付地址：`/payment/frontend/callback/paypal?target_scope=default.default.default`

## Hotfix — 2026-09-02：硬删无支付码 callback 旧路径

- 当前状态：`implemented`
- 变更：公网只保留 `callback/return/{method_code}`、`callback/cancel/{method_code}`；裸 `/callback/return|cancel` Router 404；Controller action 改为 `return-dispatch` / `cancel-dispatch`；OAuth 校验拒绝无支付码路径；开发探活页移除。
- 版本：`1.9.22` → `1.9.23`。
- 交付地址：`/payment/frontend/callback/return/paypal?target_scope=default.default.default`

## Hotfix — 2026-09-02：回调地址路径携带 method_code（支付码）

- 当前状态：`implemented`
- 变更：Return/Cancel 生成 `callback/return/{method_code}`、`callback/cancel/{method_code}`；Router 解析路径注入 `method_code`；PayPal 登记/OAuth 改为 `.../return/paypal?target_scope=...`。
- 版本：`1.9.21` → `1.9.22`。

## Hotfix — 2026-09-02：壳 callback URL 携带 shell_token / 显式支付码

- 当前状态：`implemented`
- 变更：`PaymentBrowserCallbackTokenService`（HMAC `shell_token`）；`PaymentBrowserReturnContextResolver`；Catalog 区分 `browserReturnRegister`（Developer 登记）与运行时 `browserReturn(method, txn)`；Dispatcher/Cancel 优先解码 token；`transaction_no` 不再被 provider reference 覆盖。
- 版本：`1.9.20` → `1.9.21`。
- 验收：`PaymentBrowserCallbackTokenServiceTest`、`PaymentShellCallbackUrlCatalogContractTest`。

## Hotfix — 2026-09-02：三层支付成功落地 + 壳 URL 收齐

- 当前状态：`implemented`
- 变更：`PaymentShellCallbackUrlCatalog`、`callback/cancel` + `cancelPayment`、硬删 PayPal Controller；`PaymentCheckoutSession` 写入 `browser_landing_url`；L1/L2/L3（success/handoff）；Checkout pay 注入 landing；像素 `payment_success` / L2 suppress。
- 版本：`1.9.19` → `1.9.20`；Checkout `1.4.4` → `1.4.5`。
- 验收：Payment/Checkout 契约测试；PayPal 回跳经 `callback/return` → L2 → L3。
- 交付地址：`/payment/frontend/callback/return`（网关登记）；成功链 `/payment/frontend/checkout/success`、`/payment/frontend/checkout/handoff`、`/checkout/success-page`

## Hotfix — 2026-09-02：结账回跳 section 身份门禁

- 当前状态：`implemented`
- 变更：为结账回跳页既有的配送与支付信息、订单商品、订单汇总、失败支付摘要四个区块补齐唯一语义 `weline-code`；不改变 DOM 层级、样式、文案或支付业务逻辑。
- 版本：仅模板身份元数据与文档变更，模块版本保持不变。
- 验收：`php bin/w frontend:check-section-code`、`php bin/w frontend:check-theme-layout-widgets`、`php bin/w setup:upgrade --route --module=Weline_Theme`，并在真实宿主浏览器打开回跳页。
- 交付地址：https://p05113ef3.test.weline.com:9555/payment/frontend/checkout/return

## Hotfix — 2026-08-31：页脚支付方式指南部件

- 当前状态：`implemented`
- 变更：新增 `footer-payment-methods-link`，经 `default_injections` 注入 Theme `footer-payment-account-links`；链接指向 `/guide/payment`。
- 版本：`1.9.18` → `1.9.19`。

<!-- weline:module-development-baseline:generated-once -->
# Weline_Payment 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.9.18`（Scope 拖拽排序） | implementing | `FIX-PAYMENT-SCOPE-DRAG-SORT` | 2026-08-30 | 2026-08-30 | local verified |
| `1.9.2`（文档基线） | docs_handoff | `REQ-PAYMENT-DOCS-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.9.2` — 文档制度启用基线

- 基线版本：`1.9.2`
- 目标版本：`1.9.2`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-PAYMENT-DOCS-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-PAYMENT-DOCS-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-30 | implementing | → method_scope_drag_sort | 列表排序列为静态表字段，无法按 Scope 调整结账展示顺序 | 列表按 scoped `sort_order` 排序；拖拽写入 `payment/method/{code}/sort_order`（当前 target_scope）；Query `reorderPaymentMethods`；结账已读 runtime sort；`1.9.18` | 用户 | `Method/index.phtml`、`PaymentMethodManager`、`PaymentQueryProvider` |
| 2026-08-30 | implementing | → deeplink_new_tab | 深度链接希望新标签打开 | 列表/编辑页配置深度链接加 `target=_blank` + `rel=noopener noreferrer`；`1.9.14` | 用户 | `Method/index.phtml`、`Method/edit.phtml` |
| 2026-08-30 | implementing | → method_invalid_scope_redirect | 超管登录后支付方式页报「操作授权条件不满足」；URL 含截断 `target_scope=default.` | 非法/截断 Scope 302 到 `default.default.default`；去掉吞掉异常的笼统文案；`setup:upgrade` 补对象 Scope 授权；`1.9.16` | 用户 | `Method.php`、`Dashboard.php` |
| 2026-08-30 | implementing | → method_list_enable_toggle | 列表「是否启用」仅徽章，需点进配置中心 | 列表启用列改用 `<w:config:embed>`；删除手写开关与页面内 `setScopedConfig` JS；`1.9.17` | 用户 | `Method/index.phtml` |
| 2026-08-30 | implementing | → authorize_callback_copy | PayPal 报 invalid redirect_uri（未登记带 target_scope 的 URL） | 一键授权旁展示可复制 Return URL；`Payment 1.9.15` / `SystemConfig 1.2.6` | 用户 | `paypal.phtml`、`config/index.phtml` |
| 2026-08-30 | implementing | → scoped_return_uri | 店铺授权回调无范围且可静默写 Global | Return URL 强制 `?target_scope=`；Connect/Dispatcher 缺参 fail-closed；`1.9.14` | 用户 | `PaymentBrowserCallbackRoutes`、`PayPalOAuthService`、`Connect.php` |
| 2026-08-30 | implementing | → deeplink_locate_fix | 支付配置深度链接打不开定位 | 修 SystemConfig：仅 guide_locate 不再被清空；Builder 全方法带 guide_key+search；edit 页打开 guide；`1.9.13`/`1.2.4` | 用户 | `PaymentMethodConfigDeepLinkBuilder`、`Config::guideParams` |
| 2026-08-30 | implementing | → provider_icon_config | Provider 必须提供支付图标，后台可覆盖 | `icon_url` 契约 + 默认 SVG；SystemConfig `type=image` 选图；`PaymentMethodIconResolver`；列表/结账生效；`1.9.12` / SystemConfig `1.2.3` | 用户 | `PaymentMethodIconResolver.php`、config templates、`config/index.phtml` |
| 2026-08-30 | implementing | → method_config_deeplink | 支付方式列表需要直达配置中心的入口 | 新增「配置深度链接」按钮 + `PaymentMethodConfigDeepLinkBuilder`（PayPal 带 guide_locate） | 用户 | `Method/index.phtml`、`PaymentMethodConfigDeepLinkBuilder.php` |
| 2026-08-30 | implementing | → method_edit_parse_error | 「查看配置」500：edit.phtml 缺 `$configUrl=` 赋值，ParseError unexpected `=>` | 恢复配置中心深链并带 target_scope；去掉重复页头 | 用户 | `Method/edit.phtml` |
| 2026-08-30 | implementing | → method_page_header_dedupe | 支付方式页标题重复且无面包屑（页头落到模块名 Weline_Payment） | 布局页头匹配忽略菜单 action query；模板去掉重复页头/卡片同名标题 | 用户 | `PageHeaderResolver.php`、`Method/index.phtml` |
| 2026-08-30 | implementing | → method_bare_scope_redirect | 沙箱裸链 `payment/backend/method` 无 `target_scope` 时报「操作授权条件不满足」 | Method/Dashboard 缺参 302 补齐 `default.default.default`；Connect 失败回跳带 target_scope | 用户 | `Method.php`、`Dashboard.php`、`Connect.php` |
| 2026-08-30 | implementing | → oauth_success_copy | 授权回跳 toast 像「保存成功/操作成功」，语义不清 | 标题改为「PayPal 沙箱/正式环境 授权完成」，正文引导去测连接 | 用户 | `PayPalOAuthService`、`PaymentBrowserReturnDispatcher` |
| 2026-08-30 | implementing | → oauth_return_toast | PayPal 授权回跳后报 `MessageManager::add_success()` 未定义 | 改为 `MessageManager::success()` | 用户 | `PaymentBrowserReturnDispatcher` |
| 2026-08-30 | implementing | → user_agreement | PayPal Developer 同意页缺 User agreement；模块仅有 guide/policy | 新增 `/guide/payment/{method}/agreement`：接口/Registry/路由/侧栏；PayPal+FakeCard 模板；`1.9.3`→`1.9.4` | 用户 | `PaymentCustomerGuideInterface`、`agreement.phtml` |
| 2026-08-29 | implementing | → paypal_oauth_fix | 一键授权失败：短 scope 写入被拒；redirect_uri 残缺 `http:/payment/...`；Global 适配入口被挡 | Connect/Dispatcher 规范化 storage_scope；PublicOrigin 拒畸形 host；纠正无效 return_url；Global 允许点 adapter | 用户 | `PaymentConnectDispatcher`、`PayPalSandboxPublicOriginService`、`Connect` |
| 2026-08-29 | implementing | → return_env | 用户要求生产空参回跳首页，开发停留提示 | `PaymentBrowserReturnDispatcher` / `Checkout::return` 按 deploy 区分落地页与 `/` | 用户 | `PaymentBrowserReturnLandingContractTest` |
| 2026-08-29 | implementing | → payment_shell | 万能支付壳架构：去 PayPal 硬编码入口、结账三层、幂等文档、Connect | 新增 `payment-shell.md`；`ProviderConnectInterface` + `PaymentBrowserReturnDispatcher` + `Connect`；`Callback` 无网关 OAuth；Catalog 通用化；`NEXT_IFRAME`；契约测试 | 用户 | `doc/payment-shell.md`、`PaymentShellAntiLeakContractTest`、`PaymentShellIdempotencyContractTest` |
| 2026-08-29 | implementing | → unified_return_uri | 用户要求 OAuth/浏览器回调只登记一条；万能支付模块统一处理 | 唯一 Return URL=`payment/frontend/callback/return`；Webhook 仍用 `callback/notify`；目录与 OAuth 不再并列 paypal/return、后台 callback | 用户 | `Callback::return`、`PaymentBrowserCallbackRoutes`、`PaymentRedirectUriCatalog` |
| 2026-08-29 | implementing | → oauth_302_fix | 「沙箱一键授权」本地弹出 `Response terminate with status 302`；非必须上线 | Connect `authorize()` 放行 `ResponseTerminateException`，并对 OAuth URL 直接 `RedirectException`；旧 `paypal/authorize` 为兼容别名 | 用户 | `Controller/Backend/Connect.php`、`PayPalAuthorizeRedirectContractTest` |
| 2026-08-29 | implementing | → panel_nested | 用户要求高级维护最右；DevRelay 不再占一级 Tab | DevRelay 改为 `WelineAdvancedMaintenance.registerChild` 二级 Tab | 用户 | `dev-tool-panel/search-areas-after.phtml`、`doc/dev-webhook-relay.md` |
| 2026-08-29 | implementing | → spawn_host | 面板 HTTP 拉起 worker 被 WLS 回收；线上 pair 因未允许生产中转返回 400 | 生产写入 allow_on_production；本机优先 launchctl/osascript；失败提示 CLI start | 用户 | `DevRelayLocalConnectService::spawnDetachedWorker` |
| 2026-08-28 | implementing | → implementing | 用户要求静默 DevRelay：本机只填站点地址+用户 Token，后台 SSE 挂线上 | 新增 pair/public stream、LocalConnect worker、CLI、面板 Tab、文档 | 用户 | `doc/dev-webhook-relay.md`、`Service/DevRelayLocalConnectService.php` |
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-PAYMENT-DOCS-0001` / `1.9.2` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
