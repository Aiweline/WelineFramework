# 纪要：Team:架构师: — payment-method-incentive-discount

| 项 | 值 |
| --- | --- |
| seat | Team:架构师: |
| slug | payment-method-incentive-discount |
| 日期 | 2026-09-22 |
| 波次 | align_freeze |
| 模式 | 只文档（禁业务码） |
| result | closed |
| notify_pm | true |

## 1. 输入

- 规格 / 领域探查 / 电商顾问 / 需求分析
- channel `align-freeze.md`（含扩展点、支付、UI、主题、顾问 stance）
- `扩展点选型.md`、`payment-shell.md`、`meetings/扩展点.md`
- MCP `prepare_project` ready；索引工具 DISABLED → 以仓内文档为准

## 2. mechanism_summary

Payment 壳拥有激励计价编排：SystemConfig per-method → `PaymentMethodIncentiveQuoteInterface` → Event `Weline_Payment::checkout::available_methods::enrich` → Query `incentive_discount.amount_minor`；选中后写入统一 `discount_lines`（`payment_method_incentive`）进 session 快照。券行归 Marketing/Checkout；壳合并守恒。Provider capability `amount_breakdown` + `discount_passthrough`；首期 PayPal breakdown（站内多行/网关聚合）+ fake `echo_breakdown`。禁跨模块直调、禁平行计价、禁 surcharge 主路径、禁复用 `supported_discount_actions`。切换 method 重建 Intent/Attempt。

## 3. 与扩展点对齐

同波已读扩展点产出：机制类别一致。本席钉死其开放点（事件名归属 Payment、payload、PayPal 粒度、Ledger）。**无冲突、无 escalate**。

## 4. 开放点决议

| # | 决议 |
|---|------|
| OQ-1 部分退 | 比例 |
| OQ-2 PayPal | 站内多行 + 网关聚合；守恒硬 |
| OQ-3 Ledger | `TYPE_DISCOUNT` + `ROLE_DISCOUNT` 同发布单元 |
| OQ-4 payload | `incentive_discount.amount_minor` |

## 5. 表态

**同意冻结**（架构契约 + 命名 + 顾问约束已写入 contracts）。不停工。

## 6. paths_changed

- `surfaces.md`（与扩展点合并并钉命名）
- `contracts.md`（架构契约节）
- `deps.md`
- `channel/align-freeze.md`
- `meetings/架构师.md`（本文件）

## 7. 回报

- `result`: **closed**
- `notify_pm`: **true**
- `@项目经理：本席已交付/上报，请检查并更新 SESSION`
- `related_web_urls`: N/A
- `session_hint`: align-freeze · 架构师 closed；contracts/surfaces/deps 已钉；待测试补 UC 步骤与顾问复核
