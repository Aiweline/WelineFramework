# 汇审 — paypal-google-apple-pay

- 日期：2026-09-20
- 方案：`paypal-gpay-apay-funding-v1` Phase 1
- 结论：**可收口 Phase 1**（完整钱包实付 onApprove/capture 属 Phase 2）

## 交付

| 项 | 状态 |
|---|---|
| 后台 `google_pay_enabled` / `apple_pay_enabled` default=1 | 已落盘 `paypal.phtml` |
| bool 归一化 | `PaymentScopeConfigService` |
| `PayPalJsSdkUrlBuilder` + funding 四象限 | UT PASS |
| Payment `paypal.phtml` data-paypal-sdk-src + 容器 | 已落盘 |
| `paypalWalletButtons` 模块 + resource:compile | 已编译 |
| Checkout 无 `paypal.com/sdk` | 契约 PASS |
| redirect 主链 | 未改轨 |

## 实测

- UT：9 tests / 47 assertions PASS
- 本机 ConfigStore：两开关 raw=`1`；SDK URL 含 `enable-funding=googlepay,applepay`
- e2e plan-suite：曾因 admin session bootstrap 失败（环境登录）；功能契约以 UT + 配置实测为准，e2e 需环境可登录时重跑

## Phase 2 未做

- Buttons `createOrder`/`onApprove` 完整收单
- Apple 生产域关联
- 实付 Google/Apple 钱包扣款 CI 门禁
