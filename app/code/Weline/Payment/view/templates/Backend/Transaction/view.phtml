<?php
/**
 * 交易记录详情页面（内容模板）
 * 由布局系统自动包裹 HTML 框架
 */
?>
<!-- 页面标题 -->
<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-backend-page__heading">
            <h4 class="w-backend-page__title">
                <w:icon name="eye" size="sm"></w:icon><lang>交易记录详情</lang>
            </h4>
        </div>
    </div>
</div>

<!-- 消息 -->
<div class="w-grid">
    <template>Weline_Component::message.phtml</template>
</div>

<!-- 交易详情 -->
<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-card">
            <div class="w-card__header">
                <h4 class="w-card__title" style="--w-mb:0;">
                    <w:icon name="eye" size="sm"></w:icon><?= __('交易记录详情') ?>
                </h4>
            </div>
            <div class="w-card__body">
                <div class="w-grid">
                    <div style="--w-span-md:6;">
                        <table class="w-table">
                            <tr>
                                <th width="30%"><?= __('交易号') ?></th>
                                <td><?= htmlspecialchars($transaction->getData('transaction_no')) ?></td>
                            </tr>
                            <tr>
                                <th><?= __('订单ID') ?></th>
                                <td><?= htmlspecialchars($transaction->getData('order_id')) ?></td>
                            </tr>
                            <tr>
                                <th><?= __('支付方式') ?></th>
                                <td><?= htmlspecialchars($transaction->getData('method_code')) ?></td>
                            </tr>
                            <tr>
                                <th><?= __('支付金额') ?></th>
                                <td><?= htmlspecialchars($transaction->getData('amount')) ?> <?= htmlspecialchars($transaction->getData('currency')) ?></td>
                            </tr>
                            <tr>
                                <th><?= __('支付状态') ?></th>
                                <td>
                                    <?php
                                    $status = $transaction->getData('status');
                                    $statusMap = [
                                        'pending' => ['label' => __('待支付'), 'class' => 'warning'],
                                        'processing' => ['label' => __('处理中'), 'class' => 'info'],
                                        'success' => ['label' => __('支付成功'), 'class' => 'success'],
                                        'failed' => ['label' => __('支付失败'), 'class' => 'danger'],
                                        'refunded' => ['label' => __('已退款'), 'class' => 'secondary'],
                                    ];
                                    $statusInfo = $statusMap[$status] ?? ['label' => $status, 'class' => 'secondary'];
                                    ?>
                                    <span class="w-badge bg-<?= $statusInfo['class'] ?>"><?= $statusInfo['label'] ?></span>
                                </td>
                            </tr>
                            <tr>
                                <th><?= __('创建时间') ?></th>
                                <td><?= htmlspecialchars($transaction->getData('created_at')) ?></td>
                            </tr>
                            <tr>
                                <th><?= __('更新时间') ?></th>
                                <td><?= htmlspecialchars($transaction->getData('updated_at')) ?></td>
                            </tr>
                            <?php if ($transaction->getData('paid_at')): ?>
                                <tr>
                                    <th><?= __('支付完成时间') ?></th>
                                    <td><?= htmlspecialchars($transaction->getData('paid_at')) ?></td>
                                </tr>
                            <?php endif; ?>
                        </table>
                    </div>
                </div>
                
                <!-- 请求数据 -->
                <?php $requestData = $transaction->getRequestData(); ?>
                <?php if (!empty($requestData)): ?>
                    <div style="--w-mt:var(--weline-space-4);">
                        <h5><?= __('请求数据') ?></h5>
                        <pre class="weline-payment-pre"><?= htmlspecialchars(json_encode($requestData, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE)) ?></pre>
                    </div>
                <?php endif; ?>
                
                <!-- 响应数据 -->
                <?php $responseData = $transaction->getResponseData(); ?>
                <?php if (!empty($responseData)): ?>
                    <div style="--w-mt:var(--weline-space-4);">
                        <h5><?= __('响应数据') ?></h5>
                        <pre class="weline-payment-pre"><?= htmlspecialchars(json_encode($responseData, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE)) ?></pre>
                    </div>
                <?php endif; ?>
                
                <!-- 回调数据 -->
                <?php $callbackData = $transaction->getCallbackData(); ?>
                <?php if (!empty($callbackData)): ?>
                    <div style="--w-mt:var(--weline-space-4);">
                        <h5><?= __('回调数据') ?></h5>
                        <pre class="weline-payment-pre"><?= htmlspecialchars(json_encode($callbackData, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE)) ?></pre>
                    </div>
                <?php endif; ?>
                
                <div style="--w-mt:var(--weline-space-4);">
                    <a href="<?= $this->getUrl('*/backend/transaction/index') ?>" class="w-button" data-tone="neutral">
                        <w:icon name="arrow-left" size="sm"></w:icon><?= __('返回') ?>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.weline-payment-pre {
    background: var(--backend-color-card-bg, #f8f9fa);
    padding: 1rem;
    border-radius: var(--backend-radius-md, 0.375rem);
    border: 1px solid var(--backend-color-border-default, #dee2e6);
    color: var(--backend-color-text-primary, #333);
}
</style>
