<?php
/**
 * 统一浏览器回跳入口落地页（裸访问 / Developer 探活）。
 * 独立完整文档：不挂商城主题壳，避免「只有顶栏/空白」的凌乱观感。
 */
$status = (string) ($browser_return_status ?? 'ready');
$title = (string) ($browser_return_title ?? '');
$message = (string) ($browser_return_message ?? '');
$pageTitle = $title !== '' ? $title : (string) __('支付浏览器回跳入口');
?>
<!DOCTYPE html>
<html lang="zh-Hans-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex,nofollow">
    <title><?= htmlspecialchars($pageTitle, ENT_QUOTES, 'UTF-8') ?></title>
    <style>
        :root {
            color-scheme: light;
            --pr-bg: #eef2f7;
            --pr-card: #ffffff;
            --pr-ink: #111827;
            --pr-muted: #4b5563;
            --pr-line: #e5e7eb;
            --pr-ok: #047857;
            --pr-ok-bg: #ecfdf5;
            --pr-btn: #111827;
        }
        * { box-sizing: border-box; }
        html, body {
            margin: 0;
            min-height: 100%;
            background: var(--pr-bg);
            color: var(--pr-ink);
            font-family: "Noto Sans SC", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
        }
        .weline-payment-return {
            align-items: center;
            display: flex;
            justify-content: center;
            min-height: 100vh;
            padding: 28px 16px 36px;
        }
        .weline-payment-return__panel {
            background: var(--pr-card);
            border: 1px solid var(--pr-line);
            border-radius: 16px;
            box-shadow: 0 18px 40px rgba(17, 24, 39, 0.08);
            display: grid;
            gap: 14px;
            justify-items: center;
            max-width: 520px;
            padding: 32px 28px 36px;
            text-align: center;
            width: 100%;
        }
        .weline-payment-return__badge {
            align-items: center;
            background: var(--pr-ok-bg);
            border-radius: 999px;
            color: var(--pr-ok);
            display: inline-flex;
            height: 56px;
            justify-content: center;
            width: 56px;
        }
        .weline-payment-return__badge .w-icon {
            height: 28px;
            width: 28px;
        }
        .weline-payment-return__title {
            color: var(--pr-ink);
            font-size: 1.5rem;
            font-weight: 700;
            line-height: 1.35;
            margin: 0;
        }
        .weline-payment-return__lead {
            color: var(--pr-muted);
            font-size: 0.98rem;
            line-height: 1.65;
            margin: 0;
            max-width: 34rem;
        }
        .weline-payment-return__meta {
            color: #6b7280;
            font-size: 0.875rem;
            margin: 0;
        }
        .weline-payment-return__meta code {
            background: #f3f4f6;
            border-radius: 6px;
            color: var(--pr-ink);
            font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
            font-size: 0.85em;
            padding: 0.15em 0.45em;
        }
        .weline-payment-return__actions {
            margin-top: 10px;
        }
        .weline-payment-return__btn {
            background: var(--pr-btn);
            border-radius: 10px;
            color: #fff !important;
            display: inline-block;
            font-size: 0.95rem;
            font-weight: 600;
            padding: 12px 20px;
            text-decoration: none;
        }
        .weline-payment-return__btn:hover {
            background: #1f2937;
            color: #fff !important;
        }
    </style>
</head>
<body>
<main class="weline-payment-return" weline-code="payment.browser_return">
    <section class="weline-payment-return__panel" weline-code="payment.browser_return.panel">
        <div class="weline-payment-return__badge" aria-hidden="true">
            <w:icon name="check" size="md"></w:icon>
        </div>
        <h1 class="weline-payment-return__title">
            <?php if ($title !== ''): ?>
                <?= htmlspecialchars($title, ENT_QUOTES, 'UTF-8') ?>
            <?php else: ?>
                <lang>支付浏览器回跳入口</lang>
            <?php endif; ?>
        </h1>
        <p class="weline-payment-return__lead">
            <?php if ($message !== ''): ?>
                <?= htmlspecialchars($message, ENT_QUOTES, 'UTF-8') ?>
            <?php else: ?>
                <lang>【开发环境提示】此地址是支付模块统一浏览器回跳入口，已就绪。正常支付或 OAuth 授权会自动带回参数；直接打开不会完成支付。生产环境空参访问会跳转首页。</lang>
            <?php endif; ?>
        </p>
        <p class="weline-payment-return__meta">
            <lang>状态</lang>: <code><?= htmlspecialchars($status, ENT_QUOTES, 'UTF-8') ?></code>
        </p>
        <div class="weline-payment-return__actions">
            <a class="weline-payment-return__btn" href="@url{'/'}"><lang>返回首页</lang></a>
        </div>
    </section>
</main>
</body>
</html>
