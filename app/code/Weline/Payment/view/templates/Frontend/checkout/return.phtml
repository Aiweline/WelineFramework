<?php
/**
 * 支付返回页面
 */
?>
<div class="payment-return">
    <div class="w-container">
        <div class="w-grid" data-justify="center">
            <div style="--w-span-md:6;">
                <div class="w-card" style="--w-mt:var(--weline-space-12);">
                    <div class="w-card__body w-text" data-align="center">
                        <?php if ($transaction && $transaction->isSuccess()): ?>
                            <div style="--w-mb:var(--weline-space-4);">
                                <w:icon name="check" size="sm"></w:icon>
                            </div>
                            <h3 class="w-text" data-tone="success"><?= __('支付成功') ?></h3>
                            <p class="w-text" data-tone="muted"><?= __('您的订单已支付成功') ?></p>
                            <div style="--w-mt:var(--weline-space-6);">
                                <p><strong><?= __('交易号') ?>:</strong> <?= htmlspecialchars($transaction->getData('transaction_no')) ?></p>
                                <p><strong><?= __('支付金额') ?>:</strong> <?= htmlspecialchars($transaction->getData('amount')) ?> <?= htmlspecialchars($transaction->getData('currency')) ?></p>
                            </div>
                        <?php else: ?>
                            <div style="--w-mb:var(--weline-space-4);">
                                <w:icon name="close" size="sm"></w:icon>
                            </div>
                            <h3 class="w-text" data-tone="danger"><?= __('支付失败') ?></h3>
                            <p class="w-text" data-tone="muted"><?= __('支付未完成，请重试') ?></p>
                        <?php endif; ?>
                        
                        <w:hook>Weline_Payment::frontend::checkout::payment-result</w:hook>
                        
                        <div style="--w-mt:var(--weline-space-6);">
                            <a href="/" class="w-button" data-tone="primary"><?= __('返回首页') ?></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

