<?php
/**
 * 支付返回页面
 */
?>
<div class="payment-return">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card mt-5">
                    <div class="card-body text-center">
                        <?php if ($transaction && $transaction->isSuccess()): ?>
                            <div class="mb-3">
                                <i class="mdi mdi-check-circle text-success" style="font-size: 64px;"></i>
                            </div>
                            <h3 class="text-success"><?= __('支付成功') ?></h3>
                            <p class="text-muted"><?= __('您的订单已支付成功') ?></p>
                            <div class="mt-4">
                                <p><strong><?= __('交易号') ?>:</strong> <?= htmlspecialchars($transaction->getData('transaction_no')) ?></p>
                                <p><strong><?= __('支付金额') ?>:</strong> <?= htmlspecialchars($transaction->getData('amount')) ?> <?= htmlspecialchars($transaction->getData('currency')) ?></p>
                            </div>
                        <?php else: ?>
                            <div class="mb-3">
                                <i class="mdi mdi-close-circle text-danger" style="font-size: 64px;"></i>
                            </div>
                            <h3 class="text-danger"><?= __('支付失败') ?></h3>
                            <p class="text-muted"><?= __('支付未完成，请重试') ?></p>
                        <?php endif; ?>
                        
                        <w:hook>Weline_Payment::frontend::checkout::payment-result</w:hook>
                        
                        <div class="mt-4">
                            <a href="/" class="btn btn-primary"><?= __('返回首页') ?></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

