# REQ-MARKETING-0015 — 挽回运营看板 MVP

## EARS

- When 运营打开挽回看板，the system shall 按活动汇总 sent / skipped(reason) / coupons_issued / coupons_redeemed。
- The system shall 按 SendLog 主体键前缀 `order:` / `qt:` / `cart:` 分类计数。

## 非目标

- 邮件打开率、点击率（无像素追踪前不做）。
