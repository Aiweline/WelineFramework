# Weline_Marketing 开发日志

## 2026-09-22 — 续付订单券 paint：refresh:false 也画券标（1.3.5）

- `checkout-coupon.js`：`weline:cart-updated` 在 `refresh:false` 时仍按 `discount_preview`/`coupon_code` `syncAppliedState`，避免续付有优惠金额却看不到 DEMO10 标签。
- 版本：`1.3.4` → `1.3.5`。

## 2026-09-22 — RandomCoupon issue 支持 context.valid_days（1.3.4）

- `issueRandomCoupon`：`context['valid_days']` 为正 int 时按该天数算 `END_DATE`；缺省仍 30 天（WaitGift 兼容）。
- Newsletter 欢迎礼可传 14；本波不改 upsert。
- 版本：`1.3.3` → `1.3.4`。

## 2026-09-21 — 结账优惠券 presence 对齐 widget code（1.3.3）

- `data-testid` / `data-widget-code` = `checkout-coupon`（保留 `data-marketing-checkout-coupon` DOM 钩子）。
- 协同 Theme 2.2.511 required injection 整区替换，消除结账优惠券双渲。
- 版本：`1.3.2` → `1.3.3`。

## 2026-09-15 — 邮件预览 items_html 样例改为真实明细表（1.3.2）

- 现象：可视化正文出现空 `<table>…</table>` / 省略号碎屑，商品明细不可读。
- 改：`MailChannelProvider` 用 `WinbackMailItemsHtmlBuilder` 生成预览样例行，替代占位 `<table>…</table>`。
- 版本：`1.3.1` → `1.3.2`。

## 2026-09-15 — 营销路线图 Playwright e2e / plan-suite（续：Ch5）

- 新增前台：`Test/e2e/frontend/Weline_Marketing-cart-progress.spec.js` + `cart-progress-fixture.php`（契约 / 部件渲染 / Service 进度 / `/cart` 槽位）。
- `e2e-plan-suite` 收口含 Ch5 registry + render + `build_progress`。
- 跑法：`PLAYWRIGHT_TARGET_ORIGIN=https://127.0.0.1:9555 php bin/w e2e:run …` → 路线图+Ch5 **11 passed**。
- 已知未绿：R4.3 侧栏菜单套件（需隔离库 `WELINE_E2E_ISOLATED_DB=1`）；smoke FLOW 交互用例易挂起；部分既有 PHPUnit 与本路线图无关失败。

## 2026-09-15 — 营销路线图 Playwright e2e / plan-suite

- 新增：`Test/e2e/backend/Weline_Marketing-lifecycle-roadmap.spec.js`（Ch1–4/6 后台通路）与 `*-plan-suite.spec.js`（`e2e-plan-suite` 收口）。
- 跑法：`PLAYWRIGHT_TARGET_ORIGIN=https://127.0.0.1:9555 php bin/w e2e:run … --module=Weline_Marketing --project=chromium --headless` → **6 passed**。
- smoke 候选路由补 `winbackdashboard` / `lifecycle` / `segment`。

## 2026-09-15 — Upgrade 种子 GLOBAL 邮件模板（1.3.1）

- 范围：`Setup/Upgrade` 调用 `MailTemplateSeeder::syncAll(SCOPE_GLOBAL)`，把欢迎/遗弃车等渠道默认模板写入抽象默认站层，建站助手按继承不再待办。
- 验收：契约 UT；默认站 `#0` `smtp_mail_template` / `smtp_mail_template_i18n` done。
- 版本：`1.3.0` → `1.3.1`。

## 2026-09-15 — Marketing 挽回路线图 Ch1–Ch6（1.3.0）

- Ch1：购物车遗弃 `cart_abandon_reminder`（Runner/Cron/十语模板/后台类型）；规格 `winback-cart-abandon.md`；`REQ-MARKETING-0010`；UT `WinbackCartAbandonRunnerTest`；SendLog `cart:{key}`；活跃结账 skip。
- Ch2a/2b：`incentive_rule_id` + 多步 `WinbackStepResolver`；step≥2 发券；`coupon_code` 列/模板/渠道；未付 `order:`、结账 `qt:` 前缀；分群钩子。
- Ch3：`LifecycleCampaign`/`LifecycleSendLog` + 注册 Observer + `welcome_customer` 邮件 + 后台 CRUD；`REQ-MARKETING-0012`。
- Ch4：`AudienceSegment` + `customer_signals`；`REQ-MARKETING-0013`。
- Ch5：`MarketingCartProgressService` + Taglib/widget；`REQ-MARKETING-0014`。
- Ch6：挽回看板聚合（无 open/click）；`REQ-MARKETING-0015`。
- 版本：`1.2.17` → `1.3.0`。

## 2026-09-15 — 挽回邮件商品金额货币符号（1.2.17）

- 审图一致性：行价/小计由 `CNY 285.00` 改为 `¥285.00`（`WinbackMailItemsHtmlBuilder::formatMoney`）。
- 版本：`1.2.16` → `1.2.17`。

## 2026-09-14 — 催付邮件摘要表间距 + 下单时间（1.2.16）

- 审图：摘要行 `width:38%` 中间大空；`下单时间` 空（订单 `created_at` 常空，实值在 `create_time`）。
- 改：未付/遗弃模板摘要表 label `width:1%;white-space:nowrap`；`WinbackMailNotifier` 规范化时间；Order/Checkout 信号回退 `create_time`。
- 版本：`1.2.15` → `1.2.16`。

## 2026-09-14 — 挽回邮件商品图 Host 可达（1.2.15）

- 审图：邮件预览商品明细裂图。
- 根因：图 Origin 落在不可达 `e2e-test-*.weline.test`（website_id 指向 e2e 站 / 域名评分偏高）。
- 改：Smtp `MailBrandContextService` 贬低 e2e/`*.weline.test`，回落 default `*.test.weline.com`；Marketing `WinbackMailNotifier` 商品图 Origin 同样门禁；builder 异 Host `/pub|/media` 改写到 site_url。
- 验：UT builder 5/5；即使 website_id=1 发信，商品图 `p05113ef3…:9555` 且 HTTP 200；Browser embed 可见实拍缩略图。
- 版本：`1.2.14` → `1.2.15`。

## 2026-09-14 — 挽回邮件商品明细块（1.2.14）


- 未付订单 / 结账遗弃模板注入 `{{var.items_html|raw}}`（图+标题+规格+数量单价+小计）。
- `WinbackMailItemsHtmlBuilder` 组装邮件安全表格；相对图 URL 按站点 `site_url` 绝对化并补本机 `:9555`。
- Order `line_items`（`UnpaidOrderMailLineProjector`）与 Checkout `checkout_signals.line_items` 供 Marketing 注入，不 `use` 对方 Model。
- 版本：`1.2.13` → `1.2.14`。

## 2026-09-14 — 结账遗弃挽回 Winback Phase 2（1.2.13）

- 规格：`doc/开发/spec/winback-checkout-abandon.md`；需求 `REQ-MARKETING-0009`。
- 边界：遗弃时限=`WinbackCampaign.abandon_after_hours`；主体=`quote_token`（存 SendLog.`order_uuid`）；Checkout `checkout_signals`（1.4.91）+ `continue_checkout_url`；禁止 use Checkout/Order Model。
- 码：`TYPE_CHECKOUT_ABANDON_REMINDER` / `WinbackCheckoutAbandonRunner` / Cron `marketing_winback_checkout_abandon` `*/15` / Smtp `checkout_abandon_reminder` 十语模板 / 后台类型选择。
- 验：Runner/渠道 UT PASS；实发预览 log_id=9（无裸 `{{#if}}`、logo 带 `:9555`）；Browser 新建表单含「结账遗弃提醒」；诊断页无挽回 CTA。
- 版本：`1.2.12` → `1.2.13`；optional 增 `Weline_Checkout`。

## 2026-09-14 — 未付订单催付渠道名九语（1.2.12）

- 现象：发件记录渠道下拉「未付订单催付」在 bn_BD 等仍中文。
- 改：Marketing `i18n` 九语译文 + LocaleDictionary upsert/publish。
- 验：Browser bn_BD 渠道值为「অপরিশোধিত অর্ডার অনুস্মারক」。

## 2026-09-14 — 挽回邮件用订单 scope 快照语言（1.2.11）

- `WinbackMailNotifier`：`smtp.send` 的 `locale` 优先 DTO.`locale`（来自 Order `scope_snapshot`），空/`default` 才回退网站默认语言。
- 验：`WinbackMailNotifierLocaleTest`（en_US 覆盖 / default 回退 / 发信载荷带 locale）。
- 版本：`1.2.10` → `1.2.11`。

## 2026-09-14 — 促销源键九语（1.2.10）

- Marketing `i18n` 九语补 `促销`；Dictionary upsert + `i18n:collect`。

## 2026-09-14 — 未付订单挽回 Winback Phase 1（1.2.9）

- 规格：`doc/开发/spec/winback-unpaid-order.md`；需求 `REQ-MARKETING-0008`。
- 边界：遗弃时限=`WinbackCampaign.abandon_after_hours`；Order 仅 `order_signals` 未付事实 + `continue_pay_url`；禁止 use Order/Checkout Model。
- 码：独立 Winback 表 / `WinbackUnpaidOrderRunner` / Cron `*/15` / Smtp `unpaid_order_reminder` / 后台「挽回活动」+ MailChannelProvider。
- 验：`WinbackUnpaidOrderRunnerTest`（未到 abandon 不发、再验已付 skip、可发 sent）9 UT PASS；`setup:upgrade` 建表；Cron `marketing_winback_unpaid_order` 已收集；Browser 禁缓存创建启用活动「Phase1 UT 催付-浏览器验收」；`order_signals` 经 extends rebuild + compile 注册。
- 版本：`1.2.8` → `1.2.9`；optional `Weline_Order` / `Weline_Smtp` / `Weline_Cron`。

## 2026-09-14 — 站点默认时区录入 → UTC 存判（活动/券/特价/布局）

- 规格：`doc/开发/spec/website-utc-schedule-windows.md`（Framework `DateTime\Timezone` 唯一入口）。
- Framework：`Timezone` 门面 + `ScheduleWindowUtcMigrator`；UT `TimezoneTest`。
- Marketing：Campaign/Rule/Coupon/DateRange 存判改 UTC；表单旁注站点时区并回填本地墙钟。
- 迁移：`scripts/migrate-schedule-windows-to-utc.php`（Upgrade 一次调用，marker 防双跑）。
- 验收：18 UT；Browser 活动新建见「按站点时区」；e2e `Weline_Marketing-schedule-utc-window.spec.js` PASS。

## 2026-09-14 — 结账页优惠券金额不显示（幽灵标签）

- 审图：`web_ui` / `ui_shot`（结账摘要）；标签已应用、无优惠金额、应付未扣。
- 线稿：小计/运费 → 券 Tab+绿标签（无金额）→ 应付合计。
- 原型：服务端补行报价；成功水合 `refresh:false` 推 `discount_preview`；结账合并 pending 预览。
- 修：`MarketingCheckoutCouponSession::resolveCartLines`；禁止 0 额 persist；`checkout-coupon.js` 清幽灵券；bump `?v=20260914-coupon-totals1`。
- 验收：`CheckoutCouponScriptContractTest`；Browser 结账优惠行与标签金额一致。

## 2026-09-14 — 优惠券列表搜索/来源过滤与分页修复

- 问题：列表无筛选条；`getChildHtml('pagination')` 无法输出分页 HTML。
- 修：代码搜索 + 来源活动/类型/状态过滤；分页改为 `{{pagination}}` 并保留筛选 query。
- 验收：Browser 筛选「维护等待礼金」仅 MW*；契约锁定 filter + pagination。

## 2026-09-14 — 优惠券来源标记（1.2.8）

- 审图：`web_ui` / `ui_shot`（优惠券列表）；线稿：缺「来源」列，折扣值未标基准货币。
- 原型：列表加来源徽章；折扣值列头「基准货币 CNY」；固定金额行旁注币种。
- 修：Coupon 增 `source_*`；`CouponSourceAttribution`；`issueRandomCoupon`/手工建券写入；列表打开时回填历史维护券；表单展示来源徽章。
- 规格：`doc/开发/spec/coupon-source-attribution.md`；需求 `REQ-MARKETING-0007`。
- 验收：契约 UT；Browser 列表见「来源」。
- 版本：`1.2.7` → `1.2.8`。

## 2026-09-14 — 审图：配置值明示「按基准货币」

- 审图：`web_ui` / `ui_shot`；红箭头指向折扣值、最低订单金额。
- 问题：固定金额时单位/帮助仍可能停在 `%`；配置值未在标签层写明「基准货币」。
- 修：标签改为「折扣值（基准货币 CNY）/（%）」与「最低订单金额（基准货币 CNY）」；单位与帮助改为独立节点切换；JS 捕获 `change`/`input` 防不同步。
- 验收：Browser 固定金额 → 标签/CNY addon/帮助一致；契约 UT 通过。

## 2026-09-14 — 审图：优惠券新建表单人性化分组

- 审图分流：`web_ui` / `ui_shot`（后台券新建表单；用户仅附图）。
- 线稿：顶 alert → 单卡扁平行（规则+代码 / 类型+值+瘦基准币+状态 / 门槛+次数）→ 主 CTA。
- 原型调整（fold）：三分组「基础信息 / 优惠设置 / 使用限制」；状态归基础；折扣值 `w-field__addon` 随类型切 `%`/`CNY`；基准货币只读徽章移出挤列；免运费/赠品隐藏折扣值。
- fail→pass：人性化（混排/百分比旁像货币）、规范美观（1 列瘦字段）、主题契合（`w-field`/`w-field__addon`/`w-alert` Token）。
- 改：`Backend/coupon/form.phtml`、`css/coupon-form.css`、`js/backend/coupon-form.js`、`backend/weline.modules.js`（compile）。
- 验收：Browser 禁缓存 WB-OP；契约 `FixedAmountBaseCurrencyContractTest`；切换固定金额单位变 CNY。

## 2026-09-14 — 固定额优惠按基准货币换算（1.2.7）

- 审图：`web_ui` / `ui_shot`（结账优惠区）；线稿：合计行用 `USD` 码，券标签曾用 `-$10.00` 裸符号；原型：标签与合计统一 `CURRENCY amount`。
- 问题：固定额/门槛按结账币数字 1:1 扣减，CNY 10 与 USD 10 同数字可被薅羊毛。
- 修：`MarketingBaseCurrencyAmount` + `CurrencyRateService::tryConvert`；`FixedAmount` / `Percentage.max_discount` / `BuyXGetY` 固定额、券 `min_amount`、订单小计/商品价格条件均基准币录入、结账换算；缺汇率 fail-closed；后台券表单基准货币只读提示；结账标签 `-USD 10.00`。
- 规格：`doc/开发/spec/fixed-amount-base-currency.md`。
- 验收：`MarketingBaseCurrencyAmountTest`、`FixedAmountBaseCurrencyContractTest`、`CheckoutCouponScriptContractTest`；实机探针 10 CNY→USD≈1.25（非 10）；Browser WB-OP 券表单基准货币提示；e2e `MARKETING-FX-001`（代理无壳时诚实 skip，与 smoke 同口径）。
- 版本：`1.2.6` → `1.2.7`。

## 2026-09-11 — 随机礼金 apply_to=cart 无优惠额（1.2.5）

- 审图：跳过（报价金额计算，无 UI 改版）。
- 现象：维护等待礼金已配置面额并同步 Marketing 规则，结账应用券后无优惠信息（`amount_minor=0`）。
- 根因：`RandomCouponCampaignProvider` upsert 写入 `apply_to=cart`，而 `FixedAmount`/`Percentage` 只识别 `subtotal|shipping|matched_products`，基数为 0 → `min(面额,0)=0`。
- 修：Provider 改为 `apply_to=subtotal`；动作层将 `cart` 别名到 `subtotal` 兼容已落库规则；契约 `FixedAmountApplyToCartAliasContractTest`。
- 探针：券 `MWE589A077` + 小计 50 → `amount_minor=1000`，文案「优惠 10.00 元」。
- 版本：`1.2.4` → `1.2.5`。

## 2026-09-10 — restore 0 额仍展示已应用券码（1.2.4）

- 审图：`web_ui` / `error_shot`；结账优惠券区空却报 `discount_fixed_amount`。
- 根因：`restoreOnly` 报价额为 0 时 `clearAppliedState` 清 UI，但会话券仍在，`freezeQuote` 静默带上动作。
- 修：`restoreOnly` 走 `syncAppliedState(code, null)` 展示标签；契约锁定。
- 版本：`1.2.3` → `1.2.4`。

## Hotfix — 2026-09-08：切回零售恢复优惠券 UI（1.2.3）

- 问题：批发→零售时 DOM 仍为 tob，hydrate 误清且可能误删 toc 会话券
- 修：`hydrateForCurrentType` 用事件 `cart_type` 强制类型；`restoreOnly` 不 wipe 会话；`weline:cart-updated` 再 hydrate
- 版本：`1.2.2` → `1.2.3`

## Hotfix — 2026-09-08：切回零售恢复优惠券标签（1.2.3）

- 问题：批发切回零售时 DOM 仍是 tob，hydrate 误清且未用事件 `cart_type`
- 修：`hydrateForCurrentType(forcedType)` + `restoreOnly` 不误删 toc 会话券；`weline:cart-updated` 再 hydrate
- 版本：`1.2.2` → `1.2.3`

## Hotfix — 2026-09-08：优惠券按 cart_type 分记（1.2.2）

- 问题：零售车应用的券（会话全局）串到批发车摘要/标签（DEMO10 仍显示）
- 修：`MarketingCheckoutCouponSession` 按 `cart_type` 分键；`toc` 默认可券，`disablesStorefrontDiscounts`/非 toc 不生效；Cart 折扣预览与 Checkout freeze 按类型读券；`checkout-coupon.js` 传 `cart_type` 且切批发清 tags
- 版本：`1.2.1` → `1.2.2`

## Hotfix — 2026-09-05：活动主题 deal 避免结账双重折扣（1.2.1）

- 问题：前台购物车快照已写入活动成交价后，Marketing `promotion_activity_theme` automatic 规则若再按百分比扣减会双重折扣。
- 修复：`Percentage` / `FixedAmount` 对 `external_managed` + `source_type=promotion_activity_theme` 返回 0（成交价已在 Product 加购快照烘焙）。
- 版本：`1.2.0` → `1.2.1`

## 2026-09-13 — 迷你车优惠券标签有额但小计不扣

- 当前状态：`implemented`
- 问题：优惠券芯片显示 `MWE589A077 -¥10`，底部仅「小计」未出现「商品小计 / 优惠行 / 应付扣减」。
- 根因：标签金额来自 Marketing `quoteDiscount`（请求显式带码）；小计依赖 `getCart` → `discount_preview`（仅读会话码，且易被缓存/会话延迟打空）。
- 变更：`checkout-coupon.js` 应用时带行报价，事件附带 `discount_preview`+`coupon_code`；`getCart` 允许 `coupon_code`；迷你车乐观合并预览并在刷新时传码；客户身份与 Cart resolver 对齐。
- 版本：`1.2.5` → `1.2.6`

## Hotfix — 2026-09-05：结账优惠券英文提示中文化

- 当前状态：`implemented`
- 问题：英文店面下优惠券校验失败提示仍为中文（`优惠券无效、不可用或已达使用上限`）。
- 根因：`en_US.csv` 对应词条未译；`checkout-coupon.js` 客户端兜底文案硬编码中文。
- 变更：补齐结账优惠券相关英文词条；模板 `data-i18n-*` 透传；JS 经属性读取；迷你购物车部件同步。
- 验收：`en_US/checkout` 应用无效码后提示为 `Coupon is invalid, unavailable, or has reached its usage limit`；`zh_Hans_CN` 仍为中文；契约单测通过；已 `resource:compile welineModules`。
- 版本：`1.1.9` → `1.2.0`

## Hotfix — 2026-09-03：结账优惠券 Tab 标签

- 变更：`checkout-coupon` 根节点补 `data-mini-cart-tab-label` / `data-mini-cart-tab-label-source`，供结账/迷你购物车 extras Tab 显示「优惠券」。
- 版本：`1.1.8` → `1.1.9`

## 2026-09-03 — 优惠券报价与小计客户身份对齐

- 当前状态：`implemented`
- 问题：Marketing `quoteDiscount` 未注入登录客户，标签显示 -¥4.80；Cart `discount_preview` 带 `customer_id` 命中每人限用 → `amount_minor=0`，小计不扣减。
- 变更：`MarketingCheckoutCouponSession` 服务端注入当前客户；`applyCoupon` 带购物车行报价校验后才写会话；前端 apply 传 lines，金额≤0 时不展示伪折扣。
- 版本：`1.1.7` → `1.1.8`

## Feature — 2026-09-03：购物车页优惠券部件

- 当前状态：`implemented`
- 变更：新增 `cart-coupon` → `cart-summary-discount`；复用结账优惠券模板与会话；Upgrade 回填 cart 布局槽。
- 版本：`1.1.6` → `1.1.7`

## Hotfix — 2026-08-31：页脚活动改链 Promotion 特价

- 当前状态：`implemented`
- 变更：`footer-campaign-link` 目标由 `/marketing/campaign` 改为 `/promotion/deals`（与顶栏「今日特价」同源）。
- 版本：`1.1.5` → `1.1.6`。

## Hotfix — 2026-08-31：页脚活动入口 + 前台活动页

- 当前状态：`implemented`
- 变更：新增 `/marketing/campaign` 活动列表页；`footer-campaign-link` 默认注入 Theme `footer-payment-account-links`（文案「活动」）。
- 版本：`1.1.4` → `1.1.5`。

## 2026-08-30 — 维护等待礼金随机券扩展点

- 当前状态：`implemented`
- 变更：新增 `RandomCouponCampaignProviderInterface`（`provides`）；支持外部模块 upsert 券规则并 `issueRandomCoupon` 按人随机一码。
- 消费方：`Weline_Maintenance` 维护等待礼金。
- 验收：`RandomCouponCampaignProviderContractTest`。

## 2026-08-30 — 前台 JS 改 modules + data-weline-load（第二波）

- 当前状态：`implemented`
- 变更：登记 `view/statics/frontend/weline.modules.js`；部件/布局改 `data-weline-load`，删除 `@static`/`<js>` 直引。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。

<!-- weline:module-development-baseline:generated-once -->
# Weline_Marketing 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 2026-08-28 — 后台 URL 禁止 getEdit 段 + 补齐 rule/edit

- 当前状态：`implemented`
- 问题：列表链到 `…/rule/getEdit`；`get*` 是 HTTP 方法前缀，正确路径为 `…/rule/edit`。
- 实现：模板/redirect 改为 `add`/`edit`/`save`；补 `Rule::getEdit`；Campaign 同步修正；MCP `session_startup_notices` + `AI硬规则索引` 硬约定。
- 验收：`/marketing/backend/rule/edit?id=4` 可打开（托管规则只读提示）。

## 2026-08-28 — 规则列表空数据修复 + 外部折扣对接文档

- 当前状态：`implemented`
- 问题：`loadLocalDescription` 联表 `weline_marketing_rule_local_description` 不存在，异常被吞导致「暂无数据」；活动已绑定规则 4 但后台看不到。
- 实现：LocalDescription 补齐 `#[Col]`/`#[Table]`；`Setup/Upgrade` 建表；列表查询本地化失败时回退无 join；新增 `doc/外部活动折扣集成.md`（一活动一规则 / upsert 联动）。
- 验收：升级后列表可见活动同步规则；对接文档可独立给其他模块使用。

## 2026-08-28 — 规则列表去掉页面内重复面包屑

- 当前状态：`implemented`
- 问题：布局已渲染菜单面包屑，模板再写「后台 / 营销 / 规则管理」导致双层。
- 实现：`view/templates/backend/rule/index.phtml` 移除内联 `w-breadcrumb`，只保留布局面包屑。

## 2026-08-28 — 外部托管折扣禁止在 Marketing 删除

- 当前状态：`implemented`
- 问题：活动同步规则若在折扣模块被删，会与来源活动脱节。
- 实现：`ExternalManagedRuleOwnership` 解析 `external_managed` 标记；`Rule::delete` / `postDelete` 拒绝并提示到来源模块管理；列表「系统同步」徽章。
- 验收：单元测 `ExternalManagedRuleOwnershipTest`；deals 规则同步后后台不可删。

## 2026-08-28 — ExternalDealDiscountProvider 扩展

- 当前状态：`implemented`
- 问题：活动/主题等业务模块需要创建自动折扣，但不能直接依赖 Rule 模型。
- 实现：`Api/Deal/ExternalDealDiscountProviderInterface` + `ExternalDealDiscountProvider`；`module.php` provides 注册；契约测 `ExternalDealDiscountProviderContractTest`。
- 验收：Promotion 活动主题通过提供者 upsert automatic 规则；报价 `matched_products` 生效。

## 2026-08-27 — checkout-coupon 部件槽 + Amazon 默认样式

- 当前状态：`implemented`
- 问题：结账优惠券经 Hook 直出且被 totals flex 压扁；应用按钮使用站点主色。
- 实现：`checkout-summary-discount` 槽 + `default_injections`；部件模板与 CSS 改为 Amazon 输入+灰底应用；移除 `summary-discount-before` Hook 直出。
- 验收：契约测试 `CheckoutCouponWidgetContractTest`；结账页运费与合计之间全宽优惠券行。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.1`（文档基线） | docs_handoff | `REQ-MARKETING-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.0.1` — 文档制度启用基线

- 基线版本：`1.0.1`
- 目标版本：`1.0.1`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-MARKETING-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-MARKETING-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-MARKETING-0001` / `1.0.1` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。

## `1.1.0` — 万能优惠规则 / DiscountQuote / Checkout V2

- 基线版本：`1.1.0`（`etc/module.php`）
- 需求 ID：`REQ-MARKETING-0002` ~ `REQ-MARKETING-0005`
- 范围：DiscountQuote 契约、Checkout freeze/submit 接入、BinQuery 门面、结账券 Widget、Cart 只读预览（M1.5 在 Cart 模块）

### ACL 迁移对照（旧 → 新）

| 旧 ACL source | 新 ACL source | 菜单/控制器 |
|---|---|---|
| `Weline_Marketing::coupon` / `coupon_list` | `Weline_Marketing::commerce:marketing:coupons`（组）+ `coupons_index`（列表） | 万能优惠券 |
| `Weline_Marketing::coupon_add` | `Weline_Marketing::commerce:marketing:coupons_add` | 新建优惠券 |
| `Weline_Marketing::coupon_save` | `Weline_Marketing::commerce:marketing:coupons_save` | 保存优惠券 |
| `Weline_Marketing::campaign` / `campaign_list` | `Weline_Marketing::commerce:marketing:campaigns`（组）+ `campaigns_index`（列表） | 万能促销活动 |
| `Weline_Marketing::campaign_add` | `Weline_Marketing::commerce:marketing:campaigns_add` | 新建促销 |
| `Weline_Marketing::campaign_save` | `Weline_Marketing::commerce:marketing:campaigns_save` | 保存促销 |
| （新增） | `Weline_Marketing::commerce:marketing:rules`（组）+ `rules_index`（列表） | 万能优惠规则 |

> 后台角色需重新勾选新 ACL；旧 source 不再绑定菜单。

### Submit 门禁（Checkout V2）

- `freezeAndQuote` 写入 `discount`、`coupon_code`、`payment_method` 至 quote session。
- `submit` 前：`DiscountQuoteService::validateToken()` + `DiscountValidationService`（payment × action_payloads）。
- 客户端 `discount_amount` / `discount_amount_minor` 一律拒绝（V2 `rejectClientAuthority` + legacy `placeOrder` / `Checkout.php`）。

### 测试

- `Test/Unit/Service/DiscountQuoteContextBuilderTest.php`
- `Test/Unit/Service/DiscountQuoteServiceTest.php`
- `Test/Unit/Query/MarketingQueryProviderContractTest.php`

### 已知限制

- 订单取消不自动回滚 RuleUsage / 券核销（MVP）。
- `Controller/Api/Marketing.php` 已 `@deprecated`，前台走 BinQuery。
