<?php
/** @var \Weline\Framework\View\Template $this */
$rules = $rules ?? [];
?>
<div class="w-grid" data-testid="marketing-coupon-form">
    <div style="--w-span:12;">
        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
            <h4 class="w-backend-page__title"><?= __('新建优惠券') ?></h4>
            <a class="w-button" href="<?= $this->getBackendUrl('marketing/backend/coupon/index') ?>" data-tone="neutral">
                <?= __('返回列表') ?>
            </a>
        </div>
        <template>Weline_Component::message.phtml</template>
        <div class="w-card">
            <div class="w-card__body">
                <w:form method="post" action="@var($this->getBackendUrl('marketing/backend/coupon/save'))">
                    <div class="w-grid">
                        <div style="--w-span-md:6;--w-mb:var(--weline-space-4);">
                            <label class="w-field__label" for="marketing-coupon-rule"><?= __('营销规则') ?></label>
                            <select id="marketing-coupon-rule" data-testid="marketing-coupon-rule" class="w-select" name="rule_id" required>
                                <option value=""><?= __('请选择') ?></option>
                                <?php foreach ($rules as $rule): ?>
                                    <option value="<?= (int)$rule->getId() ?>">
                                        <?= htmlspecialchars((string)$rule->getData('name'), ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div style="--w-span-md:6;--w-mb:var(--weline-space-4);">
                            <label class="w-field__label" for="marketing-coupon-code"><?= __('优惠券代码') ?></label>
                            <input id="marketing-coupon-code" data-testid="marketing-coupon-code" class="w-input" type="text" name="code" required maxlength="100">
                        </div>
                    </div>
                    <div class="w-grid">
                        <div style="--w-span-md:4;--w-mb:var(--weline-space-4);">
                            <label class="w-field__label" for="marketing-coupon-type"><?= __('优惠类型') ?></label>
                            <select id="marketing-coupon-type" data-testid="marketing-coupon-type" class="w-select" name="type" required>
                                <option value="percentage"><?= __('百分比折扣') ?></option>
                                <option value="fixed_amount"><?= __('固定金额') ?></option>
                                <option value="free_shipping"><?= __('免运费') ?></option>
                                <option value="gift"><?= __('赠品') ?></option>
                            </select>
                        </div>
                        <div style="--w-span-md:4;--w-mb:var(--weline-space-4);">
                            <label class="w-field__label" for="marketing-coupon-value"><?= __('折扣值') ?></label>
                            <input id="marketing-coupon-value" data-testid="marketing-coupon-value" class="w-input" type="number" name="discount_value" min="0" step="0.0001" value="10">
                        </div>
                        <div style="--w-span-md:4;--w-mb:var(--weline-space-4);">
                            <label class="w-field__label" for="marketing-coupon-status"><?= __('状态') ?></label>
                            <select id="marketing-coupon-status" class="w-select" name="status">
                                <option value="active"><?= __('启用') ?></option>
                                <option value="inactive"><?= __('禁用') ?></option>
                            </select>
                        </div>
                    </div>
                    <div class="w-grid">
                        <div style="--w-span-md:4;--w-mb:var(--weline-space-4);">
                            <label class="w-field__label" for="marketing-coupon-minimum"><?= __('最低订单金额') ?></label>
                            <input id="marketing-coupon-minimum" class="w-input" type="number" name="min_amount" min="0" step="0.0001" value="0">
                        </div>
                        <div style="--w-span-md:4;--w-mb:var(--weline-space-4);">
                            <label class="w-field__label" for="marketing-coupon-limit"><?= __('总使用次数') ?></label>
                            <input id="marketing-coupon-limit" class="w-input" type="number" name="usage_limit" min="0" value="100">
                        </div>
                        <div style="--w-span-md:4;--w-mb:var(--weline-space-4);">
                            <label class="w-field__label" for="marketing-coupon-customer-limit"><?= __('每客户次数') ?></label>
                            <input id="marketing-coupon-customer-limit" class="w-input" type="number" name="customer_limit" min="1" value="1">
                        </div>
                    </div>
                    <button data-testid="marketing-coupon-submit" class="w-button" type="submit" data-tone="primary">
                        <?= __('保存优惠券') ?>
                    </button>
                </w:form>
            </div>
        </div>
    </div>
</div>
