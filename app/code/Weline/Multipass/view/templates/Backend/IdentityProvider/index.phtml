<?php
/**
 * Multipass official login provider management.
 *
 * @var \Weline\Framework\View\Template $this
 */
$providers = $this->getData('providers') ?? [];
$scopeOptions = ['profile.basic', 'profile.email', 'account.bind', 'appstore.account', 'community.account'];
$saveUrl = $this->getBackendUrl('multipass/backend/identity-provider/save');
$deleteUrl = $this->getBackendUrl('multipass/backend/identity-provider/delete');
?>
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><?= __('官网登录提供方') ?></h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="@url('admin')"><?= __('首页') ?></a></li>
                    <li class="breadcrumb-item"><?= __('Multipass管理') ?></li>
                    <li class="breadcrumb-item"><?= __('官网登录提供方') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <template>Weline_Component::message.phtml</template>
</div>

<div class="row">
    <div class="col-xl-5">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0"><?= __('添加官网登录提供方') ?></h5>
            </div>
            <div class="card-body">
                <form method="post" action="<?= $saveUrl ?>">
                    <?= $this->getFormKey('multipass_identity_provider_save') ?>
                    <div class="mb-3">
                        <label class="form-label"><?= __('名称') ?> <span class="text-danger">*</span></label>
                        <input class="form-control" name="name" required placeholder="<?= __('例如：Weline官网') ?>">
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('官网授权地址') ?> <span class="text-danger">*</span></label>
                        <input class="form-control" type="url" name="issuer_base_url" required placeholder="https://www.aiweline.com">
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('官网接口地址') ?></label>
                        <input class="form-control" type="url" name="rest_base_url" placeholder="https://www.aiweline.com/api123">
                        <small class="text-muted"><?= __('留空时默认使用官网授权地址 + /api123。') ?></small>
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('Client ID') ?> <span class="text-danger">*</span></label>
                        <input class="form-control" name="client_id" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('Client Secret') ?> <span class="text-danger">*</span></label>
                        <input class="form-control" name="client_secret" required autocomplete="off">
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('本站回调地址') ?></label>
                        <input class="form-control" type="url" name="redirect_uri" placeholder="https://site.example.com/multipass/frontend/identity/callback">
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><?= __('授权范围') ?></label>
                        <div class="border rounded p-3">
                            <?php foreach ($scopeOptions as $scope): ?>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" name="scopes[]" value="<?= $scope ?>" id="new_scope_<?= str_replace('.', '_', $scope) ?>" <?= in_array($scope, ['profile.basic', 'profile.email', 'account.bind'], true) ? 'checked' : '' ?>>
                                    <label class="form-check-label" for="new_scope_<?= str_replace('.', '_', $scope) ?>"><?= $scope ?></label>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label"><?= __('状态') ?></label>
                            <select class="form-select" name="status">
                                <option value="active"><?= __('启用') ?></option>
                                <option value="disabled"><?= __('禁用') ?></option>
                            </select>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label"><?= __('排序') ?></label>
                            <input class="form-control" type="number" name="sort_order" value="100">
                        </div>
                    </div>
                    <button class="btn btn-primary" type="submit">
                        <i class="mdi mdi-content-save"></i> <?= __('保存') ?>
                    </button>
                </form>
            </div>
        </div>
    </div>

    <div class="col-xl-7">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0"><?= __('已配置提供方') ?></h5>
            </div>
            <div class="card-body">
                <?php if (empty($providers)): ?>
                    <div class="text-center text-muted py-5">
                        <i class="mdi mdi-login-variant" style="font-size: 44px;"></i>
                        <p class="mt-3 mb-0"><?= __('暂无官网登录提供方。目标站点需要配置官网生成的 client_id 和 client_secret 后才能发起授权登录。') ?></p>
                    </div>
                <?php else: ?>
                    <?php foreach ($providers as $provider): ?>
                        <div class="border rounded p-3 mb-3">
                            <form method="post" action="<?= $saveUrl ?>">
                                <?= $this->getFormKey('multipass_identity_provider_update_' . $provider->getId()) ?>
                                <input type="hidden" name="provider_id" value="<?= (int)$provider->getId() ?>">
                                <div class="d-flex justify-content-between align-items-start gap-3 mb-3">
                                    <div>
                                        <h6 class="mb-1"><?= htmlspecialchars($provider->getName(), ENT_QUOTES, 'UTF-8') ?></h6>
                                        <span class="badge <?= $provider->isActive() ? 'bg-success' : 'bg-secondary' ?>"><?= htmlspecialchars($provider->getStatus(), ENT_QUOTES, 'UTF-8') ?></span>
                                    </div>
                                    <div class="text-muted small"><?= __('密钥') ?>: <?= htmlspecialchars($provider->getMaskedClientSecret(), ENT_QUOTES, 'UTF-8') ?></div>
                                </div>
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label"><?= __('名称') ?></label>
                                        <input class="form-control" name="name" value="<?= htmlspecialchars($provider->getName(), ENT_QUOTES, 'UTF-8') ?>" required>
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label"><?= __('Client ID') ?></label>
                                        <input class="form-control" name="client_id" value="<?= htmlspecialchars($provider->getClientId(), ENT_QUOTES, 'UTF-8') ?>" required>
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label"><?= __('官网授权地址') ?></label>
                                        <input class="form-control" type="url" name="issuer_base_url" value="<?= htmlspecialchars($provider->getIssuerBaseUrl(), ENT_QUOTES, 'UTF-8') ?>" required>
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label"><?= __('官网接口地址') ?></label>
                                        <input class="form-control" type="url" name="rest_base_url" value="<?= htmlspecialchars($provider->getRestBaseUrl(), ENT_QUOTES, 'UTF-8') ?>">
                                    </div>
                                    <div class="col-md-12 mb-3">
                                        <label class="form-label"><?= __('本站回调地址') ?></label>
                                        <input class="form-control" type="url" name="redirect_uri" value="<?= htmlspecialchars($provider->getRedirectUri(), ENT_QUOTES, 'UTF-8') ?>">
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label"><?= __('Client Secret') ?></label>
                                        <input class="form-control" name="client_secret" value="" placeholder="<?= __('留空保持不变') ?>" autocomplete="off">
                                    </div>
                                    <div class="col-md-3 mb-3">
                                        <label class="form-label"><?= __('状态') ?></label>
                                        <select class="form-select" name="status">
                                            <option value="active" <?= $provider->getStatus() === 'active' ? 'selected' : '' ?>><?= __('启用') ?></option>
                                            <option value="disabled" <?= $provider->getStatus() === 'disabled' ? 'selected' : '' ?>><?= __('禁用') ?></option>
                                        </select>
                                    </div>
                                    <div class="col-md-3 mb-3">
                                        <label class="form-label"><?= __('排序') ?></label>
                                        <input class="form-control" type="number" name="sort_order" value="<?= (int)$provider->getSortOrder() ?>">
                                    </div>
                                </div>
                                <div class="mb-3">
                                    <label class="form-label"><?= __('授权范围') ?></label>
                                    <div class="d-flex flex-wrap gap-3">
                                        <?php foreach ($scopeOptions as $scope): ?>
                                            <label class="form-check mb-0">
                                                <input class="form-check-input" type="checkbox" name="scopes[]" value="<?= $scope ?>" <?= in_array($scope, $provider->getScopes(), true) ? 'checked' : '' ?>>
                                                <span class="form-check-label"><?= $scope ?></span>
                                            </label>
                                        <?php endforeach; ?>
                                    </div>
                                </div>
                                <button class="btn btn-sm btn-primary" type="submit">
                                    <i class="mdi mdi-content-save"></i> <?= __('保存') ?>
                                </button>
                            </form>
                            <form class="mt-2" method="post" action="<?= $deleteUrl ?>">
                                <?= $this->getFormKey('multipass_identity_provider_delete_' . $provider->getId()) ?>
                                <input type="hidden" name="provider_id" value="<?= (int)$provider->getId() ?>">
                                <button class="btn btn-sm btn-outline-danger" type="submit">
                                    <i class="mdi mdi-delete"></i> <?= __('删除') ?>
                                </button>
                            </form>
                        </div>
                    <?php endforeach; ?>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
