<?php
/**
 * 元数据编辑 - 内容模板
 * @var \Weline\Framework\View\Template $this
 */

use Weline\Meta\Model\Meta as MetaModel;

/** @var \Weline\Meta\Model\Meta $meta */
$meta = $this->getData('meta');
$metaId = $meta?->getData(MetaModel::fields_ID);
$metaDataRaw = (string)$meta?->getData(MetaModel::fields_META_DATA);
$metaSettingRaw = (string)$meta?->getData(MetaModel::fields_SETTING);
$metaDataArr = json_decode($metaDataRaw, true) ?: [];
$metaInfo = $metaDataArr['info'] ?? [];
$translationKeyPreview = $meta->getId()
    ? $meta->generateTranslationKey()
    : '@meta::{namespace}.{type}.{identify}.info.name';
$pageTitle = $metaId ? __('编辑元数据 #%1', $metaId) : __('新增元数据');
$saveUrl = $this->getBackendUrl('meta/backend/meta/save');
$backUrl = $this->getBackendUrl('meta/backend/meta');
?>
<style>
    .json-textarea {
        font-family: "Fira Code", Monaco, Consolas, monospace;
        min-height: 260px;
    }

    .meta-info-card .list-group-item {
        border: 0;
        border-radius: 0.75rem;
        background: var(--backend-color-card-bg-secondary, #f8f9fb);
        margin-bottom: 0.5rem;
    }
</style>

<div class="w-grid" style="--w-mb:var(--weline-space-4);">
    <div style="--w-span:12;">
        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
            <h4 style="--w-mb-sm:0;"><?= $pageTitle ?></h4>
            <div class="w-cluster">
                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                    <li class="w-breadcrumb__item">
                        <a href="<?= $backUrl ?>"><?= __('元数据管理') ?></a>
                    </li>
                    <li class="w-breadcrumb__item" data-state="active"><?= $pageTitle ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="w-grid">
    <div style="--w-span:12;">
        <template>Weline_Component::message.phtml</template>
    </div>
</div>

<w:form action="@var($saveUrl)" method="post" class="w-form" novalidate>
    <input type="hidden" name="<?= MetaModel::fields_ID ?>" value="<?= htmlspecialchars((string)$metaId) ?>">
    <div class="w-grid" style="--w-gap:var(--weline-space-6);">
        <div style="--w-span-lg:8;">
            <div class="w-card">
                <div class="w-card__body">
                    <h4 class="w-card__title" style="--w-mb:var(--weline-space-4);"><?= __('基础信息') ?></h4>
                    <div class="w-grid" style="--w-gap:var(--weline-space-4);">
                        <div style="--w-span-md:6;">
                            <label class="w-field__label"><?= __('命名空间') ?></label>
                            <input type="text" class="w-input" required name="<?= MetaModel::fields_NAMESPACE ?>" value="<?= htmlspecialchars((string)$meta?->getData(MetaModel::fields_NAMESPACE)) ?>">
                            <div class="w-field__error"><?= __('请输入命名空间') ?></div>
                        </div>
                        <div style="--w-span-md:6;">
                            <label class="w-field__label"><?= __('类型') ?></label>
                            <input type="text" class="w-input" required name="<?= MetaModel::fields_META_TYPE ?>" value="<?= htmlspecialchars((string)$meta?->getData(MetaModel::fields_META_TYPE)) ?>">
                        </div>
                        <div style="--w-span-md:6;">
                            <label class="w-field__label"><?= __('标识') ?></label>
                            <input type="text" class="w-input" required name="<?= MetaModel::fields_META_IDENTIFY ?>" value="<?= htmlspecialchars((string)$meta?->getData(MetaModel::fields_META_IDENTIFY)) ?>">
                        </div>
                        <div style="--w-span-md:3;">
                            <label class="w-field__label"><?= __('区域') ?></label>
                            <input type="text" class="w-input" name="<?= MetaModel::fields_AREA ?>" value="<?= htmlspecialchars((string)$meta?->getData(MetaModel::fields_AREA)) ?>">
                        </div>
                        <div style="--w-span-md:3;">
                            <label class="w-field__label"><?= __('分类') ?></label>
                            <input type="text" class="w-input" name="<?= MetaModel::fields_CATEGORY ?>" value="<?= htmlspecialchars((string)$meta?->getData(MetaModel::fields_CATEGORY)) ?>">
                        </div>
                        <div style="--w-span-md:6;">
                            <label class="w-field__label"><?= __('相对路径') ?></label>
                            <input type="text" class="w-input" name="<?= MetaModel::fields_FILE_PATH ?>" value="<?= htmlspecialchars((string)$meta?->getData(MetaModel::fields_FILE_PATH)) ?>">
                        </div>
                        <div style="--w-span-md:6;">
                            <label class="w-field__label"><?= __('完整路径') ?></label>
                            <input type="text" class="w-input" name="<?= MetaModel::fields_FILE_FULL_PATH ?>" value="<?= htmlspecialchars((string)$meta?->getData(MetaModel::fields_FILE_FULL_PATH)) ?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="w-card">
                <div class="w-card__body">
                    <div class="w-cluster" data-align="center" data-justify="between">
                        <div>
                            <h4 class="w-card__title" style="--w-mb:0;"><?= __('Meta 数据') ?></h4>
                            <p class="w-text" data-tone="muted" data-size="sm" style="--w-mb:0;"><?= __('使用 JSON 格式定义完整的元数据结构') ?></p>
                        </div>
                        <div class="w-button-group">
                            <button type="button" class="w-button" id="beautifyMetaJson" data-tone="neutral" data-variant="outline" data-size="sm">
                                <w:icon name="circle" size="sm"></w:icon><?= __('格式化') ?>
                            </button>
                            <button type="button" class="w-button" id="validateMetaJson" data-tone="info" data-variant="outline" data-size="sm">
                                <w:icon name="check" size="sm"></w:icon><?= __('校验 JSON') ?>
                            </button>
                        </div>
                    </div>
                    <textarea class="w-textarea json-textarea" name="<?= MetaModel::fields_META_DATA ?>" id="metaJsonTextarea" required style="--w-mt:var(--weline-space-4);"><?= htmlspecialchars($metaDataRaw ?: json_encode([
                                'namespace' => $meta?->getData(MetaModel::fields_NAMESPACE),
                                'type' => $meta?->getData(MetaModel::fields_META_TYPE),
                                'identify' => $meta?->getData(MetaModel::fields_META_IDENTIFY),
                                'info' => $metaInfo ?: [
                                    'name' => '',
                                    'description' => ''
                                ]
                            ], JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE)) ?></textarea>
                    <div class="w-field__error"><?= __('请输入合法的 JSON') ?></div>
                </div>
            </div>

            <div class="w-card">
                <div class="w-card__body">
                    <div class="w-cluster" data-align="center" data-justify="between">
                        <div>
                            <h4 class="w-card__title" style="--w-mb:0;"><?= __('可选设置 (setting)') ?></h4>
                            <p class="w-text" data-tone="muted" data-size="sm" style="--w-mb:0;"><?= __('存放表单默认值、展示参数等额外配置') ?></p>
                        </div>
                        <button type="button" class="w-button" id="beautifySettingJson" data-tone="neutral" data-variant="outline" data-size="sm">
                            <w:icon name="circle" size="sm"></w:icon><?= __('格式化') ?>
                        </button>
                    </div>
                    <textarea class="w-textarea json-textarea" rows="8" name="<?= MetaModel::fields_SETTING ?>" id="settingJsonTextarea" style="--w-mt:var(--weline-space-4);"><?= htmlspecialchars($metaSettingRaw) ?></textarea>
                </div>
            </div>
        </div>

        <div style="--w-span-lg:4;">
            <div class="w-card meta-info-card">
                <div class="w-card__body">
                    <h5 class="w-card__title"><?= __('信息预览') ?></h5>
                    <p class="w-text" data-tone="muted" style="--w-mb:var(--weline-space-4);"><?= __('从 JSON info 节点中解析出的可读信息，便于确认翻译内容。') ?></p>
                    <?php if (!empty($metaInfo)): ?>
                        <div class="w-list-group">
                            <?php foreach ($metaInfo as $key => $value): ?>
                                <div class="w-list-group__item">
                                    <div class="w-cluster" data-justify="between">
                                        <span class="w-text" data-transform="uppercase" data-tone="muted" data-size="sm"><?= htmlspecialchars($key) ?></span>
                                        <span class="w-text" data-weight="strong"><?= htmlspecialchars($value) ?></span>
                                    </div>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    <?php else: ?>
                        <div class="w-alert" data-tone="warning" style="--w-mb:0;">
                            <?= __('当前 JSON 中未找到 info.* 字段，建议补充 name、description 等信息。') ?>
                        </div>
                    <?php endif; ?>
                </div>
            </div>

            <div class="w-card">
                <div class="w-card__body">
                    <h5 class="w-card__title"><?= __('翻译 Key 预览') ?></h5>
                    <p class="w-text" data-tone="muted" data-size="sm" style="--w-mb:var(--weline-space-2);"><?= __('根据当前数据自动生成，可在 Taglib 翻译页中使用。') ?></p>
                    <div data-w-background="muted" data-w-radius="md" style="--w-pt:var(--weline-space-4);--w-pe:var(--weline-space-4);--w-pb:var(--weline-space-4);--w-ps:var(--weline-space-4);">
                        <code id="translationKeyPreview"><?= htmlspecialchars($translationKeyPreview) ?></code>
                        <button type="button" class="w-button w-text" id="copyTranslationKey" data-tone="muted" data-size="sm" style="--w-ps:0;--w-pe:0;">
                            <w:icon name="file" size="sm"></w:icon><?= __('复制') ?>
                        </button>
                    </div>
                    <hr>
                    <p class="w-text" data-tone="muted" data-size="sm" style="--w-mb:var(--weline-space-2);"><?= __('常用操作') ?></p>
                    <ul class="ps-3 w-text" data-tone="muted" data-size="sm" style="--w-mb:0;">
                        <li><?= __('Meta JSON 中的 info.* 字段会自动收集为词条。') ?></li>
                        <li><?= __('保存后可在"元数据列表"页快速跳转翻译。') ?></li>
                        <li><?= __('Setting 字段建议保持 JSON 格式，方便扩展。') ?></li>
                    </ul>
                </div>
            </div>

            <div class="w-card">
                <div class="w-card__body">
                    <div class="w-stack" style="--w-gap:var(--weline-space-2);">
                        <button type="submit" class="w-button" data-tone="primary" data-size="lg">
                            <w:icon name="save" size="sm"></w:icon><?= __('保存') ?>
                        </button>
                        <a href="<?= $backUrl ?>" class="w-button" data-tone="neutral">
                            <?= __('返回列表') ?>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</w:form>

<script>
(function() {
    'use strict';
    
    var beautify = function(textarea) {
        if (!textarea) {
            return;
        }
        try {
            var value = textarea.value.trim();
            if (!value) {
                return;
            }
            textarea.value = JSON.stringify(JSON.parse(value), null, 2);
        } catch (e) {
            if (window.Weline.UI.toast) {
                Weline.UI.toast.error('JSON 解析失败：' + e.message);
            } else if (window.Toast) {
                Toast.error('JSON 解析失败：' + e.message);
            }
        }
    };

    var metaJsonTextarea = document.getElementById('metaJsonTextarea');
    var settingTextarea = document.getElementById('settingJsonTextarea');

    document.getElementById('beautifyMetaJson').addEventListener('click', function() {
        beautify(metaJsonTextarea);
    });
    
    document.getElementById('beautifySettingJson').addEventListener('click', function() {
        beautify(settingTextarea);
    });
    
    document.getElementById('validateMetaJson').addEventListener('click', function() {
        try {
            JSON.parse(metaJsonTextarea.value);
            if (window.Weline.UI.toast) {
                Weline.UI.toast.success('<?= __('JSON 校验通过') ?>');
            } else if (window.Toast) {
                Toast.success('JSON 校验通过');
            }
        } catch (e) {
            if (window.Weline.UI.toast) {
                Weline.UI.toast.error(e.message);
            } else if (window.Toast) {
                Toast.error(e.message);
            }
        }
    });

    var translationKeyPreview = document.getElementById('translationKeyPreview').textContent;
    document.getElementById('copyTranslationKey').addEventListener('click', function() {
        navigator.clipboard.writeText(translationKeyPreview).then(function() {
            var btn = document.getElementById('copyTranslationKey');
            btn.innerHTML = '<w:icon name="check" size="sm"></w:icon><?= __('已复制') ?>';
            setTimeout(function() {
                btn.innerHTML = '<w:icon name="file" size="sm"></w:icon><?= __('复制') ?>';
            }, 1500);
        });
    });
})();
</script>
