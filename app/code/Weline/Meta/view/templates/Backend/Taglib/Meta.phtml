<?php
/**
 * Meta翻译 - 内容模板
 * @var \Weline\Framework\View\Template $this
 */
?>
<div class="w-grid">
    <template>Weline_Component::message.phtml</template>
</div>

<div class="w-grid">
    <div style="--w-span:12;">
        <div class="w-card">
            <div class="w-card__header">
                <h5><?= __('Meta翻译') ?></h5>
                <p class="w-text" data-tone="muted" style="--w-mb:0;"><small><?= htmlspecialchars($meta_key ?? '') ?></small></p>
            </div>
            <div class="w-card__body">
                <w:form id="metaTranslationForm" role='form' class="w-form" novalidate='' action='{{action}}' method='post'>
                    <div class="w-grid w-field">
                        <empty name="translations">
                            <lang>请前往i18n模块收集词语后重试！或者开启实时收集词语功能！</lang>
                        </empty>
                        <foreach name="translations">
                            <input type='hidden' name='translation[{{v.local_code}}][local_code]'
                                   value='{{v.local_code}}'>
                            <div style="--w-span-lg:12;">
                                <div class="w-field__control" style="--w-mb:var(--weline-space-4);">
                                    <span class="w-field__addon" title='{{v.local.name}}' id='{{v.local_code}}-translate-input-addon'>
                                        {{v.local.flag}}
                                    </span>
                                    <input class="w-input" type='text' id='{{v.local_code}}-translate-input' placeholder='{{v.translate}}' aria-label='{{v.translate}}' value='{{v.translate}}' name='translation[{{v.local_code}}][translate]' required='required' aria-describedby='{{v.local_code}}-translate-input-addon'>
                                    <div class="w-field__hint">
                                        <lang>填写正确！</lang>
                                    </div>
                                    <span class="w-field__addon" title='{{v.local.name}}'>
                                        {{v.local.name}}
                                    </span>
                                </div>
                            </div>
                        </foreach>
                    </div>
                    <div class="w-field">
                        <button class="w-button" type='submit' data-tone="primary">
                            <lang>提交</lang>
                        </button>
                    </div>
                </w:form>
            </div>
        </div>
    </div>
</div>
