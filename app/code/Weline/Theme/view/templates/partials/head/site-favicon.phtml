<?php
/** @var \Weline\Framework\View\Template $this */
use Weline\Framework\Manager\ObjectManager;
use Weline\Theme\Helper\SiteBrand;

$siteBrand = ObjectManager::getInstance(SiteBrand::class);
$iconUrl = $siteBrand->resolveIconUrl($this);
$appleTouchIconUrl = $siteBrand->resolveAppleTouchIconUrl($this);
?>
<link rel="icon" type="image/png" sizes="128x128" href="<?= htmlspecialchars($iconUrl, ENT_QUOTES, 'UTF-8') ?>">
<link rel="shortcut icon" href="<?= htmlspecialchars($iconUrl, ENT_QUOTES, 'UTF-8') ?>">
<link rel="bookmark" href="<?= htmlspecialchars($iconUrl, ENT_QUOTES, 'UTF-8') ?>">
<link rel="apple-touch-icon" sizes="180x180" href="<?= htmlspecialchars($appleTouchIconUrl, ENT_QUOTES, 'UTF-8') ?>">
