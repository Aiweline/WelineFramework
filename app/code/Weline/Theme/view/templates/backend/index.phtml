<?php
/**
 * 主题管理列表页面（简化版）
 * 
 * @var \Weline\Framework\View\Template $this
 * @var array $themes
 */

$url = \Weline\Framework\Manager\ObjectManager::getInstance(\Weline\Framework\Http\Url::class);
?>
<!-- 页面标题 -->
                <div class="row">
                    <div class="col-12">
                        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                            <h4 class="mb-sm-0"><?= __('主题管理') ?></h4>
                            <div class="page-title-right">
                                <ol class="breadcrumb m-0">
                                    <li class="breadcrumb-item"><a href="javascript:void(0);"><?= __('系统') ?></a></li>
                                    <li class="breadcrumb-item active"><?= __('主题管理') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 消息提示 -->
                <div class="row">
                    <template>Weline_Component::message.phtml</template>
                </div>

                <!-- 主题列表 -->
                <div class="row">
                    <?php if (empty($themes)): ?>
                        <div class="col-12">
                            <div class="card">
                                <div class="card-body">
                                    <div class="empty-state">
                                        <i class="mdi mdi-palette-outline"></i>
                                        <h5><?= __('暂无主题') ?></h5>
                                        <p><?= __('系统中还没有安装任何主题') ?></p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <?php else: ?>
                        <?php foreach ($themes as $theme): ?>
                            <div class="col-xl-3 col-lg-4 col-md-6 mb-4">
                                <div class="theme-card <?= ($theme['is_active'] ?? 0) ? 'active' : '' ?>">
                                    <div class="theme-preview">
                                        <i class="mdi mdi-palette theme-icon"></i>
                                        <?php if ($theme['is_active'] ?? 0): ?>
                                            <span class="active-badge">
                                                <i class="mdi mdi-check-circle"></i> <?= __('已激活') ?>
                                            </span>
                                        <?php endif; ?>
                                    </div>
                                    
                                    <div class="theme-info">
                                        <h5 class="theme-name"><?= htmlspecialchars($theme['name'] ?? '') ?></h5>
                                        <div class="theme-meta">
                                            <i class="mdi mdi-folder-outline"></i>
                                            <?= htmlspecialchars($theme['module_name'] ?? '') ?>
                                        </div>
                                        <?php if (!empty($theme['parent_theme_name'])): ?>
                                            <div class="theme-meta">
                                                <i class="mdi mdi-arrow-up-bold-circle-outline"></i>
                                                <?= __('继承自') ?>: <?= htmlspecialchars($theme['parent_theme_name']) ?>
                                            </div>
                                        <?php endif; ?>
                                    </div>
                                    
                                    <div class="theme-actions">
                                        <!-- 激活按钮 -->
                                        <button type="button" 
                                                class="btn btn-sm <?= ($theme['is_active'] ?? 0) ? 'btn-success' : 'btn-outline-success' ?> activate-btn"
                                                data-theme-id="<?= $theme['id'] ?>"
                                                data-theme-name="<?= htmlspecialchars($theme['name'] ?? '') ?>"
                                                <?= ($theme['is_active'] ?? 0) ? 'disabled' : '' ?>>
                                            <i class="mdi mdi-check-circle"></i>
                                            <?= ($theme['is_active'] ?? 0) ? __('已激活') : __('激活') ?>
                                        </button>
                                        
                                        <!-- 预览按钮 -->
                                        <a href="<?= $url->getBackendUrl('theme/backend/index/preview', ['theme_id' => $theme['id'], 'area' => 'frontend']) ?>" 
                                           class="btn btn-sm btn-outline-primary"
                                           target="_blank"
                                           title="<?= __('预览主题') ?>">
                                            <i class="mdi mdi-eye"></i>
                                            <?= __('预览') ?>
                                        </a>
                                        
                                        <!-- 可视化编辑按钮 -->
                                        <a href="<?= $url->getBackendUrl('theme/backend/theme-editor', ['theme_id' => $theme['id']]) ?>" 
                                           class="btn btn-sm btn-outline-info"
                                           title="<?= __('可视化编辑') ?>">
                                            <i class="mdi mdi-palette-swatch"></i>
                                            <?= __('编辑') ?>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </div>
