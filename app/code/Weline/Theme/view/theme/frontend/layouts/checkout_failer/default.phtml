<?php
/**
 * 布局：结账页 - 失败页面布局
 *
 * 订单提交失败后的错误提示页面布局
 * 字段
 * @meta.name {default="结账失败页面布局",name="结账失败页面布局",description="结账失败页面布局，用于描述布局的名称"}
 * @meta.description {default="订单提交失败后的错误提示页面布局",name="订单提交失败后的错误提示页面布局",description="订单提交失败后的错误提示页面布局，用于描述布局的用途"}
 * @preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="是否需要登录，0不需要登录，1需要登录，这个不是元素据，是预览系统判断是否需要自动登录的。"}
 * @param title {default="订单提交失败",name="页面标题",description="页面标题，用于设置页面标题"}
 * @param errorMessage {default="很抱歉，您的订单提交失败，请稍后重试。",name="错误提示信息",description="错误提示信息，用于显示失败原因"}
 * @param errorCode {default="",name="错误代码（可选）",description="错误代码（可选），用于显示错误代码"}
 * @param retryUrl {default="",name="重试链接（可选）",description="重试链接（可选），用于设置重试提交订单的链接"}
 * @param backToCart {default="/cart",name="返回购物车链接",description="返回购物车链接，用于设置返回购物车的链接"}
 * @param contactSupport {default="",name="联系客服链接（可选）",description="联系客服链接（可选），用于设置联系客服的链接"}
 * @param class {default="",name="额外CSS类",description="额外CSS类，用于设置页面额外CSS类"}
 *
 * 参数配置由 ControllerFetchFileBefore Observer 自动加载到 meta 数组中
 * 模板中使用 meta.参数 输出内容，使用 meta.参数 在条件判断中
 * 页面标题由 Observer 自动设置（如果控制器未设置，则使用 meta.title 的默认值）
 */
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" dir="{{htmlDir}}" data-local="{{lang_local}}" data-lang="{{lang}}" data-dir="{{htmlDir}}">
<head>
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::default::head-after</w:hook>
    <style>
        body.checkout-failer-layout {
            margin: 0;
            background: var(--weline-layout-surface-secondary);
            color: var(--weline-layout-text-primary);
        }

        .checkout-failer-main {
            width: min(960px, calc(100% - 40px));
            margin: 0 auto;
            padding: var(--weline-layout-spacing-3xl) 0 calc(var(--weline-layout-spacing-3xl) * 1.125);
        }

        .checkout-failer-content {
            border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--weline-theme-border-color-subtle);
            border-radius: var(--weline-layout-radius-lg);
            background: var(--weline-layout-surface-primary);
            padding: var(--weline-layout-spacing-3xl) var(--weline-layout-spacing-xl);
            text-align: center;
            box-shadow: var(--weline-theme-shadow-md);
        }

        .failer-icon {
            width: 64px;
            height: 64px;
            margin: 0 auto var(--weline-layout-spacing-lg);
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: var(--weline-layout-radius-full);
            background: var(--weline-theme-danger-subtle);
            color: var(--weline-theme-danger);
            font-size: var(--weline-layout-font-size-4xl);
        }

        .failer-title {
            margin: 0;
            font-size: var(--weline-layout-font-size-4xl);
            line-height: var(--weline-layout-line-height-tight);
        }

        .error-message,
        .error-code,
        .help-tips {
            margin-top: var(--weline-layout-spacing-md);
            color: var(--weline-layout-text-secondary);
            line-height: var(--weline-layout-line-height-relaxed);
        }

        .error-code code {
            margin-left: var(--weline-layout-spacing-xs);
            padding: var(--weline-layout-spacing-xs) var(--weline-layout-spacing-sm);
            border-radius: var(--weline-layout-radius-md);
            background: var(--weline-layout-surface-secondary);
            color: var(--weline-layout-text-primary);
        }

        .failer-actions {
            display: flex;
            flex-wrap: wrap;
            gap: var(--weline-layout-spacing-sm);
            justify-content: center;
            margin-top: var(--weline-layout-spacing-lg);
        }

        .w-btn {
            min-height: 42px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: var(--weline-layout-spacing-xs);
            padding: 0 var(--weline-layout-spacing-lg);
            border-radius: var(--weline-layout-radius-md);
            text-decoration: none;
            font-weight: var(--weline-layout-font-weight-bold);
        }

        .w-btn-primary {
            color: var(--weline-theme-on-primary);
            border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--weline-layout-link-color);
            background: var(--weline-layout-link-color);
        }

        .w-btn-secondary,
        .w-btn-outline {
            color: var(--weline-layout-text-primary);
            border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--weline-theme-border-color-strong);
            background: var(--weline-layout-surface-primary);
        }

        .help-tips {
            max-width: 620px;
            margin-left: auto;
            margin-right: auto;
            text-align: left;
        }

        .help-tips ul {
            margin: var(--weline-layout-spacing-sm) 0 0;
            padding-left: 20px;
        }

        @media (max-width: 640px) {
            .checkout-failer-main {
                width: min(100% - 28px, 960px);
                padding: 40px 0 56px;
            }
        }
    </style>
    <w:hook>seo::head</w:hook>
</head>
<body class="checkout-failer-layout">
    <w:hook>seo::body</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>
    <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>
    
    <main class="checkout-failer-main <?= htmlspecialchars($meta['class'] ?? '') ?>" id="checkout-failer-main" data-layout="checkout_failer">
        <w:slot id="content"
                name="内容区"
                accept="layout-checkout-failer-content,layout-default-content,content,checkout-failer-content,checkout-status,order-summary,support-panel"
                reject="header,footer"
                multiple="true"
                position="content"
                class="checkout-failer-main__content-root-slot">
        <w:slot id="checkout-failer-content"
                name="结算失败内容"
                accept="layout-checkout-failer-content,checkout-failer,checkout-error,content"
                reject="header,footer"
                multiple="true"
                position="content"
                class="checkout-failer-container">
            <div class="checkout-failer-content">
                <div class="failer-icon">
                    <i class="ri-close-line"></i>
                </div>
                <h1 class="failer-title"><?= htmlspecialchars($meta['title'] ?? __('订单提交失败')) ?></h1>
                
                <div class="error-message">
                    <p><?= htmlspecialchars($meta['errorMessage'] ?? __('很抱歉，您的订单提交失败，请稍后重试。')) ?></p>
                </div>
                
                <if condition="meta.errorCode">
                    <p class="error-code">
                        <?= __('错误代码：') ?><code><?= htmlspecialchars($meta['errorCode'] ?? '') ?></code>
                    </p>
                </if>
                
                <div class="failer-actions">
                    <if condition="meta.retryUrl">
                        <a href="<?= htmlspecialchars($meta['retryUrl'] ?? '') ?>" class="w-btn w-btn-primary">
                            <i class="ri-refresh-line"></i> <?= __('重试提交') ?>
                        </a>
                    </if>
                    <a href="<?= htmlspecialchars($meta['backToCart'] ?? '/cart') ?>" class="w-btn w-btn-secondary">
                        <i class="ri-shopping-cart-2-line"></i> <?= __('返回购物车') ?>
                    </a>
                    <if condition="meta.contactSupport">
                        <a href="<?= htmlspecialchars($meta['contactSupport'] ?? '') ?>" class="w-btn w-btn-outline">
                            <i class="ri-customer-service-2-line"></i> <?= __('联系客服') ?>
                        </a>
                    </if>
                </div>
                
                <div class="help-tips">
                    <p class="tips-title"><?= __('温馨提示：') ?></p>
                    <ul>
                        <li><?= __('请检查您的网络连接是否正常') ?></li>
                        <li><?= __('请确认您的支付信息是否正确') ?></li>
                        <li><?= __('如问题持续存在，请联系客服寻求帮助') ?></li>
                    </ul>
                </div>
            </div>
        </w:slot>
        </w:slot>
    </main>
    
    <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default" vars="copyright,links,showBackToTop,socialMedia,showSocialMedia"/>
    <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
    <w:hook>seo::footer</w:hook>
</body>
</html>

