<?php
$meta = $this->getData('meta') ?? [];
$title = $meta['title'] ?? $this->getData('title') ?? __('Category');
$meta['showHeader'] = $meta['showHeader'] ?? true;
$meta['showFooter'] = $meta['showFooter'] ?? true;
$this->assign('meta', $meta);
$this->assign('title', $title);
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" dir="{{htmlDir}}" data-local="{{lang_local}}" data-lang="{{lang}}" data-dir="{{htmlDir}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes, shrink-to-fit=no">
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::base::head-after</w:hook>
    <w:hook>seo::head</w:hook>
</head>
<body>
    <w:hook>seo::body</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::body-start</w:hook>
    <div class="weline-page-wrapper weshop-category-layout weshop-category-layout--festival" data-weshop-layout-option="festival">
        <if condition="meta.showHeader">
            <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default"/>
            <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>
        </if>

        <main class="weshop-category-layout__main" id="category-layout-main" data-layout="category" data-weshop-layout-option="festival">
            <section class="weshop-category-layout__banner">
                <span class="weshop-category-layout__eyebrow"><?= __('Scheduled category layout') ?></span>
                <h1 class="weshop-category-layout__title"><?= htmlspecialchars((string)$title, ENT_QUOTES, 'UTF-8') ?></h1>
            </section>

            <w:slot id="category-products"
                    name="Category products"
                    accept="layout-category-products,product-grid,product-list-view,category-children"
                    multiple="true"
                    position="content"
                    class="weshop-category-layout__content">
                <w:hook>
                    WeShop_Catalog::frontend::layouts::category::products-content
                    <else/>
                    <if condition="meta.content">{{meta.content}}</if>
                </w:hook>
            </w:slot>
        </main>

        <if condition="meta.showFooter">
            <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
            <footer class="weline-footer">
                <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default"/>
            </footer>
            <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
        </if>
    </div>
    <w:hook>Weline_Theme::frontend::layouts::base::body-end</w:hook>
    <style>
        .weshop-category-layout__main {
            width: 100%;
            padding: 24px 0 56px;
            background: #fff7ed;
        }
        .weshop-category-layout__banner,
        .weshop-category-layout__content {
            width: min(1180px, calc(100% - 32px));
            margin: 0 auto 24px;
        }
        .weshop-category-layout__banner {
            padding: 18px 22px;
            border: 1px solid #fed7aa;
            border-radius: 8px;
            background: #fff;
        }
        .weshop-category-layout__eyebrow {
            display: inline-flex;
            margin-bottom: 8px;
            color: #c2410c;
            font-size: 0.875rem;
            font-weight: 700;
        }
        .weshop-category-layout__title {
            margin: 0;
            color: #111827;
            font-size: clamp(1.5rem, 2vw, 2.25rem);
            line-height: 1.15;
        }
    </style>
    <w:hook>seo::footer</w:hook>
</body>
</html>
