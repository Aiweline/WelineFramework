<?php
/**
 * 布局：购物车 - 空购物车布局
 *
 * 空购物车页面布局，提示用户购物车为空
 * 字段
 * @meta.name {default="空购物车布局",name="空购物车布局",description="空购物车布局，用于描述布局的名称"}
 * @meta.description {default="空购物车页面布局，提示用户购物车为空",name="空购物车页面布局，提示用户购物车为空",description="空购物车页面布局，提示用户购物车为空，用于描述布局的用途"}
 * @preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="是否需要登录，0不需要登录，1需要登录，这个不是元素据，是预览系统判断是否需要自动登录的。"}
 * @param title {default="购物车",name="页面标题",description="页面标题，用于设置页面标题"}
 * @param message {default="您的购物车是空的",name="提示信息（可选）",description="提示信息（可选），用于设置提示信息"}
 * @param continueShopping {default="/",name="继续购物链接",description="继续购物链接，用于设置继续购物链接"}
 * @param class {default="",name="额外CSS类",description="额外CSS类，用于设置页面额外CSS类"}
 *
 * 参数配置由 ControllerFetchFileBefore Observer 自动加载到 meta 数组中
 * 模板中使用 meta.参数 输出内容，使用 meta.参数 在条件判断中
 * 页面标题由 Observer 自动设置（如果控制器未设置，则使用 meta.title 的默认值）
 */
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" dir="{{htmlDir}}" data-local="{{lang_local}}" data-lang="{{lang}}" data-dir="{{htmlDir}}">
<head>
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::default::head-after</w:hook>
    <style>
        body.cart-empty-layout {
            margin: 0;
            background: var(--weline-layout-surface-secondary);
            color: var(--weline-layout-text-primary);
        }

        .cart-empty-main {
            width: min(1120px, calc(100% - 40px));
            min-height: 54vh;
            margin: 0 auto;
            padding: var(--weline-layout-spacing-3xl) 0 calc(var(--weline-layout-spacing-3xl) * 1.125);
        }

        .cart-empty-content {
            border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--weline-theme-border-color-subtle);
            border-radius: var(--weline-layout-radius-lg);
            background: var(--weline-layout-surface-primary);
            padding: var(--weline-layout-spacing-2xl) var(--weline-layout-spacing-xl);
            text-align: center;
            box-shadow: var(--weline-theme-shadow-md);
        }

        .cart-empty-icon {
            width: 64px;
            height: 64px;
            margin: 0 auto var(--weline-layout-spacing-lg);
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: var(--weline-layout-radius-full);
            background: var(--weline-theme-primary-subtle);
            color: var(--weline-layout-link-color);
            font-size: var(--weline-layout-font-size-4xl);
        }

        .cart-empty-title {
            margin: 0;
            font-size: var(--weline-layout-font-size-4xl);
            line-height: var(--weline-layout-line-height-tight);
        }

        .cart-empty-message {
            margin: var(--weline-layout-spacing-sm) auto 0;
            max-width: 520px;
            color: var(--weline-layout-text-secondary);
            line-height: var(--weline-layout-line-height-relaxed);
        }

        .continue-shopping-btn,
        .w-btn {
            min-height: 42px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-top: var(--weline-layout-spacing-lg);
            padding: 0 var(--weline-layout-spacing-lg);
            border-radius: var(--weline-layout-radius-md);
            text-decoration: none;
            font-weight: var(--weline-layout-font-weight-bold);
        }

        .w-btn-primary {
            color: var(--weline-theme-on-primary);
            border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--weline-layout-link-color);
            background: var(--weline-layout-link-color);
        }

        .cart-empty-recommendations {
            margin-top: var(--weline-layout-spacing-xl);
        }

        @media (max-width: 640px) {
            .cart-empty-main {
                width: min(100% - 28px, 1120px);
                padding: 40px 0 56px;
            }
        }
    </style>
    <w:hook>seo::head</w:hook>
</head>
<body class="cart-empty-layout">
    <w:hook>seo::body</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>
    <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>
    
    <main class="cart-empty-main <?= htmlspecialchars($meta['class'] ?? '') ?>" id="cart-empty-main" data-layout="cart_empty">
        <w:slot id="content"
                name="内容区"
                accept="layout-cart-empty-content,layout-default-content,content,empty-state,cart-content,product-carousel,recently-viewed,you-may-like"
                reject="header,footer"
                multiple="true"
                position="content"
                class="cart-empty-main__content-root-slot">
        <w:slot id="cart-empty-content"
                name="空购物车内容"
                accept="layout-cart-empty-content,layout-cart-content,cart-empty,content"
                reject="header,footer"
                multiple="true"
                position="content"
                class="cart-empty-container">
            <div class="cart-empty-content">
                <div class="cart-empty-icon">
                    <i class="ri-shopping-cart-2-line"></i>
                </div>
                <h2 class="cart-empty-title"><?= __('购物车是空的') ?></h2>
                <p class="cart-empty-message"><?= htmlspecialchars($meta['message'] ?? __('您的购物车是空的')) ?></p>
                <a href="<?= htmlspecialchars($meta['continueShopping'] ?? '/') ?>" class="w-btn w-btn-primary continue-shopping-btn">
                    <?= __('继续购物') ?>
                </a>
            </div>
        </w:slot>
        <w:slot id="cart-empty-recommendations"
                name="空购物车推荐区"
                accept="layout-cart-recommendations,featured-products,bestsellers,new-arrivals,you-may-like,product-carousel"
                multiple="true"
                position="content"
                wrapper="section"
                class="cart-empty-recommendations">
            <w:hook>Weline_Theme::frontend::layouts::cart-empty::recommendations</w:hook>
        </w:slot>
        </w:slot>
    </main>
    
    <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default" vars="copyright,links,showBackToTop,socialMedia,showSocialMedia"/>
    <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
    <w:hook>seo::footer</w:hook>
</body>
</html>
