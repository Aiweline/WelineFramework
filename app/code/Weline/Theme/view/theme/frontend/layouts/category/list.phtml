<!--
布局：分类页 - 列表页布局

分类列表页布局，包含分类筛选、产品展示等
字段
@meta.name {default="分类列表页布局",name="分类列表页布局",description="分类列表页布局，用于描述布局的名称"}
@meta.description {default="分类列表页布局，包含分类筛选、产品展示等",name="分类列表页布局，包含分类筛选、产品展示等",description="分类列表页布局，包含分类筛选、产品展示等，用于描述布局的用途"}
@preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="是否需要登录，0不需要登录，1需要登录，这个不是元素据，是预览系统判断是否需要自动登录的。"}
@param title {default="分类",name="页面标题",description="页面标题，用于设置页面标题"}
@param content {default="",name="主要内容（HTML字符串）",description="主要内容（HTML字符串），用于设置页面内容"}
@param categoryNav {default="",name="分类导航",description="分类导航，用于设置分类导航"}
@param filters {default="",name="筛选器内容",description="筛选器内容，用于设置筛选器"}
@param class {default="",name="额外CSS类",description="额外CSS类，用于设置页面额外CSS类"}
-->
<?php
$title = $this->getData('title') ?? __('分类');
$content = $this->getData('content') ?? '';
$categoryNav = $this->getData('categoryNav') ?? '';
$filters = $this->getData('filters') ?? '';
$class = $this->getData('class') ?? '';

// 设置页面标题
$this->assign('title', $title);
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" dir="{{htmlDir}}" data-local="{{lang_local}}" data-lang="{{lang}}" data-dir="{{htmlDir}}">
<head>
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::default::head-after</w:hook>
    <style>
        body.category-list-layout {
            margin: 0;
            background: var(--weline-layout-surface-secondary);
            color: var(--weline-layout-text-primary);
        }

        .category-list-main {
            width: min(1200px, calc(100% - 40px));
            margin: 0 auto;
            padding: var(--weline-layout-spacing-xl) 0 calc(var(--weline-layout-spacing-3xl) * 1.125);
        }

        .category-list-container {
            display: grid;
            gap: var(--weline-layout-spacing-lg);
        }

        .category-nav,
        .category-filters,
        .category-content {
            border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--weline-theme-border-color-subtle);
            border-radius: var(--weline-layout-radius-lg);
            background: var(--weline-layout-surface-primary);
        }

        .category-nav {
            padding: var(--weline-layout-spacing-lg);
        }

        .category-content-wrapper {
            display: grid;
            grid-template-columns: minmax(220px, 280px) minmax(0, 1fr);
            gap: var(--weline-layout-spacing-lg);
            align-items: start;
        }

        .category-filters {
            padding: var(--weline-layout-spacing-lg);
        }

        .category-content {
            min-height: 360px;
            padding: var(--weline-layout-spacing-lg);
        }

        .category-list__placeholder,
        .category-list__filter-placeholder,
        .category-list__nav-placeholder {
            color: var(--weline-layout-text-secondary);
            line-height: var(--weline-layout-line-height-relaxed);
        }

        .category-list__placeholder h1 {
            margin: 0;
            font-size: var(--weline-layout-font-size-4xl);
            line-height: var(--weline-layout-line-height-tight);
            color: var(--weline-layout-text-primary);
        }

        .category-list__placeholder p {
            margin: var(--weline-layout-spacing-sm) 0 0;
        }

        .category-list__grid {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: var(--weline-layout-spacing-md);
            margin-top: var(--weline-layout-spacing-lg);
        }

        .category-list__card {
            min-height: 168px;
            border: var(--weline-theme-border-width) var(--border-style-dashed) var(--weline-theme-border-color-strong);
            border-radius: var(--weline-layout-radius-lg);
            background: var(--weline-theme-primary-subtle);
        }

        @media (max-width: 900px) {
            .category-list-main {
                width: min(100% - 28px, 1200px);
                padding: 24px 0 56px;
            }

            .category-content-wrapper {
                grid-template-columns: 1fr;
            }

            .category-list__grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
    <w:hook>seo::head</w:hook>
</head>
<body class="category-list-layout">
    <w:hook>seo::body</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>
    <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>

    <!-- 分类列表页布局的面包屑导航区域 -->
    <w:hook>Weline_Theme::frontend::layouts::base::breadcrumb-before</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::category::breadcrumb-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="breadcrumb" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::category::breadcrumb-after</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::breadcrumb-after</w:hook>
    
    <main class="category-list-main <?= htmlspecialchars($class ?? '') ?>" id="category-list-main" data-layout="category_list">
        <w:slot id="content"
                name="内容区"
                accept="layout-category-list-content,layout-category-content,layout-default-content,content,category-list,category-filter,category-grid,category-nav"
                reject="header,footer"
                multiple="true"
                position="content"
                class="category-list-main__content-root-slot">
        <div class="category-list-container">
            <nav class="category-nav">
                    <w:slot id="category-list-nav"
                            name="分类列表导航"
                            accept="layout-category-list-nav,category-list,category-menu,navigation"
                            multiple="true"
                            position="content">
                        <?php if ($categoryNav): ?>
                            {{categoryNav}}
                        <?php else: ?>
                            <div class="category-list__nav-placeholder"><?= __('分类导航 slot，可由分类模块或导航部件接管。') ?></div>
                        <?php endif; ?>
                    </w:slot>
            </nav>
            
            <div class="category-content-wrapper">
                <aside class="category-filters">
                        <w:slot id="category-list-filters"
                                name="分类列表筛选"
                                accept="layout-category-filters,layout-category-list-filters,category-filters,attribute-filter,price-filter"
                                multiple="true"
                                position="sidebar">
                            <?php if ($filters): ?>
                                {{filters}}
                            <?php else: ?>
                                <div class="category-list__filter-placeholder"><?= __('筛选 slot，可由筛选模块或筛选部件接管。') ?></div>
                            <?php endif; ?>
                        </w:slot>
                </aside>
                
                <w:slot id="category-list-content"
                        name="分类列表内容"
                        accept="layout-category-list-content,layout-category-products,product-grid,product-list-view,category-children"
                        reject="header,footer"
                        multiple="true"
                        position="content"
                        class="category-content">
                    <?php if ($content): ?>
                        {{content}}
                    <?php else: ?>
                        <div class="category-list__placeholder">
                            <h1><?= htmlspecialchars($title, ENT_QUOTES, 'UTF-8') ?></h1>
                            <p><?= __('这里是分类列表的默认展示区域。接入真实分类或商品模块后，会通过 hook 或部件替换为真实列表。') ?></p>
                            <div class="category-list__grid" aria-hidden="true">
                                <div class="category-list__card"></div>
                                <div class="category-list__card"></div>
                                <div class="category-list__card"></div>
                            </div>
                        </div>
                    <?php endif; ?>
                </w:slot>
            </div>
        </div>
        </w:slot>
    </main>
    
    <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default" vars="copyright,links,showBackToTop,socialMedia,showSocialMedia"/>
    <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
    <w:hook>seo::footer</w:hook>
</body>
</html>

