<?php
/**
 * 布局：分类页 - 默认网格布局
 *
 * 电商分类页面布局，包含筛选侧边栏、子分类网格、产品列表区域
 * 支持移动端筛选抽屉、响应式网格布局
 *
 * @meta.name {default="分类页面布局",name="分类页面布局",description="电商分类页面主布局，包含筛选侧边栏和产品网格展示区域"}
 * @meta.description {default="电商分类页面布局，包含筛选侧边栏、子分类卡片网格和产品列表区域，支持移动端筛选抽屉和响应式设计",name="电商分类页面布局",description="电商分类页面布局，包含筛选侧边栏、子分类卡片网格和产品列表区域，支持移动端筛选抽屉和响应式设计"}
 * @meta.category {default="catalog",name="分类",description="布局所属分类"}
 * @preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="是否需要登录，0不需要登录，1需要登录"}
 * @param title {default="分类",name="页面标题",description="页面标题，用于设置页面标题"}
 * @param content {default="",name="主要内容（HTML字符串）",description="主要内容（HTML字符串），用于设置页面内容"}
 * @param class {default="",name="额外CSS类",description="额外CSS类，用于设置页面额外CSS类"}
 * @param showHeader {default=true,name="是否显示header",description="是否显示header，用于控制header的显示"}
 * @param showFooter {default=true,name="是否显示footer",description="是否显示footer，用于控制footer的显示"}
 * @param showFilters {default=true,name="是否显示筛选器",description="是否显示左侧筛选器区域"}
 * @param filterDrawerTitle {default="筛选条件",name="筛选抽屉标题",description="移动端筛选抽屉的标题文字"}
 * @param gridColumns {default=4,type=number,name="网格列数",description="桌面端每行显示的分类卡片数量"}
 * @param layout.leftsider {default=1,option={0:不显示,1:显示},name="显示左侧栏",description="是否显示左侧栏，用于控制布局结构"}
 * @param layout.rightsider {default=0,option={0:不显示,1:显示},name="显示右侧栏",description="是否显示右侧栏，用于控制布局结构"}
 */
?>
<?php
$meta = $this->getData('meta') ?? [];
$title = $meta['title'] ?? $this->getData('title') ?? __('分类');
$content = $meta['content'] ?? $this->getData('content') ?? '';
$class = $meta['class'] ?? $this->getData('class') ?? '';
$filter_sidebar = $meta['filter_sidebar'] ?? $this->getData('filter_sidebar') ?? '';

$showHeader = $meta['showHeader'] ?? $this->getData('showHeader') ?? true;
$showFooter = $meta['showFooter'] ?? $this->getData('showFooter') ?? true;
$showFilters = $meta['showFilters'] ?? $this->getData('showFilters') ?? true;
$filterDrawerTitle = \Weline\Theme\Helper\WidgetI18n::label(
    (string)($meta['filterDrawerTitle'] ?? $this->getData('filterDrawerTitle') ?? '筛选条件')
);
$gridColumns = $meta['gridColumns'] ?? $this->getData('gridColumns') ?? 4;
$pagination = $this->request->getData('pagination') ?? $this->getData('pagination') ?? '';
$paginationData = $this->request->getData('pagination_data') ?? $this->getData('pagination_data') ?? [];

$meta['showHeader'] = $showHeader;
$meta['showFooter'] = $showFooter;
$meta['showFilters'] = $showFilters;
$meta['filterDrawerTitle'] = $filterDrawerTitle;
$meta['gridColumns'] = $gridColumns;
$this->assign('meta', $meta);

$this->assign('title', $title);
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" dir="{{htmlDir}}" data-local="{{lang_local}}" data-lang="{{lang}}" data-dir="{{htmlDir}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes, shrink-to-fit=no">
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::base::head-after</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::default::head-after</w:hook>
    <w:hook>seo::head</w:hook>
</head>
<body>
    <w:hook>seo::body</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::body-start</w:hook>
    
    <div class="weline-page-wrapper">
        <if condition="meta.showHeader">
            <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
            <w:hook>Weline_Theme::frontend::layouts::category::header-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>
            <w:hook>Weline_Theme::frontend::layouts::category::header-after</w:hook>
            <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>
        </if>

        <div class="weline-main-content">
            <!-- 分类页布局的面包屑导航区域 -->
            <w:hook>Weline_Theme::frontend::layouts::base::breadcrumb-before</w:hook>
            <w:hook>Weline_Theme::frontend::layouts::category::breadcrumb-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="frontend" type="breadcrumb" default-option="default"/>
            <w:hook>Weline_Theme::frontend::layouts::category::breadcrumb-after</w:hook>
            <w:hook>Weline_Theme::frontend::layouts::base::breadcrumb-after</w:hook>

            <w:hook>Weline_Theme::frontend::layouts::base::content-before</w:hook>
            <w:hook>Weline_Theme::frontend::layouts::category::content-before</w:hook>

            <w:slot id="content"
                    name="内容区"
                    accept="layout-category-content,layout-default-content,content,category-content,category-filter,category-grid,product-grid,pagination,product-carousel"
                    reject="header,footer"
                    multiple="true"
                    position="content"
                    class="category-layout__content-root-slot">
            <main class="category-layout__main <?= htmlspecialchars($class ?? '') ?>" id="category-layout-main" data-layout="category" data-columns="<?= (int)$gridColumns ?>">
                <w:hook>Weline_Theme::frontend::layouts::category::main-before</w:hook>

                <div class="category-layout__container">
                    <!-- 移动端筛选切换按钮 -->
                    <if condition="meta.showFilters">
                        <button type="button" class="category-layout__filter-toggle" aria-label="@lang{筛选条件}" aria-expanded="false" data-filter-toggle>
                            <svg class="category-layout__filter-icon" width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                                <path d="M2 5h16M5 10h10M8 15h4" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                            </svg>
                            <span class="category-layout__filter-label"><lang>筛选</lang></span>
                            <span class="category-layout__filter-badge" id="filter-count-badge" hidden>@lang{0}</span>
                        </button>
                    </if>

                    <!-- 筛选侧边栏 -->
                    <if condition="meta.showFilters">
                        <aside class="category-layout__sidebar" id="category-filter-sidebar" data-sidebar>
                            <div class="category-layout__sidebar-header">
                                <h3 class="category-layout__sidebar-title">{{meta.filterDrawerTitle}}</h3>
                                <button type="button" class="category-layout__sidebar-close" aria-label="@lang{关闭筛选}" data-sidebar-close>
                                    <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                                        <path d="M15 5L5 15M5 5l10 10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                                    </svg>
                                </button>
                            </div>
                            <div class="category-layout__sidebar-content">
                                <w:slot id="category-filters"
                                        name="分类筛选区"
                                        accept="layout-category-filters,category-filters,attribute-filter,price-filter,category-filter,rating-filter"
                                        multiple="true"
                                        position="sidebar">
                                    <w:hook>Weline_Theme::frontend::layouts::category::filters-before</w:hook>
                                    <w:hook>Weline_Theme::frontend::layouts::category::subcategories-filter</w:hook>
                                    <if condition="meta.filter_sidebar">
                                        {{meta.filter_sidebar}}
                                    <elseif condition="filter_sidebar"/>
                                        {{filter_sidebar}}
                                    </if>
                                    <w:widget type="sidebar" name="category-filters"/>
                                    <w:hook>Weline_Theme::frontend::layouts::category::filters-after</w:hook>
                                </w:slot>
                            </div>
                        </aside>
                    </if>

                    <!-- 移动端遮罩层 -->
                    <div class="category-layout__overlay" id="category-filter-overlay" data-overlay hidden></div>

                    <!-- 分类内容区域 -->
                    <section class="category-layout__content">
                        <w:slot id="category-products"
                                name="分类产品区域"
                                accept="layout-category-products,product-grid,product-list-view,category-children"
                                multiple="true"
                                position="content"
                                class="category-layout__products">
                            <w:hook>
                                WeShop_Catalog::frontend::layouts::category::products-content
                                <else/>
                                <if condition="meta.content">
                                    {{meta.content}}
                                <elseif condition="content"/>
                                    {{content}}
                                <else/>
                                    <div class="category-layout__placeholder" data-placeholder="category-products">
                                        <div class="category-layout__placeholder-icon">
                                            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                                                <path d="M3 7V5C3 3.89543 3.89543 3 5 3H19C20.1046 3 21 3.89543 21 5V7M3 7V19C3 20.1046 3.89543 21 5 21H19C20.1046 21 21 20.1046 21 19V7M3 7H21" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                                            </svg>
                                        </div>
                                        <span class="category-layout__placeholder-text"><lang>分类产品区域 - 展示该分类下的产品</lang></span>
                                    </div>
                                </if>
                            </w:hook>
                        </w:slot>
                        <?php if ($pagination || ((int)($paginationData['pages'] ?? 0)) > 1): ?>
                            <w:slot id="category-pagination"
                                    name="分类分页区"
                                    accept="layout-category-pagination,pagination,load-more"
                                    exclusive="true"
                                    multiple="false"
                                    position="content"
                                    wrapper="div"
                                    class="category-layout__pagination"
                                    >
                            <div data-category-pagination>
                                <?php if ($pagination): ?>
                                    <?= $pagination ?>
                                <?php else: ?>
                                    <?php
                                    $currentPage = max(1, (int)($paginationData['page'] ?? 1));
                                    $totalPages = max(1, (int)($paginationData['pages'] ?? 1));
                                    $prevUrl = (string)($paginationData['prev_url'] ?? '');
                                    $nextUrl = (string)($paginationData['next_url'] ?? '');
                                    ?>
                                    <nav class="category-pagination" aria-label="<?= htmlspecialchars((string)__('Pagination'), ENT_QUOTES, 'UTF-8') ?>">
                                        <a
                                            href="<?= htmlspecialchars($prevUrl !== '' ? $prevUrl : '#', ENT_QUOTES, 'UTF-8') ?>"
                                            class="category-pagination__link <?= $prevUrl !== '' ? '' : 'is-disabled' ?>"
                                            aria-disabled="<?= $prevUrl !== '' ? 'false' : 'true' ?>"
                                        >
                                            <?= __('Previous') ?>
                                        </a>
                                        <span class="category-pagination__status">
                                            <?= __('Page %{1} of %{2}', [$currentPage, $totalPages]) ?>
                                        </span>
                                        <a
                                            href="<?= htmlspecialchars($nextUrl !== '' ? $nextUrl : '#', ENT_QUOTES, 'UTF-8') ?>"
                                            class="category-pagination__link <?= $nextUrl !== '' ? '' : 'is-disabled' ?>"
                                            aria-disabled="<?= $nextUrl !== '' ? 'false' : 'true' ?>"
                                        >
                                            <?= __('Next') ?>
                                        </a>
                                    </nav>
                                <?php endif; ?>
                            </div>
                            </w:slot>
                        <?php endif; ?>
                    </section>
                </div>

                <w:hook>Weline_Theme::frontend::layouts::category::main-after</w:hook>
            </main>

            <w:slot id="category-recommendations"
                    name="分类推荐区"
                    accept="layout-category-recommendations,featured-products,bestsellers,new-arrivals,category-grid,product-carousel"
                    multiple="true"
                    position="content"
                    wrapper="section"
                    class="category-layout__recommendations">
                <w:hook>Weline_Theme::frontend::layouts::category::recommendations</w:hook>
            </w:slot>

            </w:slot>

            <w:hook>Weline_Theme::frontend::layouts::category::content-after</w:hook>
            <w:hook>Weline_Theme::frontend::layouts::base::content-after</w:hook>
        </div>

        <if condition="meta.showFooter">
            <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
            <w:hook>Weline_Theme::frontend::layouts::category::footer-before</w:hook>
            <footer class="weline-footer">
                <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default"/>
            </footer>
            <w:hook>Weline_Theme::frontend::layouts::category::footer-after</w:hook>
            <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
        </if>
    </div>

    <w:hook>Weline_Theme::frontend::layouts::category::body-end</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::body-end</w:hook>

    <style>
    /* ========================================
       分类页布局样式
       使用主题 CSS 变量，支持明暗主题
       BEM 命名规范
       ======================================== */

    /* ---- 页面主体 ---- */
    .category-layout__main {
        width: 100%;
        padding: var(--spacing-4, 2rem) 0 var(--spacing-6, 3rem);
    }

    .category-layout__container {
        width: 100%;
        max-width: var(--weline-layout-content-max-width, var(--layout-max-width, 1440px));
        margin: 0 auto;
        display: grid;
        grid-template-columns: var(--sidebar-width, 280px) minmax(0, 1fr);
        gap: var(--spacing-6, 1.5rem);
        align-items: flex-start;
        padding: 0 var(--weline-layout-content-padding-inline, var(--spacing-4, 1.5rem));
        box-sizing: border-box;
    }

    /* ---- 筛选侧边栏 ---- */
    .category-layout__sidebar {
        background-color: var(--color-bg-primary);
        border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--color-border-light);
        border-radius: var(--radius-lg, 12px);
        box-shadow: var(--weline-theme-shadow-sm);
        position: sticky;
        top: calc(var(--header-height, 70px) + var(--spacing-4, 1rem));
        max-height: calc(100vh - var(--header-height, 70px) - var(--spacing-8, 4rem));
        overflow-y: auto;
        overscroll-behavior: contain;
    }

    .category-layout__sidebar-header {
        display: none;
        align-items: center;
        justify-content: space-between;
        padding: var(--spacing-4, 1rem) var(--spacing-5, 1.25rem);
        border-bottom: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--color-border-light);
        background-color: var(--color-bg-primary);
        position: sticky;
        top: 0;
        z-index: 10;
    }

    .category-layout__sidebar-title {
        font-size: var(--font-size-lg, 1.125rem);
        font-weight: var(--font-weight-semibold, 600);
        margin: 0;
        color: var(--color-text-primary);
    }

    .category-layout__sidebar-close {
        width: 36px;
        height: 36px;
        padding: 0;
        background: none;
        border: none;
        border-radius: var(--radius-md, 8px);
        cursor: pointer;
        color: var(--color-text-secondary);
        display: flex;
        align-items: center;
        justify-content: center;
        transition: background-color 0.2s ease, color 0.2s ease;
    }

    .category-layout__sidebar-close:hover {
        background-color: var(--color-bg-secondary);
        color: var(--color-text-primary);
    }

    .category-layout__sidebar-content {
        padding: var(--spacing-4, 1rem) var(--spacing-5, 1.25rem);
    }

    /* ---- 筛选切换按钮（移动端） ---- */
    .category-layout__filter-toggle {
        display: none;
        align-items: center;
        gap: var(--spacing-2, 0.5rem);
        padding: var(--spacing-2, 0.5rem) var(--spacing-4, 1rem);
        background-color: var(--color-bg-primary);
        border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--color-border-light);
        border-radius: var(--radius-full, 9999px);
        cursor: pointer;
        color: var(--color-text-primary);
        font-size: var(--font-size-sm, 0.875rem);
        font-weight: var(--font-weight-medium, 500);
        box-shadow: var(--weline-theme-shadow-md);
        transition: all 0.2s ease;
    }

    .category-layout__filter-toggle:hover {
        background-color: var(--color-bg-secondary);
        box-shadow: var(--weline-theme-shadow-lg);
    }

    .category-layout__filter-toggle[aria-expanded="true"] {
        background-color: var(--color-accent);
        border-color: var(--color-accent);
        color: var(--color-text-light);
    }

    .category-layout__filter-icon {
        width: 18px;
        height: 18px;
        flex-shrink: 0;
    }

    .category-layout__filter-badge {
        min-width: 18px;
        height: 18px;
        padding: 0 4px;
        background-color: var(--color-error);
        color: var(--color-text-light);
        border-radius: var(--radius-full, 9999px);
        font-size: var(--weline-layout-font-size-xs);
        font-weight: var(--font-weight-semibold, 600);
        display: inline-flex;
        align-items: center;
        justify-content: center;
    }

    /* ---- 遮罩层 ---- */
    .category-layout__overlay {
        position: fixed;
        inset: 0;
        background-color: color-mix(in srgb, var(--weline-theme-text) 50%, transparent);
        z-index: var(--z-index-overlay, 998);
        opacity: 0;
        visibility: hidden;
        transition: opacity 0.3s ease, visibility 0.3s ease;
    }

    .category-layout__overlay.is-visible {
        opacity: 1;
        visibility: visible;
    }

    /* ---- 内容区域 ---- */
    .category-layout__content {
        min-width: 0;
    }

    .category-layout__pagination {
        display: flex;
        justify-content: center;
        margin-top: var(--spacing-6, 2rem);
    }

    .category-pagination {
        display: inline-flex;
        align-items: center;
        gap: var(--spacing-4, 1rem);
        padding: 0.75rem 1rem;
        border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--color-border-light);
        border-radius: var(--radius-lg, 12px);
        background: var(--color-bg-primary);
        box-shadow: var(--weline-theme-shadow-sm);
    }

    .category-pagination__link {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-width: 5rem;
        padding: 0.5rem 0.9rem;
        border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--color-border-light);
        border-radius: var(--weline-layout-radius-full);
        color: var(--color-text-primary);
        font-size: var(--font-size-sm, 0.875rem);
        font-weight: var(--font-weight-semibold, 600);
        text-decoration: none;
        transition: border-color 0.2s ease, color 0.2s ease;
    }

    .category-pagination__link:hover {
        border-color: var(--weline-theme-primary);
        color: var(--weline-theme-primary);
    }

    .category-pagination__link.is-disabled {
        pointer-events: none;
        opacity: 0.45;
    }

    .category-pagination__status {
        color: var(--color-text-secondary);
        font-size: var(--font-size-sm, 0.875rem);
        white-space: nowrap;
    }

    .category-layout__products {
        width: 100%;
    }

    .category-layout__products > * + * {
        margin-top: var(--spacing-8, 2.75rem);
    }

    /* ---- 占位符 ---- */
    .category-layout__placeholder {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: var(--spacing-4, 1rem);
        padding: var(--spacing-8, 4rem) var(--spacing-4, 1.5rem);
        text-align: center;
        color: var(--color-text-tertiary);
    }

    .category-layout__placeholder-icon {
        color: var(--color-text-disabled);
    }

    .category-layout__placeholder-text {
        font-size: var(--font-size-sm, 0.875rem);
    }

    /* ---- 筛选栏内部样式（通用） ---- */
    .category-filter-mock {
        display: flex;
        flex-direction: column;
        gap: var(--spacing-3, 0.75rem);
        font-size: var(--font-size-sm, 0.875rem);
        color: var(--color-text-secondary);
    }

    .category-filter-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: var(--spacing-1, 0.25rem);
    }

    .category-filter-title {
        font-size: var(--font-size-sm, 0.875rem);
        font-weight: var(--font-weight-semibold, 600);
        color: var(--color-text-primary);
    }

    .category-filter-clear {
        font-size: var(--font-size-xs, 0.8rem);
        color: var(--color-link);
        text-decoration: none;
        transition: color 0.2s ease;
    }

    .category-filter-clear:hover {
        color: var(--color-link-hover);
        text-decoration: underline;
    }

    .category-filter-applied {
        display: flex;
        flex-wrap: wrap;
        gap: var(--spacing-1, 0.25rem);
        margin-bottom: var(--spacing-1, 0.25rem);
    }

    .filter-chip {
        display: inline-flex;
        align-items: center;
        gap: var(--spacing-1, 0.25rem);
        padding: 2px var(--spacing-2, 0.5rem);
        border-radius: var(--radius-full, 9999px);
        background-color: var(--color-accent-light);
        color: var(--color-accent);
        border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--color-accent-medium);
        font-size: var(--font-size-xs, 0.75rem);
        font-weight: var(--font-weight-medium, 500);
        transition: all 0.2s ease;
    }

    .filter-chip:hover {
        background-color: var(--color-accent-medium);
    }

    .filter-chip__remove {
        background: none;
        border: none;
        padding: 0;
        margin: 0;
        cursor: pointer;
        color: inherit;
        font-size: inherit;
        line-height: 1;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .category-filter-group {
        padding-top: var(--spacing-3, 0.75rem);
        margin-top: var(--spacing-3, 0.75rem);
        border-top: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--color-border-light);
    }

    .category-filter-group-title {
        font-size: var(--font-size-sm, 0.875rem);
        font-weight: var(--font-weight-semibold, 600);
        margin-bottom: var(--spacing-1, 0.25rem);
        color: var(--color-text-primary);
    }

    .category-filter-list {
        list-style: none;
        padding: 0;
        margin: var(--spacing-1, 0.25rem) 0 0;
    }

    .category-filter-item {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: var(--spacing-1, 0.25rem) 0;
        color: var(--color-text-primary);
        text-decoration: none;
        cursor: pointer;
        border-radius: var(--radius-sm, 4px);
        transition: background-color 0.2s ease;
    }

    .category-filter-item:hover {
        background-color: var(--color-bg-secondary);
    }

    .category-filter-item__label {
        display: inline-flex;
        align-items: center;
        gap: var(--spacing-2, 0.5rem);
        font-size: var(--font-size-sm, 0.875rem);
    }

    .category-filter-item__stars {
        color: var(--color-warning);
        font-size: var(--font-size-xs, 0.8rem);
    }

    .category-filter-item__count {
        font-size: var(--font-size-xs, 0.75rem);
        color: var(--color-text-secondary);
    }

    /* ---- 响应式设计 ---- */
    @media screen and (max-width: 992px) {
        .category-layout__container {
            grid-template-columns: 1fr;
            padding: 0 var(--spacing-4, 1rem);
        }

        .category-layout__filter-toggle {
            display: inline-flex;
            position: fixed;
            bottom: var(--spacing-6, 1.5rem);
            right: var(--spacing-4, 1rem);
            z-index: var(--z-index-sticky, 997);
        }

        .category-layout__sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: min(85%, 360px);
            height: 100vh;
            max-height: 100vh;
            border-radius: var(--border-radius-none);
            border: none;
            border-right: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--color-border-light);
            z-index: var(--z-index-drawer, 999);
            transform: translateX(-100%);
            transition: transform 0.3s ease;
            display: flex;
            flex-direction: column;
        }

        .category-layout__sidebar.is-open {
            transform: translateX(0);
        }

        .category-layout__sidebar-header {
            display: flex;
        }

        .category-layout__sidebar-content {
            flex: 1;
            overflow-y: auto;
            overscroll-behavior: contain;
        }

        .category-layout__overlay {
            display: block;
        }
    }

    @media screen and (max-width: 768px) {
        .category-layout__main {
            padding: var(--spacing-3, 1rem) 0 var(--spacing-8, 4rem);
        }

        .category-layout__container {
            padding: 0 var(--spacing-3, 0.75rem);
            gap: var(--spacing-3, 0.75rem);
        }

        .category-layout__products > * + * {
            margin-top: var(--spacing-6, 1.75rem);
        }

        .category-layout__filter-toggle {
            bottom: var(--spacing-5, 1.25rem);
            right: var(--spacing-3, 0.75rem);
        }
    }

    @media screen and (max-width: 480px) {
        .category-layout__main {
            padding: var(--spacing-2, 0.5rem) 0 var(--spacing-7, 3.5rem);
        }

        .category-layout__container {
            padding: 0 var(--spacing-2, 0.5rem);
            gap: var(--spacing-2, 0.5rem);
        }

        .category-layout__products > * + * {
            margin-top: var(--spacing-5, 1.25rem);
        }

        .category-layout__filter-toggle {
            bottom: var(--spacing-4, 1rem);
            right: var(--spacing-2, 0.5rem);
            padding: var(--spacing-2, 0.5rem) var(--spacing-3, 0.75rem);
        }

        .category-layout__filter-label {
            display: none;
        }
    }
    </style>


    <w:hook>seo::footer</w:hook>
</body>
</html>
