<?php
/**
 * 布局：结账页 - 默认布局
 * 
 * 结账页面默认布局，包含订单信息、收货地址、支付方式等
 * 字段
 * @meta.name {default="结账页默认布局",name="结账页默认布局",description="结账页默认布局，用于描述布局的名称"}
 * @meta.description {default="结账页面默认布局，包含订单信息、收货地址、支付方式等",name="结账页面默认布局，包含订单信息、收货地址、支付方式等",description="结账页面默认布局，包含订单信息、收货地址、支付方式等，用于描述布局的用途"}
 * @preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="是否需要登录，0不需要登录，1需要登录，这个不是元素据，是预览系统判断是否需要自动登录的。"}
 * @param title {default="结账",name="页面标题",description="页面标题，用于设置页面标题"}
 * @param content {default="",name="主要内容（HTML字符串）",description="主要内容（HTML字符串），用于设置页面内容"}
 * @param orderSummary {default="",name="订单摘要（HTML字符串）",description="订单摘要（HTML字符串），用于设置订单摘要区域"}
 * @param steps {default="",name="结账步骤导航（HTML字符串，可选）",description="结账步骤导航（HTML字符串，可选），用于设置结账步骤导航"}
 * @param breadcrumb {default="",name="面包屑导航（可选）",description="面包屑导航（可选），用于设置面包屑导航"}
 * @param class {default="",name="额外CSS类",description="额外CSS类，用于设置页面额外CSS类"}
 */
$title = $this->getData('title') ?? __('结账');
$content = $this->getData('content') ?? '';
$orderSummary = $this->getData('orderSummary') ?? '';
$steps = $this->getData('steps') ?? '';
$breadcrumb = $this->getData('breadcrumb') ?? '';
$class = $this->getData('class') ?? '';

// 设置页面标题
$this->assign('title', $title);
?>
<!DOCTYPE html>
<html lang="<?= str_replace('_', '-', $_SERVER['WELINE_USER_LANG'] ?? 'zh_Hans_CN') ?>">
<head>
    @template(Weline_Frontend::templates/public/head.phtml)
</head>
<body>
    @template(Weline_Frontend::templates/public/header.phtml)
    
    <?php if ($breadcrumb): ?>
        <nav class="breadcrumb-nav">
            <?= $breadcrumb ?>
        </nav>
    <?php endif; ?>
    
    <main class="checkout-main <?= htmlspecialchars($class) ?>">
        <div class="checkout-container">
            <?php if ($steps): ?>
                <div class="checkout-steps">
                    <?= $steps ?>
                </div>
            <?php endif; ?>
            
            <div class="checkout-content-wrapper">
                <div class="checkout-form">
                    <?= $content ?>
                </div>
                
                <?php if ($orderSummary): ?>
                    <aside class="checkout-summary">
                        <?= $orderSummary ?>
                    </aside>
                <?php endif; ?>
            </div>
        </div>
    </main>
    
    @template(Weline_Frontend::templates/public/footer.phtml)
</body>
</html>

