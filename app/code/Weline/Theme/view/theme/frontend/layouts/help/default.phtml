<?php
/**
 * 帮助中心默认布局（亚马逊风格）
 *
 * 自动读取站点名称、描述与联系方式；提供主题话题、FAQ、联系卡片，
 * 并预留可扩展 slot 供其他模块投放小部件。
 *
 * @meta.name {default="帮助中心",name="帮助中心",description="电商帮助中心默认布局"}
 * @meta.description {default="站点帮助中心：主题导航、FAQ、联系方式与可扩展部件槽",name="描述",description="帮助中心页面布局"}
 * @preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="帮助中心通常允许访客访问"}
 * @param.title {default="帮助中心",name="页面标题",description="帮助中心标题"}
 * @param.subtitle {default="",name="副标题",description="留空时使用站点描述"}
 * @param.showHeader {default=true,name="显示 header",description="是否渲染站点头部"}
 * @param.showFooter {default=true,name="显示 footer",description="是否渲染站点底部"}
 * @param.class {default="",name="额外 CSS 类",description="主内容额外 CSS 类"}
 */
use Weline\Framework\Manager\ObjectManager;
use Weline\Theme\Helper\SiteContactInfo;

$meta = $this->getData('meta') ?? [];
$escape = static fn($value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');

$contact = [
    'site_name' => 'Weline',
    'site_description' => (string)__('官方商城帮助与客户服务'),
    'contact_email' => 'support@example.com',
    'contact_phone' => '',
    'service_hours' => (string)__('周一至周五 9:00 - 18:00（法定节假日除外）'),
    'contact_address' => '',
];
try {
    $resolver = ObjectManager::getInstance(SiteContactInfo::class);
    if ($resolver instanceof SiteContactInfo) {
        $contact = array_merge($contact, $resolver->resolve());
    }
} catch (\Throwable) {
}

$title = (string)($meta['title'] ?? __('帮助中心'));
$subtitle = trim((string)($meta['subtitle'] ?? ''));
if ($subtitle === '') {
    $subtitle = (string)$contact['site_description'];
}
if ($subtitle === '' || $subtitle === (string)$contact['site_description']) {
    $lead = (string)__('查找订单、配送、退换、支付与账户问题的自助指南。也可搜索关键词，或从下方主题快速进入。');
} else {
    $lead = $subtitle;
}
$siteName = (string)$contact['site_name'];
$email = (string)$contact['contact_email'];
$phone = (string)$contact['contact_phone'];
$hours = (string)$contact['service_hours'];
$address = (string)$contact['contact_address'];
$mailHref = 'mailto:' . $email;
$updatedAt = date('Y-m-d');
$topics = [
    ['icon' => 'truck', 'title' => __('物流配送'), 'desc' => __('发货时效、运费与配送范围'), 'url' => 'guide/shipping', 'route' => true],
    ['icon' => 'undo', 'title' => __('退换货'), 'desc' => __('退货条件、流程与退款进度'), 'url' => 'guide/returns', 'route' => true],
    ['icon' => 'credit-card', 'title' => __('支付与发票'), 'desc' => __('支付方式、账单与发票开具'), 'url' => 'guide/payment', 'route' => true],
    ['icon' => 'box', 'title' => __('订单问题'), 'desc' => __('下单、改单、取消与订单状态'), 'url' => 'customer/account/index#orders', 'route' => true],
    ['icon' => 'user', 'title' => __('账户与安全'), 'desc' => __('登录、密码与个人信息'), 'url' => 'customer/account/index', 'route' => true],
    ['icon' => 'headset', 'title' => __('联系客服'), 'desc' => __('电话、邮箱与在线支持通道'), 'url' => '#contact-service', 'open_cs' => true],
];

$faqs = [
    ['q' => __('下单后多久发货？'), 'a' => __('现货订单通常在付款成功后 1–3 个工作日内发出（节假日顺延）；预售、定制或以商品页标注时效为准。详见「配送说明」。')],
    ['q' => __('如何查询物流？'), 'a' => __('登录后打开「我的订单」可查看承运商与运单节点。长时间无更新时，可先排除节假日，再通过「联系客服」并提供订单号协助查询。')],
    ['q' => __('运费如何计算？是否包邮？'), 'a' => __('运费按收货地区、重量/体积与配送服务在结账页实时计算。满足满额包邮或活动门槛时会自动减免，以结算页显示为准。')],
    ['q' => __('支持哪些支付方式？'), 'a' => __('支持站点已开通的在线支付渠道（如 PayPal 等）。具体可用方式以结算页与「支付方式」指南为准，并可查阅各支付商的用户协议。')],
    ['q' => __('如何申请退换货？'), 'a' => __('请先阅读「退换政策」确认期限与品类要求，再在「我的订单」提交售后并上传凭证。质量问题与个人原因的运费承担规则不同，详见政策页。')],
    ['q' => __('退款多久到账？'), 'a' => __('退回商品质检通过后，退款一般在 3–15 个工作日内原路退回，具体到账时间以支付渠道为准。完整规则见「退款政策」。')],
    ['q' => __('可以修改或取消订单吗？'), 'a' => __('未发货订单可在「我的订单」尝试取消或联系客服协助修改地址/备注。已发货订单无法直接取消，可按签收后的退换政策办理。')],
    ['q' => __('个人信息如何保护？'), 'a' => __('我们仅在提供交易与服务所必需的范围内处理个人信息，详见「隐私政策」与「Cookie 政策」。您可在账户设置中管理部分偏好。')],
];
$quickLinks = [
    ['label' => __('我的订单'), 'url' => 'customer/account/index#orders', 'route' => true],
    ['label' => __('我的账户'), 'url' => 'customer/account/index', 'route' => true],
    ['label' => __('配送说明'), 'url' => 'guide/shipping', 'route' => true],
    ['label' => __('退换政策'), 'url' => 'guide/returns', 'route' => true],
    ['label' => __('退款政策'), 'url' => 'refund', 'route' => true],
    ['label' => __('支付方式'), 'url' => 'guide/payment', 'route' => true],
    ['label' => __('隐私政策'), 'url' => 'privacy', 'route' => true],
    ['label' => __('服务条款'), 'url' => 'terms', 'route' => true],
    ['label' => __('Cookie 政策'), 'url' => 'cookies', 'route' => true],
    ['label' => __('联系客服'), 'url' => '#contact-service', 'route' => false, 'open_cs' => true],
];
$ariaSupport = (string)__('帮助支持');
$ariaQuick = (string)__('快捷入口');
$ariaSearch = (string)__('搜索帮助');
$searchPlaceholder = (string)__('搜索帮助主题，例如：退货、运费、发票');
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" dir="{{htmlDir}}" data-local="{{lang_local}}" data-lang="{{lang}}" data-dir="{{htmlDir}}" data-w-area="frontend">
<head>
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::base::head-after</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::help::head-after</w:hook>
    <style>
        body.help-layout {
            margin: 0;
            min-height: 100vh;
            color: var(--color-text-primary, #0f1111);
            background: var(--color-bg-canvas, #ffffff);
            font-family: "Amazon Ember", Arial, sans-serif;
        }
        .amazon-help.weline-page-wrapper {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .amazon-help__main { flex: 1 0 auto; display: flex; flex-direction: column; }
        .amazon-help__hero {
            margin: 0 0 1.25rem;
            padding: clamp(1.35rem, 3vw, 1.85rem) clamp(1.15rem, 2.5vw, 1.75rem);
            border-radius: 8px;
            background:
                radial-gradient(ellipse at top right, rgba(255, 153, 0, .14), transparent 46%),
                linear-gradient(135deg, var(--color-bg-dark-secondary) 0%, var(--color-bg-dark) 58%, var(--color-bg-dark-tertiary) 100%);
            color: #fff;
            box-sizing: border-box;
        }
        .amazon-help__stage {
            box-sizing: border-box;
            width: min(100%, var(--weline-layout-content-max-width));
            max-width: 100%;
            min-width: 0;
            margin-inline: auto;
            padding-inline: var(--weline-layout-content-padding-inline);
            padding-block: clamp(2.25rem, 4.5vw, 3.25rem) clamp(3rem, 6vw, 4.5rem);
        }
        .amazon-help__eyebrow {
            margin: 0 0 .45rem;
            font-size: .85rem;
            font-weight: 700;
            letter-spacing: .08em;
            text-transform: uppercase;
            color: #febd69;
        }
        .amazon-help__hero h1 {
            margin: 0;
            font-size: clamp(1.7rem, 3.6vw, 2.4rem);
            line-height: 1.2;
            font-weight: 700;
            color: #fff;
        }
        .amazon-help__hero-lead {
            margin: .85rem 0 0;
            max-width: 46rem;
            line-height: 1.65;
            color: #ddd;
        }
        .amazon-help__meta {
            margin-top: 1rem;
            display: flex;
            flex-wrap: wrap;
            gap: .75rem 1.25rem;
            font-size: .9rem;
            color: #febd69;
        }
        .amazon-help__meta a { color: inherit; text-decoration: underline; }
        .amazon-help__search {
            margin-top: 1.25rem;
            max-width: 40rem;
        }
        .amazon-help__search-form {
            display: flex;
            gap: 0;
            background: #fff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, .22);
        }
        .amazon-help__search-form input {
            flex: 1;
            min-width: 0;
            min-height: 2.85rem;
            border: 0;
            padding: 0 1rem;
            font-size: 1rem;
            color: #0f1111;
            outline: none;
        }
        .amazon-help__search-form button {
            min-height: 2.85rem;
            border: 0;
            padding: 0 1.25rem;
            background: #febd69;
            color: #111;
            font-weight: 700;
            cursor: pointer;
        }
        .amazon-help__search-form button:hover { background: #f3a847; }
        .amazon-help__body {
            flex: 1 0 auto;
            width: 100%;
            background: linear-gradient(180deg, #eaeded 0, #f3f3f3 12rem);
        }
        .amazon-help__panel {
            background: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 1px 2px rgba(15, 17, 17, .08);
            padding: clamp(1.15rem, 2.4vw, 1.65rem);
            box-sizing: border-box;
        }
        .amazon-help__panel + .amazon-help__panel,
        .amazon-help__panel + .help-layout__support,
        .help-layout__primary > * + * {
            margin-top: 1.15rem;
        }
        .amazon-help__panel-title {
            margin: 0 0 1rem;
            font-size: 1.2rem;
            font-weight: 700;
            border-left: 4px solid var(--color-primary);
            padding-left: .65rem;
            color: #0f1111;
        }
        .help-layout__topics {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: .85rem;
        }
        .help-layout__topic {
            display: grid;
            gap: .45rem;
            padding: 1rem 1.05rem;
            border: 1px solid #e3e6e6;
            border-radius: 8px;
            text-decoration: none;
            color: inherit;
            background: #fafafa;
            transition: border-color .15s ease, box-shadow .15s ease, background .15s ease;
        }
        .help-layout__topic:hover {
            border-color: var(--color-primary);
            background: #fff;
            box-shadow: 0 1px 3px rgba(15, 17, 17, .08);
            text-decoration: none;
        }
        .help-layout__topic strong {
            font-size: 1rem;
            color: #0f1111;
        }
        .help-layout__topic span {
            color: #565959;
            font-size: .9rem;
            line-height: 1.5;
        }
        .help-layout__faq { display: grid; gap: .75rem; }
        .help-layout__faq-item {
            padding: .95rem 1rem;
            border: 1px solid #e3e6e6;
            border-radius: 8px;
            background: #fafafa;
        }
        .help-layout__faq-item strong {
            display: block;
            margin-bottom: .4rem;
            color: #0f1111;
            border-left: 3px solid var(--color-primary);
            padding-left: .55rem;
        }
        .help-layout__faq-item p {
            margin: 0;
            color: #333;
            line-height: 1.65;
            font-size: .96rem;
        }
        .help-layout__extras { display: grid; gap: 1rem; }
        .help-layout__support { margin-top: 1.15rem; width: 100%; }
        .help-layout__sidebar-slot {
            display: block;
            width: 100%;
            min-width: 0;
        }
        .help-layout__support-grid {
            display: grid;
            grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.4fr);
            gap: 1rem;
            align-items: stretch;
            width: 100%;
            min-width: 0;
        }
        .help-layout__card {
            min-width: 0;
            padding: 1.15rem 1.2rem;
            border: 1px solid #ddd;
            border-radius: 8px;
            background: #fff;
            box-shadow: 0 1px 2px rgba(15, 17, 17, .08);
        }
        .help-layout__card h3 {
            margin: 0 0 .9rem;
            font-size: 1.05rem;
            font-weight: 700;
            border-left: 4px solid var(--color-primary);
            padding-left: .55rem;
            writing-mode: horizontal-tb;
            white-space: normal;
            line-height: 1.35;
        }
        .help-layout__contact { display: grid; gap: .55rem; color: #565959; line-height: 1.6; }
        .help-layout__contact p { margin: 0; }
        .help-layout__contact a { color: var(--color-link); text-decoration: none; }
        .help-layout__contact a:hover { color: var(--color-link-hover); text-decoration: underline; }
        .help-layout__contact-cta {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-top: .55rem;
            min-height: 2.45rem;
            padding: 0 1rem;
            border-radius: 8px;
            background: var(--color-primary);
            color: #111 !important;
            font-weight: 700;
            text-decoration: none !important;
            width: fit-content;
        }
        .help-layout__contact-cta:hover { filter: brightness(.96); text-decoration: none !important; }
        .help-layout__quick-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(9rem, 1fr));
            gap: .65rem;
            width: 100%;
            min-width: 0;
        }
        .help-layout__quick-link {
            display: flex;
            align-items: center;
            min-height: 2.55rem;
            padding: .55rem .8rem;
            border: 1px solid #e3e6e6;
            border-radius: 8px;
            background: #fafafa;
            color: #0f1111;
            text-decoration: none;
            font-size: .95rem;
            line-height: 1.35;
            white-space: nowrap;
        }
        .help-layout__quick-link:hover {
            border-color: var(--color-primary);
            color: var(--color-link-hover);
            background: #fff;
            text-decoration: none;
        }
        @media (max-width: 900px) {
            .help-layout__topics { grid-template-columns: 1fr 1fr; }
            .help-layout__support-grid { grid-template-columns: 1fr; }
        }
        @media (max-width: 560px) {
            .help-layout__topics { grid-template-columns: 1fr; }
            .amazon-help__search-form { flex-direction: column; }
            .amazon-help__search-form button { min-height: 2.6rem; }
        }
    </style>
    <w:hook>seo::head</w:hook>
</head>
<body class="help-layout amazon-help-layout <?= $escape($meta['class'] ?? '') ?>">
    <w:hook>seo::body</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::body-start</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::help::body-start</w:hook>

    <div class="weline-page-wrapper amazon-help" data-testid="storefront-help-page">
        <if condition="meta.showHeader">
            <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>
            <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>
        </if>

        <main class="help-layout__main amazon-help__main" id="help-main" data-layout="help">
            <div class="amazon-help__body">
                <div class="amazon-help__stage help-layout__inner">
                    <header class="amazon-help__hero" aria-labelledby="help-layout-title" weline-code="theme.help.hero">
                        <p class="amazon-help__eyebrow"><?= $escape($siteName) ?></p>
                        <h1 id="help-layout-title"><?= $escape($title) ?></h1>
                        <p class="amazon-help__hero-lead"><?= $escape($lead) ?></p>
                        <div class="amazon-help__meta">
                            <span><lang>最后更新：</lang><?= $escape($updatedAt) ?></span>
                            <a href="@url{'guide/shipping'}"><lang>配送说明</lang></a>
                            <a href="@url{'guide/returns'}"><lang>退换政策</lang></a>
                            <a href="@url{'privacy'}"><lang>隐私政策</lang></a>
                            <a href="@url{'terms'}"><lang>服务条款</lang></a>
                        </div>
                        <div class="amazon-help__search help-layout__search">
                            <w:form action="@url{'search'}" method="get" class="amazon-help__search-form help-layout__search-form">
                                <input type="search" name="q" placeholder="<?= $escape($searchPlaceholder) ?>" aria-label="<?= $escape($ariaSearch) ?>">
                                <button type="submit"><lang>搜索</lang></button>
                            </w:form>
                        </div>
                    </header>
                    <div class="help-layout__primary">
                        <section class="amazon-help__panel" aria-labelledby="help-topics-title" weline-code="theme.help.topics">
                            <h2 class="amazon-help__panel-title" id="help-topics-title"><lang>热门帮助主题</lang></h2>
                            <w:slot id="help-topics"
                                    name="帮助主题"
                                    accept="layout-help-topics,help-topics,content,faq,notice"
                                    reject="header,footer,logo,navigation"
                                    multiple="true"
                                    append="true"
                                    max="12"
                                    position="content"
                                    class="help-layout__topics-slot">
                                <div class="help-layout__topics" id="help-topics">
                                    <?php foreach ($topics as $topic): ?>
                                        <?php
                                        $topicOpenCs = !empty($topic['open_cs']);
                                        $topicAttrs = $topicOpenCs ? ' data-cs-footer-open-chat role="button"' : '';
                                        ?>
                                        <a class="help-layout__topic"
                                           href="<?php if (!empty($topic['route'])): ?>@url{$topic['url']}<?php else: ?><?= $escape($topic['url']) ?><?php endif; ?>"<?= $topicAttrs ?>>
                                            <strong><?= $escape($topic['title']) ?></strong>
                                            <span><?= $escape($topic['desc']) ?></span>
                                        </a>
                                    <?php endforeach; ?>
                                </div>
                            </w:slot>
                        </section>

                        <section class="amazon-help__panel help-layout__section" id="help-faq" aria-labelledby="help-faq-title" weline-code="theme.help.faq">
                            <h2 class="amazon-help__panel-title" id="help-faq-title"><?= $escape(__('常见问题')) ?></h2>
                            <w:slot id="help-faq"
                                    name="帮助 FAQ"
                                    accept="layout-help-faq,faq,content,notice"
                                    reject="header,footer,logo,navigation"
                                    multiple="true"
                                    append="true"
                                    max="12"
                                    position="content"
                                    class="help-layout__faq-slot">
                                <div class="help-layout__faq">
                                    <?php foreach ($faqs as $faq): ?>
                                        <article class="help-layout__faq-item">
                                            <strong><?= $escape($faq['q']) ?></strong>
                                            <p><?= $escape($faq['a']) ?></p>
                                        </article>
                                    <?php endforeach; ?>
                                </div>
                            </w:slot>
                        </section>

                        <w:slot id="help-extras"
                                name="帮助扩展区"
                                accept="layout-help-extras,content,faq,notice,banner,promo-banner,trust-badges"
                                reject="header,footer,logo,navigation,product"
                                multiple="true"
                                append="true"
                                max="16"
                                position="content"
                                class="help-layout__extras">
                            <w:hook>Weline_Theme::frontend::layouts::help::extras</w:hook>
                        </w:slot>
                    </div>

                    <section class="help-layout__support" aria-label="<?= $escape($ariaSupport) ?>" data-testid="help-support" weline-code="theme.help.support">
                        <w:slot id="help-sidebar"
                                name="帮助侧栏"
                                accept="layout-help-sidebar,content,notice,faq,banner,contact"
                                reject="header,footer,logo,navigation,product"
                                multiple="true"
                                append="true"
                                max="10"
                                position="content"
                                class="help-layout__sidebar-slot">
                            <div class="help-layout__support-grid">
                                <div class="help-layout__card help-layout__card--contact">
                                    <h3><?= $escape(__('联系我们')) ?></h3>
                                    <div class="help-layout__contact">
                                        <?php if ($email !== ''): ?>
                                            <p><a href="<?= $escape($mailHref) ?>"><?= $escape($email) ?></a></p>
                                        <?php endif; ?>
                                        <?php if ($phone !== ''): ?>
                                            <p><a href="tel:<?= $escape(preg_replace('/\s+/', '', $phone) ?? $phone) ?>"><?= $escape($phone) ?></a></p>
                                        <?php endif; ?>
                                        <?php if ($hours !== ''): ?>
                                            <p><?= $escape($hours) ?></p>
                                        <?php endif; ?>
                                        <?php if ($address !== ''): ?>
                                            <p><?= $escape($address) ?></p>
                                        <?php endif; ?>
                                        <a class="help-layout__contact-cta" href="@url{'contact'}"><?= $escape(__('前往联系我们页')) ?></a>
                                    </div>
                                </div>
                                <div class="help-layout__card help-layout__card--quick">
                                    <h3><?= $escape(__('快捷入口')) ?></h3>
                                    <nav class="help-layout__quick-grid" aria-label="<?= $escape($ariaQuick) ?>">
                                        <?php foreach ($quickLinks as $link): ?>
                                            <?php
                                            $linkOpenCs = !empty($link['open_cs']);
                                            $linkAttrs = $linkOpenCs ? ' data-cs-footer-open-chat role="button"' : '';
                                            ?>
                                            <a class="help-layout__quick-link"
                                               href="<?php if (!empty($link['route'])): ?>@url{$link['url']}<?php else: ?><?= $escape($link['url']) ?><?php endif; ?>"<?= $linkAttrs ?>>
                                                <?= $escape($link['label']) ?>
                                            </a>
                                        <?php endforeach; ?>
                                    </nav>
                                </div>
                            </div>
                        </w:slot>
                    </section>
                </div>
            </div>
        </main>

        <if condition="meta.showFooter">
            <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default" vars="copyright,links,showBackToTop,socialMedia,showSocialMedia"/>
            <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
        </if>
    </div>

    <w:hook>Weline_Theme::frontend::layouts::help::body-end</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::body-end</w:hook>
    <w:hook>seo::footer</w:hook>
</body>
</html>
