<?php
/**
 * 布局：404 页面（Amazon 风格）- 头尾 + 推荐产品槽
 *
 * @meta.name {default="404 页面布局",name="404 页面布局",description="Amazon 风格 404，静态快照由 Theme 生成"}
 * @meta.description {default="Amazon 风格页面不存在布局",name="404 页面布局",description="404 静态页生成模板"}
 * @preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="预览是否自动登录"}
 * @param title {default="抱歉，找不到您要的页面",name="页面标题",description="页面标题"}
 * @param pageTitle {default="抱歉，找不到您要的页面",name="404 标题",description="404 主标题"}
 * @param pageLead {default="您访问的链接可能已失效，或该页面已被移除。",name="404 说明",description="404 主要说明"}
 * @param pageHint {default="请检查网址是否输入正确，或返回首页继续购物。",name="404 提示",description="404 次要提示"}
 * @param homeHref {default="/",name="首页链接",description="返回首页链接"}
 * @param statusCode {default=404,name="状态码",description="HTTP 状态码"}
 * @param class {default="",name="额外CSS类",description="额外 CSS 类"}
 */
$meta = is_array($this->getData('meta')) ? $this->getData('meta') : [];
$statusCode = (int)($meta['statusCode'] ?? 404);
$pageTitle = (string)($meta['pageTitle'] ?? $meta['title'] ?? __('抱歉，找不到您要的页面'));
$pageLead = (string)($meta['pageLead'] ?? __('您访问的链接可能已失效，或该页面已被移除。'));
$pageHint = (string)($meta['pageHint'] ?? __('请检查网址是否输入正确，或返回首页继续购物。'));
$homeHref = (string)($meta['homeHref'] ?? '/');
$homeJs = json_encode($homeHref !== '' ? $homeHref : '/', JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE);
$esc = static fn (string $value): string => htmlspecialchars($value, ENT_QUOTES, 'UTF-8');
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" dir="{{htmlDir}}" data-local="{{lang_local}}" data-lang="{{lang}}" data-dir="{{htmlDir}}" data-w-area="frontend" data-theme-preference="light" data-theme="light">
<head>
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::base::head-after</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::default::head-after</w:hook>
    <meta name="robots" content="noindex, nofollow">
    <title><?= $esc((string)$statusCode) ?> · <?= $esc($pageTitle) ?></title>
    <style>
        .amazon-not-found-layout {
            margin: 0;
            background: var(--color-bg-canvas, #ffffff);
            color: var(--color-text-primary, #0f1111);
            font-family: "Amazon Ember", Arial, sans-serif;
        }

        .amazon-not-found {
            width: 100%;
        }

        .amazon-not-found__intro {
            background: var(--color-bg-canvas, #ffffff);
            border-bottom: 1px solid var(--color-border-default, #ddd);
        }

        .amazon-not-found__intro-inner {
            width: min(var(--weline-layout-content-max-width, 1440px), calc(100% - 2rem));
            margin: 0 auto;
            padding: 2.5rem 0 2.75rem;
            display: grid;
            grid-template-columns: minmax(180px, 280px) minmax(0, 1fr);
            gap: clamp(1.25rem, 3vw, 2.5rem);
            align-items: center;
        }

        .amazon-not-found__visual {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 0.5rem;
        }

        .amazon-not-found__visual svg {
            width: min(100%, 240px);
            height: auto;
        }

        .amazon-not-found__kicker {
            margin: 0 0 0.35rem;
            font-size: 0.95rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            color: var(--color-text-secondary, #565959);
        }

        .amazon-not-found__title {
            margin: 0 0 0.75rem;
            font-size: clamp(1.45rem, 2.4vw, 1.85rem);
            line-height: 1.25;
            font-weight: 700;
            color: var(--color-text-primary, #0f1111);
        }

        .amazon-not-found__lead,
        .amazon-not-found__hint {
            margin: 0;
            max-width: 42rem;
            line-height: 1.55;
            color: var(--color-text-secondary, #565959);
        }

        .amazon-not-found__hint {
            margin-top: 0.55rem;
            font-size: 0.95rem;
        }

        .amazon-not-found__actions {
            margin-top: 1.15rem;
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            gap: 0.85rem 1.25rem;
        }

        .amazon-not-found__home-link {
            display: inline-flex;
            align-items: center;
            min-height: 2rem;
            padding: 0.35rem 0.95rem;
            border-radius: 999px;
            border: 1px solid var(--color-primary-border, #cc7700);
            background: linear-gradient(180deg, #ffe08a 0%, var(--color-primary, #ff9900) 100%);
            box-shadow: 0 1px 0 rgba(255, 255, 255, 0.4) inset;
            color: var(--color-on-primary, #111);
            font-size: 0.92rem;
            font-weight: 700;
            text-decoration: none;
        }

        .amazon-not-found__home-link:hover {
            background: linear-gradient(180deg, #ffd56b 0%, var(--color-primary-hover, #e68900) 100%);
            color: var(--color-on-primary, #111);
        }

        .amazon-not-found__back-link {
            appearance: none;
            border: none;
            background: transparent;
            padding: 0;
            color: var(--color-link, #007185);
            font: inherit;
            font-size: 0.95rem;
            cursor: pointer;
            text-decoration: none;
        }

        .amazon-not-found__back-link:hover {
            color: var(--color-link-hover, #c45500);
            text-decoration: underline;
        }

        .amazon-not-found__shop {
            background: var(--color-bg-secondary, #eaeded);
            padding: 1.35rem 0 2.5rem;
        }

        .amazon-not-found__shop-panel {
            width: min(var(--weline-layout-content-max-width, 1440px), calc(100% - 2rem));
            margin: 0 auto;
            padding: 1.15rem 1.15rem 1.35rem;
            border: 1px solid var(--color-border-default, #d5d9d9);
            border-radius: 8px;
            background: var(--color-bg-primary, #fff);
            box-shadow: 0 1px 2px rgba(15, 17, 17, 0.06);
        }

        .amazon-not-found__shop-panel .weline-product-recommended {
            margin: 0;
        }

        @media (max-width: 768px) {
            .amazon-not-found__intro-inner {
                grid-template-columns: 1fr;
                padding: 1.75rem 0 2rem;
                text-align: left;
            }

            .amazon-not-found__visual {
                justify-content: flex-start;
            }

            .amazon-not-found__visual svg {
                width: min(72vw, 200px);
            }

            .amazon-not-found__actions {
                flex-direction: column;
                align-items: flex-start;
            }

            .amazon-not-found__shop {
                padding: 1rem 0 2rem;
            }

            .amazon-not-found__shop-panel {
                width: min(100% - 1rem, var(--weline-layout-content-max-width, 1440px));
                padding: 0.85rem 0.75rem 1rem;
            }
        }
    </style>
    <w:hook>seo::head</w:hook>
</head>
<body class="not-found-layout amazon-not-found-layout">
    <w:hook>Weline_Theme::frontend::layouts::base::body-start</w:hook>
    <w:hook>seo::body</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>
    <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>

    <main class="amazon-not-found <?= $esc((string)($meta['class'] ?? '')) ?>"
          id="not-found-main"
          data-layout="not_found"
          data-testid="storefront-not-found-page">
        <section class="amazon-not-found__intro" weline-code="theme.not_found.intro" aria-labelledby="amazon-not-found-title">
            <w:slot id="not-found-content"
                    name="404 内容"
                    accept="layout-not-found-content,content,empty-state"
                    reject="header,footer"
                    multiple="true"
                    position="content"
                    class="amazon-not-found__intro-inner not-found-container">
                <div class="amazon-not-found__visual" aria-hidden="true">
                    <svg viewBox="0 0 240 200" role="img" focusable="false">
                        <rect x="28" y="56" width="118" height="92" rx="10" fill="#ffd699"/>
                        <rect x="38" y="66" width="98" height="72" rx="8" fill="#fffaf0" stroke="#ff9900" stroke-width="3"/>
                        <path d="M170 72c18 0 34 14 34 34s-16 34-34 34h-10V72h10z" fill="#ff9900"/>
                        <circle cx="194" cy="106" r="18" fill="#fffaf0" stroke="#ff9900" stroke-width="3"/>
                        <text x="194" y="113" text-anchor="middle" font-size="20" font-weight="700" fill="#c45500">?</text>
                        <path d="M78 118h38M78 104h48" stroke="#cc7700" stroke-width="4" stroke-linecap="round"/>
                        <ellipse cx="120" cy="168" rx="62" ry="10" fill="#eaeded"/>
                    </svg>
                </div>
                <div class="amazon-not-found__message">
                    <p class="amazon-not-found__kicker"><?= $esc((string)$statusCode) ?> · <?= __('SORRY') ?></p>
                    <h1 class="amazon-not-found__title" id="amazon-not-found-title"><?= $esc($pageTitle) ?></h1>
                    <?php if ($pageLead !== ''): ?>
                        <p class="amazon-not-found__lead"><?= $esc($pageLead) ?></p>
                    <?php endif; ?>
                    <?php if ($pageHint !== ''): ?>
                        <p class="amazon-not-found__hint"><?= $esc($pageHint) ?></p>
                    <?php endif; ?>
                    <div class="amazon-not-found__actions">
                        <a class="amazon-not-found__home-link" href="<?= $esc($homeHref !== '' ? $homeHref : '/') ?>">
                            <?= __('前往首页继续购物') ?>
                        </a>
                        <button type="button" class="amazon-not-found__back-link" data-w-error-back>
                            <?= __('返回上一页') ?>
                        </button>
                    </div>
                </div>
            </w:slot>
        </section>

        <section class="amazon-not-found__shop" weline-code="theme.not_found.shop" aria-label="<?= $esc((string)__('推荐产品')) ?>">
            <div class="amazon-not-found__shop-panel">
                <w:slot id="not-found-recommendations"
                        name="404 推荐产品"
                        accept="layout-not-found-recommendations,recommended-products,featured-products,bestsellers,new-arrivals,you-may-like,product-carousel"
                        multiple="true"
                        position="content"
                        wrapper="section"
                        class="not-found-recommendations"
                        weline-code="theme.not_found.recommendations">
                    <w:hook>Weline_Theme::frontend::layouts::not-found::recommendations</w:hook>
                </w:slot>
            </div>
        </section>
    </main>

    <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default" vars="copyright,links,showBackToTop,socialMedia,showSocialMedia"/>
    <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
    <w:hook>seo::footer</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::not-found::body-end</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::body-end</w:hook>
    <script>
        document.addEventListener('click', function (e) {
            var btn = e.target && e.target.closest && e.target.closest('[data-w-error-back]');
            if (!btn) return;
            if (window.history.length > 1) { window.history.back(); return; }
            window.location.href = <?= $homeJs ?>;
        });
    </script>
</body>
</html>
