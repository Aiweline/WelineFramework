<?php
/**
 * 联系我们页面布局。
 *
 * 布局负责页面框架，并向可视化编辑器暴露可替换插槽。插槽本体内容就是未替换时的默认内容。
 *
 * @meta.name {default="联系我们页面布局",name="联系我们页面布局",description="Theme 自有的联系我们页面布局样本"}
 * @meta.description {default="包含 header、面包屑、联系主视觉、联系方式、表单、常见问题和 footer",name="描述",description="带布局码插槽的灵活联系我们页面布局"}
 * @preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="联系我们页面通常允许访客访问"}
 * @param.title {default="联系我们",name="页面标题",description="联系我们页面标题"}
 * @param.subtitle {default="告诉我们你需要什么，支持团队会将请求分派给合适的处理人。",name="页面副标题",description="联系我们页面引导文案"}
 * @param.contactEmail {default="support@example.com",name="客服邮箱",description="主要客服邮箱"}
 * @param.contactPhone {default="+1 000 000 0000",name="客服电话",description="主要客服电话"}
 * @param.contactHours {default="周一至周五，9:00 - 18:00",name="服务时间",description="客服可用时间"}
 * @param.showHeader {default=true,name="显示 header",description="是否渲染站点头部"}
 * @param.showFooter {default=true,name="显示 footer",description="是否渲染站点底部"}
 * @param.class {default="",name="额外 CSS 类",description="联系我们主内容区域的额外 CSS 类"}
 */
$meta = $this->getData('meta') ?? [];
$escape = static fn($value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
$title = (string)($meta['title'] ?? __('联系我们'));
$subtitle = (string)($meta['subtitle'] ?? __('告诉我们你需要什么，支持团队会将请求分派给合适的处理人。'));
$contactEmail = (string)($meta['contactEmail'] ?? 'support@example.com');
$contactPhone = (string)($meta['contactPhone'] ?? '+1 000 000 0000');
$contactHours = (string)($meta['contactHours'] ?? __('周一至周五，9:00 - 18:00'));
$accountUrl = $this->getUrl('customer/account');
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" data-local="{{lang_local}}" data-lang="{{lang}}">
<head>
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::base::head-after</w:hook>
    <style>
        body.contact-layout {
            margin: 0;
            min-height: 100vh;
            color: var(--weline-layout-text-primary, #172033);
            background: var(--weline-layout-surface-primary, #fff);
        }

        .contact-layout__page {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .contact-layout__breadcrumb {
            border-bottom: 1px solid var(--weline-layout-border-subtle, #e4e8f0);
            background: var(--weline-layout-surface-secondary, #f8fafc);
        }

        .contact-layout__main {
            flex: 1 0 auto;
            width: 100%;
        }

        .contact-layout__inner {
            width: min(1120px, calc(100% - 40px));
            margin: 0 auto;
            padding: 48px 0 72px;
        }

        .contact-layout__hero-grid,
        .contact-layout__body-grid {
            display: grid;
            grid-template-columns: minmax(0, 1.25fr) minmax(280px, .75fr);
            gap: 24px;
            align-items: stretch;
        }

        .contact-layout__body-grid {
            margin-top: 24px;
            align-items: start;
        }

        .contact-layout__section,
        .contact-layout__panel,
        .contact-layout__method,
        .contact-layout__faq-item {
            border: 1px solid var(--weline-layout-border-subtle, #e4e8f0);
            border-radius: 8px;
            background: var(--weline-layout-surface-primary, #fff);
        }

        .contact-layout__section {
            padding: 32px;
        }

        .contact-layout__panel {
            padding: 28px;
            background: var(--weline-layout-surface-secondary, #f8fafc);
        }

        .contact-layout__eyebrow {
            display: inline-flex;
            margin: 0 0 14px;
            font-size: 13px;
            font-weight: 700;
            color: var(--weline-layout-link-color, #0f766e);
        }

        .contact-layout h1,
        .contact-layout h2,
        .contact-layout h3 {
            margin: 0;
            letter-spacing: 0;
            color: var(--weline-layout-text-primary, #172033);
        }

        .contact-layout h1 {
            font-size: 44px;
            line-height: 1.05;
        }

        .contact-layout h2 {
            font-size: 24px;
            line-height: 1.2;
        }

        .contact-layout h3 {
            font-size: 18px;
            line-height: 1.25;
        }

        .contact-layout p {
            margin: 0;
            color: var(--weline-layout-text-secondary, #687286);
            line-height: 1.7;
        }

        .contact-layout__lead {
            max-width: 680px;
            margin-top: 18px;
            font-size: 17px;
        }

        .contact-layout__actions {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            margin-top: 28px;
        }

        .contact-layout__button {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 44px;
            padding: 0 18px;
            border-radius: 6px;
            border: 1px solid var(--weline-layout-border-strong, #cbd5e1);
            text-decoration: none;
            font-weight: 700;
            color: var(--weline-layout-text-primary, #172033);
            background: var(--weline-layout-surface-primary, #fff);
        }

        .contact-layout__button:hover {
            text-decoration: none;
            border-color: var(--weline-layout-link-color, #0f766e);
        }

        .contact-layout__button--primary {
            border-color: var(--weline-layout-link-color, #0f766e);
            background: var(--weline-layout-link-color, #0f766e);
            color: #fff;
        }

        .contact-layout__method-list,
        .contact-layout__faq-list {
            display: grid;
            gap: 14px;
        }

        .contact-layout__method {
            padding: 20px;
        }

        .contact-layout__method p,
        .contact-layout__faq-item p {
            margin-top: 8px;
        }

        .contact-layout__form {
            display: grid;
            gap: 16px;
        }

        .contact-layout__field {
            display: grid;
            gap: 7px;
        }

        .contact-layout__field label {
            font-size: 14px;
            font-weight: 700;
            color: var(--weline-layout-text-primary, #172033);
        }

        .contact-layout__field input,
        .contact-layout__field textarea {
            width: 100%;
            min-height: 44px;
            border: 1px solid var(--weline-layout-border-subtle, #d8dee9);
            border-radius: 6px;
            padding: 10px 12px;
            color: var(--weline-layout-text-primary, #172033);
            background: #fff;
            box-sizing: border-box;
        }

        .contact-layout__field textarea {
            min-height: 132px;
            resize: vertical;
        }

        .contact-layout__faq-item {
            padding: 20px;
        }

        .contact-layout__side-stack {
            display: grid;
            gap: 24px;
        }

        @media (max-width: 860px) {
            .contact-layout__inner {
                width: min(100% - 28px, 1120px);
                padding: 32px 0 56px;
            }

            .contact-layout__hero-grid,
            .contact-layout__body-grid {
                grid-template-columns: 1fr;
            }

            .contact-layout__section,
            .contact-layout__panel {
                padding: 24px;
            }

            .contact-layout h1 {
                font-size: 34px;
            }
        }
    </style>
</head>
<body class="contact-layout">
    <w:hook>Weline_Theme::frontend::layouts::base::body-start</w:hook>

    <div class="contact-layout__page">
        <if condition="meta.showHeader">
            <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>
            <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>
        </if>

        <div class="contact-layout__breadcrumb">
            <w:hook>Weline_Theme::frontend::layouts::base::breadcrumb-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="frontend" type="breadcrumb" default-option="default"/>
            <w:hook>Weline_Theme::frontend::layouts::base::breadcrumb-after</w:hook>
        </div>

        <main class="contact-layout__main <?= $escape($meta['class'] ?? '') ?>" id="main-content" data-layout="contact">
            <w:slot id="content"
                    name="内容区"
                    accept="layout-contact-content,layout-default-content,content,contact-content,contact-form,contact-methods,contact-faq,contact-help,customer-service"
                    reject="header,footer,navigation,product,category,cart,checkout"
                    multiple="true"
                    position="content"
                    class="contact-layout__content-root-slot">
            <div class="contact-layout__inner">
                <div class="contact-layout__hero-grid">
                    <w:slot id="contact-hero"
                            name="联系页主视觉"
                            accept="layout-contact-hero,layout-contact-content,layout-default-content,banner,content,cta"
                            reject="header,footer,navigation,product,category,cart,checkout"
                            multiple="false"
                            position="hero"
                            class="contact-layout__hero-slot">
                        <section class="contact-layout__section" aria-labelledby="contact-layout-title">
                            <span class="contact-layout__eyebrow"><?= __('客户支持') ?></span>
                            <h1 id="contact-layout-title"><?= $escape($title) ?></h1>
                            <p class="contact-layout__lead"><?= $escape($subtitle) ?></p>
                            <div class="contact-layout__actions">
                                <a class="contact-layout__button contact-layout__button--primary" href="javascript:void(0)" data-contact-chat>
                                    <?= __('打开在线客服') ?>
                                </a>
                                <a class="contact-layout__button" href="<?= $escape($accountUrl) ?>">
                                    <?= __('查看我的账户') ?>
                                </a>
                            </div>
                        </section>
                    </w:slot>

                    <w:slot id="contact-help-panel"
                            name="联系页帮助面板"
                            accept="layout-contact-help,layout-contact-sidebar,layout-contact-content,content,cta,trust"
                            reject="header,footer,navigation,product,category,cart,checkout"
                            multiple="false"
                            position="sidebar"
                            class="contact-layout__help-slot">
                        <aside class="contact-layout__panel">
                            <h2><?= __('需要订单支持？') ?></h2>
                            <p style="margin-top: 12px;"><?= __('请附上订单号、邮箱和简短问题描述，方便团队快速分派处理。') ?></p>
                            <div class="contact-layout__actions">
                                <a class="contact-layout__button contact-layout__button--primary" href="<?= $escape($accountUrl) ?>">
                                    <?= __('进入支持中心') ?>
                                </a>
                            </div>
                        </aside>
                    </w:slot>
                </div>

                <div class="contact-layout__body-grid">
                    <div class="contact-layout__side-stack">
                        <w:slot id="contact-methods"
                                name="联系方式"
                                accept="layout-contact-methods,layout-contact-sidebar,layout-contact-content,content,social,trust"
                                reject="header,footer,navigation,product,category,cart,checkout"
                                multiple="true"
                                position="sidebar"
                                class="contact-layout__methods-slot">
                            <section class="contact-layout__panel" aria-labelledby="contact-methods-title">
                                <h2 id="contact-methods-title"><?= __('联系方式') ?></h2>
                                <div class="contact-layout__method-list" style="margin-top: 18px;">
                                    <article class="contact-layout__method">
                                        <h3><?= __('邮箱') ?></h3>
                                        <p><?= $escape($contactEmail) ?></p>
                                    </article>
                                    <article class="contact-layout__method">
                                        <h3><?= __('电话') ?></h3>
                                        <p><?= $escape($contactPhone) ?></p>
                                    </article>
                                    <article class="contact-layout__method">
                                        <h3><?= __('服务时间') ?></h3>
                                        <p><?= $escape($contactHours) ?></p>
                                    </article>
                                </div>
                            </section>
                        </w:slot>

                        <w:slot id="contact-faq"
                                name="联系页常见问题"
                                accept="layout-contact-faq,layout-contact-sidebar,layout-contact-content,faq,content"
                                reject="header,footer,navigation,product,category,cart,checkout"
                                multiple="true"
                                position="sidebar"
                                class="contact-layout__faq-slot">
                            <section class="contact-layout__panel" aria-labelledby="contact-faq-title">
                                <h2 id="contact-faq-title"><?= __('提交前请确认') ?></h2>
                                <div class="contact-layout__faq-list" style="margin-top: 18px;">
                                    <article class="contact-layout__faq-item">
                                        <h3><?= __('订单问题') ?></h3>
                                        <p><?= __('请提供订单号和下单时使用的邮箱。') ?></p>
                                    </article>
                                    <article class="contact-layout__faq-item">
                                        <h3><?= __('退换货') ?></h3>
                                        <p><?= __('商品照片和物流信息可以帮助团队更快处理退换货请求。') ?></p>
                                    </article>
                                </div>
                            </section>
                        </w:slot>
                    </div>

                    <w:slot id="contact-form"
                            name="联系表单"
                            accept="layout-contact-form,layout-contact-content,layout-default-content,form,content,customer-service"
                            reject="header,footer,navigation,product,category,cart,checkout"
                            multiple="false"
                            position="content"
                            class="contact-layout__form-slot">
                        <section class="contact-layout__section" aria-labelledby="contact-form-title">
                            <h2 id="contact-form-title"><?= __('发送消息') ?></h2>
                            <p style="margin-top: 10px;"><?= __('请尽量提供完整背景，减少客服再次确认基础信息的等待时间。') ?></p>
                            <form class="contact-layout__form" action="/contact" method="post" style="margin-top: 22px;">
                                <div class="contact-layout__field">
                                    <label for="contact-name"><?= __('姓名') ?></label>
                                    <input id="contact-name" name="name" type="text" autocomplete="name">
                                </div>
                                <div class="contact-layout__field">
                                    <label for="contact-email"><?= __('邮箱') ?></label>
                                    <input id="contact-email" name="email" type="email" autocomplete="email">
                                </div>
                                <div class="contact-layout__field">
                                    <label for="contact-message"><?= __('消息内容') ?></label>
                                    <textarea id="contact-message" name="message"></textarea>
                                </div>
                                <button class="contact-layout__button contact-layout__button--primary" type="submit">
                                    <?= __('提交请求') ?>
                                </button>
                            </form>
                        </section>
                    </w:slot>
                </div>
            </div>
            </w:slot>
        </main>

        <if condition="meta.showFooter">
            <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default" vars="copyright,links,showBackToTop,socialMedia,showSocialMedia"/>
            <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
        </if>
    </div>

    <script>
    document.addEventListener('click', function (event) {
        var trigger = event.target.closest('[data-contact-chat]');
        if (!trigger) {
            return;
        }

        event.preventDefault();
        if (typeof CustomerServiceWidget !== 'undefined' && typeof CustomerServiceWidget.toggleChat === 'function') {
            CustomerServiceWidget.toggleChat();
            return;
        }

        var widgetButton = document.getElementById('cs-chat-button');
        if (widgetButton) {
            widgetButton.click();
        }
    });
    </script>

    <w:hook>Weline_Theme::frontend::layouts::base::body-end</w:hook>
</body>
</html>
