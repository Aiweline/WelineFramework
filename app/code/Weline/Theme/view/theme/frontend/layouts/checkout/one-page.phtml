<?php
/**
 * 布局：结账页 - 单页布局
 *
 * 单页结账布局，所有步骤在一个页面完成
 * 字段
 * @meta.name {default="结账页单页布局",name="结账页单页布局",description="结账页单页布局，用于描述布局的名称"}
 * @meta.description {default="单页结账布局，所有步骤在一个页面完成",name="单页结账布局，所有步骤在一个页面完成",description="单页结账布局，所有步骤在一个页面完成，用于描述布局的用途"}
 * @preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="是否需要登录，0不需要登录，1需要登录，这个不是元素据，是预览系统判断是否需要自动登录的。"}
 * @param title {default="结账",name="页面标题",description="页面标题，用于设置页面标题"}
 * @param content {default="",name="主要内容（HTML字符串）",description="主要内容（HTML字符串），用于设置页面内容"}
 * @param orderSummary {default="",name="订单摘要（HTML字符串）",description="订单摘要（HTML字符串），用于设置订单摘要区域"}
 * @param breadcrumb {default="",name="面包屑导航（可选）",description="面包屑导航（可选），用于设置面包屑导航"}
 * @param class {default="",name="额外CSS类",description="额外CSS类，用于设置页面额外CSS类"}
 */
?>
<?php
$title = $this->getData('title') ?? __('结账');
$content = $this->getData('content') ?? '';
$orderSummary = $this->getData('orderSummary') ?? '';
$breadcrumb = $this->getData('breadcrumb') ?? '';
$class = $this->getData('class') ?? '';

// 设置页面标题
$this->assign('title', $title);
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" dir="{{htmlDir}}" data-local="{{lang_local}}" data-lang="{{lang}}" data-dir="{{htmlDir}}">
<head>
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::default::head-after</w:hook>
    <style>
        body.checkout-one-page-layout {
            margin: 0;
            background: var(--weline-layout-surface-secondary);
            color: var(--weline-layout-text-primary);
        }

        .checkout-main.one-page {
            width: min(1180px, calc(100% - 40px));
            margin: 0 auto;
            padding: var(--weline-layout-spacing-3xl) 0 calc(var(--weline-layout-spacing-3xl) * 1.125);
        }

        .checkout-content-wrapper {
            display: grid;
            grid-template-columns: minmax(0, 1fr) minmax(280px, 360px);
            gap: var(--weline-layout-spacing-lg);
            align-items: start;
        }

        .checkout-form,
        .checkout-summary {
            border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--weline-theme-border-color-subtle);
            border-radius: var(--weline-layout-radius-lg);
            background: var(--weline-layout-surface-primary);
            padding: var(--weline-layout-spacing-lg);
        }

        .checkout-form {
            min-height: 420px;
        }

        .checkout-summary.sticky {
            position: sticky;
            top: var(--weline-layout-spacing-lg);
        }

        .checkout-one-page__placeholder h1,
        .checkout-one-page__summary-placeholder h2 {
            margin: 0;
            line-height: var(--weline-layout-line-height-tight);
            color: var(--weline-layout-text-primary);
        }

        .checkout-one-page__placeholder h1 {
            font-size: var(--weline-layout-font-size-4xl);
        }

        .checkout-one-page__placeholder p,
        .checkout-one-page__summary-placeholder p {
            margin: var(--weline-layout-spacing-sm) 0 0;
            color: var(--weline-layout-text-secondary);
            line-height: var(--weline-layout-line-height-relaxed);
        }

        .checkout-one-page__steps {
            display: grid;
            gap: var(--weline-layout-spacing-sm);
            margin-top: var(--weline-layout-spacing-lg);
        }

        .checkout-one-page__step {
            border: var(--weline-theme-border-width) var(--border-style-dashed) var(--weline-theme-border-color-strong);
            border-radius: var(--weline-layout-radius-lg);
            padding: var(--weline-layout-spacing-lg);
            color: var(--weline-layout-text-secondary);
        }

        .checkout-one-page__summary-line {
            display: flex;
            justify-content: space-between;
            gap: var(--weline-layout-spacing-sm);
            padding: var(--weline-layout-spacing-sm) 0;
            border-bottom: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--weline-theme-border-color-subtle);
            color: var(--weline-layout-text-secondary);
        }

        @media (max-width: 900px) {
            .checkout-main.one-page {
                width: min(100% - 28px, 1180px);
                padding: 28px 0 56px;
            }

            .checkout-content-wrapper {
                grid-template-columns: 1fr;
            }

            .checkout-summary.sticky {
                position: static;
            }
        }
    </style>
    <w:hook>seo::head</w:hook>
</head>
<body class="checkout-one-page-layout">
    <w:hook>seo::body</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>
    <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>
    
    <?php if ($breadcrumb): ?>
        <nav class="breadcrumb-nav">
            {{breadcrumb}}
        </nav>
    <?php endif; ?>
    
    <main class="checkout-main one-page <?= htmlspecialchars($class ?? '') ?>" id="checkout-main" data-layout="checkout_one_page">
        <w:slot id="content"
                name="内容区"
                accept="layout-checkout-one-page-content,layout-checkout-content,layout-default-content,content,checkout-content,checkout-form,order-summary,trust-badges"
                reject="header,footer"
                multiple="true"
                position="content"
                class="checkout-main__content-root-slot">
        <div class="checkout-container">
            <div class="checkout-content-wrapper">
                <w:slot id="checkout-one-page-content"
                        name="单页结算表单"
                        accept="layout-checkout-one-page-content,layout-checkout-content,checkout-content,content"
                        reject="header,footer"
                        multiple="true"
                        position="content"
                        class="checkout-form">
                    <?php if ($content): ?>
                        {{content}}
                    <?php else: ?>
                        <div class="checkout-one-page__placeholder">
                            <h1><?= htmlspecialchars($title, ENT_QUOTES, 'UTF-8') ?></h1>
                            <p><?= __('这里是单页结账的表单区域。接入结账模块后，会替换为真实地址、配送、支付和确认步骤。') ?></p>
                            <div class="checkout-one-page__steps" aria-hidden="true">
                                <div class="checkout-one-page__step"><?= __('联系信息与收货地址') ?></div>
                                <div class="checkout-one-page__step"><?= __('配送方式') ?></div>
                                <div class="checkout-one-page__step"><?= __('支付与订单确认') ?></div>
                            </div>
                        </div>
                    <?php endif; ?>
                </w:slot>
                
                <w:slot id="checkout-one-page-summary"
                            name="单页结算摘要"
                            accept="layout-checkout-one-page-summary,order-summary,checkout-summary"
                            exclusive="true"
                            multiple="false"
                            position="sidebar"
                            wrapper="aside"
                            class="checkout-summary sticky">
                    <?php if ($orderSummary): ?>
                        {{orderSummary}}
                    <?php else: ?>
                        <div class="checkout-one-page__summary-placeholder">
                            <h2><?= __('订单摘要') ?></h2>
                            <p><?= __('摘要 slot，可由结账模块写入真实金额、优惠和配送信息。') ?></p>
                            <div class="checkout-one-page__summary-line"><span><?= __('商品') ?></span><span>--</span></div>
                            <div class="checkout-one-page__summary-line"><span><?= __('配送') ?></span><span>--</span></div>
                            <div class="checkout-one-page__summary-line"><strong><?= __('合计') ?></strong><strong>--</strong></div>
                        </div>
                    <?php endif; ?>
                </w:slot>
            </div>
        </div>
        </w:slot>
    </main>
    
    <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default" vars="copyright,links,showBackToTop,socialMedia,showSocialMedia"/>
    <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
    <w:hook>seo::footer</w:hook>
</body>
</html>

