<?php
/**
 * 测试布局：用于验证CSS/JS提取功能
 * 
 * 此布局文件包含内联CSS和JS，用于测试LayoutAssetsExtractor功能
 */
$layoutType = $this->getData('layoutType') ?? 'test';
$layoutOption = $this->getData('layoutOption') ?? 'assets-test';

// 确保布局信息被设置
$this->setData('layoutType', $layoutType);
$this->setData('layoutOption', $layoutOption);
?>
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS/JS提取功能测试</title>
    
    <!-- 内联CSS - 应该被提取到外部文件 -->
    <style>
        .test-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: var(--weline-layout-spacing-lg);
            background: var(--weline-layout-surface-primary);
        }
        .test-header {
            color: var(--weline-theme-primary);
            font-size: var(--weline-layout-font-size-2xl);
            margin-bottom: var(--weline-layout-spacing-lg);
        }
        .test-content {
            padding: var(--weline-layout-spacing-md);
            border: var(--weline-theme-border-width) var(--weline-theme-border-style) var(--weline-theme-border-color);
            border-radius: var(--weline-layout-radius-md);
        }
    </style>
    <w:hook>seo::head</w:hook>
</head>
<body>
    <w:hook>seo::body</w:hook>
    <div class="test-container">
        <w:slot id="content"
                name="内容区"
                accept="layout-test-assets-content,layout-default-content,content"
                reject="header,footer"
                multiple="true"
                position="content"
                class="test-assets__content-root-slot">
            <w:slot id="test-assets-content"
                name="资源提取测试内容"
                accept="layout-test-assets-content,layout-default-content,content"
                reject="header,footer"
                multiple="true"
                position="content">
            <h1 class="test-header">CSS/JS提取功能测试</h1>
            <div class="test-content">
                <p>这是一个测试页面，用于验证CSS/JS提取功能是否正常工作。</p>
                <button id="test-button">点击测试</button>
            </div>
            </w:slot>
        </w:slot>
    </div>
    
    <w:hook>seo::footer</w:hook>
</body>
</html>

