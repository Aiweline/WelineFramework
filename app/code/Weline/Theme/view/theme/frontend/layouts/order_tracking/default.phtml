<?php
/**
 * 订单跟踪默认布局
 *
 * 提供订单号/联系方式查询表单骨架，并预留可扩展 slot。
 * 真实物流查询可由订单/物流模块通过 hook 或部件接入。
 *
 * @meta.name {default="订单跟踪",name="订单跟踪",description="订单跟踪默认布局"}
 * @meta.description {default="访客输入订单号查询物流状态，并提供帮助扩展槽",name="描述",description="订单跟踪页面布局"}
 * @preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="订单跟踪通常允许访客访问"}
 * @param.title {default="订单跟踪",name="页面标题",description="订单跟踪标题"}
 * @param.subtitle {default="输入订单号与下单邮箱/手机号，查询最新物流状态。",name="副标题",description="引导文案"}
 * @param.showHeader {default=true,name="显示 header",description="是否渲染站点头部"}
 * @param.showFooter {default=true,name="显示 footer",description="是否渲染站点底部"}
 * @param.class {default="",name="额外 CSS 类",description="主内容额外 CSS 类"}
 */
use Weline\Framework\Manager\ObjectManager;
use Weline\Theme\Helper\SiteContactInfo;

$meta = $this->getData('meta') ?? [];
$escape = static fn($value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');

$contact = [
    'site_name' => 'Weline',
    'contact_email' => 'support@example.com',
    'contact_phone' => '',
    'service_hours' => (string)__('周一至周五 9:00 - 18:00（法定节假日除外）'),
];
try {
    $resolver = ObjectManager::getInstance(SiteContactInfo::class);
    if ($resolver instanceof SiteContactInfo) {
        $contact = array_merge($contact, $resolver->resolve());
    }
} catch (\Throwable) {
}

$title = (string)($meta['title'] ?? __('订单跟踪'));
$subtitle = (string)($meta['subtitle'] ?? __('输入订单号与下单邮箱/手机号，查询最新物流状态。'));
$email = (string)$contact['contact_email'];
$phone = (string)$contact['contact_phone'];
$hours = (string)$contact['service_hours'];
$helpUrl = '/help';
$accountOrdersUrl = '/account/orders';
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" dir="{{htmlDir}}" data-local="{{lang_local}}" data-lang="{{lang}}" data-dir="{{htmlDir}}">
<head>
    <w:hook>Weline_Theme::frontend::layouts::base::head-before</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::frontend::layouts::base::head-after</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::order-tracking::head-after</w:hook>
    <style>
        body.order-tracking-layout { margin: 0; min-height: 100vh; color: var(--weline-layout-text-primary); background: var(--weline-layout-surface-primary); }
        .order-tracking__inner { width: min(var(--weline-layout-content-max-width), calc(100% - (var(--weline-layout-content-padding-inline) * 2))); margin: 0 auto; padding: var(--size-control-md) 0 var(--token-size-64px); }
        .order-tracking__hero { display: grid; gap: var(--spacing-2_5); margin-bottom: var(--spacing-lg); }
        .order-tracking__hero h1 { margin: 0; font-size: clamp(var(--font-size-3xl), 4vw, var(--token-size-38px)); line-height: 1.15; }
        .order-tracking__hero p { margin: 0; color: var(--weline-layout-text-secondary); line-height: 1.7; }
        .order-tracking__panel { padding: var(--spacing-lg); border: var(--border-width-thin) solid var(--color-border-slate); border-radius: var(--border-radius-xl); background: var(--color-bg-primary); }
        .order-tracking__panel > h2 { margin: 0 0 var(--weline-space-3); font-size: var(--font-size-lg); }
        .w-sr-only {
            position: absolute; width: var(--spacing-px); height: var(--spacing-px); padding: 0; margin: var(--spacing-neg-px);
            overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;
        }
        .order-tracking__form { display: grid; gap: var(--spacing-3_5); }
        .order-tracking__field { display: grid; gap: var(--spacing-1_5); }
        .order-tracking__field label { font-size: var(--font-size-sm); font-weight: 700; }
        .order-tracking__field input { min-height: var(--size-control-lg); border: var(--border-width-thin) solid var(--color-border-panel-2); border-radius: var(--border-radius-lg); padding: 0 var(--weline-space-3); }
        .order-tracking__actions { display: flex; flex-wrap: wrap; gap: var(--spacing-2_5); margin-top: var(--spacing-xs); }
        .order-tracking__button { display: inline-flex; align-items: center; justify-content: center; min-height: var(--size-control-lg); padding: 0 var(--spacing-md); border-radius: var(--border-radius-lg); border: var(--border-width-thin) solid var(--color-text-light-secondary); background: var(--color-bg-primary); color: inherit; text-decoration: none; font-weight: 700; cursor: pointer; }
        .order-tracking__button--primary { border-color: var(--weline-layout-link-color); background: var(--weline-layout-link-color); color: var(--color-bg-primary); }
        .order-tracking__tips { margin-top: var(--spacing-4_5); color: var(--weline-layout-text-secondary); line-height: 1.6; font-size: var(--font-size-sm); }
        .order-tracking__result { margin-top: var(--weline-space-5); min-height: var(--size-control-xl); }
        .order-tracking__side-grid { display: grid; grid-template-columns: 1.4fr .8fr; gap: var(--weline-space-5); margin-top: var(--spacing-lg); align-items: start; }
        .order-tracking__card { padding: var(--spacing-4_5); border: var(--border-width-thin) solid var(--color-border-slate); border-radius: var(--border-radius-xl); background: var(--color-bg-slate); }
        .order-tracking__card h3 { margin: 0 0 var(--spacing-2_5); font-size: var(--font-size-base); }
        .order-tracking__card p, .order-tracking__card a { margin: 0 0 var(--spacing-sm); color: var(--weline-layout-text-secondary); text-decoration: none; line-height: 1.6; }
        .order-tracking__card a:hover { color: var(--weline-layout-link-color); text-decoration: underline; }
        .order-tracking__extras { margin-top: var(--spacing-lg); display: grid; gap: var(--spacing-md); }
        @media (max-width: 820px) {
            .order-tracking__side-grid { grid-template-columns: 1fr; }
        }
    </style>
    <w:hook>seo::head</w:hook>
</head>
<body class="order-tracking-layout <?= $escape($meta['class'] ?? '') ?>">
    <w:hook>seo::body</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::body-start</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::order-tracking::body-start</w:hook>

    <div class="weline-page-wrapper">
        <if condition="meta.showHeader">
            <w:hook>Weline_Theme::frontend::layouts::base::header-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="frontend" type="header" default-option="default" vars="logo,logoText,navItems,showSearch,showUserMenu"/>
            <w:hook>Weline_Theme::frontend::layouts::base::header-after</w:hook>
        </if>

        <main class="order-tracking__main" id="order-tracking-main" data-layout="order_tracking">
            <div class="order-tracking__inner">
                <header class="order-tracking__hero">
                    <h1><?= $escape($title) ?></h1>
                    <p><?= $escape($subtitle) ?></p>
                </header>

                <w:slot id="order-tracking-form"
                        name="订单跟踪表单"
                        accept="layout-order-tracking-form,order-tracking-form,content"
                        reject="header,footer,logo,navigation,product"
                        multiple="true"
                        append="true"
                        max="6"
                        position="content"
                        class="order-tracking__form-slot">
                    <section class="order-tracking__panel" aria-labelledby="order-tracking-form-title" weline-code="theme.order_tracking.form">
                        <h2 id="order-tracking-form-title" class="w-sr-only"><?= $escape(__('查询表单')) ?></h2>
                        <w:form action="/orders/track" method="get" class="order-tracking__form" id="order-tracking-form">
                            <div class="order-tracking__field">
                                <label for="order_number"><?= $escape(__('订单号')) ?></label>
                                <input id="order_number" name="order_number" type="text" required autocomplete="off" placeholder="<?= $escape(__('例如：WL202608240001')) ?>">
                            </div>
                            <div class="order-tracking__field">
                                <label for="contact"><?= $escape(__('下单邮箱或手机号')) ?></label>
                                <input id="contact" name="contact" type="text" required autocomplete="email" placeholder="<?= $escape(__('用于核验订单归属')) ?>">
                            </div>
                            <div class="order-tracking__actions">
                                <button type="submit" class="order-tracking__button order-tracking__button--primary"><?= $escape(__('查询物流')) ?></button>
                                <a class="order-tracking__button" href="<?= $escape($accountOrdersUrl) ?>"><?= $escape(__('登录后查看我的订单')) ?></a>
                            </div>
                        </w:form>
                        <p class="order-tracking__tips">
                            <?= $escape(__('提示：若已登录账户，可直接前往「我的订单」查看全部订单与物流详情。真实物流轨迹可由订单/物流模块通过本页 hook 或部件接入。')) ?>
                        </p>
                        <div class="order-tracking__result" id="order-tracking-result" aria-live="polite">
                            <w:hook>Weline_Theme::frontend::layouts::order-tracking::result</w:hook>
                        </div>
                    </section>
                </w:slot>

                <div class="order-tracking__side-grid">
                    <w:slot id="order-tracking-content"
                            name="订单跟踪内容"
                            accept="layout-order-tracking-content,content,faq,notice,banner"
                            reject="header,footer,logo,navigation,product"
                            multiple="true"
                            append="true"
                            max="12"
                            position="content"
                            class="order-tracking__content-slot">
                        <section class="order-tracking__panel" weline-code="theme.order_tracking.guide">
                            <h2><?= $escape(__('查询说明')) ?></h2>
                            <p class="order-tracking__tips" style="margin-top: 0;">
                                <?= $escape(__('请使用下单时填写的联系方式核验。若长时间无物流更新，请先确认是否周末/节假日，或前往帮助中心联系客服。')) ?>
                            </p>
                        </section>
                    </w:slot>

                    <aside>
                        <w:slot id="order-tracking-sidebar"
                                name="订单跟踪侧栏"
                                accept="layout-order-tracking-sidebar,content,notice,faq,contact"
                                reject="header,footer,logo,navigation,product"
                                multiple="true"
                                append="true"
                                max="10"
                                position="content"
                                class="order-tracking__sidebar-slot">
                            <div class="order-tracking__card">
                                <h3><?= $escape(__('需要帮助？')) ?></h3>
                                <p><a href="<?= $escape($helpUrl) ?>"><?= $escape(__('前往帮助中心')) ?></a></p>
                                <?php if ($email !== ''): ?>
                                    <p><a href="mailto:<?= $escape($email) ?>"><?= $escape($email) ?></a></p>
                                <?php endif; ?>
                                <?php if ($phone !== ''): ?>
                                    <p><a href="tel:<?= $escape(preg_replace('/\s+/', '', $phone) ?? $phone) ?>"><?= $escape($phone) ?></a></p>
                                <?php endif; ?>
                                <?php if ($hours !== ''): ?>
                                    <p><?= $escape($hours) ?></p>
                                <?php endif; ?>
                            </div>
                        </w:slot>
                    </aside>
                </div>

                <w:slot id="order-tracking-extras"
                        name="订单跟踪扩展区"
                        accept="layout-order-tracking-extras,content,faq,notice,banner,promo-banner,trust-badges"
                        reject="header,footer,logo,navigation,product"
                        multiple="true"
                        append="true"
                        max="16"
                        position="content"
                        class="order-tracking__extras">
                    <w:hook>Weline_Theme::frontend::layouts::order-tracking::extras</w:hook>
                </w:slot>
            </div>
        </main>

        <if condition="meta.showFooter">
            <w:hook>Weline_Theme::frontend::layouts::base::footer-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="frontend" type="footer" default-option="default" vars="copyright,links,showBackToTop,socialMedia,showSocialMedia"/>
            <w:hook>Weline_Theme::frontend::layouts::base::footer-after</w:hook>
        </if>
    </div>

    <w:hook>Weline_Theme::frontend::layouts::order-tracking::body-end</w:hook>
    <w:hook>Weline_Theme::frontend::layouts::base::body-end</w:hook>
    <w:hook>seo::footer</w:hook>
</body>
</html>
