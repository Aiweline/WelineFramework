<?php
/**
 * 布局：购物车 - 默认布局
 * 
 * 购物车页面默认布局，包含商品列表、价格汇总等
 * 字段
 * @meta.name {default="购物车默认布局",name="购物车默认布局",description="购物车默认布局，用于描述布局的名称"}
 * @meta.description {default="购物车页面默认布局，包含商品列表、价格汇总等",name="购物车页面默认布局，包含商品列表、价格汇总等",description="购物车页面默认布局，包含商品列表、价格汇总等，用于描述布局的用途"}
 * @preview.login {default=0,option={0:不需要登录,1:需要登录},name="是否需要登录",description="是否需要登录，0不需要登录，1需要登录，这个不是元素据，是预览系统判断是否需要自动登录的。"}
 * @param title {default="购物车",name="页面标题",description="页面标题，用于设置页面标题"}
 * @param content {default="",name="主要内容（HTML字符串）",description="主要内容（HTML字符串），用于设置页面内容"}
 * @param summary {default="",name="价格汇总区域（HTML字符串）",description="价格汇总区域（HTML字符串），用于设置价格汇总区域"}
 * @param breadcrumb {default="",name="面包屑导航（可选）",description="面包屑导航（可选），用于设置面包屑导航"}
 * @param continueShopping {default="",name="继续购物链接（可选）",description="继续购物链接（可选），用于设置继续购物链接"}
 * @param class {default="",name="额外CSS类",description="额外CSS类，用于设置页面额外CSS类"}
 */
$title = $this->getData('title') ?? __('购物车');
$content = $this->getData('content') ?? '';
$summary = $this->getData('summary') ?? '';
$breadcrumb = $this->getData('breadcrumb') ?? '';
$continueShopping = $this->getData('continueShopping') ?? '';
$class = $this->getData('class') ?? '';

// 设置页面标题
$this->assign('title', $title);
?>
<!DOCTYPE html>
<html lang="<?= str_replace('_', '-', $_SERVER['WELINE_USER_LANG'] ?? 'zh_Hans_CN') ?>">
<head>
    @template(Weline_Frontend::templates/public/head.phtml)
</head>
<body>
    @template(Weline_Frontend::templates/public/header.phtml)
    
    <?php if ($breadcrumb): ?>
        <nav class="breadcrumb-nav">
            <?= $breadcrumb ?>
        </nav>
    <?php endif; ?>
    
    <main class="cart-main <?= htmlspecialchars($class) ?>">
        <div class="cart-container">
            <div class="cart-header">
                <h1 class="cart-title"><?= __('购物车') ?></h1>
                <?php if ($continueShopping): ?>
                    <a href="<?= htmlspecialchars($continueShopping) ?>" class="continue-shopping-link">
                        <?= __('继续购物') ?>
                    </a>
                <?php endif; ?>
            </div>
            
            <div class="cart-content-wrapper">
                <div class="cart-items">
                    <?= $content ?>
                </div>
                
                <?php if ($summary): ?>
                    <aside class="cart-summary">
                        <?= $summary ?>
                    </aside>
                <?php endif; ?>
            </div>
        </div>
    </main>
    
    @template(Weline_Frontend::templates/public/footer.phtml)
</body>
</html>

