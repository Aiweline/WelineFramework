<?php
/**
 * 片段：产品网格
 *
 * 电商产品网格展示组件，支持网格/列表视图切换
 *
 * @meta.name {default="产品网格",name="产品网格",description="电商产品网格展示组件"}
 * @meta.description {default="电商产品网格展示组件，支持网格视图和列表视图，可配置每行列数"}
 * @param products {default=[],name="产品列表",description="产品数据数组，每个产品包含 id、name、url、image、price、original_price、rating、review_count、is_new、is_sale、stock 等字段"}
 * @param columns {default=4,name="每行列数",description="桌面端每行显示的产品数量"}
 * @param columnsTablet {default=3,name="平板每行列数",description="平板端每行显示的产品数量"}
 * @param columnsMobile {default=2,name="移动端每行列数",description="移动端每行显示的产品数量"}
 * @param viewMode {default="grid",name="默认视图",description="默认视图模式：grid 或 list"}
 * @param showPrice {default=true,type=bool,name="显示价格",description="是否显示产品价格"}
 * @param showRating {default=true,type=bool,name="显示评分",description="是否显示产品评分"}
 * @param showAddToCart {default=true,type=bool,name="显示加购按钮",description="是否显示加购按钮"}
 * @param showWishlist {default=true,type=bool,name="显示收藏按钮",description="是否显示收藏按钮"}
 * @param showCompare {default=true,type=bool,name="显示对比按钮",description="是否显示对比按钮"}
 * @param showQuickview {default=true,type=bool,name="显示快速查看",description="是否显示快速查看按钮"}
 * @param class {default="",name="额外CSS类",description="额外的CSS类名"}
 */

$metaParams = \Weline\Theme\Helper\ThemeData::getFileParams('partials.product.grid');
foreach ($metaParams as $key => $value) {
    $this->setData("meta.{$key}", $value);
}

$products = $this->getData('meta.products') ?? [];
$columns = (int)($this->getData('meta.columns') ?? 4);
$columnsTablet = (int)($this->getData('meta.columnsTablet') ?? 3);
$columnsMobile = (int)($this->getData('meta.columnsMobile') ?? 2);
$viewMode = $this->getData('meta.viewMode') ?? 'grid';
$showPrice = (bool)($this->getData('meta.showPrice') ?? true);
$showRating = (bool)($this->getData('meta.showRating') ?? true);
$showAddToCart = (bool)($this->getData('meta.showAddToCart') ?? true);
$showWishlist = (bool)($this->getData('meta.showWishlist') ?? true);
$showCompare = (bool)($this->getData('meta.showCompare') ?? true);
$showQuickview = (bool)($this->getData('meta.showQuickview') ?? true);
$class = $this->getData('meta.class') ?? '';

$uniqueId = 'product-grid-' . uniqid();
?>
<?php if (!empty($products)): ?>
<div class="product-grid-partial<?= $class ? ' ' . htmlspecialchars($class) : '' ?>"
     id="<?= $uniqueId ?>"
     data-view-mode="<?= htmlspecialchars($viewMode) ?>"
     data-columns="<?= $columns ?>"
     data-columns-tablet="<?= $columnsTablet ?>"
     data-columns-mobile="<?= $columnsMobile ?>">
    <div class="product-grid-partial__grid">
        <?php foreach ($products as $product): ?>
            <?php
            $productId = (int)($product['id'] ?? $product['product_id'] ?? 0);
            $productName = htmlspecialchars((string)($product['name'] ?? ''));
            $productUrl = htmlspecialchars((string)($product['url'] ?? '#'));
            $productImage = htmlspecialchars((string)($product['image'] ?? ''));
            $productPrice = (float)($product['price'] ?? 0);
            $productOriginalPrice = (float)($product['original_price'] ?? $product['price'] ?? 0);
            $productRating = (float)($product['rating'] ?? 0);
            $productReviewCount = (int)($product['review_count'] ?? 0);
            $isNew = (bool)($product['is_new'] ?? false);
            $isSale = (bool)($product['is_sale'] ?? false);
            $isInStock = (bool)($product['in_stock'] ?? ((int)($product['stock'] ?? 0) > 0));
            $productSku = htmlspecialchars((string)($product['sku'] ?? ''));
            $shortDescription = htmlspecialchars((string)($product['short_description'] ?? ''));
            ?>
            <article class="product-grid-partial__item product-card" data-product-id="<?= $productId ?>">
                <div class="product-grid-partial__card">
                    <div class="product-grid-partial__media">
                        <a href="<?= $productUrl ?>" class="product-grid-partial__image-link">
                            <div class="product-grid-partial__image-wrapper">
                            <?php if ($productImage): ?>
                                <img src="<?= $productImage ?>"
                                     alt="<?= $productName ?>"
                                     class="product-grid-partial__image"
                                     loading="lazy">
                            <?php else: ?>
                                <div class="product-grid-partial__image-placeholder">
                                    <svg width="48" height="48" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                                        <path d="M3 7V5C3 3.89543 3.89543 3 5 3H19C20.1046 3 21 3.89543 21 5V7M3 7V19C3 20.1046 3.89543 21 5 21H19C20.1046 21 21 20.1046 21 19V7M3 7H21" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                                    </svg>
                                </div>
                            <?php endif; ?>

                            <?php if ($isNew || $isSale): ?>
                                <div class="product-grid-partial__badges">
                                    <?php if ($isNew): ?>
                                        <span class="product-grid-partial__badge product-grid-partial__badge--new">@lang{NEW}</span>
                                    <?php endif; ?>
                                    <?php if ($isSale): ?>
                                        <span class="product-grid-partial__badge product-grid-partial__badge--sale">@lang{SALE}</span>
                                    <?php endif; ?>
                                </div>
                            <?php endif; ?>

                            <?php if (!$isInStock): ?>
                                <div class="product-grid-partial__out-of-stock">@lang{缺货}</div>
                            <?php endif; ?>
                        </div>
                        </a>
                        <?= $this->fetch('Weline_Theme::theme/frontend/partials/product/shopper-actions.phtml', [
                            'product_id' => $productId,
                            'show_wishlist' => $showWishlist,
                            'show_compare' => $showCompare,
                            'show_quickview' => $showQuickview,
                        ]) ?>
                    </div>

                    <div class="product-grid-partial__content">
                        <h3 class="product-grid-partial__title">
                            <a href="<?= $productUrl ?>"><?= $productName ?></a>
                        </h3>

                        <?php if ($showRating && $productRating > 0): ?>
                            <div class="product-grid-partial__rating">
                                <div class="product-grid-partial__stars" style="--rating: <?= $productRating ?>;">
                                    <svg class="product-grid-partial__star" viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.39 5.64L18 7.27l-4.12 3.72L15 17 10 13.77 5 17l1.12-6L2 7.27l5.61-.63L10 1z"/></svg>
                                    <svg class="product-grid-partial__star" viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.39 5.64L18 7.27l-4.12 3.72L15 17 10 13.77 5 17l1.12-6L2 7.27l5.61-.63L10 1z"/></svg>
                                    <svg class="product-grid-partial__star" viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.39 5.64L18 7.27l-4.12 3.72L15 17 10 13.77 5 17l1.12-6L2 7.27l5.61-.63L10 1z"/></svg>
                                    <svg class="product-grid-partial__star" viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.39 5.64L18 7.27l-4.12 3.72L15 17 10 13.77 5 17l1.12-6L2 7.27l5.61-.63L10 1z"/></svg>
                                    <svg class="product-grid-partial__star" viewBox="0 0 20 20" fill="currentColor"><path d="M10 1l2.39 5.64L18 7.27l-4.12 3.72L15 17 10 13.77 5 17l1.12-6L2 7.27l5.61-.63L10 1z"/></svg>
                                </div>
                                <?php if ($productReviewCount > 0): ?>
                                    <span class="product-grid-partial__review-count">(<?= $productReviewCount ?>)</span>
                                <?php endif; ?>
                            </div>
                        <?php endif; ?>

                        <?php if ($showPrice): ?>
                            <div class="product-grid-partial__price">
                                <?php if ($productOriginalPrice > $productPrice): ?>
                                    <span class="product-grid-partial__price-original">¥<?= number_format($productOriginalPrice, 2) ?></span>
                                <?php endif; ?>
                                <span class="product-grid-partial__price-current">¥<?= number_format($productPrice, 2) ?></span>
                            </div>
                        <?php endif; ?>

                        <?php if ($showAddToCart): ?>
                            <div class="product-grid-partial__actions">
                                <?php if ($isInStock): ?>
                                    <button type="button" class="product-grid-partial__add-to-cart" data-product-id="<?= $productId ?>">
                                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                                            <path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4zM3 6h18M16 10a4 4 0 01-8 0" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                                        </svg>
                                        <span>@lang{加入购物车}</span>
                                    </button>
                                <?php else: ?>
                                    <button type="button" class="product-grid-partial__out-of-stock-btn" disabled>@lang{暂时缺货}</button>
                                <?php endif; ?>
                            </div>
                        <?php endif; ?>
                    </div>
                </div>
            </article>
        <?php endforeach; ?>
    </div>
</div>

<style>
.product-grid-partial__grid {
    display: grid;
    grid-template-columns: repeat(<?= $columns ?>, 1fr);
    gap: var(--spacing-5);
}

.product-grid-partial__item {
    display: flex;
}

.product-grid-partial__card {
    width: 100%;
    background-color: var(--color-bg-primary);
    border: var(--border-width-thin) solid var(--color-border-light);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-sm);
    transition: box-shadow 0.3s ease, transform 0.2s ease, border-color 0.2s ease;
    position: relative;
}
.product-grid-partial__media {
    position: relative;
}
.product-grid-partial__media .product-actions {
    position: absolute;
    top: var(--spacing-3);
    right: var(--spacing-3);
    z-index: 3;
    display: flex;
    flex-direction: column;
    gap: var(--spacing-1);
    opacity: 0;
    transform: translateX(var(--spacing-sm));
    transition: all 0.3s ease;
}
.product-grid-partial__card:hover .product-actions,
.product-grid-partial__card:focus-within .product-actions {
    opacity: 1;
    transform: translateX(0);
}
.product-grid-partial__media .action-btn {
    width: var(--size-control-sm);
    height: var(--size-control-sm);
    border: 0;
    border-radius: 50%;
    background: var(--color-bg-primary);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    box-shadow: var(--shadow-md);
}

.product-grid-partial__card:hover {
    border-color: var(--color-border-focus);
    box-shadow: var(--shadow-lg);
    transform: translateY(var(--spacing-neg-0_5));
}

.product-grid-partial__image-link {
    display: block;
    text-decoration: none;
}

.product-grid-partial__image-wrapper {
    position: relative;
    padding-top: 100%;
    background-color: var(--color-bg-secondary);
    overflow: hidden;
}

.product-grid-partial__image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
}

.product-grid-partial__card:hover .product-grid-partial__image {
    transform: scale(1.05);
}

.product-grid-partial__image-placeholder {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--color-bg-secondary);
    color: var(--color-text-disabled);
}

.product-grid-partial__badges {
    position: absolute;
    top: var(--spacing-3);
    left: var(--spacing-3);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-1);
    z-index: 1;
}

.product-grid-partial__badge {
    padding: var(--spacing-0_5) var(--spacing-2);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    text-transform: uppercase;
    border-radius: var(--radius-sm);
    letter-spacing: var(--token-size-0-5px);
}

.product-grid-partial__badge--new {
    background-color: var(--color-success);
    color: var(--color-text-light);
}

.product-grid-partial__badge--sale {
    background-color: var(--color-error);
    color: var(--color-text-light);
}

.product-grid-partial__out-of-stock {
    position: absolute;
    top: var(--spacing-3);
    right: var(--spacing-3);
    padding: var(--spacing-xs) var(--spacing-2);
    background-color: var(--color-text-secondary);
    color: var(--color-text-light);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    border-radius: var(--radius-sm);
    z-index: 1;
}

.product-grid-partial__wishlist { display: none; }

.product-grid-partial__content {
    padding: var(--spacing-4);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-2);
}

.product-grid-partial__title {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    line-height: 1.4;
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.product-grid-partial__title a {
    color: var(--color-text-primary);
    text-decoration: none;
    transition: color 0.2s ease;
}

.product-grid-partial__title a:hover {
    color: var(--color-link);
}

.product-grid-partial__rating {
    display: flex;
    align-items: center;
    gap: var(--spacing-2);
}

.product-grid-partial__stars {
    display: inline-flex;
    position: relative;
    color: var(--color-text-disabled);
}

.product-grid-partial__stars::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: calc(var(--rating) / 5 * 100%);
    background: linear-gradient(90deg, var(--color-warning), var(--color-warning));
    -webkit-background-clip: text;
    background-clip: text;
}

.product-grid-partial__star {
    width: var(--font-size-sm);
    height: var(--font-size-sm);
}

.product-grid-partial__review-count {
    font-size: var(--font-size-xs);
    color: var(--color-text-tertiary);
}

.product-grid-partial__price {
    display: flex;
    align-items: baseline;
    gap: var(--spacing-2);
    margin-top: var(--spacing-1);
}

.product-grid-partial__price-original {
    font-size: var(--font-size-sm);
    color: var(--color-text-tertiary);
    text-decoration: line-through;
}

.product-grid-partial__price-current {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-bold);
    color: var(--color-error);
}

.product-grid-partial__actions {
    margin-top: var(--spacing-2);
}

.product-grid-partial__add-to-cart {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-2);
    padding: var(--spacing-2) var(--spacing-3);
    background-color: var(--color-accent);
    color: var(--color-text-light);
    border: none;
    border-radius: var(--radius-md);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    cursor: pointer;
    transition: background-color 0.2s ease;
}

.product-grid-partial__add-to-cart:hover {
    background-color: var(--color-accent-hover);
}

.product-grid-partial__out-of-stock-btn {
    width: 100%;
    padding: var(--spacing-2) var(--spacing-3);
    background-color: var(--color-bg-tertiary);
    color: var(--color-text-secondary);
    border: none;
    border-radius: var(--radius-md);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    cursor: not-allowed;
}

/* ---- 响应式 ---- */
@media (max-width: 1200px) {
    .product-grid-partial__grid {
        grid-template-columns: repeat(<?= min($columns, 4) ?>, 1fr);
    }
}

@media (max-width: 992px) {
    .product-grid-partial__grid {
        grid-template-columns: repeat(<?= $columnsTablet ?>, 1fr);
    }
}

@media (max-width: 768px) {
    .product-grid-partial__grid {
        grid-template-columns: repeat(<?= $columnsMobile ?>, 1fr);
        gap: var(--spacing-3);
    }

    .product-grid-partial__content {
        padding: var(--spacing-3);
    }

    .product-grid-partial__media .product-actions {
        opacity: 1;
        transform: translateX(0);
    }
}

@media (max-width: 480px) {
    .product-grid-partial__grid {
        grid-template-columns: repeat(<?= min($columnsMobile, 2) ?>, 1fr);
        gap: var(--spacing-2);
    }
}
</style>

<script>
(function() {
    'use strict';
    var grid = document.getElementById('<?= $uniqueId ?>');
    if (!grid) return;

    grid.querySelectorAll('.product-grid-partial__add-to-cart').forEach(function(btn) {
        btn.addEventListener('click', function(e) {
            e.preventDefault();
            var productId = this.dataset.productId;
            var originalHtml = this.innerHTML;
            this.innerHTML = '<svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M5 13l4 4L19 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg> <span>@lang{已添加}</span>';
            this.style.backgroundColor = 'var(--color-success)';
            setTimeout(function() {
                btn.innerHTML = originalHtml;
                btn.style.backgroundColor = '';
            }, 2000);
        });
    });
})();
</script>
<?php endif; ?>
