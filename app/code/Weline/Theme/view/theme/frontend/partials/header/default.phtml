<?php
/**
 * Theme partial: default frontend header.
 *
 * @meta.name {default="Default Header",name="Default Header",description="Theme-owned frontend header"}
 * @param.logo {default="",name="Logo URL",description="Logo image URL"}
 * @param.logoText {default="Weline",name="Logo Text",description="Text shown when no logo image is configured"}
 * @param.navItems {default=[],name="Navigation Items",description="Navigation items, format: [{\"text\":\"Home\",\"url\":\"/\"}]"}
 * @param.showSearch {default=true,name="Show Search",description="Whether the default search area is shown"}
 * @param.showUserMenu {default=true,name="Show User Menu",description="Whether account/cart actions are shown"}
 * @param.class {default="",name="Extra CSS class",description="Extra CSS class"}
 */

$meta = $this->getData('meta') ?? [];
$logo = (string)($meta['logo'] ?? '');
$logoText = (string)($meta['logoText'] ?? __('Weline'));
$logoHeight = max(28, (int)($meta['logoHeight'] ?? 38));
$class = (string)($meta['class'] ?? '');
$navItems = $this->getData('navItems') ?? $meta['navItems'] ?? [];

if (is_string($navItems)) {
    $decodedNavItems = json_decode($navItems, true);
    $navItems = is_array($decodedNavItems) ? $decodedNavItems : [];
}

if (!is_array($navItems) || $navItems === []) {
    $navItems = [
        ['text' => __('Home'), 'url' => '/'],
        ['text' => __('Products'), 'url' => '/products'],
        ['text' => __('Solutions'), 'url' => '/solutions'],
        ['text' => __('Docs'), 'url' => '/docs'],
        ['text' => __('Support'), 'url' => '/support'],
    ];
}

$boolValue = static function ($value, bool $default): bool {
    if ($value === null || $value === '') {
        return $default;
    }
    $parsed = filter_var($value, FILTER_VALIDATE_BOOL, FILTER_NULL_ON_FAILURE);
    return $parsed ?? $default;
};

$showSearch = $boolValue($meta['showSearch'] ?? true, true);
$showUserMenu = $boolValue($meta['showUserMenu'] ?? true, true);
$esc = static fn($value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
?>
<!-- theme-partial:header option=default -->
<style data-theme-inline="header-default">
    .weline-header {
        position: sticky;
        top: 0;
        z-index: 50;
        background: rgba(255, 255, 255, 0.94);
        border-bottom: 1px solid var(--weline-theme-border, #e3e8ef);
        box-shadow: 0 10px 28px rgba(23, 32, 51, 0.06);
        backdrop-filter: blur(16px);
    }

    .weline-header__announcement {
        background: var(--weline-theme-text, #172033);
        color: #fff;
        font-size: 13px;
    }

    .weline-header__announcement-inner {
        min-height: 34px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 12px;
        text-align: center;
    }

    .weline-header__bar {
        display: grid;
        grid-template-columns: auto minmax(260px, 1fr) auto;
        align-items: center;
        gap: 22px;
        min-height: 74px;
    }

    .weline-header__brand {
        display: inline-flex;
        align-items: center;
        gap: 11px;
        min-width: 0;
        color: var(--weline-theme-text, #172033);
        font-weight: 800;
    }

    .weline-header__brand > span:last-child {
        min-width: 0;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .weline-header__mark {
        display: grid;
        place-items: center;
        width: 40px;
        height: 40px;
        border-radius: 12px;
        background: linear-gradient(135deg, var(--weline-theme-primary, #5b4cf5), #18a47a);
        color: #fff;
        font-weight: 900;
        box-shadow: 0 12px 26px rgba(91, 76, 245, 0.22);
    }

    .weline-header__logo {
        display: block;
        height: auto;
        width: auto;
        max-width: 180px;
        object-fit: contain;
    }

    .weline-header__nav {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 4px;
        min-width: 0;
    }

    .weline-header__nav-list {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 4px;
        margin: 0;
        padding: 0;
        list-style: none;
        flex-wrap: wrap;
    }

    .weline-header__nav-link {
        display: inline-flex;
        align-items: center;
        min-height: 38px;
        padding: 0 12px;
        border-radius: 10px;
        color: var(--weline-theme-muted, #657083);
        font-weight: 700;
        font-size: 14px;
        white-space: nowrap;
    }

    .weline-header__nav-link:hover {
        background: rgba(91, 76, 245, 0.08);
        color: var(--weline-theme-primary, #5b4cf5);
    }

    .weline-header__tools {
        display: flex;
        align-items: center;
        justify-content: flex-end;
        gap: 10px;
        min-width: 0;
    }

    .weline-header__search {
        width: min(100%, 330px);
    }

    .weline-header__search-form {
        display: flex;
        align-items: center;
        overflow: hidden;
        min-height: 42px;
        border: 1px solid var(--weline-theme-border, #e3e8ef);
        border-radius: 12px;
        background: #fff;
    }

    .weline-header__search-input {
        min-width: 0;
        width: 100%;
        border: 0;
        outline: 0;
        padding: 0 13px;
        color: var(--weline-theme-text, #172033);
        background: transparent;
    }

    .weline-header__search-button {
        display: grid;
        place-items: center;
        width: 44px;
        min-height: 42px;
        border: 0;
        background: var(--weline-theme-primary, #5b4cf5);
        color: #fff;
        cursor: pointer;
    }

    .weline-header__actions {
        display: flex;
        align-items: center;
        gap: 8px;
        min-width: 0;
    }

    .weline-header__action {
        display: inline-flex;
        align-items: center;
        gap: 7px;
        min-height: 38px;
        padding: 0 10px;
        border: 1px solid transparent;
        border-radius: 10px;
        color: var(--weline-theme-muted, #657083);
        font-size: 13px;
        font-weight: 700;
        white-space: nowrap;
    }

    .weline-header__action:hover {
        border-color: var(--weline-theme-border, #e3e8ef);
        background: #fff;
        color: var(--weline-theme-primary, #5b4cf5);
    }

    .weline-header__menu-toggle {
        display: none;
        place-items: center;
        width: 42px;
        height: 42px;
        border: 1px solid var(--weline-theme-border, #e3e8ef);
        border-radius: 11px;
        background: #fff;
        color: var(--weline-theme-text, #172033);
        cursor: pointer;
    }

    @media (max-width: 1040px) {
        .weline-header {
            --weline-header-panel-gap: 8px;
        }

        .weline-header__bar {
            grid-template-columns: minmax(0, 1fr) auto;
            gap: 14px;
            min-height: 66px;
        }

        .weline-header__brand-slot {
            grid-column: 1;
            grid-row: 1;
            min-width: 0;
        }

        .weline-header__nav {
            display: none;
            grid-column: 1 / -1;
            grid-row: 2;
            width: 100%;
            justify-content: stretch;
            padding: 0 0 14px;
        }

        .weline-header.is-open .weline-header__nav {
            display: block;
        }

        .weline-header__nav-list {
            display: grid;
            width: 100%;
            grid-template-columns: repeat(auto-fit, minmax(148px, 1fr));
            gap: var(--weline-header-panel-gap);
            justify-content: stretch;
            padding-top: 12px;
            border-top: 1px solid var(--weline-theme-border, #e3e8ef);
        }

        .weline-header__nav-link {
            min-height: 42px;
            justify-content: center;
            padding: 0 12px;
            border: 1px solid var(--weline-theme-border, #e3e8ef);
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.86);
        }

        .weline-header__brand {
            gap: 9px;
        }

        .weline-header__tools {
            grid-column: 2;
            grid-row: 1;
            gap: 6px;
        }

        .weline-header__actions {
            justify-content: flex-end;
            gap: 4px;
        }

        .weline-header__actions .weline-choice-switcher {
            min-height: 38px;
            padding: 0 8px;
            border-radius: 10px;
            flex-direction: row;
            align-items: center;
            gap: 6px;
        }

        .weline-header__actions .weline-choice-switcher .action-line-1 {
            display: none;
        }

        .weline-header__actions .weline-choice-switcher .action-line-2 {
            min-width: 0;
            font-size: 0.8rem;
        }

        .weline-header__actions .weline-choice-switcher .current-language {
            max-width: 7rem;
        }

        .weline-header__action {
            min-height: 38px;
            padding: 0 8px;
            border-radius: 10px;
        }

        .weline-header__menu-toggle {
            display: grid;
            flex: 0 0 auto;
            width: 38px;
            height: 38px;
            border-radius: 10px;
        }

        .weline-header__search {
            display: none;
        }

        .weline-header.is-open .weline-header__menu-toggle {
            border-color: var(--weline-theme-primary, #5b4cf5);
            background: var(--weline-theme-primary, #5b4cf5);
            color: #fff;
        }
    }

    @media (min-width: 721px) and (max-width: 900px) {
        .weline-header__action span {
            display: none;
        }

        .weline-header__actions .weline-choice-switcher .current-language {
            max-width: 5.75rem;
        }

        .weline-header__actions .weline-choice-switcher .current-currency-symbol {
            display: none;
        }
    }

    @media (max-width: 720px) {
        .weline-header__bar {
            grid-template-columns: minmax(0, 1fr) auto;
            min-height: 64px;
            gap: 10px;
            padding-top: 10px;
            padding-bottom: 10px;
        }

        .weline-header__brand-slot {
            min-width: 0;
            grid-column: 1;
            grid-row: 1;
        }

        .weline-header__brand {
            max-width: 100%;
        }

        .weline-header__mark {
            width: 38px;
            height: 38px;
            border-radius: 11px;
        }

        .weline-header__tools {
            display: contents;
        }

        .weline-header__menu-toggle {
            grid-column: 2;
            grid-row: 1;
            align-self: center;
            justify-self: end;
        }

        .weline-header__nav {
            grid-column: 1 / -1;
            grid-row: 2;
            padding: 2px 0 0;
        }

        .weline-header__actions {
            display: none;
            grid-column: 1 / -1;
            grid-row: 3;
            width: 100%;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 8px;
            padding-top: 10px;
            border-top: 1px solid var(--weline-theme-border, #e3e8ef);
        }

        .weline-header.is-open .weline-header__actions {
            display: grid;
        }

        .weline-header__action-slot {
            min-width: 0;
        }

        .weline-header__action {
            width: 100%;
            justify-content: center;
            min-height: 40px;
            padding: 0 10px;
            border-color: var(--weline-theme-border, #e3e8ef);
            background: #fff;
        }

        .weline-header__action span {
            display: inline;
        }

        .weline-header__actions .weline-choice-switcher {
            width: 100%;
            min-height: 40px;
            padding: 0 10px;
            border-color: var(--weline-theme-border, #e3e8ef);
            background: #fff;
            justify-content: center;
        }

        .weline-header__actions .weline-choice-switcher .weline-choice-panel {
            position: fixed;
            top: 76px;
            left: 12px;
            right: 12px;
            width: auto;
            min-width: 0;
            max-width: none;
        }

        .weline-header__actions .weline-choice-switcher .action-line-1 {
            display: none;
        }

        .weline-header__actions .weline-choice-switcher .action-line-2 {
            min-width: 0;
            font-size: 0.78rem;
        }

        .weline-header__actions .weline-choice-switcher .current-language {
            max-width: 5.5rem;
        }

        .weline-header__nav-list {
            grid-template-columns: 1fr;
            padding-top: 10px;
        }

        .weline-header__nav-link {
            justify-content: flex-start;
        }
    }

    @media (max-width: 520px) {
        .weline-header__actions {
            grid-template-columns: 1fr;
        }

        .weline-header__action,
        .weline-header__actions .weline-choice-switcher {
            justify-content: flex-start;
        }
    }

    @media (max-width: 420px) {
        .weline-header__brand > span:last-child {
            display: none;
        }

        .weline-header__actions .weline-choice-switcher .current-language {
            max-width: 4.75rem;
        }
    }
</style>

<header id="header"
        class="weline-header <?= $esc($class) ?>"
        data-layout="default"
        data-layout-type="header"
        data-layout-option="default"
        data-wslot="header"
        data-wslot-name="Header area"
        data-wslot-accept="layout-global-header,header-container,header-banner,announcement"
        data-wslot-multiple="true"
        data-wslot-append="true"
        data-wslot-position="header">
    <w:hook>Weline_Theme::frontend::partials::header::before</w:hook>

    <w:slot id="header-announcement"
            name="Header announcement"
            accept="layout-header-announcement,announcement,banner,promo-banner"
            multiple="true"
            append="true"
            position="header"
            class="weline-header__announcement">
        <w:hook>Weline_Theme::frontend::partials::header::announcement<else/>
        <div class="weline-container weline-header__announcement-inner">
            <span><?= __('Free theme-ready header. Replace this area from any module hook or slot.') ?></span>
        </div>
        </w:hook>
    </w:slot>

    <div class="weline-container weline-header__bar">
        <w:slot id="logo"
                name="Logo"
                accept="layout-header-logo,layout-global-header-logo,logo,brand"
                exclusive="true"
                position="header"
                class="weline-header__brand-slot">
            <w:hook>Weline_Theme::frontend::partials::header::logo<else/>
            <a class="weline-header__brand" href="/" aria-label="<?= $esc($logoText) ?>">
                <?php if ($logo !== ''): ?>
                    <img class="weline-header__logo"
                         src="<?= $esc($logo) ?>"
                         alt="<?= $esc($logoText) ?>"
                         style="max-height: <?= (int)$logoHeight ?>px">
                <?php else: ?>
                    <span class="weline-header__mark"><?= $esc(mb_substr($logoText, 0, 1)) ?></span>
                    <span><?= $esc($logoText) ?></span>
                <?php endif; ?>
            </a>
            </w:hook>
        </w:slot>

        <w:slot id="header-navigation"
                name="Header navigation"
                accept="layout-header-navigation,layout-global-navigation,navigation,menu,category-menu"
                exclusive="true"
                position="header"
                wrapper="nav"
                class="weline-header__nav"
                style="">
            <w:hook>Weline_Theme::frontend::partials::header::navigation<else/>
            <ul class="weline-header__nav-list">
                <?php foreach ($navItems as $item): ?>
                    <?php if (!is_array($item)) continue; ?>
                    <li>
                        <a class="weline-header__nav-link" href="<?= $esc($item['url'] ?? '#') ?>">
                            <?= $esc($item['text'] ?? '') ?>
                        </a>
                    </li>
                <?php endforeach; ?>
            </ul>
            </w:hook>
        </w:slot>

        <div class="weline-header__tools">
            <?php if ($showSearch): ?>
                <w:slot id="search"
                        name="Header search"
                        accept="layout-header-search,layout-global-search,search,search-box"
                        exclusive="true"
                        position="header"
                        class="weline-header__search">
                    <w:hook>Weline_Theme::frontend::partials::header::search<else/>
                    <form class="weline-header__search-form" action="/search" method="get" role="search">
                        <input class="weline-header__search-input"
                               type="search"
                               name="q"
                               placeholder="<?= $esc(__('Search...')) ?>"
                               autocomplete="off">
                        <button class="weline-header__search-button" type="submit" aria-label="<?= $esc(__('Search')) ?>">
                            <i class="ri-search-line"></i>
                        </button>
                    </form>
                    </w:hook>
                </w:slot>
            <?php endif; ?>

            <?php if ($showUserMenu): ?>
                <w:slot id="header-actions"
                        name="Header actions"
                        accept="layout-header-actions,layout-global-header-actions,header-actions,account,currency,language,cart"
                        multiple="true"
                        append="true"
                        position="header"
                        class="weline-header__actions">
                    <w:slot id="header-language"
                            name="Language switcher"
                            accept="layout-header-language,language-switcher,language"
                            exclusive="true"
                            position="header"
                            class="weline-header__action-slot">
                        <w:hook>Weline_Theme::frontend::partials::header::language<else/>
                        <w:hook>header-language-switcher<else/>
                        <span class="weline-header__action"><i class="ri-translate-2"></i><span>ZH</span></span>
                        </w:hook>
                        </w:hook>
                    </w:slot>

                    <w:slot id="header-currency"
                            name="Currency switcher"
                            accept="layout-header-currency,currency-switcher,currency"
                            exclusive="true"
                            position="header"
                            class="weline-header__action-slot">
                        <w:hook>Weline_Theme::frontend::partials::header::currency<else/>
                        <w:hook>header-currency-switcher<else/>
                        <span class="weline-header__action"><i class="ri-money-cny-circle-line"></i><span>CNY</span></span>
                        </w:hook>
                        </w:hook>
                    </w:slot>

                    <w:slot id="header-account"
                            name="Account action"
                            accept="layout-header-account,account,user-menu"
                            exclusive="true"
                            position="header"
                            class="weline-header__action-slot">
                        <w:hook>Weline_Theme::frontend::partials::header::account<else/>
                        <w:hook>header-account<else/>
                        <a class="weline-header__action" href="/account"><i class="ri-user-3-line"></i><span><?= __('Account') ?></span></a>
                        </w:hook>
                        </w:hook>
                    </w:slot>

                    <w:slot id="header-cart"
                            name="Cart action"
                            accept="layout-header-cart,cart,mini-cart"
                            exclusive="true"
                            position="header"
                            class="weline-header__action-slot">
                        <w:hook>Weline_Theme::frontend::partials::header::cart<else/>
                        <w:hook>header-cart<else/>
                        <a class="weline-header__action" href="/cart"><i class="ri-shopping-bag-3-line"></i><span><?= __('Cart') ?></span></a>
                        </w:hook>
                        </w:hook>
                    </w:slot>
                </w:slot>
            <?php endif; ?>

            <button class="weline-header__menu-toggle" type="button" aria-label="<?= $esc(__('Toggle navigation')) ?>" aria-controls="header-navigation" aria-expanded="false" data-theme-header-toggle>
                <i class="ri-menu-line"></i>
            </button>
        </div>
    </div>

    <w:hook>Weline_Theme::frontend::partials::header::after</w:hook>
</header>

<script data-no-extract="true">
    (function() {
        var header = document.getElementById('header');
        if (!header) {
            return;
        }
        var toggle = header.querySelector('[data-theme-header-toggle]');
        if (!toggle) {
            return;
        }

        var nav = header.querySelector('#header-navigation');
        var icon = toggle.querySelector('i');
        var mobileQuery = window.matchMedia('(max-width: 1040px)');

        function setOpen(open) {
            header.classList.toggle('is-open', open);
            toggle.setAttribute('aria-expanded', open ? 'true' : 'false');
            if (icon) {
                icon.className = open ? 'ri-close-line' : 'ri-menu-line';
            }
        }

        toggle.addEventListener('click', function() {
            setOpen(!header.classList.contains('is-open'));
        });

        if (nav) {
            nav.addEventListener('click', function(event) {
                var link = event.target && event.target.closest ? event.target.closest('a') : null;
                if (link) {
                    setOpen(false);
                }
            });
        }

        document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape') {
                setOpen(false);
            }
        });

        if (typeof mobileQuery.addEventListener === 'function') {
            mobileQuery.addEventListener('change', function(event) {
                if (!event.matches) {
                    setOpen(false);
                }
            });
        } else if (typeof mobileQuery.addListener === 'function') {
            mobileQuery.addListener(function(event) {
                if (!event.matches) {
                    setOpen(false);
                }
            });
        }
    })();
</script>
