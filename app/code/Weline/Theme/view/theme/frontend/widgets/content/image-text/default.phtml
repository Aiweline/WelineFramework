<?php
/**
 * @widget.source {Weline_Theme::css/widgets/widget-content-image-text-default.css,Weline_Theme::css/widgets/widget-instance-styles.css,Weline_Theme::js/widgets/widget-instance-styles.js}
 * 图文组合部件
 *
 * @var \Weline\Framework\View\Template $this
 *
 * @widget.code {image-text}
 * @widget.name {图文组合}
 * @widget.description {图片和文字的组合布局，支持多种排列方式}
 * @widget.type {content}
 * @widget.area {frontend}
 * @widget.position {["content"]}
 * @widget.page_layouts {["*"]}
 * @widget.compatible {true}
 *
 * @param image {default="",type="media_image",label="图片"}
 * @param title {default="图文标题",type="string",label="标题"}
 * @param content {default="在这里介绍内容、服务或品牌故事。",type="textarea",label="内容"}
 * @param button_text {default="了解更多",type="string",label="按钮文字"}
 * @param button_link {default="",type="string",label="按钮链接"}
 * @param layout {default="image-left",type="select",label="布局",options={image-left:"图左文右",image-right:"图右文左",image-top:"图上文下",image-bg:"图片背景"}}
 * @param image_width {default="50",type="select",label="图片宽度",options={33:"33%",40:"40%",50:"50%",60:"60%"}}
 */

use Weline\Theme\Helper\WidgetUiScope;

$scope = WidgetUiScope::forWidget('image-text');
$wc = $scope->cssClass;
$esc = static fn (string $value): string => htmlspecialchars($value, ENT_QUOTES, 'UTF-8');

$isPreviewMode = (bool)($this->getData('preview_mode') ?? false);
$defaultImage = '';
$image = \Weline\Theme\Helper\LegacyMediaUrl::sanitize($this->getData('image') ?? $defaultImage);
$backgroundImage = \Weline\Theme\Helper\LegacyMediaUrl::sanitize($image, true);
$imageFileHtml = (string)($this->getData('image_file_html') ?? '');
$title = trim((string)($this->getData('title') ?? __('图文标题')));
$content = (string)($this->getData('content') ?? __('在这里介绍内容、服务或品牌故事。'));
$buttonText = trim((string)($this->getData('button_text') ?? __('了解更多')));
$buttonLink = \Weline\Theme\Helper\LegacyMediaUrl::sanitize($this->getData('button_link') ?? '');
$layout = strtolower(trim((string)($this->getData('layout') ?? 'image-left')));
$imageWidth = (string)($this->getData('image_width') ?? '50');

if (!in_array($layout, ['image-left', 'image-right', 'image-top', 'image-bg'], true)) {
    $layout = 'image-left';
}
if (!in_array($imageWidth, ['33', '40', '50', '60'], true)) {
    $imageWidth = '50';
}

$rootStyle = '--wc-image-width:' . $imageWidth . '%;';
if ($layout === 'image-bg' && $imageFileHtml === '' && $backgroundImage !== '') {
    $rootStyle .= '--wc-image-bg:url("' . $backgroundImage . '");';
}
?>

<section class="<?= $esc($wc) ?> layout-<?= $esc($layout) ?><?= $isPreviewMode ? ' is-preview' : '' ?>"
         weline-code="<?= $esc($scope->code) ?>"
         data-uid="<?= $esc($scope->uid) ?>"
         data-js-ns="<?= $esc($scope->jsNs) ?>"
         data-widget-style="<?= $esc($rootStyle) ?>">

    <?php if ($layout === 'image-bg'): ?>
        <div class="bg-container">
            <?php if ($imageFileHtml !== ''): ?>
                <div class="bg-media"><?= $imageFileHtml ?></div>
            <?php endif; ?>
            <div class="bg-overlay"></div>
            <div class="text-content">
                <?php if ($title !== ''): ?>
                    <h2 class="content-title"><?= $esc($title) ?></h2>
                <?php endif; ?>
                <?php if ($content !== ''): ?>
                    <div class="content-text"><?= $content ?></div>
                <?php endif; ?>
                <?php if ($buttonText !== '' && $buttonLink !== ''): ?>
                    <a href="<?= $esc($buttonLink) ?>" class="content-button w-button w-button-primary"><?= $esc($buttonText) ?></a>
                <?php endif; ?>
            </div>
        </div>
    <?php elseif ($layout === 'image-top'): ?>
        <div class="image-section">
            <?php if ($imageFileHtml !== ''): ?>
                <div class="content-image content-image--asset"><?= $imageFileHtml ?></div>
            <?php elseif ($image !== ''): ?>
                <img src="<?= $esc($image) ?>" alt="<?= $esc($title) ?>" class="content-image" loading="lazy" decoding="async">
            <?php endif; ?>
        </div>
        <div class="text-section">
            <?php if ($title !== ''): ?>
                <h2 class="content-title"><?= $esc($title) ?></h2>
            <?php endif; ?>
            <?php if ($content !== ''): ?>
                <div class="content-text"><?= $content ?></div>
            <?php endif; ?>
            <?php if ($buttonText !== '' && $buttonLink !== ''): ?>
                <a href="<?= $esc($buttonLink) ?>" class="content-button w-button w-button-primary"><?= $esc($buttonText) ?></a>
            <?php endif; ?>
        </div>
    <?php else: ?>
        <div class="content-row">
            <div class="image-section">
                <?php if ($imageFileHtml !== ''): ?>
                    <div class="content-image content-image--asset"><?= $imageFileHtml ?></div>
                <?php elseif ($image !== ''): ?>
                    <img src="<?= $esc($image) ?>" alt="<?= $esc($title) ?>" class="content-image" loading="lazy" decoding="async">
                <?php endif; ?>
            </div>
            <div class="text-section">
                <?php if ($title !== ''): ?>
                    <h2 class="content-title"><?= $esc($title) ?></h2>
                <?php endif; ?>
                <?php if ($content !== ''): ?>
                    <div class="content-text"><?= $content ?></div>
                <?php endif; ?>
                <?php if ($buttonText !== '' && $buttonLink !== ''): ?>
                    <a href="<?= $esc($buttonLink) ?>" class="content-button w-button w-button-primary"><?= $esc($buttonText) ?></a>
                <?php endif; ?>
            </div>
        </div>
    <?php endif; ?>
</section>

