<?php
/**
 * 完整头部部件模板 (独占部件)
 *
 * @var array $config 部件配置
 * @var \Weline\Framework\View\Block $block 当前 Block 实例
 *
 * @widget.code {full-header}
 * @widget.name {完整头部}
 * @widget.description {一个完整的、自包含的头部组件，包含Logo、搜索框、导航菜单、用户区域。独占头部区域，整体替换默认Header。}
 * @widget.type {header}
 * @widget.area {frontend}
 * @widget.position {["header"]}
 * @widget.page_layouts {["*"]}
 * @widget.exclusive {true}
 *
 * @param logo_text {default="WELINE",type="string",label="Logo文字"}
 * @param logo_image {default="",type="image",label="Logo图片",description="Logo图片URL，留空显示文字"}
 * @param sticky {default=true,type="bool",label="吸顶效果"}
 * @param bg_color {default="var(--weline-theme-surface)",type="color",label="背景颜色"}
 * @param text_color {default="var(--weline-theme-text)",type="color",label="文字颜色"}
 * @param accent_color {default="var(--weline-theme-primary)",type="color",label="强调色"}
 * @param search_placeholder {default="搜索商品...",type="string",label="搜索框占位符"}
 * @param show_search {default=true,type="bool",label="显示搜索框"}
 * @param show_account {default=true,type="bool",label="显示账户"}
 * @param show_wishlist {default=true,type="bool",label="显示收藏"}
 * @param show_cart {default=true,type="bool",label="显示购物车"}
 * @param show_nav {default=true,type="bool",label="显示导航菜单"}
 * @param nav_style {default="simple",type="select",label="导航样式",options={simple:"简洁",expanded:"展开",mega:"Mega菜单"}}
 * @param shadow {default=true,type="bool",label="显示阴影"}
 */

use Weline\Theme\Helper\ThemeUiColor;
use Weline\Theme\Helper\WidgetUiScope;

$scope = WidgetUiScope::forWidget('full-header');
$wc = $scope->cssClass;
$esc = static fn (string $value): string => htmlspecialchars($value, ENT_QUOTES, 'UTF-8');

$config = $config ?? [];
$isPreviewMode = (bool)($config['preview_mode'] ?? false);
$logoText = $config['logo_text'] ?? 'WELINE';
$logoImage = \Weline\Theme\Helper\LegacyMediaUrl::sanitize($config['logo_image'] ?? '');
$logoImageFileHtml = (string)($config['logo_image_file_html'] ?? '');
$sticky = (bool)($config['sticky'] ?? true);
$bgColor = ThemeUiColor::sanitize($config['bg_color'] ?? '', 'var(--weline-theme-surface)');
$textColor = ThemeUiColor::sanitize($config['text_color'] ?? '', 'var(--weline-theme-text)');
$accentColor = ThemeUiColor::sanitize($config['accent_color'] ?? '', 'var(--weline-theme-primary)');
$searchPlaceholder = $config['search_placeholder'] ?? __('搜索商品...');
$showSearch = (bool)($config['show_search'] ?? true);
$showAccount = (bool)($config['show_account'] ?? true);
$showWishlist = (bool)($config['show_wishlist'] ?? true);
$showCart = (bool)($config['show_cart'] ?? true);
$showNav = (bool)($config['show_nav'] ?? true);
$navStyle = strtolower(trim((string)($config['nav_style'] ?? 'simple')));
if (!in_array($navStyle, ['simple', 'expanded', 'mega'], true)) {
    $navStyle = 'simple';
}
$shadow = (bool)($config['shadow'] ?? true);

$rootStyle = '--wc-header-bg:' . $bgColor . ';'
    . '--wc-header-text:' . $textColor . ';'
    . '--wc-header-accent:' . $accentColor . ';'
    . ($shadow ? '' : '--wc-header-shadow:none;');
?>

<section class="<?= $esc($wc) ?><?= $sticky ? ' is-sticky' : '' ?> nav-style-<?= $esc($navStyle) ?><?= $isPreviewMode ? ' is-preview' : '' ?>"
         weline-code="<?= $esc($scope->code) ?>"
         data-uid="<?= $esc($scope->uid) ?>"
         data-js-ns="<?= $esc($scope->jsNs) ?>"
         style="<?= $esc($rootStyle) ?>">
    <div class="header-topbar">
        <div class="w-container">
            <div class="topbar-left">
                <span class="topbar-text"><?= __('欢迎光临！免费配送满') ?> ¥299</span>
            </div>
            <div class="topbar-right">
                <a href="<?= $block->getUrl('customer/account') ?>" class="topbar-link"><?= __('帮助中心') ?></a>
                <span class="topbar-divider">|</span>
                <a href="<?= $block->getUrl('order/tracking') ?>" class="topbar-link"><?= __('订单跟踪') ?></a>
            </div>
        </div>
    </div>

    <div class="header-main">
        <div class="w-container">
            <div class="header-logo">
                <a href="<?= $block->getUrl('/') ?>" class="logo-link" title="<?= $esc((string)$logoText) ?>">
                    <?php if ($logoImageFileHtml !== ''): ?>
                        <span class="logo-image logo-image--asset"><?= $logoImageFileHtml ?></span>
                    <?php elseif ($logoImage !== ''): ?>
                        <img src="<?= $esc($logoImage) ?>" alt="<?= $esc((string)$logoText) ?>" class="logo-image">
                    <?php else: ?>
                        <span class="logo-text"><?= $esc((string)$logoText) ?></span>
                    <?php endif; ?>
                </a>
            </div>

            <?php if ($showSearch): ?>
            <div class="header-search">
                <w:form action="@var($block->getUrl('search'))" method="get" class="search-form">
                    <div class="search-input-wrapper">
                        <input type="text" name="q" class="search-input" placeholder="<?= $esc((string)$searchPlaceholder) ?>" autocomplete="off">
                        <button type="submit" class="search-btn" aria-label="<?= __('搜索') ?>">
                            <w:icon name="search" size="sm"></w:icon>
                        </button>
                    </div>
                </w:form>
            </div>
            <?php endif; ?>

            <div class="header-tools">
                <?php if ($showAccount): ?>
                <div class="tool-item tool-account">
                    <a href="<?= $block->getUrl('customer/account') ?>" class="tool-link">
                        <w:icon name="user" size="sm"></w:icon>
                        <span class="tool-label"><?= __('账户') ?></span>
                    </a>
                </div>
                <?php endif; ?>
                <?php if ($showWishlist): ?>
                <div class="tool-item tool-wishlist">
                    <a href="<?= $block->getUrl('wishlist') ?>" class="tool-link">
                        <w:icon name="heart" size="sm"></w:icon>
                        <span class="tool-label"><?= __('收藏') ?></span>
                        <span class="tool-badge wishlist-count" data-count="0" hidden>0</span>
                    </a>
                </div>
                <?php endif; ?>
                <?php if ($showCart): ?>
                <div class="tool-item tool-cart">
                    <a href="<?= $block->getUrl('checkout/cart') ?>" class="tool-link">
                        <w:icon name="pin" size="sm"></w:icon>
                        <span class="tool-label"><?= __('购物车') ?></span>
                        <span class="tool-badge cart-count" data-count="0">0</span>
                    </a>
                </div>
                <?php endif; ?>
                <button class="mobile-menu-toggle" type="button" aria-label="<?= __('菜单') ?>" aria-expanded="false">
                    <w:icon name="menu" size="sm"></w:icon>
                </button>
            </div>
        </div>
    </div>

    <?php if ($showNav): ?>
    <nav class="header-nav">
        <div class="w-container">
            <ul class="nav-list">
                <li class="nav-item">
                    <a href="<?= $block->getUrl('/') ?>" class="nav-link" data-state="active">
                        <w:icon name="home" size="sm"></w:icon><span><?= __('首页') ?></span>
                    </a>
                </li>
                <li class="nav-item has-dropdown">
                    <a href="<?= $block->getUrl('catalog/category') ?>" class="nav-link">
                        <w:icon name="grid" size="sm"></w:icon><span><?= __('商品分类') ?></span>
                        <w:icon name="arrow-down" size="sm"></w:icon>
                    </a>
                    <div class="nav-dropdown">
                        <div class="dropdown-inner">
                            <div class="dropdown-column">
                                <h4 class="dropdown-title"><?= __('热门分类') ?></h4>
                                <ul class="dropdown-list">
                                    <li><a href="#"><?= __('电子产品') ?></a></li>
                                    <li><a href="#"><?= __('服装鞋帽') ?></a></li>
                                    <li><a href="#"><?= __('家居生活') ?></a></li>
                                    <li><a href="#"><?= __('美妆护肤') ?></a></li>
                                </ul>
                            </div>
                            <div class="dropdown-column">
                                <h4 class="dropdown-title"><?= __('特色推荐') ?></h4>
                                <ul class="dropdown-list">
                                    <li><a href="#"><?= __('新品上市') ?></a></li>
                                    <li><a href="#"><?= __('限时特惠') ?></a></li>
                                    <li><a href="#"><?= __('品牌精选') ?></a></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </li>
                <li class="nav-item"><a href="<?= $block->getUrl('catalog/products', ['filter' => 'hot']) ?>" class="nav-link"><w:icon name="fire" size="sm"></w:icon><span><?= __('热卖') ?></span></a></li>
                <li class="nav-item"><a href="<?= $block->getUrl('catalog/products', ['filter' => 'new']) ?>" class="nav-link"><w:icon name="bolt" size="sm"></w:icon><span><?= __('新品') ?></span></a></li>
                <li class="nav-item"><a href="<?= $block->getUrl('promotions') ?>" class="nav-link highlight"><w:icon name="circle" size="sm"></w:icon><span><?= __('促销活动') ?></span></a></li>
                <li class="nav-item"><a href="<?= $block->getUrl('about') ?>" class="nav-link"><w:icon name="info" size="sm"></w:icon><span><?= __('关于我们') ?></span></a></li>
            </ul>
        </div>
    </nav>
    <?php endif; ?>
</section>

<?php if ($sticky): ?>
<div class="header-placeholder" hidden></div>
<?php endif; ?>

<style>
.<?= $wc ?> {
    background: var(--wc-header-bg, var(--weline-theme-surface));
    color: var(--wc-header-text, var(--weline-theme-text));
    box-shadow: var(--wc-header-shadow, var(--weline-theme-shadow-sm));
    width: 100%;
    z-index: 1000;
    transition: all var(--weline-theme-transition-base, 0.3s ease);
}

.<?= $wc ?>.is-sticky {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
}

.<?= $wc ?>.is-sticky + .header-placeholder {
    display: block !important;
}

.<?= $wc ?> .header-topbar {
    background: var(--wc-header-text, var(--weline-theme-text));
    color: var(--wc-header-bg, var(--weline-theme-surface));
    font-size: var(--weline-font-size-xs, 12px);
    padding: var(--weline-space-2, 8px) 0;
}

.<?= $wc ?> .header-topbar .w-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.<?= $wc ?> .topbar-link {
    color: inherit;
    text-decoration: none;
    opacity: 0.9;
}

.<?= $wc ?> .topbar-divider {
    margin: 0 var(--weline-space-3, 10px);
    opacity: 0.5;
}

.<?= $wc ?> .header-main {
    padding: var(--weline-space-4, 15px) 0;
}

.<?= $wc ?> .header-main .w-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--weline-space-8, 30px);
}

.<?= $wc ?> .logo-text {
    font-size: var(--weline-font-size-2xl, 28px);
    font-weight: 700;
    color: var(--wc-header-accent, var(--weline-theme-primary));
    letter-spacing: 2px;
}

.<?= $wc ?> .logo-image,
.<?= $wc ?> .logo-image--asset img {
    max-height: 50px;
    width: auto;
}

.<?= $wc ?> .header-search {
    flex: 1;
    max-width: 550px;
}

.<?= $wc ?> .search-input-wrapper {
    display: flex;
    align-items: center;
    border: var(--weline-theme-border-width, 2px) var(--weline-theme-border-style, solid) var(--weline-theme-border);
    border-radius: var(--weline-radius-round, 999px);
    overflow: hidden;
    transition: border-color var(--weline-theme-transition-base, 0.2s ease);
}

.<?= $wc ?> .search-input-wrapper:focus-within {
    border-color: var(--wc-header-accent, var(--weline-theme-primary));
}

.<?= $wc ?> .search-input {
    flex: 1;
    border: none;
    padding: var(--weline-space-3, 12px) var(--weline-space-5, 20px);
    font-size: var(--weline-font-size-sm, 14px);
    background: transparent;
    outline: none;
    color: var(--wc-header-text, var(--weline-theme-text));
}

.<?= $wc ?> .search-btn {
    background: var(--wc-header-accent, var(--weline-theme-primary));
    color: var(--weline-theme-on-primary);
    border: none;
    padding: var(--weline-space-3, 12px) var(--weline-space-5, 20px);
    cursor: pointer;
}

.<?= $wc ?> .header-tools {
    display: flex;
    align-items: center;
    gap: var(--weline-space-5, 20px);
}

.<?= $wc ?> .tool-link {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-decoration: none;
    color: var(--wc-header-text, var(--weline-theme-text));
    transition: color var(--weline-theme-transition-base, 0.2s ease);
}

.<?= $wc ?> .tool-link:hover {
    color: var(--wc-header-accent, var(--weline-theme-primary));
}

.<?= $wc ?> .tool-label {
    font-size: var(--weline-font-size-xs, 12px);
}

.<?= $wc ?> .tool-badge {
    position: absolute;
    top: -5px;
    right: 0;
    background: var(--wc-header-accent, var(--weline-theme-primary));
    color: var(--weline-theme-on-primary);
    font-size: var(--weline-font-size-xs, 10px);
    min-width: 18px;
    height: 18px;
    line-height: 18px;
    text-align: center;
    border-radius: var(--weline-radius-round, 999px);
    padding: 0 var(--weline-space-1, 5px);
}

.<?= $wc ?> .tool-item {
    position: relative;
}

.<?= $wc ?> .mobile-menu-toggle {
    display: none;
    background: none;
    border: none;
    cursor: pointer;
    color: var(--wc-header-text, var(--weline-theme-text));
}

.<?= $wc ?> .header-nav {
    background: var(--weline-theme-surface-muted);
    border-top: var(--weline-theme-border-width, 1px) var(--weline-theme-border-style, solid) var(--weline-theme-border);
}

.<?= $wc ?> .nav-list {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    gap: var(--weline-space-1, 5px);
}

.<?= $wc ?> .nav-item {
    position: relative;
}

.<?= $wc ?> .nav-link {
    display: flex;
    align-items: center;
    gap: var(--weline-space-2, 6px);
    padding: var(--weline-space-3, 12px) var(--weline-space-5, 18px);
    text-decoration: none;
    color: var(--wc-header-text, var(--weline-theme-text));
    font-size: var(--weline-font-size-sm, 14px);
    font-weight: 500;
    border-radius: var(--weline-radius-sm, 4px);
    transition: all var(--weline-theme-transition-base, 0.2s ease);
}

.<?= $wc ?> .nav-link:hover,
.<?= $wc ?> .nav-link[data-state="active"] {
    color: var(--wc-header-accent, var(--weline-theme-primary));
    background: var(--weline-theme-primary-surface);
}

.<?= $wc ?> .nav-link.highlight {
    color: var(--wc-header-accent, var(--weline-theme-primary));
    font-weight: 600;
}

.<?= $wc ?> .nav-dropdown {
    position: absolute;
    top: 100%;
    left: 0;
    min-width: 400px;
    background: var(--weline-theme-surface-raised);
    border-radius: var(--weline-radius-md, 8px);
    box-shadow: var(--weline-theme-shadow-lg);
    opacity: 0;
    visibility: hidden;
    transform: translateY(10px);
    transition: all var(--weline-theme-transition-base, 0.2s ease);
    z-index: 100;
}

.<?= $wc ?> .nav-item:hover .nav-dropdown {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
}

.<?= $wc ?> .dropdown-inner {
    display: flex;
    padding: var(--weline-space-5, 20px);
    gap: var(--weline-space-8, 30px);
}

.<?= $wc ?> .dropdown-title {
    font-size: var(--weline-font-size-sm, 14px);
    font-weight: 600;
    margin: 0 0 var(--weline-space-3, 12px);
    padding-bottom: var(--weline-space-2, 8px);
    border-bottom: var(--weline-theme-border-width, 2px) var(--weline-theme-border-style, solid) var(--wc-header-accent, var(--weline-theme-primary));
}

.<?= $wc ?> .dropdown-list {
    list-style: none;
    margin: 0;
    padding: 0;
}

.<?= $wc ?> .dropdown-list a {
    color: var(--weline-theme-text-muted);
    text-decoration: none;
    font-size: var(--weline-font-size-sm, 13px);
}

.<?= $wc ?> .dropdown-list a:hover {
    color: var(--wc-header-accent, var(--weline-theme-primary));
}

.widget-preview-canvas .<?= $wc ?>,
.<?= $wc ?>.is-preview {
    font-size: var(--weline-font-size-xs, 12px);
}

.widget-preview-canvas .<?= $wc ?> .header-topbar,
.<?= $wc ?>.is-preview .header-topbar {
    display: none;
}

.widget-preview-canvas .<?= $wc ?> .header-nav,
.<?= $wc ?>.is-preview .header-nav {
    display: none;
}

@media (max-width: 992px) {
    .<?= $wc ?> .header-main .w-container {
        flex-wrap: wrap;
    }

    .<?= $wc ?> .header-search {
        order: 3;
        max-width: none;
        width: 100%;
        margin-top: var(--weline-space-4, 15px);
    }

    .<?= $wc ?> .tool-label {
        display: none;
    }

    .<?= $wc ?> .mobile-menu-toggle {
        display: block;
    }

    .<?= $wc ?> .header-nav {
        display: none;
    }

    .<?= $wc ?> .header-nav.is-open {
        display: block;
    }

    .<?= $wc ?> .nav-list {
        flex-direction: column;
    }
}
</style>

<script>
(function () {
    var root = document.querySelector('[data-uid="<?= $esc($scope->uid) ?>"]');
    if (!root) return;

    var placeholder = root.nextElementSibling;
    var menuToggle = root.querySelector('.mobile-menu-toggle');
    var nav = root.querySelector('.header-nav');

    <?php if ($sticky): ?>
    var headerHeight = root.offsetHeight;

    function updatePlaceholder() {
        if (placeholder && root.classList.contains('is-sticky')) {
            placeholder.style.height = headerHeight + 'px';
            placeholder.hidden = false;
        }
    }

    window.addEventListener('resize', function () {
        headerHeight = root.offsetHeight;
        updatePlaceholder();
    });

    updatePlaceholder();
    <?php endif; ?>

    if (menuToggle && nav) {
        menuToggle.addEventListener('click', function () {
            var open = nav.classList.toggle('is-open');
            menuToggle.setAttribute('aria-expanded', open ? 'true' : 'false');
        });
    }
})();
</script>
