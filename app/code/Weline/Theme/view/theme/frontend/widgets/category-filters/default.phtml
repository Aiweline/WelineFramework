<?php
/**
 * 部件模板：分类页左侧筛选（Category Filters）
 *
 * @widget.code {category-filters}
 * @widget.name {分类筛选}
 * @widget.description {分类页左侧筛选器，支持价格、品牌、属性筛选}
 * @widget.type {sidebar}
 * @widget.area {frontend}
 * @widget.position {["sidebar"]}
 * @widget.page_layouts {["category", "search"]}
 * @widget.supports {["layout-category-filters","layout-product-list-filters"]}
 *
 * @param html {default="",type="textarea",label="筛选 HTML",description="业务渲染的筛选结构，留空则使用 Hook 或 Mock"}
 */

use Weline\Theme\Helper\WidgetUiScope;

$scope = WidgetUiScope::forWidget('category-filters');
$wc = $scope->cssClass;
$esc = static fn (string $value): string => htmlspecialchars($value, ENT_QUOTES, 'UTF-8');

$isPreviewMode = (bool)($this->getData('preview_mode') ?? false);
$meta = $this->getData('meta') ?? [];
$filtersHtml = $this->getData('html') ?? ($meta['filters'] ?? '');
$hasExternalHtml = is_string($filtersHtml) && trim($filtersHtml) !== '';

if ($hasExternalHtml) {
    ?>
<section class="<?= $esc($wc) ?> has-external<?= $isPreviewMode ? ' is-preview' : '' ?>"
         weline-code="<?= $esc($scope->code) ?>"
         data-uid="<?= $esc($scope->uid) ?>"
         data-js-ns="<?= $esc($scope->jsNs) ?>">
    <div class="filters-body"><?= $filtersHtml ?></div>
</section>
<style>
.<?= $wc ?> {
    color: var(--weline-theme-text);
}
.<?= $wc ?> .filters-body {
    width: 100%;
}
</style>
    <?php
    return;
}

$hookContent = $this->getHook('Weline_Theme::frontend::layouts::category::filters-sidebar');

if (is_string($hookContent) && trim($hookContent) !== '') {
    ?>
<section class="<?= $esc($wc) ?> has-hook<?= $isPreviewMode ? ' is-preview' : '' ?>"
         weline-code="<?= $esc($scope->code) ?>"
         data-uid="<?= $esc($scope->uid) ?>"
         data-js-ns="<?= $esc($scope->jsNs) ?>">
    <div class="filters-body"><?= $hookContent ?></div>
</section>
<style>
.<?= $wc ?> {
    color: var(--weline-theme-text);
}
.<?= $wc ?> .filters-body {
    width: 100%;
}
</style>
    <?php
    return;
}
?>

<section class="<?= $esc($wc) ?><?= $isPreviewMode ? ' is-preview' : '' ?>"
         weline-code="<?= $esc($scope->code) ?>"
         data-uid="<?= $esc($scope->uid) ?>"
         data-js-ns="<?= $esc($scope->jsNs) ?>">

    <div class="filter-panel">
        <div class="filter-header">
            <div class="filter-title"><?= __('筛选条件') ?></div>
            <button type="button" class="filter-clear"><?= __('清除全部') ?></button>
        </div>

        <div class="filter-applied">
            <span class="filter-chip is-active">
                <?= __('电子产品') ?>
                <button type="button" class="chip-remove" aria-label="<?= __('移除') ?>">×</button>
            </span>
            <span class="filter-chip">
                <?= __('100 - 299 元') ?>
                <button type="button" class="chip-remove" aria-label="<?= __('移除') ?>">×</button>
            </span>
        </div>

        <div class="filter-group">
            <div class="filter-group-title"><?= __('价格区间') ?></div>
            <ul class="filter-list">
                <li><a href="#" class="filter-item"><span class="label"><?= __('0 - 99 元') ?></span><span class="count">128</span></a></li>
                <li><a href="#" class="filter-item"><span class="label"><?= __('100 - 299 元') ?></span><span class="count">86</span></a></li>
                <li><a href="#" class="filter-item"><span class="label"><?= __('300 元以上') ?></span><span class="count">42</span></a></li>
            </ul>
        </div>

        <div class="filter-group">
            <div class="filter-group-title"><?= __('配送方式') ?></div>
            <ul class="filter-list">
                <li><a href="#" class="filter-item"><span class="label"><?= __('Prime 极速达') ?></span><span class="count">64</span></a></li>
                <li><a href="#" class="filter-item"><span class="label"><?= __('免运费') ?></span><span class="count">97</span></a></li>
                <li><a href="#" class="filter-item"><span class="label"><?= __('当日/次日达') ?></span><span class="count">23</span></a></li>
            </ul>
        </div>

        <div class="filter-group">
            <div class="filter-group-title"><?= __('品牌') ?></div>
            <ul class="filter-list">
                <li><a href="#" class="filter-item"><span class="label">Brand A</span><span class="count">56</span></a></li>
                <li><a href="#" class="filter-item"><span class="label">Brand B</span><span class="count">34</span></a></li>
                <li><a href="#" class="filter-item"><span class="label">Brand C</span><span class="count">19</span></a></li>
            </ul>
        </div>

        <div class="filter-group">
            <div class="filter-group-title"><?= __('颜色') ?></div>
            <ul class="filter-list">
                <li><a href="#" class="filter-item"><span class="label"><?= __('黑色') ?></span><span class="count">73</span></a></li>
                <li><a href="#" class="filter-item"><span class="label"><?= __('白色') ?></span><span class="count">41</span></a></li>
                <li><a href="#" class="filter-item"><span class="label"><?= __('蓝色') ?></span><span class="count">27</span></a></li>
            </ul>
        </div>

        <div class="filter-group">
            <div class="filter-group-title"><?= __('用户评分') ?></div>
            <ul class="filter-list">
                <li><a href="#" class="filter-item">
                    <span class="label"><span class="stars">★★★★★</span> <?= __('及以上') ?></span>
                    <span class="count">12</span>
                </a></li>
                <li><a href="#" class="filter-item">
                    <span class="label"><span class="stars">★★★★☆</span> <?= __('及以上') ?></span>
                    <span class="count">48</span>
                </a></li>
                <li><a href="#" class="filter-item">
                    <span class="label"><span class="stars">★★★☆☆</span> <?= __('及以上') ?></span>
                    <span class="count">96</span>
                </a></li>
            </ul>
        </div>
    </div>
</section>

<style>
.<?= $wc ?> {
    color: var(--weline-theme-text);
}

.<?= $wc ?> .filter-panel {
    background: var(--weline-theme-surface-raised);
    border-radius: var(--weline-radius-lg, 10px);
    padding: var(--weline-space-5, 20px);
    border: var(--weline-theme-border-width, 1px) var(--weline-theme-border-style, solid) var(--weline-theme-border);
}

.<?= $wc ?> .filter-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--weline-space-4, 15px);
    padding-bottom: var(--weline-space-4, 15px);
    border-bottom: var(--weline-theme-border-width, 1px) var(--weline-theme-border-style, solid) var(--weline-theme-border);
}

.<?= $wc ?> .filter-title {
    font-size: var(--weline-font-size-base, 16px);
    font-weight: 600;
    color: var(--weline-theme-text);
}

.<?= $wc ?> .filter-clear {
    background: transparent;
    border: 0;
    padding: 0;
    font-size: var(--weline-font-size-sm, 13px);
    color: var(--weline-theme-primary);
    cursor: pointer;
    text-decoration: underline;
    text-underline-offset: 2px;
}

.<?= $wc ?> .filter-applied {
    display: flex;
    flex-wrap: wrap;
    gap: var(--weline-space-2, 8px);
    margin-bottom: var(--weline-space-5, 20px);
}

.<?= $wc ?> .filter-chip {
    display: inline-flex;
    align-items: center;
    gap: var(--weline-space-1, 4px);
    padding: var(--weline-space-1, 4px) var(--weline-space-3, 10px);
    border-radius: var(--weline-radius-round, 999px);
    background: var(--weline-theme-surface-muted);
    border: var(--weline-theme-border-width, 1px) var(--weline-theme-border-style, solid) var(--weline-theme-border);
    font-size: var(--weline-font-size-sm, 13px);
    color: var(--weline-theme-text-muted);
}

.<?= $wc ?> .filter-chip.is-active {
    background: var(--weline-theme-primary-surface);
    border-color: var(--weline-theme-primary);
    color: var(--weline-theme-primary);
}

.<?= $wc ?> .chip-remove {
    background: transparent;
    border: 0;
    padding: 0;
    cursor: pointer;
    color: inherit;
    font-size: var(--weline-font-size-base, 16px);
    line-height: 1;
}

.<?= $wc ?> .filter-group {
    margin-bottom: var(--weline-space-5, 20px);
}

.<?= $wc ?> .filter-group:last-child {
    margin-bottom: 0;
}

.<?= $wc ?> .filter-group-title {
    font-size: var(--weline-font-size-sm, 14px);
    font-weight: 600;
    margin-bottom: var(--weline-space-3, 10px);
    color: var(--weline-theme-text);
}

.<?= $wc ?> .filter-list {
    list-style: none;
    margin: 0;
    padding: 0;
}

.<?= $wc ?> .filter-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--weline-space-3, 12px);
    padding: var(--weline-space-2, 8px) 0;
    color: var(--weline-theme-text-muted);
    text-decoration: none;
    font-size: var(--weline-font-size-sm, 13px);
    transition: color var(--weline-theme-transition-base, 0.2s ease);
}

.<?= $wc ?> .filter-item:hover {
    color: var(--weline-theme-primary);
}

.<?= $wc ?> .filter-item .count {
    color: var(--weline-theme-text-muted);
    font-size: var(--weline-font-size-xs, 12px);
}

.<?= $wc ?> .stars {
    color: var(--weline-theme-warning);
    letter-spacing: 1px;
}

.widget-preview-canvas .<?= $wc ?>,
.<?= $wc ?>.is-preview {
    min-height: 120px;
}
</style>

<script>
(function () {
    var root = document.querySelector('[data-uid="<?= $esc($scope->uid) ?>"]');
    if (!root) return;

    root.addEventListener('click', function (event) {
        var target = event.target;
        if (!(target instanceof Element)) return;

        var action = target.closest('.filter-clear, .filter-item, .chip-remove');
        if (!action || !root.contains(action)) return;

        event.preventDefault();
    });
})();
</script>
