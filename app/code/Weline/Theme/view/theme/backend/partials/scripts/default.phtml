<?php
/**
 * 片段：Backend Scripts - 默认风格
 *
 * 后端管理界面的公共脚本引用
 * 字段
 * @meta.name {default="Backend Scripts公共脚本",name="Scripts公共脚本片段",description="Scripts公共脚本片段，用于描述片段的名称"}
 * @meta.description {default="后端管理界面的公共脚本引用（bootstrap、metismenu、simplebar等）",name="公共脚本",description="后端管理界面的公共脚本引用，用于描述片段的用途"}
 * @param.class {default="",name="额外CSS类",description="额外CSS类，用于设置额外CSS类"}
 *
 * 参数配置说明：
 * - Partials 自己的 meta 数据：通过 $meta 或 $this->getData('meta') 访问
 * - Layout 的 meta 数据：通过 $layout 或 $this->getData('layout') 访问
 */
/** @var \Weline\Framework\View\Template $this */
$meta = $this->getData('meta') ?? [];
$layout = $this->getData('layout') ?? [];
$class = $meta['class'] ?? '';
?>
<!-- ========== Common Scripts Start ========== -->
<w:template>Weline_Admin::common/footer.phtml</w:template>
<!-- ========== Common Scripts End ========== -->
