<?php
/**
 * 片段：Backend Loading - 默认风格
 *
 * 后端管理界面的加载中组件
 * 字段
 * @meta.name {default="Backend Loading加载中组件",name="Loading加载中组件片段",description="Loading加载中组件片段，用于描述片段的名称"}
 * @meta.description {default="后端管理界面的页面加载中动画组件",name="加载中组件",description="后端管理界面的页面加载中动画组件，用于描述片段的用途"}
 * @meta.cache.mode {default="chrome",name="缓存模式"}
 * @meta.cache.auth {default="role",name="鉴权维度"}
 * @param.class {default="",name="额外CSS类",description="额外CSS类，用于设置额外CSS类"}
 *
 * 参数配置说明：
 * - Partials 自己的 meta 数据：通过 $meta 或 $this->getData('meta') 访问
 * - Layout 的 meta 数据：通过 $layout 或 $this->getData('layout') 访问
 */
/** @var \Weline\Framework\View\Template $this */
$meta = $this->getData('meta') ?? [];
$layout = $this->getData('layout') ?? [];
$class = $meta['class'] ?? '';
?>
<!-- ========== Loading Start ========== -->
<w:template>Weline_Admin::common/page/loading.phtml</w:template>
<!-- ========== Loading End ========== -->
