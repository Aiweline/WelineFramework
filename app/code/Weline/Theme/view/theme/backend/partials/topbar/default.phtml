<?php
/**
 * 片段：Backend Topbar - 默认风格
 *
 * 后端管理界面的顶部工具栏（Header）
 * 字段
 * @meta.name {default="Backend Topbar顶部工具栏",name="Topbar顶部工具栏片段",description="Topbar顶部工具栏片段，用于描述片段的名称"}
 * @meta.description {default="后端管理界面的顶部工具栏（包含Logo、搜索、语言切换、用户菜单等）",name="顶部工具栏",description="后端管理界面的顶部工具栏，用于描述片段的用途"}
 * @meta.cache.mode {default="chrome",name="缓存模式"}
 * @meta.cache.auth {default="user",name="鉴权维度"}
 * @param.class {default="",name="额外CSS类",description="额外CSS类，用于设置额外CSS类"}
 *
 * 参数配置说明：
 * - Partials 自己的 meta 数据：通过 $meta 或 $this->getData('meta') 访问
 * - Layout 的 meta 数据：通过 $layout 或 $this->getData('layout') 访问
 */
/** @var \Weline\Framework\View\Template $this */
$meta = $this->getData('meta') ?? [];
$layout = $this->getData('layout') ?? [];
$class = $meta['class'] ?? '';
?>
<!-- ========== Topbar Start ========== -->
<w:block class='Weline\Admin\Block\Backend\Page\Topbar'/>
<!-- ========== Topbar End ========== -->
