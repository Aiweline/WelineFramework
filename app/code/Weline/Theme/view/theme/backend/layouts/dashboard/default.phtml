<?php
/**
 * @meta.name {default="Weline UI 2.0 后台仪表盘",name="Weline UI 2.0 后台仪表盘"}
 * @meta.description {default="Native Weline dashboard shell and four semantic widget regions.",name="自研后台仪表盘布局"}
 * @param.showHeader {default=true,name="显示顶部栏"}
 * @param.showSidebar {default=true,name="显示侧栏"}
 * @param.showFooter {default=true,name="显示页脚"}
 * @param.showRightSidebar {default=true,name="显示设置抽屉"}
 * @param.class {default="",name="额外 Weline CSS 类"}
 *
 * @var \Weline\Framework\View\Template $this
 */
$themeConfig = \Weline\Framework\Manager\ObjectManager::getInstance(
    \Weline\Backend\Api\View\BackendThemeConfigInterface::class
);
$meta = is_array($this->getData('meta')) ? $this->getData('meta') : [];
$showHeader = ($meta['showHeader'] ?? true) !== false;
$showSidebar = ($meta['showSidebar'] ?? true) !== false;
$showFooter = ($meta['showFooter'] ?? true) !== false;
$showRightSidebar = ($meta['showRightSidebar'] ?? true) !== false;
$extraClasses = array_values(array_filter(
    preg_split('/\s+/', trim((string)($meta['class'] ?? ''))) ?: [],
    static fn(string $class): bool => preg_match('/^w-[a-z0-9_-]+$/', $class) === 1
));
$safe = static fn(mixed $value): string => htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" data-local="{{lang_local}}" data-lang="{{lang}}" <?= $themeConfig->getThemeHtmlAttributes() ?><?= $themeConfig->getThemeModel() === 'rtl' ? ' dir="rtl"' : '' ?>>
<head>
    <w:hook>Weline_Theme::backend::layouts::base::head-before</w:hook>
    <w:template>Weline_Admin::common/head.phtml</w:template>
    <w:block class="Weline\Theme\Block\Partials" area="backend" type="head" default-option="default"/>
    <w:hook>Weline_Theme::backend::layouts::base::head-after</w:hook>
    <w:hook>Weline_Theme::backend::layouts::dashboard::head-after</w:hook>
</head>
<body>
    <w:hook>Weline_Theme::backend::layouts::base::body-start</w:hook>
    <w:hook>Weline_Theme::backend::layouts::dashboard::body-start</w:hook>
    <w:block class="Weline\Theme\Block\Partials" area="backend" type="loading" default-option="default"/>

    <div class="w-backend-shell w-dashboard-shell">
        <?php if ($showHeader): ?>
            <w:hook>Weline_Theme::backend::layouts::base::topbar-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="backend" type="topbar" default-option="default"/>
            <w:hook>Weline_Theme::backend::layouts::base::topbar-after</w:hook>
        <?php endif; ?>

        <?php if ($showSidebar): ?>
            <w:hook>Weline_Theme::backend::layouts::base::sidebar-before</w:hook>
            <w:block class="Weline\Theme\Block\Partials" area="backend" type="sidebar" default-option="left"/>
            <w:hook>Weline_Theme::backend::layouts::base::sidebar-after</w:hook>
        <?php endif; ?>

        <div class="w-backend-main w-dashboard-main">
            <w:hook>Weline_Theme::backend::layouts::base::content-before</w:hook>
            <w:hook>Weline_Theme::backend::layouts::dashboard::content-before</w:hook>

            <?php
            $layoutMainClass = trim('w-backend-page ' . implode(' ', $extraClasses));
            echo $this->fetch('Weline_Theme::theme/backend/partials/layout/main-content.phtml', [
                'layoutMainClass' => $layoutMainClass,
                'layoutShowPageHeader' => false,
            ]);
            ?>

            <section class="w-dashboard-canvas" data-dashboard-layout-slots aria-label="<?= $safe(__('仪表盘部件区域')) ?>">
                <div class="w-dashboard-layout-grid">
                    <w:slot id="dashboard-summary"
                            name="Dashboard Summary"
                            accept="dashboard-slot-summary,dashboard-stat,dashboard-kpi,builder-component,builder-display,builder-container"
                            position="dashboard-summary"
                            append="true"
                            multiple="true"
                            max="4"
                            class="w-dashboard-region w-dashboard-region-summary w-dashboard-slot-summary">
                        <header class="w-dashboard-region-head">
                            <div>
                                <h2><?= $safe(__('指标区')) ?></h2>
                                <p><?= $safe(__('关键数值、KPI 与状态概览。')) ?></p>
                            </div>
                            <span><?= $safe(__('最多 4 个')) ?></span>
                        </header>
                        <div class="w-dashboard-empty">
                            <strong><?= $safe(__('指标区')) ?></strong>
                            <span><?= $safe(__('选择 KPI 或统计数字部件')) ?></span>
                        </div>
                    </w:slot>

                    <w:slot id="dashboard-analysis"
                            name="Dashboard Analysis"
                            accept="dashboard-slot-analysis,dashboard-chart,dashboard-trend,builder-component,builder-display,builder-container"
                            position="dashboard-analysis"
                            append="true"
                            multiple="true"
                            max="3"
                            class="w-dashboard-region w-dashboard-region-analysis w-dashboard-slot-stack">
                        <header class="w-dashboard-region-head">
                            <div>
                                <h2><?= $safe(__('主分析区')) ?></h2>
                                <p><?= $safe(__('趋势、图表与主要观察。')) ?></p>
                            </div>
                        </header>
                        <div class="w-dashboard-empty">
                            <strong><?= $safe(__('主分析区')) ?></strong>
                            <span><?= $safe(__('选择图表或趋势部件')) ?></span>
                        </div>
                    </w:slot>

                    <w:slot id="dashboard-side"
                            name="Dashboard Side"
                            accept="dashboard-slot-side,dashboard-status,dashboard-list,dashboard-stat,builder-component,builder-display,builder-container"
                            position="dashboard-side"
                            append="true"
                            multiple="true"
                            max="4"
                            class="w-dashboard-region w-dashboard-region-side w-dashboard-slot-stack">
                        <header class="w-dashboard-region-head">
                            <div>
                                <h2><?= $safe(__('侧栏洞察')) ?></h2>
                                <p><?= $safe(__('状态、短列表与异常提醒。')) ?></p>
                            </div>
                        </header>
                        <div class="w-dashboard-empty">
                            <strong><?= $safe(__('侧栏洞察')) ?></strong>
                            <span><?= $safe(__('选择状态或列表部件')) ?></span>
                        </div>
                    </w:slot>

                    <w:slot id="dashboard-detail"
                            name="Dashboard Detail"
                            accept="dashboard-slot-detail,dashboard-table,dashboard-list,builder-component,builder-display,builder-container,builder-form"
                            position="dashboard-detail"
                            append="true"
                            multiple="true"
                            max="6"
                            class="w-dashboard-region w-dashboard-region-detail w-dashboard-slot-stack">
                        <header class="w-dashboard-region-head">
                            <div>
                                <h2><?= $safe(__('明细区')) ?></h2>
                                <p><?= $safe(__('表格、排行与可展开列表。')) ?></p>
                            </div>
                        </header>
                        <article class="w-card dashboard-widget dashboard-widget-detail-snapshot" data-w-height="full">
                            <div class="w-card__header">
                                <h3 class="w-card__title"><?= $safe(__('默认明细')) ?></h3>
                                <span class="w-text" data-tone="muted" data-size="sm"><?= $safe(__('Dashboard')) ?></span>
                            </div>
                            <div class="w-card__body" style="--w-pt:0;--w-pb:0;">
                                <div class="w-table-wrap">
                                    <table class="w-table" style="--w-mb:0;">
                                        <tbody>
                                        <tr><th scope="row"><span class="w-text" data-tone="muted"><?= $safe(__('视图')) ?></span></th><td><strong class="w-text" data-weight="strong"><?= $safe(__('默认概览')) ?></strong><span class="w-text" data-tone="muted" data-size="sm" style="--w-mt:var(--weline-space-1);"><?= $safe(__('系统共享视图')) ?></span></td></tr>
                                        <tr><th scope="row"><span class="w-text" data-tone="muted"><?= $safe(__('布局')) ?></span></th><td><strong class="w-text" data-weight="strong"><?= $safe(__('4 个固定区域')) ?></strong><span class="w-text" data-tone="muted" data-size="sm" style="--w-mt:var(--weline-space-1);"><?= $safe(__('指标、分析、侧栏、明细')) ?></span></td></tr>
                                        <tr><th scope="row"><span class="w-text" data-tone="muted"><?= $safe(__('部件')) ?></span></th><td><strong class="w-text" data-weight="strong"><?= $safe(__('后台专用')) ?></strong><span class="w-text" data-tone="muted" data-size="sm" style="--w-mt:var(--weline-space-1);"><?= $safe(__('由后台统计部件提供')) ?></span></td></tr>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </article>
                    </w:slot>
                </div>
            </section>

            <w:hook>Weline_Theme::backend::layouts::dashboard::content-after</w:hook>
            <w:hook>Weline_Theme::backend::layouts::base::content-after</w:hook>

            <?php if ($showFooter): ?>
                <footer class="w-backend-footer w-cluster" data-justify="between">
                    <span><?= date('Y') ?> &copy; Weline.</span>
                    <a href="https://www.aiweline.com/">aiweline.com</a>
                </footer>
            <?php endif; ?>
        </div>
    </div>

    <?php if ($showRightSidebar): ?>
        <w:hook>Weline_Theme::backend::layouts::base::right-sidebar-before</w:hook>
        <w:block class="Weline\Theme\Block\Partials" area="backend" type="right-sidebar" default-option="default"/>
        <w:hook>Weline_Theme::backend::layouts::base::right-sidebar-after</w:hook>
    <?php endif; ?>

    <w:hook>Weline_Theme::backend::layouts::dashboard::body-end</w:hook>
    <w:hook>Weline_Theme::backend::layouts::base::body-end</w:hook>
</body>
</html>
