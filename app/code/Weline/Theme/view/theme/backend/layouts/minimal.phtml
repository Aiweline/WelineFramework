<?php
/**
 * 布局：后端 - 极简布局
 *
 * 后端极简布局，无header和footer，适合弹窗、打印等场景
 * 字段
 * @info description 描述：后端极简布局，无header和footer，适合弹窗、打印等场景
 * @info name 名称：极简布局
 * @param title 页面标题
 * @param content 内容（HTML字符串）
 * @param class 额外CSS类
 *
 * 参数配置由 ControllerFetchFileBefore Observer 自动加载到 meta 数组中
 * 模板中使用 meta.参数 输出内容，使用 meta.参数 在条件判断中
 * 页面标题由 Observer 自动设置（如果控制器未设置，则使用 meta.title 的默认值）
 */
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}">
<head>
    @template(Weline_Backend::templates/public/head.phtml)
</head>
<body class="<?= htmlspecialchars($meta['class'] ?? '') ?>">
    {{meta.content}}
</body>
</html>

