# 汇审：有部件必入声明槽验收（重复部件）

- 日期：2026-09-21
- 席位：项目经理 / 测试

## 首验 FAIL

PDP `/product/196`（bypass FPC）：`product-add-to-cart` / `product-buy-now` 各 **3** 个 `widget-wrapper`。
根因：Overlay 对同 slot id 的**每一空区域**都补一份（嵌套空占位被当成缺件）。

## 修复 2.2.498

任一区域已有该部件则不再注入；SlotFiller 二次扫改为「全无才缺」。

## 复验

- 购买槽内：加购根 **1**、立即买根 **1**（`widget-wrapper` 已去掉重复份）
- 快捷支付 testid **1**
- 另有 hidden failsafe 模板（`hidden`），非可见双份
- UT：RequiredDefaultInjection 契约 **13 PASS**
- 交付：`https://p05113ef3.test.weline.com:9555/product/196`（HTTP 200，FPC bypass）

## 结论

**PASS（去重后）**。旧「逐区必填」会复现重复前端部件，已纠正。
