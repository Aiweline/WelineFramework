# 停工（已解除 · 店面保证）

## 原矛盾

用户要求 required 默认注入一直出现，除非本版本认定卸载。旧规则把「没进已发布实体」当成空槽，并禁止店面渲染时补齐。

## 2026-09-20 用户拍板

> 除非人工卸载应用，否则就无关任何，只要有 slot 就要注入。

决议见：`meetings/设计共识-有槽必注.md`。

- 店面：采纳乙（渲染期 overlay / Contract::merge），实体缺节点不是省略理由。
- 卸载：版本化 `user_deleted@{versionId}`（丙语义）。
- 0012：收窄为「删除/卸载后编辑器不回尸」，不再挡 0036。

## 仍可跟进（不阻塞共识）

- 通用空 required 槽再扫、overlay 失败可观测、bake/snapshot 双保险对齐。
- 若将来要求「打开编辑器也对未卸载 required 静默写草稿」，须另开需求收窄 0012 写路径（当前**不**做）。
