# 汇审：真实测试闭环（槽清单先读 P0 · 2.2.506）

- 日期：2026-09-21
- 席位：项目经理 / 测试 / 后端
- MCP：本回合 `prepare_project` 曾 STALE/Not connected；已 ensure bounce；闭环以宿主 UT + 本机店面为准

## 1. 行为 UT

```text
php vendor/bin/phpunit --no-configuration --bootstrap app/bootstrap.php \
  app/code/Weline/Theme/test/Unit/LayoutEntity/RequiredDefaultInjectionInventoryPlannerTest.php \
  app/code/Weline/Theme/test/Unit/LayoutEntity/RequiredDefaultInjectionOverlayExecuteTest.php \
  app/code/Weline/Theme/test/Unit/LayoutEntity/RequiredDefaultInjectionContractTest.php \
  app/code/Weline/Theme/test/Unit/LayoutEntity/RequiredDefaultInjectionGhostSlotTest.php
```

**结果：20 tests / 91 assertions PASS**  
含：Inventory 嵌套闭包、Planner 拓扑序、Overlay 同槽空兄弟不去重扇出、`unfilled` 硬抛。

## 2. 运行时

- `php bin/w server:reload` 完成（2 workers）
- 探活：`https://p05113ef3.test.weline.com:9555/product/196`  
  - `x-wls-fpc-bypass: 1` → HTTP **200**（跳转 SEO URL）
- 购买槽（`product-purchase-actions` 边界内、剔 script）：
  - 加购根 **1**、立即买根 **1**
  - failsafe 未可见露出
  - express `data-testid` **1**
  - 无 `required_default_injection_*` 错误串
- 槽标记：`product-purchase-actions` open 标记 **1**（不再多区扇出）

**店面槽域判定：PASS**

## 3. Browser

- 打开 PDP；快照买盒可见 `Add to Cart` / `Proceed to checkout` / `Pay with PayPal`
- 列表中大量 Add to Cart 来自相关商品卡，**不计入**购买槽重复
- CDP `setCacheDisabled` 已发；后续 evaluate 时 tab 丢失 → 以 curl 槽域计数为权威闭环，Browser 作存在性旁证

## 4. 结论

**真实闭环 PASS（P0）**：计划注入 + 整槽 once 在本机 PDP 成立。  
未提交。P1（inventory bake/缓存）未纳入本闭环。
