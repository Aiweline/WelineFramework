# 架构冻结 — widget-static-assets-bake

**Verdict：有条件采纳（按用户属性契约施工）**

1. Bake 主路径：节点 `layout_source`/`source` ∪ 注册表 `@widget.layout_source`/`source` → sidecar JSON。  
2. Head 消费：`ThemeLayoutStorefrontHeadAssets`；挂 page 段（非 route-invariant assets-prefix）。  
3. layout 桶白名单：header/footer/navigation/chrome type 或 code allowlist。  
4. JS：仅 bake 清单 head script；业务模块仍 modules。  
5. 请求 Δ 辅路径保留接口，本波可先空实现。

`architect_joint=true` · 2026-09-23
