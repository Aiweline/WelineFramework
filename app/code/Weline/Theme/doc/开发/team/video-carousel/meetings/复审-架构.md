# 复审 · 架构（video-carousel）

- slug: `video-carousel`
- 席位: 架构师
- 轨: 合规复审
- 时间: 2026-09-22T00:14:00+08:00
- 对照权威: `contracts.md` / `surfaces.md` / `components.md`
- **stance: 同意**
- **verdict: pass**
- **result: closed**

> 只读审实现一致性；本席未改生产码。

---

## 1. 关键文件 / API 存在性（硬抽检）

| 合约 / 表面 | 路径或符号 | 结果 |
|---|---|---|
| S1 / §3 部件 | `app/code/Weline/Theme/view/theme/frontend/widgets/video/video-carousel/default.phtml` | **存在**；根 `data-site-block="video-carousel"` |
| S1 保留 C1 | `…/widgets/video/video-player/default.phtml` | **仍在**；未删未全局替换 |
| S1 / §3 双 homepage | Theme `layouts/homepage/default.phtml` + hanfu 同路径 | **两端** `accept` 含 `video-carousel`；else 默认 `name="video-carousel"`；无 `default_injections` |
| S2 / §2 ParamSchema | `Theme/Ui/ParamSchema/video_carousel_items.php` | **存在**；`base_type=array` + sortable + 项字段含 `product_ids=product_picker` |
| S2 登记 | `Theme/extends/module/Weline_Widget/Weline_Theme/widget.php` | **存在**；`type=video_carousel_items` 模板登记 |
| S3 / §1 ArrayType | `Widget/Ui/ParamType/ArrayType.php` `case 'product_picker'` → 委托 `ProductPickerType` | **存在** |
| S5 / §4 Catalog | `Product/Service/StorefrontProductWidgetCatalog::cardsByIds` | **存在**；保序、不强制 HF-* 丢品；`limit` 钳制 1…24 |
| S4 / C2–C3 | `VideoEmbedResolver::resolveBilibiliId` + `trustedEmbedHosts` + CSP 合同测 | **已扩** bilibili；部件模板复用同一 Resolver |
| S6 / C6–C7 | 部件内 `w-dialog` + `data-w-component="dialog"` + SSR `cardsByIds` → `<w:product:card>` | **一致**；JS 走 `Weline.UI.dialog.open`；无模板原生 `fetch` |

---

## 2. 与 surfaces / components 一致性

| 项 | 判定 |
|---|---|
| 无新建 Video 模块 / Model / 后台 CRUD | pass |
| homepage 仅换默认嵌件；`video-player` 可继续嵌 | pass |
| ArrayType 扩项内 picker；未在 Theme 平行第二套数组渲染器 | pass |
| Catalog 公开 `cardsByIds`；部件经 Catalog 消费，非模板 `new` Product Model | pass |
| 浮层强制复用 `Weline.UI.dialog`；商品卡强制 `<w:product:card>` | pass |
| 未复用 Compare quickview 单品内容布局 | pass |
| 轮播 chrome（S7 / C11） | **部件作用域** prev/next/dot（落在 N1），非全站级 carousel 库、非平行 modal；**未**开 `component-negotiate` 线程——记为流程缺口，**不**构成否决（未触 anti-pattern「全站级组件库 / 私造浮层」） |

---

## 3. 与 contracts 主路径

| 合约节 | 判定 |
|---|---|
| §0 槽 `#homepage-videos` / 选择器 / CTA 源串「查看关联商品」 | pass |
| §1 ArrayType + 编辑器 fallback 认 `product_picker` | pass（施工 channel D1 + 源码） |
| §2 ParamSchema + 部件 + Resolver/CSP + 登记 | pass（D2 + D346） |
| §3 双 layout 默认嵌件 | pass（字面一致，无 default/hanfu 漂移） |
| §4 `cardsByIds` | pass（D5；部件调用） |
| §5 轮播 + dialog JS；禁 alert / 禁原生 fetch | pass（只读抽检） |
| §6 Token / 视觉 | 不挡架构；交 UI/主题合规轨 |
| §7–§8 i18n / 测试执行证据 | 本席不代验；不挡架构同意 |

---

## 4. findings（非阻断）

1. **轮播 chrome 协商纪要缺失**：S7 预起草写「不足则 component-negotiate」；实现采用部件内轻量控件（合法 N1）。建议验收后由主题/UI 在 `components.md` 补一句「已选部件内轻量 chrome，无需全站库」或补空协商闭环；**不返工生产码**。
2. `cardsByIds` 默认 `$limit=12`（合约允许「或等价」；技术方案草案曾写 24）——能力边界可接受，UC 多卡意图未砍。

---

## 5. 结论

- **stance: 同意**（无否决、不 escalate）
- **verdict: pass**
- 实现与冻结 `contracts` / `surfaces` / `components` 主表面一致；关键能力 **video-carousel / cardsByIds / ArrayType∋product_picker / 双 homepage 嵌件** 均已落地。
- 通道回执：`channel/review-arch.md`
- **result: closed**
