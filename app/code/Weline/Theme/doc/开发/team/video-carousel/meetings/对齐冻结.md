# 对齐冻结会 · video-carousel

- 席位主持: **测试**
- 时间: 2026-09-21T22:30:00+08:00
- 仓库: `/Users/weline/Project/Official/框架`
- 通道: `channel/align-freeze.md`
- 规格: `doc/开发/spec/video-carousel.md`
- 探查: `meetings/立项-领域探查.md`
- 产物: `contracts.md` + `deps.md` + 本文件可执行 UC
- result: **closed**（三者已钉死；验收意图未改）

## 参会 / 表态

| 席位 | 表态 | 说明 |
|---|---|---|
| 测试（主持） | **同意冻结** | 可执行 UC + contracts + deps 已落盘；通道 `kind=stance` |
| 架构师 / 原型 / 前端 / 主题 / UI / 扩展点 / 查询 / i18n 等 | 待项目经理 resume 追加 stance | **不阻塞**本会钉死三者；异议须回会改契约，不得施工中私改 UC |

## 决议

1. **采纳**已拍板 Q1–Q7 / EARS；本会只升格可执行步骤与对接契约，**不改验收意图**。
2. 选择器与基址字面冻结如下；实现须满足断言，允许技术方案微调 class 名但须同步回本会修订选择器表。
3. 施工按 `deps.md` 唤醒；测试红灯骨架路径见下节。
4. 套件 id：`video-carousel-plan-suite`。

---

## 冻结选择器 / 基址

| 键 | 值 |
|---|---|
| BASE | `https://p05113ef3.test.weline.com:9555/` |
| 槽 | `#homepage-videos` |
| 部件根 | `[data-site-block="video-carousel"]` |
| 轮播下一张 | `[data-site-block="video-carousel"] [data-action="carousel-next"], [data-site-block="video-carousel"] .video-carousel__next, [data-site-block="video-carousel"] [aria-label*="下一"]`（实现定一主选择器后回填） |
| 当前项文案 | `[data-site-block="video-carousel"] [data-carousel-field="author"], [data-site-block="video-carousel"] [data-carousel-field="description"]` |
| CTA | 可见控件，accessible name / 文本含 `查看关联商品` |
| Dialog | `[data-w-component="dialog"].w-dialog[open], dialog.w-dialog[open], [data-w-component="dialog"][data-open="true"]` |
| 商品卡 | dialog 内 `[data-testid="weline-product-card"]` |

---

## 可执行 UC-1…UC-4（权威 · 喂 Playwright / WB-OP）

> 与规格 EARS 对齐；步骤可点选、可断言。环境失败记环境，不改意图。

### UC-1 首页默认视频轮播可见

| 字段 | 内容 |
|---|---|
| id | UC-1 |
| 名称 | 首页默认渲染 video-carousel |
| 角色 | 店面访客（匿名） |
| 前置 | 本机站点可访问；Theme default 或当前激活主题 homepage Hook else 默认嵌件为 `video-carousel`；至少可观察媒体或诚实空态 |
| 步骤 | 1. `page.goto('https://p05113ef3.test.weline.com:9555/')`，`waitUntil=domcontentloaded`<br>2. `locator('#homepage-videos').scrollIntoViewIfNeeded()`<br>3. `expect(#homepage-videos [data-site-block="video-carousel"]).toBeAttached({timeout:20000})`<br>4. `expect` 部件根内作者 **或** 简介 **或** 区标题之一 `toBeVisible` 且 `innerText` trim 非空；**或**诚实空态节点可见（二者择一，禁止空白失败占位）<br>5. 源码合同（可同套件）：homepage layout 含 `name="video-carousel"`；该槽 Hook else **不是**默认唯一 `name="video-player"`<br>6. `expect(body).not.toContainText(/Fatal error\|ParseError\|WLS Runtime Error/i)` |
| 期望 | 首页视频区为轮播部件；无 JS fatal |
| 映射 | e2e `video-carousel-plan-suite` / WB-OP-首页视频区 |

### UC-2 轮播切换多项

| 字段 | 内容 |
|---|---|
| id | UC-2 |
| 名称 | 多视频项轮播切换 |
| 角色 | 店面访客 |
| 前置 | `video_carousel_items` ≥2；两项作者或简介文本可区分；已保存前台可见 |
| 步骤 | 1. `goto` 同上 BASE<br>2. 滚动 `#homepage-videos`，定位 `[data-site-block="video-carousel"]`<br>3. 读取当前项可见作者或简介文本 → 记为 `A`<br>4. `click` 下一张控件（冻结选择器表）<br>5. `expect` 作者或简介文本变为 `B`（`B !== A`），**或** `data-index` / `aria-current` 幻灯索引变化<br>6. 无整页强制 `reload` 要求；无 fatal |
| 备选 | 仅 1 项：控件可隐藏；本 case `test.skip` + 依赖造数，**不改**「多项可切换」意图 |
| 映射 | e2e 轮播交互章 |

### UC-3 查看关联商品对话框（主成功 · 真实通路）

| 字段 | 内容 |
|---|---|
| id | UC-3 |
| 名称 | 关联商品 Weline.UI.dialog 多卡 |
| 角色 | 店面访客 |
| 前置 | 当前项 `product_ids` ≥2 且商品前台可见；CTA「查看关联商品」可见 |
| 步骤 | 1. `goto` BASE<br>2. 滚动至轮播，确保目标项为当前项（必要时先 UC-2 切换）<br>3. `click` 文本/name 含「查看关联商品」的控件<br>4. `expect` 打开态 dialog（冻结 Dialog 选择器）`toBeVisible`<br>5. `expect` dialog 内 `[data-testid="weline-product-card"]` **count ≥ 2**<br>6. 关闭 dialog（Close 按钮或 `Weline.UI.dialog` 关闭控件）→ `expect` dialog **不可见** / 非 open<br>7. 断言未出现原生 `alert`（监听 `dialog` 事件 count=0）；无 fatal |
| 禁止 | 仅断言 CTA 存在、仅断言 dialog 节点在 DOM 但未 open、卡数&lt;2 冒充通过 |
| 映射 | e2e 关联商品弹层章；合规抽检商品卡 |

### UC-4 无关联商品诚实行为

| 字段 | 内容 |
|---|---|
| id | UC-4 |
| 名称 | 空 product_ids 不伪造成功列表 |
| 角色 | 店面访客 |
| 前置 | 存在一项 `product_ids` 为空或全部失效 |
| 步骤 | 1. `goto` BASE（或带预览参数的等价前台页）<br>2. 切换到无关联商品项<br>3. **分支 A**：CTA「查看关联商品」`toHaveCount(0)` 或 `not.toBeVisible`<br>**分支 B**：CTA 可点 → dialog 可见且内文为诚实空态，**且**商品卡 count=0<br>4. 控制台无未捕获异常；无原生 `alert` |
| 期望 | 不展示虚假商品网格 |
| 映射 | e2e 空关联边界章 |

---

## 红灯骨架（路径说明）

对齐冻结后、施工开跑即可落盘；**执行**待可测面就绪。禁止改生产实现骗绿。

### e2e 草稿（已建空骨架）

| 路径 | 说明 |
|---|---|
| `app/code/Weline/Theme/test/e2e/frontend/video-carousel-plan-suite.spec.js` | plan-suite 草稿；UC-1…UC-4 `test.fix`/`moduleCase` 占位，实现前保持红/skip，施工后摘 skip 变绿 |

### 待写 / 待改合同测名（施工波按 deps 增量）

| 测名（建议） | 对应 |
|---|---|
| `ThemeHanfuHomepageDefaultsContractTest`（改断言 `video-carousel`） | D6 / UC-1 |
| `VideoCarouselItemsParamSchemaContractTest`（新建） | D3 |
| `ArrayTypeProductPickerItemFieldContractTest`（新建或扩 Widget） | D1 |
| `VideoEmbedResolverTest` + `ThemeVideoEmbedCspContractTest`（扩 bilibili） | D2 |
| `VideoCarouselWidgetContractTest`（新建：根 data-site-block / dialog 壳） | D4 |
| `StorefrontProductWidgetCatalogCardsByIdsTest`（新建 · Product） | D5 |

### Browser WB-OP 待跑名

- `WB-OP-首页视频区-video-carousel`
- `WB-OP-关联商品-dialog-多卡`

打开验收 Browser：**必须** `Network.setCacheDisabled`；交付后关 Browser。

---

## 异议保留

无（主持冻结）。他席若否决选择器或 Catalog 签名，走通道 `kind=stance` + 回本会修订，**禁止**偷改 UC 多卡/dialog 意图。
