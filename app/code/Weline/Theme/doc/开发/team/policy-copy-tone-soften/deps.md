# deps — policy-copy-tone-soften

```text
advisor-ops-brief (closed escalate)
    └─→ req-clarify 并入顾问约束 → align_freeze_ready
            └─→ theme-copy（主题开发工程师）
                    └─→ i18n-dict（翻译工程师）
                            └─→ ops-accept（电商顾问 waiting_acceptance）
                                    └─→ 测试抽检（可选轻量）→ PM 汇审
```

并行：`req-clarify` 并入与 `theme-copy` 可同波启动（brief 已齐）；`i18n-dict` 必须等 theme-copy closed（源串+中英 CSV 落盘后）。
