# 汇审：主题编辑器「返回」同 URL / 编辑器 referrer

- **结论**：通过
- **改动**：`ThemeEditor/index.phtml` 返回链：referrer 与当前 URL 相同，或 referrer 路径仍是 `theme-editor`（含 `_t` rewrite）时，不写 `data-return-href`，回退 `href`（主题列表）
- **证据**：`ThemeEditorBackReturnUrlContractTest` PASS；Browser 点击「返回」落到 `theme/backend/index`
- **e2e**：已补 `theme-editor-back-return.spec.js`；本机 runner 登录 bootstrap 失败（环境），逻辑以 UT+WB 收口
