# 汇审：画布禁链只拦跳转不拦点选（2.2.488）

- 日期：2026-09-20
- 结论：**通过**
- 需求语义：禁链 = `a[href]` **禁止默认跳转**；点击须继续冒泡，父级/部件选中可监听。

## 改动

- `editor-mode.js` / `theme-preview.js` / `theme-editor.js`：禁链守卫去掉 `stopPropagation`，保留 `preventDefault`
- `editor-mode.css`：去掉多余 `}`；禁链仅改光标
- 工具栏 title + i18n；契约 `ThemeEditorUiCapabilityContractTest`
- e2e 增补 `link-block prevents a navigation but still selects the widget`（本机 `loginAsAdmin` FPM bootstrap 暂失败，与基线用例同因）
- `resource:compile welineUi`；本机 `theme_static_version` 已刷新以便验收吃到新包

## 证据

| 项 | 结果 |
|---|---|
| UT `testSelectionTargetModesDefaultSlotWidget` | PASS（73 assertions） |
| Browser WB-OP（禁缓存） | 开启禁链后点分类菜单 `a`：`navigated=false`，`selected=true`，`parentSelected=1`，捕获阶段 `stopped=false` |
| 交付探活 | `https://p05113ef3.test.weline.com:9555/.../theme-editor?...` → 302（需登录）可接受 |

## 未阻断说明

- `php bin/w e2e:run … --grep link-block` 与同文件基线用例均因 Admin session bootstrap FPM 未登录失败；正式用例已落盘，环境修复后应 PASS。
