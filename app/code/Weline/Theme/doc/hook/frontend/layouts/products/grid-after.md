# 产品列表网格之后

Hook: `Weline_Theme::frontend::layouts::products::grid-after`

在产品列表网格之后触发。

## Implementation

Contributing modules implement this hook under `view/hooks/` by mapping `::` to directories and keeping templates thin.
