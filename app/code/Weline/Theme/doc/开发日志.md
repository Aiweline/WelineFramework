## Hotfix — 2026-09-01：页头页脚默认继承全局 + 显式脱离（2.2.76）

- 确认：用户确认默认跟随全局 chrome；仅显式「本布局独立」切断；可「恢复全局继承」。
- 实现：
  - `SharedChromeService`：`inherit`/`local`、写路径改写到 homepage 载体、`detach`/`restore`
  - ThemeEditor：`chrome-mode` / `detach-chrome` / `restore-chrome`；继承态 save/config/remove 回落全局载体
  - 编辑器槽/部件配置面板：全局徽章 + 独立/恢复操作
- 契约：`SharedChromeInheritContractTest`、`ThemeEditorUiCapabilityContractTest` API 属性
- 静态戳：`20260901-shared-chrome-inherit-v1`
- 备注：本会话 `HOST_MCP_SESSION_CATALOG_STALE`（缺 `submit_task_plan`）→ 精确路径原生编辑
- 版本：`2.2.75` → `2.2.76`

## Hotfix — 2026-09-01：部件配置保存 theme_scope_revision_conflict（2.2.75）

- 问题：主题编辑器「保存配置」双 toast：`保存失败` + `theme_scope_revision_conflict`
- 根因：
  1. `postSaveWidgetConfig` 已 `updateWidgetConfig` 升 revision，客户端再 `queueWidgetConfigOwnership` 用旧 revision 二次写入
  2. `bindAccordionFormEvents` 与 document submit 双重绑定，同一次点击并发两次保存
- 修复：返回 `scoped_workspace`；默认语言只 `syncLayoutWorkspaceAfterServerMutation`；`requestSaveWidgetConfig` 串行 + 冲突一次重试；去掉表单级重复 submit
- 静态戳 `20260901-widget-config-revision-v2`（覆盖并发会话 stamp）
- 契约：`ThemeEditorScopedWidgetConfigContractTest` / `ThemeEditorUiCapabilityContractTest`
- 备注：`HOST_MCP_SESSION_CATALOG_STALE`（缺 `submit_task_plan`）→ 精确路径原生编辑
- 版本落地：代码合入后随 `2.2.76` 同仓；模块号以 `etc/module.php` 为准
## Hotfix — 2026-09-01：页头页脚默认继承全局 + 显式脱离（2.2.74）

- 状态：并入 `2.2.76` 交付（中间被 `2.2.75` 配置保存冲突热修占用版本号）。

## Hotfix — 2026-09-01：顶栏问候显示名（2.2.73）

- 问题：登录后「您好, email」过长。
- 变更：`widgets/header/account` 与 `hooks/header-account` 改用 `Customer::getDisplayName()`（非 `session->getUsername()` 鉴权邮箱）；问候行补 ellipsis。
- 版本：`2.2.72` → `2.2.73`。

## Hotfix — 2026-09-01：mega-menu 无子分类展示 Banner/描述（2.2.72）

- 需求：无子分类时右侧面板展示当前侧栏分类 Banner + 描述；有子分类时默认先 Banner/描述再子分类；有子分类时是否展示可由部件参数控制
- 实现：
  - `mega-menu-panel.phtml` 增加 intro（banner + 描述/summary），移除「暂无子分类」空文案
  - `category-menu` / Header 增加 `show_banner_with_children`（默认 true）
  - `MenuTreeNormalizer` 透传 `banner`/`summary`；片段缓存指纹纳入 banner 字段
- 契约：`MegaMenuPanelBannerContractTest`、`AllMenuNavTreeContractTest`

## Hotfix — 2026-09-01：顶栏收藏移到账户左侧（2.2.71）

- 变更：`partials/header/default.phtml` 的 `user-area` 内将 `header-wishlist-icon` 置于 account 之前，顺序为货币后 → 收藏 → 账户 → 订单 → 购物车。
- 验证：Wishlist 顶栏 Hook 契约测试；前台首页顶栏。

## Hotfix — 2026-09-01：顶栏分类对接万能分类真实树（2.2.70）

- 问题：顶栏横向分类仍显示「电子产品/服装服饰」等 `defaultNavItemsFactory` 假数据
- 根因：无 catalog 回落路径；且曾误用 AllMenu 页种子覆盖横向条
- 修复：`HeaderCommerceData::resolveCategoryNavItems()` 软依赖 `StorefrontAllMenuCategoryTreeService`；横向条优先 catalog；抽屉仍优先 AllMenu 发布树；演示树仅 `editorPreviewLight` / 部件 `preview_mode`
- 契约：`HeaderCommerceDataTest`
- 备注：`HOST_MCP_SESSION_CATALOG_STALE`（缺 `submit_task_plan`）→ `HOST_MCP_NOT_ATTACHED` 精确路径原生编辑

## Hotfix — 2026-09-01：页头「我的账户」下拉菜单空白（2.2.69）

- 问题：登录后悬浮「我的账户」只见「欢迎回来」与邮箱，个人中心链接空白
- 根因：`theme.css` 全局 `.dropdown-menu { display:none }` 面向 `.dropdown` 触发；账户部件内层 `ul.dropdown-menu` 被误藏；且未挂 `header-account-links`
- 修复：内层菜单强制 `display:block` + 静态定位覆盖；默认菜单改为 `<w:hook>header-account-links</w:hook>`（Customer/Shipping/Wishlist 等贡献）
- 契约：`ThemeHeaderMobileAmazonContractTest`
- 备注：`HOST_MCP_SESSION_CATALOG_STALE`（缺 `submit_task_plan`）→ `HOST_MCP_NOT_ATTACHED` 精确路径原生编辑

## Hotfix — 2026-09-01：多语言弹出层长列表无法滚动（2.2.68）

- 问题：语言很多时弹出层底部被裁切，无法下滑；「申请语言」不可达
- 根因：`document` capture `scroll` 把面板内滚动当成视口变化 → `positionFloating` 清坐标并设 `pending`（闪隐）与 `max-block-size` 抖动，打掉 `scrollTop`
- 修复：忽略浮层内部 scroll；已可见面板更新时不进 pending，并快照恢复滚动；菜单改为搜索/列表/底栏三分区（申请入口钉底）
- 契约：`ThemeColorModeContractTest`；编译 `resource:compile welineUi`
- 备注：`HOST_MCP_SESSION_CATALOG_STALE`（缺 `submit_task_plan`）→ `HOST_MCP_NOT_ATTACHED`

## Hotfix — 2026-09-01：mega-menu 深色侧栏类目字对比度（2.2.68）

- 问题：家居用品等分栏菜单左侧深色栏上「家具/家纺」等字近乎看不见
- 根因：`w-surface-body` / `[data-surface=body]` 面板与链接规则强制 body 深色字，特异性高于 `.mega-menu-sidebar-item`
- 修复：mega 面板改 on-dark 字色；`a` 规则排除侧栏项并补高特异性浅色；右侧 `.mega-menu-panels` / `.category-link` 仍深字
- 备注：`HOST_MCP_SESSION_CATALOG_STALE`（缺 `submit_task_plan`）→ `HOST_MCP_NOT_ATTACHED` 精确路径原生编辑

## Hotfix — 2026-09-01：页头页脚全局 chrome 语义对齐（2.2.67）

- 确认：用户明确页头/页脚是全局的，一改都改，不属于某个布局
- 纠正：不得表述为「属于 homepage」；homepage workspace 只是可视化当前持久化全局 chrome 的载体
- 变更：`mergeSharedChromeSlotWidgets` 覆盖 `header`+`footer`；注释/需求/契约同步「全局 chrome」语义
- 备注：`HOST_MCP_SESSION_CATALOG_STALE`（缺 `submit_task_plan`）→ `HOST_MCP_NOT_ATTACHED`

## Hotfix — 2026-09-01：非首页共享全局页脚 chrome（2.2.66）

- 问题：可视化发布后博客/产品/搜索/购物车等页仅空壳 `weline-footer--shell`；首页有 `footer-container`
- 根因：全局页脚部件落在 chrome 持久化载体（homepage workspace）；非首页按 pageType 取布局时未合并全局 chrome 槽
- 修复：`SlotRendererService::mergeSharedChromeSlotWidgets` 在本页 chrome 槽为空时合并全局载体；本页已有部件不覆盖
- 契约：`SlotRendererSharedChromeFooterContractTest`
- 备注：`HOST_MCP_SESSION_CATALOG_STALE`（缺 `submit_task_plan`）→ `HOST_MCP_NOT_ATTACHED` 精确路径原生编辑

## Hotfix — 2026-09-01：布局 slot 继承读取短路（双页脚根因）（2.2.65）

- 问题：可视化发布后前台两个完整页脚；Store Release 同时含 Website 与 Store 各自 `ADD_NODE` 的 `footer-container`
- 根因：继承合并按 `node_uid` 叠加，本层已占用的 slot 仍混入父级同槽节点（继承链读错）
- 修复：`ThemePatchEngine::apply` 对本层 `ADD_NODE` 占用的 `slot_id` 整槽短路，effective 只留本层 ADD；字段级 SET 不切断整槽
- 契约：`ThemePatchEngineTest`（整槽剥离 / 字段 CoW 保留兄弟 / 仅匹配 slot）
- 备注：`HOST_MCP_SESSION_CATALOG_STALE`（缺 `submit_task_plan`）→ `HOST_MCP_NOT_ATTACHED` 精确路径原生编辑；已发布 Release 需 republish 烘焙

## Feature — 2026-09-01：Frontend Dom 微核（属性门控按需）

- 协同 Frontend：`dom` / `welineDom` 按需微核；规范表 §7 增行
- 触发：`data-weline-when` / `data-weline-on` 或 `data-weline-load="dom"`；无标记不拉脚本
- 权威：本文件 §7；`Frontend/doc/开发日志.md`
- 备注：`HOST_MCP_SESSION_CATALOG_STALE` → 原生精确编辑

## Hotfix — 2026-09-01：部件库头部筛选布局挤叠（2.2.64）

- 问题：部件库 header HTML 已改三行，但 `.panel-header` 仍横向 flex，且缺 type-chip CSS，筛选标签重叠错位
- 修复：`.widget-library-panel-header` 改纵向；补齐 actions / type-filters / chip（含 `is-active`）；缓存戳 `header-v3`
- 备注：`HOST_MCP_NOT_ATTACHED` 精确路径原生编辑

## Feature — 2026-09-01：水墨品牌配色盘 ink（2.2.63）

- 需求：配色盘增加中国水墨风格色系
- 美学：墨分五色字阶、宣纸画布 `#F7F4EF`、朱砂主色 `#B84A3C`、石青链接、石绿/赭石/胭脂/花青状态色
- 变更：新增 `frontend/colors/_ink.css`、`backend/colors/_ink.css`（`palette_role=brand`，`@meta.theme=ink`）
- 备注：宿主 MCP 会话缺 `submit_task_plan`（`HOST_MCP_SESSION_CATALOG_STALE`）→ `HOST_MCP_NOT_ATTACHED` 精确路径原生编辑

## Hotfix — 2026-09-01：主题版本=可视化编辑器已发布名（2.2.62）

- 问题：用户要的是 Theme 可视化「已发布」版本名，不是维护波次 / scope release（`theme_editor_publish` / `r142`）
- 修复：`ThemePublishedVersionRuntimeResolver` 改读 `ThemeLayoutVersionService::getPublishedVersion` → `version_id` + `getDisplayName()`（如「原始布局」）；按 store→website→default 身份链
- 备注：`HOST_MCP_NOT_ATTACHED` 精确路径原生编辑

## Hotfix — 2026-09-01：主题版本标签用人读修订号（2.2.61）

- 问题：维护页显示 `Theme theme_editor_publish`；用户误以为未发布。实际已有 release `#22`（父 `#21`），`theme_editor_publish` 只是发布 reason 机器码
- 修复：`releaseLabel` 优先人工 summary / `r{revision_no}`，禁止 snake_case reason（含 `theme_editor_publish`）；传播 release 追溯父级修订号
- 备注：`HOST_MCP_NOT_ATTACHED` 精确路径原生编辑

## Hotfix — 2026-09-01：前端主题版本=后台发布版本（2.2.60）

- 问题：维护波次/页展示 `Theme 2.1.x` 误用 `Weline_Theme` 模块版本
- 修复：版本权威仅 `theme_scope_release`（`ThemePublishedVersionRuntimeResolver`）；CLI/无 Request 时优先 `default.__store__/.__website__`；`parent_release_propagation` 向上追溯用户发布 reason
- 协同：`Weline_Maintenance` 1.1.5 删除 `Theme/etc/module.php` 回退
- 备注：宿主 MCP 缺 `submit_task_plan`（`HOST_MCP_NOT_ATTACHED`）

## Hotfix — 2026-09-01：主题编辑器清理 Theme 缓存按钮（2.2.59）

- 问题：预览偶发旧 HTML / 健康检测误报，需手动失效运行时缓存
- 变更：预览工具栏「恢复原始布局」旁增加「清理 Theme 缓存」；`POST clear-theme-cache` 走 `ThemeRuntimeCacheCleaner`（有 Scope 用 clearScopedCaches，否则 clearNonGlobalCaches）+ `clearVersionPreviewCaches`；清理后刷新预览；不改布局草稿
- 静态戳：`20260901-clear-theme-cache-v1`；契约 `ThemeEditorUiCapabilityContractTest`
- 备注：宿主 MCP 缺 `submit_task_plan`，记 `HOST_MCP_NOT_ATTACHED`

## Hotfix — 2026-09-01：Hero 可视化高度自我完全（2.2.58）

- 问题：可视化里 Hero 高度被压扁（发布态正常）；部件 CSS 不应被编辑器外壳影响
- 根因：`preview_mode && !editor_mode` 把 height 压成 `120px`，且样式选择外部 `.widget-preview-canvas`
- 修复：hero-slider 始终用配置 `height`（默认 500px）；删除紧凑预览分支与外部选择器；库卡片缩略交给编辑器 canvas+scale
- 约定：部件开发指南补充「CSS 自我完全」；契约 `testHeroSliderKeepsSelfContainedFullHeight`

## Hotfix — 2026-09-01：编辑器拖入 Hero 上下白缝（2.2.57）

- 问题：主题编辑器拖入「Hero 轮播图」后上下对称白缝，橙色条偏矮
- 根因：SlotRenderer 给编辑器 iframe 写入 `preview_mode=true`，hero-slider 误当作部件库卡片压缩为 `height:120px`；再叠加 section `padding-block` 形成上下空白
- 修复：仅「无 editor_mode」的紧凑预览压高度；`editor_mode` 全页预览用真实 height；homepage `:has(hero-slider|promo-banner)` 零 padding 兜底
- 契约：`ThemeHomepageWidgetWidthContractTest::testHeroSliderKeepsFullHeightInEditorLivePreview`（已被 2.2.58 自我完全策略取代）

## Hotfix — 2026-09-01：移除 SlotRenderer 旧 Dom 槽位引擎（2.2.56）

- 问题：Boundaries 与 Dom 双路径并存；缺 `@weline-slot` 时 soft-fallback Dom，可能丢 style/script、抬出 wrapper 子树
- 变更：`doProcessSlots` 仅走 `processSlotsWithBoundaries`；缺 markers → `SlotBoundaryRequiredException`；删除 `processSlotsWithDom` / `processSlotFragmentWithDom` / `processSlotElement` 等 Dom fill；`parkDomOpaqueBlocks` 仅保留给 `stampFinal` 健康巡检
- 契约：`SlotRendererCowMultipleSlotContractTest`、`SlotRendererDomOpaqueBlocksTest`；文档 `widget-slot-system.md`

## Hotfix — 2026-09-01：默认首页 Hero Banner 顶白缝（2.2.55）

- 问题：默认 Banner（homepage-hero / hero-slider）与上方分类导航之间有明显白缝
- 根因：Shell B homepage `.homepage-section` 统一 `padding-block`，全宽 Hero/Promo 仍吃到纵向 padding
- 修复：`.homepage-hero` / `.homepage-promo` 显式 `padding-block: 0`；契约 `ThemeHomepageWidgetWidthContractTest::testHomepageHeroAndPromoSectionsHaveZeroBlockPadding`；文档 `theme-layout-content-width.md` 补充 Homepage Shell B 特例

## Hotfix — 2026-09-01：部件库首屏空列表（预览 ParseError 隔离）（2.2.54）

- 问题：首屏部件库显示「当前主题暂无可用部件」；通用/基础计数 0，仅应用 Tab 有数据
- 根因（Browser 实测）：`loadWidgetLibrary` 一次拉取 limit=50 时，`content/ai-reconstruct` 等部件预览触发 `ParseError`；`buildWidgetPreviewHtml` 仅 `catch (Exception)` 未捕获 Error，整批 `editorRequest` 失败
- 修复：`buildWidgetPreviewHtml` 外层/内层均 `catch (Throwable)`，单部件预览失败降级为占位，不阻断 catalog；保留 2.2.53 general↔basic Tab 仅 visibility 切换
- 验收：Browser 首屏 通用 27 / 基础 23 / 应用 15；切换 Tab 计数不丢失

## Hotfix — 2026-09-01：部件库通用/基础 Tab 计数与全量渲染（2.2.53）

- 问题：点「基础」23 个后再点「通用」，「基础」计数变 0；首屏仅「应用」有部件
- 根因：2.2.51 在 general↔basic 切换时 `reloadWidgetLibrary` 并带 `library_tabs` 单 Tab 过滤，DOM 只剩当前 Tab 项
- 修复：general↔basic 仅 `applyWidgetLibraryTabVisibility`；全量 catalog 一次加载；应用 Tab 往返才 reload；Chip 视觉与 API 过滤解耦
- 静态戳：`20260901-widget-library-tab-count-v1`

## Hotfix — 2026-09-01：多部件槽 CoW 保留 mini-cart / 去重 wishlist（2.2.52）

- 问题：收藏夹应用后双收藏、mini-cart 消失（退货已恢复）
- 根因：merger 过滤空壳模板块时被误删 mini-cart；layout wishlist 追加与 hook 重复渲染
- 修复：multiple 槽仅 tombstone 删除；空壳模板块 hydrate 渲染；wishlist 布局注入替换 hook 而非追加
- 静态戳：`20260901-cow-multiple-slot-v2`

## Hotfix — 2026-09-01：多部件槽 CoW 保留 hook/兄弟 HTML + 部件库 Tab 切换重载（2.2.51）

- 问题：wishlist-icon 应用后 user-area 内「退货」链接与 mini-cart 消失；通用/基础 Tab 切换后列表为空
- 根因：CoW 合并后误 `$isExclusive=true` 整槽替换；general↔basic 切换仅改 CSS 隐藏未重新请求 `library_tabs`
- 修复：`spliceCowMergedMultipleSlotInner` 保留非模板 HTML；`setWidgetLibraryTab` 在 Tab 变更时 `reloadWidgetLibrary`
- 契约：`SlotRendererCowMultipleSlotContractTest`

## Hotfix — 2026-09-01：应用 Tab 已安装卡片卸载与预览定位（2.2.50）

- 需求 ID：`REQ-THEME-0014` 应用 Tab 交互补全
- 能力：已应用卡片显示「卸载」；点击卡片在 iframe 预览 scrollIntoView + 结构树选中；后端 `resolveAppliedLayoutNode` 返回 `node_uid`/`removable`
- 静态戳：`20260901-default-injection-locate-v1`；契约 `ThemeEditorUiCapabilityContractTest::testDefaultInjectionAppliedCardsSupportUninstallAndPreviewLocate`

## Hotfix — 2026-09-01：多部件槽模板直嵌不误拦应用部件（2.2.49）

- 需求 ID：`REQ-THEME-0014` 运行时修复
- 问题：原始布局「应用」Tab 角标 1 — `wishlist-icon` 报「目标插槽已有模板内联部件」；`user-area` 为 multiple 槽，Theme 直嵌 account/mini-cart 不应整槽拦截其它声明；手动点「+」仍失败因页面缓存旧 API/JS
- 修复：`slotBlocksInjectionDueToTemplateInline` — 独占槽仍整槽拦截；multiple 槽仅拦截与直嵌同 code；手动 apply 已应用时刷新列表；静态戳 `20260901-template-inline-multiple-v1`
- 验收：homepage `getDeclaredForLayout` 无 template_inline blocker；`wishlist-icon` 可 apply；契约 `WidgetDefaultInjectionWishlistMultipleSlotTest`

## Hotfix — 2026-09-01：多部件槽模板直嵌不误拦应用部件（2.2.48）

- 需求 ID：`REQ-THEME-0014` 运行时修复
- 问题：原始布局「应用」Tab 角标 1 — `wishlist-icon` 报「目标插槽已有模板内联部件」；`user-area` 为 multiple 槽，Theme 直嵌 account/mini-cart 不应整槽拦截其它声明
- 修复：`slotBlocksInjectionDueToTemplateInline` — 独占槽仍整槽拦截；multiple 槽仅拦截与直嵌同 code；契约 `WidgetDefaultInjectionTemplateInlineContractTest`
- 验收：homepage `getDeclaredForLayout` 无 template_inline blocker；`wishlist-icon` 可 `applyInjectionByKeyResult`

## Hotfix — 2026-09-01：页脚硬链与扩展槽同构裸 a（2.2.46）

- 需求 ID：`REQ-THEME-0016` 样式收口
- 当前状态：`implemented`
- 问题：「关于我们」「供应商合作」自开 `<ul class="footer-section__list">`，浏览器默认圆点+缩进，与扩展槽「我要推广」等裸 `<a>` 不一致
- 修复：硬链改为 `footer-section__link` 裸 a，与扩展槽同列于 `footer-section__links`；去掉 list；CSS 戳 `footer-container-v2`
- 验收：`FooterContainerWidgetContractTest` / `FooterAboutLinksSlotContractTest`
- 备注：宿主 MCP 缺 `submit_task_plan`，记 `HOST_MCP_NOT_ATTACHED`

## Hotfix — 2026-09-01：REQ-THEME-0016 整页脚主部件 footer-container（2.2.45）

- 需求 ID：`REQ-THEME-0016`
- 当前状态：`implementing`
- 变更：
  1. 重写 `footer-container` 为配置驱动整页脚（分组开关/增删、分项、法律、社交、版权、语言货币）
  2. 标准四组固定扩展槽迁入部件模板；`partials/footer` 变独占槽薄壳
  3. `FooterDefaultLinksHelper` 增加 key/enabled/分项归一化；新增 ParamSchema `footer_link_*` / `footer_legal_links` / `footer_social_items`
  4. `footer-container` required 默认安装到 `footer`；`footer-links` 标记弃用
- 备注：宿主 MCP 缺 `submit_task_plan`，记 `HOST_MCP_NOT_ATTACHED`，原生改已知路径
- 验收：`FooterContainerWidgetContractTest` 与页脚槽契约测试

## Hotfix — 2026-09-01：PDP 槽位重复 + 最近浏览空 HTML（2.2.45）

- 需求 ID：`REQ-THEME-0016`（运行时修复）
- 当前状态：`implemented`
- 问题：DEV toast「最近浏览 @product-recently-viewed: 部件 HTML 为空」；红框 `w:slot 标签 id 重复: product-purchase-actions`（`unknown:0`）
- 根因：
  1. `product-info` 子槽 `product-purchase-actions` 经动态属性走 `renderRuntimeTag`；请求级 Slot 注册表在 CoW/健康修复重渲染时未重置
  2. `recently-viewed` 无浏览记录时直接 `return`，健康检测报 `empty_html`
- 修复：`doRenderWidget` 开头 `Slot::clearRegisteredSlots()`；最近浏览输出 `recently-viewed-empty` 空态壳
- 验收：`SlotTaglibCompileStateTest` 双次运行时渲染 + `RecentlyViewedEmptyShellContractTest`
- 备注：宿主 MCP 缺 `submit_task_plan`，本轮记 `HOST_MCP_NOT_ATTACHED`，原生改已知路径

## Hotfix — 2026-09-01：REQ-THEME-0015 AI 虚拟部件制造与检索（2.2.44）

- 需求 ID：`REQ-THEME-0015`
- 当前状态：`implemented`
- 变更：
  1. TE-CAP-021 悬停 AI 改绑 Theme Ai SSE → `postPublish`（返回 `data.widget`）→ `placeWidgetFromProvider(replace)`；不再调用 VirtualTheme `block-action` / fork `layout_option`
  2. 新增 `prepare-refine`；虚拟部件 refine 复用 `revertVersion`；非虚拟 create + `reference_component_code`
  3. replace 前浅合并旧 config；field i18n / 旧 `node_uid` 不迁移（follow-up）
  4. 部件库搜索类型 chip（通用/基础 与 应用* 互斥）+ AI 附加筛选；`WidgetLibraryTabResolver` 服务端同规则；卡片 AI 徽章
  5. 编辑器接线 `data-api-theme-ai-*`；静态戳 `20260901-theme-ai-component-v1`
- 验收：契约 `ThemeEditorUiCapabilityContractTest` / `ThemeAiPublishPlaceableContractTest` / `WidgetLibraryTabResolverTest`；用例全文见 `doc/验收/2026-09-01-REQ-THEME-0015-测试用例.md`（browser P0 尚未实跑）

## Hotfix — 2026-09-01：默认安装竞态 / 删除 revision 冲突 / 热应用缺资源（2.2.43）

- 需求 ID：`REQ-THEME-0014` 运行时修复
- 根因：
  1. `remove-widget` 已写 scoped `remove_node`，客户端再 `queueRemovedLayoutNode` 用旧 `expected_revision` → `theme_scope_revision_conflict`
  2. `loadScopedWorkspace` fire-and-forget reconcile 与手动删除/一键安装并发抢 revision
  3. 新部件仅 DOM 注入 `preview_html`，`sanitize` 剥掉 link/script，CSS/JS/`data-weline-load` 未生效
- 修复：删除后只同步 `scoped_workspace`；对账串行入队；一键/单点应用同步 revision 并 `loadLayoutPreview`；冲突一次重试
- 静态戳 `20260901-scope-hot-apply-v1`

## Hotfix — 2026-09-01：应用卡安装失败可点弹详情（2.2.42）

- 需求 ID：`REQ-THEME-0014` 体验补强
- 变更：应用 Tab「安装失败」条可点击弹出 `dialog.alert` 详情（人话摘要 + 错误码/标识/插槽等）；右上角关闭仍 dismiss；机器码映射可读文案；双 JS/双 CSS 同步；静态戳 `20260901-blocker-detail-v1`
- 备注：宿主 MCP 缺 `submit_task_plan`，本轮记 `HOST_MCP_NOT_ATTACHED`，原生改已知路径

## Hotfix — 2026-09-01：Theme 绿field 审查计划最终收口（2.2.41）

- **结论**：审查计划 todos **全部 completed**；无计划内 pending
- **门禁证据汇总**：
  1. Browser Store：写路径 / 版本 `debt-gate-store-20260901`（当前+已发布）/ 前台 200
  2. `theme_layout` / `theme_virtual_layout` 表 **不存在**（framework `tableExist`）
  3. phase-1：`saveSourceVersion` → `status=theme_virtual_layout_missing`（ObjectManager 冒烟复验：`asset_available=NO`）
  4. phase-2：控制器提前门禁 + `virtual_layout_asset_available` + 编辑器 JS 前置提示
- **范围外（不阻塞）**：VirtualTheme / 相关文件**物理拆除**、跨 iframe 真指针 DnD（已有 drop bridge）
- 注：本机曾出现并发 `setup:upgrade` 文件门禁占用；模块文件版本已为 **2.2.41**，静态戳 `…-virtual-gate-v1`

## Hotfix — 2026-09-01：VirtualTheme phase-2 UI/控制器门禁（2.2.41）

- 证据：`w_theme_virtual_layout` / `theme_layout` **表不存在**（framework `tableExist`）
- **服务**：公开 `isVirtualLayoutAssetAvailable()`
- **控制器**：`create-draft` / `block-action` / `save-source` / `publish` / `rollback` 提前返回 `theme_virtual_layout_missing`（避免无表仍打 AI）；`ai-catalog` 下发 `virtual_layout_asset_available`
- **编辑器 JS**：`ensureVirtualLayoutAssetAvailable()`；部件 AI / 锁定虚拟布局保存前置提示
- 静态戳 `20260901-theme-editor-virtual-gate-v1`；契约扩展 `GreenfieldVirtualLayoutAssetFailClosedContractTest`
- **计划**：追加 `debt-virtual-theme-ui-gate` → completed（控制器全栈拆除仍属更后阶段，非本轮）

## Hotfix — 2026-09-01：REQ-THEME-0014 应用级部件默认安装与恢复（2.2.41）

- 当前状态：`implemented`
- 需求 ID：`REQ-THEME-0014`
- 变更：
  1. `required:true` = UI「默认安装」自动装；`required:false` =「推荐」仅手动；决策查找与 injection_key 同维（含 slot/area）
  2. 方案 A：layout draft ready 后 `reconcile-required-defaults`；一键 `apply-required-defaults`；恢复原始回填 required
  3. 应用 Tab 卡片复用部件库 `preview_html` 画布；安装失败挂卡片 + Tab 角标（无 toast）
  4. Widget 账本 created|updated 且含 default_injections 均进入对账（仅 required）
- 验收：契约 `WidgetDefaultInjectionRequiredContractTest`；清库/upgrade 后应用卡可见预览；homepage 页脚扩展槽可被默认安装回填

## Hotfix — 2026-09-01：debt-virtual-layout-drop 另案 phase-1（2.2.40）

- 定案：`theme_virtual_layout*` 已在 `drop_legacy_theme_layout_20260831-v2.2.0` DROP + SchemaDiffExcluded；**资产/版本 API fail-closed**；**SystemConfig 布局选择**（`saveLayoutSelection` 等）保留
- **落地**：`ThemeVirtualLayoutService::virtualLayoutAssetTableExists` + `status=theme_virtual_layout_missing`；门禁 `saveSourceVersion` / `copy` / `publish` / `rollback` / runtime resolve / `listLayoutOptions` / load* 私有读
- **契约**：`GreenfieldVirtualLayoutAssetFailClosedContractTest`
- **计划**：`debt-virtual-layout-drop` → **completed（phase-1）**；删除 VirtualTheme 控制器/AI UI 全栈另开 phase-2（非本债硬依赖）
- i18n：`虚拟布局资产表已移除，请使用主题编辑器 scoped 工作区`

## Hotfix — 2026-09-01：debt-js-layout-id-compat 再缩减（2.2.39）

- 当前状态：布局绿field 完成；本债务收口为「非 hex fallback 保留、hex 不再双匹配」
- **变更**：
  1. `dataLayoutIdSelector`：32-hex 只返回 `[data-node-uid=…]`（去掉 `:is(..., [data-layout-id])`）
  2. `findWidgetsInSlot`：去掉冗余 `.widget-wrapper[data-layout-id]` 专行（由 `WIDGET_IDENTITY_MATCH` 覆盖非 hex）
  3. `readWidgetIdentityFromElement`：从遗留 hex `data-layout-id` 恢复 `nodeUid`
  4. 静态戳 `20260901-theme-editor-debt-layout-id-v2`；模块 **2.2.39**
- **契约**：`testThemeEditorJsPrefersNodeUidIdentityAttrs` / AssetInjector / UiCapability **OK**
- **计划**：`debt-js-layout-id-compat` → **completed**（共享 matcher 内非 hex `data-layout-id` 为有意保留）

## Hotfix — 2026-09-01：完成门禁 — 版本保存 + 发布闭环（布局绿field 完成）

- 当前状态：**布局绿field 完成**（主链路 + Browser 操作员门禁已举证；残留债务不阻塞本宣告）
- **证据（Store `default.__store__.default`）**：
  1. 「保存版本」→ 名称 `debt-gate-store-20260901` → 版本历史出现该条目；顶栏版本按钮同名
  2. 「发布」→「Confirm publish」→ `Publish`；版本卡片徽章 **当前** + **已发布**
  3. 前台 `https://p05113ef3.weline.test:9555/` curl **200**（含 Home）
  4. 先序写路径冒烟仍有效：编辑锁恢复、布局配置、选槽添加/删除 `product-carousel`
- **计划打标**：`debt-browser-operator-reverify` / `debt-greenfield-complete-gate` → **completed**
- **残留（不阻塞布局绿field）**：`debt-js-layout-id-compat`（DOM 兼容选择器仍多）；`debt-virtual-layout-drop` 另案；跨 iframe 真指针 DnD 靠 drop bridge（Cursor Browser 原生 DnD 不可靠）

## Hotfix — 2026-09-01：Browser 刷新后编辑锁恢复 + 写路径冒烟（partial → 已被上条收口）

- 当时状态：`partial`（`debt-browser-operator-reverify` 解除 blocked，发布/版本仍待）
- 用户指示可刷新后：
  1. Store 编辑器刷新 →「正在确认编辑权限」→ **锁成功**（无只读阻断）
  2. Page Title=`Home`；点「保存布局配置」可交互
  3. 结构视图选 `homepage-featured` → 库搜 `product-carousel` →「添加到当前插槽」落盘 `node_uid=fab5a4a3dab94976d6ee8af10b43d793`
  4. 「删除」→「确认删除」后 iframe 内 `product-carousel` 计数归 0
- **后续**：发布/版本见上条 Hotfix（门禁已关）

## Hotfix — 2026-09-01：绿field 债务入计划并开工（2.2.38，partial）

- 当前状态：`partial`
- **计划**：已写入 [`theme_绿field_审查_79ff25ec.plan.md`](~/.cursor/plans/theme_绿field_审查_79ff25ec.plan.md)「收口债务」todos 并打标
- **本步完成**：
  1. `debt-integration-mock-cleanup`：**completed** — PreviewFlow / UpdateConfig 改为 scoped 源码契约
  2. `debt-legacy-api-stubs`：**completed**（定案保留 stub）
  3. `debt-js-layout-id-compat`：**in_progress** — 配置弹层/锚点 hex 不再写入 `layout_id`；静态戳 `20260901-theme-editor-debt-layout-id-v1`
  4. `debt-browser-operator-reverify`：**in_progress/blocked** — Store 编辑器已登录可开，但「编辑锁服务暂时不可用」只读阻断写路径全量复验
- 契约：GreenfieldLegacy + PreviewFlow + UpdateConfig + AssetInjector **OK**（8 tests）
- **未宣告完成**（`debt-greenfield-complete-gate` 仍 pending）

## Hotfix — 2026-09-01：绿field 收口 — 去掉 getWidgetByLayoutId 调用链 + 冒烟（2.2.37，partial）

- 当前状态：`partial`（生产路径已无 `getWidgetByLayoutId`；定向契约/HTTP 冒烟通过；Browser 全量操作员复验与「全部完成」仍未宣告）
- **本步变更（2.2.36 → 2.2.37）**：
  1. `ThemeEditor::postRenderWidget`：删除数字 `layout_id` → `getWidgetByLayoutId` 分支；仅 `node_uid` scoped 草稿 + module/code 库预览回退
  2. E2E `theme-editor-fixture`：注入成功判定改为仅 `node_uid`
  3. 契约 `GreenfieldLegacyLayoutIdApiContractTest` 增补：编辑器无 `layoutService->getWidgetByLayoutId`、fixture 断言
- **冒烟证据**：
  - PHPUnit：GreenfieldLegacy / FixtureSweep / RemoveWidgetScoped / ScopedWidgetConfig **OK**（4 tests）
  - curl：`/` `/search` `/cart` → **200**；编辑器 Store/Global → **302**（需登录）
- **仍未完成**：登录态 Browser 全量操作员复验；Integration 测例里对 `getWidgetByLayoutId` 的 mock 可后续清理；不宣告绿field「全部完成」

## Hotfix — 2026-09-01：绿field 收口 — legacy layout_id 外壳 + node_uid payload（2.2.36，partial）

- 当前状态：`partial`（计划主链路 + 本轮 legacy 数字 ID 写外壳 / JS payload 收口；全量 E2E「全部完成」仍未宣告）
- **本步变更（2.2.35 → 2.2.36）**：
  1. `ThemeLayoutService`：`deleteWidget` / `updateWidgetConfig` / `getWidgetByLayoutId` / 排序移动 / orphan / tombstone 等数字 `layout_id` API 改为 fail-closed stub（保留签名）
  2. `ThemeEditor`：推荐注入无 `node_uid` 时 fail-closed；`buildPreviewHtmlForLayoutId` 恒返回 null
  3. 编辑器 JS：`buildWidgetIdentityPayloadFields` 有 hex `node_uid` 时 `layout_id=0`（不再把 hex 误当数字）；静态戳 `20260901-theme-editor-node-uid-payload-v1`
  4. 契约：`GreenfieldLegacyLayoutIdApiContractTest`
- **表策略（明确）**：
  | 表/模型 | 策略 |
  |---------| |------|
  | `w_theme_layout` / `ThemeLayout` | 已 DROP；Model `SchemaDiffExcluded` 占位 |
  | `theme_component*` / `ThemeComponent*` | **保留**：`ThemeAiDraftService` / 目录 / theme_component 渲染仍依赖 |
  | `theme_virtual_layout*` | 已 `SchemaDiffExcluded`；DROP 另案，非本轮布局绿field 硬依赖 |
  | 无前缀历史孤儿（若库中残留） | 运维清库 / Factory Reset；不随 SchemaDiff 自动复活布局行 |
- **计划对照更新**：`delete-dualwrite` / `editor-node-uid` 再推进一层；仍 `partial`（全量 E2E 实跑与「全部完成」宣告另开）

## Hotfix — 2026-09-01：绿field 计划审查 + fixture/Dashboard 去 legacy（2.2.35，partial）

- 当前状态：`partial`（计划主链路大多已落地；本轮收口 fixture sweep / Dashboard projectDraft）
- **计划 vs 现实（审查举证）**：

| 计划 todo | 状态 | 证据 |
|-----------|------|------|
| runtime-scoped-read | 完成 | runtime 读 `theme_scope_release`；前台可渲染 |
| editor-node-uid | 基本完成 | PreviewResolver 无 legacy 投影；JS 仍有 `layout_id` 兼容读（38 处） |
| version-facade | 完成 | `w_theme_layout_version` 作 facade；版本面板可用 |
| injection-seeder-scoped | 完成 | 注入走 `ThemeScopedLayoutWriteService` |
| delete-dualwrite-migration | 基本完成 | LegacyAdapter/Migration/Migrate **已删**；`projectDraft` 为 no-op |
| schema-drop-2.2.0 | 完成 | `w_theme_layout` 已 DROP；Model 仍 SchemaDiffExcluded 占位 |
| factory-reset-ui | 完成 | Factory Reset + 用户授权清库已执行过 |
| copy-meta-i18n | 完成 | Global「网站、店铺和渠道」文案；`theme.frontend/backend` 分域 |
| test-fixture-sweep | **本轮推进** | Dashboard/WidgetDemo fixture 改 scoped；不再 fail-closed 要 `theme_layout` |
| Browser DnD 验收 | 完成 | drop bridge + 防静默丢（2.2.33/2.2.34） |

- 本步变更：
  1. `DashboardViewService`：去掉 `projectDraft`；空态 seed 走 scoped `dashboard_scoped_layout_seed`
  2. Dashboard / WidgetDemo E2E fixture：`fixture_clear_scoped_layout` / snapshot 读 workspace
  3. `initDraftFromPublished` 标 `@deprecated`（已无 ThemeEditor 调用）
  4. 契约 `GreenfieldPlanFixtureSweepContractTest` OK；版本 `2.2.34` → `2.2.35`
- **仍未完成**：删空 `ThemeLayoutService` legacy 写方法外壳 / 缩减 JS `layout_id` 兼容；`theme_component*` / 无前缀孤儿表策略；全量 E2E 实跑与「绿field 全部完成」宣告

## Hotfix — 2026-09-01：绿field — 跨 iframe 放置「不得静默丢」（2.2.34，partial）

- 当前状态：`partial`（跨 iframe 真指针落盘 + 防静默丢已通；绿field 计划其它节点另计）
- 用户反馈：按计划松手在预览上**不应静默丢失**
- 加固（`2.2.33` → `2.2.34`）：
  1. `resolveDropAtPoint`：`collectSlotsAtPoint` 向上收集 + 同点矩形补扫，优先最深可接收插槽（避免外层 `header` 吞掉放置）
  2. 父页 bridge `drop`：无 candidate 时 toast「无法放入当前位置…」，禁止静默失败
  3. 静态 `?v=20260901-theme-editor-drop-no-silent-v1`
- Browser 证据（Store）：`browser_drag` → `homepage-featured`，rev **101→102**；冒烟节点已清理
- **DnD 操作员路径**：结构视图真指针 / iframe 协议 / 父页 bridge 真指针 / 防静默丢 — **本项收口**
- **仍未完成（计划对照）**：fixture 全量 sweep、文档三件套与绿field「全部完成」宣告前的其它举证项

## Hotfix — 2026-09-01：绿field — 跨 iframe 父页 drop bridge（2.2.33，partial）

- 当前状态：`partial`（跨 iframe **真指针**库→预览落盘已通；绿field 全量操作员闭环仍非全部完成）
- 根因：Chromium/Electron 跨 iframe HTML5 `DataTransfer`/`dragover` 常丢失，iframe 内无法建立 `drop-candidate`
- 修复（`2.2.32` → `2.2.33`）：
  1. 父页 `#previewDropBridge`：库拖拽期间盖住预览 iframe，事件留在父文档
  2. 预览 `Weline.Theme.Preview.resolveDropAtPoint`：按坐标命中插槽并回传 candidate
  3. dragend 无 candidate 时再 hit-test 兜底；静态 `?v=20260901-theme-editor-drop-bridge-v1`
- Browser 证据（Store）：
  1. Cursor `browser_drag`：`product-carousel` → 预览 `homepage-featured`，workspace **rev 99→100**
  2. 冒烟节点已清理；Page Title=`Home`
  3. 契约：`testPreviewDropBridgeKeepsLibraryDragEventsInParentDocument` + AssetInjector **OK**
- **仍未完成**：绿field 其它非 DnD 收口项以计划对照为准（legacy 清理等）；人手实机手感可再抽检

## Hotfix — 2026-09-01：绿field — 真指针拖放收口（partial）

- 当前状态：`partial`（同文档结构视图真指针拖放已通；跨 iframe 真指针仍受自动化 HTML5 DataTransfer 限制）
- 本步完成（有证据，版本仍为 `2.2.32`，无代码 bump；冒烟节点已清理）：
  1. Cursor `browser_drag`：部件库 `product-carousel` → 结构视图「内容」区，workspace rev `90→91`，节点 `slot=content`
  2. 跨 iframe：同工具拖到预览 frame 坐标**未落盘**（`drop may not have been accepted` / rev 不变）——与先前结论一致，跨 frame 仍依赖预览侧 `widget-dropped` 协议路径
  3. Page Title 仍为 `Home`；清理后无残留 carousel
- **仍未完成**：实机人手跨 iframe 真指针手感（自动化环境无法可靠触发 iframe HTML5 drop）

## Hotfix — 2026-08-31：绿field — 跨 iframe 放置 + 插槽排序冒烟（partial）

- 当前状态：`partial`（主写路径/添加/删除/标题/iframe 放置协议/插槽排序已通；操作系统级真指针拖入 iframe 未用实机指针完成）
- 本步完成（有证据，版本仍为 `2.2.32`，无代码 bump；冒烟节点已从 Store 草稿清理）：
  1. 跨 iframe：须由预览 frame 以 `source=weline-theme-preview` + `type=widget-dropped` 投递（父窗口 `postMessage` 会被 `e.source !== previewFrame` 拒收）
  2. Store：`product-carousel`→`homepage-featured`（rev 83→84），`category-grid`（84→85）
  3. 插槽排序：`/update-sort` 返回「排序已更新」（rev 85→86），同槽 `sort_order` 反转成功
  4. 清理后 featured 槽无残留 smoke 节点；Page Title 仍为 `Home`；版本按钮仍为 `browser-accept-store-20260831`
- **仍未完成**：操作系统级真指针从部件库拖入 iframe 的手感验收（协议路径已通）

## Hotfix — 2026-08-31：绿field — 删除闭环 + 布局配置保存（2.2.32，partial）

- 当前状态：`partial`（写路径/添加/拖放/标题/删除 API 已通；跨 iframe 真指针拖放全量仍未收口）
- 根因与修复：
  1. `buildWidgetDeletePayload` 未带 `scope`/`editor_context`（与 `saveWidget` 不一致）→ Store 删除常回「删除失败」；结构视图可残留 ghost 节点
  2. `removeScopedLayoutNodeFromWorkspace`：节点已不在草稿时改为幂等成功；存在时走 `ThemeScopedLayoutWriteService::removeWidget`
  3. 布局配置表单默认仍渲染部件「保存配置」→ 易误点触发「缺少布局节点 UID」；改为 `save_button => false`，仅保留「保存布局配置」
  4. 添加成功后刷新 scoped layout workspace，降低 ghost 概率
- Browser 证据（Store）：
  1. Page Title=`Home`；布局表单动作仅「保存布局配置」（无部件「保存配置」）
  2. 添加 `promo-banner` → workspace `draft_revision` 80 且有节点；UI 删除后 revision 81、promo 节点归零
  3. 静态 `?v=20260831-theme-editor-delete-scope-v1`；契约测试 OK
- 版本：`2.2.31` → `2.2.32`
- **仍未完成**：跨 iframe 真指针拖放 / 插槽内排序全量操作员验收

## Hotfix — 2026-08-31：绿field — 原生拖拽放置冒烟（partial）

- 当前状态：`partial`（三层 Scope 写路径 + 添加按钮 + 库→画布 HTML5 拖放已通；跨 iframe/插槽内排序全量仍未收口）
- 本步完成（有证据，版本仍为 `2.2.31`，无代码 bump）：
  1. Store 编辑器：`DataTransfer` + `dragstart`/`dragover`/`drop` 将「促销横幅」(`promo-banner`) 拖入「内容」预览区，预览部件 `8→9`，节点出现在 `.preview-widget-item`
  2. 拖放会即时写入编辑草稿（不能仅靠整页重载丢弃）；前台发布态未污染：首页仍 `Home | Homepage Default`，hero `data-node-uid=732319b9…`；页上 `promo-banner` 为模板槽 `homepage-promo` 的默认模板部件（无本次拖入的 `35817bd6…`）
  3. Page Title 探针：配置面板输入已改回 `Home`，但「保存布局配置」夹杂 `缺少布局节点 UID`（表单无 `node_uid`）与 `Layout config draft validated`，**持久化未确认**
- **仍未完成**：跨 iframe 真实指针拖放、插槽内排序/删除闭环稳定验收；子 Scope 布局配置 `node_uid` 缺失导致标题探针难清

## Hotfix — 2026-08-31：绿field — 添加到当前插槽冒烟（partial）

- 当前状态：`partial`（三层 Scope 写路径 + 选中插槽后添加部件已通；原生拖拽全量仍未收口）
- 本步完成（有证据，版本仍为 `2.2.31`，无代码 bump；冒烟后未保存/未发布，已整页重载丢弃草稿）：
  1. 须先点选预览区插槽（如「内容」）→ toast「已选中插槽: 内容区域」后，「添加到当前插槽」才可点
  2. 独占容器：`Content 容器` → toast「已替换到 content」（替换语义）
  3. 非独占：`Hero 轮播图` → toast「添加成功」，预览区部件数 `7→8`
  4. 注意：编辑锁未就绪时部件库会显示「当前主题暂无可用部件」；`data-legacy-scope-readonly=0` 时 DOM 仍可能残留 hidden 旧作用域文案（非写阻断）
- **仍未完成**：原生拖拽放置全量操作员验收；配置面板子 Scope 探针标题清理

## Hotfix — 2026-08-31：绿field — Store Scope 保存/发布（partial）

- 当前状态：`partial`（Global / Website / Store 写路径均已通；拖拽全量仍未收口）
- 本步完成（有证据，版本仍为 `2.2.31`，无代码 bump）：
  1. Store（`default.__store__.default`，identity `store_mode=normal`）锁可写、`legacy=0`
  2. 继承 workspace（`draft_revision_id=null`，`effective_release_id=4`）下保存版本 `browser-accept-store-20260831`（`version_id=15`）成功
  3. Browser「发布」后 `published_version_id=15`、`is_published=true`；版本按钮显示该名称
  4. 前台首页仍 `Home | Homepage Default`，`data-node-uid=3`，无探针污染
- **仍未完成**：部件「添加到当前插槽」需先选中插槽（无选中时 `pointer-events:none`）；拖拽/增删全量操作员验收；配置面板 Page Title 在子 Scope 仍可能显示历史探针文案（前台未污染）

## Hotfix — 2026-08-31：绿field — Website Scope 保存/发布（2.2.31，partial）

- 当前状态：`partial`（Global + Website 写路径已通；store/拖拽全量仍未收口）
- 根因与修复：
  1. Website 纯继承 workspace：`draft_revision_id/published_release_id` 为空但有 `effective_release_id` + 继承 `draft_payload` → `scopedLayoutSnapshot` 误抛 `theme_scoped_layout_workspace_missing`
  2. `buildLayoutVersionIdentityPayload` 把 Global/Website 的 `store_mode` 强成 `normal`，会破坏 `ScopeIdentity`（Website 不允许携带 store_mode）
- 本步完成（有证据）：
  1. Website（`default.__website__.default`）保存版本 `browser-accept-website-2.2.31`（`version_id=11`）成功
  2. Browser「发布」后 `published_version_id=11`、`is_published=true`
  3. 前台首页仍 `Home | Homepage Default`，`data-node-uid=3`，无探针污染文案
  4. 契约：`testScopedLayoutSnapshotAllowsInheritedEffectivePayload` + `testEditorPayloadStoreModeKeepsNullForGlobalAndWebsite`；CopyScoped 相关 **OK**
- 版本：`2.2.30` → `2.2.31`
- **仍未完成**：store Scope 写路径；拖拽/部件增删全量操作员验收；Website 配置面板曾短暂显示探针标题（前台未污染）

## Hotfix — 2026-08-31：绿field — Browser Global 保存→发布写路径（partial）

- 当前状态：`partial`（Global 写路径已通；全量操作员闭环仍未收口）
- 诊断更正（勿再误判）：
  1. Legacy alert / `#themeScopeConflictPanel` 可在 **hidden** 时仍出现在 a11y 文本中；CLI `legacy_readonly=false`；`data-legacy-scope-readonly="0"`；保存按钮未 disabled
  2. `PreviewContextService::DEFAULT_SCOPE=default` 目录化为 website，不等同 Global；写测须显式 `scope=default.default.default`
  3. 编辑器 API 走 `editorRequest`，不是裸 `window.fetch`，fetch 钩子会漏报
- 本步完成（有证据）：
  1. Global 下 UI「保存版本」落盘：版本列表含 `browser-accept-global-20260831` / `ui-save-probe` 等；当前版本按钮显示 `ui-save-probe`
  2. UI「发布」确认后：`version_id=10` `ui-save-probe` **`is_published=true`**，`published_version_id=10`
  3. 前台首页仍为 `Home | Homepage Default`，`data-node-uid=3`（含 hero `732319b9…`），`data-layout-id=0`
  4. CDP 直调 `save-version` 须带完整 Global `ScopeIdentity`（`website_id/codes/store_mode=null` + `context_version=v1`）；残缺 payload 会报缺字段 / 「三段必须全空」
- 版本：仍为 `2.2.30`（本步无代码 bump）
- **仍未完成**：website/store Scope 写路径；拖拽/部件增删全量操作员验收；配置面板 Page Title 曾被探针对话框误写入 `browser-accept-ui-save`（草稿侧污染，需回看是否已物化）

## Hotfix — 2026-08-31：绿field — Browser 操作员验收（partial，已被上条更正）

- 历史记录：曾误判 legacy/conflict 文案为写阻断；实际 Global 保存/发布可写。详见上条。

## Hotfix — 2026-08-31：绿field 继续 — 非默认语言继承 default locale 布局 Release（2.2.30）

- 当前状态：`partial`
- 宿主：无 `submit_task_plan` → `PLAN_REQUIRED` 后精确路径原生编辑；未宣称 greenfield 全量完成
- 根因：前台 `RequestContext::locale=zh_Hans_CN` 写入布局 identity；`publishedState` 只沿 Scope 父链回退、不回退 locale→`default` → 种子布局（locale=default）未命中 → `slotWidgets` 空 → 仅保留模板 `data-weline-template-widget`，零 `data-node-uid`（`maybeWrap` 在 CLI 单部件渲染已正常）
- 本步完成（有证据）：
  1. `ThemeEditorContext::withLocale`
  2. `ThemeScopedWorkspace::publishedState`：layout/meta 在走 Scope 父链前先查同 Scope 的 `locale=default` Release
  3. CopyScoped 增补 `testPublishedLayoutInheritsDefaultLocaleBeforeScopeParents`
- **仍未完成**：Browser 操作员全量交互验收
- 版本：`2.2.29` → `2.2.30`
- 验收：CopyScoped **21/21**（216）；升级完成；首页 `node_uid=3`、search/cart 各 `1`；裸编辑器 **302**；`hex_as_layout_id=0`

## Hotfix — 2026-08-31：绿field 继续 — scoped 部件无 layout_id 也包 wrapper（2.2.29）

- 当前状态：`partial`
- 根因：清库后节点仅有 `node_uid`、无数字 `layout_id`；`maybeWrapWidgetHtml` 旧条件 `$layoutId === ''` 跳过包装 → 前台 HTML 零 `data-node-uid`
- 本步完成（有证据）：
  1. `maybeWrapWidgetHtml`：有合法 `node_uid` 或 `layout_id>0` 即包 `.widget-wrapper`
  2. CopyScoped 增补 `testMaybeWrapWidgetHtmlPrefersNodeUidIdentity`
- **仍未完成**：Browser 操作员全量交互验收
- 版本：`2.2.28` → `2.2.29`
- 验收：见本回合契约 / 升级 / 首页·search 身份属性计数

## Hotfix — 2026-08-31：用户授权再次执行清库 Factory Reset（2.2.28）

- 当前状态：`partial`（清库与重装已执行；绿field 非全部完成）
- 用户授权：`执行清库，继续`
- 清库前：`patch=181` `release=7` `workspace=9` `revision=44` `theme=1` `injection=20` `layout_version=5` `meta_theme=0` `dict_theme=54`
- 证据：
  1. `ThemeFactoryResetService::resetAll('RESET')` 清空表：`theme_scope_patch/revision/release/workspace`、`theme_widget_default_injection`、`theme_layout_version`、`weline_theme`；Meta 删 **0**；字典 `@meta::theme.%` 删 **54**
  2. `setup:upgrade -m Weline_Theme` exit **0**，重装 Default 主题 `id=1`
  3. activate frontend/backend success；`DefaultLayoutSeeder` seed 7 pageType 并 publish release **2–8**（homepage/cart/category/…）
  4. 清库后种子态：`patch=104` `release=8` `workspace=8` `revision=49` `theme=1` `injection=16` `meta_theme=0`
- 探活：首页/search/cart **200**；裸编辑器 **302**；带 Session 编辑器 **200**（`#themeEditor`/保存版本/发布）；Browser 首页冒烟标题 `Home | Homepage Default`（**非**全量操作员验收）
- 契约：CopyScoped **19/19**（203）
- **仍未完成**：Browser 操作员全量交互验收；其它绿field 收口项以最新计划对照为准
- 版本：`2.2.27` → `2.2.28`；升级完成

## Hotfix — 2026-08-31：绿field 继续 — E2E workflows / sort payload 认 node_uid（2.2.27）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. E2E `theme-editor-workflows`：save/config/sort 改 `node_uid`；不再要求数字 `layout_id>0`
  2. `ThemeEditor::normalizeEditorSortDataPayload`：兼容 map 与 list 行格式
  3. `weline-theme-editor.js` 槽容量计数补上 `data-node-uid`（与 theme-editor.js 对齐）
  4. CopyScoped 增补 `testThemeEditorWorkflowsE2ePrefersNodeUid`
- **仍未完成**：Browser 操作员全量验收未跑；Factory Reset 清库未执行
- 版本：`2.2.26` → `2.2.27`
- 验收：CopyScoped **19/19**（203 assertions）；升级 `2.2.26`→`2.2.27` 完成；探活首页/search **200**，裸编辑器 **302**；带历史后台 Session 的编辑器 HTML **200**（可见 `#themeEditor` /「保存版本」/「发布」；**非**全量 Browser 操作员验收）

## Hotfix — 2026-08-31：绿field 继续 — iframe 预览/E2E 身份对齐 node_uid（2.2.26）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. `editor-mode.js` / `weline-theme-preview.js`：`getSlotWidgetElements` + `resolvePreviewWidgetIdentity`；拖放 `reference_layout_id` 认 node_uid
  2. `preview.phtml` 点击 postMessage 优先 node_uid
  3. E2E `theme-editor-default-injections`：结构面板选择器 / 行身份不再要求数字 `layout_id>0`
  4. CopyScoped 增补 `testPreviewIframeScriptsPreferNodeUidIdentity`
- **仍未完成**：Browser 操作员全量验收未跑；Factory Reset 清库未执行
- 版本：`2.2.25` → `2.2.26`
- 验收：CopyScoped **18/18**（191 assertions）；`node --check` 通过；升级 `2.2.25`→`2.2.26` 完成；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — Widget ParamType 身份属性对齐（2.2.25）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. 跨模块 `Weline_Widget`：`AbstractParamType` / `ParamTypeRenderer` hex 只写 `data-node-uid`
  2. CopyScoped 增补 `testWidgetParamTypePrefersNodeUidIdentityAttrs`
  3. Widget `1.0.2`→`1.0.3`；Theme `2.2.24`→`2.2.25`
- **仍未完成**：Browser 操作员全量验收未跑；Factory Reset 清库未执行
- 验收：CopyScoped **17/17**（173 assertions）；Widget→`1.0.3`、Theme→`2.2.25` 升级完成；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — 服务端预览 HTML 身份属性对齐（2.2.24）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. `SlotRendererService::buildWidgetWrapperAttrs`：hex `node_uid` 只写 `data-node-uid`；否则数字 `layout_id` 写 `data-layout-id`
  2. 健康检查 meta / `LayoutSlotRenderer::htmlHasRenderedWidgetWrappers` 可读 `data-node-uid`
  3. ThemeEditor `widget-item` / `config-form` / `preview` 与 `ThemeCacheGenerator` 同步单写规则
  4. CopyScoped 契约增补 `testServerPreviewHtmlPrefersNodeUidIdentityAttrs`
- **仍未完成**：Browser 操作员全量验收未跑；跨模块 Widget ParamType 按钮 attrs 可选后续
- 版本：`2.2.23` → `2.2.24`
- 验收：CopyScoped **16/16**（162 assertions）；升级 `2.2.23`→`2.2.24` 完成；探活见本回合 curl

## Hotfix — 2026-08-31：绿field 继续 — hex node_uid 停止双写 data-layout-id（2.2.23）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. `widgetIdentityDataAttrs`：32-hex 只写 `data-node-uid`；非 hex 才写 `data-layout-id`
  2. 新增 `applyWidgetIdentityToElement`；物化/表单/新插入 wrapper/保存回填统一走该助手
  3. 双属性同写模板清零；`node --check` 通过
  4. 契约增补 apply/hex-only 断言
- **仍未完成**：服务端预览 HTML 若仍输出双属性需另步对齐；Browser 操作员全量验收未跑
- 版本：`2.2.22` → `2.2.23`
- 验收：CopyScoped **15/15**（143 assertions）；升级完成；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — 选择器字面量统一 WIDGET_*_MATCH（2.2.22）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. 双份 JS 抽出 `WIDGET_WRAPPER_MATCH` / `WIDGET_IDENTITY_MATCH` / `PREVIEW_OR_CODE_WIDGET_MATCH`
  2. findWidgetsInSlot / iframe 点击 / hover 栈 / 排序 DOM 查询 / 锚点收集等改走共享匹配器；`saveWidgetSortOrder` 改 `dataLayoutIdSelector`
  3. 裸 `` [data-layout-id="${...}"] `` 查询清零；`node --check` 双份 JS 通过
  4. 契约增补共享匹配器与 sort 选择器断言
- **仍未完成**：双写仍保留 `data-layout-id` 属性（兼容回退，非选择器缺口）；Browser 操作员全量验收未跑
- 版本：`2.2.21` → `2.2.22`
- 验收：CopyScoped **15/15**（137 assertions）；升级完成；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — 清遗留 dataset.layoutId 直读（2.2.21）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. 删除目标 / 数组项记忆 / 内外槽解析 / 语言切换 / 模态保存 / 左侧加载 / 删除部件 / update-sort 键统一 `resolveDomWidgetIdentity`
  2. 业务路径 `.dataset.layoutId` 直读已收口；仅助手函数与双写赋值仍触达该字段
  3. 契约 JS 身份断言增补 deleteTarget / selectedWidget / widgetElement 符号
- **仍未完成**：DOM 选择器字面量仍大量含 `data-layout-id`（兼容回退，非直读）；Browser 操作员全量验收未跑
- 版本：`2.2.20` → `2.2.21`
- 验收：CopyScoped **15/15**（129 assertions）；升级完成；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — siblings/发布预览清理优先 node_uid（2.2.20）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. `stripWidgetWrappersFromHtml` 展开/剥离同时处理 `data-node-uid` + `data-layout-id`
  2. `resolveAnchorPlacementInfo` siblings 索引用 `resolveDomWidgetIdentity`
  3. 预览选中 / 模板物化 / 手风琴懒加载 / 左右侧保存 / modal 加载改走身份助手；slot 判定排除 node_uid
  4. 契约 JS 身份断言增补 strip / siblings / form 符号
- **仍未完成**：编辑器内仍有遗留 `dataset.layoutId` 直读；Browser 操作员全量验收未跑
- 版本：`2.2.19` → `2.2.20`
- 验收：CopyScoped **15/15**（121 assertions）；升级完成；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — 结构面板/排序/拖拽优先 node_uid（2.2.19）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. 插槽手风琴 / 结构视图 `preview-widget-item` / `modalConfigForm` 经 `widgetIdentityDataAttrs` 双写身份
  2. 拖拽排序 `resolveWidget` / dragstart / drop 用 `resolveDomWidgetIdentity`；上下移兄弟节点认 `data-node-uid`
  3. `updateSiblingMoveButtons` / `setDraggableOnSlotWidgets` 选择器纳入 node_uid；AI 动作查询改走 `dataLayoutIdSelector`
  4. 契约 JS 身份断言增补排序/结构面板符号
- **仍未完成**：发布预览清理、部分结构索引/siblings 仍裸读 `data-layout-id`；Browser 操作员全量验收未跑
- 版本：`2.2.18` → `2.2.19`
- 验收：CopyScoped **15/15**（113 assertions）；升级完成；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — DOM 选择器/工具条优先 node_uid（2.2.18）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. `dataLayoutIdSelector` 对 32-hex 身份改用 `:is([data-node-uid], [data-layout-id])`，复合前缀选择器仍合法
  2. `resolveWidgetWrapper` / iframe 点击识别纳入 `[data-node-uid]`
  3. hover 工具条与左侧 `widgetConfigForm`/删除按钮经 `widgetIdentityDataAttrs` 双写 `data-node-uid`
  4. 工具条动作委托用 `resolveDomWidgetIdentity` 取身份
  5. 契约 `testThemeEditorJsPrefersNodeUidIdentityAttrs` 增补 `:is` / wrapper / hover 断言
- **仍未完成**：结构面板/排序/拖拽等路径仍有大量裸 `data-layout-id`；Browser 操作员全量验收未跑
- 版本：`2.2.17` → `2.2.18`
- 验收：CopyScoped **15/15**（105 assertions）；升级完成；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — 删除兼容别名 + JS 身份优先 node_uid（2.2.17）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑；本步未宣称 greenfield 全量完成
- 本步完成（有证据）：
  1. **物理删除** `ThemeScopedCompatibilityAdapter` / `ThemeLegacyResourceAdapter`；`module.php` 仅 provides `ThemeScopedResourceProjector`
  2. 双份编辑器 JS（`theme-editor.js` / `weline-theme-editor.js`）新增 `resolveDomWidgetIdentity` / `widgetIdentityDataAttrs`；配置表单、i18n 面板/按钮、删除/保存委托优先读 `data-node-uid`
  3. 契约：`testLegacyAdapterDropsThemeLayoutDependency` 改为断言别名文件不存在；新增 `testThemeEditorJsPrefersNodeUidIdentityAttrs`；并补 `BP` 定义
  4. 升级：`2.2.16 → 2.2.17` 完成；DI 解析仍为 `ThemeScopedResourceProjector`；generated 无别名类
- **仍未完成**：编辑器 DOM/选择器大量 `data-layout-id` 符号未清零；Browser 操作员全量验收未跑
- 验收：CopyScoped **15/15**（99 assertions）；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — Binding 投影拆出 + facade 收紧（2.2.16）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑
- 本步完成（有证据）：
  1. 新增 **`ThemeScopedBindingProjector`**（active theme load / assert / global project）
  2. `ThemeScopedResourceProjector` 仅保留 layout assert/compile 编排；Meta/Appearance/I18n/Binding 全委托
  3. facade 去掉 `WelineTheme $themes` DI
  4. 契约：`testBindingProjectionDelegates`
  5. **兼容别名仍保留**（`ThemeScopedCompatibilityAdapter` / `ThemeLegacyResourceAdapter` 薄 extends）；仓库内除契约外无业务引用，但未本步删除（避免生成工厂/外部引用断裂）
- **仍未完成**：别名物理删除需再扫全仓/生成物引用；JS `layout_id` 符号未清零；Browser 操作员全量验收
- 版本：`2.2.15` → `2.2.16`
- 升级：`2.2.15 → 2.2.16` 完成；契约 CopyScoped **14/14**（89 assertions）；DI 四协作类齐备；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — Appearance/I18n 投影拆出（2.2.15）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑
- 本步完成（有证据）：
  1. 新增 **`ThemeScopedAppearanceProjector`**（tokens/disks/brand load+project + disk compile）
  2. 新增 **`ThemeScopedI18nProjector`**（translations load+project + wi_{node_uid} identify）
  3. `ThemeScopedResourceProjector` facade 现委托 Meta/Appearance/I18n；仅保留 binding + layout assert/compile
  4. 契约：`testAppearanceAndI18nProjectionDelegates` + 更新 Legacy adapter 断言指向 I18nProjector
- **仍未完成**：binding 再拆出后才能删兼容别名；JS `layout_id` 符号未清零；Browser 操作员全量验收
- 版本：`2.2.14` → `2.2.15`
- 升级：`2.2.14 → 2.2.15` 完成；契约 CopyScoped **13/13**（80 assertions）；DI 持有 Meta/Appearance/I18n 三协作类；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — Meta 投影拆出 ThemeScopedMetaProjector（2.2.14）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑
- 本步完成（有证据）：
  1. 新增 **`ThemeScopedProjectionSupport`**（共享 scope/identity/encode 助手）
  2. 新增 **`ThemeScopedMetaProjector`**（Meta `load`/`project`）
  3. `ThemeScopedResourceProjector` 委托 Meta；Appearance/I18n/binding 仍在 facade
  4. 契约：`testMetaProjectionDelegatesToMetaProjector`
- **仍未完成**：Appearance/I18n/binding 继续拆出后才能删兼容别名；JS `layout_id` 符号未清零；Browser 操作员全量验收
- 版本：`2.2.13` → `2.2.14`
- 升级：`2.2.13 → 2.2.14` 完成；契约 CopyScoped **12/12**（68 assertions）；DI facade 持有 `ThemeScopedMetaProjector`；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — ResourceProjector 正名 + tombstone fail-closed（2.2.13）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑
- 本步完成（有证据）：
  1. **`ThemeScopedResourceProjector`** 成为 Meta/Appearance/I18n/binding 投影权威实现；`module.php` provides 已切
  2. `ThemeScopedCompatibilityAdapter` / `ThemeLegacyResourceAdapter` 保留薄别名 extends（未宣称可删：投影仍在 projector）
  3. `clearTemplateDeletedTombstonesForSlot` 表缺失 fail-closed
  4. 表单 submit / i18n 保存改用 `buildWidgetIdentityPayloadFields`（双 JS）
  5. 契约更新指向 Projector
- **仍未完成**：Meta/Appearance/I18n/binding 从 projector 再拆分独立服务后才能删别名；JS `layout_id` 符号未清零；Browser 操作员全量验收
- 版本：`2.2.12` → `2.2.13`
- 升级：`2.2.12 → 2.2.13` 完成；契约 CopyScoped **11/11** + Tombstone **3/3**；DI=`ThemeScopedResourceProjector`；探活首页/search **200**，编辑器 **302**

## Hotfix — 2026-08-31：绿field 继续 — ThemeLayoutService 残留 mutator fail-closed（2.2.12）

- 当前状态：`partial`
- 宿主：`submit_task_plan` 仍不可用 → 精确路径原生编辑；本回合 `HOME` 曾被污染为站点 URL 导致 ensure 失败，已用 `HOME=/Users/weline` 修复并重跑 ready
- 本步完成（有证据）：
  1. `ThemeLayoutService::{updateWidgetConfig,updateSortOrder,moveWidget,swapWidgetOrder,getSlotWidgets,updateSlotWidgetsOrder}` 表缺失 fail-closed
  2. 契约：`testLegacyMutatorsFailClosedWithoutLayoutTable` + `testApplyDefaultInjectionRecognizesScopedNodeUid`
  3. 配置表单渲染（双 JS）写入 `data-node-uid` / 传 `node_uid`
- **仍未完成**：兼容适配器 Meta/Appearance/I18n/binding 投影迁出后才能删除；JS `layout_id` 符号未清零；Browser 操作员全量验收
- 版本：`2.2.11` → `2.2.12`
- 升级：`2.2.11 → 2.2.12` 完成；契约 **10/10**（55 assertions）；探活首页/search **200**，编辑器裸请求 **302**

## Hotfix — 2026-08-31：绿field 继续 — applyInjection node_uid 门禁 + JS 身份载荷（2.2.11）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → `PLAN_REQUIRED`；精确路径原生编辑（`MCP_TARGET_UNAVAILABLE` / 工具缺口）
- 本步完成（有证据）：
  1. **修 bug**：`postApplyDefaultInjection` 原先用 `empty(layout_id)` 判成功，但 scoped `applyInjectionByKey` 固定 `layout_id=0` + `node_uid` → 永远失败；现认 `node_uid`，scoped 路径不再 `themeLayout->load`，也不再二次 `replaceScoped` 覆盖刚写入的 draft
  2. JS 新增 `buildWidgetIdentityPayloadFields`；`saveWidgetConfigWithLocale` / `translateI18nValues` / `handleWidgetAiAction` / placement anchors 双脚本发 `node_uid`
  3. `postRemoveOrphanWidgets` slot 扫描与 `deleteOrphanWidgetByLayoutId` 表缺失 fail-closed；`postRenderWidget` 优先 scoped `node_uid`
  4. 抽出 `legacyThemeLayoutTableExists()`
- **仍未完成**：兼容适配器 Meta/Appearance/I18n/binding 投影迁出后才能删除；JS `layout_id` 符号未清零；Browser 操作员全量验收
- 版本：`2.2.10` → `2.2.11`
- 升级：`2.2.10 → 2.2.11` 完成；curl 探活首页/search **200**，编辑器裸请求 **302**→登录；Browser 本机已有后台 Session 时直接打开「主题编辑器」UI（非全量 WB-OP）
- Browser 最小验收：首页 `Home | Homepage Default`；编辑器可见顶栏「保存版本 / 发布」与部件库（有会话时）

## Hotfix — 2026-08-31：绿field 继续 — scoped fixture seed + sort/load fail-closed（2.2.10）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑
- 本步完成（有证据）：
  1. fixture 新增 `prepare_scoped_layout` / `seed_scoped_layout`：cleanup → `DefaultLayoutSeeder` → `publishLayout`；search 冒烟 `seeded=true, published=true, workspace revision=2`
  2. `postUpdateConfig` 不再直接 `themeLayout->load`；统一 `resolveNodeUidFromEditorRequest`
  3. `getWidgetByLayoutId` 表缺失 fail-closed
  4. `persistSlotSortOrder` 容器探测含 `[data-node-uid]`（两份 JS）
  5. `apply_default_injection` 计 applied 时认 `node_uid`
  6. 契约：**12/12**（断言增至 66）
- **仍未完成**：兼容适配器 Meta/Appearance/I18n/binding 投影迁出后才能删除；JS `layout_id` 符号未清零；Browser 操作员全量验收
- 版本：`2.2.9` → `2.2.10`
- 升级：`2.2.9 → 2.2.10` 完成；探活首页/search **200**，编辑器 **302**
- fixture 冒烟：`prepare_scoped_layout` search → `seeded=true, published=true`

## Hotfix — 2026-08-31：绿field 继续 — adapter 改名 + swap node_uid + scoped snapshot（2.2.9）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑
- 本步完成（有证据）：
  1. `ThemeLegacyResourceAdapter` → **`ThemeScopedCompatibilityAdapter`**（`module.php` provides 已切）；旧类保留薄别名 extends
  2. `postSwapWidgetOrder` / JS `swapWidgetOrder` 接受 `node_uid_1/2`（修 hex 被 `(int)` 截断为 0）
  3. fixture `snapshot` 增加 `scoped.workspaces` 摘要；冒烟可见 homepage workspace revision=7
  4. 契约：**12/12**
- **仍未完成**：Meta/Appearance/I18n/binding 投影迁出后才能删兼容适配器；JS `layout_id` 符号未清零；fixture 未做 scoped seed 写入；Browser 操作员全量验收
- 版本：`2.2.8` → `2.2.9`
- 升级：`2.2.8 → 2.2.9` 完成；DI 解析为 `ThemeScopedCompatibilityAdapter`；探活首页 **200** / 编辑器 **302**
- fixture 冒烟：`scoped.workspace_count=1`（homepage revision=7）

## Hotfix — 2026-08-31：绿field 继续 — widget-config 改 node_uid + fixture scoped 清理（2.2.8）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑
- 本步完成（有证据）：
  1. `ThemeEditor::getWidgetConfig` / `postSaveWidgetConfig` / `getWidgetFieldI18nPayload` 改为 `resolveNodeUidFromEditorRequest` + scoped draft node（不再 `themeLayout->load(layout_id)`）
  2. 默认配置保存路径在 locale=null 时真正调用 `ThemeScopedLayoutWriteService::updateWidgetConfig`
  3. 编辑器 JS：`buildSavedWidgetConfigUrl` / i18n load-save / 表单 submit 优先传 `node_uid`（两份脚本）
  4. fixture：`cleanup_scoped_layout_workspaces` 按 theme/layout identity 清理 workspace/patch/revision/release
  5. 墓碑契约：`projectDraft` 改为断言 no-op（布局权威仅 scoped）
  6. 契约：`ThemeLayoutCopyScopedContractTest` + tombstone + `ThemeEditorScopedWidgetConfigContractTest`
- **仍未完成**：adapter 整类删除/改名落地；JS `layout_id` 符号未清零；fixture 未做 scoped seed；Browser 操作员全量验收
- 版本：`2.2.7` → `2.2.8`
- 升级：`2.2.7 → 2.2.8` 完成；探活首页 **200** / 编辑器 **302**
- 契约：**11/11**

## Hotfix — 2026-08-31：绿field 继续 — Legacy 去 ThemeLayout + delete/i18n/fixture（2.2.7）

- 当前状态：`partial`
- 宿主：仍无 `submit_task_plan` → 精确路径原生编辑
- 本步完成（有证据）：
  1. `ThemeLegacyResourceAdapter` 去掉 `ThemeLayout` DI；删除布局投影死代码（baseline/selection/legacyNodeUid/tombstone/projectLayout）
  2. I18n 投影/读取改为 `wi_{node_uid}` identify（不依赖 `theme_layout` 行）；`projectI18n` 不再因空 nodes 抛 `theme_i18n_projection_node_identity_missing`
  3. 编辑器删除路径：`handleWidgetDelete` 识别 `nodeUid`；结构/残留选择器优先 `data-node-uid`（两份 JS）
  4. `theme-editor-fixture.php`：`theme_layout` 缺失时 cleanup/snapshot fail-closed；snapshot 冒烟返回 `legacy_table=false`
  5. 契约：`ThemeLayoutCopyScopedContractTest` **7/7**
- **仍未完成**：adapter 整类删除（Meta/Appearance/I18n/binding 投影仍在）；JS `layout_id` 符号未清零；fixture 未改为 scoped seed；Browser 操作员全量验收
- 版本：`2.2.6` → `2.2.7`
- 升级：`2.2.6 → 2.2.7` 完成；探活首页 **200** / 编辑器 **302**
- fixture 冒烟：`snapshot` → `legacy_table=false` / `theme_layout_missing_scoped_authority`

## Hotfix — 2026-08-31：绿field 继续 — saveLayout/publishLayout 改 scoped（2.2.6）

- 当前状态：`partial`
- 宿主：`submit_task_plan` 仍不可用 → `PLAN_REQUIRED`；本步精确路径原生编辑（记录 `HOST_MCP_NOT_ATTACHED` 工具缺口）
- 本步完成（有证据）：
  1. `ThemeLayoutService::saveLayout` → `ThemeScopedLayoutWriteService::replaceDraftFromSnapshot`（+ published 时 `workspace->publish`）；不再 `deleteLayoutRows`/`saveWidget` 写 `theme_layout`
  2. `publishLayout` → 校验草稿后直接 `workspace->publish`；不再把 draft 行拷贝进 published 表
  3. `saveWidget` → scoped `addWidget`/`updateWidgetConfig`；拒绝纯 numeric `layout_id` 更新；返回 `0`
  4. `deleteLayoutRows`/`deleteWidget`/`cleanOrphanWidgets`/`shiftSortOrder`/`removeExclusiveWidgets`：表缺失时 fail-closed
  5. `ThemeLegacyResourceAdapter::loadLegacyLayout` 恒返回空 nodes（布局权威仅 scoped）
  6. 编辑器 JS：`resolveWidgetElementSelector` 优先 `[data-node-uid]`（`theme-editor.js` + `weline-theme-editor.js`）
  7. 契约：`ThemeLayoutCopyScopedContractTest` 增 `saveLayout`/`publishLayout`/`loadLegacyLayout` 断言
- 探活：首页 **200**；编辑器 **302**（`https://p05113ef3.weline.test:9555/`）
- **仍未完成**：Legacy adapter 整类删除（Meta/Appearance/I18n/binding 仍依赖）；JS `layout_id`/`layoutId` 引用未清零；fixture 全量；Browser 操作员全量验收
- 版本：`2.2.5` → `2.2.6`
- 契约：`ThemeLayoutCopyScopedContractTest` **5/5**

## 2026-08-31 — 页脚法律链接补齐 /cookies 与 /ads-preferences

- 问题：默认 Theme 底部法律行指向 `/terms` `/privacy` `/cookies` `/ads-preferences`，后两者未进 `Router::defaultPublicRouteMap`，且缺 `policy/ads-preferences` 布局，点击报错。
- 修复：路由增加 `cookies`/`cookie-policy` → `policy.cookie`、`ads-preferences`/`advertising-preferences` → `policy.ads-preferences`；新增广告偏好布局（对齐 cookie/privacy 的 meta/hook/slot/`<lang>`/设计令牌）；`Policy` 白名单与标题同步；契约测试覆盖 legalLinks URL 与布局壳。
- 版本：`2.2.3` → `2.2.4`；验收需 `php bin/w setup:upgrade --route`。
- 备注：本回合宿主 MCP 未暴露 `submit_task_plan`，记录 `HOST_MCP_NOT_ATTACHED`，精确路径原生编辑。

## Hotfix — 2026-08-31：执行清库 Factory Reset + 重装默认主题（2.2.5）

- 当前状态：`partial`（清库与重装已执行；绿field 计划其它项仍未全部完成）
- 用户授权：`执行清库`
- 证据：
  - `ThemeFactoryResetService::resetAll('RESET')`：清空 scoped/injection/layout_version/weline_theme；Meta `theme.frontend|backend` 删 **10309**；字典 `@meta::theme.%` 删 **10165**
  - 清库后行数：patch/release/workspace/theme/injection/layout_version/**meta_theme = 0**
  - `setup:upgrade -m Weline_Theme` 重装 Default 主题（id=1）
  - 激活 frontend/backend；`DefaultLayoutSeeder` seed 7 个 pageType 并 publish release 1–7
  - 修复：`DefaultLayoutSeeder::clearLayout` 改 scoped（不再碰已删 `w_theme_layout`）
  - `factoryResetPayload` 后续自动 activate + seed + publish
- 探活：首页 200；编辑器 302
- **仍未完成**：Legacy adapter 整类删除、JS layout_id 清零、fixture 全量、Browser 全量验收
- 版本：`2.2.4` → `2.2.5`

## Hotfix — 2026-08-31：绿field 继续 — 四级复制改 scoped + FactoryReset PG 方言（2.2.4）

- 当前状态：`partial`
- 本步完成：
  1. `copyLayoutIdentityBetweenThemes` 改 `ThemeScopedLayoutWriteService::replaceDraftFromSnapshot`（+ 可选 publish）；不再 `saveLayout`/`theme_layout`
  2. `LayoutWorkspace::hasLayout` / `deleteLayout` 改 scoped
  3. `ThemeFactoryResetService` 去掉 MySQL 反引号；PG 用 `RESTART IDENTITY CASCADE`；Meta/字典按 `theme.frontend|backend` / `@meta::theme.%` 清理
  4. Legacy adapter：`projectLayout` 空实现；`loadLegacyLayout` 表不存在时返回空
  5. 四级文案：`Global（全部网站和店铺）` 英/中译对齐「网站、店铺和渠道」
  6. 契约：`ThemeLayoutCopyScopedContractTest`
- **未完成**：未执行 Factory Reset 清库；Legacy adapter 整类删除；JS `layout_id` 清零；fixture 全量；Browser 全量验收
- 版本：`2.2.3` → `2.2.4`

## Hotfix — 2026-08-31：绿field 继续 — 去 legacy 读/写回退 + DROP 无前缀孤儿表（2.2.3）

- 当前状态：`partial`（本步完成 delete-dualwrite 读路径收口 + orphan DROP；**未**执行全模块 Factory Reset 清库）
- 计划对照（修订 9 todos）：
  - runtime-scoped-read：有证据（`ThemeRuntimeLayoutResolver` / SlotRenderer）
  - editor-node-uid：部分（JS 仍有 `layout_id` 引用；本步去掉编辑器 `projectDraft`/`initDraftFromPublished` 开页双写）
  - version-facade：保留 `w_theme_layout_version`
  - injection-seeder-scoped：有证据（`ThemeScopedLayoutWriteService`）
  - delete-dualwrite-migration：本步推进 — Migration 服务已删；去掉 `getLayout`/`hasDraft`/`discardDraft`/`initDraftFromPublished` legacy 回退；`projectDraft` 调用已去（adapter 方法仍为 no-op，类暂保留给 Meta `projectPublished`）
  - schema-drop：prefixed 已 DROP；无前缀孤儿已 DROP（2.2.2 migration 因 Connector 自动加 `w_` 前缀空跑；2.2.3 用裸 SQL 纠正；本库已 `DROP TABLE` 验证 `to_regclass` 为空）
  - factory-reset-ui：UI/API 已有；**未执行** `RESET` 清 scoped 数据（`scope_patch` 仍约 1280）
  - copy-meta-i18n / test-fixture-sweep：**未完成**
- 变更：ThemeEditor 开页只 `workspace->load`；ThemeLayoutService scoped-only；migration `…v2.2.3` 裸名 DROP
- 版本：`2.2.1` → `2.2.3`
- HOST：本回合 `weline_project_intelligence` MCP discovery 失败，记录 `HOST_MCP_NOT_ATTACHED`，精确路径原生编辑

## Hotfix — 2026-08-31：Theme 绿field 进度诚实纠偏（未完成）

- 当前状态：`partial`（**不是** `implemented` / 主链路完成）
- 纠偏原因：此前把「部分代码切换」虚报为绿field 已完成；对照计划缺证据收口（现已写入 MCP `plan_todo_evidence_closeout`）。
- 证据（2026-08-31 DB 实测）：
  - scoped 仍有数据：`w_theme_scope_patch=1280`、`release=62`、`workspace=105`、`revision=153`、`w_theme_widget_default_injection=369`、`w_theme_layout_version=16`、`w_weline_theme=2`
  - 未执行 Factory Reset / 全表清库
  - 无前缀孤儿表仍在：`theme_layout≈10996`、`theme_layout_version≈383`、`theme_widget_default_injection≈5536`
  - 双写链仍在：`ThemeEditor` → `projectDraft` / `initDraftFromPublished`；`ThemeLayoutService::initDraftFromPublished`；`ThemeLegacyResourceAdapter::projectDraft`
- 已做（有证据）：模块版本 `2.2.1`；prefixed `w_theme_layout*` DROP + SchemaDiffExcluded；runtime scoped resolver / scoped write / FactoryReset **服务代码**存在；编辑器 JS `node_uid`；slot marker 保留与 DOM 回退；契约测试 fail-closed（非 scoped 重写）
- **未完成清单**（后续 2.2.3 Hotfix 已推进部分项，以最新条目为准）：
  1. 用户授权后执行清库 / `ThemeFactoryResetService::resetAll`
  2. 删除 Legacy adapter 布局相关残留 / Meta `projectPublished` 解耦后删类
  3. E2E/fixture 改写为 scoped（非仅 fail-closed）
  4. Meta 域拆分 + 四级继承复制
  5. VirtualTheme / Builder（若仍在计划范围）
  6. 全量 Browser 操作员验收
  7. ~~无前缀孤儿表~~（2.2.3 已 DROP）
  8. ~~开页 projectDraft / initDraftFromPublished 双写~~（2.2.3 已去）
- 版本：Theme 当时为 `2.2.1`（本条仅文档纠偏）。

## Hotfix — 2026-08-31：Theme 2.2.1 绿field 收尾（DROP legacy layout 表）

- 当前状态：`partial`（原误标 `implemented`；仅 DROP prefixed legacy + 运行时/编辑器局部修复，**不等于**绿field 计划完成）
- 问题：2.2.0 主链路已切 scoped，但 SchemaDiff 仍按 Model 重建 `w_theme_layout*`；drop migration 虽已登记却被后续 checkpoint 回建；清 tpl 后前台 500（slot 边界注释被剥 / narrow 丢 marker）。
- 变更：
  1. `ThemeLayout` / `ThemeVirtualLayout*` 实现 `SchemaDiffExcludedModelInterface`，退出 SchemaDiff
  2. 手动 DROP `w_theme_layout` / `w_theme_virtual_layout` / `w_theme_virtual_layout_version`；保留 `w_theme_layout_version`（版本 facade）与 scoped 表
  3. bump `2.2.0` → `2.2.1` 解决 schema checkpoint 冲突
  4. `theme-editor.js` 同步 `node_uid`（删除 payload / identity / 排序 / 模板物化）
  5. `Template.php` 剥注释时保留 `<!--@weline-slot:...-->`；`SlotRendererService` narrow 扩边界 + 无 marker 时 DOM 回退；`ThemeComponentDefinition` use Dto
  6. UI 契约测试对齐；Dashboard/WidgetDemo E2E fixture 在无 legacy 表时 fail-closed 提示改写
- 验收：契约通过；前台 200；编辑器 302；prefixed legacy layout 表保持 DROPPED。**未**清 scoped 数据、**未**删双写、**未** DROP 无前缀孤儿表。
- 版本：Theme `2.2.0` → `2.2.1`。

## Hotfix — 2026-09-01：迷你购物车优惠券/订单留言未渲染（opaque park）

- 当前状态：`implemented`
- 问题：前台迷你购物车小计上方空白，`footer-extras` 槽空；DB `mini-cart` 布局已有 `mini-cart-coupon` / `order-notice`，merge/filter 正常，但 `processSlotFragmentWithBoundaries` 未填充。
- 根因：`SlotHtmlOpaqueParker::parkWidgetWrapperInners` 把模板直嵌 `mini-cart-icon` 的 wrapper 内层（含 `footer-extras` 边界）停泊掉；原先在 `restore` **之前**调用 `fillRemainingUnmarkedSlotWidgets`，嵌套槽对扫描不可见。
- 变更：先 `$parker->restore($html)`，再 `fillRemainingUnmarkedSlotWidgets`；孤儿检测合并 restore 后的 slot id。契约：`MiniCartLayoutSlotContractTest`（restore→fill 顺序 + park 隐藏/恢复断言）。
- 扩展点：`none`（SlotRenderer 内部顺序修复）。
- 版本：Theme `2.2.73` → `2.2.74`。

## Hotfix — 2026-08-31：新品上架改读真实 catalog

- 当前状态：`implemented`
- 问题：`new-arrivals` 固定 `ThemeDemoCatalog`，与 `featured-products` 真实 catalog 撞 ID、预览像「串台」。
- 变更：`StorefrontProductWidgetCatalog::newArrivalCards($limit, $days)` 按已发布商品 `created_at` 倒序 + 天数窗过滤；部件模板优先真实数据，仅预览且无 catalog 时回退 demo。
- 版本：Theme `2.1.39` → `2.1.40`；Product `1.0.25` → `1.0.26`。

## Hotfix — 2026-08-31：孤儿检测按 page_layouts 排除布局专有槽误报

- 当前状态：`implemented`
- 问题：主题编辑器预览首页等非 `mini-cart` 布局时，把 `footer-extras` 上的「迷你购物车优惠券 / 订单留言」误报为孤儿（部件属 `page_layouts: ['mini-cart']`，槽不在当前页 HTML）。
- 变更：`SlotRendererService::detectOrphanWidgets` 传入当前 `pageType`；注册表 `page_layouts` 不含当前页（且无 `*`/`default`/layout-support 命中）则跳过告警；编辑 `mini-cart` 且确实缺槽仍告警。契约：`OrphanPageLayoutsFilterContractTest`。
- 扩展点：`none`（服务内部过滤，复用 PlaceableRegistry 定义）。
- 版本：`2.1.38` → `2.1.39`。

## Hotfix — 2026-08-31：产品卡内资源 URL 裸 `&` 打断 HTML 解析

- 当前状态：`implemented`
- 问题：用户截图像「第三张卡空壳、标题/加购全宽泄漏」；审计最终 HTML 时 `featured-products` 的 `article/div` 闭合完整，但卡内 `<link href="@static(...)&v=...">` 在 `@static` 已带 `?v=preview_…` 时变成裸 `&`，libxml 报 `htmlParseEntityRef`；编辑器 DOM/CoW 通道历史上会因此拆散后续节点。
- 变更：`shopper-actions` / `add-to-cart` / `mini-cart-icon` 的裸 `&v=` 改为 `&amp;v=`；`featured-products` / `new-arrivals` 的 `@static(...)?v=` 改为 `&amp;v=`（避免预览态叠成 `?v=preview_x?v=stamp`）。
- 实测（预览 content，editor_mode=1）：`featured` 第 3 卡 `article/div` 开闭 1/1、12/12，无卡外泄漏节点；全页 `article` 24/24。
- 附：`product-label` 仍在每张卡内重复输出 `<style>`，属资源自带策略副作用，未改语义。
- 版本：`2.1.37` → `2.1.38`。

## Hotfix — 2026-08-31：预览窄屏产品网格单列假「部件散架」

- 当前状态：`implemented`
- 问题：主题编辑器 iframe ≈618px 时，`new-arrivals`/`up-sell` 的 `@media (max-width:620px){grid-template-columns:1fr}` 把卡片打成整页宽；亚马逊卡 `padding-top:100%` 大方图 + 全宽加购/购买，看起来像部件内部布局泄漏。DOM 嵌套实测完整。
- 变更：与 `featured-products`/`bestsellers` 对齐——中等窄屏保持 2 列，仅 `≤480px` 才单列；`related-products` 的 `520px→1fr` 同步下调到 `480px`。
- 验收：预览宽 ~618px 时 `new-arrivals .products-grid` 为 2 列，卡片不再整页宽堆叠。
- 版本：`2.1.36` → `2.1.37`。

## Hotfix — 2026-08-31：插槽部件手风琴无法逐个展开

- 当前状态：`implemented`
- 变更：`renderSlotWidgetsConfig` 的 panel 补 `data-w-disclosure-panel`（否则 Weline UI disclosure 不挂载，仅首项静态展开）；`collapseId` 纳入循环索引防同 layoutId/templateRef 撞 id；`loadWidgetConfigForAccordion` 接受具体 body 实例。缓存戳 `20260831-slot-accordion-disclosure-v1`。
- 版本：`2.1.35` → `2.1.36`。

## Hotfix — 2026-08-31：header-nav-fill 内容自适应宽度

- 当前状态：`implemented`
- 变更：右侧 `#header-nav-fill` / `.header-nav-right-slot` 由 `flex: 1 1 0` 改为 `flex: 0 1 auto`（跟快捷条目内容宽），中间空白归左侧分类；`adjustNavLinks` 先判断无压缩时不预留「更多」宽，避免假溢出。同步 `header-chrome-amazon.css`、`nav-links.md`、契约测试。
- 版本：`2.1.34` → `2.1.35`。

## Hotfix — 2026-08-31：去掉礼品心愿单/礼品卡入口

- 当前状态：`implemented`
- 变更：页头快捷导航、侧栏默认服务链、main-nav 默认 shortcut 删除「礼品心愿单」`/registry` 与「礼品卡」`/gift-cards`；Hook 文档对齐。业务确认不做 GiftRegistry / GiftCard 模块；个人收藏仍由 `Weline_Wishlist` 负责。
- 版本：`2.1.32` → `2.1.33`。

## Hotfix — 2026-08-31：顶栏去掉会员视频

- 当前状态：`implemented`
- 变更：页头快捷导航与 main-nav 默认项删除「会员视频」`/video`；侧栏默认快捷同步去掉；Hook 文档示例对齐。
- 版本：`2.1.31` → `2.1.32`。

## Hotfix — 2026-08-31：页脚帮助中心改回 Theme /help

- 当前状态：`implemented`
- 变更：新增 Theme `footer-help-center-link`（默认注入 `footer-help-links` → `/help`）；`layouts/help` 主题入口对齐 `guide/shipping` / `guide/returns` / `guide/payment` / 账户 / 悬浮客服。
- 版本：`2.1.33` → `2.1.34`。

## Hotfix — 2026-08-31：帮助中心改业务部件注入

- 当前状态：`implemented`
- 变更：帮助中心硬链清空；新增 `footer-help-links` 空槽（accept：`footer-my-account-link` / `footer-my-orders-link` / `footer-shipping-info-link` / `footer-returns-policy-link` / `footer-help-center-link` / `footer-contact-service-link`）；契约测试覆盖；Theme Router 为 Shipping 保留 `guide/shipping` / `guide/returns` 公共别名。
- 版本：`2.1.30` → `2.1.31`。

## Hotfix — 2026-08-31：支付与账户改业务部件注入

- 当前状态：`implemented`
- 变更：支付与账户硬链清空（删账户充值/礼品卡/支付方式/货币）；新增 `footer-payment-account-links` 空槽（accept：`footer-payment-methods-link` / `footer-currency-rates-link` / `footer-campaign-link`）；契约测试覆盖。
- 版本：`2.1.29` → `2.1.30`。

## Hotfix — 2026-08-31：合作信息去掉开店/联盟/推广硬链

- 当前状态：`implemented`
- 变更：默认链接仅保留「供应商合作」→ `/inquiry/suppliers`；删除「我要开店」「加入联盟」「我要推广」；`footer-partner-links` accept 增加 `footer-promote-link`（Affiliate 默认注入）；已发布布局若仍携带退役硬链，页脚 partial 运行时清洗。
- 版本：随 Theme `2.1.31` 发布面落地。

## Hotfix — 2026-08-30：Channel 稀疏覆盖截断 Website 页脚部件

- 当前状态：`implemented`
- 问题：中文前台 Channel 仅有 `delivery` 布局行时，`getLayout` 整页 ownership 截断 Website 的 `footer-blog-link` / `footer-news-link`，发布后页脚了解我们扩展槽为空。
- 变更：`ThemeLayoutService::getLayout` 改为按 slot 近优先合并 Scope 链；`ThemeRuntimeCacheCleaner` 增加 storefront chrome hot cache 清理。
- 版本：`2.1.27` → `2.1.28`。

## Hotfix — 2026-08-30：主题发布按 Scope 清缓存 + 前台发布回执

- 当前状态：`implemented`
- 问题：Website Scope 已发布（release `theme_editor_publish`）但前台 HTML `themePublishedVersion*` 为空；`flushFullPageCache` 调用 `clearNonGlobalCaches(null)` 未按发布 Scope 失效，且跳过 `generated_theme_cache`；scoped `blocked` 仍返回 success。
- 变更：`ThemePublishedVersionRuntimeResolver` 按请求 Scope 祖先链（及任意已发布回退）解析回执；ThemeEditor 发布走 `clearScopedCaches(scope, themeId)`；blocked 发布 `success=false` 且不清缓存；契约测试覆盖。
- 版本：`2.1.26` → `2.1.27`。

## Hotfix — 2026-08-30：删除独立订单跟踪页布局

- 当前状态：`implemented`
- 变更：移除 `layouts/order_tracking`、相关 Hook/文档与预览槽；物流入口改走账户订单，旧 URL 由 Order Router 302。
- 版本：`2.1.25` → `2.1.26`。

## Hotfix — 2026-08-30：页脚「合作信息」去掉自行出版硬链

- 当前状态：`implemented`
- 变更：默认链接去掉「自行出版」；`footer-partner-links` 空槽承接业务部件（accept：`footer-publish-link` / `layout-footer-partner-links`）；契约测试覆盖。
- 版本：`2.1.24` → `2.1.25`。

## Hotfix — 2026-08-30：两步验证按钮对齐回归（CSS 丢失 + 未挂懒加载）

- 当前状态：`implemented`
- 问题：日志称已修「验证并继续」同行居中，但 `challenge-idle/busy` 选择器从 CSS 回退丢失；且 `account-challenge` 未登记 `lazyComponentSources/Styles`，挑战页常吃不到亚马逊登录样式，图标竖排偏位。
- 变更：恢复 challenge label + submit `inline-flex`；`weline-ui.js` 登记 challenge JS/共用 login CSS 并刷新 `?v=`；契约测试断言懒加载登记。
- 版本：`2.1.23` → `2.1.24`。

## Hotfix — 2026-08-30：拆分后台预览与真实前端预览

- 当前状态：`implemented`
- 问题：`#btnPreview` / 编辑器 iframe 误调 `start-preview`，种 `weline_preview_token` Cookie 并激活真实店面「预览模式」浮层。
- 变更：iframe 与 `#btnPreview` 只走 `theme-preview/content`（后台会话 + editor_context）；仅 `#btnFrontendPreview` 调用 `start-preview`。
- 版本：`2.1.22` → `2.1.23`。

## Hotfix — 2026-08-30：页脚「了解我们」扩展槽交给 Blog

- 当前状态：`implemented`
- 变更：默认链接去掉人才招聘/投资者关系/博客/新闻中心硬链；`footer-about-links` 空槽承接 Blog 部件；CSS 对齐列表间距。
- 版本：`2.1.21` → `2.1.22`。

## Hotfix — 2026-08-30：首页推荐/新品/分类左右顶边

- 当前状态：`accepting`
- 问题：homepage 未包 `.w-container`，部件按 Shell 契约 `padding-inline:0`，推荐产品/热门分类/新品等贴视口左右沿。
- 变更：`layouts/homepage/default.phtml` 对非 Hero/Promo 区子节点补 `--weline-layout-content-max-width` + `--weline-layout-content-padding-inline`；契约测试覆盖。
- 版本：`2.1.18` → `2.1.19`。

## Hotfix — 2026-08-30：两步验证提交强制携带令牌与验证码

- 当前状态：`implemented`
- 变更：`weline-customer-account-challenge.js` 从隐藏域/data/URL 同步 `challenge_token`，`FormData.set` 写入令牌与纯数字验证码；输入时清除 MessageManager flash。
- 版本：`2.1.17` → `2.1.18`。

## Hotfix — 2026-08-30：两步验证「验证并继续」图标与文字同行居中

- 当前状态：`implemented`
- 问题：挑战页按钮使用 `data-w-challenge-idle-label`，CSS 只覆盖登录的 `data-w-login-*-label`，图标与文案纵向堆叠。
- 变更：`weline-customer-account-login.css` 为 challenge idle/busy label 补 `inline-flex` + 垂直居中；提交按钮自身 `inline-flex` 居中。
- 版本：`2.1.16` → `2.1.17`。

## Hotfix — 2026-08-30：两步验证 account.challenge 布局与可配背景部件

- 当前状态：`implemented`
- 变更：新增 `layouts/account/challenge.phtml` 与 `form/account-challenge`（背景图/主色/广告文案）；`ControllerFetchFileBefore` 快路径；`ThemePageTypeResolver` 识别 challenge URI；`weline-ui` 注册 `account-challenge` document POST。
- 版本：`2.1.14` → `2.1.15`。

## Hotfix — 2026-08-30：账户下拉去掉硬编码「我的收藏」

- 当前状态：`implemented`
- 变更：`widgets/header/account` 删除硬编码收藏项与 `data-i18n-wishlist`；收藏入口仅由 `Weline_Wishlist` 的 `header-account-links` 等 Hook 写入。`full-header` / `sidebar-menu` 演示链改为 `customer/account/index#wishlist`。
- 版本：`2.1.12`。

## Hotfix — 2026-08-30：页头账户部件声明 data-weline-load

- 当前状态：`implemented`
- 问题：`widgets/header/account` 未在部件根声明 `api,account`，首页仅路径启发式无匹配时控制台「立即加载」无 account，FPC `w_auth` 刷新不触发。
- 变更：部件根补 `data-weline-load="api,account"`（**禁止**写到 header 布局壳）；规范表补「Theme 页头账户」行；契约测试断言属性。
- 权威：`doc/前端JS模块加载规范.md` §3 / §7；MCP `theme_js_module_declare_only`。
- 版本：`2.1.11`。

## Hotfix — 2026-08-30：路径启发式优先于属性加载（文档）

- 当前状态：`implemented`
- `doc/前端JS模块加载规范.md` §1.1：路径启发式优先；属性通道跳过已认领/已加载模块。
- 版本：`2.1.10`。

## Hotfix — 2026-08-30：前端 JS 模块第二波属性声明迁移

- 当前状态：`implemented`
- 范围：Compare / Shipping / Review(+Blog) / Marketing / Product / Order / CustomerService / TwoFactorAuth / Theme headerSearch+店面兜底 / I18n currency 资源；Frontend `globalVar:null` 显式生效。
- 权威：`doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。
- 版本：`2.1.9`。

## Hotfix — 2026-08-30：前端 JS 模块「收集门槛」写清

- 当前状态：`implemented`
- `doc/前端JS模块加载规范.md` §2 补扫描路径、`base/` 产物跳过、完整样板头、compile 写出 Frontend/Backend base 的流程；避免只写片段导致「怎么写才会被收集」不清。
- 版本：`2.1.8`。

## Hotfix — 2026-08-30：维护页恢复自动刷新镜像

- 当前状态：`implemented`
- `view/statics/ui/pages/weline-maintenance.js` 与 `Weline_Maintenance` 恢复探测对齐：非 503 即 reload、30s 硬刷新、`/pub/errors/maintenance/` 改探首页。
- 版本：`2.1.7`。

## 2026-08-30 — 前端 JS 模块加载硬约束落地

- 当前状态：`implemented`
- 新增：`doc/前端JS模块加载规范.md`；总指南 §2.6 / 部件开发指南同步；商品卡购买槽与迷你购物车去掉 `@static` 直引，改 `data-weline-load`（`cart` / `miniCartExtras` / `miniCartIcon`）。
- MCP：`HardConstraintsCatalog` + `AI硬规则索引` 规则 id `theme_js_module_declare_only`。
- 注册：`view/statics/frontend/weline.modules.js`（Theme 迷你购物车模块）。

## 2026-08-30 — 页头 account：SSR 读 Session + w_auth 由 Account 对象处理

- 当前状态：`implemented`
- 问题：header/account 部件写死游客壳；登录跳回 FPC 首页仍显示登录/注册。曾误在 `body-end` 挂独立 `header-account.js`。
- 变更：部件按前台 Session 渲染双壳；`w_auth` 一次性刷新收进 `Weline_Frontend::js/weline-api-account.js`（`handleAuthRefreshSignal`）；`theme.js` HeaderManager 经 `Weline.Account` 调用；不再单独挂 body-end 脚本。
- 验收：登录/退出跳转含 `w_auth=1|0`；页头由 Account 模块切壳并清标记。

## Hotfix — 2026-08-30：Theme Scope 迁移 Unbounded SELECT

- 当前状态：`fixed`
- 现象：`php bin/w se:up` 在 Theme `2.1.5→2.1.6` 报 `Theme 2.1.1 Scope 继承迁移失败：Unbounded SELECT detected`（阈值 10000）；`setup_version` 未推进。
- 根因：`ThemeScopeMigrationService` 对 layout/meta/version 等全表 `select()->fetchArray()`，本地 `theme_layout` 等超过 `database.fetch.max_rows`。
- 修复：新增 `fetchAllRows()`，经 `fetchIterator()` 物化；`layoutRows`/`metaRows`/`rewriteModelScopes`/`rewriteTargetAwareModelIdentities`/`relatedScopeRewriteCollisions` 改用流式读取。语义不变，无需 bump `module.php`（Service-only；Setup 将重跑未完成的 2.1.6）。
- 续：流式读取通过后，publish 再报 `Weline_Customer::form::account-login 未注册`——部件已归属 Theme，遗留行仍写 Customer。`apply()` 在迁移前把 `account-login`/`account-register` 的 `widget_module` 回写为 `Weline_Theme`。
- 验收：`php bin/w se:up` 通过 Theme 模块升级阶段。

## 2026-08-30 — 个人中心大屏侧栏/内容间距与 padding 丢失

- 当前状态：`implemented`
- 问题：小屏/平板侧栏与内容有间距，大屏贴死；内容面板内边距也在大屏塌掉。
- 根因：`account/dashboard`、`account/default` 大屏 `gap`/`padding` 使用无 fallback 的 `var(--weline-layout-spacing-xl)`；前台 head 加载 `_spacing.css`/`foundation` 但不加载 `theme.css`，该 token 常未定义 → 声明失效为 0。平板媒体查询改用 `--spacing-md` / `--weline-space-5` 故表现正常。
- 变更：上述布局 CSS 为 layout-spacing / spacing / space 增加字面 rem fallback；`ThemeAccountLayoutsPartialsGuardTest` 锁住大屏 fallback 契约。
- 验收：登录后宽屏 `/en_US/customer/account` 侧栏与主卡之间可见 gutter，主卡内粉头/正文有 padding。

## 2026-08-29 — 登录改文档级 JSON POST（Cookie 落 jar）

- 当前状态：`implemented`
- 问题：Theme UI `account-login` 走 QueryBin Worker，内嵌浏览器 Session Cookie 不落页；表现为登录成功后账户页踢回 login。
- 变更：`weline-customer-account-login.js` 与 Customer 同源改为 `fetch(form.action)` JSON；缓存串 `login-docpost1`。
- 验收：登录按钮 busy → Personal center 不 referer 回登录。

## 2026-08-29 — 登录/注册部件 dock 去掉重复 UI 挂载

- 当前状态：`implemented`
- 问题：`form/account-login`（及 register）dock 与 Customer 表单卡片同时挂载同名 `data-w-component`，一次提交双触发 Query，验证码失效。
- 变更：dock 仅作布局容器；组件挂载保留在 Customer `login.phtml` / `register.phtml`；UI 包缓存串 `login-single1` / `login-busy1`。
- 验收：登录页仅卡片挂载 `account-login`；单次点击仅一次 `account.login`。

## 2026-08-29 — 个人中心布局对齐主题版心宽度

- 当前状态：`implemented`
- 问题：账户 `account.dashboard` / `account.default` 主内容行 `max-width` 仅绑 `--weline-layout-content-max-width`（无 1440 回退），宽屏下相对页头 `.header-container` / `.w-container` 版心易失配。
- 变更：恢复 `max-width: var(--weline-layout-content-max-width, var(--layout-max-width, var(--container-max-width, 1440px)))`；补 `--account-page-top-gap` 内边距与 inset 阴影；dashboard 主区 `align-items: center` + `margin: 0 auto`。
- 验收：`ThemeAccountLayoutsPartialsGuardTest`；登录后 `/en_US/customer/account` 侧栏与主卡左右缘对齐页头搜索栏版心。

## 2026-08-29 — 注册页 account-register 懒加载死循环卡死

- 当前状态：`implemented`
- 根因：注册部件 `data-w-component="account-register"` 懒加载同一 page 模块，但模块只 `define('account-login')`；`loadLazyComponent` 完成后再次 `mountElement`，定义仍缺失 → **微任务无限重入**，浏览器「页面无响应」。登录页组件名匹配故正常；首页无此组件——故不是页头页尾体积问题。
- 修复：`UI.define('account-register')`；`loadLazyComponent` 定义仍缺失时禁止 remount；资源 `?v=20260829-register-define1`。

## 2026-08-29 — 认证页提前加载 weline.js 导致 api 404

- 当前状态：`implemented`
- 根因：`account/auth` 在 Partials head（runtime 配置）之前插入 `weline.js`，锁死默认 `modulesBaseUrl=/static/Weline_Frontend/js/weline-api`（404）；后续 head 再引 `weline.js` 因 `__initialized` 直接跳过。
- 修复：去掉认证布局提前的 `weline.js`；`weline.js` 默认路径改为 view/statics，并按 runtime/DOM 动态解析 modulesBaseUrl。

## 2026-08-29 — 登录/注册部件可配置文案未走 i18n

- 问题：`account-login`/`account-register` 把 Theme Editor 中文默认 title/subtitle/promo 直接写入模板；英文店面仍中文。`view/tpl` 旧编译缓存会挡住源码修复。
- 变更：默认文案改 `<lang>` 编译期烘焙；仅自定义覆盖走 `__()`；补齐 Theme en_US 舞台词条；Customer 表单 `__($heading)` 兜底。
- 验收：清 `view/tpl` 后英文登录页标题/副标题/舞台/验证码英文化；`AccountAuthCopyI18nContractTest`。

## 2026-08-29 — 顶栏分类链接跟随语言 path

- 问题：英语店面 mega-menu/侧栏分类 href 为裸 `/category/...`，点击后丢失 `/en_US` 回默认中文。
- 变更：`MenuTreeNormalizer::localizeUrl` + `HeaderNavFragment::localizeHref`；header/mega-menu/drawer 渲染统一走 `getFrontendUrl`。
- 验收：`AllMenuNavTreeContractTest`；英语页分类链接含 `en_US` 前缀。
- 续：mega-menu 四级 leaf 与 category-grid/list 演示 href 同样走 `localizeHref`，避免英语页残留裸 `/category/...`。
- 续：mega-menu /「更多」侧栏分类由 `<button>` 改为带 `href` 的 `<a role="tab">`；`mega-menu.js` 对真实 URL 不再 `preventDefault`，悬停仍切面板、点击进分类。
- 续：状态栏有 href 仍点不动——强制 `location.assign`，并为 `weline-mega-menu.js` 懒加载加 `?v=20260829-cat-click1` 破缓存。

## 2026-08-29 — 主题编辑器「基础信息」+ 顶栏收起

- 问题：网站 Favicon/Logo 不宜挂 Website schema；维护页与前台品牌需跟 Scope；编辑器顶栏过挤需可收起。
- 变更：
  - Appearance payload 增加 `brand`（`/brand/favicon|apple_touch_icon|logo_light|logo_dark`）；`ThemeBrandResolver` 读已发布 Scope；`SiteBrand` 优先 Theme Scope → Backend → 默认。
  - 顶栏「作用范围」旁「基础信息」Drawer；收起细条保留 Scope/基础信息/发布；固定/展开写入 localStorage。
  - 维护页静态生成同样优先 Theme Scope brand。
- 验收：`ThemeBrandBasicsContractTest`、`MaintenanceFaviconContractTest`；`resource:compile welineUi`；主题编辑器保存草稿后发布 appearance，前台/维护页 favicon 跟随。

## 2026-08-29 — 外观盘注释分组 + Meta 多语言

- 问题：前缀组标题是技术名（`--font-size`），非中文读者难懂；希望用 CSS 分段注释且走 Meta/AI 翻译。
- 变更：`CssVariableParser` 解析 `/* ========== 组名 #id ========== */` → `category`/`category_id`；`AppearanceTokenGroupMeta` 注册 `@meta::theme.{area}.appearance.token_group.{id}.name`；Catalog 返回 `category_label`；外观盘注释优先、前缀兜底。AI 复用 Meta cron（`@meta::`）。
- 验收：`AppearanceTokenGroupMetaTest`、`ThemeEditorUiCapabilityContractTest`；`resource:compile welineUi`；Theme Editor color 盘可见中文组标题。

## 2026-08-31 — shopper toast CSS 归入 theme.css 主题全局

- 问题：toast 是主题全局 chrome，不应挂在加购/shopper 组件上逐页 emit。
- 变更：样式并入 `view/theme/frontend/assets/css/theme.css`；前台 `head/default|minimal` 加载 theme.css；去掉组件级 toast `<link>` 与 `StorefrontComponentAssets`；独立 `storefront-shopper-toast-amazon.css` 改为弃用指针。toast JS 仍由 CTA 组件 `data-weline-load="storefrontShopperToast"` 声明。
- 验收：`StorefrontShopperToastContractTest`、`ThemeColorModeContractTest`；首页 head 含 theme.css 且含 `.w-amz-shopper-toast`。

## 2026-08-31 — body-end 全局 CSS 收进所属组件

- 问题：Theme/Cart `body-end` 全局挂 toast / mini-cart CSS，无购物车或无 CTA 的页面也会带上。
- 变更：Theme `body-end` 仅保留图片占位 + `storefrontImageFallback`；Cart `body-end` 去掉 toast 重复挂载；`mini-cart-drawer.css` → `mini-cart-icon`；toast CSS → `add-to-cart` / `shopper-actions` 经 `StorefrontComponentAssets::emitShopperToastStylesheetOnce`；Compare/Consent/CustomerService CSS 仍与各自 body-end 组件 markup 同文件（站点级浮动组件自带）。
- 验收：`StorefrontShopperToastContractTest`、`MiniCartShopifyDrawerContractTest`、`ComparePageHydrateContractTest`；首页无 Theme 全局 toast/mini-cart CSS link。

## 2026-08-31 — 购买区 CSS 随组件输出，取消页面级注入

- 问题：曾把 `product-card-purchase-actions.css` 挂到预览 `<head>` / layout `body-end`，无 CTA 的页面也会带上；与「用到组件才带资源」不一致。购物车按钮之所以少出问题，是部件本地写了 `.btn-add-to-cart`，且加购走同一组件槽。
- 变更：删除 `EditorModeAssetInjector` / `ensurePurchaseActionsCssInHtml` / Cart `body-end` 的 purchase-actions 全局挂载；仅 `partials/product/add-to-cart.phtml` `emit…Once` 内联；preview repair 重渲染前 `resetPurchaseActionsAssetsEmission()`。
- 验收：无购买 CTA 的预览头不再含 purchase-actions；渲染该组件的卡片仍有内联 style；单测 Assets / Injector 通过。

## 2026-08-31 — 精选商品「立即购买」预览仍为 UA 默认按钮

- 问题：主题预览 iframe 里 `featured-products` 的「立即购买」仍是灰框默认按钮；用户误以为 HTML 结构错误导致 CSS 未加载。DOM 实测按钮仍在 `.product-card-purchase-actions` 内，结构完好。
- 根因：共享 `product-card-purchase-actions.css` 在预览头里缺失；`featured-products` 本地样式只覆盖了 `.btn-add-to-cart`。
- 变更：`ProductCardAddToCartParams::ensurePurchaseActionsCssInHtml` 在 finalize 后补注入；`EditorModeAssetInjector` 预览头挂 purchase-actions `<link>`；`featured-products` 补 `.btn-buy-now` 与购买区栅格。
- 验收：编辑器 iframe 内 23 个 `.btn-buy-now` 均为橙色渐变胶囊，`uaCount=0`，`headPurchase=true`。
- 后续：页面级注入已按「组件自带」回退，见同日「购买区 CSS 随组件输出」条目。

## 2026-08-30 — 立即购买按钮回退成默认样式

- 问题：热销卡「立即购买」变灰默认按钮；「加入购物车」仍正常。
- 根因：部件 CSS 只写了 `.btn-add-to-cart`；共享 `product-card-purchase-actions.css` 靠 `emit…Once`，预览 repair 重渲染时 flag 已置位导致 CSS 未再输出。
- 变更：bestsellers 补 `.btn-buy-now` + purchase-actions 栅格；repair 重渲染前 `resetPurchaseActionsAssetsEmission()`。
- 验收：主题编辑器 homepage 热销卡两按钮均为胶囊渐变样式。

## 2026-08-30 — 部件 HTML 健康：空壳/拆散按 config 重渲染愈合

- 问题：park 后仍有 bestsellers 拆散、trust-badges 空壳（模板壳 data-config 完好）；属性内 `>` 会截断开标签正则导致后续 wrapper 未 park。
- 变更：`parkDomWidgetWrapperInners` 改为引号感知开标签 + 不平衡时继续；`repairUnhealthyWidgetWrappers` 在 DEV/预览对 `empty_html`/`broken_widget_shell`/PHP 错误按 `data-config` 重渲染并清理抬出的 product 碎片；`ThemePreviewContentRenderer` 同步 park。
- 验收：`SlotRendererDomOpaqueBlocksTest`、`WidgetHtmlHealthInspectorTest`；主题编辑器 homepage 预览三部件结构恢复。

## 2026-08-30 — 部件 HTML 健康：DOM wrapper 保全 + PHP 错误捞取

- 问题：主题编辑器预览 toast「hero-slider/trust-badges 部件 HTML 为空」「bestsellers 未检测到闭包根类」；实为 `LIBXML_HTML_NOIMPLIED` 把 `<section>` 抬出 `.widget-wrapper`，CoW 留下空壳/拆散网格；component 级 `weline-code` 误触发缺根类。
- 变更：`SlotRendererService` park `.widget-wrapper` 内部（保留开标签供 CoW 读 `template_ref`）+ 嵌套 opaque 迭代 restore；`stampFinal` 行走前 park script/style；`WidgetHtmlHealthInspector` 捞取 PHP Fatal/Warning/Uncaught/WLS 错误（仅 DEV/预览）；`missing_closed_root` 收紧为部件自身 code，拆散改报 `broken_widget_shell`；trust-badges 图标改动态 `badge.icon`。
- 验收：`WidgetHtmlHealthInspectorTest`、`SlotRendererDomOpaqueBlocksTest`；主题编辑器 homepage draft 预览三部件不再误报。

## 2026-08-30 — 部件 HTML 健康检测改页头注入 + DOM 空标签保全

- 问题：预览页样式乱；标签闭环检测挂在 body 尾，坏 HTML 时脚本不跑；`DOMDocument::saveHTML` 丢掉空 `</li>`（account divider / my-menu-divider）导致标签错乱。
- 变更：`EditorModeAssetInjector` 预览 JS 进 `<head>`并修复 `?v=` 叠戳；`SlotRendererService` Toast 桥改页头且预览态始终注入；DOM 前 pad 空可选闭合标签；序列化后再扫 wrapper 打 `data-w-widget-health*`。
- 验收：`EditorModeAssetInjectorTest`、`SlotRendererDomOpaqueBlocksTest`；预览页 head 含 health bridge / preview JS。

## 2026-08-29 — 主题编辑器媒体 Scope 目录锁定

- Theme/Widget 打开 MediaManager 时：`path=websites/{website}/{store}[/channel]/{default_directory}`、`lockPath=1`、`lockRoot=websites/{website}/{store}[/channel]`；`default_directory`（如 `banner`）为 scope 下相对子目录。
- channel 仅在非空且不等于 `default` 时追加；段名非法回退 `default`。
- MediaManager 右上角可解锁/再锁定；切换 scope 后路径随之变化。

## 2026-08-28 — 部件 HTML 健康：data-form-id 误报重复 id

- 问题：结账配送 `@delivery` toast「部件内重复 id: checkout-delivery-quick-add-form×2」。
- 根因：`inspectDuplicateIds` 用 `\bid=`，会把 `data-form-id` 末尾的 `id=` 当成真实 id（与 `<form id>` 成对误报）。
- 变更：改为 `(?<![\w:-])id=`；单测覆盖 lazy captcha `data-form-id` 场景。
- 验收：`WidgetHtmlHealthInspectorTest`；配送部件不再因 `data-form-id` 误报。

## 2026-08-28 — 外观盘改为前缀自动分组

- 问题：按 panel 硬编码中文分组难覆盖全部变量；用户希望「每种都按前缀分组」。
- 变更：`appearanceTokenPrefix` 剥叶子修饰后取词干，全 panel 通用；标题如 `--font-size` / `--spacing`；重编译 theme-editor bundle。
- 验收：typography/color/spacing 均出现 `--*` 前缀分组标题。

## 2026-08-28 — 外观盘变量分组扩展到 typography/spacing 等

- 问题：语义色分组只挂在 `color` panel；字体/间距等变量盘仍扁平，看起来像「没分组」。
- 变更：`theme-disk-appearance.js` 按 panel 矩阵分组（typography/spacing/borders/shadows/auth/color）；契约与文档同步。
- 验收：切到 typography 可见「字体族/字号/…」分组标题。

## 2026-08-28 — 语义色重要程度矩阵统一

- 问题：色盘已有 success/warning/danger/info/secondary 叶子，但 Foundation 只桥 primary/text/surface/border；light 默认边过重；无 secondary tone；外观盘扁平难改「重要程度」。
- 变更：`:root` / dark / `[data-w-area=backend]` 全套语义桥接；light 边改为 `#e2e8f0`；`data-tone=secondary`；外观盘按品牌/中性/状态分组；文档 `theme-semantic-color-matrix.md` + 契约测试。
- 验收：`ThemeSemanticColorBridgeContractTest`、`ThemeEditorUiCapabilityContractTest`；编译 `welineUi`；Theme Editor 外观盘分组可见，改 status 色后组件跟随。

## 2026-08-28 — 登录改为有边界部件卡片（非整页布局）

- 变更：部件卡片 `account-login-widget__card` 承载内部分栏；`auth` fallback 同步；不再全宽全高布局劈页。
- 验收：登录页可见独立部件卡片，非左右通栏布局。

## 2026-08-28 — 登录左右分栏（左营销 / 右表单）

- 变更：`form/account-login` 左右分栏；`auth.phtml` 首屏 fallback 同步；部件参数增加左侧主色/点缀色。
- 验收：登录页 PC 左右栏；无营销图时左侧色块渐变。

## 2026-08-28 — 登录卡片顶对齐（亚马逊布局）

- 变更：`auth.phtml` 首屏 fallback 与 Customer 舞台 CSS 同步为顶对齐，避免卡片垂直居中。
- 验收：登录页卡片靠上，头栏下方留白适中。

## 2026-08-28 — 登录舞台间距与无图渐变

- 变更：`account.auth` 主内容透明 + flex；Customer/`auth` 首屏 fallback 用主题 spacing-3xl；无图默认 `#f3f4f4→#eaeded→#e3e6e6`。
- 验收：PC/平板头栏到卡片有可见灰阶留白；非纯白贴边。

## 2026-08-28 — 登录 account-login 改为 Theme 模板内嵌

- 问题：应用注入依赖 layout_option 身份，可视化换图路径偏绕；用户要求内嵌部件即可。
- 变更：`layouts/account/auth.phtml` 槽内嵌 Theme `<w:widget type="form" name="account-login" />`；新增 `widgets/form/account-login`；去掉 Customer `default_injections`。
- 验收：`frontend:check-theme-layout-widgets`；登录页含 `account-login-widget`；主题编辑器可改背景图。

## 2026-08-28 — 可视化编辑器内店面购物运行时放行

- 问题：编辑器 iframe 点击迷你购物车会把 `/cart` 链接触发成「切换 cart 布局」，抽屉被冲掉并一直转圈；`data-preview-mode` 还会短路真实拉车。
- 原则：主题预览内购物/加购/心愿单等店面交互保持 live，与真站同一套 API；仅迷你车专用布局画布可用 demo-chrome。
- 变更：`isShopperRuntimeEventTarget` 放行购物控件；mini-cart live + 超时清 busy；trigger 标 `data-editor-interactive`。
- 关键修复：父页 `instanceof Element` 对 iframe 节点跨 realm 恒为 false，白名单失效；改为 `typeof target.closest === 'function'`（`shopper-live2`）。
- 验收：编辑器内点 Cart 打开抽屉且不切布局；加购/拉车正常；「查看购物车/去结算」仍可预览导航。

## 2026-08-28 — 登录 account-login 部件背景舞台

- 状态：`superseded`（内嵌取代注入占位）
- 问题：登录区背景死板深灰，无法在后台换图。
- 变更：曾用 Customer default_injections + 占位；已改为 Theme 模板内嵌。
- 验收：见上条。

## 2026-08-28 — 认证布局默认主题头尾（登录 Amazon 风格配套）

- 问题：`account.auth` 与 fast-path Observer 强制关闭 header/footer，登录页无商城顶底栏。
- 变更：`layouts/account/auth.phtml` 默认 Partials header/footer；`ControllerFetchFileBefore` 保留显式 meta；有 chrome 时 `ControllerFetchFileAfter` fast-path 回退到真实布局。
- 验收：`ThemeAccountLayoutsPartialsGuardTest`；登录页 HTML 含主题 header/footer。

## 2026-08-28 — 编辑预览特色产品 CSS 打乱（DOM 二次解析）

- 问题：主题编辑器 iframe（`theme-preview/content`）内 `featured-products` 丢失 `weline-amz-card-widget` / `amazon-product-card.css`，卡片样式乱；前台真实首页正常。
- 根因：`ThemePreviewContentRenderer::extractSlotHtml` 在 `processSlots` 恢复 script/style 后再次 `DOMDocument::loadHTML` 且未 park；前置槽（如 promo-banner）内联 JS 含裸 `<` 时截断后续槽 HTML。空 content 时 `theme-preview/content.phtml` stub 还会冲掉首页嵌套槽。
- 修复：extractSlotHtml park/restore；promo-banner 比较改为 `!(x >= 24)`；purchase-actions URL `&amp;`；空 content 不再输出 stub。
- 验收：契约测试 `testExtractSlotHtmlPreservesFeaturedAmazonMarkupWhenPriorSlotHasScriptLt`；预览 HTML 含 amazon 卡片作用域。

## 2026-08-28 — remote-drawer 内部区域同步主题 loading

- 当前状态：`implemented`
- 问题：工具栏按钮有 loading 时，iframe 内表单仍可编辑，异步期间易被改脏。
- 变更：`setActionBusy` 同步在 frame 外包一层 `w-remote-drawer__frame-host`，显示主题 loading 遮罩并 `pointer-events:none` 阻断交互；清除 busy 时隐藏。
- 验收：`RemoteDrawerActionLoadingContractTest`；网站管理编辑抽屉点保存/刷新时内部区域遮罩可见且不可编辑。

## 2026-08-28 — 全局 remote-drawer 工具栏主题 loading

- 当前状态：`implemented`
- 问题：编辑网站等全局 offcanvas 点「刷新/保存」无主题 loading，异步期间可重复点击。
- 变更：`registerRemoteDrawer` 在 reload/submit 时对触发按钮 `setAttribute('aria-busy','true')` 并禁用工具栏动作；iframe `load` / 动作失败 / drawer `close` 清除；foundation 为 `.w-button[aria-busy=true]` 增加主题 spinner。
- 验收：`RemoteDrawerActionLoadingContractTest`；`resource:compile welineUi` 后网站管理编辑抽屉点击保存可见按钮 loading。

## 2026-08-28 — 404 推荐加购需 base::body-end

- 当前状态：`fixed`
- 问题：`not_found` 缺 `base::body-end`，Cart/Compare 购买与卡片操作脚本未注入；推荐 CTA 无 Cart 契约。
- 变更：布局补 `base::head-after/body-start/body-end`；文档约定加购 markup；重发静态 404。
- 验收：Browser 点击「加入购物车」出现「已加入购物车」通知，迷你购物车计数为 1。

## 2026-08-28 — 产品卡 shopper 操作按钮亚马逊风格

- 当前状态：`implemented`
- 问题：产品卡图区收藏/对比/快速查看呈硬边框白方块，与亚马逊式轻量圆形控件不一致。
- 变更：`shopper-actions.phtml` 增加 `product-actions--amz`；`amazon-product-card.css` 与全局 `product-shopper-chrome.css` 统一为无硬边框圆形白底、柔和阴影、hover `#c45500` / active `#e47911`；缓存戳 `20260828-amz-actions1`。
- 验收：首页推荐/新品卡片悬停可见横向圆形操作钮；契约测试覆盖样式关键字。

## 2026-08-28 — 404 推荐产品链接须根相对

- 当前状态：`fixed`
- 问题：静态 404 快照中推荐产品卡片链接错误（空 host 烘焙）。
- 变更：`doc/hook/frontend/layouts/not-found/recommendations.md` 明确 `/product/{slug}` 约定；`StorefrontNotFoundStaticGenerator::publishAll` 已重发各语种静态页。
- 验收：嵌套路径 404 响应内推荐区 href 均为 `/product/...`。

## 2026-08-27 — 主题开发文档：前端 i18n 推荐 `@lang` / `<lang>`

- 当前状态：`implemented`
- 变更：`Theme开发总指南.md` 新增 §4.1；`README.md`、`frontend/README.md`、`部件开发指南.md` 同步强调前端模板优先 `<lang>` / `@lang()` 编译期静态译文，避免 HTML 里 `<?= __() ?>`。

## 2026-08-27 — 演示商品标记标签 DEMO（REQ-THEME-0013）

- 当前状态：`implementing`
- 问题：推荐产品等 Theme 演示目录商品无「演示」角标，易被当成可售真品（加购常因无 offer 禁用呈灰色）。
- 实现：标签体系新增 `demo` kind；`ThemeDemoCatalog` 输出 `is_demo=1`；`labels.phtml` / 色盘 / foundation CSS 同步；i18n `DEMO`→演示。`featured-products` 线上走真实目录、无货隐藏，仅主题预览回落演示占位。
- 验收：`ProductLabelStyleTest`、`ThemeDemoCatalogTest`；店面演示卡片可见演示标签。

## 2026-08-26 — 可视化主题编辑器预览 Token 与编辑壳隔离

- 当前状态：`implemented`
- 问题：可视化主题编辑器自身被识别成预览页，显示「预览模式 / 退出预览」浮窗；退出后刷新又复现，iframe 还可能报「Theme preview requires a valid token or an authenticated backend session」。
- 根因：编辑器首页为 iframe 生成短期 Token 后，又把 `shell=preview + preview_token` 持久化并同步回同一个后台 Request/Session；刷新时重复生成、重复污染。旧实现的退出网关还返回相对后台路径，前台语言路由会把它改写到 `/en_US/{backend-prefix}/...` 并落入 404。
- 实现：编辑壳入口显式清空既有 `preview_token`；首页生成的 Token 只放进 iframe `src`，不写编辑器 Session、Request 或 Cookie；显式打开独立预览仍沿用原有持久化流程。退出网关把合法后台路径归一成当前 origin 的绝对 HTTPS URL，并继续移除 Token 参数。
- 运行验收（专用 WLS `:9559`）：同一后台会话显式启动预览返回 HTTP 200 与短期 Token；随后刷新编辑器完整 HTML 中 `preview-exit-float=0`、`preview-exit-btn=0`，iframe URL 含 Token且 iframe 返回 200、无鉴权/WLS 错误；退出返回 301 到同源后台绝对地址、清除 HttpOnly `weline_preview_token`，退出后再次刷新仍为 0/0。回归用例 8/8、25 assertions。
- 交付入口：[主题编辑器](https://p05113ef3.weline.test:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/theme/backend/theme-editor?theme_id=1&page_type=homepage&layout_option=default&editor_area=frontend&preview_area=frontend&status=draft&interaction_mode=edit)；[预览退出网关](https://p05113ef3.weline.test:9555/theme/frontend/theme-preview/gateway?exit=1)。

## 2026-08-26 — 主题编辑器预览 iframe 专项缓存

- 当前状态：`implemented`
- 问题：用户反馈主题编辑器首页预览仍 ~4.5s（网络链路 ~4s）；与 storefront FPC 优化无关——预览走 `/theme/frontend/theme-preview/content`，禁用 FPC 且 `editor_mode=1` 曾 bypass 导航片段缓存。
- 实现：
  1. `ThemePreviewRenderCache`：scope-hot 缓存整页 iframe HTML（draft revision 指纹，保存后自动 miss）；
  2. `HeaderNavFragment`：`theme-preview/content` 下允许 `editor_mode=1` 仍走 mega-menu/sidebar 片段缓存（catalog 数据，非 draft slot）。
- 验收：`ThemePreviewRenderCacheTest`、`HeaderNavFragmentBypassTest`、`ThemeEditorUiCapabilityContractTest`；对照 timing.log `theme-preview/content` 冷/暖 `total_ms`。

## 2026-08-26 — P1：FPC 预热 + header chrome 跨 worker SWR

- 当前状态：`implemented`
- 问题：FPC 冷路径 `build_lock` 仍 5–8s；header chrome 仅进程内 LRU，多 worker 重复 SSR。
- 实现：
  1. `StorefrontFpcWarmer`（`cache:warm` / `Weline_Server::start_after` / `cache:clear` 后）HTTP 预热 `/`、`/en_US/`、`/zh_Hans_CN/`；
  2. `Partials` 前端 header/footer 在 L1 miss 时走 `StorefrontScopeHotCache`（website+lang，SWR）；分类变更清 `theme.chrome.*`。
- 验收：`StorefrontFpcWarmerTest`；`cache:clear` 后首页 TTFB 与 timing 冷路径对比。


- 当前状态：`implemented`
- 现象：分类页（如 `/category/books/av/albums`）Slot 处理前有 `#category-layout-main`（~870KB），处理后主区消失（~407KB）；仅剩 Header/Footer。
- 根因：已发布布局同时有 header 槽（`delivery`）与 content 槽（`category-recommendations`）。`narrowHtmlToSlotFragment` 裁出跨 `</header>` 的片段；`LIBXML_HTML_NOIMPLIED` 把 `weline-main-content` 提升为 slot-root 的 document 兄弟，序列化只输出 slot-root 子节点，主内容被丢。
- 实现：1) 边界跨越 `</header>` 时跳过 narrow，改走整 body DOM；2) `reparentPromotedSlotRootSiblings` 把被提升的兄弟挂回 slot-root。
- 验收：`SlotRendererHeaderNarrowFragmentTest`（cross-header skip + reparent）；分类页 HTML 含 `#category-layout-main` / `.weline-main-content`。

## 2026-08-26 — slot 工具条仅 hover 显示

- 问题：插槽工具条 anchored-float 在 hover 结束后未 hide，fixed 定位条常驻（如 search 槽上方）。
- 修复：`clearSlotHoverTargets` / 父页 MutationObserver 在 hover 结束时调用 `anchored-float.hide`；初始化 attach 后默认 hide；CSS 非 hover 目标强制 `display:none`。
- 验收：`ThemeEditorUiCapabilityContractTest`；`resource:compile welineUi` + WLS reload。

## 2026-08-25 — slot 默认工具条增加显式「初始化」

- 当前状态：`implemented`
- 产品规则：默认部件只在主题初始化（第一个主题版本 / 未改过）时自动出现；用户改过后哪怕清空 slot 也不自动回填。
- 显式入口：插槽 hover 工具条增加「初始化」按钮，调用 `init-slot-defaults` 按 `default_injections` 回填该 slot 缺失默认部件（清除该 slot 的 `user_deleted`）。
- 实现：`WidgetDefaultInjectionService::initSlotDefaultInjections`、`ThemeEditor::postInitSlotDefaults`、Query 路由、`editor-mode`/`theme-editor` 工具条与消息、契约与 i18n。
- 验收：`ThemeEditorEmptySlotNoAutoInjectContractTest`、`ThemeEditorUiCapabilityContractTest`；`resource:compile welineUi`。

## 2026-08-25 — 空 slot 禁止打开/渲染时自动回填 default_injections

- 当前状态：`implemented`
- 问题：主题编辑器删除部件后刷新，空 slot 仍被 `applyMissingForAllPageTypes`（打开编辑器）与 `applyMissingForLayout`（Slot 渲染）自动补回。
- 产品规则：默认部件只在主题初始化，或用户「应用」tab / 显式 slot 初始化时写入；空 slot 必须可保持为空。
- 实现：移除 `ThemeEditor::index` 与 `SlotRendererService::getLayoutData` 中的自动回填；保留草稿重置、首次入库、Dashboard view ready、手动应用入口。
- 验收：`ThemeEditorEmptySlotNoAutoInjectContractTest`；删除后刷新不再回填。

## 2026-08-25 — theme:address 补 runtimeCallback，修复分类页被 PHP 泄漏吞掉

- 当前状态：`implemented`
- 现象：前台分类页（如 `/category/books/av/albums`）主内容空白/布局乱；HTML 中泄漏 `Weline_Taglib_resolve`/`$Taglib__*`，且 `=>`/`?>` 被转成 `&gt;`。
- 根因：`Shipping` quick-add 里 `<w:theme:address country="<?= ... ?>">` 因动态属性走 `renderRuntimeTag`；`Address` 只有编译期 `callback`（返回 `<?php ... ?>`），运行期当文本输出；浏览器把未闭合的 `<?php` 当 PI 吞掉后续主内容。
- 实现：参照 `Icon::runtimeCallback`，为 `Theme\Taglib\Address` 增加运行期直接输出 `w-address` HTML；编译期路径不变。另修 header `$renderMenuItem` 闭包未 `use $headerEscDesc` 的 Warning。
- 验收：`AddressTaglibRuntimeCallbackTest`；分类页 HTML 无 Taglib PHP 泄漏，quick-add 正常输出 `data-w-address`。
- 残留观察：泄漏修复后主区仍缺席的问题已由同日「SlotRenderer 跨 header/content narrow 丢主内容」条目修复。

## 2026-08-25 — 店面媒体禁止 data:image 直出，统一一份静态 SVG src

- 当前状态：`implemented`
- 规则：媒体图不允许 `data:image/svg+xml;charset=…` 直出；无图只用 `images/storefront-placeholder/default.svg` 一份本地文件（浏览器只加载一次）。
- 实现：`StorefrontImagePlaceholder` 收敛为单文件；JS/`data-fallback` 遇 data URI 改为静态 src；文档改为硬禁止。
- 验收：首页 mega 占位均为同一 `default.svg`；单测；`storefront-image-placeholder.md`。

## 2026-08-25 — 店面占位图改为静态 SVG src

- 当前状态：`superseded`（见上条：进一步收敛为单文件 + 硬禁止 data URI）
- 问题：首页/mega-menu 数百次内联 `data:image/svg+xml`（仅 6 种重复），HTML 多出约 300KB，拖慢预览与店面。
- 实现：`StorefrontImagePlaceholder` / `ThemeDemoCatalog::productImage` 改为静态 SVG 普通 `src`；JS 同步。
- 验收：首页 `img[src^=data:image]` 占位为 0。

## 2026-08-25 — 预览切语言后 header 语言切换器粘着（chrome 缓存）

- 当前状态：`implemented`
- 问题：`locale=en_US` 已正确；再切 `zh_Hans_CN` 时 `html[data-lang]`/themeConfig 已是中文，但 header 语言切换器仍 English，欢迎条仍英文。
- 根因：前端 chrome header 进程内 LRU 的 `KeyBuilder::environmentHash` 在 `website=true` 时用 storefront Cookie 语言维度，忽略 `State::setRequestLanguageOverride`；LanguageSwitcher HTML 被英文键命中。
- 修复：chrome schema `v4`，显式写入 `State::getLang()`/`getLangLocal()`；header data context 同步带 lang。
- 验收：预览 English↔中文后切换器与文案同步；`PartialsChromeCachePolicyTest` schema 契约。

## 2026-08-25 — 预览 Token 粘着 locale 盖住 URL 语言切换

- 当前状态：`implemented`
- 问题：工具栏/预览内切 `en_US` 后 URL 已带 `locale=en_US`，但 iframe 仍中文；`html[data-lang]`/`LanguageSwitcher`/`Phrase` 跟 Cookie 或 Token 内旧 locale。
- 根因：`PreviewContextService::getCurrentContext` 在合并 request 后仍用 preview token 覆盖全部字段（含 `locale`），显式 query `locale` 被粘着 Token 语言压掉；`applyPreviewLocale` 拿不到英文 override。
- 修复：Token 仍锁定 theme/scope/target；token 合并后再次应用显式 `locale`（优先 raw query，WLS 剥掉 QUERY 时回退 request bag，含 `default`/空清空）。配合 `i18n.js` document/query 优先于 Cookie。
- 验收：`PreviewContextServiceTest` locale 覆盖用例；预览内 English↔中文后切换器与文案同步，Cookie 不变。

## 2026-08-25 — 预览内切语言后切换器仍钉中文（i18n.js Cookie 优先）

- 当前状态：`implemented`
- 问题：预览 URL/`html[data-lang]` 已是 `en_US`，但语言切换器文案与 `aria-checked` 仍像中文；用户以为「点了没切」。
- 根因：`WelineI18n.getCurrentLang()` 在无路径语言段时优先 `WELINE_USER_LANG` Cookie，`updateCurrentLanguageDisplay` 启动时把服务端已渲染的 current 盖回站外偏好语言。不是 Taglib 缺兼容。
- 修复：`getCurrentLang` 改为 path → `data-lang`/`lang` → query `locale`/`lang` → Cookie → 主题配置；预览切换仍不写 Cookie。
- 验收：预览内 English↔中文后切换器与 `html[lang]` 一致；Cookie 不变；`I18nJsAuthoritativeHrefBehaviorTest.js`。

## 2026-08-25 — 部件配置打开时切换语言失败（ScopeIdentity）

- 当前状态：`implemented`（浏览器复现：打开 `checkout-delivery-context` 配置后切 `zh_Hans_CN`，toast「语言切换失败，已恢复原语言」）
- 根因：`setActiveConfigLocale` → `widget-config?locale=` 走 `mergeTranslatedPaths` → `ThemeContextService::resolveCurrentScope(frontend)`；backend `theme.editorRequest` 无冻结 storefront `ScopeIdentity`，QueryBin 500。默认（空）locale 不合并翻译故正常。
- 修复：`ThemeData::seedRequestedScope`；`ThemeEditor::mergeTranslatedPathsForLayout` 从 `editor_context` 注入存储 scope，缺省回退 `DEFAULT_SCOPE`，不伪造 RequestContext 身份。
- 验收：部件配置打开时切语言成功；契约 `testWidgetConfigLocaleMergeSeedsEditorStorageScopeWithoutFrozenIdentity`。

## 2026-08-25 — 主题编辑器预览服务端降载

- 当前状态：`implemented`
- 问题：可视化编辑 iframe `theme-preview/content` TTFB 高（homepage p50≈1s / max≈36s）；根因是全量动态渲染且强制 `skip_view_file_cache`，header mega-menu + delivery/captcha 叠加热点。
- 实现：预览 Content 不再强制跳过视图路径缓存（仍 `Cache-Control: private, no-store`）；`editor_mode` 下 header 扁平化分类树跳过 mega-menu 面板；Checkout 配送上下文 stub 并跳过 quick-add。
- 验收：`PreviewRequestInspectorTest`、`ThemeEditorUiCapabilityContractTest`、`CheckoutDeliveryQuickAddHookContractTest`；建议对照 `var/log/wls/timing.log` 看 preview total_ms。

## 2026-08-25 — 激活主题后网站首页接到 layouts/homepage

- 当前状态：`implemented`
- 需求 ID：`REQ-THEME-0011`
- 问题：激活前台主题后，站点 `/` 仍可能走 `Weline_Index` 自绘页，未进入 Theme `layouts/homepage`。
- 实现：`activateThemeForArea` 成功后种子 homepage（不覆盖已有布局）；`FrontendLayoutProvider` 增加 `homepage`；Index 在激活前台主题时 `layoutType=homepage` + shell。
- 验收：契约测试 `IndexHomepageLayoutContractTest`；有主题时根路径应含 `theme-layout-homepage`。

## 2026-08-25 — 编辑器预览可见商品评论壳

- 当前状态：`implemented`
- 问题：应用 Tab 显示「商品评论 / 已应用」，但主题编辑器预览看不到部件。
- 根因：`SlotRendererService` 渲染时硬编码 `preview_mode=false`；评论部件在无商品 offer 且非预览时直接空返回。
- 实现：编辑器/live 预览传 `preview_mode=true`；评论部件识别 `editor_mode`；预览 content 优先 `product-reviews`。
- 验收：商品布局编辑预览可见评论壳；前台 `/product/2` 仍正常。

## 2026-08-25 — 应用 Tab 展示已声明默认注入（含万能评论）

- 当前状态：`implemented`
- 问题：商品详情评论槽已入库但旧模板缓存导致评论区段不渲染；「应用」Tab 只列缺失默认注入，已应用后为 0，万能评论看起来未被识别。
- 实现：`WidgetDefaultInjectionService::getDeclaredForLayout` + API `injection_status`；应用 Tab 保留已声明项并标「已应用」；product 布局布尔参数归一；清模板缓存后详情可见评论。
- 验收：`storefront-product-reviews`；编辑器 product 布局「应用」可见「商品评论」。

## 2026-08-25 — 店面商品无图统一 SVG 占位标准

- 当前状态：`implemented`
- 需求 ID：`REQ-THEME-0010`
- 问题：各商品图位占位实现分散（字母 W、FCDC、内联 SVG、破图红字），标准不统一。
- 实现：新增 `StorefrontImagePlaceholder` 与全局 `storefront-image-fallback.js`（`base::body-end`）；Product/Theme 商品图位接入 `resolve` + `data-storefront-img`。
- 验收：无图/失效图回退统一 SVG；Helper 单测覆盖。

## 2026-08-25 — 非 Theme 部件禁止布局内嵌（硬规则）

- 当前状态：`implemented`
- 问题：header/cart 等处内嵌 Checkout/Product 部件，与「仅 Theme 部件可布局内嵌、其他走应用注入」不一致。
- 实现：去掉 `checkout-delivery-context` / cart `cross-sell` 内嵌；文档与 MCP `frontend_development` 硬规则；`frontend:check-theme-layout-widgets` + ContractTest。
- 验收：CLI 与 Theme/Checkout/Product 契约测试。

## 2026-08-25 — 预览 Token 写入共享缓存失败时回退 file

- 当前状态：`implemented`
- 问题：主题编辑器偶发 toast「Theme preview token could not be written to shared cache」；WLS 下 `w_cache('theme')` 可能被劫持为 `wls_memory`，侧车短暂不可用时 `set` 失败，编辑器初始化与部件库卡住。
- 实现：`PreviewTokenService` 对 capability 双写（主池 + `var/cache/theme_preview_token` 共享 file）；校验/续期/删除均读删双路径；仅双写均失败才抛原异常。
- 验收：单元测试覆盖主池失败 file 成功、双失败、主池 miss 读 file；编辑器编辑模式可进、部件库可加载。

## 2026-08-25 — 可视化编辑产品页改用 Theme 预览路由并识别 product/{slug}

- 当前状态：`implemented`
- 需求 ID：`REQ-THEME-0009`
- 问题：产品预览默认走 `product/default`，被 Product Router 当成不存在的 slug 301 到 `/products`；`detectLayoutTypeFromUri` 在 trim 后用不含前导斜杠的 `str_contains('/product/')`，裸路径 `product/{slug}` 无法识别，带 `preview_theme` 时也不能改写到 Theme 可视化网关。
- 实现：URI 段匹配修复；产品/分类/列表预览路由改为 `theme/frontend/theme-preview/content`；preview rewrite 保留 `theme_public_route=product/{slug}`。
- 验收：`/product/benq-screenbar` 识别为 product；带 preview 上下文改写到 theme-preview gateway 且保留 public route。

## 2026-08-25 — 商品详情主信息槽改为预览占位 + 应用注入

- 当前状态：`implemented`
- 需求 ID：`REQ-THEME-0008`
- 问题：`product-main` 仍靠 Controller `contentTemplate` 渲染产品主信息，主题编辑器无法按部件编排，空槽也缺少可视化预览。
- 实现：布局去掉 `contentTemplate` 回退，改为 FCDC/标题/SKU 预览占位；禁止内嵌 `product-info`；seeder 由 Product `product-info` 默认注入该槽。
- 验收：布局契约测试；详情页主信息由 Product 部件注入。

## 2026-08-25 — 预览内点语言选项“点了没切换”

- 当前状态：`implemented`
- 浏览器证据：iframe 已 `postMessage(locale-change)`，但父页 `setActiveConfigLocale` 在 state 已是目标语言时早退，预览 URL 仍 `locale=default`，`html[lang]`/`语言切换器`不变。不是 i18n 标签缺兼容，是预览未按新 locale 重载。
- 修复：`locale-change` 带 `forceReload: true`；同语言也强制 `loadLayoutPreview`；拦截优先命中 `.w-language-switcher__option`。缓存 `v4`。
- 验收：预览里头点中文/英文后，工具栏语言与预览 `lang`/切换器文案应同步变化。

## 2026-08-25 — 编辑器语言切换被锁遮罩挡住

- 当前状态：`implemented`
- 浏览器复现：`#themeEditorLockOverlay` 全页盖住工具栏；`check-lock` 失败时无法点「配置语言」。窄屏语言选择还会被 overflow 收进「更多」。
- 修复：`check-lock` 改为 POST JSON（`postCheckLock`）并重试；锁遮罩挂到 `.editor-main` 不挡工具栏；语言 select `data-w-toolbar-overflow-pin`。缓存 `?v=20260825-editor-locale-v3`。
- 验收：即使锁失败也可切语言预览；锁成功后工具栏语言仍可用。

## 2026-08-25 — 预览 iframe 内语言切换器无法切换（不改外部 Cookie）

- 当前状态：`implemented`
- 问题：编辑模式下点预览里头语言选项被 `editor-mode` 拦掉；预览模式会导航出编辑器并可能写 `WELINE_USER_LANG`。
- 修复：iframe 捕获阶段拦截语言选项 → `postMessage(locale-change)` → 父编辑器 `setActiveConfigLocale` 重载预览；不写 Cookie、不离开 theme-preview。工具栏语言与预览内切换走同一路径。缓存 `?v=20260825-editor-locale-v2`。
- 验收：Edit/Preview 下预览内切语言应刷新预览且 Cookie 不变；后台/前台顶栏语言切换仍正常。

## 2026-08-25 — 可视化编辑器内切换语言不生效

- 当前状态：`implemented`（浏览器复现：工具栏切 `en_US` 后预览 URL 已带 `locale=en_US`，iframe 仍 `zh-CN` 且常 500「图片语境语言与当前布局语言不一致」）
- 根因：
  1. `State::getLang()` 优先 Cookie，`applyPreviewLocale` 只写 `RequestContext`，Phrase/`html[lang]` 仍跟后台 Cookie；
  2. `PreviewContextService` 把 `locale=default` 规范化成空串后忽略，会话仍粘着上次语言；
  3. `FileImageLayoutValueHydrator` 在 preview 下布局 locale ≠ 图片 usage locale 直接抛错。
- 修复：请求级 `State::setRequestLanguageOverride`（不写 `WELINE_USER_LANG`）；`locale=default` 显式清空预览会话语言；preview purpose 下 file-image 回退到 usage locale。外部顶栏语言切换仍走 Cookie + 导航，不受影响。
- 验收：编辑器内切英文预览不再 500，文案/`lang` 跟预览 locale；后台顶栏切换语言仍改 Cookie 并导航。

## 2026-08-25 — 多语言面板打开自动回填（TE-CAP-020）

- 当前状态：`implemented`
- 问题：点开「多语言」只见语言名 placeholder，已保存译文不回填。
- 根因：「保存多语言」写入 scoped i18n 草稿，旧 `loadI18nValues` 串行打 `widget-config` 且草稿读取失败会整请求失败；未优先读 scoped workspace。
- 修复：打开面板先回填源文案；按 node_uid 并行加载各 locale 的 scoped i18n 草稿；再回退 `widget-field-i18n` / 并行 `widget-config`；`getWidgetConfig` 草稿异常改为软失败。缓存 `?v=20260825-i18n-scoped-backfill-v4`。
- 验收：保存多语言后关闭再打开，各语言输入应为真实译文（非灰色 placeholder）。

## 2026-08-25 — 部件悬浮条铅笔打开配置（非 AI）

- 当前状态：`implemented`
- 问题：悬浮工具栏铅笔图标绑在 `ai-edit` 上，点击误开 AI 编辑；用户期望铅笔=部件配置，星标=AI 编辑。
- 修复：新增 `data-action="config"` 铅笔按钮打开配置面板；`ai-edit` 改用 sparkles 星标图标。缓存 `?v=20260825-widget-config-pencil-v1`。
- 验收：选中部件后点铅笔应打开左侧配置；点星标仍打开 AI 编辑。

## 2026-08-25 — 侧栏配置/部件恢复推挤预览（TE-CAP side-panel-push）

- 当前状态：`implemented`（浏览器复现：756px 视口 `editor-compact-mode` 下面板 `position:absolute` 盖住全宽预览）
- 根因：`@media (max-width: 1100px)` 将紧凑模式侧栏改为覆盖式 drawer，并把 `--w-theme-editor-active-*-panel-width` 置 0、主区单列。
- 修复：窄屏仍用网格列宽推挤预览；去掉 compact absolute 覆盖与单列强制。缓存 `?v=20260825-side-panel-push-v1`。窄屏仍互斥只开一个侧栏。
- 验收：打开配置/部件后预览变窄，面板与预览并排，不悬浮遮盖。

## 2026-08-25 — 模板部件 AI 翻译静默无效果（TE-CAP-020）

- 当前状态：`implemented`（浏览器复现 + 修复；契约断言已补）
- 根因：结账页等模板内嵌部件（如 `checkout-delivery-context`）无数字 `layout_id`，多语言面板 `data-layout-id=""`；前端 `[data-ai-i18n]` 点击守卫 `if (panel && fieldKey && layoutId)` 因空字符串静默 return。后端 `aiTranslateConfigPayload` 并不依赖 `layout_id`。
- 修复：`theme-editor.js` / `weline-theme-editor.js` 在仅有 panel+fieldKey 时即调用 `translateI18nValues`；缺字段时 toast「无法定位多语言字段」。静态资源缓存 `?v=20260825-ai-translate-template-v1`。
- 验收：模板部件源文案已填时点 AI Translate 应发起 `ai-translate-config` 并回填或错误 toast，不再无反馈。

## 2026-08-24 — 主题 CSS/PHTML 禁止硬编码（REQ-THEME-0007）

- 当前状态：`implemented_validated`（文档 + 默认主题全量 Token 化；复扫业务侧硬编码为 0；**提取必落盘**纠偏；**Surface/Text 语义**防黑底黑字）
- 需求 ID：`REQ-THEME-0007`
- 范围：强约束文档；扩展语义 Token；剥离 `var(--t, 字面量)`；默认主题业务 CSS/PHTML 映射到 Token；尺寸等杂项可收编 `variables/_auto-literals.css`，**颜色禁止丢在 auto-literals**。
- 纠偏（同日）：将前后台 `_auto-literals.css` 中 70 个 `--token-color-*` 迁入 `variables/_colors.css`（color panel）；补齐 bridge 色/阴影默认；文档增加「提取必落盘」高压线。
- 纠偏（同日·UI）：新增 `theme-surface-text-roles.md`；`header-belt` 标记 `data-surface="inverse"`；配送入口用 `.w-text` / `.w-text-muted`；去掉脆弱 `$colors['textLight']` 注入。
- 纠偏（同日·布局）：`@media` 内 `var(--breakpoint-*)` / `var(--token-bp-*)` 全部还原为 `px` 字面量（105 处，68 文件）；补齐 `weshop-*` 桥接别名；配送浮层默认样式写入 `theme.css`。
- 纠偏（同日·控件高度）：统一 `--control-height-*` 刻度（sm=35/md=40/default=42/lg=44/xl=48）；`size-control-*` 别名对齐；`.w-button[data-size]` / `.btn-sm` / 表单控件全部接 `weline-component-control-height-*`；文档增加「提取须绑定组件」。
- 验收：排除 `variables/**`、`colors/**`、注释与 data-URL 后，`hard_color`/`hard_size`/`var_fallback` = 0；`--token-color-*` 仅存在于 `_colors.css`。
- 基线（改造前）：约 color 511、size 1415、fallback 303、涉文件 96。

## 2.1.1 — Theme Editor 四级 Scope 逐值继承

- 当前状态：`implemented_validated`；自动化与唯一 WLS + 内置 Browser 真实验收均完成。
- 需求 ID：`REQ-THEME-0004`。
- 最终语义：Scope 是编辑器最高级上下文，层级固定为 `Global → Website → Store → Channel`。Website、Store、Channel 均可独立切换 Theme；本级没有 Theme Patch 或配置 Patch 时继承直接父级，仅实际修改路径成为本级所有，恢复继承即删除该路径 Patch。
- 需求纠正：本节覆盖 2026-08-22 12:25 “不新增 Channel”以及 15:43 三层树验收的阶段性结论；它们只保留为历史过程证据，不再代表当前产品契约。
- 实现范围：公共 typed Scope/父链与权威目录、Theme workspace/revision/patch/release、Theme/布局/Meta 等 Adapter、稳定节点 UID 与结构合并、父级发布传播、迁移预检和幂等回填、Scope-first 编辑器、逐字段来源/恢复继承、容器自适应输入与浮层限界。
- 静态复审：架构边界、缺陷和安全通过；运行时只读已发布 Release，草稿预览使用受控 typed context，写入拒绝短 Scope 和伪造 ID/code，业务 target 与 Website/Store/Channel Scope 正交。结构位置所有权与节点内容所有权已分离，新增后移动和新增后删除可组合且不会制造错误墓碑。
- 调试修复：frontend iframe 改为通过预览启动 API 获取短期 Token，Scope 快速切换使用序列号丢弃过期异步响应；`ThemeData` 请求态按连接身份哈希隔离；普通 backend 无冻结 Scope 时使用显式 Global 布局身份；GET 请求会修复缺失或无效的 typed `editor_context`，对象查询值统一 JSON 序列化；外观盘复合 token 控件补齐 Grid/Flex 可收缩与换行契约。
- 单身份写边界：默认注入等布局写入只作用于当前 typed context，不向同布局其他身份批量扩散；UI 仅暴露“应用当前身份”，E2E 验证同级身份保持未修改。
- 验收证据：聚焦 Theme 集合 `118 tests / 582 assertions`；UI/请求契约 `17/267`，新增 UI/普通后台布局身份回归 `11/250`；迁移 0 碰撞/0 重复 UID/0 歧义且幂等；官方 UI 连续编译 JS/CSS 哈希稳定；唯一 WLS 9654 上五个专项 E2E 全部通过。
- Browser 证据：Global/Website/Store/Channel 四层真实切换，Store/Channel canonical Scope、继承来源与 Token 预览正确；320px 外观编辑器 `30 controls / 0 overflow`，640px（200% 等效）`31 / 0`，默认视口 `8 / 0`；Theme Editor 与 Dashboard 最终 `0 error / 0 warning`。
- 范围排除：不自动分发消费站点，不清理旧表，不新增平行 Scope 身份。

## 2.1.0 — 后台媒体管理器 Theme Scope 回归修复

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-THEME-0004`、`REQ-MEDIA-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 需求差异：无；恢复已确认的后台媒体管理器可用性，不新增 Scope、权限或预览语义。
- 实现执行者/范围：Codex 主代理直接实施；Theme 后台壳层 Scope 解析、MediaManager 后台 FileAsset 访问上下文、聚焦测试与记录。
- 安全边界：普通 backend 页面可使用显式 Global 布局范围；Theme Editor/预览仍必须提供权威冻结 ScopeIdentity，不读取 URL/body 伪造身份。
- 复审：通过。架构上由 Theme 布局观察器处理 backend 壳层，不耦合 MediaManager；缺陷上两处 ThemeData 预加载均复用已解析 Scope；安全上仅普通 backend 且请求无 ScopeIdentity 时使用 Global，Theme Editor/预览与 frontend 继续 fail-closed，ACL/认证未改。
- 门禁：Theme 聚焦回归 `13 tests / 41 assertions`、MediaManager Global 访问上下文 `2 tests / 10 assertions`、PHP 语法与目标差异检查通过；`default:9555` 四个 Worker 滚动重载成功。
- 2026-08-22 补记：`ThemeContextService::resolveCurrentScope` 对普通 backend 在无冻结 ScopeIdentity 时回退 `DEFAULT_SCOPE`，修复 Partials 直调导致的媒体管理器 500；并刷新 Storage `provides`/DI 以解析 `StorageConfigSnapshotGuardInterface`。真实 Weline Chrome 验收确认 `/media/backend/manager` 可加载文件夹树与 11 个根项目，嵌入式 `/media/backend/manager/iframe` 可按图片类型过滤、预览并选中 `image.png`；连接器请求均为 200。127.0.0.1 因主机 Cookie 隔离跳转后台登录，不再返回 500。
- 2026-08-22 补记：`SlotRendererService::currentLayoutIdentity` / 类型化布局值解析对普通 backend 同样回退 Global，修复仪表盘 `/weline_dashboard/backend/dashboard` 布局插槽渲染 500；不向 RequestContext 安装合成 ScopeIdentity。

## 2026-08-22 — loading 区块语义标识修复

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-THEME-0005`
- 范围：仅把 loading 源模板的非语义 `weline-code` 调整为 `theme.component.loading`，保留用户现有 UI 改动。
- 验收：`frontend:check-section-code` 0 违规、主题编译和规范仓完整路由升级通过。

## 2026-08-22 — 商品业务路由与 product 布局内容回退修复

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-THEME-0006`
- 根因：Theme 公共页面 fallback 在 Product Router 前消费商品别名；product 布局只读取 `meta.content`，忽略 Controller 的 `contentTemplate`。
- 实现：安装 Product 时让 `/products`、`/product-list`、`/product/{正整数}` 继续交给 Product Router；product 布局在无 `meta.content` 时回退渲染 `contentTemplate`。
- 验收：路由聚焦测试 `2 tests / 5 assertions`、布局测试 `2 tests / 11 assertions`、完整 `setup:upgrade --route` 与独立 WLS 商品详情/Review Browser 验收通过。

> 按目标版本记录真实进度与验收证据；不补写未经验证的历史。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.8` | implementation_reopened | `REQ-THEME-0001` | 2026-08-10 | 2026-08-11 | Theme full suite required |
| `1.0.8` | implemented_validated | `REQ-THEME-0002` | 2026-08-17 | 2026-08-17 | Head contracts + storefront Browser pass |
| `2.0.0` | implementing | `REQ-THEME-0003`、`REQ-THEME-0004` | 2026-08-21 | 2026-08-22 | implementation pending |

## 2.0.0 — Weline UI 2.0 自研主题与组件体系

- 基线版本：`1.0.8`
- 目标版本：`2.0.0`
- 当前状态：`implementing`
- 需求 ID：`REQ-THEME-0003`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：Theme UI 架构、资源编译/审计、组件、图标和跨模块旧 UI 迁移；不改业务 API，不同步站点或发布。
- 负责人/确认人：Codex 主任务直接实施 / 用户。

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-theme-0003-weline-ui-20-自研主题与组件体系` | 2026-08-21 |
| 实现执行者/模型/范围已记录 | pass | Codex 主任务直接实施；计划限定路径与零兼容边界 | 2026-08-21 |
| 架构/缺陷/安全复审通过 | pending | — | — |
| 分层测试通过 | pending | — | — |
| 真实路径 WebUI/E2E 通过 | pending | — | — |
| 可重复 E2E 固化并通过 | pending | — | — |
| 功能/运营文档完成 | pending | — | — |
| 用户/产品验收 | pending | — | — |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-29 | empty product widget HTML | empty_html toast → page-context reinject | DEV toast「产品主要信息/商品评论 Widget HTML is empty」；根因 ComponentRenderer unsetData 清掉 storefront_offer，且无 offer 时直接空 return；预览缓存键未含 theme_public_route | SlotRenderer 捕获并回注页面上下文+preview_mode；预览缓存键含 public route；product-info/reviews 无 offer 时输出 unavailable 壳 | 用户 + Composer | SlotRendererService、ThemePreviewRenderCache、product-info/product-reviews、契约 |
| 2026-08-29 | preview exit FPC poison | HIT 毒化首页 → bypass+forbid+清 L1 | 店面退出后仍见「预览模式」浮窗；根因是 `weline_preview_token_wN` Cookie 未进 FPC bypass，预览 HTML 写入公共 Process FPC，退出清 Cookie 后仍 HIT，bootstrap 还可能回种 token | `isEditorOrPreviewRequest`/`Worker mustBypass` 识别作用域预览 Cookie+query；`ResolvePreviewToken` forbid 共享缓存；退出 `clearProcessCache`；CDN 规则改 contains token 名 | 用户 + Composer | `FullPageCacheCoordinator.php`、`WorkerFullPageCacheFastPath.php`、`ResolvePreviewToken.php`、`PreviewExitService.php`、契约 |
| 2026-08-28 | preview exit simplify | POST/form 多重跳转 → 单次 replace | 过度修复导致落到 `gateway?` 空参，报「需要后台登录」JSON 死页 | 恢复简单路径：清客户端 token + `location.replace(gateway?exit=1&…)`；裸 gateway 未登录浏览器导航改 302 回首页 | 用户 + Composer | `LayoutSlotRenderer.php`、`Gateway.php`、`theme-preview.js`、契约 |
| 2026-08-28 | preview exit float stuck | parent-only postMessage → local clear+forceNavigate | 店面「退出预览」点了变处理中却不退出；内嵌壳 parent!==window 时只 postMessage；bootstrap 失效 token 未清 sessionStorage 可能回种 Cookie | navigateExitPreview 始终清客户端态、POST gateway、forceNavigate（replace/href/form）；bootstrap 持久化失败一律 clearClientToken；契约测试 | 用户 + Composer | `LayoutSlotRenderer.php`、`theme-preview.js`、`preview-bootstrap.js`、`PreviewExitFloatNavigateContractTest` |
| 2026-08-28 | widget HTML health | none → DEV/preview inspect+toast | 部件半成品/坏 HTML 导致布局乱却难定位（浏览器会修 DOM） | 服务端 `WidgetHtmlHealthInspector` 检测标签栈/空壳/重复 id；`SlotRendererService` 写入 `data-w-widget-health*`；预览 `editor-mode.js`+父页 `widget-health` 走 `Weline.UI.toast`（禁 alert） | 用户 + Composer | `WidgetHtmlHealthInspector.php`、`SlotRendererService.php`、`editor-mode.js`、`theme-editor.js`、单测、`resource:compile welineUi` |
| 2026-08-26 | slot toolbar selection payload | closest toast → stamped payload | 父页捕获点「选择」时 closest 拿不到插槽，toast「缺少插槽标识」；未对齐 openWidgetPanelForSlotSelection 所需 id/accept/area | 工具条写入 `data-slot-selection`（与 selectSlot 同结构）；父页 `readSlotSelectionFromToolbar`→`handleSlotSelected`；资料不足时不拦截放行 iframe postMessage | 用户 + Composer | `editor-mode.js`、`theme-editor.js`、资源戳、契约、`resource:compile welineUi` |
| 2026-08-26 | slot toolbar immediate actions | tree/confirm → direct | 「选择」弹子树不立刻开部件库；「初始化」弹确认且预览模式被拦截 | 选择直调 selectSlot 开部件侧栏；初始化直调 init-slot-defaults；两消息预览模式前优先处理并切 edit | 用户 + Composer | `editor-mode.js`、`theme-editor.js`、契约、`resource:compile welineUi` |
| 2026-08-26 | slot toolbar kind label | unlabeled → 插槽 prefix | 插槽条与部件条同为 `.widget-hover-actions`，仅靠绿色仍易混淆 | 工具条首项加 `.slot-toolbar-kind` 显示「插槽」；选择按钮左圆角改 0 | 用户 + Composer | `editor-mode.js/css`、契约、`resource:compile welineUi` |
| 2026-08-25 | slot toolbar green | green-amber → pure green | 插槽选择/信息按钮沿用绿-琥珀渐变，与「插槽用绿色」要求不符 | 新增 `--editor-mode-slot-toolbar-gradient/#22c55e→#15803d`；选择/信息按钮与槽名标签改用绿色白字；绿边框 | 用户 + Composer | `editor-mode.css`、契约、`resource:compile welineUi` |
| 2026-08-25 | slot toolbar as widget | half custom → widget-hover-actions | 插槽模式 CSS 全局隐藏 `.widget-hover-actions`，若插槽改用该 class 会被藏掉；iframe attach 与部件路径不一致 | 插槽条 `widget-hover-actions slot-toolbar` + `data-slot-hover-actions`；slot 模式只藏部件条；父页 `initSlotToolbarFloats` 挂载；iframe 仅 `slot-hover-sync` | 用户 + Composer | `editor-mode.js/css`、`theme-editor.js`、契约、`anchored-float.md`、`resource:compile welineUi` |
| 2026-08-25 | theme-editor syntax | Unexpected token ) → fixed switch close | 加 slot-hover-sync 时漏写 switch 闭合 `}`，整页 JS 语法失败 | 补回 `switch` 结束括号并重编译 | 用户 + Composer | `theme-editor.js`、`resource:compile welineUi` |
| 2026-08-25 | slot hover visible while over | leave-flash only → stable on hover | `:hover>.slot-toolbar{display:none}` + 每帧 clear hover-target 导致 float pending/隐藏；移开 sticky 才闪 | 删 :hover 隐藏；同目标跳过 clear；显示 flex+visibility !important | 用户 + Composer | `editor-mode.css/js`、契约、`resource:compile welineUi` |
| 2026-08-25 | slot float parent attach | iframe-only attach → parent initSlotToolbarFloats | 用户对照部件：部件正常贴边可点；插槽仍只见槽名/闪一下。iframe 自 attach 不可靠，且 ::before 槽名抢视觉 | 父页与部件同路径 attach+MutationObserver/sync；slot-hover-sync；hover 隐藏槽名标签 | 用户 + Composer | `theme-editor.js`、`editor-mode.js/css`、契约、`anchored-float.md`、`resource:compile welineUi` |
| 2026-08-25 | slot toolbar anchored-float | hand top/left → UI.floating.attach | hover 只见槽名标签，移开才闪「选择」条且难点到；手写 calculateButtonPosition 易被 overflow 裁切/错位 | `.slot-toolbar` 对齐部件：anchored-float + sync/place；CSS floating positioned；契约/文档 | 用户 + Composer | `editor-mode.js/css`、`anchored-float.md`、`ThemeEditorUiCapabilityContractTest`、`resource:compile welineUi` |
| 2026-08-25 | slot hover sticky clear | outer lock + timer restart → deepest + pendingKey | 插槽模式强制 index=0 被 header contains 锁死；离开后 mousemove 反复重置 180ms sticky，工具条残留且其它槽无法出条 | 恢复 deepest 默认；同 pendingKey 不重置计时；部件 nest sticky 同步；契约/文档 | 用户 + Composer | `editor-mode.js`、`theme-editor.js`、`anchored-float.md`、`ThemeEditorUiCapabilityContractTest`、`resource:compile welineUi` |
| 2026-08-25 | theme editor load order | late script waterfall → preload+critical-first | 主脚本在页尾才发现、widget-param 串行挡执行；init 同时打 versions/injections/layout-config/部件库与 start-preview，预览出得慢 | CSS 旁 modulepreload；主脚本优先、widget-param async；`kickoffLayoutPreview` 优先且 backend 复用服务端 src；次要 API 进 `scheduleSecondaryEditorBootstrap`；契约/矩阵同步 | 用户 + Composer | `ThemeEditor/index.phtml`、`weline-theme-editor.js`、`ThemeEditorUiCapabilityContractTest`、`weline-ui-2-capability-matrix.md` |
| 2026-08-25 | slot hover innermost | outer swallow → deepest+pin | 悬停 navigation 等子槽时薄适配器 CSS :hover 画绿框，但 JS 默认 index=0 且 contains 锁死外层 header，选择条不跟容器 | 默认 deepestSlotHoverIndex；穿透 pin；outline 仅跟 data-w-slot-hover-target；full 引擎禁用薄适配器 :hover；契约/文档同步 | 用户 + Composer | `editor-mode.js/css`、`theme-preview.css`、`anchored-float.md`、`ThemeEditorUiCapabilityContractTest`、`resource:compile welineUi` |
| 2026-08-25 | link-block separate toggle | nolink in selection group → independent | 禁链与默认/插槽/部件互斥，无法叠加 | 拆成独立 `preview-link-block` 开关，可与选中目标同时开启 | 用户 + Composer | `ThemeEditor/index.phtml`、`theme-editor.js`、`editor-mode.js`、契约、`resource:compile welineUi` |
| 2026-08-25 | selection nolink mode | default/slot/widget → +nolink | 链接包裹区域一点击就跳转/切预览页，难点选 | 选中目标增加「禁链」：捕获阶段彻底阻止 a 导航，父页链接桥接同步短路 | 用户 + Composer | `ThemeEditor/index.phtml`、`theme-editor.js`、`editor-mode.js`、契约、`resource:compile welineUi` |
| 2026-08-25 | selection target modes | mixed hover only → default/slot/widget | header 大槽与空塌缩容器难选中；内层部件盖住外层 | 预览工具栏增加「默认/插槽/部件」选中目标；插槽模式最外层优先+空槽占位；部件模式隐藏 slot chrome；默认同现行为 | 用户 + Composer | `ThemeEditor/index.phtml`、`theme-editor.js`、`editor-mode.js`、`editor-mode.css`、契约、`resource:compile welineUi` |
| 2026-08-25 | editor hover sticky | flash-hide on gap → tooltip-like stay | 插槽/部件外置提示条出现后，鼠标移向工具条会穿过空隙导致目标被清掉，无法点选 | slot 与部件 hover 增加 chrome 命中保持 + ≈180ms 延迟切换/关闭；契约与 anchored-float 文档同步 | 用户 + Composer | `editor-mode.js`、`theme-editor.js`、`anchored-float.md`、`ThemeEditorUiCapabilityContractTest`、`resource:compile welineUi` |
| 2026-08-25 | mega-menu base UI | header-local → Weline.UI component | nav 悬浮分栏仅 Header 私有样式/脚本，无法复用 | 新增 `mega-menu` 懒加载组件（CSS/JS/文档）；面板声明 `data-w-component=mega-menu`；Header 改 UI.mount | 用户 + Composer | `ui/js/components/mega-menu.js`、`ui/css/components/mega-menu.css`、`doc/widgets/mega-menu.md`、`resource:compile welineUi` |
| 2026-08-25 | mega-menu chrome bg | frosted white → nav secondary | 分类悬浮弹窗顶层白玻璃底 + 侧栏白字对比差；用户要求顶层沿用 nav 色 | `.is-megamenu` / overflow 顶层与侧栏改用 `--weline-chrome-bg-dark-secondary`；右侧 `.mega-menu-panels` 保持浅色 | 用户 + Composer | `header/default.phtml` |
| 2026-08-25 | header right More menu | broken li/a overlay → shared menu hover | 右侧快捷导航 More 被 `checkNavFillOverflow` 覆盖成 `li>a` 且不 mount，与分类/通用 nav 悬浮 menu 不一致 | 删除重复溢出脚本；仅 `adjustNavLinks` 写入 `w-menu__item` 并 `UI.mount(menu)`；文档与契约同步 | 用户 + Composer | `header/default.phtml`、`nav-links.md`、`ThemeHeaderMobileAmazonContractTest` |
| 2026-08-25 | header right More spacing | Gift cardMore glued → peer gap | `.header-nav-links` gap=0，More 在 ul 外吃不到列表间距 | 与 `.nav-links-list` 同用 `--weline-space-5`；More 按钮 padding 对齐链接项 | 用户 + Composer | `header/default.phtml` |
| 2026-08-25 | mobile All/More strip | hide All+More leave orphans → keep + overflow | ≤768 强制藏 All/More，却仍露分类与右侧残链；992 横向滚动绕过折叠 | 窄屏保留 All；槽位 overflow:hidden；分类/右侧折进 More；契约同步 | 用户 + Composer | `header/default.phtml`、`ThemeHeaderMobileAmazonContractTest` |
| 2026-08-25 | mobile hide-narrow orphans | categories vanish → keep + More | ≤768 `hide-narrow` 整块藏分类；More CSS 强刷 display:flex 干扰 JS | 窄屏禁用 hide-narrow；More 仅 flex-shrink，可见性交 adjust* | 用户 + Composer | `header/default.phtml` |
| 2026-08-25 | header lang/currency contrast | dark quiet text → surface/token light | 深色 `header-belt` 内语言/货币 `quiet` 触发器仍用 `--weline-theme-text`，English/CNY 近黑不可读 | quiet 按钮跟 `--w-surface-fg`；theme.css 为语言/货币触发器补齐 header/on-dark Token；文档与契约同步 | 用户 + Composer | `foundation.css`、`theme.css`、`theme-surface-text-roles.md`、`ThemeSurfaceTextRolesContractTest`、`resource:compile welineUi` |
| 2026-08-25 | widget save local preview | full iframe reload → local preview_html only | 部件「保存配置」后预览整页闪刷新；`updateWidgetPreviewInIframe`/`saveWidgetConfig` 在缺节点或缺 preview_html(locale) 时回退 `loadLayoutPreview()`，FormRenderer 默认 GET 亦可能整页导航 | 已有部件更新失败不再整页回退；locale 缺 preview_html 只告警；取消 `weline:form:prepare-submit`；同步源与 loaded bundle；契约断言 + 缓存版本 | 用户 + Composer | `theme-editor.js`、`weline-theme-editor.js`、`ThemeEditorUiCapabilityContractTest` |
| 2026-08-25 | config panel footer actions | clipped/delete-only → pinned save+delete row | 左侧部件配置底栏被裁切、仅删除、白字透明底、窄面板竖排叠放；头与 Search 易重叠 | 字段区滚动 + 底栏贴底悬浮保留主题 padding；`ParamTypeRenderer`/前端兜底「保存配置」；窄面板强制横排；`.w-param-actions` 主色/危险色对比；头搜索 flex 不压缩 | 用户 + Composer | `theme-editor.js`、`theme-editor.css`、`theme-editor-widget-config.css`、`ParamTypeRenderer.php`、Browser 验收 |
| 2026-08-25 | editor chrome contrast | dark storefront aliases → fixed light chrome | 深色页头上插槽「选择」条/部件操作条跟随店铺表面变成黑底，图标几乎不可见 | 编辑器悬浮 chrome 固定浅底深字与高对比渐变；`slot-toolbar` 加白底描边；overlay 操作条同步 | 用户 + Composer | `editor-mode.css`、`theme-editor-overlay.css`、`resource:compile welineUi` |
| 2026-08-25 | anchored-float base | theme-editor local hover bar → shared UI component | 贴左缘小部件操作条被裁切且盖住自身；定位若写在 Theme Editor 内无法复用 | 在 `Weline.UI` 新增 `anchored-float` + `floating.attach`，视口/iframe 感知 flip·shift；编辑器 `.widget-hover-actions` 仅声明复用；补文档与契约测试 | 用户 + Composer | `view/ui/js/weline-ui.js`、`foundation.css`、`theme-editor-overlay.css`、`theme-editor.js`、`doc/widgets/anchored-float.md`、`ThemeEditorUiCapabilityContractTest`、`resource:compile welineUi` |
| 2026-08-22 | responsive form control repair | implementing → implementing | foundation 的 `.w-input/.w-select/.w-textarea` 已有 `width: 100%`，但原生控件和公共文本控件缺少统一的 `min-width: 0` / `max-width: 100%`，在 flex/grid/modal 窄容器中可能按固有宽度撑开 | 用户确认补充 `REQ-THEME-0003`；在 canonical foundation 源收敛容器上限与可收缩规则，保留 checkbox/radio/button 等固有尺寸语义，不增加页面级补丁 | 用户 + Codex | `doc/需求.md`、`view/ui/css/foundation.css`、`doc/开发/Theme开发总指南.md` |
| 2026-08-22 | responsive form control validation / WebUI acceptance | implementation → accepted | canonical foundation 已经官方资源编译；聚焦单测 `8 tests / 158 assertions`、目标差异检查、变更路径 UI audit `0 violation` 通过；专用 9763 WLS 的 375px 登录表单与真实语言浮层中，301px 文本框和 325px 超长搜索输入均处于容器内，document `375/375` 无横向滚动，checkbox 保持 `17.59px` 固有宽度 | 本次响应式输入控件修复结案；Browser 控制台 `0 error`，仅记录一条既存 Visitor Pixel 未解析 warning；整体 `REQ-THEME-0003` 其余门禁状态不受本任务结论影响 | Codex | `resource:compile welineUi`、`ThemeColorModeContractTest`、`theme:ui:audit --format=json` 变更路径过滤结果、内置 Browser 截图/DOM/尺寸记录 |
| 2026-08-22 | MediaManager native Menu/theme repair | local menu skin/manual positioning → shared Weline UI contract | MediaManager 菜单 portal 位于 `.mmf-wrap` 外，旧 `--backend-color-*` 别名只在容器内定义，暗色下因 fallback 变成白底；业务还重复实现视口坐标、键盘与关闭监听 | 将目录菜单改为 `Weline.UI` Menu + `w-menu` 语义皮肤，用 1px 虚拟锚点保留右键位置；全局 Menu 补齐 Tab 关闭，primary/danger tone 下沉 foundation，不改提供者动作和业务功能 | 用户 + Codex | `view/ui/{css,js}`、MediaManager template/CSS/JS；MediaManager `8 tests / 96 assertions`、Theme `8 tests / 161 assertions`；375px Chromium E2E `1 passed`；真实 WLS 深浅色、右边界 8px、同点 3 次、Escape/外部点击/刷新验收通过 |
| 2026-08-21 06:02 | requirements | planned → requirements_confirmed | 后台存在重复 Bootstrap、旧插件和字体图标仓库；用户要求全部转自研且旧契约零兼容 | 目标定为 2.0.0，专用非 UI 引擎仅按批准白名单路由隔离 | 用户 | `REQ-THEME-0003`、任务记录 |
| 2026-08-21 | requirement correction / implementation | requirements_confirmed → implementing | 用户明确指出外部 UI 替换不得把 Theme 可视化编辑器变为薄壳，并发现通知浮层左右漂移、越界和无法关闭 | 迁移以完整功能清单为基线，只替换 UI 实现层；浮层增加确定性锚定、可视边界、真实隐藏和幂等挂载契约 | 用户 | `REQ-THEME-0003`、`view/ui/js/weline-ui.js`、Theme Editor 能力矩阵（待完成） |
| 2026-08-22 | requirement confirmation / implementation | implementing → implementing | 用户再次确认长期边界：精简的是重复 CSS/JS 与通用 UI 依赖，不是 Theme Editor 等产品功能；重写成本接近独立项目的拖拽、编辑器等功能引擎不机械删除 | 将该边界固化到组件开发技能；Theme Editor 按 29 项能力矩阵回归，保留引擎必须模块持有、适配器封装、路由级加载且不接管全局 UI | 用户 | `REQ-THEME-0003`、`doc/visual-editor/weline-ui-2-capability-matrix.md`、`归属模块文档（由 MCP 动态检索）` |
| 2026-08-22 | visual editor drag/drop repair | preview thin adapter → full engine restored | 新预览 bundle 只包含 slot 薄适配器，导致部件库拖入 iframe 后没有块内/块前/块后提示，也不会进入原保存链路；完整原生编辑引擎源码仍在 Theme 模块但未被 manifest 编译 | 将完整引擎置于 Weline 预览适配器之前并保持单 bundle/路由加载；恢复同源 drag-state bridge、真实 slot 接纳、嵌套 slot、sort order、exclusive/max/multiple 校验、跨 slot/取消清理及移动端可视边界提示，不改配置、版本、AI、锁、外观盘等产品能力 | 用户 + Codex | `etc/weline-ui-assets.json`、`view/statics/js/editor-mode.js`、`view/statics/js/theme-editor.js`、`view/statics/css/editor-mode.css`、`test/e2e/backend/theme-editor-preview.spec.js` |
| 2026-08-22 | visual editor runtime correction | full engine restored → cross-frame release hardening | 真实 WLS 复现出 iframe 内 dragover 已得到绿色 before 候选，但 Chromium 跨 iframe 释放不派发 drop；父页 dragend 随即清空状态，根 content slot 同时仅约 40px 高，表现为提示消失、不插入和画布不可视 | 增加唯一 drag session、实时 drop-candidate、父页统一校验/去重、96ms 释放兜底与 Escape/离界/切换/刷新清理；只在 editor-mode 扩大根内容画布，不改变正式前台或精简任何编辑器功能 | 用户 + Codex | `view/statics/js/theme-editor.js`、`view/statics/js/editor-mode.js`、`view/statics/css/editor-mode.css`、真实 WLS/Browser 验收 |
| 2026-08-22 | shared floating UI hardening | route-specific positioning → shared responsive contract | 真实 WLS 在 375px 复现前台移动导航 `left=333/right=676`，document `scrollWidth=676`；同时审查发现嵌套 portal 外部点击误判、抽屉 transform 改变 fixed 参考系、Escape 可级联关闭 | 移动导航改用共享 Popover；内核增加逻辑 portal 栈、顶层 dismiss、每次定位清旧坐标、body/top-layer 分流与旋转重排；CSS 统一可用尺寸、滚动、断词和 44px 触控目标 | 用户 + Codex | `view/ui/js/weline-ui.js`、`view/ui/css/foundation.css`、`view/ui/css/frontend.css`、`view/theme/frontend/partials/header/default.phtml`、聚焦契约/E2E |
| 2026-08-22 11:20 | Theme Editor widget config regression | implementing → implementing | 用户 9555 WLS 中 Banner 显示“该部件无可配置项”；真实 `widget-config` 已返回图片等 7 个 schema，但 QueryProvider 直派请求把名为 `params` 的 body 字段覆盖成整包参数，`paramrender/form` 因此收到空 schema | 按既有 `REQ-THEME-0003` 恢复完整配置能力；Codex 主代理直接修复 `ThemeQueryProvider::injectEditorRequestParams()` 的同名字段注入顺序并补聚焦回归测试，不改 Widget 注册、保存协议或产品行为 | 用户 + Codex | `迁移前历史任务记录（已清理）`、用户真实 URL、layout `687` |
| 2026-08-22 11:23 | Theme Editor widget config review | implementation → review_passed | 完整任务 diff 已从架构、缺陷、安全复审；请求桥仍由 Theme QueryProvider 持有，保留 body 覆盖 query 和现有同源/ACL/方法白名单，测试在 finally 恢复全局 Request | 无范围内发现，不需要返工；整体 `REQ-THEME-0003` 门禁仍保持 pending，仅本回归修复进入分层测试 | Codex | `ThemeQueryProvider::injectEditorRequestParams()`、`ThemeEditorRequestParamsTest`、任务记录 |
| 2026-08-22 11:40 | Theme Editor widget config focused validation | review_passed → focused_passed | Theme 请求注入用例 `1 test / 4 assertions`、相关 PHP 语法和目标差异检查通过；`query:help theme editorRequest --json` 仍可发现公开操作 | 静态资源未改，`resource:compile welineUi` 为 N/A；进入真实 WLS 验收 | Codex | `ThemeEditorRequestParamsTest`、CLI 输出、任务记录 |
| 2026-08-22 11:44 | Theme Editor widget config WebUI acceptance | focused_passed → accepted | `default` 9555 WLS 完成 4/4 Worker 滚动重载；Weline Chrome 中当前 4 个 Banner 均恢复完整字段和媒体入口，图片选择可打开同源 MediaManager；单标签跨完整心跳周期两次请求 200、增量错误/警告 0 | 本回归修复结案；当前数据无未知部件，不伪造 schema，既有真实空态分支保持不变；整体 `REQ-THEME-0003` 其余门禁状态不受本任务结论影响 | Codex | `迁移前历史任务记录（已清理）`、Browser 截图与日志 |
| 2026-08-22 12:25 | Theme Editor scope requirement | implementing → requirements_confirmed | 用户要求可视化主题编辑支持网站、店铺级别切换保存 | 固化为 canonical Global/Website/Store，不新增 Channel 或平行 scope 身份 | 用户 + Codex | `REQ-THEME-0004`、任务记录 |
| 2026-08-22 12:27 | Theme Editor scope implementation | requirements_confirmed → implementing | 后端布局、配置、预览、版本和保存已携带 scope，缺口是编辑器没有可见的作用域目录与切换入口 | 复用 SystemConfig TargetScope 目录，切换经现有锁释放和编辑器壳整页导航，避免跨 scope DOM 状态复用 | Codex | `ThemeEditorScopeCatalogService`、`scopeSelect` |
| 2026-08-22 14:58 | Theme Editor scope hierarchy reconfirmed | implementing → requirements_reconfirmed | 用户明确 scope 是最高级上下文，要求顶部先作用范围、后主题，并按 Website → Store 树形选择 | 采用官方 TreeSelect；保留 canonical scope、锁释放、整页导航与保存 identity，不引入平行作用域协议 | 用户 + Codex | `REQ-THEME-0004`、`scope-switching.md` |
| 2026-08-22 15:43 | Theme Editor Scope tree runtime acceptance | requirements_reconfirmed → accepted | 真实 9555 `default` WLS + Weline Chrome 确认首屏 `name=scope`、Global 当前值、Scope → Theme → 其他顺序、3 个 Website 各含 1 个 Store；Enter/方向键可从 Global 进入 Website 与二级 Store；Banner 的背景图片、Select、upload、URL 与文本配置均可用 | `w:theme:tree-select` 首屏主动加载当前值，Scope 切换继续使用整页导航；`resource:compile welineUi`、DI 编译与 4 Worker 滚动重载通过；聚焦 UI/请求/Normalizer 共 11 tests / 165 assertions 通过 | Codex | `TreeSelect`、`ThemeEditor/index.phtml`、`ThemeEditorUiCapabilityContractTest` |
| 2026-08-22 15:43 | Store Scope save isolation and cleanup | runtime_blocked → accepted | 首次真实保存因 `w_theme_layout.node_uid` 缺列失败；定向升级又被既有 Storage 版本约束校验中断并提前推进 Theme checkpoint。按 `ThemeLayout` 声明精确补齐 nullable `varchar(32)` 列与 `uk_theme_layout_identity_node` 后，店铺临时 Banner 新增、保存、刷新持久化成功，Global 原配置不变；临时 layout `695` 随后删除，店铺结构与最终 Global 页面恢复原状，最近控制台无新错误 | 数据库只做模型声明对应的幂等列/索引修复，不改 Storage/SystemConfig；最终以真实浏览器的保存→刷新→跨 Scope 隔离→删除恢复为验收依据 | Codex | `ThemeLayout::schema_fields_NODE_UID`、`default.__store__.default` |

> 验证备注：现有未跟踪的 `ThemeEditorScopeCatalogServiceTest` 仍按旧构造夹具向工作区已修改的 `SystemConfigTargetScopeService` 传入 Closure，因此其 2 个用例未计入本次最终通过数；本次未修改测试或 SystemConfig 源码。

### 2026-08-22 可视化编辑器拖拽回归证据

- 聚焦契约：`EditorModeAssetInjectorTest` 与 `ThemeEditorUiCapabilityContractTest` 共 `8 tests / 126 assertions` 通过；源文件和生成 JS 语法检查通过。
- 编译：连续两次 `resource:compile welineUi` 后 preview CSS、preview JS、editor JS 的 SHA-256 分别稳定为 `f54d14b4...29c9`、`30c5e21a...a338`、`82e523db...4c9`。
- 浏览器引擎验证：Chromium 在 `375/768/1024/1440` 下均得到 inside/before/after 三种状态；1200px 宽预览内容中的提示仍保持在 viewport 内；跨 slot 旧状态、取消状态可清理；父子窗口实测消息给出 before=`sort_order 0`、after=`sort_order 2`。
- 审计：全仓 `theme:ui:audit` 仍因迁移中的历史文件报 `5852` 项；本次 Theme Editor 拖拽修复路径没有匹配到新增违规，不能据此把全局门禁标为通过。
- 真实 WLS：`codex-ui2-20260822` 已运行并收到编译缓存清理；Playwright 三条 Theme preview 用例均在管理员会话引导阶段因默认管理员密码无效而停止，内置 Browser 同样停在登录页，尚未形成登录后的真实拖拽验收结论。

### 实现范围

- 变更路径/符号：待实现后按审计生成的精确清单登记。
- 数据/配置/迁移：图标值一次性数据迁移；无 Model schema 变更。
- 兼容与回退：无运行时兼容；删除前保留入口清单、资源哈希和数据快照。

## 1.0.8 — Head 币种上下文单一来源

- 当前状态：`implemented_validated`
- 需求 ID：`REQ-THEME-0002`
- 实现范围：默认 Theme Head 从 `State::getCurrency()` 取得当前币种并移除 Head 内的二次校验/回退；Motor 覆盖采用同一契约。
- 数据/配置/迁移：无 schema、无配置写入。
- 验收：默认 Head 与 Motor Head 契约测试先红后绿；9514 商品、搜索、详情、购物车和结账预检均显示 USD，控制台 0 error / 0 warning。

## 1.0.8 — 全局亮暗色与 Bootstrap 组件适配

- 基线版本：`1.0.7`
- 目标版本：`1.0.8`
- 当前状态：`implementation_reopened`
- 需求 ID：`REQ-THEME-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：Theme CSS/JS/head、默认设计主题 token overlay、Theme 编辑器；不升级 Bootstrap、不改 schema、不作站点同步。
- 负责人/确认人：实施子代理 `gpt-5.6-terra` / 用户。

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | recorded | `doc/需求.md#req-theme-0001-全局亮暗色与-bootstrap-组件适配` | 2026-08-10 |
| 实现执行者/模型/范围已记录 | recorded | 用户确认的 Terra 委派规则；本版本范围 | 2026-08-10 |
| 架构/缺陷/安全复审通过 | pass | 三轮整改后的任务范围 diff 复审；未发现剩余阻断级问题 | 2026-08-11 |
| 分层测试通过 | pass (focused) | Theme 颜色契约 `4/692`、Partials 缓存回归 `2/3`，Backend `20/46`，Admin `2/42` | 2026-08-11 |
| 真实路径 WebUI/E2E 通过 | partial | 前台完整通过；后台实时切换、Card、键盘与控制台通过，刷新后 WLS 登录态丢失阻断持久化闭环 | 2026-08-11 |
| 可重复 E2E 固化并通过 | partial | 前台 `THEME-COLOR-FE-001` 通过；后台在首次 `page.reload()` 后回到登录页 | 2026-08-11 |
| 功能/运营文档完成 | pass | README、Theme.js、继承约定、固定需求与开发日志 | 2026-08-11 |
| 用户/产品验收 | pending | — | — |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-16 | dashboard performance | shared IPC hot path → process-local chrome LRU | 后台请求 trace 中 9 次 theme_runtime get/set/cache_get 各约 200ms，chrome partial 热路径累计约 1.86s；与 Worker credential 风暴叠加后页面 TTFB 达 4.44s | 以模板 @meta.cache 为权威，改用 256 项进程内 LRU；key 绑定模板状态、主题、页面和认证上下文，身份不确定时绕过；删除请求热路径 SharedState IPC | 用户 + Codex | RED→GREEN Theme partial 14 tests / 67 assertions；默认 WLS 重启后公开请求 20 次平均 TTFB 63ms、P95 148ms；登录态 Dashboard 数字待用户会话手动刷新确认 |
| 2026-08-10 10:31 | requirements | planned → requirements_confirmed | 原固定需求/日志文档缺失；用户确认 REQ-THEME-0001 | 先建立诚实基线，再实施 | 用户 | `doc/需求.md` |
| 2026-08-10 | implementation | requirements_confirmed → implementing_pending_validation | 三态 runtime、前后台 head prepaint、Bootstrap adapter、Default token overlay 与测试资产正在完成静态复审 | 核心资源改由固定模块 URL 加载，活动主题仅保留色盘 Token | 用户已确认需求 / `gpt-5.6-terra` 实施 | `view/theme/**`、`app/design/Weline/default/**`、`test/**/theme-color-mode*` |
| 2026-08-10 | implementation | risk recorded | 历史 `view/statics/js/visual-editor.js` 含非 UTF-8 字节，`apply_patch` 无法安全读取修改 | 新增 UTF-8 `visual-editor-theme-mode.js` 作为三态桥，旧按钮隐藏仅供旧脚本依赖 | `gpt-5.6-terra` | `view/statics/js/visual-editor-theme-mode.js` |
| 2026-08-10 | review repair | parent first review blocked → second re-review pending | 首轮父级复审发现 Token、三态优先级、同源消息、adapter、无障碍和验收资产缺口，已返回定点修复 | 未运行 lint、单元、E2E 或 Browser；第二轮父级复审待开始 | `gpt-5.6-terra` | 本任务工作区与相关源码 |
| 2026-08-10 | review repair | second re-review blocked → third repair in progress | 独立复审继续指出 legacy false 语义、Login 首屏、核心资产覆盖、fallback 等价、组件 raw Token 与预览持久化问题 | 修复回到 Terra；测试和 WebUI 均未执行 | `gpt-5.6-terra` | `REQ-THEME-0001` 第三轮复审清单 |
| 2026-08-10 | review repair | third repair → static closure pending parent re-review | Bootstrap body/emphasis RGB 契约、5.1 校验态/tooltip 对比、Theme Editor 语义化、独立预览壳加载顺序、MediaManager runtime 优先与 Widget 关联版本已收口 | 仅精确静态扫描与 `git diff --check`；未执行 PHP/JS/E2E/WLS/Browser | `gpt-5.6-terra` | `view/theme/**`、`MediaManager/view/statics/js/manager.js`、`Widget/etc/module.php`、`test/**` |
| 2026-08-11 | review | static closure → review passed | 完成架构、缺陷、安全复审；组件颜色统一由语义 Token/Bootstrap adapter 驱动，未新增业务页暗色选择器 | 进入分层验证 | 主代理 | 任务范围 diff 与静态扫描 |
| 2026-08-11 | runtime repair | focused validation → runtime repaired | WLS 预览暴露 `Partials::clearAllCaches()` 缺失导致 500；补回公开兼容入口及回归测试。真实 Theme 管理卡片补标准 `.card` 契约 | 修复实际运行阻断，不改变主题接口语义 | `gpt-5.6-terra` | `Block/Partials.php`、`test/Unit/PartialsStaticOutputEmptyCacheTest.php`、`view/templates/backend/index.phtml` |
| 2026-08-11 | validation | review passed → focused pass | Theme/Backend/Admin 契约测试和前台颜色 E2E 全部通过；PHP/JS 语法、diff check、重复资源及硬编码扫描通过 | 保留全模块历史夹具失败的诚实记录 | 主代理 | `test/Unit/**ThemeColorMode*`、`test/e2e/frontend/theme-color-mode.spec.js` |
| 2026-08-11 | WebUI | focused pass → browser pass / refresh blocked | 内置 Browser 验证首页 system 随系统暗/亮变化；后台真实 Theme 页有 4 个 `.theme-card.card`，暗/亮计算样式自动切换，三态菜单首个 ArrowDown 聚焦 system，后续 light/dark 顺序正确；9517 前后台无 console error | 后台单文件 E2E 在首次刷新后回登录页，归因于独立 WLS backend session 保持问题 | 主代理 | 唯一 `ai-test-theme-color-mode-final2-20260811`，端口 `9517` |
| 2026-08-11 | full module E2E | requested command executed | `php bin/w e2e:run --module=Weline_Theme --project=chromium` 为 `7 passed / 10 failed`；颜色前台通过，后台经定位器修正后在 reload 登录态处超时；其余失败为当前主题不含旧 `Weline_Theme` widget/`motor-welcome-modal`/Search 延迟脚本及夹具清理保护 | 不伪造旧主题业务资产；整体发布验收保持 pending | 主代理 | Playwright 汇总与错误上下文 |
| 2026-08-11 | acceptance scope | partial → full-suite repair | 用户要求“线全部跑绿”，确认目标为 `Weline_Theme` 整模块 Playwright 全部通过；不以聚焦用例替代整模块验收 | 先修 session scope、夹具生命周期及失效预览契约，随后复跑整套与真实 WebUI | 用户 | `REQ-THEME-0001` |
| 2026-08-11 | repair review | session/fixture review passed | 复审真实 WLS cookie scope 与 Theme E2E 的测试网站清理：cookie 名从服务响应获取；清理以 `e2e-theme-default-` 前缀、精确 ID 和删除顺序保护 | 进入针对性 E2E，任何失败修复均重新复审 | 主代理 | `tests/e2e/framework/runtime.js`、`theme-editor-fixture.php` |

### 实现范围

- 变更路径/符号：Theme 资源、`Weline.Theme`、设计主题 token overlay、前后台主题入口与测试资产。
- 数据/配置/迁移：无 schema；后台用户设置兼容旧值。
- 兼容与回退：light/dark 记录不变；system 回退亮色；不改站点仓库。

### 审查与整改

- 架构：passed；Theme 是三态解析与组件 adapter 的唯一 owner，活动设计主题只提供品牌 Token。
- 缺陷：passed for task scope；运行期发现的 Partials 缓存兼容入口、真实 Theme Card 契约及测试 strict locator 已修复并复验。
- 安全：passed；后台枚举严格校验、消息同源校验、无新增 schema/外部请求。
- 用户批准延期：无；WLS backend session 刷新问题和旧主题预览夹具不在本需求中伪修复。

### 测试与验收

- 聚焦/单元/集成：Theme 颜色契约 `4 tests / 692 assertions`；Partials 缓存回归 `2/3`；Backend `20/46`；Admin `2/42`，均通过（仅 PHPUnit deprecation）。
- 真实 WLS + WebUI：前台首页 system 暗/亮、后台 Theme 管理 Card 暗/亮、键盘顺序和控制台通过；临时账号/配置/角色已清零，9517 已完整停止。
- E2E 固化：前台颜色模式 `1 passed`，覆盖 system/light/dark、刷新、系统变化、完整 Bootstrap 组件矩阵、焦点/对比度及真实首页。
- 后台剩余门禁：单文件用例越过组件矩阵后，在首次 `page.reload()` 返回登录页，最终等待主题触发器超时；需 WLS session 修复后复跑。
- 全模块非本需求失败：旧 Theme Editor/default injection/preview fixtures 依赖当前活动主题不存在的业务 widget、`motor-welcome-modal`、Search 延迟脚本，另有网站引用保护阻止物理删除。

### 文档与运营交付

- 需求文档：`REQ-THEME-0001` / `1.0.8`。
- 功能/API/架构文档：Theme.js、继承机制、Default README 已更新；需在复审后确认无遗漏。
- 运营文档：N/A；现有 Theme.js 与主题继承文档承担使用说明，实施后复核。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：实现完成，聚焦验证通过；完整发布验收因独立 WLS backend session 刷新问题与历史主题预览夹具保持 pending。
- 完成证据：契约/单元、前台颜色 E2E、唯一 WLS + 内置 Browser 前后台实时视觉与键盘验收。
- 剩余事项与恢复条件：修复 WLS backend session 刷新保持后复跑后台颜色 E2E；按当前活动主题重建或参数化旧预览夹具。未经用户确认不作 core→site 同步。
