<?php
/** @var \Weline\Framework\View\Template $this */
/** @var \Weline\Backend\Block\ThemeConfig $themeConfig */
$themeConfig = \Weline\Framework\Manager\ObjectManager::getInstance(\Weline\Backend\Block\ThemeConfig::class);

use Weline\Order\Model\Order;
use Weline\Order\Model\OrderItem;

$order = $this->getData('order');
$items = $this->getData('items') ?? [];

$isEdit = !empty($order);
$orderData = $isEdit ? (is_object($order) ? $order->getData() : $order) : [];
$orderId = $orderData[Order::fields_ID] ?? 0;
?>
<!DOCTYPE html>
<html lang="zh-CN" <?= ('rtl' === $themeConfig->getThemeModel()) ? ' dir="rtl"' : '' ?>>
<head>
    @template(Weline_Admin::common/head.phtml)
    <title><?= $isEdit ? __('编辑订单') : __('新建订单') ?></title>
</head>
<body <?= $themeConfig->getLayouts() ?? ''; ?>>
@template(Weline_Admin::common/page/loading.phtml)
<div id="layout-wrapper">
    <w:block class="Weline\Admin\Block\Backend\Page\Topbar"/>
    <w:block class="Weline\Admin\Block\Backend\Page\Topnav"/>
    @template(Weline_Admin::common/left-sidebar.phtml)
    <div class="main-content">
        <div class="page-content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                            <h4 class="mb-sm-0"><?= $isEdit ? __('编辑订单') : __('新建订单') ?></h4>
                            <div class="page-title-right">
                                <ol class="breadcrumb m-0">
                                    <li class="breadcrumb-item"><a href="@backend-url('admin/index/index')"><?= __('系统') ?></a></li>
                                    <li class="breadcrumb-item"><a href="<?= $this->getBackendUrl('order/backend/order/index') ?>"><?= __('订单管理') ?></a></li>
                                    <li class="breadcrumb-item active"><?= $isEdit ? __('编辑订单') : __('新建订单') ?></li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title mb-0"><?= $isEdit ? __('编辑订单') : __('新建订单') ?></h5>
                            </div>
                            <div class="card-body">
                                <form method="post" action="<?= $this->getBackendUrl('order/backend/order/save') ?>">
                                    <?php if ($isEdit): ?>
                                        <input type="hidden" name="order_id" value="<?= $orderId ?>">
                                    <?php endif; ?>
                                    
                                    <div class="row">
                                        <div class="col-md-6">
                                            <div class="mb-3">
                                                <label class="form-label"><?= __('客户ID') ?></label>
                                                <input type="number" name="customer_id" class="form-control" 
                                                       value="<?= htmlspecialchars($orderData[Order::fields_CUSTOMER_ID] ?? '') ?>">
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="mb-3">
                                                <label class="form-label"><?= __('客户姓名') ?></label>
                                                <input type="text" name="customer_name" class="form-control" 
                                                       value="<?= htmlspecialchars($orderData[Order::fields_CUSTOMER_NAME] ?? '') ?>" required>
                                            </div>
                                        </div>
                                    </div>
                                    
                                    <div class="row">
                                        <div class="col-md-6">
                                            <div class="mb-3">
                                                <label class="form-label"><?= __('客户邮箱') ?></label>
                                                <input type="email" name="customer_email" class="form-control" 
                                                       value="<?= htmlspecialchars($orderData[Order::fields_CUSTOMER_EMAIL] ?? '') ?>">
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="mb-3">
                                                <label class="form-label"><?= __('客户电话') ?></label>
                                                <input type="text" name="customer_phone" class="form-control" 
                                                       value="<?= htmlspecialchars($orderData[Order::fields_CUSTOMER_PHONE] ?? '') ?>">
                                            </div>
                                        </div>
                                    </div>
                                    
                                    <div class="row">
                                        <div class="col-md-6">
                                            <div class="mb-3">
                                                <label class="form-label"><?= __('支付方式') ?></label>
                                                <input type="text" name="payment_method" class="form-control" 
                                                       value="<?= htmlspecialchars($orderData[Order::fields_PAYMENT_METHOD] ?? '') ?>">
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="mb-3">
                                                <label class="form-label"><?= __('配送方式') ?></label>
                                                <input type="text" name="shipping_method" class="form-control" 
                                                       value="<?= htmlspecialchars($orderData[Order::fields_SHIPPING_METHOD] ?? '') ?>">
                                            </div>
                                        </div>
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label class="form-label"><?= __('订单备注') ?></label>
                                        <textarea name="notes" class="form-control" rows="3"><?= htmlspecialchars($orderData[Order::fields_NOTES] ?? '') ?></textarea>
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label class="form-label"><?= __('订单项') ?></label>
                                        <div id="order-items">
                                            <?php if (!empty($items)): ?>
                                                <?php foreach ($items as $index => $item): ?>
                                                    <?php
                                                    $itemData = is_object($item) ? $item->getData() : $item;
                                                    ?>
                                                    <div class="order-item mb-2 p-2 border">
                                                        <div class="row">
                                                            <div class="col-md-4">
                                                                <input type="text" name="items[<?= $index ?>][product_name]" class="form-control" 
                                                                       placeholder="<?= __('商品名称') ?>" 
                                                                       value="<?= htmlspecialchars($itemData[OrderItem::fields_PRODUCT_NAME] ?? '') ?>" required>
                                                            </div>
                                                            <div class="col-md-2">
                                                                <input type="text" name="items[<?= $index ?>][product_sku]" class="form-control" 
                                                                       placeholder="<?= __('SKU') ?>" 
                                                                       value="<?= htmlspecialchars($itemData[OrderItem::fields_PRODUCT_SKU] ?? '') ?>">
                                                            </div>
                                                            <div class="col-md-2">
                                                                <input type="number" name="items[<?= $index ?>][qty_ordered]" class="form-control" 
                                                                       placeholder="<?= __('数量') ?>" 
                                                                       value="<?= htmlspecialchars($itemData[OrderItem::fields_QTY_ORDERED] ?? '1') ?>" required step="0.01">
                                                            </div>
                                                            <div class="col-md-2">
                                                                <input type="number" name="items[<?= $index ?>][price]" class="form-control" 
                                                                       placeholder="<?= __('单价') ?>" 
                                                                       value="<?= htmlspecialchars($itemData[OrderItem::fields_PRICE] ?? '0') ?>" required step="0.01">
                                                            </div>
                                                            <div class="col-md-2">
                                                                <button type="button" class="btn btn-danger btn-sm remove-item"><?= __('删除') ?></button>
                                                            </div>
                                                        </div>
                                                    </div>
                                                <?php endforeach; ?>
                                            <?php endif; ?>
                                        </div>
                                        <button type="button" class="btn btn-primary btn-sm mt-2" id="add-item"><?= __('添加订单项') ?></button>
                                    </div>
                                    
                                    <div class="text-end">
                                        <a href="<?= $this->getBackendUrl('order/backend/order/index') ?>" class="btn btn-secondary"><?= __('取消') ?></a>
                                        <button type="submit" class="btn btn-primary"><?= __('保存') ?></button>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script>
document.addEventListener('DOMContentLoaded', function() {
    let itemIndex = <?= count($items) ?>;
    
    document.getElementById('add-item').addEventListener('click', function() {
        const itemsContainer = document.getElementById('order-items');
        const itemHtml = `
            <div class="order-item mb-2 p-2 border">
                <div class="row">
                    <div class="col-md-4">
                        <input type="text" name="items[${itemIndex}][product_name]" class="form-control" 
                               placeholder="<?= __('商品名称') ?>" required>
                    </div>
                    <div class="col-md-2">
                        <input type="text" name="items[${itemIndex}][product_sku]" class="form-control" 
                               placeholder="<?= __('SKU') ?>">
                    </div>
                    <div class="col-md-2">
                        <input type="number" name="items[${itemIndex}][qty_ordered]" class="form-control" 
                               placeholder="<?= __('数量') ?>" required step="0.01" value="1">
                    </div>
                    <div class="col-md-2">
                        <input type="number" name="items[${itemIndex}][price]" class="form-control" 
                               placeholder="<?= __('单价') ?>" required step="0.01" value="0">
                    </div>
                    <div class="col-md-2">
                        <button type="button" class="btn btn-danger btn-sm remove-item"><?= __('删除') ?></button>
                    </div>
                </div>
            </div>
        `;
        itemsContainer.insertAdjacentHTML('beforeend', itemHtml);
        itemIndex++;
    });
    
    document.addEventListener('click', function(e) {
        if (e.target.classList.contains('remove-item')) {
            e.target.closest('.order-item').remove();
        }
    });
});
</script>
@template(Weline_Admin::common/footer.phtml)
</body>
</html>

