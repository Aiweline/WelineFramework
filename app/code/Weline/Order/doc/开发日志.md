## 2026-09-22 — create 兑现 discount_amount_minor（2.13.44）

- `OrderFacade::plan/create` 写入 `options.discount_amount_minor` 到 MoneySnapshot（支付方式激励等订单级折扣）。
- 协同 Payment `1.9.106`、Checkout `1.5.43`。版本：`2.13.43` → `2.13.44`。

## 2026-09-22 — 账户订单配送方式名漏译（2.13.43）

- 账户订单详情 `orders.phtml` 对 `shipping.method` 展示名套 `__()`；后台 totals/chrome presenter 同步 `__()`。
- 版本：`2.13.42` → `2.13.43`。

## 2026-09-21 — 账户未付「继续支付」（2.13.42）

- `ContinuePayUrlBuilder` 优先拼 `#payment-recovery`；账户 Loader/Presenter/订单卡 CTA。
- 协同 Checkout 支付取消续付规格。
- 验：UT `ContinuePayUrlBuilderTest` / `AccountOrdersTemplateTest`；e2e `account-continue-pay`。
- 版本：`2.13.41` → `2.13.42`。

## 2026-09-20 — 后补运单号派发 order_shipped

- **问题**：`updateShipmentTracking` 仅在「首次填单号且通知客户」时派发；`FulfillmentService::updateTracking` 从不派发 → 支付网关物流回传漏同步。
- **修**：有运单号写入后一律派发 `Weline_Order::order_shipped`；`notify_customer` 仍只控制邮件（后补默认 false / 按原邮件门禁）。
- **协同**：Payment 壳 `ProviderShipmentTrackingInterface` 听该事件按支付方式回传。
- **PHP 8.4**：`EventsManager::dispatch` 第二参须可引用；四处 `order_shipped` 改为先赋变量再 dispatch。

## 2026-09-17 — 我的订单分区加载失败修复

- 现象：账户「我的订单」白屏报「分区加载失败，请重试」。
- 根因：`getSidebarSection` 偶发 success+空 Hook HTML（主题页缓存嵌套目录 `pages/account/` 未 mkdir 等）；前端空 html 直接 `markSectionLoadFailed`。
- 修：Theme `ThemeCacheGenerator` 写 page 前递归创建 dirname；Customer `account-index` 空 html 一次重试 + reveal 清 loading。
- 验收：本机 Browser `#orders` →「暂无订单」；profile↔orders 切换稳定。

### 汇审

| 项 | 结果 |
|---|---|
| work_kind | bugfix（simple） |
| UT | PASS（Customer AccountSidebarHookHostTest） |
| WB-OP | PASS（空态；无分区失败） |

## 2026-09-17 — 账户订单记录整卡手风琴（原型 A）

- 选型：草图 A（点摘要行开/关；挂单+沟通在记录正下方全宽，非右侧）。
- 变更：`orders.phtml` 有 hang/明细时包 `<details class="account-orders__accordion">`；面板内 hang `data-chat-embedded` + `$chatOpen=true` 隐藏嵌套「订单沟通」钮；`account-orders.css` chevron/panel；未读角标；契约测试。
- 验收：UT PASS；Browser 夹具点 summary 展开，沟通在下方。

### 汇审

| 项 | 结果 |
|---|---|
| work_kind | ui_polish（simple） |
| 原型 | A |
| UT | PASS |
| WB-OP | PASS（收起隐藏 hang；展开 hangBelow + toolbar 隐藏） |

## 2026-09-17 — 账户订单沟通改到记录下方全宽展开

- 审图 `ui_shot`：沟通与挂单进度挤在订单 summary 行右侧（flex 同行），非「整条订单记录下」展开。
- 原型：`view/statics/prototype-account-orders-chat-below.html`（A 现状 / B 下方全宽推荐 / C 下方+行内开关）；选型 **B**。
- 变更：`orders.phtml` 将 hang/聊天移出 `.account-orders__summary`，包在 `.account-orders__hang`；`account-orders.css` 全宽网格 + 顶部分隔；契约测试断言结构。
- 验收：UT AccountOrdersTemplateTest / AccountSidebarHookTemplateTest PASS；Browser 原型 B + 账户 `#orders` 夹具几何 `hangBelow`/`hangFullWidth` PASS。

### 汇审

| 项 | 结果 |
|---|---|
| work_kind | ui_polish（simple） |
| 原型选型 | B（下方全宽） |
| UT | PASS（2 tests / 75 assertions） |
| WB-OP | PASS（沟通在 summary 下全宽，非右侧） |

## 2026-09-17 — 账户下拉补「我的订单」入口

- 审图：顶栏「我的账户」下拉缺「我的订单」（账户/资料/安全/地址/询价/收藏/批发身份齐全）。
- 根因：`header-account-links` 有模块贡献时覆盖 Theme else 回退；Order 仅有侧栏/header-orders，未贡献账户下拉项。
- 变更：新增 `view/hooks/header-account-links.phtml` → `#orders`；契约测试覆盖。
- 验收：UT PASS；首页 HTML `data-hook-source="Weline_Order"`；DOM 登录态菜单第 4 项为「我的订单」。

### 汇审

| 项 | 结果 |
|---|---|
| work_kind | ui_polish（simple） |
| UT | PASS（AccountSidebarHookTemplateTest） |
| WB-OP | PASS（菜单含「我的订单」→ `#orders`） |

## 2026-09-16 — 结账成功页可读结账地址（Facade 落库 billing）

- create/createDb 从 options.billing_address 写入订单行；缺省回落配送地址。
- Orm 落库 `billing_address` 列；hydrate + OrderReadResult 读出。
- 版本：`2.13.40` → `2.13.41`。

## 2026-09-15 — 客户调整并入订单办理 Tab（2.13.40）

- 编辑页「客户调整」不再下方折叠 +「前往」跳转；表单直嵌「订单办理 → 客户调整」页签，去掉重复入口。
- 新建订单仍用折叠 writable；表单抽 `partial/customer-adjust-form.phtml`。
- 版本：`2.13.39` → `2.13.40`。

## 2026-09-14 — order_signals 下单时间回退 create_time（2.13.34）

- 结账落单常留空 `created_at`，框架 `create_time` 有值；`UnpaidOrderSignalService::resolveCreatedAtDisplay` 优先 create_time 并去微秒，供催付邮件「下单时间」。
- 版本：`2.13.33` → `2.13.34`。

## 2026-09-15 — 订单邮件金额货币与摘要人性化（2.13.38）

- 审图：金额露裸数 `337.00`；摘要含技术向「订单 UUID」；商品行用 `CNY 285.00` 码制。
- `OrderMailNotifier` 经 Currency 格式化金额（失败回退 `¥337.00`/`CNY 337.00`）；`OrderMailItemsHtmlBuilder` 同行价/小计改货币符号；客户向摘要去掉 UUID；空 message/comment 不占行。
- 版本：`2.13.37` → `2.13.38`。

## 2026-09-15 — 订单邮件状态译写（2.13.37）

- 审图：创建邮件摘要「状态」露 `fulfilled / fulfilled`。
- `OrderMailNotifier` 在邮件 locale 下用 `Order::getStatusLabel`（及支付/履约标签）写入 vars。
- 版本：`2.13.36` → `2.13.37`。

## 2026-09-15 — 订单创建邮件商品明细（2.13.36）

- `OrderMailItemsHtmlBuilder` + `UnpaidOrderMailLineProjector` → `items_html`；`order_created` 十语种模板 `{{#if var.items_html}}`。
- `OrderMailNotifier` 注入 `currency`/`items_html`（图 Host 拒 e2e/`*.weline.test`）。
- 版本：`2.13.35` → `2.13.36`。

## 2026-09-14 — 全站邮件摘要表紧排（2.13.35）

- 审查：订单类邮件（创建/已付/发货/退款/状态变更）仍用 `width:38%` 标签列，中间空洞同催付邮件。
- 改：10 语种 × 5 渠道种子 HTML 标签列改为 `width:1%;white-space:nowrap`；DB 已 sync。
- 版本：`2.13.34` → `2.13.35`。

## 2026-09-14 — unpaid 信号 created_at 回退 create_time（2.13.34）

- 店面下单行常空 `created_at`，框架 `create_time` 有值；催付邮件「下单时间」空白。
- `UnpaidOrderSignalService::resolveCreatedAtDisplay` 优先 `create_time` 并去微秒。
- 版本：`2.13.33` → `2.13.34`。

## 2026-09-14 — unpaid mail line_items 投影（2.13.33）

- `UnpaidOrderMailLineProjector`：经 `BackendOrderLinePresenter` 输出 name/sku/qty/价格/options_text/image_url。
- `UnpaidOrderSignalService` DTO 增 `line_items`，供 Marketing 挽回邮件商品块。
- 版本：`2.13.32` → `2.13.33`。

## 2026-09-14 — 发货对齐 Shopify（登记/打单/锁定/税号嵌入）

- Mark as fulfilled：结账配送仅参考；承运商下拉+可选运单+后补；无运单不发信。
- Shipping：`OrderShippingFulfillmentGateway` + 持久打单幂等/孤儿补偿；label 缺重拒绝静默 100g。
- 最小通道锁定：`shipping_snapshot_json.fulfillment_channel`；后台可切换。
- Tax：`BuyerTaxIdentityService` + `checkout-tax-identity` 槽嵌入（欧盟 VAT 示例）；禁 EAV。
- 验：命令/面板/网关/税号 UT；Browser 发货面板。

## 2026-09-14 — 发货面板：物流号 + 已发货邮件（审图 ui_shot）

- 分流：`web_ui` / `ui_shot`（订单编辑「发货」Tab 过于简陋）。
- 线稿：Tab → 说明 → 裸 UUID 行 + 仓库 `0` + 仅数量按钮（缺物流号/通知）。
- 原型：候选改为「待发货包裹」卡；承运商/物流号必填/数量/默认勾选发邮件/确认发货。
- 修：`OrderTradeAdminCommandService::ship` 强制 `tracking_number`，落 `OrderShipment`，派发 `order_shipped`（尊重 `notify_customer`）；未绑仓 `legacy_default`/`warehouse_id=0` 可发；面板与发货管理页同步。
- 验：UT（命令/契约）；Browser 禁缓存：订单 `#93` 提交 `SFTEST83916486` → 单元已发完、订单 `fulfilled`、发货记录入库。

## 2026-09-14 — order_signals 投影 scope locale（2.13.32）

- DTO 增 `locale`（`scope_snapshot_json.locale|language`）；供 Marketing 催付邮件选模板语言。
- 验：`UnpaidOrderSignalLocaleTest` + 合同测含 `localeFromScopeSnapshotJson`。
- 版本：`2.13.31` → `2.13.32`。

## 2026-09-14 — order_signals 注册 backend_acl（2.13.31）

- 修：`OrderSignalsQueryProvider` 补 `backend_acl`（list→`order_list`，get→`order_view`）；`extends:rebuild` + `framework:compile` 可入库。
- 修：`UnpaidOrderSignalService` 条件符 `neq`/`nin` → `!=` / `not in`（对齐 SqlTrait）。
- 版本：`2.13.30` → `2.13.31`。

## 2026-09-14 — Shipping→Order 发货部件默认注入

- Order：详情/列表留空槽 `backend-order-shipments` / `backend-order-list-shipping` + Hook；详情不再硬编码发货表。
- Shipping：部件 + `default_injections` + Hook 委托；`BackendOrderShipmentsService` 读 OrderShipment / 待发货摘要。
- 验：槽位/部件契约 UT。

## 2026-09-14 — 订单详情配送方式人性化（审图 ui_shot）

- 线稿：「备注与履约方式」卡内「配送方式」主值原为 `SEED_LANE_*` 服务码。
- 修：view 注入 `payment_chrome`；主显 `service_name`（如「美洲」）；服务码降为 quiet 小字按钮，点击复制。
- 验：`BackendOrderViewShippingMethodHumanizeContractTest`；Browser 禁缓存。

## 2026-09-14 — 未付订单信号 order_signals（供营销挽回）

- 规格：`doc/开发/spec/order-unpaid-signals.md`。
- Query `order_signals.list_unpaid_orders` / `get_unpaid_order`；`UnpaidOrderSignalService` + `ContinuePayUrlBuilder` + Checkout `findSubmittedTokenByOrderUuid`。
- `OrderReadResult` 增 `paymentStatus` / `checkoutEntry`；Facade hydrate 对齐。
- 不定义遗弃 TTL；最大回看 90 天为工程护栏；无触达邮件。
- 验：`OrderSignalsQueryProviderContractTest` + `ContinuePayUrlBuilderTest`。

## 2026-09-14 — 订单结账来源 checkout_entry（2.13.30）

- 与 Checkout `checkout_entry` 对齐：订单列可筛；列表展示「万能结账 / 快捷支付 / …」。
- OrderFacade / OrmOrderFacadeStore 落库；后台筛选 `?checkout_entry=`。

## 2026-09-14 — 退款成功态绿勾一眼可读（2.13.28）

- 审图：退款历史「状态/顾客视图」裸写 `succeeded` 像代码，难扫读。
- 变更：`BackendOrderOutcomeChip` + `partial/outcome-chip.phtml`；成功用 `data-tone=success` + `check-circle` +「成功」；失败红叉；顶栏退款行同步。
- 验收：OutcomeChip UT/合同；e2e 顶栏+退款 Tab；Browser 禁缓存。

## 2026-09-14 — 后台订单顶部状态流转（2.13.27）

- 需求：有退款/状态流转时要做到顶部一眼可见，不依赖退款 Tab 与「更多」历史表。
- 变更：`BackendOrderStatusFlowPresenter`；`partial/status-flow.phtml`；管理页/详情标题下挂载；退款案例展示支付方式与渠道退款号。
- 原型：`view/statics/prototype/order-status-flow.html`（采纳 A 横向轨）。
- 验收：Presenter/合同 UT；e2e `ORDER-EDIT-STATUS-FLOW-001` + `e2e-plan-suite`；Browser 禁缓存。

## 2026-09-14 — 退款成功回写订单历史/头状态（2.13.26）

- 现象：PayPal 沙盒退款渠道成功，后台退款 Tab 有案例，但订单时间线只有「已支付」，`payment_status` 仍 paid，可退候选仍显示已退行。
- 修：渠道 succeeded 后 `syncPersistedRefundProgress` 回写 `qty_refunded`；全额退走状态机 `paid→refunded` 并记历史（含支付方式、渠道退款号、案例 UUID）；部分退只记历史。`refundCandidates`/`refundCases` 按订单过滤并扣占量。
- 验收：`OrderRefundCandidatesUuidContractTest`；后台订单编辑退款 Tab + 时间线。

## 2026-09-14 — 订单行 order_id=0 修复 + 退款候选按 UUID（2.13.25）

- 现象：PgSQL `Order::save()` 不回填自增主键，item 写成 `order_id=0`；后台退款候选按 `order_id` 查为空。
- 修：`OrmOrderFacadeStore` save 后按 `order_uuid` 再读主键；`hydrateOrder` 带出 `payment_method`/`payment_status`；`refundCandidates` 优先 `order_uuid`。
- 后台退款历史补支付方式、渠道退款号、原因、channel_status。
- 验收：`OrderRefundCandidatesUuidContractTest`。

## 2026-09-14 — 运费第4章：COD 进 money_snapshot + 分批发快照（2.13.24）

- `MoneySnapshot.cod_fee_amount_minor` 计入 `withComputedGrandTotal`；`OrderPaidContext` 守恒含 COD。
- `OrderFacade`：支付方式 COD 时加算 grand；`shipping_snapshot_json` 扩展 `split_shipment_shipping`/`outbound_shipping_minor`/`duty_notice`。
- `BackendOrderTotalsPresenter` 展示「货到付款手续费」行。

## 2026-09-13 — 结账支付方式落库修复

- 现象：管理订单摘要「未指定 · 部分支付」，但结账/挂账曾选支付方式。
- 根因：Checkout V2 `submit` 未把 `payment_method` 写入 `CreateCheckoutGroupCommand`；挂账定金成功只改 `payment_status`，不回写 method。
- 变更：submit options 透传；OrderFacade/Orm 持久化；deposit/createPayment 回写空 method。
- 验收：OrderFacade UT；Checkout 合同 UT。

## 2026-09-13 — 运输方式可读名 + 运费/优惠项目展示

- 审图/需求：管理订单摘要与配送手风琴仍展示 `SEED_LANE_DOMESTIC` 等代码；运费与优惠项目清单缺失。
- 纠偏：摘要展示「运输方式」可读名、「运费」金额、「优惠项目」清单（无则「无优惠项目」）；配送手风琴摘要为名称+运费，代码降级为次要信息。
- 扩展点：`OrderShippingMethodCatalogInterface`（Shipping `OrderShippingMethodCatalog` → `service_name`）；`BackendOrderPaymentChromePresenter` / `BackendOrderTotalsPresenter`。
- 验收：Presenter UT；e2e `ORDER-EDIT-RICH-001`；Browser 禁缓存。

## 2026-09-13 — 支付方式手风琴与空值纠偏

- 审图 `ui_shot`：客户调整里支付方式为空白 text input，与地址手风琴不一致且空值不可读。
- 纠偏：支付/配送改为 `w-card`+`w-disclosure`；空 method 摘要「未指定 · 支付状态」；展开为 Payment 目录 select。
- 扩展点：`OrderPaymentMethodCatalogInterface`（Payment 实现）+ `BackendOrderPaymentChromePresenter`。
- 验收：Presenter/合同 UT；e2e `ORDER-EDIT-RICH-001`；Browser 禁缓存。

## 2026-09-12 — 管理订单商品行缩略图

- 审图 `ui_shot`：订单商品表仅名称/SKU 文本，缺主图，识别成本高。
- 纠偏：不以 live catalog 改价；仅展示缩略图 + 新单快照透传 `image`；历史单按 `product_id` 解耦回退。
- 扩展点：`OrderCatalogImageResolverInterface`（Product `OrderCatalogImageResolver`）；Checkout `pricingChromeFromLine` / OrderFacade `plannedItem` 透传 `image`；`BackendOrderLinePresenter` 投影 `image_src`；edit/view `w-cluster` 40px 紧凑缩略图。
- 验收：Presenter UT；Checkout chrome 合同；e2e `ORDER-EDIT-RICH-001`（`order-edit-item-image`）；Browser 禁缓存。

## 2026-09-12 — 管理订单页紧凑原型（默认收起）

- 审图 `ui_shot`：地址虽折叠，客户四字段/支付配送/大备注/历史等仍常开摊开，纵向过长。
- 纠偏：信息层次改为摘要→办理→商品→客户调整摘要行→更多；扩展点 `w-disclosure`。
- 变更：`order-edit-writable`/`order-edit-more` 默认 closed；字段密排；备注 rows=2；间距 space-2。
- 验收：`BackendOrderEditCompactContractTest`；e2e `ORDER-EDIT-RICH-001`；Browser。

## 2026-09-12 — 管理订单办理异步 Tab

- 审图 `ui_shot`：发货/退款/支付沟通为跳转入口按钮，无法同页办完；布局偏宽。
- 纠偏：同页 Theme `w-tabs` + `Order::panel` AJAX 片段（按 order_id 过滤）；禁止 iframe/整页列表跳转。
- 变更：`BackendOrderOpsPanelPresenter`；panel/shipment|refund|comms；edit「订单办理」Tab；`panel` 用 `template()` 返回纯片段；JS 抽取 `order-edit-panel-*`；execute `return_url` 回管理页。
- 运维：补齐 `order/backend/order/panel` 路由后 `server:reload`；Presenter 手动注入 `OrderTradeAdminCommandService`（`object` 类型无法 DI）。
- 验收：Presenter/Tabs UT PASS；e2e `ORDER-EDIT-RICH-001` PASS；Browser 禁缓存见发货表。

## 2026-09-12 — 管理订单摘要紧凑多列

- 审图 `ui_shot`：订单摘要左右宽表 label/value 拉满横向，空隙大、同屏信息少。
- 变更：摘要改为 Theme `w-stat-tiles` 自适应密铺（`--w-stat-tile-min:11rem`、小 gap）+ `w-cluster` 标签值同行；宽屏自动多列；保留 totals testid。
- 验收：`BackendOrderSummaryCompactContractTest`；e2e 无宽表；Browser。

## 2026-09-12 — 管理订单地址改为默认收起卡片

- 需求：地址信息收起来做成卡片，不要直接展开。
- 变更：去掉首屏常开地址预览；收货/账单并入 `w-disclosure` 卡片，trigger 显示摘要，panel 默认 `hidden`。
- 验收：`BackendOrderAddressDisclosureContractTest`；e2e 断言收起、点击展开；Browser。

## 2026-09-12 — 订单行价格/活动折扣快照与管理页展示

- 审图 `ui_shot`：订单商品表仅成交单价，缺原价/活动名/行优惠。
- 需求纠偏：只改 UI 不够；Cart freeze 已有 `compare_at_minor`/`campaign_*`，须在 Checkout `bucketBySplitKey`/submit 与 OrderFacade plannedItem 透传写入 `catalog_snapshot`。
- 变更：Checkout 透传；OrderFacade 落 `line_discount_minor`；`BackendOrderLinePresenter` 投影；edit/view 价格快照列。
- 验收：Presenter UT + Checkout 契约；e2e `ORDER-EDIT-RICH-001`；Browser 管理页见「价格快照/活动」。

## 2026-09-12 — 管理订单：小计累积 + 客户 Taglib + 地址可调

- 审图/反馈：摘要金额不全、客户只能填数字 ID、地址只读不可调。
- 需求纠偏：仍冻结行拓扑改价；补齐「为客户调整」— 行累计/小计/运费税折总额、`<w:customer:admin:select>`、收货/账单 `<w:theme:address>` + 明细/邮编种子字段、状态/备注/取消入口。
- 变更：`BackendOrderTotalsPresenter`；`BackendOrderCustomerAdjustService`（白名单 + 地址 merge）；`edit` 赋 totals/forms/customer select；`edit.phtml` 可调表单；`save`/`addComment`。
- 验收：Totals/Adjust UT PASS；e2e `ORDER-EDIT-RICH-001` PASS（`PLAYWRIGHT_TARGET_ORIGIN=https://p05113ef3.test.weline.com:9555`）；Browser `edit?id=59`。

## 2026-09-12 — 后台「管理订单」富信息页（对齐 Magento/Shopify 只读深度）

- 审图 `ui_shot`：编辑页空荡，缺商品行/地址/金额/备注上下文。
- 需求纠偏：不做可任意改行的 Magento 克隆编辑器；编辑=管理订单（只读快照 + 窄写备注/联系/支付配送）。
- 根因补充：拓扑行常 `order_id=0` 仅挂 `order_uuid`，原 `getOrderItems` 漏行；另 `customer_*` 空而地址在 `shipping_address` JSON。
- 变更：`BackendOrderListPresenter` 地址行；`BackendOrderLinePresenter`；`getOrderItems` uuid 回退；`edit`/`view` 富分段模板；行只读不可提交 `items[]`。
- 验收：Presenter UT；e2e `plan-order-edit-rich`；Browser `edit?id=59` 见摘要/收货地址/商品行/可编辑区。

## 2026-09-12 — 后台订单列表可搜前台分组号 G-xxx

- 审图 `error_shot`：搜 `985b4913`（前台 `G-985b4913`）暂无数据；后台订单号为 `9779203997`。
- 根因：keyword 只 LIKE `order_number`/客户字段；前台展示号来自 `checkout_group_uuid` 前 8 位。
- 变更：`OrderListKeywordNormalizer`（去 `G-`）+ `getOrderList` OR 搜 `checkout_group_uuid`/`order_uuid`；列表副标 `G-…`；placeholder 提示。
- 验收：`OrderListKeywordNormalizerTest`；Browser 搜 `985b4913`/`G-985b4913`。

## 2026-09-12 — 订单邮件 OrderMailNotifier（channel+vars）

- 当前状态：`verified`
- 变更：`OrderMailNotifier` + `OrderCreated/Paid/StatusChanged(/Shipped/Refunded)` Observer；发货/退款映射专用渠道；空邮箱跳过并打日志；显式 website/scope/locale。`MailChannelProvider` 默认模板 `view/email/order_*`。
- 验收：`OrderMailNotifierContractTest` PASS。

## 2026-09-12 — 账户/后台订单类型徽章可读化

- 当前状态：`implemented`
- 问题：账户「我的订单」无类型标记；后台类型列曾用弱 `muted`/`bg-*`，零售/批发不易辨认。
- 变更：`TocCommerceOrderType`/`TobCommerceOrderType` 文案「零售订单」「批发订单」+ tone `info`/`warning`；`AccountCheckoutGroupPresenter` 注入 `order_type_*`；账户列表/详情徽章 + `account-orders.css` tone；后台列表/详情改 `w-badge data-tone`。
- 验收：模板契约单测；Browser 账户 `#orders` 与后台订单列表可见类型徽章。

## 2026-09-12 — 后台菜单/跳转前缀统一为 order

- 当前状态：`implemented`
- 问题：菜单与部分 redirect/模板仍写 `weline_order/backend/*`，与 `etc/env.php` router=`order`、`generated/routers/backend_pc.php` 键不一致，导致 `…/weline_order/backend/records/payment` 落入 frontend 404。
- 变更：`etc/backend/menu.xml`、Backend 控制器 redirect、Records/Invoice/Refund/Shipment/Status 模板、`r43-menu` e2e 统一为 `order/backend/*`；契约单测 `BackendOrderRouterPrefixContractTest`。
- 验收：未登录 `order/backend/records/payment` → 302 登录；错误前缀仍 404。

## 2026-09-10 — type_payload 落库 + Payable order_type

- `OrmOrderFacadeStore` 持久化 `order_type` + `type_payload_json`；`OrderFacade::mergeTypePayload`；`OrderPayableResolver` 快照带 `order_type`/`type_payload`；后台详情展示批发信用优惠行。

## 2026-09-07 — 后台订单类型徽章与类型化订单事件（2.13.14）

- 变更：后台订单列表/详情展示 `order_type` 徽章（`w-badge bg-*`，未注册历史 tob →「历史批发」）；列表筛选项仅已注册 code。新增 `OrderTypeEventEnvelope`，在 `order_created` / `order_status_changed` / `order_paid` 派发处非破坏追加 `order_type`+`type_payload`（toc：`discounts_applied`；tob：hang/group/deposit 若有）。
- 单测：`OrderTypeEventEnvelopeTest`。
- 版本：`2.13.13` → `2.13.14`

## 2026-09-07 — ToC/ToB 一期：订单事件追加 `order_type` + `type_payload`

- 当前状态：`implemented`（运行时信封已落地，见上条）
- 变更：`doc/event/order_created.md` 写明非破坏追加 `order_type`/`type_payload` 与 toc/tob payload 示例；`order_paid.md`、`order_status_changed.md` 短注同合同。
- 关联：`Weline_B2B` `REQ-B2B-0007`；禁止破坏性改写既有事件字段。

## 2026-09-04 — 物流追踪英文补译

- 当前状态：`implemented`
- 变更：`i18n/en_US.csv` 补全账户详情可见的「物流追踪 / 已下单 / 订单已支付，等待发货 / 支付完成，仓库备货中 / 当前」及同块系统追踪相关键的英文译文（原先中文自译）。
- 目标版本：`2.13.12`

## 2026-09-04 — 修复物流追踪竖排挤碎

- 当前状态：`implemented`
- 变更：`result-panel` 经 PHP include 不编译 Taglib；原 `<w:static>` 在 HTML 中未闭合，后续文案落入 `width:2rem` 图标容器导致一字一排。改为 `fetchTagSource`+`<img>`；重写 `order-tracking.css`（flex 步进条 + 时间线）；CSS 版本 `amazon3`。
- 目标版本：`2.13.11`

## 2026-09-04 — 订单详情 binquery 软加载

- 当前状态：`implemented`
- 变更：详情链接增加 `data-order-uuid`；返回链接 `data-account-orders-back`；由 Customer `account-index.js` 经 `account.getSidebarSection` 动态替换订单分区，避免整页闪烁。
- 目标版本：`2.13.10`

## 2026-09-04 — 物流追踪面板亚马逊风格

- 当前状态：`implemented`
- 变更：`result-panel.phtml` 改为横向进度步进 + 事件时间线；当前步骤 `data-state="current"` 使用主色文字；新增 `order-tracking.css`，侧栏 `<css>` 与详情 `@static` 挂载。
- 目标版本：`2.13.9`

## 2026-09-04 — 个人中心订单面板补齐 CSS

- 当前状态：`implemented`
- 变更：新增 `view/statics/css/account-orders.css`；`account.sidebar.content` 声明 `<css>Weline_Order::css/account-orders.css</css>`，经侧栏 `loadTrustedSidebarStyles` 提升到 head（禁止依赖会被 sanitize 剥掉的内联 style）。选择器限定 `[data-account-orders]`，避免与 Theme `account_orders` 布局壳冲突。
- 目标版本：`2.13.8`

## 2026-09-03 — 订单留言三表面 Slot 注入

- 当前状态：`implemented`
- 变更：`order-notice` 扩展到 `mini-cart` / `cart` / `checkout`；模板按 `surface` 切换迷你车 tab 与摘要面板样式；Upgrade 回填默认注入。
- 目标版本：`2.13.7`

## 2026-09-02 — 访客订单认领 API

- 当前状态：`implemented`
- 变更：`OrderFacadeInterface::attachCustomerToGuestOrders`；读投影增 `customer_email`。仅空 `customer_id` 可绑，冲突 fail-closed。
- 目标版本：`2.13.6`

## 2026-09-02 — 订单详情支付记录改为空槽 + Payment 部件

- 当前状态：`implemented`
- 变更：详情页去掉硬编码支付表与 `payments` 赋值；留 `<w:slot id="backend-order-payment-records">` + Hook；删除错误的 `BackendOrderPaymentHistoryResolver`。客户/时间继续用 `BackendOrderListPresenter`（含电话回退）。
- 目标版本：`2.13.5`

## 2026-09-02 — 订单列表客户与创建时间

- 当前状态：`implemented`
- 变更：新增 `BackendOrderListPresenter`：创建时间取 `create_time`（回退 `created_at`）；客户取 `customer_*` 并回退 `shipping_address` 姓名/邮箱；`customer_id` 空标记「匿名」。列表模板与 `getOrderList` 排序同步。
- 目标版本：`2.13.4`

## 2026-09-02 — 订单详情面包屑订单管理路由

- 当前状态：`implemented`
- 变更：`Backend/Order/view|edit.phtml` 面包屑与同页动作链接由错误的 `weline_order/backend/order/*` 改为权威 `order/backend/order/*`（与 `etc/env.php` router=`order`、generated routers 一致）。
- 目标版本：`2.13.3`

## 2026-08-30 — 删除独立订单跟踪页壳

- 当前状态：`implemented`
- 变更：删除 Theme `order_tracking` 布局、相关 Hook/文档与 `TrackRedirect`；旧 URL 仅由 `Order\Controller\Router` 302 到账户订单。
- 目标版本：`2.13.2`

## 2026-08-30 — 物流追踪并入个人中心订单

- 当前状态：`implemented`
- 变更：账户订单详情渲染 TrackingProvider 结果；列表展示履约摘要；顶栏/帮助入口改链 `#orders`；`/orders/track` 经 Router 302 到账户订单。
- 目标版本：`2.13.1`

## 2026-08-30 — TrackingProvider 跟踪壳

- 当前状态：`implemented`
- 变更：落地 `TrackingProviderInterface`、System/Fake Provider、反馈 Inbox、统一 notify、开发中继。
- 文档：`doc/tracking-shell.md`、`doc/tracking-provider-development.md`、`doc/dev-tracking-relay.md`
- 目标版本：`2.13.0`

## 2026-08-30 — 前台 JS 改 modules + data-weline-load（第二波）

- 当前状态：`implemented`
- 变更：登记 `view/statics/frontend/weline.modules.js`；部件/布局改 `data-weline-load`，删除 `@static`/`<js>` 直引。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。

# Weline_Order 开发日志

## 2026-09-10 — type_payload 落库 + Payable order_type

- `OrmOrderFacadeStore` 持久化 `order_type` + `type_payload_json`；`OrderFacade::mergeTypePayload`；`OrderPayableResolver` 快照带 `order_type`/`type_payload`；后台详情展示批发信用优惠行。

## 2026-09-07 — ToC/ToB 一期

> 按目标版本记录真实进度、调整与验收证据；2026-08-17 前的历史不补造。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `2.12.5` | verification_in_progress | `REQ-ORDER-0001`, `REQ-ORDER-0002` | 2026-08-17 | 2026-08-17 | Backend deterministic pass; Browser pending |

## `2.12.5` — 账户订单隔离与请求状态修复

- 基线版本：`2.12.4`
- 目标版本：`2.12.5`
- 当前状态：`verification_in_progress`
- 需求 ID：`REQ-ORDER-0002`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：账户订单 Hook 与聚焦测试；不创建新账户页，不改 Order Schema。
- 负责人/确认人：主执行者直接实现（未委派）/用户

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#req-order-0002-个人中心订单隔离与-wls-请求安全` | 2026-08-17 |
| 实现执行者/模型/范围已记录 | pass | 本条“负责人/确认人”；主执行者直接执行 | 2026-08-17 |
| 架构/缺陷/安全复审通过 | pass | 移除 process-global Hook 标记；customer+website/UUID 授权回归保留 | 2026-08-17 |
| 分层测试通过 | pass | 站点 Cart/Checkout/Order：168 tests / 986 assertions；框架 peer：150 / 900 | 2026-08-17 |
| 真实路径 WebUI/E2E 通过 | pending | 待 9514 个人中心 Browser 验收 | 2026-08-17 |
| 可重复 E2E 固化并通过 | pending | 待账户订单回归 | 2026-08-17 |
| 功能/运营文档完成 | pass | `doc/hook/frontend/account/index/orders.md` 已补 WLS 请求隔离约束 | 2026-08-17 |
| 用户/产品验收 | pending | 待用户确认 | 2026-08-17 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-17 | requirements | planned → requirements_confirmed | 账户 Hook 存在无人读取/清理的 `$GLOBALS` 标记 | 移除进程全局请求状态并保留所有权校验 | 用户 | 当前任务“继续”确认 |
| 2026-08-17 | implementation | requirements_confirmed → implemented | 账户订单 Hook 不再用 `$GLOBALS` 标记请求渲染状态 | 避免常驻 Worker 跨请求泄漏，同时保持账户所有权校验 | 用户 | `view/hooks/Weline_Order/frontend/account/index/orders.phtml` |
| 2026-08-17 | verification | implemented → verification_in_progress | 核心刷新后的 Order/account 回归已恢复并通过全套测试 | 真实账户订单 Browser 仍待绑定 Weline Chrome 复验 | 主执行者 | 168 / 986；150 / 900 |

### 实现范围

- 变更路径/符号：账户订单 Hook、`AccountCheckoutGroupPresenter`、账户订单模板/控制器及聚焦测试。
- 数据/配置/迁移：无 Model Schema。
- 兼容与回退：保留现有 Hook 名、section id 与账户投影。

### 审查与整改

- 架构：pass；Hook 生命周期不再写进程全局状态。
- 缺陷：pass；账户订单列表/详情恢复实现已纳入完整商城回归。
- 安全：pass；customer+website 所有权与越权 UUID 聚焦测试通过。
- 用户批准延期：none。

### 测试与验收

- 聚焦/单元/集成：pass，站点 168 tests / 986 assertions；框架 peer 150 / 900。
- 真实 WLS + WebUI：pending。
- E2E 固化：pending。
- 未验证项：9514 真实登录账户订单列表/详情 Browser 复验。

### 文档与运营交付

- 需求文档：`REQ-ORDER-0002`。
- 功能/API/架构文档：`doc/hook/frontend/account/index/orders.md` 已更新。
- 运营文档：N/A；无新增运营入口或配置。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。 待生成器刷新。

### 版本结论

- 结论：verification_in_progress（实现与确定性门禁通过，尚未 accepted）。
- 完成/发布证据：未提交、未推送、未部署。
- 剩余事项与恢复条件：完成 9514 Weline Chrome 账户订单 Browser 验收与用户验收。

## `REQ-ORDER-0001` — 通用订单支付后状态补全

- 默认 `OrderPostPaymentHookInterface` 已恢复为 `OrderPaidStateHook`，只依赖
  `OrderPaidContext` 与 Order 自有持久快照，不引用具体支付渠道 Model/Service。
- 首次支付成功按合法状态机推进 `pending → processing → paid`，并只派发一次
  `Weline_Order::order_paid`；重放保持 paid/version 不变。
- `AbstractModel::load()` 身份映射已按框架 request ID 分桶，并在对应
  `RequestContext` 清理时删除；CAS 后读取持久 post-image，避免 WLS Fiber
  跨请求串用陈旧订单。
- 确定性证据：Order Unit `94 tests / 593 assertions`（4 个既有 incomplete）；
  Facade DB Integration `3 / 50`；RequestContext Fiber `7 / 47`；聚焦通用支付、
  同事务连续 CAS、跨 request ID 隔离 `5 / 34`。
- 本次是后端契约修复，无新增 UI；`REQ-ORDER-0001` 的支付成功页到账户订单
  真实 WebUI 全链仍随既有 Browser 验收项执行，不以单元测试冒充。
