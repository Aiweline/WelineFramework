## 2026-09-17 — 活动首页只展示有活动标记商品（1.1.30）

- 现象：`/promotion` 货架混入无划线价、无「今日特价」等活动标的普通商品。
- 根因：index 页无主题选品时 `loadProducts` 回退 `publishedOfferSummaries` 全站目录切片；Assembler 仅对可命中活动的 SKU 打标，其余原价展示。
- 修：`listHubStorefrontProductIds` 聚合当前 scope 活跃主题选品；index 只 `loadProductsByIds`；定价后 `keepDealMarkedItemsOnly`；枢纽按卡透传 `promotion_theme_id`。
- 版本：`1.1.29` → `1.1.30`。

### 汇审

| 项 | 结果 |
|---|---|
| work_kind | bugfix（活动首页货架过滤） |
| 审图 | web_ui / ui_shot；线稿货架态一致性 fail→修过滤；E/F/G pass（主题 Token，无新色板） |
| UT | HubDealFilter / AssemblerPricing / ActiveDealResolver / ProductDedupe 契约 PASS |
| Browser | `/promotion?weline_no_fpc=1` 禁缓存；匹配商品 · 10；10/10 卡均有划线价+活动标 |
| e2e | 本波未扩 suite（过滤契约 + WB-OP 已覆盖；布局壳既有 suite 未回归） |

## 2026-09-17 — 画布不再水合：走真实路由

- 画布 iframe 打开 `preview_sample` 公网路径；控制器正常套 promotion 布局。
- 已删除 `LayoutCanvasBodyObserver`（无需 Theme 专用 canvas API）。

## 2026-09-17 — 移除 layout_canvas_body Observer

- 画布改由 Theme `StorefrontSampleCanvasHydrator` 按 `layout_preview_sample` 调真实控制器；删除 `LayoutCanvasBodyObserver`。
- 模块只需：控制器 + promotion 布局 + preview_sample。

## 2026-09-17 — 画布 layout_canvas_body 水合（1.1.29）

- 现象：编辑器促销壳画布空态，与 `/promotion/deals` 不对齐。
- 修：`LayoutCanvasBodyObserver` 声称 `content_template` + `build(slug)` assign；优先 deals。
- 版本：`1.1.28` → `1.1.29`。

## 2026-09-17 — promotion 壳可视化对齐首页（1.1.28）

- 目标：`layout_path=promotion` 与 homepage 同套草稿/真店面预览/发布；slug 仅取样 URL。
- 实现：`LayoutResolve`/`preview_sample` Observer；壳迁入 Promotion；Theme 编辑器 `preview-sample` + `theme_public_route`；`resource:compile welineUi`。
- 验收：UT 契约；e2e `promotion-layout-shell-plan-suite` 3 passed；Browser `/promotion` 与 `/promotion/deals` 同 `data-layout=promotion`。
- 版本：`1.1.27` → `1.1.28`。

### 汇审

| 项 | 结果 |
|---|---|
| work_kind | feature（布局可视化能力对齐） |
| UT | LayoutResolve/Promotion/ThemePublicPageMatrix 契约 PASS |
| e2e | `promotion-layout-shell-plan-suite` 3/3 PASS（headless chromium） |
| Browser | `/promotion`、`/promotion/deals` 禁缓存；`data-layout=promotion` |
| 未做 | 本波未在 Browser 内完成完整「改槽→发布」联调（依赖后台登录会话；契约与 start-preview 接线已覆盖） |

## 2026-09-15 — 页头今日特价部件默认注入右簇（1.1.27）

- 新增 `header-deals-link` → `header-nav-extensions`（homepage required，sort 10）；Theme 去掉硬编码「今日特价」。
- 契约：`HeaderDealsLinkWidgetContractTest`。
- 版本：`1.1.26` → `1.1.27`。

## 2026-09-14 — 促销源键九语（1.1.25）

## 2026-09-14 — FAQ/chrome i18n（1.1.26）

- deals 页 title/hero 对汉字源串走 WidgetI18n，避免 DB 中文直出。

- 模块 CSV 补 `促销` 九语；Dictionary upsert + `i18n:collect`。

## 2026-09-14 — 默认站 chrome 漏译补齐（1.1.24）

- 默认站八语补齐店面 chrome 漏译：图片、看今日特价。

# Weline_Promotion 开发日志

## 2026-09-14 — 活动主题 starts_at/ends_at（站点 TZ 录入 → UTC）

- 模型增 `starts_at`/`ends_at`（UTC）；表单 `datetime-local` + 站点时区旁注。
- 店面 `PromotionStorefrontActiveDealResolver`：`status=active` ∧ `Timezone::isWithinUtcWindow`。
- 同步 Marketing automatic 规则时写入同一 UTC 窗（`ExternalDealDiscountRequest`）。
- 修复主题表单编译：`{{total}}` 被 Taglib 展开导致 ParseError；改为 `[[…]]` + 花括号拼接，并修正 JS 内 `@admin-url`。
- 验证：Timezone/主题窗契约 UT；Marketing+Promotion schedule-utc e2e；Browser 主题表单旁注与起止控件。

## 2026-09-14 v1.1.22 活动徽章中文源串按店面 locale 翻译

- 现象：印地语列表卡 `wpc-campaign` 仍显示「今日特价」（Theme CSV/`headerEsc` 已译）。
- 根因：`resolveCampaignDisplayLabel` 直接输出 DB/Local 中文源键，未 `__()`；`defaultNavLabel` 虽已 `__()`，有 `nav_label` 时不会走到。
- 变更：`translateStorefrontCampaignLabel` 对含汉字展示名调用 `__()`；补 `i18n/hi_IN.csv`；契约测 `StorefrontCampaignLabelI18nContractTest`。
- 验证：`i18n:collect Weline_Promotion`；定向 phpunit。

## 2026-09-08 v1.1.21 活动页进 PDP 带入活动上下文

- 需求：从活动专场点进详情须优先生效该活动（eligible 内），避免货架与 PDP 错位。
- 变更：`buildStorefrontPage` 透出 `theme_id`；货架 Assembler preferred + URL `promotion_theme_id`/`campaign`；`resolveActiveThemeIdByPageSlug`。
- 验证：契约/定向；Browser 从 `/promotion/deals` 进 PDP。

## 2026-09-08 v1.1.20 多活动默认按 sort_order

- 需求：交叉活动须默认选中并计价；列表与 PDP 同序；禁止活动标显示 slug（如 `deals`）。
- 变更：`listEligibleDealsForProduct` / `resolveForProduct` 按主题 `sort_order` 升序，默认取序首；透传 `campaign_label`/`campaign_url`；`resolveCampaignDisplayLabel` 拒绝 slug 回退到「今日特价」等展示名。
- 验证：`PromotionStorefrontActiveDealResolverTest`；Browser PDP 默认活动名与序首价。

## 2026-09-08 v1.1.19 多活动交叉可选 + 后台确认

- 需求：SKU 落入多个真实折扣活动时，PDP 活动标可下拉选择；默认最强；后台保存交叉时弹窗确认。
- 变更：`listEligibleDealsForProduct` / `preferredThemeId`；`eligible_campaigns` 进 Assembler；PDP campaign picker；Cart `selection.promotion_theme_id`；`findActiveDealOverlaps` + save `force_overlap`。
- 验证：契约测；Browser 多活动 PDP 下拉切价。

## 2026-09-08 v1.1.18 今日特价 ≠ 今日精选

- 问题：顶栏「今日特价」进 `/promotion/deals`，商品活动标却是「今日精选」（deals 主题默认 `nav_label` 误用精选文案）。
- 变更：deals 默认/回退文案改为「今日特价 / 今日特价专场 / 看今日特价」；`migrateLegacyDealsFeaturedBranding` 改写已知遗留「精选/Picks」串；i18n 与需求同步。今日精选留给 featured 精选陈列，不作 deals `campaign_label`。
- 验证：LaunchReadiness 契约；Browser PDP/deals 活动标为今日特价。

## 2026-09-08 促销选择冷读取回归（生产版本不变）

- 商品选择性能修复位于 Product 1.0.185：既有选择接口批量读取店铺覆盖及权威商品身份，未修改促销生产算法。
- 对齐 `PromotionStorefrontActiveDealResolverTest` 的既有 URL 依赖夹具，避免未初始化属性异常被 resolver 捕获后误判空活动；公开签名断言改为 Reflection 参数兼容检查。
- 促销选择、最强折扣、空匹配、手动 price_band 与网站隔离随 Product 联合 29 tests / 158 assertions 通过；真实页面由主任务验收。

## 2026-09-08 v1.1.17 促销前台 SEO 主权

- 问题：`/promotion/deals` 仅有通用 Head 兜底（WebPage、布局后缀标题），无 CollectionPage / ItemList。
- 变更：
  - `PromotionSeoFactsBuilder`：`page_type=product_list`，description=hero_lede，item_list 来自货架（空时回退入口卡）。
  - `PromotionStorefrontPageService::build()` + `Controller\Index` `assign('seo')`；UI `page_type` 仍为 deals/sale 等 slug。
  - `PromotionSeoProfileProvider` / `PromotionSitemapUrlProvider`。
- 验证：契约测 OK；线上 `/promotion/deals` → title 无布局后缀、`page-type=product-list`、JSON-LD=`CollectionPage+BreadcrumbList+ItemList`。

## 2026-09-08 v1.1.16 汉服活动矩阵调整（下线礼盒）

- 运营：删除礼盒（gifts）活动；内置主题改为 deals / sale / weekend / wedding。
- 文案：今日精选可配优惠；节令主题、出游常服、婚嫁礼服默认无优惠纯陈列；修正 sale 英文残留。
- 变更：`ensureDefaultThemes` + `retireGiftThemes`；默认文案 / i18n / 契约同步。
- 选品：默认站四个活动改为运营手动绑定各 12 个互不重叠的真实可售商品（此前 manual 空绑导致价位回退货架雷同）。
- 验证：契约测 + `/promotion` 导航无礼盒、可见婚嫁礼服；各 slug 货架 product_id 集合互异。

## 2026-09-07 v1.1.15 英文活动首页可见文案补齐

- 问题：`/en_US/promotion` Hero 说明与「匹配商品」仍中文，与已译标题/CTA 混排。
- 修复：`i18n/en_US.csv` 补齐首页 Hero lede、匹配商品及 weekend/gifts 默认说明英文译文；契约锁定。
- 验证：`PromotionStorefrontLaunchReadinessContractTest` + Browser `/en_US/promotion`。

## 2026-09-07 v1.1.14 活动 Tab URL 走框架 getFrontendUrl

- 问题：活动页二级 Tab 硬编码 `/promotion`、`/promotion/{slug}`，多语言前缀（如 `/en_US`）丢失。
- 修复：`PromotionActivityThemeService::storefrontUrl()` → `Url::getFrontendUrl`；Tab / 入口 / campaign / 后台预览 / PageService `list_url` / 模板回退 / 定价 Provider 回退统一走该出口。Theme `main-nav` / `sidebar-ads` 默认入口同步。
- 验证：`PromotionStorefrontUrlBuilderContractTest`；Browser 中文 Tab 为 `…/promotion…`，英文为 `…/en_US/promotion…`。

## 2026-09-07 v1.1.13 活动货架迁统一 product:card

- 问题：活动页手写 `product-card` + `amazon-product-card.css`，与全站统一卡内核不一致。
- 变更：模板改 `<w:product:card>`；`normalizeItems` 透传 `sku/sellable/global_offer_uuid`；`promotion-hub.css` 仅保留网格定位；契约改断言 `weline-product-card-shelf`。
- 验证：契约测 + `/promotion` Browser。

## 2026-09-06 v1.1.12 货架入参改用 catalog_price_minor

- 目录统一 Assembler 后 `unit_price_minor` 已是成交价；货架 `normalizeItems` 若再以其入 Assembler 会叠折。
- 修复：优先 `catalog_price_minor`（原价）再 Assembler。
- 验证：货架/目录卡/PDP 同价。

## 2026-09-06 v1.1.11 货架活动价与 PDP Assembler 同源

- 问题：`/promotion/deals` 卡片显示九折（如 89→80.10），PDP 仍目录价；根因是 FILTER 空选品时货架回退全站目录并强制套 `pageDeal`，而 PDP/`ActiveDealResolver` 正确拒绝空筛选。另：默认站 `website_id=0` 手动绑定曾被 `resolveStorefrontProductIds` 跳过。
- 修复：`applyStorefrontPricing` 在 Assembler 可用时以 Assembler 为准（无优惠保持目录价）；FILTER 空选品不再 `publishedOffers` 假货架；`website_id=0` 绑定保留。
- 验证：契约测 + 浏览器 PDP/货架同价验收。

## 2026-09-06 v1.1.10 活动价接入 Product Assembler + 卡片活动名

- Product 统一定价出口：`PromotionThemeDealPriceAdjustmentProvider` 注册 unit 调整（含活动名/URL）；货架 `PromotionStorefrontPageService` 改读 Assembler。
- 卡片划线旁展示 `campaign_label`（链到活动页）；`resolveStorefrontCampaignMeta` 提供 nav/page 标题。
- 验证：`PromotionThemeDealPriceAdjustmentProviderContractTest`。

## 2026-09-06 — ID选品复用基础投影能力

- `PromotionThemeProductService::searchIds` 对支持 `ProductSelectionReadInterface` 的既有Reader调用 `searchSelectionRows`，只取得当前消费所需的product_id/updated_at，消除完整价格/媒体/展示店铺呈现。第三方旧Reader继续使用原search接口。
- 范围合并、必要店铺筛选、日期/limit/排序、空集与异常处理、优惠计算保持；后台选品预览仍走完整search。现有统一请求缓存及网站/店铺/渠道维度保持，不新增缓存。
- 最小实际Service/Repository基准先RED于额外详情读取；日期、limit、主题网站与请求店铺解析均先通过。正式回归与WLS请求证据见 `/private/tmp/weline-goal/acceptance-report.md`；冷请求<1秒和Browser验收尚待整体目标达成。
- 交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products)。
- 活动选品诊断复用统一 `RequestLifecycleTrace::measurePhase`：`promotion.selection.reader` 包围 `productReader()` 的接口检查、ObjectManager 获取/构造与类型检查；`promotion.selection.search` 只包围所选 `searchSelectionRows` 或兼容 `search` 调用。每次原调用仍只执行一次，原异常/空集/范围/日期/limit 行为保持。两阶段各自记录包含耗时与 DB/WLS 计数，属于父 `product.catalog.snapshot.deals`，不可把父子时长相加；`readerFilters` 中的店铺解析及后续过滤循环仍在这两个子阶段之外，不应把父阶段残差直接归因到 Reader 构造或查询。固定低基数标签由统一 Trace 容量管理，不增加私有日志或缓存。

## 2026-09-06 — 空筛选活动不进入手动价格带兜底

- 实际商品页验收发现同24商品中18件变为九折；当前10%活动按SKU筛选仍0匹配。新价格装配入口传入catalogPrice后，ActiveDealResolver原空集合价格带兜底没有限制pick_mode，误把filter空结果当作手动空绑定。
- 兜底仅限归一化manual模式；filter未匹配返回无优惠。现有手动空绑定价格带、有效选品ID、最强优惠算法和统一请求范围缓存保持，不修改活动/商品数据。
- 最小真实resolver回归先RED（4tests/18assertions仅此错误优惠失败）；正式验证及实际页面价格、性能与Browser状态见 `/private/tmp/weline-goal/price-filter-guard-runtime-report.md`。此为性能验收中发现的行为修复，不代表整页冷请求<1秒已达标。

## 2026-09-06 — 活动选品切断目录递归并复用请求计算

- 实际瓶颈：商品快照计算活动价→活动选品→空结果/异常回退完整目录→快照再次计算活动价。真实商品请求虽约60秒空响应，后台仍执行617.89/658.34秒，目录回源234/275次。
- 修复：活动选品只依赖ProductAdminReadInterface基础读，空匹配、不可用或异常返回空集合，移除反向完整目录fallback；空选品不再误当全场优惠。当前活动筛选SKU在182商品/4276 offers的基础数据中确实无匹配，未用删除有效选品换取加速。
- 性能：ActiveDealResolver通过Framework统一rememberForRequest，按完整scope复用活动列表，并按theme+scope复用选品；同请求各商品继续使用原最强优惠与价格计算算法。未新建私有缓存，无跨请求持久化价格，也不修改PDP/Cart同源计算入口。
- 最小回归先RED，候选7 tests/23 assertions通过，覆盖空结果/失败/不可用不入catalog、排序limit、空集无优惠、最强优惠、同范围复用与网站隔离。正式应用后的单测、真实冷暖HTTP及剩余目标见 `/private/tmp/weline-goal/acceptance-report.md`。
- 完整目标仍为正常访问且WLS冷请求低于1秒；未达到前不得以单测或暖缓存命中宣称全部完成。本次Service/Test/文档调整，无Model/Controller变更。
- 交付地址：[商品页](https://p05113ef3.test.weline.com:9555/en_US/products?nocache=1&wls_trace=1)。浏览器最终验收和小于1秒指标尚待实测。

## 2026-09-05 v1.1.9 活动价贯通详情（ActiveDealResolver）

- 问题：活动货架展示 43.20，详情仍目录价 48；仅询价商品却能加购。
- 修复：新增 `PromotionStorefrontActiveDealResolver`（活动主题未取消即生效）；PDP/加购快照同源 `applyDealToPrice`；`listActiveThemesForStorefront` 公开列表。
- 验证：`PromotionStorefrontActiveDealResolverContractTest`。

## 2026-09-05 v1.1.8 活动卡深链反选同价规格

- 问题：`/promotion/deals` 卡片展示 listing 代表 offer 活动价（如单拍上衣 79→71.10），链接却是裸 `/product/{slug}`；详情默认/其它变体（如单拍裙子黑色 160）价不对，用户疑惑。
- 修复：`PromotionStorefrontPageService::normalizeItems` 复用 Product `StorefrontOfferDetailQuery::params`，给 URL 附加公开变体轴（或 `offer` uuid），进入 PDP 后反选与卡片价格同源的规格。
- 验证：`PromotionStorefrontPageServiceOfferDeepLinkContractTest`、`PromotionStorefrontLaunchReadinessContractTest`；清 FPC 后活动卡 href 含 `size`/`style_type`（或 `offer`）。

## 2026-09-05 v1.1.7 活动顶栏左侧内边距

- 问题：活动页导航「活动首页」等 tab 贴着白色顶栏左缘，与下方 Hero 文案缩进不对齐。
- 修复：抽出 `--promo-rail-pad-inline: clamp(1.5rem, 4vw, 2.75rem)`，`.w-promotion-page__nav` 与 `.w-promotion-page__hero-inner` 共用；静态资源版本 bump 到 `20260905-promo-hub7`。

## 2026-09-04 v1.1.6 汉服活动中心上线收口

- 真实货架：`PromotionStorefrontPageService` 改为直接读取当前 scope 的 `StorefrontCatalogViewService::publishedOffers()`，按价格带筛选、去重并生成真实 PDP URL；删除验收占位商品与内联 SVG 占位图。
- 本地化：LocalModel 读取改为 `lang_local` 优先；内置活动主题支持翻译后默认文案，补全英文前台词条并新增 `ar_SA.csv`。
- 品牌视觉：活动 Hero 复用首页真实汉服影像，改为暖纸色、墨色宋体与朱砂 CTA；价格显示跟随 offer 币种，CSS 使用逻辑方向间距，并在 RTL 下镜像 Hero 渐变与人物焦点。
- 语义结构：促销业务模板根节点改为普通容器，由 Theme 促销布局唯一提供 `<main>` 地标，避免嵌套主内容区影响无障碍导航与 SEO。
- 权限收口：`promotion_admin` Query Provider 与运营台菜单统一使用可见页 ACL `desk_index`，避免菜单可见但页内请求被父级 ACL 拒绝。
- 验证：`PromotionStorefrontLaunchReadinessContractTest` 覆盖真实商品、无占位素材、单一主内容地标、locale 优先级和 zh/en/ar 字典合同。

## 2026-08-28 默认店铺 store_id=0 预览

- 根因：条件选品预览遍历默认站 `store_id=0` 时被 Product 分片仓拒绝；`resolveStoreId` 也曾把 `id=0` 当成无效。
- 修复：接受 `store_id=0`；与 Product `assertStoreOverlayId` 合法化配套。
- 验证：编辑活动主题 Website=#0 刷新预览不再报「store_id 不能为 0」。

## 2026-08-28 活动折扣改走 Marketing 提供者扩展

- Marketing 发布 `ExternalDealDiscountProviderInterface`；Promotion 仅通过 `RuntimeProviderResolver` 调用 upsert。
- `PromotionThemeDealDiscountSyncService` 不再引用 `Marketing\Model\Rule\Rule`；契约测 `PromotionThemeDealDiscountSyncProviderContractTest`。

## 2026-08-28 今日特价只认 /promotion/deals

- 取消顶级别名 `/deals`；Router / `storefrontUrls` 不再改写或登记 `/deals`。
- Theme 顶部导航插槽「今日特价」仅使用 `/promotion/deals`（`header/default.phtml`、`main-nav`、nav-links 文档）。

## 2026-08-28 活动价同步 Marketing 自动规则

- 主题字段：`deal_discount_type` / `deal_discount_value` / `marketing_rule_id`；保存时 upsert automatic 规则，description 标记 `source=promotion_activity_theme`。
- Marketing：报价上下文带 `products.sku`；百分比/固定减额支持 `apply_to=matched_products`。
- 前台展示活动价 + 划线原价；加购折扣与陈列同源。

## 2026-08-28 前台选品产品层去重

- 根因：条件选品 `product_ids` 已正确（如仅 `[27]`），但 `loadProductsByIds` 调用 `publishedOffersForProductIds(..., max(12, n))`，目录重复 offer 行被截成 12 条同商品卡片。
- 修复：按 `product_id` 去重保序；显式选品不再回退全站 12 条；契约测 `PromotionStorefrontPageServiceProductDedupeContractTest`。
- 验证：`build('deals')` → `front_total=1`；清 FPC 后 `/promotion/deals` 仅 1 张商品卡。

## 2026-08-28 活动主题 w_changed + 本地 FPC

- 文档：`doc/event/resource_changed.md`、`event.php`、控制面目录登记 `promotion_activity_theme`；硬规则要求 changed 走 `w_changed`。
- 保存：`saveTheme` 写事务内 `PromotionActivityThemeResourceChangePublisher`；`afterCommit` 调 `PromotionStorefrontCacheInvalidator` 清 FPC/router；CDN 不本地 purge。

## 2026-08-28 预览异步翻页与命中总数

- 条件筛选预览支持 `page` / `page_size` 异步翻页；`filter_limit` 仍为前台最多展示上限。
- 修复摘要「共 条命中」：`__('%{1}')` 无参会吞占位符，改为 `{{total}}` / `{{limit}}`。

## 2026-08-28 刷新预览可见反馈

- 问题：点击「刷新预览」无任何反馈（疑似依赖 `<form>` 选择器静默退出，或错误/空态不可见）。
- 修复：预览脚本改为以页面根节点绑定；失败/未选 Website/接口错误均写可见状态文案 + `Weline.UI.toast`；允许 `website_id=0`；模板 JSON 配置避免二次 HTML escape；清理模板缓存并热重载静态。

## 2026-08-28 website_id=0 默认网站可选品

- 根因：本环境商品在 Website=0「默认网站」（约 26 个），Website 1/2 为空；此前误将 `0` 当成「全站广播」在保存/搜索/JS 预览中拦截。
- 修复：`0` 作为真实站点参与选品、条件筛选预览、绑定与范围精确匹配；文档与单测同步。
- 验证：`previewFilterProducts(website_id=0)` 返回命中商品；相关 Unit 10 通过。

## 文档初始化

- 由 MCP 自动修复流程补齐文档契约。
- 本记录不推断或补造模块历史；后续变更应记录版本、范围与验证证据。


## 2026-08-27 前台活动中心主题化

- 按亚马逊 Today's Deals 原型落地 `/promotion`：导航条、全宽 hero、活动入口、商品货架。
- 样式：`view/statics/css/promotion-hub.css`，复用 Theme `amazon-product-card` / `--amz-*` 视觉语言；**Shell A**：外层 `default.default` 已包 `.w-container`，本页 `padding-inline: 0`、禁止私有 max-width。
- 版心对齐：依赖 Theme `variables/_spacing.css` 的 `--weline-layout-content-*`；`w-frontend-content-grid` 空白 sidebar 不得占栏（Theme `weline-frontend.css` `:blank` 修复）。
- Hero 文字对比度：覆盖 Theme 全局 `h1` 色，Hero 内标题/说明强制 `#fff`。
- 文案约束：不展示虚假折扣；货架仅展示真实可售商品成交价。
- 原型参考：`doc/assets/promotion-hub-amazon-prototype.png`。

## 2026-08-27 前台 scope 生效链路加固

- `PromotionScopeResolver` 优先走 `CartScopeResolverInterface` 解析访客 website / store / channel，避免仅冻结 website 层时漏判店铺/渠道。
- `PromotionStorefrontPageService`：导航 Tab / 入口卡片 / slug 页 / 选品均随当前 scope；移除前台硬编码 deals/sale Tab fallback。
- 后台 `theme/index` 增加 website / store / channel 筛选与可读范围标签；列表按「当前 scope 下会生效」过滤。
- 单测：`PromotionStorefrontScopeContractTest`、`PromotionActivityThemeScopeFilterTest`、`PromotionThemeIndexTemplateContractTest`。

- 新增 `PromotionActivityTheme` / `PromotionActivityThemeLocal`；后台菜单「活动主题」。
- `PromotionScopeResolver` 解析当前 website / store / channel；`PromotionActivityThemeScopeMatcher` 负责匹配与 slug 去重（更具体范围优先）。
- 默认种子 4 个全局主题：deals、sale、weekend、gifts（scope `0/*/*`）。
- 移除 `legacyThemeFallback`：scope 无匹配时 `/promotion/{slug}` 404，避免全站乱投。
- 单测：`PromotionActivityThemeScopeMatcherTest`。
- 2026-08-27 后台范围过滤：运营台与活动主题列表增加 website / store / channel 筛选；创建表单预填范围；前台仍走 `PromotionScopeResolver` + `PromotionActivityThemeScopeMatcher`。

## 2026-09-14
- hi_IN 店面 chrome：路径 locale + WidgetI18n；客服工具栏/活动标/批发信用漏译收口。

## 2026-09-17 — 促销布局底部 slot（promotion-bottom）

- 需求：促销布局底部再设 slot，便于挂载其他内容/部件。
- 改动：`layouts/promotion/default.phtml` 在 `content` 后新增 `<w:slot id="promotion-bottom">`（accept banner/content/product…，挂 `promotion::content-after` / `base::content-after`）；空槽隐藏。
- 契约：UT `PromotionStorefrontLayoutContractTest` 断言 slot；e2e 断言 `[data-wslot=promotion-bottom]`。
- 验收：UT PASS；e2e promotion-layout-shell-plan-suite 3/3 PASS；Browser `/promotion` 可见底部槽。

### 汇审
| 项 | 结果 |
|---|---|
| work_kind | feature（布局扩展点） |
| UT | PASS |
| e2e | 3/3 PASS |
| Browser | `/promotion?weline_no_fpc=1` 见 `data-wslot=promotion-bottom` |
