## 2026-09-12 — 像素供应商 Provider 自报 CSP（PixelEventVendorsCsp）

- 需求：Visitor 对接其他分析供应商时 CSP 须由 Provider 自报（对齐 Captcha/Payment）。
- 实现：`PixelEventVendorInterface::cspDirectives()`；Ga4/Gtm/System 落地；Extends `PixelEventVendorsCsp` 聚合；移除硬编码 `VisitorAnalyticsCsp`。
- 版本：`1.1.21` → `1.1.22`。

## 2026-09-12 — Visitor 分析 CSP 自报（VisitorAnalyticsCsp）

- 需求：GTM/GA 域名由 Visitor 模块 Extends 贡献，不再堆 Framework Defaults。
- 实现：`extends/.../Security/Csp/VisitorAnalyticsCsp.php` + 契约 UT。
- 版本：`1.1.20` → `1.1.21`。

<!-- weline:module-development-baseline:generated-once -->
# Weline_Visitor 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 2026-09-11 — 沙盒缓冲改共享缓存服务（纠偏）

- 纠偏：不做 Worker WLS 进程 L1 / 文件 flock 真源；对齐 `PixelHotBufferService`，accumulate 落 `SharedBufferState` 命名空间 `visitor.pixel.picker_acc`。
- 清理：共享 pack CAS 清空 + `generation` incr；`getLiveEvents` 返回 `changed`/`generation`；前端带 generation 轮询，世代升高则本地归零。
- 顺带 purge 旧 `w_cache(visitor)` 键与 `var/pixel_picker_acc` 遗留文件。

## 2026-09-11 — 沙盒会话清理清不掉

- 根因：accumulate 仅进程内 `w_cache`，多 Worker 下清理与继电不一致；清理后 `since=0` 短轮询又把旧缓冲 merge 回来。
- 改（已被上条纠偏）：初版文件回落已废弃，改为共享缓存服务。
- UT：契约断言 SharedBuffer / generation / changed。
- e2e：`VISITOR-SANDBOX-CLEAR-001`（清理后短轮询不回流）。
- 注：改 PHP 后需 `php bin/w server:reload`。


- 问题：`postRecord`/`handle(persist)` 成功后只回读旧 revision，池已有事件再录入/覆盖 map 时不 bump，店面不重载。
- 改：`EventPicker::bumpRuntimeRevisionMeta`；persist 成功路径强制 `invalidateAfterMutation`；picker `applyRecordRevisionAfterPersist` → `noteConfigRevision`。
- UT：`VisitorTrackingRuntimeReloadContractTest` / `PixelEventDiscoveryContractTest`。


- 问题：自动发现空态写「已打标点击仅对照」；未打标点击无反馈，无法录入。
- 改：`pixel-event-picker.js` 恢复 `suggestCustomName`；simple/chain 对任意可点目标出草稿候选（`source=click|marker`）；捕捉闪示 + outline；空态/hint 纠偏；build `1.1.11-any-click-capture`。
- 门禁不变：仍须改名并「录入自定义」才进池。
- UT：`PixelEventDiscoveryContractTest` 契约更新。


- 运维：本机 `default.default.default` 池 promote+remove+清注解，删除 12 条（`browser_auto_disc_*` / `auto_disc_*` / `e2e_auto_disc_*` / `e2e_sf_auto_*`）；保留 `banner_title` 等手工自定义及 `buy_now`/`add_to_wishlist`。
- 代码：`DiscoveredEventService::isTestResidualAutoDiscoverName` / `purgeTestResiduals`；`EventAnnotationService::removeMeta`；契约 UT。
- `config_revision` → 107。

## 2026-09-11 — 沙盒搜索单开 + 事件链标记/路径回放

- 沙盒：事件名搜索；展开态按 `eventStreamKey` 手风琴单开；点事件栏切换；删除底部「收起参数」。
- 事件链：闭环名注解 `category=chain`（徽章「事件链」）；封链（仅录入也）即 upsert 运行时定义。
- 触发：runtime `eventChains` 用 `buildConfigScopeContext` 取 websiteId（修空链注入）；无打标点击降级 page 路径步；click/track 步支持按 path 回放推进。
- UT：`EventChainServiceTest` / 契约断言；e2e `Weline_Visitor-sandbox-stream-expand.spec.js`。

## 2026-09-11 — 纠偏：沙盒会话流去掉服务端长轮询

- 问题：`getLiveEvents?wait_ms=2500` + 原生 `\usleep` 堵 WLS worker，拖慢同进程 memory RPC。
- 纠偏：店面继电已写入会话缓冲，后台不需要服务端长等；`getLiveEvents` 改为即时 since 读（`mode=short_poll`）；前端 `LIVE_POLL_MS=1000` 短间隔短请求。
- UT：`EventSandboxMonitorContractTest` / `PixelEventDiscoveryContractTest` 禁止 `usleep`/`wait_ms=`。

## 2026-09-11 — 自动发现事件：本地缓存 + 后台锁定

- 闸门：仅沙盒 Monitor/会话开启时扫 DOM + 点击兜底；生产无沙盒不写后台。
- 写：`EventPicker::postRegisterAuto`（经 `mapped?register_auto=1`，`sandbox=1`、名称限长、短窗限流）；字典/泛名跳过；池中已有（含 manual）幂等不改 origin；新名 `auto_discovered`+`deletable=false`，默认 `event_name equals {name}`；`invalidateAfterMutation`。
- 锁：注解 `origin`/`deletable`；`patch` 保锁（除非 `promote=1`）；`DiscoveredEventService::remove` + `postDeleteCustomEvent` 双层拒删（403 `locked_auto_discovered`）。
- 读：全量 `runtime_config` 保持 `private, no-store`；LS key `weline-visitor-custom-events:{scope}`（revision/events/pending_register）；revision 未变不重拉全量。
- UI：列表徽章「自动发现」、禁删；可编辑/搭接。
- 不走 observe/stream 写池。
- e2e：`test/e2e/backend/Weline_Visitor-auto-discover-locked.spec.js`（register-auto 门禁+幂等+后台禁删；店面沙盒声明元素注册）。

## 2026-09-11 — click 全量进沙盒 + 多级点路径

- 问题：自定义命中后吞掉 click 透传；`params_json` 截断导致沙盒「无事件参数」；`elementInfo.text` 多级点未解析。
- 修：每次点击都 `publish` click 流；继电改为 `__sandboxCompactParams` 标量；匹配/组装支持点路径与 `elementInfo`。
- 验收：数据流可见 click（流过）；自定义 banner 仍有参数；条件可用 `elementInfo.text`。

## 2026-09-11 — 自定义事件参数多选映射与组装

- 需求：源参数可勾选多选；可自取产出名；用 `{源名}` 模板组装；保留「复制源事件中的参数」。
- 实现：`param_mappings`（name/from）；后台 chip 多选 + 映射表；pixel 先全量复制再映射覆盖；兼容旧 `extract_params` 列表。
- 验收：勾选/组装保存后命中 payload 含改名与拼接字段。

## 2026-09-11 — 自定义事件从源事件提取参数

- 问题：创建弹窗有「复制源事件中的参数」，命中自定义事件时未写入源参数。
- 修：注解增加 `extract_params` 白名单；pixel 命中时按 copy_params/extract_params 从源上下文提取标量参数进 `track`/`params`；`normalizeExtractParams` 三元加括号修复 PHP 编译错误。
- 验收：白名单 `banner` 命中仅带 className/text/href/page_path；`banner_title` 全量复制源标量。
- 注：已由「参数多选映射与组装」取代单框白名单交互。

## 2026-09-11 — 创建自定义事件后前台即时条件命中

- 问题：后台 `default.default.default` 新建事件后，前台 runtime 仍读空/`website.{id}` 范围，自定义池缺条件；点击只透传 `click`，不按 `match_conditions` 命中。
- 修：`resolveStorageScopeForRuntime` 对齐三段码；runtime 热更带 `storage_scope`；pixel 按条件 AND 求值后 `track` 自定义名（`custom_match`）。
- 验收：创建/保存 → Broadcast/revision 拉新池 → 点匹配元素即蓝自定义命中。

## 2026-09-11 — 配置版本展示含站点/店铺/渠道

- 文案统一：`站点 {code} · 店铺 {code|default} · 渠道 {code|default} · 版本 {N}`。
- 后台顶栏与保存提示、沙盒监视/助手同源展示；runtime 增加 `configScope`；店面 `__WelinePixelEnv` 注入 store/channel。
- 缓存戳 `monitor9`。

## 2026-09-11 — 后台/沙盒/助手展示配置版本

- 后台 TrackingVendor 顶栏常显「配置版本 N」；自动保存/创建/整页保存成功提示带版本。
- 事件监视（沙盒流/监听助手同源）标题与 meta 展示 `vN` /「配置版本 N」，监听 `weline:visitor-tracking-config-applied` 即时刷新。
- 缓存戳 `monitor8`。

## 2026-09-11 — 事件编辑版本标记 + 沙盒自行重载

- 流程：实时编辑事件 → `invalidateAfterMutation` 清缓存并 bump `configRevision` → 店面既有提交响应带回版本 → 沙盒 `noteConfigRevision` 见变大 → 拉 runtime + `rebuildVendorFrames` 更新浏览器内事件数据。
- 与「发布」无关；无定时轮询/长连接；本机 BroadcastChannel 仅辅助后台预览。

## 2026-09-11 — 线上发布后用户侧事件配置更新路径

- 场景：生产后台改事件，终端用户如何拿到新配置（非本机 BroadcastChannel）。
- 主路径：`invalidateAfterMutation` bump revision **并清店面 FPC** → 用户**下次进页 SSR** 注入新 `vendors/customEvents`。
- 辅路径（长停留同页）：既有沙盒继电响应顺带 `config_revision`，仅变大时拉全量；标签可见时只探 `revision_only=1`（`max-age=15`），禁止长连/定时轮询。

## 2026-09-11 — 去掉 tracking runtime 定时轮询

- 纠偏：15s `setInterval` 请求过多且不宜长连接；改为后台保存 `BroadcastChannel`/`localStorage` 推送 + 标签 `visibilitychange`/`pageshow` 按需单次对齐；整页 SSR 仍注入最新配置。

## 2026-09-11 — 后台编辑事件后即时清缓存并重载沙盒配置

- 问题：后台改供应商/自定义事件/注解后，店面 SSR 配置与沙盒 iframe 不刷新，助手 hit_kind 不准。
- 修：`VisitorTrackingConfig` bump `configRevision` + runtime 注入 `customEvents`；写路径（注解/发现池/事件链/供应商保存）失效；`GET .../event-picker/mapped?runtime_config=1`；后台 `BroadcastChannel`/`localStorage` 通知；`pixel.js`/`pixel.phtml` fetch 后 `rebuildVendorFrames`。
- UT：`VisitorTrackingRuntimeReloadContractTest`。

## 2026-09-11 — 本会话沙盒打洞实时读 + 清理重听

- 审图：`web_ui` / `ui_shot`；红箭头指向右栏沙盒；列表仍见 `pixel_db` 冒充会话流；缺「清理」。
- 纠偏：默认 `sandbox_only=1` 只读 TokenService 继电缓冲；`getLiveEvents` since 长轮询打洞；`postClearSessionSandbox` + 模板「清理」清空缓冲/sessionStorage 后继续监听。
- 根因补丁：本机 `website_id=0` 时 pixel 继电曾跳过、`postStream` 曾拒 `<1`，已放行 `>=0`。
- 验收：前台 click/emit → 后台数据流近乎即时出现；清理后列表空并可重新监听。

## 2026-09-11 — 监视器：上一页 + 累积链 Tab

- Tab 并排：系统命中 / 自定义命中 / 上一页 / 累积链 / 数据流。
- 上一页：复用 previous 桶展示；累积链：跨页保存 chain 事件流水（`CHAIN_KEY`）。
- 数据流内「高级事件链」进度区块保留不动。
- 缓存戳 `monitor7`。

## 2026-09-11 — 复制后就近提示「已复制」

- Monitor：`flashCopied` 在点击按钮旁弹出绿色 tip「已复制」，约 1s 后淡出；缓存戳 `monitor6`。

## 2026-09-11 — 事件监视：仅事件栏展开/收起

- 审图 ui_shot：展开参数区双击选中被整行 click 收起拦截。
- 原型：expand 热区收口到 `wesm-row__bar`；参数区 `user-select:text`，点击不 toggle。
- 缓存戳 `monitor5`。

## 2026-09-11 — 事件监视参数名/值可点复制

- 数据流参数由 JSON `<pre>` 改为 KV 行；点参数名复制名、点值复制值（clipboard + fallback）；不触发行展开。
- 缓存戳 `monitor4`。

## 2026-09-11 — 沙盒全量流继电到后台会话数据流

- 问题：前台点击透传只进 Monitor，后台「本会话沙盒」无 click。
- 修：`EventPicker::postStream`（经 `observe?sandbox_stream=1` 复用已注册路由）+ `WelineEventSandbox.relayToAdminSession`；透传记「流过」不虚增自定义命中。

## 2026-09-11 — 创建弹窗浮起整块本会话沙盒（实心）

- 审图：`web_ui` / `ui_shot`；另造 companion 半透明叠字，不像浮起真沙盒。
- 纠偏：删除 companion；`aside.tvp-map-stream[data-float=1]` 固定层 + 实心 `--weline-color-surface` + z-index 1300。
- 展开快拷参数仍在真列表；「收起」仅取消浮层。

## 2026-09-11 — 创建弹窗联动会话沙盒快拷参数

- 需求：创建/编辑事件弹窗打开时同步弹出会话沙盒侧栏；流事件可展开，一键复制参数名 / 值 / 键值。
- 实现：`tv-sandbox-companion` + `showSandboxCompanion`；`copyableParamsHtml` / `data-acc-expand`；对话框 `data-with-sandbox` 让位右侧。
- UT：companion / copy / expand 契约断言。

## 2026-09-11 — 沙盒全量流 + 系统/自定义命中 Tab

- 需求：沙盒流全部记入会话并携带参数；系统命中绿、自定义命中蓝；顶部两 Tab 汇总命中与个数；数据流 Tab 持续监控。
- 后端：`pushAccumulate` 持久 `params`/`hit_kind`；`getRecentEvents` 全量合并、解析 `browser_info`、标注 `hit_kind`、返回 `hit_summary`。
- 后台右栏：三 Tab「系统命中 / 自定义命中 / 数据流」+ `sessionStorage` 会话合并。
- 前台 Monitor：同样三 Tab；缓存戳 `monitor3`。
- UT：`testPickerAccumulateStoresParams` + 模板/Monitor 契约断言。

## 2026-09-11 — 会话沙盒右栏数据流 + 子 Tab 主题按钮

- 审图：`web_ui` / `ui_shot`；红箭头要把「本会话沙盒」挪到右侧常驻；子 Tab 裸 button 未走主题。
- 原型：双栏 `subtabs-stream`（主列三分 Tab + 右栏 sticky 数据流）；子 Tab / 沙盒卡片动作改 `w-button` outline+neutral。
- UT：断言 `subtabs-stream` / `tvp-map-stream`，禁止 `data-map-subtab="sandbox"`。

## 2026-09-11 — 事件搭接子 Tab + 自定义事件搜编删

- 审图：`web_ui` / `ui_shot`；红框标在范围提示下空白，要求拆子 tab。
- 线稿：主 tab「事件搭接」下叠录入+自定义池+搭接表+沙盒 → 密度过高。
- 原型 Variant A：子 tab「自定义事件 / 事件搭接 / 录入与拾取 / 本会话沙盒」；自定义池加搜索与编辑（复用 GA4 弹窗 + postAddCustomEvent upsert），删除保留。
- 契约 UT 增断言 `tv-map-subtabs` / `tv-custom-search` / `data-custom-edit`。

## 2026-09-11 — 事件监视：系统命中绿 / 自定义命中蓝

- 沙盒 envelope 增 `hit_kind`（system|custom）；字典/生命周期=系统，自定义池/非字典搭接=自定义。
- Monitor：`hit-system`→`--weline-color-success`，`hit-custom`→`--weline-color-info`；异常仍主题色。

## 2026-09-11 — 创建事件匹配条件参数可选可填

- 审图：`web_ui` / `ui_shot`；「参数」原为只读 select。
- 原型：`input[list=datalist]` combobox；预设 + 手写（如 `className`）。
- 后端：`normalizeMatchConditions` 放开非预设参数（保留大小写、消毒）；预设增 `className`/`text`/`href`/`tag`。

## 2026-09-11 — Monitor 对齐「沙盒全量流 + 自定义命中呈现」

- 纠偏：监视器只记录流经沙盒的 envelope；是否自定义事件是提取/搭接逻辑，命中则绿色呈现。
- `publish` 内 `emit` 通知订阅者（观察透传）；去掉 `__emitVisitorPixelEvent` 旁路重复 emit。
- 无业务 track 的点击走 `publish` 透传（`__publishSandboxClickPassthrough`），不另造 Monitor 专用监听。
- `__sandboxIsCustomEventHit`：event_map / mapping / 自定义池 → `event_hit`。

## 2026-09-11 — Monitor 全量点击透传

- 问题：随便点非链接/无像素标记元素时，行为遥测不 `track`，监视器无流。
- 修：`__emitSandboxClickPassthrough`；click 处理器在未业务 track 时仍 `Sandbox.emit`；排除监视器自身 DOM。
- `pixel.js` / `pixel.phtml` 双改。

## 2026-09-11 — 事件沙盒 Monitor 重构（全量流观察器）

- 主线：Monitor 观察流经沙盒的全量数据流（含点击）；命中事件绿色、异常主题色；不改透传架构。
- `WelineEventSandbox`（`WelinePixelSandbox` 别名）：`emit` / `subscribe` / `normalizeEnvelope` / lifecycle 桥接；`monitor` 委托。
- 新脚本 `event-sandbox-monitor.js`；面板按钮「开启/关闭事件监视」；bootstrap/body-end 续载新 session key。
- `lifecycle-event-assistant.js` 薄 re-export 一周期；契约 UT `EventSandboxMonitorContractTest`。
- 后台「本会话沙盒」仍独立，不与前台 Monitor 合并。

## 2026-09-11 — 结账小助手标题与字段同步收口（1.1.20）

- 问题：结账表单已同步 amount/currency，横幅为「待下单」，但标题仍写「当前缺失」；旧脚本缓存易漂移。
- 修：`render` 每次重算 `awaitingOrder`；标题 `data-wla-awaiting`；`fields-ready` 刷新；脚本 `lifecycle-assistant10`。
- 版本：`1.1.19` → `1.1.20`。

## 2026-09-11 — 事件搭接子 Tab + 自定义事件搜编删

- 审图：`web_ui` / `ui_shot`；红框标在范围提示下空白，要求拆子 tab。
- 线稿：主 tab「事件搭接」下叠录入+自定义池+搭接表+沙盒 → 密度过高。
- 原型 Variant A：子 tab「自定义事件 / 事件搭接 / 录入与拾取 / 本会话沙盒」；自定义池加搜索与编辑（复用 GA4 弹窗 + postAddCustomEvent upsert），删除保留。
- 契约 UT 增断言 `tv-map-subtabs` / `tv-custom-search` / `data-custom-edit`。

## 2026-09-11 — 小助手按页面上下文探针（1.1.19）

- 审图：`web_ui` / `ui_shot`（后台事件搭接页）；浮层 `other` 仍渲染结账 4 步链并红标「当前缺失：订单已创建」。
- 线稿：主列事件录入 + 右下浮层误套结账漏斗。
- 原型 Variant A：`isLifecycleSurface` 两态——结账/支付保留链；其它面改「本页事件」探针（触发列表 / 参数缺失 / 异常），订阅 `weline:visitor:event`。
- 版本：`1.1.18` → `1.1.19`；脚本 `lifecycle-assistant9`。

## 2026-09-11 — 结账页生命周期提示补齐（1.1.18）

- 审图：`web_ui` / `error_shot`；结账表单被误报「缺 order_uuid / amount / currency · 字段不全无法派发」。
- 修：`pageFieldSnapshot` 回退读 grand-total/currency；下单前 `awaiting_order` 不把订单号当异常；横幅/事件链/字段格子补齐「待下单 / ✓✗…」提示。
- 版本：`1.1.17` → `1.1.18`；脚本 `lifecycle-assistant7`。

## 2026-09-11 — 创建事件改弹窗（降复杂度）

- 主搭接页 intake 精简为「创建事件 / 拾取 / 更多」工具条；GA4 条件面板迁入 `w-dialog`（`#tv-ga4-create-dialog`）。
- `focusCreateEvent` → `Weline.UI.dialog.open`；自定义创建成功后 `close`。
- 自定义池与沙盒带入共用同一弹窗；契约 UT 断言 dialog 入口。

## 2026-09-10 — 生命周期小助手可关闭清理 + 拖动记位（1.1.16）

- 浮层标题栏可拖动；位置写入 `POSITION_KEY`，刷新恢复。
- 右上角 × 调用 `disable()`：清开关 / 事件历史 / 位置并卸载。
- 版本：`1.1.15` → `1.1.16`。

## 2026-09-10 — 生命周期小助手仅保留本页+上一页事件（1.1.15）

- 事件写入 `HISTORY_KEY` 两槽窗口：current / previous；路径变化时 current→previous，更早删除。
- UI：本页事件链/字段/触发/异常 + 上一页事件区；页脚文案对齐。
- 版本：`1.1.14` → `1.1.15`。

## 2026-09-10 — 创建事件对齐 GA4 匹配条件面板

- 对照 Google：自定义事件名 + 匹配条件（参数 / 运算符 / 值），可选复制源参数。
- UI：匹配类型预设「事件名 / URL / 自定义」一键填条件行；条件可增删。
- 持久化：`EventAnnotationService.match_conditions` + `match_type`；列表展示条件摘要。
- 本回合只落规则与创建面板；店面运行时条件评估后续再接。

## 2026-09-10 — 补齐「创建事件」闭环（审图跟进）

- 问题：占位 `add_to_cart` 是系统字典事件，`listCustomEvents` 会过滤，点录入像「没创建」；且新接口 `postAddCustomEvent` 需 `setup:upgrade --route` + `server:reload` 才挂上。
- 修：主 CTA「创建事件」；占位改 `vip_apply`；接口返回 `is_custom`；系统名明确提示；自定义池旁与沙盒可带入创建；路由别名 `add-custom-event`。
- 验收：创建 `vip_apply_ok3` → 本范围自定义列表 count=2（含 page_exit）。

## 2026-09-10 — 事件搭接 Tab：录入区 + GA4 式 + 沙盒参数（审图）

- 分流：`web_ui` / `ui_shot`（事件供应商 · 事件搭接乱页）。
- 线稿：顶栏范围长提示 × 黄底 callout × 三按钮平铺 × 长说明 → 主列挤乱；右侧累计只甩整段 URL。
- 原型 Variant A（落地）：**录入区**（拾取主 CTA + GA4 事件名表单）→ 自定义池 → 搭接表；右侧改 **本会话沙盒**（解析 query / UTM / 渠道线索）；范围/渠道只读摘要，写入口仍页顶 `w:scope`。
- 实现：`postAddCustomEvent`；`index.phtml` + `tracking-vendor-admin.css`；契约 UT 断言。
- 需求纠偏：不做第二套渠道编辑器；沙盒参数前端解析，不另起规则引擎。

## 2026-09-10 — 小助手续载改走面板全局注入（1.1.14）

- 根因：结账/首页 HTML 无 `body-end` 的 `data-wla-bootstrap`，刷新后会话在但脚本不加载，浮层「掉了」。
- 修：`VisitorPanelBootstrapHtmlService` 与访问事件 Tab 同次注入 `data-wla-bootstrap`；`mount` 等 body；body-end 保留双保险。
- 版本：`1.1.13` → `1.1.14`。

## 2026-09-10 — 生命周期小助手跨页续活 / 关面板清除（1.1.13）

- 开启写入 `sessionStorage` + cookie；`body-end` bootstrap 跨页续载 `lifecycle-event-assistant.js`。
- 关闭 Weline 面板（toggle/closePanel：展开→折叠）或面板内关小助手 → 清除会话与浮层。
- 版本：`1.1.12` → `1.1.13`。

## 2026-09-10 — 像素事件列表表格横向滚动 / URL 限宽

- 问题：热表明细 `list` 起初无法横滚；改为 `max-content` 后长 URL 又把表撑得过宽。
- 修复：`list.phtml` / `archive_list.phtml` 保留 `overflow-x:auto` + `min-width:52rem`（窄屏可滚）；URL 列 `weline-pixel-list-url` `max-width:18rem` + `data-truncate`，`title` 看全文。
- 验收：UT 契约断言；Browser 确认 URL 截断且不再被长链撑开。

## 2026-09-10 — 生命周期事件小助手（1.1.12）

- 「访问事件」工具栏：开启/关闭生命周期小助手；异步加载 `lifecycle-event-assistant.js` 浮层。
- 浮层展示事件链进度、已触发事件、派发 anomaly、三页缺订单字段异常；仅面板可关。
- 版本：`1.1.11` → `1.1.12`。

## 2026-09-10 — 热表明细筛选：站点溢出与分区收口

- 审图 `web_ui`/`ui_shot`：站点 Taglib 选中态压住「事件」输入；筛选项过密。
- `list.phtml` / `archive_list.phtml`：主筛分区 + 站点 `overflow` 截断；UTM 折入「高级归因」；事件名精确提示。
- UT：`PixelDashboardListShellContractTest` 断言布局契约。

## 2026-09-10 — 删除确认改主题弹窗（禁原生 confirm）

- MCP/开发标准已禁止原生 `alert`/`confirm`/`prompt`；搭接页删除仍用 `window.confirm`，现改 `Weline.UI.dialog.confirm`（danger），失败走 toast。
- 拾取重复录入确认同步 Promise 化；build `1.1.10-theme-confirm`。
- 硬约束 `no_generated_no_routes_xml` 摘要补全 prompt + 首选 API。
- 契约：`PixelEventDiscoveryContractTest` 断言无 `window.confirm(`。

## 2026-09-10 — 拾取选值模式（类 FB 点元素取值）

- 自定义录入旁「选择值」：进入选值模式后点页面元素取值，capture 阻断其它事件记录（发现/链/track 挂钩）。
- 取值回填预览；录入时附带 `value` + `params_json`（selector/content）。
- build：`1.1.9-value-pick`。

## 2026-09-10 — 事件标星 / 分类 / 有值标记

- 本范围自定义事件与会话累计：可标星（置顶）、选分类、自动/观测「有值」。
- `EventAnnotationService` 按 `storage_scope` 持久化注解；`postAnnotateEvent` JSON。
- 字典 `required_params` 或运行时 value/params → `has_value`；筛选：全部/星标/有值。

## 2026-09-10 — 各 Tab 自动保存 + Tab 状态持久化

- 痛点：加入搭接只改 DOM，须点「保存」才落库；保存后整页跳转回到「基本配置」，像映射没存上。
- `postAutosave` JSON 落库（复用 `saveCustom`），不 redirect；前端 debounce 自动保存，状态「已自动保存 · 映射 N 条」。
- Tab：`sessionStorage`（`tvp_ui_tab_v1`）+ URL `?tab=`；手动保存 redirect 带 `tab`；刷新仍停在事件搭接等当前 Tab。

## 2026-09-10 — 自定义事件本范围可删（非只累积）

- 需求：本范围自定义池可删除，避免跨主题/网站一直累积。
- `DiscoveredEventService::remove`：只改当前 `storage_scope`；网站级同步清 legacy key，防 `list()` 回落。
- 后台：`postDeleteCustomEvent` + 搭接 Tab 每行「删除」（confirm：搭接表行不自动删）。
- 文案：「可加入搭接；不用的请删除（仅当前范围，不会一直累积）」。
- UT：契约断言 `remove` / `data-custom-del` / `deleteCustomEvent`。

## 2026-09-10 — 闭环事件：仅客户端按序凑齐后入库

- 产品：链进度只放客户 localStorage；`complete_event`（如 `all_add_cart`）禁止直接 track 入库。
- 前端：`pixel-event-chains.js` 对已注册闭环名无 `__event_chain_complete` 则跳过发送。
- 后端：`PixelEventService::track` 对已发布 `complete_event` 无链标记返回 `skipped=chain_complete_required`。
- UT：`EventChainServiceTest` 标记检测 + 源码门禁断言。

## 2026-09-10 — 说明：all_add_cart 须按事件链测，不可单次 track 冒充

- `all_add_cart` = 链 `chain_all_add_cart` 的 `complete_event`（12 步严格按序）。
- 正确测法：按 steps 顺序喂 `page/track/click/input` 信号（`WelineEventChains.onSignal` 或真实跨页操作），凑齐后自动 `track(all_add_cart)` 且带 `__event_chain_complete`。
- 已验收：按序回放 12 步 → 闭环触发；入库 `w_pixel` id=28574（`name=chain_all_add_cart_complete`）。
- 注意：当前链含 `site_error` 与 `click/input` 步；运行时默认只自动吃 `page`+`track`，真实访客可能卡在 click/input，宜重录精简链。

## 2026-09-10 — 验收：自定义收录事件前端可触发并入库

- 前端 `Weline.Api.resource('visitor').trackPixel` / `WelinePixel.track` 可接收字典外自定义事件名。
- 实测入库 `w_pixel`：`custom_browser_verify_event`、`all_add_cart`、`page_exit`、`custom_div_slide_*`、`persist_probe_*` 等；后台像素事件列表可见。
- 说明：Web 运行时可能先入热缓冲再落库（约数秒）；`buffered:true` 不代表失败。

## 2026-09-10 — 修复：自定义搭接保存后刷新丢失

- 根因：`use_default_map=1` 时 `index` 触发 `syncModuleProviders` 用默认 map 覆盖刚保存的自定义搭接。
- 改：`saveCustom` 写入非默认 map 时强制 `use_default_map=0`；勾选默认则恢复供应商默认 map；sync 若误标默认但含自定义键则保留并关默认。
- 前端：改/加/删搭接行自动取消「使用默认 map」；提示文案；拾取录入合并 map 也关默认。
- 另：已保存自定义 key 若不在 mappable 下拉，刷新后 option 缺失看起来像没保存；`mergeEventMapKeysIntoMappable` + 行级兜底 option。
- UT：`PixelEventVendorContractTest` 门禁断言。

## 2026-09-10 — 自定义池门禁：命名录入 / 链封录才进池

- 需求：拾取须输入名字再录入，或高级事件链仅录入/发布，才进自定义池；自动发现/observe 不写池。
- 后端：`EventPicker`（Analytics + REST）仅 `$persistMap`（record）时 `DiscoveredEventService::add`。
- 前端：底部「录入自定义」、链「仅录入/发布」改 `submitRecord`；文案对齐门禁。
- UT：契约断言门禁注释与 `persistMap` 包裹 `discovered->add`。

## 2026-09-10 — 修复供应商保存误报 302

- 根因：`postSave`/`postCreate` 的 `catch (\\Throwable)` 吞掉 `ResponseTerminateException`（redirect 正常终止）。
- 改：重抛 `ResponseTerminateException`，业务异常才显示「保存失败」。

## 2026-09-10 — 自定义事件按配置范围隔离 + 提示

- `DiscoveredEventService` 按 `storage_scope` 存池，禁止跨网站/店/渠共用；拾取 token 携带范围。
- 搭接 Tab：「本范围自定义事件」列表可「加入搭接」；黄底提示主题/网站变更需复核。
- 顶栏配置范围增加「自定义事件不继承」提示。

## 2026-09-10 — 拾取跨页续活（session/cookie）

- 根因：脚本仅认 URL query，跳转丢参后浮层整层卸载（链 sessionStorage 仍在但看不见）。
- 修复：`wpp_session_v1` + cookie 保活；body-end 轻量 bootstrap 跨页续载；同域链接改写；模式/步骤跨页恢复。
- Browser：无 query 的 `/search` 仍 `hasRoot` + chain 步骤续录。

## 2026-09-10 — 拾取：已录入查看 + 重复录入提示

- 浮层新增「已录入」Tab；`POST/GET .../event-picker/mapped` 返回当前供应商 event_map（自定义优先）。
- 录入重复返回 `error=duplicate`；前端 `confirmDuplicateRecord`，确认后 `force=1` 覆盖。
- build：`1.1.4-mapped-dup`；契约测试断言 mapped/duplicate/force。

## 2026-09-10 — 纠偏：自定义事件≠点击即记

- 需求纠偏：自定义事件 = 前端像素自动 `track` 成功；禁止任意点击 invent `custom_*`；仅「高级事件链」内允许点击入步
- 改：`pixel-event-picker.js` 简易模式改「自动发现」；hookTrack 标 `source=track`；删除 `suggestCustomName`
- UT：`PixelEventDiscoveryContractTest` 断言新文案与无 suggestCustomName

## 2026-09-10 — 修复 tracking-vendor 缺 backend 段 404

- 根因：`redirect('*/tracking-vendor/index')` 中 `*` 只展开为 module router `visitor`，丢掉 `Controller/Backend` 路径段，生成 `/visitor/tracking-vendor/index` → 404
- 修复：redirect 改为完整 `visitor/backend/tracking-vendor/index`；范围切换 JS 纠偏错误 pathname
- UT：`testTrackingVendorRedirectsKeepBackendAreaSegment`
- 版本：`1.1.6` → `1.1.7`

## 2026-09-10 — 配置范围左上 + 继承制度

- 审图：`ui_shot` 红箭头：范围控件在右 → 应左上
- 原型：`tvp-topbar` 左 `<w:scope>` / 右同步·新建；继承文案 channel←store←website
- MCP：`weline_business_scope_hierarchy` 补充 SystemConfig 式继承回退
- 版本：Visitor `1.1.6`；Ai `1.2.56`

## 2026-09-10 — 配置范围=站店渠；路径过滤改名；MCP 收录

- 审图：`ui_shot` 范围 Tab 仅路径 `*` → 语义错位（范围≠路径）
- 产品：页顶 `<w:scope>`（Website→Store→Channel）；Tab 上段「配置范围」下段「路径过滤」；`scope_json` 仍只路径+areas
- Controller：`SystemConfigTargetScopeService` 解析 `target_scope`；供应商行仍按 `website_id`
- MCP：`weline_business_scope_hierarchy` + AI硬规则索引路由行
- UT：`TrackingVendorAdminUiContractTest::testWorkScopeToolbarUsesOfficialScopeTaglib`
- 版本：Visitor `1.1.5`；Ai `1.2.55`

## 2026-09-10 — 系统像素默认启用且不可关闭

- 用户澄清：默认就是启用，不能关（纠正上一回合误做的 opt-in）
- 种子：`legacyEnabledSeed(weline)=true`；`saveCustom`/`syncModuleProviders` 强制 `ENABLED=1`
- UI：系统像素勾选 locked + hidden `enabled=1`；提示「系统像素默认启用且不可关闭」
- UT：`testSystemVendorAlwaysEnabledAndLocked`

## 2026-09-10 — 系统像素默认停用（opt-in）（已撤销）

- 审图：`ui_shot` 箭头指向「启用此供应商」默认勾选 → 人性化 fail（高风险能力不可默认开）
- 线稿：侧栏「模块 · 启用」与勾选态同读 `enabled`；种子错默认导致双端一致错误
- 原型调整：默认 unchecked + 侧栏「停用」+ 字段提示「默认停用，需手动开启」
- 根因：`PixelEventVendorManager::legacyEnabledSeed` 对 `weline`/`CODE_SYSTEM` 硬编码 `true`
- 修复：种子改为 `false`；sync 更新路径仍不覆写用户已保存 `enabled`
- UT：`PixelEventVendorContractTest::testSystemVendorLegacyEnabledSeedDefaultsOff`；UI 契约含启用提示
- 定稿合同：系统供应商由「默认同步启用」改为「默认同步停用（opt-in）」

## 2026-09-10 — 修复自定义录入「失败」

- 根因：站点维护模式 503，前端只认 `ok` 一律显示「失败」
- 改：`maintenance:disable`；前端统一 success/ok 与可读错误；后端 map 写失败时 discovered 仍算成功降级
- 审图：`error_shot`（录入失败按钮）

## 2026-09-10 — 拾取纠偏：FB 式点击发现

- 需求纠偏：任意点击都可进列表；系统字典/已搭接只展示「已默认无需重新录入」；只录自定义 + 高级事件链
- 改：`isSystemOwned`；点击发现模式；无标记点击生成 `custom_*`；按钮「录入自定义」
- 文案：空态/提示不再写「仅高亮」

## 2026-09-10 — 拾取：后台已搭接不再催录入

- 审图：`add_to_cart` 已在 map 仍显示「录入搭接」×2
- 改：读 runtime `vendors.event_map`；已映射显示「已默认无需重新录入」；同名去重
- UT：断言文案 `已默认无需重新录入`

## 2026-09-10 — 拾取浮层「高级事件链」入口可见性

- 审图：用户截图仍为旧简易浮层（无模式切换）；强化「简易拾取 | 高级事件链」分段 + 简易态引导 CTA；脚本 `?v=1.1.4-mtime` 破缓存
- UT：`PixelEventDiscoveryContractTest` 断言「高级事件链」文案

## 2026-09-10 — 高级有序事件链（跨页 + 闭环总事件）

- `EventChainService`：website 级 `{version,chains}`；`complete_event` 为凑齐后上报的闭环总事件
- runtime：`VisitorTrackingConfig.eventChains`；前台 `pixel-event-chains.js` 按 version 清 localStorage，有序匹配跨页进度
- 拾取器：「发布事件链」写定义；后台「发布演示跨页链」
- 版本：`1.1.3` → `1.1.4`
- UT：`EventChainServiceTest`

## 2026-09-10 — 事件拾取器：操作链 + 浮层美化

- 简易 / 操作链双模式；链步骤 sessionStorage 跨页保留
- 封链：`kind=chain` + `chain_json` → 累计/录入；后台累计显示「链」徽标
- 浮层改版：模式切换、步骤时间线、主色 token（`--weline-color-*`）
- 版本：`1.1.2` → `1.1.3`

## 2026-09-10 — 像素默认事件对齐 + 运营发现（累计/拾取）

- 需求：字典固定事件 vs 运营把登录写成 `cta_click`；需自定义扩展与发现工具
- P0：`event_dictionary.json` v1.1.0 补 `lead_submit` + 全量 `label_zh`；Vendor 默认 map `login`/`register`；同步时 `use_default_map` 刷新并 remap `*_success`
- P1：搭接页「本会话累计」轮询 + 链到像素事件列表；`filterMappableMap` 允许字典外自定义 key
- P2：`EventPickerTokenService` + 前台 `pixel-event-picker.js` 浮层；`EventPicker` observe/record；`DiscoveredEventService` 网站级已发现池
- 版本：`1.1.1` → `1.1.2`
- UT：`PixelEventDiscoveryContractTest`

## 2026-09-10 — 主题切换钉到编辑器右上并压丑按钮

- 审图：用户圈右上角，实则白底按钮落在左上（绝对定位未吃到 / 后台 button 样式覆盖）
- 改：float 挂入 `CodeMirror` wrapper；`top/right` + 高特异性暗色胶囊分段


- 审图：`web_ui` / `ui_shot`（用户怒批工具条下拉「垃圾样式」，圈编辑器右上）
- 线稿：外置 select → 编辑器 stage 右上悬浮墨夜/深渊；悬停/聚焦显示
- 改：去掉 `暗色`+select；`tvp-js-editor__float` 分段按钮；两编辑器同步


- 审图：`web_ui` / `ui_shot`（脚本区纯 textarea）
- 线稿：双 textarea → 工具条（格式化 + 两暗色）+ CodeMirror
- 原型调整：仅高亮/格式化/墨夜·深渊；不做 lint/补全
- 落地：vendored CodeMirror5 + js-beautify；`tracking-vendor-js-editor.js`
- UT：契约断言编辑器挂载标记


- 扩展点：`SystemVendor`（code=`weline`）模块源，默认同步启用且不可关闭
- 字段：`scope_json`（path_include / path_exclude / areas）
- 后台：详情新增「范围」Tab；列表系统供应商置顶
- runtime：`__vendorScopeMatches` 过滤扇出
- 版本：`1.1.0` → `1.1.1`（需 `setup:upgrade` 加列）
- UT：`PixelEventVendorScopeTest` + `PixelEventVendorContractTest`


- 分流：`web_ui` / `ui_shot`（用户圈右侧空白「空着做什么」）
- 线稿：内容根 `max-inline-size:72rem` 人为截断 → 详情栏右侧留死白
- 原型调整：不造第三栏；根容器 `inline-size:100%` + 详情 `minmax(0,1fr)` 吃满后台内容槽
- 改码：`tracking-vendor-admin.css`；UT 断言 `max-inline-size: none`
- 验收：`visitor/backend/tracking-vendor/index`


- 分流：`web_ui` / `ui_shot`（用户附图 +「很乱」）
- 线稿：顶栏赘文+双主 CTA；DEV 条抢视线；详情头 meta/模式说明重复；表单稀疏散落
- 原型调整：右对齐单主 CTA（新建 primary / 同步 outline）；DEV 一行可展开；详情仅标题+状态徽章；模式 hint 合并一行；凭证分区；底栏保存
- 改码：`TrackingVendor/index.phtml` + 新 `tracking-vendor-admin.css`（token only）
- UT：`PixelEventVendorContractTest` 6/6
- 验收：`visitor/backend/tracking-vendor/index`


- 分流：`web_ui` / `ui_shot`（用户「原型和UI都看不出来么」→ 正式页仍可见双标题、挡路说明、双标签）
- 线稿：壳标题 + 内容 h1 重复；mode-hint 在 Tab 之上；启用外层+内层双 label
- 改：工具条无 h1；mode-hint 进 config；`tvp-check` 单标签；原型同步
- UT：`TrackingVendorAdminUiContractTest` 3/3
- 验收：`visitor/backend/tracking-vendor/index`

## 2026-09-10 — 审图疏解 variant=2 信息密度

- 分流：`web_ui` / `ui_shot`（用户附图 +「密密麻麻的」→ 非报错改 UI）
- 线稿：顶栏长副标 + 长 DEV 条 + 左轨芯片汤 + 详情模式长文 + 底 JSON 同屏抢视觉
- 原型调整：左轨改名/单行 meta；DEV 与模式说明 `<details>`；状态 JSON 默认折叠
- 改码：`TrackingVendorPrototype/index.phtml` + `tracking-vendor-prototype.css`；正式页 `TrackingVendor/index.phtml` 同步疏解
- Browser：正式页可见短副标 + 可折叠 DEV 条；UT 契约通过
- 验收：`visitor/backend/tracking-vendor/index`（正式）/ 原型 `?variant=2&vendor=custom_demo`

## 2026-09-10 — 事件供应商壳 Phase2（选定变体2）

- 当前状态：`implemented`
- 用户选定 UI **方式2（左列表右详情）**
- 落地：`PixelEventVendorInterface` + extends + Ga4/Gtm；`pixel_event_vendor` Model；Scanner/Manager；后台 `TrackingVendor`；runtime `vendors[]` + `__fanoutPixelVendors`
- 版本：`1.0.9` → `1.1.0`
- UT：`PixelEventVendorContractTest` OK
- 需 `setup:upgrade` 建表后正式同步可用

## 2026-09-10 — 事件供应商管理 UI 原型（Phase1）

- 当前状态：`prototype_ready`
- 交付：`Controller/Backend/TrackingVendorPrototype` + 三变体模板 + CSS；假数据覆盖模块 ga4/gtm 与自定义；DEV 不上报 / 默认 sandbox / 搭接 / 沙盒 JS
- 合同：`doc/像素事件供应商管理-定稿合同.md`
- UT：`TrackingVendorPrototypeContractTest` OK
- 验收 URL：`visitor/backend/tracking-vendor-prototype/index?variant=1|2|3`
- Phase2（扩展点/落库/runtime）待用户确认 UI 变体后另开

## 2026-09-08 — statistics/website 空日期 SQL + HTTP 200/业务 500（1.0.9）

- 审图分流：附图 = `web_ui`（`/dev/tool/docs/api` 在线测试响应卡）。
- fail：人性化 — HTTP 状态码绿 200，响应 Code 红 500，反馈不可信；根因在 Framework REST helper 丢 HTTP 码（见 Framework 2.5.41）。本模块另有空 `startDate`/`endDate` 绑 `""` 进 PG timestamp。
- 修复：`normalizeOptionalDateParam`；`Pixel::getWebsiteStatsByDateRange` 忽略空串。
- 宿主原生编辑。

## 2026-09-06 — body-end @static 嵌套 PHP 导致首页 500（1.0.8）

- 当前状态：`fixed`
- 根因：`$__pixel_script_url = '@static(...)'` 被模板编译器展开成嵌套 PHP，ParseError（unexpected statics）。同轮另有未提交 `Taglib.php` 注释含 `?>` 提前结束 PHP，整站无法加载 Taglib。
- 修复：body-end 改为 `fetchTagSource('statics', ...)`；`Taglib.php` 恢复为 HEAD；清 Visitor body-end tpl 缓存。
- 验收：首页 HTTP 200；`PixelStickyUtmJsContractTest` OK。
- 版本：`1.0.7` → `1.0.8`

## 2026-09-05 — 本机 Pixel Host 判定对齐 test.weline.com（1.0.7）

- 当前状态：`implemented`
- 变更：Pixel 本机 Host / 审计探针识别 `*.test.weline.com`，并保留遗留 `*.weline.test`。
- 版本：`1.0.6` → `1.0.7`

## 2026-08-29 — Pixel 语/币停用偏好 Cookie

- 当前状态：`fixed`
- `pixel.js` / `pixel.phtml`：语言/货币解析为 **inject → path → query/`data-*` → 硬默认**；不再回落 `WELINE_USER_LANG` / `WELINE_USER_CURRENCY`；仍可读 `WELINE_WEBSITE_*`。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.0.6` | implementing | `REQ-VISITOR-0002` | 2026-08-28 | 2026-08-28 | pending |
| `1.0.5`（文档基线） | docs_handoff | `REQ-VISITOR-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.0.6` — 全站像素错误监控与 Msg 订阅闭环

- 需求：`REQ-VISITOR-0002`
- 实现：`site_error` 事件、`PixelErrorIncident`、TopicProvider、`pixel.js` 捕获、后台错误监控、主题版本 runtime 注入、Checkout 服务端互补上报
- 专题：`doc/错误监控与订阅闭环.md`
- 2026-08-28 补丁：`Frontend` `Weline.Api.notifyApiFailure()` 始终派发 `weline:api:error`（含 QueryBin 业务/能力拒绝），像素 `window`+`document` 双绑可捕获；契约测试已扩
- 2026-08-28 UI：错误监控列表/详情用色彩徽章与行色条表示 `severity` 重要程度
- 2026-08-28 修复：详情页避免无属性/`dd` Taglib 冲突导致 `ParseError`（改用 `w-stack` 布局）
- 2026-08-28 UX：错误详情改为列表内快速弹出层（`w-dialog` + `detail?format=json`），不再进独立详情页
- 2026-08-28 UX：弹层改「一眼全貌」（严重度/中文类型/消息 + 四格 + 轨迹 chips + `<details>` 折叠技术字段）
- 后续：发券挽回仅文档记项

## `1.0.5` — 文档制度启用基线

- 基线版本：`1.0.5`
- 目标版本：`1.0.5`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-VISITOR-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-VISITOR-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-VISITOR-0001` / `1.0.5` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
