<?php

declare(strict_types=1);

// The implementation remains in the historical filename so PHPUnit can scan it
// without discovering a non-PHPUnit *Test class. PSR-4 loads this bridge for
// the manual HTTP probe controller.
require_once __DIR__ . '/PixelApiHttpTest.php';
