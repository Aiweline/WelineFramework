## 2026-09-14 — 博客九语正文管线（1.0.42）

- 目标：160 篇 en_US 已配对；补 ar_SA/bn_BD/es_ES/fr_FR/hi_IN/id_ID/pt_BR/ur_PK 独立 Post 行（slug 后缀 -ar/-bn/…）。
- 变更：`BlogContentResolver` 通用 locale slug 后缀；`data/export-en-posts-for-locale-packs.php` + `upsert-blog-locale-packs.php`；详情先精确 locale 再回退默认语。
- 版本：`1.0.41` → `1.0.42`（正文分批译写入库进行中）。

## 2026-09-14 — 无译文 locale 回退默认语博文（1.0.41）


- 现象：hi_IN/ar_SA 等非 zh/en 分类页博文 0 篇（库内仅 zh_Hans_CN/en_US 内容行）。
- 变更：`BlogContentResolver` 列表/按 slug 读取走 `contentLocaleCandidates`（请求语 → `zh_Hans_CN`），按 identity 去重优先本语；分类展示名仍用请求语。
- 验收：九语 `/blog/category` 卡片非 0；en_US 仍优先英文行。
- 版本：`1.0.40` → `1.0.41`。

## 2026-09-14 — 默认站博客分类名九语 EAV 补全（1.0.40）


- 现象：侧栏「新闻中心」在八语仍中文；民族服饰等约 60 类九语仍英文占位。
- 变更：`data/upsert-category-nine-locale-names.php` 写 website=0 EAV name；CSV/词典补「新闻中心」；`writeAttribute` EAV 写入后 bump `blog/content/website/*`。
- 验收：九语 `/blog/category` 侧栏首项本地化，中文分类名 0。
- 版本：`1.0.39` → `1.0.40`。

## 2026-09-14 — 搜索命中卡片分类名 WidgetI18n（1.0.37）

## 2026-09-14 — FAQ/chrome i18n（1.0.38）

- 订阅 RSS 九语非 identity 译文。

- 现象：EN 搜索博客卡仍显示「穿搭与造型」等中文分类名。
- 变更：`search-hit-post.phtml` 分类标签走 `WidgetI18n::label`；词典补九语。
- 版本：`1.0.36` → `1.0.37`。

## 2026-09-14 — 博客搜索索引按 content locale 隔离（1.0.36）

- 根因：`BlogSearchIndexDocumentBuilder` 把索引 `locale` 置空；`DatabaseSearchProviderIndexStore` 对请求 locale 仍匹配空串 → EN 搜索命中中文博文标题。
- 变更：索引写入 `trim($article->locale)`；`search:provider-index:rebuild` 后 en=160/zh=160，en 标题 CJK=0。
- 契约：`BlogContentResolverContractTest` 断言不再写 `locale: ''`。

## 2026-09-09 — 分类页 title/description 软预算（1.0.34）

- 现象：全站审计真实分类（如「汉服百科」）composed title 仅 27 字 → `title_length`；description 偏短。
- 变更：H1 仍用短分类名；`<title>`/SEO 叶标题改为「「名」分类博客 · 穿搭与选购」；subtitle/description 加长。
- 版本：`1.0.33` → `1.0.34`。

## 2026-09-09 — EN 详情 Person 身份链默认值（1.0.33）

- 现象：`en_US` 博文 Person 仅 `name`，SEO 面板 Who tip「Person url or sameAs」。
- 变更：`BlogSeoFactsBuilder::defaultAuthorIdentity` + Resolver 运行时回填；Upgrade 持久化缺省 url/sameAs/bio/jobTitle。
- 版本：`1.0.32` → `1.0.33`。

## 2026-09-09 — 作者身份链落地与 byline 链接（1.0.32）

- 现象：样例博文 Person 仅 name；DB 缺 `author_url` 等列（1.0.29 声明未落到表）。
- 变更：补齐 `author_url/bio/job_title/sameAs` 列并回填样例文；详情 byline 在有 url 时输出 `rel=author` 链接。
- 版本：`1.0.31` → `1.0.32`。

## 2026-09-09 — 博客详情 SEO：面包屑 locale 与分类链接（1.0.31）

- 现象：JSON-LD BreadcrumbList 的 Home/Blog 丢 `/USD/en_US/`；可见分类链仍是相对 `/blog/category/...`。
- 变更：`BlogSeoFactsBuilder::localeAwarePath` 从 canonical 继承货币语言前缀；详情分类 href 走 `getUrl`；分类页面包屑改 `getUrl`。
- 版本：`1.0.30` → `1.0.31`。

## 2026-09-09 — 博客详情 SEO：H1 / canonical / 分享图 alt / section（1.0.30）

- 现象：详情 H1=0（包在 `<header>`，Inspector 排除）；canonical 缺 `/USD/en_US/`；缺 `twitter:image:alt`；缺 `article:section`。
- 根因：详情页头用 `<header>`；`BlogSeoProfileProvider` 重建 profile 时丢弃控制器 `getUrl` canonical。
- 变更：页头改为 `div`；View/`preferredDetailCanonical` 保留货币语言路径；`image_alt` + `articleSection`；CMS bridge 不覆盖已有 locale canonical。
- 版本：`1.0.29` → `1.0.30`。

## 2026-09-09 — 作者深字段支撑 E-E-A-T（1.0.29）

- 目标：BlogPosting Person 可携带 `url` / `description` / `jobTitle` / `sameAs`，消除 SEO 面板「浅作者」严苛 tip。
- 变更：`Post` 增 `author_url`/`author_bio`/`author_job_title`/`author_same_as`；后台表单与 `BlogSeoFactsBuilder` 输出深 Person；详情 byline 展示职称；RSS item 传 `author`。
- 版本：`1.0.28` → `1.0.29`。

## 2026-09-09 — 博客列表 SEO：短 title + 封面尺寸 + ItemList 事实（1.0.28）

- 现象：完整 title 过长扣分；列表卡片 img 缺 width/height；富结果只见站点 OnlineStore/WebSite。
- 变更：列表 title 改为「汉服博客」/`Hanfu Blog`；`item_list` 补 `published_at`/`image`；列表/详情卡片封面 `width=1200 height=800`。列表级 CollectionPage+ItemList 由 Seo HeadRenderer 1.2.73 输出。
- 版本：`1.0.27` → `1.0.28`。

## 2026-09-09 — 博客列表 SEO 分享图 / title-H1 / 卡片非标题（1.0.27）

- 扣分：缺 `og:image`/`twitter:image`/`twitter:card=summary_large_image`；H1「Blog」与 title 长尾不对齐；列表 50×H3；mega-menu 短英文 alt 触发 image alt。
- 修复：`BlogSeoFactsBuilder::buildListProfile` 发首篇封面（回退首页分享图）+ `image_alt`；Index H1 与 SEO title 同源；卡片标题改为 `.blog-storefront__card-title`；`en_US` 补英文列表 title。
- 版本：`1.0.26` → `1.0.27`。

## 2026-09-09 — 博客列表 SEO 结构（H1/H2/canonical）（1.0.26）

- 问题：SEO Inspector 计 H1=0（H1 包在 listing `<header>`，outline 排除所有 `header`）；卡片全是 H2；分类筛选 H2 抢在 H1 前；canonical 硬编码 `baseUrl/blog`，与 `hreflang` self（如 `/USD/en_US/blog`）不一致。
- 修复：listing 页头改为 `div`；增加「文章列表」H2，卡片标题改为 H3；分类标题改为 `<p>`；封面 `alt` 用文章标题；`Index`/`Category` 列表 canonical 改 `getUrl`；Theme `blog_category` 同步 H3/section 样式。
- 版本：`1.0.25` → `1.0.26`。

## 2026-09-09 — 博客 SEO：author、分类元数据与描述加厚（1.0.25）

- BlogPosting 发 `authors` Person；分类页保留分化 title，并加厚 description。
- 版本：`1.0.23` → `1.0.25`。

## 2026-09-09 — GEO Feed 变更写入（1.0.23）

- `BlogPostUrlNotifier` 在 SEO 通知外直写 `FeedSubmitService`（可选依赖 `Weline_Geo`）。
- 版本：`1.0.22` → `1.0.23`。

## 2026-09-09 — 博客页 RSS 订阅按钮（1.0.22）

- 列表/分类/详情页头增加「订阅 RSS」outline 按钮（`partials/rss-subscribe.phtml`）；分类指向分类 feed。
- 版本：`1.0.21` → `1.0.22`。

## 2026-09-08 — 读者侧 RSS（1.0.21）

- 入口：`/blog/rss.xml`、`/blog/category/{slug}/rss.xml` → `Controller/Frontend/Rss.php`；`RssFeedWriter` 输出 RSS 2.0。
- Discovery：`BlogSeoFactsBuilder` / 分类页 `feeds` → Seo Head `application/rss+xml`。
- 路由：`Router` 在文章 slug 前认领 `rss.xml`；`BlogNamespace` 增加 `rssPublicPath` / `categoryRssPublicPath`。
- 版本：`1.0.20` → `1.0.21`。

## 2026-09-08 — 博客列表 description 加长（1.0.20）

- `/blog` meta description 约 42 字；加长到约 80–170，并更新 i18n。
- 版本：`1.0.19` → `1.0.20`。

## 2026-09-08 — 分类树 EAV 批量读取与请求快照（1.0.19）

- 最终定向验证：27 tests / 152 assertions 通过；10 个 PHP 文件 lint 与限定 diff check 通过。未由单元测试替代主任务 HTTP/浏览器验收。

- 原路径 `BlogSearchCategoryScopeService → BlogCategoryAdminService::tree` 对 86 分类逐个读取 name/image/banner/summary/description，实际 443 次 DB；现在一次向 EAV 正式批量接口提交分类与字段集合，按三种 value table 各 1 次 SELECT。
- 分类树通过 `BlogContentCache → StorefrontScopeHotCache::rememberForRequest` 按 website/locale 复用；既有 Blog 模型和属性写清理请求快照时推进请求 revision，活动事务直接读取。保留同一名称 fallback、cleared、slug/path、树层级与排序；空名称不再二次 EAV 读取。
- 最小 SQLite RED：两分类/五字段/两语言读 20 次而非 2 次；批量后重复请求内读取曾为 3 次而非 2 次，加入请求快照后通过。EAV SQLite 同时覆盖四层 scope、三种 locale、空值、缺值、多值和 cleared。
- 真实只读 probe `/tmp/weline-blog-category-batch-read-probe.php`：同入口 443→17 DB，三张值表各 1 次；同请求重读 0 DB / 0.08ms。首读两次观测 39.85ms、181.30ms，运行负载波动，不能视为整页性能承诺。
- 未新增 typed EAV 值的跨请求缓存：独立 `EntityAttributeStore::writeScopedValue/clearScopedValue → upsertScopedRow` 尚未发布规范 ResourceChange/namespace 版本，不能拿 Blog Category/Post changed 代替属性失效。HTTP/浏览器由主任务统一验收；未重载、清库或写业务数据。

## 2026-09-08 — Blog 公共内容缓存与提交后失效（1.0.18）

- `BlogContentCache` 复用 Framework `StorefrontScopeHotCache` 的进程 L1、共享 pool L2；原始文章列表/slug 查询、分类行、关键词按显式 website/locale/参数区分。公共值不含 request ID、请求域名或最终 URL。
- 正向 slug 命中写入实际查询范围，修复网站内容回退至 website=0 后仍重复查询；全局与原站点 namespace 依赖同时纳入策略。
- 关联候选仍按数量上限读取；分类和关键词按 distinct ID 批量补齐，请求内复用缺失翻译。分类 EAV 本地化沿既有 EntityAttributeStore 缓存/changed，CSV 名称回退留在请求内。
- `Post`、`Category`、`Post\LocalDescription` 模型保存/删除通过 `BlogContentMutation`：事实写入、ResourceChange 与 namespace authority 同事务，提交后发布 Worker 版本通知，回滚清请求副本。WHERE 删除保留原始条件并按实际受影响行失效；失败保存不发布。
- 活动事务读取不写共享缓存；临时关键词读取异常只作本请求回退，避免污染共享负缓存。
- 行为测试 `BlogContentReadBehaviorTest` 6 tests / 25 assertions；RED 曾确认全局命中和关键词各重复回源两次；相关回归合计 21 tests / 80 assertions 通过。
- 真实配置运行库验收通过（`/tmp/weline-cache-remediation-blog-runtime.php`，结果 `/tmp/weline-cache-remediation-blog-runtime.json`）：临时草稿 328 创建提交，`global/storefront/blog/content/website/0` generation 从 2→3，已热化 fingerprint 同步改变；更新事实读回及版本推进、事务回滚保持事实/generation/frozen fingerprint、带 WHERE 删除及版本推进全部通过，临时草稿已删除。
- 首轮探针将不存在 namespace 的 generation 默认写为 1，导致首次 0→1 被误判；按 `NamespaceGenerationRepository` 实际缺省 0 修正探针后复验通过，无需因此改动业务实现。主任务继续 rolling reload、HTTP 和浏览器验收。

## 2026-09-08 — 文章解析复用 Context / identity map（1.0.17）

- 首次发布态行 `rememberPublishedPostRow`（按 id/slug 挂 Context + `Post::hydrateLoadedRow`）；详情正文优先读 Context。
- 列表按 `limit` 取数，不再扩到 50；CMS 合并 slug 批量查询并缓存 miss。
- 分类 meta 请求内复用。验收：`BlogContentResolverReuseContractTest`。
- 版本：`1.0.16` → `1.0.17`

## 2026-09-07 — 英文 All Posts 空列表：locale 大小写规范化（1.0.16）

- 真实失败：`/USD/en_us/blog`（路径小写）「全部文章」显示 0 条；同站 `/USD/en_US/blog` 与中文 `/blog` 正常有卡片。
- 根因：`BlogScopeResolver::locale()` 读 `State::getLang()` 保留 URL 段大小写（`en_us`），`listPublishedPosts` 对 `Post.locale`（存 `en_US`）精确匹配失败。`getLangLocal()` 经 I18n `getLocalByCode` 可大小写不敏感映射到已安装编码。
- 修复：`locale()` 优先 `State::getLangLocal()`，空时回退 `getLang()`，再统一 `-`→`_`；契约测试断言 `getLangLocal()`。
- 验证：`BlogContentResolverContractTest`；浏览器 `/USD/en_us/blog` 须 `>0` 卡片且无 `blog-list-empty`。
- 交付地址：[英文小写路径博客列表](http://127.0.0.1:9555/USD/en_us/blog)
  `https://p05113ef3.test.weline.com:9555/USD/en_us/blog`

## 2026-09-07 — 文章关键词 LocalModel 与展示本地化（1.0.15）

- 问题：英文详情页关键词 pill 中英混显（如「俄罗斯族服饰 / Russian dress / 民族服装」）；此前 keywords 原样输出，未做 LocalModel。
- 方案：新增 `Post\LocalDescription`（`weline_blog_post_local.keywords`）接入 I18n LocalModel / AI 翻译队列；后台表单增加 `<local>`；`BlogContentResolver` 优先读 Local 译文。
- 回退：Local 未写入前，`BlogKeywordLocalizer` 按 locale 做汉字/拉丁脚本过滤，并对异语标签走 `__()`，避免英文页继续露出未翻译中文标签。
- 说明：正文仍按 Post.`locale` 分行；LocalModel 只覆盖关键词字段，不把整篇文章改成 Local 架构。
- 验证：`BlogKeywordLocalizerContractTest` 6/6；`testDetailTemplateRendersAmazonArticle` 通过；`setup:upgrade -m Weline_Blog` 成功。真实 HTML 英文页关键词 pill 仅 `Russian dress`，无「俄罗斯族服饰 / 民族服装」。
- 交付地址：[英文俄罗斯族服饰概览](http://127.0.0.1:9555/en_US/blog/ethnic-russian-dress-overview)
  `https://p05113ef3.test.weline.com:9555/en_US/blog/ethnic-russian-dress-overview`

## 2026-09-07 — 列表页博客链接走框架 getUrl（1.0.14）

- 真实失败：`/en_US/blog` 卡片与分类链接输出裸 `/blog/{slug}`、`/blog/category/{slug}`，离开当前语言前缀。
- 修复：`frontend/index.phtml` 与 `partials/category-filter.phtml` 对相对路径调用 `$this->getUrl(ltrim(...))`（绝对 URL 原样保留）；DTO `public_url` / 分类 `url` 仍为 `BlogNamespace` 相对路径，不影响 Sitemap 契约。
- 验证：`BlogFrontendTemplateContractTest` 增加 getUrl 契约断言；浏览器验收列表卡片 href 须含 `/en_US/blog/`。
- 交付地址：[英文博客列表](https://p05113ef3.test.weline.com:9555/en_US/blog)。

## 2026-09-05 — 默认新闻分类中英文名称与真实页面回读

- 当前状态：`implemented_pending_browser_acceptance`。修复英文新闻页混排，不代表整店可上线。
- 实际 RED：`/en_US/blog/category/news` 为 HTTP 200、`lang=en-US`，但 title 为 `新闻中心 | 博客分类页布局`、列表标题为 `新闻中心`；最小 HTML 探针 1/3 passed。新建分类行为测试也实际读到中文回退，既有自定义英文保留用例已通过。
- 根因：`BlogNewsCategoryBootstrap::ensure()` 的新建分支仅写 locale 为空的中文名称；现有读取按 EAV 回退拿到非空中文后，不再使用 CSV。没有修改这个回退语义，也没有给全站添加新的运行限制。
- 最小源码修复：仅新分类追加 `zh_Hans_CN=新闻中心`、`en_US=News Center` 两次现有 `writeName()` 调用。保留中性名称、slug、排序和已存在分类的原有早返回；既有自定义分类不会被 bootstrap 改写。未改 Controller/Model/模块版本。
- 当前站点数据：只读确认 website=0 / category_id=91 / name attribute_id=36，原来恰有 value_id=1223 的中性中文 explicit 行，没有英文或 cleared 覆盖。再次核对同一前像后，通过现有属性服务只新增 `en_US=News Center`；读回名称行 1→2，原中性行逐字段不变，中文仍为 `新闻中心`。未改其他分类、文章或 Theme 草稿/发布数据。
- 验证：新增 `BlogNewsCategoryLocaleTest` 2 tests / 6 assertions；既有 Bootstrap 契约 2 tests / 4 assertions；Theme 实际 CSV 解析 1 test / 2 assertions，合计 5 tests / 12 assertions，相关 PHP lint 与限定 diff check 通过。
- 真实运行：Theme collect 成功；既有 `cacheClearAndWait('default', 20.0)` 返回 `ctrl_op_52` completed、expected=2 / acked=2 / failures=[]。英文原地址 title=`News Center | Blog Category Page`，列表标题=`News Center`，新探针 3/3 passed；中文原地址 HTTP 200、`lang=zh-Hans-CN`，中文标题保持；英文首页原地址回归 12/12 passed。未使用绕缓存参数或重启共享服务。
- WebUI 边界：本轮再次以用户指定的内置浏览器打开英文新闻原地址，43.9 秒后工具 timeout、kernel reset，无法取得页面截图/可交互状态；未擅自切换外部 Chrome。主题编辑范围切换、保存/发布回读、购物及 375/768/1024 视觉仍未验收。现有 `blog_category/default.phtml` 仍有旧 Amazon 风格 CSS，本条未重设计或声称视觉已合格。
- 宿主原生编辑。
- 交付地址：[英文新闻分类](https://p05113ef3.test.weline.com:9555/en_US/blog/category/news)；[中文新闻分类](https://p05113ef3.test.weline.com:9555/blog/category/news)。后台/API 无新增入口。

## 2026-09-05 — 新闻页脚入口保留当前语言

- 真实失败：英文首页 `News Center` 仍链接裸 `/blog/category/news`，点击会离开英文路径。
- 修复：`footer-news-link.phtml` 保持 `BlogNamespace::categoryPublicPath()` 与现有分类 slug，仅改为由 `@url` 生成当前站点语言/货币路径；未改默认插槽、文章内容、分类数据或模块版本。
- 验证：Theme 真实首页探针先复现该链接失败，修复并取得 default WLS 2/2 Worker 清缓存回执后，实际为 `https://p05113ef3.test.weline.com:9555/en_US/blog/category/news`，完整首页探针 12/12 通过，模板 PHP lint 通过。目标页面 HTTP 200、`lang=en-US`。
- 边界：本条完成时目标页自身的默认新闻分类名及布局元标题仍输出中文，后续修复见上方「默认新闻分类中英文名称与真实页面回读」；浏览器点击验收仍因页面读取超时待补。
- 交付地址：[英文新闻分类入口](https://p05113ef3.test.weline.com:9555/en_US/blog/category/news)。

## 2026-09-05 — Sitemap 目标网站完整 URL 修复（1.0.13）

- 真实失败：SEO 同步读到 Blog 321 条 `/blog...` 相对 `loc`，违反 Sitemap URL Provider 完整 URL 契约；原提供器还误用当前请求 Scope 的基址，无法保证目标网站正确。
- 最小修复：`BlogSitemapUrlProvider` 使用已注入的 `WebsiteDirectoryInterface::get($websiteId)->url` 调用 builder，仅将 Blog 自有 `/blog`、`/blog/...` 相对 loc 补为完整 URL，保留目标端口与部署路径。原有完整 URL 留给 SEO 严格校验；未更改 `BlogArticle.publicUrl`、前端 DTO、内容或图片。
- 定向验证：`php app/code/Weline/Blog/Test/Regression/BlogSitemapOriginRegression.script.php`，网站 `0` 和网站 `4` 两个行为用例修改前均失败、修改后均通过，覆盖不同请求 origin、端口/部署路径、已有完整 URL 和前端相对路径保持。
- 真实同步第二次失败定位：321 条提供器输出有 160 对重复 canonical。只读前四条证明是 `en_US`（`-en` storage_slug）与 `zh_Hans_CN` Post 共用 `/blog/{slug}`；DTO 有 locale，但现有 builder 的 Sitemap 行没有 locale，非 CMS/Post 影子冲突。首对 `326/325` 中，现有无 locale 前端路由选择 `325`，不能仅保留枚举首条英文记录。
- 去重修复：仅在 Provider 规范 loc 后按现有行的 `(locale, loc)` 分组；重复组调用既有 `resolveBySlug` 选择实际路由对应项，保留它的原 `url_key`，并用 Post 优先、稳定 url_key 顺序处理未解析组。不修改 DTO、语言路由或数据库。原有三个构造参数保留，新 resolver 只作为可选测试注入，生产重复组惰性解析。
- 新增真实形状回归先失败后通过：中英相同公开 URL 与 CMS 候选，无论枚举顺序都只保留路由 `blog-post-325`；合计三个行为用例通过。真实只读复核为 `count=161 / unique_locs=161`，前四条保留 `blog-list / blog-post-325 / blog-post-323 / blog-post-321`；整个探针含 160 组去重及四条路由检查约 1.58 秒。此证据仅证明 Provider 输出，最终同步与 XML 文件结果由主任务记录。
- 运行边界：本子任务未执行升级、热更新或数据库写入；真实 Provider 再探针、SEO 同步和 sitemap 文件生成由 SEO/CDN 主任务执行并记录于 SEO 开发日志。

## 2026-09-05 — 穿搭指南实看纠正、摘要边界与馆藏结构图

- 真实页面暴露两项遗漏：指南仍有“真正有用的文章”式编辑说明；所谓结构图只是同一母图右侧裁切，主要领口未入镜。哈希差异不等于图文语义一致。
- 定向重写指南中英文两篇，按场合、基础层、领口与裙门、尺码护理、配色配饰、行动试穿组织正文；纠正“圆领露出交领内搭必然错误”的笼统判断。其他 318 篇正文、标题、分类、封面和发布时间保持不变。
- 摘要从首段提取，不再把后续标题拼入；真实生成回归先复现 34/320 篇越界，再验证为 0/320。发布时另修正 33 篇原有摘要，未覆盖它们的正文。
- 指南 form 图改为 The Met《Skirt》（30.75.88，19 世纪，丝及金属线，object 68568）的官方展开视图。按 Open Access/Public Domain 使用，完整等比适配 1200×800，不裁掉裙门和褶裥；来源、许可、图注和中英 reviewed/manual 元数据均经 FileManager 接口保存并逐字段读回。
- 新图键为 `blog/hanfu/r3/inline/hanfu-styling-complete-guide/form-016fabfffcde.webp`。旧 `form-97e3bacbeaf1.webp` 经 Blog 全字段、项目运行文件和 FileManager 三类零引用检查后删除；两种 locale 均不再返回活动资产，物理文件不存在。旧图、旧元数据及文章快照保留在项目外恢复备份，未使用的研究下载和无效长截图移出项目。
- 本轮只有一个素材记录改变，其余 443 条来源记录保持一致；当前 444 张正文图为 431 张既有 AI 衍生图、8 张 ImageGen 原创编辑图、5 件 Met 开放藏品。审计读回 604 个现用资产、1,208 条双语元数据、320 篇、888 个 figure，0 errors。
- 原中文链接曾命中旧 shared FPC，不能用带参数页面代替验收。调用既有运行时 `cacheClearAndWait("default", 12.0)`，收到 2/2 worker 完成回执后重新请求中英文原地址，均 HTTP/2 200、3 个 figure、新正文及新图键存在、旧图键不存在。
- 验证：`HanfuR2ContentRemediationContractTest` 12 tests / 11,342 assertions；`HanfuR3ContextualImageManifestTest` 4 tests / 5,136 assertions；素材审计、实时发布审计、`--verify`、320 篇摘要边界检查均通过。未增加正文措辞测试；只同步指南题纲与新稿内容。
- 实看：375×812 截图完整显示新裙图、图注、来源链接及后续护理段落；768×1024 截图确认新版摘要和结构段落。证据位于 `doc/evidence/2026-09-05-contextual-acceptance/`。临时视口已复位。浏览器导航偶有超时，但后续真实单屏截图有效；整篇小屏滚动和故意断图尚未完成，不将局部实看或无效拼接长截图视为整站验收。

## 2026-09-04 — Hanfu 正文语境配图混合证据整改与近重复清理

- 方案：160 个主题继续使用专业编辑插图承载结构、搭配、护理与工艺阅读；能核对到开放藏品的节庆/织物证据位保留 4 件大都会博物馆 Open Access/Public Domain 实物。图片均明确区分“编辑配图”与“馆藏证据”，不把 AI 图冒充历史实物、田野记录或特定礼仪身份。
- 文章配图：320 篇中英文文章共插入 888 个语义 `<figure>`，每篇 2–3 张；图片固定 1200×800、3:2、lazy/async，并使用当前语言的专属 alt、description、caption。最终素材为 432 张已核准 R3 编辑衍生图、8 张重新生成的角色专属编辑图和 4 张 Met 开放藏品，共 444 张且 SHA-256 全部唯一。
- 去重纠正：真实浏览器复核后新增“同一主题不同图位 dHash 距离必须大于 16”的行为门禁，先稳定复现 8 组近重复，再分别重制完整穿搭护理、中式婚礼护理、哈尼工艺证据、节庆护理、汉族服饰结构、俄罗斯族形制、电商礼服形制阅读和新中式结构对照图。
- FileManager：当前 160 张封面 + 444 张正文图共 604 个活动资产，双语 locale descriptor 共 1,208 行；`display_name/default_alt/description/default_caption`、来源边界及 `translation_state=reviewed`、`translation_origin=manual` 均由独立审计逐项读回。
- 清理：8 个被替换的旧 inline object key 在 Blog 全字段、项目运行时文件和 FileManager 引用数均为 0 后按冻结 allowlist 删除；删除后两种 locale 均 `asset_ready=false`、asset id 为空且物理文件不存在，现用图片与源素材未被误删。
- 验证：精确相关套件 46 tests / 40,056 assertions；素材审计 444/444、唯一 SHA 444、0 errors；`--verify` 为 320 posts / 160 pairs / 888 figures / 每篇 2–3 张 / 最大正文复用 2；独立 live audit 为 604 assets / 1,208 locale descriptors / 0 errors。WLS 重启后博客首页、民族服饰分类页、完整穿搭、节庆与哈尼代表页均返回 HTTP 200，实时 HTML 只含新图键与专属图注。
- WebUI 剩余门禁：2026-09-05 已在 macOS 解锁后重新解析精确 `Weline` Chrome 实例；WLS 的间歇性 HTTP/2 断连经 `server:reload default` 后恢复，连续 3 次 HTTP/2 与 5 个交付 URL 均为 200。随后 375×812 视口可设置，但 Chrome 扩展调试器无法附着（`Page.navigate` timeout / debugger-not-attached），DOM、截图和故意断图均无法形成可观察证据；临时视口已复位。响应式 CSS 单测只作为开发证据，不替代真实浏览器。Task 7 保持 `partial`，待释放/重启该扩展调试连接后补测 375px、768px 与断图 fallback。

## 2026-09-04 — 博客评论跟随 Review 默认翻页

- 共用 `Weline_Review` `product-reviews.js/css`：列表默认翻页（上一页/下一页）；`blog-reviews` 模板补充 pager 文案与 CSS 缓存戳。
- 验收：与 Review 1.0.5 同测 `/blog/ethnic-blang-dress-overview#blog-reviews`。

## 2026-09-04 — 博客评论列表跟随 Review 限高无限滚动

- 当前状态：`superseded`
- 共用 `Weline_Review` `product-reviews.js/css`：列表限高下滑加载更多；`blog-reviews` 模板补充 `loadingMore`/`loadEnd` 文案与 CSS 缓存戳。
- 验收：与 Review 1.0.3 同测 `/blog/ethnic-blang-dress-overview#blog-reviews`。

## 2026-09-04 — Hanfu R4 读者语气重写与反提示词门禁

- 根因：R3 核心文章和民族服饰编辑器把内部写作指令直接组装成前台段落，导致读者看到“证据卡”“核查清单”“可发布记录”“场合诊断”等工作流程语气，形成“像提示词”的阅读体验。
- 正文：重写 48 个核心主题和 56×2 个民族服饰主题的中英双语文章，共 320 篇本地化记录。核心文章改为“汉服老师 + 选购编辑”的自然口吻，民族服饰按各族资料分别讲清地域范围、服装结构、材料工艺、穿着场合与地方语境，不再复制通用清单模板。
- 防回归：生成和数据库 `--verify` 同时拦截中英文提示词/流程词；单元契约覆盖 96 篇核心本地化文章与 224 篇民族服饰本地化文章，同时检查知识要点、篇幅、章节和归一化段落复用。
- 资产边界：本轮不换图、不删图，继续使用 R3 的 160 张唯一封面；FileManager 中 320 行中英 locale 元数据及 `reviewed/manual` 状态完整保留，边界契约通过。
- 执行：`--apply` / `--verify` 读回 320 posts、160 topic pairs、160 unique covers、4,416 paragraphs，归一化段落最大复用数为 2，`deleted_obsolete_assets=0`；博客正文、民族服饰编辑、R3 图片清单和 FileManager 边界联合验证为 36 tests / 29,617 assertions。
- 真实运行：服务重启后发现 `Weline_Blog` 代码版本 1.0.11 与数据库 1.0.10 不一致，定向执行模块升级后路由恢复。博客首页、民族服饰分类页及五篇代表文章均返回 HTTP 200，标题和正文为新版内容。

## 2026-09-04 — 博客详情对接万能评论（1.0.11）

- 问题：Theme `blog` 布局已有 `blog-reviews` 槽与 Blog `blog-reviews` required default_injection / `BlogReviewTypeProvider`，但注入只落在 draft，未 `publishLayout`，前台评论容器为空。
- 修复：对活动主题发布 blog 布局；`Setup/Upgrade::ensureBlogReviewsPublished()` 调用 `applyRequiredMissingForIdentity` + `publishLayout`，避免仅草稿不上线。
- 验收：`/blog/ethnic-blang-dress-overview` 可见 `data-testid=storefront-blog-reviews`、`data-type-code=blog`、`data-entity-uuid=blog:post:*`；浏览器可展开「写评论」表单。

## 2026-09-04 — Hanfu R3 正文、图片、FileManager 元数据与旧图清理（R3 完成）

- 清单：新增 `hanfuR3ImageManifest()`，精确覆盖 48 个核心主题与 56×2 个民族服饰主题，共 160 项；每项登记形制、必见/可见/禁称特征、ImageGen 来源、项目图片许可、用途、关系、创建目的、复核人/日期和独立研究引用。所有保留 AI 图均明确不能证明具体实物、支系身份、礼仪等级、朝代、纤维或手工制作。
- 双语元数据：每项均提供人工编写并校对的 `zh_Hans_CN` / `en_US` `display_name`、`default_alt`、`description`、`default_caption`，因此 locale 固定 `translation_state=reviewed`、`translation_origin=manual`；图片本身只登记为 `contact_sheet_screened`，范围限于当前 160 张源图的宽泛主题、卡片裁切、logo/水印与视觉碰撞筛查，不冒充服饰实物、族群身份或历史文化鉴定。manifest 同时记录筛查方法、局限和 contact sheet/source 证据路径。`remediate-hanfu-content-r2.php` 已改为按 manifest 的精确源路径、locale 行和 asset payload 驱动 FileManager，不再生成通用标题元数据。
- 选择性替换：仅覆盖 `var/hanfu-production/final/` 下形制指南、马面裙/襦裙对比、完整穿搭和水族概览四张 1600×900 WebP，SHA-256 前缀依次为 `3d9726db3770`、`f1f70ad4df1f`、`24e3f964a817`、`b391920c44a9`。正式 apply 后，四个旧哈希 object key 通过 Blog 全字段/全状态、项目运行时文件、FileManager 派生引用三重零引用门禁，再逐对象删除；数据库保留软删除审计行，四个 `pub/media` 实体已读回确认不存在。
- 水族边界：水族概览明确标为马尾绣技法的 AI 编辑插图，不是博物馆实物照片或田野证据；可见线索限于靛蓝底、丝线包缠马尾芯的凸起轮廓、彩色扁线填绣、手针和亮片，不指认制作者、村寨、年代、礼仪角色或收藏编号。研究依据分别记录国家民委页面与中国非物质文化遗产网项目页，和图片许可分开。
- 视觉契约：`HanfuR3ImageManifestTest` 对全部 160 张图检查 1600×900、跨主题 SHA 唯一、整图 64-bit dHash 距离至少 9，以及 left/right/top/bottom 任意跨主题半面板组合距离大于 5；实测替换前水族与仡佬概览左半距离为 2，替换后无碰撞。
- 正式执行：`setup:upgrade -m Weline_FileManager --skip-classmap` 已将 FileManager 从 1.1.5 升至 1.1.6；`--apply` 写入 320 篇文章、160 个主题资产与 320 行中英 locale 元数据；逐资产写后读回确认 selectable、源 SHA、asset metadata SHA、文案字段及 reviewed/manual 状态全部一致。
- 验证与清理：目标 PHPUnit 35 tests / 27,088 assertions；`--verify` 返回 320 posts、160 topic pairs、160 unique covers、4,848 paragraphs、`maximum_normalized_reuse=3`。`--cleanup` 的 allowlist 恰为 4，三类引用命中均为 0，`deleted_obsolete_assets=4`；清理后再次 `--verify` 通过。

## 2026-09-04 — Hanfu R3 Task 6 重新发布门禁（阻塞，未写入）

- 本次状态：**blocked before stateful publication**。Task 6 只新增只读审计与运行报告，未改生产代码，未运行 `--apply` / `--verify` / `--cleanup`，因此未写 320 篇文章、未登记 160 cover + 444 inline FileManager 资产或 1,208 locale descriptor，也没有删除任何文件。
- 前置证据：PHP 8.4.22 的 GD/WebP 可用；Task 2 资产审计为 444/444、provenance 444/444、440 个 approved AI derivative + 4 个 Met public-domain、0 errors；`remediate-hanfu-content-r2.php --dry-run` 返回 320 posts、160 pairs、160 unique covers、444 contextual descriptors、888 figures、每篇 2–3 张、最大归一化复用 2。
- 独立 RED：`audit-live-publication.php` 从 R3 cover/contextual manifest、源文件 SHA 与 provenance 派生预期值；apply 前预期 604 assets（160 covers + 444 inline）/1,208 locale descriptors，实测只有 160 active cover assets，所有新增 inline public object、metadata/locale descriptor 和 figure markup 缺失；虽已有 320 行/160 locale pairs/888 个预期 slot，审计仍以 14,652 个字段级不匹配退出 1，未降低门槛。
- 阻塞原因：指定全量 PHPUnit 命令为 `51 tests, 39035 assertions, 3 failures`。`BlogFrontendTemplateContractTest` 第 57/83/98 行分别期望 header Blog、footer Blog、footer News default injection page 为 `*`，当前脏工作区实际为 `homepage`。该问题超出 Task 6、发生在 apply 前，故按门禁停止。
- 清理：frozen allowlist 为四个旧 cover key（`hanfu-styles-ruqun-mamian-yuanling-2ed07e6cb2d6`、`choose-first-mamian-or-ruqun-01907a1bee4d`、`hanfu-styling-complete-guide-6da1159389d7`、`shui-overview-1802aa2cee55`）；只读检查均为未登记且物理文件已不存在。它们均记为 apply 前 already absent，不伪报为删除；Blog/project/FileManager 三重零引用删除门禁尚未执行。
- 后续：先恢复上述 3 个非本任务 PHPUnit 契约，再完整重跑 suite → apply → verify → 独立 GREEN audit，全部通过后才允许四键 allowlisted cleanup；Task 7 仍负责真实浏览器视觉/计算样式验收。

## 2026-09-04 — Hanfu R3 Task 6 重新发布门禁（批准后完成）

- 批准依据：控制器只读确认 `widget.php` 工作区无 diff，`homepage` 是自 2026-09-03 起已存在于 HEAD 的值，而旧 `BlogFrontendTemplateContractTest` 仍断言 `*`；Task 1–5 精确六文件套件独立通过（44 tests / 39,000 assertions）。未修改该旧测试或 widget 语义。
- 发布：`remediate-hanfu-content-r2.php --apply` exit 0，写入/读回 320 posts、160 topic pairs、160 unique covers、444 contextual descriptor、888 figures（每篇 2–3）、4,416 paragraphs、maximum normalized reuse=2；来源资产为 440 approved AI editorial derivatives + 4 Met Open Access/Public Domain。
- 验证：`--verify` exit 0；独立只读 live audit 从 manifests、SHA-keyed provenance 和物理/公开 FileManager descriptor 重建预期，发布后及清理后均为 604 active assets（160 cover + 444 inline）、1,208 reviewed/manual locale descriptor、320 rows / 160 pairs / 888 figures / 160 covers、0 errors。
- 审计修正：初次发布后审计的 888 inline metadata mismatch 由 helper 将缺省 `slot_id` 误建为空造成；生产实现以 `visual_role` 回退。另将 caption 断言从 alt+caption 的重复文本计数收窄为 `<figcaption>`。修正仅涉及 task-local read-only audit；存储 SHA、正文或生产脚本均未改动。
- 清理：`--cleanup` exit 0；immutable allowlist size=4，Blog/project/FileManager reference hits 均为 0，`deleted_obsolete_assets=0`。四个指定旧 key 都是在清理前已 absent（未登记、无 asset id、无物理文件），故分别记录为 already absent，未伪报删除；当前 cover、inline、source asset 和无关文件均未移除。
- 浏览器：Task 7 仍负责真实浏览器视觉与 computed-style 验收，本记录不替代该门禁。

## 2026-09-03 — Hanfu R3 编辑语料、证据记录与反模板契约

- 范围：仅整改 `Weline_Blog` 的双语 Hanfu 正文、资料记录与测试；不触及 Product、Theme/Banner、`generated/` 或媒体文件。
- 证据：新增结构化 `hanfuR3SourceEvidence()` 资料表，登记故宫明代官服/圆领研究、中国丝绸博物馆马面裙、UNESCO 云锦/蚕桑丝织/黎族纺染织绣，以及大都会博物馆中国织物出版物与开放藏品；每条记录都有机构、URL、限定的 claim scope、语言和访问日期。
- 结构：48 个核心主题按平台尽调、新中式边界、形制/历史、面料工艺/尺码护理、场合搭配、购买决策、品牌/工厂审计、全球传统服饰比较和 56 民族服饰枢纽分配职责；每篇输出职责专属 H2 顺序、诊断表/清单和至少两条相关资料。
- 民族服饰：56 组资料增加社区框架、穿着系统、材料/工艺、使用场合、照片推断限度与资料键。文章明确照片不能自动把活态民族服饰归类为汉服、特定支系或礼仪身份。
- 防回归：去除标题前缀式段落变形器；按去标签、实体解码、URL/引文移除、Unicode 空白和标点归一化后，段落与列表项单语言最大复用为 4。中英文正文门槛分别为至少 1,200 个汉字和 700 个英文词；每篇至少六个 H2，脚本 dry-run/verify 均不创建或更新 FileManager、locale 或 Blog 记录。
- 验证：`vendor/bin/phpunit app/code/Weline/Blog/Test/Unit/Data/HanfuR2ContentRemediationContractTest.php app/code/Weline/Blog/Test/Unit/Data/HanfuR2EthnicEditorialQualityTest.php`；`php app/code/Weline/Blog/data/remediate-hanfu-content-r2.php --dry-run`。

## 2026-09-03 — 博客二级分类树（1.0.9）

- 需求：万能分类 `space=blog` 支持二级展开；`china-ethnic-dress` 下挂各民族 `ethnic-cn-*`。
- 实现：`weline_blog_category.parent_id`（限深 2）；`BlogCategoryAdminService` 真树；前台筛选可展开；父级列表 `IN(self+descendants)` 聚合文章。
- 数据：`seed-china-ethnic-categories.php` 末尾 `remountEthnicChildren`。
- 契约：`BlogCategoryTreeContractTest`。

## 2026-09-03 — 页头导航扩展默认注入博客入口（1.0.8）

- 需求：Header 分类后方开放扩展槽，Blog 以应用部件默认注入。
- 实现：新增 `header-blog-link`（`default_injections → header-nav-extensions`，sort_order=0）；Theme partial 禁止内嵌。
- 契约：`BlogFrontendTemplateContractTest::testHeaderBlogLinkWidgetRegistersDefaultInjection`。

## 2026-09-03 — 民族服饰文章语义去重与配图重制

- 复核结论：撤销 2026-09-02 仅凭 URL、SHA256 和精确段落计数得出的 `accepted` 结论。实测同民族双主题普遍仍复用人物、服装、场景或母图；224 篇本地化文章继续使用固定导语和少量标题骨架。
- 内容整改：民族文章改用证据驱动的双语编辑模型，固定核对地域、形制、材料、场合与纹样，分别以七章讲清“概览”和“场合”，并标注族称、地方传统与编辑配图的证据边界。
- 防回归：新增行为测试覆盖 56 × 2 × 2，要求 224 篇正文及导语唯一、标题结构不少于 64 组、五类资料齐备且同民族双主题不再高度相似。
- 图片门槛：双主题不得复用同一人物、服装、场景或母图裁切；原创建图统一标注为编辑配图，不冒充历史实物或田野照片。
- 元数据与清理：继续通过 `FileAssetLibraryInterface` 统一登记并补齐双语元数据；旧文件仅在引用归零后移出项目。
- 实际验收以 `--verify`、感知相似度报告、文件管理器探针和 Weline Chrome 的 375/768/1024 页面结果为准，不再手写未经运行证明的通过数字。

## 2026-09-02 — 汉服博客专业内容与独立配图整改（复核后撤销验收）

- 当日脚本写入 320 篇文章与 160 个文件路径，但“唯一”检查只覆盖 URL、二进制 SHA256 和完全相同的段落字符串。
- 后续视觉复核发现同民族双主题普遍来自同一人物、服装或母图；文本复核发现民族文章仍由固定骨架批量展开。
- 因此当日状态改为 `invalidated`；原数量只保留为历史运行输出，不再作为上线质量证明。

## 2026-09-01 — 中国 56 民族服饰子分类 + 汉服男装图文（增量）

- 当前状态：`implemented`
- 变更：增量种子（不 purge）新增 `hanfu-mens`、`china-ethnic-dress` 与 56 个 `ethnic-cn-*` 分类；每民族 2 篇图文（概览 + 场合/工艺）× 中英，另含男装与枢纽文；配图遵守 `product-*.jpg` 不重复硬规则。
- 脚本：`seed-china-ethnic-categories.php`、`seed-china-ethnic-articles.php`、`china-ethnic-groups.php`
- 宿主原生编辑。

## Hotfix — 2026-08-31：页脚帮助中心改回 Theme，移除 Blog 帮助分类部件

- 当前状态：`implemented`
- 变更：删除 `footer-help-center-link` 与 `BlogHelpCenterCategoryBootstrap`；帮助中心入口改由 Theme `/help` 承接。
- 版本：`1.0.6` → `1.0.7`。

## Hotfix — 2026-08-31：页脚帮助中心分类与部件注入

- 当前状态：`implemented`
- 变更：`BlogHelpCenterCategoryBootstrap` 确保 slug=`help` 帮助中心分类；新增 `footer-help-center-link` 经 `default_injections` 注入 Theme `footer-help-links`。
- 版本：`1.0.5` → `1.0.6`。

## Hotfix — 2026-08-30：页脚博客链接也默认注入

- 当前状态：`implemented`
- 变更：`footer-blog-link` 增加 `default_injections`（`footer-about-links`，sort_order=0），与新闻中心一样可在「应用」页签一键落槽。
- 版本：`1.0.4` → `1.0.5`。

## 2026-08-30 — 页脚了解我们：博客/新闻中心部件

- 当前状态：`implemented`
- 变更：新增 `footer-blog-link` 与 `footer-news-link`（均经 `default_injections` 注入 `footer-about-links`）；`BlogNewsCategoryBootstrap` 确保 slug=`news` 新闻分类；Setup Install/Upgrade 调用。
- 版本：`1.0.3` → `1.0.4`。

## 2026-08-30 — 前台 JS 改 modules + data-weline-load（第二波）

- 当前状态：`implemented`
- 变更：登记 `view/statics/frontend/weline.modules.js`；部件/布局改 `data-weline-load`，删除 `@static`/`<js>` 直引。
- 权威：`Theme/doc/前端JS模块加载规范.md`；MCP `theme_js_module_declare_only`。

# Weline_Blog 开发日志

| 时间 | 阶段 | 说明 |
|------|------|------|
| 2026-08-27 | S1 脚手架 | 模块注册、Post/Category Model、BlogArticle DTO |
| 2026-08-27 | CMS 协作 | `CmsUriInterceptSkipInterface` + `ProcessCmsPageUriBefore` 豁免 |
| 2026-08-27 | 路由/解析 | Resolver、前台 Index/View、CMS 渲染桥 |
| 2026-08-27 | SEO/Sitemap | BlogSeoProfileProvider、BlogSitemapUrlProvider、CMS sitemap 排除 |
| 2026-08-27 | Search | BlogSearchProvider；Search 解耦 Product optional |
| 2026-08-27 | Search 索引 | 禁止 SQL 回退；Cron `search_provider_index_rebuild` 定时重建 Provider 索引 |
| 2026-08-27 | Search UI | 博客侧栏分类（对齐商品）、命中卡片网格 `storefront-search__grid--blog` |
| 2026-08-27 | Theme 布局 | `layouts/blog` + `layouts/blog_category` 亚马逊风格详情/分类页 |
| 2026-08-27 | 后台 | 文章 CRUD、菜单、i18n、契约测试 |
