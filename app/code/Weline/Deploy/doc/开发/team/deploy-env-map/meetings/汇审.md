# 汇审：deploy-env-map

| 项 | 内容 |
|---|---|
| 日期 | 2026-09-21 |
| 模块 | Weline_Deploy `1.1.3` |
| 规格 | `doc/开发/spec/deploy-env-map.md` |
| 结论 | **通过**（机制入库；映射文件不入库；有文件才扭转） |

## 交付摘要

- 服务：`DeployEnvMapService`（`deploy-env-map.v1` / 结果 `deploy-env-map-result.v1`）
- 钩子：`Weline_Deploy::release_after` → `DeployEnvMapReleaseAfter`（sort=200）
- CLI：`php bin/w deploy:env-map:apply [--dry-run] [--level=] [--deploy-root=] [--json]`
- 示例：`deploy.env-map.php.example`；真实文件 `/deploy.env-map.php` 已 gitignore
- 文档：`doc/deploy-env-map.md`、spec、README 索引

## 自验

| 门禁 | 结果 |
|---|---|
| UT `DeployEnvMapServiceTest` | 6 tests / 26 assertions PASS |
| CLI 无文件 | `php bin/w deploy:env-map:apply --dry-run --json` → `status=skipped` / `map_file_missing` |
| UI / e2e | N/A（无 Web 面） |
| 审图 | skip |

## 运维用法

1. 线上 `cp deploy.env-map.php.example deploy.env-map.php` 并按 Scope/支付方式改 rules（勿提交）
2. 正常 `deploy:release`；或单独 `deploy:env-map:apply --level=prod`
