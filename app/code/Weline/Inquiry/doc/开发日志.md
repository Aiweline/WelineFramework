# Weline_Inquiry 开发日志

## UI — 2026-09-08：编辑工具条与布局页头去重

- 当前状态：`implemented`
- 变更：chrome 去掉重复「编辑询盘表单」标题，仅保留表单标识与操作按钮；样式改为轻量工具条。
- 版本：`1.0.19` → `1.0.20`。

## UI — 2026-09-08：对齐改由主题 gutter 承担

- 当前状态：`implemented`
- 变更：去掉编辑器 `--iq-pad` / chrome 负 margin；左右边距交 Theme `--backend-layout-inline`。
- 版本：`1.0.18` → `1.0.19`。

## UI — 2026-09-08：编辑页面包屑与页边距

- 当前状态：`implemented`
- 问题：编辑页贴边；布局页头显示模块名且无面包屑（菜单 action 写成 `/index`，编辑路由前缀匹配失败）。
- 变更：菜单改为 `inquiry/backend/inquiry`；控制器 `page_title`；布局 `BackendPageHeaderResolver` 输出面包屑；编辑器 `--iq-pad` 左右内边距。
- 版本：`1.0.17` → `1.0.18`。

## UI — 2026-09-07：编辑器字段行窄屏响应式

- 当前状态：`implemented`
- 问题：窄屏下字段轨仅两列自动流，类型下拉被挤成一字、删除键单独掉行。
- 变更：`field-main` 改为 mobile-first 命名栅格（键名+删除 / 类型+必填），中屏单行，宽屏保持轨头对齐。
- 版本：`1.0.16` → `1.0.17`。

## UI — 2026-09-07：前台文案可视化配置

- 当前状态：`implemented`
- 问题：编辑页翻译区只有原始 JSON，中英文混杂技术词，配置步骤不直观。
- 变更：顶部四步引导；「前台文案」语言页签 + 标题/说明/按钮/字段标签表单；JSON 收进高级折叠；字段类型与 country 配置改中文标签。
- 版本：`1.0.15` → `1.0.16`。

## Feature — 2026-09-07：询盘翻译 AI 填充其它语言

- 当前状态：`implemented`
- 变更：`InquiryTranslationTree` 压平/回写叶子；`InquiryTranslationAiService` 复用 `I18nAiTranslationAdapter`；Query `adminAiTranslate`；编辑器「AI 填充其它语言」（空目标写入，强制可重翻）。
- 版本：`1.0.14` → `1.0.15`。

## UI — 2026-09-07：后台询盘表单编辑器重做

- 当前状态：`implemented`
- 问题：`inquiry/backend/inquiry/edit` 字段行用 Bootstrap 残留类，类型下拉缺 `country/number/hidden`，供应商申请的 `country` 显示空白。
- 变更：按 Weline Backend 令牌重做编辑器布局（粘性操作条、字段轨、country extras）；类型对齐 `FormSchemaService::FIELD_TYPES`；新增 `inquiry-editor.css`。
- 版本：`1.0.13` → `1.0.14`。
- 补充：首版模板误加 `declare(strict_types=1)`/`use`，编译后非首语句触发 Fatal；已改为 FQCN 且去掉 declare。

## Hotfix — 2026-09-07：供应商国家字段单选级联到区县

- 当前状态：`implemented`
- 问题：供应商「国家 / 地区」此前仅单级国家；需要可单选级联到区县（区域）。
- 变更：`country` 字段 `levels=country|province|city|district` + `selection=single` + `catalog=global`；schema 增加可选 province/city/district；Renderer 挂载级联地址组件。
- 版本：`1.0.12` → `1.0.13`。

## Hotfix — 2026-09-07：供应商页英文翻译与表单 locale

- 当前状态：`implemented`
- 问题：`en_US/inquiry/suppliers` 左栏仍中文；根因是 `@lang{'…'}` 被 `i18n:collect` 写成带引号脏键，且表单 schema 用浏览器 `navigator.language` 而非店面 URL locale。
- 变更：清洗 Inquiry CSV 键；`InquiryRenderer` 注入 `locale` 并优先页面/runtime locale；配合 I18n 收集器剥离 `@lang{}` 包裹引号。
- 版本：`1.0.11` → `1.0.12`。

## Hotfix — 2026-09-07：供应商页分栏等高 + 国家全球目录属性

- 当前状态：`implemented`
- 问题：左栏 `max-height` 截断，矮于右侧申请表；国家字段虽 schema 标 global，地址组件在预填 CN 后把目录换成省市区，下拉表现为站点地区。
- 变更：atelier 左栏去掉固定高度并与表单列 stretch 等高；`InquiryRenderer` 输出 `data-catalog` + 仅 `names.country`；`FormSchemaService` 支持字段级 `catalog` 属性。
- 版本：`1.0.10` → `1.0.11`。

## Hotfix — 2026-09-07：供应商合作说明补齐

- 当前状态：`implemented`
- 问题：页脚「供应商合作」仅落到申请表单页，左栏叙事过薄，缺少与配送/退换说明同级的合作须知。
- 变更：`inquiry/suppliers` 左栏改为含目录的合作说明（合作对象、准入与资料、样品与质检、交期与产能、结算与对账、禁止事项、申请流程）；右栏申请表单不变；中英 CSV 同步。
- 版本：`1.0.9` → `1.0.10`。

## Hotfix — 2026-09-06：供应商申请页定稿分栏工坊

- 当前状态：`implemented`
- 变更：折叠原型胜出方案 B；`inquiry/suppliers` 固定 atelier 分栏（左合作叙事 + 申请流程摘要，右表单）；移除 `?variant` 切换条与 A/C 资产。
- 版本：`1.0.8` → `1.0.9`。

## Prototype — 2026-09-06：供应商申请页三布局原型

- 当前状态：`superseded`（已定稿为分栏工坊）
- 问题：默认 Amazon 窄卡片偏运营后台感，与云裳前台品牌气质脱节；需要先看清几种结构再定稿。
- 变更：`inquiry/suppliers?variant=A|B|C` 在同一路由切换三种结构（A 精简卡片 / B 分栏工坊 / C 分区工作台）；dev 模式底部浮动切换条；表单仍走 `InquiryRenderer` + `skin=amazon`。
- 范围：仅外壳布局与 CSS/JS；不改提交契约。选定后折叠胜出变体并移除切换条。
- 版本：`1.0.7` → `1.0.8`。

## Hotfix — 2026-08-31：供应商申请国家字段改用全球目录

- 当前状态：`implemented`
- 变更：`country` 字段默认 `validation.catalog=global`；`theme:address` / region API 支持 `catalog=global`（Symfony Intl 全球国家，不限已安装国家）。
- 版本：`1.0.4` → `1.0.5`。

## Hotfix — 2026-08-31：供应商申请国家字段改用 theme:address

- 当前状态：`implemented`
- 变更：`FormSchemaService` 增加字段类型 `country`；`InquiryRenderer` 挂载官方 `data-w-address` + `address-loader.js`；`InquiryFormBootstrap` 将 `supplier-application.country` 升级为 `country` 并重新发布。
- 版本：`1.0.3` → `1.0.4`。

## Hotfix — 2026-08-31：询盘后台菜单路由前缀纠错

- 当前状态：`implemented`
- 问题：菜单/模板写成 `weline_inquiry/backend/...`，实际路由前缀为 `inquiry`，导致后台入口 404。
- 变更：`menu.xml` 与 Backend 列表/编辑模板统一为 `inquiry/backend/inquiry/*`。
- 版本：`1.0.2` → `1.0.3`。

## Hotfix — 2026-08-31：供应商申请表单默认亚马逊风格

- 当前状态：`implemented`
- 变更：`InquiryRenderer` 支持 `skin=amazon`；`inquiry/suppliers` 默认加载 `supplier-application-amazon.css`（对齐登录页 Amazon token：卡片边框、黄 CTA、输入框 focus）。
- 范围：仅供应商申请页；全局询盘默认灰框不变。
- 版本：`1.0.1` → `1.0.2`。

## Hotfix — 2026-08-31：供应商申请页 Taglib 原文泄漏

- 当前状态：`implemented`
- 问题：`suppliers.phtml` 在 `<w:inquiry code="<?= ... ?>">` 内嵌短标签，编译后 PHP 被当成 HTML 转义输出。
- 变更：与 Widget 一致，改为 `InquiryRendererInterface::render()` 直出 inline 表单。
- 版本：保持 `1.0.1`。

## Hotfix — 2026-08-31：供应商申请表单与前台页

- 当前状态：`implemented`
- 变更：种子并发布 `supplier-application`；前台 `inquiry/suppliers` 嵌入询盘表单；Install/Upgrade 共用 `InquiryFormBootstrap`。
- 版本：`1.0.0` → `1.0.1`。

## 2026-08-10

- 建立独立模块边界与 AI 索引。
- 确认以当前任务负责人直接执行；不因指定模型不可用而阻塞。
- 实施版本化 schema、多语言回退、提交快照、Taglib、Widget 与后台编辑能力。
- 同步到摩托车项目属于后续受确认步骤，本次框架实现阶段不改主题集成。
