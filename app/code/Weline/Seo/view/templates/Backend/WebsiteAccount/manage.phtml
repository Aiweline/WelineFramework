<?php
/**
 * @var \Weline\Framework\View\Template $this
 */

$website = $this->getData('website') ?: [];
$scope = (string)($this->getData('scope') ?: '');
$websiteId = (int)($website['website_id'] ?? $website['id'] ?? 0);
$websiteName = (string)($website['name'] ?? __('当前网站'));
$isAjax = $this->request->isAjax() || $this->request->getHeader('X-Requested-With') === 'XMLHttpRequest';
?>
<?php if (!$isAjax): ?>
<!DOCTYPE html>
<html lang="zh-CN" data-w-area="backend" data-theme-preference="system" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars(__('管理站点 SEO 账户启用'), ENT_QUOTES, 'UTF-8') ?> - <?= htmlspecialchars($websiteName, ENT_QUOTES, 'UTF-8') ?></title>
    <w:block class="Weline\Backend\Block\Header\Base" template="Weline_Backend::header/base.phtml"/>
    <link href="@static(Weline_Theme::ui/weline-foundation.css)" rel="stylesheet">
    <link href="@static(Weline_Theme::ui/weline-backend.css)" rel="stylesheet">
    <script type="module" src="@static(Weline_Theme::ui/weline-ui.js)"></script>
</head>
<body><main class="w-container" style="--w-pt:var(--weline-space-6);--w-pb:var(--weline-space-6);">
<?php endif; ?>

<?= framework_view_process_block([
    'class' => \Weline\Seo\Block\Widget\WebsiteAccountEnablement::class,
    'website_id' => $websiteId,
    'website' => $website,
    'scope' => $scope,
    'mode' => 'standalone',
    'field_prefix' => '',
    'show_submit' => true,
]) ?>

<?php if (!$isAjax): ?>
</main></body>
</html>
<?php endif; ?>
