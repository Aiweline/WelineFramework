<?php
/**@var \Weline\Framework\View\Template $this */


/**
 * 变量：
 * @var array $subjects SEO主体列表
 * @var string $title 页面标题
 */

$subjects = $this->getData('subjects') ?: [];
$title = $this->getData('title') ?: __('SEO总览');
?>
<!-- 消息提示 -->
                <div class='row'>
                    <div class='col-12'>
                        <template>Weline_Component::message.phtml</template>
                    </div>
                </div>
                
                <!-- Page Title -->
                <div class='row'>
                    <div class='col-12'>
                        <div class='page-title-box d-sm-flex align-items-center justify-content-between'>
                            <h4 class='mb-sm-0'>
                                <i class="mdi mdi-view-dashboard me-2"></i>
                                <?= htmlspecialchars($title) ?>
                            </h4>
                            <div class='page-title-right'>
                                <ol class='breadcrumb m-0'>
                                    <li class='breadcrumb-item'><a href='javascript: void(0);'>
                                            <lang>SEO管理</lang>
                                        </a></li>
                                    <li class='breadcrumb-item active'>
                                        <lang>总览</lang>
                                    </li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Stats Cards -->
                <div class="row">
                    <div class="col-xl-3 col-md-6 mb-3">
                        <div class="stat-card">
                            <i class="mdi mdi-file-document-multiple stat-icon"></i>
                            <div class="stat-label"><lang>SEO主体总数</lang></div>
                            <div class="stat-number"><?= count($subjects) ?></div>
                        </div>
                    </div>
                    
                    <div class="col-xl-3 col-md-6 mb-3">
                        <div class="stat-card success">
                            <i class="mdi mdi-tag-multiple stat-icon"></i>
                            <div class="stat-label"><lang>关键词总数</lang></div>
                            <div class="stat-number">
                                <?php
                                $totalKeywords = 0;
                                foreach ($subjects as $subject) {
                                    $totalKeywords += $subject['keyword_count'] ?? 0;
                                }
                                echo $totalKeywords;
                                ?>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-xl-3 col-md-6 mb-3">
                        <div class="stat-card info">
                            <i class="mdi mdi-check-circle stat-icon"></i>
                            <div class="stat-label"><lang>已启用主体</lang></div>
                            <div class="stat-number"><?= count($subjects) ?></div>
                        </div>
                    </div>
                    
                    <div class="col-xl-3 col-md-6 mb-3">
                        <div class="stat-card">
                            <i class="mdi mdi-trending-up stat-icon"></i>
                            <div class="stat-label"><lang>平均关键词数</lang></div>
                            <div class="stat-number">
                                <?php
                                $avgKeywords = count($subjects) > 0 
                                    ? round($totalKeywords / count($subjects)) 
                                    : 0;
                                echo $avgKeywords;
                                ?>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- SEO主体列表 -->
                <div class="row">
                    <div class="col-12">
                        <?php if (empty($subjects)): ?>
                            <div class="card dashboard-card">
                                <div class="card-body">
                                    <div class="empty-state">
                                        <i class="mdi mdi-file-document-outline"></i>
                                        <h5><lang>暂无SEO主体</lang></h5>
                                        <p class="text-muted"><lang>当前没有启用的SEO主体，请先创建SEO主体。</lang></p>
                                    </div>
                                </div>
                            </div>
                        <?php else: ?>
                            <?php foreach ($subjects as $subject): ?>
                                <div class="card dashboard-card">
                                    <div class="card-body">
                                        <div class="subject-header">
                                            <div>
                                                <h5 class="subject-title">
                                                    <?= htmlspecialchars($subject['title'] ?? __('未命名主体')) ?>
                                                </h5>
                                                <?php if (!empty($subject['url'])): ?>
                                                    <small class="text-muted">
                                                        <i class="mdi mdi-link-variant me-1"></i>
                                                        <a href="<?= htmlspecialchars($subject['url']) ?>" target="_blank" class="text-decoration-none">
                                                            <?= htmlspecialchars($subject['url']) ?>
                                                        </a>
                                                    </small>
                                                <?php endif; ?>
                                            </div>
                                            <div class="subject-meta">
                                                <span class="badge badge-custom bg-info">
                                                    <i class="mdi mdi-tag me-1"></i>
                                                    <?= $subject['keyword_count'] ?? 0 ?> <lang>个关键词</lang>
                                                </span>
                                                <a href="<?= $this->getUrl('*/backend/dashboard/subject', ['id' => $subject['subject_id']]) ?>" 
                                                   class="btn btn-view-detail">
                                                    <i class="mdi mdi-eye me-1"></i>
                                                    <lang>查看详情</lang>
                                                </a>
                                            </div>
                                        </div>
                                        
                                        <?php if (!empty($subject['description'])): ?>
                                            <p class="text-muted mb-3">
                                                <?= htmlspecialchars(mb_substr($subject['description'], 0, 200)) ?>
                                                <?= mb_strlen($subject['description']) > 200 ? '...' : '' ?>
                                            </p>
                                        <?php endif; ?>
                                        
                                        <?php if (!empty($subject['keywords']) && is_array($subject['keywords'])): ?>
                                            <div class="keyword-list">
                                                <?php foreach ($subject['keywords'] as $index => $keyword): ?>
                                                    <div class="keyword-item">
                                                        <span class="priority"><?= $index + 1 ?></span>
                                                        <?= htmlspecialchars($keyword['keyword'] ?? '') ?>
                                                        <?php if (!empty($keyword['priority'])): ?>
                                                            <span class="badge bg-secondary ms-2"><?= $keyword['priority'] ?></span>
                                                        <?php endif; ?>
                                                    </div>
                                                <?php endforeach; ?>
                                                <?php if (($subject['keyword_count'] ?? 0) > count($subject['keywords'])): ?>
                                                    <div class="keyword-item">
                                                        <i class="mdi mdi-dots-horizontal me-1"></i>
                                                        <lang>还有 <?= ($subject['keyword_count'] ?? 0) - count($subject['keywords']) ?> 个关键词</lang>
                                                    </div>
                                                <?php endif; ?>
                                            </div>
                                        <?php else: ?>
                                            <div class="alert alert-info mb-0">
                                                <i class="mdi mdi-information me-2"></i>
                                                <lang>该主体暂无关键词</lang>
                                            </div>
                                        <?php endif; ?>
                                    </div>
                                </div>
                            <?php endforeach; ?>
                        <?php endif; ?>
                    </div>
                </div>
