<?php
/**@var \Weline\Framework\View\Template $this */


/**
 * 变量：
 * @var array $subjects SEO主体列表
 * @var string $title 页面标题
 */

$subjects = $this->getData('subjects') ?: [];
$title = $this->getData('seo_page_title') ?: __('SEO总览');
?>
<!-- 消息提示 -->
                <div class="w-grid">
                    <div style="--w-span:12;">
                        <template>Weline_Component::message.phtml</template>
                    </div>
                </div>
                
                <!-- Page Title -->
                <div class="w-grid" data-testid="seo-dashboard-management">
                    <div style="--w-span:12;">
                        <div class="w-backend-page__heading w-cluster" data-align="center" data-justify="between">
                            <h4 style="--w-mb-sm:0;">
                                <w:icon name="grid" size="sm"></w:icon>
                                <?= htmlspecialchars($title) ?>
                            </h4>
                            <div class="w-cluster">
                                <ol class="w-breadcrumb" style="--w-mt:0;--w-me:0;--w-mb:0;--w-ms:0;">
                                    <li class="w-breadcrumb__item"><span>
                                            <lang>SEO管理</lang>
                                        </span></li>
                                    <li class="w-breadcrumb__item" data-state="active">
                                        <lang>总览</lang>
                                    </li>
                                </ol>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Stats Cards -->
                <div class="w-grid">
                    <div style="--w-span-lg:3;--w-span-md:6;--w-mb:var(--weline-space-4);">
                        <div class="stat-card">
                            <w:icon name="file" size="sm"></w:icon>
                            <div class="stat-label"><lang>SEO主体总数</lang></div>
                            <div class="stat-number"><?= count($subjects) ?></div>
                        </div>
                    </div>
                    
                    <div style="--w-span-lg:3;--w-span-md:6;--w-mb:var(--weline-space-4);">
                        <div class="stat-card success">
                            <w:icon name="tag" size="sm"></w:icon>
                            <div class="stat-label"><lang>关键词总数</lang></div>
                            <div class="stat-number">
                                <?php
                                $totalKeywords = 0;
                                foreach ($subjects as $subject) {
                                    $totalKeywords += $subject['keyword_count'] ?? 0;
                                }
                                echo $totalKeywords;
                                ?>
                            </div>
                        </div>
                    </div>
                    
                    <div style="--w-span-lg:3;--w-span-md:6;--w-mb:var(--weline-space-4);">
                        <div class="stat-card info">
                            <w:icon name="check" size="sm"></w:icon>
                            <div class="stat-label"><lang>已启用主体</lang></div>
                            <div class="stat-number"><?= count($subjects) ?></div>
                        </div>
                    </div>
                    
                    <div style="--w-span-lg:3;--w-span-md:6;--w-mb:var(--weline-space-4);">
                        <div class="stat-card">
                            <w:icon name="circle" size="sm"></w:icon>
                            <div class="stat-label"><lang>平均关键词数</lang></div>
                            <div class="stat-number">
                                <?php
                                $avgKeywords = count($subjects) > 0 
                                    ? round($totalKeywords / count($subjects)) 
                                    : 0;
                                echo $avgKeywords;
                                ?>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- SEO主体列表 -->
                <div class="w-grid">
                    <div style="--w-span:12;">
                        <?php if (empty($subjects)): ?>
                            <div class="w-card dashboard-card">
                                <div class="w-card__body">
                                    <div class="empty-state">
                                        <w:icon name="file" size="sm"></w:icon>
                                        <h5><lang>暂无SEO主体</lang></h5>
                                        <p class="w-text" data-tone="muted"><lang>当前没有启用的SEO主体，请先创建SEO主体。</lang></p>
                                    </div>
                                </div>
                            </div>
                        <?php else: ?>
                            <?php foreach ($subjects as $subject): ?>
                                <div class="w-card dashboard-card">
                                    <div class="w-card__body">
                                        <div class="subject-header">
                                            <div>
                                                <h5 class="subject-title">
                                                    <?= htmlspecialchars($subject['title'] ?? __('未命名主体')) ?>
                                                </h5>
                                                <?php if (!empty($subject['url'])): ?>
                                                    <small class="w-text" data-tone="muted">
                                                        <w:icon name="link" size="sm"></w:icon>
                                                        <a href="<?= htmlspecialchars($subject['url']) ?>" target="_blank" data-w-decoration="none">
                                                            <?= htmlspecialchars($subject['url']) ?>
                                                        </a>
                                                    </small>
                                                <?php endif; ?>
                                            </div>
                                            <div class="subject-meta">
                                                <span class="w-badge badge-custom" data-w-background="info">
                                                    <w:icon name="tag" size="sm"></w:icon>
                                                    <?= $subject['keyword_count'] ?? 0 ?> <lang>个关键词</lang>
                                                </span>
                                                <a href="<?= $this->getUrl('*/backend/dashboard/subject', ['id' => $subject['subject_id']]) ?>" class="w-button btn-view-detail">
                                                    <w:icon name="eye" size="sm"></w:icon>
                                                    <lang>查看详情</lang>
                                                </a>
                                            </div>
                                        </div>
                                        
                                        <?php if (!empty($subject['description'])): ?>
                                            <p class="w-text" data-tone="muted" style="--w-mb:var(--weline-space-4);">
                                                <?= htmlspecialchars(mb_substr($subject['description'], 0, 200)) ?>
                                                <?= mb_strlen($subject['description']) > 200 ? '...' : '' ?>
                                            </p>
                                        <?php endif; ?>
                                        
                                        <?php if (!empty($subject['keywords']) && is_array($subject['keywords'])): ?>
                                            <div class="keyword-list">
                                                <?php foreach ($subject['keywords'] as $index => $keyword): ?>
                                                    <div class="keyword-item">
                                                        <span class="priority"><?= $index + 1 ?></span>
                                                        <?= htmlspecialchars($keyword['keyword'] ?? '') ?>
                                                        <?php if (!empty($keyword['priority'])): ?>
                                                            <span class="w-badge ms-2" data-w-background="muted"><?= $keyword['priority'] ?></span>
                                                        <?php endif; ?>
                                                    </div>
                                                <?php endforeach; ?>
                                                <?php if (($subject['keyword_count'] ?? 0) > count($subject['keywords'])): ?>
                                                    <div class="keyword-item">
                                                        <w:icon name="more-horizontal" size="sm"></w:icon>
                                                        <lang>还有 <?= ($subject['keyword_count'] ?? 0) - count($subject['keywords']) ?> 个关键词</lang>
                                                    </div>
                                                <?php endif; ?>
                                            </div>
                                        <?php else: ?>
                                            <div class="w-alert" data-tone="info" style="--w-mb:0;">
                                                <w:icon name="info" size="sm"></w:icon>
                                                <lang>该主体暂无关键词</lang>
                                            </div>
                                        <?php endif; ?>
                                    </div>
                                </div>
                            <?php endforeach; ?>
                        <?php endif; ?>
                    </div>
                </div>
