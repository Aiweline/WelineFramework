<!-- weline:module-development-baseline:generated-once -->
# Weline_Seo 开发日志

## 2026-09-18 — canonical 默认货币与 State 对齐（1.2.95）

- 现象：非默认货币路径（如 `/CNY/...`）被 `canonicalPath` 误剥；与 I18n hreflang 货币策略不一致时触发 `canonical path` FAIL（Expected `/USD/...` Got 无货币段）。
- 变更：`PageSeoContextResolver::canonicalPath` 优先 `State::resolveWebsiteDefaultCurrency()`；在生成 hreflang **之前**先 `canonicalizeUrl`；`syncSelfHreflangToCanonical` 保证 self = final canonical。
- 版本：`1.2.94` → `1.2.95`。

## 2026-09-15 — Provider 不得覆盖实体 bag 主 SEO（1.2.94）

- 现象：`LayoutSeoFallbackContractTest` 中 bag `title` 被 `ProductSeoProfileProvider` 经 `mergeProviderContext` 覆盖成商品 `meta_name`。
- 变更：`PageSeoContextResolver::mergeProviderContext` 对已解析的 title/description 等主字段与已有 breadcrumbs 保底，Provider 只补空与结构化事实。
- 版本：`1.2.93` → `1.2.94`。

## 2026-09-15 — About/壳页 BreadcrumbList（1.2.93）

- 现象：SEO 面板 CONTRACT「面包屑」失败——本页期望 BreadcrumbList，JSON-LD 未输出；`/about` 曾被 URL 启发式误判为 contact。
- 变更：Theme `Policy::assignThemeShellSeo` 对公开壳发布 Home→当前页 breadcrumbs；inspector 独立 `about` 规则（AboutPage+BreadcrumbList）；URL `/about` → `about`。
- 版本：Seo `1.2.92` → `1.2.93`；Theme `2.2.364` → `2.2.365`。

## 2026-09-15 — 布局 SEO 兜底桥（实体优先，1.2.92）

- 诉求：Theme 布局显式 SEO 能进入独立 `<w:seo>` head；实体/控制器 bag 永远优先；`@meta.name` 不进公开 title。
- 变更：`SeoPageProfileBag` 独立 `layout_fallback`；`PageSeoContextResolver` 优先级链插入 fallback；规格 `doc/开发/spec/layout-seo-fallback.md`；契约 `LayoutSeoFallbackContractTest`。
- 版本：`1.2.91` → `1.2.92`。

## 2026-09-09 — Sitemap 采样 help/* → faq/*

- `SitemapAuditUrlSampler` 重复结构改为 `faq/*`，Hub 标签 `faq`；UT 断言同步。
- `ContentDuplicateScan` 补 `unlock_timeout()`，避免 setup:upgrade 末段 cron collect 致命错误。

## 2026-09-09 — 长安汉服真实主体种子（1.2.91）

- 诉求：禁止 example.com /「示例画像」假种子；须与实站一致。
- 变更：绑定默认站 `website/0`；标题/描述对齐 `HanfuHomepageSeoProfileProvider` 与首页 meta；关键词为长安汉服/明制/宋制/唐制/马面裙；URL 从 WebsiteDomain 优先 `*.test.weline.com`；清理历史 `900001` 假种子。
- 版本：`1.2.90` → `1.2.91`。

## 2026-09-09 — 汉服默认 SEO 主体种子（1.2.90）

- 诉求：主体管理安装后为空，运营不知「画像」怎么用。
- 变更：初版种子（后被 1.2.91 纠正为实站长安文案）。
- 版本：`1.2.89` → `1.2.90`。

## 2026-09-09 — 审图：SEO 总览空态乱（1.2.89）

- 附图：SEO 总览四行扁平 KPI + 无 CTA 空盒；用户「很乱」。
- E/F fail：无用途说明、无主按钮、未对齐 `seo-admin` / 账户页节奏。
- 变更：`Dashboard/index.phtml` 变体 A（头图说明 + `w-stat-tiles` + 空态 CTA + 推荐下一步）；关键词计数修正为全量；原型 `seo-dashboard-overview-ui.html`。
- 版本：`1.2.88` → `1.2.89`。

## 2026-09-09 — 分发渠道「为什么配置」提示（1.2.88）

- 诉求：说清 Platform Property 区用途，避免误解为自动分发或加信任分。
- 变更：`GoogleSearchConsoleAdapter` 分区 hint + 账户表单 Google 指南/能力摘要；契约 UT。
- 版本：`1.2.87` → `1.2.88`。

## 2026-09-09 — Helpful Content：Why 意图不计 tip 且不标 pass（1.2.87）

- 现象：EN 博客详情 Google 自测常驻 2 tip（Person 身份 + Why manual）；1.2.85 恒 tip 与「清零可修 tip」冲突，1.2.83 标 pass 又违反审图（意图不可机判）。
- 变更：`why_primary_audience` 仅在主内容代理失败时 tip；代理通过时省略该项（区头 note 仍提示人工对照），绝不标「已通过」。
- Person tip 由 Blog 作者 url/sameAs 回填清零。
- 版本：`1.2.86` → `1.2.87`。

## 2026-09-09 — 审图：平台卡片过挤（1.2.86）

- 附图：推荐接入 Google/Bing/百度/IndexNow 四卡贴边；用户「挤成这样」。
- E/F fail：`auto-fill` + `minmax(9.5rem)` 在宽容器留下空列，卡片被挤到约 164px；`space-3` gap/padding 过密。
- 变更：`seo-platform-grid` → `repeat(auto-fit,minmax(12rem,1fr))` + `gap/padding: --weline-space-4`；logo 32px；原型同步。
- 验收：UT OK；Browser 实测 `gap=16px`、`padding=16px`、卡宽≈382、列数 4（`?v=2.2.286_platform_gap`）。
- 版本：`1.2.85` → `1.2.86`。

## 2026-09-09 — 审图：Why 人工项勿标已通过 + 双轨说明（1.2.85）

- 附图：Why「audience & intent (manual)」出现在「已通过」→ 人性化 fail（官方 Why 意图不可机判）。
- 修复：该条恒为 tip；自测注记写明与上方「对照 Google 官方示例」JSON-LD 字段对齐是两套机制。
- 版本：`1.2.84` → `1.2.85`。

## 2026-09-09 — 修复重复报告 URL 缺后台前缀 404（1.2.84）

- 现象：`https://…:9555/seo/backend/duplicate/report?run_id=1` 前台 404；正确应为 `/{backend_frontName}/seo/backend/duplicate/report?run_id=`。
- 根因：`DuplicateReportUrlBuilder::absoluteForRun` 只取 origin，丢掉 area frontName；模板硬编码裸 `/seo/backend/...`。
- 变更：path/absolute 默认带 frontName；报告页展示与历史列表一律实时重建 URL；模板改用 `panel_path` / `report_path_*`。
- 验收：UT `DuplicateReportUrlBuilderTest` OK；探活带前缀 URL → 登录重定向（非 404）。
- 版本：`1.2.83` → `1.2.84`。

## 2026-09-09 — Why 人工 tip 改机检代理通过（1.2.83）

- 现象：主内容充足时仍常驻 tip「Why: audience & intent (manual)」，与 Who 可修 tip 并列易误解。
- 变更：`why_primary_audience` 在主内容代理通过时记 pass（详情含人工对照说明）；仅代理失败时仍 tip。
- 版本：`1.2.82` → `1.2.83`。

## 2026-09-09 — 审图：Helpful Content Why 维与通过项可见（1.2.82）

- 附图 SEO 面板：文案 Who/How/Why 但无 Why 列；How 全通过时像空列（人性化 fail）。
- 修复：补 `Why` 维（机检代理 + 人工意图 tip）；各维展示「已通过 N 项」摘要。
- 关联数据：博客 `author_*` 列落地并回填样例文 Person url/sameAs。
- 版本：`1.2.81` → `1.2.82`。

## 2026-09-09 — 平台卡片真实品牌 Logo（1.2.81）

- 诉求：平台选择卡片字母色块过丑，改用真实平台 logo。
- 资源：`view/statics/platform-logos/*.svg`（约 35 个主流引擎）。
- 渲染：`PlatformVisual::renderIcon` 优先内联对应 SVG；无资源时回退字母标。
- CSS：`.seo-platform-logo`（主题 token 边框/圆角）；原型 `seo-platform-tabs-ui.html` 同步 `img` 引用。
- 验收：`PlatformVisualLogoContractTest` OK；Browser 原型 A 可见 Google 四色标。

## 2026-09-09 — 审图：内容重复检测面板 UI（1.2.80）

- 分流：后台「内容重复检测」截图 → `web_ui`；用户质疑「原型和 UI 没参与」。
- 结论：首轮只落功能扁平行表单，未走 prototype / frontend-design；E/F/G fail。
- 原型：`view/statics/prototype/duplicate-panel-ui.html` 三变体（A 分区表单 / B 左右分栏 / C 向导）；生产落点 **A**（对齐账户页 `seo-form-section`）。
- 变更：`Duplicate/index.phtml`、`report.phtml` 改用 `w-field`/`w-input`/`w-select` + 分区 + empty-state；`seo-admin.css` 增 `seo-dup-meta` 等（主题 token）；契约 UT `DuplicatePanelThemeContractTest`。
- 验收：UT `OK (2 tests, 27 assertions)`；Browser DOM：`variant=A`、`seo-form-section×4`、`w-field×7`、`seo-switch-card`；报告页 KPI + 范围 meta + 问题对表可见。
- 版本：`1.2.79` → `1.2.80`。

## 2026-09-09 — 审图：账户平台选择分组 Tab（1.2.79）

- 分流：后台「新增 SEO 账户」平台墙截图 → `web_ui`。
- 结论：平台为单选必填（非全填）；一屏 30+ 卡片 E/F fail（密度过高）。
- 变更：`SeoPlatformGroupCatalog` + 账户表单 `w-tabs` 分组（推荐/IndexNow/中国/隐私/区域）；卡片样式迁入 `seo-admin.css` 主题 token；隐藏原生 select 保留校验。
- 原型：`view/statics/prototype/seo-platform-tabs-ui.html` 三变体，生产落点 A（顶栏 Tab）。
- 版本：`1.2.78` → `1.2.79`。

## 2026-09-09 — Google Helpful Content 自测对齐（1.2.78）

- 目标：官方 Who/How/Why 有可机检规则的部分，做成站内自测；明确 ≠ 排名门槛、≠ 完整 E-E-A-T 评分。
- 变更：面板 `EEAT_STRICT_RULES` 改为 Who/How/Trust ID（`who_*` / `how_*` / `trust_*`）；`auditEeatStrict` 完整文章 Who 路径（可见 byline、byline↔Person、url|sameAs、publisher）；Experience 误导项降为 `how_content_substance`；Profile/爬审软告警对齐 url|sameAs；旧 ID 保留别名。
- 版本：`1.2.77` → `1.2.78`。

## 2026-09-09 — 站内正文近重复检测与报告 URL（1.2.77）

- 需求：`REQ-SEO-0002` — 按范围扫描站内正文近重复；报告 URL 为一等产物；`w_msg` 必须带报告地址。
- 变更：MinHash+Jaccard；`SeoDupDoc/Pair/Run`；面板/报告页；Cron `seo_content_duplicate_scan`；topic `seo_duplicate_content`。
- 报告契约：`/seo/backend/duplicate/report?run_id=`。
- 验收：UT `Test/Unit/Service/Duplicate/*` 10 通过；登录后报告页 200，含 `seo-duplicate-report` / 问题对表。
- 交付：`https://p05113ef3.test.weline.com:9555/jRaxfEJaRUyO6ZBOA3wJX8bituje6oqH/seo/backend/duplicate/report?run_id=1`
- 版本：`1.2.76` → `1.2.77`。

## 2026-09-09 — Google Discover / 分发渠道后台配置（1.2.76）

- 目标：后台 Google 账户可配置 Discover/News 统计拉取，并登记 YouTube/X/Instagram/TikTok/LinkedIn 主页对照 GSC Platform Property。
- 实现：`GoogleSearchConsoleAdapter` 扩展 section/checkbox/url 字段；`SeoPlatformCapabilityService` 放行 `section`+`group`；账户表单渲染分区与渠道网格；`GoogleSitemapAdapter::getStats` 按开关拉 `type=discover`/`googleNews`，渠道写入 `extra.distribution_channels`；Platform Property 标明 `manual_only`（官方 API 未开放）。
- 原型：`view/statics/prototype/gsc-channels-ui.html` 三变体，落点采用变体 A（分区堆叠）。
- 验收：契约 UT；Browser 验原型与账户表单。
- 版本：`1.2.75` → `1.2.76`。

## 2026-09-09 — E-E-A-T 严苛检测分区（1.2.75）

- 目标：Helpful Content / E-E-A-T 技术信号可在 SEO 面板指出缺口（非硬收录门槛）。
- 变更：`inspector.js` 新增 `EEAT_STRICT_RULES` / `auditEeatStrict` / `renderEeatStrictSection`（富文本 Tab）；`scoringExempt` 不改四维本地分；`SeoProfileValidationService` 浅作者 / 缺 `sameAs` 软告警；爬审 `auditEeatStrictStructuredSignals`（`eeat_org_sameas` / `eeat_article_author_missing`）。
- 版本：`1.2.74` → `1.2.75`。

## 2026-09-09 — 富结果对照完整展示本站 JSON（1.2.74）

- 现象：对照区把 `availableLanguage` 写成 `"… +6"`，易被误认为页面真输出；博客 `CollectionPage` 被当成电商列表警告。
- 变更：对照区完整输出本站字段（含语言列表）；注明 `{search_term_string}` 是 Google 必填字面量；`blog_list` CollectionPage/ItemList 改 note 不误报。
- 版本：`1.2.73` → `1.2.74`；asset `rich-full-json-1`。

## 2026-09-09 — 博客列表 CollectionPage+Article ItemList（1.2.73）

- 现象：博客列表富结果只见 OnlineStore/WebSite，像首页站点壳；电商策略曾 stub 掉全部 ItemList。
- 变更：仅 `blog_list`/`blog_category` 输出 `CollectionPage` + `ItemList(BlogPosting)`；电商列表仍不输出 Product ItemList。
- 版本：`1.2.72` → `1.2.73`；asset `blog-list-itemlist-1`。

## 2026-09-09 — 博客列表富结果勿按文章页（1.2.72）

- 现象：`/USD/en_US/blog` 富文本 Tab 标「博客页」并失败（缺 Article）；CONTRACT 已是「列表页 · WebPage · pass」。
- 根因：`inferSeoTypeFromUrlPath` 把任意 `/blog` 当成文章页；URL 推断盖住 `page-type=blog-list`。
- 变更：列表/分类 → `collection`；仅 `/blog/{slug}` → `blog`；显式 `blog_list`/`blog_category` 优先；asset `blog-list-rich-1`。
- 版本：`1.2.71` → `1.2.72`。

## 2026-09-09 — 本地 CWV/Google 外部项只说明不扣分（1.2.71）

- 现象：引擎适配被本地 Performance/CWV、Google 平台专项 Warning 拖到约 87。
- 变更：本机无法验真的 CWV/CrUX/GSC/平台外部项改为 `info` + `scoringExempt`；矩阵标「说明·不扣分」；引擎区顶部醒目蓝条解释；卡片展示说明块。真实 Fail/Warning 仍扣分。
- 版本：`1.2.70` → `1.2.71`；asset `cwv-info-no-deduct-1`。

## 2026-09-09 — 汇总卡标明「项」避免当成分数（1.2.70）

- 现象：顶栏 Passed 77 被当成「77 分」。
- 变更：汇总改为「N 项 / 通过|失败|警告|提示」；asset `summary-items-1`。
- 版本：`1.2.69` → `1.2.70`。

## 2026-09-09 — SEO 面板 ogImage 未定义（1.2.69）

- 现象：首页 SEO Tab 只显示 `ogImage is not defined`。
- 根因：`inspector.js` 用了 `ogImage`/`twitterImage` 未声明。
- 变更：补 `meta[property=og:image]` / `meta[name=twitter:image]` 读取；asset `ogimage-def-1`。
- 版本：`1.2.68` → `1.2.69`。

## 2026-09-09 — tip 项写明不扣分原因（1.2.68）

- 现象：`static compression` 在本机以 WARN 计入「需处理」，生产却会自动压缩。
- 变更：压缩/未 minify/DEV 大 CSS/第三方大 JS 改为 tip；Issue 区增加「提示 · 不扣分」，detail/建议写明生产自压或 DEV 预期等不扣分原因。
- 版本：`1.2.67` → `1.2.68`；asset `tip-no-score-1`。

## 2026-09-09 — 补齐 normalizeBreadcrumbTrails（1.2.67）

- 现象：首页 500 `Call to undefined method ...::normalizeBreadcrumbTrails()`。
- 根因：context 写入 trails 的调用已进 Worker，方法体曾丢失/未同步。
- 变更：补回 `normalizeBreadcrumbTrails`；context 写入 `breadcrumb_trails`；provider merge 整表替换 trails。
- 版本：`1.2.66` → `1.2.67`。

## 2026-09-09 — 分数区「本地优先」引导（1.2.66）

- 需求：本地做不了的（CrUX/GSC/外链）提示清楚、本地可不纠结；展示时标明主看哪些分。
- 变更：分数卡上方「本地优先看什么」；可收录/可理解/体验标「本地主看」，引擎适配标「本地参考」；限制区改名并写明外部项。
- 版本：`1.2.65` → `1.2.66`；asset `local-focus-copy-1`。

## 2026-09-09 — 可修项做好 / 外部项提示清楚（1.2.64）

- 原则：能修的修好；做不了的（CrUX/站长后台）标明【外部验证】；真实问题继续扣分。
- 变更：详情区空 alt 不再当装饰图放过；DEV 大 CSS 改 tip、生产仍大继续 warn 扣分；抽屉/dialog 标题不进大纲；CWV 文案写明面板不能代替 CrUX。
- 版本：`1.2.63` → `1.2.64`；asset `fixable-external-1`。

## 2026-09-09 — 个人中心内部不要求 seo::footer（1.2.65）

- 配合 Theme 2.2.274：覆盖契约排除账户中心内部布局；登录/注册壳仍要求挂点。
- 版本：`1.2.64` → `1.2.65`。

## 2026-09-09 — 店面布局 seo::footer 覆盖契约（1.2.63）

- 博客漏挂后补扫：Theme 全量 + Product `best_sellers` + Customer `contact` 均需 `seo::footer`。
- 新增 `SeoFooterLayoutCoverageContractTest` 防回归；迷你购物车预览布局除外。
- 版本：`1.2.62` → `1.2.63`。

## 2026-09-09 — 站内可做 SEO 收口：绝对 canonical 与 author（1.2.61）

- 现象：博客/Help 输出相对 canonical；BlogPosting `author.name=Array`。
- 变更：`PageSeoContextResolver` 在 Provider 合并后强制绝对化 canonical/url/面包屑；`articleAuthors` 正确解析 authors 列表，禁止 `(string)array`。
- 版本：`1.2.60` → `1.2.61`。

## 2026-09-09 — EUR 列表页 SEO 面板与货币路径（1.2.60）

- 配合 Product `1.0.205`：列表补 og:image/keywords、缩短英文 title。
- 面板：`languageSegmentFromUrl` 剥 ISO 货币段；title 略超长改为 warn；CRITICAL 去掉 meta keywords。
- 版本：`1.2.59` → `1.2.60`；asset `eur-products-seo-1`。

## 2026-09-09 — 首页 SEO 面板误扣分修正（1.2.59）

- 现象：首页可理解性 ~49 / 体验 ~58；`-16 meta keywords`、chrome 区 Heading jumps、`og:image:alt` 与 title 对齐误 warn、传输体积把 Google 性能打成 FAIL。
- 变更：keywords 缺失改为 `info` 不扣分；扩大 `SEO_AUDIT_IGNORE_SELECTOR` 并排除 header/footer/nav 标题污染；内容图过滤 ignored 节点 + CJK 有用 alt；`og:image:alt` 有描述即过；传输/资源数仅 warn 不因体积 FAIL；首页无 BreadcrumbList 改为 `info`（不再 `-7 Breadcrumb items`）。
- 版本：`1.2.58` → `1.2.59`；asset `home-score-fix-1`。

## 2026-09-09 — 同源 Sitemap 探测，去掉「缺失」误导（1.2.58）

- 现象：本机 robots/sitemap 正常，但搜索平台关注点含「sitemap」+ 本地 CWV FAIL，看起来像 Sitemap 缺失。
- 探活：`/sitemap.xml` 200（3 子文件 200）；`robots.txt` 含 `Sitemap:`；商品 slug 在 product sitemap。
- 变更：`ensureSitemapProbe`；矩阵 Sitemap 显示发现链；关注点标「非失败项」；本地 CWV 一律 warn。
- 版本：`1.2.57` → `1.2.58`。

## 2026-09-09 — OnlineStore.sameAs 由 Theme 页脚/长安兜底补齐（配合 Theme 2.2.270）

- 对照 Google Organization/OnlineStore 示例缺 `sameAs`；发射侧 `HeadRenderer` 本已支持，缺的是 context 数据。
- 数据由 Theme 注入：页脚真实社交链优先；长安演示站无配置时兜底四条社媒 URL。
- 版本：文档同步；模块版本仍 `1.2.57`（逻辑在 Theme）。

## 2026-09-09 — 首页误报 BreadcrumbList 与 CONTRACT 普通页面（1.2.57）

- 现象：首页本地检测「建议 BreadcrumbList」；底部 CONTRACT 显示「普通页面 · WebPage」；对照区本站 JSON 被 `availableLanguage` 挤掉，看不到已有的 `potentialAction`。
- 根因：`page-type=homepage` 未别名到 `home`；Google BreadcrumbList 需 ≥2 个 ListItem，首页单级「首页」不能也不该硬造；对照压缩未优先保留 SearchAction。
- 变更：`homepage→home` 别名；首页期望去掉 optional BreadcrumbList；对照 JSON 优先输出 `potentialAction` 并截断语种列表；`HanfuHomepageSeoProfileProvider` 写 `page_type=home`；asset `home-omissions-1`。
- 版本：`1.2.56` → `1.2.57`。

## 2026-09-09 — 本地富结果对照 Google 官方示例（1.2.56）

- 需求：检测结果直接带 Google Search Central 官方 JSON-LD 示例，与本站输出并排对照。
- 变更：`GOOGLE_OFFICIAL_RICH_EXAMPLES`（BreadcrumbList / Organization·OnlineStore / WebSite / Product / Article / FAQ / Review / ItemList）；卡片「对照 Google 官方示例」双栏 + 差异条 + 文档链；asset `google-example-compare-1`。
- 版本：`1.2.55` → `1.2.56`。

## 2026-09-09 — 富结果检测 URL 行内按钮（1.2.55）

- 布局：`检测 URL` 输入框与右侧按钮同一行：`复制` / `本地测试|开始检测` / `填入当前页`；空间不足时 URL 用省略号，悬停 title 显示完整地址。
- 复制：始终复制 `window.location.href`（本站当前页正确地址），并同步写入输入框与持久化。
- 版本：`1.2.54` → `1.2.55`；inspector `url-row-copy-1`。

## 2026-09-09 — 列表页补齐 WebSite.potentialAction（1.2.54）

- 现象：本地富结果对 `/products/` 警告「建议补充 potentialAction」；首页/PDP 已有 SearchAction。
- 根因：`searchUrlTemplate` 用 `filter_var(..., FILTER_VALIDATE_BOOLEAN)`，空串也会被当成 false；坏的自定义模板缺 `{search_term_string}` 时直接放弃而非回退默认。
- 变更：仅显式 false/0/off 关闭；缺占位符回退 `/search?q={search_term_string}`；`CatalogSeoProfileProvider` 显式 `site_search_enabled=true`。
- 版本：`1.2.53` → `1.2.54`。

## 2026-09-09 — 补全全站 SearchAction 与 AggregateOffer.url（1.2.53）

- 现象：列表/首页 WebSite 缺 `potentialAction`（面板警告），PDP 却有；根因 `site_search_enabled` 缺省被 `(bool)''` 变成 false。
- 变更：缺省开启站内搜索 SearchAction；仅显式 false 才关闭；AggregateOffer 补 `url`；Offer 默认 `itemCondition=NewCondition`。
- 说明：`/products` 仍不输出 Product/ItemList（Google Carousel 不支持电商 Product）；商品富结果在详情页。
- 版本：`1.2.52` → `1.2.53`。

## 2026-09-09 — 按 Google 官方示例对齐结构化数据与检测（1.2.52）

- 目标：跟紧 Google Search Central Breadcrumb / Product / Carousel / Organization 示例，停止自造电商 ItemList 富结果。
- 发射：`BreadcrumbList` 无 `@id`、末级省略 `item`、非末级绝对 URL；列表/分类不再输出 Product `ItemList`/`CollectionPage`；壳为 `WebPage`；站点 `@type: OnlineStore`；FAQ 壳不再标第二个 FAQPage。
- 检测：列表页必测改为 BreadcrumbList+Org+WebSite；检出电商 Product ItemList / `#breadcrumb` 记失败或警告；去掉自造集合层级树。
- 版本：`1.2.51` → `1.2.52`；inspector `google-schema-align-1`。

## 2026-09-09 — 修复 SEO 面板 engineScore is not defined（1.2.51）

- 现象：打开 SEO 面板白屏，控制台 `Uncaught ReferenceError: engineScore is not defined`（`buildEngineMatrix`）。
- 根因：搜索平台矩阵重构时误删 `engineScore` / `engineStatus` / `engineRecommendations`。
- 变更：按既有扣分规则恢复三函数；asset `enginescore-restore-1`。
- 版本：`1.2.50` → `1.2.51`。

## 2026-09-08 — 搜索平台矩阵对齐官方规则目录（1.2.50）

- 需求：多平台检测须对照各引擎开发文档，禁止自创硬门槛。
- 变更：
  - `inspector.js` 增加 `SEARCH_ENGINE_RULE_CATALOG`（rule_id / engines / officialUrls / signal / severity / browserTestable）。
  - `buildEngineRows(engine, …)` 按引擎差异化公共行；去掉 Yahoo title 硬规、DDG Organization、Ecosia 隐私页、Yandex BreadcrumbList 伪硬规。
  - Sitemap 以 robots.txt `Sitemap:` 为主路径；favicon 按 Google 支持格式且不进 Indexability。
  - 百度空短页保留为标注启发式（非官方字数配额）。
- 契约：`SeoInspectorPanelGateContractTest` 断言 catalog / `buildEngineRows`；服务端审计文案不再把 HTML sitemap link 当主发现路径。
- 版本：`1.2.49` → `1.2.50`；inspector `engine-rule-catalog-1`。
## 2026-09-08 — Head `feeds` 发现链（1.2.49）

- 变更：`PageSeoContextResolver` 读取并规范化 `seo.feeds`；`HeadRenderer` 输出 `<link rel="alternate" type="application/rss+xml" …>`；`alternates` 仍仅作 hreflang。
- 契约：`SeoProfileExtensionGuide` 增加 `feeds` 字段说明；单测 `HeadRendererSeoProfileTest`。
- 版本：`1.2.48` → `1.2.49`。

## 2026-09-08 — 对齐 Google 文档：纠正误 FAIL + BreadcrumbList 绝对 URL（1.2.48）

- 现象：产品页 SEO 面板 5 Failed（robots meta / sitemap link / favicon svg+png）与 Issue「大 JS recaptcha」；BreadcrumbList 首页 `item:"/"`。
- 对照 Google Search Central：
  - 缺 robots meta 默认即 index,follow，不得 FAIL；不得要求固定 max-snippet 串。
  - Sitemap 以 robots.txt `Sitemap:` 为主，HTML `<link rel="sitemap">` 仅可选。
  - Favicon：任一支持的 `rel=icon` 即可，不得强制 SVG + PNG 32x32。
  - BreadcrumbList 示例：`ListItem.item` 为绝对 URL；末级可省略 `item`。
  - 第三方 reCAPTCHA 不可 tree-shake，不得按首包大 JS WARN。
- 变更：`inspector.js` REQUIRED_HEAD soft 化；大 JS 区分 first/third-party；面包屑校验绝对 URL；`HeadRenderer::breadcrumbNode` 绝对化并末级省略 item；默认输出 sitemap link；Product 面包屑首页绝对化、叶节点用商品名。
- 版本：`1.2.47` → `1.2.48`；inspector `breadcrumb-trail-1`（缓存戳刷新）。

## 2026-09-08 — 面包屑：展示层级路径，去掉误导性 #breadcrumb（1.2.47）

- 现象：面板/说明里出现 `…/products#breadcrumb`，看起来像「只给 Google 一个链接」。
- 事实：JSON-LD `itemListElement` 本就有「首页 › 商品列表」及各级绝对 URL；`@id` 不是 Google 必填。
- 变更：`breadcrumbNode` 不再输出 `#breadcrumb` `@id`；面板标题强制用 name 路径，并列出「路径」facts；校验至少 2 级、禁止把 fragment 当 ListItem.item。
- 版本：`1.2.46` → `1.2.47`；inspector `breadcrumb-trail-1`。

## 2026-09-08 — 产品页评价：有评才输出，无评不误警告（1.2.46）

- 现象：无已审评论的 PDP 本地富结果仍警告「评价未输出」；用户误以为结构化数据漏了评论。
- 说明：`ProductReviewSeoProfileProvider` + `HeadRenderer` 在有已审评论时会写 `review`/`aggregateRating`（已用有评商品验证）；无评不伪造。
- 变更：`PAGE_RICH_EXPECTATIONS.product.optional` 去掉 `Review`（改为机会性出现才校验）；为本机验收商品补已审样例评论后 JSON-LD 可见评价。
- 版本：`1.2.45` → `1.2.46`；inspector `v=20260908-weline-panel-seo-review-optional-1`。

## 2026-09-08 — 集合页 ItemList 层级可读性（1.2.45）

- 现象：面板把 CollectionPage / ItemList 画成平铺卡片，只显示页标题，看起来「没层级、不像全部商品」。
- 说明：JSON-LD 本就有 `CollectionPage.mainEntity → ItemList`；Google 电商商品星级富结果仍以 PDP 为准，列表页 ItemList 主要是理解/发现信号。
- 变更：列表项 `@type=Product`；ItemList 注明本页范围；面板增加层级树 + 条目预览；SKU 名优先用人话 slug。
- 版本：`1.2.44` → `1.2.45`；inspector `collection-hierarchy-1`。

## 2026-09-08 — 本地富结果：商品卡片按字段摘要（1.2.44）

- 现象：「商品摘要」只像长标题，甚至出现空逗号拼接，看不出品牌/价格/库存等对应值。
- 变更：`localRichItemPreview` 按类型抽取有价值字段（商品：类型/品牌/价格/库存/SKU/变体/图片；面包屑路径；列表条目数等）；卡片改标题 + 键值 facts；标签「商品摘要」→「商品」。
- 版本：`1.2.43` → `1.2.44`；inspector `v=20260908-weline-panel-seo-rich-facts-1`。

## 2026-09-08 — 审图：富文本检测 URL 单列布局与失配主 CTA（1.2.43）

- 分流：`web_ui`；用户空截图+红箭头：地址栏 `/products/` vs 检测框超长 PDP URL，控件挤一行很丑。
- 原型 verdict：失配横幅 → 全宽 monospace URL → 动作行；失配时主 CTA「填入当前页」。
- 变更：`renderToolUrlForm` + `url-mismatch` / `url-actions`；CSS 单列；填入当前页后 `refreshRichPanel`；asset `url-tool-layout-1`。
- 版本：`1.2.42` → `1.2.43`。

## 2026-09-08 — 检测 URL 粘滞：强制上线新 inspector（1.2.42）

- 现象：浏览器已在 `/products/`，检测 URL 仍是旧商品 `you-ai-…`；线上 HTML 仍引用 `rich-route-conflict-1`（WLS OPcache/旧 worker）。
- 变更：打开富文本/当前页 tab 时重渲染输入框；`applyPanelTabUi` 避免与 refresh 递归；异页 URL 不强制 sticky；asset `toolurl-sync-5`（含 mtime）。
- 版本：`1.2.41` → `1.2.42`。

## 2026-09-08 — 检测 URL 与当前页不同步（异页 slug 粘滞）（1.2.41）

- 现象：「当前页面」正确，但「检测 URL」仍回填另一个商品 slug（如旧 you-ai…）。
- 根因：`persistToolUrl` / 本地测试把异页 URL 以当前浏览页 `pageKey` 写入 localStorage；换商品后 path 不同仍被当作本页覆盖。
- 变更：仅同 path 才 sticky；异页 URL 可单次检测但不落库；打开时自动愈合污染状态；asset `toolurl-sync-3`（含 mtime）。
- 版本：`1.2.40` → `1.2.41`；inspector `v=20260908-weline-panel-seo-toolurl-sync-3`。

## 2026-09-08 — 富结果：商品 URL 落到列表页时标出跳转冲突（1.2.40）

- 现象：输入框是 `/product/...`，结果却判成列表页；根因常是商品不可用 **301 → /products**，不是检测器把 Product 错标成 ItemList。
- 变更：记录请求/最终 URL；路径语义冲突时顶部橙条警告；页类型按最终落地页推断；URL 强路由优先于被污染的 page-type meta。
- 版本：`1.2.39` → `1.2.40`；inspector `v=20260908-weline-panel-seo-rich-route-conflict-1`。

## 2026-09-08 — 富结果结果顶栏醒目页类型（1.2.39）

- 需求：本地检测结果顶部要一眼看出判定的页类型，便于对照是否检错。
- 变更：结果区顶部「判定页类型」横幅（中文名 + seoType）；期望/检出类型芯片；状态条文案带页类型。
- 版本：`1.2.38` → `1.2.39`；inspector `v=20260908-weline-panel-seo-rich-pagetype-banner-1`。

## 2026-09-08 — ItemList 补 numberOfItems（1.2.38）

- 现象：列表页本地富结果「建议补充 numberOfItems」。
- 变更：`HeadRenderer::itemListNode` 写入 `numberOfItems = count(itemListElement)`。
- 版本：`1.2.37` → `1.2.38`。

## 2026-09-08 — 富结果：product_list 误标文章页（1.2.37）

- 现象：`/products`（page_type=`product_list`）本地富结果写「文章页」，期望只剩 Organization/WebSite。
- 根因：别名表缺 `product_list` 等集合别名；`jsonLdRuleForSeoType` 未知类型回退 `article`。
- 变更：集合类 page_type 映射到 `collection`；未知回退 `web_page`。
- 版本：`1.2.36` → `1.2.37`；inspector `v=20260908-weline-panel-seo-rich-list-alias-1`。

## 2026-09-08 — 本地富结果全检测器（按页类型期望）（1.2.35）

- 需求：富结果目录不止当前页碰巧出现的类型；不同页类型期望项不同，缺期望类型应失败。
- 变更：`PAGE_RICH_EXPECTATIONS` + `inferSeoTypeForRich`；`buildLocalRichReport` 合并「有则验字段 / 缺 required→fail / 缺 optional→warn」；目录补 `CollectionPage`；结果 UI 展示页类型与期望清单。
- 版本：`1.2.34` → `1.2.35`；inspector `v=20260908-weline-panel-seo-rich-full-detector-1`。

## 2026-09-08 — 富结果警告：AggregateOffer.availability + 无评价不强求 rating（1.2.34）

- 现象：本地富结果 2 pass / 2 warn — 商品缺 `offers.availability`（变体 Offer 有、AggregateOffer 顶层无）；`aggregateRating.ratingValue` 在无评论时被无条件推荐。
- 变更：`HeadRenderer::aggregateOffer` 从子 Offer 推导顶层 `availability`；Organization `logo` 走绝对 URL；inspector 仅在已有 `review`/`aggregateRating` 时提示 ratingValue。
- 协同：Theme `OrganizationLogoSeoProfileProvider` 用 SiteBrand 补商家 logo。
- 版本：`1.2.33` → `1.2.34`；inspector `v=20260908-weline-panel-seo-rich-avail-logo-1`。

## 2026-09-08 — 产品富结果：ProductGroup + AggregateOffer 对齐（1.2.32）

- 现象：多变体商品页输出 ProductGroup，面板契约仍要求 Product+offers →「产品页 · Product · fail」；组级无价格汇总，本地富结果提示缺 offers.price。
- 变更：`HeadRenderer::productOffers` 在有 variants 时输出 `AggregateOffer`（保留 hasVariant）；inspector `Product↔ProductGroup` 等价、`offers|hasVariant`；默认启用 WebSite SearchAction。
- 协同：Product `ProductSeoProfileProvider` 补 breadcrumbs / site_search_enabled。
- 版本：`1.2.31` → `1.2.32`；inspector `v=20260908-weline-panel-seo-product-group-rich-1`。

## 2026-09-08 — 富文本本地检测（本机域名可用）（1.2.31）

- 需求：本地/内网域名 Google 富媒体测试不可用，需要面板内本地测 JSON-LD 富结果。
- 变更：富文本 Tab 主按钮改为「本地测试」；当前页解析 DOM；其他同源 URL 先 fetch，失败再走服务端 `mode=page`（pages 附带 jsonLd）；覆盖 Product/Article/FAQ/面包屑等常见类型。
- 版本：`1.2.30` → `1.2.31`；inspector `v=20260908-weline-panel-seo-local-rich-1`。

## 2026-09-08 — 当前页检测 / 富结果独立 Tab + 可填 URL（1.2.30）

- 需求：当前页服务端检测、富结果（Google 富媒体，口语亦称富文本）从「SEO 校验」内联升为独立 Tab；默认当前页，可手填 URL。
- 变更：`inspector.js` 增加 `page` / `rich` Tab；共享 `toolUrl`；page 走 `seo/crawl/start mode=page`；rich 打开官方 Rich Results Test / GSC inspect / 当前页 HTML 复制。
- 版本：`1.2.29` → `1.2.30`；inspector `v=20260908-weline-panel-seo-tabs-rich-1`。

## 2026-09-08 — 未 minify 不扣分，提示生产会压缩（1.2.29）

- 需求：本机/DEV 审计「检测到未 minify 的 CSS/JS」不应进扣分来源；生产 `!DEV` 下 `deploy:upgrade` / `setup:upgrade` 会自动 minify。
- 变更：`unminified_static_assets` → `notice`、`deduction=0`；文案说明开发可不压、生产会压；inspector 客户端改为 pass + 同口径提示；`HeadRenderer` `v=20260908-weline-panel-seo-unminified-notice-0`。
- 版本：`1.2.28` → `1.2.29`。

## 2026-09-08 — description 长度改为 Google 口径软提示（1.2.28）

- 背景：硬阈值 90–170 把 89 字也判 WARNING 并扣分；Google Search Central 明确 **无强制字数**，截断按设备宽度。
- 变更：`SitemapCrawlerAuditService` — 缺 description 仍 warning；`<50` / `>320` 仅 notice 且 **deduction=0**；中间长度不告警。inspector 同步 softMin/softMax；文案去掉「必须 90–170」。
- 版本：`1.2.27` → `1.2.28`；inspector `v=20260908-weline-panel-seo-desc-soft-1`。

## 2026-09-08 — 面板支持检测当前 URL（1.2.27）

- 需求：全站审计之外，要对当前打开的页面做同规则服务端检测。
- 实现：`SitemapCrawlerAuditService::begin(mode=page)` 只审 `pageUrl`；面板「SEO 校验」与顶栏增加「检测当前 URL」，复用 `seo/crawl/start|result`。
- 版本：`1.2.26` → `1.2.27`；inspector 资源 `v=` bump。

## 2026-09-08 — 按审计结果收口 SEO 合规噪声（1.2.26）

- 商品多变体页输出 `ProductGroup`，审计原先只认 `Product` → 误报缺 JSON-LD；现接受 Product/ProductGroup，并校验 offers/hasVariant。
- 未 minify / 图片尺寸：忽略平台模块静态与 UI 装饰图，聚焦商家内容媒体。
- `HeadRenderer` product 判断改用 `normalizePageType`，缺 name 时回退 title。
- 配合 Theme/Blog/Product 加长 description 至约 80–170。
- 版本：`1.2.25` → `1.2.26`。

## 2026-09-08 — 扣分按发现页面+图片（1.2.25）

- 问题：sitemap `image:loc`（`/pub/media/...webp`）被当成页面抓取，大量「缺少 title」其实是图片 URL；用户要的是「在哪个页面发现 → 哪张图」。
- 修复：urlset 只采 `url/loc`；忽略媒体/静态资源；issue 按发现页面分组并挂载相关图片；面板改为「发现页面 → 相关图片」。
- 版本：`1.2.24` → `1.2.25`；inspector 资源 `v=` bump。

## 2026-09-08 — 扣分按上层路径分组（1.2.24）

- 需求：98 个地址本身次要；先看问题来自哪类上层路径（商品/博客/分类/帮助/单页），点开分组再看具体 URL，便于对症修模块。
- 实现：`SitemapAuditUrlSampler::classify/groupUrls`；issue/deduction 附带 `affectedGroups`；面板扣分来源与 Issue 先展示分组摘要再展开地址。
- 版本：`1.2.23` → `1.2.24`；inspector 资源 `v=` bump。

## 2026-09-08 — 扣分来源列出受影响地址（1.2.23）

- 问题：全站审计「扣分来源」只显示「98 URLs」，看不到具体路径；issue 也只保留前 30 个 URL、面板只展 8 个。
- 修复：`affectedUrls` 保留上限升到 `DISCOVERY_LIMIT`；`health.deductions` 附带地址列表；扣分项/Issue 用可展开列表展示全部地址。
- 版本：`1.2.22` → `1.2.23`；inspector 资源 `v=` bump。

## 2026-09-08 — 全站审计：同结构 URL 抽样（1.2.22）

- 需求：sitemap 分析后，`product/*`、`blog/*`、`blog/category/*`、`category/*`、`help/*`、`promotion/*`、`page/*` 等重复结构各保留 1 个代表；其余单例 URL 全部审查。
- 实现：`SitemapAuditUrlSampler`；`SitemapCrawlerAuditService::begin` 先按 `DISCOVERY_LIMIT=500` 发现再抽样，再截断到审计 `limit`；报告 `crawl.sampling` + assumptions 说明。
- 面板：健康卡展示 Discovered / Sampled / Collapsed；完成文案附抽样摘要；inspector 资源 `v=` bump。
- 版本：`1.2.21` → `1.2.22`。

## 2026-09-08 — 全站审计改为分批 begin/advance（1.2.20）

- 问题：面板 `POST seo/crawl/start` 同步跑满站 crawl，慢页 ~6–8s × limit 100 触发前端 `[Weline.Api] worker request timed out`（默认 15s）。
- 方案：`SitemapCrawlerAuditService` 增加 JSON 可序列化 job 的 `begin` / `advance`（默认 batchSize=2）/ `toReport`；`crawl()` 仍为 begin+advance 直到完成（CLI/测试）。
- 面板：`Crawl::postStart` begin+首批 advance 后写入 `DevToolPayloadStore`；`getResult` 继续 advance；inspector 约 500ms 轮询并传 `requestTimeoutMs: 25000`。
- 版本：`1.2.19` → `1.2.20`；inspector 资源 query 版本 bump。

## 2026-09-08 — 公开 title 管道收口（1.2.21）

- 禁止把 layout `@meta.name`（如「搜索页默认布局」）拼进公开 `<title>`。
- `HeadRenderer` 接入 `TitleComposer`，统一追加站点名。
- 配合 Search/Theme/Cart/Blog/Help 补齐或加长 `assign('seo')`。

## 2026-09-08 — 全站审计：识别 *.test.weline.com 本地 TLS 豁免

- 问题：面板全站审计对 `p05113ef3.test.weline.com` 报「sitemap HTTPS 证书不被信任」且 0 页扫描；PHP curl 遇自签链失败。
- 根因：`shouldRelaxTlsVerification` 只认以 `.test` 结尾的 Host，而本机默认是 `{hash}.test.weline.com`（后缀 `.com`）。
- 修复：抽出 `LocalDevelopmentHostPolicy`，覆盖 `.test.weline.com` / `.weline.test` / `.test` / localhost / 私网 IP。

## 2026-09-08 — 面板 SEO 补齐富结果/GSC/CWV（仍仅 weline 门禁后）

- 约束：日常页面只注入轻量 bootstrap；`inspector.js` 仅 SEO Tab / report 后加载；GSC API 需 Panel Token。
- 增量：
  - Inspector：Product/Collection 富结果启发式加强；「复制 HTML → Rich Results Test」；面板打开后短时 PerformanceObserver（LCP/CLS/INP）。
  - 面板 REST `seo/gsc/inspect` + `PanelGoogleUrlInspectionService` + `GoogleSitemapAdapter::inspectUrl`。
- 明确不做：外链/关键词排名；把审计脚本提前到键入 `weline` 之前。

## 2026-09-08 — 串页根因：内容卡写 bag + head 当 chrome，不是下游擦洗

- 根因链（已复现逻辑）：`ControllerFetchFileAfter` **先渲染内容**（商品卡 `setData('storefront_offer')`）→ `Template::publishAssignedSeoPageProfile` 把任意 `storefront_offer`/`product` 升成 `page_type=product` 写入 `SeoPageProfileBag` → 再渲染布局 head 读到脏 bag → `/` `/products` `/search` `/en_US` 输出 Product canonical/JSON-LD。放大器：`partials/head` 曾声明 `@meta.cache.mode=chrome`，chrome 键故意不含 route，串页 HTML 被共享缓存放大。
- 根因修复（禁止擦屁股式 path sanitize）：
  1. **仅 `assign('seo')` 可写 page profile bag**；卡片/部件的 `product`/`storefront_offer` 不得改写页面 SEO。
  2. **frontend head `cache.mode=off`**，且 Partials 运行时对 frontend head 跳过一切 output/chrome 缓存。
- 撤掉 `PageSeoContextResolver` 按 path 丢弃 bag 的防御逻辑；商品页不输出 ItemList 仍保留（结构语义，非串页擦洗）。
- 契约：`SeoPageProfileBagNoCardPollutionTest`；`PartialsChromeCachePolicyTest` 断言 head 非 chrome。

## 2026-09-08 — 线上复访：head chrome 串页 + Geo/JSON-LD 确认

- 访问确认：维护结束后复扫 `/` `/products` `/category/*` `/product/*` `/blog` `/best-sellers` `/new-arrivals` `/search` `/en_US`；Geo head（`weline-geo` + feed/llms 链）存在；`geo-feed.json/xml` 当前 items 为空（FeedItem 未灌数）；`llms.txt` 曾用「默认网站」。
- 严重问题：列表/分类/搜索/英文首页 JSON-LD 串成商品页（head chrome 跨 URL 复用 + `storefront_offers` 误升 ItemList）。
- 修复：head chrome 缓存键强制 `request_path` + `seo_fp` 并 bump schema；bag `replace`/按 page_type 隔离；禁止 offers→ItemList；商品页不输出 ItemList；Geo llms 站点名走 SEO 解析。

## 2026-09-08 — 各页面类型 SEO 结构接线（Provider/控制器事实，非写死）

- 覆盖：首页 `homepage`；商品 Product+面包屑；分类叶子 ItemList+BreadcrumbList；products/best-sellers/new-arrivals CollectionPage+ItemList；博客列表/分类/详情（page_type 别名打通）；账户/404 保持 noindex。
- 变更：`StorefrontSeoListingFacts`；`CatalogSeoProfileProvider` 消费 offers/面包屑；叶子 `/category/{slug}` 不再误用分类索引文案；Blog/Product 控制器 `assign('seo')`；`BlogSeoProfileProvider` 接受 `blog`/`blog_category`；Review Provider 仅 enrich 商品页；bag 扩展 item_list/article/breadcrumbs。
- 原则：HeadRenderer 只渲染结构；商家文案与列表事实由各模块 Provider/控制器供给。

## 2026-09-08 — SEO 结构最优：请求级 page_profile + 品牌/商品 schema

- 观察（`p05113ef3.test.weline.com:9555`）：title/canonical/desc 已单份；但商品页 `page-type=web-page`、JSON-LD 仅 Organization/WebSite/WebPage（无 Product）、无 `og:image`；`og:site_name`/Organization 落成「默认网站 默认店铺」；列表页未升为 CollectionPage；404/账户布局手写 meta 与 SEO 并行。
- 根因：Theme 布局 `unsetData()` 清掉 Controller `assign('seo'/'product')`，Partials head 读不到商品事实；Hanfu Provider 未识别拼接后的默认站名；head chrome 缓存未按页面 SEO 指纹分桶。
- 变更：
  - 新增 `SeoPageProfileBag`；`Template::setData` 在写入 `seo`/`product` 时发布到 RequestContext；`PageSeoContextResolver` 合并 bag；head chrome 缓存键加入 `seo_fp`。
  - Hanfu Provider 识别「默认网站 默认店铺」等占位并同步 `organization.name`。
  - Catalog Provider 补 `page_type`；`product_list` → CollectionPage；`@graph` 按 `@id` 去重。
  - 404/账户布局改为经 SEO 单源输出 robots/description，去掉重复手写标签。
- 本地验收：bag → resolver → HeadRenderer 可输出 Product + og:image + og:type=product。线上需同步代码并清 FPC/chrome head 后再 curl 复核。

## 2026-09-07 — 消除 SEO head 双重渲染（public/head + seo::head）

- 版本：`Weline_Seo 1.2.18`。
- 问题：店面 `<head>` 中 title/description/canonical/hreflang/OG/JSON-LD 成组出现两遍；首页 `/` 的 FPC 载荷也固化了重复标签。根因是 `Weline_Frontend::templates/public/head.phtml` 已输出 `<w:seo slot="head"/>`，布局兼容钩子 `seo::head` 默认实现又输出一次；请求级去重在部分路径/缓存下无法兜住。
- 变更：默认 `view/hooks/seo/head.phtml` 改为纯扩展点（不再输出 `<w:seo>`）；`HeadRenderer` 增加同请求 id 的进程内 claim；补充单源契约测试。
- 说明：测试域名出现在 canonical/hreflang 是当前请求 Host 的正常行为；hreflang 各语种已指向 `/en_US` 等独立路径，并非全部指向同一 URL。上线正式域时由站点 Origin/域名配置决定，无需在 SEO 模块写死域名。
- 验收：单元契约 + 清 FPC 后 curl `/` 仅一组 SEO 标签。

## 2026-09-06 — Google 站点属性 https 自动转为 sc-domain

- 版本：`Weline_Seo 1.2.17`。
- 问题：用户常填 `https://www.example.com`，而 GSC 域名属性为 `sc-domain:example.com`，验证报属性 URL 不一致。
- 变更：保存/验证前将 http(s) URL（及裸域名）规范为 `sc-domain:`（去掉 www）；字段 hint 说明自动转换。
- 验收：`SeoAccountConfigGoogleSitePropertyTest`；保存后配置为 sc-domain 形式。

## 2026-09-06 — 修复 GSC 验证文案多占位符未替换

- 版本：`Weline_Seo 1.2.16`。
- 问题：验证失败弹窗出现字面量 `%{2}`/`%{3}`/`%{4}`；`__()` 多数字占位符须传数组，误用可变参数时只替换 `%{1}`。
- 变更：`buildSearchConsoleVerifyFailureMessage` 全部改为 `__($text, [$…])`。
- 验收：CLI/浏览器文案完整替换，无残留 `%{n}`。

## 2026-09-06 — Google 配置指南与属性 URL 不一致提示

- 版本：`Weline_Seo 1.2.15`。
- 问题：用户已把 Service Account 加成 GSC 用户，但仍因属性 URL（`https://www…` vs `sc-domain:`）不一致验证失败；配置区引导不够强调「先 GSC 验证 → 加 SA → URL 一致 → 再验证」。
- 变更：重写 Google 指南步骤与 `site_url`/`service_account` 字段 hint；404 在账号已有可见属性时明确提示改 URL。
- 验收：契约测试；账户表单指南与字段说明可见。

## 2026-09-06 — Google GSC 验证失败分型提示与跳转

- 版本：`Weline_Seo 1.2.14`。
- 问题：验证失败只显示「HTTP 404」，用户误以为本站或接口坏了；实际是属性对该 Service Account 不可见。
- 变更：`verifyAccount` 区分缺站点 URL、OAuth/出网、GSC 超时、404/403/权限不足；404 文案引导去 Search Console 验证域名并把 `client_email` 加成所有者；前端失败对话框提供「去 Google Search Console」。
- 验收：`AccountFormProviderConfigFieldsContractTest`；浏览器点验证可见新文案。

## 2026-09-06 — Google 账户表单出网前置提示

- 版本：`Weline_Seo 1.2.13`。
- 背景：本机/WLS 无法直连 `oauth2.googleapis.com`，验证必超时；用户易误判为凭证未读。
- 变更：选 Google 时展示「出网依赖」warning；`config_hint` 补充验证需可访问 Google；验证失败超时文案（1.2.12）保留。
- 验收：账户编辑页可见前置提示；`AccountFormProviderConfigFieldsContractTest`。

## 2026-09-06 — Google 验证失败实为 OAuth 出网超时

- 版本：`Weline_Seo 1.2.12`。
- 诊断：账户 #2 已入库 `service_account`（`seo-indexing@weline-framework.iam.gserviceaccount.com`，私钥 openssl 可用）；失败发生在请求 `oauth2.googleapis.com/token`，本机/WLS 出网超时约 30s，并非“未读到凭证”。
- 变更：`GoogleSitemapAdapter::requestAccessToken` 区分缺凭证、私钥无效、网络超时、Google HTTP 拒绝；验证失败提示不再一律说检查 JSON。
- 验收：本地探针 `Connection timed out`；浏览器验证应看到超时类文案。

## 2026-09-05 — Google「启用URL定时推送」禁用无反馈

- 版本：`Weline_Seo 1.2.11`（1.2.10 起）。
- 问题：Google 账户页「启用URL定时推送」因 `supports_url_push=false` 被 `disabled`，点击无任何说明，看起来像坏了。
- 变更：不支持的能力开关保持可点，拦截后 toast + 下方 hint 说明「请改用 Sitemap 定时提交」；不放开 Google 通用 URL 推送。
- 验收：`AccountFormProviderConfigFieldsContractTest`；浏览器点该开关可见提示。

## 2026-09-05 — 账户「验证配置与连接」loading 与失败提示

- 版本：`Weline_Seo 1.2.9`（1.2.8 起）。
- 问题：编辑账户点「验证配置与连接」时按钮被 Bootstrap `spinner-border` 清空且无 loading 文案，表现为空白；BinQuery `business_error` 仅控制台可见，页面无 toast/内联提示。
- 变更：`seo-admin.js` 改用 `aria-busy` 标准按钮 loading；toast 统一走 `Weline.UI.toast.show`；表单增加 `data-seo-account-feedback`；验证请求改 `keepBusinessResult` 在业务失败时仍走 then，并用 `dialog.alert` 强制弹出失败原因。
- 验收：`AccountFormProviderConfigFieldsContractTest`；浏览器点验证可见「验证中…」与失败对话框/内联告警。

## 2026-09-05 — 网站/店铺/渠道/商品 SEO 与 CDN 联动

- 版本：`Weline_Seo 1.2.7`；CDN、Product、Websites 版本及变更见各自开发日志。
- 根因：产品缺少 sitemap Provider/变更 URL，默认网站 0 被部分生成和绑定入口跳过；robots 混入开发别名；API 结果与平台能力误判；账户编辑与后台实际 API 入口不一致。
- 实现：产品新旧地址事件；无账户也执行异步 sitemap 刷新；网站/店铺/渠道继承；平台合规 API、批量及部分接收结果；保留旧凭证并提供独立账户验证；CDN 精确匹配作用域。
- 配置、平台依据与外部验收边界：[SEO-CDN上线配置与验收](SEO-CDN上线配置与验收.md)。
- 已复现并修复的定向测试：`WebsiteOriginRoutingTest`、`ResourceChangedZeroSiteTest`、`ScopeSaveAfterTest`，9 tests / 31 assertions；Product/Websites 6 tests / 114 assertions，三个 SEO API 契约脚本、CDN 13 案例通过。
- 宿主原生编辑。
- 真实升级：正常 `setup:upgrade --module="Weline_Seo Weline_Cdn Weline_Product Weline_Websites" --skip-composer-dump` 退出 0；原 Cron 调度恢复并核对，WLS 已重载。
- 真实账户运行：PostgreSQL 中 SEO 后台服务及 CDN 发布 query 保存成功；空密钥编辑保留原值，默认网站 0 绑定/解绑成功，临时禁用账户已清理。
- 真实 HTTP：首页/商品详情各一组 title、description、canonical；robots 仅列正确协议/主机/端口的规范 sitemap。首次同步已写入 79 个商品 URL；Blog 的 321 条相对 loc 导致生成失败，已由其 Provider 定向修正，最终结果追加。
- 外部边界：当前无真实 SEO/CDN 账户，外部验证/提交与线上清理未执行；Chrome Weline 页面控制请求持续超时，后台点击流程未验证。
- 真实商品链路：事件 `59a035e3517f0da536a9cc774ecfe69c` 对应刷新任务 1；商品/Offer/EAV 哈希不变，正常 TaskProcessor 消费耗时 2.362 秒，终态 `done`。
- 同次事件声明扫描缓存修复见 Framework 开发日志；真实单行写入由 412.168 秒降为 2.890 秒，未跳过事件校验或模型保存。
- CDN 实际派发仍即时执行：原有 `delivery=async` 元数据未接入当前 Event.dispatch 分流，不能宣称 Outbox/异步重试已可用；未扩展全框架异步流水线或开启全局开关。
- 最终全站验收：修正 Blog 相对 loc 及多语言共享 canonical 后，真实后台生成服务成功，错误数 0，241 URL / 4 文件（1 首页、79 商品、161 博客）；任务 2 正常消费为 `done`。
- 最终 HTTP 验收：robots 规范 sitemap、241 URL 中包含 79 个商品、所有分片及页面 URL 同源三项全通过；交付地址实测可访问。浏览器再次读取后台仍报告调试器未连接，未冒充页面点击已验收。
- 交付地址：https://p05113ef3.test.weline.com:9555/

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 2026-08-28 — 账户配置指南字段预览改版

- 当前状态：`implemented`
- 问题：字段预览用 success alert 横排，暗色下绿底+浅色 pre 突兀且留白大。
- 变更：改为 `seo-field-preview` 纵向标题+主题化代码块；表单加载 `seo-admin.css`。
- 验收：`AccountFormProviderConfigFieldsContractTest`；暗色主题下预览与指南卡片一致。

## 2026-08-28 — 账户已验证站点改 WebsiteSelect（名/域可搜）

- 当前状态：`implemented`
- 问题：站点 URL 手填易错；WebsiteSelect 仅按名称/编码搜索，域名搜不到。
- 变更：`WebsiteSelectOptions`/`WebsiteSelect` 纳入 `url`/`domain` 并支持名称+域名搜索；Provider `site`/`site_url` 改为 `website_url`；账户表单用 `<w:websites:website:select>` 选站写入公网 URL。
- 验收：契约测试；选百度后可用域名过滤并点选站点自动填充 URL。

## 2026-09-03 — SEO head 请求级幂等渲染

- 当前状态：`implemented`，真实 Browser 验收待宿主解锁后补齐。
- 根因：默认 head partial 已通过 `<w:seo slot="head"/>` 渲染 SEO，布局兼容 `seo::head` hook 又使用另一 Template 对象渲染一次；既有 Template 对象标记无法跨对象去重，导致 title/meta/canonical/hreflang/schema 成组重复。
- 变更：head slot 在已初始化的 `RequestContext` 内执行请求级 claim；同一请求仅首个 Template 输出，下一请求随 Context 清理后正常输出。CLI/孤立渲染没有请求上下文时继续使用原 Template 标记。
- 回归：`HeadRendererI18nTest::testRendersHeadOnlyOnceAcrossTemplateObjectsWithinARequest` 覆盖同请求跨 Template 去重及下一请求恢复。
- 交付地址：https://p05113ef3.test.weline.com:9555/

## 2026-09-03 — 恢复编译型页面 SEO 的国际化 head context

- 当前状态：`implemented`，真实 Browser 验收待宿主解锁后补齐。
- 根因：`PageSeoContextResolver` 的 `HeadIntegrationContextService` 是带默认值的 nullable 构造参数；编译工厂采用无参实例化，使它在真实请求中恒为 `null`，已有 `Weline_Seo::integration::head_context_resolve` 未被 dispatch。
- 变更：`resolveIntegrationContext()` 与 SEO profile registry 保持一致，在未显式注入时经 `ObjectManager` 惰性解析既有 integration service；实例化或事件失败继续返回空 context，不影响 canonical。
- 回归：新增默认构造路径黑盒测试，通过 `ObjectManager::setInstance()` 注入 integration stub，验证 locale、alternate 与 x-default 会从公开 `resolve()` 合并。
- 交付地址：https://p05113ef3.test.weline.com:9555/ 、https://p05113ef3.test.weline.com:9555/products 、https://p05113ef3.test.weline.com:9555/categories 、https://p05113ef3.test.weline.com:9555/product/hanfu-1688-604560496347

## 2026-08-28 — 账户配置改为 Provider 自声明字段

- 当前状态：`implemented`
- 问题：新增账户统一 JSON 区，百度等平台仍出现 Google Service Account 提示与模板。
- 变更：`SearchEngineAdapterInterface::getAccountConfigFields`；各 Provider 自声明字段；能力服务带出 `config_fields`；表单按平台动态渲染；保存按字段组装 config。
- 验收：选百度仅见 Token/站点/快速收录；选 Google 才见 Service Account JSON 上传。

## 2026-08-28 — SEO 账户配置指南链接泄漏修复

- 当前状态：`implemented`
- 问题：配置指南用 `textContent` 渲染步骤，`<a>` 源码泄漏；步骤自带 `1.` 又在 `<ol>` 中显示成 `1. 1.`。
- 变更：新增 `appendSafeGuideStep`（仅放行 http(s) 链接与有限强调标签），并去掉步骤开头手写序号。
- 验收：选百度后第一步为可点击 `https://ziyuan.baidu.com`，列表序号不再重复。

## 2026-08-28 — SEO 账户名强制平台前缀

- 当前状态：`implemented`
- 问题：新增账户先填自由账户名，无法与平台绑定命名约定。
- 变更：表单改为先选平台；选中后账户名字段出现并强制 `{平台显示名}-{账户名}`；`Account::save` 同步校验。
- 验收：`AccountFormPlatformNameFormatContractTest`；新增页选择百度后名称固定为 `百度-` 前缀。

## 2026-08-28 — SEO 账户列表页改自研主题

- 当前状态：`implemented`
- 问题：`Backend/Account/index` 使用硬编码浅色面板、损坏的 `mdi` KPI 图标与自造空态/状态胶囊，暗色主题下突兀。
- 变更：模板改为 `w-stat-tiles`/`w-card`/`w-empty`/`w-badge`/`w:icon`；`seo-admin.css` 根令牌映射 `--weline-theme-*`，并补账户页主题表面样式。
- 验收：`AccountIndexThemeContractTest`；后台 SEO 账户列表在浅/暗色与平板/PC 断点下与周边自研主题一致。

## 2026-08-28 — 网站表单 SEO 注入面板改自研主题

- 当前状态：`implemented`
- 问题：`WebsiteAccountEnablement` 内联硬编码浅色样式，暗色主题下白底突兀；hook 残留 Bootstrap `collapsed`。
- 变更：样式迁入 `seo-admin.css` 使用 `--weline-theme-*`；模板改 `w-card`/`w-empty`/`w-badge`；去掉内联 `<style>` 与 `ms-2`/`collapsed`。
- 验收：`WebsiteAccountEnablementThemeContractTest`；网站编辑抽屉「SEO 协议与搜索提交」面板与周围 disclosure 主题一致。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.2.5`（文档基线） | docs_handoff | `REQ-SEO-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.2.5` — 文档制度启用基线

- 基线版本：`1.2.5`
- 目标版本：`1.2.5`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-SEO-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-SEO-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-SEO-0001` / `1.2.5` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
