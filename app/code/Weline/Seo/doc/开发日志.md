<!-- weline:module-development-baseline:generated-once -->
# Weline_Seo 开发日志

> 2026-08-22 起启用固定开发日志。本记录只追踪本次文档制度基线，不补写无法由仓库证实的历史开发或验收状态。

## 2026-08-28 — 账户配置指南字段预览改版

- 当前状态：`implemented`
- 问题：字段预览用 success alert 横排，暗色下绿底+浅色 pre 突兀且留白大。
- 变更：改为 `seo-field-preview` 纵向标题+主题化代码块；表单加载 `seo-admin.css`。
- 验收：`AccountFormProviderConfigFieldsContractTest`；暗色主题下预览与指南卡片一致。

## 2026-08-28 — 账户已验证站点改 WebsiteSelect（名/域可搜）

- 当前状态：`implemented`
- 问题：站点 URL 手填易错；WebsiteSelect 仅按名称/编码搜索，域名搜不到。
- 变更：`WebsiteSelectOptions`/`WebsiteSelect` 纳入 `url`/`domain` 并支持名称+域名搜索；Provider `site`/`site_url` 改为 `website_url`；账户表单用 `<w:websites:website:select>` 选站写入公网 URL。
- 验收：契约测试；选百度后可用域名过滤并点选站点自动填充 URL。

## 2026-08-28 — 账户配置改为 Provider 自声明字段

- 当前状态：`implemented`
- 问题：新增账户统一 JSON 区，百度等平台仍出现 Google Service Account 提示与模板。
- 变更：`SearchEngineAdapterInterface::getAccountConfigFields`；各 Provider 自声明字段；能力服务带出 `config_fields`；表单按平台动态渲染；保存按字段组装 config。
- 验收：选百度仅见 Token/站点/快速收录；选 Google 才见 Service Account JSON 上传。

## 2026-08-28 — SEO 账户配置指南链接泄漏修复

- 当前状态：`implemented`
- 问题：配置指南用 `textContent` 渲染步骤，`<a>` 源码泄漏；步骤自带 `1.` 又在 `<ol>` 中显示成 `1. 1.`。
- 变更：新增 `appendSafeGuideStep`（仅放行 http(s) 链接与有限强调标签），并去掉步骤开头手写序号。
- 验收：选百度后第一步为可点击 `https://ziyuan.baidu.com`，列表序号不再重复。

## 2026-08-28 — SEO 账户名强制平台前缀

- 当前状态：`implemented`
- 问题：新增账户先填自由账户名，无法与平台绑定命名约定。
- 变更：表单改为先选平台；选中后账户名字段出现并强制 `{平台显示名}-{账户名}`；`Account::save` 同步校验。
- 验收：`AccountFormPlatformNameFormatContractTest`；新增页选择百度后名称固定为 `百度-` 前缀。

## 2026-08-28 — SEO 账户列表页改自研主题

- 当前状态：`implemented`
- 问题：`Backend/Account/index` 使用硬编码浅色面板、损坏的 `mdi` KPI 图标与自造空态/状态胶囊，暗色主题下突兀。
- 变更：模板改为 `w-stat-tiles`/`w-card`/`w-empty`/`w-badge`/`w:icon`；`seo-admin.css` 根令牌映射 `--weline-theme-*`，并补账户页主题表面样式。
- 验收：`AccountIndexThemeContractTest`；后台 SEO 账户列表在浅/暗色与平板/PC 断点下与周边自研主题一致。

## 2026-08-28 — 网站表单 SEO 注入面板改自研主题

- 当前状态：`implemented`
- 问题：`WebsiteAccountEnablement` 内联硬编码浅色样式，暗色主题下白底突兀；hook 残留 Bootstrap `collapsed`。
- 变更：样式迁入 `seo-admin.css` 使用 `--weline-theme-*`；模板改 `w-card`/`w-empty`/`w-badge`；去掉内联 `<style>` 与 `ms-2`/`collapsed`。
- 验收：`WebsiteAccountEnablementThemeContractTest`；网站编辑抽屉「SEO 协议与搜索提交」面板与周围 disclosure 主题一致。

## 版本索引

| 目标版本 | 当前状态 | 需求 ID | 开始日期 | 最后更新 | 验收/发布 |
|---|---|---|---|---|---|
| `1.2.5`（文档基线） | docs_handoff | `REQ-SEO-0001` | 2026-08-22 | 2026-08-22 | pending user / not released |

## `1.2.5` — 文档制度启用基线

- 基线版本：`1.2.5`
- 目标版本：`1.2.5`（文档追踪键；未修改 `etc/module.php`）
- 当前状态：`docs_handoff`
- 需求 ID：`REQ-SEO-0001`
- 任务记录：`迁移前历史任务记录（已清理）`
- 范围/排除：新增当前源码能力快照与固定文档入口；排除一切可执行功能、配置、数据、版本和发布变更。
- 负责人/确认人：Codex / 用户（文档维护请求）

### 门禁状态

| 门禁 | 状态 | 证据/路径 | 日期 |
|---|---|---|---|
| 需求差异已确认并写入 | pass | `doc/需求.md#REQ-SEO-0001`；用户明确要求全模块文档校准 | 2026-08-22 |
| 实现执行者/模型/范围已记录 | N/A | 纯 Markdown/索引任务，无可执行代码实现 | 2026-08-22 |
| 架构/缺陷/安全复审通过 | pass | 全量文档差异审查未发现内容冲突；既有手写文档未被批量改写 | 2026-08-22 |
| 分层测试通过 | pass | 91/91 固定文档、版本、索引、相对路径校验；生成器仅三处手工保留入口例外；见任务记录 | 2026-08-22 |
| 真实路径 WebUI/E2E 通过 | N/A | 无产品/UI 行为变更 | 2026-08-22 |
| 可重复 E2E 固化并通过 | N/A | 无产品行为变更 | 2026-08-22 |
| 功能/运营文档完成 | pass | `doc/功能现状.md`、`doc/需求.md`、`doc/开发日志.md`；`prepare_project` 可动态发现 | 2026-08-22 |
| 用户/产品验收 | pending | 等待用户确认文档交付 | 2026-08-22 |

### 进度与调整（追加）

| 日期时间 | 阶段 | 状态变化 | 事实/发现 | 调整与原因 | 决定人 | 证据 |
|---|---|---|---|---|---|---|
| 2026-08-22 | requirements | planned → requirements_confirmed | 用户要求全模块文档不再陈旧 | 采用源码证据快照，不推断产品历史 | 用户 | `doc/需求.md` |
| 2026-08-22 | implementing | requirements_confirmed → implementing | 模块缺少固定文档基线 | 新增需求、开发日志与功能现状入口 | Codex | `迁移前历史任务记录（已清理）` |
| 2026-08-22 | docs_handoff | implementing → docs_handoff | 全模块文档结构与索引校验通过 | 保持产品运行状态为未验证，不写成已验收 | Codex | `迁移前历史任务记录（已清理）` |

### 实现范围

- 变更路径/符号：`doc/需求.md`、`doc/开发日志.md`、`doc/功能现状.md`
- 数据/配置/迁移：N/A；纯文档。
- 兼容与回退：删除本次新增文档并恢复索引即可；既有文档原文不改写。

### 审查与整改

- 架构：pass；自动快照与手写产品语义分离，未改变模块边界。
- 缺陷：pass；版本、文件路径、固定文档和索引引用已做确定性校验。
- 安全：文档不得包含凭据、会话、客户或支付数据。
- 用户批准延期：无。

### 测试与验收

- 聚焦/单元/集成：N/A；无可执行代码。
- 文档校验：pass；完整命令与结果记录于 `迁移前历史任务记录（已清理）`。
- 真实 WLS + WebUI：N/A；无页面或交互变化。
- E2E 固化：N/A；无产品行为变化。
- 未验证项：模块现有功能的运行状态与历史验收。

### 文档与运营交付

- 需求文档：`REQ-SEO-0001` / `1.2.5` 文档基线。
- 功能/API/架构文档：`doc/功能现状.md` 与既有专题文档。
- 运营文档：N/A；本次不新增运营操作。
- 知识索引：由 MCP SQLite 动态生成，不在仓库保存索引文件。

### 版本结论

- 结论：文档结构验证通过，进入 `docs_handoff`，待用户验收。
- 完成/发布证据：91/91 模块固定文档与索引校验通过；未请求发布。
- 剩余事项与恢复条件：等待用户确认；模块现有功能的真实运行状态仍以各功能验收记录为准。
