<?php
/**
 * 翻译记录管理页面
 * 
 * @var \Weline\Framework\View\Template $this
 * @var array $records 翻译记录列表
 * @var array $providers 渠道列表
 * @var array $stats 统计信息
 * @var int $page 当前页码
 * @var int $totalPages 总页数
 * @var array $filters 筛选条件
 */
$records = $this->getData('records') ?? [];
$providers = $this->getData('providers') ?? [];
$stats = $this->getData('stats') ?? [];
$page = $this->getData('page') ?? 1;
$totalPages = $this->getData('totalPages') ?? 1;
$filters = $this->getData('filters') ?? [];
$dialogJsonFlags = JSON_HEX_TAG | JSON_HEX_APOS | JSON_HEX_QUOT | JSON_HEX_AMP | JSON_THROW_ON_ERROR;
$confirmMessages = [
    'confirmTitle' => (string)__('确认操作'),
    'confirmButton' => (string)__('确定'),
    'cancelButton' => (string)__('取消'),
    'deleteRecord' => (string)__('确定要删除此记录吗？'),
    'deleteSelected' => (string)__('确定要删除选中的记录吗？'),
];
?>
<div class="translation-record-page">
    <div class="page-header mb-4">
        <h1><?= __('翻译记录管理') ?></h1>
        <p class="text-muted"><?= __('查看和管理翻译服务的使用记录和统计信息') ?></p>
    </div>

    <!-- 统计卡片 -->
    <div class="row mb-4">
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title"><?= __('总记录数') ?></h5>
                    <h2 class="mb-0"><?= number_format($stats['total_records'] ?? 0) ?></h2>
                    <small class="text-muted"><?= __('今日：%{1}', [number_format($stats['today_records'] ?? 0)]) ?></small>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title"><?= __('成功记录') ?></h5>
                    <h2 class="mb-0 text-success"><?= number_format($stats['success_records'] ?? 0) ?></h2>
                    <small class="text-muted"><?= __('失败：%{1}', [number_format($stats['failed_records'] ?? 0)]) ?></small>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title"><?= __('总字符数') ?></h5>
                    <h2 class="mb-0"><?= number_format($stats['total_characters'] ?? 0) ?></h2>
                    <small class="text-muted"><?= __('今日：%{1}', [number_format($stats['today_characters'] ?? 0)]) ?></small>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title"><?= __('总成本') ?></h5>
                    <h2 class="mb-0 text-primary">¥<?= number_format($stats['total_cost'] ?? 0, 4) ?></h2>
                    <small class="text-muted"><?= __('今日：¥%{1}', [number_format($stats['today_cost'] ?? 0, 4)]) ?></small>
                </div>
            </div>
        </div>
    </div>

    <!-- 筛选表单 -->
    <div class="card mb-4">
        <div class="card-header">
            <h3><?= __('筛选条件') ?></h3>
        </div>
        <div class="card-body">
            <form method="get" action="<?= $this->getBackendUrl('*/backend/record') ?>">
                <div class="row">
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label"><?= __('渠道') ?></label>
                            <select class="form-select" name="provider_id">
                                <option value=""><?= __('全部') ?></option>
                                <?php foreach ($providers as $provider): ?>
                                    <option value="<?= $provider->getId() ?>" <?= ($filters['provider_id'] ?? '') == $provider->getId() ? 'selected' : '' ?>>
                                        <?= htmlspecialchars($provider->getData('provider_name')) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="mb-3">
                            <label class="form-label"><?= __('状态') ?></label>
                            <select class="form-select" name="status">
                                <option value=""><?= __('全部') ?></option>
                                <option value="success" <?= ($filters['status'] ?? '') == 'success' ? 'selected' : '' ?>><?= __('成功') ?></option>
                                <option value="failed" <?= ($filters['status'] ?? '') == 'failed' ? 'selected' : '' ?>><?= __('失败') ?></option>
                                <option value="pending" <?= ($filters['status'] ?? '') == 'pending' ? 'selected' : '' ?>><?= __('待处理') ?></option>
                            </select>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="mb-3">
                            <label class="form-label"><?= __('源语言') ?></label>
                            <input type="text" class="form-control" name="source_language" value="<?= htmlspecialchars($filters['source_language'] ?? '') ?>" placeholder="如：zh">
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="mb-3">
                            <label class="form-label"><?= __('目标语言') ?></label>
                            <input type="text" class="form-control" name="target_language" value="<?= htmlspecialchars($filters['target_language'] ?? '') ?>" placeholder="如：en">
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label"><?= __('调用模块') ?></label>
                            <input type="text" class="form-control" name="module_name" value="<?= htmlspecialchars($filters['module_name'] ?? '') ?>" placeholder="如：Weline_Cms">
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label"><?= __('开始日期') ?></label>
                            <input type="date" class="form-control" name="start_date" value="<?= htmlspecialchars($filters['start_date'] ?? '') ?>">
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="mb-3">
                            <label class="form-label"><?= __('结束日期') ?></label>
                            <input type="date" class="form-control" name="end_date" value="<?= htmlspecialchars($filters['end_date'] ?? '') ?>">
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label">&nbsp;</label>
                            <div>
                                <button type="submit" class="btn btn-primary">
                                    <i class="mdi mdi-filter"></i> <?= __('筛选') ?>
                                </button>
                                <a href="<?= $this->getBackendUrl('*/backend/record') ?>" class="btn btn-secondary">
                                    <i class="mdi mdi-refresh"></i> <?= __('重置') ?>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- 记录列表 -->
    <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h3><?= __('翻译记录') ?></h3>
            <div>
                <button type="button" class="btn btn-sm btn-danger" data-translation-record-action="batch-delete">
                    <i class="mdi mdi-delete"></i> <?= __('批量删除') ?>
                </button>
                <button type="button" class="btn btn-sm btn-info" data-translation-record-action="export-stats">
                    <i class="mdi mdi-download"></i> <?= __('导出统计') ?>
                </button>
            </div>
        </div>
        <div class="card-body">
            <?php if (empty($records)): ?>
                <div class="alert alert-info">
                    <?= __('暂无翻译记录') ?>
                </div>
            <?php else: ?>
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>
                                    <input type="checkbox" id="select-all" data-translation-record-select-all>
                                </th>
                                <th><?= __('ID') ?></th>
                                <th><?= __('渠道') ?></th>
                                <th><?= __('源文本') ?></th>
                                <th><?= __('翻译文本') ?></th>
                                <th><?= __('语言') ?></th>
                                <th><?= __('字符数') ?></th>
                                <th><?= __('成本') ?></th>
                                <th><?= __('响应时间') ?></th>
                                <th><?= __('状态') ?></th>
                                <th><?= __('调用模块') ?></th>
                                <th><?= __('创建时间') ?></th>
                                <th><?= __('操作') ?></th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($records as $record): ?>
                                <tr>
                                    <td>
                                        <input type="checkbox" name="ids[]" value="<?= $record->getId() ?>" class="record-checkbox">
                                    </td>
                                    <td><?= $record->getId() ?></td>
                                    <td>
                                        <code><?= htmlspecialchars($record->getData('provider_code')) ?></code>
                                    </td>
                                    <td>
                                        <div style="max-width: 200px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;" 
                                             title="<?= htmlspecialchars($record->getData('source_text')) ?>">
                                            <?= htmlspecialchars(mb_substr($record->getData('source_text'), 0, 50)) ?>
                                        </div>
                                    </td>
                                    <td>
                                        <div style="max-width: 200px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;" 
                                             title="<?= htmlspecialchars($record->getData('translated_text')) ?>">
                                            <?= htmlspecialchars(mb_substr($record->getData('translated_text'), 0, 50)) ?>
                                        </div>
                                    </td>
                                    <td>
                                        <span class="badge bg-secondary"><?= htmlspecialchars($record->getData('source_language')) ?></span>
                                        <i class="mdi mdi-arrow-right"></i>
                                        <span class="badge bg-primary"><?= htmlspecialchars($record->getData('target_language')) ?></span>
                                    </td>
                                    <td><?= number_format($record->getData('character_count') ?? 0) ?></td>
                                    <td>¥<?= number_format($record->getData('cost') ?? 0, 4) ?></td>
                                    <td><?= number_format($record->getData('response_time') ?? 0) ?>ms</td>
                                    <td>
                                        <?php if ($record->isSuccess()): ?>
                                            <span class="badge bg-success"><?= __('成功') ?></span>
                                        <?php elseif ($record->isFailed()): ?>
                                            <span class="badge bg-danger" title="<?= htmlspecialchars($record->getData('error_message') ?? '') ?>">
                                                <?= __('失败') ?>
                                            </span>
                                        <?php else: ?>
                                            <span class="badge bg-warning"><?= __('待处理') ?></span>
                                        <?php endif; ?>
                                    </td>
                                    <td>
                                        <?php if ($record->getData('module_name')): ?>
                                            <code><?= htmlspecialchars($record->getData('module_name')) ?></code>
                                        <?php else: ?>
                                            <span class="text-muted">-</span>
                                        <?php endif; ?>
                                    </td>
                                    <td><?= htmlspecialchars($record->getData('created_at')) ?></td>
                                    <td>
                                        <div class="btn-group btn-group-sm">
                                            <a href="<?= $this->getBackendUrl('*/backend/record/view', ['id' => $record->getId()]) ?>" 
                                               class="btn btn-outline-info">
                                                <i class="mdi mdi-eye"></i>
                                            </a>
                                            <a href="<?= $this->getBackendUrl('*/backend/record/delete', ['id' => $record->getId()]) ?>" 
                                               class="btn btn-outline-danger"
                                               data-translation-record-confirm="deleteRecord">
                                                <i class="mdi mdi-delete"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>

                <!-- 分页 -->
                <?php if ($totalPages > 1): ?>
                    <nav aria-label="Page navigation">
                        <ul class="pagination justify-content-center">
                            <?php if ($page > 1): ?>
                                <li class="page-item">
                                    <a class="page-link" href="<?= $this->getBackendUrl('*/backend/record', array_merge($filters, ['page' => $page - 1])) ?>">
                                        <?= __('上一页') ?>
                                    </a>
                                </li>
                            <?php endif; ?>
                            
                            <?php for ($i = max(1, $page - 2); $i <= min($totalPages, $page + 2); $i++): ?>
                                <li class="page-item <?= $i == $page ? 'active' : '' ?>">
                                    <a class="page-link" href="<?= $this->getBackendUrl('*/backend/record', array_merge($filters, ['page' => $i])) ?>">
                                        <?= $i ?>
                                    </a>
                                </li>
                            <?php endfor; ?>
                            
                            <?php if ($page < $totalPages): ?>
                                <li class="page-item">
                                    <a class="page-link" href="<?= $this->getBackendUrl('*/backend/record', array_merge($filters, ['page' => $page + 1])) ?>">
                                        <?= __('下一页') ?>
                                    </a>
                                </li>
                            <?php endif; ?>
                        </ul>
                    </nav>
                <?php endif; ?>
            <?php endif; ?>
        </div>
    </div>
</div>

<!-- SweetAlert2 CSS -->
<link href='@static(Weline_Admin::assets/libs/sweetalert2/sweetalert2.min.css)' rel='stylesheet' type='text/css'/>
<!-- SweetAlert2 JS -->
<script src='@static(Weline_Admin::assets/libs/sweetalert2/sweetalert2.min.js)'></script>
<script>
const translationRecordMessages = <?= json_encode($confirmMessages, $dialogJsonFlags) ?>;

function showLocalChoice(message) {
    return new Promise(function(resolve) {
        const overlay = document.createElement('div');
        overlay.style.cssText = 'position:fixed;inset:0;z-index:1055;background:rgba(15,23,42,.45);display:flex;align-items:center;justify-content:center;padding:20px;';

        const dialog = document.createElement('div');
        dialog.setAttribute('role', 'dialog');
        dialog.setAttribute('aria-modal', 'true');
        dialog.style.cssText = 'width:min(420px,100%);background:#fff;border-radius:8px;box-shadow:0 20px 60px rgba(15,23,42,.25);padding:24px;';

        const title = document.createElement('h4');
        title.textContent = translationRecordMessages.confirmTitle;
        title.style.cssText = 'margin:0 0 12px;font-size:18px;';

        const body = document.createElement('p');
        body.textContent = message;
        body.style.cssText = 'margin:0 0 20px;color:#475569;';

        const actions = document.createElement('div');
        actions.style.cssText = 'display:flex;justify-content:flex-end;gap:10px;';

        const cancelButton = document.createElement('button');
        cancelButton.type = 'button';
        cancelButton.className = 'btn btn-secondary';
        cancelButton.textContent = translationRecordMessages.cancelButton;

        const confirmButton = document.createElement('button');
        confirmButton.type = 'button';
        confirmButton.className = 'btn btn-primary';
        confirmButton.textContent = translationRecordMessages.confirmButton;

        function close(value) {
            document.removeEventListener('keydown', onKeydown);
            overlay.remove();
            resolve(value);
        }

        function onKeydown(event) {
            if (event.key === 'Escape') {
                close(false);
            }
        }

        cancelButton.addEventListener('click', function() { close(false); });
        confirmButton.addEventListener('click', function() { close(true); });
        overlay.addEventListener('click', function(event) {
            if (event.target === overlay) {
                close(false);
            }
        });

        actions.append(cancelButton, confirmButton);
        dialog.append(title, body, actions);
        overlay.appendChild(dialog);
        document.body.appendChild(overlay);
        document.addEventListener('keydown', onKeydown);
        confirmButton.focus();
    });
}

function showTranslationRecordChoice(message) {
    if (typeof Swal !== 'undefined' && typeof Swal.fire === 'function') {
        return Swal.fire({
            icon: 'warning',
            title: translationRecordMessages.confirmTitle,
            text: message,
            showCancelButton: true,
            confirmButtonText: translationRecordMessages.confirmButton,
            cancelButtonText: translationRecordMessages.cancelButton
        }).then(function(result) {
            return !!(result && result.isConfirmed);
        });
    }

    return showLocalChoice(message);
}

document.querySelectorAll('[data-translation-record-confirm]').forEach(function(link) {
    link.addEventListener('click', function(event) {
        const key = link.getAttribute('data-translation-record-confirm');
        const message = translationRecordMessages[key];
        if (!message) {
            return;
        }

        event.preventDefault();
        showTranslationRecordChoice(message).then(function(confirmed) {
            if (confirmed) {
                window.location.href = link.href;
            }
        });
    });
});

function toggleSelectAll() {
    const selectAll = document.getElementById('select-all');
    const checkboxes = document.querySelectorAll('.record-checkbox');
    checkboxes.forEach(function(checkbox) {
        checkbox.checked = selectAll.checked;
    });
}

function batchDelete() {
    const checked = document.querySelectorAll('.record-checkbox:checked');
    if (checked.length === 0) {
        if (typeof Swal !== 'undefined') {
            Swal.fire({
                icon: 'warning',
                title: '<?= __('提示') ?>',
                text: '<?= __('请选择要删除的记录') ?>',
                confirmButtonText: '<?= __('确定') ?>'
            });
        } else {
            console.error('<?= __('请选择要删除的记录') ?>');
        }
        return;
    }
    
    showTranslationRecordChoice(translationRecordMessages.deleteSelected).then(function(confirmed) {
        if (!confirmed) {
            return;
        }

        const ids = Array.from(checked).map(function(checkbox) {
            return checkbox.value;
        });

        const form = document.createElement('form');
        form.method = 'POST';
        form.action = '<?= $this->getBackendUrl('*/backend/record/batchDelete') ?>';

        ids.forEach(function(id) {
            const input = document.createElement('input');
            input.type = 'hidden';
            input.name = 'ids[]';
            input.value = id;
            form.appendChild(input);
        });

        document.body.appendChild(form);
        form.submit();
    });
}

function exportStats() {
    window.location.href = '<?= $this->getBackendUrl('*/backend/record/exportStats') ?>';
}

document.querySelectorAll('[data-translation-record-select-all]').forEach(function(checkbox) {
    checkbox.addEventListener('change', toggleSelectAll);
});

document.querySelectorAll('[data-translation-record-action]').forEach(function(button) {
    button.addEventListener('click', function() {
        if (button.getAttribute('data-translation-record-action') === 'batch-delete') {
            batchDelete();
            return;
        }

        exportStats();
    });
});
</script>
