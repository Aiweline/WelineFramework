<?php
/** @var \Weline\Framework\View\Template $this */
$pageTitle = (string)($this->getData('page_title') ?? __('A2A 结算分支与仲裁'));
$hasError = (bool)($this->getData('has_error') ?? false);
$errorMessage = (string)($this->getData('error_message') ?? '');
$order = $this->getData('order') ?? [];
$caseEvidence = $this->getData('case_evidence') ?? [];
$ledgerImpact = $this->getData('ledger_impact') ?? [];
$ledgerRows = $this->getData('ledger_rows') ?? [];
$arbitrationSteps = $this->getData('arbitration_steps') ?? [];
$persisted = $this->getData('persisted') ?? [];
$caseApplication = $this->getData('case_application') ?? [];
$isPreviewMode = (bool)($this->getData('is_preview_mode') ?? false);
$caseType = (string)($this->getData('case_type') ?? '');
$isDisputeCase = $caseType === 'dispute';
$caseRecordLabel = $isPreviewMode && (string)($persisted['settlement_case_id'] ?? '0') === '0'
    ? (string)__('未创建')
    : (string)($persisted['settlement_case_id'] ?? '');
?>
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($pageTitle) ?></title>
    <style>
        :root {
            --a2a-bg: #f6f8fb;
            --a2a-surface: #ffffff;
            --a2a-border: #d8e0ea;
            --a2a-text: #172033;
            --a2a-muted: #64748b;
            --a2a-primary: #0369a1;
            --a2a-primary-strong: #075985;
            --a2a-warning: #a16207;
            --a2a-success: #15803d;
            --a2a-danger: #b91c1c;
            --a2a-radius: 8px;
            --a2a-shadow: 0 16px 40px rgba(15, 23, 42, 0.08);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            background: var(--a2a-bg);
            color: var(--a2a-text);
            font-family: "Plus Jakarta Sans", "Segoe UI", Arial, sans-serif;
            letter-spacing: 0;
        }

        .a2a-case-shell {
            width: min(1120px, calc(100% - 32px));
            margin: 0 auto;
            padding: 28px 0 44px;
        }

        .a2a-case-nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
            margin-bottom: 18px;
        }

        .a2a-case-nav a,
        .a2a-button {
            min-height: 38px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 8px 12px;
            background: var(--a2a-surface);
            color: var(--a2a-text);
            font-size: 14px;
            font-weight: 700;
            text-decoration: none;
        }

        .a2a-button-primary {
            background: var(--a2a-primary);
            border-color: var(--a2a-primary);
            color: #fff;
        }

        .a2a-button-primary:hover {
            background: var(--a2a-primary-strong);
            color: #fff;
        }

        .a2a-button {
            cursor: pointer;
        }

        .a2a-case-grid {
            display: grid;
            grid-template-columns: minmax(0, 1.05fr) minmax(320px, .95fr);
            gap: 16px;
            align-items: start;
        }

        .a2a-case-main {
            min-width: 0;
        }

        .a2a-panel {
            min-width: 0;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: var(--a2a-surface);
            box-shadow: var(--a2a-shadow);
            padding: 18px;
        }

        .a2a-panel + .a2a-panel {
            margin-top: 16px;
        }

        .a2a-kicker,
        .a2a-state-pill {
            display: inline-flex;
            min-height: 28px;
            align-items: center;
            border-radius: var(--a2a-radius);
            padding: 4px 9px;
            background: #e0f2fe;
            color: var(--a2a-primary);
            font-size: 12px;
            font-weight: 800;
        }

        h1,
        h2,
        p {
            margin-top: 0;
        }

        h1 {
            margin: 14px 0 10px;
            font-size: clamp(28px, 4vw, 42px);
            line-height: 1.08;
        }

        .a2a-muted {
            color: var(--a2a-muted);
            line-height: 1.65;
        }

        .a2a-id-stack {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 12px;
        }

        .a2a-public-id {
            display: inline-flex;
            min-height: 32px;
            align-items: center;
            border-radius: var(--a2a-radius);
            padding: 5px 9px;
            background: #f1f5f9;
            color: var(--a2a-text);
            font-weight: 800;
        }

        .a2a-summary {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 10px;
            margin-top: 16px;
        }

        .a2a-summary div,
        .a2a-list li,
        .a2a-ledger-row {
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 10px;
            background: #f8fafc;
            line-height: 1.5;
        }

        .a2a-summary strong,
        .a2a-ledger-row strong {
            display: block;
            margin-bottom: 4px;
            color: var(--a2a-text);
            font-size: 18px;
        }

        .a2a-summary span,
        .a2a-ledger-row span {
            color: var(--a2a-muted);
            font-size: 13px;
        }

        .a2a-list,
        .a2a-ledger-list {
            display: grid;
            gap: 10px;
            margin: 0;
            padding: 0;
            list-style: none;
        }

        .a2a-application-form {
            display: grid;
            gap: 12px;
        }

        .a2a-field {
            display: grid;
            gap: 7px;
        }

        .a2a-field span {
            color: var(--a2a-text);
            font-size: 13px;
            font-weight: 800;
        }

        .a2a-field textarea,
        .a2a-field input {
            width: 100%;
            min-height: 46px;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 10px 11px;
            background: #fff;
            color: var(--a2a-text);
            font: inherit;
            line-height: 1.45;
        }

        .a2a-field textarea {
            min-height: 92px;
            resize: vertical;
        }

        .a2a-field textarea:focus,
        .a2a-field input:focus,
        .a2a-button:focus-visible,
        .a2a-case-nav a:focus-visible {
            outline: 3px solid rgba(3, 105, 161, 0.24);
            outline-offset: 2px;
            border-color: var(--a2a-primary);
        }

        .a2a-application-note {
            border: 1px solid #bae6fd;
            border-radius: var(--a2a-radius);
            padding: 10px;
            background: #eff6ff;
            color: var(--a2a-primary);
            font-size: 13px;
            font-weight: 700;
            line-height: 1.55;
        }

        .a2a-risk-note {
            border-color: #fed7aa;
            background: #fff7ed;
            color: var(--a2a-warning);
        }

        .a2a-persisted-strip {
            display: grid;
            grid-template-columns: 1.2fr repeat(2, minmax(0, .6fr));
            gap: 8px;
            margin-top: 14px;
            padding: 10px;
            border: 1px solid #fed7aa;
            border-radius: var(--a2a-radius);
            background: #fff7ed;
        }

        .a2a-persisted-strip strong {
            display: block;
            margin-bottom: 2px;
            color: var(--a2a-warning);
            font-size: 14px;
        }

        .a2a-persisted-strip span {
            color: var(--a2a-muted);
            font-size: 12px;
        }

        .a2a-error {
            border-color: #fecaca;
            background: #fff1f2;
            color: var(--a2a-danger);
        }

        @media (max-width: 860px) {
            .a2a-case-grid,
            .a2a-summary,
            .a2a-persisted-strip {
                grid-template-columns: 1fr;
            }

            .a2a-case-nav {
                align-items: flex-start;
                flex-direction: column;
            }
        }
    </style>
    <?= $this->fetch('Aiweline_A2A::templates/Frontend/partials/console-style.phtml') ?>
</head>
<body>
    <main class="a2a-case-shell">
        <nav class="a2a-case-nav" aria-label="<?= __('A2A 结算分支导航') ?>">
            <a href="/a2a/frontend#orders"><?= __('返回订单工作台') ?></a>
            <?php if (!$hasError): ?>
                <a class="a2a-button a2a-button-primary" href="/a2a/frontend/acceptance?order=<?= urlencode((string)$this->getData('order_id')) ?>"><?= __('查看验收页') ?></a>
                <?php if ($isDisputeCase && !$isPreviewMode): ?>
                    <a class="a2a-button a2a-button-primary" href="/a2a/frontend/arbitration-ruling?order=<?= urlencode((string)$this->getData('order_id')) ?>&amp;ruling=partial_release"><?= __('生成仲裁裁决') ?></a>
                <?php endif; ?>
            <?php endif; ?>
        </nav>

        <?php if ($hasError): ?>
            <section class="a2a-panel a2a-error">
                <span class="a2a-kicker"><?= __('无法进入结算分支') ?></span>
                <h1><?= __('结算分支不可执行') ?></h1>
                <p><?= htmlspecialchars($errorMessage) ?></p>
                <a class="a2a-button" href="/a2a/frontend#orders"><?= __('返回订单工作台') ?></a>
            </section>
        <?php else: ?>
            <section class="a2a-panel">
                <span class="a2a-kicker"><?= htmlspecialchars((string)$this->getData('case_title')) ?></span>
                <h1><?= htmlspecialchars((string)$order['sku_title']) ?></h1>
                <p class="a2a-muted">
                    <?= htmlspecialchars((string)$order['provider']) ?> ·
                    <?= $isPreviewMode ? __('申请预览') : __('证据快照已锁定') ?> ·
                    <?= $isPreviewMode ? __('提交前账本不变') : __('账本状态已更新') ?>
                </p>
                <div class="a2a-id-stack">
                    <span class="a2a-public-id"><?= htmlspecialchars((string)$this->getData('order_id')) ?></span>
                    <span class="a2a-public-id"><?= htmlspecialchars((string)$this->getData('acceptance_id')) ?></span>
                    <span class="a2a-public-id"><?= htmlspecialchars((string)$this->getData('case_id')) ?></span>
                    <span class="a2a-state-pill"><?= htmlspecialchars((string)$this->getData('status')) ?></span>
                </div>
                <div class="a2a-summary">
                    <div><strong><?= htmlspecialchars((string)$order['amount']) ?></strong><span><?= __('订单金额') ?></span></div>
                    <div><strong><?= htmlspecialchars((string)$order['platform_fee']) ?></strong><span><?= __('平台服务费') ?></span></div>
                    <div><strong><?= htmlspecialchars((string)$order['provider_payout']) ?></strong><span><?= __('Agent 收入') ?></span></div>
                </div>
                <?php if (is_array($persisted) && $persisted): ?>
                    <div class="a2a-persisted-strip" aria-label="<?= __('持久化状态') ?>">
                        <div><strong><?= htmlspecialchars((string)$persisted['status']) ?></strong><span><?= __('持久化状态') ?></span></div>
                        <div><strong><?= htmlspecialchars($caseRecordLabel) ?></strong><span><?= __('案件主键') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['ledger_rows']) ?></strong><span><?= __('账本行') ?></span></div>
                    </div>
                <?php endif; ?>
            </section>

            <div class="a2a-case-grid">
                <div class="a2a-case-main">
                    <section class="a2a-panel">
                        <?php if ($isPreviewMode): ?>
                            <h2><?= $isDisputeCase ? __('争议仲裁申请') : __('退款复核申请') ?></h2>
                            <p class="a2a-muted"><?= __('买方需要先提交结构化申请，系统才会创建案件、冻结或改写托管账本。') ?></p>
                            <form class="a2a-application-form" method="get" action="/a2a/frontend/settlement-case">
                                <input type="hidden" name="case" value="<?= htmlspecialchars($caseType) ?>">
                                <input type="hidden" name="order" value="<?= htmlspecialchars((string)$this->getData('order_id')) ?>">
                                <input type="hidden" name="submit" value="1">
                                <label class="a2a-field">
                                    <span><?= htmlspecialchars((string)($caseApplication['reason_label'] ?? __('申请原因'))) ?></span>
                                    <textarea name="reason" required><?= htmlspecialchars((string)($caseApplication['reason'] ?? '')) ?></textarea>
                                </label>
                                <label class="a2a-field">
                                    <span><?= htmlspecialchars((string)($caseApplication['desired_outcome_label'] ?? __('期望处理结果'))) ?></span>
                                    <textarea name="desired_outcome" required><?= htmlspecialchars((string)($caseApplication['desired_outcome'] ?? '')) ?></textarea>
                                </label>
                                <label class="a2a-field">
                                    <span><?= htmlspecialchars((string)($caseApplication['evidence_note_label'] ?? __('证据说明'))) ?></span>
                                    <textarea name="evidence_note" required><?= htmlspecialchars((string)($caseApplication['evidence_note'] ?? '')) ?></textarea>
                                </label>
                                <div class="a2a-application-note"><?= htmlspecialchars((string)($caseApplication['preview_notice'] ?? '')) ?></div>
                                <div class="a2a-application-note a2a-risk-note"><?= htmlspecialchars((string)($caseApplication['safety_notice'] ?? '')) ?></div>
                                <button class="a2a-button a2a-button-primary" type="submit"><?= htmlspecialchars((string)($caseApplication['submit_label'] ?? __('提交申请'))) ?></button>
                        </form>
                    <?php else: ?>
                        <h2><?= $isDisputeCase ? __('争议申请已提交') : __('退款申请已提交') ?></h2>
                        <p class="a2a-muted"><?= __('申请已写入结算分支，当前页面展示可审计证据、账本影响和后续处理流程。') ?></p>
                        <div class="a2a-application-note a2a-risk-note">
                            <?= $isDisputeCase
                                ? __('资金已进入争议冻结，等待仲裁员复核并签发裁决。')
                                : __('退款复核已开启，Agent 收入暂停结算，平台等待补证或裁决。') ?>
                        </div>
                    <?php endif; ?>
                </section>

                    <section class="a2a-panel">
                        <h2><?= $isPreviewMode ? __('提交后证据包预览') : ($isDisputeCase ? __('仲裁证据包') : __('退款证据包')) ?></h2>
                        <p class="a2a-muted"><?= __('结算分支必须保留验收快照、交付证据和账本影响，供后续仲裁与信誉重算使用。') ?></p>
                        <ul class="a2a-list">
                            <?php foreach ($caseEvidence as $item): ?>
                                <li><?= htmlspecialchars((string)$item) ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </section>
                </div>
                <aside>
                    <section class="a2a-panel">
                        <h2><?= __('账本影响') ?></h2>
                        <ul class="a2a-list">
                            <?php foreach ($ledgerImpact as $item): ?>
                                <li><?= htmlspecialchars((string)$item) ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </section>
                    <section class="a2a-panel">
                        <h2><?= __('当前托管账本') ?></h2>
                        <div class="a2a-ledger-list">
                            <?php foreach ($ledgerRows as $row): ?>
                                <div class="a2a-ledger-row">
                                    <strong><?= htmlspecialchars((string)$row['amount']) ?></strong>
                                    <span><?= htmlspecialchars((string)$row['label']) ?> · <?= htmlspecialchars((string)$row['status_label']) ?></span>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    </section>
                    <section class="a2a-panel">
                        <h2><?= $isDisputeCase ? __('仲裁流程') : __('退款复核流程') ?></h2>
                        <ul class="a2a-list">
                            <?php foreach ($arbitrationSteps as $item): ?>
                                <li><?= htmlspecialchars((string)$item) ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </section>
                </aside>
            </div>
        <?php endif; ?>
    </main>
</body>
</html>
