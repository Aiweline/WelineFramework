<?php
/** @var \Weline\Framework\View\Template $this */
$pageTitle = (string)($this->getData('page_title') ?? __('A2A 角色权限控制台'));
$hasError = (bool)($this->getData('has_error') ?? false);
$errorMessage = (string)($this->getData('error_message') ?? '');
$roleTabs = $this->getData('role_tabs') ?? [];
$actions = $this->getData('actions') ?? [];
$guards = $this->getData('guards') ?? [];
$caseSummary = $this->getData('case_summary') ?? [];
$ledgerRows = $this->getData('ledger_rows') ?? [];
$order = $this->getData('order') ?? [];
$persisted = $this->getData('persisted') ?? [];
$actionResult = $this->getData('action_result') ?? [];
$actor = $this->getData('actor') ?? [];
$actorNotes = is_array($actor['notes'] ?? null) ? $actor['notes'] : [];
$actorAcl = $this->getData('actor_acl') ?? [];
$actorAclNotices = is_array($actorAcl['notices'] ?? null) ? $actorAcl['notices'] : [];
$bindingResult = is_array($actorAcl['binding_result'] ?? null) ? $actorAcl['binding_result'] : [];
$currentAssignment = is_array($actorAcl['current_assignment'] ?? null) ? $actorAcl['current_assignment'] : [];
$actorAssignments = is_array($actorAcl['assignments'] ?? null) ? $actorAcl['assignments'] : [];
$actorMetrics = is_array($actorAcl['metrics'] ?? null) ? $actorAcl['metrics'] : [];
$orderId = (string)($this->getData('order_id') ?? 'A2A-ORDER-Q-48E8DB');
$isForbidden = (bool)($this->getData('is_forbidden') ?? false);
?>
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($pageTitle) ?></title>
    <style>
        :root {
            --a2a-bg: #f6f8fb;
            --a2a-surface: #ffffff;
            --a2a-border: #d8e0ea;
            --a2a-text: #172033;
            --a2a-muted: #64748b;
            --a2a-primary: #0369a1;
            --a2a-primary-strong: #075985;
            --a2a-warning: #a16207;
            --a2a-success: #15803d;
            --a2a-danger: #b91c1c;
            --a2a-radius: 8px;
            --a2a-shadow: 0 16px 40px rgba(15, 23, 42, 0.08);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            background: var(--a2a-bg);
            color: var(--a2a-text);
            font-family: "Plus Jakarta Sans", "Segoe UI", Arial, sans-serif;
            letter-spacing: 0;
        }

        .a2a-role-shell {
            width: min(1180px, calc(100% - 32px));
            margin: 0 auto;
            padding: 28px 0 44px;
        }

        .a2a-role-nav,
        .a2a-role-tabs,
        .a2a-action-links {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            align-items: center;
        }

        .a2a-role-nav {
            justify-content: space-between;
            margin-bottom: 18px;
        }

        .a2a-role-nav a,
        .a2a-role-tab,
        .a2a-button {
            min-height: 38px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 8px 12px;
            background: var(--a2a-surface);
            color: var(--a2a-text);
            font-size: 14px;
            font-weight: 700;
            text-decoration: none;
        }

        .a2a-role-tab-active,
        .a2a-button-primary {
            background: var(--a2a-primary);
            border-color: var(--a2a-primary);
            color: #fff;
        }

        .a2a-button-primary:hover {
            background: var(--a2a-primary-strong);
            color: #fff;
        }

        .a2a-role-grid {
            display: grid;
            grid-template-columns: minmax(0, 1.15fr) minmax(320px, .85fr);
            gap: 16px;
            align-items: start;
        }

        .a2a-panel {
            min-width: 0;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: var(--a2a-surface);
            box-shadow: var(--a2a-shadow);
            padding: 18px;
        }

        .a2a-panel + .a2a-panel {
            margin-top: 16px;
        }

        .a2a-kicker,
        .a2a-state-pill {
            display: inline-flex;
            min-height: 28px;
            align-items: center;
            border-radius: var(--a2a-radius);
            padding: 4px 9px;
            background: #e0f2fe;
            color: var(--a2a-primary);
            font-size: 12px;
            font-weight: 800;
        }

        .a2a-state-allowed {
            background: #dcfce7;
            color: var(--a2a-success);
        }

        .a2a-state-blocked,
        .a2a-forbidden {
            background: #fee2e2;
            color: var(--a2a-danger);
        }

        h1,
        h2,
        p {
            margin-top: 0;
        }

        h1 {
            margin: 14px 0 10px;
            font-size: clamp(28px, 4vw, 42px);
            line-height: 1.08;
        }

        .a2a-muted {
            color: var(--a2a-muted);
            line-height: 1.65;
        }

        .a2a-id-stack,
        .a2a-summary {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 12px;
        }

        .a2a-identity-strip {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(118px, 1fr));
            gap: 8px;
            margin: 12px 0;
        }

        .a2a-identity-strip div {
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: #f8fafc;
            padding: 10px;
        }

        .a2a-identity-strip strong {
            display: block;
            margin-bottom: 3px;
            font-size: 18px;
        }

        .a2a-identity-strip span {
            color: var(--a2a-muted);
            font-size: 13px;
        }

        .a2a-identity-badge {
            display: inline-flex;
            align-items: center;
            min-height: 26px;
            border-radius: var(--a2a-radius);
            margin: 6px 0 4px;
            padding: 3px 8px;
            background: #fef3c7;
            color: var(--a2a-warning);
            font-size: 12px;
            font-weight: 800;
        }

        .a2a-identity-badge-ready {
            background: #dcfce7;
            color: var(--a2a-success);
        }

        .a2a-role-session-note {
            margin: 10px 0 0;
        }

        .a2a-public-id,
        .a2a-summary-item,
        .a2a-action-row,
        .a2a-list li,
        .a2a-ledger-row {
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: #f8fafc;
        }

        .a2a-public-id {
            display: inline-flex;
            min-height: 32px;
            align-items: center;
            padding: 5px 9px;
            font-weight: 800;
        }

        .a2a-summary-item {
            flex: 1 1 180px;
            padding: 10px;
        }

        .a2a-summary-item strong,
        .a2a-ledger-row strong {
            display: block;
            margin-bottom: 4px;
            font-size: 18px;
        }

        .a2a-summary-item span,
        .a2a-ledger-row span {
            color: var(--a2a-muted);
            font-size: 13px;
        }

        .a2a-action-list,
        .a2a-list,
        .a2a-ledger-list {
            display: grid;
            gap: 10px;
            margin: 0;
            padding: 0;
            list-style: none;
        }

        .a2a-action-row {
            padding: 12px;
        }

        .a2a-action-head {
            display: flex;
            justify-content: space-between;
            gap: 10px;
            align-items: flex-start;
            margin-bottom: 8px;
        }

        .a2a-action-head h3 {
            margin: 0;
            font-size: 18px;
        }

        .a2a-list li,
        .a2a-ledger-row {
            padding: 10px;
            line-height: 1.5;
        }

        .a2a-result {
            border: 1px solid #bfdbfe;
            border-radius: var(--a2a-radius);
            margin-top: 14px;
            padding: 12px;
            background: #eff6ff;
            color: var(--a2a-primary);
            font-weight: 700;
        }

        .a2a-result-forbidden {
            border-color: #fecaca;
            background: #fff1f2;
            color: var(--a2a-danger);
        }

        .a2a-persisted-strip {
            display: grid;
            grid-template-columns: 1.2fr repeat(4, minmax(0, .55fr));
            gap: 8px;
            margin-top: 14px;
            padding: 10px;
            border: 1px solid #fed7aa;
            border-radius: var(--a2a-radius);
            background: #fff7ed;
        }

        .a2a-persisted-strip strong {
            display: block;
            margin-bottom: 2px;
            color: var(--a2a-warning);
            font-size: 14px;
        }

        .a2a-persisted-strip span {
            color: var(--a2a-muted);
            font-size: 12px;
        }

        .a2a-error {
            border-color: #fecaca;
            background: #fff1f2;
            color: var(--a2a-danger);
        }

        @media (max-width: 880px) {
            .a2a-role-grid,
            .a2a-persisted-strip,
            .a2a-identity-strip {
                grid-template-columns: 1fr;
            }

            .a2a-role-nav {
                align-items: flex-start;
                flex-direction: column;
            }

            .a2a-action-head {
                flex-direction: column;
            }
        }
    </style>
    <?= $this->fetch('Aiweline_A2A::templates/Frontend/partials/console-style.phtml') ?>
</head>
<body>
    <main class="a2a-role-shell">
        <nav class="a2a-role-nav" aria-label="<?= __('A2A 角色权限导航') ?>">
            <a href="/a2a/frontend#roles"><?= __('返回角色权限') ?></a>
            <?php if (!$hasError): ?>
                <a class="a2a-button a2a-button-primary" href="/a2a/frontend/reputation?agent=dataclean-pro-agent"><?= __('查看 Agent 信誉') ?></a>
            <?php endif; ?>
        </nav>

        <?php if ($hasError): ?>
            <section class="a2a-panel a2a-error">
                <span class="a2a-kicker"><?= __('A2A 角色权限控制台') ?></span>
                <h1><?= __('角色权限不可用') ?></h1>
                <p><?= htmlspecialchars($errorMessage) ?></p>
                <a class="a2a-button" href="/a2a/frontend#roles"><?= __('返回角色权限') ?></a>
            </section>
        <?php else: ?>
            <section class="a2a-panel">
                <span class="a2a-kicker"><?= __('A2A 角色权限控制台') ?></span>
                <h1><?= htmlspecialchars((string)$this->getData('role_label')) ?></h1>
                <p class="a2a-muted"><?= __('每个交易动作都要同时满足角色、订单状态、验收证据、退款/争议状态和账本约束。') ?></p>
                <div class="a2a-role-tabs" aria-label="<?= __('切换会话角色') ?>">
                    <?php foreach ($roleTabs as $tab): ?>
                        <a class="a2a-role-tab <?= !empty($tab['active']) ? 'a2a-role-tab-active' : '' ?>" href="<?= htmlspecialchars((string)$tab['href']) ?>"><?= htmlspecialchars((string)$tab['label']) ?></a>
                    <?php endforeach; ?>
                </div>
                <p class="a2a-muted a2a-role-session-note"><?= __('当前角色由 A2A 会话保存，普通 role 参数不会直接授权动作。') ?></p>
                <?php if ($actorNotes || $actorAclNotices): ?>
                    <ul class="a2a-list a2a-role-session-note" aria-label="<?= __('会话角色提示') ?>">
                        <?php foreach ($actorNotes as $note): ?>
                            <li><?= htmlspecialchars((string)$note) ?></li>
                        <?php endforeach; ?>
                        <?php foreach ($actorAclNotices as $notice): ?>
                            <li><?= htmlspecialchars((string)$notice) ?></li>
                        <?php endforeach; ?>
                    </ul>
                <?php endif; ?>
                <div class="a2a-id-stack">
                    <span class="a2a-public-id"><?= htmlspecialchars($orderId) ?></span>
                    <span class="a2a-state-pill"><?= __('会话角色') ?>: <?= htmlspecialchars((string)($actor['role_label'] ?? $this->getData('role_label'))) ?></span>
                    <span class="a2a-state-pill"><?= htmlspecialchars((string)($actor['source_label'] ?? __('A2A 会话角色'))) ?></span>
                    <?php if ($currentAssignment): ?>
                        <span class="a2a-state-pill"><?= __('订单归属') ?>: <?= htmlspecialchars((string)($currentAssignment['actor_display'] ?? '')) ?></span>
                        <span class="a2a-state-pill"><?= htmlspecialchars((string)($currentAssignment['binding_label'] ?? '')) ?></span>
                        <span class="a2a-state-pill"><?= __('身份就绪') ?>: <?= htmlspecialchars((string)($currentAssignment['identity_readiness_label'] ?? __('待绑定身份'))) ?></span>
                        <span class="a2a-state-pill"><?= __('运行时实证') ?>: <?= htmlspecialchars((string)($currentAssignment['runtime_proof_label'] ?? __('缺少运行时实证'))) ?></span>
                    <?php endif; ?>
                    <span class="a2a-state-pill"><?= htmlspecialchars((string)$order['status_label']) ?></span>
                    <span class="a2a-state-pill"><?= htmlspecialchars((string)$order['provider_queue']) ?></span>
                    <?php if ($isForbidden): ?>
                        <span class="a2a-state-pill a2a-forbidden"><?= __('HTTP 403 动作阻断') ?></span>
                    <?php endif; ?>
                </div>
                <div class="a2a-summary">
                    <div class="a2a-summary-item"><strong><?= htmlspecialchars((string)$order['amount']) ?></strong><span><?= htmlspecialchars((string)$order['sku_title']) ?></span></div>
                    <div class="a2a-summary-item"><strong><?= htmlspecialchars((string)$order['platform_fee']) ?></strong><span><?= __('平台服务费') ?></span></div>
                    <div class="a2a-summary-item"><strong><?= htmlspecialchars((string)$order['provider_payout']) ?></strong><span><?= __('Agent 收入') ?></span></div>
                </div>
                <?php if (is_array($actionResult) && $actionResult): ?>
                    <div class="a2a-result <?= $isForbidden ? 'a2a-result-forbidden' : '' ?>">
                        <?= htmlspecialchars((string)$actionResult['message']) ?>
                    </div>
                <?php endif; ?>
                <?php if ($bindingResult): ?>
                    <div class="a2a-result">
                        <?= htmlspecialchars((string)($bindingResult['message'] ?? '')) ?>
                        <?php if (!empty($bindingResult['subject_display'])): ?>
                            · <?= htmlspecialchars((string)$bindingResult['subject_display']) ?>
                        <?php endif; ?>
                        <?php if (!empty($bindingResult['identity_readiness_label'])): ?>
                            · <?= __('身份就绪') ?>: <?= htmlspecialchars((string)$bindingResult['identity_readiness_label']) ?>
                        <?php endif; ?>
                        <?php if (!empty($bindingResult['runtime_proof_label'])): ?>
                            · <?= __('运行时实证') ?>: <?= htmlspecialchars((string)$bindingResult['runtime_proof_label']) ?>
                        <?php endif; ?>
                    </div>
                <?php endif; ?>
                <?php if (is_array($persisted) && $persisted): ?>
                    <div class="a2a-persisted-strip" aria-label="<?= __('持久化状态') ?>">
                        <div><strong><?= htmlspecialchars((string)$persisted['status']) ?></strong><span><?= __('策略状态') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['trade_order_id']) ?></strong><span><?= __('订单主键') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['role_code']) ?></strong><span><?= __('角色代码') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['case_count']) ?></strong><span><?= __('结算案例') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)($actorMetrics['assignment_count_label'] ?? __('未生成'))) ?></strong><span><?= __('订单角色归属') ?></span></div>
                    </div>
                <?php endif; ?>
            </section>

            <div class="a2a-role-grid">
                <section class="a2a-panel">
                    <h2><?= __('可执行动作矩阵') ?></h2>
                    <p class="a2a-muted"><?= __('同一个订单在不同角色下会暴露不同动作。被阻断的动作不会进入资金或仲裁写入。') ?></p>
                    <div class="a2a-action-list">
                        <?php foreach ($actions as $action): ?>
                            <?php $stateClass = ($action['state'] ?? '') === 'allowed' ? 'a2a-state-allowed' : 'a2a-state-blocked'; ?>
                            <article class="a2a-action-row">
                                <div class="a2a-action-head">
                                    <h3><?= htmlspecialchars((string)$action['label']) ?></h3>
                                    <span class="a2a-state-pill <?= $stateClass ?>"><?= htmlspecialchars((string)$action['state_label']) ?></span>
                                </div>
                                <p class="a2a-muted"><?= htmlspecialchars((string)$action['rule']) ?></p>
                                <div class="a2a-action-links">
                                    <a class="a2a-button" href="/a2a/frontend/role-console?order=<?= rawurlencode($orderId) ?>&amp;action=<?= rawurlencode((string)$action['code']) ?>"><?= __('验证此动作') ?></a>
                                    <?php if (!empty($action['runtime_proof_url'])): ?>
                                        <a class="a2a-button" href="<?= htmlspecialchars((string)$action['runtime_proof_url']) ?>"><?= __('生成运行时实证票据') ?></a>
                                    <?php endif; ?>
                                    <?php if (($action['state'] ?? '') === 'allowed' && (string)($action['href'] ?? '') !== ''): ?>
                                        <a class="a2a-button a2a-button-primary" href="<?= htmlspecialchars((string)$action['href']) ?>"><?= __('打开业务入口') ?></a>
                                    <?php endif; ?>
                                </div>
                            </article>
                        <?php endforeach; ?>
                    </div>
                </section>
                <aside>
                    <?php if ($actorAssignments): ?>
                        <section class="a2a-panel">
                            <h2><?= __('订单角色归属') ?></h2>
                            <p class="a2a-muted"><?= __('每个敏感动作必须对应正式订单里的角色归属；登录账号绑定完成前仍属于原型授权。') ?></p>
                            <p class="a2a-muted"><strong><?= __('运行时实证') ?></strong> / <?= __('区分真实账号、权限契约、后台 ACL 和原型绑定，避免把合同就绪误判为生产授权。') ?></p>
                            <div class="a2a-identity-strip" aria-label="<?= __('生产级身份状态') ?>">
                                <div><strong><?= htmlspecialchars((string)($actorMetrics['production_identity_ready_label'] ?? '0/4')) ?></strong><span><?= __('生产可用身份') ?></span></div>
                                <div><strong><?= htmlspecialchars((string)($actorMetrics['runtime_proof_passed_label'] ?? '0/4')) ?></strong><span><?= __('运行时已实证') ?></span></div>
                                <div><strong><?= htmlspecialchars((string)($actorMetrics['contract_ready_label'] ?? '0/4')) ?></strong><span><?= __('权限契约待实证') ?></span></div>
                                <div><strong><?= htmlspecialchars((string)($actorMetrics['runtime_proof_missing_label'] ?? '0/4')) ?></strong><span><?= __('运行时缺口') ?></span></div>
                            </div>
                            <?php if ($currentAssignment && (string)($currentAssignment['binding_status'] ?? '') !== 'account_bound'): ?>
                                <p>
                                    <a class="a2a-button a2a-button-primary" href="/a2a/frontend/role-console?bind_actor=1&amp;order=<?= rawurlencode($orderId) ?>"><?= __('绑定当前角色') ?></a>
                                </p>
                            <?php endif; ?>
                            <ul class="a2a-list">
                                <?php foreach ($actorAssignments as $assignment): ?>
                                    <?php $identityBadgeClass = !empty($assignment['production_ready']) ? 'a2a-identity-badge-ready' : ''; ?>
                                    <li>
                                        <strong><?= htmlspecialchars((string)($assignment['role_label'] ?? '')) ?> / <?= htmlspecialchars((string)($assignment['actor_display'] ?? '')) ?></strong><br>
                                        <?= htmlspecialchars((string)($assignment['binding_label'] ?? '')) ?> / <?= htmlspecialchars((string)($assignment['verification_label'] ?? '')) ?><br>
                                        <span class="a2a-identity-badge <?= $identityBadgeClass ?>"><?= __('身份就绪') ?>: <?= htmlspecialchars((string)($assignment['identity_readiness_label'] ?? __('待绑定身份'))) ?></span><br>
                                        <?= __('身份来源') ?>: <?= htmlspecialchars((string)($assignment['identity_source_label'] ?? __('未解析'))) ?><br>
                                        <?= __('证据') ?>: <?= htmlspecialchars((string)($assignment['identity_evidence_label'] ?? __('无身份解析证据'))) ?><br>
                                        <?= __('运行时实证') ?>: <?= htmlspecialchars((string)($assignment['runtime_proof_label'] ?? __('缺少运行时实证'))) ?><br>
                                        <?= __('实证证据') ?>: <?= htmlspecialchars((string)($assignment['runtime_proof_evidence'] ?? __('无运行时实证'))) ?><br>
                                        <?= __('缺口') ?>: <?= htmlspecialchars((string)($assignment['runtime_proof_gap'] ?? __('需要补齐与角色匹配的会话、账号或 ACL 证据。'))) ?><br>
                                        <?php if (!empty($assignment['runtime_acl_source'])): ?>
                                            <?= __('ACL 来源') ?>: <?= htmlspecialchars((string)$assignment['runtime_acl_source']) ?><br>
                                            <?= __('ACL 注册') ?>: <?= htmlspecialchars((string)($assignment['runtime_acl_registration_label'] ?? __('ACL Source 未注册'))) ?><br>
                                            <span class="a2a-muted"><?= htmlspecialchars((string)($assignment['runtime_acl_registration_evidence'] ?? '')) ?></span><br>
                                            <?php if (!empty($assignment['runtime_acl_proof_url'])): ?>
                                                <?= __('后台实证入口') ?>:
                                                <a href="<?= htmlspecialchars((string)$assignment['runtime_acl_proof_url']) ?>"><?= __('打开后台 ACL 实证') ?></a><br>
                                                <span class="a2a-muted"><?= __('该入口由 Weline 后台 URL 生成器生成，裸路由不会作为生产验证入口。') ?></span><br>
                                            <?php endif; ?>
                                        <?php endif; ?>
                                        <?php if (!empty($assignment['bound_subject_display'])): ?>
                                            <?= __('绑定主体') ?>: <?= htmlspecialchars((string)$assignment['bound_subject_display']) ?><br>
                                        <?php endif; ?>
                                        <?php if (!empty($assignment['identity_risk_label'])): ?>
                                            <span class="a2a-muted"><?= htmlspecialchars((string)$assignment['identity_risk_label']) ?></span><br>
                                        <?php endif; ?>
                                        <span class="a2a-muted"><?= htmlspecialchars((string)($assignment['ownership_scope'] ?? '')) ?></span>
                                    </li>
                                <?php endforeach; ?>
                            </ul>
                        </section>
                    <?php endif; ?>
                    <section class="a2a-panel">
                        <h2><?= __('硬性守卫规则') ?></h2>
                        <ul class="a2a-list">
                            <?php foreach ($guards as $guard): ?>
                                <li><?= htmlspecialchars((string)$guard) ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </section>
                    <section class="a2a-panel">
                        <h2><?= __('结算案例') ?></h2>
                        <ul class="a2a-list">
                            <?php foreach ($caseSummary as $case): ?>
                                <li><strong><?= htmlspecialchars((string)$case['case_id']) ?></strong><br><?= htmlspecialchars((string)$case['type']) ?> · <?= htmlspecialchars((string)$case['status']) ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </section>
                    <section class="a2a-panel">
                        <h2><?= __('托管账本状态') ?></h2>
                        <div class="a2a-ledger-list">
                            <?php foreach ($ledgerRows as $row): ?>
                                <div class="a2a-ledger-row">
                                    <strong><?= htmlspecialchars((string)$row['amount']) ?></strong>
                                    <span><?= htmlspecialchars((string)$row['label']) ?> · <?= htmlspecialchars((string)$row['status_label']) ?></span>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    </section>
                </aside>
            </div>
        <?php endif; ?>
    </main>
</body>
</html>
