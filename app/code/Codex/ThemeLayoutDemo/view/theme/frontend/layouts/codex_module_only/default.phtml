<?php
/**
 * Demo module-only layout.
 *
 * @meta.name {default="Module Only Demo",name="Module Only Demo",description="Layout contributed by a module view/theme directory"}
 * @param.title {default="Module Only Demo",name="Page Title",description="Page title"}
 * @param.content {default="",name="Content HTML",description="Main content"}
 */

$meta = $this->getData('meta') ?? [];
$title = htmlspecialchars((string)($meta['title'] ?? __('Module Only Demo')), ENT_QUOTES, 'UTF-8');
?>
<!DOCTYPE html>
<html lang="{{htmlLang}}" dir="{{htmlDir}}">
<head>
    <w:block class="Weline\Theme\Block\Partials" area="frontend" type="head" default-option="default"/>
</head>
<body class="theme-layout-codex-module-only">
<w:slot id="content" area="content" label="Content">
    <main class="w-container" data-layout-source="module">
        <h1><?= $title ?></h1>
        <w:slot id="codex-module-main" area="content" label="Module demo content">
            <?= (string)($meta['content'] ?? '') ?>
        </w:slot>
    </main>
</w:slot>
</body>
</html>
