<?php
/**
 * About content component
 *
 * @component_start
 * code: about-content
 * name: 关于我们内容
 * name_en: About Content
 * description: 企业/品牌关于页面内容展示组件，继承主题配色
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_stats => 显示数据卡片:select:yes|yes,no
 * display.show_values => 显示价值模块:select:yes|yes,no
 * display.show_team => 显示团队模块:select:yes|yes,no
 * display.show_cta => 显示行动区:select:yes|yes,no
 *
 * group:hero => Hero区域
 * hero.title => 页面标题:text:|留空使用页面标题
 * hero.subtitle => 页面副标题:textarea:Built for players who want quick rounds, clear controls, and reliable card-table flow.
 * hero.bg_color => 背景颜色:color:#fff4d8
 * hero.bg_gradient_end => 渐变结束色:color:#ffe4a8
 * hero.title_color => 标题颜色:color:#402217
 * hero.text_color => 正文颜色:color:#6b4e41
 * hero.padding => 内边距:number:80|px
 *
 * group:stats => 数据卡片
 * stats.stat_1_number => 数据1数值:text:30M+
 * stats.stat_1_label => 数据1标题:text:Hands Played Monthly
 * stats.stat_2_number => 数据2数值:text:4.8/5
 * stats.stat_2_label => 数据2标题:text:Player Rating
 * stats.stat_3_number => 数据3数值:text:24/7
 * stats.stat_3_label => 数据3标题:text:Table Availability
 * stats.stat_4_number => 数据4数值:text:1.2s
 * stats.stat_4_label => 数据4标题:text:Average Match Join
 * stats.number_color => 数值颜色:color:#402217
 * stats.label_color => 标题颜色:color:#9b7967
 * stats.card_bg => 卡片背景色:color:rgba(255,255,255,0.78)
 *
 * group:story => 故事模块
 * story.title => 标题:text:How Sunlit Durbar Took Shape
 * story.subtitle => 副标题:text:From static lobbies to a faster match rhythm
 * story.content => 内容:textarea:Callbreak Sunlit Durbar started with one goal: reduce waiting and keep every table interaction clear.
 * story.bg_color => 背景颜色:color:#fffdf8
 * story.title_color => 标题颜色:color:#402217
 *
 * group:values => 价值模块
 * values.title => 标题:text:What We Optimize Every Release
 * values.subtitle => 副标题:text:Three priorities shape each update
 * values.value_1_icon => 价值1图标:text:A
 * values.value_1_title => 价值1标题:text:Access Speed
 * values.value_1_desc => 价值1描述:textarea:Join rounds quickly with lightweight flows and clear route entry points.
 * values.value_2_icon => 价值2图标:text:F
 * values.value_2_title => 价值2标题:text:Fair Sessions
 * values.value_2_desc => 价值2描述:textarea:Transparent table logic and trackable wallet history keep trust high.
 * values.value_3_icon => 价值3图标:text:M
 * values.value_3_title => 价值3标题:text:Mobile First
 * values.value_3_desc => 价值3描述:textarea:Layouts, controls, and interactions are tuned for one-hand play.
 * values.bg_color => 背景颜色:color:#ddfff5
 *
 * group:team => 团队模块
 * team.title => 标题:text:Team Behind The Tables
 * team.subtitle => 副标题:text:Operators, designers, and game-flow engineers
 * team.member_1_icon => 成员1图标:text:R
 * team.member_1_name => 成员1姓名:text:Rohan Singh
 * team.member_1_role => 成员1职位:text:Product Lead
 * team.member_2_icon => 成员2图标:text:P
 * team.member_2_name => 成员2姓名:text:Priya Mehta
 * team.member_2_role => 成员2职位:text:Gameplay Ops
 * team.member_3_icon => 成员3图标:text:K
 * team.member_3_name => 成员3姓名:text:Karan Patel
 * team.member_3_role => 成员3职位:text:Reward Systems
 * team.member_4_icon => 成员4图标:text:S
 * team.member_4_name => 成员4姓名:text:Sneha Rao
 * team.member_4_role => 成员4职位:text:Player Success
 *
 * group:cta => 行动区
 * cta.title => 标题:text:Ready To Join A Faster Table?
 * cta.description => 描述:textarea:Download the latest Callbreak Sunlit Durbar build and jump into live match routes.
 * cta.button_text => 按钮文本:text:Download Now
 * cta.button_url => 按钮链接:text:#download
 * cta.button_bg => 按钮背景:text:linear-gradient(110deg, #ffe181 0%, #ffb05f 46%, #ff7156 100%)
 * cta.button_text_color => 按钮文字颜色:color:#2f160d
 *
 * @fields_end
 */

$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $component_config ?? $this->getData('component_config') ?? [];
$config = array_merge(
    is_array($styleSettings) ? $styleSettings : [],
    is_array($componentConfig) ? $componentConfig : []
);

$getConfig = function ($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

$showStats = $getConfig('display.show_stats', 'yes') !== 'no';
$showValues = $getConfig('display.show_values', 'yes') !== 'no';
$showTeam = $getConfig('display.show_team', 'yes') !== 'no';
$showCta = $getConfig('display.show_cta', 'yes') !== 'no';

$page = $this->getData('page');
$pageTitle = $page ? (string)($page->getData('title') ?: 'About Callbreak Sunlit Durbar') : 'About Callbreak Sunlit Durbar';
$pageContent = $page ? (string)($page->getData('content') ?: '') : '';

if (!isset($colors) || !is_array($colors)) {
    $colors = [];
}

$heroBg = $getConfig('hero.bg_color', $colors['hero_bg'] ?? '#fff6ea');
$heroGradientEnd = $getConfig('hero.bg_gradient_end', $colors['hero_bg_gradient_end'] ?? '#ffe2bb');
$heroTitleColor = $getConfig('hero.title_color', $colors['heading_color'] ?? '#3d2217');
$heroTextColor = $getConfig('hero.text_color', $colors['text_secondary'] ?? '#6b4e41');
$heroPadding = (int)$getConfig('hero.padding', '80');

$sectionBg1 = $getConfig('story.bg_color', $colors['section_bg_1'] ?? '#fffdf8');
$sectionBg2 = $getConfig('values.bg_color', $colors['section_bg_2'] ?? '#eef9ff');
$textPrimary = $colors['text_primary'] ?? '#332016';
$textSecondary = $colors['text_secondary'] ?? '#6b4e41';
$textMuted = $colors['text_muted'] ?? '#9a7d6d';
$borderLight = $colors['border_light'] ?? 'rgba(126,91,67,0.14)';
$cardBg = $getConfig('stats.card_bg', $colors['card_bg'] ?? 'rgba(255,255,255,0.78)');
$surfaceColor = $getConfig('color_scheme.surface', 'rgba(255,255,255,0.82)');
$primaryColor = $getConfig('color_scheme.primary', '#ff7a45');
$secondaryColor = $getConfig('color_scheme.secondary', '#00cdb6');

$heroTitle = $getConfig('hero.title', $pageTitle);
$heroSubtitle = $getConfig('hero.subtitle', 'Built for players who want quick rounds, clear controls, and reliable card-table flow.');

$stats = [
    [
        'number' => $getConfig('stats.stat_1_number', '30M+'),
        'label' => $getConfig('stats.stat_1_label', 'Hands Played Monthly'),
    ],
    [
        'number' => $getConfig('stats.stat_2_number', '4.8/5'),
        'label' => $getConfig('stats.stat_2_label', 'Player Rating'),
    ],
    [
        'number' => $getConfig('stats.stat_3_number', '24/7'),
        'label' => $getConfig('stats.stat_3_label', 'Table Availability'),
    ],
    [
        'number' => $getConfig('stats.stat_4_number', '1.2s'),
        'label' => $getConfig('stats.stat_4_label', 'Average Match Join'),
    ],
];
$statsNumberColor = $getConfig('stats.number_color', $heroTitleColor);
$statsLabelColor = $getConfig('stats.label_color', $textMuted);

$storyTitle = $getConfig('story.title', 'How Sunlit Durbar Took Shape');
$storySubtitle = $getConfig('story.subtitle', 'From static lobbies to a faster match rhythm');
$storyTitleColor = $getConfig('story.title_color', $heroTitleColor);
$storyContent = $getConfig('story.content', 'Callbreak Sunlit Durbar started with one goal: reduce waiting and keep every table interaction clear.');

$storyRaw = trim($storyContent !== '' ? $storyContent : strip_tags($pageContent));
$storyParagraphs = array_values(array_filter(array_map('trim', preg_split('/\r?\n+/', $storyRaw))));
if (empty($storyParagraphs)) {
    $storyParagraphs = [
        'Callbreak Sunlit Durbar started with one goal: reduce waiting and keep every table interaction clear.',
    ];
}
$storyLead = $storyParagraphs[0] ?? '';
$storyTrail = array_values(array_slice($storyParagraphs, 1));

$valuesTitle = $getConfig('values.title', 'What We Optimize Every Release');
$valuesSubtitle = $getConfig('values.subtitle', 'Three priorities shape each update');
$values = [
    [
        'icon' => $getConfig('values.value_1_icon', 'A'),
        'title' => $getConfig('values.value_1_title', 'Access Speed'),
        'desc' => $getConfig('values.value_1_desc', 'Join rounds quickly with lightweight flows and clear route entry points.'),
        'tone' => 'sun',
    ],
    [
        'icon' => $getConfig('values.value_2_icon', 'F'),
        'title' => $getConfig('values.value_2_title', 'Fair Sessions'),
        'desc' => $getConfig('values.value_2_desc', 'Transparent table logic and trackable wallet history keep trust high.'),
        'tone' => 'mint',
    ],
    [
        'icon' => $getConfig('values.value_3_icon', 'M'),
        'title' => $getConfig('values.value_3_title', 'Mobile First'),
        'desc' => $getConfig('values.value_3_desc', 'Layouts, controls, and interactions are tuned for one-hand play.'),
        'tone' => 'peach',
    ],
];

$teamTitle = $getConfig('team.title', 'Team Behind The Tables');
$teamSubtitle = $getConfig('team.subtitle', 'Operators, designers, and game-flow engineers');
$teamMembers = [
    [
        'icon' => $getConfig('team.member_1_icon', 'R'),
        'name' => $getConfig('team.member_1_name', 'Rohan Singh'),
        'role' => $getConfig('team.member_1_role', 'Product Lead'),
    ],
    [
        'icon' => $getConfig('team.member_2_icon', 'P'),
        'name' => $getConfig('team.member_2_name', 'Priya Mehta'),
        'role' => $getConfig('team.member_2_role', 'Gameplay Ops'),
    ],
    [
        'icon' => $getConfig('team.member_3_icon', 'K'),
        'name' => $getConfig('team.member_3_name', 'Karan Patel'),
        'role' => $getConfig('team.member_3_role', 'Reward Systems'),
    ],
    [
        'icon' => $getConfig('team.member_4_icon', 'S'),
        'name' => $getConfig('team.member_4_name', 'Sneha Rao'),
        'role' => $getConfig('team.member_4_role', 'Player Success'),
    ],
];

$ctaTitle = $getConfig('cta.title', 'Ready To Join A Faster Table?');
$ctaDescription = $getConfig('cta.description', 'Download the latest Callbreak Sunlit Durbar build and jump into live match routes.');
$ctaButtonText = $getConfig('cta.button_text', 'Download Now');
$ctaButtonUrl = $getConfig('cta.button_url', '#download');
$ctaButtonBg = $getConfig('cta.button_bg', $colors['button_primary_bg'] ?? 'linear-gradient(100deg, #ff9f4d 0%, #ff4d4d 100%)');
$ctaButtonTextColor = $getConfig('cta.button_text_color', $colors['button_primary_text'] ?? '#2b140d');

$instanceId = $component_instance_id ?? 'about-content-' . uniqid();
?>

<section id="<?= $instanceId ?>" class="durbar-about-page">
    <div class="about-hero">
        <span class="hero-glow hero-glow-left" data-durbar-parallax data-durbar-speed="0.06" aria-hidden="true"></span>
        <span class="hero-glow hero-glow-right" data-durbar-parallax data-durbar-speed="0.09" aria-hidden="true"></span>
        <div class="section-inner">
            <div class="hero-layout">
                <header class="hero-copy" data-durbar-reveal data-durbar-effect="animate__fadeInUp">
                    <p class="section-tag"><?= __('About') ?></p>
                    <h1><?= htmlspecialchars($heroTitle) ?></h1>
                    <p class="hero-summary"><?= htmlspecialchars($heroSubtitle) ?></p>
                </header>

                <?php if ($showStats): ?>
                    <aside class="hero-board" aria-label="About highlights" data-durbar-reveal data-durbar-effect="animate__fadeInRight">
                        <?php foreach ($stats as $index => $stat): ?>
                            <article class="board-chip board-chip-<?= $index + 1 ?>">
                                <strong><?= htmlspecialchars($stat['number']) ?></strong>
                                <span><?= htmlspecialchars($stat['label']) ?></span>
                            </article>
                        <?php endforeach; ?>
                    </aside>
                <?php endif; ?>
            </div>
        </div>
    </div>

    <section class="story-stage">
        <span class="story-aura story-aura-left" data-durbar-parallax data-durbar-speed="0.05" aria-hidden="true"></span>
        <span class="story-aura story-aura-right" data-durbar-parallax data-durbar-speed="0.08" aria-hidden="true"></span>
        <div class="section-inner">
            <div class="story-shell">
                <header class="story-intro" data-durbar-reveal data-durbar-effect="animate__fadeInUp">
                    <p class="section-tag"><?= __('Story') ?></p>
                    <h2><?= htmlspecialchars($storyTitle) ?></h2>
                    <?php if ($storySubtitle !== ''): ?>
                        <p class="section-lead"><?= htmlspecialchars($storySubtitle) ?></p>
                    <?php endif; ?>

                </header>

                <div class="story-panels">
                    <?php if ($storyLead !== ''): ?>
                        <article class="story-feature" data-durbar-reveal data-durbar-effect="animate__fadeInRight" data-wow-delay="90ms">
                            <span class="story-feature-index" aria-hidden="true">01</span>
                            <div class="story-feature-copy">
                                <p><?= htmlspecialchars($storyLead) ?></p>
                            </div>
                            <div class="story-feature-ornament" aria-hidden="true">
                                <span></span>
                                <span></span>
                                <span></span>
                            </div>
                        </article>
                    <?php endif; ?>

                    <?php if (!empty($storyTrail)): ?>
                        <ol class="story-stack" aria-label="<?= htmlspecialchars($storyTitle) ?>">
                            <?php foreach ($storyTrail as $index => $paragraph): ?>
                                <li class="story-strip" data-durbar-reveal data-durbar-effect="animate__fadeInUp" data-wow-delay="<?= min(($index + 1) * 90, 320) ?>ms">
                                    <span class="story-strip-index" aria-hidden="true"><?= str_pad((string)($index + 2), 2, '0', STR_PAD_LEFT) ?></span>
                                    <p><?= htmlspecialchars($paragraph) ?></p>
                                </li>
                            <?php endforeach; ?>
                        </ol>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </section>

    <?php if ($showValues): ?>
        <section class="values-stage">
            <span class="values-orbit" data-durbar-parallax data-durbar-speed="0.05" aria-hidden="true"></span>
            <div class="section-inner">
                <header class="section-heading align-right" data-durbar-reveal data-durbar-effect="animate__fadeInUp">
                    <p class="section-tag"><?= __('Values') ?></p>
                    <h2><?= htmlspecialchars($valuesTitle) ?></h2>
                    <?php if ($valuesSubtitle !== ''): ?>
                        <p class="section-lead"><?= htmlspecialchars($valuesSubtitle) ?></p>
                    <?php endif; ?>
                </header>

                <div class="values-grid">
                    <?php foreach ($values as $index => $value): ?>
                        <article class="value-card tone-<?= htmlspecialchars($value['tone']) ?>" data-durbar-reveal data-durbar-effect="animate__fadeInUp" data-wow-delay="<?= min($index * 90, 320) ?>ms">
                            <span class="value-icon"><?= htmlspecialchars($value['icon']) ?></span>
                            <div class="value-copy">
                                <h3><?= htmlspecialchars($value['title']) ?></h3>
                                <p><?= htmlspecialchars($value['desc']) ?></p>
                            </div>
                        </article>
                    <?php endforeach; ?>
                </div>
            </div>
        </section>
    <?php endif; ?>

    <?php if ($showTeam): ?>
        <section class="team-stage">
            <div class="section-inner">
                <header class="section-heading" data-durbar-reveal data-durbar-effect="animate__fadeInUp">
                    <p class="section-tag"><?= __('Team') ?></p>
                    <h2><?= htmlspecialchars($teamTitle) ?></h2>
                    <?php if ($teamSubtitle !== ''): ?>
                        <p class="section-lead"><?= htmlspecialchars($teamSubtitle) ?></p>
                    <?php endif; ?>
                </header>

                <div class="team-grid">
                    <?php foreach ($teamMembers as $index => $member): ?>
                        <article class="team-card" data-durbar-reveal data-durbar-effect="animate__fadeInUp" data-wow-delay="<?= min($index * 70, 280) ?>ms">
                            <span class="team-badge"><?= htmlspecialchars($member['icon']) ?></span>
                            <h3><?= htmlspecialchars($member['name']) ?></h3>
                            <p><?= htmlspecialchars($member['role']) ?></p>
                        </article>
                    <?php endforeach; ?>
                </div>
            </div>
        </section>
    <?php endif; ?>

    <?php if ($showCta): ?>
        <section class="about-cta">
            <div class="section-inner">
                <article class="cta-shell" data-durbar-reveal data-durbar-effect="animate__fadeInUp">
                    <div class="cta-copy">
                        <p class="section-tag"><?= __('Download') ?></p>
                        <h2><?= htmlspecialchars($ctaTitle) ?></h2>
                        <p><?= htmlspecialchars($ctaDescription) ?></p>
                    </div>
                    <a class="cta-link" href="<?= htmlspecialchars($ctaButtonUrl) ?>"><?= htmlspecialchars($ctaButtonText) ?></a>
                </article>
            </div>
        </section>
    <?php endif; ?>
</section>

<style>
#<?= $instanceId ?> {
    --hero-bg: <?= htmlspecialchars($heroBg) ?>;
    --hero-bg-end: <?= htmlspecialchars($heroGradientEnd) ?>;
    --hero-title: <?= htmlspecialchars($heroTitleColor) ?>;
    --hero-text: <?= htmlspecialchars($heroTextColor) ?>;
    --section-bg-1: <?= htmlspecialchars($sectionBg1) ?>;
    --section-bg-2: <?= htmlspecialchars($sectionBg2) ?>;
    --text-primary: <?= htmlspecialchars($textPrimary) ?>;
    --text-secondary: <?= htmlspecialchars($textSecondary) ?>;
    --text-muted: <?= htmlspecialchars($textMuted) ?>;
    --border-light: <?= htmlspecialchars($borderLight) ?>;
    --card-bg: <?= htmlspecialchars($cardBg) ?>;
    --surface: <?= htmlspecialchars($surfaceColor) ?>;
    --stats-number: <?= htmlspecialchars($statsNumberColor) ?>;
    --stats-label: <?= htmlspecialchars($statsLabelColor) ?>;
    --story-title: <?= htmlspecialchars($storyTitleColor) ?>;
    --primary: <?= htmlspecialchars($primaryColor) ?>;
    --secondary: <?= htmlspecialchars($secondaryColor) ?>;
    --cta-bg: <?= htmlspecialchars($ctaButtonBg) ?>;
    --cta-text: <?= htmlspecialchars($ctaButtonTextColor) ?>;
    color: var(--text-primary);
}

#<?= $instanceId ?> .section-inner {
    width: min(1180px, calc(100% - 1.2rem));
    margin: 0 auto;
}

#<?= $instanceId ?> .section-tag {
    margin: 0;
    display: inline-flex;
    width: fit-content;
    padding: 0.34rem 0.72rem;
    border-radius: 999px;
    border: 1px solid rgba(0, 205, 182, 0.28);
    background: rgba(0, 205, 182, 0.1);
    color: #0e9f8d;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.09em;
    text-transform: uppercase;
}

#<?= $instanceId ?> .section-heading {
    display: grid;
    gap: 0.8rem;
    margin-bottom: 1.4rem;
}

#<?= $instanceId ?> .section-heading.align-right {
    justify-items: end;
    text-align: right;
}

#<?= $instanceId ?> h1,
#<?= $instanceId ?> h2,
#<?= $instanceId ?> h3 {
    margin: 0;
    font-family: 'Syne', 'Manrope', sans-serif;
    letter-spacing: -0.04em;
}

#<?= $instanceId ?> .section-heading h2,
#<?= $instanceId ?> .story-intro h2 {
    color: var(--story-title);
    font-size: clamp(1.9rem, 4vw, 3rem);
    line-height: 1.02;
}

#<?= $instanceId ?> .section-lead,
#<?= $instanceId ?> .hero-summary {
    margin: 0;
    color: var(--text-secondary);
    line-height: 1.8;
    font-size: 1rem;
}

#<?= $instanceId ?> .about-hero {
    position: relative;
    overflow: hidden;
    padding: clamp(6.8rem, 10vw, <?= max($heroPadding, 104) ?>px) 0 clamp(2.6rem, 5vw, 4rem);
    background:
        linear-gradient(140deg, rgba(255, 255, 255, 0.65), rgba(255, 255, 255, 0) 36%),
        linear-gradient(135deg, var(--hero-bg) 0%, var(--hero-bg-end) 100%);
}

#<?= $instanceId ?> .hero-glow {
    position: absolute;
    border-radius: 999px;
    pointer-events: none;
    filter: blur(10px);
}

#<?= $instanceId ?> .hero-glow-left {
    top: 2rem;
    left: -4rem;
    width: clamp(9rem, 22vw, 16rem);
    height: clamp(9rem, 22vw, 16rem);
    background: radial-gradient(circle, rgba(0, 205, 182, 0.22), rgba(0, 205, 182, 0) 72%);
}

#<?= $instanceId ?> .hero-glow-right {
    right: -5rem;
    bottom: -2rem;
    width: clamp(10rem, 28vw, 19rem);
    height: clamp(10rem, 28vw, 19rem);
    background: radial-gradient(circle, rgba(255, 122, 69, 0.22), rgba(255, 122, 69, 0) 72%);
}

#<?= $instanceId ?> .hero-layout {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(280px, 0.92fr);
    gap: clamp(1rem, 3vw, 2rem);
    align-items: end;
}

#<?= $instanceId ?> .hero-copy {
    display: grid;
    gap: 1rem;
    max-width: 720px;
}

#<?= $instanceId ?> .hero-copy h1 {
    color: var(--hero-title);
    font-size: clamp(2.6rem, 6vw, 4.8rem);
    line-height: 0.94;
}

#<?= $instanceId ?> .hero-board {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.85rem;
    padding: clamp(0.9rem, 2vw, 1.25rem);
    border: 1px solid rgba(255, 255, 255, 0.55);
    border-radius: 28px;
    background: linear-gradient(180deg, rgba(255,255,255,0.92), rgba(255,245,230,0.78));
    box-shadow: 0 24px 44px rgba(230, 172, 120, 0.18);
    backdrop-filter: blur(16px);
}

#<?= $instanceId ?> .board-chip {
    display: grid;
    gap: 0.28rem;
    padding: 0.9rem;
    border-radius: 22px;
    border: 1px solid rgba(126, 91, 67, 0.12);
    background: rgba(255,255,255,0.72);
}

#<?= $instanceId ?> .board-chip strong {
    color: var(--stats-number);
    font-size: clamp(1.05rem, 2vw, 1.45rem);
    font-weight: 800;
}

#<?= $instanceId ?> .board-chip span {
    color: var(--stats-label);
    line-height: 1.55;
    font-size: 0.86rem;
}

#<?= $instanceId ?> .story-stage {
    position: relative;
    overflow: clip;
    background:
        radial-gradient(760px 260px at 95% 0%, rgba(255, 122, 69, 0.14), transparent 72%),
        radial-gradient(640px 260px at 0% 100%, rgba(0, 205, 182, 0.16), transparent 70%),
        var(--section-bg-1);
    padding: clamp(3rem, 7vw, 5rem) 0;
}

#<?= $instanceId ?> .story-aura {
    position: absolute;
    pointer-events: none;
    border-radius: 999px;
    filter: blur(10px);
}

#<?= $instanceId ?> .story-aura-left {
    left: -4rem;
    top: 3rem;
    width: clamp(9rem, 22vw, 14rem);
    height: clamp(9rem, 22vw, 14rem);
    background: radial-gradient(circle, rgba(255,255,255,0.78), rgba(255,255,255,0) 72%);
}

#<?= $instanceId ?> .story-aura-right {
    right: -3rem;
    bottom: 1rem;
    width: clamp(10rem, 24vw, 16rem);
    height: clamp(10rem, 24vw, 16rem);
    background: radial-gradient(circle, rgba(255, 168, 93, 0.3), rgba(255, 168, 93, 0) 72%);
}

#<?= $instanceId ?> .story-shell {
    display: grid;
    grid-template-columns: minmax(240px, 310px) minmax(0, 1fr);
    gap: clamp(1.2rem, 3vw, 2.4rem);
    align-items: start;
}

#<?= $instanceId ?> .story-intro {
    position: sticky;
    top: 104px;
    align-self: start;
    display: grid;
    gap: 0.85rem;
    padding: clamp(1rem, 2vw, 1.2rem);
    border-radius: 32px;
    border: 1px solid rgba(255, 255, 255, 0.72);
    background: linear-gradient(180deg, rgba(255,255,255,0.92), rgba(255,247,236,0.84));
    box-shadow: 0 22px 38px rgba(215, 186, 160, 0.16);
    backdrop-filter: blur(14px);
}

#<?= $instanceId ?> .story-panels {
    position: relative;
    display: grid;
    gap: 1rem;
    align-content: start;
}

#<?= $instanceId ?> .story-feature {
    position: relative;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 1rem;
    align-items: start;
    padding: clamp(1.2rem, 3vw, 1.65rem);
    border-radius: 38px 18px 38px 38px;
    border: 1px solid rgba(255, 255, 255, 0.76);
    background:
        linear-gradient(135deg, rgba(255,250,241,0.98), rgba(255,236,214,0.9)),
        rgba(255,255,255,0.88);
    box-shadow: 0 24px 42px rgba(215, 186, 160, 0.18);
    overflow: hidden;
}

#<?= $instanceId ?> .story-feature::before {
    content: '';
    position: absolute;
    inset: auto -2.8rem -3.3rem auto;
    width: 12rem;
    height: 12rem;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(255, 122, 69, 0.24), rgba(255, 122, 69, 0) 70%);
}

#<?= $instanceId ?> .story-feature::after {
    content: '';
    position: absolute;
    top: 1rem;
    right: 1rem;
    width: 4.6rem;
    height: 4.6rem;
    border-top: 1px solid rgba(61, 34, 23, 0.12);
    border-right: 1px solid rgba(61, 34, 23, 0.12);
    border-radius: 0 28px 0 0;
    opacity: 0.8;
}

#<?= $instanceId ?> .story-feature-index {
    position: relative;
    z-index: 1;
    width: 3.25rem;
    height: 4.2rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 24px 24px 14px 14px;
    background: linear-gradient(180deg, rgba(255,255,255,0.94), rgba(255,242,226,0.86));
    border: 1px solid rgba(126, 91, 67, 0.12);
    color: var(--hero-title);
    font-family: 'Syne', 'Manrope', sans-serif;
    font-size: 1.18rem;
    font-weight: 800;
    box-shadow: 0 12px 24px rgba(215, 186, 160, 0.16);
}

#<?= $instanceId ?> .story-feature-copy {
    position: relative;
    z-index: 1;
    display: grid;
    align-content: start;
    padding-right: 4.8rem;
}

#<?= $instanceId ?> .story-feature-copy p {
    margin: 0;
    color: var(--text-secondary);
    font-size: clamp(1.04rem, 1.8vw, 1.24rem);
    line-height: 1.92;
    word-break: break-word;
}

#<?= $instanceId ?> .story-feature-ornament {
    position: absolute;
    right: 1.15rem;
    bottom: 1rem;
    z-index: 1;
    display: grid;
    grid-template-columns: repeat(3, 0.78rem);
    gap: 0.42rem;
}

#<?= $instanceId ?> .story-feature-ornament span {
    width: 0.78rem;
    height: 0.78rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.92);
    box-shadow: 0 0 0 1px rgba(255,255,255,0.5);
}

#<?= $instanceId ?> .story-feature-ornament span:nth-child(2) {
    background: rgba(0, 205, 182, 0.92);
}

#<?= $instanceId ?> .story-feature-ornament span:nth-child(3) {
    background: rgba(255, 159, 77, 0.92);
}

#<?= $instanceId ?> .story-stack {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
}

#<?= $instanceId ?> .story-strip {
    position: relative;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.85rem;
    align-items: start;
    padding: 1.02rem 1.05rem;
    border-radius: 28px;
    border: 1px solid var(--border-light);
    box-shadow: 0 18px 30px rgba(215, 186, 160, 0.15);
    overflow: hidden;
}

#<?= $instanceId ?> .story-strip::after {
    content: '';
    position: absolute;
    inset: auto -20% -30% auto;
    width: 8rem;
    height: 8rem;
    border-radius: 999px;
    opacity: 0.34;
}

#<?= $instanceId ?> .story-strip:nth-child(4n + 1) {
    transform: translateX(-0.85rem);
    background: linear-gradient(180deg, rgba(255,249,236,0.98), rgba(255,237,215,0.92));
}

#<?= $instanceId ?> .story-strip:nth-child(4n + 1)::after {
    background: radial-gradient(circle, rgba(255, 159, 77, 0.26), rgba(255, 159, 77, 0) 72%);
}

#<?= $instanceId ?> .story-strip:nth-child(4n + 2) {
    transform: translateY(0.95rem);
    background: linear-gradient(180deg, rgba(240,255,251,0.98), rgba(226,250,245,0.9));
}

#<?= $instanceId ?> .story-strip:nth-child(4n + 2)::after {
    background: radial-gradient(circle, rgba(0, 205, 182, 0.24), rgba(0, 205, 182, 0) 72%);
}

#<?= $instanceId ?> .story-strip:nth-child(4n + 3) {
    transform: translateX(0.75rem);
    background: linear-gradient(180deg, rgba(255,247,243,0.98), rgba(255,232,224,0.9));
}

#<?= $instanceId ?> .story-strip:nth-child(4n + 3)::after {
    background: radial-gradient(circle, rgba(255, 122, 69, 0.24), rgba(255, 122, 69, 0) 72%);
}

#<?= $instanceId ?> .story-strip:nth-child(4n + 4) {
    background: linear-gradient(180deg, rgba(247,248,255,0.98), rgba(236,240,255,0.92));
}

#<?= $instanceId ?> .story-strip:nth-child(4n + 4)::after {
    background: radial-gradient(circle, rgba(112, 140, 255, 0.22), rgba(112, 140, 255, 0) 72%);
}

#<?= $instanceId ?> .story-strip-index {
    position: relative;
    z-index: 1;
    width: 2.55rem;
    height: 2.55rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 18px;
    background: rgba(255,255,255,0.9);
    border: 1px solid rgba(126, 91, 67, 0.12);
    color: var(--hero-title);
    font-family: 'Syne', 'Manrope', sans-serif;
    font-size: 0.96rem;
    font-weight: 800;
}

#<?= $instanceId ?> .story-strip p {
    position: relative;
    z-index: 1;
    margin: 0;
    color: var(--text-secondary);
    line-height: 1.84;
    font-size: 0.98rem;
    word-break: break-word;
}

#<?= $instanceId ?> .values-stage {
    position: relative;
    overflow: hidden;
    padding: clamp(3rem, 7vw, 5rem) 0;
    background:
        radial-gradient(680px 220px at 12% 100%, rgba(0, 205, 182, 0.16), transparent 72%),
        var(--section-bg-2);
}

#<?= $instanceId ?> .values-orbit {
    position: absolute;
    top: -4rem;
    right: -5rem;
    width: clamp(10rem, 26vw, 18rem);
    height: clamp(10rem, 26vw, 18rem);
    border-radius: 38% 62% 55% 45% / 54% 42% 58% 46%;
    background: radial-gradient(circle, rgba(255,255,255,0.44), rgba(255,255,255,0) 70%);
    pointer-events: none;
}

#<?= $instanceId ?> .values-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1rem;
}

#<?= $instanceId ?> .value-card {
    position: relative;
    display: grid;
    gap: 1rem;
    padding: 1.25rem;
    border-radius: 30px;
    border: 1px solid rgba(126, 91, 67, 0.12);
    box-shadow: 0 18px 32px rgba(156, 133, 114, 0.14);
    overflow: hidden;
}

#<?= $instanceId ?> .value-card::after {
    content: '';
    position: absolute;
    inset: auto -18% -22% auto;
    width: 8rem;
    height: 8rem;
    border-radius: 999px;
    opacity: 0.42;
}

#<?= $instanceId ?> .value-card.tone-sun {
    background: linear-gradient(180deg, rgba(255,249,236,0.96), rgba(255,236,213,0.9));
}

#<?= $instanceId ?> .value-card.tone-sun::after {
    background: radial-gradient(circle, rgba(255, 122, 69, 0.26), rgba(255, 122, 69, 0) 70%);
}

#<?= $instanceId ?> .value-card.tone-mint {
    background: linear-gradient(180deg, rgba(239,255,251,0.96), rgba(224,251,246,0.9));
    transform: translateY(1rem);
}

#<?= $instanceId ?> .value-card.tone-mint::after {
    background: radial-gradient(circle, rgba(0, 205, 182, 0.26), rgba(0, 205, 182, 0) 70%);
}

#<?= $instanceId ?> .value-card.tone-peach {
    background: linear-gradient(180deg, rgba(255,247,243,0.96), rgba(255,229,216,0.9));
}

#<?= $instanceId ?> .value-card.tone-peach::after {
    background: radial-gradient(circle, rgba(255, 179, 92, 0.28), rgba(255, 179, 92, 0) 70%);
}

#<?= $instanceId ?> .value-icon {
    position: relative;
    z-index: 1;
    width: 3rem;
    height: 3rem;
    border-radius: 22px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: rgba(255,255,255,0.86);
    color: var(--hero-title);
    font-size: 1.25rem;
    font-weight: 800;
    box-shadow: 0 10px 18px rgba(189, 145, 110, 0.12);
}

#<?= $instanceId ?> .value-copy {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 0.5rem;
}

#<?= $instanceId ?> .value-copy h3 {
    font-size: 1.35rem;
    color: var(--hero-title);
}

#<?= $instanceId ?> .value-copy p {
    margin: 0;
    color: var(--text-secondary);
    line-height: 1.8;
}

#<?= $instanceId ?> .team-stage {
    padding: clamp(3rem, 7vw, 5rem) 0;
    background:
        radial-gradient(720px 240px at 95% 0%, rgba(255, 179, 92, 0.18), transparent 70%),
        #fff7ef;
}

#<?= $instanceId ?> .team-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0.95rem;
}

#<?= $instanceId ?> .team-card {
    display: grid;
    gap: 0.75rem;
    align-content: start;
    padding: 1.2rem;
    min-height: 14rem;
    border-radius: 28px;
    border: 1px solid var(--border-light);
    background: linear-gradient(180deg, rgba(255,255,255,0.95), rgba(255,247,240,0.88));
    box-shadow: 0 18px 30px rgba(215, 186, 160, 0.16);
}

#<?= $instanceId ?> .team-card:nth-child(2n) {
    background: linear-gradient(180deg, rgba(240,255,251,0.98), rgba(230,249,245,0.9));
}

#<?= $instanceId ?> .team-card:nth-child(3n) {
    background: linear-gradient(180deg, rgba(255,247,235,0.98), rgba(255,234,212,0.9));
}

#<?= $instanceId ?> .team-badge {
    width: 3.2rem;
    height: 3.2rem;
    border-radius: 24px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(255,255,255,0.88), rgba(255,255,255,0.64));
    border: 1px solid rgba(126, 91, 67, 0.12);
    font-size: 1.15rem;
    font-weight: 800;
    color: var(--hero-title);
}

#<?= $instanceId ?> .team-card h3 {
    font-size: 1.28rem;
    color: var(--hero-title);
}

#<?= $instanceId ?> .team-card p {
    margin: 0;
    color: var(--text-secondary);
    line-height: 1.72;
}

#<?= $instanceId ?> .about-cta {
    padding: 0 0 clamp(3.2rem, 7vw, 4.6rem);
    background: #fff7ef;
}

#<?= $instanceId ?> .cta-shell {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 1rem;
    align-items: center;
    padding: clamp(1.2rem, 3vw, 1.6rem);
    border-radius: 32px;
    background:
        radial-gradient(380px 160px at 0% 100%, rgba(0,205,182,0.2), rgba(0,205,182,0) 72%),
        linear-gradient(135deg, rgba(255,247,222,0.96), rgba(255,235,217,0.94));
    border: 1px solid rgba(126, 91, 67, 0.12);
    box-shadow: 0 22px 38px rgba(215, 186, 160, 0.18);
}

#<?= $instanceId ?> .cta-copy {
    display: grid;
    gap: 0.8rem;
}

#<?= $instanceId ?> .cta-copy h2 {
    font-size: clamp(1.7rem, 4vw, 2.8rem);
    color: var(--hero-title);
}

#<?= $instanceId ?> .cta-copy p {
    margin: 0;
    color: var(--text-secondary);
    line-height: 1.82;
    max-width: 38rem;
}

#<?= $instanceId ?> .cta-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 3.3rem;
    padding: 0.85rem 1.35rem;
    border-radius: 999px;
    background: var(--cta-bg);
    color: var(--cta-text);
    text-decoration: none;
    font-weight: 800;
    letter-spacing: 0.01em;
    box-shadow: 0 18px 30px rgba(255, 122, 69, 0.22);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
}

#<?= $instanceId ?> .cta-link:hover {
    transform: translateY(-2px);
    box-shadow: 0 22px 34px rgba(255, 122, 69, 0.26);
}

@media (max-width: 1024px) {
    #<?= $instanceId ?> .story-shell,
    #<?= $instanceId ?> .hero-layout {
        grid-template-columns: 1fr;
    }

    #<?= $instanceId ?> .story-intro {
        position: static;
    }

    #<?= $instanceId ?> .story-stack {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    #<?= $instanceId ?> .values-grid,
    #<?= $instanceId ?> .team-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    #<?= $instanceId ?> .cta-shell {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 767px) {
    #<?= $instanceId ?> .section-heading.align-right {
        justify-items: start;
        text-align: left;
    }

    #<?= $instanceId ?> .hero-board {
        grid-template-columns: 1fr 1fr;
        border-radius: 24px;
    }

    #<?= $instanceId ?> .story-intro {
        border-radius: 28px;
    }

    #<?= $instanceId ?> .story-feature {
        grid-template-columns: 1fr;
        gap: 0.9rem;
        border-radius: 30px 16px 30px 30px;
    }

    #<?= $instanceId ?> .story-feature-index {
        width: 3rem;
        height: 3rem;
        border-radius: 20px;
    }

    #<?= $instanceId ?> .story-feature-copy {
        padding-right: 0;
    }

    #<?= $instanceId ?> .story-feature-ornament {
        right: 1rem;
        bottom: auto;
        top: 1rem;
    }

    #<?= $instanceId ?> .story-stack {
        grid-template-columns: 1fr;
    }

    #<?= $instanceId ?> .story-strip,
    #<?= $instanceId ?> .story-strip:nth-child(4n + 1),
    #<?= $instanceId ?> .story-strip:nth-child(4n + 2),
    #<?= $instanceId ?> .story-strip:nth-child(4n + 3),
    #<?= $instanceId ?> .story-strip:nth-child(4n + 4) {
        transform: none;
    }

    #<?= $instanceId ?> .values-grid,
    #<?= $instanceId ?> .team-grid,
    #<?= $instanceId ?> .hero-board {
        grid-template-columns: 1fr;
    }

    #<?= $instanceId ?> .value-card.tone-mint {
        transform: none;
    }

    #<?= $instanceId ?> .team-card {
        min-height: auto;
    }

    #<?= $instanceId ?> .cta-link {
        width: 100%;
    }
}
</style>
