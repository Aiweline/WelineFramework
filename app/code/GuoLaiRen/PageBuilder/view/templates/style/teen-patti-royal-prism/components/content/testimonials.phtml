<?php
/**
 * Teen Patti Royal Prism - Testimonials Component
 *
 * @component_start
 * code: testimonials
 * name: 用户评价
 * name_en: Testimonials
 * description: Player feedback rendered as a responsive signal wall.
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_section => 显示此区域:select:yes|yes,no
 * display.show_title => 显示标题:select:yes|yes,no
 * display.show_rating => 显示评分:select:yes|yes,no
 * display.show_avatar => 显示头像:select:yes|yes,no
 *
 * group:style => 样式配置
 * style.bg_color => 背景颜色:color:#ecf8ff
 * style.title_color => 标题颜色:color:#3d2217
 * style.title_font_size => 标题字号(PC端):number:36|px
 * style.title_font_size_mobile => 标题字号(移动端):number:24|px
 * style.card_bg_color => 卡片背景色:color:rgba(255,255,255,0.84)
 * style.card_border_radius => 卡片圆角:number:18|px
 * style.card_border_left_color => 卡片高亮色:color:#53e0c5
 * style.card_border_left_width => 卡片高亮宽度:number:3|px
 * style.card_padding => 卡片内边距:number:24|px
 * style.name_color => 姓名颜色:color:#3d2217
 * style.name_font_size => 姓名字号:number:16|px
 * style.rating_color => 评分颜色:color:#ffc27a
 * style.content_color => 内容颜色:color:#6b4e41
 * style.content_font_size => 内容字号:number:14|px
 * style.avatar_size => 头像尺寸:number:58|px
 * style.avatar_border_radius => 头像圆角:number:50|%
 * style.section_padding => 区域内边距(PC端):number:60|px
 * style.section_padding_mobile => 区域内边距(移动端):number:40|px
 * style.card_gap => 卡片间距:number:20|px
 *
 * group:avatars => 头像配置
 * avatars.avatar_1 => 评价1头像:textarea:|默认使用本地头像
 * avatars.avatar_2 => 评价2头像:textarea:|默认使用本地头像
 * avatars.avatar_3 => 评价3头像:textarea:|默认使用本地头像
 *
 * group:texts => 文本内容
 * texts.title => 区域标题:text:Player Signals From The Tables
 * texts.testimonial_1_name => 评价1姓名:text:Rajesh Kumar (Mumbai)
 * texts.testimonial_1_rating => 评价1评分:text:4.9/5
 * texts.testimonial_1_content => 评价1内容:textarea:Tables load quickly, the banner feels lively, and the lobby is easier to scan on mobile.
 * texts.testimonial_2_name => 评价2姓名:text:Priya Sharma (Delhi)
 * texts.testimonial_2_rating => 评价2评分:text:4.8/5
 * texts.testimonial_2_content => 评价2内容:textarea:I like the color separation between sections because each route feels easier to spot.
 * texts.testimonial_3_name => 评价3姓名:text:Amit Patel (Ahmedabad)
 * texts.testimonial_3_rating => 评价3评分:text:4.8/5
 * texts.testimonial_3_content => 评价3内容:textarea:The page feels energetic without becoming hard to read, especially on a smaller screen.
 *
 * @fields_end
 */

$componentId = 'prism-testimonials-' . uniqid();

$page = $this->getData('page');
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$config = array_merge($styleSettings, $componentConfig);

$getConfig = function ($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

$display = $getConfig('display.show_section', $getConfig('testimonials.display', 'yes')) !== 'no';
if (!$display) {
    return;
}
$showTitle = $getConfig('display.show_title', 'yes') !== 'no';
$showRating = $getConfig('display.show_rating', 'yes') !== 'no';
$showAvatar = $getConfig('display.show_avatar', 'yes') !== 'no';

$bgColor = $getConfig('style.bg_color', $getConfig('testimonials.bg_color', '#ecf8ff'));
$titleColor = $getConfig('style.title_color', '#3d2217');
$titleFontSize = (int)$getConfig('style.title_font_size', '36');
$titleFontSizeMobile = (int)$getConfig('style.title_font_size_mobile', '24');
$cardBgColor = $getConfig('style.card_bg_color', 'rgba(255,255,255,0.84)');
$cardBorderRadius = (int)$getConfig('style.card_border_radius', '18');
$cardAccentColor = $getConfig('style.card_border_left_color', '#53e0c5');
$cardAccentWidth = (int)$getConfig('style.card_border_left_width', '3');
$cardPadding = (int)$getConfig('style.card_padding', '24');
$nameColor = $getConfig('style.name_color', '#3d2217');
$nameFontSize = (int)$getConfig('style.name_font_size', '16');
$ratingColor = $getConfig('style.rating_color', '#ffc27a');
$contentColor = $getConfig('style.content_color', '#6b4e41');
$contentFontSize = (int)$getConfig('style.content_font_size', '14');
$avatarSize = (int)$getConfig('style.avatar_size', '58');
$avatarRadius = (int)$getConfig('style.avatar_border_radius', '50');
$sectionPadding = (int)$getConfig('style.section_padding', '60');
$sectionPaddingMobile = (int)$getConfig('style.section_padding_mobile', '40');
$cardGap = (int)$getConfig('style.card_gap', '20');

$defaultAvatar1 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/teen-patti-royal-prism/asset/img/user/Rajesh-Kumar-1.avif');
$defaultAvatar2 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/teen-patti-royal-prism/asset/img/user/Priya-Sharma-2.avif');
$defaultAvatar3 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/teen-patti-royal-prism/asset/img/user/Amit-Patel-3.avif');

$avatar1 = $getConfig('avatars.avatar_1', $getConfig('testimonials.avatar_1', '')) ?: $defaultAvatar1;
$avatar2 = $getConfig('avatars.avatar_2', $getConfig('testimonials.avatar_2', '')) ?: $defaultAvatar2;
$avatar3 = $getConfig('avatars.avatar_3', $getConfig('testimonials.avatar_3', '')) ?: $defaultAvatar3;

$defaultSectionTitle = 'Player Signals From The Tables';
$title = $getConfig('texts.title', $getConfig('testimonials.title', $getConfig('texts.testimonials_title', $defaultSectionTitle)));
$name1 = $getConfig('texts.testimonial_1_name', 'Rajesh Kumar (Mumbai)');
$rating1 = $getConfig('texts.testimonial_1_rating', '4.9/5');
$content1 = $getConfig('texts.testimonial_1_content', 'Tables load quickly, the banner feels lively, and the lobby is easier to scan on mobile.');
$name2 = $getConfig('texts.testimonial_2_name', 'Priya Sharma (Delhi)');
$rating2 = $getConfig('texts.testimonial_2_rating', '4.8/5');
$content2 = $getConfig('texts.testimonial_2_content', 'I like the color separation between sections because each route feels easier to spot.');
$name3 = $getConfig('texts.testimonial_3_name', 'Amit Patel (Ahmedabad)');
$rating3 = $getConfig('texts.testimonial_3_rating', '4.8/5');
$content3 = $getConfig('texts.testimonial_3_content', 'The page feels energetic without becoming hard to read, especially on a smaller screen.');

$cards = [
    ['avatar' => $avatar1, 'name' => $name1, 'rating' => $rating1, 'content' => $content1, 'tone' => 'sun'],
    ['avatar' => $avatar2, 'name' => $name2, 'rating' => $rating2, 'content' => $content2, 'tone' => 'mint'],
    ['avatar' => $avatar3, 'name' => $name3, 'rating' => $rating3, 'content' => $content3, 'tone' => 'rose'],
];

$ratingValues = [];
foreach ($cards as $card) {
    if (preg_match('/(\d+(?:\.\d+)?)/', (string)$card['rating'], $matches)) {
        $ratingValues[] = (float)$matches[1];
    }
}
$averageScore = $ratingValues ? number_format(array_sum($ratingValues) / count($ratingValues), 1) : '4.8';
?>
<section id="<?= $componentId ?>" class="prism-testimonials" aria-labelledby="<?= $componentId ?>-title">
    <div class="signal-beam signal-beam-left" data-prism-parallax data-prism-speed="0.05" aria-hidden="true"></div>
    <div class="signal-beam signal-beam-right" data-prism-parallax data-prism-speed="0.08" aria-hidden="true"></div>
    <div class="signal-dot-cluster" data-prism-parallax data-prism-speed="0.04" aria-hidden="true">
        <span></span>
        <span></span>
        <span></span>
        <span></span>
    </div>
    <div class="section-inner">
        <?php if ($showTitle): ?>
            <header class="testimonials-head">
                <span class="testimonials-kicker" data-prism-reveal data-prism-effect="animate__fadeInDown">Signal wall</span>
                <h2 id="<?= $componentId ?>-title" data-prism-reveal data-prism-effect="animate__fadeInUp" data-wow-delay="70ms"><?= htmlspecialchars($title) ?></h2>
            </header>
        <?php endif; ?>

        <div class="signal-stage">
            <aside class="signal-hub prism-glass" data-prism-reveal data-prism-effect="animate__fadeInLeft" data-wow-delay="110ms">
                <span class="signal-label">Average player score</span>
                <strong><?= htmlspecialchars($averageScore) ?></strong>
                <div class="signal-hub-bars" aria-hidden="true">
                    <span></span>
                    <span></span>
                    <span></span>
                </div>
            </aside>

            <div class="signal-grid">
                <?php foreach ($cards as $index => $card): ?>
                    <article class="signal-card signal-card-<?= htmlspecialchars($card['tone']) ?>" data-prism-reveal data-prism-effect="<?= $index === 1 ? 'animate__fadeInUp' : 'animate__fadeInRight' ?>" data-wow-delay="<?= htmlspecialchars((string)(180 + ($index * 90))) ?>ms">
                        <div class="signal-card-head">
                            <?php if ($showAvatar): ?>
                                <img src="<?= htmlspecialchars($card['avatar']) ?>" alt="<?= htmlspecialchars($card['name']) ?>" loading="lazy" decoding="async">
                            <?php endif; ?>
                            <div>
                                <h3><?= htmlspecialchars($card['name']) ?></h3>
                                <?php if ($showRating): ?>
                                    <p class="rating"><?= htmlspecialchars($card['rating']) ?></p>
                                <?php endif; ?>
                            </div>
                        </div>
                        <p class="signal-body"><?= htmlspecialchars($card['content']) ?></p>
                    </article>
                <?php endforeach; ?>
            </div>
        </div>
    </div>
</section>
<style>
#<?= $componentId ?> {
    position: relative;
    overflow: clip;
    isolation: isolate;
    padding: <?= (int)$sectionPadding ?>px 0;
    background:
        radial-gradient(820px 320px at 8% 0%, rgba(255, 214, 97, 0.18), transparent 68%),
        radial-gradient(920px 400px at 100% 100%, rgba(0, 205, 182, 0.16), transparent 68%),
        <?= htmlspecialchars($bgColor) ?>;
}

#<?= $componentId ?> .signal-beam,
#<?= $componentId ?> .signal-dot-cluster {
    position: absolute;
    pointer-events: none;
    z-index: 2;
}

#<?= $componentId ?> .signal-beam {
    width: 18px;
    border-radius: 999px;
    opacity: 0.8;
}

#<?= $componentId ?> .signal-beam-left {
    top: 54px;
    left: clamp(1rem, 6vw, 5rem);
    height: 130px;
    background: linear-gradient(180deg, rgba(255,255,255,0), rgba(255, 198, 97, 0.9), rgba(255,255,255,0));
}

#<?= $componentId ?> .signal-beam-right {
    right: clamp(1rem, 7vw, 6rem);
    bottom: 60px;
    height: 170px;
    background: linear-gradient(180deg, rgba(255,255,255,0), rgba(31, 202, 182, 0.9), rgba(255,255,255,0));
}

#<?= $componentId ?> .signal-dot-cluster {
    top: 92px;
    right: clamp(1rem, 10vw, 8rem);
    display: grid;
    grid-template-columns: repeat(2, 10px);
    gap: 0.5rem;
    filter: drop-shadow(0 12px 20px rgba(201, 141, 104, 0.12));
}

#<?= $componentId ?> .signal-dot-cluster span {
    width: 10px;
    height: 10px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.9);
}

#<?= $componentId ?> .signal-dot-cluster span:nth-child(2),
#<?= $componentId ?> .signal-dot-cluster span:nth-child(3) {
    background: rgba(31, 202, 182, 0.84);
}

#<?= $componentId ?> .section-inner {
    width: min(1240px, calc(100% - 1.2rem));
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

#<?= $componentId ?> .testimonials-head {
    display: grid;
    margin-bottom: 1.7rem;
    position: relative;
    z-index: 1;
}

#<?= $componentId ?> .testimonials-kicker {
    display: inline-flex;
    width: fit-content;
    padding: 0.34rem 0.74rem;
    border-radius: 999px;
    background: rgba(255,255,255,0.76);
    border: 1px solid rgba(255,255,255,0.86);
    color: #7a5746;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    margin-bottom: 0.74rem;
}

#<?= $componentId ?> .testimonials-head h2 {
    margin: 0;
    color: <?= htmlspecialchars($titleColor) ?>;
    font-family: 'Syne', 'Manrope', sans-serif;
    font-size: clamp(<?= (int)$titleFontSizeMobile ?>px, 5vw, <?= (int)$titleFontSize ?>px);
    font-weight: 800;
    line-height: 0.95;
    letter-spacing: -0.05em;
    max-width: none;
    text-wrap: balance;
}

#<?= $componentId ?> .signal-stage {
    display: grid;
    grid-template-columns: minmax(260px, 0.42fr) minmax(0, 1fr);
    gap: <?= (int)$cardGap ?>px;
    align-items: start;
}

#<?= $componentId ?> .signal-hub {
    display: grid;
    gap: 0.8rem;
    padding: 1.2rem;
    border-radius: 30px;
    background: linear-gradient(180deg, rgba(255,255,255,0.84), rgba(255, 240, 211, 0.8));
}

#<?= $componentId ?> .signal-label {
    display: inline-flex;
    width: fit-content;
    padding: 0.28rem 0.58rem;
    border-radius: 999px;
    background: rgba(255,255,255,0.78);
    color: #8b6856;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
}

#<?= $componentId ?> .signal-hub strong {
    font-family: 'Syne', 'Manrope', sans-serif;
    font-size: clamp(3rem, 7vw, 4.6rem);
    line-height: 0.9;
    color: #341a0f;
}

#<?= $componentId ?> .signal-hub p {
    margin: 0;
}

#<?= $componentId ?> .signal-hub-bars {
    display: grid;
    gap: 0.5rem;
}

#<?= $componentId ?> .signal-hub-bars span {
    display: block;
    height: 12px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.88);
}

#<?= $componentId ?> .signal-hub-bars span:nth-child(1) {
    width: 100%;
}

#<?= $componentId ?> .signal-hub-bars span:nth-child(2) {
    width: 72%;
    background: rgba(255, 205, 108, 0.92);
}

#<?= $componentId ?> .signal-hub-bars span:nth-child(3) {
    width: 48%;
    background: rgba(83, 224, 197, 0.92);
}

#<?= $componentId ?> .signal-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: <?= (int)$cardGap ?>px;
}

#<?= $componentId ?> .signal-card {
    display: grid;
    gap: 1rem;
    min-height: 100%;
    padding: <?= (int)$cardPadding ?>px;
    border-radius: <?= (int)$cardBorderRadius ?>px;
    background: <?= htmlspecialchars($cardBgColor) ?>;
    border: 1px solid rgba(255,255,255,0.84);
    border-left: <?= (int)$cardAccentWidth ?>px solid <?= htmlspecialchars($cardAccentColor) ?>;
    box-shadow: 0 24px 42px rgba(195, 148, 106, 0.12);
}

#<?= $componentId ?> .signal-card-sun {
    background: linear-gradient(180deg, rgba(255,255,255,0.86), rgba(255, 243, 196, 0.84));
}

#<?= $componentId ?> .signal-card-mint {
    background: linear-gradient(180deg, rgba(255,255,255,0.86), rgba(216, 255, 244, 0.84));
}

#<?= $componentId ?> .signal-card-rose {
    background: linear-gradient(180deg, rgba(255,255,255,0.86), rgba(255, 229, 234, 0.84));
}

#<?= $componentId ?> .signal-card-head {
    display: flex;
    align-items: center;
    gap: 0.8rem;
}

#<?= $componentId ?> .signal-card-head img {
    width: <?= (int)$avatarSize ?>px;
    height: <?= (int)$avatarSize ?>px;
    border-radius: <?= (int)$avatarRadius ?>%;
    object-fit: cover;
    flex-shrink: 0;
}

#<?= $componentId ?> .signal-card-head h3 {
    margin: 0;
    color: <?= htmlspecialchars($nameColor) ?>;
    font-size: <?= (int)$nameFontSize ?>px;
    line-height: 1.3;
}

#<?= $componentId ?> .rating {
    margin: 0.3rem 0 0;
    color: <?= htmlspecialchars($ratingColor) ?>;
    font-size: 0.84rem;
    font-weight: 800;
}

#<?= $componentId ?> .signal-body {
    margin: 0;
    color: <?= htmlspecialchars($contentColor) ?>;
    font-size: <?= (int)$contentFontSize ?>px;
    line-height: 1.8;
}

@media (max-width: 980px) {
    #<?= $componentId ?> .signal-stage {
        grid-template-columns: 1fr;
    }

    #<?= $componentId ?> .signal-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }
}

@media (max-width: 767px) {
    #<?= $componentId ?> {
        padding: <?= (int)$sectionPaddingMobile ?>px 0;
    }

    #<?= $componentId ?> .signal-beam {
        width: 14px;
        opacity: 0.92;
    }

    #<?= $componentId ?> .signal-beam-left {
        top: 20px;
        left: 0.45rem;
        height: 88px;
    }

    #<?= $componentId ?> .signal-beam-right {
        top: 14px;
        right: 1rem;
        bottom: auto;
        height: 92px;
    }

    #<?= $componentId ?> .signal-dot-cluster {
        top: 104px;
        right: 1rem;
        grid-template-columns: repeat(2, 9px);
        gap: 0.42rem;
    }

    #<?= $componentId ?> .signal-dot-cluster span {
        width: 9px;
        height: 9px;
    }

    #<?= $componentId ?> .testimonials-head {
        padding: 0 4.1rem 0 0.9rem;
        min-height: 7.6rem;
    }

    #<?= $componentId ?> .signal-grid {
        grid-template-columns: 1fr;
        gap: 0.9rem;
        padding: 0;
        overflow: visible;
    }

    #<?= $componentId ?> .signal-card {
        --signal-mobile-overhang: 2.35rem;
        width: calc(100% + var(--signal-mobile-overhang));
        max-width: none;
    }

    #<?= $componentId ?> .signal-card:nth-child(odd) {
        margin-left: calc(var(--signal-mobile-overhang) * -1);
        margin-right: 0;
        border-left-width: 0;
        border-right: <?= (int)$cardAccentWidth ?>px solid <?= htmlspecialchars($cardAccentColor) ?>;
        border-top-left-radius: 0;
        border-bottom-left-radius: 0;
    }

    #<?= $componentId ?> .signal-card:nth-child(even) {
        margin-left: 0;
        margin-right: calc(var(--signal-mobile-overhang) * -1);
        border-right-width: 0;
        border-left: <?= (int)$cardAccentWidth ?>px solid <?= htmlspecialchars($cardAccentColor) ?>;
        border-top-right-radius: 0;
        border-bottom-right-radius: 0;
    }
}
</style>
