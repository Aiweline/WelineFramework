<?php
/**
 * Teen Patti Royal Prism - FAQ Component
 *
 * @component_start
 * code: faq
 * name: 常见问题
 * name_en: FAQ
 * description: FAQ rendered as a responsive editorial help board.
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_section => 显示此区域:select:yes|yes,no
 * display.show_title => 显示标题:select:yes|yes,no
 * display.first_open => 默认展开第一项:select:yes|yes,no
 *
 * group:style => 样式配置
 * style.bg_color => 背景颜色:color:#fff1cc
 * style.title_color => 标题颜色:color:#3d2217
 * style.title_font_size => 标题字号(PC端):number:36|px
 * style.title_font_size_mobile => 标题字号(移动端):number:24|px
 * style.item_bg_color => 项目背景色:color:rgba(255,255,255,0.82)
 * style.item_border_radius => 项目圆角:number:16|px
 * style.item_border_color => 项目边框颜色:color:rgba(126,91,67,0.14)
 * style.item_gap => 项目间距:number:14|px
 * style.question_color => 问题颜色:color:#3a2015
 * style.question_font_size => 问题字号(PC端):number:16|px
 * style.question_font_size_mobile => 问题字号(移动端):number:14|px
 * style.question_padding => 问题内边距:number:18|px
 * style.question_hover_bg => 问题悬停背景色:color:rgba(255,248,240,0.94)
 * style.icon_color => 图标颜色:color:#53e0c5
 * style.icon_size => 图标尺寸:number:18|px
 * style.answer_color => 答案颜色:color:#6b4e41
 * style.answer_font_size => 答案字号:number:14|px
 * style.answer_padding => 答案内边距:number:18|px
 * style.section_padding => 区域内边距(PC端):number:60|px
 * style.section_padding_mobile => 区域内边距(移动端):number:40|px
 *
 * group:texts => 文本内容
 * texts.title => 区域标题:text:Quick Answers Before You Install
 * texts.faq_1_question => 问题1:text:Is Teen Patti Royal Prism safe for real-money play?
 * texts.faq_1_answer => 答案1:textarea:Protected sessions and transaction checks support account safety. Always install from the official source.
 * texts.faq_2_question => 问题2:text:How do I download the latest APK build?
 * texts.faq_2_answer => 答案2:textarea:Use the official download button on this page and allow installation from trusted sources on Android.
 * texts.faq_3_question => 问题3:text:Can I switch between Teen Patti, Rummy, and Slots quickly?
 * texts.faq_3_answer => 答案3:textarea:Yes. The lobby is designed for fast route switching so you can move between game styles in a few taps.
 * texts.faq_4_question => 问题4:text:How are withdrawals handled?
 * texts.faq_4_answer => 答案4:textarea:Wallet history tracks each payout request so status changes stay visible in one place.
 * texts.faq_5_question => 问题5:text:Does it work smoothly on low-network conditions?
 * texts.faq_5_answer => 答案5:textarea:Gameplay is optimized for unstable networks and active rounds recover with session continuity.
 *
 * @fields_end
 */

$componentId = 'prism-faq-' . uniqid();

$page = $this->getData('page');
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$config = array_merge($styleSettings, $componentConfig);

$getConfig = function ($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

$display = $getConfig('display.show_section', $getConfig('faq.display', 'yes')) !== 'no';
if (!$display) {
    return;
}
$showTitle = $getConfig('display.show_title', 'yes') !== 'no';
$firstOpen = $getConfig('display.first_open', 'yes') === 'yes';

$bgColor = $getConfig('style.bg_color', $getConfig('faq.bg_color', '#fff1cc'));
$titleColor = $getConfig('style.title_color', '#3d2217');
$titleFontSize = (int)$getConfig('style.title_font_size', '36');
$titleFontSizeMobile = (int)$getConfig('style.title_font_size_mobile', '24');
$itemBgColor = $getConfig('style.item_bg_color', 'rgba(255,255,255,0.82)');
$itemBorderRadius = (int)$getConfig('style.item_border_radius', '16');
$itemBorderColor = $getConfig('style.item_border_color', 'rgba(126,91,67,0.14)');
$itemGap = (int)$getConfig('style.item_gap', '14');
$questionColor = $getConfig('style.question_color', '#3a2015');
$questionFontSize = (int)$getConfig('style.question_font_size', '16');
$questionFontSizeMobile = (int)$getConfig('style.question_font_size_mobile', '14');
$questionPadding = (int)$getConfig('style.question_padding', '18');
$questionHoverBg = $getConfig('style.question_hover_bg', 'rgba(255,248,240,0.94)');
$iconColor = $getConfig('style.icon_color', '#53e0c5');
$iconSize = (int)$getConfig('style.icon_size', '18');
$answerColor = $getConfig('style.answer_color', '#6b4e41');
$answerFontSize = (int)$getConfig('style.answer_font_size', '14');
$answerPadding = (int)$getConfig('style.answer_padding', '18');
$sectionPadding = (int)$getConfig('style.section_padding', '60');
$sectionPaddingMobile = (int)$getConfig('style.section_padding_mobile', '40');

$defaultSectionTitle = 'Quick Answers Before You Install';
$title = $getConfig('texts.title', $getConfig('faq.title', $getConfig('texts.faq_title', $defaultSectionTitle)));

$faqs = [
    [
        'question' => $getConfig('texts.faq_1_question', 'Is Teen Patti Royal Prism safe for real-money play?'),
        'answer' => $getConfig('texts.faq_1_answer', 'Protected sessions and transaction checks support account safety. Always install from the official source.'),
        'tag' => 'Safety',
    ],
    [
        'question' => $getConfig('texts.faq_2_question', 'How do I download the latest APK build?'),
        'answer' => $getConfig('texts.faq_2_answer', 'Use the official download button on this page and allow installation from trusted sources on Android.'),
        'tag' => 'Install',
    ],
    [
        'question' => $getConfig('texts.faq_3_question', 'Can I switch between Teen Patti, Rummy, and Slots quickly?'),
        'answer' => $getConfig('texts.faq_3_answer', 'Yes. The lobby is designed for fast route switching so you can move between game styles in a few taps.'),
        'tag' => 'Routes',
    ],
    [
        'question' => $getConfig('texts.faq_4_question', 'How are withdrawals handled?'),
        'answer' => $getConfig('texts.faq_4_answer', 'Wallet history tracks each payout request so status changes stay visible in one place.'),
        'tag' => 'Wallet',
    ],
    [
        'question' => $getConfig('texts.faq_5_question', 'Does it work smoothly on low-network conditions?'),
        'answer' => $getConfig('texts.faq_5_answer', 'Gameplay is optimized for unstable networks and active rounds recover with session continuity.'),
        'tag' => 'Network',
    ],
];
?>
<section id="<?= $componentId ?>" class="prism-faq" aria-labelledby="<?= $componentId ?>-title">
    <div class="faq-orbit faq-orbit-left" data-prism-parallax data-prism-speed="0.05" aria-hidden="true"></div>
    <div class="faq-orbit faq-orbit-right" data-prism-parallax data-prism-speed="0.08" aria-hidden="true"></div>
    <div class="faq-chip-stack" data-prism-parallax data-prism-speed="0.04" aria-hidden="true">
        <span></span>
        <span></span>
        <span></span>
    </div>
    <div class="section-inner">
        <div class="faq-layout">
            <aside class="faq-intro prism-glass">
                <span class="faq-kicker" data-prism-reveal data-prism-effect="animate__fadeInDown">Help board</span>
                <?php if ($showTitle): ?>
                    <h2 id="<?= $componentId ?>-title" data-prism-reveal data-prism-effect="animate__fadeInUp" data-wow-delay="70ms"><?= htmlspecialchars($title) ?></h2>
                <?php endif; ?>
                <div class="faq-meta" data-prism-reveal data-prism-effect="animate__fadeInUp" data-wow-delay="130ms">
                    <span><?= count($faqs) ?> answers</span>
                    <span>Mobile friendly</span>
                    <span>Clear hierarchy</span>
                </div>
            </aside>

            <div class="faq-board">
                <?php foreach ($faqs as $index => $faq): ?>
                    <details class="faq-panel faq-panel-<?= (int)($index + 1) ?>" data-prism-reveal data-prism-effect="<?= $index % 2 === 0 ? 'animate__fadeInLeft' : 'animate__fadeInRight' ?>" data-wow-delay="<?= htmlspecialchars((string)(140 + ($index * 70))) ?>ms"<?= ($firstOpen && $index === 0) ? ' open' : '' ?>>
                        <summary>
                            <span class="faq-badge"><?= htmlspecialchars(sprintf('%02d', $index + 1)) ?></span>
                            <span class="faq-heading-wrap">
                                <strong><?= htmlspecialchars($faq['question']) ?></strong>
                                <small><?= htmlspecialchars($faq['tag']) ?></small>
                            </span>
                            <span class="faq-icon" aria-hidden="true"></span>
                        </summary>
                        <div class="faq-answer">
                            <p><?= nl2br(htmlspecialchars($faq['answer'])) ?></p>
                        </div>
                    </details>
                <?php endforeach; ?>
            </div>
        </div>
    </div>
</section>
<style>
#<?= $componentId ?> {
    position: relative;
    overflow: clip;
    isolation: isolate;
    padding: <?= (int)$sectionPadding ?>px 0;
    background:
        radial-gradient(760px 260px at 0% 100%, rgba(255, 118, 77, 0.12), transparent 68%),
        radial-gradient(920px 340px at 100% 0%, rgba(0, 205, 182, 0.16), transparent 68%),
        <?= htmlspecialchars($bgColor) ?>;
}

#<?= $componentId ?> .faq-orbit,
#<?= $componentId ?> .faq-chip-stack {
    position: absolute;
    pointer-events: none;
}

#<?= $componentId ?> .faq-orbit {
    border-radius: 999px;
    filter: blur(10px);
}

#<?= $componentId ?> .faq-orbit-left {
    left: -80px;
    top: 110px;
    width: 220px;
    height: 220px;
    background: radial-gradient(circle, rgba(255, 187, 89, 0.18), rgba(255, 187, 89, 0.02) 72%, transparent 80%);
}

#<?= $componentId ?> .faq-orbit-right {
    right: -86px;
    bottom: 42px;
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, rgba(0, 205, 182, 0.18), rgba(0, 205, 182, 0.02) 72%, transparent 80%);
}

#<?= $componentId ?> .faq-chip-stack {
    top: 84px;
    left: clamp(1rem, 8vw, 7rem);
    display: grid;
    gap: 0.42rem;
}

#<?= $componentId ?> .faq-chip-stack span {
    display: block;
    width: 74px;
    height: 14px;
    border-radius: 999px;
    background: rgba(255,255,255,0.84);
}

#<?= $componentId ?> .faq-chip-stack span:nth-child(2) {
    width: 48px;
    background: rgba(255, 200, 95, 0.88);
}

#<?= $componentId ?> .faq-chip-stack span:nth-child(3) {
    width: 28px;
    background: rgba(0, 205, 182, 0.82);
}

#<?= $componentId ?> .section-inner {
    width: min(1240px, calc(100% - 1.2rem));
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

#<?= $componentId ?> .faq-layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(280px, 0.38fr);
    gap: <?= (int)$itemGap ?>px;
    align-items: start;
}

#<?= $componentId ?> .faq-intro {
    display: grid;
    gap: 0.85rem;
    padding: 0.3rem 1.3rem 0 0;
    border-radius: 0;
    background: transparent;
    border: 0;
    box-shadow: none;
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
    order: 2;
    text-align: right;
    justify-items: end;
    position: relative;
}

#<?= $componentId ?> .faq-intro::before {
    content: '';
    position: absolute;
    top: 0.2rem;
    bottom: 0.2rem;
    right: 0;
    width: 3px;
    border-radius: 999px;
    background: linear-gradient(180deg, rgba(255, 176, 95, 0.92), rgba(31, 202, 182, 0.92));
}

#<?= $componentId ?> .faq-kicker {
    display: inline-flex;
    width: fit-content;
    padding: 0.34rem 0.74rem;
    border-radius: 999px;
    background: rgba(255,255,255,0.76);
    border: 1px solid rgba(255,255,255,0.86);
    color: #7a5746;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
}

#<?= $componentId ?> .faq-intro h2 {
    margin: 0;
    color: <?= htmlspecialchars($titleColor) ?>;
    font-family: 'Syne', 'Manrope', sans-serif;
    font-size: clamp(<?= (int)$titleFontSizeMobile ?>px, 5vw, <?= (int)$titleFontSize ?>px);
    font-weight: 800;
    line-height: 0.95;
    letter-spacing: -0.05em;
    text-wrap: balance;
}

#<?= $componentId ?> .faq-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    justify-content: flex-end;
}

#<?= $componentId ?> .faq-meta span {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0;
    border-radius: 0;
    background: transparent;
    color: #7e6151;
    font-size: 0.78rem;
    font-weight: 700;
}

#<?= $componentId ?> .faq-meta span + span::before {
    content: '';
    width: 6px;
    height: 6px;
    border-radius: 999px;
    background: rgba(255, 176, 95, 0.9);
}

#<?= $componentId ?> .faq-board {
    display: grid;
    gap: <?= (int)$itemGap ?>px;
    order: 1;
}

#<?= $componentId ?> .faq-panel {
    border-radius: <?= (int)$itemBorderRadius ?>px;
    border: 1px solid <?= htmlspecialchars($itemBorderColor) ?>;
    background: <?= htmlspecialchars($itemBgColor) ?>;
    overflow: hidden;
    box-shadow: 0 22px 40px rgba(195, 148, 106, 0.08);
}

#<?= $componentId ?> .faq-panel:nth-child(1) {
    background: linear-gradient(180deg, rgba(255,255,255,0.84), rgba(255, 240, 210, 0.84));
}

#<?= $componentId ?> .faq-panel:nth-child(2) {
    background: linear-gradient(180deg, rgba(255,255,255,0.84), rgba(216, 255, 244, 0.84));
}

#<?= $componentId ?> .faq-panel:nth-child(3) {
    background: linear-gradient(180deg, rgba(255,255,255,0.84), rgba(255, 229, 234, 0.84));
}

#<?= $componentId ?> .faq-panel:nth-child(4) {
    background: linear-gradient(180deg, rgba(255,255,255,0.84), rgba(228, 244, 255, 0.84));
}

#<?= $componentId ?> .faq-panel:nth-child(5) {
    background: linear-gradient(180deg, rgba(255,255,255,0.84), rgba(255, 245, 221, 0.84));
}

#<?= $componentId ?> .faq-panel summary {
    list-style: none;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 0.9rem;
    padding: <?= (int)$questionPadding ?>px;
    cursor: pointer;
}

#<?= $componentId ?> .faq-panel summary::-webkit-details-marker {
    display: none;
}

#<?= $componentId ?> .faq-panel summary:hover {
    background: <?= htmlspecialchars($questionHoverBg) ?>;
}

#<?= $componentId ?> .faq-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    border-radius: 18px;
    background: rgba(255,255,255,0.78);
    color: #4b2a1b;
    font-family: 'Syne', 'Manrope', sans-serif;
    font-size: 1rem;
    font-weight: 800;
}

#<?= $componentId ?> .faq-heading-wrap {
    display: grid;
    gap: 0.3rem;
}

#<?= $componentId ?> .faq-heading-wrap strong {
    color: <?= htmlspecialchars($questionColor) ?>;
    font-size: <?= (int)$questionFontSize ?>px;
    line-height: 1.45;
}

#<?= $componentId ?> .faq-heading-wrap small {
    color: #8b6957;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
}

#<?= $componentId ?> .faq-icon {
    width: <?= (int)$iconSize ?>px;
    height: <?= (int)$iconSize ?>px;
    position: relative;
    color: <?= htmlspecialchars($iconColor) ?>;
}

#<?= $componentId ?> .faq-icon::before,
#<?= $componentId ?> .faq-icon::after {
    content: '';
    position: absolute;
    left: 50%;
    top: 50%;
    width: <?= (int)$iconSize ?>px;
    height: 2px;
    background: currentColor;
    transform: translate(-50%, -50%);
    border-radius: 999px;
    transition: transform 0.25s ease, opacity 0.25s ease;
}

#<?= $componentId ?> .faq-icon::after {
    transform: translate(-50%, -50%) rotate(90deg);
}

#<?= $componentId ?> .faq-panel[open] .faq-icon::after {
    opacity: 0;
    transform: translate(-50%, -50%) rotate(90deg) scaleX(0.2);
}

#<?= $componentId ?> .faq-answer {
    padding: 0 <?= (int)$answerPadding ?>px <?= (int)$answerPadding ?>px calc(<?= (int)$answerPadding ?>px + 58px);
}

#<?= $componentId ?> .faq-answer p {
    margin: 0;
    color: <?= htmlspecialchars($answerColor) ?>;
    font-size: <?= (int)$answerFontSize ?>px;
    line-height: 1.85;
}

@media (max-width: 900px) {
    #<?= $componentId ?> .faq-layout {
        grid-template-columns: 1fr;
    }

    #<?= $componentId ?> .faq-intro,
    #<?= $componentId ?> .faq-board {
        order: initial;
    }

    #<?= $componentId ?> .faq-intro {
        text-align: left;
        justify-items: start;
        padding-left: 1.1rem;
        padding-right: 0;
    }

    #<?= $componentId ?> .faq-intro::before {
        left: 0;
        right: auto;
    }

    #<?= $componentId ?> .faq-meta {
        justify-content: flex-start;
    }
}

@media (max-width: 767px) {
    #<?= $componentId ?> {
        padding: <?= (int)$sectionPaddingMobile ?>px 0;
    }

    #<?= $componentId ?> .faq-orbit-left {
        left: -42px;
        top: 62px;
        width: 154px;
        height: 154px;
    }

    #<?= $componentId ?> .faq-orbit-right {
        right: -56px;
        bottom: 20px;
        width: 188px;
        height: 188px;
    }

    #<?= $componentId ?> .faq-chip-stack {
        top: 58px;
        left: auto;
        right: 1rem;
        transform: scale(0.84);
        transform-origin: top right;
    }

    #<?= $componentId ?> .faq-heading-wrap strong {
        font-size: <?= (int)$questionFontSizeMobile ?>px;
    }

    #<?= $componentId ?> .faq-intro {
        padding-top: 1.6rem;
    }

    #<?= $componentId ?> .faq-intro::before {
        top: 0;
        left: 0;
        right: auto;
        width: 72px;
        height: 3px;
        bottom: auto;
    }

    #<?= $componentId ?> .faq-answer {
        padding-left: <?= (int)$answerPadding ?>px;
    }
}
</style>
