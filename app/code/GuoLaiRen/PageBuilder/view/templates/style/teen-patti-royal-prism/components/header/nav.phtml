<?php
/**
 * Teen Patti Royal Prism Header Navigation Component
 *
 * @component_start
 * code: header-nav
 * name: Prism Navigation Header
 * name_en: Prism Navigation Header
 * description: Bright sticky navigation with mobile drawer and luminous motion.
 * category: header
 * region: header
 * thumbnail: asset/img/prism-logo-icon.svg
 * @component_end
 *
 * @fields_start
 *
 * group:logo => Logo设置
 * logo.display => 显示Logo:select:yes|yes,no
 * logo.text => Logo文本:text:Teen Patti Royal Prism
 * logo.subline => Logo副标题:text:Bright mobile card lounge
 * logo.url => Logo地址:textarea:|默认使用本地prism-logo-icon.svg
 * logo.width => Logo宽度:responsive:70/70|px
 * logo.height => Logo高度:responsive:|px[可选，留空跟随宽度]
 *
 * group:navigation => 导航设置
 * navigation.display => 显示导航:select:yes|yes,no
 * navigation.bg_color => 导航背景色:color:rgba(255,250,240,0.84)
 * navigation.bg_color_scrolled => 滚动后背景色:color:rgba(255,246,233,0.96)
 * navigation.text_color => 导航文字颜色:color:#392016
 * navigation.link_color => 链接颜色:color:#5c4033
 * navigation.link_hover_color => 链接悬停颜色:color:#ff7a45
 * navigation.link_font_size => 链接字号:number:14|px
 * navigation.link_font_weight => 链接字重:number:600
 * navigation.link_gap => 链接间距:number:28|px
 * navigation.padding_x => 水平内边距:number:20|px
 * navigation.padding_y => 垂直内边距:number:12|px
 * navigation.min_height => 最小高度:number:76|px
 * navigation.box_shadow => 阴影:text:0 24px 48px rgba(226,159,118,0.16)
 * navigation.backdrop_blur => 背景模糊:number:14|px
 * navigation.items => 导航项:textarea:Home=>/\r\nAbout=>/about\r\nBlog=>/blog\r\nFAQs=>/faqs|每行一个，格式：名称=>链接
 * navigation.use_custom_links => 使用自定义导航链接:select:yes|yes,no
 * navigation.mobile_menu_label => 移动端菜单标题:text:Menu
 * group:navigation_labels => 导航菜单显示名称（有对应页面时显示，留空使用页面标题）
 * navigation.label_home_page => 首页菜单名:text:
 * navigation.label_about_page => 关于我们菜单名:text:
 * navigation.label_blog_list => 博客菜单名:text:
 * navigation.label_contact_page => 联系我们菜单名:text:
 * navigation.label_terms_of_service => 条款菜单名:text:
 * navigation.label_privacy_policy => 隐私政策菜单名:text:
 *
 * group:download_button => 下载按钮
 * download_button.display => 显示下载按钮:select:yes|yes,no
 * download_button.text => 按钮文本:text:Download App
 * download_button.url => 按钮链接:text:#download
 * download_button.bg_color => 按钮背景色:color:#ff7a45
 * download_button.bg_hover_color => 按钮悬停背景色:color:#df5a2a
 * download_button.text_color => 按钮文字颜色:color:#2f160d
 * download_button.font_size => 按钮字号:number:13|px
 * download_button.font_weight => 按钮字重:number:700
 * download_button.border_radius => 按钮圆角:number:999|px
 * download_button.padding_x => 按钮水平内边距:number:20|px
 * download_button.padding_y => 按钮垂直内边距:number:11|px
 *
 * @fields_end
 */

$page = $this->getData('page');
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$config = array_merge($styleSettings, $componentConfig);

$standaloneShell = !($this->getData('__tpr_shell_managed') || $this->getData('__pagebuilder_shell_managed'));

$getConfig = function ($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

$normalizeToggle = static function ($value, $default = 'yes') {
    if ($value === '' || $value === null) {
        return $default;
    }
    if ($value === 0 || $value === '0') {
        return 'yes';
    }
    if ($value === 1 || $value === '1') {
        return 'no';
    }
    return ($value === 'no' || $value === false || $value === 'false') ? 'no' : 'yes';
};

$parseResponsive = function ($value) {
    if (empty($value)) {
        return ['mobile' => '', 'desktop' => '', 'unit' => 'px'];
    }
    $value = trim((string)$value);
    $unit = 'px';
    if (preg_match('/([a-z%]+)$/i', $value, $matches)) {
        $unit = $matches[1];
        $value = preg_replace('/[a-z%]+$/i', '', $value);
    }
    $parts = explode('/', trim($value));
    return [
        'mobile' => trim((string)($parts[0] ?? '')),
        'desktop' => trim((string)($parts[1] ?? ($parts[0] ?? ''))),
        'unit' => $unit,
    ];
};

$genResponsiveCSS = function ($parsed) {
    $mobile = (float)$parsed['mobile'];
    $desktop = (float)$parsed['desktop'];
    $unit = $parsed['unit'];
    if ($mobile === $desktop) {
        return $mobile . $unit;
    }
    $diff = $desktop - $mobile;
    return "clamp({$mobile}{$unit}, calc({$mobile}{$unit} + {$diff} * ((100vw - 375px) / 905)), {$desktop}{$unit})";
};

$sanitizeTitle = static function ($value) {
    $value = trim((string)$value);
    if ($value === '') {
        return $value;
    }
    $value = preg_replace('/^样式模板预览\s*[-–—]*\s*/u', '', $value);
    $value = str_replace('样式模板预览', '', $value);
    return trim($value);
};

$templateCode = $page ? $page->getData('style') : 'teen-patti-royal-prism';
if (empty($templateCode)) {
    $request = $this->getRequest();
    if ($request) {
        $templateCode = $request->getParam('style_code') ?: '';
    }
}
if (empty($templateCode)) {
    $templateCode = 'teen-patti-royal-prism';
}
$this->assign('template_code', $templateCode);
$this->assign('style', $styleSettings);
$this->assign('style_settings', $styleSettings);
?>
<div style="display:none;">
    <w:template>GuoLaiRen_PageBuilder::base/colors.phtml</w:template>
</div>
<?php
if (!isset($colors) || !is_array($colors)) {
    $colors = [];
}

$brandName = trim((string)$getConfig('logo.text', $getConfig('header.logo.text', $getConfig('app_info.app_name', ''))));
if ($brandName === '') {
    $brandName = 'Teen Patti Royal Prism';
}
$brandName = $sanitizeTitle($brandName);
if ($brandName === '') {
    $brandName = 'Teen Patti Royal Prism';
}
$brandSubline = trim((string)$getConfig('logo.subline', $getConfig('header.logo.subline', 'Bright mobile card lounge')));
$mobileMenuLabel = trim((string)$getConfig('navigation.mobile_menu_label', $getConfig('header.navigation.mobile_menu_label', 'Menu')));

$metaTitleRaw = $page ? ($page->getData('meta_title') ?: $page->getData('title')) : $brandName;
$metaTitle = $sanitizeTitle($metaTitleRaw);
if ($metaTitle === '') {
    $metaTitle = $brandName;
}
$metaDescription = $page ? ($page->getData('meta_description') ?: '') : '';
$metaKeywords = $page ? ($page->getData('meta_keywords') ?: '') : '';
$pageIcon = $page ? ($page->getData('icon') ?: '') : '';

$logoDisplay = $normalizeToggle($getConfig('logo.display', $getConfig('header.logo.display', 'yes'))) !== 'no';
$defaultLogoUrl = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/teen-patti-royal-prism/asset/img/prism-logo-icon.svg');
$logoUrl = $getConfig('logo.url', $getConfig('header.logo.url', '')) ?: ($page && $page->getData('logo') ? $page->getData('logo') : $defaultLogoUrl);
$logoWidth = $genResponsiveCSS($parseResponsive($getConfig('logo.width', $getConfig('header.logo.width', '70/70')) . 'px'));
$logoHeightConfig = $getConfig('logo.height', $getConfig('header.logo.height', ''));
$logoHeight = empty($logoHeightConfig) ? $logoWidth : $genResponsiveCSS($parseResponsive($logoHeightConfig . 'px'));

$navDisplay = $normalizeToggle($getConfig('navigation.display', $getConfig('header.navigation.display', 'yes'))) !== 'no';
$navBgColor = $getConfig('navigation.bg_color', $getConfig('header.navigation.bg_color', $colors['nav_bg'] ?? 'rgba(255,249,240,0.82)'));
$navBgColorScrolled = $getConfig('navigation.bg_color_scrolled', $getConfig('header.navigation.bg_color_scrolled', $colors['nav_bg_scrolled'] ?? 'rgba(255,244,232,0.95)'));
$navTextColor = $getConfig('navigation.text_color', $getConfig('header.navigation.text_color', $colors['nav_text'] ?? '#2f1a12'));
$navLinkColor = $getConfig('navigation.link_color', $getConfig('header.navigation.link_color', $colors['nav_link'] ?? '#5c4033'));
$navLinkHoverColor = $getConfig('navigation.link_hover_color', $getConfig('header.navigation.link_hover_color', $colors['nav_link_hover'] ?? '#ff7a45'));
$navLinkFontSize = (int)$getConfig('navigation.link_font_size', '14');
$navLinkFontWeight = (int)$getConfig('navigation.link_font_weight', '600');
$navLinkGap = (int)$getConfig('navigation.link_gap', '28');
$navPaddingX = (int)$getConfig('navigation.padding_x', '20');
$navPaddingY = (int)$getConfig('navigation.padding_y', '12');
$navMinHeight = (int)$getConfig('navigation.min_height', '76');
$navBoxShadow = $getConfig('navigation.box_shadow', '0 24px 48px rgba(226,159,118,0.16)');
$navBackdropBlur = (int)$getConfig('navigation.backdrop_blur', '14');
$useCustomLinksRaw = trim((string)$getConfig('navigation.use_custom_links', $getConfig('header.navigation.use_custom_links', '')));
$useCustomLinks = $useCustomLinksRaw !== ''
    ? $normalizeToggle($useCustomLinksRaw, 'yes') === 'yes'
    : $normalizeToggle($getConfig('navigation.use_subpages', 'no'), 'no') !== 'yes';

$downloadBtnDisplay = $normalizeToggle($getConfig('download_button.display', $getConfig('header.download_button.display', 'yes'))) !== 'no';
$downloadBtnText = $getConfig('download_button.text', $getConfig('header.download_button.text', 'Download App'));
$downloadBtnUrl = $getConfig('download_button.url', $getConfig('header.download_button.url', '#download'));
$downloadBtnBgColor = $getConfig('download_button.bg_color', $colors['accent_primary'] ?? '#ff7a45');
$downloadBtnBgHoverColor = $getConfig('download_button.bg_hover_color', $colors['accent_primary_dark'] ?? '#df5a2a');
$downloadBtnTextColor = $getConfig('download_button.text_color', $colors['button_primary_text'] ?? '#2f160d');
$downloadBtnFontSize = (int)$getConfig('download_button.font_size', '13');
$downloadBtnFontWeight = (int)$getConfig('download_button.font_weight', '700');
$downloadBtnRadius = (int)$getConfig('download_button.border_radius', '999');
$downloadBtnPaddingX = (int)$getConfig('download_button.padding_x', '20');
$downloadBtnPaddingY = (int)$getConfig('download_button.padding_y', '11');

$navItems = [];
if (!$useCustomLinks && $page) {
    $navigationPages = method_exists($page, 'getHeaderNavigationPages')
        ? $page->getHeaderNavigationPages(10)
        : $page->getNavigationPages([], 10);
    foreach ($navigationPages as $navPage) {
        $type = (string)($navPage['type'] ?? '');
        $configuredLabel = trim((string)$getConfig('navigation.label_' . $type, $getConfig('header.navigation.label_' . $type, '')));
        $navItems[] = [
            'text' => $configuredLabel !== '' ? $configuredLabel : ($navPage['title'] ?? ''),
            'href' => $navPage['url'] ?? '#',
        ];
    }
}
if (empty($navItems)) {
    $navItemsConfig = $getConfig('navigation.items', "Home=>/\nAbout=>/about\nBlog=>/blog\nFAQs=>/faqs");
    if (strpos($navItemsConfig, '\\n') !== false && strpos($navItemsConfig, "\n") === false) {
        $navItemsConfig = str_replace('\\n', "\n", $navItemsConfig);
    }
    foreach (preg_split('/\r?\n/', $navItemsConfig) as $line) {
        $line = trim((string)$line);
        if ($line === '' || strpos($line, '=>') === false) {
            continue;
        }
        $parts = explode('=>', $line, 2);
        $text = trim((string)($parts[0] ?? ''));
        $href = trim((string)($parts[1] ?? ''));
        if ($text === '') {
            continue;
        }
        if ($href === '') {
            $href = '/' . strtolower(trim((string)preg_replace('/[^a-zA-Z0-9]+/', '-', $text), '-'));
        }
        $navItems[] = ['text' => $text, 'href' => $href];
    }
}
if (empty($navItems)) {
    $navItems = [
        ['text' => 'Home', 'href' => '/'],
        ['text' => 'About', 'href' => '/about'],
        ['text' => 'Blog', 'href' => '/blog'],
        ['text' => 'FAQs', 'href' => '/faqs'],
    ];
}

$mobilePrimaryItem = $navItems[0] ?? null;
$mobileSecondaryItems = array_slice($navItems, 1);

$componentId = 'prism-header-nav-' . uniqid();
?>
<?php if ($standaloneShell): ?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($metaTitle) ?></title>
    <?php if (!empty($pageIcon)): ?>
        <link rel="icon" type="image/x-icon" href="<?= htmlspecialchars($pageIcon) ?>">
        <link rel="shortcut icon" type="image/x-icon" href="<?= htmlspecialchars($pageIcon) ?>">
    <?php else: ?>
        <link rel="icon" type="image/svg+xml" href="<?= htmlspecialchars($defaultLogoUrl) ?>">
    <?php endif; ?>
    <?php if ($metaDescription): ?>
        <meta name="description" content="<?= htmlspecialchars($metaDescription) ?>">
    <?php endif; ?>
    <?php if ($metaKeywords): ?>
        <meta name="keywords" content="<?= htmlspecialchars($metaKeywords) ?>">
    <?php endif; ?>
    <w:template>GuoLaiRen_PageBuilder::base/tracking.phtml</w:template>
    <script>
    window.tailwind = window.tailwind || {};
    window.tailwind.config = {
        theme: {
            extend: {
                colors: {
                    'prism-cream': '<?= htmlspecialchars($colors['body_bg'] ?? '#fff9ef') ?>',
                    'prism-peach': '<?= htmlspecialchars($colors['section_bg_3'] ?? '#ffe8d1') ?>',
                    'prism-mint': '<?= htmlspecialchars($colors['section_bg_2'] ?? '#e7fbf6') ?>',
                    'prism-coral': '<?= htmlspecialchars($colors['accent_primary'] ?? '#ff7a45') ?>',
                    'prism-gold': '<?= htmlspecialchars($colors['accent_secondary'] ?? '#ffc95a') ?>',
                    'prism-ink': '<?= htmlspecialchars($colors['text_primary'] ?? '#2f1a12') ?>'
                }
            }
        }
    };
    </script>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://unpkg.com/animate.css@4.1.1/animate.min.css" data-prism-animate="1">
    <script id="prism-wow-script" src="https://cdnjs.cloudflare.com/ajax/libs/wow/1.1.2/wow.min.js?v=1"></script>
<?php endif; ?>
<style>
@import url('https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&family=Syne:wght@500;700;800&display=swap');

:root {
    --prism-body: <?= htmlspecialchars($colors['body_bg'] ?? '#fff9ef') ?>;
    --prism-hero: <?= htmlspecialchars($colors['hero_bg'] ?? '#fff7eb') ?>;
    --prism-mint: <?= htmlspecialchars($colors['section_bg_2'] ?? '#e7fbf6') ?>;
    --prism-peach: <?= htmlspecialchars($colors['section_bg_3'] ?? '#ffe8d1') ?>;
    --prism-sun: <?= htmlspecialchars($colors['section_bg_4'] ?? '#fff1d8') ?>;
    --prism-surface: <?= htmlspecialchars($colors['card_bg'] ?? 'rgba(255,255,255,0.72)') ?>;
    --prism-border: <?= htmlspecialchars($colors['border_light'] ?? 'rgba(126,91,67,0.14)') ?>;
    --prism-text: <?= htmlspecialchars($colors['text_primary'] ?? '#2f1a12') ?>;
    --prism-muted: <?= htmlspecialchars($colors['text_muted'] ?? '#9b7967') ?>;
    --prism-coral: <?= htmlspecialchars($colors['accent_primary'] ?? '#ff7a45') ?>;
    --prism-coral-strong: <?= htmlspecialchars($colors['accent_primary_dark'] ?? '#df5a2a') ?>;
    --prism-gold: <?= htmlspecialchars($colors['accent_secondary'] ?? '#ffc95a') ?>;
    --prism-mint-accent: <?= htmlspecialchars($colors['accent_primary_vibrant'] ?? '#1fcab6') ?>;
    --prism-shell-width: min(1240px, calc(100% - clamp(1rem, 4vw, 2rem)));
}

* {
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

[hidden] {
    display: none !important;
}

body {
    margin: 0;
    min-height: 100vh;
    overflow-x: hidden;
    font-family: 'Manrope', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    background:
        radial-gradient(920px 420px at 0% 4%, rgba(255, 210, 138, 0.44), transparent 60%),
        radial-gradient(820px 420px at 100% 10%, rgba(31, 202, 182, 0.22), transparent 60%),
        linear-gradient(180deg, #fffdf9 0%, var(--prism-body) 40%, #fff6ec 100%);
    color: var(--prism-text);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}

body::before {
    content: '';
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: -1;
    background:
        linear-gradient(90deg, rgba(255,255,255,0.32) 1px, transparent 1px),
        linear-gradient(180deg, rgba(255,255,255,0.26) 1px, transparent 1px),
        radial-gradient(circle at 50% 20%, rgba(255,255,255,0.38), transparent 46%);
    background-size: 42px 42px, 42px 42px, 100% 100%;
    opacity: 0.3;
}

body.prism-mobile-lock {
    overflow: hidden;
}

img {
    display: block;
    max-width: 100%;
}

a {
    color: inherit;
}

button,
input,
textarea {
    font: inherit;
}

.prism-skip-link {
    position: absolute;
    left: 12px;
    top: -48px;
    z-index: 2000;
    padding: 0.7rem 1rem;
    border-radius: 999px;
    background: #ffffff;
    color: var(--prism-text);
    text-decoration: none;
    box-shadow: 0 14px 30px rgba(0, 0, 0, 0.12);
}

.prism-skip-link:focus {
    top: 12px;
}

.prism-shell {
    width: var(--prism-shell-width);
    margin: 0 auto;
}

.prism-section-shell {
    width: min(1240px, calc(100% - 1.2rem));
    margin: 0 auto;
}

.prism-glass {
    backdrop-filter: blur(18px);
    -webkit-backdrop-filter: blur(18px);
    background: rgba(255, 255, 255, 0.64);
    border: 1px solid rgba(255, 255, 255, 0.58);
    box-shadow: 0 22px 52px rgba(224, 160, 115, 0.16);
}

.wow {
    visibility: visible;
}

html.wow-ready .wow {
    visibility: hidden;
}

.prism-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.4rem 0.8rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.72);
    border: 1px solid rgba(255, 255, 255, 0.8);
    box-shadow: 0 18px 34px rgba(222, 150, 104, 0.14);
}

#<?= $componentId ?> {
    position: fixed;
    top: 0;
    left: 0;
    z-index: 1200;
    width: 100%;
    padding: 0.85rem 0.75rem 0;
    color: <?= htmlspecialchars($navTextColor) ?>;
    pointer-events: none;
}

#<?= $componentId ?> .prism-nav-shell,
#<?= $componentId ?> .prism-mobile-backdrop,
#<?= $componentId ?> .prism-mobile-panel {
    pointer-events: auto;
}

#<?= $componentId ?> .prism-nav-shell {
    position: relative;
    width: var(--prism-shell-width);
    margin: 0 auto;
    min-height: <?= $navMinHeight ?>px;
    padding: <?= $navPaddingY ?>px <?= $navPaddingX ?>px;
    border-radius: 28px;
    background: transparent;
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
    box-shadow: none;
    border: 1px solid transparent;
    display: flex;
    align-items: center;
    gap: 1rem;
    overflow: hidden;
    transition: background 0.28s ease, border-color 0.28s ease, box-shadow 0.28s ease, backdrop-filter 0.28s ease, -webkit-backdrop-filter 0.28s ease, transform 0.28s ease;
}

#<?= $componentId ?>.is-scrolled .prism-nav-shell,
#<?= $componentId ?>.is-mobile-open .prism-nav-shell {
    background: <?= htmlspecialchars($navBgColorScrolled) ?>;
    backdrop-filter: blur(<?= $navBackdropBlur ?>px);
    -webkit-backdrop-filter: blur(<?= $navBackdropBlur ?>px);
    box-shadow: <?= htmlspecialchars($navBoxShadow) ?>;
    border-color: rgba(255, 255, 255, 0.72);
}

#<?= $componentId ?> .prism-nav-shell::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
        radial-gradient(280px 90px at 14% 0%, rgba(255, 210, 138, 0.46), transparent 72%),
        radial-gradient(260px 110px at 92% 18%, rgba(31, 202, 182, 0.24), transparent 72%),
        linear-gradient(120deg, rgba(255,255,255,0.64), rgba(255,255,255,0));
    opacity: 0;
    transition: opacity 0.28s ease;
}

#<?= $componentId ?> .prism-nav-shell::after {
    content: '';
    position: absolute;
    left: -30%;
    top: 0;
    width: 38%;
    height: 100%;
    background: linear-gradient(115deg, transparent 0%, rgba(255,255,255,0.58) 48%, transparent 100%);
    transform: skewX(-22deg);
    animation: prism-nav-sheen 9s ease-in-out infinite;
    opacity: 0;
    transition: opacity 0.28s ease;
}

#<?= $componentId ?>.is-scrolled .prism-nav-shell::before,
#<?= $componentId ?>.is-mobile-open .prism-nav-shell::before {
    opacity: 1;
}

#<?= $componentId ?>.is-scrolled .prism-nav-shell::after,
#<?= $componentId ?>.is-mobile-open .prism-nav-shell::after {
    opacity: 0.66;
}

#<?= $componentId ?> .prism-brand {
    position: relative;
    z-index: 1;
    display: inline-flex;
    align-items: center;
    gap: 0.85rem;
    min-width: 0;
    text-decoration: none;
}

#<?= $componentId ?> .prism-brand img {
    width: <?= $logoWidth ?>;
    height: <?= $logoHeight ?>;
    object-fit: contain;
    flex-shrink: 0;
}

#<?= $componentId ?> .prism-brand-copy {
    min-width: 0;
    display: grid;
    gap: 0.08rem;
}

#<?= $componentId ?> .prism-brand-copy strong {
    font-family: 'Syne', 'Manrope', sans-serif;
    font-size: clamp(0.95rem, 2vw, 1.1rem);
    font-weight: 800;
    line-height: 1;
    letter-spacing: -0.02em;
    color: <?= htmlspecialchars($navTextColor) ?>;
}

#<?= $componentId ?> .prism-brand-copy span {
    font-size: 0.73rem;
    color: <?= htmlspecialchars($colors['text_muted'] ?? '#9b7967') ?>;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 700;
}

#<?= $componentId ?> .prism-nav-links {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: <?= $navLinkGap ?>px;
    flex: 1 1 auto;
    flex-wrap: wrap;
}

#<?= $componentId ?> .prism-nav-links a,
#<?= $componentId ?> .prism-mobile-links a {
    position: relative;
    text-decoration: none;
    color: <?= htmlspecialchars($navLinkColor) ?>;
    font-size: <?= $navLinkFontSize ?>px;
    font-weight: <?= $navLinkFontWeight ?>;
    letter-spacing: 0.02em;
    transition: color 0.24s ease, transform 0.24s ease;
}

#<?= $componentId ?> .prism-nav-links a::after,
#<?= $componentId ?> .prism-mobile-links a::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -6px;
    width: 100%;
    height: 2px;
    border-radius: 999px;
    background: linear-gradient(90deg, <?= htmlspecialchars($navLinkHoverColor) ?> 0%, <?= htmlspecialchars($colors['accent_primary_vibrant'] ?? '#1fcab6') ?> 100%);
    transform: scaleX(0);
    transform-origin: left center;
    transition: transform 0.24s ease;
}

#<?= $componentId ?> .prism-nav-links a:hover,
#<?= $componentId ?> .prism-mobile-links a:hover {
    color: <?= htmlspecialchars($navLinkHoverColor) ?>;
    transform: translateY(-1px);
}

#<?= $componentId ?> .prism-nav-links a:hover::after,
#<?= $componentId ?> .prism-mobile-links a:hover::after {
    transform: scaleX(1);
}

#<?= $componentId ?> .prism-nav-actions {
    position: relative;
    z-index: 1;
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    margin-left: auto;
}

#<?= $componentId ?> .prism-cta,
#<?= $componentId ?> .prism-mobile-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: <?= $downloadBtnPaddingY ?>px <?= $downloadBtnPaddingX ?>px;
    border-radius: <?= $downloadBtnRadius ?>px;
    background: linear-gradient(110deg, <?= htmlspecialchars($downloadBtnBgColor) ?> 0%, <?= htmlspecialchars($colors['accent_secondary'] ?? '#ffc95a') ?> 100%);
    color: <?= htmlspecialchars($downloadBtnTextColor) ?>;
    text-decoration: none;
    font-size: <?= $downloadBtnFontSize ?>px;
    font-weight: <?= $downloadBtnFontWeight ?>;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    box-shadow: 0 18px 32px rgba(255, 122, 69, 0.22);
    transition: transform 0.25s ease, box-shadow 0.25s ease, filter 0.25s ease;
}

#<?= $componentId ?> .prism-cta:hover,
#<?= $componentId ?> .prism-mobile-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 20px 36px rgba(255, 122, 69, 0.32);
    filter: brightness(1.03);
}

#<?= $componentId ?> .prism-toggle {
    width: 42px;
    height: 42px;
    display: none;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 4px;
    border: 1px solid rgba(132, 98, 76, 0.16);
    border-radius: 16px;
    background: rgba(255, 255, 255, 0.64);
    color: <?= htmlspecialchars($navTextColor) ?>;
    cursor: pointer;
    box-shadow: 0 12px 26px rgba(201, 141, 104, 0.14);
}

#<?= $componentId ?> .prism-toggle span {
    width: 18px;
    height: 2px;
    border-radius: 999px;
    background: currentColor;
    transition: transform 0.24s ease, opacity 0.24s ease;
}

#<?= $componentId ?> .prism-toggle.is-open span:nth-child(1) {
    transform: translateY(6px) rotate(45deg);
}

#<?= $componentId ?> .prism-toggle.is-open span:nth-child(2) {
    opacity: 0;
}

#<?= $componentId ?> .prism-toggle.is-open span:nth-child(3) {
    transform: translateY(-6px) rotate(-45deg);
}

#<?= $componentId ?> .prism-mobile-backdrop {
    position: fixed;
    inset: 0;
    z-index: 1180;
    border: 0;
    background:
        radial-gradient(620px 240px at 12% 8%, rgba(255, 210, 138, 0.34), transparent 64%),
        radial-gradient(560px 280px at 92% 12%, rgba(31, 202, 182, 0.24), transparent 64%),
        rgba(74, 44, 28, 0.26);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    opacity: 0;
    cursor: pointer;
    transition: opacity 0.24s ease;
}

#<?= $componentId ?> .prism-mobile-panel {
    position: fixed;
    left: 50%;
    bottom: 0.7rem;
    z-index: 1190;
    width: min(560px, calc(100% - 1rem));
    max-height: calc(100dvh - 1rem);
    transform: translate(-50%, 110%);
    opacity: 0;
    pointer-events: none;
    border-radius: 28px;
    background: linear-gradient(180deg, rgba(255,252,247,0.94) 0%, rgba(255,241,220,0.98) 100%);
    border: 1px solid rgba(255,255,255,0.85);
    box-shadow: 0 26px 54px rgba(189, 128, 91, 0.22);
    padding: 1rem;
    overflow: hidden;
    isolation: isolate;
    overflow-y: auto;
    overscroll-behavior: contain;
    scrollbar-width: thin;
    transition: transform 0.28s ease, opacity 0.28s ease;
}

#<?= $componentId ?> .prism-mobile-panel::-webkit-scrollbar {
    width: 6px;
}

#<?= $componentId ?> .prism-mobile-panel::-webkit-scrollbar-thumb {
    border-radius: 999px;
    background: rgba(126, 91, 67, 0.22);
}

#<?= $componentId ?> .prism-mobile-panel::before,
#<?= $componentId ?> .prism-mobile-panel::after {
    content: '';
    position: absolute;
    pointer-events: none;
    z-index: 0;
}

#<?= $componentId ?> .prism-mobile-panel::before {
    inset: 0;
    background:
        radial-gradient(280px 160px at 8% 0%, rgba(255, 210, 138, 0.34), transparent 70%),
        radial-gradient(260px 180px at 100% 12%, rgba(31, 202, 182, 0.18), transparent 70%);
}

#<?= $componentId ?> .prism-mobile-panel::after {
    top: 12px;
    right: 18px;
    width: 92px;
    height: 92px;
    border-radius: 24px;
    transform: rotate(16deg);
    background: linear-gradient(135deg, rgba(255, 201, 90, 0.24), rgba(255, 122, 69, 0.06));
    opacity: 0.65;
}

#<?= $componentId ?>.is-mobile-open .prism-mobile-backdrop {
    opacity: 1;
}

#<?= $componentId ?>.is-mobile-open .prism-mobile-panel {
    transform: translate(-50%, 0);
    opacity: 1;
    pointer-events: auto;
}

#<?= $componentId ?> .prism-mobile-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.8rem;
    position: relative;
    z-index: 1;
}

#<?= $componentId ?> .prism-mobile-brandline {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    min-width: 0;
}

#<?= $componentId ?> .prism-mobile-brandline img {
    width: 52px;
    height: 52px;
    object-fit: contain;
    flex-shrink: 0;
    padding: 0.7rem;
    border-radius: 18px;
    background: linear-gradient(145deg, rgba(255,255,255,0.94), rgba(255, 239, 200, 0.88));
    box-shadow: 0 16px 28px rgba(201, 141, 104, 0.14);
}

#<?= $componentId ?> .prism-mobile-head-copy {
    min-width: 0;
    display: grid;
    gap: 0.16rem;
}

#<?= $componentId ?> .prism-mobile-head p {
    margin: 0;
    font-size: 0.74rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--prism-muted);
}

#<?= $componentId ?> .prism-mobile-head strong {
    display: block;
    font-family: 'Syne', 'Manrope', sans-serif;
    font-size: clamp(1.08rem, 4vw, 1.34rem);
    line-height: 0.98;
    letter-spacing: -0.04em;
    color: var(--prism-text);
}

#<?= $componentId ?> .prism-mobile-close {
    width: 40px;
    height: 40px;
    border: 0;
    border-radius: 16px;
    background: rgba(255, 255, 255, 0.88);
    color: var(--prism-text);
    box-shadow: 0 12px 20px rgba(201, 141, 104, 0.12);
    cursor: pointer;
    font-size: 1.15rem;
    line-height: 1;
}

#<?= $componentId ?> .prism-mobile-track {
    display: flex;
    gap: 0.4rem;
    margin-bottom: 0.8rem;
    position: relative;
    z-index: 1;
    width: min(220px, 68%);
}

#<?= $componentId ?> .prism-mobile-track span {
    height: 3px;
    border-radius: 999px;
    background: linear-gradient(90deg, <?= htmlspecialchars($colors['accent_primary_vibrant'] ?? '#1fcab6') ?> 0%, <?= htmlspecialchars($colors['accent_primary'] ?? '#ff7a45') ?> 100%);
}

#<?= $componentId ?> .prism-mobile-track span:first-child {
    width: 40%;
}

#<?= $componentId ?> .prism-mobile-track span:last-child {
    width: 60%;
    opacity: 0.7;
}

#<?= $componentId ?> .prism-mobile-links {
    display: grid;
    gap: 0.72rem;
    position: relative;
    z-index: 1;
    padding-bottom: 0.2rem;
}

#<?= $componentId ?> .prism-mobile-links a {
    position: relative;
    overflow: hidden;
}

#<?= $componentId ?> .prism-mobile-feature,
#<?= $componentId ?> .prism-mobile-card {
    display: grid;
    align-content: space-between;
    gap: 0.42rem;
    padding: 0.82rem 0.9rem;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.78);
    box-shadow: 0 18px 32px rgba(201, 141, 104, 0.09);
}

#<?= $componentId ?> .prism-mobile-feature::after,
#<?= $componentId ?> .prism-mobile-card::after {
    display: none;
}

#<?= $componentId ?> .prism-mobile-feature {
    min-height: 92px;
    background: linear-gradient(145deg, rgba(255,255,255,0.86), rgba(255, 235, 197, 0.92));
}

#<?= $componentId ?> .prism-mobile-feature::before,
#<?= $componentId ?> .prism-mobile-card::before {
    content: '';
    position: absolute;
    right: -30px;
    bottom: -32px;
    width: 108px;
    height: 108px;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(255,255,255,0.42), rgba(255,255,255,0.02) 72%, transparent 80%);
    pointer-events: none;
}

#<?= $componentId ?> .prism-mobile-feature-top {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 0.75rem;
}

#<?= $componentId ?> .prism-mobile-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(138px, 1fr));
    gap: 0.72rem;
}

#<?= $componentId ?> .prism-mobile-card {
    min-height: 32px;
}

#<?= $componentId ?> .prism-mobile-card-1 {
    background: linear-gradient(145deg, rgba(255,255,255,0.84), rgba(220, 255, 244, 0.92));
}

#<?= $componentId ?> .prism-mobile-card-2 {
    background: linear-gradient(145deg, rgba(255,255,255,0.84), rgba(255, 229, 234, 0.92));
}

#<?= $componentId ?> .prism-mobile-card-3 {
    background: linear-gradient(145deg, rgba(255,255,255,0.84), rgba(230, 243, 255, 0.92));
}

#<?= $componentId ?> .prism-mobile-feature strong,
#<?= $componentId ?> .prism-mobile-card strong {
    display: block;
    margin: 0;
    font-family: 'Syne', 'Manrope', sans-serif;
    font-size: 1.02rem;
    line-height: 1.08;
    letter-spacing: -0.04em;
    color: var(--prism-text);
}

#<?= $componentId ?> .prism-mobile-card strong {
    font-size: 0.96rem;
}

#<?= $componentId ?> .prism-mobile-arrow {
    position: relative;
    width: 34px;
    height: 34px;
    border-radius: 999px;
    background: rgba(255,255,255,0.72);
    box-shadow: inset 0 0 0 1px rgba(255,255,255,0.74);
}

#<?= $componentId ?> .prism-mobile-arrow::before,
#<?= $componentId ?> .prism-mobile-arrow::after {
    content: '';
    position: absolute;
    background: currentColor;
    border-radius: 999px;
    color: <?= htmlspecialchars($navLinkHoverColor) ?>;
}

#<?= $componentId ?> .prism-mobile-arrow::before {
    top: 11px;
    left: 11px;
    width: 12px;
    height: 2px;
    transform: rotate(45deg);
    transform-origin: right center;
}

#<?= $componentId ?> .prism-mobile-arrow::after {
    top: 11px;
    left: 16px;
    width: 2px;
    height: 12px;
    transform: rotate(45deg);
    transform-origin: center bottom;
}

#<?= $componentId ?> .prism-mobile-feature:hover,
#<?= $componentId ?> .prism-mobile-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 24px 38px rgba(201, 141, 104, 0.15);
}

#<?= $componentId ?> .prism-mobile-cta {
    width: 100%;
    margin-top: 1rem;
}

#<?= $componentId ?> .prism-nav-spark,
#<?= $componentId ?> .prism-nav-orb {
    position: absolute;
    pointer-events: none;
}

#<?= $componentId ?> .prism-nav-orb {
    top: 8px;
    right: 110px;
    width: 82px;
    height: 82px;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(31,202,182,0.22) 0%, rgba(31,202,182,0.02) 70%, transparent 78%);
    opacity: 0;
    transition: opacity 0.28s ease;
}

#<?= $componentId ?> .prism-nav-spark {
    bottom: 10px;
    right: 42px;
    width: 42px;
    height: 42px;
    border-radius: 16px;
    transform: rotate(22deg);
    background: linear-gradient(135deg, rgba(255, 201, 90, 0.44), rgba(255, 122, 69, 0.2));
    opacity: 0;
    transition: opacity 0.28s ease;
}

#<?= $componentId ?>.is-scrolled .prism-nav-orb,
#<?= $componentId ?>.is-scrolled .prism-nav-spark,
#<?= $componentId ?>.is-mobile-open .prism-nav-orb,
#<?= $componentId ?>.is-mobile-open .prism-nav-spark {
    opacity: 1;
}

@keyframes prism-nav-sheen {
    0%, 18% { transform: translateX(-170%) skewX(-22deg); }
    55%, 100% { transform: translateX(420%) skewX(-22deg); }
}

@media (max-width: 991px) {
    #<?= $componentId ?> .prism-nav-links,
    #<?= $componentId ?> .prism-cta {
        display: none;
    }

    #<?= $componentId ?> .prism-toggle {
        display: inline-flex;
    }

    #<?= $componentId ?> .prism-brand-copy span {
        display: none;
    }
}

@media (max-width: 767px) {
    #<?= $componentId ?> {
        padding: 0.45rem 0.5rem 0;
    }

    #<?= $componentId ?> .prism-nav-shell {
        min-height: 68px;
        border-radius: 22px;
        padding: 12px 14px;
        width: calc(100% - 0.2rem);
    }

    #<?= $componentId ?> .prism-brand img {
        width: clamp(52px, 14vw, 62px);
        height: clamp(52px, 14vw, 62px);
    }

    #<?= $componentId ?> .prism-brand-copy strong {
        font-size: 0.92rem;
    }

    #<?= $componentId ?> .prism-nav-orb {
        width: 64px;
        height: 64px;
        right: 64px;
        top: 4px;
    }

    #<?= $componentId ?> .prism-mobile-panel {
        padding: 0.95rem;
        border-radius: 26px;
    }

    #<?= $componentId ?> .prism-mobile-brandline img {
        width: 48px;
        height: 48px;
        padding: 0.62rem;
    }
}

@media (max-width: 420px) {
    #<?= $componentId ?> .prism-mobile-grid {
        grid-template-columns: 1fr;
    }
}

@media (prefers-reduced-motion: reduce) {
    html {
        scroll-behavior: auto;
    }

    .wow,
    html.wow-ready .wow {
        visibility: visible !important;
    }

    .animate__animated {
        animation: none !important;
    }

    #<?= $componentId ?> .prism-nav-shell::after {
        animation: none;
    }
}
</style>
<?php if ($standaloneShell): ?>
</head>
<body class="bg-prism-cream text-prism-ink antialiased">
    <a class="prism-skip-link" href="#main-content">Skip to content</a>
<?php endif; ?>
<header id="<?= $componentId ?>" class="prism-site-header" data-prism-nav>
    <nav class="prism-nav-shell" aria-label="Primary navigation">
        <span class="prism-nav-orb" data-prism-parallax data-prism-speed="0.06" aria-hidden="true"></span>
        <span class="prism-nav-spark" data-prism-parallax data-prism-speed="0.1" aria-hidden="true"></span>

        <a class="prism-brand" href="/" aria-label="<?= htmlspecialchars($brandName) ?> home">
            <?php if ($logoDisplay): ?>
                <img src="<?= htmlspecialchars($logoUrl) ?>" alt="<?= htmlspecialchars($brandName) ?>" loading="lazy" decoding="async">
            <?php endif; ?>
            <span class="prism-brand-copy">
                <strong><?= htmlspecialchars($brandName) ?></strong>
                <?php if ($brandSubline !== ''): ?>
                    <span><?= htmlspecialchars($brandSubline) ?></span>
                <?php endif; ?>
            </span>
        </a>

        <?php if ($navDisplay): ?>
            <div class="prism-nav-links" role="list">
                <?php foreach ($navItems as $item): ?>
                    <a href="<?= htmlspecialchars($item['href']) ?>"><?= htmlspecialchars($item['text']) ?></a>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>

        <div class="prism-nav-actions">
            <?php if ($downloadBtnDisplay): ?>
                <a class="prism-cta" href="<?= htmlspecialchars($downloadBtnUrl) ?>"><?= htmlspecialchars($downloadBtnText) ?></a>
            <?php endif; ?>
            <button type="button" class="prism-toggle" aria-expanded="false" aria-label="Toggle menu">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
    </nav>

    <button type="button" class="prism-mobile-backdrop" hidden aria-label="Close mobile menu"></button>
    <div class="prism-mobile-panel" hidden role="dialog" aria-modal="true" aria-label="Mobile navigation">
        <div class="prism-mobile-head">
            <div class="prism-mobile-brandline">
                <?php if ($logoDisplay): ?>
                    <img src="<?= htmlspecialchars($logoUrl) ?>" alt="<?= htmlspecialchars($brandName) ?>" loading="lazy" decoding="async">
                <?php endif; ?>
                <div class="prism-mobile-head-copy">
                    <?php if ($mobileMenuLabel !== ''): ?>
                        <p><?= htmlspecialchars($mobileMenuLabel) ?></p>
                    <?php endif; ?>
                    <strong><?= htmlspecialchars($brandName) ?></strong>
                </div>
            </div>
            <button type="button" class="prism-mobile-close" aria-label="Close menu">&times;</button>
        </div>
        <div class="prism-mobile-track" aria-hidden="true">
            <span></span>
            <span></span>
        </div>
        <?php if ($navDisplay): ?>
            <nav class="prism-mobile-links" aria-label="Mobile navigation links">
                <?php if ($mobilePrimaryItem): ?>
                    <a class="prism-mobile-feature" href="<?= htmlspecialchars($mobilePrimaryItem['href']) ?>">
                        <span class="prism-mobile-feature-top">
                            <span class="prism-mobile-arrow" aria-hidden="true"></span>
                        </span>
                        <strong><?= htmlspecialchars($mobilePrimaryItem['text']) ?></strong>
                    </a>
                <?php endif; ?>

                <?php if (!empty($mobileSecondaryItems)): ?>
                    <div class="prism-mobile-grid" role="list">
                        <?php foreach ($mobileSecondaryItems as $index => $item): ?>
                            <a class="prism-mobile-card prism-mobile-card-<?= (int)(($index % 3) + 1) ?>" href="<?= htmlspecialchars($item['href']) ?>">
                                <strong><?= htmlspecialchars($item['text']) ?></strong>
                            </a>
                        <?php endforeach; ?>
                    </div>
                <?php endif; ?>
            </nav>
        <?php endif; ?>
        <?php if ($downloadBtnDisplay): ?>
            <a class="prism-mobile-cta" href="<?= htmlspecialchars($downloadBtnUrl) ?>"><?= htmlspecialchars($downloadBtnText) ?></a>
        <?php endif; ?>
    </div>
</header>
