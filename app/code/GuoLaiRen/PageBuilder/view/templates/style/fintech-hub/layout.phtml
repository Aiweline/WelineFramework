<?php
/**
 * FinTech Hub 模板 - Main Layout
 * 
 * 定义 header/content/footer 三个区域容器
 * 支持组件动态加载和可视化拖拽
 * 
 * @var \Weline\Framework\View\Template $this
 * @var \GuoLaiRen\PageBuilder\Model\Page $page
 */

$page = $this->getData('page');
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$pageId = $page && $page->getId() ? $page->getId() : 0;
$templateCode = 'fintech-hub';

// 获取页面布局配置（包含组件配置）
$layoutConfig = $this->getData('layout_config') ?: [];
$headerComponents = $layoutConfig['header'] ?? [];
$contentComponents = $layoutConfig['content'] ?? [];
$footerComponents = $layoutConfig['footer'] ?? [];

// 如果没有配置，使用默认组件
if (empty($headerComponents)) {
    $headerComponents = [
        ['code' => 'header-nav', 'enabled' => true, 'config' => []]
    ];
}

if (empty($contentComponents)) {
    $contentComponents = [
        ['code' => 'hero', 'enabled' => true, 'config' => []],
        ['code' => 'features', 'enabled' => true, 'config' => []],
        ['code' => 'stats', 'enabled' => true, 'config' => []],
        ['code' => 'services', 'enabled' => true, 'config' => []],
        ['code' => 'testimonials', 'enabled' => true, 'config' => []],
        ['code' => 'faq', 'enabled' => true, 'config' => []],
    ];
}

if (empty($footerComponents)) {
    $footerComponents = [
        ['code' => 'footer-links', 'enabled' => true, 'config' => []]
    ];
}

// 组件代码到文件的映射
$componentFiles = [
    // Header 组件
    'header-nav' => 'header/nav.phtml',
    
    // Content 组件
    'hero' => 'content/hero.phtml',
    'features' => 'content/features.phtml',
    'stats' => 'content/stats.phtml',
    'services' => 'content/services.phtml',
    'testimonials' => 'content/testimonials.phtml',
    'faq' => 'content/faq.phtml',
    
    // Footer 组件
    'footer-links' => 'footer/links.phtml',
];

/**
 * 渲染单个组件
 */
$renderComponent = function($component) use ($componentFiles, $page, $styleSettings) {
    $code = $component['code'] ?? '';
    $enabled = $component['enabled'] ?? true;
    $componentConfig = $component['config'] ?? [];
    
    if (!$enabled || empty($code)) {
        return '';
    }
    
    $file = $componentFiles[$code] ?? null;
    if (!$file) {
        return "<!-- Component not found: {$code} -->";
    }
    
    ob_start();
    $this->assign('page', $page);
    $this->assign('style', $styleSettings);
    $this->assign('style_settings', $styleSettings);
    $this->assign('component_config', $componentConfig);
    echo $this->fetchTemplate("GuoLaiRen_PageBuilder::templates/style/fintech-hub/components/{$file}");
    return ob_get_clean();
};

// 加载颜色配置
$this->assign('template_code', $templateCode);
$this->assign('style', $styleSettings);
$this->assign('style_settings', $styleSettings);
?>
<div style="display:none;">
    <w:template>GuoLaiRen_PageBuilder::base/colors.phtml</w:template>
</div>
<?php
// 确保 $colors 已定义
if (!isset($colors) || !is_array($colors)) {
    $colors = [];
}

// 颜色辅助函数
if (!function_exists('fh_color')) {
    function fh_color($colors, $key, $default = '') {
        return $colors[$key] ?? $default;
    }
}

// 配置辅助函数
if (!function_exists('fh_getConfig')) {
    function fh_getConfig($settings, $key, $default = '') {
        $value = $settings[$key] ?? $default;
        if ($value === '' && $default !== '') {
            return $default;
        }
        return $value;
    }
}

// 获取页面标题和元数据
$pageTitle = $page ? $page->getData('title') : 'VaultPay - Digital Finance Platform';
$pageDescription = $page ? $page->getData('meta_description') : 'Secure payments, smart investments, and global transfers';
$pageLogo = $page ? $page->getData('logo') : '';

// 导航配置
$navBgColor = fh_color($colors, 'nav_bg', 'rgba(8, 11, 22, 0.95)');
$navTextColor = fh_color($colors, 'nav_text', '#f8fafc');
$navLinkColor = fh_color($colors, 'nav_link', '#94a3b8');
$navLinkHoverColor = fh_color($colors, 'nav_link_hover', '#3b82f6');

// Body 背景
$bodyBg = fh_color($colors, 'body_bg', '#080b16');
?>
<!DOCTYPE html>
<html lang="<?= $this->getData('lang') ?? 'en' ?>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($pageTitle) ?></title>
    <meta name="description" content="<?= htmlspecialchars($pageDescription) ?>">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        /* ========== CSS Reset & Base ========== */
        *, *::before, *::after {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        
        html {
            scroll-behavior: smooth;
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: <?= $bodyBg ?>;
            color: <?= fh_color($colors, 'text_primary', '#f8fafc') ?>;
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
        
        img {
            max-width: 100%;
            height: auto;
            display: block;
        }
        
        a {
            text-decoration: none;
            color: inherit;
            transition: color 0.3s ease;
        }
        
        button {
            font-family: inherit;
            cursor: pointer;
            border: none;
            background: none;
        }
        
        ul, ol {
            list-style: none;
        }
        
        /* ========== Container ========== */
        .fh-container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        @media (max-width: 768px) {
            .fh-container {
                padding: 0 16px;
            }
        }
        
        /* ========== Section Base ========== */
        .fh-section {
            padding: 80px 0;
        }
        
        @media (max-width: 768px) {
            .fh-section {
                padding: 60px 0;
            }
        }
        
        .fh-section-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: <?= fh_color($colors, 'heading_color', '#f8fafc') ?>;
            margin-bottom: 16px;
            line-height: 1.2;
        }
        
        .fh-section-subtitle {
            font-size: 1.125rem;
            color: <?= fh_color($colors, 'text_secondary', '#94a3b8') ?>;
            max-width: 600px;
        }
        
        @media (max-width: 768px) {
            .fh-section-title {
                font-size: 1.75rem;
            }
            .fh-section-subtitle {
                font-size: 1rem;
            }
        }
        
        /* ========== Buttons ========== */
        .fh-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            padding: 14px 28px;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 12px;
            transition: all 0.3s ease;
            cursor: pointer;
        }
        
        .fh-btn-primary {
            background: <?= fh_color($colors, 'button_primary_bg', 'linear-gradient(135deg, #2563eb 0%, #3b82f6 100%)') ?>;
            color: <?= fh_color($colors, 'button_primary_text', '#ffffff') ?>;
            box-shadow: 0 4px 20px <?= fh_color($colors, 'button_primary_shadow', 'rgba(37, 99, 235, 0.4)') ?>;
        }
        
        .fh-btn-primary:hover {
            background: <?= fh_color($colors, 'button_primary_bg_hover', 'linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%)') ?>;
            box-shadow: 0 6px 30px <?= fh_color($colors, 'button_primary_shadow_hover', 'rgba(37, 99, 235, 0.6)') ?>;
            transform: translateY(-2px);
        }
        
        .fh-btn-secondary {
            background: transparent;
            color: <?= fh_color($colors, 'button_secondary_text', '#3b82f6') ?>;
            border: 2px solid <?= fh_color($colors, 'button_secondary_border', '#2563eb') ?>;
        }
        
        .fh-btn-secondary:hover {
            background: rgba(37, 99, 235, 0.1);
            border-color: <?= fh_color($colors, 'accent_primary_light', '#3b82f6') ?>;
        }
        
        /* ========== Cards ========== */
        .fh-card {
            background: <?= fh_color($colors, 'card_bg', 'rgba(255, 255, 255, 0.03)') ?>;
            border: 1px solid <?= fh_color($colors, 'card_border', 'rgba(255, 255, 255, 0.08)') ?>;
            border-radius: 16px;
            padding: 24px;
            transition: all 0.3s ease;
        }
        
        .fh-card:hover {
            background: <?= fh_color($colors, 'card_bg_hover', 'rgba(255, 255, 255, 0.06)') ?>;
            border-color: <?= fh_color($colors, 'card_border_hover', 'rgba(37, 99, 235, 0.4)') ?>;
            transform: translateY(-4px);
            box-shadow: <?= fh_color($colors, 'card_shadow_hover', '0 8px 32px rgba(37, 99, 235, 0.2)') ?>;
        }
        
        /* ========== Grid System ========== */
        .fh-grid {
            display: grid;
            gap: 24px;
        }
        
        .fh-grid-2 { grid-template-columns: repeat(2, 1fr); }
        .fh-grid-3 { grid-template-columns: repeat(3, 1fr); }
        .fh-grid-4 { grid-template-columns: repeat(4, 1fr); }
        
        @media (max-width: 992px) {
            .fh-grid-3, .fh-grid-4 { grid-template-columns: repeat(2, 1fr); }
        }
        
        @media (max-width: 576px) {
            .fh-grid-2, .fh-grid-3, .fh-grid-4 { grid-template-columns: 1fr; }
        }
        
        /* ========== Region Styles ========== */
        .fh-region {
            position: relative;
        }
        
        .fh-region-header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            background: <?= $navBgColor ?>;
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
        }
        
        .fh-region-content {
            padding-top: 80px; /* Header height offset */
        }
        
        .fh-region-footer {
            background: <?= fh_color($colors, 'footer_bg', '#060912') ?>;
            border-top: 1px solid <?= fh_color($colors, 'footer_border', 'rgba(255, 255, 255, 0.06)') ?>;
        }
        
        /* ========== Component Wrapper ========== */
        .fh-component-wrapper,
        .tpmst-component-wrapper {
            position: relative;
        }
        
        /* ========== Animations ========== */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .fh-animate {
            animation: fadeInUp 0.6s ease forwards;
        }
        
        /* ========== Scrollbar ========== */
        ::-webkit-scrollbar {
            width: 8px;
        }
        
        ::-webkit-scrollbar-track {
            background: <?= fh_color($colors, 'section_bg_4', '#080d1a') ?>;
        }
        
        ::-webkit-scrollbar-thumb {
            background: <?= fh_color($colors, 'accent_primary', '#2563eb') ?>;
            border-radius: 4px;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: <?= fh_color($colors, 'accent_primary_light', '#3b82f6') ?>;
        }
    </style>
</head>
<body>
    <!-- ========== HEADER 区域 ========== -->
    <header class="fh-region fh-region-header" data-region="header" data-multiple="false">
        <?php if (!empty($headerComponents)): ?>
            <?php foreach ($headerComponents as $component): ?>
                <?php if ($component['enabled'] ?? true): ?>
                <div class="fh-component-wrapper tpmst-component-wrapper" 
                     data-component="<?= htmlspecialchars($component['code']) ?>"
                     data-region="header">
                    <?= $renderComponent($component) ?>
                </div>
                <?php endif; ?>
            <?php endforeach; ?>
        <?php endif; ?>
    </header>
    
    <!-- ========== CONTENT 区域 ========== -->
    <main class="fh-region fh-region-content" data-region="content" data-multiple="true">
        <?php if (!empty($contentComponents)): ?>
            <?php foreach ($contentComponents as $index => $component): ?>
                <?php if ($component['enabled'] ?? true): ?>
                <div class="fh-component-wrapper tpmst-component-wrapper" 
                     data-component="<?= htmlspecialchars($component['code']) ?>"
                     data-region="content"
                     data-index="<?= $index ?>">
                    <?= $renderComponent($component) ?>
                </div>
                <?php endif; ?>
            <?php endforeach; ?>
        <?php endif; ?>
    </main>
    
    <!-- ========== FOOTER 区域 ========== -->
    <footer class="fh-region fh-region-footer" data-region="footer" data-multiple="false">
        <?php if (!empty($footerComponents)): ?>
            <?php foreach ($footerComponents as $component): ?>
                <?php if ($component['enabled'] ?? true): ?>
                <div class="fh-component-wrapper tpmst-component-wrapper" 
                     data-component="<?= htmlspecialchars($component['code']) ?>"
                     data-region="footer">
                    <?= $renderComponent($component) ?>
                </div>
                <?php endif; ?>
            <?php endforeach; ?>
        <?php endif; ?>
    </footer>
    
    <script>
        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });
        
        // Header scroll effect
        const header = document.querySelector('.fh-region-header');
        let lastScroll = 0;
        
        window.addEventListener('scroll', () => {
            const currentScroll = window.pageYOffset;
            
            if (currentScroll > 50) {
                header.style.boxShadow = '0 4px 20px rgba(0, 0, 0, 0.3)';
            } else {
                header.style.boxShadow = 'none';
            }
            
            lastScroll = currentScroll;
        });
    </script>
</body>
</html>
