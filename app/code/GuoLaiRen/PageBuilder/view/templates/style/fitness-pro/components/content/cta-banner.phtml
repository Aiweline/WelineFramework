<?php
/**
 * Fitness Pro - CTA Banner Component
 * 
 * @var \Weline\Framework\View\Template $this
 * @var array $colors
 * @var array $component_config
 * 
 * @fields_start
 * group:content => 内容设置
 * content.title => 标题:text:Start Your Transformation Today
 * content.description => 描述:textarea:Join hundreds of members who have already changed their lives. Your first session is on us — no strings attached.
 * group:buttons => 按钮设置
 * buttons.primary_text => 主按钮文字:text:Join Now
 * buttons.primary_url => 主按钮链接:text:#cta
 * buttons.secondary_text => 次按钮文字:text:Book Free Trial
 * buttons.secondary_url => 次按钮链接:text:/contact
 * buttons.show_secondary => 显示次按钮:select:yes|yes,no
 * @fields_end
 */

$config = $component_config ?? [];
if (!isset($colors) || !is_array($colors)) { $colors = []; }

$ctaBg = $colors['cta_bg'] ?? 'linear-gradient(135deg, #1a0f08 0%, #140e08 100%)';
$textPrimary = $colors['text_primary'] ?? '#fef3c7';
$textSecondary = $colors['text_secondary'] ?? '#d6b87a';
$accentPrimary = $colors['accent_primary'] ?? '#f97316';
$accentPrimaryLight = $colors['accent_primary_light'] ?? '#fb923c';
$buttonPrimaryBg = $colors['button_primary_bg'] ?? 'linear-gradient(135deg, #f97316 0%, #ec4899 100%)';
$borderLight = $colors['border_light'] ?? 'rgba(255, 255, 255, 0.08)';

$title = $config['content.title'] ?? 'Start Your Transformation Today';
$description = $config['content.description'] ?? 'Join hundreds of members who have already changed their lives. Your first session is on us — no strings attached.';
$primaryText = $config['buttons.primary_text'] ?? 'Join Now';
$primaryUrl = $config['buttons.primary_url'] ?? '#cta';
$secondaryText = $config['buttons.secondary_text'] ?? 'Book Free Trial';
$secondaryUrl = $config['buttons.secondary_url'] ?? '/contact';
$showSecondary = ($config['buttons.show_secondary'] ?? 'yes') !== 'no';

$instanceId = 'fp-cta-' . uniqid();
?>

<section id="<?= $instanceId ?>">
    <style>
        #<?= $instanceId ?> { background: <?= $ctaBg ?>; padding: 100px 0; position: relative; overflow: hidden; }
        #<?= $instanceId ?>::before { content: ''; position: absolute; top: -50%; left: -20%; width: 140%; height: 200%; background: radial-gradient(ellipse at center, rgba(249,115,22,0.08) 0%, transparent 60%); pointer-events: none; }
        #<?= $instanceId ?>::after { content: ''; position: absolute; bottom: -30%; right: -10%; width: 500px; height: 500px; background: radial-gradient(circle, rgba(236,72,153,0.06) 0%, transparent 70%); pointer-events: none; }
        #<?= $instanceId ?> .fp-cta-container { max-width: 800px; margin: 0 auto; padding: 0 20px; text-align: center; position: relative; z-index: 1; }
        #<?= $instanceId ?> .fp-cta-card { background: rgba(255,255,255,0.03); border: 1px solid <?= $borderLight ?>; border-radius: 24px; padding: 64px 48px; }
        #<?= $instanceId ?> .fp-cta-icon { font-size: 3rem; margin-bottom: 24px; }
        #<?= $instanceId ?> .fp-cta-title { font-size: 2.5rem; font-weight: 700; color: <?= $textPrimary ?>; margin-bottom: 16px; line-height: 1.2; }
        #<?= $instanceId ?> .fp-cta-desc { font-size: 1.125rem; color: <?= $textSecondary ?>; margin-bottom: 40px; line-height: 1.7; }
        #<?= $instanceId ?> .fp-cta-buttons { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
        #<?= $instanceId ?> .fp-cta-btn-primary { display: inline-flex; align-items: center; gap: 10px; padding: 16px 36px; background: <?= $buttonPrimaryBg ?>; color: #fff; font-size: 1.05rem; font-weight: 600; border-radius: 12px; transition: all 0.3s ease; box-shadow: 0 4px 20px rgba(249,115,22,0.4); text-decoration: none; }
        #<?= $instanceId ?> .fp-cta-btn-primary:hover { transform: translateY(-3px); box-shadow: 0 8px 30px rgba(249,115,22,0.5); }
        #<?= $instanceId ?> .fp-cta-btn-secondary { display: inline-flex; align-items: center; gap: 10px; padding: 16px 36px; background: transparent; color: <?= $accentPrimaryLight ?>; font-size: 1.05rem; font-weight: 600; border: 2px solid rgba(249,115,22,0.4); border-radius: 12px; transition: all 0.3s ease; text-decoration: none; }
        #<?= $instanceId ?> .fp-cta-btn-secondary:hover { background: rgba(249,115,22,0.1); border-color: <?= $accentPrimaryLight ?>; }
        @media (max-width: 768px) {
            #<?= $instanceId ?> .fp-cta-card { padding: 40px 24px; }
            #<?= $instanceId ?> .fp-cta-title { font-size: 1.75rem; }
            #<?= $instanceId ?> .fp-cta-buttons { flex-direction: column; align-items: center; }
            #<?= $instanceId ?> .fp-cta-btn-primary, #<?= $instanceId ?> .fp-cta-btn-secondary { width: 100%; max-width: 320px; justify-content: center; }
        }
    </style>
    <script>
    (function(){
        var primaryUrl = <?= json_encode($primaryUrl, JSON_UNESCAPED_SLASHES) ?>;
        var secondaryUrl = <?= json_encode($secondaryUrl, JSON_UNESCAPED_SLASHES) ?>;
        document.querySelectorAll('#<?= $instanceId ?> [data-download-trigger="primary"]').forEach(function(el){
            el.addEventListener('click', function(){ window.location.href = primaryUrl; });
        });
        document.querySelectorAll('#<?= $instanceId ?> [data-download-trigger="secondary"]').forEach(function(el){
            el.addEventListener('click', function(){ window.location.href = secondaryUrl; });
        });
    })();
    </script>
    <div class="fp-cta-container" id="cta">
        <div class="fp-cta-card">
            <div class="fp-cta-icon">🔥</div>
            <h2 class="fp-cta-title"><?= htmlspecialchars($title) ?></h2>
            <p class="fp-cta-desc"><?= htmlspecialchars($description) ?></p>
            <div class="fp-cta-buttons">
                <a href="javascript:void(0)" role="button" class="fp-cta-btn-primary" data-download-trigger="primary">
                    <?= htmlspecialchars($primaryText) ?>
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                </a>
                <?php if ($showSecondary): ?>
                <a href="javascript:void(0)" role="button" class="fp-cta-btn-secondary" data-download-trigger="secondary">
                    <?= htmlspecialchars($secondaryText) ?>
                </a>
                <?php endif; ?>
            </div>
        </div>
    </div>
</section>
