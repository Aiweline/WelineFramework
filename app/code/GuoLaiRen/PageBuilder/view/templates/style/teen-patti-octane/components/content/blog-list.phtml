<?php
/**
 * Teen Patti Octane - Blog List Component
 *
 * @fields_start
 * blog.title|Blog list title|text|Latest Teen Patti Octane Updates
 * blog.subtitle|Subtitle|text|Tips, patch notes, and strategy breakdowns from the Octane desk
 * blog.posts_per_page|Posts per page|number|9
 * blog.show_sidebar|Show sidebar|select:yes=Yes,no=No|yes
 * blog.show_category|Show category|select:yes=Yes,no=No|yes
 * blog.show_date|Show date|select:yes=Yes,no=No|yes
 * blog.show_author|Show author|select:yes=Yes,no=No|yes
 * blog.card_style|Card style|select:default=Default,minimal=Minimal,featured=Featured|default
 * @fields_end
 */

$style = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$blogPosts = $this->getData('blog_posts') ?: [];
$blogCategories = $this->getData('blog_categories') ?: [];
$recentPosts = $this->getData('recent_posts') ?: [];

$getConfig = function ($key, $default = '') use ($style, $componentConfig) {
    if (isset($componentConfig[$key]) && $componentConfig[$key] !== '') {
        return $componentConfig[$key];
    }
    return $style[$key] ?? $default;
};

$title = $getConfig('blog.title', __('Latest Teen Patti Octane Updates'));
$subtitle = $getConfig('blog.subtitle', __('Tips, patch notes, and strategy breakdowns from the Octane desk'));
$postsPerPage = max(1, (int)$getConfig('blog.posts_per_page', 9));
$showSidebar = $getConfig('blog.show_sidebar', 'yes') === 'yes';
$showCategory = $getConfig('blog.show_category', 'yes') === 'yes';
$showDate = $getConfig('blog.show_date', 'yes') === 'yes';
$showAuthor = $getConfig('blog.show_author', 'yes') === 'yes';
$cardStyle = $getConfig('blog.card_style', 'default');

$primaryColor = $getConfig('color_scheme.primary', '#ff8a3d');
$secondaryColor = $getConfig('color_scheme.secondary', '#53e0c5');
$bgColor = $getConfig('color_scheme.background', '#16100b');
$textColor = $getConfig('color_scheme.text', '#fff5ea');

$posts = array_slice($blogPosts, 0, $postsPerPage);
$recent = !empty($recentPosts) ? array_slice($recentPosts, 0, 5) : array_slice($posts, 0, 5);

$formatDate = static function ($value) {
    if (!$value) {
        return '';
    }
    $timestamp = strtotime((string)$value);
    return $timestamp ? date('M d, Y', $timestamp) : (string)$value;
};
?>

<section class="tpmst-section oct-blog-list" id="blog">
    <div class="section-inner">
        <header class="blog-head">
            <span class="blog-kicker">Octane Journal</span>
            <div class="blog-head-row">
                <h2><?= htmlspecialchars($title) ?></h2>
                <?php if (!empty($posts)): ?>
                    <span class="blog-count"><?= count($posts) ?> <?= __('Stories') ?></span>
                <?php endif; ?>
            </div>
            <?php if ($subtitle): ?>
                <p><?= htmlspecialchars($subtitle) ?></p>
            <?php endif; ?>
        </header>

        <div class="blog-layout<?= $showSidebar ? ' with-sidebar' : ' full-width' ?>">
            <main class="posts-grid" aria-label="Blog posts">
                <?php if (empty($posts)): ?>
                    <article class="empty-state">
                        <h3><?= __('No posts yet') ?></h3>
                        <p><?= __('New strategy and product updates will appear here soon.') ?></p>
                    </article>
                <?php else: ?>
                    <?php foreach ($posts as $index => $post): ?>
                        <?php
                        $postUrl = !empty($post['url']) ? $post['url'] : '#';
                        $postTitle = !empty($post['title']) ? $post['title'] : __('Untitled Post');
                        $postSummary = !empty($post['summary']) ? $post['summary'] : '';
                        $postImage = !empty($post['cover_image']) ? $post['cover_image'] : '';
                        $postCategory = !empty($post['category_name']) ? $post['category_name'] : '';
                        $postDate = $formatDate($post['published_at'] ?? '');
                        $postAuthor = !empty($post['author']) ? $post['author'] : '';
                        $laneClass = 'lane-' . (($index % 3) + 1);
                        ?>
                        <article class="post-card style-<?= htmlspecialchars($cardStyle) ?> <?= htmlspecialchars($laneClass) ?><?= $index === 0 && $cardStyle === 'featured' ? ' is-featured' : '' ?>">
                            <?php if ($postImage): ?>
                                <a class="post-image" href="<?= htmlspecialchars($postUrl) ?>">
                                    <img src="<?= htmlspecialchars($postImage) ?>" alt="<?= htmlspecialchars($postTitle) ?>" loading="lazy" decoding="async">
                                </a>
                            <?php endif; ?>

                            <div class="post-body">
                                <div class="post-meta">
                                    <?php if ($showCategory && $postCategory): ?>
                                        <span class="post-chip"><?= htmlspecialchars($postCategory) ?></span>
                                    <?php endif; ?>
                                    <?php if ($showDate && $postDate): ?>
                                        <span class="post-date"><?= htmlspecialchars($postDate) ?></span>
                                    <?php endif; ?>
                                </div>

                                <h3><a href="<?= htmlspecialchars($postUrl) ?>"><?= htmlspecialchars($postTitle) ?></a></h3>

                                <?php if ($postSummary): ?>
                                    <p class="post-summary">
                                        <?= htmlspecialchars(mb_substr($postSummary, 0, 165)) ?><?= mb_strlen($postSummary) > 165 ? '...' : '' ?>
                                    </p>
                                <?php endif; ?>

                                <div class="post-footer">
                                    <?php if ($showAuthor && $postAuthor): ?>
                                        <span class="post-author"><?= htmlspecialchars($postAuthor) ?></span>
                                    <?php endif; ?>
                                    <a class="read-more" href="<?= htmlspecialchars($postUrl) ?>"><?= __('Read More') ?></a>
                                </div>
                            </div>
                        </article>
                    <?php endforeach; ?>
                <?php endif; ?>
            </main>

            <?php if ($showSidebar): ?>
                <aside class="sidebar">
                    <?php if (!empty($blogCategories)): ?>
                        <section class="sidebar-card">
                            <h4><?= __('Categories') ?></h4>
                            <ul>
                                <?php foreach ($blogCategories as $category): ?>
                                    <?php
                                    $categoryName = $category['name'] ?? '';
                                    $categoryUrl = $category['url'] ?? '#';
                                    if ($categoryName === '') continue;
                                    ?>
                                    <li><a href="<?= htmlspecialchars($categoryUrl) ?>"><?= htmlspecialchars($categoryName) ?></a></li>
                                <?php endforeach; ?>
                            </ul>
                        </section>
                    <?php endif; ?>

                    <?php if (!empty($recent)): ?>
                        <section class="sidebar-card">
                            <h4><?= __('Recent Posts') ?></h4>
                            <ul class="recent-list">
                                <?php foreach ($recent as $post): ?>
                                    <?php
                                    $postUrl = $post['url'] ?? '#';
                                    $postTitle = $post['title'] ?? __('Untitled Post');
                                    $postDate = $formatDate($post['published_at'] ?? '');
                                    ?>
                                    <li>
                                        <a href="<?= htmlspecialchars($postUrl) ?>">
                                            <span class="recent-title"><?= htmlspecialchars($postTitle) ?></span>
                                            <?php if ($postDate): ?>
                                                <span class="recent-date"><?= htmlspecialchars($postDate) ?></span>
                                            <?php endif; ?>
                                        </a>
                                    </li>
                                <?php endforeach; ?>
                            </ul>
                        </section>
                    <?php endif; ?>
                </aside>
            <?php endif; ?>
        </div>
    </div>
</section>

<style>
.oct-blog-list {
    padding: 58px 0;
    background:
        radial-gradient(960px 280px at 2% 0%, rgba(83, 224, 197, 0.22), transparent 70%),
        radial-gradient(760px 320px at 96% 100%, rgba(255, 138, 61, 0.2), transparent 72%),
        <?= htmlspecialchars($bgColor) ?>;
    color: <?= htmlspecialchars($textColor) ?>;
}

.oct-blog-list .section-inner {
    width: min(1180px, calc(100% - 1.2rem));
    margin: 0 auto;
}

.oct-blog-list .blog-head {
    text-align: left;
    margin-bottom: 30px;
    display: grid;
    gap: 0.7rem;
}

.oct-blog-list .blog-kicker {
    width: fit-content;
    display: inline-flex;
    padding: 0.26rem 0.66rem;
    border-radius: 999px;
    color: #53e0c5;
    border: 1px solid rgba(83, 224, 197, 0.38);
    background: rgba(83, 224, 197, 0.12);
    font-size: 0.72rem;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    font-weight: 800;
}

.oct-blog-list .blog-head-row {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 1rem;
    flex-wrap: wrap;
}

.oct-blog-list .blog-head h2 {
    margin: 0;
    font-size: clamp(1.8rem, 4vw, 2.7rem);
    background: linear-gradient(120deg, <?= htmlspecialchars($primaryColor) ?>, <?= htmlspecialchars($secondaryColor) ?>);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
}

.oct-blog-list .blog-count {
    color: rgba(255, 245, 234, 0.8);
    border: 1px solid rgba(255, 233, 207, 0.2);
    border-radius: 999px;
    padding: 0.34rem 0.66rem;
    font-size: 0.76rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    font-weight: 700;
}

.oct-blog-list .blog-head p {
    margin: 0;
    max-width: 760px;
    color: rgba(255, 245, 234, 0.78);
    line-height: 1.74;
}

.oct-blog-list .blog-layout {
    display: grid;
    gap: 18px;
}

.oct-blog-list .blog-layout.with-sidebar {
    grid-template-columns: minmax(0, 1fr) minmax(260px, 320px);
}

.oct-blog-list .posts-grid {
    display: grid;
    grid-template-columns: repeat(6, minmax(0, 1fr));
    gap: 16px;
}

.oct-blog-list .post-card {
    grid-column: span 3;
    border: 1px solid rgba(255, 233, 207, 0.16);
    border-radius: 16px;
    overflow: hidden;
    background: linear-gradient(170deg, rgba(255, 245, 232, 0.08), rgba(255, 245, 232, 0.04));
    display: grid;
}

.oct-blog-list .post-card.lane-1 {
    border-color: rgba(83, 224, 197, 0.32);
}

.oct-blog-list .post-card.lane-2 {
    border-color: rgba(255, 138, 61, 0.3);
}

.oct-blog-list .post-card.lane-3 {
    border-color: rgba(255, 214, 168, 0.24);
}

.oct-blog-list .post-card:first-child {
    grid-column: span 6;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.02fr);
}

.oct-blog-list .post-card:first-child .post-image {
    aspect-ratio: auto;
    min-height: 100%;
}

.oct-blog-list .post-card.style-minimal .post-image {
    display: none;
}

.oct-blog-list .post-card.style-featured.is-featured {
    grid-column: span 2;
}

.oct-blog-list .post-image {
    display: block;
    aspect-ratio: 16 / 9;
    overflow: hidden;
}

.oct-blog-list .post-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
}

.oct-blog-list .post-card:hover .post-image img {
    transform: scale(1.05);
}

.oct-blog-list .post-body {
    padding: 16px;
    display: grid;
    gap: 0.72rem;
}

.oct-blog-list .post-meta {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    flex-wrap: wrap;
}

.oct-blog-list .post-chip {
    font-size: 0.72rem;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: #53e0c5;
    border: 1px solid rgba(83, 224, 197, 0.4);
    border-radius: 999px;
    padding: 0.18rem 0.52rem;
}

.oct-blog-list .post-date {
    font-size: 0.76rem;
    color: rgba(255, 245, 234, 0.65);
}

.oct-blog-list .post-body h3 {
    margin: 0;
    font-size: 1.12rem;
    line-height: 1.4;
}

.oct-blog-list .post-body h3 a {
    color: inherit;
    text-decoration: none;
}

.oct-blog-list .post-summary {
    margin: 0;
    color: rgba(255, 245, 234, 0.82);
    line-height: 1.76;
    font-size: 0.92rem;
}

.oct-blog-list .post-footer {
    margin-top: 0.15rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.7rem;
}

.oct-blog-list .post-author {
    color: rgba(255, 245, 234, 0.68);
    font-size: 0.8rem;
}

.oct-blog-list .read-more {
    color: <?= htmlspecialchars($secondaryColor) ?>;
    text-decoration: none;
    font-weight: 700;
    font-size: 0.83rem;
}

.oct-blog-list .sidebar {
    display: grid;
    gap: 14px;
    align-content: start;
    position: sticky;
    top: 96px;
    height: fit-content;
}

.oct-blog-list .sidebar-card {
    border: 1px solid rgba(255, 233, 207, 0.16);
    border-radius: 14px;
    background: rgba(255, 245, 232, 0.06);
    padding: 14px;
}

.oct-blog-list .sidebar-card h4 {
    margin: 0;
    color: <?= htmlspecialchars($primaryColor) ?>;
    font-size: 1rem;
}

.oct-blog-list .sidebar-card ul {
    margin: 0.75rem 0 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0.56rem;
}

.oct-blog-list .sidebar-card a {
    color: rgba(255, 245, 234, 0.85);
    text-decoration: none;
    line-height: 1.55;
}

.oct-blog-list .recent-list .recent-title {
    display: block;
    font-size: 0.88rem;
}

.oct-blog-list .recent-list .recent-date {
    display: block;
    font-size: 0.74rem;
    color: rgba(255, 245, 234, 0.58);
    margin-top: 0.2rem;
}

.oct-blog-list .empty-state {
    grid-column: 1 / -1;
    border: 1px solid rgba(255, 233, 207, 0.16);
    border-radius: 16px;
    padding: 28px;
    text-align: center;
    background: rgba(255, 245, 232, 0.05);
}

.oct-blog-list .empty-state h3 {
    margin: 0;
    color: <?= htmlspecialchars($primaryColor) ?>;
}

.oct-blog-list .empty-state p {
    margin: 0.7rem 0 0;
    color: rgba(255, 245, 234, 0.78);
}

@media (max-width: 1024px) {
    .oct-blog-list .blog-layout.with-sidebar {
        grid-template-columns: 1fr;
    }

    .oct-blog-list .posts-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .oct-blog-list .post-card,
    .oct-blog-list .post-card:first-child {
        grid-column: span 1;
        grid-template-columns: 1fr;
    }

    .oct-blog-list .sidebar {
        position: static;
    }
}

@media (max-width: 767px) {
    .oct-blog-list {
        padding: 44px 0;
    }

    .oct-blog-list .section-inner {
        width: calc(100% - 1rem);
    }

    .oct-blog-list .post-body {
        padding: 14px;
    }
}
</style>
