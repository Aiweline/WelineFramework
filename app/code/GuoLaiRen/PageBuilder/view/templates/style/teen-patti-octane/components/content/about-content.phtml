<?php
/**
 * About content component
 *
 * @component_start
 * code: about-content
 * name: 关于我们内容
 * name_en: About Content
 * description: 企业/品牌关于页面内容展示组件，继承主题配色
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示设置
 * display.show_stats => 显示统计:select:yes|yes,no
 * display.show_values => 显示价值观:select:yes|yes,no
 * display.show_team => 显示团队:select:yes|yes,no
 * display.show_cta => 显示底部按钮:select:yes|yes,no
 *
 * group:hero => 首屏设置
 * hero.kicker => 顶部提示:text:Teen Patti Octane Story
 * hero.title => 页面标题:text:About Teen Patti Octane
 * hero.subtitle => 页面副标题:textarea:Built for players who want quick rounds, clear controls, and reliable card-table flow.
 * hero.bg_color => 背景颜色:color:#1b0f08
 * hero.bg_gradient_end => 渐变结束色:color:#34190c
 * hero.title_color => 标题颜色:color:#ffd6a8
 * hero.text_color => 正文颜色:color:#f2ddca
 * hero.emblem_label => 徽章上文字:text:Octane
 * hero.emblem_value => 徽章下文字:text:Flow
 * hero.padding => 内边距:number:80|px
 *
 * group:stats => 统计设置
 * stats.stat_1_number => 统计1数值:text:30M+
 * stats.stat_1_label => 统计1标签:text:Hands Played Monthly
 * stats.stat_2_number => 统计2数值:text:4.8/5
 * stats.stat_2_label => 统计2标签:text:Player Rating
 * stats.stat_3_number => 统计3数值:text:24/7
 * stats.stat_3_label => 统计3标签:text:Table Availability
 * stats.stat_4_number => 统计4数值:text:1.2s
 * stats.stat_4_label => 统计4标签:text:Average Match Join
 * stats.number_color => 数值颜色:color:#ffd6a8
 * stats.label_color => 标签颜色:color:#c5ad95
 * stats.card_bg => 卡片背景色:color:rgba(255,245,232,0.08)
 *
 * group:story => 品牌故事
 * story.title => 区块标题:text:How Octane Was Built
 * story.subtitle => 区块副标题:text:From static lobbies to a faster match rhythm
 * story.content => 区块内容:textarea:Teen Patti Octane started with one goal: reduce waiting and keep every table interaction clear.
 * story.bg_color => 区块背景色:color:#16100b
 * story.title_color => 区块标题颜色:color:#ffd6a8
 *
 * group:values => 价值观设置
 * values.title => 区块标题:text:What We Optimize Every Release
 * values.subtitle => 区块副标题:text:Three priorities shape each update
 * values.value_1_icon => 价值观1图标:text:A
 * values.value_1_title => 价值观1标题:text:Access Speed
 * values.value_1_desc => 价值观1描述:textarea:Join rounds quickly with lightweight flows and clear route entry points.
 * values.value_2_icon => 价值观2图标:text:F
 * values.value_2_title => 价值观2标题:text:Fair Sessions
 * values.value_2_desc => 价值观2描述:textarea:Transparent table logic and trackable wallet history keep trust high.
 * values.value_3_icon => 价值观3图标:text:M
 * values.value_3_title => 价值观3标题:text:Mobile First
 * values.value_3_desc => 价值观3描述:textarea:Layouts, controls, and interactions are tuned for one-hand play.
 * values.bg_color => 区块背景色:color:#27170f
 *
 * group:team => 团队设置
 * team.title => 区块标题:text:Team Behind The Tables
 * team.subtitle => 区块副标题:text:Operators, designers, and game-flow engineers
 * team.member_1_icon => 成员1图标:text:R
 * team.member_1_name => 成员1姓名:text:Rohan Singh
 * team.member_1_role => 成员1角色:text:Product Lead
 * team.member_2_icon => 成员2图标:text:P
 * team.member_2_name => 成员2姓名:text:Priya Mehta
 * team.member_2_role => 成员2角色:text:Gameplay Ops
 * team.member_3_icon => 成员3图标:text:K
 * team.member_3_name => 成员3姓名:text:Karan Patel
 * team.member_3_role => 成员3角色:text:Reward Systems
 * team.member_4_icon => 成员4图标:text:S
 * team.member_4_name => 成员4姓名:text:Sneha Rao
 * team.member_4_role => 成员4角色:text:Player Success
 *
 * group:cta => 底部按钮
 * cta.title => 按钮区标题:text:Ready To Join A Faster Table?
 * cta.description => 按钮区描述:textarea:Download the latest Teen Patti Octane build and jump into live match routes.
 * cta.button_text => 按钮文本:text:Download Now
 * cta.button_url => 按钮链接:text:#download
 * cta.button_bg => 按钮基础色:color:#ff9f4d
 * cta.button_text_color => 按钮文字颜色:color:#2b140d
 *
 * @fields_end
 */

$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $component_config ?? $this->getData('component_config') ?? [];
$config = array_merge(
    is_array($styleSettings) ? $styleSettings : [],
    is_array($componentConfig) ? $componentConfig : []
);

$getConfig = function ($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

$showStats = $getConfig('display.show_stats', 'yes') !== 'no';
$showValues = $getConfig('display.show_values', 'yes') !== 'no';
$showTeam = $getConfig('display.show_team', 'yes') !== 'no';
$showCta = $getConfig('display.show_cta', 'yes') !== 'no';

$page = $this->getData('page');
$pageTitle = $page ? (string)($page->getData('title') ?: 'About Teen Patti Octane') : 'About Teen Patti Octane';
$pageContent = $page ? (string)($page->getData('content') ?: '') : '';

if (!isset($colors) || !is_array($colors)) {
    $colors = [];
}

$heroBg = $getConfig('hero.bg_color', $colors['hero_bg'] ?? '#1b0f08');
$heroGradientEnd = $getConfig('hero.bg_gradient_end', $colors['hero_bg_gradient_end'] ?? '#34190c');
$heroTitleColor = $getConfig('hero.title_color', $colors['heading_color'] ?? '#ffd6a8');
$heroTextColor = $getConfig('hero.text_color', $colors['text_secondary'] ?? '#f2ddca');
$heroPadding = (int)$getConfig('hero.padding', '80');

$sectionBg1 = $getConfig('story.bg_color', $colors['section_bg_1'] ?? '#16100b');
$sectionBg2 = $getConfig('values.bg_color', $colors['section_bg_2'] ?? '#27170f');
$textPrimary = $colors['text_primary'] ?? '#fff5ea';
$textSecondary = $colors['text_secondary'] ?? '#f2ddca';
$textMuted = $colors['text_muted'] ?? '#c5ad95';
$borderLight = $colors['border_light'] ?? 'rgba(255,233,207,0.16)';
$cardBg = $getConfig('stats.card_bg', $colors['card_bg'] ?? 'rgba(255,245,232,0.08)');

$heroKicker = $getConfig('hero.kicker', 'Teen Patti Octane Story');
$heroTitle = $getConfig('hero.title', $pageTitle);
$heroSubtitle = $getConfig('hero.subtitle', 'Built for players who want quick rounds, clear controls, and reliable card-table flow.');
$heroEmblemLabel = $getConfig('hero.emblem_label', 'Octane');
$heroEmblemValue = $getConfig('hero.emblem_value', 'Flow');

$stats = [
    [
        'number' => $getConfig('stats.stat_1_number', '30M+'),
        'label' => $getConfig('stats.stat_1_label', 'Hands Played Monthly'),
    ],
    [
        'number' => $getConfig('stats.stat_2_number', '4.8/5'),
        'label' => $getConfig('stats.stat_2_label', 'Player Rating'),
    ],
    [
        'number' => $getConfig('stats.stat_3_number', '24/7'),
        'label' => $getConfig('stats.stat_3_label', 'Table Availability'),
    ],
    [
        'number' => $getConfig('stats.stat_4_number', '1.2s'),
        'label' => $getConfig('stats.stat_4_label', 'Average Match Join'),
    ],
];
$statsNumberColor = $getConfig('stats.number_color', $heroTitleColor);
$statsLabelColor = $getConfig('stats.label_color', $textMuted);

$storyTitle = $getConfig('story.title', 'How Octane Was Built');
$storySubtitle = $getConfig('story.subtitle', 'From static lobbies to a faster match rhythm');
$storyTitleColor = $getConfig('story.title_color', $heroTitleColor);
$storyContent = $getConfig('story.content', 'Teen Patti Octane started with one goal: reduce waiting and keep every table interaction clear.');

$storyRaw = trim($storyContent !== '' ? $storyContent : strip_tags($pageContent));
$storyParagraphs = array_values(array_filter(array_map('trim', preg_split('/\r?\n+/', $storyRaw))));
if (empty($storyParagraphs)) {
    $storyParagraphs = [
        'Teen Patti Octane started with one goal: reduce waiting and keep every table interaction clear.',
    ];
}

$valuesTitle = $getConfig('values.title', 'What We Optimize Every Release');
$valuesSubtitle = $getConfig('values.subtitle', 'Three priorities shape each update');
$values = [
    [
        'icon' => $getConfig('values.value_1_icon', 'A'),
        'title' => $getConfig('values.value_1_title', 'Access Speed'),
        'desc' => $getConfig('values.value_1_desc', 'Join rounds quickly with lightweight flows and clear route entry points.'),
    ],
    [
        'icon' => $getConfig('values.value_2_icon', 'F'),
        'title' => $getConfig('values.value_2_title', 'Fair Sessions'),
        'desc' => $getConfig('values.value_2_desc', 'Transparent table logic and trackable wallet history keep trust high.'),
    ],
    [
        'icon' => $getConfig('values.value_3_icon', 'M'),
        'title' => $getConfig('values.value_3_title', 'Mobile First'),
        'desc' => $getConfig('values.value_3_desc', 'Layouts, controls, and interactions are tuned for one-hand play.'),
    ],
];

$teamTitle = $getConfig('team.title', 'Team Behind The Tables');
$teamSubtitle = $getConfig('team.subtitle', 'Operators, designers, and game-flow engineers');
$teamMembers = [
    [
        'icon' => $getConfig('team.member_1_icon', 'R'),
        'name' => $getConfig('team.member_1_name', 'Rohan Singh'),
        'role' => $getConfig('team.member_1_role', 'Product Lead'),
    ],
    [
        'icon' => $getConfig('team.member_2_icon', 'P'),
        'name' => $getConfig('team.member_2_name', 'Priya Mehta'),
        'role' => $getConfig('team.member_2_role', 'Gameplay Ops'),
    ],
    [
        'icon' => $getConfig('team.member_3_icon', 'K'),
        'name' => $getConfig('team.member_3_name', 'Karan Patel'),
        'role' => $getConfig('team.member_3_role', 'Reward Systems'),
    ],
    [
        'icon' => $getConfig('team.member_4_icon', 'S'),
        'name' => $getConfig('team.member_4_name', 'Sneha Rao'),
        'role' => $getConfig('team.member_4_role', 'Player Success'),
    ],
];

$ctaTitle = $getConfig('cta.title', 'Ready To Join A Faster Table?');
$ctaDescription = $getConfig('cta.description', 'Download the latest Teen Patti Octane build and jump into live match routes.');
$ctaButtonText = $getConfig('cta.button_text', 'Download Now');
$ctaButtonUrl = $getConfig('cta.button_url', '#download');
$ctaButtonBgConfig = $getConfig('cta.button_bg', '#ff9f4d');
$ctaButtonBg = stripos($ctaButtonBgConfig, 'gradient(') !== false
    ? $ctaButtonBgConfig
    : 'linear-gradient(100deg, ' . $ctaButtonBgConfig . ' 0%, #ff4d4d 100%)';
$ctaButtonTextColor = $getConfig('cta.button_text_color', $colors['button_primary_text'] ?? '#2b140d');

$instanceId = $component_instance_id ?? 'about-content-' . uniqid();
?>

<section id="<?= $instanceId ?>" class="oct-about-content">
    <div class="about-hero">
        <div class="section-inner">
            <div class="hero-grid">
                <div class="hero-copy">
                    <p class="hero-kicker"><?= htmlspecialchars($heroKicker) ?></p>
                    <h1><?= htmlspecialchars($heroTitle) ?></h1>
                    <p><?= htmlspecialchars($heroSubtitle) ?></p>
                    <?php if ($showStats): ?>
                        <div class="hero-pulse">
                            <?php foreach (array_slice($stats, 0, 3) as $stat): ?>
                                <span><?= htmlspecialchars($stat['number']) ?> <em><?= htmlspecialchars($stat['label']) ?></em></span>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </div>
                <div class="hero-emblem" aria-hidden="true">
                    <span class="emblem-ring ring-a"></span>
                    <span class="emblem-ring ring-b"></span>
                    <div class="emblem-core">
                        <small><?= htmlspecialchars($heroEmblemLabel) ?></small>
                        <strong><?= htmlspecialchars($heroEmblemValue) ?></strong>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <?php if ($showStats): ?>
        <div class="about-stats">
            <div class="section-inner stats-grid">
                <?php foreach ($stats as $stat): ?>
                    <article class="stat-card">
                        <h3><?= htmlspecialchars($stat['number']) ?></h3>
                        <p><?= htmlspecialchars($stat['label']) ?></p>
                    </article>
                <?php endforeach; ?>
            </div>
        </div>
    <?php endif; ?>

    <div class="about-story">
        <div class="section-inner story-shell">
            <h2><?= htmlspecialchars($storyTitle) ?></h2>
            <p class="story-subtitle"><?= htmlspecialchars($storySubtitle) ?></p>
            <div class="story-body">
                <?php foreach ($storyParagraphs as $paragraph): ?>
                    <p><?= htmlspecialchars($paragraph) ?></p>
                <?php endforeach; ?>
            </div>
        </div>
    </div>

    <?php if ($showValues): ?>
        <div class="about-values">
            <div class="section-inner">
                <h2><?= htmlspecialchars($valuesTitle) ?></h2>
                <p class="values-subtitle"><?= htmlspecialchars($valuesSubtitle) ?></p>
                <div class="values-grid">
                    <?php foreach ($values as $value): ?>
                        <article class="value-card">
                            <span class="value-icon"><?= htmlspecialchars($value['icon']) ?></span>
                            <h3><?= htmlspecialchars($value['title']) ?></h3>
                            <p><?= htmlspecialchars($value['desc']) ?></p>
                        </article>
                    <?php endforeach; ?>
                </div>
            </div>
        </div>
    <?php endif; ?>

    <?php if ($showTeam): ?>
        <div class="about-team">
            <div class="section-inner">
                <h2><?= htmlspecialchars($teamTitle) ?></h2>
                <p class="team-subtitle"><?= htmlspecialchars($teamSubtitle) ?></p>
                <div class="team-grid">
                    <?php foreach ($teamMembers as $member): ?>
                        <article class="team-card">
                            <span class="team-avatar"><?= htmlspecialchars($member['icon']) ?></span>
                            <h3><?= htmlspecialchars($member['name']) ?></h3>
                            <p><?= htmlspecialchars($member['role']) ?></p>
                        </article>
                    <?php endforeach; ?>
                </div>
            </div>
        </div>
    <?php endif; ?>

    <?php if ($showCta): ?>
        <div class="about-cta">
            <div class="section-inner cta-shell">
                <div>
                    <h2><?= htmlspecialchars($ctaTitle) ?></h2>
                    <p><?= htmlspecialchars($ctaDescription) ?></p>
                </div>
                <a class="cta-button" href="<?= htmlspecialchars($ctaButtonUrl) ?>"><?= htmlspecialchars($ctaButtonText) ?></a>
            </div>
        </div>
    <?php endif; ?>
</section>

<style>
#<?= $instanceId ?> {
    --hero-bg: <?= htmlspecialchars($heroBg) ?>;
    --hero-bg-end: <?= htmlspecialchars($heroGradientEnd) ?>;
    --hero-title: <?= htmlspecialchars($heroTitleColor) ?>;
    --hero-text: <?= htmlspecialchars($heroTextColor) ?>;
    --section-bg-1: <?= htmlspecialchars($sectionBg1) ?>;
    --section-bg-2: <?= htmlspecialchars($sectionBg2) ?>;
    --text-primary: <?= htmlspecialchars($textPrimary) ?>;
    --text-secondary: <?= htmlspecialchars($textSecondary) ?>;
    --text-muted: <?= htmlspecialchars($textMuted) ?>;
    --border-light: <?= htmlspecialchars($borderLight) ?>;
    --card-bg: <?= htmlspecialchars($cardBg) ?>;
    --stats-number: <?= htmlspecialchars($statsNumberColor) ?>;
    --stats-label: <?= htmlspecialchars($statsLabelColor) ?>;
    --story-title: <?= htmlspecialchars($storyTitleColor) ?>;
    --cta-bg: <?= htmlspecialchars($ctaButtonBg) ?>;
    --cta-text: <?= htmlspecialchars($ctaButtonTextColor) ?>;
}

#<?= $instanceId ?> .section-inner {
    width: min(1180px, calc(100% - 1.2rem));
    margin: 0 auto;
}

#<?= $instanceId ?> .about-hero {
    padding: <?= $heroPadding ?>px 0;
    background: linear-gradient(140deg, var(--hero-bg), var(--hero-bg-end));
    position: relative;
    overflow: hidden;
}

#<?= $instanceId ?> .about-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
        radial-gradient(600px 220px at 10% 0%, rgba(83, 224, 197, 0.2), transparent 68%),
        radial-gradient(700px 260px at 90% 100%, rgba(255, 122, 74, 0.2), transparent 70%);
    pointer-events: none;
}

#<?= $instanceId ?> .about-hero .section-inner {
    position: relative;
    z-index: 1;
}

#<?= $instanceId ?> .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 260px;
    gap: 1rem;
    align-items: center;
}

#<?= $instanceId ?> .hero-copy {
    max-width: 760px;
}

#<?= $instanceId ?> .hero-kicker {
    margin: 0 0 0.8rem;
    width: fit-content;
    color: #53e0c5;
    border: 1px solid rgba(83, 224, 197, 0.36);
    background: rgba(83, 224, 197, 0.1);
    border-radius: 999px;
    padding: 0.3rem 0.72rem;
    font-size: 0.74rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    font-weight: 800;
}

#<?= $instanceId ?> .about-hero h1 {
    margin: 0;
    color: var(--hero-title);
    font-size: clamp(2rem, 5vw, 3.4rem);
    line-height: 1.1;
}

#<?= $instanceId ?> .about-hero p {
    margin: 1rem 0 0;
    max-width: 680px;
    color: var(--hero-text);
    line-height: 1.78;
}

#<?= $instanceId ?> .hero-pulse {
    margin-top: 1.15rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
}

#<?= $instanceId ?> .hero-pulse span {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.36rem 0.58rem;
    border-radius: 999px;
    border: 1px solid rgba(83, 224, 197, 0.3);
    background: rgba(83, 224, 197, 0.1);
    color: #9ef1df;
    font-size: 0.73rem;
    font-weight: 800;
    letter-spacing: 0.03em;
}

#<?= $instanceId ?> .hero-pulse span em {
    color: rgba(242, 221, 202, 0.84);
    font-style: normal;
    font-size: 0.68rem;
    font-weight: 600;
}

#<?= $instanceId ?> .hero-emblem {
    position: relative;
    width: 220px;
    height: 220px;
    margin-left: auto;
    border-radius: 50%;
    display: grid;
    place-items: center;
}

#<?= $instanceId ?> .emblem-ring {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    border: 1px dashed rgba(159, 239, 255, 0.34);
}

#<?= $instanceId ?> .emblem-ring.ring-b {
    inset: 16%;
    border-style: solid;
    border-color: rgba(255, 214, 168, 0.34);
}

#<?= $instanceId ?> .emblem-core {
    width: 104px;
    height: 104px;
    border-radius: 50%;
    border: 1px solid rgba(159, 239, 255, 0.42);
    background: linear-gradient(160deg, rgba(18, 11, 8, 0.78), rgba(33, 17, 11, 0.76));
    display: grid;
    place-content: center;
    text-align: center;
    gap: 0.16rem;
    box-shadow: 0 12px 22px rgba(0, 0, 0, 0.34);
}

#<?= $instanceId ?> .emblem-core small {
    color: #9ef1df;
    font-size: 0.62rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    font-weight: 800;
}

#<?= $instanceId ?> .emblem-core strong {
    color: #ffd6a8;
    font-size: 0.9rem;
    letter-spacing: 0.03em;
}

#<?= $instanceId ?> .about-stats {
    background: var(--section-bg-1);
    padding: 28px 0 10px;
}

#<?= $instanceId ?> .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 14px;
}

#<?= $instanceId ?> .stat-card {
    border: 1px solid var(--border-light);
    background: var(--card-bg);
    border-radius: 14px;
    padding: 18px 14px;
    text-align: center;
}

#<?= $instanceId ?> .stat-card h3 {
    margin: 0;
    color: var(--stats-number);
    font-size: clamp(1.1rem, 3vw, 2rem);
}

#<?= $instanceId ?> .stat-card p {
    margin: 0.45rem 0 0;
    color: var(--stats-label);
    font-size: 0.84rem;
    letter-spacing: 0.02em;
}

#<?= $instanceId ?> .about-story {
    background: var(--section-bg-1);
    padding: 42px 0 64px;
}

#<?= $instanceId ?> .story-shell {
    border: 1px solid var(--border-light);
    border-radius: 18px;
    background: rgba(18, 11, 8, 0.52);
    padding: clamp(1rem, 3vw, 2rem);
}

#<?= $instanceId ?> .about-story h2 {
    margin: 0;
    color: var(--story-title);
    text-align: center;
    font-size: clamp(1.5rem, 4vw, 2.3rem);
}

#<?= $instanceId ?> .story-subtitle {
    margin: 0.8rem 0 0;
    text-align: center;
    color: var(--text-muted);
}

#<?= $instanceId ?> .story-body {
    margin-top: 1.1rem;
    display: grid;
    gap: 0.85rem;
}

#<?= $instanceId ?> .story-body p {
    margin: 0;
    color: var(--text-secondary);
    line-height: 1.8;
}

#<?= $instanceId ?> .about-values {
    background: var(--section-bg-2);
    padding: 58px 0;
}

#<?= $instanceId ?> .about-values h2,
#<?= $instanceId ?> .about-team h2 {
    margin: 0;
    text-align: center;
    color: var(--hero-title);
    font-size: clamp(1.45rem, 4vw, 2.2rem);
}

#<?= $instanceId ?> .values-subtitle,
#<?= $instanceId ?> .team-subtitle {
    margin: 0.8rem auto 0;
    text-align: center;
    max-width: 740px;
    color: var(--text-muted);
    line-height: 1.7;
}

#<?= $instanceId ?> .values-grid {
    margin-top: 1.35rem;
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 16px;
}

#<?= $instanceId ?> .value-card {
    border: 1px solid var(--border-light);
    border-radius: 14px;
    background: var(--card-bg);
    padding: 18px;
}

#<?= $instanceId ?> .value-icon {
    width: 34px;
    height: 34px;
    border-radius: 10px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #53e0c5;
    border: 1px solid rgba(83, 224, 197, 0.36);
    background: rgba(83, 224, 197, 0.12);
    font-weight: 800;
}

#<?= $instanceId ?> .value-card h3 {
    margin: 0.7rem 0 0;
    color: var(--text-primary);
    font-size: 1.05rem;
}

#<?= $instanceId ?> .value-card p {
    margin: 0.5rem 0 0;
    color: var(--text-secondary);
    line-height: 1.7;
    font-size: 0.92rem;
}

#<?= $instanceId ?> .about-team {
    background: var(--section-bg-1);
    padding: 58px 0;
}

#<?= $instanceId ?> .team-grid {
    margin-top: 1.35rem;
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 14px;
}

#<?= $instanceId ?> .team-card {
    border: 1px solid var(--border-light);
    background: var(--card-bg);
    border-radius: 14px;
    padding: 16px 14px;
    text-align: center;
}

#<?= $instanceId ?> .team-avatar {
    width: 48px;
    height: 48px;
    margin: 0 auto;
    border-radius: 50%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #ffd6a8;
    border: 1px solid rgba(255, 214, 168, 0.4);
    background: rgba(255, 214, 168, 0.12);
    font-weight: 800;
    font-size: 1.05rem;
}

#<?= $instanceId ?> .team-card h3 {
    margin: 0.7rem 0 0;
    color: var(--text-primary);
    font-size: 1rem;
}

#<?= $instanceId ?> .team-card p {
    margin: 0.45rem 0 0;
    color: var(--text-muted);
    font-size: 0.86rem;
}

#<?= $instanceId ?> .about-cta {
    background: var(--section-bg-2);
    padding: 0 0 60px;
}

#<?= $instanceId ?> .cta-shell {
    border: 1px solid var(--border-light);
    border-radius: 16px;
    background: rgba(18, 11, 8, 0.66);
    padding: clamp(1rem, 3vw, 1.6rem);
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
}

#<?= $instanceId ?> .cta-shell h2 {
    margin: 0;
    color: var(--hero-title);
    font-size: clamp(1.3rem, 3vw, 1.9rem);
}

#<?= $instanceId ?> .cta-shell p {
    margin: 0.65rem 0 0;
    color: var(--text-secondary);
    line-height: 1.7;
}

#<?= $instanceId ?> .cta-button {
    text-decoration: none;
    color: var(--cta-text);
    background: var(--cta-bg);
    border-radius: 999px;
    padding: 12px 22px;
    font-weight: 800;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    font-size: 0.8rem;
    white-space: nowrap;
}

@media (max-width: 1024px) {
    #<?= $instanceId ?> .hero-grid {
        grid-template-columns: 1fr;
    }

    #<?= $instanceId ?> .hero-emblem {
        margin: 0;
    }

    #<?= $instanceId ?> .stats-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    #<?= $instanceId ?> .values-grid {
        grid-template-columns: 1fr;
    }

    #<?= $instanceId ?> .team-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    #<?= $instanceId ?> .cta-shell {
        flex-direction: column;
        align-items: flex-start;
    }
}

@media (max-width: 767px) {
    #<?= $instanceId ?> .section-inner {
        width: calc(100% - 1rem);
    }

    #<?= $instanceId ?> .about-hero {
        padding: 62px 0;
    }

    #<?= $instanceId ?> .hero-emblem {
        width: 170px;
        height: 170px;
    }

    #<?= $instanceId ?> .hero-pulse span {
        font-size: 0.68rem;
    }

    #<?= $instanceId ?> .stats-grid,
    #<?= $instanceId ?> .team-grid {
        grid-template-columns: 1fr;
    }

    #<?= $instanceId ?> .about-story,
    #<?= $instanceId ?> .about-values,
    #<?= $instanceId ?> .about-team {
        padding: 44px 0;
    }
}
</style>
