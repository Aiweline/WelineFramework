<?php
/**
 * Call Break Legend Banner Component
 *
 * 独立的 Banner 横幅组件，用于 content 区域
 *
 * @component_start
 * code: banner
 * name: Banner横幅
 * name_en: Banner
 * description: 首屏Banner横幅，支持响应式图片和下载按钮
 * category: content
 * region: content
 * thumbnail: asset/img/banner-03.svg
 * @component_end
 *
 * @fields_start
 *
 * group:banner => Banner设置
 * banner.display => 显示Banner:select:yes|yes,no
 * banner.image_mobile => 移动端图片:textarea:|默认使用本地banner-2.svg
 * banner.image_desktop => PC端图片:textarea:|默认使用本地banner-03.svg
 * banner.alt => 图片描述:text:Call Break Legend banner
 * banner.download_btn_image => 下载按钮图片:textarea:|默认使用download-btn.svg
 * banner.bg_color => 背景颜色:color:#0b1525
 * banner.bg_gradient_end => 背景渐变色结束:color:#0f2b3e
 * banner.image_offset_top => 图片向上偏移:number:90|px（用于与header重叠效果）
 * banner.image_offset_top_mobile => 移动端容器向上偏移:number:75|px
 *
 * @fields_end
 */

$componentId = 'cbl-banner-' . uniqid();

$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$config = array_merge($styleSettings, $componentConfig);

$getConfig = function($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

$bannerDisplay = $getConfig('banner.display', 'yes') !== 'no';
if (!$bannerDisplay) {
    return;
}

$defaultBannerMobile = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/callbreak-legend/asset/img/banner-2.svg');
$defaultBannerDesktop = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/callbreak-legend/asset/img/banner-03.svg');
$defaultDownloadBtn = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/callbreak-legend/asset/img/download-btn.svg');

$bannerMobile = $getConfig('banner.image_mobile', '') ?: $defaultBannerMobile;
$bannerDesktop = $getConfig('banner.image_desktop', '') ?: $defaultBannerDesktop;
$bannerAlt = $getConfig('banner.alt', 'Call Break Legend banner');
$bannerDownloadImg = $getConfig('banner.download_btn_image', '') ?: $defaultDownloadBtn;
$bannerBgColor = $getConfig('banner.bg_color', '#0b1525');
$bannerBgGradientEnd = $getConfig('banner.bg_gradient_end', '#0f2b3e');
$pokerBg = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/callbreak-legend/asset/img/poker-header-bg.svg');

$bannerImageOffsetTop = intval($getConfig('banner.image_offset_top', '90'));
$bannerImageOffsetTopMobile = intval($getConfig('banner.image_offset_top_mobile', '75'));
?>

<div id="<?= $componentId ?>" class="cbl-banner-component">
    <section class="cbl-banner">
        <div class="banner-shell">
            <div class="banner-stack">
                <span class="stack-label stack-label-left">Arena Cover</span>
                <span class="stack-label stack-label-right">Responsive Visual</span>

                <div class="stack-layer layer-back" aria-hidden="true"></div>
                <div class="stack-layer layer-mid" aria-hidden="true"></div>

                <div class="banner-screen">
                    <picture>
                        <source media="(max-width: 768px)" srcset="<?= htmlspecialchars($bannerMobile) ?>">
                        <source media="(min-width: 769px)" srcset="<?= htmlspecialchars($bannerDesktop) ?>">
                        <img class="banner-image"
                             src="<?= htmlspecialchars($bannerMobile) ?>"
                             alt="<?= htmlspecialchars($bannerAlt) ?>"
                             fetchpriority="high"
                             decoding="async">
                    </picture>
                </div>

                <?php if ($bannerDownloadImg): ?>
                    <div class="banner-pocket" aria-label="Download app">
                        <img src="<?= htmlspecialchars($bannerDownloadImg) ?>"
                             alt="DOWNLOAD APP"
                             width="210"
                             height="54">
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </section>
</div>

<style>
#<?= $componentId ?> .cbl-banner {
    position: relative;
    overflow: hidden;
    padding: clamp(68px, 10vw, 124px) 0;
    background:
        radial-gradient(560px 240px at 18% 14%, rgba(30, 185, 128, 0.14), transparent 72%),
        radial-gradient(620px 280px at 82% 22%, rgba(242, 169, 59, 0.12), transparent 72%),
        linear-gradient(180deg, <?= htmlspecialchars($bannerBgColor) ?> 0%, <?= htmlspecialchars($bannerBgGradientEnd) ?> 100%);
}

#<?= $componentId ?> .cbl-banner::before {
    content: '';
    position: absolute;
    inset: 0;
    background: url('<?= $pokerBg ?>') center top/cover no-repeat;
    opacity: 0.12;
    pointer-events: none;
}

#<?= $componentId ?> .cbl-banner::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
        radial-gradient(circle at 50% 0%, rgba(255, 255, 255, 0.08), transparent 30%),
        linear-gradient(180deg, rgba(255, 255, 255, 0.02) 0%, transparent 26%, transparent 74%, rgba(255, 255, 255, 0.02) 100%);
    pointer-events: none;
}

#<?= $componentId ?> .banner-shell {
    max-width: 1220px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
}

#<?= $componentId ?> .banner-stack {
    position: relative;
    max-width: 980px;
    margin: 0 auto;
    transform: translateY(calc(-1px * <?= (int)$bannerImageOffsetTop ?>));
}

#<?= $componentId ?> .stack-label {
    position: absolute;
    top: 16px;
    z-index: 4;
    display: inline-flex;
    align-items: center;
    padding: 0.38rem 0.78rem;
    border-radius: 999px;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    backdrop-filter: blur(8px);
}

#<?= $componentId ?> .stack-label-left {
    left: 20px;
    color: #d2fff0;
    background: rgba(30, 185, 128, 0.14);
    border: 1px solid rgba(30, 185, 128, 0.32);
}

#<?= $componentId ?> .stack-label-right {
    right: 20px;
    color: #f8fafc;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(122, 161, 192, 0.18);
}

#<?= $componentId ?> .stack-layer {
    position: absolute;
    inset: 0;
    border-radius: 30px;
    pointer-events: none;
}

#<?= $componentId ?> .layer-back {
    background: linear-gradient(160deg, rgba(11, 21, 37, 0.56) 0%, rgba(15, 43, 62, 0.36) 100%);
    border: 1px solid rgba(122, 161, 192, 0.16);
    transform: translate(18px, 18px) scale(0.985);
}

#<?= $componentId ?> .layer-mid {
    background: linear-gradient(160deg, rgba(11, 21, 37, 0.72) 0%, rgba(15, 43, 62, 0.46) 100%);
    border: 1px solid rgba(242, 169, 59, 0.12);
    transform: translate(8px, 8px) scale(0.992);
}

#<?= $componentId ?> .banner-screen {
    position: relative;
    z-index: 3;
    border-radius: 30px;
    overflow: hidden;
    border: 1px solid rgba(122, 161, 192, 0.2);
    background: linear-gradient(160deg, rgba(11, 21, 37, 0.9) 0%, rgba(15, 43, 62, 0.64) 100%);
    box-shadow: 0 32px 72px rgba(5, 8, 16, 0.52);
}

#<?= $componentId ?> .banner-screen::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 22%;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.08), transparent);
    pointer-events: none;
    z-index: 1;
}

#<?= $componentId ?> .banner-image {
    display: block;
    width: 100%;
    height: auto;
    position: relative;
    z-index: 0;
}

#<?= $componentId ?> .banner-pocket {
    position: absolute;
    left: 50%;
    bottom: -18px;
    z-index: 5;
    transform: translateX(-50%);
    padding: 0.65rem;
    border-radius: 18px;
    background: rgba(7, 16, 28, 0.78);
    border: 1px solid rgba(122, 161, 192, 0.16);
    box-shadow: 0 16px 32px rgba(2, 6, 12, 0.34);
}

#<?= $componentId ?> .banner-pocket img {
    display: block;
    filter: drop-shadow(0 12px 24px rgba(10, 6, 30, 0.42));
    animation: <?= $componentId ?>-pulse 2.5s ease-in-out infinite;
}

@keyframes <?= $componentId ?>-pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(0.95); }
}

@media (max-width: 991px) {
    #<?= $componentId ?> .cbl-banner {
        padding: 48px 0 70px;
    }

    #<?= $componentId ?> .banner-shell {
        transform: translateY(calc(-1px * <?= (int)$bannerImageOffsetTopMobile ?>));
    }

    #<?= $componentId ?> .banner-stack {
        transform: none;
    }

    #<?= $componentId ?> .stack-layer {
        border-radius: 22px;
    }

    #<?= $componentId ?> .banner-screen {
        border-radius: 22px;
    }
}

@media (max-width: 768px) {
    #<?= $componentId ?> .banner-shell {
        padding: 0 15px;
    }

    #<?= $componentId ?> .stack-label {
        top: 12px;
        font-size: 0.66rem;
        padding: 0.3rem 0.62rem;
    }

    #<?= $componentId ?> .stack-label-right {
        display: none;
    }

    #<?= $componentId ?> .layer-back {
        transform: translate(12px, 12px) scale(0.985);
    }

    #<?= $componentId ?> .layer-mid {
        transform: translate(5px, 5px) scale(0.992);
    }

    #<?= $componentId ?> .banner-pocket {
        bottom: -14px;
        padding: 0.52rem;
    }
}

@media (prefers-reduced-motion: reduce) {
    #<?= $componentId ?> .banner-pocket img {
        animation: none;
    }
}
</style>
