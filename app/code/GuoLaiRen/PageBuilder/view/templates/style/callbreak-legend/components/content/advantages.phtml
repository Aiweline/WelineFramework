<?php
/**
 * @component_start
 * code: advantages
 * name: 优势展示
 * name_en: Advantages
 * description: 展示产品核心优势
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_section => 显示此区域:select:yes|yes,no
 * display.show_title => 显示标题:select:yes|yes,no
 *
 * group:style => 样式配置
 * style.bg_color => 背景颜色:color:#0b1525
 * style.title_color => 标题颜色:color:#f2a93b
 * style.card_bg_color => 卡片背景色:color:rgba(11,21,37,0.7)
 * style.card_border_color => 卡片边框色:color:rgba(30,185,128,0.18)
 * style.card_title_color => 卡片标题颜色:color:#e9f5f2
 * style.card_desc_color => 卡片描述颜色:color:#cbd5e1
 * style.section_padding => 区域内边距(PC端):number:80|px
 * style.section_padding_mobile => 区域内边距(移动端):number:48|px
 * style.card_gap => 卡片间距:number:24|px
 *
 * group:texts => 文本内容
 * texts.kicker => 辅助标签:text:TACTICAL ADVANTAGES
 * texts.title => 区域标题:text:Why Choose Call Break Legend
 * texts.summary => 区域说明:textarea:Built for players who win by reads, rhythm, and disciplined bidding.
 * texts.primary_label => 主卡辅助标签:text:Core Edge
 * texts.advantage_1_icon => 优势1图标:text:🃏
 * texts.advantage_1_title => 优势1标题:text:True Call Break Rules
 * texts.advantage_1_desc => 优势1描述:textarea:Standard trick-taking rules with smooth rounds and instant shuffles.
 * texts.advantage_2_icon => 优势2图标:text:🏆
 * texts.advantage_2_title => 优势2标题:text:Skill-Based Tables
 * texts.advantage_2_desc => 优势2描述:textarea:Balanced matchmaking, ranked ladders, and daily challenges.
 * texts.advantage_3_icon => 优势3图标:text:🔒
 * texts.advantage_3_title => 优势3标题:text:Secure & Transparent
 * texts.advantage_3_desc => 优势3描述:textarea:Anti-fraud protection, safe payments, and verified fair play.
 * texts.signal_1_label => 指标1标签:text:Round Pace
 * texts.signal_1_value => 指标1内容:text:8-12 min
 * texts.signal_2_label => 指标2标签:text:Skill Ladder
 * texts.signal_2_value => 指标2内容:text:Realtime
 * texts.signal_3_label => 指标3标签:text:Table Access
 * texts.signal_3_value => 指标3内容:text:24/7
 *
 * @fields_end
 */

$componentId = 'cbl-advantages-' . uniqid();

$page = $this->getData('page');
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$config = array_merge($styleSettings, $componentConfig);

$getConfig = function($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

$display = $getConfig('display.show_section', $getConfig('advantages.display', 'yes')) !== 'no';
if (!$display) return;
$showTitle = $getConfig('display.show_title', 'yes') !== 'no';

$bgColor = $getConfig('style.bg_color', '#0b1525');
$titleColor = $getConfig('style.title_color', '#f2a93b');
$cardBgColor = $getConfig('style.card_bg_color', 'rgba(11,21,37,0.7)');
$cardBorderColor = $getConfig('style.card_border_color', 'rgba(30,185,128,0.18)');
$cardTitleColor = $getConfig('style.card_title_color', '#e9f5f2');
$cardDescColor = $getConfig('style.card_desc_color', '#cbd5e1');
$sectionPadding = $getConfig('style.section_padding', '80');
$sectionPaddingMobile = $getConfig('style.section_padding_mobile', '48');
$cardGap = $getConfig('style.card_gap', '24');

$kicker = $getConfig('texts.kicker', 'TACTICAL ADVANTAGES');
$title = $getConfig('texts.title', 'Why Choose Call Break Legend');
$summary = $getConfig('texts.summary', 'Built for players who win by reads, rhythm, and disciplined bidding.');
$primaryLabel = $getConfig('texts.primary_label', 'Core Edge');
$items = [
    [
        'icon' => $getConfig('texts.advantage_1_icon', '🃏'),
        'title' => $getConfig('texts.advantage_1_title', 'True Call Break Rules'),
        'desc' => $getConfig('texts.advantage_1_desc', 'Standard trick-taking rules with smooth rounds and instant shuffles.'),
    ],
    [
        'icon' => $getConfig('texts.advantage_2_icon', '🏆'),
        'title' => $getConfig('texts.advantage_2_title', 'Skill-Based Tables'),
        'desc' => $getConfig('texts.advantage_2_desc', 'Balanced matchmaking, ranked ladders, and daily challenges.'),
    ],
    [
        'icon' => $getConfig('texts.advantage_3_icon', '🔒'),
        'title' => $getConfig('texts.advantage_3_title', 'Secure & Transparent'),
        'desc' => $getConfig('texts.advantage_3_desc', 'Anti-fraud protection, safe payments, and verified fair play.'),
    ],
];

$primary = $items[0] ?? ['icon' => '🃏', 'title' => '', 'desc' => ''];
$secondary = array_slice($items, 1);
$signals = [
    ['label' => $getConfig('texts.signal_1_label', 'Round Pace'), 'value' => $getConfig('texts.signal_1_value', '8-12 min')],
    ['label' => $getConfig('texts.signal_2_label', 'Skill Ladder'), 'value' => $getConfig('texts.signal_2_value', 'Realtime')],
    ['label' => $getConfig('texts.signal_3_label', 'Table Access'), 'value' => $getConfig('texts.signal_3_value', '24/7')],
];
?>

<section id="<?= $componentId ?>" class="cbl-advantages">
    <div class="adv-shell">
        <?php if ($showTitle): ?>
            <header class="adv-header">
                <?php if ($kicker !== ''): ?>
                    <span class="adv-kicker"><?= htmlspecialchars($kicker) ?></span>
                <?php endif; ?>
                <h2 class="adv-title"><?= htmlspecialchars($title) ?></h2>
                <?php if ($summary !== ''): ?>
                    <p class="adv-subtitle"><?= htmlspecialchars($summary) ?></p>
                <?php endif; ?>
            </header>
        <?php endif; ?>

        <div class="adv-grid" style="--adv-gap: <?= htmlspecialchars($cardGap) ?>px;">
            <article class="adv-highlight">
                <?php if ($primaryLabel !== ''): ?>
                    <span class="adv-highlight-chip"><?= htmlspecialchars($primaryLabel) ?></span>
                <?php endif; ?>
                <div class="adv-highlight-icon"><?= htmlspecialchars($primary['icon']) ?></div>
                <h3><?= htmlspecialchars($primary['title']) ?></h3>
                <p><?= htmlspecialchars($primary['desc']) ?></p>
            </article>

            <div class="adv-track">
                <?php foreach ($secondary as $index => $item): ?>
                    <article class="adv-step">
                        <div class="adv-step-index"><?= sprintf('%02d', $index + 2) ?></div>
                        <div class="adv-step-body">
                            <div class="adv-step-top">
                                <span class="adv-step-icon"><?= htmlspecialchars($item['icon']) ?></span>
                                <h3><?= htmlspecialchars($item['title']) ?></h3>
                            </div>
                            <p><?= htmlspecialchars($item['desc']) ?></p>
                        </div>
                    </article>
                <?php endforeach; ?>
            </div>
        </div>

        <div class="adv-signal-row">
            <?php foreach ($signals as $signal): ?>
                <div class="adv-signal-card">
                    <span class="signal-label"><?= htmlspecialchars($signal['label']) ?></span>
                    <strong><?= htmlspecialchars($signal['value']) ?></strong>
                </div>
            <?php endforeach; ?>
        </div>
    </div>
</section>

<style>
#<?= $componentId ?> {
    --adv-bg: <?= htmlspecialchars($bgColor) ?>;
    --adv-title: <?= htmlspecialchars($titleColor) ?>;
    --adv-card-bg: <?= htmlspecialchars($cardBgColor) ?>;
    --adv-card-border: <?= htmlspecialchars($cardBorderColor) ?>;
    --adv-card-title: <?= htmlspecialchars($cardTitleColor) ?>;
    --adv-card-desc: <?= htmlspecialchars($cardDescColor) ?>;
    position: relative;
    padding: <?= htmlspecialchars($sectionPadding) ?>px 0;
    background:
        radial-gradient(680px 300px at 8% 8%, rgba(30, 185, 128, 0.18), transparent 72%),
        radial-gradient(620px 280px at 92% 20%, rgba(242, 169, 59, 0.14), transparent 70%),
        linear-gradient(180deg, var(--adv-bg) 0%, #0f2538 48%, #0b1525 100%);
    overflow: hidden;
}

#<?= $componentId ?>::before {
    content: '';
    position: absolute;
    inset: auto -8% -140px auto;
    width: min(320px, 42vw);
    aspect-ratio: 1 / 1;
    border-radius: 999px;
    border: 1px solid rgba(30, 185, 128, 0.28);
    opacity: 0.65;
}

#<?= $componentId ?> .adv-shell {
    width: min(1200px, calc(100% - 2rem));
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

#<?= $componentId ?> .adv-header {
    max-width: 760px;
    text-align: center;
    margin: 0 auto 2rem;
}

#<?= $componentId ?> .adv-kicker {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    padding: 0.38rem 0.9rem;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #0b1525;
    background: linear-gradient(120deg, #1eb980, #f2a93b);
}

#<?= $componentId ?> .adv-title {
    margin-top: 1rem;
    font-size: clamp(1.9rem, 4.7vw, 3.2rem);
    line-height: 1.1;
    color: var(--adv-title);
    text-shadow: 0 12px 30px rgba(2, 6, 12, 0.42);
}

#<?= $componentId ?> .adv-subtitle {
    margin-top: 0.9rem;
    color: rgba(233, 245, 242, 0.84);
    font-size: clamp(0.98rem, 1.65vw, 1.12rem);
    line-height: 1.7;
}

#<?= $componentId ?> .adv-grid {
    display: grid;
    gap: var(--adv-gap);
    grid-template-columns: minmax(0, 1.08fr) minmax(0, 0.92fr);
}

#<?= $componentId ?> .adv-highlight {
    position: relative;
    border-radius: 26px;
    border: 1px solid var(--adv-card-border);
    background: linear-gradient(160deg, rgba(11, 21, 37, 0.74) 0%, var(--adv-card-bg) 100%);
    padding: clamp(1.35rem, 3.1vw, 2rem);
    box-shadow: 0 24px 50px rgba(2, 6, 12, 0.45);
    overflow: hidden;
}

#<?= $componentId ?> .adv-highlight::after {
    content: '';
    position: absolute;
    width: 220px;
    height: 220px;
    right: -90px;
    top: -90px;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(30, 185, 128, 0.22) 0%, transparent 70%);
}

#<?= $componentId ?> .adv-highlight-chip {
    display: inline-flex;
    align-items: center;
    border-radius: 999px;
    border: 1px solid rgba(30, 185, 128, 0.5);
    background: rgba(30, 185, 128, 0.14);
    color: #b6f8e5;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 0.32rem 0.75rem;
}

#<?= $componentId ?> .adv-highlight-icon {
    margin-top: 1.05rem;
    font-size: clamp(2rem, 5vw, 3rem);
    line-height: 1;
}

#<?= $componentId ?> .adv-highlight h3 {
    margin: 0.85rem 0 0;
    color: var(--adv-card-title);
    font-size: clamp(1.2rem, 2.1vw, 1.72rem);
    line-height: 1.25;
}

#<?= $componentId ?> .adv-highlight p {
    margin: 0.85rem 0 0;
    color: var(--adv-card-desc);
    line-height: 1.75;
    font-size: 0.97rem;
    max-width: 42ch;
}

#<?= $componentId ?> .adv-track {
    display: grid;
    gap: var(--adv-gap);
}

#<?= $componentId ?> .adv-step {
    border-radius: 20px;
    border: 1px solid var(--adv-card-border);
    background: linear-gradient(150deg, rgba(15, 43, 62, 0.65) 0%, rgba(11, 21, 37, 0.88) 100%);
    padding: 1.1rem;
    display: grid;
    grid-template-columns: 54px minmax(0, 1fr);
    gap: 0.8rem;
    align-items: start;
    box-shadow: 0 14px 34px rgba(2, 6, 12, 0.34);
}

#<?= $componentId ?> .adv-step-index {
    width: 54px;
    height: 54px;
    border-radius: 14px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 0.95rem;
    font-weight: 800;
    color: #0b1525;
    background: linear-gradient(145deg, #f2a93b, #ffd88f);
}

#<?= $componentId ?> .adv-step-top {
    display: flex;
    align-items: center;
    gap: 0.6rem;
}

#<?= $componentId ?> .adv-step-icon {
    font-size: 1.25rem;
    line-height: 1;
}

#<?= $componentId ?> .adv-step h3 {
    margin: 0;
    font-size: 1.02rem;
    color: var(--adv-card-title);
    line-height: 1.3;
}

#<?= $componentId ?> .adv-step p {
    margin: 0.55rem 0 0;
    color: var(--adv-card-desc);
    line-height: 1.65;
    font-size: 0.92rem;
}

#<?= $componentId ?> .adv-signal-row {
    margin-top: 1.15rem;
    display: grid;
    gap: 0.75rem;
    grid-template-columns: repeat(3, minmax(0, 1fr));
}

#<?= $componentId ?> .adv-signal-card {
    border-radius: 14px;
    border: 1px solid rgba(233, 245, 242, 0.12);
    background: rgba(11, 21, 37, 0.5);
    padding: 0.8rem 0.95rem;
    text-align: center;
}

#<?= $componentId ?> .signal-label {
    display: block;
    color: rgba(203, 213, 225, 0.88);
    font-size: 0.76rem;
}

#<?= $componentId ?> .adv-signal-card strong {
    display: block;
    margin-top: 0.15rem;
    color: #f2a93b;
    font-size: 1.02rem;
    line-height: 1.2;
}

@media (max-width: 980px) {
    #<?= $componentId ?> .adv-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 768px) {
    #<?= $componentId ?> {
        padding: <?= htmlspecialchars($sectionPaddingMobile) ?>px 0;
    }

    #<?= $componentId ?> .adv-shell {
        width: calc(100% - 1rem);
    }

    #<?= $componentId ?> .adv-signal-row {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    #<?= $componentId ?> .adv-signal-card:last-child {
        grid-column: 1 / -1;
    }
}

@media (max-width: 480px) {
    #<?= $componentId ?> .adv-step {
        grid-template-columns: 42px minmax(0, 1fr);
        padding: 0.95rem;
    }

    #<?= $componentId ?> .adv-step-index {
        width: 42px;
        height: 42px;
        border-radius: 11px;
        font-size: 0.82rem;
    }
}
</style>
