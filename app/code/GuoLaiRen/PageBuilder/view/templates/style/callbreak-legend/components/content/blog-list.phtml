<?php
/**
 * Call Break Legend Blog List Component
 *
 * @fields_start
 * blog.title|博客列表标题|text|Latest Blog Posts
 * blog.subtitle|副标题|text|Stay updated with our latest news and articles
 * blog.posts_per_page|每页显示数量|number|9
 * blog.show_sidebar|显示侧边栏|select:yes=是,no=否|yes
 * blog.show_category|显示分类|select:yes=是,no=否|yes
 * blog.show_date|显示日期|select:yes=是,no=否|yes
 * blog.show_author|显示作者|select:yes=是,no=否|yes
 * blog.card_style|卡片样式|select:default=默认,minimal=简约,featured=特色|default
 * @fields_end
 */

$style = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$blogPosts = $this->getData('blog_posts') ?: [];
$blogCategories = $this->getData('blog_categories') ?: [];
$recentPosts = $this->getData('recent_posts') ?: [];

$getConfig = function($key, $default = '') use ($style, $componentConfig) {
    if (isset($componentConfig[$key]) && $componentConfig[$key] !== '') {
        return $componentConfig[$key];
    }
    return $style[$key] ?? $default;
};

$title = $getConfig('blog.title', __('Latest Blog Posts'));
$subtitle = $getConfig('blog.subtitle', __('Stay updated with our latest news and articles'));
$showSidebar = $getConfig('blog.show_sidebar', 'yes') === 'yes';
$showCategory = $getConfig('blog.show_category', 'yes') === 'yes';
$showDate = $getConfig('blog.show_date', 'yes') === 'yes';
$showAuthor = $getConfig('blog.show_author', 'yes') === 'yes';
$cardStyle = $getConfig('blog.card_style', 'default');

$primaryColor = $getConfig('color_scheme.primary', '#1eb980');
$secondaryColor = $getConfig('color_scheme.secondary', '#f2a93b');
$bgColor = $getConfig('color_scheme.background', '#0b1525');
$textColor = $getConfig('color_scheme.text', '#f8fafc');
$pokerBg = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/callbreak-legend/asset/img/poker-header-bg.svg');

$featuredPost = $blogPosts[0] ?? null;
$gridPosts = array_slice($blogPosts, 1);
$cardStyleClass = 'style-' . preg_replace('/[^a-z0-9-]+/i', '', (string)$cardStyle);
?>

<section class="cbl-blog-list" id="blog">
    <div class="blog-shell">
        <header class="blog-masthead">
            <span class="masthead-kicker">Editorial Dispatch</span>
            <h2><?= htmlspecialchars($title) ?></h2>
            <?php if ($subtitle): ?>
                <p><?= htmlspecialchars($subtitle) ?></p>
            <?php endif; ?>
        </header>

        <?php if (empty($blogPosts)): ?>
            <div class="blog-empty">
                <div class="empty-icon">
                    <svg width="64" height="64" viewBox="0 0 24 24" fill="currentColor" opacity="0.3">
                        <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-5 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"/>
                    </svg>
                </div>
                <p><?= __('There are no blog posts yet. Please check back later!') ?></p>
            </div>
        <?php else: ?>
            <?php if ($featuredPost): ?>
                <article class="featured-story">
                    <?php if (!empty($featuredPost['cover_image'])): ?>
                        <a href="<?= htmlspecialchars($featuredPost['url']) ?>" class="featured-media">
                            <img src="<?= htmlspecialchars($featuredPost['cover_image']) ?>" alt="<?= htmlspecialchars($featuredPost['title']) ?>" loading="lazy">
                        </a>
                    <?php endif; ?>

                    <div class="featured-copy">
                        <div class="featured-meta">
                            <?php if ($showCategory && !empty($featuredPost['category_name'])): ?>
                                <span class="story-chip"><?= htmlspecialchars($featuredPost['category_name']) ?></span>
                            <?php endif; ?>
                            <?php if ($showDate && !empty($featuredPost['published_at'])): ?>
                                <span class="story-date"><?= date('M d, Y', strtotime($featuredPost['published_at'])) ?></span>
                            <?php endif; ?>
                        </div>

                        <h3>
                            <a href="<?= htmlspecialchars($featuredPost['url']) ?>">
                                <?= htmlspecialchars($featuredPost['title']) ?>
                            </a>
                        </h3>

                        <?php if (!empty($featuredPost['summary'])): ?>
                            <p class="featured-summary"><?= htmlspecialchars(mb_substr($featuredPost['summary'], 0, 220)) ?><?= mb_strlen($featuredPost['summary']) > 220 ? '...' : '' ?></p>
                        <?php endif; ?>

                        <div class="featured-footer">
                            <?php if ($showAuthor && !empty($featuredPost['author'])): ?>
                                <span class="story-author"><?= htmlspecialchars($featuredPost['author']) ?></span>
                            <?php endif; ?>
                            <a href="<?= htmlspecialchars($featuredPost['url']) ?>" class="story-link">
                                <?= __('Read Story') ?>
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                    <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                </svg>
                            </a>
                        </div>
                    </div>
                </article>
            <?php endif; ?>

            <div class="news-deck<?= $showSidebar ? ' has-rail' : '' ?>">
                <div class="story-grid <?= htmlspecialchars($cardStyleClass) ?>">
                    <?php foreach ($gridPosts as $index => $post): ?>
                        <?php $cardExtraClass = ($cardStyle === 'featured' && $index === 0) ? ' is-wide' : ''; ?>
                        <article class="story-card<?= $cardExtraClass ?>">
                            <?php if (!empty($post['cover_image'])): ?>
                                <a href="<?= htmlspecialchars($post['url']) ?>" class="story-cover">
                                    <img src="<?= htmlspecialchars($post['cover_image']) ?>" alt="<?= htmlspecialchars($post['title']) ?>" loading="lazy">
                                </a>
                            <?php endif; ?>

                            <div class="story-body">
                                <div class="story-topline">
                                    <?php if ($showCategory && !empty($post['category_name'])): ?>
                                        <span class="story-chip"><?= htmlspecialchars($post['category_name']) ?></span>
                                    <?php endif; ?>
                                    <?php if ($showDate && !empty($post['published_at'])): ?>
                                        <span class="story-date"><?= date('M d, Y', strtotime($post['published_at'])) ?></span>
                                    <?php endif; ?>
                                </div>

                                <h3 class="story-title">
                                    <a href="<?= htmlspecialchars($post['url']) ?>"><?= htmlspecialchars($post['title']) ?></a>
                                </h3>

                                <?php if ($cardStyle !== 'minimal' && !empty($post['summary'])): ?>
                                    <p class="story-summary">
                                        <?= htmlspecialchars(mb_substr($post['summary'], 0, 140)) ?><?= mb_strlen($post['summary']) > 140 ? '...' : '' ?>
                                    </p>
                                <?php endif; ?>

                                <div class="story-foot">
                                    <?php if ($showAuthor && !empty($post['author'])): ?>
                                        <span class="story-author"><?= htmlspecialchars($post['author']) ?></span>
                                    <?php endif; ?>
                                    <a href="<?= htmlspecialchars($post['url']) ?>" class="story-link">
                                        <?= __('Read More') ?>
                                        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                            <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                        </svg>
                                    </a>
                                </div>
                            </div>
                        </article>
                    <?php endforeach; ?>
                </div>

                <?php if ($showSidebar): ?>
                    <aside class="signal-rail">
                        <?php if (!empty($blogCategories)): ?>
                            <section class="rail-card">
                                <h4>Categories</h4>
                                <div class="category-cloud">
                                    <?php foreach ($blogCategories as $category): ?>
                                        <a href="<?= htmlspecialchars($category['url']) ?>" class="category-pill"><?= htmlspecialchars($category['name']) ?></a>
                                    <?php endforeach; ?>
                                </div>
                            </section>
                        <?php endif; ?>

                        <?php if (!empty($recentPosts)): ?>
                            <section class="rail-card">
                                <h4>Recent Posts</h4>
                                <div class="recent-stack">
                                    <?php foreach (array_slice($recentPosts, 0, 5) as $post): ?>
                                        <a href="<?= htmlspecialchars($post['url']) ?>" class="recent-link">
                                            <?php if (!empty($post['cover_image'])): ?>
                                                <img src="<?= htmlspecialchars($post['cover_image']) ?>" alt="<?= htmlspecialchars($post['title']) ?>" loading="lazy">
                                            <?php endif; ?>
                                            <span>
                                                <strong><?= htmlspecialchars($post['title']) ?></strong>
                                                <?php if (!empty($post['published_at'])): ?>
                                                    <small><?= date('M d, Y', strtotime($post['published_at'])) ?></small>
                                                <?php endif; ?>
                                            </span>
                                        </a>
                                    <?php endforeach; ?>
                                </div>
                            </section>
                        <?php endif; ?>
                    </aside>
                <?php endif; ?>
            </div>
        <?php endif; ?>
    </div>
</section>

<style>
.cbl-blog-list {
    position: relative;
    padding: 84px 0;
    background:
        radial-gradient(1200px 600px at 15% 10%, rgba(30, 185, 128, 0.22), transparent 60%),
        radial-gradient(800px 500px at 85% 20%, rgba(242, 169, 59, 0.16), transparent 55%),
        <?= htmlspecialchars($bgColor) ?>;
    color: <?= htmlspecialchars($textColor) ?>;
    overflow: hidden;
}

.cbl-blog-list::before {
    content: '';
    position: absolute;
    inset: 0;
    background: url('<?= $pokerBg ?>') center top/cover no-repeat;
    opacity: 0.1;
    pointer-events: none;
}

.cbl-blog-list::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(7, 10, 19, 0.25) 0%, rgba(7, 10, 19, 0.65) 100%);
    pointer-events: none;
}

.cbl-blog-list .blog-shell {
    max-width: 1220px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
}

.cbl-blog-list .blog-masthead {
    max-width: 760px;
    margin-bottom: 1rem;
}

.cbl-blog-list .masthead-kicker {
    display: inline-flex;
    align-items: center;
    padding: 0.38rem 0.82rem;
    border-radius: 999px;
    border: 1px solid rgba(30, 185, 128, 0.3);
    background: rgba(30, 185, 128, 0.12);
    color: #d2fff0;
    font-size: 0.74rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.cbl-blog-list .blog-masthead h2 {
    margin-top: 1rem;
    font-size: clamp(2.1rem, 4.2vw, 3.4rem);
    line-height: 1.04;
    letter-spacing: -0.04em;
    color: #f8fafc;
}

.cbl-blog-list .blog-masthead p {
    margin-top: 0.85rem;
    color: rgba(233, 245, 242, 0.82);
    line-height: 1.78;
}

.cbl-blog-list .featured-story,
.cbl-blog-list .story-card,
.cbl-blog-list .rail-card,
.cbl-blog-list .blog-empty {
    border-radius: 24px;
    border: 1px solid rgba(122, 161, 192, 0.2);
    background: linear-gradient(160deg, rgba(11, 21, 37, 0.9) 0%, rgba(15, 43, 62, 0.62) 100%);
    box-shadow: 0 22px 52px rgba(2, 6, 12, 0.4);
}

.cbl-blog-list .featured-story {
    display: grid;
    grid-template-columns: minmax(0, 1.02fr) minmax(320px, 0.98fr);
    gap: 1rem;
    overflow: hidden;
    margin-bottom: 1rem;
}

.cbl-blog-list .featured-media {
    min-height: 100%;
    position: relative;
    display: block;
    background: rgba(5, 16, 30, 0.4);
}

.cbl-blog-list .featured-media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

.cbl-blog-list .featured-copy {
    padding: clamp(1.3rem, 3vw, 2rem);
    display: grid;
    align-content: center;
}

.cbl-blog-list .featured-meta,
.cbl-blog-list .story-topline {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    align-items: center;
}

.cbl-blog-list .story-chip,
.cbl-blog-list .story-date {
    display: inline-flex;
    align-items: center;
    padding: 0.32rem 0.72rem;
    border-radius: 999px;
    font-size: 0.74rem;
    font-weight: 700;
}

.cbl-blog-list .story-chip {
    background: linear-gradient(120deg, <?= htmlspecialchars($primaryColor) ?> 0%, <?= htmlspecialchars($secondaryColor) ?> 100%);
    color: #0b1525;
}

.cbl-blog-list .story-date {
    color: rgba(233, 245, 242, 0.78);
    border: 1px solid rgba(122, 161, 192, 0.18);
    background: rgba(255, 255, 255, 0.04);
}

.cbl-blog-list .featured-copy h3 {
    margin-top: 1rem;
    font-size: clamp(1.8rem, 3vw, 2.8rem);
    line-height: 1.08;
}

.cbl-blog-list .featured-copy h3 a,
.cbl-blog-list .story-title a {
    color: #f8fafc;
    text-decoration: none;
    transition: color 0.25s ease;
}

.cbl-blog-list .featured-copy h3 a:hover,
.cbl-blog-list .story-title a:hover {
    color: <?= htmlspecialchars($secondaryColor) ?>;
}

.cbl-blog-list .featured-summary,
.cbl-blog-list .story-summary {
    margin-top: 0.9rem;
    color: rgba(233, 245, 242, 0.78);
    line-height: 1.74;
}

.cbl-blog-list .featured-footer,
.cbl-blog-list .story-foot {
    margin-top: 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.85rem;
    flex-wrap: wrap;
}

.cbl-blog-list .story-author {
    color: rgba(203, 213, 225, 0.74);
    font-size: 0.88rem;
}

.cbl-blog-list .story-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: <?= htmlspecialchars($primaryColor) ?>;
    text-decoration: none;
    font-weight: 700;
    transition: gap 0.25s ease, color 0.25s ease;
}

.cbl-blog-list .story-link:hover {
    gap: 0.72rem;
    color: <?= htmlspecialchars($secondaryColor) ?>;
}

.cbl-blog-list .news-deck {
    display: grid;
    gap: 1rem;
}

.cbl-blog-list .news-deck.has-rail {
    grid-template-columns: minmax(0, 1fr) minmax(260px, 0.3fr);
    align-items: start;
}

.cbl-blog-list .story-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
}

.cbl-blog-list .story-card {
    overflow: hidden;
}

.cbl-blog-list .story-grid.style-featured .story-card.is-wide {
    grid-column: span 2;
}

.cbl-blog-list .story-cover {
    position: relative;
    display: block;
    padding-top: 60%;
    overflow: hidden;
}

.cbl-blog-list .story-cover img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.35s ease;
}

.cbl-blog-list .story-card:hover .story-cover img {
    transform: scale(1.05);
}

.cbl-blog-list .story-body {
    padding: 1.15rem;
}

.cbl-blog-list .story-title {
    margin-top: 0.85rem;
    font-size: 1.2rem;
    line-height: 1.35;
}

.cbl-blog-list .story-grid.style-minimal .story-summary {
    display: none;
}

.cbl-blog-list .signal-rail {
    display: grid;
    gap: 1rem;
    position: sticky;
    top: 110px;
}

.cbl-blog-list .rail-card {
    padding: 1.1rem;
}

.cbl-blog-list .rail-card h4 {
    color: #f8fafc;
    font-size: 0.9rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.cbl-blog-list .category-cloud {
    margin-top: 0.85rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
}

.cbl-blog-list .category-pill {
    display: inline-flex;
    align-items: center;
    padding: 0.42rem 0.8rem;
    border-radius: 999px;
    border: 1px solid rgba(30, 185, 128, 0.24);
    background: rgba(30, 185, 128, 0.08);
    color: rgba(233, 245, 242, 0.84);
    text-decoration: none;
    transition: background 0.25s ease, color 0.25s ease, transform 0.25s ease;
}

.cbl-blog-list .category-pill:hover {
    background: rgba(242, 169, 59, 0.14);
    color: #f8fafc;
    transform: translateY(-2px);
}

.cbl-blog-list .recent-stack {
    margin-top: 0.85rem;
    display: grid;
    gap: 0.75rem;
}

.cbl-blog-list .recent-link {
    display: grid;
    grid-template-columns: 64px minmax(0, 1fr);
    gap: 0.75rem;
    text-decoration: none;
    color: inherit;
}

.cbl-blog-list .recent-link img {
    width: 64px;
    height: 64px;
    border-radius: 12px;
    object-fit: cover;
}

.cbl-blog-list .recent-link span {
    display: grid;
    gap: 0.28rem;
}

.cbl-blog-list .recent-link strong {
    color: #f8fafc;
    font-size: 0.92rem;
    line-height: 1.45;
}

.cbl-blog-list .recent-link small {
    color: rgba(203, 213, 225, 0.68);
    font-size: 0.76rem;
}

.cbl-blog-list .blog-empty {
    text-align: center;
    padding: 4rem 1.5rem;
}

.cbl-blog-list .empty-icon {
    margin-bottom: 1rem;
}

@media (max-width: 1080px) {
    .cbl-blog-list .featured-story,
    .cbl-blog-list .news-deck.has-rail {
        grid-template-columns: 1fr;
    }

    .cbl-blog-list .signal-rail {
        position: static;
    }
}

@media (max-width: 768px) {
    .cbl-blog-list {
        padding: 64px 0;
    }

    .cbl-blog-list .blog-shell {
        padding: 0 15px;
    }

    .cbl-blog-list .story-grid {
        grid-template-columns: 1fr;
    }

    .cbl-blog-list .story-grid.style-featured .story-card.is-wide {
        grid-column: auto;
    }
}
</style>
