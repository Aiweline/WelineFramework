<?php
/**
 * Call Break Legend - Hero Slider Component
 *
 * 战术牌桌风Hero区域，强调策略节奏和竞技氛围
 *
 * @component_start
 * code: hero-slider
 * name: Hero轮播区
 * name_en: Hero Slider
 * description: 全屏视差效果，动态渐变背景
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:hero => Hero设置
 * hero.label => 辅助标签:text:Call Break Playbook
 * hero.title => 主标题:text:Master the Tricks in Call Break Legend
 * hero.subtitle => 副标题:textarea:Predict every hand, win every trick. India’s beloved trick-taking battle, now ultra-fast.
 * hero.cta_text => CTA按钮文字:text:Get the App
 * hero.cta_url => CTA按钮链接:text:#download
 * hero.secondary_cta_text => 次按钮文字:text:Watch Rules
 * hero.secondary_cta_url => 次按钮链接:text:#howto
 * hero.stat_1_value => 数据1数值:text:4P
 * hero.stat_1_label => 数据1标签:text:Live Table Battle
 * hero.stat_2_value => 数据2数值:text:13
 * hero.stat_2_label => 数据2标签:text:Tricks Each Round
 * hero.stat_3_value => 数据3数值:text:A+K
 * hero.stat_3_label => 数据3标签:text:Trump Pressure
 * hero.seat_north_label => 北位标签:text:NORTH
 * hero.seat_north_bid => 北位叫分:text:Bid 4
 * hero.seat_east_label => 东位标签:text:EAST
 * hero.seat_east_bid => 东位叫分:text:Bid 3
 * hero.seat_south_label => 南位标签:text:YOU
 * hero.seat_south_bid => 南位叫分:text:Bid 5
 * hero.seat_west_label => 西位标签:text:WEST
 * hero.seat_west_bid => 西位叫分:text:Bid 1
 * hero.core_badge => 中心徽章:text:Trump Ready
 * hero.step_1_title => 步骤1标题:text:Read the Table
 * hero.step_1_desc => 步骤1描述:textarea:Track bids fast and predict weak suits before the first break.
 * hero.step_2_title => 步骤2标题:text:Control Trump Timing
 * hero.step_2_desc => 步骤2描述:textarea:Delay your strongest cards until rivals expose their anchors.
 * hero.step_3_title => 步骤3标题:text:Close with Safe Exits
 * hero.step_3_desc => 步骤3描述:textarea:Finish rounds with low-risk winners and protect your lead.
 * hero.image => 左侧图片:textarea:|留空使用默认图
 * hero.bg_image => 背景图片:textarea:|留空使用渐变背景
 *
 * @fields_end
 */

$componentId = 'cbl-hero-' . uniqid();
$config = $this->getData('component_config') ?: [];

$getConfig = function($key, $default = '') use ($config) {
    return $config[$key] ?? $default;
};

$heroLabel = $getConfig('hero.label', 'Call Break Playbook');
$title = $getConfig('hero.title', 'Master the Tricks in Call Break Legend');
$subtitle = $getConfig('hero.subtitle', 'Predict every hand, win every trick. India’s beloved trick-taking battle, now ultra-fast.');
$ctaText = $getConfig('hero.cta_text', 'Get the App');
$ctaUrl = $getConfig('hero.cta_url', '#download');
$ctaSecondaryText = $getConfig('hero.secondary_cta_text', 'Watch Rules');
$ctaSecondaryUrl = $getConfig('hero.secondary_cta_url', '#howto');
$heroImage = $getConfig('hero.image', '');
$heroImage = $heroImage ?: $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/callbreak-legend/asset/img/hero-info-graphic.svg');
$bgImage = $getConfig('hero.bg_image', '');

$quickStats = [
    ['value' => $getConfig('hero.stat_1_value', '4P'), 'label' => $getConfig('hero.stat_1_label', 'Live Table Battle')],
    ['value' => $getConfig('hero.stat_2_value', '13'), 'label' => $getConfig('hero.stat_2_label', 'Tricks Each Round')],
    ['value' => $getConfig('hero.stat_3_value', 'A+K'), 'label' => $getConfig('hero.stat_3_label', 'Trump Pressure')],
];

$playbookSteps = [
    ['title' => $getConfig('hero.step_1_title', 'Read the Table'), 'desc' => $getConfig('hero.step_1_desc', 'Track bids fast and predict weak suits before the first break.')],
    ['title' => $getConfig('hero.step_2_title', 'Control Trump Timing'), 'desc' => $getConfig('hero.step_2_desc', 'Delay your strongest cards until rivals expose their anchors.')],
    ['title' => $getConfig('hero.step_3_title', 'Close with Safe Exits'), 'desc' => $getConfig('hero.step_3_desc', 'Finish rounds with low-risk winners and protect your lead.')],
];
$northLabel = $getConfig('hero.seat_north_label', 'NORTH');
$northBid = $getConfig('hero.seat_north_bid', 'Bid 4');
$eastLabel = $getConfig('hero.seat_east_label', 'EAST');
$eastBid = $getConfig('hero.seat_east_bid', 'Bid 3');
$southLabel = $getConfig('hero.seat_south_label', 'YOU');
$southBid = $getConfig('hero.seat_south_bid', 'Bid 5');
$westLabel = $getConfig('hero.seat_west_label', 'WEST');
$westBid = $getConfig('hero.seat_west_bid', 'Bid 1');
$coreBadge = $getConfig('hero.core_badge', 'Trump Ready');
?>

<style>
#<?= $componentId ?> {
    position: relative;
    min-height: 100vh;
    padding: clamp(5.5rem, 9vw, 8rem) 0 clamp(3.5rem, 7vw, 5.5rem);
    display: flex;
    align-items: center;
    overflow: hidden;
    background: #0b1525;
}

#<?= $componentId ?> .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
}

<?php if ($bgImage): ?>
#<?= $componentId ?> .hero-bg::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url('<?= htmlspecialchars($bgImage) ?>');
    background-size: cover;
    background-position: center;
    opacity: 0.28;
}
<?php else: ?>
#<?= $componentId ?> .hero-bg::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url('<?= $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/callbreak-legend/asset/img/poker-header-bg.svg') ?>');
    background-size: cover;
    background-position: center;
    opacity: 0.32;
}
<?php endif; ?>

#<?= $componentId ?> .hero-bg::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
        radial-gradient(760px 360px at 5% 0%, rgba(30, 185, 128, 0.24), transparent 68%),
        radial-gradient(880px 420px at 95% 20%, rgba(242, 169, 59, 0.2), transparent 72%),
        linear-gradient(130deg, rgba(11, 21, 37, 0.95) 0%, rgba(11, 21, 37, 0.72) 45%, rgba(15, 43, 62, 0.92) 100%);
}

#<?= $componentId ?> .hero-orbit {
    position: absolute;
    border-radius: 999px;
    border: 1px solid rgba(30, 185, 128, 0.24);
    pointer-events: none;
    z-index: 1;
    animation: orbitPulse 7s ease-in-out infinite;
}

#<?= $componentId ?> .hero-orbit.orbit-one {
    width: clamp(180px, 22vw, 320px);
    height: clamp(180px, 22vw, 320px);
    left: -80px;
    top: 12%;
}

#<?= $componentId ?> .hero-orbit.orbit-two {
    width: clamp(220px, 26vw, 380px);
    height: clamp(220px, 26vw, 380px);
    right: -110px;
    bottom: 8%;
    border-color: rgba(242, 169, 59, 0.26);
    animation-delay: 1.8s;
}

#<?= $componentId ?> .hero-content {
    position: relative;
    z-index: 2;
    width: min(1220px, calc(100% - 2rem));
    margin: 0 auto;
}

#<?= $componentId ?> .hero-layout {
    display: grid;
    grid-template-columns: minmax(0, 1.02fr) minmax(0, 0.98fr);
    gap: clamp(1.2rem, 3.5vw, 2.8rem);
    align-items: stretch;
}

#<?= $componentId ?> .hero-copy {
    padding: clamp(1.6rem, 3.6vw, 2.7rem);
    border-radius: 28px;
    background:
        linear-gradient(160deg, rgba(12, 26, 40, 0.86) 0%, rgba(11, 21, 37, 0.7) 100%);
    border: 1px solid rgba(30, 185, 128, 0.24);
    box-shadow: 0 30px 70px rgba(2, 6, 12, 0.52);
    backdrop-filter: blur(8px);
    position: relative;
    overflow: hidden;
}

#<?= $componentId ?> .hero-copy::before {
    content: '';
    position: absolute;
    width: 220px;
    height: 220px;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(30, 185, 128, 0.22) 0%, transparent 72%);
    right: -100px;
    top: -90px;
}

#<?= $componentId ?> .hero-label {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 7px 15px;
    border-radius: 999px;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #0b1525;
    background: linear-gradient(120deg, #1eb980, #f2a93b);
    margin-bottom: 1.35rem;
    position: relative;
    z-index: 2;
}

#<?= $componentId ?> .hero-title {
    font-size: clamp(2rem, 5vw, 3.85rem);
    font-weight: 800;
    background: linear-gradient(128deg, #7df7d3 0%, #f2a93b 58%, #e9f5f2 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1.06;
    text-shadow: 0 0 20px rgba(30, 185, 128, 0.28);
    animation: revealUp 0.7s ease-out;
    position: relative;
    z-index: 2;
}

#<?= $componentId ?> .hero-subtitle {
    font-size: clamp(1rem, 1.8vw, 1.2rem);
    color: rgba(233, 245, 242, 0.85);
    margin-top: 1.35rem;
    line-height: 1.75;
    max-width: 42ch;
    animation: revealUp 0.82s ease-out 0.16s both;
    position: relative;
    z-index: 2;
}

#<?= $componentId ?> .hero-stats {
    margin-top: 1.8rem;
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.75rem;
    position: relative;
    z-index: 2;
}

#<?= $componentId ?> .stat-card {
    border: 1px solid rgba(233, 245, 242, 0.12);
    border-radius: 16px;
    padding: 0.72rem 0.78rem;
    background: rgba(11, 21, 37, 0.58);
}

#<?= $componentId ?> .stat-card strong {
    display: block;
    font-size: 1.06rem;
    color: #f2a93b;
    font-weight: 800;
    line-height: 1.2;
}

#<?= $componentId ?> .stat-card span {
    display: block;
    margin-top: 0.2rem;
    color: rgba(203, 213, 225, 0.9);
    font-size: 0.79rem;
    letter-spacing: 0.02em;
}

#<?= $componentId ?> .entry-footer {
    margin-top: 1.8rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.9rem;
    position: relative;
    z-index: 2;
}

#<?= $componentId ?> .hero-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.95rem 1.9rem;
    font-size: 0.95rem;
    font-weight: 700;
    letter-spacing: 0.01em;
    border-radius: 16px;
    text-decoration: none;
    transition: transform 0.28s ease, box-shadow 0.28s ease, background 0.28s ease;
    border: 2px solid transparent;
    line-height: 1;
    animation: revealUp 0.94s ease-out 0.3s both;
}

#<?= $componentId ?> .hero-cta.primary {
    background: linear-gradient(120deg, #1eb980 0%, #0f7f61 100%);
    color: #0b1525;
    box-shadow: 0 18px 45px rgba(30, 185, 128, 0.45);
}

#<?= $componentId ?> .hero-cta.secondary {
    background: rgba(11, 21, 37, 0.6);
    color: #e9f5f2;
    border-color: rgba(242, 169, 59, 0.6);
}

#<?= $componentId ?> .hero-cta:hover {
    transform: translateY(-4px) scale(1.01);
}

#<?= $componentId ?> .hero-stage {
    position: relative;
    padding: clamp(1.2rem, 3.2vw, 1.8rem);
    border-radius: 30px;
    border: 1px solid rgba(242, 169, 59, 0.25);
    background: linear-gradient(150deg, rgba(15, 37, 56, 0.84) 0%, rgba(11, 21, 37, 0.8) 100%);
    box-shadow: 0 28px 68px rgba(2, 6, 12, 0.55);
    overflow: hidden;
}

#<?= $componentId ?> .hero-stage::before {
    content: '';
    position: absolute;
    inset: auto auto -120px -120px;
    width: 260px;
    height: 260px;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(242, 169, 59, 0.22) 0%, transparent 72%);
}

#<?= $componentId ?> .table-board {
    position: relative;
    width: min(420px, 100%);
    aspect-ratio: 1 / 1;
    margin: 0 auto;
}

#<?= $componentId ?> .table-board::before {
    content: '';
    position: absolute;
    inset: 8%;
    border-radius: 999px;
    border: 1px dashed rgba(233, 245, 242, 0.16);
    animation: spinSlow 26s linear infinite;
}

#<?= $componentId ?> .table-board::after {
    content: '';
    position: absolute;
    inset: 20%;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(30, 185, 128, 0.14) 0%, rgba(11, 21, 37, 0.2) 70%, transparent 100%);
}

#<?= $componentId ?> .table-lines {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    opacity: 0.35;
    z-index: 1;
}

#<?= $componentId ?> .table-seat {
    position: absolute;
    min-width: 92px;
    padding: 0.52rem 0.6rem;
    border-radius: 12px;
    border: 1px solid rgba(30, 185, 128, 0.32);
    background: rgba(11, 21, 37, 0.76);
    text-align: center;
    z-index: 2;
    --seat-x: 0%;
    --seat-y: 0%;
    transform: translate(var(--seat-x), var(--seat-y));
    animation: seatFloat 4.5s ease-in-out infinite;
}

#<?= $componentId ?> .table-seat .seat-label {
    display: block;
    font-size: 0.72rem;
    color: #f2a93b;
    font-weight: 700;
    letter-spacing: 0.08em;
}

#<?= $componentId ?> .table-seat .seat-bid {
    display: block;
    margin-top: 0.18rem;
    font-size: 0.72rem;
    color: rgba(233, 245, 242, 0.92);
}

#<?= $componentId ?> .table-seat.north {
    top: 3.5%;
    left: 50%;
    --seat-x: -50%;
    --seat-y: 0%;
    animation-delay: 0s;
}

#<?= $componentId ?> .table-seat.east {
    top: 50%;
    right: 2%;
    --seat-x: 0%;
    --seat-y: -50%;
    animation-delay: 0.8s;
}

#<?= $componentId ?> .table-seat.south {
    bottom: 3.5%;
    left: 50%;
    --seat-x: -50%;
    --seat-y: 0%;
    animation-delay: 1.4s;
}

#<?= $componentId ?> .table-seat.west {
    top: 50%;
    left: 2%;
    --seat-x: 0%;
    --seat-y: -50%;
    animation-delay: 2s;
}

#<?= $componentId ?> .table-core {
    position: absolute;
    inset: 27%;
    border-radius: 22px;
    background: linear-gradient(180deg, rgba(11, 21, 37, 0.98) 0%, rgba(15, 43, 62, 0.82) 100%);
    border: 1px solid rgba(242, 169, 59, 0.3);
    display: grid;
    place-items: center;
    gap: 0.4rem;
    padding: 0.65rem;
    z-index: 3;
    box-shadow: 0 18px 36px rgba(2, 6, 12, 0.5);
}

#<?= $componentId ?> .table-core img {
    width: 100%;
    max-width: 145px;
    height: auto;
    display: block;
    border-radius: 12px;
    border: 1px solid rgba(30, 185, 128, 0.25);
}

#<?= $componentId ?> .table-core .core-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.34rem 0.7rem;
    border-radius: 999px;
    background: rgba(30, 185, 128, 0.18);
    border: 1px solid rgba(30, 185, 128, 0.5);
    color: #c8ffed;
    font-size: 0.66rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

#<?= $componentId ?> .hero-playbook {
    margin-top: 1.05rem;
    display: grid;
    gap: 0.68rem;
    position: relative;
    z-index: 2;
}

#<?= $componentId ?> .playbook-step {
    display: grid;
    grid-template-columns: 30px minmax(0, 1fr);
    gap: 0.68rem;
    align-items: start;
    border: 1px solid rgba(233, 245, 242, 0.12);
    background: rgba(11, 21, 37, 0.46);
    border-radius: 13px;
    padding: 0.56rem 0.62rem;
}

#<?= $componentId ?> .step-index {
    width: 30px;
    height: 30px;
    border-radius: 999px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 0.78rem;
    font-weight: 800;
    color: #0b1525;
    background: linear-gradient(140deg, #f2a93b, #ffd88f);
}

#<?= $componentId ?> .step-copy h3 {
    margin: 0;
    font-size: 0.88rem;
    line-height: 1.25;
    color: #f8fafc;
}

#<?= $componentId ?> .step-copy p {
    margin: 0.26rem 0 0;
    font-size: 0.76rem;
    line-height: 1.45;
    color: rgba(203, 213, 225, 0.92);
}

@keyframes revealUp {
    from { opacity: 0; transform: translateY(18px); }
    to { opacity: 1; transform: translateY(0); }
}

@keyframes orbitPulse {
    0%, 100% { transform: scale(1); opacity: 0.7; }
    50% { transform: scale(1.06); opacity: 0.95; }
}

@keyframes seatFloat {
    0%, 100% { transform: translate(var(--seat-x), var(--seat-y)); }
    50% { transform: translate(var(--seat-x), calc(var(--seat-y) - 6px)); }
}

@keyframes spinSlow {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

@media (max-width: 1080px) {
    #<?= $componentId ?> {
        min-height: auto;
        padding-top: clamp(5rem, 10vw, 6.8rem);
    }

    #<?= $componentId ?> .hero-layout {
        grid-template-columns: 1fr;
        gap: 1rem;
    }

    #<?= $componentId ?> .hero-stage {
        order: -1;
    }

    #<?= $componentId ?> .table-board {
        width: min(360px, 100%);
    }
}

@media (max-width: 720px) {
    #<?= $componentId ?> .hero-content {
        width: calc(100% - 1rem);
    }

    #<?= $componentId ?> .hero-title {
        font-size: clamp(1.8rem, 9vw, 2.7rem);
    }

    #<?= $componentId ?> .hero-stats {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    #<?= $componentId ?> .hero-stats .stat-card:last-child {
        grid-column: 1 / -1;
    }

    #<?= $componentId ?> .entry-footer {
        gap: 0.65rem;
    }

    #<?= $componentId ?> .hero-cta {
        width: 100%;
        min-height: 46px;
    }

    #<?= $componentId ?> .table-seat {
        min-width: 72px;
        padding: 0.44rem 0.45rem;
    }

    #<?= $componentId ?> .table-seat .seat-label,
    #<?= $componentId ?> .table-seat .seat-bid {
        font-size: 0.66rem;
    }

    #<?= $componentId ?> .table-core {
        inset: 30%;
    }

    #<?= $componentId ?> .step-copy h3 {
        font-size: 0.83rem;
    }
}

@media (max-width: 460px) {
    #<?= $componentId ?> .table-board {
        width: 100%;
    }

    #<?= $componentId ?> .table-seat.north,
    #<?= $componentId ?> .table-seat.south {
        left: 50%;
    }

    #<?= $componentId ?> .table-seat.east {
        right: -2px;
    }

    #<?= $componentId ?> .table-seat.west {
        left: -2px;
    }
}

@media (prefers-reduced-motion: reduce) {
    #<?= $componentId ?> .hero-title,
    #<?= $componentId ?> .hero-subtitle,
    #<?= $componentId ?> .hero-cta,
    #<?= $componentId ?> .hero-orbit,
    #<?= $componentId ?> .table-seat,
    #<?= $componentId ?> .table-board::before {
        animation: none !important;
    }
}
</style>

<section id="<?= $componentId ?>">
    <div class="hero-bg"></div>
    <div class="hero-orbit orbit-one" aria-hidden="true"></div>
    <div class="hero-orbit orbit-two" aria-hidden="true"></div>

    <div class="hero-content">
        <div class="hero-layout">
            <article class="hero-copy">
                <header class="entry-header">
                    <?php if ($heroLabel !== ''): ?>
                        <span class="hero-label"><?= htmlspecialchars($heroLabel) ?></span>
                    <?php endif; ?>
                    <h1 class="hero-title entry-title"><?= htmlspecialchars($title) ?></h1>
                </header>

                <div class="entry-content">
                    <p class="hero-subtitle"><?= htmlspecialchars($subtitle) ?></p>
                </div>

                <div class="hero-stats" aria-label="Game highlights">
                    <?php foreach ($quickStats as $stat): ?>
                        <div class="stat-card">
                            <strong><?= htmlspecialchars($stat['value']) ?></strong>
                            <span><?= htmlspecialchars($stat['label']) ?></span>
                        </div>
                    <?php endforeach; ?>
                </div>

                <footer class="entry-footer">
                    <a href="<?= htmlspecialchars($ctaUrl) ?>" class="hero-cta primary">
                        <?= htmlspecialchars($ctaText) ?>
                    </a>
                    <?php if (!empty($ctaSecondaryText)): ?>
                        <a href="<?= htmlspecialchars($ctaSecondaryUrl) ?>" class="hero-cta secondary">
                            <?= htmlspecialchars($ctaSecondaryText) ?>
                        </a>
                    <?php endif; ?>
                </footer>
            </article>

            <aside class="hero-stage" aria-label="Strategy table preview">
                <div class="table-board">
                    <svg class="table-lines" viewBox="0 0 400 400" aria-hidden="true" focusable="false">
                        <path d="M200 40V360M40 200H360" stroke="rgba(233,245,242,0.35)" stroke-width="1.5" />
                        <circle cx="200" cy="200" r="126" fill="none" stroke="rgba(233,245,242,0.2)" stroke-width="1.5" />
                    </svg>

                    <div class="table-seat north">
                        <span class="seat-label"><?= htmlspecialchars($northLabel) ?></span>
                        <span class="seat-bid"><?= htmlspecialchars($northBid) ?></span>
                    </div>
                    <div class="table-seat east">
                        <span class="seat-label"><?= htmlspecialchars($eastLabel) ?></span>
                        <span class="seat-bid"><?= htmlspecialchars($eastBid) ?></span>
                    </div>
                    <div class="table-seat south">
                        <span class="seat-label"><?= htmlspecialchars($southLabel) ?></span>
                        <span class="seat-bid"><?= htmlspecialchars($southBid) ?></span>
                    </div>
                    <div class="table-seat west">
                        <span class="seat-label"><?= htmlspecialchars($westLabel) ?></span>
                        <span class="seat-bid"><?= htmlspecialchars($westBid) ?></span>
                    </div>

                    <div class="table-core">
                        <?php if ($coreBadge !== ''): ?>
                            <span class="core-badge"><?= htmlspecialchars($coreBadge) ?></span>
                        <?php endif; ?>
                        <img src="<?= htmlspecialchars($heroImage) ?>" alt="Call Break visual strategy card" loading="lazy" decoding="async">
                    </div>
                </div>

                <div class="hero-playbook">
                    <?php foreach ($playbookSteps as $index => $step): ?>
                        <div class="playbook-step">
                            <span class="step-index"><?= (int)($index + 1) ?></span>
                            <div class="step-copy">
                                <h3><?= htmlspecialchars($step['title']) ?></h3>
                                <p><?= htmlspecialchars($step['desc']) ?></p>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            </aside>
        </div>
    </div>
</section>
