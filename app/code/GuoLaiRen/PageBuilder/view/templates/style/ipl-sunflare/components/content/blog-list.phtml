<?php
/**
 * IPL Sunflare Blog List Component
 *
 * @fields_start
 * blog.title|博客列表标题|text|Latest Blog Posts
 * blog.subtitle|副标题|text|Stay updated with our latest news and articles
 * blog.posts_per_page|每页显示数量|number|9
 * blog.show_sidebar|显示侧边栏|select:yes=是,no=否|yes
 * blog.show_category|显示分类|select:yes=是,no=否|yes
 * blog.show_date|显示日期|select:yes=是,no=否|yes
 * blog.show_author|显示作者|select:yes=是,no=否|yes
 * blog.card_style|卡片样式|select:default=默认,minimal=简约,featured=特色|default
 * @fields_end
 */

$style = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$blogPosts = $this->getData('blog_posts') ?: [];
$blogCategories = $this->getData('blog_categories') ?: [];
$recentPosts = $this->getData('recent_posts') ?: [];

$getConfig = function($key, $default = '') use ($style, $componentConfig) {
    if (isset($componentConfig[$key]) && $componentConfig[$key] !== '') {
        return $componentConfig[$key];
    }
    return $style[$key] ?? $default;
};

$title = $getConfig('blog.title', __('Latest Blog Posts'));
$subtitle = $getConfig('blog.subtitle', __('Stay updated with our latest news and articles'));
$showSidebar = $getConfig('blog.show_sidebar', 'yes') === 'yes';
$showCategory = $getConfig('blog.show_category', 'yes') === 'yes';
$showDate = $getConfig('blog.show_date', 'yes') === 'yes';
$showAuthor = $getConfig('blog.show_author', 'yes') === 'yes';
$cardStyle = $getConfig('blog.card_style', 'default');

$primaryColor = $getConfig('color_scheme.primary', '#ff6a3d');
$secondaryColor = $getConfig('color_scheme.secondary', '#ff4d8d');
$bgColor = $getConfig('color_scheme.background', '#120a14');
$textColor = $getConfig('color_scheme.text', '#ffe6d1');
$pokerBg = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/ipl-sunflare/asset/img/poker-header-bg.svg');

$featuredPost = $blogPosts[0] ?? null;
$sparkPosts = $featuredPost ? array_slice($blogPosts, 1, 2) : [];
$streamPosts = $featuredPost ? array_slice($blogPosts, 3) : $blogPosts;
$cardStyleClass = 'style-' . preg_replace('/[^a-z0-9-]+/i', '', (string)$cardStyle);
?>

<section class="dts-blog-list" id="blog">
    <div class="blog-shell">
        <header class="blog-masthead">
            <div class="masthead-copy">
                <span class="masthead-kicker">Sunflare Journal</span>
                <h2><?= htmlspecialchars($title) ?></h2>
                <?php if ($subtitle): ?>
                    <p><?= htmlspecialchars($subtitle) ?></p>
                <?php endif; ?>
            </div>
            <div class="masthead-metrics" aria-label="Blog overview">
                <span><strong><?= count($blogPosts) ?></strong><?= __('Posts') ?></span>
                <span><strong><?= count($blogCategories) ?></strong><?= __('Categories') ?></span>
            </div>
        </header>

        <?php if (empty($blogPosts)): ?>
            <div class="blog-empty">
                <div class="empty-icon">
                    <svg width="64" height="64" viewBox="0 0 24 24" fill="currentColor" opacity="0.3">
                        <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-5 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"/>
                    </svg>
                </div>
                <p><?= __('There are no blog posts yet. Please check back later!') ?></p>
            </div>
        <?php else: ?>
            <?php if ($featuredPost): ?>
                <section class="lead-grid">
                    <article class="lead-story">
                        <?php if (!empty($featuredPost['cover_image'])): ?>
                            <a href="<?= htmlspecialchars($featuredPost['url']) ?>" class="lead-media">
                                <img src="<?= htmlspecialchars($featuredPost['cover_image']) ?>" alt="<?= htmlspecialchars($featuredPost['title']) ?>" loading="lazy">
                            </a>
                        <?php endif; ?>

                        <div class="lead-copy">
                            <div class="lead-meta">
                                <?php if ($showCategory && !empty($featuredPost['category_name'])): ?>
                                    <span class="story-chip"><?= htmlspecialchars($featuredPost['category_name']) ?></span>
                                <?php endif; ?>
                                <?php if ($showDate && !empty($featuredPost['published_at'])): ?>
                                    <span class="story-date"><?= date('M d, Y', strtotime($featuredPost['published_at'])) ?></span>
                                <?php endif; ?>
                            </div>

                            <h3>
                                <a href="<?= htmlspecialchars($featuredPost['url']) ?>">
                                    <?= htmlspecialchars($featuredPost['title']) ?>
                                </a>
                            </h3>

                            <?php if (!empty($featuredPost['summary'])): ?>
                                <p class="lead-summary"><?= htmlspecialchars(mb_substr($featuredPost['summary'], 0, 220)) ?><?= mb_strlen($featuredPost['summary']) > 220 ? '...' : '' ?></p>
                            <?php endif; ?>

                            <div class="lead-foot">
                                <?php if ($showAuthor && !empty($featuredPost['author'])): ?>
                                    <span class="story-author"><?= htmlspecialchars($featuredPost['author']) ?></span>
                                <?php endif; ?>
                                <a href="<?= htmlspecialchars($featuredPost['url']) ?>" class="story-link">
                                    <?= __('Read Story') ?>
                                    <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                        <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                    </svg>
                                </a>
                            </div>
                        </div>
                    </article>

                    <?php if (!empty($sparkPosts)): ?>
                        <div class="spark-column">
                            <?php foreach ($sparkPosts as $spark): ?>
                                <article class="spark-card">
                                    <h4><a href="<?= htmlspecialchars($spark['url']) ?>"><?= htmlspecialchars($spark['title']) ?></a></h4>
                                    <?php if (!empty($spark['summary'])): ?>
                                        <p><?= htmlspecialchars(mb_substr($spark['summary'], 0, 100)) ?><?= mb_strlen($spark['summary']) > 100 ? '...' : '' ?></p>
                                    <?php endif; ?>
                                    <a href="<?= htmlspecialchars($spark['url']) ?>" class="story-link">
                                        <?= __('Open') ?>
                                        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                            <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                        </svg>
                                    </a>
                                </article>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </section>
            <?php endif; ?>

            <div class="news-body<?= $showSidebar ? ' has-rail' : '' ?>">
                <div class="stream-grid <?= htmlspecialchars($cardStyleClass) ?>">
                    <?php foreach ($streamPosts as $index => $post): ?>
                        <?php $streamClass = ($cardStyle === 'featured' && $index === 0) ? ' is-surge' : ''; ?>
                        <article class="stream-card<?= $streamClass ?>">
                            <?php if (!empty($post['cover_image'])): ?>
                                <a href="<?= htmlspecialchars($post['url']) ?>" class="stream-cover">
                                    <img src="<?= htmlspecialchars($post['cover_image']) ?>" alt="<?= htmlspecialchars($post['title']) ?>" loading="lazy">
                                </a>
                            <?php endif; ?>

                            <div class="stream-copy">
                                <div class="stream-meta">
                                    <?php if ($showCategory && !empty($post['category_name'])): ?>
                                        <span class="story-chip"><?= htmlspecialchars($post['category_name']) ?></span>
                                    <?php endif; ?>
                                    <?php if ($showDate && !empty($post['published_at'])): ?>
                                        <span class="story-date"><?= date('M d, Y', strtotime($post['published_at'])) ?></span>
                                    <?php endif; ?>
                                </div>

                                <h3><a href="<?= htmlspecialchars($post['url']) ?>"><?= htmlspecialchars($post['title']) ?></a></h3>

                                <?php if ($cardStyle !== 'minimal' && !empty($post['summary'])): ?>
                                    <p class="stream-summary">
                                        <?= htmlspecialchars(mb_substr($post['summary'], 0, 145)) ?><?= mb_strlen($post['summary']) > 145 ? '...' : '' ?>
                                    </p>
                                <?php endif; ?>

                                <div class="stream-foot">
                                    <?php if ($showAuthor && !empty($post['author'])): ?>
                                        <span class="story-author"><?= htmlspecialchars($post['author']) ?></span>
                                    <?php endif; ?>
                                    <a href="<?= htmlspecialchars($post['url']) ?>" class="story-link">
                                        <?= __('Read More') ?>
                                        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                            <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                        </svg>
                                    </a>
                                </div>
                            </div>
                        </article>
                    <?php endforeach; ?>

                    <?php if (empty($streamPosts)): ?>
                        <article class="stream-card placeholder-card">
                            <div class="stream-copy">
                                <h3><?= __('More stories are on the way') ?></h3>
                                <p class="stream-summary"><?= __('We are preparing additional IPL Sunflare updates and strategy notes. Please check back soon.') ?></p>
                            </div>
                        </article>
                    <?php endif; ?>
                </div>

                <?php if ($showSidebar): ?>
                    <aside class="signal-rail">
                        <?php if (!empty($blogCategories)): ?>
                            <section class="rail-card">
                                <h4><?= __('Categories') ?></h4>
                                <div class="category-cloud">
                                    <?php foreach ($blogCategories as $category): ?>
                                        <a href="<?= htmlspecialchars($category['url']) ?>" class="category-pill"><?= htmlspecialchars($category['name']) ?></a>
                                    <?php endforeach; ?>
                                </div>
                            </section>
                        <?php endif; ?>

                        <?php if (!empty($recentPosts)): ?>
                            <section class="rail-card">
                                <h4><?= __('Recent Posts') ?></h4>
                                <div class="recent-stack">
                                    <?php foreach (array_slice($recentPosts, 0, 5) as $post): ?>
                                        <a href="<?= htmlspecialchars($post['url']) ?>" class="recent-link">
                                            <?php if (!empty($post['cover_image'])): ?>
                                                <img src="<?= htmlspecialchars($post['cover_image']) ?>" alt="<?= htmlspecialchars($post['title']) ?>" loading="lazy">
                                            <?php endif; ?>
                                            <span>
                                                <strong><?= htmlspecialchars($post['title']) ?></strong>
                                                <?php if (!empty($post['published_at'])): ?>
                                                    <small><?= date('M d, Y', strtotime($post['published_at'])) ?></small>
                                                <?php endif; ?>
                                            </span>
                                        </a>
                                    <?php endforeach; ?>
                                </div>
                            </section>
                        <?php endif; ?>
                    </aside>
                <?php endif; ?>
            </div>
        <?php endif; ?>
    </div>
</section>

<style>
.dts-blog-list {
    position: relative;
    padding: 84px 0;
    background:
        radial-gradient(1180px 620px at 12% 6%, rgba(255, 111, 51, 0.15), transparent 60%),
        radial-gradient(880px 560px at 88% 16%, rgba(255, 77, 141, 0.18), transparent 62%),
        <?= htmlspecialchars($bgColor) ?>;
    color: <?= htmlspecialchars($textColor) ?>;
    overflow: hidden;
}

.dts-blog-list::before {
    content: '';
    position: absolute;
    inset: 0;
    background: url('<?= $pokerBg ?>') center top/cover no-repeat;
    opacity: 0.08;
    pointer-events: none;
}

.dts-blog-list::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(255, 182, 120, 0.06) 0%, rgba(25, 11, 25, 0.62) 100%);
    pointer-events: none;
}

.dts-blog-list .blog-shell {
    max-width: 1220px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
    display: grid;
    gap: 1rem;
}

.dts-blog-list .blog-masthead {
    display: grid;
    gap: 1rem;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: end;
}

.dts-blog-list .masthead-kicker {
    display: inline-flex;
    align-items: center;
    padding: 0.38rem 0.84rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 189, 126, 0.34);
    background: rgba(255, 145, 56, 0.12);
    color: #ffd595;
    font-size: 0.74rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.dts-blog-list .blog-masthead h2 {
    margin-top: 1rem;
    color: #ffe8d1;
    font-size: clamp(2.05rem, 4.1vw, 3.4rem);
    line-height: 1.04;
    letter-spacing: -0.04em;
}

.dts-blog-list .blog-masthead p {
    margin-top: 0.85rem;
    color: rgba(255, 213, 184, 0.78);
    line-height: 1.76;
}

.dts-blog-list .masthead-metrics {
    display: flex;
    gap: 0.7rem;
    flex-wrap: wrap;
    justify-content: flex-end;
}

.dts-blog-list .masthead-metrics span {
    display: inline-flex;
    align-items: center;
    gap: 0.42rem;
    padding: 0.46rem 0.86rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 193, 135, 0.28);
    background: rgba(30, 14, 30, 0.48);
    color: rgba(255, 222, 197, 0.82);
    font-size: 0.82rem;
}

.dts-blog-list .masthead-metrics strong {
    color: #ffcf91;
}

.dts-blog-list .lead-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(260px, 0.92fr);
    gap: 1rem;
}

.dts-blog-list .lead-story,
.dts-blog-list .spark-card,
.dts-blog-list .stream-card,
.dts-blog-list .rail-card,
.dts-blog-list .blog-empty {
    border-radius: 24px;
    border: 1px solid rgba(255, 190, 127, 0.2);
    background: linear-gradient(160deg, rgba(54, 28, 45, 0.92) 0%, rgba(74, 37, 58, 0.88) 100%);
    box-shadow: 0 22px 56px rgba(0, 0, 0, 0.34);
}

.dts-blog-list .lead-story {
    overflow: hidden;
    display: grid;
    grid-template-columns: minmax(280px, 0.95fr) minmax(0, 1.05fr);
}

.dts-blog-list .lead-media {
    display: block;
    min-height: 100%;
}

.dts-blog-list .lead-media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

.dts-blog-list .lead-copy {
    padding: clamp(1.2rem, 2.8vw, 1.9rem);
    display: grid;
    align-content: center;
}

.dts-blog-list .lead-meta,
.dts-blog-list .stream-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    align-items: center;
}

.dts-blog-list .story-chip,
.dts-blog-list .story-date {
    display: inline-flex;
    align-items: center;
    padding: 0.3rem 0.7rem;
    border-radius: 999px;
    font-size: 0.72rem;
    font-weight: 700;
}

.dts-blog-list .story-chip {
    background: linear-gradient(120deg, <?= htmlspecialchars($primaryColor) ?> 0%, <?= htmlspecialchars($secondaryColor) ?> 100%);
    color: #ffe8d1;
}

.dts-blog-list .story-date {
    color: rgba(255, 214, 186, 0.72);
    border: 1px solid rgba(255, 194, 134, 0.22);
    background: rgba(255, 255, 255, 0.03);
}

.dts-blog-list .lead-copy h3 {
    margin-top: 0.95rem;
    font-size: clamp(1.75rem, 2.9vw, 2.6rem);
    line-height: 1.08;
}

.dts-blog-list .lead-copy h3 a,
.dts-blog-list .spark-card h4 a,
.dts-blog-list .stream-copy h3 a {
    color: #ffe8d1;
    text-decoration: none;
    transition: color 0.25s ease;
}

.dts-blog-list .lead-copy h3 a:hover,
.dts-blog-list .spark-card h4 a:hover,
.dts-blog-list .stream-copy h3 a:hover {
    color: <?= htmlspecialchars($secondaryColor) ?>;
}

.dts-blog-list .lead-summary,
.dts-blog-list .spark-card p,
.dts-blog-list .stream-summary {
    margin-top: 0.88rem;
    color: rgba(255, 214, 186, 0.74);
    line-height: 1.72;
}

.dts-blog-list .lead-foot,
.dts-blog-list .stream-foot {
    margin-top: 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.8rem;
    flex-wrap: wrap;
}

.dts-blog-list .story-author {
    color: rgba(255, 203, 170, 0.66);
    font-size: 0.86rem;
}

.dts-blog-list .story-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: <?= htmlspecialchars($primaryColor) ?>;
    text-decoration: none;
    font-weight: 700;
    transition: gap 0.25s ease, color 0.25s ease;
}

.dts-blog-list .story-link:hover {
    gap: 0.7rem;
    color: <?= htmlspecialchars($secondaryColor) ?>;
}

.dts-blog-list .spark-column {
    display: grid;
    gap: 1rem;
}

.dts-blog-list .spark-card {
    padding: 1.05rem;
    display: grid;
    align-content: center;
}

.dts-blog-list .spark-card h4 {
    margin: 0;
    font-size: 1.06rem;
    line-height: 1.45;
}

.dts-blog-list .news-body {
    display: grid;
    gap: 1rem;
}

.dts-blog-list .news-body.has-rail {
    grid-template-columns: minmax(0, 1fr) minmax(260px, 0.3fr);
    align-items: start;
}

.dts-blog-list .stream-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
}

.dts-blog-list .stream-grid.style-featured .stream-card.is-surge {
    grid-column: span 2;
}

.dts-blog-list .stream-cover {
    display: block;
    position: relative;
    padding-top: 58%;
    overflow: hidden;
}

.dts-blog-list .stream-cover img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.35s ease;
}

.dts-blog-list .stream-card:hover .stream-cover img {
    transform: scale(1.05);
}

.dts-blog-list .stream-copy {
    padding: 1.05rem;
    display: grid;
    align-content: start;
}

.dts-blog-list .stream-copy h3 {
    margin-top: 0.85rem;
    font-size: 1.14rem;
    line-height: 1.38;
}

.dts-blog-list .stream-grid.style-minimal .stream-summary {
    display: none;
}

.dts-blog-list .placeholder-card {
    grid-column: 1 / -1;
}

.dts-blog-list .signal-rail {
    display: grid;
    gap: 1rem;
    position: sticky;
    top: 110px;
}

.dts-blog-list .rail-card {
    padding: 1.05rem;
}

.dts-blog-list .rail-card h4 {
    color: #ffe8d1;
    font-size: 0.9rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.dts-blog-list .category-cloud {
    margin-top: 0.8rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
}

.dts-blog-list .category-pill {
    display: inline-flex;
    align-items: center;
    padding: 0.4rem 0.76rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 192, 129, 0.22);
    background: rgba(255, 143, 54, 0.09);
    color: rgba(255, 214, 186, 0.82);
    text-decoration: none;
    transition: transform 0.25s ease, color 0.25s ease, background 0.25s ease;
}

.dts-blog-list .category-pill:hover {
    transform: translateY(-2px);
    color: #ffe8d1;
    background: rgba(255, 77, 141, 0.14);
}

.dts-blog-list .recent-stack {
    margin-top: 0.85rem;
    display: grid;
    gap: 0.75rem;
}

.dts-blog-list .recent-link {
    display: grid;
    grid-template-columns: 62px minmax(0, 1fr);
    gap: 0.7rem;
    text-decoration: none;
    color: inherit;
}

.dts-blog-list .recent-link img {
    width: 62px;
    height: 62px;
    border-radius: 11px;
    object-fit: cover;
}

.dts-blog-list .recent-link span {
    display: grid;
    gap: 0.28rem;
}

.dts-blog-list .recent-link strong {
    color: #ffe8d1;
    font-size: 0.9rem;
    line-height: 1.42;
}

.dts-blog-list .recent-link small {
    color: rgba(255, 203, 170, 0.64);
    font-size: 0.75rem;
}

.dts-blog-list .blog-empty {
    text-align: center;
    padding: 4rem 1.5rem;
}

.dts-blog-list .empty-icon {
    margin-bottom: 1rem;
}

@media (max-width: 1100px) {
    .dts-blog-list .blog-masthead,
    .dts-blog-list .lead-grid,
    .dts-blog-list .lead-story,
    .dts-blog-list .news-body.has-rail {
        grid-template-columns: 1fr;
    }

    .dts-blog-list .signal-rail {
        position: static;
    }
}

@media (max-width: 768px) {
    .dts-blog-list {
        padding: 64px 0;
    }

    .dts-blog-list .blog-shell {
        padding: 0 15px;
    }

    .dts-blog-list .stream-grid,
    .dts-blog-list .stream-grid.style-featured .stream-card.is-surge {
        grid-template-columns: 1fr;
        grid-column: auto;
    }

    .dts-blog-list .lead-story,
    .dts-blog-list .spark-card,
    .dts-blog-list .stream-card,
    .dts-blog-list .rail-card,
    .dts-blog-list .blog-empty {
        border-radius: 20px;
    }
}
</style>
