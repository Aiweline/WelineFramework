<?php
/**
 * TPMST 游戏介绍组件 - Games Component
 *
 * @component_start
 * code: games
 * name: 游戏介绍
 * name_en: Games Showcase
 * description: 游戏/产品介绍卡片，支持图片和描述
 * category: content
 * region: content
 * thumbnail: asset/img/game/game-i1.webp
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_section => 显示此区域:select:yes|yes,no
 * display.show_title => 显示标题:select:yes|yes,no
 * display.show_description => 显示卡片描述:select:yes|yes,no
 *
 * group:style => 样式配置
 * style.bg_color => 背景颜色:color:#0f0f1e
 * style.title_color => 标题颜色:color:#ffd700
 * style.title_font_size => 标题字体大小(PC端):number:36|px
 * style.title_font_size_mobile => 标题字体大小(移动端):number:24|px
 * style.card_bg_color => 卡片背景色:color:rgba(255,255,255,0.05)
 * style.card_border_radius => 卡片圆角:number:12|px
 * style.card_border_color => 卡片边框色:color:rgba(255,255,255,0.1)
 * style.card_title_color => 卡片标题颜色:color:#ffd700
 * style.card_title_font_size => 卡片标题字号:number:20|px
 * style.card_desc_color => 卡片描述颜色:color:#ffffff
 * style.card_desc_font_size => 卡片描述字号:number:14|px
 * style.section_padding => 区域内边距(PC端):number:60|px
 * style.section_padding_mobile => 区域内边距(移动端):number:40|px
 * style.card_gap => 卡片间距:number:30|px
 * style.card_content_padding => 卡片内容内边距:number:20|px
 * style.hover_transform => 悬停上移距离:number:5|px
 *
 * group:images => 图片配置
 * images.image_1_desktop => 游戏1桌面端图片:textarea:|默认使用本地图片
 * images.image_1_mobile => 游戏1移动端图片:textarea:|默认使用本地图片
 * images.image_2 => 游戏2图片:textarea:|默认使用本地图片
 * images.image_3 => 游戏3图片:textarea:|默认使用本地图片
 *
 * group:texts => 游戏文本
 * texts.title => 区域标题:text:Choose your court rhythm
 * texts.summary => 区域辅助文案:text:Three Court Piece routes built for table control, faster calls, and stronger partner momentum.
 * texts.game_1_title => 游戏1标题:text:Classic Court Piece
 * texts.game_1_desc => 游戏1描述:textarea:The signature four-seat format with clean trick flow, table awareness, and a stronger emphasis on partner coordination.
 * texts.game_2_title => 游戏2标题:text:Blind Trump Rush
 * texts.game_2_desc => 游戏2描述:textarea:Start faster, read pressure earlier, and turn bold openings into dramatic momentum swings.
 * texts.game_3_title => 游戏3标题:text:Partner League Tables
 * texts.game_3_desc => 游戏3描述:textarea:Built for repeat sessions, streak play, and team chemistry with richer reward framing.
 *
 * @fields_end
 */

$componentId = 'court-piece-electric-mahal-games-' . uniqid();
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$config = array_merge(is_array($styleSettings) ? $styleSettings : [], is_array($componentConfig) ? $componentConfig : []);
$colors = isset($colors) && is_array($colors) ? $colors : [];

$getConfig = function ($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

if ($getConfig('display.show_section', $getConfig('games.display', 'yes')) === 'no') {
    return;
}

$showTitle = $getConfig('display.show_title', 'yes') !== 'no';
$showDescription = $getConfig('display.show_description', 'yes') !== 'no';
$bgColor = $getConfig('style.bg_color', $colors['games_bg'] ?? '#0b2337');
$titleColor = $getConfig('style.title_color', '#ffffff');
$titleFontSize = (int) $getConfig('style.title_font_size', '36');
$titleFontSizeMobile = (int) $getConfig('style.title_font_size_mobile', '24');
$cardBgColor = $getConfig('style.card_bg_color', 'rgba(255,255,255,0.06)');
$cardBorderRadius = (int) $getConfig('style.card_border_radius', '22');
$cardBorderColor = $getConfig('style.card_border_color', 'rgba(255,255,255,0.12)');
$cardTitleColor = $getConfig('style.card_title_color', '#fff5d1');
$cardTitleFontSize = (int) $getConfig('style.card_title_font_size', '20');
$cardDescColor = $getConfig('style.card_desc_color', '#efe6ff');
$cardDescFontSize = (int) $getConfig('style.card_desc_font_size', '14');
$sectionPadding = (int) $getConfig('style.section_padding', '80');
$sectionPaddingMobile = (int) $getConfig('style.section_padding_mobile', '46');
$cardGap = (int) $getConfig('style.card_gap', '26');
$cardContentPadding = (int) $getConfig('style.card_content_padding', '24');
$hoverTransform = (int) $getConfig('style.hover_transform', '8');

$defaultImage1Desktop = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/court-piece-electric-mahal/asset/img/game/game-i1.webp');
$defaultImage1Mobile = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/court-piece-electric-mahal/asset/img/game/game-i1-m.webp');
$defaultImage2 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/court-piece-electric-mahal/asset/img/game/game-i2.webp');
$defaultImage3 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/court-piece-electric-mahal/asset/img/game/game-i3.webp');

$cards = [
    [
        'index' => '01',
        'tag' => 'Signature table',
        'emoji' => '♠',
        'tokens' => ['4 seats', 'Trump read', 'Partner sync'],
        'desktop' => $getConfig('images.image_1_desktop', $getConfig('games.image_1_desktop', '')) ?: $defaultImage1Desktop,
        'mobile' => $getConfig('images.image_1_mobile', $getConfig('games.image_1_mobile', '')) ?: $defaultImage1Mobile,
        'title' => $getConfig('texts.game_1_title', 'Classic Court Piece'),
        'desc' => $getConfig('texts.game_1_desc', 'The signature four-seat format with clean trick flow, table awareness, and a stronger emphasis on partner coordination.'),
    ],
    [
        'index' => '02',
        'tag' => 'Fast pressure',
        'emoji' => '♥',
        'tokens' => ['Quick join', 'Bold call', 'Sharp swing'],
        'desktop' => $getConfig('images.image_2', $getConfig('games.image_2', '')) ?: $defaultImage2,
        'mobile' => $getConfig('images.image_2', $getConfig('games.image_2', '')) ?: $defaultImage2,
        'title' => $getConfig('texts.game_2_title', 'Blind Trump Rush'),
        'desc' => $getConfig('texts.game_2_desc', 'Start faster, read pressure earlier, and turn bold openings into dramatic momentum swings.'),
    ],
    [
        'index' => '03',
        'tag' => 'Reward loops',
        'emoji' => '♣',
        'tokens' => ['Streak play', 'Team table', 'Rematch'],
        'desktop' => $getConfig('images.image_3', $getConfig('games.image_3', '')) ?: $defaultImage3,
        'mobile' => $getConfig('images.image_3', $getConfig('games.image_3', '')) ?: $defaultImage3,
        'title' => $getConfig('texts.game_3_title', 'Partner League Tables'),
        'desc' => $getConfig('texts.game_3_desc', 'Built for repeat sessions, streak play, and team chemistry with richer reward framing.'),
    ],
];
$title = $getConfig('texts.title', $getConfig('games.title', 'Choose your court rhythm'));
$summary = trim((string) $getConfig('texts.summary', $getConfig('games.summary', 'Three Court Piece routes built for table control, faster calls, and stronger partner momentum.')));
$leftCards = array_slice($cards, 0, 2);
$rightCard = $cards[2] ?? null;
?>
<section id="<?= $componentId ?>" class="cpe-games" aria-labelledby="<?= $componentId ?>-title">
    <style>
        #<?= $componentId ?> {
            position: relative;
            padding: <?= $sectionPadding ?>px 0;
            background:
                radial-gradient(circle at 8% 16%, rgba(255,204,87,0.18), transparent 18%),
                radial-gradient(circle at 92% 14%, rgba(81,228,255,0.16), transparent 19%),
                radial-gradient(circle at 52% 84%, rgba(255,118,75,0.18), transparent 26%),
                linear-gradient(135deg, <?= htmlspecialchars($bgColor) ?> 0%, #10263f 45%, #090f19 100%);
            overflow: hidden;
        }

        #<?= $componentId ?>::before,
        #<?= $componentId ?>::after {
            content: "";
            position: absolute;
            inset: 0;
            pointer-events: none;
        }

        #<?= $componentId ?>::before {
            background:
                linear-gradient(115deg, transparent 0 12%, rgba(255,255,255,0.05) 12% 13%, transparent 13% 26%, rgba(255,255,255,0.04) 26% 27%, transparent 27% 40%, rgba(255,255,255,0.04) 40% 41%, transparent 41% 100%);
            opacity: 0.4;
        }

        #<?= $componentId ?>::after {
            content: "♠  ♥  ♣";
            inset: auto auto 18px 28px;
            color: rgba(255,255,255,0.08);
            font-size: clamp(34px, 5vw, 72px);
            font-weight: 700;
            letter-spacing: 0.26em;
            white-space: nowrap;
        }

        #<?= $componentId ?> .cpe-wrap {
            position: relative;
            z-index: 1;
            width: min(100% - 32px, 1220px);
            margin: 0 auto;
        }

        #<?= $componentId ?> .cpe-stage {
            display: grid;
            grid-template-columns: minmax(0, 1fr) minmax(320px, 0.94fr);
            gap: <?= $cardGap ?>px;
            align-items: start;
        }

        #<?= $componentId ?> .cpe-rail {
            display: grid;
            gap: <?= $cardGap ?>px;
            min-width: 0;
        }

        #<?= $componentId ?> .cpe-head-card {
            position: relative;
            display: grid;
            gap: 18px;
            grid-column: 2;
            grid-row: 1;
            padding: 14px 0 26px;
            overflow: visible;
            text-align: right;
        }

        #<?= $componentId ?> .cpe-head-card::before {
            content: "";
            position: absolute;
            inset: auto 0 2px auto;
            width: min(190px, 48%);
            height: 14px;
            border-radius: 999px;
            background: linear-gradient(90deg, rgba(255,214,96,0.92), rgba(110,243,255,0.82));
            box-shadow: 0 0 28px rgba(255,214,96,0.28);
            transform: skewX(-26deg);
            opacity: 0.88;
        }

        #<?= $componentId ?> .cpe-head-card::after {
            content: "";
            position: absolute;
            right: -20px;
            top: -28px;
            width: 180px;
            height: 180px;
            background: radial-gradient(circle, rgba(255,214,96,0.18) 0%, rgba(110,243,255,0.08) 42%, transparent 72%);
            filter: blur(10px);
            pointer-events: none;
        }

        #<?= $componentId ?> .cpe-rail--left {
            grid-column: 1;
            grid-row: 1 / span 2;
        }

        #<?= $componentId ?> .cpe-head-top {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            align-items: center;
            justify-content: flex-end;
        }

        #<?= $componentId ?> .cpe-track {
            display: inline-flex;
            align-items: center;
            padding: 10px 16px;
            border-radius: 999px;
            border: 1px solid rgba(255,255,255,0.14);
            background: rgba(7, 10, 20, 0.36);
            color: #6ef3ff;
            font-size: 12px;
            font-weight: 800;
            letter-spacing: 0.16em;
            text-transform: uppercase;
            box-shadow: inset 0 0 0 1px rgba(255,255,255,0.04);
        }

        #<?= $componentId ?> .cpe-suit-row {
            display: inline-flex;
            gap: 10px;
            color: rgba(255,240,175,0.88);
            font-size: 22px;
        }

        #<?= $componentId ?> .cpe-head-card h2 {
            margin: 0;
            margin-left: auto;
            color: <?= htmlspecialchars($titleColor) ?>;
            font: 800 clamp(<?= $titleFontSizeMobile ?>px, 5vw, <?= $titleFontSize ?>px) / 1.02 "Palatino Linotype", "Book Antiqua", Georgia, serif;
            letter-spacing: -0.035em;
            text-wrap: balance;
            text-shadow: 0 0 18px rgba(255,214,96,0.18), 0 0 34px rgba(110,243,255,0.12);
        }

        #<?= $componentId ?> .cpe-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            justify-content: flex-end;
        }

        #<?= $componentId ?> .cpe-summary {
            margin: 0;
            max-width: 38ch;
            margin-left: auto;
            color: <?= htmlspecialchars($cardDescColor) ?>;
            font-size: 15px;
            line-height: 1.82;
        }

        #<?= $componentId ?> .cpe-tags span {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            min-height: 38px;
            padding: 0 16px;
            border-radius: 999px;
            background: rgba(255,255,255,0.08);
            border: 1px solid rgba(255,255,255,0.14);
            color: #fff6de;
            font-size: 12px;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }

        #<?= $componentId ?> .cpe-tags b {
            color: #6ef3ff;
            font-size: 15px;
        }

        #<?= $componentId ?> .cpe-ticket {
            position: relative;
            display: grid;
            gap: 18px;
            height: 100%;
            padding: <?= $cardContentPadding ?>px;
            border-radius: <?= $cardBorderRadius ?>px;
            background:
                linear-gradient(160deg, <?= htmlspecialchars($cardBgColor) ?> 0%, rgba(7,10,20,0.82) 100%);
            border: 1px solid <?= htmlspecialchars($cardBorderColor) ?>;
            box-shadow: 0 28px 56px rgba(0,0,0,0.2);
            transition: transform 0.28s ease, box-shadow 0.28s ease;
            overflow: hidden;
        }

        #<?= $componentId ?> .cpe-ticket::before {
            content: "";
            position: absolute;
            inset: 0 auto 0 0;
            width: 8px;
            background: linear-gradient(180deg, rgba(255,240,175,0.9), rgba(110,243,255,0.42));
        }

        #<?= $componentId ?> .cpe-ticket:hover {
            transform: translateY(-<?= $hoverTransform ?>px);
            box-shadow: 0 34px 64px rgba(0,0,0,0.24);
        }

        #<?= $componentId ?> .cpe-ticket--a {
            background:
                radial-gradient(circle at 100% 0%, rgba(255,214,96,0.22), transparent 24%),
                linear-gradient(160deg, rgba(255,255,255,0.08) 0%, rgba(7,10,20,0.88) 100%);
        }

        #<?= $componentId ?> .cpe-ticket--b {
            background:
                radial-gradient(circle at 100% 0%, rgba(255, 112, 112, 0.2), transparent 28%), linear-gradient(160deg, rgba(255, 255, 255, 0.06) 0%, rgb(105 58 129) 100%);
        }
        #<?= $componentId ?> .cpe-ticket--b:before {
            inset: 0 -2px 0 auto;
            transform: rotate(1deg);
            width: 10px;
        }
        #<?= $componentId ?> .cpe-ticket--b .cpe-emoji{color: #fe536f;}

        #<?= $componentId ?> .cpe-ticket--c {
            grid-column: 2;
            grid-row: 2;
            background:
                radial-gradient(circle at 100% 0%, rgba(110,243,255,0.22), transparent 28%),
                linear-gradient(160deg, rgba(255,255,255,0.06) 0%, rgba(10,18,24,0.86) 100%);
        }

        #<?= $componentId ?> .cpe-ticket--b::after,
        #<?= $componentId ?> .cpe-ticket--c::after {
            content: "";
            position: absolute;
            top: -36px;
            right: -34px;
            width: 132px;
            height: 132px;
            border-radius: 28px;
            border: 1px solid rgba(255,255,255,0.08);
            transform: rotate(24deg);
        }

        #<?= $componentId ?> .cpe-ticket--a .cpe-media::before,
        #<?= $componentId ?> .cpe-ticket--c .cpe-media::before {
            content: "";
            position: absolute;
            inset: 16px auto auto 16px;
            width: 72px;
            height: 8px;
            border-radius: 999px;
            background: linear-gradient(90deg, rgba(255,214,96,0.94), rgba(110,243,255,0.82));
            z-index: 1;
        }

        #<?= $componentId ?> .cpe-ticket-top {
            display: grid;
            grid-template-columns: auto 1fr auto;
            gap: 12px;
            align-items: center;
        }

        #<?= $componentId ?> .cpe-index {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-width: 58px;
            height: 42px;
            padding: 0 12px;
            border-radius: 14px;
            background: rgba(255,255,255,0.08);
            border: 1px solid rgba(255,255,255,0.14);
            color: #fff0af;
            font-size: 13px;
            font-weight: 800;
            letter-spacing: 0.18em;
        }

        #<?= $componentId ?> .cpe-tag {
            min-width: 0;
            color: #d7f8ff;
            font-size: 12px;
            font-weight: 700;
            letter-spacing: 0.12em;
            text-transform: uppercase;
        }

        #<?= $componentId ?> .cpe-emoji {
            color: #fff0af;
            font-size: 28px;
            line-height: 1;
        }

        #<?= $componentId ?> .cpe-ticket-body {
            display: grid;
            grid-template-columns: minmax(0, 1fr) minmax(240px, 0.9fr);
            gap: 18px;
            align-items: stretch;
        }

        #<?= $componentId ?> .cpe-ticket--b .cpe-ticket-body {
            grid-template-columns: minmax(180px, 0.82fr) minmax(0, 1fr);
        }

        #<?= $componentId ?> .cpe-ticket--c .cpe-ticket-body {
            grid-template-columns: minmax(140px, 260px) minmax(0, 1fr);
        }

        #<?= $componentId ?> .cpe-copy {
            display: grid;
            align-content: space-between;
            gap: 16px;
            min-width: 0;
        }

        #<?= $componentId ?> .cpe-copy h3 {
            margin: 0;
            color: <?= htmlspecialchars($cardTitleColor) ?>;
            font-size: <?= $cardTitleFontSize ?>px;
            font-weight: 800;
            line-height: 1.14;
            text-wrap: balance;
        }

        #<?= $componentId ?> .cpe-copy p {
            margin: 0;
            color: <?= htmlspecialchars($cardDescColor) ?>;
            font-size: <?= $cardDescFontSize ?>px;
            line-height: 1.82;
        }

        #<?= $componentId ?> .cpe-token-row {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        #<?= $componentId ?> .cpe-token-row span {
            display: inline-flex;
            align-items: center;
            min-height: 34px;
            padding: 0 14px;
            border-radius: 999px;
            background: rgba(255,255,255,0.07);
            border: 1px solid rgba(255,255,255,0.12);
            color: #ffffff;
            font-size: 12px;
            font-weight: 700;
        }

        #<?= $componentId ?> .cpe-media {
            position: relative;
            overflow: hidden;
            border-radius: calc(<?= $cardBorderRadius ?>px - 4px);
            min-height: 240px;
            background: rgba(255,255,255,0.06);
        }

        #<?= $componentId ?> .cpe-ticket--b .cpe-media {
            min-height: 210px;
            order: -1;
        }

        #<?= $componentId ?> .cpe-ticket--c .cpe-media {
            min-height: 160px;
            max-height: 180px;
            max-width: 280px;
        }

        #<?= $componentId ?> .cpe-media::after {
            content: "";
            position: absolute;
            inset: auto 14px 14px auto;
            width: 54px;
            height: 54px;
            border-radius: 50%;
            background: radial-gradient(circle at 30% 30%, rgba(255,255,255,0.36), rgba(255,255,255,0.04));
            backdrop-filter: blur(8px);
            border: 1px solid rgba(255,255,255,0.14);
        }

        #<?= $componentId ?> .cpe-media picture,
        #<?= $componentId ?> .cpe-media img {
            display: block;
            width: 100%;
            height: 100%;
        }

        #<?= $componentId ?> .cpe-media img {
            object-fit: cover;
        }

        @media (max-width: 991px) {
            #<?= $componentId ?> .cpe-stage {
                grid-template-columns: 1fr;
            }

            #<?= $componentId ?> .cpe-head-card {
                grid-column: auto;
                grid-row: auto;
                text-align: left;
            }

            #<?= $componentId ?> .cpe-rail--left,
            #<?= $componentId ?> .cpe-ticket--c {
                grid-column: auto;
                grid-row: auto;
            }

            #<?= $componentId ?> .cpe-head-top,
            #<?= $componentId ?> .cpe-tags {
                justify-content: flex-start;
            }

            #<?= $componentId ?> .cpe-head-card h2,
            #<?= $componentId ?> .cpe-summary {
                margin-left: 0;
            }

            #<?= $componentId ?> .cpe-ticket-body,
            #<?= $componentId ?> .cpe-ticket--b .cpe-ticket-body,
            #<?= $componentId ?> .cpe-ticket--c .cpe-ticket-body {
                grid-template-columns: 1fr;
            }

            #<?= $componentId ?> .cpe-ticket--b .cpe-media {
                order: 0;
            }
        }

        @media (max-width: 767px) {
            #<?= $componentId ?> {
                padding: <?= $sectionPaddingMobile ?>px 0;
            }

            #<?= $componentId ?> .cpe-wrap {
                width: min(100% - 24px, 1220px);
            }

            #<?= $componentId ?> .cpe-stage {
                gap: 18px;
            }

            #<?= $componentId ?> .cpe-head-card {
                padding: 8px 0 18px;
                text-align: left;
                width: 100%;
                margin-left: 0;
                transform: none;
            }

            #<?= $componentId ?> .cpe-head-card::before {
                inset: auto auto 4px 0;
                width: min(132px, 46%);
                height: 12px;
            }

            #<?= $componentId ?> .cpe-head-top,
            #<?= $componentId ?> .cpe-tags {
                justify-content: flex-start;
            }

            #<?= $componentId ?> .cpe-head-card h2,
            #<?= $componentId ?> .cpe-summary {
                margin-left: 0;
            }

            #<?= $componentId ?> .cpe-rail--left,
            #<?= $componentId ?> .cpe-ticket--c {
                gap: 14px;
            }

            #<?= $componentId ?> .cpe-rail--left {
                gap: 14px;
            }

            #<?= $componentId ?> .cpe-rail--left .cpe-ticket:nth-child(odd) {
                width: min(94%, 100%);
                margin-right: auto;
                transform: rotate(1.2deg);
            }

            #<?= $componentId ?> .cpe-rail--left .cpe-ticket:nth-child(even) {
                width: min(90%, 100%);
                margin-left: auto;
                transform: rotate(-1deg);
            }

            #<?= $componentId ?> .cpe-ticket--c {
                width: min(92%, 100%);
                margin-right: auto;
                transform: rotate(1deg);
            }

            #<?= $componentId ?> .cpe-ticket {
                padding: 18px;
                min-height: 100%;
                gap: 16px;
            }

            #<?= $componentId ?> .cpe-ticket-top {
                grid-template-columns: auto 1fr;
            }

            #<?= $componentId ?> .cpe-emoji {
                display: none;
            }

            #<?= $componentId ?> .cpe-copy h3 {
                font-size: 19px;
            }

            #<?= $componentId ?> .cpe-token-row {
                gap: 8px;
            }

            #<?= $componentId ?> .cpe-token-row span {
                min-height: 30px;
                padding: 0 12px;
                font-size: 11px;
            }

            #<?= $componentId ?> .cpe-ticket--b .cpe-media {
                order: 0;
            }

            #<?= $componentId ?> .cpe-media {
                min-height: 220px;
            }

            #<?= $componentId ?> .cpe-ticket--c .cpe-media {
                min-height: 220px;
            }
        }
    </style>

    <div class="cpe-wrap">
        <div class="cpe-stage">
            <header class="cpe-head-card">
                <div class="cpe-head-top">
                    <span class="cpe-track">Mode atlas</span>
                    <div class="cpe-suit-row" aria-hidden="true">
                        <span>♠</span>
                        <span>♥</span>
                        <span>♣</span>
                    </div>
                </div>
                <?php if ($showTitle): ?>
                <h2 id="<?= $componentId ?>-title"><?= htmlspecialchars($title) ?></h2>
                <?php endif; ?>
                <?php if ($summary !== ''): ?>
                <p class="cpe-summary"><?= htmlspecialchars($summary) ?></p>
                <?php endif; ?>
                <div class="cpe-tags" aria-label="Game mode highlights">
                    <?php foreach ($cards as $card): ?>
                    <span><b><?= htmlspecialchars($card['emoji']) ?></b><?= htmlspecialchars($card['tag']) ?></span>
                    <?php endforeach; ?>
                </div>
            </header>

            <div class="cpe-rail cpe-rail--left" aria-label="Court Piece left game lane">
                <?php foreach ($leftCards as $index => $card): ?>
                <article class="cpe-ticket <?= $index === 0 ? 'cpe-ticket--a' : 'cpe-ticket--b' ?>">
                    <div class="cpe-ticket-top">
                        <span class="cpe-index"><?= htmlspecialchars($card['index']) ?></span>
                        <span class="cpe-tag"><?= htmlspecialchars($card['tag']) ?></span>
                        <span class="cpe-emoji" aria-hidden="true"><?= htmlspecialchars($card['emoji']) ?></span>
                    </div>

                    <div class="cpe-ticket-body">
                        <div class="cpe-copy">
                            <div>
                                <h3><?= htmlspecialchars((string) $card['title']) ?></h3>
                                <?php if ($showDescription): ?>
                                <p><?= htmlspecialchars((string) $card['desc']) ?></p>
                                <?php endif; ?>
                            </div>

                            <div class="cpe-token-row" aria-label="<?= htmlspecialchars((string) $card['title']) ?> highlights">
                                <?php foreach ($card['tokens'] as $token): ?>
                                <span><?= htmlspecialchars($token) ?></span>
                                <?php endforeach; ?>
                            </div>
                        </div>

                        <figure class="cpe-media">
                            <picture>
                                <source media="(max-width: 768px)" srcset="<?= htmlspecialchars((string) $card['mobile']) ?>">
                                <source media="(min-width: 769px)" srcset="<?= htmlspecialchars((string) $card['desktop']) ?>">
                                <img src="<?= htmlspecialchars((string) $card['desktop']) ?>" alt="<?= htmlspecialchars((string) $card['title']) ?>"<?= $index === 0 ? '' : ' loading="lazy" decoding="async"' ?>>
                            </picture>
                        </figure>
                    </div>
                </article>
                <?php endforeach; ?>
            </div>

            <?php if ($rightCard): ?>
            <article class="cpe-ticket cpe-ticket--c">
                <div class="cpe-ticket-top">
                    <span class="cpe-index"><?= htmlspecialchars($rightCard['index']) ?></span>
                    <span class="cpe-tag"><?= htmlspecialchars($rightCard['tag']) ?></span>
                    <span class="cpe-emoji" aria-hidden="true"><?= htmlspecialchars($rightCard['emoji']) ?></span>
                </div>

                <div class="cpe-ticket-body">
                    <div class="cpe-copy">
                        <div>
                            <h3><?= htmlspecialchars((string) $rightCard['title']) ?></h3>
                            <?php if ($showDescription): ?>
                            <p><?= htmlspecialchars((string) $rightCard['desc']) ?></p>
                            <?php endif; ?>
                        </div>

                        <div class="cpe-token-row" aria-label="<?= htmlspecialchars((string) $rightCard['title']) ?> highlights">
                            <?php foreach ($rightCard['tokens'] as $token): ?>
                            <span><?= htmlspecialchars($token) ?></span>
                            <?php endforeach; ?>
                        </div>
                    </div>

                    <figure class="cpe-media">
                        <picture>
                            <source media="(max-width: 768px)" srcset="<?= htmlspecialchars((string) $rightCard['mobile']) ?>">
                            <source media="(min-width: 769px)" srcset="<?= htmlspecialchars((string) $rightCard['desktop']) ?>">
                            <img src="<?= htmlspecialchars((string) $rightCard['desktop']) ?>" alt="<?= htmlspecialchars((string) $rightCard['title']) ?>" loading="lazy" decoding="async">
                        </picture>
                    </figure>
                </div>
            </article>
            <?php endif; ?>
        </div>
    </div>
</section>
