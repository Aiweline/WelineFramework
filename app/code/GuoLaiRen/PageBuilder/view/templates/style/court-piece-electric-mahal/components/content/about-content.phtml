<?php
/**
 * 关于我们内容组件
 *
 * @component_start
 * code: about-content
 * name: 关于我们内容
 * name_en: About Content
 * description: 企业/品牌关于页面内容展示组件，继承主题配色
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_stats => 显示数据统计:select:yes|yes,no
 * display.show_values => 显示价值观:select:yes|yes,no
 * display.show_team => 显示团队:select:yes|yes,no
 * display.show_cta => 显示底部CTA:select:yes|yes,no
 *
 * group:hero => 顶部横幅
 * hero.title => 页面标题:text:Inside Court Piece Electric Mahal
 * hero.subtitle => 页面副标题:textarea:Step into a high-voltage card arena built for quick rounds, bold table energy, and a smoother mobile-first play experience.
 * hero.bg_color => 背景色:color:#0c0060
 * hero.bg_gradient_end => 渐变结束色:color:#1d1850
 * hero.title_color => 标题颜色:color:#ffd700
 * hero.text_color => 文字颜色:color:#cccccc
 * hero.padding => 内边距:number:80|px
 *
 * group:stats => 数据统计
 * stats.stat_1_number => 统计1数字:text:4 Seats
 * stats.stat_1_label => 统计1标签:text:team play focus
 * stats.stat_2_number => 统计2数字:text:3 Modes
 * stats.stat_2_label => 统计2标签:text:match energy zones
 * stats.stat_3_number => 统计3数字:text:24 7
 * stats.stat_3_label => 统计3标签:text:table access
 * stats.stat_4_number => 统计4数字:text:100 pct
 * stats.stat_4_label => 统计4标签:text:mobile ready
 * stats.number_color => 数字颜色:color:#ffd700
 * stats.label_color => 标签颜色:color:#8b8b8b
 * stats.card_bg => 卡片背景:color:rgba(255,255,255,0.05)
 *
 * group:story => 故事区块
 * story.title => 标题:text:Why Players Remember The Mahal
 * story.subtitle => 副标题:text:A louder home base for fast court-piece sessions
 * story.content => 内容:textarea:Court Piece Electric Mahal turns classic partnership card play into a brighter, more cinematic destination. The experience leans into sharp table chemistry, rapid entry, and strong visual contrast so every section feels alive instead of flat.
 * story.rail_1_title => 侧边信息1标题:text:Built for
 * story.rail_1_text => 侧边信息1内容:text:Partnership rounds with quick reading and bold table pacing
 * story.rail_2_title => 侧边信息2标题:text:Known for
 * story.rail_2_text => 侧边信息2内容:text:Electric contrast, dramatic motion, and clearer mobile scanning
 * story.rail_3_title => 侧边信息3标题:text:Perfect for
 * story.rail_3_text => 侧边信息3内容:text:Players who want style, speed, and a memorable court-piece vibe
 * story.bg_color => 背景色:color:#0d0e13
 * story.title_color => 标题颜色:color:#ffd700
 *
 * group:values => 价值观区块
 * values.title => 标题:text:What Shapes The Experience
 * values.subtitle => 副标题:text:The ideas behind every table-facing detail
 * values.value_1_icon => 价值1图标:text:♠
 * values.value_1_title => 价值1标题:text:Clear match flow
 * values.value_1_desc => 价值1描述:textarea:From headline order to section rhythm, every block helps players understand the app quickly and move toward download with less friction.
 * values.value_2_icon => 价值2图标:text:♦
 * values.value_2_title => 价值2标题:text:Contrast with intent
 * values.value_2_desc => 价值2描述:textarea:Each module shifts tone, depth, and color pressure so the page feels staged like a live card room rather than a recycled landing page.
 * values.value_3_icon => 价值3图标:text:♣
 * values.value_3_title => 价值3标题:text:Responsive impact
 * values.value_3_desc => 价值3描述:textarea:The layout keeps its personality on small screens, with content blocks that still feel intentional, dramatic, and easy to scan.
 * values.bg_color => 背景色:color:#1d1850
 *
 * group:team => 团队区块
 * team.title => 标题:text:Who Makes The Table Glow
 * team.subtitle => 副标题:text:The crew behind the mahal atmosphere
 * team.member_1_icon => 成员1头像:text:🎯
 * team.member_1_name => 成员1姓名:text:Match Starter
 * team.member_1_role => 成员1职位:text:Guides new players from first glance to first download
 * team.member_2_icon => 成员2头像:text:🧠
 * team.member_2_name => 成员2姓名:text:Table Strategist
 * team.member_2_role => 成员2职位:text:Keeps every content block sharp, readable, and game-focused
 * team.member_3_icon => 成员3头像:text:🎨
 * team.member_3_name => 成员3姓名:text:Light Director
 * team.member_3_role => 成员3职位:text:Pushes glow, contrast, and dramatic mahal mood across the page
 * team.member_4_icon => 成员4头像:text:📱
 * team.member_4_name => 成员4姓名:text:Mobile Dealer
 * team.member_4_role => 成员4职位:text:Keeps the handheld layout tight, fast, and visually punchy
 *
 * group:cta => 底部CTA
 * cta.title => CTA标题:text:Ready To Join The Electric Table
 * cta.description => CTA描述:textarea:Move from story to action and connect with the Court Piece Electric Mahal team for updates, support, and the next big round.
 * cta.button_text => 按钮文字:text:Go To Contact Page
 * cta.button_url => 按钮链接:text:/contact
 * cta.button_bg => 按钮背景:color:linear-gradient(90deg, #ffd700 0%, #ffa500 100%)
 * cta.button_text_color => 按钮文字颜色:color:#8b4513
 *
 * @fields_end
 */

$config = $component_config ?? [];
$page = $this->getData('page');
$colors = isset($colors) && is_array($colors) ? $colors : [];

$showStats = ($config['display.show_stats'] ?? 'yes') !== 'no';
$showValues = ($config['display.show_values'] ?? 'yes') !== 'no';
$showTeam = ($config['display.show_team'] ?? 'yes') !== 'no';
$showCta = ($config['display.show_cta'] ?? 'yes') !== 'no';

$heroTitle = $config['hero.title'] ?? ($page ? ($page->getData('title') ?? 'Inside Court Piece Electric Mahal') : 'Inside Court Piece Electric Mahal');
$heroSubtitle = $config['hero.subtitle'] ?? 'Step into a high-voltage card arena built for quick rounds, bold table energy, and a smoother mobile-first play experience.';
$heroBg = $config['hero.bg_color'] ?? ($colors['hero_bg'] ?? '#2b0f54');
$heroGradientEnd = $config['hero.bg_gradient_end'] ?? ($colors['hero_bg_gradient_end'] ?? '#0b6a72');
$heroTitleColor = $config['hero.title_color'] ?? ($colors['accent_primary'] ?? '#ffd166');
$heroTextColor = $config['hero.text_color'] ?? ($colors['text_secondary'] ?? '#e6dff6');
$heroPadding = (int) ($config['hero.padding'] ?? '88');

$stats = [
    ['number' => $config['stats.stat_1_number'] ?? '4 Seats', 'label' => $config['stats.stat_1_label'] ?? 'team play focus'],
    ['number' => $config['stats.stat_2_number'] ?? '3 Modes', 'label' => $config['stats.stat_2_label'] ?? 'match energy zones'],
    ['number' => $config['stats.stat_3_number'] ?? '24 7', 'label' => $config['stats.stat_3_label'] ?? 'table access'],
    ['number' => $config['stats.stat_4_number'] ?? '100 pct', 'label' => $config['stats.stat_4_label'] ?? 'mobile ready'],
];

$storyTitle = $config['story.title'] ?? 'Why Players Remember The Mahal';
$storySubtitle = $config['story.subtitle'] ?? 'A louder home base for fast court-piece sessions';
$storyContent = $config['story.content'] ?? 'Court Piece Electric Mahal turns classic partnership card play into a brighter, more cinematic destination. The experience leans into sharp table chemistry, rapid entry, and strong visual contrast so every section feels alive instead of flat.';
$storyRail = [
    ['title' => $config['story.rail_1_title'] ?? 'Built for', 'text' => $config['story.rail_1_text'] ?? 'Partnership rounds with quick reading and bold table pacing'],
    ['title' => $config['story.rail_2_title'] ?? 'Known for', 'text' => $config['story.rail_2_text'] ?? 'Electric contrast, dramatic motion, and clearer mobile scanning'],
    ['title' => $config['story.rail_3_title'] ?? 'Perfect for', 'text' => $config['story.rail_3_text'] ?? 'Players who want style, speed, and a memorable court-piece vibe'],
];
$storyBg = $config['story.bg_color'] ?? ($colors['section_bg_1'] ?? '#140a22');
$storyTitleColor = $config['story.title_color'] ?? ($colors['accent_primary'] ?? '#ffd166');

$valuesTitle = $config['values.title'] ?? 'What Shapes The Experience';
$valuesSubtitle = $config['values.subtitle'] ?? 'The ideas behind every table-facing detail';
$valuesBg = $config['values.bg_color'] ?? ($colors['section_bg_2'] ?? '#2d1248');
$values = [
    ['icon' => $config['values.value_1_icon'] ?? '♠', 'title' => $config['values.value_1_title'] ?? 'Clear match flow', 'desc' => $config['values.value_1_desc'] ?? 'From headline order to section rhythm, every block helps players understand the app quickly and move toward download with less friction.'],
    ['icon' => $config['values.value_2_icon'] ?? '♦', 'title' => $config['values.value_2_title'] ?? 'Contrast with intent', 'desc' => $config['values.value_2_desc'] ?? 'Each module shifts tone, depth, and color pressure so the page feels staged like a live card room rather than a recycled landing page.'],
    ['icon' => $config['values.value_3_icon'] ?? '♣', 'title' => $config['values.value_3_title'] ?? 'Responsive impact', 'desc' => $config['values.value_3_desc'] ?? 'The layout keeps its personality on small screens, with content blocks that still feel intentional, dramatic, and easy to scan.'],
];

$teamTitle = $config['team.title'] ?? 'Who Makes The Table Glow';
$teamSubtitle = $config['team.subtitle'] ?? 'The crew behind the mahal atmosphere';
$team = [
    ['icon' => $config['team.member_1_icon'] ?? '🎯', 'name' => $config['team.member_1_name'] ?? 'Match Starter', 'role' => $config['team.member_1_role'] ?? 'Guides new players from first glance to first download'],
    ['icon' => $config['team.member_2_icon'] ?? '🧠', 'name' => $config['team.member_2_name'] ?? 'Table Strategist', 'role' => $config['team.member_2_role'] ?? 'Keeps every content block sharp, readable, and game-focused'],
    ['icon' => $config['team.member_3_icon'] ?? '🎨', 'name' => $config['team.member_3_name'] ?? 'Light Director', 'role' => $config['team.member_3_role'] ?? 'Pushes glow, contrast, and dramatic mahal mood across the page'],
    ['icon' => $config['team.member_4_icon'] ?? '📱', 'name' => $config['team.member_4_name'] ?? 'Mobile Dealer', 'role' => $config['team.member_4_role'] ?? 'Keeps the handheld layout tight, fast, and visually punchy'],
];

$ctaTitle = $config['cta.title'] ?? 'Ready To Join The Electric Table';
$ctaDescription = $config['cta.description'] ?? 'Move from story to action and connect with the Court Piece Electric Mahal team for updates, support, and the next big round.';
$ctaButtonText = $config['cta.button_text'] ?? 'Go To Contact Page';
$ctaButtonUrl = $config['cta.button_url'] ?? '/contact';
$ctaButtonBg = $config['cta.button_bg'] ?? ($colors['button_primary_bg'] ?? 'linear-gradient(120deg, #ffd166 0%, #ff7a3c 52%, #ff477e 100%)');
$ctaButtonTextColor = $config['cta.button_text_color'] ?? ($colors['button_primary_text'] ?? '#351500');
$statsNumberColor = $config['stats.number_color'] ?? ($colors['accent_primary'] ?? '#ffd166');
$statsLabelColor = $config['stats.label_color'] ?? ($colors['text_muted'] ?? '#ad9fc4');
$statsCardBg = $config['stats.card_bg'] ?? ($colors['card_bg'] ?? 'rgba(255,255,255,0.08)');
$instanceId = $component_instance_id ?? 'about-content-' . uniqid();
?>
<section id="<?= $instanceId ?>" class="cpe-about-page">
    <style>
        #<?= $instanceId ?> .cpe-hero {
            padding: <?= $heroPadding ?>px 0 72px;
            background:
                radial-gradient(circle at 18% 16%, rgba(255,111,60,0.18), transparent 24%),
                linear-gradient(135deg, <?= htmlspecialchars($heroBg) ?> 0%, <?= htmlspecialchars($heroGradientEnd) ?> 100%);
            text-align: center;
        }
        #<?= $instanceId ?> .cpe-shell { width: min(100% - 32px, 1180px); margin: 0 auto; }
        #<?= $instanceId ?> .cpe-hero h1,
        #<?= $instanceId ?> h2 {
            margin: 0;
            font-family: "Palatino Linotype", "Book Antiqua", Georgia, serif;
            letter-spacing: -0.03em;
        }
        #<?= $instanceId ?> .cpe-hero h1 {
            color: <?= htmlspecialchars($heroTitleColor) ?>;
            font-size: clamp(42px, 7vw, 76px);
        }
        #<?= $instanceId ?> .cpe-hero p {
            max-width: 760px;
            margin: 18px auto 0;
            color: <?= htmlspecialchars($heroTextColor) ?>;
            font-size: 18px;
            line-height: 1.85;
        }
        #<?= $instanceId ?> .cpe-stats {
            display: grid;
            grid-template-columns: repeat(4, minmax(0, 1fr));
            gap: 18px;
            margin-top: -34px;
        }
        #<?= $instanceId ?> .cpe-stat {
            padding: 22px;
            border-radius: 24px;
            background: <?= htmlspecialchars($statsCardBg) ?>;
            border: 1px solid rgba(255,255,255,0.12);
            text-align: center;
            box-shadow: 0 28px 56px rgba(0,0,0,0.18);
        }
        #<?= $instanceId ?> .cpe-stat strong {
            display: block;
            margin-bottom: 6px;
            color: <?= htmlspecialchars($statsNumberColor) ?>;
            font-size: 28px;
            font-weight: 800;
            text-transform: uppercase;
        }
        #<?= $instanceId ?> .cpe-stat span { color: <?= htmlspecialchars($statsLabelColor) ?>; font-size: 13px; text-transform: uppercase; letter-spacing: 0.12em; }
        #<?= $instanceId ?> .cpe-story,
        #<?= $instanceId ?> .cpe-values,
        #<?= $instanceId ?> .cpe-team,
        #<?= $instanceId ?> .cpe-cta {
            padding: 72px 0;
        }
        #<?= $instanceId ?> .cpe-story { background: <?= htmlspecialchars($storyBg) ?>; }
        #<?= $instanceId ?> .cpe-values { background: <?= htmlspecialchars($valuesBg) ?>; }
        #<?= $instanceId ?> .cpe-team { background: linear-gradient(180deg, #12081d 0%, #1c1030 100%); }
        #<?= $instanceId ?> .cpe-cta { background: linear-gradient(180deg, #0f0718 0%, #09050f 100%); }
        #<?= $instanceId ?> .cpe-section-head { display: grid; gap: 12px; margin-bottom: 28px; max-width: 760px; }
        #<?= $instanceId ?> .cpe-section-head h2 { color: <?= htmlspecialchars($storyTitleColor) ?>; font-size: clamp(30px, 5vw, 52px); }
        #<?= $instanceId ?> .cpe-section-head p { margin: 0; color: #e6dff6; font-size: 17px; line-height: 1.8; }
        #<?= $instanceId ?> .cpe-story-grid {
            display: grid;
            grid-template-columns: minmax(0, 1.1fr) minmax(240px, 0.9fr);
            gap: 26px;
            align-items: start;
        }
        #<?= $instanceId ?> .cpe-story-panel,
        #<?= $instanceId ?> .cpe-value,
        #<?= $instanceId ?> .cpe-member,
        #<?= $instanceId ?> .cpe-cta-panel {
            padding: 26px;
            border-radius: 28px;
            background: rgba(255,255,255,0.08);
            border: 1px solid rgba(255,255,255,0.12);
            box-shadow: 0 28px 56px rgba(0,0,0,0.18);
        }
        #<?= $instanceId ?> .cpe-story-panel p { margin: 0; color: #efe6ff; line-height: 1.9; font-size: 16px; }
        #<?= $instanceId ?> .cpe-story-rail { display: grid; gap: 14px; }
        #<?= $instanceId ?> .cpe-story-rail div { padding: 18px; border-radius: 22px; background: rgba(255,255,255,0.06); color: #fff0af; }
        #<?= $instanceId ?> .cpe-story-rail strong { display: block; margin-bottom: 6px; letter-spacing: 0.12em; text-transform: uppercase; font-size: 11px; color: #6ef3ff; }
        #<?= $instanceId ?> .cpe-values-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 22px; }
        #<?= $instanceId ?> .cpe-value .icon,
        #<?= $instanceId ?> .cpe-member .icon {
            width: 56px;
            height: 56px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: 18px;
            background: rgba(255,255,255,0.08);
            margin-bottom: 16px;
            font-size: 28px;
        }
        #<?= $instanceId ?> .cpe-value h3,
        #<?= $instanceId ?> .cpe-member h3 { margin: 0 0 10px; color: #fff0af; font-size: 22px; }
        #<?= $instanceId ?> .cpe-value p,
        #<?= $instanceId ?> .cpe-member p { margin: 0; color: #e6dff6; line-height: 1.8; }
        #<?= $instanceId ?> .cpe-team-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 18px; }
        #<?= $instanceId ?> .cpe-member .role { color: #6ef3ff; font-size: 12px; letter-spacing: 0.14em; text-transform: uppercase; margin-top: 10px; }
        #<?= $instanceId ?> .cpe-cta-panel { display: grid; gap: 16px; max-width: 820px; }
        #<?= $instanceId ?> .cpe-cta-panel p { margin: 0; color: #e6dff6; line-height: 1.85; }
        #<?= $instanceId ?> .cpe-cta-panel a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: fit-content;
            min-height: 54px;
            padding: 14px 24px;
            border-radius: 999px;
            background: <?= htmlspecialchars($ctaButtonBg) ?>;
            color: <?= htmlspecialchars($ctaButtonTextColor) ?>;
            font-weight: 800;
            letter-spacing: 0.04em;
            text-transform: uppercase;
        }
        @media (max-width: 991px) {
            #<?= $instanceId ?> .cpe-stats,
            #<?= $instanceId ?> .cpe-values-grid,
            #<?= $instanceId ?> .cpe-team-grid,
            #<?= $instanceId ?> .cpe-story-grid { grid-template-columns: 1fr 1fr; }
        }
        @media (max-width: 767px) {
            #<?= $instanceId ?> .cpe-shell { width: min(100% - 24px, 1180px); }
            #<?= $instanceId ?> .cpe-stats,
            #<?= $instanceId ?> .cpe-values-grid,
            #<?= $instanceId ?> .cpe-team-grid,
            #<?= $instanceId ?> .cpe-story-grid { grid-template-columns: 1fr; }
            #<?= $instanceId ?> .cpe-hero,
            #<?= $instanceId ?> .cpe-story,
            #<?= $instanceId ?> .cpe-values,
            #<?= $instanceId ?> .cpe-team,
            #<?= $instanceId ?> .cpe-cta { padding: 48px 0; }
        }
    </style>

    <header class="cpe-hero">
        <div class="cpe-shell" data-cpe-reveal>
            <h1><?= htmlspecialchars($heroTitle) ?></h1>
            <p><?= htmlspecialchars($heroSubtitle) ?></p>
        </div>
    </header>

    <?php if ($showStats): ?>
    <div class="cpe-shell">
        <div class="cpe-stats" data-cpe-reveal>
            <?php foreach ($stats as $stat): ?>
            <div class="cpe-stat">
                <strong><?= htmlspecialchars($stat['number']) ?></strong>
                <span><?= htmlspecialchars($stat['label']) ?></span>
            </div>
            <?php endforeach; ?>
        </div>
    </div>
    <?php endif; ?>

    <section class="cpe-story">
        <div class="cpe-shell">
            <div class="cpe-section-head" data-cpe-reveal>
                <h2><?= htmlspecialchars($storyTitle) ?></h2>
                <p><?= htmlspecialchars($storySubtitle) ?></p>
            </div>
            <div class="cpe-story-grid">
                <article class="cpe-story-panel" data-cpe-reveal>
                    <p><?= nl2br(htmlspecialchars($storyContent)) ?></p>
                </article>
                <aside class="cpe-story-rail" data-cpe-reveal>
                    <?php foreach ($storyRail as $rail): ?>
                    <div><strong><?= htmlspecialchars($rail['title']) ?></strong><?= htmlspecialchars($rail['text']) ?></div>
                    <?php endforeach; ?>
                </aside>
            </div>
        </div>
    </section>

    <?php if ($showValues): ?>
    <section class="cpe-values">
        <div class="cpe-shell">
            <div class="cpe-section-head" data-cpe-reveal>
                <h2><?= htmlspecialchars($valuesTitle) ?></h2>
                <p><?= htmlspecialchars($valuesSubtitle) ?></p>
            </div>
            <div class="cpe-values-grid">
                <?php foreach ($values as $value): ?>
                <article class="cpe-value" data-cpe-reveal>
                    <span class="icon"><?= htmlspecialchars($value['icon']) ?></span>
                    <h3><?= htmlspecialchars($value['title']) ?></h3>
                    <p><?= htmlspecialchars($value['desc']) ?></p>
                </article>
                <?php endforeach; ?>
            </div>
        </div>
    </section>
    <?php endif; ?>

    <?php if ($showTeam): ?>
    <section class="cpe-team">
        <div class="cpe-shell">
            <div class="cpe-section-head" data-cpe-reveal>
                <h2><?= htmlspecialchars($teamTitle) ?></h2>
                <p><?= htmlspecialchars($teamSubtitle) ?></p>
            </div>
            <div class="cpe-team-grid">
                <?php foreach ($team as $member): ?>
                <article class="cpe-member" data-cpe-reveal>
                    <span class="icon"><?= htmlspecialchars($member['icon']) ?></span>
                    <h3><?= htmlspecialchars($member['name']) ?></h3>
                    <p><?= htmlspecialchars($member['role']) ?></p>
                    <div class="role">Theme role</div>
                </article>
                <?php endforeach; ?>
            </div>
        </div>
    </section>
    <?php endif; ?>

    <?php if ($showCta): ?>
    <section class="cpe-cta">
        <div class="cpe-shell">
            <div class="cpe-cta-panel" data-cpe-reveal>
                <h2><?= htmlspecialchars($ctaTitle) ?></h2>
                <p><?= htmlspecialchars($ctaDescription) ?></p>
                <a href="<?= htmlspecialchars($ctaButtonUrl) ?>"><?= htmlspecialchars($ctaButtonText) ?></a>
            </div>
        </div>
    </section>
    <?php endif; ?>
</section>
