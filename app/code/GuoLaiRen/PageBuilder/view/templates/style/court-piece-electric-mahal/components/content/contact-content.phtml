<?php
/**
 * 联系我们内容组件
 *
 * @component_start
 * code: contact-content
 * name: 联系我们内容
 * name_en: Contact Content
 * description: 联系方式和表单展示组件，继承主题配色
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_map => 显示地图:select:yes|yes,no
 * display.show_form => 显示联系表单:select:yes|yes,no
 * display.show_social => 显示社交链接:select:yes|yes,no
 * display.show_info => 显示联系信息:select:yes|yes,no
 *
 * group:hero => 顶部横幅
 * hero.title => 页面标题:text:Get in touch with the table team
 * hero.subtitle => 页面副标题:textarea:Use this page to handle partner inquiries, campaign updates, or launch-specific creative requests.
 * hero.bg_color => 背景色:color:#0c0060
 * hero.bg_gradient_end => 渐变结束色:color:#1d1850
 * hero.title_color => 标题颜色:color:#ffd700
 *
 * group:contact_info => 联系信息
 * contact_info.address_label => 地址标题:text:Address
 * contact_info.address => 地址:textarea:Design desk Tower 7 Mumbai
 * contact_info.phone_label => 电话标题:text:Phone
 * contact_info.phone => 电话:text:+91 90000 12345
 * contact_info.email_label => 邮箱标题:text:Email
 * contact_info.email => 邮箱:text:hello@courtpiece.example
 * contact_info.work_hours_label => 工作时间标题:text:Hours
 * contact_info.work_hours => 工作时间:text:Mon to Sat 10 AM to 8 PM IST
 *
 * group:social => 社交媒体
 * social.wechat => 微信:text:
 * social.weibo => 微博:text:
 * social.facebook => Facebook:text:https://facebook.com
 * social.twitter => Twitter:text:https://x.com
 * social.linkedin => LinkedIn:text:https://linkedin.com
 *
 * group:form => 表单设置
 * form.title => 表单标题:text:Send a launch note
 * form.name_label => 姓名字段标题:text:Full name
 * form.email_label => 邮箱字段标题:text:Email address
 * form.subject_label => 主题字段标题:text:Subject
 * form.message_label => 消息字段标题:text:Project note
 * form.submit_text => 提交按钮:text:Send message
 * form.success_message => 成功提示:text:Thanks. Your note is queued for review.
 *
 * group:map => 地图设置
 * map.title => 地图标题:text:Map slot ready
 * map.embed_url => 地图嵌入链接:textarea:|百度地图或Google地图嵌入链接
 * map.height => 地图高度:number:400|px
 *
 * @fields_end
 */

$config = $component_config ?? [];
$page = $this->getData('page');
$colors = isset($colors) && is_array($colors) ? $colors : [];

$showMap = ($config['display.show_map'] ?? 'yes') !== 'no';
$showForm = ($config['display.show_form'] ?? 'yes') !== 'no';
$showSocial = ($config['display.show_social'] ?? 'yes') !== 'no';
$showInfo = ($config['display.show_info'] ?? 'yes') !== 'no';

$heroTitle = $config['hero.title'] ?? ($page ? ($page->getData('title') ?? 'Get in touch with the table team') : 'Get in touch with the table team');
$heroSubtitle = $config['hero.subtitle'] ?? 'Use this page to handle partner inquiries, campaign updates, or launch-specific creative requests.';
$heroBg = $config['hero.bg_color'] ?? ($colors['hero_bg'] ?? '#2b0f54');
$heroGradientEnd = $config['hero.bg_gradient_end'] ?? ($colors['hero_bg_gradient_end'] ?? '#0b6a72');
$heroTitleColor = $config['hero.title_color'] ?? ($colors['accent_primary'] ?? '#ffd166');

$contactAddress = $config['contact_info.address'] ?? 'Design desk Tower 7 Mumbai';
$contactAddressLabel = $config['contact_info.address_label'] ?? 'Address';
$contactPhone = $config['contact_info.phone'] ?? '+91 90000 12345';
$contactPhoneLabel = $config['contact_info.phone_label'] ?? 'Phone';
$contactEmail = $config['contact_info.email'] ?? 'hello@courtpiece.example';
$contactEmailLabel = $config['contact_info.email_label'] ?? 'Email';
$contactWorkHours = $config['contact_info.work_hours'] ?? 'Mon to Sat 10 AM to 8 PM IST';
$contactWorkHoursLabel = $config['contact_info.work_hours_label'] ?? 'Hours';

$socials = [
    ['label' => 'FB', 'url' => $config['social.facebook'] ?? 'https://facebook.com'],
    ['label' => 'X', 'url' => $config['social.twitter'] ?? 'https://x.com'],
    ['label' => 'IN', 'url' => $config['social.linkedin'] ?? 'https://linkedin.com'],
];

$formTitle = $config['form.title'] ?? 'Send a launch note';
$formNameLabel = $config['form.name_label'] ?? 'Full name';
$formEmailLabel = $config['form.email_label'] ?? 'Email address';
$formSubjectLabel = $config['form.subject_label'] ?? 'Subject';
$formMessageLabel = $config['form.message_label'] ?? 'Project note';
$formSubmitText = $config['form.submit_text'] ?? 'Send message';
$formSuccessMessage = $config['form.success_message'] ?? 'Thanks. Your note is queued for review.';
$mapTitle = $config['map.title'] ?? 'Map slot ready';
$mapEmbedUrl = $config['map.embed_url'] ?? '';
$mapHeight = (int) ($config['map.height'] ?? '400');
$instanceId = $component_instance_id ?? 'contact-content-' . uniqid();
?>
<section id="<?= $instanceId ?>" class="cpe-contact-page">
    <style>
        #<?= $instanceId ?> .cpe-hero {
            padding: 86px 0 64px;
            background:
                radial-gradient(circle at 82% 18%, rgba(110,243,255,0.14), transparent 24%),
                linear-gradient(135deg, <?= htmlspecialchars($heroBg) ?> 0%, <?= htmlspecialchars($heroGradientEnd) ?> 100%);
            text-align: center;
        }
        #<?= $instanceId ?> .cpe-shell { width: min(100% - 32px, 1180px); margin: 0 auto; }
        #<?= $instanceId ?> h1,
        #<?= $instanceId ?> h2,
        #<?= $instanceId ?> h3 { margin: 0; font-family: "Palatino Linotype", "Book Antiqua", Georgia, serif; }
        #<?= $instanceId ?> .cpe-hero h1 { color: <?= htmlspecialchars($heroTitleColor) ?>; font-size: clamp(40px, 7vw, 72px); letter-spacing: -0.03em; }
        #<?= $instanceId ?> .cpe-hero p { max-width: 720px; margin: 16px auto 0; color: #e6dff6; line-height: 1.85; font-size: 18px; }
        #<?= $instanceId ?> .cpe-main { padding: 64px 0; background: linear-gradient(180deg, #13091f 0%, #0b0712 100%); }
        #<?= $instanceId ?> .cpe-grid { display: grid; grid-template-columns: minmax(0, 0.95fr) minmax(0, 1.05fr); gap: 26px; align-items: start; }
        #<?= $instanceId ?> .cpe-panel {
            padding: 28px;
            border-radius: 28px;
            background: rgba(255,255,255,0.08);
            border: 1px solid rgba(255,255,255,0.12);
            box-shadow: 0 28px 56px rgba(0,0,0,0.18);
        }
        #<?= $instanceId ?> .cpe-info-grid { display: grid; gap: 16px; margin-top: 20px; }
        #<?= $instanceId ?> .cpe-info-item { display: grid; grid-template-columns: 52px 1fr; gap: 16px; align-items: start; }
        #<?= $instanceId ?> .cpe-chip {
            width: 52px; height: 52px; display: inline-flex; align-items: center; justify-content: center;
            border-radius: 18px; background: rgba(255,255,255,0.08); color: #fff0af; font-size: 22px;
        }
        #<?= $instanceId ?> .cpe-info-item strong { display: block; margin-bottom: 6px; color: #6ef3ff; font-size: 12px; letter-spacing: 0.12em; text-transform: uppercase; }
        #<?= $instanceId ?> .cpe-info-item p, #<?= $instanceId ?> .cpe-info-item a { margin: 0; color: #efe6ff; line-height: 1.8; text-decoration: none; }
        #<?= $instanceId ?> .cpe-socials { display: flex; gap: 12px; margin-top: 18px; }
        #<?= $instanceId ?> .cpe-socials a {
            width: 46px; height: 46px; display: inline-flex; align-items: center; justify-content: center;
            border-radius: 50%; border: 1px solid rgba(255,255,255,0.12); background: rgba(255,255,255,0.08);
            color: #fff0af; font-weight: 800; letter-spacing: 0.08em;
        }
        #<?= $instanceId ?> form { display: grid; gap: 14px; margin-top: 20px; }
        #<?= $instanceId ?> label { display: grid; gap: 8px; color: #fff0af; font-size: 13px; letter-spacing: 0.08em; text-transform: uppercase; }
        #<?= $instanceId ?> input,
        #<?= $instanceId ?> textarea {
            width: 100%;
            border: 1px solid rgba(255,255,255,0.12);
            border-radius: 18px;
            background: rgba(10,8,18,0.72);
            color: #fff9f1;
            padding: 16px 18px;
            font: inherit;
        }
        #<?= $instanceId ?> textarea { min-height: 150px; resize: vertical; }
        #<?= $instanceId ?> .cpe-submit {
            width: fit-content;
            min-height: 54px;
            padding: 14px 24px;
            border: 0;
            border-radius: 999px;
            background: linear-gradient(120deg, #ffd166 0%, #ff7a3c 52%, #ff477e 100%);
            color: #351500;
            font-weight: 800;
            letter-spacing: 0.04em;
            text-transform: uppercase;
        }
        #<?= $instanceId ?> .cpe-success { display: none; color: #6ef3ff; font-size: 14px; }
        #<?= $instanceId ?> .cpe-map {
            margin-top: 22px;
            border-radius: 24px;
            overflow: hidden;
            border: 1px solid rgba(255,255,255,0.12);
            min-height: <?= $mapHeight ?>px;
            background: linear-gradient(160deg, rgba(255,255,255,0.08), rgba(11, 40, 59, 0.86));
            display: grid;
            place-items: center;
            color: #e6dff6;
            text-align: center;
            padding: 24px;
        }
        #<?= $instanceId ?> .cpe-map iframe { width: 100%; height: <?= $mapHeight ?>px; border: 0; }
        @media (max-width: 991px) {
            #<?= $instanceId ?> .cpe-grid { grid-template-columns: 1fr; }
        }
        @media (max-width: 767px) {
            #<?= $instanceId ?> .cpe-shell { width: min(100% - 24px, 1180px); }
            #<?= $instanceId ?> .cpe-main, #<?= $instanceId ?> .cpe-hero { padding: 48px 0; }
            #<?= $instanceId ?> .cpe-submit { width: 100%; }
        }
    </style>

    <header class="cpe-hero">
        <div class="cpe-shell" data-cpe-reveal>
            <h1><?= htmlspecialchars($heroTitle) ?></h1>
            <p><?= htmlspecialchars($heroSubtitle) ?></p>
        </div>
    </header>

    <section class="cpe-main">
        <div class="cpe-shell cpe-grid">
            <?php if ($showInfo): ?>
            <article class="cpe-panel" data-cpe-reveal>
                <h2>Contact desk</h2>
                <div class="cpe-info-grid">
                    <div class="cpe-info-item"><span class="cpe-chip">📍</span><div><strong><?= htmlspecialchars($contactAddressLabel) ?></strong><p><?= nl2br(htmlspecialchars($contactAddress)) ?></p></div></div>
                    <div class="cpe-info-item"><span class="cpe-chip">☎</span><div><strong><?= htmlspecialchars($contactPhoneLabel) ?></strong><a href="tel:<?= htmlspecialchars($contactPhone) ?>"><?= htmlspecialchars($contactPhone) ?></a></div></div>
                    <div class="cpe-info-item"><span class="cpe-chip">✉</span><div><strong><?= htmlspecialchars($contactEmailLabel) ?></strong><a href="mailto:<?= htmlspecialchars($contactEmail) ?>"><?= htmlspecialchars($contactEmail) ?></a></div></div>
                    <div class="cpe-info-item"><span class="cpe-chip">⏱</span><div><strong><?= htmlspecialchars($contactWorkHoursLabel) ?></strong><p><?= htmlspecialchars($contactWorkHours) ?></p></div></div>
                </div>
                <?php if ($showSocial): ?>
                <div class="cpe-socials">
                    <?php foreach ($socials as $social): ?>
                    <?php if (!empty($social['url'])): ?><a href="<?= htmlspecialchars($social['url']) ?>" target="_blank" rel="noopener"><?= htmlspecialchars($social['label']) ?></a><?php endif; ?>
                    <?php endforeach; ?>
                </div>
                <?php endif; ?>
            </article>
            <?php endif; ?>

            <?php if ($showForm): ?>
            <article class="cpe-panel" data-cpe-reveal>
                <h2><?= htmlspecialchars($formTitle) ?></h2>
                <form novalidate>
                    <label><?= htmlspecialchars($formNameLabel) ?><input type="text" name="name" placeholder="Your name"></label>
                    <label><?= htmlspecialchars($formEmailLabel) ?><input type="email" name="email" placeholder="you@example.com"></label>
                    <label><?= htmlspecialchars($formSubjectLabel) ?><input type="text" name="subject" placeholder="Campaign or launch topic"></label>
                    <label><?= htmlspecialchars($formMessageLabel) ?><textarea name="message" placeholder="Tell us what you want this landing page to do."></textarea></label>
                    <button type="submit" class="cpe-submit"><?= htmlspecialchars($formSubmitText) ?></button>
                    <div class="cpe-success"><?= htmlspecialchars($formSuccessMessage) ?></div>
                </form>
                <?php if ($showMap): ?>
                <div class="cpe-map">
                    <?php if ($mapEmbedUrl): ?>
                    <iframe src="<?= htmlspecialchars($mapEmbedUrl) ?>" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
                    <?php else: ?>
                    <div>
                        <strong style="display:block;margin-bottom:10px;color:#fff0af;font-size:16px;"><?= htmlspecialchars($mapTitle) ?></strong>
                        Add a Google Maps or Baidu embed URL in the component config to replace this placeholder card.
                    </div>
                    <?php endif; ?>
                </div>
                <?php endif; ?>
            </article>
            <?php endif; ?>
        </div>
    </section>

    <script>
        (function () {
            var root = document.getElementById('<?= $instanceId ?>');
            if (!root) return;
            var form = root.querySelector('form');
            var success = root.querySelector('.cpe-success');
            if (!form || !success) return;
            form.addEventListener('submit', function (event) {
                event.preventDefault();
                success.style.display = 'block';
            });
        }());
    </script>
</section>
