<?php
/**
 * Court Piece Electric Mahal - Footer
 *
 * @fields_start
 *
 * group:style => 样式配置
 * style.background_color => 背景颜色:color:#0d0718
 * style.text_color => 文字颜色:color:#beaed8
 * style.link_color => 链接颜色:color:#f8ecff
 * style.link_hover_color => 链接悬停颜色:color:#6ef3ff
 * style.border_top => 显示顶部边框:select:yes|yes,no
 * style.border_color => 边框颜色:color:rgba(255,255,255,0.1)
 *
 * group:layout => 布局设置
 * layout.max_width => 最大宽度:number:1220
 * layout.padding_horizontal => 左右内边距:responsive:20/36|px[MD]
 * layout.padding_vertical => 上下内边距:responsive:24/42|px[MD]
 *
 * group:footer => 页脚品牌设置
 * footer.logo_text => 品牌名称:text:Court Piece Electric Mahal
 * footer.logo_url => 品牌Logo地址:textarea:|默认使用本地mahal-emblem.svg
 * footer.links => 页脚链接:textarea:Home|/,Blog|/blog,FAQ|/faqs,About|/about,Privacy|/privacy-policy
 * footer.copyright => 版权信息:text:© 2026 Court Piece Electric Mahal | All rights reserved
 * footer.disclaimer_text => 免责声明:textarea:18+ Only. Review local gaming and promotional rules before publishing.
 *
 * group:content => 内容配置
 * group:content_footer_labels => 页脚菜单显示名称（有对应页面时显示，留空使用页面标题）
 * content.footer_label_home_page => 首页:text:
 * content.footer_label_about_page => 关于我们:text:
 * content.footer_label_blog_list => 博客:text:
 * content.footer_label_contact_page => 联系我们:text:
 * content.footer_label_terms_of_service => 条款:text:
 * content.footer_label_privacy_policy => 隐私政策:text:
 *
 * group:typography => 排版设置
 * typography.font_size => 字体大小:responsive:12/14|px[MD]
 * typography.line_height => 行高:number:1.7
 *
 * @fields_end
 */

$page = $this->getData('page');
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];

$templateCode = $page ? $page->getData('style') : 'court-piece-electric-mahal';
if (empty($templateCode)) {
    $request = $this->getRequest();
    if ($request) {
        $templateCode = $request->getParam('style_code') ?: '';
    }
}
if (empty($templateCode)) {
    $templateCode = 'court-piece-electric-mahal';
}

$this->assign('template_code', $templateCode);
$this->assign('style', $styleSettings);
$this->assign('style_settings', $styleSettings);
$this->assign('component_config', is_array($componentConfig) ? $componentConfig : []);
$this->assign('__cpe_shell_managed', true);
$this->assign('__pagebuilder_shell_managed', true);
?>
<?= $this->fetchTemplate('GuoLaiRen_PageBuilder::style/court-piece-electric-mahal/components/footer/links.phtml') ?>
