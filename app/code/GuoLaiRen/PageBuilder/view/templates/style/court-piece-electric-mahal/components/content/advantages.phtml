<?php
/**
 * TPMST 优势展示组件 - Advantages Component
 *
 * @component_start
 * code: advantages
 * name: 优势展示
 * name_en: Advantages Section
 * description: 展示产品/应用的核心优势，三列卡片布局
 * category: content
 * region: content
 * thumbnail: asset/img/our-advantages/icon01.webp
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_section => 显示此区域:select:yes|yes,no
 * display.show_title => 显示标题:select:yes|yes,no
 * display.show_description => 显示描述:select:yes|yes,no
 * display.show_button => 显示按钮:select:yes|yes,no
 *
 * group:style => 样式配置
 * style.bg_color => 背景颜色:color:#1a1a2e
 * style.title_color => 标题颜色:color:#ffed4e
 * style.title_font_size => 标题字体大小(PC端):number:36|px
 * style.title_font_size_mobile => 标题字体大小(移动端):number:24|px
 * style.card_title_color => 卡片标题颜色:color:#ffed4e
 * style.card_title_font_size => 卡片标题字号:number:18|px
 * style.card_desc_color => 卡片描述颜色:color:#ffffff
 * style.card_desc_font_size => 卡片描述字号:number:14|px
 * style.icon_size => 图标尺寸:number:85|px
 * style.icon_border_radius => 图标圆角:number:50|%
 * style.section_padding => 区域内边距(PC端):number:60|px
 * style.section_padding_mobile => 区域内边距(移动端):number:40|px
 * style.card_gap => 卡片间距:number:30|px
 *
 * group:button => 按钮配置
 * button.text => 按钮文本:text:Download
 * button.url => 按钮链接:text:#download
 * button.bg_gradient_start => 按钮渐变起始色:color:#ffd700
 * button.bg_gradient_end => 按钮渐变结束色:color:#ffa500
 * button.text_color => 按钮文字颜色:color:#8b4513
 * button.border_radius => 按钮圆角:number:8|px
 * button.padding_x => 按钮水平内边距:number:40|px
 * button.padding_y => 按钮垂直内边距:number:12|px
 *
 * group:icons => 图标配置
 * icons.icon_1 => 优势1图标:textarea:|默认使用本地icon01.webp
 * icons.icon_2 => 优势2图标:textarea:|默认使用本地icon02.webp
 * icons.icon_3 => 优势3图标:textarea:|默认使用本地icon03.webp
 *
 * group:texts => 文本内容
 * texts.title => 区域标题:text:Why players stay in the room
 * texts.summary => 区域辅助文案:text:Three strategic routes shaped around trump control, partner rhythm, and brighter late-night table energy.
 * texts.advantage_1_title => 优势1标题:text:Trump Intelligence
 * texts.advantage_1_desc => 优势1描述:textarea:Call the right suit, read tempo early, and turn each trick into a control move instead of a guess.
 * texts.advantage_2_title => 优势2标题:text:Partner Sync
 * texts.advantage_2_desc => 优势2描述:textarea:Designed around four-seat rhythm so the table always feels collaborative, social, and tactically alive.
 * texts.advantage_3_title => 优势3标题:text:Festival Atmosphere
 * texts.advantage_3_desc => 优势3描述:textarea:High-contrast modules, bold reward framing, and richer motion cues make every session feel event-ready.
 *
 * @fields_end
 */

$componentId = 'court-piece-electric-mahal-advantages-' . uniqid();
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$config = array_merge(is_array($styleSettings) ? $styleSettings : [], is_array($componentConfig) ? $componentConfig : []);
$colors = isset($colors) && is_array($colors) ? $colors : [];

$getConfig = function ($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

if ($getConfig('display.show_section', $getConfig('advantages.display', 'yes')) === 'no') {
    return;
}

$showTitle = $getConfig('display.show_title', 'yes') !== 'no';
$showDescription = $getConfig('display.show_description', 'yes') !== 'no';
$showButton = $getConfig('display.show_button', 'yes') !== 'no';
$bgColor = $getConfig('style.bg_color', $colors['advantages_bg'] ?? '#fff0d8');
$titleColor = $getConfig('style.title_color', '#2d170b');
$titleFontSize = (int) $getConfig('style.title_font_size', '36');
$titleFontSizeMobile = (int) $getConfig('style.title_font_size_mobile', '24');
$cardTitleColor = $getConfig('style.card_title_color', '#2d170b');
$cardTitleFontSize = (int) $getConfig('style.card_title_font_size', '18');
$cardDescColor = $getConfig('style.card_desc_color', '#6b4b34');
$cardDescFontSize = (int) $getConfig('style.card_desc_font_size', '14');
$iconSize = (int) $getConfig('style.icon_size', '85');
$iconBorderRadius = (int) $getConfig('style.icon_border_radius', '50');
$sectionPadding = (int) $getConfig('style.section_padding', '72');
$sectionPaddingMobile = (int) $getConfig('style.section_padding_mobile', '44');
$cardGap = (int) $getConfig('style.card_gap', '26');
$btnText = $getConfig('button.text', 'Download');
$btnUrl = $getConfig('button.url', '#download');
$btnHref = \GuoLaiRen\PageBuilder\Helper\PageHelper::resolveAppDownloadUrl((string) $btnUrl);
$btnRef = \GuoLaiRen\PageBuilder\Helper\GlrDownloadRegistry::register($btnHref, 'url');
$btnGradientStart = $getConfig('button.bg_gradient_start', '#ffd166');
$btnGradientEnd = $getConfig('button.bg_gradient_end', '#ff7a3c');
$btnTextColor = $getConfig('button.text_color', '#351500');
$btnBorderRadius = (int) $getConfig('button.border_radius', '999');
$btnPaddingX = (int) $getConfig('button.padding_x', '34');
$btnPaddingY = (int) $getConfig('button.padding_y', '14');

$defaultIcon1 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/court-piece-electric-mahal/asset/img/our-advantages/icon01.webp');
$defaultIcon2 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/court-piece-electric-mahal/asset/img/our-advantages/icon02.webp');
$defaultIcon3 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/court-piece-electric-mahal/asset/img/our-advantages/icon03.webp');
$items = [
    [
        'eyebrow' => '01',
        'icon' => $getConfig('icons.icon_1', $getConfig('advantages.icon_1', '')) ?: $defaultIcon1,
        'title' => $getConfig('texts.advantage_1_title', 'Trump Intelligence'),
        'desc' => $getConfig('texts.advantage_1_desc', 'Call the right suit, read tempo early, and turn each trick into a control move instead of a guess.'),
    ],
    [
        'eyebrow' => '02',
        'icon' => $getConfig('icons.icon_2', $getConfig('advantages.icon_2', '')) ?: $defaultIcon2,
        'title' => $getConfig('texts.advantage_2_title', 'Partner Sync'),
        'desc' => $getConfig('texts.advantage_2_desc', 'Designed around four-seat rhythm so the table always feels collaborative, social, and tactically alive.'),
    ],
    [
        'eyebrow' => '03',
        'icon' => $getConfig('icons.icon_3', $getConfig('advantages.icon_3', '')) ?: $defaultIcon3,
        'title' => $getConfig('texts.advantage_3_title', 'Festival Atmosphere'),
        'desc' => $getConfig('texts.advantage_3_desc', 'High-contrast modules, bold reward framing, and richer motion cues make every session feel event-ready.'),
    ],
];
$title = $getConfig('texts.title', $getConfig('advantages.title', 'Why players stay in the room'));
$summary = trim((string) $getConfig('texts.summary', $getConfig('texts.advantages_summary', 'Three strategic routes shaped around trump control, partner rhythm, and brighter late-night table energy.')));
?>
<section id="<?= $componentId ?>" class="cpe-advantages" aria-labelledby="<?= $componentId ?>-title">
    <style>
        #<?= $componentId ?> {
            position: relative;
            padding: <?= $sectionPadding ?>px 0;
            background:
                radial-gradient(circle at 14% 18%, rgba(255,255,255,0.48), transparent 18%),
                radial-gradient(circle at 86% 16%, rgba(255,173,94,0.24), transparent 24%),
                linear-gradient(135deg, <?= htmlspecialchars($bgColor) ?> 0%, #ffe7c5 46%, #ffc889 100%);
            overflow: hidden;
        }

        #<?= $componentId ?>::before {
            content: "";
            position: absolute;
            inset: 0;
            background:
                linear-gradient(90deg, rgba(255,255,255,0.12) 0%, rgba(255,255,255,0) 18%, rgba(255,255,255,0.08) 52%, rgba(255,255,255,0) 100%),
                repeating-linear-gradient(128deg, rgba(133,68,29,0.08) 0 1px, transparent 1px 30px);
            opacity: 0.2;
            pointer-events: none;
        }

        #<?= $componentId ?>::after {
            content: "♠  ♥  ♣  ♦";
            position: absolute;
            right: 24px;
            top: 34px;
            color: rgba(133,68,29,0.12);
            font-size: clamp(54px, 8vw, 112px);
            font-weight: 700;
            letter-spacing: 0.18em;
            line-height: 1;
            transform: rotate(-8deg);
            pointer-events: none;
            white-space: nowrap;
        }

        #<?= $componentId ?> .cpe-wrap {
            position: relative;
            z-index: 1;
            width: min(100% - 32px, 1220px);
            margin: 0 auto;
        }

        #<?= $componentId ?> .cpe-layout {
            display: grid;
            grid-template-columns: minmax(280px, 0.82fr) minmax(0, 1.18fr);
            gap: <?= $cardGap ?>px;
            align-items: start;
        }

        #<?= $componentId ?> .cpe-command {
            position: sticky;
            top: 92px;
            display: grid;
            gap: 18px;
            padding: 8px 0 8px 10px;
        }

        #<?= $componentId ?> .cpe-command::before {
            content: "";
            position: absolute;
            inset: 18px 18px 18px -24px;
            z-index: -1;
            border-radius: 32px;
            background:
                radial-gradient(circle at 18% 18%, rgba(255,255,255,0.6), transparent 28%),
                linear-gradient(180deg, rgba(255,255,255,0.34), rgba(255,240,219,0.12));
            box-shadow: 0 24px 48px rgba(174, 84, 33, 0.1);
            opacity: 0.88;
        }

        #<?= $componentId ?> .cpe-command-top {
            display: grid;
            gap: 14px;
        }

        #<?= $componentId ?> .cpe-command-kicker {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            width: fit-content;
            padding: 8px 14px;
            border-radius: 999px;
            background: rgba(255,255,255,0.42);
            color: #ff7a3c;
            font-size: 12px;
            font-weight: 700;
            letter-spacing: 0.14em;
            text-transform: uppercase;
            box-shadow: 0 10px 24px rgba(174, 84, 33, 0.08);
        }

        #<?= $componentId ?> .cpe-command h2 {
            margin: 0;
            color: <?= htmlspecialchars($titleColor) ?>;
            font: 800 clamp(<?= $titleFontSizeMobile ?>px, 5vw, <?= $titleFontSize ?>px) / 1.02 "Palatino Linotype", "Book Antiqua", Georgia, serif;
            letter-spacing: -0.03em;
            text-wrap: balance;
        }

        #<?= $componentId ?> .cpe-command strong {
            display: block;
            margin-bottom: 0;
            color: #ff7a3c;
            font-size: 11px;
            letter-spacing: 0.16em;
            text-transform: uppercase;
        }

        #<?= $componentId ?> .cpe-command p {
            margin: 0 0 18px;
            color: <?= htmlspecialchars($cardDescColor) ?>;
            font-size: 14px;
            line-height: 1.9;
        }

        #<?= $componentId ?> .cpe-command-list {
            display: grid;
            gap: 12px;
            margin: 0 0 24px;
            padding: 0;
            list-style: none;
        }

        #<?= $componentId ?> .cpe-command-list li {
            display: grid;
            grid-template-columns: auto 1fr;
            gap: 12px;
            align-items: start;
            padding: 12px 14px;
            border-radius: 18px;
            background: rgba(255,255,255,0.72);
            border: 1px solid rgba(95,49,24,0.08);
        }

        #<?= $componentId ?> .cpe-command-list em {
            color: #ff7a3c;
            font-style: normal;
            font-size: 11px;
            font-weight: 800;
            letter-spacing: 0.16em;
            text-transform: uppercase;
        }

        #<?= $componentId ?> .cpe-command-list span {
            color: #2d170b;
            font-size: 14px;
            font-weight: 700;
            line-height: 1.6;
            padding: 0;
            background: none;
            border: 0;
            text-transform: none;
            letter-spacing: normal;
        }

        #<?= $componentId ?> .cpe-button {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: <?= $btnPaddingY ?>px <?= $btnPaddingX ?>px;
            border-radius: <?= $btnBorderRadius ?>px;
            background: linear-gradient(120deg, <?= htmlspecialchars($btnGradientStart) ?> 0%, <?= htmlspecialchars($btnGradientEnd) ?> 100%);
            color: <?= htmlspecialchars($btnTextColor) ?>;
            font-weight: 800;
            letter-spacing: 0.04em;
            text-transform: uppercase;
            box-shadow: 0 18px 38px rgba(255, 111, 60, 0.2);
        }

        #<?= $componentId ?> .cpe-lanes {
            display: grid;
            gap: <?= $cardGap ?>px;
            margin: 0;
            padding: 0;
            list-style: none;
        }

        #<?= $componentId ?> .cpe-lanes li:nth-child(2) article {
            transform: translateX(26px);
        }

        #<?= $componentId ?> .cpe-lanes li:nth-child(3) article {
            transform: translateX(54px);
        }

        #<?= $componentId ?> .cpe-lanes article {
            position: relative;
            display: grid;
            grid-template-columns: auto auto 1fr;
            gap: 18px;
            align-items: center;
            padding: 26px;
            border-radius: 28px;
            background: linear-gradient(160deg, rgba(255,255,255,0.82), rgba(255,247,236,0.96));
            border: 1px solid rgba(95,49,24,0.08);
            box-shadow: 0 20px 44px rgba(174, 84, 33, 0.14);
            overflow: hidden;
        }

        #<?= $componentId ?> .cpe-lanes article::before {
            content: "";
            position: absolute;
            inset: 0 auto 0 0;
            width: 6px;
            background: linear-gradient(180deg, #ff7a3c 0%, #ffb347 100%);
        }

        #<?= $componentId ?> .cpe-index {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 54px;
            height: 54px;
            border-radius: 18px;
            background: rgba(255,255,255,0.68);
            border: 1px solid rgba(95,49,24,0.08);
            color: #ff7a3c;
            font-size: 12px;
            font-weight: 800;
            letter-spacing: 0.16em;
            text-transform: uppercase;
        }

        #<?= $componentId ?> .cpe-icon {
            width: <?= $iconSize ?>px;
            height: <?= $iconSize ?>px;
            border-radius: <?= $iconBorderRadius ?>%;
            object-fit: cover;
            background: rgba(255,255,255,0.58);
            box-shadow: 0 12px 28px rgba(174, 84, 33, 0.14);
        }

        #<?= $componentId ?> .cpe-copy h3 {
            margin: 0 0 10px;
            color: <?= htmlspecialchars($cardTitleColor) ?>;
            font-size: <?= $cardTitleFontSize ?>px;
            font-weight: 800;
        }

        #<?= $componentId ?> .cpe-copy p {
            margin: 0;
            color: <?= htmlspecialchars($cardDescColor) ?>;
            font-size: <?= $cardDescFontSize ?>px;
            line-height: 1.85;
        }

        @media (max-width: 991px) {
            #<?= $componentId ?> .cpe-layout {
                grid-template-columns: 1fr;
            }

            #<?= $componentId ?> .cpe-command {
                position: static;
                padding-left: 0;
            }

            #<?= $componentId ?> .cpe-command::before {
                inset: 0;
            }

            #<?= $componentId ?> .cpe-lanes li:nth-child(2) article,
            #<?= $componentId ?> .cpe-lanes li:nth-child(3) article {
                transform: none;
            }
        }

        @media (max-width: 767px) {
            #<?= $componentId ?> {
                padding: <?= $sectionPaddingMobile ?>px 0;
            }

            #<?= $componentId ?> .cpe-wrap {
                width: min(100% - 24px, 1220px);
            }

            #<?= $componentId ?> .cpe-command,
            #<?= $componentId ?> .cpe-lanes article {
                padding: 22px 18px;
                border-radius: 24px;
            }

            #<?= $componentId ?> .cpe-command::before {
                inset: 0;
                border-radius: 24px;
            }

            #<?= $componentId ?> .cpe-command-top {
                gap: 10px;
            }

            #<?= $componentId ?> .cpe-command-list {
                gap: 10px;
                margin-bottom: 18px;
            }

            #<?= $componentId ?> .cpe-lanes article {
                grid-template-columns: auto 1fr;
                align-items: start;
                gap: 14px;
            }

            #<?= $componentId ?> .cpe-index {
                width: 48px;
                height: 48px;
                border-radius: 16px;
            }

            #<?= $componentId ?> .cpe-icon {
                width: 58px;
                height: 58px;
            }

            #<?= $componentId ?> .cpe-copy {
                display: grid;
                gap: 8px;
            }

            #<?= $componentId ?> .cpe-copy h3 {
                margin-bottom: 0;
            }

            #<?= $componentId ?> .cpe-copy p {
                font-size: 13px;
                line-height: 1.75;
            }

            #<?= $componentId ?> .cpe-button {
                width: 100%;
            }
        }
    </style>

    <div class="cpe-wrap">
        <div class="cpe-layout">
            <aside class="cpe-command" data-cpe-reveal aria-label="Advantage overview">
                <div class="cpe-command-top">
                    <span class="cpe-command-kicker">Seat advantage matrix</span>
                    <?php if ($showTitle): ?>
                    <h2 id="<?= $componentId ?>-title"><?= htmlspecialchars($title) ?></h2>
                    <?php endif; ?>
                </div>
                <strong>Table control</strong>
                <p><?= htmlspecialchars($summary) ?></p>
                <ul class="cpe-command-list" aria-label="Advantage highlights">
                    <?php foreach ($items as $item): ?>
                    <li>
                        <em><?= htmlspecialchars($item['eyebrow']) ?></em>
                        <span><?= htmlspecialchars($item['title']) ?></span>
                    </li>
                    <?php endforeach; ?>
                </ul>
                <?php if ($showButton): ?>
                <a class="cpe-button download-btn" href="<?= htmlspecialchars(\GuoLaiRen\PageBuilder\Helper\GlrDownloadRegistry::codeHref($btnRef)) ?>" data-glr-ref="<?= htmlspecialchars($btnRef) ?>">
                    <?= htmlspecialchars($btnText) ?>
                </a>
                <?php endif; ?>
            </aside>

            <ol class="cpe-lanes" aria-label="Core advantages">
                <?php foreach ($items as $item): ?>
                <li>
                    <article data-cpe-reveal>
                        <span class="cpe-index"><?= htmlspecialchars($item['eyebrow']) ?></span>
                        <img class="cpe-icon" src="<?= htmlspecialchars((string) $item['icon']) ?>" alt="<?= htmlspecialchars((string) $item['title']) ?>">
                        <div class="cpe-copy">
                            <h3><?= htmlspecialchars((string) $item['title']) ?></h3>
                            <?php if ($showDescription): ?>
                            <p><?= htmlspecialchars((string) $item['desc']) ?></p>
                            <?php endif; ?>
                        </div>
                    </article>
                </li>
                <?php endforeach; ?>
            </ol>
        </div>
    </div>
</section>
