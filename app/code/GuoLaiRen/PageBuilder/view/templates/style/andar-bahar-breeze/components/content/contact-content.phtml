<?php
/**
 * Andar Bahar Breeze - Contact Content Component
 *
 * @component_start
 * code: contact-content
 * name: 联系我们内容
 * name_en: Contact Content
 * description: 联系方式和联系表单展示组件
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_map => 显示地图:select:yes|yes,no
 * display.show_info => 显示联系信息:select:yes|yes,no
 * display.show_social => 显示社交链接:select:yes|yes,no
 * display.show_form => 显示联系表单:select:yes|yes,no
 *
 * group:hero => 页面文案
 * hero.title => 页面标题:text:Contact Andar Bahar Breeze
 * hero.subtitle => 页面描述:textarea:Need help with account access, table routes, or payment questions? Use the contact options below to reach the right Andar Bahar Breeze desk.
 *
 * group:contact_info => 联系信息
 * contact_info.address_label => 地址标签:text:Office Base
 * contact_info.email => 邮箱:text:support@andarbaharbreeze.com
 * contact_info.phone_label => 电话标签:text:Call Line
 * contact_info.phone => 电话:text:+91 1800-XXX-XXXX
 * contact_info.email_label => 邮箱标签:text:Email Desk
 * contact_info.address => 地址:text:Mumbai, India
 * contact_info.work_hours_label => 工作时间标签:text:Response Window
 * contact_info.work_hours => 服务时间:text:24/7 Customer Support
 *
 * group:social => 社交媒体
 * social.wechat => 微信:text:
 * social.weibo => 微博:text:
 * social.facebook => Facebook链接:text:
 * social.twitter => Twitter链接:text:
 * social.linkedin => LinkedIn链接:text:
 *
 * group:form => 表单设置
 * form.title => 表单标题:text:Send us a Message
 * form.name_label => 姓名字段标签:text:Full Name
 * form.email_label => 邮箱字段标签:text:Email Address
 * form.subject_label => 主题字段标签:text:Subject
 * form.message_label => 消息字段标签:text:Message
 * form.submit_text => 提交按钮:text:Send Message
 * form.success_message => 成功提示:text:Share the issue clearly and we will route it to the right support desk.
 * form.queue_label => 表单辅助标签:text:Message Queue
 *
 * group:support => 辅助文案
 * support.summary_kicker => 联系摘要标签:text:Support Board
 * support.social_title => 社交区域标题:text:Follow The Breeze Desk
 *
 * group:map => 地图设置
 * map.embed_url => 地图嵌入链接:textarea:
 * map.height => 地图高度:number:360|px
 *
 * @fields_end
 */

$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: ($component_config ?? []);
$config = array_merge($styleSettings, $componentConfig);
$componentId = 'abb-contact-' . uniqid();

$getConfig = function($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

$showMap = $getConfig('display.show_map', 'yes') !== 'no';
$showInfo = $getConfig('display.show_info', 'yes') !== 'no';
$showSocial = $getConfig('display.show_social', 'yes') !== 'no';
$showForm = $getConfig('display.show_form', 'yes') !== 'no';

$page = $this->getData('page');
$pageTitle = $page ? trim((string)($page->getData('title') ?? '')) : '';
$pageContent = $page ? trim((string)($page->getData('content') ?? '')) : '';

$title = trim((string)$getConfig('hero.title', $getConfig('content.title', $pageTitle)));
if ($title === '') {
    $title = 'Contact Andar Bahar Breeze';
}
$description = trim((string)$getConfig('hero.subtitle', $getConfig('content.description', 'Need help with account access, table routes, or payment questions? Use the contact options below to reach the right Andar Bahar Breeze desk.')));
$formTitle = trim((string)$getConfig('form.title', $getConfig('content.form_title', 'Send us a Message')));
$formNameLabel = trim((string)$getConfig('form.name_label', 'Full Name'));
$formEmailLabel = trim((string)$getConfig('form.email_label', 'Email Address'));
$formSubjectLabel = trim((string)$getConfig('form.subject_label', 'Subject'));
$formMessageLabel = trim((string)$getConfig('form.message_label', 'Message'));
$formSuccessMessage = trim((string)$getConfig('form.success_message', $getConfig('content.form_intro', 'Share the issue clearly and we will route it to the right support desk.')));
$formSubmitText = trim((string)$getConfig('form.submit_text', 'Send Message'));
$formQueueLabel = trim((string)$getConfig('form.queue_label', 'Message Queue'));
$supportSummaryKicker = trim((string)$getConfig('support.summary_kicker', 'Support Board'));
$supportSocialTitle = trim((string)$getConfig('support.social_title', 'Follow The Breeze Desk'));

$emailLabel = trim((string)$getConfig('contact_info.email_label', 'Email Desk'));
$phoneLabel = trim((string)$getConfig('contact_info.phone_label', 'Call Line'));
$addressLabel = trim((string)$getConfig('contact_info.address_label', 'Office Base'));
$hoursLabel = trim((string)$getConfig('contact_info.work_hours_label', 'Response Window'));
$email = trim((string)$getConfig('contact_info.email', $getConfig('contact.email', 'support@andarbaharbreeze.com')));
$phone = trim((string)$getConfig('contact_info.phone', $getConfig('contact.phone', '+91 1800-XXX-XXXX')));
$address = trim((string)$getConfig('contact_info.address', $getConfig('contact.address', 'Mumbai, India')));
$hours = trim((string)$getConfig('contact_info.work_hours', $getConfig('contact.hours', '24/7 Customer Support')));

$mapEmbedUrl = trim((string)$getConfig('map.embed_url', ''));
$mapHeight = max(240, (int)$getConfig('map.height', 360));

$socialMap = [
    'social.wechat' => 'WeChat',
    'social.weibo' => 'Weibo',
    'social.facebook' => 'Facebook',
    'social.twitter' => 'Twitter',
    'social.linkedin' => 'LinkedIn',
];
$socials = [];
foreach ($socialMap as $key => $label) {
    $url = trim((string)$getConfig($key, ''));
    if ($url === '') {
        continue;
    }
    $socials[] = [
        'name' => $label,
        'url' => $url,
    ];
}

if (empty($socials)) {
    $socialRaw = (string)$getConfig('social.items', "Facebook|#\nTwitter|#\nInstagram|#\nYouTube|#");
    if (strpos($socialRaw, '\\n') !== false && strpos($socialRaw, "\n") === false) {
        $socialRaw = str_replace('\\n', "\n", $socialRaw);
    }
    foreach (preg_split('/[\r\n]+/', $socialRaw) as $line) {
        if (empty(trim($line))) {
            continue;
        }
        $parts = explode('|', $line, 2);
        if (!empty(trim($parts[0] ?? ''))) {
            $socials[] = [
                'name' => trim($parts[0]),
                'url' => trim($parts[1] ?? '#'),
            ];
        }
    }
}

$channels = [];
$channelDefaults = [
    ['icon' => '📧', 'label' => $emailLabel !== '' ? $emailLabel : 'Email Desk', 'value' => $email],
    ['icon' => '📞', 'label' => $phoneLabel !== '' ? $phoneLabel : 'Call Line', 'value' => $phone],
    ['icon' => '📍', 'label' => $addressLabel !== '' ? $addressLabel : 'Office Base', 'value' => $address],
    ['icon' => '🕐', 'label' => $hoursLabel !== '' ? $hoursLabel : 'Response Window', 'value' => $hours],
];
foreach ($channelDefaults as $channel) {
    if (($channel['value'] ?? '') === '') {
        continue;
    }
    $channels[] = $channel;
}
?>

<section id="<?= $componentId ?>" class="abb-contact">
    <div class="contact-shell">
        <header class="contact-head">
            <h1><?= htmlspecialchars($title) ?></h1>
            <?php if ($description !== ''): ?>
                <p><?= htmlspecialchars($description) ?></p>
            <?php endif; ?>
        </header>

        <div class="contact-board<?= !$showInfo ? ' is-form-only' : '' ?><?= $showForm ? '' : ' is-info-only' ?>">
            <?php if ($showInfo): ?>
                <aside class="contact-routes">
                    <article class="route-highlight">
                        <?php if ($supportSummaryKicker !== ''): ?>
                            <span class="route-kicker"><?= htmlspecialchars($supportSummaryKicker) ?></span>
                        <?php endif; ?>
                        <h2><?= htmlspecialchars($hours !== '' ? $hours : $title) ?></h2>
                        <?php if ($description !== ''): ?>
                            <p><?= htmlspecialchars($description) ?></p>
                        <?php endif; ?>
                    </article>

                    <?php if (!empty($channels)): ?>
                        <div class="route-grid">
                            <?php foreach ($channels as $index => $channel): ?>
                                <article class="route-card route-card-<?= (int)$index ?>">
                                    <div class="route-mark"><?= htmlspecialchars($channel['icon']) ?></div>
                                    <span class="route-label"><?= htmlspecialchars($channel['label']) ?></span>
                                    <strong><?= htmlspecialchars($channel['value']) ?></strong>
                                </article>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>

                    <?php if ($showSocial && !empty($socials)): ?>
                        <div class="social-dock">
                            <?php if ($supportSocialTitle !== ''): ?>
                                <span class="social-title"><?= htmlspecialchars($supportSocialTitle) ?></span>
                            <?php endif; ?>
                            <div class="social-row">
                                <?php foreach ($socials as $social): ?>
                                    <a href="<?= htmlspecialchars($social['url']) ?>" class="social-badge" target="_blank" rel="noopener" title="<?= htmlspecialchars($social['name']) ?>">
                                        <span><?= htmlspecialchars(mb_substr($social['name'], 0, 1)) ?></span>
                                        <small><?= htmlspecialchars($social['name']) ?></small>
                                    </a>
                                <?php endforeach; ?>
                            </div>
                        </div>
                    <?php endif; ?>
                </aside>
            <?php endif; ?>

            <?php if ($showForm): ?>
                <section class="contact-sheet">
                    <div class="sheet-head">
                        <?php if ($formQueueLabel !== ''): ?>
                            <span class="sheet-kicker"><?= htmlspecialchars($formQueueLabel) ?></span>
                        <?php endif; ?>
                        <h2><?= htmlspecialchars($formTitle) ?></h2>
                        <?php if ($formSuccessMessage !== ''): ?>
                            <p><?= htmlspecialchars($formSuccessMessage) ?></p>
                        <?php endif; ?>
                    </div>

                    <div class="sheet-grid<?= $pageContent === '' ? ' no-note' : '' ?>">
                        <?php if ($pageContent !== ''): ?>
                            <div class="sheet-note">
                                <div class="sheet-richtext"><?= $pageContent ?></div>
                            </div>
                        <?php endif; ?>

                        <form class="contact-form">
                            <div class="field field-half">
                                <label><?= htmlspecialchars($formNameLabel !== '' ? $formNameLabel : 'Full Name') ?></label>
                                <input type="text" placeholder="<?= htmlspecialchars($formNameLabel !== '' ? $formNameLabel : 'Full Name') ?>">
                            </div>
                            <div class="field field-half">
                                <label><?= htmlspecialchars($formEmailLabel !== '' ? $formEmailLabel : 'Email Address') ?></label>
                                <input type="email" placeholder="<?= htmlspecialchars($formEmailLabel !== '' ? $formEmailLabel : 'Email Address') ?>">
                            </div>
                            <div class="field">
                                <label><?= htmlspecialchars($formSubjectLabel !== '' ? $formSubjectLabel : 'Subject') ?></label>
                                <input type="text" placeholder="<?= htmlspecialchars($formSubjectLabel !== '' ? $formSubjectLabel : 'Subject') ?>">
                            </div>
                            <div class="field">
                                <label><?= htmlspecialchars($formMessageLabel !== '' ? $formMessageLabel : 'Message') ?></label>
                                <textarea placeholder="<?= htmlspecialchars($formMessageLabel !== '' ? $formMessageLabel : 'Message') ?>"></textarea>
                            </div>
                            <button type="submit" class="submit-btn"><?= htmlspecialchars($formSubmitText) ?></button>
                        </form>
                    </div>
                </section>
            <?php endif; ?>
        </div>

        <?php if ($showMap && $mapEmbedUrl !== ''): ?>
            <section class="contact-map">
                <iframe src="<?= htmlspecialchars($mapEmbedUrl) ?>" loading="lazy" referrerpolicy="no-referrer-when-downgrade" style="height: <?= (int)$mapHeight ?>px;"></iframe>
            </section>
        <?php endif; ?>
    </div>
</section>

<style>
#<?= $componentId ?> {
    position: relative;
    padding: 96px 0;
    background:
        radial-gradient(720px 340px at 10% 12%, rgba(89, 199, 191, 0.1), transparent 72%),
        radial-gradient(640px 340px at 90% 20%, rgba(141, 201, 255, 0.12), transparent 72%),
        linear-gradient(180deg, #f9fffd 0%, #eefcf8 54%, #f6fcff 100%);
    overflow: hidden;
}

#<?= $componentId ?> .contact-shell {
    width: min(1220px, calc(100% - 2rem));
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

#<?= $componentId ?> .contact-head {
    max-width: 760px;
    margin-bottom: 1rem;
}

#<?= $componentId ?> .contact-badge,
#<?= $componentId ?> .route-kicker,
#<?= $componentId ?> .sheet-kicker {
    display: inline-flex;
    align-items: center;
    padding: 0.4rem 0.82rem;
    border-radius: 999px;
    font-size: 0.74rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

#<?= $componentId ?> .contact-badge,
#<?= $componentId ?> .sheet-kicker {
    color: #2f9fa4;
    border: 1px solid rgba(89, 199, 191, 0.24);
    background: rgba(89, 199, 191, 0.08);
}

#<?= $componentId ?> .contact-head h1 {
    margin-top: 0;
    color: #21444b;
    font-size: clamp(2.2rem, 4.4vw, 3.9rem);
    line-height: 1.03;
    letter-spacing: -0.04em;
}

#<?= $componentId ?> .contact-head p {
    margin-top: 0.9rem;
    color: rgba(85, 121, 127, 0.82);
    line-height: 1.8;
}

#<?= $componentId ?> .contact-board {
    display: grid;
    grid-template-columns: minmax(300px, 0.86fr) minmax(0, 1.14fr);
    gap: 1rem;
    align-items: start;
}

#<?= $componentId ?> .contact-board.is-info-only {
    grid-template-columns: 1fr;
}

#<?= $componentId ?> .contact-board.is-form-only {
    grid-template-columns: 1fr;
}

#<?= $componentId ?> .route-highlight,
#<?= $componentId ?> .route-card,
#<?= $componentId ?> .social-dock,
#<?= $componentId ?> .contact-sheet,
#<?= $componentId ?> .sheet-note {
    border-radius: 28px;
    border: 1px solid rgba(122, 161, 192, 0.22);
    background: linear-gradient(160deg, rgba(248, 255, 252, 0.92) 0%, rgba(233, 247, 255, 0.9) 100%);
    box-shadow: 0 22px 52px rgba(81, 135, 140, 0.12);
}

#<?= $componentId ?> .contact-routes {
    display: grid;
    gap: 1rem;
}

#<?= $componentId ?> .route-highlight {
    padding: 1.35rem;
}

#<?= $componentId ?> .route-kicker {
    color: #8dc9ff;
    border: 1px solid rgba(141, 201, 255, 0.26);
    background: rgba(141, 201, 255, 0.12);
}

#<?= $componentId ?> .route-highlight h2 {
    margin-top: 0.95rem;
    color: #21444b;
    font-size: clamp(1.5rem, 2.8vw, 2.35rem);
    line-height: 1.08;
}

#<?= $componentId ?> .route-highlight p {
    margin-top: 0.75rem;
    color: rgba(85, 121, 127, 0.82);
    line-height: 1.74;
}

#<?= $componentId ?> .route-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.85rem;
}

#<?= $componentId ?> .route-card {
    padding: 1rem;
}

#<?= $componentId ?> .route-mark {
    width: 2.95rem;
    height: 2.95rem;
    border-radius: 18px;
    display: grid;
    place-items: center;
    background: linear-gradient(135deg, rgba(89, 199, 191, 0.14), rgba(141, 201, 255, 0.16));
    border: 1px solid rgba(141, 201, 255, 0.18);
    font-size: 1.2rem;
}

#<?= $componentId ?> .route-label {
    display: block;
    margin-top: 0.8rem;
    color: rgba(108, 144, 150, 0.74);
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
}

#<?= $componentId ?> .route-card strong {
    display: block;
    margin-top: 0.38rem;
    color: #21444b;
    line-height: 1.58;
    word-break: break-word;
}

#<?= $componentId ?> .social-dock {
    padding: 1rem;
}

#<?= $componentId ?> .social-title {
    display: block;
    color: #21444b;
    font-size: 0.9rem;
    font-weight: 800;
    letter-spacing: 0.04em;
}

#<?= $componentId ?> .social-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem;
    margin-top: 0.85rem;
}

#<?= $componentId ?> .social-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.62rem 0.72rem;
    border-radius: 999px;
    border: 1px solid rgba(89, 199, 191, 0.2);
    background: rgba(255, 255, 255, 0.72);
    color: #21444b;
    text-decoration: none;
    transition: transform 0.22s ease, box-shadow 0.22s ease, border-color 0.22s ease;
}

#<?= $componentId ?> .social-badge span {
    width: 1.8rem;
    height: 1.8rem;
    border-radius: 999px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(89, 199, 191, 0.18), rgba(141, 201, 255, 0.18));
    color: #2f9fa4;
    font-weight: 800;
}

#<?= $componentId ?> .social-badge small {
    font-size: 0.78rem;
    font-weight: 700;
}

#<?= $componentId ?> .social-badge:hover {
    transform: translateY(-2px);
    border-color: rgba(141, 201, 255, 0.45);
    box-shadow: 0 12px 24px rgba(81, 135, 140, 0.12);
}

#<?= $componentId ?> .contact-sheet {
    padding: 1.35rem;
    position: relative;
    overflow: hidden;
}

#<?= $componentId ?> .contact-sheet::after {
    content: '';
    position: absolute;
    right: -80px;
    top: -60px;
    width: 220px;
    height: 220px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(89, 199, 191, 0.08), transparent 72%);
    pointer-events: none;
}

#<?= $componentId ?> .sheet-head h2 {
    margin-top: 0.95rem;
    color: #21444b;
    font-size: clamp(1.7rem, 3vw, 2.5rem);
    line-height: 1.08;
}

#<?= $componentId ?> .sheet-head p {
    margin-top: 0.7rem;
    color: rgba(85, 121, 127, 0.8);
    line-height: 1.72;
}

#<?= $componentId ?> .sheet-grid {
    display: grid;
    grid-template-columns: minmax(200px, 0.38fr) minmax(0, 0.62fr);
    gap: 1rem;
    margin-top: 1rem;
    position: relative;
    z-index: 1;
}

#<?= $componentId ?> .sheet-grid.no-note {
    grid-template-columns: 1fr;
}

#<?= $componentId ?> .sheet-note {
    padding: 1rem;
    align-self: start;
}

#<?= $componentId ?> .sheet-richtext {
    color: rgba(85, 121, 127, 0.78);
    line-height: 1.7;
}

#<?= $componentId ?> .sheet-richtext > :first-child {
    margin-top: 0;
}

#<?= $componentId ?> .sheet-richtext > :last-child {
    margin-bottom: 0;
}

#<?= $componentId ?> .contact-form {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.85rem;
}

#<?= $componentId ?> .field {
    display: grid;
    gap: 0.42rem;
}

#<?= $componentId ?> .field-half {
    grid-column: span 1;
}

#<?= $componentId ?> .field:not(.field-half) {
    grid-column: 1 / -1;
}

#<?= $componentId ?> .field label {
    color: #21444b;
    font-size: 0.84rem;
    font-weight: 700;
}

#<?= $componentId ?> .field input,
#<?= $componentId ?> .field textarea {
    width: 100%;
    padding: 0.88rem 0.95rem;
    border-radius: 18px;
    border: 1px solid rgba(122, 161, 192, 0.2);
    background: rgba(255, 255, 255, 0.82);
    color: #21444b;
    font: inherit;
    outline: none;
    transition: border-color 0.22s ease, box-shadow 0.22s ease, transform 0.22s ease;
}

#<?= $componentId ?> .field textarea {
    min-height: 180px;
    resize: vertical;
}

#<?= $componentId ?> .field input:focus,
#<?= $componentId ?> .field textarea:focus {
    border-color: rgba(141, 201, 255, 0.5);
    box-shadow: 0 0 0 4px rgba(141, 201, 255, 0.14);
    transform: translateY(-1px);
}

#<?= $componentId ?> .submit-btn {
    grid-column: 1 / -1;
    justify-self: start;
    min-width: 170px;
    padding: 0.82rem 1.18rem;
    border: 0;
    border-radius: 999px;
    background: linear-gradient(135deg, #59c7bf 0%, #8dc9ff 100%);
    color: #f7fffc;
    font-weight: 800;
    letter-spacing: 0.04em;
    cursor: pointer;
    box-shadow: 0 16px 30px rgba(81, 135, 140, 0.18);
    transition: transform 0.22s ease, box-shadow 0.22s ease;
}

#<?= $componentId ?> .submit-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 20px 34px rgba(81, 135, 140, 0.22);
}

#<?= $componentId ?> .contact-map {
    margin-top: 1rem;
    border-radius: 28px;
    border: 1px solid rgba(122, 161, 192, 0.22);
    background: linear-gradient(160deg, rgba(248, 255, 252, 0.92) 0%, rgba(233, 247, 255, 0.9) 100%);
    box-shadow: 0 22px 52px rgba(81, 135, 140, 0.12);
    overflow: hidden;
}

#<?= $componentId ?> .contact-map iframe {
    display: block;
    width: 100%;
    border: 0;
}

@media (max-width: 980px) {
    #<?= $componentId ?> .contact-board,
    #<?= $componentId ?> .sheet-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 640px) {
    #<?= $componentId ?> {
        padding: 64px 0;
    }

    #<?= $componentId ?> .contact-shell {
        width: min(1220px, calc(100% - 1rem));
    }

    #<?= $componentId ?> .route-grid,
    #<?= $componentId ?> .contact-form {
        grid-template-columns: 1fr;
    }

    #<?= $componentId ?> .route-highlight,
    #<?= $componentId ?> .route-card,
    #<?= $componentId ?> .social-dock,
    #<?= $componentId ?> .contact-sheet,
    #<?= $componentId ?> .sheet-note {
        border-radius: 24px;
    }
}
</style>
