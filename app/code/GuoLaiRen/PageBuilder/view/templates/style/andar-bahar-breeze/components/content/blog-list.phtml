<?php
/**
 * Andar Bahar Breeze Blog List Component
 *
 * @fields_start
 * blog.title|博客列表标题|text|Latest Blog Posts
 * blog.subtitle|副标题|text|Stay updated with our latest news and articles
 * blog.posts_per_page|每页显示数量|number|9
 * blog.show_sidebar|显示侧边栏|select:yes=是,no=否|yes
 * blog.show_category|显示分类|select:yes=是,no=否|yes
 * blog.show_date|显示日期|select:yes=是,no=否|yes
 * blog.show_author|显示作者|select:yes=是,no=否|yes
 * blog.card_style|卡片样式|select:default=默认,minimal=简约,featured=特色|default
 * @fields_end
 */

$style = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$blogPosts = $this->getData('blog_posts') ?: [];
$blogCategories = $this->getData('blog_categories') ?: [];
$recentPosts = $this->getData('recent_posts') ?: [];

$getConfig = function($key, $default = '') use ($style, $componentConfig) {
    if (isset($componentConfig[$key]) && $componentConfig[$key] !== '') {
        return $componentConfig[$key];
    }
    return $style[$key] ?? $default;
};

$title = $getConfig('blog.title', __('Latest Blog Posts'));
$subtitle = $getConfig('blog.subtitle', __('Stay updated with our latest news and articles'));
$showSidebar = $getConfig('blog.show_sidebar', 'yes') === 'yes';
$showCategory = $getConfig('blog.show_category', 'yes') === 'yes';
$showDate = $getConfig('blog.show_date', 'yes') === 'yes';
$showAuthor = $getConfig('blog.show_author', 'yes') === 'yes';
$cardStyle = $getConfig('blog.card_style', 'default');

$primaryColor = $getConfig('color_scheme.primary', '#59c7bf');
$secondaryColor = $getConfig('color_scheme.secondary', '#8dc9ff');
$bgColor = $getConfig('color_scheme.background', '#f7fffc');
$textColor = $getConfig('color_scheme.text', '#21444b');

$featuredPost = $blogPosts[0] ?? null;
$streamPosts = array_slice($blogPosts, 1);
$streamClass = 'stream-' . preg_replace('/[^a-z0-9-]+/i', '', (string)$cardStyle);
?>

<section class="abb-blog-list" id="blog">
    <div class="blog-shell">
        <header class="blog-masthead">
            <span class="masthead-kicker">Editorial Flow</span>
            <h2><?= htmlspecialchars($title) ?></h2>
            <?php if ($subtitle): ?>
                <p><?= htmlspecialchars($subtitle) ?></p>
            <?php endif; ?>
        </header>

        <?php if (empty($blogPosts)): ?>
            <div class="blog-empty">
                <div class="empty-icon">
                    <svg width="64" height="64" viewBox="0 0 24 24" fill="currentColor" opacity="0.3">
                        <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-5 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"/>
                    </svg>
                </div>
                <p><?= __('There are no blog posts yet. Please check back later!') ?></p>
            </div>
        <?php else: ?>
            <div class="journal-board<?= $showSidebar ? ' has-rail' : '' ?>">
                <div class="journal-main">
                    <?php if ($featuredPost): ?>
                        <article class="issue-lead">
                            <div class="lead-copy">
                                <div class="lead-meta">
                                    <?php if ($showCategory && !empty($featuredPost['category_name'])): ?>
                                        <span class="story-chip"><?= htmlspecialchars($featuredPost['category_name']) ?></span>
                                    <?php endif; ?>
                                    <?php if ($showDate && !empty($featuredPost['published_at'])): ?>
                                        <span class="story-date"><?= date('M d, Y', strtotime($featuredPost['published_at'])) ?></span>
                                    <?php endif; ?>
                                </div>

                                <h3>
                                    <a href="<?= htmlspecialchars($featuredPost['url']) ?>">
                                        <?= htmlspecialchars($featuredPost['title']) ?>
                                    </a>
                                </h3>

                                <?php if (!empty($featuredPost['summary'])): ?>
                                    <p class="lead-summary"><?= htmlspecialchars(mb_substr($featuredPost['summary'], 0, 240)) ?><?= mb_strlen($featuredPost['summary']) > 240 ? '...' : '' ?></p>
                                <?php endif; ?>

                                <div class="lead-foot">
                                    <?php if ($showAuthor && !empty($featuredPost['author'])): ?>
                                        <span class="story-author"><?= htmlspecialchars($featuredPost['author']) ?></span>
                                    <?php endif; ?>
                                    <a href="<?= htmlspecialchars($featuredPost['url']) ?>" class="story-link">
                                        <?= __('Read Story') ?>
                                        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                            <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                        </svg>
                                    </a>
                                </div>
                            </div>

                            <?php if (!empty($featuredPost['cover_image'])): ?>
                                <a href="<?= htmlspecialchars($featuredPost['url']) ?>" class="lead-media">
                                    <img src="<?= htmlspecialchars($featuredPost['cover_image']) ?>" alt="<?= htmlspecialchars($featuredPost['title']) ?>" loading="lazy">
                                </a>
                            <?php endif; ?>
                        </article>
                    <?php endif; ?>

                    <?php if (!empty($streamPosts)): ?>
                        <div class="story-river <?= htmlspecialchars($streamClass) ?>">
                            <?php foreach ($streamPosts as $index => $post): ?>
                                <article class="river-card<?= $index % 2 === 1 ? ' is-reverse' : '' ?>">
                                    <?php if (!empty($post['cover_image'])): ?>
                                        <a href="<?= htmlspecialchars($post['url']) ?>" class="river-media">
                                            <img src="<?= htmlspecialchars($post['cover_image']) ?>" alt="<?= htmlspecialchars($post['title']) ?>" loading="lazy">
                                        </a>
                                    <?php endif; ?>

                                    <div class="river-copy">
                                        <div class="river-meta">
                                            <?php if ($showCategory && !empty($post['category_name'])): ?>
                                                <span class="story-chip"><?= htmlspecialchars($post['category_name']) ?></span>
                                            <?php endif; ?>
                                            <?php if ($showDate && !empty($post['published_at'])): ?>
                                                <span class="story-date"><?= date('M d, Y', strtotime($post['published_at'])) ?></span>
                                            <?php endif; ?>
                                        </div>

                                        <h3 class="story-title">
                                            <a href="<?= htmlspecialchars($post['url']) ?>">
                                                <?= htmlspecialchars($post['title']) ?>
                                            </a>
                                        </h3>

                                        <?php if (!empty($post['summary'])): ?>
                                            <p class="river-summary"><?= htmlspecialchars(mb_substr($post['summary'], 0, 170)) ?><?= mb_strlen($post['summary']) > 170 ? '...' : '' ?></p>
                                        <?php endif; ?>

                                        <div class="river-foot">
                                            <?php if ($showAuthor && !empty($post['author'])): ?>
                                                <span class="story-author"><?= htmlspecialchars($post['author']) ?></span>
                                            <?php endif; ?>
                                            <a href="<?= htmlspecialchars($post['url']) ?>" class="story-link">
                                                <?= __('Read More') ?>
                                                <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                                    <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                                </svg>
                                            </a>
                                        </div>
                                    </div>
                                </article>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </div>

                <?php if ($showSidebar): ?>
                    <aside class="editorial-rail">
                        <?php if (!empty($blogCategories)): ?>
                            <section class="rail-card">
                                <h4><?= __('Categories') ?></h4>
                                <div class="category-cloud">
                                    <?php foreach ($blogCategories as $category): ?>
                                        <a href="<?= htmlspecialchars($category['url']) ?>" class="category-pill">
                                            <span><?= htmlspecialchars($category['name']) ?></span>
                                            <?php if (isset($category['post_count'])): ?>
                                                <small><?= (int)$category['post_count'] ?></small>
                                            <?php endif; ?>
                                        </a>
                                    <?php endforeach; ?>
                                </div>
                            </section>
                        <?php endif; ?>

                        <?php if (!empty($recentPosts)): ?>
                            <section class="rail-card">
                                <h4><?= __('Recent Posts') ?></h4>
                                <div class="recent-stack">
                                    <?php foreach (array_slice($recentPosts, 0, 5) as $post): ?>
                                        <a href="<?= htmlspecialchars($post['url']) ?>" class="recent-link">
                                            <?php if (!empty($post['cover_image'])): ?>
                                                <img src="<?= htmlspecialchars($post['cover_image']) ?>" alt="<?= htmlspecialchars($post['title']) ?>" loading="lazy">
                                            <?php endif; ?>
                                            <span>
                                                <strong><?= htmlspecialchars($post['title']) ?></strong>
                                                <?php if (!empty($post['published_at'])): ?>
                                                    <small><?= date('M d, Y', strtotime($post['published_at'])) ?></small>
                                                <?php endif; ?>
                                            </span>
                                        </a>
                                    <?php endforeach; ?>
                                </div>
                            </section>
                        <?php endif; ?>
                    </aside>
                <?php endif; ?>
            </div>
        <?php endif; ?>
    </div>
</section>

<style>
.abb-blog-list {
    position: relative;
    padding: 88px 0;
    background:
        radial-gradient(1200px 620px at 14% 10%, rgba(89, 199, 191, 0.14), transparent 60%),
        radial-gradient(880px 560px at 86% 18%, rgba(141, 201, 255, 0.16), transparent 58%),
        <?= htmlspecialchars($bgColor) ?>;
    color: <?= htmlspecialchars($textColor) ?>;
    overflow: hidden;
}

.abb-blog-list::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.16) 0%, rgba(233, 247, 255, 0.32) 100%);
    pointer-events: none;
}

.abb-blog-list .blog-shell {
    width: min(1220px, calc(100% - 2rem));
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

.abb-blog-list .blog-masthead {
    max-width: 820px;
    margin: 0 auto 1.6rem;
    text-align: center;
}

.abb-blog-list .masthead-kicker {
    display: inline-flex;
    align-items: center;
    padding: 0.4rem 0.82rem;
    border-radius: 999px;
    border: 1px solid rgba(89, 199, 191, 0.22);
    background: rgba(89, 199, 191, 0.08);
    color: #2f9fa4;
    font-size: 0.74rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.abb-blog-list .blog-masthead h2 {
    margin-top: 1rem;
    color: #21444b;
    font-size: clamp(2.1rem, 4.3vw, 3.45rem);
    line-height: 1.04;
    letter-spacing: -0.04em;
}

.abb-blog-list .blog-masthead p {
    margin-top: 0.85rem;
    color: rgba(85, 121, 127, 0.82);
    line-height: 1.8;
}

.abb-blog-list .issue-lead,
.abb-blog-list .river-card,
.abb-blog-list .rail-card,
.abb-blog-list .blog-empty {
    border-radius: 28px;
    border: 1px solid rgba(122, 161, 192, 0.2);
    background: linear-gradient(160deg, rgba(248, 255, 252, 0.94) 0%, rgba(233, 247, 255, 0.9) 100%);
    box-shadow: 0 22px 52px rgba(81, 135, 140, 0.12);
}

.abb-blog-list .blog-empty {
    padding: 3rem 1.2rem;
    text-align: center;
}

.abb-blog-list .empty-icon {
    display: inline-flex;
    margin-bottom: 0.8rem;
}

.abb-blog-list .journal-board {
    display: grid;
    gap: 1rem;
}

.abb-blog-list .journal-board.has-rail {
    grid-template-columns: minmax(0, 1fr) minmax(280px, 0.34fr);
    align-items: start;
}

.abb-blog-list .journal-main {
    display: grid;
    gap: 1rem;
}

.abb-blog-list .issue-lead {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(280px, 0.95fr);
    overflow: hidden;
}

.abb-blog-list .lead-copy {
    padding: 1.45rem;
    display: flex;
    flex-direction: column;
}

.abb-blog-list .lead-media {
    min-height: 100%;
    display: block;
    background: rgba(141, 201, 255, 0.14);
}

.abb-blog-list .lead-media img,
.abb-blog-list .river-media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

.abb-blog-list .lead-meta,
.abb-blog-list .river-meta,
.abb-blog-list .river-foot,
.abb-blog-list .lead-foot {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.6rem;
}

.abb-blog-list .story-chip,
.abb-blog-list .story-date {
    display: inline-flex;
    align-items: center;
    padding: 0.34rem 0.76rem;
    border-radius: 999px;
    font-size: 0.76rem;
    font-weight: 700;
}

.abb-blog-list .story-chip {
    background: linear-gradient(120deg, <?= htmlspecialchars($primaryColor) ?> 0%, <?= htmlspecialchars($secondaryColor) ?> 100%);
    color: #f7fffc;
}

.abb-blog-list .story-date {
    color: rgba(85, 121, 127, 0.78);
    border: 1px solid rgba(122, 161, 192, 0.18);
    background: rgba(255, 255, 255, 0.46);
}

.abb-blog-list .lead-copy h3,
.abb-blog-list .story-title {
    margin-top: 0.95rem;
}

.abb-blog-list .lead-copy h3 a,
.abb-blog-list .story-title a {
    color: #21444b;
    text-decoration: none;
    transition: color 0.22s ease;
}

.abb-blog-list .lead-copy h3 a:hover,
.abb-blog-list .story-title a:hover {
    color: <?= htmlspecialchars($secondaryColor) ?>;
}

.abb-blog-list .lead-copy h3 a {
    font-size: clamp(1.6rem, 3vw, 2.45rem);
    line-height: 1.08;
}

.abb-blog-list .story-title a {
    font-size: clamp(1.15rem, 2vw, 1.45rem);
    line-height: 1.25;
}

.abb-blog-list .lead-summary,
.abb-blog-list .river-summary {
    margin-top: 0.9rem;
    color: rgba(85, 121, 127, 0.8);
    line-height: 1.76;
}

.abb-blog-list .lead-foot {
    margin-top: auto;
    padding-top: 1rem;
    justify-content: space-between;
}

.abb-blog-list .story-author {
    color: rgba(108, 144, 150, 0.74);
    font-size: 0.9rem;
}

.abb-blog-list .story-link {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    color: <?= htmlspecialchars($secondaryColor) ?>;
    font-weight: 800;
    text-decoration: none;
}

.abb-blog-list .story-river {
    display: grid;
    gap: 0.95rem;
}

.abb-blog-list .story-river.stream-minimal .river-summary {
    display: none;
}

.abb-blog-list .river-card {
    display: grid;
    grid-template-columns: minmax(220px, 0.42fr) minmax(0, 0.58fr);
    overflow: hidden;
}

.abb-blog-list .river-card.is-reverse {
    grid-template-columns: minmax(0, 0.58fr) minmax(220px, 0.42fr);
}

.abb-blog-list .river-card.is-reverse .river-media {
    order: 2;
}

.abb-blog-list .river-copy {
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
}

.abb-blog-list .river-summary {
    margin-top: 0.75rem;
}

.abb-blog-list .river-foot {
    margin-top: auto;
    padding-top: 0.9rem;
    justify-content: space-between;
}

.abb-blog-list .editorial-rail {
    display: grid;
    gap: 1rem;
}

.abb-blog-list .rail-card {
    padding: 1rem;
}

.abb-blog-list .rail-card h4 {
    color: #21444b;
    font-size: 0.9rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.abb-blog-list .category-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    margin-top: 0.85rem;
}

.abb-blog-list .category-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.42rem 0.8rem;
    border-radius: 999px;
    border: 1px solid rgba(89, 199, 191, 0.18);
    background: rgba(89, 199, 191, 0.08);
    color: rgba(85, 121, 127, 0.84);
    text-decoration: none;
    transition: background 0.22s ease, color 0.22s ease, transform 0.22s ease;
}

.abb-blog-list .category-pill:hover {
    background: rgba(141, 201, 255, 0.14);
    color: #21444b;
    transform: translateY(-2px);
}

.abb-blog-list .category-pill small {
    color: rgba(108, 144, 150, 0.68);
}

.abb-blog-list .recent-stack {
    display: grid;
    gap: 0.75rem;
    margin-top: 0.85rem;
}

.abb-blog-list .recent-link {
    display: grid;
    grid-template-columns: 72px minmax(0, 1fr);
    gap: 0.75rem;
    align-items: center;
    text-decoration: none;
}

.abb-blog-list .recent-link img {
    width: 72px;
    height: 72px;
    border-radius: 18px;
    object-fit: cover;
}

.abb-blog-list .recent-link strong {
    display: block;
    color: #21444b;
    line-height: 1.45;
}

.abb-blog-list .recent-link small {
    display: block;
    margin-top: 0.2rem;
    color: rgba(108, 144, 150, 0.66);
}

@media (max-width: 980px) {
    .abb-blog-list .journal-board.has-rail,
    .abb-blog-list .issue-lead,
    .abb-blog-list .river-card,
    .abb-blog-list .river-card.is-reverse {
        grid-template-columns: 1fr;
    }

    .abb-blog-list .river-card.is-reverse .river-media {
        order: 0;
    }
}

@media (max-width: 640px) {
    .abb-blog-list {
        padding: 64px 0;
    }

    .abb-blog-list .blog-shell {
        width: min(1220px, calc(100% - 1rem));
    }

    .abb-blog-list .lead-copy,
    .abb-blog-list .river-copy,
    .abb-blog-list .rail-card {
        padding: 1rem;
    }

    .abb-blog-list .issue-lead,
    .abb-blog-list .river-card,
    .abb-blog-list .rail-card,
    .abb-blog-list .blog-empty {
        border-radius: 24px;
    }

    .abb-blog-list .lead-foot,
    .abb-blog-list .river-foot {
        align-items: flex-start;
        flex-direction: column;
    }
}
</style>
