<?php
/**
 * AI Component Framework - Content
 * 
 * 内容组件框架模板，通用的内容区块结构
 * AI只需填充特定区域的代码
 * 
 * @var \Weline\Framework\View\Template $this
 * @var \GuoLaiRen\PageBuilder\Model\Page $page
 * @var array $component_config 组件配置
 * 
 * @component_start
 * name => {{COMPONENT_NAME}}
 * name_en => {{COMPONENT_NAME_EN}}
 * description => {{COMPONENT_DESC}}
 * category => content
 * type => section
 * sort_order => 50
 * compatible_styles => *
 * ai_generated => true
 * @component_end
 * 
 * @fields_start
 * 
 * group:content => 内容设置
 * content.title => 标题:text:
 * content.subtitle => 副标题:text:
 * content.description => 描述:textarea:
 * 
 * group:layout => 布局设置
 * layout.container_width => 容器宽度:select:1200|960,1200,1400,full
 * layout.padding_top => 顶部间距:number:80|px
 * layout.padding_bottom => 底部间距:number:80|px
 * layout.text_align => 文字对齐:select:center|left,center,right
 * 
 * group:style => 样式设置
 * style.bg_type => 背景类型:select:color|color,gradient,image
 * style.bg_color => 背景颜色:color:#ffffff
 * style.bg_gradient => 渐变背景:text:linear-gradient(135deg, #667eea 0%, #764ba2 100%)
 * style.bg_image => 背景图片:textarea:
 * style.text_color => 文字颜色:color:#374151
 * style.title_color => 标题颜色:color:#111827
 * style.accent_color => 强调色:color:#6366f1
 * 
 * {{EXTRA_FIELDS}}
 * 
 * @fields_end
 */

// ===== 固定的PHP数据获取部分 =====
$page = $this->getData('page');
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$config = array_merge($styleSettings, $componentConfig);

$getConfig = function($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

$componentId = 'content-' . substr(uniqid(), -8);
$cls = $componentId;

// 内容配置
$title = $getConfig('content.title', '');
$subtitle = $getConfig('content.subtitle', '');
$description = $getConfig('content.description', '');

// 布局配置
$containerWidth = $getConfig('layout.container_width', '1200');
$paddingTop = (int)$getConfig('layout.padding_top', 80);
$paddingBottom = (int)$getConfig('layout.padding_bottom', 80);
$textAlign = $getConfig('layout.text_align', 'center');

// 样式配置
$bgType = $getConfig('style.bg_type', 'color');
$bgColor = $getConfig('style.bg_color', '#ffffff');
$bgGradient = $getConfig('style.bg_gradient', 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)');
$bgImage = $getConfig('style.bg_image', '');
$textColor = $getConfig('style.text_color', '#374151');
$titleColor = $getConfig('style.title_color', '#111827');
$accentColor = $getConfig('style.accent_color', '#6366f1');

// 计算背景样式
$bgStyle = match($bgType) {
    'gradient' => $bgGradient,
    'image' => $bgImage ? "url('" . htmlspecialchars($bgImage) . "') center/cover no-repeat" : $bgColor,
    default => $bgColor,
};

// 容器宽度
$maxWidth = $containerWidth === 'full' ? '100%' : $containerWidth . 'px';

// ===== AI生成的额外PHP变量 =====
try {
    $stat1Value = $getConfig('wealth.stat1_value', '¥ 8,888,888');
$stat1Label = $getConfig('wealth.stat1_label', '今日收益');
$stat2Value = $getConfig('wealth.stat2_value', '¥ 68,888,888');
$stat2Label = $getConfig('wealth.stat2_label', '本月收益');
$stat3Value = $getConfig('wealth.stat3_value', '¥ 888,888,888');
$stat3Label = $getConfig('wealth.stat3_label', '年度收益');
$feature1Title = $getConfig('wealth.feature1_title', '财富增值');
$feature1Desc = $getConfig('wealth.feature1_desc', '专业的财富管理方案，实现资产持续增值');
$feature2Title = $getConfig('wealth.feature2_title', '投资回报');
$feature2Desc = $getConfig('wealth.feature2_desc', '高回报率的投资机会，让您的资金发挥最大价值');
$feature3Title = $getConfig('wealth.feature3_title', '风险控制');
$feature3Desc = $getConfig('wealth.feature3_desc', '严格的风险管理体系，保障您的投资安全');
$ctaText = $getConfig('wealth.cta_text', '立即加入财富计划');
$ctaNote = $getConfig('wealth.cta_note', '已有 8,888 位会员加入我们的财富计划');
} catch (\Throwable $e) {
    w_log_error('[AI Component Error] ' . $componentId . ': ' . $e->getMessage());
}
?>
<div class="<?= $cls ?>-body">
    <div class="<?= $cls ?>-wealth-section">
        <div class="<?= $cls ?>-wealth-stats">
            <div class="<?= $cls ?>-stat-item">
                <div class="<?= $cls ?>-stat-number"><?= htmlspecialchars($stat1Value ?? '¥ 8,888,888') ?></div>
                <div class="<?= $cls ?>-stat-label"><?= htmlspecialchars($stat1Label ?? '今日收益') ?></div>
            </div>
            <div class="<?= $cls ?>-stat-item">
                <div class="<?= $cls ?>-stat-number"><?= htmlspecialchars($stat2Value ?? '¥ 68,888,888') ?></div>
                <div class="<?= $cls ?>-stat-label"><?= htmlspecialchars($stat2Label ?? '本月收益') ?></div>
            </div>
            <div class="<?= $cls ?>-stat-item">
                <div class="<?= $cls ?>-stat-number"><?= htmlspecialchars($stat3Value ?? '¥ 888,888,888') ?></div>
                <div class="<?= $cls ?>-stat-label"><?= htmlspecialchars($stat3Label ?? '年度收益') ?></div>
            </div>
        </div>
        
        <div class="<?= $cls ?>-wealth-features">
            <div class="<?= $cls ?>-feature-card">
                <div class="<?= $cls ?>-feature-icon">💰</div>
                <h3 class="<?= $cls ?>-feature-title"><?= htmlspecialchars($feature1Title ?? '财富增值') ?></h3>
                <p class="<?= $cls ?>-feature-desc"><?= htmlspecialchars($feature1Desc ?? '专业的财富管理方案，实现资产持续增值') ?></p>
            </div>
            <div class="<?= $cls ?>-feature-card">
                <div class="<?= $cls ?>-feature-icon">📈</div>
                <h3 class="<?= $cls ?>-feature-title"><?= htmlspecialchars($feature2Title ?? '投资回报') ?></h3>
                <p class="<?= $cls ?>-feature-desc"><?= htmlspecialchars($feature2Desc ?? '高回报率的投资机会，让您的资金发挥最大价值') ?></p>
            </div>
            <div class="<?= $cls ?>-feature-card">
                <div class="<?= $cls ?>-feature-icon">🛡️</div>
                <h3 class="<?= $cls ?>-feature-title"><?= htmlspecialchars($feature3Title ?? '风险控制') ?></h3>
                <p class="<?= $cls ?>-feature-desc"><?= htmlspecialchars($feature3Desc ?? '严格的风险管理体系，保障您的投资安全') ?></p>
            </div>
        </div>
        
        <div class="<?= $cls ?>-cta-section">
            <button class="<?= $cls ?>-cta-button" type="button"><?= htmlspecialchars($ctaText ?? '立即加入财富计划') ?></button>
            <p class="<?= $cls ?>-cta-note"><?= htmlspecialchars($ctaNote ?? '已有 8,888 位会员加入我们的财富计划') ?></p>
        </div>
    </div>
</div>
<style>
@media (max-width: 768px) {
    #<?= $componentId ?> .<?= $cls ?>-wealth-stats {
        grid-template-columns: 1fr;
        gap: 16px;
        margin-bottom: 40px;
    }
    
    #<?= $componentId ?> .<?= $cls ?>-wealth-features {
        grid-template-columns: 1fr;
        gap: 24px;
        margin-bottom: 40px;
    }
    
    #<?= $componentId ?> .<?= $cls ?>-stat-item {
        padding: 24px 16px;
    }
    
    #<?= $componentId ?> .<?= $cls ?>-feature-card {
        padding: 32px 24px;
    }
    
    #<?= $componentId ?> .<?= $cls ?>-cta-button {
        padding: 16px 32px;
        font-size: 1rem;
    }
}

@media (max-width: 480px) {
    #<?= $componentId ?> .<?= $cls ?>-stat-number {
        font-size: 1.75rem;
    }
    
    #<?= $componentId ?> .<?= $cls ?>-feature-title {
        font-size: 1.25rem;
    }
    
    #<?= $componentId ?> .<?= $cls ?>-feature-desc {
        font-size: 0.875rem;
    }
}
</style>
<script>
const ctaButton = component.querySelector('.<?= $cls ?>-cta-button');
if (ctaButton) {
    ctaButton.addEventListener('click', () => {
        if (typeof FrontendToast !== 'undefined') {
            FrontendToast.success('正在为您跳转到财富计划页面...');
        }
        setTimeout(() => {
            window.location.href = '/wealth-plan';
        }, 1000);
    });
}

const statItems = component.querySelectorAll('.<?= $cls ?>-stat-item');
statItems.forEach(item => {
    item.addEventListener('mouseenter', () => {
        item.style.transform = 'translateY(-8px)';
    });
    item.addEventListener('mouseleave', () => {
        item.style.transform = 'translateY(0)';
    });
});
</script>
