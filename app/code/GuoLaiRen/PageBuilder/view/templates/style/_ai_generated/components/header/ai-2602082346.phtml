<?php
$showGoldEffect = $getConfig('style.gold_effect', 'yes') !== 'no';
$showShadows = $getConfig('style.shadows', 'yes') !== 'no';
$headerHeight = (int)$getConfig('style.height', 90);
$stickyHeader = $getConfig('style.sticky', 'yes') !== 'no';
?>
<header id="<?= $componentId ?>" class="bj-header">
    <div class="bj-header-container">
        <?php if ($showLogo): ?>
        <a href="/" class="bj-header-logo">
            <?php if ($logoImage): ?>
            <img src="<?= htmlspecialchars($logoImage) ?>" alt="<?= htmlspecialchars($logoText) ?>" loading="lazy">
            <?php else: ?>
            <span class="bj-header-logo-text"><?= htmlspecialchars($logoText) ?></span>
            <?php endif; ?>
        </a>
        <?php endif; ?>

        <?php if ($showNav && !empty($navItems)): ?>
        <nav class="bj-header-nav">
            <ul class="bj-header-nav-links">
                <?php foreach ($navItems as $item): ?>
                <li>
                    <a href="<?= htmlspecialchars($item['href']) ?>" class="bj-header-nav-link">
                        <?= htmlspecialchars($item['text']) ?>
                    </a>
                </li>
                <?php endforeach; ?>
            </ul>
        </nav>
        <?php endif; ?>

        <div class="bj-header-actions">
            <?php if ($showCta): ?>
            <a href="<?= htmlspecialchars($ctaUrl) ?>" class="bj-header-cta">
                <?= htmlspecialchars($ctaText) ?>
            </a>
            <?php endif; ?>
            
            <button class="bj-header-menu-toggle" aria-label="菜单">
                <span class="bj-header-menu-toggle-line"></span>
                <span class="bj-header-menu-toggle-line"></span>
                <span class="bj-header-menu-toggle-line"></span>
            </button>
        </div>
    </div>
</header>
<style>
#componentId {
    --pb-primary: var(--pb-accent);
    --pb-bg: #0a0a0f;
    --pb-text: #ffffff;
    --pb-heading: #ffffff;
    --pb-accent: #ffd700;
    --pb-link: #b8b8d0;
    --pb-link-hover: #ffd700;
    --pb-border: rgba(255, 215, 0, 0.2);
    --pb-gold-light: #fff8dc;
    --pb-gold-dark: #b8860b;
    --pb-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);

    background: linear-gradient(135deg, var(--pb-bg) 0%, #1a1a2e 100%);
    color: var(--pb-text);
    position: relative;
    overflow: hidden;
}

#componentId::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--pb-accent), transparent);
    z-index: 1;
}

#componentId .bj-header-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 30px;
    height: 90px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: relative;
    z-index: 2;
}

#componentId .bj-header-logo {
    display: flex;
    align-items: center;
    gap: 15px;
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--pb-text);
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 1px;
    position: relative;
    padding: 10px 20px;
    border-radius: 8px;
    background: rgba(255, 215, 0, 0.1);
    border: 1px solid rgba(255, 215, 0, 0.3);
    transition: all 0.3s ease;
}

#componentId .bj-header-logo:hover {
    background: rgba(255, 215, 0, 0.2);
    border-color: var(--pb-accent);
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(255, 215, 0, 0.2);
}

#componentId .bj-header-logo img {
    height: 45px;
    width: auto;
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
}

#componentId .bj-header-logo-text {
    background: linear-gradient(45deg, var(--pb-gold-light), var(--pb-accent));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
}

#componentId .bj-header-nav {
    display: flex;
    align-items: center;
}

#componentId .bj-header-nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    gap: 40px;
}

#componentId .bj-header-nav-link {
    color: var(--pb-link);
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    padding: 8px 16px;
    border-radius: 6px;
    position: relative;
    transition: all 0.3s ease;
}

#componentId .bj-header-nav-link::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    width: 0;
    height: 2px;
    background: var(--pb-accent);
    transform: translateX(-50%);
    transition: width 0.3s ease;
}

#componentId .bj-header-nav-link:hover {
    color: var(--pb-text);
    background: rgba(255, 215, 0, 0.1);
}

#componentId .bj-header-nav-link:hover::before {
    width: 60%;
}

#componentId .bj-header-actions {
    display: flex;
    align-items: center;
    gap: 20px;
}

#componentId .bj-header-cta {
    padding: 12px 28px;
    background: linear-gradient(45deg, var(--pb-accent), var(--pb-gold-dark));
    color: #000;
    font-size: 1rem;
    font-weight: 700;
    border-radius: 8px;
    text-decoration: none;
    border: none;
    cursor: pointer;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(255, 215, 0, 0.3);
}

#componentId .bj-header-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    transition: left 0.5s ease;
}

#componentId .bj-header-cta:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 25px rgba(255, 215, 0, 0.4);
}

#componentId .bj-header-cta:hover::before {
    left: 100%;
}

#componentId .bj-header-menu-toggle {
    display: none;
    flex-direction: column;
    justify-content: space-between;
    width: 30px;
    height: 24px;
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 0;
    z-index: 100;
}

#componentId .bj-header-menu-toggle-line {
    width: 100%;
    height: 3px;
    background: var(--pb-text);
    border-radius: 2px;
    transition: all 0.3s ease;
}

#componentId .bj-header-menu-toggle.active .bj-header-menu-toggle-line:nth-child(1) {
    transform: rotate(45deg) translate(6px, 6px);
}

#componentId .bj-header-menu-toggle.active .bj-header-menu-toggle-line:nth-child(2) {
    opacity: 0;
}

#componentId .bj-header-menu-toggle.active .bj-header-menu-toggle-line:nth-child(3) {
    transform: rotate(-45deg) translate(6px, -6px);
}
@media (max-width: 1024px) {
    #componentId .bj-header-container {
        padding: 0 20px;
    }
    
    #componentId .bj-header-nav-links {
        gap: 20px;
    }
}

@media (max-width: 768px) {
    #componentId .bj-header-container {
        height: 70px;
        padding: 0 15px;
    }
    
    #componentId .bj-header-nav {
        position: fixed;
        top: 70px;
        left: 0;
        right: 0;
        background: var(--pb-bg);
        padding: 20px;
        transform: translateY(-100%);
        opacity: 0;
        visibility: hidden;
        transition: all 0.3s ease;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        border-top: 1px solid var(--pb-border);
        z-index: 99;
    }
    
    #componentId .bj-header-nav.active {
        transform: translateY(0);
        opacity: 1;
        visibility: visible;
    }
    
    #componentId .bj-header-nav-links {
        flex-direction: column;
        gap: 15px;
    }
    
    #componentId .bj-header-nav-link {
        display: block;
        padding: 12px 20px;
        text-align: center;
        border: 1px solid var(--pb-border);
    }
    
    #componentId .bj-header-nav-link::before {
        display: none;
    }
    
    #componentId .bj-header-menu-toggle {
        display: flex;
    }
    
    #componentId .bj-header-logo {
        padding: 8px 15px;
        font-size: 1.2rem;
    }
    
    #componentId .bj-header-logo img {
        height: 35px;
    }
    
    #componentId .bj-header-cta {
        padding: 10px 20px;
        font-size: 0.9rem;
    }
}

@media (max-width: 480px) {
    #componentId .bj-header-container {
        height: 60px;
    }
    
    #componentId .bj-header-nav {
        top: 60px;
    }
    
    #componentId .bj-header-logo {
        font-size: 1rem;
        padding: 6px 12px;
    }
    
    #componentId .bj-header-logo img {
        height: 30px;
    }
    
    #componentId .bj-header-cta {
        padding: 8px 16px;
        font-size: 0.85rem;
    }
}
</style>
<script>
const menuToggle = component.querySelector('.bj-header-menu-toggle');
const nav = component.querySelector('.bj-header-nav');
const navLinks = component.querySelectorAll('.bj-header-nav-link');

menuToggle.addEventListener('click', () => {
    menuToggle.classList.toggle('active');
    nav.classList.toggle('active');
});

navLinks.forEach(link => {
    link.addEventListener('click', () => {
        if (window.innerWidth <= 768) {
            menuToggle.classList.remove('active');
            nav.classList.remove('active');
        }
    });
});

let lastScrollTop = 0;
const header = component;

window.addEventListener('scroll', () => {
    const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
    
    if (scrollTop > 100) {
        header.style.transform = 'translateY(-100%)';
        header.style.transition = 'transform 0.3s ease';
    } else {
        header.style.transform = 'translateY(0)';
    }
    
    if (scrollTop < lastScrollTop && scrollTop > 100) {
        header.style.transform = 'translateY(0)';
    }
    
    lastScrollTop = scrollTop;
});

const currentPath = window.location.pathname;
navLinks.forEach(link => {
    if (link.getAttribute('href') === currentPath || 
        (currentPath === '/' && link.getAttribute('href') === '/')) {
        link.classList.add('active');
    }
});
</script>