<?php
/**
 * Rummy Overdrive - Dark Header
 *
 * @component_start
 * code: header-dark
 * name: 深色头部
 * name_en: Dark Header
 * description: 深色头部封装，复用主题导航组件
 * category: header
 * region: header
 * @component_end
 *
 * @fields_start
 * button.display => 显示按钮:select:yes|yes,no
 * button.text => 按钮文字:text:Download Pulse
 * button.url => 按钮链接:text:#download
 * logo.display => 显示Logo:select:yes|yes,no
 * logo.url => Logo地址:textarea:|留空使用主题Logo
 * logo.width => Logo宽度:responsive:52/52|px
 * navigation.display => 显示导航:select:yes|yes,no
 * navigation.items => 导航项:textarea:Home=>/\r\nTables=>#tables\r\nWhy Overdrive=>#advantages\r\nPlayers=>#reviews\r\nFAQ=>#faq|每行一项，格式：标题=>链接
 * navigation.use_subpages => 使用页面导航:select:no|yes,no
 * style.accent_color => 强调色:color:#ff4d7a
 * style.background => 背景色:color:rgba(7,10,20,0.82)
 * style.height => 区域高度:number:78|px
 * style.nav_color => 导航文字颜色:color:#f7f8fc
 * @fields_end
 */

$componentConfig = $this->getData('component_config') ?: [];
$componentConfig = is_array($componentConfig) ? $componentConfig : [];
if (!isset($componentConfig['download_button.display']) && isset($componentConfig['button.display']) && $componentConfig['button.display'] !== '') {
    $componentConfig['download_button.display'] = $componentConfig['button.display'];
}
if (!isset($componentConfig['download_button.text']) && isset($componentConfig['button.text']) && $componentConfig['button.text'] !== '') {
    $componentConfig['download_button.text'] = $componentConfig['button.text'];
}
if (!isset($componentConfig['download_button.url']) && isset($componentConfig['button.url']) && $componentConfig['button.url'] !== '') {
    $componentConfig['download_button.url'] = $componentConfig['button.url'];
}
if (!isset($componentConfig['navigation.bg_color']) && isset($componentConfig['style.background']) && $componentConfig['style.background'] !== '') {
    $componentConfig['navigation.bg_color'] = $componentConfig['style.background'];
}
if (!isset($componentConfig['navigation.text_color']) && isset($componentConfig['style.nav_color']) && $componentConfig['style.nav_color'] !== '') {
    $componentConfig['navigation.text_color'] = $componentConfig['style.nav_color'];
    $componentConfig['navigation.link_color'] = $componentConfig['style.nav_color'];
}
if (!isset($componentConfig['navigation.link_hover_color']) && isset($componentConfig['style.accent_color']) && $componentConfig['style.accent_color'] !== '') {
    $componentConfig['navigation.link_hover_color'] = $componentConfig['style.accent_color'];
    $componentConfig['download_button.bg_color'] = $componentConfig['style.accent_color'];
}
if (!isset($componentConfig['navigation.min_height']) && isset($componentConfig['style.height']) && $componentConfig['style.height'] !== '') {
    $componentConfig['navigation.min_height'] = $componentConfig['style.height'];
}

$this->assign('component_config', $componentConfig);
$this->assign('__rov_shell_managed', true);
$this->assign('__pagebuilder_shell_managed', true);
?>
<?= $this->fetchTemplate('GuoLaiRen_PageBuilder::style/rummy-overdrive/components/header/nav.phtml') ?>
