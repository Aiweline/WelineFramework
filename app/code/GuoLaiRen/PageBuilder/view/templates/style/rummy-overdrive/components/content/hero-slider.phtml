<?php
/**
 * Rummy Overdrive - Hero Slider Component
 *
 * @component_start
 * code: hero-slider
 * name: Hero首屏
 * name_en: Hero Slider
 * description: 中央视觉舞台式首屏，保留 tpmst 的 banner/download_btn/layout 配置入口
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:banner => Banner设置
 * banner.display => 显示Banner:select:yes|yes,no
 * banner.image_mobile => 移动端Banner图片:textarea:|默认使用本地hero-rummy-overdrive-mobile.svg
 * banner.image_desktop => PC端Banner图片:textarea:|默认使用本地hero-rummy-overdrive.svg
 * banner.alt => Banner图片描述:text:Rummy Overdrive hero preview
 * banner.bg_color => Banner背景颜色:color:#090f1d
 *
 * group:download_btn => 下载按钮
 * download_btn.display => 显示下载按钮:select:yes|yes,no
 * download_btn.image => 下载按钮图片:textarea:|默认使用本地download-btn-overdrive.svg
 * download_btn.alt => 下载按钮描述:text:Download Rummy Overdrive
 * download_btn.url => 下载链接:text:#download
 *
 * group:layout => 布局设置
 * layout.slider_classes => 轮播区域CSS类:textarea:rov-hero-stage|可配置首屏外层类名
 * layout.margin_top => 顶部负边距(移动端):number:0|px
 * layout.img_margin_top => 图片顶部偏移(PC端):number:-18|px
 *
 * group:hero => 文案设置
 * hero.display => 显示Hero:select:yes|yes,no
 * hero.badge => 徽标:text:Rummy tables. Faster reads.
 * hero.title => 主标题:text:Push Every Rummy Round Into Overdrive
 * hero.subtitle => 副标题:textarea:Rummy Overdrive keeps meld-heavy rooms, joker pressure, and cash-table entry readable at a glance, while giving the homepage a bolder neon pulse.
 * hero.cta_text => 主按钮文字:text:Download Overdrive
 * hero.cta_url => 主按钮链接:text:#download
 * hero.secondary_cta_text => 次按钮文字:text:View Table Modes
 * hero.secondary_cta_url => 次按钮链接:text:#tables
 * hero.image => Hero桌面端图片:textarea:|留空不显示额外Hero图
 * hero.image_mobile => Hero移动端图片:textarea:|留空时沿用桌面端图片
 * hero.alt => Hero图片描述:text:Rummy Overdrive hero preview
 * hero.bg_color => Hero背景基础色:color:#090f1d
 * hero.stat_1_value => 数据1数值:text:13-card
 * hero.stat_1_label => 数据1说明:text:Rummy-first table logic
 * hero.stat_2_value => 数据2数值:text:Joker-led
 * hero.stat_2_label => 数据2说明:text:Fast hand reading
 * hero.stat_3_value => 数据3数值:text:24/7
 * hero.stat_3_label => 数据3说明:text:Cash and practice rooms
 *
 * @fields_end
 */

$page = $this->getData('page') ?? null;
$componentConfig = $this->getData('component_config') ?: [];
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$styleSettings = is_array($styleSettings) ? $styleSettings : [];
$componentConfig = is_array($componentConfig) ? $componentConfig : [];
$componentId = 'rov-hero-' . uniqid();

$getConfigAny = function (array $keys, $default = '') use ($styleSettings, $componentConfig) {
    foreach ($keys as $key) {
        if (array_key_exists($key, $componentConfig) && $componentConfig[$key] !== '') {
            return $componentConfig[$key];
        }
    }

    foreach ($keys as $key) {
        if (array_key_exists($key, $styleSettings) && $styleSettings[$key] !== '') {
            return $styleSettings[$key];
        }
    }

    return $default;
};

$sanitizeTitle = static function ($value) {
    $value = trim((string) $value);
    if ($value === '') {
        return $value;
    }
    $value = preg_replace('/^样式模板预览\s*[-–—]*\s*/u', '', $value);
    $value = str_replace('样式模板预览', '', $value);
    return trim($value);
};

$normalizeToggle = static function ($value, $default = 'yes') {
    if ($value === '' || $value === null) {
        return $default;
    }
    if ($value === 0 || $value === '0') {
        return 'yes';
    }
    if ($value === 1 || $value === '1') {
        return 'no';
    }
    return ($value === 'no' || $value === false || $value === 'false') ? 'no' : 'yes';
};

$pageTitle = $sanitizeTitle((string) ($page ? ($page->getData('meta_title') ?: $page->getData('title') ?: '') : ''));
if ($pageTitle === '') {
    $pageTitle = 'Rummy Overdrive';
}

$brandName = $sanitizeTitle((string) $getConfigAny(['logo.text', 'header.logo.text'], $pageTitle));
if ($brandName === '') {
    $brandName = 'Rummy Overdrive';
}

$heroDisplay = $normalizeToggle($getConfigAny(['hero.display', 'banner.display'], 'yes'), 'yes') === 'yes';
if (!$heroDisplay) {
    return;
}

$bannerAlt = trim((string) $getConfigAny(['hero.alt', 'banner.alt'], ''));
$bannerBgColor = (string) ($styleSettings['hero_bg'] ?? $getConfigAny(['hero.bg_color', 'banner.bg_color'], '#090f1d'));

$downloadBtnDisplay = $normalizeToggle($getConfigAny(['download_btn.display'], 'yes'), 'yes') === 'yes';
$downloadBtnImage = trim((string) $getConfigAny(['download_btn.image'], ''));
$downloadBtnAlt = (string) $getConfigAny(['download_btn.alt'], 'Download Rummy Overdrive');

$sliderClasses = trim((string) $getConfigAny(['layout.slider_classes'], 'rov-hero-stage'));
$marginTop = (int) $getConfigAny(['layout.margin_top'], '0');
$imgMarginTop = (int) $getConfigAny(['layout.img_margin_top'], '-18');
$badge = (string) $getConfigAny(['hero.badge'], 'Rummy tables. Faster reads.');
$title = $sanitizeTitle((string) $getConfigAny(['hero.title'], $pageTitle));
if ($title === '') {
    $title = 'Push Every Rummy Round Into Overdrive';
}
$subtitle = trim((string) $getConfigAny(
    ['hero.subtitle'],
    'Rummy Overdrive keeps meld-heavy rooms, joker pressure, and cash-table entry readable at a glance, while giving the homepage a bolder neon pulse.'
));
$ctaText = (string) $getConfigAny(['hero.cta_text', 'download_btn.alt'], 'Download Overdrive');
$ctaUrl = (string) $getConfigAny(['hero.cta_url', 'download_btn.url'], '#download');
$secondaryText = (string) $getConfigAny(['hero.secondary_cta_text'], 'View Table Modes');
$secondaryUrl = (string) $getConfigAny(['hero.secondary_cta_url'], '#tables');
$heroImageDesktop = trim((string) $getConfigAny(['hero.image', 'banner.image_desktop'], ''));
$heroImageMobile = trim((string) $getConfigAny(['hero.image_mobile', 'banner.image_mobile'], ''));
if ($heroImageMobile === '') {
    $heroImageMobile = $heroImageDesktop;
}
$heroAlt = $bannerAlt !== '' ? $bannerAlt : $title;

$heroStats = [
    [
        'value' => trim((string) $getConfigAny(['hero.stat_1_value'], '13-card')),
        'label' => trim((string) $getConfigAny(['hero.stat_1_label'], 'Rummy-first table logic')),
    ],
    [
        'value' => trim((string) $getConfigAny(['hero.stat_2_value'], 'Joker-led')),
        'label' => trim((string) $getConfigAny(['hero.stat_2_label'], 'Fast hand reading')),
    ],
    [
        'value' => trim((string) $getConfigAny(['hero.stat_3_value'], '24/7')),
        'label' => trim((string) $getConfigAny(['hero.stat_3_label'], 'Cash and practice rooms')),
    ],
];
$heroStats = array_values(array_filter($heroStats, static function ($item) {
    return ($item['value'] ?? '') !== '' || ($item['label'] ?? '') !== '';
}));

$fallingCards = [
    ['rank' => 'A',  'symbol' => '&spades;',  'suit' => 'spades',   'left' => '4%',  'size' => '78px',  'delay' => '-2.4s', 'duration' => '14.8s', 'drift' => '32px',  'rotateStart' => '-18deg', 'rotateEnd' => '14deg',  'opacity' => '0.22', 'radius' => '18px 24px 22px 28px / 24px 18px 28px 22px'],
    ['rank' => 'K',  'symbol' => '&hearts;',  'suit' => 'hearts',   'left' => '11%', 'size' => '92px',  'delay' => '-8.2s', 'duration' => '17.6s', 'drift' => '-38px', 'rotateStart' => '12deg',  'rotateEnd' => '-14deg', 'opacity' => '0.18', 'radius' => '26px 18px 24px 20px / 20px 26px 18px 24px'],
    ['rank' => '7',  'symbol' => '&clubs;',   'suit' => 'clubs',    'left' => '18%', 'size' => '66px',  'delay' => '-5.1s', 'duration' => '13.4s', 'drift' => '22px',  'rotateStart' => '-8deg',  'rotateEnd' => '18deg',  'opacity' => '0.26', 'radius' => '16px 22px 18px 24px / 22px 16px 24px 18px'],
    ['rank' => 'Q',  'symbol' => '&diams;',   'suit' => 'diamonds', 'left' => '24%', 'size' => '104px', 'delay' => '-10.7s','duration' => '18.8s', 'drift' => '42px',  'rotateStart' => '-16deg', 'rotateEnd' => '10deg',  'opacity' => '0.16', 'radius' => '24px 28px 18px 24px / 18px 24px 28px 22px'],
    ['rank' => '10', 'symbol' => '&spades;',  'suit' => 'spades',   'left' => '31%', 'size' => '72px',  'delay' => '-1.8s', 'duration' => '15.2s', 'drift' => '-26px', 'rotateStart' => '8deg',   'rotateEnd' => '-18deg', 'opacity' => '0.2',  'radius' => '22px 18px 24px 18px / 18px 24px 18px 22px'],
    ['rank' => 'J',  'symbol' => '&hearts;',  'suit' => 'hearts',   'left' => '39%', 'size' => '88px',  'delay' => '-9.3s', 'duration' => '19.6s', 'drift' => '34px',  'rotateStart' => '-14deg', 'rotateEnd' => '16deg',  'opacity' => '0.14', 'radius' => '28px 22px 20px 26px / 22px 26px 18px 24px'],
    ['rank' => '5',  'symbol' => '&clubs;',   'suit' => 'clubs',    'left' => '46%', 'size' => '60px',  'delay' => '-3.4s', 'duration' => '12.6s', 'drift' => '-20px', 'rotateStart' => '10deg',  'rotateEnd' => '-8deg',  'opacity' => '0.24', 'radius' => '18px 20px 16px 22px / 20px 18px 22px 16px'],
    ['rank' => '6',  'symbol' => '&hearts;',  'suit' => 'hearts',   'left' => '49%', 'size' => '58px',  'delay' => '-13.3s','duration' => '11.9s', 'drift' => '16px',  'rotateStart' => '-9deg',  'rotateEnd' => '12deg',  'opacity' => '0.26', 'radius' => '18px 16px 20px 18px / 16px 22px 18px 20px'],
    ['rank' => 'A',  'symbol' => '&diams;',   'suit' => 'diamonds', 'left' => '53%', 'size' => '110px', 'delay' => '-6.8s', 'duration' => '20.8s', 'drift' => '48px',  'rotateStart' => '-12deg', 'rotateEnd' => '22deg',  'opacity' => '0.12', 'radius' => '30px 20px 28px 18px / 20px 28px 18px 30px'],
    ['rank' => '9',  'symbol' => '&spades;',  'suit' => 'spades',   'left' => '59%', 'size' => '76px',  'delay' => '-12.1s','duration' => '16.2s', 'drift' => '-30px', 'rotateStart' => '18deg',  'rotateEnd' => '-16deg', 'opacity' => '0.2',  'radius' => '24px 18px 24px 20px / 18px 24px 20px 26px'],
    ['rank' => '4',  'symbol' => '&diams;',   'suit' => 'diamonds', 'left' => '63%', 'size' => '56px',  'delay' => '-14.7s','duration' => '11.5s', 'drift' => '-14px', 'rotateStart' => '7deg',   'rotateEnd' => '-10deg', 'opacity' => '0.28', 'radius' => '16px 20px 18px 22px / 20px 16px 22px 18px'],
    ['rank' => 'K',  'symbol' => '&clubs;',   'suit' => 'clubs',    'left' => '66%', 'size' => '68px',  'delay' => '-4.7s', 'duration' => '14.2s', 'drift' => '24px',  'rotateStart' => '-10deg', 'rotateEnd' => '12deg',  'opacity' => '0.23', 'radius' => '20px 22px 18px 20px / 22px 20px 24px 18px'],
    ['rank' => 'Q',  'symbol' => '&hearts;',  'suit' => 'hearts',   'left' => '72%', 'size' => '94px',  'delay' => '-11.5s','duration' => '18.2s', 'drift' => '-40px', 'rotateStart' => '14deg',  'rotateEnd' => '-18deg', 'opacity' => '0.15', 'radius' => '24px 30px 20px 24px / 26px 18px 28px 20px'],
    ['rank' => '7',  'symbol' => '&spades;',  'suit' => 'spades',   'left' => '76%', 'size' => '54px',  'delay' => '-6.3s', 'duration' => '10.8s', 'drift' => '12px',  'rotateStart' => '-12deg', 'rotateEnd' => '8deg',   'opacity' => '0.28', 'radius' => '18px 14px 20px 16px / 14px 20px 16px 22px'],
    ['rank' => '8',  'symbol' => '&diams;',   'suit' => 'diamonds', 'left' => '79%', 'size' => '70px',  'delay' => '-7.4s', 'duration' => '13.8s', 'drift' => '18px',  'rotateStart' => '-6deg',  'rotateEnd' => '14deg',  'opacity' => '0.22', 'radius' => '18px 24px 18px 22px / 24px 18px 22px 18px'],
    ['rank' => '10', 'symbol' => '&spades;',  'suit' => 'spades',   'left' => '85%', 'size' => '82px',  'delay' => '-2.9s', 'duration' => '15.8s', 'drift' => '-28px', 'rotateStart' => '11deg',  'rotateEnd' => '-12deg', 'opacity' => '0.18', 'radius' => '26px 20px 24px 22px / 18px 24px 20px 28px'],
    ['rank' => 'J',  'symbol' => '&clubs;',   'suit' => 'clubs',    'left' => '91%', 'size' => '64px',  'delay' => '-9.8s', 'duration' => '12.9s', 'drift' => '14px',  'rotateStart' => '-16deg', 'rotateEnd' => '10deg',  'opacity' => '0.24', 'radius' => '20px 18px 22px 16px / 18px 22px 16px 20px'],
    ['rank' => '5',  'symbol' => '&hearts;',  'suit' => 'hearts',   'left' => '95%', 'size' => '52px',  'delay' => '-15.6s','duration' => '10.6s', 'drift' => '-10px', 'rotateStart' => '9deg',   'rotateEnd' => '-7deg',  'opacity' => '0.26', 'radius' => '16px 18px 14px 20px / 18px 14px 20px 16px'],
];
?>

<section id="<?= $componentId ?>" class="rov-hero <?= htmlspecialchars($sliderClasses) ?>" aria-labelledby="<?= $componentId ?>-title">
    <div class="hero-noise" aria-hidden="true"></div>
    <div class="hero-card-rain" aria-hidden="true">
        <span class="hero-card-glow glow-a"></span>
        <span class="hero-card-glow glow-b"></span>
        <span class="hero-card-glow glow-c"></span>
        <span class="hero-light-trail trail-a"></span>
        <span class="hero-light-trail trail-b"></span>
        <span class="hero-light-trail trail-c"></span>
        <span class="hero-light-trail trail-d"></span>
        <?php if ($heroImageDesktop !== '' || $heroImageMobile !== ''): ?>
            <picture class="hero-stage-art">
                <?php if ($heroImageMobile !== ''): ?>
                    <source media="(max-width: 767px)" srcset="<?= htmlspecialchars($heroImageMobile) ?>">
                <?php endif; ?>
                <?php if ($heroImageDesktop !== ''): ?>
                    <img src="<?= htmlspecialchars($heroImageDesktop) ?>" alt="" loading="eager" decoding="async">
                <?php else: ?>
                    <img src="<?= htmlspecialchars($heroImageMobile) ?>" alt="" loading="eager" decoding="async">
                <?php endif; ?>
            </picture>
        <?php endif; ?>
        <?php foreach ($fallingCards as $card): ?>
            <span
                class="hero-falling-card suit-<?= htmlspecialchars($card['suit']) ?>"
                style="--card-left: <?= htmlspecialchars($card['left']) ?>; --card-size: <?= htmlspecialchars($card['size']) ?>; --card-delay: <?= htmlspecialchars($card['delay']) ?>; --card-duration: <?= htmlspecialchars($card['duration']) ?>; --card-drift: <?= htmlspecialchars($card['drift']) ?>; --card-rotate-start: <?= htmlspecialchars($card['rotateStart']) ?>; --card-rotate-end: <?= htmlspecialchars($card['rotateEnd']) ?>; --card-opacity: <?= htmlspecialchars($card['opacity']) ?>; --card-radius: <?= htmlspecialchars($card['radius']) ?>;"
            >
                <span class="card-rank card-rank-top"><?= htmlspecialchars($card['rank']) ?></span>
                <span class="card-suit"><?= $card['symbol'] ?></span>
                <span class="card-rank card-rank-bottom"><?= htmlspecialchars($card['rank']) ?></span>
            </span>
        <?php endforeach; ?>
    </div>
    <div class="hero-shell">
        <header class="hero-copy">
            <?php if ($badge !== ''): ?>
                <span class="hero-badge hero-reveal hero-reveal-down"><?= htmlspecialchars($badge) ?></span>
            <?php endif; ?>

            <h1 id="<?= $componentId ?>-title" class="hero-reveal hero-reveal-up" style="--hero-delay: 80ms;" data-title="<?= htmlspecialchars($title) ?>"><?= htmlspecialchars($title) ?></h1>

            <?php if ($subtitle !== ''): ?>
                <p class="hero-subtitle hero-reveal hero-reveal-up" style="--hero-delay: 160ms;"><?= htmlspecialchars($subtitle) ?></p>
            <?php endif; ?>

            <div class="hero-actions hero-reveal hero-reveal-up" style="--hero-delay: 240ms;">
                <?php if ($downloadBtnDisplay && $ctaText !== ''): ?>
                    <a href="<?= htmlspecialchars($ctaUrl) ?>" class="hero-primary" aria-label="<?= htmlspecialchars($ctaText) ?>">
                        <?php if ($downloadBtnImage !== ''): ?>
                            <span class="hero-primary-image-wrap" aria-hidden="true">
                                <img class="hero-primary-image" src="<?= htmlspecialchars($downloadBtnImage) ?>" alt="" loading="lazy" decoding="async">
                            </span>
                        <?php else: ?>
                            <span class="hero-primary-icon" aria-hidden="true">
                                <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                                    <path d="M12 4.5V14.5" stroke="currentColor" stroke-width="2.6" stroke-linecap="round"/>
                                    <path d="M7.75 11.25L12 15.75L16.25 11.25" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/>
                                </svg>
                            </span>
                        <?php endif; ?>
                        <span class="hero-primary-copy">
                            <em><?= htmlspecialchars($brandName) ?></em>
                            <strong><?= htmlspecialchars($ctaText) ?></strong>
                        </span>
                    </a>
                <?php endif; ?>
                <?php if ($secondaryText !== ''): ?>
                    <a href="<?= htmlspecialchars($secondaryUrl) ?>" class="hero-secondary"><?= htmlspecialchars($secondaryText) ?></a>
                <?php endif; ?>
            </div>

            <?php if (!empty($heroStats)): ?>
                <div class="hero-stats" aria-label="<?= htmlspecialchars($heroAlt) ?>">
                    <?php foreach ($heroStats as $index => $stat): ?>
                        <article class="hero-stat hero-reveal hero-reveal-up" style="--hero-delay: <?= 320 + ($index * 80) ?>ms;">
                            <strong><?= htmlspecialchars($stat['value']) ?></strong>
                            <span><?= htmlspecialchars($stat['label']) ?></span>
                        </article>
                    <?php endforeach; ?>
                </div>
            <?php endif; ?>
        </header>
    </div>
</section>

<style>
#<?= $componentId ?> {
    --hero-bg: <?= htmlspecialchars($bannerBgColor) ?>;
    --hero-mobile-offset: <?= $marginTop ?>px;
    --hero-art-offset: <?= (int) $imgMarginTop ?>px;
    position: relative;
    margin-top: 0;
    padding: clamp(6.6rem, 9vw, 8.6rem) 0 clamp(4.2rem, 7vw, 5.8rem);
    min-height: clamp(660px, 94vh, 900px);
    overflow: hidden;
    isolation: isolate;
    background:
        radial-gradient(920px 520px at 50% -12%, rgba(255, 77, 122, 0.3), transparent 62%),
        radial-gradient(720px 460px at 8% 18%, rgba(43, 231, 255, 0.18), transparent 62%),
        radial-gradient(680px 420px at 92% 20%, rgba(210, 255, 63, 0.15), transparent 58%),
        linear-gradient(180deg, rgba(6, 9, 17, 0.94) 0%, var(--hero-bg) 42%, #04060d 100%);
}

#<?= $componentId ?> .hero-noise {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
        linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px),
        linear-gradient(180deg, rgba(255,255,255,0.025) 1px, transparent 1px);
    background-size: 34px 34px;
    opacity: 0.12;
}

#<?= $componentId ?> .hero-shell {
    width: var(--rov-shell-width, min(1280px, calc(100% - 1.2rem)));
    margin: 0 auto;
    position: relative;
    z-index: 2;
    min-height: clamp(520px, 70vh, 720px);
    display: flex;
    align-items: center;
}

#<?= $componentId ?> .hero-copy {
    max-width: 920px;
    margin: 0 auto;
    text-align: center;
}

#<?= $componentId ?> .hero-card-rain {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
    z-index: 0;
}

#<?= $componentId ?> .hero-card-rain::before,
#<?= $componentId ?> .hero-card-rain::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
}

#<?= $componentId ?> .hero-card-rain::before {
    background:
        radial-gradient(520px 220px at 18% 16%, rgba(43, 231, 255, 0.08), transparent 72%),
        radial-gradient(620px 260px at 82% 18%, rgba(255, 77, 122, 0.08), transparent 74%),
        radial-gradient(540px 220px at 52% 88%, rgba(210, 255, 63, 0.05), transparent 70%);
    opacity: 0.9;
}

#<?= $componentId ?> .hero-card-rain::after {
    background: linear-gradient(180deg, rgba(255,255,255,0.06) 0%, rgba(255,255,255,0) 22%, rgba(255,255,255,0.04) 52%, rgba(255,255,255,0) 100%);
    mix-blend-mode: screen;
    opacity: 0.22;
}

#<?= $componentId ?> .hero-stage-art {
    position: absolute;
    left: 50%;
    bottom: clamp(28px, 6vw, 72px);
    width: min(560px, 58vw);
    transform: translate3d(-50%, var(--hero-art-offset), 0);
    opacity: 0.14;
    z-index: 1;
    filter: drop-shadow(0 24px 48px rgba(0, 0, 0, 0.26));
    mix-blend-mode: screen;
}

#<?= $componentId ?> .hero-stage-art img {
    width: 100%;
    height: auto;
    display: block;
}

#<?= $componentId ?> .hero-card-glow {
    position: absolute;
    border-radius: 999px;
    filter: blur(110px);
    opacity: 0.3;
    animation: rov-glow-breathe 11s ease-in-out infinite;
}

#<?= $componentId ?> .glow-a {
    width: 320px;
    height: 320px;
    left: -6%;
    top: 18%;
    background: rgba(43, 231, 255, 0.28);
}

#<?= $componentId ?> .glow-b {
    width: 420px;
    height: 420px;
    right: -10%;
    top: 4%;
    background: rgba(255, 77, 122, 0.24);
    animation-delay: -3.5s;
}

#<?= $componentId ?> .glow-c {
    width: 360px;
    height: 360px;
    left: 38%;
    bottom: -10%;
    background: rgba(210, 255, 63, 0.16);
    animation-delay: -7s;
}

#<?= $componentId ?> .hero-light-trail {
    --trail-angle: -14deg;
    position: absolute;
    left: -28%;
    width: 56vw;
    height: 3px;
    border-radius: 999px;
    opacity: 0;
    filter: blur(2px);
    background: linear-gradient(90deg, rgba(255,255,255,0) 0%, rgba(255,255,255,0.18) 16%, rgba(43,231,255,0.28) 42%, rgba(255,77,122,0.22) 72%, rgba(255,255,255,0) 100%);
    box-shadow:
        0 0 20px rgba(43, 231, 255, 0.12),
        0 0 28px rgba(255, 77, 122, 0.1);
    transform: rotate(var(--trail-angle));
    transform-origin: left center;
    animation: rov-trail-sweep 18s linear infinite;
    will-change: transform, opacity;
}

#<?= $componentId ?> .trail-a {
    top: 14%;
    --trail-angle: -14deg;
    animation-delay: -2s;
}

#<?= $componentId ?> .trail-b {
    top: 32%;
    width: 48vw;
    --trail-angle: -11deg;
    animation-duration: 21s;
    animation-delay: -9s;
}

#<?= $componentId ?> .trail-c {
    top: 56%;
    width: 54vw;
    --trail-angle: -16deg;
    animation-duration: 19.5s;
    animation-delay: -5.5s;
}

#<?= $componentId ?> .trail-d {
    top: 74%;
    width: 44vw;
    --trail-angle: -10deg;
    animation-duration: 23s;
    animation-delay: -12s;
}

#<?= $componentId ?> .hero-falling-card {
    position: absolute;
    top: -28%;
    left: var(--card-left);
    width: var(--card-size);
    aspect-ratio: 0.7;
    border-radius: var(--card-radius);
    border: 1px solid rgba(255, 255, 255, 0.34);
    background:
        linear-gradient(155deg, rgba(255, 255, 255, 0.96) 0%, rgba(249, 247, 240, 0.82) 58%, rgba(255, 255, 255, 0.9) 100%);
    box-shadow:
        0 18px 38px rgba(0, 0, 0, 0.22),
        inset 0 1px 0 rgba(255, 255, 255, 0.65);
    opacity: 0;
    transform: translate3d(0, -18vh, 0) rotate(var(--card-rotate-start));
    transform-origin: center;
    animation: rov-card-fall var(--card-duration) linear infinite;
    animation-delay: var(--card-delay);
    will-change: transform, opacity;
}

#<?= $componentId ?> .hero-falling-card::before {
    content: '';
    position: absolute;
    inset: 6px;
    border-radius: inherit;
    background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.32) 0%, rgba(255, 255, 255, 0) 52%),
        radial-gradient(circle at 24% 18%, rgba(255, 255, 255, 0.55), transparent 36%);
}

#<?= $componentId ?> .hero-falling-card::after {
    content: '';
    position: absolute;
    inset: 14% 18%;
    border-radius: inherit;
    background: linear-gradient(120deg, rgba(255,255,255,0.16) 0%, rgba(255,255,255,0) 42%, rgba(255,255,255,0.18) 100%);
    opacity: 0.75;
}

#<?= $componentId ?> .card-rank,
#<?= $componentId ?> .card-suit {
    position: absolute;
    z-index: 1;
}

#<?= $componentId ?> .card-rank {
    left: 12%;
    font-family: 'Sora', 'Space Grotesk', sans-serif;
    font-size: clamp(0.8rem, 1vw, 1rem);
    font-weight: 800;
    letter-spacing: -0.05em;
}

#<?= $componentId ?> .card-rank-top {
    top: 10%;
}

#<?= $componentId ?> .card-rank-bottom {
    right: 12%;
    bottom: 10%;
    left: auto;
    transform: rotate(180deg);
}

#<?= $componentId ?> .card-suit {
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: clamp(1.4rem, 2.5vw, 2.4rem);
}

#<?= $componentId ?> .suit-hearts .card-rank,
#<?= $componentId ?> .suit-hearts .card-suit,
#<?= $componentId ?> .suit-diamonds .card-rank,
#<?= $componentId ?> .suit-diamonds .card-suit {
    color: #ff5d77;
}

#<?= $componentId ?> .suit-spades .card-rank,
#<?= $componentId ?> .suit-spades .card-suit {
    color: #1a2030;
}

#<?= $componentId ?> .suit-clubs .card-rank,
#<?= $componentId ?> .suit-clubs .card-suit {
    color: #0b7f9f;
}

#<?= $componentId ?> .hero-reveal {
    opacity: 0;
    animation-duration: 0.92s;
    animation-timing-function: cubic-bezier(0.22, 1, 0.36, 1);
    animation-fill-mode: both;
    animation-delay: var(--hero-delay, 0ms);
    will-change: transform, opacity;
}

#<?= $componentId ?> .hero-reveal-down {
    animation-name: rov-hero-drop-in;
}

#<?= $componentId ?> .hero-reveal-up {
    animation-name: rov-hero-rise-in;
}

#<?= $componentId ?> .hero-reveal-scale {
    animation-name: rov-hero-scale-in;
}

#<?= $componentId ?> .hero-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 34px;
    padding: 0.45rem 0.9rem;
    border-radius: 999px;
    border: 1px solid rgba(43, 231, 255, 0.22);
    background: rgba(9, 16, 32, 0.68);
    color: #9ff4ff;
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    box-shadow: 0 0 24px rgba(43, 231, 255, 0.12);
    text-shadow:
        0 0 12px rgba(43, 231, 255, 0.18),
        0 0 24px rgba(43, 231, 255, 0.1);
}

#<?= $componentId ?> h1 {
    position: relative;
    isolation: isolate;
    margin-top: 1rem;
    color: #ffffff;
    font-size: clamp(2.7rem, 5.8vw, 5.6rem);
    line-height: 0.92;
    letter-spacing: -0.06em;
    text-shadow:
        0 0 16px rgba(255, 255, 255, 0.28),
        0 0 34px rgba(255, 255, 255, 0.18),
        0 0 54px rgba(255, 77, 122, 0.24),
        0 0 88px rgba(43, 231, 255, 0.2),
        0 0 128px rgba(255, 77, 122, 0.14);
    filter:
        drop-shadow(0 0 14px rgba(255, 255, 255, 0.16))
        drop-shadow(0 0 28px rgba(255, 77, 122, 0.18))
        drop-shadow(0 0 42px rgba(43, 231, 255, 0.16));
}

#<?= $componentId ?> h1::before,
#<?= $componentId ?> h1::after {
    content: attr(data-title);
    position: absolute;
    inset: 0;
    pointer-events: none;
}

#<?= $componentId ?> h1::before {
    z-index: -2;
    color: rgba(255, 255, 255, 0.82);
    opacity: 0.5;
    filter: blur(26px);
    text-shadow:
        0 0 38px rgba(255, 255, 255, 0.22),
        0 0 88px rgba(255, 77, 122, 0.2),
        0 0 140px rgba(43, 231, 255, 0.16);
    animation: rov-title-breathe-soft 5.8s ease-in-out infinite;
}

#<?= $componentId ?> h1::after {
    z-index: -1;
    color: rgba(255, 255, 255, 0.96);
    opacity: 0.92;
    filter: blur(14px);
    text-shadow:
        0 0 24px rgba(255, 255, 255, 0.34),
        0 0 54px rgba(255, 77, 122, 0.34),
        0 0 92px rgba(43, 231, 255, 0.28),
        0 0 138px rgba(255, 77, 122, 0.18);
    animation: rov-title-breathe 5.8s ease-in-out infinite;
}

#<?= $componentId ?> .hero-subtitle {
    margin: 1.15rem auto 0;
    max-width: 48rem;
    color: #c9cfdd;
    font-size: clamp(1rem, 1.5vw, 1.14rem);
    line-height: 1.85;
    text-shadow:
        0 0 12px rgba(255, 255, 255, 0.08),
        0 0 28px rgba(43, 231, 255, 0.08);
}

#<?= $componentId ?> .hero-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: 0.8rem;
    margin-top: 1.55rem;
}

#<?= $componentId ?> .hero-primary,
#<?= $componentId ?> .hero-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 54px;
    padding: 0.8rem 1.2rem;
    border-radius: 999px;
    font-size: 0.9rem;
    font-weight: 800;
    text-decoration: none;
    transition: transform 0.26s ease, box-shadow 0.26s ease, background 0.26s ease;
}

#<?= $componentId ?> .hero-primary {
    gap: 0.1rem;
    min-height: 54px;
    padding: 0.46rem 1.35rem 0.46rem 0.5rem;
    color: #05070e;
    background: linear-gradient(90deg, #ff4d7a 0%, #ff8c42 52%, #d2ff3f 100%);
    box-shadow:
        0 18px 38px rgba(255, 77, 122, 0.24),
        inset 0 1px 0 rgba(255, 255, 255, 0.42);
}

#<?= $componentId ?> .hero-secondary {
    color: #f7f8fc;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.1);
}

#<?= $componentId ?> .hero-primary-icon {
    width: 40px;
    height: 40px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

#<?= $componentId ?> .hero-primary-icon svg {
    width: 30px;
    height: 30px;
    display: block;
}

#<?= $componentId ?> .hero-primary-image-wrap {
    width: 42px;
    height: 42px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

#<?= $componentId ?> .hero-primary-image {
    max-width: 100%;
    max-height: 100%;
    display: block;
}

#<?= $componentId ?> .hero-primary-copy {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    text-align: left;
    line-height: 1.05;
}

#<?= $componentId ?> .hero-primary-copy em {
    font-style: normal;
    font-size: 0.66rem;
    font-weight: 800;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    opacity: 0.76;
}

#<?= $componentId ?> .hero-primary-copy strong {
    display: block;
    margin-top: 0.18rem;
    font-size: 1rem;
    letter-spacing: -0.03em;
    text-shadow: 0 0 18px rgba(255, 255, 255, 0.18);
}

#<?= $componentId ?> .hero-primary:hover,
#<?= $componentId ?> .hero-secondary:hover {
    transform: translateY(-2px);
}

#<?= $componentId ?> .hero-stats {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.85rem;
    margin: 1.8rem auto 0;
    max-width: 860px;
}

#<?= $componentId ?> .hero-stat {
    min-width: 0;
    padding: 0.95rem 1rem;
    border-radius: 22px;
    border: 1px solid rgba(255,255,255,0.08);
    background: linear-gradient(180deg, rgba(18, 24, 43, 0.82) 0%, rgba(8, 11, 20, 0.72) 100%);
    box-shadow: 0 18px 34px rgba(0, 0, 0, 0.18);
}

#<?= $componentId ?> .hero-stat strong {
    display: block;
    color: #d2ff3f;
    font-size: clamp(1.12rem, 2vw, 1.36rem);
    text-shadow:
        0 0 14px rgba(210, 255, 63, 0.2),
        0 0 28px rgba(210, 255, 63, 0.1);
}

#<?= $componentId ?> .hero-stat span {
    display: block;
    margin-top: 0.3rem;
    color: #aeb7ca;
    font-size: 0.84rem;
    line-height: 1.55;
    text-shadow: 0 0 18px rgba(255, 255, 255, 0.06);
}

@keyframes rov-hero-drop-in {
    0% {
        opacity: 0;
        transform: translate3d(0, -18px, 0) scale(0.98);
    }
    100% {
        opacity: 1;
        transform: translate3d(0, 0, 0) scale(1);
    }
}

@keyframes rov-hero-rise-in {
    0% {
        opacity: 0;
        transform: translate3d(0, 28px, 0);
    }
    100% {
        opacity: 1;
        transform: translate3d(0, 0, 0);
    }
}

@keyframes rov-hero-scale-in {
    0% {
        opacity: 0;
        transform: translate3d(0, 24px, 0) scale(0.94);
    }
    100% {
        opacity: 1;
        transform: translate3d(0, 0, 0) scale(1);
    }
}

@keyframes rov-card-fall {
    0% {
        opacity: 0;
        transform: translate3d(0, -18vh, 0) rotate(var(--card-rotate-start)) scale(0.92);
    }
    8% {
        opacity: var(--card-opacity);
    }
    48% {
        opacity: var(--card-opacity);
    }
    100% {
        opacity: 0;
        transform: translate3d(var(--card-drift), 122vh, 0) rotate(var(--card-rotate-end)) scale(1.06);
    }
}

@keyframes rov-glow-breathe {
    0%, 100% {
        transform: scale(0.94);
        opacity: 0.24;
    }
    50% {
        transform: scale(1.08);
        opacity: 0.38;
    }
}

@keyframes rov-trail-sweep {
    0% {
        opacity: 0;
        transform: translate3d(0, 0, 0) rotate(var(--trail-angle)) scaleX(0.88);
    }
    10% {
        opacity: 0.18;
    }
    42% {
        opacity: 0.22;
    }
    100% {
        opacity: 0;
        transform: translate3d(168vw, 0, 0) rotate(var(--trail-angle)) scaleX(1.04);
    }
}

@keyframes rov-title-breathe {
    0%, 100% {
        opacity: 0.82;
        filter: blur(12px);
        transform: scale(0.996);
    }
    50% {
        opacity: 1;
        filter: blur(18px);
        transform: scale(1.018);
    }
}

@keyframes rov-title-breathe-soft {
    0%, 100% {
        opacity: 0.34;
        filter: blur(24px);
        transform: scale(0.99);
    }
    50% {
        opacity: 0.58;
        filter: blur(34px);
        transform: scale(1.03);
    }
}

@media (max-width: 991px) {
    #<?= $componentId ?> {
        margin-top: var(--hero-mobile-offset);
        padding-top: clamp(6rem, 18vw, 7rem);
        min-height: 0;
    }

    #<?= $componentId ?> .hero-shell {
        min-height: 0;
    }

    #<?= $componentId ?> .hero-stats {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    #<?= $componentId ?> .hero-stage-art {
        width: min(440px, 64vw);
        bottom: 52px;
    }
}

@media (max-width: 767px) {
    #<?= $componentId ?> {
        padding-bottom: 2.5rem;
    }

    #<?= $componentId ?> .hero-shell {
        width: min(100% - 1rem, 100%);
    }

    #<?= $componentId ?> .hero-actions {
        gap: 0.7rem;
    }

    #<?= $componentId ?> .hero-primary,
    #<?= $componentId ?> .hero-secondary {
        flex: 1 1 100%;
        min-width: 0;
    }

    #<?= $componentId ?> .hero-primary {
        min-height: 60px;
        padding: 0.42rem 1rem 0.42rem 0.44rem;
    }

    #<?= $componentId ?> .hero-primary-icon {
        width: 44px;
        height: 44px;
    }

    #<?= $componentId ?> .hero-primary-image-wrap {
        width: 44px;
        height: 44px;
    }

    #<?= $componentId ?> .hero-primary-copy em {
        font-size: 0.6rem;
    }

    #<?= $componentId ?> .hero-primary-copy strong {
        font-size: 0.94rem;
    }

    #<?= $componentId ?> .hero-stats {
        grid-template-columns: 1fr 1fr;
        gap: 0.75rem;
    }

    #<?= $componentId ?> .hero-stat {
        padding: 0.9rem;
        border-radius: 20px;
    }

    #<?= $componentId ?> .hero-falling-card {
        width: min(var(--card-size), 84px);
    }

    #<?= $componentId ?> .hero-light-trail {
        width: 74vw;
    }

    #<?= $componentId ?> .hero-stage-art {
        width: min(320px, 72vw);
        bottom: 72px;
    }
}

@media (max-width: 540px) {
    #<?= $componentId ?> .hero-stats {
        grid-template-columns: 1fr;
    }

    #<?= $componentId ?> .hero-copy {
        max-width: 100%;
    }

    #<?= $componentId ?> .hero-badge {
        font-size: 0.7rem;
        letter-spacing: 0.06em;
    }

    #<?= $componentId ?> .hero-light-trail {
        width: 88vw;
        height: 2px;
    }

    #<?= $componentId ?> .hero-falling-card:nth-child(4n) {
        display: none;
    }
}

@media (prefers-reduced-motion: reduce) {
    #<?= $componentId ?> .hero-reveal,
    #<?= $componentId ?> .hero-card-glow,
    #<?= $componentId ?> .hero-light-trail,
    #<?= $componentId ?> .hero-falling-card {
        animation: none !important;
    }

    #<?= $componentId ?> h1::before,
    #<?= $componentId ?> h1::after {
        animation: none !important;
    }

    #<?= $componentId ?> .hero-reveal {
        opacity: 1 !important;
        transform: none !important;
    }

    #<?= $componentId ?> .hero-falling-card {
        opacity: 0.12 !important;
        transform: none !important;
    }
}
</style>
