<?php
/**
 * LudoEmpire - Blog Category Component
 * 
 * 博客分类列表页
 * 
 * @var \Weline\Framework\View\Template $this
 * @var array $colors 颜色配置
 * @var array $component_config 组件配置
 * 
 * @fields_start
 * 
 * group:content => 内容设置
 * content.badge => 徽章文字:text:Categories
 * content.title => 标题:text:Browse by Category
 * content.description => 描述:textarea:Explore our blog posts organized by topic.
 * 
 * @fields_end
 */

$config = $component_config ?? [];

if (!isset($colors) || !is_array($colors)) {
    $colors = [];
}

$sectionBg = $colors['section_bg_1'] ?? '#0d0020';
$textPrimary = $colors['text_primary'] ?? '#f5f3ff';
$textSecondary = $colors['text_secondary'] ?? '#c4b5fd';
$accentPrimaryLight = $colors['accent_primary_light'] ?? '#a78bfa';
$accentSecondary = $colors['accent_secondary'] ?? '#ec4899';
$cardBg = $colors['card_bg'] ?? 'rgba(255, 255, 255, 0.04)';
$borderLight = $colors['border_light'] ?? 'rgba(139, 92, 246, 0.12)';

$badge = $config['content.badge'] ?? 'Categories';
$title = $config['content.title'] ?? 'Browse by Category';
$description = $config['content.description'] ?? 'Explore our blog posts organized by topic.';

$categories = [
    ['name' => 'Strategies', 'icon' => '🧠', 'count' => 12, 'desc' => 'Master the art of Ludo with proven winning strategies.', 'color' => '#8b5cf6'],
    ['name' => 'Tournaments', 'icon' => '🏆', 'count' => 8, 'desc' => 'Tournament news, results, and upcoming events.', 'color' => '#ec4899'],
    ['name' => 'Updates', 'icon' => '🚀', 'count' => 15, 'desc' => 'Latest app updates, new features, and improvements.', 'color' => '#06b6d4'],
    ['name' => 'Tips & Tricks', 'icon' => '💡', 'count' => 10, 'desc' => 'Quick tips to improve your gameplay instantly.', 'color' => '#f59e0b'],
    ['name' => 'Community', 'icon' => '👥', 'count' => 6, 'desc' => 'Stories and highlights from our amazing player community.', 'color' => '#22c55e'],
    ['name' => 'News', 'icon' => '📰', 'count' => 9, 'desc' => 'Industry news and Ludo gaming world updates.', 'color' => '#ef4444'],
];

$instanceId = 'le-blog-cat-' . uniqid();
?>

<section id="<?= $instanceId ?>">
    <style>
        #<?= $instanceId ?> {
            background: <?= $sectionBg ?>;
            padding: 100px 0;
        }
        
        #<?= $instanceId ?> .le-bc-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        #<?= $instanceId ?> .le-bc-header {
            text-align: center;
            margin-bottom: 60px;
        }
        
        #<?= $instanceId ?> .le-bc-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 18px;
            background: rgba(139, 92, 246, 0.15);
            border: 1px solid rgba(139, 92, 246, 0.3);
            border-radius: 50px;
            font-size: 0.875rem;
            font-weight: 700;
            color: <?= $accentPrimaryLight ?>;
            margin-bottom: 20px;
        }
        
        #<?= $instanceId ?> .le-bc-title {
            font-size: 2.5rem;
            font-weight: 800;
            color: <?= $textPrimary ?>;
            margin-bottom: 16px;
        }
        
        #<?= $instanceId ?> .le-bc-subtitle {
            font-size: 1.125rem;
            color: <?= $textSecondary ?>;
        }
        
        #<?= $instanceId ?> .le-bc-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 24px;
        }
        
        #<?= $instanceId ?> .le-bc-card {
            background: <?= $cardBg ?>;
            border: 1px solid <?= $borderLight ?>;
            border-radius: 20px;
            padding: 32px;
            transition: all 0.3s ease;
            text-decoration: none;
            display: block;
        }
        
        #<?= $instanceId ?> .le-bc-card:hover {
            transform: translateY(-6px);
            box-shadow: 0 12px 40px rgba(139, 92, 246, 0.15);
        }
        
        #<?= $instanceId ?> .le-bc-card-icon {
            font-size: 2.5rem;
            margin-bottom: 16px;
        }
        
        #<?= $instanceId ?> .le-bc-card-name {
            font-size: 1.2rem;
            font-weight: 700;
            color: <?= $textPrimary ?>;
            margin-bottom: 8px;
        }
        
        #<?= $instanceId ?> .le-bc-card-count {
            font-size: 0.85rem;
            font-weight: 600;
            color: <?= $accentSecondary ?>;
            margin-bottom: 12px;
        }
        
        #<?= $instanceId ?> .le-bc-card-desc {
            font-size: 0.9rem;
            color: <?= $textSecondary ?>;
            line-height: 1.6;
        }
        
        @media (max-width: 992px) {
            #<?= $instanceId ?> .le-bc-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }
        
        @media (max-width: 576px) {
            #<?= $instanceId ?> .le-bc-grid {
                grid-template-columns: 1fr;
            }
            
            #<?= $instanceId ?> .le-bc-title {
                font-size: 1.75rem;
            }
        }
    </style>
    
    <div class="le-bc-container">
        <div class="le-bc-header">
            <div class="le-bc-badge"><?= htmlspecialchars($badge) ?></div>
            <h2 class="le-bc-title"><?= htmlspecialchars($title) ?></h2>
            <p class="le-bc-subtitle"><?= htmlspecialchars($description) ?></p>
        </div>
        
        <div class="le-bc-grid">
            <?php foreach ($categories as $cat): ?>
            <a href="#" class="le-bc-card" style="border-top: 3px solid <?= htmlspecialchars($cat['color']) ?>;">
                <div class="le-bc-card-icon"><?= htmlspecialchars($cat['icon']) ?></div>
                <h3 class="le-bc-card-name"><?= htmlspecialchars($cat['name']) ?></h3>
                <div class="le-bc-card-count"><?= htmlspecialchars($cat['count']) ?> articles</div>
                <p class="le-bc-card-desc"><?= htmlspecialchars($cat['desc']) ?></p>
            </a>
            <?php endforeach; ?>
        </div>
    </div>
</section>
