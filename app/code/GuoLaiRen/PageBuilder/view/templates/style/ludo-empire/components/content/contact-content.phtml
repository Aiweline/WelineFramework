<?php
/**
 * LudoEmpire - Contact Content Component
 * 
 * 联系页面，包含联系表单和联系信息
 * 
 * @var \Weline\Framework\View\Template $this
 * @var array $colors 颜色配置
 * @var array $component_config 组件配置
 * 
 * @fields_start
 * 
 * group:content => 内容设置
 * content.badge => 徽章文字:text:Contact Us
 * content.title => 标题:text:Get in Touch
 * content.description => 描述:textarea:Have questions or feedback? We'd love to hear from you!
 * 
 * group:contact => 联系信息
 * contact.email => 邮箱:text:support@ludoempire.com
 * contact.phone => 电话:text:+91 98765 43210
 * contact.address => 地址:text:Mumbai, India
 * contact.hours => 工作时间:text:24/7 Support Available
 * 
 * @fields_end
 */

$config = $component_config ?? [];

if (!isset($colors) || !is_array($colors)) {
    $colors = [];
}

$sectionBg = $colors['section_bg_1'] ?? '#0d0020';
$textPrimary = $colors['text_primary'] ?? '#f5f3ff';
$textSecondary = $colors['text_secondary'] ?? '#c4b5fd';
$textMuted = $colors['text_muted'] ?? '#7c6fad';
$accentPrimary = $colors['accent_primary'] ?? '#8b5cf6';
$accentPrimaryLight = $colors['accent_primary_light'] ?? '#a78bfa';
$accentSecondary = $colors['accent_secondary'] ?? '#ec4899';
$buttonPrimaryBg = $colors['button_primary_bg'] ?? 'linear-gradient(135deg, #8b5cf6 0%, #ec4899 100%)';
$cardBg = $colors['card_bg'] ?? 'rgba(255, 255, 255, 0.04)';
$borderLight = $colors['border_light'] ?? 'rgba(139, 92, 246, 0.12)';
$inputBg = $colors['input_bg'] ?? 'rgba(139, 92, 246, 0.08)';
$inputBorder = $colors['input_border'] ?? 'rgba(139, 92, 246, 0.2)';
$inputBorderFocus = $colors['input_border_focus'] ?? '#8b5cf6';

$badge = $config['content.badge'] ?? 'Contact Us';
$title = $config['content.title'] ?? 'Get in Touch';
$description = $config['content.description'] ?? "Have questions or feedback? We'd love to hear from you!";
$email = $config['contact.email'] ?? 'support@ludoempire.com';
$phone = $config['contact.phone'] ?? '+91 98765 43210';
$address = $config['contact.address'] ?? 'Mumbai, India';
$hours = $config['contact.hours'] ?? '24/7 Support Available';

$instanceId = 'le-contact-' . uniqid();
?>

<section id="<?= $instanceId ?>">
    <style>
        #<?= $instanceId ?> {
            background: <?= $sectionBg ?>;
            padding: 100px 0;
        }
        
        #<?= $instanceId ?> .le-ct-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        #<?= $instanceId ?> .le-ct-header {
            text-align: center;
            margin-bottom: 60px;
        }
        
        #<?= $instanceId ?> .le-ct-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 18px;
            background: rgba(139, 92, 246, 0.15);
            border: 1px solid rgba(139, 92, 246, 0.3);
            border-radius: 50px;
            font-size: 0.875rem;
            font-weight: 700;
            color: <?= $accentPrimaryLight ?>;
            margin-bottom: 20px;
        }
        
        #<?= $instanceId ?> .le-ct-title {
            font-size: 2.5rem;
            font-weight: 800;
            color: <?= $textPrimary ?>;
            margin-bottom: 16px;
        }
        
        #<?= $instanceId ?> .le-ct-subtitle {
            font-size: 1.125rem;
            color: <?= $textSecondary ?>;
        }
        
        #<?= $instanceId ?> .le-ct-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 48px;
        }
        
        #<?= $instanceId ?> .le-ct-form {
            background: <?= $cardBg ?>;
            border: 1px solid <?= $borderLight ?>;
            border-radius: 20px;
            padding: 36px;
        }
        
        #<?= $instanceId ?> .le-ct-form-title {
            font-size: 1.35rem;
            font-weight: 700;
            color: <?= $textPrimary ?>;
            margin-bottom: 24px;
        }
        
        #<?= $instanceId ?> .le-ct-field {
            margin-bottom: 20px;
        }
        
        #<?= $instanceId ?> .le-ct-label {
            display: block;
            font-size: 0.9rem;
            font-weight: 600;
            color: <?= $textSecondary ?>;
            margin-bottom: 8px;
        }
        
        #<?= $instanceId ?> .le-ct-input,
        #<?= $instanceId ?> .le-ct-textarea {
            width: 100%;
            padding: 12px 16px;
            background: <?= $inputBg ?>;
            border: 1px solid <?= $inputBorder ?>;
            border-radius: 10px;
            color: <?= $textPrimary ?>;
            font-size: 0.95rem;
            font-family: inherit;
            transition: border-color 0.3s ease;
        }
        
        #<?= $instanceId ?> .le-ct-input:focus,
        #<?= $instanceId ?> .le-ct-textarea:focus {
            outline: none;
            border-color: <?= $inputBorderFocus ?>;
            box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.15);
        }
        
        #<?= $instanceId ?> .le-ct-textarea {
            min-height: 120px;
            resize: vertical;
        }
        
        #<?= $instanceId ?> .le-ct-submit {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 14px 32px;
            background: <?= $buttonPrimaryBg ?>;
            color: #ffffff;
            font-size: 1rem;
            font-weight: 700;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            justify-content: center;
        }
        
        #<?= $instanceId ?> .le-ct-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 24px rgba(139, 92, 246, 0.4);
        }
        
        #<?= $instanceId ?> .le-ct-info-cards {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }
        
        #<?= $instanceId ?> .le-ct-info-card {
            background: <?= $cardBg ?>;
            border: 1px solid <?= $borderLight ?>;
            border-radius: 16px;
            padding: 24px;
            display: flex;
            gap: 16px;
            transition: all 0.3s ease;
        }
        
        #<?= $instanceId ?> .le-ct-info-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 30px rgba(139, 92, 246, 0.12);
        }
        
        #<?= $instanceId ?> .le-ct-info-icon {
            width: 52px;
            height: 52px;
            background: rgba(139, 92, 246, 0.12);
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            flex-shrink: 0;
        }
        
        #<?= $instanceId ?> .le-ct-info-label {
            font-size: 0.85rem;
            color: <?= $textMuted ?>;
            margin-bottom: 4px;
        }
        
        #<?= $instanceId ?> .le-ct-info-value {
            font-size: 1rem;
            font-weight: 600;
            color: <?= $textPrimary ?>;
        }
        
        #<?= $instanceId ?> .le-ct-info-value a {
            color: <?= $accentPrimaryLight ?>;
            text-decoration: none;
        }
        
        #<?= $instanceId ?> .le-ct-info-value a:hover {
            color: <?= $accentSecondary ?>;
        }
        
        @media (max-width: 992px) {
            #<?= $instanceId ?> .le-ct-grid {
                grid-template-columns: 1fr;
            }
        }
        
        @media (max-width: 576px) {
            #<?= $instanceId ?> .le-ct-title {
                font-size: 1.75rem;
            }
            
            #<?= $instanceId ?> .le-ct-form {
                padding: 24px;
            }
        }
    </style>
    
    <div class="le-ct-container">
        <div class="le-ct-header">
            <div class="le-ct-badge"><?= htmlspecialchars($badge) ?></div>
            <h1 class="le-ct-title"><?= htmlspecialchars($title) ?></h1>
            <p class="le-ct-subtitle"><?= htmlspecialchars($description) ?></p>
        </div>
        
        <div class="le-ct-grid">
            <div class="le-ct-form">
                <h2 class="le-ct-form-title">Send us a Message</h2>
                <form>
                    <div class="le-ct-field">
                        <label class="le-ct-label">Your Name</label>
                        <input type="text" class="le-ct-input" placeholder="Enter your name">
                    </div>
                    <div class="le-ct-field">
                        <label class="le-ct-label">Email Address</label>
                        <input type="email" class="le-ct-input" placeholder="your@email.com">
                    </div>
                    <div class="le-ct-field">
                        <label class="le-ct-label">Subject</label>
                        <input type="text" class="le-ct-input" placeholder="How can we help?">
                    </div>
                    <div class="le-ct-field">
                        <label class="le-ct-label">Message</label>
                        <textarea class="le-ct-textarea" placeholder="Tell us more..."></textarea>
                    </div>
                    <button type="submit" class="le-ct-submit">Send Message</button>
                </form>
            </div>
            
            <div class="le-ct-info-cards">
                <div class="le-ct-info-card">
                    <div class="le-ct-info-icon">📧</div>
                    <div>
                        <div class="le-ct-info-label">Email</div>
                        <div class="le-ct-info-value"><a href="mailto:<?= htmlspecialchars($email) ?>"><?= htmlspecialchars($email) ?></a></div>
                    </div>
                </div>
                <div class="le-ct-info-card">
                    <div class="le-ct-info-icon">📞</div>
                    <div>
                        <div class="le-ct-info-label">Phone</div>
                        <div class="le-ct-info-value"><?= htmlspecialchars($phone) ?></div>
                    </div>
                </div>
                <div class="le-ct-info-card">
                    <div class="le-ct-info-icon">📍</div>
                    <div>
                        <div class="le-ct-info-label">Address</div>
                        <div class="le-ct-info-value"><?= htmlspecialchars($address) ?></div>
                    </div>
                </div>
                <div class="le-ct-info-card">
                    <div class="le-ct-info-icon">🕐</div>
                    <div>
                        <div class="le-ct-info-label">Support Hours</div>
                        <div class="le-ct-info-value"><?= htmlspecialchars($hours) ?></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
