<?php
/**
 * PokerArena 模板 - Content
 * 
 * 内容区域渲染文件，通过组件系统加载内容组件
 * 
 * @var \Weline\Framework\View\Template $this
 * 
 * @fields_start
 * 
 * group:layout => 布局设置
 * layout.max_width => 最大宽度:number:1200|px
 * 
 * group:style => 样式设置
 * style.background_color => 背景颜色:color:#060d08
 * style.text_color => 文字颜色:color:#86efac
 * style.heading_color => 标题颜色:color:#ecfdf5
 * style.link_color => 链接颜色:color:#10b981
 * 
 * @fields_end
 */

$page = $this->getData('page');
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];

// 加载颜色配置
$templateCode = $page ? $page->getData('style') : 'poker-arena';
if (empty($templateCode)) {
    $templateCode = 'poker-arena';
}
$this->assign('template_code', $templateCode);
?>
<div style="display:none;">
    <w:template>GuoLaiRen_PageBuilder::base/colors.phtml</w:template>
</div>
<?php
if (!isset($colors) || !is_array($colors)) {
    $colors = [];
}

if (!function_exists('pa_content_color')) {
    function pa_content_color($colors, $key, $default = '') {
        return $colors[$key] ?? $default;
    }
}

if (!function_exists('pa_content_getConfig')) {
    function pa_content_getConfig($settings, $key, $default = '') {
        return $settings[$key] ?? $default;
    }
}

$maxWidth = pa_content_getConfig($styleSettings, 'layout.max_width', '1200');
$bgColor = pa_content_getConfig($styleSettings, 'style.background_color', pa_content_color($colors, 'body_bg', '#060d08'));
$textColor = pa_content_getConfig($styleSettings, 'style.text_color', pa_content_color($colors, 'text_secondary', '#86efac'));
$headingColor = pa_content_getConfig($styleSettings, 'style.heading_color', pa_content_color($colors, 'text_primary', '#ecfdf5'));
$linkColor = pa_content_getConfig($styleSettings, 'style.link_color', pa_content_color($colors, 'accent_primary_light', '#10b981'));

$layoutConfig = $this->getData('layout_config') ?? [];
$contentComponents = $layoutConfig['content'] ?? [];
?>

<style>
    .pa-content {
        background: <?= htmlspecialchars($bgColor) ?>;
        color: <?= htmlspecialchars($textColor) ?>;
        min-height: 50vh;
        padding-top: 80px;
    }
    
    .pa-content-container {
        max-width: <?= intval($maxWidth) ?>px;
        margin: 0 auto;
        padding: 0 20px;
    }
    
    .pa-content h1, .pa-content h2, .pa-content h3, .pa-content h4 {
        color: <?= htmlspecialchars($headingColor) ?>;
    }
    
    .pa-content a {
        color: <?= htmlspecialchars($linkColor) ?>;
        text-decoration: none;
        transition: color 0.3s ease;
    }
    
    .pa-content a:hover {
        color: #fbbf24;
    }
</style>

<main class="pa-content">
    <?php
    $componentFiles = [
        'hero-slider' => 'content/hero-slider.phtml',
        'games' => 'content/games.phtml',
        'advantages' => 'content/advantages.phtml',
        'app-download' => 'content/app-download.phtml',
        'testimonials' => 'content/testimonials.phtml',
        'faq' => 'content/faq.phtml',
        'cta-banner' => 'content/cta-banner.phtml',
        'blog-list' => 'content/blog-list.phtml',
        'blog-detail' => 'content/blog-detail.phtml',
        'blog-category' => 'content/blog-category.phtml',
        'legal-content' => 'content/legal-content.phtml',
        'about-content' => 'content/about-content.phtml',
        'contact-content' => 'content/contact-content.phtml',
    ];
    
    if (!empty($contentComponents)) {
        foreach ($contentComponents as $index => $component) {
            $componentCode = $component['code'] ?? $component['component'] ?? '';
            $componentConfig = $component['config'] ?? [];
            
            if (empty($componentCode)) continue;
            
            $componentFile = $componentFiles[$componentCode] ?? null;
            if ($componentFile) {
                $this->assign('component_config', $componentConfig);
                $this->assign('component_instance_id', 'pa-component-' . $componentCode . '-' . $index);
                $this->assign('colors', $colors);
                ?>
                <div class="pa-component-wrapper tpmst-component-wrapper" data-component="<?= htmlspecialchars($componentCode) ?>" data-index="<?= $index ?>">
                    <?= $this->fetchTemplate("GuoLaiRen_PageBuilder::templates/style/poker-arena/components/{$componentFile}") ?>
                </div>
                <?php
            }
        }
    } else {
        if ($page) {
            $pageContent = $page->getData('content') ?? '';
            if ($pageContent) {
                echo '<div class="pa-content-container">' . $pageContent . '</div>';
            }
        }
    }
    ?>
</main>
