<?php
/**
 * SaaS Starter - CTA Banner Component
 * 
 * @var \Weline\Framework\View\Template $this
 * @var array $colors
 * @var array $component_config
 * 
 * @fields_start
 * group:content => 内容设置
 * content.title => 标题:text:Ready to supercharge your workflow?
 * content.description => 描述:textarea:Join 10,000+ teams already using our platform. Start your free 14-day trial today.
 * group:buttons => 按钮设置
 * buttons.primary_text => 主按钮文字:text:Start Free Trial
 * buttons.primary_url => 主按钮链接:text:#pricing
 * buttons.secondary_text => 次按钮文字:text:Talk to Sales
 * buttons.secondary_url => 次按钮链接:text:/contact
 * buttons.show_secondary => 显示次按钮:select:yes|yes,no
 * @fields_end
 */

$config = $component_config ?? [];
if (!isset($colors) || !is_array($colors)) { $colors = []; }

$ctaBg = $colors['cta_bg'] ?? 'linear-gradient(135deg, #0b1a2e 0%, #0d1321 100%)';
$textPrimary = $colors['text_primary'] ?? '#f1f5f9';
$textSecondary = $colors['text_secondary'] ?? '#94a3b8';
$accentPrimary = $colors['accent_primary'] ?? '#10b981';
$accentPrimaryLight = $colors['accent_primary_light'] ?? '#34d399';
$buttonPrimaryBg = $colors['button_primary_bg'] ?? 'linear-gradient(135deg, #10b981 0%, #06b6d4 100%)';
$borderLight = $colors['border_light'] ?? 'rgba(255, 255, 255, 0.08)';

$title = $config['content.title'] ?? 'Ready to supercharge your workflow?';
$description = $config['content.description'] ?? 'Join 10,000+ teams already using our platform. Start your free 14-day trial today.';
$primaryText = $config['buttons.primary_text'] ?? 'Start Free Trial';
$primaryUrl = $config['buttons.primary_url'] ?? '#pricing';
$secondaryText = $config['buttons.secondary_text'] ?? 'Talk to Sales';
$secondaryUrl = $config['buttons.secondary_url'] ?? '/contact';
$showSecondary = ($config['buttons.show_secondary'] ?? 'yes') !== 'no';

$instanceId = 'ss-cta-' . uniqid();
?>

<section id="<?= $instanceId ?>">
    <style>
        #<?= $instanceId ?> { background: <?= $ctaBg ?>; padding: 100px 0; position: relative; overflow: hidden; }
        #<?= $instanceId ?>::before { content: ''; position: absolute; top: -50%; left: -20%; width: 140%; height: 200%; background: radial-gradient(ellipse at center, rgba(16,185,129,0.08) 0%, transparent 60%); pointer-events: none; }
        #<?= $instanceId ?> .ss-cta-container { max-width: 800px; margin: 0 auto; padding: 0 20px; text-align: center; position: relative; z-index: 1; }
        #<?= $instanceId ?> .ss-cta-card { background: rgba(255,255,255,0.03); border: 1px solid <?= $borderLight ?>; border-radius: 24px; padding: 64px 48px; }
        #<?= $instanceId ?> .ss-cta-title { font-size: 2.5rem; font-weight: 700; color: <?= $textPrimary ?>; margin-bottom: 16px; line-height: 1.2; }
        #<?= $instanceId ?> .ss-cta-desc { font-size: 1.125rem; color: <?= $textSecondary ?>; margin-bottom: 40px; line-height: 1.7; }
        #<?= $instanceId ?> .ss-cta-buttons { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
        #<?= $instanceId ?> .ss-cta-btn-primary { display: inline-flex; align-items: center; gap: 10px; padding: 16px 36px; background: <?= $buttonPrimaryBg ?>; color: #fff; font-size: 1.05rem; font-weight: 600; border-radius: 12px; transition: all 0.3s ease; box-shadow: 0 4px 20px rgba(16,185,129,0.4); text-decoration: none; }
        #<?= $instanceId ?> .ss-cta-btn-primary:hover { transform: translateY(-3px); box-shadow: 0 8px 30px rgba(16,185,129,0.5); }
        #<?= $instanceId ?> .ss-cta-btn-secondary { display: inline-flex; align-items: center; gap: 10px; padding: 16px 36px; background: transparent; color: <?= $accentPrimaryLight ?>; font-size: 1.05rem; font-weight: 600; border: 2px solid rgba(16,185,129,0.4); border-radius: 12px; transition: all 0.3s ease; text-decoration: none; }
        #<?= $instanceId ?> .ss-cta-btn-secondary:hover { background: rgba(16,185,129,0.1); border-color: <?= $accentPrimaryLight ?>; }
        @media (max-width: 768px) {
            #<?= $instanceId ?> .ss-cta-card { padding: 40px 24px; }
            #<?= $instanceId ?> .ss-cta-title { font-size: 1.75rem; }
            #<?= $instanceId ?> .ss-cta-buttons { flex-direction: column; align-items: center; }
            #<?= $instanceId ?> .ss-cta-btn-primary, #<?= $instanceId ?> .ss-cta-btn-secondary { width: 100%; max-width: 320px; justify-content: center; }
        }
    </style>
    
    <div class="ss-cta-container">
        <div class="ss-cta-card">
            <h2 class="ss-cta-title"><?= htmlspecialchars($title) ?></h2>
            <p class="ss-cta-desc"><?= htmlspecialchars($description) ?></p>
            <div class="ss-cta-buttons">
                <a href="<?= htmlspecialchars($primaryUrl) ?>" class="ss-cta-btn-primary">
                    <?= htmlspecialchars($primaryText) ?>
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                </a>
                <?php if ($showSecondary): ?>
                <a href="<?= htmlspecialchars($secondaryUrl) ?>" class="ss-cta-btn-secondary">
                    <?= htmlspecialchars($secondaryText) ?>
                </a>
                <?php endif; ?>
            </div>
        </div>
    </div>
</section>
