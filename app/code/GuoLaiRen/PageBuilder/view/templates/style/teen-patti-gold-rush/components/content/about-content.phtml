<?php
/**
 * Teen Patti Gold Rush - About Content Component
 *
 * @component_start
 * code: about-content
 * name: 关于我们内容
 * name_en: About Content
 * description: 企业/品牌关于页面内容展示组件，继承主题配色
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_stats => 显示数据统计:select:yes|yes,no
 * display.show_values => 显示价值主张:select:yes|yes,no
 * display.show_team => 显示团队信息:select:yes|yes,no
 * display.show_cta => 显示行动按钮区:select:yes|yes,no
 *
 * group:hero => Hero设置
 * hero.title => 页面标题:text:About Teen Patti Gold Rush
 * hero.subtitle => 页面副标题:textarea:Built for players who want quick rounds, clear controls, and reliable card-table flow.
 * hero.bg_color => 背景颜色:color:#150b1d
 * hero.bg_gradient_end => 渐变结束色:color:#4d1a5f
 * hero.title_color => 标题颜色:color:#ffe5f3
 * hero.text_color => 文字颜色:color:#f4deef
 * hero.padding => 内边距:number:80|px
 *
 * group:stats => 数据统计
 * stats.stat_1_number => 数据1数值:text:30M+
 * stats.stat_1_label => 数据1说明:text:Hands Played Monthly
 * stats.stat_2_number => 数据2数值:text:4.8/5
 * stats.stat_2_label => 数据2说明:text:Player Rating
 * stats.stat_3_number => 数据3数值:text:24/7
 * stats.stat_3_label => 数据3说明:text:Table Availability
 * stats.stat_4_number => 数据4数值:text:1.2s
 * stats.stat_4_label => 数据4说明:text:Average Match Join
 * stats.number_color => 数值颜色:color:#ffe5f3
 * stats.label_color => 说明颜色:color:#be9fc9
 * stats.card_bg => 卡片背景色:color:rgba(255, 244, 250, 0.09)
 *
 * group:story => 品牌故事
 * story.title => 模块标题:text:How Gold Rush Was Built
 * story.subtitle => 模块副标题:text:From static lobbies to a faster match rhythm
 * story.content => 模块内容:textarea:Teen Patti Gold Rush started with one goal: reduce waiting and keep every table interaction clear.
 * story.bg_color => 背景颜色:color:#160d22
 * story.title_color => 标题颜色:color:#a9ff5e
 *
 * group:values => 价值主张
 * values.title => 模块标题:text:What We Optimize Every Release
 * values.subtitle => 模块副标题:text:Three priorities shape each update
 * values.value_1_icon => 价值1图标:text:A
 * values.value_1_title => 价值1标题:text:Access Speed
 * values.value_1_desc => 价值1描述:textarea:Join rounds quickly with lightweight flows and clear route entry points.
 * values.value_2_icon => 价值2图标:text:F
 * values.value_2_title => 价值2标题:text:Fair Sessions
 * values.value_2_desc => 价值2描述:textarea:Transparent table logic and trackable wallet history keep trust high.
 * values.value_3_icon => 价值3图标:text:M
 * values.value_3_title => 价值3标题:text:Mobile First
 * values.value_3_desc => 价值3描述:textarea:Layouts, controls, and interactions are tuned for one-hand play.
 * values.bg_color => 背景颜色:color:#1b2b18
 *
 * group:team => 团队信息
 * team.title => 模块标题:text:Team Behind The Tables
 * team.subtitle => 模块副标题:text:Operators, designers, and game-flow engineers
 * team.member_1_icon => 成员1图标:text:R
 * team.member_1_name => 成员1姓名:text:Rohan Singh
 * team.member_1_role => 成员1角色:text:Product Lead
 * team.member_2_icon => 成员2图标:text:P
 * team.member_2_name => 成员2姓名:text:Priya Mehta
 * team.member_2_role => 成员2角色:text:Gameplay Ops
 * team.member_3_icon => 成员3图标:text:K
 * team.member_3_name => 成员3姓名:text:Karan Patel
 * team.member_3_role => 成员3角色:text:Reward Systems
 * team.member_4_icon => 成员4图标:text:S
 * team.member_4_name => 成员4姓名:text:Sneha Rao
 * team.member_4_role => 成员4角色:text:Player Success
 *
 * group:cta => 行动号召
 * cta.title => 区块标题:text:Ready To Join A Faster Table?
 * cta.description => 区块描述:textarea:Download the latest Teen Patti Gold Rush build and jump into live match routes.
 * cta.button_text => 按钮文字:text:Download Now
 * cta.button_url => 按钮链接:text:#download
 * cta.button_bg => 按钮背景色:color:linear-gradient(106deg, #ffb1dd 0%, #ff5ab6 52%, #8d6dff 100%)
 * cta.button_text_color => 按钮文字颜色:color:#210c1d
 *
 * @fields_end
 */

$config = $component_config ?? $this->getData('component_config') ?? [];

$showStats = ($config['display.show_stats'] ?? 'yes') !== 'no';
$showValues = ($config['display.show_values'] ?? 'yes') !== 'no';
$showTeam = ($config['display.show_team'] ?? 'yes') !== 'no';
$showCta = ($config['display.show_cta'] ?? 'yes') !== 'no';

$heroTitle = $config['hero.title'] ?? '';
$heroSubtitle = $config['hero.subtitle'] ?? '';
$heroBgColorConfig = $config['hero.bg_color'] ?? '';
$heroGradientEndConfig = $config['hero.bg_gradient_end'] ?? '';
$heroTitleColorConfig = $config['hero.title_color'] ?? '';
$heroTextColorConfig = $config['hero.text_color'] ?? '';
$heroPadding = (int)($config['hero.padding'] ?? '80');

$stat1Number = $config['stats.stat_1_number'] ?? '30M+';
$stat1Label = $config['stats.stat_1_label'] ?? 'Hands Played Monthly';
$stat2Number = $config['stats.stat_2_number'] ?? '4.8/5';
$stat2Label = $config['stats.stat_2_label'] ?? 'Player Rating';
$stat3Number = $config['stats.stat_3_number'] ?? '24/7';
$stat3Label = $config['stats.stat_3_label'] ?? 'Table Availability';
$stat4Number = $config['stats.stat_4_number'] ?? '1.2s';
$stat4Label = $config['stats.stat_4_label'] ?? 'Average Match Join';
$statsNumberColorConfig = $config['stats.number_color'] ?? '';
$statsLabelColorConfig = $config['stats.label_color'] ?? '';
$statsCardBgConfig = $config['stats.card_bg'] ?? '';

$storyTitle = $config['story.title'] ?? 'How Gold Rush Was Built';
$storySubtitle = $config['story.subtitle'] ?? 'From static lobbies to a faster match rhythm';
$storyContent = $config['story.content'] ?? '';
$storyBgColorConfig = $config['story.bg_color'] ?? '';
$storyTitleColorConfig = $config['story.title_color'] ?? '';

$valuesTitle = $config['values.title'] ?? 'What We Optimize Every Release';
$valuesSubtitle = $config['values.subtitle'] ?? 'Three priorities shape each update';
$value1Icon = $config['values.value_1_icon'] ?? 'A';
$value1Title = $config['values.value_1_title'] ?? 'Access Speed';
$value1Desc = $config['values.value_1_desc'] ?? 'Join rounds quickly with lightweight flows and clear route entry points.';
$value2Icon = $config['values.value_2_icon'] ?? 'F';
$value2Title = $config['values.value_2_title'] ?? 'Fair Sessions';
$value2Desc = $config['values.value_2_desc'] ?? 'Transparent table logic and trackable wallet history keep trust high.';
$value3Icon = $config['values.value_3_icon'] ?? 'M';
$value3Title = $config['values.value_3_title'] ?? 'Mobile First';
$value3Desc = $config['values.value_3_desc'] ?? 'Layouts, controls, and interactions are tuned for one-hand play.';
$valuesBgColorConfig = $config['values.bg_color'] ?? '';

$teamTitle = $config['team.title'] ?? 'Team Behind The Tables';
$teamSubtitle = $config['team.subtitle'] ?? 'Operators, designers, and game-flow engineers';
$member1Icon = $config['team.member_1_icon'] ?? 'R';
$member1Name = $config['team.member_1_name'] ?? 'Rohan Singh';
$member1Role = $config['team.member_1_role'] ?? 'Product Lead';
$member2Icon = $config['team.member_2_icon'] ?? 'P';
$member2Name = $config['team.member_2_name'] ?? 'Priya Mehta';
$member2Role = $config['team.member_2_role'] ?? 'Gameplay Ops';
$member3Icon = $config['team.member_3_icon'] ?? 'K';
$member3Name = $config['team.member_3_name'] ?? 'Karan Patel';
$member3Role = $config['team.member_3_role'] ?? 'Reward Systems';
$member4Icon = $config['team.member_4_icon'] ?? 'S';
$member4Name = $config['team.member_4_name'] ?? 'Sneha Rao';
$member4Role = $config['team.member_4_role'] ?? 'Player Success';

$ctaTitle = $config['cta.title'] ?? 'Ready To Join A Faster Table?';
$ctaDescription = $config['cta.description'] ?? 'Download the latest Teen Patti Gold Rush build and jump into live match routes.';
$ctaButtonText = $config['cta.button_text'] ?? 'Download Now';
$ctaButtonUrl = $config['cta.button_url'] ?? '#download';
$ctaButtonBgConfig = $config['cta.button_bg'] ?? '';
$ctaButtonTextColorConfig = $config['cta.button_text_color'] ?? '';

$page = $this->getData('page');
$pageTitle = $heroTitle ?: ($page ? ($page->getData('title') ?? 'About Teen Patti Gold Rush') : 'About Teen Patti Gold Rush');
$pageContent = $page ? ($page->getData('content') ?? '') : '';

$storyRaw = trim((string)($storyContent !== '' ? $storyContent : strip_tags((string)$pageContent)));
if ($storyRaw === '') {
    $storyRaw = 'Teen Patti Gold Rush started with one goal: reduce waiting and keep every table interaction clear.';
}
$storyParagraphs = array_values(array_filter(array_map('trim', preg_split('/\r?\n+/', $storyRaw))));
if (empty($storyParagraphs)) {
    $storyParagraphs = [$storyRaw];
}

$stats = [
    ['number' => $stat1Number, 'label' => $stat1Label],
    ['number' => $stat2Number, 'label' => $stat2Label],
    ['number' => $stat3Number, 'label' => $stat3Label],
    ['number' => $stat4Number, 'label' => $stat4Label],
];

$values = [
    ['icon' => $value1Icon, 'title' => $value1Title, 'desc' => $value1Desc],
    ['icon' => $value2Icon, 'title' => $value2Title, 'desc' => $value2Desc],
    ['icon' => $value3Icon, 'title' => $value3Title, 'desc' => $value3Desc],
];

$teamMembers = [
    ['icon' => $member1Icon, 'name' => $member1Name, 'role' => $member1Role],
    ['icon' => $member2Icon, 'name' => $member2Name, 'role' => $member2Role],
    ['icon' => $member3Icon, 'name' => $member3Name, 'role' => $member3Role],
    ['icon' => $member4Icon, 'name' => $member4Name, 'role' => $member4Role],
];

if (!isset($colors) || !is_array($colors)) {
    $colors = [];
}

$heroBg = $heroBgColorConfig ?: ($colors['hero_bg'] ?? '#150b1d');
$heroGradientEnd = $heroGradientEndConfig ?: ($colors['hero_bg_gradient_end'] ?? '#4d1a5f');
$sectionBg1 = $storyBgColorConfig ?: ($colors['section_bg_1'] ?? '#160d22');
$sectionBg2 = $valuesBgColorConfig ?: ($colors['section_bg_2'] ?? '#1b2b18');
$textPrimary = $colors['text_primary'] ?? '#fff0f8';
$textSecondary = $heroTextColorConfig ?: ($colors['text_secondary'] ?? '#f4deef');
$textMuted = $colors['text_muted'] ?? '#be9fc9';
$accentPrimary = $heroTitleColorConfig ?: ($colors['accent_primary'] ?? '#ff5ab6');
$accentSecondary = $colors['accent_secondary'] ?? '#59d7ff';
$accentAlt = $colors['accent_primary_vibrant'] ?? '#a9ff5e';
$borderLight = $colors['border_light'] ?? 'rgba(255, 206, 236, 0.22)';
$cardBg = $statsCardBgConfig ?: ($colors['card_bg'] ?? 'rgba(255, 244, 250, 0.09)');
$statsNumberColor = $statsNumberColorConfig ?: $accentPrimary;
$statsLabelColor = $statsLabelColorConfig ?: $textMuted;
$storyTitleColor = $storyTitleColorConfig ?: $accentAlt;
$buttonPrimaryBg = $ctaButtonBgConfig ?: ($colors['button_primary_bg'] ?? 'linear-gradient(106deg, #ffb1dd 0%, #ff5ab6 52%, #8d6dff 100%)');
$buttonPrimaryText = $ctaButtonTextColorConfig ?: ($colors['button_primary_text'] ?? '#210c1d');

$instanceId = $component_instance_id ?? 'about-content-' . uniqid();
?>

<section id="<?= $instanceId ?>" class="tpmst-section gdr-about-content">
    <div class="gdr-about-hero">
        <div class="gdr-inner">
            <div class="gdr-hero-layout">
                <div class="gdr-hero-main">
                    <p class="gdr-tag">Teen Patti Gold Rush</p>
                    <h1><?= htmlspecialchars((string)$pageTitle) ?></h1>
                    <?php if ($heroSubtitle !== ''): ?>
                        <p class="gdr-hero-subtitle"><?= htmlspecialchars((string)$heroSubtitle) ?></p>
                    <?php endif; ?>
                </div>
                <aside class="gdr-story-panel">
                    <p class="gdr-story-kicker"><?= htmlspecialchars((string)$storyTitle) ?></p>
                    <?php if ($storySubtitle !== ''): ?>
                        <h2><?= htmlspecialchars((string)$storySubtitle) ?></h2>
                    <?php endif; ?>
                    <?php foreach (array_slice($storyParagraphs, 0, 2) as $paragraph): ?>
                        <p><?= htmlspecialchars((string)$paragraph) ?></p>
                    <?php endforeach; ?>
                </aside>
            </div>
        </div>
    </div>

    <?php if ($showStats): ?>
        <div class="gdr-stat-stage">
            <div class="gdr-inner">
                <div class="gdr-stat-grid">
                    <?php foreach ($stats as $stat): ?>
                        <article class="gdr-stat-card">
                            <strong><?= htmlspecialchars((string)$stat['number']) ?></strong>
                            <span><?= htmlspecialchars((string)$stat['label']) ?></span>
                        </article>
                    <?php endforeach; ?>
                </div>
            </div>
        </div>
    <?php endif; ?>

    <div class="gdr-about-core">
        <div class="gdr-inner">
            <?php if ($showValues): ?>
                <section class="gdr-values-block" aria-label="values">
                    <p class="gdr-tag gdr-tag-soft">Core Priorities</p>
                    <h2><?= htmlspecialchars((string)$valuesTitle) ?></h2>
                    <?php if ($valuesSubtitle !== ''): ?>
                        <p class="gdr-section-note"><?= htmlspecialchars((string)$valuesSubtitle) ?></p>
                    <?php endif; ?>
                    <div class="gdr-values-grid">
                        <?php foreach ($values as $index => $value): ?>
                            <article class="gdr-value-card value-<?= (int)($index + 1) ?>">
                                <span class="gdr-value-icon"><?= htmlspecialchars((string)$value['icon']) ?></span>
                                <h3><?= htmlspecialchars((string)$value['title']) ?></h3>
                                <p><?= htmlspecialchars((string)$value['desc']) ?></p>
                            </article>
                        <?php endforeach; ?>
                    </div>
                </section>
            <?php endif; ?>

            <?php if ($showTeam): ?>
                <section class="gdr-team-block" aria-label="team">
                    <p class="gdr-tag gdr-tag-soft">Team</p>
                    <h2><?= htmlspecialchars((string)$teamTitle) ?></h2>
                    <?php if ($teamSubtitle !== ''): ?>
                        <p class="gdr-section-note"><?= htmlspecialchars((string)$teamSubtitle) ?></p>
                    <?php endif; ?>
                    <div class="gdr-team-grid">
                        <?php foreach ($teamMembers as $index => $member): ?>
                            <article class="gdr-team-card member-<?= (int)($index + 1) ?>">
                                <span class="gdr-team-avatar"><?= htmlspecialchars((string)$member['icon']) ?></span>
                                <div>
                                    <h3><?= htmlspecialchars((string)$member['name']) ?></h3>
                                    <p><?= htmlspecialchars((string)$member['role']) ?></p>
                                </div>
                            </article>
                        <?php endforeach; ?>
                    </div>
                </section>
            <?php endif; ?>
        </div>
    </div>

    <?php if ($showCta): ?>
        <div class="gdr-about-cta">
            <div class="gdr-inner gdr-cta-layout">
                <h2><?= htmlspecialchars((string)$ctaTitle) ?></h2>
                <?php if ($ctaDescription !== ''): ?>
                    <p><?= htmlspecialchars((string)$ctaDescription) ?></p>
                <?php endif; ?>
                <a href="<?= htmlspecialchars((string)$ctaButtonUrl) ?>" class="gdr-cta-btn"><?= htmlspecialchars((string)$ctaButtonText) ?></a>
            </div>
        </div>
    <?php endif; ?>
</section>

<style>
#<?= $instanceId ?> {
    --hero-bg: <?= htmlspecialchars((string)$heroBg) ?>;
    --hero-bg-end: <?= htmlspecialchars((string)$heroGradientEnd) ?>;
    --section-bg-1: <?= htmlspecialchars((string)$sectionBg1) ?>;
    --section-bg-2: <?= htmlspecialchars((string)$sectionBg2) ?>;
    --text-primary: <?= htmlspecialchars((string)$textPrimary) ?>;
    --text-secondary: <?= htmlspecialchars((string)$textSecondary) ?>;
    --text-muted: <?= htmlspecialchars((string)$textMuted) ?>;
    --accent-primary: <?= htmlspecialchars((string)$accentPrimary) ?>;
    --accent-secondary: <?= htmlspecialchars((string)$accentSecondary) ?>;
    --accent-alt: <?= htmlspecialchars((string)$accentAlt) ?>;
    --border-light: <?= htmlspecialchars((string)$borderLight) ?>;
    --card-bg: <?= htmlspecialchars((string)$cardBg) ?>;
    --stats-number: <?= htmlspecialchars((string)$statsNumberColor) ?>;
    --stats-label: <?= htmlspecialchars((string)$statsLabelColor) ?>;
    --story-title: <?= htmlspecialchars((string)$storyTitleColor) ?>;
    --button-bg: <?= htmlspecialchars((string)$buttonPrimaryBg) ?>;
    --button-text: <?= htmlspecialchars((string)$buttonPrimaryText) ?>;
}

#<?= $instanceId ?> .gdr-inner {
    width: min(1180px, calc(100% - 1.2rem));
    margin: 0 auto;
}

#<?= $instanceId ?> .gdr-about-hero {
    padding: <?= $heroPadding ?>px 0 38px;
    background:
        radial-gradient(900px 320px at 4% -6%, rgba(255, 90, 182, 0.22), transparent 70%),
        radial-gradient(860px 300px at 98% 8%, rgba(89, 215, 255, 0.22), transparent 72%),
        linear-gradient(138deg, var(--hero-bg) 0%, var(--hero-bg-end) 46%, #1c0f2a 100%);
    position: relative;
    overflow: hidden;
}

#<?= $instanceId ?> .gdr-about-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
        repeating-linear-gradient(
            -120deg,
            rgba(255, 255, 255, 0.04) 0 2px,
            transparent 2px 18px
        );
    mix-blend-mode: soft-light;
    pointer-events: none;
}

#<?= $instanceId ?> .gdr-about-hero::after {
    content: '';
    position: absolute;
    inset: auto -10% -94px -10%;
    height: 220px;
    background: radial-gradient(closest-side, rgba(169, 255, 94, 0.26), transparent 72%);
    filter: blur(18px);
    pointer-events: none;
}

#<?= $instanceId ?> .gdr-about-hero .gdr-inner {
    position: relative;
    z-index: 1;
}

#<?= $instanceId ?> .gdr-hero-layout {
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(280px, 0.9fr);
    gap: 16px;
    align-items: stretch;
}

#<?= $instanceId ?> .gdr-hero-main {
    padding: 6px 2px;
}

#<?= $instanceId ?> .gdr-tag {
    margin: 0 0 0.75rem;
    width: fit-content;
    border-radius: 999px;
    border: 1px solid rgba(169, 255, 94, 0.42);
    background: rgba(169, 255, 94, 0.14);
    color: #d8ffb8;
    font-size: 0.72rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.09em;
    padding: 0.28rem 0.72rem;
}

#<?= $instanceId ?> .gdr-tag.gdr-tag-soft {
    border-color: rgba(89, 215, 255, 0.35);
    background: rgba(89, 215, 255, 0.12);
    color: #c8f3ff;
}

#<?= $instanceId ?> .gdr-hero-main h1 {
    margin: 0;
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1.05;
    color: var(--accent-primary);
    text-wrap: balance;
}

#<?= $instanceId ?> .gdr-hero-subtitle {
    margin: 1rem 0 0;
    max-width: 720px;
    color: var(--text-secondary);
    line-height: 1.75;
}

#<?= $instanceId ?> .gdr-story-panel {
    border-radius: 18px;
    background:
        linear-gradient(160deg, rgba(255, 244, 250, 0.14), rgba(255, 244, 250, 0.04)),
        var(--section-bg-1);
    padding: 16px 16px 14px;
    display: grid;
    align-content: start;
    gap: 0.58rem;
    box-shadow: 0 14px 30px rgba(0, 0, 0, 0.24);
}

#<?= $instanceId ?> .gdr-story-kicker {
    margin: 0;
    color: var(--story-title);
    font-size: 0.74rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    font-weight: 800;
}

#<?= $instanceId ?> .gdr-story-panel h2 {
    margin: 0;
    font-size: clamp(1.08rem, 2vw, 1.35rem);
    color: var(--text-primary);
}

#<?= $instanceId ?> .gdr-story-panel p {
    margin: 0;
    color: var(--text-muted);
    line-height: 1.66;
    font-size: 0.92rem;
}

#<?= $instanceId ?> .gdr-stat-stage {
    background:
        radial-gradient(760px 260px at 0% 0%, rgba(89, 215, 255, 0.1), transparent 72%),
        linear-gradient(180deg, #190f25 0%, var(--section-bg-1) 44%, #130d1d 100%);
    padding: 0 0 34px;
}

#<?= $instanceId ?> .gdr-stat-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 12px;
}

#<?= $instanceId ?> .gdr-stat-card {
    border-radius: 14px;
    background: linear-gradient(160deg, rgba(255, 244, 250, 0.12), rgba(255, 244, 250, 0.04));
    min-height: 108px;
    padding: 14px;
    display: grid;
    align-content: center;
    justify-items: center;
    text-align: center;
    gap: 0.35rem;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.18);
}

#<?= $instanceId ?> .gdr-stat-card strong {
    color: var(--stats-number);
    font-size: clamp(1.2rem, 3vw, 1.9rem);
    line-height: 1;
}

#<?= $instanceId ?> .gdr-stat-card span {
    color: var(--stats-label);
    font-size: 0.8rem;
    line-height: 1.4;
}

#<?= $instanceId ?> .gdr-about-core {
    background:
        radial-gradient(780px 300px at 100% 20%, rgba(255, 90, 182, 0.11), transparent 74%),
        linear-gradient(180deg, #130d1d 0%, var(--section-bg-2) 100%);
    padding: 18px 0 58px;
}

#<?= $instanceId ?> .gdr-values-block,
#<?= $instanceId ?> .gdr-team-block {
    border-radius: 16px;
    background: rgba(255, 244, 250, 0.04);
    padding: 14px 0 4px;
    position: relative;
}

#<?= $instanceId ?> .gdr-team-block {
    margin-top: 16px;
}

#<?= $instanceId ?> .gdr-values-block h2,
#<?= $instanceId ?> .gdr-team-block h2 {
    margin: 0;
    color: var(--text-primary);
    font-size: clamp(1.2rem, 3vw, 1.8rem);
}

#<?= $instanceId ?> .gdr-section-note {
    margin: 0.55rem 0 0.85rem;
    color: var(--text-muted);
    line-height: 1.7;
}

#<?= $instanceId ?> .gdr-values-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
}

#<?= $instanceId ?> .gdr-value-card {
    border-radius: 14px;
    padding: 14px;
    background: rgba(255, 244, 250, 0.09);
    display: grid;
    gap: 0.55rem;
    box-shadow: 0 8px 18px rgba(0, 0, 0, 0.14);
}

#<?= $instanceId ?> .gdr-value-card.value-2 {
    background: rgba(169, 255, 94, 0.1);
}

#<?= $instanceId ?> .gdr-value-icon {
    width: 34px;
    height: 34px;
    border-radius: 10px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #210c1d;
    font-weight: 800;
    background: var(--button-bg);
}

#<?= $instanceId ?> .gdr-value-card h3 {
    margin: 0;
    color: var(--text-primary);
    font-size: 1rem;
}

#<?= $instanceId ?> .gdr-value-card p {
    margin: 0;
    color: var(--text-muted);
    line-height: 1.65;
    font-size: 0.9rem;
}

#<?= $instanceId ?> .gdr-team-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 10px;
}

#<?= $instanceId ?> .gdr-team-card {
    border-radius: 12px;
    background: rgba(255, 244, 250, 0.08);
    padding: 12px;
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 10px;
    align-items: center;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.12);
}

#<?= $instanceId ?> .gdr-team-card.member-2,
#<?= $instanceId ?> .gdr-team-card.member-4 {
    background: rgba(89, 215, 255, 0.1);
}

#<?= $instanceId ?> .gdr-team-avatar {
    width: 38px;
    height: 38px;
    border-radius: 50%;
    background: rgba(255, 244, 250, 0.18);
    color: var(--text-primary);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
}

#<?= $instanceId ?> .gdr-team-card h3 {
    margin: 0;
    color: var(--text-primary);
    font-size: 0.95rem;
}

#<?= $instanceId ?> .gdr-team-card p {
    margin: 0.2rem 0 0;
    color: var(--text-muted);
    font-size: 0.82rem;
}

#<?= $instanceId ?> .gdr-about-cta {
    background:
        linear-gradient(180deg, var(--section-bg-2) 0%, #140c1f 100%);
    padding: 0 0 62px;
}

#<?= $instanceId ?> .gdr-cta-layout {
    border-radius: 18px;
    padding: 18px;
    background:
        radial-gradient(460px 140px at 14% 18%, rgba(169, 255, 94, 0.18), transparent 74%),
        radial-gradient(420px 140px at 86% 80%, rgba(89, 215, 255, 0.14), transparent 76%),
        rgba(255, 244, 250, 0.08);
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: center;
    gap: 10px 16px;
    box-shadow: 0 14px 28px rgba(0, 0, 0, 0.2);
}

#<?= $instanceId ?> .gdr-cta-layout h2 {
    margin: 0;
    color: var(--text-primary);
    font-size: clamp(1.2rem, 3vw, 1.7rem);
}

#<?= $instanceId ?> .gdr-cta-layout p {
    margin: 0;
    grid-column: 1;
    color: var(--text-muted);
    line-height: 1.7;
}

#<?= $instanceId ?> .gdr-cta-btn {
    grid-column: 2;
    grid-row: 1 / span 2;
    white-space: nowrap;
    text-decoration: none;
    border-radius: 999px;
    padding: 0.7rem 1.15rem;
    background: var(--button-bg);
    color: var(--button-text);
    font-weight: 800;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
}

@media (max-width: 980px) {
    #<?= $instanceId ?> .gdr-hero-layout,
    #<?= $instanceId ?> .gdr-values-grid,
    #<?= $instanceId ?> .gdr-team-grid {
        grid-template-columns: 1fr;
    }

    #<?= $instanceId ?> .gdr-stat-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    #<?= $instanceId ?> .gdr-cta-layout {
        grid-template-columns: 1fr;
    }

    #<?= $instanceId ?> .gdr-cta-btn {
        grid-column: 1;
        grid-row: auto;
    }
}

@media (max-width: 767px) {
    #<?= $instanceId ?> .gdr-inner {
        width: calc(100% - 1rem);
    }

    #<?= $instanceId ?> .gdr-about-hero {
        padding: 54px 0 28px;
    }

    #<?= $instanceId ?> .gdr-stat-stage {
        padding-bottom: 24px;
    }

    #<?= $instanceId ?> .gdr-stat-grid {
        grid-template-columns: 1fr;
    }

    #<?= $instanceId ?> .gdr-values-block,
    #<?= $instanceId ?> .gdr-team-block,
    #<?= $instanceId ?> .gdr-cta-layout {
        border-radius: 14px;
        padding: 14px;
    }
}
</style>
