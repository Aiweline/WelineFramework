<?php
/**
 * Teen Patti Gold Rush - Contact Content Component
 *
 * @component_start
 * code: contact-content
 * name: 联系我们内容
 * name_en: Contact Content
 * description: 联系方式和表单展示组件，继承主题配色
 * category: content
 * region: content
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示控制
 * display.show_map => 显示地图:select:yes|yes,no
 * display.show_form => 显示表单:select:yes|yes,no
 * display.show_social => 显示社交链接:select:yes|yes,no
 * display.show_info => 显示联系信息:select:yes|yes,no
 *
 * group:hero => Hero设置
 * hero.title => 页面标题:text:Contact Teen Patti Gold Rush
 * hero.subtitle => 页面副标题:textarea:Need support or business help? Reach us through the channels below.
 * hero.bg_color => 背景色:color:#150b1d
 * hero.bg_gradient_end => 渐变结束色:color:#4d1a5f
 * hero.title_color => 标题颜色:color:#ffe5f3
 *
 * group:contact_info => 联系信息
 * contact_info.address_label => 地址标签:text:Address
 * contact_info.address => 地址:textarea:Mumbai Operations Hub, Andheri East, Maharashtra, India
 * contact_info.phone_label => 电话标签:text:Phone
 * contact_info.phone => 电话:text:+91 98765 43210
 * contact_info.email_label => 邮箱标签:text:Email
 * contact_info.email => 邮箱:text:support@teenpattigoldrush.com
 * contact_info.work_hours_label => 工作时间标签:text:Hours
 * contact_info.work_hours => 工作时间:text:24/7 Player Support
 *
 * group:social => 社交媒体
 * social.wechat => WeChat:text:
 * social.weibo => Weibo:text:
 * social.facebook => Facebook:text:https://facebook.com
 * social.twitter => Twitter:text:https://x.com
 * social.linkedin => LinkedIn:text:https://linkedin.com
 *
 * group:form => 表单设置
 * form.title => 表单标题:text:Send A Support Message
 * form.name_label => 姓名标签:text:Name
 * form.email_label => 邮箱标签:text:Email
 * form.subject_label => 主题标签:text:Subject
 * form.message_label => 留言标签:text:Message
 * form.submit_text => 提交按钮:text:Send Message
 * form.success_message => 成功提示:text:Thanks. Our team will reply soon.
 *
 * group:map => 地图设置
 * map.title => 地图标题:text:Location Map
 * map.embed_url => 地图嵌入链接:textarea:
 * map.height => 地图高度:number:400|px
 *
 * @fields_end
 */

$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $component_config ?? $this->getData('component_config') ?? [];
$styleSettings = is_array($styleSettings) ? $styleSettings : [];
$componentConfig = is_array($componentConfig) ? $componentConfig : [];

$getConfigAny = function (array $keys, $default = '') use ($styleSettings, $componentConfig) {
    foreach ($keys as $key) {
        if (array_key_exists($key, $componentConfig) && $componentConfig[$key] !== '') {
            return $componentConfig[$key];
        }
    }

    foreach ($keys as $key) {
        if (array_key_exists($key, $styleSettings) && $styleSettings[$key] !== '') {
            return $styleSettings[$key];
        }
    }

    return $default;
};

$showMap = $getConfigAny(['display.show_map'], 'yes') !== 'no';
$showForm = $getConfigAny(['display.show_form'], 'yes') !== 'no';
$showSocial = $getConfigAny(['display.show_social'], 'yes') !== 'no';
$showInfo = $getConfigAny(['display.show_info'], 'yes') !== 'no';

$heroTitle = $getConfigAny(['hero.title'], '');
$heroSubtitle = $getConfigAny(['hero.subtitle'], '');
$heroBgColorConfig = $getConfigAny(['hero.bg_color'], '');
$heroGradientEndConfig = $getConfigAny(['hero.bg_gradient_end'], '');
$heroTitleColorConfig = $getConfigAny(['hero.title_color'], '');

$contactAddressLabel = (string)$getConfigAny(['contact_info.address_label'], 'Address');
$contactAddress = (string)$getConfigAny(['contact_info.address'], 'Mumbai Operations Hub, Andheri East, Maharashtra, India');
$contactPhoneLabel = (string)$getConfigAny(['contact_info.phone_label'], 'Phone');
$contactPhone = (string)$getConfigAny(['contact_info.phone'], '+91 98765 43210');
$contactEmailLabel = (string)$getConfigAny(['contact_info.email_label'], 'Email');
$contactEmail = (string)$getConfigAny(['contact_info.email'], 'support@teenpattigoldrush.com');
$contactWorkHoursLabel = (string)$getConfigAny(['contact_info.work_hours_label'], 'Hours');
$contactWorkHours = (string)$getConfigAny(['contact_info.work_hours'], '24/7 Player Support');

$socialWechat = $getConfigAny(['social.wechat'], '');
$socialWeibo = $getConfigAny(['social.weibo'], '');
$socialFacebook = $getConfigAny(['social.facebook'], '');
$socialTwitter = $getConfigAny(['social.twitter'], '');
$socialLinkedIn = $getConfigAny(['social.linkedin'], '');

$formTitle = (string)$getConfigAny(['form.title'], 'Send A Support Message');
$formNameLabel = (string)$getConfigAny(['form.name_label'], 'Name');
$formEmailLabel = (string)$getConfigAny(['form.email_label'], 'Email');
$formSubjectLabel = (string)$getConfigAny(['form.subject_label'], 'Subject');
$formMessageLabel = (string)$getConfigAny(['form.message_label'], 'Message');
$formSubmitText = (string)$getConfigAny(['form.submit_text'], 'Send Message');
$formSuccessMessage = (string)$getConfigAny(['form.success_message'], 'Thanks. Our team will reply soon.');

$mapTitle = trim((string)$getConfigAny(['map.title'], 'Location Map'));
$mapEmbedUrl = trim((string)$getConfigAny(['map.embed_url'], ''));
$mapHeight = max(240, (int)$getConfigAny(['map.height'], '400'));
$showMap = $showMap && $mapEmbedUrl !== '';

$page = $this->getData('page');
$pageTitle = $heroTitle ?: ($page ? ($page->getData('title') ?? 'Contact Teen Patti Gold Rush') : 'Contact Teen Patti Gold Rush');

if (!isset($colors) || !is_array($colors)) {
    $colors = [];
}

$heroBg = $heroBgColorConfig ?: ($colors['hero_bg'] ?? '#150b1d');
$heroGradientEnd = $heroGradientEndConfig ?: ($colors['hero_bg_gradient_end'] ?? '#4d1a5f');
$textPrimary = $colors['text_primary'] ?? '#fff0f8';
$textSecondary = $colors['text_secondary'] ?? '#f4deef';
$textMuted = $colors['text_muted'] ?? '#be9fc9';
$sectionBg1 = $colors['section_bg_1'] ?? '#160d22';
$sectionBg2 = $colors['section_bg_2'] ?? '#1b2b18';
$borderLight = $colors['border_light'] ?? 'rgba(255, 206, 236, 0.22)';
$cardBg = $colors['card_bg'] ?? 'rgba(255, 244, 250, 0.09)';
$accentPrimary = $heroTitleColorConfig ?: ($colors['accent_primary'] ?? '#ff5ab6');
$accentSecondary = $colors['accent_secondary'] ?? '#59d7ff';
$accentAlt = $colors['accent_primary_vibrant'] ?? '#a9ff5e';
$buttonPrimaryBg = $colors['button_primary_bg'] ?? 'linear-gradient(106deg, #ffb1dd 0%, #ff5ab6 52%, #8d6dff 100%)';
$buttonPrimaryText = $colors['button_primary_text'] ?? '#210c1d';

$socialLinks = [
    ['name' => 'Facebook', 'url' => trim((string)$socialFacebook), 'short' => 'FB'],
    ['name' => 'Twitter', 'url' => trim((string)$socialTwitter), 'short' => 'X'],
    ['name' => 'LinkedIn', 'url' => trim((string)$socialLinkedIn), 'short' => 'IN'],
    ['name' => 'WeChat', 'url' => trim((string)$socialWechat), 'short' => 'WC'],
    ['name' => 'Weibo', 'url' => trim((string)$socialWeibo), 'short' => 'WB'],
];
$socialLinks = array_values(array_filter($socialLinks, static function ($item) {
    return $item['url'] !== '';
}));

$phoneHref = preg_replace('/[^0-9+]/', '', (string)$contactPhone);

$instanceId = $component_instance_id ?? 'contact-content-' . uniqid();
?>

<section id="<?= $instanceId ?>" class="tpmst-section gdr-contact-content">
    <header class="gdr-contact-hero">
        <div class="gdr-inner">
            <div class="gdr-hero-grid">
                <div class="gdr-hero-copy">
                    <p class="gdr-kicker">Support Center</p>
                    <h1><?= htmlspecialchars((string)$pageTitle) ?></h1>
                    <?php if ($heroSubtitle !== ''): ?>
                        <p class="gdr-subtitle"><?= htmlspecialchars((string)$heroSubtitle) ?></p>
                    <?php endif; ?>
                </div>
                <div class="gdr-quick-actions" aria-label="quick contact channels">
                    <a href="tel:<?= htmlspecialchars((string)$phoneHref) ?>">
                        <span><?= htmlspecialchars($contactPhoneLabel) ?></span>
                        <strong><?= htmlspecialchars((string)$contactPhone) ?></strong>
                    </a>
                    <a href="mailto:<?= htmlspecialchars((string)$contactEmail) ?>">
                        <span><?= htmlspecialchars($contactEmailLabel) ?></span>
                        <strong><?= htmlspecialchars((string)$contactEmail) ?></strong>
                    </a>
                    <div>
                        <span><?= htmlspecialchars($contactWorkHoursLabel) ?></span>
                        <strong><?= htmlspecialchars((string)$contactWorkHours) ?></strong>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <div class="gdr-contact-main">
        <div class="gdr-inner gdr-contact-grid<?= !$showForm ? ' no-form' : '' ?><?= !$showInfo ? ' no-info' : '' ?>">
            <?php if ($showInfo): ?>
                <aside class="gdr-contact-lane">
                    <h2><?= __('Contact Channels') ?></h2>
                    <div class="gdr-lane-list">
                        <article class="gdr-info-card">
                            <small><?= htmlspecialchars($contactAddressLabel) ?></small>
                            <p><?= nl2br(htmlspecialchars((string)$contactAddress)) ?></p>
                        </article>
                        <article class="gdr-info-card">
                            <small><?= htmlspecialchars($contactPhoneLabel) ?></small>
                            <p><a href="tel:<?= htmlspecialchars((string)$phoneHref) ?>"><?= htmlspecialchars((string)$contactPhone) ?></a></p>
                        </article>
                        <article class="gdr-info-card">
                            <small><?= htmlspecialchars($contactEmailLabel) ?></small>
                            <p><a href="mailto:<?= htmlspecialchars((string)$contactEmail) ?>"><?= htmlspecialchars((string)$contactEmail) ?></a></p>
                        </article>
                        <article class="gdr-info-card">
                            <small><?= htmlspecialchars($contactWorkHoursLabel) ?></small>
                            <p><?= htmlspecialchars((string)$contactWorkHours) ?></p>
                        </article>
                    </div>

                    <?php if ($showSocial && !empty($socialLinks)): ?>
                        <div class="gdr-social-row">
                            <?php foreach ($socialLinks as $social): ?>
                                <a href="<?= htmlspecialchars((string)$social['url']) ?>" target="_blank" rel="noopener" aria-label="<?= htmlspecialchars((string)$social['name']) ?>">
                                    <span><?= htmlspecialchars((string)$social['short']) ?></span>
                                    <em><?= htmlspecialchars((string)$social['name']) ?></em>
                                </a>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </aside>
            <?php endif; ?>

            <?php if ($showForm): ?>
                <section class="gdr-form-panel">
                    <h2><?= htmlspecialchars((string)$formTitle) ?></h2>
                    <form class="gdr-form" onsubmit="return false;" aria-label="contact form">
                        <div class="gdr-row">
                            <label>
                                <?= htmlspecialchars($formNameLabel) ?>
                                <input type="text" name="name" placeholder="<?= htmlspecialchars($formNameLabel) ?>" required>
                            </label>
                            <label>
                                <?= htmlspecialchars($formEmailLabel) ?>
                                <input type="email" name="email" placeholder="<?= htmlspecialchars($formEmailLabel) ?>" required>
                            </label>
                        </div>

                        <label>
                            <?= htmlspecialchars($formSubjectLabel) ?>
                            <input type="text" name="subject" placeholder="<?= htmlspecialchars($formSubjectLabel) ?>">
                        </label>

                        <label>
                            <?= htmlspecialchars($formMessageLabel) ?>
                            <textarea name="message" rows="5" placeholder="<?= htmlspecialchars($formMessageLabel) ?>" required></textarea>
                        </label>

                        <button type="submit" class="gdr-submit"><?= htmlspecialchars((string)$formSubmitText) ?></button>
                        <?php if ($formSuccessMessage !== ''): ?>
                            <p class="gdr-form-note"><?= htmlspecialchars((string)$formSuccessMessage) ?></p>
                        <?php endif; ?>
                    </form>
                </section>
            <?php endif; ?>
        </div>
    </div>

    <?php if ($showMap): ?>
        <div class="gdr-map-stage" style="--map-height: <?= (int)$mapHeight ?>px;">
            <div class="gdr-inner">
                <?php if ($mapTitle !== ''): ?>
                    <h2 class="gdr-map-title"><?= htmlspecialchars($mapTitle) ?></h2>
                <?php endif; ?>
                <div class="gdr-map-frame">
                    <iframe src="<?= htmlspecialchars((string)$mapEmbedUrl) ?>" loading="lazy" referrerpolicy="no-referrer-when-downgrade" title="Map"></iframe>
                </div>
            </div>
        </div>
    <?php endif; ?>
</section>

<style>
#<?= $instanceId ?> {
    --hero-bg: <?= htmlspecialchars((string)$heroBg) ?>;
    --hero-bg-end: <?= htmlspecialchars((string)$heroGradientEnd) ?>;
    --text-primary: <?= htmlspecialchars((string)$textPrimary) ?>;
    --text-secondary: <?= htmlspecialchars((string)$textSecondary) ?>;
    --text-muted: <?= htmlspecialchars((string)$textMuted) ?>;
    --section-bg-1: <?= htmlspecialchars((string)$sectionBg1) ?>;
    --section-bg-2: <?= htmlspecialchars((string)$sectionBg2) ?>;
    --border-light: <?= htmlspecialchars((string)$borderLight) ?>;
    --card-bg: <?= htmlspecialchars((string)$cardBg) ?>;
    --accent-primary: <?= htmlspecialchars((string)$accentPrimary) ?>;
    --accent-secondary: <?= htmlspecialchars((string)$accentSecondary) ?>;
    --accent-alt: <?= htmlspecialchars((string)$accentAlt) ?>;
    --button-bg: <?= htmlspecialchars((string)$buttonPrimaryBg) ?>;
    --button-text: <?= htmlspecialchars((string)$buttonPrimaryText) ?>;
}

#<?= $instanceId ?> .gdr-inner {
    width: min(1180px, calc(100% - 1.2rem));
    margin: 0 auto;
}

#<?= $instanceId ?> .gdr-contact-hero {
    padding: 76px 0 42px;
    background:
        radial-gradient(820px 300px at 8% -10%, rgba(255, 90, 182, 0.22), transparent 68%),
        radial-gradient(920px 340px at 96% 6%, rgba(89, 215, 255, 0.22), transparent 70%),
        linear-gradient(135deg, var(--hero-bg) 0%, var(--hero-bg-end) 48%, #1c0f2a 100%);
    position: relative;
    overflow: hidden;
}

#<?= $instanceId ?> .gdr-contact-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
        repeating-linear-gradient(
            115deg,
            rgba(255, 255, 255, 0.045) 0 2px,
            transparent 2px 18px
        );
    mix-blend-mode: soft-light;
    pointer-events: none;
}

#<?= $instanceId ?> .gdr-contact-hero::after {
    content: '';
    position: absolute;
    inset: auto -8% -88px -8%;
    height: 200px;
    background: radial-gradient(closest-side, rgba(255, 90, 182, 0.24), transparent 72%);
    filter: blur(16px);
    pointer-events: none;
}

#<?= $instanceId ?> .gdr-contact-hero .gdr-inner {
    position: relative;
    z-index: 1;
}

#<?= $instanceId ?> .gdr-hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(250px, 0.82fr);
    gap: 16px;
    align-items: end;
}

#<?= $instanceId ?> .gdr-kicker {
    margin: 0 0 0.72rem;
    width: fit-content;
    border-radius: 999px;
    padding: 0.28rem 0.68rem;
    border: 1px solid rgba(169, 255, 94, 0.4);
    background: rgba(169, 255, 94, 0.14);
    color: #d8ffb8;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

#<?= $instanceId ?> .gdr-hero-copy h1 {
    margin: 0;
    color: var(--accent-primary);
    font-size: clamp(2rem, 5vw, 3.2rem);
    line-height: 1.06;
}

#<?= $instanceId ?> .gdr-subtitle {
    margin: 0.9rem 0 0;
    max-width: 720px;
    color: var(--text-secondary);
    line-height: 1.76;
}

#<?= $instanceId ?> .gdr-quick-actions {
    display: grid;
    gap: 8px;
}

#<?= $instanceId ?> .gdr-quick-actions a,
#<?= $instanceId ?> .gdr-quick-actions div {
    border-radius: 12px;
    background: rgba(255, 244, 250, 0.12);
    padding: 10px 12px;
    text-decoration: none;
    display: grid;
    gap: 0.2rem;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.16);
}

#<?= $instanceId ?> .gdr-quick-actions span {
    color: var(--accent-alt);
    font-size: 0.66rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 800;
}

#<?= $instanceId ?> .gdr-quick-actions strong {
    color: var(--text-primary);
    font-size: 0.86rem;
    line-height: 1.36;
    word-break: break-word;
}

#<?= $instanceId ?> .gdr-contact-main {
    background:
        radial-gradient(720px 260px at 0% 100%, rgba(169, 255, 94, 0.12), transparent 72%),
        radial-gradient(620px 220px at 100% 0%, rgba(89, 215, 255, 0.12), transparent 70%),
        linear-gradient(180deg, #190f25 0%, var(--section-bg-1) 45%, #130d1d 100%);
    padding: 32px 0 56px;
}

#<?= $instanceId ?> .gdr-contact-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.92fr) minmax(0, 1.08fr);
    gap: 14px;
}

#<?= $instanceId ?> .gdr-contact-grid.no-form,
#<?= $instanceId ?> .gdr-contact-grid.no-info {
    grid-template-columns: 1fr;
}

#<?= $instanceId ?> .gdr-contact-lane,
#<?= $instanceId ?> .gdr-form-panel {
    border-radius: 16px;
    background: var(--card-bg);
    padding: 14px;
    box-shadow: 0 14px 26px rgba(0, 0, 0, 0.2);
}

#<?= $instanceId ?> .gdr-contact-lane h2,
#<?= $instanceId ?> .gdr-form-panel h2 {
    margin: 0;
    color: var(--text-primary);
    font-size: clamp(1.15rem, 2.8vw, 1.6rem);
}

#<?= $instanceId ?> .gdr-lane-list {
    margin-top: 0.9rem;
    display: grid;
    gap: 8px;
}

#<?= $instanceId ?> .gdr-info-card {
    border-radius: 11px;
    background: rgba(255, 244, 250, 0.1);
    padding: 10px 11px;
    position: relative;
}

#<?= $instanceId ?> .gdr-info-card small {
    color: var(--accent-secondary);
    font-size: 0.68rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 700;
}

#<?= $instanceId ?> .gdr-info-card p {
    margin: 0.34rem 0 0;
    color: var(--text-secondary);
    line-height: 1.6;
    font-size: 0.9rem;
}

#<?= $instanceId ?> .gdr-info-card a {
    color: var(--text-primary);
    text-decoration: none;
}

#<?= $instanceId ?> .gdr-social-row {
    margin-top: 10px;
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
}

#<?= $instanceId ?> .gdr-social-row a {
    border-radius: 999px;
    background: rgba(255, 244, 250, 0.14);
    color: var(--text-primary);
    text-decoration: none;
    padding: 0.28rem 0.55rem;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.14);
}

#<?= $instanceId ?> .gdr-social-row span {
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: rgba(169, 255, 94, 0.16);
    color: #d8ffb8;
    font-size: 0.66rem;
    font-weight: 800;
    display: inline-flex;
    align-items: center;
    justify-content: center;
}

#<?= $instanceId ?> .gdr-social-row em {
    font-style: normal;
    font-size: 0.74rem;
}

#<?= $instanceId ?> .gdr-form {
    margin-top: 0.9rem;
    display: grid;
    gap: 0.75rem;
}

#<?= $instanceId ?> .gdr-row {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.65rem;
}

#<?= $instanceId ?> label {
    display: grid;
    gap: 0.3rem;
    font-size: 0.8rem;
    color: var(--text-primary);
    font-weight: 700;
}

#<?= $instanceId ?> input,
#<?= $instanceId ?> textarea {
    width: 100%;
    border: 0;
    border-radius: 9px;
    background: rgba(255, 244, 250, 0.1);
    color: var(--text-primary);
    padding: 10px 11px;
    font-size: 0.9rem;
    font-family: inherit;
    box-shadow: inset 0 0 0 1px rgba(255, 206, 236, 0.22);
}

#<?= $instanceId ?> textarea {
    resize: vertical;
    min-height: 120px;
}

#<?= $instanceId ?> .gdr-submit {
    width: fit-content;
    border: 0;
    border-radius: 999px;
    padding: 0.64rem 1rem;
    background: var(--button-bg);
    color: var(--button-text);
    font-weight: 800;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    cursor: pointer;
}

#<?= $instanceId ?> .gdr-form-note {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.8rem;
}

#<?= $instanceId ?> .gdr-map-stage {
    background:
        linear-gradient(180deg, #130d1d 0%, var(--section-bg-2) 100%);
    padding: 0 0 58px;
}

#<?= $instanceId ?> .gdr-map-title {
    margin: 0 0 1rem;
    color: var(--text-primary);
    font-size: clamp(1.2rem, 3vw, 1.65rem);
}

#<?= $instanceId ?> .gdr-map-frame,
#<?= $instanceId ?> .gdr-map-empty {
    border-radius: 16px;
    overflow: hidden;
    background:
        linear-gradient(150deg, rgba(255, 244, 250, 0.15), rgba(255, 244, 250, 0.06));
    min-height: var(--map-height);
    box-shadow: 0 14px 26px rgba(0, 0, 0, 0.18);
}

#<?= $instanceId ?> iframe {
    width: 100%;
    height: var(--map-height);
    border: 0;
}

#<?= $instanceId ?> .gdr-map-empty {
    display: grid;
    place-content: center;
    text-align: center;
    gap: 0.5rem;
    padding: 16px;
}

#<?= $instanceId ?> .gdr-map-empty h3 {
    margin: 0;
    color: var(--accent-primary);
}

#<?= $instanceId ?> .gdr-map-empty p {
    margin: 0;
    color: var(--text-muted);
}

@media (max-width: 980px) {
    #<?= $instanceId ?> .gdr-hero-grid,
    #<?= $instanceId ?> .gdr-contact-grid,
    #<?= $instanceId ?> .gdr-row {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 767px) {
    #<?= $instanceId ?> .gdr-inner {
        width: calc(100% - 1rem);
    }

    #<?= $instanceId ?> .gdr-contact-hero {
        padding: 54px 0 28px;
    }

    #<?= $instanceId ?> .gdr-contact-main {
        padding: 22px 0 40px;
    }

    #<?= $instanceId ?> .gdr-contact-lane,
    #<?= $instanceId ?> .gdr-form-panel,
    #<?= $instanceId ?> .gdr-map-frame,
    #<?= $instanceId ?> .gdr-map-empty {
        border-radius: 12px;
    }
}
</style>
