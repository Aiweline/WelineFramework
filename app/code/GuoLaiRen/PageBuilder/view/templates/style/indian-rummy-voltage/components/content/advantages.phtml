<?php
/**
 * Indian Rummy Voltage - Advantages Component
 *
 * @component_start
 * code: advantages
 * name: 优势展示
 * name_en: Advantages Section
 * description: 展示产品/应用核心优势，三卡异形布局
 * category: content
 * region: content
 * thumbnail: asset/img/our-advantages/icon01.webp
 * @component_end
 *
 * @fields_start
 *
 * group:display => 显示设置
 * display.show_section => 显示区块:select:yes|yes,no
 * display.show_title => 显示标题:select:yes|yes,no
 * display.show_description => 显示描述:select:yes|yes,no
 * display.show_button => 显示按钮:select:yes|yes,no
 *
 * group:style => 样式设置
 * style.bg_color => 背景颜色:color:#16363f
 * style.title_color => 标题颜色:color:#fff2df
 * style.title_font_size => 标题字号桌面端:number:36|px
 * style.title_font_size_mobile => 标题字号移动端:number:24|px
 * style.card_title_color => 卡片标题颜色:color:#0f2630
 * style.card_title_font_size => 卡片标题字号:number:20|px
 * style.card_desc_color => 卡片描述颜色:color:#223740
 * style.card_desc_font_size => 卡片描述字号:number:14|px
 * style.icon_size => 图标尺寸:number:82|px
 * style.icon_border_radius => 图标圆角:number:24|%
 * style.section_padding => 区块上下间距桌面端:number:70|px
 * style.section_padding_mobile => 区块上下间距移动端:number:42|px
 * style.card_gap => 卡片间距:number:24|px
 *
 * group:button => 按钮设置
 * button.text => 按钮文本:text:Join Table
 * button.url => 按钮链接:text:#download
 * button.bg_gradient_start => 按钮渐变起始色:color:#f3bf52
 * button.bg_gradient_end => 按钮渐变结束色:color:#c9472d
 * button.text_color => 按钮文字颜色:color:#1d1408
 * button.border_radius => 按钮圆角:number:999|px
 * button.padding_x => 按钮水平内边距:number:30|px
 * button.padding_y => 按钮垂直内边距:number:10|px
 *
 * group:icons => 图标设置
 * icons.icon_1 => 优势1图标:textarea:|默认使用本地图标icon01.webp
 * icons.icon_2 => 优势2图标:textarea:|默认使用本地图标icon02.webp
 * icons.icon_3 => 优势3图标:textarea:|默认使用本地图标icon03.webp
 *
 * group:texts => 文本设置
 * texts.title => 区块标题:text:Our Advantages
 * texts.summary => 区块描述:textarea:Why players pick Indian Rummy Voltage for faster table entry, stable round sync, and cleaner action focus.
 * texts.advantage_1_title => 优势1标题:text:Fast Rummy Entry
 * texts.advantage_1_desc => 优势1描述:textarea:Open active Rummy rooms quickly with fewer taps and clearer mode grouping.
 * texts.advantage_2_title => 优势2标题:text:Stable Round Sync
 * texts.advantage_2_desc => 优势2描述:textarea:Card state and wallet values update in a clean rhythm across repeat rounds.
 * texts.advantage_3_title => 优势3标题:text:Pace-Friendly UI
 * texts.advantage_3_desc => 优势3描述:textarea:Readable hierarchy and stronger focus zones help players scan decisions faster.
 *
 * @fields_end
 */

$componentId = 'rv-advantages-' . uniqid();

$page = $this->getData('page');
$styleSettings = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$config = array_merge($styleSettings, $componentConfig);

$getConfig = function ($key, $default = '') use ($config) {
    return isset($config[$key]) && $config[$key] !== '' ? $config[$key] : $default;
};

$display = $getConfig('display.show_section', $getConfig('advantages.display', 'yes')) !== 'no';
if (!$display) {
    return;
}

$showTitle = $getConfig('display.show_title', 'yes') !== 'no';
$showDescription = $getConfig('display.show_description', 'yes') !== 'no';
$showButton = $getConfig('display.show_button', 'yes') !== 'no';

$bgColor = $getConfig('style.bg_color', $getConfig('advantages.bg_color', '#16363f'));
$titleColor = $getConfig('style.title_color', '#fff2df');
$titleFontSize = (int)$getConfig('style.title_font_size', '36');
$titleFontSizeMobile = (int)$getConfig('style.title_font_size_mobile', '24');
$cardTitleColor = $getConfig('style.card_title_color', '#0f2630');
$cardTitleFontSize = (int)$getConfig('style.card_title_font_size', '20');
$cardDescColor = $getConfig('style.card_desc_color', '#223740');
$cardDescFontSize = (int)$getConfig('style.card_desc_font_size', '14');
$iconSize = (int)$getConfig('style.icon_size', '82');
$iconBorderRadius = (int)$getConfig('style.icon_border_radius', '24');
$sectionPadding = (int)$getConfig('style.section_padding', '70');
$sectionPaddingMobile = (int)$getConfig('style.section_padding_mobile', '42');
$cardGap = (int)$getConfig('style.card_gap', '24');

$btnText = $getConfig('button.text', 'Join Table');
$btnUrl = $getConfig('button.url', '#download');
$btnGradientStart = $getConfig('button.bg_gradient_start', '#f3bf52');
$btnGradientEnd = $getConfig('button.bg_gradient_end', '#c9472d');
$btnTextColor = $getConfig('button.text_color', '#1d1408');
$btnBorderRadius = (int)$getConfig('button.border_radius', '999');
$btnPaddingX = (int)$getConfig('button.padding_x', '30');
$btnPaddingY = (int)$getConfig('button.padding_y', '10');

$defaultIcon1 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/indian-rummy-voltage/asset/img/our-advantages/icon01.webp');
$defaultIcon2 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/indian-rummy-voltage/asset/img/our-advantages/icon02.webp');
$defaultIcon3 = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/indian-rummy-voltage/asset/img/our-advantages/icon03.webp');

$icon1 = $getConfig('icons.icon_1', $getConfig('advantages.icon_1', '')) ?: $defaultIcon1;
$icon2 = $getConfig('icons.icon_2', $getConfig('advantages.icon_2', '')) ?: $defaultIcon2;
$icon3 = $getConfig('icons.icon_3', $getConfig('advantages.icon_3', '')) ?: $defaultIcon3;

$title = $getConfig('texts.title', $getConfig('advantages.title', 'Our Advantages'));
$brandName = $getConfig('logo.text', $getConfig('header.logo.text', $getConfig('texts.app_name', $getConfig('app_info.app_name', 'Indian Rummy Voltage'))));
$sectionDesc = $getConfig('texts.summary', $getConfig('texts.section_desc', $getConfig('advantages.description', 'Why players pick Indian Rummy Voltage for faster table entry, stable round sync, and cleaner action focus.')));
$title1 = $getConfig('texts.advantage_1_title', 'Fast Rummy Entry');
$desc1 = $getConfig('texts.advantage_1_desc', 'Open active Rummy rooms quickly with fewer taps and clearer mode grouping.');
$title2 = $getConfig('texts.advantage_2_title', 'Stable Round Sync');
$desc2 = $getConfig('texts.advantage_2_desc', 'Card state and wallet values update in a clean rhythm across repeat rounds.');
$title3 = $getConfig('texts.advantage_3_title', 'Pace-Friendly UI');
$desc3 = $getConfig('texts.advantage_3_desc', 'Readable hierarchy and stronger focus zones help players scan decisions faster.');

$cards = [
    ['icon' => $icon1, 'title' => $title1, 'desc' => $desc1],
    ['icon' => $icon2, 'title' => $title2, 'desc' => $desc2],
    ['icon' => $icon3, 'title' => $title3, 'desc' => $desc3],
];
?>

<section id="<?= $componentId ?>" class="rvadv" aria-labelledby="<?= $componentId ?>-title">
    <div class="rvadv-inner">
        <header class="rvadv-intro">
            <?php if ($showTitle): ?>
                <span class="rvadv-kicker"><?= htmlspecialchars($brandName) ?></span>
                <div class="rvadv-title-wrap">
                    <h2 id="<?= $componentId ?>-title"><?= htmlspecialchars($title) ?></h2>
                    <span class="rvadv-title-mark" aria-hidden="true">
                        <span class="mark-line"></span>
                        <span class="mark-dot"></span>
                    </span>
                </div>
            <?php endif; ?>

            <?php if ($showDescription && $sectionDesc !== ''): ?>
                <p class="rvadv-desc"><?= htmlspecialchars($sectionDesc) ?></p>
            <?php endif; ?>

            <?php if ($showButton): ?>
                <a href="<?= htmlspecialchars($btnUrl) ?>" class="rvadv-cta"><?= htmlspecialchars($btnText) ?></a>
            <?php endif; ?>
        </header>

        <div class="rvadv-arena" style="--rvadv-gap: <?= (int)$cardGap ?>px;">
            <div class="rvadv-pot" aria-hidden="true">
                <span>Table Core</span>
                <strong>3x</strong>
            </div>

            <?php foreach ($cards as $index => $card): ?>
                <article class="rvadv-card rvadv-card-<?= (int)($index + 1) ?> rvadv-card-<?= $index === 0 ? 'a' : ($index === 1 ? 'b' : 'c') ?>">
                    <div class="rvadv-card-head">
                        <img src="<?= htmlspecialchars($card['icon']) ?>" alt="<?= htmlspecialchars($card['title']) ?>" loading="lazy" decoding="async">
                        <span><?= sprintf('%02d', $index + 1) ?></span>
                    </div>
                    <h3><?= htmlspecialchars($card['title']) ?></h3>
                    <?php if ($showDescription): ?>
                        <p><?= htmlspecialchars($card['desc']) ?></p>
                    <?php endif; ?>
                </article>
            <?php endforeach; ?>
        </div>
    </div>
</section>

<style>
#<?= $componentId ?> {
    position: relative;
    overflow: hidden;
    padding: <?= (int)$sectionPadding ?>px 0;
    background:
        radial-gradient(900px 340px at 14% 10%, rgba(255, 188, 96, 0.15), transparent 68%),
        linear-gradient(180deg, rgba(5, 33, 27, 0.97) 0%, rgba(4, 24, 21, 0.99) 100%),
        <?= htmlspecialchars($bgColor) ?>;
}

#<?= $componentId ?>::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background-image: radial-gradient(rgba(255, 255, 255, 0.2) 0.7px, transparent 0.7px);
    background-size: 16px 16px;
    opacity: .18;
}

#<?= $componentId ?>::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
        repeating-linear-gradient(90deg, transparent 0 68px, rgba(255, 208, 103, 0.07) 68px 69px),
        radial-gradient(circle at 86% 78%, rgba(0, 224, 178, 0.12) 0 14%, transparent 14.6%);
    opacity: .32;
}

#<?= $componentId ?> .rvadv-inner {
    width: min(1220px, calc(100% - 1.2rem));
    margin: 0 auto;
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: minmax(0, .8fr) minmax(0, 1.2fr);
    gap: 1.2rem;
    align-items: center;
}

#<?= $componentId ?> .rvadv-intro {
    min-width: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding-right: .2rem;
}

#<?= $componentId ?> .rvadv-kicker {
    display: inline-flex;
    width: fit-content;
    padding: .34rem .74rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 208, 103, 0.48);
    background: rgba(255, 208, 103, 0.14);
    color: #ffe8ba;
    font-size: .72rem;
    letter-spacing: .1em;
    text-transform: uppercase;
    font-weight: 800;
}

#<?= $componentId ?> .rvadv-title-wrap {
    margin: .76rem 0 0;
    display: flex;
    align-items: center;
    gap: .66rem;
}

#<?= $componentId ?> .rvadv-intro h2 {
    margin: 0;
    color: <?= htmlspecialchars($titleColor) ?>;
    font-size: clamp(<?= (int)$titleFontSizeMobile ?>px, 3.6vw, <?= (int)$titleFontSize ?>px);
    line-height: 1.06;
}

#<?= $componentId ?> .rvadv-title-mark {
    display: inline-flex;
    align-items: center;
    gap: .34rem;
    min-width: 78px;
    flex: 1;
}

#<?= $componentId ?> .rvadv-title-mark .mark-line,
#<?= $componentId ?> .rvadv-title-mark .mark-dot {
    border-radius: 999px;
    display: inline-flex;
}

#<?= $componentId ?> .rvadv-title-mark .mark-line {
    width: min(180px, 100%);
    height: 10px;
    position: relative;
    overflow: hidden;
    background: linear-gradient(90deg, rgba(255, 208, 103, 0.26) 0%, rgba(255, 208, 103, 0.84) 38%, rgba(0, 224, 178, 0.78) 100%);
}

#<?= $componentId ?> .rvadv-title-mark .mark-line::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(120deg, rgba(255, 255, 255, 0) 12%, rgba(255, 255, 255, 0.86) 50%, rgba(255, 255, 255, 0) 88%);
    transform: translateX(-130%);
    animation: rvadv-mark-scan 6.1s ease-in-out infinite;
}

#<?= $componentId ?> .rvadv-title-mark .mark-dot {
    width: 12px;
    height: 12px;
    background: radial-gradient(circle at 30% 30%, #fff2cf 0%, #ffd067 52%, #ff8f42 100%);
    box-shadow: 0 0 0 3px rgba(255, 208, 103, 0.2);
}

#<?= $componentId ?> .rvadv-desc {
    margin: .76rem 0 0;
    color: #d6e6ee;
    font-size: <?= (int)$cardDescFontSize ?>px;
    line-height: 1.76;
    max-width: 460px;
}

#<?= $componentId ?> .rvadv-cta {
    margin-top: 1rem;
    width: fit-content;
    text-decoration: none;
    padding: <?= (int)$btnPaddingY ?>px <?= (int)$btnPaddingX ?>px;
    border-radius: <?= (int)$btnBorderRadius ?>px;
    background: linear-gradient(112deg, <?= htmlspecialchars($btnGradientStart) ?> 0%, <?= htmlspecialchars($btnGradientEnd) ?> 100%);
    color: <?= htmlspecialchars($btnTextColor) ?>;
    font-size: .78rem;
    font-weight: 800;
    letter-spacing: .08em;
    text-transform: uppercase;
    box-shadow: 0 14px 24px rgba(201, 71, 45, 0.4);
}

#<?= $componentId ?> .rvadv-arena {
    position: relative;
    min-height: 460px;
    border-radius: 34px;
    background:
        radial-gradient(circle at 50% 46%, rgba(3, 112, 89, 0.86) 0%, rgba(3, 79, 66, 0.92) 54%, rgba(2, 36, 34, 0.98) 100%);
    border: 1px solid rgba(255, 208, 103, 0.35);
    box-shadow:
        inset 0 0 0 2px rgba(255, 208, 103, 0.18),
        0 30px 44px rgba(0, 0, 0, 0.32);
}

#<?= $componentId ?> .rvadv-arena::before {
    content: '';
    position: absolute;
    inset: 7%;
    border-radius: 28px;
    border: 1px dashed rgba(255, 228, 166, 0.38);
    pointer-events: none;
}

#<?= $componentId ?> .rvadv-pot {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    width: 122px;
    height: 122px;
    border-radius: 999px;
    display: grid;
    place-content: center;
    text-align: center;
    z-index: 2;
    background: radial-gradient(circle at 34% 32%, #fff2cf 0%, #ffd067 58%, #ff8f42 100%);
    box-shadow: 0 0 0 9px rgba(255, 208, 103, 0.14), 0 10px 18px rgba(0, 0, 0, 0.28);
}

#<?= $componentId ?> .rvadv-pot span {
    font-size: .62rem;
    letter-spacing: .08em;
    text-transform: uppercase;
    font-weight: 700;
    color: #5f3e0b;
}

#<?= $componentId ?> .rvadv-pot strong {
    margin-top: .12rem;
    font-size: 1.32rem;
    line-height: 1;
    color: #3d2405;
}

#<?= $componentId ?> .rvadv-card {
    position: absolute;
    width: min(46%, 294px);
    position: relative;
    border-radius: 20px;
    padding: 1rem;
    background: rgba(252, 246, 233, 0.96);
    box-shadow: 0 18px 30px rgba(0, 0, 0, 0.26);
    transform-origin: center;
}

#<?= $componentId ?> .rvadv-card-a {
    left: 7%;
    top: 10%;
    transform: rotate(-9deg);
}

#<?= $componentId ?> .rvadv-card-b {
    right: 7%;
    top: 12%;
    transform: rotate(8deg);
}

#<?= $componentId ?> .rvadv-card-c {
    left: 50%;
    bottom: 9%;
    transform: translateX(-50%) rotate(1.5deg);
}

#<?= $componentId ?> .rvadv-card-2 {
    background: linear-gradient(142deg, rgba(255, 208, 103, 0.94), rgba(255, 90, 93, 0.9));
}

#<?= $componentId ?> .rvadv-card-2 h3,
#<?= $componentId ?> .rvadv-card-2 p,
#<?= $componentId ?> .rvadv-card-2 .rvadv-card-head span {
    color: #fffaf1;
}

#<?= $componentId ?> .rvadv-card-2 .rvadv-card-head img {
    border-color: rgba(255, 255, 255, 0.55);
    background: rgba(0, 0, 0, 0.2);
}

#<?= $componentId ?> .rvadv-card-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: .7rem;
}

#<?= $componentId ?> .rvadv-card-head img {
    width: <?= (int)$iconSize ?>px;
    height: <?= (int)$iconSize ?>px;
    border-radius: <?= (int)$iconBorderRadius ?>%;
    object-fit: cover;
    border: 1px solid rgba(17, 40, 49, 0.22);
    background: rgba(255, 255, 255, 0.88);
    padding: .2rem;
}

#<?= $componentId ?> .rvadv-card-head span {
    display: inline-flex;
    min-width: 2.1rem;
    height: 2.1rem;
    border-radius: 999px;
    align-items: center;
    justify-content: center;
    background: rgba(16, 53, 61, 0.12);
    color: #2a4049;
    font-size: .76rem;
    letter-spacing: .06em;
    font-weight: 800;
}

#<?= $componentId ?> .rvadv-card h3 {
    margin: .78rem 0 0;
    color: <?= htmlspecialchars($cardTitleColor) ?>;
    font-size: <?= (int)$cardTitleFontSize ?>px;
    line-height: 1.24;
}

#<?= $componentId ?> .rvadv-card p {
    margin: .62rem 0 0;
    color: <?= htmlspecialchars($cardDescColor) ?>;
    font-size: <?= (int)$cardDescFontSize ?>px;
    line-height: 1.68;
}

@keyframes rvadv-mark-scan {
    0%, 35% { transform: translateX(-130%); }
    62% { transform: translateX(128%); }
    100% { transform: translateX(128%); }
}

@media (max-width: 900px) {
    #<?= $componentId ?> {
        padding: <?= (int)$sectionPaddingMobile ?>px 0;
    }

    #<?= $componentId ?> .rvadv-inner {
        width: calc(100% - 1rem);
        grid-template-columns: 1fr;
        gap: .86rem;
    }

    #<?= $componentId ?> .rvadv-intro {
        padding-right: 0;
    }

    #<?= $componentId ?> .rvadv-desc {
        max-width: none;
    }

    #<?= $componentId ?> .rvadv-title-wrap {
        gap: .5rem;
    }

    #<?= $componentId ?> .rvadv-title-mark .mark-line {
        width: 76px;
    }

    #<?= $componentId ?> .rvadv-arena {
        min-height: auto;
        border-radius: 18px;
        padding: .72rem;
    }

    #<?= $componentId ?> .rvadv-arena::before,
    #<?= $componentId ?> .rvadv-pot {
        display: none;
    }

    #<?= $componentId ?> .rvadv-card {
        position: relative;
        width: 100%;
        border-radius: 14px;
        padding: .86rem;
        margin: 0 0 .62rem;
    }

    #<?= $componentId ?> .rvadv-card-head img {
        width: clamp(58px, 18vw, <?= (int)$iconSize ?>px);
        height: clamp(58px, 18vw, <?= (int)$iconSize ?>px);
    }

    #<?= $componentId ?> .rvadv-card-a {
        transform: rotate(-2deg);
        left: auto;
        top: auto;
    }

    #<?= $componentId ?> .rvadv-card-b {
        transform: rotate(1.6deg);
        right: auto;
        top: auto;
    }

    #<?= $componentId ?> .rvadv-card-c {
        transform: rotate(-1deg);
        left: auto;
        bottom: auto;
    }
}
</style>
