<?php
/**
 * TPMST 博客文章详情组件
 * 
 * @fields_start
 * blog.show_sidebar|显示侧边栏|select:yes=是,no=否|yes
 * blog.show_cover|显示封面图|select:yes=是,no=否|yes
 * blog.show_author|显示作者|select:yes=是,no=否|yes
 * blog.show_date|显示日期|select:yes=是,no=否|yes
 * blog.show_tags|显示标签|select:yes=是,no=否|yes
 * blog.show_share|显示分享按钮|select:yes=是,no=否|yes
 * blog.show_related|显示相关文章|select:yes=是,no=否|yes
 * blog.related_count|相关文章数量|number|3
 * @fields_end
 * 
 * @var \Weline\Framework\View\Template $this
 * @var array $current_post 当前文章
 * @var array $related_posts 相关文章
 * @var array $blog_categories 博客分类
 * @var array $recent_posts 最近文章
 * @var array $style_settings 样式配置
 */

$style = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$currentPost = $this->getData('current_post') ?: [];
$relatedPosts = $this->getData('related_posts') ?: [];
$blogCategories = $this->getData('blog_categories') ?: [];
$recentPosts = $this->getData('recent_posts') ?: [];

// 配置项
$getConfig = function($key, $default = '') use ($style, $componentConfig) {
    if (isset($componentConfig[$key]) && $componentConfig[$key] !== '') {
        return $componentConfig[$key];
    }
    return $style[$key] ?? $default;
};

$showSidebar = $getConfig('blog.show_sidebar', 'yes') === 'yes';
$showCover = $getConfig('blog.show_cover', 'yes') === 'yes';
$showAuthor = $getConfig('blog.show_author', 'yes') === 'yes';
$showDate = $getConfig('blog.show_date', 'yes') === 'yes';
$showTags = $getConfig('blog.show_tags', 'yes') === 'yes';
$showShare = $getConfig('blog.show_share', 'yes') === 'yes';
$showRelated = $getConfig('blog.show_related', 'yes') === 'yes';

// 颜色配置
$primaryColor = $getConfig('color_scheme.primary', '#ffd700');
$secondaryColor = $getConfig('color_scheme.secondary', '#9a24c1');
$bgColor = $getConfig('color_scheme.background', '#12072a');
$textColor = $getConfig('color_scheme.text', '#f8fafc');
$pokerBg = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/teen-patti-master/asset/img/poker-header-bg.svg');

// 如果没有当前文章数据，显示空状态
if (empty($currentPost)):
?>
<section class="tpmst-section tpmst-blog-detail">
    <div class="tpmst-section-inner">
        <div class="blog-not-found">
            <div class="not-found-icon">
                <svg width="80" height="80" viewBox="0 0 24 24" fill="currentColor" opacity="0.3">
                    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"/>
                </svg>
            </div>
            <h2><?= __('Article Not Found') ?></h2>
            <p><?= __('The article you are looking for does not exist or has been removed.') ?></p>
            <a href="/blog" class="btn-back-to-blog"><?= __('Back to Blog') ?></a>
        </div>
    </div>
</section>
<?php else: ?>
<section class="tpmst-section tpmst-blog-detail">
    <div class="tpmst-section-inner">
        <!-- 面包屑导航 -->
        <nav class="blog-breadcrumb">
            <a href="/"><?= __('Home') ?></a>
            <span class="separator">/</span>
            <a href="/blog"><?= __('Blog') ?></a>
            <span class="separator">/</span>
            <span class="current"><?= htmlspecialchars(mb_substr($currentPost['title'], 0, 30)) ?><?= mb_strlen($currentPost['title']) > 30 ? '...' : '' ?></span>
        </nav>
        
        <div class="blog-detail-wrapper <?= $showSidebar ? 'with-sidebar' : 'full-width' ?>">
            <!-- 文章主体 -->
            <article class="blog-article">
                <!-- 文章标题 -->
                <header class="article-header">
                    <h1 class="article-title"><?= htmlspecialchars($currentPost['title']) ?></h1>
                    
                    <div class="article-meta">
                        <?php if ($showDate && !empty($currentPost['published_at'])): ?>
                        <span class="meta-item meta-date">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M19 4h-1V2h-2v2H8V2H6v2H5c-1.11 0-1.99.9-1.99 2L3 20c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V9h14v11zM9 11H7v2h2v-2zm4 0h-2v2h2v-2zm4 0h-2v2h2v-2z"/>
                            </svg>
                            <?= date('F d, Y', strtotime($currentPost['published_at'])) ?>
                        </span>
                        <?php endif; ?>
                        
                        <?php if ($showAuthor && !empty($currentPost['author'])): ?>
                        <span class="meta-item meta-author">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                            </svg>
                            <?= htmlspecialchars($currentPost['author']) ?>
                        </span>
                        <?php endif; ?>
                        
                        <?php if (!empty($currentPost['view_count'])): ?>
                        <span class="meta-item meta-views">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"/>
                            </svg>
                            <?= number_format($currentPost['view_count']) ?> <?= __('views') ?>
                        </span>
                        <?php endif; ?>
                    </div>
                </header>
                
                <!-- 封面图 -->
                <?php if ($showCover && !empty($currentPost['cover_image'])): ?>
                <div class="article-cover">
                    <img src="<?= htmlspecialchars($currentPost['cover_image']) ?>" 
                         alt="<?= htmlspecialchars($currentPost['title']) ?>">
                </div>
                <?php endif; ?>
                
                <!-- 摘要 -->
                <?php if (!empty($currentPost['summary'])): ?>
                <div class="article-summary">
                    <p><?= htmlspecialchars($currentPost['summary']) ?></p>
                </div>
                <?php endif; ?>
                
                <!-- 文章内容 -->
                <div class="article-content">
                    <?= $currentPost['content'] ?? '' ?>
                </div>
                
                <!-- 标签 -->
                <?php if ($showTags && !empty($currentPost['tags'])): ?>
                <div class="article-tags">
                    <span class="tags-label"><?= __('Tags:') ?></span>
                    <?php 
                    $tags = is_array($currentPost['tags']) ? $currentPost['tags'] : explode(',', $currentPost['tags']);
                    foreach ($tags as $tag): 
                        $tag = trim($tag);
                        if (empty($tag)) continue;
                    ?>
                    <a href="/blog/tag/<?= urlencode($tag) ?>" class="tag">#<?= htmlspecialchars($tag) ?></a>
                    <?php endforeach; ?>
                </div>
                <?php endif; ?>
                
                <!-- 分享按钮 -->
                <?php if ($showShare): ?>
                <div class="article-share">
                    <span class="share-label"><?= __('Share:') ?></span>
                    <div class="share-buttons">
                        <a href="https://www.facebook.com/sharer/sharer.php?u=<?= urlencode(\w_env_request_uri()) ?>"
                           target="_blank" rel="noopener" class="share-btn share-facebook" title="Share on Facebook">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M18.77 7.46H14.5v-1.9c0-.9.6-1.1 1-1.1h3V.5h-4.33C10.24.5 9.5 3.44 9.5 5.32v2.15h-3v4h3v12h5v-12h3.85l.42-4z"/>
                            </svg>
                        </a>
                        <a href="https://twitter.com/intent/tweet?url=<?= urlencode(\w_env_request_uri()) ?>&text=<?= urlencode($currentPost['title']) ?>"
                           target="_blank" rel="noopener" class="share-btn share-twitter" title="Share on Twitter">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
                            </svg>
                        </a>
                        <a href="https://www.linkedin.com/shareArticle?mini=true&url=<?= urlencode(\w_env_request_uri()) ?>&title=<?= urlencode($currentPost['title']) ?>"
                           target="_blank" rel="noopener" class="share-btn share-linkedin" title="Share on LinkedIn">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
                            </svg>
                        </a>
                        <button class="share-btn share-copy" title="Copy Link" onclick="copyToClipboard()">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"/>
                            </svg>
                        </button>
                    </div>
                </div>
                <?php endif; ?>
                
                <!-- 返回博客列表 -->
                <div class="article-navigation">
                    <a href="/blog" class="btn-back">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M20 11H7.83l5.59-5.59L12 4l-8 8 8 8 1.41-1.41L7.83 13H20v-2z"/>
                        </svg>
                        <?= __('Back to Blog') ?>
                    </a>
                </div>
            </article>
            
            <!-- 侧边栏 -->
            <?php if ($showSidebar): ?>
            <aside class="blog-sidebar">
                <!-- 分类 -->
                <?php if (!empty($blogCategories)): ?>
                <div class="sidebar-widget">
                    <h4 class="widget-title"><?= __('Categories') ?></h4>
                    <ul class="category-list">
                        <?php foreach ($blogCategories as $category): ?>
                        <li>
                            <a href="<?= htmlspecialchars($category['url']) ?>">
                                <?= htmlspecialchars($category['name']) ?>
                            </a>
                        </li>
                        <?php endforeach; ?>
                    </ul>
                </div>
                <?php endif; ?>
                
                <!-- 最近文章 -->
                <?php if (!empty($recentPosts)): ?>
                <div class="sidebar-widget">
                    <h4 class="widget-title"><?= __('Recent Posts') ?></h4>
                    <ul class="recent-posts-list">
                        <?php foreach (array_slice($recentPosts, 0, 5) as $post): ?>
                        <li>
                            <a href="<?= htmlspecialchars($post['url']) ?>">
                                <?php if (!empty($post['cover_image'])): ?>
                                <img src="<?= htmlspecialchars($post['cover_image']) ?>" 
                                     alt="<?= htmlspecialchars($post['title']) ?>"
                                     loading="lazy">
                                <?php endif; ?>
                                <div class="recent-post-info">
                                    <span class="recent-post-title"><?= htmlspecialchars($post['title']) ?></span>
                                    <?php if (!empty($post['published_at'])): ?>
                                    <span class="recent-post-date"><?= date('M d, Y', strtotime($post['published_at'])) ?></span>
                                    <?php endif; ?>
                                </div>
                            </a>
                        </li>
                        <?php endforeach; ?>
                    </ul>
                </div>
                <?php endif; ?>
            </aside>
            <?php endif; ?>
        </div>
        
        <!-- 相关文章 -->
        <?php if ($showRelated && !empty($relatedPosts)): ?>
        <div class="related-posts">
            <h3 class="related-title"><?= __('Related Articles') ?></h3>
            <div class="related-posts-grid">
                <?php foreach (array_slice($relatedPosts, 0, 3) as $post): ?>
                <article class="related-card">
                    <?php if (!empty($post['cover_image'])): ?>
                    <a href="<?= htmlspecialchars($post['url']) ?>" class="related-card-image">
                        <img src="<?= htmlspecialchars($post['cover_image']) ?>" 
                             alt="<?= htmlspecialchars($post['title']) ?>"
                             loading="lazy">
                    </a>
                    <?php endif; ?>
                    <div class="related-card-content">
                        <h4 class="related-card-title">
                            <a href="<?= htmlspecialchars($post['url']) ?>">
                                <?= htmlspecialchars($post['title']) ?>
                            </a>
                        </h4>
                        <?php if (!empty($post['published_at'])): ?>
                        <span class="related-card-date"><?= date('M d, Y', strtotime($post['published_at'])) ?></span>
                        <?php endif; ?>
                    </div>
                </article>
                <?php endforeach; ?>
            </div>
        </div>
        <?php endif; ?>
    </div>
</section>

<script>
function copyToClipboard() {
    navigator.clipboard.writeText(window.location.href).then(function() {
        alert('<?= __('Link copied to clipboard!') ?>');
    });
}
</script>
<?php endif; ?>

<style>
.tpmst-blog-detail {
    position: relative;
    padding: 60px 0 80px;
    background: radial-gradient(1200px 600px at 15% 10%, rgba(154, 36, 193, 0.18), transparent 60%),
        radial-gradient(800px 500px at 85% 20%, rgba(255, 215, 0, 0.12), transparent 55%),
        <?= htmlspecialchars($bgColor) ?>;
    color: <?= htmlspecialchars($textColor) ?>;
    min-height: 60vh;
    width: 100%;
    overflow: hidden;
}

.tpmst-blog-detail::before {
    content: '';
    position: absolute;
    inset: 0;
    background: url('<?= $pokerBg ?>') center top/cover no-repeat;
    opacity: 0.12;
    pointer-events: none;
}

.tpmst-blog-detail::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(7, 4, 16, 0.35) 0%, rgba(7, 4, 16, 0.7) 100%);
    pointer-events: none;
}

.tpmst-blog-detail .tpmst-section-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
}

/* 面包屑 */
.blog-breadcrumb {
    margin-bottom: 30px;
    font-size: 0.9rem;
    opacity: 0.8;
}

.blog-breadcrumb a {
    color: inherit;
    text-decoration: none;
    transition: opacity 0.3s;
}

.blog-breadcrumb a:hover {
    opacity: 1;
    color: <?= htmlspecialchars($primaryColor) ?>;
}

.blog-breadcrumb .separator {
    margin: 0 10px;
}

.blog-breadcrumb .current {
    color: <?= htmlspecialchars($primaryColor) ?>;
}

/* 布局 */
.blog-detail-wrapper {
    display: flex;
    gap: 40px;
    align-items: flex-start;
}

.blog-detail-wrapper.full-width .blog-article {
    max-width: 860px;
    margin: 0 auto;
}

.blog-article {
    flex: 1;
    min-width: 0;
    background: rgba(12, 8, 26, 0.8);
    border: 1px solid rgba(255, 215, 0, 0.14);
    border-radius: 20px;
    padding: 32px;
    box-shadow: 0 18px 45px rgba(8, 5, 20, 0.5);
    backdrop-filter: blur(10px);
}

/* 文章头部 */
.article-header {
    margin-bottom: 30px;
}

.article-title {
    font-size: clamp(2rem, 3.4vw, 2.9rem);
    font-weight: 800;
    line-height: 1.3;
    margin-bottom: 20px;
    color: <?= htmlspecialchars($primaryColor) ?>;
    text-shadow: 0 0 24px rgba(255, 215, 0, 0.25);
}

.article-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
    opacity: 0.75;
    font-size: 0.9rem;
}

.meta-item {
    display: inline-flex;
    align-items: center;
    gap: 6px;
}

.meta-item svg {
    width: 16px;
    height: 16px;
}

/* 封面图 */
.article-cover {
    margin-bottom: 30px;
}

.article-cover img {
    width: 100%;
    height: auto;
    border-radius: 16px;
    object-fit: cover;
    box-shadow: 0 20px 45px rgba(6, 4, 15, 0.6);
}

/* 摘要 */
.article-summary {
    background: rgba(255, 215, 0, 0.08);
    border-left: 4px solid <?= htmlspecialchars($primaryColor) ?>;
    padding: 20px 22px;
    margin-bottom: 30px;
    border-radius: 10px;
    font-size: 1.05rem;
    line-height: 1.7;
}

/* 文章内容 */
.article-content {
    line-height: 1.8;
    font-size: 1.02rem;
}

.article-content h2 {
    font-size: 1.5rem;
    margin: 35px 0 15px;
    color: <?= htmlspecialchars($primaryColor) ?>;
}

.article-content h3 {
    font-size: 1.25rem;
    margin: 30px 0 12px;
    color: <?= htmlspecialchars($textColor) ?>;
}

.article-content p {
    margin-bottom: 16px;
    opacity: 0.9;
}

.article-content ul,
.article-content ol {
    margin: 0 0 16px 20px;
}

.article-content li {
    margin-bottom: 8px;
}

.article-content a {
    color: <?= htmlspecialchars($primaryColor) ?>;
    text-decoration: underline;
}

.article-content blockquote {
    border-left: 4px solid <?= htmlspecialchars($secondaryColor) ?>;
    padding: 15px 20px;
    margin: 20px 0;
    background: rgba(154, 36, 193, 0.12);
    border-radius: 0 8px 8px 0;
}

.article-content table {
    width: 100%;
    border-collapse: collapse;
    margin: 20px 0;
}

.article-content th,
.article-content td {
    padding: 12px 15px;
    text-align: left;
    border: 1px solid rgba(255, 215, 0, 0.12);
}

.article-content th {
    background: rgba(12, 8, 26, 0.9);
    font-weight: 600;
}

/* 标签 */
.article-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 30px;
    padding-top: 20px;
    border-top: 1px solid rgba(255, 215, 0, 0.12);
}

.tags-label {
    font-weight: 600;
    opacity: 0.85;
}

.tag {
    display: inline-block;
    padding: 6px 14px;
    background: rgba(255, 215, 0, 0.15);
    border-radius: 999px;
    font-size: 0.85rem;
    color: <?= htmlspecialchars($textColor) ?>;
    text-decoration: none;
    transition: background 0.3s;
}

.tag:hover {
    background: <?= htmlspecialchars($primaryColor) ?>;
    color: #1a0f3d;
}

/* 分享 */
.article-share {
    display: flex;
    align-items: center;
    gap: 15px;
    margin-top: 25px;
}

.share-label {
    font-weight: 600;
    opacity: 0.85;
}

.share-buttons {
    display: flex;
    gap: 10px;
}

.share-btn {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: rgba(12, 8, 26, 0.7);
    border: 1px solid rgba(255, 215, 0, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: transform 0.3s, background 0.3s;
    color: <?= htmlspecialchars($primaryColor) ?>;
}

.share-btn:hover {
    transform: translateY(-2px);
    background: <?= htmlspecialchars($primaryColor) ?>;
    color: #1a0f3d;
}

/* 导航 */
.article-navigation {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 30px;
    padding-top: 20px;
    border-top: 1px solid rgba(255, 215, 0, 0.12);
}

.btn-back {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: <?= htmlspecialchars($primaryColor) ?>;
    text-decoration: none;
    font-weight: 500;
}

/* 侧边栏 */
.blog-sidebar {
    width: 300px;
    flex-shrink: 0;
}

.sidebar-widget {
    background: rgba(12, 8, 26, 0.78);
    border-radius: 16px;
    padding: 22px;
    margin-bottom: 25px;
    border: 1px solid rgba(255, 215, 0, 0.12);
    box-shadow: 0 16px 35px rgba(8, 5, 20, 0.35);
    backdrop-filter: blur(10px);
}

.widget-title {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 15px;
    padding-bottom: 10px;
    border-bottom: 2px solid <?= htmlspecialchars($primaryColor) ?>;
    color: <?= htmlspecialchars($primaryColor) ?>;
}

.category-list,
.recent-posts-list {
    list-style: none;
    padding: 0;
    margin: 0;
}

.category-list li,
.recent-posts-list li {
    margin-bottom: 12px;
}

.category-list a,
.recent-posts-list a {
    color: inherit;
    text-decoration: none;
    opacity: 0.85;
    transition: opacity 0.3s, padding-left 0.3s;
    display: block;
    padding: 6px 0;
}

.category-list a:hover,
.recent-posts-list a:hover {
    opacity: 1;
    padding-left: 8px;
    color: <?= htmlspecialchars($primaryColor) ?>;
}

.recent-posts-list a {
    display: flex;
    gap: 12px;
    align-items: center;
}

.recent-posts-list img {
    width: 60px;
    height: 60px;
    object-fit: cover;
    border-radius: 8px;
    flex-shrink: 0;
}

.recent-post-info {
    display: flex;
    flex-direction: column;
    gap: 5px;
}

.recent-post-title {
    font-size: 0.9rem;
    font-weight: 500;
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.recent-post-date {
    font-size: 0.75rem;
    opacity: 0.6;
}

/* 相关文章 */
.related-posts {
    margin-top: 50px;
}

.related-title {
    font-size: 1.6rem;
    font-weight: 700;
    margin-bottom: 25px;
    color: <?= htmlspecialchars($primaryColor) ?>;
}

.related-posts-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 20px;
}

.related-card {
    background: rgba(12, 8, 26, 0.78);
    border-radius: 16px;
    overflow: hidden;
    transition: transform 0.3s;
    border: 1px solid rgba(255, 215, 0, 0.12);
    box-shadow: 0 16px 35px rgba(8, 5, 20, 0.35);
    backdrop-filter: blur(10px);
}

.related-card:hover {
    transform: translateY(-5px);
}

.related-card-image {
    display: block;
    position: relative;
    padding-top: 56.25%;
    overflow: hidden;
}

.related-card-image img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.related-card-content {
    padding: 16px;
}

.related-card-title {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 8px;
}

.related-card-title a {
    color: <?= htmlspecialchars($textColor) ?>;
    text-decoration: none;
}

.related-card-title a:hover {
    color: <?= htmlspecialchars($primaryColor) ?>;
}

.related-card-date {
    font-size: 0.8rem;
    opacity: 0.7;
}

/* 空状态 */
.blog-not-found {
    text-align: center;
    padding: 80px 20px;
    opacity: 0.7;
}

.btn-back-to-blog {
    display: inline-block;
    margin-top: 20px;
    padding: 12px 30px;
    background: linear-gradient(120deg, <?= htmlspecialchars($primaryColor) ?>, <?= htmlspecialchars($secondaryColor) ?>);
    color: #1a0f3d;
    text-decoration: none;
    border-radius: 999px;
    font-weight: 700;
    transition: transform 0.3s, box-shadow 0.3s;
}

.btn-back-to-blog:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 24px rgba(251, 191, 36, 0.35);
}

/* 响应式 */
@media (max-width: 992px) {
    .blog-detail-wrapper {
        flex-direction: column;
    }

    .blog-sidebar {
        width: 100%;
    }
}

@media (max-width: 768px) {
    .tpmst-blog-detail {
        padding: 40px 0 60px;
    }

    .tpmst-blog-detail .tpmst-section-inner {
        padding: 0 15px;
    }

    .blog-article {
        padding: 24px;
    }

    .article-title {
        font-size: 2rem;
    }
}
</style>
