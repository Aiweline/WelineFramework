<?php
declare(strict_types=1);
/**
 * 法律类页面默认占位 HTML（批量建站 / 后台 include 用）。
 *
 * 与 {@see \GuoLaiRen\PageBuilder\Service\DefaultLegalPlaceholder} 同源，避免缺失文件导致 Warning。
 *
 * 可选变量（include 前赋值）：
 * @var string $legalType  PageModel::TYPE_* 常量值（亦兼容 $pageType）
 * @var string $legalTitle 页面标题（亦兼容 $title）
 */
$legalType = (string) ($legalType ?? $pageType ?? '');
$legalTitle = (string) ($legalTitle ?? $title ?? '');

echo \GuoLaiRen\PageBuilder\Service\DefaultLegalPlaceholder::toHtml($legalType, $legalTitle);
