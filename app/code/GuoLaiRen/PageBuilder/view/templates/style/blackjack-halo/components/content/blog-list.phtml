<?php
/**
 * Blackjack Halo Blog List Component
 *
 * @fields_start
 * blog.title|博客列表标题|text|Latest Blog Posts
 * blog.subtitle|副标题|text|Stay updated with our latest news and articles
 * blog.posts_per_page|每页显示数量|number|9
 * blog.show_sidebar|显示侧边栏|select:yes=是,no=否|yes
 * blog.show_category|显示分类|select:yes=是,no=否|yes
 * blog.show_date|显示日期|select:yes=是,no=否|yes
 * blog.show_author|显示作者|select:yes=是,no=否|yes
 * blog.card_style|卡片样式|select:default=默认,minimal=简约,featured=特色|default
 * @fields_end
 */

$style = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$blogPosts = $this->getData('blog_posts') ?: [];
$blogCategories = $this->getData('blog_categories') ?: [];
$recentPosts = $this->getData('recent_posts') ?: [];

$getConfig = function ($key, $default = '') use ($style, $componentConfig) {
    if (isset($componentConfig[$key]) && $componentConfig[$key] !== '') {
        return $componentConfig[$key];
    }

    return $style[$key] ?? $default;
};

$title = $getConfig('blog.title', __('Latest Blog Posts'));
$subtitle = $getConfig('blog.subtitle', __('Stay updated with our latest news and articles'));
$showSidebar = $getConfig('blog.show_sidebar', 'yes') === 'yes';
$showCategory = $getConfig('blog.show_category', 'yes') === 'yes';
$showDate = $getConfig('blog.show_date', 'yes') === 'yes';
$showAuthor = $getConfig('blog.show_author', 'yes') === 'yes';
$cardStyle = $getConfig('blog.card_style', 'default');

$primaryColor = $getConfig('color_scheme.primary', '#1f7a5b');
$secondaryColor = $getConfig('color_scheme.secondary', '#d0a03f');
$bgColor = $getConfig('color_scheme.background', '#f7f5ed');
$textColor = $getConfig('color_scheme.text', '#1f312d');

$featuredPost = $blogPosts[0] ?? null;
$remainingPosts = array_slice($blogPosts, 1);
$signalPosts = array_slice($remainingPosts, 0, 2);
$archivePosts = array_slice($remainingPosts, 2);
if (!$featuredPost && !empty($blogPosts)) {
    $archivePosts = $blogPosts;
}
?>

<section class="bkh-blog-list" id="blog">
    <div class="blog-shell">
        <header class="editorial-head">
            <span class="editorial-kicker">Halo Editorial Desk</span>
            <h2><?= htmlspecialchars($title) ?></h2>
            <?php if ($subtitle): ?>
                <p><?= htmlspecialchars($subtitle) ?></p>
            <?php endif; ?>
        </header>

        <?php if (empty($blogPosts)): ?>
            <div class="blog-empty">
                <div class="empty-icon">
                    <svg width="64" height="64" viewBox="0 0 24 24" fill="currentColor" opacity="0.3">
                        <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-5 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"/>
                    </svg>
                </div>
                <p><?= __('There are no blog posts yet. Please check back later!') ?></p>
            </div>
        <?php else: ?>
            <div class="editorial-stage<?= $showSidebar ? ' has-rail' : '' ?>">
                <div class="editorial-main">
                    <?php if ($featuredPost): ?>
                        <div class="lead-lattice<?= empty($signalPosts) ? ' no-signals' : '' ?>">
                            <article class="spotlight-board">
                                <?php if (!empty($featuredPost['cover_image'])): ?>
                                    <a href="<?= htmlspecialchars($featuredPost['url']) ?>" class="spotlight-media">
                                        <img src="<?= htmlspecialchars($featuredPost['cover_image']) ?>" alt="<?= htmlspecialchars($featuredPost['title']) ?>" loading="lazy">
                                    </a>
                                <?php endif; ?>

                                <div class="spotlight-copy">
                                    <div class="story-meta">
                                        <?php if ($showCategory && !empty($featuredPost['category_name'])): ?>
                                            <span class="story-chip"><?= htmlspecialchars($featuredPost['category_name']) ?></span>
                                        <?php endif; ?>
                                        <?php if ($showDate && !empty($featuredPost['published_at'])): ?>
                                            <span class="story-date"><?= date('M d, Y', strtotime($featuredPost['published_at'])) ?></span>
                                        <?php endif; ?>
                                    </div>

                                    <h3>
                                        <a href="<?= htmlspecialchars($featuredPost['url']) ?>"><?= htmlspecialchars($featuredPost['title']) ?></a>
                                    </h3>

                                    <?php if (!empty($featuredPost['summary'])): ?>
                                        <p class="spotlight-summary"><?= htmlspecialchars(mb_substr($featuredPost['summary'], 0, 220)) ?><?= mb_strlen($featuredPost['summary']) > 220 ? '...' : '' ?></p>
                                    <?php endif; ?>

                                    <div class="story-foot">
                                        <?php if ($showAuthor && !empty($featuredPost['author'])): ?>
                                            <span class="story-author"><?= htmlspecialchars($featuredPost['author']) ?></span>
                                        <?php endif; ?>
                                        <a href="<?= htmlspecialchars($featuredPost['url']) ?>" class="story-link">
                                            <?= __('Read Story') ?>
                                            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                                <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                            </svg>
                                        </a>
                                    </div>
                                </div>
                            </article>

                            <?php if (!empty($signalPosts)): ?>
                                <div class="signal-stack">
                                    <?php foreach ($signalPosts as $index => $post): ?>
                                        <article class="signal-card">
                                            <span class="signal-index">0<?= (int) ($index + 1) ?></span>
                                            <div class="story-meta">
                                                <?php if ($showCategory && !empty($post['category_name'])): ?>
                                                    <span class="story-chip"><?= htmlspecialchars($post['category_name']) ?></span>
                                                <?php endif; ?>
                                                <?php if ($showDate && !empty($post['published_at'])): ?>
                                                    <span class="story-date"><?= date('M d, Y', strtotime($post['published_at'])) ?></span>
                                                <?php endif; ?>
                                            </div>
                                            <h3><a href="<?= htmlspecialchars($post['url']) ?>"><?= htmlspecialchars($post['title']) ?></a></h3>
                                            <?php if ($cardStyle !== 'minimal' && !empty($post['summary'])): ?>
                                                <p><?= htmlspecialchars(mb_substr($post['summary'], 0, 120)) ?><?= mb_strlen($post['summary']) > 120 ? '...' : '' ?></p>
                                            <?php endif; ?>
                                            <a href="<?= htmlspecialchars($post['url']) ?>" class="story-link">
                                                <?= __('Open') ?>
                                                <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                                    <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                                </svg>
                                            </a>
                                        </article>
                                    <?php endforeach; ?>
                                </div>
                            <?php endif; ?>
                        </div>
                    <?php endif; ?>

                    <?php if (!empty($archivePosts)): ?>
                        <div class="archive-grid archive-grid-<?= htmlspecialchars(preg_replace('/[^a-z0-9-]+/i', '', (string) $cardStyle)) ?>">
                            <?php foreach ($archivePosts as $index => $post): ?>
                                <?php $wideCard = $cardStyle === 'featured' && $index === 0; ?>
                                <article class="archive-card<?= $wideCard ? ' is-wide' : '' ?>">
                                    <span class="archive-index">0<?= (int) ($index + 3) ?></span>
                                    <?php if (!empty($post['cover_image'])): ?>
                                        <a href="<?= htmlspecialchars($post['url']) ?>" class="archive-media">
                                            <img src="<?= htmlspecialchars($post['cover_image']) ?>" alt="<?= htmlspecialchars($post['title']) ?>" loading="lazy">
                                        </a>
                                    <?php endif; ?>

                                    <div class="archive-copy">
                                        <div class="story-meta">
                                            <?php if ($showCategory && !empty($post['category_name'])): ?>
                                                <span class="story-chip"><?= htmlspecialchars($post['category_name']) ?></span>
                                            <?php endif; ?>
                                            <?php if ($showDate && !empty($post['published_at'])): ?>
                                                <span class="story-date"><?= date('M d, Y', strtotime($post['published_at'])) ?></span>
                                            <?php endif; ?>
                                        </div>

                                        <h3><a href="<?= htmlspecialchars($post['url']) ?>"><?= htmlspecialchars($post['title']) ?></a></h3>

                                        <?php if ($cardStyle !== 'minimal' && !empty($post['summary'])): ?>
                                            <p><?= htmlspecialchars(mb_substr($post['summary'], 0, $wideCard ? 180 : 120)) ?><?= mb_strlen($post['summary']) > ($wideCard ? 180 : 120) ? '...' : '' ?></p>
                                        <?php endif; ?>

                                        <div class="story-foot">
                                            <?php if ($showAuthor && !empty($post['author'])): ?>
                                                <span class="story-author"><?= htmlspecialchars($post['author']) ?></span>
                                            <?php endif; ?>
                                            <a href="<?= htmlspecialchars($post['url']) ?>" class="story-link">
                                                <?= __('Read More') ?>
                                                <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                                    <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                                </svg>
                                            </a>
                                        </div>
                                    </div>
                                </article>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </div>

                <?php if ($showSidebar): ?>
                    <aside class="editorial-rail">
                        <?php if (!empty($blogCategories)): ?>
                            <section class="rail-card">
                                <h4><?= __('Categories') ?></h4>
                                <div class="category-cloud">
                                    <?php foreach ($blogCategories as $category): ?>
                                        <a href="<?= htmlspecialchars($category['url']) ?>" class="category-pill">
                                            <span><?= htmlspecialchars($category['name']) ?></span>
                                            <?php if (isset($category['post_count'])): ?>
                                                <small><?= (int) $category['post_count'] ?></small>
                                            <?php endif; ?>
                                        </a>
                                    <?php endforeach; ?>
                                </div>
                            </section>
                        <?php endif; ?>

                        <?php if (!empty($recentPosts)): ?>
                            <section class="rail-card">
                                <h4><?= __('Recent Posts') ?></h4>
                                <div class="recent-stack">
                                    <?php foreach (array_slice($recentPosts, 0, 5) as $post): ?>
                                        <a href="<?= htmlspecialchars($post['url']) ?>" class="recent-link">
                                            <?php if (!empty($post['cover_image'])): ?>
                                                <img src="<?= htmlspecialchars($post['cover_image']) ?>" alt="<?= htmlspecialchars($post['title']) ?>" loading="lazy">
                                            <?php endif; ?>
                                            <span>
                                                <strong><?= htmlspecialchars($post['title']) ?></strong>
                                                <?php if (!empty($post['published_at'])): ?>
                                                    <small><?= date('M d, Y', strtotime($post['published_at'])) ?></small>
                                                <?php endif; ?>
                                            </span>
                                        </a>
                                    <?php endforeach; ?>
                                </div>
                            </section>
                        <?php endif; ?>
                    </aside>
                <?php endif; ?>
            </div>
        <?php endif; ?>
    </div>
</section>

<style>
.bkh-blog-list {
    position: relative;
    padding: 84px 0;
    background:
        radial-gradient(900px 420px at 12% 8%, rgba(31, 122, 91, 0.14), transparent 68%),
        radial-gradient(780px 360px at 88% 16%, rgba(208, 160, 63, 0.16), transparent 68%),
        linear-gradient(180deg, rgba(255, 255, 255, 0.18) 0%, rgba(255, 255, 255, 0) 28%),
        <?= htmlspecialchars($bgColor) ?>;
    color: <?= htmlspecialchars($textColor) ?>;
    overflow: hidden;
}

.bkh-blog-list::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
        radial-gradient(420px 160px at 18% 18%, rgba(255, 255, 255, 0.28) 0%, rgba(255, 255, 255, 0) 72%),
        radial-gradient(520px 180px at 80% 100%, rgba(208, 160, 63, 0.08) 0%, rgba(208, 160, 63, 0) 72%);
}

.bkh-blog-list .blog-shell {
    max-width: 1220px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
}

.bkh-blog-list .editorial-head {
    max-width: 760px;
    margin-bottom: 1rem;
}

.bkh-blog-list .editorial-kicker,
.bkh-blog-list .story-chip,
.bkh-blog-list .story-date,
.bkh-blog-list .signal-index,
.bkh-blog-list .archive-index {
    display: inline-flex;
    align-items: center;
    width: fit-content;
    border-radius: 999px;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.bkh-blog-list .editorial-kicker {
    padding: 0.42rem 0.84rem;
    background: rgba(31, 122, 91, 0.08);
    border: 1px solid rgba(31, 122, 91, 0.16);
    color: #1f7a5b;
}

.bkh-blog-list .editorial-head h2 {
    margin-top: 1rem;
    color: #1f312d;
    font-size: clamp(2.1rem, 4.3vw, 3.7rem);
    line-height: 1.02;
    letter-spacing: -0.05em;
}

.bkh-blog-list .editorial-head p {
    margin-top: 0.85rem;
    color: #647a70;
    line-height: 1.78;
}

.bkh-blog-list .blog-empty,
.bkh-blog-list .spotlight-board,
.bkh-blog-list .signal-card,
.bkh-blog-list .archive-card,
.bkh-blog-list .rail-card {
    position: relative;
    border-radius: 28px;
    border: 1px solid rgba(32, 53, 45, 0.08);
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.95) 0%, rgba(247, 250, 244, 0.88) 100%);
    box-shadow: 0 24px 48px rgba(31, 82, 63, 0.08);
    overflow: hidden;
}

.bkh-blog-list .blog-empty,
.bkh-blog-list .signal-card,
.bkh-blog-list .archive-card,
.bkh-blog-list .rail-card {
    padding: 1.15rem;
}

.bkh-blog-list .blog-empty {
    text-align: center;
}

.bkh-blog-list .empty-icon {
    display: inline-flex;
    color: rgba(31, 122, 91, 0.32);
}

.bkh-blog-list .editorial-stage {
    display: grid;
    gap: 1rem;
}

.bkh-blog-list .editorial-stage.has-rail {
    grid-template-columns: minmax(0, 1fr) 300px;
    align-items: start;
}

.bkh-blog-list .editorial-main,
.bkh-blog-list .editorial-rail,
.bkh-blog-list .signal-stack,
.bkh-blog-list .recent-stack {
    display: grid;
    gap: 1rem;
}

.bkh-blog-list .lead-lattice {
    display: grid;
    grid-template-columns: minmax(0, 1.16fr) minmax(260px, 0.84fr);
    gap: 1rem;
}

.bkh-blog-list .lead-lattice.no-signals {
    grid-template-columns: 1fr;
}

.bkh-blog-list .spotlight-board {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(280px, 0.94fr);
}

.bkh-blog-list .spotlight-media,
.bkh-blog-list .archive-media,
.bkh-blog-list .recent-link img {
    display: block;
}

.bkh-blog-list .spotlight-media {
    min-height: 100%;
    background: rgba(31, 122, 91, 0.08);
}

.bkh-blog-list .spotlight-media img,
.bkh-blog-list .archive-media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

.bkh-blog-list .spotlight-copy,
.bkh-blog-list .archive-copy {
    display: grid;
    align-content: start;
    gap: 0.9rem;
}

.bkh-blog-list .spotlight-copy {
    padding: 1.25rem 1.2rem;
}

.bkh-blog-list .story-meta,
.bkh-blog-list .story-foot {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.55rem;
}

.bkh-blog-list .story-chip {
    padding: 0.34rem 0.74rem;
    background: linear-gradient(90deg, <?= htmlspecialchars($primaryColor) ?> 0%, <?= htmlspecialchars($secondaryColor) ?> 100%);
    color: #f7f5ed;
}

.bkh-blog-list .story-date,
.bkh-blog-list .signal-index,
.bkh-blog-list .archive-index {
    padding: 0.34rem 0.74rem;
    background: rgba(31, 122, 91, 0.08);
    border: 1px solid rgba(31, 122, 91, 0.12);
    color: #295347;
}

.bkh-blog-list .spotlight-copy h3,
.bkh-blog-list .signal-card h3,
.bkh-blog-list .archive-copy h3 {
    color: #1f312d;
    line-height: 1.15;
}

.bkh-blog-list .spotlight-copy h3 {
    font-size: clamp(1.8rem, 3vw, 2.7rem);
}

.bkh-blog-list .signal-card h3,
.bkh-blog-list .archive-copy h3 {
    font-size: 1.16rem;
}

.bkh-blog-list .spotlight-copy a,
.bkh-blog-list .signal-card a,
.bkh-blog-list .archive-copy a,
.bkh-blog-list .rail-card a {
    color: inherit;
    text-decoration: none;
}

.bkh-blog-list .spotlight-summary,
.bkh-blog-list .signal-card p,
.bkh-blog-list .archive-copy p {
    color: #667c72;
    line-height: 1.78;
}

.bkh-blog-list .story-author {
    color: #6a8076;
    font-size: 0.86rem;
    font-weight: 700;
}

.bkh-blog-list .story-link {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    color: <?= htmlspecialchars($secondaryColor) ?>;
    font-weight: 800;
}

.bkh-blog-list .archive-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
}

.bkh-blog-list .archive-card {
    display: grid;
    gap: 0.95rem;
}

.bkh-blog-list .archive-card.is-wide {
    grid-column: span 2;
    grid-template-columns: minmax(0, 0.92fr) minmax(240px, 1.08fr);
    align-items: stretch;
}

.bkh-blog-list .archive-card.is-wide .archive-copy {
    padding: 0.05rem 0;
}

.bkh-blog-list .archive-media {
    aspect-ratio: 16 / 10;
    border-radius: 22px;
    overflow: hidden;
    background: rgba(31, 122, 91, 0.08);
}

.bkh-blog-list .archive-card.is-wide .archive-media {
    aspect-ratio: auto;
    min-height: 100%;
}

.bkh-blog-list .rail-card h4 {
    color: #1f312d;
    font-size: 1rem;
}

.bkh-blog-list .category-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 0.65rem;
    margin-top: 0.9rem;
}

.bkh-blog-list .category-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.5rem 0.78rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.82);
    border: 1px solid rgba(32, 53, 45, 0.08);
    color: #264b3f;
    font-size: 0.84rem;
    font-weight: 700;
}

.bkh-blog-list .category-pill small {
    color: #6d8378;
    font-size: 0.76rem;
}

.bkh-blog-list .recent-link {
    display: grid;
    grid-template-columns: 72px minmax(0, 1fr);
    gap: 0.8rem;
    align-items: center;
}

.bkh-blog-list .recent-link img {
    width: 72px;
    height: 72px;
    border-radius: 18px;
    object-fit: cover;
}

.bkh-blog-list .recent-link strong {
    display: block;
    color: #1f312d;
    line-height: 1.45;
}

.bkh-blog-list .recent-link small {
    display: block;
    margin-top: 0.28rem;
    color: #6b8177;
    font-size: 0.8rem;
}

@media (max-width: 1100px) {
    .bkh-blog-list .editorial-stage.has-rail,
    .bkh-blog-list .lead-lattice,
    .bkh-blog-list .spotlight-board {
        grid-template-columns: 1fr;
    }

    .bkh-blog-list .editorial-rail {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }
}

@media (max-width: 767px) {
    .bkh-blog-list {
        padding: 56px 0;
    }

    .bkh-blog-list .blog-shell {
        padding: 0 0.45rem;
    }

    .bkh-blog-list .editorial-head,
    .bkh-blog-list .editorial-main,
    .bkh-blog-list .editorial-rail,
    .bkh-blog-list .signal-stack,
    .bkh-blog-list .archive-grid {
        gap: 0.9rem;
    }

    .bkh-blog-list .blog-empty,
    .bkh-blog-list .spotlight-board,
    .bkh-blog-list .signal-card,
    .bkh-blog-list .archive-card,
    .bkh-blog-list .rail-card {
        border-radius: 24px;
    }

    .bkh-blog-list .spotlight-copy,
    .bkh-blog-list .signal-card,
    .bkh-blog-list .archive-card,
    .bkh-blog-list .rail-card {
        padding: 1rem;
    }

    .bkh-blog-list .archive-grid,
    .bkh-blog-list .editorial-rail {
        grid-template-columns: 1fr;
    }

    .bkh-blog-list .archive-card.is-wide {
        grid-column: auto;
        grid-template-columns: 1fr;
    }

    .bkh-blog-list .archive-media,
    .bkh-blog-list .archive-card.is-wide .archive-media {
        aspect-ratio: 16 / 10;
    }
}
</style>
