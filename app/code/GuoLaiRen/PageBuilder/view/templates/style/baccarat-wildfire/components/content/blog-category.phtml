<?php
/**
 * Baccarat Wildfire Blog Category Component
 *
 * @fields_start
 * blog.title|分类标题|text|Category Posts
 * blog.subtitle|副标题|text|Browse articles by category
 * blog.posts_per_page|每页显示数量|number|9
 * blog.show_sidebar|显示侧边栏|select:yes=是,no=否|yes
 * blog.show_category|显示分类|select:yes=是,no=否|yes
 * blog.show_date|显示日期|select:yes=是,no=否|yes
 * blog.show_author|显示作者|select:yes=是,no=否|yes
 * blog.card_style|卡片样式|select:default=默认,minimal=简约,featured=特色|default
 * @fields_end
 */

$style = $this->getData('style') ?: $this->getData('style_settings') ?: [];
$componentConfig = $this->getData('component_config') ?: [];
$blogPosts = $this->getData('blog_posts') ?: [];
$blogCategories = $this->getData('blog_categories') ?: [];
$currentCategory = $this->getData('current_category') ?: [];
$recentPosts = $this->getData('recent_posts') ?: [];

$getConfig = function($key, $default = '') use ($style, $componentConfig) {
    if (isset($componentConfig[$key]) && $componentConfig[$key] !== '') {
        return $componentConfig[$key];
    }
    return $style[$key] ?? $default;
};

$title = $getConfig('blog.title', $currentCategory['name'] ?? __('Category Posts'));
$subtitle = $getConfig('blog.subtitle', __('Browse articles by category'));
$showSidebar = $getConfig('blog.show_sidebar', 'yes') === 'yes';
$showCategory = $getConfig('blog.show_category', 'yes') === 'yes';
$showDate = $getConfig('blog.show_date', 'yes') === 'yes';
$showAuthor = $getConfig('blog.show_author', 'yes') === 'yes';

$primaryColor = $getConfig('color_scheme.primary', '#f26b4f');
$secondaryColor = $getConfig('color_scheme.secondary', '#f3b53f');
$bgColor = $getConfig('color_scheme.background', '#fff7ed');
$textColor = $getConfig('color_scheme.text', '#3f2418');
$pokerBg = $this->fetchTemplateStatic('GuoLaiRen_PageBuilder::style/baccarat-wildfire/asset/img/poker-header-bg.svg');

$spotlightPost = $blogPosts[0] ?? null;
$streamPosts = array_slice($blogPosts, 1);
?>

<section class="bwf-blog-category" id="blog-category">
    <div class="category-shell">
        <header class="category-masthead">
            <?php if (!empty($currentCategory['name'])): ?>
                <span class="category-chip"><?= htmlspecialchars($currentCategory['name']) ?></span>
            <?php endif; ?>
            <h2><?= htmlspecialchars($title) ?></h2>
            <?php if ($subtitle): ?>
                <p class="category-subtitle"><?= htmlspecialchars($subtitle) ?></p>
            <?php endif; ?>
            <?php if (!empty($currentCategory['description'])): ?>
                <p class="category-description"><?= htmlspecialchars($currentCategory['description']) ?></p>
            <?php endif; ?>
        </header>

        <?php if (empty($blogPosts)): ?>
            <div class="category-empty">
                <div class="empty-icon">
                    <svg width="64" height="64" viewBox="0 0 24 24" fill="currentColor" opacity="0.3">
                        <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-5 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"/>
                    </svg>
                </div>
                <p><?= __('该分类下暂时还没有文章，稍后再来看看吧～') ?></p>
                <a href="/blog" class="back-link"><?= __('返回博客列表') ?></a>
            </div>
        <?php else: ?>
            <?php if ($spotlightPost): ?>
                <article class="spotlight-story">
                    <div class="spotlight-copy">
                        <span class="spotlight-kicker">Category Focus</span>
                        <h3>
                            <a href="<?= htmlspecialchars($spotlightPost['url']) ?>"><?= htmlspecialchars($spotlightPost['title']) ?></a>
                        </h3>
                        <div class="spotlight-meta">
                            <?php if ($showCategory && !empty($spotlightPost['category_name'])): ?>
                                <span><?= htmlspecialchars($spotlightPost['category_name']) ?></span>
                            <?php endif; ?>
                            <?php if ($showDate && !empty($spotlightPost['published_at'])): ?>
                                <span><?= date('M d, Y', strtotime($spotlightPost['published_at'])) ?></span>
                            <?php endif; ?>
                            <?php if ($showAuthor && !empty($spotlightPost['author'])): ?>
                                <span><?= htmlspecialchars($spotlightPost['author']) ?></span>
                            <?php endif; ?>
                        </div>
                        <?php if (!empty($spotlightPost['summary'])): ?>
                            <p><?= htmlspecialchars(mb_substr($spotlightPost['summary'], 0, 220)) ?><?= mb_strlen($spotlightPost['summary']) > 220 ? '...' : '' ?></p>
                        <?php endif; ?>
                        <a href="<?= htmlspecialchars($spotlightPost['url']) ?>" class="story-action">
                            <?= __('Read Article') ?>
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                            </svg>
                        </a>
                    </div>

                    <?php if (!empty($spotlightPost['cover_image'])): ?>
                        <a href="<?= htmlspecialchars($spotlightPost['url']) ?>" class="spotlight-media">
                            <img src="<?= htmlspecialchars($spotlightPost['cover_image']) ?>" alt="<?= htmlspecialchars($spotlightPost['title']) ?>" loading="lazy">
                        </a>
                    <?php endif; ?>
                </article>
            <?php endif; ?>

            <div class="category-stream<?= $showSidebar ? ' has-rail' : '' ?>">
                <div class="stream-list">
                    <?php foreach ($streamPosts as $post): ?>
                        <article class="stream-card">
                            <?php if (!empty($post['cover_image'])): ?>
                                <a href="<?= htmlspecialchars($post['url']) ?>" class="stream-media">
                                    <img src="<?= htmlspecialchars($post['cover_image']) ?>" alt="<?= htmlspecialchars($post['title']) ?>" loading="lazy">
                                </a>
                            <?php endif; ?>

                            <div class="stream-copy">
                                <div class="stream-meta">
                                    <?php if ($showCategory && !empty($post['category_name'])): ?>
                                        <span class="stream-chip"><?= htmlspecialchars($post['category_name']) ?></span>
                                    <?php endif; ?>
                                    <?php if ($showDate && !empty($post['published_at'])): ?>
                                        <span class="stream-date"><?= date('M d, Y', strtotime($post['published_at'])) ?></span>
                                    <?php endif; ?>
                                </div>

                                <h3><a href="<?= htmlspecialchars($post['url']) ?>"><?= htmlspecialchars($post['title']) ?></a></h3>

                                <?php if (!empty($post['summary'])): ?>
                                    <p><?= htmlspecialchars(mb_substr($post['summary'], 0, 170)) ?><?= mb_strlen($post['summary']) > 170 ? '...' : '' ?></p>
                                <?php endif; ?>

                                <div class="stream-foot">
                                    <?php if ($showAuthor && !empty($post['author'])): ?>
                                        <span class="stream-author"><?= htmlspecialchars($post['author']) ?></span>
                                    <?php endif; ?>
                                    <a href="<?= htmlspecialchars($post['url']) ?>" class="story-action">
                                        <?= __('Read More') ?>
                                        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                                            <path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/>
                                        </svg>
                                    </a>
                                </div>
                            </div>
                        </article>
                    <?php endforeach; ?>
                </div>

                <?php if ($showSidebar): ?>
                    <aside class="stream-rail">
                        <?php if (!empty($blogCategories)): ?>
                            <section class="rail-card">
                                <h4><?= __('Categories') ?></h4>
                                <div class="category-list">
                                    <?php foreach ($blogCategories as $category): ?>
                                        <?php $active = !empty($currentCategory['id']) && isset($category['id']) && $category['id'] == $currentCategory['id']; ?>
                                        <a href="<?= htmlspecialchars($category['url']) ?>" class="category-entry<?= $active ? ' is-active' : '' ?>">
                                            <span><?= htmlspecialchars($category['name']) ?></span>
                                            <?php if (isset($category['post_count'])): ?>
                                                <small><?= (int)$category['post_count'] ?></small>
                                            <?php endif; ?>
                                        </a>
                                    <?php endforeach; ?>
                                </div>
                            </section>
                        <?php endif; ?>

                        <?php if (!empty($recentPosts)): ?>
                            <section class="rail-card">
                                <h4><?= __('Recent Posts') ?></h4>
                                <div class="recent-stack">
                                    <?php foreach (array_slice($recentPosts, 0, 5) as $post): ?>
                                        <a href="<?= htmlspecialchars($post['url']) ?>" class="recent-link">
                                            <?php if (!empty($post['cover_image'])): ?>
                                                <img src="<?= htmlspecialchars($post['cover_image']) ?>" alt="<?= htmlspecialchars($post['title']) ?>" loading="lazy">
                                            <?php endif; ?>
                                            <span>
                                                <strong><?= htmlspecialchars($post['title']) ?></strong>
                                                <?php if (!empty($post['published_at'])): ?>
                                                    <small><?= date('M d, Y', strtotime($post['published_at'])) ?></small>
                                                <?php endif; ?>
                                            </span>
                                        </a>
                                    <?php endforeach; ?>
                                </div>
                            </section>
                        <?php endif; ?>
                    </aside>
                <?php endif; ?>
            </div>
        <?php endif; ?>
    </div>
</section>

<style>
.bwf-blog-category {
    position: relative;
    padding: 84px 0;
    overflow: hidden;
    background:
        radial-gradient(780px 360px at 8% 0%, rgba(242,107,79,0.16), transparent 68%),
        radial-gradient(720px 320px at 92% 16%, rgba(8,231,216,0.12), transparent 64%),
        linear-gradient(180deg, #fffaf2 0%, <?= htmlspecialchars($bgColor) ?> 100%);
    color: <?= htmlspecialchars($textColor) ?>;
}

.bwf-blog-category::before {
    content: '';
    position: absolute;
    inset: 0;
    background: url('<?= $pokerBg ?>') center top/cover no-repeat;
    opacity: 0.08;
    pointer-events: none;
}

.bwf-blog-category .category-shell {
    width: min(1260px, calc(100% - 1.2rem));
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

.bwf-blog-category .category-masthead {
    max-width: 42rem;
}

.bwf-blog-category .category-chip,
.bwf-blog-category .stream-chip,
.bwf-blog-category .stream-date,
.bwf-blog-category .stream-author,
.bwf-blog-category .spotlight-meta span {
    display: inline-flex;
    align-items: center;
    min-height: 30px;
    padding: 0.34rem 0.72rem;
    border-radius: 999px;
    font-size: 0.74rem;
}

.bwf-blog-category .category-chip,
.bwf-blog-category .stream-chip {
    background: rgba(242,107,79,0.12);
    color: #b24d34;
    font-weight: 800;
}

.bwf-blog-category .category-masthead h2 {
    margin-top: 1rem;
    font-family: 'Outfit', 'Manrope', sans-serif;
    font-size: clamp(2.4rem, 5vw, 4.4rem);
    line-height: 0.94;
    letter-spacing: -0.07em;
}

.bwf-blog-category .category-subtitle,
.bwf-blog-category .category-description {
    margin-top: 0.9rem;
    color: rgba(63,36,24,0.74);
    line-height: 1.84;
}

.bwf-blog-category .category-empty,
.bwf-blog-category .spotlight-story,
.bwf-blog-category .stream-card,
.bwf-blog-category .rail-card {
    position: relative;
    border: 1px solid rgba(63,36,24,0.08);
    box-shadow: 0 24px 48px rgba(98,50,32,0.08);
}

.bwf-blog-category .category-empty {
    margin-top: 1.2rem;
    padding: 2rem 1.2rem;
    border-radius: 30px;
    text-align: center;
    background: linear-gradient(180deg, rgba(255,255,255,0.94) 0%, rgba(255,244,235,0.92) 100%);
}

.bwf-blog-category .back-link,
.bwf-blog-category .story-action {
    display: inline-flex;
    align-items: center;
    gap: 0.42rem;
    color: #b24d34;
    font-weight: 800;
    text-decoration: none;
}

.bwf-blog-category .spotlight-story {
    margin-top: 1.2rem;
    display: grid;
    grid-template-columns: minmax(0, 0.95fr) minmax(320px, 1.05fr);
    gap: 1rem;
    padding: 1rem;
    border-radius: 34px;
    background: linear-gradient(160deg, rgba(255,255,255,0.96) 0%, rgba(255,244,235,0.92) 100%);
}

.bwf-blog-category .spotlight-copy {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 0.4rem;
}

.bwf-blog-category .spotlight-kicker {
    display: inline-flex;
    min-height: 32px;
    align-items: center;
    padding: 0.38rem 0.82rem;
    border-radius: 999px;
    background: rgba(255,255,255,0.74);
    color: #b24d34;
    font-size: 0.74rem;
    font-weight: 900;
    letter-spacing: 0.1em;
    text-transform: uppercase;
}

.bwf-blog-category .spotlight-copy h3,
.bwf-blog-category .stream-copy h3 {
    margin-top: 1rem;
    font-family: 'Outfit', 'Manrope', sans-serif;
    line-height: 1.02;
}

.bwf-blog-category .spotlight-copy h3 {
    font-size: clamp(1.9rem, 3vw, 2.9rem);
    letter-spacing: -0.05em;
}

.bwf-blog-category .spotlight-copy h3 a,
.bwf-blog-category .stream-copy h3 a,
.bwf-blog-category .recent-link strong {
    color: inherit;
    text-decoration: none;
}

.bwf-blog-category .spotlight-meta,
.bwf-blog-category .stream-meta,
.bwf-blog-category .stream-foot {
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem;
    align-items: center;
}

.bwf-blog-category .spotlight-meta,
.bwf-blog-category .stream-meta,
.bwf-blog-category .stream-copy p {
    margin-top: 0.9rem;
}

.bwf-blog-category .spotlight-meta span,
.bwf-blog-category .stream-date,
.bwf-blog-category .stream-author {
    background: rgba(35,21,27,0.05);
    color: rgba(63,36,24,0.72);
}

.bwf-blog-category .spotlight-copy p,
.bwf-blog-category .stream-copy p {
    color: rgba(63,36,24,0.74);
    line-height: 1.82;
}

.bwf-blog-category .spotlight-media,
.bwf-blog-category .stream-media {
    display: block;
    overflow: hidden;
}

.bwf-blog-category .spotlight-media {
    border-radius: 28px;
}

.bwf-blog-category .spotlight-media img,
.bwf-blog-category .stream-media img,
.bwf-blog-category .recent-link img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

.bwf-blog-category .category-stream {
    margin-top: 1.2rem;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(260px, 300px);
    gap: 1rem;
}

.bwf-blog-category .category-stream:not(.has-rail) {
    grid-template-columns: 1fr;
}

.bwf-blog-category .stream-list {
    display: grid;
    gap: 1rem;
}

.bwf-blog-category .stream-card {
    display: grid;
    grid-template-columns: 240px minmax(0, 1fr);
    gap: 1rem;
    padding: 1rem;
    border-radius: 28px;
    background: linear-gradient(180deg, rgba(255,255,255,0.96) 0%, rgba(255,247,240,0.9) 100%);
}

.bwf-blog-category .stream-media {
    border-radius: 22px;
}

.bwf-blog-category .stream-copy {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 0.2rem 0.1rem;
}

.bwf-blog-category .stream-copy h3 {
    font-size: 1.46rem;
    letter-spacing: -0.04em;
}

.bwf-blog-category .stream-rail {
    display: grid;
    gap: 0.9rem;
}

.bwf-blog-category .rail-card {
    padding: 1rem;
    border-radius: 24px;
    background: linear-gradient(180deg, rgba(255,255,255,0.95) 0%, rgba(255,244,235,0.92) 100%);
}

.bwf-blog-category .rail-card h4 {
    font-family: 'Outfit', 'Manrope', sans-serif;
    font-size: 1.2rem;
}

.bwf-blog-category .category-list,
.bwf-blog-category .recent-stack {
    margin-top: 0.82rem;
    display: grid;
    gap: 0.66rem;
}

.bwf-blog-category .category-entry {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.8rem;
    min-height: 42px;
    padding: 0.56rem 0.74rem;
    border-radius: 16px;
    text-decoration: none;
    color: #3b2426;
    background: rgba(35,21,27,0.05);
}

.bwf-blog-category .category-entry.is-active {
    background: linear-gradient(135deg, rgba(242,107,79,0.14) 0%, rgba(255,179,71,0.18) 100%);
}

.bwf-blog-category .recent-link {
    display: grid;
    grid-template-columns: 72px minmax(0, 1fr);
    gap: 0.7rem;
    text-decoration: none;
    color: #3b2426;
}

.bwf-blog-category .recent-link img {
    border-radius: 16px;
    aspect-ratio: 1 / 1;
}

.bwf-blog-category .recent-link span {
    display: grid;
    gap: 0.28rem;
}

.bwf-blog-category .recent-link small {
    color: rgba(63,36,24,0.64);
}

@media (max-width: 980px) {
    .bwf-blog-category .spotlight-story,
    .bwf-blog-category .category-stream,
    .bwf-blog-category .stream-card {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 640px) {
    .bwf-blog-category {
        padding: 56px 0;
    }

    .bwf-blog-category .category-shell {
        width: min(100% - 0.9rem, 100%);
    }

    .bwf-blog-category .spotlight-story,
    .bwf-blog-category .stream-card,
    .bwf-blog-category .rail-card,
    .bwf-blog-category .category-empty {
        border-radius: 24px;
    }
}
</style>
