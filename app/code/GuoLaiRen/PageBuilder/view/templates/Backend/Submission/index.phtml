<?php
/**@var \Weline\Framework\View\Template $this */
?>
<w:template>Weline_Admin::Backend/page-layout/main-content-before.phtml</w:template>

<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0">
                <?= $this->getData('page_title') ?: __('表单提交管理') ?>
            </h4>

            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item">
                        <a href="<?= $this->getBackendUrl('*/backend/page/index') ?>">
                            <?= $this->getData('breadcrumb_parent') ?: __('页面构建器') ?>
                        </a>
                    </li>
                    <li class="breadcrumb-item active">
                        <?= $this->getData('breadcrumb_current') ?: __('表单提交') ?>
                    </li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- start page message -->
<div class='row'>
    <template>Weline_Component::message.phtml</template>
</div>
<!-- end message -->

<!-- 筛选表单 -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <form action="<?= $this->getBackendUrl('*/backend/submission/index') ?>" method="get" class="row g-3">
                    <div class="col-md-3">
                        <label class="form-label"><?= __('搜索（邮箱/电话）') ?></label>
                        <input type="text" class="form-control" name="search" 
                               value="<?= htmlspecialchars($this->getData('search')) ?>"
                               placeholder="<?= __('输入邮箱或电话') ?>">
                    </div>
                    
                    <div class="col-md-2">
                        <label class="form-label"><?= __('页面') ?></label>
                        <select class="form-select" name="page_id">
                            <option value=""><?= __('全部页面') ?></option>
                            <?php foreach ($this->getData('pages') as $page): ?>
                                <option value="<?= $page->getId() ?>" 
                                        <?= ($this->getData('page_id') == $page->getId()) ? 'selected' : '' ?>>
                                    <?= htmlspecialchars($page->getData('name')) ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                    
                    <div class="col-md-2">
                        <label class="form-label"><?= __('开始日期') ?></label>
                        <input type="date" class="form-control" name="start_date" 
                               value="<?= htmlspecialchars($this->getData('start_date')) ?>">
                    </div>
                    
                    <div class="col-md-2">
                        <label class="form-label"><?= __('结束日期') ?></label>
                        <input type="date" class="form-control" name="end_date" 
                               value="<?= htmlspecialchars($this->getData('end_date')) ?>">
                    </div>
                    
                    <?php if (!empty($this->getData('extra_field_keys'))): ?>
                        <div class="col-md-2">
                            <label class="form-label"><?= __('额外字段') ?></label>
                            <select class="form-select" name="extra_field_key">
                                <option value=""><?= __('选择字段') ?></option>
                                <?php foreach ($this->getData('extra_field_keys') as $key): ?>
                                    <option value="<?= htmlspecialchars($key) ?>"
                                            <?= ($this->getData('extra_field_key') == $key) ? 'selected' : '' ?>>
                                        <?= htmlspecialchars($key) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        
                        <div class="col-md-2">
                            <label class="form-label"><?= __('字段值') ?></label>
                            <input type="text" class="form-control" name="extra_field_value" 
                                   value="<?= htmlspecialchars($this->getData('extra_field_value')) ?>"
                                   placeholder="<?= __('字段值') ?>">
                        </div>
                    <?php endif; ?>
                    
                    <div class="col-md-12">
                        <button type="submit" class="btn btn-primary">
                            <i class="mdi mdi-magnify"></i> <?= __('筛选') ?>
                        </button>
                        <a href="<?= $this->getBackendUrl('*/backend/submission/index') ?>" class="btn btn-secondary">
                            <i class="mdi mdi-refresh"></i> <?= __('重置') ?>
                        </a>
                        <acl source="GuoLaiRen_PageBuilder::form_submissions_export">
                            <a href="<?= $this->getBackendUrl('*/backend/submission/export') ?>" class="btn btn-success">
                                <i class="mdi mdi-download"></i> <?= __('导出CSV') ?>
                            </a>
                        </acl>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- 提交列表 -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">
                    <i class="mdi mdi-format-list-bulleted"></i> <?= __('提交记录列表') ?>
                </h4>
                
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead>
                            <tr>
                                <th><?= __('ID') ?></th>
                                <th><?= __('页面') ?></th>
                                <th><?= __('邮箱') ?></th>
                                <th><?= __('电话') ?></th>
                                <th><?= __('IP地址') ?></th>
                                <th><?= __('提交时间') ?></th>
                                <th><?= __('操作') ?></th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($this->getData('submissions') as $submission): ?>
                                <tr>
                                    <td><?= $submission->getId() ?></td>
                                    <td>
                                        <?php 
                                        $pageId = $submission->getData('page_id');
                                        if ($pageId) {
                                            foreach ($this->getData('pages') as $page) {
                                                if ($page->getId() == $pageId) {
                                                    echo htmlspecialchars($page->getData('name'));
                                                    break;
                                                }
                                            }
                                        } else {
                                            echo __('无');
                                        }
                                        ?>
                                    </td>
                                    <td><?= htmlspecialchars($submission->getData('email') ?: '-') ?></td>
                                    <td><?= htmlspecialchars($submission->getData('phone') ?: '-') ?></td>
                                    <td><?= htmlspecialchars($submission->getData('ip_address') ?: '-') ?></td>
                                    <td><?= htmlspecialchars($submission->getData('submitted_at')) ?></td>
                                    <td>
                                        <acl source="GuoLaiRen_PageBuilder::form_submissions_view">
                                            <a href="<?= $this->getBackendUrl('*/backend/submission/view', ['id' => $submission->getId()]) ?>" 
                                               class="btn btn-sm btn-soft-info" title="<?= __('查看详情') ?>">
                                                <i class="mdi mdi-eye"></i>
                                            </a>
                                        </acl>
                                        <acl source="GuoLaiRen_PageBuilder::form_submissions_delete">
                                            <form action="<?= $this->getBackendUrl('*/backend/submission/delete') ?>" 
                                                  method="post" style="display: inline-block;"
                                                  onsubmit="return confirm('<?= __('确定要删除这条记录吗？') ?>');">
                                                <input type="hidden" name="form_key" value="<?= $this->getFormKey() ?>">
                                                <input type="hidden" name="id" value="<?= $submission->getId() ?>">
                                                <button type="submit" class="btn btn-sm btn-soft-danger" title="<?= __('删除') ?>">
                                                    <i class="mdi mdi-delete"></i>
                                                </button>
                                            </form>
                                        </acl>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                            
                            <?php if (empty($this->getData('submissions'))): ?>
                                <tr>
                                    <td colspan="7" class="text-center text-muted">
                                        <?= __('暂无提交记录') ?>
                                    </td>
                                </tr>
                            <?php endif; ?>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<w:template>Weline_Admin::Backend/page-layout/main-content-after.phtml</w:template>

