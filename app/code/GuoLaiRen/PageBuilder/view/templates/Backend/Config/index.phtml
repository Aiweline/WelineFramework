<?php
/**
 * PageBuilder 配置页面 - 内容模板
 * 由布局系统自动包裹
 *
 * @var \Weline\Framework\View\Template $this
 * @var string $ai_enabled
 * @var string $i18n_enabled
 */
$aiEnabled = $ai_enabled ?? '0';
$i18nEnabled = $i18n_enabled ?? '0';
?>

<style>
/* ============================================
   组件：PageBuilder Config
   模块：GuoLaiRen_PageBuilder
   前缀：pagebuilder-config-
   ============================================ */
.pagebuilder-config-card {
    margin-bottom: 1.5rem;
    background: var(--backend-color-card-bg, #fff);
    border: 1px solid var(--backend-color-border-default, #dee2e6);
    border-radius: var(--backend-border-radius-md, 8px);
}

.pagebuilder-config-card .card-header {
    background-color: var(--backend-color-bg-secondary, #f8f9fa);
    border-bottom: 1px solid var(--backend-color-border-default, #dee2e6);
}

.pagebuilder-config-card .card-header h5 {
    margin: 0;
    font-weight: 600;
    color: var(--backend-color-text-primary, #212529);
}

.pagebuilder-config-card .card-body {
    background: var(--backend-color-card-bg, #fff);
    color: var(--backend-color-text-primary, #212529);
}

.pagebuilder-config-description {
    color: var(--backend-color-text-secondary, #6c757d);
    font-size: 0.875rem;
    margin-top: 0.5rem;
}

/* 表单元素适配主题 */
.pagebuilder-config-card .form-check-label {
    color: var(--backend-color-text-primary, #212529);
}

.pagebuilder-config-card hr {
    border-color: var(--backend-color-border-light, #e9ecef);
    opacity: 1;
}

/* SEO 管理卡片样式 */
.pagebuilder-seo-card {
    background: var(--backend-color-card-bg, #fff);
    border: 1px solid var(--backend-color-primary, #556ee6);
    border-radius: var(--backend-border-radius-md, 8px);
}

.pagebuilder-seo-card .card-body {
    background: var(--backend-color-card-bg, #fff);
}

.pagebuilder-seo-card .card-title {
    color: var(--backend-color-text-primary, #212529);
}

.pagebuilder-seo-card .text-muted {
    color: var(--backend-color-text-secondary, #6c757d) !important;
}

.pagebuilder-seo-card .alert-info {
    background: var(--backend-color-info-bg-subtle, rgba(80, 165, 241, 0.1));
    border-color: var(--backend-color-info, #50a5f1);
    color: var(--backend-color-text-primary, #212529);
}

.pagebuilder-seo-card .alert-info ul {
    color: var(--backend-color-text-secondary, #6c757d);
}

/* 页面标题适配 */
.page-title-box h4 {
    color: var(--backend-color-text-primary, #212529);
}
</style>

<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0 font-size-18"><lang>页面构建器配置</lang></h4>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- 消息提示区域 -->
<div class='row'>
    <div class="col-12">
        <template>Weline_Component::message.phtml</template>
        <div id="pagebuilder-config-message-container"></div>
    </div>
</div>

<!-- 配置表单 -->
<div class="row">
    <div class="col-xl-12">
        <div class="card pagebuilder-config-card">
            <div class="card-header">
                <h5><i class="mdi mdi-cog me-2"></i><lang>功能配置</lang></h5>
            </div>
            <div class="card-body">
                <form id="pagebuilderConfigForm">
                    <!-- AI功能配置 -->
                    <div class="mb-4">
                        <div class="form-check form-switch">
                            <input class="form-check-input" 
                                   type="checkbox" 
                                   name="ai_enabled"
                                   value="1"
                                   id="pagebuilder_ai_enabled"
                                   <?= $aiEnabled === '1' ? 'checked' : '' ?>>
                            <label class="form-check-label" for="pagebuilder_ai_enabled">
                                <strong><lang>开启AI功能</lang></strong>
                            </label>
                        </div>
                        <div class="pagebuilder-config-description">
                            <lang>开启后，在页面编辑界面和可视化编辑界面中将显示AI生成内容功能和AI生成配置功能。关闭后，这些AI相关功能将隐藏。</lang>
                        </div>
                    </div>

                    <hr>

                    <!-- 多语言功能配置 -->
                    <div class="mb-4">
                        <div class="form-check form-switch">
                            <input class="form-check-input" 
                                   type="checkbox" 
                                   name="i18n_enabled"
                                   value="1"
                                   id="pagebuilder_i18n_enabled"
                                   <?= $i18nEnabled === '1' ? 'checked' : '' ?>>
                            <label class="form-check-label" for="pagebuilder_i18n_enabled">
                                <strong><lang>开启多语言功能</lang></strong>
                            </label>
                        </div>
                        <div class="pagebuilder-config-description">
                            <lang>开启后，在页面编辑界面中将显示多语言内容翻译功能，后端会提示未翻译的语言信息，可视化编辑界面中也会显示语言切换配置。关闭后，这些多语言相关功能将隐藏。</lang>
                        </div>
                    </div>

                    <div class="mt-4">
                        <button type="submit" class="btn btn-primary">
                            <i class="mdi mdi-content-save me-2"></i><lang>保存配置</lang>
                        </button>
                        <button type="button" class="btn btn-secondary ms-2" id="pagebuilderConfigResetBtn">
                            <i class="mdi mdi-refresh me-2"></i><lang>重置</lang>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- SEO 账户配置 Widget -->
<div class="row">
    <div class="col-xl-12">
        <div class="card pagebuilder-seo-card">
            <div class="card-body">
                <h5 class="card-title mb-3">
                    <i class="mdi mdi-chart-line me-2" style="color: var(--backend-color-primary, #556ee6);"></i>
                    <lang>SEO 管理</lang>
                </h5>
                <p class="text-muted mb-3">
                    <lang>SEO账户配置、站点关联、主体管理等功能已整合到专门的SEO管理页面。</lang>
                </p>
                <div class="alert alert-info mb-3">
                    <i class="mdi mdi-information-outline me-2"></i>
                    <strong><lang>功能说明：</lang></strong>
                    <ul class="mb-0 mt-2">
                        <li><lang>配置Google、Bing、Baidu等搜索引擎账户</lang></li>
                        <li><lang>为不同站点绑定SEO账户并配置提交频率</lang></li>
                        <li><lang>管理PageBuilder页面的SEO主体信息</lang></li>
                        <li><lang>自动生成Sitemap并提交到搜索引擎</lang></li>
                    </ul>
                </div>
                <a href="<?= $this->getBackendUrl('pagebuilder/backend/seo/index') ?>" 
                   class="btn btn-primary btn-lg">
                    <i class="mdi mdi-arrow-right-circle me-2"></i>
                    <lang>前往 SEO 管理</lang>
                </a>
            </div>
        </div>
    </div>
</div>

<script>
(function() {
    'use strict';
    
    var MESSAGE_TYPE_MAP = {
        success: 'success',
        error: 'danger',
        warning: 'warning',
        info: 'info'
    };
    
    function showPageMessage(type, message) {
        var container = document.getElementById('pagebuilder-config-message-container');
        if (!container) {
            console.warn('page message container not found');
            if (typeof AdminToast !== 'undefined') {
                AdminToast[type] ? AdminToast[type](message) : AdminToast.info(message);
            } else {
                alert(message);
            }
            return;
        }
        var alertType = MESSAGE_TYPE_MAP[type] || 'info';
        var alertEl = document.createElement('div');
        alertEl.className = 'alert alert-' + alertType + ' alert-dismissible fade show';
        alertEl.setAttribute('role', 'alert');
        alertEl.innerHTML = '<span>' + message + '</span>' +
            '<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>';
        container.appendChild(alertEl);
        setTimeout(function() {
            try {
                var bsAlert = bootstrap.Alert.getOrCreateInstance(alertEl);
                bsAlert.close();
            } catch (e) {
                alertEl.remove();
            }
        }, 4000);
    }
    
    function saveConfig(event) {
        event.preventDefault();
        var form = document.getElementById('pagebuilderConfigForm');
        var formData = new FormData(form);
        
        var aiEnabled = document.getElementById('pagebuilder_ai_enabled').checked ? '1' : '0';
        var i18nEnabled = document.getElementById('pagebuilder_i18n_enabled').checked ? '1' : '0';
        formData.set('ai_enabled', aiEnabled);
        formData.set('i18n_enabled', i18nEnabled);
        
        var data = {};
        for (var pair of formData.entries()) {
            data[pair[0]] = pair[1];
        }
        
        fetch('<?= $this->getBackendUrl('pagebuilder/backend/config/save') ?>', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/x-www-form-urlencoded',
            },
            body: new URLSearchParams(data)
        })
        .then(function(response) { return response.json(); })
        .then(function(data) {
            if (data.success) {
                showPageMessage('success', data.message);
                setTimeout(function() {
                    window.location.reload();
                }, 1000);
            } else {
                showPageMessage('error', data.message);
            }
        })
        .catch(function(error) {
            console.error('Error:', error);
            showPageMessage('error', '<?= __('保存失败') ?>');
        });
    }
    
    function resetForm() {
        if (typeof AdminConfirm !== 'undefined') {
            AdminConfirm.show('<?= __('确定要重置表单吗？') ?>').then(function(confirmed) {
                if (confirmed) {
                    document.getElementById('pagebuilderConfigForm').reset();
                }
            });
        } else if (confirm('<?= __('确定要重置表单吗？') ?>')) {
            document.getElementById('pagebuilderConfigForm').reset();
        }
    }
    
    document.addEventListener('DOMContentLoaded', function() {
        var form = document.getElementById('pagebuilderConfigForm');
        if (form) {
            form.addEventListener('submit', saveConfig);
        }
        
        var resetBtn = document.getElementById('pagebuilderConfigResetBtn');
        if (resetBtn) {
            resetBtn.addEventListener('click', resetForm);
        }
    });
})();
</script>
