<?php
/**
 * 阶段一内联面板：顶部说明 + AI 调整入口。
 *
 * 设计要点：
 *  - 方案预览常驻页面，不再做 Tab 切换。
 *  - 阶段级微调/重建入口已移除；具体块操作在预览中进入统一弹窗。
 *  - 队列/进度信息走独立的进度条与 SSE 终端（见 body 与 modals 模板）。
 */
?>
<div class="pb-ai-plan-inline-panel-head border rounded shadow-sm bg-body mt-4">
    <div class="px-4 py-3 border-bottom bg-light rounded-top">
        <div class="d-flex flex-wrap align-items-start justify-content-between gap-3">
            <div class="flex-grow-1">
                <div class="fw-semibold mb-1"><?= htmlspecialchars((string)__('第一阶段方案'), ENT_QUOTES, 'UTF-8') ?></div>
                <div class="small text-muted mb-0"><?= htmlspecialchars((string)__('先看方案是否符合预期；需要改动时点「AI 调整方案」，确认后系统会自动进入第二阶段任务拆解。'), ENT_QUOTES, 'UTF-8') ?></div>
            </div>
        </div>
    </div>
    <div class="alert alert-info border-0 rounded-0 mb-0">
        <i class="mdi mdi-information-outline me-1"></i>
        <?= htmlspecialchars((string)__('长耗时 AI 任务在后台队列执行；页面只展示进度、队列状态与最终可确认结果。'), ENT_QUOTES, 'UTF-8') ?>
    </div>
</div>
