<?php
/**
 * @var \Weline\Framework\View\Template $this
 * @var string $error_message
 */
$msg = (string) ($this->getData('error_message') ?? __('发生错误'));
$backUrl = $this->getBackendUrl('*/backend/aiSiteAgent/index');
?>
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0">
                <i class="mdi mdi-alert-circle-outline me-1"></i>
                <?= htmlspecialchars(__('AI 建站工作台'), ENT_QUOTES, 'UTF-8') ?>
            </h4>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-lg-8">
        <div class="alert alert-warning" role="alert">
            <?= htmlspecialchars($msg, ENT_QUOTES, 'UTF-8') ?>
        </div>
        <a href="<?= htmlspecialchars($backUrl, ENT_QUOTES, 'UTF-8') ?>" class="btn btn-primary">
            <i class="mdi mdi-arrow-left me-1"></i>
            <lang>返回入口</lang>
        </a>
    </div>
</div>
