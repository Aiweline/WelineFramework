<div class="card border-0 shadow-sm" data-stage-panel="visual_edit">

    <div class="card-header bg-transparent border-0 pt-4 pb-0">

        <h5 class="card-title mb-1"><?= htmlspecialchars((string)__('可视化编辑'), ENT_QUOTES, 'UTF-8') ?></h5>

        <p class="text-muted mb-0"><?= htmlspecialchars((string)__('切换页面时只会更新当前预览页和可视化 URL，不需要再手动填写 preview_page_id。'), ENT_QUOTES, 'UTF-8') ?></p>

    </div>

    <div class="card-body p-4">

        <div class="pb-ai-visual-editor-toolbar mb-3" role="toolbar" aria-label="<?= htmlspecialchars((string)__('可视化编辑器工具栏'), ENT_QUOTES, 'UTF-8') ?>">

            <div class="pb-ai-visual-toolbar-pages<?= $previewPageOptions === [] ? ' d-none' : '' ?>" id="pb-ai-preview-tabs-wrap">

                <div class="small text-muted mb-1"><?= htmlspecialchars((string)__('当前预览页面'), ENT_QUOTES, 'UTF-8') ?></div>

                <ul class="nav nav-tabs pb-ai-preview-tabs" role="tablist">

                    <?php foreach ($previewPageOptions as $opt): ?>

                        <?php

                        $optPageType = (string)($opt['type'] ?? '');

                        $optPageId = (int)($opt['page_id'] ?? 0);

                        $optValue = (string)($opt['value'] ?? ($optPageId > 0 ? (string)$optPageId : $optPageType));

                        $isSelected = $optPageType !== ''

                            ? $optPageType === $previewPageType

                            : $optPageId === $previewPageId;

                        ?>

                        <li class="nav-item" role="presentation">

                            <button type="button"

                                    class="nav-link pb-ai-preview-tab<?= $isSelected ? ' active' : '' ?>"

                                    data-page-id="<?= $optPageId ?>"

                                    data-page-type="<?= htmlspecialchars($optPageType, ENT_QUOTES, 'UTF-8') ?>"

                                    data-page-value="<?= htmlspecialchars($optValue, ENT_QUOTES, 'UTF-8') ?>">

                                <?= htmlspecialchars((string)($opt['label'] ?? $optValue), ENT_QUOTES, 'UTF-8') ?>

                            </button>

                        </li>

                    <?php endforeach; ?>

                </ul>

            </div>

            <div class="pb-ai-visual-toolbar-devices">

                <div class="pb-ai-preview-device-bar mb-0" id="pb-ai-preview-device-bar" aria-label="<?= htmlspecialchars((string)__('预览设备'), ENT_QUOTES, 'UTF-8') ?>">

                    <span class="small text-muted me-1"><?= htmlspecialchars((string)__('预览'), ENT_QUOTES, 'UTF-8') ?></span>

                    <button type="button" class="btn btn-outline-secondary btn-sm pb-ai-preview-device-btn active" data-preview-device="desktop" aria-pressed="true"><?= htmlspecialchars((string)__('PC'), ENT_QUOTES, 'UTF-8') ?></button>

                    <button type="button" class="btn btn-outline-secondary btn-sm pb-ai-preview-device-btn" data-preview-device="tablet" aria-pressed="false"><?= htmlspecialchars((string)__('iPad'), ENT_QUOTES, 'UTF-8') ?></button>

                    <button type="button" class="btn btn-outline-secondary btn-sm pb-ai-preview-device-btn" data-preview-device="mobile" aria-pressed="false"><?= htmlspecialchars((string)__('手机'), ENT_QUOTES, 'UTF-8') ?></button>

                    <button type="button"
                            class="btn btn-outline-secondary btn-sm pb-ai-visual-preview-fullscreen-trigger"
                            title="<?= htmlspecialchars((string)__('放大预览'), ENT_QUOTES, 'UTF-8') ?>"
                            aria-label="<?= htmlspecialchars((string)__('放大预览'), ENT_QUOTES, 'UTF-8') ?>">
                        <i class="mdi mdi-arrow-expand-all" aria-hidden="true"></i>
                    </button>

                </div>

            </div>

        </div>

        <?php
        $pbAiVisualEditCanEnterPublish = !empty($state['can_publish']);
        $pbAiVisualEditPublishBadge = $pbAiVisualEditCanEnterPublish
            ? (string)__('可进入第三阶段')
            : (string)__('等待构建完成');
        $pbAiVisualEditPublishTitle = $pbAiVisualEditCanEnterPublish
            ? (string)__('虚拟主题已就绪，下一步进入确认发布')
            : (string)__('第三阶段确认发布入口会在虚拟主题就绪后开放');
        $pbAiVisualEditPublishHint = $pbAiVisualEditCanEnterPublish
            ? (string)__('进入第三阶段后可先运行发布前预检，再确认准备发布。')
            : (string)__('当前仍可继续预览和编辑；当虚拟主题构建完成后，这里会直接提供进入第三阶段的按钮。');
        ?>

        <div class="border rounded p-3 mb-3 bg-light" id="pb-ai-publish-stage-entry-card">

            <div class="d-flex flex-wrap align-items-start justify-content-between gap-3">

                <div class="min-w-0">

                    <div class="d-flex flex-wrap align-items-center gap-2 mb-2">

                        <span class="badge <?= $pbAiVisualEditCanEnterPublish ? 'bg-success' : 'bg-secondary' ?>" id="pb-ai-publish-stage-entry-badge"><?= htmlspecialchars($pbAiVisualEditPublishBadge, ENT_QUOTES, 'UTF-8') ?></span>

                        <span class="small text-muted"><?= htmlspecialchars((string)__('阶段三'), ENT_QUOTES, 'UTF-8') ?></span>

                    </div>

                    <div class="fw-semibold mb-1" id="pb-ai-publish-stage-entry-title"><?= htmlspecialchars($pbAiVisualEditPublishTitle, ENT_QUOTES, 'UTF-8') ?></div>

                    <div class="small text-muted" id="pb-ai-publish-stage-entry-hint"><?= htmlspecialchars($pbAiVisualEditPublishHint, ENT_QUOTES, 'UTF-8') ?></div>

                </div>

                <div class="d-flex flex-wrap align-items-center gap-2">

                    <button type="button"
                            class="btn btn-primary"
                            id="pb-ai-go-publish-stage"
                            data-stage="publish"<?= $pbAiVisualEditCanEnterPublish ? '' : ' disabled aria-disabled="true"' ?>>
                        <i class="mdi mdi-flag-checkered me-1"></i><?= htmlspecialchars((string)__('进入第三阶段确认发布'), ENT_QUOTES, 'UTF-8') ?>
                    </button>

                </div>

            </div>

        </div>

        <div class="border rounded p-3 mb-3<?= $pagesByType !== [] ? '' : ' d-none' ?>" id="pb-ai-materialized-pages-card">

            <div class="fw-semibold mb-2"><?= htmlspecialchars((string)__('已生成页面'), ENT_QUOTES, 'UTF-8') ?></div>

            <div class="d-flex flex-wrap gap-2" id="pb-ai-materialized-pages-list">

                <?php foreach ($pagesByType as $pt => $pageData): ?>

                    <?php if ((int)($pageData['page_id'] ?? 0) <= 0) { continue; } ?>

                    <span class="badge bg-soft-info text-info"><?= htmlspecialchars((string)($pageTypeLabels[$pt] ?? $pt), ENT_QUOTES, 'UTF-8') ?>#<?= (int)($pageData['page_id'] ?? 0) ?></span>

                <?php endforeach; ?>

            </div>

        </div>

        <div class="alert alert-info mb-0<?= $embeddedVisualUrl !== '' ? ' d-none' : '' ?>" id="pb-ai-visual-edit-empty-state"><?= htmlspecialchars((string)__('当前还没有可用的可视化预览地址，请先完成虚拟主题构建。'), ENT_QUOTES, 'UTF-8') ?></div>

        <?php if (!empty($guidedUi)): ?>

        <div class="pb-ai-guided-visual-canvas border-top mt-4 pt-0">

            <div class="pb-ai-visual-preview-shell position-relative rounded-bottom overflow-hidden">

                <button type="button"
                        class="btn btn-light btn-sm pb-ai-visual-preview-zoom-trigger pb-ai-visual-preview-fullscreen-trigger"
                        title="<?= htmlspecialchars((string)__('放大预览'), ENT_QUOTES, 'UTF-8') ?>"
                        aria-label="<?= htmlspecialchars((string)__('放大预览'), ENT_QUOTES, 'UTF-8') ?>">
                    <i class="mdi mdi-arrow-expand-all" aria-hidden="true"></i>
                </button>

                <div class="pb-ai-visual-preview-watermark" aria-hidden="true"><?= htmlspecialchars((string)__('预览可视化编辑器'), ENT_QUOTES, 'UTF-8') ?></div>

                <div id="pb-ai-visual-preview-error-overlay" class="pb-ai-visual-preview-error-overlay d-none" role="alert">

                    <div class="pb-ai-visual-preview-error-text small fw-semibold text-danger"></div>

                </div>

                <div id="pb-ai-visual-preview-stage" class="pb-ai-preview-device-stage is-desktop">

                    <div id="pb-ai-visual-preview-host">

                        <?php if ($embeddedVisualUrl !== ''): ?>

                            <iframe id="pb-ai-visual-preview-frame"

                                    title="<?= htmlspecialchars((string)__('PageBuilder 可视化预览'), ENT_QUOTES, 'UTF-8') ?>"

                                    src="<?= htmlspecialchars($embeddedVisualUrl, ENT_QUOTES, 'UTF-8') ?>"

                                    loading="lazy"

                                    referrerpolicy="no-referrer"

                                    style="width:100%;min-height:600px;border:1px solid var(--backend-color-border-default, #e5e7eb);border-radius:16px;background:var(--backend-color-bg-primary, #fff);display:block;">

                            </iframe>

                        <?php else: ?>

                            <div class="alert alert-info mb-0 pb-ai-preview-host-empty m-3"><?= htmlspecialchars((string)__('完成虚拟主题构建后，这里会直接显示真实的 PageBuilder 可视化预览。'), ENT_QUOTES, 'UTF-8') ?></div>

                        <?php endif; ?>

                    </div>

                </div>

            </div>

        </div>

        <?php endif; ?>

    </div>

</div>
