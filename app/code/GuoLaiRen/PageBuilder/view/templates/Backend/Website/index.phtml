<?php
/**@var \Weline\Framework\View\Template $this */
?>
<w:template>Weline_Admin::Backend/page-layout/main-content-before.phtml</w:template>

<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0">
                <?= $this->getData('page_title') ?: __('站点分配') ?>
            </h4>

            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item">
                        <a href="javascript:void(0);">
                            <?= $this->getData('breadcrumb_parent') ?: __('页面管理') ?>
                        </a>
                    </li>
                    <li class="breadcrumb-item active">
                        <?= $this->getData('breadcrumb_current') ?: __('站点分配') ?>
                    </li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- start page message -->
<div class='row'>
    <template>Weline_Component::message.phtml</template>
</div>
<!-- end message -->

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">
                    <lang>站点与后台用户一对一分配</lang>
                </h4>
                <p class="card-title-desc text-muted mb-3">
                    <lang>每个站点同一时间只能分配给一个后台用户，将站点分配给新的用户后，会自动取消原有用户的归属关系。</lang>
                </p>

                <?php
                $websites = $this->getData('websites') ?? [];
                $assignedMap = $this->getData('assigned_map') ?? [];
                $backendUsers = $this->getData('backend_users') ?? [];
                ?>

                <?php if (empty($websites)): ?>
                    <div class="alert alert-info" role="alert">
                        <i class="mdi mdi-information-outline me-2"></i>
                        <lang>当前还没有可分配的站点，请先在 Websites 模块中创建站点。</lang>
                    </div>
                <?php else: ?>
                    <form action="<?= $this->getBackendUrl('*/backend/website/index') ?>" method="post">
                        <div class="table-responsive">
                            <table class="table table-bordered align-middle">
                                <thead class="table-light">
                                <tr>
                                    <th style="width: 60px;"><lang>ID</lang></th>
                                    <th><lang>站点名称</lang></th>
                                    <th><lang>站点代码</lang></th>
                                    <th><lang>站点URL</lang></th>
                                    <th style="width: 260px;"><lang>当前分配用户</lang></th>
                                </tr>
                                </thead>
                                <tbody>
                                <?php foreach ($websites as $website): ?>
                                    <?php
                                    $websiteId = (int)($website['website_id'] ?? 0);
                                    $assignedUserId = $assignedMap[$websiteId] ?? 0;
                                    ?>
                                    <tr>
                                        <td><?= $websiteId ?></td>
                                        <td><?= htmlspecialchars($website['name'] ?? '') ?></td>
                                        <td><?= htmlspecialchars($website['code'] ?? '') ?></td>
                                        <td><?= htmlspecialchars($website['url'] ?? '') ?></td>
                                        <td>
                                            <select class="form-select" name="assign[<?= $websiteId ?>]">
                                                <option value="0">
                                                    <lang>未分配（任何用户不可见）</lang>
                                                </option>
                                                <?php foreach ($backendUsers as $user): ?>
                                                    <?php
                                                    $userId = (int)($user['user_id'] ?? 0);
                                                    $username = $user['username'] ?? '';
                                                    $email = $user['email'] ?? '';
                                                    ?>
                                                    <option value="<?= $userId ?>" <?= $assignedUserId === $userId ? 'selected' : '' ?>>
                                                        <?= htmlspecialchars($username . ' <' . $email . '>') ?>
                                                    </option>
                                                <?php endforeach; ?>
                                            </select>
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>

                        <div class="mt-3 d-flex justify-content-between align-items-center">
                            <div class="text-muted">
                                <small>
                                    <i class="mdi mdi-alert-circle-outline me-1 text-warning"></i>
                                    <lang>提示：拥有“站点分配”权限的用户可以看到所有站点并修改归属关系，普通用户仅能看到分配给自己的站点。</lang>
                                </small>
                            </div>
                            <div>
                                <button type="submit" class="btn btn-primary">
                                    <i class="mdi mdi-content-save me-1"></i>
                                    <lang>保存分配</lang>
                                </button>
                            </div>
                        </div>
                    </form>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<w:template>Weline_Admin::Backend/page-layout/main-content-after.phtml</w:template>
