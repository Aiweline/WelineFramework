<?php
/**
 * 博客分类编辑/新建表单模板
 *
 * 变量：
 * - $category       Category|null  分类对象（编辑时）
 * - $action         string         表单提交地址
 * - $parent_options array          父级分类选项
 */

use GuoLaiRen\Blog\Model\Category;

$isEdit = $category && $category->getId();
?>
<w:template>Weline_Admin::Backend/page-layout/main-content-before.phtml</w:template>

<!-- start page title -->
<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><?= $isEdit ? __('编辑分类') : __('新建分类') ?></h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="<?= $this->getBackendUrl('') ?>"><?= __('首页') ?></a></li>
                    <li class="breadcrumb-item"><a href="<?= $this->getUrl('blog/backend/category/index') ?>"><?= __('博客分类') ?></a></li>
                    <li class="breadcrumb-item active"><?= $isEdit ? __('编辑分类') : __('新建分类') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- end page title -->

<!-- start page message -->
<div class='row'>
    <template>Weline_Component::message.phtml</template>
</div>
<!-- end message -->

<div class="row">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title mb-0">
                    <?= $isEdit ? __('编辑分类') : __('新建分类') ?>
                </h4>
            </div>
            <div class="card-body">
                <form action="<?= htmlspecialchars($action) ?>" method="post">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="name" class="form-label">
                                    <lang>分类名称</lang> <span class="text-danger">*</span>
                                </label>
                                <input type="text" class="form-control" id="name" name="name"
                                       value="<?= htmlspecialchars($category ? $category->getData(Category::fields_NAME) : '') ?>"
                                       required>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="slug" class="form-label">
                                    <lang>URL别名</lang> <span class="text-danger">*</span>
                                </label>
                                <input type="text" class="form-control" id="slug" name="slug"
                                       value="<?= htmlspecialchars($category ? $category->getData(Category::fields_SLUG) : '') ?>"
                                       pattern="[a-z0-9\-]+"
                                       title="<?= __('只能包含小写字母、数字和连字符') ?>"
                                       required>
                                <small class="text-muted"><lang>只能包含小写字母、数字和连字符</lang></small>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="parent_id" class="form-label">
                                    <lang>父级分类</lang>
                                </label>
                                <select class="form-select" id="parent_id" name="parent_id">
                                    <option value="0"><lang>无（顶级分类）</lang></option>
                                    <?php foreach ($parent_options as $id => $name): ?>
                                        <option value="<?= $id ?>"
                                            <?= ($category && $category->getData(Category::fields_PARENT_ID) == $id) ? 'selected' : '' ?>>
                                            <?= htmlspecialchars($name) ?>
                                        </option>
                                    <?php endforeach; ?>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="mb-3">
                                <label for="sort_order" class="form-label">
                                    <lang>排序</lang>
                                </label>
                                <input type="number" class="form-control" id="sort_order" name="sort_order"
                                       value="<?= (int)($category ? $category->getData(Category::fields_SORT_ORDER) : 0) ?>"
                                       min="0">
                                <small class="text-muted"><lang>数字越小越靠前</lang></small>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="mb-3">
                                <label for="status" class="form-label">
                                    <lang>状态</lang>
                                </label>
                                <select class="form-select" id="status" name="status">
                                    <?php foreach (Category::getStatusOptions() as $value => $label): ?>
                                        <option value="<?= $value ?>"
                                            <?= ($category && $category->getData(Category::fields_STATUS) == $value) ? 'selected' : '' ?>
                                            <?= (!$category && $value == Category::STATUS_ENABLED) ? 'selected' : '' ?>>
                                            <?= $label ?>
                                        </option>
                                    <?php endforeach; ?>
                                </select>
                            </div>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="description" class="form-label">
                            <lang>分类描述</lang>
                        </label>
                        <textarea class="form-control" id="description" name="description" rows="3"><?= htmlspecialchars($category ? $category->getData(Category::fields_DESCRIPTION) : '') ?></textarea>
                    </div>

                    <div class="mb-3">
                        <label for="cover_image" class="form-label">
                            <?= __('封面图片') ?>
                        </label>
                        <?php $cat_cover_image = $category ? $category->getData(Category::fields_COVER_IMAGE) : ''; ?>
                        <input type="hidden" id="cover_image" name="cover_image"
                               value="<?= htmlspecialchars($cat_cover_image) ?>">
                        <file-manager target="#cover_image"
                                      title="选择封面图片"
                                      vars="cat_cover_image"
                                      path="blog/categories/"
                                      value="cat_cover_image"
                                      multi="0"
                                      ext="jpg,jpeg,png,gif,webp"
                                      w="100"
                                      h="60"/>
                        <small class="text-muted"><?= __('点击上方按钮选择或上传封面图片') ?></small>
                    </div>

                    <hr>
                    <h5 class="mb-3"><lang>SEO设置</lang></h5>

                    <div class="mb-3">
                        <label for="meta_title" class="form-label">
                            <lang>SEO标题</lang>
                        </label>
                        <input type="text" class="form-control" id="meta_title" name="meta_title"
                               value="<?= htmlspecialchars($category ? $category->getData(Category::fields_META_TITLE) : '') ?>">
                        <small class="text-muted"><lang>留空则使用分类名称</lang></small>
                    </div>

                    <div class="mb-3">
                        <label for="meta_description" class="form-label">
                            <lang>SEO描述</lang>
                        </label>
                        <textarea class="form-control" id="meta_description" name="meta_description" rows="2"><?= htmlspecialchars($category ? $category->getData(Category::fields_META_DESCRIPTION) : '') ?></textarea>
                    </div>

                    <div class="mb-3">
                        <label for="meta_keywords" class="form-label">
                            <lang>SEO关键词</lang>
                        </label>
                        <input type="text" class="form-control" id="meta_keywords" name="meta_keywords"
                               value="<?= htmlspecialchars($category ? $category->getData(Category::fields_META_KEYWORDS) : '') ?>"
                               placeholder="<?= __('关键词1, 关键词2, 关键词3') ?>">
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="<?= $this->getUrl('blog/backend/category/index') ?>" class="btn btn-secondary">
                            <i class="mdi mdi-arrow-left"></i> <lang>返回列表</lang>
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="mdi mdi-content-save"></i>
                            <?= $isEdit ? __('保存分类') : __('创建分类') ?>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0"><lang>帮助说明</lang></h5>
            </div>
            <div class="card-body">
                <p class="text-muted mb-2">
                    <i class="mdi mdi-information-outline text-info"></i>
                    <lang>分类用于对博客文章进行归类整理。</lang>
                </p>
                <p class="text-muted mb-2">
                    <i class="mdi mdi-link text-primary"></i>
                    <lang>URL别名将用于生成分类页面的访问地址。</lang>
                </p>
                <p class="text-muted mb-0">
                    <i class="mdi mdi-sort-ascending text-success"></i>
                    <lang>排序数字越小，分类在列表中显示越靠前。</lang>
                </p>
            </div>
        </div>
    </div>
</div>

<script>
// 自动生成 URL 别名
document.getElementById('name').addEventListener('blur', function() {
    const slugInput = document.getElementById('slug');
    if (slugInput.value === '') {
        const name = this.value.toLowerCase()
            .replace(/[^a-z0-9\u4e00-\u9fa5\s-]/g, '')
            .replace(/[\s]+/g, '-')
            .replace(/-+/g, '-')
            .replace(/^-|-$/g, '');
        // 如果是中文，进行简单的拼音转换（这里只是移除中文字符）
        slugInput.value = name.replace(/[\u4e00-\u9fa5]/g, '');
    }
});
</script>

<w:template>Weline_Admin::Backend/page-layout/main-content-after.phtml</w:template>
