<?php
/** @var array $profiles */
/** @var int $selected_profile_id */
/** @var array $top10 */
/** @var string $error */
/** @var string $profile_name */
$profiles = $this->getData('profiles') ?? [];
$selected_profile_id = (int)($this->getData('selected_profile_id') ?? 0);
$top10 = $this->getData('top10') ?? [];
$error = (string)($this->getData('error') ?? '');
$profile_name = (string)($this->getData('profile_name') ?? '');
?>
<w:template>Weline_Admin::Backend/page-layout/main-content-before.phtml</w:template>

<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><?= $this->getData('page_title') ?: __('趋势词分析') ?></h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="<?= $this->getBackendUrl('blog/backend/post/index') ?>"><?= __('博客管理') ?></a></li>
                    <li class="breadcrumb-item active"><?= __('趋势词分析') ?></li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class='row'>
    <template>Weline_Component::message.phtml</template>
</div>

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <form method="get" class="mb-4 d-flex align-items-end gap-2">
                    <div>
                        <label for="profile_id" class="form-label"><?= __('画像') ?></label>
                        <select name="profile_id" id="profile_id" class="form-select" style="width:auto;">
                            <?php foreach ($profiles as $p): ?>
                                <option value="<?= (int)$p['profile_id'] ?>" <?= $selected_profile_id === (int)$p['profile_id'] ? 'selected' : '' ?>>
                                    <?= htmlspecialchars($p['name']) ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary"><?= __('刷新') ?></button>
                </form>

                <?php if ($error !== ''): ?>
                    <div class="alert alert-warning"><?= htmlspecialchars($error) ?></div>
                <?php elseif (!empty($top10)): ?>
                    <h5 class="mb-3"><?= __('Top 10 趋势词') ?><?= $profile_name !== '' ? ' - ' . htmlspecialchars($profile_name) : '' ?></h5>
                    <div class="table-responsive">
                        <table class="table table-hover table-centered mb-0">
                            <thead>
                                <tr>
                                    <th width="60"><?= __('排名') ?></th>
                                    <th><?= __('关键词') ?></th>
                                    <th><?= __('趋势值') ?></th>
                                    <th><?= __('日期') ?></th>
                                    <th><?= __('地区') ?></th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($top10 as $idx => $row): ?>
                                    <tr>
                                        <td><?= $idx + 1 ?></td>
                                        <td><strong><?= htmlspecialchars($row['keyword']) ?></strong></td>
                                        <td><?= (int)$row['value'] ?></td>
                                        <td><?= htmlspecialchars($row['date']) ?></td>
                                        <td><?= htmlspecialchars($row['region']) ?></td>
                                    </tr>
                                <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>
                <?php elseif ($selected_profile_id > 0): ?>
                    <div class="alert alert-info">
                        <?= __('暂无趋势数据，请确认已配置 SerpApi API Key 并选择画像。') ?>
                        <a href="<?= $this->getBackendUrl('blog/backend/trends-config/index') ?>" class="alert-link ms-2">
                            <i class="mdi mdi-cog"></i> <?= __('前往配置') ?>
                        </a>
                        <a href="https://serpapi.com" target="_blank" rel="noopener noreferrer" class="alert-link ms-2">
                            <i class="mdi mdi-open-in-new"></i> <?= __('获取 API Key') ?>
                        </a>
                    </div>
                <?php else: ?>
                    <div class="alert alert-info"><?= __('请选择画像后点击刷新。') ?></div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<w:template>Weline_Admin::Backend/page-layout/main-content-after.phtml</w:template>
