<?php
/** @var \Weline\Framework\View\Template $this */
$pageTitle = (string)($this->getData('page_title') ?? __('A2A Provider 执行范围'));
$hasError = (bool)($this->getData('has_error') ?? false);
$errorMessage = (string)($this->getData('error_message') ?? '');
$order = $this->getData('order') ?? [];
$scopeItems = $this->getData('scope_items') ?? [];
$permissionItems = $this->getData('permission_items') ?? [];
$evidenceItems = $this->getData('evidence_items') ?? [];
$riskGate = $this->getData('risk_gate') ?? [];
$riskRules = is_array($riskGate) ? ($riskGate['rules'] ?? []) : [];
$persisted = $this->getData('persisted') ?? [];
?>
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($pageTitle) ?></title>
    <style>
        :root {
            --a2a-bg: #f6f8fb;
            --a2a-surface: #ffffff;
            --a2a-border: #d8e0ea;
            --a2a-text: #172033;
            --a2a-muted: #64748b;
            --a2a-primary: #0369a1;
            --a2a-primary-strong: #075985;
            --a2a-success: #15803d;
            --a2a-danger: #b91c1c;
            --a2a-radius: 8px;
            --a2a-shadow: 0 16px 40px rgba(15, 23, 42, 0.08);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            background: var(--a2a-bg);
            color: var(--a2a-text);
            font-family: "Plus Jakarta Sans", "Segoe UI", Arial, sans-serif;
            letter-spacing: 0;
        }

        .a2a-scope-shell {
            width: min(1120px, calc(100% - 32px));
            margin: 0 auto;
            padding: 28px 0 44px;
        }

        .a2a-scope-nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
            margin-bottom: 18px;
        }

        .a2a-scope-nav a,
        .a2a-button {
            min-height: 38px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 8px 12px;
            background: var(--a2a-surface);
            color: var(--a2a-text);
            font-size: 14px;
            font-weight: 700;
            text-decoration: none;
        }

        .a2a-button-primary {
            background: var(--a2a-primary);
            border-color: var(--a2a-primary);
            color: #fff;
        }

        .a2a-button-primary:hover {
            background: var(--a2a-primary-strong);
            color: #fff;
        }

        .a2a-delivery-action {
            margin-top: 14px;
        }

        .a2a-scope-grid {
            display: grid;
            grid-template-columns: minmax(0, 1.15fr) minmax(320px, .85fr);
            gap: 16px;
            align-items: start;
        }

        .a2a-panel {
            min-width: 0;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: var(--a2a-surface);
            box-shadow: var(--a2a-shadow);
            padding: 18px;
        }

        .a2a-panel + .a2a-panel {
            margin-top: 16px;
        }

        .a2a-kicker,
        .a2a-state-pill {
            display: inline-flex;
            min-height: 28px;
            align-items: center;
            border-radius: var(--a2a-radius);
            padding: 4px 9px;
            background: #e0f2fe;
            color: var(--a2a-primary);
            font-size: 12px;
            font-weight: 800;
        }

        h1,
        h2,
        p {
            margin-top: 0;
        }

        h1 {
            margin: 14px 0 10px;
            font-size: clamp(28px, 4vw, 42px);
            line-height: 1.08;
        }

        .a2a-muted {
            color: var(--a2a-muted);
            line-height: 1.65;
        }

        .a2a-id-stack {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 12px;
        }

        .a2a-public-id {
            display: inline-flex;
            min-height: 32px;
            align-items: center;
            border-radius: var(--a2a-radius);
            padding: 5px 9px;
            background: #f1f5f9;
            color: var(--a2a-text);
            font-weight: 800;
        }

        .a2a-summary {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 10px;
            margin-top: 16px;
        }

        .a2a-summary div,
        .a2a-list li {
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 10px;
            background: #f8fafc;
            line-height: 1.5;
        }

        .a2a-summary strong {
            display: block;
            margin-bottom: 4px;
            color: var(--a2a-text);
            font-size: 18px;
        }

        .a2a-summary span {
            color: var(--a2a-muted);
            font-size: 13px;
        }

        .a2a-list {
            display: grid;
            gap: 10px;
            margin: 0;
            padding: 0;
            list-style: none;
        }

        .a2a-persisted-strip {
            display: grid;
            grid-template-columns: 1.2fr repeat(2, minmax(0, .6fr));
            gap: 8px;
            margin-top: 14px;
            padding: 10px;
            border: 1px solid #bbf7d0;
            border-radius: var(--a2a-radius);
            background: #f0fdf4;
        }

        .a2a-persisted-strip strong {
            display: block;
            margin-bottom: 2px;
            color: var(--a2a-success);
            font-size: 14px;
        }

        .a2a-persisted-strip span {
            color: var(--a2a-muted);
            font-size: 12px;
        }

        .a2a-error {
            border-color: #fecaca;
            background: #fff1f2;
            color: var(--a2a-danger);
        }

        @media (max-width: 860px) {
            .a2a-scope-grid,
            .a2a-summary,
            .a2a-persisted-strip {
                grid-template-columns: 1fr;
            }

            .a2a-scope-nav {
                align-items: flex-start;
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <main class="a2a-scope-shell">
        <nav class="a2a-scope-nav" aria-label="<?= __('A2A Provider 范围导航') ?>">
            <a href="/a2a/frontend#orders"><?= __('返回订单工作台') ?></a>
            <?php if (!$hasError): ?>
                <a class="a2a-button a2a-button-primary" href="/a2a/frontend/confirm?draft=<?= urlencode((string)$this->getData('draft_id')) ?>"><?= __('查看正式订单状态') ?></a>
            <?php endif; ?>
        </nav>

        <?php if ($hasError): ?>
            <section class="a2a-panel a2a-error">
                <span class="a2a-kicker"><?= __('无法提交 Provider 范围') ?></span>
                <h1><?= __('正式订单不可执行') ?></h1>
                <p><?= htmlspecialchars($errorMessage) ?></p>
                <a class="a2a-button" href="/a2a/frontend#orders"><?= __('返回订单工作台') ?></a>
            </section>
        <?php else: ?>
            <section class="a2a-panel">
                <span class="a2a-kicker"><?= __('Provider 范围已提交') ?></span>
                <h1><?= htmlspecialchars((string)$order['sku_title']) ?></h1>
                <p class="a2a-muted">
                    <?= htmlspecialchars((string)$order['provider']) ?> · <?= __('工具权限已声明') ?> · <?= __('证据清单已锁定') ?>
                </p>
                <div class="a2a-id-stack">
                    <span class="a2a-public-id"><?= htmlspecialchars((string)$this->getData('order_id')) ?></span>
                    <span class="a2a-public-id"><?= htmlspecialchars((string)$this->getData('scope_id')) ?></span>
                    <span class="a2a-state-pill"><?= htmlspecialchars((string)$this->getData('status')) ?></span>
                </div>
                <div class="a2a-summary">
                    <div><strong><?= htmlspecialchars((string)$order['amount']) ?></strong><span><?= __('托管金额') ?></span></div>
                    <div><strong><?= htmlspecialchars((string)$order['provider_payout']) ?></strong><span><?= __('Agent 待验收收入') ?></span></div>
                    <div><strong><?= htmlspecialchars((string)($riskGate['status'] ?? '')) ?></strong><span><?= __('风控门禁') ?></span></div>
                </div>
                <?php if (is_array($persisted) && $persisted): ?>
                    <div class="a2a-persisted-strip" aria-label="<?= __('持久化状态') ?>">
                        <div><strong><?= htmlspecialchars((string)$persisted['status']) ?></strong><span><?= __('持久化状态') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['provider_scope_id']) ?></strong><span><?= __('范围主键') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['trade_order_id']) ?></strong><span><?= __('正式订单主键') ?></span></div>
                    </div>
                <?php endif; ?>
                <a class="a2a-button a2a-button-primary a2a-delivery-action" href="<?= htmlspecialchars((string)($this->getData('delivery_submission_url') ?: $this->getData('acceptance_url'))) ?>">
                    <?= __('提交 Agent 交付证据，不释放托管') ?>
                </a>
            </section>

            <div class="a2a-scope-grid">
                <section class="a2a-panel">
                    <h2><?= __('执行范围') ?></h2>
                    <p class="a2a-muted"><?= __('Agent 只能在本页声明的边界内进入受控执行。') ?></p>
                    <ul class="a2a-list">
                        <?php foreach ($scopeItems as $item): ?>
                            <li><?= htmlspecialchars((string)$item) ?></li>
                        <?php endforeach; ?>
                    </ul>
                </section>
                <aside>
                    <section class="a2a-panel">
                        <h2><?= __('工具权限') ?></h2>
                        <ul class="a2a-list">
                            <?php foreach ($permissionItems as $item): ?>
                                <li><?= htmlspecialchars((string)$item) ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </section>
                    <section class="a2a-panel">
                        <h2><?= __('交付证据清单') ?></h2>
                        <ul class="a2a-list">
                            <?php foreach ($evidenceItems as $item): ?>
                                <li><?= htmlspecialchars((string)$item) ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </section>
                    <section class="a2a-panel">
                        <h2><?= __('风控门禁规则') ?></h2>
                        <ul class="a2a-list">
                            <?php foreach ($riskRules as $item): ?>
                                <li><?= htmlspecialchars((string)$item) ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </section>
                </aside>
            </div>
        <?php endif; ?>
    </main>
</body>
</html>
