<?php
/** @var \Weline\Framework\View\Template $this */
$pageTitle = (string)($this->getData('page_title') ?? __('A2A Agent 托管交易平台'));
$summaryMetrics = $this->getData('summary_metrics') ?? [];
$marketplaceSegments = $this->getData('marketplace_segments') ?? [];
$tierFilters = $this->getData('tier_filters') ?? [];
$sortOptions = $this->getData('sort_options') ?? [];
$abilitySkus = $this->getData('ability_skus') ?? [];
$pipelineStages = $this->getData('pipeline_stages') ?? [];
$featuredAgents = $this->getData('featured_agents') ?? [];
$quoteRows = $this->getData('quote_rows') ?? [];
$providerTasks = $this->getData('provider_tasks') ?? [];
$orderTimeline = $this->getData('order_timeline') ?? [];
$ledgerRows = $this->getData('ledger_rows') ?? [];
$rolePermissions = $this->getData('role_permissions') ?? [];
$disputeCases = $this->getData('dispute_cases') ?? [];
$commercialRules = $this->getData('commercial_rules') ?? [];
$riskItems = $this->getData('risk_items') ?? [];
$prototypeFlow = $this->getData('prototype_flow') ?? [];
?>
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($pageTitle) ?></title>
    <style>
        :root {
            --a2a-bg: #f6f8fb;
            --a2a-surface: #ffffff;
            --a2a-surface-soft: #eef6f5;
            --a2a-border: #d8e0ea;
            --a2a-border-strong: #a9b8ca;
            --a2a-text: #172033;
            --a2a-muted: #64748b;
            --a2a-primary: #0369a1;
            --a2a-primary-strong: #075985;
            --a2a-accent: #0f766e;
            --a2a-warning: #b45309;
            --a2a-danger: #b91c1c;
            --a2a-success: #15803d;
            --a2a-radius: 8px;
            --a2a-shadow: 0 16px 40px rgba(15, 23, 42, 0.08);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            background: var(--a2a-bg);
            color: var(--a2a-text);
            font-family: "Plus Jakarta Sans", "Segoe UI", Arial, sans-serif;
            letter-spacing: 0;
        }

        .a2a-page {
            min-height: 100vh;
        }

        .a2a-shell {
            width: min(1440px, calc(100% - 32px));
            margin: 0 auto;
            padding: 24px 0 40px;
        }

        .a2a-shell > *,
        .a2a-grid > *,
        .a2a-panel,
        .a2a-card,
        .a2a-agent,
        .a2a-quote-row {
            min-width: 0;
        }

        .a2a-topbar {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 16px;
            padding: 14px 0 22px;
        }

        .a2a-brand {
            display: flex;
            align-items: center;
            gap: 12px;
            min-width: 0;
        }

        .a2a-brand-mark {
            display: grid;
            place-items: center;
            width: 42px;
            height: 42px;
            border-radius: var(--a2a-radius);
            background: var(--a2a-primary);
            color: #fff;
            font-weight: 800;
            box-shadow: var(--a2a-shadow);
        }

        .a2a-brand-title {
            margin: 0;
            font-size: 20px;
            line-height: 1.2;
        }

        .a2a-brand-subtitle {
            margin: 3px 0 0;
            color: var(--a2a-muted);
            font-size: 13px;
        }

        .a2a-nav {
            display: flex;
            align-items: center;
            gap: 8px;
            flex-wrap: wrap;
        }

        .a2a-nav a,
        .a2a-button {
            min-height: 38px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 8px 12px;
            color: var(--a2a-text);
            background: var(--a2a-surface);
            text-decoration: none;
            font-size: 14px;
            font-weight: 650;
            cursor: pointer;
            transition: border-color .18s ease, background .18s ease, color .18s ease, transform .18s ease;
        }

        .a2a-nav a:hover,
        .a2a-button:hover {
            border-color: var(--a2a-primary);
            color: var(--a2a-primary);
            transform: translateY(-1px);
        }

        .a2a-button-primary {
            background: var(--a2a-primary);
            color: #fff;
            border-color: var(--a2a-primary);
        }

        .a2a-button-primary:hover {
            background: var(--a2a-primary-strong);
            color: #fff;
        }

        .a2a-grid {
            display: grid;
            grid-template-columns: minmax(0, 1.5fr) minmax(320px, .75fr);
            gap: 16px;
            align-items: start;
        }

        .a2a-panel,
        .a2a-card,
        .a2a-agent,
        .a2a-quote-row {
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: var(--a2a-surface);
            box-shadow: var(--a2a-shadow);
        }

        .a2a-panel {
            padding: 18px;
        }

        .a2a-panel + .a2a-panel {
            margin-top: 16px;
        }

        .a2a-hero {
            display: grid;
            grid-template-columns: minmax(0, 1fr) minmax(280px, .7fr);
            gap: 18px;
            align-items: stretch;
            margin-bottom: 16px;
        }

        .a2a-hero-copy {
            min-height: 280px;
            padding: 24px;
            border-radius: var(--a2a-radius);
            background: linear-gradient(135deg, #ffffff 0%, #eef6f5 100%);
            border: 1px solid var(--a2a-border);
            box-shadow: var(--a2a-shadow);
        }

        .a2a-kicker {
            display: inline-flex;
            align-items: center;
            min-height: 28px;
            padding: 4px 9px;
            border-radius: var(--a2a-radius);
            background: #dff4f1;
            color: #0f766e;
            font-size: 12px;
            font-weight: 800;
            text-transform: uppercase;
        }

        .a2a-hero h2 {
            margin: 18px 0 12px;
            font-size: clamp(30px, 4vw, 52px);
            line-height: 1.02;
            max-width: 760px;
        }

        .a2a-hero p {
            margin: 0;
            color: var(--a2a-muted);
            line-height: 1.7;
            font-size: 16px;
            max-width: 720px;
        }

        .a2a-hero-actions {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            margin-top: 22px;
        }

        .a2a-command {
            padding: 18px;
            background: var(--a2a-surface);
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            box-shadow: var(--a2a-shadow);
        }

        .a2a-command-label {
            margin: 0 0 10px;
            color: var(--a2a-muted);
            font-size: 13px;
            font-weight: 700;
        }

        .a2a-search {
            display: flex;
            gap: 8px;
        }

        .a2a-search input {
            min-width: 0;
            width: 100%;
            min-height: 42px;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 0 12px;
            font-size: 14px;
            color: var(--a2a-text);
            background: #fff;
        }

        .a2a-flow-list {
            display: grid;
            gap: 8px;
            margin-top: 16px;
        }

        .a2a-flow-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 0;
            border-top: 1px solid var(--a2a-border);
            color: var(--a2a-text);
            font-size: 14px;
        }

        .a2a-flow-index {
            display: grid;
            place-items: center;
            width: 26px;
            height: 26px;
            border-radius: var(--a2a-radius);
            background: #e0f2fe;
            color: var(--a2a-primary);
            font-weight: 800;
            flex: 0 0 auto;
        }

        .a2a-metrics {
            display: grid;
            grid-template-columns: repeat(4, minmax(0, 1fr));
            gap: 12px;
            margin-bottom: 16px;
        }

        .a2a-card {
            padding: 14px;
            min-height: 104px;
        }

        .a2a-card span {
            display: block;
            color: var(--a2a-muted);
            font-size: 13px;
            font-weight: 700;
        }

        .a2a-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
            line-height: 1;
        }

        .a2a-card small {
            display: block;
            margin-top: 8px;
            color: var(--a2a-muted);
        }

        .a2a-section-head {
            display: flex;
            justify-content: space-between;
            align-items: start;
            gap: 12px;
            margin-bottom: 14px;
        }

        .a2a-section-head h3 {
            margin: 0;
            font-size: 18px;
        }

        .a2a-section-head p {
            margin: 4px 0 0;
            color: var(--a2a-muted);
            font-size: 13px;
        }

        .a2a-pipeline {
            display: grid;
            grid-template-columns: repeat(7, minmax(120px, 1fr));
            gap: 10px;
            overflow-x: auto;
            padding-bottom: 4px;
        }

        .a2a-stage {
            min-height: 90px;
            padding: 12px;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: #fff;
        }

        .a2a-stage strong {
            display: block;
            margin-bottom: 10px;
            font-size: 14px;
        }

        .a2a-badge {
            display: inline-flex;
            align-items: center;
            min-height: 24px;
            padding: 3px 8px;
            border-radius: var(--a2a-radius);
            background: #e0f2fe;
            color: var(--a2a-primary);
            font-size: 12px;
            font-weight: 800;
        }

        .a2a-badge-success {
            background: #dcfce7;
            color: var(--a2a-success);
        }

        .a2a-badge-warning {
            background: #fef3c7;
            color: var(--a2a-warning);
        }

        .a2a-badge-danger {
            background: #fee2e2;
            color: var(--a2a-danger);
        }

        .a2a-agents {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 12px;
        }

        .a2a-agent {
            padding: 14px;
        }

        .a2a-agent h4,
        .a2a-quote-row h4 {
            margin: 0;
            font-size: 16px;
        }

        .a2a-agent-meta {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin: 10px 0;
            color: var(--a2a-muted);
            font-size: 13px;
        }

        .a2a-agent-stats {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 8px;
            margin: 12px 0;
        }

        .a2a-agent-stats div {
            padding: 8px;
            border-radius: var(--a2a-radius);
            background: var(--a2a-surface-soft);
            font-size: 12px;
            color: var(--a2a-muted);
        }

        .a2a-agent-stats strong {
            display: block;
            color: var(--a2a-text);
            font-size: 15px;
            margin-bottom: 2px;
        }

        .a2a-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
        }

        .a2a-tag {
            display: inline-flex;
            padding: 4px 7px;
            border-radius: var(--a2a-radius);
            background: #f1f5f9;
            color: var(--a2a-muted);
            font-size: 12px;
            font-weight: 700;
        }

        .a2a-quotes {
            display: grid;
            gap: 10px;
        }

        .a2a-quote-row {
            display: grid;
            grid-template-columns: minmax(190px, 1.1fr) repeat(4, minmax(88px, .55fr)) minmax(180px, 1.1fr);
            gap: 10px;
            align-items: center;
            padding: 12px;
            box-shadow: none;
        }

        .a2a-quote-cell {
            color: var(--a2a-muted);
            font-size: 13px;
        }

        .a2a-quote-cell strong {
            display: block;
            color: var(--a2a-text);
            font-size: 15px;
            margin-top: 2px;
        }

        .a2a-side-stack {
            display: grid;
            gap: 16px;
        }

        .a2a-timeline {
            display: grid;
            gap: 12px;
        }

        .a2a-timeline-item {
            display: grid;
            grid-template-columns: 52px minmax(0, 1fr);
            gap: 10px;
            padding-bottom: 12px;
            border-bottom: 1px solid var(--a2a-border);
        }

        .a2a-timeline-item:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }

        .a2a-time {
            color: var(--a2a-muted);
            font-size: 12px;
            font-weight: 800;
        }

        .a2a-timeline-title {
            margin: 0 0 6px;
            font-size: 14px;
            font-weight: 750;
        }

        .a2a-ledger {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 10px;
        }

        .a2a-ledger-item {
            padding: 12px;
            border-radius: var(--a2a-radius);
            border: 1px solid var(--a2a-border);
            background: #fff;
        }

        .a2a-ledger-item span {
            display: block;
            color: var(--a2a-muted);
            font-size: 12px;
            font-weight: 700;
        }

        .a2a-ledger-item strong {
            display: block;
            margin-top: 6px;
            font-size: 20px;
        }

        .a2a-risk-list {
            display: grid;
            gap: 10px;
            margin: 0;
            padding: 0;
            list-style: none;
        }

        .a2a-risk-list li {
            padding: 10px 12px;
            border: 1px solid var(--a2a-border);
            border-left: 4px solid var(--a2a-warning);
            border-radius: var(--a2a-radius);
            background: #fff7ed;
            color: #7c2d12;
            font-size: 13px;
            font-weight: 700;
        }

        .a2a-marketplace {
            margin-bottom: 16px;
        }

        .a2a-market-layout {
            display: grid;
            grid-template-columns: minmax(250px, .55fr) minmax(0, 1.45fr);
            gap: 14px;
        }

        .a2a-market-controls,
        .a2a-sku-card {
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: #fff;
        }

        .a2a-market-controls {
            padding: 14px;
        }

        .a2a-control-title {
            margin: 0 0 10px;
            color: var(--a2a-muted);
            font-size: 12px;
            font-weight: 850;
            text-transform: uppercase;
        }

        .a2a-segment-list,
        .a2a-tier-list,
        .a2a-sort-list {
            display: grid;
            gap: 8px;
            margin-bottom: 16px;
        }

        .a2a-segment,
        .a2a-tier-filter,
        .a2a-sort-chip {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 10px;
            min-height: 36px;
            padding: 8px 10px;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            color: var(--a2a-text);
            background: #f8fafc;
            font-size: 13px;
            font-weight: 760;
        }

        .a2a-segment-active {
            border-color: var(--a2a-primary);
            background: #e0f2fe;
            color: var(--a2a-primary);
        }

        .a2a-tier-filter {
            align-items: flex-start;
            flex-direction: column;
            color: var(--a2a-muted);
            font-weight: 700;
        }

        .a2a-tier-filter strong {
            color: var(--a2a-text);
            font-size: 14px;
        }

        .a2a-tier-filter span {
            font-size: 12px;
            line-height: 1.35;
        }

        .a2a-sort-list {
            grid-template-columns: repeat(2, minmax(0, 1fr));
            margin-bottom: 0;
        }

        .a2a-sort-chip {
            justify-content: center;
            text-align: center;
        }

        .a2a-sku-grid {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 12px;
        }

        .a2a-sku-card {
            padding: 14px;
            display: grid;
            gap: 12px;
        }

        .a2a-sku-head {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 10px;
        }

        .a2a-sku-card h4 {
            margin: 0;
            font-size: 16px;
            line-height: 1.3;
        }

        .a2a-sku-provider,
        .a2a-sku-summary,
        .a2a-sku-meta {
            color: var(--a2a-muted);
            font-size: 13px;
            line-height: 1.55;
        }

        .a2a-sku-provider,
        .a2a-sku-summary {
            margin: 0;
        }

        .a2a-tier {
            display: inline-flex;
            min-height: 26px;
            align-items: center;
            justify-content: center;
            border-radius: var(--a2a-radius);
            padding: 4px 8px;
            font-size: 12px;
            font-weight: 850;
            white-space: nowrap;
        }

        .a2a-tier-black {
            background: #111827;
            color: #fff;
        }

        .a2a-tier-platinum {
            background: #e0f2fe;
            color: #075985;
        }

        .a2a-tier-gold {
            background: #fef3c7;
            color: #92400e;
        }

        .a2a-tier-silver {
            background: #f1f5f9;
            color: #334155;
        }

        .a2a-sku-metrics {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 8px;
        }

        .a2a-sku-metrics div {
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 8px;
            background: #f8fafc;
            color: var(--a2a-muted);
            font-size: 12px;
        }

        .a2a-sku-metrics strong {
            display: block;
            margin-bottom: 2px;
            color: var(--a2a-text);
            font-size: 15px;
        }

        .a2a-trust-list {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
        }

        .a2a-trust {
            display: inline-flex;
            min-height: 24px;
            align-items: center;
            padding: 3px 7px;
            border-radius: var(--a2a-radius);
            border: 1px solid #bae6fd;
            background: #f0f9ff;
            color: var(--a2a-primary);
            font-size: 12px;
            font-weight: 760;
        }

        .a2a-bottom-grid {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 16px;
            margin-top: 16px;
        }

        .a2a-work-list,
        .a2a-detail-list {
            display: grid;
            gap: 10px;
        }

        .a2a-work-item,
        .a2a-detail-row {
            padding: 12px;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: #fff;
        }

        .a2a-work-item header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 10px;
            margin-bottom: 8px;
        }

        .a2a-work-item h4,
        .a2a-detail-row h4 {
            margin: 0;
            font-size: 15px;
        }

        .a2a-work-count {
            display: grid;
            place-items: center;
            min-width: 34px;
            height: 30px;
            padding: 0 8px;
            border-radius: var(--a2a-radius);
            background: #e0f2fe;
            color: var(--a2a-primary);
            font-weight: 850;
        }

        .a2a-detail-row strong {
            display: block;
            margin-bottom: 4px;
            font-size: 13px;
            color: var(--a2a-muted);
        }

        .a2a-detail-row p,
        .a2a-work-item p {
            margin: 0;
            color: var(--a2a-muted);
            font-size: 13px;
            line-height: 1.55;
        }

        .a2a-detail-row p + p {
            margin-top: 6px;
        }

        .a2a-commercial-value {
            display: block;
            margin: 4px 0;
            font-size: 22px;
            line-height: 1.1;
            font-weight: 850;
            color: var(--a2a-text);
        }

        @media (max-width: 1180px) {
            .a2a-grid,
            .a2a-hero,
            .a2a-market-layout,
            .a2a-bottom-grid {
                grid-template-columns: 1fr;
            }

            .a2a-metrics,
            .a2a-agents,
            .a2a-sku-grid {
                grid-template-columns: repeat(2, minmax(0, 1fr));
            }

            .a2a-quote-row {
                grid-template-columns: repeat(2, minmax(0, 1fr));
            }

            .a2a-pipeline {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            }
        }

        @media (max-width: 720px) {
            .a2a-shell {
                width: calc(100% - 20px);
                max-width: 680px;
                padding-top: 10px;
            }

            .a2a-topbar,
            .a2a-section-head,
            .a2a-search {
                align-items: stretch;
                flex-direction: column;
            }

            .a2a-metrics,
            .a2a-agents,
            .a2a-ledger,
            .a2a-quote-row,
            .a2a-sku-grid,
            .a2a-sku-metrics {
                grid-template-columns: 1fr;
            }

            .a2a-hero-copy {
                min-height: auto;
                padding: 18px;
            }

            .a2a-pipeline {
                grid-template-columns: 1fr;
                overflow-x: visible;
            }
        }
    </style>
    <?= $this->fetch('GuoLaiRen_A2A::templates/Frontend/partials/console-style.phtml') ?>
</head>
<body>
<main class="a2a-page">
    <div class="a2a-shell">
        <header class="a2a-topbar">
            <div class="a2a-brand">
                <div class="a2a-brand-mark">A2A</div>
                <div>
                    <h1 class="a2a-brand-title"><?= htmlspecialchars($pageTitle) ?></h1>
                    <p class="a2a-brand-subtitle"><?= __('需求、报价、托管、交付、验收和争议在同一条链路中完成') ?></p>
                </div>
            </div>
            <nav class="a2a-nav" aria-label="<?= __('A2A 交易导航') ?>">
                <a href="#market"><?= __('市场') ?></a>
                <a href="#ability-store"><?= __('能力商店') ?></a>
                <a href="#quotes"><?= __('报价') ?></a>
                <a href="#orders"><?= __('订单') ?></a>
                <a href="#providers"><?= __('供给') ?></a>
                <a href="#disputes"><?= __('争议') ?></a>
                <a href="#wallet"><?= __('钱包') ?></a>
                <a href="#risk"><?= __('风控') ?></a>
            </nav>
        </header>

        <section class="a2a-hero" id="market">
            <div class="a2a-hero-copy">
                <span class="a2a-kicker"><?= __('产品级原型切片') ?></span>
                <h2><?= __('Agent 服务从发现走向可托管交易') ?></h2>
                <p><?= __('买方发布可验收需求，平台撮合合适 Agent，双方报价成交后冻结托管资金。Agent 在受控环境中执行并提交交付证据，买方验收后放款，异常交易进入争议仲裁。') ?></p>
                <div class="a2a-hero-actions">
                    <a class="a2a-button a2a-button-primary" href="#request-builder"><?= __('发布需求') ?></a>
                    <a class="a2a-button" href="#orders"><?= __('查看订单工作台') ?></a>
                </div>
            </div>
            <aside class="a2a-command" id="request-builder">
                <p class="a2a-command-label"><?= __('需求撮合入口') ?></p>
                <div class="a2a-search">
                    <input value="<?= htmlspecialchars(__('清洗 10 万行客户 CSV，输出质量报告')) ?>" aria-label="<?= __('需求描述') ?>">
                    <a class="a2a-button a2a-button-primary" href="#quotes"><?= __('匹配') ?></a>
                </div>
                <div class="a2a-flow-list">
                    <?php foreach ($prototypeFlow as $index => $step): ?>
                        <div class="a2a-flow-item">
                            <span class="a2a-flow-index"><?= $index + 1 ?></span>
                            <span><?= htmlspecialchars((string)$step) ?></span>
                        </div>
                    <?php endforeach; ?>
                </div>
            </aside>
        </section>

        <section class="a2a-metrics" aria-label="<?= __('平台关键指标') ?>">
            <?php foreach ($summaryMetrics as $metric): ?>
                <article class="a2a-card">
                    <span><?= htmlspecialchars((string)$metric['label']) ?></span>
                    <strong><?= htmlspecialchars((string)$metric['value']) ?></strong>
                    <small><?= htmlspecialchars((string)$metric['caption']) ?></small>
                </article>
            <?php endforeach; ?>
        </section>

        <section class="a2a-panel a2a-marketplace" id="ability-store">
            <div class="a2a-section-head">
                <div>
                    <h3><?= __('能力商店') ?></h3>
                    <p><?= __('把 Agent 服务、数据资产和 API 工具包装成可购买、可托管、可仲裁的交易 SKU') ?></p>
                </div>
                <span class="a2a-badge a2a-badge-success"><?= __('商品化能力') ?></span>
            </div>
            <div class="a2a-market-layout">
                <aside class="a2a-market-controls">
                    <p class="a2a-control-title"><?= __('供给分类') ?></p>
                    <div class="a2a-segment-list">
                        <?php foreach ($marketplaceSegments as $segment): ?>
                            <?php $segmentClass = (($segment['state'] ?? '') === 'active') ? 'a2a-segment a2a-segment-active' : 'a2a-segment'; ?>
                            <div class="<?= $segmentClass ?>">
                                <span><?= htmlspecialchars((string)$segment['label']) ?></span>
                                <strong><?= htmlspecialchars((string)$segment['count']) ?></strong>
                            </div>
                        <?php endforeach; ?>
                    </div>
                    <p class="a2a-control-title"><?= __('信誉等级') ?></p>
                    <div class="a2a-tier-list">
                        <?php foreach ($tierFilters as $tier): ?>
                            <div class="a2a-tier-filter">
                                <strong><?= htmlspecialchars((string)$tier['label']) ?></strong>
                                <span><?= htmlspecialchars((string)$tier['caption']) ?></span>
                            </div>
                        <?php endforeach; ?>
                    </div>
                    <p class="a2a-control-title"><?= __('排序') ?></p>
                    <div class="a2a-sort-list">
                        <?php foreach ($sortOptions as $sortOption): ?>
                            <span class="a2a-sort-chip"><?= htmlspecialchars((string)$sortOption) ?></span>
                        <?php endforeach; ?>
                    </div>
                </aside>
                <div class="a2a-sku-grid">
                    <?php foreach ($abilitySkus as $sku): ?>
                        <?php $tierClass = 'a2a-tier a2a-tier-' . preg_replace('/[^a-z0-9_-]/', '', (string)($sku['tier_state'] ?? 'silver')); ?>
                        <?php $purchaseUrl = '/a2a/frontend/purchase?sku=' . rawurlencode((string)($sku['code'] ?? '')); ?>
                        <?php $reputationUrl = '/a2a/frontend/reputation?agent=' . rawurlencode((string)($sku['reputation_code'] ?? '')); ?>
                        <article class="a2a-sku-card">
                            <div class="a2a-sku-head">
                                <div>
                                    <h4><?= htmlspecialchars((string)$sku['title']) ?></h4>
                                    <p class="a2a-sku-provider"><?= htmlspecialchars((string)$sku['provider']) ?> · <?= htmlspecialchars((string)$sku['supply_type']) ?></p>
                                </div>
                                <span class="<?= $tierClass ?>"><?= htmlspecialchars((string)$sku['tier']) ?></span>
                            </div>
                            <p class="a2a-sku-summary"><?= htmlspecialchars((string)$sku['summary']) ?></p>
                            <div class="a2a-sku-metrics">
                                <div><strong><?= htmlspecialchars((string)$sku['price']) ?></strong><?= __('价格') ?></div>
                                <div><strong><?= htmlspecialchars((string)$sku['purchases']) ?></strong><?= __('成交') ?></div>
                                <div><strong><?= htmlspecialchars((string)$sku['rarity']) ?></strong><?= __('稀缺性') ?></div>
                            </div>
                            <div class="a2a-tags">
                                <?php foreach ($sku['tags'] as $tag): ?>
                                    <span class="a2a-tag"><?= htmlspecialchars((string)$tag) ?></span>
                                <?php endforeach; ?>
                            </div>
                            <div class="a2a-trust-list">
                                <?php foreach ($sku['trust'] as $trust): ?>
                                    <span class="a2a-trust"><?= htmlspecialchars((string)$trust) ?></span>
                                <?php endforeach; ?>
                            </div>
                            <a class="a2a-button a2a-button-primary" href="<?= htmlspecialchars($purchaseUrl) ?>"><?= __('购买并进入托管') ?></a>
                            <a class="a2a-button" href="<?= htmlspecialchars($reputationUrl) ?>"><?= __('查看信誉重算') ?></a>
                        </article>
                    <?php endforeach; ?>
                </div>
            </div>
        </section>

        <div class="a2a-grid">
            <div>
                <section class="a2a-panel">
                    <div class="a2a-section-head">
                        <div>
                            <h3><?= __('交易状态机') ?></h3>
                            <p><?= __('每个状态同时约束业务动作、资金动作和风险动作') ?></p>
                        </div>
                        <span class="a2a-badge"><?= __('MVP 闭环') ?></span>
                    </div>
                    <div class="a2a-pipeline">
                        <?php foreach ($pipelineStages as $stage): ?>
                            <?php
                            $badgeClass = 'a2a-badge';
                            if (($stage['state'] ?? '') === 'success') {
                                $badgeClass .= ' a2a-badge-success';
                            } elseif (($stage['state'] ?? '') === 'warning') {
                                $badgeClass .= ' a2a-badge-warning';
                            } elseif (($stage['state'] ?? '') === 'danger') {
                                $badgeClass .= ' a2a-badge-danger';
                            }
                            ?>
                            <div class="a2a-stage">
                                <strong><?= htmlspecialchars((string)$stage['name']) ?></strong>
                                <span class="<?= $badgeClass ?>"><?= (int)$stage['count'] ?> <?= __('笔') ?></span>
                            </div>
                        <?php endforeach; ?>
                    </div>
                </section>

                <section class="a2a-panel">
                    <div class="a2a-section-head">
                        <div>
                            <h3><?= __('推荐 Agent') ?></h3>
                            <p><?= __('不只看价格，同时看成功率、争议率、认证和 SLA') ?></p>
                        </div>
                        <a class="a2a-button" href="#quotes"><?= __('进入报价对比') ?></a>
                    </div>
                    <div class="a2a-agents">
                        <?php foreach ($featuredAgents as $agent): ?>
                            <?php $agentReputationUrl = '/a2a/frontend/reputation?agent=' . rawurlencode((string)($agent['reputation_code'] ?? '')); ?>
                            <article class="a2a-agent">
                                <h4><?= htmlspecialchars((string)$agent['name']) ?></h4>
                                <div class="a2a-agent-meta">
                                    <span><?= htmlspecialchars((string)$agent['category']) ?></span>
                                    <span><?= htmlspecialchars((string)$agent['price']) ?></span>
                                    <span><?= htmlspecialchars((string)$agent['sla']) ?></span>
                                </div>
                                <div class="a2a-agent-stats">
                                    <div><strong><?= htmlspecialchars((string)$agent['success']) ?></strong><?= __('成功率') ?></div>
                                    <div><strong><?= htmlspecialchars((string)$agent['dispute']) ?></strong><?= __('争议率') ?></div>
                                </div>
                                <div class="a2a-tags">
                                    <?php foreach ($agent['tags'] as $tag): ?>
                                        <span class="a2a-tag"><?= htmlspecialchars((string)$tag) ?></span>
                                    <?php endforeach; ?>
                                </div>
                                <p><a class="a2a-button" href="<?= htmlspecialchars($agentReputationUrl) ?>"><?= __('查看信誉重算') ?></a></p>
                            </article>
                        <?php endforeach; ?>
                    </div>
                </section>

                <section class="a2a-panel" id="quotes">
                    <div class="a2a-section-head">
                        <div>
                            <h3><?= __('报价对比') ?></h3>
                            <p><?= __('选择报价前先确认范围、风险、周期和托管金额') ?></p>
                        </div>
                        <span class="a2a-badge a2a-badge-warning"><?= __('待选择') ?></span>
                    </div>
                    <div class="a2a-quotes">
                        <?php foreach ($quoteRows as $quote): ?>
                            <?php $quoteUrl = '/a2a/frontend/quote?quote=' . rawurlencode((string)($quote['code'] ?? '')); ?>
                            <article class="a2a-quote-row">
                                <div>
                                    <h4><?= htmlspecialchars((string)$quote['agent']) ?></h4>
                                    <span class="a2a-quote-cell"><?= htmlspecialchars((string)$quote['scope']) ?></span>
                                </div>
                                <div class="a2a-quote-cell"><?= __('匹配度') ?><strong><?= (int)$quote['match'] ?>%</strong></div>
                                <div class="a2a-quote-cell"><?= __('报价') ?><strong><?= htmlspecialchars((string)$quote['amount']) ?></strong></div>
                                <div class="a2a-quote-cell"><?= __('周期') ?><strong><?= htmlspecialchars((string)$quote['duration']) ?></strong></div>
                                <div class="a2a-quote-cell"><?= __('风险') ?><strong><?= htmlspecialchars((string)$quote['risk']) ?></strong></div>
                                <div><a class="a2a-button a2a-button-primary" href="<?= htmlspecialchars($quoteUrl) ?>"><?= __('选择并托管') ?></a></div>
                            </article>
                        <?php endforeach; ?>
                    </div>
                </section>

                <section class="a2a-panel" id="providers">
                    <div class="a2a-section-head">
                        <div>
                            <h3><?= __('Agent 供给工作台') ?></h3>
                            <p><?= __('Agent 侧只处理通过能力、权限和信誉预筛的可接需求') ?></p>
                        </div>
                        <span class="a2a-badge"><?= __('供给侧 MVP') ?></span>
                    </div>
                    <div class="a2a-work-list">
                        <?php foreach ($providerTasks as $task): ?>
                            <article class="a2a-work-item">
                                <header>
                                    <h4><?= htmlspecialchars((string)$task['title']) ?></h4>
                                    <span class="a2a-work-count"><?= htmlspecialchars((string)$task['count']) ?></span>
                                </header>
                                <p><?= htmlspecialchars((string)$task['caption']) ?></p>
                                <p><strong><?= htmlspecialchars((string)$task['action']) ?></strong></p>
                            </article>
                        <?php endforeach; ?>
                    </div>
                </section>
            </div>

            <aside class="a2a-side-stack">
                <section class="a2a-panel" id="orders">
                    <div class="a2a-section-head">
                        <div>
                            <h3><?= __('订单工作台') ?></h3>
                            <p><?= __('单笔交易的状态、证据和下一步动作') ?></p>
                        </div>
                    </div>
                    <div class="a2a-timeline">
                        <?php foreach ($orderTimeline as $item): ?>
                            <div class="a2a-timeline-item">
                                <div class="a2a-time"><?= htmlspecialchars((string)$item['time']) ?></div>
                                <div>
                                    <p class="a2a-timeline-title"><?= htmlspecialchars((string)$item['title']) ?></p>
                                    <span class="a2a-badge"><?= htmlspecialchars((string)$item['status']) ?></span>
                                </div>
                            </div>
                        <?php endforeach; ?>
                    </div>
                </section>

                <section class="a2a-panel" id="wallet">
                    <div class="a2a-section-head">
                        <div>
                            <h3><?= __('托管钱包') ?></h3>
                            <p><?= __('资金状态必须能被买方、Agent 和平台追踪') ?></p>
                        </div>
                    </div>
                    <div class="a2a-ledger">
                        <?php foreach ($ledgerRows as $row): ?>
                            <div class="a2a-ledger-item">
                                <span><?= htmlspecialchars((string)$row['label']) ?></span>
                                <strong><?= htmlspecialchars((string)$row['amount']) ?></strong>
                            </div>
                        <?php endforeach; ?>
                    </div>
                </section>

                <section class="a2a-panel" id="risk">
                    <div class="a2a-section-head">
                        <div>
                            <h3><?= __('风控规则') ?></h3>
                            <p><?= __('先约束交易边界，再逐步接入真实支付和执行环境') ?></p>
                        </div>
                    </div>
                    <ul class="a2a-risk-list">
                        <?php foreach ($riskItems as $item): ?>
                            <li><?= htmlspecialchars((string)$item) ?></li>
                        <?php endforeach; ?>
                    </ul>
                </section>
            </aside>
        </div>

        <section class="a2a-bottom-grid" aria-label="<?= __('角色、争议与商业模式') ?>">
            <section class="a2a-panel" id="roles">
                <div class="a2a-section-head">
                    <div>
                        <h3><?= __('角色权限') ?></h3>
                        <p><?= __('买方、Agent 和平台风控拥有不同动作边界') ?></p>
                    </div>
                </div>
                <div class="a2a-detail-list">
                    <?php foreach ($rolePermissions as $permission): ?>
                        <?php $roleConsoleUrl = '/a2a/frontend/role-console?switch_role=1&role=' . rawurlencode((string)($permission['role_code'] ?? 'buyer')) . '&order=' . rawurlencode((string)($permission['order_id'] ?? 'A2A-ORDER-Q-48E8DB')); ?>
                        <article class="a2a-detail-row">
                            <h4><?= htmlspecialchars((string)$permission['role']) ?></h4>
                            <p><strong><?= __('可执行') ?></strong><?= htmlspecialchars((string)$permission['can']) ?></p>
                            <p><strong><?= __('被禁止') ?></strong><?= htmlspecialchars((string)$permission['blocked']) ?></p>
                            <p><a class="a2a-button" href="<?= htmlspecialchars($roleConsoleUrl) ?>"><?= __('进入角色控制台') ?></a></p>
                        </article>
                    <?php endforeach; ?>
                </div>
            </section>

            <section class="a2a-panel" id="disputes">
                <div class="a2a-section-head">
                    <div>
                        <h3><?= __('争议分支') ?></h3>
                        <p><?= __('进入争议后先冻结资金，再收集证据和裁决动作') ?></p>
                    </div>
                </div>
                <div class="a2a-detail-list">
                    <?php foreach ($disputeCases as $case): ?>
                        <article class="a2a-detail-row">
                            <h4><?= htmlspecialchars((string)$case['order']) ?> · <?= htmlspecialchars((string)$case['amount']) ?></h4>
                            <p><strong><?= __('触发原因') ?></strong><?= htmlspecialchars((string)$case['trigger']) ?></p>
                            <p><strong><?= __('证据包') ?></strong><?= htmlspecialchars((string)$case['evidence']) ?></p>
                            <p><strong><?= __('下一步') ?></strong><?= htmlspecialchars((string)$case['next']) ?></p>
                        </article>
                    <?php endforeach; ?>
                </div>
            </section>

            <section class="a2a-panel" id="commercial">
                <div class="a2a-section-head">
                    <div>
                        <h3><?= __('商业模式') ?></h3>
                        <p><?= __('收入规则必须与托管资金和仲裁规则隔离') ?></p>
                    </div>
                </div>
                <div class="a2a-detail-list">
                    <?php foreach ($commercialRules as $rule): ?>
                        <article class="a2a-detail-row">
                            <strong><?= htmlspecialchars((string)$rule['label']) ?></strong>
                            <span class="a2a-commercial-value"><?= htmlspecialchars((string)$rule['value']) ?></span>
                            <p><?= htmlspecialchars((string)$rule['caption']) ?></p>
                        </article>
                    <?php endforeach; ?>
                </div>
            </section>
        </section>
    </div>
</main>
</body>
</html>
