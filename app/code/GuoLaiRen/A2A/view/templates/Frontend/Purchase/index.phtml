<?php
/** @var \Weline\Framework\View\Template $this */
$pageTitle = (string)($this->getData('page_title') ?? __('A2A 托管订单草稿'));
$hasError = (bool)($this->getData('has_error') ?? false);
$errorMessage = (string)($this->getData('error_message') ?? '');
$sku = $this->getData('sku') ?? [];
$escrow = $this->getData('escrow') ?? [];
$ledgerPreview = $this->getData('ledger_preview') ?? [];
$acceptanceRules = $this->getData('acceptance_rules') ?? [];
$nextActions = $this->getData('next_actions') ?? [];
$persisted = $this->getData('persisted') ?? [];
$isConfirmed = (string)$this->getData('status') === (string)__('托管已确认，正式订单已生成');
?>
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($pageTitle) ?></title>
    <style>
        :root {
            --a2a-bg: #f6f8fb;
            --a2a-surface: #ffffff;
            --a2a-border: #d8e0ea;
            --a2a-text: #172033;
            --a2a-muted: #64748b;
            --a2a-primary: #0369a1;
            --a2a-primary-strong: #075985;
            --a2a-success: #15803d;
            --a2a-danger: #b91c1c;
            --a2a-radius: 8px;
            --a2a-shadow: 0 16px 40px rgba(15, 23, 42, 0.08);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            background: var(--a2a-bg);
            color: var(--a2a-text);
            font-family: "Plus Jakarta Sans", "Segoe UI", Arial, sans-serif;
            letter-spacing: 0;
        }

        .a2a-purchase-shell {
            width: min(1120px, calc(100% - 32px));
            margin: 0 auto;
            padding: 28px 0 44px;
        }

        .a2a-purchase-nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
            margin-bottom: 18px;
        }

        .a2a-purchase-nav a,
        .a2a-button {
            min-height: 38px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 8px 12px;
            background: var(--a2a-surface);
            color: var(--a2a-text);
            font-size: 14px;
            font-weight: 700;
            text-decoration: none;
        }

        .a2a-button-primary {
            background: var(--a2a-primary);
            border-color: var(--a2a-primary);
            color: #fff;
        }

        .a2a-button-primary:hover {
            background: var(--a2a-primary-strong);
            color: #fff;
        }

        .a2a-confirm-button {
            width: 100%;
            margin-bottom: 10px;
        }

        .a2a-purchase-grid {
            display: grid;
            grid-template-columns: minmax(0, 1.2fr) minmax(320px, .8fr);
            gap: 16px;
            align-items: start;
        }

        .a2a-panel {
            min-width: 0;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: var(--a2a-surface);
            box-shadow: var(--a2a-shadow);
            padding: 18px;
        }

        .a2a-panel + .a2a-panel {
            margin-top: 16px;
        }

        .a2a-kicker {
            display: inline-flex;
            min-height: 28px;
            align-items: center;
            border-radius: var(--a2a-radius);
            padding: 4px 9px;
            background: #e0f2fe;
            color: var(--a2a-primary);
            font-size: 12px;
            font-weight: 800;
        }

        h1,
        h2,
        h3,
        p {
            margin-top: 0;
        }

        h1 {
            margin: 14px 0 10px;
            font-size: clamp(28px, 4vw, 44px);
            line-height: 1.08;
        }

        .a2a-muted {
            color: var(--a2a-muted);
            line-height: 1.65;
        }

        .a2a-order-id {
            display: inline-flex;
            min-height: 32px;
            align-items: center;
            border-radius: var(--a2a-radius);
            padding: 5px 9px;
            background: #f1f5f9;
            color: var(--a2a-text);
            font-weight: 800;
        }

        .a2a-summary {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 10px;
            margin-top: 16px;
        }

        .a2a-summary div,
        .a2a-ledger-row {
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 10px;
            background: #f8fafc;
        }

        .a2a-summary strong,
        .a2a-ledger-row strong {
            display: block;
            margin-bottom: 4px;
            color: var(--a2a-text);
            font-size: 18px;
        }

        .a2a-summary span,
        .a2a-ledger-row span {
            color: var(--a2a-muted);
            font-size: 13px;
        }

        .a2a-list {
            display: grid;
            gap: 10px;
            margin: 0;
            padding: 0;
            list-style: none;
        }

        .a2a-list li {
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 10px;
            background: #f8fafc;
            line-height: 1.5;
        }

        .a2a-persisted-strip {
            display: grid;
            grid-template-columns: 1.2fr repeat(3, minmax(0, .6fr));
            gap: 8px;
            margin-top: 14px;
            padding: 10px;
            border: 1px solid #bbf7d0;
            border-radius: var(--a2a-radius);
            background: #f0fdf4;
        }

        .a2a-persisted-strip strong {
            display: block;
            margin-bottom: 2px;
            color: var(--a2a-success);
            font-size: 14px;
        }

        .a2a-persisted-strip span {
            color: var(--a2a-muted);
            font-size: 12px;
        }

        .a2a-error {
            border-color: #fecaca;
            background: #fff1f2;
            color: var(--a2a-danger);
        }

        @media (max-width: 860px) {
            .a2a-purchase-grid,
            .a2a-summary,
            .a2a-persisted-strip {
                grid-template-columns: 1fr;
            }

            .a2a-purchase-nav {
                align-items: flex-start;
                flex-direction: column;
            }
        }
    </style>
    <?= $this->fetch('GuoLaiRen_A2A::templates/Frontend/partials/console-style.phtml') ?>
</head>
<body>
    <main class="a2a-purchase-shell">
        <nav class="a2a-purchase-nav" aria-label="<?= __('A2A 托管订单导航') ?>">
            <a href="/a2a/frontend#ability-store"><?= __('返回能力商店') ?></a>
            <a class="a2a-button a2a-button-primary" href="/a2a/frontend#quotes"><?= __('对比报价') ?></a>
        </nav>

        <?php if ($hasError): ?>
            <section class="a2a-panel a2a-error">
                <span class="a2a-kicker"><?= __('无法创建订单草稿') ?></span>
                <h1><?= __('能力 SKU 不可购买') ?></h1>
                <p><?= htmlspecialchars($errorMessage) ?></p>
                <a class="a2a-button" href="/a2a/frontend#ability-store"><?= __('返回能力商店') ?></a>
            </section>
        <?php else: ?>
            <section class="a2a-panel">
                <span class="a2a-kicker"><?= __('托管订单草稿') ?></span>
                <h1><?= htmlspecialchars((string)$sku['title']) ?></h1>
                <p class="a2a-muted">
                    <?= htmlspecialchars((string)$sku['provider']) ?> · <?= htmlspecialchars((string)$sku['supply_type']) ?> · <?= htmlspecialchars((string)$sku['tier']) ?>
                </p>
                <span class="a2a-order-id"><?= htmlspecialchars((string)$this->getData('order_id')) ?></span>
                <div class="a2a-summary">
                    <div><strong><?= htmlspecialchars((string)$escrow['amount']) ?></strong><span><?= __('托管金额') ?></span></div>
                    <div><strong><?= htmlspecialchars((string)$escrow['platform_fee']) ?></strong><span><?= __('平台服务费预估') ?></span></div>
                    <div><strong><?= htmlspecialchars((string)$this->getData('status')) ?></strong><span><?= __('当前状态') ?></span></div>
                </div>
                <?php if (is_array($persisted) && $persisted): ?>
                    <div class="a2a-persisted-strip" aria-label="<?= __('持久化状态') ?>">
                        <div><strong><?= htmlspecialchars((string)$persisted['status']) ?></strong><span><?= __('持久化状态') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['order_draft_id']) ?></strong><span><?= __('订单草稿主键') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['capability_sku_id']) ?></strong><span><?= __('能力 SKU 主键') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['ledger_rows']) ?></strong><span><?= __('账本行') ?></span></div>
                    </div>
                <?php endif; ?>
            </section>

            <div class="a2a-purchase-grid">
                <section class="a2a-panel">
                    <h2><?= __('验收与风控规则') ?></h2>
                    <p class="a2a-muted"><?= __('下单进入托管前，平台先锁定验收口径和资金处理路径。') ?></p>
                    <ul class="a2a-list">
                        <?php foreach ($acceptanceRules as $rule): ?>
                            <li><?= htmlspecialchars((string)$rule) ?></li>
                        <?php endforeach; ?>
                    </ul>
                </section>
                <aside>
                    <section class="a2a-panel">
                        <h2><?= __('托管账本预览') ?></h2>
                        <div class="a2a-list">
                            <?php foreach ($ledgerPreview as $row): ?>
                                <div class="a2a-ledger-row">
                                    <strong><?= htmlspecialchars((string)$row['value']) ?></strong>
                                    <span><?= htmlspecialchars((string)$row['label']) ?></span>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    </section>
                    <section class="a2a-panel">
                        <h2><?= __('下一步动作') ?></h2>
                        <a class="a2a-button a2a-button-primary a2a-confirm-button" href="/a2a/frontend/confirm?draft=<?= urlencode((string)$this->getData('order_id')) ?>">
                            <?= $isConfirmed ? __('查看正式订单状态') : __('确认托管并生成正式订单') ?>
                        </a>
                        <ul class="a2a-list">
                            <?php foreach ($nextActions as $action): ?>
                                <li><?= htmlspecialchars((string)$action) ?></li>
                            <?php endforeach; ?>
                        </ul>
                    </section>
                </aside>
            </div>
        <?php endif; ?>
    </main>
</body>
</html>
