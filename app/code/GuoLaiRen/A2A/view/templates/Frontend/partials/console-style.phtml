<?php
/** @var \Weline\Framework\View\Template $this */
?>
<style>
    :root {
        --a2a-bg: #f4f6f8;
        --a2a-surface: #ffffff;
        --a2a-surface-soft: #f8fafc;
        --a2a-panel: #f8fafc;
        --a2a-border: #d6dde6;
        --a2a-border-strong: #9aa8b8;
        --a2a-text: #172033;
        --a2a-muted: #5f6f84;
        --a2a-primary: #0f6b8f;
        --a2a-primary-strong: #0b4f6a;
        --a2a-accent: #0f766e;
        --a2a-success: #167a3a;
        --a2a-warning: #946006;
        --a2a-danger: #b42318;
        --a2a-radius: 8px;
        --a2a-shadow: 0 10px 26px rgba(20, 31, 46, .07);
        --a2a-shadow-soft: 0 1px 2px rgba(20, 31, 46, .08);
        --a2a-focus: 0 0 0 3px rgba(15, 107, 143, .18);
    }

    html {
        background: var(--a2a-bg);
    }

    body {
        background: var(--a2a-bg);
        color: var(--a2a-text);
        font-family: Inter, "Segoe UI", "Microsoft YaHei", Arial, sans-serif;
        letter-spacing: 0;
        text-rendering: optimizeLegibility;
    }

    .a2a-page,
    .a2a-shell,
    .a2a-accept-shell,
    .a2a-ruling-shell,
    .a2a-confirm-shell,
    .a2a-delivery-shell,
    .a2a-scope-shell,
    .a2a-purchase-shell,
    .a2a-quote-shell,
    .a2a-reputation-shell,
    .a2a-role-shell,
    .a2a-case-shell,
    .a2a-wallet-shell {
        color: var(--a2a-text);
    }

    .a2a-shell,
    .a2a-accept-shell,
    .a2a-ruling-shell,
    .a2a-confirm-shell,
    .a2a-delivery-shell,
    .a2a-scope-shell,
    .a2a-purchase-shell,
    .a2a-quote-shell,
    .a2a-reputation-shell,
    .a2a-role-shell,
    .a2a-case-shell,
    .a2a-wallet-shell {
        width: min(1360px, calc(100% - 40px));
        padding: 22px 0 40px;
    }

    .a2a-panel,
    .a2a-card,
    .a2a-agent,
    .a2a-quote-row,
    .a2a-sku-card,
    .a2a-option,
    .a2a-work-item,
    .a2a-flow-item,
    .a2a-detail-row,
    .a2a-evidence-row,
    .a2a-action-row,
    .a2a-ledger-row,
    .a2a-ledger-item,
    .a2a-instruction-row,
    .a2a-contract-row,
    .a2a-summary-item,
    .a2a-metric,
    .a2a-field,
    .a2a-score-card,
    .a2a-decision-note,
    .a2a-risk-note {
        border-color: var(--a2a-border);
        border-radius: var(--a2a-radius);
        box-shadow: var(--a2a-shadow-soft);
        min-width: 0;
    }

    .a2a-panel {
        background: rgba(255, 255, 255, .96);
        padding: 20px;
    }

    .a2a-card,
    .a2a-summary-item,
    .a2a-metric,
    .a2a-detail-row,
    .a2a-evidence-row,
    .a2a-ledger-row,
    .a2a-ledger-item,
    .a2a-instruction-row,
    .a2a-contract-row,
    .a2a-action-row,
    .a2a-work-item,
    .a2a-flow-item,
    .a2a-field {
        background: var(--a2a-surface-soft);
        box-shadow: none;
    }

    .a2a-hero-copy,
    .a2a-command {
        border-color: var(--a2a-border);
        background: var(--a2a-surface);
        box-shadow: var(--a2a-shadow);
    }

    h1,
    h2,
    h3,
    p {
        letter-spacing: 0;
    }

    h1 {
        margin-bottom: 10px;
        font-size: 36px;
        line-height: 1.12;
    }

    h2 {
        font-size: 22px;
        line-height: 1.22;
    }

    h3 {
        font-size: 16px;
        line-height: 1.28;
    }

    .a2a-hero h2 {
        font-size: 42px;
        line-height: 1.06;
    }

    .a2a-muted,
    .a2a-card span,
    .a2a-summary-item span,
    .a2a-ledger-row span,
    .a2a-contract-row span,
    .a2a-instruction-row span,
    .a2a-meta-grid span {
        color: var(--a2a-muted);
    }

    .a2a-button,
    .a2a-nav a,
    .a2a-accept-nav a,
    .a2a-ruling-nav a,
    .a2a-confirm-nav a,
    .a2a-delivery-nav a,
    .a2a-scope-nav a,
    .a2a-purchase-nav a,
    .a2a-quote-nav a,
    .a2a-reputation-nav a,
    .a2a-case-nav a,
    .a2a-wallet-nav a,
    .a2a-role-nav a,
    .a2a-role-tab,
    .a2a-mode-tab,
    .a2a-option-tab,
    .a2a-tier-filter,
    .a2a-sort-chip,
    .a2a-segment {
        border-color: var(--a2a-border);
        background: var(--a2a-surface);
        color: var(--a2a-text);
        box-shadow: var(--a2a-shadow-soft);
        font-weight: 700;
        text-decoration: none;
        transition: background-color .18s ease, border-color .18s ease, color .18s ease, box-shadow .18s ease, transform .18s ease;
    }

    .a2a-button:hover,
    .a2a-nav a:hover,
    .a2a-accept-nav a:hover,
    .a2a-ruling-nav a:hover,
    .a2a-confirm-nav a:hover,
    .a2a-delivery-nav a:hover,
    .a2a-scope-nav a:hover,
    .a2a-purchase-nav a:hover,
    .a2a-quote-nav a:hover,
    .a2a-reputation-nav a:hover,
    .a2a-case-nav a:hover,
    .a2a-wallet-nav a:hover,
    .a2a-role-nav a:hover,
    .a2a-role-tab:hover,
    .a2a-mode-tab:hover,
    .a2a-option-tab:hover,
    .a2a-tier-filter:hover,
    .a2a-sort-chip:hover,
    .a2a-segment:hover {
        border-color: var(--a2a-primary);
        color: var(--a2a-primary);
        box-shadow: var(--a2a-shadow);
        transform: translateY(-1px);
    }

    .a2a-button-primary,
    .a2a-role-tab-active,
    .a2a-mode-active,
    .a2a-option-active,
    .a2a-segment-active,
    .a2a-tier-filter-active,
    .a2a-sort-chip-active,
    .a2a-confirm-button,
    .a2a-scope-button,
    .a2a-delivery-action {
        border-color: var(--a2a-primary);
        background: var(--a2a-primary);
        color: #fff;
    }

    .a2a-button-primary:hover,
    .a2a-role-tab-active:hover,
    .a2a-mode-active:hover,
    .a2a-option-active:hover,
    .a2a-segment-active:hover,
    .a2a-tier-filter-active:hover,
    .a2a-sort-chip-active:hover,
    .a2a-confirm-button:hover,
    .a2a-scope-button:hover,
    .a2a-delivery-action:hover {
        border-color: var(--a2a-primary-strong);
        background: var(--a2a-primary-strong);
        color: #fff;
    }

    .a2a-button-danger {
        border-color: var(--a2a-danger);
        background: var(--a2a-danger);
        color: #fff;
    }

    .a2a-button-warning {
        border-color: #d69020;
        background: #f6c15d;
        color: #2d1b02;
    }

    .a2a-button-disabled,
    .a2a-option-locked {
        cursor: default;
        opacity: .64;
        transform: none;
    }

    .a2a-kicker,
    .a2a-state-pill,
    .a2a-public-id,
    .a2a-badge,
    .a2a-tag,
    .a2a-tier,
    .a2a-identity-badge {
        border: 1px solid rgba(15, 107, 143, .18);
        border-radius: var(--a2a-radius);
        background: #e9f5f8;
        color: var(--a2a-primary);
        font-weight: 800;
    }

    .a2a-public-id,
    .a2a-order-id {
        background: #eef2f7;
        color: var(--a2a-text);
        overflow-wrap: anywhere;
    }

    .a2a-state-success,
    .a2a-badge-success,
    .a2a-identity-badge-ready,
    .a2a-tier-gold,
    .a2a-tier-platinum {
        border-color: rgba(22, 122, 58, .18);
        background: #e8f6ee;
        color: var(--a2a-success);
    }

    .a2a-state-warning,
    .a2a-badge-warning,
    .a2a-tier-silver,
    .a2a-tier-risk {
        border-color: rgba(148, 96, 6, .22);
        background: #fff5df;
        color: var(--a2a-warning);
    }

    .a2a-state-danger,
    .a2a-badge-danger,
    .a2a-state-blocked,
    .a2a-forbidden,
    .a2a-tier-black {
        border-color: rgba(180, 35, 24, .18);
        background: #fff0ef;
        color: var(--a2a-danger);
    }

    .a2a-alert,
    .a2a-result {
        border-color: rgba(15, 107, 143, .22);
        background: #edf8fb;
        color: var(--a2a-primary-strong);
        box-shadow: none;
    }

    .a2a-error,
    .a2a-result-forbidden {
        border-color: rgba(180, 35, 24, .24);
        background: #fff4f3;
        color: var(--a2a-danger);
    }

    .a2a-persisted-strip {
        border-color: rgba(148, 96, 6, .24);
        background: #fff8ea;
        box-shadow: none;
    }

    .a2a-persisted-strip-review {
        border-color: rgba(15, 107, 143, .22);
        background: #edf8fb;
    }

    .a2a-persisted-strip-rework {
        border-color: rgba(180, 35, 24, .2);
        background: #fff4f3;
    }

    .a2a-grid,
    .a2a-accept-grid,
    .a2a-ruling-grid,
    .a2a-confirm-grid,
    .a2a-delivery-grid,
    .a2a-scope-grid,
    .a2a-purchase-grid,
    .a2a-quote-grid,
    .a2a-reputation-grid,
    .a2a-role-grid,
    .a2a-case-grid,
    .a2a-wallet-grid,
    .a2a-bottom-grid,
    .a2a-score-layout,
    .a2a-market-layout {
        gap: 18px;
    }

    .a2a-topbar,
    .a2a-accept-nav,
    .a2a-ruling-nav,
    .a2a-confirm-nav,
    .a2a-delivery-nav,
    .a2a-scope-nav,
    .a2a-purchase-nav,
    .a2a-quote-nav,
    .a2a-reputation-nav,
    .a2a-role-nav,
    .a2a-case-nav,
    .a2a-wallet-nav {
        border: 1px solid var(--a2a-border);
        border-radius: var(--a2a-radius);
        background: rgba(255, 255, 255, .92);
        box-shadow: var(--a2a-shadow-soft);
        margin-bottom: 18px;
        padding: 10px;
    }

    .a2a-agent:hover,
    .a2a-quote-row:hover,
    .a2a-sku-card:hover,
    .a2a-option:hover,
    .a2a-action-row:hover,
    .a2a-instruction-row:hover {
        border-color: var(--a2a-border-strong);
        box-shadow: var(--a2a-shadow);
    }

    .a2a-card strong,
    .a2a-summary-item strong,
    .a2a-ledger-row strong,
    .a2a-contract-row strong,
    .a2a-metric strong,
    .a2a-score-card strong {
        color: var(--a2a-text);
        overflow-wrap: anywhere;
    }

    .a2a-list,
    .a2a-ledger-list,
    .a2a-work-list,
    .a2a-risk-list,
    .a2a-evidence-list,
    .a2a-flow-list,
    .a2a-option-list,
    .a2a-action-list,
    .a2a-trust-list,
    .a2a-next-actions {
        gap: 10px;
    }

    .a2a-search input,
    .a2a-field input,
    .a2a-field textarea,
    .a2a-field select,
    input,
    textarea,
    select {
        border-color: var(--a2a-border);
        border-radius: var(--a2a-radius);
    }

    .a2a-button:focus-visible,
    .a2a-nav a:focus-visible,
    .a2a-accept-nav a:focus-visible,
    .a2a-ruling-nav a:focus-visible,
    .a2a-confirm-nav a:focus-visible,
    .a2a-delivery-nav a:focus-visible,
    .a2a-scope-nav a:focus-visible,
    .a2a-purchase-nav a:focus-visible,
    .a2a-quote-nav a:focus-visible,
    .a2a-reputation-nav a:focus-visible,
    .a2a-role-nav a:focus-visible,
    .a2a-case-nav a:focus-visible,
    .a2a-wallet-nav a:focus-visible,
    .a2a-role-tab:focus-visible,
    .a2a-mode-tab:focus-visible,
    .a2a-option-tab:focus-visible,
    input:focus-visible,
    textarea:focus-visible,
    select:focus-visible {
        outline: none;
        box-shadow: var(--a2a-focus);
    }

    @media (max-width: 960px) {
        .a2a-shell,
        .a2a-accept-shell,
        .a2a-ruling-shell,
        .a2a-confirm-shell,
        .a2a-delivery-shell,
        .a2a-scope-shell,
        .a2a-purchase-shell,
        .a2a-quote-shell,
        .a2a-reputation-shell,
        .a2a-role-shell,
        .a2a-case-shell,
        .a2a-wallet-shell {
            width: min(100% - 24px, 720px);
            padding: 12px 0 28px;
        }

        h1 {
            font-size: 28px;
        }

        h2,
        .a2a-hero h2 {
            font-size: 24px;
        }

        .a2a-panel {
            padding: 16px;
        }

        .a2a-topbar,
        .a2a-accept-nav,
        .a2a-ruling-nav,
        .a2a-confirm-nav,
        .a2a-delivery-nav,
        .a2a-scope-nav,
        .a2a-purchase-nav,
        .a2a-quote-nav,
        .a2a-reputation-nav,
        .a2a-role-nav,
        .a2a-case-nav,
        .a2a-wallet-nav {
            align-items: stretch;
            flex-direction: column;
        }

        .a2a-nav,
        .a2a-nav-links,
        .a2a-role-tabs,
        .a2a-mode-tabs,
        .a2a-option-actions,
        .a2a-action-links,
        .a2a-hero-actions {
            display: grid;
            grid-template-columns: 1fr;
            width: 100%;
        }

        .a2a-button,
        .a2a-nav a,
        .a2a-accept-nav a,
        .a2a-ruling-nav a,
        .a2a-confirm-nav a,
        .a2a-delivery-nav a,
        .a2a-scope-nav a,
        .a2a-purchase-nav a,
        .a2a-quote-nav a,
        .a2a-reputation-nav a,
        .a2a-role-nav a,
        .a2a-case-nav a,
        .a2a-wallet-nav a,
        .a2a-role-tab,
        .a2a-mode-tab,
        .a2a-option-tab {
            width: 100%;
            min-height: 42px;
            white-space: normal;
        }
    }

    @media (prefers-reduced-motion: reduce) {
        *,
        *::before,
        *::after {
            scroll-behavior: auto !important;
            transition-duration: .01ms !important;
            animation-duration: .01ms !important;
            animation-iteration-count: 1 !important;
        }

        .a2a-button:hover,
        .a2a-nav a:hover,
        .a2a-role-tab:hover,
        .a2a-mode-tab:hover,
        .a2a-option-tab:hover,
        .a2a-tier-filter:hover,
        .a2a-sort-chip:hover,
        .a2a-segment:hover {
            transform: none;
        }
    }
</style>
