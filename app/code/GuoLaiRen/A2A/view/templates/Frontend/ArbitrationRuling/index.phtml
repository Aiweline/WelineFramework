<?php
/** @var \Weline\Framework\View\Template $this */
$pageTitle = (string)($this->getData('page_title') ?? __('A2A 仲裁裁决与钱包指令'));
$hasError = (bool)($this->getData('has_error') ?? false);
$errorMessage = (string)($this->getData('error_message') ?? '');
$order = $this->getData('order') ?? [];
$rulingOptions = $this->getData('ruling_options') ?? [];
$amounts = $this->getData('amounts') ?? [];
$evidence = $this->getData('evidence') ?? [];
$walletPlan = $this->getData('wallet_plan') ?? [];
$walletInstructions = $this->getData('wallet_instructions') ?? [];
$ledgerRows = $this->getData('ledger_rows') ?? [];
$persisted = $this->getData('persisted') ?? [];
?>
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($pageTitle) ?></title>
    <style>
        :root {
            --a2a-bg: #f6f8fb;
            --a2a-surface: #ffffff;
            --a2a-border: #d8e0ea;
            --a2a-text: #172033;
            --a2a-muted: #64748b;
            --a2a-primary: #0369a1;
            --a2a-primary-strong: #075985;
            --a2a-warning: #a16207;
            --a2a-success: #15803d;
            --a2a-danger: #b91c1c;
            --a2a-radius: 8px;
            --a2a-shadow: 0 16px 40px rgba(15, 23, 42, 0.08);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            background: var(--a2a-bg);
            color: var(--a2a-text);
            font-family: "Plus Jakarta Sans", "Segoe UI", Arial, sans-serif;
            letter-spacing: 0;
        }

        .a2a-ruling-shell {
            width: min(1180px, calc(100% - 32px));
            margin: 0 auto;
            padding: 28px 0 44px;
        }

        .a2a-ruling-nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
            margin-bottom: 18px;
        }

        .a2a-button,
        .a2a-ruling-nav a,
        .a2a-option-tab {
            min-height: 38px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 8px 12px;
            background: var(--a2a-surface);
            color: var(--a2a-text);
            font-size: 14px;
            font-weight: 800;
            text-decoration: none;
        }

        .a2a-button-primary,
        .a2a-option-active {
            background: var(--a2a-primary);
            border-color: var(--a2a-primary);
            color: #fff;
        }

        .a2a-button-primary:hover,
        .a2a-option-active:hover {
            background: var(--a2a-primary-strong);
            color: #fff;
        }

        .a2a-panel {
            min-width: 0;
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            background: var(--a2a-surface);
            box-shadow: var(--a2a-shadow);
            padding: 18px;
        }

        .a2a-panel + .a2a-panel {
            margin-top: 16px;
        }

        .a2a-grid {
            display: grid;
            grid-template-columns: minmax(0, 1fr) minmax(320px, .85fr);
            gap: 16px;
            align-items: start;
        }

        .a2a-kicker,
        .a2a-state-pill {
            display: inline-flex;
            min-height: 28px;
            align-items: center;
            border-radius: var(--a2a-radius);
            padding: 4px 9px;
            background: #e0f2fe;
            color: var(--a2a-primary);
            font-size: 12px;
            font-weight: 800;
        }

        h1,
        h2,
        h3,
        p {
            margin-top: 0;
        }

        h1 {
            margin: 14px 0 10px;
            font-size: clamp(30px, 4vw, 46px);
            line-height: 1.08;
        }

        h2 {
            font-size: 22px;
        }

        .a2a-muted {
            color: var(--a2a-muted);
            line-height: 1.65;
        }

        .a2a-id-stack,
        .a2a-options {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 12px;
        }

        .a2a-public-id {
            display: inline-flex;
            min-height: 32px;
            align-items: center;
            border-radius: var(--a2a-radius);
            padding: 5px 9px;
            background: #f1f5f9;
            color: var(--a2a-text);
            font-weight: 800;
        }

        .a2a-summary {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 10px;
            margin-top: 16px;
        }

        .a2a-card,
        .a2a-list li,
        .a2a-ledger-row,
        .a2a-instruction-row {
            border: 1px solid var(--a2a-border);
            border-radius: var(--a2a-radius);
            padding: 10px;
            background: #f8fafc;
            line-height: 1.5;
        }

        .a2a-card strong,
        .a2a-ledger-row strong,
        .a2a-instruction-row strong {
            display: block;
            margin-bottom: 4px;
            color: var(--a2a-text);
            font-size: 18px;
        }

        .a2a-card span,
        .a2a-ledger-row span,
        .a2a-instruction-row span {
            color: var(--a2a-muted);
            font-size: 13px;
        }

        .a2a-list,
        .a2a-stack {
            display: grid;
            gap: 10px;
            margin: 0;
            padding: 0;
            list-style: none;
        }

        .a2a-persisted-strip {
            display: grid;
            grid-template-columns: 1.2fr repeat(3, minmax(0, .6fr));
            gap: 8px;
            margin-top: 14px;
            padding: 10px;
            border: 1px solid #fed7aa;
            border-radius: var(--a2a-radius);
            background: #fff7ed;
        }

        .a2a-persisted-strip strong {
            display: block;
            margin-bottom: 2px;
            color: var(--a2a-warning);
            font-size: 14px;
        }

        .a2a-persisted-strip span {
            color: var(--a2a-muted);
            font-size: 12px;
        }

        .a2a-error {
            border-color: #fecaca;
            background: #fff1f2;
            color: var(--a2a-danger);
        }

        .a2a-alert {
            margin-top: 14px;
            border: 1px solid #bae6fd;
            border-radius: var(--a2a-radius);
            padding: 12px;
            background: #f0f9ff;
            color: var(--a2a-primary-strong);
            font-weight: 800;
            line-height: 1.5;
        }

        @media (max-width: 860px) {
            .a2a-grid,
            .a2a-summary,
            .a2a-persisted-strip {
                grid-template-columns: 1fr;
            }

            .a2a-ruling-nav {
                align-items: flex-start;
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <main class="a2a-ruling-shell">
        <nav class="a2a-ruling-nav" aria-label="<?= __('A2A 仲裁裁决导航') ?>">
            <a href="/a2a/frontend/role-console?switch_role=1&amp;role=arbitrator&amp;order=<?= urlencode((string)$this->getData('order_id')) ?>"><?= __('返回仲裁员控制台') ?></a>
            <?php if (!$hasError): ?>
                <div class="a2a-options" style="margin-top: 0;">
                    <a class="a2a-button" href="/a2a/frontend/wallet-monitor?order=<?= urlencode((string)$this->getData('order_id')) ?>&amp;mode=dry_run_execute"><?= __('执行钱包 dry-run') ?></a>
                    <a class="a2a-button a2a-button-primary" href="/a2a/frontend/reputation?agent=dataclean-pro-agent"><?= __('裁决后重算信誉') ?></a>
                </div>
            <?php endif; ?>
        </nav>

        <?php if ($hasError): ?>
            <section class="a2a-panel a2a-error">
                <span class="a2a-kicker"><?= __('无法生成仲裁裁决') ?></span>
                <h1><?= __('仲裁裁决不可执行') ?></h1>
                <p><?= htmlspecialchars($errorMessage) ?></p>
                <a class="a2a-button" href="/a2a/frontend#roles"><?= __('返回角色权限') ?></a>
            </section>
        <?php else: ?>
            <section class="a2a-panel">
                <span class="a2a-kicker"><?= __('A2A 仲裁裁决与钱包指令') ?></span>
                <h1><?= htmlspecialchars((string)$this->getData('ruling_title')) ?></h1>
                <p class="a2a-muted">
                    <?= htmlspecialchars((string)$order['sku_title']) ?> · <?= htmlspecialchars((string)$order['provider']) ?>
                </p>
                <div class="a2a-id-stack">
                    <span class="a2a-public-id"><?= htmlspecialchars((string)$this->getData('order_id')) ?></span>
                    <span class="a2a-public-id"><?= htmlspecialchars((string)$this->getData('case_id')) ?></span>
                    <span class="a2a-public-id"><?= htmlspecialchars((string)$this->getData('ruling_id')) ?></span>
                    <span class="a2a-state-pill"><?= htmlspecialchars((string)$this->getData('ruling_status')) ?></span>
                </div>
                <div class="a2a-options" aria-label="<?= __('裁决类型切换') ?>">
                    <?php foreach ($rulingOptions as $option): ?>
                        <a class="a2a-option-tab <?= ($option['active'] ?? false) ? 'a2a-option-active' : '' ?>" href="<?= htmlspecialchars((string)$option['href']) ?>">
                            <?= htmlspecialchars((string)$option['label']) ?>
                        </a>
                    <?php endforeach; ?>
                </div>
                <div class="a2a-summary">
                    <?php foreach ($amounts as $amount): ?>
                        <div class="a2a-card">
                            <strong><?= htmlspecialchars((string)$amount['value']) ?></strong>
                            <span><?= htmlspecialchars((string)$amount['label']) ?></span>
                        </div>
                    <?php endforeach; ?>
                </div>
                <div class="a2a-alert"><?= __('钱包指令仅进入原型 dry-run 队列，不执行真实资金动作。') ?></div>
                <?php if (is_array($persisted) && $persisted): ?>
                    <div class="a2a-persisted-strip" aria-label="<?= __('持久化状态') ?>">
                        <div><strong><?= htmlspecialchars((string)$persisted['status']) ?></strong><span><?= __('持久化状态') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['arbitration_ruling_id']) ?></strong><span><?= __('裁决主键') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['wallet_instructions']) ?></strong><span><?= __('钱包指令') ?></span></div>
                        <div><strong><?= htmlspecialchars((string)$persisted['adapter_mode']) ?></strong><span><?= __('适配器模式') ?></span></div>
                    </div>
                <?php endif; ?>
            </section>

            <div class="a2a-grid">
                <section class="a2a-panel">
                    <h2><?= __('裁决证据') ?></h2>
                    <p class="a2a-muted"><?= __('最终裁决必须引用争议单和订单证据，并把钱包动作拆成可审计指令。') ?></p>
                    <ul class="a2a-list">
                        <?php foreach ($evidence as $item): ?>
                            <li><?= htmlspecialchars((string)$item) ?></li>
                        <?php endforeach; ?>
                    </ul>
                </section>

                <aside>
                    <section class="a2a-panel">
                        <h2><?= __('钱包适配器计划') ?></h2>
                        <div class="a2a-stack">
                            <?php foreach ($walletPlan as $plan): ?>
                                <div class="a2a-instruction-row">
                                    <strong><?= htmlspecialchars((string)$plan['amount']) ?> · <?= htmlspecialchars((string)$plan['instruction_type']) ?></strong>
                                    <span><?= htmlspecialchars((string)$plan['entry_type']) ?> · <?= htmlspecialchars((string)$plan['adapter_status']) ?> · <?= htmlspecialchars((string)$plan['ledger_status']) ?></span>
                                    <p class="a2a-muted"><?= htmlspecialchars((string)$plan['note']) ?></p>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    </section>
                </aside>
            </div>

            <div class="a2a-grid" style="margin-top: 16px;">
                <section class="a2a-panel">
                    <h2><?= __('已生成钱包指令') ?></h2>
                    <div class="a2a-stack">
                        <?php foreach ($walletInstructions as $instruction): ?>
                            <div class="a2a-instruction-row">
                                <strong><?= htmlspecialchars((string)$instruction['id']) ?></strong>
                                <span><?= htmlspecialchars((string)$instruction['instruction_type']) ?> · <?= htmlspecialchars((string)$instruction['amount']) ?> · <?= htmlspecialchars((string)$instruction['adapter_status']) ?></span>
                                <?php if ((string)($instruction['idempotency_key'] ?? '') !== ''): ?>
                                    <span><?= __('幂等键') ?>：<?= htmlspecialchars((string)$instruction['idempotency_key']) ?></span>
                                <?php endif; ?>
                                <?php if ((string)($instruction['external_reference'] ?? '') !== ''): ?>
                                    <span><?= __('外部引用') ?>：<?= htmlspecialchars((string)$instruction['external_reference']) ?></span>
                                <?php endif; ?>
                                <p class="a2a-muted"><?= htmlspecialchars((string)$instruction['note']) ?></p>
                            </div>
                        <?php endforeach; ?>
                    </div>
                </section>
                <aside>
                    <section class="a2a-panel">
                        <h2><?= __('裁决后托管账本') ?></h2>
                        <div class="a2a-stack">
                            <?php foreach ($ledgerRows as $row): ?>
                                <div class="a2a-ledger-row">
                                    <strong><?= htmlspecialchars((string)$row['amount']) ?></strong>
                                    <span><?= htmlspecialchars((string)$row['label']) ?> · <?= htmlspecialchars((string)$row['status_label']) ?></span>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    </section>
                </aside>
            </div>
        <?php endif; ?>
    </main>
</body>
</html>
