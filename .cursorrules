# Cursor entry for WelineFramework

Read `AI-ENTRY.md`, then `dev/ai/global-constraints.md`. Select task-specific skills through `dev/ai/skills/_index.md`.

Cursor-specific `.cursor/rules/*.mdc` files are conditional adapters only; they must point to canonical rules or skills and must not mirror long rule bodies.
