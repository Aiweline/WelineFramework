# 测试遗留图片清理审查清单

- 生成时间：2026-09-25
- 范围：深度清理候选（仅审查清单，**未删除任何文件**）
- 方法：全仓只读扫描，排除 `vendor/` `pub/` `*/node_modules/` `.git/`；重复判定用 md5 逐字节比对 + `diff -rq` / `cmp -s`

## 重要前提

1. 下列**全部条目均为 git-untracked 且被 `.gitignore` 忽略**（`dev/.gitignore` 的 `*` 规则、根 `.gitignore`），清理不影响版本库。
2. 但反过来：**git 无法恢复它们**。删除后只能靠备份。
3. 本轮**只出清单**。确认后我会「先备份到 `var/backup/` → 走系统回收站」，不直接 `rm`，且分批执行、每批核对。

---

## T1 — 纯重复副本（有全同副本保留，删除零信息损失）

| # | 路径 | 体量 | 判定依据 | 保留的副本 |
|---|------|------|----------|------------|
| 1 | `dev/tmp/daocharms-launch-audit/` | 58 图 / 6.8M | `cmp -s` 逐文件比对：**58/58 字节级全同** | `app/design/Weline/daocharms/doc/evidence/2026-09-25-ui-43page/` |
| 2 | `dev/tmp/daocharms-audit-before-full/` | 36 图 / 5.6M | `diff -rq` **无任何差异输出**（目录全同） | `dev/tmp/daocharms-audit-before/` |
| 3 | `dev/ai-preview/changan-hanfu-homepage-atelier-mockup.png` | 2.7M | md5 `78e4b18c…` 相同 | `app/design/Weline/hanfu/doc/资料/mockups/homepage-desktop.png` |
| 4 | `dev/ai-preview/changan-hanfu-homepage-midpage-mockup.png` | 2.5M | md5 `5142eb5c…` 相同 | `app/design/Weline/hanfu/doc/资料/mockups/homepage-midpage-desktop.png` |
| 5 | `dev/ai-preview/hanfu/mockups/` | 38 图 / 15M | `diff -rq` 全同 | `app/design/Weline/hanfu/doc/资料/mockups/` |
| 6 | `dev/ai-preview/hanfu/prototypes/chrome-prototype.png` | 240K | 与 app/design 侧同内容 | `app/design/Weline/hanfu/doc/资料/prototypes/` |
| 7 | `dev/ai-preview/hanfu/wireframes/effects-html/brand/changan-seal.png`<br>`dev/ai-preview/hanfu/wireframes/assets/brand/changan-seal.png` | 2 份 | md5 `f0d3d39a…` 共 4 份同内容 | app/design 镜像内 2 份 |
| 8 | `dev/team/daocharms-storefront-shentu-launch/meetings/shots/` 中 `home.png` `shop.png` `contact.png` `products.png` `about.png` | 5 图 | **五者 md5 全同**（均 40853B）＝页面未加载的空白失败抓拍，零信息 | — |
| 9 | `dev/tmp/daocharms-launch-audit/22..28-*-1440.jpg` | 7 图 | **md5 全同**＝账号/订单/收藏等页渲染成同一张，失败抓拍 | — |

**T1 小计 ≈ 31M。** 第 8、9 项是「失败抓拍」，按你的指示一并删掉。

> 附带确认：`dev/tmp/daocharms-launch-audit`、`daocharms-audit-before*` 三个目录在全仓库**无任何文档/脚本引用**（grep 零命中），是孤儿目录。

---

## T2 — 可再生成的运行残留（跑一次即重建）

| # | 路径 | 体量 | 理由 |
|---|------|------|------|
| 10 | `tests/e2e/playwright-report/` | 540K | Playwright 每次运行覆盖重写（`index.html`，mtime 09-25 19:33） |
| 11 | `tests/e2e/test-results/.last-run.json` | 4K | 仅存上次运行状态 |
| 12 | `test-results/.last-run.json` | 4K | 同上（根目录，09-18 起未更新） |
| 13 | `app/code/Weline/Shipping/test-results/.last-run.json` | 4K | 同上（09-13） |
| 14 | `app/code/Weline/Checkout/test-results/.last-run.json` | 4K | 同上（09-13） |
| 15 | `.cursor/debug-*.log` ×7 | 108K | 编辑器调试日志，最老 08-26、最新 09-11，均过期 |
| 16 | `dev/tmp/blog-search-scope.log` | 26K | 09-25 调试输出 |

**T2 小计 ≈ 690K。** 收益小但零风险，属"顺手清"。

---

## T3 — 会话中间产物（`dev/tmp/`，无引用）

| # | 路径 | 体量 | 理由 |
|---|------|------|------|
| 17 | `dev/tmp/url-query-batch-20260924/` | 40 文件 / **46M** | 9-24 URL 查询批处理中间产物；**大头是 `round3-metadata-batch.json` 21.7M + `round3-metadata-scalar.json` 21.7M 两个重复 dump**，结论已沉淀到 `dev/session/url-query-batch-20260924.md` |
| 18 | `dev/tmp/webp-convert-20260924/` | 8 文件 / 7.6M | WebP 转换过程稿；含 `db-tables-*.dump` 6.9M（**数据库表导出，建议单独确认**） |
| 19 | `dev/tmp/daocharms-audit-before/` | 36 图 / 5.6M | 改版前基线截图；改版已收口（见 `dev/session/2026-09-25-daocharms-ui-43page-closeout.md`），基线已被 evidence 取代 |
| 20 | `dev/tmp/perf-20260924/` | 11 文件 / 96K | 性能诊断过程稿（含 `n-wave-bench.json`），结论已入 `dev/session/wls-storefront-perf-20260924.md` |
| 21 | `dev/tmp/dc-404.html` | 575K | 404 页抓取快照 |
| 22 | `dev/tmp/layout-entity-plan-review-20260925-be9ad057.md` | 19K | 方案评审过程稿 |
| 23 | `dev/tmp/daocharms-404-republish.php`<br>`dev/tmp/probe-daocharms-products-fragments.php`<br>`dev/tmp/dc-completeness.py`<br>`dev/tmp/dc-reach.py` | ~8K | 一次性探针脚本 |

**T3 小计 ≈ 60M。**

> ⚠️ 第 18 项里的 `db-tables-20260924114941.dump`（6.9M）是数据库表导出，不是"图片/测试残留"。删前请单独点头。

---

## T4 — 历史过程稿：**仓库内唯一副本，删除不可恢复**（建议逐个决定）

| # | 路径 | 体量 | 理由 / 风险 |
|---|------|------|-------------|
| 24 | `.superpowers/` | **31M** | brainstorm×2 + plans + sdd×2 草稿。其中 `sdd/2026-09-03-hanfu-blog-editorial-remediation/generated/` 的 4 张封面（`forms-guide-cover` / `mamian-vs-ruqun-cover` / `styling-guide-cover` / `shui-overview-cover`，png+webp）**全仓无其它副本**，删了就没了 |
| 25 | `artifacts/product-design-audit/2026-09-04-hanfu-storefront/` | 604K | 9-04 产品设计审计：7 张 before/after 截图 + `audit-summary.md`。**审计结论本身有价值**，建议至少留 md |
| 26 | `dev/ai-preview/hanfu/live-shots/` | 31 图 / 10M | 上线实拍截图。**app/design 侧没有对应目录**，是唯一副本 |
| 27 | `dev/ai-preview/hanfu/wireframes/` | 358 文件（114 图）/ **55M** | ⚠️ **与 `app/design/.../资料/wireframes/` 双向漂移，不能当重复删**，详见下节 |

**T4 小计 ≈ 96M**（但风险最高）。

---

## ⚠️ 需要你决策：wireframes 两份已双向漂移

对比 `dev/ai-preview/hanfu/wireframes/`（下称 **dev**）与 `app/design/Weline/hanfu/doc/资料/wireframes/`（下称 **appd**）：

| 维度 | dev | appd |
|------|-----|------|
| 文件总数 | **358** | 314 |
| 目录 mtime | 09-20 15:45 | **09-24 09:35**（更新） |
| 图片差异 | 无（图片全同） | 无 |
| HTML 差异 | **163 个 HTML 内容不同** | 同左 |
| `effects/` 目录（80 张效果图） | **有** | **没有** |

**结论：谁都不是谁的超集。** dev 多出 `effects/` 80 张效果图，appd 的 HTML 更新。**不建议自动删任何一侧**，需要你确认哪份为准，或先合并再清理。

---

## 汇总

| 档位 | 内容 | 体量 | 风险 |
|------|------|------|------|
| T1 | 纯重复 + 失败抓拍 | ≈ 31M | 零 |
| T2 | 运行残留 | ≈ 0.7M | 零 |
| T3 | dev/tmp 中间产物 | ≈ 60M | 低（含 1 个 db dump 待确认） |
| T4 | 唯一副本过程稿 | ≈ 96M | **高，需逐个决定** |

**T1+T2+T3 ≈ 92M 可安全回收**；T4 视你对历史过程稿的保留意愿。

## 明确建议保留（本次未列入清理）

- `app/design/Weline/hanfu/doc/资料/**`、`app/design/Weline/daocharms/doc/evidence/**` — 设计交付物与正式证据
- `app/code/**/view/statics/**` 下主题/组件图片 — 产品资源
- `docs/assets/readme/*.png` 26 种语言封面 — 文档配图
- `var/backup/**` 18 张图 — 备份目录，性质不同，另行判断
- `NUL`（0 字节空文件）、`app/etc/*.sqlite.bak-*` — 非图片，本次未动

---

# 执行记录（2026-09-25 20:18–20:22）

用户指令：**「app/design 里面的先不动，先清理其他的」** → 保留 `app/design` 侧，清理其余。

## 执行方式

1. 全量备份 → `var/backup/test-residue-cleanup-20260925_2018/`（**136M**，`rsync -aR` 保持相对路径）
2. 备份校验：11 个目录文件数逐一比对全部 OK；`diff -rq` 抽样逐字节校验通过
3. 分 5 批走系统回收站（`/usr/bin/trash`），每批后核对，**未使用 `rm`**

## 已清理（共 30 项）

| 批次 | 内容 | 结果 |
|------|------|------|
| A1 | T1 #1–#6（launch-audit、audit-before-full、2 张 ai-preview 根 PNG、mockups、prototype） | 6/6 removed |
| A2 | T1 #8 失败抓拍 5 张（`*-reverify.png` 已保留） | 5/5 removed |
| B1 | T2 #10–#14（playwright-report + 4 处 `.last-run.json`） | 5/5 removed |
| B2 | T2 #15–#16（`.cursor/debug-*.log` ×7 + `blog-search-scope.log`） | 8/8 removed，`.cursor/hooks.json`/`mcp.json`/`rules` 保留 |
| C | T3 #17–#23（dev/tmp 全部 10 项） | `dev/tmp/` 已清空 |
| D | T4 #24–#26（`.superpowers`、`artifacts/product-design-audit`、`live-shots`） | 3/3 removed |

删除前确认：`.superpowers/brainstorm/*/state/server.pid` 两个进程**均已死亡**（3 周前陈旧状态），无活动进程。

## 未清理

- **T4 #27 `dev/ai-preview/hanfu/wireframes/`** — 双向漂移未决，两侧**均保留**（dev 358 文件 / appd 314 文件）。删 dev 侧会丢失 app/design 没有的 `effects/` 80 张效果图。

## ⚠️ 过程中的一次失误与补救

批次 B1 误删了两个 **git 跟踪**文件（我在前一轮扫描时漏查了这两个路径的跟踪状态）：

- `app/code/Weline/Shipping/test-results/.last-run.json`
- `app/code/Weline/Checkout/test-results/.last-run.json`

**已从备份还原**（未用 `git checkout`），`git diff` 与 HEAD 比对为空。复核后 `git status` 中 `D` 条目 = 3，全部为本次操作前既有的 Theme 文件删除，与本次清理无关。

**教训**：判断"可安全删除"必须逐路径跑 `git ls-files --error-unmatch`，不能只看 `git check-ignore`——被忽略 ≠ 未跟踪，且同一目录名（`test-results/`）在不同层级规则不同。

## 最终状态

- 回收空间：**≈ 136M**
- `app/design/**`：1727 文件，mtime 未变，**完全未动**
- `wireframes` 两侧均保留
- 版本库：无本次清理引入的变更

## 备份中的「唯一副本」（若日后需要请从此处取）

- `var/backup/test-residue-cleanup-20260925_2018/.superpowers/sdd/2026-09-03-hanfu-blog-editorial-remediation/generated/` — 4 张博客封面（png+webp，**未发布到 `pub/`，全仓唯一副本**）
- `var/backup/test-residue-cleanup-20260925_2018/artifacts/product-design-audit/2026-09-04-hanfu-storefront/audit-summary.md` — 9-04 审计结论
- `var/backup/test-residue-cleanup-20260925_2018/dev/ai-preview/hanfu/live-shots/` — 31 张上线实拍
- `var/backup/test-residue-cleanup-20260925_2018/dev/tmp/webp-convert-20260924/db-tables-20260924114941.dump` — 6.9M 数据库表导出

---

# 第二轮：测试结果类产物排查（2026-09-25 20:26，**仅扫描，未删除**）

用户追问「还有没有那种 test result 这种测试结果的临时存档」。范围扩大到 XML 报告、覆盖率、缓存、e2e 截图、测试运行日志。

## 排查方法

1. 目录/文件名模式扫描（剪枝 `.git` `vendor` `pub` `node_modules`，否则 find 超时）
2. **逐路径核验**：`git ls-files --error-unmatch`（是否跟踪）+ `git check-ignore`（是否忽略）
3. 读 `tests/.gitignore`、根 `.gitignore` 确认项目自身对"生成物"的声明

## A. PHPUnit 生成物 —— 未跟踪 + 已忽略 ✅ 可删

`tests/.gitignore` **自身明确声明**这些是 generated：

```
phpunit/config.xml
phpunit/test-results.xml
phpunit/.phpunit.cache/
phpunit/report/
phpunit/coverage-html/  phpunit/coverage.txt  phpunit/coverage.xml
```

| 路径 | 体量 | 说明 |
|------|------|------|
| `tests/phpunit/.phpunit.result.cache` | 1.5M | 测试结果缓存（本组最大） |
| `tests/phpunit/config.xml` | 884K | 自动生成，6682 行 / **6252 个 `<file>` 条目** |
| `tests/phpunit/report/` | 16K | `index.html` `junit.xml` `teamcity.txt` `testdox.txt` |
| `tests/phpunit/test-results.xml` | 4K | |
| `tests/phpunit/.phpunit.cache/` | 4K | |

## B. 模块级 phpunit 残留 —— 未跟踪 + 已忽略 ✅ 可删

| 路径 | 体量 |
|------|------|
| `extend/.phpunit.result.cache` | 168K |
| `app/code/Weline/Order/Test/.phpunit.result.cache` | 24K |
| `app/code/Weline/DataTable/Test/.phpunit.result.cache` | 12K |
| `.phpunit.result.cache`（根） | 4K |
| `app/code/Weline/Order/Test/test-results.xml` | 4K |

## C. e2e 测试截图 —— 未跟踪 + 已忽略（`.gitignore:183 /tests/e2e/**/artifacts/`）✅ 可删

| 路径 | 体量 |
|------|------|
| `tests/e2e/artifacts/backend/Weline_Deploy/*.png` ×15 | **4.3M** |

截图清单：`webhook-config-desktop/mobile`、`webhook-delivery-audit`、`release-control-index`、`release-history-index`、`core-update-index`、`core-update-rollback-mobile`、`wls-deploy-*`（preflight / profile / manual-plan，desktop+mobile）、`wls-deploy-webhook-replay-desktop/mobile`

⚠️ 同目录 `tests/e2e/artifacts/backend/Weline_Server/upload-e2e.txt` 是**被跟踪**文件 → **必须保留**。

## D. e2e 生成索引 / 临时脚本 —— 未跟踪 + 已忽略 ✅ 可删

| 路径 | 体量 |
|------|------|
| `tests/e2e/collected-tests.json` | 92K |
| `tests/e2e/modules.json` | 44K |
| `tests/e2e/tmp-polish-ui.mjs` | 4K |
| `tests/e2e/.weline-e2e-target-origin` | 4K |
| `tests/e2e/200/.npm/` | ~0 |
| `test-results/`、`tests/e2e/test-results/`（空目录） | 0 |

## E. 测试运行日志 —— 未跟踪 + 已忽略 ⚠️ 但**今天刚产生**

| 路径 | 体量 | 说明 |
|------|------|------|
| `var/runtime/theme-t6-handoff/` | **21M** | 见下 |
| `var/runtime/theme-layout-binding-acceptance-20260923/` | 576K | 测试日志 |

`theme-t6-handoff/` 明细（mtime 2026-09-25 18:00–19:34，**几小时前**）：

- `phpunit-full-theme-HEAD.log` **7.9M**
- `phpunit-full-theme.log` **6.2M**
- `phpunit-full-theme-after.log` **6.1M**
- `phpunit-controller.log` 160K
- `e2e-uc06/uc06-uc08/uc09/uc10/uc12.log`、`phpunit-baseline/version.log`、`formal-unit-theme.log`
- 另有 `HANDOFF.md`(25K)、`plan-after-round1.md`(50K)、`tracked-changes.diff`(632K)、`verify.sh`、`plan.sha256`

**风险提示**：这是一个**会话交接包**（含 HANDOFF.md / plan / verify.sh），且是今天刚生成的。删掉日志会丢失测试证据；但 `HANDOFF.md`、`plan*.md`、`verify.sh` 是文档不是日志。**建议只删 3 个大日志（≈20M），保留交接文档**。

## F. 被 git 跟踪的测试产物 —— ❌ 不能删（除非你明确决定）

| 路径 | 体量 | 说明 |
|------|------|------|
| `tests/var/codex-tmp/weline-fixed-affiliate-closed-loop-1779953477524.png` | 339K | 测试截图，**已入库**（无匹配忽略规则） |
| `app/code/Weline/DataTable/Test/test-results.xml` | 48K | 已入库的测试结果 |
| `app/code/Weline/Ai/Test/Http/Backend/batch-test-results.json` | 4K | 已入库 |
| `app/code/Weline/Ai/FINAL_TEST_REPORT.md` | 0B | 已入库的**空文件** |
| `app/code/Weline/Checkout/test-results/.last-run.json` | 4K | 已入库 |
| `app/code/Weline/Shipping/test-results/.last-run.json` | 4K | 已入库 |
| `tests/e2e/tmp-wquery-help-browser.mjs` | 8K | `tmp-` 前缀但已入库 |
| `tests/e2e/artifacts/backend/Weline_Server/upload-e2e.txt` | 4K | 已入库 |

删这些需要 `git rm`，会在版本库里产生一次删除提交 —— 属于仓库治理决策，不由清理任务顺手做。

## 体量小结

| 组 | 体量 | 结论 |
|----|------|------|
| A+B+C+D | **≈ 7.1M** | 未跟踪+已忽略，可直接清 |
| E | 21.6M（其中 20M 是 3 个大日志） | 建议只删日志，保留交接文档 |
| F | ≈ 0.4M | **不能动** |

## 非测试产物（已排除，不要误清）

- `app/code/Weline/Eav/view/statics/libs/bootstrap-select-1.14.0-beta3/cypress/`（5 文件）—— 第三方库自带源码，非我们的测试输出
- `tests/unit/node_modules/`、`tests/e2e/node_modules/` —— 依赖
- `tests/e2e/framework/`、`manifests/`、`scripts/` —— 已入库的测试基建
- `var/runtime/theme-components`、`theme-layout-entities`、`theme-version-convert`、`resumable`、`website_detect` —— 运行时数据，非测试结果

---

# 第二轮执行记录（2026-09-25 20:48–20:50）

用户指令：**「先按照建议来，没清理的再列出来」** → 按上节建议执行：清 A+B+C+D，E 只删 3 个大日志，F 不动。

## 执行方式

1. 删前把全部 **35 项逐路径跑 `git ls-files --error-unmatch`** → 被跟踪项数 **0**（第一轮教训已固化）
2. 全量备份 → `var/backup/test-results-cleanup-20260925_2048/`（**27M**）
3. 备份校验：19 项文件数逐一比对全 OK；3 个大日志 `cmp -s` 逐字节通过；`diff -rq tests/phpunit` 全同
4. 分 6 批走 `/usr/bin/trash`，每批后核对

## 已清理（35 项，回收 ≈ 27M）

| 批次 | 内容 | 结果 |
|------|------|------|
| A | `tests/phpunit/` 生成物 5 项（cache 1.5M、config.xml 884K、report/、test-results.xml、.phpunit.cache/） | 5/5，目录现为空 |
| B | 模块级残留 5 项（根 / `extend/` / `Order/Test/` ×2 / `DataTable/Test/`） | 5/5 |
| C1+C2 | e2e 截图 15 张（`Weline_Deploy/`） | 15/15 |
| D | e2e 生成物 7 项（collected-tests.json、modules.json、tmp-polish-ui.mjs、.weline-e2e-target-origin、200/.npm、两个空 test-results/） | 7/7 |
| E | 3 个大 phpunit 日志（7.9M + 6.2M + 6.1M） | 3/3 |

## 保护性核验（全部 ✅）

- `git status` 的 `D` 条目 = **3**，仍全为操作前既有的 Theme 文件，**无新增**
- `tests/phpunit` 跟踪文件数 = 0
- 8 个必须保留的跟踪文件全部在位：`upload-e2e.txt`、`tmp-wquery-help-browser.mjs`、`DataTable/Test/test-results.xml`、`batch-test-results.json`、`FINAL_TEST_REPORT.md`、`codex-tmp/*.png`、`{Shipping,Checkout}/test-results/.last-run.json`

## 体积变化

| 目录 | 清理前 | 清理后 |
|------|--------|--------|
| `tests/phpunit/` | ~2.4M | 0 |
| `tests/e2e/` | 6.2M | 1.8M |
| `var/runtime/theme-t6-handoff/` | 21M | 928K |

---

# 未清理清单

## 一、被 git 跟踪 —— 需 `git rm` 才能删（属仓库治理决策）

| 路径 | 体量 | 说明 |
|------|------|------|
| `tests/var/codex-tmp/weline-fixed-affiliate-closed-loop-1779953477524.png` | 339K | 测试截图**已入库**；`.gitignore` 有 `/tests/var/codex-tmp/` 但规则不追溯已提交文件 |
| `app/code/Weline/DataTable/Test/test-results.xml` | 48K | 已入库的测试结果 |
| `tests/e2e/tmp-wquery-help-browser.mjs` | 8K | `tmp-` 前缀但已入库 |
| `app/code/Weline/Ai/Test/Http/Backend/batch-test-results.json` | 4K | 已入库 |
| `app/code/Weline/Shipping/test-results/.last-run.json` | 4K | 已入库 |
| `app/code/Weline/Checkout/test-results/.last-run.json` | 4K | 已入库 |
| `tests/e2e/artifacts/backend/Weline_Server/upload-e2e.txt` | 4K | 已入库（同目录 15 张 PNG 已清，此文件保留） |
| `app/code/Weline/Ai/FINAL_TEST_REPORT.md` | 0B | 已入库的**空文件**，疑似误提交残留 |

## 二、E 组保留项（按"只删大日志、留交接文档"的建议）

`var/runtime/theme-t6-handoff/` 剩余 **928K**：

| 文件 | 体量 | 类型 |
|------|------|------|
| `tracked-changes.diff` | 632K | 变更快照（**剩余最大件**） |
| `phpunit-controller.log` | 160K | 日志 |
| `plan-after-round1.md` | 50K | 文档 |
| `HANDOFF.md` | 25K | 交接文档 |
| `git-status-baseline.txt` | 9.3K | 基线快照 |
| `tracked-changes.stat.txt` | 6.6K | 快照 |
| `e2e-uc06/uc06-uc08/uc09/uc10/uc12.log` | ~14K | 日志 |
| `controller-baseline.txt`、`head.txt`、`phpunit-baseline.log`、`phpunit-version.log`、`formal-unit-theme.log`、`plan.sha256`、`verify.sh` | ~4K | 文档/脚本 |

**另**：`var/runtime/theme-layout-binding-acceptance-20260923/` **576K** —— 上一轮提及但未纳入本次建议，仍是测试验收日志，可清。

## 三、第一轮遗留

| 路径 | 体量 | 原因 |
|------|------|------|
| `dev/ai-preview/hanfu/wireframes/` | 55M | 与 `app/design` 侧**双向漂移未决**，删任一侧都丢独有内容 |
| `app/design/**` | — | 用户明确「先不动」 |
| `var/backup/test-residue-cleanup-20260925_2018/` | **136M** | 第一轮备份，确认无需回滚后可自行清理 |
| `var/backup/test-results-cleanup-20260925_2048/` | **27M** | 本轮备份，同上 |

## 四、已排除（非测试产物，不要清）

- `app/code/Weline/Eav/view/statics/libs/bootstrap-select-1.14.0-beta3/cypress/` —— 第三方库自带源码
- `tests/unit/node_modules/`、`tests/e2e/node_modules/` —— 依赖
- `tests/e2e/framework|manifests|scripts/`、`tests/unit/`、`tests/Integration/` —— 已入库测试基建
- `var/runtime/{theme-components,theme-layout-entities,theme-version-convert,resumable,website_detect}` —— 运行时数据
- `NUL`（根目录 0 字节文件）、`app/etc/*.sqlite.bak-*` —— 非测试产物，本次未动

## 五、留下的空目录（无害，工具会重建）

- `tests/phpunit/`（生成物已清空，跑测试时重新生成 `config.xml`）
- `tests/e2e/artifacts/backend/Weline_Deploy/`（e2e 跑完会重新写截图）

---

# 第三轮：bootstrap-select 未使用核实与清理（2026-09-25 20:53–21:15）

用户问「bootstrap-select 这个我们用不到的吧？看下」→ 核实结论：**确认未使用**，已全删。

## 未使用证据（三段式）

| 检查 | 结果 |
|------|------|
| `git grep selectpicker`（全部跟踪文件） | **空** |
| `git grep bootstrap-select`（排除自身） | **空** |
| Eav 代码 / 模板 / `doc` / `register.php` | 无引用 |
| 全仓 `statics/libs/` 引用 | 仅 `Frontend/doc/主题设计/主题目录结构.md:270` 的目录结构示例，非调用 |
| 唯一近似命中 | `bootstrapSelectedWebsite`（SEO 模块函数名，无关） |

**兄弟对照（关键）**：同目录 `bootstrap-colorpicker` **确实在用** ——
`Eav/view/templates/Backend/Attribute/form.phtml:1026` 调 `$('#swatch_color_picker').colorpicker()`。
→ 证明 `libs/` 目录是活的，bootstrap-select 是其中的死代码。清理后 colorpicker 完好（2 文件）。

## ⚠️ 副本数是 5 份，不是 3 份

第一轮枚举用了 `find pub -iname '*bootstrap-select*' | head -10`，被 `head` **静默截断**，漏报 2 份。

| # | 路径 | 体量 | 跟踪状态 | 处理 |
|---|------|------|----------|------|
| 1 | `app/code/Weline/Eav/view/statics/libs/bootstrap-select-1.14.0-beta3` | 1.0M / 97 | **tracked** | `git rm -r` |
| 2 | `pub/static/Weline/Eav/libs/bootstrap-select-1.14.0-beta3` | 1.0M / 97 | untracked+ignored | trash |
| 3 | `pub/static/Weline/hanfu/Weline/Eav/view/statics/libs/bootstrap-select-1.14.0-beta3` | 1.0M / 97 | untracked+ignored | trash |
| 4 | `pub/static/Weline/Theme/view/theme/Weline/Eav/view/statics/libs/bootstrap-select-1.14.0-beta3` | 1.0M / 97 | untracked+ignored | trash |
| 5 | `pub/static/Users/weline/Project/Official/框架/app/code/Weline/Theme/view/theme/Weline/Eav/view/statics/libs/bootstrap-select-1.14.0-beta3` | 1.0M / 97 | untracked+ignored | trash |

**第 5 份是畸形发布路径** —— 绝对路径被嵌进 `pub/static/` 根下，属发布 bug 产物。

## 副本差异（不能假设"全同只备份一份"）

源仓未压缩（`Gruntfile.js` 6705B / 295 行）；pub 副本被静态发布机制**压过**（4081B、**无换行符**），共 49 个文件不同。
pub 的 #2 与 #3 全同；#4、#5 与它们也全同。→ 备份保留了「源仓」+「pub 压缩版」两种形态。

## 备份与核验

- 备份：`var/backup/bootstrap-select-removal-20260925_2055/`（**5.1M**，5 份路径全保留，各 97 文件）
- 校验：文件数逐一比对 OK + `diff -rq` 逐字节通过
- 核验：`app/` 与 `pub/` 下 bootstrap-select 目录数均 = **0**；`git diff --cached` 的 97 个删除**全为** bootstrap-select；未暂存删除仍只有 3 个既有 Theme 文件；`bootstrap-colorpicker` 完好

## git 状态提示

97 个删除**已暂存**（`git rm -r` 的行为），**尚未提交** —— 由你决定何时 commit。

## 顺带发现的目录质量问题（供参考，未处理）

静态发布机制把 `view/statics/**` **整目录**拷进 `pub/static/`，包含 `docs/`(140K)、`tests/`(32K)、`cypress/`(24K)、`.github/`(32K)、`package-lock.json`(388K)、`nuget/` ——
这些**永远不该进 web 根**。1 份源码因此被放大成 5 份 × 1.0M。
**建议**：给 `view/statics/libs/**` 加发布排除规则（至少排除 `docs/ tests/ cypress/ .github/ nuget/ package-lock.json Gruntfile.js *.md`），否则下一个 vendored 库还会重复这个问题。
