# 未完成任务盘点（承接上个账号）

**盘点时间**：2026-09-26 11:00
**盘点范围**：本机 `~/.workbuddy-ai/tasks/` 全部 9 个会话的 99 条任务 + 对应会话正文
**结论**：状态上 25 条未闭环，**实际只有 3 块真没干完**，其余是干完了没回勾。

---

## 根因：额度耗尽被硬截断，不是主动放弃

6 个会话的**最后一条消息**就是 `429 Credits exhausted`：

| 会话 | 主题 | 断点 |
|---|---|---|
| `52311dca` | 静态资源公共面统一口径 | 09-26 00:43 |
| `c5b9c657` | PayPal 全链路 + 商城前端功能清单 | 09-26 00:43 |
| `dd87cb02` | PayPal 切换闪回修复 | 09-26 00:43 |
| `ef3d2ffe` | Theme 版本独占隔离 · 任务6 收口 | 09-26 00:43 |
| `e7eace88` | DaoCharms 43 页视觉验收 | 09-26 08:09 |
| `fba1747d` | 关线上 Local 测试支付 | 09-26 08:09（撞 429 后自愈跑完 12/12） |

> 云端 `conversation_search` 查不到上个账号历史（换账号后返回 0 条），本清单**全部基于本机记录**。

---

## 🔴 真未完成

### 1. Theme 版本独占隔离 · 任务6 收口（`ef3d2ffe`，4/16）

- [ ] **UC-15 多 Worker HIT 未实测** —— 唯一没跑过的用例
- [ ] **任务6 行为表仍有未验行** → 故不可宣称完成（计划文档自己写着「未宣称任务 6 完成」）
- [ ] `candidate_write_ok` 仍仅由请求参数传入，**未自动判定**
- [ ] 文档收口：计划文档任务6 进度块、UC-09 实测状态行、开发日志、只读探针证据登记

**已完成（勿重做）**：缺口 **#1~#5 全已修**。
其中 **#5 曾让 UC-09「删除不复活」直接失效**（主 owner `selection.published=105` 但 flags 指向陈旧 `8`，`getCurrent/getPublished` 读 flags → 决定写到陈旧版）。
最新实测：e2e `theme-version-artifact-isolation.spec.js` **11 passed**；计划套件 257/1527 + 6/24 + 29/354 全绿；旧读法 0；线上主 owner 跑前跑后逐字段一致；`e2e-iso-*` 残留 0。

**落点**：`app/code/Weline/Theme/doc/开发/spec/layout-entity-per-version-isolation.md`（405 行，缺口普查在 §314+）

---

### 2. PayPal 取消 → 续付 真链路（`c5b9c657`，10/13）

- [ ] 摸清真 `cancel_url` / browser-cancel-entry / 续付入口，以及 `PayPalProvider` 怎么构造 `cancel_url`
- [ ] 写**真** runner：PayPal → 批准页点 Cancel → 落到待付/恢复页 → 走续付入口 → 批准 → 订单变已付
- [ ] 重跑并同步两份交付物（`doc/测试/商城前端功能清单与PayPal购物流程测试用例.md` + `doc/测试/商城前端验收看板.html`）

**⚠️ 已知坑**：`sandbox-continue-pay-real-storefront-runner.js` **会假通过** ——
它用 `fake_card`（立即支付成功）再写"已取消"恢复态，`continue_pay_url` 实际 302 回 `/checkout/success`。
**不能**拿它验证取消→续付。

---

### 3. 静态资源公共面 · 文档已改好，但**一个都没提交**（`52311dca`，17/18）

- [ ] 提交并推送 **7 个文件**（见下）
- [ ] 在有 nginx 的机器上 `nginx -t` 验证 `nginx.static-surface.conf`
- [x] ~~改写 `static-resource-versioning.md` 的「已知分歧（未合并）」~~ → 全文已搜不到该节
- [x] ~~更新报告第五章决策表与第六章~~ → 决策表已全标 ✅，第六章 §3 已写明「不建议改」

**⚠️ 实测修正（2026-09-26 11:00 `git status`）**：文档内容其实已改好，但**改动一个都没提交**：

| 文件 | 状态 |
|---|---|
| `app/code/Weline/Framework/Deploy/StaticPublicSurface.php` | **`??` untracked** |
| `app/code/Weline/Framework/Test/Unit/Deploy/StaticPublicSurfaceContractTest.php` | **`??` untracked** |
| `nginx.static-surface.conf` | **`??` untracked** |
| `静态发布排除与pub-static存量清单-20260925.md` | **`??` untracked** |
| `app/code/Weline/Framework/Router/Core.php` | `M` |
| `nginx.conf` | `M` |
| `app/code/Weline/Framework/doc/static-resource-versioning.md` | `M` |

`git log -- <上述文件>` **无任何提交** → 从未落库。

**两个真实风险**：
1. **安全修复只在本机**。`Router\Core::StaticFile()` 原唯一守卫是「路径含 `view`」，不匹配时原样
   `file_get_contents()` 回吐 —— 实测纯 WLS 下 `.php` 源码可被 HTTP 读出（200 `text/x-php`）。
   修复（含 `StaticPublicSurface.php`）**untracked** → 一次 `git clean` 或换机器即丢失。
2. **nginx 配置从未验证**。本机**没有 nginx**（`nginx: command not found`）→ `nginx -t` 从未跑过。

---

## 🟠 审计已出、缺陷未修（`e7eace88` 下游，不在任务表里）

DaoCharms 43 页验收**其实已交付**（findings A/A2/B/C/D/E/E2 七份全在，差异清单 09-26 10:33 已更新，
README 已更正 22–28 假通过）。但审出的问题**一条没修**：

- [ ] **P0 结构性 19 条 / P1 明显偏差 85 条**
- [ ] **上线污染（合规风险最高）**：42/07/13 页把内部注记当顾客文案发布
      （`(verify real numbers before launch)`、`1688 offer 696266298522`、`this floor is the chart itself`、`N/A No verified public URL…`）；
      02-shop 四张系列图是同一张吊坠照且带中文烘焙字 + `design` 水印
- [ ] **隐形文字**：02-shop / 08-compare 的按钮文案与填充同色 → 对比度 **1.00:1**；
      3 个 CTA 类缺 `color` 声明（`.daocharms-shop-hub__card-cta`、`.storefront-compare__cta`、`.new-arrivals-page__action`）
- [ ] 价格 `￥` / `$` 混用；卡片价格渲染成裸「28」（无符号无小数）
- [ ] 设计稿自身也需先定规范：政策 7 页模板不统一、账户 7 页外壳不统一 → **没有一套实现能同时匹配 7 张稿**

**清单**：`app/design/Weline/daocharms/doc/视觉验收差异清单-2026-09-25.md`
**证据**：`app/design/Weline/daocharms/doc/evidence/2026-09-25-visual-audit/`

---

## ✅ 已完结（勿重复劳动）

| 会话 | 主题 | 状态 |
|---|---|---|
| `ee59674c` | .gitignore 智能体规则清理 | 3 commits 已推 |
| `dd87cb02` | PayPal 切换闪回修复 | 5/5 |
| `fba1747d` | 关线上 Local 测试支付 | 12/12（撞 429 后自愈跑完） |
| `061781e3` | 前端基础问答（闭包/异步） | 纯问答，无任务清单 |

## 🟡 小尾巴

- `18dab004` 通知「全部已读」改 binquery：**已修完**并真 Chromium 验证 `POST /api/framework/query-bin → 200`、`CIRCUIT_ERRORS=[]`、按钮渲染正常。
  留了一句**没回你的话**：本地 `admin/admin` 用户缺 `Weline_Backend::notification` ACL 导致 500（生产用户 `weline/weline18` 不受影响）—— 要不要查。

---

## 怎么接手（4 份可粘贴简报已生成）

**关于「在当前账号下创建会话」**：做不到程序化创建 —— 会话注册表在应用自己的实时库
`~/.workbuddy-ai/workbuddy.db` 的 `sessions` 表里（`sessions/*.json` 只是 PID 心跳文件），
插一行不会变成可用对话，也没有对应的对话正文。**硬写那个库有风险，不做。**

**可行且已被验证的做法**：本仓已有交接范式 —— `var/runtime/<任务>-handoff/` 放简报，
你开一个新会话、把「完整版」整段粘进去即可。上一轮就是这么交接的（`theme-t6-handoff`）。

| # | 简报（复制 A 段） | 对应会话 | 一句话任务 |
|---|---|---|---|
| 1 | `var/runtime/theme-t6-handoff/TAKEOVER-PROMPT-ROUND2.md` | `ef3d2ffe` | Theme 任务 6 收口（缺口 #1~#5 已修，**别重做**） |
| 2 | `var/runtime/paypal-continuepay-handoff/TAKEOVER-PROMPT.md` | `c5b9c657` | 场景 C 取消→续付 真通路（现有 runner 会假通过） |
| 3 | `var/runtime/static-surface-handoff/TAKEOVER-PROMPT.md` | `52311dca` | 静态口径收尾：提交 7 个文件 + nginx 验证 |
| 4 | `var/runtime/daocharms-p0-handoff/TAKEOVER-PROMPT.md` | `e7eace88` | DaoCharms P0/P1 修复（验收已完成，动手修） |

> 每份简报都含：读什么、做什么、**硬约束**、**踩过的坑**、验收纪律、开工回报要求。
> 沿用第一轮验证过的「先读 → 对齐基线 → 回报理解 → 等确认再动手」流程。

---

## 建议顺序

1. **静态口径提交**（`#3`）—— 最快，且**风险最高**：核心安全修复 untracked，只在本机。
   顺手把 `nginx -t` 排到有 nginx 的机器上验。
2. **Theme 收口**（`#1`）—— 离完成最近，且是唯一还挂着"未验行为"的正式验收门禁。
3. **PayPal 续付**（`#2`）—— 要真跑浏览器，最费时，但坑已探明。
4. **DaoCharms P0**（`#4`）—— 量最大，但纯前端改样式，可批量；**上线污染类建议先做**（合规风险）。
