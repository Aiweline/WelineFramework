# 2026-03-26

- Continued the post-fix framework closure after the fallback storefront/query-provider performance regression was already green.
- Added another safety layer around the query stack:
  - `QueryProviderRegistryTest` now also covers deferred dynamic provider-name resolution, proving the lazy fast path does not break providers whose names cannot be parsed from literal source returns.
  - new `FrameworkQueryServiceTest` now locks the core query-service contract for before/after event dispatch, before-event denial, and `framework/introspect` summarization.
- Focused verification is green:
  - `php vendor/bin/phpunit --no-coverage app/code/Weline/Framework/Test/Unit/Service/Query/QueryProviderRegistryTest.php app/code/Weline/Framework/Test/Unit/Service/Query/FrameworkQueryServiceTest.php --colors=never` (`6 tests / 26 assertions`)
- Fixed the follow-up CLI regression behind recent framework refresh work: `php bin/w setup:upgrade --route` had been rejecting its own documented `--route` flag because `Setup\\Upgrade` validated only normalized option keys while the CLI parser also passed raw prefixed keys for valueless options.
- Narrowed the compatibility fix to `Weline\\Framework\\Setup\\Console\\Setup\\Upgrade`: argument validation now accepts either the raw key or the normalized key, with new focused coverage in `UpgradeArgsValidationTest`.
- Verification for the CLI fix is green: syntax checks for the touched files, focused PHPUnit (`2 tests / 3 assertions`, existing PHPUnit deprecation unchanged), and a real `php bin/w setup:upgrade --route` run that completed the route-only upgrade flow successfully.
