# 2026-03-22

- Created daily memory file.
- Started investigating IPC idle timeout (15s) that force kills Master and cleans all instance processes.
- Fixed Weline Theme runtime 500: added missing isPreviewThemeMode() in app/code/Weline/Theme/Observer/LayoutSlotRenderer.php and aligned legacy preview detection with frontend-area guard.
- Completed Websites AI workbench Epic 1 foundation: added `AiSiteBuilderProvider` / `WebsiteThemeSource` extension contracts, registries, tests, and built-in `websites_default` provider; refreshed `generated/extends.php` successfully.
- Completed WLS stop-timeout hardening patch:
  - `Stop.php`: idle timeout no longer immediate-fails; adaptive hard timeout from orchestrator stop config.
  - `ServiceOrchestrator.php`: periodic stage2/stage4 stop heartbeats to keep IPC progress active.
- Verified both touched files with `php -l` and restored runtime to no running server instances.
- Continued Theme preview unification:
  - Expanded `Weline\Theme\Controller\Router::rewritePreviewThemeQuery()` so non-homepage frontend URLs with `?preview_theme=` can resolve page layout type from URI and enter preview gateway.
  - Kept skip guards for static/API paths to avoid accidental route hijack.
- Runtime parity validation is currently blocked: requests to `http://127.0.0.1:9981` time out despite open port (no response payload).
- Completed WLS `server:reload` rolling reload concurrency fix:
  - `ServiceOrchestrator` now does dispatcher remove -> batch drain -> stop -> concurrent start -> wait READY -> rejoin for Worker batches.
  - `Processer::batchCreateWindows()` now performs real concurrent child startup instead of sequential Fiber-wrapped `create()`.
  - `Reload.php` wait mode now uses adaptive timeout and clearer staged progress.
- Runtime verification succeeded:
  - `php -l` passed for `Processer.php`, `ServiceOrchestrator.php`, and `Reload.php`.
  - `php bin/w server:reload` completed successfully with 12 Workers in 3 batches and reported success after 101.6s.
  - `var/server/instances/default.json` confirmed all 12 Workers `ready`.
- Important debugging lesson:
  - temporary session-server test scripts using `--role=session_server` polluted `var/session/session_server.token`, creating a false reload failure (`Client auth failed` on new workers).
  - recovery was: stop temp test session servers -> `php bin/w server:start` -> confirm token regenerated -> rerun reload.
- Fixed the remaining Theme preview regression where explicit `preview_theme=11` was still being rewritten to `frontend_theme_id=10` under WLS.
- Root cause: preview route rewriting updated `$_GET` but did not sync the live `Request` singleton, so `ThemePreview\Gateway` could read stale cached query params.
- Added `RouterPreviewRewriteTest`, patched router request-sync behavior, and hardened gateway precedence so explicit `preview_theme` always wins over stale cached theme ids.
- Re-verified runtime on `https://127.0.0.1:9982`: `preview_theme=10` resolves to default markers only, `preview_theme=11` resolves to motor markers only.
- Investigated a user-reported `php bin/w s:start -r -f -frontend -p 9982` `ParseError` in `app/code/Weline/Server/Console/Server/Start.php`.
- Restored the corrupted startup note strings in `Start.php`; `php -l` and `php bin/w s:start --help` now pass again.
- Re-ran the original startup command and confirmed it no longer fails at class parsing; it now advances into runtime startup behavior instead.
- Continued the Windows frontend-launch follow-up after the `Start.php` parse fix:
  - Replaced `Processer` Windows foreground launches with a temp self-deleting `.cmd` launcher driven by `cmd.exe /d /c`, instead of the previous non-interactive PowerShell `Start-Process` path.
  - Made `batchCreateWindows()` fall back to per-process `create()` whenever any command requests `foreground=true`, so frontend worker/debug windows no longer reuse the old headless PowerShell batch path.
  - Normalized foreground `--name=`, `--launch-id=`, and `--epoch=` arguments to unquoted values when safe, then changed PID confirmation to validate by `process name + launch-id`.
  - Added `ProcesserTest` coverage for foreground batch fallback, `cmd/start` launcher command generation, self-deleting foreground scripts, and argument normalization.
  - Verified with `php -l`, targeted PHPUnit, `php bin/w server:stop`, `php bin/w s:start -r -f -frontend -p 9982`, `php bin/w server:status --all`, and `curl.exe -k -I https://127.0.0.1:9982/`.
  - Final runtime state is healthy again: `default` instance on `https://127.0.0.1:9982` is back to `12/12 workers` ready and returns `HTTP/1.1 200 OK`.
  - Remaining uncertainty: this tool environment still shows `MainWindowHandle = 0`, so true visible worker windows are not fully proven here even though the launch chain now goes through `cmd.exe /d /c <temp>.cmd` wrappers instead of the old headless PowerShell path.
- Fixed a major Theme layout rendering performance bug:
  - `ControllerFetchFileBefore` now preserves the original content template as the controller fetch target and passes the resolved layout separately via `layoutTemplate`.
  - `ControllerFetchFileAfter` now reuses the already-rendered content HTML instead of fetching the content template again.
  - Updated the remaining frontend/backend layout templates that still preferred `contentTemplate` so they now prefer prepared `content` / `meta.content` and only fall back when needed.
  - Added `ControllerFetchFileAfterTest`.
  - Successful live probes on `https://127.0.0.1:9983` reported `X-Wls-Performance-Routerstart` around 285ms to 526ms, though local WLS listener instability prevented a longer verification run.
- Followed the user's exact PageBuilder backend URL and removed a second performance hotspot:
  - `app/code/GuoLaiRen/PageBuilder/Controller/Backend/Page.php` now caches accessible website IDs/lists per request and batch-loads visible website metadata into `website_map`.
  - `app/code/GuoLaiRen/PageBuilder/view/templates/Backend/Page/index.phtml` now reuses that map for website badges, sitemap links, and child-page website links instead of per-row `Website::load()` calls.
  - CLI validation for the exact route is still limited by backend login redirect, so authenticated page timing still needs a browser-session check.
- Fixed the backend Theme blank-main regression after the `fetch_file_after` optimization.
- Root cause: Weline Theme templates cannot safely use inline fallback syntax like `{{content ?: meta.content}}`; the Taglib compiler treats `{{ }}` as variable-path syntax, so `?:` compiles into broken PHP and can blank the main content region.
- Replaced the affected backend/frontend layout fallbacks with explicit `<if>/<elseif>` branches and added `ThemeTemplateInlineFallbackSyntaxTest` to prevent reintroducing `{{ a ?: b }}` in `app/code/Weline/Theme/view/theme/**/*.phtml`.
