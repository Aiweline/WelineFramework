# 2026-03-25

- Added clean-route alias controllers for `WeShop_Subscription` so `/subscription` and `/subscription/view` reuse the storefront flows without touching theme modules, and backed them with tiny phpunit tests of the alias classes plus a new `tests/e2e/specs/frontend/weshop-subscription.spec.js` that proves the guest `/subscription` entry redirects to login.
- Continued the next WeShop storefront base-layer slice with canonical account-route normalization for the login-protected purchase chain:
- `Cart / Checkout / Order` guest redirects now point to `weshop/customer/account/login`.
- `OrderListPageDataService` back-link data plus the touched default-theme header / checkout / order-success templates now point to `weshop/customer/account/index` or the canonical login route instead of bare `customer/account*`.
- Focused PHPUnit for the slice is green (`26` tests / `185` assertions), and the targeted write scope no longer contains bare `customer/account*` references.
- Browser acceptance for the slice is still blocked by environment, not by the route change itself:
- `php bin/w server:start -p 9982` and `php bin/w s:start -r -f -frontend -p 9982` both currently fail behind a shared-session sidecar reuse/readiness problem on `19970`.
- The fallback e2e runtime reaches the routes but dies with an unrelated `Maximum execution time of 30 seconds exceeded` fatal in `Weline\Framework\System\Process\Driver\AbstractProcessDriver.php`, so the next practical follow-up is runtime recovery before rerunning this spec.
- Checkpointed the next WeShop storefront follow-up as `e2a822ea` (`fix(weshop): align checkout retry and product list routes`): checkout retry-payment now stays on the existing order context, both `/product/list` and `/weshop/product/list` are back to `200 OK` on `9982`, and focused checkout/product browser coverage is green again.
- Continued the bounded WeShop unified API auth bearer slice:
- `Weline\Api\Observer\ApiControllerInitBefore` now falls back to `WeShopAuthTokenService` for Bearer tokens that are not legacy `Weline_Api` tokens, binds `weshop_actor_context` / `weshop_auth_user` onto the request, and unblocks `WeShop_Customer` request-scoped auth resolution.
- Added the installable `WeShop_ApiBridge` module so real internal route keys now include `api/rest/v1/weshop/auth/*`; after `php bin/w setup:upgrade WeShop_ApiBridge --yes`, `route:list` shows both legacy `weshop/rest/v1/auth/*` and contract routes.
- Live runtime reminder for this environment: the externally reachable new contract path is currently `https://127.0.0.1:9982/api123/api/rest/v1/weshop/auth/*`, while the legacy path remains `https://127.0.0.1:9982/api123/weshop/rest/v1/auth/*`; both returned controller-level `422` validation responses on empty payloads.
- Focused auth/phpunit batch is green (`15` tests / `31` assertions across matcher + observer + customer-context + ApiBridge), but the final live protected bearer smoke is still blocked by local WLS instability after setup reload because the shared session sidecar on port `19970` remained occupied while `server:status` reported no running instance.
- Continued the WeShop clean-route hardening with the next real production blocker: alias controllers that reused parent actions but still resolved templates relative to the alias namespace.
- Fixed the live `9982` `product/view?id=1` `500` by adding a Product alias proxy template, then expanded the same pattern to Cart, Checkout, Invoice, Order, and RMA so clean routes now use module-qualified template paths or module fallback templates instead of fragile relative lookups.
- Added `tests/e2e/specs/frontend/weshop-product-clean-route.spec.js`, replaced the old incomplete Product detail controller tests with real branch coverage, added the missing Cart guest unit test, and re-ran both the focused frontend clean-route specs and the full `node tests/e2e/start.js` wrapper successfully.
- Completed the public storefront customer auth closure checkpoint on top of `Weline_Customer`: added public register / forgot-password / challenge routes and templates, linked the login page to forgot-password, rewrote the old `WeShop_Customer` controller tests into real assertions, and kept the touched login flows on `addWarning()` instead of the nonexistent `addNotice()`.
- Re-validated the auth/compliance/RMA browser path on the `9982` runtime and kept the full grouped `node tests/e2e/start.js` suite green before preparing the next checkpoint commit.
- Checkpointed three more verified WeShop commits in one continued push:
- `b7ddd5c1` public customer auth routes
- `5fe280d3` clean storefront route alias/template hardening
- `cb0fb406` compare + recently-viewed + wishlist storefront completion
- The saved-item storefront wave now gives the account center real discovery/quick-link cards for `RecentlyViewed` and `Wishlist`, and `Compare` now hydrates product cards through `w_query()` instead of directly touching the product model.
- Implemented a pixel hook-placement hardening slice in `WeShop_Analytics`: added slot-aware snippet routing (`head/body/footer`), introduced default layout hook templates for `base::head-before`, `base::body-start`, `base::footer-after` (with `head-after` compatibility fallback + dedupe guard), split `FacebookPixel` `noscript` into body placement, and kept `WeShop_Analytics` unit tests + `tests/e2e/framework/preflight-refresh.php` green.
- Extended the same analytics slice with new backend-manageable channels: `TikTok Pixel` and `Bing Ads (UET)` providers were added, both default disabled, analytics save-controller switched to dynamic provider-field persistence (no hardcoded field list), and analytics unit coverage expanded to 41 tests with preflight still green.
- Followed up by completing real TikTok/Bing server-side conversion delivery: `TikTokPixel::sendEvent()` now posts to TikTok Business API `open_api/v1.3/pixel/track/` with access-token auth and mapped commerce payload, `BingAds::sendEvent()` now posts to Microsoft UET Conversion API `/v1/{uet_tag_id}/events` with bearer auth; both providers now require API token fields when enabled, and Analytics unit coverage is green at `47` tests / `168` assertions.
- Fixed a WLS shared-sidecar persistence storm after user reported repeated `[SessionStore] Failed to write persist file`: `SessionStore` now supports configurable persist file names, uses unique temp files + directory self-heal + failure backoff + detailed error logs, and `session_server.php` now defaults memory role to `wls_memory_store.dat` (session stays on `wls_session_store.dat`) to avoid session/memory file contention.
- Restored the legacy backend provisioning path `/saas/backend/provisioning/index` by adding a thin `Weline_Saas` compatibility module that aliases the canonical `Weline_Websites` controller/template instead of changing `Weline_Websites` routing. Focused `phpunit`, syntax checks, and generated-router inspection all passed, but live smoke on `https://127.0.0.1:9982` is still blocked by an unhealthy WLS instance (`server:status --all` reports `default` at `0/2 workers`) plus an unrelated `setup:upgrade -m Weline_Saas --yes` tail hang after existing EAV upgrade SQL errors.
- Landed the Search/OpenSearch hardening slice: installer now supports external temp/install dirs plus disk-space checks and process env overrides, OpenSearch 3.5.0 was actually installed to `D:/WelineRuntime/opensearch`, started successfully on `127.0.0.1:9200`, and `app/etc/env.php` + DB default search config were switched to `opensearch`.
- Rebuilt `WeShop_Search` into an extensible provider-based indexing flow: `WeShop_Product` and `WeShop_Catalog` now register search document providers through `extends/module/WeShop_Search/Document`, `w_query('search', ...)` exposes search/index operations, Search unit tests (`25`) pass, and focused storefront browser coverage `node tests/e2e/start.js specs/frontend/weshop-search.spec.js` passed against real indexed product/category documents.
- Finished the WeShop Analytics Google bridge slice end-to-end: Visitor pixel sandbox events now bridge into Google `gtag`, the backend analytics page now shows explicit GA4 setup guidance plus an `Open Google Analytics` button, and the required backend browser spec `PLAYWRIGHT_RUNTIME_STRATEGY=wls PLAYWRIGHT_E2E_TRANSPORT=direct node tests/e2e/start.js specs/backend/weshop-analytics.spec.js` passed on the restored live WLS runtime `https://127.0.0.1:9982`.
- Followed up on the same analytics task by implanting key pixel marker classes into default theme conversion controls so click events are not missing from the UI layer: added `weline-pixel::add_to_cart`/`add_to_wishlist` to product + category CTAs and `weline-pixel::begin_checkout` to cart/header checkout CTAs plus checkout Place Order; added `DefaultThemePixelMarkerTest` and re-verified `WeShop_Analytics` unit (`49`/`180`) + preflight refresh green.
- Upgraded Weline_Bot role configuration UX with a guided wizard + AI suggestion endpoint (Role::postSuggest/getSuggest), added RoleConfigAssistant fallback-safe draft engine, rebuilt backend role form to auto-apply AI drafts into model/adapter/skills/permissions/model config, added RoleConfigAssistantTest (3 tests / 18 assertions), and verified route/menu/acl refresh through 	ests/e2e/framework/preflight-refresh.php (note: setup:upgrade --route remains blocked by existing CLI arg-validation inconsistency in this workspace).
- Unified WLS logging through a new WlsLogService so core WLS runtime/process logs now resolve to var/log/wls/{instance}/, and refactored WLS logger/config/start/bin fallback writers plus added focused WlsLogService unit coverage.
- Fixed a WLS orchestrator startup-order race after a real `worker#1 READY timeout` incident: `ServiceOrchestrator` now gates worker reload / batch restart / rolling restart on critical sidecar readiness (`session_server` and enabled `memory_server`) and temporarily keeps critical resurrection work flowing during that wait so worker replacement does not start before session infrastructure is back. Focused `Weline_Server` PHPUnit is green; live reload re-verification was intentionally skipped because the current runtime was already dirty/unhealthy.
- Followed up on a later shared-state auth-failure storm by hardening `BackendStatusService`: sidecar health checks no longer hardcode `session_server.token` / `memory_server.token`, but instead use the runtime instance metadata `token_file_name` with focused `Unit/Control` coverage. This should eliminate one likely source of short-lived probe auth failures against shared session/memory services.
- Closed the user-reported `s:start -r -f -frontend -p 9982` shared-state startup-order blocker: `Start` now skips post-ensure port-release checks for ensured shared sidecars, shared ensure timeout was raised to 30s with quieter short probe retries, non-shared sidecars are no longer reused as shared, shared `session_server` no longer orphan-exits before master attach, and session token write/cleanup is race-safe. Focused WLS unit suite (`16` tests / `50` assertions) passed and captured startup log now shows both `Session Server`/`Memory Service` shared sidecars reused without reproducing `Shared Memory Service ... failed to become ready`.
- Clarified the Websites/PageBuilder AI site-building wording so the unified Websites workbench now states that Websites owns the default/base flow while PageBuilder only takes over as a provider extension after the preparation stage; touched `PageBuilderProvider.php` and the Websites workbench hero hint, and verified both files with `php -l`.
- Continued and completed the second-round Weline_Bot role UX enhancement after the user asked to continue: added guided listing quick-start (profile/template cards), added `bot_profile` + non-AI template baseline apply on role form, and extended `RoleConfigAssistant` with profile/workflow/risk strategy so multi-project and enterprise scenarios generate more practical defaults.
- Verification refresh for that pass: `php -l` on touched Bot files passed, `php vendor/bin/phpunit --no-coverage app/code/Weline/Bot/Test/Unit/Service/RoleConfigAssistantTest.php --colors=never` passed (`4 tests / 26 assertions`, existing deprecation notice unchanged), and `php tests/e2e/framework/preflight-refresh.php` passed.
- Continued the Websites/PageBuilder AI workbench slice after the user rejected the earlier wording-only treatment: `Weline_Websites\Controller\Backend\SiteBuilderAgent` now owns a real `pagebuilder-handoff` path that seeds/resumes a native PageBuilder workspace, the Websites workspace now redirects into that native handoff when the PageBuilder lane leaves `prepare`, and focused validation is green (`php -l`, the new `PageBuilderProviderTest`, `php tests/e2e/framework/preflight-refresh.php`, and `node tests/e2e/start.js specs/backend/ai-site-workbench.spec.js` with `4 passed`).
- Fixed the follow-up WLS shared-service reuse regression after the user reported repeated `端口 19970 仍被 Weline 进程占用` / `执行复活 session_server#1` logs even while the shared public sidecar was healthy: `ServiceOrchestrator` now treats adopted `shared_external` sidecars as a special alive-probed path instead of a generic `no_ipc_timeout` zombie, and focused `ServiceOrchestratorStartupTest` coverage is green.
- Closed the bounded WeShop unified auth bearer slice all the way through a real protected Order API on the restored `9982` runtime: fixed `WeShopAuthTokenService` access-token mutation, bound WeShop bearer actors into `ApiControllerInitBefore`, added `WeShop_ApiBridge` auth aliases, fixed `OrderService::getUnpaidOrderCount()` null-int crash, repaired `WeShop_Order` / `WeShop_Invoice` REST JSON wrappers, and revalidated live `order/list`, `unpaid-count`, and `unpaid-list` as `200 OK` with bearer auth.
- Followed that auth/order closure by fixing the storefront header-account unpaid-orders hook so it now builds the real generated order API URLs through the framework URL builder instead of the stale `/api/rest/v1/weshop_order/order/*` path; added a view contract test to keep that integration from drifting again.
- Implemented the approved WLS shared Session/Memory lifecycle convergence slice: `SharedStateServiceManager` now covers stale sweep + acquire/release ref-count semantics + idle-shutdown scheduling, `SessionProtocol`/`SessionServer` gained token-protected graceful `shutdown`, `session_server.php` now self-checks shared idle shutdown, storage/backend/admin/PageBuilder query callers were rewired onto new `SessionStateFacade` / `MemoryStateFacade` plus new `w_query('session', ...)` / `w_query('memory', ...)`, and focused PHPUnit coverage was added for lifecycle, query delegation, and shutdown command paths.
- Finished the Search/OpenSearch closure properly after the user asked to standardize the install under `extend/server`: OpenSearch binaries now run from `extend/server/opensearch`, but `path.data` / `path.logs` are intentionally externalized to `D:/WelineRuntime/opensearch-data` and `D:/WelineRuntime/opensearch-logs` because the repo `E:` drive was low enough to trigger OpenSearch `cluster.blocks.create_index=true` when data stayed inside the project tree.
- Search installer follow-up details: fixed the malformed `m_weshop_search_engine_config.config_data` JSON, taught `install_opensearch.php` to persist/support `SEARCH_OPENSEARCH_DATA_DIR` + `SEARCH_OPENSEARCH_LOG_DIR` and to reuse `app/etc/env.php` OpenSearch settings during later `install/check` runs, then re-ran `env:install opensearch`, restarted the local node, and confirmed `install_opensearch.php check`, `search:index`, direct OpenSearch `_search`, Search PHPUnit (`25`) and `tests/e2e/specs/frontend/weshop-search.spec.js` all passed again.
- Continued the WeShop international-commerce base-layer closure with a bounded `Shipping + Tax + Checkout` slice:
  - `WeShop_Tax` now exposes a `tax` query provider and configurable tax-rate resolution (`default_rate`, `country_rates`, `region_rates`, optional shipping taxation).
  - `WeShop_Checkout` now computes placed-order summary shipping/tax through `w_query('shipping', 'calculateShipping', ...)` and `w_query('tax', 'calculateTax', ...)` instead of inheriting zero-value cart placeholders.
  - `ShippingProviderInterface` was aligned with the actual DHL/FedEx provider surface by adding concrete `calculateShipping/createShipping/trackShipping` wrappers, and focused unit coverage was added for the provider contract.
  - `app/design/WeShop/default/frontend/pages/cart/index.phtml` and `app/design/WeShop/default/frontend/pages/checkout/index.phtml` now expose row-level summary hooks for subtotal/shipping/tax/discount/grand-total extensions.
  - Focused verification for the slice is green: syntax checks, combined PHPUnit batch (`20` tests / `136` assertions), `php tests/e2e/framework/preflight-refresh.php`, and `node tests/e2e/start.js specs/frontend/weshop-order-checkout-clean-routes.spec.js` (`2 passed`).
- Closed the lingering Weline_Bot backend E2E blocker: the failing chat-session flow was using the wrong /api123/... prefix for ot/api/v1/chat/send; switched the spec to the real frontend route /bot/api/v1/chat/send, fixed Session/Skill getView() template resolution to explicitly use iew.phtml, and re-ran the focused Bot backend spec green at 10 passed.
- Fixed a small but real WLS CLI ergonomics bug after the user reported `php bin/w se:rel defaul`: `ServerInstanceManager` now resolves unique persisted instance-name prefixes and ranks close suggestions, `server:reload` auto-maps `defaul -> default` when unambiguous, and missing-instance guidance now points to `server:listing` / concrete candidate names instead of the older `server:status -a` hint. Focused `ReloadCommandInstanceParseTest` + new `ServerInstanceManagerNameResolutionTest` passed (`7` assertions total); no live reload smoke was run.
- Closed the hidden `Weline_Bot` chat/runtime contract drift after the earlier backend E2E-green checkpoint: restored `Weline\Ai\Service\AiService` syntax, added a structured-response entry (`generateStructured()`), rewired `Bot\Service\AgentEngine` off the dead `executeAgent*()` contract and the missing `w_query(''ai'')` default-model lookup, normalized Bot tool definitions to the flat provider format, and revalidated `tests/e2e/specs/backend/weline-bot-backend.spec.js` green at `10 passed` with an assertion that chat JSON no longer leaks `QueryProviderInterface` / old internal contract text.
- Followed up on the same `Weline_Server` shared-state convergence audit with a fourth real fix: stopped instances no longer stay “active” in `server:status --all` just because their persisted record still references adopted `shared_external` session/memory sidecars. `ServerInstanceManager` now excludes those sidecars from tracked PID/process-name and runtime-liveness calculations, `Status` excludes them only in the active-instance filter path, new unit coverage was added, full `Weline_Server` unit tests re-passed (`123` tests / `398` assertions, `1` skip), and live `php bin/w server:status --all` now only shows the real active `default` instance.
- Follow-up live smoke exposed and fixed one more `Weline_Server` status defect in the same pass: explicit `php bin/w server:status test` had still been bypassing stale validation and rendering a stale persisted `test` detail page. `Status::showInstanceStatus()` now goes through the validated lookup path, a regression test covers that branch, the full `Weline_Server` unit suite re-passed at `124` tests / `400` assertions (`1` skip), and live `server:status test` now reports the instance missing instead of showing stale detail.
- Fixed another `Weline_Server` shared-service reuse bug after a fresh multi-instance repro showed workers still failing behind reused Session/Memory: the core failure mode was adopted external shared sidecars being treated as dead just because they never established Master IPC, which queued local takeover and overwrote token files. `ServiceOrchestrator` now preserves alive `shared_external` session/memory instances across health/no-IPC/resurrect paths, focused `ServiceOrchestratorStartupTest` coverage passed (`9/9`), and a disposable smoke run confirmed the remaining live failure is due to the current `default` runtime already being polluted by the old bug and needing a restart under the patched code.
- Followed up on the same WLS shared-session multi-instance bug after newer live logs showed a second root cause: even when `Start` had already ensured shared Session/Memory runtime, `MasterProcess -> ServiceOrchestrator` lost that runtime context and could still launch another local `session_server` on the shared port under Windows.
- Fixed that handoff gap by preserving the resolved runtime under `wls.shared_state.runtime` in `MasterProcess`, then teaching `ServiceOrchestrator` to adopt configured shared Session/Memory sidecars before any local launch path; added focused regression coverage in `ServiceOrchestratorStartupTest`, added `MasterProcessSharedStateRuntimeTest`, and kept `StartSharedStateRuntimeConfigTest` green.
- Focused verification for this follow-up is green: syntax checks on touched files plus PHPUnit for `ServiceOrchestratorStartupTest` (`10` tests), `MasterProcessSharedStateRuntimeTest` (`1` test), and `StartSharedStateRuntimeConfigTest` (`6` tests). Live end-to-end verification still needs restarting the polluted owner instance on the patched code.
- Re-checked the live `default` runtime after the fix and confirmed the remaining failure is inherited bad state, not a new code-path miss: `19970/19971` are still held by local `default` child processes instead of shared sidecars, `var/session/session_server.token` is now missing while that local `session_server` process is still alive, and freshly resurrected workers fail `SessionClient->connect()` for exactly that reason. The most likely source is the old duplicate-local-session bug letting another instance overwrite then delete the same token file; a clean owner restart under the patched code is now the required next step.
- Closed the next WeShop base-layer slice for `Filters`:
  - added `CategoryFilterPageDataService` so category pages always assign `filters`, `applied_filters`, `clear_all_url`, and `filtered_product_ids` even when no filters are currently selected
  - implemented the missing canonical hook `WeShop_Filters::frontend::partials::filters::container`
  - moved storefront filter AJAX onto clean routes `filters/filter`, `filters/options`, `filters/counts`
  - added focused unit coverage (`19` tests / `97` assertions with the category hook-host contract) plus a browser smoke `node tests/e2e/start.js specs/frontend/weshop-filters.spec.js` (`1 passed`)
- Recovered the blocked `9982` WLS acceptance runtime enough to verify the WeShop checkout/order clean-route slice on the intended server:
  - patched `Weline\Framework\System\Process\Processer::isWelineServerProcess()` so shared sidecars launched with quoted `--name="weline-..."` or `session_server.php --shared-service=1` are treated as reusable Weline processes
  - added focused `ProcesserTest` coverage for those two command-line shapes and re-passed the PHPUnit slice (`40` tests / `80` assertions)
  - `php bin/w server:start -p 9982` now reuses the shared session sidecar on `19970` instead of failing with the earlier non-reusable-port error
  - live `curl -k -I https://127.0.0.1:9982/checkout` now returns `301` to `/CNY/zh_Hans_CN/weshop/customer/account/login/`
  - forcing Playwright onto the WLS runtime with `PLAYWRIGHT_RUNTIME_STRATEGY=wls PLAYWRIGHT_E2E_TRANSPORT=direct` made `tests/e2e/specs/frontend/weshop-order-checkout-clean-routes.spec.js` pass (`2 passed`)
  - remaining runtime note: `default` still shows `Memory Service #1` stopped while Session/Dispatcher/Workers stay up, and fallback-runtime Playwright can still hit the unrelated `AbstractProcessDriver.php:102` timeout fatal
- Closed another real WeShop base-layer defect immediately after runtime recovery:
  - `AttributeFilterService` now skips malformed EAV attributes that do not have a persisted `id`, instead of letting them reach `EavAttribute::w_getValueModel()` and crash category-page filter collection
  - added new focused framework coverage in `app/code/Weline/Eav/Test/Unit/Service/AttributeFilterServiceTest.php` and re-passed the existing `WeShop_Filters` unit suite
  - live `curl -k -L -s https://127.0.0.1:9982/catalog/category/view?id=1` no longer returns a fatal page, and forcing `tests/e2e/specs/frontend/weshop-filters.spec.js` onto WLS/direct passed (`1 passed`)
  - follow-up smoke `tests/e2e/specs/frontend/weshop-homepage.spec.js` also passed on `9982`, so the next obvious base-layer gap is now `WeShop_Price` rather than `Store / Frontend`
- Closed the deeper Search/OpenSearch dynamic EAV follow-up:
  - repaired the newly landed browse/query/controller/indexer files that had malformed strings or hidden syntax damage, then finished the provider+extender indexing chain so product search documents can carry `eav_search_text` and nested `eav_facets`
  - unified search, category browse, and filters AJAX browsing on `w_query('search', 'browseProducts', ...)`, while keeping DB fallback behavior and legacy engine compatibility for `category_id` / `price_min` / `price_max`
  - updated the storefront search template to render real facet groups, applied filters, and pagination fallback, and made the default category page tolerate browse-result pagination data
  - fixed the real runtime blocker that had made `php bin/w search:index --yes` fail (`ProductEavService::getAttributeGroups()` was ordering by nonexistent `sort_order`) and synced `app/etc/env.php` with OpenSearch `data_dir` / `log_dir`
  - verification for the slice is green: `php app/code/WeShop/Search/env/script/install_opensearch.php check`, `php bin/w search:index --yes`, focused phpunit (`47` tests / `212` assertions across Search+Filters+Product EAV touchpoints), `php tests/e2e/framework/preflight-refresh.php`, and `node tests/e2e/start.js specs/frontend/weshop-search.spec.js specs/frontend/weshop-filters.spec.js`
- Completed the `Weline_DataTable` frontend demo and E2E closure:
  - kept the pragmatic demo strategy based on inline bootstrap plus demo-only REST APIs instead of going back to repair nested `w:field` compile chains
  - fixed semantic select-type inference, default newest-first sorting, upload field id collisions, and PostgreSQL sequence sync in the demo transaction path
  - focused verification is green: `php vendor/bin/phpunit --no-coverage app/code/Weline/DataTable/Test/Unit --colors=never` (`76` tests / `286` assertions / `1` deprecation), `php tests/e2e/framework/preflight-refresh.php`, and pinned Playwright `node tests/e2e/start.js specs/frontend/weline-datatable.spec.js` (`3 passed`) against `http://127.0.0.1:9996`
  - `php bin/w setup:upgrade --route` remains blocked by the existing CLI argument-validation bug, so that route-refresh check stays recorded as an environment issue rather than a DataTable logic blocker
- Closed the fallback storefront performance blocker that still sat behind earlier Bot/backend green runs:
  - recovered `app/code/Weline/Framework/View/Template.php` from `HEAD` after a prior accidental UTF-16 rewrite
  - traced the real 16s hotspot to eager `QueryProviderRegistry` warmup, not to `WeShop_Analytics`
  - refactored `Weline\Framework\Service\Query\QueryProviderRegistry` to scan provider metadata first and instantiate only the requested provider on `getProvider()`
  - added `Weline\Framework\Test\Unit\Service\Query\QueryProviderRegistryTest` to lock literal provider-name parsing + lazy single-provider instantiation
  - re-ran `Weline_Bot` `AgentEngineTest` and the full grouped `node tests/e2e/start.js` suite green after the change
  - measured the main recovery on the fallback runtime: `getProvider('analytics')` about `15600ms -> 15ms`, `/customer/account/login` about `16.11s -> 0.55s`, `/compare` about `0.40s`
