# 2026-03-23

- Created daily memory file.
- Started a focused WLS concurrency task: initial startup phase still does not truly batch-start services/workers concurrently.
- Scope for this task is intentionally narrow: only fix concurrent startup for the first startup phase and worker startup phase; do not expand into unrelated WLS issues.
- Integrated a first Websites/PageBuilder AI workbench entry slice:
  - Websites `SiteBuilderAgent` is now a friendlier unified entry with provider cards and explicit AI-mode UX.
  - PageBuilder now registers `pagebuilder` under `Weline_Websites` `AiSiteBuilderProvider`.
  - PageBuilder AI workbench index now redirects to the Websites hub by default, while `?legacy=1` still opens the old workbench.
  - Removed the duplicated Websites PageBuilder-group menu entry.
- Completed the WLS startup concurrency fix:
  - root cause was `Processer::batchCreateWindows()` falling back to serial launch whenever any command requested `foreground=true`
  - Windows frontend phase-1 startup and worker startup now keep the batch path and use visible `cmd.exe /d /c <temp>.cmd` launchers for foreground items
  - added batch PID resolution helper plus updated `ProcesserTest`
- Validation passed with `php -l` on touched files and targeted `ProcesserTest`; live `server:status --all` timed out so no fresh end-to-end boot verification was recorded.
- Reworked Codex task logging to eliminate shared mutable `dev/ai/codex/ACTIVE.md` state:
  - startup rules now point to `dev/ai/codex/README.md` and per-task workspaces under `dev/ai/codex/tasks/YYYY-MM-DD/YYYY-MM-DD-HHMM-short-slug/`
  - added `dev/ai/codex/scripts/init-task.php` plus `task.md` / `plan.md` / `progress.md` / `result.md` templates
  - added repo skill `dev/ai/skills/codex-task-workspace/SKILL.md`
  - updated planning/code-generation/testing/skill-mapping docs so Codex delivery now explicitly expects SOLID, TDD, and E2E-aware verification
- Follow-up parameter-isolation audit found one more same-class startup issue:
  - `Processer::create()` had tied managed-process reuse to `!$foreground`, so a display parameter could disable startup de-duplication in strategy-mode and legacy direct-start paths
  - fixed by centralizing reuse policy in `shouldTryManagedProcessReuse()`, which now depends only on `block`
  - re-verified with updated `ProcesserTest` (`31` tests / `66` assertions)
- Refined the broader WLS async-control-plane redesign plan:
  - Master control commands are now explicitly planned around a strict single-active-operation invariant
  - queued command outcomes are defined as `enqueue` / `coalesce` / `preempt` / `reject`
  - child-process protocol events (`register` / `ready` / `disconnect` / `exited`) are explicitly excluded from that queue so they can keep driving async progress
- Created the next implementation workspace for that plan:
  - `dev/ai/codex/tasks/2026-03-23/2026-03-23-0312-wls-single-active-operation-queue-implementation/`
  - scoped as the first coding slice to eliminate overlapping Master control operations before deeper startup/reload state-machine work
- Implemented the first WLS control-plane serialization slice:
  - `ServiceOrchestrator` now has a single-active-operation queue for mutating control commands
  - `handleCommand()` no longer directly runs reload/rolling-restart/maintenance/cache-clear-like mutating flows
  - `stop` now clears queued operations and marks the current active operation as aborting before entering the existing stop pipeline
- Added `ServiceOrchestratorControlQueueTest` and re-ran the focused WLS unit tests:
  - all targeted assertions passed
  - PHPUnit still exits with environment/repo warnings (`no code coverage driver`, one deprecation), so command exit code remains non-zero despite green assertions
- Checkpointed pending WLS control-plane messaging fixes as commit `f97c679c` (`fix(wls): refresh control plane messaging`).
- Continued the next WLS runtime slice focused on reload latency and static cache visibility:
  - `ServerInstanceManager` now uses a persisted-state fast path for instance/worker/dispatcher running stats instead of walking every worker through realtime PID identity checks for CLI gating.
  - `IpcControlGateway` non-wait imperial commands (`reloadAsync`, `cacheClear`) now use short ACK waits plus write-success fallback, so CLI no longer reports false 3s IPC timeouts just because Master is temporarily busy.
  - Added `ServerInstanceManagerRunningStatsTest` and updated `IpcControlGatewayTest`.
  - Re-verified with `php -l`, targeted PHPUnit, repeated static `curl` probes, and live `php bin/w server:reload --no-wait`.
- Continued the follow-up WLS reload-latency slice after that:
  - `ServerInstanceManager` control-plane dispatch now uses persisted instance-file discovery directly, so reload admission no longer pays stale-instance realtime validation before dispatch.
  - `ServiceOrchestrator` periodic maintenance work now polls IPC and yields when queued imperial commands/full-restart requests appear, instead of forcing reload to wait behind long health/reconcile/liveness/audit passes.
  - Updated `BroadcastControlDispatchServiceTest` and reworked `ServerInstanceManagerRunningStatsTest` to exercise the persisted fast path.
  - Live `php bin/w server:reload --no-wait` probes dropped from the earlier ~21s wall time to about 1.7s-1.8s locally.
- Finalized that WLS reload-latency slice:
  - committed as `2e6634db` (`perf(wls): let imperial commands preempt master loop`)
  - post-commit local `php bin/w server:reload --no-wait` probe completed in about `0.69s`
  - remaining reload latency work is now centered on the real rolling-restart body behind `server:reload --wait`, not the CLI/control-plane admission path
- Continued the Websites AI workbench after the entry-bridge slice:
  - `Weline_Websites` now has a first platform-owned resumable workspace shell with create-session, workspace, state JSON, scope merge/replace, stage update, manual message append, and SSE event stream actions.
  - The Websites hub now lets admins create a resumable workbench from the same brief used for quick-build, and it lists recent sessions for one-click resume.
  - Added a new Websites-owned backend workspace template that shows provider context, editable brief fields, message history, advanced Scope JSON editing, recent events, and compatible handoff back to provider-native flows such as PageBuilder legacy workbench.
  - `SessionService` now supports `replaceScope()` and `mergeScope()` for the Websites AI workbench flow.
- Completed the next Websites AI workbench slice for provider-driven scope/tools:
  - added `AiSiteBuilderWorkbenchProviderInterface` plus `ProviderWorkbenchService`, so other modules can declare initial scope/provider-state, initial stage, handoff metadata, and shared-workspace tools
  - `SiteBuilderAgent` now reads provider workbench config during session creation, workspace rendering, recent-session cards, and artifact handoff generation instead of relying only on hardcoded provider matches
  - the Websites workspace now renders provider tools and supports `link` plus `scope_patch` actions
  - `WebsitesDefaultProvider` and `PageBuilderProvider` were upgraded as the first concrete implementations of the new contract
  - validation passed with `php -l`, targeted PHPUnit (`4` tests / `44` assertions, plus one existing PHPUnit deprecation note), and `php bin/w setup:upgrade -m Weline_Websites -m GuoLaiRen_PageBuilder --yes`
  - while validating, a stale timed-out `var/process/setup_upgrade.lock` was confirmed by dead PID and renamed before rerunning upgrade successfully
- Completed the follow-up naming/menu cleanup for the Websites/PageBuilder AI workbench:
  - PageBuilder Quick Build `AI 建站工作台` now points directly to `websites/backend/site-builder-agent/index?provider=pagebuilder`
  - remaining user-visible `建站智能体` labels in PageBuilder/Websites templates, i18n, and AI agent naming were aligned to `AI 建站工作台`
  - validation passed with `php -l` on touched PHP/`.phtml` files and `php bin/w setup:upgrade -m Weline_Websites -m GuoLaiRen_PageBuilder --yes`
  - the first upgrade attempt timed out but left a stale `var/process/setup_upgrade.lock`; the dead-PID lock was renamed to `setup_upgrade.lock.stale-20260323-1417` before rerunning successfully
- Completed the `Weline_Bt_Center` server-management/monitoring slice:
  - added seeded BT server defaults, health fields, 10-minute cron checks, BT health notification topic, and Telegram notification adapter wiring
  - validated the 6 provided BT panels are now inserted and probe as `up` with HTTP `200`
- Useful verification lesson:
  - framework `php bin/w database:query` can return stale cached SELECT results right after writes
  - for immediate persistence checks, prefer direct `psql` queries against PostgreSQL
- Another framework persistence lesson from `Weline_Bt_Center`:
  - `AbstractModel->__call('delete')` only prepares the delete query
  - model deletions are not executed until `->fetch()` is called, so controller-level `delete()` alone can return a false-success flow
- Continued the WeShop auth/API foundation on the user-corrected runtime port `9982`:
  - confirmed the real live WeShop auth URL is `https://127.0.0.1:9982/api/weshop/rest/v1/auth/*`
  - the earlier `401 {"code":401,"msg":"请先登录","data":null}` was not only `Weline_Api`; `Weline_Acl::RouteBefore` was also re-gating the same public auth routes
  - added `Weline\Framework\Http\PublicApiAuthRouteMatcher` and wired both `Weline\Api\Observer\ApiControllerInitBefore` and `Weline\Acl\Observer\RouteBefore` to use it
  - targeted tests passed for the matcher, the API observer, and the ACL observer
  - live probes on `9982` now reach `WeShop\Auth\Api\Rest\V1\Auth::postToken()` and `WeShop\Auth\Api\Rest\V1\Auth\Challenge::postVerify()` and return controller-level `422` validation errors instead of framework `401`
- Fixed the `Weline_Bt_Center` backend management follow-up:
  - added module `etc/env.php` router metadata and moved the backend controller onto the `Weline\Bt\Center\...` namespace expected by the module route scanner, with a compatibility alias back to `Weline\Bt_Center\...`
  - corrected BT backend menu/list/form URLs to use the working `*/backend/bt-server...` paths and refreshed the list/form page layout structure to fit backend card/grid conventions better
  - discovered the real local backend entry is a random `app/etc/env.php` prefix (`f7LYPUzS4UD9UL1kqkf0hzzPxyxmvT8c` here), so raw `/backend/...` probes were misleading during validation
  - changed the BT delete action from `delete()` to `postDelete()` so the list page's AJAX `POST` matches the generated backend route; unauthenticated probes now hit login redirects instead of `404`
- Continued WeShop with a production-facing `ImportExport` slice:
  - replaced the placeholder `ImportExportService` with real CSV product export, product import, and order export logic
  - normalized product import defaults so SKU/name/price-only CSV rows can still be persisted with generated SPU/meta/descriptions/status/weight/image defaults
  - replaced the two zero-byte backend import/export controllers with thin service adapters and added a backend landing page plus `Weline_Backend::data_tools_group` menu entry
  - targeted ImportExport PHPUnit assertions passed; PHPUnit still reports the repo-wide no-coverage-driver warning so raw exit codes stay non-zero
- Followed up on WLS stop/reload exit sequencing:
  - `ServiceOrchestrator` stop flow now batch-dispatches child exits in phase 3, treats phase 4 as a non-blocking settle, and moves the real wait/verification/force-kill work into phase 5
  - added `ServiceOrchestratorStopFlowTest`
  - focused `php -l` and PHPUnit assertions passed; PHPUnit still reports the existing coverage warning/deprecation environment
- Resumed that WLS stop/reload exit-sequencing slice later in the day:
  - removed an accidental UTF-8 BOM from `ServiceOrchestrator.php` that had been introduced during an earlier file restore and temporarily broke `strict_types`
  - re-confirmed the stop-flow patch was clean and re-ran the focused syntax/PHPUnit checks successfully
- Continued WLS stop-flow debugging from a real user stop trace:
  - found that `broadcastDrainToAll()` was incorrectly sending per-instance ports during global stop, so `Dispatcher` interpreted stop-all drain as selective worker drain and never completed phase-2 drain
  - hardened stop-flow disconnect handling so draining instances move to `STATE_STOPPING` on disconnect instead of remaining stuck in `STATE_DRAINING`
  - added a `Stop` CLI fallback to switch into `waitForMasterExit()` once all child exits are already observed, even if the explicit final Master-exit progress sentence is missing
  - added focused unit tests for both the orchestrator and stop-command heuristics; all targeted assertions passed, with the same existing PHPUnit coverage warning/deprecation noise
- Resumed the AI site workbench E2E pass late in the day:
  - local default runtimes had drifted again: the earlier healthy `9982` WLS runtime was no longer stable, and the remembered `9981` CLI listeners were stale/unresponsive
  - patched `tests/e2e/framework/runtime.js` so backend URL helpers honor `PLAYWRIGHT_DISABLE_PROXY` and `loginAsAdmin()` now derives the real logged-in backend root from `page.url()`
  - patched `tests/e2e/specs/backend/helpers/ai-workbench.js` so the Websites AI workbench route reuses that localized backend root instead of only the backend prefix
  - hardened `tests/e2e/specs/backend/ai-site-workbench.spec.js` by serializing the three scenarios and explicitly reloading the workspace after the PageBuilder provider-tool stage transition
  - green verification used a clean local PHP built-in server target on `http://127.0.0.1:9991` with `PLAYWRIGHT_TARGET_ORIGIN=http://127.0.0.1:9991` and `PLAYWRIGHT_DISABLE_PROXY=1`
  - final wrapper command `cd tests/e2e && node start.js specs/backend/ai-site-workbench.spec.js` passed again under that override with `3 passed`
- Completed the PageBuilder/Blog publish-visibility and cache invalidation fix:
  - added `GuoLaiRen\Blog\Service\BlogSiteCacheInvalidator` and wired it into backend blog create/edit/publish/delete/batch flows plus the AI publish cron, so published blog changes now clear frontend/router cache and attempt CDN purge for affected site IDs
  - discovered the user-facing display bug was deeper than cache: the active `ludo-empire` PageBuilder `blog-list.phtml` was hardcoded with demo posts and ignored runtime `blog_posts`
  - updated `ludo-empire`, `rummy-royal`, and `poker-arena` blog-list templates to consume runtime posts on live pages, keep sample posts only for preview/editor mode, and show an explicit empty state when a live site has no published articles
  - verified end-to-end with a temporary published post on `http://127.0.0.1:18081/blog`, confirmed the article appeared after invalidation, then deleted the smoke post and re-verified the site returned to empty state
  - direct PostgreSQL verification confirmed site `1` had `0` published posts after cleanup; CLI blog queries again showed the framework's known stale cached-SELECT behavior, so direct DB queries remain the reliable immediate source of truth after writes
- Finalized the AI site workbench E2E follow-up after that:
  - found the real remaining instability was the launcher/runtime path, not the workbench business flow: stale proxy listeners on `3999`, drifting WLS `9982`, and socket-level reachability checks that were too weak for a reliable default path
  - updated `tests/e2e/framework/runtime.js` so direct-mode backend/API URLs use the real runtime prefixes instead of proxy-only alias paths
  - updated `tests/e2e/specs/backend/ai-site-workbench.spec.js` to wait for the workspace page's own auto-reload after provider-tool application instead of racing it with a second manual reload
  - reworked `tests/e2e/start.js` to prefer a stable local PHP runtime when the caller does not explicitly pin `PLAYWRIGHT_TARGET_ORIGIN`, defaulting the wrapped run to direct mode for a more predictable local E2E experience
  - verified the true no-env wrapper path now passes again: `cd tests/e2e && node start.js specs/backend/ai-site-workbench.spec.js` -> `3 passed`
- Continued the WLS live stop-latency slice after the user-reported IPC timeout traces:
  - tightened `Stop` CLI Master-exit waiting so it only switches on explicit `Master 即将退出...` progress instead of inferring from “all children exited”
  - added `ServiceOrchestrator::finalizeStopAllMasterExit()` so the stop flow ends with an explicit Master exit instead of lingering after child cleanup
  - changed Windows stop phase-3 termination to detached batch dispatch and limited it to IPC-disconnected residual children, keeping Master off synchronous per-process polling during the critical stop path
  - optimized Windows small PID-set running checks to avoid the expensive full `tasklist` scan in stop verification
  - focused syntax/PHPUnit verification stayed green apart from the existing repo warning/deprecation noise
  - live `php bin/w server:stop default` improved from the earlier reproduced ~`61.9s` down to a latest measured ~`32.3s`
  - the major user-visible fix is that the old phase-3/phase-4 idle hole is gone; remaining stop latency is now concentrated in phase-5 exit verification
- Continued the next WLS stop-tail slice after committing that pass:
  - `ServiceOrchestrator::verifyAndKillRemainingProcesses()` now begins from one batched PID liveness snapshot and splits phase-5 verification by control-plane state
  - IPC-connected children still keep the short graceful verification window, but IPC-disconnected residuals now skip that wait and fall straight into the final batch cleanup set
  - added a focused `ServiceOrchestratorStopFlowTest` regression proving disconnected residual PIDs no longer consume the graceful verification loop
  - targeted syntax/PHPUnit checks stayed green apart from the existing no-coverage-driver warning/deprecation noise
  - live validation is currently blocked by a separate runtime issue: local instances can still report healthy workers while the recorded `master_pid` is already dead, and a fresh `server:start -frontend -p 9982` probe later timed out after about `304s`
- Continued the WLS runtime-hardening follow-up after that:
  - added `Weline\Server\Service\LongRunningPhpRuntime` and applied it in `MasterProcess` plus the long-lived WLS entry scripts (`worker`, `worker_ssl`, `session_server`, `dispatcher`, `http_redirect_worker`) so PHP `max_execution_time` and abort defaults stop killing the control plane
  - found the separate false-negative stop root cause on Windows foreground runs: the foreground Master is the original `php bin/w server:start ... -frontend` process, so its real command line has no `--name=...`, but `Processer::isManagedProcessRunning()` previously insisted on seeing that synthetic managed name and therefore treated a live foreground Master as dead
  - fixed that by trusting the recorded `command_line_hash` when it still matches the live process command line, then added a focused `ProcesserTest` regression for the foreground Master case
  - live detached foreground verification with `codex-fg-hash` on `9990` confirmed `php bin/w server:stop codex-fg-hash` now stays on the normal IPC stop path instead of immediately dropping into `Master 进程不存在` residual cleanup
- Completed the `WeShop_RecentlyViewed` storefront slice:
  - added clean `recently-viewed` routes, a guest-safe remove endpoint, a page-data service, and a default-theme history page
  - wired logged-in product detail views into a new `StorefrontRecentlyViewedRecorder`
  - updated account quick links and account-center page links to use the clean `wishlist` / `recently-viewed` routes where available
  - targeted `RecentlyViewed` unit tests now pass assertions (`7 tests / 28 assertions`), with the same repo-wide no-coverage-driver warning
  - `setup:upgrade -m WeShop_RecentlyViewed -m WeShop_Product --yes` refreshed routes successfully; live smoke while WLS was up on `9982` confirmed guest redirect on `GET /recently-viewed` and JSON redirect payload on `POST /recently-viewed/remove`
  - next adjacent WeShop storefront candidate remains `WeShop_Compare`
- Completed the next WeShop storefront slice for `WeShop_Compare`:
  - added clean `compare` routes, thin add/index/remove controllers, a compare page-data service, and a new `default` compare page
  - wired compare entry points into product detail via hook, category cards via default-theme actions/hooks, and account center via quick links plus discovery-card hook
  - enriched account dashboard aggregation so compare preview/count also feed recommendations
  - targeted Compare unit tests and the touched `AccountDashboardDataServiceTest` now pass assertions; PHPUnit still exits with the existing no-coverage-driver warning
  - `setup:upgrade -m WeShop_Compare -m WeShop_Customer --yes` refreshed routes successfully; live smoke was blocked because no listener was active on `127.0.0.1:9982` in this shell and `http:req` still defaulted to stale `9981`
