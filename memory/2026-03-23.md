# 2026-03-23

- Created daily memory file.
- Started a focused WLS concurrency task: initial startup phase still does not truly batch-start services/workers concurrently.
- Scope for this task is intentionally narrow: only fix concurrent startup for the first startup phase and worker startup phase; do not expand into unrelated WLS issues.
- Completed the WLS startup concurrency fix:
  - root cause was `Processer::batchCreateWindows()` falling back to serial launch whenever any command requested `foreground=true`
  - Windows frontend phase-1 startup and worker startup now keep the batch path and use visible `cmd.exe /d /c <temp>.cmd` launchers for foreground items
  - added batch PID resolution helper plus updated `ProcesserTest`
- Validation passed with `php -l` on touched files and targeted `ProcesserTest`; live `server:status --all` timed out so no fresh end-to-end boot verification was recorded.
- Reworked Codex task logging to eliminate shared mutable `dev/ai/codex/ACTIVE.md` state:
  - startup rules now point to `dev/ai/codex/README.md` and per-task workspaces under `dev/ai/codex/tasks/YYYY-MM-DD/YYYY-MM-DD-HHMM-short-slug/`
  - added `dev/ai/codex/scripts/init-task.php` plus `task.md` / `plan.md` / `progress.md` / `result.md` templates
  - added repo skill `dev/ai/skills/codex-task-workspace/SKILL.md`
  - updated planning/code-generation/testing/skill-mapping docs so Codex delivery now explicitly expects SOLID, TDD, and E2E-aware verification
- Follow-up parameter-isolation audit found one more same-class startup issue:
  - `Processer::create()` had tied managed-process reuse to `!$foreground`, so a display parameter could disable startup de-duplication in strategy-mode and legacy direct-start paths
  - fixed by centralizing reuse policy in `shouldTryManagedProcessReuse()`, which now depends only on `block`
  - re-verified with updated `ProcesserTest` (`31` tests / `66` assertions)
