# 2026-03-24

- Created the 2026-03-24 WeShop Review storefront slice workstream (task `2026-03-24-0109-weshop-review-slice`).
- Continued the broader WeShop storefront completion wave with a local QA/Review/default-theme compatibility batch:
  - restored `WeShop_QA` hook registration and added default-theme QA page hook docs
  - cleaned up Review hook docs and default-theme review form handling
  - kept the shared customer account `orders::cards` host slot and product tab hook hosts as the compatibility baseline for post-purchase/storefront modules
- Targeted PHPUnit for `WeShop_QA` and `WeShop_Review` both passed assertions again; repo-wide `No code coverage driver available` still makes the raw exit code non-zero.
- `php bin/w setup:upgrade -m WeShop_QA -m WeShop_Review --yes` no longer fails on hook-doc lookup, but the command is still blocked later by an unrelated environment/module path that tries to instantiate the removed SQLite adapter instead of Pgsql.
- Parallel worker follow-up:
  - `WeShop_Invoice` storefront/account slice completed in a sidecar worker and is ready for local review/integration
  - `WeShop_Subscription` worker is still in progress
- Reviewed and tightened the next storefront/account batch after the QA/Review commit:
  - `WeShop_Invoice` now has a clean `invoice` route, default-theme invoice page, account-center order card, and customer-wide invoice summary counts
  - `WeShop_Subscription` now has a clean `subscription` route, list/detail default-theme pages, account-center order card, and targeted service tests
- Targeted `Invoice` / `Subscription` syntax checks and PHPUnit assertions passed; `setup:upgrade -m WeShop_Invoice -m WeShop_Subscription --yes` still fails later because of the same unrelated SQLite adapter environment issue.
- Completed `WeShop_GiftCard` storefront/default-theme/account-center slice:
  - added clean `gift-card` route, thin storefront controllers, `GiftCardPageDataService`, upgraded `GiftCardService` customer-scoped summary/query flow, and account-center discovery hook card injection
  - added default theme `gift-card` page and GiftCard hook manifest/docs
  - added unit tests for GiftCard page-data, summary behavior, and storefront controller
  - syntax and GiftCard PHPUnit assertions passed; `setup:upgrade -m WeShop_GiftCard --yes` still fails later due the same unrelated global SQLite adapter issue
- Completed `WeShop_Affiliate` storefront/default-theme/account-center slice:
  - added clean `affiliate` route, thin storefront controllers, `AffiliatePageDataService`, and upgraded `AffiliateService`/`Affiliate` model summary flow
  - added Affiliate hook manifest/docs, account-center discovery hook card, and default theme `affiliate` page
  - added unit tests for Affiliate page-data/controller/service summary
  - syntax and Affiliate PHPUnit assertions passed; `setup:upgrade -m WeShop_Affiliate --yes` still fails later due the same unrelated global SQLite adapter issue
- Completed the `WeShop_Address` storefront/account slice:
  - replaced the legacy ObjectManager/session flow with `CustomerContextInterface` controllers, `AddressPageDataService`, and an `AddressService` facade over `Weline_Shipping` delivery addresses
  - added clean `address` routing, default-theme address book page, and account-center discovery card
  - normalized customer-account and checkout-success links to the clean `address` route and preserved checkout compatibility by mapping delivery-address fields back into the legacy address keys it already expects
  - hardened `Weline_Shipping\Service\DeliveryAddressService` delete execution and widened phone/postcode validation toward international addresses
  - targeted Address PHPUnit assertions passed (`7` tests / `38` assertions); `setup:upgrade -m WeShop_Address --yes` progressed through module refresh and then failed later on the same unrelated `Aiweline\Stock` SQLite/Pgsql environment issue
- Parallel audit follow-up after Address:
  - highest storefront/theme compatibility gaps now sit in `Catalog` canonical hook hosts, `Filters` category-page container hosts, and `Checkout` shipping host parity across layout variants
  - safest next backend/admin slice is `WeShop_Promotion` menu/tests/controller hardening; highest-value larger slice is `WeShop_Order` backend/API completion
- Completed the follow-up default-theme compatibility slice after that:
  - normalized `app/design/WeShop/default` catalog category/listing templates to expose canonical `WeShop_Filters::frontend::partials::filters::*` and `WeShop_Catalog::frontend::layouts::category::products-content` hosts while preserving legacy `Weline_Theme` sidebar fallback and built-in fallback markup
  - added `WeShop_Shipping::checkout::methods` to checkout layout variants `1..4`, so shipping modules can inject dynamic method UI consistently across the default checkout layouts
  - added view-guard unit tests for the category/listing and checkout hook-host contracts
  - focused syntax checks and targeted PHPUnit passed (`3` tests / `39` assertions, plus the existing repo PHPUnit deprecation noise)
- Delivered the `WeShop_Report` backend slice: added the router/menu entry, repository/service/controller/template, localized copy, and unit tests so admins can see a simple sales-summary dashboard.
- Completed the local `WeShop_Order` backend/admin slice:
  - added backend order-management menu wiring under `Weline_Backend::order_group`
  - replaced the empty backend order controllers with list/detail/status-update flows backed by a new `OrderAdminPageDataService`
  - expanded `OrderService` with admin query, summary, and status-validation helpers
  - created backend list/detail templates plus backend/unit tests for the new admin slice
  - targeted syntax checks and `php vendor/bin/phpunit --no-coverage app/code/WeShop/Order/Test/Unit --colors=never` passed (`13` tests / `19` assertions); `setup:upgrade -m WeShop_Order --yes` later failed on the repo-wide/global schema issue `未知的索引类型：BTREE`
- Completed the Promotion backend slice:
  - introduced PromotionCouponRepository and PromotionCouponManagementService so coupon stats and persistence are centralized
  - refactored the backend Coupon backend controllers to delegate to the service, added the marketing menu entry/template, and kept translations in sync
  - added PromotionCouponManagementServiceTest, ran PHP lint/phpunit, and captured the slice in the task workspace
