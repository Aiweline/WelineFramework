# 2026-03-21

- 新增工作区约定：Codex 每次任务都必须在 `dev/ai/codex/` 中落盘，记录任务目标、进展、风险、结果与恢复信息，避免网络或会话中断导致上下文丢失。
- 纠正后的项目级约定：`dev/ai/plans/AI建站智能体的计划` 仅保留计划项愿景文本；结构化方案写入 `dev/ai/codex/plans/`（如 `ai-site-agent.plan.md`）；单次执行与恢复写入 `dev/ai/codex/tasks/` 与 `ACTIVE.md`。实现向进度另见 `dev/ai/plans/codex-pagebuilder-ai-site-agent.plan.md`。
- 已完成 `dev/ai/plans/codex-plan-recovery-followup.plan.md` 所列收尾（核对原文档、补强 codex 计划交叉引用、任务日志与 ACTIVE）。

- 接管 `Weline/Theme` 主题改造未完成工作，确认主线是“前后台主题拆分 + scope 化配置读取 + 预览图分区持久化”。
- 核对现有改动后，继续补齐 runtime 链路中的旧逻辑：多个入口原本仍用全局 `getActiveTheme()`，会让 backend 主题继续吃到 frontend/legacy 激活态。
- 已修正按 `area` 取主题的关键入口：主题布局解析、partials 解析、主题编辑器默认主题、资源/配置/变量扫描、主题目录解析、组件目录解析、`theme` 查询提供器。
- 当前 `Weline_Theme` 模块全量 PHPUnit 仍有历史失败项（Taglib mock / `reset` mock / `static` 返回类型 mock），本次未扩散新语法错误；已完成相关文件 lint。
- 核对并完成 `dev/ai/plans/codex-slot-compile-lifecycle.plan.md`：代码侧已有编译周期重置 + `file/line` 注入 + `StateManager` 对 `Slot::$registeredSlots` 的请求级重置；`SlotTaglibCompileStateTest` 过滤运行通过；计划与 `dev/ai/codex/tasks/2026-03-21/2026-03-21-1630-slot-compile-lifecycle-plan-closeout.md` 已更新。

- 推进 `dev/ai/plans/codex-pagebuilder-ai-site-agent.plan.md`：在 `GuoLaiRen_PageBuilder` 增加 `AiSiteAgentSession` / `AiSiteAgentSessionEvent` 与 `AiSiteAgentSessionService`，模块版本 1.0.24；虚拟部件仍走 `Weline_Theme`，会话用 `weline_theme_id` 关联。
- 完成同一计划 todo 3：`PageBuilderThemeComponentBridge`、`PageRenderService::render` 第五参 `weline_theme_id`、`ComponentRenderer` 虚拟回退；`register` 1.0.25 + 依赖 `Weline_Theme`；预览 `weline_theme_id` 查询参数。

- 继续 `dev/ai/plans/codex-pagebuilder-site-builder.plan.md`：在「快速建站」菜单下增加 PageBuilder 原生「AI 建站工作台」（`AiSiteAgent` 控制器 + 会话列表/创建/工作台视图），并扩展 `AiSiteAgentSessionService::listRecentSessionsForAdmin`。任务记录见 `dev/ai/codex/tasks/2026-03-21/2026-03-21-1730-pagebuilder-site-builder-plan.md`。

- 推进 `codex-pagebuilder-ai-site-agent`：`AiSiteAgent` 增加会话 JSON API、`getStreamSse` 增量事件流、`replaceScope`/`listEventsAfterId` 等服务方法；工作台支持 Scope/阶段/绑定操作与 `weline_theme_id` 全页预览链接（`scope.preview_page_id`）。任务记录 `dev/ai/codex/tasks/2026-03-21/2026-03-21-1800-ai-site-agent-api-sse.md`。

- 建站向导：`workspace` 注入 `scope`/`wizard_links`；八阶段 Pill + 分步表单经 `mergeScope` 写入约定字段，JSON 编辑折叠为高级区。任务 `dev/ai/codex/tasks/2026-03-21/2026-03-21-1900-ai-site-agent-wizard-ui.md`。

- 向导接入 `postAiGenerateBrief`（AiService + AiResponseJsonParser）与 `postQueryDomainStatus`（QuickBuildAggregator 生命周期）。任务 `dev/ai/codex/tasks/2026-03-21/2026-03-21-2000-ai-site-agent-wizard-ai-domain.md`。
- 清理误恢复计划：删除 `dev/ai/plans/codex-homepage-plugin-gating.plan.md`，确认其为非本项目计划，仅在任务日志中保留历史痕迹，避免后续恢复流程再次误用。

- 完成 `dev/ai/plans/codex-theme-runtime-unification.plan.md`：`ThemeContextService` 统一 Partials / TemplateFetchFile / ControllerFetchFileBefore 的预览与 scope；AssetMerger、ConfigMerger、ThemeConfigManager、ThemeQueryProvider、CLI `theme:active` 对齐；新增 `ThemeContextServiceTest`；需 `setup:upgrade` 刷新 DI。任务见 `dev/ai/codex/tasks/2026-03-21/2026-03-21-theme-runtime-unification.md`。

- PageBuilder AI 建站：`Visual/Api/Component` 的 `postPreview`/`postAdd` 透传 `weline_theme_id`；`ComponentRenderer::renderPreview` 支持合并额外选项；可视化 `visual_config`/`visualConfig` 预览 iframe 与 `component_panel` 预览请求带主题 ID；`register` 1.0.27。
- PageBuilder：`SlotValidator` + `getComponentsForBuilder` 支持会话 `weline_theme_id` 下列出/校验 Weline 虚拟部件；`component/list` 与 `postValidate` 透传；i18n「主题虚拟部件」；`register` 1.0.30。
- PageBuilder：`component/compatible` 与 `component/slots` 也支持 `weline_theme_id` / `theme_component_area`，虚拟部件可查询兼容列表与容器 slots；`register` 1.0.31。
- AI 建站向导补强：新增 `postPublishChecklist`（会话发布前检查并落 `publish_check` 事件）、域名阶段「查询状态」按钮回补、等待阶段自动轮询（10 秒）开关与发布检查结果面板；已执行 `setup:upgrade -m GuoLaiRen_PageBuilder --yes` 刷新路由。
- AI 建站 todo6 自动化验证：改用直接 `vendor/phpunit` 执行 `GuoLaiRen_PageBuilder` 套件，结果 `34 tests / 99 assertions / 0 failure / 0 risky`（4 skipped，1 deprecation）；同时修复 `SlotValidatorTest::testSlotPlacement_Valid` 无断言风险。
- AI 建站联调续：已启动并确认 WLS `https://127.0.0.1:9981` 可访问，`aiSiteAgent/index` 在未登录态正确 302 到后台登录页；当前 CLI 无后台会话，API/SSE 端到端联调标记为待登录态补测。
