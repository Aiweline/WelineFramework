# 指令：产品图优化

**MCP：跳过**（`content_ops_skills_skip_mcp`）。勿 `prepare_project` / `get_skill`；宿主 Read 本文件 + 仓内技能。

**触发词**

- `产品图优化`
- `修产品图`
- `低质量主图` / `详情图`（图质量）
- `双层背景` / `框中框` / `接缝` / `假拓边`

> **层级**  
> 只做主图/画廊/规格 + 详情实拍像素。  
> **必须并行开 2 子智能体**（①主图管线 ②详情实拍）。  
> 要翻译 → 改用 `产品优化.md`。

```
产品图优化
├── ① 主图 / 画廊 / 规格
└── ② 详情·实拍图
排队：product_id 倒序；台账只写 last_product_id
```

## 权威

| 层 | 路径 |
|----|------|
| 本指令 | `dev/ai-command/product/产品图优化.md` |
| 技能 | `app/code/Weline/Product/doc/ai/skills/ecommerce-product-image-optimize/SKILL.md` |
| ① | `ecommerce-product-image/SKILL.md` + `weline-image-pipeline.md` |
| ② | 管线 §7；必要时 `ecommerce-detail-suite` + `详情优化.md` |
| 进度 | `app/code/Weline/Product/doc/evidence/product-image-optimize-progress.v1.md`（**仅** `last_product_id`） |

## Agent 步骤

1. 读技能 + 进度台账 `last_product_id`。  
2. 积压按 `product_id` **DESC** 取下一批（默认 12）。  
3. 并行 ①②；备份 → 剥框 → 真 outpaint → `replaceContent`。  
4. 类审接缝/框中框/假垫边 → 0 BAD。  
5. 更新台账：**只改** `last_product_id=`。  
6. 汇报本波 IDs + 新 `last_product_id`。
