# I18n 语言申请、统一表单与 Captcha 实施归档

本文件保留本次需求与工程边界。具体完成状态和验收证据见
`dev/ai/tasks/2026-07-27-i18n-language-request-captcha-form.md`。

## 固定边界

- 原生 `<form>` 不受影响；只有 `<w:form>` 使用框架协议。
- 前端业务请求只走 `Weline.Api` / QueryProvider。
- 申请站点来自服务端 RequestContext，`website_id=0` 合法。
- `accepted` 与 `ready` 分离；网站只消费 ready。
- Website 语言分配只增不删，不修改默认语言。
- Google 已配置但校验失败时 fail closed；仅未启用 Google 时使用一次性本地挑战。
- 核心先在官方框架仓库完成，再通过核心更新流程同步 QiPai。
