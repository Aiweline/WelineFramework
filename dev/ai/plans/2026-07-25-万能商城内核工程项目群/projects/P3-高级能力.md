# P3 高级能力项目计划

## 1. 阶段目标与滚动细化

P3 在 P2 交易事实稳定后增加多仓、完整税务和搜索，但不能改变 P2 旧单快照或事实源。

**验收状态（2026-07-25）：** `GATE-P3A=GO`；`GATE-P3B=GO`；`GATE-P3C=GO`；**`PG-6 / GATE-P3 = GO`**；`PRJ-WAREHOUSE-30` / `PRJ-TAX-31` / `PRJ-SEARCH-32` 全部 **DONE**。解锁 RT-4。

> **命名归属（`DEF-AUDIT-05`，2026-07-25）：** 不存在独立的 `Weline_Warehouse` 模块。`PRJ-WAREHOUSE-30` 的多仓能力实现在 **`Weline_Inventory`** 内：`Model/Warehouse.php`、`Model/WarehousePool.php`、`Service/DefaultLogicalWarehouseResolver.php` 等。查代码请按 `app/code/Weline/Inventory/**` 定位，不要按项目名找模块目录。
>
> **程序群状态：** `PROGRAM_DONE` / `PG-8=GO`（2026-07-25）。`PRJ-WAREHOUSE-30` 实现归属见上（`DEF-AUDIT-05` FIXED）。

当前项目边界、TASK、依赖和 Gate 已冻结并验收完毕；禁止以滞后「解锁状态」段落回退为 `IN_PROGRESS`/`READY`。

## 2. 修正后的依赖

```text
GATE-P2 / PG-5
  ↓
PRJ-WAREHOUSE-30: P3A-001 → P3A-002(mode off) → MIG-P3A → GATE-P3A
  ├─ PRJ-TAX-31: P3B-001 → P3B-002(mode off) → MIG-P3B → GATE-P3B
  └─ PRJ-SEARCH-32: P3C-001 → P3C-002(mode off) → MIG-P3C → GATE-P3C

GATE-P3A + GATE-P3B + GATE-P3C → PG-6 / GATE-P3
```

L4 卡：`projects/l4/PRJ-WAREHOUSE-30-L4.md`、`PRJ-TAX-31-L4.md`、`PRJ-SEARCH-32-L4.md`。

迁移/cutover 必须在完整目标能力和安全 fallback 已经以 mode off 构建后执行。禁止先迁移再补 reader、degraded、refund 或 snapshot 能力。

## 3. `PRJ-WAREHOUSE-30` 多仓履约

### 目标

把 P2 Store 逻辑库存映射到默认逻辑仓，再交付 Warehouse/Pool/Quota、仓维 reservation、部分发货和原仓退款；normal/test 不共享真实物理仓。

### 工作包

| 顺序 | TASK | 输出 | 关键验证 |
|---:|---|---|---|
| 1 | `TASK-P3A-001` | **ACCEPTED** additive Warehouse/Pool/Quota、默认逻辑仓、授权矩阵 | schema/API、授权、默认仓唯一 |
| 2 | `TASK-P3A-002` | **ACCEPTED** mode-off 仓维部分发货 CAS、原仓 refund step、新单默认逻辑仓 | 并发、部分履约、退款来源、P2 兼容 |
| 3 | `TASK-MIG-P3A` | **ACCEPTED** checkpoint、逐 Offer 映射/守恒、allowlist cutover | fresh verify、重复 apply、mode-off 回退 |

**`GATE-P3A-BUILD = GO`**（`2300-task-p3a-001-002-warehouse`）。  
**`GATE-P3A = GO`**（`2315-task-mig-p3a-warehouse`）。Tax / Search 已解锁。

### L4 必拆项

- Warehouse/Pool/Quota 模型与唯一键；
- Store→default logical warehouse 确定映射；
- Inventory capability 的仓维版本；
- Reservation warehouse owner/version/CAS；
- Order FulfillmentUnit/部分发货；
- Refund 原仓独立 step；
- MIG preflight/checkpoint/apply/fresh verify/allowlist；
- 文档、指标、reconcile 和回滚。

### DoR

`PG-5` 通过；P2 Inventory/Order/Refund 接口版本冻结；默认逻辑仓语义和 normal/test 隔离明确；所有共享 Inventory/Order 文件有串行 Owner；隔离数据库与库存样本可重算。

### DoD

逐 Offer 数量和 ledger 守恒；仓维并发不超卖；部分发货和退款不改旧快照；mode off 后新单回默认逻辑仓、旧仓单继续履约；R4 `GATE-P3A` 通过。

### 停止条件

需要改写 P2 历史 ledger、无法证明原仓退款、normal/test 必须共享真实仓、迁移前必须开启 writer、无法在 mode off 保持旧单可读。

## 4. `PRJ-TAX-31` 完整税务

### 目标

交付 Scope-aware Tax rules/engine、shadow comparator、同版本 LKG 和 Checkout/Order/Invoice 税快照；错误时阻断新结账，绝不回零税。

### 工作包

| 顺序 | TASK | 输出 | 关键验证 |
|---:|---|---|---|
| 1 | `TASK-P3B-001` | **ACCEPTED** TaxClass/Rule、SystemConfig adapter、engine、shadow diff、versioned LKG | 规则优先级、Scope、版本、错误/LKG |
| 2 | `TASK-P3B-002` | **ACCEPTED** Checkout/Order 税快照；mode off stub；无 LKG 阻断；规则版本冲突 | TEST-P3B-02/03/04 |
| 3 | `TASK-MIG-P3B` | **ACCEPTED** checkpoint、100 shadow samples、allowlist cutover、fresh verify | 零未分类 mismatch、同版本 LKG、mode off |

**`GATE-P3B = GO`**（`2310-task-mig-p3b-tax-cutover`）：隔离 clone 门禁、verified LKG、allowlist 与 immutable snapshot rollback 已通过 harness；共享开发库 setup 仍保留 Schema checkpoint 阻断证据，未绕过。

### L4 必拆项

- Tax module skeleton/models/indexes；
- TaxEngine public interface 与错误模型；
- SystemConfig 四层 adapter；
- rule version/LKG store；
- shadow comparator 和分类指标；
- Checkout/Order/Invoice snapshot 接入；
- MIG shadow/allowlist；
- Browser/Order/Invoice/日志证据与 docs。

### DoR

`GATE-P3A` 通过；P2 Tax stub、Checkout quote、Order/Invoice snapshot 扩展点冻结；税务 jurisdiction/rounding/precision 规则在 L4 明确；存在不含真实税务责任的受控样本。

### DoD

旧单不回算；新报价携带 rule/schema/version；错误只使用同版本已验证 LKG，否则阻断；shadow mismatch 全部分类且阈值满足；R4 `GATE-P3B` 通过。

### 停止条件

业务要求 Tax 错误回零、LKG 跨版本、修改旧单快照、客户端计算税、规则/管辖区仍需执行者临场决定。

## 5. `PRJ-SEARCH-32` 搜索与索引

### 目标

交付 Website shard index、全量/增量 watermark、Scope/locale/currency query、alias CAS 和显式 degraded direct read；Product 仍是唯一目录事实源。

### 工作包

| 顺序 | TASK | 输出 | 关键验证 |
|---:|---|---|---|
| 1 | `TASK-P3C-001` | **ACCEPTED** Framework extends schema provider、Search shard/registry、indexer、full/incremental watermark、queue | shard fingerprint、重建、乱序/重复增量（TEST-P3C-01） |
| 2 | `TASK-P3C-002` | **ACCEPTED** mode-off Scope filter、locale/currency、degraded marker、direct reader、recovery gate | 跨站隔离、索引不可用、恢复前水位（TEST-P3C-02/03） |
| 3 | `TASK-MIG-P3C` | **ACCEPTED** checkpoint、shadow index、全量+增量追平、alias CAS、`commerce:migrate-p3c-search` | 追平阈值、CAS 冲突、失败回 direct read（`SearchMigrationServiceTest`） |

**`GATE-P3C = GO`**（BUILD + MIG，`2026-07-24-2354-slug-task-mig-p3c-search-cutover`）。Search schema provider 唯一路径为 `Weline/Search/extends/module/Weline_Framework/Schema/SearchShardSchemaProvider.php`；禁止在 `Setup/` 创建平行 Provider。

### L4 必拆项

- module/schema provider/registry；
- index document version 和 Scope fields；
- full builder；
- incremental consumer/watermark/idempotency；
- query interface/filter/currency/locale；
- degraded direct reader/marker/recovery；
- shadow comparator；
- MIG watermark/alias CAS/rollback；
- Browser/metrics/docs。

### DoR

`GATE-P3A` 通过；Product published read/query 契约、Queue Envelope、Shard SPI 冻结；搜索后端与索引版本选型由规划者明确；direct reader 有确定性能预算；文件路径通过 P0/P3 rolling-wave 核实。

### DoD

跨 Website/Store/Channel 查询不泄漏；watermark 可证明全量+增量追平；alias CAS 冲突不丢索引；索引故障页面明确 degraded 并走 Product direct read；Search 不可写目录事实；R4 `GATE-P3C` 通过。

### 停止条件

Search 被要求成为事实源、故障返回空成功、alias 切流早于 query/degraded build、无法证明 Scope filter、路径/后端/版本仍开放。

## 6. P3 并行和文件锁

- P3B 与 P3C 只有在 `GATE-P3A` 后并行；
- Inventory/Order/Refund 文件先归 Warehouse，P3A 验收后才交 Tax/其他消费者；
- Checkout/Order/Invoice 共享文件由 Tax 独占窗口；
- Product/Queue/Framework Schema 共享文件由 Search 分片、indexer、query 子包按顺序持锁；
- 三个 MIG 使用独立切换窗口，不与相应 schema/writer 或其他 MIG 并发；
- docs 的 AI-INDEX/README 由每个 Project 的文档整合窗口统一写入。

## 7. `PG-6` 退出

- `GATE-P3A`、`GATE-P3B`、`GATE-P3C` 显式通过；
- 旧 P2 单/ledger/snapshot 不变；
- Warehouse/Tax/Search 各自可 mode off，互不要求整体回滚；
- Tax 无 fail-open，Search 无空成功，多仓/税/索引不变量与 watermark 合格；
- Browser/WLS/HTTP/数据/日志/清理证据完整；
- 未解决 CRITICAL 风险、未分类 mismatch、urgent/dead-letter 均为 0。

**`PG-6` / `GATE-P3 = GO`（2026-07-25）**：P3A/P3B/P3C 均 ACCEPTED；解锁 `PRJ-VENDOR-40`（RT-4）。
