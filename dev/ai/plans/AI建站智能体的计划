E:\WelineFramework\DEV-workspace\app\code\GuoLaiRen\PageBuilder  内部需要完善一个建站智能体（SSE实时控制面板，可以不断修改过程性信息和提示词，持久记住用scope标签可以记住）（PageBuilder 有这个智能体的菜单，PageBuilder需要完全的自己的一个建站智能体），这个智能体是从描述需要，生成站点信息（网站标题，网站主题，网站seo描述，网站内容等等）（提供微调），然后进入域名推荐阶段，这个阶段先选择要购买域名的平台账户，然后可以不断刷新推荐的域名（因为有些域名已被使用，也可调整推荐提示词或者输入域名检测可用性），选择好域名后可以直接购买域名（SSE控制台可以直接看到购买过程并提示购买结果，成功【会自动拉取到本地】则继续下一步【因为域名需要后台准备dns解析等等操作，等待域名可建站状态，所以要友好提示域名购买的状态，每10秒查一下，这个域名的状态在SSE组件的顶部显示域名当前流转阶段】），成功后进入智能体生成网站模板的阶段（目前模板还是文件生成式的和AI生成组件组合的，理论上一个主题也是header和content组件以及footer组件结合而成的，但是我想整个主题的布局json随着AI生成整个主题的时候完整的处理好【需要结合落库的模板主题】，目前的模板都是文件式的。E:\WelineFramework\DEV-workspace\app\code\GuoLaiRen\PageBuilder内的模板支持需要文件模板主题结合虚拟模板的形式，现在需兼容虚拟模板【落库的主题】，主题的形式就一模一样的就行，但是需要设计虚拟模板的架构支持）。

生成虚拟主题后智能体的SSE控制台需要为域名（站点）选择不同的页面类型（首页必选），其他的页面类型则可以选择（默认全选）（勾选后就要生成页面类型对应的主题内容【可以提示词调整，可以点击组件直接修改配置（有AI协助生成：和Ai组件工坊一样的功能）】【这些内容都和第一步填写的站点信息有关】）可以不断微调虚拟主题的其中某个组件，直到可视化虚拟主体调整完成，没有任何问题后，确认进入下一步，等待刚才购买的域名进度，或者可以返回继续调整主题。域名完成可用后，建站（这个和页面构建器内的建站一个逻辑，www和root域名绑定一起建立一个站点，站点名称什么的第一步有，然后是建设页面，最后把页面挂载到网站上），完成后可以显示网站的地址和预览效果（可以点击看效果，和可视化编辑的效果一致）

注意多语言的支持

