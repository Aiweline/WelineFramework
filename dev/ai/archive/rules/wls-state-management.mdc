---
description: WLS 常驻内存下 static 状态管理。新增 static 时必须评估是否注册 StateManager 重置。
globs:
  - "**/*.php"
alwaysApply: true
---

# WLS 状态管理（硬性）

请求级 static 须注册 StateManager 重置；禁止用 static 跨请求传数据且不注册。详见 `.cursor/skills/weline-server/SKILL.md`「WLS 状态管理」。
