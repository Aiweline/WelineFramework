# 任务

完成 WLS POSIX Direct/Dispatcher 统一策略计划的剩余项，验证并强化 TLS 1.3，继续压缩 HTTPS/FPC 热路径和长尾，完成可在当前 macOS 主机执行的性能、稳定性与故障恢复门禁。

约束：使用独立 `codex/wls-tls13-extreme-performance` 分支和专用 `ai-test-*` 实例；不触碰默认 9501；不覆盖主工作区其他智能体改动。
