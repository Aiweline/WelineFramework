# Progress - framework login page performance audit

- 2026-03-24 06:52 Created the task workspace.
- 2026-03-24 15:54 Pulled live backend login response headers and body from port `9982`; baseline headers showed `X-Wls-Performance-Total: 370-423ms`, `router_start: 314-359ms`, `run_after: 45-49ms`, and response bodies around `219KB`.
- 2026-03-24 16:00 Extracted `window.__WELINE_REQUEST_TRACE__` from the live login page and ranked the hottest spans.
- 2026-03-24 16:00 Biggest trace findings:
- `Weline\I18n\Observer\ParserWordsRegister` alone cost about `179ms` in `route_after`.
- `Weline\Framework\View\Template::getFetchFile()` still recompiles templates on every DEV request because the guard started with `DEV || ...`.
- 2026-03-24 16:08 Confirmed the login page also carries the full inline DevTool panel payload (`id="dev-tool-panel"`), which still hurts both backend `run_after` and frontend DOM parse, but deferred that as a second-wave optimization.
- 2026-03-24 16:12 Patched `Template.php` so DEV reuses compiled templates when the source file has not changed, with an escape hatch config `template.force_recompile_in_dev`.
- 2026-03-24 16:14 Patched `ParserWordsRegister.php` so it collects only request-used translated words and only writes cache when there is a real delta, instead of merging the full loaded phrase dictionary every request.
- 2026-03-24 16:18 Added focused unit tests for both changes and reloaded the local WLS runtime with `php bin/w server:reload --no-wait`.
- 2026-03-24 16:21 Re-measured the same live login page after reload:
- `X-Wls-Performance-Total` dropped to about `173.8-177.4ms`
- `X-Wls-Performance-Routerstart` dropped to about `128.4ms`
- repeated hot-request `time_starttransfer` dropped to about `0.193-0.230s`
- updated trace showed `ParserWordsRegister` dropped from about `179ms` to about `57ms`
- 2026-03-24 17:05 Resumed the same task after user clarified that the DeveloperWorkspace DevTool panel should not be removed; instead its cost should be surfaced explicitly in performance accounting.
- 2026-03-24 17:10 Confirmed the accounting gap/root cause:
- `WlsRuntime` only exposed `run_before/url_parser/router_start/run_after`, which made the DevTool panel cost easy to misread as generic `run_after` work.
- the response did not expose a separate developer-tool metric, and `window.__WELINE_REQUEST_TRACE__` did not isolate panel rendering work.
- 2026-03-24 17:20 Added request-trace helpers plus a focused runtime unit test so framework code can sum named spans (`dev_tool_panel`) without coupling to a concrete module implementation.
- 2026-03-24 17:28 Patched `DevToolPanelObserver` to record explicit `dev_tool_panel`, `dev_tool_panel::render_panel`, and `dev_tool_panel::inject_html` spans, and to inject the final request-trace payload after those spans are known.
- 2026-03-24 17:34 Patched `WlsRuntime` to expose the new accounting headers:
- `X-WLS-Performance-PreTelemetryTotal`
- `X-WLS-Performance-Telemetry`
- `X-WLS-Performance-DevTool`
- and to keep `X-WLS-Performance-Total` aligned with the post-telemetry response stage rather than the earlier partial subtotal.
- 2026-03-24 17:40 Follow-up fix: `injectTraceScript()` had to stop treating any plain `window.__WELINE_REQUEST_TRACE__` reference as an already-injected assignment; otherwise moving the trace injection after panel rendering caused the assignment script to disappear.
- 2026-03-24 17:44 Trimmed panel-generated nested spans from the returned trace payload so the page still shows DevTool cost without recursively bloating the inline JSON.
- 2026-03-24 17:50 Live same-request verification on port `9982` showed the new accounting was aligned:
- headers: `Total 326.62ms`, `RunAfter 48.08ms`, `PreTelemetryTotal 325.84ms`, `Telemetry 0.77ms`, `DevTool 47.43ms`, `X-WLS-Process-Time 333.94ms`
- injected trace: `dev_tool_panel 47.43ms`, `dev_tool_panel::render_panel 47.4ms`, `dev_tool_panel::inject_html 0.01ms`
- this confirmed that nearly all of the current `run_after` cost on the login page is the inline DevTool panel, and that the cost is now explicit instead of hidden inside a generic stage bucket.
- 2026-03-24 18:10 Cleaned the stale unreachable branch that remained in `DevToolPanelObserver::execute()` after the new early-return path, then reran syntax checks plus the three focused PHPUnit suites.
- 2026-03-24 18:14 Reloaded WLS again and re-probed the same login URL; the environment had drifted cold/noisy (`Total 4712.82ms`, `Routerstart 4550.46ms`), but the new accounting still held (`RunAfter 84.74ms`, `DevTool 83.43ms`) and the page still carried `dev_tool_panel`, `dev_tool_panel::render_panel`, and `dev_tool_panel::inject_html` in the injected trace.
- 2026-03-24 18:18 Committed the current index as `a6295041` with message `perf: reduce login overhead and expose devtool cost`; note that the commit also picked up pre-existing staged Theme/runtime files that were already in the index before the final commit step, so the final commit scope is broader than the eight performance-targeted files I explicitly added during this pass.
- 2026-03-24 20:18 Resumed the task and validated the pending `Weline_BackendActivity` observer rewrites; both files passed `php -l`.
- 2026-03-24 20:24 Live hot-path reprofile after the BackendActivity skip logic showed the login page had flattened substantially:
- headers sample: `Total 106.66ms`, `Routerstart 97.22ms`, `RunAfter 5.45ms`, `DevTool 5.08ms`
- the injected trace no longer showed `BackendControllerInit` / `BackendControllerRouteAfter` as meaningful hotspots (`0.22ms` / `0.01ms`)
- the new recurring DB hotspot was repeated `m_system_config` reads inside `controller_chain::action_execute`
- 2026-03-24 20:33 Implemented the next framework-level cache slice:
- `Weline\SystemConfig\Model\SystemConfig` now uses request-scope memoization plus persistent cache envelopes, so cached `null`/empty results are treated as real hits
- added module-level `getConfigMapByModule()` bulk lookup
- `Weline_SystemConfig` query provider now exposes `getConfigs`
- `Weline\Backend\Model\Config` now exposes `getConfigs`
- backend login now bulk-loads `Weline_Backend` config once instead of issuing multiple `getConfig()` calls
- 2026-03-24 20:40 Added focused `SystemConfigTest` coverage for cached scalar hits, cached null misses, and cached module-map normalization.
- 2026-03-24 20:46 Re-ran syntax checks for `SystemConfig`, its query provider, backend config wrapper, login controller, and the new unit test; all passed.
- 2026-03-24 20:48 Re-ran focused PHPUnit for `SystemConfigTest`, `ParserWordsRegisterTest`, `SeoUrlGenerateRewriteTest`, and `BackendVerificationCodeGateTest`; all assertions passed, with the same existing PHPUnit deprecation notice from the repo environment.
- 2026-03-24 20:54 Reloaded WLS and warmed the live backend login route again.
- 2026-03-24 20:55 New live probe after the config-cache slice:
- warm run samples: `710.24ms`, `336.33ms`, `51.22ms`, `80.65ms`, `33.35ms`
- best observed hot request: `Total 33.35ms`, `Routerstart 25.36ms`, `RunAfter 4.80ms`, `DevTool 4.42ms`, `X-WLS-Process-Time 36.51ms`
- hottest remaining spans on that best run were `router_start 25.88ms`, `action_execute 19.37ms`, `dev_tool_panel 4.42ms`, and `ProcessUrlBefore 2.18ms`
- the hot trace no longer contained `m_system_config` DB spans; the only remaining visible DB hotspot on that sample was `m_guolairen_page_builder_page` (`1.55ms`) under `ProcessUrlBefore`
