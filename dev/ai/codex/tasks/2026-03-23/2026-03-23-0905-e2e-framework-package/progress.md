# Progress - e2e framework package

- 2026-03-23 09:05 Created the task workspace.
- 2026-03-23 17:xx Read startup context files required by the workspace rules; `SOUL.md`, `USER.md`, and `MEMORY.md` are currently absent, while `memory/2026-03-22.md`, `memory/2026-03-23.md`, and `dev/ai/codex/README.md` exist.
- 2026-03-23 17:xx Routed the task through the Weline skill router and loaded the `testing`, `weline-routing`, and `codex-task-workspace` guidance.
- 2026-03-23 17:xx Audited the existing E2E stack:
- 2026-03-23 17:xx `tests/e2e/collect-tests.js` already auto-discovers shared specs plus module `test/e2e` directories via `modules.json` / fallback scanning.
- 2026-03-23 17:xx `tests/e2e/playwright.config.js` still hardcodes `baseURL: http://127.0.0.1:9981`.
- 2026-03-23 17:xx Multiple specs hardcode stale addresses, backend assumptions, or random backend prefixes instead of deriving them from runtime.
- 2026-03-23 17:xx Confirmed the current live runtime differs from those assumptions: `app/etc/env.php` has dynamic backend prefixes and `var/server/instances/default.json` points to HTTPS WLS on `127.0.0.1:9982`.
- 2026-03-23 17:xx Decided to implement a framework E2E package under `tests/e2e` that resolves runtime info in real time, exposes a stable local proxy entry, and centralizes backend/API/auth helpers.
- 2026-03-23 19:xx Extended `tests/e2e/framework/runtime-info.php` so background jobs no longer depend only on ephemeral `var/server/instances/*.json`; it now falls back to `var/server/config/*.json`, reports reachability, and defaults the proxy host to `127.0.0.1` to avoid Windows `localhost` IPv4/IPv6 drift.
- 2026-03-23 19:xx Updated `tests/e2e/framework/proxy-server.js` to normalize legacy `/?preview_theme=...` requests into the theme preview gateway, fail health checks until the real target is reachable, and auto-start the configured WLS instance when the proxy boots against an unreachable WLS target.
- 2026-03-23 19:xx Updated `tests/e2e/framework/runtime.js` navigation helpers to use commit-first navigation with post-navigation readiness checks, fixed `gotoFrontend()` to honor proxy-disabled/direct-access flows, and relaxed backend login waits toward commit-based navigation.
- 2026-03-23 19:xx Refined `app/code/Weline/Theme/test/e2e/frontend/theme-override.spec.js` to assert stable preview context markers (`frontend_theme_id`, `weline_preview_token`, themed slots/widgets) instead of stale UI-only selectors.
- 2026-03-23 20:xx Verification:
- 2026-03-23 20:xx `php -l tests/e2e/framework/runtime-info.php`
- 2026-03-23 20:xx `node --check tests/e2e/framework/runtime.js`
- 2026-03-23 20:xx `node --check tests/e2e/framework/proxy-server.js`
- 2026-03-23 20:xx `node --check app/code/Weline/Theme/test/e2e/frontend/theme-override.spec.js`
- 2026-03-23 20:xx `npx playwright test theme-override.spec.js --project=chromium --workers=1` from `tests/e2e` now passes cold-start, including automatic WLS bootstrap through the proxy package.
- 2026-03-23 20:xx Additional smoke on `app/code/Weline/Seo/Test/e2e/backend/sitemap-management.spec.js` still reveals module-level residual issues outside the new theme-preview flow: the suite keeps the default 30s per-test timeout in `beforeEach`, backend login remains borderline for repeated runs, and the suite reports noisy JS/public sitemap issues that were not fully normalized in this task.
