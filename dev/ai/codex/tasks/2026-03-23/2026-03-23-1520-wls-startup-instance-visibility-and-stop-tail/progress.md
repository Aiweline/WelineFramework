# Progress - WLS Startup Instance Visibility And Stop Tail

- 2026-03-23 15:20 Created the task workspace.
- 2026-03-24 00:00 Resumed the workspace, renamed the placeholder folder/title, and narrowed this slice to WLS startup visibility plus stop/status stale-index latency.
- 2026-03-24 00:00 Confirmed the current leading hypothesis from the prior slice: historical `name_index` pollution was removed, but freshly started instances can still fail visibility checks even while PID JSON files are present.
- 2026-03-24 02:00 Continued the stop-tail follow-up from the user IPC timeout trace instead of reopening startup concurrency work.
- 2026-03-24 02:00 Confirmed the remaining high-risk stop issue lives in `Stop::sendStopViaIpcAndWait()`:
  - the CLI treated each `fread()` chunk as if it were a complete IPC line
  - fragmented TCP frames could drop the final `Master 即将退出` progress message
  - when that happened, the CLI could keep waiting until the hard timeout even though the Master had already finished the stop flow
- 2026-03-24 02:00 Patched `Stop.php` to:
  - buffer partial IPC frames and only decode complete newline-delimited messages
  - flush the final trailing buffer on socket close
  - track observed stop stage plus `childrenFullyExited`, so the CLI can transition into `waitForMasterExit()` from the final stop stage even if the last explicit exit phrase is not the only signal received
- 2026-03-24 02:00 Added `StopCommandIpcStreamBufferTest` to lock in the fragmented-stream regression and the stage-5-complete fallback behavior.
- 2026-03-24 02:00 Validation:
  - `php -l app/code/Weline/Server/Console/Server/Stop.php`
  - `php -l app/code/Weline/Server/Test/Unit/Console/StopCommandIpcStreamBufferTest.php`
  - `vendor/bin/phpunit.bat --configuration phpunit.xml app/code/Weline/Server/Test/Unit/Console/StopCommandIpcStreamBufferTest.php`
  - `vendor/bin/phpunit.bat --configuration phpunit.xml app/code/Weline/Server/Test/Unit/Console/StopCommandProgressHeuristicTest.php`
- 2026-03-24 02:00 Both PHPUnit runs completed their assertions successfully; the command exit code stayed non-zero only because this repo still emits the existing `No code coverage driver available` warning / deprecation noise.
