# Progress - weshop-payment-checkout-dynamic-methods-slice

- 2026-03-23 07:41 Created the task workspace.
- 2026-03-23 18:xx Recovered task context, re-read workspace/task standards, and synced the latest WeShop handoff state.
- 2026-03-23 18:xx Confirmed the broken checkout path: `PlaceOrder` calls missing `CheckoutService::placeOrder()`.
- 2026-03-23 18:xx Audited `Payment`, `Checkout`, and `default` checkout layouts:
  - `PaymentService` only exposes hardcoded methods and naive provider resolution.
  - `Payment` controllers are empty and there is no payment `QueryProvider`.
  - `default` checkout page already expects `payment_methods`, but checkout layout variants still hardcode placeholder payment sections.
- 2026-03-23 18:xx Locked the current slice plan:
  - add payment registry metadata and `PaymentQueryProvider`
  - add `CheckoutPageDataService` plus real `CheckoutService::placeOrder()`
  - render controller `content` first in all `default` checkout layout variants so page-level dynamic payment UI works
  - keep `WeShop_Theme` and `Weline_Theme` untouched
- 2026-03-23 17:10 Completed the main implementation pass:
  - rebuilt `PaymentService` as a checkout-capable method registry with enabled methods, disabled reserved methods, metadata sorting, and status querying
  - added `PaymentQueryProvider`, `ManualTransfer`, `CashOnDelivery`, and upgraded `PayPal` provider output
  - added real `CheckoutService::placeOrder()` flow plus `CheckoutPageDataService`
  - converted checkout and payment frontend controllers from placeholders into thin JSON/page adapters
  - made `default` checkout page and checkout layout variants render controller-provided `content` before fallback placeholder blocks
- 2026-03-23 17:18 Added focused tests for this slice:
  - `CheckoutPageDataServiceTest`
  - `Payment\Controller\Frontend\Payment\ProcessTest`
  - `Payment\Controller\Frontend\Payment\CallbackTest`
- 2026-03-23 17:26 Re-ran focused verification:
  - route smoke on `9982` confirmed `checkout/frontend/checkout/index`, `checkout/frontend/checkout/place-order`, `payment/frontend/payment/process`, and `payment/frontend/payment/callback` resolve to live controllers
  - targeted PHPUnit assertions passed for `PaymentServiceTest`, `PaymentQueryProviderTest`, `ProcessTest`, `CallbackTest`, `CheckoutServiceTest`, and `CheckoutPageDataServiceTest`
  - PHPUnit still exits with the known repo warning/deprecation noise (`No code coverage driver available`), so raw exit codes remain non-zero even though assertions are green
