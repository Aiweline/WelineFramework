# Progress - ai site workbench e2e and provider lane fix

- 2026-03-23 06:33 Created the task workspace.
- 2026-03-23 15:40 Confirmed the reported `#provider-lane` bug was not a missing DOM anchor. The backend fragment-only link was resolving against the backend base URL and dropping the current `site-builder-agent/index` route, causing navigation to the wrong page.
- 2026-03-23 15:52 Patched the Websites AI workbench hub to generate provider-lane links from the current entry URL instead of plain fragment-only `href="#provider-lane"`.
- 2026-03-23 16:08 Added fake-mode support in `SiteBuilderAgent` so local quick-build flows can simulate domain suggestion, purchase/bootstrap, page generation, virtual theme generation, and visual-edit preview without real registrar/build dependencies.
- 2026-03-23 16:17 Added targeted Playwright coverage plus helper utilities for backend login and workbench URL construction.
- 2026-03-23 16:23 Reproduced and diagnosed a remaining E2E failure: the fake flow finished, but the page-level terminal callback suppressed the visible `done` message, so the spec was asserting against text the user could not actually see.
- 2026-03-23 16:28 Updated the hub page script to surface terminal `done/error` payload messages again while still re-enabling the quick-build button, and tightened the Playwright assertions to the terminal content container instead of the full page body.
- 2026-03-23 16:30 Re-ran syntax checks and the targeted Playwright spec; all three AI site workbench scenarios passed locally.
- 2026-03-23 17:06 Followed through on Playwright integration: `collect-tests.js` now also collects shared `tests/e2e/specs/**/*.spec.js` cases and can auto-detect a module `test/e2e` directory from `base_path` even if `modules.json` metadata is stale.
- 2026-03-23 17:12 Reworked `tests/e2e/playwright.config.js` so default discovery uses the collected file list across both shared specs and module-local specs instead of dropping shared backend specs whenever multiple test directories exist.
- 2026-03-23 17:16 Fixed module-local Playwright dependency resolution by prepending `tests/e2e/node_modules` to `NODE_PATH` inside the config, allowing module specs under `app/code/**/test/e2e` to resolve `@playwright/test`.
- 2026-03-23 17:22 Verified default test discovery with `playwright test --list`, module filtering with `MODULE_FILTER=Weline_Theme`, direct shared-spec execution via `playwright test specs/backend/ai-site-workbench.spec.js`, and the wrapper entrypoint `node start.js specs/backend/ai-site-workbench.spec.js`.
- 2026-03-23 18:57 Resumed the AI workbench E2E follow-up after the local default runtimes drifted. The earlier healthy `9982` WLS runtime was no longer stable, and the remembered `9981` CLI runtime had stale/unresponsive listeners.
- 2026-03-23 19:16 Updated `tests/e2e/framework/runtime.js` so URL helpers respect `PLAYWRIGHT_DISABLE_PROXY`, and `loginAsAdmin()` now derives the real logged-in backend root from `page.url()` instead of returning only the backend prefix.
- 2026-03-23 19:17 Updated `tests/e2e/specs/backend/helpers/ai-workbench.js` so the Websites AI workbench URL reuses that real backend root (`/{backendPrefix}/{currency}/{language}`) when building backend routes.
- 2026-03-23 19:18 Reproduced a remaining flaky failure around the PageBuilder `prepare_visual_edit_stage` provider tool. Network tracing confirmed the backend state transition succeeds, but the spec had been racing the workspace page's asynchronous reload.
- 2026-03-23 19:20 Hardened `tests/e2e/specs/backend/ai-site-workbench.spec.js` by serializing the three AI workbench scenarios and explicitly reloading the workspace after the provider-tool action before asserting the final stage.
- 2026-03-23 19:24 Re-validated the complete AI site workbench flow against a clean local PHP built-in server target on `http://127.0.0.1:9991` using `PLAYWRIGHT_TARGET_ORIGIN=http://127.0.0.1:9991` and `PLAYWRIGHT_DISABLE_PROXY=1`.
- 2026-03-23 19:26 Final wrapper validation passed with `cd tests/e2e && node start.js specs/backend/ai-site-workbench.spec.js` reporting `3 passed` under that clean-runtime override.
- 2026-03-23 20:26 Resumed the last no-env wrapper failure. Confirmed the real blocker was no longer the AI workbench business flow itself, but unstable default runtime selection: stale proxy listeners on `3999`, a drifting WLS target on `9982`, and socket-level reachability that did not guarantee a usable backend login/workbench flow.
- 2026-03-23 20:34 Updated `tests/e2e/framework/runtime.js` so direct-mode backend/API URL helpers use the real runtime prefixes instead of proxy-only `@backend` / `@api` aliases. This restored correct direct-mode admin login and workspace routing on the local PHP runtime.
- 2026-03-23 20:39 Reworked the `prepare_visual_edit_stage` assertion in `tests/e2e/specs/backend/ai-site-workbench.spec.js` to follow the page's real behavior. The workspace template already calls `window.location.reload()` after provider-tool application, so the spec now waits for the stage value instead of racing it with a second manual reload.
- 2026-03-23 20:48 Reworked `tests/e2e/start.js` into a more human-friendly launcher: when no explicit `PLAYWRIGHT_TARGET_ORIGIN` is pinned, it now prefers a reusable local PHP runtime (or starts one), defaults the wrapped run to direct mode, and only keeps the drifting WLS/proxy path when the caller explicitly opts into it.
- 2026-03-23 20:51 Verified the standard wrapper path again with no extra environment overrides: `cd tests/e2e && node start.js specs/backend/ai-site-workbench.spec.js` now passes locally with `3 passed`.
