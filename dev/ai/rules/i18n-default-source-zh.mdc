---
description: 用户可见文案的 __()/国际化默认字符串须以简体中文为源码语言（禁止英文作默认）
alwaysApply: true
---

# 国际化默认文案语言（强制）

- **默认语言**：凡写入 PHP / 模板、面向运维或后台展示的 **`__()`、`MessageManager::*(__('…'))`、`<lang>…</lang>`、Hook 规约 `hook.php` 中的 `name` / `description` 等**，其**括号内默认字符串必须以简体中文书写**，作为源码级主文案。
- **禁止**：用英文作为默认占位文案、再在翻译表里「译成中文」作为主路径；英文等其他语言应通过各语言包（如 `i18n/*.csv`）或框架等价机制提供译文。
- **适用范围**：控制器提示、表单校验、Hook 规约、模块配置中的可读说明等一切「展示给人的句子」；代码标识符、协议名、第三方品牌名等技术专有名词除外。
- **对齐**：与 `dev/ai/rules/QUALITY_SYSTEM_CURSOR_RULES.md` 第 5b 节「国际化默认文案」一致。
