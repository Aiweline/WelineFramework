---
description: 分层(Framework/模块)、按需技能、示例与禁止见 dev/ai；禁止见 global-constraints.md。
alwaysApply: true
---

# 代码生成规则（极简版）

分层：只允许 模块→Framework。按需读技能；禁止项见 `dev/ai/global-constraints.md`。需示例与完整规则时读 `code-generation-standards` 技能。
