---
description: |
  代码生成必须遵循框架技能与下列硬性规则。生成前请阅 AI 提示词.md、AI-常犯错误.md。
alwaysApply: true
---

# 代码生成规则（强制）

## 架构角度级别（生成前必审）

生成代码**必须**从架构分层做判断，再落笔：

- **Framework 层**：只做流程、契约、事件名；不写死任何业务模块 URL/路由/类名；通过事件让上层提供「地址/实现」。
- **模块层**：只做具体实现（观察者通过事件回传 URL、配置等）；不把「是否/何时做」的流程写在模块、让 Framework 只当工具用。
- **依赖方向**：仅允许「模块 → Framework」；扩展用事件 + 观察者，Framework 消费 `data['xxx']`。

详见 **code-generation-standards** 技能「架构角度级别生成代码」章节。

## 必须遵循的技能

代码生成**必须**参考并遵循以下技能（按场景选用）：

- **code-generation-standards** — 模块结构、框架边界、禁止自造方法
- **module-development** — register.php、数据库 schema 声明式(#[Col])、Setup/Install.php、接口签名、路由(setup:upgrade --route)、event.xml(单数)
- **theme-development** ⭐⭐⭐ — **前端/CSS/JS/组件必读！** 主题变量、禁止硬编码颜色、CSS 命名空间、JS 闭包、模板标签
- **i18n-internationalization** — 用户可见文案 `<lang>`/`@lang()`/`__()`，禁止硬编码
- **friendly-notifications** ⭐⭐ — **禁止 alert/confirm/prompt！** 必须使用 BackendToast/BackendConfirm（后台）或 FrontendToast（前台）
- **database-model-standards** — 链式 fetch()、禁止业务层手写 SQL；表结构用 #[Col]/#[Table]、Schema diff（setup:upgrade）
- **create-event** — dispatch 用变量、data 键、触发顺序、event.xml 与 dispatch 对应
- **unified-query-provider** ⭐ — **模块间查询/通信必读！** `w_query()`、`FrameworkQueryService`、禁止为查询创建独立事件
- **debug-logging** — 禁止 error_log()，使用 Env::log_error/log_warning 等
- **documentation-standards** — 仅当用户明确要求或 3 类文档时写文档
- **quality-assurance** — 功能验证、测试、扣分机制
- **php-unit-testing** ⭐ — **测试优先写单元测试！** 测试代码放模块 `test/` 目录，禁止单独创建测试脚本

详细规范与示例均在对应 SKILL.md 中。

## 前端开发强制规范（CSS/JS/组件）

**写 CSS、JS 或前端组件时，必须阅读 theme-development 技能！**

### CSS 强制规范
- ❌ **禁止私自定义颜色**：不得使用 `#fff`、`#333`、`rgb()`、`rgba()` 等硬编码颜色
- ✅ **必须使用主题变量**：`var(--backend-color-card-bg)`、`var(--backend-color-text-primary)` 等
- ❌ **禁止通用类名**：`.card`、`.header`、`.item` 等会污染全局
- ✅ **必须使用组件前缀**：`.weline-media-card`、`.store-form-header` 等

### JS 强制规范
- ❌ **禁止全局变量/函数**：`var myData = []`、`function handleClick()` 等
- ✅ **必须使用 IIFE 闭包**：`(function() { 'use strict'; ... })();`
- ✅ **显式暴露 API**：需要全局访问时用 `window.MyModule = { ... };`

### 组件强制规范
- ✅ **CSS 独立作用域**：所有类名带组件前缀
- ✅ **JS 独立作用域**：所有代码在 IIFE 闭包内
- ✅ **暗色模式兼容**：所有颜色使用 `--backend-color-*` 变量

### 用户提示/通知强制规范 ⭐⭐

**任何需要向用户展示消息、确认、警告的场景，必须使用框架组件！**

- ❌ **绝对禁止**：`alert()`、`confirm()`、`prompt()`、`window.alert`、`window.confirm`、`window.prompt`
- ✅ **后台必须使用**：`BackendToast.success/error/warning/info()`、`BackendConfirm.show()`
- ✅ **前台必须使用**：自定义 `FrontendToast` 或第三方库（参考 friendly-notifications 技能）

```javascript
// ❌ 错误写法
alert('保存成功');
if (confirm('确定删除吗？')) { ... }

// ✅ 正确写法（后台）
BackendToast.success('保存成功');
BackendConfirm.show('确定删除吗？').then(confirmed => { if (confirmed) { ... } });
```

**触发词**：提示、通知、消息、弹窗、确认、toast、alert、confirm、dialog、notification

## 不确定时：查阅再生成（强制）

生成代码时，若因时间久远或上下文不足导致**某处细节不清楚**（如：某技能的具体约定、某 API 的签名、某命名/路径规范等）：

- **必须**先调用对应技能或规则（Read 对应 SKILL.md / 规则文件）查阅清楚，再基于查阅结果重新规划并生成代码。
- **禁止**在不确定的情况下凭记忆或猜测继续写码；可先记住大纲，细节通过工具查阅后再落笔。
- 适用场景示例：记不清事件命名格式 → 读 create-event；记不清 #[Col]/Schema diff/表结构写法 → 读 database-model-standards 或 module-development；记不清模板标签语法 → 读 theme-development；等等。

原则：**宁可先查再写，不可写了再错。**

## 硬性禁止（全局）

- **禁止**编辑 `generated/` 下任何文件；须改源文件后重新生成。
- **禁止**使用 `error_log()`、echo/print 输出错误；须用 `Env::log_error()` 等。
- **禁止**在模板标签内容中使用 PHP 变量或 `<?= ?>`；须用 `{{variable}}` 或先取到值再输出。
- **禁止**在模板 .phtml 中定义全局函数或使用 `function_exists(...)` 打补丁；须用闭包（如 `$renderXxx = function(...) use (&$renderXxx) { ... };`）。详见 theme-development 技能「0.5 模板中禁止定义全局函数」。
- **禁止**在 .phtml 中出现未闭合的 PHP 控制结构；`<?php if (): ?>` 必须成对出现 `<?php endif; ?>`，`<?php foreach (): ?>` 必须对应 `<?php endforeach; ?>`，否则编译后报 `ParseError: unexpected end of file, expecting elseif/else/endif`。详见 theme-development 技能「Phtml PHP 控制结构」。
- **禁止**使用 `routes.xml`；路由为自动扫描，新控制器后执行 `php bin/w setup:upgrade --route`。
- **禁止**在未验证修复成功前更新错误知识库（见 auto-update-skills-on-error.mdc）。
- **禁止**使用原生浏览器弹窗 `alert()`、`confirm()`、`prompt()`；须用 `BackendToast`/`BackendConfirm.show()`/`BackendConfirm.showInput()`（后台）或自定义 FrontendToast（前台）。详见 friendly-notifications 技能。
- **禁止**使用框架不存在的方法（如 `whereIn()`、`orWhere()`、`select([数组])`）；须查阅 database-model-standards 技能确认方法签名。**不确定时先查再用，禁止凭记忆或猜测。**
- **禁止**在 Setup/Upgrade.php 中做字段 CRUD（ADD COLUMN、DROP COLUMN、ALTER TABLE 等）；字段变更由 Model #[Col] 声明 + SchemaDiff 完成；Upgrade 仅处理非字段的业务逻辑（种子数据、数据迁移等）。详见 module-development、database-model-standards。

## 开发前与验证

- 开发前评估并编写需求文档（可放在模块 `doc/` 下）。
- 功能完成后必须验证（单元测试/HTTP 测试/浏览器 MCP 等）；禁止仅修复不验证。
- **测试优先写 PHPUnit 单元测试**，测试代码统一放模块 `test/` 目录，禁止单独创建测试脚本文件。
- 参考：`./AI 提示词.md`、`./开发文档.md`、`./AI-常犯错误.md`、`app/code/Weline/Admin/doc/主题配色变量文档.md`。
