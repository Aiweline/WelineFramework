---
description: |
  计划采用总计划概述+引入子计划；大计划 .cursor/plans/ 写概述并链接子计划，小计划 模块/doc/开发/plan.md 只写本模块变化；进度在模块 plan.md 更新，完成与任务细分在 task.md。计划必须标记状态：🔴未开始 🟡进行中 🟢已完成 🔵测试中
alwaysApply: true
---

# 计划与任务协议（强制）

## 计划状态标记（强制）

**所有计划文件必须在文件头标注当前状态**（YAML frontmatter 或文件首行）：

| 状态 | 标记 | 说明 |
|------|------|------|
| 🔴 未开始 | `status: pending` | 计划已创建，未开始实施 |
| 🟡 进行中 | `status: in_progress` | 正在开发实施 |
| 🔵 测试中 | `status: testing` | 开发完成，测试阶段 |
| 🟢 已完成 | `status: completed` | 测试通过，计划完成 |
| ⚫ 已取消 | `status: cancelled` | 计划取消不再执行 |

**示例（总计划）**：
```markdown
# 消息订阅通知系统

**状态**：🔵 测试中（status: testing）
**当前阶段**：测试阶段（2025-02-28）
**完成度**：95%（7/8 阶段完成）
```

**示例（模块计划）**：
```markdown
# Weline_Backend 消息订阅通知系统 - 模块计划

**状态**：🔵 测试中
```

## 子计划进度汇总表（总计划必须包含）

总计划的「涉及模块及子计划」表必须包含状态列：

```markdown
| 模块 | 子计划 | 任务进度 | 状态 | 完成度 |
|------|--------|---------|------|--------|
| Weline_Backend | [plan.md](...) | [task.md](...) | 🔵 测试中 | 95% |
| Weline_Framework | [plan.md](...) | [task.md](...) | 🟢 已完成 | 100% |
```

## 状态更新触发条件（强制）

- **用户说完成/搞定/做完**：更新对应 task.md 任务状态，评估整体是否完成
- **用户说进入测试**：更新状态为 🔵 测试中
- **用户说计划完成**：更新状态为 🟢 已完成，更新完成时间
- **阶段性完成**：在总计划更新完成度百分比

## 完成计划检查清单（强制）

计划标记为完成前，必须执行 `post-plan-completion-check` 技能检查：
1. 所有 task.md 任务是否标记 [x]
2. 所有测试是否通过
3. 是否有遗漏功能
4. 是否更新相关文档

## 计划文件结构（强制）

- **大计划包含小计划**：总计划写概述并**引入子计划**（链接各模块 doc/开发/plan.md）；小计划只写**本模块涉及的变化**。
- **大/整体计划** → `.cursor/plans/<名称>.plan.md`（总计划概述 + 子计划列表/链接 + 状态汇总表）
- **模块/小计划** → `app/code/<Vendor>/<Module>/doc/开发/plan.md`（仅该模块变更与进度 + 状态标记）
- **进度更新** → 在对应模块 `doc/开发/plan.md` 内更新
- **新增** → 修改对应 plan；**完成** → 在对应模块 `doc/开发/task.md` 标记
- **模块任务细分** → 写在对应模块 `doc/开发/task.md`，认真标记每项进度

**技能**：`.cursor/skills/create-plan/SKILL.md`
