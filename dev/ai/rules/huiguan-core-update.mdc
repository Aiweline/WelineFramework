---
description: 精确口令「回灌」= 授权在框架仓修核心并 update:core 回站点验证；未给框架仓或未说回灌则禁止
alwaysApply: true
---

# 回灌（核心修复验证口令）

权威全文：`dev/ai/global-constraints.md` §7「回灌」；可执行步骤：`dev/ai/skills/CI发布工程师-回灌验证/SKILL.md`。

## 口令

用户说 **「回灌」** ⇒ 明确允许：在**已提供的框架仓**修复本任务所需核心 → 合入/推送 → `php bin/w update:core` 到当前消费站点 → 本地/线上验证。

## 硬闸门

- **未给框架仓路径/可写访问** → 禁止本流程；消费仓只汇报，不改 `Weline/**`。
- **未出现精确口令「回灌」** → 禁止改核、禁止主动回灌（`改核心`/`同步`/`分项` 等近似词不算）。
- 核心源在框架仓；禁止把消费仓 `Weline/**` 当长期修改源。

## 与「分项」

「分项」= 已有 tip 分发到多站点；「回灌」= 修核 + 回当前站验证。二者不可互换。
