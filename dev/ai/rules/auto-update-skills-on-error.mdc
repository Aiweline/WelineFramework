---
description: |
  错误修复后必须更新技能知识库。触发词: error, exception, 报错, 错误, 修复, fixed, bug, 异常
globs:
  - "**/error-tracking/**"
  - "**/error-learning/**"
alwaysApply: false
---

# 错误修复后自动更新技能（强制）

## 触发条件

当以下任一发生时**必须**执行：用户提供错误信息、成功修复代码错误、解决运行时问题、发现框架使用不当。

## 必须执行的步骤

1. **验证修复**（优先单元测试/功能测试/HTTP 测试，最多重试 2 次）。验证失败禁止更新知识库。
2. **调用 `error-learning` 技能**，分析错误类型与根本原因，记录到知识库。
3. **更新 `error-tracking/ERROR_LOG.md`**（完整条目）与 **`COMMON_ERRORS.md`**（快速参考）。模板与格式见 `error-learning`、`error-tracking` 技能。
4. **更新至少 1 个相关技能文档**（按错误类型：路由→weline-routing，控制器→module-development，数据库→database-model-standards，事件→create-event，Hook→create-hook/create-extends，通知→friendly-notifications 等），添加 Q&A 与交叉引用。

## 禁止

- 修复后不更新技能文档
- 不调用 `error-learning` 即更新 ERROR_LOG
- 验证失败时更新知识库

**详细模板与清单见**：`.cursor/skills/error-learning/SKILL.md`、`.cursor/skills/error-tracking/SKILL.md`
