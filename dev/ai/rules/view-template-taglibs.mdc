---
description: .phtml 模板优先使用框架模板标签（Taglib），少用大段原生 PHP
alwaysApply: true
---

# 视图模板：优先标签语法（全局）

与 `dev/ai/global-constraints.md` 第 7 节「必做」及第 8 节「布局文件（layout）约定」一致。layout 与 Hook/widget 模板分工不同，见 `layout-files-convention.mdc`。

## 要求

- 编写或修改 **`*.phtml`** 时，在等价能力下**优先使用项目提供的模板标签**（如 `<notempty>`、`<var>`、`<lang>`、条件/循环类标签、`<w:*>` / `w:` 体系等），而不是习惯性写成 `<?php if ?> … <?= ?>`
- **不要随意**把模板里已有的标签实现改成原生 PHP「以求直观」；除非用户明确要求或标签无法覆盖该场景
- **允许使用 PHP 的典型情况**：文件顶部变量准备、`/** @var */`、标签无法表达的复杂逻辑、或与框架文档明确要求的写法

## 目的

- 与编译/缓存、i18n、主题覆盖链路保持一致，减少重复风格与误改风险
